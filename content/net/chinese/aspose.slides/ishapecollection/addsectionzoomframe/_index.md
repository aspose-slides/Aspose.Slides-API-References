---
title: 添加节缩放框架
second_title: Aspose.Slides for .NET API 参考
description: 将新的节缩放对象添加到集合的末尾。
type: docs
weight: 140
url: /zh/aspose.slides/ishapecollection/addsectionzoomframe/
---

## AddSectionZoomFrame(float, float, float, float, ISection) {#addsectionzoomframe}

将新的节缩放对象添加到集合的末尾。

```csharp
public ISectionZoomFrame AddSectionZoomFrame(float x, float y, float width, float height, 
    ISection section)
```

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| x | Single | 新节缩放框的 X 坐标 Single。 |
| y | Single | 新节缩放框的 Y 坐标 Single。 |
| width | Single | 新节缩放框的宽度 Single。 |
| height | Single | 新节缩放框的高度 Single。 |
| section | ISection | 节缩放框引用的节对象 [`ISection`](../../isection)。 |

### 返回值

创建的节缩放对象 [`ISectionZoomFrame`](../../isectionzoomframe)。

### 异常

| 异常 | 条件 |
| --- | --- |
| ArgumentException | 被引用的节不属于当前演示文稿或不包含任何幻灯片。 |

### 示例

此示例演示如何将节缩放对象添加到集合的末尾（假设 "Presentation.pptx" 演示文稿中至少有两个节）：

```csharp
[C#]
using (Presentation pres = new Presentation("Presentation.pptx"))
{
    ISectionZoomFrame zoomFrame = pres.Slides[0].Shapes.AddSectionZoomFrame(150, 20, 50, 50, pres.Sections[1]);
}
```

### 另请参见

* 接口 [ISectionZoomFrame](../../isectionzoomframe)
* 接口 [ISection](../../isection)
* 接口 [IShapeCollection](../../ishapecollection)
* 命名空间 [Aspose.Slides](../../ishapecollection)
* 程序集 [Aspose.Slides](../../../)

---

## AddSectionZoomFrame(float, float, float, float, ISection, IPPImage) {#addsectionzoomframe_1}

将新的节缩放对象添加到集合的末尾，并具有预定义的图像。

```csharp
public ISectionZoomFrame AddSectionZoomFrame(float x, float y, float width, float height, 
    ISection section, IPPImage image)
```

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| x | Single | 新节缩放框的 X 坐标 Single。 |
| y | Single | 新节缩放框的 Y 坐标 Single。 |
| width | Single | 新节缩放框的宽度 Single。 |
| height | Single | 新节缩放框的高度 Single。 |
| section | ISection | 节缩放框引用的节对象 [`ISection`](../../isection)。 |
| image | IPPImage | 被引用幻灯片的图像 [`IPPImage`](../../ippimage) |

### 返回值

创建的节缩放对象 [`ISectionZoomFrame`](../../isectionzoomframe)。

### 异常

| 异常 | 条件 |
| --- | --- |
| ArgumentException | 被引用的节不属于当前演示文稿或不包含任何幻灯片。 |

### 示例

此示例演示如何将节缩放对象添加到集合的末尾（假设 "Presentation.pptx" 演示文稿中至少有两个节）：

```csharp
[C#]
using (Presentation pres = new Presentation("Presentation.pptx"))
{
    IPPImage image = pres.Images.AddImage(Image.FromFile("image.png"));
    ISectionZoomFrame zoomFrame = pres.Slides[0].Shapes.AddSectionZoomFrame(150, 20, 50, 50, pres.Sections[1], image);
}
```

### 另请参见

* 接口 [ISectionZoomFrame](../../isectionzoomframe)
* 接口 [ISection](../../isection)
* 接口 [IPPImage](../../ippimage)
* 接口 [IShapeCollection](../../ishapecollection)
* 命名空间 [Aspose.Slides](../../ishapecollection)
* 程序集 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->