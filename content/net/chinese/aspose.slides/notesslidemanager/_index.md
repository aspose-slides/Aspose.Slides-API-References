---
title: NotesSlideManager
second_title: Aspose.Sildes .NET API 参考
description: 备注幻灯片管理器。
type: docs
weight: 9160
url: /zh/aspose.slides/notesslidemanager/
---
## NotesSlideManager 类

备注幻灯片管理器。

```csharp
public sealed class NotesSlideManager : DomObject<Slide>, INotesSlideManager
```

## 属性

| 名称 | 描述 |
| --- | --- |
| [NotesSlide](../../aspose.slides/notesslidemanager/notesslide) { get; } | 返回当前幻灯片的备注幻灯片。如果幻灯片没有备注幻灯片，则返回 null。只读 [`INotesSlide`](../inotesslide)。 |

## 方法

| 名称 | 描述 |
| --- | --- |
| [AddNotesSlide](../../aspose.slides/notesslidemanager/addnotesslide)() | 返回当前幻灯片的备注幻灯片，如果不存在则创建一个。 |
| [RemoveNotesSlide](../../aspose.slides/notesslidemanager/removenotesslide)() | 删除当前幻灯片的备注幻灯片。 |

### 示例

以下示例展示了如何向特定的 PowerPoint 演示文稿幻灯片添加备注。

```csharp
[C#]
	// 实例化一个表示演示文稿文件的 Presentation 对象
	using(Presentation presentation = new Presentation(dataDir + "AccessSlides.pptx")) {
	  // 向第一张幻灯片添加备注
	  INotesSlideManager mgr = presentation.Slides[0].NotesSlideManager;
	  INotesSlide noteSlide = mgr.AddNotesSlide();
	  noteSlide.NotesTextFrame.Text = "Your Notes";
	  // 将演示文稿保存到磁盘
	  presentation.Save(dataDir + "RemoveNotesAtSpecificSlide_out.pptx", SaveFormat.Pptx);
	}
```

以下示例展示了如何从 PowerPoint 演示文稿的特定幻灯片中删除备注。

```csharp
[C#]
	// 实例化一个表示演示文稿文件的 Presentation 对象
	using(Presentation presentation = new Presentation(dataDir + "AccessSlides.pptx")) {
	  // 删除第一张幻灯片的备注
	  INotesSlideManager mgr = presentation.Slides[0].NotesSlideManager;
	  mgr.RemoveNotesSlide();
	  // 将演示文稿保存到磁盘
	  presentation.Save(dataDir + "RemoveNotesAtSpecificSlide_out.pptx", SaveFormat.Pptx);
	}
```

### 另请参见

* 类 [DomObject&lt;TParent&gt;](../domobject-1)
* 类 [Slide](../slide)
* 接口 [INotesSlideManager](../inotesslidemanager)
* 命名空间 [Aspose.Slides](../../aspose.slides)
* 程序集 [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->