---
title: 等于
second_title: Aspose.Slides for .NET API 参考
description: 判断两个 IBaseSlide 实例是否相等。返回值的计算基于幻灯片的结构和静态内容。两个幻灯片相等的前提是所有形状、样式、文本、动画以及其他设置等都是相等的。比较时不考虑唯一标识符值，例如 SlideId 和动态内容，例如日期占位符中的当前日期值。
type: docs
weight: 130
url: /zh/aspose.slides/baseslide/equals/
---

## BaseSlide.Equals 方法

判断两个 IBaseSlide 实例是否相等。返回值的计算基于幻灯片的结构和静态内容。两个幻灯片相等的前提是所有形状、样式、文本、动画以及其他设置等都是相等的。比较时不考虑唯一标识符值，例如 SlideId 和动态内容，例如日期占位符中的当前日期值。

```csharp
public bool Equals(IBaseSlide slide)
```

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| slide | IBaseSlide | 要与当前 IBaseSlide 进行比较的 IBaseSlide。 |

### 返回值

如果指定的 IBaseSlide 与当前 IBaseSlide 相等，则返回 **true**；否则返回 **false**。

### 示例

以下示例演示如何比较两个幻灯片。

```csharp
[C#]
using (Presentation presentation1 = new Presentation("AccessSlides.pptx"))
{
	using (Presentation presentation2 = new Presentation("HelloWorld.pptx"))
	{
		for (int i = 0; i < presentation1.Masters.Count; i++)
		{
			for (int j = 0; j < presentation2.Masters.Count; j++)
			{
				if (presentation1.Masters[i].Equals(presentation2.Masters[j]))
					Console.WriteLine(string.Format("SomePresentation1 MasterSlide#{0} 与 SomePresentation2 MasterSlide#{1} 相等", i, j));
			}
		}
	}
}
```

### 另请参阅

* 接口 [IBaseSlide](../../ibaseslide)
* 类 [BaseSlide](../../baseslide)
* 命名空间 [Aspose.Slides](../../baseslide)
* 程序集 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->