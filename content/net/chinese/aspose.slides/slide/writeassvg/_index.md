---
title: WriteAsSvg
second_title: Aspose.Sildes for .NET API 参考
description: 将幻灯片内容保存为SVG文件。
type: docs
weight: 140
url: /zh/aspose.slides/slide/writeassvg/
---

## WriteAsSvg(Stream) {#writeassvg}

将幻灯片内容保存为SVG文件。

```csharp
public void WriteAsSvg(Stream stream)
```

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| stream | Stream | 目标流 |

### 示例

以下代码示例演示如何将PowerPoint演示文稿中的第一张幻灯片转换为SVG文件。

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (Stream fileStream = System.IO.File.Create("slide_1.svg"))
    {
        // 将第一张幻灯片保存为SVG文件
        pres.Slides[0].WriteAsSvg(fileStream);
    }
}
```

### 参见

* 类 [Slide](../../slide)
* 命名空间 [Aspose.Slides](../../slide)
* 程序集 [Aspose.Slides](../../../)

---

## WriteAsSvg(Stream, ISVGOptions) {#writeassvg_1}

将幻灯片内容保存为SVG文件。

```csharp
public void WriteAsSvg(Stream stream, ISVGOptions svgOptions)
```

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| stream | Stream | 目标流 |
| svgOptions | ISVGOptions | SVG生成选项 |

### 示例

以下代码示例演示如何将PowerPoint演示文稿中的第一张幻灯片转换为带选项的SVG文件。

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (Stream fileStream = System.IO.File.Create("slide_1.svg"))
    {
        var options = new SVGOptions() { VectorizeText = true };
        // 将第一张幻灯片保存为SVG文件
        pres.Slides[0].WriteAsSvg(fileStream, options);
    }
}
```

### 参见

* 接口 [ISVGOptions](../../../aspose.slides.export/isvgoptions)
* 类 [Slide](../../slide)
* 命名空间 [Aspose.Slides](../../slide)
* 程序集 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->