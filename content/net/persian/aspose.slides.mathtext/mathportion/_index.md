---
title: MathPortion
second_title: Aspose.Sildes برای .NET – مرجع API
description: یک بخش را که دارای زمینهٔ ریاضی درون‌اش است، نمایندگی می‌کند.
type: docs
weight: 8930
url: /fa/aspose.slides.mathtext/mathportion/
---
## کلاس MathPortion

یک بخش با زمینهٔ ریاضی را نمایندگی می‌کند.

```csharp
public sealed class MathPortion : Portion, IMathPortion
```

## سازندگان

| نام | توضیح |
| --- | --- |
| [MathPortion](mathportion)() | یک نمونه جدید از کلاس MathPortion را مقداردهی اولیه می‌کند. |

## ویژگی‌ها

| نام | توضیح |
| --- | --- |
| [Field](../../aspose.slides/portion/field) { get; } | یک فیلد از این بخش را برمی‌گرداند. فقط-خواندنی [`IField`](../../aspose.slides/ifield). |
| [MathParagraph](../../aspose.slides.mathtext/mathportion/mathparagraph) { get; } | پاراگراف ریاضی |
| [PortionFormat](../../aspose.slides/portion/portionformat) { get; } | یک شیء قالب‌بندی را برمی‌گرداند که شامل ویژگی‌های قالب‌بندی به‌صورت صریح تنظیم‌شدهٔ بخش متن است و هیچ ارث‌بری اعمال نمی‌شود. فقط-خواندنی [`IPortionFormat`](../../aspose.slides/iportionformat). |
| [Text](../../aspose.slides/portion/text) { get; set; } | متن سادهٔ یک بخش را می‌گیرد یا تنظیم می‌کند. قابلیت خواندن/نوشتن رشته. |

## متدها

| نام | توضیح |
| --- | --- |
| [AddField](../../aspose.slides/portion/addfield)(IFieldType) | این بخش را به فیلدی که به‌طور خودکار به‌روزرسانی می‌شود تبدیل می‌کند. |
| [AddField](../../aspose.slides/portion/addfield)(string) | این بخش را به فیلدی که به‌طور خودکار به‌روزرسانی می‌شود تبدیل می‌کند. |
| [GetCoordinates](../../aspose.slides/portion/getcoordinates)() | مختصات آغاز بخش را دریافت کنید. مختصات X نقطه نشان‌دهندهٔ آغاز بخش از اولین کاراکتر به‌همراه حاشیهٔ سمت چپ است. مختصات Y شامل حاشیهٔ بالایی می‌شود. |
| [GetRect](../../aspose.slides/portion/getrect)() | مختصات مستطیلی که بخش را محدود می‌کند دریافت کنید. این مستطیل شامل تمام خطوط متن در بخش، حتی خطوط خالی، است. |
| [RemoveField](../../aspose.slides/portion/removefield)() | این بخش فیلد را به بخش ساده تبدیل می‌کند. |

### مثال‌ها

مثال:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    IAutoShape shape = pres.Slides[0].Shapes.AddMathShape(0, 0, 300, 50);
    IParagraph paragraph = shape.TextFrame.Paragraphs[0];
    MathPortion mathPortion = new MathPortion();
    paragraph.Portions.Add(mathPortion);
}
```

### مراجع

* کلاس [Portion](../../aspose.slides/portion)
* رابط [IMathPortion](../imathportion)
* فضای‌نام [Aspose.Slides.MathText](../../aspose.slides.mathtext)
* مجموعه [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->