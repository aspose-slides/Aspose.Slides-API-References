---
title: MathPortion
second_title: مرجع API Aspose.Sildes برای .NET
description: بخشی را که دارای زمینهٔ ریاضی است، نمایان می‌کند.
type: docs
weight: 8930
url: /fa/aspose.slides.mathtext/mathportion/
---
## کلاس MathPortion

نمایش‌دهندهٔ یک بخش با زمینهٔ ریاضی است.

```csharp
public sealed class MathPortion : Portion, IMathPortion
```

## سازنده‌ها

| نام | توضیح |
| --- | --- |
| [MathPortion](mathportion)() | یک نمونه جدید از کلاس MathPortion را مقداردهی اولیه می‌کند. |

## خصوصیات

| نام | توضیح |
| --- | --- |
| [Field](../../aspose.slides/portion/field) { get; } | یک فیلد از این بخش را برمی‌گرداند. فقط-خواندنی [`IField`](../../aspose.slides/ifield). |
| [MathParagraph](../../aspose.slides.mathtext/mathportion/mathparagraph) { get; } | پاراگراف ریاضی |
| [PortionFormat](../../aspose.slides/portion/portionformat) { get; } | یک شیء قالب‌بندی را برمی‌گرداند که شامل ویژگی‌های قالب‌بندی به‌صورت صریح تنظیم‌شدهٔ بخش متن است که هیچ وراثتی اعمال نمی‌شود. فقط-خواندنی [`IPortionFormat`](../../aspose.slides/iportionformat). |
| [Text](../../aspose.slides/portion/text) { get; set; } | متن سادهٔ یک بخش را دریافت یا تنظیم می‌کند. خواندن/نوشتن رشته. |

## متدها

| نام | توضیح |
| --- | --- |
| [AddField](../../aspose.slides/portion/addfield)(IFieldType) | این بخش را به فیلد به‌طور خودکار به‌روز‌شده تبدیل می‌کند. |
| [AddField](../../aspose.slides/portion/addfield)(string) | این بخش را به فیلد به‌طور خودکار به‌روز‌شده تبدیل می‌کند. |
| [GetCoordinates](../../aspose.slides/portion/getcoordinates)() | مختصات شروع بخش را دریافت می‌کند. مختصات X نقطه نشان‌دهندهٔ شروع بخش از اولین کاراکتر شامل فاصلهٔ سمت چپ است. مختصات Y شامل فاصلهٔ بالا است. |
| [GetRect](../../aspose.slides/portion/getrect)() | مختصات مستطیلی که بخش را محصور می‌کند را دریافت می‌کند. این مستطیل شامل تمام خطوط متن در بخش، از جمله خطوط خالی، است. |
| [RemoveField](../../aspose.slides/portion/removefield)() | این بخش فیلد را به بخش ساده تبدیل می‌کند. |

### مثال‌ها

مثال:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    IAutoShape shape = pres.Slides[0].Shapes.AddMathShape(0, 0, 300, 50);
    IParagraph paragraph = shape.TextFrame.Paragraphs[0];
    MathPortion mathPortion = new MathPortion();
    paragraph.Portions.Add(mathPortion);
}
```

### موارد مرتبط

* کلاس [Portion](../../aspose.slides/portion)
* رابط [IMathPortion](../imathportion)
* فضای‌نام [Aspose.Slides.MathText](../../aspose.slides.mathtext)
* مونتاژ [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->