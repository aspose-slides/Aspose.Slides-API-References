---
title: MathAccent
second_title: Aspose.Sildes برای مرجع API .NET
description: یک لهجهٔ ریاضی ایجاد می‌کند که بر یک عنصر ریاضی مشخص اعمال می‌شود و از مقدار پیش‌فرض کاراکتر لهجه استفاده می‌کند
type: docs
weight: 10
url: /fa/aspose.slides.mathtext/mathaccent/mathaccent/
---
## MathAccent(IMathElement) {#constructor}

یک لهجهٔ ریاضی ایجاد می‌کند که بر یک عنصر ریاضی مشخص اعمال می‌شود و از مقدار پیش‌فرض کاراکتر لهجه استفاده می‌کند

```csharp
public MathAccent(IMathElement element)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| element | IMathElement | یک عنصر ریاضی برای اعمال لهجه |
| accentCharacter | Char | کاراکتر لهجه |

### مثال‌ها

مثال:

```csharp
[C#]
IMathElement baseElement = new MathematicalText("x");
MathAccent accent = new MathAccent(baseElement);
```

### مراجع

* رابط [IMathElement](../../imathelement)
* کلاس [MathAccent](../../mathaccent)
* فضای نام [Aspose.Slides.MathText](../../mathaccent)
* مجموعه [Aspose.Slides](../../../)

---

## MathAccent(IMathElement, char) {#constructor_1}

یک لهجهٔ ریاضی ایجاد می‌کند که بر یک عنصر ریاضی مشخص اعمال می‌شود

```csharp
public MathAccent(IMathElement element, char accentCharacter)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| element | IMathElement | عنصر ریاضی برای اعمال لهجه |
| accentCharacter | Char | کاراکتر لهجه |

### مثال‌ها

مثال:

```csharp
[C#]
IMathElement baseElement = new MathematicalText("x");
MathAccent accent = new MathAccent(baseElement, '~');
```

### مراجع

* رابط [IMathElement](../../imathelement)
* کلاس [MathAccent](../../mathaccent)
* فضای نام [Aspose.Slides.MathText](../../mathaccent)
* مجموعه [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->