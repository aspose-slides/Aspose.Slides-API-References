---
title: PresentationPlayer
second_title: Aspose.Sildes برای .NET مرجع API
description: نمایانگر پخش‌کننده‌ی انیمیشن‌های مرتبط با Presentation../aspose.slides/presentation است.
type: docs
weight: 4390
url: /fa/aspose.slides.export/presentationplayer/
---
## کلاس PresentationPlayer

نمادگذار پخش‌کننده‌ی انیمیشن‌های مرتبط با [`Presentation`](../../aspose.slides/presentation).

```csharp
public class PresentationPlayer : IDisposable
```

## سازنده‌ها

| نام | توضیح |
| --- | --- |
| [PresentationPlayer](presentationplayer)(PresentationAnimationsGenerator, double) | یک نمونه جدید از [`PresentationPlayer`](../presentationplayer) ایجاد می‌کند. |

## ویژگی‌ها

| نام | توضیح |
| --- | --- |
| [FrameIndex](../../aspose.slides.export/presentationplayer/frameindex) { get; } | اندیس فریم را برمی‌گرداند. |

## متدها

| نام | توضیح |
| --- | --- |
| [Dispose](../../aspose.slides.export/presentationplayer/dispose)() | نمونه‌ی [`PresentationPlayer`](../presentationplayer) را از بین می‌برد. |

## سایر اعضا

| نام | توضیح |
| --- | --- |
| delegate [FrameTickHandler](presentationplayer.frametickhandler) | نمایانگر دستگیره‌ی تیک فریم رویداد [`FrameTick`](./frametick) است. |

### مثال‌ها

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(pres))
    {
        // پخش انیمیشن با 33 FPS
        using (var player = new PresentationPlayer(animationsGenerator, 33))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("33fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
        
        // پخش انیمیشن با 45 FPS
        using (var player = new PresentationPlayer(animationsGenerator, 45))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("45fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
    }
}
```

### موارد مرتبط

* فضای‌نام [Aspose.Slides.Export](../../aspose.slides.export)
* مجتمع [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->