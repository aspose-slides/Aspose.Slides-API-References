---
title: PresentationPlayer
second_title: Aspose.Sildes برای .NET مرجع API
description: نماینده پخش‌کننده انیمیشن‌های مرتبط با Presentation../aspose.slides/presentation است.
type: docs
weight: 4390
url: /fa/aspose.slides.export/presentationplayer/
---
## PresentationPlayer کلاس

نمایش‌گر پخش انیمیشن‌های مربوط به [`Presentation`](../../aspose.slides/presentation) است.

```csharp
public class PresentationPlayer : IDisposable
```

## سازنده‌ها

| نام | توضیح |
| --- | --- |
| [PresentationPlayer](presentationplayer)(PresentationAnimationsGenerator, double) | یک نمونه جدید از [`PresentationPlayer`](../presentationplayer) را ایجاد می‌کند. |

## ویژگی‌ها

| نام | توضیح |
| --- | --- |
| [FrameIndex](../../aspose.slides.export/presentationplayer/frameindex) { get; } | شاخص فریم را دریافت می‌کند. |

## متدها

| نام | توضیح |
| --- | --- |
| [Dispose](../../aspose.slides.export/presentationplayer/dispose)() | نمونه [`PresentationPlayer`](../presentationplayer) را از بین می‌برد. |

## اعضای دیگر

| نام | توضیح |
| --- | --- |
| delegate [FrameTickHandler](presentationplayer.frametickhandler) | نمایانگر هندلر تیک فریم برای رویداد [`FrameTick`](./frametick) است. |

### مثال‌ها

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(pres))
    {
        // پخش انیمیشن با 33 فریم بر ثانیه
        using (var player = new PresentationPlayer(animationsGenerator, 33))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("33fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
        
        // پخش انیمیشن با 45 فریم بر ثانیه
        using (var player = new PresentationPlayer(animationsGenerator, 45))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("45fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
    }
}
```

### موارد مرتبط

* namespace [Aspose.Slides.Export](../../aspose.slides.export)
* assembly [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->