---
title: SmartArtLayoutType
second_title: Aspose.Sildes برای .NET مرجع API
description: نوع طرح‌بندی یک نمودار SmartArt را نشان می‌دهد.
type: docs
weight: 10620
url: /fa/aspose.slides.smartart/smartartlayouttype/
---
## SmartArtLayoutType شمارش

نمایانگر نوع طرح‌بندی یک نمودار SmartArt است.

```csharp
public enum SmartArtLayoutType
```

### مقادیر

| نام | مقدار | توضیح |
| --- | --- | --- |
| AccentProcess | `0` | برای نمایش یک پیشرفت، یک خط زمان یا گام‌های متوالی در یک کار، فرایند یا جریان کاری استفاده می‌شود. برای نشان دادن هم متن سطح 1 و هم متن سطح 2 مناسب است. |
| AccentedPicture | `1` | برای نمایش یک ایدهٔ عکسی مرکزی با ایده‌های مرتبط در کنار آن استفاده می‌شود. متن سطح 1 بالای تصویر مرکزی قرار می‌گیرد. متن مربوط به سایر اشکال سطح 1 در کنار تصاویر دایره‌ای کوچک ظاهر می‌شود. این طرح همچنین بدون متن کار می‌کند. |
| AlternatingFlow | `2` | برای نمایش گروه‌های اطلاعات یا گام‌های متوالی در یک کار، فرایند یا جریان کاری استفاده می‌شود. بر تعامل یا روابط بین گروه‌های اطلاعات تأکید می‌کند. |
| AlternatingHexagons | `3` | برای نمایش مجموعه‌ای از ایده‌های به هم پیوسته استفاده می‌شود. متن سطح 1 داخل شش‌ضلعی‌ها ظاهر می‌شود. متن سطح 2 خارج از اشکال قرار می‌گیرد. |
| AlternatingPictureBlocks | `4` | برای نمایش یک سری تصویر از بالا به پایین استفاده می‌شود. متن به‌صورت متناوب در سمت راست یا چپ تصویر قرار می‌گیرد. |
| AlternatingPictureCircles | `5` | برای نمایش مجموعه‌ای از تصاویر با متن استفاده می‌شود. متن مربوطه در دایره‌های مرکزی ظاهر می‌شود و تصاویر به‌صورت متناوب از سمت چپ به راست می‌آیند. |
| ArrowRibbon | `6` | برای نمایش مفاهیم مرتبط یا متضاد با ارتباطی مانند نیروهای مخالف استفاده می‌شود. دو خط اول متن سطح 1 برای متن در پیکان‌ها استفاده می‌شود. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس می‌ماند. |
| AscendingPictureAccentProcess | `7` | برای نمایش یک سری تصویر صعودی با متن توصیفی استفاده می‌شود. برای متن کم مناسب‌ترین گزینه است. |
| Balance | `8` | برای مقایسه یا نمایش رابطه بین دو ایده استفاده می‌شود. هر یک از دو خط اول متن سطح 1 به متن بالای یک سمت نقطهٔ مرکزی مربوط می‌شود. بر متن سطح 2 که به چهار شکل در هر طرف محدود است تأکید می‌کند. تعادل به طرفی که بیشترین شکل‌های حاوی متن سطح 2 را دارد تمایل دارد. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس است. |
| BasicBendingProcess | `9` | برای نمایش یک پیشرفت یا گام‌های متوالی در یک کار، فرایند یا جریان کاری استفاده می‌شود. بیشترین فضای افقی و عمودی برای اشکال را به کار می‌گیرد. |
| BasicBlockList | `10` | برای نمایش بلوک‌های اطلاعات غیرمتوالی یا گروه‌بندی شده استفاده می‌شود. بیشترین فضای افقی و عمودی برای اشکال را به کار می‌گیرد. |
| BasicChevronProcess | `11` | برای نمایش یک پیشرفت؛ یک خط زمان؛ گام‌های متوالی در یک کار، فرایند یا جریان کاری؛ یا برای تأکید بر حرکت یا جهت استفاده می‌شود. متن سطح 1 داخل یک شکل پیکان ظاهر می‌شود در حالی که متن سطح 2 زیر اشکال پیکان قرار می‌گیرد. |
| BasicCycle | `12` | برای نشان دادن یک توالی پیوسته از مراحل، وظایف یا وقایع در یک جریان دایره‌ای استفاده می‌شود. بر مراحل یا گام‌ها به‌جای پیکان‌ها یا جریان تأکید دارد. بهترین حالت استفاده فقط با متن سطح 1 است. |
| BasicMatrix | `13` | برای نمایش رابطهٔ مؤلفه‌ها به یک کل در چهار ربع استفاده می‌شود. چهار خط اول متن سطح 1 در ربع‌ها ظاهر می‌شوند. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس می‌ماند. |
| BasicPie | `14` | برای نمایش چگونگی ترکیب قسمت‌های فردی به یک کل استفاده می‌شود. هفت خط اول متن سطح 1 به شکل‌های برش یا دایره‌ای توزیع شدهٔ یکنواخت مربوط می‌شود. شکل متن سطح 1 بالایی برای تأکید بیرون از بقیهٔ دایره ظاهر می‌شود. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس است. |
| BasicProcess | `15` | برای نمایش یک پیشرفت یا گام‌های متوالی در یک کار، فرایند یا جریان کاری استفاده می‌شود. |
| BasicPyramid | `16` | برای نمایش روابط متناسب، به‌هم‌پیوسته یا سلسله‌مراتبی با بزرگ‌ترین مؤلفه در پایین و کاهش به سمت بالا استفاده می‌شود. متن سطح 1 در بخش‌های هرم ظاهر می‌شود و متن سطح 2 در اشکال کنار هر بخش قرار می‌گیرد. |
| BasicRadial | `17` | برای نمایش رابطه به یک ایدهٔ مرکزی در یک چرخه استفاده می‌شود. خط اول متن سطح 1 به شکل مرکزی مرتبط است و متن سطح 2 آن به شکل‌های دایره‌ای اطراف مربوط می‌شود. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس می‌ماند. |
| BasicTarget | `18` | برای نمایش محصورسازی، درجات یا روابط سلسله‌مراتبی استفاده می‌شود. پنج خط اول متن سطح 1 به یک دایره مربوط می‌شود. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس است. |
| BasicTimeline | `19` | برای نمایش گام‌های متوالی در یک کار، فرایند یا جریان کاری یا برای نشان دادن اطلاعات خط زمان استفاده می‌شود. برای هر دو متن سطح 1 و سطح 2 مناسب است. |
| BasicVenn | `20` | برای نمایش روابط همپوشانی یا به‌هم‌پیوسته استفاده می‌شود. هفت خط اول متن سطح 1 به یک دایره مربوط می‌شود. اگر چهار خط یا کمتر داشته باشید، متن داخل دایره‌ها قرار می‌گیرد؛ اگر بیش از چهار خط باشد، متن خارج از دایره‌ها قرار می‌گیرد. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس می‌ماند. |
| BendingPictureAccentList | `21` | برای نمایش بلوک‌های اطلاعات غیرمتوالی یا گروه‌بندی شده استفاده می‌شود. شکل‌های دایره‌ای کوچک برای نگهداری تصویرها طراحی شده‌اند. برای نشان دادن هر دو متن سطح 1 و سطح 2 مناسب است. بیشترین فضای افقی و عمودی برای اشکال را به کار می‌گیرد. |
| BendingPictureBlocks | `22` | برای نمایش یک سری تصویر استفاده می‌شود. جعبهٔ پایین گوشه می‌تواند مقدار کمی متن را در خود داشته باشد. |
| BendingPictureCaption | `23` | برای نمایش یک سری تصویری متوالی استفاده می‌شود. جعبهٔ پایین گوشه می‌تواند مقدار کمی متن را در خود داشته باشد. |
| BendingPictureCaptionList | `24` | برای نمایش یک سری تصویر استفاده می‌شود. عنوان و توضیح در یک شکل فراخوان زیر هر تصویر ظاهر می‌شود. |
| BendingPictureSemiTransparentText | `25` | برای نمایش یک سری تصویر استفاده می‌شود. یک جعبهٔ نیمه‌شفاف بخش پایین تصویر را می‌پوشاند و تمام سطوح متن را در خود دارد. |
| BlockCycle | `26` | برای نشان دادن توالی پیوسته از مراحل، وظایف یا وقایع در یک جریان دایره‌ای استفاده می‌شود. بر مراحل یا گام‌ها به‌جای پیکان‌ها یا جریان تأکید دارد. |
| BubblePictureList | `27` | برای نمایش یک سری تصویر استفاده می‌شود. می‌تواند تا هشت تصویر سطح 1 را شامل شود. متن و تصاویر استفاده‌نشده نشان داده نمی‌شوند اما در صورت تغییر طرح در دسترس هستند. برای متن‌های کم مناسب است. |
| CaptionedPictures | `28` | برای نمایش تصویرها با سطوح متعدد متن استفاده می‌شود. برای مقدار کم متن سطح 1 و مقدار متوسط متن سطح 2 بهترین است. |
| ChevronList | `29` | برای نمایش پیشرفت از طریق چندین فرایند که یک جریان کاری کلی را تشکیل می‌دهند استفاده می‌شود. همچنین برای نشان دادن فرایندهای متقابل مناسب است. متن سطح 1 به اولین شکل پیکان در سمت چپ مربوط می‌شود، در حالی که متن سطح 2 به زیر-گام‌های افقی برای هر شکلی که متن سطح 1 دارد مربوط می‌شود. |
| CircleAccentTimeline | `30` | برای نمایش یک سری وقایع یا اطلاعات خط زمان استفاده می‌شود. متن سطح 1 در کنار اشکال دایره‌ای بزرگتر ظاهر می‌شود. متن سطح 2 در کنار اشکال دایره‌ای کوچکتر ظاهر می‌شود. |
| CircleArrowProcess | `31` | برای نمایش موارد متوالی با متن پشتیبان برای هر مورد استفاده می‌شود. این نمودار برای متن‌های کم سطح 1 بهترین است. |
| CirclePictureHierarchy | `32` | برای نمایش اطلاعات سلسله‌مراتبی یا روابط گزارش‌دهی در یک سازمان استفاده می‌شود. تصویرها در دایره‌ها قرار می‌گیرند و متن مربوطه در کنار تصویرها ظاهر می‌شود. |
| CircleRelationship | `33` | برای نشان دادن رابطه به یا از یک ایدهٔ مرکزی استفاده می‌شود. متن سطح 2 به‌صورت غیرمتوالی اضافه می‌شود و به پنج مورد محدود است. فقط می‌تواند یک مورد سطح 1 داشته باشد. |
| CircularBendingProcess | `34` | برای نمایش یک توالی طولانی یا غیرخطی از گام‌ها در یک کار، فرایند یا جریان کاری استفاده می‌شود. بهترین حالت استفاده فقط با متن سطح 1 است. بیشترین فضای افقی و عمودی برای اشکال را به کار می‌گیرد. |
| CircularPictureCallout | `35` | برای نمایش یک ایدهٔ مرکزی و زیرایده‌ها یا موارد مرتبط استفاده می‌شود. متن اولین تصویر بخش پایین تصویر را می‌پوشاند. متن مربوط به سایر اشکال سطح 1 در کنار تصویرهای دایره‌ای کوچک ظاهر می‌شود. این نمودار همچنین بدون متن کار می‌کند. |
| ClosedChevronProcess | `36` | برای نمایش پیشرفت، یک خط زمان یا گام‌های متوالی در یک کار، فرایند یا جریان کاری یا برای تأکید بر حرکت یا جهت استفاده می‌شود. می‌تواند برای تأکید بر اطلاعات در شکل آغازین به کار رود. بهترین حالت استفاده فقط با متن سطح 1 است. |
| ContinuousArrowProcess | `37` | برای نمایش خط زمان یا گام‌های متوالی در یک کار، فرایند یا جریان کاری استفاده می‌شود. بهترین حالت استفاده فقط با متن سطح 1 است زیرا هر خط متن سطح 1 داخل شکل پیکان ظاهر می‌شود. متن سطح 2 خارج از شکل پیکان ظاهر می‌شود. |
| ContinuousBlockProcess | `38` | برای نمایش پیشرفت یا گام‌های متوالی در یک کار، فرایند یا جریان کاری استفاده می‌شود. بهترین حالت استفاده با کمترین متن سطح 1 و سطح 2 است. |
| ContinuousCycle | `39` | برای نشان دادن توالی پیوسته از مراحل، وظایف یا وقایع در یک جریان دایره‌ای استفاده می‌شود. بر اتصال همه مؤلفه‌ها تأکید دارد. بهترین حالت استفاده فقط با متن سطح 1 است. |
| ContinuousPictureList | `40` | برای نمایش گروه‌های اطلاعات به‌هم‌پیوسته استفاده می‌شود. شکل‌های دایره‌ای برای نگهداری تصویرها طراحی شده‌اند. |
| ConvergingArrows | `41` | برای نمایش ایده‌ها یا مفاهیمی که به یک نقطهٔ مرکزی می‌گرند استفاده می‌شود. بهترین حالت استفاده فقط با متن سطح 1 است. |
| ConvergingRadial | `42` | برای نمایش رابطهٔ مفاهیم یا مؤلفه‌ها به یک ایدهٔ مرکزی در یک چرخه استفاده می‌شود. خط اول متن سطح 1 به شکل دایره‌ای مرکزی مربوط است و خطوط متن سطح 2 به شکل‌های مستطیلی اطراف مرتبط می‌شوند. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس می‌ماند. |
| CounterbalanceArrows | `43` | برای نمایش دو ایدهٔ مخالف یا مفهومی استفاده می‌شود. هر یک از دو خط اول متن سطح 1 به یک پیکان مربوط می‌شود و با متن سطح 2 خوب کار می‌کند. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس است. |
| CycleMatrix | `44` | برای نمایش رابطه به یک ایدهٔ مرکزی در یک پیشرفت دوری استفاده می‌شود. هر یک از چهار خط اول متن سطح 1 به یک برش یا شکل دایره‌ای مربوط است و متن سطح 2 در یک شکل مستطیلی کنار برش یا دایره ظاهر می‌شود. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس می‌ماند. |
| DescendingBlockList | `45` | برای نمایش گروه‌های ایده‌های مرتبط یا فهرست‌های اطلاعات استفاده می‌شود. اشکال متنی به‌صورت متوالی ارتفاع خود را کاهش می‌دهند و متن سطح 1 به‌صورت عمودی نمایش داده می‌شود. |
| DescendingProcess | `46` | برای نمایش یک سری وقایع نزولی استفاده می‌شود. اولین متن سطح 1 در بالای پیکان و آخرین متن سطح 1 در پایین پیکان نمایش داده می‌شود. فقط هفت مورد اول سطح 1 ظاهر می‌شوند. بهترین حالت استفاده با متن‌های کم تا متوسط است. |
| DetailedProcess | `47` | برای نمایش پیشرفت از طریق سطوح با مقدار زیاد متن سطح 2 استفاده می‌شود. |
| DivergingArrows | `48` | برای نمایش ایده‌ها یا مفاهیمی که از یک منبع مرکزی به بیرون گسترش می‌یابند استفاده می‌شود. بهترین حالت استفاده فقط با متن سطح 1 است. |
| DivergingRadial | `49` | برای نمایش روابط به یک ایدهٔ مرکزی در یک چرخه استفاده می‌شود. خط اول متن سطح 1 به شکل دایره‌ای مرکزی مرتبط است. بر دایره‌های اطراف به‌جای ایدهٔ مرکزی تأکید دارد. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس است. |
| Equation | `50` | برای نمایش گام‌های متوالی یا وظایفی که یک برنامه یا نتیجه را نشان می‌دهند استفاده می‌شود. خط آخر متن سطح 1 پس از علامت مساوی (=) ظاهر می‌شود. بهترین حالت استفاده فقط با متن سطح 1 است. |
| FramedTextPicture | `51` | برای نمایش تصویرها با متن سطح 1 مرتبط که در یک قاب نمایش داده می‌شود استفاده می‌شود. |
| Funnel | `52` | برای نمایش فیلتر کردن اطلاعات یا چگونگی ترکیب بخش‌ها به یک کل استفاده می‌شود. بر نتیجهٔ نهایی تأکید دارد. می‌تواند تا چهار خط متن سطح 1 داشته باشد؛ خط آخر این چهار خط زیر قیف ظاهر می‌شود و سایر خطوط به یک شکل دایره‌ای مربوط می‌شوند. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس است. |
| Gear | `53` | برای نمایش ایده‌های درگیر یک‌دیگر استفاده می‌شود. هر یک از سه خط اول متن سطح 1 به یک شکل دنده مربوط می‌شود و متن‌های سطح 2 متناظر در مستطیل‌های کناری دنده ظاهر می‌شوند. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس است. |
| GridMatrix | `54` | برای نمایش قرارگیری مفاهیم در دو محور استفاده می‌شود. بر مؤلفه‌های فردی به‌جای کل تأکید دارد. چهار خط اول متن سطح 1 در ربع‌ها ظاهر می‌شوند. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس است. |
| GroupedList | `55` | برای نمایش گروه‌ها و زیرگروه‌های اطلاعات یا گام‌ها و زیرگام‌ها در یک کار، فرایند یا جریان کاری استفاده می‌شود. متن سطح 1 به اشکال افقی بالایی سطح بالا مربوط می‌شود و متن سطح 2 به زیرگام‌های عمودی زیر هر شکل سطح بالا مرتبط است. برای تأکید بر زیرگروه‌ها، اطلاعات سلسله‌مراتبی یا فهرست‌های متعدد اطلاعات مناسب است. |
| HalfCircleOrganizationChart | `56` | برای نمایش اطلاعات سلسله‌مراتبی یا روابط گزارش‌دهی در یک سازمان استفاده می‌شود. اشکال دستیار و طرح‌های آویزان نمودار سازمانی با این طرح در دسترس هستند. |
| HexagonCluster | `57` | برای نمایش تصویرها با متن توصیفی مرتبط استفاده می‌شود. شش‌ضلعی‌های کوچک نشانگر جفت تصویر و متن هستند. بهترین حالت استفاده با متن کم است. |
| Hierarchy | `58` | برای نمایش روابط سلسله‌مراتبی که از بالا به پایین پیش می‌روند استفاده می‌شود. |
| HierarchyList | `59` | برای نمایش روابط سلسله‌مراتبی که در گروه‌ها پیش می‌روند استفاده می‌شود. همچنین می‌تواند برای گروه‌بندی یا فهرست‌کردن اطلاعات استفاده شود. |
| HorizontalBulletList | `60` | برای نمایش فهرست‌های اطلاعات غیرمتوالی یا گروه‌بندی شده استفاده می‌شود. برای متن‌های زیاد مناسب است. تمام متن‌ها همان سطح تأکید را دارند و جهتی القا نمی‌شود. |
| HorizontalHierarchy | `61` | برای نمایش روابط سلسله‌مراتبی که افقی پیش می‌روند استفاده می‌شود. برای درخت‌های تصمیم‌گیری مناسب است. |
| HorizontalLabeledHierarchy | `62` | برای نمایش روابط سلسله‌مراتبی که افقی پیش می‌روند و به صورت سلسله‌مراتبی گروه‌بندی شده‌اند استفاده می‌شود. بر عنوان یا متن سطح 1 تأکید دارد. خط اول متن سطح 1 در شکل شروع سلسله‌مراتب ظاهر می‌شود و خطوط دوم و بعدی در بالای مستطیل‌های بلند قرار می‌گیرند. |
| HorizontalMultiLevelHierarchy | `63` | برای نمایش مقدار زیاد اطلاعات سلسله‌مراتبی که افقی پیش می‌روند استفاده می‌شود. بالای سلسله‌مراتب به‌صورت عمودی نمایش داده می‌شود. این طرح از سطوح متعدد در سلسله‌مراتب پشتیبانی می‌کند. |
| HorizontalOrganizationChart | `64` | برای نمایش اطلاعات سلسله‌مراتبی به‌صورت افقی یا روابط گزارش‌دهی در یک سازمان استفاده می‌شود. اشکال دستیار و طرح‌های آویزان نمودار سازمانی با این طرح در دسترس هستند. |
| HorizontalPictureList | `65` | برای نمایش اطلاعات غیرمتوالی یا گروه‌بندی شده با تأکید بر تصویرهای مرتبط استفاده می‌شود. شکل‌های بالایی برای نگهداری تصویرها طراحی شده‌اند. |
| IncreasingArrowsProcess | `66` | برای نمایش گام‌های متوالی و هم‌پوشانی در یک فرایند استفاده می‌شود. به‌پنج مورد سطح 1 محدود است. متن سطح 2 می‌تواند مقدار زیادی داشته باشد. |
| IncreasingCircleProcess | `67` | برای نمایش یک سری گام‌ها که داخل دایره با هر گام بزرگ‌تر می‌شود استفاده می‌شود. به هفت گام سطح 1 محدود است اما تعداد نامحدودی مورد سطح 2 دارد. برای متن‌های زیاد سطح 2 مناسب است. |
| InvertedPyramid | `68` | برای نمایش روابط متناسب، به‌هم‌پیوسته یا سلسله‌مراتبی که بزرگ‌ترین مؤلفه در بالا و به‌تدریج به پایین می‌آید استفاده می‌شود. متن سطح 1 در بخش‌های هرم ظاهر می‌شود و متن سطح 2 در اشکال کنار هر بخش قرار می‌گیرد. |
| LabeledHierarchy | `69` | برای نمایش روابط سلسله‌مراتبی که از بالا به پایین پیش می‌روند و به‌صورت سلسله‌مراتبی گروه‌بندی شده‌اند استفاده می‌شود. بر عنوان یا متن سطح 1 تأکید دارد. خط اول متن سطح 1 در شکل آغاز سلسله‌مراتب ظاهر می‌شود و تمام خطوط بعدی متن سطح 1 در سمت چپ مستطیل‌های بلند قرار می‌گیرند. |
| LinearVenn | `70` | برای نمایش روابط همپوشانی در یک توالی استفاده می‌شود. بهترین حالت استفاده فقط با متن سطح 1 است. |
| LinedList | `71` | برای نمایش مقدار زیاد متن تقسیم‌شده به دسته‌ها و زیردسته‌ها استفاده می‌شود. برای سطوح متعدد متن مناسب است. متن‌های هم‌سطح با خطوط جدا می‌شوند. |
| MultidirectionalCycle | `72` | برای نشان دادن توالی پیوسته‌ای از مراحل، وظایف یا وقایع که می‌تواند در هر جهتی رخ دهد استفاده می‌شود. |
| NameandTitleOrganizationChart | `73` | برای نمایش اطلاعات سلسله‌مراتبی یا روابط گزارش‌دهی در یک سازمان استفاده می‌شود. برای وارد کردن متن در جعبه عنوان، مستقیماً در شکل مستطیل کوچک‌تر تایپ کنید. اشکال دستیار و طرح‌های آویزان نمودار سازمانی با این طرح در دسترس هستند. |
| NestedTarget | `74` | برای نمایش روابط محصورسازی استفاده می‌شود. هر یک از سه خط اول متن سطح 1 به متن بالا-چپ در اشکال مربوط می‌شود و متن سطح 2 به شکل‌های کوچک‌تر مربوط می‌شود. بهترین حالت استفاده با کمترین خطوط متن سطح 2 است. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس است. |
| NondirectionalCycle | `75` | برای نشان دادن توالی پیوسته‌ای از مراحل، وظایف یا وقایع در یک جریان دایره‌ای استفاده می‌شود. هر شکل همان سطح اهمیت را دارد. وقتی نیازی به نشان دادن جهت نیست مناسب است. |
| OpposingArrows | `76` | برای نمایش دو ایدهٔ مخالف یا مفاهیمی که از یک نقطهٔ مرکزی به بیرون می‌ریزند استفاده می‌شود. هر یک از دو خط اول متن سطح 1 به یک پیکان مربوط می‌شود. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس است. |
| OpposingIdeas | `77` | برای نمایش دو ایدهٔ مخالف یا متضاد استفاده می‌شود. می‌تواند یک یا دو مورد سطح 1 داشته باشد. هر متن سطح 1 می‌تواند سطوح زیرمجموعهٔ متعدد داشته باشد. برای متن‌های زیاد مناسب است. |
| OrganizationChart | `78` | برای نمایش اطلاعات سلسله‌مراتبی یا روابط گزارش‌دهی در یک سازمان استفاده می‌شود. اشکال دستیار و طرح‌های آویزان نمودار سازمانی با این طرح در دسترس هستند. |
| PhasedProcess | `79` | برای نمایش سه فاز یک فرایند استفاده می‌شود. به سه مورد سطح 1 محدود است. هر دو مورد اول سطح 1 می‌توانند چهار مورد سطح 2 داشته باشند و مورد سوم می‌تواند تعداد نامحدودی موارد سطح 2 داشته باشد. بهترین حالت استفاده با متن کم است. |
| PictureAccentBlocks | `80` | برای نمایش یک گروه تصویر در بلوک‌ها که از گوشه شروع می‌شود استفاده می‌شود. متن مربوطه به‌صورت عمودی نمایش داده می‌شود. برای تأکید بر عنوان یا زیرعنوان اسلایدها یا برای جداسازی بخش‌های سند مناسب است. |
| PictureAccentList | `81` | برای نمایش اطلاعات گروه‌بندی یا مرتبط استفاده می‌شود. شکل‌های کوچک در گوشه‌های بالا برای نگهداری تصویرها طراحی شده‌اند. بر متن سطح 2 نسبت به سطح 1 تأکید دارد و برای مقدار زیاد متن سطح 2 مناسب است. |
| PictureAccentProcess | `82` | برای نمایش گام‌های متوالی در یک کار، فرایند یا جریان کاری استفاده می‌شود. شکل‌های مستطیلی پس‌زمینه برای نگهداری تصویرها طراحی شده‌اند. |
| PictureCaptionList | `83` | برای نمایش بلوک‌های اطلاعات غیرمتوالی یا گروه‌بندی شده استفاده می‌شود. شکل‌های بالایی برای نگهداری تصویرها طراحی شده‌اند و تصویرها نسبت به متن تأکید دارند. برای تصویرهایی با توضیح کوتاه مناسب است. |
| PictureGrid | `84` | برای نمایش تصویرها روی یک شبکهٔ مربعی استفاده می‌شود. بهترین حالت استفاده با مقدار کمی متن سطح 1 است که بالای تصویر ظاهر می‌شود. |
| PictureLineup | `85` | برای نمایش یک سری تصویر به‌صورت کنار هم استفاده می‌شود. متن سطح 1 بالای تصویر را می‌پوشاند. متن سطح 2 زیر تصویر ظاهر می‌شود. |
| PictureStrips | `86` | برای نمایش یک سری تصویر از بالا به پایین با متن سطح 1 در کنار هر کدام استفاده می‌شود. |
| PieProcess | `87` | برای نمایش گام‌های یک فرایند با هر قطعهٔ دایره‌ای که تا هفت شکل رشد می‌کند استفاده می‌شود. متن سطح 1 به‌صورت عمودی نمایش داده می‌شود. |
| PlusandMinus | `88` | برای نمایش مزایا و معایب دو ایده استفاده می‌شود. هر متن سطح 1 می‌تواند سطوح زیرمجموعهٔ متعدد داشته باشد. برای متن‌های زیاد مناسب است. به دو مورد سطح 1 محدود است. |
| ProcessArrows | `89` | برای نمایش اطلاعات نشان‌دهنده یک فرایند یا جریان کاری استفاده می‌شود. متن سطح 1 در شکل‌های دایره‌ای و متن سطح 2 در شکل‌های پیکان ظاهر می‌شود. بهترین حالت استفاده برای متن کم و برای تأکید بر حرکت یا جهت است. |
| ProcessList | `90` | برای نمایش گروه‌های متعدد اطلاعات یا گام‌ها و زیرگام‌ها در یک کار، فرایند یا جریان کاری استفاده می‌شود. متن سطح 1 به شکل‌های افقی بالایی مربوط می‌شود و متن سطح 2 به زیرگام‌های عمودی زیر هر شکل سطح 1 مرتبط است. |
| PyramidList | `91` | برای نمایش روابط متناسب، به‌هم‌پیوسته یا سلسله‌مراتبی استفاده می‌شود. متن در شکل‌های مستطیلی در بالای پس‌زمینهٔ هرم ظاهر می‌شود. |
| RadialCluster | `92` | برای نمایش داده‌هایی که به یک ایده یا تم مرکزی مرتبط هستند استفاده می‌شود. متن اولین خط سطح 1 در مرکز ظاهر می‌شود. متن سطح 2 در شکل‌های اطراف ظاهر می‌شود. می‌تواند تا هفت شکل سطح 2 داشته باشد. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس است. بهترین حالت استفاده با متن کم است. |
| RadialCycle | `93` | برای نمایش رابطه به یک ایدهٔ مرکزی استفاده می‌شود. بر هر دو اطلاعات در دایرهٔ مرکزی و نحوهٔ مشارکت اطلاعات در حلقهٔ بیرونی دایره‌ها در ایدهٔ مرکزی تأکید دارد. اولین خط متن سطح 1 به دایرهٔ مرکزی مربوط می‌شود و متن سطح 2 آن به حلقهٔ بیرونی دایره‌ها مربوط می‌شود. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس است. |
| RadialList | `94` | برای نمایش روابط به یک ایدهٔ مرکزی در یک چرخه استفاده می‌شود. شکل مرکزی می‌تواند یک تصویر داشته باشد. متن سطح 1 در دایره‌های کوچک‌تر ظاهر می‌شود و هر متن سطح 2 مربوطه در کنار دایره‌های کوچک قرار می‌گیرد. |
| RadialVenn | `95` | برای نمایش همپوشانی روابط و همچنین رابطه به یک ایدهٔ مرکزی در یک چرخه استفاده می‌شود. اولین خط متن سطح 1 به شکل مرکزی مربوط می‌شود و خطوط متن سطح 2 به شکل‌های دایره‌ای اطراف مربوط می‌شوند. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس است. |
| RandomToResultProcess | `96` | برای نشان دادن، از طریق یک سری گام، چگونگی تبدیل چندین ایدهٔ آشوبی به یک هدف یا ایدهٔ یکپارچه استفاده می‌شود. از موارد متعدد متن سطح 1 پشتیبانی می‌کند اما شکل‌های متن سطح 1 اول و آخر ثابت هستند. بهترین حالت استفاده با متن کم سطح 1 و متن متوسط سطح 2 است. |
| RepeatingBendingProcess | `97` | برای نمایش پیشرفت یا گام‌های متوالی در یک کار، فرایند یا جریان کاری استفاده می‌شود. بیشترین فضای افقی و عمودی برای اشکال را به کار می‌گیرد. |
| ReverseList | `98` | برای تغییر بین دو مورد استفاده می‌شود. فقط دو مورد اول متن نمایش داده می‌شوند و هر مورد می‌تواند مقدار زیادی متن داشته باشد. برای نشان دادن تغییر بین دو مورد یا جابجایی در ترتیب مناسب است. |
| SegmentedCycle | `99` | برای نمایش پیشرفت یا توالی مراحل، وظایف یا وقایعی در یک جریان دایره‌ای استفاده می‌شود. بر قطعات به‌هم‌پیوسته تأکید دارد. هر یک از هفت خط اول متن سطح 1 به یک برش یا شکل دایره‌ای مربوط می‌شود. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس است. |
| SegmentedProcess | `100` | برای نمایش پیشرفت یا گام‌های متوالی در یک کار، فرایند یا جریان کاری استفاده می‌شود. بر متن سطح 2 تأکید دارد، زیرا هر خط در یک شکل جداگانه ظاهر می‌شود. |
| SegmentedPyramid | `101` | برای نمایش محصورسازی، نسبت‌مند یا روابط به‌هم‌پیوسته استفاده می‌شود. نه خط اول متن سطح 1 در شکل‌های مثلثی ظاهر می‌شوند. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس است. بهترین حالت استفاده فقط با متن سطح 1 است. |
| SnapshotPictureList | `102` | برای نمایش تصویرها با متن توضیحی استفاده می‌شود. متن سطح 2 می‌تواند فهرست‌های اطلاعاتی را نمایش دهد. برای مقدار زیاد متن مناسب است. |
| SpiralPicture | `103` | برای نمایش حداکثر پنج تصویر با عنوان‌های سطح 1 متناظر که به‌صورت مارپیچی به مرکز می‌پیچند استفاده می‌شود. |
| SquareAccentList | `104` | برای نمایش فهرست‌های اطلاعاتی تقسیم‌شده به دسته‌ها استفاده می‌شود. متن سطح 2 در کنار یک شکل مربع کوچک ظاهر می‌شود. برای مقدار زیاد متن سطح 2 مناسب است. |
| StackedList | `105` | برای نمایش گروه‌های اطلاعات یا گام‌ها در یک کار، فرایند یا جریان کاری استفاده می‌شود. شکل‌های دایره‌ای حاوی متن سطح 1 و مستطیل‌های مربوط حاوی متن سطح 2 هستند. برای جزئیات زیاد و متن کم سطح 1 مناسب است. |
| StackedVenn | `106` | برای نمایش روابط همپوشانی استفاده می‌شود. گزینهٔ خوبی برای تأکید بر رشد یا درجات است. بهترین حالت استفاده فقط با متن سطح 1 است. هفت خط اول متن سطح 1 به یک شکل دایره‌ای مربوط می‌شود. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس است. |
| StaggeredProcess | `107` | برای نمایش پیشرفت نزولی از طریق سطوح استفاده می‌شود. هر یک از پنج خط اول متن سطح 1 به یک مستطیل مربوط می‌شود. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس است. |
| StepDownProcess | `108` | برای نمایش یک فرایند نزولی با گام‌ها و زیرگام‌های متعدد استفاده می‌شود. برای مقدار کم متن مناسب است. |
| StepUpProcess | `109` | برای نمایش یک سری صعودی از گام‌ها یا فهرست‌های اطلاعات استفاده می‌شود. |
| SubStepProcess | `110` | برای نمایش یک فرایند چندگامی با زیرگام‌ها بین هر نمونه متن سطح 1 استفاده می‌شود. برای مقدار کم متن مناسب است و به هفت گام سطح 1 محدود می‌شود. هر گام سطح 1 می‌تواند زیرگام‌های نامحدودی داشته باشد. |
| TableHierarchy | `111` | برای نمایش گروه‌های اطلاعاتی ساخته‌شده از بالا به پایین و سلسله‌مراتب‌ها در هر گروه استفاده می‌شود. این طرح خطوط اتصال ندارد. |
| TableList | `112` | برای نمایش اطلاعات گروه‌بندی‌شده یا مرتبط با ارزش مساوی استفاده می‌شود. اولین خط متن سطح 1 به شکل بالایی مربوط می‌شود و متن سطح 2 برای فهرست‌های بعدی استفاده می‌شود. |
| TargetList | `113` | برای نمایش اطلاعات متقابل یا همپوشانی استفاده می‌شود. هر یک از هفت خط اول متن سطح 1 در شکل مستطیلی ظاهر می‌شود. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس است. برای هر دو متن سطح 1 و سطح 2 مناسب است. |
| TextCycle | `114` | برای نشان دادن توالی پیوسته‌ای از مراحل، وظایف یا وقایع در یک جریان دایره‌ای استفاده می‌شود. بر پیکان‌ها یا جریان به‌جای مراحل یا گام‌ها تأکید دارد. بهترین حالت استفاده فقط با متن سطح 1 است. |
| TitlePictureLineup | `115` | برای نمایش یک سری تصویر که هر کدام عنوان و توضیح خود را دارند استفاده می‌شود. متن سطح 1 در جعبه بالای تصویر ظاهر می‌شود. متن سطح 2 زیر تصویر ظاهر می‌شود. |
| TitledMatrix | `116` | برای نمایش روابط چهار ربع به یک کل استفاده می‌شود. خط اول متن سطح 1 به شکل مرکزی مربوط می‌شود و چهار خط اول متن سطح 2 در ربع‌ها ظاهر می‌شوند. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس است. |
| TitledPictureAccentList | `117` | برای نمایش فهرست‌های اطلاعاتی با یک تصویر تأکیدی برای هر متن سطح 2 استفاده می‌شود. متن سطح 1 در یک جعبهٔ جداگانه در بالای فهرست نمایش داده می‌شود. |
| TitledPictureBlocks | `118` | برای نمایش یک سری تصویر استفاده می‌شود. متن سطح 1 بالای هر تصویر ظاهر می‌شود. متن سطح 2 در کنار و کمی همپوشانی هر تصویر قرار می‌گیرد. |
| TrapezoidList | `119` | برای نمایش اطلاعات گروه‌بندی یا مرتبط با ارزش مساوی استفاده می‌شود. برای مقدار زیاد متن مناسب است. |
| UpwardArrow | `120` | برای نمایش پیشرفت یا گام‌هایی که به‌صورت صعودی در یک کار، فرایند یا جریان کاری روند دارند استفاده می‌شود. هر یک از پنج خط اول متن سطح 1 به یک نقطه از پیکان مربوط می‌شود. بهترین حالت استفاده با متن کم است. متن استفاده‌نشده نشان داده نمی‌شود اما در صورت تغییر طرح در دسترس است. |
| VerticalAccentList | `121` | برای نمایش فهرست‌های اطلاعات استفاده می‌شود. متن سطح 2 در شکل‌های مستطیلی روی شین‌های عمودی ظاهر می‌شود. بر متن سطح 2 نسبت به سطح 1 تأکید دارد و گزینهٔ خوبی برای مقدار متوسط متن سطح 2 است. |
| VerticalArrowList | `122` | برای نمایش پیشرفت یا گام‌های متوالی در یک کار، فرایند یا جریان کاری که به یک هدف مشترک هدایت می‌شوند استفاده می‌شود. برای فهرست‌های بولت‌دار اطلاعات مناسب است. |
| VerticalBendingProcess | `123` | برای نمایش پیشرفت یا گام‌های متوالی در یک کار، فرایند یا جریان کاری استفاده می‌شود. بیشترین فضای افقی و عمودی برای اشکال را به کار می‌گیرد. بر روابط بین اشکال بیشتر از جهت یا حرکت تأکید دارد. |
| VerticalBlockList | `124` | برای نمایش گروه‌های اطلاعات یا گام‌ها در یک کار، فرایند یا جریان کاری استفاده می‌شود. برای مقدار زیاد متن سطح 2 مناسب است. گزینهٔ خوبی برای متن با یک نکته اصلی و زیرنکات متعدد است. |
| VerticalBoxList | `125` | برای نمایش چندین گروه اطلاعات، به‌ویژه گروه‌های با مقدار زیاد متن سطح 2 استفاده می‌شود. گزینهٔ خوبی برای فهرست‌های بولت‌دار است. |
| VerticalBulletList | `126` | برای نمایش بلوک‌های اطلاعات غیرمتوالی یا گروه‌بندی شده استفاده می‌شود. برای فهرست‌های با عناوین طولانی یا اطلاعات سطح بالایی مناسب است. |
| VerticalChevronList | `127` | برای نمایش پیشرفت یا گام‌های متوالی در یک کار، فرایند یا جریان کاری یا برای تأکید بر حرکت یا جهت استفاده می‌شود. بر متن سطح 2 نسبت به سطح 1 تأکید دارد و گزینهٔ خوبی برای مقدار زیاد متن سطح 2 است. |
| VerticalCircleList | `128` | برای نمایش داده‌های متوالی یا گروه‌بندی شده استفاده می‌شود. بهترین حالت استفاده برای متن سطح 1 است که در کنار یک شکل دایره‌ای بزرگ ظاهر می‌شود. سطوح پایینتر متن با شکل‌های دایره‌ای کوچکتر جدا می‌شوند. |
| VerticalCurvedList | `129` | برای نمایش یک فهرست منحنی از اطلاعات استفاده می‌شود. برای افزودن تصویرها به شکل‌های دایره‌ای تأکیدی، پر کردن تصویر را اعمال کنید. |
| VerticalEquation | `130` | برای نمایش گام‌های متوالی یا وظایفی که یک برنامه یا نتیجه را نشان می‌دهند استفاده می‌شود. خط آخر متن سطح 1 پس از علامت پیکان ظاهر می‌شود. بهترین حالت استفاده فقط با متن سطح 1 است. |
| VerticalPictureAccentList | `131` | برای نمایش بلوک‌های اطلاعات غیرمتوالی یا گروه‌بندی شده استفاده می‌شود. دایره‌های کوچک برای نگهداری تصویرها طراحی شده‌اند. |
| VerticalPictureList | `132` | برای نمایش بلوک‌های اطلاعات غیرمتوالی یا گروه‌بندی شده استفاده می‌شود. شکل‌های کوچک در سمت چپ برای نگهداری تصویرها طراحی شده‌اند. |
| VerticalProcess | `133` | برای نمایش پیشرفت یا گام‌های متوالی در یک کار، فرایند یا جریان کاری از بالا به پایین استفاده می‌شود. بهترین حالت استفاده فقط با متن سطح 1 است، زیرا فضای عمودی محدود است. |
| Custom | `134` | نمایانگر یک نمودار SmartArt با الگوی طرح‌بندی سفارشی است |
| PictureOrganizationChart | `135` | برای نمایش اطلاعات سلسله‌مراتبی یا روابط گزارش‌دهی در یک سازمان با تصویرهای مربوطه استفاده می‌شود. اشکال دستیار و طرح‌های آویزان نمودار سازمانی با این طرح در دسترس هستند. |

### مراجع

* namespace [Aspose.Slides.SmartArt](../../aspose.slides.smartart)
* assembly [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->