---
title: SmartArtLayoutType
second_title: Aspose.Sildes برای .NET مستندات API
description: نمایانگر نوع طرح‌بندی یک نمودار SmartArt است.
type: docs
weight: 10620
url: /fa/aspose.slides.smartart/smartartlayouttype/
---
## SmartArtLayoutType enumeration

نمایانگر نوع طرح‌بندی یک نمودار SmartArt است.

```csharp
public enum SmartArtLayoutType
```

### مقادیر

| نام | مقدار | توضیح |
| --- | --- | --- |
| AccentProcess | `0` | از آن برای نمایش پیشرفت، یک خط زمان یا مراحل متوالی در یک تکلیف، فرایند یا جریان کار استفاده می‌شود. برای نمایش هر دو متن سطح 1 و سطح 2 مناسب است. |
| AccentedPicture | `1` | از آن برای نمایش یک ایدهٔ عکسی مرکزی با ایده‌های مرتبط در کنار استفاده می‌شود. متن سطح 1 بالایی روی تصویر مرکزی نمایش داده می‌شود. متن متناظر برای سایر اشکال سطح 1 در کنار تصاویر کوچک دایره‌ای ظاهر می‌شود. این طرح‌بندی حتی بدون متن نیز مناسب است. |
| AlternatingFlow | `2` | از آن برای نمایش گروه‌های اطلاعات یا مراحل متوالی در یک تکلیف، فرایند یا جریان کار استفاده می‌شود. تعامل یا روابط بین گروه‌های اطلاعات را برجسته می‌کند. |
| AlternatingHexagons | `3` | از آن برای نشان دادن مجموعه‌ای از ایده‌های مرتبط استفاده می‌شود. متن سطح 1 در داخل شش‌ضلعی‌ها ظاهر می‌شود. متن سطح 2 در خارج از اشکال قرار می‌گیرد. |
| AlternatingPictureBlocks | `4` | از آن برای نمایش مجموعه‌ای از تصاویر از بالا به پایین استفاده می‌شود. متن به‌صورت متناوب در سمت راست یا چپ تصویر قرار می‌گیرد. |
| AlternatingPictureCircles | `5` | از آن برای نمایش مجموعه‌ای از تصاویر همراه با متن استفاده می‌شود. متن متناظر در دایره‌های مرکزی ظاهر می‌شود و تصاویر به‌صورت چپ به راست متناوب می‌شوند. |
| ArrowRibbon | `6` | از آن برای نمایش مفاهیم مرتبط یا متضاد با برخی ارتباط‌ها، مانند نیروهای مخالف، استفاده می‌شود. دو خط اول متن سطح 1 برای متن در پیکان‌ها استفاده می‌شود. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| AscendingPictureAccentProcess | `7` | از آن برای نمایش سری صعودی از تصاویر با متن توصیفی استفاده می‌شود. بهترین عملکرد را هنگام داشتن مقدار کمی متن دارد. |
| Balance | `8` | از آن برای مقایسه یا نشان دادن رابطه بین دو ایده استفاده می‌شود. هر یک از دو خط اول متن سطح 1 متن بالای یکی از دو سمت نقطهٔ مرکزی را نشان می‌دهد. متن سطح 2 را برجسته می‌کند که به چهار شکل در هر سمت محدود می‌شود. تعادل به سمت طرفی که بیشترین شکل‌های شامل متن سطح 2 را دارد, متمایل می‌شود. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| BasicBendingProcess | `9` | از آن برای نمایش پیشرفت یا مراحل متوالی در یک تکلیف، فرایند یا جریان کار استفاده می‌شود. فضای نمایش افقی و عمودی اشکال را بهینه می‌کند. |
| BasicBlockList | `10` | از آن برای نمایش بلوک‌های اطلاعاتی غیرمتوالی یا گروه‌بندی‌شده استفاده می‌شود. فضای نمایش افقی و عمودی اشکال را بهینه می‌کند. |
| BasicChevronProcess | `11` | از آن برای نمایش پیشرفت؛ یک خط زمان؛ مراحل متوالی در یک تکلیف، فرایند یا جریان کار؛ یا برای تأکید بر حرکت یا جهت استفاده می‌شود. متن سطح 1 در داخل شکل پیکان ظاهر می‌شود در حالی که متن سطح 2 زیر اشکال پیکان قرار می‌گیرد. |
| BasicCycle | `12` | از آن برای نمایاندن یک دنباله مداوم از مراحل، وظایف یا رویدادها در یک جریان دایره‌ای استفاده می‌شود. به جای پیکان‌ها یا جریان، بر مراحل یا گام‌ها تأکید دارد. بهترین عملکرد را هنگام داشتن تنها متن سطح 1 دارد. |
| BasicMatrix | `13` | از آن برای نشان دادن رابطهٔ مؤلفه‌ها با کل در چهارک‌ها استفاده می‌شود. چهار خط اول متن سطح 1 در چهارک‌ها ظاهر می‌شود. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| BasicPie | `14` | از آن برای نشان دادن نحوهٔ ترکیب بخش‌های فردی به یک کل استفاده می‌شود. هفت خط اول متن سطح 1 به بخش‌های مساوی توزیع‌شدهٔ قالب یا دایره پیک مرتبط هستند. شکل متن سطح 1 بالایی برای تأکید، خارج از بقیهٔ دایره قرار می‌گیرد. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| BasicProcess | `15` | از آن برای نمایش پیشرفت یا مراحل متوالی در یک تکلیف، فرایند یا جریان کار استفاده می‌شود. |
| BasicPyramid | `16` | از آن برای نمایش روابط متناسب، مرتبط یا سلسله‌مراتبی با بزرگ‌ترین مؤلفه در پایین و به سمت بالا کاهش‌یاب استفاده می‌شود. متن سطح 1 در بخش‌های هرم ظاهر می‌شود و متن سطح 2 در شکل‌های کنار هر بخش قرار می‌گیرد. |
| BasicRadial | `17` | از آن برای نشان دادن رابطه با یک ایدهٔ مرکزی در یک چرخه استفاده می‌شود. خط اول متن سطح 1 به شکل مرکزی مربوط است و متن سطح 2 آن به اشکال دایره‌ای اطراف مرتبط است. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| BasicTarget | `18` | از آن برای نمایش حاوی بودن، درجات یا روابط سلسله‌مراتبی استفاده می‌شود. پنج خط اول متن سطح 1 با یک دایره مرتبط هستند. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| BasicTimeline | `19` | از آن برای نمایش مراحل متوالی در یک تکلیف، فرایند یا جریان کار یا نمایش اطلاعات خط زمان استفاده می‌شود. برای متن‌های سطح 1 و سطح 2 هر دو مناسب است. |
| BasicVenn | `20` | از آن برای نمایش روابط همپوشانی یا مرتبط استفاده می‌شود. هفت خط اول متن سطح 1 با یک دایره مرتبط است. اگر چهار خط یا کمتر متن سطح 1 باشد، متن داخل دایره‌ها قرار می‌گیرد. اگر بیش از چهار خط متن سطح 1 باشد، متن خارج از دایره‌ها قرار می‌گیرد. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| BendingPictureAccentList | `21` | از آن برای نمایش بلوک‌های اطلاعاتی غیرمتوالی یا گروه‌بندی‌شده استفاده می‌شود. شکل‌های دایره‌ای کوچک برای دربرداشتن تصاویر طراحی شده‌اند. برای نمایش هم متن سطح 1 و سطح 2 مناسب است. فضای نمایش افقی و عمودی اشکال را بهینه می‌کند. |
| BendingPictureBlocks | `22` | از آن برای نمایش مجموعه‌ای از تصاویر استفاده می‌شود. جعبه‌ای که گوشهٔ پایین را می‌پوشاند می‌تواند مقدار کمی متن را در خود داشته باشد. |
| BendingPictureCaption | `23` | از آن برای نمایش یک سلسلهٔ متوالی از تصاویر استفاده می‌شود. جعبه‌ای که گوشهٔ پایین را می‌پوشاند می‌تواند مقدار کمی متن در خود داشته باشد. |
| BendingPictureCaptionList | `24` | از آن برای نمایش مجموعه‌ای از تصاویر استفاده می‌شود. عنوان و توضیح در یک شکل حاشیه‌ای زیر هر تصویر ظاهر می‌شوند. |
| BendingPictureSemiTransparentText | `25` | از آن برای نمایش مجموعه‌ای از تصاویر استفاده می‌شود. جعبه‌ای نیمه‌شفاف بخش پایینی تصویر را می‌پوشاند و تمام سطوح متن را در خود دارد. |
| BlockCycle | `26` | از آن برای نمایاندن دنباله مداوم از مراحل، وظایف یا رویدادها در یک جریان دایره‌ای استفاده می‌شود. به جای پیکان‌های متصل یا جریان، بر مراحل یا گام‌ها تأکید دارد. |
| BubblePictureList | `27` | از آن برای نمایش مجموعه‌ای از تصاویر استفاده می‌شود. می‌تواند تا هشت تصویر سطح 1 را در خود داشته باشد. متن‌ها و تصاویر استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. برای مقدار کمی متن مناسب است. |
| CaptionedPictures | `28` | از آن برای نمایش تصاویر با چندین سطح متن استفاده می‌شود. برای مقدار کمی متن سطح 1 و مقدار متوسط متن سطح 2 مناسب است. |
| ChevronList | `29` | از آن برای نمایش پیشرفت از طریق چندین فرایند که یک جریان کار کلی را تشکیل می‌دهند استفاده می‌شود. همچنین برای نشان دادن فرایندهای متضاد مناسب است. متن سطح 1 مربوط به اولین شکل پیکان در سمت چپ است، در حالی که متن سطح 2 به زیرمراحل افقی برای هر شکلی که متن سطح 1 را دارد، مربوط می‌شود. |
| CircleAccentTimeline | `30` | از آن برای نمایش مجموعه‌ای از رویدادها یا اطلاعات خط زمان استفاده می‌شود. متن سطح 1 در کنار اشکال دایره‌ای بزرگ‌تر ظاهر می‌شود. متن سطح 2 در کنار اشکال دایره‌ای کوچکتر قرار می‌گیرد. |
| CircleArrowProcess | `31` | از آن برای نمایش آیتم‌های متوالی با متن پشتیبان برای هر آیتم استفاده می‌شود. این نمودار برای مقدار کمی متن سطح 1 مناسب است. |
| CirclePictureHierarchy | `32` | از آن برای نمایش اطلاعات سلسله‌مراتبی یا روابط گزارش‌دهی در یک سازمان استفاده می‌شود. تصاویر در دایره‌ها ظاهر می‌شوند و متن متناظر در کنار تصاویر قرار می‌گیرد. |
| CircleRelationship | `33` | از آن برای نشان دادن رابطه با یا از یک ایدهٔ مرکزی استفاده می‌شود. متن سطح 2 به‌صورت غیرمتوالی اضافه می‌شود و به پنج آیتم محدود است. فقط می‌تواند یک آیتم سطح 1 وجود داشته باشد. |
| CircularBendingProcess | `34` | از آن برای نمایش توالی طولانی یا غیرخطی از مراحل در یک تکلیف، فرایند یا جریان کار استفاده می‌شود. بهترین عملکرد را تنها با متن سطح 1 دارد. فضای نمایش افقی و عمودی اشکال را بهینه می‌کند. |
| CircularPictureCallout | `35` | از آن برای نمایش یک ایدهٔ مرکزی و زیرایده‌ها یا آیتم‌های مرتبط استفاده می‌شود. متن اولین تصویر بخش پایینی تصویر را می‌پوشاند. متن متناظر برای سایر اشکال سطح 1 در کنار تصاویر دایره‌ای کوچک ظاهر می‌شود. این نمودار حتی بدون متن نیز مناسب است. |
| ClosedChevronProcess | `36` | از آن برای نمایش پیشرفت، یک خط زمان یا مراحل متوالی در یک تکلیف، فرایند یا جریان کار یا برای تأکید بر حرکت یا جهت استفاده می‌شود. می‌تواند برای برجسته‌سازی اطلاعات در شکل شروع به‌کار رود. بهترین عملکرد را تنها با متن سطح 1 دارد. |
| ContinuousArrowProcess | `37` | از آن برای نمایش خط زمان یا مراحل متوالی در یک تکلیف، فرایند یا جریان کار استفاده می‌شود. بهترین عملکرد را با متن سطح 1 دارد زیرا هر خط از متن سطح 1 در داخل شکل پیکان ظاهر می‌شود. متن سطح 2 در خارج از شکل پیکان قرار می‌گیرد. |
| ContinuousBlockProcess | `38` | از آن برای نمایش پیشرفت یا مراحل متوالی در یک تکلیف، فرایند یا جریان کار استفاده می‌شود. برای متن‌های سطح 1 و سطح 2 حداقل مناسب است. |
| ContinuousCycle | `39` | از آن برای نمایاندن دنباله مداوم از مراحل، وظایف یا رویدادها در یک جریان دایره‌ای استفاده می‌شود. ارتباط بین تمام مؤلفه‌ها را برجسته می‌کند. بهترین عملکرد را تنها با متن سطح 1 دارد. |
| ContinuousPictureList | `40` | از آن برای نمایش گروه‌های اطلاعات مرتبط استفاده می‌شود. اشکال دایره‌ای برای دربرداشتن تصاویر طراحی شده‌اند. |
| ConvergingArrows | `41` | از آن برای نمایش ایده‌ها یا مفاهیمی که به یک نقطهٔ مرکزی می‌جمع شوند استفاده می‌شود. بهترین عملکرد را تنها با متن سطح 1 دارد. |
| ConvergingRadial | `42` | از آن برای نمایش روابط مفاهیم یا مؤلفه‌ها به یک ایدهٔ مرکزی در یک چرخه استفاده می‌شود. خط اول متن سطح 1 به شکل دایره‌ای مرکزی مرتبط است و خطوط متن سطح 2 به اشکال مستطیلی اطراف مربوط می‌شود. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| CounterbalanceArrows | `43` | از آن برای نمایش دو ایده یا مفهوم مخالف استفاده می‌شود. هر یک از دو خط اول متن سطح 1 به یک پیکان مرتبط است و با متن سطح 2 به خوبی کار می‌کند. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| CycleMatrix | `44` | از آن برای نشان دادن رابطه با یک ایدهٔ مرکزی در یک پیشرفت دوره‌ای استفاده می‌شود. هر یک از چهار خط اول متن سطح 1 به یک بخش یا شکل دایره‌ای مرتبط است و متن سطح 2 در یک شکل مستطیلی در کنار آن بخش نمایش داده می‌شود. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| DescendingBlockList | `45` | از آن برای نمایش گروه‌های ایده‌های مرتبط یا فهرست‌های اطلاعاتی استفاده می‌شود. اشکال متن به‌صورت متوالی ارتفاع آن‌ها کاهش می‌یابد و متن سطح 1 به‌صورت عمودی نمایش داده می‌شود. |
| DescendingProcess | `46` | از آن برای نمایش سری نزولی از رویدادها استفاده می‌شود. اولین متن سطح 1 در بالای پیکان قرار دارد و آخرین متن سطح 1 در پایین پیکان نمایش می‌یابد. فقط هفت آیتم اولیه سطح 1 ظاهر می‌شوند. برای مقدار کم تا متوسط متن مناسب است. |
| DetailedProcess | `47` | با مقادیر زیاد متن سطح 2 برای نمایش پیشرفت از طریق مراحل استفاده می‌شود. |
| DivergingArrows | `48` | از آن برای نمایش ایده‌ها یا مفاهیمی که به‌صورت شعاعی از یک منبع مرکزی پیش می‌روند استفاده می‌شود. بهترین عملکرد را تنها با متن سطح 1 دارد. |
| DivergingRadial | `49` | از آن برای نمایش روابط با یک ایدهٔ مرکزی در یک چرخه استفاده می‌شود. اولین خط متن سطح 1 به شکل دایره‌ای مرکزی مرتبط است. به جای ایدهٔ مرکزی، بر دایره‌های اطراف تأکید دارد. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| Equation | `50` | از آن برای نمایش مراحل یا وظایف متوالی که یک برنامه یا نتیجه را نشان می‌دهند استفاده می‌شود. آخرین خط متن سطح 1 پس از علامت مساوی (=) ظاهر می‌شود. بهترین عملکرد را تنها با متن سطح 1 دارد. |
| FramedTextPicture | `51` | از آن برای نمایش تصاویر با متن سطح 1 متناظر که در یک قاب نمایش داده می‌شود استفاده می‌شود. |
| Funnel | `52` | از آن برای نمایش فیلتر کردن اطلاعات یا چگونگی ترکیب بخش‌ها به یک کل استفاده می‌شود. نتایج نهایی را برجسته می‌کند. می‌تواند تا چهار خط متن سطح 1 را در خود داشته باشد؛ آخرین این چهار خط زیر قیف ظاهر می‌شود و خطوط دیگر به یک شکل دایره‌ای مرتبط هستند. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| Gear | `53` | از آن برای نمایش ایده‌های درهم‌تنیده استفاده می‌شود. هر یک از سه خط اول متن سطح 1 به یک شکل چرخ‌دنده مرتبط است و متن سطح 2 متناظر در مستطیل‌های کناری آن شکل قرار می‌گیرد. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| GridMatrix | `54` | از آن برای نمایش جایگاه مفاهیم در امتداد دو محور استفاده می‌شود. به جای کل، بر مؤلفه‌های جداگانه تأکید دارد. چهار خط اول متن سطح 1 در چهارک‌ها ظاهر می‌شود. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| GroupedList | `55` | از آن برای نمایش گروه‌ها و زیرگروه‌های اطلاعات، یا گام‌ها و زیرگام‌ها در یک تکلیف، فرایند یا جریان کار استفاده می‌شود. متن سطح 1 به اشکال افقی سطح بالایی مربوط است و متن سطح 2 به زیرگام‌های عمودی زیر هر شکل سطح بالایی مرتبط می‌شود. برای تأکید بر زیرگروپ‌ها یا زیرگام‌ها، اطلاعات سلسله‌مراتبی یا فهرست‌های متعدد اطلاعاتی مناسب است. |
| HalfCircleOrganizationChart | `56` | از آن برای نمایش اطلاعات سلسله‌مراتبی یا روابط گزارش‌دهی در یک سازمان استفاده می‌شود. شکل‌های کمکی و طرح‌های آویز نمودار سازمانی با این طرح‌بندی در دسترس هستند. |
| HexagonCluster | `57` | از آن برای نمایش تصاویر همراه با متن توصیفی مرتبط استفاده می‌شود. شش‌ضلعی‌های کوچک نشانگر جفت تصویر و متن هستند. برای مقدار کم متن مناسب است. |
| Hierarchy | `58` | از آن برای نمایش روابط سلسله‌مراتبی که از بالا به پایین پیش می‌روند استفاده می‌شود. |
| HierarchyList | `59` | از آن برای نمایش روابط سلسله‌مراتبی که در میان گروه‌ها پیش می‌روند استفاده می‌شود. همچنین می‌تواند برای گروه‌بندی یا فهرست‌کردن اطلاعات به کار رود. |
| HorizontalBulletList | `60` | از آن برای نمایش فهرست‌های اطلاعاتی غیرمتوالی یا گروه‌بندی‌شده استفاده می‌شود. برای مقادیر زیاد متن مناسب است. تمام متن‌ها همان سطح تأکید را دارند و جهت خاصی القا نمی‌شود. |
| HorizontalHierarchy | `61` | از آن برای نمایش روابط سلسله‌مراتبی که به‌صورت افقی پیش می‌روند استفاده می‌شود. برای درخت‌های تصمیم مناسب است. |
| HorizontalLabeledHierarchy | `62` | از آن برای نمایش روابط سلسله‌مراتبی که به‌صورت افقی پیش می‌روند و به صورت سلسله‌مراتبی گروه‌بندی شده‌اند استفاده می‌شود. بر عنوان یا متن سطح 1 تأکید دارد. اولین خط متن سطح 1 در شکلی در ابتدای سلسله‌مراتب ظاهر می‌شود و خط دوم و تمام خطوط بعدی متن سطح 1 در بالای مستطیل‌های بلند قرار می‌گیرند. |
| HorizontalMultiLevelHierarchy | `63` | از آن برای نمایش مقادیر زیاد اطلاعات سلسله‌مراتبی که به‌صورت افقی پیش می‌روند استفاده می‌شود. بالای سلسله‌مراتب به‌صورت عمودی نمایش داده می‌شود. این طرح‌بندی از سطوح متعدد در سلسله‌مراتب پشتیبانی می‌کند. |
| HorizontalOrganizationChart | `64` | از آن برای نمایش اطلاعات سلسله‌مراتبی به‌صورت افقی یا روابط گزارش‌دهی در یک سازمان استفاده می‌شود. شکل کمکی و طرح‌های آویز نمودار سازمانی با این طرح‌بندی در دسترس هستند. |
| HorizontalPictureList | `65` | از آن برای نمایش اطلاعات غیرمتوالی یا گروه‌بندی‌شده با تأکید بر تصاویر مرتبط استفاده می‌شود. اشکال بالایی برای دربرداشتن تصاویر طراحی شده‌اند. |
| IncreasingArrowsProcess | `66` | از آن برای نمایش مراحل یا وظایف متوالی که یک برنامه یا نتیجه را نشان می‌دهند استفاده می‌شود. آخرین خط متن سطح 1 پس از علامت مساوی (=) ظاهر می‌شود. بهترین عملکرد را تنها با متن سطح 1 دارد. |
| IncreasingCircleProcess | `67` | از آن برای نمایش مجموعه‌ای از گام‌ها استفاده می‌شود، که داخل دایره با هر گام افزایش می‌یابد. به هفت گام سطح 1 محدود است اما آیتم‌های سطح 2 نامحدود هستند. برای مقادیر زیاد متن سطح 2 مناسب است. |
| InvertedPyramid | `68` | از آن برای نمایش روابط متناسب، مرتبط یا سلسله‌مراتبی که بزرگ‌ترین مؤلفه در بالا و به‌تدریج باریک می‌شود استفاده می‌شود. متن سطح 1 در بخش‌های هرم ظاهر می‌شود و متن سطح 2 در اشکال کنار هر بخش قرار می‌گیرد. |
| LabeledHierarchy | `69` | از آن برای نمایش روابط سلسله‌مراتبی که از بالا به پایین پیش می‌روند و به‌صورت سلسله‌مراتبی گروه‌بندی شده‌اند استفاده می‌شود. بر عنوان یا متن سطح 1 تأکید دارد. اولین خط متن سطح 1 در شکلی در ابتدای سلسله‌مراتب ظاهر می‌شود و تمام خطوط بعدی متن سطح 1 در سمت چپ مستطیل‌های بلند قرار می‌گیرند. |
| LinearVenn | `70` | از آن برای نمایش روابط همپوشانی در یک توالی استفاده می‌شود. بهترین عملکرد را تنها با متن سطح 1 دارد. |
| LinedList | `71` | از آن برای نمایش مقادیر زیاد متن که به دسته‌ها و زیردسته‌ها تقسیم شده‌اند استفاده می‌شود. برای سطوح متعدد متن مناسب است. متن در همان سطح با خطوط جدا می‌شود. |
| MultidirectionalCycle | `72` | از آن برای نمایاندن دنباله مداوم از مراحل، وظایف یا رویدادهایی که می‌توانند در هر جهتی رخ دهند استفاده می‌شود. |
| NameandTitleOrganizationChart | `73` | از آن برای نمایش اطلاعات سلسله‌مراتبی یا روابط گزارش‌دهی در یک سازمان استفاده می‌شود. برای وارد کردن متن در جعبه عنوان، مستقیماً در شکل مستطیلی کوچک‌تر بنویسید. شکل کمکی و طرح‌های آویز نمودار سازمانی با این طرح‌بندی در دسترس هستند. |
| NestedTarget | `74` | از آن برای نمایش روابط حاوی بودن استفاده می‌شود. هر یک از سه خط اول متن سطح 1 به متن بالا-چپ در اشکال مربوط است و متن سطح 2 به شکل‌های کوچکتر مرتبط است. بهترین عملکرد را با خطوط کم متن سطح 2 دارد. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| NondirectionalCycle | `75` | از آن برای نمایاندن دنباله مداوم از مراحل، وظایف یا رویدادها در یک جریان دایره‌ای استفاده می‌شود. هر شکل همان سطح اهمیت را دارد. وقتی نیازی به نشان دادن جهت نیست مناسب است. |
| OpposingArrows | `76` | از آن برای نمایش دو ایدهٔ مخالف یا ایده‌هایی که از یک نقطهٔ مرکزی جدا می‌شوند استفاده می‌شود. هر یک از دو خط اول متن سطح 1 به یک پیکان مرتبط است. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| OpposingIdeas | `77` | از آن برای نمایش دو ایدهٔ مخالف یا متضاد استفاده می‌شود. می‌تواند یک یا دو آیتم سطح 1 داشته باشد. هر متن سطح 1 می‌تواند سطوح زیرمجموعهٔ متعدد داشته باشد. برای مقدار زیاد متن مناسب است. |
| OrganizationChart | `78` | از آن برای نمایش اطلاعات سلسله‌مراتبی یا روابط گزارش‌دهی در یک سازمان استفاده می‌شود. شکل کمکی و طرح‌های آویز نمودار سازمانی با این طرح‌بندی در دسترس هستند. |
| PhasedProcess | `79` | از آن برای نمایش سه فاز یک فرایند استفاده می‌شود. به سه آیتم سطح 1 محدود است. دو آیتم اول سطح 1 می‌توانند هر کدام چهار آیتم سطح 2 داشته باشند و آیتم سوم سطح 1 می‌تواند تعداد نامحدودی آیتم سطح 2 داشته باشد. برای مقدار کمی متن مناسب است. |
| PictureAccentBlocks | `80` | از آن برای نمایش گروهی از تصاویر در بلوک‌ها از گوشه استفاده می‌شود. متن متناظر به‌صورت عمودی نمایش داده می‌شود. به‌عنوان تأکید بر اسلایدهای عنوان یا زیرعنوان یا برای جداسازی بخش‌های سند مناسب است. |
| PictureAccentList | `81` | از آن برای نمایش اطلاعات گروه‌بندی‌شده یا مرتبط استفاده می‌شود. شکل‌های کوچک در گوشه‌های بالایی برای دربرداشتن تصاویر طراحی شده‌اند. بر متن سطح 2 بیش از سطح 1 تأکید دارد و برای مقدار زیاد متن سطح 2 گزینهٔ خوبی است. |
| PictureAccentProcess | `82` | از آن برای نمایش مراحل متوالی در یک تکلیف، فرایند یا جریان کار استفاده می‌شود. اشکال مستطیلی در پس‌زمینه برای دربرداشتن تصاویر طراحی شده‌اند. |
| PictureCaptionList | `83` | از آن برای نمایش بلوک‌های اطلاعاتی غیرمتوالی یا گروه‌بندی‌شده استفاده می‌شود. اشکال بالایی برای دربرداشتن تصاویر طراحی شده‌اند و تصاویر نسبت به متن برجسته می‌شوند. برای تصاویر با توضیحات کوتاه متنی مناسب است. |
| PictureGrid | `84` | از آن برای نمایش تصاویر روی یک شبکهٔ مربعی استفاده می‌شود. بهترین حالت زمانی است که مقدار کمی متن سطح 1 داشته باشد که بالای تصویر ظاهر می‌شود. |
| PictureLineup | `85` | از آن برای نمایش مجموعه‌ای از تصاویر در کنار یکدیگر استفاده می‌شود. متن سطح 1 بالای تصویر را می‌پوشاند. متن سطح 2 زیر تصویر ظاهر می‌شود. |
| PictureStrips | `86` | از آن برای نمایش مجموعه‌ای از تصاویر از بالا به پایین با متن سطح 1 در کنار هر کدام استفاده می‌شود. |
| PieProcess | `87` | از آن برای نمایش گام‌های یک فرایند با هر بخش دایره‌ای که تا هفت شکل بزرگتر می‌شود استفاده می‌شود. متن سطح 1 به‌صورت عمودی نمایش داده می‌شود. |
| PlusandMinus | `88` | از آن برای نمایش مزایا و معایب دو ایده استفاده می‌شود. هر متن سطح 1 می‌تواند سطوح زیرمجموعهٔ متعدد داشته باشد. برای مقدار زیاد متن مناسب است. به دو آیتم سطح 1 محدود است. |
| ProcessArrows | `89` | از آن برای نمایش اطلاعات نشان‌دهنده یک فرایند یا جریان کار استفاده می‌شود. متن سطح 1 در اشکال دایره‌ای ظاهر می‌شود و متن سطح 2 در اشکال پیکان. برای متن کم مناسب است و برای تأکید بر حرکت یا جهت مفید است. |
| ProcessList | `90` | از آن برای نمایش چندین گروه اطلاعات یا گام‌ها و زیرگام‌ها در یک تکلیف، فرایند یا جریان کار استفاده می‌شود. متن سطح 1 به اشکال افقی بالایی مربوط است و متن سطح 2 به زیرگام‌های عمودی زیر هر شکل سطح بالایی مرتبط می‌شود. |
| PyramidList | `91` | از آن برای نمایش روابط متناسب، مرتبط یا سلسله‌مراتبی استفاده می‌شود. متن در اشکال مستطیلی بالای پس‌زمینهٔ هرم ظاهر می‌شود. |
| RadialCluster | `92` | از آن برای نمایش داده‌هایی که به یک ایده یا تم مرکزی مرتبط هستند استفاده می‌شود. متن سطح 1 بالا در مرکز ظاهر می‌شود. متن سطح 2 در اشکال اطراف نمایش داده می‌شود. می‌تواند تا هفت شکل سطح 2 را در خود داشته باشد. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. برای مقدار کم متن مناسب است. |
| RadialCycle | `93` | از آن برای نمایش رابطه با یک ایدهٔ مرکزی استفاده می‌شود. هم اطلاعات در دایرهٔ مرکزی و هم نحوهٔ مشارکت اطلاعات در حلقهٔ بیرونی دایره‌ها در ایدهٔ مرکزی برجسته می‌شود. اولین خط متن سطح 1 به دایرهٔ مرکزی مرتبط است و متن سطح 2 آن به حلقهٔ بیرونی دایره‌ها مربوط می‌شود. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| RadialList | `94` | از آن برای نمایش روابط با یک ایدهٔ مرکزی در یک چرخه استفاده می‌شود. شکل مرکزی می‌تواند شامل یک تصویر باشد. متن سطح 1 در دایره‌های کوچکتر ظاهر می‌شود و هر متن سطح 2 مرتبط در کنار دایره‌های کوچکتر قرار می‌گیرد. |
| RadialVenn | `95` | از آن برای نمایش هم روابط همپوشانی و هم رابطه با یک ایدهٔ مرکزی در یک چرخه استفاده می‌شود. اولین خط متن سطح 1 به شکل مرکزی مرتبط است و خطوط متن سطح 2 به اشکال دایره‌ای اطراف مربوط می‌شود. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| RandomToResultProcess | `96` | از آن برای نشان دادن این که چگونه چند ایدهٔ آشوب‌مانند می‌توانند از طریق مجموعه‌ای از گام‌ها به یک هدف یا ایدهٔ یکپارچه منجر شوند استفاده می‌شود. از چندین آیتم متن سطح 1 پشتیبانی می‌کند، اما شکل‌های متناظر اولین و آخرین متن سطح 1 ثابت هستند. برای مقدار کم متن سطح 1 و مقدار متوسط متن سطح 2 مناسب است. |
| RepeatingBendingProcess | `97` | از آن برای نمایش پیشرفت یا گام‌های متوالی در یک تکلیف، فرایند یا جریان کار استفاده می‌شود. فضای نمایش افقی و عمودی اشکال را بهینه می‌کند. |
| ReverseList | `98` | از آن برای جابجایی بین دو آیتم استفاده می‌شود. تنها دو آیتم اول متن نمایش داده می‌شوند و هر آیتم می‌تواند مقدار زیادی متن داشته باشد. برای نشان دادن تغییر بین دو آیتم یا جابه‌جایی در ترتیب مناسب است. |
| SegmentedCycle | `99` | از آن برای نمایش پیشرفت یا یک توالی از مراحل، وظایف یا رویدادها در یک جریان دایره‌ای استفاده می‌شود. قطعات مرتبط را برجسته می‌کند. هر یک از هفت خط اول متن سطح 1 به یک بخش یا شکل دایره‌ای مرتبط است. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| SegmentedProcess | `100` | از آن برای نمایش پیشرفت یا مراحل متوالی در یک تکلیف، فرایند یا جریان کار استفاده می‌شود. متن سطح 2 را برجسته می‌کند، زیرا هر خط در یک شکل جداگانه ظاهر می‌شود. |
| SegmentedPyramid | `101` | از آن برای نمایش روابط حاوی بودن، نسبت‌دار یا مرتبط استفاده می‌شود. نه خط اول متن سطح 1 در اشکال مثلثی ظاهر می‌شوند. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. بهترین عملکرد را تنها با متن سطح 1 دارد. |
| SnapshotPictureList | `102` | از آن برای نمایش تصاویر با متن توضیحی استفاده می‌شود. متن سطح 2 می‌تواند فهرست‌های اطلاعاتی را نمایش دهد. برای مقدار زیاد متن مناسب است. |
| SpiralPicture | `103` | از آن برای نمایش مجموعه‌ای تا پنج تصویر با کپشن‌های متن سطح 1 که به‌صورت مارپیچ به مرکز می‌پیچند استفاده می‌شود. |
| SquareAccentList | `104` | از آن برای نمایش فهرست‌های اطلاعاتی تقسیم‌شده به دسته‌ها استفاده می‌شود. متن سطح 2 در کنار یک شکل مربع کوچک ظاهر می‌شود. برای مقدار زیاد متن سطح 2 مناسب است. |
| StackedList | `105` | از آن برای نمایش گروه‌های اطلاعاتی یا گام‌ها در یک تکلیف، فرایند یا جریان کار استفاده می‌شود. اشکال دایره‌ای شامل متن سطح 1 هستند و مستطیل‌های متناظر شامل متن سطح 2. برای جزئیات زیاد و متن سطح 1 کم مناسب است. |
| StackedVenn | `106` | از آن برای نمایش روابط همپوشانی استفاده می‌شود. گزینهٔ خوبی برای برجسته‌سازی رشد یا تدریج است. بهترین عملکرد را تنها با متن سطح 1 دارد. هفت خط اول متن سطح 1 به یک شکل دایره‌ای مرتبط است. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| StaggeredProcess | `107` | از آن برای نمایش پیشرفت نزولی در مراحل استفاده می‌شود. هر یک از پنج خط اول متن سطح 1 به یک مستطیل مرتبط است. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| StepDownProcess | `108` | از آن برای نمایش یک فرایند نزولی با چندین گام و زیرگام استفاده می‌شود. برای مقدار کم متن مناسب است. |
| StepUpProcess | `109` | از آن برای نمایش یک سری صعودی از گام‌ها یا فهرست‌های اطلاعاتی استفاده می‌شود. |
| SubStepProcess | `110` | از آن برای نمایش یک فرایند چندگامی با زیرگام‌ها بین هر نمونهٔ متن سطح 1 استفاده می‌شود. برای مقدار کم متن مناسب است و به هفت گام سطح 1 محدود می‌شود. هر گام سطح 1 می‌تواند زیرگام‌های نامحدودی داشته باشد. |
| TableHierarchy | `111` | از آن برای نمایش گروه‌های اطلاعاتی ساخته‌شده از بالا به پایین و سلسله‌مراتب‌های داخل هر گروه استفاده می‌شود. این طرح‌بندی خطوط اتصال‌دار ندارد. |
| TableList | `112` | از آن برای نمایش اطلاعات گروه‌بندی‌شده یا مرتبط با ارزش برابر استفاده می‌شود. اولین خط متن سطح 1 به شکل بالایی مرتبط است و متن سطح 2 برای فهرست‌های بعدی استفاده می‌شود. |
| TargetList | `113` | از آن برای نمایش اطلاعات مرتبط یا همپوشانی استفاده می‌شود. هر یک از هفت خط اول متن سطح 1 در شکل مستطیلی ظاهر می‌شود. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. برای هر دو متن سطح 1 و سطح 2 مناسب است. |
| TextCycle | `114` | از آن برای نمایش دنباله مداوم از مراحل، وظایف یا رویدادها در یک جریان دایره‌ای استفاده می‌شود. بر پیکان‌ها یا جریان بیشتر از مراحل یا گام‌ها تأکید دارد. بهترین عملکرد را تنها با متن سطح 1 دارد. |
| TitlePictureLineup | `115` | از آن برای نمایش مجموعه‌ای از تصاویر که هر کدام عنوان و توضیح خود را دارند استفاده می‌شود. متن سطح 1 در جعبه بالای تصویر ظاهر می‌شود. متن سطح 2 زیر تصویر نمایش داده می‌شود. |
| TitledMatrix | `116` | از آن برای نمایش روابط چهارربع به یک کل استفاده می‌شود. اولین خط متن سطح 1 به شکل مرکزی مرتبط است و چهار خط اول متن سطح 2 در چهارک‌ها ظاهر می‌شود. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| TitledPictureAccentList | `117` | از آن برای نمایش فهرست‌های اطلاعاتی با یک تصویر تأکید برای هر متن سطح 2 استفاده می‌شود. متن سطح 1 در یک جعبه جداگانه در بالای فهرست نمایش داده می‌شود. |
| TitledPictureBlocks | `118` | از آن برای نمایش مجموعه‌ای از تصاویر استفاده می‌شود. متن سطح 1 بالای هر تصویر ظاهر می‌شود. متن سطح 2 در کنار و کمی روی هر تصویر قرار می‌گیرد. |
| TrapezoidList | `119` | از آن برای نمایش اطلاعات گروه‌بندی‌شده یا مرتبط با ارزش برابر استفاده می‌شود. برای مقدار زیاد متن مناسب است. |
| UpwardArrow | `120` | از آن برای نمایش پیشرفت یا گام‌هایی که به سمت بالا متمایل هستند در یک تکلیف، فرایند یا جریان کار استفاده می‌شود. هر یک از پنج خط اول متن سطح 1 به یک نقطه از پیکان مرتبط است. بهترین عملکرد را با متن کم دارد. متن‌های استفاده نشده نمایش داده نمی‌شوند، اما در صورت تغییر طرح‌بندی در دسترس می‌مانند. |
| VerticalAccentList | `121` | از آن برای نمایش فهرست‌های اطلاعاتی استفاده می‌شود. متن سطح 2 در اشکال مستطیلی روی شِور‌های عمودی ظاهر می‌شود. بر متن سطح 2 بیش از سطح 1 تأکید دارد و برای مقدار متوسط متن سطح 2 گزینهٔ خوبی است. |
| VerticalArrowList | `122` | از آن برای نمایش پیشرفت یا گام‌های متوالی در یک تکلیف، فرایند یا جریان کار که به هدف مشترک حرکت می‌کنند استفاده می‌شود. برای فهرست‌های بولت‌دار اطلاعات مناسب است. |
| VerticalBendingProcess | `123` | از آن برای نمایش پیشرفت یا گام‌های متوالی در یک تکلیف، فرایند یا جریان کار استفاده می‌شود. فضای نمایش افقی و عمودی اشکال را بهینه می‌کند. بر روابط بین اشکال بیشتر از جهت یا حرکت تأکید دارد. |
| VerticalBlockList | `124` | از آن برای نمایش گروه‌های اطلاعاتی یا گام‌ها در یک تکلیف، فرایند یا جریان کار استفاده می‌شود. برای مقدار زیاد متن سطح 2 مناسب است. گزینهٔ خوبی برای متنی با نکتهٔ اصلی و زیرنکات متعدد است. |
| VerticalBoxList | `125` | از آن برای نمایش چندین گروه اطلاعاتی، به‌ویژه گروه‌هایی با مقدار زیاد متن سطح 2 استفاده می‌شود. گزینهٔ خوبی برای فهرست‌های بولت‌دار اطلاعات است. |
| VerticalBulletList | `126` | از آن برای نمایش بلوک‌های اطلاعاتی غیرمتوالی یا گروه‌بندی‌شده استفاده می‌شود. برای فهرست‌هایی با عنوان‌های طولانی یا اطلاعات سطح بالایی مناسب است. |
| VerticalChevronList | `127` | از آن برای نمایش پیشرفت یا گام‌های متوالی در یک تکلیف، فرایند یا جریان کار یا برای تأکید بر حرکت یا جهت استفاده می‌شود. بر متن سطح 2 بیش از سطح 1 تأکید دارد و برای مقدار زیاد متن سطح 2 گزینهٔ خوبی است. |
| VerticalCircleList | `128` | از آن برای نمایش داده‌های متوالی یا گروه‌بندی‌شده استفاده می‌شود. برای متن سطح 1 که در کنار یک شکل دایره‌ای بزرگ نمایش می‌یابد مناسب است. سطوح پایین‌تر متن با دایره‌های کوچکتر جدا می‌شوند. |
| VerticalCurvedList | `129` | از آن برای نمایش فهرست منحنی اطلاعات استفاده می‌شود. برای افزودن تصاویر به شکل‌های دایره‌ای تأکیدی، پرکردن تصویر را اعمال کنید. |
| VerticalEquation | `130` | از آن برای نمایش گام‌ها یا وظایف متوالی که یک برنامه یا نتیجه را نشان می‌دهند استفاده می‌شود. آخرین خط متن سطح 1 پس از پیکان ظاهر می‌شود. بهترین عملکرد را تنها با متن سطح 1 دارد. |
| VerticalPictureAccentList | `131` | از آن برای نمایش بلوک‌های اطلاعاتی غیرمتوالی یا گروه‌بندی‌شده استفاده می‌شود. دایره‌های کوچک برای دربرداشتن تصاویر طراحی شده‌اند. |
| VerticalPictureList | `132` | از آن برای نمایش بلوک‌های اطلاعاتی غیرمتوالی یا گروه‌بندی‌شده استفاده می‌شود. شکل‌های کوچک در سمت چپ برای دربرداشتن تصاویر طراحی شده‌اند. |
| VerticalProcess | `133` | از آن برای نمایش پیشرفت یا گام‌های متوالی در یک تکلیف، فرایند یا جریان کار از بالا به پایین استفاده می‌شود. بهترین عملکرد را با متن سطح 1 دارد، زیرا فضای عمودی محدود است. |
| Custom | `134` | نمایانگر یک نمودار SmartArt با قالب طرح‌بندی سفارشی است |
| PictureOrganizationChart | `135` | از آن برای نمایش اطلاعات سلسله‌مراتبی یا روابط گزارش‌دهی در یک سازمان با تصاویر متناظر استفاده می‌شود. شکل کمکی و طرح‌های آویز نمودار سازمانی با این طرح‌بندی در دسترس هستند. |

### همچنین ببینید

* فضای‌نام [Aspose.Slides.SmartArt](../../aspose.slides.smartart)
* مجمع [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->