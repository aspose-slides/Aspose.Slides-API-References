---
title: AddDataPointForScatterSeries
second_title: Aspose.Sildes برای .NET مرجع API
description: یک نقطه داده جدید ایجاد می‌کند و آن را به انتهای مجموعه اضافه می‌گرداند. برای سلسله‌هایی که chartType یکی از زیرنوع‌های Scatter است کاربرد دارد؛ همچنین به متد ChartTypeCharacterizer.IsChartTypeScatterChartType مراجعه کنید.
type: docs
weight: 180
url: /fa/aspose.slides.charts/ichartdatapointcollection/adddatapointforscatterseries/
---
## AddDataPointForScatterSeries(IChartDataCell, IChartDataCell) {#adddatapointforscatterseries}

یک نقطه داده جدید ایجاد می‌کند و آن را به انتهای مجموعه اضافه می‌گرداند. برای سلسله‌هایی که chartType یکی از زیرنوع‌های Scatter است قابل استفاده است (همچنین به متد ChartTypeCharacterizer.IsChartTypeScatter(ChartType) مراجعه کنید).

```csharp
public IChartDataPoint AddDataPointForScatterSeries(IChartDataCell xValue, IChartDataCell yValue)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| xValue | IChartDataCell | نقطه داده XValue |
| yValue | IChartDataCell | نقطه داده YValue |

### مقدار بازگشتی

نقطه داده جدید.

### موارد مرتبط

* رابط [IChartDataPoint](../../ichartdatapoint)
* رابط [IChartDataCell](../../ichartdatacell)
* رابط [IChartDataPointCollection](../../ichartdatapointcollection)
* فضای نام [Aspose.Slides.Charts](../../ichartdatapointcollection)
* مجتمع [Aspose.Slides](../../../)

---

## AddDataPointForScatterSeries(double, IChartDataCell) {#adddatapointforscatterseries_2}

یک نقطه داده جدید ایجاد می‌کند و آن را به انتهای مجموعه اضافه می‌گرداند. برای سلسله‌هایی که chartType یکی از زیرنوع‌های Scatter است قابل استفاده است (همچنین به متد ChartTypeCharacterizer.IsChartTypeScatter(ChartType) مراجعه کنید).

```csharp
public IChartDataPoint AddDataPointForScatterSeries(double xValue, IChartDataCell yValue)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| xValue | Double | نقطه داده XValue |
| yValue | IChartDataCell | نقطه داده YValue |

### مقدار بازگشتی

نقطه داده جدید.

### موارد مرتبط

* رابط [IChartDataPoint](../../ichartdatapoint)
* رابط [IChartDataCell](../../ichartdatacell)
* رابط [IChartDataPointCollection](../../ichartdatapointcollection)
* فضای نام [Aspose.Slides.Charts](../../ichartdatapointcollection)
* مجتمع [Aspose.Slides](../../../)

---

## AddDataPointForScatterSeries(string, IChartDataCell) {#adddatapointforscatterseries_4}

یک نقطه داده جدید ایجاد می‌کند و آن را به انتهای مجموعه اضافه می‌گرداند. برای سلسله‌هایی که chartType یکی از زیرنوع‌های Scatter است قابل استفاده است (همچنین به متد ChartTypeCharacterizer.IsChartTypeScatter(ChartType) مراجعه کنید).

```csharp
public IChartDataPoint AddDataPointForScatterSeries(string xValue, IChartDataCell yValue)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| xValue | String | نقطه داده XValue |
| yValue | IChartDataCell | نقطه داده YValue |

### مقدار بازگشتی

نقطه داده جدید.

### موارد مرتبط

* رابط [IChartDataPoint](../../ichartdatapoint)
* رابط [IChartDataCell](../../ichartdatacell)
* رابط [IChartDataPointCollection](../../ichartdatapointcollection)
* فضای نام [Aspose.Slides.Charts](../../ichartdatapointcollection)
* مجتمع [Aspose.Slides](../../../)

---

## AddDataPointForScatterSeries(IChartDataCell, double) {#adddatapointforscatterseries_1}

یک نقطه داده جدید ایجاد می‌کند و آن را به انتهای مجموعه اضافه می‌گرداند. برای سلسله‌هایی که chartType یکی از زیرنوع‌های Scatter است قابل استفاده است (همچنین به متد ChartTypeCharacterizer.IsChartTypeScatter(ChartType) مراجعه کنید).

```csharp
public IChartDataPoint AddDataPointForScatterSeries(IChartDataCell xValue, double yValue)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| xValue | IChartDataCell | نقطه داده XValue |
| yValue | Double | نقطه داده YValue |

### مقدار بازگشتی

نقطه داده جدید.

### موارد مرتبط

* رابط [IChartDataPoint](../../ichartdatapoint)
* رابط [IChartDataCell](../../ichartdatacell)
* رابط [IChartDataPointCollection](../../ichartdatapointcollection)
* فضای نام [Aspose.Slides.Charts](../../ichartdatapointcollection)
* مجتمع [Aspose.Slides](../../../)

---

## AddDataPointForScatterSeries(double, double) {#adddatapointforscatterseries_3}

یک نقطه داده جدید ایجاد می‌کند و آن را به انتهای مجموعه اضافه می‌گرداند. برای سلسله‌هایی که chartType یکی از زیرنوع‌های Scatter است قابل استفاده است (همچنین به متد ChartTypeCharacterizer.IsChartTypeScatter(ChartType) مراجعه کنید).

```csharp
public IChartDataPoint AddDataPointForScatterSeries(double xValue, double yValue)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| xValue | Double | نقطه داده XValue |
| yValue | Double | نقطه داده YValue |

### مقدار بازگشتی

نقطه داده جدید.

### موارد مرتبط

* رابط [IChartDataPoint](../../ichartdatapoint)
* رابط [IChartDataPointCollection](../../ichartdatapointcollection)
* فضای نام [Aspose.Slides.Charts](../../ichartdatapointcollection)
* مجتمع [Aspose.Slides](../../../)

---

## AddDataPointForScatterSeries(string, double) {#adddatapointforscatterseries_5}

یک نقطه داده جدید ایجاد می‌کند و آن را به انتهای مجموعه اضافه می‌گرداند. برای سلسله‌هایی که chartType یکی از زیرنوع‌های Scatter است قابل استفاده است (همچنین به متد ChartTypeCharacterizer.IsChartTypeScatter(ChartType) مراجعه کنید).

```csharp
public IChartDataPoint AddDataPointForScatterSeries(string xValue, double yValue)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| xValue | String | نقطه داده XValue |
| yValue | Double | نقطه داده YValue |

### مقدار بازگشتی

نقطه داده جدید.

### موارد مرتبط

* رابط [IChartDataPoint](../../ichartdatapoint)
* رابط [IChartDataPointCollection](../../ichartdatapointcollection)
* فضای نام [Aspose.Slides.Charts](../../ichartdatapointcollection)
* مجتمع [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->