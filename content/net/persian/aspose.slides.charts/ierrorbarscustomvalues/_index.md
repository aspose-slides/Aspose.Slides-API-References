---
title: IErrorBarsCustomValues
second_title: مرجع API Aspose.Sildes برای .NET
description: مقدار نوارهای خطا را مشخص می‌کند. این فقط زمانی باید استفاده شود که نوع مقدار نوارهای خطا «Custom» باشد.
type: docs
weight: 2080
url: /fa/aspose.slides.charts/ierrorbarscustomvalues/
---
## IErrorBarsCustomValues رابط

مقدار نوارهای خطا را مشخص می‌کند. تنها زمانی که نوع مقدار نوارهای خطا «Custom» باشد باید استفاده شود.

```csharp
public interface IErrorBarsCustomValues
```

## خواص

| نام | توضیح |
| --- | --- |
| [XMinus](../../aspose.slides.charts/ierrorbarscustomvalues/xminus) { get; } | مقدار نوار خطا را در جهت منفی مشخص می‌کند. اگر نوع مقدار نوارهای خطا «Custom» باشد و ErrorBarsXFormat اجازه داده شده باشد، در دسترس است. در غیر این صورت این ویژگی null برمی‌گرداند. فقط‌خواندنی [`IDoubleChartValue`](../idoublechartvalue). |
| [XPlus](../../aspose.slides.charts/ierrorbarscustomvalues/xplus) { get; } | مقدار نوار خطا را در جهت مثبت مشخص می‌کند. اگر نوع مقدار نوارهای خطا «Custom» باشد و ErrorBarsXFormat اجازه داده شده باشد، در دسترس است. در غیر این صورت این ویژگی null برمی‌گرداند. فقط‌خواندنی [`IDoubleChartValue`](../idoublechartvalue). |
| [YMinus](../../aspose.slides.charts/ierrorbarscustomvalues/yminus) { get; } | مقدار نوار خطا را در جهت منفی مشخص می‌کند. اگر نوع مقدار نوارهای خطا «Custom» باشد و ErrorBarsYFormat اجازه داده شده باشد، در دسترس است. در غیر این صورت این ویژگی null برمی‌گرداند. فقط‌خواندنی [`IDoubleChartValue`](../idoublechartvalue). |
| [YPlus](../../aspose.slides.charts/ierrorbarscustomvalues/yplus) { get; } | مقدار نوار خطا را در جهت مثبت مشخص می‌کند. اگر نوع مقدار نوارهای خطا «Custom» باشد و ErrorBarsYFormat اجازه داده شده باشد، در دسترس است. در غیر این صورت این ویژگی null برمی‌گرداند. فقط‌خواندنی [`IDoubleChartValue`](../idoublechartvalue). |

### همچنین ببینید

* فضای نام [Aspose.Slides.Charts](../../aspose.slides.charts)
* مجموعه [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->