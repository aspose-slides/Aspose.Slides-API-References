---
title: ITrendline
second_title: Aspose.Sildes برای .NET مرجع API
description: کلاس نمایانگر خط روند سری نمودار
type: docs
weight: 2240
url: /fa/aspose.slides.charts/itrendline/
---
## ITrendline رابط

کلاس نمایانگر خط روند سری نمودار است

```csharp
public interface ITrendline : IOverridableText
```

## ویژگی‌ها

| نام | توضیح |
| --- | --- |
| [AsIOverridableText](../../aspose.slides.charts/itrendline/asioverridabletext) { get; } | IOverridableText رابط را برمی‌گرداند. فقط-خواندنی [`IOverridableText`](../ioverridabletext). |
| [Backward](../../aspose.slides.charts/itrendline/backward) { get; set; } | تعداد دسته‌ها (یا واحدها در نمودار پراکنده) را که خط روند پیش از داده‌های سری که در حال روند است گسترش می‌یابد، مشخص می‌کند. در نمودارهای پراکنده و غیرپراکنده، مقدار می‌تواند هر مقدار غیرمنفی باشد. قابل‌خواندن/قابل‌نوشتن Double. |
| [DisplayEquation](../../aspose.slides.charts/itrendline/displayequation) { get; set; } | مشخص می‌کند که معادله خط روند بر روی نمودار نمایش داده شود (در همان برچسبی که Rsquaredvalue قرار دارد). قابل‌خواندن/قابل‌نوشتن Boolean. |
| [DisplayRSquaredValue](../../aspose.slides.charts/itrendline/displayrsquaredvalue) { get; set; } | مشخص می‌کند که مقدار R-squared خط روند بر روی نمودار نمایش داده شود (در همان برچسبی که معادله قرار دارد). قابل‌خواندن/قابل‌نوشتن Boolean. |
| [Format](../../aspose.slides.charts/itrendline/format) { get; set; } | قالب خط روند را نشان می‌دهد. قابل‌خواندن/قابل‌نوشتن [`IFormat`](../iformat). |
| [Forward](../../aspose.slides.charts/itrendline/forward) { get; set; } | تعداد دسته‌ها (یا واحدها در نمودار پراکنده) را که خط روند پس از داده‌های سری که در حال روند است گسترش می‌یابد، مشخص می‌کند. در نمودارهای پراکنده و غیرپراکنده، مقدار می‌تواند هر مقدار غیرمنفی باشد. قابل‌خواندن/قابل‌نوشتن Double. |
| [Intercept](../../aspose.slides.charts/itrendline/intercept) { get; set; } | مقداری را که خط روند باید محور y را قطع کند مشخص می‌کند. این ویژگی فقط زمانی پشتیبانی می‌شود که نوع خط روند exp، linear یا poly باشد. قابل‌خواندن/قابل‌نوشتن Double. |
| [Order](../../aspose.slides.charts/itrendline/order) { get; set; } | ترتیب خط روند چندجمله‌ای را مشخص می‌کند. برای سایر انواع خط روند نادیده گرفته می‌شود. مقدار باید بین ۲ تا ۶ باشد. قابل‌خواندن/قابل‌نوشتن Byte. |
| [Period](../../aspose.slides.charts/itrendline/period) { get; set; } | دوره‌ی خط روند برای خط روند میانگین متحرک را مشخص می‌کند. برای سایر انواع خط روند نادیده گرفته می‌شود. مقدار باید بین ۲ تا ۲۵۵ باشد. قابل‌خواندن/قابل‌نوشتن Byte. |
| [RelatedLegendEntry](../../aspose.slides.charts/itrendline/relatedlegendentry) { get; } | ورودی افسانه مرتبط با این خط روند را نشان می‌دهد. فقط-خواندنی [`ILegendEntryProperties`](../ilegendentryproperties). |
| [TrendlineName](../../aspose.slides.charts/itrendline/trendlinename) { get; set; } | نام خط روند را دریافت یا تنظیم می‌کند. قابل‌خواندن/قابل‌نوشتن String. |
| [TrendlineType](../../aspose.slides.charts/itrendline/trendlinetype) { get; set; } | نوع خط روند را دریافت یا تنظیم می‌کند. قابل‌خواندن/قابل‌نوشتن [`TrendlineType`](./trendlinetype). |

### همچنین ببینید

* رابط [IOverridableText](../ioverridabletext)
* فضای‌نام [Aspose.Slides.Charts](../../aspose.slides.charts)
* مجوعه [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->