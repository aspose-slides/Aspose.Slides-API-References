---
title: Process
second_title: Aspose.Sildes برای .NET مرجع API
description: چندین ارائه PowerPoint با قالب یکسان را به یک فایل ارائهٔ واحد ادغام می‌کند.
type: docs
weight: 10
url: /fa/aspose.slides.lowcode/merger/process/
---
## Process(string[], string) {#process_2}

چندین ارائهٔ PowerPoint با همان قالب را به یک فایل ارائهٔ واحد ادغام می‌کند.

```csharp
public static void Process(string[] inputFileNames, string outputFileName)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| inputFileNames | String[] | آرایه‌ای از نام‌های فایل ارائهٔ ورودی. |
| outputFileName | String | نام فایل خروجی ارائهٔ ادغام- شده. |

### استثناها

| استثنا | شرط |
| --- | --- |
| ArgumentException | در زمانی که نام‌فایل‌های ورودی نامعتبر هستند یا قالب‌ها مطابقت ندارند، رخ می‌دهد. |

### مثال‌ها

```csharp
Merger.Process(new string[] { "pres1.ppt", "pres2.ppt" }, "merged.ppt");
```

### موارد مرتبط

* کلاس [Merger](../../merger)
* فضای‌نام [Aspose.Slides.LowCode](../../merger)
* مجمع [Aspose.Slides](../../../)

---

## Process(string[], string, ISaveOptions) {#process_3}

چندین ارائهٔ PowerPoint با همان قالب را به یک فایل ارائهٔ واحد ادغام می‌کند.

```csharp
public static void Process(string[] inputFileNames, string outputFileName, ISaveOptions options)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| inputFileNames | String[] | آرایه‌ای از نام‌های فایل ارائهٔ ورودی. |
| outputFileName | String | نام فایل خروجی ارائهٔ ادغام- شده. |
| options | ISaveOptions | گزینه‌های اضافه‌ای که نحوهٔ ذخیرهٔ ارائهٔ ادغام شده را تعریف می‌کند. |

### استثناها

| استثنا | شرط |
| --- | --- |
| ArgumentException | در زمانی که نام‌فایل‌های ورودی نامعتبر هستند یا قالب‌ها مطابقت ندارند، رخ می‌دهد. |

### مثال‌ها

```csharp
Merger.Process(new string[] { "pres1.pptx", "pres2.pptx" }, "merged.pptx", new PptxOptions() { RefreshThumbnail = false });
```

### موارد مرتبط

* رابط [ISaveOptions](../../../aspose.slides.export/isaveoptions)
* کلاس [Merger](../../merger)
* فضای‌نام [Aspose.Slides.LowCode](../../merger)
* مجمع [Aspose.Slides](../../../)

---

## Process(string[], Stream) {#process}

چندین ارائهٔ PowerPoint با همان قالب را به یک فایل ارائهٔ واحد ادغام می‌کند.

```csharp
public static void Process(string[] inputFileNames, Stream outputStream)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| inputFileNames | String[] | آرایه‌ای از نام‌های فایل ارائهٔ ورودی. |
| outputStream | Stream | جریان خروجی. |

### استثناها

| استثنا | شرط |
| --- | --- |
| ArgumentException | در زمانی که نام‌فایل‌های ورودی نامعتبر هستند یا قالب‌ها مطابقت ندارند، رخ می‌دهد. |

### مثال‌ها

```csharp
using(var stream = new MemoryStream())
    Merger.Process(new string[] { "pres1.ppt", "pres2.ppt" }, stream);
```

### موارد مرتبط

* کلاس [Merger](../../merger)
* فضای‌نام [Aspose.Slides.LowCode](../../merger)
* مجمع [Aspose.Slides](../../../)

---

## Process(string[], Stream, ISaveOptions) {#process_1}

چندین ارائهٔ PowerPoint با همان قالب را به یک فایل ارائهٔ واحد ادغام می‌کند.

```csharp
public static void Process(string[] inputFileNames, Stream outputStream, ISaveOptions options)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| inputFileNames | String[] | آرایه‌ای از نام‌های فایل ارائهٔ ورودی. |
| outputStream | Stream | جریان خروجی. |
| options | ISaveOptions | گزینه‌های اضافه‌ای که نحوهٔ ذخیرهٔ ارائهٔ ادغام شده را تعریف می‌کند. |

### استثناها

| استثنا | شرط |
| --- | --- |
| ArgumentException | در زمانی که نام‌فایل‌های ورودی نامعتبر هستند یا قالب‌ها مطابقت ندارند، رخ می‌دهد. |

### مثال‌ها

```csharp
using(var stream = new MemoryStream())
    Merger.Process(new string[] { "pres1.pptx", "pres2.pptx" }, stream, new PptxOptions() { RefreshThumbnail = false });
```

### موارد مرتبط

* رابط [ISaveOptions](../../../aspose.slides.export/isaveoptions)
* کلاس [Merger](../../merger)
* فضای‌نام [Aspose.Slides.LowCode](../../merger)
* مجمع [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->