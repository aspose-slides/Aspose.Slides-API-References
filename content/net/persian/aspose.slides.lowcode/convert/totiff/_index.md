---
title: ToTiff
second_title: Aspose.Sildes برای مرجع API .NET
description: ارائهٔ ورودی را به مجموعه‌ای از تصاویر با فرمت TIFF تبدیل می‌کند. اگر نام فایل خروجی به صورت myPath/myFilename.tiff داده شود، نتیجه به‌صورت مجموعه‌ای از پرونده‌های myPath/myFilename_N.tiff ذخیره می‌شود که N شمارهٔ اسلاید است.
type: docs
weight: 60
url: /fa/aspose.slides.lowcode/convert/totiff/
---
## ToTiff(Presentation, string) {#totiff}

ارائهٔ ورودی را به مجموعه‌ای از تصاویر با فرمت TIFF تبدیل می‌کند. اگر نام فایل خروجی به صورت "myPath/myFilename.tiff" باشد، نتیجه به‌صورت مجموعه‌ای از فایل‌های "myPath/myFilename_N.tiff" ذخیره می‌شود که N شمارهٔ اسلاید است.

```csharp
public static void ToTiff(Presentation pres, string outputFileName)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| pres | Presentation | ارائهٔ ورودی. |
| outputFileName | String | نام فایل خروجی. |

### استثناها

| استثنا | شرط |
| --- | --- |
| ArgumentException |  |

### مثال‌ها

```csharp
using (var pres = new Presentation("pres.pptx"))
   Convert.ToTiff(pres, "presImage.tiff");
```

### مراجعه کنید

* کلاس [Presentation](../../../aspose.slides/presentation)
* کلاس [Convert](../../convert)
* فضای‌نام [Aspose.Slides.LowCode](../../convert)
* اسمبلی [Aspose.Slides](../../../)

---

## ToTiff(Presentation, string, ITiffOptions, bool) {#totiff_1}

ارائهٔ ورودی را به فرمت TIFF با گزینه‌های سفارشی تبدیل می‌کند. اگر نام فایل خروجی به صورت "myPath/myFilename.tiff" باشد و *multipage* برابر `false` باشد، نتیجه به‌صورت مجموعه‌ای از فایل‌های "myPath/myFilename_N.tiff" ذخیره می‌شود که N شمارهٔ اسلاید است. در غیر این صورت، اگر *multipage* برابر `true` باشد، نتیجه یک سند چندصفحهٔ "myPath/myFilename.tiff" خواهد بود.

```csharp
public static void ToTiff(Presentation pres, string outputFileName, ITiffOptions options, 
    bool multipage)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| pres | Presentation | ارائهٔ ورودی. |
| outputFileName | String | نام فایل خروجی. |
| options | ITiffOptions | گزینه‌های ذخیره‌سازی ITiffOptions. |
| multipage | Boolean | مشخص می‌کند که آیا سند TIFF تولید‌شده باید چندصفحه باشد یا خیر. |

### استثناها

| استثنا | شرط |
| --- | --- |
| ArgumentException |  |

### مثال‌ها

```csharp
ITiffOptions options = new TiffOptions()
{
    CompressionType = TiffCompressionTypes.CCITT3,
    SlidesLayoutOptions = new NotesCommentsLayoutingOptions()
    {
        NotesPosition = NotesPositions.BottomTruncated
    }
};

using (var pres = new Presentation("pres.pptx"))
   Convert.ToTiff(pres, "pres.tiff", options, false);
```

### مراجعه کنید

* کلاس [Presentation](../../../aspose.slides/presentation)
* رابط [ITiffOptions](../../../aspose.slides.export/itiffoptions)
* کلاس [Convert](../../convert)
* فضای‌نام [Aspose.Slides.LowCode](../../convert)
* اسمبلی [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->