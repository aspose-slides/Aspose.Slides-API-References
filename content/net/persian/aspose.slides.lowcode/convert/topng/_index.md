---
title: ToPng
second_title: Aspose.Sildes برای .NET مرجع API
description: ارائهٔ ورودی را به مجموعه‌ای از تصاویر با فرمت PNG تبدیل می‌کند. اگر نام فایل خروجی به صورت myPath/myFilename.png ارائه شود، نتیجه به‌صورت مجموعه‌ای از فایل‌های myPath/myFilename_N.png ذخیره می‌شود که N شمارهٔ اسلاید است.
type: docs
weight: 40
url: /fa/aspose.slides.lowcode/convert/topng/
---
## ToPng(Presentation, string) {#topng}

ارائهٔ ورودی را به مجموعه‌ای از تصاویر با فرمت PNG تبدیل می‌کند. اگر نام فایل خروجی به صورت "myPath/myFilename.png" ارائه شود، نتیجه به‌صورت مجموعه‌ای از فایل‌های "myPath/myFilename_N.png" ذخیره می‌شود که در آن N شمارهٔ اسلاید است.

```csharp
public static void ToPng(Presentation pres, string outputFileName)
```

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| pres | Presentation | ارائهٔ ورودی. |
| outputFileName | String | نام فایل خروجی. |

### استثنائات

| استثنا | شرط |
| --- | --- |
| ArgumentException |  |

### مثال‌ها

```csharp
using (var pres = new Presentation("pres.pptx"))
   Convert.ToPng(pres, "presImage.png");
```

### موارد دیگر

* کلاس [Presentation](../../../aspose.slides/presentation)
* کلاس [Convert](../../convert)
* فضای‌نام [Aspose.Slides.LowCode](../../convert)
* مجتمع [Aspose.Slides](../../../)

---

## ToPng(Presentation, string, Size) {#topng_2}

ارائهٔ ورودی را به مجموعه‌ای از تصاویر با فرمت PNG تبدیل می‌کند. اگر نام فایل خروجی به صورت "myPath/myFilename.png" ارائه شود، نتیجه به‌صورت مجموعه‌ای از فایل‌های "myPath/myFilename_N.png" ذخیره می‌شود که در آن N شمارهٔ اسلاید است.

```csharp
public static void ToPng(Presentation pres, string outputFileName, Size imageSize)
```

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| pres | Presentation | ارائهٔ ورودی |
| outputFileName | String | نام فایل خروجی. |
| imageSize | Size | اندازهٔ هر تصویر تولید شده. |

### استثنائات

| استثنا | شرط |
| --- | --- |
| ArgumentException |  |

### مثال‌ها

```csharp
using (var pres = new Presentation("pres.pptx"))
    Convert.ToPng(pres, "presImage.png", new Size(720, 540));
```

### موارد دیگر

* کلاس [Presentation](../../../aspose.slides/presentation)
* کلاس [Convert](../../convert)
* فضای‌نام [Aspose.Slides.LowCode](../../convert)
* مجتمع [Aspose.Slides](../../../)

---

## ToPng(Presentation, string, float, IRenderingOptions) {#topng_1}

ارائهٔ ورودی را به مجموعه‌ای از تصاویر با فرمت PNG تبدیل می‌کند. اگر نام فایل خروجی به صورت "myPath/myFilename.png" ارائه شود، نتیجه به‌صورت مجموعه‌ای از فایل‌های "myPath/myFilename_N.png" ذخیره می‌شود که در آن N شمارهٔ اسلاید است.

```csharp
public static void ToPng(Presentation pres, string outputFileName, float scale, 
    IRenderingOptions options)
```

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| pres | Presentation | ارائهٔ ورودی. |
| outputFileName | String | نام فایل خروجی. |
| scale | Single | ضریب مقیاس اعمال‌شده بر تصاویر خروجی نسبت به اندازهٔ اصل اسلاید. |
| options | IRenderingOptions | گزینه‌های رندرینگ. |

### استثنائات

| استثنا | شرط |
| --- | --- |
| ArgumentException |  |

### مثال‌ها

```csharp
IRenderingOptions options = new RenderingOptions()
{
    SlidesLayoutOptions = new NotesCommentsLayoutingOptions()
    {
        NotesPosition = NotesPositions.BottomTruncated
    }
};

using (var pres = new Presentation("pres.pptx"))
   Convert.ToPng(pres, "presImage.png", 2f, options);
```

### موارد دیگر

* کلاس [Presentation](../../../aspose.slides/presentation)
* اینترفیس [IRenderingOptions](../../../aspose.slides.export/irenderingoptions)
* کلاس [Convert](../../convert)
* فضای‌نام [Aspose.Slides.LowCode](../../convert)
* مجتمع [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->