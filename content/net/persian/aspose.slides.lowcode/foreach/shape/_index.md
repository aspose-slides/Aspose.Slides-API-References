---
title: Shape
second_title: Aspose.Sildes برای .NET مرجع API
description: هر Shapeaspose.slides.lowcode/foreach/shape را در Presentationaspose.slides/presentation تکرار کنید. اشکال در تمام انواع اسلایدها - Slideaspose.slides.lowcode/foreach/slide MasterSlideaspose.slides.lowcode/foreach/masterslide و LayoutSlideaspose.slides.lowcode/foreach/layoutslide تکرار خواهند شد
type: docs
weight: 50
url: /fa/aspose.slides.lowcode/foreach/shape/
---
## Shape(Presentation, ForEachShapeCallback) {#shape_2}

هر `Shape` را در [`Presentation`](../../../aspose.slides/presentation) تکرار کنید. اشکال در همه انواع اسلایدها - [`Slide`](../slide)، [`MasterSlide`](../masterslide) و [`LayoutSlide`](../layoutslide) تکرار خواهند شد.

```csharp
public static void Shape(Presentation pres, ForEachShapeCallback forEachShape)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| pres | Presentation | Presentation to iterate layout shapes |
| forEachShape | ForEachShapeCallback | Callback that will be invoked for each shape |

### مثال‌ها

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Shape(pres, (shape, slide, index) => 
    {
        System.Console.WriteLine($"{shape.Name}, index: {index}");
    });
} 
```

### موارد مرتبط

* کلاس [Presentation](../../../aspose.slides/presentation)
* delegate [ForEachShapeCallback](../../foreach.foreachshapecallback)
* کلاس [ForEach](../../foreach)
* فضای‌نام [Aspose.Slides.LowCode](../../foreach)
* اسمبل [Aspose.Slides](../../../)

---

## Shape(Presentation, bool, ForEachShapeCallback) {#shape_1}

هر [`Shape`](../shape) را در [`Presentation`](../../../aspose.slides/presentation) تکرار کنید. اشکال در همه انواع اسلایدها - [`Slide`](../slide)، [`MasterSlide`](../masterslide)، [`LayoutSlide`](../layoutslide) و [`NotesSlide`](../../../aspose.slides/notesslide) در صورت نیاز تکرار خواهند شد.

```csharp
public static void Shape(Presentation pres, bool includeNotes, ForEachShapeCallback forEachShape)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| pres | Presentation | Presentation to iterate layout shapes |
| includeNotes | Boolean | Flag that indicates whether NotesSlides should be included in processing. |
| forEachShape | ForEachShapeCallback | Callback that will be invoked for each shape |

### مثال‌ها

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Shape(pres, true, (shape, slide, index) => 
    {
        System.Console.WriteLine($"{shape.Name}, index: {index}");
    });
} 
```

### موارد مرتبط

* کلاس [Presentation](../../../aspose.slides/presentation)
* delegate [ForEachShapeCallback](../../foreach.foreachshapecallback)
* کلاس [ForEach](../../foreach)
* فضای‌نام [Aspose.Slides.LowCode](../../foreach)
* اسمبل [Aspose.Slides](../../../)

---

## Shape(BaseSlide, ForEachShapeCallback) {#shape}

هر [`Shape`](../shape) را در [`BaseSlide`](../../../aspose.slides/baseslide) تکرار کنید. [`BaseSlide`](../../../aspose.slides/baseslide) نوع پایه برای [`Slide`](../slide)، [`MasterSlide`](../masterslide) و [`LayoutSlide`](../layoutslide) است.

```csharp
public static void Shape(BaseSlide baseSlide, ForEachShapeCallback forEachShape)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| baseSlide | BaseSlide | Slide to iterate layout shapes |
| forEachShape | ForEachShapeCallback | Callback that will be invoked for each shape |

### مثال‌ها

```csharp
(Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Slide(pres, (slide, index) =>
    {
        ForEach.Shape(slide, (shape, baseSlide, shapeIndex) =>
        {
            System.Console.WriteLine($"{shape.Name}, index: {shapeIndex}");
        });
    });
} 
```

### موارد مرتبط

* کلاس [BaseSlide](../../../aspose.slides/baseslide)
* delegate [ForEachShapeCallback](../../foreach.foreachshapecallback)
* کلاس [ForEach](../../foreach)
* فضای‌نام [Aspose.Slides.LowCode](../../foreach)
* اسمبل [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->