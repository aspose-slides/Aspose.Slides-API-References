---
title: GetEffective
second_title: Aspose.Sildes برای مرجع API .NET
description: داده‌های قالب‌بندی بخش مؤثر را با اعمال وراثت دریافت می‌کند.
type: docs
weight: 70
url: /fa/aspose.slides/portionformat/geteffective/
---
## PortionFormat.GetEffective متد

داده‌های قالب‌بندی بخش مؤثر را با اعمال وراثت دریافت می‌کند.

```csharp
public IPortionFormatEffectiveData GetEffective()
```

### مقدار بازگشت

یک [`IPortionFormatEffectiveData`](../../iportionformateffectivedata).

### مثال‌ها

این مثال دریافت برخی از ویژگی‌های قالب‌بندی بخش مؤثر را نشان می‌دهد.

```csharp
[C#]
using (Presentation pres = new Presentation(@"MyPresentation.pptx"))
{
	IAutoShape shape = pres.Slides[0].Shapes[0] as IAutoShape;
	IPortionFormatEffectiveData effectivePortionFormat = shape.TextFrame.Paragraphs[0].Portions[0].PortionFormat.GetEffective();

	Console.WriteLine("Latin font: " + effectivePortionFormat.LatinFont.FontName);
	Console.WriteLine("Font height: " + effectivePortionFormat.FontHeight);
	Console.WriteLine("Fill type: " + effectivePortionFormat.FillFormat.FillType);
}
```

### موارد مرتبط

* رابط [IPortionFormatEffectiveData](../../iportionformateffectivedata)
* کلاس [PortionFormat](../../portionformat)
* فضای نام [Aspose.Slides](../../portionformat)
* مونتاژ [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->