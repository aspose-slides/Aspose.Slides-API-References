---
title: ModernComment
second_title: Aspose.Sildes برای .NET مرجع API
description: یک نظر را روی اسلاید نمایش می‌دهد.
type: docs
weight: 9090
url: /fa/aspose.slides/moderncomment/
---
## ModernComment کلاس

یک نظر روی اسلاید را نمایش می‌دهد.

```csharp
public sealed class ModernComment : Comment, IModernComment
```

## خصوصیات

| نام | توضیح |
| --- | --- |
| [Author](../../aspose.slides/comment/author) { get; } | نویسندهٔ یک نظر را برمی‌گرداند. فقط خواندنی [`ICommentAuthor`](../icommentauthor). |
| [CreatedTime](../../aspose.slides/comment/createdtime) { get; set; } | زمان ایجاد یک نظر را برمی‌گرداند یا تنظیم می‌کند. تنظیم این ویژگی به MinValue به این معنی است که زمان نظری تنظیم نشده است. قابل خواندن/نوشتن DateTime. |
| [ParentComment](../../aspose.slides/comment/parentcomment) { get; set; } | نظر والد را می‌گیرد یا تنظیم می‌کند. قابل خواندن/نوشتن [`IComment`](../icomment). |
| [Position](../../aspose.slides/comment/position) { get; set; } | موقعیت یک نظر روی اسلاید را برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن PointF. |
| [Shape](../../aspose.slides/moderncomment/shape) { get; } | شکلی که به نظر مرتبط است را برمی‌گرداند. فقط خواندنی [`IShape`](../ishape). |
| [Slide](../../aspose.slides/comment/slide) { get; } | اسلاید والد یک نظر را برمی‌گرداند یا تنظیم می‌کند. فقط خواندنی [`ISlide`](../islide). |
| [Status](../../aspose.slides/moderncomment/status) { get; set; } | وضعیت نظر را می‌گیرد یا تنظیم می‌کند. قابل خواندن/نوشتن [`ModernCommentStatus`](../moderncommentstatus). |
| [Text](../../aspose.slides/comment/text) { get; set; } | متن سادهٔ یک نظر اسلاید را برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن String. |
| [TextSelectionLength](../../aspose.slides/moderncomment/textselectionlength) { get; set; } | طول انتخاب متن در فریم متن را اگر نظر با AutoShape مرتبط باشد می‌گیرد یا تنظیم می‌کند. قابل خواندن/نوشتن Int32. |
| [TextSelectionStart](../../aspose.slides/moderncomment/textselectionstart) { get; set; } | موقعیت شروع انتخاب متن در فریم متن را اگر نظر با AutoShape مرتبط باشد می‌گیرد یا تنظیم می‌کند. قابل خواندن/نوشتن Int32. |

## متدها

| نام | توضیح |
| --- | --- |
| [Remove](../../aspose.slides/comment/remove)() | نظر و تمام پاسخ‌های آن را از مجموعهٔ والد حذف می‌کند. |

### مثال‌ها

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    ICommentAuthor newAuthor = pres.CommentAuthors.AddAuthor("Some Author", "SA");
    IModernComment modernComment = newAuthor.Comments.AddModernComment("This is modern comment", pres.Slides[0], null, new PointF(100, 100), DateTime.Now);

    pres.Save(outPptxFileName, SaveFormat.Pptx);
}
```

### موارد مرتبط

* کلاس [Comment](../comment)
* رابط [IModernComment](../imoderncomment)
* فضای نام [Aspose.Slides](../../aspose.slides)
* اسمبلی [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->