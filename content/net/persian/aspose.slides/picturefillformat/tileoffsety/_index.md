---
title: TileOffsetY
second_title: Aspose.Sildes برای .NET مرجع API
description: مقدار جابجایی عمودی بافت نسبت به مبدأ شکل را به صورت نقطه برمی‌گرداند یا تنظیم می‌کند. یک مقدار مثبت بافت را به سمت پایین می‌برد، در حالی که مقدار منفی آن را به سمت بالا می‌برد. قابل خواندن/قابل نوشتن Single.
type: docs
weight: 150
url: /fa/aspose.slides/picturefillformat/tileoffsety/
---
## PictureFillFormat.TileOffsetY ویژگی
مقدار جابجایی عمودی بافت نسبت به مبدأ شکل را بر حسب نقطه برمی‌گرداند یا تنظیم می‌کند. یک مقدار مثبت بافت را به سمت پایین می‌برد، در حالی که مقدار منفی آن را به سمت بالا می‌برد. قابل خواندن/قابل نوشتن Single.

```csharp
public float TileOffsetY { get; set; }
```

### مثال‌ها

```csharp
[C#]
using (Presentation presentation = new Presentation("demo.pptx"))
{
    ISlide slide = presentation.Slides[0];

    // قالب پر کردن تصویر شکل را دریافت می‌کند
    IPictureFillFormat pictureFillFormat = slide.Shapes[0].FillFormat.PictureFillFormat;

    // حالت پر کردن تصویر را به Tile تنظیم می‌کند
    pictureFillFormat.PictureFillMode = PictureFillMode.Tile;

    // جابجایی عمودی بافت را به -50 نقطه تنظیم می‌کند
    pictureFillFormat.TileOffsetY = -50;
}
```

### موارد مرتبط

* کلاس [PictureFillFormat](../../picturefillformat)
* فضای نام [Aspose.Slides](../../picturefillformat)
* اسمبلی [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->