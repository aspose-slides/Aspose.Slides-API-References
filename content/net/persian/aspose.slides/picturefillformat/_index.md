---
title: PictureFillFormat
second_title: Aspose.Sildes برای .NET مرجع API
description: یک سبک پر کردن تصویر را نشان می‌دهد.
type: docs
weight: 9390
url: /fa/aspose.slides/picturefillformat/
---
## PictureFillFormat کلاس

یک سبک پر کردن تصویر را نشان می‌دهد.

```csharp
public sealed class PictureFillFormat : PVIObject, IPictureFillFormat
```

## ویژگی‌ها

| نام | توضیح |
| --- | --- |
| [AsIPresentationComponent](../../aspose.slides/pviobject/asipresentationcomponent) { get; } | اجازه می‌دهد رابط IPresentationComponent پایه را دریافت کند. فقط خواندنی [`IPresentationComponent`](../ipresentationcomponent). |
| [CropBottom](../../aspose.slides/picturefillformat/cropbottom) { get; set; } | مقدار درصدهای ارتفاع واقعی تصویر که از پایین تصویر برش داده می‌شود را بازمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن Single. |
| [CropLeft](../../aspose.slides/picturefillformat/cropleft) { get; set; } | مقدار درصدهای عرض واقعی تصویر که از سمت چپ تصویر برش داده می‌شود را بازمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن Single. |
| [CropRight](../../aspose.slides/picturefillformat/cropright) { get; set; } | مقدار درصدهای عرض واقعی تصویر که از سمت راست تصویر برش داده می‌شود را بازمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن Single. |
| [CropTop](../../aspose.slides/picturefillformat/croptop) { get; set; } | مقدار درصدهای ارتفاع واقعی تصویر که از بالای تصویر برش داده می‌شود را بازمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن Single. |
| [Dpi](../../aspose.slides/picturefillformat/dpi) { get; set; } | مقدار dpi که برای پر کردن تصویر استفاده می‌شود را بازمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن Int32. |
| [Picture](../../aspose.slides/picturefillformat/picture) { get; } | تصویر را بازمی‌گرداند. فقط خواندنی [`ISlidesPicture`](../islidespicture). |
| [PictureFillMode](../../aspose.slides/picturefillformat/picturefillmode) { get; set; } | حالت پر کردن تصویر را بازمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن [`PictureFillMode`](../picturefillmode). |
| [StretchOffsetBottom](../../aspose.slides/picturefillformat/stretchoffsetbottom) { get; set; } | لبه پایین مستطیل پرکننده که توسط جابجایی درصدی از لبه پایین جعبه محدود شکل تعریف می‌شود را بازمی‌گرداند یا تنظیم می‌کند. درصد مثبت یک تو رفتگی را مشخص می‌کند، در حالی که درصد منفی یک برآمدگی را نشان می‌دهد. قابل خواندن/نوشتن Single. |
| [StretchOffsetLeft](../../aspose.slides/picturefillformat/stretchoffsetleft) { get; set; } | لبه چپ مستطیل پرکننده که توسط جابجایی درصدی از لبه چپ جعبه محدود شکل تعریف می‌شود را بازمی‌گرداند یا تنظیم می‌کند. درصد مثبت یک تو رفتگی را مشخص می‌کند، در حالی که درصد منفی یک برآمدگی را نشان می‌دهد. قابل خواندن/نوشتن Single. |
| [StretchOffsetRight](../../aspose.slides/picturefillformat/stretchoffsetright) { get; set; } | لبه راست مستطیل پرکننده که توسط جابجایی درصدی از لبه راست جعبه محدود شکل تعریف می‌شود را بازمی‌گرداند یا تنظیم می‌کند. درصد مثبت یک تو رفتگی را مشخص می‌کند، در حالی که درصد منفی یک برآمدگی را نشان می‌دهد. قابل خواندن/نوشتن Single. |
| [StretchOffsetTop](../../aspose.slides/picturefillformat/stretchoffsettop) { get; set; } | لبه بالای مستطیل پرکننده که توسط جابجایی درصدی از لبه بالای جعبه محدود شکل تعریف می‌شود را بازمی‌گرداند یا تنظیم می‌کند. درصد مثبت یک تو رفتگی را مشخص می‌کند، در حالی که درصد منفی یک برآمدگی را نشان می‌دهد. قابل خواندن/نوشتن Single. |
| [TileAlignment](../../aspose.slides/picturefillformat/tilealignment) { get; set; } | نحوه تنظیم ترازبندی بافت درون شکل را بازمی‌گرداند یا تنظیم می‌کند. این تنظیم نقطه شروع الگوی بافت و چگونگی تکرار آن در سراسر شکل را کنترل می‌کند. قابل خواندن/نوشتن [`RectangleAlignment`](../rectanglealignment). |
| [TileFlip](../../aspose.slides/picturefillformat/tileflip) { get; set; } | کاشی بافت را حول محور افقی، عمودی یا هر دو محور می‌چرخاند. قابل خواندن/نوشتن [`TileFlip`](../tileflip). |
| [TileOffsetX](../../aspose.slides/picturefillformat/tileoffsetx) { get; set; } | جابجایی افقی بافت نسبت به مبدا شکل بر حسب نقاط را بازمی‌گرداند یا تنظیم می‌کند. مقدار مثبت بافت را به سمت راست حرکت می‌دهد، در حالی که مقدار منفی به سمت چپ. قابل خواندن/نوشتن Single. |
| [TileOffsetY](../../aspose.slides/picturefillformat/tileoffsety) { get; set; } | جابجایی عمودی بافت نسبت به مبدا شکل بر حسب نقاط را بازمی‌گرداند یا تنظیم می‌کند. مقدار مثبت بافت را به سمت پایین حرکت می‌دهد، در حالی که مقدار منفی به سمت بالا. قابل خواندن/نوشتن Single. |
| [TileScaleX](../../aspose.slides/picturefillformat/tilescalex) { get; set; } | مقیاس افقی پر کردن بافت به صورت درصد را بازمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن Single. |
| [TileScaleY](../../aspose.slides/picturefillformat/tilescaley) { get; set; } | مقیاس عمودی پر کردن بافت به صورت درصد را بازمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن Single. |

## متدها

| نام | توضیح |
| --- | --- |
| [CompressImage](../../aspose.slides/picturefillformat/compressimage#compressimage_1)(bool, float) | تصویر را با کاهش اندازه آن بر اساس اندازه شکل و وضوح مشخص فشرده می‌کند. به‌صورت انتخابی، نواحی برش‌خورده را نیز حذف می‌نماید. |
| [CompressImage](../../aspose.slides/picturefillformat/compressimage#compressimage)(bool, PicturesCompression) | تصویر را با کاهش اندازه آن بر اساس اندازه شکل و وضوح مشخص فشرده می‌کند. به‌صورت انتخابی، نواحی برش‌خورده را نیز حذف می‌نماید. |
| [DeletePictureCroppedAreas](../../aspose.slides/picturefillformat/deletepicturecroppedareas)() | نواحی برش‌خورده تصویر پرکننده را حذف می‌کند. |
| override [Equals](../../aspose.slides/pviobject/equals)(object) | با شیء مشخص شده مقایسه می‌کند. |
| override [GetHashCode](../../aspose.slides/pviobject/gethashcode)() | کد هش را بازمی‌گرداند. |

### موارد مرتبط

* کلاس [PVIObject](../pviobject)
* رابط [IPictureFillFormat](../ipicturefillformat)
* فضای‌نام [Aspose.Slides](../../aspose.slides)
* مجوعه [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->