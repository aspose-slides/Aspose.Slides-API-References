---
title: PictureFillFormat
second_title: Aspose.Sildes برای مرجع API .NET
description: یک سبک پرکردن تصویر را نمایندگی می‌کند.
type: docs
weight: 9390
url: /fa/aspose.slides/picturefillformat/
---
## PictureFillFormat کلاس

یک سبک پرکردن تصویر را نمایندگی می‌کند.

```csharp
public sealed class PictureFillFormat : PVIObject, IPictureFillFormat
```

## خصوصیات

| Name | Description |
| --- | --- |
| [AsIPresentationComponent](../../aspose.slides/pviobject/asipresentationcomponent) { get; } | به‌دست آوردن رابط پایه IPresentationComponent امکان می‌دهد. فقط-خواندنی [`IPresentationComponent`](../ipresentationcomponent). |
| [CropBottom](../../aspose.slides/picturefillformat/cropbottom) { get; set; } | مقدار درصد ارتفاع واقعی تصویر که از قسمت پایین تصویر برش داده می‌شود را برمی‌گرداند یا تنظیم می‌کند. خواندنی-نوشتنی Single. |
| [CropLeft](../../aspose.slides/picturefillformat/cropleft) { get; set; } | مقدار درصد عرض واقعی تصویر که از سمت چپ تصویر برش داده می‌شود را برمی‌گرداند یا تنظیم می‌کند. خواندنی-نوشتنی Single. |
| [CropRight](../../aspose.slides/picturefillformat/cropright) { get; set; } | مقدار درصد عرض واقعی تصویر که از سمت راست تصویر برش داده می‌شود را برمی‌گرداند یا تنظیم می‌کند. خواندنی-نوشتنی Single. |
| [CropTop](../../aspose.slides/picturefillformat/croptop) { get; set; } | مقدار درصد ارتفاع واقعی تصویر که از قسمت بالای تصویر برش داده می‌شود را برمی‌گرداند یا تنظیم می‌کند. خواندنی-نوشتنی Single. |
| [Dpi](../../aspose.slides/picturefillformat/dpi) { get; set; } | dpi که برای پرکردن تصویر استفاده می‌شود را برمی‌گرداند یا تنظیم می‌کند. خواندنی-نوشتنی Int32. |
| [Picture](../../aspose.slides/picturefillformat/picture) { get; } | تصویر را برمی‌گرداند. فقط-خواندنی [`ISlidesPicture`](../islidespicture). |
| [PictureFillMode](../../aspose.slides/picturefillformat/picturefillmode) { get; set; } | حالت پر کردن تصویر را برمی‌گرداند یا تنظیم می‌کند. خواندنی-نوشتنی [`PictureFillMode`](../picturefillmode). |
| [StretchOffsetBottom](../../aspose.slides/picturefillformat/stretchoffsetbottom) { get; set; } | لبهٔ پایین مستطیل پر کردن که با درصدی نسبت به لبهٔ پایین جعبهٔ محدودهٔ شکل تعریف می‌شود را برمی‌گرداند یا تنظیم می‌کند. درصد مثبت مقدار تو رفتگی داخلی و درصد منفی مقدار برآمدگی را مشخص می‌کند. خواندنی-نوشتنی Single. |
| [StretchOffsetLeft](../../aspose.slides/picturefillformat/stretchoffsetleft) { get; set; } | لبهٔ چپ مستطیل پر کردن که با درصدی نسبت به لبهٔ چپ جعبهٔ محدودهٔ شکل تعریف می‌شود را برمی‌گرداند یا تنظیم می‌کند. درصد مثبت مقدار تو رفتگی داخلی و درصد منفی مقدار برآمدگی را مشخص می‌کند. خواندنی-نوشتنی Single. |
| [StretchOffsetRight](../../aspose.slides/picturefillformat/stretchoffsetright) { get; set; } | لبهٔ راست مستطیل پر کردن که با درصدی نسبت به لبهٔ راست جعبهٔ محدودهٔ شکل تعریف می‌شود را برمی‌گرداند یا تنظیم می‌کند. درصد مثبت مقدار تو رفتگی داخلی و درصد منفی مقدار برآمدگی را مشخص می‌کند. خواندنی-نوشتنی Single. |
| [StretchOffsetTop](../../aspose.slides/picturefillformat/stretchoffsettop) { get; set; } | لبهٔ بالا مستطیل پر کردن که با درصدی نسبت به لبهٔ بالا جعبهٔ محدودهٔ شکل تعریف می‌شود را برمی‌گرداند یا تنظیم می‌کند. درصد مثبت مقدار تو رفتگی داخلی و درصد منفی مقدار برآمدگی را مشخص می‌کند. خواندنی-نوشتنی Single. |
| [TileAlignment](../../aspose.slides/picturefillformat/tilealignment) { get; set; } | نحوهٔ تراز بافت درون شکل را برمی‌گرداند یا تنظیم می‌کند. این تنظیم نقطهٔ شروع الگوی بافت و نحوهٔ تکرار آن را در سراسر شکل کنترل می‌کند. خواندنی-نوشتنی [`RectangleAlignment`](../rectanglealignment). |
| [TileFlip](../../aspose.slides/picturefillformat/tileflip) { get; set; } | کاشی بافت را حول محور افقی، عمودی یا هر دو محور وارون می‌کند. خواندنی-نوشتنی [`TileFlip`](../tileflip). |
| [TileOffsetX](../../aspose.slides/picturefillformat/tileoffsetx) { get; set; } | جابه‌جایی افقی بافت نسبت به مبدأ شکل بر حسب نقاط را برمی‌گرداند یا تنظیم می‌کند. مقدار مثبت بافت را به راست و مقدار منفی به چپ می‌برد. خواندنی-نوشتنی Single. |
| [TileOffsetY](../../aspose.slides/picturefillformat/tileoffsety) { get; set; } | جابه‌جایی عمودی بافت نسبت به مبدأ شکل بر حسب نقاط را برمی‌گرداند یا تنظیم می‌کند. مقدار مثبت بافت را به پایین و مقدار منفی به بالا می‌برد. خواندنی-نوشتنی Single. |
| [TileScaleX](../../aspose.slides/picturefillformat/tilescalex) { get; set; } | مقیاس افقی پر کردن بافت را به صورت درصد برمی‌گرداند یا تنظیم می‌کند. خواندنی-نوشتنی Single. |
| [TileScaleY](../../aspose.slides/picturefillformat/tilescaley) { get; set; } | مقیاس عمودی پر کردن بافت را به صورت درصد برمی‌گرداند یا تنظیم می‌کند. خواندنی-نوشتنی Single. |

## توابع

| Name | Description |
| --- | --- |
| [CompressImage](../../aspose.slides/picturefillformat/compressimage#compressimage_1)(bool, float) | تصویر را با کاهش اندازه آن بر پایهٔ اندازه شکل و وضوح مشخص فشرده می‌کند. به‌صورت اختیاری، نواحی برش خورده را نیز حذف می‌کند. |
| [CompressImage](../../aspose.slides/picturefillformat/compressimage#compressimage)(bool, PicturesCompression) | تصویر را با کاهش اندازه آن بر پایهٔ اندازه شکل و وضوح مشخص فشرده می‌کند. به‌صورت اختیاری، نواحی برش خورده را نیز حذف می‌کند. |
| [DeletePictureCroppedAreas](../../aspose.slides/picturefillformat/deletepicturecroppedareas)() | نواحی برش خوردهٔ تصویر پر شده را حذف می‌کند. |
| override [Equals](../../aspose.slides/pviobject/equals)(object) | با شیء مشخص‌شده مقایسه می‌کند. |
| override [GetHashCode](../../aspose.slides/pviobject/gethashcode)() | کد هش را برمی‌گرداند. |

### مراجع

* کلاس [PVIObject](../pviobject)
* رابط [IPictureFillFormat](../ipicturefillformat)
* فضای‌نامی [Aspose.Slides](../../aspose.slides)
* اسمبلی [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->