---
title: CaptionTracks
second_title: مرجع API Aspose.Sildes برای .NET
description: مجموعۀ زیرنویس‌های بسته مرتبط با قاب صوتی را دریافت می‌کند. این خاصیت فقط‌قابل‌خواندن است و یک ICaptionsCollectionaspose.slides/icaptionscollection حاوی تمام مسیرهای زیرنویس را برمی‌گرداند.
type: docs
weight: 50
url: /fa/aspose.slides/audioframe/captiontracks/
---
## AudioFrame.CaptionTracks خاصیت

مجموعهٔ زیرنویس‌های بسته مرتبط با قاب صوتی را دریافت می‌کند. این خاصیت فقط‌قابل‌خواندن است و یک [`ICaptionsCollection`](../../icaptionscollection) حاوی تمام مسیرهای زیرنویس را برمی‌گرداند.

```csharp
public ICaptionsCollection CaptionTracks { get; }
```

### مثال‌ها

مثال:

```csharp
[C#]
using (Presentation pres = new Presentation("audio with captions.pptx"))
{
    foreach (IShape shape in pres.Slides[0].Shapes)
    {
        if (shape is IAudioFrame audioFrame)
        {
            // داده‌های باینری مسیر زیرنویس را به‌صورت فایل .vtt ذخیره کنید
            foreach (var captionTrack in audioFrame.CaptionTracks)
                File.WriteAllBytes(captionTrack.CaptionId + ".vtt", captionTrack.BinaryData);
        }
    }
}
```

### موارد مرتبط

* رابط [ICaptionsCollection](../../icaptionscollection)
* کلاس [AudioFrame](../../audioframe)
* فضای‌نام [Aspose.Slides](../../audioframe)
* مجموعه [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->