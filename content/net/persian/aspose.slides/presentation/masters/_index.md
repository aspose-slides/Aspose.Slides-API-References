---
title: Masters
second_title: Aspose.Sildes برای .NET مرجع API
description: فهرستی از تمام اسلایدهای اصلی که در ارائه تعریف شده‌اند را بر می‌گرداند. فقط خواندنی IMasterSlideCollectionaspose.slides/imasterslidecollection.
type: docs
weight: 180
url: /fa/aspose.slides/presentation/masters/
---
## Presentation.Masters خصوصیت

لیست تمام اسلایدهای اصلی که در ارائه تعریف شده‌اند را برمی‌گرداند. فقط خواندنی [`IMasterSlideCollection`](../../imasterslidecollection).

```csharp
public IMasterSlideCollection Masters { get; }
```

### مثال‌ها

مثال‌های زیر نشان می‌دهند چگونه تصاویر را به اسلایدهای اصلی ارائه PowerPoint اضافه کنید.

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    ISlide slide = pres.Slides[0];
    IMasterSlide masterSlide = slide.LayoutSlide.MasterSlide;
    IPPImage image = pres.Images.AddImage(File.ReadAllBytes("image.png"));
    masterSlide.Shapes.AddPictureFrame(ShapeType.Rectangle, 10, 10, 100, 100, image);
    pres.Save("pres.pptx", SaveFormat.Pptx);
}
```

مثال‌های زیر نشان می‌دهند چگونه رنگ پس‌زمینه اسلاید اصلی ارائه PowerPoint را تغییر دهید.

```csharp
[C#]
// نمونه‌سازی کلاس Presentation که نمایانگر فایل ارائه است
using (Presentation pres = new Presentation())
{
    // رنگ پس‌زمینه اسلاید اصلی (Master ISlide) را به سبز جنگلی تنظیم کنید
    pres.Masters[0].Background.Type = BackgroundType.OwnBackground;
    pres.Masters[0].Background.FillFormat.FillType = FillType.Solid;
    pres.Masters[0].Background.FillFormat.SolidFillColor.Color = Color.ForestGreen;
    // ذخیرهٔ ارائه بر روی دیسک
    pres.Save("SetSlideBackgroundMaster_out.pptx", SaveFormat.Pptx);
}
```

مثال‌های زیر نشان می‌دهند چگونه طرح‌بندی اسلاید را به ارائه PowerPoint اضافه کنید.

```csharp
[C#]
// نمونه‌سازی کلاس Presentation که نمایانگر فایل ارائه است
using (Presentation presentation = new Presentation("AccessSlides.pptx"))
{
    // تلاش برای جستجو بر اساس نوع اسلاید چیدمان
    IMasterLayoutSlideCollection layoutSlides = presentation.Masters[0].LayoutSlides;
    ILayoutSlide layoutSlide = layoutSlides.GetByType(SlideLayoutType.TitleAndObject) ?? layoutSlides.GetByType(SlideLayoutType.Title);
    if (layoutSlide == null)
    {
        // وضعیت زمانی که ارائه حاوی برخی انواع چیدمان‌ها نیست.
        // فایل ارائه فقط شامل انواع چیدمان Blank و Custom است.
        // اما اسلایدهای چیدمان با انواع سفارشی نام‌های متفاوتی دارند،
        // مانند "Title"، "Title and Content" و غیره. و امکان استفاده از این
        // نام‌ها برای انتخاب اسلاید چیدمان وجود دارد.
        // همچنین می‌توان مجموعه‌ای از انواع اشکال placeholder را استفاده کرد. برای مثال،
        // اسلاید عنوان باید فقط نوع placeholder عنوان را داشته باشد، و غیره.
        foreach (ILayoutSlide titleAndObjectLayoutSlide in layoutSlides)
        {
            if (titleAndObjectLayoutSlide.Name == "Title and Object")
            {
                layoutSlide = titleAndObjectLayoutSlide;
                break;
            }
        }
        if (layoutSlide == null)
        {
            foreach (ILayoutSlide titleLayoutSlide in layoutSlides)
            {
                if (titleLayoutSlide.Name == "Title")
                {
                    layoutSlide = titleLayoutSlide;
                    break;
                }
            }
            if (layoutSlide == null)
            {
                layoutSlide = layoutSlides.GetByType(SlideLayoutType.Blank);
                if (layoutSlide == null)
                {
                    layoutSlide = layoutSlides.Add(SlideLayoutType.TitleAndObject, "Title and Object");
                }
            }
        }
    }
    // افزودن اسلاید خالی با اسلاید چیدمان اضافه شده
    presentation.Slides.InsertEmptySlide(0, layoutSlide);
    // ذخیرهٔ ارائه
    presentation.Save("AddLayoutSlides_out.pptx", SaveFormat.Pptx);
}
```

### موارد مرتبط

* رابط [IMasterSlideCollection](../../imasterslidecollection)
* کلاس [Presentation](../../presentation)
* فضای‌نام [Aspose.Slides](../../presentation)
* مجمع [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->