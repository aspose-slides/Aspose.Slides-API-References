---
title: SetGeometryPaths
second_title: Aspose.Sildes برای .NET API Reference
description: هندسهٔ شکل را از آرایه‌ای از IGeometryPathaspose.slides/igeometrypath به‌روز می‌کند. مختصات باید نسبت به گوشهٔ بالا-چپ شکل باشد. نوع شکل ShapeTypeaspose.slides/geometryshape/shapetype را به Custom تغییر می‌دهد.
type: docs
weight: 70
url: /fa/aspose.slides/geometryshape/setgeometrypaths/
---
## GeometryShape.SetGeometryPaths متد

از آرایه‌ای از [`IGeometryPath`](../../igeometrypath) هندسهٔ شکل را به‌روز می‌کند. مختصات باید نسبت به گوشهٔ بالا-چپ شکل باشد. نوع شکل ([`ShapeType`](../shapetype)) را به Custom تغییر می‌دهد.

```csharp
public void SetGeometryPaths(IGeometryPath[] geometryPaths)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| geometryPaths | IGeometryPath[] | آرایهٔ مسیرهای هندسی |

### استثناها

| استثنا | شرط |
| --- | --- |
| ArgumentException | مسیر یافت نشد |
| ArgumentException | مسیر خالی |

### مثال‌ها

مثال:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    GeometryShape shape = pres.Slides[0].Shapes.AddAutoShape(ShapeType.Rectangle, 100, 100, 200, 100) as GeometryShape;

    GeometryPath geometryPath = shape.GetGeometryPaths()[0];

    geometryPath.LineTo(100, 50, 1);
    geometryPath.LineTo(100, 50, 4);

    shape.SetGeometryPath(geometryPath);

    pres.Save("output.pptx", SaveFormat.Pptx);
}
```

### موارد مرتبط

* رابط [IGeometryPath](../../igeometrypath)
* کلاس [GeometryShape](../../geometryshape)
* فضای‌نام [Aspose.Slides](../../geometryshape)
* مجتمع [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->