---
title: NotesSlideManager
second_title: Aspose.Sildes برای .NET مرجع API
description: مدیر اسلاید یادداشت‌ها.
type: docs
weight: 9160
url: /fa/aspose.slides/notesslidemanager/
---
## NotesSlideManager کلاس

مدیر اسلاید یادداشت‌ها.

```csharp
public sealed class NotesSlideManager : DomObject<Slide>, INotesSlideManager
```

## خصوصیات

| نام | توضیح |
| --- | --- |
| [NotesSlide](../../aspose.slides/notesslidemanager/notesslide) { get; } | اسلاید یادداشت‌ها را برای اسلاید فعلی بر می‌گرداند. اگر اسلاید یادداشت نداشته باشد، مقدار null بر می‌گرداند. فقط-خواندنی [`INotesSlide`](../inotesslide). |

## متدها

| نام | توضیح |
| --- | --- |
| [AddNotesSlide](../../aspose.slides/notesslidemanager/addnotesslide)() | اسلاید یادداشت‌ها را برای اسلاید فعلی بر می‌گرداند و در صورت عدم وجود، یک اسلاید جدید ایجاد می‌کند. |
| [RemoveNotesSlide](../../aspose.slides/notesslidemanager/removenotesslide)() | اسلاید یادداشت‌های اسلاید فعلی را حذف می‌کند. |

### مثال‌ها

مثال زیر نشان می‌دهد چگونه می‌توان به اسلاید خاص یک ارائه PowerPoint یادداشت‌ها را اضافه کرد.

```csharp
[C#]
	// یک شی Presentation که یک فایل ارائه را نشان می‌دهد را ایجاد می‌کند
	using(Presentation presentation = new Presentation(dataDir + "AccessSlides.pptx")) {
	  // یادداشت‌ها را به اسلاید اول اضافه کنید
	  INotesSlideManager mgr = presentation.Slides[0].NotesSlideManager;
	  INotesSlide noteSlide = mgr.AddNotesSlide();
	  noteSlide.NotesTextFrame.Text = "Your Notes";
	  // ارائه را روی دیسک ذخیره کنید
	  presentation.Save(dataDir + "RemoveNotesAtSpecificSlide_out.pptx", SaveFormat.Pptx);
	}
```

مثال زیر نشان می‌دهد چگونه می‌توان یادداشت‌ها را از اسلاید خاص یک ارائه PowerPoint حذف کرد.

```csharp
[C#]
	// یک شی Presentation که یک فایل ارائه را نشان می‌دهد را ایجاد می‌کند
	using(Presentation presentation = new Presentation(dataDir + "AccessSlides.pptx")) {
	  // حذف یادداشت‌های اسلاید اول
	  INotesSlideManager mgr = presentation.Slides[0].NotesSlideManager;
	  mgr.RemoveNotesSlide();
	  // ارائه را روی دیسک ذخیره کنید
	  presentation.Save(dataDir + "RemoveNotesAtSpecificSlide_out.pptx", SaveFormat.Pptx);
	}
```

### موارد مرتبط

* کلاس [DomObject&lt;TParent&gt;](../domobject-1)
* کلاس [Slide](../slide)
* رابط [INotesSlideManager](../inotesslidemanager)
* فضای‌نام [Aspose.Slides](../../aspose.slides)
* اسمبلی [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->