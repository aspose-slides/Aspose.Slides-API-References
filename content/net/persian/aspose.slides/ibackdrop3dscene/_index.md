---
title: IBackdrop3DScene
second_title: Aspose.Sildes برای .NET مرجع API
description: یک صفحه را تعریف می‌کند که اثراتی مانند درخشش و سایه در رابطه با شکلی که به آن اعمال می‌شود، اعمال می‌شود.
type: docs
weight: 5260
url: /fa/aspose.slides/ibackdrop3dscene/
---
## IBackdrop3DScene رابط

یک صفحه را تعریف می‌کند که اثراتی مانند درخشش و سایه در رابطه با شکلی که به آن اعمال می‌شود، اعمال می‌شوند.

```csharp
public interface IBackdrop3DScene
```

## ویژگی‌ها

| نام | توضیح |
| --- | --- |
| [AnchorPoint](../../aspose.slides/ibackdrop3dscene/anchorpoint) { get; set; } | یک نقطه در فضای 3D را برمی‌گرداند یا تنظیم می‌کند. این نقطه، نقطه‌ای در فضا است که صفحهٔ بک‌دراپ را ثابت می‌کند. نقطهٔ 3D توسط آرایه‌ای از ۳ مقدار float که مختصات X، Y و Z را تعریف می‌کند، نشان داده می‌شود. خواند/نوشت Single[]. |
| [NormalVector](../../aspose.slides/ibackdrop3dscene/normalvector) { get; set; } | یک بردار نرمال را برمی‌گرداند یا تنظیم می‌کند. برای دقیق‌تر، این ویژگی یک بردار را تعریف می‌کند که عمود بر سطح صفحهٔ بک‌دراپ است. بردار توسط آرایه‌ای از ۳ مقدار float که مختصات X، Y و Z را تعیین می‌کند، نشان داده می‌شود. خواند/نوشت Single[]. |
| [UpVector](../../aspose.slides/ibackdrop3dscene/upvector) { get; set; } | یک بردار نشان‌دهندهٔ جهت بالا را برمی‌گرداند یا تنظیم می‌کند. برای دقیق‌تر، این ویژگی یک بردار را تعریف می‌کند که جهت بالا را نسبت به سطح صفحهٔ بک‌دراپ نشان می‌دهد. بردار توسط آرایه‌ای از ۳ مقدار float که مختصات X، Y و Z را تعیین می‌کند، نشان داده می‌شود. خواند/نوشت Single[]. |

### موارد مرتبط

* فضای‌نام [Aspose.Slides](../../aspose.slides)
* مجمع [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->