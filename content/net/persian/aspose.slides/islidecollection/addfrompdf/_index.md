---
title: AddFromPdf
second_title: Aspose.Sildes برای .NET - مرجع API
description: اسلایدها را از سند PDF ایجاد می‌کند و به انتهای مجموعه اضافه می‌نماید.
type: docs
weight: 50
url: /fa/aspose.slides/islidecollection/addfrompdf/
---
## AddFromPdf(string) {#addfrompdf_2}

اسلایدها را از سند PDF ایجاد می‌کند و به انتهای مجموعه اضافه می‌نماید.

```csharp
public ISlide[] AddFromPdf(string path)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| path | String | مسیر به سند PDF |

### مقدار بازگشتی

اسلایدهای اضافه شده

### مثال‌ها

مثال:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    pres.Slides.AddFromPdf("document.pdf");
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### موارد مرتبط

* رابط [ISlide](../../islide)
* رابط [ISlideCollection](../../islidecollection)
* فضای‌نام [Aspose.Slides](../../islidecollection)
* مونتاژ [Aspose.Slides](../../../)

---

## AddFromPdf(string, PdfImportOptions) {#addfrompdf_3}

اسلایدها را از سند PDF ایجاد می‌کند و با در نظر گرفتن گزینه‌های وارد کردن PDF به انتهای مجموعه اضافه می‌نماید.

```csharp
public ISlide[] AddFromPdf(string path, PdfImportOptions pdfImportOptions)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| path | String | مسیر به سند PDF |
| pdfImportOptions | PdfImportOptions | گزینه‌های وارد کردن PDF |

### مقدار بازگشتی

اسلایدهای اضافه شده

### مثال‌ها

مثال:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    pres.Slides.AddFromPdf("document.pdf, new PdfImportOptions { DetectTables = true });
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### موارد مرتبط

* رابط [ISlide](../../islide)
* کلاس [PdfImportOptions](../../../aspose.slides.import/pdfimportoptions)
* رابط [ISlideCollection](../../islidecollection)
* فضای‌نام [Aspose.Slides](../../islidecollection)
* مونتاژ [Aspose.Slides](../../../)

---

## AddFromPdf(Stream, PdfImportOptions) {#addfrompdf_1}

اسلایدها را از سند PDF ایجاد می‌کند و به انتهای مجموعه اضافه می‌نماید.

```csharp
public ISlide[] AddFromPdf(Stream pdfStream, PdfImportOptions pdfImportOptions)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| pdfStream | Stream | جریانی که به عنوان منبع سند PDF استفاده می‌شود |
| pdfImportOptions | PdfImportOptions | گزینه‌های وارد کردن PDF |

### مقدار بازگشتی

اسلایدهای اضافه شده

### مثال‌ها

مثال:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    using (Stream stream = new FileStream("document.pdf", FileMode.Open, FileAccess.Read, FileShare.Read))
    {
        pres.Slides.AddFromPdf(stream, new PdfImportOptions { DetectTables = true });
    }
    
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### موارد مرتبط

* رابط [ISlide](../../islide)
* کلاس [PdfImportOptions](../../../aspose.slides.import/pdfimportoptions)
* رابط [ISlideCollection](../../islidecollection)
* فضای‌نام [Aspose.Slides](../../islidecollection)
* مونتاژ [Aspose.Slides](../../../)

---

## AddFromPdf(Stream) {#addfrompdf}

اسلایدها را از سند PDF ایجاد می‌کند و به انتهای مجموعه اضافه می‌نماید.

```csharp
public ISlide[] AddFromPdf(Stream pdfStream)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| pdfStream | Stream | جریانی که به عنوان منبع سند PDF استفاده می‌شود |

### مقدار بازگشتی

اسلایدهای اضافه شده

### مثال‌ها

مثال:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    using (Stream stream = new FileStream("document.pdf", FileMode.Open, FileAccess.Read, FileShare.Read))
    {
        pres.Slides.AddFromPdf(stream);
    }
    
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### موارد مرتبط

* رابط [ISlide](../../islide)
* رابط [ISlideCollection](../../islidecollection)
* فضای‌نام [Aspose.Slides](../../islidecollection)
* مونتاژ [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->