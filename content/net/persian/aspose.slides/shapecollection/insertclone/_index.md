---
title: InsertClone
second_title: Aspose.Sildes برای .NET مرجع API
description: یک کپی از شکل مشخص‌شده ایجاد می‌کند و آن را در مجموعهٔ اشکال در اندیس مشخص‌شده وارد می‌سازد.
type: docs
weight: 320
url: /fa/aspose.slides/shapecollection/insertclone/
---
## InsertClone(int, IShape, float, float, float, float) {#insertclone_2}

یک نسخهٔ کپی از شکل مشخص‌شده ایجاد می‌کند و آن را در مجموعهٔ اشکال در اندیس مشخص‌شده وارد می‌کند.

```csharp
public IShape InsertClone(int index, IShape sourceShape, float x, float y, float width, 
    float height)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| index | Int32 | اندیس صفر-پایه‌ای که برای وارد کردن شکل کپی‌شده استفاده می‌شود. |
| sourceShape | IShape | [`IShape`](../../ishape) برای کپی. |
| x | Single | مختصات x چارچوب شکل کپی‌شده، به نقطه. |
| y | Single | مختصات y چارچوب شکل کپی‌شده، به نقطه. |
| width | Single | عرض چارچوب شکل کپی‌شده، به نقطه. |
| height | Single | ارتفاع چارچوب شکل کپی‌شده، به نقطه. |

### مقدار بازگشتی

[`IShape`](../../ishape) جدیداً ساخته‌شده.

### موارد مرتبط

* رابط [IShape](../../ishape)
* کلاس [ShapeCollection](../../shapecollection)
* فضای نام [Aspose.Slides](../../shapecollection)
* مونتاژ [Aspose.Slides](../../../)

---

## InsertClone(int, IShape, float, float) {#insertclone_1}

یک نسخهٔ کپی از شکل مشخص‌شده ایجاد می‌کند و آن را در مجموعهٔ اشکال در اندیس مشخص‌شده وارد می‌کند. شکل جدید عرض و ارتفاع *sourceShape* را حفظ می‌کند.

```csharp
public IShape InsertClone(int index, IShape sourceShape, float x, float y)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| index | Int32 | اندیس صفر-پایه‌ای که برای وارد کردن شکل کپی‌شده استفاده می‌شود. |
| sourceShape | IShape | [`IShape`](../../ishape) برای کپی. |
| x | Single | مختصات x چارچوب شکل کپی‌شده، به نقطه. |
| y | Single | مختصات y چارچوب شکل کپی‌شده، به نقطه. |

### مقدار بازگشتی

[`IShape`](../../ishape) جدیداً ساخته‌شده.

### موارد مرتبط

* رابط [IShape](../../ishape)
* کلاس [ShapeCollection](../../shapecollection)
* فضای نام [Aspose.Slides](../../shapecollection)
* مونتاژ [Aspose.Slides](../../../)

---

## InsertClone(int, IShape) {#insertclone}

یک نسخهٔ کپی از شکل مشخص‌شده ایجاد می‌کند و آن را در مجموعهٔ اشکال در اندیس مشخص‌شده وارد می‌کند. شکل کپی‌شده موقعیت و اندازهٔ اصلی را حفظ می‌کند.

```csharp
public IShape InsertClone(int index, IShape sourceShape)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| index | Int32 | اندیس صفر-پایه‌ای که برای وارد کردن شکل کپی‌شده استفاده می‌شود. |
| sourceShape | IShape | [`IShape`](../../ishape) برای کپی. |

### مقدار بازگشتی

[`IShape`](../../ishape) جدیداً ساخته‌شده.

### موارد مرتبط

* رابط [IShape](../../ishape)
* کلاس [ShapeCollection](../../shapecollection)
* فضای نام [Aspose.Slides](../../shapecollection)
* مونتاژ [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->