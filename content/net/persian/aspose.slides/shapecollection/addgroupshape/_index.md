---
title: AddGroupShape
second_title: Aspose.Sildes برای .NET - مرجع API
description: یک شکل گروه خالی جدید ایجاد می‌کند و آن را به انتهای مجموعهٔ اشکال اضافه می‌نماید. چارچوب گروه به‌طور خودکار تنظیم می‌شود تا هر شکلی که به آن اضافه شود را در بر گیرد.
type: docs
weight: 130
url: /fa/aspose.slides/shapecollection/addgroupshape/
---
## AddGroupShape() {#addgroupshape}

یک شکل گروه خالی جدید ایجاد می‌کند و آن را به انتهای مجموعهٔ شکل‌ها اضافه می‌نماید. چارچوب گروه به‌صورت خودکار تنظیم می‌شود تا هر شکلی که به آن اضافه شود را دربر گیرد.

```csharp
public IGroupShape AddGroupShape()
```

### مقدار بازگشت

‏[`IGroupShape`](../../igroupshape) به‌تازگی ایجاد شد.

### مثال‌ها

مثال زیر نشان می‌دهد چگونه یک شکل گروه به یک اسلاید از ارائهٔ پاورپوینت اضافه می‌شود.

```csharp
[C#]
// نمونه‌سازی کلاس Presentation
using (Presentation pres = new Presentation())
{
    // دریافت اولین اسلاید
    ISlide sld = pres.Slides[0];
    // دسترسی به مجموعهٔ اشکال اسلایدها
    IShapeCollection slideShapes = sld.Shapes;
    // افزودن یک شکل گروه به اسلاید
    IGroupShape groupShape = slideShapes.AddGroupShape();
    // افزودن اشکال به داخل شکل گروه اضافه شده
    groupShape.Shapes.AddAutoShape(ShapeType.Rectangle, 300, 100, 100, 100);
    groupShape.Shapes.AddAutoShape(ShapeType.Rectangle, 500, 100, 100, 100);
    groupShape.Shapes.AddAutoShape(ShapeType.Rectangle, 300, 300, 100, 100);
    groupShape.Shapes.AddAutoShape(ShapeType.Rectangle, 500, 300, 100, 100);
    // افزودن چارچوب شکل گروه
    groupShape.Frame = new ShapeFrame(100, 300, 500, 40, NullableBool.False, NullableBool.False, 0);
    // نوشتن فایل PPTX روی دیسک
    pres.Save("GroupShape_out.pptx", SaveFormat.Pptx);
}
```

### موارد مرتبط

* رابط [IGroupShape](../../igroupshape)
* کلاس [ShapeCollection](../../shapecollection)
* فضای‌نام [Aspose.Slides](../../shapecollection)
* مجمع [Aspose.Slides](../../../)

---

## AddGroupShape(ISvgImage, float, float, float, float) {#addgroupshape_1}

یک شکل گروه جدید ایجاد می‌کند، تصویر SVG مشخص‌شده را به اشکال جداگانه تبدیل می‌نماید و گروه حاصل را به انتهای مجموعهٔ شکل‌ها اضافه می‌کند.

```csharp
public IGroupShape AddGroupShape(ISvgImage svgImage, float x, float y, float width, float height)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| svgImage | ISvgImage | ‏[`ISvgImage`](../../isvgimage) شامل محتوای وکتور برای تبدیل به اشکال. |
| x | Single | مختصات x چارچوب گروه، بر حسب نقطه. |
| y | Single | مختصات y چارچوب گروه، بر حسب نقطه. |
| width | Single | عرض چارچوب گروه، بر حسب نقطه. |
| height | Single | ارتفاع چارچوب گروه، بر حسب نقطه. |

### مقدار بازگشت

‏[`IGroupShape`](../../igroupshape) به‌تازگی ایجاد شد.

### موارد مرتبط

* رابط [IGroupShape](../../igroupshape)
* رابط [ISvgImage](../../isvgimage)
* کلاس [ShapeCollection](../../shapecollection)
* فضای‌نام [Aspose.Slides](../../shapecollection)
* مجمع [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->