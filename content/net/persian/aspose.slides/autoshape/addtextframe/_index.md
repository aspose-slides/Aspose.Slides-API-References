---
title: AddTextFrame
second_title: Aspose.Sildes برای .NET مرجع API
description: یک TextFrame جدید به یک شکل اضافه می‌کند. اگر شکل قبلاً TextFrame داشته باشد، به سادگی متن آن را تغییر می‌دهد.
type: docs
weight: 60
url: /fa/aspose.slides/autoshape/addtextframe/
---
## AutoShape.AddTextFrame متد

یک TextFrame جدید به یک شکل اضافه می‌کند. اگر شکل قبلاً TextFrame داشته باشد، به سادگی متن آن را تغییر می‌دهد.

```csharp
public ITextFrame AddTextFrame(string text)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| text | String | متن پیش‌فرض برای یک TextFrame جدید. |

### نمونه‌ها

کد نمونه زیر نشان می‌دهد که چگونه متن علامت‌آبی را در ارائه PowerPoint Presentation اضافه کنید.

```csharp
[C#]
	using (var presentation = new Presentation())
	{
		ISlide slide = presentation.Slides[0];
		IAutoShape watermarkShape = slide.Shapes.AddAutoShape(ShapeType.Triangle, 0, 0, 150, 50);
		ITextFrame watermarkTextFrame = watermarkShape.AddTextFrame("Watermark");
	}
```

مثال زیر نشان می‌دهد چگونه یک Text Box روی Slide ایجاد کنید.

```csharp
[C#]
// نمونه‌سازی Presentation
using (Presentation pres = new Presentation())
{
    // اسلاید اول ارائه را دریافت می‌کند
    ISlide sld = pres.Slides[0];
    // یک AutoShape با نوع Rectangle اضافه می‌کند
    IAutoShape ashp = sld.Shapes.AddAutoShape(ShapeType.Rectangle, 150, 75, 150, 50);
    // TextFrame را به Rectangle اضافه می‌کند
    ashp.AddTextFrame(" ");
    // به TextFrame دسترسی می‌یابد
    ITextFrame txtFrame = ashp.TextFrame;
    // شی Paragraph برای TextFrame ایجاد می‌کند
    IParagraph para = txtFrame.Paragraphs[0];
    // شی Portion برای Paragraph ایجاد می‌کند
    IPortion portion = para.Portions[0];
    // متن را تنظیم می‌کند
    portion.Text = "Aspose TextBox";
    // ارائه را روی دیسک ذخیره می‌کند
    pres.Save("TextBox_out.pptx", Aspose.Slides.Export.SaveFormat.Pptx);
}
```

مثال زیر نشان می‌دهد چگونه یک ستون را در Text Box اضافه کنید.

```csharp
[C#]
using (Presentation presentation = new Presentation())
{
	// اسلاید اول ارائه را دریافت می‌کند
	ISlide slide = presentation.Slides[0];
	// یک AutoShape با نوع Rectangle اضافه می‌کند
	IAutoShape aShape = slide.Shapes.AddAutoShape(ShapeType.Rectangle, 100, 100, 300, 300);
	// TextFrame را به Rectangle اضافه می‌کند
	aShape.AddTextFrame("All these columns are limited to be within a single text container -- " +
	"you can add or delete text and the new or remaining text automatically adjusts " +
	"itself to flow within the container. You cannot have text flow from one container " +
	"to other though -- we told you PowerPoint's column options for text are limited!");
	// قالب متن TextFrame را دریافت می‌کند
	ITextFrameFormat format = aShape.TextFrame.TextFrameFormat;
	// تعداد ستون‌ها در TextFrame را مشخص می‌کند
	format.ColumnCount = 3;
	// فاصله بین ستون‌ها را مشخص می‌کند
	format.ColumnSpacing = 10;
	// ارائه را ذخیره می‌کند
	presentation.Save("ColumnCount.pptx", SaveFormat.Pptx);
}
```

### موارد مرتبط

* رابط [ITextFrame](../../itextframe)
* کلاس [AutoShape](../../autoshape)
* فضای‌نام [Aspose.Slides](../../autoshape)
* مجمع [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->