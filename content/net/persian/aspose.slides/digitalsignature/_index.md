---
title: DigitalSignature
second_title: Aspose.Sildes برای .NET مرجع API
description: امضای دیجیتال در فایل امضاشده.
type: docs
weight: 2770
url: /fa/aspose.slides/digitalsignature/
---
## DigitalSignature کلاس

امضای دیجیتال در فایل امضاشده.

```csharp
public class DigitalSignature : IDigitalSignature
```

## سازنده‌ها

| نام | توضیح |
| --- | --- |
| [DigitalSignature](digitalsignature#constructor)(X509Certificate2) | یک شی DigitalSignature جدید با گواهی‌نامهٔ مشخص‌شده ایجاد می‌کند. |
| [DigitalSignature](digitalsignature#constructor_1)(string, string) | یک شی DigitalSignature جدید با مسیر فایل گواهی‌نامه و رمز عبور مشخص‌شده ایجاد می‌کند. |

## خصوصیات

| نام | توضیح |
| --- | --- |
| [Certificate](../../aspose.slides/digitalsignature/certificate) { get; } | شی گواهی‌نامه‌ای که برای امضای سند استفاده شده است. فقط خواندنی X509Certificate2. |
| [Comments](../../aspose.slides/digitalsignature/comments) { get; set; } | هدف امضا. قابل خواندن و نوشتن String. |
| [IsValid](../../aspose.slides/digitalsignature/isvalid) { get; } | اگر این امضای دیجیتال معتبر باشد و سند دست‌کاری نشده باشد، این مقدار true خواهد بود. فقط خواندنی Boolean. |
| [SignTime](../../aspose.slides/digitalsignature/signtime) { get; } | زمانی که سند امضا شده است. فقط خواندنی DateTime. |

### مثال‌ها

مثال زیر نشان می‌دهد چگونه می‌توان امضای دیجیتال را از یک گواهی PFX به ارائهٔ PowerPoint اضافه کرد.

```csharp
[C#]
// نمونه Presentation را مقداردهی اولیه کنید
using (Presentation pres = new Presentation())
{
    // شی DigitalSignature را با فایل PFX و رمز عبور PFX ایجاد کنید
    DigitalSignature signature = new DigitalSignature("testsignature1.pfx", @"testpass1");
    // ثبت نظر برای امضای دیجیتال جدید
    signature.Comments = "Aspose.Slides digital signing test.";
    // امضای دیجیتال را به ارائه اضافه کنید
    pres.DigitalSignatures.Add(signature);
    // ذخیرهٔ ارائه
    pres.Save("SomePresentationSigned.pptx", SaveFormat.Pptx);
}
```

کد نمونهٔ زیر نشان می‌دهد چگونه می‌توان اعتبار امضای دیجیتال ارائهٔ PowerPoint را بررسی کرد.

```csharp
[C#]
// مقداردهی اولیهٔ نمونه Presentation
using (Presentation pres = new Presentation("SomePresentationSigned.pptx"))
{
    if (pres.DigitalSignatures.Count < 0)
    {
        bool allSignaturesAreValid = true;
        Console.WriteLine("Signatures used to sign the presentation: ");
        // بررسی صحت تمام امضای دیجیتال
        foreach (DigitalSignature signature in pres.DigitalSignatures)
        {
            Console.WriteLine(signature.Certificate.SubjectName.Name + ", "
                    + signature.SignTime.ToString("yyyy-MM-dd HH:mm") + " -- " + (signature.IsValid ? "VALID" : "INVALID"));
            allSignaturesAreValid &= signature.IsValid;
        }
        if (allSignaturesAreValid)
            Console.WriteLine("Presentation is genuine, all signatures are valid.");
        else
            Console.WriteLine("Presentation has been modified since signing.");
    }
}
```

### موارد مرتبط

* رابط [IDigitalSignature](../idigitalsignature)
* فضای‌نام [Aspose.Slides](../../aspose.slides)
* اسمبلی [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->