---
title: DigitalSignature
second_title: Aspose.Sildes برای .NET مرجع API
description: امضای دیجیتال در فایل امضا شده.
type: docs
weight: 2770
url: /fa/aspose.slides/digitalsignature/
---
## کلاس DigitalSignature

امضای دیجیتال در فایل امضا شده.

```csharp
public class DigitalSignature : IDigitalSignature
```

## سازنده‌ها

| نام | توضیح |
| --- | --- |
| [DigitalSignature](digitalsignature#constructor)(X509Certificate2) | یک شیء DigitalSignature جدید با گواهی‌نامه مشخص‌شده ایجاد می‌کند. |
| [DigitalSignature](digitalsignature#constructor_1)(string, string) | یک شیء DigitalSignature جدید با مسیر فایل گواهی‌نامه و گذرواژه مشخص‌شده ایجاد می‌کند. |

## ویژگی‌ها

| نام | توضیح |
| --- | --- |
| [Certificate](../../aspose.slides/digitalsignature/certificate) { get; } | شیء گواهی‌نامه‌ای که برای امضای سند استفاده شده است. فقط‌خواندنی X509Certificate2. |
| [Comments](../../aspose.slides/digitalsignature/comments) { get; set; } | هدف امضا. خواندنی/نوشتنی String. |
| [IsValid](../../aspose.slides/digitalsignature/isvalid) { get; } | اگر این امضای دیجیتال معتبر باشد و سند دست‌کاری نشده باشد، این مقدار true خواهد بود. فقط‌خواندنی Boolean. |
| [SignTime](../../aspose.slides/digitalsignature/signtime) { get; } | زمانی که سند امضا شده است. فقط‌خواندنی DateTime. |

### مثال‌ها

مثال زیر نشان می‌دهد چگونه امضای دیجیتال را از یک گواهی‌نامه PFX در ارائه PowerPoint اضافه کنیم.

```csharp
[C#]
// نمونه Presentation را مقداردهی اولیه کنید
using (Presentation pres = new Presentation())
{
    // شی DigitalSignature را با فایل PFX و گذرواژه PFX ایجاد کنید
    DigitalSignature signature = new DigitalSignature("testsignature1.pfx", @"testpass1");
    // توضیح برای امضای دیجیتال جدید
    signature.Comments = "Aspose.Slides digital signing test.";
    // افزودن امضای دیجیتال به ارائه
    pres.DigitalSignatures.Add(signature);
    // ذخیره ارائه
    pres.Save("SomePresentationSigned.pptx", SaveFormat.Pptx);
}
```

کد نمونه زیر نشان می‌دهد چگونه امضای دیجیتال ارائه PowerPoint را اعتبارسنجی کنیم.

```csharp
[C#]
// نمونه Presentation را مقداردهی اولیه کنید
using (Presentation pres = new Presentation("SomePresentationSigned.pptx"))
{
    if (pres.DigitalSignatures.Count < 0)
    {
        bool allSignaturesAreValid = true;
        Console.WriteLine("Signatures used to sign the presentation: ");
        // بررسی کنید آیا همه امضاهای دیجیتال معتبر هستند
        foreach (DigitalSignature signature in pres.DigitalSignatures)
        {
            Console.WriteLine(signature.Certificate.SubjectName.Name + ", "
                    + signature.SignTime.ToString("yyyy-MM-dd HH:mm") + " -- " + (signature.IsValid ? "VALID" : "INVALID"));
            allSignaturesAreValid &= signature.IsValid;
        }
        if (allSignaturesAreValid)
            Console.WriteLine("Presentation is genuine, all signatures are valid.");
        else
            Console.WriteLine("Presentation has been modified since signing.");
    }
}
```

### موارد مرتبط

* رابط [IDigitalSignature](../idigitalsignature)
* فضای‌نام [Aspose.Slides](../../aspose.slides)
* مجتمع [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->