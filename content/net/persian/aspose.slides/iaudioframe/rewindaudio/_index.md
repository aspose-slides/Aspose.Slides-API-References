---
title: RewindAudio
second_title: Aspose.Sildes برای .NET مرجع API
description: مشخص می‌کند که آیا صدا پس از پخش به‌صورت خودکار به ابتدا بازگردانده می‌شود یا خیر. خواندنی/نوشتنی Boolean.
type: docs
weight: 160
url: /fa/aspose.slides/iaudioframe/rewindaudio/
---
## IAudioFrame.RewindAudio ویژگی

مشخص می‌کند که آیا صدا به‌صورت خودکار پس از پخش به ابتداء بازگردانده می‌شود یا خیر. خواندنی/نوشتنی Boolean.

```csharp
public bool RewindAudio { get; set; }
```

### مثال‌ها

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    ISlide slide = pres.Slides[0];

    // افزودن فریم صوتی
    IAudioFrame audioFrame = slide.Shapes.AddAudioFrameLinked(50, 50, 100, 100, "sampleaudio.wav");

    // تنظیم صدا برای پخش در تمام اسلایدها
    audioFrame.PlayAcrossSlides = true;

    // تنظیم صدا برای بازگشت خودکار به ابتدا پس از پخش
    audioFrame.RewindAudio = true;

    pres.Save("AudioFrame_out.pptx", SaveFormat.Pptx);
}
```

### موارد مرتبط

* رابط [IAudioFrame](../../iaudioframe)
* فضای‌نام [Aspose.Slides](../../iaudioframe)
* مونتاژ [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->