---
title: ColorTransformOperation
second_title: مرجع API Aspose.Sildes برای .NET
description: عملیات تبدیل رنگ را تعریف می‌کند.
type: docs
weight: 2570
url: /fa/aspose.slides/colortransformoperation/
---
## ColorTransformOperation enumeration

Defines color transform operation.

```csharp
public enum ColorTransformOperation
```

### مقادیر

| نام | مقدار | توضیح |
| --- | --- | --- |
| Tint | `0` | رنگ را روشن می‌کند. پارامتر در بازه 0 (رنگ اصلی) تا 1 (سفید) قرار دارد. |
| Shade | `1` | رنگ را تیره می‌کند. پارامتر در بازه 0 (رنگ اصلی) تا 1 (سیاه) قرار دارد. |
| Complement | `2` | رنگ را به یک رنگ مکمل RGB تغییر می‌دهد. m = Max(r, g, b); r = m - r; g = m - g; b = m - b; |
| Inverse | `3` | رنگ را به رنگ معکوس تغییر می‌دهد. r = 1 - r; g = 1 - g; b = 1 - b; |
| Grayscale | `4` | رنگ را به یک خاکستری با روشنایی یکسان تغییر می‌دهد. پارامتر نادیده گرفته می‌شود. |
| SetAlpha | `5` | یک جزء آلفا برای رنگ تعریف می‌کند. پارامتر در بازه 0 (شفاف) تا 1 (غیرشفاف) قرار دارد. |
| AddAlpha | `6` | مقدار پارامتر را به جزء آلفای رنگ اضافه می‌کند. پارامتر در بازه -1 تا 1 قرار دارد. |
| MultiplyAlpha | `7` | یک جزء آلفا را در مقدار پارامتر ضرب می‌کند. |
| SetHue | `8` | جزء hue رنگ را به مقدار پارامتر تغییر می‌دهد. پارامتر در بازه 0 تا 360 قرار دارد. |
| AddHue | `9` | مقدار پارامتر را به جزء hue رنگ اضافه می‌کند. پارامتر در بازه -360 تا 360 قرار دارد. |
| MultiplyHue | `10` | یک جزء hue را در مقدار پارامتر ضرب می‌کند. |
| SetSaturation | `11` | جزء saturation رنگ را به مقدار پارامتر تغییر می‌دهد. پارامتر در بازه 0 تا 1 قرار دارد. |
| AddSaturation | `12` | مقدار پارامتر را به جزء saturation رنگ اضافه می‌کند. پارامتر در بازه -1 تا 1 قرار دارد. |
| MultiplySaturation | `13` | یک جزء saturation را در مقدار پارامتر ضرب می‌کند. |
| SetLuminance | `14` | جزء luminance رنگ را به مقدار پارامتر تغییر می‌دهد. پارامتر در بازه 0 تا 1 قرار دارد. |
| AddLuminance | `15` | مقدار پارامتر را به جزء luminance رنگ اضافه می‌کند. پارامتر در بازه -1 تا 1 قرار دارد. |
| MultiplyLuminance | `16` | یک جزء luminance را در مقدار پارامتر ضرب می‌کند. |
| SetRed | `17` | جزء red رنگ را به مقدار پارامتر تغییر می‌دهد. پارامتر در بازه 0 تا 1 قرار دارد. |
| AddRed | `18` | مقدار پارامتر را به جزء red رنگ اضافه می‌کند. پارامتر در بازه -1 تا 1 قرار دارد. |
| MultiplyRed | `19` | جزء red را در پارامتر ضرب می‌کند. |
| SetGreen | `20` | جزء green رنگ را به مقدار پارامتر تغییر می‌دهد. پارامتر در بازه 0 تا 1 قرار دارد. |
| AddGreen | `21` | پارامتر را به جزء green رنگ اضافه می‌کند. پارامتر در بازه -1 تا 1 قرار دارد. |
| MultiplyGreen | `22` | جزء green را در مقدار پارامتر ضرب می‌کند. |
| SetBlue | `23` | جزء blue رنگ را به مقدار پارامتر تغییر می‌دهد. پارامتر در بازه 0 تا 360 قرار دارد. |
| AddBlue | `24` | مقدار پارامتر را به جزء blue رنگ اضافه می‌کند. پارامتر در بازه -1 تا 1 قرار دارد. |
| MultiplyBlue | `25` | جزء blue را در مقدار پارامتر ضرب می‌کند. |
| Gamma | `26` | تصحیح گاما. پارامتر نادیده گرفته می‌شود. |
| InverseGamma | `27` | تصحیح گاما معکوس. پارامتر نادیده گرفته می‌شود. |

### موارد دیگر

* namespace [Aspose.Slides](../../aspose.slides)
* assembly [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->