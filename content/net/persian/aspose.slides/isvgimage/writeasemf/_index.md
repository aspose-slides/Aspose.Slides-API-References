---
title: WriteAsEmf
second_title: Aspose.Sildes برای .NET مرجع API
description: تصویر SVG را به صورت یک فایل EMF ذخیره می‌کند.
type: docs
weight: 50
url: /fa/aspose.slides/isvgimage/writeasemf/
---
## ISvgImage.WriteAsEmf متد

تصویر SVG را به صورت یک فایل EMF ذخیره می‌کند.

```csharp
public void WriteAsEmf(Stream stream)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| stream | Stream | جریان هدف |

### استثناها

| استثنا | شرط |
| --- | --- |
| ArgumentNullException | جریان هدف **null** است |

### مثال‌ها

مثال زیر نشان می‌دهد که چگونه تصویر SVG را به یک متافایل ذخیره کنید.

```csharp
[C#]
// تصویر SVG جدید را ایجاد می‌کند
ISvgImage svgImage = new SvgImage(System.IO.File.ReadAllText("content.svg"));

// تصویر SVG را به صورت یک متافایل ذخیره می‌کند
using (var fileStream = System.IO.File.OpenWrite("SvgAsEmf.emf"))
    svgImage.WriteAsEmf(fileStream);
```

این نمونه نشان می‌دهد که چگونه تصویر SVG را به عنوان یک متافایل به مجموعه تصاویر ارائه اضافه کنید.

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    // تصویر SVG جدید را ایجاد می‌کند
    ISvgImage svgImage = new SvgImage(System.IO.File.ReadAllText("content.svg"));
    using (var memStream = new MemoryStream())
    {
        // تصویر SVG را به صورت یک متافایل ذخیره می‌کند
        svgImage.WriteAsEmf(memStream);
        // متافایل را به مجموعه تصاویر اضافه می‌کند
        pres.Images.AddImage(memStream.ToArray());
    }
}
```

### موارد مرتبط

* رابط [ISvgImage](../../isvgimage)
* فضای‌نام [Aspose.Slides](../../isvgimage)
* مجتمع [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->