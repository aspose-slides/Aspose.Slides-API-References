---
title: MasterNotesSlideHeaderFooterManager
second_title: Aspose.Sildes برای .NET مرجع API
description: نماینده‌ای است که رفتار فوتر اسلاید یادداشت‌های اصلی، متغیرهای جایگزین تاریخ-زمان، شماره صفحه و تمام متغیرهای جایگزین فرزند را نگه می‌دارد. متغیرهای جایگزین فرزند به این معنی است که این متغیرها در اسلایدهای یادداشت وابسته قرار دارند. اسلایدهای یادداشت وابسته از اسلاید یادداشت اصلی استفاده می‌کنند و به آن وابسته‌اند.
type: docs
weight: 8020
url: /fa/aspose.slides/masternotesslideheaderfootermanager/
---
## MasterNotesSlideHeaderFooterManager کلاس

مدیری را نشان می‌دهد که رفتار فوتر اسلاید یادداشت‌های اصلی، متغیرهای جایگزین تاریخ-زمان، شماره صفحه و تمام متغیرهای جایگزین فرزند را نگه می‌دارد. متغیرهای جایگزین فرزند به این معنی است که این متغیرها در اسلایدهای یادداشت وابسته قرار دارند. اسلایدهای یادداشت وابسته از اسلاید یادداشت اصلی استفاده می‌کنند و به آن وابسته‌اند.

```csharp
public sealed class MasterNotesSlideHeaderFooterManager : BaseHandoutNotesSlideHeaderFooterManager, 
    IMasterNotesSlideHeaderFooterManager
```

## ویژگی‌ها

| نام | توضیح |
| --- | --- |
| [IsDateTimeVisible](../../aspose.slides/baseslideheaderfootermanager/isdatetimevisible) { get; } | مقداری را برمی‌گرداند که نشان می‌دهد یک متغیر جایگزین تاریخ-زمان وجود دارد. ReadBoolean. |
| [IsFooterVisible](../../aspose.slides/baseslideheaderfootermanager/isfootervisible) { get; } | مقداری را برمی‌گرداند که نشان می‌دهد یک متغیر جایگزین فوتر وجود دارد. Read Boolean. |
| [IsHeaderVisible](../../aspose.slides/basehandoutnotesslideheaderfootermanager/isheadervisible) { get; } | مقداری را برمی‌گرداند که نشان می‌دهد یک متغیر جایگزین هدر وجود دارد. Read Boolean. |
| [IsSlideNumberVisible](../../aspose.slides/baseslideheaderfootermanager/isslidenumbervisible) { get; } | مقداری را برمی‌گرداند که نشان می‌دهد یک متغیر جایگزین شماره صفحه وجود دارد. ReadBoolean. |

## متدها

| نام | توضیح |
| --- | --- |
| [SetDateTimeAndChildDateTimesText](../../aspose.slides/masternotesslideheaderfootermanager/setdatetimeandchilddatetimestext)(string) | متن را در متغیر جایگزین تاریخ-زمان اسلاید اصلی و تمام متغیرهای جایگزین تاریخ-زمان فرزند تنظیم می‌کند. متغیرهای جایگزین فرزند به این معنی است که این متغیرها در اسلایدهای یادداشت وابسته قرار دارند. اسلایدهای یادداشت وابسته از اسلاید یادداشت اصلی استفاده می‌کنند و به آن وابسته‌اند. |
| [SetDateTimeAndChildDateTimesVisibility](../../aspose.slides/masternotesslideheaderfootermanager/setdatetimeandchilddatetimesvisibility)(bool) | قابلیت نمایش متغیر جایگزین تاریخ-زمان اسلاید اصلی و تمام متغیرهای جایگزین تاریخ-زمان فرزند را تغییر می‌دهد. متغیرهای جایگزین فرزند به این معنی است که این متغیرها در اسلایدهای یادداشت وابسته قرار دارند. اسلایدهای یادداشت وابسته از اسلاید یادداشت اصلی استفاده می‌کنند و به آن وابسته‌اند. |
| [SetDateTimeText](../../aspose.slides/baseslideheaderfootermanager/setdatetimetext)(string) | متن را در متغیر جایگزین تاریخ-زمان اسلاید تنظیم می‌کند. |
| [SetDateTimeVisibility](../../aspose.slides/baseslideheaderfootermanager/setdatetimevisibility)(bool) | قابلیت نمایش متغیر جایگزین تاریخ-زمان اسلاید را تغییر می‌دهد. |
| [SetFooterAndChildFootersText](../../aspose.slides/masternotesslideheaderfootermanager/setfooterandchildfooterstext)(string) | متن را در متغیر جایگزین فوتر اسلاید اصلی و تمام متغیرهای جایگزین فوتر فرزند تنظیم می‌کند. متغیرهای جایگزین فرزند به این معنی است که این متغیرها در اسلایدهای یادداشت وابسته قرار دارند. اسلایدهای یادداشت وابسته از اسلاید یادداشت اصلی استفاده می‌کنند و به آن وابسته‌اند. |
| [SetFooterAndChildFootersVisibility](../../aspose.slides/masternotesslideheaderfootermanager/setfooterandchildfootersvisibility)(bool) | قابلیت نمایش متغیر جایگزین فوتر اسلاید اصلی و تمام متغیرهای جایگزین فوتر فرزند را تغییر می‌دهد. متغیرهای جایگزین فرزند به این معنی است که این متغیرها در اسلایدهای یادداشت وابسته قرار دارند. اسلایدهای یادداشت وابسته از اسلاید یادداشت اصلی استفاده می‌کنند و به آن وابسته‌اند. |
| [SetFooterText](../../aspose.slides/baseslideheaderfootermanager/setfootertext)(string) | متن را در متغیر جایگزین فوتر اسلاید تنظیم می‌کند. |
| [SetFooterVisibility](../../aspose.slides/baseslideheaderfootermanager/setfootervisibility)(bool) | قابلیت نمایش متغیر جایگزین فوتر اسلاید را تغییر می‌دهد. |
| [SetHeaderAndChildHeadersText](../../aspose.slides/masternotesslideheaderfootermanager/setheaderandchildheaderstext)(string) | متن را در متغیر جایگزین هدر اسلاید یادداشت اصلی و تمام متغیرهای جایگزین هدر فرزند تنظیم می‌کند. متغیرهای جایگزین فرزند به این معنی است که این متغیرها در اسلایدهای یادداشت وابسته قرار دارند. اسلایدهای یادداشت وابسته از اسلاید یادداشت اصلی استفاده می‌کنند و به آن وابسته‌اند. |
| [SetHeaderAndChildHeadersVisibility](../../aspose.slides/masternotesslideheaderfootermanager/setheaderandchildheadersvisibility)(bool) | قابلیت نمایش متغیر جایگزین هدر اسلاید یادداشت اصلی و تمام متغیرهای جایگزین هدر فرزند را تغییر می‌دهد. متغیرهای جایگزین فرزند به این معنی است که این متغیرها در اسلایدهای یادداشت وابسته قرار دارند. اسلایدهای یادداشت وابسته از اسلاید یادداشت اصلی استفاده می‌کنند و به آن وابسته‌اند. |
| [SetHeaderText](../../aspose.slides/basehandoutnotesslideheaderfootermanager/setheadertext)(string) | متن را در متغیر جایگزین هدر اسلاید تنظیم می‌کند. |
| [SetHeaderVisibility](../../aspose.slides/basehandoutnotesslideheaderfootermanager/setheadervisibility)(bool) | قابلیت نمایش متغیر جایگزین هدر اسلاید را تغییر می‌دهد. |
| [SetSlideNumberAndChildSlideNumbersVisibility](../../aspose.slides/masternotesslideheaderfootermanager/setslidenumberandchildslidenumbersvisibility)(bool) | قابلیت نمایش متغیر جایگزین شماره صفحه اسلاید اصلی و تمام متغیرهای جایگزین شماره صفحه فرزند را تغییر می‌دهد. متغیرهای جایگزین فرزند به این معنی است که این متغیرها در اسلایدهای یادداشت وابسته قرار دارند. اسلایدهای یادداشت وابسته از اسلاید یادداشت اصلی استفاده می‌کنند و به آن وابسته‌اند. |
| [SetSlideNumberVisibility](../../aspose.slides/baseslideheaderfootermanager/setslidenumbervisibility)(bool) | قابلیت نمایش متغیر جایگزین شماره صفحه اسلاید را تغییر می‌دهد. |

### مطالب مرتبط

* کلاس [BaseHandoutNotesSlideHeaderFooterManager](../basehandoutnotesslideheaderfootermanager)
* رابط [IMasterNotesSlideHeaderFooterManager](../imasternotesslideheaderfootermanager)
* فضای‌نام [Aspose.Slides](../../aspose.slides)
* مونتاژ [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->