---
title: TileOffsetX
second_title: Aspose.Sildes برای .NET API Reference
description: مقدار افقی جابجایی بافت نسبت به مبدأ شکل بر حسب نقطه برمی‌گرداند یا تنظیم می‌کند. یک مقدار مثبت بافت را به سمت راست حرکت می‌دهد، در حالی که مقدار منفی آن را به سمت چپ می‌برد. خواندنی/نوشتنی Single.
type: docs
weight: 150
url: /fa/aspose.slides/ipicturefillformat/tileoffsetx/
---
## IPictureFillFormat.TileOffsetX ویژگی

مقدار افقی جابجایی بافت نسبت به مبدأ شکل را بر حسب نقطه برمی‌گرداند یا تنظیم می‌کند. یک مقدار مثبت بافت را به سمت راست حرکت می‌دهد، در حالی که مقدار منفی آن را به سمت چپ می‌برد. خواندنی/نوشتنی Single.

```csharp
public float TileOffsetX { get; set; }
```

### مثال‌ها

```csharp
[C#]
using (Presentation presentation = new Presentation("demo.pptx"))
{
    ISlide slide = presentation.Slides[0];

    // قالب پر کردن تصویر شکل را دریافت می‌کند
    IPictureFillFormat pictureFillFormat = slide.Shapes[0].FillFormat.PictureFillFormat;

    // حالت پر کردن تصویر را به Tile تنظیم می‌کند
    pictureFillFormat.PictureFillMode = PictureFillMode.Tile;

    // جابجایی افقی بافت را به 20 نقطه تنظیم می‌کند
    pictureFillFormat.TileOffsetX = 20f;
}
```

### موارد مرتبط

* رابط [IPictureFillFormat](../../ipicturefillformat)
* فضای‌نام [Aspose.Slides](../../ipicturefillformat)
* مونتاژ [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->