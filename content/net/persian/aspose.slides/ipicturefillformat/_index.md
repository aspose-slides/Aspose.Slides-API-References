---
title: IPictureFillFormat
second_title: مرجع API Aspose.Sildes برای .NET
description: یک سبک پر کردن تصویر را نشان می‌دهد.
type: docs
weight: 6650
url: /fa/aspose.slides/ipicturefillformat/
---
## IPictureFillFormat رابط

نمایانگر یک سبک پر کردن تصویر است.

```csharp
public interface IPictureFillFormat : IFillParamSource
```

## خواص

| نام | توضیح |
| --- | --- |
| [AsIFillParamSource](../../aspose.slides/ipicturefillformat/asifillparamsource) { get; } | اجازه می‌دهد رابط پایه IFillParamSource را دریافت کند. فقط-خواندنی [`IFillParamSource`](../ifillparamsource). |
| [CropBottom](../../aspose.slides/ipicturefillformat/cropbottom) { get; set; } | مقدار درصد ارتفاع واقعی تصویر که از پایین تصویر بریده می‌شود را برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/قابل نوشتن Single. |
| [CropLeft](../../aspose.slides/ipicturefillformat/cropleft) { get; set; } | مقدار درصد عرض واقعی تصویر که از سمت چپ تصویر بریده می‌شود را برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/قابل نوشتن Single. |
| [CropRight](../../aspose.slides/ipicturefillformat/cropright) { get; set; } | مقدار درصد عرض واقعی تصویر که از سمت راست تصویر بریده می‌شود را برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/قابل نوشتن Single. |
| [CropTop](../../aspose.slides/ipicturefillformat/croptop) { get; set; } | مقدار درصد ارتفاع واقعی تصویر که از بالای تصویر بریده می‌شود را برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/قابل نوشتن Single. |
| [Dpi](../../aspose.slides/ipicturefillformat/dpi) { get; set; } | مقدار dpi که برای پر کردن تصویر استفاده می‌شود را برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/قابل نوشتن Int32. |
| [Picture](../../aspose.slides/ipicturefillformat/picture) { get; } | تصویر را برمی‌گرداند. فقط-خواندنی [`ISlidesPicture`](../islidespicture). |
| [PictureFillMode](../../aspose.slides/ipicturefillformat/picturefillmode) { get; set; } | حالت پر کردن تصویر را برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/قابل نوشتن [`PictureFillMode`](../picturefillmode). |
| [StretchOffsetBottom](../../aspose.slides/ipicturefillformat/stretchoffsetbottom) { get; set; } | لبهٔ پایین مستطیل پرشده که توسط جابجایی درصدی از لبهٔ پایین جعبهٔ محدود‌کنندهٔ شکل تعریف می‌شود را برمی‌گرداند یا تنظیم می‌کند. درصد مثبت یک تو رفتگی و درصد منفی یک بیرون رفتگی را مشخص می‌کند. قابل خواندن/قابل نوشتن Single. |
| [StretchOffsetLeft](../../aspose.slides/ipicturefillformat/stretchoffsetleft) { get; set; } | لبهٔ چپ مستطیل پرشده که توسط جابجایی درصدی از لبهٔ چپ جعبهٔ محدود‌کنندهٔ شکل تعریف می‌شود را برمی‌گرداند یا تنظیم می‌کند. درصد مثبت یک تو رفتگی و درصد منفی یک بیرون رفتگی را مشخص می‌کند. قابل خواندن/قابل نوشتن Single. |
| [StretchOffsetRight](../../aspose.slides/ipicturefillformat/stretchoffsetright) { get; set; } | لبهٔ راست مستطیل پرشده که توسط جابجایی درصدی از لبهٔ راست جعبهٔ محدود‌کنندهٔ شکل تعریف می‌شود را برمی‌گرداند یا تنظیم می‌کند. درصد مثبت یک تو رفتگی و درصد منفی یک بیرون رفتگی را مشخص می‌کند. قابل خواندن/قابل نوشتن Single. |
| [StretchOffsetTop](../../aspose.slides/ipicturefillformat/stretchoffsettop) { get; set; } | لبهٔ بالا مستطیل پرشده که توسط جابجایی درصدی از لبهٔ بالا جعبهٔ محدود‌کنندهٔ شکل تعریف می‌شود را برمی‌گرداند یا تنظیم می‌کند. درصد مثبت یک تو رفتگی و درصد منفی یک بیرون رفتگی را مشخص می‌کند. قابل خواندن/قابل نوشتن Single. |
| [TileAlignment](../../aspose.slides/ipicturefillformat/tilealignment) { get; set; } | نحوهٔ تراز بافت درون شکل را برمی‌گرداند یا تنظیم می‌کند. این تنظیم نقطهٔ شروع الگوی بافت و نحوهٔ تکرار آن در سرتاسر شکل را کنترل می‌کند. قابل خواندن/قابل نوشتن [`RectangleAlignment`](../rectanglealignment). |
| [TileFlip](../../aspose.slides/ipicturefillformat/tileflip) { get; set; } | کاشی بافت را حول محور افقی، عمودی یا هر دو محور می‌چرخاند. قابل خواندن/قابل نوشتن [`TileFlip`](../tileflip). |
| [TileOffsetX](../../aspose.slides/ipicturefillformat/tileoffsetx) { get; set; } | جابجایی افقی بافت نسبت به مبدأ شکل بر حسب نقطه را برمی‌گرداند یا تنظیم می‌کند. مقدار مثبت بافت را به سمت راست و مقدار منفی به سمت چپ حرکت می‌دهد. قابل خواندن/قابل نوشتن Single. |
| [TileOffsetY](../../aspose.slides/ipicturefillformat/tileoffsety) { get; set; } | جابجایی عمودی بافت نسبت به مبدأ شکل بر حسب نقطه را برمی‌گرداند یا تنظیم می‌کند. مقدار مثبت بافت را به سمت پایین و مقدار منفی به سمت بالا حرکت می‌دهد. قابل خواندن/قابل نوشتن Single. |
| [TileScaleX](../../aspose.slides/ipicturefillformat/tilescalex) { get; set; } | مقیاس افقی پر کردن بافت به درصد را برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/قابل نوشتن Single. |
| [TileScaleY](../../aspose.slides/ipicturefillformat/tilescaley) { get; set; } | مقیاس عمودی پر کردن بافت به درصد را برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/قابل نوشتن Single. |

## متدها

| نام | توضیح |
| --- | --- |
| [CompressImage](../../aspose.slides/ipicturefillformat/compressimage#compressimage_1)(bool, float) | تصویر را با کاهش اندازه بر اساس اندازهٔ شکل و وضوح مشخص فشرده می‌کند. به‌صورت اختیاری، نواحی بریده‌شده را نیز حذف می‌کند. |
| [CompressImage](../../aspose.slides/ipicturefillformat/compressimage#compressimage)(bool, PicturesCompression) | تصویر را با کاهش اندازه بر اساس اندازهٔ شکل و وضوح مشخص فشرده می‌کند. به‌صورت اختیاری، نواحی بریده‌شده را نیز حذف می‌کند. |
| [DeletePictureCroppedAreas](../../aspose.slides/ipicturefillformat/deletepicturecroppedareas)() | نواحی بریده‌شدهٔ تصویر پر شده را حذف می‌کند. |

### موارد مرتبط

* رابط [IFillParamSource](../ifillparamsource)
* فضای‌نام [Aspose.Slides](../../aspose.slides)
* مجمع [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->