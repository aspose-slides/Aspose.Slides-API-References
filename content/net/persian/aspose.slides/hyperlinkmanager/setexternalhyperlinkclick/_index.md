---
title: SetExternalHyperlinkClick
second_title: Aspose.Sildes برای .NET مرجع API
description: پیوند خارجی را هنگام کلیک تنظیم می‌کند.
type: docs
weight: 30
url: /fa/aspose.slides/hyperlinkmanager/setexternalhyperlinkclick/
---
## HyperlinkManager.SetExternalHyperlinkClick متد

یک پیوند خارجی را هنگام کلیک تنظیم می‌کند.

```csharp
public IHyperlink SetExternalHyperlinkClick(string url)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| url | String | آدرس پیوند. |

### مثال‌ها

کد نمونه زیر نشان می‌دهد چگونه یک Text Box با Hyperlink اضافه کنید.

```csharp
[C#]
// یک شی از کلاس Presentation که نمایانگر یک فایل PPTX است را نمونه‌سازی می‌کند
using(Presentation pptxPresentation = new Presentation()) {
  // اسلاید اول ارائه را دریافت می‌کند
  ISlide slide = pptxPresentation.Slides[0];
  // یک شی AutoShape با نوع مستطیل اضافه می‌کند
  IShape pptxShape = slide.Shapes.AddAutoShape(ShapeType.Rectangle, 150, 150, 150, 50);
  // شکل را به AutoShape تبدیل می‌کند
  IAutoShape pptxAutoShape = (IAutoShape) pptxShape;
  // به ویژگی ITextFrame مرتبط با AutoShape دسترسی پیدا می‌کند
  pptxAutoShape.AddTextFrame("");
  ITextFrame textFrame = pptxAutoShape.TextFrame;
  // متنی را به فریم اضافه می‌کند
  textFrame.Paragraphs[0].Portions[0].Text = "Aspose.Slides";
  // پیوند (Hyperlink) را برای متن بخش تنظیم می‌کند
  IHyperlinkManager HypMan = textFrame.Paragraphs[0].Portions[0].PortionFormat.HyperlinkManager;
  HypMan.SetExternalHyperlinkClick("http://www.aspose.com");
  // ارائه PPTX را ذخیره می‌کند
  pptxPresentation.Save("hLinkPPTX_out.pptx", Aspose.Slides.Export.SaveFormat.Pptx);
}
```

### موارد مرتبط

* رابط [IHyperlink](../../ihyperlink)
* کلاس [HyperlinkManager](../../hyperlinkmanager)
* فضای‌نام [Aspose.Slides](../../hyperlinkmanager)
* اسمبل [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->