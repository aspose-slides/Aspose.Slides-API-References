---
title: GetBasePlaceholder
second_title: Aspose.Sildes برای .NET مرجع API
description: یک شکل جایگزین پایه را از طرح‌بندی و/یا اسلاید اصلی که شکل فعلی از آن ارث برده شده است، برمی‌گرداند. اگر شکل فعلی ارث‌برده نشده باشد، مقدار null برگردانده می‌شود.
type: docs
weight: 310
url: /fa/aspose.slides/ishape/getbaseplaceholder/
---
## IShape.GetBasePlaceholder متد

یک شکل جایگزین پایه را باز می‌گرداند (شکلی از طرح‌بندی و/یا اسلاید اصلی که شکل فعلی از آن ارث برده شده است). اگر شکل فعلی ارث‌برده نشده باشد، مقدار null بازگردانده می‌شود.

```csharp
public IShape GetBasePlaceholder()
```

### مثال‌ها

```csharp
[C#]
// تمام افکت‌های انیمیشن (master/layout/slide) شکل جایگزین را دریافت کنید
using (Presentation pres = new Presentation("sample.pptx"))
{
    ISlide slide = pres.Slides[0];
    IShape shape = slide.Shapes[0];
    IEffect[] shapeEffects = slide.Timeline.MainSequence.GetEffectsByShape(shape);

    IShape layoutShape = shape.GetBasePlaceholder();
    IEffect[] layoutShapeEffects = slide.LayoutSlide.Timeline.MainSequence.GetEffectsByShape(layoutShape);

    IShape masterShape = layoutShape.GetBasePlaceholder();
    IEffect[] masterShapeEffects = slide.LayoutSlide.MasterSlide.Timeline.MainSequence.GetEffectsByShape(masterShape);
}
```

### مشاهده نیز

* رابط [IShape](../../ishape)
* فضای‌نام [Aspose.Slides](../../ishape)
* مجتمع [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->