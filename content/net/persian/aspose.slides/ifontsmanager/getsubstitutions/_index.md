---
title: GetSubstitutions
second_title: مرجع API Aspose.Sildes برای .NET
description: اطلاعات مربوط به قلم‌هایی که در هنگام رندر ارائه‌ها جایگزین خواهند شد را دریافت می‌کند.
type: docs
weight: 80
url: /fa/aspose.slides/ifontsmanager/getsubstitutions/
---
## GetSubstitutions() {#getsubstitutions}

اطلاعات مربوط به قلم‌هایی که در زمان رندر ارائه جایگزین خواهند شد را دریافت می‌کند.

```csharp
public IEnumerable<FontSubstitutionInfo> GetSubstitutions()
```

### مقدار برگشتی

مجموعه‌ای از تمام جایگزینی‌های قلم [`FontSubstitutionInfo`](../../fontsubstitutioninfo).

### مثال‌ها

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    foreach (var fontSubstitution in pres.FontsManager.GetSubstitutions())
    {
        Console.WriteLine("{0} -> {1}", fontSubstitution.OriginalFontName, fontSubstitution.SubstitutedFontName);
    }
}        
```

### موارد مرتبط

* کلاس [FontSubstitutionInfo](../../fontsubstitutioninfo)
* رابط [IFontsManager](../../ifontsmanager)
* فضای‌نام [Aspose.Slides](../../ifontsmanager)
* مجوعه [Aspose.Slides](../../../)

---

## GetSubstitutions(int[]) {#getsubstitutions_1}

اطلاعات مربوط به قلم‌هایی که در هنگام رندر اسلایدهای مشخص شده جایگزین خواهند شد را دریافت می‌کند.

```csharp
public IEnumerable<FontSubstitutionInfo> GetSubstitutions(int[] slides)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| slides | Int32[] | یک آرایه از اندیس‌های اسلاید که برای آن‌ها اطلاعات جایگزینی قلم دریافت می‌شود، از 1 شروع می‌شود. |

### مقدار برگشتی

یک مجموعه از تمام جایگزینی‌های قلم ([`FontSubstitutionInfo`](../../fontsubstitutioninfo)) برای اسلایدهای مشخص شده.

### مثال‌ها

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    int[] targetSlides = { 1, 2, 5 };
    foreach (var fontSubstitution in pres.FontsManager.GetSubstitutions(targetSlides))
    {
        Console.WriteLine("{0} -> {1}", fontSubstitution.OriginalFontName, fontSubstitution.SubstitutedFontName);
    }
}
```

### موارد مرتبط

* کلاس [FontSubstitutionInfo](../../fontsubstitutioninfo)
* رابط [IFontsManager](../../ifontsmanager)
* فضای‌نام [Aspose.Slides](../../ifontsmanager)
* مجوعه [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->