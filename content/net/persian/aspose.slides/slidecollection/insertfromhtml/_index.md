---
title: InsertFromHtml
second_title: Aspose.Sildes برای .NET مرجع API
description: اسلایدهایی را از متن HTML ایجاد می‌کند و در موقعیت مشخص به مجموعه اضافه می‌نماید.
type: docs
weight: 140
url: /fa/aspose.slides/slidecollection/insertfromhtml/
---
## InsertFromHtml(int, string, IExternalResourceResolver, string) {#insertfromhtml_7}

اسلایدهایی را از متن HTML ایجاد می‌کند و در موقعیت مشخص به مجموعه اضافه می‌نماید.

```csharp
public ISlide[] InsertFromHtml(int index, string htmlText, IExternalResourceResolver resolver, 
    string uri)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| index | Int32 | موقعیت درج. |
| htmlText | String | HTML برای افزودن. |
| resolver | IExternalResourceResolver | یک شی بازگشتی که برای دریافت اشیاء خارجی استفاده می‌شود. اگر این پارامتر null باشد، تمام اشیاء خارجی نادیده گرفته می‌شوند. |
| uri | String | یک URI از HTML مشخص‌شده. برای حل پیوندهای نسبی استفاده می‌شود. |

### مقدار بازگشت

اسلایدهای اضافه‌شده.

### موارد مرتبط

* رابط [ISlide](../../islide)
* رابط [IExternalResourceResolver](../../../aspose.slides.import/iexternalresourceresolver)
* کلاس [SlideCollection](../../slidecollection)
* فضای‌نام [Aspose.Slides](../../slidecollection)
* اسمبلی [Aspose.Slides](../../../)

---

## InsertFromHtml(int, string, IExternalResourceResolver, string, bool) {#insertfromhtml_8}

اسلایدهایی را از متن HTML ایجاد می‌کند و در موقعیت مشخص به مجموعه اضافه می‌نماید.

```csharp
public ISlide[] InsertFromHtml(int index, string htmlText, IExternalResourceResolver resolver, 
    string uri, bool useSlideWithIndexAsStart)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| index | Int32 | موقعیت درج. |
| htmlText | String | HTML برای افزودن. |
| resolver | IExternalResourceResolver | یک شی بازگشتی که برای دریافت اشیاء خارجی استفاده می‌شود. اگر این پارامتر null باشد، تمام اشیاء خارجی نادیده گرفته می‌شوند. |
| uri | String | یک URI از HTML مشخص‌شده. برای حل پیوندهای نسبی استفاده می‌شود. |
| useSlideWithIndexAsStart | Boolean | این پرچم تعیین می‌کند که درج از یک اسلاید جدید آغاز شود یا از اسلایدی با اندیس مشخص‌شده. اگر **true** باشد، درج داده‌ها از فضای خالی در اسلاید با اندیس مشخص‌شده شروع می‌شود. اگر **false** باشد، داده‌ها به اسلایدهای ایجاد‌شده اضافه می‌شوند. |

### مقدار بازگشت

اسلایدهای اضافه‌شده.

### موارد مرتبط

* رابط [ISlide](../../islide)
* رابط [IExternalResourceResolver](../../../aspose.slides.import/iexternalresourceresolver)
* کلاس [SlideCollection](../../slidecollection)
* فضای‌نام [Aspose.Slides](../../slidecollection)
* اسمبلی [Aspose.Slides](../../../)

---

## InsertFromHtml(int, string) {#insertfromhtml_6}

اسلایدهایی را از متن HTML ایجاد می‌کند و در موقعیت مشخص به مجموعه اضافه می‌نماید.

```csharp
public ISlide[] InsertFromHtml(int index, string htmlText)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| index | Int32 | موقعیت درج. |
| htmlText | String | HTML برای افزودن. |

### مقدار بازگشت

اسلایدهای اضافه‌شده

### موارد مرتبط

* رابط [ISlide](../../islide)
* کلاس [SlideCollection](../../slidecollection)
* فضای‌نام [Aspose.Slides](../../slidecollection)
* اسمبلی [Aspose.Slides](../../../)

---

## InsertFromHtml(int, string, bool) {#insertfromhtml_9}

اسلایدهایی را از متن HTML ایجاد می‌کند و در موقعیت مشخص به مجموعه اضافه می‌نماید.

```csharp
public ISlide[] InsertFromHtml(int index, string htmlText, bool useSlideWithIndexAsStart)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| index | Int32 | موقعیت درج. |
| htmlText | String | HTML برای افزودن. |
| useSlideWithIndexAsStart | Boolean | این پرچم تعیین می‌کند که درج از یک اسلاید جدید آغاز شود یا از اسلایدی با اندیس مشخص‌شده. اگر **true** باشد، درج داده‌ها از فضای خالی در اسلاید با اندیس مشخص‌شده شروع می‌شود. اگر **false** باشد، داده‌ها به اسلایدهای ایجاد‌شده اضافه می‌شوند. |

### مقدار بازگشت

اسلایدهای اضافه‌شده

### موارد مرتبط

* رابط [ISlide](../../islide)
* کلاس [SlideCollection](../../slidecollection)
* فضای‌نام [Aspose.Slides](../../slidecollection)
* اسمبلی [Aspose.Slides](../../../)

---

## InsertFromHtml(int, TextReader, IExternalResourceResolver, string) {#insertfromhtml_5}

اسلایدهایی را از متن HTML ایجاد می‌کند و در موقعیت مشخص به مجموعه اضافه می‌نماید.

```csharp
public ISlide[] InsertFromHtml(int index, TextReader htmlReader, 
    IExternalResourceResolver resolver, string uri)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| index | Int32 | موقعیت درج. |
| htmlReader | TextReader | شی TextReader که به عنوان منبع یک فایل HTML استفاده می‌شود. |
| resolver | IExternalResourceResolver | یک شی بازگشتی که برای دریافت اشیاء خارجی استفاده می‌شود. اگر این پارامتر null باشد، تمام اشیاء خارجی نادیده گرفته می‌شوند. |
| uri | String | یک URI از HTML مشخص‌شده. برای حل پیوندهای نسبی استفاده می‌شود. |

### مقدار بازگشت

اسلایدهای اضافه‌شده.

### موارد مرتبط

* رابط [ISlide](../../islide)
* رابط [IExternalResourceResolver](../../../aspose.slides.import/iexternalresourceresolver)
* کلاس [SlideCollection](../../slidecollection)
* فضای‌نام [Aspose.Slides](../../slidecollection)
* اسمبلی [Aspose.Slides](../../../)

---

## InsertFromHtml(int, TextReader) {#insertfromhtml_4}

اسلایدهایی را از متن HTML ایجاد می‌کند و در موقعیت مشخص به مجموعه اضافه می‌نماید.

```csharp
public ISlide[] InsertFromHtml(int index, TextReader htmlReader)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| index | Int32 | موقعیت درج. |
| htmlReader | TextReader | شی TextReader که به عنوان منبع یک فایل HTML استفاده می‌شود. |

### مقدار بازگشت

اسلایدهای اضافه‌شده

### موارد مرتبط

* رابط [ISlide](../../islide)
* کلاس [SlideCollection](../../slidecollection)
* فضای‌نام [Aspose.Slides](../../slidecollection)
* اسمبلی [Aspose.Slides](../../../)

---

## InsertFromHtml(int, Stream, IExternalResourceResolver, string) {#insertfromhtml_1}

اسلایدهایی را از متن HTML ایجاد می‌کند و در موقعیت مشخص به مجموعه اضافه می‌نماید.

```csharp
public ISlide[] InsertFromHtml(int index, Stream htmlStream, IExternalResourceResolver resolver, 
    string uri)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| index | Int32 | موقعیت درج. |
| htmlStream | Stream | شی Stream که به عنوان منبع یک فایل HTML استفاده می‌شود. |
| resolver | IExternalResourceResolver | یک شی بازگشتی که برای دریافت اشیاء خارجی استفاده می‌شود. اگر این پارامتر null باشد، تمام اشیاء خارجی نادیده گرفته می‌شوند. |
| uri | String | یک URI از HTML مشخص‌شده. برای حل پیوندهای نسبی استفاده می‌شود. |

### مقدار بازگشت

اسلایدهای اضافه‌شده.

### موارد مرتبط

* رابط [ISlide](../../islide)
* رابط [IExternalResourceResolver](../../../aspose.slides.import/iexternalresourceresolver)
* کلاس [SlideCollection](../../slidecollection)
* فضای‌نام [Aspose.Slides](../../slidecollection)
* اسمبلی [Aspose.Slides](../../../)

---

## InsertFromHtml(int, Stream, IExternalResourceResolver, string, bool) {#insertfromhtml_2}

اسلایدهایی را از متن HTML ایجاد می‌کند و در موقعیت مشخص به مجموعه اضافه می‌نماید.

```csharp
public ISlide[] InsertFromHtml(int index, Stream htmlStream, IExternalResourceResolver resolver, 
    string uri, bool useSlideWithIndexAsStart)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| index | Int32 | موقعیت درج. |
| htmlStream | Stream | شی Stream که به عنوان منبع یک فایل HTML استفاده می‌شود. |
| resolver | IExternalResourceResolver | یک شی بازگشتی که برای دریافت اشیاء خارجی استفاده می‌شود. اگر این پارامتر null باشد، تمام اشیاء خارجی نادیده گرفته می‌شوند. |
| uri | String | یک URI از HTML مشخص‌شده. برای حل پیوندهای نسبی استفاده می‌شود. |
| useSlideWithIndexAsStart | Boolean | این پرچم تعیین می‌کند که درج از یک اسلاید جدید آغاز شود یا از اسلایدی با اندیس مشخص‌شده. اگر **true** باشد، درج داده‌ها از فضای خالی در اسلاید با اندیس مشخص‌شده شروع می‌شود. اگر **false** باشد، داده‌ها به اسلایدهای ایجاد‌شده اضافه می‌شوند. |

### مقدار بازگشت

اسلایدهای اضافه‌شده.

### موارد مرتبط

* رابط [ISlide](../../islide)
* رابط [IExternalResourceResolver](../../../aspose.slides.import/iexternalresourceresolver)
* کلاس [SlideCollection](../../slidecollection)
* فضای‌نام [Aspose.Slides](../../slidecollection)
* اسمبلی [Aspose.Slides](../../../)

---

## InsertFromHtml(int, Stream) {#insertfromhtml}

اسلایدهایی را از متن HTML ایجاد می‌کند و در موقعیت مشخص به مجموعه اضافه می‌نماید.

```csharp
public ISlide[] InsertFromHtml(int index, Stream htmlStream)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| index | Int32 | موقعیت درج. |
| htmlStream | Stream | شی Stream که به عنوان منبع یک فایل HTML استفاده می‌شود. |

### مقدار بازگشت

اسلایدهای اضافه‌شده

### موارد مرتبط

* رابط [ISlide](../../islide)
* کلاس [SlideCollection](../../slidecollection)
* فضای‌نام [Aspose.Slides](../../slidecollection)
* اسمبلی [Aspose.Slides](../../../)

---

## InsertFromHtml(int, Stream, bool) {#insertfromhtml_3}

اسلایدهایی را از متن HTML ایجاد می‌کند و در موقعیت مشخص به مجموعه اضافه می‌نماید.

```csharp
public ISlide[] InsertFromHtml(int index, Stream htmlStream, bool useSlideWithIndexAsStart)
```

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| index | Int32 | موقعیت درج. |
| htmlStream | Stream | شی Stream که به عنوان منبع یک فایل HTML استفاده می‌شود. |
| useSlideWithIndexAsStart | Boolean | این پرچم تعیین می‌کند که درج از یک اسلاید جدید آغاز شود یا از اسلایدی با اندیس مشخص‌شده. اگر **true** باشد، درج داده‌ها از فضای خالی در اسلاید با اندیس مشخص‌شده شروع می‌شود. اگر **false** باشد، داده‌ها به اسلایدهای ایجاد‌شده اضافه می‌شوند. |

### مقدار بازگشت

اسلایدهای اضافه‌شده

### موارد مرتبط

* رابط [ISlide](../../islide)
* کلاس [SlideCollection](../../slidecollection)
* فضای‌نام [Aspose.Slides](../../slidecollection)
* اسمبلی [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->