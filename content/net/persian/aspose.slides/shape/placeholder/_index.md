---
title: Placeholder
second_title: مرجع API Aspose.Sildes برای .NET
description: placeholder را برای یک شکل برمی‌گرداند. اگر شکل placeholder نداشته باشد null برمی‌گرداند. فقط-خواندنی IPlaceholderaspose.slides/iplaceholder.
type: docs
weight: 210
url: /fa/aspose.slides/shape/placeholder/
---
## Shape.Placeholder property

placeholder را برای یک شکل برمی‌گرداند. اگر شکل placeholder نداشته باشد null برمی‌گرداند. فقط-خواندنی [`IPlaceholder`](../../iplaceholder).

```csharp
public IPlaceholder Placeholder { get; }
```

### مثال‌ها

مثال زیر نشان می‌دهد چگونه Text را در Placeholder تغییر داد.

```csharp
[C#]
// یک شی از کلاس Presentation ایجاد می‌کند
using (Presentation pres = new Presentation("ReplacingText.pptx"))
{
    // به اسلاید اول دسترسی می‌یابد
    ISlide sld = pres.Slides[0];
    // از شکل‌ها عبور می‌کند تا placeholder را پیدا کند
    foreach (IShape shp in sld.Shapes)
        if (shp.Placeholder != null)
        {
            // متن هر placeholder را تغییر می‌دهد
            ((IAutoShape)shp).TextFrame.Text = "This is a Placeholder";
        }
    // ارائه را بر روی دیسک ذخیره می‌کند
    pres.Save("output_out.pptx", Aspose.Slides.Export.SaveFormat.Pptx);
}
```

مثال زیر نشان می‌دهد چگونه Prompt Text را در Placeholder تنظیم کرد.

```csharp
[C#]
using (Presentation pres = new Presentation("Presentation2.pptx"))
{
    ISlide slide = pres.Slides[0];
    foreach (IShape shape in slide.Slide.Shapes) // بر روی اسلاید عبور می‌کند
    {
        if (shape.Placeholder != null && shape is AutoShape)
        {
            string text = "";
            if (shape.Placeholder.Type == PlaceholderType.CenteredTitle) // PowerPoint نمایش می‌دهد "Click to add title"
            {
                text = "Add Title";
            }
            else if (shape.Placeholder.Type == PlaceholderType.Subtitle) // زیرعنوان را اضافه می‌کند
            {
                text = "Add Subtitle";
            }
            ((IAutoShape)shape).TextFrame.Text = text;
            Console.WriteLine($"Placeholder with text: {text}");
        }
    }
    pres.Save("Placeholders_PromptText.pptx", SaveFormat.Pptx);
}
```

### موارد مرتبط

* رابط [IPlaceholder](../../iplaceholder)
* کلاس [Shape](../../shape)
* فضای‌نام [Aspose.Slides](../../shape)
* اسمبلی [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->