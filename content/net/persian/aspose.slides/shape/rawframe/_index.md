---
title: RawFrame
second_title: Aspose.Sildes برای مرجع API .NET
description: مقادیر ویژگی‌های چارچوب خام شکل را بازگردانده یا تنظیم می‌کند. خواندن/نوشتن IShapeFrameaspose.slides/ishapeframe.
type: docs
weight: 230
url: /fa/aspose.slides/shape/rawframe/
---
## Shape.RawFrame ویژگی

مقدار یا تنظیم ویژگی‌های چارچوب خام شکل. خواندن/نوشتن [`IShapeFrame`](../../ishapeframe).

```csharp
public IShapeFrame RawFrame { get; set; }
```

### مثال‌ها

کدی که سعی می‌کند چارچوب تعریف نشده را به IShape.Frame اختصاص دهد در حالت کلی معنایی ندارد (به‌ویژه در موقعیتی که GroupShape والد چندین بار در داخل دیگر GroupShape-ها تو در تو باشد). به عنوان مثال:

```csharp
IShape shape = ...;
shape.Frame = new ShapeFrame(float.NaN, float.NaN, float.NaN, float.NaN, NullableBool.NotDefined, NullableBool.NotDefined, float.NaN);
```

یا

```csharp
slide.Shapes.AddAutoShape(ShapeType.RoundCornerRectangle, float.NaN, float.NaN, float.NaN, float.NaN);
```

چنین کدی می‌تواند به وضعیت‌های نامشخص منجر شود. بنابراین محدودیت‌هایی برای استفاده از مقادیر تعریف‌نشده در IShape.Frame اضافه شده است. مقادیر x، y، width، height، flipH، flipV و rotationAngle باید تعریف شوند (نه float.NaN nor NullableBool.NotDefined). کد مثال فوق اکنون استثنای ArgumentException را پرتاب می‌کند. این محدودیت‌ها برای موارد استفاده زیر اعمال می‌شود:

```csharp
IShape shape = ...;
shape.Frame = ...; // نمی‌تواند نامعین باشد

IShapeCollection shapes = ...;
// پارامترهای x, y, width, height نمی‌توانند مقدار float.NaN باشند:
{
    shapes.AddAudioFrameCD(...);
    shapes.AddAudioFrameEmbedded(...);
    shapes.AddAudioFrameLinked(...);
    shapes.AddAutoShape(...);
    shapes.AddChart(...);
    shapes.AddConnector(...);
    shapes.AddOleObjectFrame(...);
    shapes.AddPictureFrame(...);
    shapes.AddSmartArt(...);
    shapes.AddTable(...);
    shapes.AddVideoFrame(...);
    shapes.InsertAudioFrameEmbedded(...);
    shapes.InsertAudioFrameLinked(...);
    shapes.InsertAutoShape(...);
    shapes.InsertChart(...);
    shapes.InsertConnector(...);
    shapes.InsertOleObjectFrame(...);
    shapes.InsertPictureFrame(...);
    shapes.InsertTable(...);
    shapes.InsertVideoFrame(...);
}
```

اما ویژگی‌های چارچوب IShape.RawFrame می‌توانند تعریف‌نشده باشند. این زمانی معقول است که شکل به یک متغیر نگهدارنده (placeholder) لینک شده باشد. در این صورت مقادیر تعریف‌نشده چارچوب شکل از متغیر نگهدارنده والد بازنویسی می‌شود. اگر برای آن شکل متغیر نگهدارنده والد وجود نداشته باشد، شکل از مقادیر پیش‌فرض استفاده می‌کند هنگامی که چارچوب مؤثر براساس IShape.RawFrame خود محاسبه می‌شود. مقادیر پیش‌فرض 0 و NullableBool.False برای x، y، width، height، flipH، flipV و rotationAngle هستند. به عنوان مثال:

```csharp
IShape shape = ...; // شکل به placeholder لینک شده است
shape.RawFrame = new ShapeFrame(float.NaN, float.NaN, 100, float.NaN, NullableBool.NotDefined, NullableBool.NotDefined, 0); // اکنون شکل مقادیر x، y، height، flipH، flipV را از placeholder به ارث می‌برد و عرض=100 و rotationAngle=0 را بازنویسی می‌کند.
```

### موارد مرتبط

* رابط [IShapeFrame](../../ishapeframe)
* کلاس [Shape](../../shape)
* فضای نام [Aspose.Slides](../../shape)
* اسمبلی [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->