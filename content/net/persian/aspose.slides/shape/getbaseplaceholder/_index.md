---
title: GetBasePlaceholder
second_title: Aspose.Sildes برای .NET مرجع API
description: یک شکل متغیر پایه ساده را برمی‌گرداند (شکلی که از چیدمان و/یا اسلاید اصلی که شکل جاری از آن ارث‌بری شده است). اگر شکل جاری ارث‌بری نشده باشد مقدار null برگردانده می‌شود.
type: docs
weight: 340
url: /fa/aspose.slides/shape/getbaseplaceholder/
---
## Shape.GetBasePlaceholder متد

یک شکل متغیر پایه ساده را برمی‌گرداند (شکلی که از چیدمان و/یا اسلاید اصلی که شکل جاری از آن ارث‌بری شده است). اگر شکل جاری ارث‌بری نشده باشد مقدار null برگردانده می‌شود.

```csharp
public IShape GetBasePlaceholder()
```

### مثال‌ها

```csharp
[C#]
// دریافت تمام افکت‌های انیمیشن (master/layout/slide) شکل متغیر
using (Presentation pres = new Presentation("sample.pptx"))
{
    ISlide slide = pres.Slides[0];
    IShape shape = slide.Shapes[0];
    IEffect[] shapeEffects = slide.Timeline.MainSequence.GetEffectsByShape(shape);

    IShape layoutShape = shape.GetBasePlaceholder();
    IEffect[] layoutShapeEffects = slide.LayoutSlide.Timeline.MainSequence.GetEffectsByShape(layoutShape);

    IShape masterShape = layoutShape.GetBasePlaceholder();
    IEffect[] masterShapeEffects = slide.LayoutSlide.MasterSlide.Timeline.MainSequence.GetEffectsByShape(masterShape);
}
```

### موارد مرتبط

* رابط [IShape](../../ishape)
* کلاس [Shape](../../shape)
* فضای‌نام [Aspose.Slides](../../shape)
* مجمع [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->