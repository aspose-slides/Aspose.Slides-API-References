---
title: GetCell
second_title: Aspose.Sildes برای .NET - مرجع API
description: یک سلول را از برگه‌کار مشخص‌شده با استفاده از شاخص آن و مختصات سلول بازیابی می‌کند.
type: docs
weight: 10
url: /fa/aspose.slides.excel/iexceldataworkbook/getcell/
---
## GetCell(int, int, int) {#getcell}

یک سلول را از برگه‌کار مشخص‌شده با استفاده از شاخص آن و مختصات سلول بازیابی می‌کند.

```csharp
public IExcelDataCell GetCell(int worksheetIndex, int row, int column)
```

| Parameter | Type | Description |
| --- | --- | --- |
| worksheetIndex | Int32 | شاخص صفر-پایهٔ برگه‌کار. |
| row | Int32 | شاخص صفر-پایهٔ سطر سلول. |
| column | Int32 | شاخص صفر-پایهٔ ستون سلول. |

### مقدار بازگشتی

سلول در مکان مشخص‌شده.

### مثال‌ها

مثال:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell(1, 1, 1);
Console.WriteLine(cell.Value.ToString());
```

### موارد مرتبط

* رابط [IExcelDataCell](../../iexceldatacell)
* رابط [IExcelDataWorkbook](../../iexceldataworkbook)
* فضای‌نام [Aspose.Slides.Excel](../../iexceldataworkbook)
* مجموعه [Aspose.Slides](../../../)

---

## GetCell(string, int, int) {#getcell_2}

یک سلول را از برگه‌کار مشخص‌شده با استفاده از نام آن و مختصات سلول بازیابی می‌کند.

```csharp
public IExcelDataCell GetCell(string worksheetName, int row, int column)
```

| Parameter | Type | Description |
| --- | --- | --- |
| worksheetName | String | نام برگه‌کار. |
| row | Int32 | شاخص صفر-پایهٔ سطر سلول. |
| column | Int32 | شاخص صفر-پایهٔ ستون سلول. |

### مقدار بازگشتی

سلول در مکان مشخص‌شده.

### مثال‌ها

مثال:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell("Sheet1", 1, 1);
Console.WriteLine(cell.Value.ToString());
```

### موارد مرتبط

* رابط [IExcelDataCell](../../iexceldatacell)
* رابط [IExcelDataWorkbook](../../iexceldataworkbook)
* فضای‌نام [Aspose.Slides.Excel](../../iexceldataworkbook)
* مجموعه [Aspose.Slides](../../../)

---

## GetCell(int, string) {#getcell_1}

یک سلول را از برگه‌کار مشخص‌شده با استفاده از شاخص آن و نام سلول به سبک اکسل (مثلاً "B2") بازیابی می‌کند.

```csharp
public IExcelDataCell GetCell(int worksheetIndex, string cellName)
```

| Parameter | Type | Description |
| --- | --- | --- |
| worksheetIndex | Int32 | شاخص صفر-پایهٔ برگه‌کار. |
| cellName | String | مرجع سلول به سبک اکسل (مثلاً "A1"، "C5"). |

### مقدار بازگشتی

سلول در مکان مشخص‌شده.

### مثال‌ها

مثال:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell(1, "B2");
Console.WriteLine(cell.Value.ToString());
```

### موارد مرتبط

* رابط [IExcelDataCell](../../iexceldatacell)
* رابط [IExcelDataWorkbook](../../iexceldataworkbook)
* فضای‌نام [Aspose.Slides.Excel](../../iexceldataworkbook)
* مجموعه [Aspose.Slides](../../../)

---

## GetCell(string, string) {#getcell_3}

یک سلول را از برگه‌کار مشخص‌شده با استفاده از نام سلول به سبک اکسل (مثلاً "B2") بازیابی می‌کند.

```csharp
public IExcelDataCell GetCell(string worksheetName, string cellName)
```

| Parameter | Type | Description |
| --- | --- | --- |
| worksheetName | String | نام برگه‌کار. |
| cellName | String | مرجع سلول به سبک اکسل (مثلاً "A1"، "C5"). |

### مقدار بازگشتی

سلول در مکان مشخص‌شده.

### مثال‌ها

مثال:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell("Sheet1", "B2");
Console.WriteLine(cell.Value.ToString());
```

### موارد مرتبط

* رابط [IExcelDataCell](../../iexceldatacell)
* رابط [IExcelDataWorkbook](../../iexceldataworkbook)
* فضای‌نام [Aspose.Slides.Excel](../../iexceldataworkbook)
* مجموعه [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->