---
title: GetCell
second_title: Aspose.Sildes برای .NET مرجع API
description: سلولی را از کاربرگ مشخص شده با استفاده از شاخص و مختصات سلول بازیابی می‌کند.
type: docs
weight: 20
url: /fa/aspose.slides.excel/exceldataworkbook/getcell/
---
## GetCell(int, int, int) {#getcell}

یک سلول را از کاربرگ مشخص شده با استفاده از شاخص و مختصات سلول بازیابی می‌کند.

```csharp
public IExcelDataCell GetCell(int worksheetIndex, int row, int column)
```

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| worksheetIndex | Int32 | شاخص کاربرگ با مبنای صفر. |
| row | Int32 | شاخص ردیف سلول با مبنای صفر. |
| column | Int32 | شاخص ستون سلول با مبنای صفر. |

### مقدار بازگشتی

سلول در مکان مشخص شده.

### مثال‌ها

مثال:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell(1, 1, 1);
Console.WriteLine(cell.Value.ToString());
```

### موارد مرتبط

* رابط [IExcelDataCell](../../iexceldatacell)
* کلاس [ExcelDataWorkbook](../../exceldataworkbook)
* فضای‌نام [Aspose.Slides.Excel](../../exceldataworkbook)
* مجتمع [Aspose.Slides](../../../)

---

## GetCell(string, int, int) {#getcell_2}

یک سلول را از کاربرگ مشخص شده با استفاده از نام و مختصات سلول بازیابی می‌کند.

```csharp
public IExcelDataCell GetCell(string worksheetName, int row, int column)
```

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| worksheetName | String | نام کاربرگ. |
| row | Int32 | شاخص ردیف سلول با مبنای صفر. |
| column | Int32 | شاخص ستون سلول با مبنای صفر. |

### مقدار بازگشتی

سلول در مکان مشخص شده.

### مثال‌ها

مثال:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell("Sheet1", 1, 1);
Console.WriteLine(cell.Value.ToString());
```

### موارد مرتبط

* رابط [IExcelDataCell](../../iexceldatacell)
* کلاس [ExcelDataWorkbook](../../exceldataworkbook)
* فضای‌نام [Aspose.Slides.Excel](../../exceldataworkbook)
* مجتمع [Aspose.Slides](../../../)

---

## GetCell(int, string) {#getcell_1}

یک سلول را از کاربرگ مشخص شده با استفاده از شاخص و نام سلول به سبک اکسل (مانند "B2") بازیابی می‌کند.

```csharp
public IExcelDataCell GetCell(int worksheetIndex, string cellName)
```

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| worksheetIndex | Int32 | شاخص کاربرگ با مبنای صفر. |
| cellName | String | مرجع سلول به سبک اکسل (مانند "A1", "C5"). |

### مقدار بازگشتی

سلول در مکان مشخص شده.

### مثال‌ها

مثال:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell(1, "B2");
Console.WriteLine(cell.Value.ToString());
```

### موارد مرتبط

* رابط [IExcelDataCell](../../iexceldatacell)
* کلاس [ExcelDataWorkbook](../../exceldataworkbook)
* فضای‌نام [Aspose.Slides.Excel](../../exceldataworkbook)
* مجتمع [Aspose.Slides](../../../)

---

## GetCell(string, string) {#getcell_3}

یک سلول را از کاربرگ مشخص شده با استفاده از نام سلول به سبک اکسل (مانند "B2") بازیابی می‌کند.

```csharp
public IExcelDataCell GetCell(string worksheetName, string cellName)
```

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| worksheetName | String | نام کاربرگ. |
| cellName | String | مرجع سلول به سبک اکسل (مانند "A1", "C5"). |

### مقدار بازگشتی

سلول در مکان مشخص شده.

### مثال‌ها

مثال:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell("Sheet1", "B2");
Console.WriteLine(cell.Value.ToString());
```

### موارد مرتبط

* رابط [IExcelDataCell](../../iexceldatacell)
* کلاس [ExcelDataWorkbook](../../exceldataworkbook)
* فضای‌نام [Aspose.Slides.Excel](../../exceldataworkbook)
* مجتمع [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->