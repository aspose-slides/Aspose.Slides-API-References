---
title: AddOleObjectFrame
second_title: Aspose.Sildes voor .NET API-referentie
description: Maakt een nieuw OLE-objectframe en voegt dit toe aan het einde van de vormverzameling.
type: docs
weight: 150
url: /nl/aspose.slides/shapecollection/addoleobjectframe/
---
## AddOleObjectFrame(float, float, float, float, IOleEmbeddedDataInfo) {#addoleobjectframe}

Maakt een nieuw OLE-objectframe en voegt dit toe aan het einde van de vormverzameling.

```csharp
public IOleObjectFrame AddOleObjectFrame(float x, float y, float width, float height, 
    IOleEmbeddedDataInfo dataInfo)
```

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| x | Single | De x-coördinaat van het nieuwe OLE-frame, in punten. |
| y | Single | De y-coördinaat van het nieuwe OLE-frame, in punten. |
| width | Single | De breedte van het nieuwe OLE-frame, in punten. |
| height | Single | De hoogte van het nieuwe OLE-frame, in punten. |
| dataInfo | IOleEmbeddedDataInfo | De informatie over de ingebedde OLE-gegevens ([`IOleEmbeddedDataInfo`](../../ioleembeddeddatainfo)). |

### Retourwaarde

De nieuw aangemaakte [`IOleObjectFrame`](../../ioleobjectframe).

### Voorbeelden

De volgende voorbeelden laten zien hoe OLE-objectframes aan dia's van een PowerPoint-presentatie kunnen worden toegevoegd.

```csharp
[C#]
// Instantieert de Presentation-klasse die het PPTX-bestand voorstelt
using (Presentation pres = new Presentation())
{
    // Toegang tot de eerste dia
    ISlide sld = pres.Slides[0];
    // Laadt een Excel-bestand naar een stream
    MemoryStream mstream = new MemoryStream();
    using (FileStream fs = new FileStream("book1.xlsx", FileMode.Open, FileAccess.Read))
    {
        byte[] buf = new byte[4096];
        while (true)
        {
            int bytesRead = fs.Read(buf, 0, buf.Length);
            if (bytesRead <= 0)
                break;
            mstream.Write(buf, 0, bytesRead);
        }
    }
    // Maakt een data-object voor insluiten
    IOleEmbeddedDataInfo dataInfo = new OleEmbeddedDataInfo(mstream.ToArray(), "xlsx");
    // Voegt een Ole Object Frame-vorm toe
    IOleObjectFrame oleObjectFrame = sld.Shapes.AddOleObjectFrame(0, 0, pres.SlideSize.Size.Width,
        pres.SlideSize.Size.Height, dataInfo);
    //Schrijft het PPTX-bestand naar schijf
    pres.Save("OleEmbed_out.pptx", SaveFormat.Pptx);
}
```

### Zie ook

* interface [IOleObjectFrame](../../ioleobjectframe)
* interface [IOleEmbeddedDataInfo](../../ioleembeddeddatainfo)
* class [ShapeCollection](../../shapecollection)
* namespace [Aspose.Slides](../../shapecollection)
* assembly [Aspose.Slides](../../../)

---

## AddOleObjectFrame(float, float, float, float, string, string) {#addoleobjectframe_1}

Maakt een nieuw OLE-objectframe en voegt dit toe aan het einde van de vormverzameling.

```csharp
public IOleObjectFrame AddOleObjectFrame(float x, float y, float width, float height, 
    string className, string path)
```

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| x | Single | De x-coördinaat van het nieuwe OLE-frame, in punten. |
| y | Single | De y-coördinaat van het nieuwe OLE-frame, in punten. |
| width | Single | De breedte van het nieuwe OLE-frame, in punten. |
| height | Single | De hoogte van het nieuwe OLE-frame, in punten. |
| className | String | De klassenaam van het OLE-object. |
| path | String | Het pad naar het gekoppelde bestand. |

### Retourwaarde

De nieuw aangemaakte [`IOleObjectFrame`](../../ioleobjectframe).

### Zie ook

* interface [IOleObjectFrame](../../ioleobjectframe)
* class [ShapeCollection](../../shapecollection)
* namespace [Aspose.Slides](../../shapecollection)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->