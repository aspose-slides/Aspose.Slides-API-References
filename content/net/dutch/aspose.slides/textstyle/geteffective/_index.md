---
title: GetEffective
second_title: Aspose.Sildes voor .NET API-referentie
description: Haalt de effectieve tekstopmaakgegevens van de tekststijl op met de toegepaste overerving.
type: docs
weight: 20
url: /nl/aspose.slides/textstyle/geteffective/
---
## TextStyle.GetEffective methode

Haalt de effectieve tekstopmaakgegevens van de tekststijl op met de toegepaste overerving.

```csharp
public ITextStyleEffectiveData GetEffective()
```

### Retourwaarde

Een [`ITextStyleEffectiveData`](../../itextstyleeffectivedata).

### Voorbeelden

Dit voorbeeld laat zien hoe enkele effectieve tekststijl-eigenschappen worden opgehaald.

```csharp
[C#]
using (Presentation pres = new Presentation(@"MyPresentation.pptx"))
{
    IAutoShape shape = pres.Slides[0].Shapes[0] as IAutoShape;
    ITextStyleEffectiveData effectiveTextStyle = shape.TextFrame.TextFrameFormat.TextStyle.GetEffective();

    for (int i = 0; i <= 8; i++)
    {
        IParagraphFormatEffectiveData effectiveStyleLevel = effectiveTextStyle.GetLevel(i);
        Console.WriteLine("= Effective paragraph formatting for style level #" + i + " =");

        Console.WriteLine("Depth: " + effectiveStyleLevel.Depth);
        Console.WriteLine("Indent: " + effectiveStyleLevel.Indent);
        Console.WriteLine("Alignment: " + effectiveStyleLevel.Alignment);
        Console.WriteLine("Font alignment: " + effectiveStyleLevel.FontAlignment);
    }
}
```

### Zie ook

* interface [ITextStyleEffectiveData](../../itextstyleeffectivedata)
* klasse [TextStyle](../../textstyle)
* namespace [Aspose.Slides](../../textstyle)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->