---
title: GetBasePlaceholder
second_title: Aspose.Sildes voor .NET API-referentie
description: Retourneert een basis placeholder shape van de lay-out en/of master slide waarvan de huidige shape is geërfd. Er wordt null geretourneerd als de huidige shape niet is geërfd.
type: docs
weight: 310
url: /nl/aspose.slides/ishape/getbaseplaceholder/
---
## IShape.GetBasePlaceholder methode

Retourneert een basic placeholder shape (shape uit de lay-out en/of master slide waarvan de huidige shape is geërfd). Er wordt null geretourneerd als de huidige shape niet is geërfd.

```csharp
public IShape GetBasePlaceholder()
```

### Voorbeelden

```csharp
[C#]
// haal alle (master/layout/slide) geanimeerde effecten van de placeholder shape op
using (Presentation pres = new Presentation("sample.pptx"))
{
    ISlide slide = pres.Slides[0];
    IShape shape = slide.Shapes[0];
    IEffect[] shapeEffects = slide.Timeline.MainSequence.GetEffectsByShape(shape);

    IShape layoutShape = shape.GetBasePlaceholder();
    IEffect[] layoutShapeEffects = slide.LayoutSlide.Timeline.MainSequence.GetEffectsByShape(layoutShape);

    IShape masterShape = layoutShape.GetBasePlaceholder();
    IEffect[] masterShapeEffects = slide.LayoutSlide.MasterSlide.Timeline.MainSequence.GetEffectsByShape(masterShape);
}
```

### Zie ook

* interface [IShape](../../ishape)
* naamruimte [Aspose.Slides](../../ishape)
* assemblage [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->