---
title: WriteAsEmf
second_title: Aspose.Sildes voor .NET API-referentie
description: Slaat de SVG-afbeelding op als een EMF-bestand.
type: docs
weight: 60
url: /nl/aspose.slides/svgimage/writeasemf/
---
## SvgImage.WriteAsEmf methode

Slaat de SVG-afbeelding op als een EMF-bestand.

```csharp
public void WriteAsEmf(Stream stream)
```

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| stream | Stream | Doelstream |

### Uitzonderingen

| uitzondering | voorwaarde |
| --- | --- |
| ArgumentNullException | Doelstream is **null** |

### Voorbeelden

Het volgende voorbeeld laat zien hoe u de SVG-afbeelding opslaat naar het metabestand.

```csharp
[C#]
// Maakt de nieuwe SVG-afbeelding
ISvgImage svgImage = new SvgImage(System.IO.File.ReadAllText("content.svg"));

// Slaat de SVG-afbeelding op als een metabestand
using (var fileStream = System.IO.File.OpenWrite("SvgAsEmf.emf"))
    svgImage.WriteAsEmf(fileStream);
```

Dit voorbeeld toont hoe u de SVG-afbeelding als metabestand toevoegt aan de afbeeldingsverzameling van de presentatie.

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    // Maakt de nieuwe SVG-afbeelding
    ISvgImage svgImage = new SvgImage(System.IO.File.ReadAllText("content.svg"));
    using (var memStream = new MemoryStream())
    {
        // Slaat de SVG-afbeelding op als een metabestand
        svgImage.WriteAsEmf(memStream);
        // Voegt metabestand toe aan de afbeeldingverzameling
        pres.Images.AddImage(memStream.ToArray());
    }
}
```

### Zie ook

* klasse [SvgImage](../../svgimage)
* naamruimte [Aspose.Slides](../../svgimage)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->