---
title: CompressImage
second_title: Aspose.Sildes voor .NET API-referentie
description: Comprimeert de afbeelding door de grootte te verkleinen op basis van de vormgrootte en de gespecificeerde resolutie. Optioneel verwijdert het ook bijgesneden delen.
type: docs
weight: 180
url: /nl/aspose.slides/picturefillformat/compressimage/
---
## CompressImage(bool, PicturesCompression) {#compressimage}

Comprimeert de afbeelding door de grootte te verkleinen op basis van de vormgrootte en de gespecificeerde resolutie. Optioneel verwijdert het ook bijgesneden delen.

```csharp
public bool CompressImage(bool deleteCroppedAreasOfImage, PicturesCompression resolution)
```

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| deleteCroppedAreasOfImage | Boolean | Als true, zal de methode de bijgesneden delen van de afbeelding verwijderen, waardoor de grootte mogelijk verder wordt verkleind. |
| resolution | PicturesCompression | De doelformaatresolutie voor compressie, gespecificeerd als een waarde van de [`PicturesCompression`](../../../aspose.slides.export/picturescompression) enum. |

### Retourwaarde

Een Boolean die aangeeft of de afbeelding succesvol is gecomprimeerd. Retourneert `true` als de afbeelding is verkleind of bijgesneden, anders `false`.

### Uitzonderingen

| Uitzondering | Voorwaarde |
| --- | --- |
| ArgumentException | Wordt gegooid wanneer de resolutie geen geldige waarde is. |

### Opmerkingen

Deze methode wijzigt de grootte en resolutie van de afbeelding, vergelijkbaar met de functie “Picture Format -> Compress Pictures” van PowerPoint.

### Voorbeelden

Het volgende voorbeeld toont hoe de `CompressImage` methode kan worden gebruikt om de grootte van een afbeelding in een presentatie te verkleinen door een doelformaatresolutie in te stellen en bijgesneden delen te verwijderen:

```csharp
using (Presentation presentation = new Presentation("demo.pptx"))
{
    ISlide slide = presentation.Slides[0];
    IPictureFrame picFrame = slide.Shapes[0] as IPictureFrame;
    // Comprimeer de afbeelding met een doelresolutie van 150 DPI (webresolutie) en verwijder bijgesneden delen
    bool result = picFrame.PictureFormat.CompressImage(true, PicturesCompression.Dpi150);
}
```

### Zie ook

* enum [PicturesCompression](../../../aspose.slides.export/picturescompression)
* klasse [PictureFillFormat](../../picturefillformat)
* naamruimte [Aspose.Slides](../../picturefillformat)
* assemblage [Aspose.Slides](../../../)

---

## CompressImage(bool, float) {#compressimage_1}

Comprimeert de afbeelding door de grootte te verkleinen op basis van de vormgrootte en de gespecificeerde resolutie. Optioneel verwijdert het ook bijgesneden delen.

```csharp
public bool CompressImage(bool deleteCroppedAreasOfImage, float resolution)
```

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| deleteCroppedAreasOfImage | Boolean | Als true, zal de methode de bijgesneden delen van de afbeelding verwijderen, waardoor de grootte mogelijk verder wordt verkleind. |
| resolution | Single | De doelformaatresolutie in DPI. Deze waarde moet positief zijn en bepaalt hoe de afbeelding wordt verkleind. |

### Retourwaarde

Een Boolean die aangeeft of de afbeelding succesvol is gecomprimeerd. Retourneert `true` als de afbeelding is verkleind of bijgesneden, anders `false`.

### Uitzonderingen

| Uitzondering | Voorwaarde |
| --- | --- |
| ArgumentException | Wordt gegooid wanneer de resolutie geen positieve waarde is. |

### Opmerkingen

Deze methode wijzigt de grootte en resolutie van de afbeelding, vergelijkbaar met de functie “Picture Format -> Compress Pictures” van PowerPoint.

### Voorbeelden

Het volgende voorbeeld toont hoe de `CompressImage` methode kan worden gebruikt om de grootte van een afbeelding in een presentatie te verkleinen door een doelformaatresolutie in te stellen en bijgesneden delen te verwijderen:

```csharp
[C#]
using (Presentation presentation = new Presentation("demo.pptx"))
{
    ISlide slide = presentation.Slides[0];

    // Haalt het PictureFrame op
    IPictureFrame picFrame = slide.Shapes[0] as IPictureFrame;

    // Comprimeer de afbeelding met een doelresolutie van 150 DPI (webresolutie) en verwijder bijgesneden delen
    bool result=picFrame.PictureFormat.CompressImage(true, 150f); // Webresolutie
}
```

### Zie ook

* klasse [PictureFillFormat](../../picturefillformat)
* naamruimte [Aspose.Slides](../../picturefillformat)
* assemblage [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->