---
title: CompressImage
second_title: Aspose.Sildes voor .NET API-referentie
description: Comprimeert de afbeelding door de grootte te verkleinen op basis van de vormgrootte en de opgegeven resolutie. Optioneel verwijdert het ook bijgesneden gebieden.
type: docs
weight: 190
url: /nl/aspose.slides/ipicturefillformat/compressimage/
---
## CompressImage(bool, PicturesCompression) {#compressimage}

Comprimeert de afbeelding door de grootte te verkleinen op basis van de vormgrootte en de opgegeven resolutie. Optioneel verwijdert het ook bijgesneden delen.

```csharp
public bool CompressImage(bool deleteCroppedAreasOfImage, PicturesCompression resolution)
```

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| deleteCroppedAreasOfImage | Boolean | If true, the method will remove the cropped areas of the image, potentially further reducing its size. |
| resolution | PicturesCompression | The target resolution for compression, specified as a value of the [`PicturesCompression`](../../../aspose.slides.export/picturescompression) enum. |

### Retourwaarde

Een Boolean die aangeeft of de afbeelding succesvol is gecomprimeerd. Retourneert `true` als de afbeelding is verkleind of bijgesneden, anders `false`.

### Uitzonderingen

| uitzondering | conditie |
| --- | --- |
| ArgumentException | Wordt gegooid wanneer de resolutie geen geldige waarde is. |

### Opmerkingen

Deze methode wijzigt de grootte en resolutie van de afbeelding, vergelijkbaar met de PowerPoint-functie "Picture Format -> Compress Pictures".

### Voorbeelden

Het volgende voorbeeld laat zien hoe de `CompressImage`-methode kan worden gebruikt om de grootte van een afbeelding in een presentatie te verkleinen door een doelfunctie in te stellen en bijgesneden delen te verwijderen:

```csharp
using (Presentation presentation = new Presentation("demo.pptx"))
{
    ISlide slide = presentation.Slides[0];
    IPictureFrame picFrame = slide.Shapes[0] as IPictureFrame;
    // Comprimeer de afbeelding met een doelresolutie van 150 DPI (webresolutie) en verwijder bijgesneden gebieden
    bool result = picFrame.PictureFormat.CompressImage(true, PicturesCompression.Dpi150);
}
```

### Zie ook

* enum [PicturesCompression](../../../aspose.slides.export/picturescompression)
* interface [IPictureFillFormat](../../ipicturefillformat)
* namespace [Aspose.Slides](../../ipicturefillformat)
* assembly [Aspose.Slides](../../../)

---

## CompressImage(bool, float) {#compressimage_1}

Comprimeert de afbeelding door de grootte te verkleinen op basis van de vormgrootte en de opgegeven resolutie. Optioneel verwijdert het ook bijgesneden delen.

```csharp
public bool CompressImage(bool deleteCroppedAreasOfImage, float resolution)
```

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| deleteCroppedAreasOfImage | Boolean | If true, the method will remove the cropped areas of the image, potentially further reducing its size. |
| resolution | Single | The target resolution in DPI. This value must be positive and defines how the image will be resized. |

### Retourwaarde

Een Boolean die aangeeft of de afbeelding succesvol is gecomprimeerd. Retourneert `true` als de afbeelding is verkleind of bijgesneden, anders `false`.

### Uitzonderingen

| uitzondering | conditie |
| --- | --- |
| ArgumentException | Wordt gegooid wanneer de resolutie geen positieve waarde is. |

### Opmerkingen

Deze methode wijzigt de grootte en resolutie van de afbeelding, vergelijkbaar met de PowerPoint-functie "Picture Format -> Compress Pictures".

### Voorbeelden

Het volgende voorbeeld laat zien hoe de `CompressImage`-methode kan worden gebruikt om de grootte van een afbeelding in een presentatie te verkleinen door een doelfunctie in te stellen en bijgesneden delen te verwijderen:

```csharp
[C#]
using (Presentation presentation = new Presentation("demo.pptx"))
{
    ISlide slide = presentation.Slides[0];

    // Haalt het PictureFrame op
    IPictureFrame picFrame = slide.Shapes[0] as IPictureFrame;

    // Comprimeer de afbeelding met een doelresolutie van 150 DPI (webresolutie) en verwijder bijgesneden gebieden
    bool result=picFrame.PictureFormat.CompressImage(true, 150f); // Webresolutie
}
```

### Zie ook

* interface [IPictureFillFormat](../../ipicturefillformat)
* namespace [Aspose.Slides](../../ipicturefillformat)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->