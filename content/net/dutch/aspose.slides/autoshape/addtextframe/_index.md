---
title: AddTextFrame
second_title: Aspose.Sildes voor .NET API-referentie
description: Voegt een nieuw TextFrame toe aan een vorm. Als de vorm al een TextFrame heeft, wordt de tekst eenvoudigweg gewijzigd.
type: docs
weight: 60
url: /nl/aspose.slides/autoshape/addtextframe/
---
## AutoShape.AddTextFrame methode

Voegt een nieuw TextFrame toe aan een vorm. Als de vorm al een TextFrame heeft, wordt de tekst eenvoudigweg gewijzigd.

```csharp
public ITextFrame AddTextFrame(string text)
```

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| text | String | Standaardtekst voor een nieuw TextFrame. |

### Voorbeelden

De volgende voorbeeldcode toont hoe u watermerktekst kunt toevoegen in een PowerPoint-presentatie.

```csharp
[C#]
	using (var presentation = new Presentation())
	{
		ISlide slide = presentation.Slides[0];
		IAutoShape watermarkShape = slide.Shapes.AddAutoShape(ShapeType.Triangle, 0, 0, 150, 50);
		ITextFrame watermarkTextFrame = watermarkShape.AddTextFrame("Watermark");
	}
```

Het volgende voorbeeld toont hoe u een Text Box kunt maken op een slide.

```csharp
[C#]
// Instantieert een Presentation
using (Presentation pres = new Presentation())
{
    // Haalt de eerste slide op in de presentatie
    ISlide sld = pres.Slides[0];
    // Voegt een AutoShape toe met als type Rectangle
    IAutoShape ashp = sld.Shapes.AddAutoShape(ShapeType.Rectangle, 150, 75, 150, 50);
    // Voegt TextFrame toe aan de Rectangle
    ashp.AddTextFrame(" ");
    // Toegang tot het TextFrame
    ITextFrame txtFrame = ashp.TextFrame;
    // Creëert het Paragraph-object voor het TextFrame
    IParagraph para = txtFrame.Paragraphs[0];
    // Creëert een Portion-object voor de Paragraph
    IPortion portion = para.Portions[0];
    // Stelt de tekst in
    portion.Text = "Aspose TextBox";
    // Slaat de presentatie op op schijf
    pres.Save("TextBox_out.pptx", Aspose.Slides.Export.SaveFormat.Pptx);
}
```

Het volgende voorbeeld toont hoe u een kolom kunt toevoegen in een Text Box.

```csharp
[C#]
using (Presentation presentation = new Presentation())
{
	// Haalt de eerste slide op in de presentatie
	ISlide slide = presentation.Slides[0];
	// Voeg een AutoShape toe met als type Rectangle
	IAutoShape aShape = slide.Shapes.AddAutoShape(ShapeType.Rectangle, 100, 100, 300, 300);
	// Voeg TextFrame toe aan de Rectangle
	aShape.AddTextFrame("All these columns are limited to be within a single text container -- " +
	"you can add or delete text and the new or remaining text automatically adjusts " +
	"itself to flow within the container. You cannot have text flow from one container " +
	"to other though -- we told you PowerPoint's column options for text are limited!");
	// Haalt het tekstformaat van TextFrame op
	ITextFrameFormat format = aShape.TextFrame.TextFrameFormat;
	// Bepaalt het aantal kolommen in TextFrame
	format.ColumnCount = 3;
	// Bepaalt de ruimte tussen kolommen
	format.ColumnSpacing = 10;
	// Slaat de presentatie op
	presentation.Save("ColumnCount.pptx", SaveFormat.Pptx);
}
```

### Zie ook

* interface [ITextFrame](../../itextframe)
* klasse [AutoShape](../../autoshape)
* naamruimte [Aspose.Slides](../../autoshape)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->