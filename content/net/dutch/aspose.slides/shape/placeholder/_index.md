---
title: Placeholder
second_title: Aspose.Sildes for .NET API Referentie
description: Retourneert de placeholder voor een vorm. Retourneert null als de vorm geen placeholder heeft. Alleen-lezen IPlaceholderaspose.slides/iplaceholder.
type: docs
weight: 210
url: /nl/aspose.slides/shape/placeholder/
---
## Shape.Placeholder eigenschap

Retourneert de placeholder voor een vorm. Retourneert null als de vorm geen placeholder heeft. Alleen-lezen [`IPlaceholder`](../../iplaceholder).

```csharp
public IPlaceholder Placeholder { get; }
```

### Voorbeelden

Het volgende voorbeeld laat zien hoe je Text in Placeholder wijzigt.

```csharp
[C#]
// Instantieert een Presentation-klasse
using (Presentation pres = new Presentation("ReplacingText.pptx"))
{
    // Toegang tot de eerste dia
    ISlide sld = pres.Slides[0];
    // Doorloopt vormen om de placeholder te vinden
    foreach (IShape shp in sld.Shapes)
        if (shp.Placeholder != null)
        {
            // Wijzigt de tekst in elke placeholder
            ((IAutoShape)shp).TextFrame.Text = "This is a Placeholder";
        }
    // Slaat de presentatie op schijf
    pres.Save("output_out.pptx", Aspose.Slides.Export.SaveFormat.Pptx);
}
```

Het volgende voorbeeld laat zien hoe je Prompt Text in Placeholder instelt.

```csharp
[C#]
using (Presentation pres = new Presentation("Presentation2.pptx"))
{
    ISlide slide = pres.Slides[0];
    foreach (IShape shape in slide.Slide.Shapes) // Doorloopt de dia
    {
        if (shape.Placeholder != null && shape is AutoShape)
        {
            string text = "";
            if (shape.Placeholder.Type == PlaceholderType.CenteredTitle) // PowerPoint toont "Klik om titel toe te voegen"
            {
                text = "Add Title";
            }
            else if (shape.Placeholder.Type == PlaceholderType.Subtitle) // Voegt ondertitel toe
            {
                text = "Add Subtitle";
            }
            ((IAutoShape)shape).TextFrame.Text = text;
            Console.WriteLine($"Placeholder with text: {text}");
        }
    }
    pres.Save("Placeholders_PromptText.pptx", SaveFormat.Pptx);
}
```

### Zie ook

* interface [IPlaceholder](../../iplaceholder)
* klasse [Shape](../../shape)
* naamruimte [Aspose.Slides](../../shape)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->