---
title: SlideSize
second_title: Aspose.Sildes voor .NET API Referentie
description: Retourneert slide size object. Alleen-lezen ISlideSizeaspose.slides/islidesize.
type: docs
weight: 260
url: /nl/aspose.slides/presentation/slidesize/
---
## Presentation.SlideSize property

Retourneert een slide-size-object. Alleen-lezen [`ISlideSize`](../../islidesize).

```csharp
public ISlideSize SlideSize { get; }
```

### Voorbeelden

Het volgende voorbeeld toont hoe u de slidegrootte kunt wijzigen in een PowerPoint-presentatie.

```csharp
[C#]
using (Presentation pres = new Presentation("pres-4x3-aspect-ratio.pptx"))
{
    pres.SlideSize.SetSize(SlideSizeType.OnScreen16x9, SlideSizeScaleType.DoNotScale);
    pres.Save("pres-4x3-aspect-ratio.pptx", SaveFormat.Pptx);
}
```

Het volgende voorbeeld toont hoe u de slidegrootte kunt instellen met betrekking tot het schalen van de inhoud voor een PowerPoint-presentatie.

```csharp
[C#]
// Maak een Presentation-object aan dat een presentatiebestand vertegenwoordigt
using(Presentation presentation = new Presentation("AccessSlides.pptx")) {
  using(Presentation auxPresentation = new Presentation()) {
    ISlide slide = presentation.Slides[0];
    // Stel de slidegrootte van gegenereerde presentaties in op die van de bron
    presentation.SlideSize.SetSize(540, 720, SlideSizeScaleType.EnsureFit); // Methode SetSize wordt gebruikt om de slidegrootte in te stellen met geschaalde inhoud om te passen
    presentation.SlideSize.SetSize(SlideSizeType.A4Paper, SlideSizeScaleType.Maximize); // Methode SetSize wordt gebruikt om de slidegrootte in te stellen met maximale inhoudsgrootte
    // Sla de presentatie op naar schijf
    auxPresentation.Save("Set_Size&Type_out.pptx", SaveFormat.Pptx);
  }
}
```

Het volgende voorbeeld toont hoe u aangepaste slidegroottes kunt specificeren in een PowerPoint-presentatie.

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    pres.SlideSize.SetSize(780, 540, SlideSizeScaleType.DoNotScale); // A4-papiergrootte
    pres.Save("pres-a4-slide-size.pptx", SaveFormat.Pptx);
}
```

### Zie ook

* interface [ISlideSize](../../islidesize)
* klasse [Presentation](../../presentation)
* naamruimte [Aspose.Slides](../../presentation)
* assemblage [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->