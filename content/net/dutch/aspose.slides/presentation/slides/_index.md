---
title: Slides
second_title: Aspose.Sildes voor .NET API-referentie
description: Retourneert een lijst van alle dia's die in de presentatie zijn gedefinieerd. Alleen-lezen ISlideCollectionaspose.slides/islidecollection.
type: docs
weight: 240
url: /nl/aspose.slides/presentation/slides/
---
## Presentation.Slides eigenschap

Returns a list of all slides that are defined in the presentation. Read-only [`ISlideCollection`](../nl/../islidecollection).

```csharp
public ISlideCollection Slides { get; }
```

### Voorbeelden

Het volgende voorbeeld laat zien hoe de achtergrondkleur van dia's van een PowerPoint-presentatie kan worden ingesteld.

```csharp
[C#]
// Instantieer de Presentation-klasse die het presentatiebestand vertegenwoordigt
using (Presentation pres = new Presentation())
{
    // Stel de achtergrondkleur van de eerste ISlide in op blauw
    pres.Slides[0].Background.Type = BackgroundType.OwnBackground;
    pres.Slides[0].Background.FillFormat.FillType = FillType.Solid;
    pres.Slides[0].Background.FillFormat.SolidFillColor.Color = Color.Blue;
    pres.Save("ContentBG_out.pptx", SaveFormat.Pptx);
}
```

Het volgende voorbeeld laat zien hoe de achtergrondafbeelding van dia's van een PowerPoint-presentatie kan worden ingesteld.

```csharp
[C#]
// Instantieer de Presentation-klasse die het presentatiebestand vertegenwoordigt
using (Presentation pres = new Presentation("SetImageAsBackground.pptx"))
{
    // Stel de achtergrond in met afbeelding
    pres.Slides[0].Background.Type = BackgroundType.OwnBackground;
    pres.Slides[0].Background.FillFormat.FillType = FillType.Picture;
    pres.Slides[0].Background.FillFormat.PictureFillFormat.PictureFillMode = PictureFillMode.Stretch;
    // Stel de afbeelding in
    System.Drawing.Image img = (System.Drawing.Image)new Bitmap(dataDir + "Tulips.jpg");
    // Voeg afbeelding toe aan de afbeeldingsverzameling van de presentatie
    IPPImage imgx = pres.Images.AddImage(img);
    pres.Slides[0].Background.FillFormat.PictureFillFormat.Picture.Image = imgx;
    // Schrijf de presentatie naar schijf
    pres.Save("ContentBG_Img_out.pptx", SaveFormat.Pptx);
}
```

Het volgende voorbeeld laat zien hoe een diaovergang aan een presentatie kan worden toegevoegd.

```csharp
[C#]
// Instantieer de Presentation-klasse om het bronpresentatiebestand te laden
using (Presentation presentation = new Presentation("AccessSlides.pptx"))
{
    // Pas cirkeltype overgang toe op dia 1
    presentation.Slides[0].SlideShowTransition.Type = TransitionType.Circle;
    // Pas kamtype overgang toe op dia 2
    presentation.Slides[1].SlideShowTransition.Type = TransitionType.Comb;
    // Schrijf de presentatie naar schijf
    presentation.Save("SampleTransition_out.pptx", SaveFormat.Pptx);
}
```

Het volgende voorbeeld laat zien hoe een geavanceerde diaovergang kan worden toegevoegd.

```csharp
[C#]
// Instantieer de Presentation-klasse die een presentatiebestand vertegenwoordigt
using (Presentation pres = new Presentation("BetterSlideTransitions.pptx"))
{
    // Pas cirkeltype overgang toe op dia 1
    pres.Slides[0].SlideShowTransition.Type = TransitionType.Circle;
    // Stel de overgangstijd in op 3 seconden
    pres.Slides[0].SlideShowTransition.AdvanceOnClick = true;
    pres.Slides[0].SlideShowTransition.AdvanceAfterTime = 3000;
    // Pas kamtype overgang toe op dia 2
    pres.Slides[1].SlideShowTransition.Type = TransitionType.Comb;
    // Stel de overgangstijd in op 5 seconden
    pres.Slides[1].SlideShowTransition.AdvanceOnClick = true;
    pres.Slides[1].SlideShowTransition.AdvanceAfterTime = 5000;
    // Pas zoomtype overgang toe op dia 3
    pres.Slides[2].SlideShowTransition.Type = TransitionType.Zoom;
    // Stel de overgangstijd in op 7 seconden
    pres.Slides[2].SlideShowTransition.AdvanceOnClick = true;
    pres.Slides[2].SlideShowTransition.AdvanceAfterTime = 7000;
    // Schrijf de presentatie naar schijf
    pres.Save("SampleTransition_out.pptx", SaveFormat.Pptx);
}
```

### Zie ook

* interface [ISlideCollection](../../islidecollection)
* klasse [Presentation](../../presentation)
* naamruimte [Aspose.Slides](../../presentation)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->