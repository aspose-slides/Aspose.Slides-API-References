---
title: Shape
second_title: Aspose.Sildes voor .NET API-referentie
description: Itereer elke Shapeaspose.slides.lowcode/foreach/shape in de Presentationaspose.slides/presentation. Shapes worden doorlopen in alle soorten dia's - Slideaspose.slides.lowcode/foreach/slide MasterSlideaspose.slides.lowcode/foreach/masterslide en LayoutSlideaspose.slides.lowcode/foreach/layoutslide
type: docs
weight: 50
url: /nl/aspose.slides.lowcode/foreach/shape/
---
## Shape(Presentation, ForEachShapeCallback) {#shape_2}

Itereer over elke `Shape` in de [`Presentation`](../../../aspose.slides/presentation). Shapes worden doorlopen in alle soorten dia's - [`Slide`](../slide), [`MasterSlide`](../masterslide) en [`LayoutSlide`](../layoutslide)

```csharp
public static void Shape(Presentation pres, ForEachShapeCallback forEachShape)
```

| Parameter | Type | Description |
| --- | --- | --- |
| pres | Presentation | Presentatie om lay-out shapes te itereren |
| forEachShape | ForEachShapeCallback | Callback die wordt aangeroepen voor elke shape |

### Voorbeelden

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Shape(pres, (shape, slide, index) => 
    {
        System.Console.WriteLine($"{shape.Name}, index: {index}");
    });
} 
```

### Zie ook

* klasse [Presentation](../../../aspose.slides/presentation)
* delegate [ForEachShapeCallback](../../foreach.foreachshapecallback)
* klasse [ForEach](../../foreach)
* naamruimte [Aspose.Slides.LowCode](../../foreach)
* assemblage [Aspose.Slides](../../../)

---

## Shape(Presentation, bool, ForEachShapeCallback) {#shape_1}

Itereer over elke [`Shape`](../shape) in de [`Presentation`](../../../aspose.slides/presentation). Shapes worden doorlopen in alle soorten dia's - [`Slide`](../slide), [`MasterSlide`](../masterslide), [`LayoutSlide`](../layoutslide) en [`NotesSlide`](../../../aspose.slides/notesslide) indien nodig.

```csharp
public static void Shape(Presentation pres, bool includeNotes, ForEachShapeCallback forEachShape)
```

| Parameter | Type | Description |
| --- | --- | --- |
| pres | Presentation | Presentatie om lay-out shapes te itereren |
| includeNotes | Boolean | Vlag die aangeeft of NotesSlides moeten worden opgenomen in de verwerking. |
| forEachShape | ForEachShapeCallback | Callback die wordt aangeroepen voor elke shape |

### Voorbeelden

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Shape(pres, true, (shape, slide, index) => 
    {
        System.Console.WriteLine($"{shape.Name}, index: {index}");
    });
} 
```

### Zie ook

* klasse [Presentation](../../../aspose.slides/presentation)
* delegate [ForEachShapeCallback](../../foreach.foreachshapecallback)
* klasse [ForEach](../../foreach)
* naamruimte [Aspose.Slides.LowCode](../../foreach)
* assemblage [Aspose.Slides](../../../)

---

## Shape(BaseSlide, ForEachShapeCallback) {#shape}

Itereer over elke [`Shape`](../shape) in de [`BaseSlide`](../../../aspose.slides/baseslide). [`BaseSlide`](../../../aspose.slides/baseslide) is het basistype voor [`Slide`](../slide), [`MasterSlide`](../masterslide) en [`LayoutSlide`](../layoutslide)

```csharp
public static void Shape(BaseSlide baseSlide, ForEachShapeCallback forEachShape)
```

| Parameter | Type | Description |
| --- | --- | --- |
| baseSlide | BaseSlide | Dia om lay-out shapes te itereren |
| forEachShape | ForEachShapeCallback | Callback die wordt aangeroepen voor elke shape |

### Voorbeelden

```csharp
(Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Slide(pres, (slide, index) =>
    {
        ForEach.Shape(slide, (shape, baseSlide, shapeIndex) =>
        {
            System.Console.WriteLine($"{shape.Name}, index: {shapeIndex}");
        });
    });
} 
```

### Zie ook

* klasse [BaseSlide](../../../aspose.slides/baseslide)
* delegate [ForEachShapeCallback](../../foreach.foreachshapecallback)
* klasse [ForEach](../../foreach)
* naamruimte [Aspose.Slides.LowCode](../../foreach)
* assemblage [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->