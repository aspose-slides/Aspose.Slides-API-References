---
title: InsertClone
second_title: Aspose.Sildes per .NET Riferimento API
description: Inserisce una copia di una master slide specificata nella posizione indicata della collezione. Anche le slide di layout collegate verranno copiate.
type: docs
weight: 80
url: /it/aspose.slides/masterslidecollection/insertclone/
---
## MasterSlideCollection.InsertClone metodo

Inserisce una copia di una master slide specificata nella posizione indicata della collezione. Anche le slide di layout collegate verranno copiate.

```csharp
public IMasterSlide InsertClone(int index, IMasterSlide sourceMaster)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| index | Int32 | Indice della nuova slide. |
| sourceMaster | IMasterSlide | Slide da clonare. |

### Valore restituito

Master slide inserita.

### Esempi

Il seguente esempio mostra come clonare una master slide in un'altra presentazione PowerPoint.

```csharp
[C#]
// Istanzia la classe Presentation per caricare il file della presentazione sorgente
using (Presentation srcPres = new Presentation("CloneToAnotherPresentationWithMaster.pptx"))
{
    // Istanzia la classe Presentation per la presentazione di destinazione (dove la slide verrà clonata)
    using (Presentation destPres = new Presentation())
    {
        // Istanzia ISlide dalla collezione di slide nella presentazione sorgente insieme a
        // slide master
        ISlide SourceSlide = srcPres.Slides[0];
        IMasterSlide SourceMaster = SourceSlide.LayoutSlide.MasterSlide;
		// Ottieni le slide master della presentazione di destinazione
        IMasterSlideCollection masters = destPres.Masters;
        // Clona la slide master desiderata dalla presentazione sorgente nella collezione di master nella
        // presentazione di destinazione
        IMasterSlide iSlide = masters.AddClone(SourceMaster);
        // Collezione di slide nella presentazione di destinazione
        ISlideCollection slds = destPres.Slides;
		// Clona la slide sorgente nella collezione di slide di destinazione.
        slds.AddClone(SourceSlide, iSlide, true);
        // Salva la presentazione di destinazione su disco
        destPres.Save("CloneToAnotherPresentationWithMaster_out.pptx", SaveFormat.Pptx);
    }
}
```

### Vedi anche

* interfaccia [IMasterSlide](../../imasterslide)
* classe [MasterSlideCollection](../../masterslidecollection)
* spazio dei nomi [Aspose.Slides](../../masterslidecollection)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->