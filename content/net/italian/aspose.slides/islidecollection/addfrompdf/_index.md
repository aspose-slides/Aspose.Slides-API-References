---
title: AddFromPdf
second_title: Aspose.Sildes per .NET Riferimento API
description: Crea diapositive dal documento PDF e le aggiunge alla fine della raccolta.
type: docs
weight: 50
url: /it/aspose.slides/islidecollection/addfrompdf/
---
## AddFromPdf(string) {#addfrompdf_2}

Crea diapositive dal documento PDF e le aggiunge alla fine della raccolta.

```csharp
public ISlide[] AddFromPdf(string path)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| path | String | Un percorso al documento PDF |

### Valore restituito

Diapositive aggiunte

### Esempi

Esempio:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    pres.Slides.AddFromPdf("document.pdf");
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Vedi anche

* interfaccia [ISlide](../../islide)
* interfaccia [ISlideCollection](../../islidecollection)
* spazio dei nomi [Aspose.Slides](../../islidecollection)
* assembly [Aspose.Slides](../../../)

---

## AddFromPdf(string, PdfImportOptions) {#addfrompdf_3}

Crea diapositive dal documento PDF e le aggiunge alla fine della raccolta tenendo conto delle opzioni di importazione PDF.

```csharp
public ISlide[] AddFromPdf(string path, PdfImportOptions pdfImportOptions)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| path | String | Un percorso al documento PDF |
| pdfImportOptions | PdfImportOptions | Opzioni per l'importazione PDF |

### Valore restituito

Diapositive aggiunte

### Esempi

Esempio:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    pres.Slides.AddFromPdf("document.pdf, new PdfImportOptions { DetectTables = true });
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Vedi anche

* interfaccia [ISlide](../../islide)
* classe [PdfImportOptions](../../../aspose.slides.import/pdfimportoptions)
* interfaccia [ISlideCollection](../../islidecollection)
* spazio dei nomi [Aspose.Slides](../../islidecollection)
* assembly [Aspose.Slides](../../../)

---

## AddFromPdf(Stream, PdfImportOptions) {#addfrompdf_1}

Crea diapositive dal documento PDF e le aggiunge alla fine della raccolta.

```csharp
public ISlide[] AddFromPdf(Stream pdfStream, PdfImportOptions pdfImportOptions)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| pdfStream | Stream | Un flusso che sarà usato come sorgente del documento PDF |
| pdfImportOptions | PdfImportOptions | Opzioni per l'importazione PDF |

### Valore restituito

Diapositive aggiunte

### Esempi

Esempio:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    using (Stream stream = new FileStream("document.pdf", FileMode.Open, FileAccess.Read, FileShare.Read))
    {
        pres.Slides.AddFromPdf(stream, new PdfImportOptions { DetectTables = true });
    }
    
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Vedi anche

* interfaccia [ISlide](../../islide)
* classe [PdfImportOptions](../../../aspose.slides.import/pdfimportoptions)
* interfaccia [ISlideCollection](../../islidecollection)
* spazio dei nomi [Aspose.Slides](../../islidecollection)
* assembly [Aspose.Slides](../../../)

---

## AddFromPdf(Stream) {#addfrompdf}

Crea diapositive dal documento PDF e le aggiunge alla fine della raccolta.

```csharp
public ISlide[] AddFromPdf(Stream pdfStream)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| pdfStream | Stream | Un flusso che sarà usato come sorgente del documento PDF |

### Valore restituito

Diapositive aggiunte

### Esempi

Esempio:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    using (Stream stream = new FileStream("document.pdf", FileMode.Open, FileAccess.Read, FileShare.Read))
    {
        pres.Slides.AddFromPdf(stream);
    }
    
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Vedi anche

* interfaccia [ISlide](../../islide)
* interfaccia [ISlideCollection](../../islidecollection)
* spazio dei nomi [Aspose.Slides](../../islidecollection)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->