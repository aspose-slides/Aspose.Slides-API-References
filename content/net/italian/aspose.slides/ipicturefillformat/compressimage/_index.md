---
title: CompressImage
second_title: Riferimento API Aspose.Sildes per .NET
description: Comprimi l'immagine riducendo le sue dimensioni in base alle dimensioni della forma e alla risoluzione specificata. Facoltativamente elimina anche le aree ritagliate.
type: docs
weight: 190
url: /it/aspose.slides/ipicturefillformat/compressimage/
---
## CompressImage(bool, PicturesCompression) {#compressimage}

Comprimi l'immagine riducendo le sue dimensioni in base alle dimensioni della forma e alla risoluzione specificata. Facoltativamente, elimina anche le aree ritagliate.

```csharp
public bool CompressImage(bool deleteCroppedAreasOfImage, PicturesCompression resolution)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| deleteCroppedAreasOfImage | Boolean | Se true, il metodo rimuoverà le aree ritagliate dell'immagine, riducendo ulteriormente le sue dimensioni. |
| resolution | PicturesCompression | La risoluzione target per la compressione, specificata come valore dell'enumerazione [`PicturesCompression`](../../../aspose.slides.export/picturescompression). |

### Valore di ritorno

Un Boolean che indica se l'immagine è stata compressa correttamente. Restituisce `true` se l'immagine è stata ridimensionata o ritagliata, altrimenti `false`.

### Eccezioni

| eccezione | condizione |
| --- | --- |
| ArgumentException | Generata quando la risoluzione non è un valore valido. |

### Osservazioni

Questo metodo modifica la dimensione e la risoluzione dell'immagine in modo simile alla funzione "Formato immagine -&gt; Comprimi immagini" di PowerPoint.

### Esempi

Il seguente esempio dimostra come utilizzare il metodo `CompressImage` per ridurre la dimensione di un'immagine in una presentazione impostando una risoluzione target e rimuovendo le aree ritagliate:

```csharp
using (Presentation presentation = new Presentation("demo.pptx"))
{
    ISlide slide = presentation.Slides[0];
    IPictureFrame picFrame = slide.Shapes[0] as IPictureFrame;
    // Comprimi l'immagine con una risoluzione target di 150 DPI (risoluzione Web) e rimuovi le aree ritagliate
    bool result = picFrame.PictureFormat.CompressImage(true, PicturesCompression.Dpi150);
}
```

### Vedi anche

* enum [PicturesCompression](../../../aspose.slides.export/picturescompression)
* interfaccia [IPictureFillFormat](../../ipicturefillformat)
* spazio dei nomi [Aspose.Slides](../../ipicturefillformat)
* assembly [Aspose.Slides](../../../)

---

## CompressImage(bool, float) {#compressimage_1}

Comprimi l'immagine riducendo le sue dimensioni in base alle dimensioni della forma e alla risoluzione specificata. Facoltativamente, elimina anche le aree ritagliate.

```csharp
public bool CompressImage(bool deleteCroppedAreasOfImage, float resolution)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| deleteCroppedAreasOfImage | Boolean | Se true, il metodo rimuoverà le aree ritagliate dell'immagine, riducendo ulteriormente le sue dimensioni. |
| resolution | Single | La risoluzione target in DPI. Questo valore deve essere positivo e definisce come l'immagine verrà ridimensionata. |

### Valore di ritorno

Un Boolean che indica se l'immagine è stata compressa correttamente. Restituisce `true` se l'immagine è stata ridimensionata o ritagliata, altrimenti `false`.

### Eccezioni

| eccezione | condizione |
| --- | --- |
| ArgumentException | Generata quando la risoluzione non è un valore positivo. |

### Osservazioni

Questo metodo modifica la dimensione e la risoluzione dell'immagine in modo simile alla funzione "Formato immagine -&gt; Comprimi immagini" di PowerPoint.

### Esempi

Il seguente esempio dimostra come utilizzare il metodo `CompressImage` per ridurre la dimensione di un'immagine in una presentazione impostando una risoluzione target e rimuovendo le aree ritagliate:

```csharp
[C#]
using (Presentation presentation = new Presentation("demo.pptx"))
{
    ISlide slide = presentation.Slides[0];

    // Ottiene il PictureFrame
    IPictureFrame picFrame = slide.Shapes[0] as IPictureFrame;

    // Comprimi l'immagine con una risoluzione target di 150 DPI (risoluzione Web) e rimuovi le aree ritagliate
    bool result=picFrame.PictureFormat.CompressImage(true, 150f); // risoluzione Web
}
```

### Vedi anche

* interfaccia [IPictureFillFormat](../../ipicturefillformat)
* spazio dei nomi [Aspose.Slides](../../ipicturefillformat)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->