---
title: Slides
second_title: Riferimento API Aspose.Sildes per .NET
description: Restituisce un elenco di tutte le diapositive definite nella presentazione. Sola lettura ISlideCollectionaspose.slides/islidecollection.
type: docs
weight: 240
url: /it/aspose.slides/presentation/slides/
---
## Presentation.Slides proprietà

Restituisce un elenco di tutte le diapositive definite nella presentazione. Sola lettura [`ISlideCollection`](../it/../islidecollection).

```csharp
public ISlideCollection Slides { get; }
```

### Esempi

Il seguente esempio mostra come impostare il colore di sfondo delle diapositive di una presentazione PowerPoint.

```csharp
[C#]
// Istanziare la classe Presentation che rappresenta il file di presentazione
using (Presentation pres = new Presentation())
{
    // Impostare il colore di sfondo della prima ISlide su Blu
    pres.Slides[0].Background.Type = BackgroundType.OwnBackground;
    pres.Slides[0].Background.FillFormat.FillType = FillType.Solid;
    pres.Slides[0].Background.FillFormat.SolidFillColor.Color = Color.Blue;
    pres.Save("ContentBG_out.pptx", SaveFormat.Pptx);
}
```

Il seguente esempio mostra come impostare l'immagine di sfondo delle diapositive di una presentazione PowerPoint.

```csharp
[C#]
// Istanziare la classe Presentation che rappresenta il file di presentazione
using (Presentation pres = new Presentation("SetImageAsBackground.pptx"))
{
    // Impostare lo sfondo con immagine
    pres.Slides[0].Background.Type = BackgroundType.OwnBackground;
    pres.Slides[0].Background.FillFormat.FillType = FillType.Picture;
    pres.Slides[0].Background.FillFormat.PictureFillFormat.PictureFillMode = PictureFillMode.Stretch;
    // Impostare l'immagine
    System.Drawing.Image img = (System.Drawing.Image)new Bitmap(dataDir + "Tulips.jpg");
    // Aggiungere l'immagine alla collezione di immagini della presentazione
    IPPImage imgx = pres.Images.AddImage(img);
    pres.Slides[0].Background.FillFormat.PictureFillFormat.Picture.Image = imgx;
    // Scrivere la presentazione su disco
    pres.Save("ContentBG_Img_out.pptx", SaveFormat.Pptx);
}
```

Il seguente esempio mostra come aggiungere una transizione alle diapositive di una presentazione.

```csharp
[C#]
// Instanziare la classe Presentation per caricare il file di presentazione sorgente
using (Presentation presentation = new Presentation("AccessSlides.pptx"))
{
    // Applicare la transizione di tipo Circle sulla diapositiva 1
    presentation.Slides[0].SlideShowTransition.Type = TransitionType.Circle;
    // Applicare la transizione di tipo Comb sulla diapositiva 2
    presentation.Slides[1].SlideShowTransition.Type = TransitionType.Comb;
    // Scrivere la presentazione su disco
    presentation.Save("SampleTransition_out.pptx", SaveFormat.Pptx);
}
```

Il seguente esempio mostra come aggiungere una transizione avanzata alle diapositive.

```csharp
[C#]
// Istanziare la classe Presentation che rappresenta un file di presentazione
using (Presentation pres = new Presentation("BetterSlideTransitions.pptx"))
{
    // Applicare la transizione di tipo Circle sulla diapositiva 1
    // Impostare il tempo di transizione a 3 secondi
    pres.Slides[0].SlideShowTransition.AdvanceOnClick = true;
    pres.Slides[0].SlideShowTransition.AdvanceAfterTime = 3000;
    // Applicare la transizione di tipo Comb sulla diapositiva 2
    // Impostare il tempo di transizione a 5 secondi
    pres.Slides[1].SlideShowTransition.AdvanceOnClick = true;
    pres.Slides[1].SlideShowTransition.AdvanceAfterTime = 5000;
    // Applicare la transizione di tipo Zoom sulla diapositiva 3
    // Impostare il tempo di transizione a 7 secondi
    pres.Slides[2].SlideShowTransition.AdvanceOnClick = true;
    pres.Slides[2].SlideShowTransition.AdvanceAfterTime = 7000;
    // Scrivere la presentazione su disco
    pres.Save("SampleTransition_out.pptx", SaveFormat.Pptx);
}
```

### Vedi anche

* interfaccia [ISlideCollection](../../islidecollection)
* classe [Presentation](../../presentation)
* spazio dei nomi [Aspose.Slides](../../presentation)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->