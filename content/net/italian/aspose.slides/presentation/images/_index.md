---
title: Images
second_title: Riferimento API Aspose.Sildes per .NET
description: Restituisce la raccolta di tutte le immagini nella presentazione. Sola lettura IImageCollectionaspose.slides/iimagecollection.
type: docs
weight: 140
url: /it/aspose.slides/presentation/images/
---
## Presentation.Images proprietà

Restituisce la raccolta di tutte le immagini nella presentazione. Sola lettura [`IImageCollection`](../../iimagecollection).

```csharp
public IImageCollection Images { get; }
```

### Esempi

Il seguente esempio mostra come aggiungere un'immagine come BLOB in una presentazione PowerPoint.

```csharp
[C#]
string pathToLargeImage = "large_image.jpg";
// crea una nuova presentazione a cui verrà aggiunta l'immagine.
using (Presentation pres = new Presentation())
{
	using (FileStream fileStream = new FileStream(pathToLargeImage, FileMode.Open))
	{
		// Aggiungiamo l'immagine alla presentazione - scegliamo il comportamento KeepLocked perché noi
		// NON abbiamo intenzione di accedere al file "largeImage.png".
		IPPImage img = pres.Images.AddImage(fileStream, LoadingStreamBehavior.KeepLocked);
		pres.Slides[0].Shapes.AddPictureFrame(ShapeType.Rectangle, 0, 0, 300, 200, img);
		// Salva la presentazione. Mentre viene generata una presentazione di grandi dimensioni, il consumo di memoria
		// rimane basso per l'intero ciclo di vita dell'oggetto pres
		pres.Save("presentationWithLargeImage.pptx", SaveFormat.Pptx);
	}
}
```

Il seguente esempio aggiunge un collegamento ipertestuale a un'immagine in una presentazione PowerPoint.

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    // Aggiunge l'immagine alla presentazione
    IPPImage image = pres.Images.AddImage(File.ReadAllBytes("image.png"));
    // Crea un frame immagine sulla diapositiva 1 basato sull'immagine aggiunta in precedenza
    IPictureFrame pictureFrame = pres.Slides[0].Shapes.AddPictureFrame(ShapeType.Rectangle, 10, 10, 100, 100, image);
    pictureFrame.HyperlinkClick = new Hyperlink("https://www.aspose.com/");
    pictureFrame.HyperlinkClick.Tooltip = "More than 70% Fortune 100 companies trust Aspose APIs";
    pres.Save("pres-out.pptx", SaveFormat.Pptx);
}
```

### Vedi anche

* interfaccia [IImageCollection](../../iimagecollection)
* classe [Presentation](../../presentation)
* spazio dei nomi [Aspose.Slides](../../presentation)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->