---
title: GetEffective
second_title: Riferimento API Aspose.Sildes per .NET
description: Ottiene le proprietà di formattazione della cella tabella effettive con ereditarietà e stili tabella applicati.
type: docs
weight: 90
url: /it/aspose.slides/cellformat/geteffective/
---
## CellFormat.GetEffective method

Ottiene le proprietà di formattazione della cella tabella effettive con ereditarietà e stili tabella applicati.

```csharp
public ICellFormatEffectiveData GetEffective()
```

### Valore di ritorno

A [`ICellFormatEffectiveData`](../../icellformateffectivedata).

### Esempi

Questo esempio dimostra come ottenere il formato di riempimento effettivo per diverse parti logiche della tabella. Si noti che la formattazione delle celle ha sempre priorità più alta rispetto alla formattazione delle righe, le righe hanno priorità più alta rispetto alle colonne, le colonne hanno priorità più alta rispetto all'intera tabella. Pertanto, alla fine le proprietà di CellFormatEffectiveData vengono sempre utilizzate per disegnare la tabella. Il codice seguente è solo un esempio di API.

```csharp
[C#]
using (Presentation pres = new Presentation(@"MyPresentation.pptx"))
{
    ITable tbl = pres.Slides[0].Shapes[0] as ITable;
    IFillFormatEffectiveData tableFillFormatEffective = tbl.TableFormat.GetEffective().FillFormat;
    IFillFormatEffectiveData rowFillFormatEffective = tbl.Rows[0].RowFormat.GetEffective().FillFormat;
    IFillFormatEffectiveData columnFillFormatEffective = tbl.Columns[0].ColumnFormat.GetEffective().FillFormat;
    IFillFormatEffectiveData cellFillFormatEffective = tbl[0, 0].CellFormat.GetEffective().FillFormat;
    /* Uscita e confronto */
}
```

### Vedi anche

* interfaccia [ICellFormatEffectiveData](../../icellformateffectivedata)
* classe [CellFormat](../../cellformat)
* spazio dei nomi [Aspose.Slides](../../cellformat)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->