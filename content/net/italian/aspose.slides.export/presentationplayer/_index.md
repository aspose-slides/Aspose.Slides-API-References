---
title: PresentationPlayer
second_title: Aspose.Sildes per .NET Riferimento API
description: Rappresenta il lettore delle animazioni associate alla Presentation../aspose.slides/presentation.
type: docs
weight: 4390
url: /it/aspose.slides.export/presentationplayer/
---
## PresentationPlayer classe

Rappresenta il lettore delle animazioni associate al [`Presentation`](../../aspose.slides/presentation).

```csharp
public class PresentationPlayer : IDisposable
```

## Costruttori

| Nome | Descrizione |
| --- | --- |
| [PresentationPlayer](presentationplayer)(PresentationAnimationsGenerator, double) | Crea una nuova istanza di [`PresentationPlayer`](../presentationplayer). |

## Proprietà

| Nome | Descrizione |
| --- | --- |
| [FrameIndex](../../aspose.slides.export/presentationplayer/frameindex) { get; } | Restituisce l'indice del fotogramma. |

## Metodi

| Nome | Descrizione |
| --- | --- |
| [Dispose](../../aspose.slides.export/presentationplayer/dispose)() | Rilascia l'istanza di [`PresentationPlayer`](../presentationplayer). |

## Altri membri

| Nome | Descrizione |
| --- | --- |
| delegate [FrameTickHandler](presentationplayer.frametickhandler) | Rappresenta il gestore di tick del fotogramma dell'evento [`FrameTick`](./frametick). |

### Esempi

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(pres))
    {
        // Riproduci l'animazione a 33 FPS
        using (var player = new PresentationPlayer(animationsGenerator, 33))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("33fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
        
        // Riproduci l'animazione a 45 FPS
        using (var player = new PresentationPlayer(animationsGenerator, 45))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("45fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
    }
}
```

### Vedi anche

* spazio dei nomi [Aspose.Slides.Export](../../aspose.slides.export)
* assembly [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->