---
title: Run
second_title: Aspose.Sildes per .NET Riferimento API
description: Esegue la generazione degli eventi di animazione per ogni diapositiva.
type: docs
weight: 80
url: /it/aspose.slides.export/presentationanimationsgenerator/run/
---
## Run(IEnumerable&lt;ISlide&gt;) {#run}

Esegue la generazione degli eventi di animazione per ogni diapositiva.

```csharp
public void Run(IEnumerable<ISlide> slides)
```

### Esempi

```csharp
[C#]
using (Presentation presentation = new Presentation("animated.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(presentation.SlideSize.Size.ToSize()))
    using (var player = new PresentationPlayer(animationsGenerator, 33))
    {
        animationsGenerator.NewAnimation += animationPlayer =>
        {
            // gestisci la nuova animazione
        };
        
        player.FrameTick += (sender, args) =>
        {
            // gestisci il tick del frame nella nuova animazione
        };
        
        animationsGenerator.Run(presentation.Slides);
    }
}
```

### Vedi anche

* interfaccia [ISlide](../../../aspose.slides/islide)
* classe [PresentationAnimationsGenerator](../../presentationanimationsgenerator)
* spazio dei nomi [Aspose.Slides.Export](../../presentationanimationsgenerator)
* assembly [Aspose.Slides](../../../)

---

## Run(IEnumerable&lt;ISlide&gt;, int, FrameTickHandler) {#run_1}

Esegue la generazione degli eventi di animazione per ogni diapositiva.

```csharp
public void Run(IEnumerable<ISlide> slides, int fps, FrameTickHandler onFrame)
```

### Esempi

```csharp
[C#]
using (Presentation presentation = new Presentation("animated.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(presentation.SlideSize.Size.ToSize()))
    {
        animationsGenerator.Run(presentation.Slides, 33, (sender, args) =>
        {
            sender.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save($"frame_{sender.FrameIndex}.png");
            };
        });
    }
}
```

### Vedi anche

* interfaccia [ISlide](../../../aspose.slides/islide)
* delegato [FrameTickHandler](../../presentationplayer.frametickhandler)
* classe [PresentationAnimationsGenerator](../../presentationanimationsgenerator)
* spazio dei nomi [Aspose.Slides.Export](../../presentationanimationsgenerator)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->