---
title: ToTiff
second_title: Aspose.Sildes per .NET Riferimento API
description: Converte la presentazione di input in un insieme di immagini in formato TIFF. Se il nome del file di output è fornito come myPath/myFilename.tiff, il risultato verrà salvato come un insieme di file myPath/myFilename_N.tiff dove N è il numero di una diapositiva.
type: docs
weight: 60
url: /it/aspose.slides.lowcode/convert/totiff/
---
## ToTiff(Presentation, string) {#totiff}

Converte la presentazione di input in un insieme di immagini in formato TIFF. Se il nome del file di output è fornito come "myPath/myFilename.tiff", il risultato verrà salvato come un insieme di file "myPath/myFilename_N.tiff", dove N è il numero della diapositiva.

```csharp
public static void ToTiff(Presentation pres, string outputFileName)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| pres | Presentation | La presentazione di input. |
| outputFileName | String | Il nome del file di output. |

### Eccezioni

| eccezione | condizione |
| --- | --- |
| ArgumentException |  |

### Esempi

```csharp
using (var pres = new Presentation("pres.pptx"))
   Convert.ToTiff(pres, "presImage.tiff");
```

### Vedi anche

* classe [Presentation](../../../aspose.slides/presentation)
* classe [Convert](../../convert)
* spazio dei nomi [Aspose.Slides.LowCode](../../convert)
* assembly [Aspose.Slides](../../../)

---

## ToTiff(Presentation, string, ITiffOptions, bool) {#totiff_1}

Converte la presentazione di input in formato TIFF con opzioni personalizzate. Se il nome del file di output è fornito come "myPath/myFilename.tiff" e *multipage* è `false`, il risultato verrà salvato come un insieme di file "myPath/myFilename_N.tiff", dove N è il numero della diapositiva. Altrimenti, se *multipage* è `true`, il risultato sarà un documento multipagina "myPath/myFilename.tiff".

```csharp
public static void ToTiff(Presentation pres, string outputFileName, ITiffOptions options, 
    bool multipage)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| pres | Presentation | La presentazione di input. |
| outputFileName | String | Il nome del file di output. |
| options | ITiffOptions | Le opzioni di salvataggio TIFF. |
| multipage | Boolean | Specifica se il documento TIFF generato deve essere multipagina. |

### Eccezioni

| eccezione | condizione |
| --- | --- |
| ArgumentException |  |

### Esempi

```csharp
ITiffOptions options = new TiffOptions()
{
    CompressionType = TiffCompressionTypes.CCITT3,
    SlidesLayoutOptions = new NotesCommentsLayoutingOptions()
    {
        NotesPosition = NotesPositions.BottomTruncated
    }
};

using (var pres = new Presentation("pres.pptx"))
   Convert.ToTiff(pres, "pres.tiff", options, false);
```

### Vedi anche

* classe [Presentation](../../../aspose.slides/presentation)
* interfaccia [ITiffOptions](../../../aspose.slides.export/itiffoptions)
* classe [Convert](../../convert)
* spazio dei nomi [Aspose.Slides.LowCode](../../convert)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->