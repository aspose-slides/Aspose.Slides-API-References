---
title: Run
second_title: Aspose.Sildes dla .NET – dokumentacja API
description: Uruchamia generowanie zdarzeń animacji dla każdego slajdu.
type: docs
weight: 80
url: /pl/aspose.slides.export/presentationanimationsgenerator/run/
---
## Run(IEnumerable&lt;ISlide&gt;) {#run}

Uruchamia generowanie zdarzeń animacji dla każdego slajdu.

```csharp
public void Run(IEnumerable<ISlide> slides)
```

### Przykłady

```csharp
[C#]
using (Presentation presentation = new Presentation("animated.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(presentation.SlideSize.Size.ToSize()))
    using (var player = new PresentationPlayer(animationsGenerator, 33))
    {
        animationsGenerator.NewAnimation += animationPlayer =>
        {
            // obsłuż nową animację
        };
        
        player.FrameTick += (sender, args) =>
        {
            // obsłuż tick klatki w nowej animacji
        };
        
        animationsGenerator.Run(presentation.Slides);
    }
}
```

### Zobacz także

* interfejs [ISlide](../../../aspose.slides/islide)
* klasa [PresentationAnimationsGenerator](../../presentationanimationsgenerator)
* przestrzeń nazw [Aspose.Slides.Export](../../presentationanimationsgenerator)
* zestaw [Aspose.Slides](../../../)

---

## Run(IEnumerable&lt;ISlide&gt;, int, FrameTickHandler) {#run_1}

Uruchamia generowanie zdarzeń animacji dla każdego slajdu.

```csharp
public void Run(IEnumerable<ISlide> slides, int fps, FrameTickHandler onFrame)
```

### Przykłady

```csharp
[C#]
using (Presentation presentation = new Presentation("animated.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(presentation.SlideSize.Size.ToSize()))
    {
        animationsGenerator.Run(presentation.Slides, 33, (sender, args) =>
        {
            sender.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save($"frame_{sender.FrameIndex}.png");
            };
        });
    }
}
```

### Zobacz także

* interfejs [ISlide](../../../aspose.slides/islide)
* delegat [FrameTickHandler](../../presentationplayer.frametickhandler)
* klasa [PresentationAnimationsGenerator](../../presentationanimationsgenerator)
* przestrzeń nazw [Aspose.Slides.Export](../../presentationanimationsgenerator)
* zestaw [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->