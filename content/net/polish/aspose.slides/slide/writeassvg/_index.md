---
title: WriteAsSvg
second_title: Aspose.Sildes dla .NET - dokumentacja API
description: Zapisuje zawartość slajdu jako plik SVG.
type: docs
weight: 140
url: /pl/aspose.slides/slide/writeassvg/
---
## WriteAsSvg(Stream) {#writeassvg}

Zapisuje zawartość slajdu jako plik SVG.

```csharp
public void WriteAsSvg(Stream stream)
```

| Parametr | Typ | Opis |
| --- | --- | --- |
| stream | Stream | Strumień docelowy |

### Przykłady

Poniższy przykład kodu demonstruje, jak przekonwertować pierwszy slajd z prezentacji PowerPoint do pliku SVG.

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (Stream fileStream = System.IO.File.Create("slide_1.svg"))
    {
        // Zapisuje pierwszy slajd jako plik SVG
        pres.Slides[0].WriteAsSvg(fileStream);
    }
}
```

### Zobacz także

* klasa [Slide](../../slide)
* przestrzeń nazw [Aspose.Slides](../../slide)
* zestaw [Aspose.Slides](../../../)

---

## WriteAsSvg(Stream, ISVGOptions) {#writeassvg_1}

Zapisuje zawartość slajdu jako plik SVG.

```csharp
public void WriteAsSvg(Stream stream, ISVGOptions svgOptions)
```

| Parametr | Typ | Opis |
| --- | --- | --- |
| stream | Stream | Strumień docelowy |
| svgOptions | ISVGOptions | Opcje generowania SVG |

### Przykłady

Poniższy przykład kodu demonstruje, jak przekonwertować pierwszy slajd z prezentacji PowerPoint do pliku SVG z opcjami.

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (Stream fileStream = System.IO.File.Create("slide_1.svg"))
    {
        var options = new SVGOptions() { VectorizeText = true };
        // Zapisuje pierwszy slajd jako plik SVG
        pres.Slides[0].WriteAsSvg(fileStream, options);
    }
}
```

### Zobacz także

* interfejs [ISVGOptions](../../../aspose.slides.export/isvgoptions)
* klasa [Slide](../../slide)
* przestrzeń nazw [Aspose.Slides](../../slide)
* zestaw [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->