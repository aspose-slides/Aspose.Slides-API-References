---
title: WriteAsEmf
second_title: Aspose.Sildes dla .NET – dokumentacja API
description: Zapisuje obraz SVG jako plik EMF.
type: docs
weight: 60
url: /pl/aspose.slides/svgimage/writeasemf/
---
## SvgImage.WriteAsEmf metoda

Zapisuje obraz SVG jako plik EMF.

```csharp
public void WriteAsEmf(Stream stream)
```

| Parametr | Typ | Opis |
| --- | --- | --- |
| stream | Stream | Docelowy strumień |

### Wyjątki

| wyjątek | warunek |
| --- | --- |
| ArgumentNullException | Docelowy strumień jest **null** |

### Przykłady

Poniższy przykład pokazuje, jak zapisać obraz SVG do metapliku.

```csharp
[C#]
// Tworzy nowy obraz SVG
ISvgImage svgImage = new SvgImage(System.IO.File.ReadAllText("content.svg"));

// Zapisuje obraz SVG jako plik metafile
using (var fileStream = System.IO.File.OpenWrite("SvgAsEmf.emf"))
    svgImage.WriteAsEmf(fileStream);
```

Ten przykład demonstruje, jak dodać obraz SVG jako metaplik do kolekcji obrazów prezentacji.

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    // Tworzy nowy obraz SVG
    ISvgImage svgImage = new SvgImage(System.IO.File.ReadAllText("content.svg"));
    using (var memStream = new MemoryStream())
    {
        // Zapisuje obraz SVG jako plik metafile
        svgImage.WriteAsEmf(memStream);
        // Dodaje plik metafile do kolekcji obrazów
        pres.Images.AddImage(memStream.ToArray());
    }
}
```

### Zobacz także

* klasa [SvgImage](../../svgimage)
* przestrzeń nazw [Aspose.Slides](../../svgimage)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->