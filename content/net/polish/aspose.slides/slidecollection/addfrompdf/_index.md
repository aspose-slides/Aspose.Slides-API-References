---
title: AddFromPdf
second_title: Aspose.Sildes dla .NET – odniesienie API
description: Tworzy slajdy z dokumentu PDF i dodaje je na koniec kolekcji.
type: docs
weight: 80
url: /pl/aspose.slides/slidecollection/addfrompdf/
---
## AddFromPdf(string) {#addfrompdf_2}

Tworzy slajdy z dokumentu PDF i dodaje je na końcu kolekcji.

```csharp
public ISlide[] AddFromPdf(string path)
```

| Parametr | Typ | Opis |
| --- | --- | --- |
| path | String | Ścieżka do dokumentu PDF |

### Wartość zwracana

Dodane slajdy

### Przykłady

Przykład:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    pres.Slides.AddFromPdf("document.pdf");
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Zobacz także

* interfejs [ISlide](../../islide)
* klasa [SlideCollection](../../slidecollection)
* przestrzeń nazw [Aspose.Slides](../../slidecollection)
* zestaw [Aspose.Slides](../../../)

---

## AddFromPdf(string, PdfImportOptions) {#addfrompdf_3}

Tworzy slajdy z dokumentu PDF i dodaje je na końcu kolekcji, uwzględniając opcje importu PDF.

```csharp
public ISlide[] AddFromPdf(string path, PdfImportOptions pdfImportOptions)
```

| Parametr | Typ | Opis |
| --- | --- | --- |
| path | String | Ścieżka do dokumentu PDF |
| pdfImportOptions | PdfImportOptions | Opcje importu PDF |

### Wartość zwracana

Dodane slajdy

### Przykłady

Przykład:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    pres.Slides.AddFromPdf("document.pdf, new PdfImportOptions { DetectTables = true });
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Zobacz także

* interfejs [ISlide](../../islide)
* klasa [PdfImportOptions](../../../aspose.slides.import/pdfimportoptions)
* klasa [SlideCollection](../../slidecollection)
* przestrzeń nazw [Aspose.Slides](../../slidecollection)
* zestaw [Aspose.Slides](../../../)

---

## AddFromPdf(Stream) {#addfrompdf}

Tworzy slajdy z dokumentu PDF i dodaje je na końcu kolekcji.

```csharp
public ISlide[] AddFromPdf(Stream pdfStream)
```

| Parametr | Typ | Opis |
| --- | --- | --- |
| pdfStream | Stream | Strumień, który będzie używany jako źródło dokumentu PDF |

### Wartość zwracana

Dodane slajdy

### Przykłady

Przykład:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    using (Stream stream = new FileStream("document.pdf", FileMode.Open, FileAccess.Read, FileShare.Read))
    {
        pres.Slides.AddFromPdf(stream);
    }
    
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Zobacz także

* interfejs [ISlide](../../islide)
* klasa [SlideCollection](../../slidecollection)
* przestrzeń nazw [Aspose.Slides](../../slidecollection)
* zestaw [Aspose.Slides](../../../)

---

## AddFromPdf(Stream, PdfImportOptions) {#addfrompdf_1}

Tworzy slajdy z dokumentu PDF i dodaje je na końcu kolekcji.

```csharp
public ISlide[] AddFromPdf(Stream pdfStream, PdfImportOptions pdfImportOptions)
```

| Parametr | Typ | Opis |
| --- | --- | --- |
| pdfStream | Stream | Strumień, który będzie używany jako źródło dokumentu PDF |
| pdfImportOptions | PdfImportOptions | Opcje importu PDF |

### Wartość zwracana

Dodane slajdy

### Przykłady

Przykład:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    using (Stream stream = new FileStream("document.pdf", FileMode.Open, FileAccess.Read, FileShare.Read))
    {
        pres.Slides.AddFromPdf(stream, new PdfImportOptions { DetectTables = true });
    }
    
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Zobacz także

* interfejs [ISlide](../../islide)
* klasa [PdfImportOptions](../../../aspose.slides.import/pdfimportoptions)
* klasa [SlideCollection](../../slidecollection)
* przestrzeń nazw [Aspose.Slides](../../slidecollection)
* zestaw [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->