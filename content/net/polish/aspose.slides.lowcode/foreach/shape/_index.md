---
title: Shape
second_title: Aspose.Slides dla .NET – referencja API
description: Iteruj każdy Shapeaspose.slides.lowcode/foreach/shape w Presentationaspose.slides/presentation. Kształty będą iterowane we wszystkich typach slajdów – Slideaspose.slides.lowcode/foreach/slide MasterSlideaspose.slides.lowcode/foreach/masterslide i LayoutSlideaspose.slides.lowcode/foreach/layoutslide
type: docs
weight: 50
url: /pl/aspose.slides.lowcode/foreach/shape/
---
## Shape(Presentation, ForEachShapeCallback) {#shape_2}

Iteruj każdy `Shape` w [`Presentation`](../../../aspose.slides/presentation). Kształty będą iterowane we wszystkich typach slajdów - [`Slide`](../slide), [`MasterSlide`](../masterslide) i [`LayoutSlide`](../layoutslide)

```csharp
public static void Shape(Presentation pres, ForEachShapeCallback forEachShape)
```

| Parametr | Typ | Opis |
| --- | --- | --- |
| pres | Presentation | Prezentacja, w której będą iterowane kształty układu |
| forEachShape | ForEachShapeCallback | Wywołanie zwrotne, które zostanie wywołane dla każdego kształtu |

### Przykłady

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Shape(pres, (shape, slide, index) => 
    {
        System.Console.WriteLine($"{shape.Name}, index: {index}");
    });
} 
```

### Zobacz także

* klasa [Presentation](../../../aspose.slides/presentation)
* delegat [ForEachShapeCallback](../../foreach.foreachshapecallback)
* klasa [ForEach](../../foreach)
* przestrzeń nazw [Aspose.Slides.LowCode](../../foreach)
* zestaw [Aspose.Slides](../../../)

---

## Shape(Presentation, bool, ForEachShapeCallback) {#shape_1}

Iteruj każdy [`Shape`](../shape) w [`Presentation`](../../../aspose.slides/presentation). Kształty będą iterowane we wszystkich typach slajdów - [`Slide`](../slide), [`MasterSlide`](../masterslide), [`LayoutSlide`](../layoutslide) i [`NotesSlide`](../../../aspose.slides/notesslide) w razie potrzeby.

```csharp
public static void Shape(Presentation pres, bool includeNotes, ForEachShapeCallback forEachShape)
```

| Parametr | Typ | Opis |
| --- | --- | --- |
| pres | Presentation | Prezentacja, w której będą iterowane kształty układu |
| includeNotes | Boolean | Flaga określająca, czy NotesSlides powinny być uwzględnione w przetwarzaniu. |
| forEachShape | ForEachShapeCallback | Wywołanie zwrotne, które zostanie wywołane dla każdego kształtu |

### Przykłady

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Shape(pres, true, (shape, slide, index) => 
    {
        System.Console.WriteLine($"{shape.Name}, index: {index}");
    });
} 
```

### Zobacz także

* klasa [Presentation](../../../aspose.slides/presentation)
* delegat [ForEachShapeCallback](../../foreach.foreachshapecallback)
* klasa [ForEach](../../foreach)
* przestrzeń nazw [Aspose.Slides.LowCode](../../foreach)
* zestaw [Aspose.Slides](../../../)

---

## Shape(BaseSlide, ForEachShapeCallback) {#shape}

Iteruj każdy [`Shape`](../shape) w [`BaseSlide`](../../../aspose.slides/baseslide). [`BaseSlide`](../../../aspose.slides/baseslide) jest typem bazowym dla [`Slide`](../slide), [`MasterSlide`](../masterslide) i [`LayoutSlide`](../layoutslide)

```csharp
public static void Shape(BaseSlide baseSlide, ForEachShapeCallback forEachShape)
```

| Parametr | Typ | Opis |
| --- | --- | --- |
| baseSlide | BaseSlide | Slajd, w którym będą iterowane kształty układu |
| forEachShape | ForEachShapeCallback | Wywołanie zwrotne, które zostanie wywołane dla każdego kształtu |

### Przykłady

```csharp
(Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Slide(pres, (slide, index) =>
    {
        ForEach.Shape(slide, (shape, baseSlide, shapeIndex) =>
        {
            System.Console.WriteLine($"{shape.Name}, index: {shapeIndex}");
        });
    });
} 
```

### Zobacz także

* klasa [BaseSlide](../../../aspose.slides/baseslide)
* delegat [ForEachShapeCallback](../../foreach.foreachshapecallback)
* klasa [ForEach](../../foreach)
* przestrzeń nazw [Aspose.Slides.LowCode](../../foreach)
* zestaw [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->