---
title: AddFromPdf
second_title: Aspose.Sildes for .NET API संदर्भ
description: PDF दस्तावेज़ से स्लाइड्स बनाता है और उन्हें संग्रह के अंत में जोड़ता है।
type: docs
weight: 50
url: /hi/aspose.slides/islidecollection/addfrompdf/
---
## AddFromPdf(string) {#addfrompdf_2}

PDF दस्तावेज़ से स्लाइड्स बनाता है और उन्हें संग्रह के अंत में जोड़ता है।

```csharp
public ISlide[] AddFromPdf(string path)
```

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| path | String | PDF दस्तावेज़ के लिए पथ |

### वापसी मान

जोड़े गए स्लाइड्स

### उदाहरण

Example:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    pres.Slides.AddFromPdf("document.pdf");
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### देखें

* इंटरफ़ेस [ISlide](../../islide)
* इंटरफ़ेस [ISlideCollection](../../islidecollection)
* नामस्थान [Aspose.Slides](../../islidecollection)
* असेंबली [Aspose.Slides](../../../)

---

## AddFromPdf(string, PdfImportOptions) {#addfrompdf_3}

PDF दस्तावेज़ से स्लाइड्स बनाता है और pdf आयात विकल्पों को ध्यान में रखते हुए उन्हें संग्रह के अंत में जोड़ता है।

```csharp
public ISlide[] AddFromPdf(string path, PdfImportOptions pdfImportOptions)
```

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| path | String | PDF दस्तावेज़ के लिए पथ |
| pdfImportOptions | PdfImportOptions | pdf आयात के विकल्प |

### वापसी मान

जोड़े गए स्लाइड्स

### उदाहरण

Example:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    pres.Slides.AddFromPdf("document.pdf, new PdfImportOptions { DetectTables = true });
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### देखें

* इंटरफ़ेस [ISlide](../../islide)
* क्लास [PdfImportOptions](../../../aspose.slides.import/pdfimportoptions)
* इंटरफ़ेस [ISlideCollection](../../islidecollection)
* नामस्थान [Aspose.Slides](../../islidecollection)
* असेंबली [Aspose.Slides](../../../)

---

## AddFromPdf(Stream, PdfImportOptions) {#addfrompdf_1}

PDF दस्तावेज़ से स्लाइड्स बनाता है और उन्हें संग्रह के अंत में जोड़ता है।

```csharp
public ISlide[] AddFromPdf(Stream pdfStream, PdfImportOptions pdfImportOptions)
```

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| pdfStream | Stream | PDF दस्तावेज़ के स्रोत के रूप में उपयोग किया जाने वाला स्ट्रीम |
| pdfImportOptions | PdfImportOptions | pdf आयात के विकल्प |

### वापसी मान

जोड़े गए स्लाइड्स

### उदाहरण

Example:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    using (Stream stream = new FileStream("document.pdf", FileMode.Open, FileAccess.Read, FileShare.Read))
    {
        pres.Slides.AddFromPdf(stream, new PdfImportOptions { DetectTables = true });
    }
    
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### देखें

* इंटरफ़ेस [ISlide](../../islide)
* क्लास [PdfImportOptions](../../../aspose.slides.import/pdfimportoptions)
* इंटरफ़ेस [ISlideCollection](../../islidecollection)
* नामस्थान [Aspose.Slides](../../islidecollection)
* असेंबली [Aspose.Slides](../../../)

---

## AddFromPdf(Stream) {#addfrompdf}

PDF दस्तावेज़ से स्लाइड्स बनाता है और उन्हें संग्रह के अंत में जोड़ता है।

```csharp
public ISlide[] AddFromPdf(Stream pdfStream)
```

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| pdfStream | Stream | PDF दस्तावेज़ के स्रोत के रूप में उपयोग किया जाने वाला स्ट्रीम |

### वापसी मान

जोड़े गए स्लाइड्स

### उदाहरण

Example:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    using (Stream stream = new FileStream("document.pdf", FileMode.Open, FileAccess.Read, FileShare.Read))
    {
        pres.Slides.AddFromPdf(stream);
    }
    
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### देखें

* इंटरफ़ेस [ISlide](../../islide)
* इंटरफ़ेस [ISlideCollection](../../islidecollection)
* नामस्थान [Aspose.Slides](../../islidecollection)
* असेंबली [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->