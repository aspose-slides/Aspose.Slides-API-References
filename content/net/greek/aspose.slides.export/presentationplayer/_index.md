---
title: PresentationPlayer
second_title: Aspose.Sildes για .NET API Αναφορά
description: Αντιπροσωπεύει τον αναπαραγωγέα των κινήσεων που σχετίζονται με την Presentation../aspose.slides/presentation.
type: docs
weight: 4370
url: /el/aspose.slides.export/presentationplayer/
---
## PresentationPlayer κλάση

Αντιπροσωπεύει τον αναπαραγωγέα των κινήσεων που σχετίζονται με το [`Presentation`](../../aspose.slides/presentation).

```csharp
public class PresentationPlayer : IDisposable
```

## Κατασκευαστές

| Όνομα | Περιγραφή |
| --- | --- |
| [PresentationPlayer](presentationplayer)(PresentationAnimationsGenerator, double) | Δημιουργεί μια νέα παρουσία του [`PresentationPlayer`](../presentationplayer). |

## Ιδιότητες

| Όνομα | Περιγραφή |
| --- | --- |
| [FrameIndex](../../aspose.slides.export/presentationplayer/frameindex) { get; } | Επιστρέφει το δείκτη πλαισίου. |

## Μέθοδοι

| Όνομα | Περιγραφή |
| --- | --- |
| [Dispose](../../aspose.slides.export/presentationplayer/dispose)() | Καταστρέφει την παρουσία του [`PresentationPlayer`](../presentationplayer). |

## Άλλα Μέλη

| Όνομα | Περιγραφή |
| --- | --- |
| delegate [FrameTickHandler](presentationplayer.frametickhandler) | Αντιπροσωπεύει τον χειριστή του ρυθμού πλαισίου του συμβάντος [`FrameTick`](./frametick). |

### Παραδείγματα

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(pres))
    {
        // Αναπαραγωγή κίνησης με 33 FPS
        using (var player = new PresentationPlayer(animationsGenerator, 33))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("33fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
        
        // Αναπαραγωγή κίνησης με 45 FPS
        using (var player = new PresentationPlayer(animationsGenerator, 45))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("45fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
    }
}
```

### Δείτε επίσης

* χώρο ονομάτων [Aspose.Slides.Export](../../aspose.slides.export)
* συγκρότημα [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->