---
title: GetCell
second_title: Aspose.Sildes για .NET API Αναφορά
description: Ανακτά ένα κελί από το καθορισμένο φύλλο εργασίας χρησιμοποιώντας το δείκτη του και τις συντεταγμένες του κελιού.
type: docs
weight: 10
url: /el/aspose.slides.excel/iexceldataworkbook/getcell/
---
## GetCell(int, int, int) {#getcell}

Επιστρέφει ένα κελί από το καθορισμένο φύλλο εργασίας χρησιμοποιώντας το δείκτη του και τις συντεταγμένες του κελιού.

```csharp
public IExcelDataCell GetCell(int worksheetIndex, int row, int column)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| worksheetIndex | Int32 | Δείκτης του φύλλου εργασίας με βάση το μηδέν. |
| row | Int32 | Δείκτης γραμμής του κελιού με βάση το μηδέν. |
| column | Int32 | Δείκτης στήλης του κελιού με βάση το μηδέν. |

### Τιμή Επιστροφής

Το κελί στη συγκεκριμένη θέση.

### Παραδείγματα

Παράδειγμα:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell(1, 1, 1);
Console.WriteLine(cell.Value.ToString());
```

### Δείτε επίσης

* διασύνδεση [IExcelDataCell](../../iexceldatacell)
* διασύνδεση [IExcelDataWorkbook](../../iexceldataworkbook)
* χώρος ονομάτων [Aspose.Slides.Excel](../../iexceldataworkbook)
* συναρμολόγηση [Aspose.Slides](../../../)

---

## GetCell(string, int, int) {#getcell_2}

Επιστρέφει ένα κελί από το καθορισμένο φύλλο εργασίας χρησιμοποιώντας το όνομά του και τις συντεταγμένες του κελιού.

```csharp
public IExcelDataCell GetCell(string worksheetName, int row, int column)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| worksheetName | String | Το όνομα του φύλλου εργασίας. |
| row | Int32 | Δείκτης γραμμής του κελιού με βάση το μηδέν. |
| column | Int32 | Δείκτης στήλης του κελιού με βάση το μηδέν. |

### Τιμή Επιστροφής

Το κελί στη συγκεκριμένη θέση.

### Παραδείγματα

Παράδειγμα:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell("Sheet1", 1, 1);
Console.WriteLine(cell.Value.ToString());
```

### Δείτε επίσης

* διασύνδεση [IExcelDataCell](../../iexceldatacell)
* διασύνδεση [IExcelDataWorkbook](../../iexceldataworkbook)
* χώρος ονομάτων [Aspose.Slides.Excel](../../iexceldataworkbook)
* συναρμολόγηση [Aspose.Slides](../../../)

---

## GetCell(int, string) {#getcell_1}

Επιστρέφει ένα κελί από το καθορισμένο φύλλο εργασίας χρησιμοποιώντας το δείκτη του και το όνομα κελιού σε μορφή Excel (π.χ., "B2").

```csharp
public IExcelDataCell GetCell(int worksheetIndex, string cellName)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| worksheetIndex | Int32 | Δείκτης του φύλλου εργασίας με βάση το μηδέν. |
| cellName | String | Η αναφορά κελιού σε μορφή Excel (π.χ., "A1", "C5"). |

### Τιμή Επιστροφής

Το κελί στη συγκεκριμένη θέση.

### Παραδείγματα

Παράδειγμα:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell(1, "B2");
Console.WriteLine(cell.Value.ToString());
```

### Δείτε επίσης

* διασύνδεση [IExcelDataCell](../../iexceldatacell)
* διασύνδεση [IExcelDataWorkbook](../../iexceldataworkbook)
* χώρος ονομάτων [Aspose.Slides.Excel](../../iexceldataworkbook)
* συναρμολόγηση [Aspose.Slides](../../../)

---

## GetCell(string, string) {#getcell_3}

Επιστρέφει ένα κελί από το καθορισμένο φύλλο εργασίας χρησιμοποιώντας το όνομα κελιού σε μορφή Excel (π.χ., "B2").

```csharp
public IExcelDataCell GetCell(string worksheetName, string cellName)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| worksheetName | String | Το όνομα του φύλλου εργασίας. |
| cellName | String | Η αναφορά κελιού σε μορφή Excel (π.χ., "A1", "C5"). |

### Τιμή Επιστροφής

Το κελί στη συγκεκριμένη θέση.

### Παραδείγματα

Παράδειγμα:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell("Sheet1", "B2");
Console.WriteLine(cell.Value.ToString());
```

### Δείτε επίσης

* διασύνδεση [IExcelDataCell](../../iexceldatacell)
* διασύνδεση [IExcelDataWorkbook](../../iexceldataworkbook)
* χώρος ονομάτων [Aspose.Slides.Excel](../../iexceldataworkbook)
* συναρμολόγηση [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->