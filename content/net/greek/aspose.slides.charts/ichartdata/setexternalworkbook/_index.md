---
title: SetExternalWorkbook
second_title: Aspose.Sildes για .NET API Αναφορά
description: Ορίζει εξωτερικό βιβλίο εργασίας ως πηγή δεδομένων για το γράφημα. Τα δεδομένα του γραφήματος θα ενημερωθούν από το βιβλίο εργασίας-στόχο.
type: docs
weight: 120
url: /el/aspose.slides.charts/ichartdata/setexternalworkbook/
---
## SetExternalWorkbook(string) {#setexternalworkbook}

Ορίζει εξωτερικό βιβλίο εργασίας ως πηγή δεδομένων για το γράφημα. Τα δεδομένα του γραφήματος θα ενημερωθούν από το βιβλίο εργασίας-στόχο.

```csharp
public void SetExternalWorkbook(string workbookPath)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| workbookPath | String | Διαδρομή προς το βιβλίο εργασίας-στόχο |

### Εξαιρέσεις

| εξαίρεση | συνθήκη |
| --- | --- |
| InvalidOperationException | Το εξωτερικό βιβλίο εργασίας δεν είναι διαθέσιμο ή δεν μπορεί να φορτωθεί. |

### Παραδείγματα

```csharp
[C#]
using (Presentation pres = new Presentation())

   IChart chart = pres.Slides[0].Shapes.AddChart(ChartType.Pie, 50, 50, 400, 600, true);
   IChartData chartData = chart.ChartData;
   (chartData as ChartData).SetExternalWorkbook("../../workbook.xlsx");

```

### Δείτε επίσης

* διασύνδεση [IChartData](../../ichartdata)
* χώρο ονομάτων [Aspose.Slides.Charts](../../ichartdata)
* συναρμολόγηση [Aspose.Slides](../../../)

---

## SetExternalWorkbook(string, bool) {#setexternalworkbook_1}

Ορίζει εξωτερικό βιβλίο εργασίας ως πηγή δεδομένων για το γράφημα.

```csharp
public void SetExternalWorkbook(string workbookPath, bool updateChartData)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| workbookPath | String | Διαδρομή προς το βιβλίο εργασίας-στόχο |
| updateChartData | Boolean | Εάν η τιμή είναι false, θα ενημερωθεί μόνο η διαδρομή του βιβλίου εργασίας. Τα δεδομένα του γραφήματος δεν θα φορτωθούν και δεν θα ενημερωθούν από το βιβλίο εργασίας-στόχο. Μπορεί να χρησιμοποιηθεί όταν το βιβλίο εργασίας-στόχο δεν υπάρχει ή δεν είναι διαθέσιμο. Εάν η τιμή είναι true, τα δεδομένα του γραφήματος θα ενημερωθούν από το βιβλίο εργασίας-στόχο. |

### Εξαιρέσεις

| εξαίρεση | συνθήκη |
| --- | --- |
| InvalidOperationException | Το εξωτερικό βιβλίο εργασίας δεν είναι διαθέσιμο ή δεν μπορεί να φορτωθεί. |

### Παραδείγματα

```csharp
using (Presentation pres = new Presentation())

   IChart chart = pres.Slides[0].Shapes.AddChart(ChartType.Pie, 50, 50, 400, 600, true);
   IChartData chartData = chart.ChartData;

   (chartData as ChartData).SetExternalWorkbook("http://path/doesnt/exists", false);


```

### Δείτε επίσης

* διασύνδεση [IChartData](../../ichartdata)
* χώρο ονομάτων [Aspose.Slides.Charts](../../ichartdata)
* συναρμολόγηση [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->