---
title: DigitalSignature
second_title: Aspose.Sildes για .NET Αναφορά API
description: Ψηφιακή υπογραφή σε υπογεγραμμένο αρχείο.
type: docs
weight: 2750
url: /el/aspose.slides/digitalsignature/
---
## Κλάση DigitalSignature

Ψηφιακή υπογραφή σε υπογεγραμμένο αρχείο.

```csharp
public class DigitalSignature : IDigitalSignature
```

## Κατασκευαστές

| Όνομα | Περιγραφή |
| --- | --- |
| [DigitalSignature](digitalsignature#constructor)(X509Certificate2) | Δημιουργεί ένα νέο αντικείμενο DigitalSignature με το καθορισμένο πιστοποιητικό. |
| [DigitalSignature](digitalsignature#constructor_1)(string, string) | Δημιουργεί ένα νέο αντικείμενο DigitalSignature με το καθορισμένο μονοπάτι αρχείου πιστοποιητικού και κωδικό πρόσβασης. |

## Ιδιότητες

| Όνομα | Περιγραφή |
| --- | --- |
| [Certificate](../../aspose.slides/digitalsignature/certificate) { get; } | Αντικείμενο πιστοποιητικού που χρησιμοποιήθηκε για την υπογραφή του εγγράφου. Μόνο για ανάγνωση X509Certificate2. |
| [Comments](../../aspose.slides/digitalsignature/comments) { get; set; } | Ο σκοπός της υπογραφής. Ανάγνωση/εγγραφή String. |
| [IsValid](../../aspose.slides/digitalsignature/isvalid) { get; } | Εάν αυτή η ψηφιακή υπογραφή είναι έγκυρη και το έγγραφο δεν έχει παραποιηθεί, αυτή η τιμή θα είναι αληθής. Μόνο για ανάγνωση Boolean. |
| [SignTime](../../aspose.slides/digitalsignature/signtime) { get; } | Η ώρα που υπογράφηκε το έγγραφο. Μόνο για ανάγνωση DateTime. |

### Παραδείγματα

Το παρακάτω παράδειγμα δείχνει πώς να προσθέσετε ψηφιακή υπογραφή από πιστοποιητικό PFX σε παρουσίαση PowerPoint.

```csharp
[C#]
// Αρχικοποίηση του αντικειμένου Presentation
using (Presentation pres = new Presentation())
{
    // Δημιουργία αντικειμένου DigitalSignature με αρχείο PFX και κωδικό πρόσβασης PFX
    DigitalSignature signature = new DigitalSignature("testsignature1.pfx", @"testpass1");
    // Σχόλιο νέας ψηφιακής υπογραφής
    signature.Comments = "Aspose.Slides digital signing test.";
    // Προσθήκη ψηφιακής υπογραφής στην παρουσίαση
    pres.DigitalSignatures.Add(signature);
    // Αποθήκευση της παρουσίασης
    pres.Save("SomePresentationSigned.pptx", SaveFormat.Pptx);
}
```

Το παρακάτω δείγμα κώδικα δείχνει πώς να επαληθεύσετε την ψηφιακή υπογραφή μιας παρουσίασης PowerPoint.

```csharp
[C#]
// Αρχικοποίηση του αντικειμένου Presentation
using (Presentation pres = new Presentation("SomePresentationSigned.pptx"))
{
    if (pres.DigitalSignatures.Count < 0)
    {
        bool allSignaturesAreValid = true;
        Console.WriteLine("Signatures used to sign the presentation: ");
        // Έλεγχος εάν όλες οι ψηφιακές υπογραφές είναι έγκυρες
        foreach (DigitalSignature signature in pres.DigitalSignatures)
        {
            Console.WriteLine(signature.Certificate.SubjectName.Name + ", "
                    + signature.SignTime.ToString("yyyy-MM-dd HH:mm") + " -- " + (signature.IsValid ? "VALID" : "INVALID"));
            allSignaturesAreValid &= signature.IsValid;
        }
        if (allSignaturesAreValid)
            Console.WriteLine("Presentation is genuine, all signatures are valid.");
        else
            Console.WriteLine("Presentation has been modified since signing.");
    }
}
```

### Δείτε επίσης

* διασύνδεση [IDigitalSignature](../idigitalsignature)
* χώρος ονομάτων [Aspose.Slides](../../aspose.slides)
* συγκρότηση [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->