---
title: AddOleObjectFrame
second_title: Aspose.Sildes για .NET API Αναφορά
description: Δημιουργεί ένα νέο πλαίσιο αντικειμένου OLE και το προσθέτει στο τέλος της συλλογής σχημάτων.
type: docs
weight: 150
url: /el/aspose.slides/shapecollection/addoleobjectframe/
---
## AddOleObjectFrame(float, float, float, float, IOleEmbeddedDataInfo) {#addoleobjectframe}

Δημιουργεί ένα νέο πλαίσιο αντικειμένου OLE και το προσθέτει στο τέλος της συλλογής σχημάτων.

```csharp
public IOleObjectFrame AddOleObjectFrame(float x, float y, float width, float height, 
    IOleEmbeddedDataInfo dataInfo)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| x | Single | Η συντεταγμένη x του νέου πλαισίου OLE, σε σημεία. |
| y | Single | Η συντεταγμένη y του νέου πλαισίου OLE, σε σημεία. |
| width | Single | Το πλάτος του νέου πλαισίου OLE, σε σημεία. |
| height | Single | Το ύψος του νέου πλαισίου OLE, σε σημεία. |
| dataInfo | IOleEmbeddedDataInfo | Οι πληροφορίες σχετικά με τα ενσωματωμένα δεδομένα OLE ([`IOleEmbeddedDataInfo`](../../ioleembeddeddatainfo)). |

### Τιμή Επιστροφής

Το νεοδημιουργημένο [`IOleObjectFrame`](../../ioleobjectframe).

### Παραδείγματα

Τα παρακάτω παραδείγματα δείχνουν πώς να προσθέτετε πλαίσια αντικειμένου OLE σε διαφάνειες παρουσίασης PowerPoint.

```csharp
[C#]
// Δημιουργεί ένα αντικείμενο της κλάσης Presentation που αντιπροσωπεύει το αρχείο PPTX
using (Presentation pres = new Presentation())
{
    // Προσπελαύει την πρώτη διαφάνεια
    ISlide sld = pres.Slides[0];
    // Φορτώνει ένα αρχείο excel σε ροή
    MemoryStream mstream = new MemoryStream();
    using (FileStream fs = new FileStream("book1.xlsx", FileMode.Open, FileAccess.Read))
    {
        byte[] buf = new byte[4096];
        while (true)
        {
            int bytesRead = fs.Read(buf, 0, buf.Length);
            if (bytesRead <= 0)
                break;
            mstream.Write(buf, 0, bytesRead);
        }
    }
    // Δημιουργεί ένα αντικείμενο δεδομένων για ενσωμάτωση
    IOleEmbeddedDataInfo dataInfo = new OleEmbeddedDataInfo(mstream.ToArray(), "xlsx");
    // Προσθέτει ένα σχήμα Ole Object Frame
    IOleObjectFrame oleObjectFrame = sld.Shapes.AddOleObjectFrame(0, 0, pres.SlideSize.Size.Width,
        pres.SlideSize.Size.Height, dataInfo);
    //Γράφει το αρχείο PPTX στο δίσκο
    pres.Save("OleEmbed_out.pptx", SaveFormat.Pptx);
}
```

### Δείτε επίσης

* διασύνδεση [IOleObjectFrame](../../ioleobjectframe)
* διασύνδεση [IOleEmbeddedDataInfo](../../ioleembeddeddatainfo)
* κλάση [ShapeCollection](../../shapecollection)
* χώρο ονομάτων [Aspose.Slides](../../shapecollection)
* συγκρότηση [Aspose.Slides](../../../)

---

## AddOleObjectFrame(float, float, float, float, string, string) {#addoleobjectframe_1}

Δημιουργεί ένα νέο πλαίσιο αντικειμένου OLE και το προσθέτει στο τέλος της συλλογής σχημάτων.

```csharp
public IOleObjectFrame AddOleObjectFrame(float x, float y, float width, float height, 
    string className, string path)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| x | Single | Η συντεταγμένη x του νέου πλαισίου OLE, σε σημεία. |
| y | Single | Η συντεταγμένη y του νέου πλαισίου OLE, σε σημεία. |
| width | Single | Το πλάτος του νέου πλαισίου OLE, σε σημεία. |
| height | Single | Το ύψος του νέου πλαισίου OLE, σε σημεία. |
| className | String | Το όνομα κλάσης του αντικειμένου OLE. |
| path | String | Η διαδρομή προς το συνδεδεμένο αρχείο. |

### Τιμή Επιστροφής

Το νεοδημιουργημένο [`IOleObjectFrame`](../../ioleobjectframe).

### Δείτε επίσης

* διασύνδεση [IOleObjectFrame](../../ioleobjectframe)
* κλάση [ShapeCollection](../../shapecollection)
* χώρο ονομάτων [Aspose.Slides](../../shapecollection)
* συγκρότηση [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->