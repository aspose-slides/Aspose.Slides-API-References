---
title: Placeholder
second_title: Aspose.Sildes για .NET Αναφορά API
description: Επιστρέφει το placeholder για ένα σχήμα. Επιστρέφει null εάν το σχήμα δεν έχει placeholder. Μόνο-ανάγνωση IPlaceholderaspose.slides/iplaceholder.
type: docs
weight: 210
url: /el/aspose.slides/shape/placeholder/
---
## Shape.Placeholder ιδιότητα

Επιστρέφει το placeholder για ένα σχήμα. Επιστρέφει null εάν το σχήμα δεν έχει placeholder. Μόνο-ανάγνωση [`IPlaceholder`](../../iplaceholder).

```csharp
public IPlaceholder Placeholder { get; }
```

### Παραδείγματα

Το παρακάτω παράδειγμα δείχνει πώς να αλλάξετε το κείμενο στο Placeholder.

```csharp
[C#]
// Δημιουργεί μια κλάση Presentation
using (Presentation pres = new Presentation("ReplacingText.pptx"))
{
    // Πρόσβαση στην πρώτη διαφάνεια
    ISlide sld = pres.Slides[0];
    // Διατρέχει τα σχήματα για να βρει το placeholder
    foreach (IShape shp in sld.Shapes)
        if (shp.Placeholder != null)
        {
            // Αλλάζει το κείμενο σε κάθε placeholder
            ((IAutoShape)shp).TextFrame.Text = "This is a Placeholder";
        }
    // Αποθηκεύει την παρουσίαση στο δίσκο
    pres.Save("output_out.pptx", Aspose.Slides.Export.SaveFormat.Pptx);
}
```

Το παρακάτω παράδειγμα δείχνει πώς να ορίσετε το κείμενο Prompt στο Placeholder.

```csharp
[C#]
using (Presentation pres = new Presentation("Presentation2.pptx"))
{
    ISlide slide = pres.Slides[0];
    foreach (IShape shape in slide.Slide.Shapes) // Διατρέχει τη διαφάνεια
    {
        if (shape.Placeholder != null && shape is AutoShape)
        {
            string text = "";
            if (shape.Placeholder.Type == PlaceholderType.CenteredTitle) // Το PowerPoint εμφανίζει "Κάντε κλικ για να προσθέσετε τίτλο"
            {
                text = "Add Title";
            }
            else if (shape.Placeholder.Type == PlaceholderType.Subtitle) // Προσθέτει υπότιτλο
            {
                text = "Add Subtitle";
            }
            ((IAutoShape)shape).TextFrame.Text = text;
            Console.WriteLine($"Placeholder with text: {text}");
        }
    }
    pres.Save("Placeholders_PromptText.pptx", SaveFormat.Pptx);
}
```

### Δείτε επίσης

* διεπαφή [IPlaceholder](../../iplaceholder)
* κλάση [Shape](../../shape)
* χώρο ονομάτων [Aspose.Slides](../../shape)
* συναρμολόγηση [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->