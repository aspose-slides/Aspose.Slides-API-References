---
title: Masters
second_title: Αναφορά API Aspose.Sildes για .NET
description: Επιστρέφει μια λίστα με όλες τις κύριες διαφάνειες που ορίζονται στην παρουσίαση. Μόνο για ανάγνωση IMasterSlideCollectionaspose.slides/imasterslidecollection.
type: docs
weight: 180
url: /el/aspose.slides/presentation/masters/
---
## Presentation.Masters ιδιότητα

Επιστρέφει μια λίστα με όλες τις κύριες διαφάνειες που ορίζονται στην παρουσίαση. Μόνο για ανάγνωση [`IMasterSlideCollection`](../../imasterslidecollection).

```csharp
public IMasterSlideCollection Masters { get; }
```

### Παραδείγματα

Τα παρακάτω παραδείγματα δείχνουν πώς να προσθέτετε Εικόνες σε κύριες διαφάνειες παρουσίασης PowerPoint.

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    ISlide slide = pres.Slides[0];
    IMasterSlide masterSlide = slide.LayoutSlide.MasterSlide;
    IPPImage image = pres.Images.AddImage(File.ReadAllBytes("image.png"));
    masterSlide.Shapes.AddPictureFrame(ShapeType.Rectangle, 10, 10, 100, 100, image);
    pres.Save("pres.pptx", SaveFormat.Pptx);
}
```

Τα παρακάτω παραδείγματα δείχνουν πώς να αλλάζετε το χρώμα φόντου της κύριας διαφάνειας της παρουσίασης PowerPoint.

```csharp
[C#]
// Δημιουργεί την κλάση Presentation που αντιπροσωπεύει το αρχείο παρουσίασης
using (Presentation pres = new Presentation())
{
    // Ορίζει το χρώμα φόντου της κύριας ISlide σε Πράσινο δάσους
    pres.Masters[0].Background.Type = BackgroundType.OwnBackground;
    pres.Masters[0].Background.FillFormat.FillType = FillType.Solid;
    pres.Masters[0].Background.FillFormat.SolidFillColor.Color = Color.ForestGreen;
    // Γράφει την παρουσίαση στο δίσκο
    pres.Save("SetSlideBackgroundMaster_out.pptx", SaveFormat.Pptx);
}
```

Τα παρακάτω παραδείγματα δείχνουν πώς να προσθέτετε διάταξη διαφάνειας στην παρουσίαση PowerPoint.

```csharp
[C#]
// Δημιουργεί αντικείμενο κλάσης Presentation που αντιπροσωπεύει το αρχείο παρουσίασης
using (Presentation presentation = new Presentation("AccessSlides.pptx"))
{
    // Προσπάθεια αναζήτησης κατά τύπο διάταξης διαφάνειας
    IMasterLayoutSlideCollection layoutSlides = presentation.Masters[0].LayoutSlides;
    ILayoutSlide layoutSlide = layoutSlides.GetByType(SlideLayoutType.TitleAndObject) ?? layoutSlides.GetByType(SlideLayoutType.Title);
    if (layoutSlide == null)
    {
        // Η περίπτωση όταν μια παρουσίαση δεν περιέχει κάποιον τύπο διατάξεων.
        // Το αρχείο παρουσίασης περιέχει μόνο τύπους διατάξεων Blank και Custom.
        // Όμως οι διαφάνειες διάταξης με τύπους Custom έχουν διαφορετικά ονόματα διαφάνειας,
        // όπως "Title", "Title and Content" κλπ. Και είναι δυνατόν να χρησιμοποιηθούν αυτά
        // ονόματα για την επιλογή διάταξης διαφάνειας.
        // Επίσης είναι δυνατόν να χρησιμοποιηθεί το σύνολο των τύπων placeholder σχημάτων. Για παράδειγμα,
        // Η διαφάνεια τίτλου πρέπει να έχει μόνο τύπο placeholder Title, κλπ.
        foreach (ILayoutSlide titleAndObjectLayoutSlide in layoutSlides)
        {
            if (titleAndObjectLayoutSlide.Name == "Title and Object")
            {
                layoutSlide = titleAndObjectLayoutSlide;
                break;
            }
        }
        if (layoutSlide == null)
        {
            foreach (ILayoutSlide titleLayoutSlide in layoutSlides)
            {
                if (titleLayoutSlide.Name == "Title")
                {
                    layoutSlide = titleLayoutSlide;
                    break;
                }
            }
            if (layoutSlide == null)
            {
                layoutSlide = layoutSlides.GetByType(SlideLayoutType.Blank);
                if (layoutSlide == null)
                {
                    layoutSlide = layoutSlides.Add(SlideLayoutType.TitleAndObject, "Title and Object");
                }
            }
        }
    }
    // Προσθήκη κενής διαφάνειας με την προστιθέμενη διάταξη διαφάνειας
    presentation.Slides.InsertEmptySlide(0, layoutSlide);
    // Αποθήκευση παρουσίασης
    presentation.Save("AddLayoutSlides_out.pptx", SaveFormat.Pptx);
}
```

### Δείτε επίσης

* διασύνδεση [IMasterSlideCollection](../../imasterslidecollection)
* κλάση [Presentation](../../presentation)
* χώρος ονομάτων [Aspose.Slides](../../presentation)
* συναρμολόγηση [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->