---
title: Slides
second_title: Αναφορά API Aspose.Sildes για .NET
description: Επιστρέφει μια λίστα με όλες τις διαφάνειες που έχουν οριστεί στην παρουσίαση. Μόνο για ανάγνωση ISlideCollectionaspose.slides/islidecollection.
type: docs
weight: 240
url: /el/aspose.slides/presentation/slides/
---
## Presentation.Slides ιδιότητα

Επιστρέφει μια λίστα με όλες τις διαφάνειες που έχουν οριστεί στην παρουσίαση. Μόνο για ανάγνωση [`ISlideCollection`](../el/../islidecollection).

```csharp
public ISlideCollection Slides { get; }
```

### Παραδείγματα

Το παρακάτω παράδειγμα δείχνει πώς να ορίσετε το χρώμα φόντου των διαφανειών της παρουσίασης PowerPoint.

```csharp
[C#]
// Δημιουργήστε ένα αντίτυπο της κλάσης Presentation που αντιπροσωπεύει το αρχείο παρουσίασης
using (Presentation pres = new Presentation())
{
    // Ορίστε το χρώμα φόντου της πρώτης ISlide σε Μπλε
    pres.Slides[0].Background.Type = BackgroundType.OwnBackground;
    pres.Slides[0].Background.FillFormat.FillType = FillType.Solid;
    pres.Slides[0].Background.FillFormat.SolidFillColor.Color = Color.Blue;
    pres.Save("ContentBG_out.pptx", SaveFormat.Pptx);
}
```

Το παρακάτω παράδειγμα δείχνει πώς να ορίσετε την εικόνα φόντου των διαφανειών της παρουσίασης PowerPoint.

```csharp
[C#]
// Δημιουργήστε την κλάση Presentation που αντιπροσωπεύει το αρχείο παρουσίασης
using (Presentation pres = new Presentation("SetImageAsBackground.pptx"))
{
    // Ορίστε το φόντο με εικόνα
    pres.Slides[0].Background.Type = BackgroundType.OwnBackground;
    pres.Slides[0].Background.FillFormat.FillType = FillType.Picture;
    pres.Slides[0].Background.FillFormat.PictureFillFormat.PictureFillMode = PictureFillMode.Stretch;
    // Ορίστε την εικόνα
    System.Drawing.Image img = (System.Drawing.Image)new Bitmap(dataDir + "Tulips.jpg");
    // Προσθέστε την εικόνα στη συλλογή εικόνων της παρουσίασης
    IPPImage imgx = pres.Images.AddImage(img);
    pres.Slides[0].Background.FillFormat.PictureFillFormat.Picture.Image = imgx;
    // Αποθηκεύστε την παρουσίαση στο δίσκο
    pres.Save("ContentBG_Img_out.pptx", SaveFormat.Pptx);
}
```

Το παρακάτω παράδειγμα δείχνει πώς να προσθέσετε μετάβαση διαφάνειας στην παρουσίαση.

```csharp
[C#]
// Δημιουργήστε την κλάση Presentation για να φορτώσετε το αρχικό αρχείο παρουσίασης
using (Presentation presentation = new Presentation("AccessSlides.pptx"))
{
    // Εφαρμόστε μετάβαση τύπου κύκλου στη διαφάνεια 1
    presentation.Slides[0].SlideShowTransition.Type = TransitionType.Circle;
    // Εφαρμόστε μετάβαση τύπου comb στη διαφάνεια 2
    presentation.Slides[1].SlideShowTransition.Type = TransitionType.Comb;
    // Αποθηκεύστε την παρουσίαση στο δίσκο
    presentation.Save("SampleTransition_out.pptx", SaveFormat.Pptx);
}
```

Το παρακάτω παράδειγμα δείχνει πώς να προσθέσετε προχωρημένη μετάβαση διαφάνειας.

```csharp
[C#]
// Δημιουργήστε την κλάση Presentation που αντιπροσωπεύει ένα αρχείο παρουσίασης
using (Presentation pres = new Presentation("BetterSlideTransitions.pptx"))
{
    // Εφαρμόστε μετάβαση τύπου κύκλου στη διαφάνεια 1
    pres.Slides[0].SlideShowTransition.Type = TransitionType.Circle;
    // Ορίστε το χρόνο μετάβασης στα 3 δευτερόλεπτα
    pres.Slides[0].SlideShowTransition.AdvanceOnClick = true;
    pres.Slides[0].SlideShowTransition.AdvanceAfterTime = 3000;
    // Εφαρμόστε μετάβαση τύπου comb στη διαφάνεια 2
    pres.Slides[1].SlideShowTransition.Type = TransitionType.Comb;
    // Ορίστε το χρόνο μετάβασης στα 5 δευτερόλεπτα
    pres.Slides[1].SlideShowTransition.AdvanceOnClick = true;
    pres.Slides[1].SlideShowTransition.AdvanceAfterTime = 5000;
    // Εφαρμόστε μετάβαση τύπου ζουμ στη διαφάνεια 3
    pres.Slides[2].SlideShowTransition.Type = TransitionType.Zoom;
    // Ορίστε το χρόνο μετάβασης στα 7 δευτερόλεπτα
    pres.Slides[2].SlideShowTransition.AdvanceOnClick = true;
    pres.Slides[2].SlideShowTransition.AdvanceAfterTime = 7000;
    // Αποθηκεύστε την παρουσίαση στο δίσκο
    pres.Save("SampleTransition_out.pptx", SaveFormat.Pptx);
}
```

### Δείτε επίσης

* διασύνδεση [ISlideCollection](../../islidecollection)
* κλάση [Presentation](../../presentation)
* χώρος ονομάτων [Aspose.Slides](../../presentation)
* συναρμολόγηση [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->