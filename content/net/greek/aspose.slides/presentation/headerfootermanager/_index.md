---
title: HeaderFooterManager
second_title: Aspose.Sildes για .NET Αναφορά API
description: Επιστρέφει τον τρέχοντα διαχειριστή HeaderFooter. Μόνο για ανάγνωση IPresentationHeaderFooterManageraspose.slides/ipresentationheaderfootermanager.
type: docs
weight: 120
url: /el/aspose.slides/presentation/headerfootermanager/
---
## Presentation.HeaderFooterManager ιδιότητα

Επιστρέφει τον τρέχοντα διαχειριστή HeaderFooter. Μόνο για ανάγνωση [`IPresentationHeaderFooterManager`](../../ipresentationheaderfootermanager).

```csharp
public IPresentationHeaderFooterManager HeaderFooterManager { get; }
```

### Παραδείγματα

Το παρακάτω παράδειγμα δείχνει πώς να ορίσετε την ορατότητα του υποσέλιδου μέσα σε Slide του PowerPoint Presentation.

```csharp
[C#]
using (Presentation presentation = new Presentation("presentation.ppt"))
{
    IBaseSlideHeaderFooterManager headerFooterManager = presentation.Slides[0].HeaderFooterManager;
	// Η ιδιότητα IsFooterVisible χρησιμοποιείται για να υποδείξει ότι δεν υπάρχει placeholder υποσέλιδου διαφάνειας.
    if (!headerFooterManager.IsFooterVisible)
    {
		// Η μέθοδος SetFooterVisibility χρησιμοποιείται για να κάνει ορατό το placeholder υποσέλιδου διαφάνειας.
        headerFooterManager.SetFooterVisibility(true);
    }
	// Η ιδιότητα IsSlideNumberVisible χρησιμοποιείται για να υποδείξει ότι δεν υπάρχει placeholder αριθμού σελίδας διαφάνειας.
    if (!headerFooterManager.IsSlideNumberVisible)
    {
		// Η μέθοδος SetSlideNumberVisibility χρησιμοποιείται για να κάνει ορατό το placeholder αριθμού σελίδας διαφάνειας.
        headerFooterManager.SetSlideNumberVisibility(true);
    }
	// Η ιδιότητα IsDateTimeVisible χρησιμοποιείται για να υποδείξει ότι δεν υπάρχει placeholder ημερομηνίας-ώρας διαφάνειας.
    if (!headerFooterManager.IsDateTimeVisible)
    {
		// Η μέθοδος SetFooterVisibility χρησιμοποιείται για να κάνει ορατό το placeholder ημερομηνίας-ώρας διαφάνειας.
        headerFooterManager.SetDateTimeVisibility(true);
    }
	// Η μέθοδος SetFooterText χρησιμοποιείται για να θέσει κείμενο στο placeholder υποσέλιδου διαφάνειας.
    headerFooterManager.SetFooterText("Footer text");
	// Η μέθοδος SetDateTimeText χρησιμοποιείται για να θέσει κείμενο στο placeholder ημερομηνίας-ώρας διαφάνειας.
    headerFooterManager.SetDateTimeText("Date and time text");
	presentation.Save("Presentation.ppt",SaveFormat.ppt);
}
```

Το παρακάτω παράδειγμα δείχνει πώς να ορίσετε την ορατότητα του υποσέλιδου παιδιού μέσα σε Slide.

```csharp
[C#]
using (Presentation presentation = new Presentation("presentation.ppt"))
{
    IMasterSlideHeaderFooterManager headerFooterManager = presentation.Masters[0].HeaderFooterManager;
	// Η μέθοδος SetFooterAndChildFootersVisibility χρησιμοποιείται για να κάνει ορατά τα placeholder υποσέλιδου της κύριας διαφάνειας και όλων των παιδικών.
    headerFooterManager.SetFooterAndChildFootersVisibility(true);
	// Η μέθοδος SetSlideNumberAndChildSlideNumbersVisibility χρησιμοποιείται για να κάνει ορατά τα placeholder αριθμού σελίδας της κύριας διαφάνειας και όλων των παιδικών.
    headerFooterManager.SetSlideNumberAndChildSlideNumbersVisibility(true);
	// Η μέθοδος SetDateTimeAndChildDateTimesVisibility χρησιμοποιείται για να κάνει ορατά τα placeholder ημερομηνίας-ώρας της κύριας διαφάνειας και όλων των παιδικών.
    headerFooterManager.SetDateTimeAndChildDateTimesVisibility(true);
	// Η μέθοδος SetFooterAndChildFootersText χρησιμοποιείται για τον ορισμό κειμένου στα placeholder υποσέλιδου της κύριας διαφάνειας και όλων των παιδικών.
    headerFooterManager.SetFooterAndChildFootersText("Footer text");
	// Η μέθοδος SetDateTimeAndChildDateTimesText χρησιμοποιείται για τον ορισμό κειμένου στα placeholder ημερομηνίας-ώρας της κύριας διαφάνειας και όλων των παιδικών.
    headerFooterManager.SetDateTimeAndChildDateTimesText("Date and time text");
}
```

### Δείτε επίσης

* διασύνδεση [IPresentationHeaderFooterManager](../../ipresentationheaderfootermanager)
* κλάση [Presentation](../../presentation)
* χώρο ονομάτων [Aspose.Slides](../../presentation)
* συγκρότημα [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->