---
title: WriteAsEmf
second_title: Aspose.Sildes για .NET Αναφορά API
description: Αποθηκεύει την εικόνα SVG ως αρχείο EMF.
type: docs
weight: 60
url: /el/aspose.slides/svgimage/writeasemf/
---
## SvgImage.WriteAsEmf μέθοδος

Αποθηκεύει την εικόνα SVG ως αρχείο EMF.

```csharp
public void WriteAsEmf(Stream stream)
```

| Parameter | Type | Description |
| --- | --- | --- |
| stream | Stream | Ροή-στόχος |

### Εξαιρέσεις

| εξαίρεση | συνθήκη |
| --- | --- |
| ArgumentNullException | Η ροή-στόχος είναι **null** |

### Παραδείγματα

Το παρακάτω παράδειγμα δείχνει πώς να αποθηκεύσετε την εικόνα SVG στο metafile.

```csharp
[C#]
// Δημιουργεί τη νέα εικόνα SVG
ISvgImage svgImage = new SvgImage(System.IO.File.ReadAllText("content.svg"));

// Αποθηκεύει την εικόνα SVG ως metafile
using (var fileStream = System.IO.File.OpenWrite("SvgAsEmf.emf"))
    svgImage.WriteAsEmf(fileStream);
```

Αυτό το παράδειγμα δείχνει πώς να προσθέσετε την εικόνα SVG ως metafile στη συλλογή εικόνων παρουσίασης.

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    // Δημιουργεί τη νέα εικόνα SVG
    ISvgImage svgImage = new SvgImage(System.IO.File.ReadAllText("content.svg"));
    using (var memStream = new MemoryStream())
    {
        // Αποθηκεύει την εικόνα SVG ως metafile
        svgImage.WriteAsEmf(memStream);
        // Προσθέτει το metafile στη συλλογή εικόνων
        pres.Images.AddImage(memStream.ToArray());
    }
}
```

### Δείτε επίσης

* κλάση [SvgImage](../../svgimage)
* χώρο ονομάτων [Aspose.Slides](../../svgimage)
* συγκρότηση [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->