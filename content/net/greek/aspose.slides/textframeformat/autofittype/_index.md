---
title: AutofitType
second_title: Aspose.Sildes για .NET API Αναφορά
description: Επιστρέφει ή ορίζει τη λειτουργία αυτόματης προσαρμογής κειμένου. Read/write TextAutofitTypeaspose.slides/textautofittype.
type: docs
weight: 30
url: /el/aspose.slides/textframeformat/autofittype/
---
## TextFrameFormat.AutofitType ιδιότητα

Επιστρέφει ή ορίζει τη λειτουργία αυτόματης προσαρμογής κειμένου. Ανάγνωση/εγγραφή [`TextAutofitType`](../../textautofittype).

```csharp
public TextAutofitType AutofitType { get; set; }
```

### Παραδείγματα

Το παρακάτω παράδειγμα κώδικα δείχνει πώς να αλλάξετε το μέγεθος του σχήματος ώστε να προσαρμόζεται το κείμενο σε μια παρουσίαση PowerPoint.

```csharp
[C#]
 using (Presentation pres = new Presentation())
{
    ISlide slide = pres.Slides[0];
    IAutoShape autoShape = slide.Shapes.AddAutoShape(ShapeType.Rectangle, 30, 30, 350, 100);
    Portion portion = new Portion("lorem ipsum...");
    portion.PortionFormat.FillFormat.SolidFillColor.Color = Color.Black;
    portion.PortionFormat.FillFormat.FillType = FillType.Solid;
    autoShape.TextFrame.Paragraphs[0].Portions.Add(portion);
    ITextFrameFormat textFrameFormat = autoShape.TextFrame.TextFrameFormat;
    textFrameFormat.AutofitType = TextAutofitType.Shape;
    pres.Save("Output-presentation.pptx", SaveFormat.Pptx);
}
```

Το παρακάτω παράδειγμα κώδικα δείχνει πώς να μειώσετε το κείμενο όταν υπάρχει υπερχείλιση.

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    ISlide slide = pres.Slides[0];
    IAutoShape autoShape = slide.Shapes.AddAutoShape(ShapeType.Rectangle, 30, 30, 350, 100);
    Portion portion = new Portion("lorem ipsum...");
    portion.PortionFormat.FillFormat.SolidFillColor.Color = Color.Black;
    portion.PortionFormat.FillFormat.FillType = FillType.Solid;
    autoShape.TextFrame.Paragraphs[0].Portions.Add(portion);
    ITextFrameFormat textFrameFormat = autoShape.TextFrame.TextFrameFormat;
    textFrameFormat.AutofitType = TextAutofitType .Normal;
    pres.Save("Output-presentation.pptx", SaveFormat.Pptx);
}
```

### Δείτε επίσης

* enum [TextAutofitType](../../textautofittype)
* κλάση [TextFrameFormat](../../textframeformat)
* χώρος ονομάτων [Aspose.Slides](../../textframeformat)
* συγκρότηση [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->