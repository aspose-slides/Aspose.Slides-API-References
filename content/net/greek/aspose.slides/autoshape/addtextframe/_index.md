---
title: AddTextFrame
second_title: Aspose.Sildes για .NET API Αναφορά
description: Προσθέτει ένα νέο TextFrame σε ένα σχήμα. Εάν το σχήμα έχει ήδη TextFrame, τότε απλώς αλλάζει το κείμενό του.
type: docs
weight: 60
url: /el/aspose.slides/autoshape/addtextframe/
---
## AutoShape.AddTextFrame μέθοδος

Προσθέτει ένα νέο TextFrame σε ένα σχήμα. Εάν το σχήμα έχει ήδη TextFrame, τότε απλώς αλλάζει το κείμενό του.

```csharp
public ITextFrame AddTextFrame(string text)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| text | String | Προεπιλεγμένο κείμενο για ένα νέο TextFrame. |

### Παραδείγματα

Ο παρακάτω κώδικας δείχνει πώς να προσθέσετε υδατογράφημα κειμένου σε παρουσίαση PowerPoint.

```csharp
[C#]
	using (var presentation = new Presentation())
	{
		ISlide slide = presentation.Slides[0];
		IAutoShape watermarkShape = slide.Shapes.AddAutoShape(ShapeType.Triangle, 0, 0, 150, 50);
		ITextFrame watermarkTextFrame = watermarkShape.AddTextFrame("Watermark");
	}
```

Το παρακάτω παράδειγμα δείχνει πώς να δημιουργήσετε πλαίσιο κειμένου σε μια διαφάνεια.

```csharp
[C#]
// Δημιουργεί μια παρουσίαση
using (Presentation pres = new Presentation())
{
    // Λαμβάνει την πρώτη διαφάνεια στην παρουσίαση
    ISlide sld = pres.Slides[0];
    // Προσθέτει AutoShape με τύπο ορισμένο ως Rectangle
    IAutoShape ashp = sld.Shapes.AddAutoShape(ShapeType.Rectangle, 150, 75, 150, 50);
    // Προσθέτει TextFrame στο Rectangle
    ashp.AddTextFrame(" ");
    // Πρόσβαση στο πλαίσιο κειμένου
    ITextFrame txtFrame = ashp.TextFrame;
    // Δημιουργεί το αντικείμενο Paragraph για το πλαίσιο κειμένου
    IParagraph para = txtFrame.Paragraphs[0];
    // Δημιουργεί ένα αντικείμενο Portion για την παράγραφο
    IPortion portion = para.Portions[0];
    // Ορίζει το κείμενο
    portion.Text = "Aspose TextBox";
    // Αποθηκεύει την παρουσίαση στο δίσκο
    pres.Save("TextBox_out.pptx", Aspose.Slides.Export.SaveFormat.Pptx);
}
```

Το παρακάτω παράδειγμα δείχνει πώς να προσθέσετε στήλη σε πλαίσιο κειμένου.

```csharp
[C#]
using (Presentation presentation = new Presentation())
{
	// Λαμβάνει την πρώτη διαφάνεια στην παρουσίαση
	ISlide slide = presentation.Slides[0];
	// Προσθέτει AutoShape με τύπο ορισμένο ως Rectangle
	IAutoShape aShape = slide.Shapes.AddAutoShape(ShapeType.Rectangle, 100, 100, 300, 300);
	// Προσθέτει TextFrame στο Rectangle
	aShape.AddTextFrame("All these columns are limited to be within a single text container -- " +
	"you can add or delete text and the new or remaining text automatically adjusts " +
	"itself to flow within the container. You cannot have text flow from one container " +
	"to other though -- we told you PowerPoint's column options for text are limited!");
	// Λαμβάνει τη μορφή κειμένου του TextFrame
	ITextFrameFormat format = aShape.TextFrame.TextFrameFormat;
	// Καθορίζει τον αριθμό των στηλών στο TextFrame
	format.ColumnCount = 3;
	// Καθορίζει το διάστημα μεταξύ των στηλών
	format.ColumnSpacing = 10;
	// Αποθηκεύει την παρουσίαση
	presentation.Save("ColumnCount.pptx", SaveFormat.Pptx);
}
```

### Δείτε επίσης

* διασύνδεση [ITextFrame](../../itextframe)
* τάξη [AutoShape](../../autoshape)
* χώρος ονομάτων [Aspose.Slides](../../autoshape)
* μονάδα [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->