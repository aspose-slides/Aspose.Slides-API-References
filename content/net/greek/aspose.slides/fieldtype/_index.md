---
title: FieldType
second_title: Aspose.Sildes για .NET Αναφορά API
description: Αναπαριστά έναν τύπο πεδίου. Αυτή η τιμή καθορίζει ποιο κείμενο θα οριστεί στο τμήμα του πεδίου όταν ενημερωθεί.
type: docs
weight: 4740
url: /el/aspose.slides/fieldtype/
---
## FieldType κλάση

Αναπαριστά έναν τύπο πεδίου. Αυτή η τιμή καθορίζει ποιο κείμενο θα οριστεί στο τμήμα του πεδίου όταν ενημερωθεί.

```csharp
public sealed class FieldType : IFieldType
```

## Κατασκευαστές

| Name | Description |
| --- | --- |
| [FieldType](fieldtype)(string) | Αρχικοποιεί ένα νέο στιγμιότυπο της κλάσης FieldType. |

## Ιδιότητες

| Name | Description |
| --- | --- |
| static [DateTime](../../aspose.slides/fieldtype/datetime) { get; } | Τρέχουσα ημερομηνία και ώρα στην προεπιλεγμένη μορφή ημερομηνίας-ώρα για την εφαρμογή απόδοσης. Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime1](../../aspose.slides/fieldtype/datetime1) { get; } | Τρέχουσα ημερομηνία και ώρα σε πρώτη προρυθμισμένη μορφή (MM/DD/YYYY for english). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime10](../../aspose.slides/fieldtype/datetime10) { get; } | Τρέχουσα ημερομηνία και ώρα σε δέκατη προρυθμισμένη μορφή (hh:mm for english). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime11](../../aspose.slides/fieldtype/datetime11) { get; } | Τρέχουσα ημερομηνία και ώρα σε ενδέκατη προρυθμισμένη μορφή (hh:mm:ss for english). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime12](../../aspose.slides/fieldtype/datetime12) { get; } | Τρέχουσα ημερομηνία και ώρα σε δωδέκατη προρυθμισμένη μορφή (hh:mm AM/PM for english). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime13](../../aspose.slides/fieldtype/datetime13) { get; } | Τρέχουσα ημερομηνία και ώρα σε δωδέκατη προρυθμισμένη μορφή (hh:mm:ss AM/PM for english). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime2](../../aspose.slides/fieldtype/datetime2) { get; } | Τρέχουσα ημερομηνία και ώρα σε δεύτερη προρυθμισμένη μορφή (Day, Month DD, YYYY for english). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime3](../../aspose.slides/fieldtype/datetime3) { get; } | Τρέχουσα ημερομηνία και ώρα σε τρίτη προρυθμισμένη μορφή (DD Month YYYY for english). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime4](../../aspose.slides/fieldtype/datetime4) { get; } | Τρέχουσα ημερομηνία και ώρα σε τέταρτη προρυθμισμένη μορφή (Month DD, YYYY for english). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime5](../../aspose.slides/fieldtype/datetime5) { get; } | Τρέχουσα ημερομηνία και ώρα σε πέμπτη προρυθμισμένη μορφή (DD-Mon-YY for english). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime6](../../aspose.slides/fieldtype/datetime6) { get; } | Τρέχουσα ημερομηνία και ώρα σε έκτη προρυθμισμένη μορφή (Month YY for english). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime7](../../aspose.slides/fieldtype/datetime7) { get; } | Τρέχουσα ημερομηνία και ώρα σε έβδομη προρυθμισμένη μορφή (Mon-YY for english). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime8](../../aspose.slides/fieldtype/datetime8) { get; } | Τρέχουσα ημερομηνία και ώρα σε όγδοη προρυθμισμένη μορφή (MM/DD/YYYY hh:mm AM/PM for english). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime9](../../aspose.slides/fieldtype/datetime9) { get; } | Τρέχουσα ημερομηνία και ώρα σε δεύτερη προρυθμισμένη μορφή (MM/DD/YYYY hh:mm:ss AM/PM for english). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [Footer](../../aspose.slides/fieldtype/footer) { get; } | Υποσέλιδο διαφάνειας. Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [Header](../../aspose.slides/fieldtype/header) { get; } | Κεφαλίδα διαφάνειας. Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [SlideNumber](../../aspose.slides/fieldtype/slidenumber) { get; } | Αριθμός τρέχουσας διαφάνειας. Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| [InternalString](../../aspose.slides/fieldtype/internalstring) { get; set; } | Επιστρέφει το εσωτερικό όνομα αυτού του αντικειμένου FieldType. Ανάγνωση/εγγραφή String. |

## Μέθοδοι

| Name | Description |
| --- | --- |
| override [Equals](../../aspose.slides/fieldtype/equals)(object) | Ελέγχει αν αυτό το πεδίο είναι ίσο με ένα άλλο. |
| override [GetHashCode](../../aspose.slides/fieldtype/gethashcode)() | Επιστρέφει τον κωδικό hash για αυτό το αντικείμενο. |
| [operator ==](../../aspose.slides/fieldtype/op_equality) | Ελέγχει αν δύο αντικείμενα FieldType είναι ίσα. |
| [operator !=](../../aspose.slides/fieldtype/op_inequality) | Ελέγχει αν δύο αντικείμενα FieldType δεν είναι ίσα. |

### Δείτε επίσης

* interface [IFieldType](../ifieldtype)
* namespace [Aspose.Slides](../../aspose.slides)
* assembly [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->