---
title: FieldType
second_title: Aspose.Sildes για .NET API Αναφορά
description: Αναπαριστά έναν τύπο πεδίου. Αυτή η τιμή καθορίζει ποιο κείμενο θα οριστεί στο τμήμα του πεδίου όταν θα ενημερωθεί.
type: docs
weight: 4760
url: /el/aspose.slides/fieldtype/
---
## FieldType κλάση

Αναπαριστά έναν τύπο πεδίου. Αυτή η τιμή καθορίζει το κείμενο που θα οριστεί στο μέρος του πεδίου όταν θα ενημερωθεί.

```csharp
public sealed class FieldType : IFieldType
```

## Κατασκευαστές

| Όνομα | Περιγραφή |
| --- | --- |
| [FieldType](fieldtype)(string) | Δημιουργεί ένα νέο αντικείμενο της κλάσης FieldType. |

## Ιδιότητες

| Όνομα | Περιγραφή |
| --- | --- |
| static [DateTime](../../aspose.slides/fieldtype/datetime) { get; } | Τρέχουσα ημερομηνία και ώρα στην προεπιλεγμένη μορφή ημερομηνίας ώρας για την εφαρμογή απόδοσης. Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime1](../../aspose.slides/fieldtype/datetime1) { get; } | Τρέχουσα ημερομηνία και ώρα σε πρώτο προκαθορισμένο μορφότυπο (MM/DD/YYYY για αγγλικά). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime10](../../aspose.slides/fieldtype/datetime10) { get; } | Τρέχουσα ημερομηνία και ώρα σε δέκατο προκαθορισμένο μορφότυπο (hh:mm για αγγλικά). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime11](../../aspose.slides/fieldtype/datetime11) { get; } | Τρέχουσα ημερομηνία και ώρα σε ενδέκατο προκαθορισμένο μορφότυπο (hh:mm:ss για αγγλικά). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime12](../../aspose.slides/fieldtype/datetime12) { get; } | Τρέχουσα ημερομηνία και ώρα σε δωδέκατο προκαθορισμένο μορφότυπο (hh:mm AM/PM για αγγλικά). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime13](../../aspose.slides/fieldtype/datetime13) { get; } | Τρέχουσα ημερομηνία και ώρα σε δέκατο τρίτο προκαθορισμένο μορφότυπο (hh:mm:ss AM/PM για αγγλικά). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime2](../../aspose.slides/fieldtype/datetime2) { get; } | Τρέχουσα ημερομηνία και ώρα σε δεύτερο προκαθορισμένο μορφότυπο (Day, Month DD, YYYY για αγγλικά). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime3](../../aspose.slides/fieldtype/datetime3) { get; } | Τρέχουσα ημερομηνία και ώρα σε τρίτο προκαθορισμένο μορφότυπο (DD Month YYYY για αγγλικά). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime4](../../aspose.slides/fieldtype/datetime4) { get; } | Τρέχουσα ημερομηνία και ώρα σε τέταρτο προκαθορισμένο μορφότυπο (Month DD, YYYY για αγγλικά). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime5](../../aspose.slides/fieldtype/datetime5) { get; } | Τρέχουσα ημερομηνία και ώρα σε πέμπτο προκαθορισμένο μορφότυπο (DD-Mon-YY για αγγλικά). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime6](../../aspose.slides/fieldtype/datetime6) { get; } | Τρέχουσα ημερομηνία και ώρα σε έκτο προκαθορισμένο μορφότυπο (Month YY για αγγλικά). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime7](../../aspose.slides/fieldtype/datetime7) { get; } | Τρέχουσα ημερομηνία και ώρα σε έβδομο προκαθορισμένο μορφότυπο (Mon-YY για αγγλικά). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime8](../../aspose.slides/fieldtype/datetime8) { get; } | Τρέχουσα ημερομηνία και ώρα σε όγδοο προκαθορισμένο μορφότυπο (MM/DD/YYYY hh:mm AM/PM για αγγλικά). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [DateTime9](../../aspose.slides/fieldtype/datetime9) { get; } | Τρέχουσα ημερομηνία και ώρα σε ένατο προκαθορισμένο μορφότυπο (MM/DD/YYYY hh:mm:ss AM/PM για αγγλικά). Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [Footer](../../aspose.slides/fieldtype/footer) { get; } | Υπόσπασμα διαφάνειας. Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [Header](../../aspose.slides/fieldtype/header) { get; } | Κεφαλίδα διαφάνειας. Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| static [SlideNumber](../../aspose.slides/fieldtype/slidenumber) { get; } | Αριθμός τρέχουσας διαφάνειας. Μόνο ανάγνωση [`FieldType`](../fieldtype). |
| [InternalString](../../aspose.slides/fieldtype/internalstring) { get; set; } | Επιστρέφει το εσωτερικό όνομα αυτού του αντικειμένου FieldType. Ανάγνωση/Εγγραφή String. |

## Μέθοδοι

| Όνομα | Περιγραφή |
| --- | --- |
| override [Equals](../../aspose.slides/fieldtype/equals)(object) | Ελέγχει αν αυτό το πεδίο είναι ίσο με άλλο. |
| override [GetHashCode](../../aspose.slides/fieldtype/gethashcode)() | Επιστρέφει το hashcode για αυτό το αντικείμενο. |
| [operator ==](../../aspose.slides/fieldtype/op_equality) | Ελέγχει αν δύο αντικείμενα FieldType είναι ίσα. |
| [operator !=](../../aspose.slides/fieldtype/op_inequality) | Ελέγχει αν δύο αντικείμενα FieldType δεν είναι ίσα. |

### Δείτε επίσης

* διασύνδεση [IFieldType](../ifieldtype)
* χώρο ονομάτων [Aspose.Slides](../../aspose.slides)
* συναρμολόγηση [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->