---
title: SmartArtLayoutType
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Đại diện cho loại bố cục của một sơ đồ SmartArt.
type: docs
weight: 10620
url: /vi/aspose.slides.smartart/smartartlayouttype/
---
## SmartArtLayoutType liệt kê

Đại diện cho loại bố cục của một sơ đồ SmartArt.

```csharp
public enum SmartArtLayoutType
```

### Các giá trị

| Tên | Giá trị | Mô tả |
| --- | --- | --- |
| AccentProcess | `0` | Sử dụng để hiển thị một quá trình, một dòng thời gian hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. Thích hợp cho việc minh họa cả văn bản Cấp 1 và Cấp 2. |
| AccentedPicture | `1` | Sử dụng để hiển thị một ý tưởng ảnh trung tâm cùng các ý tưởng liên quan ở hai bên. Văn bản Cấp 1 ở trên hiển thị phía trên ảnh trung tâm. Văn bản tương ứng cho các hình Cấp 1 khác xuất hiện bên cạnh các ảnh tròn nhỏ. Bố cục này cũng hoạt động tốt khi không có văn bản. |
| AlternatingFlow | `2` | Sử dụng để hiển thị các nhóm thông tin hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. Nhấn mạnh sự tương tác hoặc mối quan hệ giữa các nhóm thông tin. |
| AlternatingHexagons | `3` | Sử dụng để biểu thị một loạt các ý tưởng liên kết. Văn bản Cấp 1 xuất hiện bên trong các hình lục giác. Văn bản Cấp 2 xuất hiện bên ngoài các hình. |
| AlternatingPictureBlocks | `4` | Sử dụng để hiển thị một chuỗi ảnh từ trên xuống dưới. Văn bản xuất hiện xen kẽ ở bên phải hoặc bên trái của ảnh. |
| AlternatingPictureCircles | `5` | Sử dụng để hiển thị một tập hợp ảnh kèm văn bản. Văn bản tương ứng xuất hiện trong các vòng tròn trung tâm với các ảnh xen kẽ từ trái sang phải. |
| ArrowRibbon | `6` | Sử dụng để hiển thị các khái niệm liên quan hoặc trái ngược có một số kết nối, ví dụ như các lực đối lập. Hai dòng đầu tiên của văn bản Cấp 1 được dùng làm văn bản trong các mũi tên. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| AscendingPictureAccentProcess | `7` | Sử dụng để hiển thị một chuỗi ảnh tăng dần cùng văn bản mô tả. Thích hợp nhất với một lượng nhỏ văn bản. |
| Balance | `8` | Sử dụng để so sánh hoặc hiển thị mối quan hệ giữa hai ý tưởng. Mỗi trong hai dòng đầu tiên của văn bản Cấp 1 tương ứng với văn bản ở trên một bên của điểm trung tâm. Nhấn mạnh văn bản Cấp 2, giới hạn bốn hình ở mỗi bên của điểm trung tâm. Cân bằng nghiêng về phía có nhiều hình chứa văn bản Cấp 2 nhất. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| BasicBendingProcess | `9` | Sử dụng để hiển thị một quá trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. Tối đa hoá không gian hiển thị ngang và dọc cho các hình. |
| BasicBlockList | `10` | Sử dụng để hiển thị các khối thông tin không tuần tự hoặc được nhóm lại. Tối đa hoá không gian hiển thị ngang và dọc cho các hình. |
| BasicChevronProcess | `11` | Sử dụng để hiển thị một quá trình; một dòng thời gian; các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc; hoặc để nhấn mạnh chuyển động hoặc hướng đi. Văn bản Cấp 1 xuất hiện bên trong một hình mũi tên trong khi văn bản Cấp 2 xuất hiện bên dưới các hình mũi tên. |
| BasicCycle | `12` | Sử dụng để biểu thị một chuỗi liên tục của các giai đoạn, nhiệm vụ hoặc sự kiện trong một luồng tròn. Nhấn mạnh các giai đoạn hoặc bước hơn là các mũi tên hoặc luồng kết nối. Thích hợp nhất với chỉ văn bản Cấp 1. |
| BasicMatrix | `13` | Sử dụng để hiển thị mối quan hệ của các thành phần với toàn bộ trong các góc phần tư. Bốn dòng đầu tiên của văn bản Cấp 1 xuất hiện trong các góc phần tư. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| BasicPie | `14` | Sử dụng để hiển thị cách các phần riêng lẻ tạo thành một toàn thể. Bảy dòng đầu tiên của văn bản Cấp 1 tương ứng với các hình bánh tròn hoặc miếng bánh được phân bố đều. Hình văn bản Cấp 1 ở trên cùng xuất hiện bên ngoài phần còn lại của bánh để nhấn mạnh. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| BasicProcess | `15` | Sử dụng để hiển thị một quá trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. |
| BasicPyramid | `16` | Sử dụng để hiển thị các mối quan hệ tỷ lệ, liên kết hoặc phân cấp với thành phần lớn nhất ở dưới và thu hẹp lên phía trên. Văn bản Cấp 1 xuất hiện trong các đoạn kim tự tháp và văn bản Cấp 2 xuất hiện trong các hình bên cạnh mỗi đoạn. |
| BasicRadial | `17` | Sử dụng để hiển thị mối quan hệ với một ý tưởng trung tâm trong một vòng tròn. Dòng đầu tiên của văn bản Cấp 1 tương ứng với hình trung tâm, và văn bản Cấp 2 của nó tương ứng với các hình tròn bao quanh. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| BasicTarget | `18` | Sử dụng để hiển thị sự chứa đựng, độ dần hoặc các mối quan hệ phân cấp. Năm dòng đầu tiên của văn bản Cấp 1 được gắn với một vòng tròn. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| BasicTimeline | `19` | Sử dụng để hiển thị các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc, hoặc để hiển thị thông tin dòng thời gian. Thích hợp cho cả văn bản Cấp 1 và Cấp 2. |
| BasicVenn | `20` | Sử dụng để hiển thị các mối quan hệ chồng chéo hoặc liên kết. Bảy dòng đầu tiên của văn bản Cấp 1 tương ứng với một vòng tròn. Nếu có bốn dòng hoặc ít hơn của văn bản Cấp 1, văn bản nằm bên trong các vòng tròn. Nếu có hơn bốn dòng của văn bản Cấp 1, văn bản nằm bên ngoài các vòng tròn. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| BendingPictureAccentList | `21` | Sử dụng để hiển thị các khối thông tin không tuần tự hoặc được nhóm lại. Các hình tròn nhỏ được thiết kế để chứa ảnh. Thích hợp cho việc minh họa cả văn bản Cấp 1 và Cấp 2. Tối đa hoá không gian hiển thị ngang và dọc cho các hình. |
| BendingPictureBlocks | `22` | Sử dụng để hiển thị một chuỗi ảnh. Hộp bao phủ góc dưới có thể chứa một lượng nhỏ văn bản. |
| BendingPictureCaption | `23` | Sử dụng để hiển thị một chuỗi ảnh tuần tự. Hộp bao phủ góc dưới có thể chứa một lượng nhỏ văn bản. |
| BendingPictureCaptionList | `24` | Sử dụng để hiển thị một chuỗi ảnh. Tiêu đề và mô tả xuất hiện trong một hình chú thích dưới mỗi ảnh. |
| BendingPictureSemiTransparentText | `25` | Sử dụng để hiển thị một chuỗi ảnh. Một hộp bán trong suốt bao phủ phần dưới của ảnh và chứa tất cả các cấp văn bản. |
| BlockCycle | `26` | Sử dụng để biểu thị một chuỗi liên tục của các giai đoạn, nhiệm vụ hoặc sự kiện trong một luồng tròn. Nhấn mạnh các giai đoạn hoặc bước hơn là các mũi tên hoặc luồng kết nối. |
| BubblePictureList | `27` | Sử dụng để hiển thị một chuỗi ảnh. Có thể chứa tới tám ảnh Cấp 1. Văn bản và ảnh không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. Thích hợp nhất với một lượng nhỏ văn bản. |
| CaptionedPictures | `28` | Sử dụng để hiển thị ảnh với nhiều cấp văn bản. Thích hợp nhất với một lượng nhỏ văn bản Cấp 1 và một lượng vừa phải văn bản Cấp 2. |
| ChevronList | `29` | Sử dụng để hiển thị một quá trình tiến triển qua nhiều quy trình tạo nên một luồng công việc tổng thể. Cũng hoạt động cho việc minh họa các quy trình trái ngược. Văn bản Cấp 1 tương ứng với hình mũi tên đầu tiên ở bên trái, trong khi văn bản Cấp 2 tương ứng với các bước phụ ngang cho mỗi hình chứa văn bản Cấp 1. |
| CircleAccentTimeline | `30` | Sử dụng để hiển thị một chuỗi sự kiện hoặc thông tin dòng thời gian. Văn bản Cấp 1 xuất hiện bên cạnh các vòng tròn lớn hơn. Văn bản Cấp 2 xuất hiện bên cạnh các vòng tròn nhỏ hơn. |
| CircleArrowProcess | `31` | Sử dụng để hiển thị các mục tuần tự với văn bản hỗ trợ cho mỗi mục. Sơ đồ này hoạt động tốt nhất với một lượng nhỏ văn bản Cấp 1. |
| CirclePictureHierarchy | `32` | Sử dụng để hiển thị thông tin phân cấp hoặc mối quan hệ báo cáo trong một tổ chức. Ảnh xuất hiện trong các vòng tròn và văn bản tương ứng xuất hiện bên cạnh các ảnh. |
| CircleRelationship | `33` | Sử dụng để hiển thị mối quan hệ tới hoặc từ một ý tưởng trung tâm. Văn bản Cấp 2 được thêm không tuần tự và giới hạn ở năm mục. Chỉ có một mục Cấp 1 duy nhất. |
| CircularBendingProcess | `34` | Sử dụng để hiển thị một chuỗi dài hoặc phi tuyến tính các bước trong một nhiệm vụ, quy trình hoặc luồng công việc. Thích hợp nhất với chỉ văn bản Cấp 1. Tối đa hoá không gian hiển thị ngang và dọc cho các hình. |
| CircularPictureCallout | `35` | Sử dụng để hiển thị một ý tưởng trung tâm và các ý tưởng phụ hoặc các mục liên quan. Văn bản cho ảnh đầu tiên bao phủ phần dưới của ảnh. Văn bản tương ứng cho các hình Cấp 1 khác xuất hiện bên cạnh các ảnh tròn nhỏ. Sơ đồ này cũng hoạt động tốt khi không có văn bản. |
| ClosedChevronProcess | `36` | Sử dụng để hiển thị một quá trình, một dòng thời gian hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc, hoặc để nhấn mạnh chuyển động hoặc hướng đi. Có thể được dùng để nhấn mạnh thông tin trong hình bắt đầu. Thích hợp nhất với chỉ văn bản Cấp 1. |
| ContinuousArrowProcess | `37` | Sử dụng để hiển thị một dòng thời gian hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. Thích hợp nhất với văn bản Cấp 1 vì mỗi dòng văn bản Cấp 1 xuất hiện bên trong hình mũi tên. Văn bản Cấp 2 xuất hiện bên ngoài hình mũi tên. |
| ContinuousBlockProcess | `38` | Sử dụng để hiển thị một quá trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. Thích hợp nhất với văn bản Cấp 1 và Cấp 2 tối thiểu. |
| ContinuousCycle | `39` | Sử dụng để biểu thị một chuỗi liên tục của các giai đoạn, nhiệm vụ hoặc sự kiện trong một luồng tròn. Nhấn mạnh sự kết nối giữa tất cả các thành phần. Thích hợp nhất với chỉ văn bản Cấp 1. |
| ContinuousPictureList | `40` | Sử dụng để hiển thị các nhóm thông tin liên kết. Các hình tròn được thiết kế để chứa ảnh. |
| ConvergingArrows | `41` | Sử dụng để hiển thị các ý tưởng hoặc khái niệm tụ lại một điểm trung tâm. Thích hợp nhất với chỉ văn bản Cấp 1. |
| ConvergingRadial | `42` | Sử dụng để hiển thị mối quan hệ của các khái niệm hoặc thành phần tới một ý tưởng trung tâm trong một vòng tròn. Dòng đầu tiên của văn bản Cấp 1 tương ứng với hình tròn trung tâm và các dòng văn bản Cấp 2 tương ứng với các hình chữ nhật bao quanh. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| CounterbalanceArrows | `43` | Sử dụng để hiển thị hai ý tưởng hoặc khái niệm đối lập. Mỗi trong hai dòng đầu tiên của văn bản Cấp 1 tương ứng với một mũi tên và hoạt động tốt với văn bản Cấp 2. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| CycleMatrix | `44` | Sử dụng để hiển thị mối quan hệ tới một ý tưởng trung tâm trong một quá trình chu kỳ. Mỗi trong bốn dòng đầu tiên của văn bản Cấp 1 tương ứng với một miếng bánh hoặc hình bánh, và văn bản Cấp 2 xuất hiện trong một hình chữ nhật bên cạnh miếng bánh hoặc hình bánh. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| DescendingBlockList | `45` | Sử dụng để hiển thị các nhóm ý tưởng liên quan hoặc danh sách thông tin. Các hình văn bản giảm dần chiều cao một cách tuần tự, và văn bản Cấp 1 hiển thị theo chiều dọc. |
| DescendingProcess | `46` | Sử dụng để hiển thị một chuỗi sự kiện giảm dần. Văn bản Cấp 1 đầu tiên ở trên mũi tên, và văn bản Cấp 1 cuối cùng hiển thị ở dưới mũi tên. Chỉ bảy mục Cấp 1 đầu tiên xuất hiện. Thích hợp nhất với một lượng văn bản vừa phải đến nhỏ. |
| DetailedProcess | `47` | Sử dụng với một lượng lớn văn bản Cấp 2 để hiển thị một quá trình tiến triển qua các giai đoạn. |
| DivergingArrows | `48` | Sử dụng để hiển thị các ý tưởng hoặc khái niệm lan rộng ra từ một nguồn trung tâm. Thích hợp nhất với chỉ văn bản Cấp 1. |
| DivergingRadial | `49` | Sử dụng để hiển thị mối quan hệ tới một ý tưởng trung tâm trong một vòng tròn. Dòng đầu tiên của văn bản Cấp 1 tương ứng với hình tròn trung tâm. Nhấn mạnh các vòng tròn bao quanh hơn là ý tưởng trung tâm. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| Equation | `50` | Sử dụng để hiển thị các bước hoặc nhiệm vụ tuần tự mô tả một kế hoạch hoặc kết quả. Dòng cuối cùng của văn bản Cấp 1 xuất hiện sau dấu bằng (=). Thích hợp nhất với chỉ văn bản Cấp 1. |
| FramedTextPicture | `51` | Sử dụng để hiển thị ảnh với văn bản Cấp 1 tương ứng được hiển thị trong một khung. |
| Funnel | `52` | Sử dụng để hiển thị việc lọc thông tin hoặc cách các phần hợp nhất thành một toàn thể. Nhấn mạnh kết quả cuối cùng. Có thể chứa tới bốn dòng văn bản Cấp 1; dòng cuối cùng trong bốn dòng này xuất hiện dưới phễu và các dòng còn lại tương ứng với một hình tròn. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| Gear | `53` | Sử dụng để hiển thị các ý tưởng liên kết. Mỗi trong ba dòng đầu tiên của văn bản Cấp 1 tương ứng với một hình bánh răng, và văn bản Cấp 2 tương ứng xuất hiện trong các hình chữ nhật bên cạnh bánh răng. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| GridMatrix | `54` | Sử dụng để hiển thị vị trí của các khái niệm dọc hai trục. Nhấn mạnh các thành phần riêng lẻ hơn là toàn bộ. Bốn dòng đầu tiên của văn bản Cấp 1 xuất hiện trong các góc phần tư. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| GroupedList | `55` | Sử dụng để hiển thị các nhóm và nhóm phụ của thông tin, hoặc các bước và bước phụ trong một nhiệm vụ, quy trình hoặc luồng công việc. Văn bản Cấp 1 tương ứng với các hình ngang cấp cao nhất, và văn bản Cấp 2 tương ứng với các bước phụ dọc dưới mỗi hình cấp cao liên quan. Thích hợp cho việc nhấn mạnh các nhóm phụ hoặc các bước phụ, thông tin phân cấp, hoặc nhiều danh sách thông tin. |
| HalfCircleOrganizationChart | `56` | Sử dụng để hiển thị thông tin phân cấp hoặc mối quan hệ báo cáo trong một tổ chức. Các hình trợ giúp và bố cục treo Org Chart có sẵn với bố cục này. |
| HexagonCluster | `57` | Sử dụng để hiển thị ảnh với văn bản mô tả đi kèm. Các hình lục giác nhỏ chỉ ra cặp ảnh và văn bản. Thích hợp nhất với một lượng nhỏ văn bản. |
| Hierarchy | `58` | Sử dụng để hiển thị các mối quan hệ phân cấp tiến triển từ trên xuống dưới. |
| HierarchyList | `59` | Sử dụng để hiển thị các mối quan hệ phân cấp tiến triển qua các nhóm. Cũng có thể được sử dụng để nhóm hoặc liệt kê thông tin. |
| HorizontalBulletList | `60` | Sử dụng để hiển thị các danh sách thông tin không tuần tự hoặc được nhóm lại. Thích hợp với một lượng lớn văn bản. Tất cả văn bản có cùng mức độ nhấn mạnh, và không ngụ ý hướng. |
| HorizontalHierarchy | `61` | Sử dụng để hiển thị các mối quan hệ phân cấp tiến triển theo chiều ngang. Thích hợp cho các cây quyết định. |
| HorizontalLabeledHierarchy | `62` | Sử dụng để hiển thị các mối quan hệ phân cấp tiến triển theo chiều ngang và được nhóm phân cấp. Nhấn mạnh tiêu đề hoặc văn bản Cấp 1. Dòng đầu tiên của văn bản Cấp 1 xuất hiện trong hình ở đầu của phân cấp, và dòng thứ hai và các dòng tiếp theo của văn bản Cấp 1 xuất hiện ở trên các hình chữ nhật cao. |
| HorizontalMultiLevelHierarchy | `63` | Sử dụng để hiển thị một lượng lớn thông tin phân cấp tiến triển theo chiều ngang. Đỉnh của phân cấp được hiển thị dọc. Bố cục này hỗ trợ nhiều cấp trong phân cấp. |
| HorizontalOrganizationChart | `64` | Sử dụng để hiển thị thông tin phân cấp theo chiều ngang hoặc mối quan hệ báo cáo trong một tổ chức. Hình trợ giúp và bố cục treo Org Chart có sẵn với bố cục này. |
| HorizontalPictureList | `65` | Sử dụng để hiển thị thông tin không tuần tự hoặc được nhóm lại với nhấn mạnh vào các ảnh liên quan. Các hình trên cùng được thiết kế để chứa ảnh. |
| IncreasingArrowsProcess | `66` | Sử dụng để hiển thị các bước tuần tự và chồng chập trong một quy trình. Giới hạn tới năm mục Cấp 1. Cấp 2 có thể chứa một lượng lớn văn bản. |
| IncreasingCircleProcess | `67` | Sử dụng để hiển thị một chuỗi các bước, với bên trong vòng tròn tăng dần theo mỗi bước. Giới hạn tới bảy bước Cấp 1 nhưng không giới hạn mục Cấp 2. Thích hợp với một lượng lớn văn bản Cấp 2. |
| InvertedPyramid | `68` | Sử dụng để hiển thị các mối quan hệ tỷ lệ, liên kết hoặc phân cấp với thành phần lớn nhất ở trên và thu hẹp xuống. Văn bản Cấp 1 xuất hiện trong các đoạn kim tự tháp và văn bản Cấp 2 xuất hiện trong các hình bên cạnh mỗi đoạn. |
| LabeledHierarchy | `69` | Sử dụng để hiển thị các mối quan hệ phân cấp tiến triển từ trên xuống dưới và được nhóm phân cấp. Nhấn mạnh tiêu đề hoặc văn bản Cấp 1. Dòng đầu tiên của văn bản Cấp 1 xuất hiện trong hình ở đầu của phân cấp, và tất cả các dòng tiếp theo của văn bản Cấp 1 xuất hiện bên trái các hình chữ nhật dài. |
| LinearVenn | `70` | Sử dụng để hiển thị các mối quan hệ chồng chéo trong một chuỗi. Thích hợp nhất với chỉ văn bản Cấp 1. |
| LinedList | `71` | Sử dụng để hiển thị một lượng lớn văn bản được chia thành các danh mục và danh mục phụ. Thích hợp với nhiều cấp văn bản. Văn bản cùng mức được ngăn cách bằng các đường. |
| MultidirectionalCycle | `72` | Sử dụng để biểu thị một chuỗi liên tục của các giai đoạn, nhiệm vụ hoặc sự kiện có thể xảy ra theo bất kỳ hướng nào. |
| NameandTitleOrganizationChart | `73` | Sử dụng để hiển thị thông tin phân cấp hoặc mối quan hệ báo cáo trong một tổ chức. Để nhập văn bản vào hộp tiêu đề, gõ trực tiếp trong hình chữ nhật nhỏ hơn. Hình trợ giúp và bố cục treo Org Chart có sẵn với bố cục này. |
| NestedTarget | `74` | Sử dụng để hiển thị các mối quan hệ chứa đựng. Mỗi trong ba dòng đầu tiên của văn bản Cấp 1 tương ứng với văn bản ở góc trên bên trái trong các hình, và văn bản Cấp 2 tương ứng với các hình nhỏ hơn. Thích hợp nhất với ít dòng văn bản Cấp 2. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| NondirectionalCycle | `75` | Sử dụng để biểu thị một chuỗi liên tục của các giai đoạn, nhiệm vụ hoặc sự kiện trong một luồng tròn. Mỗi hình có mức quan trọng như nhau. Thích hợp khi không cần chỉ ra hướng. |
| OpposingArrows | `76` | Sử dụng để hiển thị hai ý tưởng đối lập, hoặc các ý tưởng phân kỳ từ một điểm trung tâm. Mỗi trong hai dòng đầu tiên của văn bản Cấp 1 tương ứng với một mũi tên. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| OpposingIdeas | `77` | Sử dụng để hiển thị hai ý tưởng đối lập hoặc trái ngược. Có thể có một hoặc hai mục Cấp 1. Mỗi văn bản Cấp 1 có thể chứa nhiều cấp phụ. Thích hợp với một lượng lớn văn bản. |
| OrganizationChart | `78` | Sử dụng để hiển thị thông tin phân cấp hoặc mối quan hệ báo cáo trong một tổ chức. Hình trợ giúp và bố cục treo Org Chart có sẵn với bố cục này. |
| PhasedProcess | `79` | Sử dụng để hiển thị ba giai đoạn của một quy trình. Giới hạn tới ba mục Cấp 1. Hai mục Cấp 1 đầu có thể mỗi mục chứa bốn mục Cấp 2, và mục Cấp 1 thứ ba có thể chứa không giới hạn số mục Cấp 2. Thích hợp nhất với một lượng nhỏ văn bản. |
| PictureAccentBlocks | `80` | Sử dụng để hiển thị một nhóm ảnh trong các khối bắt đầu từ góc. Văn bản tương ứng hiển thị theo chiều dọc. Thích hợp làm điểm nhấn cho các slide tiêu đề hoặc phụ đề hoặc cho các phần ngắt của tài liệu. |
| PictureAccentList | `81` | Sử dụng để hiển thị thông tin được nhóm hoặc liên quan. Các hình nhỏ ở góc trên được thiết kế để chứa ảnh. Nhấn mạnh văn bản Cấp 2 hơn văn bản Cấp 1, và là lựa chọn tốt cho một lượng lớn văn bản Cấp 2. |
| PictureAccentProcess | `82` | Sử dụng để hiển thị các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. Các hình chữ nhật ở nền được thiết kế để chứa ảnh. |
| PictureCaptionList | `83` | Sử dụng để hiển thị các khối thông tin không tuần tự hoặc được nhóm lại. Các hình trên cùng được thiết kế để chứa ảnh và ảnh được nhấn mạnh hơn văn bản. Thích hợp cho ảnh có chú thích ngắn. |
| PictureGrid | `84` | Sử dụng để hiển thị ảnh được sắp xếp trên một lưới vuông. Tốt nhất với một lượng nhỏ văn bản Cấp 1, xuất hiện phía trên ảnh. |
| PictureLineup | `85` | Sử dụng để hiển thị một chuỗi ảnh bên cạnh nhau. Văn bản Cấp 1 bao phủ phần trên của ảnh. Văn bản Cấp 2 xuất hiện bên dưới ảnh. |
| PictureStrips | `86` | Sử dụng để hiển thị một chuỗi ảnh từ trên xuống dưới với văn bản Cấp 1 bên cạnh mỗi ảnh. |
| PieProcess | `87` | Sử dụng để hiển thị các bước trong một quy trình với mỗi miếng bánh tăng kích thước tới bảy hình. Văn bản Cấp 1 hiển thị theo chiều dọc. |
| PlusandMinus | `88` | Sử dụng để hiển thị ưu và nhược điểm của hai ý tưởng. Mỗi văn bản Cấp 1 có thể chứa nhiều cấp phụ. Thích hợp với một lượng lớn văn bản. Giới hạn tới hai mục Cấp 1. |
| ProcessArrows | `89` | Sử dụng để hiển thị thông tin minh họa một quy trình hoặc luồng công việc. Văn bản Cấp 1 xuất hiện trong các hình tròn và văn bản Cấp 2 xuất hiện trong các hình mũi tên. Thích hợp cho văn bản tối thiểu và để nhấn mạnh chuyển động hoặc hướng đi. |
| ProcessList | `90` | Sử dụng để hiển thị nhiều nhóm thông tin hoặc các bước và bước phụ trong một nhiệm vụ, quy trình hoặc luồng công việc. Văn bản Cấp 1 tương ứng với các hình ngang trên cùng, và văn bản Cấp 2 tương ứng với các bước phụ dọc dưới mỗi hình cấp cao liên quan. |
| PyramidList | `91` | Sử dụng để hiển thị các mối quan hệ tỷ lệ, liên kết hoặc phân cấp. Văn bản xuất hiện trong các hình chữ nhật trên nền kim tự tháp. |
| RadialCluster | `92` | Sử dụng để hiển thị dữ liệu liên quan đến một ý tưởng hoặc chủ đề trung tâm. Văn bản Cấp 1 trên cùng xuất hiện ở trung tâm. Văn bản Cấp 2 xuất hiện trong các hình bao quanh. Có thể chứa tới bảy hình Cấp 2. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. Thích hợp nhất với một lượng nhỏ văn bản. |
| RadialCycle | `93` | Sử dụng để hiển thị mối quan hệ tới một ý tưởng trung tâm. Nhấn mạnh cả thông tin trong vòng tròn trung tâm và cách thông tin trong vòng tròn ngoài cùng đóng góp vào ý tưởng trung tâm. Dòng đầu tiên của văn bản Cấp 1 tương ứng với vòng tròn trung tâm, và văn bản Cấp 2 của nó tương ứng với vòng tròn ngoài. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| RadialList | `94` | Sử dụng để hiển thị các mối quan hệ tới một ý tưởng trung tâm trong một vòng tròn. Hình trung tâm có thể chứa một ảnh. Văn bản Cấp 1 xuất hiện trong các vòng tròn nhỏ và bất kỳ văn bản Cấp 2 nào liên quan xuất hiện bên cạnh các vòng tròn nhỏ. |
| RadialVenn | `95` | Sử dụng để hiển thị cả các mối quan hệ chồng chéo và mối quan hệ tới một ý tưởng trung tâm trong một vòng tròn. Dòng đầu tiên của văn bản Cấp 1 tương ứng với hình trung tâm và các dòng văn bản Cấp 2 tương ứng với các hình tròn bao quanh. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| RandomToResultProcess | `96` | Sử dụng để hiển thị, thông qua một chuỗi bước, cách một số ý tưởng hỗn loạn có thể dẫn đến một mục tiêu hoặc ý tưởng thống nhất. Hỗ trợ nhiều mục văn bản Cấp 1, nhưng các hình tương ứng của mục Cấp 1 đầu và cuối là cố định. Thích hợp nhất với một lượng nhỏ văn bản Cấp 1 và một lượng vừa phải văn bản Cấp 2. |
| RepeatingBendingProcess | `97` | Sử dụng để hiển thị một quá trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. Tối đa hoá không gian hiển thị ngang và dọc cho các hình. |
| ReverseList | `98` | Sử dụng để chuyển đổi giữa hai mục. Chỉ hai mục văn bản đầu tiên hiển thị, và mỗi mục có thể chứa một lượng lớn văn bản. Thích hợp để hiển thị sự thay đổi giữa hai mục hoặc sự chuyển đổi trong thứ tự. |
| SegmentedCycle | `99` | Sử dụng để hiển thị một quá trình hoặc một chuỗi các giai đoạn, nhiệm vụ hoặc sự kiện trong một luồng tròn. Nhấn mạnh các phần liên kết. Mỗi trong bảy dòng đầu tiên của văn bản Cấp 1 tương ứng với một miếng bánh hoặc hình bánh. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| SegmentedProcess | `100` | Sử dụng để hiển thị một quá trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. Nhấn mạnh văn bản Cấp 2, vì mỗi dòng xuất hiện trong một hình riêng biệt. |
| SegmentedPyramid | `101` | Sử dụng để hiển thị các mối quan hệ chứa đựng, tỷ lệ hoặc liên kết. Chín dòng đầu tiên của văn bản Cấp 1 xuất hiện trong các hình tam giác. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. Thích hợp nhất với chỉ văn bản Cấp 1. |
| SnapshotPictureList | `102` | Sử dụng để hiển thị ảnh kèm văn bản giải thích. Văn bản Cấp 2 có thể hiển thị các danh sách thông tin. Thích hợp với một lượng lớn văn bản. |
| SpiralPicture | `103` | Sử dụng để hiển thị một chuỗi tới năm ảnh với các chú thích Cấp 1 tương ứng xoắn vào trung tâm. |
| SquareAccentList | `104` | Sử dụng để hiển thị các danh sách thông tin chia thành các danh mục. Văn bản Cấp 2 xuất hiện bên cạnh một hình vuông nhỏ. Thích hợp với một lượng lớn văn bản Cấp 2. |
| StackedList | `105` | Sử dụng để hiển thị các nhóm thông tin hoặc các bước trong một nhiệm vụ, quy trình hoặc luồng công việc. Các hình tròn chứa văn bản Cấp 1, và các hình chữ nhật tương ứng chứa văn bản Cấp 2. Thích hợp cho nhiều chi tiết và văn bản Cấp 1 tối thiểu. |
| StackedVenn | `106` | Sử dụng để hiển thị các mối quan hệ chồng chéo. Lựa chọn tốt để nhấn mạnh sự tăng trưởng hoặc độ dần. Thích hợp nhất với chỉ văn bản Cấp 1. Bảy dòng đầu tiên của văn bản Cấp 1 tương ứng với một hình tròn. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| StaggeredProcess | `107` | Sử dụng để hiển thị một quá trình tiến xuống qua các giai đoạn. Mỗi trong năm dòng đầu tiên của văn bản Cấp 1 tương ứng với một hình chữ nhật. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| StepDownProcess | `108` | Sử dụng để hiển thị một quá trình giảm dần với nhiều bước và bước phụ. Thích hợp với một lượng nhỏ văn bản. |
| StepUpProcess | `109` | Sử dụng để hiển thị một chuỗi tăng dần của các bước hoặc danh sách thông tin. |
| SubStepProcess | `110` | Sử dụng để hiển thị một quy trình đa bước với các bước phụ giữa mỗi lần xuất hiện của văn bản Cấp 1. Thích hợp với một lượng nhỏ văn bản và giới hạn tới bảy bước Cấp 1. Mỗi bước Cấp 1 có thể có không giới hạn bước phụ. |
| TableHierarchy | `111` | Sử dụng để hiển thị các nhóm thông tin được xây dựng từ trên xuống và các phân cấp bên trong mỗi nhóm. Bố cục này không chứa các đường kết nối. |
| TableList | `112` | Sử dụng để hiển thị thông tin được nhóm hoặc liên quan có giá trị ngang nhau. Dòng đầu tiên của văn bản Cấp 1 tương ứng với hình trên cùng và văn bản Cấp 2 của nó được dùng cho các danh sách tiếp theo. |
| TargetList | `113` | Sử dụng để hiển thị thông tin liên quan hoặc chồng chéo. Mỗi trong bảy dòng đầu tiên của văn bản Cấp 1 xuất hiện trong hình chữ nhật. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. Thích hợp với cả văn bản Cấp 1 và Cấp 2. |
| TextCycle | `114` | Sử dụng để biểu thị một chuỗi liên tục của các giai đoạn, nhiệm vụ hoặc sự kiện trong một luồng tròn. Nhấn mạnh các mũi tên hoặc luồng hơn là các giai đoạn hoặc bước. Thích hợp nhất với chỉ văn bản Cấp 1. |
| TitlePictureLineup | `115` | Sử dụng để hiển thị một chuỗi ảnh mỗi ảnh có tiêu đề và mô tả riêng. Văn bản Cấp 1 xuất hiện trong hộp phía trên ảnh. Văn bản Cấp 2 xuất hiện dưới ảnh. |
| TitledMatrix | `116` | Sử dụng để hiển thị các mối quan hệ của bốn góc phần tư với một toàn thể. Dòng đầu tiên của văn bản Cấp 1 tương ứng với hình trung tâm, và bốn dòng đầu tiên của văn bản Cấp 2 xuất hiện trong các góc phần tư. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| TitledPictureAccentList | `117` | Sử dụng để hiển thị các danh sách thông tin với một ảnh nhấn mạnh cho mỗi văn bản Cấp 2. Văn bản Cấp 1 hiển thị trong một hộp riêng ở đầu danh sách. |
| TitledPictureBlocks | `118` | Sử dụng để hiển thị một chuỗi ảnh. Văn bản Cấp 1 xuất hiện phía trên mỗi ảnh. Văn bản Cấp 2 xuất hiện bên cạnh và hơi che phủ mỗi ảnh. |
| TrapezoidList | `119` | Sử dụng để hiển thị các nhóm hoặc thông tin liên quan có giá trị ngang nhau. Thích hợp với một lượng lớn văn bản. |
| UpwardArrow | `120` | Sử dụng để hiển thị một quá trình hoặc các bước có xu hướng tăng lên trong một nhiệm vụ, quy trình hoặc luồng công việc. Mỗi trong năm dòng đầu tiên của văn bản Cấp 1 tương ứng với một điểm trên mũi tên. Thích hợp nhất với văn bản tối thiểu. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| VerticalAccentList | `121` | Sử dụng để hiển thị các danh sách thông tin. Văn bản Cấp 2 xuất hiện trong các hình chữ nhật trên các hình nêm dọc. Nhấn mạnh văn bản Cấp 2 hơn văn bản Cấp 1, và là lựa chọn tốt cho một lượng vừa phải văn bản Cấp 2. |
| VerticalArrowList | `122` | Sử dụng để hiển thị một quá trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc di chuyển về phía mục tiêu chung. Thích hợp cho các danh sách dấu đầu dòng. |
| VerticalBendingProcess | `123` | Sử dụng để hiển thị một quá trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. Tối đa hoá không gian hiển thị ngang và dọc cho các hình. Đặt nhấn mạnh nhiều hơn vào các mối quan hệ giữa các hình hơn là hướng hoặc chuyển động. |
| VerticalBlockList | `124` | Sử dụng để hiển thị các nhóm thông tin hoặc các bước trong một nhiệm vụ, quy trình hoặc luồng công việc. Thích hợp với một lượng lớn văn bản Cấp 2. Lựa chọn tốt cho văn bản có một điểm chính và nhiều điểm phụ. |
| VerticalBoxList | `125` | Sử dụng để hiển thị nhiều nhóm thông tin, đặc biệt là các nhóm có nhiều văn bản Cấp 2. Lựa chọn tốt cho các danh sách dấu đầu dòng. |
| VerticalBulletList | `126` | Sử dụng để hiển thị các khối thông tin không tuần tự hoặc được nhóm lại. Thích hợp cho các danh sách có tiêu đề dài hoặc thông tin cấp cao. |
| VerticalChevronList | `127` | Sử dụng để hiển thị một quá trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc, hoặc để nhấn mạnh chuyển động hoặc hướng đi. Nhấn mạnh văn bản Cấp 2 hơn văn bản Cấp 1, và là lựa chọn tốt cho một lượng lớn văn bản Cấp 2. |
| VerticalCircleList | `128` | Sử dụng để hiển thị dữ liệu tuần tự hoặc được nhóm. Thích hợp nhất cho văn bản Cấp 1, hiển thị bên cạnh một hình tròn lớn. Các cấp văn bản thấp hơn được ngăn cách bằng các hình tròn nhỏ hơn. |
| VerticalCurvedList | `129` | Sử dụng để hiển thị một danh sách thông tin cong. Để thêm ảnh vào các hình vòng tròn nhấn mạnh, áp dụng nền ảnh. |
| VerticalEquation | `130` | Sử dụng để hiển thị các bước hoặc nhiệm vụ tuần tự mô tả một kế hoạch hoặc kết quả. Dòng cuối cùng của văn bản Cấp 1 xuất hiện sau mũi tên. Thích hợp nhất với chỉ văn bản Cấp 1. |
| VerticalPictureAccentList | `131` | Sử dụng để hiển thị các khối thông tin không tuần tự hoặc được nhóm lại. Các vòng tròn nhỏ được thiết kế để chứa ảnh. |
| VerticalPictureList | `132` | Sử dụng để hiển thị các khối thông tin không tuần tự hoặc được nhóm lại. Các hình nhỏ ở bên trái được thiết kế để chứa ảnh. |
| VerticalProcess | `133` | Sử dụng để hiển thị một quá trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc từ trên xuống dưới. Thích hợp nhất với chỉ văn bản Cấp 1, vì không gian dọc bị giới hạn. |
| Custom | `134` | Đại diện cho một sơ đồ SmartArt với mẫu bố cục tùy chỉnh |
| PictureOrganizationChart | `135` | Sử dụng để hiển thị thông tin phân cấp hoặc mối quan hệ báo cáo trong một tổ chức, kèm ảnh tương ứng. Hình trợ giúp và bố cục treo Org Chart có sẵn với bố cục này. |

### Xem thêm

* không gian tên [Aspose.Slides.SmartArt](../../aspose.slides.smartart)
* tập hợp [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->