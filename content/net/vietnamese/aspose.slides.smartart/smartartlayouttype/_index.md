---
title: SmartArtLayoutType
second_title: Aspose.Sildes cho Tham chiếu API .NET
description: Mô tả loại bố cục của một sơ đồ SmartArt.
type: docs
weight: 10620
url: /vi/aspose.slides.smartart/smartartlayouttype/
---
## SmartArtLayoutType liệt kê

Mô tả loại bố trí của một sơ đồ SmartArt.

```csharp
public enum SmartArtLayoutType
```

### Giá trị

| Tên | Giá trị | Mô tả |
| --- | --- | --- |
| AccentProcess | `0` | Dùng để hiển thị một tiến trình, một dòng thời gian, hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. Thích hợp cho việc minh họa cả văn bản Cấp 1 và Cấp 2. |
| AccentedPicture | `1` | Dùng để hiển thị một ý tưởng trung tâm dạng ảnh với các ý tưởng liên quan ở hai bên. Văn bản Cấp 1 ở trên xuất hiện phía trên hình ảnh trung tâm. Văn bản tương ứng cho các hình dạng Cấp 1 khác xuất hiện bên cạnh các hình ảnh tròn nhỏ. Bố cục này cũng hoạt động tốt khi không có văn bản. |
| AlternatingFlow | `2` | Dùng để hiển thị các nhóm thông tin hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. Nhấn mạnh sự tương tác hoặc mối quan hệ giữa các nhóm thông tin. |
| AlternatingHexagons | `3` | Dùng để biểu diễn một chuỗi các ý tưởng có liên kết với nhau. Văn bản Cấp 1 xuất hiện bên trong các hình lục giác. Văn bản Cấp 2 xuất hiện bên ngoài các hình dạng. |
| AlternatingPictureBlocks | `4` | Dùng để hiển thị một chuỗi hình ảnh từ trên xuống dưới. Văn bản xuất hiện xen kẽ ở bên phải hoặc bên trái của hình ảnh. |
| AlternatingPictureCircles | `5` | Dùng để hiển thị một tập hợp hình ảnh có kèm văn bản. Văn bản tương ứng xuất hiện trong các vòng tròn trung tâm với các hình ảnh xen kẽ từ trái sang phải. |
| ArrowRibbon | `6` | Dùng để hiển thị các khái niệm liên quan hoặc tương phản có một số kết nối, như các lực đối nghịch. Hai dòng đầu của văn bản Cấp 1 được dùng cho văn bản trong các mũi tên. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| AscendingPictureAccentProcess | `7` | Dùng để hiển thị một chuỗi hình ảnh tăng dần kèm văn bản mô tả. Hoạt động tốt nhất với một lượng văn bản nhỏ. |
| Balance | `8` | Dùng để so sánh hoặc hiển thị mối quan hệ giữa hai ý tưởng. Hai dòng đầu của văn bản Cấp 1 tương ứng với văn bản ở phía trên của mỗi bên điểm trung tâm. Nhấn mạnh văn bản Cấp 2, giới hạn ở bốn hình dạng ở mỗi bên của điểm trung tâm. Cân bằng nghiêng về phía có nhiều hình dạng chứa văn bản Cấp 2 nhất. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| BasicBendingProcess | `9` | Dùng để hiển thị một tiến trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. Tối đa hoá không gian hiển thị ngang và dọc cho các hình dạng. |
| BasicBlockList | `10` | Dùng để hiển thị các khối thông tin không tuần tự hoặc được nhóm lại. Tối đa hoá không gian hiển thị ngang và dọc cho các hình dạng. |
| BasicChevronProcess | `11` | Dùng để hiển thị một tiến trình; một dòng thời gian; các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc; hoặc để nhấn mạnh chuyển động hoặc hướng đi. Văn bản Cấp 1 xuất hiện bên trong hình mũi tên trong khi văn bản Cấp 2 xuất hiện dưới các hình mũi tên. |
| BasicCycle | `12` | Dùng để biểu diễn một chuỗi liên tục các giai đoạn, nhiệm vụ hoặc sự kiện trong một luồng vòng tròn. Nhấn mạnh các giai đoạn hoặc bước thay vì các mũi tên hoặc luồng kết nối. Hoạt động tốt nhất chỉ với văn bản Cấp 1. |
| BasicMatrix | `13` | Dùng để hiển thị mối quan hệ của các thành phần đối với tổng thể trong các phần tư. Bốn dòng đầu của văn bản Cấp 1 xuất hiện trong các phần tư. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| BasicPie | `14` | Dùng để hiển thị cách các phần riêng lẻ tạo thành một tổng thể. Bảy dòng đầu của văn bản Cấp 1 tương ứng với các miền hoặc hình bánh chia đều. Hình dạng văn bản Cấp 1 phía trên xuất hiện bên ngoài phần còn lại của bánh để nhấn mạnh. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| BasicProcess | `15` | Dùng để hiển thị một tiến trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. |
| BasicPyramid | `16` | Dùng để hiển thị các mối quan hệ tỷ lệ, có liên kết hoặc phân cấp với thành phần lớn nhất ở dưới cùng và thu hẹp lên trên. Văn bản Cấp 1 xuất hiện trong các phần của kim tự tháp và văn bản Cấp 2 xuất hiện trong các hình dạng bên cạnh mỗi phần. |
| BasicRadial | `17` | Dùng để hiển thị mối quan hệ với một ý tưởng trung tâm trong một vòng. Dòng đầu của văn bản Cấp 1 tương ứng với hình dạng trung tâm, và văn bản Cấp 2 của nó tương ứng với các hình dạng tròn bao quanh. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| BasicTarget | `18` | Dùng để hiển thị các quan hệ chứa, thang độ hoặc phân cấp. Năm dòng đầu của văn bản Cấp 1 được gắn với một vòng tròn. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| BasicTimeline | `19` | Dùng để hiển thị các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc, hoặc để hiển thị thông tin dòng thời gian. Thích hợp cho cả văn bản Cấp 1 và Cấp 2. |
| BasicVenn | `20` | Dùng để hiển thị các mối quan hệ chồng lấn hoặc có liên kết. Bảy dòng đầu của văn bản Cấp 1 tương ứng với một vòng tròn. Nếu có bốn dòng hoặc ít hơn của văn bản Cấp 1, văn bản nằm bên trong các vòng tròn. Nếu có hơn bốn dòng của văn bản Cấp 1, văn bản nằm bên ngoài các vòng tròn. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| BendingPictureAccentList | `21` | Dùng để hiển thị các khối thông tin không tuần tự hoặc được nhóm lại. Các hình tròn nhỏ được thiết kế để chứa hình ảnh. Thích hợp cho việc minh họa cả văn bản Cấp 1 và Cấp 2. Tối đa hoá không gian hiển thị ngang và dọc cho các hình dạng. |
| BendingPictureBlocks | `22` | Dùng để hiển thị một chuỗi hình ảnh. Hộp bao phủ góc dưới có thể chứa một lượng văn bản nhỏ. |
| BendingPictureCaption | `23` | Dùng để hiển thị một chuỗi hình ảnh tuần tự. Hộp bao phủ góc dưới có thể chứa một lượng văn bản nhỏ. |
| BendingPictureCaptionList | `24` | Dùng để hiển thị một chuỗi hình ảnh. Tiêu đề và mô tả xuất hiện trong một hình gọi chú thích dưới mỗi hình ảnh. |
| BendingPictureSemiTransparentText | `25` | Dùng để hiển thị một chuỗi hình ảnh. Một hộp nửa trong suốt phủ phần dưới của hình ảnh và chứa tất cả các cấp văn bản. |
| BlockCycle | `26` | Dùng để biểu diễn một chuỗi liên tục các giai đoạn, nhiệm vụ hoặc sự kiện trong một luồng vòng tròn. Nhấn mạnh các giai đoạn hoặc bước thay vì các mũi tên hoặc luồng kết nối. |
| BubblePictureList | `27` | Dùng để hiển thị một chuỗi hình ảnh. Có thể chứa tới tám hình ảnh Cấp 1. Văn bản và hình ảnh không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. Thích hợp nhất với một lượng văn bản nhỏ. |
| CaptionedPictures | `28` | Dùng để hiển thị hình ảnh với nhiều cấp văn bản. Thích hợp nhất với một lượng văn bản Cấp 1 nhỏ và một lượng văn bản Cấp 2 trung bình. |
| ChevronList | `29` | Dùng để hiển thị một tiến trình qua nhiều quy trình tạo nên một luồng công việc tổng thể. Cũng hoạt động cho việc minh họa các quy trình tương phản. Văn bản Cấp 1 tương ứng với hình mũi tên đầu tiên bên trái, trong khi văn bản Cấp 2 tương ứng với các bước phụ ngang cho mỗi hình có văn bản Cấp 1. |
| CircleAccentTimeline | `30` | Dùng để hiển thị một chuỗi sự kiện hoặc thông tin dòng thời gian. Văn bản Cấp 1 xuất hiện bên cạnh các hình tròn lớn hơn. Văn bản Cấp 2 xuất hiện bên cạnh các hình tròn nhỏ hơn. |
| CircleArrowProcess | `31` | Dùng để hiển thị các mục tuần tự có văn bản hỗ trợ cho mỗi mục. Sơ đồ này hoạt động tốt nhất với một lượng văn bản Cấp 1 nhỏ. |
| CirclePictureHierarchy | `32` | Dùng để hiển thị thông tin phân cấp hoặc mối quan hệ báo cáo trong một tổ chức. Hình ảnh xuất hiện trong các vòng tròn và văn bản tương ứng xuất hiện bên cạnh các hình ảnh. |
| CircleRelationship | `33` | Dùng để hiển thị mối quan hệ tới hoặc từ một ý tưởng trung tâm. Văn bản Cấp 2 được thêm không tuần tự và giới hạn ở năm mục. Chỉ có thể có một mục Cấp 1. |
| CircularBendingProcess | `34` | Dùng để hiển thị một chuỗi dài hoặc phi tuyến tính các bước trong một nhiệm vụ, quy trình hoặc luồng công việc. Hoạt động tốt nhất chỉ với văn bản Cấp 1. Tối đa hoá không gian hiển thị ngang và dọc cho các hình dạng. |
| CircularPictureCallout | `35` | Dùng để hiển thị một ý tưởng trung tâm và các ý tưởng phụ hoặc các mục liên quan. Văn bản cho hình ảnh đầu tiên phủ phần dưới của hình ảnh. Văn bản tương ứng cho các hình dạng Cấp 1 khác xuất hiện bên cạnh các hình ảnh tròn nhỏ. Sơ đồ này cũng hoạt động tốt khi không có văn bản. |
| ClosedChevronProcess | `36` | Dùng để hiển thị một tiến trình, dòng thời gian, hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc, hoặc để nhấn mạnh chuyển động hoặc hướng đi. Có thể được dùng để nhấn mạnh thông tin trong hình dạng bắt đầu. Hoạt động tốt nhất chỉ với văn bản Cấp 1. |
| ContinuousArrowProcess | `37` | Dùng để hiển thị một dòng thời gian hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. Hoạt động tốt nhất với văn bản Cấp 1 vì mỗi dòng văn bản Cấp 1 xuất hiện bên trong hình mũi tên. Văn bản Cấp 2 xuất hiện bên ngoài hình mũi tên. |
| ContinuousBlockProcess | `38` | Dùng để hiển thị một tiến trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. Hoạt động tốt nhất với văn bản Cấp 1 và Cấp 2 tối thiểu. |
| ContinuousCycle | `39` | Dùng để biểu diễn một chuỗi liên tục các giai đoạn, nhiệm vụ hoặc sự kiện trong một luồng vòng tròn. Nhấn mạnh kết nối giữa tất cả các thành phần. Hoạt động tốt nhất chỉ với văn bản Cấp 1. |
| ContinuousPictureList | `40` | Dùng để hiển thị các nhóm thông tin có liên kết. Các hình tròn được thiết kế để chứa hình ảnh. |
| ConvergingArrows | `41` | Dùng để hiển thị các ý tưởng hoặc khái niệm hội tụ về một điểm trung tâm. Hoạt động tốt nhất chỉ với văn bản Cấp 1. |
| ConvergingRadial | `42` | Dùng để hiển thị mối quan hệ của các khái niệm hoặc thành phần tới một ý tưởng trung tâm trong một vòng. Dòng đầu của văn bản Cấp 1 tương ứng với hình tròn trung tâm và các dòng văn bản Cấp 2 tương ứng với các hình chữ nhật bao quanh. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| CounterbalanceArrows | `43` | Dùng để hiển thị hai ý tưởng hoặc khái niệm đối lập. Hai dòng đầu của văn bản Cấp 1 tương ứng với một mũi tên và hoạt động tốt với văn bản Cấp 2. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| CycleMatrix | `44` | Dùng để hiển thị mối quan hệ tới một ý tưởng trung tâm trong một tiến trình tuần hoàn. Bốn dòng đầu của văn bản Cấp 1 tương ứng với một miền hoặc hình bánh, và văn bản Cấp 2 xuất hiện trong một hình chữ nhật bên cạnh miền hoặc hình bánh. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| DescendingBlockList | `45` | Dùng để hiển thị các nhóm ý tưởng liên quan hoặc danh sách thông tin. Các hình dạng văn bản giảm dần chiều cao theo thứ tự, và văn bản Cấp 1 hiển thị theo chiều dọc. |
| DescendingProcess | `46` | Dùng để hiển thị một chuỗi sự kiện giảm dần. Văn bản Cấp 1 đầu tiên nằm ở đầu mũi tên, và văn bản Cấp 1 cuối cùng hiển thị ở đáy mũi tên. Chỉ hiển thị bảy mục Cấp 1 đầu tiên. Thích hợp cho một lượng văn bản vừa và nhỏ. |
| DetailedProcess | `47` | Dùng với một lượng lớn văn bản Cấp 2 để hiển thị một tiến trình qua các giai đoạn. |
| DivergingArrows | `48` | Dùng để hiển thị các ý tưởng hoặc khái niệm tiến ra ngoài từ một nguồn trung tâm. Hoạt động tốt nhất chỉ với văn bản Cấp 1. |
| DivergingRadial | `49` | Dùng để hiển thị mối quan hệ tới một ý tưởng trung tâm trong một vòng. Dòng đầu của văn bản Cấp 1 tương ứng với hình tròn trung tâm. Nhấn mạnh các vòng tròn xung quanh hơn là ý tưởng trung tâm. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| Equation | `50` | Dùng để hiển thị các bước hoặc nhiệm vụ tuần tự mô tả một kế hoạch hoặc kết quả. Dòng cuối của văn bản Cấp 1 xuất hiện sau dấu bằng (=). Hoạt động tốt nhất chỉ với văn bản Cấp 1. |
| FramedTextPicture | `51` | Dùng để hiển thị hình ảnh với văn bản Cấp 1 tương ứng được hiển thị trong một khung. |
| Funnel | `52` | Dùng để hiển thị việc lọc thông tin hoặc cách các phần hợp lại thành một tổng thể. Nhấn mạnh kết quả cuối cùng. Có thể chứa tới bốn dòng văn bản Cấp 1; dòng cuối trong bốn dòng này xuất hiện dưới phễu và các dòng còn lại tương ứng với một hình tròn. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| Gear | `53` | Dùng để hiển thị các ý tưởng khớp nhau. Ba dòng đầu của văn bản Cấp 1 tương ứng với hình bánh răng, và văn bản Cấp 2 tương ứng xuất hiện trong các hình chữ nhật bên cạnh hình bánh răng. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| GridMatrix | `54` | Dùng để hiển thị vị trí của các khái niệm dọc hai trục. Nhấn mạnh các thành phần riêng lẻ hơn là tổng thể. Bốn dòng đầu của văn bản Cấp 1 xuất hiện trong các phần tư. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| GroupedList | `55` | Dùng để hiển thị các nhóm và nhóm con của thông tin, hoặc các bước và bước phụ trong một nhiệm vụ, quy trình hoặc luồng công việc. Văn bản Cấp 1 tương ứng với các hình dạng ngang ở cấp cao nhất, và văn bản Cấp 2 tương ứng với các bước phụ dọc dưới mỗi hình dạng cấp cao nhất liên quan. Thích hợp để nhấn mạnh các nhóm con hoặc bước phụ, thông tin phân cấp, hoặc nhiều danh sách thông tin. |
| HalfCircleOrganizationChart | `56` | Dùng để hiển thị thông tin phân cấp hoặc mối quan hệ báo cáo trong một tổ chức. Các hình dạng trợ giúp và bố cục treo Org Chart có sẵn với bố cục này. |
| HexagonCluster | `57` | Dùng để hiển thị hình ảnh kèm văn bản mô tả liên quan. Các hình lục giác nhỏ chỉ ra cặp hình ảnh và văn bản. Thích hợp với một lượng văn bản nhỏ. |
| Hierarchy | `58` | Dùng để hiển thị các mối quan hệ phân cấp tiến từ trên xuống dưới. |
| HierarchyList | `59` | Dùng để hiển thị các mối quan hệ phân cấp tiến qua các nhóm. Cũng có thể được dùng để nhóm hoặc liệt kê thông tin. |
| HorizontalBulletList | `60` | Dùng để hiển thị danh sách thông tin không tuần tự hoặc được nhóm lại. Thích hợp với một lượng lớn văn bản. Tất cả văn bản có cùng mức độ nhấn mạnh và không ngụ ý hướng. |
| HorizontalHierarchy | `61` | Dùng để hiển thị các mối quan hệ phân cấp tiến theo chiều ngang. Thích hợp cho cây quyết định. |
| HorizontalLabeledHierarchy | `62` | Dùng để hiển thị các mối quan hệ phân cấp tiến theo chiều ngang và được nhóm theo phân cấp. Nhấn mạnh tiêu đề hoặc văn bản cấp 1. Dòng đầu của văn bản Cấp 1 xuất hiện trong hình dạng ở đầu phân cấp, và dòng thứ hai và các dòng tiếp theo của văn bản Cấp 1 xuất hiện ở trên các hình chữ nhật cao. |
| HorizontalMultiLevelHierarchy | `63` | Dùng để hiển thị một lượng lớn thông tin phân cấp tiến theo chiều ngang. Đỉnh của phân cấp được hiển thị theo chiều dọc. Bố cục này hỗ trợ nhiều cấp trong phân cấp. |
| HorizontalOrganizationChart | `64` | Dùng để hiển thị thông tin phân cấp theo chiều ngang hoặc mối quan hệ báo cáo trong một tổ chức. Các hình dạng trợ giúp và bố cục treo Org Chart có sẵn với bố cục này. |
| HorizontalPictureList | `65` | Dùng để hiển thị thông tin không tuần tự hoặc được nhóm lại với nhấn mạnh vào các hình ảnh liên quan. Các hình dạng trên cùng được thiết kế để chứa hình ảnh. |
| IncreasingArrowsProcess | `66` | Dùng để hiển thị các bước tuần tự và chồng lên nhau trong một quy trình. Giới hạn tới năm mục Cấp 1. Cấp 2 có thể chứa một lượng lớn văn bản. |
| IncreasingCircleProcess | `67` | Dùng để hiển thị một chuỗi bước, với phần trong của vòng tròn tăng lên theo mỗi bước. Giới hạn tới bảy bước Cấp 1 nhưng không giới hạn số mục Cấp 2. Thích hợp với một lượng lớn văn bản Cấp 2. |
| InvertedPyramid | `68` | Dùng để hiển thị các mối quan hệ tỷ lệ, có liên kết hoặc phân cấp với thành phần lớn nhất ở trên và thu hẹp xuống. Văn bản Cấp 1 xuất hiện trong các phần của kim tự tháp và văn bản Cấp 2 xuất hiện trong các hình dạng bên cạnh mỗi phần. |
| LabeledHierarchy | `69` | Dùng để hiển thị các mối quan hệ phân cấp tiến từ trên xuống dưới và được nhóm theo phân cấp. Nhấn mạnh tiêu đề hoặc văn bản cấp 1. Dòng đầu của văn bản Cấp 1 xuất hiện trong hình dạng ở đầu phân cấp, và tất cả các dòng tiếp theo của văn bản Cấp 1 xuất hiện ở bên trái các hình chữ nhật dài. |
| LinearVenn | `70` | Dùng để hiển thị các mối quan hệ chồng lấn trong một chuỗi. Hoạt động tốt nhất chỉ với văn bản Cấp 1. |
| LinedList | `71` | Dùng để hiển thị một lượng lớn văn bản được chia thành các danh mục và tiểu mục. Thích hợp với nhiều cấp văn bản. Văn bản cùng cấp được ngăn cách bằng các đường kẻ. |
| MultidirectionalCycle | `72` | Dùng để biểu diễn một chuỗi liên tục các giai đoạn, nhiệm vụ hoặc sự kiện có thể xảy ra theo bất kỳ hướng nào. |
| NameandTitleOrganizationChart | `73` | Dùng để hiển thị thông tin phân cấp hoặc mối quan hệ báo cáo trong một tổ chức. Để nhập văn bản vào hộp tiêu đề, gõ trực tiếp trong hình chữ nhật nhỏ hơn. Các hình dạng trợ giúp và bố cục treo Org Chart có sẵn với bố cục này. |
| NestedTarget | `74` | Dùng để hiển thị các quan hệ chứa. Ba dòng đầu của văn bản Cấp 1 tương ứng với văn bản trên trái trong các hình dạng, và văn bản Cấp 2 tương ứng với các hình dạng nhỏ hơn. Thích hợp với số dòng Văn bản Cấp 2 tối thiểu. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| NondirectionalCycle | `75` | Dùng để biểu diễn một chuỗi liên tục các giai đoạn, nhiệm vụ hoặc sự kiện trong một luồng vòng tròn. Mỗi hình dạng có cùng mức độ quan trọng. Thích hợp khi không cần chỉ ra hướng. |
| OpposingArrows | `76` | Dùng để hiển thị hai ý tưởng hoặc khái niệm đối lập. Hai dòng đầu của văn bản Cấp 1 tương ứng với một mũi tên và hoạt động tốt với văn bản Cấp 2. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| OpposingIdeas | `77` | Dùng để hiển thị hai ý tưởng đối lập hoặc tương phản. Có thể có một hoặc hai mục Cấp 1. Mỗi văn bản Cấp 1 có thể chứa nhiều cấp phụ. Thích hợp với một lượng lớn văn bản. |
| OrganizationChart | `78` | Dùng để hiển thị thông tin phân cấp hoặc mối quan hệ báo cáo trong một tổ chức. Các hình dạng trợ giúp và bố cục treo Org Chart có sẵn với bố cục này. |
| PhasedProcess | `79` | Dùng để hiển thị ba giai đoạn của một quy trình. Giới hạn tới ba mục Cấp 1. Hai mục Cấp 1 đầu tiên mỗi cái có thể chứa bốn mục Cấp 2, và mục Cấp 1 thứ ba có thể chứa số lượng không giới hạn các mục Cấp 2. Thích hợp nhất với một lượng văn bản nhỏ. |
| PictureAccentBlocks | `80` | Dùng để hiển thị một nhóm hình ảnh trong các khối bắt đầu từ góc. Văn bản tương ứng hiển thị theo chiều dọc. Thích hợp làm điểm nhấn trên các slide tiêu đề hoặc phụ đề hoặc cho các phần tách của tài liệu. |
| PictureAccentList | `81` | Dùng để hiển thị thông tin được nhóm hoặc có liên quan. Các hình dạng nhỏ ở các góc trên được thiết kế để chứa hình ảnh. Nhấn mạnh văn bản Cấp 2 hơn văn bản Cấp 1, và là lựa chọn tốt cho một lượng lớn văn bản Cấp 2. |
| PictureAccentProcess | `82` | Dùng để hiển thị các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. Các hình chữ nhật nền được thiết kế để chứa hình ảnh. |
| PictureCaptionList | `83` | Dùng để hiển thị các khối thông tin không tuần tự hoặc được nhóm lại. Các hình dạng trên cùng được thiết kế để chứa hình ảnh và hình ảnh được nhấn mạnh hơn văn bản. Thích hợp cho hình ảnh có chú thích văn bản ngắn. |
| PictureGrid | `84` | Dùng để hiển thị hình ảnh được sắp xếp trên lưới vuông. Tốt nhất với một lượng văn bản Cấp 1 nhỏ, xuất hiện phía trên hình ảnh. |
| PictureLineup | `85` | Dùng để hiển thị một chuỗi hình ảnh đặt cạnh nhau. Văn bản Cấp 1 phủ phần trên của hình ảnh. Văn bản Cấp 2 xuất hiện dưới hình ảnh. |
| PictureStrips | `86` | Dùng để hiển thị một chuỗi hình ảnh từ trên xuống dưới với văn bản Cấp 1 bên cạnh từng hình ảnh. |
| PieProcess | `87` | Dùng để hiển thị các bước trong một quy trình với mỗi miếng bánh tăng kích thước lên tới bảy hình. Văn bản Cấp 1 hiển thị theo chiều dọc. |
| PlusandMinus | `88` | Dùng để hiển thị ưu và nhược điểm của hai ý tưởng. Mỗi văn bản Cấp 1 có thể chứa nhiều cấp phụ. Thích hợp với một lượng lớn văn bản. Giới hạn tới hai mục Cấp 1. |
| ProcessArrows | `89` | Dùng để hiển thị thông tin minh họa một quy trình hoặc luồng công việc. Văn bản Cấp 1 xuất hiện trong các hình tròn và văn bản Cấp 2 xuất hiện trong các hình mũi tên. Hoạt động tốt nhất cho văn bản tối thiểu và để nhấn mạnh chuyển động hoặc hướng đi. |
| ProcessList | `90` | Dùng để hiển thị nhiều nhóm thông tin hoặc các bước và bước phụ trong một nhiệm vụ, quy trình hoặc luồng công việc. Văn bản Cấp 1 tương ứng với các hình dạng ngang trên cùng, và văn bản Cấp 2 tương ứng với các bước phụ dọc dưới mỗi hình dạng cấp cao nhất liên quan. |
| PyramidList | `91` | Dùng để hiển thị các mối quan hệ tỷ lệ, có liên kết hoặc phân cấp. Văn bản xuất hiện trong các hình chữ nhật trên nền kim tự tháp. |
| RadialCluster | `92` | Dùng để hiển thị dữ liệu liên quan tới một ý tưởng hoặc chủ đề trung tâm. Văn bản Cấp 1 trên cùng xuất hiện ở trung tâm. Văn bản Cấp 2 xuất hiện trong các hình dạng bao quanh. Có thể chứa tới bảy hình dạng Cấp 2. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. Thích hợp nhất với một lượng văn bản nhỏ. |
| RadialCycle | `93` | Dùng để hiển thị mối quan hệ tới một ý tưởng trung tâm. Nhấn mạnh cả thông tin trong vòng tròn trung tâm và cách thông tin trong vòng tròn phía ngoài đóng góp cho ý tưởng trung tâm. Dòng đầu của văn bản Cấp 1 tương ứng với vòng tròn trung tâm, và văn bản Cấp 2 của nó tương ứng với vòng tròn phía ngoài. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| RadialList | `94` | Dùng để hiển thị các mối quan hệ tới một ý tưởng trung tâm trong một vòng. Hình dạng trung tâm có thể chứa một hình ảnh. Văn bản Cấp 1 xuất hiện trong các vòng tròn nhỏ hơn và bất kỳ văn bản Cấp 2 liên quan nào xuất hiện bên cạnh các vòng tròn nhỏ hơn. |
| RadialVenn | `95` | Dùng để hiển thị cả các mối quan hệ chồng lấn và mối quan hệ tới một ý tưởng trung tâm trong một vòng. Dòng đầu của văn bản Cấp 1 tương ứng với hình dạng trung tâm và các dòng văn bản Cấp 2 tương ứng với các hình dạng tròn bao quanh. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| RandomToResultProcess | `96` | Dùng để hiển thị, qua một chuỗi bước, cách một số ý tưởng hỗn loạn có thể dẫn đến một mục tiêu hoặc ý tưởng thống nhất. Hỗ trợ nhiều mục văn bản Cấp 1, nhưng các hình dạng tương ứng của văn bản Cấp 1 đầu và cuối là cố định. Thích hợp nhất với một lượng văn bản Cấp 1 nhỏ và một lượng văn bản Cấp 2 trung bình. |
| RepeatingBendingProcess | `97` | Dùng để hiển thị một tiến trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. Tối đa hoá không gian hiển thị ngang và dọc cho các hình dạng. |
| ReverseList | `98` | Dùng để chuyển đổi giữa hai mục. Chỉ hiển thị hai mục văn bản đầu tiên, và mỗi mục có thể chứa một lượng lớn văn bản. Thích hợp để hiển thị sự thay đổi giữa hai mục hoặc sự chuyển đổi thứ tự. |
| SegmentedCycle | `99` | Dùng để hiển thị một tiến trình hoặc một chuỗi các giai đoạn, nhiệm vụ hoặc sự kiện trong một luồng vòng tròn. Nhấn mạnh các phần kết nối với nhau. Bảy dòng đầu của văn bản Cấp 1 tương ứng với một miền hoặc hình bánh. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| SegmentedProcess | `100` | Dùng để hiển thị một tiến trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. Nhấn mạnh văn bản Cấp 2, vì mỗi dòng xuất hiện trong một hình dạng riêng. |
| SegmentedPyramid | `101` | Dùng để hiển thị các quan hệ chứa, tỷ lệ hoặc có liên kết. Chín dòng đầu của văn bản Cấp 1 xuất hiện trong các hình tam giác. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. Hoạt động tốt nhất chỉ với văn bản Cấp 1. |
| SnapshotPictureList | `102` | Dùng để hiển thị hình ảnh kèm văn bản giải thích. Văn bản Cấp 2 có thể hiển thị danh sách thông tin. Thích hợp với một lượng lớn văn bản. |
| SpiralPicture | `103` | Dùng để hiển thị một chuỗi tối đa năm hình ảnh kèm chú thích Cấp 1 xoắn vào trung tâm. |
| SquareAccentList | `104` | Dùng để hiển thị danh sách thông tin được chia thành các danh mục. Văn bản Cấp 2 xuất hiện bên cạnh một hình vuông nhỏ. Thích hợp với một lượng lớn văn bản Cấp 2. |
| StackedList | `105` | Dùng để hiển thị các nhóm thông tin hoặc các bước trong một nhiệm vụ, quy trình hoặc luồng công việc. Các hình tròn chứa văn bản Cấp 1, và các hình chữ nhật tương ứng chứa văn bản Cấp 2. Thích hợp cho nhiều chi tiết và văn bản Cấp 1 tối thiểu. |
| StackedVenn | `106` | Dùng để hiển thị các mối quan hệ chồng lấn. Lựa chọn tốt để nhấn mạnh sự tăng trưởng hoặc cấp độ. Hoạt động tốt nhất chỉ với văn bản Cấp 1. Bảy dòng đầu của văn bản Cấp 1 tương ứng với một hình tròn. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| StaggeredProcess | `107` | Dùng để hiển thị một tiến trình đi xuống qua các giai đoạn. Năm dòng đầu của văn bản Cấp 1 tương ứng với một hình chữ nhật. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| StepDownProcess | `108` | Dùng để hiển thị một quy trình giảm dần với nhiều bước và bước phụ. Thích hợp nhất với một lượng văn bản nhỏ. |
| StepUpProcess | `109` | Dùng để hiển thị một chuỗi bước tăng dần hoặc danh sách thông tin. |
| SubStepProcess | `110` | Dùng để hiển thị một quy trình đa bước với các bước phụ giữa mỗi lần xuất hiện của văn bản Cấp 1. Thích hợp nhất với một lượng văn bản nhỏ và giới hạn tới bảy bước Cấp 1. Mỗi bước Cấp 1 có thể có số lượng không giới hạn các bước phụ. |
| TableHierarchy | `111` | Dùng để hiển thị các nhóm thông tin được xây dựng từ trên xuống dưới, và các cấp phân cấp trong mỗi nhóm. Bố cục này không chứa các đường nối. |
| TableList | `112` | Dùng để hiển thị thông tin được nhóm hoặc liên quan với giá trị bằng nhau. Dòng đầu của văn bản Cấp 1 tương ứng với hình dạng trên cùng và văn bản Cấp 2 của nó được dùng cho các danh sách tiếp theo. |
| TargetList | `113` | Dùng để hiển thị thông tin liên quan hoặc chồng lấn. Bảy dòng đầu của văn bản Cấp 1 xuất hiện trong hình chữ nhật. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. Thích hợp cho cả văn bản Cấp 1 và Cấp 2. |
| TextCycle | `114` | Dùng để biểu diễn một chuỗi liên tục các giai đoạn, nhiệm vụ hoặc sự kiện trong một luồng vòng tròn. Nhấn mạnh các mũi tên hoặc luồng hơn là các giai đoạn hoặc bước. Hoạt động tốt nhất chỉ với văn bản Cấp 1. |
| TitlePictureLineup | `115` | Dùng để hiển thị một chuỗi hình ảnh, mỗi hình có tiêu đề và mô tả riêng. Văn bản Cấp 1 xuất hiện trong hộp phía trên hình ảnh. Văn bản Cấp 2 xuất hiện dưới hình ảnh. |
| TitledMatrix | `116` | Dùng để hiển thị mối quan hệ của bốn phần tư với một tổng thể. Dòng đầu của văn bản Cấp 1 tương ứng với hình dạng trung tâm, và bốn dòng đầu của văn bản Cấp 2 xuất hiện trong các phần tư. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| TitledPictureAccentList | `117` | Dùng để hiển thị danh sách thông tin với một hình ảnh nhấn mạnh cho mỗi văn bản Cấp 2. Văn bản Cấp 1 hiển thị trong một hộp riêng ở đầu danh sách. |
| TitledPictureBlocks | `118` | Dùng để hiển thị một chuỗi hình ảnh. Văn bản Cấp 1 xuất hiện phía trên mỗi hình ảnh. Văn bản Cấp 2 xuất hiện ở bên cạnh và hơi chồng lên mỗi hình ảnh. |
| TrapezoidList | `119` | Dùng để hiển thị thông tin được nhóm hoặc liên quan có giá trị bằng nhau. Thích hợp với một lượng lớn văn bản. |
| UpwardArrow | `120` | Dùng để hiển thị một tiến trình hoặc các bước có xu hướng tăng lên trong một nhiệm vụ, quy trình hoặc luồng công việc. Năm dòng đầu của văn bản Cấp 1 tương ứng với một điểm trên mũi tên. Thích hợp nhất với văn bản tối thiểu. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn có sẵn nếu bạn chuyển bố cục. |
| VerticalAccentList | `121` | Dùng để hiển thị danh sách thông tin. Văn bản Cấp 2 xuất hiện trong các hình chữ nhật trên các biểu tượng chéo dọc. Nhấn mạnh văn bản Cấp 2 hơn văn bản Cấp 1, và là lựa chọn tốt cho một lượng trung bình văn bản Cấp 2. |
| VerticalArrowList | `122` | Dùng để hiển thị một tiến trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc di chuyển về phía mục tiêu chung. Thích hợp cho các danh sách dạng dấu đầu dòng. |
| VerticalBendingProcess | `123` | Dùng để hiển thị một tiến trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. Tối đa hoá không gian hiển thị ngang và dọc cho các hình dạng. Nhấn mạnh nhiều hơn vào mối quan hệ giữa các hình dạng hơn là hướng hoặc chuyển động. |
| VerticalBlockList | `124` | Dùng để hiển thị các nhóm thông tin hoặc các bước trong một nhiệm vụ, quy trình hoặc luồng công việc. Thích hợp với một lượng lớn văn bản Cấp 2. Lựa chọn tốt cho văn bản có một điểm chính và nhiều điểm phụ. |
| VerticalBoxList | `125` | Dùng để hiển thị một số nhóm thông tin, đặc biệt là các nhóm có một lượng lớn văn bản Cấp 2. Lựa chọn tốt cho các danh sách dạng dấu đầu dòng. |
| VerticalBulletList | `126` | Dùng để hiển thị các khối thông tin không tuần tự hoặc được nhóm lại. Thích hợp cho các danh sách có tiêu đề dài hoặc thông tin cấp cao. |
| VerticalChevronList | `127` | Dùng để hiển thị một tiến trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc, hoặc để nhấn mạnh chuyển động hoặc hướng đi. Nhấn mạnh văn bản Cấp 2 hơn văn bản Cấp 1, và là lựa chọn tốt cho một lượng lớn văn bản Cấp 2. |
| VerticalCircleList | `128` | Dùng để hiển thị dữ liệu tuần tự hoặc được nhóm lại. Thích hợp nhất cho văn bản Cấp 1, hiển thị bên cạnh một hình tròn lớn. Các cấp văn bản thấp hơn được ngăn cách bằng các hình tròn nhỏ hơn. |
| VerticalCurvedList | `129` | Dùng để hiển thị một danh sách thông tin cong. Để thêm hình ảnh vào các hình tròn nhấn mạnh, áp dụng đầy hình ảnh. |
| VerticalEquation | `130` | Dùng để hiển thị các bước hoặc nhiệm vụ tuần tự mô tả một kế hoạch hoặc kết quả. Dòng cuối của văn bản Cấp 1 xuất hiện sau mũi tên. Hoạt động tốt nhất chỉ với văn bản Cấp 1. |
| VerticalPictureAccentList | `131` | Dùng để hiển thị các khối thông tin không tuần tự hoặc được nhóm lại. Các vòng tròn nhỏ được thiết kế để chứa hình ảnh. |
| VerticalPictureList | `132` | Dùng để hiển thị các khối thông tin không tuần tự hoặc được nhóm lại. Các hình dạng nhỏ ở phía trái được thiết kế để chứa hình ảnh. |
| VerticalProcess | `133` | Dùng để hiển thị một tiến trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc từ trên xuống dưới. Hoạt động tốt nhất với văn bản Cấp 1, vì không gian dọc bị giới hạn. |
| Custom | `134` | Mô tả một sơ đồ SmartArt với mẫu bố cục tùy chỉnh |
| PictureOrganizationChart | `135` | Dùng để hiển thị thông tin phân cấp hoặc mối quan hệ báo cáo trong một tổ chức, với hình ảnh tương ứng. Các hình dạng trợ giúp và bố cục treo Org Chart có sẵn với bố cục này. |

### Xem thêm

* không gian tên [Aspose.Slides.SmartArt](../../aspose.slides.smartart)
* tập hợp [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->