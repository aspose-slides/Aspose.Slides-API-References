---
title: GapDepth
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Trả về hoặc đặt khoảng cách dưới dạng phần trăm độ rộng dấu đánh dấu giữa các chuỗi dữ liệu trong biểu đồ 3D. Đây là thuộc tính không chỉ của chuỗi này mà của tất cả các chuỗi trong nhóm chuỗi cha - đây là phép chiếu của thuộc tính nhóm tương ứng. Do đó, thuộc tính này chỉ đọc. Sử dụng thuộc tính ParentSeriesGroup để truy cập nhóm chuỗi cha. Sử dụng thuộc tính ParentSeriesGroup.GapDepth đọc/ghi để thay đổi giá trị. Chỉ đọc Int32.
type: docs
weight: 120
url: /vi/aspose.slides.charts/ichartseries/gapdepth/
---
## IChartSeries.GapDepth thuộc tính

Trả về hoặc đặt khoảng cách, tính bằng phần trăm độ rộng dấu đánh dấu, giữa các chuỗi dữ liệu trong biểu đồ 3D. Đây là thuộc tính không chỉ của chuỗi này mà của tất cả các chuỗi trong nhóm chuỗi cha - đây là phép chiếu của thuộc tính nhóm tương ứng. Do đó thuộc tính này chỉ đọc. Sử dụng thuộc tính ParentSeriesGroup để truy cập nhóm chuỗi cha. Sử dụng thuộc tính ParentSeriesGroup.GapDepth đọc/ghi để thay đổi giá trị. Chỉ đọc Int32.

```csharp
public int GapDepth { get; }
```

### Ghi chú

Đây là phép chiếu của thuộc tính ParentSeriesGroup.GapDepth.

### Xem thêm

* interface [IChartSeries](../../ichartseries)
* namespace [Aspose.Slides.Charts](../../ichartseries)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->