---
title: HasSeriesLines
second_title: Aspose.Sildes cho Tham khảo API .NET
description: Xác định xem có đường chuỗi cho chuỗi này và các chuỗi cùng loại hay không. Đây là thuộc tính không chỉ của chuỗi này mà còn của tất cả các chuỗi trong nhóm chuỗi cha - đây là phép chiếu của thuộc tính nhóm tương ứng. Do đó thuộc tính này chỉ đọc. Sử dụng thuộc tính ParentSeriesGroup để truy cập nhóm chuỗi cha. Sử dụng thuộc tính ParentSeriesGroup.HasSeriesLines đọc/ghi để thay đổi giá trị. Sử dụng thuộc tính ParentSeriesGroup.SeriesLinesFormat để định dạng các đường chuỗi. Boolean chỉ đọc.
type: docs
weight: 140
url: /vi/aspose.slides.charts/ichartseries/hasserieslines/
---
## IChartSeries.HasSeriesLines thuộc tính

Xác định liệu có các đường chuỗi cho chuỗi này và các chuỗi cùng loại hay không. Đây là thuộc tính không chỉ của chuỗi này mà còn của tất cả các chuỗi trong nhóm chuỗi cha — đây là phép chiếu của thuộc tính nhóm thích hợp. Do đó thuộc tính này chỉ đọc. Sử dụng thuộc tính ParentSeriesGroup để truy cập nhóm chuỗi cha. Sử dụng thuộc tính ParentSeriesGroup.HasSeriesLines đọc/ghi để thay đổi giá trị. Sử dụng thuộc tính ParentSeriesGroup.SeriesLinesFormat để định dạng các đường chuỗi. Boolean chỉ đọc.

```csharp
public bool HasSeriesLines { get; }
```

### Ghi chú

Đây là phép chiếu của thuộc tính ParentSeriesGroup.HasSeriesLines.

### Xem thêm

* giao diện [IChartSeries](../../ichartseries)
* không gian tên [Aspose.Slides.Charts](../../ichartseries)
* tập hợp [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->