---
title: GapWidth
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Xác định khoảng cách giữa các cụm thanh hoặc cột, tính bằng phần trăm của độ rộng thanh hoặc cột. Thuộc tính này không chỉ áp dụng cho chuỗi này mà cho tất cả các chuỗi của nhóm chuỗi cha - đây là phép chiếu của thuộc tính nhóm tương ứng. Do đó, thuộc tính này chỉ đọc. Sử dụng thuộc tính ParentSeriesGroup để truy cập nhóm chuỗi cha. Sử dụng thuộc tính ParentSeriesGroup.GapWidth đọc/ghi để thay đổi giá trị. Int32 chỉ đọc.
type: docs
weight: 130
url: /vi/aspose.slides.charts/ichartseries/gapwidth/
---
## IChartSeries.GapWidth thuộc tính

Xác định khoảng cách giữa các cụm thanh hoặc cột, tính bằng phần trăm của độ rộng thanh hoặc cột. Thuộc tính này không chỉ áp dụng cho chuỗi này mà cho tất cả các chuỗi của nhóm chuỗi cha - đây là phép chiếu của thuộc tính nhóm tương ứng. Do đó, thuộc tính này chỉ đọc. Sử dụng thuộc tính ParentSeriesGroup để truy cập nhóm chuỗi cha. Sử dụng thuộc tính ParentSeriesGroup.GapWidth đọc/ghi để thay đổi giá trị. Int32 chỉ đọc.

```csharp
public int GapWidth { get; }
```

### Ghi chú

Đây là phép chiếu của thuộc tính ParentSeriesGroup.GapWidth.

### Xem thêm

* giao diện [IChartSeries](../../ichartseries)
* không gian tên [Aspose.Slides.Charts](../../ichartseries)
* tập hợp [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->