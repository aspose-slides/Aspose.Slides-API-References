---
title: DoughnutHoleSize
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Xác định kích thước của lỗ trong biểu đồ donut, có thể nằm trong khoảng từ 10% đến 90% kích thước vùng vẽ. Đây là thuộc tính không chỉ của chuỗi này mà của tất cả các chuỗi trong nhóm chuỗi cha — là phép chiếu của thuộc tính nhóm tương ứng. Do đó thuộc tính này chỉ đọc. Sử dụng thuộc tính ParentSeriesGroup để truy cập nhóm chuỗi cha. Sử dụng thuộc tính ParentSeriesGroup.DoughnutHoleSize đọc/ghi để thay đổi giá trị. Chỉ đọc Byte.
type: docs
weight: 60
url: /vi/aspose.slides.charts/ichartseries/doughnutholesize/
---
## IChartSeries.DoughnutHoleSize thuộc tính

Xác định kích thước của lỗ trong biểu đồ donut (có thể nằm trong khoảng 10 đến 90 phần trăm của kích thước khu vực vẽ). Đây không chỉ là thuộc tính của chuỗi này mà còn của tất cả các chuỗi trong nhóm chuỗi cha – đây là phép chiếu của thuộc tính nhóm phù hợp. Do đó thuộc tính này là chỉ đọc. Sử dụng thuộc tính ParentSeriesGroup để truy cập nhóm chuỗi cha. Sử dụng thuộc tính ParentSeriesGroup.DoughnutHoleSize đọc/ghi để thay đổi giá trị. Chỉ đọc Byte.

```csharp
public byte DoughnutHoleSize { get; }
```

### Ghi chú

Đây là phép chiếu của thuộc tính ParentSeriesGroup.DoughnutHoleSize.

### Xem thêm

* giao diện [IChartSeries](../../ichartseries)
* không gian tên [Aspose.Slides.Charts](../../ichartseries)
* tập hợp [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->