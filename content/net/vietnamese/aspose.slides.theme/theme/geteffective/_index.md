---
title: GetEffective
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Lấy dữ liệu chủ đề thực tế với việc kế thừa đã được áp dụng.
type: docs
weight: 50
url: /vi/aspose.slides.theme/theme/geteffective/
---
## Theme.GetEffective phương thức

Lấy dữ liệu chủ đề thực tế với việc kế thừa đã được áp dụng.

```csharp
public IThemeEffectiveData GetEffective()
```

### Giá trị trả về

Một [`IThemeEffectiveData`](../../ithemeeffectivedata).

### Ví dụ

Ví dụ này minh họa việc lấy các thuộc tính chủ đề hiệu quả.

```csharp
[C#]
using (Presentation pres = new Presentation(@"MyPresentation.pptx"))
{
	IThemeEffectiveData effectiveTheme  = pres.Slides[0].ThemeManager.OverrideTheme.GetEffective();

	Console.WriteLine("Font scheme name: " + effectiveTheme.FontScheme.Name);
	Console.WriteLine("Major latin font: " + effectiveTheme.FontScheme.Major.LatinFont.FontName);
	Console.WriteLine("Minor latin font: " + effectiveTheme.FontScheme.Minor.LatinFont.FontName);
}
```

### Xem thêm

* giao diện [IThemeEffectiveData](../../ithemeeffectivedata)
* lớp [Theme](../../theme)
* không gian tên [Aspose.Slides.Theme](../../theme)
* tập hợp [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->