---
title: PresentationPlayer
second_title: Aspose.Sildes cho Tham chiếu API .NET
description: Đại diện cho trình phát các hoạt ảnh liên kết với Presentation../aspose.slides/presentation.
type: docs
weight: 4390
url: /vi/aspose.slides.export/presentationplayer/
---
## PresentationPlayer lớp

Đại diện cho trình phát các hoạt ảnh liên kết với [`Presentation`](../../aspose.slides/presentation).

```csharp
public class PresentationPlayer : IDisposable
```

## Bộ khởi tạo

| Tên | Mô tả |
| --- | --- |
| [PresentationPlayer](presentationplayer)(PresentationAnimationsGenerator, double) | Tạo một thể hiện mới của [`PresentationPlayer`](../presentationplayer). |

## Thuộc tính

| Tên | Mô tả |
| --- | --- |
| [FrameIndex](../../aspose.slides.export/presentationplayer/frameindex) { get; } | Lấy chỉ mục khung. |

## Phương thức

| Tên | Mô tả |
| --- | --- |
| [Dispose](../../aspose.slides.export/presentationplayer/dispose)() | Giải phóng thể hiện của [`PresentationPlayer`](../presentationplayer). |

## Các thành viên khác

| Tên | Mô tả |
| --- | --- |
| delegate [FrameTickHandler](presentationplayer.frametickhandler) | Đại diện cho trình xử lý tick khung của sự kiện [`FrameTick`](./frametick). |

### Ví dụ

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(pres))
    {
        // Phát hoạt ảnh với 33 FPS
        using (var player = new PresentationPlayer(animationsGenerator, 33))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("33fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
        
        // Phát hoạt ảnh với 45 FPS
        using (var player = new PresentationPlayer(animationsGenerator, 45))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("45fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
    }
}
```

### Xem thêm

* không gian tên [Aspose.Slides.Export](../../aspose.slides.export)
* tập tin [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->