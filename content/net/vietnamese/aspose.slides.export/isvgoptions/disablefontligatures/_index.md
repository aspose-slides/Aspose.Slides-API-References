---
title: DisableFontLigatures
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Lấy hoặc đặt một giá trị cho biết liệu văn bản có được hiển thị mà không sử dụng ligatures hay không. Khi đặt thành true, ligatures sẽ bị tắt trong đầu ra đã được hiển thị. Mặc định, thuộc tính này được đặt thành false.
type: docs
weight: 40
url: /vi/aspose.slides.export/isvgoptions/disablefontligatures/
---
## ISVGOptions.DisableFontLigatures thuộc tính

Lấy hoặc đặt một giá trị cho biết liệu văn bản có được hiển thị mà không sử dụng ligatures hay không. Khi đặt thành `true`, ligatures sẽ bị tắt trong đầu ra đã được hiển thị. Mặc định, thuộc tính này được đặt thành `false`.

```csharp
public bool DisableFontLigatures { get; set; }
```

### Ví dụ

Ví dụ:

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    SVGOptions options = new SVFOptions
    {
        DisableFontLigatures = true // Vô hiệu hoá ligatures trong việc hiển thị văn bản
    };
    
    using (FileStream fileStream = new FileStream("slide-0.svg", FileMode.Create, FileAccess.Write))
    {
        pres.Slides[0].WriteAsSvg(fileStream);   
    }
}
```

### Xem Thêm

* giao diện [ISVGOptions](../../isvgoptions)
* không gian tên [Aspose.Slides.Export](../../isvgoptions)
* tập hợp [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->