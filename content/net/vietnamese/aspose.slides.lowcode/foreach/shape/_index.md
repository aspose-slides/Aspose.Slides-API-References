---
title: Shape
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Lặp lại từng Shapeaspose.slides.lowcode/foreach/shape trong Presentationaspose.slides/presentation. Shapes sẽ được lặp lại trong mọi loại slide - Slideaspose.slides.lowcode/foreach/slide MasterSlideaspose.slides.lowcode/foreach/masterslide và LayoutSlideaspose.slides.lowcode/foreach/layoutslide
type: docs
weight: 50
url: /vi/aspose.slides.lowcode/foreach/shape/
---
## Shape(Presentation, ForEachShapeCallback) {#shape_2}

Lặp lại từng `Shape` trong [`Presentation`](../../../aspose.slides/presentation). Shapes sẽ được lặp lại trong tất cả các loại slide - [`Slide`](../slide), [`MasterSlide`](../masterslide) và [`LayoutSlide`](../layoutslide)

```csharp
public static void Shape(Presentation pres, ForEachShapeCallback forEachShape)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| pres | Presentation | Presentation để lặp lại layout shapes |
| forEachShape | ForEachShapeCallback | Callback sẽ được gọi cho mỗi shape |

### Ví dụ

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Shape(pres, (shape, slide, index) => 
    {
        System.Console.WriteLine($"{shape.Name}, index: {index}");
    });
} 
```

### Xem thêm

* lớp [Presentation](../../../aspose.slides/presentation)
* delegate [ForEachShapeCallback](../../foreach.foreachshapecallback)
* lớp [ForEach](../../foreach)
* không gian tên [Aspose.Slides.LowCode](../../foreach)
* assembly [Aspose.Slides](../../../)

---

## Shape(Presentation, bool, ForEachShapeCallback) {#shape_1}

Lặp lại từng [`Shape`](../shape) trong [`Presentation`](../../../aspose.slides/presentation). Shapes sẽ được lặp lại trong tất cả các loại slide - [`Slide`](../slide), [`MasterSlide`](../masterslide), [`LayoutSlide`](../layoutslide) và [`NotesSlide`](../../../aspose.slides/notesslide) nếu cần.

```csharp
public static void Shape(Presentation pres, bool includeNotes, ForEachShapeCallback forEachShape)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| pres | Presentation | Presentation để lặp lại layout shapes |
| includeNotes | Boolean | Cờ cho biết liệu NotesSlides có nên được bao gồm trong quá trình xử lý hay không. |
| forEachShape | ForEachShapeCallback | Callback sẽ được gọi cho mỗi shape |

### Ví dụ

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Shape(pres, true, (shape, slide, index) => 
    {
        System.Console.WriteLine($"{shape.Name}, index: {index}");
    });
} 
```

### Xem thêm

* lớp [Presentation](../../../aspose.slides/presentation)
* delegate [ForEachShapeCallback](../../foreach.foreachshapecallback)
* lớp [ForEach](../../foreach)
* không gian tên [Aspose.Slides.LowCode](../../foreach)
* assembly [Aspose.Slides](../../../)

---

## Shape(BaseSlide, ForEachShapeCallback) {#shape}

Lặp lại từng [`Shape`](../shape) trong [`BaseSlide`](../../../aspose.slides/baseslide). [`BaseSlide`](../../../aspose.slides/baseslide) là kiểu cơ sở cho [`Slide`](../slide), [`MasterSlide`](../masterslide) và [`LayoutSlide`](../layoutslide)

```csharp
public static void Shape(BaseSlide baseSlide, ForEachShapeCallback forEachShape)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| baseSlide | BaseSlide | Slide để lặp lại layout shapes |
| forEachShape | ForEachShapeCallback | Callback sẽ được gọi cho mỗi shape |

### Ví dụ

```csharp
(Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Slide(pres, (slide, index) =>
    {
        ForEach.Shape(slide, (shape, baseSlide, shapeIndex) =>
        {
            System.Console.WriteLine($"{shape.Name}, index: {shapeIndex}");
        });
    });
} 
```

### Xem thêm

* lớp [BaseSlide](../../../aspose.slides/baseslide)
* delegate [ForEachShapeCallback](../../foreach.foreachshapecallback)
* lớp [ForEach](../../foreach)
* không gian tên [Aspose.Slides.LowCode](../../foreach)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->