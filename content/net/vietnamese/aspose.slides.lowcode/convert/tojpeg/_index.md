---
title: ToJpeg
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Chuyển đổi bài thuyết trình đầu vào thành một tập hợp các hình ảnh định dạng JPEG. Nếu tên tệp đầu ra được chỉ định là myPath/myFilename.jpeg, kết quả sẽ được lưu thành một tập hợp các tệp myPath/myFilename_N.jpeg, trong đó N là số slide.
type: docs
weight: 20
url: /vi/aspose.slides.lowcode/convert/tojpeg/
---
## ToJpeg(Presentation, string) {#tojpeg}

Chuyển đổi bài thuyết trình đầu vào thành một tập hợp các hình ảnh định dạng JPEG. Nếu tên tệp đầu ra được chỉ định là "myPath/myFilename.jpeg", kết quả sẽ được lưu thành một tập hợp các tệp "myPath/myFilename_N.jpeg", trong đó N là số slide.

```csharp
public static void ToJpeg(Presentation pres, string outputFileName)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| pres | Presentation | Bài thuyết trình đầu vào. |
| outputFileName | String | Tên tệp đầu ra. |

### Ngoại lệ

| Ngoại lệ | Điều kiện |
| --- | --- |
| ArgumentException |  |

### Ví dụ

```csharp
using (var pres = new Presentation("pres.pptx"))
    Convert.ToJpeg(pres, "presImage.jpeg");
```

### Xem thêm

* lớp [Presentation](../../../aspose.slides/presentation)
* lớp [Convert](../../convert)
* không gian tên [Aspose.Slides.LowCode](../../convert)
* assembly [Aspose.Slides](../../../)

---

## ToJpeg(Presentation, string, Size) {#tojpeg_2}

Chuyển đổi bài thuyết trình đầu vào thành một tập hợp các hình ảnh định dạng JPEG. Nếu tên tệp đầu ra được chỉ định là "myPath/myFilename.jpeg", kết quả sẽ được lưu thành một tập hợp các tệp "myPath/myFilename_N.jpeg", trong đó N là số slide.

```csharp
public static void ToJpeg(Presentation pres, string outputFileName, Size imageSize)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| pres | Presentation | Bài thuyết trình đầu vào |
| outputFileName | String | Tên tệp đầu ra. |
| imageSize | Size | Kích thước của mỗi hình ảnh được tạo. |

### Ngoại lệ

| Ngoại lệ | Điều kiện |
| --- | --- |
| ArgumentException |  |

### Ví dụ

```csharp
using (var pres = new Presentation("pres.pptx"))
    Convert.ToJpeg(pres, "presImage.jpeg", new Size(720, 540));
```

### Xem thêm

* lớp [Presentation](../../../aspose.slides/presentation)
* lớp [Convert](../../convert)
* không gian tên [Aspose.Slides.LowCode](../../convert)
* assembly [Aspose.Slides](../../../)

---

## ToJpeg(Presentation, string, float, IRenderingOptions) {#tojpeg_1}

Chuyển đổi bài thuyết trình đầu vào thành một tập hợp các hình ảnh định dạng JPEG. Nếu tên tệp đầu ra được chỉ định là "myPath/myFilename.jpeg", kết quả sẽ được lưu thành một tập hợp các tệp "myPath/myFilename_N.jpeg", trong đó N là số slide.

```csharp
public static void ToJpeg(Presentation pres, string outputFileName, float scale, 
    IRenderingOptions options)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| pres | Presentation | Bài thuyết trình đầu vào. |
| outputFileName | String | Tên tệp đầu ra. |
| scale | Single | Hệ số phóng đại được áp dụng cho các hình ảnh đầu ra so với kích thước slide gốc. |
| options | IRenderingOptions | Các tùy chọn render. |

### Ngoại lệ

| Ngoại lệ | Điều kiện |
| --- | --- |
| ArgumentException |  |

### Ví dụ

```csharp
IRenderingOptions options = new RenderingOptions()
{
    SlidesLayoutOptions = new NotesCommentsLayoutingOptions()
    {
        NotesPosition = NotesPositions.BottomTruncated
    }
};

using (var pres = new Presentation("pres.pptx"))
    Convert.ToJpeg(pres, "presImage.jpeg", 2f, options);
```

### Xem thêm

* lớp [Presentation](../../../aspose.slides/presentation)
* giao diện [IRenderingOptions](../../../aspose.slides.export/irenderingoptions)
* lớp [Convert](../../convert)
* không gian tên [Aspose.Slides.LowCode](../../convert)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->