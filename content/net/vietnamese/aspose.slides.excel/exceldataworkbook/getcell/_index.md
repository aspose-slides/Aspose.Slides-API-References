---
title: GetCell
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Lấy một ô từ trang tính được chỉ định bằng chỉ mục và tọa độ ô.
type: docs
weight: 20
url: /vi/aspose.slides.excel/exceldataworkbook/getcell/
---
## GetCell(int, int, int) {#getcell}

Lấy một ô từ trang tính được chỉ định bằng chỉ mục và tọa độ ô.

```csharp
public IExcelDataCell GetCell(int worksheetIndex, int row, int column)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| worksheetIndex | Int32 | Chỉ mục dựa trên 0 của trang tính. |
| row | Int32 | Chỉ mục hàng dựa trên 0 của ô. |
| column | Int32 | Chỉ mục cột dựa trên 0 của ô. |

### Giá trị trả về

Ô tại vị trí đã chỉ định.

### Ví dụ

Ví dụ:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell(1, 1, 1);
Console.WriteLine(cell.Value.ToString());
```

### Xem thêm

* giao diện [IExcelDataCell](../../iexceldatacell)
* lớp [ExcelDataWorkbook](../../exceldataworkbook)
* không gian tên [Aspose.Slides.Excel](../../exceldataworkbook)
* bộ lắp ráp [Aspose.Slides](../../../)

---

## GetCell(string, int, int) {#getcell_2}

Lấy một ô từ trang tính được chỉ định bằng tên và tọa độ ô.

```csharp
public IExcelDataCell GetCell(string worksheetName, int row, int column)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| worksheetName | String | Tên của trang tính. |
| row | Int32 | Chỉ mục hàng dựa trên 0 của ô. |
| column | Int32 | Chỉ mục cột dựa trên 0 của ô. |

### Giá trị trả về

Ô tại vị trí đã chỉ định.

### Ví dụ

Ví dụ:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell("Sheet1", 1, 1);
Console.WriteLine(cell.Value.ToString());
```

### Xem thêm

* giao diện [IExcelDataCell](../../iexceldatacell)
* lớp [ExcelDataWorkbook](../../exceldataworkbook)
* không gian tên [Aspose.Slides.Excel](../../exceldataworkbook)
* bộ lắp ráp [Aspose.Slides](../../../)

---

## GetCell(int, string) {#getcell_1}

Lấy một ô từ trang tính được chỉ định bằng chỉ mục và tên ô theo kiểu Excel (ví dụ: "B2").

```csharp
public IExcelDataCell GetCell(int worksheetIndex, string cellName)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| worksheetIndex | Int32 | Chỉ mục dựa trên 0 của trang tính. |
| cellName | String | Tham chiếu ô theo kiểu Excel (ví dụ: "A1", "C5"). |

### Giá trị trả về

Ô tại vị trí đã chỉ định.

### Ví dụ

Ví dụ:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell(1, "B2");
Console.WriteLine(cell.Value.ToString());
```

### Xem thêm

* giao diện [IExcelDataCell](../../iexceldatacell)
* lớp [ExcelDataWorkbook](../../exceldataworkbook)
* không gian tên [Aspose.Slides.Excel](../../exceldataworkbook)
* bộ lắp ráp [Aspose.Slides](../../../)

---

## GetCell(string, string) {#getcell_3}

Lấy một ô từ trang tính được chỉ định bằng tên ô theo kiểu Excel (ví dụ: "B2").

```csharp
public IExcelDataCell GetCell(string worksheetName, string cellName)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| worksheetName | String | Tên của trang tính. |
| cellName | String | Tham chiếu ô theo kiểu Excel (ví dụ: "A1", "C5"). |

### Giá trị trả về

Ô tại vị trí đã chỉ định.

### Ví dụ

Ví dụ:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell("Sheet1", "B2");
Console.WriteLine(cell.Value.ToString());
```

### Xem thêm

* giao diện [IExcelDataCell](../../iexceldatacell)
* lớp [ExcelDataWorkbook](../../exceldataworkbook)
* không gian tên [Aspose.Slides.Excel](../../exceldataworkbook)
* bộ lắp ráp [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->