---
title: GetEffective
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Lấy dữ liệu định dạng đường viền hiệu quả với kế thừa được áp dụng.
type: docs
weight: 190
url: /vi/aspose.slides/lineformat/geteffective/
---
## LineFormat.GetEffective phương thức

Lấy dữ liệu định dạng đường viền hiệu quả với kế thừa được áp dụng.

```csharp
public ILineFormatEffectiveData GetEffective()
```

### Giá trị trả về

Một [`ILineFormatEffectiveData`](../../ilineformateffectivedata).

### Ví dụ

Ví dụ này trình bày cách lấy các thuộc tính định dạng đường viền hiệu quả của shape.

```csharp
[C#]
using (Presentation pres = new Presentation(@"MyPresentation.pptx"))
{
	ILineFormatEffectiveData effectiveLineFormat = pres.Slides[0].Shapes[0].LineFormat.GetEffective();

	Console.WriteLine("Style: " + effectiveLineFormat.Style);
	Console.WriteLine("Width: " + effectiveLineFormat.Width);
	Console.WriteLine("Fill type: " + effectiveLineFormat.FillFormat.FillType);
}
```

### Xem thêm

* giao diện [ILineFormatEffectiveData](../../ilineformateffectivedata)
* lớp [LineFormat](../../lineformat)
* không gian tên [Aspose.Slides](../../lineformat)
* tập hợp [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->