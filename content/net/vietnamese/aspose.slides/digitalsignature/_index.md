---
title: DigitalSignature
second_title: Tham chiếu API .NET của Aspose.Sildes
description: Chữ ký số trong tệp đã ký.
type: docs
weight: 2770
url: /vi/aspose.slides/digitalsignature/
---
## DigitalSignature lớp

Chữ ký số trong tệp đã ký.

```csharp
public class DigitalSignature : IDigitalSignature
```

## Các hàm khởi tạo

| Tên | Mô tả |
| --- | --- |
| [DigitalSignature](digitalsignature#constructor)(X509Certificate2) | Tạo một đối tượng DigitalSignature mới với chứng chỉ được chỉ định. |
| [DigitalSignature](digitalsignature#constructor_1)(string, string) | Tạo một đối tượng DigitalSignature mới với đường dẫn tệp chứng chỉ và mật khẩu được chỉ định. |

## Thuộc tính

| Tên | Mô tả |
| --- | --- |
| [Certificate](../../aspose.slides/digitalsignature/certificate) { get; } | Đối tượng Certificate đã được sử dụng để ký tài liệu. Chỉ đọc X509Certificate2. |
| [Comments](../../aspose.slides/digitalsignature/comments) { get; set; } | Mục đích của chữ ký. Đọc/ghi String. |
| [IsValid](../../aspose.slides/digitalsignature/isvalid) { get; } | Nếu chữ ký số này hợp lệ và tài liệu không bị thay đổi, giá trị này sẽ đúng. Chỉ đọc Boolean. |
| [SignTime](../../aspose.slides/digitalsignature/signtime) { get; } | Thời gian tài liệu được ký. Chỉ đọc DateTime. |

### Ví dụ

Ví dụ sau minh họa cách thêm chữ ký số từ chứng chỉ PFX trong PowerPoint Presentation.

```csharp
[C#]
// Khởi tạo đối tượng Presentation
using (Presentation pres = new Presentation())
{
    // Tạo đối tượng DigitalSignature bằng tệp PFX và mật khẩu PFX
    DigitalSignature signature = new DigitalSignature("testsignature1.pfx", @"testpass1");
    // Ghi chú chữ ký số mới
    signature.Comments = "Aspose.Slides digital signing test.";
    // Thêm chữ ký số vào bản trình bày
    pres.DigitalSignatures.Add(signature);
    // Lưu bản trình bày
    pres.Save("SomePresentationSigned.pptx", SaveFormat.Pptx);
}
```

Mã mẫu sau minh họa cách kiểm tra tính hợp lệ của chữ ký số trong PowerPoint Presentation.

```csharp
[C#]
// Khởi tạo đối tượng Presentation
using (Presentation pres = new Presentation("SomePresentationSigned.pptx"))
{
    if (pres.DigitalSignatures.Count < 0)
    {
        bool allSignaturesAreValid = true;
        Console.WriteLine("Signatures used to sign the presentation: ");
        // Kiểm tra xem tất cả chữ ký số có hợp lệ không
        foreach (DigitalSignature signature in pres.DigitalSignatures)
        {
            Console.WriteLine(signature.Certificate.SubjectName.Name + ", "
                    + signature.SignTime.ToString("yyyy-MM-dd HH:mm") + " -- " + (signature.IsValid ? "VALID" : "INVALID"));
            allSignaturesAreValid &= signature.IsValid;
        }
        if (allSignaturesAreValid)
            Console.WriteLine("Presentation is genuine, all signatures are valid.");
        else
            Console.WriteLine("Presentation has been modified since signing.");
    }
}
```

### Xem thêm

* giao diện [IDigitalSignature](../idigitalsignature)
* không gian tên [Aspose.Slides](../../aspose.slides)
* assembly [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->