---
title: GetEffective
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Lấy dữ liệu định dạng phần hiệu quả với kế thừa đã được áp dụng.
type: docs
weight: 70
url: /vi/aspose.slides/portionformat/geteffective/
---
## PortionFormat.GetEffective phương thức

Lấy dữ liệu định dạng phần hiệu quả với kế thừa đã được áp dụng.

```csharp
public IPortionFormatEffectiveData GetEffective()
```

### Giá trị trả về

Một [`IPortionFormatEffectiveData`](../../iportionformateffectivedata).

### Ví dụ

Ví dụ này minh họa cách lấy một số thuộc tính định dạng phần hiệu quả.

```csharp
[C#]
using (Presentation pres = new Presentation(@"MyPresentation.pptx"))
{
	IAutoShape shape = pres.Slides[0].Shapes[0] as IAutoShape;
	IPortionFormatEffectiveData effectivePortionFormat = shape.TextFrame.Paragraphs[0].Portions[0].PortionFormat.GetEffective();

	Console.WriteLine("Latin font: " + effectivePortionFormat.LatinFont.FontName);
	Console.WriteLine("Font height: " + effectivePortionFormat.FontHeight);
	Console.WriteLine("Fill type: " + effectivePortionFormat.FillFormat.FillType);
}
```

### Xem thêm

* giao diện [IPortionFormatEffectiveData](../../iportionformateffectivedata)
* lớp [PortionFormat](../../portionformat)
* không gian tên [Aspose.Slides](../../portionformat)
* tập hợp [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->