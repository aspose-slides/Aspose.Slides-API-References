---
title: ReplaceRegex
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Thay thế tất cả các khớp của biểu thức chính quy bằng chuỗi đã chỉ định.
type: docs
weight: 110
url: /vi/aspose.slides/itextframe/replaceregex/
---
## ITextFrame.ReplaceRegex phương thức

Thay thế tất cả các khớp của biểu thức chính quy bằng chuỗi đã chỉ định.

```csharp
public void ReplaceRegex(Regex regex, string newText, IFindResultCallback callback)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| regex | Regex | Biểu thức chính quy Regex để lấy các chuỗi cần thay thế. |
| newText | String | Chuỗi dùng để thay thế tất cả các lần xuất hiện của các chuỗi cần được thay thế. |
| callback | IFindResultCallback | Đối tượng callback để nhận kết quả tìm kiếm [`IFindResultCallback`](../../ifindresultcallback). |

### Ví dụ

Đoạn mã mẫu dưới đây cho thấy cách thay thế văn bản bằng biểu thức chính quy với chuỗi đã chỉ định.

```csharp
[C#]
using (Presentation presentation = new Presentation("SomePresentation.pptx"))
{
	Regex regex = new Regex(@"\b[^\s]{10,}\b");
	// Thay thế tất cả các từ có 10 ký tự trở lên bằng '***'
	((AutoShape)presentation.Slides[0].Shapes[0]).TextFrame.ReplaceRegex(regex, "***", null);
	presentation.Save("SomePresentation-out.pptx", SaveFormat.Pptx);
}
```

### Xem thêm

* giao diện [IFindResultCallback](../../ifindresultcallback)
* giao diện [ITextFrame](../../itextframe)
* không gian tên [Aspose.Slides](../../itextframe)
* tập hợp [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->