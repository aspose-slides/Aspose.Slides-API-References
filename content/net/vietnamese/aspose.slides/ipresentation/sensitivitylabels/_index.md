---
title: SensitivityLabels
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Trả về bộ sưu tập các nhãn nhạy cảm được áp dụng cho tài liệu bản trình chiếu. Chỉ đọc ISensitivityLabelCollectionaspose.slides/isensitivitylabelcollection.
type: docs
weight: 240
url: /vi/aspose.slides/ipresentation/sensitivitylabels/
---
## IPresentation.SensitivityLabels thuộc tính

Trả về bộ sưu tập các nhãn nhạy cảm được áp dụng cho tài liệu bản trình chiếu. Chỉ đọc [`ISensitivityLabelCollection`](../../isensitivitylabelcollection).

```csharp
public ISensitivityLabelCollection SensitivityLabels { get; }
```

### Ví dụ

```csharp
[C#]
using (Presentation pres = new Presentation("SomePresentation.pptx"))
{
    ISensitivityLabelCollection sensitivityLabels = pres.SensitivityLabels;
    
    // In ra các nhãn đã áp dụng
    foreach (var sensitivityLabel in sensitivityLabels)
        Console.WriteLine("Label Id " + sensitivityLabel.Id + " from Azure AD site " + sensitivityLabel.SiteId);
    
    // Thêm nhãn mới
    string labelIdString = "{xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx}"; // Lấy Id nhãn nhạy cảm từ chính sách
    Guid siteIdGuid = Guid.Parse("{xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx}"); // Lấy định danh site Azure AD từ chính sách
    var label = sensitivityLabels.Add(labelIdString, siteIdGuid, true, SensitivityLabelAssignmentType.Privileged);
    label.ContentMarkTypes.Add(SensitivityLabelContentType.Footer);
    
    pres.Save("SensitivityLabel.pptx", SaveFormat.Pptx);
}
```

### Xem Thêm

* giao diện [ISensitivityLabelCollection](../../isensitivitylabelcollection)
* giao diện [IPresentation](../../ipresentation)
* không gian tên [Aspose.Slides](../../ipresentation)
* tập hợp [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->