---
title: GetBasePlaceholder
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Trả về một hình giữ chỗ cơ bản từ bố cục và/hoặc slide master mà hình hiện tại được kế thừa. Nếu hình hiện tại không được kế thừa, trả về null.
type: docs
weight: 310
url: /vi/aspose.slides/ishape/getbaseplaceholder/
---
## IShape.GetBasePlaceholder phương thức

Trả về một hình giữ chỗ cơ bản (hình từ bố cục và/hoặc slide master mà hình hiện tại được kế thừa). Nếu hình hiện tại không được kế thừa, trả về null.

```csharp
public IShape GetBasePlaceholder()
```

### Ví dụ

```csharp
[C#]
// lấy tất cả các hiệu ứng hoạt ảnh (master/layout/slide) của hình giữ chỗ
using (Presentation pres = new Presentation("sample.pptx"))
{
    ISlide slide = pres.Slides[0];
    IShape shape = slide.Shapes[0];
    IEffect[] shapeEffects = slide.Timeline.MainSequence.GetEffectsByShape(shape);

    IShape layoutShape = shape.GetBasePlaceholder();
    IEffect[] layoutShapeEffects = slide.LayoutSlide.Timeline.MainSequence.GetEffectsByShape(layoutShape);

    IShape masterShape = layoutShape.GetBasePlaceholder();
    IEffect[] masterShapeEffects = slide.LayoutSlide.MasterSlide.Timeline.MainSequence.GetEffectsByShape(masterShape);
}
```

### Xem thêm

* giao diện [IShape](../../ishape)
* không gian tên [Aspose.Slides](../../ishape)
* bộ lắp ráp [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->