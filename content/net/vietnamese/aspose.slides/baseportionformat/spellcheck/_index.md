---
title: SpellCheck
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Lấy hoặc thiết lập một giá trị cho biết có bật kiểm tra chính tả cho phần văn bản hay không. Khi thuộc tính này được đặt thành false, việc kiểm tra chính tả cho các yếu tố văn bản sẽ bị tránh. Khi được đặt thành true, kiểm tra chính tả được cho phép. Giá trị mặc định là false.
type: docs
weight: 220
url: /vi/aspose.slides/baseportionformat/spellcheck/
---
## BasePortionFormat.SpellCheck thuộc tính

Lấy hoặc thiết lập một giá trị cho biết có bật kiểm tra chính tả cho phần văn bản hay không. Khi thuộc tính này được đặt thành false, việc kiểm tra chính tả cho các yếu tố văn bản sẽ bị tránh. Khi được đặt thành true, kiểm tra chính tả được cho phép. Giá trị mặc định là `false`.

```csharp
public bool SpellCheck { get; set; }
```

### Ví dụ

Ví dụ tiếp theo minh họa cách bật cờ SpellCheck trước khi lưu bản trình chiếu:

```csharp
[C#]
using (var pres = new Presentation("input.pptx"))
{
    // Truy cập phần văn bản đầu tiên bên trong hình dạng đầu tiên trên slide đầu tiên
    var portion = ((AutoShape)pres.Slides[0].Shapes[0]).TextFrame.Paragraphs[0].Portions[0];
    // Bật kiểm tra chính tả cho phần văn bản này
    portion.PortionFormat.SpellCheck = true;
    // Lưu bản trình chiếu đã chỉnh sửa
    pres.Save("output-with-spellcheck.pptx", SaveFormat.Pptx);
}
```

### Xem Thêm

* lớp [BasePortionFormat](../../baseportionformat)
* không gian tên [Aspose.Slides](../../baseportionformat)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->