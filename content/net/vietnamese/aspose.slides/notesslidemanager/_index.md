---
title: NotesSlideManager
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Trình quản lý slide ghi chú.
type: docs
weight: 9160
url: /vi/aspose.slides/notesslidemanager/
---
## NotesSlideManager lớp

Trình quản lý ghi chú slide.

```csharp
public sealed class NotesSlideManager : DomObject<Slide>, INotesSlideManager
```

## Thuộc tính

| Tên | Mô tả |
| --- | --- |
| [NotesSlide](../../aspose.slides/notesslidemanager/notesslide) { get; } | Trả về slide ghi chú cho slide hiện tại. Trả về null nếu slide không có slide ghi chú. Chỉ đọc [`INotesSlide`](../inotesslide). |

## Phương thức

| Tên | Mô tả |
| --- | --- |
| [AddNotesSlide](../../aspose.slides/notesslidemanager/addnotesslide)() | Trả về slide ghi chú cho slide hiện tại, tạo mới nếu không có. |
| [RemoveNotesSlide](../../aspose.slides/notesslidemanager/removenotesslide)() | Xóa slide ghi chú của slide hiện tại. |

### Ví dụ

Ví dụ sau cho thấy cách Thêm Ghi chú vào slide cụ thể của PowerPoint Presentation.

```csharp
[C#]
	// Tạo một đối tượng Presentation đại diện cho một tệp bản trình chiếu
	using(Presentation presentation = new Presentation(dataDir + "AccessSlides.pptx")) {
	  // Thêm ghi chú vào slide đầu tiên
	  INotesSlideManager mgr = presentation.Slides[0].NotesSlideManager;
	  INotesSlide noteSlide = mgr.AddNotesSlide();
	  noteSlide.NotesTextFrame.Text = "Your Notes";
	  // Lưu bản trình chiếu vào đĩa
	  presentation.Save(dataDir + "RemoveNotesAtSpecificSlide_out.pptx", SaveFormat.Pptx);
	}
```

Ví dụ sau cho thấy cách xóa Ghi chú khỏi slide cụ thể của PowerPoint Presentation.

```csharp
[C#]
	// Khởi tạo một đối tượng Presentation đại diện cho một tệp bản trình chiếu
	using(Presentation presentation = new Presentation(dataDir + "AccessSlides.pptx")) {
	  // Xóa ghi chú của slide đầu tiên
	  INotesSlideManager mgr = presentation.Slides[0].NotesSlideManager;
	  mgr.RemoveNotesSlide();
	  // Lưu bản trình chiếu vào đĩa
	  presentation.Save(dataDir + "RemoveNotesAtSpecificSlide_out.pptx", SaveFormat.Pptx);
	}
```

### Xem thêm

* lớp [DomObject&lt;TParent&gt;](../domobject-1)
* lớp [Slide](../slide)
* giao diện [INotesSlideManager](../inotesslidemanager)
* không gian tên [Aspose.Slides](../../aspose.slides)
* bộ lắp ráp [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->