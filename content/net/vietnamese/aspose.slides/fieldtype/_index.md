---
title: FieldType
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Biểu thị một loại trường. Giá trị này xác định văn bản nào sẽ được đặt vào phần trường khi nó được cập nhật.
type: docs
weight: 4760
url: /vi/aspose.slides/fieldtype/
---
## lớp FieldType

Biểu thị một loại trường. Giá trị này xác định văn bản nào sẽ được đặt vào phần trường khi nó được cập nhật.

```csharp
public sealed class FieldType : IFieldType
```

## Các hàm khởi tạo

| Tên | Mô tả |
| --- | --- |
| [FieldType](fieldtype)(string) | Khởi tạo một thể hiện mới của lớp FieldType. |

## Thuộc tính

| Tên | Mô tả |
| --- | --- |
| static [DateTime](../../aspose.slides/fieldtype/datetime) { get; } | Ngày và giờ hiện tại ở định dạng ngày giờ mặc định cho ứng dụng hiển thị. Chỉ đọc [`FieldType`](../fieldtype). |
| static [DateTime1](../../aspose.slides/fieldtype/datetime1) { get; } | Ngày và giờ hiện tại ở định dạng định trước thứ nhất (MM/DD/YYYY cho tiếng Anh). Chỉ đọc [`FieldType`](../fieldtype). |
| static [DateTime10](../../aspose.slides/fieldtype/datetime10) { get; } | Ngày và giờ hiện tại ở định dạng định trước thứ mười (hh:mm cho tiếng Anh). Chỉ đọc [`FieldType`](../fieldtype). |
| static [DateTime11](../../aspose.slides/fieldtype/datetime11) { get; } | Ngày và giờ hiện tại ở định dạng định trước thứ mười một (hh:mm:ss cho tiếng Anh). Chỉ đọc [`FieldType`](../fieldtype). |
| static [DateTime12](../../aspose.slides/fieldtype/datetime12) { get; } | Ngày và giờ hiện tại ở định dạng định trước thứ mười hai (hh:mm AM/PM cho tiếng Anh). Chỉ đọc [`FieldType`](../fieldtype). |
| static [DateTime13](../../aspose.slides/fieldtype/datetime13) { get; } | Ngày và giờ hiện tại ở định dạng định trước thứ mười ba (hh:mm:ss AM/PM cho tiếng Anh). Chỉ đọc [`FieldType`](../fieldtype). |
| static [DateTime2](../../aspose.slides/fieldtype/datetime2) { get; } | Ngày và giờ hiện tại ở định dạng định trước thứ hai (Day, Month DD, YYYY cho tiếng Anh). Chỉ đọc [`FieldType`](../fieldtype). |
| static [DateTime3](../../aspose.slides/fieldtype/datetime3) { get; } | Ngày và giờ hiện tại ở định dạng định trước thứ ba (DD Month YYYY cho tiếng Anh). Chỉ đọc [`FieldType`](../fieldtype). |
| static [DateTime4](../../aspose.slides/fieldtype/datetime4) { get; } | Ngày và giờ hiện tại ở định dạng định trước thứ tư (Month DD, YYYY cho tiếng Anh). Chỉ đọc [`FieldType`](../fieldtype). |
| static [DateTime5](../../aspose.slides/fieldtype/datetime5) { get; } | Ngày và giờ hiện tại ở định dạng định trước thứ năm (DD-Mon-YY cho tiếng Anh). Chỉ đọc [`FieldType`](../fieldtype). |
| static [DateTime6](../../aspose.slides/fieldtype/datetime6) { get; } | Ngày và giờ hiện tại ở định dạng định trước thứ sáu (Month YY cho tiếng Anh). Chỉ đọc [`FieldType`](../fieldtype). |
| static [DateTime7](../../aspose.slides/fieldtype/datetime7) { get; } | Ngày và giờ hiện tại ở định dạng định trước thứ bảy (Mon-YY cho tiếng Anh). Chỉ đọc [`FieldType`](../fieldtype). |
| static [DateTime8](../../aspose.slides/fieldtype/datetime8) { get; } | Ngày và giờ hiện tại ở định dạng định trước thứ tám (MM/DD/YYYY hh:mm AM/PM cho tiếng Anh). Chỉ đọc [`FieldType`](../fieldtype). |
| static [DateTime9](../../aspose.slides/fieldtype/datetime9) { get; } | Ngày và giờ hiện tại ở định dạng định trước thứ chín (MM/DD/YYYY hh:mm:ss AM/PM cho tiếng Anh). Chỉ đọc [`FieldType`](../fieldtype). |
| static [Footer](../../aspose.slides/fieldtype/footer) { get; } | Chân trang của slide. Chỉ đọc [`FieldType`](../fieldtype). |
| static [Header](../../aspose.slides/fieldtype/header) { get; } | Đầu trang của slide. Chỉ đọc [`FieldType`](../fieldtype). |
| static [SlideNumber](../../aspose.slides/fieldtype/slidenumber) { get; } | Số thứ tự hiện tại của slide. Chỉ đọc [`FieldType`](../fieldtype). |
| [InternalString](../../aspose.slides/fieldtype/internalstring) { get; set; } | Trả về tên nội bộ của đối tượng FieldType này. Đọc/ghi String. |

## Phương thức

| Tên | Mô tả |
| --- | --- |
| override [Equals](../../aspose.slides/fieldtype/equals)(object) | Kiểm tra xem trường này có bằng với trường khác hay không. |
| override [GetHashCode](../../aspose.slides/fieldtype/gethashcode)() | Trả về mã băm cho đối tượng này. |
| [operator ==](../../aspose.slides/fieldtype/op_equality) | Kiểm tra xem hai đối tượng FieldType có bằng nhau hay không. |
| [operator !=](../../aspose.slides/fieldtype/op_inequality) | Kiểm tra xem hai đối tượng FieldType có khác nhau hay không. |

### Xem thêm

* interface [IFieldType](../ifieldtype)
* namespace [Aspose.Slides](../../aspose.slides)
* assembly [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->