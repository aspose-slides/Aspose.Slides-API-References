---
title: AddFromPdf
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Tạo các slide từ tài liệu PDF và thêm chúng vào cuối bộ sưu tập.
type: docs
weight: 50
url: /vi/aspose.slides/islidecollection/addfrompdf/
---
## AddFromPdf(string) {#addfrompdf_2}

Tạo các slide từ tài liệu PDF và thêm chúng vào cuối bộ sưu tập.

```csharp
public ISlide[] AddFromPdf(string path)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| path | String | A path to the PDF document |

### Giá trị trả về

Các slide đã được thêm

### Ví dụ

Ví dụ:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    pres.Slides.AddFromPdf("document.pdf");
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Xem thêm

* giao diện [ISlide](../../islide)
* giao diện [ISlideCollection](../../islidecollection)
* không gian tên [Aspose.Slides](../../islidecollection)
* tập hợp [Aspose.Slides](../../../)

---

## AddFromPdf(string, PdfImportOptions) {#addfrompdf_3}

Tạo các slide từ tài liệu PDF và thêm chúng vào cuối bộ sưu tập, cân nhắc các tùy chọn nhập PDF.

```csharp
public ISlide[] AddFromPdf(string path, PdfImportOptions pdfImportOptions)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| path | String | A path to the PDF document |
| pdfImportOptions | PdfImportOptions | Options for pdf import |

### Giá trị trả về

Các slide đã được thêm

### Ví dụ

Ví dụ:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    pres.Slides.AddFromPdf("document.pdf, new PdfImportOptions { DetectTables = true });
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Xem thêm

* giao diện [ISlide](../../islide)
* lớp [PdfImportOptions](../../../aspose.slides.import/pdfimportoptions)
* giao diện [ISlideCollection](../../islidecollection)
* không gian tên [Aspose.Slides](../../islidecollection)
* tập hợp [Aspose.Slides](../../../)

---

## AddFromPdf(Stream, PdfImportOptions) {#addfrompdf_1}

Tạo các slide từ tài liệu PDF và thêm chúng vào cuối bộ sưu tập.

```csharp
public ISlide[] AddFromPdf(Stream pdfStream, PdfImportOptions pdfImportOptions)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| pdfStream | Stream | A stream which will be used as a source of the PDF document |
| pdfImportOptions | PdfImportOptions | Options for pdf import |

### Giá trị trả về

Các slide đã được thêm

### Ví dụ

Ví dụ:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    using (Stream stream = new FileStream("document.pdf", FileMode.Open, FileAccess.Read, FileShare.Read))
    {
        pres.Slides.AddFromPdf(stream, new PdfImportOptions { DetectTables = true });
    }
    
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Xem thêm

* giao diện [ISlide](../../islide)
* lớp [PdfImportOptions](../../../aspose.slides.import/pdfimportoptions)
* giao diện [ISlideCollection](../../islidecollection)
* không gian tên [Aspose.Slides](../../islidecollection)
* tập hợp [Aspose.Slides](../../../)

---

## AddFromPdf(Stream) {#addfrompdf}

Tạo các slide từ tài liệu PDF và thêm chúng vào cuối bộ sưu tập.

```csharp
public ISlide[] AddFromPdf(Stream pdfStream)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| pdfStream | Stream | A stream which will be used as a source of the PDF document |

### Giá trị trả về

Các slide đã được thêm

### Ví dụ

Ví dụ:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    using (Stream stream = new FileStream("document.pdf", FileMode.Open, FileAccess.Read, FileShare.Read))
    {
        pres.Slides.AddFromPdf(stream);
    }
    
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Xem thêm

* giao diện [ISlide](../../islide)
* giao diện [ISlideCollection](../../islidecollection)
* không gian tên [Aspose.Slides](../../islidecollection)
* tập hợp [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->