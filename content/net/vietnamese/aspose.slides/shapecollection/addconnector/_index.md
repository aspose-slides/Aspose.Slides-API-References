---
title: AddConnector
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Tạo một hình kết nối mới với kiểu mẫu mặc định và thêm nó vào cuối bộ sưu tập hình.
type: docs
weight: 120
url: /vi/aspose.slides/shapecollection/addconnector/
---
## AddConnector(ShapeType, float, float, float, float) {#addconnector}

Tạo một hình kết nối mới với kiểu mẫu mặc định và thêm nó vào cuối bộ sưu tập hình.

```csharp
public IConnector AddConnector(ShapeType shapeType, float x, float y, float width, float height)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| shapeType | ShapeType | [`ShapeType`](../../shapetype) của hình nối để thêm. |
| x | Single | Tọa độ x của khung nối, tính bằng điểm. |
| y | Single | Tọa độ y của khung nối, tính bằng điểm. |
| width | Single | Chiều rộng của khung nối, tính bằng điểm. |
| height | Single | Chiều cao của khung nối, tính bằng điểm. |

### Giá trị trả về

Đối tượng [`IConnector`](../../iconnector) mới được tạo.

### Ví dụ

Ví dụ dưới đây cho thấy cách thêm một kết nối (kết nối cong) giữa hai hình (ellipse và rectangle) trong PowerPoint Presentation.

```csharp
[C#]
// Khởi tạo một lớp presentation đại diện cho tệp PPTX
using (Presentation input = new Presentation())
{
    // Truy cập bộ sưu tập hình cho một slide cụ thể
    IShapeCollection shapes = input.Slides[0].Shapes;
    // Thêm một autoshape Ellipse
    IAutoShape ellipse = shapes.AddAutoShape(ShapeType.Ellipse, 0, 100, 100, 100);
    // Thêm một autoshape Rectangle
    IAutoShape rectangle = shapes.AddAutoShape(ShapeType.Rectangle, 100, 300, 100, 100);
    // Thêm một hình connector vào bộ sưu tập hình của slide
    IConnector connector = shapes.AddConnector(ShapeType.BentConnector2, 0, 0, 10, 10);
    // Kết nối các hình bằng connector
    connector.StartShapeConnectedTo = ellipse;
    connector.EndShapeConnectedTo = rectangle;
    // Gọi reroute để thiết lập đường ngắn nhất tự động giữa các hình
    connector.Reroute();
    // Lưu presentation
    input.Save("Shapes-connector.pptx", SaveFormat.Pptx);
}
```

### Xem thêm

* giao diện [IConnector](../../iconnector)
* liệt kê [ShapeType](../../shapetype)
* lớp [ShapeCollection](../../shapecollection)
* không gian tên [Aspose.Slides](../../shapecollection)
* tập hợp [Aspose.Slides](../../../)

---

## AddConnector(ShapeType, float, float, float, float, bool) {#addconnector_1}

Tạo một hình kết nối mới và thêm nó vào cuối bộ sưu tập hình, tùy chọn áp dụng kiểu mẫu mặc định.

```csharp
public IConnector AddConnector(ShapeType shapeType, float x, float y, float width, float height, 
    bool createFromTemplate)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| shapeType | ShapeType | [`ShapeType`](../../shapetype) của hình nối để tạo. |
| x | Single | Tọa độ x của khung nối, tính bằng điểm. |
| y | Single | Tọa độ y của khung nối, tính bằng điểm. |
| width | Single | Chiều rộng của khung nối, tính bằng điểm. |
| height | Single | Chiều cao của khung nối, tính bằng điểm. |
| createFromTemplate | Boolean | True để áp dụng kiểu mẫu mặc định (tên không rỗng, kiểu đơn giản); false để tạo kết nối với các giá trị thuộc tính mặc định. |

### Giá trị trả về

Đối tượng [`IConnector`](../../iconnector) mới được tạo.

### Xem thêm

* giao diện [IConnector](../../iconnector)
* liệt kê [ShapeType](../../shapetype)
* lớp [ShapeCollection](../../shapecollection)
* không gian tên [Aspose.Slides](../../shapecollection)
* tập hợp [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->