---
title: AddOleObjectFrame
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Tạo một khung đối tượng OLE mới và thêm nó vào cuối bộ sưu tập hình dạng.
type: docs
weight: 150
url: /vi/aspose.slides/shapecollection/addoleobjectframe/
---
## AddOleObjectFrame(float, float, float, float, IOleEmbeddedDataInfo) {#addoleobjectframe}

Tạo một khung đối tượng OLE mới và thêm nó vào cuối bộ sưu tập hình dạng.

```csharp
public IOleObjectFrame AddOleObjectFrame(float x, float y, float width, float height, 
    IOleEmbeddedDataInfo dataInfo)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| x | Single | Tọa độ x của khung OLE mới, tính bằng point. |
| y | Single | Tọa độ y của khung OLE mới, tính bằng point. |
| width | Single | Chiều rộng của khung OLE mới, tính bằng point. |
| height | Single | Chiều cao của khung OLE mới, tính bằng point. |
| dataInfo | IOleEmbeddedDataInfo | Thông tin về dữ liệu OLE được nhúng ([`IOleEmbeddedDataInfo`](../../ioleembeddeddatainfo)). |

### Giá trị trả về

Đối tượng mới được tạo [`IOleObjectFrame`](../../ioleobjectframe).

### Ví dụ

Các ví dụ sau cho thấy cách thêm Khung Đối tượng OLE vào các slide của bản trình chiếu PowerPoint.

```csharp
[C#]
// Khởi tạo lớp Presentation đại diện cho tệp PPTX
using (Presentation pres = new Presentation())
{
    // Truy cập slide đầu tiên
    ISlide sld = pres.Slides[0];
    // Tải tệp excel vào luồng
    MemoryStream mstream = new MemoryStream();
    using (FileStream fs = new FileStream("book1.xlsx", FileMode.Open, FileAccess.Read))
    {
        byte[] buf = new byte[4096];
        while (true)
        {
            int bytesRead = fs.Read(buf, 0, buf.Length);
            if (bytesRead <= 0)
                break;
            mstream.Write(buf, 0, bytesRead);
        }
    }
    // Tạo đối tượng dữ liệu để nhúng
    IOleEmbeddedDataInfo dataInfo = new OleEmbeddedDataInfo(mstream.ToArray(), "xlsx");
    // Thêm hình dạng Ole Object Frame
    IOleObjectFrame oleObjectFrame = sld.Shapes.AddOleObjectFrame(0, 0, pres.SlideSize.Size.Width,
        pres.SlideSize.Size.Height, dataInfo);
    // Ghi tệp PPTX vào đĩa
    pres.Save("OleEmbed_out.pptx", SaveFormat.Pptx);
}
```

### Xem thêm

* giao diện [IOleObjectFrame](../../ioleobjectframe)
* giao diện [IOleEmbeddedDataInfo](../../ioleembeddeddatainfo)
* lớp [ShapeCollection](../../shapecollection)
* không gian tên [Aspose.Slides](../../shapecollection)
* tập hợp [Aspose.Slides](../../../)

---

## AddOleObjectFrame(float, float, float, float, string, string) {#addoleobjectframe_1}

Tạo một khung đối tượng OLE mới và thêm nó vào cuối bộ sưu tập hình dạng.

```csharp
public IOleObjectFrame AddOleObjectFrame(float x, float y, float width, float height, 
    string className, string path)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| x | Single | Tọa độ x của khung OLE mới, tính bằng point. |
| y | Single | Tọa độ y của khung OLE mới, tính bằng point. |
| width | Single | Chiều rộng của khung OLE mới, tính bằng point. |
| height | Single | Chiều cao của khung OLE mới, tính bằng point. |
| className | String | Tên lớp của đối tượng OLE. |
| path | String | Đường dẫn đến tệp tin được liên kết. |

### Giá trị trả về

Đối tượng mới được tạo [`IOleObjectFrame`](../../ioleobjectframe).

### Xem thêm

* giao diện [IOleObjectFrame](../../ioleobjectframe)
* lớp [ShapeCollection](../../shapecollection)
* không gian tên [Aspose.Slides](../../shapecollection)
* tập hợp [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->