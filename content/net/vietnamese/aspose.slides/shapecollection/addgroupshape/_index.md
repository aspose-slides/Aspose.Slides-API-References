---
title: AddGroupShape
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Tạo một shape nhóm trống mới và thêm nó vào cuối bộ sưu tập shape. Khung của nhóm sẽ tự động điều chỉnh để phù hợp với bất kỳ shape nào được thêm vào.
type: docs
weight: 130
url: /vi/aspose.slides/shapecollection/addgroupshape/
---
## AddGroupShape() {#addgroupshape}

Tạo một shape nhóm trống mới và thêm nó vào cuối bộ sưu tập shape. Khung của nhóm sẽ tự động điều chỉnh để phù hợp với bất kỳ shape nào được thêm vào.

```csharp
public IGroupShape AddGroupShape()
```

### Giá trị trả về

Đối tượng [`IGroupShape`](../../igroupshape) mới được tạo.

### Ví dụ

Ví dụ sau đây cho thấy cách thêm một shape nhóm vào một slide của PowerPoint Presentation.

```csharp
[C#]
// Tạo đối tượng lớp Presentation
using (Presentation pres = new Presentation())
{
    // Lấy slide đầu tiên
    ISlide sld = pres.Slides[0];
    // Truy cập bộ sưu tập shape của các slide
    IShapeCollection slideShapes = sld.Shapes;
    // Thêm một shape nhóm vào slide
    IGroupShape groupShape = slideShapes.AddGroupShape();
    // Thêm các shape vào bên trong shape nhóm đã thêm
    groupShape.Shapes.AddAutoShape(ShapeType.Rectangle, 300, 100, 100, 100);
    groupShape.Shapes.AddAutoShape(ShapeType.Rectangle, 500, 100, 100, 100);
    groupShape.Shapes.AddAutoShape(ShapeType.Rectangle, 300, 300, 100, 100);
    groupShape.Shapes.AddAutoShape(ShapeType.Rectangle, 500, 300, 100, 100);
    // Thêm khung shape nhóm
    groupShape.Frame = new ShapeFrame(100, 300, 500, 40, NullableBool.False, NullableBool.False, 0);
    // Ghi tệp PPTX ra đĩa
    pres.Save("GroupShape_out.pptx", SaveFormat.Pptx);
}
```

### Xem thêm

* giao diện [IGroupShape](../../igroupshape)
* lớp [ShapeCollection](../../shapecollection)
* không gian tên [Aspose.Slides](../../shapecollection)
* assembly [Aspose.Slides](../../../)

---

## AddGroupShape(ISvgImage, float, float, float, float) {#addgroupshape_1}

Tạo một shape nhóm mới, chuyển đổi hình ảnh SVG đã chỉ định thành các shape riêng lẻ, và thêm nhóm kết quả vào cuối bộ sưu tập shape.

```csharp
public IGroupShape AddGroupShape(ISvgImage svgImage, float x, float y, float width, float height)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| svgImage | ISvgImage | Đối tượng [`ISvgImage`](../../isvgimage) chứa nội dung vector để chuyển đổi thành shape. |
| x | Single | Toạ độ x của khung nhóm, tính bằng điểm. |
| y | Single | Toạ độ y của khung nhóm, tính bằng điểm. |
| width | Single | Chiều rộng của khung nhóm, tính bằng điểm. |
| height | Single | Chiều cao của khung nhóm, tính bằng điểm. |

### Giá trị trả về

Đối tượng [`IGroupShape`](../../igroupshape) mới được tạo.

### Xem thêm

* giao diện [IGroupShape](../../igroupshape)
* giao diện [ISvgImage](../../isvgimage)
* lớp [ShapeCollection](../../shapecollection)
* không gian tên [Aspose.Slides](../../shapecollection)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->