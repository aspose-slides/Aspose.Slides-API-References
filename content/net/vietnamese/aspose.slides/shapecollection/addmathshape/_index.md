---
title: AddMathShape
second_title: Aspose.Sildes cho .NET Tham chiếu API
description: Tạo một hình tự động hình chữ nhật mới để chứa nội dung toán học và thêm nó vào cuối bộ sưu tập hình.
type: docs
weight: 140
url: /vi/aspose.slides/shapecollection/addmathshape/
---
## ShapeCollection.AddMathShape phương thức

Tạo một hình tự động hình chữ nhật mới để chứa nội dung toán học và thêm nó vào cuối bộ sưu tập hình.

```csharp
public IAutoShape AddMathShape(float x, float y, float width, float height)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| x | Single | Toạ độ x của khung hình, tính bằng điểm. |
| y | Single | Toạ độ y của khung hình, tính bằng điểm. |
| width | Single | Chiều rộng của khung hình, tính bằng điểm. |
| height | Single | Chiều cao của khung hình, tính bằng điểm. |

### Giá trị trả về

Đối tượng [`IAutoShape`](../../iautoshape) mới được tạo.

### Ví dụ

Ví dụ sau cho thấy cách thêm Phương trình Toán học trong trình chiếu PowerPoint.

```csharp
[C#]
using (Presentation pres = new Presentation())
{
   IAutoShape mathShape = pres.Slides[0].Shapes.AddMathShape(0, 0, 720, 150);
   var mathParagraph = (mathShape.TextFrame.Paragraphs[0].Portions[0] as MathPortion).MathParagraph;
   var fraction = new MathematicalText("x").Divide("y");
   mathParagraph.Add(new MathBlock(fraction));
   var mathBlock = new MathematicalText("c")
        .SetSuperscript("2")
        .Join("=")
        .Join(new MathematicalText("a").SetSuperscript("2"))
        .Join("+")
        .Join(new MathematicalText("b").SetSuperscript("2"));
    mathParagraph.Add(mathBlock);
    pres.Save("math.pptx", SaveFormat.Pptx);
}
```

### Xem thêm

* giao diện [IAutoShape](../../iautoshape)
* lớp [ShapeCollection](../../shapecollection)
* không gian tên [Aspose.Slides](../../shapecollection)
* tập hợp [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->