---
title: CaptionTracks
second_title: Aspose.Sildes cho Tham chiếu API .NET
description: Lấy bộ sưu tập các phụ đề đóng liên quan đến khung âm thanh. Thuộc tính này chỉ đọc và trả về một ICaptionsCollectionaspose.slides/icaptionscollection chứa tất cả các track phụ đề.
type: docs
weight: 60
url: /vi/aspose.slides/iaudioframe/captiontracks/
---
## IAudioFrame.CaptionTracks thuộc tính

Lấy bộ sưu tập các phụ đề đóng liên quan đến khung âm thanh. Thuộc tính này chỉ đọc và trả về một [`ICaptionsCollection`](../../icaptionscollection) chứa tất cả các track phụ đề.

```csharp
public ICaptionsCollection CaptionTracks { get; }
```

### Ví dụ

Ví dụ:

```csharp
[C#]
using (Presentation pres = new Presentation("audio with captions.pptx"))
{
    foreach (IShape shape in pres.Slides[0].Shapes)
    {
        if (shape is IAudioFrame audioFrame)
        {
            // Lưu dữ liệu nhị phân của track phụ đề thành tệp .vtt
            foreach (var captionTrack in audioFrame.CaptionTracks)
                File.WriteAllBytes(captionTrack.CaptionId + ".vtt", captionTrack.BinaryData);
        }
    }
}
```

### Xem Thêm

* giao diện [ICaptionsCollection](../../icaptionscollection)
* giao diện [IAudioFrame](../../iaudioframe)
* không gian tên [Aspose.Slides](../../iaudioframe)
* tập hợp [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->