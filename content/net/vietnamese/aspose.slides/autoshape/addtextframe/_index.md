---
title: AddTextFrame
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Thêm một TextFrame mới vào shape. Nếu shape đã có TextFrame thì chỉ đơn giản thay đổi văn bản của nó.
type: docs
weight: 60
url: /vi/aspose.slides/autoshape/addtextframe/
---
## AutoShape.AddTextFrame phương thức

Thêm một TextFrame mới vào shape. Nếu shape đã có TextFrame thì chỉ thay đổi văn bản của nó.

```csharp
public ITextFrame AddTextFrame(string text)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| text | String | Văn bản mặc định cho một TextFrame mới. |

### Ví dụ

Mã mẫu dưới đây cho thấy cách thêm văn bản watermark vào PowerPoint Presentation.

```csharp
[C#]
	using (var presentation = new Presentation())
	{
		ISlide slide = presentation.Slides[0];
		IAutoShape watermarkShape = slide.Shapes.AddAutoShape(ShapeType.Triangle, 0, 0, 150, 50);
		ITextFrame watermarkTextFrame = watermarkShape.AddTextFrame("Watermark");
	}
```

Ví dụ dưới đây cho thấy cách tạo Text Box trên Slide.

```csharp
[C#]
// Khởi tạo Presentation
using (Presentation pres = new Presentation())
{
    // Lấy slide đầu tiên trong presentation
    ISlide sld = pres.Slides[0];
    // Thêm AutoShape với kiểu được đặt là Rectangle
    IAutoShape ashp = sld.Shapes.AddAutoShape(ShapeType.Rectangle, 150, 75, 150, 50);
    // Thêm TextFrame vào Rectangle
    ashp.AddTextFrame(" ");
    // Truy cập vào khung văn bản
    ITextFrame txtFrame = ashp.TextFrame;
    // Tạo đối tượng Paragraph cho khung văn bản
    IParagraph para = txtFrame.Paragraphs[0];
    // Tạo đối tượng Portion cho đoạn văn
    IPortion portion = para.Portions[0];
    // Đặt văn bản
    portion.Text = "Aspose TextBox";
    // Lưu presentation vào đĩa
    pres.Save("TextBox_out.pptx", Aspose.Slides.Export.SaveFormat.Pptx);
}
```

Ví dụ dưới đây cho thấy cách thêm cột vào Text Box.

```csharp
[C#]
using (Presentation presentation = new Presentation())
{
	// Lấy slide đầu tiên trong presentation
	ISlide slide = presentation.Slides[0];
	// Thêm AutoShape với kiểu được đặt là Rectangle
	IAutoShape aShape = slide.Shapes.AddAutoShape(ShapeType.Rectangle, 100, 100, 300, 300);
	// Thêm TextFrame vào Rectangle
	aShape.AddTextFrame("All these columns are limited to be within a single text container -- " +
	"you can add or delete text and the new or remaining text automatically adjusts " +
	"itself to flow within the container. You cannot have text flow from one container " +
	"to other though -- we told you PowerPoint's column options for text are limited!");
	// Lấy định dạng văn bản của TextFrame
	ITextFrameFormat format = aShape.TextFrame.TextFrameFormat;
	// Chỉ định số cột trong TextFrame
	format.ColumnCount = 3;
	// Chỉ định khoảng cách giữa các cột
	format.ColumnSpacing = 10;
	// Lưu presentation
	presentation.Save("ColumnCount.pptx", SaveFormat.Pptx);
}
```

### Xem thêm

* giao diện [ITextFrame](../../itextframe)
* lớp [AutoShape](../../autoshape)
* không gian tên [Aspose.Slides](../../autoshape)
* tập hợp [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->