---
title: GetSensitivityLabels
second_title: Aspose.Sildes cho .NET Tham chiếu API
description: Lấy một mảng các nhãn độ nhạy cảm từ các thuộc tính tài liệu tùy chỉnh Microsoft Information Protection SDK Metadata.
type: docs
weight: 440
url: /vi/aspose.slides/documentproperties/getsensitivitylabels/
---
## DocumentProperties.GetSensitivityLabels phương thức

Lấy một mảng các nhãn độ nhạy cảm từ các thuộc tính tài liệu tùy chỉnh (Microsoft Information Protection SDK Metadata).

```csharp
public ISensitivityLabel[] GetSensitivityLabels()
```

### Ví dụ

Đoạn mã sau cho thấy cách di chuyển thông tin nhãn độ nhạy cảm từ các thuộc tính tài liệu tùy chỉnh sang bộ sưu tập SensitivityLabels hiện đại:

```csharp
[C#]
using (Presentation pres = new Presentation("SomePresentation.pptx"))
{
    // Lấy các nhãn độ nhạy cảm từ các thuộc tính tài liệu tùy chỉnh
    ISensitivityLabel[] mipSensitivityLabels = pres.DocumentProperties.GetSensitivityLabels();

    ISensitivityLabelCollection sensitivityLabels = pres.SensitivityLabels;
    foreach (var sensitivityLabel in mipSensitivityLabels)
    {
        // Thêm nhãn vào bộ sưu tập
        // Ở đây bạn có thể thêm kiểm tra tính hợp lệ của thông tin nhãn (nhãn có sẵn, v.v.)
        sensitivityLabels.Add(sensitivityLabel);
    }

    pres.Save("SensitivityLabel.pptx", SaveFormat.Pptx);
}
```

### Xem thêm

* giao diện [ISensitivityLabel](../../isensitivitylabel)
* lớp [DocumentProperties](../../documentproperties)
* không gian tên [Aspose.Slides](../../documentproperties)
* tập hợp [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->