---
title: SensitivityLabels
second_title: Aspose.Sildes cho Tham chiếu API .NET
description: Trả về tập hợp các nhãn nhạy cảm được áp dụng cho tài liệu trình chiếu. Chỉ đọc ISensitivityLabelCollectionaspose.slides/isensitivitylabelcollection.
type: docs
weight: 230
url: /vi/aspose.slides/presentation/sensitivitylabels/
---
## Presentation.SensitivityLabels thuộc tính

Trả về tập hợp các nhãn nhạy cảm được áp dụng cho tài liệu trình chiếu. Chỉ đọc [`ISensitivityLabelCollection`](../../isensitivitylabelcollection).

```csharp
public ISensitivityLabelCollection SensitivityLabels { get; }
```

### Ví dụ

```csharp
[C#]
using (Presentation pres = new Presentation("SomePresentation.pptx"))
{
    ISensitivityLabelCollection sensitivityLabels = pres.SensitivityLabels;
    
    // In các nhãn đã áp dụng
    foreach (var sensitivityLabel in sensitivityLabels)
        Console.WriteLine("Label Id " + sensitivityLabel.Id + " from Azure AD site " + sensitivityLabel.SiteId);
    
    // Thêm nhãn mới
    string labelIdString = "{xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx}"; // Lấy Id nhãn nhạy cảm từ chính sách
    Guid siteIdGuid = Guid.Parse("{xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx}"); // Lấy định danh trang Azure AD từ chính sách
    var label = sensitivityLabels.Add(labelIdString, siteIdGuid, true, SensitivityLabelAssignmentType.Privileged);
    label.ContentMarkTypes.Add(SensitivityLabelContentType.Footer);
    
    pres.Save("SensitivityLabel.pptx", SaveFormat.Pptx);
}
```

### Xem thêm

* giao diện [ISensitivityLabelCollection](../../isensitivitylabelcollection)
* lớp [Presentation](../../presentation)
* không gian tên [Aspose.Slides](../../presentation)
* tập lắp ráp [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->