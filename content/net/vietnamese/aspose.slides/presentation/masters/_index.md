---
title: Masters
second_title: Aspose.Sildes cho .NET Tham khảo API
description: Trả về danh sách tất cả các slide master được định nghĩa trong bản trình bày. Chỉ đọc IMasterSlideCollectionaspose.slides/imasterslidecollection.
type: docs
weight: 180
url: /vi/aspose.slides/presentation/masters/
---
## Presentation.Masters thuộc tính

Trả về danh sách tất cả các slide master được định nghĩa trong bản trình bày. Chỉ đọc [`IMasterSlideCollection`](../../imasterslidecollection).

```csharp
public IMasterSlideCollection Masters { get; }
```

### Ví dụ

Các ví dụ sau cho thấy cách thêm hình ảnh vào các slide Master của bản trình bày PowerPoint.

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    ISlide slide = pres.Slides[0];
    IMasterSlide masterSlide = slide.LayoutSlide.MasterSlide;
    IPPImage image = pres.Images.AddImage(File.ReadAllBytes("image.png"));
    masterSlide.Shapes.AddPictureFrame(ShapeType.Rectangle, 10, 10, 100, 100, image);
    pres.Save("pres.pptx", SaveFormat.Pptx);
}
```

Các ví dụ sau cho thấy cách thay đổi màu nền của slide master trong bản trình bày PowerPoint.

```csharp
[C#]
// Khởi tạo lớp Presentation đại diện cho tệp bản trình bày
using (Presentation pres = new Presentation())
{
    // Đặt màu nền của ISlide Master thành màu xanh rừng
    pres.Masters[0].Background.Type = BackgroundType.OwnBackground;
    pres.Masters[0].Background.FillFormat.FillType = FillType.Solid;
    pres.Masters[0].Background.FillFormat.SolidFillColor.Color = Color.ForestGreen;
    // Ghi bản trình bày vào đĩa
    pres.Save("SetSlideBackgroundMaster_out.pptx", SaveFormat.Pptx);
}
```

Các ví dụ sau cho thấy cách thêm bố cục slide vào bản trình bày PowerPoint.

```csharp
[C#]
// Khởi tạo lớp Presentation đại diện cho tệp bản trình bày
using (Presentation presentation = new Presentation("AccessSlides.pptx"))
{
    // Cố gắng tìm kiếm theo loại layout slide
    IMasterLayoutSlideCollection layoutSlides = presentation.Masters[0].LayoutSlides;
    ILayoutSlide layoutSlide = layoutSlides.GetByType(SlideLayoutType.TitleAndObject) ?? layoutSlides.GetByType(SlideLayoutType.Title);
    if (layoutSlide == null)
    {
        // Trường hợp khi một bản trình bày không chứa một số loại bố cục.
        // Tệp bản trình bày chỉ chứa các loại bố cục Blank và Custom.
        // Tuy nhiên các slide bố cục với loại Custom có tên slide khác nhau,
        // như "Title", "Title and Content", v.v. Và có thể sử dụng chúng
        // để chọn layout slide.
        // Cũng có thể sử dụng tập hợp các loại placeholder. Ví dụ,
        // Slide tiêu đề chỉ nên có loại placeholder Title, v.v.
        foreach (ILayoutSlide titleAndObjectLayoutSlide in layoutSlides)
        {
            if (titleAndObjectLayoutSlide.Name == "Title and Object")
            {
                layoutSlide = titleAndObjectLayoutSlide;
                break;
            }
        }
        if (layoutSlide == null)
        {
            foreach (ILayoutSlide titleLayoutSlide in layoutSlides)
            {
                if (titleLayoutSlide.Name == "Title")
                {
                    layoutSlide = titleLayoutSlide;
                    break;
                }
            }
            if (layoutSlide == null)
            {
                layoutSlide = layoutSlides.GetByType(SlideLayoutType.Blank);
                if (layoutSlide == null)
                {
                    layoutSlide = layoutSlides.Add(SlideLayoutType.TitleAndObject, "Title and Object");
                }
            }
        }
    }
    // Thêm slide trống với layout slide đã thêm
    presentation.Slides.InsertEmptySlide(0, layoutSlide);
    // Lưu bản trình bày
    presentation.Save("AddLayoutSlides_out.pptx", SaveFormat.Pptx);
}
```

### Xem thêm

* giao diện [IMasterSlideCollection](../../imasterslidecollection)
* lớp [Presentation](../../presentation)
* không gian tên [Aspose.Slides](../../presentation)
* tập hợp [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->