---
title: GetFontEmbeddingLevel
second_title: Tham chiếu API Aspose.Sildes cho .NET
description: Xác định mức độ nhúng của phông chữ từ mảng byte và tên phông chữ đã cho.
type: docs
weight: 60
url: /vi/aspose.slides/ifontsmanager/getfontembeddinglevel/
---
## IFontsManager.GetFontEmbeddingLevel phương thức

Xác định mức độ nhúng của phông chữ từ mảng byte và tên phông chữ đã cho.

```csharp
public EmbeddingLevel GetFontEmbeddingLevel(byte[] fontBytes, string fontName)
```

| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| fontBytes | Byte[] | Mảng byte chứa dữ liệu phông chữ. |
| fontName | String | Tên của phông chữ. |

### Giá trị trả về

Mức độ nhúng của phông chữ đã chỉ định.

### Ngoại lệ

| ngoại lệ | điều kiện |
| --- | --- |
| ArgumentNullException | Bị ném khi *fontBytes* là null. |

### Ví dụ

```csharp
[C#]
using (Presentation pres = new Presentation(pptxFileName))
{
    // Lấy toàn bộ phông chữ được sử dụng trong bản trình chiếu
    IFontData[] fontDatas = pres.FontsManager.GetFonts();

    // Lấy mảng byte đại diện cho kiểu regular của phông chữ đầu tiên trong bản trình chiếu
    byte[] bytes = pres.FontsManager.GetFontBytes(fontDatas[0], FontStyle.Regular);

    // Xác định mức độ nhúng của phông chữ
    EmbeddingLevel embeddingLevel = pres.FontsManager.GetFontEmbeddingLevel(bytes, fontDatas[0].FontName);
}
```

### Xem thêm

* enum [EmbeddingLevel](../../embeddinglevel)
* giao diện [IFontsManager](../../ifontsmanager)
* không gian tên [Aspose.Slides](../../ifontsmanager)
* tập hợp [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->