---
title: TileAlignment
second_title: Aspose.Sildes cho .NET - Tham chiếu API
description: Trả về hoặc thiết lập cách mà họa tiết được căn chỉnh trong hình dạng. Cài đặt này kiểm soát điểm bắt đầu của mẫu họa tiết và cách nó lặp lại trên hình dạng. Đọc/ghi RectangleAlignmentaspose.slides/rectanglealignment.
type: docs
weight: 130
url: /vi/aspose.slides/ipicturefillformat/tilealignment/
---
## IPictureFillFormat.TileAlignment property

Trả về hoặc thiết lập cách mà họa tiết được căn chỉnh trong hình dạng. Cài đặt này kiểm soát điểm bắt đầu của mẫu họa tiết và cách nó lặp lại trên hình dạng. Đọc/ghi [`RectangleAlignment`](../../rectanglealignment).

```csharp
public RectangleAlignment TileAlignment { get; set; }
```

### Ghi chú

Mặc định là TopLeft.

### Ví dụ

```csharp
[C#]
using (Presentation presentation = new Presentation("demo.pptx"))
{
    ISlide slide = presentation.Slides[0];

    // Lấy định dạng tô hình ảnh của hình dạng
    IPictureFillFormat pictureFillFormat = slide.Shapes[0].FillFormat.PictureFillFormat;

    // Đặt chế độ tô hình ảnh thành Tile
    pictureFillFormat.PictureFillMode = PictureFillMode.Tile;

    // Đặt căn chỉnh cho việc lát gạch sang góc phải dưới
    pictureFillFormat.TileAlignment = RectangleAlignment.BottomRight;
}
```

### Xem thêm

* enum [RectangleAlignment](../../rectanglealignment)
* giao diện [IPictureFillFormat](../../ipicturefillformat)
* không gian tên [Aspose.Slides](../../ipicturefillformat)
* tập hợp [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->