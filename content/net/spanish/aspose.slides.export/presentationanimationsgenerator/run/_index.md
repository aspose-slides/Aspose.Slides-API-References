---
title: Run
second_title: Referencia de la API de Aspose.Slides para .NET
description: Ejecutar la generación de eventos de animación para cada diapositiva.
type: docs
weight: 80
url: /es/aspose.slides.export/presentationanimationsgenerator/run/
---

## Run(IEnumerable&lt;ISlide&gt;) {#run}

Ejecutar la generación de eventos de animación para cada diapositiva.

```csharp
public void Run(IEnumerable<ISlide> slides)
```

### Ejemplos

```csharp
[C#]
using (Presentation presentation = new Presentation("animated.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(presentation.SlideSize.Size.ToSize()))
    using (var player = new PresentationPlayer(animationsGenerator, 33))
    {
        animationsGenerator.NewAnimation += animationPlayer =>
        {
            // manejar nueva animación
        };
        
        player.FrameTick += (sender, args) =>
        {
            // manejar el tick del fotograma dentro de la nueva animación
        };
        
        animationsGenerator.Run(presentation.Slides);
    }
}
```

### Véase También

* interfaz [ISlide](../../../aspose.slides/islide)
* clase [PresentationAnimationsGenerator](../../presentationanimationsgenerator)
* espacio de nombres [Aspose.Slides.Export](../../presentationanimationsgenerator)
* ensamblado [Aspose.Slides](../../../)

---

## Run(IEnumerable&lt;ISlide&gt;, int, FrameTickHandler) {#run_1}

Ejecutar la generación de eventos de animación para cada diapositiva.

```csharp
public void Run(IEnumerable<ISlide> slides, int fps, FrameTickHandler onFrame)
```

### Ejemplos

```csharp
[C#]
using (Presentation presentation = new Presentation("animated.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(presentation.SlideSize.Size.ToSize()))
    {
        animationsGenerator.Run(presentation.Slides, 33, (sender, args) =>
        {
            sender.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save($"frame_{sender.FrameIndex}.png");
            };
        });
    }
}
```

### Véase También

* interfaz [ISlide](../../../aspose.slides/islide)
* delegado [FrameTickHandler](../../presentationplayer.frametickhandler)
* clase [PresentationAnimationsGenerator](../../presentationanimationsgenerator)
* espacio de nombres [Aspose.Slides.Export](../../presentationanimationsgenerator)
* ensamblado [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->