---
title: PresentationPlayer
second_title: Referencia de la API de Aspose.Slides para .NET
description: Representa el reproductor de animaciones asociadas con la Presentation../aspose.slides/presentation.
type: docs
weight: 4200
url: /es/aspose.slides.export/presentationplayer/
---

## Clase PresentationPlayer

Representa el reproductor de animaciones asociadas con la [`Presentation`](../../aspose.slides/presentation).

```csharp
public class PresentationPlayer : IDisposable
```

## Constructores

| Nombre | Descripción |
| --- | --- |
| [PresentationPlayer](presentationplayer)(PresentationAnimationsGenerator, double) | Crea una nueva instancia del [`PresentationPlayer`](../presentationplayer). |

## Propiedades

| Nombre | Descripción |
| --- | --- |
| [FrameIndex](../../aspose.slides.export/presentationplayer/frameindex) { get; } | Obtiene el índice del fotograma. |

## Métodos

| Nombre | Descripción |
| --- | --- |
| [Dispose](../../aspose.slides.export/presentationplayer/dispose)() | Dispone la instancia del [`PresentationPlayer`](../presentationplayer). |

## Otros Miembros

| Nombre | Descripción |
| --- | --- |
| delegate [FrameTickHandler](presentationplayer.frametickhandler) |  |

### Ejemplos

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(pres))
    {
        // Reproducir animación con 33 FPS
        using (var player = new PresentationPlayer(animationsGenerator, 33))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("33fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
        
        // Reproducir animación con 45 FPS
        using (var player = new PresentationPlayer(animationsGenerator, 45))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("45fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
    }
}
```

### Véase También

* namespace [Aspose.Slides.Export](../../aspose.slides.export)
* assembly [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->