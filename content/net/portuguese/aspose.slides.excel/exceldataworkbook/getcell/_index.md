---
title: GetCell
second_title: Referência da API Aspose.Sildes para .NET
description: Recupera uma célula da planilha especificada usando seu índice e as coordenadas da célula.
type: docs
weight: 20
url: /pt/aspose.slides.excel/exceldataworkbook/getcell/
---
## GetCell(int, int, int) {#getcell}

Recupera uma célula da planilha especificada usando seu índice e coordenadas da célula.

```csharp
public IExcelDataCell GetCell(int worksheetIndex, int row, int column)
```

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| worksheetIndex | Int32 | Índice baseado em zero da planilha. |
| row | Int32 | Índice da linha baseado em zero da célula. |
| column | Int32 | Índice da coluna baseado em zero da célula. |

### Valor de Retorno

A célula no local especificado.

### Exemplos

Exemplo:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell(1, 1, 1);
Console.WriteLine(cell.Value.ToString());
```

### Veja Também

* interface [IExcelDataCell](../../iexceldatacell)
* classe [ExcelDataWorkbook](../../exceldataworkbook)
* namespace [Aspose.Slides.Excel](../../exceldataworkbook)
* assembly [Aspose.Slides](../../../)

---

## GetCell(string, int, int) {#getcell_2}

Recupera uma célula da planilha especificada usando seu nome e coordenadas da célula.

```csharp
public IExcelDataCell GetCell(string worksheetName, int row, int column)
```

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| worksheetName | String | O nome da planilha. |
| row | Int32 | Índice da linha baseado em zero da célula. |
| column | Int32 | Índice da coluna baseado em zero da célula. |

### Valor de Retorno

A célula no local especificado.

### Exemplos

Exemplo:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell("Sheet1", 1, 1);
Console.WriteLine(cell.Value.ToString());
```

### Veja Também

* interface [IExcelDataCell](../../iexceldatacell)
* classe [ExcelDataWorkbook](../../exceldataworkbook)
* namespace [Aspose.Slides.Excel](../../exceldataworkbook)
* assembly [Aspose.Slides](../../../)

---

## GetCell(int, string) {#getcell_1}

Recupera uma célula da planilha especificada usando seu índice e o nome da célula no estilo Excel (por exemplo, "B2").

```csharp
public IExcelDataCell GetCell(int worksheetIndex, string cellName)
```

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| worksheetIndex | Int32 | Índice baseado em zero da planilha. |
| cellName | String | A referência da célula no estilo Excel (por exemplo, "A1", "C5"). |

### Valor de Retorno

A célula no local especificado.

### Exemplos

Exemplo:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell(1, "B2");
Console.WriteLine(cell.Value.ToString());
```

### Veja Também

* interface [IExcelDataCell](../../iexceldatacell)
* classe [ExcelDataWorkbook](../../exceldataworkbook)
* namespace [Aspose.Slides.Excel](../../exceldataworkbook)
* assembly [Aspose.Slides](../../../)

---

## GetCell(string, string) {#getcell_3}

Recupera uma célula da planilha especificada usando o nome da célula no estilo Excel (por exemplo, "B2").

```csharp
public IExcelDataCell GetCell(string worksheetName, string cellName)
```

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| worksheetName | String | O nome da planilha. |
| cellName | String | A referência da célula no estilo Excel (por exemplo, "A1", "C5"). |

### Valor de Retorno

A célula no local especificado.

### Exemplos

Exemplo:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell("Sheet1", "B2");
Console.WriteLine(cell.Value.ToString());
```

### Veja Também

* interface [IExcelDataCell](../../iexceldatacell)
* classe [ExcelDataWorkbook](../../exceldataworkbook)
* namespace [Aspose.Slides.Excel](../../exceldataworkbook)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->