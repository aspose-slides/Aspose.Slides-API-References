---
title: Shape
second_title: Aspose.Sildes para .NET – Referência da API
description: Iterar cada Shapeaspose.slides.lowcode/foreach/shape na Presentationaspose.slides/presentation. As Shapes serão iteradas em todos os tipos de slides - Slideaspose.slides.lowcode/foreach/slide MasterSlideaspose.slides.lowcode/foreach/masterslide e LayoutSlideaspose.slides.lowcode/foreach/layoutslide
type: docs
weight: 50
url: /pt/aspose.slides.lowcode/foreach/shape/
---
## Shape(Presentation, ForEachShapeCallback) {#shape_2}

Iterar cada `Shape` no [`Presentation`](../../../aspose.slides/presentation). As Shapes serão iteradas em todos os tipos de slides - [`Slide`](../slide), [`MasterSlide`](../masterslide) e [`LayoutSlide`](../layoutslide)

```csharp
public static void Shape(Presentation pres, ForEachShapeCallback forEachShape)
```

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| pres | Presentation | Apresentação para iterar formas de layout |
| forEachShape | ForEachShapeCallback | Callback que será invocado para cada forma |

### Exemplos

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Shape(pres, (shape, slide, index) => 
    {
        System.Console.WriteLine($"{shape.Name}, index: {index}");
    });
} 
```

### Ver Também

* classe [Presentation](../../../aspose.slides/presentation)
* delegado [ForEachShapeCallback](../../foreach.foreachshapecallback)
* classe [ForEach](../../foreach)
* espaço de nomes [Aspose.Slides.LowCode](../../foreach)
* assembly [Aspose.Slides](../../../)

---

## Shape(Presentation, bool, ForEachShapeCallback) {#shape_1}

Iterar cada [`Shape`](../shape) no [`Presentation`](../../../aspose.slides/presentation). As Shapes serão iteradas em todos os tipos de slides - [`Slide`](../slide), [`MasterSlide`](../masterslide), [`LayoutSlide`](../layoutslide) e [`NotesSlide`](../../../aspose.slides/notesslide) se necessário.

```csharp
public static void Shape(Presentation pres, bool includeNotes, ForEachShapeCallback forEachShape)
```

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| pres | Presentation | Apresentação para iterar formas de layout |
| includeNotes | Boolean | Indicador que indica se NotesSlides devem ser incluídos no processamento. |
| forEachShape | ForEachShapeCallback | Callback que será invocado para cada forma |

### Exemplos

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Shape(pres, true, (shape, slide, index) => 
    {
        System.Console.WriteLine($"{shape.Name}, index: {index}");
    });
} 
```

### Ver Também

* classe [Presentation](../../../aspose.slides/presentation)
* delegado [ForEachShapeCallback](../../foreach.foreachshapecallback)
* classe [ForEach](../../foreach)
* espaço de nomes [Aspose.Slides.LowCode](../../foreach)
* assembly [Aspose.Slides](../../../)

---

## Shape(BaseSlide, ForEachShapeCallback) {#shape}

Iterar cada [`Shape`](../shape) no [`BaseSlide`](../../../aspose.slides/baseslide). [`BaseSlide`](../../../aspose.slides/baseslide) é o tipo base para [`Slide`](../slide), [`MasterSlide`](../masterslide) e [`LayoutSlide`](../layoutslide)

```csharp
public static void Shape(BaseSlide baseSlide, ForEachShapeCallback forEachShape)
```

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| baseSlide | BaseSlide | Slide para iterar formas de layout |
| forEachShape | ForEachShapeCallback | Callback que será invocado para cada forma |

### Exemplos

```csharp
(Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Slide(pres, (slide, index) =>
    {
        ForEach.Shape(slide, (shape, baseSlide, shapeIndex) =>
        {
            System.Console.WriteLine($"{shape.Name}, index: {shapeIndex}");
        });
    });
} 
```

### Ver Também

* classe [BaseSlide](../../../aspose.slides/baseslide)
* delegado [ForEachShapeCallback](../../foreach.foreachshapecallback)
* classe [ForEach](../../foreach)
* espaço de nomes [Aspose.Slides.LowCode](../../foreach)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->