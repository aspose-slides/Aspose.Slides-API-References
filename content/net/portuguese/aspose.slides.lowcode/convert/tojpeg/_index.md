---
title: ToJpeg
second_title: Referência da API Aspose.Sildes para .NET
description: Converte a apresentação de entrada em um conjunto de imagens no formato JPEG. Se o nome do arquivo de saída for fornecido como myPath/myFilename.jpeg, o resultado será salvo como um conjunto de arquivos myPath/myFilename_N.jpeg, onde N é o número do slide.
type: docs
weight: 20
url: /pt/aspose.slides.lowcode/convert/tojpeg/
---
## ToJpeg(Presentation, string) {#tojpeg}

Converte a apresentação de entrada em um conjunto de imagens no formato JPEG. Se o nome do arquivo de saída for fornecido como "myPath/myFilename.jpeg", o resultado será salvo como um conjunto de arquivos "myPath/myFilename_N.jpeg", onde N é o número do slide.

```csharp
public static void ToJpeg(Presentation pres, string outputFileName)
```

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| pres | Presentation | A apresentação de entrada. |
| outputFileName | String | O nome do arquivo de saída. |

### Exceções

| Exceção | Condição |
| --- | --- |
| ArgumentException |  |

### Exemplos

```csharp
using (var pres = new Presentation("pres.pptx"))
    Convert.ToJpeg(pres, "presImage.jpeg");
```

### Veja Também

* classe [Presentation](../../../aspose.slides/presentation)
* classe [Convert](../../convert)
* namespace [Aspose.Slides.LowCode](../../convert)
* assembly [Aspose.Slides](../../../)

---

## ToJpeg(Presentation, string, Size) {#tojpeg_2}

Converte a apresentação de entrada em um conjunto de imagens no formato JPEG. Se o nome do arquivo de saída for fornecido como "myPath/myFilename.jpeg", o resultado será salvo como um conjunto de arquivos "myPath/myFilename_N.jpeg", onde N é o número do slide.

```csharp
public static void ToJpeg(Presentation pres, string outputFileName, Size imageSize)
```

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| pres | Presentation | A apresentação de entrada |
| outputFileName | String | O nome do arquivo de saída. |
| imageSize | Size | O tamanho de cada imagem gerada. |

### Exceções

| Exceção | Condição |
| --- | --- |
| ArgumentException |  |

### Exemplos

```csharp
using (var pres = new Presentation("pres.pptx"))
    Convert.ToJpeg(pres, "presImage.jpeg", new Size(720, 540));
```

### Veja Também

* classe [Presentation](../../../aspose.slides/presentation)
* classe [Convert](../../convert)
* namespace [Aspose.Slides.LowCode](../../convert)
* assembly [Aspose.Slides](../../../)

---

## ToJpeg(Presentation, string, float, IRenderingOptions) {#tojpeg_1}

Converte a apresentação de entrada em um conjunto de imagens no formato JPEG. Se o nome do arquivo de saída for fornecido como "myPath/myFilename.jpeg", o resultado será salvo como um conjunto de arquivos "myPath/myFilename_N.jpeg", onde N é o número do slide.

```csharp
public static void ToJpeg(Presentation pres, string outputFileName, float scale, 
    IRenderingOptions options)
```

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| pres | Presentation | A apresentação de entrada. |
| outputFileName | String | O nome do arquivo de saída. |
| scale | Single | O fator de escala aplicado às imagens de saída em relação ao tamanho original do slide. |
| options | IRenderingOptions | As opções de renderização. |

### Exceções

| Exceção | Condição |
| --- | --- |
| ArgumentException |  |

### Exemplos

```csharp
IRenderingOptions options = new RenderingOptions()
{
    SlidesLayoutOptions = new NotesCommentsLayoutingOptions()
    {
        NotesPosition = NotesPositions.BottomTruncated
    }
};

using (var pres = new Presentation("pres.pptx"))
    Convert.ToJpeg(pres, "presImage.jpeg", 2f, options);
```

### Veja Também

* classe [Presentation](../../../aspose.slides/presentation)
* interface [IRenderingOptions](../../../aspose.slides.export/irenderingoptions)
* classe [Convert](../../convert)
* namespace [Aspose.Slides.LowCode](../../convert)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->