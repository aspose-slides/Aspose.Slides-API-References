---
title: AddFromPdf
second_title: Aspose.Sildes para .NET Referência de API
description: Cria slides a partir do documento PDF e os adiciona ao final da coleção.
type: docs
weight: 50
url: /pt/aspose.slides/islidecollection/addfrompdf/
---
## AddFromPdf(string) {#addfrompdf_2}

Cria slides a partir do documento PDF e os adiciona ao final da coleção.

```csharp
public ISlide[] AddFromPdf(string path)
```

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| path | String | Um caminho para o documento PDF |

### Valor de Retorno

Slides adicionados

### Exemplos

Exemplo:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    pres.Slides.AddFromPdf("document.pdf");
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Veja Também

* interface [ISlide](../../islide)
* interface [ISlideCollection](../../islidecollection)
* namespace [Aspose.Slides](../../islidecollection)
* assembly [Aspose.Slides](../../../)

---

## AddFromPdf(string, PdfImportOptions) {#addfrompdf_3}

Cria slides a partir do documento PDF e os adiciona ao final da coleção considerando as opções de importação de PDF.

```csharp
public ISlide[] AddFromPdf(string path, PdfImportOptions pdfImportOptions)
```

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| path | String | Um caminho para o documento PDF |
| pdfImportOptions | PdfImportOptions | Opções para importação de PDF |

### Valor de Retorno

Slides adicionados

### Exemplos

Exemplo:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    pres.Slides.AddFromPdf("document.pdf, new PdfImportOptions { DetectTables = true });
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Veja Também

* interface [ISlide](../../islide)
* classe [PdfImportOptions](../../../aspose.slides.import/pdfimportoptions)
* interface [ISlideCollection](../../islidecollection)
* namespace [Aspose.Slides](../../islidecollection)
* assembly [Aspose.Slides](../../../)

---

## AddFromPdf(Stream, PdfImportOptions) {#addfrompdf_1}

Cria slides a partir do documento PDF e os adiciona ao final da coleção.

```csharp
public ISlide[] AddFromPdf(Stream pdfStream, PdfImportOptions pdfImportOptions)
```

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| pdfStream | Stream | Um stream que será usado como fonte do documento PDF |
| pdfImportOptions | PdfImportOptions | Opções para importação de PDF |

### Valor de Retorno

Slides adicionados

### Exemplos

Exemplo:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    using (Stream stream = new FileStream("document.pdf", FileMode.Open, FileAccess.Read, FileShare.Read))
    {
        pres.Slides.AddFromPdf(stream, new PdfImportOptions { DetectTables = true });
    }
    
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Veja Também

* interface [ISlide](../../islide)
* classe [PdfImportOptions](../../../aspose.slides.import/pdfimportoptions)
* interface [ISlideCollection](../../islidecollection)
* namespace [Aspose.Slides](../../islidecollection)
* assembly [Aspose.Slides](../../../)

---

## AddFromPdf(Stream) {#addfrompdf}

Cria slides a partir do documento PDF e os adiciona ao final da coleção.

```csharp
public ISlide[] AddFromPdf(Stream pdfStream)
```

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| pdfStream | Stream | Um stream que será usado como fonte do documento PDF |

### Valor de Retorno

Slides adicionados

### Exemplos

Exemplo:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    using (Stream stream = new FileStream("document.pdf", FileMode.Open, FileAccess.Read, FileShare.Read))
    {
        pres.Slides.AddFromPdf(stream);
    }
    
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Veja Também

* interface [ISlide](../../islide)
* interface [ISlideCollection](../../islidecollection)
* namespace [Aspose.Slides](../../islidecollection)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->