---
title: ColorTransformOperation
second_title: Aspose.Sildes para .NET Referência da API
description: Define a operação de transformação de cor.
type: docs
weight: 2570
url: /pt/aspose.slides/colortransformoperation/
---
## Enumeração ColorTransformOperation

Define a operação de transformação de cor.

```csharp
public enum ColorTransformOperation
```

### Valores

| Nome | Valor | Descrição |
| --- | --- | --- |
| Tint | `0` | Aplica um tom à cor. O parâmetro está no intervalo entre 0 (cor original) e 1 (branco). |
| Shade | `1` | Aplica uma sombra à cor. O parâmetro está no intervalo entre 0 (cor original) e 1 (preto). |
| Complement | `2` | Altera a cor para uma complementar RGB. m = Max(r, g, b); r = m - r; g = m - g; b = m - b; |
| Inverse | `3` | Altera a cor para uma cor invertida. r = 1 - r; g = 1 - g; b = 1 - b; |
| Grayscale | `4` | Converte a cor para um tom de cinza com a mesma luminosidade. Parâmetro ignorado. |
| SetAlpha | `5` | Define o componente alfa da cor. O parâmetro está no intervalo entre 0 (transparente) e 1 (opaco). |
| AddAlpha | `6` | Soma o valor do parâmetro ao componente alfa da cor. O parâmetro está no intervalo entre -1 e 1. |
| MultiplyAlpha | `7` | Multiplica o componente alfa pelo valor do parâmetro. |
| SetHue | `8` | Altera o componente matiz da cor para o valor do parâmetro. O parâmetro está no intervalo entre 0 e 360. |
| AddHue | `9` | Soma o valor do parâmetro ao componente matiz da cor. O parâmetro está no intervalo entre -360 e 360. |
| MultiplyHue | `10` | Multiplica o componente matiz pelo valor do parâmetro. |
| SetSaturation | `11` | Altera o componente saturação da cor para o valor do parâmetro. O parâmetro está no intervalo entre 0 e 1. |
| AddSaturation | `12` | Soma o valor do parâmetro ao componente saturação da cor. O parâmetro está no intervalo entre -1 e 1. |
| MultiplySaturation | `13` | Multiplica o componente saturação pelo valor do parâmetro. |
| SetLuminance | `14` | Altera o componente luminância da cor para o valor do parâmetro. O parâmetro está no intervalo entre 0 e 1. |
| AddLuminance | `15` | Soma o valor do parâmetro ao componente luminância da cor. O parâmetro está no intervalo entre -1 e 1. |
| MultiplyLuminance | `16` | Multiplica o componente luminância pelo valor do parâmetro. |
| SetRed | `17` | Altera o componente vermelho da cor para o valor do parâmetro. O parâmetro está no intervalo entre 0 e 1. |
| AddRed | `18` | Soma o valor do parâmetro ao componente vermelho da cor. O parâmetro está no intervalo entre -1 e 1. |
| MultiplyRed | `19` | Multiplica o componente vermelho pelo parâmetro. |
| SetGreen | `20` | Altera o componente verde da cor para o valor do parâmetro. O parâmetro está no intervalo entre 0 e 1. |
| AddGreen | `21` | Soma o parâmetro ao componente verde da cor. O parâmetro está no intervalo entre -1 e 1. |
| MultiplyGreen | `22` | Multiplica o componente verde pelo valor do parâmetro. |
| SetBlue | `23` | Altera o componente azul da cor para o valor do parâmetro. O parâmetro está no intervalo entre 0 e 360. |
| AddBlue | `24` | Soma o valor do parâmetro ao componente azul da cor. O parâmetro está no intervalo entre -1 e 1. |
| MultiplyBlue | `25` | Multiplica o componente azul pelo valor do parâmetro. |
| Gamma | `26` | Correção gama. Parâmetro ignorado. |
| InverseGamma | `27` | Correção gama inversa. Parâmetro ignorado. |

### Veja Também

* namespace [Aspose.Slides](../../aspose.slides)
* assembly [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->