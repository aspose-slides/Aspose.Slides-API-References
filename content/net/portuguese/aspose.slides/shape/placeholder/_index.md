---
title: Placeholder
second_title: Aspose.Sildes para .NET Referência da API
description: Retorna o placeholder de uma forma. Retorna null se a forma não tem placeholder. Somente leitura IPlaceholderaspose.slides/iplaceholder.
type: docs
weight: 210
url: /pt/aspose.slides/shape/placeholder/
---
## Shape.Placeholder propriedade

Retorna o espaço reservado de uma forma. Retorna null se a forma não tem espaço reservado. Somente leitura [`IPlaceholder`](../../iplaceholder).

```csharp
public IPlaceholder Placeholder { get; }
```

### Exemplos

O exemplo a seguir mostra como alterar Text em Placeholder.

```csharp
[C#]
// Instancia a classe Presentation
using (Presentation pres = new Presentation("ReplacingText.pptx"))
{
    // Acessa o primeiro slide
    ISlide sld = pres.Slides[0];
    // Itera através das formas para encontrar o placeholder
    foreach (IShape shp in sld.Shapes)
        if (shp.Placeholder != null)
        {
            // Altera o texto em cada placeholder
            ((IAutoShape)shp).TextFrame.Text = "This is a Placeholder";
        }
    // Salva a apresentação no disco
    pres.Save("output_out.pptx", Aspose.Slides.Export.SaveFormat.Pptx);
}
```

O exemplo a seguir mostra como definir Prompt Text em Placeholder.

```csharp
[C#]
using (Presentation pres = new Presentation("Presentation2.pptx"))
{
    ISlide slide = pres.Slides[0];
    foreach (IShape shape in slide.Slide.Shapes) // Itera através do slide
    {
        if (shape.Placeholder != null && shape is AutoShape)
        {
            string text = "";
            if (shape.Placeholder.Type == PlaceholderType.CenteredTitle) // PowerPoint exibe "Clique para adicionar título"
            {
                text = "Add Title";
            }
            else if (shape.Placeholder.Type == PlaceholderType.Subtitle) // Adiciona subtítulo
            {
                text = "Add Subtitle";
            }
            ((IAutoShape)shape).TextFrame.Text = text;
            Console.WriteLine($"Placeholder with text: {text}");
        }
    }
    pres.Save("Placeholders_PromptText.pptx", SaveFormat.Pptx);
}
```

### Ver também

* interface [IPlaceholder](../../iplaceholder)
* classe [Shape](../../shape)
* namespace [Aspose.Slides](../../shape)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->