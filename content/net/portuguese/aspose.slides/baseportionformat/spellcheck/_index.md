---
title: SpellCheck
second_title: Aspose.Sildes para .NET Referência da API
description: Obtém ou define um valor que indica se a verificação ortográfica está habilitada para a porção de texto. Quando esta propriedade é definida como false, as verificações ortográficas para elementos de texto são suprimidas. Quando definida como true, a verificação ortográfica é permitida. O valor padrão é false.
type: docs
weight: 220
url: /pt/aspose.slides/baseportionformat/spellcheck/
---
## BasePortionFormat.SpellCheck propriedade

Obtém ou define um valor que indica se a verificação ortográfica está habilitada para a porção de texto. Quando esta propriedade é definida como false, as verificações ortográficas para elementos de texto são suprimidas. Quando definida como true, a verificação ortográfica é permitida. O valor padrão é `false`.

```csharp
public bool SpellCheck { get; set; }
```

### Exemplos

O exemplo a seguir demonstra como habilitar a flag SpellCheck antes de salvar a apresentação:

```csharp
[C#]
using (var pres = new Presentation("input.pptx"))
{
    // Acesse a primeira porção de texto dentro da primeira forma no primeiro slide
    var portion = ((AutoShape)pres.Slides[0].Shapes[0]).TextFrame.Paragraphs[0].Portions[0];
    // Habilite a verificação ortográfica para esta porção de texto
    portion.PortionFormat.SpellCheck = true;
    // Salve a apresentação modificada
    pres.Save("output-with-spellcheck.pptx", SaveFormat.Pptx);
}
```

### Ver também

* classe [BasePortionFormat](../../baseportionformat)
* espaço de nomes [Aspose.Slides](../../baseportionformat)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->