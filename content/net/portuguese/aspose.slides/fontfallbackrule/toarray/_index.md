---
title: ToArray
second_title: Referência da API Aspose.Sildes for .NET
description: Cria e retorna uma matriz com todas as fontes FallBack para esta regra.
type: docs
weight: 110
url: /pt/aspose.slides/fontfallbackrule/toarray/
---
## ToArray() {#toarray}

Cria e retorna uma matriz com todas as fontes FallBack para esta regra.

```csharp
public string[] ToArray()
```

### Valor de retorno

Matriz de String

### Exemplos

```csharp
[C#]
// Crie uma regra que contém uma lista de fontes.
IFontFallBackRule newRule = new FontFallBackRule(0x3040, 0x309F, "MS Mincho, MS Gothic, Tahoma, Times New Roman");

// Obtenha todos os nomes de fontes como um array.
string[] fontNames = newRule.ToArray();
```

### Veja também

* classe [FontFallBackRule](../../fontfallbackrule)
* espaço de nomes [Aspose.Slides](../../fontfallbackrule)
* assembly [Aspose.Slides](../../../)

---

## ToArray(int, int) {#toarray_1}

Cria e retorna uma matriz com todas as fontes FallBack do intervalo especificado na lista.

```csharp
public string[] ToArray(int startIndex, int count)
```

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| startIndex | Int32 | Um índice da primeira fonte a ser adicionada. |
| count | Int32 | Um número de fontes a ser adicionado. |

### Valor de retorno

Matriz de String

### Exemplos

```csharp
[C#]
// Crie uma regra que contém uma lista de fontes.
IFontFallBackRule newRule = new FontFallBackRule(0x3040, 0x309F, "MS Mincho, MS Gothic, Tahoma, Times New Roman");

// Obtenha os dois últimos nomes de fontes como um array.
string[] fontNames = newRule.ToArray(2,2);
```

### Veja também

* classe [FontFallBackRule](../../fontfallbackrule)
* espaço de nomes [Aspose.Slides](../../fontfallbackrule)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->