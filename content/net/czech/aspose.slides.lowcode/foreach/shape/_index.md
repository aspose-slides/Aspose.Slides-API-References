---
title: Shape
second_title: Aspose.Sildes pro .NET - API reference
description: Iterujte každý Shapeaspose.slides.lowcode/foreach/shape v Presentationaspose.slides/presentation. Tvary budou iterovány ve všech typech snímků - Slideaspose.slides.lowcode/foreach/slide, MasterSlideaspose.slides.lowcode/foreach/masterslide a LayoutSlideaspose.slides.lowcode/foreach/layoutslide
type: docs
weight: 50
url: /cs/aspose.slides.lowcode/foreach/shape/
---
## Shape(Presentation, ForEachShapeCallback) {#shape_2}

Iterujte každý `Shape` v [`Presentation`](../../../aspose.slides/presentation). Tvary budou iterovány ve všech typech snímků - [`Slide`](../slide), [`MasterSlide`](../masterslide) a [`LayoutSlide`](../layoutslide)

```csharp
public static void Shape(Presentation pres, ForEachShapeCallback forEachShape)
```

| Parametr | Typ | Popis |
| --- | --- | --- |
| pres | Presentation | Presentation, ve kterém se mají iterovat tvary rozložení |
| forEachShape | ForEachShapeCallback | Callback, který bude vyvolán pro každý tvar |

### Příklady

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Shape(pres, (shape, slide, index) => 
    {
        System.Console.WriteLine($"{shape.Name}, index: {index}");
    });
} 
```

### Viz také

* třída [Presentation](../../../aspose.slides/presentation)
* delegát [ForEachShapeCallback](../../foreach.foreachshapecallback)
* třída [ForEach](../../foreach)
* jmenný prostor [Aspose.Slides.LowCode](../../foreach)
* sestavení [Aspose.Slides](../../../)

---

## Shape(Presentation, bool, ForEachShapeCallback) {#shape_1}

Iterujte každý [`Shape`](../shape) v [`Presentation`](../../../aspose.slides/presentation). Tvary budou iterovány ve všech typech snímků - [`Slide`](../slide), [`MasterSlide`](../masterslide), [`LayoutSlide`](../layoutslide) a [`NotesSlide`](../../../aspose.slides/notesslide) podle potřeby.

```csharp
public static void Shape(Presentation pres, bool includeNotes, ForEachShapeCallback forEachShape)
```

| Parametr | Typ | Popis |
| --- | --- | --- |
| pres | Presentation | Presentation, ve kterém se mají iterovat tvary rozložení |
| includeNotes | Boolean | Příznak určující, zda mají být zahrnuty NotesSlides při zpracování. |
| forEachShape | ForEachShapeCallback | Callback, který bude vyvolán pro každý tvar |

### Příklady

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Shape(pres, true, (shape, slide, index) => 
    {
        System.Console.WriteLine($"{shape.Name}, index: {index}");
    });
} 
```

### Viz také

* třída [Presentation](../../../aspose.slides/presentation)
* delegát [ForEachShapeCallback](../../foreach.foreachshapecallback)
* třída [ForEach](../../foreach)
* jmenný prostor [Aspose.Slides.LowCode](../../foreach)
* sestavení [Aspose.Slides](../../../)

---

## Shape(BaseSlide, ForEachShapeCallback) {#shape}

Iterujte každý [`Shape`](../shape) v [`BaseSlide`](../../../aspose.slides/baseslide). [`BaseSlide`](../../../aspose.slides/baseslide) je základní typ pro [`Slide`](../slide), [`MasterSlide`](../masterslide) a [`LayoutSlide`](../layoutslide)

```csharp
public static void Shape(BaseSlide baseSlide, ForEachShapeCallback forEachShape)
```

| Parametr | Typ | Popis |
| --- | --- | --- |
| baseSlide | BaseSlide | Snímek, ve kterém se mají iterovat tvary rozložení |
| forEachShape | ForEachShapeCallback | Callback, který bude vyvolán pro každý tvar |

### Příklady

```csharp
(Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Slide(pres, (slide, index) =>
    {
        ForEach.Shape(slide, (shape, baseSlide, shapeIndex) =>
        {
            System.Console.WriteLine($"{shape.Name}, index: {shapeIndex}");
        });
    });
} 
```

### Viz také

* třída [BaseSlide](../../../aspose.slides/baseslide)
* delegát [ForEachShapeCallback](../../foreach.foreachshapecallback)
* třída [ForEach](../../foreach)
* jmenný prostor [Aspose.Slides.LowCode](../../foreach)
* sestavení [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->