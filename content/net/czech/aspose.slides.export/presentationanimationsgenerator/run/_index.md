---
title: Run
second_title: Aspose.Sildes pro .NET API Reference
description: Spustí generování animačních událostí pro každý snímek.
type: docs
weight: 80
url: /cs/aspose.slides.export/presentationanimationsgenerator/run/
---
## Run(IEnumerable&lt;ISlide&gt;) {#run}

Spustí generování animačních událostí pro každý snímek.

```csharp
public void Run(IEnumerable<ISlide> slides)
```

### Příklady

```csharp
[C#]
using (Presentation presentation = new Presentation("animated.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(presentation.SlideSize.Size.ToSize()))
    using (var player = new PresentationPlayer(animationsGenerator, 33))
    {
        animationsGenerator.NewAnimation += animationPlayer =>
        {
            // zpracovat novou animaci
        };
        
        player.FrameTick += (sender, args) =>
        {
            // zpracovat tik rámce v nové animaci
        };
        
        animationsGenerator.Run(presentation.Slides);
    }
}
```

### Viz také

* rozhraní [ISlide](../../../aspose.slides/islide)
* třída [PresentationAnimationsGenerator](../../presentationanimationsgenerator)
* jmenný prostor [Aspose.Slides.Export](../../presentationanimationsgenerator)
* sestava [Aspose.Slides](../../../)

---

## Run(IEnumerable&lt;ISlide&gt;, int, FrameTickHandler) {#run_1}

Spustí generování animačních událostí pro každý snímek.

```csharp
public void Run(IEnumerable<ISlide> slides, int fps, FrameTickHandler onFrame)
```

### Příklady

```csharp
[C#]
using (Presentation presentation = new Presentation("animated.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(presentation.SlideSize.Size.ToSize()))
    {
        animationsGenerator.Run(presentation.Slides, 33, (sender, args) =>
        {
            sender.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save($"frame_{sender.FrameIndex}.png");
            };
        });
    }
}
```

### Viz také

* rozhraní [ISlide](../../../aspose.slides/islide)
* delegát [FrameTickHandler](../../presentationplayer.frametickhandler)
* třída [PresentationAnimationsGenerator](../../presentationanimationsgenerator)
* jmenný prostor [Aspose.Slides.Export](../../presentationanimationsgenerator)
* sestava [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->