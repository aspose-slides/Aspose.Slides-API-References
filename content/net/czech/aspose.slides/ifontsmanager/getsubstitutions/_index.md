---
title: GetSubstitutions
second_title: Aspose.Sildes pro .NET API Reference
description: Získává informace o písmech, která budou nahrazena při vykreslování prezentací.
type: docs
weight: 80
url: /cs/aspose.slides/ifontsmanager/getsubstitutions/
---
## GetSubstitutions() {#getsubstitutions}

Získává informace o písmech, která budou nahrazena při vykreslování prezentace.

```csharp
public IEnumerable<FontSubstitutionInfo> GetSubstitutions()
```

### Návratová hodnota

Kolekce všech substitucí písem [`FontSubstitutionInfo`](../../fontsubstitutioninfo).

### Příklady

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    foreach (var fontSubstitution in pres.FontsManager.GetSubstitutions())
    {
        Console.WriteLine("{0} -> {1}", fontSubstitution.OriginalFontName, fontSubstitution.SubstitutedFontName);
    }
}        
```

### Viz také

* třída [FontSubstitutionInfo](../../fontsubstitutioninfo)
* rozhraní [IFontsManager](../../ifontsmanager)
* jmenný prostor [Aspose.Slides](../../ifontsmanager)
* sestava [Aspose.Slides](../../../)

---

## GetSubstitutions(int[]) {#getsubstitutions_1}

Získává informace o písmech, která budou nahrazena při vykreslování určených snímků.

```csharp
public IEnumerable<FontSubstitutionInfo> GetSubstitutions(int[] slides)
```

| Parametr | Typ | Popis |
| --- | --- | --- |
| slides | Int32[] | Pole indexů snímků, pro které se mají získat informace o substitucích písem, počínaje od 1. |

### Návratová hodnota

Sbírka všech substitucí písem ([`FontSubstitutionInfo`](../../fontsubstitutioninfo)) pro určené snímky.

### Příklady

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    int[] targetSlides = { 1, 2, 5 };
    foreach (var fontSubstitution in pres.FontsManager.GetSubstitutions(targetSlides))
    {
        Console.WriteLine("{0} -> {1}", fontSubstitution.OriginalFontName, fontSubstitution.SubstitutedFontName);
    }
}
```

### Viz také

* třída [FontSubstitutionInfo](../../fontsubstitutioninfo)
* rozhraní [IFontsManager](../../ifontsmanager)
* jmenný prostor [Aspose.Slides](../../ifontsmanager)
* sestava [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->