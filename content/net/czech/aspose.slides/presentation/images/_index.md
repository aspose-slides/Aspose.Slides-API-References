---
title: Images
second_title: Aspose.Sildes pro .NET API Reference
description: Vrací kolekci všech obrázků v prezentaci. Pouze pro čtení IImageCollectionaspose.slides/iimagecollection.
type: docs
weight: 140
url: /cs/aspose.slides/presentation/images/
---
## Presentation.Images vlastnost

Vrací kolekci všech obrázků v prezentaci. Pouze pro čtení [`IImageCollection`](../../iimagecollection).

```csharp
public IImageCollection Images { get; }
```

### Příklady

Následující příklady ukazují, jak přidat obrázek jako BLOB do prezentace PowerPoint.

```csharp
[C#]
string pathToLargeImage = "large_image.jpg";
	// vytvoří novou prezentaci, do které bude obrázek přidán.
using (Presentation pres = new Presentation())
{
	using (FileStream fileStream = new FileStream(pathToLargeImage, FileMode.Open))
	{
		// Přidáme obrázek do prezentace – zvolíme chování KeepLocked, protože
		// NECHCEME přistupovat k souboru "largeImage.png".
		IPPImage img = pres.Images.AddImage(fileStream, LoadingStreamBehavior.KeepLocked);
		pres.Slides[0].Shapes.AddPictureFrame(ShapeType.Rectangle, 0, 0, 300, 200, img);
		// Uloží prezentaci. I když je výstup velký, spotřeba paměti zůstává nízká během životního cyklu objektu pres.
		pres.Save("presentationWithLargeImage.pptx", SaveFormat.Pptx);
	}
}
```

Následující příklady přidávají hypertextový odkaz k obrázku v prezentaci PowerPoint.

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    // Přidá obrázek do prezentace
    IPPImage image = pres.Images.AddImage(File.ReadAllBytes("image.png"));
    // Vytvoří rámeček obrázku na snímku 1 na základě dříve přidaného obrázku
    IPictureFrame pictureFrame = pres.Slides[0].Shapes.AddPictureFrame(ShapeType.Rectangle, 10, 10, 100, 100, image);
    pictureFrame.HyperlinkClick = new Hyperlink("https://www.aspose.com/");
    pictureFrame.HyperlinkClick.Tooltip = "More than 70% Fortune 100 companies trust Aspose APIs";
    pres.Save("pres-out.pptx", SaveFormat.Pptx);
}
```

### Viz také

* rozhraní [IImageCollection](../../iimagecollection)
* třída [Presentation](../../presentation)
* jmenný prostor [Aspose.Slides](../../presentation)
* assemblie [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->