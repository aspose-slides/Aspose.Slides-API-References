---
title: FieldType
second_title: Aspose.Sildes pro .NET API Reference
description: Představuje typ pole. Tato hodnota určuje, který text bude nastaven do části pole při jeho aktualizaci.
type: docs
weight: 4740
url: /cs/aspose.slides/fieldtype/
---
## FieldType třída

Representuje typ pole. Tato hodnota určuje, který text bude nastaven do části pole, když bude aktualizován.

```csharp
public sealed class FieldType : IFieldType
```

## Konstruktory

| Name | Description |
| --- | --- |
| [FieldType](fieldtype)(string) | Inicializuje novou instanci třídy FieldType. |

## Vlastnosti

| Name | Description |
| --- | --- |
| static [DateTime](../../aspose.slides/fieldtype/datetime) { get; } | Aktuální datum a čas ve výchozím formátu data a času pro renderovací aplikaci. Pouze pro čtení [`FieldType`](../fieldtype). |
| static [DateTime1](../../aspose.slides/fieldtype/datetime1) { get; } | Aktuální datum a čas v prvním předdefinovaném formátu (MM/DD/YYYY pro angličtinu). Pouze pro čtení [`FieldType`](../fieldtype). |
| static [DateTime10](../../aspose.slides/fieldtype/datetime10) { get; } | Aktuální datum a čas v desátém předdefinovaném formátu (hh:mm pro angličtinu). Pouze pro čtení [`FieldType`](../fieldtype). |
| static [DateTime11](../../aspose.slides/fieldtype/datetime11) { get; } | Aktuální datum a čas v jedenáctém předdefinovaném formátu (hh:mm:ss pro angličtinu). Pouze pro čtení [`FieldType`](../fieldtype). |
| static [DateTime12](../../aspose.slides/fieldtype/datetime12) { get; } | Aktuální datum a čas ve dvanáctém předdefinovaném formátu (hh:mm AM/PM pro angličtinu). Pouze pro čtení [`FieldType`](../fieldtype). |
| static [DateTime13](../../aspose.slides/fieldtype/datetime13) { get; } | Aktuální datum a čas ve třináctém předdefinovaném formátu (hh:mm:ss AM/PM pro angličtinu). Pouze pro čtení [`FieldType`](../fieldtype). |
| static [DateTime2](../../aspose.slides/fieldtype/datetime2) { get; } | Aktuální datum a čas ve druhém předdefinovaném formátu (Day, Month DD, YYYY pro angličtinu). Pouze pro čtení [`FieldType`](../fieldtype). |
| static [DateTime3](../../aspose.slides/fieldtype/datetime3) { get; } | Aktuální datum a čas ve třetím předdefinovaném formátu (DD Month YYYY pro angličtinu). Pouze pro čtení [`FieldType`](../fieldtype). |
| static [DateTime4](../../aspose.slides/fieldtype/datetime4) { get; } | Aktuální datum a čas ve čtvrtém předdefinovaném formátu (Month DD, YYYY pro angličtinu). Pouze pro čtení [`FieldType`](../fieldtype). |
| static [DateTime5](../../aspose.slides/fieldtype/datetime5) { get; } | Aktuální datum a čas v pátém předdefinovaném formátu (DD-Mon-YY pro angličtinu). Pouze pro čtení [`FieldType`](../fieldtype). |
| static [DateTime6](../../aspose.slides/fieldtype/datetime6) { get; } | Aktuální datum a čas ve šestém předdefinovaném formátu (Month YY pro angličtinu). Pouze pro čtení [`FieldType`](../fieldtype). |
| static [DateTime7](../../aspose.slides/fieldtype/datetime7) { get; } | Aktuální datum a čas v sedmém předdefinovaném formátu (Mon-YY pro angličtinu). Pouze pro čtení [`FieldType`](../fieldtype). |
| static [DateTime8](../../aspose.slides/fieldtype/datetime8) { get; } | Aktuální datum a čas v osmém předdefinovaném formátu (MM/DD/YYYY hh:mm AM/PM pro angličtinu). Pouze pro čtení [`FieldType`](../fieldtype). |
| static [DateTime9](../../aspose.slides/fieldtype/datetime9) { get; } | Aktuální datum a čas v devátém předdefinovaném formátu (MM/DD/YYYY hh:mm:ss AM/PM pro angličtinu). Pouze pro čtení [`FieldType`](../fieldtype). |
| static [Footer](../../aspose.slides/fieldtype/footer) { get; } | Patička snímku. Pouze pro čtení [`FieldType`](../fieldtype). |
| static [Header](../../aspose.slides/fieldtype/header) { get; } | Záhlaví snímku. Pouze pro čtení [`FieldType`](../fieldtype). |
| static [SlideNumber](../../aspose.slides/fieldtype/slidenumber) { get; } | Číslo aktuálního snímku. Pouze pro čtení [`FieldType`](../fieldtype). |
| [InternalString](../../aspose.slides/fieldtype/internalstring) { get; set; } | Vrací interní název tohoto objektu FieldType. Čtení/zápis String. |

## Metody

| Name | Description |
| --- | --- |
| override [Equals](../../aspose.slides/fieldtype/equals)(object) | Kontroluje, zda je toto pole rovno jinému. |
| override [GetHashCode](../../aspose.slides/fieldtype/gethashcode)() | Vrací hashkód tohoto objektu. |
| [operator ==](../../aspose.slides/fieldtype/op_equality) | Kontroluje, zda jsou dva objekty FieldType rovny. |
| [operator !=](../../aspose.slides/fieldtype/op_inequality) | Kontroluje, zda jsou dva objekty FieldType nerovny. |

### Viz také

* rozhraní [IFieldType](../ifieldtype)
* jmenný prostor [Aspose.Slides](../../aspose.slides)
* sestavení [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->