---
title: AddConnector
second_title: Aspose.Sildes pro .NET referenční příručka API
description: Vytvoří nový tvar spojení s výchozím šablonovým stylem a přidá jej na konec kolekce tvarů.
type: docs
weight: 120
url: /cs/aspose.slides/shapecollection/addconnector/
---
## AddConnector(ShapeType, float, float, float, float) {#addconnector}

Vytvoří nový tvar spojení s výchozím šablonovým stylem a přidá jej na konec kolekce tvarů.

```csharp
public IConnector AddConnector(ShapeType shapeType, float x, float y, float width, float height)
```

| Parametr | Typ | Popis |
| --- | --- | --- |
| shapeType | ShapeType | Typ [`ShapeType`](../../shapetype) spojovacího tvaru, který se má přidat. |
| x | Single | X-souřadnice rámečku spojení, v bodech. |
| y | Single | Y-souřadnice rámečku spojení, v bodech. |
| width | Single | Šířka rámečku spojení, v bodech. |
| height | Single | Výška rámečku spojení, v bodech. |

### Návratová hodnota

Nově vytvořený [`IConnector`](../../iconnector).

### Příklady

Následující příklad ukazuje, jak přidat spojení (zakřivené spojení) mezi dvěma tvary (elipsu a obdélník) v prezentaci PowerPoint.

```csharp
[C#]
// Vytvoří instanci třídy Presentation, která představuje soubor PPTX
using (Presentation input = new Presentation())
{
    // Přistupuje ke kolekci tvarů pro konkrétní snímek
    IShapeCollection shapes = input.Slides[0].Shapes;
    // Přidá automatický tvar Elipsa
    IAutoShape ellipse = shapes.AddAutoShape(ShapeType.Ellipse, 0, 100, 100, 100);
    // Přidá automatický tvar Obdélník
    IAutoShape rectangle = shapes.AddAutoShape(ShapeType.Rectangle, 100, 300, 100, 100);
    // Přidá tvar spojení do kolekce tvarů snímku
    IConnector connector = shapes.AddConnector(ShapeType.BentConnector2, 0, 0, 10, 10);
    // Propojí tvary pomocí spojení
    connector.StartShapeConnectedTo = ellipse;
    connector.EndShapeConnectedTo = rectangle;
    // Zavolá Reroute, který nastaví automatickou nejkratší cestu mezi tvary
    connector.Reroute();
    // Uloží prezentaci
    input.Save("Shapes-connector.pptx", SaveFormat.Pptx);
}
```

### Viz také

* rozhraní [IConnector](../../iconnector)
* výčtový typ [ShapeType](../../shapetype)
* třída [ShapeCollection](../../shapecollection)
* jmenný prostor [Aspose.Slides](../../shapecollection)
* assembly [Aspose.Slides](../../../)

---

## AddConnector(ShapeType, float, float, float, float, bool) {#addconnector_1}

Vytvoří nový tvar spojení a přidá jej na konec kolekce tvarů, volitelně použije výchozí šablonový styl.

```csharp
public IConnector AddConnector(ShapeType shapeType, float x, float y, float width, float height, 
    bool createFromTemplate)
```

| Parametr | Typ | Popis |
| --- | --- | --- |
| shapeType | ShapeType | Typ [`ShapeType`](../../shapetype) spojovacího tvaru, který se má vytvořit. |
| x | Single | X-souřadnice rámečku spojení, v bodech. |
| y | Single | Y-souřadnice rámečku spojení, v bodech. |
| width | Single | Šířka rámečku spojení, v bodech. |
| height | Single | Výška rámečku spojení, v bodech. |
| createFromTemplate | Boolean | True pro použití výchozího šablonového stylu (neprázdný název, jednoduchý styl); false pro vytvoření spojení s výchozími hodnotami vlastností. |

### Návratová hodnota

Nově vytvořený [`IConnector`](../../iconnector).

### Viz také

* rozhraní [IConnector](../../iconnector)
* výčtový typ [ShapeType](../../shapetype)
* třída [ShapeCollection](../../shapecollection)
* jmenný prostor [Aspose.Slides](../../shapecollection)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->