---
title: AddOleObjectFrame
second_title: Aspose.Sildes pro .NET – reference API
description: Vytvoří nový OLE objektový rámec a přidá jej na konec kolekce tvarů.
type: docs
weight: 150
url: /cs/aspose.slides/shapecollection/addoleobjectframe/
---
## AddOleObjectFrame(float, float, float, float, IOleEmbeddedDataInfo) {#addoleobjectframe}

Vytvoří nový OLE objektový rámec a přidá jej na konec kolekce tvarů.

```csharp
public IOleObjectFrame AddOleObjectFrame(float x, float y, float width, float height, 
    IOleEmbeddedDataInfo dataInfo)
```

| Parameter | Type | Description |
| --- | --- | --- |
| x | Single | Souřadnice x nového OLE rámce v bodech. |
| y | Single | Souřadnice y nového OLE rámce v bodech. |
| width | Single | Šířka nového OLE rámce v bodech. |
| height | Single | Výška nového OLE rámce v bodech. |
| dataInfo | IOleEmbeddedDataInfo | Informace o vložených OLE datech ([`IOleEmbeddedDataInfo`](../../ioleembeddeddatainfo)). |

### Návratová hodnota

Nově vytvořený [`IOleObjectFrame`](../../ioleobjectframe).

### Příklady

Následující příklady ukazují, jak přidávat OLE objektové rámy do snímků prezentace PowerPoint.

```csharp
[C#]
// Vytvoří instanci třídy Presentation, která představuje soubor PPTX
using (Presentation pres = new Presentation())
{
    // Přistupuje k prvnímu snímku
    ISlide sld = pres.Slides[0];
    // Načte soubor Excel do proudu
    MemoryStream mstream = new MemoryStream();
    using (FileStream fs = new FileStream("book1.xlsx", FileMode.Open, FileAccess.Read))
    {
        byte[] buf = new byte[4096];
        while (true)
        {
            int bytesRead = fs.Read(buf, 0, buf.Length);
            if (bytesRead <= 0)
                break;
            mstream.Write(buf, 0, bytesRead);
        }
    }
    // Vytvoří datový objekt pro vložení
    IOleEmbeddedDataInfo dataInfo = new OleEmbeddedDataInfo(mstream.ToArray(), "xlsx");
    // Přidá tvar Ole Object Frame
    IOleObjectFrame oleObjectFrame = sld.Shapes.AddOleObjectFrame(0, 0, pres.SlideSize.Size.Width,
        pres.SlideSize.Size.Height, dataInfo);
    // Zapíše soubor PPTX na disk
    pres.Save("OleEmbed_out.pptx", SaveFormat.Pptx);
}
```

### Viz také

* rozhraní [IOleObjectFrame](../../ioleobjectframe)
* rozhraní [IOleEmbeddedDataInfo](../../ioleembeddeddatainfo)
* třída [ShapeCollection](../../shapecollection)
* jmenný prostor [Aspose.Slides](../../shapecollection)
* sestavení [Aspose.Slides](../../../)

---

## AddOleObjectFrame(float, float, float, float, string, string) {#addoleobjectframe_1}

Vytvoří nový OLE objektový rámec a přidá jej na konec kolekce tvarů.

```csharp
public IOleObjectFrame AddOleObjectFrame(float x, float y, float width, float height, 
    string className, string path)
```

| Parameter | Type | Description |
| --- | --- | --- |
| x | Single | Souřadnice x nového OLE rámce v bodech. |
| y | Single | Souřadnice y nového OLE rámce v bodech. |
| width | Single | Šířka nového OLE rámce v bodech. |
| height | Single | Výška nového OLE rámce v bodech. |
| className | String | Název třídy OLE objektu. |
| path | String | Cesta k propojenému souboru. |

### Návratová hodnota

Nově vytvořený [`IOleObjectFrame`](../../ioleobjectframe).

### Viz také

* rozhraní [IOleObjectFrame](../../ioleobjectframe)
* třída [ShapeCollection](../../shapecollection)
* jmenný prostor [Aspose.Slides](../../shapecollection)
* sestavení [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->