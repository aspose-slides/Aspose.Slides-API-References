---
title: WriteAsSvg
second_title: Aspose.Sildes för .NET API-referens
description: Sparar bildens innehåll som en SVG-fil.
type: docs
weight: 140
url: /sv/aspose.slides/slide/writeassvg/
---
## WriteAsSvg(Stream) {#writeassvg}

Sparar bildens innehåll som en SVG-fil.

```csharp
public void WriteAsSvg(Stream stream)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| stream | Stream | Måldataström |

### Exempel

Följande kodexempel visar hur du konverterar den första bilden i en PowerPoint-presentation till en SVG-fil.

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (Stream fileStream = System.IO.File.Create("slide_1.svg"))
    {
        // Sparar den första bilden som en SVG-fil
        pres.Slides[0].WriteAsSvg(fileStream);
    }
}
```

### Se även

* klass [Slide](../../slide)
* namnrymd [Aspose.Slides](../../slide)
* assembly [Aspose.Slides](../../../)

---

## WriteAsSvg(Stream, ISVGOptions) {#writeassvg_1}

Sparar bildens innehåll som en SVG-fil.

```csharp
public void WriteAsSvg(Stream stream, ISVGOptions svgOptions)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| stream | Stream | Måldataström |
| svgOptions | ISVGOptions | SVG-genereringsalternativ |

### Exempel

Följande kodexempel visar hur du konverterar den första bilden i en PowerPoint-presentation till en SVG-fil med alternativ.

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (Stream fileStream = System.IO.File.Create("slide_1.svg"))
    {
        var options = new SVGOptions() { VectorizeText = true };
        // Sparar den första bilden som en SVG-fil
        pres.Slides[0].WriteAsSvg(fileStream, options);
    }
}
```

### Se även

* gränssnitt [ISVGOptions](../../../aspose.slides.export/isvgoptions)
* klass [Slide](../../slide)
* namnrymd [Aspose.Slides](../../slide)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->