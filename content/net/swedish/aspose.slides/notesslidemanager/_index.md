---
title: NotesSlideManager
second_title: Aspose.Sildes för .NET API-referens
description: Hantera anteckningsbilder.
type: docs
weight: 9140
url: /sv/aspose.slides/notesslidemanager/
---
## NotesSlideManager klass

Hantera anteckningsbilder.

```csharp
public sealed class NotesSlideManager : DomObject<Slide>, INotesSlideManager
```

## Egenskaper

| Namn | Beskrivning |
| --- | --- |
| [NotesSlide](../../aspose.slides/notesslidemanager/notesslide) { get; } | Returnerar notes-bilden för den aktuella bilden. Returnerar null om bilden inte har en notes-bild. Skrivskyddad [`INotesSlide`](../inotesslide). |

## Metoder

| Namn | Beskrivning |
| --- | --- |
| [AddNotesSlide](../../aspose.slides/notesslidemanager/addnotesslide)() | Returnerar notes-bilden för den aktuella bilden och skapar en om den inte finns. |
| [RemoveNotesSlide](../../aspose.slides/notesslidemanager/removenotesslide)() | Tar bort notes-bilden för den aktuella bilden. |

### Exempel

Följande exempel visar hur man lägger till anteckningar till en specifik PowerPoint-presentationbild.

```csharp
[C#]
	// Skapa ett Presentation-objekt som representerar en presentationsfil
	using(Presentation presentation = new Presentation(dataDir + "AccessSlides.pptx")) {
	  // Lägg till anteckningar på den första bilden
	  INotesSlideManager mgr = presentation.Slides[0].NotesSlideManager;
	  INotesSlide noteSlide = mgr.AddNotesSlide();
	  noteSlide.NotesTextFrame.Text = "Your Notes";
	  // Spara presentationen till disk
	  presentation.Save(dataDir + "RemoveNotesAtSpecificSlide_out.pptx", SaveFormat.Pptx);
	}
```

Följande exempel visar hur man tar bort anteckningar från en specifik bild i en PowerPoint-presentation.

```csharp
[C#]
	// Skapa ett Presentation-objekt som representerar en presentationsfil
	using(Presentation presentation = new Presentation(dataDir + "AccessSlides.pptx")) {
	  // Tar bort anteckningar från den första bilden
	  INotesSlideManager mgr = presentation.Slides[0].NotesSlideManager;
	  mgr.RemoveNotesSlide();
	  // Spara presentationen till disk
	  presentation.Save(dataDir + "RemoveNotesAtSpecificSlide_out.pptx", SaveFormat.Pptx);
	}
```

### Se även

* klass [DomObject&lt;TParent&gt;](../domobject-1)
* klass [Slide](../slide)
* gränssnitt [INotesSlideManager](../inotesslidemanager)
* namnrymd [Aspose.Slides](../../aspose.slides)
* samling [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->