---
title: AddFromPdf
second_title: Aspose.Sildes för .NET API-referens
description: Skapar bilder från PDF-dokumentet och lägger till dem i slutet av samlingen.
type: docs
weight: 50
url: /sv/aspose.slides/islidecollection/addfrompdf/
---
## AddFromPdf(string) {#addfrompdf_2}

Skapar bilder från PDF-dokumentet och lägger till dem i slutet av samlingen.

```csharp
public ISlide[] AddFromPdf(string path)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| path | String | En sökväg till PDF-dokumentet |

### Returvärde

Tillagda bilder

### Exempel

Exempel:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    pres.Slides.AddFromPdf("document.pdf");
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Se även

* gränssnitt [ISlide](../../islide)
* gränssnitt [ISlideCollection](../../islidecollection)
* namnrymd [Aspose.Slides](../../islidecollection)
* assembly [Aspose.Slides](../../../)

---

## AddFromPdf(string, PdfImportOptions) {#addfrompdf_3}

Skapar bilder från PDF-dokumentet och lägger till dem i slutet av samlingen med beaktande av pdf-importalternativen.

```csharp
public ISlide[] AddFromPdf(string path, PdfImportOptions pdfImportOptions)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| path | String | En sökväg till PDF-dokumentet |
| pdfImportOptions | PdfImportOptions | Alternativ för pdf-import |

### Returvärde

Tillagda bilder

### Exempel

Exempel:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    pres.Slides.AddFromPdf("document.pdf, new PdfImportOptions { DetectTables = true });
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Se även

* gränssnitt [ISlide](../../islide)
* klass [PdfImportOptions](../../../aspose.slides.import/pdfimportoptions)
* gränssnitt [ISlideCollection](../../islidecollection)
* namnrymd [Aspose.Slides](../../islidecollection)
* assembly [Aspose.Slides](../../../)

---

## AddFromPdf(Stream, PdfImportOptions) {#addfrompdf_1}

Skapar bilder från PDF-dokumentet och lägger till dem i slutet av samlingen.

```csharp
public ISlide[] AddFromPdf(Stream pdfStream, PdfImportOptions pdfImportOptions)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| pdfStream | Stream | En ström som används som källa för PDF-dokumentet |
| pdfImportOptions | PdfImportOptions | Alternativ för pdf-import |

### Returvärde

Tillagda bilder

### Exempel

Exempel:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    using (Stream stream = new FileStream("document.pdf", FileMode.Open, FileAccess.Read, FileShare.Read))
    {
        pres.Slides.AddFromPdf(stream, new PdfImportOptions { DetectTables = true });
    }
    
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Se även

* gränssnitt [ISlide](../../islide)
* klass [PdfImportOptions](../../../aspose.slides.import/pdfimportoptions)
* gränssnitt [ISlideCollection](../../islidecollection)
* namnrymd [Aspose.Slides](../../islidecollection)
* assembly [Aspose.Slides](../../../)

---

## AddFromPdf(Stream) {#addfrompdf}

Skapar bilder från PDF-dokumentet och lägger till dem i slutet av samlingen.

```csharp
public ISlide[] AddFromPdf(Stream pdfStream)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| pdfStream | Stream | En ström som används som källa för PDF-dokumentet |

### Returvärde

Tillagda bilder

### Exempel

Exempel:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    using (Stream stream = new FileStream("document.pdf", FileMode.Open, FileAccess.Read, FileShare.Read))
    {
        pres.Slides.AddFromPdf(stream);
    }
    
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Se även

* gränssnitt [ISlide](../../islide)
* gränssnitt [ISlideCollection](../../islidecollection)
* namnrymd [Aspose.Slides](../../islidecollection)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->