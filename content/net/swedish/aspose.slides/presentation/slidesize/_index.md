---
title: SlideSize
second_title: Aspose.Sildes för .NET API-referens
description: Returnerar slide size-objekt. Skrivskyddad ISlideSizeaspose.slides/islidesize.
type: docs
weight: 260
url: /sv/aspose.slides/presentation/slidesize/
---
## Presentation.SlideSize egenskap

Returnerar slide size-objekt. Skrivskyddad [`ISlideSize`](../../islidesize).

```csharp
public ISlideSize SlideSize { get; }
```

### Exempel

Följande exempel visar hur du ändrar slide size i en PowerPoint-presentation.

```csharp
[C#]
using (Presentation pres = new Presentation("pres-4x3-aspect-ratio.pptx"))
{
    pres.SlideSize.SetSize(SlideSizeType.OnScreen16x9, SlideSizeScaleType.DoNotScale);
    pres.Save("pres-4x3-aspect-ratio.pptx", SaveFormat.Pptx);
}
```

Följande exempel visar hur du ställer in slide size med hänsyn till innehållsskalning för en PowerPoint-presentation.

```csharp
[C#]
// Instansiera ett Presentation-objekt som representerar en presentationsfil
using(Presentation presentation = new Presentation("AccessSlides.pptx")) {
  using(Presentation auxPresentation = new Presentation()) {
    ISlide slide = presentation.Slides[0];
    // Sätt slide size för genererade presentationer till samma som källan
    presentation.SlideSize.SetSize(540, 720, SlideSizeScaleType.EnsureFit); // Metoden SetSize används för att ange slide size med skalat innehåll för att säkerställa passning
    presentation.SlideSize.SetSize(SlideSizeType.A4Paper, SlideSizeScaleType.Maximize); // Metoden SetSize används för att ange slide size med maximal storlek på innehållet
    // Spara Presentation till disk
    auxPresentation.Save("Set_Size&Type_out.pptx", SaveFormat.Pptx);
  }
}
```

Följande exempel visar hur du specificerar anpassade slide sizes i en PowerPoint-presentation.

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    pres.SlideSize.SetSize(780, 540, SlideSizeScaleType.DoNotScale); // A4-pappersstorlek
    pres.Save("pres-a4-slide-size.pptx", SaveFormat.Pptx);
}
```

### Se också

* gränssnitt [ISlideSize](../../islidesize)
* klass [Presentation](../../presentation)
* namnutrymme [Aspose.Slides](../../presentation)
* samling [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->