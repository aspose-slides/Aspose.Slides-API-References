---
title: CompressImage
second_title: Aspose.Sildes för .NET API-referens
description: Komprimerar bilden genom att minska dess storlek baserat på formens storlek och angiven upplösning. Eventuellt raderas också beskurna områden.
type: docs
weight: 190
url: /sv/aspose.slides/ipicturefillformat/compressimage/
---
## CompressImage(bool, PicturesCompression) {#compressimage}

Komprimerar bilden genom att minska dess storlek baserat på formens storlek och angiven upplösning. Eventuellt raderas även beskurna områden.

```csharp
public bool CompressImage(bool deleteCroppedAreasOfImage, PicturesCompression resolution)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| deleteCroppedAreasOfImage | Boolean | Om true, kommer metoden att ta bort de beskurna områdena i bilden, vilket eventuellt ytterligare minskar dess storlek. |
| resolution | PicturesCompression | Målupplösningen för komprimering, specificerad som ett värde av enumet [`PicturesCompression`](../../../aspose.slides.export/picturescompression). |

### Returvärde

Ett Boolean som indikerar om bilden har komprimerats framgångsrikt. Returnerar `true` om bilden har ändrat storlek eller beskärts, annars `false`.

### Undantag

| undantag | villkor |
| --- | --- |
| ArgumentException | Kastas när upplösningen inte är ett giltigt värde. |

### Anmärkningar

Denna metod ändrar bildens storlek och upplösning på liknande sätt som PowerPoints funktion "Picture Format -> Compress Pictures".

### Exempel

Följande exempel visar hur `CompressImage`-metoden kan användas för att minska storleken på en bild i en presentation genom att ange en målupplösning och ta bort beskurna områden:

```csharp
using (Presentation presentation = new Presentation("demo.pptx"))
{
    ISlide slide = presentation.Slides[0];
    IPictureFrame picFrame = slide.Shapes[0] as IPictureFrame;
    // Komprimera bilden med en målupplösning på 150 DPI (webbupplösning) och ta bort beskurna områden
    bool result = picFrame.PictureFormat.CompressImage(true, PicturesCompression.Dpi150);
}
```

### Se också

* enum [PicturesCompression](../../../aspose.slides.export/picturescompression)
* gränssnitt [IPictureFillFormat](../../ipicturefillformat)
* namnrymd [Aspose.Slides](../../ipicturefillformat)
* samling [Aspose.Slides](../../../)

---

## CompressImage(bool, float) {#compressimage_1}

Komprimerar bilden genom att minska dess storlek baserat på formens storlek och angiven upplösning. Eventuellt raderas även beskurna områden.

```csharp
public bool CompressImage(bool deleteCroppedAreasOfImage, float resolution)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| deleteCroppedAreasOfImage | Boolean | Om true, kommer metoden att ta bort de beskurna områdena i bilden, vilket eventuellt ytterligare minskar dess storlek. |
| resolution | Single | Målupplösningen i DPI. Detta värde måste vara positivt och definierar hur bilden kommer att ändras i storlek. |

### Returvärde

Ett Boolean som indikerar om bilden har komprimerats framgångsrikt. Returnerar `true` om bilden har ändrat storlek eller beskärts, annars `false`.

### Undantag

| undantag | villkor |
| --- | --- |
| ArgumentException | Kastas när upplösningen inte är ett positivt värde. |

### Anmärkningar

Denna metod ändrar bildens storlek och upplösning på liknande sätt som PowerPoints funktion "Picture Format -> Compress Pictures".

### Exempel

Följande exempel visar hur `CompressImage`-metoden kan användas för att minska storleken på en bild i en presentation genom att ange en målupplösning och ta bort beskurna områden:

```csharp
[C#]
using (Presentation presentation = new Presentation("demo.pptx"))
{
    ISlide slide = presentation.Slides[0];

    // Hämtar PictureFrame
    IPictureFrame picFrame = slide.Shapes[0] as IPictureFrame;

    // Komprimera bilden med en målupplösning på 150 DPI (Web-upplösning) och ta bort beskurna områden
    bool result=picFrame.PictureFormat.CompressImage(true, 150f); // Web-upplösning
}
```

### Se också

* gränssnitt [IPictureFillFormat](../../ipicturefillformat)
* namnrymd [Aspose.Slides](../../ipicturefillformat)
* samling [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->