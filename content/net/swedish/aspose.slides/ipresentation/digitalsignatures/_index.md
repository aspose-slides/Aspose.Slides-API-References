---
title: DigitalSignatures
second_title: Aspose.Sildes för .NET API-referens
description: Returnerar samlingen av signaturer som används för att signera presentationen. Endast läsning IDigitalSignatureCollectionaspose.slides/idigitalsignaturecollection.
type: docs
weight: 90
url: /sv/aspose.slides/ipresentation/digitalsignatures/
---
## IPresentation.DigitalSignatures egenskap

Returnerar samlingen av signaturer som används för att signera presentationen. Endast läsning [`IDigitalSignatureCollection`](../../idigitalsignaturecollection).

```csharp
public IDigitalSignatureCollection DigitalSignatures { get; }
```

### Exempel

```csharp
[C#]
using (Presentation pres = new Presentation("SomePresentationSigned.pptx"))
{
    if (pres.DigitalSignatures.Count > 0)
    {
        bool allSignaturesAreValid = true;

        Console.WriteLine("Signatures used to sign the presentation: ");
        foreach (DigitalSignature signature in pres.DigitalSignatures)
        {
            Console.WriteLine(signature.Certificate.SubjectName.Name + ", "
                    + signature.SignTime.ToString("yyyy-MM-dd HH:mm") + " -- " + (signature.IsValid ? "VALID" : "INVALID"));
            allSignaturesAreValid &= signature.IsValid;
        }

        if (allSignaturesAreValid)
            Console.WriteLine("Presentation is genuine, all signatures are valid.");
        else
            Console.WriteLine("Presentation has been modified since signing.");
    }
}
```

### Se även

* gränssnitt [IDigitalSignatureCollection](../../idigitalsignaturecollection)
* gränssnitt [IPresentation](../../ipresentation)
* namnrymd [Aspose.Slides](../../ipresentation)
* samling [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->