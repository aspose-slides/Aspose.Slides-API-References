---  
title: WriteAsEmf
second_title: Aspose.Sildes für .NET API-Referenz  
description: Speichert das SVG-Bild als EMF-Datei.
type: docs  
weight: 60  
url: /de/aspose.slides/svgimage/writeasemf/
---  
  
## SvgImage.WriteAsEmf-Methode  
  
Speichert das SVG-Bild als EMF-Datei.  
  
```csharp  
public void WriteAsEmf(Stream stream)  
```  
  
| Parameter | Typ | Beschreibung |  
| --- | --- | --- |  
| stream | Stream | Zielstream |  
  
### Ausnahmen  
  
| Ausnahme | Bedingung |  
| --- | --- |  
| ArgumentNullException | Zielstream ist **null** |  
  
### Beispiele  
  
Das folgende Beispiel zeigt, wie man das SVG-Bild in die Metadatei speichert.  
  
```csharp  
[C#]  
// Erstellt das neue SVG-Bild  
ISvgImage svgImage = new SvgImage(System.IO.File.ReadAllText("content.svg"));  
  
// Speichert das SVG-Bild als Metadatei  
using (var fileStream = System.IO.File.OpenWrite("SvgAsEmf.emf"))  
    svgImage.WriteAsEmf(fileStream);  
```  
  
Dieses Beispiel demonstriert, wie man das SVG-Bild als Metadatei zur Präsentationsbildersammlung hinzufügt.  
  
```csharp  
[C#]  
using (Presentation pres = new Presentation())  
{  
    // Erstellt das neue SVG-Bild  
    ISvgImage svgImage = new SvgImage(System.IO.File.ReadAllText("content.svg"));  
    using (var memStream = new MemoryStream())  
    {  
        // Speichert das SVG-Bild als Metadatei  
        svgImage.WriteAsEmf(memStream);  
        // Fügt die Metadatei zur Bildersammlung hinzu  
        pres.Images.AddImage(memStream.ToArray());  
    }  
}  
```  
  
### Siehe Auch  
  
* Klasse [SvgImage](../../svgimage)  
* Namespace [Aspose.Slides](../../svgimage)  
* Assembly [Aspose.Slides](../../../)  
  
<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->