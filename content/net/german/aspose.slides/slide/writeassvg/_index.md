---
title: WriteAsSvg
second_title: Aspose.Slides für .NET API-Referenz
description: Speichert den Inhalt der Folie als SVG-Datei.
type: docs
weight: 140
url: /de/aspose.slides/slide/writeassvg/
---

## WriteAsSvg(Stream) {#writeassvg}

Speichert den Inhalt der Folie als SVG-Datei.

```csharp
public void WriteAsSvg(Stream stream)
```

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| stream | Stream | Zielstream |

### Beispiele

Das folgende Codebeispiel demonstriert, wie man die erste Folie einer PowerPoint-Präsentation in eine SVG-Datei umwandelt.

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (Stream fileStream = System.IO.File.Create("slide_1.svg"))
    {
        // Speichert die erste Folie als SVG-Datei
        pres.Slides[0].WriteAsSvg(fileStream);
    }
}
```

### Siehe auch

* Klasse [Slide](../../slide)
* Namensraum [Aspose.Slides](../../slide)
* Assembly [Aspose.Slides](../../../)

---

## WriteAsSvg(Stream, ISVGOptions) {#writeassvg_1}

Speichert den Inhalt der Folie als SVG-Datei.

```csharp
public void WriteAsSvg(Stream stream, ISVGOptions svgOptions)
```

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| stream | Stream | Zielstream |
| svgOptions | ISVGOptions | Optionen zur SVG-Generierung |

### Beispiele

Das folgende Codebeispiel demonstriert, wie man die erste Folie einer PowerPoint-Präsentation in eine SVG-Datei mit Optionen umwandelt.

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (Stream fileStream = System.IO.File.Create("slide_1.svg"))
    {
        var options = new SVGOptions() { VectorizeText = true };
        // Speichert die erste Folie als SVG-Datei
        pres.Slides[0].WriteAsSvg(fileStream, options);
    }
}
```

### Siehe auch

* Schnittstelle [ISVGOptions](../../../aspose.slides.export/isvgoptions)
* Klasse [Slide](../../slide)
* Namensraum [Aspose.Slides](../../slide)
* Assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->