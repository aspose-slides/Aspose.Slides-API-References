---
title: PresentationLockingBehavior
second_title: Aspose.Sildes für .NET API Referenz
description: Stellt das Verhalten dar, wie die IPresentation../ipresentation Quelldatei Datei oder Stream beim Laden und Arbeiten mit einer Instanz von IPresentation../ipresentation behandelt wird.
type: docs
weight: 9360
url: /de/aspose.slides/presentationlockingbehavior/
---

## PresentationLockingBehavior Aufzählung

Stellt das Verhalten dar, wie die [`IPresentation`](../ipresentation) Quelle (Datei oder Stream) beim Laden und Arbeiten mit einer Instanz von [`IPresentation`](../ipresentation) behandelt wird.

```csharp
public enum PresentationLockingBehavior
```

### Werte

| Name | Wert | Beschreibung |
| --- | --- | --- |
| LoadAndRelease | `0` | Die Quelle wird nur für die Dauer der Ausführung des [`IPresentation`](../ipresentation) Konstruktors gesperrt. Wenn [`IsTemporaryFilesAllowed`](../iblobmanagementoptions/istemporaryfilesallowed) auf false gesetzt ist, werden alle BLOBs in den Arbeitsspeicher geladen. Andernfalls können andere Mittel wie temporäre Dateien verwendet werden. Dieses Verhalten ist langsamer als KeepLocked, und wenn es möglich ist, das Eigentum an der Quelle an [`IPresentation`](../ipresentation) zu übergeben, wird empfohlen, KeepLocked zu verwenden. |
| KeepLocked | `1` | Die Quelle wird für die gesamte Lebensdauer der [`IPresentation`](../ipresentation) Instanz gesperrt, bis sie entsorgt wird. [`IsTemporaryFilesAllowed`](../iblobmanagementoptions/istemporaryfilesallowed) muss auf true gesetzt sein, um dieses Verhalten zu verwenden, andernfalls wird eine Ausnahme ausgelöst. Dieses Verhalten wird empfohlen, es ist schneller und verbraucht weniger Speicher als LoadAndRelease. |

### Bemerkungen

Die Quelle ist der Parameter, der an den [`IPresentation`](../ipresentation) Konstruktor übergeben wird. Im folgenden Beispiel ist die Quelle die Datei "pres.pptx":

```csharp
LoadOptions loadOptions = new LoadOptions { 
  BlobManagementOptions = { PresentationLockingBehavior = PresentationLockingBehavior.KeepLocked } };
using (IPresentation pres = new Presentation("pres.pptx", loadOptions)) { }
```

In diesem Beispiel wird die Quelle (Datei "pres.pptx") für die Lebensdauer der [`IPresentation`](../ipresentation) Instanz gesperrt, d.h. sie kann von einem anderen Prozess nicht geändert oder gelöscht werden.

### Siehe Auch

* Namespace [Aspose.Slides](../../aspose.slides)
* Assembly [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->