---
title: CompressImage
second_title: Aspose.Sildes .NET API referencia
description: Lecsökkenti a képet a mérete csökkentésével az alakzat mérete és a megadott felbontás alapján. Opcionálisan törli a levágott területeket is.
type: docs
weight: 180
url: /hu/aspose.slides/picturefillformat/compressimage/
---
## CompressImage(bool, PicturesCompression) {#compressimage}

Lecsökkenti a kép méretét a alakzat mérete és a megadott felbontás alapján. Opcionálisan törli a levágott területeket is.

```csharp
public bool CompressImage(bool deleteCroppedAreasOfImage, PicturesCompression resolution)
```

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| deleteCroppedAreasOfImage | Boolean | Ha igaz, a metódus eltávolítja a kép levágott területeit, ami tovább csökkentheti a méretét. |
| resolution | PicturesCompression | A tömörítés célfelbontása, amely a [`PicturesCompression`](../../../aspose.slides.export/picturescompression) enum egy értékeként van megadva. |

### Visszatérési érték

Egy Boolean érték, amely azt jelzi, hogy a kép sikeresen tömörült-e. `true` értéket ad vissza, ha a képet átméretezték vagy levágták, egyébként `false`.

### Kivétel

| Kivétel | Feltétel |
| --- | --- |
| ArgumentException | Akkor kerül dobásra, ha a felbontás nem érvényes érték. |

### Megjegyzés

Ez a metódus megváltoztatja a kép méretét és felbontását, hasonlóan a PowerPoint "Picture Format -> Compress Pictures" funkciójához.

### Példák

```csharp
using (Presentation presentation = new Presentation("demo.pptx"))
{
    ISlide slide = presentation.Slides[0];
    IPictureFrame picFrame = slide.Shapes[0] as IPictureFrame;
    // Tömöríti a képet 150 DPI (webes felbontás) célfelbontással, és eltávolítja a levágott területeket
    bool result = picFrame.PictureFormat.CompressImage(true, PicturesCompression.Dpi150);
}
```

### Lásd még

* enum [PicturesCompression](../../../aspose.slides.export/picturescompression)
* osztály [PictureFillFormat](../../picturefillformat)
* névtér [Aspose.Slides](../../picturefillformat)
* összeszerelés [Aspose.Slides](../../../)

---

## CompressImage(bool, float) {#compressimage_1}

Lecsökkenti a kép méretét a alakzat mérete és a megadott felbontás alapján. Opcionálisan törli a levágott területeket is.

```csharp
public bool CompressImage(bool deleteCroppedAreasOfImage, float resolution)
```

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| deleteCroppedAreasOfImage | Boolean | Ha igaz, a metódus eltávolítja a kép levágott területeit, ami tovább csökkentheti a méretét. |
| resolution | Single | A célfelbontás DPI-ben. Ennek az értéknek pozitívnak kell lennie, és meghatározza, hogyan lesz a kép átméretezve. |

### Visszatérési érték

Egy Boolean érték, amely azt jelzi, hogy a kép sikeresen tömörült-e. `true` értéket ad vissza, ha a képet átméretezték vagy levágták, egyébként `false`.

### Kivétel

| Kivétel | Feltétel |
| --- | --- |
| ArgumentException | Akkor kerül dobásra, ha a felbontás nem pozitív érték. |

### Megjegyzés

Ez a metódus megváltoztatja a kép méretét és felbontását, hasonlóan a PowerPoint "Picture Format -> Compress Pictures" funkciójához.

### Példák

```csharp
[C#]
using (Presentation presentation = new Presentation("demo.pptx"))
{
    ISlide slide = presentation.Slides[0];

    // Lekéri a PictureFrame-et
    IPictureFrame picFrame = slide.Shapes[0] as IPictureFrame;

    // Tömöríti a képet 150 DPI (webes felbontás) célfelbontással, és eltávolítja a levágott területeket
    bool result=picFrame.PictureFormat.CompressImage(true, 150f); // Web felbontás
}
```

### Lásd még

* osztály [PictureFillFormat](../../picturefillformat)
* névtér [Aspose.Slides](../../picturefillformat)
* összeszerelés [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->