---
title: AddTextFrame
second_title: Aspose.Sildes .NET API referencia
description: Új TextFrame-et ad egy alakzathoz. Ha az alakzat már rendelkezik TextFrame-mel, akkor egyszerűen megváltoztatja annak szövegét.
type: docs
weight: 60
url: /hu/aspose.slides/autoshape/addtextframe/
---
## AutoShape.AddTextFrame metódus

Új TextFrame-et ad hozzá egy alakzathoz. Ha az alakzat már rendelkezik TextFrame-mel, akkor egyszerűen megváltoztatja annak szövegét.

```csharp
public ITextFrame AddTextFrame(string text)
```

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| text | String | Az új TextFrame alapértelmezett szövege. |

### Példák

Az alábbi példa kód bemutatja, hogyan lehet vízjel szöveget hozzáadni egy PowerPoint prezentációhoz.

```csharp
[C#]
	using (var presentation = new Presentation())
	{
		ISlide slide = presentation.Slides[0];
		IAutoShape watermarkShape = slide.Shapes.AddAutoShape(ShapeType.Triangle, 0, 0, 150, 50);
		ITextFrame watermarkTextFrame = watermarkShape.AddTextFrame("Watermark");
	}
```

Az alábbi példa bemutatja, hogyan lehet szövegdobozt létrehozni egy dián.

```csharp
[C#]
// Létrehozza a Presentation példányt
using (Presentation pres = new Presentation())
{
    // Lekéri a prezentáció első diáját
    ISlide sld = pres.Slides[0];
    // AutoShape-et ad hozzá, típusként Rectangle
    IAutoShape ashp = sld.Shapes.AddAutoShape(ShapeType.Rectangle, 150, 75, 150, 50);
    // TextFrame-et ad a Rectangle-hez
    ashp.AddTextFrame(" ");
    // Hozzáfér a szövegdobozhoz
    ITextFrame txtFrame = ashp.TextFrame;
    // Létrehozza a Paragraph objektumot a szövegdobozhoz
    IParagraph para = txtFrame.Paragraphs[0];
    // Létrehozza a Portion objektumot a bekezdéshez
    IPortion portion = para.Portions[0];
    // Beállítja a szöveget
    portion.Text = "Aspose TextBox";
    // Elmenti a prezentációt lemezre
    pres.Save("TextBox_out.pptx", Aspose.Slides.Export.SaveFormat.Pptx);
}
```

Az alábbi példa bemutatja, hogyan lehet oszlopot hozzáadni egy szövegdobozhoz.

```csharp
[C#]
using (Presentation presentation = new Presentation())
{
	// Lekéri a prezentáció első diáját
	ISlide slide = presentation.Slides[0];
	// AutoShape-et ad hozzá, típusként Rectangle
	IAutoShape aShape = slide.Shapes.AddAutoShape(ShapeType.Rectangle, 100, 100, 300, 300);
	// TextFrame-et ad a Rectangle-hez
	aShape.AddTextFrame("All these columns are limited to be within a single text container -- " +
	"you can add or delete text and the new or remaining text automatically adjusts " +
	"itself to flow within the container. You cannot have text flow from one container " +
	"to other though -- we told you PowerPoint's column options for text are limited!");
	// Lekéri a TextFrame szövegformátumát
	ITextFrameFormat format = aShape.TextFrame.TextFrameFormat;
	// Megadja a oszlopok számát a TextFrame-ben
	format.ColumnCount = 3;
	// Megadja az oszlopok közti távolságot
	format.ColumnSpacing = 10;
	// Elmenti a prezentációt
	presentation.Save("ColumnCount.pptx", SaveFormat.Pptx);
}
```

### Lásd még

* interfész [ITextFrame](../../itextframe)
* osztály [AutoShape](../../autoshape)
* névtér [Aspose.Slides](../../autoshape)
* összeállítás [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->