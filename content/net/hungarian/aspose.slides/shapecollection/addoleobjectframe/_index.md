---
title: AddOleObjectFrame
second_title: Aspose.Sildes .NET API referenciája
description: Új OLE objektum keretet hoz létre, és a alakzatgyűjtemény végéhez adja hozzá.
type: docs
weight: 150
url: /hu/aspose.slides/shapecollection/addoleobjectframe/
---
## AddOleObjectFrame(float, float, float, float, IOleEmbeddedDataInfo) {#addoleobjectframe}

Új OLE objektum keretet hoz létre, és hozzáadja a alakzatgyűjtemény végéhez.

```csharp
public IOleObjectFrame AddOleObjectFrame(float x, float y, float width, float height, 
    IOleEmbeddedDataInfo dataInfo)
```

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| x | Single | Az új OLE keret x-koordinátája pontban. |
| y | Single | Az új OLE keret y-koordinátája pontban. |
| width | Single | Az új OLE keret szélessége pontban. |
| height | Single | Az új OLE keret magassága pontban. |
| dataInfo | IOleEmbeddedDataInfo | A beágyazott OLE adatok információi ([`IOleEmbeddedDataInfo`](../../ioleembeddeddatainfo)). |

### Visszatérési érték

Az újonnan létrehozott [`IOleObjectFrame`](../../ioleobjectframe).

### Példák

A következő példák bemutatják, hogyan lehet OLE objektum kereteket hozzáadni a PowerPoint prezentáció diáihoz.

```csharp
[C#]
// Létrehozza a Presentation osztályt, amely a PPTX fájlt képviseli
using (Presentation pres = new Presentation())
{
    // Hozzáfér az első diához
    ISlide sld = pres.Slides[0];
    // Excel fájlt betölt egy adatfolyamra
    MemoryStream mstream = new MemoryStream();
    using (FileStream fs = new FileStream("book1.xlsx", FileMode.Open, FileAccess.Read))
    {
        byte[] buf = new byte[4096];
        while (true)
        {
            int bytesRead = fs.Read(buf, 0, buf.Length);
            if (bytesRead <= 0)
                break;
            mstream.Write(buf, 0, bytesRead);
        }
    }
    // Létrehoz egy adatobjektumot a beágyazáshoz
    IOleEmbeddedDataInfo dataInfo = new OleEmbeddedDataInfo(mstream.ToArray(), "xlsx");
    // Ole Object Frame alakzatot ad hozzá
    IOleObjectFrame oleObjectFrame = sld.Shapes.AddOleObjectFrame(0, 0, pres.SlideSize.Size.Width,
        pres.SlideSize.Size.Height, dataInfo);
    // A PPTX fájlt lemezre írja
    pres.Save("OleEmbed_out.pptx", SaveFormat.Pptx);
}
```

### Lásd még

* interfész [IOleObjectFrame](../../ioleobjectframe)
* interfész [IOleEmbeddedDataInfo](../../ioleembeddeddatainfo)
* osztály [ShapeCollection](../../shapecollection)
* névtér [Aspose.Slides](../../shapecollection)
* összeállítás [Aspose.Slides](../../../)

---

## AddOleObjectFrame(float, float, float, float, string, string) {#addoleobjectframe_1}

Új OLE objektum keretet hoz létre, és hozzáadja a alakzatgyűjtemény végéhez.

```csharp
public IOleObjectFrame AddOleObjectFrame(float x, float y, float width, float height, 
    string className, string path)
```

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| x | Single | Az új OLE keret x-koordinátája pontban. |
| y | Single | Az új OLE keret y-koordinátája pontban. |
| width | Single | Az új OLE keret szélessége pontban. |
| height | Single | Az új OLE keret magassága pontban. |
| className | String | Az OLE objektum osztályneve. |
| path | String | A hivatkozott fájl elérési útja. |

### Visszatérési érték

Az újonnan létrehozott [`IOleObjectFrame`](../../ioleobjectframe).

### Lásd még

* interfész [IOleObjectFrame](../../ioleobjectframe)
* osztály [ShapeCollection](../../shapecollection)
* névtér [Aspose.Slides](../../shapecollection)
* összeállítás [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->