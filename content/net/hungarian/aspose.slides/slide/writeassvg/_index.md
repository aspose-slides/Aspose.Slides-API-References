---
title: WriteAsSvg
second_title: Aspose.Sildes .NET API referencia
description: A diát SVG fájlként menti.
type: docs
weight: 140
url: /hu/aspose.slides/slide/writeassvg/
---
## WriteAsSvg(Stream) {#writeassvg}

A diát SVG fájlként menti.

```csharp
public void WriteAsSvg(Stream stream)
```

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| stream | Stream | Target stream |

### Példák

Az alábbi kódpélda bemutatja, hogyan lehet az első diát egy PowerPoint-prezentációból SVG fájlba konvertálni.

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (Stream fileStream = System.IO.File.Create("slide_1.svg"))
    {
        // Elmenti az első diát SVG fájlként
        pres.Slides[0].WriteAsSvg(fileStream);
    }
}
```

### Lásd még

* osztály [Slide](../../slide)
* névtér [Aspose.Slides](../../slide)
* összeállítás [Aspose.Slides](../../../)

---

## WriteAsSvg(Stream, ISVGOptions) {#writeassvg_1}

A diát SVG fájlként menti.

```csharp
public void WriteAsSvg(Stream stream, ISVGOptions svgOptions)
```

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| stream | Stream | Target stream |
| svgOptions | ISVGOptions | SVG generation options |

### Példák

Az alábbi kódpélda bemutatja, hogyan lehet az első diát egy PowerPoint-prezentációból SVG fájlba konvertálni opciókkal.

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (Stream fileStream = System.IO.File.Create("slide_1.svg"))
    {
        var options = new SVGOptions() { VectorizeText = true };
        // Elmenti az első diát SVG fájlként
        pres.Slides[0].WriteAsSvg(fileStream, options);
    }
}
```

### Lásd még

* interfész [ISVGOptions](../../../aspose.slides.export/isvgoptions)
* osztály [Slide](../../slide)
* névtér [Aspose.Slides](../../slide)
* összeállítás [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->