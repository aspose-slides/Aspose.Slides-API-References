---
title: HeaderFooterManager
second_title: Aspose.Sildes a .NET számára API referencia
description: Visszaadja a tényleges HeaderFooter kezelőt. Csak olvasható IPresentationHeaderFooterManageraspose.slides/ipresentationheaderfootermanager.
type: docs
weight: 120
url: /hu/aspose.slides/presentation/headerfootermanager/
---
## Presentation.HeaderFooterManager tulajdonság

Visszaadja az aktuális HeaderFooter kezelőt. Csak olvasható [`IPresentationHeaderFooterManager`](../../ipresentationheaderfootermanager).

```csharp
public IPresentationHeaderFooterManager HeaderFooterManager { get; }
```

### Példák

Az alábbi példa bemutatja, hogyan állítható be a lábléc láthatósága a PowerPoint Presentation Slide-jában.

```csharp
[C#]
using (Presentation presentation = new Presentation("presentation.ppt"))
{
    IBaseSlideHeaderFooterManager headerFooterManager = presentation.Slides[0].HeaderFooterManager;
	// Az IsFooterVisible tulajdonság arra szolgál, hogy jelezze, ha egy dián nincs lábléc helyőrző.
    if (!headerFooterManager.IsFooterVisible)
    {
		// A SetFooterVisibility metódus arra szolgál, hogy a dián a lábléc helyőrzőt láthatóvá tegye.
        headerFooterManager.SetFooterVisibility(true);
    }
	// Az IsSlideNumberVisible tulajdonság arra szolgál, hogy jelezze, ha egy dián nincs oldalszám helyőrző.
    if (!headerFooterManager.IsSlideNumberVisible)
    {
		// A SetSlideNumberVisibility metódus arra szolgál, hogy a dián az oldalszám helyőrzőt láthatóvá tegye.
        headerFooterManager.SetSlideNumberVisibility(true);
    }
	// Az IsDateTimeVisible tulajdonság arra szolgál, hogy jelezze, ha egy dián nincs dátum-idő helyőrző.
    if (!headerFooterManager.IsDateTimeVisible)
    {
		// A SetFooterVisibility metódus arra szolgál, hogy a dián a dátum-idő helyőrzőt láthatóvá tegye.
        headerFooterManager.SetDateTimeVisibility(true);
    }
	// A SetFooterText metódus arra szolgál, hogy szöveget állítson be a dián a lábléc helyőrzőhöz.
    headerFooterManager.SetFooterText("Footer text");
	// A SetDateTimeText metódus arra szolgál, hogy szöveget állítson be a dián a dátum-idő helyőrzőhöz.
    headerFooterManager.SetDateTimeText("Date and time text");
	presentation.Save("Presentation.ppt",SaveFormat.ppt);
}
```

Az alábbi példa bemutatja, hogyan állítható be a gyermek lábléc láthatósága egy Slide-on.

```csharp
[C#]
using (Presentation presentation = new Presentation("presentation.ppt"))
{
    IMasterSlideHeaderFooterManager headerFooterManager = presentation.Masters[0].HeaderFooterManager;
	// A SetFooterAndChildFootersVisibility metódus arra szolgál, hogy a mester diát és az összes gyermek lábléc helyőrzőt láthatóvá tegye.
    headerFooterManager.SetFooterAndChildFootersVisibility(true);
	// A SetSlideNumberAndChildSlideNumbersVisibility metódus arra szolgál, hogy a mester diát és az összes gyermek oldalszám helyőrzőt láthatóvá tegye.
    headerFooterManager.SetSlideNumberAndChildSlideNumbersVisibility(true);
	// A SetDateTimeAndChildDateTimesVisibility metódus arra szolgál, hogy a mester diát és az összes gyermek dátum-idő helyőrzőt láthatóvá tegye.
    headerFooterManager.SetDateTimeAndChildDateTimesVisibility(true);
	// A SetFooterAndChildFootersText metódus arra szolgál, hogy szöveget állítson be a mester dián és az összes gyermek lábléc helyőrzőhöz.
    headerFooterManager.SetFooterAndChildFootersText("Footer text");
	// A SetDateTimeAndChildDateTimesText metódus arra szolgál, hogy szöveget állítson be a mester dián és az összes gyermek dátum-idő helyőrzőhöz.
    headerFooterManager.SetDateTimeAndChildDateTimesText("Date and time text");
}
```

### Lásd még

* interfész [IPresentationHeaderFooterManager](../../ipresentationheaderfootermanager)
* osztály [Presentation](../../presentation)
* névtér [Aspose.Slides](../../presentation)
* összeállítás [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->