---
title: Masters
second_title: Aspose.Sildes .NET API referencia
description: Visszaadja a prezentációban meghatározott összes mesterdia listáját. Csak olvasható IMasterSlideCollectionaspose.slides/imasterslidecollection.
type: docs
weight: 180
url: /hu/aspose.slides/presentation/masters/
---
## Presentation.Masters tulajdonság

Visszaadja a prezentációban meghatározott összes mesterdia listáját. Csak olvasható [`IMasterSlideCollection`](../../imasterslidecollection).

```csharp
public IMasterSlideCollection Masters { get; }
```

### Példák

A következő példák bemutatják, hogyan lehet képeket hozzáadni a PowerPoint bemutató mesterdiáihoz.

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    ISlide slide = pres.Slides[0];
    IMasterSlide masterSlide = slide.LayoutSlide.MasterSlide;
    IPPImage image = pres.Images.AddImage(File.ReadAllBytes("image.png"));
    masterSlide.Shapes.AddPictureFrame(ShapeType.Rectangle, 10, 10, 100, 100, image);
    pres.Save("pres.pptx", SaveFormat.Pptx);
}
```

A következő példák bemutatják, hogyan lehet megváltoztatni a PowerPoint bemutató mesterdiájának háttérszínét.

```csharp
[C#]
// A Presentation osztály példányosítása, amely a prezentáció fájlt képviseli
using (Presentation pres = new Presentation())
{
    // A Master ISlide háttérszínének beállítása erdőzöldre
    pres.Masters[0].Background.Type = BackgroundType.OwnBackground;
    pres.Masters[0].Background.FillFormat.FillType = FillType.Solid;
    pres.Masters[0].Background.FillFormat.SolidFillColor.Color = Color.ForestGreen;
    // A prezentáció mentése a lemezre
    pres.Save("SetSlideBackgroundMaster_out.pptx", SaveFormat.Pptx);
}
```

A következő példák bemutatják, hogyan lehet diaelrendezést hozzáadni a PowerPoint bemutatóhoz.

```csharp
[C#]
// A Presentation osztály példányosítása, amely a prezentáció fájlt képviseli
using (Presentation presentation = new Presentation("AccessSlides.pptx"))
{
    // Kísérlet a layout dia típus szerinti keresésre
    IMasterLayoutSlideCollection layoutSlides = presentation.Masters[0].LayoutSlides;
    ILayoutSlide layoutSlide = layoutSlides.GetByType(SlideLayoutType.TitleAndObject) ?? layoutSlides.GetByType(SlideLayoutType.Title);
    if (layoutSlide == null)
    {
        // Az a helyzet, amikor a prezentáció nem tartalmaz bizonyos típusú layouteket.
        // A prezentációfájl csak Üres és Egyedi layout típusokat tartalmaz.
        // Azonban az Egyedi típusú layout diák különböző dianevekkel rendelkeznek,
        // például "Title", "Title and Content", stb. És lehetséges ezeket
        // neveket a layout dia kiválasztásához.
        // Továbbá lehetséges a helyőrző alakzat típusok halmazát használni. Például,
        // A cím dia csak a Title helyőrző típust kell, hogy tartalmazza, stb.
        foreach (ILayoutSlide titleAndObjectLayoutSlide in layoutSlides)
        {
            if (titleAndObjectLayoutSlide.Name == "Title and Object")
            {
                layoutSlide = titleAndObjectLayoutSlide;
                break;
            }
        }
        if (layoutSlide == null)
        {
            foreach (ILayoutSlide titleLayoutSlide in layoutSlides)
            {
                if (titleLayoutSlide.Name == "Title")
                {
                    layoutSlide = titleLayoutSlide;
                    break;
                }
            }
            if (layoutSlide == null)
            {
                layoutSlide = layoutSlides.GetByType(SlideLayoutType.Blank);
                if (layoutSlide == null)
                {
                    layoutSlide = layoutSlides.Add(SlideLayoutType.TitleAndObject, "Title and Object");
                }
            }
        }
    }
    // Üres dia hozzáadása a hozzáadott layout diával
    presentation.Slides.InsertEmptySlide(0, layoutSlide);
    // A prezentáció mentése
    presentation.Save("AddLayoutSlides_out.pptx", SaveFormat.Pptx);
}
```

### Lásd még

* interfész [IMasterSlideCollection](../../imasterslidecollection)
* osztály [Presentation](../../presentation)
* névtér [Aspose.Slides](../../presentation)
* összeállítás [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->