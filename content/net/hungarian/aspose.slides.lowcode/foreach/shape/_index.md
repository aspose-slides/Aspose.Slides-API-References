---
title: Shape
second_title: Aspose.Sildes a .NET-hez API referencia
description: Iterálja minden Shapeaspose.slides.lowcode/foreach/shape elemet a Presentationaspose.slides/presentation-ben. A Shape elemek iterálva lesznek minden diatípusban - Slideaspose.slides.lowcode/foreach/slide, MasterSlideaspose.slides.lowcode/foreach/masterslide és LayoutSlideaspose.slides.lowcode/foreach/layoutslide
type: docs
weight: 50
url: /hu/aspose.slides.lowcode/foreach/shape/
---
## Shape(Presentation, ForEachShapeCallback) {#shape_2}

Iterálja a `Shape` elemeket a [`Presentation`](../../../aspose.slides/presentation)-ben. Shape-ok iterálva lesznek minden típusú dián - [`Slide`](../slide), [`MasterSlide`](../masterslide) és [`LayoutSlide`](../layoutslide)

```csharp
public static void Shape(Presentation pres, ForEachShapeCallback forEachShape)
```

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| pres | Presentation | Presentation to iterate layout shapes |
| forEachShape | ForEachShapeCallback | Callback that will be invoked for each shape |

### Példák

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Shape(pres, (shape, slide, index) => 
    {
        System.Console.WriteLine($"{shape.Name}, index: {index}");
    });
} 
```

### Lásd még

* osztály [Presentation](../../../aspose.slides/presentation)
* delegált [ForEachShapeCallback](../../foreach.foreachshapecallback)
* osztály [ForEach](../../foreach)
* névtér [Aspose.Slides.LowCode](../../foreach)
* összeállítás [Aspose.Slides](../../../)

---

## Shape(Presentation, bool, ForEachShapeCallback) {#shape_1}

Iterálja az összes [`Shape`](../shape) a [`Presentation`](../../../aspose.slides/presentation)-ben. Shape-ok iterálva lesznek minden típusú dián - [`Slide`](../slide), [`MasterSlide`](../masterslide), [`LayoutSlide`](../layoutslide) és [`NotesSlide`](../../../aspose.slides/notesslide) ha szükséges.

```csharp
public static void Shape(Presentation pres, bool includeNotes, ForEachShapeCallback forEachShape)
```

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| pres | Presentation | Presentation to iterate layout shapes |
| includeNotes | Boolean | Flag that indicates whether NotesSlides should be included in processing. |
| forEachShape | ForEachShapeCallback | Callback that will be invoked for each shape |

### Példák

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Shape(pres, true, (shape, slide, index) => 
    {
        System.Console.WriteLine($"{shape.Name}, index: {index}");
    });
} 
```

### Lásd még

* osztály [Presentation](../../../aspose.slides/presentation)
* delegált [ForEachShapeCallback](../../foreach.foreachshapecallback)
* osztály [ForEach](../../foreach)
* névtér [Aspose.Slides.LowCode](../../foreach)
* összeállítás [Aspose.Slides](../../../)

---

## Shape(BaseSlide, ForEachShapeCallback) {#shape}

Iterálja az összes [`Shape`](../shape) a [`BaseSlide`](../../../aspose.slides/baseslide)-ban. [`BaseSlide`](../../../aspose.slides/baseslide) az alap típus a [`Slide`](../slide), [`MasterSlide`](../masterslide) és [`LayoutSlide`](../layoutslide) számára

```csharp
public static void Shape(BaseSlide baseSlide, ForEachShapeCallback forEachShape)
```

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| baseSlide | BaseSlide | Slide to iterate layout shapes |
| forEachShape | ForEachShapeCallback | Callback that will be invoked for each shape |

### Példák

```csharp
(Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Slide(pres, (slide, index) =>
    {
        ForEach.Shape(slide, (shape, baseSlide, shapeIndex) =>
        {
            System.Console.WriteLine($"{shape.Name}, index: {shapeIndex}");
        });
    });
} 
```

### Lásd még

* osztály [BaseSlide](../../../aspose.slides/baseslide)
* delegált [ForEachShapeCallback](../../foreach.foreachshapecallback)
* osztály [ForEach](../../foreach)
* névtér [Aspose.Slides.LowCode](../../foreach)
* összeállítás [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->