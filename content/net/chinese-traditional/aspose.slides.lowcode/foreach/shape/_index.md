---
title: Shape
second_title: Aspose.Sildes for .NET API 參考
description: 在 Presentationaspose.slides/presentation 中遍歷每個 Shapeaspose.slides.lowcode/foreach/shape。形狀將在所有類型的投影片中遍歷 - Slideaspose.slides.lowcode/foreach/slide MasterSlideaspose.slides.lowcode/foreach/masterslide 和 LayoutSlideaspose.slides.lowcode/foreach/layoutslide
type: docs
weight: 50
url: /zh-hant/aspose.slides.lowcode/foreach/shape/
---
## Shape(Presentation, ForEachShapeCallback) {#shape_2}

遍歷 [`Presentation`](../../../aspose.slides/presentation) 中的每個 `Shape`。形狀將在所有類型的投影片中迭代 - [`Slide`](../slide)、[`MasterSlide`](../masterslide) 和 [`LayoutSlide`](../layoutslide)

```csharp
public static void Shape(Presentation pres, ForEachShapeCallback forEachShape)
```

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| pres | Presentation | 用於遍歷版面配置形狀的 Presentation |
| forEachShape | ForEachShapeCallback | 將在每個形狀上被呼叫的 Callback |

### 範例

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Shape(pres, (shape, slide, index) => 
    {
        System.Console.WriteLine($"{shape.Name}, index: {index}");
    });
} 
```

### 另請參閱

* 類別 [Presentation](../../../aspose.slides/presentation)
* 委託 [ForEachShapeCallback](../../foreach.foreachshapecallback)
* 類別 [ForEach](../../foreach)
* 命名空間 [Aspose.Slides.LowCode](../../foreach)
* 組件 [Aspose.Slides](../../../)

---

## Shape(Presentation, bool, ForEachShapeCallback) {#shape_1}

遍歷 [`Presentation`](../../../aspose.slides/presentation) 中的每個 [`Shape`](../shape)。形狀將在所有類型的投影片中迭代 - [`Slide`](../slide)、[`MasterSlide`](../masterslide)、[`LayoutSlide`](../layoutslide) 和 [`NotesSlide`](../../../aspose.slides/notesslide)（如有需要）。

```csharp
public static void Shape(Presentation pres, bool includeNotes, ForEachShapeCallback forEachShape)
```

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| pres | Presentation | 用於遍歷版面配置形狀的 Presentation |
| includeNotes | Boolean | 指示是否應在處理中包括 NotesSlides 的旗標。 |
| forEachShape | ForEachShapeCallback | 將在每個形狀上被呼叫的 Callback |

### 範例

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Shape(pres, true, (shape, slide, index) => 
    {
        System.Console.WriteLine($"{shape.Name}, index: {index}");
    });
} 
```

### 另請參閱

* 類別 [Presentation](../../../aspose.slides/presentation)
* 委託 [ForEachShapeCallback](../../foreach.foreachshapecallback)
* 類別 [ForEach](../../foreach)
* 命名空間 [Aspose.Slides.LowCode](../../foreach)
* 組件 [Aspose.Slides](../../../)

---

## Shape(BaseSlide, ForEachShapeCallback) {#shape}

遍歷 [`BaseSlide`](../../../aspose.slides/baseslide) 中的每個 [`Shape`](../shape)。[`BaseSlide`](../../../aspose.slides/baseslide) 是 [`Slide`](../slide)、[`MasterSlide`](../masterslide) 和 [`LayoutSlide`](../layoutslide) 的基礎類型

```csharp
public static void Shape(BaseSlide baseSlide, ForEachShapeCallback forEachShape)
```

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| baseSlide | BaseSlide | 用於遍歷版面配置形狀的 Slide |
| forEachShape | ForEachShapeCallback | 將在每個形狀上被呼叫的 Callback |

### 範例

```csharp
(Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Slide(pres, (slide, index) =>
    {
        ForEach.Shape(slide, (shape, baseSlide, shapeIndex) =>
        {
            System.Console.WriteLine($"{shape.Name}, index: {shapeIndex}");
        });
    });
} 
```

### 另請參閱

* 類別 [BaseSlide](../../../aspose.slides/baseslide)
* 委託 [ForEachShapeCallback](../../foreach.foreachshapecallback)
* 類別 [ForEach](../../foreach)
* 命名空間 [Aspose.Slides.LowCode](../../foreach)
* 組件 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->