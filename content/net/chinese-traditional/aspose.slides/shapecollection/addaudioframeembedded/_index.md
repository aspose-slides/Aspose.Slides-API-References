---
title: AddAudioFrameEmbedded
second_title: Aspose.Sildes for .NET API 參考文件
description: 建立一個嵌入 WAV 檔案的新音訊框架，並將其加入形狀集合的末端。嵌入的音訊會加入 Presentation.Audios 集合。
type: docs
weight: 70
url: /zh-hant/aspose.slides/shapecollection/addaudioframeembedded/
---
## AddAudioFrameEmbedded(float, float, float, float, Stream) {#addaudioframeembedded_1}

建立一個帶有嵌入式 WAV 檔案的新音訊框架，並將其加入形狀集合的末端。嵌入的音訊會加入 Presentation.Audios 集合。

```csharp
public IAudioFrame AddAudioFrameEmbedded(float x, float y, float width, float height, 
    Stream audio_stream)
```

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| x | Single | 新音訊框架的 x 座標，單位為點。 |
| y | Single | 新音訊框架的 y 座標，單位為點。 |
| width | Single | 新音訊框架的寬度，單位為點。 |
| height | Single | 新音訊框架的高度，單位為點。 |
| audio_stream | Stream | 包含要嵌入的 WAV 音訊資料的輸入串流。 |

### 回傳值

新建立的 [`IAudioFrame`](../../iaudioframe)。

### 範例

以下範例說明如何建立 Audio Frame。

```csharp
[C#]
// 實例化表示簡報檔案的 Presentation 類別
using (Presentation pres = new Presentation())
{
    // 取得第一張投影片
    ISlide sld = pres.Slides[0];
    // 將 wav 音效檔載入為串流
    FileStream fstr = new FileStream("sampleaudio.wav", FileMode.Open, FileAccess.Read);
    // 加入音訊框架
    IAudioFrame audioFrame = sld.Shapes.AddAudioFrameEmbedded(50, 150, 100, 100, fstr);
    // 設定音訊的播放模式與音量
    audioFrame.PlayMode = AudioPlayModePreset.Auto;
    audioFrame.Volume = AudioVolumeMode.Loud;
    // 將 PowerPoint 檔案寫入磁碟
    pres.Save("AudioFrameEmbed_out.pptx", SaveFormat.Pptx);
}
```

### 相關參考

* 介面 [IAudioFrame](../../iaudioframe)
* 類別 [ShapeCollection](../../shapecollection)
* 名稱空間 [Aspose.Slides](../../shapecollection)
* 程式集 [Aspose.Slides](../../../)

---

## AddAudioFrameEmbedded(float, float, float, float, IAudio) {#addaudioframeembedded}

建立一個新音訊框架，並使用 Presentation.Audios 清單中的現有音訊物件將其加入形狀集合的末端。

```csharp
public IAudioFrame AddAudioFrameEmbedded(float x, float y, float width, float height, IAudio audio)
```

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| x | Single | 新音訊框架的 x 座標，單位為點。 |
| y | Single | 新音訊框架的 y 座標，單位為點。 |
| width | Single | 新音訊框架的寬度，單位為點。 |
| height | Single | 新音訊框架的高度，單位為點。 |
| audio | IAudio | 來自 Presentation.Audios 集合的 [`IAudio`](../../iaudio) 實例。 |

### 回傳值

新建立的 [`IAudioFrame`](../../iaudioframe)。

### 相關參考

* 介面 [IAudioFrame](../../iaudioframe)
* 介面 [IAudio](../../iaudio)
* 類別 [ShapeCollection](../../shapecollection)
* 名稱空間 [Aspose.Slides](../../shapecollection)
* 程式集 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->