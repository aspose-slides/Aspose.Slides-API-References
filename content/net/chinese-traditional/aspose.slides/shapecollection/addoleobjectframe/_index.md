---
title: AddOleObjectFrame
second_title: Aspose.Sildes for .NET API 參考
description: 建立一個新的 OLE 物件框，並將其新增至圖形集合的末端。
type: docs
weight: 150
url: /zh-hant/aspose.slides/shapecollection/addoleobjectframe/
---
## AddOleObjectFrame(float, float, float, float, IOleEmbeddedDataInfo) {#addoleobjectframe}

建立一個新的 OLE 物件框，並將其新增至圖形集合的末端。

```csharp
public IOleObjectFrame AddOleObjectFrame(float x, float y, float width, float height, 
    IOleEmbeddedDataInfo dataInfo)
```

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| x | Single | 新 OLE 框的 x 坐標，單位為點。 |
| y | Single | 新 OLE 框的 y 坐標，單位為點。 |
| width | Single | 新 OLE 框的寬度，單位為點。 |
| height | Single | 新 OLE 框的高度，單位為點。 |
| dataInfo | IOleEmbeddedDataInfo | 嵌入 OLE 資料的資訊 ([`IOleEmbeddedDataInfo`](../../ioleembeddeddatainfo))。 |

### 回傳值

新建立的 [`IOleObjectFrame`](../../ioleobjectframe)。

### 範例

以下範例說明如何將 OLE 物件框加入 PowerPoint 簡報的投影片。

```csharp
[C#]
// 實例化代表 PPTX 檔案的 Presentation 類別
using (Presentation pres = new Presentation())
{
    // 取得第一張投影片
    ISlide sld = pres.Slides[0];
    // 載入 Excel 檔案至串流
    MemoryStream mstream = new MemoryStream();
    using (FileStream fs = new FileStream("book1.xlsx", FileMode.Open, FileAccess.Read))
    {
        byte[] buf = new byte[4096];
        while (true)
        {
            int bytesRead = fs.Read(buf, 0, buf.Length);
            if (bytesRead <= 0)
                break;
            mstream.Write(buf, 0, bytesRead);
        }
    }
    // 建立用於嵌入的資料物件
    IOleEmbeddedDataInfo dataInfo = new OleEmbeddedDataInfo(mstream.ToArray(), "xlsx");
    // 新增 Ole Object Frame 形狀
    IOleObjectFrame oleObjectFrame = sld.Shapes.AddOleObjectFrame(0, 0, pres.SlideSize.Size.Width,
        pres.SlideSize.Size.Height, dataInfo);
    //將 PPTX 檔案寫入磁碟
    pres.Save("OleEmbed_out.pptx", SaveFormat.Pptx);
}
```

### 另請參閱

* 介面 [IOleObjectFrame](../../ioleobjectframe)
* 介面 [IOleEmbeddedDataInfo](../../ioleembeddeddatainfo)
* 類別 [ShapeCollection](../../shapecollection)
* 命名空間 [Aspose.Slides](../../shapecollection)
* 組件 [Aspose.Slides](../../../)

---

## AddOleObjectFrame(float, float, float, float, string, string) {#addoleobjectframe_1}

建立一個新的 OLE 物件框，並將其新增至圖形集合的末端。

```csharp
public IOleObjectFrame AddOleObjectFrame(float x, float y, float width, float height, 
    string className, string path)
```

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| x | Single | 新 OLE 框的 x 坐標，單位為點。 |
| y | Single | 新 OLE 框的 y 坐標，單位為點。 |
| width | Single | 新 OLE 框的寬度，單位為點。 |
| height | Single | 新 OLE 框的高度，單位為點。 |
| className | String | OLE 物件的類別名稱。 |
| path | String | 連結檔案的路徑。 |

### 回傳值

新建立的 [`IOleObjectFrame`](../../ioleobjectframe)。

### 另請參閱

* 介面 [IOleObjectFrame](../../ioleobjectframe)
* 類別 [ShapeCollection](../../shapecollection)
* 命名空間 [Aspose.Slides](../../shapecollection)
* 組件 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->