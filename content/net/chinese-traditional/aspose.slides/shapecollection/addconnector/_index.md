---
title: AddConnector
second_title: Aspose.Sildes for .NET API 參考
description: 建立具有預設範本樣式的新連接器形狀，並將其加入形狀集合的末端。
type: docs
weight: 120
url: /zh-hant/aspose.slides/shapecollection/addconnector/
---
## AddConnector(ShapeType, float, float, float, float) {#addconnector}

建立一個具有預設範本樣式的新連接器形狀，並將其加入形狀集合的末端。

```csharp
public IConnector AddConnector(ShapeType shapeType, float x, float y, float width, float height)
```

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| shapeType | ShapeType | 要新增的連接器形狀的 [`ShapeType`](../../shapetype)。 |
| x | Single | 連接器框架的 x 座標（單位為點）。 |
| y | Single | 連接器框架的 y 座標（單位為點）。 |
| width | Single | 連接器框架的寬度（單位為點）。 |
| height | Single | 連接器框架的高度（單位為點）。 |

### 返回值

新建立的 [`IConnector`](../../iconnector)。

### 範例

以下範例說明如何在 PowerPoint 簡報中於兩個形狀（橢圓形和矩形）之間加入連接器（彎曲連接器）。

```csharp
[C#]
// 實例化一個代表 PPTX 檔案的 Presentation 類別
using (Presentation input = new Presentation())
{
    // 存取特定投影片的圖形集合
    IShapeCollection shapes = input.Slides[0].Shapes;
    // 新增一個橢圓自動圖形
    IAutoShape ellipse = shapes.AddAutoShape(ShapeType.Ellipse, 0, 100, 100, 100);
    // 新增一個矩形自動圖形
    IAutoShape rectangle = shapes.AddAutoShape(ShapeType.Rectangle, 100, 300, 100, 100);
    // 將連接器圖形新增至投影片的圖形集合
    IConnector connector = shapes.AddConnector(ShapeType.BentConnector2, 0, 0, 10, 10);
    // 使用連接器連接圖形
    connector.StartShapeConnectedTo = ellipse;
    connector.EndShapeConnectedTo = rectangle;
    // 呼叫 reroute，設定圖形之間的自動最短路徑
    connector.Reroute();
    // 儲存簡報
    input.Save("Shapes-connector.pptx", SaveFormat.Pptx);
}
```

### 另請參閱

* 介面 [IConnector](../../iconnector)
* 列舉 [ShapeType](../../shapetype)
* 類別 [ShapeCollection](../../shapecollection)
* 命名空間 [Aspose.Slides](../../shapecollection)
* 組件 [Aspose.Slides](../../../)

---

## AddConnector(ShapeType, float, float, float, float, bool) {#addconnector_1}

建立一個新連接器形狀，並將其加入形狀集合的末端，可選擇套用預設範本樣式。

```csharp
public IConnector AddConnector(ShapeType shapeType, float x, float y, float width, float height, 
    bool createFromTemplate)
```

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| shapeType | ShapeType | 要建立之連接器形狀的 [`ShapeType`](../../shapetype)。 |
| x | Single | 連接器框架的 x 座標（單位為點）。 |
| y | Single | 連接器框架的 y 座標（單位為點）。 |
| width | Single | 連接器框架的寬度（單位為點）。 |
| height | Single | 連接器框架的高度（單位為點）。 |
| createFromTemplate | Boolean | True 代表套用預設範本樣式（非空名稱、簡單樣式）；false 代表以預設屬性值建立連接器。 |

### 返回值

新建立的 [`IConnector`](../../iconnector)。

### 另請參閱

* 介面 [IConnector](../../iconnector)
* 列舉 [ShapeType](../../shapetype)
* 類別 [ShapeCollection](../../shapecollection)
* 命名空間 [Aspose.Slides](../../shapecollection)
* 組件 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->