---
title: WriteAsEmf
second_title: Aspose.Sildes for .NET API 參考
description: 將 SVG 圖像儲存為 EMF 檔案。
type: docs
weight: 50
url: /zh-hant/aspose.slides/isvgimage/writeasemf/
---
## ISvgImage.WriteAsEmf 方法

將 SVG 圖像儲存為 EMF 檔案。

```csharp
public void WriteAsEmf(Stream stream)
```

| 參數 | 類型 | 描述 |
| --- | --- | --- |
| stream | Stream | 目標串流 |

### 例外狀況

| 例外 | 條件 |
| --- | --- |
| ArgumentNullException | 目標串流為 **null** |

### 範例

以下範例示範如何將 SVG 圖像儲存為中介檔案。

```csharp
[C#]
// 建立新的 SVG 圖像
ISvgImage svgImage = new SvgImage(System.IO.File.ReadAllText("content.svg"));

// 將 SVG 圖像儲存為中介檔案
using (var fileStream = System.IO.File.OpenWrite("SvgAsEmf.emf"))
    svgImage.WriteAsEmf(fileStream);
```

此範例示範如何將 SVG 圖像作為中介檔案加入簡報影像集合。

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    // 建立新的 SVG 圖像
    ISvgImage svgImage = new SvgImage(System.IO.File.ReadAllText("content.svg"));
    using (var memStream = new MemoryStream())
    {
        // 將 SVG 圖像儲存為中介檔案
        svgImage.WriteAsEmf(memStream);
        // 將中介檔案加入影像集合
        pres.Images.AddImage(memStream.ToArray());
    }
}
```

### 另請參閱

* 介面 [ISvgImage](../../isvgimage)
* 命名空間 [Aspose.Slides](../../isvgimage)
* 程式集 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->