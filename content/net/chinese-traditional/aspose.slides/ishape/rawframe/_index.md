---
title: RawFrame
second_title: Aspose.Sildes for .NET API 參考
description: 返回或設定原始形狀框架的屬性。讀/寫 IShapeFrameaspose.slides/ishapeframe.
type: docs
weight: 210
url: /zh-hant/aspose.slides/ishape/rawframe/
---
## IShape.RawFrame 屬性

返回或設定原始形狀框架的屬性。讀/寫 [`IShapeFrame`](../../ishapeframe).

```csharp
public IShapeFrame RawFrame { get; set; }
```

### 範例

嘗試將未定義的框架指派給 IShape.Frame 的程式碼在一般情況下是沒有意義的（特別是在父級 GroupShape 被多層嵌套於其他 GroupShape 時）。例如：

```csharp
IShape shape = ...;
shape.Frame = new ShapeFrame(float.NaN, float.NaN, float.NaN, float.NaN, NullableBool.NotDefined, NullableBool.NotDefined, float.NaN);
```

或

```csharp
slide.Shapes.AddAutoShape(ShapeType.RoundCornerRectangle, float.NaN, float.NaN, float.NaN, float.NaN);
```

此類程式碼可能導致不明確的情況。因此已新增對於在 IShape.Frame 使用未定義值的限制。x、y、width、height、flipH、flipV 與 rotationAngle 的值必須被定義（不能是 float.NaN 或 NullableBool.NotDefined）。上述範例程式碼現在會拋出 ArgumentException 例外。此限制適用於以下使用情境：

```csharp
IShape shape = ...;
shape.Frame = ...; // 不能為未定義

IShapeCollection shapes = ...;
// x、y、width、height 參數不能為 float.NaN:
{
    shapes.AddAudioFrameCD(...);
    shapes.AddAudioFrameEmbedded(...);
    shapes.AddAudioFrameLinked(...);
    shapes.AddAutoShape(...);
    shapes.AddChart(...);
    shapes.AddConnector(...);
    shapes.AddOleObjectFrame(...);
    shapes.AddPictureFrame(...);
    shapes.AddSmartArt(...);
    shapes.AddTable(...);
    shapes.AddVideoFrame(...);
    shapes.InsertAudioFrameEmbedded(...);
    shapes.InsertAudioFrameLinked(...);
    shapes.InsertAutoShape(...);
    shapes.InsertChart(...);
    shapes.InsertConnector(...);
    shapes.InsertOleObjectFrame(...);
    shapes.InsertPictureFrame(...);
    shapes.InsertTable(...);
    shapes.InsertVideoFrame(...);
}
```

但是 IShape.RawFrame 的框架屬性可以是未定義的。當形狀連結到占位符時這是有意義的。此時未定義的形狀框架值會從父占位符形狀覆寫。若該形狀沒有父占位符形狀，則在根據其 IShape.RawFrame 評估實際框架時，形狀會使用預設值。預設值為 x、y、width、height、flipH、flipV 與 rotationAngle 的 0 與 NullableBool.False。例如：

```csharp
IShape shape = ...; // shape 連結到占位符
shape.RawFrame = new ShapeFrame(float.NaN, float.NaN, 100, float.NaN, NullableBool.NotDefined, NullableBool.NotDefined, 0); // 現在 shape 從占位符繼承 x、y、height、flipH、flipV 值，並覆寫 width=100 與 rotationAngle=0。
```

### 另請參閱

* 介面 [IShapeFrame](../../ishapeframe)
* 介面 [IShape](../../ishape)
* 命名空間 [Aspose.Slides](../../ishape)
* 程序集 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->