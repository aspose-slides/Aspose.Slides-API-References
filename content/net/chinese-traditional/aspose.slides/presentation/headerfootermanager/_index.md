---
title: HeaderFooterManager
second_title: Aspose.Sildes for .NET API 參考文件
description: 傳回實際的 HeaderFooter 管理器。唯讀 IPresentationHeaderFooterManageraspose.slides/ipresentationheaderfootermanager.
type: docs
weight: 120
url: /zh-hant/aspose.slides/presentation/headerfootermanager/
---
## Presentation.HeaderFooterManager 屬性

傳回實際的 HeaderFooter 管理器。唯讀 [`IPresentationHeaderFooterManager`](../../ipresentationheaderfootermanager).

```csharp
public IPresentationHeaderFooterManager HeaderFooterManager { get; }
```

### 範例

以下範例說明如何在 PowerPoint 簡報的 Slide 中設定頁腳的可見性。

```csharp
[C#]
using (Presentation presentation = new Presentation("presentation.ppt"))
{
    IBaseSlideHeaderFooterManager headerFooterManager = presentation.Slides[0].HeaderFooterManager;
	// 屬性 IsFooterVisible 用於表示投影片的頁腳佔位符不存在。
    if (!headerFooterManager.IsFooterVisible)
    {
		// 方法 SetFooterVisibility 用於使投影片的頁腳佔位符可見。
        headerFooterManager.SetFooterVisibility(true);
    }
	// 屬性 IsSlideNumberVisible 用於表示投影片的頁碼佔位符不存在。
    if (!headerFooterManager.IsSlideNumberVisible)
    {
		// 方法 SetSlideNumberVisibility 用於使投影片的頁碼佔位符可見。
        headerFooterManager.SetSlideNumberVisibility(true);
    }
	// 屬性 IsDateTimeVisible 用於表示投影片的日期時間佔位符不存在。
    if (!headerFooterManager.IsDateTimeVisible)
    {
		// 方法 SetFooterVisibility 用於使投影片的日期時間佔位符可見。
        headerFooterManager.SetDateTimeVisibility(true);
    }
	// 方法 SetFooterText 用於設定投影片頁腳佔位符的文字。
    headerFooterManager.SetFooterText("Footer text");
	// 方法 SetDateTimeText 用於設定投影片日期時間佔位符的文字。
    headerFooterManager.SetDateTimeText("Date and time text");
	presentation.Save("Presentation.ppt",SaveFormat.ppt);
}
```

以下範例說明如何在 Slide 中設定子頁腳的可見性。

```csharp
[C#]
using (Presentation presentation = new Presentation("presentation.ppt"))
{
    IMasterSlideHeaderFooterManager headerFooterManager = presentation.Masters[0].HeaderFooterManager;
	// 方法 SetFooterAndChildFootersVisibility 用於使母版投影片及所有子頁腳佔位符可見。
    headerFooterManager.SetFooterAndChildFootersVisibility(true);
	// 方法 SetSlideNumberAndChildSlideNumbersVisibility 用於使母版投影片及所有子頁碼佔位符可見。
    headerFooterManager.SetSlideNumberAndChildSlideNumbersVisibility(true);
	// 方法 SetDateTimeAndChildDateTimesVisibility 用於使母版投影片及所有子日期時間佔位符可見。
    headerFooterManager.SetDateTimeAndChildDateTimesVisibility(true);
	// 方法 SetFooterAndChildFootersText 用於設定母版投影片及所有子頁腳佔位符的文字。
    headerFooterManager.SetFooterAndChildFootersText("Footer text");
	// 方法 SetDateTimeAndChildDateTimesText 用於設定母版投影片及所有子日期時間佔位符的文字。
    headerFooterManager.SetDateTimeAndChildDateTimesText("Date and time text");
}
```

### 另見

* 介面 [IPresentationHeaderFooterManager](../../ipresentationheaderfootermanager)
* 類別 [Presentation](../../presentation)
* 命名空間 [Aspose.Slides](../../presentation)
* 組件 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->