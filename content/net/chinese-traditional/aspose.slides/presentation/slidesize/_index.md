---
title: SlideSize
second_title: Aspose.Sildes for .NET API 參考文件
description: 傳回投影片大小物件。唯讀 ISlideSizeaspose.slides/islidesize.
type: docs
weight: 260
url: /zh-hant/aspose.slides/presentation/slidesize/
---
## Presentation.SlideSize 屬性

傳回投影片大小物件。唯讀 [`ISlideSize`](../../islidesize).

```csharp
public ISlideSize SlideSize { get; }
```

### 範例

以下範例說明如何在 PowerPoint 簡報中變更投影片大小。

```csharp
[C#]
using (Presentation pres = new Presentation("pres-4x3-aspect-ratio.pptx"))
{
    pres.SlideSize.SetSize(SlideSizeType.OnScreen16x9, SlideSizeScaleType.DoNotScale);
    pres.Save("pres-4x3-aspect-ratio.pptx", SaveFormat.Pptx);
}
```

以下範例說明如何在 PowerPoint 簡報中設定相對於內容縮放的投影片大小。

```csharp
[C#]
// 實例化一個代表簡報檔案的 Presentation 物件
using(Presentation presentation = new Presentation("AccessSlides.pptx")) {
  using(Presentation auxPresentation = new Presentation()) {
    ISlide slide = presentation.Slides[0];
    // 設定產生的簡報的投影片大小與來源相同
    presentation.SlideSize.SetSize(540, 720, SlideSizeScaleType.EnsureFit); // 使用 SetSize 方法設定投影片大小，並縮放內容以確保適合
    presentation.SlideSize.SetSize(SlideSizeType.A4Paper, SlideSizeScaleType.Maximize); // 使用 SetSize 方法設定投影片大小，並將內容最大化
    // 將簡報儲存至磁碟
    auxPresentation.Save("Set_Size&Type_out.pptx", SaveFormat.Pptx);
  }
}
```

以下範例說明如何在 PowerPoint 簡報中指定自訂投影片大小。

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    pres.SlideSize.SetSize(780, 540, SlideSizeScaleType.DoNotScale); // A4 紙張尺寸
    pres.Save("pres-a4-slide-size.pptx", SaveFormat.Pptx);
}
```

### 另請參閱

* 介面 [ISlideSize](../../islidesize)
* 類別 [Presentation](../../presentation)
* 命名空間 [Aspose.Slides](../../presentation)
* 組件 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->