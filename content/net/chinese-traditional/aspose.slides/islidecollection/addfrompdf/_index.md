---
title: AddFromPdf
second_title: Aspose.Sildes for .NET API 參考
description: 從 PDF 文件建立投影片，並將它們加入集合的末端。
type: docs
weight: 50
url: /zh-hant/aspose.slides/islidecollection/addfrompdf/
---
## AddFromPdf(string) {#addfrompdf_2}

從 PDF 文件建立投影片，並將它們加入集合的末端。

```csharp
public ISlide[] AddFromPdf(string path)
```

| 參數 | 型別 | 說明 |
| --- | --- | --- |
| path | String | PDF 文件的路徑 |

### 傳回值

已加入的投影片

### 範例

範例：

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    pres.Slides.AddFromPdf("document.pdf");
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### 另請參閱

* 介面 [ISlide](../../islide)
* 介面 [ISlideCollection](../../islidecollection)
* 命名空間 [Aspose.Slides](../../islidecollection)
* 程式集 [Aspose.Slides](../../../)

---

## AddFromPdf(string, PdfImportOptions) {#addfrompdf_3}

根據 PDF 匯入選項，從 PDF 文件建立投影片，並將它們加入集合的末端。

```csharp
public ISlide[] AddFromPdf(string path, PdfImportOptions pdfImportOptions)
```

| 參數 | 型別 | 說明 |
| --- | --- | --- |
| path | String | PDF 文件的路徑 |
| pdfImportOptions | PdfImportOptions | PDF 匯入的選項 |

### 傳回值

已加入的投影片

### 範例

範例：

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    pres.Slides.AddFromPdf("document.pdf, new PdfImportOptions { DetectTables = true });
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### 另請參閱

* 介面 [ISlide](../../islide)
* 類別 [PdfImportOptions](../../../aspose.slides.import/pdfimportoptions)
* 介面 [ISlideCollection](../../islidecollection)
* 命名空間 [Aspose.Slides](../../islidecollection)
* 程式集 [Aspose.Slides](../../../)

---

## AddFromPdf(Stream, PdfImportOptions) {#addfrompdf_1}

從 PDF 文件建立投影片，並將它們加入集合的末端。

```csharp
public ISlide[] AddFromPdf(Stream pdfStream, PdfImportOptions pdfImportOptions)
```

| 參數 | 型別 | 說明 |
| --- | --- | --- |
| pdfStream | Stream | PDF 文件的來源串流 |
| pdfImportOptions | PdfImportOptions | PDF 匯入的選項 |

### 傳回值

已加入的投影片

### 範例

範例：

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    using (Stream stream = new FileStream("document.pdf", FileMode.Open, FileAccess.Read, FileShare.Read))
    {
        pres.Slides.AddFromPdf(stream, new PdfImportOptions { DetectTables = true });
    }
    
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### 另請參閱

* 介面 [ISlide](../../islide)
* 類別 [PdfImportOptions](../../../aspose.slides.import/pdfimportoptions)
* 介面 [ISlideCollection](../../islidecollection)
* 命名空間 [Aspose.Slides](../../islidecollection)
* 程式集 [Aspose.Slides](../../../)

---

## AddFromPdf(Stream) {#addfrompdf}

從 PDF 文件建立投影片，並將它們加入集合的末端。

```csharp
public ISlide[] AddFromPdf(Stream pdfStream)
```

| 參數 | 型別 | 說明 |
| --- | --- | --- |
| pdfStream | Stream | PDF 文件的來源串流 |

### 傳回值

已加入的投影片

### 範例

範例：

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    using (Stream stream = new FileStream("document.pdf", FileMode.Open, FileAccess.Read, FileShare.Read))
    {
        pres.Slides.AddFromPdf(stream);
    }
    
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### 另請參閱

* 介面 [ISlide](../../islide)
* 介面 [ISlideCollection](../../islidecollection)
* 命名空間 [Aspose.Slides](../../islidecollection)
* 程式集 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->