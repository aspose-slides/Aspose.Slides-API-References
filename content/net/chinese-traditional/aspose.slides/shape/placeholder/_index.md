---
title: Placeholder
second_title: Aspose.Sildes for .NET API 參考文件
description: 傳回形狀的佔位符。 如果形狀沒有佔位符，則傳回 null。 唯讀 IPlaceholderaspose.slides/iplaceholder.
type: docs
weight: 210
url: /zh-hant/aspose.slides/shape/placeholder/
---
## Shape.Placeholder 屬性

傳回形狀的佔位符。如果形狀沒有佔位符，則傳回 null。唯讀 [`IPlaceholder`](../../iplaceholder)。

```csharp
public IPlaceholder Placeholder { get; }
```

### 範例

以下範例示範如何變更佔位符中的文字。

```csharp
[C#]
// 實例化 Presentation 類別
using (Presentation pres = new Presentation("ReplacingText.pptx"))
{
    // 存取第一張投影片
    ISlide sld = pres.Slides[0];
    // 遍歷形狀以尋找佔位符
    foreach (IShape shp in sld.Shapes)
        if (shp.Placeholder != null)
        {
            // 變更每個佔位符中的文字
            ((IAutoShape)shp).TextFrame.Text = "This is a Placeholder";
        }
    // 將簡報儲存至磁碟
    pres.Save("output_out.pptx", Aspose.Slides.Export.SaveFormat.Pptx);
}
```

以下範例示範如何在佔位符中設定提示文字。

```csharp
[C#]
using (Presentation pres = new Presentation("Presentation2.pptx"))
{
    ISlide slide = pres.Slides[0];
    foreach (IShape shape in slide.Slide.Shapes) // 遍歷投影片
    {
        if (shape.Placeholder.Type == PlaceholderType.CenteredTitle) // PowerPoint 顯示「點擊以新增標題」
        {
            text = "Add Title";
        }
        else if (shape.Placeholder.Type == PlaceholderType.Subtitle) // 新增副標題
        {
            text = "Add Subtitle";
        }
        ((IAutoShape)shape).TextFrame.Text = text;
        Console.WriteLine($"Placeholder with text: {text}");
    }
    pres.Save("Placeholders_PromptText.pptx", SaveFormat.Pptx);
}
```

### 另請參閱

* 介面 [IPlaceholder](../../iplaceholder)
* 類別 [Shape](../../shape)
* 命名空間 [Aspose.Slides](../../shape)
* 組件 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->