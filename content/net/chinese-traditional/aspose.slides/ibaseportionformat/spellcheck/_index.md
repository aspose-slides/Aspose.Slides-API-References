---
title: SpellCheck
second_title: Aspose.Sildes for .NET API 參考文件
description: 取得或設定一個值，指示文字片段是否啟用拼寫檢查。當此屬性設定為 false 時，會抑制文字元素的拼寫檢查。當設定為 true 時，允許拼寫檢查。預設值為 false。
type: docs
weight: 220
url: /zh-hant/aspose.slides/ibaseportionformat/spellcheck/
---
## IBasePortionFormat.SpellCheck 屬性

取得或設定一個值，指示文字片段是否啟用拼寫檢查。當此屬性設定為 false 時，會抑制文字元素的拼寫檢查。當設定為 true 時，允許拼寫檢查。預設值為 `false`。

```csharp
public bool SpellCheck { get; set; }
```

### 範例

以下示例展示了在儲存簡報之前啟用 SpellCheck 旗標：

```csharp
[C#]
using (var pres = new Presentation("input.pptx"))
{
    // 存取第一張投影片上第一個圖形中的第一個文字片段
    var portion = ((AutoShape)pres.Slides[0].Shapes[0]).TextFrame.Paragraphs[0].Portions[0];
    // 為此文字片段啟用拼寫檢查
    portion.PortionFormat.SpellCheck = true;
    // 儲存已修改的簡報
    pres.Save("output-with-spellcheck.pptx", SaveFormat.Pptx);
}
```

### 另請參閱

* 介面 [IBasePortionFormat](../../ibaseportionformat)
* 命名空間 [Aspose.Slides](../../ibaseportionformat)
* 組件 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->