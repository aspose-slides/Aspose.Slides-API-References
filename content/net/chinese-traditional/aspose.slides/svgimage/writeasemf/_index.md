---
title: WriteAsEmf
second_title: Aspose.Sildes .NET API 參考
description: 將 SVG 圖像儲存為 EMF 檔案。
type: docs
weight: 60
url: /zh-hant/aspose.slides/svgimage/writeasemf/
---
## SvgImage.WriteAsEmf 方法

將 SVG 圖像儲存為 EMF 檔案。

```csharp
public void WriteAsEmf(Stream stream)
```

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | Stream | 目標串流 |

### 例外

| 例外 | 條件 |
| --- | --- |
| ArgumentNullException | 目標串流為 **null** |

### 範例

以下範例說明如何將 SVG 圖像儲存為中繼檔。

```csharp
[C#]
// 建立新的 SVG 圖像
ISvgImage svgImage = new SvgImage(System.IO.File.ReadAllText("content.svg"));

// 儲存 SVG 圖像為中繼檔
using (var fileStream = System.IO.File.OpenWrite("SvgAsEmf.emf"))
    svgImage.WriteAsEmf(fileStream);
```

此範例示範如何將 SVG 圖像作為中繼檔加入簡報圖像集合。

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    // 建立新的 SVG 圖像
    ISvgImage svgImage = new SvgImage(System.IO.File.ReadAllText("content.svg"));
    using (var memStream = new MemoryStream())
    {
        // 將 SVG 圖像儲存為中繼檔
        svgImage.WriteAsEmf(memStream);
        // 將中繼檔加入圖像集合
        pres.Images.AddImage(memStream.ToArray());
    }
}
```

### 參見

* 類別 [SvgImage](../../svgimage)
* 命名空間 [Aspose.Slides](../../svgimage)
* 組件 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->