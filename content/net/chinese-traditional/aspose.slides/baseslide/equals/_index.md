---
title: Equals
second_title: Aspose.Sildes for .NET API 參考文件
description: 判斷兩個 IBaseSlide 實例是否相等。返回值根據投影片的結構和靜態內容計算。若所有形狀、樣式、文字、動畫及其他設定等全部相同，則兩張投影片相等。比較時不會考慮唯一識別碼值，例如 SlideId，亦不會考慮動態內容，例如日期占位符中的當前日期值。
type: docs
weight: 130
url: /zh-hant/aspose.slides/baseslide/equals/
---
## BaseSlide.Equals 方法

判斷兩個 IBaseSlide 實例是否相等。返回值根據投影片的結構和靜態內容計算。若所有形狀、樣式、文字、動畫及其他設定等全部相同，則兩張投影片相等。比較時不會考慮唯一識別碼值，例如 SlideId，亦不會考慮動態內容，例如日期占位符中的當前日期值。

```csharp
public bool Equals(IBaseSlide slide)
```

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| slide | IBaseSlide | 要與目前的 IBaseSlide 比較的 IBaseSlide。 |

### 返回值

**true** 如果指定的 IBaseSlide 與目前的 IBaseSlide 相等；否則，**false**。

### 示例

以下範例說明如何比較兩張投影片。

```csharp
using (Presentation presentation1 = new Presentation("AccessSlides.pptx"))
{
	using (Presentation presentation2 = new Presentation("HelloWorld.pptx"))
	{
		for (int i = 0; i < presentation1.Masters.Count; i++)
		{
			for (int j = 0; j < presentation2.Masters.Count; j++)
			{
				if (presentation1.Masters[i].Equals(presentation2.Masters[j]))
					Console.WriteLine(string.Format("SomePresentation1 MasterSlide#{0} is equal to SomePresentation2 MasterSlide#{1}", i, j));
			}
		}
	}
}
```

### 另請參閱

* 介面 [IBaseSlide](../../ibaseslide)
* 類別 [BaseSlide](../../baseslide)
* 命名空間 [Aspose.Slides](../../baseslide)
* 組件 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->