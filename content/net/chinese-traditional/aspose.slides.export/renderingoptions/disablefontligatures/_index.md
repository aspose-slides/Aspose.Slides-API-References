---
title: DisableFontLigatures
second_title: Aspose.Sildes for .NET API 參考文件
description: 取得或設定一個值，指示文字是否在不使用連字的情況下呈現。設定為 true 時，連字將在渲染輸出中被停用。預設情況下，此屬性設為 false。
type: docs
weight: 20
url: /zh-hant/aspose.slides.export/renderingoptions/disablefontligatures/
---
## RenderingOptions.DisableFontLigatures 屬性

取得或設定一個值，指示文字是否在不使用連字的情況下呈現。設定為 `true` 時，渲染輸出將停用連字。預設情況下，此屬性設為 `false`。

```csharp
public bool DisableFontLigatures { get; set; }
```

### 範例

範例：

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    RenderingOptions options = new RenderingOptions
    {
        DisableFontLigatures = true // 在文字渲染中停用連字
    };
    
    Bitmap[] renderedSlides = pres.GetImage(options);
    for (var index = 0; index < renderedSlides.Length; index++)
    {
        var slideImage = renderedSlides[index];
        slideImage.Save($"slide-{index}.png");
    }
}
```

### 另請參閱

* 類別 [RenderingOptions](../../renderingoptions)
* 命名空間 [Aspose.Slides.Export](../../renderingoptions)
* 組件 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->