---
title: DisableFontLigatures
second_title: Aspose.Sildes for .NET API 參考
description: 取得或設定一個值，用以指示文字在渲染時是否不使用合字。當設定為 true 時，合字將在渲染的輸出中被停用。預設情況下，此屬性設定為 false。
type: docs
weight: 70
url: /zh-hant/aspose.slides.export/svgoptions/disablefontligatures/
---
## SVGOptions.DisableFontLigatures 屬性

取得或設定一個值，用以指示文字在渲染時是否不使用合字。當設定為 `true` 時，合字將在渲染的輸出中被停用。預設情況下，此屬性設定為 `false`。

```csharp
public bool DisableFontLigatures { get; set; }
```

### 範例

範例：

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    SVGOptions options = new SVFOptions
    {
        DisableFontLigatures = true // 在文字渲染中停用合字
    };
    
    using (FileStream fileStream = new FileStream("slide-0.svg", FileMode.Create, FileAccess.Write))
    {
        pres.Slides[0].WriteAsSvg(fileStream);   
    }
}
```

### 另請參閱

* 類別 [SVGOptions](../../svgoptions)
* 命名空間 [Aspose.Slides.Export](../../svgoptions)
* 程序集 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->