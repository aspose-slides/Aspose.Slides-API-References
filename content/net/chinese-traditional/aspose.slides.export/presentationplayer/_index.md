---
title: PresentationPlayer
second_title: Aspose.Sildes for .NET API 參考
description: 表示與 Presentation 相關的動畫播放器../aspose.slides/presentation.
type: docs
weight: 4390
url: /zh-hant/aspose.slides.export/presentationplayer/
---
## PresentationPlayer 類別

表示與 [`Presentation`](../../aspose.slides/presentation) 相關的動畫播放器。

```csharp
public class PresentationPlayer : IDisposable
```

## 建構函式

| 名稱 | 說明 |
| --- | --- |
| [PresentationPlayer](presentationplayer)(PresentationAnimationsGenerator, double) | 建立 [`PresentationPlayer`](../presentationplayer) 的新實例。 |

## 屬性

| 名稱 | 說明 |
| --- | --- |
| [FrameIndex](../../aspose.slides.export/presentationplayer/frameindex) { get; } | 取得影格索引。 |

## 方法

| 名稱 | 說明 |
| --- | --- |
| [Dispose](../../aspose.slides.export/presentationplayer/dispose)() | 釋放 [`PresentationPlayer`](../presentationplayer) 的執行個體。 |

## 其他成員

| 名稱 | 說明 |
| --- | --- |
| delegate [FrameTickHandler](presentationplayer.frametickhandler) | 表示 [`FrameTick`](./frametick) 事件的影格計時處理程式。 |

### 範例

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(pres))
    {
        // 以 33 FPS 播放動畫
        using (var player = new PresentationPlayer(animationsGenerator, 33))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("33fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
        
        // 以 45 FPS 播放動畫
        using (var player = new PresentationPlayer(animationsGenerator, 45))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("45fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
    }
}
```

### 另請參閱

* namespace [Aspose.Slides.Export](../../aspose.slides.export)
* assembly [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->