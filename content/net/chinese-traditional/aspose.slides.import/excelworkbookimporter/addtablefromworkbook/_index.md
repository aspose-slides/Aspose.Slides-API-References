---
title: AddTableFromWorkbook
second_title: Aspose.Sildes for .NET API 參考
description: 從指定的 Excel 工作簿中取得表格，並將其加入至給定形狀集合的末端，位置位於指定的座標。
type: docs
weight: 20
url: /zh-hant/aspose.slides.import/excelworkbookimporter/addtablefromworkbook/
---
## AddTableFromWorkbook(IShapeCollection, float, float, IExcelDataWorkbook, string, string) {#addtablefromworkbook}

從指定的 Excel 工作簿中取得表格，並將其加入至給定形狀集合的末端，位置位於指定的座標。

```csharp
public static ITable AddTableFromWorkbook(IShapeCollection shapes, float x, float y, 
    IExcelDataWorkbook workbook, string worksheetName, string cellRange)
```

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| shapes | IShapeCollection | 要加入表格的形狀集合。 |
| x | Single | 用於定位表格的 X 座標。 |
| y | Single | 用於定位表格的 Y 座標。 |
| workbook | IExcelDataWorkbook | Excel 工作簿。 |
| worksheetName | String | 包含表格之工作表的名稱。 |
| cellRange | String | 定義表格的儲存格範圍（例如 "A1:D10"）。 |

### 返回值

已加入形狀集合的表格。

### 例外狀況

| 例外 | 條件 |
| --- | --- |
| ArgumentException | 當任何必要參數為 null 或空字串，或指定的工作表或儲存格範圍無效時拋出。 |
| InvalidOperationException | 當輸入資料格式不支援時拋出。 |

### 範例

```csharp
[C#]
IExcelDataWorkbook workbook = new ExcelDataWorkbook(testFile);
using (var pres = new Presentation())
{
    ExcelWorkbookImporter.AddTableFromWorkbook(pres.Slides[0].Shapes, 10, 10, workbook, worksheetName, "A1:D10");
    pres.Save("result.pptx", SaveFormat.Pptx);
}
```

### 另請參閱

* 介面 [ITable](../../../aspose.slides/itable)
* 介面 [IShapeCollection](../../../aspose.slides/ishapecollection)
* 介面 [IExcelDataWorkbook](../../../aspose.slides.excel/iexceldataworkbook)
* 類別 [ExcelWorkbookImporter](../../excelworkbookimporter)
* 命名空間 [Aspose.Slides.Import](../../excelworkbookimporter)
* 組件 [Aspose.Slides](../../../)

---

## AddTableFromWorkbook(IShapeCollection, float, float, string, string, string) {#addtablefromworkbook_2}

從指定的 Excel 工作簿檔案中取得表格，並將其加入至給定形狀集合的末端，位置位於指定的座標。

```csharp
public static ITable AddTableFromWorkbook(IShapeCollection shapes, float x, float y, 
    string workbookPath, string worksheetName, string cellRange)
```

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| shapes | IShapeCollection | 要加入表格的形狀集合。 |
| x | Single | 用於定位表格的 X 座標。 |
| y | Single | 用於定位表格的 Y 座標。 |
| workbookPath | String | Excel 工作簿檔案的路徑。 |
| worksheetName | String | 包含表格之工作表的名稱。 |
| cellRange | String | 定義表格的儲存格範圍（例如 "A1:D10"）。 |

### 返回值

已加入形狀集合的表格。

### 例外狀況

| 例外 | 條件 |
| --- | --- |
| ArgumentException | 當任何必要參數為 null 或空字串，或指定的工作表或儲存格範圍無效時拋出。 |
| IOException | 當存取工作簿檔案時發生 I/O 錯誤時拋出。 |
| InvalidOperationException | 當輸入資料格式不支援時拋出。 |

### 範例

```csharp
[C#]
using (var pres = new Presentation())
{
    ExcelWorkbookImporter.AddTableFromWorkbook(pres.Slides[0].Shapes, 10, 10, workbookPath, worksheetName, "A1:D10");
    pres.Save("result.pptx", SaveFormat.Pptx);
}
```

### 另請參閱

* 介面 [ITable](../../../aspose.slides/itable)
* 介面 [IShapeCollection](../../../aspose.slides/ishapecollection)
* 類別 [ExcelWorkbookImporter](../../excelworkbookimporter)
* 命名空間 [Aspose.Slides.Import](../../excelworkbookimporter)
* 組件 [Aspose.Slides](../../../)

---

## AddTableFromWorkbook(IShapeCollection, float, float, Stream, string, string) {#addtablefromworkbook_1}

從指定的 Excel 工作簿檔案中取得表格，並將其加入至給定形狀集合的末端，位置位於指定的座標。

```csharp
public static ITable AddTableFromWorkbook(IShapeCollection shapes, float x, float y, 
    Stream workbookStream, string worksheetName, string cellRange)
```

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| shapes | IShapeCollection | 要加入表格的形狀集合。 |
| x | Single | 用於定位表格的 X 座標。 |
| y | Single | 用於定位表格的 Y 座標。 |
| workbookStream | Stream | 包含工作簿資料的串流。 |
| worksheetName | String | 包含表格之工作表的名稱。 |
| cellRange | String | 定義表格的儲存格範圍（例如 "A1:D10"）。 |

### 返回值

已加入形狀集合的表格。

### 例外狀況

| 例外 | 條件 |
| --- | --- |
| ArgumentException | 當任何必要參數為 null 或空字串，或指定的工作表或儲存格範圍無效時拋出。 |
| InvalidOperationException | 當輸入資料格式不支援時拋出。 |

### 範例

```csharp
[C#]
using (var fStream = new FileStream(workbookPath, FileMode.Open, FileAccess.Read))
using (var pres = new Presentation())
{
    ExcelWorkbookImporter.AddTableFromWorkbook(pres.Slides[0].Shapes, 10, 10, fStream, worksheetName, "A1:D10");
    pres.Save("result.pptx", SaveFormat.Pptx);
}
```

### 另請參閱

* 介面 [ITable](../../../aspose.slides/itable)
* 介面 [IShapeCollection](../../../aspose.slides/ishapecollection)
* 類別 [ExcelWorkbookImporter](../../excelworkbookimporter)
* 命名空間 [Aspose.Slides.Import](../../excelworkbookimporter)
* 組件 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->