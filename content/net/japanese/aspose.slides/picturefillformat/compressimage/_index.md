---
title: CompressImage
second_title: Aspose.Sildes for .NET API リファレンス
description: 形状のサイズと指定された解像度に基づいてサイズを縮小することで画像を圧縮します。オプションで、トリミングされた領域を削除することもできます。
type: docs
weight: 180
url: /ja/aspose.slides/picturefillformat/compressimage/
---
## CompressImage(bool, PicturesCompression) {#compressimage}

形状のサイズと指定された解像度に基づいてサイズを縮小することにより画像を圧縮します。オプションで、トリミングされた領域を削除することもできます。

```csharp
public bool CompressImage(bool deleteCroppedAreasOfImage, PicturesCompression resolution)
```

| パラメーター | 型 | 説明 |
| --- | --- | --- |
| deleteCroppedAreasOfImage | Boolean | true の場合、メソッドは画像のトリミングされた領域を削除し、サイズをさらに縮小する可能性があります。 |
| resolution | PicturesCompression | 圧縮の対象となる解像度で、[`PicturesCompression`](../../../aspose.slides.export/picturescompression) 列挙体の値として指定します。 |

### 戻り値

画像が正常に圧縮されたかを示す Boolean 値です。画像がサイズ変更またはトリミングされた場合は `true` を返し、そうでない場合は `false` を返します。

### 例外

| 例外 | 条件 |
| --- | --- |
| ArgumentException | 解像度が有効な値でない場合にスローされます。 |

### 備考

このメソッドは、PowerPoint の「画像の書式設定 → 画像の圧縮」機能と同様に画像のサイズと解像度を変更します。

### 例

```csharp
using (Presentation presentation = new Presentation("demo.pptx"))
{
    ISlide slide = presentation.Slides[0];
    IPictureFrame picFrame = slide.Shapes[0] as IPictureFrame;
    // 画像を 150 DPI（Web 解像度）の目標解像度で圧縮し、トリミングされた領域を削除します
    bool result = picFrame.PictureFormat.CompressImage(true, PicturesCompression.Dpi150);
}
```

### 関連項目

* enum [PicturesCompression](../../../aspose.slides.export/picturescompression)
* class [PictureFillFormat](../../picturefillformat)
* namespace [Aspose.Slides](../../picturefillformat)
* assembly [Aspose.Slides](../../../)

---

## CompressImage(bool, float) {#compressimage_1}

形状のサイズと指定された解像度に基づいてサイズを縮小することにより画像を圧縮します。オプションで、トリミングされた領域を削除することもできます。

```csharp
public bool CompressImage(bool deleteCroppedAreasOfImage, float resolution)
```

| パラメーター | 型 | 説明 |
| --- | --- | --- |
| deleteCroppedAreasOfImage | Boolean | true の場合、メソッドは画像のトリミングされた領域を削除し、サイズをさらに縮小する可能性があります。 |
| resolution | Single | DPI の対象解像度です。この値は正の数でなければならず、画像のサイズ変更方法を定義します。 |

### 戻り値

画像が正常に圧縮されたかを示す Boolean 値です。画像がサイズ変更またはトリミングされた場合は `true` を返し、そうでない場合は `false` を返します。

### 例外

| 例外 | 条件 |
| --- | --- |
| ArgumentException | 解像度が正の値でない場合にスローされます。 |

### 備考

このメソッドは、PowerPoint の「画像の書式設定 → 画像の圧縮」機能と同様に画像のサイズと解像度を変更します。

### 例

```csharp
[C#]
using (Presentation presentation = new Presentation("demo.pptx"))
{
    ISlide slide = presentation.Slides[0];

    // PictureFrame を取得します
    IPictureFrame picFrame = slide.Shapes[0] as IPictureFrame;

    // 画像を 150 DPI（Web 解像度）の目標解像度で圧縮し、トリミングされた領域を削除します
    bool result=picFrame.PictureFormat.CompressImage(true, 150f); // Web 解像度
}
```

### 関連項目

* class [PictureFillFormat](../../picturefillformat)
* namespace [Aspose.Slides](../../picturefillformat)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->