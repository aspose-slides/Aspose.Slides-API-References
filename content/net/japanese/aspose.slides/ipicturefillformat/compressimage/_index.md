---
title: CompressImage
second_title: Aspose.Sildes for .NET API リファレンス
description: シェイプのサイズと指定された解像度に基づいてサイズを縮小することで画像を圧縮します。オプションで、クロップされた領域を削除することもできます。
type: docs
weight: 190
url: /ja/aspose.slides/ipicturefillformat/compressimage/
---
## CompressImage(bool, PicturesCompression) {#compressimage}

画像をシェイプのサイズと指定された解像度に基づいてサイズを縮小することで圧縮します。オプションで、クロップされた領域を削除することもできます。

```csharp
public bool CompressImage(bool deleteCroppedAreasOfImage, PicturesCompression resolution)
```

| パラメーター | 型 | 説明 |
| --- | --- | --- |
| deleteCroppedAreasOfImage | Boolean | true の場合、メソッドは画像のクロップされた領域を削除し、サイズをさらに縮小する可能性があります。 |
| resolution | PicturesCompression | 圧縮の対象となる解像度で、[`PicturesCompression`](../../../aspose.slides.export/picturescompression) 列挙体の値として指定します。 |

### 戻り値

画像が正常に圧縮されたかどうかを示す Boolean です。画像がリサイズまたはクロップされた場合は `true` を返し、そうでない場合は `false` を返します。

### 例外

| 例外 | 条件 |
| --- | --- |
| ArgumentException | 解像度が有効な値でない場合にスローされます。 |

### 備考

このメソッドは、PowerPoint の「画像書式 -> 画像の圧縮」機能と同様に、画像のサイズと解像度を変更します。

### 例

以下の例は、ターゲット解像度を設定し、クロップされた領域を削除することで、プレゼンテーション内の画像のサイズを縮小する `CompressImage` メソッドの使用方法を示しています。

```csharp
using (Presentation presentation = new Presentation("demo.pptx"))
{
    ISlide slide = presentation.Slides[0];
    IPictureFrame picFrame = slide.Shapes[0] as IPictureFrame;
    // 画像をターゲット解像度 150 DPI（Web 解像度）で圧縮し、クロップされた領域を削除します
    bool result = picFrame.PictureFormat.CompressImage(true, PicturesCompression.Dpi150);
}
```

### 関連項目

* enum [PicturesCompression](../../../aspose.slides.export/picturescompression)
* interface [IPictureFillFormat](../../ipicturefillformat)
* namespace [Aspose.Slides](../../ipicturefillformat)
* assembly [Aspose.Slides](../../../)

---

## CompressImage(bool, float) {#compressimage_1}

画像をシェイプのサイズと指定された解像度に基づいてサイズを縮小することで圧縮します。オプションで、クロップされた領域を削除することもできます。

```csharp
public bool CompressImage(bool deleteCroppedAreasOfImage, float resolution)
```

| パラメーター | 型 | 説明 |
| --- | --- | --- |
| deleteCroppedAreasOfImage | Boolean | true の場合、メソッドは画像のクロップされた領域を削除し、サイズをさらに縮小する可能性があります。 |
| resolution | Single | DPI 単位のターゲット解像度です。この値は正の数である必要があり、画像のリサイズ方法を定義します。 |

### 戻り値

画像が正常に圧縮されたかどうかを示す Boolean です。画像がリサイズまたはクロップされた場合は `true` を返し、そうでない場合は `false` を返します。

### 例外

| 例外 | 条件 |
| --- | --- |
| ArgumentException | 解像度が正の値でない場合にスローされます。 |

### 備考

このメソッドは、PowerPoint の「画像書式 -> 画像の圧縮」機能と同様に、画像のサイズと解像度を変更します。

### 例

以下の例は、ターゲット解像度を設定し、クロップされた領域を削除することで、プレゼンテーション内の画像のサイズを縮小する `CompressImage` メソッドの使用方法を示しています。

```csharp
[C#]
using (Presentation presentation = new Presentation("demo.pptx"))
{
    ISlide slide = presentation.Slides[0];

    // PictureFrame を取得します
    IPictureFrame picFrame = slide.Shapes[0] as IPictureFrame;

    // 画像をターゲット解像度 150 DPI（Web 解像度）で圧縮し、クロップされた領域を削除します
    bool result=picFrame.PictureFormat.CompressImage(true, 150f); // Web 解像度
}
```

### 関連項目

* interface [IPictureFillFormat](../../ipicturefillformat)
* namespace [Aspose.Slides](../../ipicturefillformat)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->