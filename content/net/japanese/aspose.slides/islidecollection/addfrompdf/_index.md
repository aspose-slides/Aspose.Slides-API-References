---
title: AddFromPdf
second_title: Aspose.Sildes for .NET API リファレンス
description: PDF ドキュメントからスライドを作成し、コレクションの末尾に追加します。
type: docs
weight: 50
url: /ja/aspose.slides/islidecollection/addfrompdf/
---
## AddFromPdf(string) {#addfrompdf_2}

PDF ドキュメントからスライドを作成し、コレクションの末尾に追加します。

```csharp
public ISlide[] AddFromPdf(string path)
```

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| path | String | PDF ドキュメントへのパス |

### 戻り値

追加されたスライド

### 例

例:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    pres.Slides.AddFromPdf("document.pdf");
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### 関連項目

* インターフェイス [ISlide](../../islide)
* インターフェイス [ISlideCollection](../../islidecollection)
* 名前空間 [Aspose.Slides](../../islidecollection)
* アセンブリ [Aspose.Slides](../../../)

---

## AddFromPdf(string, PdfImportOptions) {#addfrompdf_3}

PDF ドキュメントからスライドを作成し、PDF のインポート オプションを考慮してコレクションの末尾に追加します。

```csharp
public ISlide[] AddFromPdf(string path, PdfImportOptions pdfImportOptions)
```

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| path | String | PDF ドキュメントへのパス |
| pdfImportOptions | PdfImportOptions | PDF インポートのオプション |

### 戻り値

追加されたスライド

### 例

例:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    pres.Slides.AddFromPdf("document.pdf, new PdfImportOptions { DetectTables = true });
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### 関連項目

* インターフェイス [ISlide](../../islide)
* クラス [PdfImportOptions](../../../aspose.slides.import/pdfimportoptions)
* インターフェイス [ISlideCollection](../../islidecollection)
* 名前空間 [Aspose.Slides](../../islidecollection)
* アセンブリ [Aspose.Slides](../../../)

---

## AddFromPdf(Stream, PdfImportOptions) {#addfrompdf_1}

PDF ドキュメントからスライドを作成し、コレクションの末尾に追加します。

```csharp
public ISlide[] AddFromPdf(Stream pdfStream, PdfImportOptions pdfImportOptions)
```

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| pdfStream | Stream | PDF ドキュメントのソースとして使用されるストリーム |
| pdfImportOptions | PdfImportOptions | PDF インポートのオプション |

### 戻り値

追加されたスライド

### 例

例:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    using (Stream stream = new FileStream("document.pdf", FileMode.Open, FileAccess.Read, FileShare.Read))
    {
        pres.Slides.AddFromPdf(stream, new PdfImportOptions { DetectTables = true });
    }
    
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### 関連項目

* インターフェイス [ISlide](../../islide)
* クラス [PdfImportOptions](../../../aspose.slides.import/pdfimportoptions)
* インターフェイス [ISlideCollection](../../islidecollection)
* 名前空間 [Aspose.Slides](../../islidecollection)
* アセンブリ [Aspose.Slides](../../../)

---

## AddFromPdf(Stream) {#addfrompdf}

PDF ドキュメントからスライドを作成し、コレクションの末尾に追加します。

```csharp
public ISlide[] AddFromPdf(Stream pdfStream)
```

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| pdfStream | Stream | PDF ドキュメントのソースとして使用されるストリーム |

### 戻り値

追加されたスライド

### 例

例:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    using (Stream stream = new FileStream("document.pdf", FileMode.Open, FileAccess.Read, FileShare.Read))
    {
        pres.Slides.AddFromPdf(stream);
    }
    
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### 関連項目

* インターフェイス [ISlide](../../islide)
* インターフェイス [ISlideCollection](../../islidecollection)
* 名前空間 [Aspose.Slides](../../islidecollection)
* アセンブリ [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->