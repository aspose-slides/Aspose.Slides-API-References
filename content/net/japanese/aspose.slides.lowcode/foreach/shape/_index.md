---
title: Shape
second_title: Aspose.Sildes for .NET API リファレンス
description: Presentationaspose.slides/presentation 内の Shapeaspose.slides.lowcode/foreach/shape をそれぞれ反復処理します。Shapes はすべてのスライドタイプ - Slideaspose.slides.lowcode/foreach/slide MasterSlideaspose.slides.lowcode/foreach/masterslide と LayoutSlideaspose.slides.lowcode/foreach/layoutslide で反復されます。
type: docs
weight: 50
url: /ja/aspose.slides.lowcode/foreach/shape/
---
## Shape(Presentation, ForEachShapeCallback) {#shape_2}

[`Presentation`](../../../aspose.slides/presentation)内のすべての`Shape`を反復処理します。スライドのすべてのタイプ（[`Slide`](../slide)、[`MasterSlide`](../masterslide)、[`LayoutSlide`](../layoutslide)）で形状が反復されます。

```csharp
public static void Shape(Presentation pres, ForEachShapeCallback forEachShape)
```

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| pres | Presentation | Presentation to iterate layout shapes |
| forEachShape | ForEachShapeCallback | Callback that will be invoked for each shape |

### 例

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Shape(pres, (shape, slide, index) => 
    {
        System.Console.WriteLine($"{shape.Name}, index: {index}");
    });
} 
```

### 参照

* クラス [Presentation](../../../aspose.slides/presentation)
* デリゲート [ForEachShapeCallback](../../foreach.foreachshapecallback)
* クラス [ForEach](../../foreach)
* 名前空間 [Aspose.Slides.LowCode](../../foreach)
* アセンブリ [Aspose.Slides](../../../)

---

## Shape(Presentation, bool, ForEachShapeCallback) {#shape_1}

[`Shape`](../shape)を[`Presentation`](../../../aspose.slides/presentation)内で反復処理します。必要に応じて、すべてのスライドタイプ（[`Slide`](../slide)、[`MasterSlide`](../masterslide)、[`LayoutSlide`](../layoutslide)、[`NotesSlide`](../../../aspose.slides/notesslide)）でも形状が反復されます。

```csharp
public static void Shape(Presentation pres, bool includeNotes, ForEachShapeCallback forEachShape)
```

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| pres | Presentation | Presentation to iterate layout shapes |
| includeNotes | Boolean | Flag that indicates whether NotesSlides should be included in processing. |
| forEachShape | ForEachShapeCallback | Callback that will be invoked for each shape |

### 例

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Shape(pres, true, (shape, slide, index) => 
    {
        System.Console.WriteLine($"{shape.Name}, index: {index}");
    });
} 
```

### 参照

* クラス [Presentation](../../../aspose.slides/presentation)
* デリゲート [ForEachShapeCallback](../../foreach.foreachshapecallback)
* クラス [ForEach](../../foreach)
* 名前空間 [Aspose.Slides.LowCode](../../foreach)
* アセンブリ [Aspose.Slides](../../../)

---

## Shape(BaseSlide, ForEachShapeCallback) {#shape}

[`BaseSlide`](../../../aspose.slides/baseslide)内のすべての[`Shape`](../shape)を反復処理します。[`BaseSlide`](../../../aspose.slides/baseslide)は、[`Slide`](../slide)、[`MasterSlide`](../masterslide)、[`LayoutSlide`](../layoutslide)の基底型です。

```csharp
public static void Shape(BaseSlide baseSlide, ForEachShapeCallback forEachShape)
```

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| baseSlide | BaseSlide | Slide to iterate layout shapes |
| forEachShape | ForEachShapeCallback | Callback that will be invoked for each shape |

### 例

```csharp
(Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Slide(pres, (slide, index) =>
    {
        ForEach.Shape(slide, (shape, baseSlide, shapeIndex) =>
        {
            System.Console.WriteLine($"{shape.Name}, index: {shapeIndex}");
        });
    });
} 
```

### 参照

* クラス [BaseSlide](../../../aspose.slides/baseslide)
* デリゲート [ForEachShapeCallback](../../foreach.foreachshapecallback)
* クラス [ForEach](../../foreach)
* 名前空間 [Aspose.Slides.LowCode](../../foreach)
* アセンブリ [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->