---
title: DisableFontLigatures
second_title: Aspose.Sildes for .NET API リファレンス
description: テキストが合字を使用せずに描画されるかどうかを示す値を取得または設定します。true に設定すると、描画された出力で合字が無効になります。既定では、このプロパティは false に設定されています。
type: docs
weight: 20
url: /ja/aspose.slides.export/renderingoptions/disablefontligatures/
---
## RenderingOptions.DisableFontLigatures プロパティ

テキストが合字を使用せずに描画されるかどうかを示す値を取得または設定します。`true` に設定すると、描画された出力で合字が無効になります。既定では、このプロパティは `false` に設定されています。

```csharp
public bool DisableFontLigatures { get; set; }
```

### 例

例:

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    RenderingOptions options = new RenderingOptions
    {
        DisableFontLigatures = true // テキストの描画で合字を無効にする
    };
    
    Bitmap[] renderedSlides = pres.GetImage(options);
    for (var index = 0; index < renderedSlides.Length; index++)
    {
        var slideImage = renderedSlides[index];
        slideImage.Save($"slide-{index}.png");
    }
}
```

### 参照

* クラス [RenderingOptions](../../renderingoptions)
* 名前空間 [Aspose.Slides.Export](../../renderingoptions)
* アセンブリ [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->