---
title: DisableFontLigatures
second_title: Aspose.Sildes の .NET API リファレンス
description: テキストが合字を使用せずに描画されるかどうかを示す値を取得または設定します。true に設定すると、描画された出力で合字が無効になります。デフォルトでは、このプロパティは false に設定されています。
type: docs
weight: 40
url: /ja/aspose.slides.export/isvgoptions/disablefontligatures/
---
## ISSVGOptions.DisableFontLigatures プロパティ

テキストが合字を使用せずに描画されるかどうかを示す値を取得または設定します。`true` に設定すると、描画された出力で合字が無効になります。デフォルトでは、このプロパティは `false` に設定されています。

```csharp
public bool DisableFontLigatures { get; set; }
```

### 例

例:

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    SVGOptions options = new SVFOptions
    {
        DisableFontLigatures = true // テキスト描画時に合字を無効にする
    };
    
    using (FileStream fileStream = new FileStream("slide-0.svg", FileMode.Create, FileAccess.Write))
    {
        pres.Slides[0].WriteAsSvg(fileStream);   
    }
}
```

### 参照

* インターフェイス [ISVGOptions](../../isvgoptions)
* 名前空間 [Aspose.Slides.Export](../../isvgoptions)
* アセンブリ [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->