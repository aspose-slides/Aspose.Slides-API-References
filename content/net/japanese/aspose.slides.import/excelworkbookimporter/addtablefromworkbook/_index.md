---
title: AddTableFromWorkbook
second_title: Aspose.Sildes for .NET API リファレンス
description: 指定された Excel ワークブックからテーブルを取得し、指定された座標にあるシェイプ コレクションの末尾に追加します。
type: docs
weight: 20
url: /ja/aspose.slides.import/excelworkbookimporter/addtablefromworkbook/
---
## AddTableFromWorkbook(IShapeCollection, float, float, IExcelDataWorkbook, string, string) {#addtablefromworkbook}

指定された Excel ワークブックからテーブルを取得し、指定された座標にある対象のシェイプ コレクションの末尾に追加します。

```csharp
public static ITable AddTableFromWorkbook(IShapeCollection shapes, float x, float y, 
    IExcelDataWorkbook workbook, string worksheetName, string cellRange)
```

| パラメーター | 型 | 説明 |
| --- | --- | --- |
| shapes | IShapeCollection | テーブルが追加されるシェイプ コレクション。 |
| x | Single | テーブルの配置座標 X。 |
| y | Single | テーブルの配置座標 Y。 |
| workbook | IExcelDataWorkbook | Excel ワークブック。 |
| worksheetName | String | テーブルを含むワークシートの名前。 |
| cellRange | String | テーブルを定義するセル範囲（例: "A1:D10"）。 |

### 戻り値

シェイプ コレクションに追加されたテーブル。

### 例外

| 例外 | 条件 |
| --- | --- |
| ArgumentException | 必須パラメーターが null または空の場合、または指定されたワークシートやセル範囲が無効な場合にスローされます。 |
| InvalidOperationException | 入力データがサポートされていない形式の場合にスローされます。 |

### 例

```csharp
[C#]
IExcelDataWorkbook workbook = new ExcelDataWorkbook(testFile);
using (var pres = new Presentation())
{
    ExcelWorkbookImporter.AddTableFromWorkbook(pres.Slides[0].Shapes, 10, 10, workbook, worksheetName, "A1:D10");
    pres.Save("result.pptx", SaveFormat.Pptx);
}
```

### 関連項目

* インターフェイス [ITable](../../../aspose.slides/itable)
* インターフェイス [IShapeCollection](../../../aspose.slides/ishapecollection)
* インターフェイス [IExcelDataWorkbook](../../../aspose.slides.excel/iexceldataworkbook)
* クラス [ExcelWorkbookImporter](../../excelworkbookimporter)
* 名前空間 [Aspose.Slides.Import](../../excelworkbookimporter)
* アセンブリ [Aspose.Slides](../../../)

---

## AddTableFromWorkbook(IShapeCollection, float, float, string, string, string) {#addtablefromworkbook_2}

指定された Excel ワークブック ファイルからテーブルを取得し、指定された座標にある対象のシェイプ コレクションの末尾に追加します。

```csharp
public static ITable AddTableFromWorkbook(IShapeCollection shapes, float x, float y, 
    string workbookPath, string worksheetName, string cellRange)
```

| パラメーター | 型 | 説明 |
| --- | --- | --- |
| shapes | IShapeCollection | テーブルが追加されるシェイプ コレクション。 |
| x | Single | テーブルの配置座標 X。 |
| y | Single | テーブルの配置座標 Y。 |
| workbookPath | String | Excel ワークブック ファイルへのパス。 |
| worksheetName | String | テーブルを含むワークシートの名前。 |
| cellRange | String | テーブルを定義するセル範囲（例: "A1:D10"）。 |

### 戻り値

シェイプ コレクションに追加されたテーブル。

### 例外

| 例外 | 条件 |
| --- | --- |
| ArgumentException | 必須パラメーターが null または空の場合、または指定されたワークシートやセル範囲が無効な場合にスローされます。 |
| IOException | ワークブック ファイルにアクセス中に I/O エラーが発生した場合にスローされます。 |
| InvalidOperationException | 入力データがサポートされていない形式の場合にスローされます。 |

### 例

```csharp
[C#]
using (var pres = new Presentation())
{
    ExcelWorkbookImporter.AddTableFromWorkbook(pres.Slides[0].Shapes, 10, 10, workbookPath, worksheetName, "A1:D10");
    pres.Save("result.pptx", SaveFormat.Pptx);
}
```

### 関連項目

* インターフェイス [ITable](../../../aspose.slides/itable)
* インターフェイス [IShapeCollection](../../../aspose.slides/ishapecollection)
* クラス [ExcelWorkbookImporter](../../excelworkbookimporter)
* 名前空間 [Aspose.Slides.Import](../../excelworkbookimporter)
* アセンブリ [Aspose.Slides](../../../)

---

## AddTableFromWorkbook(IShapeCollection, float, float, Stream, string, string) {#addtablefromworkbook_1}

指定された Excel ワークブック ファイルからテーブルを取得し、指定された座標にある対象のシェイプ コレクションの末尾に追加します。

```csharp
public static ITable AddTableFromWorkbook(IShapeCollection shapes, float x, float y, 
    Stream workbookStream, string worksheetName, string cellRange)
```

| パラメーター | 型 | 説明 |
| --- | --- | --- |
| shapes | IShapeCollection | テーブルが追加されるシェイプ コレクション。 |
| x | Single | テーブルの配置座標 X。 |
| y | Single | テーブルの配置座標 Y。 |
| workbookStream | Stream | ワークブック データを含むストリーム。 |
| worksheetName | String | テーブルを含むワークシートの名前。 |
| cellRange | String | テーブルを定義するセル範囲（例: "A1:D10"）。 |

### 戻り値

シェイプ コレクションに追加されたテーブル。

### 例外

| 例外 | 条件 |
| --- | --- |
| ArgumentException | 必須パラメーターが null または空の場合、または指定されたワークシートやセル範囲が無効な場合にスローされます。 |
| InvalidOperationException | 入力データがサポートされていない形式の場合にスローされます。 |

### 例

```csharp
[C#]
using (var fStream = new FileStream(workbookPath, FileMode.Open, FileAccess.Read))
using (var pres = new Presentation())
{
    ExcelWorkbookImporter.AddTableFromWorkbook(pres.Slides[0].Shapes, 10, 10, fStream, worksheetName, "A1:D10");
    pres.Save("result.pptx", SaveFormat.Pptx);
}
```

### 関連項目

* インターフェイス [ITable](../../../aspose.slides/itable)
* インターフェイス [IShapeCollection](../../../aspose.slides/ishapecollection)
* クラス [ExcelWorkbookImporter](../../excelworkbookimporter)
* 名前空間 [Aspose.Slides.Import](../../excelworkbookimporter)
* アセンブリ [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->