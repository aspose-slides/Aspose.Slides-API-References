---
title: GetCell
second_title: Aspose.Sildes for .NET API リファレンス
description: 指定されたワークシートからインデックスとセル座標を使用してセルを取得します。
type: docs
weight: 10
url: /ja/aspose.slides.excel/iexceldataworkbook/getcell/
---
## GetCell(int, int, int) {#getcell}

指定されたワークシートからインデックスとセル座標を使用してセルを取得します。

```csharp
public IExcelDataCell GetCell(int worksheetIndex, int row, int column)
```

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| worksheetIndex | Int32 | ワークシートのゼロベースインデックス。 |
| row | Int32 | セルのゼロベース行インデックス。 |
| column | Int32 | セルのゼロベース列インデックス。 |

### 戻り値

指定された位置のセル。

### 例

例:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell(1, 1, 1);
Console.WriteLine(cell.Value.ToString());
```

### 参照

* interface [IExcelDataCell](../../iexceldatacell)
* interface [IExcelDataWorkbook](../../iexceldataworkbook)
* namespace [Aspose.Slides.Excel](../../iexceldataworkbook)
* assembly [Aspose.Slides](../../../)

---

## GetCell(string, int, int) {#getcell_2}

指定されたワークシートから名前とセル座標を使用してセルを取得します。

```csharp
public IExcelDataCell GetCell(string worksheetName, int row, int column)
```

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| worksheetName | String | ワークシートの名前。 |
| row | Int32 | セルのゼロベース行インデックス。 |
| column | Int32 | セルのゼロベース列インデックス。 |

### 戻り値

指定された位置のセル。

### 例

例:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell("Sheet1", 1, 1);
Console.WriteLine(cell.Value.ToString());
```

### 参照

* interface [IExcelDataCell](../../iexceldatacell)
* interface [IExcelDataWorkbook](../../iexceldataworkbook)
* namespace [Aspose.Slides.Excel](../../iexceldataworkbook)
* assembly [Aspose.Slides](../../../)

---

## GetCell(int, string) {#getcell_1}

指定されたワークシートからインデックスと Excel 形式のセル名（例: "B2"）を使用してセルを取得します。

```csharp
public IExcelDataCell GetCell(int worksheetIndex, string cellName)
```

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| worksheetIndex | Int32 | ワークシートのゼロベースインデックス。 |
| cellName | String | Excel 形式のセル参照（例: "A1", "C5"）。 |

### 戻り値

指定された位置のセル。

### 例

例:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell(1, "B2");
Console.WriteLine(cell.Value.ToString());
```

### 参照

* interface [IExcelDataCell](../../iexceldatacell)
* interface [IExcelDataWorkbook](../../iexceldataworkbook)
* namespace [Aspose.Slides.Excel](../../iexceldataworkbook)
* assembly [Aspose.Slides](../../../)

---

## GetCell(string, string) {#getcell_3}

指定されたワークシートから Excel 形式のセル名（例: "B2"）を使用してセルを取得します。

```csharp
public IExcelDataCell GetCell(string worksheetName, string cellName)
```

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| worksheetName | String | ワークシートの名前。 |
| cellName | String | Excel 形式のセル参照（例: "A1", "C5"）。 |

### 戻り値

指定された位置のセル。

### 例

例:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell("Sheet1", "B2");
Console.WriteLine(cell.Value.ToString());
```

### 参照

* interface [IExcelDataCell](../../iexceldatacell)
* interface [IExcelDataWorkbook](../../iexceldataworkbook)
* namespace [Aspose.Slides.Excel](../../iexceldataworkbook)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->