---
title: GetCell
second_title: Aspose.Sildes for .NET API リファレンス
description: 指定されたワークシートから、インデックスとセル座標を使用してセルを取得します。
type: docs
weight: 20
url: /ja/aspose.slides.excel/exceldataworkbook/getcell/
---
## GetCell(int, int, int) {#getcell}

指定したワークシートから、インデックスとセル座標を使用してセルを取得します。

```csharp
public IExcelDataCell GetCell(int worksheetIndex, int row, int column)
```

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| worksheetIndex | Int32 | ワークシートの 0 ベースインデックス。 |
| row | Int32 | セルの行インデックス（0 ベース）。 |
| column | Int32 | セルの列インデックス（0 ベース）。 |

### 戻り値

指定された位置にあるセル。

### 例

例:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell(1, 1, 1);
Console.WriteLine(cell.Value.ToString());
```

### 関連項目

* インターフェイス [IExcelDataCell](../../iexceldatacell)
* クラス [ExcelDataWorkbook](../../exceldataworkbook)
* 名前空間 [Aspose.Slides.Excel](../../exceldataworkbook)
* アセンブリ [Aspose.Slides](../../../)

---

## GetCell(string, int, int) {#getcell_2}

指定したワークシートから、名前とセル座標を使用してセルを取得します。

```csharp
public IExcelDataCell GetCell(string worksheetName, int row, int column)
```

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| worksheetName | String | ワークシートの名前。 |
| row | Int32 | セルの行インデックス（0 ベース）。 |
| column | Int32 | セルの列インデックス（0 ベース）。 |

### 戻り値

指定された位置にあるセル。

### 例

例:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell("Sheet1", 1, 1);
Console.WriteLine(cell.Value.ToString());
```

### 関連項目

* インターフェイス [IExcelDataCell](../../iexceldatacell)
* クラス [ExcelDataWorkbook](../../exceldataworkbook)
* 名前空間 [Aspose.Slides.Excel](../../exceldataworkbook)
* アセンブリ [Aspose.Slides](../../../)

---

## GetCell(int, string) {#getcell_1}

指定したワークシートから、インデックスと Excel 形式のセル名（例: "B2"）を使用してセルを取得します。

```csharp
public IExcelDataCell GetCell(int worksheetIndex, string cellName)
```

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| worksheetIndex | Int32 | ワークシートの 0 ベースインデックス。 |
| cellName | String | Excel 形式のセル参照（例: "A1", "C5"）。 |

### 戻り値

指定された位置にあるセル。

### 例

例:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell(1, "B2");
Console.WriteLine(cell.Value.ToString());
```

### 関連項目

* インターフェイス [IExcelDataCell](../../iexceldatacell)
* クラス [ExcelDataWorkbook](../../exceldataworkbook)
* 名前空間 [Aspose.Slides.Excel](../../exceldataworkbook)
* アセンブリ [Aspose.Slides](../../../)

---

## GetCell(string, string) {#getcell_3}

指定したワークシートから、Excel 形式のセル名（例: "B2"）を使用してセルを取得します。

```csharp
public IExcelDataCell GetCell(string worksheetName, string cellName)
```

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| worksheetName | String | ワークシートの名前。 |
| cellName | String | Excel 形式のセル参照（例: "A1", "C5"）。 |

### 戻り値

指定された位置にあるセル。

### 例

例:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell("Sheet1", "B2");
Console.WriteLine(cell.Value.ToString());
```

### 関連項目

* インターフェイス [IExcelDataCell](../../iexceldatacell)
* クラス [ExcelDataWorkbook](../../exceldataworkbook)
* 名前空間 [Aspose.Slides.Excel](../../exceldataworkbook)
* アセンブリ [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->