---
title: FieldType
second_title: Aspose.Sildes สำหรับ .NET API Reference
description: แทนประเภทของฟิลด์ ค่าตัวนี้กำหนดว่าจะตั้งข้อความใดให้กับส่วนของฟิลด์เมื่อทำการอัปเดต
type: docs
weight: 4760
url: /th/aspose.slides/fieldtype/
---
## FieldType คลาส

แทนประเภทของฟิลด์ ค่าตัวนี้กำหนดว่าจะตั้งข้อความใดให้กับส่วนของฟิลด์เมื่อทำการอัปเดต

```csharp
public sealed class FieldType : IFieldType
```

## ตัวสร้าง

| ชื่อ | คำอธิบาย |
| --- | --- |
| [FieldType](fieldtype)(string) | สร้างอินสแตนซ์ใหม่ของคลาส FieldType |

## คุณสมบัติ

| ชื่อ | คำอธิบาย |
| --- | --- |
| static [DateTime](../../aspose.slides/fieldtype/datetime) { get; } | วันที่และเวลาปัจจุบันในรูปแบบวันที่เวลาเริ่มต้นสำหรับแอปพลิเคชันการเรนเดอร์ อ่านอย่างเดียว [`FieldType`](../fieldtype). |
| static [DateTime1](../../aspose.slides/fieldtype/datetime1) { get; } | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้เป็นลำดับแรก (MM/DD/YYYY สำหรับภาษาอังกฤษ) อ่านอย่างเดียว [`FieldType`](../fieldtype). |
| static [DateTime10](../../aspose.slides/fieldtype/datetime10) { get; } | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ลำดับที่สิบ (hh:mm สำหรับภาษาอังกฤษ) อ่านอย่างเดียว [`FieldType`](../fieldtype). |
| static [DateTime11](../../aspose.slides/fieldtype/datetime11) { get; } | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ลำดับที่สิบเอ็ด (hh:mm:ss สำหรับภาษาอังกฤษ) อ่านอย่างเดียว [`FieldType`](../fieldtype). |
| static [DateTime12](../../aspose.slides/fieldtype/datetime12) { get; } | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ลำดับที่สิบสอง (hh:mm AM/PM สำหรับภาษาอังกฤษ) อ่านอย่างเดียว [`FieldType`](../fieldtype). |
| static [DateTime13](../../aspose.slides/fieldtype/datetime13) { get; } | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ลำดับที่สิบสาม (hh:mm:ss AM/PM สำหรับภาษาอังกฤษ) อ่านอย่างเดียว [`FieldType`](../fieldtype). |
| static [DateTime2](../../aspose.slides/fieldtype/datetime2) { get; } | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ลำดับที่สอง (Day, Month DD, YYYY สำหรับภาษาอังกฤษ) อ่านอย่างเดียว [`FieldType`](../fieldtype). |
| static [DateTime3](../../aspose.slides/fieldtype/datetime3) { get; } | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ลำดับที่สาม (DD Month YYYY สำหรับภาษาอังกฤษ) อ่านอย่างเดียว [`FieldType`](../fieldtype). |
| static [DateTime4](../../aspose.slides/fieldtype/datetime4) { get; } | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ลำดับที่สี่ (Month DD, YYYY สำหรับภาษาอังกฤษ) อ่านอย่างเดียว [`FieldType`](../fieldtype). |
| static [DateTime5](../../aspose.slides/fieldtype/datetime5) { get; } | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ลำดับที่ห้า (DD-Mon-YY สำหรับภาษาอังกฤษ) อ่านอย่างเดียว [`FieldType`](../fieldtype). |
| static [DateTime6](../../aspose.slides/fieldtype/datetime6) { get; } | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ลำดับที่หก (Month YY สำหรับภาษาอังกฤษ) อ่านอย่างเดียว [`FieldType`](../fieldtype). |
| static [DateTime7](../../aspose.slides/fieldtype/datetime7) { get; } | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ลำดับที่เจ็ด (Mon-YY สำหรับภาษาอังกฤษ) อ่านอย่างเดียว [`FieldType`](../fieldtype). |
| static [DateTime8](../../aspose.slides/fieldtype/datetime8) { get; } | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ลำดับที่แปด (MM/DD/YYYY hh:mm AM/PM สำหรับภาษาอังกฤษ) อ่านอย่างเดียว [`FieldType`](../fieldtype). |
| static [DateTime9](../../aspose.slides/fieldtype/datetime9) { get; } | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ลำดับที่เก้า (MM/DD/YYYY hh:mm:ss AM/PM สำหรับภาษาอังกฤษ) อ่านอย่างเดียว [`FieldType`](../fieldtype). |
| static [Footer](../../aspose.slides/fieldtype/footer) { get; } | ส่วนท้ายของสไลด์ อ่านอย่างเดียว [`FieldType`](../fieldtype). |
| static [Header](../../aspose.slides/fieldtype/header) { get; } | ส่วนหัวของสไลด์ อ่านอย่างเดียว [`FieldType`](../fieldtype). |
| static [SlideNumber](../../aspose.slides/fieldtype/slidenumber) { get; } | หมายเลขสไลด์ปัจจุบัน อ่านอย่างเดียว [`FieldType`](../fieldtype). |
| [InternalString](../../aspose.slides/fieldtype/internalstring) { get; set; } | คืนชื่อภายในของอ็อบเจกต์ FieldType นี้. อ่าน/เขียน String. |

## เมธอด

| ชื่อ | คำอธิบาย |
| --- | --- |
| override [Equals](../../aspose.slides/fieldtype/equals)(object) | ตรวจสอบว่าฟิลด์นี้เท่ากับฟิลด์อื่นหรือไม่ |
| override [GetHashCode](../../aspose.slides/fieldtype/gethashcode)() | คืนค่า hashcode สำหรับอ็อบเจกต์นี้ |
| [operator ==](../../aspose.slides/fieldtype/op_equality) | ตรวจสอบว่าสองอ็อบเจกต์ FieldType เท่ากันหรือไม่ |
| [operator !=](../../aspose.slides/fieldtype/op_inequality) | ตรวจสอบว่าสองอ็อบเจกต์ FieldType ไม่เท่ากัน |

### ดูเพิ่มเติม

* อินเทอร์เฟซ [IFieldType](../ifieldtype)
* เนมสเปซ [Aspose.Slides](../../aspose.slides)
* แอสเซมบลี [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->