---
title: AddTextFrame
second_title: Aspose.Sildes สำหรับ .NET อ้างอิง API
description: เพิ่ม TextFrame ใหม่ให้กับรูปร่าง หากรูปร่างมี TextFrame อยู่แล้วจะเปลี่ยนข้อความของมันเท่านั้น
type: docs
weight: 60
url: /th/aspose.slides/autoshape/addtextframe/
---
## AutoShape.AddTextFrame method

เพิ่ม TextFrame ใหม่ให้กับรูปร่าง หากรูปร่างมี TextFrame แล้วก็จะเปลี่ยนข้อความของมันเท่านั้น

```csharp
public ITextFrame AddTextFrame(string text)
```

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| text | String | ข้อความเริ่มต้นสำหรับ TextFrame ใหม่. |

### ตัวอย่าง

โค้ดตัวอย่างต่อไปนี้แสดงวิธีเพิ่มข้อความลายน้ำใน PowerPoint Presentation.

```csharp
[C#]
	using (var presentation = new Presentation())
	{
		ISlide slide = presentation.Slides[0];
		IAutoShape watermarkShape = slide.Shapes.AddAutoShape(ShapeType.Triangle, 0, 0, 150, 50);
		ITextFrame watermarkTextFrame = watermarkShape.AddTextFrame("Watermark");
	}
```

ตัวอย่างต่อไปนี้แสดงวิธีสร้าง Text Box บน Slide.

```csharp
[C#]
// สร้างอินสแตนซ์ Presentation
using (Presentation pres = new Presentation())
{
    // รับสไลด์แรกใน Presentation
    ISlide sld = pres.Slides[0];
    // เพิ่ม AutoShape ที่ตั้งค่าเป็น Rectangle
    IAutoShape ashp = sld.Shapes.AddAutoShape(ShapeType.Rectangle, 150, 75, 150, 50);
    // เพิ่ม TextFrame ให้กับ Rectangle
    ashp.AddTextFrame(" ");
    // เข้าถึง TextFrame
    ITextFrame txtFrame = ashp.TextFrame;
    // สร้างอ็อบเจ็กต์ Paragraph สำหรับ TextFrame
    IParagraph para = txtFrame.Paragraphs[0];
    // สร้างอ็อบเจ็กต์ Portion สำหรับ Paragraph
    IPortion portion = para.Portions[0];
    // ตั้งค่าข้อความ
    portion.Text = "Aspose TextBox";
    // บันทึก Presentation ไปยังดิสก์
    pres.Save("TextBox_out.pptx", Aspose.Slides.Export.SaveFormat.Pptx);
}
```

ตัวอย่างต่อไปนี้แสดงวิธีเพิ่มคอลัมน์ใน Text Box.

```csharp
[C#]
using (Presentation presentation = new Presentation())
{
	// รับสไลด์แรกใน Presentation
	ISlide slide = presentation.Slides[0];
	// เพิ่ม AutoShape ที่ตั้งค่าเป็น Rectangle
	IAutoShape aShape = slide.Shapes.AddAutoShape(ShapeType.Rectangle, 100, 100, 300, 300);
	// เพิ่ม TextFrame ให้กับ Rectangle
	aShape.AddTextFrame("All these columns are limited to be within a single text container -- " +
	"you can add or delete text and the new or remaining text automatically adjusts " +
	"itself to flow within the container. You cannot have text flow from one container " +
	"to other though -- we told you PowerPoint's column options for text are limited!");
	// รับรูปแบบข้อความของ TextFrame
	ITextFrameFormat format = aShape.TextFrame.TextFrameFormat;
	// ระบุจำนวนคอลัมน์ใน TextFrame
	format.ColumnCount = 3;
	// ระบุระยะห่างระหว่างคอลัมน์
	format.ColumnSpacing = 10;
	// บันทึก Presentation
	presentation.Save("ColumnCount.pptx", SaveFormat.Pptx);
}
```

### ดูเพิ่มเติม

* อินเทอร์เฟซ [ITextFrame](../../itextframe)
* คลาส [AutoShape](../../autoshape)
* เนมสเปซ [Aspose.Slides](../../autoshape)
* แอสเซมบลี [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->