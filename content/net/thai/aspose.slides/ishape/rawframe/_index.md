---
title: RawFrame
second_title: Aspose.Sildes สำหรับ .NET API Reference
description: คืนค่า หรือ ตั้งค่าคุณสมบัติของเฟรมรูปแบบดิบของ shape. อ่าน/เขียน IShapeFrameaspose.slides/ishapeframe.
type: docs
weight: 210
url: /th/aspose.slides/ishape/rawframe/
---
## IShape.RawFrame คุณสมบัติ

คืนค่า หรือ ตั้งค่าคุณสมบัติของกรอบรูปแบบดิบของ shape. อ่าน/เขียน [`IShapeFrame`](../../ishapeframe).

```csharp
public IShapeFrame RawFrame { get; set; }
```

### ตัวอย่าง

โค้ดที่พยายามกำหนดเฟรมที่ไม่ได้กำหนดค่าให้ IShape.Frame ไม่สมเหตุสมผลในกรณีทั่วไป (โดยเฉพาะในกรณีที่ GroupShape พาเรนต์ซ้อนหลายระดับใน GroupShape อื่น). ตัวอย่างเช่น:

```csharp
IShape shape = ...;
shape.Frame = new ShapeFrame(float.NaN, float.NaN, float.NaN, float.NaN, NullableBool.NotDefined, NullableBool.NotDefined, float.NaN);
```

หรือ

```csharp
slide.Shapes.AddAutoShape(ShapeType.RoundCornerRectangle, float.NaN, float.NaN, float.NaN, float.NaN);
```

โค้ดดังกล่าวอาจทำให้เกิดสถานการณ์ที่ไม่ชัดเจน ดังนั้นจึงมีการเพิ่มข้อจำกัดสำหรับการใช้ค่าที่ไม่ได้กำหนดให้กับ IShape.Frame ค่าของ x, y, width, height, flipH, flipV และ rotationAngle จะต้องถูกกำหนด (ไม่ใช่ float.NaN หรือ NullableBool.NotDefined) ตัวอย่างโค้ดด้านบนตอนนี้จะทำให้เกิดข้อยกเว้น ArgumentException นี้ใช้กับกรณีการใช้งานต่อไปนี้:

```csharp
IShape shape = ...;
shape.Frame = ...; // ไม่สามารถเป็นค่า undefined ได้

IShapeCollection shapes = ...;
// พารามิเตอร์ x, y, width, height ไม่สามารถเป็น float.NaN ได้:
{
    shapes.AddAudioFrameCD(...);
    shapes.AddAudioFrameEmbedded(...);
    shapes.AddAudioFrameLinked(...);
    shapes.AddAutoShape(...);
    shapes.AddChart(...);
    shapes.AddConnector(...);
    shapes.AddOleObjectFrame(...);
    shapes.AddPictureFrame(...);
    shapes.AddSmartArt(...);
    shapes.AddTable(...);
    shapes.AddVideoFrame(...);
    shapes.InsertAudioFrameEmbedded(...);
    shapes.InsertAudioFrameLinked(...);
    shapes.InsertAutoShape(...);
    shapes.InsertChart(...);
    shapes.InsertConnector(...);
    shapes.InsertOleObjectFrame(...);
    shapes.InsertPictureFrame(...);
    shapes.InsertTable(...);
    shapes.InsertVideoFrame(...);
}
```

แต่คุณสมบัติกระดานของ IShape.RawFrame สามารถไม่มีการกำหนดค่าได้ ซึ่งมีเหตุผลเมื่อ shape ถูกเชื่อมโยงกับ placeholder จากนั้นค่ากระดานของ shape ที่ไม่ได้กำหนดจะถูกแทนที่โดยค่าจาก placeholder shape ของพาเรนต์ หากไม่มี placeholder shape ของพาเรนต์สำหรับ shape นั้น shape จะใช้ค่าตั้งต้นเมื่อตีความกรอบที่มีประสิทธิภาพตาม IShape.RawFrame ของมันเอง ค่าเริ่มต้นคือ 0 และ NullableBool.False สำหรับ x, y, width, height, flipH, flipV และ rotationAngle ตัวอย่างเช่น:

```csharp
IShape shape = ...; // shape เชื่อมโยงกับ placeholder
shape.RawFrame = new ShapeFrame(float.NaN, float.NaN, 100, float.NaN, NullableBool.NotDefined, NullableBool.NotDefined, 0); // ตอนนี้ shape สืบทอดค่า x, y, height, flipH, flipV จาก placeholder และกำหนดค่า width=100 และ rotationAngle=0.
```

### ดูเพิ่มเติม

* อินเทอร์เฟซ [IShapeFrame](../../ishapeframe)
* อินเทอร์เฟซ [IShape](../../ishape)
* ชื่อเนมสเปซ [Aspose.Slides](../../ishape)
* แอสเซมบลี [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->