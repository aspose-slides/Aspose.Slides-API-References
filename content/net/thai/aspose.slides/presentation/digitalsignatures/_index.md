---
title: DigitalSignatures
second_title: Aspose.Sildes สำหรับ .NET API Reference
description: ส่งคืนคอลเลกชันของลายเซ็นที่ใช้ในการเซ็นพรีเซนเทชัน. อ่านอย่างเดียว IDigitalSignatureCollectionaspose.slides/idigitalsignaturecollection.
type: docs
weight: 80
url: /th/aspose.slides/presentation/digitalsignatures/
---
## Presentation.DigitalSignatures คุณสมบัติ

ส่งคืนคอลเลกชันของลายเซ็นที่ใช้ในการเซ็นพรีเซนเทชัน. อ่านอย่างเดียว [`IDigitalSignatureCollection`](../../idigitalsignaturecollection).

```csharp
public IDigitalSignatureCollection DigitalSignatures { get; }
```

### ตัวอย่าง

```csharp
[C#]
using (Presentation pres = new Presentation("SomePresentationSigned.pptx"))
{
    if (pres.DigitalSignatures.Count > 0)
    {
        bool allSignaturesAreValid = true;

        Console.WriteLine("Signatures used to sign the presentation: ");
        foreach (DigitalSignature signature in pres.DigitalSignatures)
        {
            Console.WriteLine(signature.Certificate.SubjectName.Name + ", "
                    + signature.SignTime.ToString("yyyy-MM-dd HH:mm") + " -- " + (signature.IsValid ? "VALID" : "INVALID"));
            allSignaturesAreValid &= signature.IsValid;
        }

        if (allSignaturesAreValid)
            Console.WriteLine("Presentation is genuine, all signatures are valid.");
        else
            Console.WriteLine("Presentation has been modified since signing.");
    }
}
```

### ดูเพิ่มเติม

* อินเทอร์เฟซ [IDigitalSignatureCollection](../../idigitalsignaturecollection)
* คลาส [Presentation](../../presentation)
* เนมส페ซ [Aspose.Slides](../../presentation)
* แอสเซมบลี [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->