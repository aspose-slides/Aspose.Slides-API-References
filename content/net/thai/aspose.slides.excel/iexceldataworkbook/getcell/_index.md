---
title: GetCell
second_title: เอกสารอ้างอิง API ของ Aspose.Sildes สำหรับ .NET
description: ดึงเซลล์จากเวิร์กชีตที่ระบุโดยใช้ดัชนีและพิกัดของเซลล์.
type: docs
weight: 10
url: /th/aspose.slides.excel/iexceldataworkbook/getcell/
---
## GetCell(int, int, int) {#getcell}

ดึงเซลล์จากเวิร์กชีตที่ระบุโดยใช้ดัชนีและพิกัดของเซลล์

```csharp
public IExcelDataCell GetCell(int worksheetIndex, int row, int column)
```

| Parameter | Type | Description |
| --- | --- | --- |
| worksheetIndex | Int32 | ดัชนีของเวิร์กชีตที่เริ่มจากศูนย์ |
| row | Int32 | ดัชนีแถวของเซลล์ที่เริ่มจากศูนย์ |
| column | Int32 | ดัชนีคอลัมน์ของเซลล์ที่เริ่มจากศูนย์ |

### ค่าที่ส่งกลับ

เซลล์ที่ตำแหน่งที่ระบุ

### ตัวอย่าง

ตัวอย่าง:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell(1, 1, 1);
Console.WriteLine(cell.Value.ToString());
```

### ดูเพิ่มเติม

* อินเทอร์เฟซ [IExcelDataCell](../../iexceldatacell)
* อินเทอร์เฟซ [IExcelDataWorkbook](../../iexceldataworkbook)
* เนมสเปซ [Aspose.Slides.Excel](../../iexceldataworkbook)
* แอสเซมบลี [Aspose.Slides](../../../)

---

## GetCell(string, int, int) {#getcell_2}

ดึงเซลล์จากเวิร์กชีตที่ระบุโดยใช้ชื่อและพิกัดของเซลล์

```csharp
public IExcelDataCell GetCell(string worksheetName, int row, int column)
```

| Parameter | Type | Description |
| --- | --- | --- |
| worksheetName | String | ชื่อของเวิร์กชีต |
| row | Int32 | ดัชนีแถวของเซลล์ที่เริ่มจากศูนย์ |
| column | Int32 | ดัชนีคอลัมน์ของเซลล์ที่เริ่มจากศูนย์ |

### ค่าที่ส่งกลับ

เซลล์ที่ตำแหน่งที่ระบุ

### ตัวอย่าง

ตัวอย่าง:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell("Sheet1", 1, 1);
Console.WriteLine(cell.Value.ToString());
```

### ดูเพิ่มเติม

* อินเทอร์เฟซ [IExcelDataCell](../../iexceldatacell)
* อินเทอร์เฟซ [IExcelDataWorkbook](../../iexceldataworkbook)
* เนมสเปซ [Aspose.Slides.Excel](../../iexceldataworkbook)
* แอสเซมบลี [Aspose.Slides](../../../)

---

## GetCell(int, string) {#getcell_1}

ดึงเซลล์จากเวิร์กชีตที่ระบุโดยใช้ดัชนีและชื่อเซลล์แบบ Excel (เช่น "B2")

```csharp
public IExcelDataCell GetCell(int worksheetIndex, string cellName)
```

| Parameter | Type | Description |
| --- | --- | --- |
| worksheetIndex | Int32 | ดัชนีของเวิร์กชีตที่เริ่มจากศูนย์ |
| cellName | String | อ้างอิงเซลล์แบบ Excel (เช่น "A1", "C5") |

### ค่าที่ส่งกลับ

เซลล์ที่ตำแหน่งที่ระบุ

### ตัวอย่าง

ตัวอย่าง:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell(1, "B2");
Console.WriteLine(cell.Value.ToString());
```

### ดูเพิ่มเติม

* อินเทอร์เฟซ [IExcelDataCell](../../iexceldatacell)
* อินเทอร์เฟซ [IExcelDataWorkbook](../../iexceldataworkbook)
* เนมสเปซ [Aspose.Slides.Excel](../../iexceldataworkbook)
* แอสเซมบลี [Aspose.Slides](../../../)

---

## GetCell(string, string) {#getcell_3}

ดึงเซลล์จากเวิร์กชีตที่ระบุโดยใช้ชื่อเซลล์แบบ Excel (เช่น "B2")

```csharp
public IExcelDataCell GetCell(string worksheetName, string cellName)
```

| Parameter | Type | Description |
| --- | --- | --- |
| worksheetName | String | ชื่อของเวิร์กชีต |
| cellName | String | อ้างอิงเซลล์แบบ Excel (เช่น "A1", "C5") |

### ค่าที่ส่งกลับ

เซลล์ที่ตำแหน่งที่ระบุ

### ตัวอย่าง

ตัวอย่าง:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell("Sheet1", "B2");
Console.WriteLine(cell.Value.ToString());
```

### ดูเพิ่มเติม

* อินเทอร์เฟซ [IExcelDataCell](../../iexceldatacell)
* อินเทอร์เฟซ [IExcelDataWorkbook](../../iexceldataworkbook)
* เนมสเปซ [Aspose.Slides.Excel](../../iexceldataworkbook)
* แอสเซมบลี [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->