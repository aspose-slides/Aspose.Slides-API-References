---
title: GetOrCreateDataPointByIdx
second_title: Aspose.Sildes สำหรับ .NET API Reference
description: หากคอลเลกชันมีจุดข้อมูลที่มีดัชนี index อยู่แล้วจะคืนค่าจุดข้อมูลนั้น หากคอลเลกชันไม่มีจุดข้อมูลที่ดัชนี indexN เมื่อจำนวนจุดข้อมูลในคอลเลกชันนี้น้อยกว่าหรือเท่ากับ N จะเพิ่มจุดข้อมูลที่ขาดและคืนค่าจุดข้อมูลสุดท้ายที่มีดัชนีที่ร้องขอ ตัวอย่างเช่น ดัชนีของคอลเลกชันคือ 0 1 2 และดัชนีที่ร้องขอคือ 5 จากนั้นเมธอดจะเพิ่มจุดข้อมูลที่ขาด 0 1 2 3 4 5 และคืนค่าจุดข้อมูลที่ดัชนี 5.
type: docs
weight: 300
url: /th/aspose.slides.charts/chartdatapointcollection/getorcreatedatapointbyidx/
---
## ChartDataPointCollection.GetOrCreateDataPointByIdx เมธอด

หากคอลเลกชันมีจุดข้อมูลที่มีดัชนี *index* อยู่แล้ว จะคืนค่าจุดข้อมูลนั้น หากคอลเลกชันไม่มีจุดข้อมูลที่ดัชนี *index*==N (เมื่อจำนวนจุดข้อมูลในคอลเลกชันน้อยกว่าหรือเท่ากับ N) จะเพิ่มจุดข้อมูลที่ขาดและคืนค่าจุดข้อมูลสุดท้าย (ซึ่งมีดัชนีที่ร้องขอ) ตัวอย่างเช่น ดัชนีของคอลเลกชันคือ {0, 1, 2} และดัชนีที่ร้องขอคือ 5 จากนั้นเมธอดจะเพิ่มจุดข้อมูลที่ขาด: {0, 1, 2, 3, 4, 5} และคืนค่าจุดข้อมูลที่ดัชนี 5.

```csharp
public IChartDataPoint GetOrCreateDataPointByIdx(uint index)
```

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| index | UInt32 | ดัชนี. |

### ค่าที่ส่งคืน

คืนค่าจุดข้อมูลที่ดัชนีที่ร้องขอ

### ดูเพิ่มเติม

* อินเทอร์เฟซ [IChartDataPoint](../../ichartdatapoint)
* คลาส [ChartDataPointCollection](../../chartdatapointcollection)
* เนมสเปซ [Aspose.Slides.Charts](../../chartdatapointcollection)
* แอสเซ็มบลี [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->