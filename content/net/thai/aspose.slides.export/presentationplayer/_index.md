---
title: PresentationPlayer
second_title: Aspose.Sildes สำหรับ .NET อ้างอิง API
description: แสดงถึงผู้เล่นของแอนิเมชันที่เกี่ยวข้องกับ Presentation../aspose.slides/presentation.
type: docs
weight: 4390
url: /th/aspose.slides.export/presentationplayer/
---
## PresentationPlayer คลาส

แสดงถึงผู้เล่นของแอนิเมชันที่เกี่ยวข้องกับ [`Presentation`](../../aspose.slides/presentation).

```csharp
public class PresentationPlayer : IDisposable
```

## คอนสตรัคเตอร์

| ชื่อ | คำอธิบาย |
| --- | --- |
| [PresentationPlayer](presentationplayer)(PresentationAnimationsGenerator, double) | สร้างอินสแตนซ์ใหม่ของ [`PresentationPlayer`](../presentationplayer). |

## คุณสมบัติ

| ชื่อ | คำอธิบาย |
| --- | --- |
| [FrameIndex](../../aspose.slides.export/presentationplayer/frameindex) { get; } | รับค่าดัชนีเฟรม. |

## วิธีการ

| ชื่อ | คำอธิบาย |
| --- | --- |
| [Dispose](../../aspose.slides.export/presentationplayer/dispose)() | ทำลายอินสแตนซ์ของ [`PresentationPlayer`](../presentationplayer). |

## สมาชิกอื่น

| ชื่อ | คำอธิบาย |
| --- | --- |
| delegate [FrameTickHandler](presentationplayer.frametickhandler) | แสดงถึงตัวจัดการการตีกรอบของเหตุการณ์ [`FrameTick`](./frametick). |

### ตัวอย่าง

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(pres))
    {
        // เล่นแอนิเมชันที่ 33 FPS
        using (var player = new PresentationPlayer(animationsGenerator, 33))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("33fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
        
        // เล่นแอนิเมชันที่ 45 FPS
        using (var player = new PresentationPlayer(animationsGenerator, 45))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("45fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
    }
}
```

### ดูเพิ่มเติม

* เนมสเปซ [Aspose.Slides.Export](../../aspose.slides.export)
* แอสเซมบลี [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->