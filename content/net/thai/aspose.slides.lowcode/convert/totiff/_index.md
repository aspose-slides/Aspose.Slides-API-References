---
title: ToTiff
second_title: Aspose.Sildes สำหรับอ้างอิง API ของ .NET
description: แปลงการนำเสนอที่ได้รับเป็นชุดของภาพในรูปแบบ TIFF หากชื่อไฟล์ผลลัพธ์ระบุเป็น myPath/myFilename.tiff ผลลัพธ์จะถูกบันทึกเป็นชุดของไฟล์ myPath/myFilename_N.tiff ซึ่ง N คือหมายเลขสไลด์
type: docs
weight: 60
url: /th/aspose.slides.lowcode/convert/totiff/
---
## ToTiff(Presentation, string) {#totiff}

แปลงการนำเสนอที่ได้รับเป็นชุดของภาพในรูปแบบ TIFF หากชื่อไฟล์ผลลัพธ์ระบุเป็น "myPath/myFilename.tiff" ผลลัพธ์จะถูกบันทึกเป็นชุดของไฟล์ "myPath/myFilename_N.tiff" ซึ่ง N คือหมายเลขสไลด์

```csharp
public static void ToTiff(Presentation pres, string outputFileName)
```

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| pres | Presentation | การนำเสนอที่เป็นอินพุต |
| outputFileName | String | ชื่อไฟล์ผลลัพธ์ |

### ข้อยกเว้น

| ข้อยกเว้น | เงื่อนไข |
| --- | --- |
| ArgumentException |  |

### ตัวอย่าง

```csharp
using (var pres = new Presentation("pres.pptx"))
   Convert.ToTiff(pres, "presImage.tiff");
```

### ดูเพิ่มเติม

* คลาส [Presentation](../../../aspose.slides/presentation)
* คลาส [Convert](../../convert)
* เนมสเปซ [Aspose.Slides.LowCode](../../convert)
* แอสเซมบลี [Aspose.Slides](../../../)

---

## ToTiff(Presentation, string, ITiffOptions, bool) {#totiff_1}

แปลงการนำเสนอที่ได้รับเป็นรูปแบบ TIFF พร้อมตัวเลือกที่กำหนดเอง หากชื่อไฟล์ผลลัพธ์ระบุเป็น "myPath/myFilename.tiff" และ *multipage* เป็น `false` ผลลัพธ์จะถูกบันทึกเป็นชุดของไฟล์ "myPath/myFilename_N.tiff" ซึ่ง N คือหมายเลขสไลด์ มิฉะนั้น หาก *multipage* เป็น `true` ผลลัพธ์จะเป็นเอกสารหลายหน้า "myPath/myFilename.tiff"

```csharp
public static void ToTiff(Presentation pres, string outputFileName, ITiffOptions options, 
    bool multipage)
```

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| pres | Presentation | การนำเสนอที่เป็นอินพุต |
| outputFileName | String | ชื่อไฟล์ผลลัพธ์ |
| options | ITiffOptions | ตัวเลือกการบันทึก TIFF |
| multipage | Boolean | ระบุว่าต้องการให้เอกสาร TIFF เป็นหลายหน้าหรือไม่ |

### ข้อยกเว้น

| ข้อยกเว้น | เงื่อนไข |
| --- | --- |
| ArgumentException |  |

### ตัวอย่าง

```csharp
ITiffOptions options = new TiffOptions()
{
    CompressionType = TiffCompressionTypes.CCITT3,
    SlidesLayoutOptions = new NotesCommentsLayoutingOptions()
    {
        NotesPosition = NotesPositions.BottomTruncated
    }
};

using (var pres = new Presentation("pres.pptx"))
   Convert.ToTiff(pres, "pres.tiff", options, false);
```

### ดูเพิ่มเติม

* คลาส [Presentation](../../../aspose.slides/presentation)
* อินเทอร์เฟซ [ITiffOptions](../../../aspose.slides.export/itiffoptions)
* คลาส [Convert](../../convert)
* เนมสเปซ [Aspose.Slides.LowCode](../../convert)
* แอสเซมบลี [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->