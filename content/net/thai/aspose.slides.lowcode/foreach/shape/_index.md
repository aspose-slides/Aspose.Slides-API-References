---
title: Shape
second_title: Aspose.Sildes สำหรับ .NET API Reference
description: วนซ้ำแต่ละ Shapeaspose.slides.lowcode/foreach/shape ใน Presentationaspose.slides/presentation. รูปร่างจะถูกวนซ้ำในทุกประเภทของสไลด์ - Slideaspose.slides.lowcode/foreach/slide MasterSlideaspose.slides.lowcode/foreach/masterslide และ LayoutSlideaspose.slides.lowcode/foreach/layoutslide
type: docs
weight: 50
url: /th/aspose.slides.lowcode/foreach/shape/
---
## Shape(Presentation, ForEachShapeCallback) {#shape_2}

วนซ้ำแต่ละ `Shape` ใน [`Presentation`](../../../aspose.slides/presentation). รูปร่างจะถูกวนซ้ำในทุกประเภทของสไลด์ - [`Slide`](../slide), [`MasterSlide`](../masterslide) และ [`LayoutSlide`](../layoutslide)

```csharp
public static void Shape(Presentation pres, ForEachShapeCallback forEachShape)
```

| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| pres | Presentation | Presentation ที่ใช้ในการวนซ้ำรูปแบบเลย์เอาต์ |
| forEachShape | ForEachShapeCallback | Callback ที่จะถูกเรียกใช้สำหรับแต่ละ shape |

### ตัวอย่าง

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Shape(pres, (shape, slide, index) => 
    {
        System.Console.WriteLine($"{shape.Name}, index: {index}");
    });
} 
```

### ดูเพิ่มเติม

* คลาส [Presentation](../../../aspose.slides/presentation)
* delegate [ForEachShapeCallback](../../foreach.foreachshapecallback)
* คลาส [ForEach](../../foreach)
* เนมสเปซ [Aspose.Slides.LowCode](../../foreach)
* แอสเซมบลี [Aspose.Slides](../../../)

---

## Shape(Presentation, bool, ForEachShapeCallback) {#shape_1}

วนซ้ำแต่ละ [`Shape`](../shape) ใน [`Presentation`](../../../aspose.slides/presentation). รูปร่างจะถูกวนซ้ำในทุกประเภทของสไลด์ - [`Slide`](../slide), [`MasterSlide`](../masterslide), [`LayoutSlide`](../layoutslide) และ [`NotesSlide`](../../../aspose.slides/notesslide) หากจำเป็น

```csharp
public static void Shape(Presentation pres, bool includeNotes, ForEachShapeCallback forEachShape)
```

| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| pres | Presentation | Presentation ที่ใช้ในการวนซ้ำรูปแบบเลย์เอาต์ |
| includeNotes | Boolean | Flag ที่บ่งบอกว่า NotesSlides ควรจะรวมอยู่ในการประมวลผลหรือไม่ |
| forEachShape | ForEachShapeCallback | Callback ที่จะถูกเรียกใช้สำหรับแต่ละ shape |

### ตัวอย่าง

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Shape(pres, true, (shape, slide, index) => 
    {
        System.Console.WriteLine($"{shape.Name}, index: {index}");
    });
} 
```

### ดูเพิ่มเติม

* คลาส [Presentation](../../../aspose.slides/presentation)
* delegate [ForEachShapeCallback](../../foreach.foreachshapecallback)
* คลาส [ForEach](../../foreach)
* เนมสเปซ [Aspose.Slides.LowCode](../../foreach)
* แอสเซมบลี [Aspose.Slides](../../../)

---

## Shape(BaseSlide, ForEachShapeCallback) {#shape}

วนซ้ำแต่ละ [`Shape`](../shape) ใน [`BaseSlide`](../../../aspose.slides/baseslide). [`BaseSlide`](../../../aspose.slides/baseslide) เป็นชนิดฐานสำหรับ [`Slide`](../slide), [`MasterSlide`](../masterslide) และ [`LayoutSlide`](../layoutslide)

```csharp
public static void Shape(BaseSlide baseSlide, ForEachShapeCallback forEachShape)
```

| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| baseSlide | BaseSlide | Slide ที่ใช้ในการวนซ้ำรูปแบบเลย์เอาต์ |
| forEachShape | ForEachShapeCallback | Callback ที่จะถูกเรียกใช้สำหรับแต่ละ shape |

### ตัวอย่าง

```csharp
(Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Slide(pres, (slide, index) =>
    {
        ForEach.Shape(slide, (shape, baseSlide, shapeIndex) =>
        {
            System.Console.WriteLine($"{shape.Name}, index: {shapeIndex}");
        });
    });
} 
```

### ดูเพิ่มเติม

* คลาส [BaseSlide](../../../aspose.slides/baseslide)
* delegate [ForEachShapeCallback](../../foreach.foreachshapecallback)
* คลาส [ForEach](../../foreach)
* เนมสเปซ [Aspose.Slides.LowCode](../../foreach)
* แอสเซมบลี [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->