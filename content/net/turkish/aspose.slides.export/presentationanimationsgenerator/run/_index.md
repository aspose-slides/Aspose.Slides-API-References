---
title: Run
second_title: Aspose.Sildes .NET API Referansı
description: Her slayt için animasyon olaylarının oluşturulmasını çalıştırır.
type: docs
weight: 80
url: /tr/aspose.slides.export/presentationanimationsgenerator/run/
---
## Run(IEnumerable&lt;ISlide&gt;) {#run}

Her slayt için animasyon olaylarının oluşturulmasını çalıştırır.

```csharp
public void Run(IEnumerable<ISlide> slides)
```

### Örnekler

```csharp
[C#]
using (Presentation presentation = new Presentation("animated.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(presentation.SlideSize.Size.ToSize()))
    using (var player = new PresentationPlayer(animationsGenerator, 33))
    {
        animationsGenerator.NewAnimation += animationPlayer =>
        {
            // yeni animasyonu işle
        };
        
        player.FrameTick += (sender, args) =>
        {
            // yeni animasyon içinde frame tick'i işle
        };
        
        animationsGenerator.Run(presentation.Slides);
    }
}
```

### Diğer Bağlantılar

* arayüz [ISlide](../../../aspose.slides/islide)
* sınıf [PresentationAnimationsGenerator](../../presentationanimationsgenerator)
* ad alanı [Aspose.Slides.Export](../../presentationanimationsgenerator)
* derleme [Aspose.Slides](../../../)

---

## Run(IEnumerable&lt;ISlide&gt;, int, FrameTickHandler) {#run_1}

Her slayt için animasyon olaylarının oluşturulmasını çalıştırır.

```csharp
public void Run(IEnumerable<ISlide> slides, int fps, FrameTickHandler onFrame)
```

### Örnekler

```csharp
[C#]
using (Presentation presentation = new Presentation("animated.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(presentation.SlideSize.Size.ToSize()))
    {
        animationsGenerator.Run(presentation.Slides, 33, (sender, args) =>
        {
            sender.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save($"frame_{sender.FrameIndex}.png");
            };
        });
    }
}
```

### Diğer Bağlantılar

* arayüz [ISlide](../../../aspose.slides/islide)
* delege [FrameTickHandler](../../presentationplayer.frametickhandler)
* sınıf [PresentationAnimationsGenerator](../../presentationanimationsgenerator)
* ad alanı [Aspose.Slides.Export](../../presentationanimationsgenerator)
* derleme [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->