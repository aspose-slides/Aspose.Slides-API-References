---
title: ToTiff
second_title: Aspose.Sildes for .NET API Referansı
description: Girdi sunumunu bir dizi TIFF formatı görüntüsüne dönüştürür. Çıktı dosya adı myPath/myFilename.tiff olarak verilirse, sonuç N bir slayt numarası olmak üzere myPath/myFilename_N.tiff dosyaları olarak kaydedilir.
type: docs
weight: 60
url: /tr/aspose.slides.lowcode/convert/totiff/
---
## ToTiff(Presentation, string) {#totiff}

Girdi sunumunu bir dizi TIFF formatı görüntüsüne dönüştürür. Çıktı dosya adı "myPath/myFilename.tiff" olarak verilirse, sonuç "myPath/myFilename_N.tiff" dosyaları şeklinde kaydedilir; burada N bir slayt numarasıdır.

```csharp
public static void ToTiff(Presentation pres, string outputFileName)
```

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| pres | Presentation | The input presentation. |
| outputFileName | String | The output file name. |

### İstisnalar

| istisna | koşul |
| --- | --- |
| ArgumentException |  |

### Örnekler

```csharp
using (var pres = new Presentation("pres.pptx"))
   Convert.ToTiff(pres, "presImage.tiff");
```

### İlgili

* sınıf [Presentation](../../../aspose.slides/presentation)
* sınıf [Convert](../../convert)
* ad alanı [Aspose.Slides.LowCode](../../convert)
* derleme [Aspose.Slides](../../../)

---

## ToTiff(Presentation, string, ITiffOptions, bool) {#totiff_1}

Girdi sunumunu özel seçeneklerle TIFF formatına dönüştürür. Çıktı dosya adı "myPath/myFilename.tiff" olarak verilirse ve *multipage* `false` ise, sonuç "myPath/myFilename_N.tiff" dosyaları şeklinde kaydedilir; burada N bir slayt numarasıdır. Aksi takdirde, *multipage* `true` olduğunda, sonuç çok sayfalı "myPath/myFilename.tiff" belgesi olur.

```csharp
public static void ToTiff(Presentation pres, string outputFileName, ITiffOptions options, 
    bool multipage)
```

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| pres | Presentation | The input presentation. |
| outputFileName | String | The output file name. |
| options | ITiffOptions | The TIFF saving options. |
| multipage | Boolean | Specifies whether the generated TIFF document should be a multi-page. |

### İstisnalar

| istisna | koşul |
| --- | --- |
| ArgumentException |  |

### Örnekler

```csharp
ITiffOptions options = new TiffOptions()
{
    CompressionType = TiffCompressionTypes.CCITT3,
    SlidesLayoutOptions = new NotesCommentsLayoutingOptions()
    {
        NotesPosition = NotesPositions.BottomTruncated
    }
};

using (var pres = new Presentation("pres.pptx"))
   Convert.ToTiff(pres, "pres.tiff", options, false);
```

### İlgili

* sınıf [Presentation](../../../aspose.slides/presentation)
* arayüz [ITiffOptions](../../../aspose.slides.export/itiffoptions)
* sınıf [Convert](../../convert)
* ad alanı [Aspose.Slides.LowCode](../../convert)
* derleme [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->