---
title: Placeholder
second_title: Aspose.Sildes for .NET API Referansı
description: Bir şekil için yer tutucuyu döndürür. Şeklin yer tutucusu yoksa null döndürür. Salt okunur IPlaceholderaspose.slides/iplaceholder.
type: docs
weight: 210
url: /tr/aspose.slides/shape/placeholder/
---
## Shape.Placeholder özelliği

Bir şekil için yer tutucuyu döndürür. Şeklin yer tutucusu yoksa null döndürür. Salt okunur [`IPlaceholder`](../../iplaceholder).

```csharp
public IPlaceholder Placeholder { get; }
```

### Örnekler

Aşağıdaki örnek, Yer Tutucudaki Metni nasıl değiştireceğinizi gösterir.

```csharp
[C#]
// Presentation sınıfının bir örneğini oluşturur
using (Presentation pres = new Presentation("ReplacingText.pptx"))
{
    // İlk slayta erişir
    ISlide sld = pres.Slides[0];
    // Yer tutucuyu bulmak için şekillerde döner
    foreach (IShape shp in sld.Shapes)
        if (shp.Placeholder != null)
        {
            // Her yer tutucunun metnini değiştirir
            ((IAutoShape)shp).TextFrame.Text = "This is a Placeholder";
        }
    // Sunumu diske kaydeder
    pres.Save("output_out.pptx", Aspose.Slides.Export.SaveFormat.Pptx);
}
```

Aşağıdaki örnek, Yer Tutucuda İstem Metnini nasıl ayarlayacağınızı gösterir.

```csharp
[C#]
using (Presentation pres = new Presentation("Presentation2.pptx"))
{
    ISlide slide = pres.Slides[0];
    foreach (IShape shape in slide.Slide.Shapes) // Slaytı dolaşır
    {
        if (shape.Placeholder != null && shape is AutoShape)
        {
            string text = "";
            if (shape.Placeholder.Type == PlaceholderType.CenteredTitle) // PowerPoint "Başlık eklemek için tıklayın" gösterir
            {
                text = "Add Title";
            }
            else if (shape.Placeholder.Type == PlaceholderType.Subtitle) // Altyazı ekler
            {
                text = "Add Subtitle";
            }
            ((IAutoShape)shape).TextFrame.Text = text;
            Console.WriteLine($"Placeholder with text: {text}");
        }
    }
    pres.Save("Placeholders_PromptText.pptx", SaveFormat.Pptx);
}
```

### Ayrıca Bakınız

* arayüz [IPlaceholder](../../iplaceholder)
* sınıf [Shape](../../shape)
* ad alanı [Aspose.Slides](../../shape)
* derleme [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->