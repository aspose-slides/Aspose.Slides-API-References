---
title: AddTextFrame
second_title: Aspose.Sildes için .NET API Referansı
description: Bir şekle yeni bir TextFrame ekler. Şekilde zaten bir TextFrame varsa sadece metnini değiştirir.
type: docs
weight: 60
url: /tr/aspose.slides/autoshape/addtextframe/
---
## AutoShape.AddTextFrame yöntemi

Bir şekle yeni bir TextFrame ekler. Şekilde zaten bir TextFrame varsa sadece metnini değiştirir.

```csharp
public ITextFrame AddTextFrame(string text)
```

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| text | String | Yeni bir TextFrame için varsayılan metin. |

### Örnekler

Aşağıdaki örnek kod, PowerPoint Sunumunda filigran metni nasıl ekleyeceğinizi gösterir.

```csharp
[C#]
	using (var presentation = new Presentation())
	{
		ISlide slide = presentation.Slides[0];
		IAutoShape watermarkShape = slide.Shapes.AddAutoShape(ShapeType.Triangle, 0, 0, 150, 50);
		ITextFrame watermarkTextFrame = watermarkShape.AddTextFrame("Watermark");
	}
```

Aşağıdaki örnek, Slayt üzerinde Text Box nasıl oluşturulacağını gösterir.

```csharp
[C#]
// Sunumu örnekler
using (Presentation pres = new Presentation())
{
    // Sunumdaki ilk slaytı alır
    ISlide sld = pres.Slides[0];
    // Dikdörtgen tipinde bir AutoShape ekler
    IAutoShape ashp = sld.Shapes.AddAutoShape(ShapeType.Rectangle, 150, 75, 150, 50);
    // Dikdörtgene TextFrame ekler
    ashp.AddTextFrame(" ");
    // TextFrame'e erişir
    ITextFrame txtFrame = ashp.TextFrame;
    // TextFrame için Paragraph nesnesi oluşturur
    IParagraph para = txtFrame.Paragraphs[0];
    // Paragraph için Portion nesnesi oluşturur
    IPortion portion = para.Portions[0];
    // Metni ayarlar
    portion.Text = "Aspose TextBox";
    // Sunumu diske kaydeder
    pres.Save("TextBox_out.pptx", Aspose.Slides.Export.SaveFormat.Pptx);
}
```

Aşağıdaki örnek, Text Box içine sütun nasıl ekleneceğini gösterir.

```csharp
[C#]
using (Presentation presentation = new Presentation())
{
	// Sunumdaki ilk slaytı alır
	ISlide slide = presentation.Slides[0];
	// Dikdörtgen tipinde bir AutoShape ekler
	IAutoShape aShape = slide.Shapes.AddAutoShape(ShapeType.Rectangle, 100, 100, 300, 300);
	// Dikdörtgene TextFrame ekler
	aShape.AddTextFrame("All these columns are limited to be within a single text container -- " +
	"you can add or delete text and the new or remaining text automatically adjusts " +
	"itself to flow within the container. You cannot have text flow from one container " +
	"to other though -- we told you PowerPoint's column options for text are limited!");
	// TextFrame'in metin biçimini alır
	ITextFrameFormat format = aShape.TextFrame.TextFrameFormat;
	// TextFrame içindeki sütun sayısını belirler
	format.ColumnCount = 3;
	// Sütunlar arasındaki boşluğu belirler
	format.ColumnSpacing = 10;
	// Sunumu kaydeder
	presentation.Save("ColumnCount.pptx", SaveFormat.Pptx);
}
```

### Ayrıca Bakınız

* arayüz [ITextFrame](../../itextframe)
* sınıf [AutoShape](../../autoshape)
* ad alanı [Aspose.Slides](../../autoshape)
* derleme [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->