---
title: AddConnector
second_title: Aspose.Sildes .NET için API Referansı
description: Yeni bir bağlayıcı şekli oluşturur, varsayılan şablon stilini uygular ve şekil koleksiyonunun sonuna ekler.
type: docs
weight: 120
url: /tr/aspose.slides/shapecollection/addconnector/
---
## AddConnector(ShapeType, float, float, float, float) {#addconnector}

Yeni bir bağlayıcı şekli oluşturur, varsayılan şablon stilini uygular ve şekil koleksiyonunun sonuna ekler.

```csharp
public IConnector AddConnector(ShapeType shapeType, float x, float y, float width, float height)
```

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| shapeType | ShapeType | Eklenecek bağlayıcı şeklin [`ShapeType`](../../shapetype). |
| x | Single | Bağlayıcının çerçevesinin x koordinatı, nokta cinsinden. |
| y | Single | Bağlayıcının çerçevesinin y koordinatı, nokta cinsinden. |
| width | Single | Bağlayıcının çerçevesinin genişliği, nokta cinsinden. |
| height | Single | Bağlayıcının çerçevesinin yüksekliği, nokta cinsinden. |

### Dönüş Değeri

Yeni oluşturulan [`IConnector`](../../iconnector).

### Örnekler

Aşağıdaki örnek, PowerPoint sunumunda iki şekil (bir elips ve bir dikdörtgen) arasında bir bağlayıcı (bükülmüş bağlayıcı) eklemenin nasıl yapılacağını gösterir.

```csharp
[C#]
// PPTX dosyasını temsil eden bir sunum sınıfını örnekler
using (Presentation input = new Presentation())
{
    // Belirli bir slayt için şekil koleksiyonuna erişir
    IShapeCollection shapes = input.Slides[0].Shapes;
    // Bir Elips otomatik şekli ekler
    IAutoShape ellipse = shapes.AddAutoShape(ShapeType.Ellipse, 0, 100, 100, 100);
    // Bir Dikdörtgen otomatik şekli ekler
    IAutoShape rectangle = shapes.AddAutoShape(ShapeType.Rectangle, 100, 300, 100, 100);
    // Bağlayıcı şekli slayt şekil koleksiyonuna ekler
    IConnector connector = shapes.AddConnector(ShapeType.BentConnector2, 0, 0, 10, 10);
    // Şekilleri bağlayıcı aracılığıyla bağlar
    connector.StartShapeConnectedTo = ellipse;
    connector.EndShapeConnectedTo = rectangle;
    // Şekiller arasındaki otomatik en kısa yolu ayarlayan reroute metodunu çağırır
    connector.Reroute();
    // Sunumu kaydeder
    input.Save("Shapes-connector.pptx", SaveFormat.Pptx);
}
```

### Ayrıca Bakınız

* arayüz [IConnector](../../iconnector)
* enum [ShapeType](../../shapetype)
* sınıf [ShapeCollection](../../shapecollection)
* ad alanı [Aspose.Slides](../../shapecollection)
* derleme [Aspose.Slides](../../../)

---

## AddConnector(ShapeType, float, float, float, float, bool) {#addconnector_1}

Yeni bir bağlayıcı şekli oluşturur ve şekil koleksiyonunun sonuna ekler; isteğe bağlı olarak varsayılan şablon stilini uygular.

```csharp
public IConnector AddConnector(ShapeType shapeType, float x, float y, float width, float height, 
    bool createFromTemplate)
```

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| shapeType | ShapeType | Oluşturulacak bağlayıcı şeklin [`ShapeType`](../../shapetype). |
| x | Single | Bağlayıcının çerçevesinin x koordinatı, nokta cinsinden. |
| y | Single | Bağlayıcının çerçevesinin y koordinatı, nokta cinsinden. |
| width | Single | Bağlayıcının çerçevesinin genişliği, nokta cinsinden. |
| height | Single | Bağlayıcının çerçevesinin yüksekliği, nokta cinsinden. |
| createFromTemplate | Boolean | Varsayılan şablon stilini (boş olmayan ad, basit stil) uygulamak için true; bağlayıcıyı varsayılan özellik değerleriyle oluşturmak için false. |

### Dönüş Değeri

Yeni oluşturulan [`IConnector`](../../iconnector).

### Ayrıca Bakınız

* arayüz [IConnector](../../iconnector)
* enum [ShapeType](../../shapetype)
* sınıf [ShapeCollection](../../shapecollection)
* ad alanı [Aspose.Slides](../../shapecollection)
* derleme [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->