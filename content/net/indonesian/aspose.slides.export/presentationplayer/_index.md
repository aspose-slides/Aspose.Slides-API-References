---
title: PresentationPlayer
second_title: Aspose.Sildes untuk Referensi API .NET
description: Mewakili pemutar animasi yang terkait dengan Presentation../aspose.slides/presentation.
type: docs
weight: 4370
url: /id/aspose.slides.export/presentationplayer/
---
## PresentationPlayer kelas

Mewakili pemutar animasi yang terkait dengan [`Presentation`](../../aspose.slides/presentation).

```csharp
public class PresentationPlayer : IDisposable
```

## Konstruktor

| Nama | Deskripsi |
| --- | --- |
| [PresentationPlayer](presentationplayer)(PresentationAnimationsGenerator, double) | Membuat instance baru dari [`PresentationPlayer`](../presentationplayer). |

## Properti

| Nama | Deskripsi |
| --- | --- |
| [FrameIndex](../../aspose.slides.export/presentationplayer/frameindex) { get; } | Mendapatkan indeks frame. |

## Metode

| Nama | Deskripsi |
| --- | --- |
| [Dispose](../../aspose.slides.export/presentationplayer/dispose)() | Membuang instance dari [`PresentationPlayer`](../presentationplayer). |

## Anggota Lain

| Nama | Deskripsi |
| --- | --- |
| delegate [FrameTickHandler](presentationplayer.frametickhandler) | Mewakili penangan frame tick dari peristiwa [`FrameTick`](./frametick). |

### Contoh

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(pres))
    {
        // Putar animasi dengan 33 FPS
        using (var player = new PresentationPlayer(animationsGenerator, 33))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("33fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
        
        // Putar animasi dengan 45 FPS
        using (var player = new PresentationPlayer(animationsGenerator, 45))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("45fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
    }
}
```

### Lihat Juga

* ruang nama [Aspose.Slides.Export](../../aspose.slides.export)
* assembly [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->