---
title: SpellCheck
second_title: Referensi API Aspose.Sildes untuk .NET
description: Mendapatkan atau mengatur nilai yang menunjukkan apakah pemeriksaan ejaan diaktifkan untuk bagian teks. Ketika properti ini diatur ke false, pemeriksaan ejaan untuk elemen teks dihentikan. Ketika diatur ke true, pemeriksaan ejaan diizinkan. Nilai default adalah false.
type: docs
weight: 220
url: /id/aspose.slides/baseportionformat/spellcheck/
---
## BasePortionFormat.SpellCheck properti

Mendapatkan atau mengatur nilai yang menunjukkan apakah pemeriksaan ejaan diaktifkan untuk bagian teks. Ketika properti ini diatur ke false, pemeriksaan ejaan untuk elemen teks dihentikan. Ketika diatur ke true, pemeriksaan ejaan diizinkan. Nilai default adalah `false`.

```csharp
public bool SpellCheck { get; set; }
```

### Contoh

Contoh berikut menunjukkan cara mengaktifkan flag SpellCheck sebelum menyimpan presentasi:

```csharp
[C#]
using (var pres = new Presentation("input.pptx"))
{
    // Akses bagian teks pertama di dalam bentuk pertama pada slide pertama
    var portion = ((AutoShape)pres.Slides[0].Shapes[0]).TextFrame.Paragraphs[0].Portions[0];
    // Aktifkan pemeriksaan ejaan untuk bagian teks ini
    portion.PortionFormat.SpellCheck = true;
    // Simpan presentasi yang telah dimodifikasi
    pres.Save("output-with-spellcheck.pptx", SaveFormat.Pptx);
}
```

### Lihat Juga

* kelas [BasePortionFormat](../../baseportionformat)
* ruang nama [Aspose.Slides](../../baseportionformat)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->