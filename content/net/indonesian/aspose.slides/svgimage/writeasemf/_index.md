---
title: WriteAsEmf
second_title: Referensi API Aspose.Sildes untuk .NET
description: Menyimpan gambar SVG sebagai file EMF.
type: docs
weight: 60
url: /id/aspose.slides/svgimage/writeasemf/
---
## SvgImage.WriteAsEmf metode

Menyimpan gambar SVG sebagai file EMF.

```csharp
public void WriteAsEmf(Stream stream)
```

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| stream | Stream | Target stream |

### Pengecualian

| pengecualian | kondisi |
| --- | --- |
| ArgumentNullException | Target stream is **null** |

### Contoh

Contoh berikut menunjukkan cara menyimpan gambar SVG ke metafile.

```csharp
[C#]
// Membuat gambar SVG baru
ISvgImage svgImage = new SvgImage(System.IO.File.ReadAllText("content.svg"));

// Menyimpan gambar SVG sebagai metafile
using (var fileStream = System.IO.File.OpenWrite("SvgAsEmf.emf"))
    svgImage.WriteAsEmf(fileStream);
```

Contoh ini mendemonstrasikan cara menambahkan gambar SVG sebagai metafile ke koleksi gambar presentasi.

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    // Membuat gambar SVG baru
    ISvgImage svgImage = new SvgImage(System.IO.File.ReadAllText("content.svg"));
    using (var memStream = new MemoryStream())
    {
        // Menyimpan gambar SVG sebagai metafile
        svgImage.WriteAsEmf(memStream);
        // Menambahkan metafile ke koleksi gambar
        pres.Images.AddImage(memStream.ToArray());
    }
}
```

### Lihat Juga

* kelas [SvgImage](../../svgimage)
* ruang nama [Aspose.Slides](../../svgimage)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->