---
title: AddFromPdf
second_title: Referensi API Aspose.Sildes untuk .NET
description: Membuat slide dari dokumen PDF dan menambahkannya ke akhir koleksi.
type: docs
weight: 50
url: /id/aspose.slides/islidecollection/addfrompdf/
---
## AddFromPdf(string) {#addfrompdf_2}

Membuat slide dari dokumen PDF dan menambahkannya ke akhir koleksi.

```csharp
public ISlide[] AddFromPdf(string path)
```

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| path | String | Path ke dokumen PDF |

### Nilai Kembalian

Slide yang ditambahkan

### Contoh

Example:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    pres.Slides.AddFromPdf("document.pdf");
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Lihat Juga

* antarmuka [ISlide](../../islide)
* antarmuka [ISlideCollection](../../islidecollection)
* ruang nama [Aspose.Slides](../../islidecollection)
* rakitan [Aspose.Slides](../../../)

---

## AddFromPdf(string, PdfImportOptions) {#addfrompdf_3}

Membuat slide dari dokumen PDF dan menambahkannya ke akhir koleksi dengan mempertimbangkan opsi impor pdf.

```csharp
public ISlide[] AddFromPdf(string path, PdfImportOptions pdfImportOptions)
```

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| path | String | Path ke dokumen PDF |
| pdfImportOptions | PdfImportOptions | Opsi untuk impor pdf |

### Nilai Kembalian

Slide yang ditambahkan

### Contoh

Example:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    pres.Slides.AddFromPdf("document.pdf, new PdfImportOptions { DetectTables = true });
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Lihat Juga

* antarmuka [ISlide](../../islide)
* kelas [PdfImportOptions](../../../aspose.slides.import/pdfimportoptions)
* antarmuka [ISlideCollection](../../islidecollection)
* ruang nama [Aspose.Slides](../../islidecollection)
* rakitan [Aspose.Slides](../../../)

---

## AddFromPdf(Stream, PdfImportOptions) {#addfrompdf_1}

Membuat slide dari dokumen PDF dan menambahkannya ke akhir koleksi.

```csharp
public ISlide[] AddFromPdf(Stream pdfStream, PdfImportOptions pdfImportOptions)
```

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| pdfStream | Stream | Aliran yang akan digunakan sebagai sumber dokumen PDF |
| pdfImportOptions | PdfImportOptions | Opsi untuk impor pdf |

### Nilai Kembalian

Slide yang ditambahkan

### Contoh

Example:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    using (Stream stream = new FileStream("document.pdf", FileMode.Open, FileAccess.Read, FileShare.Read))
    {
        pres.Slides.AddFromPdf(stream, new PdfImportOptions { DetectTables = true });
    }
    
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Lihat Juga

* antarmuka [ISlide](../../islide)
* kelas [PdfImportOptions](../../../aspose.slides.import/pdfimportoptions)
* antarmuka [ISlideCollection](../../islidecollection)
* ruang nama [Aspose.Slides](../../islidecollection)
* rakitan [Aspose.Slides](../../../)

---

## AddFromPdf(Stream) {#addfrompdf}

Membuat slide dari dokumen PDF dan menambahkannya ke akhir koleksi.

```csharp
public ISlide[] AddFromPdf(Stream pdfStream)
```

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| pdfStream | Stream | Aliran yang akan digunakan sebagai sumber dokumen PDF |

### Nilai Kembalian

Slide yang ditambahkan

### Contoh

Example:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    using (Stream stream = new FileStream("document.pdf", FileMode.Open, FileAccess.Read, FileShare.Read))
    {
        pres.Slides.AddFromPdf(stream);
    }
    
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### Lihat Juga

* antarmuka [ISlide](../../islide)
* antarmuka [ISlideCollection](../../islidecollection)
* ruang nama [Aspose.Slides](../../islidecollection)
* rakitan [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->