---
title: CompressImage
second_title: Referensi API Aspose.Sildes untuk .NET
description: Mengompresi gambar dengan mengurangi ukurannya berdasarkan ukuran bentuk dan resolusi yang ditentukan. Secara opsional juga menghapus area yang dipotong.
type: docs
weight: 190
url: /id/aspose.slides/ipicturefillformat/compressimage/
---
## CompressImage(bool, PicturesCompression) {#compressimage}

Mengompresi gambar dengan mengurangi ukurannya berdasarkan ukuran bentuk dan resolusi yang ditentukan. Secara opsional, juga menghapus area yang dipotong.

```csharp
public bool CompressImage(bool deleteCroppedAreasOfImage, PicturesCompression resolution)
```

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| deleteCroppedAreasOfImage | Boolean | Jika true, metode ini akan menghapus area yang dipotong dari gambar, yang berpotensi mengurangi ukurannya lebih lanjut. |
| resolution | PicturesCompression | Resolusi target untuk kompresi, ditentukan sebagai nilai dari enum [`PicturesCompression`](../../../aspose.slides.export/picturescompression). |

### Nilai Kembalian

Boolean yang menunjukkan apakah gambar berhasil dikompresi. Mengembalikan `true` jika gambar diubah ukurannya atau dipotong, jika tidak `false`.

### Pengecualian

| pengecualian | kondisi |
| --- | --- |
| ArgumentException | Dilemparkan ketika resolusi bukan nilai yang valid. |

### Catatan

Metode ini mengubah ukuran dan resolusi gambar mirip dengan fitur PowerPoint "Picture Format -> Compress Pictures".

### Contoh

Contoh berikut menunjukkan cara menggunakan metode `CompressImage` untuk mengurangi ukuran gambar dalam presentasi dengan menetapkan resolusi target dan menghapus area yang dipotong:

```csharp
using (Presentation presentation = new Presentation("demo.pptx"))
{
    ISlide slide = presentation.Slides[0];
    IPictureFrame picFrame = slide.Shapes[0] as IPictureFrame;
    // Kompres gambar dengan resolusi target 150 DPI (resolusi Web) dan hapus area yang dipotong
    bool result = picFrame.PictureFormat.CompressImage(true, PicturesCompression.Dpi150);
}
```

### Lihat Juga

* enum [PicturesCompression](../../../aspose.slides.export/picturescompression)
* antarmuka [IPictureFillFormat](../../ipicturefillformat)
* ruang nama [Aspose.Slides](../../ipicturefillformat)
* assembly [Aspose.Slides](../../../)

---

## CompressImage(bool, float) {#compressimage_1}

Mengompresi gambar dengan mengurangi ukurannya berdasarkan ukuran bentuk dan resolusi yang ditentukan. Secara opsional, juga menghapus area yang dipotong.

```csharp
public bool CompressImage(bool deleteCroppedAreasOfImage, float resolution)
```

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| deleteCroppedAreasOfImage | Boolean | Jika true, metode ini akan menghapus area yang dipotong dari gambar, yang berpotensi mengurangi ukurannya lebih lanjut. |
| resolution | Single | Resolusi target dalam DPI. Nilai ini harus positif dan menentukan bagaimana gambar akan diubah ukurannya. |

### Nilai Kembalian

Boolean yang menunjukkan apakah gambar berhasil dikompresi. Mengembalikan `true` jika gambar diubah ukurannya atau dipotong, jika tidak `false`.

### Pengecualian

| pengecualian | kondisi |
| --- | --- |
| ArgumentException | Dilemparkan ketika resolusi bukan nilai positif. |

### Catatan

Metode ini mengubah ukuran dan resolusi gambar mirip dengan fitur PowerPoint "Picture Format -> Compress Pictures".

### Contoh

Contoh berikut menunjukkan cara menggunakan metode `CompressImage` untuk mengurangi ukuran gambar dalam presentasi dengan menetapkan resolusi target dan menghapus area yang dipotong:

```csharp
[C#]
using (Presentation presentation = new Presentation("demo.pptx"))
{
    ISlide slide = presentation.Slides[0];

    // Mendapatkan PictureFrame
    IPictureFrame picFrame = slide.Shapes[0] as IPictureFrame;

    // Kompres gambar dengan resolusi target 150 DPI (resolusi Web) dan hapus area yang dipotong
    bool result=picFrame.PictureFormat.CompressImage(true, 150f); // Resolusi Web
}
```

### Lihat Juga

* antarmuka [IPictureFillFormat](../../ipicturefillformat)
* ruang nama [Aspose.Slides](../../ipicturefillformat)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->