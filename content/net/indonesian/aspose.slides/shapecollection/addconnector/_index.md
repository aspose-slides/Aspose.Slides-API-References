---
title: AddConnector
second_title: Aspose.Sildes untuk Referensi API .NET
description: Membuat bentuk penghubung baru dengan gaya templat default dan menambahkannya ke akhir koleksi bentuk.
type: docs
weight: 120
url: /id/aspose.slides/shapecollection/addconnector/
---
## AddConnector(ShapeType, float, float, float, float) {#addconnector}

Membuat bentuk penghubung baru dengan gaya templat default dan menambahkannya ke akhir koleksi bentuk.

```csharp
public IConnector AddConnector(ShapeType shapeType, float x, float y, float width, float height)
```

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| shapeType | ShapeType | [`ShapeType`](../../shapetype) dari bentuk penghubung yang akan ditambahkan. |
| x | Single | Koordinat x dari bingkai penghubung, dalam poin. |
| y | Single | Koordinat y dari bingkai penghubung, dalam poin. |
| width | Single | Lebar bingkai penghubung, dalam poin. |
| height | Single | Tinggi bingkai penghubung, dalam poin. |

### Nilai Kembali

[`IConnector`](../../iconnector) yang baru dibuat.

### Contoh

Contoh berikut menunjukkan cara menambahkan penghubung (penghubung bengkok) antara dua bentuk (elips dan persegi panjang) dalam Presentasi PowerPoint.

```csharp
[C#]
// Membuat instance kelas presentasi yang mewakili file PPTX
using (Presentation input = new Presentation())
{
    // Mengakses koleksi shape untuk slide tertentu
    IShapeCollection shapes = input.Slides[0].Shapes;
    // Menambahkan autoshape Ellipse
    IAutoShape ellipse = shapes.AddAutoShape(ShapeType.Ellipse, 0, 100, 100, 100);
    // Menambahkan autoshape Rectangle
    IAutoShape rectangle = shapes.AddAutoShape(ShapeType.Rectangle, 100, 300, 100, 100);
    // Menambahkan shape konektor ke koleksi shape slide
    IConnector connector = shapes.AddConnector(ShapeType.BentConnector2, 0, 0, 10, 10);
    // Menghubungkan shape menggunakan konektor
    connector.StartShapeConnectedTo = ellipse;
    connector.EndShapeConnectedTo = rectangle;
    // Memanggil reroute yang mengatur jalur terpendek otomatis antara shape
    connector.Reroute();
    // Menyimpan presentasi
    input.Save("Shapes-connector.pptx", SaveFormat.Pptx);
}
```

### Lihat Juga

* interface [IConnector](../../iconnector)
* enum [ShapeType](../../shapetype)
* class [ShapeCollection](../../shapecollection)
* namespace [Aspose.Slides](../../shapecollection)
* assembly [Aspose.Slides](../../../)

---

## AddConnector(ShapeType, float, float, float, float, bool) {#addconnector_1}

Membuat bentuk penghubung baru dan menambahkannya ke akhir koleksi bentuk, secara opsional menerapkan gaya templat default.

```csharp
public IConnector AddConnector(ShapeType shapeMask, float x, float y, float width, float height, 
    bool createFromTemplate)
```

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| shapeType | ShapeType | [`ShapeType`](../../shapetype) dari bentuk penghubung yang akan dibuat. |
| x | Single | Koordinat x dari bingkai penghubung, dalam poin. |
| y | Single | Koordinat y dari bingkai penghubung, dalam poin. |
| width | Single | Lebar bingkai penghubung, dalam poin. |
| height | Single | Tinggi bingkai penghubung, dalam poin. |
| createFromTemplate | Boolean | True untuk menerapkan gaya templat default (nama tidak kosong, gaya sederhana); false untuk membuat penghubung dengan nilai properti default. |

### Nilai Kembali

[`IConnector`](../../iconnector) yang baru dibuat.

### Lihat Juga

* interface [IConnector](../../iconnector)
* enum [ShapeType](../../shapetype)
* class [ShapeCollection](../../shapecollection)
* namespace [Aspose.Slides](../../shapecollection)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->