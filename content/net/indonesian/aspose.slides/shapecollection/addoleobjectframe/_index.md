---
title: AddOleObjectFrame
second_title: Referensi API Aspose.Sildes untuk .NET
description: Membuat frame objek OLE baru dan menambahkannya ke akhir koleksi shape.
type: docs
weight: 150
url: /id/aspose.slides/shapecollection/addoleobjectframe/
---
## AddOleObjectFrame(float, float, float, float, IOleEmbeddedDataInfo) {#addoleobjectframe}

Membuat frame objek OLE baru dan menambahkannya ke akhir koleksi shape.

```csharp
public IOleObjectFrame AddOleObjectFrame(float x, float y, float width, float height, 
    IOleEmbeddedDataInfo dataInfo)
```

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| x | Single | Koordinat x dari frame OLE baru, dalam poin. |
| y | Single | Koordinat y dari frame OLE baru, dalam poin. |
| width | Single | Lebar frame OLE baru, dalam poin. |
| height | Single | Tinggi frame OLE baru, dalam poin. |
| dataInfo | IOleEmbeddedDataInfo | Informasi tentang data OLE yang disematkan ([`IOleEmbeddedDataInfo`](../../ioleembeddeddatainfo)). |

### Nilai Kembali

[`IOleObjectFrame`](../../ioleobjectframe) yang baru dibuat.

### Contoh

Contoh berikut menunjukkan cara menambahkan OLE Object Frames ke Slides dari Presentasi PowerPoint.

```csharp
[C#]
// Membuat instance kelas Presentation yang merepresentasikan file PPTX
using (Presentation pres = new Presentation())
{
    // Mengakses slide pertama
    ISlide sld = pres.Slides[0];
    // Memuat file Excel ke dalam stream
    MemoryStream mstream = new MemoryStream();
    using (FileStream fs = new FileStream("book1.xlsx", FileMode.Open, FileAccess.Read))
    {
        byte[] buf = new byte[4096];
        while (true)
        {
            int bytesRead = fs.Read(buf, 0, buf.Length);
            if (bytesRead <= 0)
                break;
            mstream.Write(buf, 0, bytesRead);
        }
    }
    // Membuat objek data untuk penyematan
    IOleEmbeddedDataInfo dataInfo = new OleEmbeddedDataInfo(mstream.ToArray(), "xlsx");
    // Menambahkan shape Ole Object Frame
    IOleObjectFrame oleObjectFrame = sld.Shapes.AddOleObjectFrame(0, 0, pres.SlideSize.Size.Width,
        pres.SlideSize.Size.Height, dataInfo);
    //Menulis file PPTX ke disk
    pres.Save("OleEmbed_out.pptx", SaveFormat.Pptx);
}
```

### Lihat Juga

* antarmuka [IOleObjectFrame](../../ioleobjectframe)
* antarmuka [IOleEmbeddedDataInfo](../../ioleembeddeddatainfo)
* kelas [ShapeCollection](../../shapecollection)
* ruang nama [Aspose.Slides](../../shapecollection)
* assembly [Aspose.Slides](../../../)

---

## AddOleObjectFrame(float, float, float, float, string, string) {#addoleobjectframe_1}

Membuat frame objek OLE baru dan menambahkannya ke akhir koleksi shape.

```csharp
public IOleObjectFrame AddOleObjectFrame(float x, float y, float width, float height, 
    string className, string path)
```

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| x | Single | Koordinat x dari frame OLE baru, dalam poin. |
| y | Single | Koordinat y dari frame OLE baru, dalam poin. |
| width | Single | Lebar frame OLE baru, dalam poin. |
| height | Single | Tinggi frame OLE baru, dalam poin. |
| className | String | Nama kelas dari objek OLE. |
| path | String | Jalur ke file yang ditautkan. |

### Nilai Kembali

[`IOleObjectFrame`](../../ioleobjectframe) yang baru dibuat.

### Lihat Juga

* antarmuka [IOleObjectFrame](../../ioleobjectframe)
* kelas [ShapeCollection](../../shapecollection)
* ruang nama [Aspose.Slides](../../shapecollection)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->