---
title: SetExternalWorkbook
second_title: Aspose.Sildes for .NET API 레퍼런스
description: 외부 워크북을 차트의 데이터 소스로 설정합니다. 차트 데이터는 대상 워크북에서 업데이트됩니다.
type: docs
weight: 120
url: /ko/aspose.slides.charts/chartdata/setexternalworkbook/
---
## SetExternalWorkbook(string) {#setexternalworkbook}

외부 워크북을 차트의 데이터 소스로 설정합니다. 차트 데이터는 대상 워크북에서 업데이트됩니다.

```csharp
public void SetExternalWorkbook(string workbookPath)
```

| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| workbookPath | String | 대상 워크북의 경로 |

### 예외

| 예외 | 조건 |
| --- | --- |
| InvalidOperationException | 외부 워크북을 사용할 수 없거나 로드할 수 없습니다. |

### 예제

```csharp
[C#]
using (Presentation pres = new Presentation())

   IChart chart = pres.Slides[0].Shapes.AddChart(ChartType.Pie, 50, 50, 400, 600, true);
   IChartData chartData = chart.ChartData;
   (chartData as ChartData).SetExternalWorkbook("../../workbook.xlsx");
```

### 참조

* 클래스 [ChartData](../../chartdata)
* 네임스페이스 [Aspose.Slides.Charts](../../chartdata)
* 어셈블리 [Aspose.Slides](../../../)

---

## SetExternalWorkbook(string, bool) {#setexternalworkbook_1}

외부 워크북을 차트의 데이터 소스로 설정합니다.

```csharp
public void SetExternalWorkbook(string workbookPath, bool updateChartData)
```

| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| workbookPath | String | 대상 워크북의 경로 |
| updateChartData | Boolean | 값이 false이면 워크북 경로만 업데이트됩니다. 차트 데이터는 로드되거나 대상 워크북에서 업데이트되지 않습니다. 대상 워크북이 없거나 사용할 수 없을 때 사용할 수 있습니다. 값이 true이면 차트 데이터가 대상 워크북에서 업데이트됩니다. |

### 예외

| 예외 | 조건 |
| --- | --- |
| InvalidOperationException | 외부 워크북을 사용할 수 없거나 로드할 수 없습니다. |

### 예제

```csharp
[C#]
using (Presentation pres = new Presentation())

   IChart chart = pres.Slides[0].Shapes.AddChart(ChartType.Pie, 50, 50, 400, 600, true);
   IChartData chartData = chart.ChartData;

   (chartData as ChartData).SetExternalWorkbook("http://path/doesnt/exists", false);


```

### 참조

* 클래스 [ChartData](../../chartdata)
* 네임스페이스 [Aspose.Slides.Charts](../../chartdata)
* 어셈블리 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldoccmd for Aspose.Slides.dll -->