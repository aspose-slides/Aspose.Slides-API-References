---
title: WriteAsSvg
second_title: Aspose.Sildes .NET API 레퍼런스
description: 슬라이드 콘텐츠를 SVG 파일로 저장합니다.
type: docs
weight: 140
url: /ko/aspose.slides/slide/writeassvg/
---
## WriteAsSvg(Stream) {#writeassvg}

슬라이드 콘텐츠를 SVG 파일로 저장합니다.

```csharp
public void WriteAsSvg(Stream stream)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | Stream | 대상 스트림 |

### 예제

다음 코드 예제는 PowerPoint 프레젠테이션의 첫 번째 슬라이드를 SVG 파일로 변환하는 방법을 보여줍니다.

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (Stream fileStream = System.IO.File.Create("slide_1.svg"))
    {
        // 첫 번째 슬라이드를 SVG 파일로 저장합니다
        pres.Slides[0].WriteAsSvg(fileStream);
    }
}
```

### 관련 항목

* 클래스 [Slide](../../slide)
* 네임스페이스 [Aspose.Slides](../../slide)
* 어셈블리 [Aspose.Slides](../../../)

---

## WriteAsSvg(Stream, ISVGOptions) {#writeassvg_1}

슬라이드 콘텐츠를 SVG 파일로 저장합니다.

```csharp
public void WriteAsSvg(Stream stream, ISVGOptions svgOptions)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | Stream | 대상 스트림 |
| svgOptions | ISVGOptions | SVG 생성 옵션 |

### 예제

다음 코드 예제는 옵션을 사용하여 PowerPoint 프레젠테이션의 첫 번째 슬라이드를 SVG 파일로 변환하는 방법을 보여줍니다.

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (Stream fileStream = System.IO.File.Create("slide_1.svg"))
    {
        var options = new SVGOptions() { VectorizeText = true };
        // 첫 번째 슬라이드를 SVG 파일로 저장합니다
        pres.Slides[0].WriteAsSvg(fileStream, options);
    }
}
```

### 관련 항목

* 인터페이스 [ISVGOptions](../../../aspose.slides.export/isvgoptions)
* 클래스 [Slide](../../slide)
* 네임스페이스 [Aspose.Slides](../../slide)
* 어셈블리 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->