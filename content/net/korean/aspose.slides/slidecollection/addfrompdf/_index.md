---
title: AddFromPdf
second_title: Aspose.Sildes for .NET API 레퍼런스
description: PDF 문서에서 슬라이드를 생성하고 컬렉션의 끝에 추가합니다.
type: docs
weight: 80
url: /ko/aspose.slides/slidecollection/addfrompdf/
---
## AddFromPdf(string) {#addfrompdf_2}

PDF 문서에서 슬라이드를 생성하고 컬렉션의 끝에 추가합니다.

```csharp
public ISlide[] AddFromPdf(string path)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| path | String | PDF 문서에 대한 경로 |

### 반환값

추가된 슬라이드

### 예제

예시:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    pres.Slides.AddFromPdf("document.pdf");
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### 참조

* 인터페이스 [ISlide](../../islide)
* 클래스 [SlideCollection](../../slidecollection)
* 네임스페이스 [Aspose.Slides](../../slidecollection)
* 어셈블리 [Aspose.Slides](../../../)

---

## AddFromPdf(string, PdfImportOptions) {#addfrompdf_3}

PDF 문서에서 슬라이드를 생성하고 pdf import 옵션을 고려하여 컬렉션의 끝에 추가합니다.

```csharp
public ISlide[] AddFromPdf(string path, PdfImportOptions pdfImportOptions)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| path | String | PDF 문서에 대한 경로 |
| pdfImportOptions | PdfImportOptions | pdf import 옵션 |

### 반환값

추가된 슬라이드

### 예제

예시:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    pres.Slides.AddFromPdf("document.pdf, new PdfImportOptions { DetectTables = true });
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### 참조

* 인터페이스 [ISlide](../../islide)
* 클래스 [PdfImportOptions](../../../aspose.slides.import/pdfimportoptions)
* 클래스 [SlideCollection](../../slidecollection)
* 네임스페이스 [Aspose.Slides](../../slidecollection)
* 어셈블리 [Aspose.Slides](../../../)

---

## AddFromPdf(Stream) {#addfrompdf}

PDF 문서에서 슬라이드를 생성하고 컬렉션의 끝에 추가합니다.

```csharp
public ISlide[] AddFromPdf(Stream pdfStream)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| pdfStream | Stream | PDF 문서의 소스로 사용될 스트림 |

### 반환값

추가된 슬라이드

### 예제

예시:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    using (Stream stream = new FileStream("document.pdf", FileMode.Open, FileAccess.Read, FileShare.Read))
    {
        pres.Slides.AddFromPdf(stream);
    }
    
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### 참조

* 인터페이스 [ISlide](../../islide)
* 클래스 [SlideCollection](../../slidecollection)
* 네임스페이스 [Aspose.Slides](../../slidecollection)
* 어셈블리 [Aspose.Slides](../../../)

---

## AddFromPdf(Stream, PdfImportOptions) {#addfrompdf_1}

PDF 문서에서 슬라이드를 생성하고 컬렉션의 끝에 추가합니다.

```csharp
public ISlide[] AddFromPdf(Stream pdfStream, PdfImportOptions pdfImportOptions)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| pdfStream | Stream | PDF 문서의 소스로 사용될 스트림 |
| pdfImportOptions | PdfImportOptions | pdf import 옵션 |

### 반환값

추가된 슬라이드

### 예제

예시:

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    using (Stream stream = new FileStream("document.pdf", FileMode.Open, FileAccess.Read, FileShare.Read))
    {
        pres.Slides.AddFromPdf(stream, new PdfImportOptions { DetectTables = true });
    }
    
    pres.Save("fromPdfDocument.pptx", SaveFormat.Pptx);
}
```

### 참조

* 인터페이스 [ISlide](../../islide)
* 클래스 [PdfImportOptions](../../../aspose.slides.import/pdfimportoptions)
* 클래스 [SlideCollection](../../slidecollection)
* 네임스페이스 [Aspose.Slides](../../slidecollection)
* 어셈블리 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->