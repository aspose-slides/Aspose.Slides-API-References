---
title: FieldType
second_title: Aspose.Sildes .NET API 레퍼런스
description: 필드 유형을 나타냅니다. 이 값은 필드 부분이 업데이트될 때 설정될 텍스트를 결정합니다.
type: docs
weight: 4760
url: /ko/aspose.slides/fieldtype/
---
## FieldType 클래스

필드 유형을 나타냅니다. 이 값은 필드 부분이 업데이트될 때 어떤 텍스트가 설정되는지를 결정합니다.

```csharp
public sealed class FieldType : IFieldType
```

## 생성자

| 이름 | 설명 |
| --- | --- |
| [FieldType](fieldtype)(string) | FieldType 클래스의 새 인스턴스를 초기화합니다. |

## 속성

| 이름 | 설명 |
| --- | --- |
| static [DateTime](../../aspose.slides/fieldtype/datetime) { get; } | 렌더링 애플리케이션의 기본 날짜/시간 형식으로 현재 날짜와 시간입니다. 읽기 전용 [`FieldType`](../fieldtype). |
| static [DateTime1](../../aspose.slides/fieldtype/datetime1) { get; } | 첫 번째 미리 정의된 형식(MM/DD/YYYY for english)으로 현재 날짜와 시간입니다. 읽기 전용 [`FieldType`](../fieldtype). |
| static [DateTime10](../../aspose.slides/fieldtype/datetime10) { get; } | 열 번째 미리 정의된 형식(hh:mm for english)으로 현재 날짜와 시간입니다. 읽기 전용 [`FieldType`](../fieldtype). |
| static [DateTime11](../../aspose.slides/fieldtype/datetime11) { get; } | 열한 번째 미리 정의된 형식(hh:mm:ss for english)으로 현재 날짜와 시간입니다. 읽기 전용 [`FieldType`](../fieldtype). |
| static [DateTime12](../../aspose.slides/fieldtype/datetime12) { get; } | 열두 번째 미리 정의된 형식(hh:mm AM/PM for english)으로 현재 날짜와 시간입니다. 읽기 전용 [`FieldType`](../fieldtype). |
| static [DateTime13](../../aspose.slides/fieldtype/datetime13) { get; } | 열세 번째 미리 정의된 형식(hh:mm:ss AM/PM for english)으로 현재 날짜와 시간입니다. 읽기 전용 [`FieldType`](../fieldtype). |
| static [DateTime2](../../aspose.slides/fieldtype/datetime2) { get; } | 두 번째 미리 정의된 형식(Day, Month DD, YYYY for english)으로 현재 날짜와 시간입니다. 읽기 전용 [`FieldType`](../fieldtype). |
| static [DateTime3](../../aspose.slides/fieldtype/datetime3) { get; } | 세 번째 미리 정의된 형식(DD Month YYYY for english)으로 현재 날짜와 시간입니다. 읽기 전용 [`FieldType`](../fieldtype). |
| static [DateTime4](../../aspose.slides/fieldtype/datetime4) { get; } | 네 번째 미리 정의된 형식(Month DD, YYYY for english)으로 현재 날짜와 시간입니다. 읽기 전용 [`FieldType`](../fieldtype). |
| static [DateTime5](../../aspose.slides/fieldtype/datetime5) { get; } | 다섯 번째 미리 정의된 형식(DD-Mon-YY for english)으로 현재 날짜와 시간입니다. 읽기 전용 [`FieldType`](../fieldtype). |
| static [DateTime6](../../aspose.slides/fieldtype/datetime6) { get; } | 여섯 번째 미리 정의된 형식(Month YY for english)으로 현재 날짜와 시간입니다. 읽기 전용 [`FieldType`](../fieldtype). |
| static [DateTime7](../../aspose.slides/fieldtype/datetime7) { get; } | 일곱 번째 미리 정의된 형식(Mon-YY for english)으로 현재 날짜와 시간입니다. 읽기 전용 [`FieldType`](../fieldtype). |
| static [DateTime8](../../aspose.slides/fieldtype/datetime8) { get; } | 여덟 번째 미리 정의된 형식(MM/DD/YYYY hh:mm AM/PM for english)으로 현재 날짜와 시간입니다. 읽기 전용 [`FieldType`](../fieldtype). |
| static [DateTime9](../../aspose.slides/fieldtype/datetime9) { get; } | 아홉 번째 미리 정의된 형식(MM/DD/YYYY hh:mm:ss AM/PM for english)으로 현재 날짜와 시간입니다. 읽기 전용 [`FieldType`](../fieldtype). |
| static [Footer](../../aspose.slides/fieldtype/footer) { get; } | 슬라이드의 바닥글입니다. 읽기 전용 [`FieldType`](../fieldtype). |
| static [Header](../../aspose.slides/fieldtype/header) { get; } | 슬라이드의 머리글입니다. 읽기 전용 [`FieldType`](../fieldtype). |
| static [SlideNumber](../../aspose.slides/fieldtype/slidenumber) { get; } | 현재 슬라이드 번호입니다. 읽기 전용 [`FieldType`](../fieldtype). |
| [InternalString](../../aspose.slides/fieldtype/internalstring) { get; set; } | 이 FieldType 객체의 내부 이름을 반환합니다. 읽기/쓰기 String. |

## 메서드

| 이름 | 설명 |
| --- | --- |
| override [Equals](../../aspose.slides/fieldtype/equals)(object) | 이 필드가 다른 필드와 같은지 확인합니다. |
| override [GetHashCode](../../aspose.slides/fieldtype/gethashcode)() | 이 객체의 해시코드를 반환합니다. |
| [operator ==](../../aspose.slides/fieldtype/op_equality) | 두 FieldType 객체가 같은지 확인합니다. |
| [operator !=](../../aspose.slides/fieldtype/op_inequality) | 두 FieldType 객체가 다른지 확인합니다. |

### 참조

* 인터페이스 [IFieldType](../ifieldtype)
* 네임스페이스 [Aspose.Slides](../../aspose.slides)
* 어셈블리 [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->