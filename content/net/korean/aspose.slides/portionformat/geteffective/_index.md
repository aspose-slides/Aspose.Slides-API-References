---
title: GetEffective
second_title: Aspose.Sildes for .NET API 레퍼런스
description: 상속이 적용된 유효한 부분 서식 데이터를 가져옵니다.
type: docs
weight: 70
url: /ko/aspose.slides/portionformat/geteffective/
---
## PortionFormat.GetEffective 메서드

상속이 적용된 유효한 부분 서식 데이터를 가져옵니다.

```csharp
public IPortionFormatEffectiveData GetEffective()
```

### 반환값

다음 [`IPortionFormatEffectiveData`](../../iportionformateffectivedata).

### 예제

이 예제에서는 효과적인 부분 서식 속성을 가져오는 방법을 보여줍니다.

```csharp
[C#]
using (Presentation pres = new Presentation(@"MyPresentation.pptx"))
{
	IAutoShape shape = pres.Slides[0].Shapes[0] as IAutoShape;
	IPortionFormatEffectiveData effectivePortionFormat = shape.TextFrame.Paragraphs[0].Portions[0].PortionFormat.GetEffective();

	Console.WriteLine("Latin font: " + effectivePortionFormat.LatinFont.FontName);
	Console.WriteLine("Font height: " + effectivePortionFormat.FontHeight);
	Console.WriteLine("Fill type: " + effectivePortionFormat.FillFormat.FillType);
}
```

### 참조

* 인터페이스 [IPortionFormatEffectiveData](../../iportionformateffectivedata)
* 클래스 [PortionFormat](../../portionformat)
* 네임스페이스 [Aspose.Slides](../../portionformat)
* 어셈블리 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->