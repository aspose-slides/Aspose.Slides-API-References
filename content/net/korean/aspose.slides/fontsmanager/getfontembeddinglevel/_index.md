---
title: GetFontEmbeddingLevel
second_title: Aspose.Sildes for .NET API 레퍼런스
description: 주어진 바이트 배열과 폰트 이름으로부터 폰트의 임베딩 레벨을 결정합니다.
type: docs
weight: 60
url: /ko/aspose.slides/fontsmanager/getfontembeddinglevel/
---
## FontsManager.GetFontEmbeddingLevel 메서드

주어진 바이트 배열과 폰트 이름으로부터 폰트의 임베딩 레벨을 결정합니다.

```csharp
public EmbeddingLevel GetFontEmbeddingLevel(byte[] fontBytes, string fontName)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| fontBytes | Byte[] | 폰트 데이터를 포함하는 바이트 배열. |
| fontName | String | 폰트의 이름. |

### 반환값

지정된 폰트의 임베딩 레벨.

### 예외

| 예외 | 조건 |
| --- | --- |
| ArgumentNullException | *fontBytes*가 null인 경우 발생합니다. |

### 예제

```csharp
[C#]
using (Presentation pres = new Presentation(pptxFileName))
{
    // 프레젠테이션에서 사용된 모든 글꼴을 검색합니다
    IFontData[] fontDatas = pres.FontsManager.GetFonts();

    // 프레젠테이션에서 첫 번째 글꼴의 Regular 스타일을 나타내는 바이트 배열을 가져옵니다
    byte[] bytes = pres.FontsManager.GetFontBytes(fontDatas[0], FontStyle.Regular);

    // 글꼴의 임베딩 레벨을 결정합니다
    EmbeddingLevel embeddingLevel = pres.FontsManager.GetFontEmbeddingLevel(bytes, fontDatas[0].FontName);
}
```

### 관련 항목

* enum [EmbeddingLevel](../../embeddinglevel)
* class [FontsManager](../../fontsmanager)
* namespace [Aspose.Slides](../../fontsmanager)
* assembly [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->