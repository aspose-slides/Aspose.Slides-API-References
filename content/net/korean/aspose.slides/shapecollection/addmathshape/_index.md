---
title: AddMathShape
second_title: Aspose.Sildes for .NET API 참조
description: 수학 콘텐츠를 호스트하기 위해 새로운 사각형 자동 도형을 생성하고 이를 shape 컬렉션의 끝에 추가합니다.
type: docs
weight: 140
url: /ko/aspose.slides/shapecollection/addmathshape/
---
## ShapeCollection.AddMathShape 메서드

새로운 사각형 자동 도형을 생성하여 수학 콘텐츠를 호스트하고 shape 컬렉션의 끝에 추가합니다.

```csharp
public IAutoShape AddMathShape(float x, float y, float width, float height)
```

| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| x | Single | shape 프레임의 x 좌표(포인트). |
| y | Single | shape 프레임의 y 좌표(포인트). |
| width | Single | shape 프레임의 너비(포인트). |
| height | Single | shape 프레임의 높이(포인트). |

### 반환 값

새로 생성된 [`IAutoShape`](../../iautoshape).

### 예제

다음 예제는 PowerPoint 프레젠테이션에 수학 식을 추가하는 방법을 보여줍니다.

```csharp
[C#]
using (Presentation pres = new Presentation())
{
   IAutoShape mathShape = pres.Slides[0].Shapes.AddMathShape(0, 0, 720, 150);
   var mathParagraph = (mathShape.TextFrame.Paragraphs[0].Portions[0] as MathPortion).MathParagraph;
   var fraction = new MathematicalText("x").Divide("y");
   mathParagraph.Add(new MathBlock(fraction));
   var mathBlock = new MathematicalText("c")
        .SetSuperscript("2")
        .Join("=")
        .Join(new MathematicalText("a").SetSuperscript("2"))
        .Join("+")
        .Join(new MathematicalText("b").SetSuperscript("2"));
    mathParagraph.Add(mathBlock);
    pres.Save("math.pptx", SaveFormat.Pptx);
}
```

### 참고

* 인터페이스 [IAutoShape](../../iautoshape)
* 클래스 [ShapeCollection](../../shapecollection)
* 네임스페이스 [Aspose.Slides](../../shapecollection)
* 어셈블리 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->