---
title: AddOleObjectFrame
second_title: Aspose.Sildes for .NET API 레퍼런스
description: 새 OLE 개체 프레임을 생성하고 이를 도형 컬렉션의 끝에 추가합니다.
type: docs
weight: 150
url: /ko/aspose.slides/shapecollection/addoleobjectframe/
---
## AddOleObjectFrame(float, float, float, float, IOleEmbeddedDataInfo) {#addoleobjectframe}

새 OLE 개체 프레임을 생성하고 이를 모양 컬렉션의 끝에 추가합니다.

```csharp
public IOleObjectFrame AddOleObjectFrame(float x, float y, float width, float height, 
    IOleEmbeddedDataInfo dataInfo)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| x | Single | 새 OLE 프레임의 x 좌표(포인트 단위) |
| y | Single | 새 OLE 프레임의 y 좌표(포인트 단위) |
| width | Single | 새 OLE 프레임의 너비(포인트 단위) |
| height | Single | 새 OLE 프레임의 높이(포인트 단위) |
| dataInfo | IOleEmbeddedDataInfo | 임베드된 OLE 데이터에 대한 정보([`IOleEmbeddedDataInfo`](../../ioleembeddeddatainfo)) |

### 반환 값

새로 생성된 [`IOleObjectFrame`](../../ioleobjectframe).

### 예제

다음 예제는 PowerPoint 프레젠테이션의 슬라이드에 OLE 개체 프레임을 추가하는 방법을 보여줍니다.

```csharp
[C#]
// PPTX 파일을 나타내는 Presentation 클래스를 인스턴스화합니다
using (Presentation pres = new Presentation())
{
    // 첫 번째 슬라이드에 접근합니다
    ISlide sld = pres.Slides[0];
    // excel 파일을 스트림에 로드합니다
    MemoryStream mstream = new MemoryStream();
    using (FileStream fs = new FileStream("book1.xlsx", FileMode.Open, FileAccess.Read))
    {
        byte[] buf = new byte[4096];
        while (true)
        {
            int bytesRead = fs.Read(buf, 0, buf.Length);
            if (bytesRead <= 0)
                break;
            mstream.Write(buf, 0, bytesRead);
        }
    }
    // 임베드용 데이터 객체를 생성합니다
    IOleEmbeddedDataInfo dataInfo = new OleEmbeddedDataInfo(mstream.ToArray(), "xlsx");
    // Ole Object Frame 도형을 추가합니다
    IOleObjectFrame oleObjectFrame = sld.Shapes.AddOleObjectFrame(0, 0, pres.SlideSize.Size.Width,
        pres.SlideSize.Size.Height, dataInfo);
    //PPTX 파일을 디스크에 씁니다
    pres.Save("OleEmbed_out.pptx", SaveFormat.Pptx);
}
```

### 관련 항목

* 인터페이스 [IOleObjectFrame](../../ioleobjectframe)
* 인터페이스 [IOleEmbeddedDataInfo](../../ioleembeddeddatainfo)
* 클래스 [ShapeCollection](../../shapecollection)
* 네임스페이스 [Aspose.Slides](../../shapecollection)
* 어셈블리 [Aspose.Slides](../../../)

---

## AddOleObjectFrame(float, float, float, float, string, string) {#addoleobjectframe_1}

새 OLE 개체 프레임을 생성하고 이를 모양 컬렉션의 끝에 추가합니다.

```csharp
public IOleObjectFrame AddOleObjectFrame(float x, float y, float width, float height, 
    string className, string path)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| x | Single | 새 OLE 프레임의 x 좌표(포인트 단위) |
| y | Single | 새 OLE 프레임의 y 좌표(포인트 단위) |
| width | Single | 새 OLE 프레임의 너비(포인트 단위) |
| height | Single | 새 OLE 프레임의 높이(포인트 단위) |
| className | String | OLE 개체의 클래스 이름입니다. |
| path | String | 링크된 파일의 경로입니다. |

### 반환 값

새로 생성된 [`IOleObjectFrame`](../../ioleobjectframe).

### 관련 항목

* 인터페이스 [IOleObjectFrame](../../ioleobjectframe)
* 클래스 [ShapeCollection](../../shapecollection)
* 네임스페이스 [Aspose.Slides](../../shapecollection)
* 어셈블리 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->