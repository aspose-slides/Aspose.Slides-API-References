---
title: AddZoomFrame
second_title: Aspose.Sildes for .NET API 레퍼런스
description: 새 Zoom 프레임을 생성하고 도형 컬렉션의 끝에 추가합니다.
type: docs
weight: 220
url: /ko/aspose.slides/shapecollection/addzoomframe/
---
## AddZoomFrame(float, float, float, float, ISlide) {#addzoomframe}

새 Zoom 프레임을 생성하고 도형 컬렉션 끝에 추가합니다.

```csharp
public IZoomFrame AddZoomFrame(float x, float y, float width, float height, ISlide slide)
```

| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| x | Single | 새 Zoom 프레임의 x 좌표(포인트 단위). |
| y | Single | 새 Zoom 프레임의 y 좌표(포인트 단위). |
| width | Single | 새 Zoom 프레임의 너비(포인트 단위). |
| height | Single | 새 Zoom 프레임의 높이(포인트 단위). |
| slide | ISlide | Zoom 프레임이 참조하는 [`ISlide`](../../islide); 현재 프레젠테이션에 속해야 합니다. |

### 반환값

새로 생성된 [`IZoomFrame`](../../izoomframe).

### 예외

| 예외 | 조건 |
| --- | --- |
| ArgumentException | 참조된 슬라이드가 현재 프레젠테이션에 속하지 않은 경우 발생합니다. |

### 예제

이 예제는 컬렉션 끝에 Zoom 객체를 추가하는 방법을 보여줍니다(“Presentation.pptx” 프레젠테이션에 슬라이드가 두 개 이상 있다고 가정).

```csharp
[C#]
using (Presentation pres = new Presentation("Presentation.pptx"))
{
    IZoomFrame zoomFrame = pres.Slides[0].Shapes.AddZoomFrame(150, 20, 50, 50, pres.Slides[1]);
}
```

### 추가 보기

* 인터페이스 [IZoomFrame](../../izoomframe)
* 인터페이스 [ISlide](../../islide)
* 클래스 [ShapeCollection](../../shapecollection)
* 네임스페이스 [Aspose.Slides](../../shapecollection)
* 어셈블리 [Aspose.Slides](../../../)

---

## AddZoomFrame(float, float, float, float, ISlide, IPPImage) {#addzoomframe_1}

새 Zoom 프레임을 생성하고 도형 컬렉션 끝에 추가합니다.

```csharp
public IZoomFrame AddZoomFrame(float x, float y, float width, float height, ISlide slide, 
    IPPImage image)
```

| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| x | Single | 새 Zoom 프레임의 x 좌표(포인트 단위). |
| y | Single | 새 Zoom 프레임의 y 좌표(포인트 단위). |
| width | Single | 새 Zoom 프레임의 너비(포인트 단위). |
| height | Single | 새 Zoom 프레임의 높이(포인트 단위). |
| slide | ISlide | Zoom 프레임이 참조하는 [`ISlide`](../../islide); 현재 프레젠테이션에 속해야 합니다. |
| image | IPPImage | 참조된 슬라이드 [`IPPImage`](../../ippimage)의 이미지. |

### 반환값

새로 생성된 [`IZoomFrame`](../../izoomframe).

### 예외

| 예외 | 조건 |
| --- | --- |
| ArgumentException | 참조된 슬라이드가 현재 프레젠테이션에 속하지 않은 경우 발생합니다. |

### 예제

이 예제는 컬렉션 끝에 Zoom 객체를 추가하는 방법을 보여줍니다(“Presentation.pptx” 프레젠테이션에 슬라이드가 두 개 이상 있다고 가정).

```csharp
[C#]
using (Presentation pres = new Presentation("Presentation.pptx"))
{
    IPPImage image = pres.Images.AddImage(Image.FromFile("image.png"));
    IZoomFrame zoomFrame = pres.Slides[0].Shapes.AddZoomFrame(150, 20, 50, 50, pres.Slides[1], image);
}
```

### 추가 보기

* 인터페이스 [IZoomFrame](../../izoomframe)
* 인터페이스 [ISlide](../../islide)
* 인터페이스 [IPPImage](../../ippimage)
* 클래스 [ShapeCollection](../../shapecollection)
* 네임스페이스 [Aspose.Slides](../../shapecollection)
* 어셈블리 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->