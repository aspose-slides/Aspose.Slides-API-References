---
title: AddAudioFrameEmbedded
second_title: Aspose.Sildes for .NET API 참조
description: 임베드된 WAV 파일이 포함된 새로운 오디오 프레임을 생성하고 모양 컬렉션 끝에 추가합니다. 임베드된 오디오는 Presentation.Audios 컬렉션에 추가됩니다.
type: docs
weight: 70
url: /ko/aspose.slides/shapecollection/addaudioframeembedded/
---
## AddAudioFrameEmbedded(float, float, float, float, Stream) {#addaudioframeembedded_1}

임베드된 WAV 파일이 포함된 새로운 오디오 프레임을 생성하고 모양 컬렉션 끝에 추가합니다. 임베드된 오디오는 Presentation.Audios 컬렉션에 추가됩니다.

```csharp
public IAudioFrame AddAudioFrameEmbedded(float x, float y, float width, float height, 
    Stream audio_stream)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| x | Single | 새 오디오 프레임의 x 좌표(단위: 포인트). |
| y | Single | 새 오디오 프레임의 y 좌표(단위: 포인트). |
| width | Single | 새 오디오 프레임의 너비(단위: 포인트). |
| height | Single | 새 오디오 프레임의 높이(단위: 포인트). |
| audio_stream | Stream | 임베드할 WAV 오디오 데이터를 포함하는 입력 스트림. |

### 반환값

새로 생성된 [`IAudioFrame`](../../iaudioframe).

### 예제

다음 예제는 Audio Frame을 만드는 방법을 보여줍니다.

```csharp
[C#]
// 프레젠테이션 파일을 나타내는 Presentation 클래스를 인스턴스화합니다
using (Presentation pres = new Presentation())
{
    // 첫 번째 슬라이드를 가져옵니다
    ISlide sld = pres.Slides[0];
    // wav 사운드 파일을 스트림으로 로드합니다
    FileStream fstr = new FileStream("sampleaudio.wav", FileMode.Open, FileAccess.Read);
    // 오디오 프레임을 추가합니다
    IAudioFrame audioFrame = sld.Shapes.AddAudioFrameEmbedded(50, 150, 100, 100, fstr);
    // 오디오의 재생 모드와 볼륨을 설정합니다
    audioFrame.PlayMode = AudioPlayModePreset.Auto;
    audioFrame.Volume = AudioVolumeMode.Loud;
    // PowerPoint 파일을 디스크에 저장합니다
    pres.Save("AudioFrameEmbed_out.pptx", SaveFormat.Pptx);
}
```

### 참고

* 인터페이스 [IAudioFrame](../../iaudioframe)
* 클래스 [ShapeCollection](../../shapecollection)
* 네임스페이스 [Aspose.Slides](../../shapecollection)
* 어셈블리 [Aspose.Slides](../../../)

---

## AddAudioFrameEmbedded(float, float, float, float, IAudio) {#addaudioframeembedded}

기존 Presentation.Audios 목록에 있는 오디오 객체를 사용하여 새로운 오디오 프레임을 생성하고 모양 컬렉션 끝에 추가합니다.

```csharp
public IAudioFrame AddAudioFrameEmbedded(float x, float y, float width, float height, IAudio audio)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| x | Single | 새 오디오 프레임의 x 좌표(단위: 포인트). |
| y | Single | 새 오디오 프레임의 y 좌표(단위: 포인트). |
| width | Single | 새 오디오 프레임의 너비(단위: 포인트). |
| height | Single | 새 오디오 프레임의 높이(단위: 포인트). |
| audio | IAudio | Presentation.Audios 컬렉션에서 가져온 [`IAudio`](../../iaudio) 인스턴스. |

### 반환값

새로 생성된 [`IAudioFrame`](../../iaudioframe).

### 참고

* 인터페이스 [IAudioFrame](../../iaudioframe)
* 인터페이스 [IAudio](../../iaudio)
* 클래스 [ShapeCollection](../../shapecollection)
* 네임스페이스 [Aspose.Slides](../../shapecollection)
* 어셈블리 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->