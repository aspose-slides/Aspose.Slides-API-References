---
title: CaptionTracks
second_title: Aspose.Sildes for .NET API 참조
description: 비디오 프레임에 연결된 폐쇄 캡션 컬렉션을 가져옵니다. 이 속성은 읽기 전용이며 모든 캡션 트랙을 포함하는 ICaptionsCollectionaspose.slides/icaptionscollection을 반환합니다.
type: docs
weight: 10
url: /ko/aspose.slides/videoframe/captiontracks/
---
## VideoFrame.CaptionTracks 속성

비디오 프레임과 연결된 폐쇄 캡션 컬렉션을 가져옵니다. 이 속성은 읽기 전용이며 모든 캡션 트랙을 포함하는 [`ICaptionsCollection`](../../icaptionscollection)을 반환합니다.

```csharp
public ICaptionsCollection CaptionTracks { get; }
```

### 예제

예제:

```csharp
[C#]
using (Presentation pres = new Presentation("video with captions.pptx"))
{
    foreach (IShape shape in pres.Slides[0].Shapes)
    {
        if (!(shape is IVideoFrame videoFrame)) continue;

        foreach (var captionTrack in videoFrame.CaptionTracks)
        {
            // 캡션 바이너리 데이터를 추출하고 파일에 저장합니다.
            System.IO.File.WriteAllBytes(captionTrack.CaptionId + ".vtt", captionTrack.BinaryData);
        }
    }
}
```

### 참조

* 인터페이스 [ICaptionsCollection](../../icaptionscollection)
* 클래스 [VideoFrame](../../videoframe)
* 네임스페이스 [Aspose.Slides](../../videoframe)
* 어셈블리 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->