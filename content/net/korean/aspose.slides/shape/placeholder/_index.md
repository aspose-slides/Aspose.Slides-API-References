---
title: Placeholder
second_title: Aspose.Sildes for .NET API 참조
description: 모양에 대한 플레이스홀더를 반환합니다. 모양에 플레이스홀더가 없으면 null을 반환합니다. 읽기 전용 IPlaceholderaspose.slides/iplaceholder.
type: docs
weight: 210
url: /ko/aspose.slides/shape/placeholder/
---
## Shape.Placeholder 속성

모양에 대한 플레이스홀더를 반환합니다. 모양에 플레이스홀더가 없으면 null을 반환합니다. 읽기 전용 [`IPlaceholder`](../../iplaceholder).

```csharp
public IPlaceholder Placeholder { get; }
```

### 예제

다음 예제는 플레이스홀더의 텍스트를 변경하는 방법을 보여줍니다.

```csharp
[C#]
// Presentation 클래스를 생성합니다
using (Presentation pres = new Presentation("ReplacingText.pptx"))
{
    // 첫 번째 슬라이드에 접근합니다
    ISlide sld = pres.Slides[0];
    // 플레이스홀더를 찾기 위해 도형들을 반복합니다
    foreach (IShape shp in sld.Shapes)
        if (shp.Placeholder != null)
        {
            // 각 플레이스홀더의 텍스트를 변경합니다
            ((IAutoShape)shp).TextFrame.Text = "This is a Placeholder";
        }
    // 프레젠테이션을 디스크에 저장합니다
    pres.Save("output_out.pptx", Aspose.Slides.Export.SaveFormat.Pptx);
}
```

다음 예제는 플레이스홀더에 프롬프트 텍스트를 설정하는 방법을 보여줍니다.

```csharp
[C#]
using (Presentation pres = new Presentation("Presentation2.pptx"))
{
    ISlide slide = pres.Slides[0];
    foreach (IShape shape in slide.Slide.Shapes) // 슬라이드를 순회합니다
    {
        if (shape.Placeholder.Type == PlaceholderType.CenteredTitle) // PowerPoint에서 "Click to add title"을 표시합니다
        {
            string text = "";
            text = "Add Title";
        }
        else if (shape.Placeholder.Type == PlaceholderType.Subtitle) // 부제목을 추가합니다
        {
            text = "Add Subtitle";
        }
        ((IAutoShape)shape).TextFrame.Text = text;
        Console.WriteLine($"Placeholder with text: {text}");
    }
    pres.Save("Placeholders_PromptText.pptx", SaveFormat.Pptx);
}
```

### 참고

* 인터페이스 [IPlaceholder](../../iplaceholder)
* 클래스 [Shape](../../shape)
* 네임스페이스 [Aspose.Slides](../../shape)
* 어셈블리 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->