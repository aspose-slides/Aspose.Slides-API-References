---
title: DisableFontLigatures
second_title: Aspose.Sildes for .NET API 레퍼런스
description: 텍스트를 리가처를 사용하지 않고 렌더링할지 여부를 나타내는 값을 가져오거나 설정합니다. true 로 설정하면 렌더링 출력에서 리가처가 비활성화됩니다. 기본적으로 이 속성은 false 로 설정됩니다.
type: docs
weight: 20
url: /ko/aspose.slides.export/renderingoptions/disablefontligatures/
---
## RenderingOptions.DisableFontLigatures 속성

텍스트를 리가처를 사용하지 않고 렌더링할지 여부를 나타내는 값을 가져오거나 설정합니다. `true` 로 설정하면 렌더링 출력에서 리가처가 비활성화됩니다. 기본적으로 이 속성은 `false` 로 설정됩니다.

```csharp
public bool DisableFontLigatures { get; set; }
```

### 예제

예시:

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    RenderingOptions options = new RenderingOptions
    {
        DisableFontLigatures = true // 텍스트 렌더링에서 리가처를 비활성화합니다
    };
    
    Bitmap[] renderedSlides = pres.GetImage(options);
    for (var index = 0; index < renderedSlides.Length; index++)
    {
        var slideImage = renderedSlides[index];
        slideImage.Save($"slide-{index}.png");
    }
}
```

### 참고

* 클래스 [RenderingOptions](../../renderingoptions)
* 네임스페이스 [Aspose.Slides.Export](../../renderingoptions)
* 어셈블리 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->