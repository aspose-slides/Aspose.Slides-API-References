---
title: DisableFontLigatures
second_title: .NET용 Aspose.Sildes API 레퍼런스
description: 텍스트를 리가처를 사용하지 않고 렌더링할지 여부를 나타내는 값을 가져오거나 설정합니다. true 로 설정하면 렌더링된 출력에서 리가처가 비활성화됩니다. 기본적으로 이 속성은 false 로 설정됩니다.
type: docs
weight: 30
url: /ko/aspose.slides.export/htmloptions/disablefontligatures/
---
## HtmlOptions.DisableFontLigatures 속성

텍스트를 리가처를 사용하지 않고 렌더링할지 여부를 나타내는 값을 가져오거나 설정합니다. `true` 로 설정하면 렌더링된 출력에서 리가처가 비활성화됩니다. 기본적으로 이 속성은 `false` 로 설정됩니다.

```csharp
public bool DisableFontLigatures { get; set; }
```

### 예제

Example:

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    HtmlOptions options = new HtmlOptions
    {
        DisableFontLigatures = true // 텍스트 렌더링에서 리가처 비활성화
    };
    
    pres.Save(outputSlidePath, SaveFormat.Html, options);
}
```

### 참조

* 클래스 [HtmlOptions](../../htmloptions)
* 네임스페이스 [Aspose.Slides.Export](../../htmloptions)
* 어셈블리 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->