---
title: DisableFontLigatures
second_title: Aspose.Sildes .NET API 레퍼런스
description: 텍스트를 결합 문자(ligatures)를 사용하지 않고 렌더링할지 여부를 나타내는 값을 가져오거나 설정합니다. `true` 로 설정하면 렌더링된 출력에서 결합 문자가 비활성화됩니다. 기본적으로 이 속성은 `false` 로 설정됩니다.
type: docs
weight: 40
url: /ko/aspose.slides.export/isvgoptions/disablefontligatures/
---
## ISVGOptions.DisableFontLigatures 속성

텍스트를 결합 문자(ligatures)를 사용하지 않고 렌더링할지 여부를 나타내는 값을 가져오거나 설정합니다. `true` 로 설정하면 렌더링된 출력에서 결합 문자가 비활성화됩니다. 기본값으로 이 속성은 `false` 로 설정됩니다.

```csharp
public bool DisableFontLigatures { get; set; }
```

### 예제

예:

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    SVGOptions options = new SVFOptions
    {
        DisableFontLigatures = true // 텍스트 렌더링에서 결합 문자를 비활성화합니다
    };
    
    using (FileStream fileStream = new FileStream("slide-0.svg", FileMode.Create, FileAccess.Write))
    {
        pres.Slides[0].WriteAsSvg(fileStream);   
    }
}
```

### 관련 항목

* 인터페이스 [ISVGOptions](../../isvgoptions)
* 네임스페이스 [Aspose.Slides.Export](../../isvgoptions)
* 어셈블리 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->