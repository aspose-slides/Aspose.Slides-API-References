---
title: GetCell
second_title: Aspose.Sildes .NET용 API 참조
description: 지정된 워크시트에서 인덱스와 셀 좌표를 사용하여 셀을 검색합니다.
type: docs
weight: 10
url: /ko/aspose.slides.excel/iexceldataworkbook/getcell/
---
## GetCell(int, int, int) {#getcell}

지정된 워크시트에서 인덱스와 셀 좌표를 사용하여 셀을 검색합니다.

```csharp
public IExcelDataCell GetCell(int worksheetIndex, int row, int column)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| worksheetIndex | Int32 | 워크시트의 0부터 시작하는 인덱스. |
| row | Int32 | 셀의 0부터 시작하는 행 인덱스. |
| column | Int32 | 셀의 0부터 시작하는 열 인덱스. |

### 반환값

지정된 위치의 셀.

### 예제

Example:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell(1, 1, 1);
Console.WriteLine(cell.Value.ToString());
```

### 참조

* 인터페이스 [IExcelDataCell](../../iexceldatacell)
* 인터페이스 [IExcelDataWorkbook](../../iexceldataworkbook)
* 네임스페이스 [Aspose.Slides.Excel](../../iexceldataworkbook)
* 어셈블리 [Aspose.Slides](../../../)

---

## GetCell(string, int, int) {#getcell_2}

지정된 워크시트에서 이름과 셀 좌표를 사용하여 셀을 검색합니다.

```csharp
public IExcelDataCell GetCell(string worksheetName, int row, int column)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| worksheetName | String | 워크시트의 이름. |
| row | Int32 | 셀의 0부터 시작하는 행 인덱스. |
| column | Int32 | 셀의 0부터 시작하는 열 인덱스. |

### 반환값

지정된 위치의 셀.

### 예제

Example:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell("Sheet1", 1, 1);
Console.WriteLine(cell.Value.ToString());
```

### 참조

* 인터페이스 [IExcelDataCell](../../iexceldatacell)
* 인터페이스 [IExcelDataWorkbook](../../iexceldataworkbook)
* 네임스페이스 [Aspose.Slides.Excel](../../iexceldataworkbook)
* 어셈블리 [Aspose.Slides](../../../)

---

## GetCell(int, string) {#getcell_1}

지정된 워크시트에서 인덱스와 Excel 스타일 셀 이름(예: "B2")을 사용하여 셀을 검색합니다.

```csharp
public IExcelDataCell GetCell(int worksheetIndex, string cellName)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| worksheetIndex | Int32 | 워크시트의 0부터 시작하는 인덱스. |
| cellName | String | Excel 스타일 셀 참조(예: "A1", "C5"). |

### 반환값

지정된 위치의 셀.

### 예제

Example:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell(1, "B2");
Console.WriteLine(cell.Value.ToString());
```

### 참조

* 인터페이스 [IExcelDataCell](../../iexceldatacell)
* 인터페이스 [IExcelDataWorkbook](../../iexceldataworkbook)
* 네임스페이스 [Aspose.Slides.Excel](../../iexceldataworkbook)
* 어셈블리 [Aspose.Slides](../../../)

---

## GetCell(string, string) {#getcell_3}

지정된 워크시트에서 Excel 스타일 셀 이름(예: "B2")을 사용하여 셀을 검색합니다.

```csharp
public IExcelDataCell GetCell(string worksheetName, string cellName)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| worksheetName | String | 워크시트의 이름. |
| cellName | String | Excel 스타일 셀 참조(예: "A1", "C5"). |

### 반환값

지정된 위치의 셀.

### 예제

Example:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell("Sheet1", "B2");
Console.WriteLine(cell.Value.ToString());
```

### 참조

* 인터페이스 [IExcelDataCell](../../iexceldatacell)
* 인터페이스 [IExcelDataWorkbook](../../iexceldataworkbook)
* 네임스페이스 [Aspose.Slides.Excel](../../iexceldataworkbook)
* 어셈블리 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->