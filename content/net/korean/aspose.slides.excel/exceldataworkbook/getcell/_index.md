---
title: GetCell
second_title: Aspose.Sildes for .NET API 참조
description: 지정된 워크시트에서 인덱스와 셀 좌표를 사용하여 셀을 가져옵니다.
type: docs
weight: 20
url: /ko/aspose.slides.excel/exceldataworkbook/getcell/
---
## GetCell(int, int, int) {#getcell}

지정된 워크시트에서 인덱스와 셀 좌표를 사용하여 셀을 가져옵니다.

```csharp
public IExcelDataCell GetCell(int worksheetIndex, int row, int column)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| worksheetIndex | Int32 | 워크시트의 0부터 시작하는 인덱스입니다. |
| row | Int32 | 셀의 0부터 시작하는 행 인덱스입니다. |
| column | Int32 | 셀의 0부터 시작하는 열 인덱스입니다. |

### 반환값

지정된 위치에 있는 셀입니다.

### 예제

예제:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell(1, 1, 1);
Console.WriteLine(cell.Value.ToString());
```

### 참고

* 인터페이스 [IExcelDataCell](../../iexceldatacell)
* 클래스 [ExcelDataWorkbook](../../exceldataworkbook)
* 네임스페이스 [Aspose.Slides.Excel](../../exceldataworkbook)
* 어셈블리 [Aspose.Slides](../../../)

---

## GetCell(string, int, int) {#getcell_2}

지정된 워크시트에서 이름과 셀 좌표를 사용하여 셀을 가져옵니다.

```csharp
public IExcelDataCell GetCell(string worksheetName, int row, int column)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| worksheetName | String | 워크시트의 이름입니다. |
| row | Int32 | 셀의 0부터 시작하는 행 인덱스입니다. |
| column | Int32 | 셀의 0부터 시작하는 열 인덱스입니다. |

### 반환값

지정된 위치에 있는 셀입니다.

### 예제

예제:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell("Sheet1", 1, 1);
Console.WriteLine(cell.Value.ToString());
```

### 참고

* 인터페이스 [IExcelDataCell](../../iexceldatacell)
* 클래스 [ExcelDataWorkbook](../../exceldataworkbook)
* 네임스페이스 [Aspose.Slides.Excel](../../exceldataworkbook)
* 어셈블리 [Aspose.Slides](../../../)

---

## GetCell(int, string) {#getcell_1}

지정된 워크시트에서 인덱스와 Excel 형식 셀 이름(예: "B2")을 사용하여 셀을 가져옵니다.

```csharp
public IExcelDataCell GetCell(int worksheetIndex, string cellName)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| worksheetIndex | Int32 | 워크시트의 0부터 시작하는 인덱스입니다. |
| cellName | String | Excel 형식 셀 참조(예: "A1", "C5")입니다. |

### 반환값

지정된 위치에 있는 셀입니다.

### 예제

예제:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell(1, "B2");
Console.WriteLine(cell.Value.ToString());
```

### 참고

* 인터페이스 [IExcelDataCell](../../iexceldatacell)
* 클래스 [ExcelDataWorkbook](../../exceldataworkbook)
* 네임스페이스 [Aspose.Slides.Excel](../../exceldataworkbook)
* 어셈블리 [Aspose.Slides](../../../)

---

## GetCell(string, string) {#getcell_3}

지정된 워크시트에서 Excel 형식 셀 이름(예: "B2")을 사용하여 셀을 가져옵니다.

```csharp
public IExcelDataCell GetCell(string worksheetName, string cellName)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| worksheetName | String | 워크시트의 이름입니다. |
| cellName | String | Excel 형식 셀 참조(예: "A1", "C5")입니다. |

### 반환값

지정된 위치에 있는 셀입니다.

### 예제

예제:

```csharp
[C#]
ExcelDataWorkbook wb = new ExcelDataWorkbook(testFile);
IExcelDataCell cell = wb.GetCell("Sheet1", "B2");
Console.WriteLine(cell.Value.ToString());
```

### 참고

* 인터페이스 [IExcelDataCell](../../iexceldatacell)
* 클래스 [ExcelDataWorkbook](../../exceldataworkbook)
* 네임스페이스 [Aspose.Slides.Excel](../../exceldataworkbook)
* 어셈블리 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->