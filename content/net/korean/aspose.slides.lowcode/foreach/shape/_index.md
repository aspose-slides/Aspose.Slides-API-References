---
title: Shape
second_title: Aspose.Sildes .NET API 참조
description: Presentationaspose.slides/presentation에서 각 Shapeaspose.slides.lowcode/foreach/shape을 반복합니다. 도형은 모든 유형의 슬라이드 - Slideaspose.slides.lowcode/foreach/slide, MasterSlideaspose.slides.lowcode/foreach/masterslide 및 LayoutSlideaspose.slides.lowcode/foreach/layoutslide에서 반복됩니다.
type: docs
weight: 50
url: /ko/aspose.slides.lowcode/foreach/shape/
---
## Shape(Presentation, ForEachShapeCallback) {#shape_2}

[`Presentation`](../../../aspose.slides/presentation)에서 각 `Shape`를 반복합니다. 도형은 모든 유형의 슬라이드 - [`Slide`](../slide), [`MasterSlide`](../masterslide) 및 [`LayoutSlide`](../layoutslide)에서 반복됩니다.

```csharp
public static void Shape(Presentation pres, ForEachShapeCallback forEachShape)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| pres | Presentation | 레이아웃 도형을 반복할 Presentation |
| forEachShape | ForEachShapeCallback | 각 도형에 대해 호출될 콜백 |

### 예제

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Shape(pres, (shape, slide, index) => 
    {
        System.Console.WriteLine($"{shape.Name}, index: {index}");
    });
} 
```

### 참고

* 클래스 [Presentation](../../../aspose.slides/presentation)
* 델리게이트 [ForEachShapeCallback](../../foreach.foreachshapecallback)
* 클래스 [ForEach](../../foreach)
* 네임스페이스 [Aspose.Slides.LowCode](../../foreach)
* 어셈블리 [Aspose.Slides](../../../)

---

## Shape(Presentation, bool, ForEachShapeCallback) {#shape_1}

[`Shape`](../shape)를 [`Presentation`](../../../aspose.slides/presentation)에서 반복합니다. 도형은 필요에 따라 모든 유형의 슬라이드 - [`Slide`](../slide), [`MasterSlide`](../masterslide), [`LayoutSlide`](../layoutslide) 및 [`NotesSlide`](../../../aspose.slides/notesslide)에서 반복됩니다.

```csharp
public static void Shape(Presentation pres, bool includeNotes, ForEachShapeCallback forEachShape)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| pres | Presentation | 레이아웃 도형을 반복할 Presentation |
| includeNotes | Boolean | NotesSlides를 처리에 포함할지 여부를 나타내는 플래그 |
| forEachShape | ForEachShapeCallback | 각 도형에 대해 호출될 콜백 |

### 예제

```csharp
using (Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Shape(pres, true, (shape, slide, index) => 
    {
        System.Console.WriteLine($"{shape.Name}, index: {index}");
    });
} 
```

### 참고

* 클래스 [Presentation](../../../aspose.slides/presentation)
* 델리게이트 [ForEachShapeCallback](../../foreach.foreachshapecallback)
* 클래스 [ForEach](../../foreach)
* 네임스페이스 [Aspose.Slides.LowCode](../../foreach)
* 어셈블리 [Aspose.Slides](../../../)

---

## Shape(BaseSlide, ForEachShapeCallback) {#shape}

[`Shape`](../shape)를 [`BaseSlide`](../../../aspose.slides/baseslide)에서 반복합니다. [`BaseSlide`](../../../aspose.slides/baseslide)는 [`Slide`](../slide), [`MasterSlide`](../masterslide) 및 [`LayoutSlide`](../layoutslide)의 기본 유형입니다.

```csharp
public static void Shape(BaseSlide baseSlide, ForEachShapeCallback forEachShape)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| baseSlide | BaseSlide | 레이아웃 도형을 반복할 슬라이드 |
| forEachShape | ForEachShapeCallback | 각 도형에 대해 호출될 콜백 |

### 예제

```csharp
(Presentation pres = new Presentation("pres.pptx"))
{
    ForEach.Slide(pres, (slide, index) =>
    {
        ForEach.Shape(slide, (shape, baseSlide, shapeIndex) =>
        {
            System.Console.WriteLine($"{shape.Name}, index: {shapeIndex}");
        });
    });
} 
```

### 참고

* 클래스 [BaseSlide](../../../aspose.slides/baseslide)
* 델리게이트 [ForEachShapeCallback](../../foreach.foreachshapecallback)
* 클래스 [ForEach](../../foreach)
* 네임스페이스 [Aspose.Slides.LowCode](../../foreach)
* 어셈블리 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->