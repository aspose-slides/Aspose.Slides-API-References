---
title: ToPng
second_title: Aspose.Sildes for .NET API 참조
description: 입력 프레젠테이션을 PNG 형식 이미지 집합으로 변환합니다. 출력 파일 이름을 myPath/myFilename.png 로 지정하면 결과는 myPath/myFilename_N.png 파일 집합으로 저장되며, N은 슬라이드 번호입니다.
type: docs
weight: 40
url: /ko/aspose.slides.lowcode/convert/topng/
---
## ToPng(Presentation, string) {#topng}

입력 프레젠테이션을 PNG 형식 이미지 집합으로 변환합니다. 출력 파일 이름을 "myPath/myFilename.png" 로 지정하면 결과는 "myPath/myFilename_N.png" 파일 집합으로 저장되며, N은 슬라이드 번호입니다.

```csharp
public static void ToPng(Presentation pres, string outputFileName)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| pres | Presentation | 입력 프레젠테이션. |
| outputFileName | String | 출력 파일 이름. |

### 예외

| 예외 | 조건 |
| --- | --- |
| ArgumentException |  |

### 예제

```csharp
using (var pres = new Presentation("pres.pptx"))
   Convert.ToPng(pres, "presImage.png");
```

### 참조

* 클래스 [Presentation](../../../aspose.slides/presentation)
* 클래스 [Convert](../../convert)
* 네임스페이스 [Aspose.Slides.LowCode](../../convert)
* 어셈블리 [Aspose.Slides](../../../)

---

## ToPng(Presentation, string, Size) {#topng_2}

입력 프레젠테이션을 PNG 형식 이미지 집합으로 변환합니다. 출력 파일 이름을 "myPath/myFilename.png" 로 지정하면 결과는 "myPath/myFilename_N.png" 파일 집합으로 저장되며, N은 슬라이드 번호입니다.

```csharp
public static void ToPng(Presentation pres, string outputFileName, Size imageSize)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| pres | Presentation | 입력 프레젠테이션 |
| outputFileName | String | 출력 파일 이름. |
| imageSize | Size | 생성된 각 이미지의 크기. |

### 예외

| 예외 | 조건 |
| --- | --- |
| ArgumentException |  |

### 예제

```csharp
using (var pres = new Presentation("pres.pptx"))
    Convert.ToPng(pres, "presImage.png", new Size(720, 540));
```

### 참조

* 클래스 [Presentation](../../../aspose.slides/presentation)
* 클래스 [Convert](../../convert)
* 네임스페이스 [Aspose.Slides.LowCode](../../convert)
* 어셈블리 [Aspose.Slides](../../../)

---

## ToPng(Presentation, string, float, IRenderingOptions) {#topng_1}

입력 프레젠테이션을 PNG 형식 이미지 집합으로 변환합니다. 출력 파일 이름을 "myPath/myFilename.png" 로 지정하면 결과는 "myPath/myFilename_N.png" 파일 집합으로 저장되며, N은 슬라이드 번호입니다.

```csharp
public static void ToPng(Presentation pres, string outputFileName, float scale, 
    IRenderingOptions options)
```

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| pres | Presentation | 입력 프레젠테이션. |
| outputFileName | String | 출력 파일 이름. |
| scale | Single | 원본 슬라이드 크기에 대한 출력 이미지에 적용되는 스케일링 팩터. |
| options | IRenderingOptions | 렌더링 옵션. |

### 예외

| 예외 | 조건 |
| --- | --- |
| ArgumentException |  |

### 예제

```csharp
IRenderingOptions options = new RenderingOptions()
{
    SlidesLayoutOptions = new NotesCommentsLayoutingOptions()
    {
        NotesPosition = NotesPositions.BottomTruncated
    }
};

using (var pres = new Presentation("pres.pptx"))
   Convert.ToPng(pres, "presImage.png", 2f, options);
```

### 참조

* 클래스 [Presentation](../../../aspose.slides/presentation)
* 인터페이스 [IRenderingOptions](../../../aspose.slides.export/irenderingoptions)
* 클래스 [Convert](../../convert)
* 네임스페이스 [Aspose.Slides.LowCode](../../convert)
* 어셈블리 [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->