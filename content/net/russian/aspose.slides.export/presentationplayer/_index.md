---
title: PresentationPlayer
second_title: Aspose.Sildes для .NET API справочник
description: Представляет проигрыватель анимаций, связанный с Presentation../aspose.slides/presentation.
type: docs
weight: 4390
url: /ru/aspose.slides.export/presentationplayer/
---
## PresentationPlayer класс

Представляет проигрыватель анимаций, связанный с [`Presentation`](../../aspose.slides/presentation).

```csharp
public class PresentationPlayer : IDisposable
```

## Конструкторы

| Имя | Описание |
| --- | --- |
| [PresentationPlayer](presentationplayer)(PresentationAnimationsGenerator, double) | Создает новый экземпляр [`PresentationPlayer`](../presentationplayer). |

## Свойства

| Имя | Описание |
| --- | --- |
| [FrameIndex](../../aspose.slides.export/presentationplayer/frameindex) { get; } | Возвращает индекс кадра. |

## Методы

| Имя | Описание |
| --- | --- |
| [Dispose](../../aspose.slides.export/presentationplayer/dispose)() | Освобождает экземпляр [`PresentationPlayer`](../presentationplayer). |

## Другие члены

| Имя | Описание |
| --- | --- |
| delegate [FrameTickHandler](presentationplayer.frametickhandler) | Представляет обработчик тактов кадра события [`FrameTick`](./frametick). |

### Примеры

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(pres))
    {
        // Воспроизвести анимацию с 33 FPS
        using (var player = new PresentationPlayer(animationsGenerator, 33))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("33fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
        
        // Воспроизвести анимацию с 45 FPS
        using (var player = new PresentationPlayer(animationsGenerator, 45))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("45fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
    }
}
```

### См. также

* пространство имён [Aspose.Slides.Export](../../aspose.slides.export)
* сборка [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->