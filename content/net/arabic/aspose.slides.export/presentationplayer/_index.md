---
title: PresentationPlayer
second_title: مرجع API لـ Aspose.Sildes للـ .NET
description: يمثل مشغل الرسوم المتحركة المرتبطة بـ Presentation../aspose.slides/presentation.
type: docs
weight: 4390
url: /ar/aspose.slides.export/presentationplayer/
---
## PresentationPlayer فئة

يمثل مشغل الرسوم المتحركة المرتبطة بـ [`Presentation`](../../aspose.slides/presentation).

```csharp
public class PresentationPlayer : IDisposable
```

## المنشئات

| الاسم | الوصف |
| --- | --- |
| [PresentationPlayer](presentationplayer)(PresentationAnimationsGenerator, double) | ينشئ نسخة جديدة من [`PresentationPlayer`](../presentationplayer). |

## الخصائص

| الاسم | الوصف |
| --- | --- |
| [FrameIndex](../../aspose.slides.export/presentationplayer/frameindex) { get; } | يسترجع فهرس الإطار. |

## الطرق

| الاسم | الوصف |
| --- | --- |
| [Dispose](../../aspose.slides.export/presentationplayer/dispose)() | يتخلص من المثيل للـ [`PresentationPlayer`](../presentationplayer). |

## الأعضاء الأخرى

| الاسم | الوصف |
| --- | --- |
| delegate [FrameTickHandler](presentationplayer.frametickhandler) | يمثل معالج إشارة الإطار لحدث [`FrameTick`](./frametick). |

### أمثلة

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(pres))
    {
        // تشغيل الرسوم المتحركة بمعدل 33 إطار في الثانية
        using (var player = new PresentationPlayer(animationsGenerator, 33))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("33fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
        
        // تشغيل الرسوم المتحركة بمعدل 45 إطار في الثانية
        using (var player = new PresentationPlayer(animationsGenerator, 45))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("45fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
    }
}
```

### انظر أيضًا

* نطاق [Aspose.Slides.Export](../../aspose.slides.export)
* تجميع [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->