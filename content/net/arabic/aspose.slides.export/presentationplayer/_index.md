---
title: PresentationPlayer
second_title: Aspose.Sildes لـ .NET مرجع API
description: يمثل مشغل الرسوم المتحركة المرتبطة بالـ Presentation../aspose.slides/presentation.
type: docs
weight: 4390
url: /ar/aspose.slides.export/presentationplayer/
---
## PresentationPlayer فئة

يمثل مشغل الرسوم المتحركة المرتبطة بـ [`Presentation`](../../aspose.slides/presentation).

```csharp
public class PresentationPlayer : IDisposable
```

## المُنشئات

| الاسم | الوصف |
| --- | --- |
| [PresentationPlayer](presentationplayer)(PresentationAnimationsGenerator, double) | إنشاء نسخة جديدة من [`PresentationPlayer`](../presentationplayer). |

## الخصائص

| الاسم | الوصف |
| --- | --- |
| [FrameIndex](../../aspose.slides.export/presentationplayer/frameindex) { get; } | يحصل على فهرس الإطار. |

## الطرق

| الاسم | الوصف |
| --- | --- |
| [Dispose](../../aspose.slides.export/presentationplayer/dispose)() | يقوم بتحرير النسخة من [`PresentationPlayer`](../presentationplayer). |

## الأعضاء الأخرى

| الاسم | الوصف |
| --- | --- |
| delegate [FrameTickHandler](presentationplayer.frametickhandler) | يمثل معالج علامة الإطار لحدث [`FrameTick`](./frametick). |

### أمثلة

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(pres))
    {
        // تشغيل الرسوم المتحركة بـ 33 إطارًا في الثانية
        using (var player = new PresentationPlayer(animationsGenerator, 33))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("33fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
        
        // تشغيل الرسوم المتحركة بـ 45 إطارًا في الثانية
        using (var player = new PresentationPlayer(animationsGenerator, 45))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("45fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
    }
}
```

### انظر أيضًا

* نطاق الاسم [Aspose.Slides.Export](../../aspose.slides.export)
* تجميع [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->