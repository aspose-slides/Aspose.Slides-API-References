---
title: DigitalSignature
second_title: مرجع API لـ Aspose.Sildes لـ .NET
description: التوقيع الرقمي في الملف الموقع.
type: docs
weight: 2770
url: /ar/aspose.slides/digitalsignature/
---
## فئة DigitalSignature

توقيع رقمي في الملف الموقّع.

```csharp
public class DigitalSignature : IDigitalSignature
```

## المنشئات

| الاسم | الوصف |
| --- | --- |
| [DigitalSignature](digitalsignature#constructor)(X509Certificate2) | ينشئ كائن DigitalSignature جديد باستخدام الشهادة المحددة. |
| [DigitalSignature](digitalsignature#constructor_1)(string, string) | ينشئ كائن DigitalSignature جديد باستخدام مسار ملف الشهادة المحدد وكلمة المرور. |

## الخصائص

| الاسم | الوصف |
| --- | --- |
| [Certificate](../../aspose.slides/digitalsignature/certificate) { get; } | كائن الشهادة الذي استُخدم لتوقيع المستند. قراءة فقط X509Certificate2. |
| [Comments](../../aspose.slides/digitalsignature/comments) { get; set; } | غرض التوقيع. قراءة/كتابة String. |
| [IsValid](../../aspose.slides/digitalsignature/isvalid) { get; } | إذا كان هذا التوقيع الرقمي صالحًا ولم يتم تعديل المستند، ستكون هذه القيمة true. قراءة فقط Boolean. |
| [SignTime](../../aspose.slides/digitalsignature/signtime) { get; } | الوقت الذي تم فيه توقيع المستند. قراءة فقط DateTime. |

### أمثلة

يوضح المثال التالي كيفية إضافة توقيع رقمي من شهادة PFX في عرض PowerPoint.

```csharp
[C#]
// تهيئة كائن Presentation
using (Presentation pres = new Presentation())
{
    // إنشاء كائن DigitalSignature باستخدام ملف PFX وكلمة مرور PFX
    DigitalSignature signature = new DigitalSignature("testsignature1.pfx", @"testpass1");
    // التعليق على التوقيع الرقمي الجديد
    signature.Comments = "Aspose.Slides digital signing test.";
    // إضافة توقيع رقمي إلى العرض التقديمي
    pres.DigitalSignatures.Add(signature);
    // حفظ العرض التقديمي
    pres.Save("SomePresentationSigned.pptx", SaveFormat.Pptx);
}
```

يوضح شفرة العينة التالية كيفية التحقق من صحة التوقيع الرقمي لعرض PowerPoint.

```csharp
[C#]
// تهيئة كائن Presentation
using (Presentation pres = new Presentation("SomePresentationSigned.pptx"))
{
    if (pres.DigitalSignatures.Count < 0)
    {
        bool allSignaturesAreValid = true;
        Console.WriteLine("Signatures used to sign the presentation: ");
        // التحقق مما إذا كانت جميع التوقيعات الرقمية صالحة
        foreach (DigitalSignature signature in pres.DigitalSignatures)
        {
            Console.WriteLine(signature.Certificate.SubjectName.Name + ", "
                    + signature.SignTime.ToString("yyyy-MM-dd HH:mm") + " -- " + (signature.IsValid ? "VALID" : "INVALID"));
            allSignaturesAreValid &= signature.IsValid;
        }
        if (allSignaturesAreValid)
            Console.WriteLine("Presentation is genuine, all signatures are valid.");
        else
            Console.WriteLine("Presentation has been modified since signing.");
    }
}
```

### راجع أيضًا

* واجهة [IDigitalSignature](../idigitalsignature)
* مساحة الاسم [Aspose.Slides](../../aspose.slides)
* تجميع [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->