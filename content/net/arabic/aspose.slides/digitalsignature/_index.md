---
title: DigitalSignature
second_title: Aspose.Sildes لـ .NET مرجع API
description: التوقيع الرقمي في ملف موقّع.
type: docs
weight: 2770
url: /ar/aspose.slides/digitalsignature/
---
## فئة DigitalSignature

التوقيع الرقمي في ملف موقّع.

```csharp
public class DigitalSignature : IDigitalSignature
```

## المنشئات

| الاسم | الوصف |
| --- | --- |
| [DigitalSignature](digitalsignature#constructor)(X509Certificate2) | ينشئ كائن DigitalSignature جديدًا باستخدام الشهادة المحددة. |
| [DigitalSignature](digitalsignature#constructor_1)(string, string) | ينشئ كائن DigitalSignature جديدًا باستخدام مسار ملف الشهادة المحدد وكلمة المرور. |

## الخصائص

| الاسم | الوصف |
| --- | --- |
| [Certificate](../../aspose.slides/digitalsignature/certificate) { get; } | كائن الشهادة الذي تم استخدامه لتوقيع المستند. للقراءة فقط X509Certificate2. |
| [Comments](../../aspose.slides/digitalsignature/comments) { get; set; } | غرض التوقيع. للقراءة والكتابة String. |
| [IsValid](../../aspose.slides/digitalsignature/isvalid) { get; } | إذا كان هذا التوقيع الرقمي صالحًا ولم يتم العبث بالمستند، ستكون القيمة true. للقراءة فقط Boolean. |
| [SignTime](../../aspose.slides/digitalsignature/signtime) { get; } | الوقت الذي تم فيه توقيع المستند. للقراءة فقط DateTime. |

### أمثلة

يوضح المثال التالي كيفية إضافة توقيع رقمي من شهادة PFX في عرض PowerPoint.

```csharp
[C#]
// تهيئة كائن Presentation
using (Presentation pres = new Presentation())
{
    // إنشاء كائن DigitalSignature باستخدام ملف PFX وكلمة مرور PFX
    DigitalSignature signature = new DigitalSignature("testsignature1.pfx", @"testpass1");
    // إضافة تعليق للتوقيع الرقمي الجديد
    signature.Comments = "Aspose.Slides digital signing test.";
    // إضافة التوقيع الرقمي إلى العرض التقديمي
    pres.DigitalSignatures.Add(signature);
    // حفظ العرض التقديمي
    pres.Save("SomePresentationSigned.pptx", SaveFormat.Pptx);
}
```

يوضح رمز العينة التالي كيفية التحقق من صحة التوقيع الرقمي لعرض PowerPoint.

```csharp
[C#]
// تهيئة كائن Presentation
using (Presentation pres = new Presentation("SomePresentationSigned.pptx"))
{
    if (pres.DigitalSignatures.Count < 0)
    {
        bool allSignaturesAreValid = true;
        Console.WriteLine("Signatures used to sign the presentation: ");
        // تحقق مما إذا كانت جميع التوقيعات الرقمية صالحة
        foreach (DigitalSignature signature in pres.DigitalSignatures)
        {
            Console.WriteLine(signature.Certificate.SubjectName.Name + ", "
                    + signature.SignTime.ToString("yyyy-MM-dd HH:mm") + " -- " + (signature.IsValid ? "VALID" : "INVALID"));
            allSignaturesAreValid &= signature.IsValid;
        }
        if (allSignaturesAreValid)
            Console.WriteLine("Presentation is genuine, all signatures are valid.");
        else
            Console.WriteLine("Presentation has been modified since signing.");
    }
}
```

### انظر أيضًا

* الواجهة [IDigitalSignature](../idigitalsignature)
* نطاق الاسم [Aspose.Slides](../../aspose.slides)
* التجميع [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->