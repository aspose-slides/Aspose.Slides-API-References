---
title: CompressImage
second_title: Aspose.Sildes .NET مرجع API
description: يقوم بضغط الصورة عن طريق تقليل حجمها بناءً على حجم الشكل والدقة المحددة. اختيارياً، يقوم أيضاً بحذف المناطق المقصوصة.
type: docs
weight: 190
url: /ar/aspose.slides/ipicturefillformat/compressimage/
---
## CompressImage(bool, PicturesCompression) {#compressimage}

يقوم بضغط الصورة عن طريق تقليل حجمها بناءً على حجم الشكل والدقة المحددة. اختيارياً، يقوم أيضاً بحذف المناطق المقصوصة.

```csharp
public bool CompressImage(bool deleteCroppedAreasOfImage, PicturesCompression resolution)
```

| المعلمة | النوع | الوصف |
| --- | --- | --- |
| deleteCroppedAreasOfImage | Boolean | إذا كان true، ستقوم الطريقة بإزالة المناطق المقصوصة من الصورة، مما قد يقلل حجمها أكثر. |
| resolution | PicturesCompression | الدقة المستهدفة للضغط، المحددة كقيمة من تعداد [`PicturesCompression`](../../../aspose.slides.export/picturescompression). |

### قيمة الإرجاع

قيمة Boolean تشير إلى ما إذا تم ضغط الصورة بنجاح. تُرجع `true` إذا تم تعديل حجم الصورة أو قصها، وإلا `false`.

### الاستثناءات

| الاستثناء | الشرط |
| --- | --- |
| ArgumentException | يُرمى عندما لا تكون الدقة قيمة صالحة. |

### ملاحظات

تغيّر هذه الطريقة حجم الصورة ودقتها بطريقة مشابهة لميزة PowerPoint "Picture Format -> Compress Pictures".

### أمثلة

يعرض المثال التالي كيفية استخدام الطريقة `CompressImage` لتقليل حجم صورة في عرض تقديمي عن طريق تحديد دقة مستهدفة وإزالة المناطق المقصوصة:

```csharp
using (Presentation presentation = new Presentation("demo.pptx"))
{
    ISlide slide = presentation.Slides[0];
    IPictureFrame picFrame = slide.Shapes[0] as IPictureFrame;
    // ضغط الصورة بدقة مستهدفة 150 DPI (دقة الويب) وإزالة المناطق المقصوصة
    bool result = picFrame.PictureFormat.CompressImage(true, PicturesCompression.Dpi150);
}
```

### انظر أيضًا

* enum [PicturesCompression](../../../aspose.slides.export/picturescompression)
* واجهة [IPictureFillFormat](../../ipicturefillformat)
* مساحة أسماء [Aspose.Slides](../../ipicturefillformat)
* تجميع [Aspose.Slides](../../../)

---

## CompressImage(bool, float) {#compressimage_1}

يقوم بضغط الصورة عن طريق تقليل حجمها بناءً على حجم الشكل والدقة المحددة. اختيارياً، يقوم أيضاً بحذف المناطق المقصوصة.

```csharp
public bool CompressImage(bool deleteCroppedAreasOfImage, float resolution)
```

| المعلمة | النوع | الوصف |
| --- | --- | --- |
| deleteCroppedAreasOfImage | Boolean | إذا كان true، ستقوم الطريقة بإزالة المناطق المقصوصة من الصورة، مما قد يقلل حجمها أكثر. |
| resolution | Single | الدقة المستهدفة بوحدة DPI. يجب أن تكون هذه القيمة موجبة وتحدد كيفية تعديل حجم الصورة. |

### قيمة الإرجاع

قيمة Boolean تشير إلى ما إذا تم ضغط الصورة بنجاح. تُرجع `true` إذا تم تعديل حجم الصورة أو قصها، وإلا `false`.

### الاستثناءات

| الاستثناء | الشرط |
| --- | --- |
| ArgumentException | يُرمى عندما لا تكون الدقة قيمة موجبة. |

### ملاحظات

تغيّر هذه الطريقة حجم الصورة ودقتها بطريقة مشابهة لميزة PowerPoint "Picture Format -> Compress Pictures".

### أمثلة

يعرض المثال التالي كيفية استخدام الطريقة `CompressImage` لتقليل حجم صورة في عرض تقديمي عن طريق تحديد دقة مستهدفة وإزالة المناطق المقصوصة:

```csharp
[C#]
using (Presentation presentation = new Presentation("demo.pptx"))
{
    ISlide slide = presentation.Slides[0];

    // الحصول على PictureFrame
    IPictureFrame picFrame = slide.Shapes[0] as IPictureFrame;

    // ضغط الصورة بدقة مستهدفة 150 DPI (دقة الويب) وإزالة المناطق المقصوصة
    bool result=picFrame.PictureFormat.CompressImage(true, 150f); // دقة الويب
}
```

### انظر أيضًا

* واجهة [IPictureFillFormat](../../ipicturefillformat)
* مساحة أسماء [Aspose.Slides](../../ipicturefillformat)
* تجميع [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->