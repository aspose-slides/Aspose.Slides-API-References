---
title: RewindAudio
second_title: Aspose.Sildes لـ .NET مرجع API
description: يحدد ما إذا كان الصوت يتم إعادته تلقائيًا إلى البداية بعد التشغيل. قراءة/كتابة Boolean.
type: docs
weight: 150
url: /ar/aspose.slides/audioframe/rewindaudio/
---
## خاصية AudioFrame.RewindAudio

يحدد ما إذا كان سيتم إعادة المقاطع الصوتية تلقائيًا إلى البدء بعد التشغيل. قراءة/كتابة Boolean.

```csharp
public bool RewindAudio { get; set; }
```

### أمثلة

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    ISlide slide = pres.Slides[0];

    // إضافة إطار صوتي
    IAudioFrame audioFrame = slide.Shapes.AddAudioFrameLinked(50, 50, 100, 100, "sampleaudio.wav");

    // ضبط الصوت للتشغيل عبر الشرائح
    audioFrame.PlayAcrossSlides = true;

    // ضبط الصوت لإعادة التشغيل تلقائيًا إلى البداية بعد التشغيل
    audioFrame.RewindAudio = true;

    pres.Save("AudioFrame_out.pptx", SaveFormat.Pptx);
}
```

### انظر أيضًا

* فئة [AudioFrame](../../audioframe)
* مساحة الأسماء [Aspose.Slides](../../audioframe)
* تجميع [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->