---
title: AddMathShape
second_title: Aspose.Sildes لـ .NET مرجع API
description: ينشئ شكلاً تلقائيًا مستطيلًا جديدًا لاستضافة المحتوى الرياضي ويضيفه إلى نهاية مجموعة الأشكال.
type: docs
weight: 140
url: /ar/aspose.slides/shapecollection/addmathshape/
---
## ShapeCollection.AddMathShape طريقة

ينشئ شكلاً تلقائيًا مستطيلًا جديدًا لاستضافة المحتوى الرياضي ويضيفه إلى نهاية مجموعة الأشكال.

```csharp
public IAutoShape AddMathShape(float x, float y, float width, float height)
```

| معامل | نوع | وصف |
| --- | --- | --- |
| x | Single | الإحداثي السيني لإطار الشكل، بوحدات النقاط. |
| y | Single | الإحداثي الصادي لإطار الشكل، بوحدات النقاط. |
| width | Single | عرض إطار الشكل، بوحدات النقاط. |
| height | Single | ارتفاع إطار الشكل، بوحدات النقاط. |

### قيمة الإرجاع

العنصر المُنشأ حديثًا [`IAutoShape`](../../iautoshape).

### أمثلة

يوضح المثال التالي كيفية إضافة معادلة رياضية في عرض تقديمي لبرنامج PowerPoint.

```csharp
[C#]
using (Presentation pres = new Presentation())
{
   IAutoShape mathShape = pres.Slides[0].Shapes.AddMathShape(0, 0, 720, 150);
   var mathParagraph = (mathShape.TextFrame.Paragraphs[0].Portions[0] as MathPortion).MathParagraph;
   var fraction = new MathematicalText("x").Divide("y");
   mathParagraph.Add(new MathBlock(fraction));
   var mathBlock = new MathematicalText("c")
        .SetSuperscript("2")
        .Join("=")
        .Join(new MathematicalText("a").SetSuperscript("2"))
        .Join("+")
        .Join(new MathematicalText("b").SetSuperscript("2"));
    mathParagraph.Add(mathBlock);
    pres.Save("math.pptx", SaveFormat.Pptx);
}
```

### انظر أيضًا

* الواجهة [IAutoShape](../../iautoshape)
* الفئة [ShapeCollection](../../shapecollection)
* النطاق [Aspose.Slides](../../shapecollection)
* مجموعة التجميع [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->