---
title: AddConnector
second_title: Aspose.Sildes لـ .NET مرجع API
description: ينشئ شكلاً موصولاً جديدًا باستخدام نمط القالب الافتراضي ويضيفه إلى نهاية مجموعة الأشكال.
type: docs
weight: 120
url: /ar/aspose.slides/shapecollection/addconnector/
---
## AddConnector(ShapeType, float, float, float, float) {#addconnector}

ينشئ شكلاً موصولاً جديدًا باستخدام نمط القالب الافتراضي ويضيفه إلى نهاية مجموعة الأشكال.

```csharp
public IConnector AddConnector(ShapeType shapeType, float x, float y, float width, float height)
```

| المعامل | النوع | الوصف |
| --- | --- | --- |
| shapeType | ShapeType | The [`ShapeType`](../../shapetype) of the connector shape to add. |
| x | Single | الإحداثي السيني لإطار الموصل، بالوحدات النقطية. |
| y | Single | الإحداثي الصادي لإطار الموصل، بالوحدات النقطية. |
| width | Single | عرض إطار الموصل، بالوحدات النقطية. |
| height | Single | ارتفاع إطار الموصل، بالوحدات النقطية. |

### قيمة الإرجاع

العنصر [`IConnector`](../../iconnector) الذي تم إنشاؤه حديثًا.

### أمثلة

يوضح المثال التالي كيفية إضافة موصل (موصل معقوف) بين شكلين (بيضاوي ومستطيل) في PowerPoint Presentation.

```csharp
[C#]
// ينشئ فئة عرض تمثل ملف PPTX
using (Presentation input = new Presentation())
{
    // الوصول إلى مجموعة الأشكال لشريحة محددة
    IShapeCollection shapes = input.Slides[0].Shapes;
    // إضافة شكل بيضاوي تلقائي
    IAutoShape ellipse = shapes.AddAutoShape(ShapeType.Ellipse, 0, 100, 100, 100);
    // إضافة شكل مستطيل تلقائي
    IAutoShape rectangle = shapes.AddAutoShape(ShapeType.Rectangle, 100, 300, 100, 100);
    // إضافة شكل موصل إلى مجموعة أشكال الشريحة
    IConnector connector = shapes.AddConnector(ShapeType.BentConnector2, 0, 0, 10, 10);
    // ربط الأشكال باستخدام الموصل
    connector.StartShapeConnectedTo = ellipse;
    connector.EndShapeConnectedTo = rectangle;
    // استدعاء reroute الذي يحدد أقصر مسار تلقائي بين الأشكال
    connector.Reroute();
    // حفظ العرض
    input.Save("Shapes-connector.pptx", SaveFormat.Pptx);
}
```

### انظر أيضًا

* الواجهة [IConnector](../../iconnector)
* تعداد [ShapeType](../../shapetype)
* فئة [ShapeCollection](../../shapecollection)
* نطاق الاسم [Aspose.Slides](../../shapecollection)
* تجميع [Aspose.Slides](../../../)

---

## AddConnector(ShapeType, float, float, float, float, bool) {#addconnector_1}

ينشئ شكلاً موصولاً جديدًا ويضيفه إلى نهاية مجموعة الأشكال، مع إمكانية تطبيق نمط القالب الافتراضي.

```csharp
public IConnector AddConnector(ShapeType shapeType, float x, float y, float width, float height, 
    bool createFromTemplate)
```

| المعامل | النوع | الوصف |
| --- | --- | --- |
| shapeType | ShapeType | The [`ShapeType`](../../shapetype) of the connector shape to create. |
| x | Single | الإحداثي السيني لإطار الموصل، بالوحدات النقطية. |
| y | Single | الإحداثي الصادي لإطار الموصل، بالوحدات النقطية. |
| width | Single | عرض إطار الموصل، بالوحدات النقطية. |
| height | Single | ارتفاع إطار الموصل، بالوحدات النقطية. |
| createFromTemplate | Boolean | True to apply default template styling (non-empty name, simple style); false to create the connector with default property values. |

### قيمة الإرجاع

العنصر [`IConnector`](../../iconnector) الذي تم إنشاؤه حديثًا.

### انظر أيضًا

* الواجهة [IConnector](../../iconnector)
* تعداد [ShapeType](../../shapetype)
* فئة [ShapeCollection](../../shapecollection)
* نطاق الاسم [Aspose.Slides](../../shapecollection)
* تجميع [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->