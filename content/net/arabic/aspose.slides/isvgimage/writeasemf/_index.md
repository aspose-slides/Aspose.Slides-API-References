---
title: WriteAsEmf
second_title: Aspose.Sildes لـ .NET مرجع API
description: يحفظ صورة SVG كملف EMF.
type: docs
weight: 50
url: /ar/aspose.slides/isvgimage/writeasemf/
---
## ISvgImage.WriteAsEmf طريقة

يحفظ صورة SVG كملف EMF.

```csharp
public void WriteAsEmf(Stream stream)
```

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | Stream | تيار الهدف |

### الاستثناءات

| استثناء | الشرط |
| --- | --- |
| ArgumentNullException | تيار الهدف هو **null** |

### أمثلة

المثال التالي يوضح كيفية حفظ صورة SVG في ملف ميتا.

```csharp
[C#]
// ينشئ صورة SVG جديدة
ISvgImage svgImage = new SvgImage(System.IO.File.ReadAllText("content.svg"));

// يحفظ صورة SVG كملف ميتا
using (var fileStream = System.IO.File.OpenWrite("SvgAsEmf.emf"))
    svgImage.WriteAsEmf(fileStream);
```

هذا العينة توضح كيفية إضافة صورة SVG كملف ميتا إلى مجموعة صور العرض.

```csharp
[C#]
using (Presentation pres = new Presentation())
{
    // ينشئ صورة SVG جديدة
    ISvgImage svgImage = new SvgImage(System.IO.File.ReadAllText("content.svg"));
    using (var memStream = new MemoryStream())
    {
        // يحفظ صورة SVG كملف ميتا
        svgImage.WriteAsEmf(memStream);
        // يضيف ملف الميتا إلى مجموعة الصور
        pres.Images.AddImage(memStream.ToArray());
    }
}
```

### راجع أيضًا

* واجهة [ISvgImage](../../isvgimage)
* مساحة اسم [Aspose.Slides](../../isvgimage)
* مجمع [Aspose.Slides](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->