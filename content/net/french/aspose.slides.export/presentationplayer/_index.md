---
title: PresentationPlayer
second_title: Référence API Aspose.Slides pour .NET
description: Représente le joueur d'animations associé à la Presentation../aspose.slides/presentation.
type: docs
weight: 4200
url: /fr/aspose.slides.export/presentationplayer/
---

## Classe PresentationPlayer

Représente le joueur d'animations associé à la [`Presentation`](../../aspose.slides/presentation).

```csharp
public class PresentationPlayer : IDisposable
```

## Constructeurs

| Nom | Description |
| --- | --- |
| [PresentationPlayer](presentationplayer)(PresentationAnimationsGenerator, double) | Crée une nouvelle instance de la [`PresentationPlayer`](../presentationplayer). |

## Propriétés

| Nom | Description |
| --- | --- |
| [FrameIndex](../../aspose.slides.export/presentationplayer/frameindex) { get; } | Obtient l'index de la trame. |

## Méthodes

| Nom | Description |
| --- | --- |
| [Dispose](../../aspose.slides.export/presentationplayer/dispose)() | Dispose de l'instance de la [`PresentationPlayer`](../presentationplayer). |

## Autres membres

| Nom | Description |
| --- | --- |
| delegate [FrameTickHandler](presentationplayer.frametickhandler) |  |

### Exemples

```csharp
[C#]
using (Presentation pres = new Presentation("pres.pptx"))
{
    using (var animationsGenerator = new PresentationAnimationsGenerator(pres))
    {
        // Jouer l'animation à 33 FPS
        using (var player = new PresentationPlayer(animationsGenerator, 33))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("33fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
        
        // Jouer l'animation à 45 FPS
        using (var player = new PresentationPlayer(animationsGenerator, 45))
        {
            player.FrameTick += (sender, args) =>
            {
                args.GetFrame().Save(Path.Combine("45fps", $"frame_{sender.FrameIndex}.png"));
            };

            animationsGenerator.Run(pres.Slides);
        }
    }
}
```

### Voir aussi

* namespace [Aspose.Slides.Export](../../aspose.slides.export)
* assembly [Aspose.Slides](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Slides.dll -->