---
title: add_data_point_for_scatter_series method
second_title: Aspose.Slides para Python via .NET Referência da API
description: 
type: docs
url: /pt/aspose.slides.charts/ichartdatapointcollection/add_data_point_for_scatter_series/
weight: 120
---
## add_data_point_for_scatter_series(self, x_value, y_value) {#ichartdatacell-ichartdatacell}
Cria o novo ponto de dados e o adiciona ao final da coleção.
            Aplicável a séries cujo chartType é um dos subtipos Scatter (veja também o método ChartTypeCharacterizer.IsChartTypeScatter(ChartType)).

### Retorna

Novo ponto de dados.

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| Parâmetro | Tipo | Descrição |
| :- | :- | :- |
| x_value | [`IChartDataCell`](/slides/python-net/pt/aspose.slides.charts/ichartdatacell) | Ponto de dados XValue |
| y_value | [`IChartDataCell`](/slides/python-net/pt/aspose.slides.charts/ichartdatacell) | Ponto de dados YValue |

## add_data_point_for_scatter_series(self, x_value, y_value) {#float-ichartdatacell}
Cria o novo ponto de dados e o adiciona ao final da coleção.
            Aplicável a séries cujo chartType é um dos subtipos Scatter (veja também o método ChartTypeCharacterizer.IsChartTypeScatter(ChartType)).

### Retorna

Novo ponto de dados.

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| Parâmetro | Tipo | Descrição |
| :- | :- | :- |
| x_value | **float** | Ponto de dados XValue |
| y_value | [`IChartDataCell`](/slides/python-net/pt/aspose.slides.charts/ichartdatacell) | Ponto de dados YValue |

## add_data_point_for_scatter_series(self, x_value, y_value) {#str-ichartdatacell}
Cria o novo ponto de dados e o adiciona ao final da coleção.
            Aplicável a séries cujo chartType é um dos subtipos Scatter (veja também o método ChartTypeCharacterizer.IsChartTypeScatter(ChartType)).

### Retorna

Novo ponto de dados.

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| Parâmetro | Tipo | Descrição |
| :- | :- | :- |
| x_value | **str** | Ponto de dados XValue |
| y_value | [`IChartDataCell`](/slides/python-net/pt/aspose.slides.charts/ichartdatacell) | Ponto de dados YValue |

## add_data_point_for_scatter_series(self, x_value, y_value) {#ichartdatacell-float}
Cria o novo ponto de dados e o adiciona ao final da coleção.
            Aplicável a séries cujo chartType é um dos subtipos Scatter (veja também o método ChartTypeCharacterizer.IsChartTypeScatter(ChartType)).

### Retorna

Novo ponto de dados.

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| Parâmetro | Tipo | Descrição |
| :- | :- | :- |
| x_value | [`IChartDataCell`](/slides/python-net/pt/aspose.slides.charts/ichartdatacell) | Ponto de dados XValue |
| y_value | **float** | Ponto de dados YValue |

## add_data_point_for_scatter_series(self, x_value, y_value) {#float-float}
Cria o novo ponto de dados e o adiciona ao final da coleção.
            Aplicável a séries cujo chartType é um dos subtipos Scatter (veja também o método ChartTypeCharacterizer.IsChartTypeScatter(ChartType)).

### Retorna

Novo ponto de dados.

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| Parâmetro | Tipo | Descrição |
| :- | :- | :- |
| x_value | **float** | Ponto de dados XValue |
| y_value | **float** | Ponto de dados YValue |

## add_data_point_for_scatter_series(self, x_value, y_value) {#str-float}
Cria o novo ponto de dados e o adiciona ao final da coleção.
            Aplicável a séries cujo chartType é um dos subtipos Scatter (veja também o método ChartTypeCharacterizer.IsChartTypeScatter(ChartType)).

### Retorna

Novo ponto de dados.

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| Parâmetro | Tipo | Descrição |
| :- | :- | :- |
| x_value | **str** | Ponto de dados XValue |
| y_value | **float** | Ponto de dados YValue |

### Veja Também
* classe [`IChartDataCell`](/slides/python-net/pt/aspose.slides.charts/ichartdatacell)
* classe [`IChartDataPoint`](/slides/python-net/pt/aspose.slides.charts/ichartdatapoint)
* classe [`IChartDataPointCollection`](/slides/python-net/pt/aspose.slides.charts/ichartdatapointcollection)
* módulo [`aspose.slides.charts`](/slides/python-net/pt/aspose.slides.charts)
* biblioteca [`Aspose.Slides`](/slides/python-net)