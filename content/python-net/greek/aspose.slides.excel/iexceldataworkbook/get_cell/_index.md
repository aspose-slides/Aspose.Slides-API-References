---
title: get_cell method
second_title: Aspose.Slides για Python μέσω .NET Αναφορά API
description: 
type: docs
url: /el/aspose.slides.excel/iexceldataworkbook/get_cell/
weight: 10
---
## get_cell(self, worksheet_index, cell_name) {#int-str}
Ανακτά ένα κελί από το καθορισμένο φύλλο εργασίας χρησιμοποιώντας το δείκτη του και το όνομα κελιού σε στυλ Excel (π.χ., "B2").

### Επιστρέφει

Το κελί στη συγκεκριμένη θέση.



```python
def get_cell(self, worksheet_index, cell_name):
    ...
```


| Παράμετρος | Τύπος | Περιγραφή |
| :- | :- | :- |
| worksheet_index | **int** | Δείκτης του φύλλου εργασίας με βάση το μηδέν. |
| cell_name | **str** | Η παραπομπή κελιού σε στυλ Excel (π.χ., "A1", "C5"). |


## get_cell(self, worksheet_name, cell_name) {#str-str}
Ανακτά ένα κελί από το καθορισμένο φύλλο εργασίας χρησιμοποιώντας το όνομα του φύλλου και το όνομα κελιού σε στυλ Excel (π.χ., "B2").

### Επιστρέφει

Το κελί στη συγκεκριμένη θέση.



```python
def get_cell(self, worksheet_name, cell_name):
    ...
```


| Παράμετρος | Τύπος | Περιγραφή |
| :- | :- | :- |
| worksheet_name | **str** | Το όνομα του φύλλου εργασίας. |
| cell_name | **str** | Η παραπομπή κελιού σε στυλ Excel (π.χ., "A1", "C5"). |


## get_cell(self, worksheet_index, row, column) {#int-int-int}
Ανακτά ένα κελί από το καθορισμένο φύλλο εργασίας χρησιμοποιώντας το δείκτη του και τις συντεταγμένες κελιού.

### Επιστρέφει

Το κελί στη συγκεκριμένη θέση.



```python
def get_cell(self, worksheet_index, row, column):
    ...
```


| Παράμετρος | Τύπος | Περιγραφή |
| :- | :- | :- |
| worksheet_index | **int** | Δείκτης του φύλλου εργασίας με βάση το μηδέν. |
| row | **int** | Δείκτης γραμμής του κελιού με βάση το μηδέν. |
| column | **int** | Δείκτης στήλης του κελιού με βάση το μηδέν. |


## get_cell(self, worksheet_name, row, column) {#str-int-int}
Ανακτά ένα κελί από το καθορισμένο φύλλο εργασίας χρησιμοποιώντας το όνομα του και τις συντεταγμένες κελιού.

### Επιστρέφει

Το κελί στη συγκεκριμένη θέση.



```python
def get_cell(self, worksheet_name, row, column):
    ...
```


| Παράμετρος | Τύπος | Περιγραφή |
| :- | :- | :- |
| worksheet_name | **str** | Το όνομα του φύλλου εργασίας. |
| row | **int** | Δείκτης γραμμής του κελιού με βάση το μηδέν. |
| column | **int** | Δείκτης στήλης του κελιού με βάση το μηδέν. |



### Δείτε επίσης
* κλάση [`IExcelDataCell`](/slides/python-net/el/aspose.slides.excel/iexceldatacell)
* κλάση [`IExcelDataWorkbook`](/slides/python-net/el/aspose.slides.excel/iexceldataworkbook)
* μονάδα [`aspose.slides.excel`](/slides/python-net/el/aspose.slides.excel)
* βιβλιοθήκη [`Aspose.Slides`](/slides/python-net)