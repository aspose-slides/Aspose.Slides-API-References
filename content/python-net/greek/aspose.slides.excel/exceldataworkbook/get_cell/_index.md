---
title: get_cell method
second_title: Aspose.Slides για Python μέσω .NET API Αναφορά
description: 
type: docs
url: /el/aspose.slides.excel/exceldataworkbook/get_cell/
weight: 20
---
## get_cell(self, worksheet_index, cell_name) {#int-str}
Ανακτά ένα κελί από το καθορισμένο φύλλο εργασίας χρησιμοποιώντας το ευρετήριο του και το όνομα κελιού σε μορφή Excel (π.χ., "B2").

### Επιστροφή
Το κελί στην καθορισμένη θέση.



```python
def get_cell(self, worksheet_index, cell_name):
    ...
```


| Παράμετρος | Τύπος | Περιγραφή |
| :- | :- | :- |
| worksheet_index | **int** | Δείκτης του φύλλου εργασίας με μηδενική βάση. |
| cell_name | **str** | Η αναφορά κελιού σε μορφή Excel (π.χ., "A1", "C5"). |


## get_cell(self, worksheet_name, cell_name) {#str-str}
Ανακτά ένα κελί από το καθορισμένο φύλλο εργασίας χρησιμοποιώντας το όνομα κελιού σε μορφή Excel (π.χ., "B2").

### Επιστροφή
Το κελί στην καθορισμένη θέση.



```python
def get_cell(self, worksheet_name, cell_name):
    ...
```


| Παράμετρος | Τύπος | Περιγραφή |
| :- | :- | :- |
| worksheet_name | **str** | Το όνομα του φύλλου εργασίας. |
| cell_name | **str** | Η αναφορά κελιού σε μορφή Excel (π.χ., "A1", "C5"). |


## get_cell(self, worksheet_index, row, column) {#int-int-int}
Ανακτά ένα κελί από το καθορισμένο φύλλο εργασίας χρησιμοποιώντας το ευρετήριο του και τις συντεταγμένες του κελιού.

### Επιστροφή
Το κελί στην καθορισμένη θέση.



```python
def get_cell(self, worksheet_index, row, column):
    ...
```


| Παράμετρος | Τύπος | Περιγραφή |
| :- | :- | :- |
| worksheet_index | **int** | Δείκτης του φύλλου εργασίας με μηδενική βάση. |
| row | **int** | Δείκτης γραμμής του κελιού με μηδενική βάση. |
| column | **int** | Δείκτης στήλης του κελιού με μηδενική βάση. |


## get_cell(self, worksheet_name, row, column) {#str-int-int}
Ανακτά ένα κελί από το καθορισμένο φύλλο εργασίας χρησιμοποιώντας το όνομα του και τις συντεταγμένες του κελιού.

### Επιστροφή
Το κελί στην καθορισμένη θέση.



```python
def get_cell(self, worksheet_name, row, column):
    ...
```


| Παράμετρος | Τύπος | Περιγραφή |
| :- | :- | :- |
| worksheet_name | **str** | Το όνομα του φύλλου εργασίας. |
| row | **int** | Δείκτης γραμμής του κελιού με μηδενική βάση. |
| column | **int** | Δείκτης στήλης του κελιού με μηδενική βάση. |



### Δείτε επίσης
* κλάση [`ExcelDataWorkbook`](/slides/python-net/el/aspose.slides.excel/exceldataworkbook)
* κλάση [`IExcelDataCell`](/slides/python-net/el/aspose.slides.excel/iexceldatacell)
* μονάδα [`aspose.slides.excel`](/slides/python-net/el/aspose.slides.excel)
* βιβλιοθήκη [`Aspose.Slides`](/slides/python-net)