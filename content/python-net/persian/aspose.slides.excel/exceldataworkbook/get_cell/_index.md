---
title: get_cell method
second_title: Aspose.Slides برای Python از طریق .NET مرجع API
description: 
type: docs
url: /fa/aspose.slides.excel/exceldataworkbook/get_cell/
weight: 20
---
## get_cell(self, worksheet_index, cell_name) {#int-str}
یک سلول را از کاربرگ مشخص‌شده با استفاده از شاخص آن و نام سلول به سبک اکسل (به عنوان مثال "B2") دریافت می‌کند.

### بازگشت

سلول در محل مشخص‌شده.



```python
def get_cell(self, worksheet_index, cell_name):
    ...
```


| پارامتر | نوع | توضیح |
| :- | :- | :- |
| worksheet_index | **int** | شاخص صفر-مبتنی کاربرگ. |
| cell_name | **str** | مرجع سلول به سبک اکسل (به عنوان مثال "A1"، "C5"). |


## get_cell(self, worksheet_name, cell_name) {#str-str}
یک سلول را از کاربرگ مشخص‌شده با استفاده از نام سلول به سبک اکسل (به عنوان مثال "B2") دریافت می‌کند.

### بازگشت

سلول در محل مشخص‌شده.



```python
def get_cell(self, worksheet_name, cell_name):
    ...
```


| پارامتر | نوع | توضیح |
| :- | :- | :- |
| worksheet_name | **str** | نام کاربرگ. |
| cell_name | **str** | مرجع سلول به سبک اکسل (به عنوان مثال "A1"، "C5"). |


## get_cell(self, worksheet_index, row, column) {#int-int-int}
یک سلول را از کاربرگ مشخص‌شده با استفاده از شاخص آن و مختصات سلول دریافت می‌کند.

### بازگشت

سلول در محل مشخص‌شده.



```python
def get_cell(self, worksheet_index, row, column):
    ...
```


| پارامتر | نوع | توضیح |
| :- | :- | :- |
| worksheet_index | **int** | شاخص صفر-مبتنی کاربرگ. |
| row | **int** | شاخص صفر-مبتنی سطر سلول. |
| column | **int** | شاخص صفر-مبتنی ستون سلول. |


## get_cell(self, worksheet_name, row, column) {#str-int-int}
یک سلول را از کاربرگ مشخص‌شده با استفاده از نام آن و مختصات سلول دریافت می‌کند.

### بازگشت

سلول در محل مشخص‌شده.



```python
def get_cell(self, worksheet_name, row, column):
    ...
```


| پارامتر | نوع | توضیح |
| :- | :- | :- |
| worksheet_name | **str** | نام کاربرگ. |
| row | **int** | شاخص صفر-مبتنی سطر سلول. |
| column | **int** | شاخص صفر-مبتنی ستون سلول. |



### موارد مرتبط
* کلاس [`ExcelDataWorkbook`](/slides/python-net/fa/aspose.slides.excel/exceldataworkbook)
* کلاس [`IExcelDataCell`](/slides/python-net/fa/aspose.slides.excel/iexceldatacell)
* ماژول [`aspose.slides.excel`](/slides/python-net/fa/aspose.slides.excel)
* کتابخانه [`Aspose.Slides`](/slides/python-net)