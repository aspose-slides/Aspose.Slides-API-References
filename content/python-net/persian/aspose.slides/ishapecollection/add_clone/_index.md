---
title: add_clone method
second_title: مرجع API Aspose.Slides برای Python از طریق .NET
description: 
type: docs
url: /fa/aspose.slides/ishapecollection/add_clone/
weight: 60
---
## add_clone(self, source_shape) {#ishape}
یک کپی از شکل مشخص‌شده ایجاد می‌کند و آن را به انتهای مجموعهٔ اشکال اضافه می‌نماید.
کپی‌شده موقعیت و اندازهٔ اصلی را حفظ می‌کند.

### بازگشت

[`IShape`](/slides/python-net/fa/aspose.slides/ishape) تازه ایجاد شده.

```python
def add_clone(self, source_shape):
    ...
```

| پارامتر | نوع | توضیح |
| :- | :- | :- |
| source_shape | [`IShape`](/slides/python-net/fa/aspose.slides/ishape) | [`IShape`](/slides/python-net/fa/aspose.slides/ishape) برای تکثیر. |

## add_clone(self, source_shape, x, y) {#ishape-float-float}
یک کپی از شکل مشخص‌شده ایجاد می‌کند و آن را به انتهای مجموعهٔ اشکال اضافه می‌نماید.
شکل جدید عرض و ارتفاع `source_shape` را حفظ می‌کند.

### بازگشت

[`IShape`](/slides/python-net/fa/aspose.slides/ishape) تازه ایجاد شده.

```python
def add_clone(self, source_shape, x, y):
    ...
```

| پارامتر | نوع | توضیح |
| :- | :- | :- |
| source_shape | [`IShape`](/slides/python-net/fa/aspose.slides/ishape) | [`IShape`](/slides/python-net/fa/aspose.slides/ishape) برای تکثیر. |
| x | **float** | مختصات x قاب شکل تکثیر شده، به نقطه. |
| y | **float** | مختصات y قاب شکل تکثیر شده، به نقطه. |

## add_clone(self, source_shape, x, y, width, height) {#ishape-float-float-float-float}
یک کپی از شکل مشخص‌شده ایجاد می‌کند و آن را به انتهای مجموعهٔ اشکال اضافه می‌نماید.

### بازگشت

[`IShape`](/slides/python-net/fa/aspose.slides/ishape) تازه ایجاد شده.

```python
def add_clone(self, source_shape, x, y, width, height):
    ...
```

| پارامتر | نوع | توضیح |
| :- | :- | :- |
| source_shape | [`IShape`](/slides/python-net/fa/aspose.slides/ishape) | شکل برای تکثیر. |
| x | **float** | مختصات x قاب شکل تکثیر شده، به نقطه. |
| y | **float** | مختصات y قاب شکل تکثیر شده، به نقطه. |
| width | **float** | عرض قاب شکل تکثیر شده، به نقطه. |
| height | **float** | ارتفاع قاب شکل تکثیر شده، به نقطه. |

### همچنین ببینید
* کلاس [`IShape`](/slides/python-net/fa/aspose.slides/ishape)
* کلاس [`IShapeCollection`](/slides/python-net/fa/aspose.slides/ishapecollection)
* ماژول [`aspose.slides`](/slides/python-net/fa/aspose.slides)
* کتابخانه [`Aspose.Slides`](/slides/python-net)