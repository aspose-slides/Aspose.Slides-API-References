---
title: insert_clone method
second_title: مرجع API Aspose.Slides برای Python از طریق .NET
description: 
type: docs
url: /fa/aspose.slides/shapecollection/insert_clone/
weight: 250
---
## insert_clone(self, index, source_shape) {#int-ishape}
یک کپی از شکل مشخص‌شده ایجاد می‌کند و آن را در مجموعه شکل‌ها در شاخص مشخص شده وارد می‌نماید. شکل کلون‌شده موقعیت و اندازهٔ اصلی را حفظ می‌کند.

### Returns
[`IShape`](/slides/python-net/fa/aspose.slides/ishape) جدید ایجاد شده.

```python
def insert_clone(self, index, source_shape):
    ...
```

| Parameter | Type | Description |
| :- | :- | :- |
| index | **int** | شاخص صفر-مبنا که در آن شکل کلون‌شده وارد می‌شود. |
| source_shape | [`IShape`](/slides/python-net/fa/aspose.slides/ishape) | [`IShape`](/slides/python-net/fa/aspose.slides/ishape) برای کلون. |

## insert_clone(self, index, source_shape, x, y) {#int-ishape-float-float}
یک کپی از شکل مشخص‌شده ایجاد می‌کند و آن را در مجموعه شکل‌ها در شاخص مشخص شده وارد می‌نماید. شکل جدید عرض و ارتفاع `source_shape` را حفظ می‌کند.

### Returns
[`IShape`](/slides/python-net/fa/aspose.slides/ishape) جدید ایجاد شده.

```python
def insert_clone(self, index, source_shape, x, y):
    ...
```

| Parameter | Type | Description |
| :- | :- | :- |
| index | **int** | شاخص صفر-مبنا که در آن شکل کلون‌شده وارد می‌شود. |
| source_shape | [`IShape`](/slides/python-net/fa/aspose.slides/ishape) | [`IShape`](/slides/python-net/fa/aspose.slides/ishape) برای کلون. |
| x | **float** | مختصات x قاب شکل کلون‌شده، به نقطه. |
| y | **float** | مختصات y قاب شکل کلون‌شده، به نقطه. |

## insert_clone(self, index, source_shape, x, y, width, height) {#int-ishape-float-float-float-float}
یک کپی از شکل مشخص‌شده ایجاد می‌کند و آن را در مجموعه شکل‌ها در شاخص مشخص شده وارد می‌نماید.

### Returns
[`IShape`](/slides/python-net/fa/aspose.slides/ishape) جدید ایجاد شده.

```python
def insert_clone(self, index, source_shape, x, y, width, height):
    ...
```

| Parameter | Type | Description |
| :- | :- | :- |
| index | **int** | شاخص صفر-مبنا که در آن شکل کلون‌شده وارد می‌شود. |
| source_shape | [`IShape`](/slides/python-net/fa/aspose.slides/ishape) | [`IShape`](/slides/python-net/fa/aspose.slides/ishape) برای کلون. |
| x | **float** | مختصات x قاب شکل کلون‌شده، به نقطه. |
| y | **float** | مختصات y قاب شکل کلون‌شده، به نقطه. |
| width | **float** | عرض قاب شکل کلون‌شده، به نقطه. |
| height | **float** | ارتفاع قاب شکل کلون‌شده، به نقطه. |

### موارد مرتبط
* کلاس [`IShape`](/slides/python-net/fa/aspose.slides/ishape)
* کلاس [`ShapeCollection`](/slides/python-net/fa/aspose.slides/shapecollection)
* ماژول [`aspose.slides`](/slides/python-net/fa/aspose.slides)
* کتابخانه [`Aspose.Slides`](/slides/python-net)