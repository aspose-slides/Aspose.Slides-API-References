---
title: RectangleF class
second_title: Aspose.Slides برای Python از طریق .NET مرجع API
description: یک مجموعه از چهار عدد نقطه شناور را ذخیره می‌کند که موقعیت و اندازهٔ یک مستطیل را نشان می‌دهند.
type: docs
url: /fa/aspose.slides/rectanglef/
net_type: System.Drawing.RectangleF
---
## RectangleF کلاس

یک مجموعه از چهار عدد اعشاری را ذخیره می‌کند که موقعیت و اندازهٔ یک مستطیل را نشان می‌دهند. سازگار با .NET `System.Drawing.RectangleF`.

**وراثت:**[`RectangleF`](/slides/python-net/fa/aspose.slides/rectanglef) → [`Rectangle`](/slides/python-net/fa/aspose.slides/rectangle)

نوع RectangleF اعضای زیر را ارائه می‌دهد:

## سازنده‌ها

| سازنده | توضیح |
| :- | :- |
| [`__init__(self, x=0.0, y=0.0, width=0.0, height=0.0)`](/slides/python-net/fa/aspose.slides/rectanglef/__init__/#float-float-float-float) | یک مستطیل را با موقعیت و اندازهٔ مشخص‌شده ایجاد می‌کند. |

## ویژگی‌ها

| ویژگی | توضیح |
| :- | :- |
| [`x`](/slides/python-net/fa/aspose.slides/rectanglef/x/) | مختصات x گوشهٔ بالا-چپ این مستطیل را دریافت می‌کند.<br/>            فقط-خواندنی **float**. |
| [`y`](/slides/python-net/fa/aspose.slides/rectanglef/y/) | مختصات y گوشهٔ بالا-چپ این مستطیل را دریافت می‌کند.<br/>            فقط-خواندنی **float**. |
| [`width`](/slides/python-net/fa/aspose.slides/rectanglef/width/) | عرض این مستطیل را دریافت می‌کند.<br/>            فقط-خواندنی **float**. |
| [`height`](/slides/python-net/fa/aspose.slides/rectanglef/height/) | ارتفاع این مستطیل را دریافت می‌کند.<br/>            فقط-خواندنی **float**. |
| [`left`](/slides/python-net/fa/aspose.slides/rectanglef/left/) | مختصات x لبهٔ چپ این مستطیل را دریافت می‌کند. برابر با `x`.<br/>            فقط-خواندنی **float**. |
| [`top`](/slides/python-net/fa/aspose.slides/rectanglef/top/) | مختصات y لبهٔ بالایی این مستطیل را دریافت می‌کند. برابر با `y`.<br/>            فقط-خواندنی **float**. |
| [`right`](/slides/python-net/fa/aspose.slides/rectanglef/right/) | مختصات x که حاصل جمع `x` و `width` این مستطیل است را دریافت می‌کند.<br/>            فقط-خواندنی **float**. |
| [`bottom`](/slides/python-net/fa/aspose.slides/rectanglef/bottom/) | مختصات y که حاصل جمع `y` و `height` این مستطیل است را دریافت می‌کند.<br/>            فقط-خواندنی **float**. |
| [`is_empty`](/slides/python-net/fa/aspose.slides/rectanglef/is_empty/) | مشخص می‌کند آیا همهٔ ویژگی‌های عددی این مستطیل مقدار صفر دارند یا خیر.<br/>            فقط-خواندنی **bool**. |

## متدها

| متد | توضیح |
| :- | :- |
| [`contains(self, x, y)`](/slides/python-net/fa/aspose.slides/rectanglef/contains/#float-float) | مشخص می‌کند آیا نقطهٔ مشخص‌شده در داخل این مستطیل قرار دارد یا خیر. |
| [`contains(self, point)`](/slides/python-net/fa/aspose.slides/rectanglef/contains/#pointf) | مشخص می‌کند آیا نقطهٔ مشخص‌شده در داخل این مستطیل قرار دارد یا خیر. |
| [`contains(self, rect)`](/slides/python-net/fa/aspose.slides/rectanglef/contains/#rectanglef) | مشخص می‌کند آیا ناحیهٔ مستطیلی که توسط `rect` نشان داده شده است به طور کامل در داخل این مستطیل قرار دارد یا خیر. |

### توضیحات

مستطیل‌ها با استفاده از `==` بر اساس موقعیت و اندازهٔ خود مقایسه می‌شوند و می‌توانند به عنوان کلیدهای دیکشنری یا اعضای مجموعه استفاده شوند.

### موارد مرتبط
* کلاس [`Rectangle`](/slides/python-net/fa/aspose.slides/rectangle)
* ماژول [`aspose.slides`](/slides/python-net/fa/aspose.slides)
* کتابخانه [`Aspose.Slides`](/slides/python-net)