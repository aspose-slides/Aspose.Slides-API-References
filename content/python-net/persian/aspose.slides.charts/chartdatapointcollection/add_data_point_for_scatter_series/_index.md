---
title: add_data_point_for_scatter_series method
second_title: Aspose.Slides برای Python از طریق .NET مرجع API
description: 
type: docs
url: /fa/aspose.slides.charts/chartdatapointcollection/add_data_point_for_scatter_series/
weight: 120
---
## add_data_point_for_scatter_series(self, x_value, y_value) {#ichartdatacell-ichartdatacell}
یک نقطه داده جدید ایجاد می‌کند و آن را به انتهای مجموعه اضافه می‌نماید.
            قابل استفاده برای سری‌هایی که chartType آن‌ها یکی از زیرنوع‌های Scatter است (همچنین به روش **Aspose.Slides.Charts.ChartTypeCharacterizer.IsChartTypeScatter(Aspose.Slides.Chart** مراجعه کنید).

### بازگشت
نقطه داده جدید.

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| پارامتر | نوع | توضیح |
| :- | :- | :- |
| x_value | [`IChartDataCell`](/slides/python-net/fa/aspose.slides.charts/ichartdatacell) | XValue نقطه داده |
| y_value | [`IChartDataCell`](/slides/python-net/fa/aspose.slides.charts/ichartdatacell) | YValue نقطه داده |

## add_data_point_for_scatter_series(self, x_value, y_value) {#float-ichartdatacell}
یک نقطه داده جدید ایجاد می‌کند و آن را به انتهای مجموعه اضافه می‌نماید.
            قابل استفاده برای سری‌هایی که chartType آن‌ها یکی از زیرنوع‌های Scatter است (همچنین به روش **Aspose.Slides.Charts.ChartTypeCharacterizer.IsChartTypeScatter(Aspose.Slides.Chart** مراجعه کنید).

### بازگشت
نقطه داده جدید.

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| پارامتر | نوع | توضیح |
| :- | :- | :- |
| x_value | **float** | XValue نقطه داده |
| y_value | [`IChartDataCell`](/slides/python-net/fa/aspose.slides.charts/ichartdatacell) | YValue نقطه داده |

## add_data_point_for_scatter_series(self, x_value, y_value) {#str-ichartdatacell}
یک نقطه داده جدید ایجاد می‌کند و آن را به انتهای مجموعه اضافه می‌نماید.
            قابل استفاده برای سری‌هایی که chartType آن‌ها یکی از زیرنوع‌های Scatter است (همچنین به روش **Aspose.Slides.Charts.ChartTypeCharacterizer.IsChartTypeScatter(Aspose.Slides.Chart** مراجعه کنید).

### بازگشت
نقطه داده جدید.

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| پارامتر | نوع | توضیح |
| :- | :- | :- |
| x_value | **str** | XValue نقطه داده |
| y_value | [`IChartDataCell`](/slides/python-net/fa/aspose.slides.charts/ichartdatacell) | YValue نقطه داده |

## add_data_point_for_scatter_series(self, x_value, y_value) {#ichartdatacell-float}
یک نقطه داده جدید ایجاد می‌کند و آن را به انتهای مجموعه اضافه می‌نماید.
            قابل استفاده برای سری‌هایی که chartType آن‌ها یکی از زیرنوع‌های Scatter است (همچنین به روش **Aspose.Slides.Charts.ChartTypeCharacterizer.IsChartTypeScatter(Aspose.Slides.Chart** مراجعه کنید).

### بازگشت
نقطه داده جدید.

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| پارامتر | نوع | توضیح |
| :- | :- | :- |
| x_value | [`IChartDataCell`](/slides/python-net/fa/aspose.slides.charts/ichartdatacell) | XValue نقطه داده |
| y_value | **float** | YValue نقطه داده |

## add_data_point_for_scatter_series(self, x_value, y_value) {#float-float}
یک نقطه داده جدید ایجاد می‌کند و آن را به انتهای مجموعه اضافه می‌نماید.
            قابل استفاده برای سری‌هایی که chartType آن‌ها یکی از زیرنوع‌های Scatter است (همچنین به روش **Aspose.Slides.Charts.ChartTypeCharacterizer.IsChartTypeScatter(Aspose.Slides.Chart** مراجعه کنید).

### بازگشت
نقطه داده جدید.

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| پارامتر | نوع | توضیح |
| :- | :- | :- |
| x_value | **float** | XValue نقطه داده |
| y_value | **float** | YValue نقطه داده |

## add_data_point_for_scatter_series(self, x_value, y_value) {#str-float}
یک نقطه داده جدید ایجاد می‌کند و آن را به انتهای مجموعه اضافه می‌نماید.
            قابل استفاده برای سری‌هایی که chartType آن‌ها یکی از زیرنوع‌های Scatter است (همچنین به روش **Aspose.Slides.Charts.ChartTypeCharacterizer.IsChartTypeScatter(Aspose.Slides.Chart** مراجعه کنید).

### بازگشت
نقطه داده جدید.

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| پارامتر | نوع | توضیح |
| :- | :- | :- |
| x_value | **str** | XValue نقطه داده |
| y_value | **float** | YValue نقطه داده |

### همچنین ببینید
* کلاس [`ChartDataPointCollection`](/slides/python-net/fa/aspose.slides.charts/chartdatapointcollection)
* کلاس [`IChartDataCell`](/slides/python-net/fa/aspose.slides.charts/ichartdatacell)
* کلاس [`IChartDataPoint`](/slides/python-net/fa/aspose.slides.charts/ichartdatapoint)
* ماژول [`aspose.slides.charts`](/slides/python-net/fa/aspose.slides.charts)
* کتابخانه [`Aspose.Slides`](/slides/python-net)