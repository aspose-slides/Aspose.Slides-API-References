---
title: add_data_point_for_scatter_series method
second_title: Aspose.Slides برای Python از طریق .NET مرجع API
description: 
type: docs
url: /fa/aspose.slides.charts/ichartdatapointcollection/add_data_point_for_scatter_series/
weight: 120
---
## add_data_point_for_scatter_series(self, x_value, y_value) {#ichartdatacell-ichartdatacell}
داده جدید را ایجاد می‌کند و در انتهای مجموعه اضافه می‌نماید.
            قابل استفاده برای سری‌هایی که chartType یکی از زیرنوع‌های Scatter است (همچنین به متد ChartTypeCharacterizer.IsChartTypeScatter(ChartType) مراجعه کنید).

### Returns
نقطه داده جدید.

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| Parameter | Type | Description |
| :- | :- | :- |
| x_value | [`IChartDataCell`](/slides/python-net/fa/aspose.slides.charts/ichartdatacell) | نقطه داده XValue |
| y_value | [`IChartDataCell`](/slides/python-net/fa/aspose.slides.charts/ichartdatacell) | نقطه داده YValue |

## add_data_point_for_scatter_series(self, x_value, y_value) {#float-ichartdatacell}
داده جدید را ایجاد می‌کند و در انتهای مجموعه اضافه می‌نماید.
            قابل استفاده برای سری‌هایی که chartType یکی از زیرنوع‌های Scatter است (همچنین به متد ChartTypeCharacterizer.IsChartTypeScatter(ChartType) مراجعه کنید).

### Returns
نقطه داده جدید.

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| Parameter | Type | Description |
| :- | :- | :- |
| x_value | **float** | نقطه داده XValue |
| y_value | [`IChartDataCell`](/slides/python-net/fa/aspose.slides.charts/ichartdatacell) | نقطه داده YValue |

## add_data_point_for_scatter_series(self, x_value, y_value) {#str-ichartdatacell}
داده جدید را ایجاد می‌کند و در انتهای مجموعه اضافه می‌نماید.
            قابل استفاده برای سری‌هایی که chartType یکی از زیرنوع‌های Scatter است (همچنین به متد ChartTypeCharacterizer.IsChartTypeScatter(ChartType) مراجعه کنید).

### Returns
نقطه داده جدید.

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| Parameter | Type | Description |
| :- | :- | :- |
| x_value | **str** | نقطه داده XValue |
| y_value | [`IChartDataCell`](/slides/python-net/fa/aspose.slides.charts/ichartdatacell) | نقطه داده YValue |

## add_data_point_for_scatter_series(self, x_value, y_value) {#ichartdatacell-float}
داده جدید را ایجاد می‌کند و در انتهای مجموعه اضافه می‌نماید.
            قابل استفاده برای سری‌هایی که chartType یکی از زیرنوع‌های Scatter است (همچنین به متد ChartTypeCharacterizer.IsChartTypeScatter(ChartType) مراجعه کنید).

### Returns
نقطه داده جدید.

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| Parameter | Type | Description |
| :- | :- | :- |
| x_value | [`IChartDataCell`](/slides/python-net/fa/aspose.slides.charts/ichartdatacell) | نقطه داده XValue |
| y_value | **float** | نقطه داده YValue |

## add_data_point_for_scatter_series(self, x_value, y_value) {#float-float}
داده جدید را ایجاد می‌کند و در انتهای مجموعه اضافه می‌نماید.
            قابل استفاده برای سری‌هایی که chartType یکی از زیرنوع‌های Scatter است (همچنین به متد ChartTypeCharacterizer.IsChartTypeScatter(ChartType) مراجعه کنید).

### Returns
نقطه داده جدید.

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| Parameter | Type | Description |
| :- | :- | :- |
| x_value | **float** | نقطه داده XValue |
| y_value | **float** | نقطه داده YValue |

## add_data_point_for_scatter_series(self, x_value, y_value) {#str-float}
داده جدید را ایجاد می‌کند و در انتهای مجموعه اضافه می‌نماید.
            قابل استفاده برای سری‌هایی که chartType یکی از زیرنوع‌های Scatter است (همچنین به متد ChartTypeCharacterizer.IsChartTypeScatter(ChartType) مراجعه کنید).

### Returns
نقطه داده جدید.

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| Parameter | Type | Description |
| :- | :- | :- |
| x_value | **str** | نقطه داده XValue |
| y_value | **float** | نقطه داده YValue |

### See Also
* کلاس [`IChartDataCell`](/slides/python-net/fa/aspose.slides.charts/ichartdatacell)
* کلاس [`IChartDataPoint`](/slides/python-net/fa/aspose.slides.charts/ichartdatapoint)
* کلاس [`IChartDataPointCollection`](/slides/python-net/fa/aspose.slides.charts/ichartdatapointcollection)
* ماژول [`aspose.slides.charts`](/slides/python-net/fa/aspose.slides.charts)
* کتابخانه [`Aspose.Slides`](/slides/python-net)