---
title: add_text method
second_title: Aspose.Slides для Python через .NET справочник API
description: 
type: docs
url: /ru/aspose.slides.export/ihtmlgenerator/add_text/
weight: 30
---
## add_text(self, text) {#str}
Добавляет обычный текст в HTML-файлы, заменяя специальные символы на HTML-сущности. Переводы строк и пробелы не заменяются.

```python
def add_text(self, text):
    ...
```

| Параметр | Тип | Описание |
| :- | :- | :- |
| text | **str** | Текст для добавления. |

## add_text(self, text) {#listchar}
Добавляет обычный текст в HTML-файлы, заменяя специальные символы на HTML-сущности. Переводы строк и пробелы не заменяются.

```python
def add_text(self, text):
    ...
```

| Параметр | Тип | Описание |
| :- | :- | :- |
| text | **List[char]** | Текст для добавления. |

## add_text(self, text, start_index, length) {#listchar-int-int}
Добавляет обычный текст в HTML-файлы, заменяя специальные символы на HTML-сущности. Переводы строк и пробелы не заменяются.

```python
def add_text(self, text, start_index, length):
    ...
```

| Параметр | Тип | Описание |
| :- | :- | :- |
| text | **List[char]** | Текст для добавления. |
| start_index | **int** | Начальный индекс добавляемой части. |
| length | **int** | Длина добавляемой части. |

### См. также
* класс [`IHtmlGenerator`](/slides/python-net/ru/aspose.slides.export/ihtmlgenerator)
* модуль [`aspose.slides.export`](/slides/python-net/ru/aspose.slides.export)
* библиотека [`Aspose.Slides`](/slides/python-net)