---
title: set_custom_property_value method
second_title: Aspose.Slides für Python über .NET API-Referenz
description: 
type: docs
url: /de/aspose.slides/documentproperties/set_custom_property_value/
weight: 110
---
## set_custom_property_value(self, name, value) {#str-bool}
Setzt eine benannte boolesche benutzerdefinierte Eigenschaft.

```python
def set_custom_property_value(self, name, value):
    ...
```

| Parameter | Typ | Beschreibung |
| :- | :- | :- |
| name | **str** | Name der benutzerdefinierten Eigenschaft, die gesetzt werden soll |
| value | **bool** | Wert der benutzerdefinierten Eigenschaft |

## set_custom_property_value(self, name, value) {#str-int}
Setzt eine benannte ganzzahlige benutzerdefinierte Eigenschaft.

```python
def set_custom_property_value(self, name, value):
    ...
```

| Parameter | Typ | Beschreibung |
| :- | :- | :- |
| name | **str** | Name der benutzerdefinierten Eigenschaft, die gesetzt werden soll |
| value | **int** | Wert der benutzerdefinierten Eigenschaft |

## set_custom_property_value(self, name, value) {#str-datetime}
Setzt eine benannte DateTime-benutzerdefinierte Eigenschaft.

```python
def set_custom_property_value(self, name, value):
    ...
```

| Parameter | Typ | Beschreibung |
| :- | :- | :- |
| name | **str** | Name der benutzerdefinierten Eigenschaft, die gesetzt werden soll |
| value | **DateTime** | Wert der benutzerdefinierten Eigenschaft |

## set_custom_property_value(self, name, value) {#str-str}
Setzt eine benannte Zeichenketten-benutzerdefinierte Eigenschaft.

```python
def set_custom_property_value(self, name, value):
    ...
```

| Parameter | Typ | Beschreibung |
| :- | :- | :- |
| name | **str** | Name der benutzerdefinierten Eigenschaft, die gesetzt werden soll |
| value | **str** | Wert der benutzerdefinierten Eigenschaft |

## set_custom_property_value(self, name, value) {#str-float}
Setzt eine benannte Gleitkomma-benutzerdefinierte Eigenschaft.

```python
def set_custom_property_value(self, name, value):
    ...
```

| Parameter | Typ | Beschreibung |
| :- | :- | :- |
| name | **str** | Name der benutzerdefinierten Eigenschaft, die gesetzt werden soll |
| value | **float** | Wert der benutzerdefinierten Eigenschaft |

## set_custom_property_value(self, name, value) {#str-float}
Setzt eine benannte double-benutzerdefinierte Eigenschaft.

```python
def set_custom_property_value(self, name, value):
    ...
```

| Parameter | Typ | Beschreibung |
| :- | :- | :- |
| name | **str** | Name der benutzerdefinierten Eigenschaft, die gesetzt werden soll |
| value | **float** | Wert der benutzerdefinierten Eigenschaft |

### Siehe auch
* Klasse [`DocumentProperties`](/slides/python-net/de/aspose.slides/documentproperties)
* Modul [`aspose.slides`](/slides/python-net/de/aspose.slides)
* Bibliothek [`Aspose.Slides`](/slides/python-net)