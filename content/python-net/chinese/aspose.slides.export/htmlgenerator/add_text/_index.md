---
title: add_text method
second_title: Aspose.Slides for Python via .NET API 参考
description: 
type: docs
url: /zh/aspose.slides.export/htmlgenerator/add_text/
weight: 30
---
## add_text(self, text) {#str}
将纯文本添加到 html 文件中，并将特殊字符替换为 html 实体。
换行符和空白字符不会被替换。

```python
def add_text(self, text):
    ...
```

| Parameter | Type | Description |
| :- | :- | :- |
| text | **str** | 要添加的文本。 |

## add_text(self, text) {#listchar}
将纯文本添加到 html 文件中，并将特殊字符替换为 html 实体。
换行符和空白字符不会被替换。

```python
def add_text(self, text):
    ...
```

| Parameter | Type | Description |
| :- | :- | :- |
| text | **List[char]** | 要添加的文本。 |

## add_text(self, text, start_index, length) {#listchar-int-int}
将纯文本添加到 html 文件中，并将特殊字符替换为 html 实体。
换行符和空白字符不会被替换。

```python
def add_text(self, text, start_index, length):
    ...
```

| Parameter | Type | Description |
| :- | :- | :- |
| text | **List[char]** | 要添加的文本。 |
| start_index | **int** | 要添加部分的起始索引。 |
| length | **int** | 要添加部分的长度。 |

### 另见
* 类 [`HtmlGenerator`](/slides/python-net/zh/aspose.slides.export/htmlgenerator)
* 模块 [`aspose.slides.export`](/slides/python-net/zh/aspose.slides.export)
* 库 [`Aspose.Slides`](/slides/python-net)