---
title: add_data_point_for_scatter_series method
second_title: Aspose.Slides for Python via .NET API 参考
description: 
type: docs
url: /zh/aspose.slides.charts/ichartdatapointcollection/add_data_point_for_scatter_series/
weight: 120
---
## add_data_point_for_scatter_series(self, x_value, y_value) {#ichartdatacell-ichartdatacell}
创建新的数据点并将其添加到集合的末尾。
适用于 chartType 为 Scatter 子类型之一的系列（另请参阅 ChartTypeCharacterizer.IsChartTypeScatter(ChartType) 方法）。

### 返回

新的数据点。

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| 参数 | 类型 | 描述 |
| :- | :- | :- |
| x_value | [`IChartDataCell`](/slides/python-net/zh/aspose.slides.charts/ichartdatacell) | Data point XValue |
| y_value | [`IChartDataCell`](/slides/python-net/zh/aspose.slides.charts/ichartdatacell) | Data point YValue |

## add_data_point_for_scatter_series(self, x_value, y_value) {#float-ichartdatacell}
创建新的数据点并将其添加到集合的末尾。
适用于 chartType 为 Scatter 子类型之一的系列（另请参阅 ChartTypeCharacterizer.IsChartTypeScatter(ChartType) 方法）。

### 返回

新的数据点。

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| 参数 | 类型 | 描述 |
| :- | :- | :- |
| x_value | **float** | Data point XValue |
| y_value | [`IChartDataCell`](/slides/python-net/zh/aspose.slides.charts/ichartdatacell) | Data point YValue |

## add_data_point_for_scatter_series(self, x_value, y_value) {#str-ichartdatacell}
创建新的数据点并将其添加到集合的末尾。
适用于 chartType 为 Scatter 子类型之一的系列（另请参阅 ChartTypeCharacterizer.IsChartTypeScatter(ChartType) 方法）。

### 返回

新的数据点。

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| 参数 | 类型 | 描述 |
| :- | :- | :- |
| x_value | **str** | Data point XValue |
| y_value | [`IChartDataCell`](/slides/python-net/zh/aspose.slides.charts/ichartdatacell) | Data point YValue |

## add_data_point_for_scatter_series(self, x_value, y_value) {#ichartdatacell-float}
创建新的数据点并将其添加到集合的末尾。
适用于 chartType 为 Scatter 子类型之一的系列（另请参阅 ChartTypeCharacterizer.IsChartTypeScatter(ChartType) 方法）。

### 返回

新的数据点。

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| 参数 | 类型 | 描述 |
| :- | :- | :- |
| x_value | [`IChartDataCell`](/slides/python-net/zh/aspose.slides.charts/ichartdatacell) | Data point XValue |
| y_value | **float** | Data point YValue |

## add_data_point_for_scatter_series(self, x_value, y_value) {#float-float}
创建新的数据点并将其添加到集合的末尾。
适用于 chartType 为 Scatter 子类型之一的系列（另请参阅 ChartTypeCharacterizer.IsChartTypeScatter(ChartType) 方法）。

### 返回

新的数据点。

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| 参数 | 类型 | 描述 |
| :- | :- | :- |
| x_value | **float** | Data point XValue |
| y_value | **float** | Data point YValue |

## add_data_point_for_scatter_series(self, x_value, y_value) {#str-float}
创建新的数据点并将其添加到集合的末尾。
适用于 chartType 为 Scatter 子类型之一的系列（另请参阅 ChartTypeCharacterizer.IsChartTypeScatter(ChartType) 方法）。

### 返回

新的数据点。

```python
def add_data_point_for_scatter_series(self, x_value, y_value):
    ...
```

| 参数 | 类型 | 描述 |
| :- | :- | :- |
| x_value | **str** | Data point XValue |
| y_value | **float** | Data point YValue |

### 另请参阅
* 类 [`IChartDataCell`](/slides/python-net/zh/aspose.slides.charts/ichartdatacell)
* 类 [`IChartDataPoint`](/slides/python-net/zh/aspose.slides.charts/ichartdatapoint)
* 类 [`IChartDataPointCollection`](/slides/python-net/zh/aspose.slides.charts/ichartdatapointcollection)
* 模块 [`aspose.slides.charts`](/slides/python-net/zh/aspose.slides.charts)
* 库 [`Aspose.Slides`](/slides/python-net)