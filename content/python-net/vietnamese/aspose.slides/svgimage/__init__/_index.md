---
title: SvgImage constructor
second_title: Tham khảo API Aspose.Slides cho Python qua .NET
description: 
type: docs
url: /vi/aspose.slides/svgimage/__init__/
weight: 10
---
## __init__(self, data) {#bytes}
Tạo đối tượng SvgImage mới.

```python
def __init__(self, data):
    ...
```

| Tham số | Kiểu | Mô tả |
| :- | :- | :- |
| data | **bytes** | Dữ liệu Svg. |

## __init__(self, svg_content) {#str}
Tạo đối tượng SvgImage mới.

```python
def __init__(self, svg_content):
    ...
```

| Tham số | Kiểu | Mô tả |
| :- | :- | :- |
| svg_content | **str** | Nội dung Svg. |

## __init__(self, stream) {#iorawiobase}
Tạo đối tượng SvgImage mới.

```python
def __init__(self, stream):
    ...
```

| Tham số | Kiểu | Mô tả |
| :- | :- | :- |
| stream | **io.RawIOBase** | Luồng Svg. |

## __init__(self, data, external_res_resolver, base_uri) {#bytes-asposeslidesimportingiexternalresourceresolver-str}
Tạo đối tượng SvgImage mới.

```python
def __init__(self, data, external_res_resolver, base_uri):
    ...
```

| Tham số | Kiểu | Mô tả |
| :- | :- | :- |
| data | **bytes** | Dữ liệu Svg. |
| external_res_resolver | [`IExternalResourceResolver`](/slides/python-net/vi/aspose.slides.importing/iexternalresourceresolver) | Một đối tượng callback được sử dụng để lấy các đối tượng bên ngoài. Nếu tham số này là None, tất cả các đối tượng bên ngoài sẽ bị bỏ qua. |
| base_uri | **str** | URI cơ sở của Svg đã chỉ định. Được sử dụng để giải quyết các liên kết tương đối. |

## __init__(self, svg_content, external_res_resolver, base_uri) {#str-asposeslidesimportingiexternalresourceresolver-str}
Tạo đối tượng SvgImage mới.

```python
def __init__(self, svg_content, external_res_resolver, base_uri):
    ...
```

| Tham số | Kiểu | Mô tả |
| :- | :- | :- |
| svg_content | **str** | Nội dung Svg. |
| external_res_resolver | [`IExternalResourceResolver`](/slides/python-net/vi/aspose.slides.importing/iexternalresourceresolver) | Một đối tượng callback được sử dụng để lấy các đối tượng bên ngoài. Nếu tham số này là None, tất cả các đối tượng bên ngoài sẽ bị bỏ qua. |
| base_uri | **str** | URI cơ sở của Svg đã chỉ định. Được sử dụng để giải quyết các liên kết tương đối. |

## __init__(self, stream, external_res_resolver, base_uri) {#iorawiobase-asposeslidesimportingiexternalresourceresolver-str}
Tạo đối tượng SvgImage mới.

```python
def __init__(self, stream, external_res_resolver, base_uri):
    ...
```

| Tham số | Kiểu | Mô tả |
| :- | :- | :- |
| stream | **io.RawIOBase** | Luồng Svg. |
| external_res_resolver | [`IExternalResourceResolver`](/slides/python-net/vi/aspose.slides.importing/iexternalresourceresolver) | Một đối tượng callback được sử dụng để lấy các đối tượng bên ngoài. Nếu tham số này là None, tất cả các đối tượng bên ngoài sẽ bị bỏ qua. |
| base_uri | **str** | URI cơ sở của Svg đã chỉ định. Được sử dụng để giải quyết các liên kết tương đối. |

### Xem Thêm
* lớp [`IExternalResourceResolver`](/slides/python-net/vi/aspose.slides.importing/iexternalresourceresolver)
* lớp [`SvgImage`](/slides/python-net/vi/aspose.slides/svgimage)
* module [`aspose.slides`](/slides/python-net/vi/aspose.slides)
* thư viện [`Aspose.Slides`](/slides/python-net)