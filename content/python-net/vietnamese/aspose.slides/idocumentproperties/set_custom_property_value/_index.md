---
title: set_custom_property_value method
second_title: Tham chiếu API Aspose.Slides cho Python thông qua .NET
description: 
type: docs
url: /vi/aspose.slides/idocumentproperties/set_custom_property_value/
weight: 80
---
## set_custom_property_value(self, name, value) {#str-bool}
Đặt một thuộc tính tùy chỉnh kiểu bool có tên.

```python
def set_custom_property_value(self, name, value):
    ...
```

| Tham số | Kiểu | Mô tả |
| :- | :- | :- |
| name | **str** | Tên của thuộc tính tùy chỉnh để đặt |
| value | **bool** | Giá trị thuộc tính tùy chỉnh |

## set_custom_property_value(self, name, value) {#str-int}
Đặt một thuộc tính tùy chỉnh kiểu int có tên.

```python
def set_custom_property_value(self, name, value):
    ...
```

| Tham số | Kiểu | Mô tả |
| :- | :- | :- |
| name | **str** | Tên của thuộc tính tùy chỉnh để đặt |
| value | **int** | Giá trị thuộc tính tùy chỉnh |

## set_custom_property_value(self, name, value) {#str-datetime}
Đặt một thuộc tính tùy chỉnh kiểu DateTime có tên.

```python
def set_custom_property_value(self, name, value):
    ...
```

| Tham số | Kiểu | Mô tả |
| :- | :- | :- |
| name | **str** | Tên của thuộc tính tùy chỉnh để đặt |
| value | **DateTime** | Giá trị thuộc tính tùy chỉnh |

## set_custom_property_value(self, name, value) {#str-str}
Đặt một thuộc tính tùy chỉnh kiểu str có tên.

```python
def set_custom_property_value(self, name, value):
    ...
```

| Tham số | Kiểu | Mô tả |
| :- | :- | :- |
| name | **str** | Tên của thuộc tính tùy chỉnh để đặt |
| value | **str** | Giá trị thuộc tính tùy chỉnh |

## set_custom_property_value(self, name, value) {#str-float}
Đặt một thuộc tính tùy chỉnh kiểu float có tên.

```python
def set_custom_property_value(self, name, value):
    ...
```

| Tham số | Kiểu | Mô tả |
| :- | :- | :- |
| name | **str** | Tên của thuộc tính tùy chỉnh để đặt |
| value | **float** | Giá trị thuộc tính tùy chỉnh |

## set_custom_property_value(self, name, value) {#str-float}
Đặt một thuộc tính tùy chỉnh kiểu double có tên.

```python
def set_custom_property_value(self, name, value):
    ...
```

| Tham số | Kiểu | Mô tả |
| :- | :- | :- |
| name | **str** | Tên của thuộc tính tùy chỉnh để đặt |
| value | **float** | Giá trị thuộc tính tùy chỉnh |

### Xem thêm
* lớp [`IDocumentProperties`](/slides/python-net/vi/aspose.slides/idocumentproperties)
* mô-đun [`aspose.slides`](/slides/python-net/vi/aspose.slides)
* thư viện [`Aspose.Slides`](/slides/python-net)