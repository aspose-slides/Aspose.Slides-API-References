---
title: add_text method
second_title: Aspose.Slides for Python via .NET API 參考文件
description: 
type: docs
url: /zh-hant/aspose.slides.export/ihtmlgenerator/add_text/
weight: 30
---
## add_text(self, text) {#str}
將純文字新增至 html 檔案，並將特殊字元取代為 html 實體。換行符與空白字元不會被取代。

```python
def add_text(self, text):
    ...
```

| 參數 | 類型 | 描述 |
| :- | :- | :- |
| text | **str** | 要新增的文字。 |

## add_text(self, text) {#listchar}
將純文字新增至 html 檔案，並將特殊字元取代為 html 實體。換行符與空白字元不會被取代。

```python
def add_text(self, text):
    ...
```

| 參數 | 類型 | 描述 |
| :- | :- | :- |
| text | **List[char]** | 要新增的文字。 |

## add_text(self, text, start_index, length) {#listchar-int-int}
將純文字新增至 html 檔案，並將特殊字元取代為 html 實體。換行符與空白字元不會被取代。

```python
def add_text(self, text, start_index, length):
    ...
```

| 參數 | 類型 | 描述 |
| :- | :- | :- |
| text | **List[char]** | 要新增的文字。 |
| start_index | **int** | 要新增之部分的起始索引。 |
| length | **int** | 要新增之部分的長度。 |

### 另請參閱
* 類別 [`IHtmlGenerator`](/slides/python-net/zh-hant/aspose.slides.export/ihtmlgenerator)
* 模組 [`aspose.slides.export`](/slides/python-net/zh-hant/aspose.slides.export)
* 函式庫 [`Aspose.Slides`](/slides/python-net)