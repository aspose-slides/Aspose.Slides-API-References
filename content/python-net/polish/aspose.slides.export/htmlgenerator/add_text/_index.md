---
title: add_text method
second_title: Aspose.Slides dla Pythona via .NET - Dokumentacja API
description: 
type: docs
url: /pl/aspose.slides.export/htmlgenerator/add_text/
weight: 30
---
## add_text(self, text) {#str}
Dodaje zwykły tekst do plików html, zastępując znaki specjalne encjami html. Znaki końca linii i białe znaki nie są zastępowane.

```python
def add_text(self, text):
    ...
```

| Parametr | Typ | Opis |
| :- | :- | :- |
| text | **str** | Tekst do dodania. |

## add_text(self, text) {#listchar}
Dodaje zwykły tekst do plików html, zastępując znaki specjalne encjami html. Znaki końca linii i białe znaki nie są zastępowane.

```python
def add_text(self, text):
    ...
```

| Parametr | Typ | Opis |
| :- | :- | :- |
| text | **List[char]** | Tekst do dodania. |

## add_text(self, text, start_index, length) {#listchar-int-int}
Dodaje zwykły tekst do plików html, zastępując znaki specjalne encjami html. Znaki końca linii i białe znaki nie są zastępowane.

```python
def add_text(self, text, start_index, length):
    ...
```

| Parametr | Typ | Opis |
| :- | :- | :- |
| text | **List[char]** | Tekst do dodania. |
| start_index | **int** | Indeks początkowy części do dodania. |
| length | **int** | Długość części do dodania. |

### Zobacz także
* klasa [`HtmlGenerator`](/slides/python-net/pl/aspose.slides.export/htmlgenerator)
* moduł [`aspose.slides.export`](/slides/python-net/pl/aspose.slides.export)
* biblioteka [`Aspose.Slides`](/slides/python-net)