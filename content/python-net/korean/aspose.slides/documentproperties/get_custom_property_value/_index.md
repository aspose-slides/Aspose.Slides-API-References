---
title: get_custom_property_value method
second_title: Aspose.Slides for Python via .NET API 레퍼런스
description: 
type: docs
url: /ko/aspose.slides/documentproperties/get_custom_property_value/
weight: 80
---
## get_custom_property_value(self, name, value) {#str-any}
사용자 지정 속성에서 지정된 이름의 부울 값을 가져옵니다.

```python
def get_custom_property_value(self, name, value):
    ...
```

| 매개변수 | 형식 | 설명 |
| :- | :- | :- |
| name | **str** | 가져올 사용자 지정 속성의 이름 |
| value | **Any** | 사용자 지정 속성 값 |

## get_custom_property_value(self, name, value) {#str-any}
사용자 지정 속성에서 지정된 이름의 정수 값을 가져옵니다.

```python
def get_custom_property_value(self, name, value):
    ...
```

| 매개변수 | 형식 | 설명 |
| :- | :- | :- |
| name | **str** | 가져올 사용자 지정 속성의 이름 |
| value | **Any** | 사용자 지정 속성 값 |

## get_custom_property_value(self, name, value) {#str-any}
사용자 지정 속성에서 지정된 이름의 DateTime 값을 가져옵니다.

```python
def get_custom_property_value(self, name, value):
    ...
```

| 매개변수 | 형식 | 설명 |
| :- | :- | :- |
| name | **str** | 가져올 사용자 지정 속성의 이름 |
| value | **Any** | 사용자 지정 속성 값 |

## get_custom_property_value(self, name, value) {#str-any}
사용자 지정 속성에서 지정된 이름의 문자열 값을 가져옵니다.

```python
def get_custom_property_value(self, name, value):
    ...
```

| 매개변수 | 형식 | 설명 |
| :- | :- | :- |
| name | **str** | 가져올 사용자 지정 속성의 이름 |
| value | **Any** | 사용자 지정 속성 값 |

## get_custom_property_value(self, name, value) {#str-any}

```python
def get_custom_property_value(self, name, value):
    ...
```

| 매개변수 | 형식 | 설명 |
| :- | :- | :- |
| name | **str** |  |
| value | **Any** |  |

## get_custom_property_value(self, name, value) {#str-any}

```python
def get_custom_property_value(self, name, value):
    ...
```

| 매개변수 | 형식 | 설명 |
| :- | :- | :- |
| name | **str** |  |
| value | **Any** |  |

### 참고
* class [`DocumentProperties`](/slides/python-net/ko/aspose.slides/documentproperties)
* module [`aspose.slides`](/slides/python-net/ko/aspose.slides)
* library [`Aspose.Slides`](/slides/python-net)