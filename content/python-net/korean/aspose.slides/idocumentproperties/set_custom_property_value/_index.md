---
title: set_custom_property_value method
second_title: Aspose.Slides for Python via .NET API 레퍼런스
description: 
type: docs
url: /ko/aspose.slides/idocumentproperties/set_custom_property_value/
weight: 80
---
## set_custom_property_value(self, name, value) {#str-bool}
이름이 지정된 부울 사용자 정의 속성을 설정합니다.

```python
def set_custom_property_value(self, name, value):
    ...
```

| 매개변수 | 형식 | 설명 |
| :- | :- | :- |
| name | **str** | 설정할 사용자 정의 속성의 이름 |
| value | **bool** | 사용자 정의 속성 값 |

## set_custom_property_value(self, name, value) {#str-int}
이름이 지정된 정수 사용자 정의 속성을 설정합니다.

```python
def set_custom_property_value(self, name, value):
    ...
```

| 매개변수 | 형식 | 설명 |
| :- | :- | :- |
| name | **str** | 설정할 사용자 정의 속성의 이름 |
| value | **int** | 사용자 정의 속성 값 |

## set_custom_property_value(self, name, value) {#str-datetime}
이름이 지정된 DateTime 사용자 정의 속성을 설정합니다.

```python
def set_custom_property_value(self, name, value):
    ...
```

| 매개변수 | 형식 | 설명 |
| :- | :- | :- |
| name | **str** | 설정할 사용자 정의 속성의 이름 |
| value | **DateTime** | 사용자 정의 속성 값 |

## set_custom_property_value(self, name, value) {#str-str}
이름이 지정된 문자열 사용자 정의 속성을 설정합니다.

```python
def set_custom_property_value(self, name, value):
    ...
```

| 매개변수 | 형식 | 설명 |
| :- | :- | :- |
| name | **str** | 설정할 사용자 정의 속성의 이름 |
| value | **str** | 사용자 정의 속성 값 |

## set_custom_property_value(self, name, value) {#str-float}
이름이 지정된 float 사용자 정의 속성을 설정합니다.

```python
def set_custom_property_value(self, name, value):
    ...
```

| 매개변수 | 형식 | 설명 |
| :- | :- | :- |
| name | **str** | 설정할 사용자 정의 속성의 이름 |
| value | **float** | 사용자 정의 속성 값 |

## set_custom_property_value(self, name, value) {#str-float}
이름이 지정된 double 사용자 정의 속성을 설정합니다.

```python
def set_custom_property_value(self, name, value):
    ...
```

| 매개변수 | 형식 | 설명 |
| :- | :- | :- |
| name | **str** | 설정할 사용자 정의 속성의 이름 |
| value | **float** | 사용자 정의 속성 값 |

### 관련 항목
* 클래스 [`IDocumentProperties`](/slides/python-net/ko/aspose.slides/idocumentproperties)
* 모듈 [`aspose.slides`](/slides/python-net/ko/aspose.slides)
* 라이브러리 [`Aspose.Slides`](/slides/python-net)