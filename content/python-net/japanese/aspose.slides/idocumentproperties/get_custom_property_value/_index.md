---
title: get_custom_property_value method
second_title: Aspose.Slides for Python via .NET API リファレンス
description: 
type: docs
url: /ja/aspose.slides/idocumentproperties/get_custom_property_value/
weight: 50
---
## get_custom_property_value(self, name, value) {#str-any}
カスタム プロパティから名前付きのブール値を取得します。

```python
def get_custom_property_value(self, name, value):
    ...
```

| パラメータ | 型 | 説明 |
| :- | :- | :- |
| name | **str** | 取得するカスタム プロパティの名前 |
| value | **Any** | カスタム プロパティの値 |

## get_custom_property_value(self, name, value) {#str-any}
カスタム プロパティから名前付きの整数値を取得します。

```python
def get_custom_property_value(self, name, value):
    ...
```

| パラメータ | 型 | 説明 |
| :- | :- | :- |
| name | **str** | 取得するカスタム プロパティの名前 |
| value | **Any** | カスタム プロパティの値 |

## get_custom_property_value(self, name, value) {#str-any}
カスタム プロパティから名前付きの DateTime 値を取得します。

```python
def get_custom_property_value(self, name, value):
    ...
```

| パラメータ | 型 | 説明 |
| :- | :- | :- |
| name | **str** | 取得するカスタム プロパティの名前 |
| value | **Any** | カスタム プロパティの値 |

## get_custom_property_value(self, name, value) {#str-any}
カスタム プロパティから名前付きの文字列値を取得します。

```python
def get_custom_property_value(self, name, value):
    ...
```

| パラメータ | 型 | 説明 |
| :- | :- | :- |
| name | **str** | 取得するカスタム プロパティの名前 |
| value | **Any** | カスタム プロパティの値 |

## get_custom_property_value(self, name, value) {#str-any}

```python
def get_custom_property_value(self, name, value):
    ...
```

| パラメータ | 型 | 説明 |
| :- | :- | :- |
| name | **str** |  |
| value | **Any** |  |

## get_custom_property_value(self, name, value) {#str-any}

```python
def get_custom_property_value(self, name, value):
    ...
```

| パラメータ | 型 | 説明 |
| :- | :- | :- |
| name | **str** |  |
| value | **Any** |  |

### 参照
* クラス [`IDocumentProperties`](/slides/python-net/ja/aspose.slides/idocumentproperties)
* モジュール [`aspose.slides`](/slides/python-net/ja/aspose.slides)
* ライブラリ [`Aspose.Slides`](/slides/python-net)