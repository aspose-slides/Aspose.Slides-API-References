---
title: add_text method
second_title: Aspose.Slides لـ Python عبر .NET مرجع API
description: 
type: docs
url: /ar/aspose.slides.export/ihtmlgenerator/add_text/
weight: 30
---
## add_text(self, text) {#str}
يضيف نصًا عاديًا إلى ملفات html، مستبدلًا الأحرف الخاصة بكيانات html.
            لا يتم استبدال فواصل الأسطر والمسافات البيضاء.

```python
def add_text(self, text):
    ...
```

| معامل | نوع | وصف |
| :- | :- | :- |
| text | **str** | النص المراد إضافته. |

## add_text(self, text) {#listchar}
يضيف نصًا عاديًا إلى ملفات html، مستبدلًا الأحرف الخاصة بكيانات html.
            لا يتم استبدال فواصل الأسطر والمسافات البيضاء.

```python
def add_text(self, text):
    ...
```

| معامل | نوع | وصف |
| :- | :- | :- |
| text | **List[char]** | النص المراد إضافته. |

## add_text(self, text, start_index, length) {#listchar-int-int}
يضيف نصًا عاديًا إلى ملفات html، مستبدلًا الأحرف الخاصة بكيانات html.
            لا يتم استبدال فواصل الأسطر والمسافات البيضاء.

```python
def add_text(self, text, start_index, length):
    ...
```

| معامل | نوع | وصف |
| :- | :- | :- |
| text | **List[char]** | النص المراد إضافته. |
| start_index | **int** | فهرس البداية للجزء المراد إضافته. |
| length | **int** | طول الجزء المراد إضافته. |

### انظر أيضًا
* الفئة [`IHtmlGenerator`](/slides/python-net/ar/aspose.slides.export/ihtmlgenerator)
* الوحدة [`aspose.slides.export`](/slides/python-net/ar/aspose.slides.export)
* المكتبة [`Aspose.Slides`](/slides/python-net)