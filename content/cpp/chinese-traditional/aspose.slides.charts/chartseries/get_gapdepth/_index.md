---
title: get_GapDepth()
second_title: Aspose.Slides for C++ API 參考文件
description: 返回距離，以標記寬度的百分比表示，該距離位於 3D 圖表中的資料系列之間。此屬性不僅屬於此系列，也屬於父系列群組的所有系列——這是相應群組屬性的投影。因此此屬性為唯讀。使用 ParentSeriesGroup 屬性以存取父系列群組。使用 get_ParentSeriesGroup()->get(set)_GapDepth() 可讀寫屬性以變更值。唯讀 int32_t.
type: docs
weight: 651
url: /zh-hant/aspose.slides.charts/chartseries/get_gapdepth/
---
## ChartSeries::get_GapDepth() 方法

返回距離，以標記寬度的百分比表示，該距離位於 3D 圖表中的資料系列之間。此屬性不僅屬於此系列，也屬於父系列群組的所有系列——這是相應群組屬性的投影。因此此屬性為唯讀。使用 ParentSeriesGroup 屬性以存取父系列群組。使用 [get_ParentSeriesGroup()](../get_parentseriesgroup/)->get(set)_GapDepth() 可讀寫屬性以變更值。唯讀 **int32_t**。

```cpp
int32_t Aspose::Slides::Charts::ChartSeries::get_GapDepth() override
```

## 備註

這是屬性 [get_ParentSeriesGroup()](../get_parentseriesgroup/)->get(set)_GapDepth() 的投影。

## 另見

* 類別 [ChartSeries](../)
* 命名空間 [Aspose::Slides::Charts](../../)
* 函式庫 [Aspose.Slides](../../../)