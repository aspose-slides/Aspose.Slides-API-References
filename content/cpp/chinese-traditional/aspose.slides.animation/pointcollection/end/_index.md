---
title: end()
second_title: Aspose.Slides for C++ API 參考
description: 取得指向集合中最後一個元素（如果有）的迭代器，該迭代器位於最後一個元素之後。
type: docs
weight: 66
url: /zh-hant/aspose.slides.animation/pointcollection/end/
---
## PointCollection::end() 方法

取得指向集合中最後一個元素（如果有）的迭代器，該迭代器位於最後一個元素之後。

```cpp
iterator Aspose::Slides::Animation::PointCollection::end() noexcept
```

### 返回值

指向集合中最後一個元素（如果有）的迭代器，位於最後一個元素之後

## PointCollection::end() const 方法

取得指向集合的 const 限定實例中最後一個元素（如果有）的迭代器，該迭代器位於最後一個元素之後。

```cpp
const_iterator Aspose::Slides::Animation::PointCollection::end() const noexcept
```

### 返回值

指向集合的 const 限定實例中最後一個元素（如果有）的迭代器，位於最後一個元素之後

## 另見

* Typedef [iterator](../iterator/)
* Typedef [const_iterator](../const_iterator/)
* 類別 [PointCollection](../)
* 命名空間 [Aspose::Slides::Animation](../../)
* 函式庫 [Aspose.Slides](../../../)