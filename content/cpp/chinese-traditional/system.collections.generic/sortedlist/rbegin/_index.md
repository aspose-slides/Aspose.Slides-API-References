---
title: rbegin()
second_title: Aspose.Slides for C++ API 參考文件
description: 取得集合最後一個元素的反向迭代器（反向的第一個元素）。
type: docs
weight: 92
url: /zh-hant/system.collections.generic/sortedlist/rbegin/
---
## SortedList::rbegin() 方法

取得集合最後一個元素的反向迭代器（反向的第一個元素）。

```cpp
reverse_iterator System::Collections::Generic::SortedList<TKey, TValue>::rbegin() noexcept
```

### 返回值

指向映射中最後一個元素的反向迭代器（反向的第一個元素）。

## SortedList::rbegin() const 方法

取得 const 限定集合最後一個元素的反向迭代器（反向的第一個元素）。

```cpp
const_reverse_iterator System::Collections::Generic::SortedList<TKey, TValue>::rbegin() const noexcept
```

### 返回值

指向 const 限定映射中最後一個元素的反向迭代器（反向的第一個元素）。

## 另請參閱

* 型別別名 [reverse_iterator](../reverse_iterator/)
* 型別別名 [const_reverse_iterator](../const_reverse_iterator/)
* 類別 [SortedList](../)
* 名稱空間 [System::Collections::Generic](../../)
* 函式庫 [Aspose.Slides](../../../)