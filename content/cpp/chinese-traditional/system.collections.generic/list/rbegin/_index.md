---
title: rbegin()
second_title: Aspose.Slides for C++ API 參考
description: 取得集合中最後一個元素的反向迭代器（反向的第一個）。
type: docs
weight: 79
url: /zh-hant/system.collections.generic/list/rbegin/
---
## List::rbegin() 方法

取得集合中最後一個元素的反向迭代器（反向的第一個）。

```cpp
reverse_iterator System::Collections::Generic::List<T>::rbegin() noexcept
```

### 返回值

指向列表中最後一個元素的反向迭代器（反向的第一個）。

## List::rbegin() const 方法

取得 const 限定集合中最後一個元素的反向迭代器（反向的第一個）。

```cpp
const_reverse_iterator System::Collections::Generic::List<T>::rbegin() const noexcept
```

### 返回值

指向 const 限定列表中最後一個元素的反向迭代器（反向的第一個）。

## 另請參閱

* Typedef [reverse_iterator](../reverse_iterator/)
* Typedef [const_reverse_iterator](../const_reverse_iterator/)
* 類別 [List](../)
* 命名空間 [System::Collections::Generic](../../)
* Library [Aspose.Slides](../../../)