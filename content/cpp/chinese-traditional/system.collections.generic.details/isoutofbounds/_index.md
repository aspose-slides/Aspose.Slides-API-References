---
title: IsOutOfBounds()
second_title: Aspose.Slides C++ API 參考
description: 檢查索引是否超出容器範圍（不包括容器大小）。
type: docs
weight: 209
url: /zh-hant/system.collections.generic.details/isoutofbounds/
---
## System::Collections::Generic::Details::IsOutOfBounds(int, const Container\&) 函式

檢查索引是否超出容器範圍（不包括容器大小）。

```cpp
template<typename Container> bool System::Collections::Generic::Details::IsOutOfBounds(int idx, const Container &container)
```

### 範本參數

| 參數 | 說明 |
| --- | --- |
| Container | 容器類型。 |

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| idx | int | [Index](../../system/index/). |
| container | const Container\& | 用於檢查索引的容器。 |

### 返回值

如果索引超出容器範圍則返回 true，否則返回 false。

## System::Collections::Generic::Details::IsOutOfBounds(std::int64_t, const Container\&) 函式

檢查索引是否超出容器範圍（不包括容器大小）。

```cpp
template<typename Container> bool System::Collections::Generic::Details::IsOutOfBounds(std::int64_t idx, const Container &container)
```

### 範本參數

| 參數 | 說明 |
| --- | --- |
| Container | 容器類型。 |

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| idx | std::int64_t | [Index](../../system/index/). |
| container | const Container\& | 用於檢查索引的容器。 |

### 返回值

如果索引超出容器範圍則返回 true，否則返回 false。

## 另見

* Namespace [System::Collections::Generic::Details](../)
* Library [Aspose.Slides](../../)