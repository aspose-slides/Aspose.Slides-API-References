---
title: Equals()
second_title: Aspose.Slides for C++ API 參考
description: 檢查兩個 DateTimeOffset 物件是否代表相同的時間點。
type: docs
weight: 365
url: /zh-hant/system/datetimeoffset/equals/
---
## DateTimeOffset::Equals(const DateTimeOffset\&) const method

檢查兩個 [DateTimeOffset](../) 物件是否代表相同的時間點。

```cpp
bool System::DateTimeOffset::Equals(const DateTimeOffset &other) const
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/) 用於比較。 |

### 傳回值

若目前物件等於 **other**，則傳回 true，否則傳回 false。

## DateTimeOffset::Equals(const SharedPtr\<Object\>\&) const method

檢查兩個 [DateTimeOffset](../) 物件是否代表相同的時間點。

```cpp
bool System::DateTimeOffset::Equals(const SharedPtr<Object> &obj) const
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/) 用於比較。 |

### 傳回值

若目前物件等於 **obj**，則傳回 true，否則傳回 false。

## DateTimeOffset::Equals(const DateTimeOffset\&, const DateTimeOffset\&) method

檢查兩個 [DateTimeOffset](../) 物件是否代表相同的時間點。

```cpp
static bool System::DateTimeOffset::Equals(const DateTimeOffset &first, const DateTimeOffset &second)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| first | const [DateTimeOffset](../)\& | 第一個要比較的物件。 |
| second | const [DateTimeOffset](../)\& | 第二個要比較的物件。 |

### 傳回值

若 **first** 等於 **second**，則傳回 true，否則傳回 false。

## 另請參閱

* 型別定義 [SharedPtr](../../sharedptr/)
* 類別 [DateTimeOffset](../)
* 類別 [Object](../../object/)
* 命名空間 [System](../../)
* 函式庫 [Aspose.Slides](../../../)