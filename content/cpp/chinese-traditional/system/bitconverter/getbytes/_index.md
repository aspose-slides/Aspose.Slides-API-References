---
title: GetBytes()
second_title: Aspose.Slides for C++ API 參考文件
description: 將指定的布林值轉換為位元組陣列。
type: docs
weight: 14
url: /zh-hant/system/bitconverter/getbytes/
---
## BitConverter::GetBytes(bool) 方法

將指定的 boolean 值轉換為位元組陣列。

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(bool value)
```

### 參數

| Parameter | Type | Description |
| --- | --- | --- |
| value | **bool** | 要轉換的 boolean 值 |

### 傳回值

1-byte array representing specified value

## BitConverter::GetBytes(char_t) 方法

將指定的 char_t 值轉換為位元組陣列。

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(char_t value)
```

### 參數

| Parameter | Type | Description |
| --- | --- | --- |
| value | char_t | 要轉換的 char_t 值 |

### 傳回值

2-byte array representing specified value

## BitConverter::GetBytes(int16_t) 方法

將指定的 16-bit integer 值轉換為位元組陣列。

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int16_t value)
```

### 參數

| Parameter | Type | Description |
| --- | --- | --- |
| value | **int16_t** | 要轉換的 16-bit integer 值 |

### 傳回值

2-byte array representing specified value

## BitConverter::GetBytes(int) 方法

將指定的 32-bit integer 值轉換為位元組陣列。

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int value)
```

### 參數

| Parameter | Type | Description |
| --- | --- | --- |
| value | int | 要轉換的 32-bit integer 值 |

### 傳回值

4-byte array representing specified value

## BitConverter::GetBytes(int64_t) 方法

將指定的 64-bit integer 值轉換為位元組陣列。

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int64_t value)
```

### 參數

| Parameter | Type | Description |
| --- | --- | --- |
| value | **int64_t** | 要轉換的 64-bit integer 值 |

### 傳回值

8-byte array representing specified value

## BitConverter::GetBytes(uint16_t) 方法

將指定的 unsigned 16-bit integer 值轉換為位元組陣列。

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint16_t value)
```

### 參數

| Parameter | Type | Description |
| --- | --- | --- |
| value | **uint16_t** | 要轉換的 unsigned 16-bit integer 值 |

### 傳回值

2-byte array representing specified value

## BitConverter::GetBytes(uint32_t) 方法

將指定的 unsigned 32-bit integer 值轉換為位元組陣列。

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint32_t value)
```

### 參數

| Parameter | Type | Description |
| --- | --- | --- |
| value | **uint32_t** | 要轉換的 unsigned 32-bit integer 值 |

### 傳回值

4-byte array representing specified value

## BitConverter::GetBytes(uint64_t) 方法

將指定的 unsigned 64-bit integer 值轉換為位元組陣列。

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint64_t value)
```

### 參數

| Parameter | Type | Description |
| --- | --- | --- |
| value | **uint64_t** | 要轉換的 unsigned 64-bit integer 值 |

### 傳回值

8-byte array representing specified value

## BitConverter::GetBytes(float) 方法

將指定的 single-precision floating-point 值轉換為位元組陣列。

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(float value)
```

### 參數

| Parameter | Type | Description |
| --- | --- | --- |
| value | **float** | 要轉換的 floating-point 值 |

### 傳回值

4-byte array representing specified value

## BitConverter::GetBytes(double) 方法

將指定的 double-precision floating-point 值轉換為位元組陣列。

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(double value)
```

### 參數

| Parameter | Type | Description |
| --- | --- | --- |
| value | **double** | 要轉換的 floating-point 值 |

### 傳回值

8-byte array representing specified value

## 另見

* Typedef [ArrayPtr](../../arrayptr/)
* 類別 [BitConverter](../)
* 命名空間 [System](../../)
* 函式庫 [Aspose.Slides](../../../)