---
title: operator<<()
second_title: Aspose.Slides for C++ API 參考
description: 使用 UTF-8 編碼將資料插入串流。
type: docs
weight: 2159
url: /zh-hant/system/operator_less_less/
---
## System::operator<<(std::ostream\&, DateTime) 函式

使用 UTF-8 編碼將資料插入串流。

```cpp
std::ostream & System::operator<<(std::ostream &stream, DateTime date_time)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::ostream\& | 用於插入資料的輸出串流。 |
| date_time | [DateTime](../datetime/) | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::wostream\&, DateTime) 函式

將資料插入串流。

```cpp
std::wostream & System::operator<<(std::wostream &stream, DateTime date_time)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::wostream\& | 用於插入資料的輸出串流。 |
| date_time | [DateTime](../datetime/) | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::ostream\&, DateTimeOffset) 函式

使用 UTF-8 編碼將資料插入串流。

```cpp
std::ostream & System::operator<<(std::ostream &stream, DateTimeOffset value)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::ostream\& | 用於插入資料的輸出串流。 |
| value | [DateTimeOffset](../datetimeoffset/) | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::wostream\&, DateTimeOffset) 函式

將資料插入串流。

```cpp
std::wostream & System::operator<<(std::wostream &stream, DateTimeOffset value)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::wostream\& | 用於插入資料的輸出串流。 |
| value | [DateTimeOffset](../datetimeoffset/) | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::ostream\&, const Decimal\&) 函式

使用 UTF-8 編碼將資料插入串流。

```cpp
std::ostream & System::operator<<(std::ostream &stream, const Decimal &decimal)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::ostream\& | 用於插入資料的輸出串流。 |
| decimal | const [Decimal](../decimal/)\& | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::wostream\&, const Decimal\&) 函式

將資料插入串流。

```cpp
std::wostream & System::operator<<(std::wostream &stream, const Decimal &decimal)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::wostream\& | 用於插入資料的輸出串流。 |
| decimal | const [Decimal](../decimal/)\& | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::ostream\&, const Exception\&) 函式

使用 UTF-8 編碼將資料插入串流。

```cpp
std::ostream & System::operator<<(std::ostream &stream, const Exception &exception)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::ostream\& | 用於插入資料的輸出串流。 |
| exception | const [Exception](../exception/)\& | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::wostream\&, const Exception\&) 函式

將資料插入串流。

```cpp
std::wostream & System::operator<<(std::wostream &stream, const Exception &exception)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::wostream\& | 用於插入資料的輸出串流。 |
| exception | const [Exception](../exception/)\& | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::ostream\&, const Guid\&) 函式

使用 UTF-8 編碼將資料插入串流。

```cpp
std::ostream & System::operator<<(std::ostream &stream, const Guid &guid)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::ostream\& | 用於插入資料的輸出串流。 |
| guid | const [Guid](../guid/)\& | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::wostream\&, const Guid\&) 函式

將資料插入串流。

```cpp
std::wostream & System::operator<<(std::wostream &stream, const Guid &guid)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::wostream\& | 用於插入資料的輸出串流。 |
| guid | const [Guid](../guid/)\& | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::ostream\&, const Nullable\<T\>\&) 函式

使用 UTF-8 編碼將資料插入串流。

```cpp
template<typename T> std::ostream & System::operator<<(std::ostream &stream, const Nullable<T> &value)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::ostream\& | 用於插入資料的輸出串流。 |
| value | const [Nullable](../nullable/)\<T\>\& | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::wostream\&, const Nullable\<T\>\&) 函式

將資料插入串流。

```cpp
template<typename T> std::wostream & System::operator<<(std::wostream &stream, const Nullable<T> &value)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::wostream\& | 用於插入資料的輸出串流。 |
| value | const [Nullable](../nullable/)\<T\>\& | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::ostream\&, const System::Object\&) 函式

使用 UTF-8 編碼將資料插入串流。

```cpp
std::ostream & System::operator<<(std::ostream &stream, const System::Object &object)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::ostream\& | 用於插入資料的輸出串流。 |
| object | const [System::Object](../object/)\& | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::wostream\&, const System::Object\&) 函式

將資料插入串流。

```cpp
std::wostream & System::operator<<(std::wostream &stream, const System::Object &object)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::wostream\& | 用於插入資料的輸出串流。 |
| object | const [System::Object](../object/)\& | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::ostream\&, const OperatingSystem\&) 函式

使用 UTF-8 編碼將資料插入串流。

```cpp
std::ostream & System::operator<<(std::ostream &stream, const OperatingSystem &os)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::ostream\& | 用於插入資料的輸出串流。 |
| os | const [OperatingSystem](../operatingsystem/)\& | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::wostream\&, const OperatingSystem\&) 函式

將資料插入串流。

```cpp
std::wostream & System::operator<<(std::wostream &stream, const OperatingSystem &os)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::wostream\& | 用於插入資料的輸出串流。 |
| os | const [OperatingSystem](../operatingsystem/)\& | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::ostream\&, const SharedPtr\<T\>\&) 函式

使用 UTF-8 編碼將資料插入串流。

```cpp
template<typename T> std::ostream & System::operator<<(std::ostream &stream, const SharedPtr<T> &object_ptr)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::ostream\& | 用於插入資料的輸出串流。 |
| object_ptr | const [SharedPtr](../sharedptr/)\<T\>\& | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::wostream\&, const SharedPtr\<T\>\&) 函式

將資料插入串流。

```cpp
template<typename T> std::wostream & System::operator<<(std::wostream &stream, const SharedPtr<T> &object_ptr)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::wostream\& | 用於插入資料的輸出串流。 |
| object_ptr | const [SharedPtr](../sharedptr/)\<T\>\& | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::ostream\&, const String\&) 函式

使用 UTF-8 編碼將字串輸出至輸出串流。

```cpp
std::ostream & System::operator<<(std::ostream &os, const String &str)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| os | std::ostream\& | **basic_ostream**（**char**）的輸出串流物件。 |
| str | const [String](../string/)\& | 要傳送至輸出串流的字串。 |

### 回傳值

已加入字串的輸出串流。

## System::operator<<(std::wostream\&, const String\&) 函式

將字串輸出至輸出串流。

```cpp
std::wostream & System::operator<<(std::wostream &os, const String &str)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| os | std::wostream\& | **basic_ostream**（**wchar_t**）的輸出串流物件。 |
| str | const [String](../string/)\& | 要傳送至輸出串流的字串。 |

### 回傳值

已加入字串的輸出串流。

## System::operator<<(std::ostream\&, TimeSpan) 函式

使用 UTF-8 編碼將資料插入串流。

```cpp
std::ostream & System::operator<<(std::ostream &stream, TimeSpan time_span)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::ostream\& | 用於插入資料的輸出串流。 |
| time_span | [TimeSpan](../timespan/) | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::wostream\&, TimeSpan) 函式

將資料插入串流。

```cpp
std::wostream & System::operator<<(std::wostream &stream, TimeSpan time_span)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::wostream\& | 用於插入資料的輸出串流。 |
| time_span | [TimeSpan](../timespan/) | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::ostream\&, const TypeInfo\&) 函式

使用 UTF-8 編碼將資料插入串流。

```cpp
std::ostream & System::operator<<(std::ostream &stream, const TypeInfo &type_info)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::ostream\& | 用於插入資料的輸出串流。 |
| type_info | const [TypeInfo](../typeinfo/)\& | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::wostream\&, const TypeInfo\&) 函式

將資料插入串流。

```cpp
std::wostream & System::operator<<(std::wostream &stream, const TypeInfo &type_info)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::wostream\& | 用於插入資料的輸出串流。 |
| type_info | const [TypeInfo](../typeinfo/)\& | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::ostream\&, const Version\&) 函式

使用 UTF-8 編碼將資料插入串流。

```cpp
std::ostream & System::operator<<(std::ostream &stream, const Version &version)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::ostream\& | 用於插入資料的輸出串流。 |
| version | const [Version](../version/)\& | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::wostream\&, const Version\&) 函式

將資料插入串流。

```cpp
std::wostream & System::operator<<(std::wostream &stream, const Version &version)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::wostream\& | 用於插入資料的輸出串流。 |
| version | const [Version](../version/)\& | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::ostream\&, const WeakPtr\<T\>\&) 函式

使用 UTF-8 編碼將資料插入串流。

```cpp
template<typename T> std::ostream & System::operator<<(std::ostream &stream, const WeakPtr<T> &object_ptr)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::ostream\& | 用於插入資料的輸出串流。 |
| object_ptr | const [WeakPtr](../weakptr/)\<T\>\& | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## System::operator<<(std::wostream\&, const WeakPtr\<T\>\&) 函式

將資料插入串流。

```cpp
template<typename T> std::wostream & System::operator<<(std::wostream &stream, const WeakPtr<T> &object_ptr)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| stream | std::wostream\& | 用於插入資料的輸出串流。 |
| object_ptr | const [WeakPtr](../weakptr/)\<T\>\& | [Data](../../system.data/) 插入。 |

### 回傳值

**stream**。

## 另請參閱

* Typedef [Exception](../exception/)
* Typedef [SharedPtr](../sharedptr/)
* 類別 [DateTime](../datetime/)
* 類別 [DateTimeOffset](../datetimeoffset/)
* 類別 [Decimal](../decimal/)
* 類別 [Guid](../guid/)
* 類別 [Nullable](../nullable/)
* 類別 [Object](../object/)
* 類別 [OperatingSystem](../operatingsystem/)
* 類別 [String](../string/)
* 類別 [TimeSpan](../timespan/)
* 類別 [TypeInfo](../typeinfo/)
* 類別 [Version](../version/)
* 類別 [WeakPtr](../weakptr/)
* 命名空間 [System](../)
* Library [Aspose.Slides](../../)