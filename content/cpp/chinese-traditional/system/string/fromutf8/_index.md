---
title: FromUtf8()
second_title: Aspose.Slides for C++ API 參考文件
description: 從 utf8 字串建立 String。
type: docs
weight: 898
url: /zh-hant/system/string/fromutf8/
---
## String::FromUtf8(const char *) 方法

從 utf8 字串建立 [String](../)。

```cpp
static String System::String::FromUtf8(const char *utf8)
```

### 參數

| Parameter | Type | Description |
| --- | --- | --- |
| utf8 | const char * | 指向使用 utf8 編碼頁的 null 終止字串的指標。 |

### 返回值

[String](../) 物件，表示傳入的字串。

## String::FromUtf8(const char *, int) 方法

從 utf8 字串建立 [String](../)。

```cpp
static String System::String::FromUtf8(const char *utf8, int len)
```

### 參數

| Parameter | Type | Description |
| --- | --- | --- |
| utf8 | const char * | 指向使用 utf8 編碼頁的字串的指標。 |
| len | int | 要處理的字元數。 |

### 返回值

[String](../) 物件，表示傳入的字串。

## String::FromUtf8(const uint8_t *) 方法

從 utf8 字串建立 [String](../)。

```cpp
static String System::String::FromUtf8(const uint8_t *utf8)
```

### 參數

| Parameter | Type | Description |
| --- | --- | --- |
| utf8 | const **uint8_t** * | 指向使用 utf8 編碼頁的 null 終止字串的指標。 |

### 返回值

[String](../) 物件，表示傳入的字串。

## String::FromUtf8(const std::string\&) 方法

從 utf8 字串建立 [String](../)。

```cpp
static String System::String::FromUtf8(const std::string &utf8)
```

### 參數

| Parameter | Type | Description |
| --- | --- | --- |
| utf8 | const std::string\& | 指向使用 utf8 編碼頁的字串的指標。 |

### 返回值

[String](../) 物件，表示傳入的字串。

## 另請參閱

* 類別 [String](../)
* 命名空間 [System](../../)
* 函式庫 [Aspose.Slides](../../../)