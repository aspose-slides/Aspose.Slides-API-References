---
title: ToString()
second_title: Aspose.Slides for C++ API 參考
description: 在對值類型物件呼叫 ToString() 的情況下，用於處理 String 類的封裝器。
type: docs
weight: 365
url: /zh-hant/system/string/tostring/
---
## String::ToString() const 方法

在對值類型對象調用 [ToString()](./) 時，用於處理 [String](../) 類的封裝器。

```cpp
String System::String::ToString() const
```

### 回傳值

目前字串的副本。如果目前字串為空字串，則被視為空字串。

## String::ToString(const SharedPtr\<IFormatProvider\>\&) const 方法

在對值類型對象調用 [ToString()](./) 時，用於處理 [String](../) 類的封裝器。

```cpp
String System::String::ToString(const SharedPtr<IFormatProvider> &) const
```

### 回傳值

目前字串的副本。如果目前字串為空字串，則被視為空字串。

## 另請參閱

* Typedef [SharedPtr](../../sharedptr/)
* 類別 [String](../)
* 類別 [IFormatProvider](../../iformatprovider/)
* 命名空間 [System](../../)
* Library [Aspose.Slides](../../../)