---
title: IsLower()
second_title: Aspose.Slides C++ API 參考
description: 判斷指定字元緩衝區中指定索引處的字元是否被歸類為小寫字母。
type: docs
weight: 196
url: /zh-hant/system/char/islower/
---
## Char::IsLower(const char_t *, int) 方法


判斷指定字元緩衝區中指定索引處的字元是否被歸類為小寫字母。

```cpp
static bool System::Char::IsLower(const char_t *str, int idx)
```


### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| str | const char_t * | 指向字元緩衝區開頭的指標 |
| idx | int | 在指定緩衝區中要測試的字元的零基索引 |

### 回傳值

如果指定索引處的字元是小寫字母則回傳 True，否則回傳 false

## Char::IsLower(char_t) 方法


判斷指定的字元是否被歸類為小寫字母。

```cpp
static bool System::Char::IsLower(char_t c)
```


### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| c | char_t | 要測試的字元 |

### 回傳值

如果指定的字元是小寫字母則回傳 True，否則回傳 false

## Char::IsLower(const String\&, int) 方法


判斷指定字串中指定索引處的字元是否被歸類為小寫字母。

```cpp
static bool System::Char::IsLower(const String &str, int idx)
```


### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| str | const [String](../../string/)\& | 字串 |
| idx | int | 在指定字串中要測試的字元的零基索引 |

### 回傳值

如果指定索引處的字元是小寫字母則回傳 True，否則回傳 false

## 另請參閱

* 類別 [Char](../)
* 類別 [String](../../string/)
* 命名空間 [System](../../)
* 程式庫 [Aspose.Slides](../../../)