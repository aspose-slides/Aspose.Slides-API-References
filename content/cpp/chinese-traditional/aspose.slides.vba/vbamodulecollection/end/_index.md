---
title: end()
second_title: Aspose.Slides for C++ API 參考
description: 取得指向集合中最後一個元素（如果有）的迭代器，位置在其之後。
type: docs
weight: 118
url: /zh-hant/aspose.slides.vba/vbamodulecollection/end/
---
## VbaModuleCollection::end() 方法

取得指向集合中最後一個元素（如果有）的迭代器，位置在其之後。

```cpp
iterator Aspose::Slides::Vba::VbaModuleCollection::end() noexcept
```

### 返回值

一個指向集合中最後一個元素（如果有）的迭代器，位置在其之後。

## VbaModuleCollection::end() const 方法

取得指向常量限定的集合實例中最後一個元素（如果有）的迭代器，位置在其之後。

```cpp
const_iterator Aspose::Slides::Vba::VbaModuleCollection::end() const noexcept
```

### 返回值

一個指向常量限定的集合實例中最後一個元素（如果有）的迭代器，位置在其之後。

## 另請參閱

* 型別別名 [iterator](../iterator/)
* 型別別名 [const_iterator](../const_iterator/)
* 類別 [VbaModuleCollection](../)
* 命名空間 [Aspose::Slides::Vba](../../)
* 函式庫 [Aspose.Slides](../../../)