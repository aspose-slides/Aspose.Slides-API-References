---
title: end()
second_title: Aspose.Slides C++ API 參考文件
description: 取得指向集合中最後一個元素（若有）之後的位置的迭代器。
type: docs
weight: 144
url: /zh-hant/aspose.slides/customxmlpartcollection/end/
---
## CustomXmlPartCollection::end() 方法

取得指向集合中最後一個元素（如果有）之後的位置的迭代器。

```cpp
iterator Aspose::Slides::CustomXmlPartCollection::end() noexcept
```

### 返回值

指向集合中最後一個元素（如果有）之後的位置的迭代器

## CustomXmlPartCollection::end() const 方法

取得指向常量限定之集合實例中最後一個元素（如果有）之後的位置的迭代器。

```cpp
const_iterator Aspose::Slides::CustomXmlPartCollection::end() const noexcept
```

### 返回值

指向常量限定之集合實例中最後一個元素（如果有）之後的位置的迭代器

## 另請參閱

* Typedef [iterator](../iterator/)
* Typedef [const_iterator](../const_iterator/)
* 類別 [CustomXmlPartCollection](../)
* 命名空間 [Aspose::Slides](../../)
* 函式庫 [Aspose.Slides](../../../)