---
title: crend()
second_title: Aspose.Slides for C++ API 參考
description: 返回一個反向迭代器，指向反轉容器最後一個元素之後的元素。它對應於非反轉容器第一個元素之前的元素。此元素充當佔位符，試圖存取它會導致未定義的行為。
type: docs
weight: 300
url: /zh-hant/system.collections.specialized/stringcollection/crend/
---
## StringCollection::crend() const 方法

返回一個反向迭代器，指向反轉容器最後一個元素之後的元素。它對應於非反轉容器第一個元素之前的元素。此元素充當佔位符，試圖存取它會導致未定義的行為。

```cpp
const_reverse_iterator System::Collections::Specialized::StringCollection::crend() const noexcept
```

### 返回值

指向容器第一個元素之前的理論上 const 限定元素的迭代器。

## 另見

* Typedef [const_reverse_iterator](../const_reverse_iterator/)
* 類別 [StringCollection](../)
* 命名空間 [System::Collections::Specialized](../../)
* 函式庫 [Aspose.Slides](../../../)