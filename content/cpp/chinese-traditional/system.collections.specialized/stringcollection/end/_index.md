---
title: end()
second_title: Aspose.Slides for C++ API 參考
description: 返回一個迭代器，指向容器中最後一個元素之後的元素。此元素作為佔位符；嘗試存取它會導致未定義的行為。
type: docs
weight: 235
url: /zh-hant/system.collections.specialized/stringcollection/end/
---
## StringCollection::end() 方法

返回一個迭代器，指向容器中最後一個元素之後的元素。此元素作為佔位符；嘗試存取它會導致未定義的行為。

```cpp
iterator System::Collections::Specialized::StringCollection::end() noexcept
```

### 返回值

指向放置於容器最後一個元素之後的理論元素的迭代器。

## StringCollection::end() const 方法

返回一個迭代器，指向 const 限定容器中最後一個元素之後的元素。此元素作為佔位符；嘗試存取它會導致未定義的行為。

```cpp
const_iterator System::Collections::Specialized::StringCollection::end() const noexcept
```

### 返回值

指向放置於 const 限定容器最後一個元素之後的理論元素的迭代器。

## 另見

* Typedef [iterator](../iterator/)
* Typedef [const_iterator](../const_iterator/)
* 類別 [StringCollection](../)
* 命名空間 [System::Collections::Specialized](../../)
* 函式庫 [Aspose.Slides](../../../)