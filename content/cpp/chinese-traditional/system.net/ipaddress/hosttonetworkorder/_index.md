---
title: HostToNetworkOrder()
second_title: Aspose.Slides for C++ API 參考文件
description: 將指定的主機位元組順序轉換為相應的網路位元組順序。
type: docs
weight: 248
url: /zh-hant/system.net/ipaddress/hosttonetworkorder/
---
## IPAddress::HostToNetworkOrder(int64_t) 方法

將指定的主機位元組順序轉換為相應的網路位元組順序。

```cpp
static int64_t System::Net::IPAddress::HostToNetworkOrder(int64_t host)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| host | **int64_t** | 要轉換的值。 |

### 回傳值

相應的網路位元組順序。

## IPAddress::HostToNetworkOrder(int32_t) 方法

將指定的主機位元組順序轉換為相應的網路位元組順序。

```cpp
static int32_t System::Net::IPAddress::HostToNetworkOrder(int32_t host)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| host | **int32_t** | 要轉換的值。 |

### 回傳值

相應的網路位元組順序。

## IPAddress::HostToNetworkOrder(int16_t) 方法

將指定的主機位元組順序轉換為相應的網路位元組順序。

```cpp
static int16_t System::Net::IPAddress::HostToNetworkOrder(int16_t host)
```

### 參數

| 參數 | 類型 | 說明 |
| --- | --- | --- |
| host | **int16_t** | 要轉換的值。 |

### 回傳值

相應的網路位元組順序。

## 另請參閱

* 類別 [IPAddress](../)
* 命名空間 [System::Net](../../)
* 函式庫 [Aspose.Slides](../../../)