---
title: rbegin()
second_title: Aspose.Slides สำหรับ C++ API Reference
description: รับตัวทำซ้ำย้อนกลับไปยังองค์ประกอบสุดท้ายของคอลเลกชัน (แรกในลำดับย้อนกลับ).
type: docs
weight: 287
url: /th/system.collections.generic/linkedlist/rbegin/
---
## LinkedList::rbegin() เมธอด

รับตัวทำซ้ำย้อนกลับไปยังองค์ประกอบสุดท้ายของคอลเลกชัน (แรกในลำดับย้อนกลับ).

```cpp
reverse_iterator System::Collections::Generic::LinkedList<T>::rbegin() noexcept
```

### ค่ารีเทิร์น

ตัวทำซ้ำย้อนกลับที่ชี้ไปยังองค์ประกอบสุดท้ายในลิสต์เชื่อมโยง (แรกในลำดับย้อนกลับ).

## LinkedList::rbegin() const เมธอด

รับตัวทำซ้ำย้อนกลับไปยังองค์ประกอบสุดท้ายของคอลเลกชันที่กำหนด const (แรกในลำดับย้อนกลับ).

```cpp
const_reverse_iterator System::Collections::Generic::LinkedList<T>::rbegin() const noexcept
```

### ค่ารีเทิร์น

ตัวทำซ้ำย้อนกลับที่ชี้ไปยังองค์ประกอบสุดท้ายในลิสต์เชื่อมโยงที่กำหนด const (แรกในลำดับย้อนกลับ).

## ดูเพิ่มเติม

* Typedef [reverse_iterator](../reverse_iterator/)
* Typedef [const_reverse_iterator](../const_reverse_iterator/)
* คลาส [LinkedList](../)
* เนมสเปซ [System::Collections::Generic](../../)
* ไลบรารี [Aspose.Slides](../../../)