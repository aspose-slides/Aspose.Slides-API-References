---
title: operator[]()
second_title: อ้างอิง API ของ Aspose.Slides สำหรับ C++
description: โอเปอเรเตอร์เข้าถึงเพื่อทำงานกับการแปลงประเภทของคีย์
type: docs
weight: 14
url: /th/system.collections.generic/dictionaryptr/operator[]/
---
## DictionaryPtr::operator[](const X\&) เมธอดคอนสแตนท์

โอเปอเรเตอร์เข้าถึงเพื่อทำงานกับการแปลงประเภทคีย์

```cpp
template<class X> V & System::Collections::Generic::DictionaryPtr<T, V>::operator[](const X &key) const
```

### พารามิเตอร์แม่แบบ

| พารามิเตอร์ | คำอธิบาย |
| --- | --- |
| X | ประเภทคีย์ต้นฉบับ. |

### อาร์กิวเมนต์

| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| key | const X\& | [Dictionary](../../dictionary/) คีย์. |

### ค่าที่ส่งคืน

อ้างอิงถึงค่าที่สอดคล้องกับคีย์ที่ส่งเข้ามา, ไม่ว่าจะเป็นค่าที่มีอยู่แล้วหรือค่าที่สร้างใหม่

## DictionaryPtr::operator[](const T\&) เมธอดคอนสแตนท์

โอเปอเรเตอร์เข้าถึง

```cpp
V & System::Collections::Generic::DictionaryPtr<T, V>::operator[](const T &key) const
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| key | const T\& | [Dictionary](../../dictionary/) คีย์. |

### ค่าที่ส่งคืน

อ้างอิงถึงค่าที่สอดคล้องกับคีย์ที่ส่งเข้ามา, ไม่ว่าจะเป็นค่าที่มีอยู่แล้วหรือค่าที่สร้างใหม่

## ดูเพิ่มเติม

* คลาส [DictionaryPtr](../)
* เนมสเปซ [System::Collections::Generic](../../)
* ไลบรารี [Aspose.Slides](../../../)