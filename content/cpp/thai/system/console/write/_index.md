---
title: Write()
second_title: Aspose.Slides สำหรับการอ้างอิง API ของ C++
description: แสดงการแปลงเป็นสตริงของอ็อบเจกต์ที่ระบุไปยังสตรีมผลลัพธ์มาตรฐาน
type: docs
weight: 1
url: /th/system/console/write/
---
## Console::Write(const SharedPtr\<T\>\&) เมธอด

แสดงการแปลงเป็นสตริงของอ็อบเจกต์ที่ระบุไปยังสตรีมผลลัพธ์มาตรฐาน

```cpp
template<class T> static void System::Console::Write(const SharedPtr<T> &object)
```

### พารามิเตอร์แม่แบบ

| พารามิเตอร์ | คำอธิบาย |
| --- | --- |
| T | ประเภทของอ็อบเจกต์ที่ส่งออก |

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| object | const [SharedPtr](../../sharedptr/)\<T\>\& | [Object](../../object/) เพื่อส่งออก |

## Console::Write(bool) เมธอด

แสดงการแปลงเป็นสตริงของค่า bool ไปยังสตรีมผลลัพธ์มาตรฐาน

```cpp
static void System::Console::Write(bool value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **bool** | ค่าที่จะส่งออก |

## Console::Write(char_t) เมธอด

แสดงการแปลงเป็นสตริงของอักขระที่ระบุไปยังสตรีมผลลัพธ์มาตรฐาน

```cpp
static void System::Console::Write(char_t value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | char_t | ค่าที่จะส่งออก |

## Console::Write(const ArrayPtr\<char_t\>\&) เมธอด

แสดงการแปลงเป็นสตริงของอาร์เรย์อักขระที่ระบุไปยังสตรีมผลลัพธ์มาตรฐาน

```cpp
static void System::Console::Write(const ArrayPtr<char_t> &buffer)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | อาร์เรย์ที่จะส่งออก |

## Console::Write(const Decimal\&) เมธอด

แสดงการแปลงเป็นสตริงของค่า [Decimal](../../decimal/) ไปยังสตรีมผลลัพธ์มาตรฐาน

```cpp
static void System::Console::Write(const Decimal &value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | ค่าที่จะส่งออก |

## Console::Write(double) เมธอด

แสดงการแปลงเป็นสตริงของค่าทศนิยมความแม่นยำคู่ไปยังสตรีมผลลัพธ์มาตรฐาน

```cpp
static void System::Console::Write(double value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **double** | ค่าที่จะส่งออก |

## Console::Write(float) เมธอด

แสดงการแปลงเป็นสตริงของค่าทศนิยมความแม่นยำเดี่ยวไปยังสตรีมผลลัพธ์มาตรฐาน

```cpp
static void System::Console::Write(float value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **float** | ค่าที่จะส่งออก |

## Console::Write(int32_t) เมธอด

แสดงการแปลงเป็นสตริงของค่าเต็ม 32 บิตไปยังสตรีมผลลัพธ์มาตรฐาน

```cpp
static void System::Console::Write(int32_t value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **int32_t** | ค่าที่จะส่งออก |

## Console::Write(int64_t) เมธอด

แสดงการแปลงเป็นสตริงของค่าเต็ม 64 บิตไปยังสตรีมผลลัพธ์มาตรฐาน

```cpp
static void System::Console::Write(int64_t value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **int64_t** | ค่าที่จะส่งออก |

## Console::Write(const String\&) เมธอด

แสดงการแปลงเป็นสตริงของอ็อบเจกต์สตริงที่ระบุไปยังสตรีมผลลัพธ์มาตรฐาน

```cpp
static void System::Console::Write(const String &value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [String](../../string/)\& | อ็อบเจกต์สตริงที่จะส่งออก |

## Console::Write(const char_t *) เมธอด

แสดงการแปลงเป็นสตริงของ c-string ที่ระบุไปยังสตรีมผลลัพธ์มาตรฐาน

```cpp
static void System::Console::Write(const char_t *value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const char_t * | c-string ที่จะส่งออก |

## Console::Write(const TypeInfo\&) เมธอด

แสดงการแปลงเป็นสตริงของค่า [TypeInfo](../../typeinfo/) ไปยังสตรีมผลลัพธ์มาตรฐาน

```cpp
static void System::Console::Write(const TypeInfo &value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | ค่าที่จะส่งออก |

## Console::Write(uint32_t) เมธอด

แสดงการแปลงเป็นสตริงของค่าเต็มบวก 32 บิตไปยังสตรีมผลลัพธ์มาตรฐาน

```cpp
static void System::Console::Write(uint32_t value)
```

### อาร์กิวเม้นต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **uint32_t** | ค่าที่จะส่งออก |

## Console::Write(uint64_t) เมธอด

แสดงการแปลงเป็นสตริงของค่าเต็มบวก 64 บิตไปยังสตรีมผลลัพธ์มาตรฐาน

```cpp
static void System::Console::Write(uint64_t value)
```

### อาร์กิวเม้นต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **uint64_t** | ค่าที่จะส่งออก |

## Console::Write(const ArrayPtr\<char_t\>\&, int32_t, int32_t) เมธอด

แสดงการแปลงเป็นสตริงของช่วงที่ระบุของอาร์เรย์อักขระที่ระบุไปยังสตรีมผลลัพธ์มาตรฐาน

```cpp
static void System::Console::Write(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count)
```

### อาร์กิวเม้นต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | อาร์เรย์อักขระ |
| index | **int32_t** | ดัชนีในอาร์เรย์ที่ช่วงที่จะส่งออกเริ่มต้น |
| count | **int32_t** | จำนวนสมาชิกในช่วงที่จะส่งออก |

## Console::Write(const String\&, Args\&&...) เมธอด

แสดงการแปลงเป็นสตริงของอาร์กิวเมนต์ที่ระบุตามรูปแบบที่กำหนดไปยังสตรีมผลลัพธ์มาตรฐาน

```cpp
template<class...> static void System::Console::Write(const String &format, Args &&... args)
```

### พารามิเตอร์แม่แบบ

| พารามิเตอร์ | คำอธิบาย |
| --- | --- |
| ประเภท | ประเภทของค่าที่จะส่งออก |

### อาร์กิวเม้นต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| format | const [String](../../string/)\& | รูปแบบสตริง |
| args | Args\&&... | ค่าที่จะส่งออก |

## Console::Write(const char *) เมธอด




```cpp
static void System::Console::Write(const char *)=delete
```

## ดูเพิ่มเติม

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Class [Console](../)
* Class [Decimal](../../decimal/)
* Class [String](../../string/)
* Class [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)