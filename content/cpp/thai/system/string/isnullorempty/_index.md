---
title: IsNullOrEmpty()
second_title: Aspose.Slides สำหรับ C++ เอกสารอ้างอิง API
description: ตรวจสอบว่าสตริงว่างหรือถือว่าเป็น null.
type: docs
weight: 105
url: /th/system/string/isnullarempty/
---
## String::IsNullOrEmpty() const เมธอด

ตรวจสอบว่าสตริงว่างหรือถือว่าเป็น null.

```cpp
bool System::String::IsNullOrEmpty() const
```

### ค่าที่ส่งคืน

true หากสตริงว่างหรือเป็น null, false ในกรณีอื่น

## String::IsNullOrEmpty(const String\&) เมธอด

ตรวจสอบว่าสตริงที่ส่งเข้ามาเป็น null หรือว่าง.

```cpp
static bool System::String::IsNullOrEmpty(const String &str)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| str | const [String](../)\& | [String](../) เพื่อตรวจสอบ. |

### ค่าที่ส่งคืน

true หากสตริงเป็น null หรือว่าง, false ในกรณีอื่น

## ดูเพิ่มเติม

* คลาส [String](../)
* เนมสเปซ [System](../../)
* ไลบรารี [Aspose.Slides](../../../)