---
title: operator+=()
second_title: เอกสารอ้างอิง API ของ Aspose.Slides สำหรับ C++
description: ตัวดำเนินการกำหนดค่าการต่อ.
type: docs
weight: 287
url: /th/system/string/operator_plus_equal/
---
## String::operator+=(char_t) เมธอด

ตัวดำเนินการกำหนดค่าการต่อ

```cpp
String & System::String::operator+=(char_t c)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| c | char_t | อักขระที่จะเพิ่มไปยังท้ายของสตริงปัจจุบัน. |

### ค่าที่คืน

อ้างอิงตนเอง.

## String::operator+=(const String\&) เมธอด

ตัวดำเนินการกำหนดค่าการต่อ

```cpp
String & System::String::operator+=(const String &str)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| str | const [String](../)\& | [String](../) เพื่อเพิ่มไปยังท้ายของสตริงปัจจุบัน. |

### ค่าที่คืน

อ้างอิงตนเอง.

## String::operator+=(double) เมธอด

ตัวดำเนินการกำหนดค่าการต่อ

```cpp
String & System::String::operator+=(double value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | **double** | [Double](../../double/) เพื่อเพิ่มไปยังท้ายของสตริงปัจจุบัน. |

### ค่าที่คืน

อ้างอิงตนเอง.

## String::operator+=(uint8_t) เมธอด

ตัวดำเนินการกำหนดค่าการต่อ

```cpp
String & System::String::operator+=(uint8_t value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | **uint8_t** | [Byte](../../byte/) เพื่อเพิ่มไปยังท้ายของสตริงปัจจุบัน. |

### ค่าที่คืน

อ้างอิงตนเอง.

## String::operator+=(int16_t) เมธอด

ตัวดำเนินการกำหนดค่าการต่อ

```cpp
String & System::String::operator+=(int16_t value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | **int16_t** | Short เพื่อเพิ่มไปยังท้ายของสตริงปัจจุบัน. |

### ค่าที่คืน

อ้างอิงตนเอง.

## String::operator+=(uint16_t) เมธอด

ตัวดำเนินการกำหนดค่าการต่อ

```cpp
String & System::String::operator+=(uint16_t value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | **uint16_t** | Unsigned short เพื่อเพิ่มไปยังท้ายของสตริงปัจจุบัน. |

### ค่าที่คืน

อ้างอิงตนเอง.

## String::operator+=(int32_t) เมธอด

ตัวดำเนินการกำหนดค่าการต่อ

```cpp
String & System::String::operator+=(int32_t value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | **int32_t** | Int เพื่อเพิ่มไปยังท้ายของสตริงปัจจุบัน. |

### ค่าที่คืน

อ้างอิงตนเอง.

## String::operator+=(uint32_t) เมธอด

ตัวดำเนินการกำหนดค่าการต่อ

```cpp
String & System::String::operator+=(uint32_t value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | **uint32_t** | Unsigned int เพื่อเพิ่มไปยังท้ายของสตริงปัจจุบัน. |

### ค่าที่คืน

อ้างอิงตนเอง.

## String::operator+=(int64_t) เมธอด

ตัวดำเนินการกำหนดค่าการต่อ

```cpp
String & System::String::operator+=(int64_t value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | **int64_t** | Long เพื่อเพิ่มไปยังท้ายของสตริงปัจจุบัน. |

### ค่าที่คืน

อ้างอิงตนเอง.

## String::operator+=(uint64_t) เมธอด

ตัวดำเนินการกำหนดค่าการต่อ

```cpp
String & System::String::operator+=(uint64_t value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | **uint64_t** | Unsigned long เพื่อเพิ่มไปยังท้ายของสตริงปัจจุบัน. |

### ค่าที่คืน

อ้างอิงตนเอง.

## String::operator+=(T) เมธอด

ตัวดำเนินการกำหนดค่าการต่อ

```cpp
template<typename T,std::enable_if_t< std::is_same< T, bool >::value > *> String & System::String::operator+=(T value)
```

### พารามิเตอร์แม่แบบ

| พารามิเตอร์ | คำอธิบาย |
| --- | --- |
| T | Value type to concatenate with string. Must be bool |

### อาร์กิวเมนต์

| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | T | [Boolean](../../boolean/) เพื่อเพิ่มไปยังท้ายของสตริงปัจจุบัน. |

### ค่าที่คืน

อ้างอิงตนเอง.

## ดูเพิ่มเติม

* คลาส [String](../)
* เนมสเปซ [System](../../)
* ไลบรารี [Aspose.Slides](../../../)