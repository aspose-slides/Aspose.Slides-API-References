---
title: FromAscii()
second_title: อ้างอิง API ของ Aspose.Slides สำหรับ C++
description: สร้าง String จากสตริง ASCII.
type: docs
weight: 950
url: /th/system/string/fromascii/
---
## String::FromAscii(const char *) เมธอด

สร้าง [String](../) จากสตริง ASCII.

```cpp
static String System::String::FromAscii(const char *asciiStr)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| asciiStr | const char * | ตัวชี้ไปยังสตริงที่สิ้นสุดด้วยศูนย์และเข้ารหัสโดยใช้โค้ดเพจ ASCII. |

### ค่าที่ส่งกลับ

[String](../) อ็อบเจกต์ที่แสดงถึงสตริงที่ส่งเข้า.

## String::FromAscii(const char *, int) เมธอด

สร้าง [String](../) จากสตริง ASCII.

```cpp
static String System::String::FromAscii(const char *asciiStr, int len)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| asciiStr | const char * | ตัวชี้ไปยังสตริงที่เข้ารหัสโดยใช้โค้ดเพจ ASCII. |
| len | int | จำนวนอักขระที่ต้องจัดการ. |

### ค่าที่ส่งกลับ

[String](../) อ็อบเจกต์ที่แสดงถึงสตริงที่ส่งเข้า.

## String::FromAscii(const std::string\&) เมธอด

สร้าง [String](../) จากสตริง ASCII.

```cpp
static String System::String::FromAscii(const std::string &asciiStr)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| asciiStr | const std::string\& | สตริงที่เข้ารหัสเป็น ASCII. |

### ค่าที่ส่งกลับ

[String](../) อ็อบเจกต์ที่แสดงถึงสตริงที่ส่งเข้า.

## ดูเพิ่มเติม

* คลาส [String](../)
* เนมส페ซ [System](../../)
* ไลบรารี [Aspose.Slides](../../../)