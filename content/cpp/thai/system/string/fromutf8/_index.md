---
title: FromUtf8()
second_title: Aspose.Slides สำหรับ C++ API Reference
description: สร้าง String จากสตริง utf8.
type: docs
weight: 898
url: /th/system/string/fromutf8/
---
## String::FromUtf8(const char *) เมธอด

สร้าง [String](../) จากสตริง utf8.

```cpp
static String System::String::FromUtf8(const char *utf8)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| utf8 | const char * | ตัวชี้ไปยังสตริงที่จบด้วย null ซึ่งเข้ารหัสด้วย codepage utf8. |

### ค่าที่คืนกลับ

[String](../) object ที่แสดงถึงสตริงที่ส่งเข้ามา.

## String::FromUtf8(const char *, int) เมธอด

สร้าง [String](../) จากสตริง utf8.

```cpp
static String System::String::FromUtf8(const char *utf8, int len)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| utf8 | const char * | ตัวชี้ไปยังสตริงที่เข้ารหัสด้วย codepage utf8. |
| len | int | จำนวนอักขระที่ต้องจัดการ. |

### ค่าที่คืนกลับ

[String](../) object ที่แสดงถึงสตริงที่ส่งเข้ามา.

## String::FromUtf8(const uint8_t *) เมธอด

สร้าง [String](../) จากสตริง utf8.

```cpp
static String System::String::FromUtf8(const uint8_t *utf8)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| utf8 | const **uint8_t** * | ตัวชี้ไปยังสตริงที่จบด้วย null ซึ่งเข้ารหัสด้วย codepage utf8. |

### ค่าที่คืนกลับ

[String](../) object ที่แสดงถึงสตริงที่ส่งเข้ามา.

## String::FromUtf8(const std::string\&) เมธอด

สร้าง [String](../) จากสตริง utf8.

```cpp
static String System::String::FromUtf8(const std::string &utf8)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| utf8 | const std::string\& | ตัวชี้ไปยังสตริงที่เข้ารหัสด้วย codepage utf8. |

### ค่าที่คืนกลับ

[String](../) object ที่แสดงถึงสตริงที่ส่งเข้ามา.

## ดูเพิ่มเติม

* คลาส [String](../)
* เนมสเปซ [System](../../)
* ไลบรารี [Aspose.Slides](../../../)