---
title: Concat()
second_title: Aspose.Slides สำหรับ C++ API Reference
description: ต่อสตริงเข้าด้วยกัน.
type: docs
weight: 859
url: /th/system/string/concat/
---
## String::Concat(const ArrayPtr\<String\>\&) method


ต่อข้อความหลายสตริงเข้าด้วยกัน

```cpp
static String System::String::Concat(const ArrayPtr<String> &parts)
```


### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| parts | const [ArrayPtr](../../arrayptr/)\<[String](../)\>\& | สตริงที่จะต่อเข้าด้วยกัน |

### ค่าที่ส่งกลับ

[String](../) ที่มีสตริงที่ต่อเข้าด้วยกัน

## String::Concat(const String\&, const String\&) method


ต่อข้อความหลายสตริงเข้าด้วยกัน

```cpp
static String System::String::Concat(const String &str0, const String &str1)
```


### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| str0 | const [String](../)\& | สตริงแรกที่จะต่อเข้าด้วยกัน |
| str1 | const [String](../)\& | สตริงที่สองที่จะต่อเข้าด้วยกัน |

### ค่าที่ส่งกลับ

[String](../) ที่มีสตริงที่ต่อเข้าด้วยกัน

## String::Concat(const String\&, const String\&, const String\&) method


ต่อข้อความหลายสตริงเข้าด้วยกัน

```cpp
static String System::String::Concat(const String &str0, const String &str1, const String &str2)
```


### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| str0 | const [String](../)\& | สตริงแรกที่จะต่อเข้าด้วยกัน |
| str1 | const [String](../)\& | สตริงที่สองที่จะต่อเข้าด้วยกัน |
| str2 | const [String](../)\& | สตริงที่สามที่จะต่อเข้าด้วยกัน |

### ค่าที่ส่งกลับ

[String](../) ที่มีสตริงที่ต่อเข้าด้วยกัน

## String::Concat(const String\&, const String\&, const String\&, const String\&) method


ต่อข้อความหลายสตริงเข้าด้วยกัน

```cpp
static String System::String::Concat(const String &str0, const String &str1, const String &str2, const String &str3)
```


### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| str0 | const [String](../)\& | สตริงแรกที่จะต่อเข้าด้วยกัน |
| str1 | const [String](../)\& | สตริงที่สองที่จะต่อเข้าด้วยกัน |
| str2 | const [String](../)\& | สตริงที่สามที่จะต่อเข้าด้วยกัน |
| str3 | const [String](../)\& | สตริงที่สี่ที่จะต่อเข้าด้วยกัน |

### ค่าที่ส่งกลับ

[String](../) ที่มีสตริงที่ต่อเข้าด้วยกัน

## ดูเพิ่มเติม

* Typedef [ArrayPtr](../../arrayptr/)
* คลาส [String](../)
* เนมสเปซ [System](../../)
* ไลบรารี [Aspose.Slides](../../../)