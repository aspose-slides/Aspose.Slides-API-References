---
title: Decimal()
second_title: อ้างอิง API ของ Aspose.Slides สำหรับ C++
description: สร้างอินสแตนซ์ที่เป็นตัวแทนของค่า 0.
type: docs
weight: 1
url: /th/system/decimal/decimal/
---
## Decimal::Decimal() คอนสตรัคเตอร์

สร้างอินสแตนซ์ที่เป็นตัวแทนของค่า 0.

```cpp
System::Decimal::Decimal()
```

## Decimal::Decimal(std::int8_t) คอนสตรัคเตอร์

สร้างอินสแตนซ์ที่เป็นตัวแทนของค่าที่ระบุ.

```cpp
System::Decimal::Decimal(std::int8_t i)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| i | std::int8_t | ค่าจำนวนเต็ม 8 บิตที่จะถูกแทนโดยอ็อบเจ็กต์ [Decimal](../) ที่กำลังสร้าง |

## Decimal::Decimal(std::int16_t) คอนสตรัคเตอร์

สร้างอินสแตนซ์ที่เป็นตัวแทนของค่าที่ระบุ.

```cpp
System::Decimal::Decimal(std::int16_t i)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| i | std::int16_t | ค่าจำนวนเต็ม 16 บิตที่จะถูกแทนโดยอ็อบเจ็กต์ [Decimal](../) ที่กำลังสร้าง |

## Decimal::Decimal(std::int32_t) คอนสตรัคเตอร์

สร้างอินสแตนซ์ที่เป็นตัวแทนของค่าที่ระบุ.

```cpp
System::Decimal::Decimal(std::int32_t i)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| i | std::int32_t | ค่าจำนวนเต็ม 32 บิตที่จะถูกแทนโดยอ็อบเจ็กต์ [Decimal](../) ที่กำลังสร้าง |

## Decimal::Decimal(std::int64_t) คอนสตรัคเตอร์

สร้างอินสแตนซ์ที่เป็นตัวแทนของค่าที่ระบุ.

```cpp
System::Decimal::Decimal(std::int64_t i)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| i | std::int64_t | ค่าจำนวนเต็ม 64 บิตที่จะถูกแทนโดยอ็อบเจ็กต์ [Decimal](../) ที่กำลังสร้าง |

## Decimal::Decimal(std::uint8_t) คอนสตรัคเตอร์

สร้างอินสแตนซ์ที่เป็นตัวแทนของค่าที่ระบุ.

```cpp
System::Decimal::Decimal(std::uint8_t i)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| i | std::uint8_t | ค่าจำนวนเต็มบวก 8 บิตที่จะถูกแทนโดยอ็อบเจ็กต์ [Decimal](../) ที่กำลังสร้าง |

## Decimal::Decimal(std::uint16_t) คอนสตรัคเตอร์

สร้างอินสแตนซ์ที่เป็นตัวแทนของค่าที่ระบุ.

```cpp
System::Decimal::Decimal(std::uint16_t i)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| i | std::uint16_t | ค่าจำนวนเต็มบวก 16 บิตที่จะถูกแทนโดยอ็อบเจ็กต์ [Decimal](../) ที่กำลังสร้าง |

## Decimal::Decimal(std::uint32_t) คอนสตรัคเตอร์

สร้างอินสแตนซ์ที่เป็นตัวแทนของค่าที่ระบุ.

```cpp
System::Decimal::Decimal(std::uint32_t i)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| i | std::uint32_t | ค่าจำนวนเต็มบวก 32 บิตที่จะถูกแทนโดยอ็อบเจ็กต์ [Decimal](../) ที่กำลังสร้าง |

## Decimal::Decimal(std::uint64_t) คอนสตรัคเตอร์

สร้างอินสแตนซ์ที่เป็นตัวแทนของค่าที่ระบุ.

```cpp
System::Decimal::Decimal(std::uint64_t i)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| i | std::uint64_t | ค่าจำนวนเต็มบวก 64 บิตที่จะถูกแทนโดยอ็อบเจ็กต์ [Decimal](../) ที่กำลังสร้าง |

## Decimal::Decimal(float) คอนสตรัคเตอร์

สร้างอินสแตนซ์ที่เป็นตัวแทนของค่าที่ระบุ.

```cpp
System::Decimal::Decimal(float f)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| f | **float** | ค่าตัวเลขจริงแบบความแม่นยำเดี่ยวที่จะถูกแทนโดยอ็อบเจ็กต์ [Decimal](../) ที่กำลังสร้าง |

## Decimal::Decimal(double) คอนสตรัคเตอร์

สร้างอินสแตนซ์ที่เป็นตัวแทนของค่าที่ระบุ.

```cpp
System::Decimal::Decimal(double d)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| d | **double** | ค่าตัวเลขจริงแบบความแม่นยำคู่ที่จะถูกแทนโดยอ็อบเจ็กต์ [Decimal](../) ที่กำลังสร้าง |

## Decimal::Decimal(const std::string\&) คอนสตรัคเตอร์

สร้างอินสแตนซ์ที่เป็นตัวแทนของค่าที่มีการแสดงผลเป็นสตริงตามที่กำหนดโดยอินสแตนซ์ของคลาส std::string

```cpp
System::Decimal::Decimal(const std::string &str)
```

## Decimal::Decimal(int32_t, int32_t, int32_t, bool, uint8_t) คอนสตรัคเตอร์

สร้างอ็อบเจ็กต์ [Decimal](../) จากส่วนประกอบที่ระบุ

```cpp
System::Decimal::Decimal(int32_t lo, int32_t mid, int32_t hi, bool isNegative, uint8_t scale)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| lo | **int32_t** | 32 บิตส่วนล่างของค่า |
| mid | **int32_t** | 32 บิตส่วนกลางของค่า |
| hi | **int32_t** | 32 บิตส่วนบนของค่า |
| isNegative | **bool** | ระบุว่าค่าเป็นค่าลบหรือไม่ |
| scale | **uint8_t** | ค่ากำลังของ 10 ตั้งแต่ 0 ถึง 28 |

## Decimal::Decimal(const Decimal\&) คอนสตรัคเตอร์

สร้างอินสแตนซ์ของคลาส [Decimal](../) ที่เป็นตัวแทนของหมายเลขเดียวกันกับอ็อบเจ็กต์ [Decimal](../) ที่ระบุ

```cpp
System::Decimal::Decimal(const Decimal &d)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| d | const [Decimal](../)\& | อ็อบเจ็กต์ [Decimal](../) เพื่อคัดลอกค่าจาก |

## Decimal::Decimal(const ArrayPtr\<int32_t\>\&) คอนสตรัคเตอร์

สร้างอินสแตนซ์ของคลาส [Decimal](../) จากอาร์เรย์จำนวนเต็มที่มีการแทนค่าทวิศ

```cpp
System::Decimal::Decimal(const ArrayPtr<int32_t> &bits)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| bits | const [ArrayPtr](../../arrayptr/)\<**int32_t**\>\& | อาร์เรย์จำนวนเต็มที่บรรจุการแทนค่าทวิศ |

## Decimal::Decimal(std::nullptr_t) คอนสตรัคเตอร์

จะทำให้เกิด ArgumentNullException เสมอ

```cpp
System::Decimal::Decimal(std::nullptr_t bits)
```

## Decimal::Decimal(const number_type\&) คอนสตรัคเตอร์

สร้างอินสแตนซ์ของคลาส [Decimal](../) ที่แทนค่าที่ระบุ

```cpp
System::Decimal::Decimal(const number_type &value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [number_type](../number_type/)\& | การอ้างอิงค่าสุดที่เป็นค่าคงที่จะถูกแทนโดยอ็อบเจ็กต์ที่กำลังสร้าง |

## ดูเพิ่มเติม

* Typedef [ArrayPtr](../../arrayptr/)
* Typedef [number_type](../number_type/)
* Class [Decimal](../)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)