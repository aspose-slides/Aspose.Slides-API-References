---
title: operator==()
second_title: Aspose.Slides สำหรับ C++ API Reference
description: ตรวจสอบว่าวัตถุที่อ้างอิงเป็น null หรือไม่.
type: docs
weight: 53
url: /th/system/weakreference_tmpl_t__end_tmpl/operator_equal_equal/
---
## WeakReference< T >::operator==(std::nullptr_t) const เมธอด

ตรวจสอบว่าวัตถุที่อ้างอิงเป็น null หรือไม่.

```cpp
bool System::WeakReference<T>::operator==(std::nullptr_t) const
```

### ค่าที่ส่งคืน

True ถ้าอ็อบเจ็กต์ที่อ้างอิงเป็น null, false ในกรณีอื่น.

## WeakReference< T >::operator==(const WeakReference\<T\>\&) const เมธอด

เปรียบเทียบอ็อบเจ็กต์ที่อ้างอิงกับอินสแตนซ์อื่นของคลาส WeakReference.

```cpp
bool System::WeakReference<T>::operator==(const WeakReference<T> &other) const
```

### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| other | const [WeakReference](../weakreference/)\<T\>\& | [Object](../../object/) เปรียบเทียบกับ |

### ค่าที่ส่งคืน

True ถ้าอ็อบเจ็กต์ที่เปรียบเทียบอ้างถึงอ็อบเจ็กต์เดียวกัน, false ในกรณีอื่น.

## ดูเพิ่มเติม

* เมธอด [WeakReference](../weakreference/)
* คลาส [WeakReference< T >](../)
* เนมสเปซ [System](../../)
* ไลบรารี [Aspose.Slides](../../../)