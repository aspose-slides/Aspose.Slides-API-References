---
title: WeakReference()
second_title: อ้างอิง API Aspose.Slides สำหรับ C++
description: คอนสตรัคเตอร์ค่าเริ่มต้น.
type: docs
weight: 1
url: /th/system/weakreference_tmpl_t__end_tmpl/weakreference/
---
## WeakReference< T >::WeakReference() เมธอด

คอนสตรัคเตอร์ค่าเริ่มต้น.

```cpp
System::WeakReference<T>::WeakReference()
```

## WeakReference< T >::WeakReference(std::nullptr_t) เมธอด

คอนสตรัคเตอร์จาก nullptr.

```cpp
System::WeakReference<T>::WeakReference(std::nullptr_t)
```

## WeakReference< T >::WeakReference(const SmartPtr\<T\>\&) เมธอด

สร้างอินสแตนซ์ใหม่ของคลาส WeakReference โดยอ้างอิงถึงอ็อบเจกต์ที่ระบุ.

```cpp
System::WeakReference<T>::WeakReference(const SmartPtr<T> &data)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) เพื่อเก็บ. |

## WeakReference< T >::WeakReference(const SmartPtr\<T\>\&, bool) เมธอด

สร้างอินสแตนซ์ใหม่ของคลาส WeakReference โดยอ้างอิงถึงอ็อบเจกต์ที่ระบุ.

```cpp
System::WeakReference<T>::WeakReference(const SmartPtr<T> &data, bool trackResurrection)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) เพื่อเก็บ. |
| trackResurrection | **bool** | ละเว้น. |

## ดูเพิ่มเติม

* คลาส [WeakReference< T >](../)
* คลาส [SmartPtr](../../smartptr/)
* เนมสเปซ [System](../../)
* ไลบรารี [Aspose.Slides](../../../)