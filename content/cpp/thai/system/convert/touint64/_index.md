---
title: ToUInt64()
second_title: อ้างอิง API ของ Aspose.Slides สำหรับ C++
description: แปลงค่าบูลีนที่ระบุเป็นจำนวนเต็มบวกขนาด 64-บิตที่เทียบเท่า
type: docs
weight: 196
url: /th/system/convert/touint64/
---
## Convert::ToUInt64(bool) เมธอด

แปลงค่าบูลีนที่ระบุเป็นจำนวนเต็มบวกขนาด 64 บิตที่เทียบเท่า

```cpp
static constexpr uint64_t System::Convert::ToUInt64(bool value)
```
## Convert::ToUInt64(uint8_t) เมธอด

แปลงจำนวนเต็มบวกแบบ 8-bit ที่ระบุเป็นจำนวนเต็มบวกขนาด 64 บิตที่เทียบเท่า

```cpp
static constexpr uint64_t System::Convert::ToUInt64(uint8_t value)
```
## Convert::ToUInt64(int8_t) เมธอด

แปลงจำนวนเต็มแบบ 8-bit ที่ระบุเป็นจำนวนเต็มบวกขนาด 64 บิตที่เทียบเท่า

```cpp
static uint64_t System::Convert::ToUInt64(int8_t value)
```
## Convert::ToUInt64(uint16_t) เมธอด

แปลงจำนวนเต็มบวกแบบ 16-bit ที่ระบุเป็นจำนวนเต็มบวกขนาด 64 บิตที่เทียบเท่า

```cpp
static constexpr uint64_t System::Convert::ToUInt64(uint16_t value)
```
## Convert::ToUInt64(int16_t) เมธอด

แปลงจำนวนเต็มแบบ 16-bit ที่ระบุเป็นจำนวนเต็มบวกขนาด 64 บิตที่เทียบเท่า

```cpp
static uint64_t System::Convert::ToUInt64(int16_t value)
```
## Convert::ToUInt64(uint32_t) เมธอด

แปลงจำนวนเต็มบวกแบบ 32-bit ที่ระบุเป็นจำนวนเต็มบวกขนาด 64 บิตที่เทียบเท่า

```cpp
static constexpr uint64_t System::Convert::ToUInt64(uint32_t value)
```
## Convert::ToUInt64(int32_t) เมธอด

แปลงจำนวนเต็มแบบ 32-bit ที่ระบุเป็นจำนวนเต็มบวกขนาด 64 บิตที่เทียบเท่า

```cpp
static uint64_t System::Convert::ToUInt64(int32_t value)
```
## Convert::ToUInt64(uint64_t) เมธอด

คืนค่าจำนวนเต็มบวกแบบ 64-bit ที่ระบุ

```cpp
static constexpr uint64_t System::Convert::ToUInt64(uint64_t value)
```
## Convert::ToUInt64(int64_t) เมธอด

แปลงจำนวนเต็มแบบ 64-bit ที่ระบุเป็นจำนวนเต็มบวกขนาด 64 บิตที่เทียบเท่า

```cpp
static uint64_t System::Convert::ToUInt64(int64_t value)
```
## Convert::ToUInt64(float) เมธอด

แปลงตัวเลข float ที่ระบุเป็นจำนวนเต็มบวกขนาด 64 บิตที่เทียบเท่า

```cpp
static uint64_t System::Convert::ToUInt64(float value)
```
## Convert::ToUInt64(double) เมธอด

แปลงตัวเลข double ที่ระบุเป็นจำนวนเต็มบวกขนาด 64 บิตที่เทียบเท่า

```cpp
static uint64_t System::Convert::ToUInt64(double value)
```
## Convert::ToUInt64(const Decimal\&) เมธอด

แปลงจำนวนเลขฐานสิบหกที่ระบุเป็นจำนวนเต็มบวกขนาด 64 บิตที่เทียบเท่า

```cpp
static uint64_t System::Convert::ToUInt64(const Decimal &value)
```
## Convert::ToUInt64(char_t) เมธอด

แปลงอักขระยูนิโค้ดที่ระบุเป็นจำนวนเต็มบวกขนาด 64 บิตที่เทียบเท่า

```cpp
static constexpr uint64_t System::Convert::ToUInt64(char_t value)
```
## Convert::ToUInt64(DateTime) เมธอด

การแปลงไม่รองรับ จะทำให้เกิด InvalidCastException เสมอ

```cpp
static uint64_t System::Convert::ToUInt64(DateTime value)
```
## Convert::ToUInt64(std::nullptr_t) เมธอด

แปลงสตริง null ที่ระบุเป็นค่าตัวเลขจำนวนเต็มบวกแบบ 64-bit ที่เทียบเท่า

```cpp
static constexpr uint64_t System::Convert::ToUInt64(std::nullptr_t)
```

### ค่าที่ส่งกลับ

ศูนย์.

## Convert::ToUInt64(const char_t *) เมธอด

แปลง c-string ที่มีการแสดงตัวเลขเป็นค่าตัวเลขจำนวนเต็มบวกขนาด 64-bit ที่เทียบเท่า

```cpp
static uint64_t System::Convert::ToUInt64(const char_t *value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const char_t * | c-string ที่ต้องการแปลง |

### ค่าที่ส่งกลับ

ค่าจำนวนเต็มบวกขนาด 64-bit ที่เท่ากับค่าตัวเลขที่แสดงโดย c-string ที่ระบุ

## Convert::ToUInt64(const String\&) เมธอด

แปลงสตริงที่มีการแสดงตัวเลขเป็นค่าตัวเลขจำนวนเต็มบวกขนาด 64-bit ที่เทียบเท่า

```cpp
static uint64_t System::Convert::ToUInt64(const String &value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [String](../../string/)\& | สตริงที่ต้องการแปลง |

### ค่าที่ส่งกลับ

ค่าจำนวนเต็มบวกขนาด 64-bit ที่เท่ากับค่าตัวเลขที่แสดงโดยสตริงที่ระบุ

## Convert::ToUInt64(const String\&, int) เมธอด

แปลงสตริงที่มีการแสดงตัวเลขในฐานที่ระบุเป็นค่าตัวเลขจำนวนเต็มบวกขนาด 64-bit ที่เทียบเท่า

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, int from_base)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [String](../../string/)\& | สตริงที่ต้องการแปลง |
| from_base | int | ฐานของตัวเลขที่สตริงแสดง |

### ค่าที่ส่งกลับ

ค่าจำนวนเต็มบวกขนาด 64-bit ที่เท่ากับค่าตัวเลขที่แสดงโดยสตริงที่ระบุ

## Convert::ToUInt64(const String\&, const SharedPtr\<IFormatProvider\>\&) เมธอด

แปลงสตริงที่มีการแสดงตัวเลขเป็นค่าตัวเลขจำนวนเต็มบวกขนาด 64-bit ที่เทียบเท่าโดยใช้ข้อมูลการฟอร์แมตที่ให้มา

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, const SharedPtr<IFormatProvider> &provider)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [String](../../string/)\& | สตริงที่ต้องการแปลง |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | พอยน์เตอร์ไปยังอ็อบเจ็กต์ที่มีข้อมูลการฟอร์แมตของสตริง |

### ค่าที่ส่งกลับ

ค่าจำนวนเต็มบวกขนาด 64-bit ที่เท่ากับค่าตัวเลขที่แสดงโดยสตริงที่ระบุ

## Convert::ToUInt64(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) เมธอด




```cpp
static uint64_t System::Convert::ToUInt64(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToUInt64(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) เมธอด




```cpp
static uint64_t System::Convert::ToUInt64(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToUInt64(const String\&, std::nullptr_t) เมธอด




```cpp
static uint64_t System::Convert::ToUInt64(const String &value, std::nullptr_t)
```

## Convert::ToUInt64(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) เมธอด

แปลงสตริงที่มีการแสดงตัวเลขเป็นค่าตัวเลขจำนวนเต็มบวกขนาด 64-bit ที่เทียบเท่าโดยใช้ข้อมูลการฟอร์แมตและสไตล์ของตัวเลขที่ให้มา

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [String](../../string/)\& | สตริงที่ต้องการแปลง |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | การรวมค่าด้วยบิตของ enum NumberStyles ที่ระบุรูปแบบที่อนุญาตสำหรับการแสดงสตริงของตัวเลข |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | พอยน์เตอร์ไปยังอ็อบเจ็กต์ที่มีข้อมูลการฟอร์แมตของสตริง |

### ค่าที่ส่งกลับ

ค่าจำนวนเต็มบวกขนาด 64-bit ที่เท่ากับค่าตัวเลขที่แสดงโดยสตริงที่ระบุ

## Convert::ToUInt64(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) เมธอด




```cpp
static uint64_t System::Convert::ToUInt64(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToUInt64(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) เมธอด




```cpp
static uint64_t System::Convert::ToUInt64(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToUInt64(const String\&, Globalization::NumberStyles, std::nullptr_t) เมธอด




```cpp
static uint64_t System::Convert::ToUInt64(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToUInt64(Enum) เมธอด




```cpp
template<typename Enum,typename> static uint64_t System::Convert::ToUInt64(Enum value)
```

## Convert::ToUInt64(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) เมธอด

แปลงค่าแบบบ็อกซ์ที่ระบุเป็นค่าตัวเลขจำนวนเต็มบวกขนาด 64-bit ที่เทียบเท่า

```cpp
static uint64_t System::Convert::ToUInt64(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Shared pointer ไปยังอ็อบเจ็กต์ที่บรรจุตัวค่าเพื่อแปลง |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | รูปแบบสตริงที่จะใช้หากประเภทของค่าที่บรรจุเป็น [String](../../string/) |

### ค่าที่ส่งกลับ

ค่าจำนวนเต็มบวกขนาด 64-bit ที่เทียบเท่ากับค่าที่บ็อกซ์ที่ระบุ

## ดูเพิ่มเติม

* Enum [NumberStyles](../../../system.globalization/numberstyles/)
* Typedef [SharedPtr](../../sharedptr/)
* คลาส [Decimal](../../decimal/)
* คลาส [DateTime](../../datetime/)
* คลาส [String](../../string/)
* คลาส [IFormatProvider](../../iformatprovider/)
* คลาส [CultureInfo](../../../system.globalization/cultureinfo/)
* คลาส [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* คลาส [Object](../../object/)
* Struct [Convert](../)
* Struct [Enum](../../enum/)
* เนมสเปซ [System](../../)
* Library [Aspose.Slides](../../../)