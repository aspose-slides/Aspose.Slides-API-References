---
title: ToDecimal()
second_title: Aspose.Slides สำหรับ C++ เอกสารอ้างอิง API
description: แปลงค่า boolean ที่ระบุเป็นจำนวน decimal ที่เท่าเทียมกัน.
type: docs
weight: 235
url: /th/system/convert/todecimal/
---
## Convert::ToDecimal(bool) เมธอด

แปลงค่า boolean ที่ระบุเป็นจำนวน decimal ที่เท่าเทียมกัน.

```cpp
static Decimal System::Convert::ToDecimal(bool value)
```
## Convert::ToDecimal(uint8_t) เมธอด

แปลงค่า uint8_t ที่ระบุเป็นจำนวน decimal ที่เท่าเทียมกัน.

```cpp
static Decimal System::Convert::ToDecimal(uint8_t value)
```
## Convert::ToDecimal(int8_t) เมธอด

แปลงค่า int8_t ที่ระบุเป็นจำนวน decimal ที่เท่าเทียมกัน.

```cpp
static Decimal System::Convert::ToDecimal(int8_t value)
```
## Convert::ToDecimal(uint16_t) เมธอด

แปลงค่า uint16_t ที่ระบุเป็นจำนวน decimal ที่เท่าเทียมกัน.

```cpp
static Decimal System::Convert::ToDecimal(uint16_t value)
```
## Convert::ToDecimal(int16_t) เมธอด

แปลงค่า int16_t ที่ระบุเป็นจำนวน decimal ที่เท่าเทียมกัน.

```cpp
static Decimal System::Convert::ToDecimal(int16_t value)
```
## Convert::ToDecimal(uint32_t) เมธอด

แปลงค่า uint32_t ที่ระบุเป็นจำนวน decimal ที่เท่าเทียมกัน.

```cpp
static Decimal System::Convert::ToDecimal(uint32_t value)
```
## Convert::ToDecimal(int32_t) เมธอด

แปลงค่า int32_t ที่ระบุเป็นจำนวน decimal ที่เท่าเทียมกัน.

```cpp
static Decimal System::Convert::ToDecimal(int32_t value)
```
## Convert::ToDecimal(uint64_t) เมธอด

แปลงค่า uint64_t ที่ระบุเป็นจำนวน decimal ที่เท่าเทียมกัน.

```cpp
static Decimal System::Convert::ToDecimal(uint64_t value)
```
## Convert::ToDecimal(int64_t) เมธอด

แปลงค่า int64_t ที่ระบุเป็นจำนวน decimal ที่เท่าเทียมกัน.

```cpp
static Decimal System::Convert::ToDecimal(int64_t value)
```
## Convert::ToDecimal(float) เมธอด

แปลงค่า float ที่ระบุเป็นจำนวน decimal ที่เท่าเทียมกัน.

```cpp
static Decimal System::Convert::ToDecimal(float value)
```
## Convert::ToDecimal(double) เมธอด

แปลงค่า double ที่ระบุเป็นจำนวน decimal ที่เท่าเทียมกัน.

```cpp
static Decimal System::Convert::ToDecimal(double value)
```
## Convert::ToDecimal(const Decimal\&) เมธอด

ส่งคืนค่า decimal ที่ระบุ.

```cpp
static Decimal System::Convert::ToDecimal(const Decimal &value)
```
## Convert::ToDecimal(char_t) เมธอด

การแปลงไม่รองรับ จะทำให้เกิด InvalidCastException เสมอ

```cpp
static Decimal System::Convert::ToDecimal(char_t value)
```
## Convert::ToDecimal(DateTime) เมธอด

การแปลงไม่รองรับ จะทำให้เกิด InvalidCastException เสมอ

```cpp
static Decimal System::Convert::ToDecimal(DateTime value)
```
## Convert::ToDecimal(std::nullptr_t) เมธอด

แปลง null-string ที่ระบุเป็นค่า [Decimal](../../decimal/) ที่เท่าเทียมกัน.

```cpp
static Decimal System::Convert::ToDecimal(std::nullptr_t)
```


### ค่าที่ส่งคืน

ศูนย์.

## Convert::ToDecimal(const char_t *) เมธอด

แปลง c-string ที่ระบุซึ่งมีการแสดงผลของตัวเลขเป็นค่า [Decimal](../../decimal/) ที่เท่าเทียมกัน.

```cpp
static Decimal System::Convert::ToDecimal(const char_t *value)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const char_t * | c-string ที่จะทำการแปลง |

### ค่าที่ส่งคืน

ค่า [Decimal](../../decimal/) ที่เท่ากับตัวเลขที่แสดงโดย c-string ที่ระบุ

## Convert::ToDecimal(const String\&) เมธอด

แปลง string ที่ระบุซึ่งมีการแสดงผลของตัวเลขเป็นค่า [Decimal](../../decimal/) ที่เท่าเทียมกัน.

```cpp
static Decimal System::Convert::ToDecimal(const String &value)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [String](../../string/)\& | string ที่จะทำการแปลง |

### ค่าที่ส่งคืน

ค่า [Decimal](../../decimal/) ที่เท่ากับตัวเลขที่แสดงโดย string ที่ระบุ

## Convert::ToDecimal(const String\&, const SharedPtr\<IFormatProvider\>\&) เมธอด

แปลง string ที่ระบุซึ่งมีการแสดงผลของตัวเลขเป็นค่า [Decimal](../../decimal/) ที่เท่าเทียมกันโดยใช้ข้อมูลการจัดรูปแบบที่ให้ไว้.

```cpp
static Decimal System::Convert::ToDecimal(const String &value, const SharedPtr<IFormatProvider> &provider)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [String](../../string/)\& | string ที่จะทำการแปลง |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | ตัวชี้ไปยังอ็อบเจกต์ที่มีข้อมูลรูปแบบสตริง |

### ค่าที่ส่งคืน

ค่า [Decimal](../../decimal/) ที่เท่ากับตัวเลขที่แสดงโดย string ที่ระบุ

## Convert::ToDecimal(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) เมธอด

แปลง string ที่ระบุซึ่งมีการแสดงผลของตัวเลขเป็นค่า [Decimal](../../decimal/) ที่เท่าเทียมกันโดยใช้รูปแบบตัวเลขที่ระบุและข้อมูลการจัดรูปแบบ.

```cpp
static Decimal System::Convert::ToDecimal(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [String](../../string/)\& | string ที่จะทำการแปลง |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | การรวมค่าต่าง ๆ ของ enum NumberStyles ที่กำหนดรูปแบบที่อนุญาตให้แสดงตัวเลข |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | ตัวชี้ไปยังอ็อบเจกต์ที่มีข้อมูลรูปแบบสตริง |

### ค่าที่ส่งคืน

ค่า [Decimal](../../decimal/) ที่เท่ากับตัวเลขที่แสดงโดย string ที่ระบุ

## Convert::ToDecimal(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) เมธอด

แปลงค่า boxed ที่ระบุเป็นค่า [Decimal](../../decimal/) ที่เท่าเทียมกัน.

```cpp
static Decimal System::Convert::ToDecimal(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Shared pointer ไปยังอ็อบเจกต์ที่บรรจุค่าที่จะทำการแปลง |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | รูปแบบสตริงที่จะใช้หากประเภทของค่าที่บรรจุเป็น [String](../../string/) |

### ค่าที่ส่งคืน

ค่า [Decimal](../../decimal/) ที่เท่ากับค่าที่บรรจุที่ระบุ

## ดูเพิ่มเติม

* Enum [NumberStyles](../../../system.globalization/numberstyles/)
* Typedef [SharedPtr](../../sharedptr/)
* Class [Decimal](../../decimal/)
* Class [DateTime](../../datetime/)
* Class [String](../../string/)
* Class [IFormatProvider](../../iformatprovider/)
* Class [Object](../../object/)
* Struct [Convert](../)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)