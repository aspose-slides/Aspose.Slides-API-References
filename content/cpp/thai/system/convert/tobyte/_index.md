---
title: ToByte()
second_title: อ้างอิง API ของ Aspose.Slides สำหรับ C++
description: แปลงค่าบูลีนที่ระบุให้เป็นจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่า.
type: docs
weight: 92
url: /th/system/convert/tobyte/
---
## Convert::ToByte(bool) เมธอด


แปลงค่าบูลีนที่ระบุให้เป็นจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่า

```cpp
static constexpr uint8_t System::Convert::ToByte(bool value)
```

## Convert::ToByte(uint8_t) เมธอด


คืนค่าจำนวนเต็มไม่บวกขนาด 8 บิตที่ระบุ

```cpp
static constexpr uint8_t System::Convert::ToByte(uint8_t value)
```

## Convert::ToByte(int8_t) เมธอด


แปลงค่าจำนวนเต็มมีเครื่องหมายขนาด 8 บิตที่ระบุให้เป็นจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่า

```cpp
static uint8_t System::Convert::ToByte(int8_t value)
```

## Convert::ToByte(uint16_t) เมธอด


แปลงค่าจำนวนเต็มไม่บวกขนาด 16 บิตที่ระบุให้เป็นจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่า

```cpp
static uint8_t System::Convert::ToByte(uint16_t value)
```

## Convert::ToByte(int16_t) เมธอด


แปลงค่าจำนวนเต็มมีเครื่องหมายขนาด 16 บิตที่ระบุให้เป็นจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่า

```cpp
static uint8_t System::Convert::ToByte(int16_t value)
```

## Convert::ToByte(uint32_t) เมธอด


แปลงค่าจำนวนเต็มไม่บวกขนาด 32 บิตที่ระบุให้เป็นจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่า

```cpp
static uint8_t System::Convert::ToByte(uint32_t value)
```

## Convert::ToByte(int32_t) เมธอด


แปลงค่าจำนวนเต็มมีเครื่องหมายขนาด 32 บิตที่ระบุให้เป็นจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่า

```cpp
static uint8_t System::Convert::ToByte(int32_t value)
```

## Convert::ToByte(uint64_t) เมธอด


แปลงค่าจำนวนเต็มไม่บวกขนาด 64 บิตที่ระบุให้เป็นจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่า

```cpp
static uint8_t System::Convert::ToByte(uint64_t value)
```

## Convert::ToByte(int64_t) เมธอด


แปลงค่าจำนวนเต็มมีเครื่องหมายขนาด 64 บิตที่ระบุให้เป็นจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่า

```cpp
static uint8_t System::Convert::ToByte(int64_t value)
```

## Convert::ToByte(float) เมธอด


แปลงจำนวนแบบ float ที่ระบุให้เป็นจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่า

```cpp
static uint8_t System::Convert::ToByte(float value)
```

## Convert::ToByte(double) เมธอด


แปลงจำนวนแบบ double ที่ระบุให้เป็นจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่า

```cpp
static uint8_t System::Convert::ToByte(double value)
```

## Convert::ToByte(const Decimal\&) เมธอด


แปลงหมายเลขทศนิยมที่ระบุให้เป็นจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่า

```cpp
static uint8_t System::Convert::ToByte(const Decimal &value)
```

## Convert::ToByte(char_t) เมธอด


แปลงอักขระยูนิโค้ดที่ระบุให้เป็นจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่า

```cpp
static uint8_t System::Convert::ToByte(char_t value)
```

## Convert::ToByte(DateTime) เมธอด


การแปลงไม่สนับสนุน จะทำการโยน InvalidCastException เสมอ

```cpp
static uint8_t System::Convert::ToByte(DateTime value)
```

## Convert::ToByte(std::nullptr_t) เมธอด


แปลงสตริง null ที่ระบุให้เป็นค่าจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่า

```cpp
static constexpr uint8_t System::Convert::ToByte(std::nullptr_t)
```


### ค่าที่คืน

ศูนย์

## Convert::ToByte(const char_t *) เมธอด


แปลง c-string ที่มีการแสดงตัวเลขเป็นสตริงให้เป็นค่าจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่า

```cpp
static uint8_t System::Convert::ToByte(const char_t *value)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const char_t * | c-string ที่จะแปลง |

### ค่าที่คืน

ค่าจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่ากับตัวเลขที่สตริง c-string แสดง

## Convert::ToByte(const String\&) เมธอด


แปลงสตริงที่มีการแสดงตัวเลขเป็นสตริงให้เป็นค่าจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่า

```cpp
static uint8_t System::Convert::ToByte(const String &value)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [String](../../string/)\& | สตริงที่จะทำการแปลง |

### ค่าที่คืน

ค่าจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่ากับตัวเลขที่สตริงแสดง

## Convert::ToByte(const String\&, int) เมธอด


แปลงสตริงที่มีการแสดงตัวเลขในฐานที่ระบุให้เป็นค่าจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่า

```cpp
static uint8_t System::Convert::ToByte(const String &value, int from_base)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [String](../../string/)\& | สตริงที่จะทำการแปลง |
| from_base | int | ฐานของตัวเลขที่สตริงแสดง |

### ค่าที่คืน

ค่าจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่ากับตัวเลขที่สตริงแสดง

## Convert::ToByte(const String\&, const SharedPtr\<IFormatProvider\>\&) เมธอด


แปลงสตริงที่มีการแสดงตัวเลขให้เป็นค่าจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่าโดยใช้ข้อมูลการจัดรูปแบบที่ระบุ

```cpp
static uint8_t System::Convert::ToByte(const String &value, const SharedPtr<IFormatProvider> &provider)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [String](../../string/)\& | สตริงที่จะทำการแปลง |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | ตัวชี้ไปยังอ็อบเจ็กต์ที่มีข้อมูลรูปแบบสตริง |

### ค่าที่คืน

ค่าจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่ากับตัวเลขที่สตริงแสดง

## Convert::ToByte(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) เมธอด




```cpp
static uint8_t System::Convert::ToByte(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToByte(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) เมธอด




```cpp
static uint8_t System::Convert::ToByte(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToByte(const String\&, std::nullptr_t) เมธอด




```cpp
static uint8_t System::Convert::ToByte(const String &value, std::nullptr_t)
```

## Convert::ToByte(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) เมธอด


แปลงสตริงที่มีการแสดงตัวเลขให้เป็นค่าจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่าโดยใช้ข้อมูลการจัดรูปแบบและสไตล์ตัวเลขที่ระบุ

```cpp
static uint8_t System::Convert::ToByte(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [String](../../string/)\& | สตริงที่จะทำการแปลง |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | การรวมค่าแบบบิตของ enum NumberStyles ที่กำหนดรูปแบบที่อนุญาตของสตริงที่แสดงตัวเลข |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | ตัวชี้ไปยังอ็อบเจ็กต์ที่มีข้อมูลรูปแบบสตริง |

### ค่าที่คืน

ค่าจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่ากับตัวเลขที่สตริงแสดง

## Convert::ToByte(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) เมธอด




```cpp
static uint8_t System::Convert::ToByte(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToByte(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) เมธอด




```cpp
static uint8_t System::Convert::ToByte(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToByte(const String\&, Globalization::NumberStyles, std::nullptr_t) เมธอด 




```cpp
static uint8_t System::Convert::ToByte(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToByte(Enum) เมธอด 




```cpp
template<typename Enum,typename> static uint8_t System::Convert::ToByte(Enum value)
```

## Convert::ToByte(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) เมธอด


แปลงค่าที่ถูกบ็อกซ์ให้เป็นค่าจำนวนเต็มไม่บวกขนาด 8 บิตที่เทียบเท่า

```cpp
static uint8_t System::Convert::ToByte(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | ตัวชี้อ้างอิงแบบ shared pointer ไปยังอ็อบเจ็กต์ที่บรรจุค่าที่จะทำการแปลง |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | รูปแบบสตริงที่จะใช้หากประเภทของค่าที่บรรจุเป็น [String](../../string/) |

### ค่าที่คืน

ค่าจำนวนเต็มไม่บับขนาด 8 บิตที่เทียบเท่ากับค่าที่บ็อกซ์ที่ระบุ

## ดูเพิ่มเติม

* Enum [NumberStyles](../../../system.globalization/numberstyles/)
* Typedef [SharedPtr](../../sharedptr/)
* Class [Decimal](../../decimal/)
* Class [DateTime](../../datetime/)
* Class [String](../../string/)
* Class [IFormatProvider](../../iformatprovider/)
* Class [CultureInfo](../../../system.globalization/cultureinfo/)
* Class [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* Class [Object](../../object/)
* Struct [Convert](../)
* Struct [Enum](../../enum/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)