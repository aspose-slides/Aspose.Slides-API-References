---
title: ToSByte()
second_title: อ้างอิง API ของ Aspose.Slides สำหรับ C++
description: แปลงค่าบูลีนที่ระบุให้เป็นจำนวนเต็มแบบมีเครื่องหมายขนาด 8 บิตที่เทียบเท่า.
type: docs
weight: 105
url: /th/system/convert/tosbyte/
---
## Convert::ToSByte(bool) เมธอด

แปลงค่าบูลีนที่ระบุให้เป็นจำนวนเต็มแบบมีเครื่องหมายขนาด 8 บิตที่เทียบเท่า

```cpp
static constexpr int8_t System::Convert::ToSByte(bool value)
```
## Convert::ToSByte(uint8_t) เมธอด

แปลงจำนวนเต็มจำนวนบวกขนาด 8 บิตที่ระบุให้เป็นจำนวนเต็มแบบมีเครื่องหมายขนาด 8 บิตที่เทียบเท่า

```cpp
static int8_t System::Convert::ToSByte(uint8_t value)
```
## Convert::ToSByte(int8_t) เมธอด

คืนค่าจำนวนเต็มขนาด 8 บิตที่ระบุ

```cpp
static constexpr int8_t System::Convert::ToSByte(int8_t value)
```
## Convert::ToSByte(uint16_t) เมธอด

แปลงจำนวนเต็มจำนวนบวกขนาด 16 บิตที่ระบุให้เป็นจำนวนเต็มแบบมีเครื่องหมายขนาด 8 บิตที่เทียบเท่า

```cpp
static int8_t System::Convert::ToSByte(uint16_t value)
```
## Convert::ToSByte(int16_t) เมธอด

แปลงจำนวนเต็มขนาด 16 บิตที่ระบุให้เป็นจำนวนเต็มแบบมีเครื่องหมายขนาด 8 บิตที่เทียบเท่า

```cpp
static int8_t System::Convert::ToSByte(int16_t value)
```
## Convert::ToSByte(uint32_t) เมธอด

แปลงจำนวนเต็มจำนวนบวกขนาด 32 บิตที่ระบุให้เป็นจำนวนเต็มแบบมีเครื่องหมายขนาด 8 บิตที่เทียบเท่า

```cpp
static int8_t System::Convert::ToSByte(uint32_t value)
```
## Convert::ToSByte(int32_t) เมธอด

แปลงจำนวนเต็มขนาด 32 บิตที่ระบุให้เป็นจำนวนเต็มแบบมีเครื่องหมายขนาด 8 บิตที่เทียบเท่า

```cpp
static int8_t System::Convert::ToSByte(int32_t value)
```
## Convert::ToSByte(uint64_t) เมธอด

แปลงจำนวนเต็มจำนวนบวกขนาด 64 บิตที่ระบุให้เป็นจำนวนเต็มแบบมีเครื่องหมายขนาด 8 บิตที่เทียบเท่า

```cpp
static int8_t System::Convert::ToSByte(uint64_t value)
```
## Convert::ToSByte(int64_t) เมธอด

แปลงจำนวนเต็มขนาด 64 บิตที่ระบุให้เป็นจำนวนเต็มแบบมีเครื่องหมายขนาด 8 บิตที่เทียบเท่า

```cpp
static int8_t System::Convert::ToSByte(int64_t value)
```
## Convert::ToSByte(float) เมธอด

แปลงจำนวนทศนิยมแบบ float ที่ระบุให้เป็นจำนวนเต็มแบบมีเครื่องหมายขนาด 8 บิตที่เทียบเท่า

```cpp
static int8_t System::Convert::ToSByte(float value)
```
## Convert::ToSByte(double) เมธอด

แปลงจำนวนทศนิยมแบบ double ที่ระบุให้เป็นจำนวนเต็มแบบมีเครื่องหมายขนาด 8 บิตที่เทียบเท่า

```cpp
static int8_t System::Convert::ToSByte(double value)
```
## Convert::ToSByte(const Decimal\&) เมธอด

แปลงจำนวนทศนิยมที่ระบุให้เป็นจำนวนเต็มแบบมีเครื่องหมายขนาด 8 บิตที่เทียบเท่า

```cpp
static int8_t System::Convert::ToSByte(const Decimal &value)
```
## Convert::ToSByte(char_t) เมธอด

แปลงอักขระ Unicode ที่ระบุให้เป็นจำนวนเต็มแบบมีเครื่องหมายขนาด 8 บิตที่เทียบเท่า

```cpp
static int8_t System::Convert::ToSByte(char_t value)
```
## Convert::ToSByte(DateTime) เมธอด

การแปลงไม่รองรับ จะทำให้เกิด InvalidCastException เสมอ

```cpp
static int8_t System::Convert::ToSByte(DateTime value)
```
## Convert::ToSByte(std::nullptr_t) เมธอด

แปลงสตริง null ที่ระบุให้เป็นค่าจำนวนเต็มขนาด 8 บิตที่เทียบเท่า

```cpp
static constexpr int8_t System::Convert::ToSByte(std::nullptr_t)
```


### ค่าที่คืน

ศูนย์

## Convert::ToSByte(const char_t *) เมธอด

แปลงสตริง C ที่มีการแทนค่าตัวเลขเป็นค่าจำนวนเต็มขนาด 8 บิตที่เทียบเท่า

```cpp
static int8_t System::Convert::ToSByte(const char_t *value)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const char_t * | สตริง C ที่จะทำการแปลง |

### ค่าที่คืน

ค่าจำนวนเต็มขนาด 8 บิตที่เท่ากับตัวเลขที่แสดงโดยสตริง C ที่ระบุ

## Convert::ToSByte(const String\&) เมธอด

แปลงสตริงที่มีการแทนค่าตัวเลขเป็นค่าจำนวนเต็มขนาด 8 บิตที่เทียบเท่า

```cpp
static int8_t System::Convert::ToSByte(const String &value)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [String](../../string/)\& | สตริงที่จะทำการแปลง |

### ค่าที่คืน

ค่าจำนวนเต็มขนาด 8 บิตที่เท่ากับตัวเลขที่แสดงโดยสตริงที่ระบุ

## Convert::ToSByte(const String\&, int) เมธอด

แปลงสตริงที่มีการแทนค่าตัวเลขในฐานที่ระบุเป็นค่าจำนวนเต็มขนาด 8 บิตที่เทียบเท่า

```cpp
static int8_t System::Convert::ToSByte(const String &value, int from_base)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [String](../../string/)\& | สตริงที่จะทำการแปลง |
| from_base | int | ฐานของตัวเลขที่สตริงแทนค่า |

### ค่าที่คืน

ค่าจำนวนเต็มขนาด 8 บิตที่เท่ากับตัวเลขที่แสดงโดยสตริงที่ระบุ

## Convert::ToSByte(const String\&, const SharedPtr\<IFormatProvider\>\&) เมธอด

แปลงสตริงที่มีการแทนค่าตัวเลขเป็นค่าจำนวนเต็มจำนวนบวกขนาด 8 บิตโดยใช้ข้อมูลการจัดรูปแบบที่ให้ไว้

```cpp
static int8_t System::Convert::ToSByte(const String &value, const SharedPtr<IFormatProvider> &provider)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [String](../../string/)\& | สตริงที่จะทำการแปลง |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | ตัวชี้ไปยังอ็อบเจกต์ที่มีข้อมูลการจัดรูปแบบสตริง |

### ค่าที่คืน

ค่าจำนวนเต็มขนาด 8 บิตที่เท่ากับตัวเลขที่แสดงโดยสตริงที่ระบุ

## Convert::ToSByte(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) เมธอด




```cpp
static int8_t System::Convert::ToSByte(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToSByte(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) เมธอด




```cpp
static int8_t System::Convert::ToSByte(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToSByte(const String\&, std::nullptr_t) เมธอด




```cpp
static int8_t System::Convert::ToSByte(const String &value, std::nullptr_t)
```

## Convert::ToSByte(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) เมธอด

แปลงสตริงที่มีการแทนค่าตัวเลขเป็นค่าจำนวนเต็มขนาด 8 บิตโดยใช้ข้อมูลการจัดรูปแบบและสไตล์ของตัวเลขที่ให้ไว้

```cpp
static int8_t System::Convert::ToSByte(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [String](../../string/)\& | สตริงที่จะทำการแปลง |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | การรวมค่าด้วยบิตของ enum NumberStyles ที่ระบุสไตล์ที่อนุญาตสำหรับการแทนค่าตัวเลขของสตริง |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | ตัวชี้ไปยังอ็อบเจกต์ที่มีข้อมูลการจัดรูปแบบสตริง |

### ค่าที่คืน

ค่าจำนวนเต็มแบบไม่มีเครื่องหมายขนาด 8 บิตที่เท่ากับตัวเลขที่แสดงโดยสตริงที่ระบุ

## Convert::ToSByte(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) เมธอด




```cpp
static int8_t System::Convert::ToSByte(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToSByte(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) เมธอด




```cpp
static int8_t System::Convert::ToSByte(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToSByte(const String\&, Globalization::NumberStyles, std::nullptr_t) เมธอด 




```cpp
static int8_t System::Convert::ToSByte(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToSByte(Enum) เมธอด 




```cpp
template<typename Enum,typename> static int8_t System::Convert::ToSByte(Enum value)
```

## Convert::ToSByte(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) เมธอด

แปลงค่าที่บ็อกซ์ไว้ให้เป็นค่าจำนวนเต็มขนาด 8 บิตที่เทียบเท่า

```cpp
static int8_t System::Convert::ToSByte(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | ตัวชี้แบบ shared pointer ไปยังอ็อบเจกต์ที่บ็อกซ์ค่าที่จะทำการแปลง |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | รูปแบบสตริงที่จะใช้หากชนิดของค่าที่บ็อกซ์เป็น [String](../../string/) |

### ค่าที่คืน

ค่าจำนวนเต็มขนาด 8 บิตที่เทียบเท่ากับค่าที่บ็อกซ์ไว้

## ดูเพิ่มเติม

* Enum [NumberStyles](../../../system.globalization/numberstyles/)
* Typedef [SharedPtr](../../sharedptr/)
* Class [Decimal](../../decimal/)
* Class [DateTime](../../datetime/)
* Class [String](../../string/)
* Class [IFormatProvider](../../iformatprovider/)
* Class [CultureInfo](../../../system.globalization/cultureinfo/)
* Class [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* Class [Object](../../object/)
* Struct [Convert](../)
* Struct [Enum](../../enum/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)