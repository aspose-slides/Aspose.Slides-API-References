---
title: ToInt64()
second_title: Aspose.Slides สำหรับ C++ เอกสารอ้างอิง API
description: แปลงค่า boolean ที่ระบุให้เป็นจำนวนเต็มแบบ signed 64-bit ที่เทียบเท่า
type: docs
weight: 183
url: /th/system/convert/toint64/
---
## Convert::ToInt64(bool) เมธอด

แปลงค่า boolean ที่ระบุให้เป็นจำนวนเต็มแบบ signed 64-bit ที่เทียบเท่า

```cpp
static constexpr int64_t System::Convert::ToInt64(bool value)
```

## Convert::ToInt64(uint8_t) เมธอด

แปลงจำนวนเต็มบวก 8-bit ที่ระบุให้เป็นจำนวนเต็มแบบ signed 64-bit ที่เทียบเท่า

```cpp
static constexpr int64_t System::Convert::ToInt64(uint8_t value)
```

## Convert::ToInt64(int8_t) เมธอด

แปลงจำนวนเต็ม signed 8-bit ที่ระบุให้เป็นจำนวนเต็มแบบ signed 64-bit ที่เทียบเท่า

```cpp
static constexpr int64_t System::Convert::ToInt64(int8_t value)
```

## Convert::ToInt64(uint16_t) เมธอด

แปลงจำนวนเต็มบวก 16-bit ที่ระบุให้เป็นจำนวนเต็มแบบ signed 64-bit ที่เทียบเท่า

```cpp
static constexpr int64_t System::Convert::ToInt64(uint16_t value)
```

## Convert::ToInt64(int16_t) เมธอด

แปลงจำนวนเต็ม signed 16-bit ที่ระบุให้เป็นจำนวนเต็มแบบ signed 64-bit ที่เทียบเท่า

```cpp
static constexpr int64_t System::Convert::ToInt64(int16_t value)
```

## Convert::ToInt64(uint32_t) เมธอด

แปลงจำนวนเต็มบวก 32-bit ที่ระบุให้เป็นจำนวนเต็มแบบ signed 64-bit ที่เทียบเท่า

```cpp
static constexpr int64_t System::Convert::ToInt64(uint32_t value)
```

## Convert::ToInt64(int32_t) เมธอด

แปลงจำนวนเต็ม signed 32-bit ที่ระบุให้เป็นจำนวนเต็มแบบ signed 64-bit ที่เทียบเท่า

```cpp
static constexpr int64_t System::Convert::ToInt64(int32_t value)
```

## Convert::ToInt64(uint64_t) เมธอด

แปลงจำนวนเต็มบวก 64-bit ที่ระบุให้เป็นจำนวนเต็มแบบ signed 64-bit ที่เทียบเท่า

```cpp
static int64_t System::Convert::ToInt64(uint64_t value)
```

## Convert::ToInt64(int64_t) เมธodb

ส่งคืนจำนวนเต็ม signed 64-bit ที่ระบุ

```cpp
static constexpr int64_t System::Convert::ToInt64(int64_t value)
```

## Convert::ToInt64(float) เมธอด

แปลงตัวเลข float ที่ระบุให้เป็นจำนวนเต็มแบบ signed 64-bit ที่เทียบเท่า

```cpp
static int64_t System::Convert::ToInt64(float value)
```

## Convert::ToInt64(double) เมธอด

แปลงตัวเลข double ที่ระบุให้เป็นจำนวนเต็มแบบ signed 64-bit ที่เทียบเท่า

```cpp
static int64_t System::Convert::ToInt64(double value)
```

## Convert::ToInt64(const Decimal\&) เมธอด

แปลงตัวเลข decimal ที่ระบุให้เป็นจำนวนเต็มแบบ signed 64-bit ที่เทียบเท่า

```cpp
static int64_t System::Convert::ToInt64(const Decimal &value)
```

## Convert::ToInt64(char_t) เมธอด

แปลงอักขระ unicode ที่ระบุให้เป็นจำนวนเต็มแบบ signed 64-bit ที่เทียบเท่า

```cpp
static constexpr int64_t System::Convert::ToInt64(char_t value)
```

## Convert::ToInt64(DateTime) เมธอด

ไม่รองรับการแปลง จะขว้าง InvalidCastException เสมอ

```cpp
static int64_t System::Convert::ToInt64(DateTime value)
```

## Convert::ToInt64(std::nullptr_t) เมธอด

แปลง null-string ที่ระบุให้เป็นค่า integer 64-bit ที่เทียบเท่า

```cpp
static constexpr int64_t System::Convert::ToInt64(std::nullptr_t)
```


### ค่าที่คืน

ศูนย์

## Convert::ToInt64(const char_t *) เมธอด

แปลง c-string ที่ระบุซึ่งมีการแสดงผลเป็นสตริงของตัวเลขให้เป็นค่า integer 64-bit ที่เทียบเท่า

```cpp
static int64_t System::Convert::ToInt64(const char_t *value)
```


### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| value | const char_t * | c-string ที่จะทำการแปลง |

### ค่าที่คืน

ค่า integer 64-bit ที่เท่ากับตัวเลขที่แสดงโดย c-string ที่ระบุ

## Convert::ToInt64(const String\&) เมธอด

แปลง string ที่ระบุซึ่งมีการแสดงผลเป็นสตริงของตัวเลขให้เป็นค่า integer 64-bit ที่เทียบเท่า

```cpp
static int64_t System::Convert::ToInt64(const String &value)
```


### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | สตริงที่จะทำการแปลง |

### ค่าที่คืน

ค่า integer 64-bit ที่เท่ากับตัวเลขที่แสดงโดยสตริงที่ระบุ

## Convert::ToInt64(const String\&, int) เมธอด

แปลง string ที่ระบุซึ่งมีการแสดงผลเป็นสตริงของตัวเลขในฐานที่กำหนดให้เป็นค่า integer 64-bit ที่เทียบเท่า

```cpp
static int64_t System::Convert::ToInt64(const String &value, int from_base)
```


### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | สตริงที่จะทำการแปลง |
| from_base | int | ฐานของตัวเลขที่แสดงโดยสตริง |

### ค่าที่คืน

ค่า integer 64-bit ที่เท่ากับตัวเลขที่แสดงโดยสตริงที่ระบุ

## Convert::ToInt64(const String\&, const SharedPtr\<IFormatProvider\>\&) เมธอด

แปลง string ที่ระบุซึ่งมีการแสดงผลเป็นสตริงของตัวเลขให้เป็นค่า integer 64-bit ที่เทียบเท่าโดยใช้ข้อมูลการจัดรูปแบบที่ให้มา

```cpp
static int64_t System::Convert::ToInt64(const String &value, const SharedPtr<IFormatProvider> &provider)
```


### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | สตริงที่จะทำการแปลง |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | พอยน์เตอร์ไปยังอ็อบเจ็กต์ที่บรรจุข้อมูลการจัดรูปแบบสตริง |

### ค่าที่คืน

ค่า integer 64-bit ที่เท่ากับตัวเลขที่แสดงโดยสตริงที่ระบุ

## Convert::ToInt64(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) เมธodb


```cpp
static int64_t System::Convert::ToInt64(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToInt64(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) เมธodb


```cpp
static int64_t System::Convert::ToInt64(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToInt64(const String\&, std::nullptr_t) เมธodb


```cpp
static int64_t System::Convert::ToInt64(const String &value, std::nullptr_t)
```

## Convert::ToInt64(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) เมธodb

แปลง string ที่ระบุซึ่งมีการแสดงผลเป็นสตริงของตัวเลขให้เป็นค่า integer 64-bit ที่เทียบเท่าโดยใช้ข้อมูลการจัดรูปแบบและสไตล์ของตัวเลขที่ให้มา

```cpp
static int64_t System::Convert::ToInt64(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```


### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | สตริงที่จะทำการแปลง |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | การรวมค่าแบบบิตของ enum NumberStyles ที่ระบุสไตล์ที่อนุญาตของสตริงตัวเลข |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | พอยน์เตอร์ไปยังอ็อบเจ็กต์ที่บรรจุข้อมูลการจัดรูปแบบสตริง |

### ค่าที่คืน

ค่า integer 64-bit ที่เท่ากับตัวเลขที่แสดงโดยสตริงที่ระบุ

## Convert::ToInt64(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) เมธodb


```cpp
static int64_t System::Convert::ToInt64(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToInt64(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) เมธodb


```cpp
static int64_t System::Convert::ToInt64(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToInt64(const String\&, Globalization::NumberStyles, std::nullptr_t) เมธodb


```cpp
static int64_t System::Convert::ToInt64(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToInt64(Enum) เมธodb


```cpp
template<typename Enum,typename> static int64_t System::Convert::ToInt64(Enum value)
```

## Convert::ToInt64(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) เมธodb

แปลงค่าที่บรรจุไว้ให้เป็นค่า integer 64-bit ที่เทียบเท่า

```cpp
static int64_t System::Convert::ToInt64(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```


### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | shared pointer ไปยังอ็อบเจ็กต์ที่บรรจุค่าที่จะทำการแปลง |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | รูปแบบสตริงที่จะใช้หากประเภทของค่าที่บรรจุเป็น [String](../../string/) |

### ค่าที่คืน

ค่า integer 64-bit ที่เทียบเท่ากับค่าที่บรรจุที่ระบุ

## ดูเพิ่มเติม

* Enum [NumberStyles](../../../system.globalization/numberstyles/)
* Typedef [SharedPtr](../../sharedptr/)
* Class [Decimal](../../decimal/)
* Class [DateTime](../../datetime/)
* Class [String](../../string/)
* Class [IFormatProvider](../../iformatprovider/)
* Class [CultureInfo](../../../system.globalization/cultureinfo/)
* Class [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* Class [Object](../../object/)
* Struct [Convert](../)
* Struct [Enum](../../enum/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)