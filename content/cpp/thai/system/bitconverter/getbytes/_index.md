---
title: GetBytes()
second_title: อ้างอิง API ของ Aspose.Slides สำหรับ C++
description: แปลงค่า boolean ที่ระบุเป็นอาร์เรย์ของไบต์
type: docs
weight: 14
url: /th/system/bitconverter/getbytes/
---
## BitConverter::GetBytes(bool) เมธอด


แปลงค่า boolean ที่ระบุเป็นอาร์เรย์ของไบต์

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(bool value)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **bool** | ค่า boolean เพื่อแปลง |

### ค่าที่ส่งคืน

อาร์เรย์ขนาด 1 ไบต์ที่แสดงค่าที่ระบุ

## BitConverter::GetBytes(char_t) เมธอด


แปลงค่า char_t ที่ระบุเป็นอาร์เรย์ของไบต์

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(char_t value)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | char_t | ค่า char_t เพื่อแปลง |

### ค่าที่ส่งคืน

อาร์เรย์ขนาด 2 ไบต์ที่แสดงค่าที่ระบุ

## BitConverter::GetBytes(int16_t) เมธอด


แปลงค่าเต็ม 16-bit ที่ระบุเป็นอาร์เรย์ของไบต์

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int16_t value)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **int16_t** | ค่าเต็ม 16-bit เพื่อแปลง |

### ค่าที่ส่งคืน

อาร์เรย์ขนาด 2 ไบต์ที่แสดงค่าที่ระบุ

## BitConverter::GetBytes(int) เมธอด


แปลงค่าเต็ม 32-bit ที่ระบุเป็นอาร์เรย์ของไบต์

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int value)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | int | ค่าเต็ม 32-bit เพื่อแปลง |

### ค่าที่ส่งคืน

อาร์เรย์ขนาด 4 ไบต์ที่แสดงค่าที่ระบุ

## BitConverter::GetBytes(int64_t) เมธอด


แปลงค่าเต็ม 64-bit ที่ระบุเป็นอาร์เรย์ของไบต์

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int64_t value)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **int64_t** | ค่าเต็ม 64-bit เพื่อแปลง |

### ค่าที่ส่งคืน

อาร์เรย์ขนาด 8 ไบต์ที่แสดงค่าที่ระบุ

## BitConverter::GetBytes(uint16_t) เมธอด


แปลงค่า unsigned 16-bit ที่ระบุเป็นอาร์เรย์ของไบต์

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint16_t value)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **uint16_t** | ค่า unsigned 16-bit เพื่อแปลง |

### ค่าที่ส่งคืน

อาร์เรย์ขนาด 2 ไบต์ที่แสดงค่าที่ระบุ

## BitConverter::GetBytes(uint32_t) เมธอด


แปลงค่า unsigned 32-bit ที่ระบุเป็นอาร์เรย์ของไบต์

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint32_t value)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **uint32_t** | ค่า unsigned 32-bit เพื่อแปลง |

### ค่าที่ส่งคืน

อาร์เรย์ขนาด 4 ไบต์ที่แสดงค่าที่ระบุ

## BitConverter::GetBytes(uint64_t) เมธอด


แปลงค่า unsigned 64-bit ที่ระบุเป็นอาร์เรย์ของไบต์

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint64_t value)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **uint64_t** | ค่า unsigned 64-bit เพื่อแปลง |

### ค่าที่ส่งคืน

อาร์เรย์ขนาด 8 ไบต์ที่แสดงค่าที่ระบุ

## BitConverter::GetBytes(float) เมธอด


แปลงค่า floating-point ความแม่นยำเดียวที่ระบุเป็นอาร์เรย์ของไบต์

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(float value)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **float** | ค่า floating-point เพื่อแปลง |

### ค่าที่ส่งคืน

อาร์เรย์ขนาด 4 ไบต์ที่แสดงค่าที่ระบุ

## BitConverter::GetBytes(double) เมธอด


แปลงค่า floating-point ความแม่นยำคู่ที่ระบุเป็นอาร์เรย์ของไบต์

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(double value)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **double** | ค่า floating-point เพื่อแปลง |

### ค่าที่ส่งคืน

อาร์เรย์ขนาด 8 ไบต์ที่แสดงค่าที่ระบุ

## ดูเพิ่มเติม

* Typedef [ArrayPtr](../../arrayptr/)
* คลาส [BitConverter](../)
* เนมสเปซ [System](../../)
* ไลบรารี [Aspose.Slides](../../../)