---
title: WriteLine()
second_title: เอกสารอ้างอิง API ของ Aspose.Slides สำหรับ C++
description: ส่งค่าตัวจัดท้ายบรรทัดปัจจุบันไปยังสตรีมผลลัพธ์ที่แสดงโดยอ็อบเจกต์ปัจจุบัน
type: docs
weight: 27
url: /th/system/consoleoutput/writeline/
---
## ConsoleOutput::WriteLine() เมธอด

ส่งค่าตัวจัดท้ายบรรทัดปัจจุบันไปยังสตรีมผลลัพธ์ที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::ConsoleOutput::WriteLine() override
```

## ConsoleOutput::WriteLine(const SharedPtr\<Object\>\&) เมธอด

ส่งค่าการแสดงผลเป็นสตริงของอ็อบเจกต์ที่ระบุ ตามด้วยตัวจัดท้ายบรรทัดปัจจุบันไปยังสตรีมผลลัพธ์ที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::ConsoleOutput::WriteLine(const SharedPtr<Object> &value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | อ็อบเจกต์ที่จะแสดงผล |

## ConsoleOutput::WriteLine(bool) เมธอด

ส่งค่าการแสดงผลเป็นสตริงของค่า bool ที่ระบุ ตามด้วยตัวจัดท้ายบรรทัดปัจจุบันไปยังสตรีมผลลัพธ์ที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::ConsoleOutput::WriteLine(bool value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **bool** | อ็อบเจกต์ที่จะแสดงผล |

## ConsoleOutput::WriteLine(char_t) เมธอด

ส่งค่าตัวอักษรที่ระบุ ตามด้วยตัวจัดท้ายบรรทัดปัจจุบันไปยังสตรีมผลลัพธ์ที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::ConsoleOutput::WriteLine(char_t value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | char_t | ค่าที่จะแสดงผล |

## ConsoleOutput::WriteLine(Decimal) เมธอด

ส่งค่าการแสดงผลเป็นสตริงของค่า [Decimal](../../decimal/) ตามด้วยตัวจัดท้ายบรรทัดปัจจุบันไปยังสตรีมผลลัพธ์ที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::ConsoleOutput::WriteLine(Decimal value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | ค่าที่จะแสดงผล |

## ConsoleOutput::WriteLine(double) เมธอด

ส่งค่าการแสดงผลเป็นสตริงของค่าเลขทศนิยมความแม่นยำคู่ตามด้วยตัวจัดท้ายบรรทัดปัจจุบันไปยังสตรีมผลลัพธ์ที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::ConsoleOutput::WriteLine(double value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **double** | ค่าที่จะแสดงผล |

## ConsoleOutput::WriteLine(int) เมธอด

ส่งค่าการแสดงผลเป็นสตริงของค่า จำนวนเต็ม 32-บิตตามด้วยตัวจัดท้ายบรรทัดปัจจุบันไปยังสตรีมผลลัพธ์ที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::ConsoleOutput::WriteLine(int value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | int | ค่าที่จะแสดงผล |

## ConsoleOutput::WriteLine(int64_t) เมธอด

ส่งค่าการแสดงผลเป็นสตริงของค่า จำนวนเต็ม 64-บิตตามด้วยตัวจัดท้ายบรรทัดปัจจุบันไปยังสตรีมผลลัพธ์ที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::ConsoleOutput::WriteLine(int64_t value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **int64_t** | ค่าที่จะแสดงผล |

## ConsoleOutput::WriteLine(float) เมธอด

ส่งค่าการแสดงผลเป็นสตริงของค่าเลขทศนิยมความแม่นยำเดียวตามด้วยตัวจัดท้ายบรรทัดปัจจุบันไปยังสตรีมผลลัพธ์ที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::ConsoleOutput::WriteLine(float value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **float** | ค่าที่จะแสดงผล |

## ConsoleOutput::WriteLine(const String\&) เมธอด

ส่งอ็อบเจกต์สตริงที่ระบุ ตามด้วยตัวจัดท้ายบรรทัดปัจจุบันไปยังสตรีมผลลัพธ์ที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::ConsoleOutput::WriteLine(const String &value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [String](../../string/)\& | อ็อบเจกต์สตริงที่จะแสดงผล |

## ConsoleOutput::WriteLine(uint32_t) เมธอด

ส่งค่าการแสดงผลเป็นสตริงของค่า จำนวนเต็มบวก 32-บิตตามด้วยตัวจัดท้ายบรรทัดปัจจุบันไปยังสตรีมผลลัพธ์ที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::ConsoleOutput::WriteLine(uint32_t value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **uint32_t** | ค่าที่จะแสดงผล |

## ConsoleOutput::WriteLine(uint64_t) เมธอด

ส่งค่าการแสดงผลเป็นสตริงของค่า จำนวนเต็มบวก 64-บิตตามด้วยตัวจัดท้ายบรรทัดปัจจุบันไปยังสตรีมผลลัพธ์ที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::ConsoleOutput::WriteLine(uint64_t value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **uint64_t** | ค่าที่จะแสดงผล |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&) เมธอด

ส่งค่าการแสดงผลเป็นสตริงของอาเรย์อักขระที่ระบุ ตามด้วยตัวจัดท้ายบรรทัดปัจจุบันไปยังสตรีมผลลัพธ์ที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | อาเรย์ที่จะแสดงผล |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&, int32_t, int32_t) เมธอด

ส่งค่าการแสดงผลเป็นสตริงของช่วงค่าของอาเรย์อักขระที่ระบุ ตามด้วยตัวจัดท้ายบรรทัดปัจจุบันไปยังสตรีมผลลัพธ์ที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | อาเรย์ที่บรรจุค่าที่จะทำการแสดงผล |
| index | **int32_t** | ดัชนีที่ช่วงขององค์ประกอบที่จะทำการแสดงผลเริ่มต้น |
| count | **int32_t** | จำนวนขององค์ประกอบในช่วงขององค์ประกอบที่จะทำการแสดงผล |

## ConsoleOutput::WriteLine(const char_t \*) เมธอด

ส่งสตริง C ที่ระบุ ตามด้วยตัวจัดท้ายบรรทัดปัจจุบันไปยังสตรีมผลลัพธ์ที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::ConsoleOutput::WriteLine(const char_t *value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const char_t * | c-string ที่จะแสดงผล |

## ConsoleOutput::WriteLine(const TypeInfo\&) เมธอด

ส่งค่าการแสดงผลเป็นสตริงของอ็อบเจกต์ [TypeInfo](../../typeinfo/) ที่ระบุ ตามด้วยตัวจัดท้ายบรรทัดปัจจุบันไปยังสตรีมผลลัพธ์ที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::ConsoleOutput::WriteLine(const TypeInfo &value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | [TypeInfo](../../typeinfo/) ที่จะแสดงผล |

## ConsoleOutput::WriteLine(const char \*) เมธอด




```cpp
void System::ConsoleOutput::WriteLine(const char *)=delete
```

## ดูเพิ่มเติม

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Class [ConsoleOutput](../)
* Class [Object](../../object/)
* Class [Decimal](../../decimal/)
* Class [String](../../string/)
* Class [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)