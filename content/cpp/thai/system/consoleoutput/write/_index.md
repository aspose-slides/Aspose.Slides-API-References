---
title: Write()
second_title: Aspose.Slides สำหรับ C++ API Reference
description: ส่งออกการแสดงผลเป็นสตริงของค่า bool ที่ระบุไปยังสตรีมเอาต์พุตที่แทนด้วยอ็อบเจ็กต์ปัจจุบัน
type: docs
weight: 14
url: /th/system/consoleoutput/write/
---
## ConsoleOutput::Write(bool) เมธอด

ส่งออกการแสดงผลเป็นสตริงของค่า bool ที่ระบุไปยังสตรีมเอาต์พุตที่แทนด้วยออบเจ็กต์ปัจจุบัน

```cpp
void System::ConsoleOutput::Write(bool value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **bool** | ค่าที่จะส่งออก |

## ConsoleOutput::Write(const SharedPtr\<Object\>\&) เมธอด

ส่งออกการแสดงผลเป็นสตริงของอ็อบเจ็กต์ที่ระบุไปยังสตรีมเอาต์พุตที่แทนด้วยออบเจ็กต์ปัจจุบัน

```cpp
void System::ConsoleOutput::Write(const SharedPtr<Object> &value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | อ็อบเจ็กต์ที่ต้องการส่งออก |

## ConsoleOutput::Write(char_t) เมธอด

ส่งออกค่าตัวอักษรที่ระบุไปยังสตรีมเอาต์พุตที่แทนด้วยออบเจ็กต์ปัจจุบัน

```cpp
void System::ConsoleOutput::Write(char_t value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | char_t | ค่าที่จะส่งออก |

## ConsoleOutput::Write(Decimal) เมธอด

ส่งออกการแสดงผลเป็นสตริงของค่า [Decimal](../../decimal/) ไปยังสตรีมเอาต์พุตที่แทนด้วยออบเจ็กต์ปัจจุบัน

```cpp
void System::ConsoleOutput::Write(Decimal value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | ค่าที่จะส่งออก |

## ConsoleOutput::Write(double) เมธอด

ส่งออกการแสดงผลเป็นสตริงของค่าทศนิยมแบบ double-precision floating-point ไปยังสตรีมเอาต์พุตที่แทนด้วยออบเจ็กต์ปัจจุบัน

```cpp
void System::ConsoleOutput::Write(double value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **double** | ค่าที่จะส่งออก |

## ConsoleOutput::Write(int32_t) เมธอด

ส่งออกการแสดงผลเป็นสตริงของค่าตัวเลขเต็ม 32 บิตไปยังสตรีมเอาต์พุตที่แทนด้วยออบเจ็กต์ปัจจุบัน

```cpp
void System::ConsoleOutput::Write(int32_t value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **int32_t** | ค่าที่จะส่งออก |

## ConsoleOutput::Write(int64_t) เมธอด

ส่งออกการแสดงผลเป็นสตริงของค่าตัวเลขเต็ม 64 บิตไปยังสตรีมเอาต์พุตที่แทนด้วยออบเจ็กต์ปัจจุบัน

```cpp
void System::ConsoleOutput::Write(int64_t value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **int64_t** | ค่าที่จะส่งออก |

## ConsoleOutput::Write(float) เมธอด

ส่งออกการแสดงผลเป็นสตริงของค่าทศนิยมแบบ single-precision ไปยังสตรีมเอาต์พุตที่แทนด้วยออบเจ็กต์ปัจจุบัน

```cpp
void System::ConsoleOutput::Write(float value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **float** | ค่าที่จะส่งออก |

## ConsoleOutput::Write(const String\&) เมธอด

ส่งออกอ็อบเจ็กต์สตริงที่ระบุไปยังสตรีมเอาต์พุตที่แทนด้วยออบเจ็กต์ปัจจุบัน

```cpp
void System::ConsoleOutput::Write(const String &value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [String](../../string/)\& | อ็อบเจ็กต์สตริงที่ต้องการส่งออก |

## ConsoleOutput::Write(uint32_t) เมธอด

ส่งออกการแสดงผลเป็นสตริงของค่าตัวเลขเต็มบวก 32 บิตไปยังสตรีมเอาต์พุตที่แทนด้วยออบเจ็กต์ปัจจุบัน

```cpp
void System::ConsoleOutput::Write(uint32_t value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **uint32_t** | ค่าที่จะส่งออก |

## ConsoleOutput::Write(uint64_t) เมธอด

ส่งออกการแสดงผลเป็นสตริงของค่าตัวเลขเต็มบวก 64 บิตไปยังสตรีมเอาต์พุตที่แทนด้วยออบเจ็กต์ปัจจุบัน

```cpp
void System::ConsoleOutput::Write(uint64_t value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | **uint64_t** | ค่าที่จะส่งออก |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&) เมธอด

ส่งออกการแสดงผลเป็นสตริงของอาร์เรย์อักขระที่ระบุไปยังสตรีมเอาต์พุตที่แทนด้วยออบเจ็กต์ปัจจุบัน

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | อาร์เรย์ที่ต้องการส่งออก |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&, int32_t, int32_t) เมธอด

ส่งออกการแสดงผลเป็นสตริงของช่วงค่าจากอาร์เรย์อักขระที่ระบุไปยังสตรีมเอาต์พุตที่แทนด้วยออบเจ็กต์ปัจจุบัน

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | อาร์เรย์ที่มีค่าที่จะส่งออก |
| index | **int32_t** | ดัชนีที่ช่วงขององค์ประกอบที่จะส่งออกเริ่มต้น |
| count | **int32_t** | จำนวนขององค์ประกอบในช่วงที่ต้องการส่งออก |

## ConsoleOutput::Write(const char_t *) เมธอด

ส่งออก c-string ที่ระบุไปยังสตรีมเอาต์พุตที่แทนด้วยออบเจ็กต์ปัจจุบัน

```cpp
void System::ConsoleOutput::Write(const char_t *value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const char_t * | c-string ที่ต้องการส่งออก |

## ConsoleOutput::Write(const TypeInfo\&) เมธอด

ส่งออกการแสดงผลเป็นสตริงของอ็อบเจ็กต์ [TypeInfo](../../typeinfo/) ที่ระบุไปยังสตรีมเอาต์พุตที่แทนด้วยออบเจ็กต์ปัจจุบัน

```cpp
void System::ConsoleOutput::Write(const TypeInfo &value) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | อ็อบเจ็กต์ [TypeInfo](../../typeinfo/) ที่ต้องการส่งออก |

## ConsoleOutput::Write(const char *) เมธอด




```cpp
void System::ConsoleOutput::Write(const char *)=delete
```

## ดูเพิ่มเติม

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* คลาส [ConsoleOutput](../)
* คลาส [Object](../../object/)
* คลาส [Decimal](../../decimal/)
* คลาส [String](../../string/)
* คลาส [TypeInfo](../../typeinfo/)
* เนมสเปซ [System](../../)
* Library [Aspose.Slides](../../../)