---
title: operator<<()
second_title: Aspose.Slides สำหรับ C++ เอกสารอ้างอิง API
description: แทรกข้อมูลลงในสตรีมโดยใช้การเข้ารหัส UTF-8.
type: docs
weight: 2159
url: /th/system/operator_less_less/
---
## System::operator<<(std::ostream\&, DateTime) ฟังก์ชัน

แทรกข้อมูลลงในสตรีมโดยใช้การเข้ารหัส UTF-8.

```cpp
std::ostream & System::operator<<(std::ostream &stream, DateTime date_time)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::ostream\& | Output stream to insert data to. |
| date_time | [DateTime](../datetime/) | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::wostream\&, DateTime) ฟังก์ชัน

แทรกข้อมูลลงในสตรีม.

```cpp
std::wostream & System::operator<<(std::wostream &stream, DateTime date_time)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::wostream\& | Output stream to insert data to. |
| date_time | [DateTime](../datetime/) | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::ostream\&, DateTimeOffset) ฟังก์ชัน

แทรกข้อมูลลงในสตรีมโดยใช้การเข้ารหัส UTF-8.

```cpp
std::ostream & System::operator<<(std::ostream &stream, DateTimeOffset value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::ostream\& | Output stream to insert data to. |
| value | [DateTimeOffset](../datetimeoffset/) | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::wostream\&, DateTimeOffset) ฟังก์ชัน

แทรกข้อมูลลงในสตรีม.

```cpp
std::wostream & System::operator<<(std::wostream &stream, DateTimeOffset value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::wostream\& | Output stream to insert data to. |
| value | [DateTimeOffset](../datetimeoffset/) | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::ostream\&, const Decimal\&) ฟังก์ชัน

แทรกข้อมูลลงในสตรีมโดยใช้การเข้ารหัส UTF-8.

```cpp
std::ostream & System::operator<<(std::ostream &stream, const Decimal &decimal)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::ostream\& | Output stream to insert data to. |
| decimal | const [Decimal](../decimal/)\& | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::wostream\&, const Decimal\&) ฟังก์ชัน

แทรกข้อมูลลงในสตรีม.

```cpp
std::wostream & System::operator<<(std::wostream &stream, const Decimal &decimal)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::wostream\& | Output stream to insert data to. |
| decimal | const [Decimal](../decimal/)\& | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::ostream\&, const Exception\&) ฟังก์ชัน

แทรกข้อมูลลงในสตรีมโดยใช้การเข้ารหัส UTF-8.

```cpp
std::ostream & System::operator<<(std::ostream &stream, const Exception &exception)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::ostream\& | Output stream to insert data to. |
| exception | const [Exception](../exception/)\& | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::wostream\&, const Exception\&) ฟังก์ชัน

แทรกข้อมูลลงในสตรีม.

```cpp
std::wostream & System::operator<<(std::wostream &stream, const Exception &exception)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::wostream\& | Output stream to insert data to. |
| exception | const [Exception](../exception/)\& | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::ostream\&, const Guid\&) ฟังก์ชัน

แทรกข้อมูลลงในสตรีมโดยใช้การเข้ารหัส UTF-8.

```cpp
std::ostream & System::operator<<(std::ostream &stream, const Guid &guid)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::ostream\& | Output stream to insert data to. |
| guid | const [Guid](../guid/)\& | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::wostream\&, const Guid\&) ฟังก์ชัน

แทรกข้อมูลลงในสตรีม.

```cpp
std::wostream & System::operator<<(std::wostream &stream, const Guid &guid)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::wostream\& | Output stream to insert data to. |
| guid | const [Guid](../guid/)\& | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::ostream\&, const Nullable\<T\>\&) ฟังก์ชัน

แทรกข้อมูลลงในสตรีมโดยใช้การเข้ารหัส UTF-8.

```cpp
template<typename T> std::ostream & System::operator<<(std::ostream &stream, const Nullable<T> &value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::ostream\& | Output stream to insert data to. |
| value | const [Nullable](../nullable/)\<T\>\& | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::wostream\&, const Nullable\<T\>\&) ฟังก์ชัน

แทรกข้อมูลลงในสตรีม.

```cpp
template<typename T> std::wostream & System::operator<<(std::wostream &stream, const Nullable<T> &value)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::wostream\& | Output stream to insert data to. |
| value | const [Nullable](../nullable/)\<T\>\& | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::ostream\&, const System::Object\&) ฟังก์ชัน

แทรกข้อมูลลงในสตรีมโดยใช้การเข้ารหัส UTF-8.

```cpp
std::ostream & System::operator<<(std::ostream &stream, const System::Object &object)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::ostream\& | Output stream to insert data to. |
| object | const [System::Object](../object/)\& | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::wostream\&, const System::Object\&) ฟังก์ชัน

แทรกข้อมูลลงในสตรีม.

```cpp
std::wostream & System::operator<<(std::wostream &stream, const System::Object &object)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::wostream\& | Output stream to insert data to. |
| object | const [System::Object](../object/)\& | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::ostream\&, const OperatingSystem\&) ฟังก์ชัน

แทรกข้อมูลลงในสตรีมโดยใช้การเข้ารหัส UTF-8.

```cpp
std::ostream & System::operator<<(std::ostream &stream, const OperatingSystem &os)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::ostream\& | Output stream to insert data to. |
| os | const [OperatingSystem](../operatingsystem/)\& | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::wostream\&, const OperatingSystem\&) ฟังก์ชัน

แทรกข้อมูลลงในสตรีม.

```cpp
std::wostream & System::operator<<(std::wostream &stream, const OperatingSystem &os)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::wostream\& | Output stream to insert data to. |
| os | const [OperatingSystem](../operatingsystem/)\& | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::ostream\&, const SharedPtr\<T\>\&) ฟังก์ชัน

แทรกข้อมูลลงในสตรีมโดยใช้การเข้ารหัส UTF-8.

```cpp
template<typename T> std::ostream & System::operator<<(std::ostream &stream, const SharedPtr<T> &object_ptr)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::ostream\& | Output stream to insert data to. |
| object_ptr | const [SharedPtr](../sharedptr/)\<T\>\& | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::wostream\&, const SharedPtr\<T\>\&) ฟังก์ชัน

แทรกข้อมูลลงในสตรีม.

```cpp
template<typename T> std::wostream & System::operator<<(std::wostream &stream, const SharedPtr<T> &object_ptr)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::wostream\& | Output stream to insert data to. |
| object_ptr | const [SharedPtr](../sharedptr/)\<T\>\& | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::ostream\&, const String\&) ฟังก์ชัน

ส่งออกสตริงไปยังสตรีมเอาต์พุตโดยใช้การเข้ารหัส UTF-8.

```cpp
std::ostream & System::operator<<(std::ostream &os, const String &str)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| os | std::ostream\& | ออบเจกต์สตรีมเอาต์พุต (การสร้างอินสแตนซ์ของ **basic_ostream** กับ **char**). |
| str | const [String](../string/)\& | สตริงที่จะส่งไปยังสตรีมเอาต์พุต. |

### ค่าที่ส่งกลับ

สตรีมเอาต์พุตที่สตริงถูกเพิ่มเข้าไป.

## System::operator<<(std::wostream\&, const String\&) ฟังก์ชัน

ส่งออกสตริงไปยังสตรีมเอาต์พุต.

```cpp
std::wostream & System::operator<<(std::wostream &os, const String &str)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| os | std::wostream\& | ออบเจกต์สตรีมเอาต์พุต (การสร้างอินสแตนซ์ของ **basic_ostream** กับ ****wchar_t****). |
| str | const [String](../string/)\& | สตริงที่จะส่งไปยังสตรีมเอาต์พุต. |

### ค่าที่ส่งกลับ

สตรีมเอาต์พุตที่สตริงถูกเพิ่มเข้าไป.

## System::operator<<(std::ostream\&, TimeSpan) ฟังก์ชัน

แทรกข้อมูลลงในสตรีมโดยใช้การเข้ารหัส UTF-8.

```cpp
std::ostream & System::operator<<(std::ostream &stream, TimeSpan time_span)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::ostream\& | Output stream to insert data to. |
| time_span | [TimeSpan](../timespan/) | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::wostream\&, TimeSpan) ฟังก์ชัน

แทรกข้อมูลลงในสตรีม.

```cpp
std::wostream & System::operator<<(std::wostream &stream, TimeSpan time_span)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::wostream\& | Output stream to insert data to. |
| time_span | [TimeSpan](../timespan/) | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::ostream\&, const TypeInfo\&) ฟังก์ชัน

แทรกข้อมูลลงในสตรีมโดยใช้การเข้ารหัส UTF-8.

```cpp
std::ostream & System::operator<<(std::ostream &stream, const TypeInfo &type_info)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::ostream\& | Output stream to insert data to. |
| type_info | const [TypeInfo](../typeinfo/)\& | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::wostream\&, const TypeInfo\&) ฟังก์ชัน

แทรกข้อมูลลงในสตรีม.

```cpp
std::wostream & System::operator<<(std::wostream &stream, const TypeInfo &type_info)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::wostream\& | Output stream to insert data to. |
| type_info | const [TypeInfo](../typeinfo/)\& | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::ostream\&, const Version\&) ฟังก์ชัน

แทรกข้อมูลลงในสตรีมโดยใช้การเข้ารหัส UTF-8.

```cpp
std::ostream & System::operator<<(std::ostream &stream, const Version &version)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::ostream\& | Output stream to insert data to. |
| version | const [Version](../version/)\& | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::wostream\&, const Version\&) ฟังก์ชัน

แทรกข้อมูลลงในสตรีม.

```cpp
std::wostream & System::operator<<(std::wostream &stream, const Version &version)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::wostream\& | Output stream to insert data to. |
| version | const [Version](../version/)\& | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::ostream\&, const WeakPtr\<T\>\&) ฟังก์ชัน

แทรกข้อมูลลงในสตรีมโดยใช้การเข้ารหัส UTF-8.

```cpp
template<typename T> std::ostream & System::operator<<(std::ostream &stream, const WeakPtr<T> &object_ptr)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::ostream\& | Output stream to insert data to. |
| object_ptr | const [WeakPtr](../weakptr/)\<T\>\& | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## System::operator<<(std::wostream\&, const WeakPtr\<T\>\&) ฟังก์ชัน

แทรกข้อมูลลงในสตรีม.

```cpp
template<typename T> std::wostream & System::operator<<(std::wostream &stream, const WeakPtr<T> &object_ptr)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| stream | std::wostream\& | Output stream to insert data to. |
| object_ptr | const [WeakPtr](../weakptr/)\<T\>\& | [Data](../../system.data/) to insert. |

### ค่าที่ส่งกลับ

**stream**.

## ดูเพิ่มเติม

* Typedef [Exception](../exception/)
* Typedef [SharedPtr](../sharedptr/)
* คลาส [DateTime](../datetime/)
* คลาส [DateTimeOffset](../datetimeoffset/)
* คลาส [Decimal](../decimal/)
* คลาส [Guid](../guid/)
* คลาส [Nullable](../nullable/)
* คลาส [Object](../object/)
* คลาส [OperatingSystem](../operatingsystem/)
* คลาส [String](../string/)
* คลาส [TimeSpan](../timespan/)
* คลาส [TypeInfo](../typeinfo/)
* คลาส [Version](../version/)
* คลาส [WeakPtr](../weakptr/)
* เนมสเปซ [System](../)
* Library [Aspose.Slides](../../)