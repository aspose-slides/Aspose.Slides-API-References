---
title: "System::Net::Http::Headers"
second_title: Aspose.Slides สำหรับ C++ อ้างอิง API
description: 
type: docs
weight: 690
url: /th/system.net.http.headers/
---
## Classes

| คลาส | คำอธิบาย |
| --- | --- |
| [AuthenticationHeaderValue](./authenticationheadervalue/) | แทนค่าของส่วนหัว 'Authorization', 'ProxyAuthorization', 'WWW-Authenticate' และ 'Proxy-Authenticate' . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [CacheControlHeaderValue](./cachecontrolheadervalue/) | แทนค่าของส่วนหัว 'Cache-Control' . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [ContentDispositionHeaderValue](./contentdispositionheadervalue/) | แทนค่าของส่วนหัว 'Content-Disposition' . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [ContentRangeHeaderValue](./contentrangeheadervalue/) | แทนค่าของส่วนหัว 'Content-Range' . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [EntityTagHeaderValue](./entitytagheadervalue/) | แทนค่าของส่วนหัว 'Entity-Tag' . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [HttpContentHeaders](./httpcontentheaders/) | แทนคอลเลกชันของส่วนหัว 'Content' . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [HttpHeaders](./httpheaders/) | คอลเลกชันของส่วนหัว HTTP . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [HttpHeaderValueCollection](./httpheadervaluecollection/) | แทนคอลเลกชันของค่าของส่วนหัว . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [HttpHeaderValueCollection< System::String >](./httpheadervaluecollection_tmpl_system_string__end_tmpl/) | การกำหนดส่วนย่อยของเทมเพลต [HttpHeaderValueCollection](./httpheadervaluecollection/) สำหรับประเภท [String](../system/string/) . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [HttpRequestHeaders](./httprequestheaders/) | แทนคอลเลกชันของส่วนหัว 'Request' . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [HttpResponseHeaders](./httpresponseheaders/) | แทนคอลเลกชันของส่วนหัว 'Response' . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [MediaTypeHeaderValue](./mediatypeheadervalue/) | แทนชนิด MIME ในค่าของส่วนหัว 'Content-Type' . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [MediaTypeWithQualityHeaderValue](./mediatypewithqualityheadervalue/) | แทนชนิด MIME พร้อมกับคปัจจัยคุณภาพเพิ่มเติมในค่าของส่วนหัว 'Content-Type' . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [NameValueHeaderValue](./namevalueheadervalue/) | แทนคู่คีย์/ค่าเพื่อใช้ในส่วนหัว . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [NameValueWithParametersHeaderValue](./namevaluewithparametersheadervalue/) | แทนคู่คีย์/ค่าพร้อมพารามิเตอร์เพื่อใช้ในส่วนหัว . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [ObjectCollection](./objectcollection/) | แทนคอลเลกชันของอ็อบเจ็กต์. |
| [ProductHeaderValue](./productheadervalue/) | แทนโทเค็นผลิตภัณฑ์ในค่าของส่วนหัว 'User-Agent' . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [ProductInfoHeaderValue](./productinfoheadervalue/) | แทนผลิตภัณฑ์หรือคอมเมนต์ในค่าของส่วนหัว 'User-Agent' . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสเทคหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [RangeConditionHeaderValue](./rangeconditionheadervalue/) | แทนค่าของส่วนหัว 'If-Range' . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสเทคหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [RangeHeaderValue](./rangeheadervalue/) | แทนค่าของส่วนหัว '[Range](../system/range/)' . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสเทคหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [RangeItemHeaderValue](./rangeitemheadervalue/) | แทนช่วงของไบต์ในค่าของส่วนหัว '[Range](../system/range/)' . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสเทคหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [RetryConditionHeaderValue](./retryconditionheadervalue/) | แทนค่าของส่วนหัว 'Retry-After' . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสเทคหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [StringWithQualityHeaderValue](./stringwithqualityheadervalue/) | แทนค่าพร้อมคุณภาพเพิ่มเติมของส่วนหัวแบบสตริง . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสเทคหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [TransferCodingHeaderValue](./transfercodingheadervalue/) | แทนค่าของส่วนหัว 'Accept-Encoding' . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสเทคหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/ือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [TransferCodingWithQualityHeaderValue](./transfercodingwithqualityheadervalue/) | แทนค่าพร้อมคุณภาพเพิ่มเติมของส่วนหัว 'Accept-Encoding' . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสเทคหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [ViaHeaderValue](./viaheadervalue/) | แทนค่าของส่วนหัว 'Via' . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสเทคหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
| [WarningHeaderValue](./warningheadervalue/) | แทนค่าของส่วนหัว 'Warning' . วัตถุของคลาสนี้ควรได้รับการจัดสรรเท่านั้นด้วยฟังก์ชัน [System::MakeObject()](../system/makeobject/). อย่าสร้างอินสแตนซ์ของประเภทนี้บนสเทคหรือโดยใช้ตัวดำเนินการ new เนื่องจากจะทำให้เกิดข้อผิดพลาดรันไทม์และ/หรือการตรวจสอบข้อผิดพลาด. เสมอห่อคลาสนี้ด้วยตัวชี้ [System::SmartPtr](../system/smartptr/) และใช้ตัวชี้นี้เพื่อส่งให้ฟังก์ชันเป็นอาร์กิวเมนต์. |
## Typedefs

| ประเภทนิยาม | คำอธิบาย |
| --- | --- |
| [HeaderFunc](./headerfunc/) |  |