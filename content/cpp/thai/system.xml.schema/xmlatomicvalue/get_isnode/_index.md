---
title: get_IsNode()
second_title: Aspose.Slides สำหรับ C++ อ้างอิง API
description: ส่งคืนค่าซึ่งระบุว่าองค์ประกอบ XML หรือแอตทริบิวต์ที่ตรวจสอบแล้วเป็นโหนด XPath หรือเป็นค่าอะตอม
type: docs
weight: 1
url: /th/system.xml.schema/xmlatomicvalue/get_isnode/
---
## XmlAtomicValue::get_IsNode() เมธอด

ส่งคืนค่าซึ่งระบุว่าองค์ประกอบหรือแอตทริบิวต์ XML ที่ตรวจสอบแล้วเป็นโหนด [XPath](../../../system.xml.xpath/) หรือเป็นค่าอะตอม

```cpp
bool System::Xml::Schema::XmlAtomicValue::get_IsNode() override
```

### ค่าที่ส่งกลับ

**true** หาก XML element หรือ attribute ที่ตรวจสอบแล้วเป็นโหนด [XPath](../../../system.xml.xpath/); **false** หาก XML element หรือ attribute ที่ตรวจสอบแล้วเป็นค่าอะตอม

## ดูเพิ่ม

* คลาส [XmlAtomicValue](../)
* เนมสเปซ [System::Xml::Schema](../../)
* ไลบรารี [Aspose.Slides](../../../)