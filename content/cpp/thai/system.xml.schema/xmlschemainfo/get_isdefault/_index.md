---
title: get_IsDefault()
second_title: Aspose.Slides สำหรับ C++ เอกสารอ้างอิง API
description: คืนค่าที่บ่งชี้ว่าตำแหน่ง XML ที่ตรวจสอบนี้ถูกตั้งค่าเป็นผลลัพธ์ของค่าเริ่มต้นที่ถูกนำมาใช้ระหว่างการตรวจสอบ XML Schema Definition Language (XSD).
type: docs
weight: 27
url: /th/system.xml.schema/xmlschemainfo/get_isdefault/
---
## XmlSchemaInfo::get_IsDefault() เมธอด

คืนค่าที่บ่งชี้ว่าต้นโหนด XML ที่ตรวจสอบนี้ถูกตั้งค่าเป็นผลลัพธ์ของค่าเริ่มต้นที่ถูกนำมาใช้ระหว่างการตรวจสอบสกีมาของ XML [Schema](../../) ภาษาแบบจำเพาะ (XSD).

```cpp
bool System::Xml::Schema::XmlSchemaInfo::get_IsDefault() override
```

### ค่าที่ส่งกลับ

ค่า **bool**.

## ดูเพิ่มเติม

* คลาส [XmlSchemaInfo](../)
* เนมส페ซ [System::Xml::Schema](../../)
* ไลบรารี [Aspose.Slides](../../../)