---
title: idx_get()
second_title: Aspose.Slides สำหรับ C++ เอกสารอ้างอิง API
description: ดึงค่าหัวเรื่องโดยใช้หัวของคำขอที่ระบุ.
type: docs
weight: 14
url: /th/system.net/webheadercollection/idx_get/
---
## WebHeaderCollection::idx_get(HttpRequestHeader) เมธอด

ดึงค่าหัวเรื่องโดยใช้หัวของคำขอที่ระบุ.

```cpp
String System::Net::WebHeaderCollection::idx_get(HttpRequestHeader header)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| header | [HttpRequestHeader](../../httprequestheader/) | หัวเรื่อง |

### ค่าที่ส่งคืน

ค่าหัวเรื่องโดยใช้หัวที่ระบุ.

## WebHeaderCollection::idx_get(HttpResponseHeader) เมธอด

ดึงค่าหัวเรื่องโดยใช้หัวของการตอบสนองที่ระบุ.

```cpp
String System::Net::WebHeaderCollection::idx_get(HttpResponseHeader header)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| header | [HttpResponseHeader](../../httpresponseheader/) | หัวเรื่อง |

### ค่าที่ส่งคืน

ค่าหัวเรื่องโดยใช้หัวที่ระบุ.

## WebHeaderCollection::idx_get(String) เมธอด

ดึงค่าหัวเรื่องโดยใช้ชื่อหัวที่ระบุ.

```cpp
String System::Net::WebHeaderCollection::idx_get(String name)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| name | [String](../../../system/string/) | ชื่อหัวเรื่อง |

### ค่าที่ส่งคืน

ค่าหัวเรื่องโดยใช้ชื่อหัวที่ระบุ.

## ดูเพิ่มเติม

* Enum [HttpRequestHeader](../../httprequestheader/)
* Enum [HttpResponseHeader](../../httpresponseheader/)
* คลาส [String](../../../system/string/)
* คลาส [WebHeaderCollection](../)
* เนมสเปซ [System::Net](../../)
* Library [Aspose.Slides](../../../)