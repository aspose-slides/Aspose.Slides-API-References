---
title: HostToNetworkOrder()
second_title: Aspose.Slides สำหรับ C++ API Reference
description: แปลงลำดับไบต์ของโฮสต์ที่ระบุเป็นลำดับไบต์ของเครือข่ายที่สอดคล้องกัน.
type: docs
weight: 248
url: /th/system.net/ipaddress/hosttonetworkorder/
---
## IPAddress::HostToNetworkOrder(int64_t) เมธอด

แปลงลำดับไบต์ของโฮสต์ที่ระบุเป็นลำดับไบต์ของเครือข่ายที่สอดคล้องกัน.

```cpp
static int64_t System::Net::IPAddress::HostToNetworkOrder(int64_t host)
```

### Arguments

| พารามิเตอร์ | ชนิด | รายละเอียด |
| --- | --- | --- |
| host | **int64_t** | ค่าที่จะทำการแปลง. |

### ค่าที่ส่งกลับ

ลำดับไบต์ของเครือข่ายที่สอดคล้องกัน.

## IPAddress::HostToNetworkOrder(int32_t) เมธอด

แปลงลำดับไบต์ของโฮสต์ที่ระบุเป็นลำดับไบต์ของเครือข่ายที่สอดคล้องกัน.

```cpp
static int32_t System::Net::IPAddress::HostToNetworkOrder(int32_t host)
```

### Arguments

| พารามิเตอร์ | ชนิด | รายละเอียด |
| --- | --- | --- |
| host | **int32_t** | ค่าที่จะทำการแปลง. |

### ค่าที่ส่งกลับ

ลำดับไบต์ของเครือข่ายที่สอดคล้องกัน.

## IPAddress::HostToNetworkOrder(int16_t) เมธอด

แปลงลำดับไบต์ของโฮสต์ที่ระบุเป็นลำดับไบต์ของเครือข่ายที่สอดคล้องกัน.

```cpp
static int16_t System::Net::IPAddress::HostToNetworkOrder(int16_t host)
```

### Arguments

| พารามิเตอร์ | ชนิด | รายละเอียด |
| --- | --- | --- |
| host | **int16_t** | ค่าที่จะทำการแปลง. |

### ค่าที่ส่งกลับ

ลำดับไบต์ของเครือข่ายที่สอดคล้องกัน.

## ดูเพิ่มเติม

* คลาส [IPAddress](../)
* เนมสเปซ [System::Net](../../)
* ไลบรารี [Aspose.Slides](../../../)