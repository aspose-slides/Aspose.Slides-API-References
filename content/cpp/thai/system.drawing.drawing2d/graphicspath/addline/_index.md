---
title: AddLine()
second_title: อ้างอิง API ของ Aspose.Slides สำหรับ C++
description: เพิ่มเส้นที่ระบุไปยังพาธที่แสดงโดยอ็อบเจกต์ปัจจุบัน
type: docs
weight: 144
url: /th/system.drawing.drawing2d/graphicspath/addline/
---
## GraphicsPath::AddLine(const Point\&, const Point\&) เมธอด

เพิ่มเส้นที่ระบุไปยังพาธที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddLine(const Point &pt1, const Point &pt2)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| pt1 | const [Point](../../../system.drawing/point/)\& | จุดเริ่มต้นของเส้นที่ต้องการเพิ่ม |
| pt2 | const [Point](../../../system.drawing/point/)\& | จุดสิ้นสุดของเส้นที่ต้องการเพิ่ม |

## GraphicsPath::AddLine(const PointF\&, const PointF\&) เมธอด

เพิ่มเส้นที่ระบุไปยังพาธที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddLine(const PointF &pt1, const PointF &pt2)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| pt1 | const [PointF](../../../system.drawing/pointf/)\& | จุดเริ่มต้นของเส้นที่ต้องการเพิ่ม |
| pt2 | const [PointF](../../../system.drawing/pointf/)\& | จุดสิ้นสุดของเส้นที่ต้องการเพิ่ม |

## GraphicsPath::AddLine(int, int, int, int) เมธอด

เพิ่มเส้นที่ระบุไปยังพาธที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddLine(int x1, int y1, int x2, int y2)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| x1 | int | พิกัด X ของจุดเริ่มต้นของเส้นที่ต้องการเพิ่ม |
| y1 | int | พิกัด Y ของจุดเริ่มต้นของเส้นที่ต้องการเพิ่ม |
| x2 | int | พิกัด X ของจุดสิ้นสุดของเส้นที่ต้องการเพิ่ม |
| y2 | int | พิกัด Y ของจุดสิ้นสุดของเส้นที่ต้องการเพิ่ม |

## GraphicsPath::AddLine(float, float, float, float) เมธอด

เพิ่มเส้นที่ระบุไปยังพาธที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddLine(float x1, float y1, float x2, float y2)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| x1 | **float** | พิกัด X ของจุดเริ่มต้นของเส้นที่ต้องการเพิ่ม |
| y1 | **float** | พิกัด Y ของจุดเริ่มต้นของเส้นที่ต้องการเพิ่ม |
| x2 | **float** | พิกัด X ของจุดสิ้นสุดของเส้นที่ต้องการเพิ่ม |
| y2 | **float** | พิกัด Y ของจุดสิ้นสุดของเส้นที่ต้องการเพิ่ม |

## ดูเพิ่มเติม

* คลาส [Point](../../../system.drawing/point/)
* คลาส [GraphicsPath](../)
* คลาส [PointF](../../../system.drawing/pointf/)
* เนมสเปซ [System::Drawing::Drawing2D](../../)
* ไลบรารี [Aspose.Slides](../../../)