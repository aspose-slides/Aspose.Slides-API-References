---
title: AddCurve()
second_title: อ้างอิง API ของ Aspose.Slides สำหรับ C++
description: เพิ่มเส้นโค้งที่ระบุไปยังพาธที่แสดงโดยอ็อบเจกต์ปัจจุบัน
type: docs
weight: 274
url: /th/system.drawing.drawing2d/graphicspath/addcurve/
---
## GraphicsPath::AddCurve(const ArrayPtr\<PointF\>\&, float) เมธอด

เพิ่มเส้นโค้งที่ระบุไปยังพาธที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddCurve(const ArrayPtr<PointF> &points, float tension=0.5)
```

### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| points | const [ArrayPtr](../../../system/arrayptr/)\<[PointF](../../../system.drawing/pointf/)\>\& | จุดที่กำหนดเส้นโค้ง |
| tension | **float** | ระบุปริมาณที่เส้นโค้งโค้งระหว่างจุดควบคุม |

## GraphicsPath::AddCurve(const ArrayPtr\<Point\>\&, float) เมธอด

เพิ่มเส้นโค้งที่ระบุไปยังพาธที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddCurve(const ArrayPtr<Point> &points, float tension=0.5)
```

### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| points | const [ArrayPtr](../../../system/arrayptr/)\<[Point](../../../system.drawing/point/)\>\& | จุดที่กำหนดเส้นโค้ง |
| tension | **float** | ระบุปริมาณที่เส้นโค้งโค้งระหว่างจุดควบคุม |

## GraphicsPath::AddCurve(const ArrayPtr\<PointF\>\&, int, int, float) เมธอด

เพิ่มเส้นโค้งที่ระบุไปยังพาธที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddCurve(const ArrayPtr<PointF> &points, int offset, int number_of_segments, float tension)
```

### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| points | const [ArrayPtr](../../../system/arrayptr/)\<[PointF](../../../system.drawing/pointf/)\>\& | จุดที่กำหนดเส้นโค้ง |
| offset | int | ดัชนีของจุดใน **points** ที่ใช้เป็นจุดเริ่มต้นของเส้นโค้ง |
| number_of_segments | int | จำนวนส่วนที่ใช้วาดเส้นโค้ง |
| tension | **float** | ระบุปริมาณที่เส้นโค้งโค้งระหว่างจุดควบคุม |

## GraphicsPath::AddCurve(const ArrayPtr\<Point\>\&, int, int, float) เมธอด

เพิ่มเส้นโค้งที่ระบุไปยังพาธที่แสดงโดยอ็อบเจกต์ปัจจุบัน

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddCurve(const ArrayPtr<Point> &points, int offset, int number_of_segments, float tension)
```

### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| points | const [ArrayPtr](../../../system/arrayptr/)\<[Point](../../../system.drawing/point/)\>\& | จุดที่กำหนดเส้นโค้ง |
| offset | int | ดัชนีของจุดใน **points** ที่ใช้เป็นจุดเริ่มต้นของเส้นโค้ง |
| number_of_segments | int | จำนวนส่วนที่ใช้วาดเส้นโค้ง |
| tension | **float** | ระบุปริมาณที่เส้นโค้งโค้งระหว่างจุดควบคุม |

## ดูเพิ่มเติม

* Typedef [ArrayPtr](../../../system/arrayptr/)
* คลาส [PointF](../../../system.drawing/pointf/)
* คลาส [GraphicsPath](../)
* คลาส [Point](../../../system.drawing/point/)
* เนมสเปซ [System::Drawing::Drawing2D](../../)
* Library [Aspose.Slides](../../../)