---
title: AddBezier()
second_title: Aspose.Slides สำหรับ C++ อ้างอิง API
description: เพิ่มเส้นโค้ง Bezier แบบ cubic ที่ระบุลงในพาธที่เป็นตัวแทนของอ็อบเจกต์ปัจจุบัน
type: docs
weight: 118
url: /th/system.drawing.drawing2d/graphicspath/addbezier/
---
## GraphicsPath::AddBezier(const Point\&, const Point\&, const Point\&, const Point\&) เมธอด

เพิ่มเส้นโค้ง Bezier แบบ cubic ที่ระบุลงในพาธที่เป็นตัวแทนของอ็อบเจกต์ปัจจุบัน

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddBezier(const Point &pt1, const Point &pt2, const Point &pt3, const Point &pt4)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| pt1 | const [Point](../../../system.drawing/point/)\& | จุดเริ่มต้นของเส้นโค้งที่ต้องการเพิ่ม |
| pt2 | const [Point](../../../system.drawing/point/)\& | จุดควบคุมที่ 1 ของเส้นโค้งที่ต้องการเพิ่ม |
| pt3 | const [Point](../../../system.drawing/point/)\& | จุดควบคุมที่ 2 ของเส้นโค้งที่ต้องการเพิ่ม |
| pt4 | const [Point](../../../system.drawing/point/)\& | จุดสิ้นสุดของเส้นโค้งที่ต้องการเพิ่ม |

## GraphicsPath::AddBezier(const PointF\&, const PointF\&, const PointF\&, const PointF\&) เมธอด

เพิ่มเส้นโค้ง Bezier แบบ cubic ที่ระบุลงในพาธที่เป็นตัวแทนของอ็อบเจกต์ปัจจุบัน

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddBezier(const PointF &pt1, const PointF &pt2, const PointF &pt3, const PointF &pt4)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| pt1 | const [PointF](../../../system.drawing/pointf/)\& | จุดเริ่มต้นของเส้นโค้งที่ต้องการเพิ่ม |
| pt2 | const [PointF](../../../system.drawing/pointf/)\& | จุดควบคุมที่ 1 ของเส้นโค้งที่ต้องการเพิ่ม |
| pt3 | const [PointF](../../../system.drawing/pointf/)\& | จุดควบคุมที่ 2 ของเส้นโค้งที่ต้องการเพิ่ม |
| pt4 | const [PointF](../../../system.drawing/pointf/)\& | จุดสิ้นสุดของเส้นโค้งที่ต้องการเพิ่ม |

## GraphicsPath::AddBezier(int, int, int, int, int, int, int, int) เมธอด

เพิ่มเส้นโค้ง Bezier แบบ cubic ที่ระบุลงในพาธที่เป็นตัวแทนของอ็อบเจกต์ปัจจุบัน

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddBezier(int x1, int y1, int x2, int y2, int x3, int y3, int x4, int y4)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| x1 | int | พิกัด X ของจุดเริ่มต้นของเส้นโค้งที่ต้องการเพิ่ม |
| y1 | int | พิกัด Y ของจุดเริ่มต้นของเส้นโค้งที่ต้องการเพิ่ม |
| x2 | int | พิกัด X ของจุดควบคุมที่ 1 ของเส้นโค้งที่ต้องการเพิ่ม |
| y2 | int | พิกัด Y ของจุดควบคุมที่ 1 ของเส้นโค้งที่ต้องการเพิ่ม |
| x3 | int | พิกัด X ของจุดควบคุมที่ 2 ของเส้นโค้งที่ต้องการเพิ่ม |
| y3 | int | พิกัด Y ของจุดควบคุมที่ 2 ของเส้นโค้งที่ต้องการเพิ่ม |
| x4 | int | พิกัด X ของจุดสิ้นสุดของเส้นโค้งที่ต้องการเพิ่ม |
| y4 | int | พิกัด Y ของจุดสิ้นสุดของเส้นโค้งที่ต้องการเพิ่ม |

## GraphicsPath::AddBezier(float, float, float, float, float, float, float, float) เมธอด

เพิ่มเส้นโค้ง Bezier แบบ cubic ที่ระบุลงในพาธที่เป็นตัวแทนของอ็อบเจกต์ปัจจุบัน

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddBezier(float x1, float y1, float x2, float y2, float x3, float y3, float x4, float y4)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| x1 | **float** | พิกัด X ของจุดเริ่มต้นของเส้นโค้งที่ต้องการเพิ่ม |
| y1 | **float** | พิกัด Y ของจุดเริ่มต้นของเส้นโค้งที่ต้องการเพิ่ม |
| x2 | **float** | พิกัด X ของจุดควบคุมที่ 1 ของเส้นโค้งที่ต้องการเพิ่ม |
| y2 | **float** | พิกัด Y ของจุดควบคุมที่ 1 ของเส้นโค้งที่ต้องการเพิ่ม |
| x3 | **float** | พิกัด X ของจุดควบคุมที่ 2 ของเส้นโค้งที่ต้องการเพิ่ม |
| y3 | **float** | พิกัด Y ของจุดควบคุมที่ 2 ของเส้นโค้งที่ต้องการเพิ่ม |
| x4 | **float** | พิกัด X ของจุดสิ้นสุดของเส้นโค้งที่ต้องการเพิ่ม |
| y4 | **float** | พิกัด Y ของจุดสิ้นสุดของเส้นโค้งที่ต้องการเพิ่ม |

## ดูเพิ่มเติม

* คลาส [Point](../../../system.drawing/point/)
* คลาส [GraphicsPath](../)
* คลาส [PointF](../../../system.drawing/pointf/)
* เนมส페ซ [System::Drawing::Drawing2D](../../)
* ไลบรารี [Aspose.Slides](../../../)