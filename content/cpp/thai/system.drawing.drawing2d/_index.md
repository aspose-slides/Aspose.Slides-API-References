---
title: "System::Drawing::Drawing2D"
second_title: อ้างอิง API ของ Aspose.Slides สำหรับ C++
description: 
type: docs
weight: 495
url: /th/system.drawing.drawing2d/
---
## คลาส

| คลาส | คำอธิบาย |
| --- | --- |
| [AdjustableArrowCap](./adjustablearrowcap/) | แสดงถึงหัวเส้นแบบลูกศรที่ปรับได้. วัตถุของคลาสนี้ควรจัดสรรโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/) เท่านั้น. ห้ามสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาด runtime และ/หรือข้อผิดพลาด assertion. ควรห่อหุ้มคลาสนี้ด้วย pointer [System::SmartPtr](../system/smartptr/) เสมอและใช้ pointer นี้ส่งต่อให้ฟังก์ชันเป็นอากิวเมนต์. |
| [Blend](./blend/) | แสดงถึงรูปแบบการผสมสำหรับอ็อบเจ็กต์ [LinearGradientBrush](./lineargradientbrush/). วัตถุของคลาสนี้ควรจัดสรรโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/) เท่านั้น. ห้ามสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาด runtime และ/หรือข้อผิดพลาด assertion. ควรห่อหุ้มคลาสนี้ด้วย pointer [System::SmartPtr](../system/smartptr/) เสมอและใช้ pointer นี้ส่งต่อให้ฟังก์ชันเป็นอากิวเมนต์. |
| [ColorBlend](./colorblend/) | บรรจุอาเรย์ของสีและตำแหน่งที่ใช้สำหรับการทำอินเตอร์พอลเทชันการผสมสีในกราเดียนท์หลายสี. วัตถุของคลาสนี้ควรจัดสรรโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/) เท่านั้น. ห้ามสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาด runtime และ/หรือข้อผิดพลาด assertion. ควรห่อหุ้มคลาสนี้ด้วย pointer [System::SmartPtr](../system/smartptr/) เสมอและใช้ pointer นี้ส่งต่อให้ฟังก์ชันเป็นอากิวเมนต์. |
| [CustomLineCap](./customlinecap/) | แสดงถึงหัวเส้นที่ผู้ใช้กำหนดเอง. วัตถุของคลาสนี้ควรจัดสรรโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/) เท่านั้น. ห้ามสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาด runtime และ/หรือข้อผิดพลาด assertion. ควรห่อหุ้มคลาสนี้ด้วย pointer [System::SmartPtr](../system/smartptr/) เสมอและใช้ pointer นี้ส่งต่อให้ฟังก์ชันเป็นอากิวเมนต์. |
| [GraphicsContainer](./graphicscontainer/) | มีข้อมูลภายในของคอนเทนเนอร์กราฟิก. วัตถุของคลาสนี้ควรจัดสรรโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/) เท่านั้น. ห้ามสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาด runtime และ/หรือข้อผิดพลาด assertion. ควรห่อหุ้มคลาสนี้ด้วย pointer [System::SmartPtr](../system/smartptr/) เสมอและใช้ pointer นี้ส่งต่อให้ฟังก์ชันเป็นอากิวเมนต์. |
| [GraphicsPath](./graphicspath/) | แสดงถึงชุดของเส้นและโค้งที่เชื่อมต่อกัน. วัตถุของคลาสนี้ควรจัดสรรโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/) เท่านั้น. ห้ามสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาด runtime และ/หรือข้อผิดพลาด assertion. ควรห่อหุ้มคลาสนี้ด้วย pointer [System::SmartPtr](../system/smartptr/) เสมอและใช้ pointer นี้ส่งต่อให้ฟังก์ชันเป็นอากิวเมนต์. |
| [GraphicsState](./graphicsstate/) | แสดงถึงสถานะของอ็อบเจ็กต์ [Graphics](../system.drawing/graphics/). วัตถุของคลาสนี้ควรจัดสรรโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/) เท่านั้น. ห้ามสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาด runtime และ/หรือข้อผิดพลาด assertion. ควรห่อหุ้มคลาสนี้ด้วย pointer [System::SmartPtr](../system/smartptr/) เสมอและใช้ pointer นี้ส่งต่อให้ฟังก์ชันเป็นอากิวเมนต์. |
| [HatchBrush](./hatchbrush/) | แสดงถึงแปรงสี่เหลี่ยมที่มีลาย hatch, สีพื้นหน้าและสีพื้นหลัง. วัตถุของคลาสนี้ควรจัดสรรโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/) เท่านั้น. ห้ามสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาด runtime และ/หรือข้อผิดพลาด assertion. ควรห่อหุ้มคลาสนี้ด้วย pointer [System::SmartPtr](../system/smartptr/) เสมอและใช้ pointer นี้ส่งต่อให้ฟังก์ชันเป็นอากิวเมนต์. |
| [LinearGradientBrush](./lineargradientbrush/) | แสดงถึงแปรงสีไลน่าร์เกรเดียนท์. วัตถุของคลาสนี้ควรจัดสรรโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/) เท่านั้น. ห้ามสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาด runtime และ/หรือข้อผิดพลาด assertion. ควรห่อหุ้มคลาสนี้ด้วย pointer [System::SmartPtr](../system/smartptr/) เสมอและใช้ pointer นี้ส่งต่อให้ฟังก์ชันเป็นอากิวเมนต์. |
| [Matrix](./matrix/) | แสดงถึงเมทริกซ์ 3x3 ที่กำหนดการแปลงรูป. วัตถุของคลาสนี้ควรจัดสรรโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/) เท่านั้น. ห้ามสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาด runtime และ/หรือข้อผิดพลาด assertion. ควรห่อหุ้มคลาสนี้ด้วย pointer [System::SmartPtr](../system/smartptr/) เสมอและใช้ pointer นี้ส่งต่อให้ฟังก์ชันเป็นอากิวเมนต์. |
| [PathData](./pathdata/) | บรรจุข้อมูลกราฟิกที่แสดงถึงเส้นทาง. วัตถุของคลาสนี้ควรจัดสรรโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/) เท่านั้น. ห้ามสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาด runtime และ/หรือข้อผิดพลาด assertion. ควรห่อหุ้มคลาสนี้ด้วย pointer [System::SmartPtr](../system/smartptr/) เสมอและใช้ pointer นี้ส่งต่อให้ฟังก์ชันเป็นอากิวเมนต์. |
| [PathGradientBrush](./pathgradientbrush/) | แสดงถึงแปรงที่เติมเต็มภายในอ็อบเจ็กต์ [GraphicsPath](./graphicspath/) ด้วยกราเดียนท์. วัตถุของคลาสนี้ควรจัดสรรโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/) เท่านั้น. ห้ามสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาด runtime และ/หรือข้อผิดพลาด assertion. ควรห่อหุ้มคลาสนี้ด้วย pointer [System::SmartPtr](../system/smartptr/) เสมอและใช้ pointer นี้ส่งต่อให้ฟังก์ชันเป็นอากิวเมนต์. |
| [RegionData](./regiondata/) | มีข้อมูลที่กำหนดเขตพื้นที่. วัตถุของคลาสนี้ควรจัดสรรโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/) เท่านั้น. ห้ามสร้างอินสแตนซ์ของประเภทนี้บนสแตกหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาด runtime และ/หรือข้อผิดพลาด assertion. ควรห่อหุ้มคลาสนี้ด้วย pointer [System::SmartPtr](../system/smartptr/) เสมอและใช้ pointer นี้ส่งต่อให้ฟังก์ชันเป็นอากิวเมนต์. |

## Enum

| Enum | คำอธิบาย |
| --- | --- |
| [CombineMode](./combinemode/) | ระบุวิธีการรวมพื้นที่คลิป. |
| [CompositingMode](./compositingmode/) | ระบุวิธีการรวมสีต้นฉบับและสีพื้นหลัง. |
| [CompositingQuality](./compositingquality/) | ระดับคุณภาพที่ใช้ในกระบวนการคอมโพสิต. |
| [CoordinateSpace](./coordinatespace/) | ระบุวิธีการประเมินค่าพิกัด. |
| [DashCap](./dashcap/) | ระบุประเภทของหัวที่ใช้ที่ทั้งสองด้านของเส้นขีดในเส้นประ. |
| [DashStyle](./dashstyle/) | ระบุสไตล์ของเส้นประ. |
| [FillMode](./fillmode/) | ระบุวิธีการเติมเต็มภายในเส้นทางที่ปิด. |
| [FlushIntention](./flushintention/) | ระบุว่าคำสั่งในสแตกกราฟิกต้องหยุดทำงานทันทีหรือทำงานเร็วที่สุดที่เป็นไปได้. |
| [HatchStyle](./hatchstyle/) | ระบุรูปแบบของแปรง [HatchBrush](./hatchbrush/). |
| [InterpolationMode](./interpolationmode/) | ระบุอัลกอริทึมที่จะใช้เมื่อต้องหมุนหรือสเกลภาพ. |
| [LineCap](./linecap/) | แสดงสไตล์หัวที่ [Pen](../system.drawing/pen/) สามารถใช้จบเส้นได้. |
| [LineJoin](./linejoin/) | ระบุวิธีการเชื่อมต่อเส้นหรือโค้งต่อเนื่อง. |
| [LinearGradientMode](./lineargradientmode/) | แสดงทิศทางของกราเดียนท์เชิงเส้น. |
| [MatrixOrder](./matrixorder/) | ระบุลำดับของการแปลงเมทริกซ์. |
| [PathPointType](./pathpointtype/) | ระบุประเภทของจุดในเส้นทาง. |
| [PenAlignment](./penalignment/) | ระบุการจัดตำแหน่งของอ็อบเจ็กต์ [Pen](../system.drawing/pen/) relative to เส้นอ้างอิงความกว้าง 0. |
| [PenType](./pentype/) | ระบุประเภทของการเติมของอ็อบเจ็กต์ [Pen](../system.drawing/pen/). |
| [PixelFormat](./pixelformat/) | ระบุรูปแบบข้อมูลสีของพิกเซล. |
| [PixelOffsetMode](./pixeloffsetmode/) | ระบุวิธีการชดเชยพิกเซลเมื่อทำการเรนเดอร์. |
| [QualityMode](./qualitymode/) | ระบุคุณภาพการเรนเดอร์โดยรวม. |
| [SmoothingMode](./smoothingmode/) | ระบุประเภทของการทำ antialiasing ที่ใช้กับเส้น, โค้งและขอบของพื้นที่ที่เติม. |
| [WrapMode](./wrapmode/) | ระบุวิธีการทำ tiled ของเทกเจอร์หรือกราเดียนท์เมื่อมันเล็กกว่าพื้นที่ที่ต้องเติม. |

## Typedef

| Typedef | คำอธิบาย |
| --- | --- |
| [MatrixPtr](./matrixptr/) | นามแฝงสำหรับ shared pointer ไปยังอินสแตนซ์ของคลาส [Matrix](./matrix/). |