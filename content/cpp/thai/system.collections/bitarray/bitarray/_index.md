---
title: BitArray()
second_title: Aspose.Slides สำหรับ C++ API Reference
description: คอนสตรัคเตอร์คัดลอก.
type: docs
weight: 14
url: /th/system.collections/bitarray/bitarray/
---
## BitArray::BitArray(const bitset\&) คอนสตรัคเตอร์

คอนสตรัคเตอร์คัดลอก.

```cpp
System::Collections::BitArray::BitArray(const bitset &bits)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| bits | const [bitset](../bitset/)\& | คอนเทนเนอร์สำหรับคัดลอกข้อมูลจาก. |

## BitArray::BitArray(const BitArray\&) คอนสตรัคเตอร์

คอนสตรัคเตอร์คัดลอก.

```cpp
System::Collections::BitArray::BitArray(const BitArray &bits)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| bits | const [BitArray](../)\& | คอนเทนเนอร์สำหรับคัดลอกข้อมูลจาก. |

## BitArray::BitArray(const BitArrayPtr\&) คอนสตรัคเตอร์

คอนสตรัคเตอร์คัดลอก.

```cpp
System::Collections::BitArray::BitArray(const BitArrayPtr &array)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| array | const [BitArrayPtr](../../bitarrayptr/)\& | [BitArray](../) สำหรับคัดลอกข้อมูลจาก. |

## BitArray::BitArray(const System::ArrayPtr\<bool\>\&) คอนสตรัคเตอร์

คอนสตรัคเตอร์คัดลอก.

```cpp
System::Collections::BitArray::BitArray(const System::ArrayPtr<bool> &values)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| values | const [System::ArrayPtr](../../../system/arrayptr/)\<**bool**\>\& | คอนเทนเนอร์สำหรับคัดลอกข้อมูลแบบองค์ประกอบจาก. |

## BitArray::BitArray(const System::ArrayPtr\<uint8_t\>\&) คอนสตรัคเตอร์

คอนสตรัคเตอร์คัดลอก.

```cpp
System::Collections::BitArray::BitArray(const System::ArrayPtr<uint8_t> &bytes)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| bytes | const [System::ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\>\& | คอนเทนเนอร์สำหรับคัดลอกข้อมูลแบบบิตจาก. |

## BitArray::BitArray(const System::ArrayPtr\<int\>\&) คอนสตรัคเตอร์

คอนสตรัคเตอร์คัดลอก.

```cpp
System::Collections::BitArray::BitArray(const System::ArrayPtr<int> &values)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| values | const [System::ArrayPtr](../../../system/arrayptr/)\<int\>\& | คอนเทนเนอร์สำหรับคัดลอกข้อมูลแบบบิตจาก. |

## BitArray::BitArray(int, bool) คอนสตรัคเตอร์

คอนสตรัคเตอร์เติม.

```cpp
System::Collections::BitArray::BitArray(int length, bool defaultValue=false)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| length | int | จำนวนองค์ประกอบที่จะเติม. |
| defaultValue | **bool** | ค่าที่ใช้เติม. |

## ดูเพิ่มเติม

* Typedef [bitset](../bitset/)
* Typedef [ArrayPtr](../../../system/arrayptr/)
* คลาส [BitArray](../)
* คลาส [BitArrayPtr](../../bitarrayptr/)
* เนมสเปซ [System::Collections](../../)
* Library [Aspose.Slides](../../../)