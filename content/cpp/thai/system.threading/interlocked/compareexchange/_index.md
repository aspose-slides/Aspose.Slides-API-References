---
title: CompareExchange()
second_title: Aspose.Slides สำหรับการอ้างอิง API ของ C++
description: "ทำการเปลี่ยนค่าในตัวแปรแบบเปรียบเทียบ-แลกเปลี่ยน: ตรวจสอบว่าตัวแปรเท่ากับค่าที่กำหนดหรือไม่และเก็บค่าที่ใหม่เฉพาะเมื่อค่าที่เก็บตรงกับค่าที่คาดหวัง."
type: docs
weight: 79
url: /th/system.threading/interlocked/compareexchange/
---
## Interlocked::CompareExchange(T\&, T, T) method

ทำการเปลี่ยนค่าในตัวแปรแบบเปรียบเทียบ-แลกเปลี่ยน: ตรวจสอบว่าตัวแปรเท่ากับค่าที่กำหนดหรือไม่และเก็บค่าที่ใหม่เฉพาะเมื่อค่าที่เก็บตรงกับค่าที่คาดหวัง

```cpp
template<typename T> static std::enable_if<IsSupportedInt<T>, T>::type System::Threading::Interlocked::CompareExchange(T &location1, T value, T comparand)
```

### พารามิเตอร์เทมเพลต

| พารามิเตอร์ | คำอธิบาย |
| --- | --- |
| T | ชนิดของตัวแปร |

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| location1 | T\& | การอ้างอิงตัวแปรเพื่อเปลี่ยนแปลง |
| value | T | ค่าที่จะเก็บ |
| comparand | T | ค่าที่ใช้เปรียบเทียบค่าตัวแปรก่อนทำการแลกเปลี่ยน |

### ค่าที่ส่งคืน

ค่าของตัวแปรเมื่อเริ่มการดำเนินการโดยไม่คำนึงว่ามันถูกเปลี่ยนหรือไม่

## Interlocked::CompareExchange(T\&, T, T) method

ทำการเปลี่ยนค่าในตัวแปรแบบเปรียบเทียบ-แลกเปลี่ยน: ตรวจสอบว่าตัวแปรเท่ากับค่าที่กำหนดหรือไม่และเก็บค่าที่ใหม่เฉพาะเมื่อค่าที่เก็บตรงกับค่าที่คาดหวัง. ยังไม่ได้ดำเนินการ

```cpp
template<typename T> static std::enable_if<!IsSupportedInt<T>, T>::type System::Threading::Interlocked::CompareExchange(T &location1, T value, T comparand)
```

### พารามิเตอร์เทมเพลต

| พารามิเตอร์ | คำอธิบาย |
| --- | --- |
| T | ชนิดของตัวแปร |

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| location1 | T\& | การอ้างอิงตัวแปรเพื่อเปลี่ยนแปลง |
| value | T | ค่าที่จะเก็บ |
| comparand | T | ค่าที่ใช้เปรียบเทียบค่าตัวแปรก่อนทำการแลกเปลี่ยน |

### ค่าที่ส่งคืน

ค่าของตัวแปรเมื่อเริ่มการดำเนินการโดยไม่คำนึงว่ามันถูกเปลี่ยนหรือไม่

## Interlocked::CompareExchange(int32_t\&, int32_t, int32_t, bool\&) method

ทำการเปลี่ยนค่าในตัวแปรแบบเปรียบเทียบ-แลกเปลี่ยน: ตรวจสอบว่าตัวแปรเท่ากับค่าที่กำหนดหรือไม่และเก็บค่าที่ใหม่เฉพาะเมื่อค่าที่เก็บตรงกับค่าที่คาดหวัง

```cpp
static int32_t System::Threading::Interlocked::CompareExchange(int32_t &location1, int32_t value, int32_t comparand, bool &succeeded)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| location1 | **int32_t**\& | การอ้างอิงตัวแปรเพื่อเปลี่ยนแปลง |
| value | **int32_t** | ค่าที่จะเก็บ |
| comparand | **int32_t** | ค่าที่ใช้เปรียบเทียบค่าตัวแปรก่อนทำการแลกเปลี่ยน |
| succeeded | **bool**\& | การอ้างอิงถึงตัวแปรที่ตั้งค่าเป็น true หากการแลกเปลี่ยนเกิดขึ้นและเป็น false หากไม่เกิดขึ้น |

### ค่าที่ส่งคืน

ค่าของตัวแปรเมื่อเริ่มการดำเนินการโดยไม่คำนึงว่ามันถูกเปลี่ยนหรือไม่

## ดูเพิ่มเติม

* คลาส [Interlocked](../)
* เนมสเปซ [System::Threading](../../)
* ไลบรารี [Aspose.Slides](../../../)