---
title: Combine()
second_title: Aspose.Slides สำหรับ C++ เอกสารอ้างอิง API
description: รวมส่วนของเส้นทางที่ระบุให้เป็นเส้นทางเดียวโดยแทรกอักขระคั่นไดเรกทอรีระหว่างส่วนต่าง ๆ หากจำเป็น
type: docs
weight: 14
url: /th/system.io/path/combine/
---
## Path::Combine(const ArrayPtr\<String\>\&) เมธอด


รวมส่วนของเส้นทางที่ระบุให้เป็นเส้นทางเดียวโดยแทรกอักขระคั่นไดเรกทอรีระหว่างส่วนต่าง ๆ หากจำเป็น

```cpp
static String System::IO::Path::Combine(const ArrayPtr<String> &paths)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| paths | const [ArrayPtr](../../../system/arrayptr/)\<[String](../../../system/string/)\>\& | อาร์เรย์ที่บรรจุส่วนของเส้นทางที่จะถูกรวม |

### ค่าที่คืน

เส้นทางที่รวมแล้ว

## Path::Combine(const String\&, const String\&) เมธอด


รวมส่วนของเส้นทางสองส่วนที่ระบุให้เป็นเส้นทางเดียวโดยแทรกอักขระคั่นไดเรกทอรีระหว่างส่วนต่าง ๆ หากจำเป็น

```cpp
static String System::IO::Path::Combine(const String &path1, const String &path2)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| path1 | const [String](../../../system/string/)\& | ส่วนของเส้นทางแรก |
| path2 | const [String](../../../system/string/)\& | ส่วนของเส้นทางที่สอง |

### ค่าที่คืน

เส้นทางที่รวมแล้ว

## Path::Combine(const String\&, const String\&, const String\&) เมธอด


รวมส่วนของเส้นทางสามส่วนที่ระบุให้เป็นเส้นทางเดียวโดยแทรกอักขระคั่นไดเรกทอรีระหว่างส่วนต่าง ๆ หากจำเป็น

```cpp
static String System::IO::Path::Combine(const String &path1, const String &path2, const String &path3)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| path1 | const [String](../../../system/string/)\& | ส่วนของเส้นทางแรก |
| path2 | const [String](../../../system/string/)\& | ส่วนของเส้นทางที่สอง |
| path3 | const [String](../../../system/string/)\& | ส่วนของเส้นทางที่สาม |

### ค่าที่คืน

เส้นทางที่รวมแล้ว

## Path::Combine(const String\&, const String\&, const String\&, const String\&) เมธอด


รวมส่วนของเส้นทางสี่ส่วนที่ระบุให้เป็นเส้นทางเดียวโดยแทรกอักขระคั่นไดเรกทอรีระหว่างส่วนต่าง ๆ หากจำเป็น

```cpp
static String System::IO::Path::Combine(const String &path1, const String &path2, const String &path3, const String &path4)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| path1 | const [String](../../../system/string/)\& | ส่วนของเส้นทางแรก |
| path2 | const [String](../../../system/string/)\& | ส่วนของเส้นทางที่สอง |
| path3 | const [String](../../../system/string/)\& | ส่วนของเส้นทางที่สาม |
| path4 | const [String](../../../system/string/)\& | ส่วนของเส้นทางที่สี่ |

### ค่าที่คืน

เส้นทางที่รวมแล้ว

## ดูเพิ่มเติม

* Typedef [ArrayPtr](../../../system/arrayptr/)
* Class [String](../../../system/string/)
* Class [Path](../)
* Namespace [System::IO](../../)
* Library [Aspose.Slides](../../../)