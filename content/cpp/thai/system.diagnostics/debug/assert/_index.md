---
title: Assert()
second_title: Aspose.Slides สำหรับ C++ API อ้างอิง
description: ตรวจสอบเงื่อนไขและส่งข้อมูลเมื่อการตรวจสอบล้มเหลว.
type: docs
weight: 14
url: /th/system.diagnostics/debug/assert/
---
## Debug::Assert(bool) เมธอด


ตรวจสอบเงื่อนไขและส่งข้อมูลเมื่อการตรวจสอบล้มเหลว.

```cpp
static void System::Diagnostics::Debug::Assert(bool condition)
```


### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| condition | **bool** | ค่าของเงื่อนไข. |

## Debug::Assert(bool, const String\&) เมธอด


ตรวจสอบเงื่อนไขและส่งข้อมูลเมื่อการตรวจสอบล้มเหลว.

```cpp
static void System::Diagnostics::Debug::Assert(bool condition, const String &message)
```


### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| condition | **bool** | ค่าของเงื่อนไข. |
| message | const [String](../../../system/string/)\& | ข้อความที่จะเติมเมื่อการตรวจสอบล้มเหลว. |

## Debug::Assert(bool, const char *) เมธอด


ตรวจสอบเงื่อนไขและส่งข้อมูลเมื่อการตรวจสอบล้มเหลว.

```cpp
static void System::Diagnostics::Debug::Assert(bool condition, const char *message)
```


### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| condition | **bool** | ค่าของเงื่อนไข. |
| message | const char * | ข้อความที่จะเติมเมื่อการตรวจสอบล้มเหลว. |

## Debug::Assert(bool, const String\&, const String\&) เมธอด


ตรวจสอบเงื่อนไขและส่งข้อมูลเมื่อการตรวจสอบล้มเหลว.

```cpp
static void System::Diagnostics::Debug::Assert(bool condition, const String &message, const String &detailMessage)
```


### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| condition | **bool** | ค่าของเงื่อนไข. |
| message | const [String](../../../system/string/)\& | ข้อความที่จะเติมเมื่อการตรวจสอบล้มเหลว. |
| detailMessage | const [String](../../../system/string/)\& | ข้อความโดยละเอียดที่จะเติมเมื่อการตรวจสอบล้มเหลว. |

## ดูเพิ่มเติม

* คลาส [String](../../../system/string/)
* โครงสร้าง [Debug](../)
* เนมสเปซ [System::Diagnostics](../../)
* ไลบรารี [Aspose.Slides](../../../)