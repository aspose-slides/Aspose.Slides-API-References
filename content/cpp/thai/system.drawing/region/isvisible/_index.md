---
title: IsVisible()
second_title: Aspose.Slides สำหรับ C++ เอกสารอ้างอิง API
description: ตรวจสอบว่าจุดที่ระบุอยู่ภายในเขตที่แสดงโดยอ็อบเจ็กต์ปัจจุบันหรือไม่
type: docs
weight: 196
url: /th/system.drawing/region/isvisible/
---
## Region::IsVisible(const Point\&) const เมธอด


ตรวจสอบว่าจุดที่ระบุอยู่ภายในเขตที่แสดงโดยอ็อบเจ็กต์ปัจจุบันหรือไม่

```cpp
bool System::Drawing::Region::IsVisible(const Point &point) const
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| point | const [Point](../../point/)\& | จุดที่จะตรวจสอบ |

## Region::IsVisible(const PointF\&) const เมธอด


ตรวจสอบว่าจุดที่ระบุอยู่ภายในเขตที่แสดงโดยอ็อบเจ็กต์ปัจจุบันหรือไม่

```cpp
bool System::Drawing::Region::IsVisible(const PointF &point) const
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| point | const [PointF](../../pointf/)\& | จุดที่จะตรวจสอบ |

## Region::IsVisible(const Rectangle\&) เมธอด


ตรวจสอบว่าบางส่วนของสี่เหลี่ยมที่ระบุอยู่ภายในเขตที่แสดงโดยอ็อบเจ็กต์ปัจจุบันหรือไม่

```cpp
bool System::Drawing::Region::IsVisible(const Rectangle &rect)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| rect | const [Rectangle](../../rectangle/)\& | สี่เหลี่ยมที่จะตรวจสอบ |

## Region::IsVisible(const RectangleF\&) เมธอด


ตรวจสอบว่าบางส่วนของสี่เหลี่ยมที่ระบุอยู่ภายในเขตที่แสดงโดยอ็อบเจ็กต์ปัจจุบันหรือไม่

```cpp
bool System::Drawing::Region::IsVisible(const RectangleF &rect)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| rect | const [RectangleF](../../rectanglef/)\& | สี่เหลี่ยมที่จะตรวจสอบ |

## Region::IsVisible(const Point\&, const SharedPtr\<Graphics\>\&) const เมธอด


ตรวจสอบว่าจุดที่ระบุอยู่ภายในเขตที่แสดงโดยอ็อบเจ็กต์ปัจจุบันโดยใช้กราฟิกที่ระบุ

```cpp
bool System::Drawing::Region::IsVisible(const Point &point, const SharedPtr<Graphics> &graphics) const
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| point | const [Point](../../point/)\& | จุดที่จะตรวจสอบ |
| graphics | const [SharedPtr](../../../system/sharedptr/)\<[Graphics](../../graphics/)\>\& | บริบทกราฟิก |

## Region::IsVisible(const PointF\&, const SharedPtr\<Graphics\>\&) const เมธอด


ตรวจสอบว่าจุดที่ระบุอยู่ภายในเขตที่แสดงโดยอ็อบเจ็กต์ปัจจุบันโดยใช้กราฟิกที่ระบุ

```cpp
bool System::Drawing::Region::IsVisible(const PointF &point, const SharedPtr<Graphics> &graphics) const
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| point | const [PointF](../../pointf/)\& | จุดที่จะตรวจสอบ |
| graphics | const [SharedPtr](../../../system/sharedptr/)\<[Graphics](../../graphics/)\>\& | บริบทกราฟิก |

## Region::IsVisible(const Rectangle\&, const SharedPtr\<Graphics\>\&) เมธอด


ตรวจสอบว่าบางส่วนของสี่เหลี่ยมที่ระบุอยู่ภายในเขตที่แสดงโดยอ็อบเจ็กต์ปัจจุบันโดยใช้กราฟิกที่ระบุ

```cpp
bool System::Drawing::Region::IsVisible(const Rectangle &rect, const SharedPtr<Graphics> &graphics)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| rect | const [Rectangle](../../rectangle/)\& | สี่เหลี่ยมที่จะตรวจสอบ |
| graphics | const [SharedPtr](../../../system/sharedptr/)\<[Graphics](../../graphics/)\>\& | บริบทกราฟิก |

## Region::IsVisible(const RectangleF\&, const SharedPtr\<Graphics\>\&) เมธอด


ตรวจสอบว่าบางส่วนของสี่เหลี่ยมที่ระบุอยู่ภายในเขตที่แสดงโดยอ็อบเจ็กต์ปัจจุบันโดยใช้กราฟิกที่ระบุ

```cpp
bool System::Drawing::Region::IsVisible(const RectangleF &rect, const SharedPtr<Graphics> &graphics)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| rect | const [RectangleF](../../rectanglef/)\& | สี่เหลี่ยมที่จะตรวจสอบ |
| graphics | const [SharedPtr](../../../system/sharedptr/)\<[Graphics](../../graphics/)\>\& | บริบทกราฟิก |

## Region::IsVisible(float, float) const เมธอด


ตรวจสอบว่าจุดที่ระบุอยู่ภายในเขตที่แสดงโดยอ็อบเจ็กต์ปัจจุบันหรือไม่

```cpp
bool System::Drawing::Region::IsVisible(float x, float y) const
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| x | **float** | พิกัด X ของจุดที่จะตรวจสอบ |
| y | **float** | พิกัด Y ของจุดที่จะตรวจสอบ |

## Region::IsVisible(float, float, const SharedPtr\<Graphics\>\&) const เมธอด


ตรวจสอบว่าจุดที่ระบุอยู่ภายในเขตที่แสดงโดยอ็อบเจ็กต์ปัจจุบันโดยใช้กราฟิกที่ระบุ

```cpp
bool System::Drawing::Region::IsVisible(float x, float y, const SharedPtr<Graphics> &graphics) const
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| x | **float** | พิกัด X ของจุดที่จะตรวจสอบ |
| y | **float** | พิกัด Y ของจุดที่จะตรวจสอบ |
| graphics | const [SharedPtr](../../../system/sharedptr/)\<[Graphics](../../graphics/)\>\& | บริบทกราฟิก |

## ดูเพิ่มเติม

* Typedef [SharedPtr](../../../system/sharedptr/)
* คลาส [Point](../../point/)
* คลาส [Region](../)
* คลาส [PointF](../../pointf/)
* คลาส [Rectangle](../../rectangle/)
* คลาส [RectangleF](../../rectanglef/)
* คลาส [Graphics](../../graphics/)
* เนมสเปซ [System::Drawing](../../)
* ไลบรารี [Aspose.Slides](../../../)