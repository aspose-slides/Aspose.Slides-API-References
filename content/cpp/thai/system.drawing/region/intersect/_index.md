---
title: Intersect()
second_title: Aspose.Slides สำหรับ C++ เอกสารอ้างอิง API
description: แทนที่ภูมิภาคที่แสดงโดยอ็อบเจ็กต์ปัจจุบันด้วยผลของการตัดกันระหว่างภูมิภาคนี้และภูมิภาคที่กำหนดโดยสี่เหลี่ยมที่ระบุ
type: docs
weight: 79
url: /th/system.drawing/region/intersect/
---
## Region::Intersect(const RectangleF\&) เมธอด

แทนที่ภูมิภาคที่แสดงโดยอ็อบเจ็กต์ปัจจุบันด้วยผลของการตัดกันระหว่างภูมิภาคนี้และภูมิภาคที่กำหนดโดยสี่เหลี่ยมที่ระบุ

```cpp
void System::Drawing::Region::Intersect(const RectangleF &rect)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| rect | const [RectangleF](../../rectanglef/)\& | สี่เหลี่ยมที่กำหนดภูมิภาคเพื่อทำการตัดกับภูมิภาคนี้ |

## Region::Intersect(const Rectangle\&) เมธอด

แทนที่ภูมิภาคที่แสดงโดยอ็อบเจ็กต์ปัจจุบันด้วยผลของการตัดกันระหว่างภูมิภาคนี้และภูมิภาคที่กำหนดโดยสี่เหลี่ยมที่ระบุ

```cpp
void System::Drawing::Region::Intersect(const Rectangle &rect)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| rect | const [Rectangle](../../rectangle/)\& | สี่เหลี่ยมที่กำหนดภูมิภาคเพื่อทำการตัดกับภูมิภาคนี้ |

## Region::Intersect(const SharedPtr\<Drawing2D::GraphicsPath\>\&) เมธอด

แทนที่ภูมิภาคที่แสดงโดยอ็อบเจ็กต์ปัจจุบันด้วยผลของการตัดกันระหว่างภูมิภาคนี้และภูมิภาคที่กำหนดโดยพาธที่ระบุ

```cpp
void System::Drawing::Region::Intersect(const SharedPtr<Drawing2D::GraphicsPath> &path)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| path | const [SharedPtr](../../../system/sharedptr/)\<[Drawing2D::GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)\>\& | พาธที่กำหนดภูมิภาคเพื่อทำการตัดกับภูมิภาคนี้ |

## Region::Intersect(const SharedPtr\<Region\>\&) เมธอด

แทนที่ภูมิภาคที่แสดงโดยอ็อบเจ็กต์ปัจจุบันด้วยผลของการตัดกันระหว่างภูมิภาคนี้และภูมิภาคที่ระบุ

```cpp
void System::Drawing::Region::Intersect(const SharedPtr<Region> &region)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| region | const [SharedPtr](../../../system/sharedptr/)\<[Region](../)\>\& | ภูมิภาคที่ทำการตัดกับภูมิภาคนี้ |

## ดูเพิ่มเติม

* Typedef [SharedPtr](../../../system/sharedptr/)
* คลาส [RectangleF](../../rectanglef/)
* คลาส [Region](../)
* คลาส [Rectangle](../../rectangle/)
* คลาส [GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)
* เนมสเปซ [System::Drawing](../../)
* Library [Aspose.Slides](../../../)