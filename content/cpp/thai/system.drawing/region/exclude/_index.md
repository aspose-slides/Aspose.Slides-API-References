---
title: Exclude()
second_title: Aspose.Slides สำหรับ C++ API Reference
description: แทนที่พื้นที่ที่แสดงโดยอ็อบเจ็กต์ปัจจุบันด้วยผลลัพธ์ของการยกเว้นพื้นที่ที่กำหนดโดยสี่เหลี่ยมที่ระบุจากมัน
type: docs
weight: 92
url: /th/system.drawing/region/exclude/
---
## Region::Exclude(const RectangleF\&) เมธอด

แทนที่พื้นที่ที่แสดงโดยอ็อบเจ็กต์ปัจจุบันด้วยผลลัพธ์ของการยกเว้นพื้นที่ที่กำหนดโดยสี่เหลี่ยมที่ระบุจากมัน

```cpp
void System::Drawing::Region::Exclude(const RectangleF &rect)
```

### Arguments

| Parameter | Type | Description |
| --- | --- | --- |
| rect | const [RectangleF](../../rectanglef/)\& | สี่เหลี่ยมที่กำหนดพื้นที่ที่จะยกเว้น |

## Region::Exclude(const Rectangle\&) เมธอด

แทนที่พื้นที่ที่แสดงโดยอ็อบเจ็กต์ปัจจุบันด้วยผลลัพธ์ของการยกเว้นพื้นที่ที่กำหนดโดยสี่เหลี่ยมที่ระบุจากมัน

```cpp
void System::Drawing::Region::Exclude(const Rectangle &rect)
```

### Arguments

| Parameter | Type | Description |
| --- | --- | --- |
| rect | const [Rectangle](../../rectangle/)\& | สี่เหลี่ยมที่กำหนดพื้นที่ที่จะยกเว้น |

## Region::Exclude(const SharedPtr\<Drawing2D::GraphicsPath\>\&) เมธอด

แทนที่พื้นที่ที่แสดงโดยอ็อบเจ็กต์ปัจจุบันด้วยผลลัพธ์ของการยกเว้นพื้นที่ที่กำหนดโดยเส้นทางที่ระบุจากมัน

```cpp
void System::Drawing::Region::Exclude(const SharedPtr<Drawing2D::GraphicsPath> &path)
```

### Arguments

| Parameter | Type | Description |
| --- | --- | --- |
| path | const [SharedPtr](../../../system/sharedptr/)\<[Drawing2D::GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)\>\& | เส้นทางที่กำหนดพื้นที่ที่จะยกเว้น |

## Region::Exclude(const SharedPtr\<Region\>\&) เมธอด

แทนที่พื้นที่ที่แสดงโดยอ็อบเจ็กต์ปัจจุบันด้วยผลลัพธ์ของการยกเว้นพื้นที่ที่ระบุจากมัน

```cpp
void System::Drawing::Region::Exclude(const SharedPtr<Region> &region)
```

### Arguments

| Parameter | Type | Description |
| --- | --- | --- |
| region | const [SharedPtr](../../../system/sharedptr/)\<[Region](../)\>\& | พื้นที่ที่จะยกเว้น |

## ดูเพิ่มเติม

* Typedef [SharedPtr](../../../system/sharedptr/)
* Class [RectangleF](../../rectanglef/)
* Class [Region](../)
* Class [Rectangle](../../rectangle/)
* Class [GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)
* Namespace [System::Drawing](../../)
* Library [Aspose.Slides](../../../)