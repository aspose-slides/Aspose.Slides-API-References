---
title: Union()
second_title: Aspose.Slides สำหรับ C++ การอ้างอิง API
description: แทนที่พื้นที่ที่แสดงโดยอ็อบเจ็กต์ปัจจุบันด้วยผลลัพธ์ของการทำ union ของพื้นที่นี้และพื้นที่ที่กำหนดโดยสี่เหลี่ยมที่ระบุ
type: docs
weight: 53
url: /th/system.drawing/region/union/
---
## Region::Union(const RectangleF\&) เมธอด

แทนที่พื้นที่ที่แสดงโดยอ็อบเจ็กต์ปัจจุบันด้วยผลลัพธ์ของการทำ union ของพื้นที่นี้และพื้นที่ที่กำหนดโดยสี่เหลี่ยมที่ระบุ

```cpp
void System::Drawing::Region::Union(const RectangleF &rect)
```

### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| rect | const [RectangleF](../../rectanglef/)\& | สี่เหลี่ยมที่กำหนดพื้นที่เพื่อรวมกับพื้นที่นี้ |

## Region::Union(const Rectangle\&) เมธอด

แทนที่พื้นที่ที่แสดงโดยอ็อบเจ็กต์ปัจจุบันด้วยผลลัพธ์ของการทำ union ของพื้นที่นี้และพื้นที่ที่กำหนดโดยสี่เหลี่ยมที่ระบุ

```cpp
void System::Drawing::Region::Union(const Rectangle &rect)
```

### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| rect | const [Rectangle](../../rectangle/)\& | สี่เหลี่ยมที่กำหนดพื้นที่เพื่อรวมกับพื้นที่นี้ |

## Region::Union(const SharedPtr\<Drawing2D::GraphicsPath\>\&) เมธอด

แทนที่พื้นที่ที่แสดงโดยอ็อบเจ็กต์ปัจจุบันด้วยผลลัพธ์ของการทำ union ของพื้นที่นี้และพื้นที่ที่กำหนดโดยเส้นทางที่ระบุ

```cpp
void System::Drawing::Region::Union(const SharedPtr<Drawing2D::GraphicsPath> &path)
```

### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| path | const [SharedPtr](../../../system/sharedptr/)\<[Drawing2D::GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)\>\& | เส้นทางที่กำหนดพื้นที่เพื่อรวมกับพื้นที่นี้ |

## Region::Union(const SharedPtr\<Region\>\&) เมธอด

แทนที่พื้นที่ที่แสดงโดยอ็อบเจ็กต์ปัจจุบันด้วยผลลัพธ์ของการทำ union ของพื้นที่นี้และและพื้นที่ที่ระบุ

```cpp
void System::Drawing::Region::Union(const SharedPtr<Region> &region)
```

### อาร์กิวเมนต์

| Parameter | Type | Description |
| --- | --- | --- |
| region | const [SharedPtr](../../../system/sharedptr/)\<[Region](../)\>\& | พื้นที่เพื่อรวมกับพื้นที่นี้ |

## ดูเพิ่มเติม

* Typedef [SharedPtr](../../../system/sharedptr/)
* คลาส [RectangleF](../../rectanglef/)
* คลาส [Region](../)
* คลาส [Rectangle](../../rectangle/)
* คลาส [GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)
* เนมสเปซ [System::Drawing](../../)
* Library [Aspose.Slides](../../../)