---
title: Xor()
second_title: Aspose.Slides สำหรับ C++ เอกสารอ้างอิง API
description: แทนที่ภูมิภาคที่แสดงโดยอ็อบเจ็กต์ปัจจุบันด้วยส่วนของภูมิภาคนี้และภูมิภาคที่กำหนดโดยสี่เหลี่ยมที่ระบุซึ่งไม่ตัดกัน.
type: docs
weight: 144
url: /th/system.drawing/region/xor/
---
## Region::Xor(const RectangleF\&) เมธอด

แทนที่ภูมิภาคที่แสดงโดยอ็อบเจ็กต์ปัจจุบันด้วยส่วนของภูมิภาคนี้และภูมิภาคที่กำหนดโดยสี่เหลี่ยมที่ระบุซึ่งไม่ตัดกัน.

```cpp
void System::Drawing::Region::Xor(const RectangleF &rect)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| rect | const [RectangleF](../../rectanglef/)\& | สี่เหลี่ยมที่กำหนดภูมิภาคที่จะทำการ xor กับภูมิภาคที่แสดงโดยอ็อบเจ็กต์ปัจจุบัน |

## Region::Xor(const Rectangle\&) เมธอด

แทนที่ภูมิภาคที่แสดงโดยอ็อบเจ็กต์ปัจจุบันด้วยส่วนของภูมิภาคนี้และภูมิภาคที่กำหนดโดยสี่เหลี่ยมที่ระบุซึ่งไม่ตัดกัน.

```cpp
void System::Drawing::Region::Xor(const Rectangle &rect)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| rect | const [Rectangle](../../rectangle/)\& | สี่เหลี่ยมที่กำหนดภูมิภาคที่จะทำการ xor กับภูมิภาคที่แสดงโดยอ็อบเจ็กต์ปัจจุบัน |

## Region::Xor(const SharedPtr\<Drawing2D::GraphicsPath\>\&) เมธอด

แทนที่ภูมิภาคที่แสดงโดยอ็อบเจ็กต์ปัจจุบันด้วยส่วนของภูมิภาคนี้และเส้นทางที่กำหนดโดยพาธที่ระบุซึ่งไม่ตัดกัน.

```cpp
void System::Drawing::Region::Xor(const SharedPtr<Drawing2D::GraphicsPath> &path)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| path | const [SharedPtr](../../../system/sharedptr/)\<[Drawing2D::GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)\>\& | พาธที่กำหนดภูมิภาคที่จะทำการ xor กับภูมิภาคที่แสดงโดยอ็อบเจ็กต์ปัจจุบัน |

## Region::Xor(const SharedPtr\<Region\>\&) เมธอด

แทนที่ภูมิภาคที่แสดงโดยอ็อบเจ็กต์ปัจจุบันด้วยส่วนของภูมิภาคนี้และภูมิภาคที่ระบุซึ่งไม่ตัดกัน.

```cpp
void System::Drawing::Region::Xor(const SharedPtr<Region> &region)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| region | const [SharedPtr](../../../system/sharedptr/)\<[Region](../)\>\& | ภูมิภาคที่จะทำการ xor กับภูมิภาคที่แสดงโดยอ็อบเจ็กต์ปัจจุบัน |

## ดูเพิ่มเติม

* Typedef [SharedPtr](../../../system/sharedptr/)
* คลาส [RectangleF](../../rectanglef/)
* คลาส [Region](../)
* คลาส [Rectangle](../../rectangle/)
* คลาส [GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)
* เนมสเปซ [System::Drawing](../../)
* Library [Aspose.Slides](../../../)