---
title: Size
second_title: อ้างอิง API ของ Aspose.Slides สำหรับ C++
description: "แสดงถึงคู่ของค่าเต็มจำนวนที่เป็นค่าความกว้างและความสูงของภาพ ประเภทนี้ควรจัดสรรบนสแต็กและส่งผ่านให้ฟังก์ชันโดยค่า หรือโดยการอ้างอิง ไม่ควรใช้คลาส System::SmartPtr เพื่อจัดการออบเจกต์ประเภทนี้"
type: docs
weight: 274
url: /th/system.drawing/size/
---
## คลาสขนาด

Represents a pair of integer values that represent width and height of an image. This type should be allocated on stack and passed to functions by value or by reference. Never use [System::SmartPtr](../../system/smartptr/) class to manage objects of this type.

```cpp
class Size
```

## เมธอด

| เมธอด | คำอธิบาย |
| --- | --- |
| static [Size](./) [Add](./add/)(const [Size](./)\&, const [Size](./)\&) | ส่งคืนออบเจ็กต์ [Size](./) ใหม่ที่เป็นผลรวมของออบเจ็กต์ [Size](./) ที่ระบุ ซึ่งค่าความกว้างจะเท่ากับผลรวมของค่าความกว้างของออบเจ็กต์ที่ระบุและค่าความสูงจะเท่ากับผลรวมของค่าความสูงของออบเจ็กต์ที่ระบุ. |
| static [Size](./) [Ceiling](./ceiling/)(const [SizeF](../sizef/)\&) | สร้างออบเจ็กต์ [Size](./) จากออบเจ็กต์ [SizeF](../sizef/) ที่ระบุโดยทำการปัดค่า ความกว้างและความสูงของออบเจ็กต์ [SizeF](../sizef/) ไปยังค่าจำนวนเต็มที่สูงขึ้นต่อไป. |
| **bool** [Equals](./equals/)(const [Size](./)\&) const | กำหนดว่ามีออบเจ็กต์ปัจจุบันและออบเจ็กต์ที่ระบุเท่ากันหรือไม่ คือเป็นคู่ของค่าความกว้างและความสูงเดียวกัน. |
| int [get_Height](./get_height/)() const | ส่งคืนค่าความสูงที่ออบเจ็กต์ปัจจุบันแสดง. |
| **bool** [get_IsEmpty](./get_isempty/)() const | กำหนดว่าค่าความกว้างและความสูงทั้งสองเป็นศูนย์หรือไม่. |
| int [get_Width](./get_width/)() const | ส่งคืนค่าความกว้างที่ออบเจ็กต์ปัจจุบันแสดง. |
| **int32_t** [GetHashCode](./gethashcode/)() const | ส่งคืนค่ารหัสแฮชของออบเจ็กต์ปัจจุบัน. |
| [operator Point](./operator_point/)() const | สร้างอินสแตนซ์ของออบเจ็กต์ [Point](../point/) และกำหนดพิกัด X และ Y ของมันด้วยค่าความกว้างและความสูงของออบเจ็กต์ปัจจุบันตามลำดับ. |
| [operator SizeF](./operator_sizef/)() const | สร้างอินสแตนซ์ของออบเจ็กต์ [SizeF](../sizef/) และกำหนดค่าให้กับมันด้วยค่าความกว้างและความสูงของออบเจ็กต์ [Size](./) ปัจจุบัน. |
| static [Size](./) [Round](./round/)(const [SizeF](../sizef/)\&) | สร้างออบเจ็กต์ [Size](./) จากออบเจ็กต์ [SizeF](../sizef/) ที่ระบุโดยทำการปัดค่าความกว้างและความสูงของออบเจ็กต์ [SizeF](../sizef/) ไปยังค่าจำนวนเต็มที่ใกล้ที่สุด. |
| void [set_Height](./set_height/)(int) | ตั้งค่าความสูงที่ออบเจ็กต์ปัจจุบันแสดง. |
| void [set_Width](./set_width/)(int) | ตั้งค่าความกว้างที่ออบเจ็กต์ปัจจมัยแสดง. |
| [Size](./size/)() | สร้างออบเจ็กต์ [Size](./) ใหม่และกำหนดค่าความกว้างและความสูงของมันเป็น 0. |
| [Size](./size/)(const [Point](../point/)\&) | สร้างออบเจ็กต์ [Size](./) ใหม่และกำหนดค่าความกว้างและความสูงของมันด้วยค่าพิกัด X และ Y ของจุดที่ระบุตามลำดับ. |
| [Size](./size/)(int, int) | สร้างออบเจ็กต์ [Size](./) ใหม่และกำหนดค่าให้กับมันโดยใช้ค่าที่ระบุ. |
| static [Size](./) [Subtract](./subtract/)(const [Size](./)\&, const [Size](./)\&) | ส่งคืนออบเจ็กต์ [Size](./) ใหม่ที่เป็นผลของการลบ **size2** จาก **size1** ซึ่งค่าความกว้างเป็นผลลัพธ์ของการลบค่าความกว้างของ **size2** จากค่าความกว้างของ **size1** และค่าความสูงเป็นผลลัพธ์ของการลบค่าความสูงของ **size2** จากค่าความสูงของ **size1**. |
| [String](../../system/string/) [ToString](./tostring/)() const | ส่งคืนสตริงที่แสดงคู่ของค่าความกว้างและความสูงที่ออบเจ็กต์ปัจจุบันแสดง. |
| static [Size](./) [Truncate](./truncate/)(const [SizeF](../sizef/)\&) | สร้างออบเจ็กต์ [Size](./) จากออบเจ็กต์ [SizeF](../sizef/) ที่ระบุโดยทำการตัดค่าความกว้างและความสูงของออบเจ็กต์ [SizeF](../sizef/) ให้เป็นค่าจำนวนเต็มที่ต่ำลงต่อไป. |

## ฟิลด์

| ฟิลด์ | คำอธิบาย |
| --- | --- |
| static [Empty](./empty/) | อินสแตนซ์ว่างของคลาส [Size](./) ที่มีค่าความกว้างและความสูงเป็น 0. |

## ดูเพิ่มเติม

* เนมสเปซ [System::Drawing](../)
* ไลบรารี [Aspose.Slides](../../)