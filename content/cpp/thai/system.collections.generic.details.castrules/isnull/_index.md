---
title: IsNull()
second_title: Aspose.Slides สำหรับการอ้างอิง API ของ C++
description: ตรวจสอบว่าค่าที่แสดงเป็น nullptr.
type: docs
weight: 27
url: /th/system.collections.generic.details.castrules/isnull/
---
## System::Collections::Generic::Details::CastRules::IsNull(T) ฟังก์ชัน

ตรวจสอบว่าค่าที่แสดงเป็น nullptr.

```cpp
template<typename T> bool System::Collections::Generic::Details::CastRules::IsNull(T)
```

### พารามิเตอร์แม่แบบ

| พารามิเตอร์ | คำอธิบาย |
| --- | --- |
| T | ประเภทของค่า |

### ค่าที่ส่งกลับ

จะส่งกลับ false เสมอ

## System::Collections::Generic::Details::CastRules::IsNull(SharedPtr\<T\>) ฟังก์ชัน

ตรวจสอบว่าค่าที่แสดงเป็น nullptr.

```cpp
template<typename T> bool System::Collections::Generic::Details::CastRules::IsNull(SharedPtr<T> value)
```

### พารามิเตอร์แม่แบบ

| พารามิเตอร์ | คำอธิบาย |
| --- | --- |
| T | ประเภทของค่า |

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | [SharedPtr](../../system/sharedptr/)\<T\> | ค่าที่ต้องตรวจสอบ |

### ค่าที่ส่งกลับ

true หากค่ามีค่าเป็น nullptr, ไม่เช่นนั้น false

## System::Collections::Generic::Details::CastRules::IsNull(Nullable\<T\>) ฟังก์ชัน

ตรวจสอบว่าค่าที่แสดงเป็น nullptr.

```cpp
template<typename T> bool System::Collections::Generic::Details::CastRules::IsNull(Nullable<T> value)
```

### พารามิเตอร์แม่แบบ

| พารามิเตอร์ | คำอธิบาย |
| --- | --- |
| T | ประเภทของค่า |

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | [Nullable](../../system/nullable/)\<T\> | ค่าที่ต้องตรวจสอบ |

### ค่าที่ส่งกลับ

true หากค่ามีค่าเป็น nullptr, ไม่เช่นนั้น false

## ดูเพิ่มเติม

* Typedef [SharedPtr](../../system/sharedptr/)
* คลาส [Nullable](../../system/nullable/)
* เนมสเปซ [System::Collections::Generic::Details::CastRules](../)
* ไลบรารี [Aspose.Slides](../../)