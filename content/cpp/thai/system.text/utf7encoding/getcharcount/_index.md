---
title: GetCharCount()
second_title: Aspose.Slides สำหรับ C++ เอกสารอ้างอิง API
description: รับจำนวนอักขระที่ต้องการเพื่อถอดรหัสบัฟเฟอร์ไบต์
type: docs
weight: 79
url: /th/system.text/utf7encoding/getcharcount/
---
## UTF7Encoding::GetCharCount(ArrayPtr\<uint8_t\>, int, int) เมธอด

รับจำนวนอักขระที่ต้องการเพื่อถอดรหัสบัฟเฟอร์ไบต์.

```cpp
int System::Text::UTF7Encoding::GetCharCount(ArrayPtr<uint8_t> bytes, int index, int count) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | ไบต์ที่ต้องถอดรหัส |
| index | int | ตำแหน่งเริ่มต้นของส่วน |
| count | int | ขนาดของส่วน |

### ค่าที่คืนกลับ

จำนวนอักขระ

## UTF7Encoding::GetCharCount(const uint8_t *, int) เมธอด

รับจำนวนอักขระที่ต้องการเพื่อถอดรหัสบัฟเฟอร์ไบต์.

```cpp
int System::Text::UTF7Encoding::GetCharCount(const uint8_t *bytes, int count) override
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| bytes | const **uint8_t** * | ไบต์ที่ต้องถอดรหัส |
| count | int | จำนวนไบต์ |

### ค่าที่คืนกลับ

จำนวนอักขระ

## UTF7Encoding::GetCharCount(ArrayPtr\<uint8_t\>, int, int) เมธอด

รับจำนวนอักขระที่ต้องการเพื่อถอดรหัสบัฟเฟอร์ไบต์.

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes, int index, int count)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | ไบต์ที่ต้องถอดรหัส |
| index | int | ตำแหน่งเริ่มต้นของส่วน |
| count | int | ขนาดของส่วน |

### ค่าที่คืนกลับ

จำนวนอักขระ

## UTF7Encoding::GetCharCount(ArrayPtr\<uint8_t\>) เมธอด

รับจำนวนอักขระที่ต้องการเพื่อถอดรหัสบัฟเฟอร์ไบต์.

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | ไบต์ที่ต้องถอดรหัส |

### ค่าที่คืนกลับ

จำนวนอักขระ

## UTF7Encoding::GetCharCount(const uint8_t *, int) เมธอด

รับจำนวนอักขระที่ต้องการเพื่อถอดรหัสบัฟเฟอร์ไบต์.

```cpp
virtual int System::Text::Encoding::GetCharCount(const uint8_t *bytes, int count)
```

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| bytes | const **uint8_t** * | ไบต์ที่ต้องถอดรหัส |
| count | int | จำนวนไบต์ |

### ค่าที่คืนกลับ

จำนวนอักขระ

## ดูเพิ่มเติม

* Typedef [ArrayPtr](../../../system/arrayptr/)
* คลาส [UTF7Encoding](../)
* เนมสเปซ [System::Text](../../)
* ไลบรารี [Aspose.Slides](../../../)