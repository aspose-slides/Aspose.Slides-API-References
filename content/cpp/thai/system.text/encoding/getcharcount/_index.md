---
title: GetCharCount()
second_title: Aspose.Slides สำหรับ C++ การอ้างอิง API
description: รับจำนวนอักขระที่ต้องการเพื่อถอดรหัสบัฟเฟอร์ไบต์
type: docs
weight: 261
url: /th/system.text/encoding/getcharcount/
---
## Encoding::GetCharCount(ArrayPtr\<uint8_t\>, int, int) เมธอด


รับจำนวนอักขระที่ต้องการเพื่อถอดรหัสบัฟเฟอร์ไบต์

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes, int index, int count)
```


### พารามิเตอร์

| Parameter | Type | Description |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | ไบต์เพื่อถอดรหัส |
| index | int | จุดเริ่มต้นของสไลซ์ |
| count | int | ขนาดของสไลซ์ |

### ค่าที่คืน

จำนวนอักขระ

## Encoding::GetCharCount(ArrayPtr\<uint8_t\>) เมธอด


รับจำนวนอักขระที่ต้องการเพื่อถอดรหัสบัฟเฟอร์ไบต์

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes)
```


### พารามิเตอร์

| Parameter | Type | Description |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | ไบต์เพื่อถอดรหัส |

### ค่าที่คืน

จำนวนอักขระ

## Encoding::GetCharCount(const uint8_t *, int) เมธอด


รับจำนวนอักขระที่ต้องการเพื่อถอดรหัสบัฟเฟอร์ไบต์

```cpp
virtual int System::Text::Encoding::GetCharCount(const uint8_t *bytes, int count)
```


### พารามิเตอร์

| Parameter | Type | Description |
| --- | --- | --- |
| bytes | const **uint8_t** * | ไบต์เพื่อถอดรหัส |
| count | int | จำนวนไบต์ |

### ค่าที่คืน

จำนวนอักขระ

## ดูเพิ่มเติม

* Typedef [ArrayPtr](../../../system/arrayptr/)
* คลาส [Encoding](../)
* เนมสเปซ [System::Text](../../)
* ไลบรารี [Aspose.Slides](../../../)