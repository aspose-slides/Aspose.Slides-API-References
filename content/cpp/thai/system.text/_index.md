---
title: "System::Text"
second_title: Aspose.Slides สำหรับ C++ เอกสารอ้างอิง API
description: 
type: docs
weight: 976
url: /th/system.text/
---
## คลาส

| คลาส | คำอธิบาย |
| --- | --- |
| [ASCIIEncoding](./asciiencoding/) | เป็นการเข้ารหัส ASCII. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [Decoder](./decoder/) | สรุปการถอดรหัสลำดับไบต์เป็นลำดับอักขระ. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [DecoderExceptionFallback](./decoderexceptionfallback/) | ให้กลยุทธ์สำรองแบบโยนข้อยกเว้น. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [DecoderExceptionFallbackBuffer](./decoderexceptionfallbackbuffer/) | [Buffer](../system/buffer/) สำหรับกลยุทธ์สำรองการถอดรหัสแบบโยนข้อยกเว้น. ไม่ได้เก็บข้อมูลใด ๆ จริง ๆ แต่จะโยนข้อยกเว้นแทน. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [DecoderFallback](./decoderfallback/) | ให้ API สำรองเพื่อจัดการข้อผิดพลาดการถอดรหัส. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [DecoderFallbackBuffer](./decoderfallbackbuffer/) | ให้บัฟเฟอร์สำหรับการนำกลยุทธ์สำรองไปใช้. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [DecoderReplacementFallback](./decoderreplacementfallback/) | ให้กลยุทธ์สำรองโดยการแทนสัญลักษณ์ที่ผิดพลาดด้วยสแต็ป. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [DecoderReplacementFallbackBuffer](./decoderreplacementfallbackbuffer/) | [Buffer](../system/buffer/) สำหรับการแทนที่กลยุทธ์สำรองการถอดรหัส. |
| [Details_DecoderFallbackException](./details_decoderfallbackexception/) | ข้อยกเว้นที่ถูกโยนโดย [DecoderExceptionFallback](./decoderexceptionfallback/) เมื่อการถอดรหัสล้มเหลว. หลีกเลี่ยงการสร้างอินสแตนซ์ของคลาสนี้ด้วยตนเอง. ให้ใช้คลาส DecoderFallbackException แทน. อย่าห่อหุ้มอินสแตนซ์ของคลาส DecoderFallbackException ด้วย [System::SmartPtr](../system/smartptr/). |
| [Details_EncoderFallbackException](./details_encoderfallbackexception/) | ข้อยกเว้นที่ถูกโยนโดย [EncoderExceptionFallback](./encoderexceptionfallback/) เมื่อการเข้ารหัสล้มเหลว. หลีกเลี่ยงการสร้างอินสแตนซ์ของคลาสนี้ด้วยตนเอง. ให้ใช้คลาส EncoderFallbackException แทน. อย่าห่อหุ้มอินสแตนซ์ของคลาส EncoderFallbackExceptionด้วย [System::SmartPtr](../system/smartptr/). |
| [Encoder](./encoder/) | สรุปการเข้ารหัสลำดับอักขระเป็นลำดับไบต์. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [EncoderExceptionFallback](./encoderexceptionfallback/) | ให้กลยุทธ์สำรองแบบโยนข้อยกเว้น. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [EncoderExceptionFallbackBuffer](./encoderexceptionfallbackbuffer/) | [Buffer](../system/buffer/) สำหรับกลยุทธ์สำรองการเข้ารหัสแบบโยนข้อยกเว้น. ไม่ได้เก็บข้อมูลใด ๆ จริง ๆ แต่จะโยนข้อยกเว้นแทน. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [EncoderFallback](./encoderfallback/) | ให้ API สำรองเพื่อจัดการข้อผิดพลาดการเข้ารหัส. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [EncoderFallbackBuffer](./encoderfallbackbuffer/) | ให้บัฟเฟอร์สำหรับการนำกลยุทธ์สำรองไปใช้. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [EncoderReplacementFallback](./encoderreplacementfallback/) | ให้กลยุทธ์สำรองโดยการแทนสัญลักษณ์ที่ผิดพลาดด้วยสแต็ป. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [EncoderReplacementFallbackBuffer](./encoderreplacementfallbackbuffer/) | [Buffer](../system/buffer/) สำหรับการแทนที่กลยุทธ์สำรองการเข้ารหัส. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [Encoding](./encoding/) | [Encoding](./encoding/) services. |
| [EncodingDecoder](./encodingdecoder/) | [Decoder](./decoder/) ที่ใช้วัตถุการเข้ารหัสสำหรับการถอดรหัส. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [EncodingEncoder](./encodingencoder/) | [Encoder](./encoder/) ที่ใช้วัตถุการเข้ารหัสสำหรับการเข้ารหัส. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [EncodingInfo](./encodinginfo/) | ข้อมูลสรุปสั้น ๆ เกี่ยวกับการเข้ารหัส. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [ICUDecoder](./icudecoder/) | [Decoder](./decoder/) ที่ใช้ ICU สำหรับการถอดรหัส. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [ICUEncoder](./icuencoder/) | [Encoder](./encoder/) ที่ใช้ ICU สำหรับการเข้ารหัส. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [ICUEncoding](./icuencoding/) | การนำไปใช้การเข้ารหัสโดยอิง ICU. สำหรับการใช้ภายในเท่านั้น. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [Latin1Encoding](./latin1encoding/) | การสนับสนุนการเข้ารหัส Latin1. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [StringBuilder](./stringbuilder/) | [Buffer](../system/buffer/) เพื่อสะสมสตริงเป็นส่วน ๆ. ประเภทนี้สามารถจัดสรรได้ทั้งบนสแต็กเป็นชนิดค่าหรือบนฮีปโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). หลังจากอ็อบเจ็กต์ถูกจัดสรรแล้ว อย่าใช้พอยน์เตอร์ [SmartPtr](../system/smartptr/) ไปยังอ็อบเจ็กต์ที่จัดสรรบนสแต็กเพราะเป็นสิ่งต้องห้ามอย่างเคร่งครัด. |
| [UnicodeEncoding](./unicodeencoding/) | การเข้ารหัส Unicode. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [UTF32Encoding](./utf32encoding/) | การเข้ารหัส UTF-32. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [UTF7Encoding](./utf7encoding/) | การเข้ารหัส UTF-7. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
| [UTF8Encoding](./utf8encoding/) | การเข้ารหัส UTF-8. อ็อบเจ็กต์ของคลาสนี้ควรจะถูกจัดสรรเฉพาะโดยใช้ฟังก์ชัน [System::MakeObject()](../system/makeobject/). ห้ามสร้างอินสแตนซ์ของชนิดนี้บนสแต็กหรือโดยใช้ operator new เนื่องจากจะทำให้เกิดข้อผิดพลาดขณะรันไทม์และ/หรือการละเมิด assertion. ควรห่อหุ้มคลาสนี้ด้วยพอยน์เตอร์ [System::SmartPtr](../system/smartptr/) และใช้พอยน์เตอร์นี้เพื่อส่งเป็นอาร์กิวเมนต์ให้กับฟังก์ชัน. |
## Enum

| Enum | คำอธิบาย |
| --- | --- |
| [NormalizationForm](./normalizationform/) | กำหนดวิธีการทำให้สตริง Unicode เป็นรูปแบบมาตรฐาน. |
## Typedefs

| Typedef | คำอธิบาย |
| --- | --- |
| [DecoderFallbackException](./decoderfallbackexception/) |  |
| [EncoderFallbackException](./encoderfallbackexception/) |  |
| [ICUEncodingPtr](./icuencodingptr/) | พอยน์เตอร์การเข้ารหัส ICU. |