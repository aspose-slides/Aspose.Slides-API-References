---
title: GetCharCount()
second_title: Aspose.Slides สำหรับ C++ API Reference
description: รับจำนวนอักขระที่จำเป็นสำหรับการถอดรหัสบัฟเฟอร์ไบต์
type: docs
weight: 53
url: /th/system.text/icuencoding/getcharcount/
---
## ICUEncoding::GetCharCount(const uint8_t *, int) เมธอด


รับจำนวนอักขระที่จำเป็นสำหรับการถอดรหัสบัฟเฟอร์ไบต์

```cpp
int System::Text::ICUEncoding::GetCharCount(const uint8_t *bytes, int count) override
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| bytes | const **uint8_t** * | ไบต์เพื่อถอดรหัส |
| count | int | จำนวนไบต์ |

### ค่าที่คืน

จำนวนอักขระ

## ICUEncoding::GetCharCount(ArrayPtr\<uint8_t\>, int, int) เมธอด


รับจำนวนอักขระที่จำเป็นสำหรับการถอดรหัสบัฟเฟอร์ไบต์

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes, int index, int count)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | ไบต์เพื่อถอดรหัส |
| index | int | จุดเริ่มต้นของส่วน |
| count | int | ขนาดส่วน |

### ค่าที่คืน

จำนวนอักขระ

## ICUEncoding::GetCharCount(ArrayPtr\<uint8_t\>) เมธอด


รับจำนวนอักขระที่จำเป็นสำหรับการถอดรหัสบัฟเฟอร์ไบต์

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | ไบต์เพื่อถอดรหัส |

### ค่าที่คืน

จำนวนอักขระ

## ICUEncoding::GetCharCount(const uint8_t *, int) เมธอด


รับจำนวนอักขระที่จำเป็นสำหรับการถอดรหัสบัฟเฟอร์ไบต์

```cpp
virtual int System::Text::Encoding::GetCharCount(const uint8_t *bytes, int count)
```


### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| bytes | const **uint8_t** * | ไบต์เพื่อถอดรหัส |
| count | int | จำนวนไบต์ |

### ค่าที่คืน

จำนวนอักขระ

## ดูเพิ่มเติม

* กำหนดชนิด [ArrayPtr](../../../system/arrayptr/)
* คลาส [ICUEncoding](../)
* เนมสเปซ [System::Text](../../)
* ไลบรารี [Aspose.Slides](../../../)