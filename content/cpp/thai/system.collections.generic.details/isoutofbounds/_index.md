---
title: IsOutOfBounds()
second_title: อ้างอิง API ของ Aspose.Slides สำหรับ C++
description: ตรวจสอบว่าดัชนีอยู่นอกขอบเขตของคอนเทนเนอร์หรือไม่, ไม่รวมขนาดของคอนเทนเนอร์.
type: docs
weight: 209
url: /th/system.collections.generic.details/isoutofbounds/
---
## System::Collections::Generic::Details::IsOutOfBounds(int, const Container\&) ฟังก์ชัน

ตรวจสอบว่าดัชนีอยู่นอกขอบเขตของคอนเทนเนอร์หรือไม่, ไม่รวมขนาดของคอนเทนเนอร์.

```cpp
template<typename Container> bool System::Collections::Generic::Details::IsOutOfBounds(int idx, const Container &container)
```

### พารามิเตอร์เทมเพลต

| พารามิเตอร์ | คำอธิบาย |
| --- | --- |
| Container | ประเภท Container. |

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| idx | int | [Index](../../system/index/). |
| container | const Container\& | Container เพื่อตรวจสอบดัชนี. |

### ค่าที่ส่งคืน

จริง หากดัชนีอยู่นอกขอบเขตของคอนเทนเนอร์, เท็จในกรณีอื่น.

## System::Collections::Generic::Details::IsOutOfBounds(std::int64_t, const Container\&) ฟังก์ชัน

ตรวจสอบว่าดัชนีอยู่นอกขอบเขตของคอนเทนเนอร์หรือไม่, ไม่รวมขนาดของคอนเทนเนอร์.

```cpp
template<typename Container> bool System::Collections::Generic::Details::IsOutOfBounds(std::int64_t idx, const Container &container)
```

### พารามิเตอร์เทมเพลต

| พารามิเตอร์ | คำอธิบาย |
| --- | --- |
| Container | ประเภท Container. |

### อาร์กิวเมนต์

| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| idx | std::int64_t | [Index](../../system/index/). |
| container | const Container\& | Container เพื่อตรวจสอบดัชนี. |

### ค่าที่ส่งคืน

จริง หากดัชนีอยู่นอกขอบเขตของคอนเทนเนอร์, เท็จในกรณีอื่น.

## ดูเพิ่มเติม

* เนมสเปซ [System::Collections::Generic::Details](../)
* ไลบรารี [Aspose.Slides](../../)