---
title: IsOutOfBounds()
second_title: Aspose.Slides for C++ API リファレンス
description: インデックスがコンテナの境界外かどうかを確認します（コンテナサイズは除外）。
type: docs
weight: 209
url: /ja/system.collections.generic.details/isoutofbounds/
---
## System::Collections::Generic::Details::IsOutOfBounds(int, const Container\&) function


インデックスがコンテナの境界外かどうかを確認します（コンテナサイズは除外します）。

```cpp
template<typename Container> bool System::Collections::Generic::Details::IsOutOfBounds(int idx, const Container &container)
```


### テンプレートパラメータ

| パラメータ | 説明 |
| --- | --- |
| Container | コンテナ型。 |

### 引数

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| idx | int | [Index](../../system/index/). |
| container | const Container\& | インデックスを確認するコンテナ。 |

### 戻り値

インデックスがコンテナの境界外の場合は true、そうでない場合は false。

## System::Collections::Generic::Details::IsOutOfBounds(std::int64_t, const Container\&) function


インデックスがコンテナの境界外かどうかを確認します（コンテナサイズは除外します）。

```cpp
template<typename Container> bool System::Collections::Generic::Details::IsOutOfBounds(std::int64_t idx, const Container &container)
```


### テンプレートパラメータ

| パラメータ | 説明 |
| --- | --- |
| Container | コンテナ型。 |

### 引数

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| idx | std::int64_t | [Index](../../system/index/). |
| container | const Container\& | インデックスを確認するコンテナ。 |

### 戻り値

インデックスがコンテナの境界外の場合は true、そうでない場合は false。

## 参照

* Namespace [System::Collections::Generic::Details](../)
* Library [Aspose.Slides](../../)