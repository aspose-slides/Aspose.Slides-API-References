---
title: end()
second_title: Aspose.Slides for C++ API リファレンス
description: コンテナの最後の要素の次の要素へのイテレータを返します。この要素はプレースホルダーとして機能し、アクセスしようとすると未定義の動作が発生します。
type: docs
weight: 443
url: /ja/system/array/end/
---
## Array::end() メソッド


コンテナの最後の要素の次の要素へのイテレータを返します。この要素はプレースホルダーとして機能し、アクセスしようとすると未定義の動作が発生します。

```cpp
iterator System::Array<T>::end() noexcept
```


### 戻り値

コンテナの最後の要素の後に配置される理論上の要素を指すイテレータです。

## Array::end() const メソッド


const 修飾されたコンテナの最後の要素の次の要素へのイテレータを返します。この要素はプレースホルダーとして機能し、アクセスしようとすると未定義の動作が発生します。

```cpp
const_iterator System::Array<T>::end() const noexcept
```


### 戻り値

const 修飾されたコンテナの最後の要素の後に配置される理論上の要素を指すイテレータです。

## 参照

* Typedef [iterator](../iterator/)
* Typedef [const_iterator](../const_iterator/)
* クラス [Array](../)
* 名前空間 [System](../../)
* ライブラリ [Aspose.Slides](../../../)