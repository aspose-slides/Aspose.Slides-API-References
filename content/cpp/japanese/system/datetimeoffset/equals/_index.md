---
title: Equals()
second_title: Aspose.Slides for C++ APIリファレンス
description: 2 つの DateTimeOffset オブジェクトが同じ時点を表すかどうかを確認します。
type: docs
weight: 365
url: /ja/system/datetimeoffset/equals/
---
## DateTimeOffset::Equals(const DateTimeOffset\&) const メソッド

2 つの [DateTimeOffset](../) オブジェクトが同じ時点を表すかどうかを確認します。

```cpp
bool System::DateTimeOffset::Equals(const DateTimeOffset &other) const
```

### 引数

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | 比較対象となる [Object](../../object/)。 |

### 戻り値

現在のオブジェクトが **other** と等しい場合は true、そうでない場合は false。

## DateTimeOffset::Equals(const SharedPtr\<Object\>\&) const メソッド

2 つの [DateTimeOffset](../) オブジェクトが同じ時点を表すかどうかを確認します。

```cpp
bool System::DateTimeOffset::Equals(const SharedPtr<Object> &obj) const
```

### 引数

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | 比較対象となる [Object](../../object/)。 |

### 戻り値

現在のオブジェクトが **obj** と等しい場合は true、そうでない場合は false。

## DateTimeOffset::Equals(const DateTimeOffset\&, const DateTimeOffset\&) メソッド

2 つの [DateTimeOffset](../) オブジェクトが同じ時点を表すかどうかを確認します。

```cpp
static bool System::DateTimeOffset::Equals(const DateTimeOffset &first, const DateTimeOffset &second)
```

### 引数

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| first | const [DateTimeOffset](../)\& | 比較対象となる最初のオブジェクト。 |
| second | const [DateTimeOffset](../)\& | 比較対象となる二番目のオブジェクト。 |

### 戻り値

**first** が **second** と等しい場合は true、そうでない場合は false。

## 参照

* 型定義 [SharedPtr](../../sharedptr/)
* クラス [DateTimeOffset](../)
* クラス [Object](../../object/)
* 名前空間 [System](../../)
* ライブラリ [Aspose.Slides](../../../)