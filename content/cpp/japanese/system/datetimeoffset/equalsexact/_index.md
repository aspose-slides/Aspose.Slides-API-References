---
title: EqualsExact()
second_title: Aspose.Slides for C++ API リファレンス
description: 2つの DateTimeOffset オブジェクトが同じ時点を表し、同じオフセットを持つかどうかをチェックします。
type: docs
weight: 378
url: /ja/system/datetimeoffset/equalsexact/
---
## DateTimeOffset::EqualsExact(const DateTimeOffset\&) const メソッド

2つの [DateTimeOffset](../) オブジェクトが同じ時点を表し、同じオフセットを持つかどうかをチェックします。

```cpp
bool System::DateTimeOffset::EqualsExact(const DateTimeOffset &other) const
```

### 引数

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/) を比較します。 |

### 戻り値

current object が **other** と正確に等しい場合は true、そうでなければ false。

## DateTimeOffset::EqualsExact(const SharedPtr\<Object\>\&) const メソッド

2つの [DateTimeOffset](../) オブジェクトが同じ時点を表し、同じオフセットを持つかどうかをチェックします。

```cpp
bool System::DateTimeOffset::EqualsExact(const SharedPtr<Object> &obj) const
```

### 引数

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/) を比較します。 |

### 戻り値

current object が **obj** と正確に等しい場合は true、そうでなければ false。

## 参照

* 型定義 [SharedPtr](../../sharedptr/)
* クラス [DateTimeOffset](../)
* クラス [Object](../../object/)
* 名前空間 [System](../../)
* ライブラリ [Aspose.Slides](../../../)