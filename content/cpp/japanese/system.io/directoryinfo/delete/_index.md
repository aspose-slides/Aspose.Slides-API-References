---
title: Delete()
second_title: Aspose.Slides for C++ API リファレンス
description: 現在のオブジェクトが表すパスで参照されるディレクトリが空である場合、そのディレクトリを削除します。
type: docs
weight: 92
url: /ja/system.io/directoryinfo/delete/
---
## DirectoryInfo::Delete() メソッド


現在のオブジェクトが表すパスで参照されるディレクトリが空である場合、そのディレクトリを削除します。

```cpp
virtual void System::IO::DirectoryInfo::Delete() override
```

## DirectoryInfo::Delete(bool) メソッド


現在のオブジェクトが表すパスで参照されるディレクトリを削除します。パラメータは、ディレクトリが空でない場合に、その内容を再帰的に削除するかどうかを指定します。

```cpp
void System::IO::DirectoryInfo::Delete(bool recursive)
```


### 引数

| パラメータ | 型 | 説明 |
| --- | --- | --- |
| recursive | **bool** | ディレクトリが空でない場合に、その内容を再帰的に削除するかどうかを指定します |

## 参照

* クラス [DirectoryInfo](../)
* 名前空間 [System::IO](../../)
* ライブラリ [Aspose.Slides](../../../)