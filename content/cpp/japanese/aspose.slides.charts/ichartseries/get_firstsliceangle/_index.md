---
title: get_FirstSliceAngle()
second_title: Aspose.Slides for C++ API リファレンス
description: 最初の円グラフまたはドーナツ グラフのスライスの角度を度単位で指定します（上から時計回りに、0 から 360 度まで）。このプロパティはこのシリーズだけでなく、親シリーズ グループのすべてのシリーズに共通するもので、該当グループ プロパティの投影です。そのためこのプロパティは読み取り専用です。ParentSeriesGroup プロパティを使用して親シリーズ グループにアクセスします。get_ParentSeriesGroup()->get(set)_FirstSliceAngle() 読み書き可能 プロパティを使用して値を変更します。読み取り専用 uint16_t.
type: docs
weight: 755
url: /ja/aspose.slides.charts/ichartseries/get_firstsliceangle/
---
## IChartSeries::get_FirstSliceAngle() メソッド

最初の円グラフまたはドーナツ グラフのスライスの角度を度単位で指定します（上から時計回りに、0 から 360 度）。このプロパティはこのシリーズだけでなく、親シリーズ グループのすべてのシリーズに共通するもので、該当グループ プロパティの投影です。そのためこのプロパティは読み取り専用です。ParentSeriesGroup プロパティを使用して親シリーズ グループにアクセスします。[get_ParentSeriesGroup()](../get_parentseriesgroup/)->get(set)_FirstSliceAngle() 読み書き可能 プロパティを使用して値を変更します。読み取り専用 **uint16_t**。

```cpp
virtual uint16_t Aspose::Slides::Charts::IChartSeries::get_FirstSliceAngle()=0
```

## 備考

これは[get_ParentSeriesGroup()](../get_parentseriesgroup/)->get(set)_FirstSliceAngle() プロパティの投影です。

## 参照

* クラス [IChartSeries](../)
* 名前空間 [Aspose::Slides::Charts](../../)
* ライブラリ [Aspose.Slides](../../../)