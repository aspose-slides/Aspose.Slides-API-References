---
title: "System::Net::Http"
second_title: "Aspose.Slides for C++ API リファレンス"
description: 
type: docs
weight: 677
url: /ja/system.net.http/
---
## クラス

| クラス | 説明 |
| --- | --- |
| [ByteArrayContent](./bytearraycontent/) | HTTP コンテンツをバイト配列として表します。このクラスのオブジェクトは [System::MakeObject()](../system/makeobject/) 関数を使用してのみ割り当てるべきです。この型のインスタンスをスタック上で作成したり operator new を使用したりしないでください。これらはランタイムエラーやアサーションエラーにつながります。このクラスは常に [System::SmartPtr](../system/smartptr/) ポインタでラップし、そのポインタを関数の引数として渡してください。 |
| [Details_HttpRequestException](./details_httprequestexception/) | 基本例外クラスは [HttpClient](./httpclient/) と [HttpMessageHandler](./httpmessagehandler/) クラスによってスローされます。このクラスのインスタンスを手動で作成しないでください。代わりに HttpRequestException クラスを使用してください。HttpRequestException クラスのインスタンスを [System::SmartPtr](../system/smartptr/) にラップしないでください。 |
| [HttpClient](./httpclient/) | HTTP クライアントの基本クラスを表し、リクエストの送信とレスポンスの受信を行います。このクラスのオブジェクトは [System::MakeObject()](../system/makeobject/) 関数を使用してのみ割り当てるべきです。この型のインスタンスをスタック上で作成したり operator new を使用したりしないでください。これによりランタイムエラーやアサーションエラーが発生します。このクラスは常に [System::SmartPtr](../system/smartptr/) ポインタでラップし、そのポインタを関数の引数として渡してください。 |
| [HttpClientHandler](./httpclienthandler/) | [HttpClient](./httpclient/) クラスが使用するデフォルトのメッセージハンドラを表します。このクラスのオブジェクトは [System::MakeObject()](../system/makeobject/) 関数を使用してのみ割り当てるべきです。この型のインスタンスをスタック上で作成したり operator new を使用したりしないでください。これによりランタイムエラーやアサーションエラーが発生します。このクラスは常に [System::SmartPtr](../system/smartptr/) ポインタでラップし、そのポインタを関数の引数として渡してください。 |
| [HttpContent](./httpcontent/) | HTTP エンティティのコンテンツを表します。このクラスの [Object](../system/object/) は [System::MakeObject()](../system/makeobject/) 関数を使用してのみ割り当てるべきです。この型のインスタンスをスタック上で作成したり operator new を使用したりしないでください。ランタイムエラーやアサーションエラーが発生します。このクラスは常に [System::SmartPtr](../system/smartptr/) ポインタでラップし、そのポインタを関数の引数として渡してください。 |
| [HttpMessageHandler](./httpmessagehandler/) | HTTP メッセージハンドラの基本タイプを表します。このクラスのオブジェクトは [System::MakeObject()](../system/makeobject/) 関数を使用してのみ割り当てるべきです。この型のインスタンスをスタック上で作成したり operator new を使用したりしないでください。ランタイムエラーやアサーションエラーが発生します。このクラスは常に [System::SmartPtr](../system/smartptr/) ポインタでラップし、そのポインタを関数の引数として渡してください。 |
| [HttpMessageInvoker](./httpmessageinvoker/) | アプリケーションが HTTP ハンドラチェーン上で Send メソッドを呼び出すことを可能にします。このクラスのオブジェクトは [System::MakeObject()](../system/makeobject/) 関数を使用してのみ割り当てるべきです。この型のインスタンスをスタック上で作成したり operator new を使用したりしないでください。ランタイムエラーやアサーションエラーが発生します。このクラスは常に [System::SmartPtr](../system/smartptr/) ポインタでラップし、そのポインタを関数の引数として渡してください。 |
| [HttpMethod](./httpmethod/) | HTTP メソッドを表します。このクラスのオブジェクトは [System::MakeObject()](../system/makeobject/) 関数を使用してのみ割り当てるべきです。この型のインスタンスをスタック上で作成したり operator new を使用したりしないでください。ランタイムエラーやアサーションエラーが発生します。このクラスは常に [System::SmartPtr](../system/smartptr/) ポインタでラップし、そのポインタを関数の引数として渡してください。 |
| [HttpRequestMessage](./httprequestmessage/) | HTTP リクエストメッセージを表します。このクラスのオブジェクトは [System::MakeObject()](../system/makeobject/) 関数を使用してのみ割り当てるべきです。この型のインスタンスをスタック上で作成したり operator new を使用したりしないでください。ランタイムエラーやアサーションエラーが発生します。このクラスは常に [System::SmartPtr](../system/smartptr/) ポインタでラップし、そのポインタを関数の引数として渡してください。 |
| [HttpResponseMessage](./httpresponsemessage/) | HTTP レスポンスメッセージを表します。このクラスのオブジェクトは [System::MakeObject()](../system/makeobject/) 関数を使用してのみ割り当てるべきです。この型のインスタンスをスタック上で作成したり operator new を使用したりしないでください。ランタイムエラーやアサーションエラーが発生します。このクラスは常に [System::SmartPtr](../system/smartptr/) ポインタでラップし、そのポインタを関数の引数として渡してください。 |
| [HttpUtilities](./httputilities/) | ユーティリティメソッドを含みます。 |
| [StringContent](./stringcontent/) | HTTP コンテンツを文字列として表します。このクラスのオブジェクトは [System::MakeObject()](../system/makeobject/) 関数を使用してのみ割り当てるべきです。この型のインスタンスをスタック上で作成したり operator new を使用したりしないでください。ランタイムエラーやアサーションエラーが発生します。このクラスは常に [System::SmartPtr](../system/smartptr/) ポインタでラップし、そのポインタを関数の引数として渡してください。 |

## 関数

| 関数 | 説明 |
| --- | --- |
| **bool** [operator==](./operator_equal_equal/)([System::SharedPtr](../system/sharedptr/)\<[HttpMethod](./httpmethod/)\>, [System::SharedPtr](../system/sharedptr/)\<[HttpMethod](./httpmethod/)\>) |  |
| **bool** [operator!=](./operator_not_equal/)([System::SharedPtr](../system/sharedptr/)\<[HttpMethod](./httpmethod/)\>, [System::SharedPtr](../system/sharedptr/)\<[HttpMethod](./httpmethod/)\>) |  |

## 列挙型

| 列挙 | 説明 |
| --- | --- |
| [HttpCompletionOption](./httpcompletionoption/) | [HttpClient](./httpclient/) 操作が完了すべきタイミングを示します。 |
| [HttpParseResult](./httpparseresult/) | 解析結果を示します。 |

## 型定義

| 型定義 | 説明 |
| --- | --- |
| [HttpRequestException](./httprequestexception/) |  |