---
title: rbegin()
second_title: Référence de l'API Aspose.Slides pour C++
description: Renvoie un itérateur inverse vers le dernier élément de la collection (premier en sens inverse).
type: docs
weight: 209
url: /fr/system.collections.objectmodel/collection/rbegin/
---
## Collection::rbegin() méthode

Renvoie un itérateur inverse vers le dernier élément de la collection (premier en sens inverse).

```cpp
reverse_iterator System::Collections::ObjectModel::Collection<T>::rbegin() noexcept
```

### Valeur de retour

Un itérateur inverse pointant vers le dernier élément de la liste (premier en sens inverse).

## Collection::rbegin() const méthode

Renvoie un itérateur inverse vers le dernier élément de la collection const-qualifiée (premier en sens inverse).

```cpp
const_reverse_iterator System::Collections::ObjectModel::Collection<T>::rbegin() const noexcept
```

### Valeur de retour

Un itérateur inverse pointant vers le dernier élément de la liste const-qualifiée (premier en sens inverse).

## Voir aussi

* Typedef [reverse_iterator](../reverse_iterator/)
* Typedef [const_reverse_iterator](../const_reverse_iterator/)
* Classe [Collection](../)
* Espace de noms [System::Collections::ObjectModel](../../)
* Bibliothèque [Aspose.Slides](../../../)