---
title: IndexOfAnyExcept()
second_title: Référence de l'API Aspose.Slides pour C++
description: Renvoie l'indice du premier élément qui n'est pas égal à la valeur spécifiée dans un ReadOnlySpan<T>
type: docs
weight: 170
url: /fr/system.memoryextensions/indexofanyexcept/
---
## System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const T\&) fonction


Renvoie l'indice du premier élément qui n'est pas égal à la valeur spécifiée dans un ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan<T> &span, const T &value)
```


### Paramètres de modèle

| Paramètre | Description |
| --- | --- |
| T | Le type des éléments du span |

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Le span à parcourir |
| value | const T\& | La valeur à exclure de la recherche |

### Valeur de retour

L'indice (base zéro) du premier élément non correspondant, ou -1 si aucune correspondance n'est trouvée

## System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const T\&, const T\&) fonction


Renvoie l'indice du premier élément qui n'est pas égal à l'une des deux valeurs spécifiées dans un ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan<T> &span, const T &value0, const T &value1)
```


### Paramètres de modèle

| Paramètre | Description |
| --- | --- |
| T | Le type des éléments du span |

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Le span à parcourir |
| value0 | const T\& | La première valeur à exclure de la recherche |
| value1 | const T\& | La deuxième valeur à exclure de la recherche |

### Valeur de retour

L'indice (base zéro) du premier élément non correspondant, ou -1 si aucune correspondance n'est trouvée

## System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const T\&, const T\&, const T\&) fonction


Renvoie l'indice du premier élément qui n'est pas égal à aucune des trois valeurs spécifiées dans un ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan<T> &span, const T &value0, const T &value1, const T &value2)
```


### Paramètres de modèle

| Paramètre | Description |
| --- | --- |
| T | Le type des éléments du span |

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Le span à parcourir |
| value0 | const T\& | La première valeur à exclure de la recherche |
| value1 | const T\& | La deuxième valeur à exclure de la recherche |
| value2 | const T\& | La troisième valeur à exclure de la recherche |

### Valeur de retour

L'indice (base zéro) du premier élément non correspondant, ou -1 si aucune correspondance n'est trouvée

## System::MemoryExtensions::IndexOfAnyExcept(const Span\<T\>\&, const T\&) fonction


Renvoie l'indice du premier élément qui n'est pas égal à la valeur spécifiée dans un Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const Span<T> &span, const T &value)
```


### Paramètres de modèle

| Paramètre | Description |
| --- | --- |
| T | Le type des éléments du span |

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Le span à parcourir |
| value | const T\& | La valeur à exclure de la recherche |

### Valeur de retour

L'indice (base zéro) du premier élément non correspondant, ou -1 si aucune correspondance n'est trouvée

## System::MemoryExtensions::IndexOfAnyExcept(const Span\<T\>\&, const T\&, const T\&) fonction


Renvoie l'indice du premier élément qui n'est pas égal à l'une des deux valeurs spécifiées dans un Span<T)

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const Span<T> &span, const T &value0, const T &value1)
```


### Paramètres de modèle

| Paramètre | Description |
| --- | --- |
| T | Le type des éléments du span |

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Le span à parcourir |
| value0 | const T\& | La première valeur à exclure de la recherche |
| value1 | const T\& | La deuxième valeur à exclure de la recherche |

### Valeur de retour

L'indice (base zéro) du premier élément non correspondant, ou -1 si aucune correspondance n'est trouvée

## System::MemoryExtensions::IndexOfAnyExcept(const Span\<T\>\&, const T\&, const T\&, const T\&) fonction


Renvoie l'indice du premier élément qui n'est pas égal à aucune des trois valeurs spécifiées dans un Span<T)

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const Span<T> &span, const T &value0, const T &value1, const T &value2)
```


### Paramètres de modèle

| Paramètre | Description |
| --- | --- |
| T | Le type des éléments du span |

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Le span à parcourir |
| value0 | const T\& | La première valeur à exclure de la recherche |
| value1 | const T\& | La deuxième valeur à exclure de la recherche |
| value2 | const T\& | La troisième valeur à exclure de la recherche |

### Valeur de retour

L'indice (base zéro) du premier élément non correspondant, ou -1 si aucune correspondance n'est trouvée

## System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const ReadOnlySpan\<T\>\&) fonction


Renvoie l'indice du premier élément qui n'est pas égal à aucune des valeurs d'un span de valeurs.

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan<T> &span, const ReadOnlySpan<T> &values)
```


### Paramètres de modèle

| Paramètre | Description |
| --- | --- |
| T | Le type des éléments des spans |

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Le span à parcourir |
| values | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Le span contenant les valeurs à exclure de la recherche |

### Valeur de retour

L'indice (base zéro) du premier élément non correspondant, ou -1 si aucune correspondance n'est trouvée

## System::MemoryExtensions::IndexOfAnyExcept(const Span\<T\>\&, const ReadOnlySpan\<T\>\&) fonction


Renvoie l'indice du premier élément qui n'est pas égal à aucune des valeurs d'un span de valeurs dans un Span<T)

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const Span<T> &span, const ReadOnlySpan<T> &values)
```


### Paramètres de modèle

| Paramètre | Description |
| --- | --- |
| T | Le type des éléments des spans |

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Le span à parcourir |
| values | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Le span contenant les valeurs à exclure de la recherche |

### Valeur de retour

L'indice (base zéro) du premier élément non correspondant, ou -1 si aucune correspondance n'est trouvée

## Voir aussi

* Classe [ReadOnlySpan](../../system/readonlyspan/)
* Classe [Span](../../system/span/)
* Espace de noms [System::MemoryExtensions](../)
* Bibliothèque [Aspose.Slides](../../)