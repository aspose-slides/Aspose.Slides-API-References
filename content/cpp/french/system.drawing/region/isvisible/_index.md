---
title: IsVisible()
second_title: Référence de l'API Aspose.Slides pour C++
description: Détermine si le point spécifié est contenu dans la région représentée par l'objet actuel.
type: docs
weight: 196
url: /fr/system.drawing/region/isvisible/
---
## Region::IsVisible(const Point\&) const méthode

Détermine si le point spécifié est contenu dans la région représentée par l'objet actuel.

```cpp
bool System::Drawing::Region::IsVisible(const Point &point) const
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| point | const [Point](../../point/)\& | Le point à vérifier |

## Region::IsVisible(const PointF\&) const méthode

Détermine si le point spécifié est contenu dans la région représentée par l'objet actuel.

```cpp
bool System::Drawing::Region::IsVisible(const PointF &point) const
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| point | const [PointF](../../pointf/)\& | Le point à vérifier |

## Region::IsVisible(const Rectangle\&) méthode

Détermine si une partie du rectangle spécifié est contenue dans la région représentée par l'objet actuel.

```cpp
bool System::Drawing::Region::IsVisible(const Rectangle &rect)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| rect | const [Rectangle](../../rectangle/)\& | Le rectangle à vérifier |

## Region::IsVisible(const RectangleF\&) méthode

Détermine si une partie du rectangle spécifié est contenue dans la région représentée par l'objet actuel.

```cpp
bool System::Drawing::Region::IsVisible(const RectangleF &rect)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| rect | const [RectangleF](../../rectanglef/)\& | Le rectangle à vérifier |

## Region::IsVisible(const Point\&, const SharedPtr\<Graphics\>\&) const méthode

Détermine si le point spécifié est contenu dans la région représentée par l'objet actuel en utilisant le graphique spécifié.

```cpp
bool System::Drawing::Region::IsVisible(const Point &point, const SharedPtr<Graphics> &graphics) const
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| point | const [Point](../../point/)\& | Le point à vérifier |
| graphics | const [SharedPtr](../../../system/sharedptr/)\<[Graphics](../../graphics/)\>\& | Le contexte graphique |

## Region::IsVisible(const PointF\&, const SharedPtr\<Graphics\>\&) const méthode

Détermine si le point spécifié est contenu dans la région représentée par l'objet actuel en utilisant le graphique spécifié.

```cpp
bool System::Drawing::Region::IsVisible(const PointF &point, const SharedPtr<Graphics> &graphics) const
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| point | const [PointF](../../pointf/)\& | Le point à vérifier |
| graphics | const [SharedPtr](../../../system/sharedptr/)\<[Graphics](../../graphics/)\>\& | Le contexte graphique |

## Region::IsVisible(const Rectangle\&, const SharedPtr\<Graphics\>\&) méthode

Détermine si une partie du rectangle spécifié est contenue dans la région représentée par l'objet actuel en utilisant le graphique spécifié.

```cpp
bool System::Drawing::Region::IsVisible(const Rectangle &rect, const SharedPtr<Graphics> &graphics)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| rect | const [Rectangle](../../rectangle/)\& | Le rectangle à vérifier |
| graphics | const [SharedPtr](../../../system/sharedptr/)\<[Graphics](../../graphics/)\>\& | Le contexte graphique |

## Region::IsVisible(const RectangleF\&, const SharedPtr\<Graphics\>\&) méthode

Détermine si une partie du rectangle spécifié est contenue dans la région représentée par l'objet actuel en utilisant le graphique spécifié.

```cpp
bool System::Drawing::Region::IsVisible(const RectangleF &rect, const SharedPtr<Graphics> &graphics)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| rect | const [RectangleF](../../rectanglef/)\& | Le rectangle à vérifier |
| graphics | const [SharedPtr](../../../system/sharedptr/)\<[Graphics](../../graphics/)\>\& | Le contexte graphique |

## Region::IsVisible(float, float) const méthode

Détermine si le point spécifié est contenu dans la région représentée par l'objet actuel.

```cpp
bool System::Drawing::Region::IsVisible(float x, float y) const
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| x | **float** | La coordonnée X du point à vérifier |
| y | **float** | La coordonnée Y du point à vérifier |

## Region::IsVisible(float, float, const SharedPtr\<Graphics\>\&) const méthode

Détermine si le point spécifié est contenu dans la région représentée par l'objet actuel en utilisant le graphique spécifié.

```cpp
bool System::Drawing::Region::IsVisible(float x, float y, const SharedPtr<Graphics> &graphics) const
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| x | **float** | La coordonnée X du point à vérifier |
| y | **float** | La coordonnée Y du point à vérifier |
| graphics | const [SharedPtr](../../../system/sharedptr/)\<[Graphics](../../graphics/)\>\& | Le contexte graphique |

## Voir aussi

* Typedef [SharedPtr](../../../system/sharedptr/)
* Classe [Point](../../point/)
* Classe [Region](../)
* Classe [PointF](../../pointf/)
* Classe [Rectangle](../../rectangle/)
* Classe [RectangleF](../../rectanglef/)
* Classe [Graphics](../../graphics/)
* Espace de noms [System::Drawing](../../)
* Library [Aspose.Slides](../../../)