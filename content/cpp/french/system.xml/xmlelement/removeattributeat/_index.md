---
title: RemoveAttributeAt()
second_title: Référence API Aspose.Slides pour C++
description: Supprime le nœud d'attribut avec l'index spécifié de l'élément. (Si l'attribut supprimé possède une valeur par défaut, elle est immédiatement remplacée).
type: docs
weight: 339
url: /fr/system.xml/xmlelement/removeattributeat/
---
## XmlElement::RemoveAttributeAt(int32_t) méthode

Supprime le nœud d'attribut avec l'index spécifié de l'élément. (Si l'attribut supprimé possède une valeur par défaut, elle est immédiatement remplacée).

```cpp
virtual SharedPtr<XmlNode> System::Xml::XmlElement::RemoveAttributeAt(int32_t i)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| i | **int32_t** | L'index du nœud à supprimer. Le premier nœud a l'index 0. |

### Valeur de retour

Le nœud d'attribut supprimé ou **nullptr** s'il n'existe aucun nœud à l'index indiqué.

## Voir aussi

* Typedef [SharedPtr](../../../system/sharedptr/)
* Classe [XmlNode](../../xmlnode/)
* Classe [XmlElement](../)
* Espace de noms [System::Xml](../../)
* Bibliothèque [Aspose.Slides](../../../)