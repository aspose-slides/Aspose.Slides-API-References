---
title: Equals()
second_title: Référence de l'API Aspose.Slides for C++
description: Vérifie si deux objets DateTimeOffset représentent le même point dans le temps.
type: docs
weight: 365
url: /fr/system/datetimeoffset/equals/
---
## DateTimeOffset::Equals(const DateTimeOffset\&) const méthode


Vérifie si deux objets [DateTimeOffset](../) représentent le même point dans le temps.

```cpp
bool System::DateTimeOffset::Equals(const DateTimeOffset &other) const
```


### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/) à comparer. |

### Valeur de retour

true si l'objet actuel est égal à **other**, sinon - false.

## DateTimeOffset::Equals(const SharedPtr\<Object\>\&) const méthode


Vérifie si deux objets [DateTimeOffset](../) représentent le même point dans le temps.

```cpp
bool System::DateTimeOffset::Equals(const SharedPtr<Object> &obj) const
```


### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/) à comparer. |

### Valeur de retour

true si l'objet actuel est égal à **obj**, sinon - false.

## DateTimeOffset::Equals(const DateTimeOffset\&, const DateTimeOffset\&) méthode


Vérifie si deux objets [DateTimeOffset](../) représentent le même point dans le temps.

```cpp
static bool System::DateTimeOffset::Equals(const DateTimeOffset &first, const DateTimeOffset &second)
```


### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| first | const [DateTimeOffset](../)\& | Premier objet à comparer. |
| second | const [DateTimeOffset](../)\& | Deuxième objet à comparer. |

### Valeur de retour

true si **first** est égal à **second**, sinon - false.

## Voir aussi

* Typedef [SharedPtr](../../sharedptr/)
* Class [DateTimeOffset](../)
* Class [Object](../../object/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)