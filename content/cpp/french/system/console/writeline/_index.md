---
title: WriteLine()
second_title: Référence de l'API Aspose.Slides for C++
description: Affiche le séparateur de ligne actuel sur le flux de sortie standard.
type: docs
weight: 14
url: /fr/system/console/writeline/
---
## Console::WriteLine() méthode

Affiche le séparateur de ligne actuel sur le flux de sortie standard.

```cpp
static void System::Console::WriteLine()
```

## Console::WriteLine(const SharedPtr\<T\>\&) méthode

Affiche la représentation sous forme de chaîne de l'objet spécifié, suivie du séparateur de ligne actuel, sur le flux de sortie standard.

```cpp
template<class T> static void System::Console::WriteLine(const SharedPtr<T> &object)
```

### Paramètres de modèle

| Paramètre | Description |
| --- | --- |
| T | Type de l'objet à afficher |

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| object | const [SharedPtr](../../sharedptr/)\<T\>\& | [Object](../../object/) à afficher |

## Console::WriteLine(bool) méthode

Affiche la représentation sous forme de chaîne de la valeur booléenne, suivie du séparateur de ligne actuel, sur le flux de sortie standard.

```cpp
static void System::Console::WriteLine(bool value)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | **bool** | La valeur à afficher |

## Console::WriteLine(char_t) méthode

Affiche la valeur de caractère spécifiée, suivie du séparateur de ligne actuel, sur le flux de sortie standard.

```cpp
static void System::Console::WriteLine(char_t value)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | char_t | La valeur à afficher |

## Console::WriteLine(const ArrayPtr\<char_t\>\&) méthode

Affiche la représentation sous forme de chaîne du tableau de caractères spécifié, suivie du séparateur de ligne actuel, sur le flux de sortie standard.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Le tableau à afficher |

## Console::WriteLine(const Decimal\&) méthode

Affiche la représentation sous forme de chaîne de la valeur [Decimal](../../decimal/), suivie du séparateur de ligne actuel, sur le flux de sortie standard.

```cpp
static void System::Console::WriteLine(const Decimal &value)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | La valeur à afficher |

## Console::WriteLine(double) méthode

Affiche la représentation sous forme de chaîne de la valeur à virgule flottante double précision, suivie du séparateur de ligne actuel, sur le flux de sortie standard.

```cpp
static void System::Console::WriteLine(double value)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | **double** | La valeur à afficher |

## Console::WriteLine(float) méthode

Affiche la représentation sous forme de chaîne de la valeur à virgule flottante simple précision, suivie du séparateur de ligne actuel, sur le flux de sortie standard.

```cpp
static void System::Console::WriteLine(float value)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | **float** | La valeur à afficher |

## Console::WriteLine(int32_t) méthode

Affiche la représentation sous forme de chaîne de la valeur entière 32 bits, suivie du séparateur de ligne actuel, sur le flux de sortie standard.

```cpp
static void System::Console::WriteLine(int32_t value)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | **int32_t** | La valeur à afficher |

## Console::WriteLine(int64_t) méthode

Affiche la représentation sous forme de chaîne de la valeur entière 64 bits, suivie du séparateur de ligne actuel, sur le flux de sortie standard.

```cpp
static void System::Console::WriteLine(int64_t value)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | **int64_t** | La valeur à afficher |

## Console::WriteLine(const String\&) méthode

Affiche l'objet string spécifié, suivi du séparateur de ligne actuel, sur le flux de sortie standard.

```cpp
static void System::Console::WriteLine(const String &value)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | L'objet string à afficher |

## Console::WriteLine(const char_t *) méthode

Affiche la c-string spécifiée, suivie du séparateur de ligne actuel, sur le flux de sortie standard.

```cpp
static void System::Console::WriteLine(const char_t *value)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | const char_t * | La c-string à afficher |

## Console::WriteLine(const TypeInfo\&) méthode

Affiche la représentation sous forme de chaîne de la valeur [TypeInfo](../../typeinfo/), suivie du séparateur de ligne actuel, sur le flux de sortie standard.

```cpp
static void System::Console::WriteLine(const TypeInfo &value)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | La valeur à afficher |

## Console::WriteLine(uint32_t) méthode

Affiche la représentation sous forme de chaîne de la valeur entière non signée 32 bits, suivie du séparateur de ligne actuel, sur le flux de sortie standard.

```cpp
static void System::Console::WriteLine(uint32_t value)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | **uint32_t** | La valeur à afficher |

## Console::WriteLine(uint64_t) méthode

Affiche la représentation sous forme de chaîne de la valeur entière non signée 64 bits, suivie du séparateur de ligne actuel, sur le flux de sortie standard.

```cpp
static void System::Console::WriteLine(uint64_t value)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | **uint64_t** | La valeur à afficher |

## Console::WriteLine(const ArrayPtr\<char_t\>\&, int, int) méthode

Affiche la représentation sous forme de chaîne de la plage spécifiée du tableau de caractères spécifié, suivie du séparateur de ligne actuel, sur le flux de sortie standard.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer, int index, int count)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Le tableau de caractères |
| index | int | L'indice dans le tableau où commence la plage à afficher |
| count | int | Le nombre d'éléments dans la plage à afficher |

## Console::WriteLine(const Exception\&) méthode

Affiche la représentation sous forme de chaîne de l'objet Exception spécifié, suivi du séparateur de ligne actuel, sur le flux de sortie standard.

```cpp
static void System::Console::WriteLine(const Exception &e)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| e | const [Exception](../../exception/)\& | La valeur à afficher |

## Console::WriteLine(const String\&, Args\&&...) méthode

Affiche la représentation sous forme de chaîne des arguments spécifiés, formatés selon le format spécifié, suivie du séparateur de ligne actuel, sur le flux de sortie standard.

```cpp
template<class...> static void System::Console::WriteLine(const String &format, Args &&... args)
```

### Paramètres de modèle

| Paramètre | Description |
| --- | --- |
| The | types des valeurs à afficher |

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| format | const [String](../../string/)\& | Le format de chaîne |
| args | Args\&&... | Les valeurs à afficher |

## Console::WriteLine(const char *) méthode




```cpp
static void System::Console::WriteLine(const char *)=delete
```

## Voir aussi

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Typedef [Exception](../../exception/)
* Class [Console](../)
* Class [Decimal](../../decimal/)
* Class [String](../../string/)
* Class [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)