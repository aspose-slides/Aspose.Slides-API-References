---
title: WeakReference()
second_title: Référence de l'API Aspose.Slides pour C++
description: Constructeur par défaut.
type: docs
weight: 1
url: /fr/system/weakreference_tmpl_t__end_tmpl/weakreference/
---
## WeakReference< T >::WeakReference() méthode

Constructeur par défaut.

```cpp
System::WeakReference<T>::WeakReference()
```

## WeakReference< T >::WeakReference(std::nullptr_t) méthode

Constructeur à partir de nullptr.

```cpp
System::WeakReference<T>::WeakReference(std::nullptr_t)
```

## WeakReference< T >::WeakReference(const SmartPtr\<T\>\&) méthode

Initialise une nouvelle instance de la classe WeakReference, en faisant référence à l'objet spécifié.

```cpp
System::WeakReference<T>::WeakReference(const SmartPtr<T> &data)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) à stocker. |

## WeakReference< T >::WeakReference(const SmartPtr\<T\>\&, bool) méthode

Initialise une nouvelle instance de la classe WeakReference, en faisant référence à l'objet spécifié.

```cpp
System::WeakReference<T>::WeakReference(const SmartPtr<T> &data, bool trackResurrection)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) à stocker. |
| trackResurrection | **bool** | Ignoré. |

## Voir aussi

* Classe [WeakReference< T >](../)
* Classe [SmartPtr](../../smartptr/)
* Espace de noms [System](../../)
* Bibliothèque [Aspose.Slides](../../../)