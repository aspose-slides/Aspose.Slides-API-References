---
title: WriteLine()
second_title: Aspose.Slides for C++ Référence API
description: Écrit le séparateur de ligne actuel dans le flux de sortie représenté par l'objet courant.
type: docs
weight: 27
url: /fr/system/consoleoutput/writeline/
---
## ConsoleOutput::WriteLine() méthode

Écrit le séparateur de ligne actuel dans le flux de sortie représenté par l'objet courant.

```cpp
void System::ConsoleOutput::WriteLine() override
```

## ConsoleOutput::WriteLine(const SharedPtr\<Object\>\&) méthode

Écrit la représentation sous forme de chaîne de l'objet spécifié, suivie du séparateur de ligne actuel, dans le flux de sortie représenté par l'objet courant.

```cpp
void System::ConsoleOutput::WriteLine(const SharedPtr<Object> &value) override
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | L'objet à écrire |

## ConsoleOutput::WriteLine(bool) méthode

Écrit la représentation sous forme de chaîne de la valeur booléenne spécifiée, suivie du séparateur de ligne actuel, dans le flux de sortie représenté par l'objet courant.

```cpp
void System::ConsoleOutput::WriteLine(bool value) override
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | **bool** | La valeur booléenne à écrire |

## ConsoleOutput::WriteLine(char_t) méthode

Écrit le caractère spécifié, suivi du séparateur de ligne actuel, dans le flux de sortie représenté par l'objet courant.

```cpp
void System::ConsoleOutput::WriteLine(char_t value) override
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | char_t | La valeur à écrire |

## ConsoleOutput::WriteLine(Decimal) méthode

Écrit la représentation sous forme de chaîne de la valeur [Decimal](../../decimal/), suivie du séparateur de ligne actuel, dans le flux de sortie représenté par l'objet courant.

```cpp
void System::ConsoleOutput::WriteLine(Decimal value) override
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | La valeur à écrire |

## ConsoleOutput::WriteLine(double) méthode

Écrit la représentation sous forme de chaîne de la valeur à virgule flottante double précision, suivie du séparateur de ligne actuel, dans le flux de sortie représenté par l'objet courant.

```cpp
void System::ConsoleOutput::WriteLine(double value) override
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | **double** | La valeur à écrire |

## ConsoleOutput::WriteLine(int) méthode

Écrit la représentation sous forme de chaîne de la valeur entière 32 bits, suivie du séparateur de ligne actuel, dans le flux de sortie représenté par l'objet courant.

```cpp
void System::ConsoleOutput::WriteLine(int value) override
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | int | La valeur à écrire |

## ConsoleOutput::WriteLine(int64_t) méthode

Écrit la représentation sous forme de chaîne de la valeur entière 64 bits, suivie du séparateur de ligne actuel, dans le flux de sortie représenté par l'objet courant.

```cpp
void System::ConsoleOutput::WriteLine(int64_t value) override
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | **int64_t** | La valeur à écrire |

## ConsoleOutput::WriteLine(float) méthode

Écrit la représentation sous forme de chaîne de la valeur à virgule flottante simple précision, suivie du séparateur de ligne actuel, dans le flux de sortie représenté par l'objet courant.

```cpp
void System::ConsoleOutput::WriteLine(float value) override
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | **float** | La valeur à écrire |

## ConsoleOutput::WriteLine(const String\&) méthode

Écrit l'objet chaîne spécifié, suivi du séparateur de ligne actuel, dans le flux de sortie représenté par l'objet courant.

```cpp
void System::ConsoleOutput::WriteLine(const String &value) override
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | L'objet chaîne à écrire |

## ConsoleOutput::WriteLine(uint32_t) méthode

Écrit la représentation sous forme de chaîne de la valeur entière non signée 32 bits, suivie du séparateur de ligne actuel, dans le flux de sortie représenté par l'objet courant.

```cpp
void System::ConsoleOutput::WriteLine(uint32_t value) override
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | **uint32_t** | La valeur à écrire |

## ConsoleOutput::WriteLine(uint64_t) méthode

Écrit la représentation sous forme de chaîne de la valeur entière non signée 64 bits, suivie du séparateur de ligne actuel, dans le flux de sortie représenté par l'objet courant.

```cpp
void System::ConsoleOutput::WriteLine(uint64_t value) override
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | **uint64_t** | La valeur à écrire |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&) méthode

Écrit la représentation sous forme de chaîne du tableau de caractères spécifié, suivi du séparateur de ligne actuel, dans le flux de sortie représenté par l'objet courant.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer) override
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Le tableau à écrire |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&, int32_t, int32_t) méthode

Écrit la représentation sous forme de chaîne d'une plage de valeurs du tableau de caractères spécifié, suivie du séparateur de ligne actuel, dans le flux de sortie représenté par l'objet courant.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Le tableau contenant les valeurs à écrire |
| index | **int32_t** | L'index où commence la plage d'éléments à écrire |
| count | **int32_t** | Le nombre d'éléments dans la plage d'éléments à écrire |

## ConsoleOutput::WriteLine(const char_t *) méthode

Écrit la c-string spécifiée, suivie du séparateur de ligne actuel, dans le flux de sortie représenté par l'objet courant.

```cpp
void System::ConsoleOutput::WriteLine(const char_t *value) override
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | const char_t * | La c-string à écrire |

## ConsoleOutput::WriteLine(const TypeInfo\&) méthode

Écrit la représentation sous forme de chaîne de l'objet [TypeInfo](../../typeinfo/) spécifié, suivie du séparateur de ligne actuel, dans le flux de sortie représenté par l'objet courant.

```cpp
void System::ConsoleOutput::WriteLine(const TypeInfo &value) override
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | L'objet [TypeInfo](../../typeinfo/) à écrire |

## ConsoleOutput::WriteLine(const char *) méthode

```cpp
void System::ConsoleOutput::WriteLine(const char *)=delete
```

## Voir aussi

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Class [ConsoleOutput](../)
* Class [Object](../../object/)
* Class [Decimal](../../decimal/)
* Class [String](../../string/)
* Class [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)