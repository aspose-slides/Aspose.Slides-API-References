---
title: Write()
second_title: Référence de l'API Aspose.Slides pour C++
description: Écrit la représentation sous forme de chaîne de la valeur bool spécifiée dans le flux de sortie représenté par l'objet actuel.
type: docs
weight: 14
url: /fr/system/consoleoutput/write/
---
## ConsoleOutput::Write(bool) méthode

Écrit la représentation sous forme de chaîne de la valeur bool spécifiée dans le flux de sortie représenté par l'objet actuel.

```cpp
void System::ConsoleOutput::Write(bool value) override
```

### Paramètres

| Paramètre | Type | Description |
| --- | --- | --- |
| value | **bool** | La valeur à écrire |

## ConsoleOutput::Write(const SharedPtr\<Object\>\&) méthode

Écrit la représentation sous forme de chaîne de l'objet spécifié dans le flux de sortie représenté par l'objet actuel.

```cpp
void System::ConsoleOutput::Write(const SharedPtr<Object> &value) override
```

### Paramètres

| Paramètre | Type | Description |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | L'objet à écrire |

## ConsoleOutput::Write(char_t) méthode

Écrit la valeur de caractère spécifiée dans le flux de sortie représenté par l'objet actuel.

```cpp
void System::ConsoleOutput::Write(char_t value) override
```

### Paramètres

| Paramètre | Type | Description |
| --- | --- | --- |
| value | char_t | La valeur à écrire |

## ConsoleOutput::Write(Decimal) méthode

Écrit la représentation sous forme de chaîne de la valeur [Decimal](../../decimal/) dans le flux de sortie représenté par l'objet actuel.

```cpp
void System::ConsoleOutput::Write(Decimal value) override
```

### Paramètres

| Paramètre | Type | Description |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | La valeur à écrire |

## ConsoleOutput::Write(double) méthode

Écrit la représentation sous forme de chaîne de la valeur à virgule flottante double précision dans le flux de sortie représenté par l'objet actuel.

```cpp
void System::ConsoleOutput::Write(double value) override
```

### Paramètres

| Paramètre | Type | Description |
| --- | --- | --- |
| value | **double** | La valeur à écrire |

## ConsoleOutput::Write(int32_t) méthode

Écrit la représentation sous forme de chaîne de la valeur entière 32 bits dans le flux de sortie représenté par l'objet actuel.

```cpp
void System::ConsoleOutput::Write(int32_t value) override
```

### Paramètres

| Paramètre | Type | Description |
| --- | --- | --- |
| value | **int32_t** | La valeur à écrire |

## ConsoleOutput::Write(int64_t) méthode

Écrit la représentation sous forme de chaîne de la valeur entière 64 bits dans le flux de sortie représenté par l'objet actuel.

```cpp
void System::ConsoleOutput::Write(int64_t value) override
```

### Paramètres

| Paramètre | Type | Description |
| --- | --- | --- |
| value | **int64_t** | La valeur à écrire |

## ConsoleOutput::Write(float) méthode

Écrit la représentation sous forme de chaîne de la valeur à virgule flottante simple précision dans le flux de sortie représenté par l'objet actuel.

```cpp
void System::ConsoleOutput::Write(float value) override
```

### Paramètres

| Paramètre | Type | Description |
| --- | --- | --- |
| value | **float** | La valeur à écrire |

## ConsoleOutput::Write(const String\&) méthode

Écrit l'objet string spécifié dans le flux de sortie représenté par l'objet actuel.

```cpp
void System::ConsoleOutput::Write(const String &value) override
```

### Paramètres

| Paramètre | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | L'objet string à écrire |

## ConsoleOutput::Write(uint32_t) méthode

Écrit la représentation sous forme de chaîne de la valeur entière non signée 32 bits dans le flux de sortie représenté par l'objet actuel.

```cpp
void System::ConsoleOutput::Write(uint32_t value) override
```

### Paramètres

| Paramètre | Type | Description |
| --- | --- | --- |
| value | **uint32_t** | La valeur à écrire |

## ConsoleOutput::Write(uint64_t) méthode

Écrit la représentation sous forme de chaîne de la valeur entière non signée 64 bits dans le flux de sortie représenté par l'objet actuel.

```cpp
void System::ConsoleOutput::Write(uint64_t value) override
```

### Paramètres

| Paramètre | Type | Description |
| --- | --- | --- |
| value | **uint64_t** | La valeur à écrire |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&) méthode

Écrit la représentation sous forme de chaîne du tableau de caractères spécifié dans le flux de sortie représenté par l'objet actuel.

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer) override
```

### Paramètres

| Paramètre | Type | Description |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Le tableau à écrire |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&, int32_t, int32_t) méthode

Écrit la représentation sous forme de chaîne d’une plage de valeurs du tableau de caractères spécifié dans le flux de sortie représenté par l'objet actuel.

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```

### Paramètres

| Paramètre | Type | Description |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Le tableau contenant les valeurs à écrire |
| index | **int32_t** | L’indice où débute la plage d’éléments à écrire |
| count | **int32_t** | Le nombre d’éléments dans la plage à écrire |

## ConsoleOutput::Write(const char_t *) méthode

Écrit la chaîne C spécifiée dans le flux de sortie représenté par l'objet actuel.

```cpp
void System::ConsoleOutput::Write(const char_t *value) override
```

### Paramètres

| Paramètre | Type | Description |
| --- | --- | --- |
| value | const char_t * | La chaîne C à écrire |

## ConsoleOutput::Write(const TypeInfo\&) méthode

Écrit la représentation sous forme de chaîne de l’objet [TypeInfo](../../typeinfo/) spécifié dans le flux de sortie représenté par l'objet actuel.

```cpp
void System::ConsoleOutput::Write(const TypeInfo &value) override
```

### Paramètres

| Paramètre | Type | Description |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | L’objet [TypeInfo](../../typeinfo/) à écrire |

## ConsoleOutput::Write(const char *) méthode




```cpp
void System::ConsoleOutput::Write(const char *)=delete
```

## Voir aussi

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Classe [ConsoleOutput](../)
* Classe [Object](../../object/)
* Classe [Decimal](../../decimal/)
* Classe [String](../../string/)
* Classe [TypeInfo](../../typeinfo/)
* Espace de noms [System](../../)
* Library [Aspose.Slides](../../../)