---
title: ToString()
second_title: Référence de l'API Aspose.Slides pour C++
description: Enveloppe pour gérer la classe String dans les contextes où ToString() est appelé sur des objets de type valeur.
type: docs
weight: 365
url: /fr/system/string/tostring/
---
## String::ToString() const méthode

Enveloppe pour gérer la classe [String](../) dans les contextes où [ToString()](./) est appelé sur des objets de type valeur.

```cpp
String System::String::ToString() const
```

### Valeur de retour

Copie de la chaîne actuelle. Elle est considérée comme une chaîne nulle si la chaîne actuelle est une chaîne nulle.

## String::ToString(const SharedPtr\<IFormatProvider\>\&) const méthode

Enveloppe pour gérer la classe [String](../) dans les contextes où [ToString()](./) est appelé sur des objets de type valeur.

```cpp
String System::String::ToString(const SharedPtr<IFormatProvider> &) const
```

### Valeur de retour

Copie de la chaîne actuelle. Elle est considérée comme une chaîne nulle si la chaîne actuelle est une chaîne nulle.

## Voir aussi

* Typedef [SharedPtr](../../sharedptr/)
* Classe [String](../)
* Classe [IFormatProvider](../../iformatprovider/)
* Espace de noms [System](../../)
* Library [Aspose.Slides](../../../)