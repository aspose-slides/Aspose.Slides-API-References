---
title: IsLower()
second_title: Référence de l'API Aspose.Slides pour C++
description: Détermine si le caractère à l'index spécifié dans le tampon de caractères spécifié est classé comme une lettre minuscule.
type: docs
weight: 196
url: /fr/system/char/islower/
---
## Char::IsLower(const char_t *, int) méthode

Détermine si le caractère à l'index spécifié dans le tampon de caractères spécifié est classé comme une lettre minuscule.

```cpp
static bool System::Char::IsLower(const char_t *str, int idx)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| str | const char_t * | Pointeur vers le début du tampon de caractères |
| idx | int | Un index basé à zéro dans le tampon spécifié du caractère à tester |

### Valeur de retour

True si le caractère à l'index spécifié est une lettre minuscule, sinon - false

## Char::IsLower(char_t) méthode

Détermine si le caractère spécifié est classé comme une lettre minuscule.

```cpp
static bool System::Char::IsLower(char_t c)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| c | char_t | Le caractère à tester |

### Valeur de retour

True si le caractère spécifié est une lettre minuscule, sinon - false

## Char::IsLower(const String\&, int) méthode

Détermine si le caractère à l'index spécifié dans la chaîne spécifiée est classé comme une lettre minuscule.

```cpp
static bool System::Char::IsLower(const String &str, int idx)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| str | const [String](../../string/)\& | Une chaîne |
| idx | int | Un index basé à zéro dans la chaîne spécifiée à tester |

### Valeur de retour

True si le caractère à l'index spécifié est une lettre minuscule, sinon - false

## Voir aussi

* Classe [Char](../)
* Classe [String](../../string/)
* Espace de noms [System](../../)
* Bibliothèque [Aspose.Slides](../../../)