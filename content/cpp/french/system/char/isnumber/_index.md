---
title: IsNumber()
second_title: Référence API Aspose.Slides pour C++
description: Détermine si le caractère à l'index spécifié dans le tampon de caractères spécifié est classé comme un nombre.
type: docs
weight: 118
url: /fr/system/char/isnumber/
---
## Char::IsNumber(const char_t *, int) méthode

Détermine si le caractère à l'index spécifié dans le tampon de caractères spécifié est classé comme un nombre.

```cpp
static bool System::Char::IsNumber(const char_t *str, int idx)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| str | const char_t * | Pointeur vers le début du tampon de caractères |
| idx | int | Index zéro-base dans le tampon spécifié du caractère à tester |

### Valeur de retour

True si le caractère à l'index spécifié est un nombre, sinon - false

## Char::IsNumber(char_t) méthode

Détermine si le caractère spécifié est classé comme un nombre.

```cpp
static bool System::Char::IsNumber(char_t c)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| c | char_t | Le caractère à tester |

### Valeur de retour

True si le caractère spécifié est un nombre, sinon - false

## Voir aussi

* Classe [Char](../)
* Espace de noms [System](../../)
* Bibliothèque [Aspose.Slides](../../../)