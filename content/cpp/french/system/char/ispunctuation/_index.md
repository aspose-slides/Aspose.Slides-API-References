---
title: IsPunctuation()
second_title: Référence de l'API Aspose.Slides pour C++
description: Détermine si le caractère à l'index spécifié dans le tampon de caractères spécifié est classé comme un caractère de ponctuation.
type: docs
weight: 209
url: /fr/system/char/ispunctuation/
---
## Char::IsPunctuation(const char_t *, int) méthode

Détermine si le caractère à l'index spécifié dans le tampon de caractères spécifié est classé comme un caractère de ponctuation.

```cpp
static bool System::Char::IsPunctuation(const char_t *str, int idx)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| str | const char_t * | Pointeur vers le début du tampon de caractères |
| idx | int | Un index basé à zéro dans le tampon spécifié du caractère à tester |

### Valeur de retour

True si le caractère à l'index spécifié est un caractère de ponctuation, sinon - false

## Char::IsPunctuation(char_t) méthode

Détermine si le caractère spécifié est classé comme un caractère de ponctuation.

```cpp
static bool System::Char::IsPunctuation(char_t c)
```

### Arguments

| Paramètre | Type | Description |
| --- | --- | --- |
| c | char_t | Le caractère à tester |

### Valeur de retour

True si le caractère spécifié est un caractère de ponctuation, sinon - false

## Voir aussi

* Classe [Char](../)
* Espace de noms [System](../../)
* Bibliothèque [Aspose.Slides](../../../)