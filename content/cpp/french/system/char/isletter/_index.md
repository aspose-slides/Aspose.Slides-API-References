---
title: IsLetter()
second_title: Référence API Aspose.Slides pour C++
description: Détermine si le caractère à l'index spécifié dans le tampon de caractères spécifié est classé comme une lettre Unicode.
type: docs
weight: 92
url: /fr/system/char/isletter/
---
## Char::IsLetter(const char_t *, int) méthode

Détermine si le caractère à l’index spécifié dans le tampon de caractères spécifié est classé comme une lettre Unicode.

```cpp
static bool System::Char::IsLetter(const char_t *str, int idx)
```

### Paramètres

| Paramètre | Type | Description |
| --- | --- | --- |
| str | const char_t * | Pointeur vers le début du tampon de caractères |
| idx | int | Index basé sur zéro dans le tampon spécifié du caractère à tester |

### Valeur de retour

True if the character at the specified index is Unicode letter, otherwise - false

## Char::IsLetter(char_t) méthode

Détermine si le caractère spécifié est classé comme une lettre Unicode.

```cpp
static bool System::Char::IsLetter(char_t c)
```

### Paramètres

| Paramètre | Type | Description |
| --- | --- | --- |
| c | char_t | Le caractère à tester |

### Valeur de retour

True if the specified character is Unicode letter, otherwise - false

## Voir aussi

* Classe [Char](../)
* Espace de noms [System](../../)
* Bibliothèque [Aspose.Slides](../../../)