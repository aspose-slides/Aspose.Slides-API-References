---
title: idx_get()
second_title: Référence de l'API Aspose.Slides pour C++
description: Récupère la valeur de l'en-tête à l'aide de l'en-tête de la requête spécifiée.
type: docs
weight: 14
url: /fr/system.net/webheadercollection/idx_get/
---
## WebHeaderCollection::idx_get(HttpRequestHeader) méthode

Récupère la valeur de l’en-tête à l’aide de l’en-tête de la requête spécifiée.

```cpp
String System::Net::WebHeaderCollection::idx_get(HttpRequestHeader header)
```

### Paramètres

| Paramètre | Type | Description |
| --- | --- | --- |
| header | [HttpRequestHeader](../../httprequestheader/) | L'en-tête. |

### Valeur de retour

La valeur de l’en-tête en utilisant l’en-tête spécifié.

## WebHeaderCollection::idx_get(HttpResponseHeader) méthode

Récupère la valeur de l’en-tête à l’aide de l’en-tête de la réponse spécifiée.

```cpp
String System::Net::WebHeaderCollection::idx_get(HttpResponseHeader header)
```

### Paramètres

| Paramètre | Type | Description |
| --- | --- | --- |
| header | [HttpResponseHeader](../../httpresponseheader/) | L'en-tête. |

### Valeur de retour

La valeur de l’en-tête en utilisant l’en-tête spécifié.

## WebHeaderCollection::idx_get(String) méthode

Récupère la valeur de l’en-tête à l’aide du nom d’en-tête spécifié.

```cpp
String System::Net::WebHeaderCollection::idx_get(String name)
```

### Paramètres

| Paramètre | Type | Description |
| --- | --- | --- |
| name | [String](../../../system/string/) | Le nom de l’en-tête. |

### Valeur de retour

La valeur de l’en-tête en utilisant le nom d’en-tête spécifié.

## Voir aussi

* Enum [HttpRequestHeader](../../httprequestheader/)
* Enum [HttpResponseHeader](../../httpresponseheader/)
* Class [String](../../../system/string/)
* Class [WebHeaderCollection](../)
* Namespace [System::Net](../../)
* Library [Aspose.Slides](../../../)