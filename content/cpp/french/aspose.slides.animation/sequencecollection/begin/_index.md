---
title: begin()
second_title: Référence API Aspose.Slides pour C++
description: Obtient un itérateur pointant vers le premier élément (s'il existe) de la collection.
type: docs
weight: 92
url: /fr/aspose.slides.animation/sequencecollection/begin/
---
## SequenceCollection::begin() méthode

Obtient un itérateur pointant vers le premier élément (s'il existe) de la collection.

```cpp
iterator Aspose::Slides::Animation::SequenceCollection::begin() noexcept
```

### Valeur de retour

Un itérateur pointant vers le premier élément (s'il existe) de la collection

## SequenceCollection::begin() const méthode

Obtient un itérateur pointant vers le premier élément (s'il existe) de l'instance qualifiée const de la collection.

```cpp
const_iterator Aspose::Slides::Animation::SequenceCollection::begin() const noexcept
```

### Valeur de retour

Un itérateur pointant vers le premier élément (s'il existe) de l'instance qualifiée const de la collection

## Voir aussi

* Typedef [iterator](../iterator/)
* Typedef [const_iterator](../const_iterator/)
* classe [SequenceCollection](../)
* espace de noms [Aspose::Slides::Animation](../../)
* bibliothèque [Aspose.Slides](../../../)