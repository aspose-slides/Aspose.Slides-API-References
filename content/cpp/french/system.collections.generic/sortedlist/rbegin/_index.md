---
title: rbegin()
second_title: Référence API Aspose.Slides pour C++
description: Obtient un itérateur inverse vers le dernier élément de la collection (premier en sens inverse).
type: docs
weight: 92
url: /fr/system.collections.generic/sortedlist/rbegin/
---
## SortedList::rbegin() méthode

Obtient un itérateur inverse vers le dernier élément de la collection (premier en sens inverse).

```cpp
reverse_iterator System::Collections::Generic::SortedList<TKey, TValue>::rbegin() noexcept
```

### Valeur de retour

Un itérateur inverse pointant vers le dernier élément de la map (premier en sens inverse).

## SortedList::rbegin() const méthode

Obtient un itérateur inverse vers le dernier élément de la collection qualifiée en const (premier en sens inverse).

```cpp
const_reverse_iterator System::Collections::Generic::SortedList<TKey, TValue>::rbegin() const noexcept
```

### Valeur de retour

Un itérateur inverse pointant vers le dernier élément de la map qualifiée en const (premier en sens inverse).

## Voir aussi

* Typedef [reverse_iterator](../reverse_iterator/)
* Typedef [const_reverse_iterator](../const_reverse_iterator/)
* classe [SortedList](../)
* espace de noms [System::Collections::Generic](../../)
* bibliothèque [Aspose.Slides](../../../)