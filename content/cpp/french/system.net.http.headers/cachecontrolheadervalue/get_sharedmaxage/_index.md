---
title: get_SharedMaxAge()
second_title: Référence API Aspose.Slides pour C++
description: Obtient la valeur d'âge maximal partagé en secondes qui remplace la directive 'max-age' dans l'en-tête 'Cache-Control' ou l'en-tête 'Expires' pour un cache partagé.
type: docs
weight: 92
url: /fr/system.net.http.headers/cachecontrolheadervalue/get_sharedmaxage/
---
## CacheControlHeaderValue::get_SharedMaxAge() méthode

Obtient la valeur d'âge maximal partagé en secondes qui remplace la directive 'max-age' dans l'en-tête 'Cache-Control' ou l'en-tête 'Expires' pour un cache partagé.

```cpp
Nullable<TimeSpan> System::Net::Http::Headers::CacheControlHeaderValue::get_SharedMaxAge()
```

### Valeur de retour

La valeur d'âge maximal partagé en secondes qui remplace la directive 'max-age' dans l'en-tête 'Cache-Control' ou l'en-tête 'Expires' pour un cache partagé.

## Voir aussi

* Classe [Nullable](../../../system/nullable/)
* Classe [TimeSpan](../../../system/timespan/)
* Classe [CacheControlHeaderValue](../)
* Espace de noms [System::Net::Http::Headers](../../)
* Bibliothèque [Aspose.Slides](../../../)