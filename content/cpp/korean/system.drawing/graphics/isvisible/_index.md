---
title: IsVisible()
second_title: Aspose.Slides for C++ API 레퍼런스
description: 현재 Graphics 객체의 보이는 클립 영역에 지정된 점이 포함되어 있는지 확인합니다.
type: docs
weight: 963
url: /ko/system.drawing/graphics/isvisible/
---
## Graphics::IsVisible(Point) 메서드


현재 [Graphics](../) 객체의 보이는 클립 영역에 지정된 점이 포함되어 있는지 확인합니다.

```cpp
bool System::Drawing::Graphics::IsVisible(Point point)
```


### Arguments

| Parameter | Type | Description |
| --- | --- | --- |
| point | [Point](../../point/) | 확인할 점 |

### Return Value

현재 [Graphics](../) 객체의 보이는 클립 영역에 **point**가 포함되어 있으면 True, 그렇지 않으면 false

## Graphics::IsVisible(PointF) 메서드


구현되지 않음.

```cpp
bool System::Drawing::Graphics::IsVisible(PointF point)
```


## Graphics::IsVisible(Rectangle) 메서드


구현되지 않음.

```cpp
bool System::Drawing::Graphics::IsVisible(Rectangle rect)
```


## Graphics::IsVisible(RectangleF) 메서드


구현되지 않음.

```cpp
bool System::Drawing::Graphics::IsVisible(RectangleF rect)
```


## Graphics::IsVisible(int32_t, int32_t) 메서드


구현되지 않음.

```cpp
bool System::Drawing::Graphics::IsVisible(int32_t x, int32_t y)
```


## Graphics::IsVisible(float, float) 메서드


구현되지 않음.

```cpp
bool System::Drawing::Graphics::IsVisible(float x, float y)
```


## Graphics::IsVisible(float, float, float, float) 메서드


구현되지 않음.

```cpp
bool System::Drawing::Graphics::IsVisible(float x, float y, float width, float height)
```


## Graphics::IsVisible(int32_t, int32_t, int32_t, int32_t) 메서드


구현되지 않음.

```cpp
bool System::Drawing::Graphics::IsVisible(int32_t x, int32_t y, int32_t width, int32_t height)
```


## 참고

* 클래스 [Point](../../point/)
* 클래스 [Graphics](../)
* 클래스 [PointF](../../pointf/)
* 클래스 [Rectangle](../../rectangle/)
* 클래스 [RectangleF](../../rectanglef/)
* 네임스페이스 [System::Drawing](../../)
* 라이브러리 [Aspose.Slides](../../../)