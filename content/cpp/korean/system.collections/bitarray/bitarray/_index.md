---
title: BitArray()
second_title: Aspose.Slides for C++ API 참조
description: 복사 생성자.
type: docs
weight: 14
url: /ko/system.collections/bitarray/bitarray/
---
## BitArray::BitArray(const bitset\&) 생성자

복사 생성자.

```cpp
System::Collections::BitArray::BitArray(const bitset &bits)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| bits | const [bitset](../bitset/)\& | 데이터를 복사할 컨테이너. |

## BitArray::BitArray(const BitArray\&) 생성자

복사 생성자.

```cpp
System::Collections::BitArray::BitArray(const BitArray &bits)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| bits | const [BitArray](../)\& | 데이터를 복사할 컨테이너. |

## BitArray::BitArray(const BitArrayPtr\&) 생성자

복사 생성자.

```cpp
System::Collections::BitArray::BitArray(const BitArrayPtr &array)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| array | const [BitArrayPtr](../../bitarrayptr/)\& | [BitArray](../)를 복사할 대상. |

## BitArray::BitArray(const System::ArrayPtr\<bool\>\&) 생성자

복사 생성자.

```cpp
System::Collections::BitArray::BitArray(const System::ArrayPtr<bool> &values)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| values | const [System::ArrayPtr](../../../system/arrayptr/)\<**bool**\>\& | 요소별로 데이터를 복사할 컨테이너. |

## BitArray::BitArray(const System::ArrayPtr\<uint8_t\>\&) 생성자

복사 생성자.

```cpp
System::Collections::BitArray::BitArray(const System::ArrayPtr<uint8_t> &bytes)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| bytes | const [System::ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\>\& | 비트 단위로 데이터를 복사할 컨테이너. |

## BitArray::BitArray(const System::ArrayPtr\<int\>\&) 생성자

복사 생성자.

```cpp
System::Collections::BitArray::BitArray(const System::ArrayPtr<int> &values)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| values | const [System::ArrayPtr](../../../system/arrayptr/)\<int\>\& | 비트 단위로 데이터를 복사할 컨테이너. |

## BitArray::BitArray(int, bool) 생성자

채우기 생성자.

```cpp
System::Collections::BitArray::BitArray(int length, bool defaultValue=false)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| length | int | 채울 요소 수. |
| defaultValue | **bool** | 채우는 값. |

## 참고

* Typedef [bitset](../bitset/)
* Typedef [ArrayPtr](../../../system/arrayptr/)
* Class [BitArray](../)
* Class [BitArrayPtr](../../bitarrayptr/)
* Namespace [System::Collections](../../)
* Library [Aspose.Slides](../../../)