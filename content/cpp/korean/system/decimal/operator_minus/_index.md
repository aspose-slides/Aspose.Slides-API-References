---
title: operator-()
second_title: Aspose.Slides for C++ API 레퍼런스
description: 지정된 객체가 나타내는 값에서 현재 객체가 나타내는 값을 빼서 얻은 결과 값을 나타내는 Decimal 클래스의 새 인스턴스를 반환합니다.
type: docs
weight: 92
url: /ko/system/decimal/operator_minus/
---
## Decimal::operator-(const Decimal\&) const method


지정된 객체가 나타내는 값에서 현재 객체가 나타내는 값을 빼서 얻은 결과 값을 나타내는 [Decimal](../) 클래스의 새 인스턴스를 반환합니다.

```cpp
Decimal System::Decimal::operator-(const Decimal &d) const
```


### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| d | const [Decimal](../)\& | 빼려는 값을 나타내는 [Decimal](../) 객체 |

### 반환값

지정된 객체가 나타내는 값에서 현재 객체가 나타내는 값을 빼서 얻은 결과 값을 나타내는 [Decimal](../) 클래스의 새 인스턴스를 반환합니다.

## Decimal::operator-() const method


현재 객체가 나타내는 값의 부정 결과 값을 나타내는 [Decimal](../) 클래스의 새 인스턴스를 반환합니다.

```cpp
Decimal System::Decimal::operator-() const
```

## 또 보기

* 클래스 [Decimal](../)
* 네임스페이스 [System](../../)
* 라이브러리 [Aspose.Slides](../../../)