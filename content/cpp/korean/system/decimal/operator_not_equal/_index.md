---
title: operator!=()
second_title: C++용 Aspose.Slides API 레퍼런스
description: 현재 객체와 지정된 객체가 나타내는 값이 동등하지 않은지 확인합니다.
type: docs
weight: 157
url: /ko/system/decimal/operator_not_equal/
---
## Decimal::operator!=(const Decimal\&) const method

현재 객체와 지정된 객체가 나타내는 값이 동등하지 않은지 확인합니다.

```cpp
bool System::Decimal::operator!=(const Decimal &d) const
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| d | const [Decimal](../)\& | 현재 객체와 비교하기 위한 [Decimal](../) 객체 |

### 반환 값

현재 객체와 지정된 객체가 나타내는 값이 동등하지 않으면 true, 그렇지 않으면 - false

## Decimal::operator!=(std::nullptr_t) const method

현재 객체가 나타내는 값이 0과 다른지 확인합니다.

```cpp
bool System::Decimal::operator!=(std::nullptr_t) const
```

### 반환 값

현재 객체가 나타내는 값이 0과 다르면 true, 그렇지 않으면 - false

## 참조

* 클래스 [Decimal](../)
* 네임스페이스 [System](../../)
* 라이브러리 [Aspose.Slides](../../../)