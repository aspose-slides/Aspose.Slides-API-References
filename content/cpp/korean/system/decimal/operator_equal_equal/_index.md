---
title: operator==()
second_title: Aspose.Slides for C++ API 참조
description: 현재 객체와 지정된 객체가 나타내는 값이 같은지 판단합니다.
type: docs
weight: 144
url: /ko/system/decimal/operator_equal_equal/
---
## Decimal::operator==(const Decimal\&) const 메서드

현재 객체와 지정된 객체가 나타내는 값이 같은지 판단합니다.

```cpp
bool System::Decimal::operator==(const Decimal &d) const
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| d | const [Decimal](../)\& | 현재 객체와 비교할 [Decimal](../) 객체 |

### 반환값

현재 객체와 지정된 객체가 나타내는 값이 같으면 true, 그렇지 않으면 false

## Decimal::operator==(std::nullptr_t) const 메서드

현재 객체가 나타내는 값이 0인지 판단합니다.

```cpp
bool System::Decimal::operator==(std::nullptr_t) const
```

### 반환값

현재 객체가 나타내는 값이 0이면 true, 그렇지 않으면 false

## 참고

* 클래스 [Decimal](../)
* 네임스페이스 [System](../../)
* 라이브러리 [Aspose.Slides](../../../)