---
title: Equals()
second_title: Aspose.Slides for C++ API 레퍼런스
description: 현재 객체와 지정된 객체가 나타내는 값이 동일한지 확인합니다.
type: docs
weight: 313
url: /ko/system/decimal/equals/
---
## Decimal::Equals(const Decimal\&) const 메서드

현재 객체와 지정된 객체가 나타내는 값이 동일한지 확인합니다.

```cpp
bool System::Decimal::Equals(const Decimal &d) const
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| d | const [Decimal](../)\& | 현재 객체와 비교할 [Decimal](../) 객체 |

### 반환값

현재와 지정된 객체가 나타내는 값이 동일하면 true, 그렇지 않으면 false를 반환합니다.

## Decimal::Equals(const SharedPtr\<Object\>\&) const 메서드

현재 객체와 지정된 객체가 나타내는 값이 동일한지 확인합니다.

```cpp
bool System::Decimal::Equals(const SharedPtr<Object> &obj) const
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | 현재 객체와 비교할 객체 |

### 반환값

현재와 지정된 객체가 나타내는 값이 동일하면 true, 그렇지 않으면 false를 반환합니다.

## Decimal::Equals(const Decimal\&, const Decimal\&) 메서드

지정된 객체들이 나타내는 값이 동일한지 확인합니다.

```cpp
static bool System::Decimal::Equals(const Decimal &d1, const Decimal &d2)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| d1 | const [Decimal](../)\& | 첫 번째 비교 대상 |
| d2 | const [Decimal](../)\& | 두 번째 비교 대상 |

### 반환값

**d1**과 **d2**가 나타내는 값이 동일하면 true, 그렇지 않으면 false를 반환합니다.

## 참조

* Typedef [SharedPtr](../../sharedptr/)
* 클래스 [Decimal](../)
* 클래스 [Object](../../object/)
* 네임스페이스 [System](../../)
* 라이브러리 [Aspose.Slides](../../../)