---
title: end()
second_title: Aspose.Slides for C++ API 레퍼런스
description: 컨테이너의 마지막 요소 다음에 오는 요소에 대한 반복자를 반환합니다. 이 요소는 자리 표시자 역할을 하며, 접근을 시도하면 정의되지 않은 동작이 발생합니다.
type: docs
weight: 443
url: /ko/system/array/end/
---
## Array::end() 메서드

컨테이너의 마지막 요소 다음에 오는 요소에 대한 반복자를 반환합니다. 이 요소는 자리 표시자 역할을 하며, 접근을 시도하면 정의되지 않은 동작이 발생합니다.

```cpp
iterator System::Array<T>::end() noexcept
```

### 반환 값

컨테이너의 마지막 요소 뒤에 배치된 이론적 요소를 가리키는 반복자입니다.

## Array::end() const 메서드

const 한정자를 가진 컨테이너의 마지막 요소 다음에 오는 요소에 대한 반복자를 반환합니다. 이 요소는 자리 표시자 역할을 하며, 접근을 시도하면 정의되지 않은 동작이 발생합니다.

```cpp
const_iterator System::Array<T>::end() const noexcept
```

### 반환 값

const 한정자를 가진 컨테이너의 마지막 요소 뒤에 배치된 이론적 요소를 가리키는 반복자입니다.

## 관련 항목

* Typedef [iterator](../iterator/)
* Typedef [const_iterator](../const_iterator/)
* 클래스 [Array](../)
* 네임스페이스 [System](../../)
* 라이브러리 [Aspose.Slides](../../../)