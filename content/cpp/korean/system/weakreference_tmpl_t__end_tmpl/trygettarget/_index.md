---
title: TryGetTarget()
second_title: Aspose.Slides for C++ API 레퍼런스
description: 현재 WeakReference 객체가 참조하고 있는 객체(대상)를 가져옵니다.
type: docs
weight: 14
url: /ko/system/weakreference_tmpl_t__end_tmpl/trygettarget/
---
## WeakReference< T >::TryGetTarget(const SmartPtr\<T\>\&) const 메서드

현재 WeakReference 객체가 참조하고 있는 객체(대상)를 가져옵니다.

```cpp
bool System::WeakReference<T>::TryGetTarget(const SmartPtr<T> &data) const
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) 현재 WeakReference 객체에 의해 참조됨. |

### 반환 값

작업이 성공했으면 true, 그렇지 않으면 false.

## 참고

* 클래스 [SmartPtr](../../smartptr/)
* 클래스 [WeakReference< T >](../)
* 네임스페이스 [System](../../)
* 라이브러리 [Aspose.Slides](../../../)