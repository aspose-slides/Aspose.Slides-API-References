---
title: operator<<()
second_title: Aspose.Slides for C++ API 참조
description: UTF-8 인코딩을 사용하여 스트림에 데이터를 삽입합니다.
type: docs
weight: 2159
url: /ko/system/operator_less_less/
---
## System::operator<<(std::ostream\&, DateTime) 함수

UTF-8 인코딩을 사용하여 스트림에 데이터를 삽입합니다.

```cpp
std::ostream & System::operator<<(std::ostream &stream, DateTime date_time)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::ostream\& | 데이터를 삽입할 출력 스트림. |
| date_time | [DateTime](../datetime/) | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::wostream\&, DateTime) 함수

스트림에 데이터를 삽입합니다.

```cpp
std::wostream & System::operator<<(std::wostream &stream, DateTime date_time)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::wostream\& | 데이터를 삽입할 출력 스트림. |
| date_time | [DateTime](../datetime/) | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::ostream\&, DateTimeOffset) 함수

UTF-8 인코딩을 사용하여 스트림에 데이터를 삽입합니다.

```cpp
std::ostream & System::operator<<(std::ostream &stream, DateTimeOffset value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::ostream\& | 데이터를 삽입할 출력 스트림. |
| value | [DateTimeOffset](../datetimeoffset/) | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::wostream\&, DateTimeOffset) 함수

스트림에 데이터를 삽입합니다.

```cpp
std::wostream & System::operator<<(std::wostream &stream, DateTimeOffset value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::wostream\& | 데이터를 삽입할 출력 스트림. |
| value | [DateTimeOffset](../datetimeoffset/) | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::ostream\&, const Decimal\&) 함수

UTF-8 인코딩을 사용하여 스트림에 데이터를 삽입합니다.

```cpp
std::ostream & System::operator<<(std::ostream &stream, const Decimal &decimal)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::ostream\& | 데이터를 삽입할 출력 스트림. |
| decimal | const [Decimal](../decimal/)\& | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::wostream\&, const Decimal\&) 함수

스트림에 데이터를 삽입합니다.

```cpp
std::wostream & System::operator<<(std::wostream &stream, const Decimal &decimal)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::wostream\& | 데이터를 삽입할 출력 스트림. |
| decimal | const [Decimal](../decimal/)\& | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::ostream\&, const Exception\&) 함수

UTF-8 인코딩을 사용하여 스트림에 데이터를 삽입합니다.

```cpp
std::ostream & System::operator<<(std::ostream &stream, const Exception &exception)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::ostream\& | 데이터를 삽입할 출력 스트림. |
| exception | const [Exception](../exception/)\& | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::wostream\&, const Exception\&) 함수

스트림에 데이터를 삽입합니다.

```cpp
std::wostream & System::operator<<(std::wostream &stream, const Exception &exception)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::wostream\& | 데이터를 삽입할 출력 스트림. |
| exception | const [Exception](../exception/)\& | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::ostream\&, const Guid\&) 함수

UTF-8 인코딩을 사용하여 스트림에 데이터를 삽입합니다.

```cpp
std::ostream & System::operator<<(std::ostream &stream, const Guid &guid)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::ostream\& | 데이터를 삽입할 출력 스트림. |
| guid | const [Guid](../guid/)\& | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::wostream\&, const Guid\&) 함수

스트림에 데이터를 삽입합니다.

```cpp
std::wostream & System::operator<<(std::wostream &stream, const Guid &guid)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::wostream\& | 데이터를 삽입할 출력 스트림. |
| guid | const [Guid](../guid/)\& | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::ostream\&, const Nullable\<T\>\&) 함수

UTF-8 인코딩을 사용하여 스트림에 데이터를 삽입합니다.

```cpp
template<typename T> std::ostream & System::operator<<(std::ostream &stream, const Nullable<T> &value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::ostream\& | 데이터를 삽입할 출력 스트림. |
| value | const [Nullable](../nullable/)\<T\>\& | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::wostream\&, const Nullable\<T\>\&) 함수

스트림에 데이터를 삽입합니다.

```cpp
template<typename T> std::wostream & System::operator<<(std::wostream &stream, const Nullable<T> &value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::wostream\& | 데이터를 삽입할 출력 스트림. |
| value | const [Nullable](../nullable/)\<T\>\& | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::ostream\&, const System::Object\&) 함수

UTF-8 인코딩을 사용하여 스트림에 데이터를 삽입합니다.

```cpp
std::ostream & System::operator<<(std::ostream &stream, const System::Object &object)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::ostream\& | 데이터를 삽입할 출력 스트림. |
| object | const [System::Object](../object/)\& | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::wostream\&, const System::Object\&) 함수

스트림에 데이터를 삽입합니다.

```cpp
std::wostream & System::operator<<(std::wostream &stream, const System::Object &object)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::wostream\& | 데이터를 삽입할 출력 스트림. |
| object | const [System::Object](../object/)\& | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::ostream\&, const OperatingSystem\&) 함수

UTF-8 인코딩을 사용하여 스트림에 데이터를 삽입합니다.

```cpp
std::ostream & System::operator<<(std::ostream &stream, const OperatingSystem &os)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::ostream\& | 데이터를 삽입할 출력 스트림. |
| os | const [OperatingSystem](../operatingsystem/)\& | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::wostream\&, const OperatingSystem\&) 함수

스트림에 데이터를 삽입합니다.

```cpp
std::wostream & System::operator<<(std::wostream &stream, const OperatingSystem &os)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::wostream\& | 데이터를 삽입할 출력 스트림. |
| os | const [OperatingSystem](../operatingsystem/)\& | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::ostream\&, const SharedPtr\<T\>\&) 함수

UTF-8 인코딩을 사용하여 스트림에 데이터를 삽입합니다.

```cpp
template<typename T> std::ostream & System::operator<<(std::ostream &stream, const SharedPtr<T> &object_ptr)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::ostream\& | 데이터를 삽입할 출력 스트림. |
| object_ptr | const [SharedPtr](../sharedptr/)\<T\>\& | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::wostream\&, const SharedPtr\<T\>\&) 함수

스트림에 데이터를 삽입합니다.

```cpp
template<typename T> std::wostream & System::operator<<(std::wostream &stream, const SharedPtr<T> &object_ptr)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::wostream\& | 데이터를 삽입할 출력 스트림. |
| object_ptr | const [SharedPtr](../sharedptr/)\<T\>\& | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::ostream\&, const String\&) 함수

UTF-8 인코딩을 사용하여 문자열을 출력 스트림에 출력합니다.

```cpp
std::ostream & System::operator<<(std::ostream &os, const String &str)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| os | std::ostream\& | **char** 로 특수화된 **basic_ostream** 객체인 출력 스트림. |
| str | const [String](../string/)\& | 출력 스트림으로 보낼 문자열. |

### 반환 값

문자열이 추가된 출력 스트림.

## System::operator<<(std::wostream\&, const String\&) 함수

문자열을 출력 스트림에 출력합니다.

```cpp
std::wostream & System::operator<<(std::wostream &os, const String &str)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| os | std::wostream\& | **wchar_t** 로 특수화된 **basic_ostream** 객체인 출력 스트림. |
| str | const [String](../string/)\& | 출력 스트림으로 보낼 문자열. |

### 반환 값

문자열이 추가된 출력 스트림.

## System::operator<<(std::ostream\&, TimeSpan) 함수

UTF-8 인코딩을 사용하여 스트림에 데이터를 삽입합니다.

```cpp
std::ostream & System::operator<<(std::ostream &stream, TimeSpan time_span)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::ostream\& | 데이터를 삽입할 출력 스트림. |
| time_span | [TimeSpan](../timespan/) | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::wostream\&, TimeSpan) 함수

스트림에 데이터를 삽입합니다.

```cpp
std::wostream & System::operator<<(std::wostream &stream, TimeSpan time_span)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::wostream\& | 데이터를 삽입할 출력 스트림. |
| time_span | [TimeSpan](../timespan/) | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::ostream\&, const TypeInfo\&) 함수

UTF-8 인코딩을 사용하여 스트림에 데이터를 삽입합니다.

```cpp
std::ostream & System::operator<<(std::ostream &stream, const TypeInfo &type_info)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::ostream\& | 데이터를 삽입할 출력 스트림. |
| type_info | const [TypeInfo](../typeinfo/)\& | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::wostream\&, const TypeInfo\&) 함수

스트림에 데이터를 삽입합니다.

```cpp
std::wostream & System::operator<<(std::wostream &stream, const TypeInfo &type_info)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::wostream\& | 데이터를 삽입할 출력 스트림. |
| type_info | const [TypeInfo](../typeinfo/)\& | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::ostream\&, const Version\&) 함수

UTF-8 인코딩을 사용하여 스트림에 데이터를 삽입합니다.

```cpp
std::ostream & System::operator<<(std::ostream &stream, const Version &version)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::ostream\& | 데이터를 삽입할 출력 스트림. |
| version | const [Version](../version/)\& | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::wostream\&, const Version\&) 함수

스트림에 데이터를 삽입합니다.

```cpp
std::wostream & System::operator<<(std::wostream &stream, const Version &version)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::wostream\& | 데이터를 삽입할 출력 스트림. |
| version | const [Version](../version/)\& | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::ostream\&, const WeakPtr\<T\>\&) 함수

UTF-8 인코딩을 사용하여 스트림에 데이터를 삽입합니다.

```cpp
template<typename T> std::ostream & System::operator<<(std::ostream &stream, const WeakPtr<T> &object_ptr)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::ostream\& | 데이터를 삽입할 출력 스트림. |
| object_ptr | const [WeakPtr](../weakptr/)\<T\>\& | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## System::operator<<(std::wostream\&, const WeakPtr\<T\>\&) 함수

스트림에 데이터를 삽입합니다.

```cpp
template<typename T> std::wostream & System::operator<<(std::wostream &stream, const WeakPtr<T> &object_ptr)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| stream | std::wostream\& | 데이터를 삽입할 출력 스트림. |
| object_ptr | const [WeakPtr](../weakptr/)\<T\>\& | [Data](../../system.data/)을 삽입합니다. |

### 반환 값

**stream**.

## 참조

* Typedef [Exception](../exception/)
* Typedef [SharedPtr](../sharedptr/)
* 클래스 [DateTime](../datetime/)
* 클래스 [DateTimeOffset](../datetimeoffset/)
* 클래스 [Decimal](../decimal/)
* 클래스 [Guid](../guid/)
* 클래스 [Nullable](../nullable/)
* 클래스 [Object](../object/)
* 클래스 [OperatingSystem](../operatingsystem/)
* 클래스 [String](../string/)
* 클래스 [TimeSpan](../timespan/)
* 클래스 [TypeInfo](../typeinfo/)
* 클래스 [Version](../version/)
* 클래스 [WeakPtr](../weakptr/)
* 네임스페이스 [System](../)
* 라이브러리 [Aspose.Slides](../../)