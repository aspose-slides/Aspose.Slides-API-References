---
title: Subtract()
second_title: Aspose.Slides for C++ API 참조
description: 현재 객체에서 지정된 시간 간격을 뺍니다.
type: docs
weight: 391
url: /ko/system/datetimeoffset/subtract/
---
## DateTimeOffset::Subtract(TimeSpan) const 메서드

현재 객체에서 지정된 시간 간격을 뺍니다.

```cpp
DateTimeOffset System::DateTimeOffset::Subtract(TimeSpan value) const
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | [TimeSpan](../../timespan/) | 빼야 할 값. |

### 반환 값

연산 결과.

## DateTimeOffset::Subtract(const DateTimeOffset\&) const 메서드

현재 객체에서 지정된 [DateTimeOffset](../) 값을 뺍니다.

```cpp
TimeSpan System::DateTimeOffset::Subtract(const DateTimeOffset &value) const
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | const [DateTimeOffset](../)\& | 빼야 할 값. |

### 반환 값

연산 결과.

## 또 보기

* 클래스 [DateTimeOffset](../)
* 클래스 [TimeSpan](../../timespan/)
* 네임스페이스 [System](../../)
* 라이브러리 [Aspose.Slides](../../../)