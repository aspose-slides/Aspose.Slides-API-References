---
title: Equals()
second_title: Aspose.Slides for C++ API 참조
description: 두 DateTimeOffset 객체가 동일한 시점을 나타내는지 확인합니다.
type: docs
weight: 365
url: /ko/system/datetimeoffset/equals/
---
## DateTimeOffset::Equals(const DateTimeOffset\&) const 메서드


두 [DateTimeOffset](../) 객체가 동일한 시점을 나타내는지 확인합니다.

```cpp
bool System::DateTimeOffset::Equals(const DateTimeOffset &other) const
```


### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/) 비교용. |

### 반환 값

true if current object is equal to **other**, otherwise - false.

## DateTimeOffset::Equals(const SharedPtr\<Object\>\&) const 메서드


두 [DateTimeOffset](../) 객체가 동일한 시점을 나타내는지 확인합니다.

```cpp
bool System::DateTimeOffset::Equals(const SharedPtr<Object> &obj) const
```


### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/) 비교용. |

### 반환 값

true if current object is equal to **obj**, otherwise - false.

## DateTimeOffset::Equals(const DateTimeOffset\&, const DateTimeOffset\&) 메서드


두 [DateTimeOffset](../) 객체가 동일한 시점을 나타내는지 확인합니다.

```cpp
static bool System::DateTimeOffset::Equals(const DateTimeOffset &first, const DateTimeOffset &second)
```


### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| first | const [DateTimeOffset](../)\& | 비교할 첫 번째 객체. |
| second | const [DateTimeOffset](../)\& | 비교할 두 번째 객체. |

### 반환 값

true if **first** is equal to **second**, otherwise - false.

## 참조

* Typedef [SharedPtr](../../sharedptr/)
* 클래스 [DateTimeOffset](../)
* 클래스 [Object](../../object/)
* 네임스페이스 [System](../../)
* Library [Aspose.Slides](../../../)