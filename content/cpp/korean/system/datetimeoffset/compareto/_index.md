---
title: CompareTo()
second_title: Aspose.Slides for C++ API 레퍼런스
description: 두 DateTimeOffset 개체를 비교합니다.
type: docs
weight: 352
url: /ko/system/datetimeoffset/compareto/
---
## DateTimeOffset::CompareTo(const DateTimeOffset\&) const 메서드

두 [DateTimeOffset](../) 개체를 비교합니다.

```cpp
int System::DateTimeOffset::CompareTo(const DateTimeOffset &other) const
```

### 인수

| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | 비교할 [Object](../../object/). |

### 반환값

- 1 - 현재 객체가 **other**보다 이전이면, 1 - 현재 객체가 **other**보다 이후이면, 0 - 현재 객체가 **other**와 동일하면.

## DateTimeOffset::CompareTo(const SharedPtr\<Object\>\&) const 메서드

두 [DateTimeOffset](../) 개체를 비교합니다.

```cpp
int System::DateTimeOffset::CompareTo(const SharedPtr<Object> &obj) const
```

### 인수

| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | 비교할 [Object](../../object/). |

### 반환값

- 1 - 현재 객체가 **obj**보다 이전이면, 1 - 현재 객체가 **obj**보다 이후이면, 0 - 현재 객체가 **obj**와 동일하면.

## 참고

* 타입 정의 [SharedPtr](../../sharedptr/)
* 클래스 [DateTimeOffset](../)
* 클래스 [Object](../../object/)
* 네임스페이스 [System](../../)
* 라이브러리 [Aspose.Slides](../../../)