---
title: EqualsExact()
second_title: Aspose.Slides for C++ API 참조
description: 두 DateTimeOffset 객체가 동일한 시점을 나타내고 동일한 오프셋을 가지고 있는지 확인합니다.
type: docs
weight: 378
url: /ko/system/datetimeoffset/equalsexact/
---
## DateTimeOffset::EqualsExact(const DateTimeOffset\&) const 메서드

두 [DateTimeOffset](../) 객체가 동일한 시점을 나타내고 동일한 오프셋을 가지고 있는지 확인합니다.

```cpp
bool System::DateTimeOffset::EqualsExact(const DateTimeOffset &other) const
```

### 매개변수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/)를 비교할 대상. |

### 반환 값

현재 객체가 **other**와 정확히 동일하면 true, 그렇지 않으면 false.

## DateTimeOffset::EqualsExact(const SharedPtr\<Object\>\&) const 메서드

두 [DateTimeOffset](../) 객체가 동일한 시점을 나타내고 동일한 오프셋을 가지고 있는지 확인합니다.

```cpp
bool System::DateTimeOffset::EqualsExact(const SharedPtr<Object> &obj) const
```

### 매개변수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/)를 비교할 대상. |

### 반환 값

현재 객체가 **obj**와 정확히 동일하면 true, 그렇지 않으면 false.

## 참조

* 타입정의 [SharedPtr](../../sharedptr/)
* 클래스 [DateTimeOffset](../)
* 클래스 [Object](../../object/)
* 네임스페이스 [System](../../)
* 라이브러리 [Aspose.Slides](../../../)