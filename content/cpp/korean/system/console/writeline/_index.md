---
title: WriteLine()
second_title: Aspose.Slides for C++ API 레퍼런스
description: 현재 줄 구분자를 표준 출력 스트림에 출력합니다.
type: docs
weight: 14
url: /ko/system/console/writeline/
---
## Console::WriteLine() 메서드

현재 줄 구분자를 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::WriteLine()
```

## Console::WriteLine(const SharedPtr\<T\>\&) 메서드

지정된 객체의 문자열 표현을 현재 줄 구분자와 함께 표준 출력 스트림에 출력합니다.

```cpp
template<class T> static void System::Console::WriteLine(const SharedPtr<T> &object)
```

### 템플릿 매개변수

| 매개변수 | 설명 |
| --- | --- |
| T | 출력할 객체의 유형 |

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| object | const [SharedPtr](../../sharedptr/)\<T\>\& | [Object](../../object/) 출력 |

## Console::WriteLine(bool) 메서드

bool 값의 문자열 표현을 현재 줄 구분자와 함께 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::WriteLine(bool value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | **bool** | 출력할 값 |

## Console::WriteLine(char_t) 메서드

지정된 문자 값을 현재 줄 구분자와 함께 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::WriteLine(char_t value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | char_t | 출력할 값 |

## Console::WriteLine(const ArrayPtr\<char_t\>\&) 메서드

지정된 문자 배열의 문자열 표현을 현재 줄 구분자와 함께 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | 출력할 배열 |

## Console::WriteLine(const Decimal\&) 메서드

[Decimal](../../decimal/) 값의 문자열 표현을 현재 줄 구분자와 함께 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::WriteLine(const Decimal &value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | 출력할 값 |

## Console::WriteLine(double) 메서드

double-precision 부동 소수점 값의 문자열 표현을 현재 줄 구분자와 함께 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::WriteLine(double value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | **double** | 출력할 값 |

## Console::WriteLine(float) 메서드

single-precision 부동 소수점 값의 문자열 표현을 현재 줄 구분자와 함께 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::WriteLine(float value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | **float** | 출력할 값 |

## Console::WriteLine(int32_t) 메서드

32비트 정수 값의 문자열 표현을 현재 줄 구분자와 함께 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::WriteLine(int32_t value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | **int32_t** | 출력할 값 |

## Console::WriteLine(int64_t) 메서드

64비트 정수 값의 문자열 표현을 현재 줄 구분자와 함께 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::WriteLine(int64_t value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | **int64_t** | 출력할 값 |

## Console::WriteLine(const String\&) 메서드

지정된 문자열 객체를 현재 줄 구분자와 함께 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::WriteLine(const String &value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | const [String](../../string/)\& | 출력할 문자열 객체 |

## Console::WriteLine(const char_t *) 메서드

지정된 C 문자열을 현재 줄 구분자와 함께 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::WriteLine(const char_t *value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | const char_t * | 출력할 C 문자열 |

## Console::WriteLine(const TypeInfo\&) 메서드

[TypeInfo](../../typeinfo/) 값의 문자열 표현을 현재 줄 구분자와 함께 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::WriteLine(const TypeInfo &value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | 출력할 값 |

## Console::WriteLine(uint32_t) 메서드

unsigned 32비트 정수 값의 문자열 표현을 현재 줄 구분자와 함께 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::WriteLine(uint32_t value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | **uint32_t** | 출력할 값 |

## Console::WriteLine(uint64_t) 메서드

unsigned 64비트 정수 값의 문자열 표현을 현재 줄 구분자와 함께 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::WriteLine(uint64_t value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | **uint64_t** | 출력할 값 |

## Console::WriteLine(const ArrayPtr\<char_t\>\&, int, int) 메서드

지정된 문자 배열의 지정된 범위에 대한 문자열 표현을 현재 줄 구분자와 함께 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer, int index, int count)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | 문자 배열 |
| index | int | 출력 범위가 시작되는 배열의 인덱스 |
| count | int | 출력 범위에 포함되는 요소 수 |

## Console::WriteLine(const Exception\&) 메서드

지정된 Exception 객체의 문자열 표현을 현재 줄 구분자와 함께 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::WriteLine(const Exception &e)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| e | const [Exception](../../exception/)\& | 출력할 값 |

## Console::WriteLine(const String\&, Args\&&...) 메서드

지정된 형식에 따라 지정된 인수를 포맷한 문자열 표현을 현재 줄 구분자와 함께 표준 출력 스트림에 출력합니다.

```cpp
template<class...> static void System::Console::WriteLine(const String &format, Args &&... args)
```

### 템플릿 매개변수

| 매개변수 | 설명 |
| --- | --- |
| The | 출력할 값들의 타입 |

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| format | const [String](../../string/)\& | 문자열 형식 |
| args | Args\&&... | 출력할 값들 |

## Console::WriteLine(const char *) 메서드




```cpp
static void System::Console::WriteLine(const char *)=delete
```

## 참고

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Typedef [Exception](../../exception/)
* 클래스 [Console](../)
* 클래스 [Decimal](../../decimal/)
* 클래스 [String](../../string/)
* 클래스 [TypeInfo](../../typeinfo/)
* 네임스페이스 [System](../../)
* 라이브러리 [Aspose.Slides](../../../)