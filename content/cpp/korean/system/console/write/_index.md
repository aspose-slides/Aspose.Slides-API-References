---
title: Write()
second_title: Aspose.Slides for C++ API 참조
description: 지정된 객체의 문자열 표현을 표준 출력 스트림에 출력합니다.
type: docs
weight: 1
url: /ko/system/console/write/
---
## Console::Write(const SharedPtr\<T\>\&) 메서드

지정된 객체의 문자열 표현을 표준 출력 스트림에 출력합니다.

```cpp
template<class T> static void System::Console::Write(const SharedPtr<T> &object)
```

### 템플릿 매개변수

| 매개변수 | 설명 |
| --- | --- |
| T | 출력할 객체의 유형 |

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| object | const [SharedPtr](../../sharedptr/)\<T\>\& | [Object](../../object/)을(를) 출력 |

## Console::Write(bool) 메서드

bool 값의 문자열 표현을 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::Write(bool value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | **bool** | 출력할 값 |

## Console::Write(char_t) 메서드

지정된 문자 값을 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::Write(char_t value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | char_t | 출력할 값 |

## Console::Write(const ArrayPtr\<char_t\>\&) 메서드

지정된 문자 배열의 문자열 표현을 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::Write(const ArrayPtr<char_t> &buffer)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | 출력할 배열 |

## Console::Write(const Decimal\&) 메서드

[Decimal](../../decimal/) 값의 문자열 표현을 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::Write(const Decimal &value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | 출력할 값 |

## Console::Write(double) 메서드

double 정밀 부동소수점 값의 문자열 표현을 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::Write(double value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | **double** | 출력할 값 |

## Console::Write(float) 메서드

single 정밀 부동소수점 값의 문자열 표현을 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::Write(float value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | **float** | 출력할 값 |

## Console::Write(int32_t) 메서드

32비트 정수 값의 문자열 표현을 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::Write(int32_t value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | **int32_t** | 출력할 값 |

## Console::Write(int64_t) 메서드

64비트 정수 값의 문자열 표현을 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::Write(int64_t value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | **int64_t** | 출력할 값 |

## Console::Write(const String\&) 메서드

지정된 문자열 객체를 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::Write(const String &value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | const [String](../../string/)\& | 출력할 문자열 객체 |

## Console::Write(const char_t *) 메서드

지정된 C 문자열을 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::Write(const char_t *value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | const char_t * | 출력할 C 문자열 |

## Console::Write(const TypeInfo\&) 메서드

[TypeInfo](../../typeinfo/) 값의 문자열 표현을 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::Write(const TypeInfo &value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | 출력할 값 |

## Console::Write(uint32_t) 메서드

부호 없는 32비트 정수 값의 문자열 표현을 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::Write(uint32_t value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | **uint32_t** | 출력할 값 |

## Console::Write(uint64_t) 메서드

부호 없는 64비트 정수 값의 문자열 표현을 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::Write(uint64_t value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | **uint64_t** | 출력할 값 |

## Console::Write(const ArrayPtr\<char_t\>\&, int32_t, int32_t) 메서드

지정된 문자 배열의 지정된 범위에 대한 문자열 표현을 표준 출력 스트림에 출력합니다.

```cpp
static void System::Console::Write(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | 문자 배열 |
| index | **int32_t** | 출력 범위가 시작되는 배열 내 인덱스 |
| count | **int32_t** | 출력 범위의 요소 수 |

## Console::Write(const String\&, Args\&&...) 메서드

지정된 형식에 따라 지정된 인수를 포맷팅한 문자열 표현을 표준 출력 스트림에 출력합니다.

```cpp
template<class...> static void System::Console::Write(const String &format, Args &&... args)
```

### 템플릿 매개변수

| 매개변수 | 설명 |
| --- | --- |
| The | 출력할 값들의 유형 |

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| format | const [String](../../string/)\& | 문자열 형식 |
| args | Args\&&... | 출력할 값들 |

## Console::Write(const char *) 메서드




```cpp
static void System::Console::Write(const char *)=delete
```

## 참고

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* 클래스 [Console](../)
* 클래스 [Decimal](../../decimal/)
* 클래스 [String](../../string/)
* 클래스 [TypeInfo](../../typeinfo/)
* 네임스페이스 [System](../../)
* 라이브러리 [Aspose.Slides](../../../)