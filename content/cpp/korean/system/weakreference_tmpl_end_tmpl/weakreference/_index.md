---
title: WeakReference()
second_title: Aspose.Slides for C++ API 레퍼런스
description: 기본 생성자.
type: docs
weight: 1
url: /ko/system/weakreference_tmpl_end_tmpl/weakreference/
---
## WeakReference<>::WeakReference() 메서드

기본 생성자.

```cpp
System::WeakReference<>::WeakReference()
```

## WeakReference<>::WeakReference(std::nullptr_t) 메서드

nullptr에서 생성자.

```cpp
System::WeakReference<>::WeakReference(std::nullptr_t)
```

## WeakReference<>::WeakReference(const SmartPtr\<Object\>\&) 메서드

WeakReference 클래스를 새 인스턴스로 초기화하며, 지정된 개체를 참조합니다.

```cpp
System::WeakReference<>::WeakReference(const SmartPtr<Object> &data)
```

### 인수

| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<[Object](../../object/)\>\& | [Object](../../object/)을 저장합니다. |

## WeakReference<>::WeakReference(const SmartPtr\<Object\>\&, bool) 메서드

WeakReference 클래스를 새 인스턴스로 초기화하며, 지정된 개체를 참조합니다.

```cpp
System::WeakReference<>::WeakReference(const SmartPtr<Object> &data, bool trackResurrection)
```

### 인수

| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<[Object](../../object/)\>\& | [Object](../../object/)을 저장합니다. |
| trackResurrection | **bool** | 무시됨. |

## 참조

* 클래스 [WeakReference<>](../)
* 클래스 [SmartPtr](../../smartptr/)
* 클래스 [Object](../../object/)
* 네임스페이스 [System](../../)
* 라이브러리 [Aspose.Slides](../../../)