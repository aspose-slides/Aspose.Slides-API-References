---
title: FromUtf8()
second_title: Aspose.Slides for C++ API 레퍼런스
description: utf8 문자열에서 String을 생성합니다.
type: docs
weight: 898
url: /ko/system/string/fromutf8/
---
## String::FromUtf8(const char *) 메서드

[String](../)를 utf8 문자열에서 생성합니다.

```cpp
static String System::String::FromUtf8(const char *utf8)
```

### 인수

| Parameter | Type | Description |
| --- | --- | --- |
| utf8 | const char * | utf8 코드 페이지를 사용하여 인코딩된 null-terminated 문자열에 대한 포인터. |

### 반환 값

[String](../) 객체는 전달된 문자열을 나타냅니다.

## String::FromUtf8(const char *, int) 메서드

[String](../)를 utf8 문자열에서 생성합니다.

```cpp
static String System::String::FromUtf8(const char *utf8, int len)
```

### 인수

| Parameter | Type | Description |
| --- | --- | --- |
| utf8 | const char * | utf8 코드 페이지를 사용하여 인코딩된 문자열에 대한 포인터. |
| len | int | 처리할 문자 수. |

### 반환 값

[String](../) 객체는 전달된 문자열을 나타냅니다.

## String::FromUtf8(const uint8_t *) 메서드

[String](../)를 utf8 문자열에서 생성합니다.

```cpp
static String System::String::FromUtf8(const uint8_t *utf8)
```

### 인수

| Parameter | Type | Description |
| --- | --- | --- |
| utf8 | const **uint8_t** * | utf8 코드 페이지를 사용하여 인코딩된 null-terminated 문자열에 대한 포인터. |

### 반환 값

[String](../) 객체는 전달된 문자열을 나타냅니다.

## String::FromUtf8(const std::string\&) 메서드

[String](../)를 utf8 문자열에서 생성합니다.

```cpp
static String System::String::FromUtf8(const std::string &utf8)
```

### 인수

| Parameter | Type | Description |
| --- | --- | --- |
| utf8 | const std::string\& | utf8 코드 페이지를 사용하여 인코딩된 문자열에 대한 포인터. |

### 반환 값

[String](../) 객체는 전달된 문자열을 나타냅니다.

## 참고

* Class [String](../)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)