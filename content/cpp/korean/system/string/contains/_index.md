---
title: Contains()
second_title: Aspose.Slides for C++ API 레퍼런스
description: 현재 문자열에 str이 하위 문자열인지 확인합니다.
type: docs
weight: 404
url: /ko/system/string/contains/
---
## String::Contains(const String\&) const 메서드

현재 문자열에 str이 하위 문자열인지 확인합니다.

```cpp
bool System::String::Contains(const String &str) const
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| str | const [String](../)\& | 조회 문자열. |

### 반환값

하위 문자열이 있으면 true, 없으면 false.

## String::Contains(char16_t) const 메서드

문자열에 지정된 문자가 포함되어 있는지 확인합니다.

```cpp
bool System::String::Contains(char16_t value) const
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | char16_t | 조회 문자. |

### 반환값

문자가 있으면 true, 없으면 false.

## 참고

* 클래스 [String](../)
* 네임스페이스 [System](../../)
* 라이브러리 [Aspose.Slides](../../../)