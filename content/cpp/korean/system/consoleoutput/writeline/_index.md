---
title: WriteLine()
second_title: Aspose.Slides for C++ API 참조
description: 현재 객체가 나타내는 출력 스트림에 현재 라인 종결자를 출력합니다.
type: docs
weight: 27
url: /ko/system/consoleoutput/writeline/
---
## ConsoleOutput::WriteLine() 메서드

현재 객체가 나타내는 출력 스트림에 현재 줄 구분자를 출력합니다.

```cpp
void System::ConsoleOutput::WriteLine() override
```
## ConsoleOutput::WriteLine(const SharedPtr\<Object\>\&) 메서드

지정된 객체의 문자열 표현을 현재 행 구분자와 함께 현재 객체가 나타내는 출력 스트림에 출력합니다.

```cpp
void System::ConsoleOutput::WriteLine(const SharedPtr<Object> &value) override
```


### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | 출력할 객체 |

## ConsoleOutput::WriteLine(bool) 메서드

지정된 bool 값의 문자열 표현을 현재 행 구분자와 함께 현재 객체가 나타내는 출력 스트림에 출력합니다.

```cpp
void System::ConsoleOutput::WriteLine(bool value) override
```


### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | **bool** | 출력할 값 |

## ConsoleOutput::WriteLine(char_t) 메서드

지정된 문자 값을 현재 행 구분자와 함께 현재 객체가 나타내는 출력 스트림에 출력합니다.

```cpp
void System::ConsoleOutput::WriteLine(char_t value) override
```


### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | char_t | 출력할 값 |

## ConsoleOutput::WriteLine(Decimal) 메서드

[Decimal](../../decimal/) 값의 문자열 표현을 현재 행 구분자와 함께 현재 객체가 나타내는 출력 스트림에 출력합니다.

```cpp
void System::ConsoleOutput::WriteLine(Decimal value) override
```


### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | 출력할 값 |

## ConsoleOutput::WriteLine(double) 메서드

배정밀도 부동 소수점 값의 문자열 표현을 현재 행 구분자와 함께 현재 객체가 나타내는 출력 스트림에 출력합니다.

```cpp
void System::ConsoleOutput::WriteLine(double value) override
```


### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | **double** | 출력할 값 |

## ConsoleOutput::WriteLine(int) 메서드

32비트 정수 값의 문자열 표현을 현재 행 구분자와 함께 현재 객체가 나타내는 출력 스트림에 출력합니다.

```cpp
void System::ConsoleOutput::WriteLine(int value) override
```


### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | int | 출력할 값 |

## ConsoleOutput::WriteLine(int64_t) 메서드

64비트 정수 값의 문자열 표현을 현재 행 구분자와 함께 현재 객체가 나타내는 출력 스트림에 출력합니다.

```cpp
void System::ConsoleOutput::WriteLine(int64_t value) override
```


### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | **int64_t** | 출력할 값 |

## ConsoleOutput::WriteLine(float) 메서드

단정밀 부동 소수점 값의 문자열 표현을 현재 행 구분자와 함께 현재 객체가 나타내는 출력 스트림에 출력합니다.

```cpp
void System::ConsoleOutput::WriteLine(float value) override
```


### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | **float** | 출력할 값 |

## ConsoleOutput::WriteLine(const String\&) 메서드

지정된 문자열 객체를 현재 행 구분자와 함께 현재 객체가 나타내는 출력 스트림에 출력합니다.

```cpp
void System::ConsoleOutput::WriteLine(const String &value) override
```


### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | const [String](../../string/)\& | 출력할 문자열 객체 |

## ConsoleOutput::WriteLine(uint32_t) 메서드

부호 없는 32비트 정수 값의 문자열 표현을 현재 행 구분자와 함께 현재 객체가 나타내는 출력 스트림에 출력합니다.

```cpp
void System::ConsoleOutput::WriteLine(uint32_t value) override
```


### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | **uint32_t** | 출력할 값 |

## ConsoleOutput::WriteLine(uint64_t) 메서드

부호 없는 64비트 정수 값의 문자열 표현을 현재 행 구분자와 함께 현재 객체가 나타내는 출력 스트림에 출력합니다.

```cpp
void System::ConsoleOutput::WriteLine(uint64_t value) override
```


### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | **uint64_t** | 출력할 값 |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&) 메서드

지정된 문자 배열의 문자열 표현을 현재 행 구분자와 함께 현재 객체가 나타내는 출력 스트림에 출력합니다.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer) override
```


### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | 출력할 배열 |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&, int32_t, int32_t) 메서드

지정된 문자 배열의 값 범위에 대한 문자열 표현을 현재 행 구분자와 함께 현재 객체가 나타내는 출력 스트림에 출력합니다.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```


### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | 출력할 값을 포함하는 배열 |
| index | **int32_t** | 출력할 요소 범위가 시작되는 인덱스 |
| count | **int32_t** | 출력할 요소 범위의 요소 수 |

## ConsoleOutput::WriteLine(const char_t *) 메서드

지정된 C 문자열을 현재 행 구분자와 함께 현재 객체가 나타내는 출력 스트림에 출력합니다.

```cpp
void System::ConsoleOutput::WriteLine(const char_t *value) override
```


### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | const char_t * | 출력할 C 문자열 |

## ConsoleOutput::WriteLine(const TypeInfo\&) 메서드

지정된 [TypeInfo](../../typeinfo/) 객체의 문자열 표현을 현재 행 구분자와 함께 현재 객체가 나타내는 출력 스트림에 출력합니다.

```cpp
void System::ConsoleOutput::WriteLine(const TypeInfo &value) override
```


### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | 출력할 [TypeInfo](../../typeinfo/) 객체 |

## ConsoleOutput::WriteLine(const char *) 메서드




```cpp
void System::ConsoleOutput::WriteLine(const char *)=delete
```

## 참조

* typedef [SharedPtr](../../sharedptr/)
* typedef [ArrayPtr](../../arrayptr/)
* 클래스 [ConsoleOutput](../)
* 클래스 [Object](../../object/)
* 클래스 [Decimal](../../decimal/)
* 클래스 [String](../../string/)
* 클래스 [TypeInfo](../../typeinfo/)
* 네임스페이스 [System](../../)
* 라이브러리 [Aspose.Slides](../../../)