---
title: Increment()
second_title: Aspose.Slides for C++ API 레퍼런스
description: 값을 원자적으로 증가시킵니다.
type: docs
weight: 1
url: /ko/system.threading/interlocked/increment/
---
## Interlocked::Increment(int32_t\&) 메서드

값을 원자적으로 증가시킵니다.

```cpp
static int32_t System::Threading::Interlocked::Increment(int32_t &location)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| location | **int32_t**\& | 증가시킬 변수 참조. |

### 반환값

증가 직후 변수의 값.

## Interlocked::Increment(int64_t\&) 메서드

값을 원자적으로 증가시킵니다.

```cpp
static int64_t System::Threading::Interlocked::Increment(int64_t &location)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| location | **int64_t**\& | 증가시킬 변수 참조. |

### 반환값

증가 직후 변수의 값.

## 관련 항목

* 클래스 [Interlocked](../)
* 네임스페이스 [System::Threading](../../)
* 라이브러리 [Aspose.Slides](../../../)