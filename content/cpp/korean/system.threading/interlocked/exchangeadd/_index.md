---
title: ExchangeAdd()
second_title: Aspose.Slides for C++ API 레퍼런스
description: 교환-추가 절차를 통해 값을 원자적으로 증가시킵니다.
type: docs
weight: 53
url: /ko/system.threading/interlocked/exchangeadd/
---
## Interlocked::ExchangeAdd(int32_t\&, int32_t) method

값을 교환-추가 절차를 통해 원자적으로 증가시킵니다.

```cpp
static int32_t System::Threading::Interlocked::ExchangeAdd(int32_t &location1, int32_t value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| location1 | **int32_t**\& | 증가시킬 변수에 대한 참조. |
| value | **int32_t** | **location1**에 더할 값. |

### 반환 값

증가된 직후 변수의 값.

## Interlocked::ExchangeAdd(int64_t\&, int64_t) method

값을 교환-추가 절차를 통해 원자적으로 증가시킵니다.

```cpp
static int64_t System::Threading::Interlocked::ExchangeAdd(int64_t &location1, int64_t value)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| location1 | **int64_t**\& | 증가시킬 변수에 대한 참조. |
| value | **int64_t** | **location1**에 더할 값. |

### 반환 값

증가된 직후 변수의 값.

## 관련 항목

* 클래스 [Interlocked](../)
* 네임스페이스 [System::Threading](../../)
* 라이브러리 [Aspose.Slides](../../../)