---
title: "System::Text::RegularExpressions"
second_title: Aspose.Slides for C++ API 레퍼런스
description: 
type: docs
weight: 989
url: /ko/system.text.regularexpressions/
---
## 클래스

| 클래스 | 설명 |
| --- | --- |
| [Capture](./capture/) | 단일 하위 표현식 매칭의 결과. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수를 사용하여 할당해야 합니다. 스택에 이 유형의 인스턴스를 만들거나 operator new를 사용하지 마세요. 그렇지 않으면 런타임 오류 및/또는 단언 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 래핑하고, 해당 포인터를 인자로 함수에 전달하세요. |
| [CaptureCollection](./capturecollection/) | 단일 캡처 그룹에 의해 수행된 캡처 목록. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수를 사용하여 할당해야 합니다. 스택에 이 유형의 인스턴스를 만들거나 operator new를 사용하지 마세요. 그렇지 않으면 런타임 오류 및/또는 단언 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 래핑하고, 해당 포인터를 인자로 함수에 전달하세요. |
| [Group](./group/) | 단일 캡처 그룹에 의해 수행된 매칭 결과. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수를 사용하여 할당해야 합니다. 스택에 이 유형의 인스턴스를 만들거나 operator new를 사용하지 마세요. 그렇지 않으면 런타임 오류 및/또는 단언 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 래핑하고, 해당 포인터를 인자로 함수에 전달하세요. |
| [GroupCollection](./groupcollection/) | 단일 매치 내의 캡처 그룹 목록. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수를 사용하여 할당해야 합니다. 스택에 이 유형의 인스턴스를 만들거나 operator new를 사용하지 마세요. 그렇지 않으면 런타임 오류 및/또는 단언 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 래핑하고, 해당 포인터를 인자로 함수에 전달하세요. |
| [GroupCollectionPtr](./groupcollectionptr/) | [Group](./group/) 컬렉션 포인터. 이 유형은 다른 객체의 삭제를 관리하는 포인터입니다. 스택에 할당하고 값이나 const 레퍼런스로 함수를 호출할 때 전달해야 합니다. |
| [Match](./match/) | 문자열에 대한 정규식 매치 [Single](../system/single/). 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수를 사용하여 할당해야 합니다. 스택에 이 유형의 인스턴스를 만들거나 operator new를 사용하지 마세요. 그렇지 않으면 런타임 오류 및/또는 단언 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 래핑하고, 해당 포인터를 인자로 함수에 전달하세요. |
| [MatchCollection](./matchcollection/) | 정규식을 문자열에 반복 적용하여 얻은 매치 컬렉션. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수를 사용하여 할당해야 합니다. 스택에 이 유형의 인스턴스를 만들거나 operator new를 사용하지 마세요. 그렇지 않으면 런타임 오류 및/또는 단언 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 래핑하고, 해당 포인터를 인자로 함수에 전달하세요. |
| [Regex](./regex/) | C#와 유사한 구문을 따르는 정규식. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수를 사용하여 할당해야 합니다. 스택에 이 유형의 인스턴스를 만들거나 operator new를 사용하지 마세요. 그렇지 않으면 런타임 오류 및/또는 단언 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 래핑하고, 해당 포인터를 인자로 함수에 전달하세요. |

## 함수

| 함수 | 설명 |
| --- | --- |
| [ASPOSECPP_3RD_PARTY_UNCOPYBALE_TYPE_HOLDER](./asposecpp_3rd_party_uncopybale_type_holder/)(Detail::MatchHolder, MatchHolder, sizeof(Detail::DummyMatchHolder), Detail::DummyMatchHolder, MatchHolderAlias) | MatchHolder 클래스를 포함하지 않고 PCRE2와 함께 보유하기 위한 래퍼. |

## 열거형

| 열거형 | 설명 |
| --- | --- |
| [RegexOptions](./regexoptions/) | [Regex](./regex/) 옵션. |

## 타입정의

| 타입정의 | 설명 |
| --- | --- |
| [UStringPtr](./ustringptr/) | 복사를 방지하기 위한 공유 UnicodeString. |
| [CapturePtr](./captureptr/) | 단일 캡처 객체에 대한 포인터. |
| [CaptureCollectionPtr](./capturecollectionptr/) | 캡처 컬렉션에 대한 포인터. |
| [GroupPtr](./groupptr/) | 그룹에 대한 포인터. |
| [RegexPtr](./regexptr/) | [Regex](./regex/) 포인터. |
| [MatchPtr](./matchptr/) | [Match](./match/) 포인터. |
| [MatchCollectionPtr](./matchcollectionptr/) | [Match](./match/) 컬렉션 포인터. |
| [MatchEvaluator](./matchevaluator/) | 매치를 평가하기 위한 대리자 타입. |