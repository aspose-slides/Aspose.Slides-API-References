---
title: get_MulticastLoopback()
second_title: Aspose.Slides for C++ API 레퍼런스
description: 소켓이 송신 멀티캐스트 패킷을 수신하는지 여부를 나타내는 값을 가져옵니다.
type: docs
weight: 404
url: /ko/system.net.sockets/socket/get_multicastloopback/
---
## Socket::get_MulticastLoopback() 메서드

소켓이 송신 멀티캐스트 패킷을 수신하는지 여부를 나타내는 값을 가져옵니다.

```cpp
bool System::Net::Sockets::Socket::get_MulticastLoopback()
```

### 반환값

소켓이 송신 멀티캐스트 패킷을 수신하는지 여부를 나타내는 값.

## 참조

* 클래스 [Socket](../)
* 네임스페이스 [System::Net::Sockets](../../)
* 라이브러리 [Aspose.Slides](../../../)