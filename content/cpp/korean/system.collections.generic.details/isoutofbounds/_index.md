---
title: IsOutOfBounds()
second_title: Aspose.Slides for C++ API 레퍼런스
description: 인덱스가 컨테이너 크기를 제외한 범위를 벗어나는지 확인합니다.
type: docs
weight: 209
url: /ko/system.collections.generic.details/isoutofbounds/
---
## System::Collections::Generic::Details::IsOutOfBounds(int, const Container\&) 함수

인덱스가 컨테이너 크기를 제외한 범위를 벗어나는지 확인합니다.

```cpp
template<typename Container> bool System::Collections::Generic::Details::IsOutOfBounds(int idx, const Container &container)
```

### 템플릿 매개변수

| 매개변수 | 설명 |
| --- | --- |
| Container | 컨테이너 유형. |

### 인수

| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| idx | int | [Index](../../system/index/). |
| container | const Container\& | 인덱스를 확인할 컨테이너. |

### 반환값

인덱스가 컨테이너 범위를 벗어나면 true, 그렇지 않으면 false.

## System::Collections::Generic::Details::IsOutOfBounds(std::int64_t, const Container\&) 함수

인덱스가 컨테이너 크기를 제외한 범위를 벗어나는지 확인합니다.

```cpp
template<typename Container> bool System::Collections::Generic::Details::IsOutOfBounds(std::int64_t idx, const Container &container)
```

### 템플릿 매개변수

| 매개변수 | 설명 |
| --- | --- |
| Container | 컨테이너 유형. |

### 인수

| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| idx | std::int64_t | [Index](../../system/index/). |
| container | const Container\& | 인덱스를 확인할 컨테이너. |

### 반환값

인덱스가 컨테이너 범위를 벗어나면 true, 그렇지 않으면 false.

## 참고

* 네임스페이스 [System::Collections::Generic::Details](../)
* 라이브러리 [Aspose.Slides](../../)