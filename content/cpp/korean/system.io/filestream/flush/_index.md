---
title: Flush()
second_title: Aspose.Slides for C++ API 참조
description: 이 스트림의 버퍼를 비우고 모든 버퍼링된 데이터를 기본 파일에 씁니다.
type: docs
weight: 144
url: /ko/system.io/filestream/flush/
---
## FileStream::Flush() 메서드

이 스트림의 버퍼를 비우고 모든 버퍼링된 데이터를 기본 파일에 씁니다.

```cpp
void System::IO::FileStream::Flush() override
```

## FileStream::Flush(bool) 메서드

이 스트림의 버퍼를 비우고 모든 버퍼링된 데이터를 기본 파일에 씁니다. 메서드 [Flush()](./)의 동의어입니다.

```cpp
void System::IO::FileStream::Flush(bool flush_to_disk)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| flush_to_disk | **bool** | 중간 버퍼를 모두 플러시하려면 true, 그렇지 않으면 false. |

## 참조

* 클래스 [FileStream](../)
* 네임스페이스 [System::IO](../../)
* 라이브러리 [Aspose.Slides](../../../)