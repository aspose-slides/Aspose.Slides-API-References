---
title: NetworkToHostOrder()
second_title: Aspose.Slides for C++ API 참조
description: 지정된 네트워크 바이트 순서를 해당 호스트 바이트 순서로 변환합니다.
type: docs
weight: 261
url: /ko/system.net/ipaddress/networktohostorder/
---
## IPAddress::NetworkToHostOrder(int64_t) 메서드

지정된 네트워크 바이트 순서를 해당 호스트 바이트 순서로 변환합니다.

```cpp
static int64_t System::Net::IPAddress::NetworkToHostOrder(int64_t network)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| network | **int64_t** | 변환할 값입니다. |

### 반환 값

해당 호스트 바이트 순서.

## IPAddress::NetworkToHostOrder(int32_t) 메서드

지정된 네트워크 바이트 순서를 해당 호스트 바이트 순서로 변환합니다.

```cpp
static int32_t System::Net::IPAddress::NetworkToHostOrder(int32_t network)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| network | **int32_t** | 변환할 값입니다. |

### 반환 값

해당 호스트 바이트 순서.

## IPAddress::NetworkToHostOrder(int16_t) 메서드

지정된 네트워크 바이트 순서를 해당 호스트 바이트 순서로 변환합니다.

```cpp
static int16_t System::Net::IPAddress::NetworkToHostOrder(int16_t network)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| network | **int16_t** | 변환할 값입니다. |

### 반환 값

해당 호스트 바이트 순서.

## 참조

* 클래스 [IPAddress](../)
* 네임스페이스 [System::Net](../../)
* 라이브러리 [Aspose.Slides](../../../)