---
title: HostToNetworkOrder()
second_title: Aspose.Slides for C++ API 레퍼런스
description: 지정된 호스트 바이트 순서를 해당 네트워크 바이트 순서로 변환합니다.
type: docs
weight: 248
url: /ko/system.net/ipaddress/hosttonetworkorder/
---
## IPAddress::HostToNetworkOrder(int64_t) 메서드

지정된 호스트 바이트 순서를 해당 네트워크 바이트 순서로 변환합니다.

```cpp
static int64_t System::Net::IPAddress::HostToNetworkOrder(int64_t host)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| host | **int64_t** | 변환할 값. |

### 반환값

해당 네트워크 바이트 순서.

## IPAddress::HostToNetworkOrder(int32_t) 메서드

지정된 호스트 바이트 순서를 해당 네트워크 바이트 순서로 변환합니다.

```cpp
static int32_t System::Net::IPAddress::HostToNetworkOrder(int32_t host)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| host | **int32_t** | 변환할 값. |

### 반환값

해당 네트워크 바이트 순서.

## IPAddress::HostToNetworkOrder(int16_t) 메서드

지정된 호스트 바이트 순서를 해당 네트워크 바이트 순서로 변환합니다.

```cpp
static int16_t System::Net::IPAddress::HostToNetworkOrder(int16_t host)
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| host | **int16_t** | 변환할 값. |

### 반환값

해당 네트워크 바이트 순서.

## 참고

* 클래스 [IPAddress](../)
* 네임스페이스 [System::Net](../../)
* 라이브러리 [Aspose.Slides](../../../)