---
title: get_IsOnlyDocumentPropertiesLoaded()
second_title: Aspose.Slides for C++ API 레퍼런스
description: 이 속성은 프레젠테이션 파일이 비밀번호로 보호되고 해당 파일의 문서 속성이 공개된 경우에 의미가 있습니다. true 값은 암호를 사용하지 않고 암호화된 프레젠테이션 파일에서 문서 속성만 로드된다는 것을 의미합니다. false 값은 올바른 비밀번호를 사용하여 전체 암호화된 프레젠테이션이 로드되며, 문서 속성만 로드되는 것이 아니라는 것을 의미합니다. 프레젠테이션이 암호화되지 않은 경우 속성값은 항상 false 입니다. 암호화된 파일의 문서 속성이 공개되지 않은 경우에도 속성값은 항상 false 입니다. Presentation.EncryptDocumentProperties 가 true이면 IsOnlyDocumentPropertiesLoaded 속성값은 항상 false 입니다. 읽기 전용 bool.
type: docs
weight: 40
url: /ko/aspose.slides/protectionmanager/get_isonlydocumentpropertiesloaded/
---
## ProtectionManager::get_IsOnlyDocumentPropertiesLoaded() 메서드

프레젠테이션 파일이 암호로 보호되고 해당 파일의 문서 속성이 공개된 경우에 이 속성이 의미가 있습니다. true 값은 암호를 사용하지 않고 암호화된 프레젠테이션 파일에서 문서 속성만 로드된다는 것을 의미합니다. false 값은 올바른 암호를 사용하여 전체 암호화된 프레젠테이션이 로드되고, 문서 속성만이 로드되는 것이 아니라는 것을 의미합니다. 프레젠테이션이 암호화되지 않은 경우 속성 값은 항상 false 입니다. 암호화된 파일의 문서 속성이 공개되지 않은 경우에도 속성 값은 항상 false 입니다. Presentation.EncryptDocumentProperties 가 true이면 IsOnlyDocumentPropertiesLoaded 속성 값은 항상 false 입니다. 읽기 전용 **bool**.

```cpp
bool Aspose::Slides::ProtectionManager::get_IsOnlyDocumentPropertiesLoaded() override
```

## 참고

* 클래스 [ProtectionManager](../)
* 네임스페이스 [Aspose::Slides](../../)
* 라이브러리 [Aspose.Slides](../../../)