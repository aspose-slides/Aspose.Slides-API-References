---
title: rend()
second_title: Aspose.Slides for C++ API 레퍼런스
description: 컬렉션 시작 이전에 존재하지 않는 요소에 대한 역방향 반복자를 가져옵니다.
type: docs
weight: 92
url: /ko/system.collections.generic/list/rend/
---
## List::rend() 메서드

컬렉션 시작 이전에 존재하지 않는 요소에 대한 역방향 반복자를 가져옵니다.

```cpp
reverse_iterator System::Collections::Generic::List<T>::rend() noexcept
```

### 반환값

리스트의 첫 번째 요소 앞에 있는 이론적 요소를 가리키는 역방향 반복자(역방향에서 마지막).

## List::rend() const 메서드

const 한정 컬렉션 시작 이전에 존재하지 않는 요소에 대한 역방향 반복자를 가져옵니다.

```cpp
const_reverse_iterator System::Collections::Generic::List<T>::rend() const noexcept
```

### 반환값

const 한정 리스트의 첫 번째 요소 앞에 있는 이론적 요소를 가리키는 역방향 반복자(역방향에서 마지막).

## 참고

* Typedef [reverse_iterator](../reverse_iterator/)
* Typedef [const_reverse_iterator](../const_reverse_iterator/)
* Class [List](../)
* Namespace [System::Collections::Generic](../../)
* Library [Aspose.Slides](../../../)