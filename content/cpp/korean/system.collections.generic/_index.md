---
title: "System::Collections::Generic"
second_title: Aspose.Slides for C++ API 레퍼런스
description: 
type: docs
weight: 326
url: /ko/system.collections.generic/
---
## 클래스

| 클래스 | 설명 |
| --- | --- |
| [_KeyCollection](./_keycollection/) | [Dictionary](./dictionary/)의 키 컬렉션. 컬렉션을 참조하며, 복사하지 않습니다. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수만 사용하여 할당해야 합니다. 스택에 직접 생성하거나 operator new를 사용해 인스턴스를 만들면 런타임 오류 및/또는 어서션 오류가 발생합니다. 절대로 그렇게 하지 마십시오. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 인수로 함수에 전달하십시오. |
| [_KeyList](./_keylist/) | 사전의 키 목록을 구현합니다. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수만 사용하여 할당해야 합니다. 스택에 직접 생성하거나 operator new를 사용해 인스턴스를 만들면 런타임 오류 및/또는 어서션 오류가 발생합니다. 절대로 그렇게 하지 마십시오. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 인수로 함수에 전달하십시오. |
| [_ValueCollection](./_valuecollection/) | [Dictionary](./dictionary/)의 값 컬렉션. 컬렉션을 참조하며, 복사하지 않습니다. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수만 사용하여 할당해야 합니다. 스택에 직접 생성하거나 operator new를 사용해 인스턴스를 만들면 런타임 오류 및/또는 어서션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 인수로 함수에 전달하십시오. |
| [_ValueList](./_valuelist/) | 사전의 값 목록을 구현합니다. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수만 사용하여 할당해야 합니다. 스택에 직접 생성하거나 operator new를 사용해 인스턴스를 만들면 런타임 오류 및/또는 어서션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 인수로 함수에 전달하십시오. |
| [BaseDictionary](./basedictionary/) | 다양한 사전형 데이터 구조([Dictionary](./dictionary/), [SortedDictionary](./sorteddictionary/) 등)를 위한 공통 코드를 구현합니다. 컨테이너를 정의할 때 상속을 제외하고 직접 사용해서는 안 됩니다. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수만 사용하여 할당해야 합니다. 스택에 직접 생성하거나 operator new를 사용해 인스턴스를 만들면 런타임 오류 및/또는 어서션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 인수로 함수에 전달하십시오. |
| [BaseEnumerator](./baseenumerator/) | STL 스타일 타입을 C# 스타일로 사용하기 위한 열거자 정의입니다. 순차 반복자가 존재하는 한 컨테이너 구조에 대한 어서션을 하지 않습니다. begin()과 end() 함수를 사용합니다. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수만 사용하여 할당해야 합니다. 스택에 직접 생성하거나 operator new를 사용해 인스턴스를 만들면 런타임 오류 및/또는 어서션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 인수로 함수에 전달하십시오. |
| [BaseKVCollection](./basekvcollection/) | 키 또는 값 컬렉션에 대한 공통 코드를 보유합니다. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수만 사용하여 할당해야 합니다. 스택에 직접 생성하거나 operator new를 사용해 인스턴스를 만들면 런타임 오류 및/또는 어서션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 인수로 함수에 전달하십시오. |
| [BaseSet](./baseset/) |  |
| [Comparer](./comparer/) | [System.Collections.Generic.IComparer](./icomparer/) 제네릭 인터페이스 구현을 위한 기반 클래스를 제공합니다. |
| [DefaultComparer](./defaultcomparer/) | 기본 비교자 클래스입니다. 값 비교에 operator < 및 operator ==를 사용합니다. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수만 사용하여 할당해야 합니다. 스택에 직접 생성하거나 operator new를 사용해 인스턴스를 만들면 런타임 오류 및/또는 어서션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 인수로 함수에 전달하십시오. |
| [Details_KeyNotFoundException](./details_keynotfoundexception/) |  |
| [Dictionary](./dictionary/) | [Dictionary](./dictionary/) 클래스의 전방 선언. |
| [DictionaryIterator](./dictionaryiterator/) | [Dictionary](./dictionary/) 이터레이터는 [KeyValuePair](./keyvaluepair/) 표기법을 제공합니다. |
| [DictionaryPtr](./dictionaryptr/) | [Dictionary](./dictionary/) 포인터 클래스이며 연산자 오버로드를 제공합니다. 이 타입은 다른 객체의 삭제를 관리하는 포인터입니다. 스택에 할당하고 값을 복사하거나 const 레퍼런스로 함수에 전달해야 합니다. |
| [EnumerableExt](./enumerableext/) |  |
| [EnumeratorWrapperIterator](./enumeratorwrapperiterator/) | 미리 생성된 열거자를 감싸고 모든 호출을 해당 열거자에 전달하는 이터레이터. |
| [HashDictionary](./hashdictionary/) | [HashDictionary](./hashdictionary/) 클래스에 대한 스텁(현재 구현되지 않음). 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수만 사용하여 할당해야 합니다. 스택에 직접 생성하거나 operator new를 사용해 인스턴스를 만들면 런타임 오류 및/또는 어서션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 인수로 함수에 전달하십시오. |
| [HashSet](./hashset/) | [HashSet](./hashset/) 클래스의 전방 선언. |
| [HashSetPtr](./hashsetptr/) | [HashSet](./hashset/) 레퍼런스를 보관하는 포인터. 이 타입은 다른 객체의 삭제를 관리하는 포인터입니다. 스택에 할당하고 값을 복사하거나 const 레퍼런스로 함수에 전달해야 합니다. |
| [ICollection](./icollection/) | 요소 컬렉션 인터페이스. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수만 사용하여 할당해야 합니다. 스택에 직접 생성하거나 operator new를 사용해 인스턴스를 만들면 런타임 오류 및/또는 어서션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 인수로 함수에 전달하십시오. |
| [IComparer](./icomparer/) | 두 객체를 크기(≥, ≤) 비교하는 인터페이스. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수만 사용하여 할당해야 합니다. 스택에 직접 생성하거나 operator new를 사용해 인스턴스를 만들면 런타임 오류 및/또는 어서션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 인수로 함수에 전달하십시오. |
| [IDictionary](./idictionary/) | 사전형 컨테이너용 인터페이스. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수만 사용하여 할당해야 합니다. 스택에 직접 생성하거나 operator new를 사용해 인스턴스를 만들면 런타임 오류 및/또는 어서션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 인수로 함수에 전달하십시오. |
| [IEnumerable](./ienumerable/) | 포함된 요소에 대한 열거자를 제공하는 객체 인터페이스. |
| [IEnumerator](./ienumerator/) | 일부 요소를 순회할 수 있는 열거자 인터페이스. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수만 사용하여 할당해야 합니다. 스택에 직접 생성하거나 operator new를 사용해 인스턴스를 만들면 런타임 오류 및/또는 어서션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 인수로 함수에 전달하십시오. |
| [IEqualityComparer](./iequalitycomparer/) | 두 객체의 동등성을 비교하는 수단을 제공하는 인터페이스. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수만 사용하여 할당해야 합니다. 스택에 직접 생성하거나 operator new를 사용해 인스턴스를 만들면 런타임 오류 및/또는 어서션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 인수로 함수에 전달하십시오. |
| [IKVCollection](./ikvcollection/) | 사전형 컨테이너의 키 또는 값을 포함하는 컨테이너 인터페이스. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수만 사용하여 할당해야 합니다. 스택에 직접 생성하거나 operator new를 사용해 인스턴스를 만들면 런타임 오류 및/또는 어서션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 인수로 함수에 전달하십시오. |
| [IList](./ilist/) | 요소의 인덱스된 컨테이너 인터페이스. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수만 사용하여 할당해야 합니다. 스택에 직접 생성하거나 operator new를 사용해 인스턴스를 만들면 런타임 오류 및/또는 어서션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 인수로 함수에 전달하십시오. |
| [ISet](./iset/) | 고유한 요소 집합을 포함하는 컬렉션 인터페이스. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수만 사용하여 할당해야 합니다. 스택에 직접 생성하거나 operator new를 사용해 인스턴스를 만들면 런타임 오류 및/또는 어서션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 인수로 함수에 전달하십시오. |
| [KeyIterator](./keyiterator/) | [Dictionary](./dictionary/) 이터레이터는 키 접근을 제공합니다. |
| [KeyValuePair](./keyvaluepair/) | 키와 값의 쌍. 이 타입은 스택에 할당하고 값 복사 혹은 레퍼런스로 함수에 전달해야 합니다. 이 타입의 객체를 관리하기 위해 [System::SmartPtr](../system/smartptr/) 클래스를 사용하지 마십시오. |
| [KVPairIterator](./kvpairiterator/) | 적응 이터레이터이며, std::pair를 [Dictionary](./dictionary/)가 기대하는 KVPair로 감싸습니다. |
| [LinkedList](./linkedlist/) | [LinkedList](./linkedlist/) 전방 선언. |
| [LinkedListNode](./linkedlistnode/) | 연결 리스트의 노드. 연결 리스트에 감싸진 std::list 이터레이터에 대한 래퍼를 구현합니다. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수만 사용하여 할당해야 합니다. 스택에 직접 생성하거나 operator new를 사용해 인스턴스를 만들면 런타임 오류 및/또는 어서션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 인수로 함수에 전달하십시오. |
| [List](./list/) | [List](./list/) 전방 선언. |
| [ListExt](./listext/) | [IListWrapper](../system.collections/ilistwrapper/) 인터페이스를 구현하는 일반적인 [List](./list/) 클래스 |
| [ListPtr](./listptr/) | [List](./list/) 포인터이며 접근 연산자를 제공합니다. 이 타입은 다른 객체의 삭제를 관리하는 포인터입니다. 스택에 할당하고 값을 복사하거나 const 레퍼런스로 함수에 전달해야 합니다. |
| [Queue](./queue/) | [Queue](./queue/) 클래스 전방 선언. |
| [QueuePtr](./queueptr/) | [Queue](./queue/) 포인터. 이 타입은 다른 객체의 삭제를 관리하는 포인터입니다. 스택에 할당하고 값을 복사하거나 const 레퍼런스로 함수에 전달해야 합니다. |
| [ReverseEnumerator](./reverseenumerator/) | 컨테이너를 역순으로 순회하는 열거자. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수만 사용하여 할당해야 합니다. 스택에 직접 생성하거나 operator new를 사용해 인스턴스를 만들면 런타임 오류 및/또는 어서션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 인수로 함수에 전달하십시오. |
| [SimpleEnumerator](./simpleenumerator/) | rbegin()와 rend() 함수를 사용하여 직접 요소를 보유하는 간단한 컨테이너용 이터레이터 클래스. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수만 사용하여 할당해야 합니다. 스택에 직접 생성하거나 operator new를 사용해 인스턴스를 만들면 런타임 오류 및/또는 어서션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 인수로 함수에 전달하십시오. |
| [SortedDictionary](./sorteddictionary/) | 정렬된 사전 타입 전방 선언. |
| [SortedDictionaryPtr](./sorteddictionaryptr/) | 정렬된 사전 포인터이며 접근 연산자를 제공합니다. 이 타입은 다른 객체의 삭제를 관리하는 포인터입니다. 스택에 할당하고 값을 복사하거나 const 레퍼런스로 함수에 전달해야 합니다. |
| [SortedList](./sortedlist/) | FlatMap 구조를 감싼 정렬된 리스트. 이 클래스의 객체는 [System::MakeObject()](../system/makeobject/) 함수만 사용하여 할당해야 합니다. 스택에 직접 생성하거나 operator new를 사용해 인스턴스를 만들면 런타임 오류 및/또는 어서션 오류가 발생합니다. 항상 이 클래스를 [System::SmartPtr](../system/smartptr/) 포인터로 감싸고, 해당 포인터를 인수로 함수에 전달하십시오. |
| [SortedListHelper](./sortedlisthelper/) | 이 헬퍼 클래스는 [IDictionary](./idictionary/) 인터페이스에서 오는 가상 함수 get_Keys, get_Values를 가려서 다른 반환 타입을 가진 함수로 대체하는 데 사용됩니다. |
| [SortedSet](./sortedset/) | [SortedSet](./sortedset/) 클래스 전방 선언. |
| [SortedSetPtr](./sortedsetptr/) | [SortedSet](./sortedset/) 레퍼런스를 보관하는 포인터. 이 타입은 다른 객체의 삭제를 관리하는 포인터입니다. 스택에 할당하고 값을 복사하거나 const 레퍼런스로 함수에 전달해야 합니다. |
| [Stack](./stack/) | [Stack](./stack/) 클래스 전방 선언. |
| [StackPtr](./stackptr/) | [Stack](./stack/) 포인터. 이 타입은 다른 객체의 삭제를 관리하는 포인터입니다. 스택에 할당하고 값을 복사하거나 const 레퍼런스로 함수에 전달해야 합니다. |
| [ValueIterator](./valueiterator/) | [Dictionary](./dictionary/) 이터레이터는 값 접근을 제공합니다. |

## 구조체

| 구조체 | 설명 |
| --- | --- |
| [ComparerAdapter](./compareradapter/) | [IComparer](./icomparer/)을 STL 환경에서 사용하기 위한 어댑터. [IComparer](./icomparer/)가 설정되어 있으면 사용하고, 그렇지 않으면 operator < (가능한 경우) 를 사용하거나 그렇지 않으면 false를 반환합니다. |
| [DictionaryHashSelector](./dictionaryhashselector/) | [Dictionary](./dictionary/) 클래스에 대한 해시 함수 선택기. 대체가 제공되지 않은 경우 STL 해싱을 사용합니다. |
| [EqualityComparerAdapter](./equalitycompareradapter/) | [IEqualityComparer](./iequalitycomparer/)를 STL 스타일 컬렉션 및 알고리즘과 함께 사용할 수 있게 해주는 어댑터. [IEqualityComparer](./iequalitycomparer/)이 설정되어 있으면 사용하고, 설정되지 않으면 operator ==, [Object::Equals](../system/object/equals/) 또는 T::Equals 중 가능한 것을 사용합니다. |
| [EqualityComparerHashAdapter](./equalitycomparerhashadapter/) | [IEqualityComparer](./iequalitycomparer/)를 해싱에 사용하기 위한 어댑터. 비교자 객체가 설정되어 있으면 사용하고, 그렇지 않으면 [DictionaryHashSelector](./dictionaryhashselector/) 구조체를 사용해 선택된 사용 가능한 해시 메소드를 사용합니다. |

## 함수

| 함수 | 설명 |
| --- | --- |
| **bool** [operator==](./operator_equal_equal/)(const [KeyValuePair](./keyvaluepair/)\<TKey, TValue\>\&, const [KeyValuePair](./keyvaluepair/)\<TKey, TValue\>\&) | 두 키-값 쌍을 'equals' 의미로 비교합니다. 키와 값 모두에 대해 정의된 operator == 또는 EqualsTo 메소드를 사용합니다. |
| **bool** [operator!=](./operator_not_equal/)(const [KeyValuePair](./keyvaluepair/)\<TKey, TValue\>\&, const [KeyValuePair](./keyvaluepair/)\<TKey, TValue\>\&) | 두 키-값 쌍을 역 'equals' 의미로 비교합니다. |
| std::ostream\& [operator<<](./operator_less_less/)(std::ostream\&, const [KeyValuePair](./keyvaluepair/)\<TKey, TValue\>\&) | UTF-8 인코딩을 사용해 데이터를 스트림에 삽입합니다. |
| std::wostream\& [operator<<](./operator_less_less/)(std::wostream\&, const [KeyValuePair](./keyvaluepair/)\<TKey, TValue\>\&) | 데이터를 스트림에 삽입합니다. |

## 타입 정의

| 타입 정의 | 설명 |
| --- | --- |
| [KeyNotFoundException](./keynotfoundexception/) |  |