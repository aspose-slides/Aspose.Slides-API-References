---
title: GetValueOrDefault()
second_title: Aspose.Slides for C++ API 레퍼런스
description: 값이 있으면 반환하고, 없으면 Value() 를 반환합니다.
type: docs
weight: 144
url: /ko/system.collections.generic/idictionary/getvalueordefault/
---
## IDictionary::GetValueOrDefault(const TKey\&) const 메서드


값이 있으면 반환하고, 없으면 **Value()** 를 반환합니다.

```cpp
virtual TValue System::Collections::Generic::IDictionary<TKey, TValue>::GetValueOrDefault(const TKey &key) const
```

## IDictionary::GetValueOrDefault(const TKey\&, const TValue\&) const 메서드


값이 있으면 반환하고, 없으면 **defaultValue** 를 반환합니다.

```cpp
virtual TValue System::Collections::Generic::IDictionary<TKey, TValue>::GetValueOrDefault(const TKey &key, const TValue &defaultValue) const
```

## 참고

* 클래스 [IDictionary](../)
* 네임스페이스 [System::Collections::Generic](../../)
* 라이브러리 [Aspose.Slides](../../../)