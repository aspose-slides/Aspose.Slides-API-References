---
title: GetValueOrDefault()
second_title: Aspose.Slides for C++ API 레퍼런스
description: 값을 찾으면 반환하고, 그렇지 않으면 Value() 를 반환합니다.
type: docs
weight: 196
url: /ko/system.collections.generic/basedictionary/getvalueordefault/
---
## BaseDictionary::GetValueOrDefault(const key_t\&) const 메서드

값을 찾으면 반환하고, 그렇지 않으면 **Value()** 를 반환합니다.

```cpp
mapped_t System::Collections::Generic::BaseDictionary<Map>::GetValueOrDefault(const key_t &key) const override
```
## BaseDictionary::GetValueOrDefault(const key_t\&, const mapped_t\&) const 메서드

값을 찾으면 반환하고, 그렇지 않으면 **defaultValue** 를 반환합니다.

```cpp
mapped_t System::Collections::Generic::BaseDictionary<Map>::GetValueOrDefault(const key_t &key, const mapped_t &defaultValue) const override
```
## 참조

* 클래스 [BaseDictionary](../)
* 네임스페이스 [System::Collections::Generic](../../)
* 라이브러리 [Aspose.Slides](../../../)