---
title: TryGetValue()
second_title: Aspose.Slides for C++ API 레퍼런스
description: 키가 있는 값을 찾아서, 찾으면 반환합니다.
type: docs
weight: 144
url: /ko/system.collections.generic/basedictionary/trygetvalue/
---
## BaseDictionary::TryGetValue(const key_t\&, mapped_t\&) const method

키가 있는 값을 찾아서, 찾으면 반환합니다.

```cpp
bool System::Collections::Generic::BaseDictionary<Map>::TryGetValue(const key_t &key, mapped_t &value) const override
```

### 인수

| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| key | const key_t\& | 찾을 키. |
| value | mapped_t\& | 찾은 값을 저장할 참조. |

### 반환 값

값이 발견되어 반환되면 true, 그렇지 않으면 false.

## 참고

* 클래스 [BaseDictionary](../)
* 네임스페이스 [System::Collections::Generic](../../)
* 라이브러리 [Aspose.Slides](../../../)