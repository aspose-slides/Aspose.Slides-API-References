---
title: rbegin()
second_title: C++용 Aspose.Slides API 레퍼런스
description: 컬렉션의 마지막 요소(역방향에서는 첫 번째)로 가는 역방향 반복자를 가져옵니다.
type: docs
weight: 287
url: /ko/system.collections.generic/linkedlist/rbegin/
---
## LinkedList::rbegin() 메서드

컬렉션의 마지막 요소(역방향에서는 첫 번째)로 가는 역방향 반복자를 가져옵니다.

```cpp
reverse_iterator System::Collections::Generic::LinkedList<T>::rbegin() noexcept
```

### 반환값

연결 리스트의 마지막 요소(역방향에서는 첫 번째)를 가리키는 역방향 반복자입니다.

## LinkedList::rbegin() const 메서드

const 한정 컬렉션의 마지막 요소(역방향에서는 첫 번째)로 가는 역방향 반복자를 가져옵니다.

```cpp
const_reverse_iterator System::Collections::Generic::LinkedList<T>::rbegin() const noexcept
```

### 반환값

const 한정 연결 리스트의 마지막 요소(역방향에서는 첫 번째)를 가리키는 역방향 반복자입니다.

## 참조

* 타입 정의 [reverse_iterator](../reverse_iterator/)
* 타입 정의 [const_reverse_iterator](../const_reverse_iterator/)
* 클래스 [LinkedList](../)
* 네임스페이스 [System::Collections::Generic](../../)
* 라이브러리 [Aspose.Slides](../../../)