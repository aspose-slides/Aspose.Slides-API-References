---
title: rend()
second_title: Aspose.Slides for C++ API 참조
description: 컬렉션 시작 전에 존재하지 않는 요소에 대한 역방향 이터레이터를 가져옵니다.
type: docs
weight: 300
url: /ko/system.collections.generic/linkedlist/rend/
---
## LinkedList::rend() 메서드

컬렉션 시작 전에 존재하지 않는 요소에 대한 역방향 이터레이터를 가져옵니다.

```cpp
reverse_iterator System::Collections::Generic::LinkedList<T>::rend() noexcept
```

### 반환 값

연결 리스트의 첫 번째 요소 앞에 위치한(역방향에서는 마지막) 이론적 요소를 가리키는 역방향 이터레이터입니다.

## LinkedList::rend() const 메서드

const 한정된 컬렉션 시작 전에 존재하지 않는 요소에 대한 역방향 이터레이터를 가져옵니다.

```cpp
const_reverse_iterator System::Collections::Generic::LinkedList<T>::rend() const noexcept
```

### 반환 값

const 한정된 연결 리스트의 첫 번째 요소 앞에 위치한(역방향에서는 마지막) 이론적 요소를 가리키는 역방향 이터레이터입니다.

## 참고

* 타입 정의 [reverse_iterator](../reverse_iterator/)
* 타입 정의 [const_reverse_iterator](../const_reverse_iterator/)
* 클래스 [LinkedList](../)
* 네임스페이스 [System::Collections::Generic](../../)
* 라이브러리 [Aspose.Slides](../../../)