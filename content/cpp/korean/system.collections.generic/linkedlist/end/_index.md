---
title: end()
second_title: Aspose.Slides for C++ API 레퍼런스
description: 컬렉션의 끝 뒤에 있는 존재하지 않는 요소에 대한 반복자를 가져옵니다.
type: docs
weight: 248
url: /ko/system.collections.generic/linkedlist/end/
---
## LinkedList::end() 메서드

컬렉션의 끝 뒤에 있는 존재하지 않는 요소에 대한 반복자를 가져옵니다.

```cpp
iterator System::Collections::Generic::LinkedList<T>::end() noexcept
```

### 반환값

연결 리스트의 끝 요소 뒤에 배치된 가상의 요소를 가리키는 반복자입니다.

## LinkedList::end() const 메서드

const 한정된 컬렉션의 끝 뒤에 있는 존재하지 않는 요소에 대한 반복자를 가져옵니다.

```cpp
const_iterator System::Collections::Generic::LinkedList<T>::end() const noexcept
```

### 반환값

const 한정된 연결 리스트 인스턴스의 끝 요소 뒤에 배치된 가상의 요소를 가리키는 반복자입니다.

## 참조

* Typedef [iterator](../../ienumerable/iterator/)
* Typedef [const_iterator](../../ienumerable/const_iterator/)
* Class [LinkedList](../)
* Namespace [System::Collections::Generic](../../)
* Library [Aspose.Slides](../../../)