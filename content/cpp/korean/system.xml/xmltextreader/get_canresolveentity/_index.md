---
title: get_CanResolveEntity()
second_title: Aspose.Slides for C++ API 참조
description: 이 리더가 엔터티를 구문 분석하고 해결할 수 있는지 여부를 나타내는 값을 반환합니다.
type: docs
weight: 235
url: /ko/system.xml/xmltextreader/get_canresolveentity/
---
## XmlTextReader::get_CanResolveEntity() 메서드

이 리더가 엔터티를 구문 분석하고 해결할 수 있는지 여부를 나타내는 값을 반환합니다.

```cpp
bool System::Xml::XmlTextReader::get_CanResolveEntity() override
```

### 반환 값

리더가 엔터티를 구문 분석하고 해결할 수 있으면 **true**, 그렇지 않으면 **false**. [XmlTextReader](../) 클래스는 항상 **true** 를 반환합니다.

## 참조

* 클래스 [XmlTextReader](../)
* 네임스페이스 [System::Xml](../../)
* 라이브러리 [Aspose.Slides](../../../)