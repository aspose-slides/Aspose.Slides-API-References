---
title: ToString()
second_title: مرجع API لـ Aspose.Slides للغة C++
description: يقوم بتحويل القيمة المنطقية إلى سلسلة.
type: docs
weight: 222
url: /ar/system.xml/xmlconvert/tostring/
---
## XmlConvert::ToString(bool) دالة

يحوّل [Boolean](../../../system/boolean/) إلى [String](../../../system/string/).

```cpp
static String System::Xml::XmlConvert::ToString(bool value)
```

### المعاملات

| معلمة | نوع | وصف |
| --- | --- | --- |
| value | **bool** | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل نصي لـ [Boolean](../../../system/boolean/)، أي "true" أو "false".

## XmlConvert::ToString(char16_t) دالة

يحوّل [Char](../../../system/char/) إلى [String](../../../system/string/).

```cpp
static String System::Xml::XmlConvert::ToString(char16_t value)
```

### المعاملات

| معلمة | نوع | وصف |
| --- | --- | --- |
| value | char16_t | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل نصي لـ [Char](../../../system/char/).

## XmlConvert::ToString(Decimal) دالة

يحوّل [Decimal](../../../system/decimal/) إلى [String](../../../system/string/).

```cpp
static String System::Xml::XmlConvert::ToString(Decimal value)
```

### المعاملات

| معلمة | نوع | وصف |
| --- | --- | --- |
| value | [Decimal](../../../system/decimal/) | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل نصي لـ [Decimal](../../../system/decimal/).

## XmlConvert::ToString(int8_t) دالة

يحوّل [SByte](../../../system/sbyte/) إلى [String](../../../system/string/).

```cpp
static String System::Xml::XmlConvert::ToString(int8_t value)
```

### المعاملات

| معلمة | نوع | وصف |
| --- | --- | --- |
| value | **int8_t** | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل نصي لـ [SByte](../../../system/sbyte/).

## XmlConvert::ToString(int16_t) دالة

يحوّل [Int16](../../../system/int16/) إلى [String](../../../system/string/).

```cpp
static String System::Xml::XmlConvert::ToString(int16_t value)
```

### المعاملات

| معلمة | نوع | وصف |
| --- | --- | --- |
| value | **int16_t** | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل نصي لـ [Int16](../../../system/int16/).

## XmlConvert::ToString(int32_t) دالة

يحوّل [Int32](../../../system/int32/) إلى [String](../../../system/string/).

```cpp
static String System::Xml::XmlConvert::ToString(int32_t value)
```

### المعاملات

| معلمة | نوع | وصف |
| --- | --- | --- |
| value | **int32_t** | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل نصي لـ [Int32](../../../system/int32/).

## XmlConvert::ToString(int64_t) دالة

يحوّل [Int64](../../../system/int64/) إلى [String](../../../system/string/).

```cpp
static String System::Xml::XmlConvert::ToString(int64_t value)
```

### المعاملات

| معلمة | نوع | وصف |
| --- | --- | --- |
| value | **int64_t** | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل نصي لـ [Int64](../../../system/int64/).

## XmlConvert::ToString(uint8_t) دالة

يحوّل [Byte](../../../system/byte/) إلى [String](../../../system/string/).

```cpp
static String System::Xml::XmlConvert::ToString(uint8_t value)
```

### المعاملات

| معلمة | نوع | وصف |
| --- | --- | --- |
| value | **uint8_t** | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل نصي لـ [Byte](../../../system/byte/).

## XmlConvert::ToString(uint16_t) دالة

يحوّل [UInt16](../../../system/uint16/) إلى [String](../../../system/string/).

```cpp
static String System::Xml::XmlConvert::ToString(uint16_t value)
```

### المعاملات

| معلمة | نوع | وصف |
| --- | --- | --- |
| value | **uint16_t** | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل نصي لـ [UInt16](../../../system/uint16/).

## XmlConvert::ToString(uint32_t) دالة

يحوّل [UInt32](../../../system/uint32/) إلى [String](../../../system/string/).

```cpp
static String System::Xml::XmlConvert::ToString(uint32_t value)
```

### المعاملات

| معلمة | نوع | وصف |
| --- | --- | --- |
| value | **uint32_t** | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل نصي لـ [UInt32](../../../system/uint32/).

## XmlConvert::ToString(uint64_t) دالة

يحوّل [UInt64](../../../system/uint64/) إلى [String](../../../system/string/).

```cpp
static String System::Xml::XmlConvert::ToString(uint64_t value)
```

### المعاملات

| معلمة | نوع | وصف |
| --- | --- | --- |
| value | **uint64_t** | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل نصي لـ [UInt64](../../../system/uint64/).

## XmlConvert::ToString(float) دالة

يحوّل [Single](../../../system/single/) إلى [String](../../../system/string/).

```cpp
static String System::Xml::XmlConvert::ToString(float value)
```

### المعاملات

| معلمة | نوع | وصف |
| --- | --- | --- |
| value | **float** | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل نصي لـ [Single](../../../system/single/).

## XmlConvert::ToString(double) دالة

يحوّل [Double](../../../system/double/) إلى [String](../../../system/string/).

```cpp
static String System::Xml::XmlConvert::ToString(double value)
```

### المعاملات

| معلمة | نوع | وصف |
| --- | --- | --- |
| value | **double** | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل نصي لـ [Double](../../../system/double/).

## XmlConvert::ToString(TimeSpan) دالة

يحوّل [TimeSpan](../../../system/timespan/) إلى [String](../../../system/string/).

```cpp
static String System::Xml::XmlConvert::ToString(TimeSpan value)
```

### المعاملات

| معلمة | نوع | وصف |
| --- | --- | --- |
| value | [TimeSpan](../../../system/timespan/) | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل نصي لـ [TimeSpan](../../../system/timespan/).

## XmlConvert::ToString(DateTime) دالة

يحوّل [DateTime](../../../system/datetime/) إلى [String](../../../system/string/).

```cpp
static String System::Xml::XmlConvert::ToString(DateTime value)
```

### المعاملات

| معلمة | نوع | وصف |
| --- | --- | --- |
| value | [DateTime](../../../system/datetime/) | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل نصي لـ [DateTime](../../../system/datetime/) بالصيغة yyyy-MM-ddTHH:mm:ss حيث 'T' ثابت حرفي.

## XmlConvert::ToString(DateTime, const String\&) دالة

يحوّل [DateTime](../../../system/datetime/) إلى [String](../../../system/string/).

```cpp
static String System::Xml::XmlConvert::ToString(DateTime value, const String &format)
```

### المعاملات

| معلمة | نوع | وصف |
| --- | --- | --- |
| value | [DateTime](../../../system/datetime/) | القيمة المراد تحويلها. |
| format | const [String](../../../system/string/)\& | بنية الصيغة التي تحدد كيفية عرض السلسلة المحوّلة. الصيغ الصالحة تشمل "yyyy-MM-ddTHH:mm:sszzzzzz" وتفرعاتها. |

### قيمة الإرجاع

تمثيل نصي لـ [DateTime](../../../system/datetime/) بالصّيغة المحددة.

## XmlConvert::ToString(DateTime, XmlDateTimeSerializationMode) دالة

يحوّل [DateTime](../../../system/datetime/) إلى [String](../../../system/string/) باستخدام XmlDateTimeSerializationMode المحدد.

```cpp
static String System::Xml::XmlConvert::ToString(DateTime value, XmlDateTimeSerializationMode dateTimeOption)
```

### المعاملات

| معلمة | نوع | وصف |
| --- | --- | --- |
| value | [DateTime](../../../system/datetime/) | قيمة [DateTime](../../../system/datetime/) المراد تحويلها. |
| dateTimeOption | [XmlDateTimeSerializationMode](../../xmldatetimeserializationmode/) | أحد قيم XmlDateTimeSerializationMode التي تحدد كيفية معالجة قيمة [DateTime](../../../system/datetime/). |

### قيمة الإرجاع

[String](../../../system/string/) مكافئ لـ [DateTime](../../../system/datetime/).

## XmlConvert::ToString(DateTimeOffset) دالة

يحوّل [DateTimeOffset](../../../system/datetimeoffset/) المزوّد إلى [String](../../../system/string/).

```cpp
static String System::Xml::XmlConvert::ToString(DateTimeOffset value)
```

### المعاملات

| معلمة | نوع | وصف |
| --- | --- | --- |
| value | [DateTimeOffset](../../../system/datetimeoffset/) | [DateTimeOffset](../../../system/datetimeoffset/) الذي سيتم تحويله. |

### قيمة الإرجاع

تمثيل [String](../../../system/string/) لـ [DateTimeOffset](../../../system/datetimeoffset/) المزوّد.

## XmlConvert::ToString(DateTimeOffset, const String\&) دالة

يحوّل [DateTimeOffset](../../../system/datetimeoffset/) المزوّد إلى [String](../../../system/string/) بالصّيغة المحددة.

```cpp
static String System::Xml::XmlConvert::ToString(DateTimeOffset value, const String &format)
```

### المعاملات

| معلمة | نوع | وصف |
| --- | --- | --- |
| value | [DateTimeOffset](../../../system/datetimeoffset/) | [DateTimeOffset](../../../system/datetimeoffset/) الذي سيتم تحويله. |
| format | const [String](../../../system/string/)\& | الصيغة التي يتحول إليها **s**. يمكن أن تكون معلمة الصيغة أي جزء من توصية W3C لنوع XML dateTime. لمزيد من المعلومات، انظر قسم [dateTime](https://www.w3.org/TR/xmlschema-2/#dateTime) من مواصفة XML [Schema](../../../system.xml.schema/). |

### قيمة الإرجاع

تمثيل [String](../../../system/string/) بالصّيغة المحددة للـ [DateTimeOffset](../../../system/datetimeoffset/) المزوّد.

## XmlConvert::ToString(Guid) دالة

يحوّل [Guid](../../../system/guid/) إلى [String](../../../system/string/).

```cpp
static String System::Xml::XmlConvert::ToString(Guid value)
```

### المعاملات

| معلمة | نوع | وصف |
| --- | --- | --- |
| value | [Guid](../../../system/guid/) | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل نصي لـ [Guid](../../../system/guid/).

## انظر أيضاً

* Enum [XmlDateTimeSerializationMode](../../xmldatetimeserializationmode/)
* فئة [String](../../../system/string/)
* فئة [XmlConvert](../)
* فئة [Decimal](../../../system/decimal/)
* فئة [TimeSpan](../../../system/timespan/)
* فئة [DateTime](../../../system/datetime/)
* فئة [DateTimeOffset](../../../system/datetimeoffset/)
* فئة [Guid](../../../system/guid/)
* مساحة اسم [System::Xml](../../)
* مكتبة [Aspose.Slides](../../../)