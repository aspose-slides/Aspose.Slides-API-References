---
title: IsOutOfBounds()
second_title: مرجع API لـ Aspose.Slides للغة C++
description: يتحقق مما إذا كان الفهرس خارج حدود الحاوية، باستثناء حجم الحاوية.
type: docs
weight: 209
url: /ar/system.collections.generic.details/isoutofbounds/
---
## System::Collections::Generic::Details::IsOutOfBounds(int, const Container\&) دالة

يتحقق مما إذا كان الفهرس خارج حدود الحاوية، باستثناء حجم الحاوية.

```cpp
template<typename Container> bool System::Collections::Generic::Details::IsOutOfBounds(int idx, const Container &container)
```

### معلمات القالب

| معامل | الوصف |
| --- | --- |
| Container | نوع الحاوية. |

### المعاملات

| معامل | نوع | الوصف |
| --- | --- | --- |
| idx | int | [Index](../../system/index/). |
| container | const Container\& | الحاوية التي يُفحص الفهرس مقابلها. |

### قيمة الإرجاع

صحيح إذا كان الفهرس خارج حدود الحاوية، خطأ بخلاف ذلك.

## System::Collections::Generic::Details::IsOutOfBounds(std::int64_t, const Container\&) دالة

يتحقق مما إذا كان الفهرس خارج حدود الحاوية، باستثناء حجم الحاوية.

```cpp
template<typename Container> bool System::Collections::Generic::Details::IsOutOfBounds(std::int64_t idx, const Container &container)
```

### معلمات القالب

| معامل | الوصف |
| --- | --- |
| Container | نوع الحاوية. |

### المعاملات

| معامل | نوع | الوصف |
| --- | --- | --- |
| idx | std::int64_t | [Index](../../system/index/). |
| container | const Container\& | الحاوية التي يُفحص الفهرس مقابلها. |

### قيمة الإرجاع

صحيح إذا كان الفهرس خارج حدود الحاوية، خطأ بخلاف ذلك.

## انظر أيضًا

* مساحة الاسم [System::Collections::Generic::Details](../)
* مكتبة [Aspose.Slides](../../)