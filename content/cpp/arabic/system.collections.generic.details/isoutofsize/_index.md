---
title: IsOutOfSize()
second_title: مرجع API Aspose.Slides للغة C++
description: يتحقق مما إذا كان الفهرس خارج حدود الحاوية، بما في ذلك حجم الحاوية.
type: docs
weight: 222
url: /ar/system.collections.generic.details/isoutofsize/
---
## System::Collections::Generic::Details::IsOutOfSize(int, const Container\&) دالة

يتحقق مما إذا كان الفهرس خارج حدود الحاوية، بما في ذلك حجم الحاوية.

```cpp
template<typename Container> bool System::Collections::Generic::Details::IsOutOfSize(int count, const Container &container)
```

### معاملات القالب

| معامل | الوصف |
| --- | --- |
| Container | نوع الحاوية. |

### المعلمات

| معامل | النوع | الوصف |
| --- | --- | --- |
| count | int | [Index](../../system/index/). |
| container | const Container\& | الحاوية للتحقق من الفهرس ضدها. |

### قيمة الإرجاع

صحيح إذا كان الفهرس خارج حدود الحاوية، وإلا خطأ.

## System::Collections::Generic::Details::IsOutOfSize(std::int64_t, const Container\&) دالة

يتحقق مما إذا كان الفهرس خارج حدود الحاوية، بما في ذلك حجم الحاوية.

```cpp
template<typename Container> bool System::Collections::Generic::Details::IsOutOfSize(std::int64_t count, const Container &container)
```

### معاملات القالب

| معامل | الوصف |
| --- | --- |
| Container | نوع الحاوية. |

### المعلمات

| معامل | النوع | الوصف |
| --- | --- | --- |
| count | std::int64_t | [Index](../../system/index/). |
| container | const Container\& | الحاوية للتحقق من الفهرس ضدها. |

### قيمة الإرجاع

صحيح إذا كان الفهرس خارج حدود الحاوية، وإلا خطأ.

## انظر أيضًا

* نطاق الاسم [System::Collections::Generic::Details](../)
* المكتبة [Aspose.Slides](../../)