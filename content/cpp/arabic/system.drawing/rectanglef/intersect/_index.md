---
title: Intersect()
second_title: مرجع API لـ Aspose.Slides للغة C++
description: يستبدل المستطيل الممثَّل بواسطة الكائن الحالي بالمستطيل الناتج عن تقاطعه مع المستطيل الممثَّل بواسطة الكائن المحدد.
type: docs
weight: 274
url: /ar/system.drawing/rectanglef/intersect/
---
## طريقة RectangleF::Intersect(const RectangleF\&) method

يستبدل المستطيل الممثَّل بواسطة الكائن الحالي بالمستطيل الناتج عن تقاطعه مع المستطيل الممثَّل بواسطة الكائن المحدد.

```cpp
void System::Drawing::RectangleF::Intersect(const RectangleF &rect)
```

### المعلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| rect | const [RectangleF](../)\& | الكائن [RectangleF](../) الذي يمثل المستطيل لتقاطع المستطيل الممثَّل بواسطة الكائن الحالي معه |

## طريقة RectangleF::Intersect(const RectangleF\&, const RectangleF\&) method

يعيد مستطيلًا نتيجة لتقاطع المستطيلات المحددة.

```cpp
static RectangleF System::Drawing::RectangleF::Intersect(const RectangleF &a, const RectangleF &b)
```

### المعلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| a | const [RectangleF](../)\& | أول مستطيل للتقاطع |
| b | const [RectangleF](../)\& | ثاني مستطيل للتقاطع |

### قيمة الإرجاع

نتيجة تقاطع **a** مع **b**

## انظر أيضًا

* Class [RectangleF](../)
* Namespace [System::Drawing](../../)
* Library [Aspose.Slides](../../../)