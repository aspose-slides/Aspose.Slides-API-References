---
title: Contains()
second_title: Aspose.Slides للـ C++ مرجع API
description: يحدد ما إذا كانت النقطة المحددة تقع داخل المستطيل الممثل بالكائن الحالي.
type: docs
weight: 248
url: /ar/system.drawing/rectanglef/contains/
---
## RectangleF::Contains(float, float) طريقة

يحدد ما إذا كانت النقطة المحددة تقع داخل المستطيل الممثَّل بالكائن الحالي.

```cpp
bool System::Drawing::RectangleF::Contains(float x, float y)
```

### المعلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| x | **float** | إحداثي X للنقطة التي سيتم التحقق منها |
| y | **float** | إحداثي Y للنقطة التي سيتم التحقق منها |

### القيمة المرجعة

True إذا كانت النقطة المحددة تقع داخل المستطيل الممثَّل بالكائن الحالي، وإلا - false

## RectangleF::Contains(const PointF\&) طريقة

يحدد ما إذا كانت النقطة المحددة تقع داخل المستطيل الممثَّل بالكائن الحالي.

```cpp
bool System::Drawing::RectangleF::Contains(const PointF &point)
```

### المعلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| point | const [PointF](../../pointf/)\& | نقطة للتحقق |

### القيمة المرجعة

True إذا كانت النقطة المحددة تقع داخل المستطيل الممثَّل بالكائن الحالي، وإلا - false

## RectangleF::Contains(const RectangleF\&) طريقة

يحدد ما إذا كان المستطيل المحدد يقع داخل المستطيل الممثَّل بالكائن الحالي.

```cpp
bool System::Drawing::RectangleF::Contains(const RectangleF &rect)
```

### المعلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| rect | const [RectangleF](../)\& | مستطيل للتحقق |

### القيمة المرجعة

True إذا كان المستطيل المحدد يقع داخل المستطيل الممثَّل بالكائن الحالي، وإلا - false

## انظر أيضًا

* الصنف [RectangleF](../)
* الصنف [PointF](../../pointf/)
* النطاق [System::Drawing](../../)
* المكتبة [Aspose.Slides](../../../)