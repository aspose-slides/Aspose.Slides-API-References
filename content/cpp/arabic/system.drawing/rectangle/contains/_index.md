---
title: Contains()
second_title: Aspose.Slides للـ C++ مرجع API
description: يحدد ما إذا كانت النقطة المحددة موجودة داخل المستطيل الممثَّل بواسطة الكائن الحالي.
type: docs
weight: 248
url: /ar/system.drawing/rectangle/contains/
---
## Rectangle::Contains(int, int) const طريقة


يحدد ما إذا كانت النقطة المحددة موجودة داخل المستطيل الممثَّل بواسطة الكائن الحالي.

```cpp
bool System::Drawing::Rectangle::Contains(int x, int y) const
```


### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| x | int | إحداثي X للنقطة المراد فحصها |
| y | int | إحداثي Y للنقطة المراد فحصها |

### قيمة الإرجاع

صحيح إذا كانت النقطة المحددة موجودة داخل المستطيل الممثَّل بواسطة الكائن الحالي، وإلا - خاطئ

## Rectangle::Contains(const Point\&) const طريقة


يحدد ما إذا كانت النقطة المحددة موجودة داخل المستطيل الممثَّل بواسطة الكائن الحالي.

```cpp
bool System::Drawing::Rectangle::Contains(const Point &point) const
```


### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| point | const [Point](../../point/)\& | نقطة للتحقق |

### قيمة الإرجاع

صحيح إذا كانت النقطة المحددة موجودة داخل المستطيل الممثَّل بواسطة الكائن الحالي، وإلا - خاطئ

## Rectangle::Contains(const Rectangle\&) const طريقة


يحدد ما إذا كان المستطيل المحدد موجودًا داخل المستطيل الممثَّل بواسطة الكائن الحالي.

```cpp
bool System::Drawing::Rectangle::Contains(const Rectangle &rect) const
```


### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| rect | const [Rectangle](../)\& | مستطيل للتحقق |

### قيمة الإرجاع

صحيح إذا كان المستطيل المحدد موجودًا داخل المستطيل الممثَّل بواسطة الكائن الحالي، وإلا - خاطئ

## انظر أيضًا

* الفئة [Rectangle](../)
* الفئة [Point](../../point/)
* النطاق [System::Drawing](../../)
* المكتبة [Aspose.Slides](../../../)