---
title: Intersect()
second_title: مرجع API لـ Aspose.Slides للـ C++
description: يستبدل المستطيل الممثل بالعنصر الحالي بالمستطيل الناتج عن تقاطعه مع المستطيل الممثل بالعنصر المحدد.
type: docs
weight: 274
url: /ar/system.drawing/rectangle/intersect/
---
## Rectangle::Intersect(const Rectangle\&) طريقة

يستبدل المستطيل الممثل بالعنصر الحالي بالمستطيل الناتج عن تقاطعه مع المستطيل الممثل بالعنصر المحدد.

```cpp
void System::Drawing::Rectangle::Intersect(const Rectangle &rect)
```

### المعلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| rect | const [Rectangle](../)\& | الكائن [Rectangle](../) الذي يمثل المستطيل لتقاطع المستطيل الممثل للعنصر الحالي معه |

## Rectangle::Intersect(const Rectangle\&, const Rectangle\&) طريقة

يرجع مستطيلًا هو نتيجة تقاطع المستطيلات المحددة.

```cpp
static Rectangle System::Drawing::Rectangle::Intersect(const Rectangle &a, const Rectangle &b)
```

### المعلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| a | const [Rectangle](../)\& | المستطيل الأول للتقاطع |
| b | const [Rectangle](../)\& | المستطيل الثاني للتقاطع |

### قيمة الإرجاع

نتيجة تقاطع **a** مع **b**

## انظر أيضًا

* الفئة [Rectangle](../)
* النطاق [System::Drawing](../../)
* المكتبة [Aspose.Slides](../../../)