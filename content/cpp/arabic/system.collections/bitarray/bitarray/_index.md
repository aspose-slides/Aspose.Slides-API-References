---
title: BitArray()
second_title: Aspose.Slides لـ C++ دليل API
description: منشئ النسخ.
type: docs
weight: 14
url: /ar/system.collections/bitarray/bitarray/
---
## BitArray::BitArray(const bitset\&) منشئ

منشئ النسخ.

```cpp
System::Collections::BitArray::BitArray(const bitset &bits)
```

### Arguments

| المعامل | النوع | الوصف |
| --- | --- | --- |
| bits | const [bitset](../bitset/)\& | الحاوية لنسخ البيانات منها. |

## BitArray::BitArray(const BitArray\&) منشئ

منشئ النسخ.

```cpp
System::Collections::BitArray::BitArray(const BitArray &bits)
```

### Arguments

| المعامل | النوع | الوصف |
| --- | --- | --- |
| bits | const [BitArray](../)\& | الحاوية لنسخ البيانات منها. |

## BitArray::BitArray(const BitArrayPtr\&) منشئ

منشئ النسخ.

```cpp
System::Collections::BitArray::BitArray(const BitArrayPtr &array)
```

### Arguments

| المعامل | النوع | الوصف |
| --- | --- | --- |
| array | const [BitArrayPtr](../../bitarrayptr/)\& | [BitArray](../) لنسخ البيانات منها. |

## BitArray::BitArray(const System::ArrayPtr\<bool\>\&) منشئ

منشئ النسخ.

```cpp
System::Collections::BitArray::BitArray(const System::ArrayPtr<bool> &values)
```

### Arguments

| المعامل | النوع | الوصف |
| --- | --- | --- |
| values | const [System::ArrayPtr](../../../system/arrayptr/)\<**bool**\>\& | الحاوية لنسخ البيانات على مستوى العنصر. |

## BitArray::BitArray(const System::ArrayPtr\<uint8_t\>\&) منشئ

منشئ النسخ.

```cpp
System::Collections::BitArray::BitArray(const System::ArrayPtr<uint8_t> &bytes)
```

### Arguments

| المعامل | النوع | الوصف |
| --- | --- | --- |
| bytes | const [System::ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\>\& | الحاوية لنسخ البيانات على مستوى البت. |

## BitArray::BitArray(const System::ArrayPtr\<int\>\&) منشئ

منشئ النسخ.

```cpp
System::Collections::BitArray::BitArray(const System::ArrayPtr<int> &values)
```

### Arguments

| المعامل | النوع | الوصف |
| --- | --- | --- |
| values | const [System::ArrayPtr](../../../system/arrayptr/)\<int\>\& | الحاوية لنسخ البيانات على مستوى البت. |

## BitArray::BitArray(int, bool) منشئ

منشئ تعبئة.

```cpp
System::Collections::BitArray::BitArray(int length, bool defaultValue=false)
```

### Arguments

| المعامل | النوع | الوصف |
| --- | --- | --- |
| length | int | عدد العناصر للتعبئة. |
| defaultValue | **bool** | قيمة التعبئة. |

## انظر أيضًا

* تعريف نوع [bitset](../bitset/)
* تعريف نوع [ArrayPtr](../../../system/arrayptr/)
* فئة [BitArray](../)
* فئة [BitArrayPtr](../../bitarrayptr/)
* مساحة الأسماء [System::Collections](../../)
* مكتبة [Aspose.Slides](../../../)