---
title: GetCharCount()
second_title: مرجع API Aspose.Slides للغة C++
description: احصل على عدد الأحرف المطلوبة لفك ترميز مخزن بايت.
type: docs
weight: 261
url: /ar/system.text/encoding/getcharcount/
---
## Encoding::GetCharCount(ArrayPtr\<uint8_t\>, int, int) طريقة

احصل على عدد الأحرف المطلوبة لفك ترميز مخزن بايت.

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes, int index, int count)
```

### المعاملات

| معامل | نوع | وصف |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | البايتات المراد فك ترميزها. |
| index | int | بداية الجزء. |
| count | int | حجم الجزء. |

### قيمة الإرجاع

عدد الأحرف.

## Encoding::GetCharCount(ArrayPtr\<uint8_t\>) طريقة

احصل على عدد الأحرف المطلوبة لفك ترميز مخزن بايت.

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes)
```

### المعاملات

| معامل | نوع | وصف |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | البايتات المراد فك ترميزها. |

### قيمة الإرجاع

عدد الأحرف.

## Encoding::GetCharCount(const uint8_t *, int) طريقة

احصل على عدد الأحرف المطلوبة لفك ترميز مخزن بايت.

```cpp
virtual int System::Text::Encoding::GetCharCount(const uint8_t *bytes, int count)
```

### المعاملات

| معامل | نوع | وصف |
| --- | --- | --- |
| bytes | const **uint8_t** * | البايتات المراد فك ترميزها. |
| count | int | عدد البايتات. |

### قيمة الإرجاع

عدد الأحرف.

## انظر أيضًا

* تعريف نوع [ArrayPtr](../../../system/arrayptr/)
* فئة [Encoding](../)
* مساحة الاسم [System::Text](../../)
* مكتبة [Aspose.Slides](../../../)