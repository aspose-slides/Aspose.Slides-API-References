---
title: GetByteCount()
second_title: مرجع API Aspose.Slides لـ C++
description: احصل على عدد الأحرف اللازمة لتشفير مخزن الأحرف.
type: docs
weight: 157
url: /ar/system.text/utf7encoding/getbytecount/
---
## UTF7Encoding::GetByteCount(const char_t *, int) طريقة

احصل على عدد الأحرف اللازمة لتشفير مخزن الأحرف.

```cpp
int System::Text::UTF7Encoding::GetByteCount(const char_t *chars, int count) override
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| chars | const char_t * | مخزن الأحرف. |
| count | int | [Buffer](../../../system/buffer/) الحجم. |

### قيمة الإرجاع

حجم المخزن المطلوب.

## UTF7Encoding::GetByteCount(ArrayPtr\<char_t\>, int, int) طريقة

احصل على عدد الأحرف اللازمة لتشفير مخزن الأحرف.

```cpp
virtual int System::Text::Encoding::GetByteCount(ArrayPtr<char_t> chars, int index, int count)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| chars | [ArrayPtr](../../../system/arrayptr/)\<char_t\> | مخزن الأحرف. |
| index | int | بداية القطعة. |
| count | int | حجم القطعة. |

### قيمة الإرجاع

حجم المخزن المطلوب.

## UTF7Encoding::GetByteCount(System::Details::ArrayView\<char_t\>, int, int) طريقة

احصل على عدد الأحرف اللازمة لتشفير مخزن الأحرف.

```cpp
virtual int System::Text::Encoding::GetByteCount(System::Details::ArrayView<char_t> chars, int index, int count)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| chars | System::Details::ArrayView\<char_t\> | مخزن الأحرف. |
| index | int | بداية القطعة. |
| count | int | حجم القطعة. |

### قيمة الإرجاع

حجم المخزن المطلوب.

## UTF7Encoding::GetByteCount(const System::Details::StackArray\<char_t, N\>\&, int, int) طريقة

احصل على عدد الأحرف اللازمة لتشفير مخزن الأحرف.

```cpp
template<std::size_t> int System::Text::Encoding::GetByteCount(const System::Details::StackArray<char_t, N> &chars, int index, int count)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| chars | const System::Details::StackArray\<char_t, N\>\& | مخزن الأحرف. |
| index | int | بداية القطعة. |
| count | int | حجم القطعة. |

### قيمة الإرجاع

حجم المخزن المطلوب.

## UTF7Encoding::GetByteCount(const String\&) طريقة

احصل على عدد الأحرف اللازمة لتشفير سلسلة.

```cpp
virtual int System::Text::Encoding::GetByteCount(const String &s)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| s | const [String](../../../system/string/)\& | [String](../../../system/string/) لتشفير. |

### قيمة الإرجاع

حجم المخزن المطلوب.

## UTF7Encoding::GetByteCount(ArrayPtr\<char_t\>) طريقة

احصل على عدد الأحرف اللازمة لتشفير مخزن الأحرف.

```cpp
virtual int System::Text::Encoding::GetByteCount(ArrayPtr<char_t> chars)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| chars | [ArrayPtr](../../../system/arrayptr/)\<char_t\> | مخزن الأحرف. |

### قيمة الإرجاع

حجم المخزن المطلوب.

## UTF7Encoding::GetByteCount(const char_t *, int) طريقة

احصل على عدد الأحرف اللازمة لتشفير مخزن الأحرف.

```cpp
virtual int System::Text::Encoding::GetByteCount(const char_t *chars, int count)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| chars | const char_t * | مخزن الأحرف. |
| count | int | [Buffer](../../../system/buffer/) الحجم. |

### قيمة الإرجاع

حجم المخزن المطلوب.

## انظر أيضًا

* تعريف نوع [ArrayPtr](../../../system/arrayptr/)
* فئة [UTF7Encoding](../)
* فئة [String](../../../system/string/)
* نطاق [System::Text](../../)
* مكتبة [Aspose.Slides](../../../)