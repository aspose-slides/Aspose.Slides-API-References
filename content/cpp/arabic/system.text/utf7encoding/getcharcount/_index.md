---
title: GetCharCount()
second_title: مرجع API لـ Aspose.Slides للـ C++
description: احصل على عدد الأحرف المطلوبة لفك ترميز مخزن بايت.
type: docs
weight: 79
url: /ar/system.text/utf7encoding/getcharcount/
---
## UTF7Encoding::GetCharCount(ArrayPtr\<uint8_t\>, int, int) طريقة

احصل على عدد الأحرف المطلوبة لفك ترميز مخزن بايت.

```cpp
int System::Text::UTF7Encoding::GetCharCount(ArrayPtr<uint8_t> bytes, int index, int count) override
```

### الوسائط

| المعلمة | النوع | الوصف |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | البايتات المراد فك ترميزها. |
| index | int | بداية الجزء. |
| count | int | حجم الجزء. |

### قيمة الإرجاع

عدد الأحرف.

## UTF7Encoding::GetCharCount(const uint8_t *, int) طريقة

احصل على عدد الأحرف المطلوبة لفك ترميز مخزن بايت.

```cpp
int System::Text::UTF7Encoding::GetCharCount(const uint8_t *bytes, int count) override
```

### الوسائط

| المعلمة | النوع | الوصف |
| --- | --- | --- |
| bytes | const **uint8_t** * | البايتات المراد فك ترميزها. |
| count | int | عدد البايتات. |

### قيمة الإرجاع

عدد الأحرف.

## UTF7Encoding::GetCharCount(ArrayPtr\<uint8_t\>, int, int) طريقة

احصل على عدد الأحرف المطلوبة لفك ترميز مخزن بايت.

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes, int index, int count)
```

### الوسائط

| المعلمة | النوع | الوصف |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | البايتات المراد فك ترميزها. |
| index | int | بداية الجزء. |
| count | int | حجم الجزء. |

### قيمة الإرجاع

عدد الأحرف.

## UTF7Encoding::GetCharCount(ArrayPtr\<uint8_t\>) طريقة

احصل على عدد الأحرف المطلوبة لفك ترميز مخزن بايت.

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes)
```

### الوسائط

| المعلمة | النوع | الوصف |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | البايتات المراد فك ترميزها. |

### قيمة الإرجاع

عدد الأحرف.

## UTF7Encoding::GetCharCount(const uint8_t *, int) طريقة

احصل على عدد الأحرف المطلوبة لفك ترميز مخزن بايت.

```cpp
virtual int System::Text::Encoding::GetCharCount(const uint8_t *bytes, int count)
```

### الوسائط

| المعلمة | النوع | الوصف |
| --- | --- | --- |
| bytes | const **uint8_t** * | البايتات المراد فك ترميزها. |
| count | int | عدد البايتات. |

### قيمة الإرجاع

عدد الأحرف.

## أنظر أيضًا

* Typedef [ArrayPtr](../../../system/arrayptr/)
* Class [UTF7Encoding](../)
* Namespace [System::Text](../../)
* Library [Aspose.Slides](../../../)