---
title: GetBytes()
second_title: مرجع API Aspose.Slides للغة C++
description: يحول القيمة المنطقية المحددة إلى مصفوفة من البايتات.
type: docs
weight: 14
url: /ar/system/bitconverter/getbytes/
---
## BitConverter::GetBytes(bool) طريقة


يحوِّل القيمة المنطقية المحددة إلى مصفوفة من البايتات.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(bool value)
```


### المعلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **bool** | قيمة منطقية للتحويل |

### قيمة الإرجاع

مصفوفة بايت بطول 1 تمثل القيمة المحددة

## BitConverter::GetBytes(char_t) طريقة


يحوِّل قيمة الـ char_t المحددة إلى مصفوفة من البايتات.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(char_t value)
```


### المعلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | char_t | قيمة char_t للتحويل |

### قيمة الإرجاع

مصفوفة بايت بطول 2 تمثل القيمة المحددة

## BitConverter::GetBytes(int16_t) طريقة


يحوِّل القيمة الصحيحة 16-بت المحددة إلى مصفوفة من البايتات.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int16_t value)
```


### المعلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **int16_t** | قيمة صحيحة 16-بت للتحويل |

### قيمة الإرجاع

مصفوفة بايت بطول 2 تمثل القيمة المحددة

## BitConverter::GetBytes(int) طريقة


يحوِّل القيمة الصحيحة 32-بت المحددة إلى مصفوفة من البايتات.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int value)
```


### المعلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | int | قيمة صحيحة 32-بت للتحويل |

### قيمة الإرجاع

مصفوفة بايت بطول 4 تمثل القيمة المحددة

## BitConverter::GetBytes(int64_t) طريقة


يحوِّل القيمة الصحيحة 64-بت المحددة إلى مصفوفة من البايتات.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int64_t value)
```


### المعلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **int64_t** | قيمة صحيحة 64-بت للتحويل |

### قيمة الإرجاع

مصفوفة بايت بطول 8 تمثل القيمة المحددة

## BitConverter::GetBytes(uint16_t) طريقة


يحوِّل القيمة غير الموقعة 16-بت المحددة إلى مصفوفة من البايتات.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint16_t value)
```


### المعلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **uint16_t** | قيمة غير موقعة 16-بت للتحويل |

### قيمة الإرجاع

مصفوفة بايت بطول 2 تمثل القيمة المحددة

## BitConverter::GetBytes(uint32_t) طريقة


يحوِّل القيمة غير الموقعة 32-بت المحددة إلى مصفوفة من البايتات.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint32_t value)
```


### المعلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **uint32_t** | قيمة غير موقعة 32-بت للتحويل |

### قيمة الإرجاع

مصفوفة بايت بطول 4 تمثل القيمة المحددة

## BitConverter::GetBytes(uint64_t) طريقة


يحوِّل القيمة غير الموقعة 64-بت المحددة إلى مصفوفة من البايتات.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint64_t value)
```


### المعلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **uint64_t** | قيمة غير موقعة 64-بت للتحويل |

### قيمة الإرجاع

مصفوفة بايت بطول 8 تمثل القيمة المحددة

## BitConverter::GetBytes(float) طريقة


يحوِّل القيمة العائمة ذات الدقة الفردية المحددة إلى مصفوفة من البايتات.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(float value)
```


### المعلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **float** | قيمة عائمة للتحويل |

### قيمة الإرجاع

مصفوفة بايت بطول 4 تمثل القيمة المحددة

## BitConverter::GetBytes(double) طريقة


يحوِّل القيمة العائمة ذات الدقة المضاعفة المحددة إلى مصفوفة من البايتات.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(double value)
```


### المعلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **double** | قيمة عائمة للتحويل |

### قيمة الإرجاع

مصفوفة بايت بطول 8 تمثل القيمة المحددة

## انظر أيضا

* Typedef [ArrayPtr](../../arrayptr/)
* الفئة [BitConverter](../)
* مساحة الاسم [System](../../)
* المكتبة [Aspose.Slides](../../../)