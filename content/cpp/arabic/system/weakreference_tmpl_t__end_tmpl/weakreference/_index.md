---
title: WeakReference()
second_title: مرجع API Aspose.Slides للغة C++
description: المُنشئ الافتراضي.
type: docs
weight: 1
url: /ar/system/weakreference_tmpl_t__end_tmpl/weakreference/
---
## WeakReference< T >::WeakReference() طريقة

المُنشئ الافتراضي.

```cpp
System::WeakReference<T>::WeakReference()
```

## WeakReference< T >::WeakReference(std::nullptr_t) طريقة

المُنشئ من nullptr.

```cpp
System::WeakReference<T>::WeakReference(std::nullptr_t)
```

## WeakReference< T >::WeakReference(const SmartPtr\<T\>\&) طريقة

ينشئ نسخة جديدة من الفئة WeakReference، تشير إلى الكائن المحدد.

```cpp
System::WeakReference<T>::WeakReference(const SmartPtr<T> &data)
```

### المعلمات

| Parameter | Type | Description |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) للتخزين. |

## WeakReference< T >::WeakReference(const SmartPtr\<T\>\&, bool) طريقة

ينشئ نسخة جديدة من الفئة WeakReference، تشير إلى الكائن المحدد.

```cpp
System::WeakReference<T>::WeakReference(const SmartPtr<T> &data, bool trackResurrection)
```

### المعلمات

| Parameter | Type | Description |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) للتخزين. |
| trackResurrection | **bool** | تم تجاهلها. |

## انظر أيضًا

* الفئة [WeakReference< T >](../)
* الفئة [SmartPtr](../../smartptr/)
* النطاق [System](../../)
* المكتبة [Aspose.Slides](../../../)