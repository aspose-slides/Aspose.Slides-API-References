---
title: Equals()
second_title: مرجع API لـ Aspose.Slides للغة C++
description: يحدد ما إذا كانت القيم التي يمثلها الكائن الحالي والكائن المحدد متساوية.
type: docs
weight: 313
url: /ar/system/decimal/equals/
---
## Decimal::Equals(const Decimal\&) const طريقة

يحدد ما إذا كانت القيم التي يمثلها الكائن الحالي والكائن المحدد متساوية.

```cpp
bool System::Decimal::Equals(const Decimal &d) const
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| d | const [Decimal](../)\& | كائن [Decimal](../) للمقارنة مع الكائن الحالي |

### قيمة الإرجاع

True إذا كانت القيمة التي يمثلها الكائنان الحالي والمحدد متساوية، وإلا - false

## Decimal::Equals(const SharedPtr\<Object\>\&) const طريقة

يحدد ما إذا كانت القيم التي يمثلها الكائن الحالي والكائن المحدد متساوية.

```cpp
bool System::Decimal::Equals(const SharedPtr<Object> &obj) const
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | الكائن للمقارنة مع الكائن الحالي |

### قيمة الإرجاع

True إذا كانت القيمة التي يمثلها الكائنان الحالي والمحدد متساوية، وإلا - false

## Decimal::Equals(const Decimal\&, const Decimal\&) طريقة

يحدد ما إذا كانت القيم التي يمثلها الكائنان المحددان متساوية.

```cpp
static bool System::Decimal::Equals(const Decimal &d1, const Decimal &d2)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| d1 | const [Decimal](../)\& | المقارن الأول |
| d2 | const [Decimal](../)\& | المقارن الثاني |

### قيمة الإرجاع

True إذا كانت القيم التي يمثلها **d1** و **d2** متساوية، وإلا - false

## أنظر أيضًا

* Typedef [SharedPtr](../../sharedptr/)
* Class [Decimal](../)
* Class [Object](../../object/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)