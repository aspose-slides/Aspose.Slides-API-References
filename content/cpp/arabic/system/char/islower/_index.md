---
title: IsLower()
second_title: مرجع API Aspose.Slides للغة C++
description: يحدد ما إذا كان الحرف الموجود في الفهرس المحدد داخل مخزن الأحرف المحدد مصنفًا كحرف صغير.
type: docs
weight: 196
url: /ar/system/char/islower/
---
## Char::IsLower(const char_t *, int) طريقة

يحدد ما إذا كان الحرف الموجود في الفهرس المحدد داخل مخزن الأحرف المحدد مصنفًا كحرف صغير.

```cpp
static bool System::Char::IsLower(const char_t *str, int idx)
```

### المعاملات

| Parameter | Type | Description |
| --- | --- | --- |
| str | const char_t * | مؤشر إلى بداية مخزن الأحرف |
| idx | int | فهرس يبدأ من الصفر داخل المخزن المحدد للحرف المراد اختباره |

### قيمة الإرجاع

صحيح إذا كان الحرف الموجود في الفهرس المحدد حرفًا صغيرًا، وإلا - خطأ

## Char::IsLower(char_t) طريقة

يحدد ما إذا كان الحرف المحدد مصنفًا كحرف صغير.

```cpp
static bool System::Char::IsLower(char_t c)
```

### المعاملات

| Parameter | Type | Description |
| --- | --- | --- |
| c | char_t | الحرف المراد اختباره |

### قيمة الإرجاع

صحيح إذا كان الحرف المحدد حرفًا صغيرًا، وإلا - خطأ

## Char::IsLower(const String\&, int) طريقة

يحدد ما إذا كان الحرف الموجود في الفهرس المحدد داخل السلسلة المحددة مصنفًا كحرف صغير.

```cpp
static bool System::Char::IsLower(const String &str, int idx)
```

### المعاملات

| Parameter | Type | Description |
| --- | --- | --- |
| str | const [String](../../string/)\& | سلسلة نصية |
| idx | int | فهرس يبدأ من الصفر داخل السلسلة المحددة للاختبار |

### قيمة الإرجاع

صحيح إذا كان الحرف الموجود في الفهرس المحدد حرفًا صغيرًا، وإلا - خطأ

## انظر أيضًا

* الفئة [Char](../)
* الفئة [String](../../string/)
* النطاق [System](../../)
* المكتبة [Aspose.Slides](../../../)