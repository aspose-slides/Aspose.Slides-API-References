---
title: WriteLine()
second_title: مرجع Aspose.Slides للغة C++ API
description: يُخرج فاصل السطر الحالي إلى تدفق الإخراج القياسي.
type: docs
weight: 14
url: /ar/system/console/writeline/
---
## Console::WriteLine() طريقة

يُخرج فاصل السطر الحالي إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::WriteLine()
```

## Console::WriteLine(const SharedPtr\<T\>\&) طريقة

يُخرج تمثيل السلسلة للكائن المحدد متبوعًا بفاصل السطر الحالي إلى تدفق الإخراج القياسي.

```cpp
template<class T> static void System::Console::WriteLine(const SharedPtr<T> &object)
```

### معلمات القالب

| معامل | الوصف |
| --- | --- |
| T | نوع الكائن الذي سيتم إخراجه |

### الوسائط

| معامل | نوع | الوصف |
| --- | --- | --- |
| object | const [SharedPtr](../../sharedptr/)\<T\>\& | [Object](../../object/) للإخراج |

## Console::WriteLine(bool) طريقة

يُخرج تمثيل السلسلة لقيمة bool متبوعًا بفاصل السطر الحالي إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::WriteLine(bool value)
```

### الوسائط

| معامل | نوع | الوصف |
| --- | --- | --- |
| value | **bool** | القيمة المراد إخراجها |

## Console::WriteLine(char_t) طريقة

يُخرج قيمة الحرف المحدد متبوعًا بفاصل السطر الحالي إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::WriteLine(char_t value)
```

### الوسائط

| معامل | نوع | الوصف |
| --- | --- | --- |
| value | char_t | القيمة المراد إخراجها |

## Console::WriteLine(const ArrayPtr\<char_t\>\&) طريقة

يُخرج تمثيل السلسلة للمصفوفة الحرفية المحددة متبوعًا بفاصل السطر الحالي إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer)
```

### الوسائط

| معامل | نوع | الوصف |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | المصفوفة التي سيتم إخراجها |

## Console::WriteLine(const Decimal\&) طريقة

يُخرج تمثيل السلسلة لقيمة [Decimal](../../decimal/) متبوعًا بفاصل السطر الحالي إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::WriteLine(const Decimal &value)
```

### الوسائط

| معامل | نوع | الوصف |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | القيمة المراد إخراجها |

## Console::WriteLine(double) طريقة

يُخرج تمثيل السلسلة لقيمة ذات نقطة عائمة ذات دقة مزدوجة متبوعًا بفاصل السطر الحالي إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::WriteLine(double value)
```

### الوسائط

| معامل | نوع | الوصف |
| --- | --- | --- |
| value | **double** | القيمة المراد إخراجها |

## Console::WriteLine(float) طريقة

يُخرج تمثيل السلسلة لقيمة ذات نقطة عائمة ذات دقة أحادية متبوعًا بفاصل السطر الحالي إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::WriteLine(float value)
```

### الوسائط

| معامل | نوع | الوصف |
| --- | --- | --- |
| value | **float** | القيمة المراد إخراجها |

## Console::WriteLine(int32_t) طريقة

يُخرج تمثيل السلسلة لقيمة عدد صحيح 32-بت متبوعًا بفاصل السطر الحالي إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::WriteLine(int32_t value)
```

### الوسائط

| معامل | نوع | الوصف |
| --- | --- | --- |
| value | **int32_t** | القيمة المراد إخراجها |

## Console::WriteLine(int64_t) طريقة

يُخرج تمثيل السلسلة لقيمة عدد صحيح 64-بت متبوعًا بفاصل السطر الحالي إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::WriteLine(int64_t value)
```

### الوسائط

| معامل | نوع | الوصف |
| --- | --- | --- |
| value | **int64_t** | القيمة المراد إخراجها |

## Console::WriteLine(const String\&) طريقة

يُخرج كائن السلسلة المحدد متبوعًا بفاصل السطر الحالي إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::WriteLine(const String &value)
```

### الوسائط

| معامل | نوع | الوصف |
| --- | --- | --- |
| value | const [String](../../string/)\& | كائن السلسلة المراد إخراجه |

## Console::WriteLine(const char_t *) طريقة

يُخرج السلسلة c-string المحددة متبوعًا بفاصل السطر الحالي إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::WriteLine(const char_t *value)
```

### الوسائط

| معامل | نوع | الوصف |
| --- | --- | --- |
| value | const char_t * | السلسلة c-string المراد إخراجها |

## Console::WriteLine(const TypeInfo\&) طريقة

يُخرج تمثيل السلسلة لقيمة [TypeInfo](../../typeinfo/) متبوعًا بفاصل السطر الحالي إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::WriteLine(const TypeInfo &value)
```

### الوسائط

| معامل | نوع | الوصف |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | القيمة المراد إخراجها |

## Console::WriteLine(uint32_t) طريقة

يُخرج تمثيل السلسلة لقيمة عدد صحيح غير موقع 32-بت متبوعًا بفاصل السطر الحالي إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::WriteLine(uint32_t value)
```

### الوسائط

| معامل | نوع | الوصف |
| --- | --- | --- |
| value | **uint32_t** | القيمة المراد إخراجها |

## Console::WriteLine(uint64_t) طريقة

يُخرج تمثيل السلسلة لقيمة عدد صحيح غير موقع 64-بت متبوعًا بفاصل السطر الحالي إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::WriteLine(uint64_t value)
```

### الوسائط

| معامل | نوع | الوصف |
| --- | --- | --- |
| value | **uint64_t** | القيمة المراد إخراجها |

## Console::WriteLine(const ArrayPtr\<char_t\>\&, int, int) طريقة

يُخرج تمثيل السلسلة للنطاق المحدد من مصفوفة الحروف المحددة متبوعًا بفاصل السطر الحالي إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer, int index, int count)
```

### الوسائط

| معامل | نوع | الوصف |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | مصفوفة الحروف |
| index | int | الفهرس في المصفوفة حيث يبدأ النطاق المراد إخراجه |
| count | int | عدد العناصر في النطاق المراد إخراجه |

## Console::WriteLine(const Exception\&) طريقة

يُخرج تمثيل السلسلة لكائن Exception المحدد متبوعًا بفاصل السطر الحالي إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::WriteLine(const Exception &e)
```

### الوسائط

| معامل | نوع | الوصف |
| --- | --- | --- |
| e | const [Exception](../../exception/)\& | القيمة المراد إخراجها |

## Console::WriteLine(const String\&, Args\&&...) طريقة

يُخرج تمثيل السلسلة للوسائط المحددة بحسب التنسيق المحدد متبوعًا بفاصل السطر الحالي إلى تدفق الإخراج القياسي.

```cpp
template<class...> static void System::Console::WriteLine(const String &format, Args &&... args)
```

### معلمات القالب

| معامل | الوصف |
| --- | --- |
| ال | أنواع القيم المراد إخراجها |

### الوسائط

| معامل | نوع | الوصف |
| --- | --- | --- |
| format | const [String](../../string/)\& | تنسيق السلسلة |
| args | Args\&&... | القيم المراد إخراجها |

## Console::WriteLine(const char *) طريقة




```cpp
static void System::Console::WriteLine(const char *)=delete
```

## انظر أيضا

* تعريف [SharedPtr](../../sharedptr/)
* تعريف [ArrayPtr](../../arrayptr/)
* تعريف [Exception](../../exception/)
* فئة [Console](../)
* فئة [Decimal](../../decimal/)
* فئة [String](../../string/)
* فئة [TypeInfo](../../typeinfo/)
* مساحة الاسم [System](../../)
* مكتبة [Aspose.Slides](../../../)