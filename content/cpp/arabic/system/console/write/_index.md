---
title: Write()
second_title: مرجع API لـ Aspose.Slides للغة C++
description: يقوم بإخراج تمثيل السلسلة للكائن المحدد إلى تدفق الإخراج القياسي.
type: docs
weight: 1
url: /ar/system/console/write/
---
## Console::Write(const SharedPtr\<T\>\&) طريقة

يخرج تمثيل السلسلة للكائن المحدد إلى تدفق الإخراج القياسي.

```cpp
template<class T> static void System::Console::Write(const SharedPtr<T> &object)
```

### معاملات القالب

| Parameter | Description |
| --- | --- |
| T | نوع الكائن للإخراج |

### الوسائط

| Parameter | Type | Description |
| --- | --- | --- |
| object | const [SharedPtr](../../sharedptr/)\<T\>\& | [Object](../../object/) للإخراج |

## Console::Write(bool) طريقة

يخرج تمثيل السلسلة لقيمة bool إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::Write(bool value)
```

### الوسائط

| Parameter | Type | Description |
| --- | --- | --- |
| value | **bool** | القيمة للإخراج |

## Console::Write(char_t) طريقة

يخرج قيمة الحرف المحدد إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::Write(char_t value)
```

### الوسائط

| Parameter | Type | Description |
| --- | --- | --- |
| value | char_t | القيمة للإخراج |

## Console::Write(const ArrayPtr\<char_t\>\&) طريقة

يخرج تمثيل السلسلة للمصفوفة الحرفية المحددة إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::Write(const ArrayPtr<char_t> &buffer)
```

### الوسائط

| Parameter | Type | Description |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | المصفوفة للإخراج |

## Console::Write(const Decimal\&) طريقة

يخرج تمثيل السلسلة لقيمة [Decimal](../../decimal/) إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::Write(const Decimal &value)
```

### الوسائط

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | القيمة للإخراج |

## Console::Write(double) طريقة

يخرج تمثيل السلسلة لقيمة عدد عشري مزدوج الدقة إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::Write(double value)
```

### الوسائط

| Parameter | Type | Description |
| --- | --- | --- |
| value | **double** | القيمة للإخراج |

## Console::Write(float) طريقة

يخرج تمثيل السلسلة لقيمة عدد عشري أحادي الدقة إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::Write(float value)
```

### الوسائط

| Parameter | Type | Description |
| --- | --- | --- |
| value | **float** | القيمة للإخراج |

## Console::Write(int32_t) طريقة

يخرج تمثيل السلسلة لقيمة عدد صحيح 32-بت إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::Write(int32_t value)
```

### الوسائط

| Parameter | Type | Description |
| --- | --- | --- |
| value | **int32_t** | القيمة للإخراج |

## Console::Write(int64_t) طريقة

يخرج تمثيل السلسلة لقيمة عدد صحيح 64-بت إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::Write(int64_t value)
```

### الوسائط

| Parameter | Type | Description |
| --- | --- | --- |
| value | **int64_t** | القيمة للإخراج |

## Console::Write(const String\&) طريقة

يخرج كائن السلسلة المحدد إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::Write(const String &value)
```

### الوسائط

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | كائن السلسلة للإخراج |

## Console::Write(const char_t *) طريقة

يخرج السلسلة النصية C المحددة إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::Write(const char_t *value)
```

### الوسائط

| Parameter | Type | Description |
| --- | --- | --- |
| value | const char_t * | السلسلة النصية C للإخراج |

## Console::Write(const TypeInfo\&) طريقة

يخرج تمثيل السلسلة لقيمة [TypeInfo](../../typeinfo/) إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::Write(const TypeInfo &value)
```

### الوسائط

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | القيمة للإخراج |

## Console::Write(uint32_t) طريقة

يخرج تمثيل السلسلة لقيمة عدد صحيح غير موقع 32-بت إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::Write(uint32_t value)
```

### الوسائط

| Parameter | Type | Description |
| --- | --- | --- |
| value | **uint32_t** | القيمة للإخراج |

## Console::Write(uint64_t) طريقة

يخرج تمثيل السلسلة لقيمة عدد صحيح غير موقع 64-بت إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::Write(uint64_t value)
```

### الوسائط

| Parameter | Type | Description |
| --- | --- | --- |
| value | **uint64_t** | القيمة للإخراج |

## Console::Write(const ArrayPtr\<char_t\>\&, int32_t, int32_t) طريقة

يخرج تمثيل السلسلة للنطاق المحدد من المصفوفة الحرفية المحددة إلى تدفق الإخراج القياسي.

```cpp
static void System::Console::Write(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count)
```

### الوسائط

| Parameter | Type | Description |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | المصوفة الحرفية |
| index | **int32_t** | الفهرس في المصفوفة حيث يبدأ النطاق للإخراج |
| count | **int32_t** | عدد العناصر في النطاق للإخراج |

## Console::Write(const String\&, Args\&&...) طريقة

يخرج تمثيل السلسلة للوسائط المحددة التي تم تنسيقها وفقًا للصيغة المحددة إلى تدفق الإخراج القياسي.

```cpp
template<class...> static void System::Console::Write(const String &format, Args &&... args)
```

### معاملات القالب

| Parameter | Description |
| --- | --- |
| The | أنواع القيم للإخراج |

### الوسائط

| Parameter | Type | Description |
| --- | --- | --- |
| format | const [String](../../string/)\& | صيغة السلسلة |
| args | Args\&&... | القيم للإخراج |

## Console::Write(const char *) طريقة




```cpp
static void System::Console::Write(const char *)=delete
```

## انظر أيضًا

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Class [Console](../)
* Class [Decimal](../../decimal/)
* Class [String](../../string/)
* Class [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)