---
title: ToUInt16()
second_title: مرجع API لـ Aspose.Slides للغة C++
description: يحوِّل القيمة المنطقية المحددة إلى عدد صحيح غير موقع 16-bit مكافئ.
type: docs
weight: 144
url: /ar/system/convert/touint16/
---
## Convert::ToUInt16(bool) طريقة

يحوِّل القيمة المنطقية المحددة إلى عدد صحيح غير موقع 16-بت مكافئ.

```cpp
static constexpr uint16_t System::Convert::ToUInt16(bool value)
```

## Convert::ToUInt16(uint8_t) طريقة

يحوِّل عددًا صحيحًا غير موقع 8-بت محدد إلى عدد صحيح غير موقع 16-بت مكافئ.

```cpp
static constexpr uint16_t System::Convert::ToUInt16(uint8_t value)
```

## Convert::ToUInt16(int8_t) طريقة

يحوِّل عددًا صحيحًا موقعًا 8-بت محدد إلى عدد صحيح غير موقع 16-بت مكافئ.

```cpp
static uint16_t System::Convert::ToUInt16(int8_t value)
```

## Convert::ToUInt16(uint16_t) طريقة

يعيد عددًا صحيحًا غير موقع 16-بت محدد.

```cpp
static constexpr uint16_t System::Convert::ToUInt16(uint16_t value)
```

## Convert::ToUInt16(int16_t) طريقة

يحوِّل عددًا صحيحًا موقعًا 16-بت محدد إلى عدد صحيح غير موقع 16-بت مكافئ.

```cpp
static uint16_t System::Convert::ToUInt16(int16_t value)
```

## Convert::ToUInt16(uint32_t) طريقة

يحوِّل عددًا صحيحًا غير موقع 32-بت محدد إلى عدد صحيح غير موقع 16-بت مكافئ.

```cpp
static uint16_t System::Convert::ToUInt16(uint32_t value)
```

## Convert::ToUInt16(int32_t) طريقة

يحوِّل عددًا صحيحًا موقعًا 32-بت محدد إلى عدد صحيح غير موقع 16-بت مكافئ.

```cpp
static uint16_t System::Convert::ToUInt16(int32_t value)
```

## Convert::ToUInt16(uint64_t) طريقة

يحوِّل عددًا صحيحًا غير موقع 64-بت محدد إلى عدد صحيح غير موقع 16-بت مكافئ.

```cpp
static uint16_t System::Convert::ToUInt16(uint64_t value)
```

## Convert::ToUInt16(int64_t) طريقة

يحوِّل عددًا صحيحًا موقعًا 64-بت محدد إلى عدد صحيح غير موقع 16-بت مكافئ.

```cpp
static uint16_t System::Convert::ToUInt16(int64_t value)
```

## Convert::ToUInt16(float) طريقة

يحوِّل عددًا عشريًا مفردًا محدد إلى عدد صحيح غير موقع 16-بت مكافئ.

```cpp
static uint16_t System::Convert::ToUInt16(float value)
```

## Convert::ToUInt16(double) طريقة

يحوِّل عددًا عشريًا مزدوجًا محدد إلى عدد صحيح غير موقع 16-بت مكافئ.

```cpp
static uint16_t System::Convert::ToUInt16(double value)
```

## Convert::ToUInt16(const Decimal\&) طريقة

يحوِّل عددًا عشريًا محدد إلى عدد صحيح غير موقع 16-بت مكافئ.

```cpp
static uint16_t System::Convert::ToUInt16(const Decimal &value)
```

## Convert::ToUInt16(char_t) طريقة

يحوِّل حرفًا يونيكود محدد إلى عدد صحيح غير موقع 16-بت مكافئ.

```cpp
static constexpr uint16_t System::Convert::ToUInt16(char_t value)
```

## Convert::ToUInt16(DateTime) طريقة

التحويل غير مدعوم. دائمًا يرمي استثناء InvalidCastException.

```cpp
static uint16_t System::Convert::ToUInt16(DateTime value)
```

## Convert::ToUInt16(std::nullptr_t) طريقة

يحوِّل السلسلة الفارغة المحددة إلى قيمة عدد صحيح غير موقع 16-بت مكافئ.

```cpp
static constexpr uint16_t System::Convert::ToUInt16(std::nullptr_t)
```

### قيمة الإرجاع

صفر.

## Convert::ToUInt16(const char_t *) طريقة

يحوِّل السلسلة C المحددة التي تحتوي على تمثيل رقمي لعدد إلى قيمة عدد صحيح غير موقع 16-بت مكافئ.

```cpp
static uint16_t System::Convert::ToUInt16(const char_t *value)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | const char_t * | السلسلة C التي سيتم تحويلها |

### قيمة الإرجاع

قيمة عدد صحيح غير موقع 16-بت مكافئ للعدد الممثل بالسلسلة C المحددة

## Convert::ToUInt16(const String\&) طريقة

يحوِّل السلسلة المحددة التي تحتوي على تمثيل رقمي لعدد إلى قيمة عدد صحيح غير موقع 16-بت مكافئ.

```cpp
static uint16_t System::Convert::ToUInt16(const String &value)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | const [String](../../string/)\& | السلسلة التي سيتم تحويلها |

### قيمة الإرجاع

قيمة عدد صحيح غير موقع 16-بت مكافئ للعدد الممثل بالسلسلة المحددة

## Convert::ToUInt16(const String\&, int) طريقة

يحوِّل السلسلة المحددة التي تحتوي على تمثيل رقمي لعدد في القاعدة المحددة إلى قيمة عدد صحيح غير موقع 16-بت مكافئ.

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, int from_base)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | const [String](../../string/)\& | السلسلة التي سيتم تحويلها |
| from_base | int | قاعدة العدد الممثل بالسلسلة |

### قيمة الإرجاع

قيمة عدد صحيح غير موقع 16-بت مكافئ للعدد الممثل بالسلسلة المحددة

## Convert::ToUInt16(const String\&, const SharedPtr\<IFormatProvider\>\&) طريقة

يحوِّل السلسلة المحددة التي تحتوي على تمثيل رقمي لعدد إلى قيمة عدد صحيح غير موقع 16-بت مكافئ باستخدام معلومات التنسيق المقدمة.

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, const SharedPtr<IFormatProvider> &provider)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | const [String](../../string/)\& | السلسلة التي سيتم تحويلها |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | مؤشر إلى كائن يحتوي على معلومات تنسيق السلسلة |

### قيمة الإرجاع

قيمة عدد صحيح غير موقع 16-بت مكافئ للعدد الممثل بالسلسلة المحددة

## Convert::ToUInt16(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToUInt16(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToUInt16(const String\&, std::nullptr_t) طريقة

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, std::nullptr_t)
```

## Convert::ToUInt16(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) طريقة

يحوِّل السلسلة المحددة التي تحتوي على تمثيل رقمي لعدد إلى قيمة عدد صحيح غير موقع 16-بت مكافئ باستخدام معلومات التنسيق المقدمة ونمط الرقم.

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | const [String](../../string/)\& | السلسلة التي سيتم تحويلها |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | مزيج بتّي من قيم تعداد NumberStyles يحدد النمط المسموح به لتمثيل الرقم في السلسلة |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | مؤشر إلى كائن يحتوي على معلومات تنسيق السلسلة |

### قيمة الإرجاع

قيمة عدد صحيح غير موقع 16-بت مكافئ للعدد الممثل بالسلسلة المحددة

## Convert::ToUInt16(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToUInt16(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToUInt16(const String\&, Globalization::NumberStyles, std::nullptr_t) طريقة

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToUInt16(Enum) طريقة

```cpp
template<typename Enum,typename> static uint16_t System::Convert::ToUInt16(Enum value)
```

## Convert::ToUInt16(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) طريقة

يحوِّل القيمة المعلَّبة المحددة إلى قيمة عدد صحيح غير موقع 16-بت مكافئ.

```cpp
static uint16_t System::Convert::ToUInt16(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | مؤشر ذي مشاركة إلى الكائن الذي يعلّب القيمة التي سيتم تحويلها |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | تنسيق السلسلة الذي سيُستخدم إذا كان نوع القيمة المعلَّبة هو [String](../../string/) |

### قيمة الإرجاع

قيمة عدد صحيح غير موقع 16-بت مكافئ للقيمة المعلَّبة المحددة

## انظر أيضًا

* تعداد [NumberStyles](../../../system.globalization/numberstyles/)
* تعريف نوع [SharedPtr](../../sharedptr/)
* الفئة [Decimal](../../decimal/)
* الفئة [DateTime](../../datetime/)
* الفئة [String](../../string/)
* الفئة [IFormatProvider](../../iformatprovider/)
* الفئة [CultureInfo](../../../system.globalization/cultureinfo/)
* الفئة [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* الفئة [Object](../../object/)
* البنية [Convert](../)
* البنية [Enum](../../enum/)
* النطاق [System](../../)
* المكتبة [Aspose.Slides](../../../)