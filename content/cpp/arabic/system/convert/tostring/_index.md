---
title: ToString()
second_title: مرجع واجهة برمجة التطبيقات Aspose.Slides for C++
description: يحوّل القيمة المحددة إلى تمثيلها كسلسلة نصية.
type: docs
weight: 261
url: /ar/system/convert/tostring/
---
## Convert::ToString(int8_t) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها النصي.

```cpp
static String System::Convert::ToString(int8_t value)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **int8_t** | القيمة المراد تحويلها. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة

## Convert::ToString(uint8_t) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها النصي.

```cpp
static String System::Convert::ToString(uint8_t value)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **uint8_t** | القيمة المراد تحويلها. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة

## Convert::ToString(int16_t) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها النصي.

```cpp
static String System::Convert::ToString(int16_t value)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **int16_t** | القيمة المراد تحويلها. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة

## Convert::ToString(uint16_t) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها النصي.

```cpp
static String System::Convert::ToString(uint16_t value)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **uint16_t** | القيمة المراد تحويلها. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة

## Convert::ToString(int32_t) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها النصي.

```cpp
static String System::Convert::ToString(int32_t value)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **int32_t** | القيمة المراد تحويلها. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة

## Convert::ToString(uint32_t) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها النصي.

```cpp
static String System::Convert::ToString(uint32_t value)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **uint32_t** | القيمة المراد تحويلها. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة

## Convert::ToString(int64_t) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها النصي.

```cpp
static String System::Convert::ToString(int64_t value)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **int64_t** | القيمة المراد تحويلها. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة

## Convert::ToString(uint64_t) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها النصي.

```cpp
static String System::Convert::ToString(uint64_t value)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **uint64_t** | القيمة المراد تحويلها. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة

## Convert::ToString(float) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها النصي.

```cpp
static String System::Convert::ToString(float value)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **float** | القيمة المراد تحويلها. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة

## Convert::ToString(double) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها النصي.

```cpp
static String System::Convert::ToString(double value)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **double** | القيمة المراد تحويلها. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة

## Convert::ToString(const Decimal\&) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها النصي.

```cpp
static String System::Convert::ToString(const Decimal &value)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | القيمة المراد تحويلها. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة.

## Convert::ToString(DateTime) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها النصي.

```cpp
static String System::Convert::ToString(DateTime value)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | [DateTime](../../datetime/) | القيمة المراد تحويلها. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة.

## Convert::ToString(int8_t, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى نص باستخدام معلومات التنسيق الخاصة بالثقافة.

```cpp
static String System::Convert::ToString(int8_t value, const SharedPtr<IFormatProvider> &provider)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **int8_t** | القيمة المراد تحويلها. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة.

## Convert::ToString(int8_t, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(int8_t value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(int8_t, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(int8_t value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(int8_t, std::nullptr_t) طريقة

```cpp
static String System::Convert::ToString(int8_t value, std::nullptr_t)
```

## Convert::ToString(uint8_t, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى نص باستخدام معلومات التنسيق الخاصة بالثقافة.

```cpp
static String System::Convert::ToString(uint8_t value, const SharedPtr<IFormatProvider> &provider)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **uint8_t** | القيمة المراد تحويلها. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة.

## Convert::ToString(uint8_t, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(uint8_t value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(uint8_t, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(uint8_t value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(uint8_t, std::nullptr_t) طريقة

```cpp
static String System::Convert::ToString(uint8_t value, std::nullptr_t)
```

## Convert::ToString(int16_t, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى نص باستخدام معلومات التنسيق الخاصة بالثقافة.

```cpp
static String System::Convert::ToString(int16_t value, const SharedPtr<IFormatProvider> &provider)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **int16_t** | القيمة المراد تحويلها. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة.

## Convert::ToString(int16_t, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(int16_t value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(int16_t, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(int16_t value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(int16_t, std::nullptr_t) طريقة

```cpp
static String System::Convert::ToString(int16_t value, std::nullptr_t)
```

## Convert::ToString(uint16_t, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى نص باستخدام معلومات التنسيق الخاصة بالثقافة.

```cpp
static String System::Convert::ToString(uint16_t value, const SharedPtr<IFormatProvider> &provider)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **uint16_t** | القيمة المراد تحويلها. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة.

## Convert::ToString(uint16_t, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(uint16_t value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(uint16_t, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(uint16_t value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(uint16_t, std::nullptr_t) طريقة

```cpp
static String System::Convert::ToString(uint16_t value, std::nullptr_t)
```

## Convert::ToString(int32_t, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى نص باستخدام معلومات التنسيق الخاصة بالثقافة.

```cpp
static String System::Convert::ToString(int32_t value, const SharedPtr<IFormatProvider> &provider)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **int32_t** | القيمة المراد تحويلها. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة.

## Convert::ToString(int32_t, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(int32_t value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(int32_t, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(int32_t value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(int32_t, std::nullptr_t) طريقة

```cpp
static String System::Convert::ToString(int32_t value, std::nullptr_t)
```

## Convert::ToString(uint32_t, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى نص باستخدام معلومات التنسيق الخاصة بالثقافة.

```cpp
static String System::Convert::ToString(uint32_t value, const SharedPtr<IFormatProvider> &provider)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **uint32_t** | القيمة المراد تحويلها. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة.

## Convert::ToString(uint32_t, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(uint32_t value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(uint32_t, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(uint32_t value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(uint32_t, std::nullptr_t) طريقة

```cpp
static String System::Convert::ToString(uint32_t value, std::nullptr_t)
```

## Convert::ToString(int64_t, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى نص باستخدام معلومات التنسيق الخاصة بالثقافة.

```cpp
static String System::Convert::ToString(int64_t value, const SharedPtr<IFormatProvider> &provider)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **int64_t** | القيمة المراد تحويلها. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة.

## Convert::ToString(int64_t, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(int64_t value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(int64_t, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(int64_t value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(int64_t, std::nullptr_t) طريقة

```cpp
static String System::Convert::ToString(int64_t value, std::nullptr_t)
```

## Convert::ToString(uint64_t, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى نص باستخدام معلومات التنسيق الخاصة بالثقافة.

```cpp
static String System::Convert::ToString(uint64_t value, const SharedPtr<IFormatProvider> &provider)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **uint64_t** | القيمة المراد تحويلها. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة.

## Convert::ToString(uint64_t, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(uint64_t value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(uint64_t, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(uint64_t value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(uint64_t, std::nullptr_t) طريقة

```cpp
static String System::Convert::ToString(uint64_t value, std::nullptr_t)
```

## Convert::ToString(float, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى نص باستخدام معلومات التنسيق الخاصة بالثقافة.

```cpp
static String System::Convert::ToString(float value, const SharedPtr<IFormatProvider> &provider)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **float** | القيمة المراد تحويلها. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة.

## Convert::ToString(float, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(float value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(float, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(float value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(float, std::nullptr_t) طريقة

```cpp
static String System::Convert::ToString(float value, std::nullptr_t)
```

## Convert::ToString(double, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى نص باستخدام معلومات التنسيق الخاصة بالثقافة.

```cpp
static String System::Convert::ToString(double value, const SharedPtr<IFormatProvider> &provider)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **double** | القيمة المراد تحويلها. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة.

## Convert::ToString(double, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(double value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(double, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(double value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(double, std::nullptr_t) طريقة

```cpp
static String System::Convert::ToString(double value, std::nullptr_t)
```

## Convert::ToString(const Decimal\&, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى نص باستخدام معلومات التنسيق الخاصة بالثقافة.

```cpp
static String System::Convert::ToString(const Decimal &value, const SharedPtr<IFormatProvider> &provider)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | القيمة المراد تحويلها. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

التمثيل النصي للقيمة المحددة.

## Convert::ToString(const Decimal\&, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(const Decimal &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(const Decimal\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(const Decimal &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(const Decimal\&, std::nullptr_t) طريقة

```cpp
static String System::Convert::ToString(const Decimal &value, std::nullptr_t)
```
## Convert::ToString(DateTime, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى سلسلة باستخدام معلومات التنسيق الخاصة بالثقافة.

```cpp
static String System::Convert::ToString(DateTime value, const SharedPtr<IFormatProvider> &provider)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | [DateTime](../../datetime/) | القيمة المراد تحويلها. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة.

## Convert::ToString(DateTime, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(DateTime value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(DateTime, const SharedPtr\<Globalization::DateTimeFormatInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(DateTime value, const SharedPtr<Globalization::DateTimeFormatInfo> &dtfi)
```

## Convert::ToString(DateTime, std::nullptr_t) طريقة




```cpp
static String System::Convert::ToString(DateTime value, std::nullptr_t)
```

## Convert::ToString(int8_t, const String\&, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها السلسلي باستخدام تنسيق السلسلة المحدد ومعلومات التنسيق الخاصة بالثقافة المقدمة من كائن [IFormatProvider](../../iformatprovider/) المحدد.

```cpp
static String System::Convert::ToString(int8_t value, const String &format, const SharedPtr<IFormatProvider> &provider)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **int8_t** | القيمة المراد تحويلها. |
| format | const [String](../../string/)\& | تنسيق السلسلة. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة.

## Convert::ToString(int8_t, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(int8_t value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(int8_t, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(int8_t value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(int8_t, const String\&, std::nullptr_t) طريقة




```cpp
static String System::Convert::ToString(int8_t value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(uint8_t, const String\&, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها السلسلي باستخدام تنسيق السلسلة المحدد ومعلومات التنسيق الخاصة بالثقافة المقدمة من كائن [IFormatProvider](../../iformatprovider/) المحدد.

```cpp
static String System::Convert::ToString(uint8_t value, const String &format, const SharedPtr<IFormatProvider> &provider)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **uint8_t** | القيمة المراد تحويلها. |
| format | const [String](../../string/)\& | تنسيق السلسلة. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة.

## Convert::ToString(uint8_t, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(uint8_t value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(uint8_t, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(uint8_t value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(uint8_t, const String\&, std::nullptr_t) طريقة




```cpp
static String System::Convert::ToString(uint8_t value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(int16_t, const String\&, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها السلسلي باستخدام تنسيق السلسلة المحدد ومعلومات التنسيق الخاصة بالثقافة المقدمة من كائن [IFormatProvider](../../iformatprovider/) المحدد.

```cpp
static String System::Convert::ToString(int16_t value, const String &format, const SharedPtr<IFormatProvider> &provider)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **int16_t** | القيمة المراد تحويلها. |
| format | const [String](../../string/)\& | تنسيق السلسلة. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة.

## Convert::ToString(int16_t, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(int16_t value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(int16_t, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(int16_t value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(int16_t, const String\&, std::nullptr_t) طريقة




```cpp
static String System::Convert::ToString(int16_t value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(uint16_t, const String\&, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها السلسلي باستخدام تنسيق السلسلة المحدد ومعلومات التنسيق الخاصة بالثقافة المقدمة من كائن [IFormatProvider](../../iformatprovider/) المحدد.

```cpp
static String System::Convert::ToString(uint16_t value, const String &format, const SharedPtr<IFormatProvider> &provider)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **uint16_t** | القيمة المراد تحويلها. |
| format | const [String](../../string/)\& | تنسيق السلسلة. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة.

## Convert::ToString(uint16_t, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(uint16_t value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(uint16_t, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(uint16_t value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(uint16_t, const String\&, std::nullptr_t) طريقة




```cpp
static String System::Convert::ToString(uint16_t value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(int32_t, const String\&, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها السلسلي باستخدام تنسيق السلسلة المحدد ومعلومات التنسيق الخاصة بالثقافة المقدمة من كائن [IFormatProvider](../../iformatprovider/) المحدد.

```cpp
static String System::Convert::ToString(int32_t value, const String &format, const SharedPtr<IFormatProvider> &provider)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **int32_t** | القيمة المراد تحويلها. |
| format | const [String](../../string/)\& | تنسيق السلسلة. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة.

## Convert::ToString(int32_t, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(int32_t value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(int32_t, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(int32_t value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(int32_t, const String\&, std::nullptr_t) طريقة




```cpp
static String System::Convert::ToString(int32_t value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(uint32_t, const String\&, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها السلسلي باستخدام تنسيق السلسلة المحدد ومعلومات التنسيق الخاصة بالثقافة المقدمة من كائن [IFormatProvider](../../iformatprovider/) المحدد.

```cpp
static String System::Convert::ToString(uint32_t value, const String &format, const SharedPtr<IFormatProvider> &provider)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **uint32_t** | القيمة المراد تحويلها. |
| format | const [String](../../string/)\& | تنسيق السلسلة. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة.

## Convert::ToString(uint32_t, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(uint32_t value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(uint32_t, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(uint32_t value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(uint32_t, const String\&, std::nullptr_t) طريقة




```cpp
static String System::Convert::ToString(uint32_t value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(int64_t, const String\&, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها السلسلي باستخدام تنسيق السلسلة المحدد ومعلومات التنسيق الخاصة بالثقافة المقدمة من كائن [IFormatProvider](../../iformatprovider/) المحدد.

```cpp
static String System::Convert::ToString(int64_t value, const String &format, const SharedPtr<IFormatProvider> &provider)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **int64_t** | القيمة المراد تحويلها. |
| format | const [String](../../string/)\& | تنسيق السلسلة. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة.

## Convert::ToString(int64_t, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(int64_t value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(int64_t, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(int64_t value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(int64_t, const String\&, std::nullptr_t) طريقة




```cpp
static String System::Convert::ToString(int64_t value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(uint64_t, const String\&, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها السلسلي باستخدام تنسيق السلسلة المحدد ومعلومات التنسيق الخاصة بالثقافة المقدمة من كائن [IFormatProvider](../../iformatprovider/) المحدد.

```cpp
static String System::Convert::ToString(uint64_t value, const String &format, const SharedPtr<IFormatProvider> &provider)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **uint64_t** | القيمة المراد تحويلها. |
| format | const [String](../../string/)\& | تنسيق السلسلة. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة.

## Convert::ToString(uint64_t, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(uint64_t value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(uint64_t, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(uint64_t value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(uint64_t, const String\&, std::nullptr_t) طريقة




```cpp
static String System::Convert::ToString(uint64_t value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(float, const String\&, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها السلسلي باستخدام تنسيق السلسلة المحدد ومعلومات التنسيق الخاصة بالثقافة المقدمة من كائن [IFormatProvider](../../iformatprovider/) المحدد.

```cpp
static String System::Convert::ToString(float value, const String &format, const SharedPtr<IFormatProvider> &provider)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **float** | القيمة المراد تحويلها. |
| format | const [String](../../string/)\& | تنسيق السلسلة. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة.

## Convert::ToString(float, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(float value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(float, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(float value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(float, const String\&, std::nullptr_t) طريقة




```cpp
static String System::Convert::ToString(float value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(double, const String\&, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها السلسلي باستخدام تنسيق السلسلة المحدد ومعلومات التنسيق الخاصة بالثقافة المقدمة من كائن [IFormatProvider](../../iformatprovider/) المحدد.

```cpp
static String System::Convert::ToString(double value, const String &format, const SharedPtr<IFormatProvider> &provider)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **double** | القيمة المراد تحويلها. |
| format | const [String](../../string/)\& | تنسيق السلسلة. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة.

## Convert::ToString(double, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(double value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(double, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(double value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(double, const String\&, std::nullptr_t) طريقة




```cpp
static String System::Convert::ToString(double value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(const Decimal\&, const String\&, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها السلسلي باستخدام تنسيق السلسلة المحدد ومعلومات التنسيق الخاصة بالثقافة المقدمة من كائن [IFormatProvider](../../iformatprovider/) المحدد.

```cpp
static String System::Convert::ToString(const Decimal &value, const String &format, const SharedPtr<IFormatProvider> &provider)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | القيمة المراد تحويلها. |
| format | const [String](../../string/)\& | تنسيق السلسلة. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة.

## Convert::ToString(const Decimal\&, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(const Decimal &value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(const Decimal\&, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة




```cpp
static String System::Convert::ToString(const Decimal &value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(const Decimal\&, const String\&, std::nullptr_t) طريقة




```cpp
static String System::Convert::ToString(const Decimal &value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(DateTime, const String\&, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها السلسلي باستخدام تنسيق السلسلة المحدد ومعلومات التنسيق الخاصة بالثقافة المقدمة من كائن [IFormatProvider](../../iformatprovider/) المحدد.

```cpp
static String System::Convert::ToString(DateTime value, const String &format, const SharedPtr<IFormatProvider> &provider)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | [DateTime](../../datetime/) | القيمة المراد تحويلها. |
| format | const [String](../../string/)\& | تنسيق السلسلة. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | الكائن [IFormatProvider](../../iformatprovider/) الذي يوفر معلومات التنسيق الخاصة بالثقافة. |

### قيمة الإرجاع
تمثيل السلسلة للقيمة المحددة.

## Convert::ToString(DateTime, const String&, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(DateTime value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(DateTime, const String&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة

```cpp
static String System::Convert::ToString(DateTime value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(DateTime, const String&, std::nullptr_t) طريقة

```cpp
static String System::Convert::ToString(DateTime value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(const Guid\&) طريقة

يقوم بتحويل القيمة المحددة إلى سلسلة.

```cpp
static String System::Convert::ToString(const Guid &value)
```

### المعاملات

| معامل | نوع | وصف |
| --- | --- | --- |
| value | const [Guid](../../guid/)\& | القيمة المراد تحويلها |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة

## Convert::ToString(const Guid\&, const String\&) طريقة

يقوم بتحويل القيمة المحددة إلى سلسلة باستخدام تنسيق السلسلة المحدد.

```cpp
static String System::Convert::ToString(const Guid &value, const String &format)
```

### المعاملات

| معامل | نوع | وصف |
| --- | --- | --- |
| value | const [Guid](../../guid/)\& | القيمة المراد تحويلها |
| format | const [String](../../string/)\& | تنسيق السلسلة. |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة

## Convert::ToString(const char_t(&), std::nullptr_t) طريقة

يقوم بتحويل مصفوفة الأحرف Unicode المحددة إلى سلسلة.

```cpp
template<size_t> static String System::Convert::ToString(const char_t(&value)[N], std::nullptr_t=nullptr)
```

### معاملات القالب

| معامل | وصف |
| --- | --- |
| N | حجم المصفوفة. |

### المعاملات

| معامل | نوع | وصف |
| --- | --- | --- |
| value | const char_t(&) | المصفوفة المراد تحويلها. |

### قيمة الإرجاع

تمثيل السلسلة للمصفوفة المحددة.

## Convert::ToString(const char_t(&), const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل مصفوفة الأحرف Unicode المحددة إلى سلسلة باستخدام معلومات تنسيق الثقافة المحددة المقدمة من كائن [IFormatProvider](../../iformatprovider/).

```cpp
template<size_t> static String System::Convert::ToString(const char_t(&value)[N], const SharedPtr<IFormatProvider> &)
```

### معاملات القالب

| معامل | وصف |
| --- | --- |
| N | حجم المصفوفة. |

### المعاملات

| معامل | نوع | وصف |
| --- | --- | --- |
| value | const char_t(&) | المصفوفة المراد تحويلها. |

### قيمة الإرجاع

تمثيل السلسلة للمصفوفة المحددة.

## Convert::ToString(const char_t(&), const SharedPtr\<Globalization::CultureInfo\>\&) طريقة

```cpp
template<size_t> static String System::Convert::ToString(const char_t(&value)[N], const SharedPtr<Globalization::CultureInfo> &)
```

## Convert::ToString(const String\&, std::nullptr_t) طريقة

إرجاع القيمة المحددة؛ لا يتم إجراء تحويل.

```cpp
static String System::Convert::ToString(const String &value, std::nullptr_t=nullptr)
```

## Convert::ToString(const String\&, const SharedPtr\<IFormatProvider\>\&) طريقة

إرجاع القيمة المحددة؛ لا يتم إجراء تحويل.

```cpp
static String System::Convert::ToString(const String &value, const SharedPtr<IFormatProvider> &)
```

## Convert::ToString(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة

إرجاع القيمة المحددة؛ لا يتم إجراء تحويل.

```cpp
static String System::Convert::ToString(const String &value, const SharedPtr<Globalization::CultureInfo> &)
```

## Convert::ToString(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة

إرجاع القيمة المحددة؛ لا يتم إجراء تحويل.

```cpp
static String System::Convert::ToString(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &)
```

## Convert::ToString(char_t, std::nullptr_t) طريقة

إرجاع القيمة المحددة؛ لا يتم إجراء تحويل.

```cpp
static String System::Convert::ToString(char_t value, std::nullptr_t=nullptr)
```

## Convert::ToString(char_t, const SharedPtr\<IFormatProvider\>\&) طريقة

إرجاع القيمة المحددة؛ لا يتم إجراء تحويل.

```cpp
static String System::Convert::ToString(char_t value, const SharedPtr<IFormatProvider> &)
```

## Convert::ToString(char_t, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة

إرجاع القيمة المحددة؛ لا يتم إجراء تحويل.

```cpp
static String System::Convert::ToString(char_t value, const SharedPtr<Globalization::CultureInfo> &)
```

## Convert::ToString(char_t, const String\&, const SharedPtr\<IFormatProvider\>\&) طريقة

إرجاع القيمة المحددة؛ لا يتم إجراء تحويل.

```cpp
static String System::Convert::ToString(char_t value, const String &, const SharedPtr<IFormatProvider> &)
```

## Convert::ToString(char_t, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة

إرجاع القيمة المحددة؛ لا يتم إجراء تحويل.

```cpp
static String System::Convert::ToString(char_t value, const String &, const SharedPtr<Globalization::CultureInfo> &)
```

## Convert::ToString(char_t, const String\&, std::nullptr_t) طريقة

إرجاع القيمة المحددة؛ لا يتم إجراء تحويل.

```cpp
static String System::Convert::ToString(char_t value, const String &, std::nullptr_t)
```

## Convert::ToString(bool, std::nullptr_t) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها كسلسلة.

```cpp
static String System::Convert::ToString(bool value, std::nullptr_t=nullptr)
```

### المعاملات

| معامل | نوع | وصف |
| --- | --- | --- |
| value | **bool** | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة

## Convert::ToString(bool, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها كسلسلة.

```cpp
static String System::Convert::ToString(bool value, const SharedPtr<IFormatProvider> &)
```

### المعاملات

| معامل | نوع | وصف |
| --- | --- | --- |
| value | **bool** | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة

## Convert::ToString(bool, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها كسلسلة.

```cpp
static String System::Convert::ToString(bool value, const SharedPtr<Globalization::CultureInfo> &)
```

### المعاملات

| معامل | نوع | وصف |
| --- | --- | --- |
| value | **bool** | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة

## Convert::ToString(bool, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها كسلسلة.

```cpp
static String System::Convert::ToString(bool value, const SharedPtr<Globalization::NumberFormatInfo> &)
```

### المعاملات

| معامل | نوع | وصف |
| --- | --- | --- |
| value | **bool** | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة

## Convert::ToString(bool, const String\&, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها كسلسلة.

```cpp
static String System::Convert::ToString(bool value, const String &, const SharedPtr<IFormatProvider> &)
```

### المعاملات

| معامل | نوع | وصف |
| --- | --- | --- |
| value | **bool** | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة

## Convert::ToString(bool, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها كسلسلة.

```cpp
static String System::Convert::ToString(bool value, const String &, const SharedPtr<Globalization::CultureInfo> &)
```

### المعاملات

| معامل | نوع | وصف |
| --- | --- | --- |
| value | **bool** | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة

## Convert::ToString(bool, const String\&, std::nullptr_t) طريقة

يقوم بتحويل القيمة المحددة إلى تمثيلها كسلسلة.

```cpp
static String System::Convert::ToString(bool value, const String &, std::nullptr_t)
```

### المعاملات

| معامل | نوع | وصف |
| --- | --- | --- |
| value | **bool** | القيمة المراد تحويلها. |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة

## Convert::ToString(int8_t, int) طريقة

يقوم بتحويل القيمة العددية المحددة إلى تمثيلها كسلسلة في الأساس المحدد.

```cpp
static String System::Convert::ToString(int8_t value, int to_base)
```

### المعاملات

| معامل | نوع | وصف |
| --- | --- | --- |
| value | **int8_t** | القيمة المراد تحويلها |
| to_base | int | الأساس الذي سيتم تمثيل القيمة المحددة به |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة في الأساس المحدد

## Convert::ToString(int16_t, int) طريقة

يقوم بتحويل القيمة العددية المحددة إلى تمثيلها كسلسلة في الأساس المحدد.

```cpp
static String System::Convert::ToString(int16_t value, int to_base)
```

### المعاملات

| معامل | نوع | وصف |
| --- | --- | --- |
| value | **int16_t** | القيمة المراد تحويلها |
| to_base | int | الأساس الذي سيتم تمثيل القيمة المحددة به |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة في الأساس المحدد

## Convert::ToString(int32_t, int) طريقة

يقوم بتحويل القيمة العددية المحددة إلى تمثيلها كسلسلة في الأساس المحدد.

```cpp
static String System::Convert::ToString(int32_t value, int to_base)
```

### المعاملات

| معامل | نوع | وصف |
| --- | --- | --- |
| value | **int32_t** | القيمة المراد تحويلها |
| to_base | int | الأساس الذي سيتم تمثيل القيمة المحددة به |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة في الأساس المحدد

## Convert::ToString(int64_t, int) طريقة

يقوم بتحويل القيمة العددية المحددة إلى تمثيلها كسلسلة في الأساس المحدد.

```cpp
static 
```

### المعاملات

| معامل | نوع | وصف |
| --- | --- | --- |
| value | **int64_t** | القيمة المراد تحويلها |
| to_base | int | الأساس الذي سيتم تمثيل القيمة المحددة به |

### قيمة الإرجاع

تمثيل السلسلة للقيمة المحددة في الأساس المحدد

## Convert::ToString(uint8_t, int) طريقة

```cpp
static String System::Convert::ToString(uint8_t value, int to_base)
```

## Convert::ToString(uint16_t, int) طريقة

```cpp
static String System::Convert::ToString(uint16_t value, int to_base)
```

## Convert::ToString(uint32_t, int) طريقة

```cpp
static String System::Convert::ToString(uint32_t value, int to_base)
```

## Convert::ToString(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المغلَّفة المحددة إلى تمثيلها كسلسلة. إذا كان نوع القيمة المغلَّفة هو [String](../../string/)، يتم استخدام تنسيق السلسلة المحدد أثناء التحويل.

```cpp
static String System::Convert::ToString(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```

### المعاملات

| معامل | نوع | وصف |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | مؤشر المشاركة إلى الكائن الذي يغلف القيمة المراد تحويلها |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | تنسيق السلسلة الذي سيُستخدم إذا كان نوع القيمة المغلَّفة هو [String](../../string/) |

### قيمة الإرجاع

سلسلة تمثل القيمة المغلَّفة المحددة

## انظر أيضاً

* تعريف النوع [SharedPtr](../../sharedptr/)
* فئة [String](../../string/)
* فئة [Decimal](../../decimal/)
* فئة [DateTime](../../datetime/)
* فئة [IFormatProvider](../../iformatprovider/)
* فئة [CultureInfo](../../../system.globalization/cultureinfo/)
* فئة [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* فئة [DateTimeFormatInfo](../../../system.globalization/datetimeformatinfo/)
* فئة [Guid](../../guid/)
* فئة [Object](../../object/)
* هيكل [Convert](../)
* مساحة الاسم [System](../../)
* مكتبة [Aspose.Slides](../../../)