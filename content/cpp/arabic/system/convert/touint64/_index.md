---
title: ToUInt64()
second_title: مرجع API لـ Aspose.Slides للغة C++
description: يقوم بتحويل القيمة المنطقية المحددة إلى عدد صحيح غير موقع 64-بت معادل.
type: docs
weight: 196
url: /ar/system/convert/touint64/
---
## Convert::ToUInt64(bool) طريقة

يقوم بتحويل القيمة المنطقية المحددة إلى عدد صحيح غير موقع 64-بت معادل.

```cpp
static constexpr uint64_t System::Convert::ToUInt64(bool value)
```

## Convert::ToUInt64(uint8_t) طريقة

يقوم بتحويل العدد الصحيح غير الموقع ذو 8-بت المحدد إلى عدد صحيح غير موقع 64-بت معادل.

```cpp
static constexpr uint64_t System::Convert::ToUInt64(uint8_t value)
```

## Convert::ToUInt64(int8_t) طريقة

يقوم بتحويل العدد الصحيح الموقع ذو 8-بت المحدد إلى عدد صحيح غير موقع 64-بت معادل.

```cpp
static uint64_t System::Convert::ToUInt64(int8_t value)
```

## Convert::ToUInt64(uint16_t) طريقة

يقوم بتحويل العدد الصحيح غير الموقع ذو 16-بت المحدد إلى عدد صحيح غير موقع 64-بت معادل.

```cpp
static constexpr uint64_t System::Convert::ToUInt64(uint16_t value)
```

## Convert::ToUInt64(int16_t) طريقة

يقوم بتحويل العدد الصحيح الموقع ذو 16-بت المحدد إلى عدد صحيح غير موقع 64-بت معادل.

```cpp
static uint64_t System::Convert::ToUInt64(int16_t value)
```

## Convert::ToUInt64(uint32_t) طريقة

يقوم بتحويل العدد الصحيح غير الموقع ذو 32-بت المحدد إلى عدد صحيح غير موقع 64-بت معادل.

```cpp
static constexpr uint64_t System::Convert::ToUInt64(uint32_t value)
```

## Convert::ToUInt64(int32_t) طريقة

يقوم بتحويل العدد الصحيح الموقع ذو 32-بت المحدد إلى عدد صحيح غير موقع 64-بت معادل.

```cpp
static uint64_t System::Convert::ToUInt64(int32_t value)
```

## Convert::ToUInt64(uint64_t) طريقة

يقوم بإرجاع العدد الصحيح غير الموقع ذو 64-بت المحدد.

```cpp
static constexpr uint64_t System::Convert::ToUInt64(uint64_t value)
```

## Convert::ToUInt64(int64_t) طريقة

يقوم بتحويل العدد الصحيح الموقع ذو 64-بت المحدد إلى عدد صحيح غير موقع 64-بت معادل.

```cpp
static uint64_t System::Convert::ToUInt64(int64_t value)
```

## Convert::ToUInt64(float) طريقة

يقوم بتحويل الرقم ذو الفاصلة العائمة المحدد إلى عدد صحيح غير موقع 64-بت معادل.

```cpp
static uint64_t System::Convert::ToUInt64(float value)
```

## Convert::ToUInt64(double) طريقة

يقوم بتحويل الرقم ذو الفاصلة العشرية المزدوجة المحدد إلى عدد صحيح غير موقع 64-بت معادل.

```cpp
static uint64_t System::Convert::ToUInt64(double value)
```

## Convert::ToUInt64(const Decimal\&) طريقة

يقوم بتحويل الرقم العشري المحدد إلى عدد صحيح غير موقع 64-بت معادل.

```cpp
static uint64_t System::Convert::ToUInt64(const Decimal &value)
```

## Convert::ToUInt64(char_t) طريقة

يقوم بتحويل حرف Unicode المحدد إلى عدد صحيح غير موقع 64-بت معادل.

```cpp
static constexpr uint64_t System::Convert::ToUInt64(char_t value)
```

## Convert::ToUInt64(DateTime) طريقة

التحويل غير مدعوم. دائمًا يطلق استثناء InvalidCastException.

```cpp
static uint64_t System::Convert::ToUInt64(DateTime value)
```

## Convert::ToUInt64(std::nullptr_t) طريقة

يقوم بتحويل السلسلة الفارغة المحددة إلى القيمة المعادلة للعدد الصحيح غير الموقع 64-بت.

```cpp
static constexpr uint64_t System::Convert::ToUInt64(std::nullptr_t)
```

### قيمة الإرجاع

صفر.

## Convert::ToUInt64(const char_t *) طريقة

يقوم بتحويل السلسلة C المحددة التي تحتوي على تمثيل رقمي إلى القيمة غير الموقع 64-بت المعادلة.

```cpp
static uint64_t System::Convert::ToUInt64(const char_t *value)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | const char_t * | السلسلة C المراد تحويلها |

### قيمة الإرجاع

قيمة العدد الصحيح غير الموقع 64-بت المكافئ للرقم الممثل في السلسلة C المحددة

## Convert::ToUInt64(const String\&) طريقة

يقوم بتحويل السلسلة المحددة التي تحتوي على تمثيل رقمي إلى القيمة غير الموقع 64-بت المعادلة.

```cpp
static uint64_t System::Convert::ToUInt64(const String &value)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | const [String](../../string/)\& | السلسلة المراد تحويلها |

### قيمة الإرجاع

قيمة العدد الصحيح غير الموقع 64-بت المكافئ للرقم الممثل في السلسلة المحددة

## Convert::ToUInt64(const String\&, int) طريقة

يقوم بتحويل السلسلة المحددة التي تحتوي على تمثيل رقمي بالقاعدة المحددة إلى القيمة غير الموقع 64-بت المعادلة.

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, int from_base)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | const [String](../../string/)\& | السلسلة المراد تحويلها |
| from_base | int | الأساس للرقم الممثل في السلسلة |

### قيمة الإرجاع

قيمة العدد الصحيح غير الموقع 64-بت المكافئ للرقم الممثل في السلسلة المحددة

## Convert::ToUInt64(const String\&, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل السلسلة المحددة التي تحتوي على تمثيل رقمي إلى القيمة غير الموقع 64-بت المعادلة باستخدام معلومات التنسيق المقدمة.

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, const SharedPtr<IFormatProvider> &provider)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | const [String](../../string/)\& | السلسلة المراد تحويلها |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | مؤشر إلى كائن يحتوي على معلومات تنسيق السلسلة |

### قيمة الإرجاع

قيمة العدد الصحيح غير الموقع 64-بت المكافئ للرقم الممثل في السلسلة المحددة

## Convert::ToUInt64(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة




```cpp
static uint64_t System::Convert::ToUInt64(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToUInt64(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة




```cpp
static uint64_t System::Convert::ToUInt64(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToUInt64(const String\&, std::nullptr_t) طريقة




```cpp
static uint64_t System::Convert::ToUInt64(const String &value, std::nullptr_t)
```

## Convert::ToUInt64(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل السلسلة المحددة التي تحتوي على تمثيل رقمي إلى القيمة غير الموقع 64-بت المعادلة باستخدام معلومات التنسيق والنمط الرقمي المحددين.

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | const [String](../../string/)\& | السلسلة المراد تحويلها |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | تجميع بتّي لقيم تعداد NumberStyles يحدد النمط المسموح به لتمثيل الرقم كسلسلة |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | مؤشر إلى كائن يحتوي على معلومات تنسيق السلسلة |

### قيمة الإرجاع

قيمة العدد الصحيح غير الموقع 64-بت المكافئ للرقم الممثل في السلسلة المحددة

## Convert::ToUInt64(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة




```cpp
static uint64_t System::Convert::ToUInt64(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToUInt64(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة




```cpp
static uint64_t System::Convert::ToUInt64(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToUInt64(const String\&, Globalization::NumberStyles, std::nullptr_t) طريقة




```cpp
static uint64_t System::Convert::ToUInt64(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToUInt64(Enum) طريقة




```cpp
template<typename Enum,typename> static uint64_t System::Convert::ToUInt64(Enum value)
```

## Convert::ToUInt64(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المعلَّقة المحددة إلى قيمة عدد صحيح غير موقع 64-بت معادل.

```cpp
static uint64_t System::Convert::ToUInt64(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```

### الوسائط

| المعامل | النوع | الوصف |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | المؤشر المشترك إلى الكائن الذي يحتوي على القيمة المعلَّقة للتحويل |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | تنسيق السلسلة المستخدم إذا كان نوع القيمة المعلَّقة هو [String](../../string/) |

### قيمة الإرجاع

قيمة عدد صحيح غير موقع 64-بت مكافئة للقيمة المعلَّقة المحددة

## انظر أيضًا

* Enum [NumberStyles](../../../system.globalization/numberstyles/)
* Typedef [SharedPtr](../../sharedptr/)
* Class [Decimal](../../decimal/)
* Class [DateTime](../../datetime/)
* Class [String](../../string/)
* Class [IFormatProvider](../../iformatprovider/)
* Class [CultureInfo](../../../system.globalization/cultureinfo/)
* Class [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* Class [Object](../../object/)
* Struct [Convert](../)
* Struct [Enum](../../enum/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)