---
title: ToSByte()
second_title: مرجع API Aspose.Slides للغة C++
description: يقوم بتحويل القيمة المنطقية المحددة إلى عدد صحيح موقّع 8-بت مكافئ.
type: docs
weight: 105
url: /ar/system/convert/tosbyte/
---
## Convert::ToSByte(bool) طريقة

يقوم بتحويل القيمة المنطقية المحددة إلى عدد صحيح موقّع 8-بت مكافئ.

```cpp
static constexpr int8_t System::Convert::ToSByte(bool value)
```

## Convert::ToSByte(uint8_t) طريقة

يقوم بتحويل العدد الصحيح غير الموقّع 8-بت المحدد إلى عدد صحيح موقّع 8-بت مكافئ.

```cpp
static int8_t System::Convert::ToSByte(uint8_t value)
```

## Convert::ToSByte(int8_t) طريقة

يعيد العدد الصحيح الموقّع 8-بت المحدد.

```cpp
static constexpr int8_t System::Convert::ToSByte(int8_t value)
```

## Convert::ToSByte(uint16_t) طريقة

يقوم بتحويل العدد الصحيحة غير الموقّعة 16-بت المحدد إلى عدد صحيح موقّع 8-بت مكافئ.

```cpp
static int8_t System::Convert::ToSByte(uint16_t value)
```

## Convert::ToSByte(int16_t) طريقة

يقوم بتحويل العدد الصحيح الموقّع 16-بت المحدد إلى عدد صحيح موقّع 8-بت مكافئ.

```cpp
static int8_t System::Convert::ToSByte(int16_t value)
```

## Convert::ToSByte(uint32_t) طريقة

يقوم بتحويل العدد الصحيحة غير الموقّعة 32-بت المحدد إلى عدد صحيح موقّع 8-بت مكافئ.

```cpp
static int8_t System::Convert::ToSByte(uint32_t value)
```

## Convert::ToSByte(int32_t) طريقة

يقوم بتحويل العدد الصحيح الموقّع 32-بت المحدد إلى عدد صحيح موقّع 8-بت مكافئ.

```cpp
static int8_t System::Convert::ToSByte(int32_t value)
```

## Convert::ToSByte(uint64_t) طريقة

يقوم بتحويل العدد الصحيحة غير الموقّعة 64-بت المحدد إلى عدد صحيح موقّع 8-بت مكافئ.

```cpp
static int8_t System::Convert::ToSByte(uint64_t value)
```

## Convert::ToSByte(int64_t) طريقة

يقوم بتحويل العدد الصحيح الموقّع 64-بت المحدد إلى عدد صحيح موقّع 8-بت مكافئ.

```cpp
static int8_t System::Convert::ToSByte(int64_t value)
```

## Convert::ToSByte(float) طريقة

يقوم بتحويل العدد العشري العائم المحدد إلى عدد صحيح موقّع 8-بت مكافئ.

```cpp
static int8_t System::Convert::ToSByte(float value)
```

## Convert::ToSByte(double) طريقة

يقوم بتحويل العدد العشري المزدوج المحدد إلى عدد صحيح موقّع 8-بت مكافئ.

```cpp
static int8_t System::Convert::ToSByte(double value)
```

## Convert::ToSByte(const Decimal\&) طريقة

يقوم بتحويل العدد العشري المحدد إلى عدد صحيح موقّع 8-بت مكافئ.

```cpp
static int8_t System::Convert::ToSByte(const Decimal &value)
```

## Convert::ToSByte(char_t) طريقة

يقوم بتحويل الحرف Unicode المحدد إلى عدد صحيح موقّع 8-بت مكافئ.

```cpp
static int8_t System::Convert::ToSByte(char_t value)
```

## Convert::ToSByte(DateTime) طريقة

التحويل غير مدعوم. دائمًا يطرح استثناء InvalidCastException.

```cpp
static int8_t System::Convert::ToSByte(DateTime value)
```

## Convert::ToSByte(std::nullptr_t) طريقة

يقوم بتحويل السلسلة الفارغة المحددة إلى قيمة عدد صحيح 8-بت مكافئ.

```cpp
static constexpr int8_t System::Convert::ToSByte(std::nullptr_t)
```

### قيمة الإرجاع

الصفر.

## Convert::ToSByte(const char_t *) طريقة

يقوم بتحويل السلسلة النصية c-string التي تحتوي على تمثيل عدد إلى قيمة عدد صحيح 8-بت مكافئ.

```cpp
static int8_t System::Convert::ToSByte(const char_t *value)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | const char_t * | السلسلة c-string المراد تحويلها |

### قيمة الإرجاع

قيمة العدد الصحيح 8-بت المكافئ للعدد الممثل في السلسلة النصية المحددة

## Convert::ToSByte(const String\&) طريقة

يقوم بتحويل السلسلة النصية التي تحتوي على تمثيل عدد إلى قيمة عدد صحيح 8-بت مكافئ.

```cpp
static int8_t System::Convert::ToSByte(const String &value)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | const [String](../../string/)\& | السلسلة المراد تحويلها |

### قيمة الإرجاع

قيمة العدد الصحيح 8-بت المكافئ للعدد الممثل في السلسلة المحددة

## Convert::ToSByte(const String\&, int) طريقة

يقوم بتحويل السلسلة النصية التي تحتوي على تمثيل عدد في القاعدة المحددة إلى قيمة عدد صحيح 8-بت مكافئ.

```cpp
static int8_t System::Convert::ToSByte(const String &value, int from_base)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | const [String](../../string/)\& | السلسلة المراد تحويلها |
| from_base | int | القاعدة التي يُمثل بها العدد في السلسلة |

### قيمة الإرجاع

قيمة العدد الصحيح 8-بت المكافئ للعدد الممثل في السلسلة المحددة

## Convert::ToSByte(const String\&, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل السلسلة النصية التي تحتوي على تمثيل عدد إلى قيمة عدد صحيح غير موقّع 8-بت مكافئ باستخدام معلومات التنسيق المقدمة.

```cpp
static int8_t System::Convert::ToSByte(const String &value, const SharedPtr<IFormatProvider> &provider)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | const [String](../../string/)\& | السلسلة المراد تحويلها |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | مؤشر إلى كائن يحتوي على معلومات تنسيق السلسلة |

### قيمة الإرجاع

قيمة العدد الصحيح 8-بت المكافئ للعدد الممثل في السلسلة المحددة

## Convert::ToSByte(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة

```cpp
static int8_t System::Convert::ToSByte(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToSByte(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة

```cpp
static int8_t System::Convert::ToSByte(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToSByte(const String\&, std::nullptr_t) طريقة

```cpp
static int8_t System::Convert::ToSByte(const String &value, std::nullptr_t)
```

## Convert::ToSByte(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل السلسلة النصية التي تحتوي على تمثيل عدد إلى قيمة عدد صحيح 8-بت مكافئ باستخدام معلومات التنسيق ونمط العدد المقدمة.

```cpp
static int8_t System::Convert::ToSByte(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | const [String](../../string/)\& | السلسلة المراد تحويلها |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | مجموعة قيم عددية من تعداد NumberStyles تحدد النمط المسموح به لتمثيل العدد |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | مؤشر إلى كائن يحتوي على معلومات تنسيق السلسلة |

### قيمة الإرجاع

قيمة العدد الصحيح غير الموقّع 8-بت المكافئ للعدد الممثل في السلسلة المحددة

## Convert::ToSByte(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) طريقة

```cpp
static int8_t System::Convert::ToSByte(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToSByte(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) طريقة

```cpp
static int8_t System::Convert::ToSByte(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToSByte(const String\&, Globalization::NumberStyles, std::nullptr_t) طريقة

```cpp
static int8_t System::Convert::ToSByte(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToSByte(Enum) طريقة

```cpp
template<typename Enum,typename> static int8_t System::Convert::ToSByte(Enum value)
```

## Convert::ToSByte(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) طريقة

يقوم بتحويل القيمة المغلّفة إلى قيمة عدد صحيح 8-بت مكافئ.

```cpp
static int8_t System::Convert::ToSByte(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | مؤشر المشترك إلى الكائن الذي يحزم القيمة المراد تحويلها |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | تنسيق السلسلة الذي سيُستخدم إذا كان نوع القيمة المغلّفة هو [String](../../string/) |

### قيمة الإرجاع

قيمة عدد صحيح 8-بت مكافئة للقيمة المغلّفة المحددة

## See Also

* Enum [NumberStyles](../../../system.globalization/numberstyles/)
* Typedef [SharedPtr](../../sharedptr/)
* Class [Decimal](../../decimal/)
* Class [DateTime](../../datetime/)
* Class [String](../../string/)
* Class [IFormatProvider](../../iformatprovider/)
* Class [CultureInfo](../../../system.globalization/cultureinfo/)
* Class [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* Class [Object](../../object/)
* Struct [Convert](../)
* Struct [Enum](../../enum/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)