---
title: operator<<()
second_title: مرجع API Aspose.Slides للغة C++
description: إدراج البيانات في التدفق باستخدام ترميز UTF-8.
type: docs
weight: 2159
url: /ar/system/operator_less_less/
---
## System::operator<<(std::ostream\&, DateTime) دالة

إدراج البيانات في stream باستخدام ترميز UTF-8.

```cpp
std::ostream & System::operator<<(std::ostream &stream, DateTime date_time)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::ostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| date_time | [DateTime](../datetime/) | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::wostream\&, DateTime) دالة

إدراج البيانات في stream.

```cpp
std::wostream & System::operator<<(std::wostream &stream, DateTime date_time)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::wostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| date_time | [DateTime](../datetime/) | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::ostream\&, DateTimeOffset) دالة

إدراج البيانات في stream باستخدام ترميز UTF-8.

```cpp
std::ostream & System::operator<<(std::ostream &stream, DateTimeOffset value)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::ostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| value | [DateTimeOffset](../datetimeoffset/) | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::wostream\&, DateTimeOffset) دالة

إدراج البيانات في stream.

```cpp
std::wostream & System::operator<<(std::wostream &stream, DateTimeOffset value)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::wostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| value | [DateTimeOffset](../datetimeoffset/) | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::ostream\&, const Decimal\&) دالة

إدراج البيانات في stream باستخدام ترميز UTF-8.

```cpp
std::ostream & System::operator<<(std::ostream &stream, const Decimal &decimal)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::ostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| decimal | const [Decimal](../decimal/)\& | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::wostream\&, const Decimal\&) دالة

إدراج البيانات في stream.

```cpp
std::wostream & System::operator<<(std::wostream &stream, const Decimal &decimal)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::wostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| decimal | const [Decimal](../decimal/)\& | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::ostream\&, const Exception\&) دالة

إدراج البيانات في stream باستخدام ترميز UTF-8.

```cpp
std::ostream & System::operator<<(std::ostream &stream, const Exception &exception)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::ostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| exception | const [Exception](../exception/)\& | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::wostream\&, const Exception\&) دالة

إدراج البيانات في stream.

```cpp
std::wostream & System::operator<<(std::wostream &stream, const Exception &exception)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::wostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| exception | const [Exception](../exception/)\& | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::ostream\&, const Guid\&) دالة

إدراج البيانات في stream باستخدام ترميز UTF-8.

```cpp
std::ostream & System::operator<<(std::ostream &stream, const Guid &guid)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::ostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| guid | const [Guid](../guid/)\& | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::wostream\&, const Guid\&) دالة

إدراج البيانات في stream.

```cpp
std::wostream & System::operator<<(std::wostream &stream, const Guid &guid)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::wostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| guid | const [Guid](../guid/)\& | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::ostream\&, const Nullable\<T\>\&) دالة

إدراج البيانات في stream باستخدام ترميز UTF-8.

```cpp
template<typename T> std::ostream & System::operator<<(std::ostream &stream, const Nullable<T> &value)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::ostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| value | const [Nullable](../nullable/)\<T\>\& | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::wostream\&, const Nullable\<T\>\&) دالة

إدراج البيانات في stream.

```cpp
template<typename T> std::wostream & System::operator<<(std::wostream &stream, const Nullable<T> &value)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::wostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| value | const [Nullable](../nullable/)\<T\>\& | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::ostream\&, const System::Object\&) دالة

إدراج البيانات في stream باستخدام ترميز UTF-8.

```cpp
std::ostream & System::operator<<(std::ostream &stream, const System::Object &object)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::ostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| object | const [System::Object](../object/)\& | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::wostream\&, const System::Object\&) دالة

إدراج البيانات في stream.

```cpp
std::wostream & System::operator<<(std::wostream &stream, const System::Object &object)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::wostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| object | const [System::Object](../object/)\& | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::ostream\&, const OperatingSystem\&) دالة

إدراج البيانات في stream باستخدام ترميز UTF-8.

```cpp
std::ostream & System::operator<<(std::ostream &stream, const OperatingSystem &os)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::ostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| os | const [OperatingSystem](../operatingsystem/)\& | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::wostream\&, const OperatingSystem\&) دالة

إدراج البيانات في stream.

```cpp
std::wostream & System::operator<<(std::wostream &stream, const OperatingSystem &os)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::wostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| os | const [OperatingSystem](../operatingsystem/)\& | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::ostream\&, const SharedPtr\<T\>\&) دالة

إدراج البيانات في stream باستخدام ترميز UTF-8.

```cpp
template<typename T> std::ostream & System::operator<<(std::ostream &stream, const SharedPtr<T> &object_ptr)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::ostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| object_ptr | const [SharedPtr](../sharedptr/)\<T\>\& | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::wostream\&, const SharedPtr\<T\>\&) دالة

إدراج البيانات في stream.

```cpp
template<typename T> std::wostream & System::operator<<(std::wostream &stream, const SharedPtr<T> &object_ptr)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::wostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| object_ptr | const [SharedPtr](../sharedptr/)\<T\>\& | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::ostream\&, const String\&) دالة

يُخرج سلسلة إلى تدفق الإخراج باستخدام ترميز UTF-8.

```cpp
std::ostream & System::operator<<(std::ostream &os, const String &str)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| os | std::ostream\& | كائن تدفق إخراج (إنشاء **basic_ostream** مع **char**). |
| str | const [String](../string/)\& | سلسلة سيتم إرسالها إلى تدفق الإخراج. |

### قيمة الإرجاع

تدفق إخراج تم إضافة السلسلة إليه.

## System::operator<<(std::wostream\&, const String\&) دالة

يُخرج سلسلة إلى تدفق الإخراج.

```cpp
std::wostream & System::operator<<(std::wostream &os, const String &str)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| os | std::wostream\& | كائن تدفق إخراج (إنشاء **basic_ostream** مع **wchar_t**). |
| str | const [String](../string/)\& | سلسلة سيتم إرسالها إلى تدفق الإخراج. |

### قيمة الإرجاع

تدفق إخراج تم إضافة السلسلة إليه.

## System::operator<<(std::ostream\&, TimeSpan) دالة

إدراج البيانات في stream باستخدام ترميز UTF-8.

```cpp
std::ostream & System::operator<<(std::ostream &stream, TimeSpan time_span)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::ostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| time_span | [TimeSpan](../timespan/) | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::wostream\&, TimeSpan) دالة

إدراج البيانات في stream.

```cpp
std::wostream & System::operator<<(std::wostream &stream, TimeSpan time_span)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::wostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| time_span | [TimeSpan](../timespan/) | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::ostream\&, const TypeInfo\&) دالة

إدراج البيانات في stream باستخدام ترميز UTF-8.

```cpp
std::ostream & System::operator<<(std::ostream &stream, const TypeInfo &type_info)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::ostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| type_info | const [TypeInfo](../typeinfo/)\& | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::wostream\&, const TypeInfo\&) دالة

إدراج البيانات في stream.

```cpp
std::wostream & System::operator<<(std::wostream &stream, const TypeInfo &type_info)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::wostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| type_info | const [TypeInfo](../typeinfo/)\& | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::ostream\&, const Version\&) دالة

إدراج البيانات في stream باستخدام ترميز UTF-8.

```cpp
std::ostream & System::operator<<(std::ostream &stream, const Version &version)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::ostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| version | const [Version](../version/)\& | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::wostream\&, const Version\&) دالة

إدراج البيانات في stream.

```cpp
std::wostream & System::operator<<(std::wostream &stream, const Version &version)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::wostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| version | const [Version](../version/)\& | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::ostream\&, const WeakPtr\<T\>\&) دالة

إدراج البيانات في stream باستخدام ترميز UTF-8.

```cpp
template<typename T> std::ostream & System::operator<<(std::ostream &stream, const WeakPtr<T> &object_ptr)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::ostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| object_ptr | const [WeakPtr](../weakptr/)\<T\>\& | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## System::operator<<(std::wostream\&, const WeakPtr\<T\>\&) دالة

إدراج البيانات في stream.

```cpp
template<typename T> std::wostream & System::operator<<(std::wostream &stream, const WeakPtr<T> &object_ptr)
```

### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| stream | std::wostream\& | تدفق الإخراج لإدراج البيانات إليه. |
| object_ptr | const [WeakPtr](../weakptr/)\<T\>\& | [Data](../../system.data/) لإدراجها. |

### قيمة الإرجاع

**stream**.

## انظر أيضًا

* تعريف نوع [Exception](../exception/)
* تعريف نوع [SharedPtr](../sharedptr/)
* فئة [DateTime](../datetime/)
* فئة [DateTimeOffset](../datetimeoffset/)
* فئة [Decimal](../decimal/)
* فئة [Guid](../guid/)
* فئة [Nullable](../nullable/)
* فئة [Object](../object/)
* فئة [OperatingSystem](../operatingsystem/)
* فئة [String](../string/)
* فئة [TimeSpan](../timespan/)
* فئة [TypeInfo](../typeinfo/)
* فئة [Version](../version/)
* فئة [WeakPtr](../weakptr/)
* فضاء أسماء [System](../)
* مكتبة [Aspose.Slides](../../)