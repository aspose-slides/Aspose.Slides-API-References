---
title: Equals()
second_title: مرجع API لـ Aspose.Slides للغة C++
description: يتحقق مما إذا كان كائكان DateTimeOffset يمثلان نفس نقطة الزمن.
type: docs
weight: 365
url: /ar/system/datetimeoffset/equals/
---
## DateTimeOffset::Equals(const DateTimeOffset\&) const method

يتحقق مما إذا كان كائكان [DateTimeOffset](../) يمثلان نفس نقطة الزمن.

```cpp
bool System::DateTimeOffset::Equals(const DateTimeOffset &other) const
```

### المعطيات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/) للمقارنة. |

### قيمة الإرجاع

true إذا كان الكائن الحالي مساويًا لـ **other**، وإلا - false.

## DateTimeOffset::Equals(const SharedPtr\<Object\>\&) const method

يتحقق مما إذا كان كائكان [DateTimeOffset](../) يمثلان نفس نقطة الزمن.

```cpp
bool System::DateTimeOffset::Equals(const SharedPtr<Object> &obj) const
```

### المعطيات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/) للمقارنة. |

### قيمة الإرجاع

true إذا كان الكائن الحالي مساويًا لـ **obj**، وإلا - false.

## DateTimeOffset::Equals(const DateTimeOffset\&, const DateTimeOffset\&) method

يتحقق مما إذا كان كائكان [DateTimeOffset](../) يمثلان نفس نقطة الزمن.

```cpp
static bool System::DateTimeOffset::Equals(const DateTimeOffset &first, const DateTimeOffset &second)
```

### المعطيات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| first | const [DateTimeOffset](../)\& | الكائن الأول للمقارنة. |
| second | const [DateTimeOffset](../)\& | الكائن الثاني للمقارنة. |

### قيمة الإرجاع

true إذا كان **first** مساويًا لـ **second**، وإلا - false.

## انظر أيضًا

* تعريف نوع [SharedPtr](../../sharedptr/)
* فئة [DateTimeOffset](../)
* فئة [Object](../../object/)
* نطاق الاسم [System](../../)
* مكتبة [Aspose.Slides](../../../)