---
title: WriteLine()
second_title: مرجع API Aspose.Slides للغة C++
description: يقوم بإخراج فاصل السطر الحالي إلى تدفق الإخراج الذي يمثله الكائن الحالي.
type: docs
weight: 27
url: /ar/system/consoleoutput/writeline/
---
## ConsoleOutput::WriteLine() طريقة

يكتب فاصل السطر الحالي إلى دفق الإخراج الذي يمثله الكائن الحالي.

```cpp
void System::ConsoleOutput::WriteLine() override
```
## ConsoleOutput::WriteLine(const SharedPtr\<Object\>\&) طريقة

يكتب تمثيل النص للكائن المحدد يليه فاصل السطر الحالي إلى دفق الإخراج الذي يمثله الكائن الحالي.

```cpp
void System::ConsoleOutput::WriteLine(const SharedPtr<Object> &value) override
```


### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | الكائن المراد طباعته |

## ConsoleOutput::WriteLine(bool) طريقة

يكتب تمثيل النص للقيمة bool المحددة يليه فاصل السطر الحالي إلى دفق الإخراج الذي يمثله الكائن الحالي.

```cpp
void System::ConsoleOutput::WriteLine(bool value) override
```


### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **bool** | الكائن المراد طباعته |

## ConsoleOutput::WriteLine(char_t) طريقة

يكتب القيمة الحرفية المحددة يليه فاصل السطر الحالي إلى دفق الإخراج الذي يمثله الكائن الحالي.

```cpp
void System::ConsoleOutput::WriteLine(char_t value) override
```


### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | char_t | القيمة المراد طباعتها |

## ConsoleOutput::WriteLine(Decimal) طريقة


يكتب تمثيل النص لقيمة [Decimal](../../decimal/) يليه فاصل السطر الحالي إلى دفق الإخراج الذي يمثله الكائن الحالي.

```cpp
void System::ConsoleOutput::WriteLine(Decimal value) override
```


### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | القيمة المراد طباعتها |

## ConsoleOutput::WriteLine(double) طريقة


يكتب تمثيل النص لقيمة نقطة عائمة ذات دقة مزدوجة يليه فاصل السطر الحالي إلى دفق الإخراج الذي يمثله الكائن الحالي.

```cpp
void System::ConsoleOutput::WriteLine(double value) override
```


### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **double** | القيمة المراد طباعتها |

## ConsoleOutput::WriteLine(int) طريقة


يكتب تمثيل النص لقيمة عدد صحيح 32-بت يليه فاصل السطر الحالي إلى دفق الإخراج الذي يمثله الكائن الحالي.

```cpp
void System::ConsoleOutput::WriteLine(int value) override
```


### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | int | القيمة المراد طباعتها |

## ConsoleOutput::WriteLine(int64_t) طريقة


يكتب تمثيل النص لقيمة عدد صحيح 64-بت يليه فاصل السطر الحالي إلى دفق الإخراج الذي يمثله الكائن الحالي.

```cpp
void System::ConsoleOutput::WriteLine(int64_t value) override
```


### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **int64_t** | القيمة المراد طباعتها |

## ConsoleOutput::WriteLine(float) طريقة


يكتب تمثيل النص لقيمة نقطة عائمة ذات دقة مفردة يليه فاصل السطر الحالي إلى دفق الإخراج الذي يمثله الكائن الحالي.

```cpp
void System::ConsoleOutput::WriteLine(float value) override
```


### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **float** | القيمة المراد طباعتها |

## ConsoleOutput::WriteLine(const String\&) طريقة


يكتب كائن السلسلة النصية المحدد يليه فاصل السطر الحالي إلى دفق الإخراج الذي يمثله الكائن الحالي.

```cpp
void System::ConsoleOutput::WriteLine(const String &value) override
```


### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | const [String](../../string/)\& | كائن السلسلة النصية المراد طباعته |

## ConsoleOutput::WriteLine(uint32_t) طريقة


يكتب تمثيل النص لقيمة عدد صحيح غير موقع 32-بت يليه فاصل السطر الحالي إلى دفق الإخراج الذي يمثله الكائن الحالي.

```cpp
void System::ConsoleOutput::WriteLine(uint32_t value) override
```


### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **uint32_t** | القيمة المراد طباعتها |

## ConsoleOutput::WriteLine(uint64_t) طريقة


يكتب تمثيل النص لقيمة عدد صحيح غير موقع 64-بت يليه فاصل السطر الحالي إلى دفق الإخراج الذي يمثله الكائن الحالي.

```cpp
void System::ConsoleOutput::WriteLine(uint64_t value) override
```


### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | **uint64_t** | القيمة المراد طباعتها |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&) طريقة


يكتب تمثيل النص للمصفوفة الحرفية المحددة يليه فاصل السطر الحالي إلى دفق الإخراج الذي يمثله الكائن الحالي.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer) override
```


### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | المصفوفة المراد طباعتها |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&, int32_t, int32_t) طريقة


يكتب تمثيل النص لنطاق من القيم في المصفوفة الحرفية المحددة يليه فاصل السطر الحالي إلى دفق الإخراج الذي يمثله الكائن الحالي.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```


### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | المصفوفة التي تحتوي على القيم المراد طباعتها |
| index | **int32_t** | الفهرس الذي يبدأ منه نطاق العناصر المراد طباعتها |
| count | **int32_t** | عدد العناصر في نطاق العناصر المراد طباعتها |

## ConsoleOutput::WriteLine(const char_t *) طريقة


يكتب السلسلة c-string المحددة يليه فاصل السطر الحالي إلى دفق الإخراج الذي يمثله الكائن الحالي.

```cpp
void System::ConsoleOutput::WriteLine(const char_t *value) override
```


### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | const char_t * | السلسلة c-string المراد طباعتها |

## ConsoleOutput::WriteLine(const TypeInfo\&) طريقة


يكتب تمثيل النص للكائن [TypeInfo](../../typeinfo/) المحدد يليه فاصل السطر الحالي إلى دفق الإخراج الذي يمثله الكائن الحالي.

```cpp
void System::ConsoleOutput::WriteLine(const TypeInfo &value) override
```


### المعاملات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | الكائن [TypeInfo](../../typeinfo/) المراد طباعته |

## ConsoleOutput::WriteLine(const char *) طريقة




```cpp
void System::ConsoleOutput::WriteLine(const char *)=delete
```

## انظر أيضًا

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* الصنف [ConsoleOutput](../)
* الصنف [Object](../../object/)
* الصنف [Decimal](../../decimal/)
* الصنف [String](../../string/)
* الصنف [TypeInfo](../../typeinfo/)
* النطاق [System](../../)
* Library [Aspose.Slides](../../../)