---
title: Write()
second_title: مرجع API الخاص بـ Aspose.Slides للـ C++
description: يُخرج تمثيل السلسلة للقيمة المنطقية المحددة إلى دفق الإخراج الذي يمثله الكائن الحالي.
type: docs
weight: 14
url: /ar/system/consoleoutput/write/
---
## ConsoleOutput::Write(bool) method


يخرج تمثيل السلسلة للقيمة bool المحددة إلى تدفق الإخراج الممثّل بالكائن الحالي.

```cpp
void System::ConsoleOutput::Write(bool value) override
```


### المعلمات

| Parameter | Type | Description |
| --- | --- | --- |
| value | **bool** | القيمة التي سيتم إخراجها |

## ConsoleOutput::Write(const SharedPtr\<Object\>\&) method


يخرج تمثيل السلسلة للكائن المحدد إلى تدفق الإخراج الممثّل بالكائن الحالي.

```cpp
void System::ConsoleOutput::Write(const SharedPtr<Object> &value) override
```


### المعلمات

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | الكائن الذي سيتم إخراجه |

## ConsoleOutput::Write(char_t) method


يخرج قيمة الحرف المحدد إلى تدفق الإخراج الممثّل بالكائن الحالي.

```cpp
void System::ConsoleOutput::Write(char_t value) override
```


### المعلمات

| Parameter | Type | Description |
| --- | --- | --- |
| value | char_t | القيمة التي سيتم إخراجها |

## ConsoleOutput::Write(Decimal) method


يخرج تمثيل السلسلة لقيمة [Decimal](../../decimal/) إلى تدفق الإخراج الممثّل بالكائن الحالي.

```cpp
void System::ConsoleOutput::Write(Decimal value) override
```


### المعلمات

| Parameter | Type | Description |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | القيمة التي سيتم إخراجها |

## ConsoleOutput::Write(double) method


يخرج تمثيل السلسلة لقيمة مزدوجة الدقة إلى تدفق الإخراج الممثّل بالكائن الحالي.

```cpp
void System::ConsoleOutput::Write(double value) override
```


### المعلمات

| Parameter | Type | Description |
| --- | --- | --- |
| value | **double** | القيمة التي سيتم إخراجها |

## ConsoleOutput::Write(int32_t) method


يخرج تمثيل السلسلة لقيمة عدد صحيح 32-بت إلى تدفق الإخراج الممثّل بالكائن الحالي.

```cpp
void System::ConsoleOutput::Write(int32_t value) override
```


### المعلمات

| Parameter | Type | Description |
| --- | --- | --- |
| value | **int32_t** | القيمة التي سيتم إخراجها |

## ConsoleOutput::Write(int64_t) method


يخرج تمثيل السلسلة لقيمة عدد صحيح 64-بت إلى تدفق الإخراج الممثّل بالكائن الحالي.

```cpp
void System::ConsoleOutput::Write(int64_t value) override
```


### المعلمات

| Parameter | Type | Description |
| --- | --- | --- |
| value | **int64_t** | القيمة التي سيتم إخراجها |

## ConsoleOutput::Write(float) method


يخرج تمثيل السلسلة لقيمة عدد عائم بدقة مفردة إلى تدفق الإخراج الممثّل بالكائن الحالي.

```cpp
void System::ConsoleOutput::Write(float value) override
```


### المعلمات

| Parameter | Type | Description |
| --- | --- | --- |
| value | **float** | القيمة التي سيتم إخراجها |

## ConsoleOutput::Write(const String\&) method


يخرج كائن السلسلة المحدد إلى تدفق الإخراج الممثّل بالكائن الحالي.

```cpp
void System::ConsoleOutput::Write(const String &value) override
```


### المعلمات

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | كائن السلسلة الذي سيتم إخراجه |

## ConsoleOutput::Write(uint32_t) method


يخرج تمثيل السلسلة لقيمة عدد صحيح غير موقع 32-بت إلى تدفق الإخراج الممثّل بالكائن الحالي.

```cpp
void System::ConsoleOutput::Write(uint32_t value) override
```


### المعلمات

| Parameter | Type | Description |
| --- | --- | --- |
| value | **uint32_t** | القيمة التي سيتم إخراجها |

## ConsoleOutput::Write(uint64_t) method


يخرج تمثيل السلسلة لقيمة عدد صحيح غير موقع 64-بت إلى تدفق الإخراج الممثّل بالكائن الحالي.

```cpp
void System::ConsoleOutput::Write(uint64_t value) override
```


### المعلمات

| Parameter | Type | Description |
| --- | --- | --- |
| value | **uint64_t** | القيمة التي سيتم إخراجها |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&) method


يخرج تمثيل السلسلة للمصفوفة الحرفية المحددة إلى تدفق الإخراج الممثّل بالكائن الحالي.

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer) override
```


### المعلمات

| Parameter | Type | Description |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | المصفوفة التي سيتم إخراجها |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&, int32_t, int32_t) method


يخرج تمثيل السلسلة لمدى من القيم في مصفوفة الحروف المحددة إلى تدفق الإخراج الممثّل بالكائن الحالي.

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```


### المعلمات

| Parameter | Type | Description |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | المصفوفة التي تحتوي على القيم التي سيتم إخراجها |
| index | **int32_t** | الفهرس الذي يبدأ منه نطاق العناصر التي سيتم إخراجها |
| count | **int32_t** | عدد العناصر في نطاق العناصر التي سيتم إخراجها |

## ConsoleOutput::Write(const char_t *) method


يخرج سلسلة C المحددة إلى تدفق الإخراج الممثّل بالكائن الحالي.

```cpp
void System::ConsoleOutput::Write(const char_t *value) override
```


### المعلمات

| Parameter | Type | Description |
| --- | --- | --- |
| value | const char_t * | سلسلة C التي سيتم إخراجها |

## ConsoleOutput::Write(const TypeInfo\&) method


يخرج تمثيل السلسلة لكائن [TypeInfo](../../typeinfo/) المحدد إلى تدفق الإخراج الممثّل بالكائن الحالي.

```cpp
void System::ConsoleOutput::Write(const TypeInfo &value) override
```


### المعلمات

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | كائن [TypeInfo](../../typeinfo/) الذي سيتم إخراجه |

## ConsoleOutput::Write(const char *) method




```cpp
void System::ConsoleOutput::Write(const char *)=delete
```

## انظر أيضًا

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* الفئة [ConsoleOutput](../)
* الفئة [Object](../../object/)
* الفئة [Decimal](../../decimal/)
* الفئة [String](../../string/)
* الفئة [TypeInfo](../../typeinfo/)
* النطاق [System](../../)
* المكتبة [Aspose.Slides](../../../)