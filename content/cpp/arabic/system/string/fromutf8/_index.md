---
title: FromUtf8()
second_title: مرجع API ل Aspose.Slides للغة C++
description: ينشئ كائن String من سلسلة utf8.
type: docs
weight: 898
url: /ar/system/string/fromutf8/
---
## String::FromUtf8(const char *) طريقة

ينشئ [String](../) من سلسلة utf8.

```cpp
static String System::String::FromUtf8(const char *utf8)
```

### معلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| utf8 | const char * | مؤشر إلى سلسلة منتهية بصفر (null) ومُشفَّرة باستخدام ترميز utf8. |

### قيمة الإرجاع

كائن [String](../) يمثل السلسلة التي تم تمريرها.

## String::FromUtf8(const char *, int) طريقة

ينشئ [String](../) من سلسلة utf8.

```cpp
static String System::String::FromUtf8(const char *utf8, int len)
```

### معلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| utf8 | const char * | مؤشر إلى سلسلة مشفرة باستخدام ترميز utf8. |
| len | int | عدد الأحرف التي يجب معالجتها. |

### قيمة الإرجاع

كائن [String](../) يمثل السلسلة التي تم تمريرها.

## String::FromUtf8(const uint8_t *) طريقة

ينشئ [String](../) من سلسلة utf8.

```cpp
static String System::String::FromUtf8(const uint8_t *utf8)
```

### معلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| utf8 | const **uint8_t** * | مؤشر إلى سلسلة منتهية بصفر (null) ومُشفَّرة باستخدام ترميز utf8. |

### قيمة الإرجاع

كائن [String](../) يمثل السلسلة التي تم تمريرها.

## String::FromUtf8(const std::string\&) طريقة

ينشئ [String](../) من سلسلة utf8.

```cpp
static String System::String::FromUtf8(const std::string &utf8)
```

### معلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| utf8 | const std::string\& | مؤشر إلى سلسلة مشفرة باستخدام ترميز utf8. |

### قيمة الإرجاع

كائن [String](../) يمثل السلسلة التي تم تمريرها.

## انظر أيضًا

* فئة [String](../)
* نطاق [System](../../)
* مكتبة [Aspose.Slides](../../../)