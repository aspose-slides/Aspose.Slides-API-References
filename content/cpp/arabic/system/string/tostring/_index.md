---
title: ToString()
second_title: مرجع API Aspose.Slides للغة C++
description: مغلف لمعالجة فئة String في السياقات التي يتم فيها استدعاء ToString() على كائنات من نوع القيمة.
type: docs
weight: 365
url: /ar/system/string/tostring/
---
## String::ToString() const طريقة

Wrapper for handling [String](../) فئة in contexts where [ToString()](./) is being called on value type objects.

```cpp
String System::String::ToString() const
```

### قيمة الإرجاع

Copy of current string. It is considered a null string if current string is a null string.

## String::ToString(const SharedPtr\<IFormatProvider\>\&) const طريقة

Wrapper for handling [String](../) فئة in contexts where [ToString()](./) is being called on value type objects.

```cpp
String System::String::ToString(const SharedPtr<IFormatProvider> &) const
```

### قيمة الإرجاع

Copy of current string. It is considered a null string if current string is a null string.

## انظر أيضاً

* تعريف نوع [SharedPtr](../../sharedptr/)
* فئة [String](../)
* فئة [IFormatProvider](../../iformatprovider/)
* نطاق [System](../../)
* مكتبة [Aspose.Slides](../../../)