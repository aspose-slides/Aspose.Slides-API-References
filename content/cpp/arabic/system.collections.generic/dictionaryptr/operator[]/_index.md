---
title: operator[]()
second_title: مرجع واجهة برمجة التطبيقات Aspose.Slides للغة C++
description: عامل وصول للعمل مع تحويل نوع المفتاح.
type: docs
weight: 14
url: /ar/system.collections.generic/dictionaryptr/operator[]/
---
## DictionaryPtr::operator[](const X\&) const طريقة

عامل وصول للعمل مع تحويل نوع المفتاح.

```cpp
template<class X> V & System::Collections::Generic::DictionaryPtr<T, V>::operator[](const X &key) const
```

### معلمات القالب

| معامل | الوصف |
| --- | --- |
| X | نوع المفتاح المصدر. |

### المعاملات

| معامل | نوع | الوصف |
| --- | --- | --- |
| key | const X\& | [Dictionary](../../dictionary/) المفتاح. |

### قيمة الإرجاع

مرجع إلى القيمة المقابلة للمفتاح الممرَّر، سواء كانت موجودة أو تم إنشاؤها حديثًا.

## DictionaryPtr::operator[](const T\&) const طريقة

عامل وصول.

```cpp
V & System::Collections::Generic::DictionaryPtr<T, V>::operator[](const T &key) const
```

### المعاملات

| معامل | نوع | الوصف |
| --- | --- | --- |
| key | const T\& | [Dictionary](../../dictionary/) المفتاح. |

### قيمة الإرجاع

مرجع إلى القيمة المقابلة للمفتاح الممرَّر، سواء كانت موجودة أو تم إنشاؤها حديثًا.

## انظر أيضًا

* الفئة [DictionaryPtr](../)
* النطاق [System::Collections::Generic](../../)
* المكتبة [Aspose.Slides](../../../)