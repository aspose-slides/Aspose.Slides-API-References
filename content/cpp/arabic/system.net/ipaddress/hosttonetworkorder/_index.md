---
title: HostToNetworkOrder()
second_title: Aspose.Slides لـ C++ مرجع API
description: يقوم بتحويل ترتيب البايت المضيف المحدد إلى ترتيب البايت الشبكي المقابل.
type: docs
weight: 248
url: /ar/system.net/ipaddress/hosttonetworkorder/
---
## IPAddress::HostToNetworkOrder(int64_t) طريقة

يقوم بتحويل ترتيب البايت المضيف المحدد إلى ترتيب البايت الشبكي المقابل.

```cpp
static int64_t System::Net::IPAddress::HostToNetworkOrder(int64_t host)
```

### المُعاملات

| Parameter | Type | Description |
| --- | --- | --- |
| host | **int64_t** | القيمة التي سيتم تحويلها. |

### قيمة الإرجاع

ترتيب البايت الشبكي المقابل.

## IPAddress::HostToNetworkOrder(int32_t) طريقة

يقوم بتحويل ترتيب البايت المضيف المحدد إلى ترتيب البايت الشبكي المقابل.

```cpp
static int32_t System::Net::IPAddress::HostToNetworkOrder(int32_t host)
```

### المُعاملات

| Parameter | Type | Description |
| --- | --- | --- |
| host | **int32_t** | القيمة التي سيتم تحويلها. |

### قيمة الإرجاع

ترتيب البايت الشبكي المقابل.

## IPAddress::HostToNetworkOrder(int16_t) طريقة

يقوم بتحويل ترتيب البايت المضيف المحدد إلى ترتيب البايت الشبكي المقابل.

```cpp
static int16_t System::Net::IPAddress::HostToNetworkOrder(int16_t host)
```

### المُعاملات

| Parameter | Type | Description |
| --- | --- | --- |
| host | **int16_t** | القيمة التي سيتم تحويلها. |

### قيمة الإرجاع

ترتيب البايت الشبكي المقابل.

## انظر أيضًا

* الفئة [IPAddress](../)
* النطاق [System::Net](../../)
* المكتبة [Aspose.Slides](../../../)