---
title: AddLine()
second_title: Aspose.Slides لواجهة برمجة التطبيقات C++
description: يضيف الخط المحدد إلى المسار الممثل بالكائن الحالي.
type: docs
weight: 144
url: /ar/system.drawing.drawing2d/graphicspath/addline/
---
## GraphicsPath::AddLine(const Point\&, const Point\&) طريقة

يضيف الخط المحدد إلى المسار الممثل بالكائن الحالي.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddLine(const Point &pt1, const Point &pt2)
```

### المعلمات

| المعلمة | النوع | الوصف |
| --- | --- | --- |
| pt1 | const [Point](../../../system.drawing/point/)\& | نقطة البداية للخط المراد إضافته |
| pt2 | const [Point](../../../system.drawing/point/)\& | نقطة النهاية للخط المراد إضافته |

## GraphicsPath::AddLine(const PointF\&, const PointF\&) طريقة

يضيف الخط المحدد إلى المسار الممثل بالكائن الحالي.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddLine(const PointF &pt1, const PointF &pt2)
```

### المعلمات

| المعلمة | النوع | الوصف |
| --- | --- | --- |
| pt1 | const [PointF](../../../system.drawing/pointf/)\& | نقطة البداية للخط المراد إضافته |
| pt2 | const [PointF](../../../system.drawing/pointf/)\& | نقطة النهاية للخط المراد إضافته |

## GraphicsPath::AddLine(int, int, int, int) طريقة

يضيف الخط المحدد إلى المسار الممثل بالكائن الحالي.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddLine(int x1, int y1, int x2, int y2)
```

### المعلمات

| المعلمة | النوع | الوصف |
| --- | --- | --- |
| x1 | int | إحداثي X لنقطة البداية للخط المراد إضافته |
| y1 | int | إحداثي Y لنقطة البداية للخط المراد إضافته |
| x2 | int | إحداثي X لنقطة النهاية للخط المراد إضافته |
| y2 | int | إحداثي Y لنقطة النهاية للخط المراد إضافته |

## GraphicsPath::AddLine(float, float, float, float) طريقة

يضيف الخط المحدد إلى المسار الممثل بالكائن الحالي.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddLine(float x1, float y1, float x2, float y2)
```

### المعلمات

| المعلمة | النوع | الوصف |
| --- | --- | --- |
| x1 | **float** | إحداثي X لنقطة البداية للخط المراد إضافته |
| y1 | **float** | إحداثي Y لنقطة البداية للخط المراد إضافته |
| x2 | **float** | إحداثي X لنقطة النهاية للخط المراد إضافته |
| y2 | **float** | إحداثي Y لنقطة النهاية للخط المراد إضافته |

## انظر أيضًا

* الفئة [Point](../../../system.drawing/point/)
* الفئة [GraphicsPath](../)
* الفئة [PointF](../../../system.drawing/pointf/)
* النطاق [System::Drawing::Drawing2D](../../)
* المكتبة [Aspose.Slides](../../../)