---
title: TransformVectors()
second_title: مرجع واجهة برمجة التطبيقات Aspose.Slides للغة C++
description: يقوم بتطبيق مكونات المقياس والدوران فقط للمصفوفة التي يمثلها الكائن الحالي على النقاط المحددة.
type: docs
weight: 209
url: /ar/system.drawing.drawing2d/matrix/transformvectors/
---
## Matrix::TransformVectors(const ArrayPtr\<Point\>\&) طريقة

يقوم بتطبيق مركبات المقياس والدوران فقط للمصفوفة التي يمثلها الكائن الحالي على النقاط المحددة.

```cpp
void System::Drawing::Drawing2D::Matrix::TransformVectors(const ArrayPtr<Point> &pts)
```

### المعلمات

| Parameter | Type | Description |
| --- | --- | --- |
| pts | const [ArrayPtr](../../../system/arrayptr/)\<[Point](../../../system.drawing/point/)\>\& | مصفوفة تحتوي على النقاط للتحويل |

## Matrix::TransformVectors(const System::Details::ArrayView\<Point\>\&) طريقة

يقوم بتطبيق مركبات المقياس والدوران فقط للمصفوفة التي يمثلها الكائن الحالي على النقاط المحددة.

```cpp
void System::Drawing::Drawing2D::Matrix::TransformVectors(const System::Details::ArrayView<Point> &pts)
```

### المعلمات

| Parameter | Type | Description |
| --- | --- | --- |
| pts | const System::Details::ArrayView\<[Point](../../../system.drawing/point/)\>\& | عرض مصفوفة يحتوي على النقاط للتحويل |

## Matrix::TransformVectors(const ArrayPtr\<PointF\>\&) طريقة

يقوم بتطبيق مركبات المقياس والدوران فقط للمصفوفة التي يمثلها الكائن الحالي على النقاط المحددة.

```cpp
void System::Drawing::Drawing2D::Matrix::TransformVectors(const ArrayPtr<PointF> &pts)
```

### المعلمات

| Parameter | Type | Description |
| --- | --- | --- |
| pts | const [ArrayPtr](../../../system/arrayptr/)\<[PointF](../../../system.drawing/pointf/)\>\& | مصفوفة تحتوي على النقاط للتحويل |

## Matrix::TransformVectors(const System::Details::ArrayView\<PointF\>\&) طريقة

يقوم بتطبيق مركبات المقياس والدوران فقط للمصفوفة التي يمثلها الكائن الحالي على النقاط المحددة.

```cpp
void System::Drawing::Drawing2D::Matrix::TransformVectors(const System::Details::ArrayView<PointF> &pts)
```

### المعلمات

| Parameter | Type | Description |
| --- | --- | --- |
| pts | const System::Details::ArrayView\<[PointF](../../../system.drawing/pointf/)\>\& | عرض مصفوفة يحتوي على النقاط للتحويل |

## انظر أيضا

* تعريف نوع [ArrayPtr](../../../system/arrayptr/)
* فئة [Point](../../../system.drawing/point/)
* فئة [Matrix](../)
* فئة [PointF](../../../system.drawing/pointf/)
* مساحة الاسم [System::Drawing::Drawing2D](../../)
* مكتبة [Aspose.Slides](../../../)