---
title: TransformPoints()
second_title: مرجع API Aspose.Slides للغة C++
description: يطبّق التحويل الهندسي المحدد بالمصفوفة التي يمثلها الكائن الحالي على النقاط المحددة.
type: docs
weight: 196
url: /ar/system.drawing.drawing2d/matrix/transformpoints/
---
## Matrix::TransformPoints(const ArrayPtr\<Point\>\&) طريقة

يطبق التحويل الهندسي المحدد بالمصفوفة التي يمثلها الكائن الحالي على النقاط المحددة.

```cpp
void System::Drawing::Drawing2D::Matrix::TransformPoints(const ArrayPtr<Point> &pts)
```

### المعلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| pts | const [ArrayPtr](../../../system/arrayptr/)\<[Point](../../../system.drawing/point/)\>\& | مصفوفة تحتوي على النقاط التي سيتم تحويلها |

## Matrix::TransformPoints(const System::Details::ArrayView\<Point\>\&) طريقة

يطبق التحويل الهندسي المحدد بالمصفوفة التي يمثلها الكائن الحالي على النقاط المحددة.

```cpp
void System::Drawing::Drawing2D::Matrix::TransformPoints(const System::Details::ArrayView<Point> &pts)
```

### المعلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| pts | const System::Details::ArrayView\<[Point](../../../system.drawing/point/)\>\& | عرض مصفوفة يحتوي على النقاط التي سيتم تحويلها |

## Matrix::TransformPoints(const ArrayPtr\<PointF\>\&) طريقة

يطبق التحويل الهندسي المحدد بالمصفوفة التي يمثلها الكائن الحالي على النقاط المحددة.

```cpp
void System::Drawing::Drawing2D::Matrix::TransformPoints(const ArrayPtr<PointF> &pts)
```

### المعلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| pts | const [ArrayPtr](../../../system/arrayptr/)\<[PointF](../../../system.drawing/pointf/)\>\& | مصفوفة تحتوي على النقاط التي سيتم تحويلها |

## Matrix::TransformPoints(const System::Details::ArrayView\<PointF\>\&) طريقة

يطبق التحويل الهندسي المحدد بالمصفوفة التي يمثلها الكائن الحالي على النقاط المحددة.

```cpp
void System::Drawing::Drawing2D::Matrix::TransformPoints(const System::Details::ArrayView<PointF> &pts)
```

### المعلمات

| المعامل | النوع | الوصف |
| --- | --- | --- |
| pts | const System::Details::ArrayView\<[PointF](../../../system.drawing/pointf/)\>\& | عرض مصفوفة يحتوي على النقاط التي سيتم تحويلها |

## انظر أيضًا

* تعريف نوع [ArrayPtr](../../../system/arrayptr/)
* فئة [Point](../../../system.drawing/point/)
* فئة [Matrix](../)
* فئة [PointF](../../../system.drawing/pointf/)
* مساحة اسم [System::Drawing::Drawing2D](../../)
* مكتبة [Aspose.Slides](../../../)