---
title: Xor()
second_title: Aspose.Slides untuk Referensi API C++
description: Mengganti region yang direpresentasikan oleh objek saat ini dengan bagian-bagian dari region ini dan region yang didefinisikan oleh recangle yang ditentukan yang tidak beririsan.
type: docs
weight: 144
url: /id/system.drawing/region/xor/
---
## Region::Xor(const RectangleF\&) metode


Mengganti region yang direpresentasikan oleh objek saat ini dengan bagian-bagian dari region ini dan region yang didefinisikan oleh recangle yang ditentukan yang tidak beririsan.

```cpp
void System::Drawing::Region::Xor(const RectangleF &rect)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| rect | const [RectangleF](../../rectanglef/)\& | Sebuah persegi panjang yang mendefinisikan sebuah region untuk xor dengan region yang direpresentasikan oleh objek saat ini |

## Region::Xor(const Rectangle\&) metode


Mengganti region yang direpresentasikan oleh objek saat ini dengan bagian-bagian dari region ini dan region yang didefinisikan oleh recangle yang ditentukan yang tidak beririsan.

```cpp
void System::Drawing::Region::Xor(const Rectangle &rect)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| rect | const [Rectangle](../../rectangle/)\& | Sebuah persegi panjang yang mendefinisikan sebuah region untuk xor dengan region yang direpresentasikan oleh objek saat ini |

## Region::Xor(const SharedPtr\<Drawing2D::GraphicsPath\>\&) metode


Mengganti region yang direpresentasikan oleh objek saat ini dengan bagian-bagian dari region ini dan region yang didefinisikan oleh path yang ditentukan yang tidak beririsan.

```cpp
void System::Drawing::Region::Xor(const SharedPtr<Drawing2D::GraphicsPath> &path)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| path | const [SharedPtr](../../../system/sharedptr/)\<[Drawing2D::GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)\>\& | Sebuah path yang mendefinisikan sebuah region untuk xor dengan region yang direpresentasikan oleh objek saat ini |

## Region::Xor(const SharedPtr\<Region\>\&) metode


Mengganti region yang direpresentasikan oleh objek saat ini dengan bagian-bagian dari region ini dan region yang ditentukan yang tidak beririsan.

```cpp
void System::Drawing::Region::Xor(const SharedPtr<Region> &region)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| region | const [SharedPtr](../../../system/sharedptr/)\<[Region](../)\>\& | Sebuah region untuk xor dengan region yang direpresentasikan oleh objek saat ini |

## Lihat Juga

* Typedef [SharedPtr](../../../system/sharedptr/)
* Kelas [RectangleF](../../rectanglef/)
* Kelas [Region](../)
* Kelas [Rectangle](../../rectangle/)
* Kelas [GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)
* Ruang Nama [System::Drawing](../../)
* Pustaka [Aspose.Slides](../../../)