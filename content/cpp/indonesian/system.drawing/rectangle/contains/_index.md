---
title: Contains()
second_title: Referensi API Aspose.Slides untuk C++
description: Menentukan apakah titik yang ditentukan berada di dalam rectangle yang direpresentasikan oleh objek saat ini.
type: docs
weight: 248
url: /id/system.drawing/rectangle/contains/
---
## Rectangle::Contains(int, int) const metode

Menentukan apakah titik yang ditentukan berada di dalam Rectangle yang direpresentasikan oleh objek saat ini.

```cpp
bool System::Drawing::Rectangle::Contains(int x, int y) const
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| x | int | Koordinat X dari titik yang akan diperiksa |
| y | int | Koordinat Y dari titik yang akan diperiksa |

### Nilai Kembali

True jika titik yang ditentukan berada di dalam Rectangle yang direpresentasikan oleh objek saat ini, jika tidak - false

## Rectangle::Contains(const Point\&) const metode

Menentukan apakah titik yang ditentukan berada di dalam Rectangle yang direpresentasikan oleh objek saat ini.

```cpp
bool System::Drawing::Rectangle::Contains(const Point &point) const
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| point | const [Point](../../point/)\& | Sebuah titik untuk diperiksa |

### Nilai Kembali

True jika titik yang ditentukan berada di dalam Rectangle yang direpresentasikan oleh objek saat ini, jika tidak - false

## Rectangle::Contains(const Rectangle\&) const metode

Menentukan apakah Rectangle yang ditentukan berada di dalam Rectangle yang direpresentasikan oleh objek saat ini.

```cpp
bool System::Drawing::Rectangle::Contains(const Rectangle &rect) const
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| rect | const [Rectangle](../)\& | Sebuah Rectangle untuk diperiksa |

### Nilai Kembali

True jika Rectangle yang ditentukan berada di dalam Rectangle yang direpresentasikan oleh objek saat ini, jika tidak - false

## Lihat Juga

* Kelas [Rectangle](../)
* Kelas [Point](../../point/)
* Ruang Nama [System::Drawing](../../)
* Pustaka [Aspose.Slides](../../../)