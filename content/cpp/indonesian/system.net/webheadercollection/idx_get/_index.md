---
title: idx_get()
second_title: Referensi API Aspose.Slides untuk C++
description: Mengambil nilai header menggunakan header permintaan yang ditentukan.
type: docs
weight: 14
url: /id/system.net/webheadercollection/idx_get/
---
## WebHeaderCollection::idx_get(HttpRequestHeader) metode

Mengambil nilai header menggunakan header permintaan yang ditentukan.

```cpp
String System::Net::WebHeaderCollection::idx_get(HttpRequestHeader header)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| header | [HttpRequestHeader](../../httprequestheader/) | Header. |

### Nilai Kembali

Nilai header menggunakan header yang ditentukan.

## WebHeaderCollection::idx_get(HttpResponseHeader) metode

Mengambil nilai header menggunakan header respons yang ditentukan.

```cpp
String System::Net::WebHeaderCollection::idx_get(HttpResponseHeader header)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| header | [HttpResponseHeader](../../httpresponseheader/) | Header. |

### Nilai Kembali

Nilai header menggunakan header yang ditentukan.

## WebHeaderCollection::idx_get(String) metode

Mengambil nilai header menggunakan nama header yang ditentukan.

```cpp
String System::Net::WebHeaderCollection::idx_get(String name)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| name | [String](../../../system/string/) | Nama header. |

### Nilai Kembali

Nilai header menggunakan nama header yang ditentukan.

## Lihat Juga

* Enum [HttpRequestHeader](../../httprequestheader/)
* Enum [HttpResponseHeader](../../httpresponseheader/)
* Class [String](../../../system/string/)
* Class [WebHeaderCollection](../)
* Namespace [System::Net](../../)
* Library [Aspose.Slides](../../../)