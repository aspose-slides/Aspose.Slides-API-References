---
title: operator[]()
second_title: Aspose.Slides untuk Referensi API C++
description: Operator akses untuk bekerja dengan konversi tipe kunci.
type: docs
weight: 14
url: /id/system.collections.generic/dictionaryptr/operator[]/
---
## DictionaryPtr::operator[](const X\&) metode const

Operator akses untuk bekerja dengan konversi tipe kunci.

```cpp
template<class X> V & System::Collections::Generic::DictionaryPtr<T, V>::operator[](const X &key) const
```

### Parameter templat

| Parameter | Deskripsi |
| --- | --- |
| X | Tipe kunci sumber. |

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| key | const X\& | [Dictionary](../../dictionary/) kunci. |

### Nilai Kembalian

Referensi ke nilai yang sesuai dengan kunci yang diberikan, yang sudah ada atau baru dibuat.

## DictionaryPtr::operator[](const T\&) metode const

Operator akses.

```cpp
V & System::Collections::Generic::DictionaryPtr<T, V>::operator[](const T &key) const
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| key | const T\& | [Dictionary](../../dictionary/) kunci. |

### Nilai Kembalian

Referensi ke nilai yang sesuai dengan kunci yang diberikan, yang sudah ada atau baru dibuat.

## Lihat Juga

* Kelas [DictionaryPtr](../)
* Ruang Nama [System::Collections::Generic](../../)
* Perpustakaan [Aspose.Slides](../../../)