---
title: IsControl()
second_title: Referensi API Aspose.Slides untuk C++
description: Menentukan apakah karakter pada indeks yang ditentukan dalam buffer karakter yang ditentukan diklasifikasikan sebagai karakter kontrol Unicode.
type: docs
weight: 66
url: /id/system/char/iscontrol/
---
## Char::IsControl(const char_t *, int) metode


Menentukan apakah karakter pada indeks yang ditentukan dalam buffer karakter yang ditentukan diklasifikasikan sebagai karakter kontrol Unicode.

```cpp
static bool System::Char::IsControl(const char_t *str, int idx)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| str | const char_t * | Penunjuk ke awal buffer karakter |
| idx | int | Indeks berbasis nol dalam buffer yang ditentukan untuk karakter yang akan diuji |

### Nilai Kembalian

True if the character at the specified index is a Unicode control character, otherwise - false

## Char::IsControl(char_t) metode


Menentukan apakah karakter yang ditentukan diklasifikasikan sebagai karakter kontrol Unicode.

```cpp
static bool System::Char::IsControl(char_t c)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| c | char_t | Karakter yang akan diuji |

### Nilai Kembalian

True if the specified character is a Unicode control character, otherwise - false

## Lihat Juga

* Kelas [Char](../)
* Ruang Nama [System](../../)
* Perpustakaan [Aspose.Slides](../../../)