---
title: operator()()
second_title: Referensi API Aspose.Slides untuk C++
description: Memanggil semua delegate yang saat ini ada dalam koleksi delegate. Delegate dipanggil dalam urutan yang sama dengan saat mereka ditambahkan ke koleksi. Operator memblokir sementara delegate dieksekusi.
type: docs
weight: 235
url: /id/system/multicastdelegate_tmpl_returntype_lbrace_argumenttypes_dots_rbrace__end_tmpl/operator_call/
---
## MulticastDelegate< ReturnType(ArgumentTypes...)>::operator()(ArgumentTypes...) const method

Memanggil semua delegate yang saat ini ada di koleksi delegate. Delegate dipanggil dalam urutan yang sama dengan saat mereka ditambahkan ke koleksi. Operator memblokir sementara delegate dieksekusi.

```cpp
ReturnType System::MulticastDelegate<ReturnType(ArgumentTypes...)>::operator()(ArgumentTypes... args) const
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| args | ArgumentTypes... | Argumen yang akan diteruskan ke delegate yang akan dipanggil |

### Nilai Kembali

Nilai kembali dari delegate terakhir yang dipanggil

## Lihat Juga

* Class [MulticastDelegate< ReturnType(ArgumentTypes...)>](../)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)