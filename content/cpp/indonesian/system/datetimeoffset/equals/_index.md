---
title: Equals()
second_title: Aspose.Slides untuk Referensi API C++
description: Memeriksa apakah dua objek DateTimeOffset mewakili titik waktu yang sama.
type: docs
weight: 365
url: /id/system/datetimeoffset/equals/
---
## DateTimeOffset::Equals(const DateTimeOffset\&) const metode


Memeriksa apakah dua [DateTimeOffset](../) objek mewakili titik waktu yang sama.

```cpp
bool System::DateTimeOffset::Equals(const DateTimeOffset &other) const
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/) untuk dibandingkan. |

### Nilai Kembalian

true jika objek saat ini sama dengan **other**, jika tidak - false.

## DateTimeOffset::Equals(const SharedPtr\<Object\>\&) const metode


Memeriksa apakah dua [DateTimeOffset](../) objek mewakili titik waktu yang sama.

```cpp
bool System::DateTimeOffset::Equals(const SharedPtr<Object> &obj) const
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/) untuk dibandingkan. |

### Nilai Kembalian

true jika objek saat ini sama dengan **obj**, jika tidak - false.

## DateTimeOffset::Equals(const DateTimeOffset\&, const DateTimeOffset\&) metode


Memeriksa apakah dua [DateTimeOffset](../) objek mewakili titik waktu yang sama.

```cpp
static bool System::DateTimeOffset::Equals(const DateTimeOffset &first, const DateTimeOffset &second)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| first | const [DateTimeOffset](../)\& | Objek pertama untuk dibandingkan. |
| second | const [DateTimeOffset](../)\& | Objek kedua untuk dibandingkan. |

### Nilai Kembalian

true jika **first** sama dengan **second**, jika tidak - false.

## Lihat Juga

* Typedef [SharedPtr](../../sharedptr/)
* Kelas [DateTimeOffset](../)
* Kelas [Object](../../object/)
* Ruang Nama [System](../../)
* Pustaka [Aspose.Slides](../../../)