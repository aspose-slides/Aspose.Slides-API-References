---
title: WriteLine()
second_title: Referensi API Aspose.Slides untuk C++
description: Mengeluarkan penanda baris saat ini ke aliran output standar.
type: docs
weight: 14
url: /id/system/console/writeline/
---
## Console::WriteLine() metode


Mengeluarkan penanda baris saat ini ke aliran output standar.

```cpp
static void System::Console::WriteLine()
```

## Console::WriteLine(const SharedPtr\<T\>\&) metode


Mengeluarkan representasi string dari objek yang ditentukan diikuti oleh penanda baris saat ini ke aliran output standar.

```cpp
template<class T> static void System::Console::WriteLine(const SharedPtr<T> &object)
```


### Parameter templat

| Parameter | Deskripsi |
| --- | --- |
| T | Tipe objek yang akan dikeluarkan |

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| object | const [SharedPtr](../../sharedptr/)\<T\>\& | [Object](../../object/) untuk dikeluarkan |

## Console::WriteLine(bool) metode


Mengeluarkan representasi string dari nilai bool diikuti oleh penanda baris saat ini ke aliran output standar.

```cpp
static void System::Console::WriteLine(bool value)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **bool** | Nilai yang akan dikeluarkan |

## Console::WriteLine(char_t) metode


Mengeluarkan nilai karakter yang ditentukan diikuti oleh penanda baris saat ini ke aliran output standar.

```cpp
static void System::Console::WriteLine(char_t value)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | char_t | Nilai yang akan dikeluarkan |

## Console::WriteLine(const ArrayPtr\<char_t\>\&) metode


Mengeluarkan representasi string dari array karakter yang ditentukan diikuti oleh penanda baris saat ini ke aliran output standar.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Array yang akan dikeluarkan |

## Console::WriteLine(const Decimal\&) metode


Mengeluarkan representasi string dari nilai [Decimal](../../decimal/) diikuti oleh penanda baris saat ini ke aliran output standar.

```cpp
static void System::Console::WriteLine(const Decimal &value)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | Nilai yang akan dikeluarkan |

## Console::WriteLine(double) metode


Mengeluarkan representasi string dari nilai floating-point double-precision diikuti oleh penanda baris saat ini ke aliran output standar.

```cpp
static void System::Console::WriteLine(double value)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **double** | Nilai yang akan dikeluarkan |

## Console::WriteLine(float) metode


Mengeluarkan representasi string dari nilai floating-point single-precision diikuti oleh penanda baris saat ini ke aliran output standar.

```cpp
static void System::Console::WriteLine(float value)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **float** | Nilai yang akan dikeluarkan |

## Console::WriteLine(int32_t) metode


Mengeluarkan representasi string dari nilai integer 32-bit diikuti oleh penanda baris saat ini ke aliran output standar.

```cpp
static void System::Console::WriteLine(int32_t value)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **int32_t** | Nilai yang akan dikeluarkan |

## Console::WriteLine(int64_t) metode


Mengeluarkan representasi string dari nilai integer 64-bit diikuti oleh penanda baris saat ini ke aliran output standar.

```cpp
static void System::Console::WriteLine(int64_t value)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **int64_t** | Nilai yang akan dikeluarkan |

## Console::WriteLine(const String\&) metode


Mengeluarkan objek string yang ditentukan diikuti oleh penanda baris saat ini ke aliran output standar.

```cpp
static void System::Console::WriteLine(const String &value)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | const [String](../../string/)\& | Objek string yang akan dikeluarkan |

## Console::WriteLine(const char_t *) metode


Mengeluarkan c-string yang ditentukan diikuti oleh penanda baris saat ini ke aliran output standar.

```cpp
static void System::Console::WriteLine(const char_t *value)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | const char_t * | c-string yang akan dikeluarkan |

## Console::WriteLine(const TypeInfo\&) metode


Mengeluarkan representasi string dari nilai [TypeInfo](../../typeinfo/) diikuti oleh penanda baris saat ini ke aliran output standar.

```cpp
static void System::Console::WriteLine(const TypeInfo &value)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | Nilai yang akan dikeluarkan |

## Console::WriteLine(uint32_t) metode


Mengeluarkan representasi string dari nilai unsigned integer 32-bit diikuti oleh penanda baris saat ini ke aliran output standar.

```cpp
static void System::Console::WriteLine(uint32_t value)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **uint32_t** | Nilai yang akan dikeluarkan |

## Console::WriteLine(uint64_t) metode


Mengeluarkan representasi string dari nilai unsigned integer 64-bit diikuti oleh penanda baris saat ini ke aliran output standar.

```cpp
static void System::Console::WriteLine(uint64_t value)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **uint64_t** | Nilai yang akan dikeluarkan |

## Console::WriteLine(const ArrayPtr\<char_t\>\&, int, int) metode


Mengeluarkan representasi string dari rentang yang ditentukan dari array karakter yang ditentukan diikuti oleh penanda baris saat ini ke aliran output standar.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer, int index, int count)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Array karakter |
| index | int | Indeks dalam array tempat rentang yang akan dikeluarkan dimulai |
| count | int | Jumlah elemen dalam rentang yang akan dikeluarkan |

## Console::WriteLine(const Exception\&) metode


Mengeluarkan representasi string dari objek Exception yang ditentukan diikuti oleh penanda baris saat ini ke aliran output standar.

```cpp
static void System::Console::WriteLine(const Exception &e)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| e | const [Exception](../../exception/)\& | Nilai yang akan dikeluarkan |

## Console::WriteLine(const String\&, Args\&&...) metode


Mengeluarkan representasi string dari argumen yang ditentukan diformat sesuai format yang diberikan diikuti oleh penanda baris saat ini ke aliran output standar.

```cpp
template<class...> static void System::Console::WriteLine(const String &format, Args &&... args)
```


### Parameter templat

| Parameter | Deskripsi |
| --- | --- |
| The | tipe nilai yang akan dikeluarkan |

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| format | const [String](../../string/)\& | Format string |
| args | Args\&&... | Nilai yang akan dikeluarkan |

## Console::WriteLine(const char *) metode




```cpp
static void System::Console::WriteLine(const char *)=delete
```

## Lihat Juga

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Typedef [Exception](../../exception/)
* Kelas [Console](../)
* Kelas [Decimal](../../decimal/)
* Kelas [String](../../string/)
* Kelas [TypeInfo](../../typeinfo/)
* Ruang nama [System](../../)
* Library [Aspose.Slides](../../../)