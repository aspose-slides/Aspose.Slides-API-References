---
title: WriteLine()
second_title: Referensi API Aspose.Slides untuk C++
description: Mengeluarkan penanda akhir baris saat ini ke aliran output yang diwakili oleh objek saat ini.
type: docs
weight: 27
url: /id/system/consoleoutput/writeline/
---
## ConsoleOutput::WriteLine() metode


Mengeluarkan penanda akhir baris saat ini ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::WriteLine() override
```

## ConsoleOutput::WriteLine(const SharedPtr\<Object\>\&) metode


Mengeluarkan representasi string dari objek yang ditentukan diikuti oleh penanda akhir baris saat ini ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::WriteLine(const SharedPtr<Object> &value) override
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Objek yang akan dikeluarkan |

## ConsoleOutput::WriteLine(bool) metode


Mengeluarkan representasi string dari nilai bool yang ditentukan diikuti oleh penanda akhir baris saat ini ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::WriteLine(bool value) override
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **bool** | Objek yang akan dikeluarkan |

## ConsoleOutput::WriteLine(char_t) metode


Mengeluarkan nilai karakter yang ditentukan diikuti oleh penanda akhir baris saat ini ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::WriteLine(char_t value) override
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | char_t | Nilai yang akan dikeluarkan |

## ConsoleOutput::WriteLine(Decimal) metode


Mengeluarkan representasi string dari nilai [Decimal](../../decimal/) diikuti oleh penanda akhir baris saat ini ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::WriteLine(Decimal value) override
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | Nilai yang akan dikeluarkan |

## ConsoleOutput::WriteLine(double) metode


Mengeluarkan representasi string dari nilai floating-point double-presisi diikuti oleh penanda akhir baris saat ini ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::WriteLine(double value) override
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **double** | Nilai yang akan dikeluarkan |

## ConsoleOutput::WriteLine(int) metode


Mengeluarkan representasi string dari nilai integer 32-bit diikuti oleh penanda akhir baris saat ini ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::WriteLine(int value) override
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | int | Nilai yang akan dikeluarkan |

## ConsoleOutput::WriteLine(int64_t) metode


Mengeluarkan representasi string dari nilai integer 64-bit diikuti oleh penanda akhir baris saat ini ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::WriteLine(int64_t value) override
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **int64_t** | Nilai yang akan dikeluarkan |

## ConsoleOutput::WriteLine(float) metode


Mengeluarkan representasi string dari nilai floating-point presisi tunggal diikuti oleh penanda akhir baris saat ini ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::WriteLine(float value) override
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **float** | Nilai yang akan dikeluarkan |

## ConsoleOutput::WriteLine(const String\&) metode


Mengeluarkan objek string yang ditentukan diikuti oleh penanda akhir baris saat ini ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::WriteLine(const String &value) override
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | const [String](../../string/)\& | Objek string yang akan dikeluarkan |

## ConsoleOutput::WriteLine(uint32_t) metode


Mengeluarkan representasi string dari nilai integer tak bertanda 32-bit diikuti oleh penanda akhir baris saat ini ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::WriteLine(uint32_t value) override
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **uint32_t** | Nilai yang akan dikeluarkan |

## ConsoleOutput::WriteLine(uint64_t) metode


Mengeluarkan representasi string dari nilai integer tak bertanda 64-bit diikuti oleh penanda akhir baris saat ini ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::WriteLine(uint64_t value) override
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **uint64_t** | Nilai yang akan dikeluarkan |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&) metode


Mengeluarkan representasi string dari array karakter yang ditentukan diikuti oleh penanda akhir baris saat ini ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer) override
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Array yang akan dikeluarkan |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&, int32_t, int32_t) metode


Mengeluarkan representasi string dari rentang nilai array karakter yang ditentukan diikuti oleh penanda akhir baris saat ini ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Array yang berisi nilai-nilai yang akan dikeluarkan |
| index | **int32_t** | Indeks di mana rentang elemen yang akan dikeluarkan dimulai |
| count | **int32_t** | Jumlah elemen dalam rentang elemen yang akan dikeluarkan |

## ConsoleOutput::WriteLine(const char_t *) metode


Mengeluarkan c-string yang ditentukan diikuti oleh penanda akhir baris saat ini ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::WriteLine(const char_t *value) override
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | const char_t * | C-string yang akan dikeluarkan |

## ConsoleOutput::WriteLine(const TypeInfo\&) metode


Mengeluarkan representasi string dari objek [TypeInfo](../../typeinfo/) yang ditentukan diikuti oleh penanda akhir baris saat ini ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::WriteLine(const TypeInfo &value) override
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | Objek [TypeInfo](../../typeinfo/) yang akan dikeluarkan |

## ConsoleOutput::WriteLine(const char *) metode




```cpp
void System::ConsoleOutput::WriteLine(const char *)=delete
```

## Lihat Juga

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Kelas [ConsoleOutput](../)
* Kelas [Object](../../object/)
* Kelas [Decimal](../../decimal/)
* Kelas [String](../../string/)
* Kelas [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)