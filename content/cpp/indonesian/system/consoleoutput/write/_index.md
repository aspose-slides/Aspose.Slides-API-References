---
title: Write()
second_title: Referensi API Aspose.Slides untuk C++
description: Menghasilkan representasi string dari nilai bool yang ditentukan ke aliran output yang diwakili oleh objek saat ini.
type: docs
weight: 14
url: /id/system/consoleoutput/write/
---
## ConsoleOutput::Write(bool) metode

Menghasilkan representasi string dari nilai bool yang ditentukan ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::Write(bool value) override
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **bool** | Nilai yang akan dikeluarkan |

## ConsoleOutput::Write(const SharedPtr\<Object\>\&) metode

Menghasilkan representasi string dari objek yang ditentukan ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::Write(const SharedPtr<Object> &value) override
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Objek yang akan dikeluarkan |

## ConsoleOutput::Write(char_t) metode

Menghasilkan nilai karakter yang ditentukan ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::Write(char_t value) override
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | char_t | Nilai yang akan dikeluarkan |

## ConsoleOutput::Write(Decimal) metode

Menghasilkan representasi string dari nilai [Decimal](../../decimal/) ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::Write(Decimal value) override
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | Nilai yang akan dikeluarkan |

## ConsoleOutput::Write(double) metode

Menghasilkan representasi string dari nilai floating-point double-precision ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::Write(double value) override
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **double** | Nilai yang akan dikeluarkan |

## ConsoleOutput::Write(int32_t) metode

Menghasilkan representasi string dari nilai integer 32-bit ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::Write(int32_t value) override
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **int32_t** | Nilai yang akan dikeluarkan |

## ConsoleOutput::Write(int64_t) metode

Menghasilkan representasi string dari nilai integer 64-bit ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::Write(int64_t value) override
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **int64_t** | Nilai yang akan dikeluarkan |

## ConsoleOutput::Write(float) metode

Menghasilkan representasi string dari nilai floating-point single-precision ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::Write(float value) override
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **float** | Nilai yang akan dikeluarkan |

## ConsoleOutput::Write(const String\&) metode

Menghasilkan objek string yang ditentukan ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::Write(const String &value) override
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | const [String](../../string/)\& | Objek string yang akan dikeluarkan |

## ConsoleOutput::Write(uint32_t) metode

Menghasilkan representasi string dari nilai integer tak bertanda 32-bit ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::Write(uint32_t value) override
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **uint32_t** | Nilai yang akan dikeluarkan |

## ConsoleOutput::Write(uint64_t) metode

Menghasilkan representasi string dari nilai integer tak bertanda 64-bit ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::Write(uint64_t value) override
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **uint64_t** | Nilai yang akan dikeluarkan |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&) metode

Menghasilkan representasi string dari array karakter yang ditentukan ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer) override
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Array yang akan dikeluarkan |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&, int32_t, int32_t) metode

Menghasilkan representasi string dari rentang nilai array karakter yang ditentukan ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Array yang berisi nilai-nilai yang akan dikeluarkan |
| index | **int32_t** | Indeks di mana rentang elemen yang akan dikeluarkan dimulai |
| count | **int32_t** | Jumlah elemen dalam rentang elemen yang akan dikeluarkan |

## ConsoleOutput::Write(const char_t *) metode

Menghasilkan c-string yang ditentukan ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::Write(const char_t *value) override
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | const char_t * | c-string yang akan dikeluarkan |

## ConsoleOutput::Write(const TypeInfo\&) metode

Menghasilkan representasi string dari objek [TypeInfo](../../typeinfo/) yang ditentukan ke aliran output yang diwakili oleh objek saat ini.

```cpp
void System::ConsoleOutput::Write(const TypeInfo &value) override
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | Objek [TypeInfo](../../typeinfo/) yang akan dikeluarkan |

## ConsoleOutput::Write(const char *) metode

```cpp
void System::ConsoleOutput::Write(const char *)=delete
```

## Lihat Juga

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Kelas [ConsoleOutput](../)
* Kelas [Object](../../object/)
* Kelas [Decimal](../../decimal/)
* Kelas [String](../../string/)
* Kelas [TypeInfo](../../typeinfo/)
* Ruang Nama [System](../../)
* Library [Aspose.Slides](../../../)