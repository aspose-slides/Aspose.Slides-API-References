---
title: WeakReference()
second_title: Referensi API Aspose.Slides untuk C++
description: Konstruktor default.
type: docs
weight: 1
url: /id/system/weakreference_tmpl_end_tmpl/weakreference/
---
## WeakReference<>::WeakReference() metode

Konstruktor default.

```cpp
System::WeakReference<>::WeakReference()
```

## WeakReference<>::WeakReference(std::nullptr_t) metode

Konstruktor dari nullptr.

```cpp
System::WeakReference<>::WeakReference(std::nullptr_t)
```

## WeakReference<>::WeakReference(const SmartPtr\<Object\>\&) metode

Menginisialisasi sebuah instance baru dari kelas WeakReference, yang merujuk pada objek yang ditentukan.

```cpp
System::WeakReference<>::WeakReference(const SmartPtr<Object> &data)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<[Object](../../object/)\>\& | [Object](../../object/) untuk disimpan. |

## WeakReference<>::WeakReference(const SmartPtr\<Object\>\&, bool) metode

Menginisialisasi sebuah instance baru dari kelas WeakReference, yang merujuk pada objek yang ditentukan.

```cpp
System::WeakReference<>::WeakReference(const SmartPtr<Object> &data, bool trackResurrection)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<[Object](../../object/)\>\& | [Object](../../object/) untuk disimpan. |
| trackResurrection | **bool** | Diabaikan. |

## Lihat Juga

* Kelas [WeakReference<>](../)
* Kelas [SmartPtr](../../smartptr/)
* Kelas [Object](../../object/)
* Ruang nama [System](../../)
* Library [Aspose.Slides](../../../)