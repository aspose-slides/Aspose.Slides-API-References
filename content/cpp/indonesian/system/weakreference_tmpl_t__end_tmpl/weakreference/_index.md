---
title: WeakReference()
second_title: Referensi API Aspose.Slides untuk C++
description: Konstruktor default.
type: docs
weight: 1
url: /id/system/weakreference_tmpl_t__end_tmpl/weakreference/
---
## WeakReference< T >::WeakReference() metode

Konstruktor default.

```cpp
System::WeakReference<T>::WeakReference()
```

## WeakReference< T >::WeakReference(std::nullptr_t) metode

Konstruktor dari nullptr.

```cpp
System::WeakReference<T>::WeakReference(std::nullptr_t)
```

## WeakReference< T >::WeakReference(const SmartPtr\<T\>\&) metode

Menginisialisasi instance baru dari kelas WeakReference, merujuk ke objek yang ditentukan.

```cpp
System::WeakReference<T>::WeakReference(const SmartPtr<T> &data)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) untuk disimpan. |

## WeakReference< T >::WeakReference(const SmartPtr\<T\>\&, bool) metode

Menginisialisasi instance baru dari kelas WeakReference, merujuk ke objek yang ditentukan.

```cpp
System::WeakReference<T>::WeakReference(const SmartPtr<T> &data, bool trackResurrection)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) untuk disimpan. |
| trackResurrection | **bool** | Diabaikan. |

## Lihat Juga

* Kelas [WeakReference< T >](../)
* Kelas [SmartPtr](../../smartptr/)
* Namespace [System](../../)
* Perpustakaan [Aspose.Slides](../../../)