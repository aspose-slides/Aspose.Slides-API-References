---
title: TrimEnd()
second_title: Referensi API Aspose.Slides untuk C++
description: Menghapus elemen yang ditentukan dari akhir span bertipe.
type: docs
weight: 378
url: /id/system.memoryextensions/trimend/
---
## System::MemoryExtensions::TrimEnd(const ReadOnlySpan\<T\>\&, const T\&) fungsi


Memangkas elemen yang ditentukan dari akhir span bertipe.

```cpp
template<typename T> ReadOnlySpan<T> System::MemoryExtensions::TrimEnd(const ReadOnlySpan<T> &span, const T &trimElement)
```


### Parameter templat

| Parameter | Deskripsi |
| --- | --- |
| T | Tipe elemen dalam span |

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Span yang akan dipangkas |
| trimElement | const T\& | Elemen yang akan dipangkas |

### Nilai Kembalian

Span baru dengan elemen yang ditentukan dipangkas dari akhir

## System::MemoryExtensions::TrimEnd(Span\<T\>\&, const T\&) fungsi


Memangkas elemen yang ditentukan dari akhir span bertipe yang dapat diubah.

```cpp
template<typename T> Span<T> System::MemoryExtensions::TrimEnd(Span<T> &span, const T &trimElement)
```


### Parameter templat

| Parameter | Deskripsi |
| --- | --- |
| T | Tipe elemen dalam span |

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | [Span](../../system/span/)\<T\>\& | Span yang dapat diubah dan akan dipangkas |
| trimElement | const T\& | Elemen yang akan dipangkas |

### Nilai Kembalian

Span baru dengan elemen yang ditentukan dipangkas dari akhir

## System::MemoryExtensions::TrimEnd(const ReadOnlySpan\<T\>\&, const ReadOnlySpan\<T\>\&) fungsi


Memangkas elemen-elemen yang ditentukan dari akhir span bertipe.

```cpp
template<typename T> ReadOnlySpan<T> System::MemoryExtensions::TrimEnd(const ReadOnlySpan<T> &span, const ReadOnlySpan<T> &trimElements)
```


### Parameter templat

| Parameter | Deskripsi |
| --- | --- |
| T | Tipe elemen dalam span |

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Span yang akan dipangkas |
| trimElements | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Elemen-elemen yang akan dipangkas |

### Nilai Kembalian

Span baru dengan elemen-elemen yang ditentukan dipangkas dari akhir

## System::MemoryExtensions::TrimEnd(Span\<T\>\&, const ReadOnlySpan\<T\>\&) fungsi


Memangkas elemen-elemen yang ditentukan dari akhir span bertipe yang dapat diubah.

```cpp
template<typename T> Span<T> System::MemoryExtensions::TrimEnd(Span<T> &span, const ReadOnlySpan<T> &trimElements)
```


### Parameter templat

| Parameter | Deskripsi |
| --- | --- |
| T | Tipe elemen dalam span |

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | [Span](../../system/span/)\<T\>\& | Span yang dapat diubah dan akan dipangkas |
| trimElements | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Elemen-elemen yang akan dipangkas |

### Nilai Kembalian

Span baru dengan elemen-elemen yang ditentukan dipangkas dari akhir

## System::MemoryExtensions::TrimEnd(const ReadOnlySpan\<char16_t\>\&) fungsi


Menghapus karakter spasi dari akhir span karakter.

```cpp
ReadOnlySpan<char16_t> System::MemoryExtensions::TrimEnd(const ReadOnlySpan<char16_t> &span)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<char16_t\>\& | Span karakter yang akan dipangkas |

### Nilai Kembalian

Span baru dengan spasi yang dipangkas dari akhir

## System::MemoryExtensions::TrimEnd(Span\<char16_t\>\&) fungsi


Menghapus karakter spasi dari akhir span karakter yang dapat diubah.

```cpp
Span<char16_t> System::MemoryExtensions::TrimEnd(Span<char16_t> &span)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | [Span](../../system/span/)\<char16_t\>\& | Span karakter yang dapat diubah dan akan dipangkas |

### Nilai Kembalian

Span baru dengan spasi yang dipangkas dari akhir

## System::MemoryExtensions::TrimEnd(const ReadOnlySpan\<char16_t\>\&, char16_t) fungsi


Menghapus karakter yang ditentukan dari akhir span karakter.

```cpp
ReadOnlySpan<char16_t> System::MemoryExtensions::TrimEnd(const ReadOnlySpan<char16_t> &span, char16_t trimchar)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<char16_t\>\& | Span karakter yang akan dipangkas |
| trimchar | char16_t | Karakter yang akan dipangkas |

### Nilai Kembalian

Span baru dengan karakter yang ditentukan dipangkas dari akhir

## System::MemoryExtensions::TrimEnd(Span\<char16_t\>\&, char16_t) fungsi


Menghapus karakter yang ditentukan dari akhir span karakter yang dapat diubah.

```cpp
Span<char16_t> System::MemoryExtensions::TrimEnd(Span<char16_t> &span, char16_t trimchar)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | [Span](../../system/span/)\<char16_t\>\& | Span karakter yang dapat diubah dan akan dipangkas |
| trimchar | char16_t | Karakter yang akan dipangkas |

### Nilai Kembalian

Span baru dengan karakter yang ditentukan dipangkas dari akhir

## System::MemoryExtensions::TrimEnd(const ReadOnlySpan\<char16_t\>\&, const ReadOnlySpan\<char16_t\>\&) fungsi


Menghapus karakter-karakter yang ditentukan dari akhir span karakter.

```cpp
ReadOnlySpan<char16_t> System::MemoryExtensions::TrimEnd(const ReadOnlySpan<char16_t> &span, const ReadOnlySpan<char16_t> &trimChars)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<char16_t\>\& | Span karakter yang akan dipangkas |
| trimChars | const [ReadOnlySpan](../../system/readonlyspan/)\<char16_t\>\& | Karakter-karakter yang akan dipangkas |

### Nilai Kembalian

Span baru dengan karakter-karakter yang ditentukan dipangkas dari akhir

## System::MemoryExtensions::TrimEnd(Span\<char16_t\>\&, const ReadOnlySpan\<char16_t\>\&) fungsi


Menghapus karakter-karakter yang ditentukan dari akhir span karakter yang dapat diubah.

```cpp
Span<char16_t> System::MemoryExtensions::TrimEnd(Span<char16_t> &span, const ReadOnlySpan<char16_t> &trimchars)
```


### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | [Span](../../system/span/)\<char16_t\>\& | Span karakter yang dapat diubah dan akan dipangkas |
| trimchars | const [ReadOnlySpan](../../system/readonlyspan/)\<char16_t\>\& | Karakter-karakter yang akan dipangkas |

### Nilai Kembalian

Span baru dengan karakter-karakter yang ditentukan dipangkas dari akhir

## Lihat Juga

* Kelas [ReadOnlySpan](../../system/readonlyspan/)
* Kelas [Span](../../system/span/)
* Ruang Nama [System::MemoryExtensions](../)
* Library [Aspose.Slides](../../)