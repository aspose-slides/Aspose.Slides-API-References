---
title: IndexOfAny()
second_title: Referensi API Aspose.Slides untuk C++
description: Menemukan indeks kemunculan pertama dari salah satu dari dua nilai yang ditentukan dalam ReadOnlySpan<T>
type: docs
weight: 157
url: /id/system.memoryextensions/indexofany/
---
## System::MemoryExtensions::IndexOfAny(const ReadOnlySpan\<T\>\&, const T\&, const T\&) fungsi

Menemukan indeks kemunculan pertama dari salah satu dari dua nilai yang ditentukan dalam ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const ReadOnlySpan<T> &span, const T &value0, const T &value1)
```

### Parameter templat

| Parameter | Deskripsi |
| --- | --- |
| T | Tipe elemen dalam span |

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Span yang akan dicari |
| value0 | const T\& | Nilai pertama yang dicari |
| value1 | const T\& | Nilai kedua yang dicari |

### Nilai Kembalian

Indeks berbasis nol dari kemunculan pertama, atau -1 jika tidak ditemukan

## System::MemoryExtensions::IndexOfAny(const ReadOnlySpan\<T\>\&, const T\&, const T\&, const T\&) fungsi

Menemukan indeks kemunculan pertama dari salah satu dari tiga nilai yang ditentukan dalam ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const ReadOnlySpan<T> &span, const T &value0, const T &value1, const T &value2)
```

### Parameter templat

| Parameter | Deskripsi |
| --- | --- |
| T | Tipe elemen dalam span |

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Span yang akan dicari |
| value0 | const T\& | Nilai pertama yang dicari |
| value1 | const T\& | Nilai kedua yang dicari |
| value2 | const T\& | Nilai ketiga yang dicari |

### Nilai Kembalian

Indeks berbasis nol dari kemunculan pertama, atau -1 jika tidak ditemukan

## System::MemoryExtensions::IndexOfAny(const Span\<T\>\&, const T\&, const T\&) fungsi

Menemukan indeks kemunculan pertama dari salah satu dari dua nilai yang ditentukan dalam Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const Span<T> &span, const T &value0, const T &value1)
```

### Parameter templat

| Parameter | Deskripsi |
| --- | --- |
| T | Tipe elemen dalam span |

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Span yang akan dicari |
| value0 | const T\& | Nilai pertama yang dicari |
| value1 | const T\& | Nilai kedua yang dicari |

### Nilai Kembalian

Indeks berbasis nol dari kemunculan pertama, atau -1 jika tidak ditemukan

## System::MemoryExtensions::IndexOfAny(const Span\<T\>\&, const T\&, const T\&, const T\&) fungsi

Menemukan indeks kemunculan pertama dari salah satu dari tiga nilai yang ditentukan dalam Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const Span<T> &span, const T &value0, const T &value1, const T &value2)
```

### Parameter templat

| Parameter | Deskripsi |
| --- | --- |
| T | Tipe elemen dalam span |

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Span yang akan dicari |
| value0 | const T\& | Nilai pertama yang dicari |
| value1 | const T\& | Nilai kedua yang dicari |
| value2 | const T\& | Nilai ketiga yang dicari |

### Nilai Kembalian

Indeks berbasis nol dari kemunculan pertama, atau -1 jika tidak ditemukan

## System::MemoryExtensions::IndexOfAny(const ReadOnlySpan\<T\>\&, const ReadOnlySpan\<T\>\&) fungsi

Menemukan indeks kemunculan pertama dari nilai apa pun dari sebuah span dalam ReadOnlySpan<T> lain

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const ReadOnlySpan<T> &span, const ReadOnlySpan<T> &values)
```

### Parameter templat

| Parameter | Deskripsi |
| --- | --- |
| T | Tipe elemen dalam span |

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Span yang akan dicari |
| values | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Span yang berisi nilai-nilai yang dicari |

### Nilai Kembalian

Indeks berbasis nol dari kemunculan pertama, atau -1 jika tidak ditemukan

## System::MemoryExtensions::IndexOfAny(const Span\<T\>\&, const ReadOnlySpan\<T\>\&) fungsi

Menemukan indeks kemunculan pertama dari nilai apa pun dari sebuah span dalam Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const Span<T> &span, const ReadOnlySpan<T> &values)
```

### Parameter templat

| Parameter | Deskripsi |
| --- | --- |
| T | Tipe elemen dalam span |

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Span yang akan dicari |
| values | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Span yang berisi nilai-nilai yang dicari |

### Nilai Kembalian

Indeks berbasis nol dari kemunculan pertama, atau -1 jika tidak ditemukan

## Lihat Juga

* Kelas [ReadOnlySpan](../../system/readonlyspan/)
* Kelas [Span](../../system/span/)
* Namespace [System::MemoryExtensions](../)
* Library [Aspose.Slides](../../)