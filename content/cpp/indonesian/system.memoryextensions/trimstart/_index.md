---
title: TrimStart()
second_title: Referensi API Aspose.Slides untuk C++
description: Memangkas elemen yang ditentukan dari awal span yang bertipe.
type: docs
weight: 391
url: /id/system.memoryextensions/trimstart/
---
## System::MemoryExtensions::TrimStart(const ReadOnlySpan\<T\>\&, const T\&) function

Memangkas elemen yang ditentukan dari awal span yang bertipe.

```cpp
template<typename T> ReadOnlySpan<T> System::MemoryExtensions::TrimStart(const ReadOnlySpan<T> &span, const T &trimElement)
```

### Parameter templat

| Parameter | Deskripsi |
| --- | --- |
| T | Tipe elemen dalam span |

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Span yang akan dipangkas |
| trimElement | const T\& | Elemen yang akan dipangkas |

### Nilai Kembali

Span baru dengan elemen yang ditentukan dipangkas dari awal

## System::MemoryExtensions::TrimStart(Span\<T\>\&, const T\&) function

Memangkas elemen yang ditentukan dari awal span bertipe yang dapat diubah.

```cpp
template<typename T> Span<T> System::MemoryExtensions::TrimStart(Span<T> &span, const T &trimElement)
```

### Parameter templat

| Parameter | Deskripsi |
| --- | --- |
| T | Tipe elemen dalam span |

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | [Span](../../system/span/)\<T\>\& | Span yang dapat diubah dan akan dipangkas |
| trimElement | const T\& | Elemen yang akan dipangkas |

### Nilai Kembali

Span baru dengan elemen yang ditentukan dipangkas dari awal

## System::MemoryExtensions::TrimStart(const ReadOnlySpan\<T\>\&, const ReadOnlySpan\<T\>\&) function

Memangkas elemen yang ditentukan dari awal span yang bertipe.

```cpp
template<typename T> ReadOnlySpan<T> System::MemoryExtensions::TrimStart(const ReadOnlySpan<T> &span, const ReadOnlySpan<T> &trimElements)
```

### Parameter templat

| Parameter | Deskripsi |
| --- | --- |
| T | Tipe elemen dalam span |

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Span yang akan dipangkas |
| trimElements | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Elemen-elemen yang akan dipangkas |

### Nilai Kembali

Span baru dengan elemen yang ditentukan dipangkas dari awal

## System::MemoryExtensions::TrimStart(Span\<T\>\&, const ReadOnlySpan\<T\>\&) function

Memangkas elemen yang ditentukan dari awal span bertipe yang dapat diubah.

```cpp
template<typename T> Span<T> System::MemoryExtensions::TrimStart(Span<T> &span, const ReadOnlySpan<T> &trimElements)
```

### Parameter templat

| Parameter | Deskripsi |
| --- | --- |
| T | Tipe elemen dalam span |

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | [Span](../../system/span/)\<T\>\& | Span yang dapat diubah dan akan dipangkas |
| trimElements | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Elemen-elemen yang akan dipangkas |

### Nilai Kembali

Span baru dengan elemen yang ditentukan dipangkas dari awal

## System::MemoryExtensions::TrimStart(const ReadOnlySpan\<char16_t\>\&) function

Memangkas karakter spasi dari awal span karakter.

```cpp
ReadOnlySpan<char16_t> System::MemoryExtensions::TrimStart(const ReadOnlySpan<char16_t> &span)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<char16_t\>\& | Span karakter yang akan dipangkas |

### Nilai Kembali

Span baru dengan spasi dipangkas dari awal

## System::MemoryExtensions::TrimStart(Span\<char16_t\>\&) function

Memangkas karakter spasi dari awal span karakter yang dapat diubah.

```cpp
Span<char16_t> System::MemoryExtensions::TrimStart(Span<char16_t> &span)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | [Span](../../system/span/)\<char16_t\>\& | Span karakter yang dapat diubah dan akan dipangkas |

### Nilai Kembali

Span baru dengan spasi dipangkas dari awal

## System::MemoryExtensions::TrimStart(const ReadOnlySpan\<char16_t\>\&, char16_t) function

Memangkas karakter yang ditentukan dari awal span karakter.

```cpp
ReadOnlySpan<char16_t> System::MemoryExtensions::TrimStart(const ReadOnlySpan<char16_t> &span, char16_t trimchar)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<char16_t\>\& | Span karakter yang akan dipangkas |
| trimchar | char16_t | Karakter yang akan dipangkas |

### Nilai Kembali

Span baru dengan karakter yang ditentukan dipangkas dari awal

## System::MemoryExtensions::TrimStart(Span\<char16_t\>\&, char16_t) function

Memangkas karakter yang ditentukan dari awal span karakter yang dapat diubah.

```cpp
Span<char16_t> System::MemoryExtensions::TrimStart(Span<char16_t> &span, char16_t trimchar)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | [Span](../../system/span/)\<char16_t\>\& | Span karakter yang dapat diubah dan akan dipangkas |
| trimchar | char16_t | Karakter yang akan dipangkas |

### Nilai Kembali

Span baru dengan karakter yang ditentukan dipangkas dari awal

## System::MemoryExtensions::TrimStart(const ReadOnlySpan\<char16_t\>\&, const ReadOnlySpan\<char16_t\>\&) function

Memangkas karakter yang ditentukan dari awal span karakter.

```cpp
ReadOnlySpan<char16_t> System::MemoryExtensions::TrimStart(const ReadOnlySpan<char16_t> &span, const ReadOnlySpan<char16_t> &trimchars)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<char16_t\>\& | Span karakter yang akan dipangkas |
| trimchars | const [ReadOnlySpan](../../system/readonlyspan/)\<char16_t\>\& | Karakter-karakter yang akan dipangkas |

### Nilai Kembali

Span baru dengan karakter yang ditentukan dipangkas dari awal

## System::MemoryExtensions::TrimStart(Span\<char16_t\>\&, const ReadOnlySpan\<char16_t\>\&) function

Memangkas karakter yang ditentukan dari awal span karakter yang dapat diubah.

```cpp
Span<char16_t> System::MemoryExtensions::TrimStart(Span<char16_t> &span, const ReadOnlySpan<char16_t> &trimchars)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| span | [Span](../../system/span/)\<char16_t\>\& | Span karakter yang dapat diubah dan akan dipangkas |
| trimchars | const [ReadOnlySpan](../../system/readonlyspan/)\<char16_t\>\& | Karakter-karakter yang akan dipangkas |

### Nilai Kembali

Span baru dengan karakter yang ditentukan dipangkas dari awal

## Lihat Juga

* Kelas [ReadOnlySpan](../../system/readonlyspan/)
* Kelas [Span](../../system/span/)
* Ruang Nama [System::MemoryExtensions](../)
* Pustaka [Aspose.Slides](../../)