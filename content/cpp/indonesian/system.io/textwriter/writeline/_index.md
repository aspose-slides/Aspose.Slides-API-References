---
title: WriteLine()
second_title: Referensi API Aspose.Slides untuk C++
description: Menulis karakter pemisah baris ke aliran.
type: docs
weight: 118
url: /id/system.io/textwriter/writeline/
---
## TextWriter::WriteLine() metode

Menulis karakter pemisah baris ke aliran.

```cpp
virtual void System::IO::TextWriter::WriteLine()
```

## TextWriter::WriteLine(const SharedPtr\<Object\>\&) metode

Menulis representasi string dari objek yang ditentukan diikuti oleh karakter pemisah baris ke aliran.

```cpp
virtual void System::IO::TextWriter::WriteLine(const SharedPtr<Object> &value)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | const [SharedPtr](../../../system/sharedptr/)\<[Object](../../../system/object/)\>\& | Objek yang akan ditulis |

## TextWriter::WriteLine(bool) metode

Menulis representasi string dari nilai boolean yang ditentukan diikuti oleh karakter pemisah baris ke aliran.

```cpp
virtual void System::IO::TextWriter::WriteLine(bool value)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **bool** | Nilai yang akan ditulis |

## TextWriter::WriteLine(char_t) metode

Menulis karakter yang ditentukan diikuti oleh karakter pemisah baris ke aliran.

```cpp
virtual void System::IO::TextWriter::WriteLine(char_t value)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | char_t | Nilai yang akan ditulis |

## TextWriter::WriteLine(Decimal) metode

Menulis representasi string dari objek [Decimal](../../../system/decimal/) yang ditentukan diikuti oleh karakter pemisah baris ke aliran.

```cpp
virtual void System::IO::TextWriter::WriteLine(Decimal value)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | [Decimal](../../../system/decimal/) | Objek yang akan ditulis |

## TextWriter::WriteLine(double) metode

Menulis representasi string dari nilai titik mengambang double-precision yang ditentukan diikuti oleh karakter pemisah baris ke aliran.

```cpp
virtual void System::IO::TextWriter::WriteLine(double value)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **double** | Nilai yang akan ditulis |

## TextWriter::WriteLine(int) metode

Menulis representasi string dari nilai integer 32-bit yang ditentukan diikuti oleh karakter pemisah baris ke aliran.

```cpp
virtual void System::IO::TextWriter::WriteLine(int value)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | int | Nilai yang akan ditulis |

## TextWriter::WriteLine(int64_t) metode

Menulis representasi string dari nilai integer 64-bit yang ditentukan diikuti oleh karakter pemisah baris ke aliran.

```cpp
virtual void System::IO::TextWriter::WriteLine(int64_t value)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **int64_t** | Nilai yang akan ditulis |

## TextWriter::WriteLine(float) metode

Menulis representasi string dari nilai titik mengambang single-precision yang ditentukan diikuti oleh karakter pemisah baris ke aliran.

```cpp
virtual void System::IO::TextWriter::WriteLine(float value)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **float** | Nilai yang akan ditulis |

## TextWriter::WriteLine(const String\&) metode

Menulis string yang ditentukan diikuti oleh karakter pemisah baris ke aliran.

```cpp
virtual void System::IO::TextWriter::WriteLine(const String &value)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | const [String](../../../system/string/)\& | String yang akan ditulis |

## TextWriter::WriteLine(uint32_t) metode

Menulis representasi string dari nilai integer tak bertanda 32-bit yang ditentukan diikuti oleh karakter pemisah baris ke aliran.

```cpp
virtual void System::IO::TextWriter::WriteLine(uint32_t value)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **uint32_t** | Nilai yang akan ditulis |

## TextWriter::WriteLine(uint64_t) metode

Menulis representasi string dari nilai integer tak bertanda 64-bit yang ditentukan diikuti oleh karakter pemisah baris ke aliran.

```cpp
virtual void System::IO::TextWriter::WriteLine(uint64_t value)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | **uint64_t** | Nilai yang akan ditulis |

## TextWriter::WriteLine(const ArrayPtr\<char_t\>\&) metode

Menulis semua karakter dari array yang ditentukan diikuti oleh karakter pemisah baris ke aliran.

```cpp
virtual void System::IO::TextWriter::WriteLine(const ArrayPtr<char_t> &buffer)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../../system/arrayptr/)\<char_t\>\& | Array yang berisi karakter yang akan ditulis |

## TextWriter::WriteLine(const ArrayPtr\<char_t\>\&, int32_t, int32_t) metode

Menulis subrentang karakter UTF-16 yang ditentukan dari array karakter yang ditentukan diikuti oleh karakter pemisah baris ke aliran.

```cpp
virtual void System::IO::TextWriter::WriteLine(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../../system/arrayptr/)\<char_t\>\& | Array yang berisi karakter yang akan ditulis |
| index | **int32_t** | Indeks berbasis 0 dari elemen dalam **buffer** tempat subrentang yang akan ditulis dimulai |
| count | **int32_t** | Jumlah karakter dalam subrentang yang akan ditulis; -1 menunjukkan bahwa subrentang berakhir dimana array **buffer** berakhir |

## TextWriter::WriteLine(const char_t *) metode

Menulis c-string yang ditentukan diikuti oleh karakter pemisah baris ke aliran.

```cpp
virtual void System::IO::TextWriter::WriteLine(const char_t *value)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | const char_t * | c-string yang akan ditulis |

## TextWriter::WriteLine(const TypeInfo\&) metode

Menulis representasi string dari objek [TypeInfo](../../../system/typeinfo/) yang ditentukan diikuti oleh karakter pemisah baris ke aliran.

```cpp
virtual void System::IO::TextWriter::WriteLine(const TypeInfo &value)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | const [TypeInfo](../../../system/typeinfo/)\& | Objek yang akan ditulis |

## TextWriter::WriteLine(const String\&, const TArgs\&...) metode

Menulis nilai yang ditentukan diformat sesuai dengan format yang ditentukan diikuti oleh karakter pemisah baris ke aliran.

```cpp
template<class...> void System::IO::TextWriter::WriteLine(const String &format, const TArgs &... args)
```

### Parameter templat

| Parameter | Deskripsi |
| --- | --- |
| TArgs | Daftar tipe nilai yang akan ditulis |

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| format | const [String](../../../system/string/)\& | Format string |
| args | const TArgs\&... | Nilai yang akan ditulis |

## Lihat Juga

* Typedef [SharedPtr](../../../system/sharedptr/)
* Typedef [ArrayPtr](../../../system/arrayptr/)
* Class [TextWriter](../)
* Class [Object](../../../system/object/)
* Class [Decimal](../../../system/decimal/)
* Class [String](../../../system/string/)
* Class [TypeInfo](../../../system/typeinfo/)
* Namespace [System::IO](../../)
* Library [Aspose.Slides](../../../)