---
title: Copy()
second_title: Referensi API Aspose.Slides untuk C++
description: Menerapkan public static void Copy(IntPtr source, byte[] destination, int startIndex, int length) semantik.
type: docs
weight: 1
url: /id/system.runtime.interopservices/marshal/copy/
---
## Marshal::Copy(const IntPtr, container\&&, int, int) metode


Menerapkan public static void Copy(IntPtr source, byte[] destination, int startIndex, int length) semantik.

```cpp
template<typename container> static void System::Runtime::InteropServices::Marshal::Copy(const IntPtr source, container &&destination, int startIndex, int length)
```


### Parameter templat

| Parameter | Deskripsi |
| --- | --- |
| container | Tipe kontainer tujuan. |

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| source | const IntPtr | Penunjuk data sumber. |
| destination | container\&& | Kontainer untuk menyalin data ke dalamnya. |
| startIndex | int | Indeks mulai sumber. |
| length | int | Jumlah elemen yang disalin. |

## Marshal::Copy(const void *, container\&&, int, int) metode


Menerapkan public static void Copy(IntPtr source, byte[] destination, int startIndex, int length) semantik.

```cpp
template<typename container> static void System::Runtime::InteropServices::Marshal::Copy(const void *source, container &&destination, int startIndex, int length)
```


### Parameter templat

| Parameter | Deskripsi |
| --- | --- |
| container | Tipe kontainer tujuan. |

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| source | const void * | Penunjuk data sumber. |
| destination | container\&& | Kontainer untuk menyalin data ke dalamnya. |
| startIndex | int | Indeks mulai sumber. |
| length | int | Jumlah elemen yang disalin. |

## Marshal::Copy(const container\&, int, void *, int) metode


Menerapkan public static void Copy(char[] source, int startIndex, IntPtr destination, int length).

```cpp
template<typename container> static void System::Runtime::InteropServices::Marshal::Copy(const container &source, int startIndex, void *destination, int length)
```


### Parameter templat

| Parameter | Deskripsi |
| --- | --- |
| container | Tipe kontainer sumber. |

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| source | const container\& | Penunjuk data sumber. |
| startIndex | int | Indeks mulai sumber. |
| destination | void * | Penunjuk data tujuan. |
| length | int | Jumlah elemen yang disalin. |

## Marshal::Copy(const container\&, int, IntPtr, int) metode


Menerapkan public static void Copy(char[] source, int startIndex, IntPtr destination, int length).

```cpp
template<typename container> static void System::Runtime::InteropServices::Marshal::Copy(const container &source, int startIndex, IntPtr destination, int length)
```


### Parameter templat

| Parameter | Deskripsi |
| --- | --- |
| container | Tipe kontainer sumber. |

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| source | const container\& | Penunjuk data sumber. |
| startIndex | int | Indeks mulai sumber. |
| destination | IntPtr | Penunjuk data tujuan. |
| length | int | Jumlah elemen yang disalin. |

## Lihat Juga

* Kelas [Marshal](../)
* Ruang nama [System::Runtime::InteropServices](../../)
* Pustaka [Aspose.Slides](../../../)