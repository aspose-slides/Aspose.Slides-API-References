---
title: GetCharCount()
second_title: Aspose.Slides untuk Referensi API C++
description: Dapatkan jumlah karakter yang diperlukan untuk mendekode buffer byte.
type: docs
weight: 53
url: /id/system.text/icuencoding/getcharcount/
---
## ICUEncoding::GetCharCount(const uint8_t *, int) metode

Dapatkan jumlah karakter yang diperlukan untuk mendekode buffer byte.

```cpp
int System::Text::ICUEncoding::GetCharCount(const uint8_t *bytes, int count) override
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| bytes | const **uint8_t** * | Byte yang akan didekodekan. |
| count | int | Jumlah byte. |

### Nilai Kembalian

Jumlah karakter.

## ICUEncoding::GetCharCount(ArrayPtr\<uint8_t\>, int, int) metode

Dapatkan jumlah karakter yang diperlukan untuk mendekode buffer byte.

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes, int index, int count)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | Byte yang akan didekodekan. |
| index | int | Awal irisan. |
| count | int | Ukuran irisan. |

### Nilai Kembalian

Jumlah karakter.

## ICUEncoding::GetCharCount(ArrayPtr\<uint8_t\>) metode

Dapatkan jumlah karakter yang diperlukan untuk mendekode buffer byte.

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | Byte yang akan didekodekan. |

### Nilai Kembalian

Jumlah karakter.

## ICUEncoding::GetCharCount(const uint8_t *, int) metode

Dapatkan jumlah karakter yang diperlukan untuk mendekode buffer byte.

```cpp
virtual int System::Text::Encoding::GetCharCount(const uint8_t *bytes, int count)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| bytes | const **uint8_t** * | Byte yang akan didekodekan. |
| count | int | Jumlah byte. |

### Nilai Kembalian

Jumlah karakter.

## Lihat Juga

* Typedef [ArrayPtr](../../../system/arrayptr/)
* Kelas [ICUEncoding](../)
* Ruang Nama [System::Text](../../)
* Library [Aspose.Slides](../../../)