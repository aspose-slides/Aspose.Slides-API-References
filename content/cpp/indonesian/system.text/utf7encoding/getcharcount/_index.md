---
title: GetCharCount()
second_title: Referensi API Aspose.Slides untuk C++
description: Dapatkan jumlah karakter yang diperlukan untuk mendekode buffer byte.
type: docs
weight: 79
url: /id/system.text/utf7encoding/getcharcount/
---
## UTF7Encoding::GetCharCount(ArrayPtr\<uint8_t\>, int, int) metode

Dapatkan jumlah karakter yang diperlukan untuk mendekode buffer byte.

```cpp
int System::Text::UTF7Encoding::GetCharCount(ArrayPtr<uint8_t> bytes, int index, int count) override
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | Bytes to decode. |
| index | int | Slice beginning. |
| count | int | Slice size. |

### Nilai Kembalian

Jumlah karakter.

## UTF7Encoding::GetCharCount(const uint8_t *, int) metode

Dapatkan jumlah karakter yang diperlukan untuk mendekode buffer byte.

```cpp
int System::Text::UTF7Encoding::GetCharCount(const uint8_t *bytes, int count) override
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| bytes | const **uint8_t** * | Bytes to decode. |
| count | int | Bytes count. |

### Nilai Kembalian

Jumlah karakter.

## UTF7Encoding::GetCharCount(ArrayPtr\<uint8_t\>, int, int) metode

Dapatkan jumlah karakter yang diperlukan untuk mendekode buffer byte.

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes, int index, int count)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | Bytes to decode. |
| index | int | Slice beginning. |
| count | int | Slice size. |

### Nilai Kembalian

Jumlah karakter.

## UTF7Encoding::GetCharCount(ArrayPtr\<uint8_t\>) metode

Dapatkan jumlah karakter yang diperlukan untuk mendekode buffer byte.

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | Bytes to decode. |

### Nilai Kembalian

Jumlah karakter.

## UTF7Encoding::GetCharCount(const uint8_t *, int) metode

Dapatkan jumlah karakter yang diperlukan untuk mendekode buffer byte.

```cpp
virtual int System::Text::Encoding::GetCharCount(const uint8_t *bytes, int count)
```

### Argumen

| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| bytes | const **uint8_t** * | Bytes to decode. |
| count | int | Bytes count. |

### Nilai Kembalian

Jumlah karakter.

## Lihat Juga

* Typedef [ArrayPtr](../../../system/arrayptr/)
* Kelas [UTF7Encoding](../)
* Ruang Nama [System::Text](../../)
* Perpustakaan [Aspose.Slides](../../../)