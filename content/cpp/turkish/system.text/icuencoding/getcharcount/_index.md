---
title: GetCharCount()
second_title: Aspose.Slides for C++ API Referansı
description: Bir bayt tamponunu çözümlemek için gereken karakter sayısını alır.
type: docs
weight: 53
url: /tr/system.text/icuencoding/getcharcount/
---
## ICUEncoding::GetCharCount(const uint8_t *, int) metod


Byte tamponunu çözümlemek için gereken karakter sayısını alır.

```cpp
int System::Text::ICUEncoding::GetCharCount(const uint8_t *bytes, int count) override
```


### Parametreler

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| bytes | const **uint8_t** * | Çözülecek baytlar. |
| count | int | Bayt sayısı. |

### Dönüş Değeri

Karakter sayısı.

## ICUEncoding::GetCharCount(ArrayPtr\<uint8_t\>, int, int) metod


Byte tamponunu çözümlemek için gereken karakter sayısını alır.

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes, int index, int count)
```


### Parametreler

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | Çözülecek baytlar. |
| index | int | Dilimin başlangıcı. |
| count | int | Dilim boyutu. |

### Dönüş Değeri

Karakter sayısı.

## ICUEncoding::GetCharCount(ArrayPtr\<uint8_t\>) metod


Byte tamponunu çözümlemek için gereken karakter sayısını alır.

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes)
```


### Parametreler

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | Çözülecek baytlar. |

### Dönüş Değeri

Karakter sayısı.

## ICUEncoding::GetCharCount(const uint8_t *, int) metod


Byte tamponunu çözümlemek için gereken karakter sayısını alır.

```cpp
virtual int System::Text::Encoding::GetCharCount(const uint8_t *bytes, int count)
```


### Parametreler

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| bytes | const **uint8_t** * | Çözülecek baytlar. |
| count | int | Bayt sayısı. |

### Dönüş Değeri

Karakter sayısı.

## İlgili Bilgiler

* Tip tanımı [ArrayPtr](../../../system/arrayptr/)
* Sınıf [ICUEncoding](../)
* Ad alanı [System::Text](../../)
* Kütüphane [Aspose.Slides](../../../)