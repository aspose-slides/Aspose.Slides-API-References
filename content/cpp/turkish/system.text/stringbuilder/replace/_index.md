---
title: Replace()
second_title: Aspose.Slides for C++ API Referansı
description: Yapıcı aracılığıyla alt dizeyi değiştirir.
type: docs
weight: 196
url: /tr/system.text/stringbuilder/replace/
---
## StringBuilder::Replace(const String&, const String&) metodu

Yapıcı aracılığıyla alt dizeyi değiştirir.

```cpp
StringBuilder * System::Text::StringBuilder::Replace(const String &oldString, const String &newString)
```

### Parametreler

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| oldString | const [String](../../../system/string/)\& | [String](../../../system/string/) değiştirmek için. |
| newString | const [String](../../../system/string/)\& | Yerine konulacak dize. |

### Dönüş Değeri

Bu işaretçi.

## StringBuilder::Replace(const String&, const String&, int, int) metodu

Yapıcının aralığındaki alt dizeyi değiştirir.

```cpp
StringBuilder * System::Text::StringBuilder::Replace(const String &oldString, const String &newString, int position, int count)
```

### Parametreler

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| oldString | const [String](../../../system/string/)\& | [String](../../../system/string/) değiştirmek için. |
| newString | const [String](../../../system/string/)\& | Yerine konulacak dize. |
| position | int | Yapıcının değiştirme aralığının başlangıç konumu. |
| count | int | Yapıcının değiştirme aralığının uzunluğu. |

### Dönüş Değeri

Bu işaretçi.

## StringBuilder::Replace(char_t, char_t) metodu

Yapıcı aracılığıyla karakteri değiştirir.

```cpp
StringBuilder * System::Text::StringBuilder::Replace(char_t oldChar, char_t newChar)
```

### Parametreler

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| oldChar | char_t | Değiştirilecek karakter. |
| newChar | char_t | Yerine konulacak karakter. |

### Dönüş Değeri

Bu işaretçi.

## StringBuilder::Replace(char_t, char_t, int, int) metodu

Yapıcının aralığındaki karakteri değiştirir.

```cpp
StringBuilder * System::Text::StringBuilder::Replace(char_t oldChar, char_t newChar, int startIndex, int count)
```

### Parametreler

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| oldChar | char_t | Değiştirilecek karakter. |
| newChar | char_t | Yerine konulacak karakter. |
| startIndex | int | Yapıcının değiştirme aralığının başlangıç konumu. |
| count | int | Yapıcının değiştirme aralığının uzunluğu. |

### Dönüş Değeri

Bu işaretçi.

## Ayrıca Bakınız

* Sınıf [StringBuilder](../)
* Sınıf [String](../../../system/string/)
* Ad alanı [System::Text](../../)
* Kütüphane [Aspose.Slides](../../../)