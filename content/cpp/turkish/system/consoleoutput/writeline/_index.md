---
title: WriteLine()
second_title: Aspose.Slides için C++ API Referansı
description: Geçerli nesne tarafından temsil edilen çıktı akışına geçerli satır sonlandırıcısını yazar.
type: docs
weight: 27
url: /tr/system/consoleoutput/writeline/
---
## ConsoleOutput::WriteLine() yöntemi

Geçerli nesne tarafından temsil edilen çıktı akışına geçerli satır sonlandırıcısını yazar.

```cpp
void System::ConsoleOutput::WriteLine() override
```

## ConsoleOutput::WriteLine(const SharedPtr\<Object\>\&) yöntemi

Belirtilen nesnenin dize temsilini, ardından geçerli satır sonlandırıcısını, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::WriteLine(const SharedPtr<Object> &value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Çıktıya verilecek nesne |

## ConsoleOutput::WriteLine(bool) yöntemi

Belirtilen bool değerinin dize temsilini, ardından geçerli satır sonlandırıcısını, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::WriteLine(bool value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **bool** | Çıktıya verilecek nesne |

## ConsoleOutput::WriteLine(char_t) yöntemi

Belirtilen karakter değerini, ardından geçerli satır sonlandırıcısını, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::WriteLine(char_t value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | char_t | Çıktıya verilecek değer |

## ConsoleOutput::WriteLine(Decimal) yöntemi

[Decimal](../../decimal/) değerinin dize temsilini, ardından geçerli satır sonlandırıcısını, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::WriteLine(Decimal value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | Çıktıya verilecek değer |

## ConsoleOutput::WriteLine(double) yöntemi

Çift duyarlıklı kayan nokta değerinin dize temsilini, ardından geçerli satır sonlandırıcısını, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::WriteLine(double value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **double** | Çıktıya verilecek değer |

## ConsoleOutput::WriteLine(int) yöntemi

32 bit tam sayı değerinin dize temsilini, ardından geçerli satır sonlandırıcısını, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::WriteLine(int value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | int | Çıktıya verilecek değer |

## ConsoleOutput::WriteLine(int64_t) yöntemi

64 bit tam sayı değerinin dize temsilini, ardından geçerli satır sonlandırıcısını, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::WriteLine(int64_t value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **int64_t** | Çıktıya verilecek değer |

## ConsoleOutput::WriteLine(float) yöntemi

Tek duyarlıklı kayan nokta değerinin dize temsilini, ardından geçerli satır sonlandırıcısını, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::WriteLine(float value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **float** | Çıktıya verilecek değer |

## ConsoleOutput::WriteLine(const String\&) yöntemi

Belirtilen dize nesnesini, ardından geçerli satır sonlandırıcısını, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::WriteLine(const String &value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | const [String](../../string/)\& | Çıktıya verilecek dize nesnesi |

## ConsoleOutput::WriteLine(uint32_t) yöntemi

İmzalı olmayan 32 bit tam sayı değerinin dize temsilini, ardından geçerli satır sonlandırıcısını, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::WriteLine(uint32_t value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **uint32_t** | Çıktıya verilecek değer |

## ConsoleOutput::WriteLine(uint64_t) yöntemi

İmzalı olmayan 64 bit tam sayı değerinin dize temsilini, ardından geçerli satır sonlandırıcısını, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::WriteLine(uint64_t value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **uint64_t** | Çıktıya verilecek değer |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&) yöntemi

Belirtilen karakter dizisinin dize temsilini, ardından geçerli satır sonlandırıcısını, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Çıktıya verilecek dizi |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&, int32_t, int32_t) yöntemi

Belirtilen karakter dizisinin belirli bir değer aralığının dize temsilini, ardından geçerli satır sonlandırıcısını, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Çıktıya verilecek değerleri içeren dizi |
| index | **int32_t** | Çıktıya verilecek öğe aralığının başladığı indeks |
| count | **int32_t** | Çıktıya verilecek öğe aralığındaki öğe sayısı |

## ConsoleOutput::WriteLine(const char_t *) yöntemi

Belirtilen c-dizisini, ardından geçerli satır sonlandırıcısını, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::WriteLine(const char_t *value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | const char_t * | Çıktıya verilecek c-dizisi |

## ConsoleOutput::WriteLine(const TypeInfo\&) yöntemi

Belirtilen [TypeInfo](../../typeinfo/) nesnesinin dize temsilini, ardından geçerli satır sonlandırıcısını, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::WriteLine(const TypeInfo &value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | Çıktıya verilecek [TypeInfo](../../typeinfo/) nesnesi |

## ConsoleOutput::WriteLine(const char *) yöntemi




```cpp
void System::ConsoleOutput::WriteLine(const char *)=delete
```

## İlgili Bağlantılar

* Tip Tanımı [SharedPtr](../../sharedptr/)
* Tip Tanımı [ArrayPtr](../../arrayptr/)
* Sınıf [ConsoleOutput](../)
* Sınıf [Object](../../object/)
* Sınıf [Decimal](../../decimal/)
* Sınıf [String](../../string/)
* Sınıf [TypeInfo](../../typeinfo/)
* Ad Alanı [System](../../)
* Kütüphane [Aspose.Slides](../../../)