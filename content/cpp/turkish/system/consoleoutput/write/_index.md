---
title: Write()
second_title: Aspose.Slides for C++ API Referansı
description: Belirtilen bool değerinin dize temsilini, geçerli nesne tarafından temsil edilen çıktı akışına yazar.
type: docs
weight: 14
url: /tr/system/consoleoutput/write/
---
## ConsoleOutput::Write(bool) metodu

Belirtilen bool değerinin dize temsilini, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::Write(bool value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **bool** | Çıktılanacak değer |

## ConsoleOutput::Write(const SharedPtr\<Object\>\&) metodu

Belirtilen nesnenin dize temsilini, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::Write(const SharedPtr<Object> &value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Çıktılanacak nesne |

## ConsoleOutput::Write(char_t) metodu

Belirtilen karakter değerini, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::Write(char_t value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | char_t | Çıktılanacak değer |

## ConsoleOutput::Write(Decimal) metodu

Belirtilen [Decimal](../../decimal/) değerinin dize temsilini, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::Write(Decimal value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | Çıktılanacak değer |

## ConsoleOutput::Write(double) metodu

Çift duyarlıklı kayan nokta değerinin dize temsilini, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::Write(double value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **double** | Çıktılanacak değer |

## ConsoleOutput::Write(int32_t) metodu

32-bit tam sayı değerinin dize temsilini, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::Write(int32_t value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **int32_t** | Çıktılanacak değer |

## ConsoleOutput::Write(int64_t) metodu

64-bit tam sayı değerinin dize temsilini, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::Write(int64_t value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **int64_t** | Çıktılanacak değer |

## ConsoleOutput::Write(float) metodu

Tek duyarlıklı kayan nokta değerinin dize temsilini, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::Write(float value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **float** | Çıktılanacak değer |

## ConsoleOutput::Write(const String\&) metodu

Belirtilen dize nesnesini, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::Write(const String &value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | const [String](../../string/)\& | Çıktılanacak dize nesnesi |

## ConsoleOutput::Write(uint32_t) metodu

İşaretsiz 32-bit tam sayı değerinin dize temsilini, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::Write(uint32_t value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **uint32_t** | Çıktılanacak değer |

## ConsoleOutput::Write(uint64_t) metodu

İşaretsiz 64-bit tam sayı değerinin dize temsilini, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::Write(uint64_t value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **uint64_t** | Çıktılanacak değer |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&) metodu

Belirtilen karakter dizisinin dize temsilini, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Çıktılanacak dizi |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&, int32_t, int32_t) metodu

Belirtilen karakter dizisinin belirli bir değer aralığının dize temsilini, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Çıktılanacak değerleri içeren dizi |
| index | **int32_t** | Çıktılanacak eleman aralığının başladığı indeks |
| count | **int32_t** | Çıktılanacak eleman aralığındaki eleman sayısı |

## ConsoleOutput::Write(const char_t *) metodu

Belirtilen c-dizgesini, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::Write(const char_t *value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | const char_t * | Çıktılanacak c-dizgesi |

## ConsoleOutput::Write(const TypeInfo\&) metodu

Belirtilen [TypeInfo](../../typeinfo/) nesnesinin dize temsilini, geçerli nesne tarafından temsil edilen çıktı akışına yazar.

```cpp
void System::ConsoleOutput::Write(const TypeInfo &value) override
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | Çıktılanacak [TypeInfo](../../typeinfo/) nesnesi |

## ConsoleOutput::Write(const char *) metodu




```cpp
void System::ConsoleOutput::Write(const char *)=delete
```

## İlgili

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Class [ConsoleOutput](../)
* Class [Object](../../object/)
* Class [Decimal](../../decimal/)
* Class [String](../../string/)
* Class [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)