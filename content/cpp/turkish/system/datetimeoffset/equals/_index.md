---
title: Equals()
second_title: Aspose.Slides for C++ API Referansı
description: İki DateTimeOffset nesnesinin aynı zaman noktasını temsil edip etmediğini denetler.
type: docs
weight: 365
url: /tr/system/datetimeoffset/equals/
---
## DateTimeOffset::Equals(const DateTimeOffset\&) const yöntemi


İki [DateTimeOffset](../) nesnesinin aynı zaman noktasını temsil edip etmediğini kontrol eder.

```cpp
bool System::DateTimeOffset::Equals(const DateTimeOffset &other) const
```


### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/) karşılaştırılacak. |

### Dönüş Değeri

geçerli nesne **other** ile eşitse true, aksi takdirde false.

## DateTimeOffset::Equals(const SharedPtr\<Object\>\&) const yöntemi


İki [DateTimeOffset](../) nesnesinin aynı zaman noktasını temsil edip etmediğini kontrol eder.

```cpp
bool System::DateTimeOffset::Equals(const SharedPtr<Object> &obj) const
```


### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/) karşılaştırılacak. |

### Dönüş Değeri

geçerli nesne **obj** ile eşitse true, aksi takdirde false.

## DateTimeOffset::Equals(const DateTimeOffset\&, const DateTimeOffset\&) yöntemi


İki [DateTimeOffset](../) nesnesinin aynı zaman noktasını temsil edip etmediğini kontrol eder.

```cpp
static bool System::DateTimeOffset::Equals(const DateTimeOffset &first, const DateTimeOffset &second)
```


### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| first | const [DateTimeOffset](../)\& | Karşılaştırılacak ilk nesne. |
| second | const [DateTimeOffset](../)\& | Karşılaştırılacak ikinci nesne. |

### Dönüş Değeri

**first** **second** ile eşitse true, aksi takdirde false.

## Bakınız

* Typedef [SharedPtr](../../sharedptr/)
* Class [DateTimeOffset](../)
* Class [Object](../../object/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)