---
title: ToString()
second_title: Aspose.Slides for C++ API Referansı
description: Belirtilen biçim ve biçim sağlayıcıyı kullanarak geçerli nesneyi dizeye dönüştürür.
type: docs
weight: 443
url: /tr/system/datetimeoffset/tostring/
---
## DateTimeOffset::ToString(const String\&, const SharedPtr\<IFormatProvider\>\&) const metodu

Belirtilen biçim ve biçim sağlayıcıyı kullanarak geçerli nesneyi dizeye dönüştürür.

```cpp
String System::DateTimeOffset::ToString(const String &format, const SharedPtr<IFormatProvider> &provider) const
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| format | const [String](../../string/)\& | Biçim dizesi. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Biçim sağlayıcı. |

### Dönüş Değeri

[String](../../string/) geçerli [DateTimeOffset](../) nesnesinin temsili.

## DateTimeOffset::ToString(const SharedPtr\<IFormatProvider\>\&) const metodu

Belirtilen biçim sağlayıcıyı kullanarak geçerli nesneyi dizeye dönüştürür.

```cpp
String System::DateTimeOffset::ToString(const SharedPtr<IFormatProvider> &provider) const
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Biçim sağlayıcı. |

### Dönüş Değeri

[String](../../string/) geçerli [DateTimeOffset](../) nesnesinin temsili.

## DateTimeOffset::ToString(const String\&) const metodu

Belirtilen biçimi kullanarak geçerli nesneyi dizeye dönüştürür.

```cpp
String System::DateTimeOffset::ToString(const String &format) const
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| format | const [String](../../string/)\& | Biçim dizesi. |

### Dönüş Değeri

[String](../../string/) geçerli [DateTimeOffset](../) nesnesinin temsili.

## DateTimeOffset::ToString() const metodu

Geçerli nesneyi dizeye dönüştürür.

```cpp
String System::DateTimeOffset::ToString() const
```

### Dönüş Değeri

[String](../../string/) geçerli [DateTimeOffset](../) nesnesinin temsili.

## Bakınız

* Tip Tanımı [SharedPtr](../../sharedptr/)
* Sınıf [String](../../string/)
* Sınıf [IFormatProvider](../../iformatprovider/)
* Sınıf [DateTimeOffset](../)
* Ad Alanı [System](../../)
* Kütüphane [Aspose.Slides](../../../)