---
title: EqualsExact()
second_title: Aspose.Slides için C++ API Referansı
description: İki DateTimeOffset nesnesinin aynı zaman noktasını temsil edip aynı ofsete sahip olup olmadığını kontrol eder.
type: docs
weight: 378
url: /tr/system/datetimeoffset/equalsexact/
---
## DateTimeOffset::EqualsExact(const DateTimeOffset\&) const metodu

[DateTimeOffset](../) nesnelerinin aynı zaman noktasını temsil edip aynı ofsete sahip olup olmadığını kontrol eder.

```cpp
bool System::DateTimeOffset::EqualsExact(const DateTimeOffset &other) const
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | Karşılaştırılacak [Object](../../object/). |

### Dönüş Değeri

true if current object is exact equal to **other**, otherwise - false.

## DateTimeOffset::EqualsExact(const SharedPtr\<Object\>\&) const metodu

[DateTimeOffset](../) nesnelerinin aynı zaman noktasını temsil edip aynı ofsete sahip olup olmadığını kontrol eder.

```cpp
bool System::DateTimeOffset::EqualsExact(const SharedPtr<Object> &obj) const
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Karşılaştırılacak [Object](../../object/). |

### Dönüş Değeri

true if current object is exact equal to **obj**, otherwise - false.

## Ayrıca Bakınız

* Tip Tanımı [SharedPtr](../../sharedptr/)
* Sınıf [DateTimeOffset](../)
* Sınıf [Object](../../object/)
* Ad alanı [System](../../)
* Kütüphane [Aspose.Slides](../../../)