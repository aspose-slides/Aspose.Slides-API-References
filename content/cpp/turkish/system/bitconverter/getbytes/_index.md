---
title: GetBytes()
second_title: Aspose.Slides for C++ API Referansı
description: Belirtilen bool değerini bayt dizisine dönüştürür.
type: docs
weight: 14
url: /tr/system/bitconverter/getbytes/
---
## BitConverter::GetBytes(bool) metot


Belirtilen bool değerini bayt dizisine dönüştürür.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(bool value)
```


### Parametreler

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **bool** | Dönüştürülecek bir bool değeri |

### Dönüş Değeri

1 baytlık dizi, belirtilen değeri temsil eder

## BitConverter::GetBytes(char_t) metot


Belirtilen char_t değerini bayt dizisine dönüştürür.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(char_t value)
```


### Parametreler

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | char_t | Dönüştürülecek bir char_t değeri |

### Dönüş Değeri

2 baytlık dizi, belirtilen değeri temsil eder

## BitConverter::GetBytes(int16_t) metot


Belirtilen 16-bit tamsayı değerini bayt dizisine dönüştürür.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int16_t value)
```


### Parametreler

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **int16_t** | Dönüştürülecek bir 16-bit tamsayı değeri |

### Dönüş Değeri

2 baytlık dizi, belirtilen değeri temsil eder

## BitConverter::GetBytes(int) metot


Belirtilen 32-bit tamsayı değerini bayt dizisine dönüştürür.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int value)
```


### Parametreler

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | int | Dönüştürülecek bir 32-bit tamsayı değeri |

### Dönüş Değeri

4 baytlık dizi, belirtilen değeri temsil eder

## BitConverter::GetBytes(int64_t) metot


Belirtilen 64-bit tamsayı değerini bayt dizisine dönüştürür.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int64_t value)
```


### Parametreler

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **int64_t** | Dönüştürülecek bir 64-bit tamsayı değeri |

### Dönüş Değeri

8 baytlık dizi, belirtilen değeri temsil eder

## BitConverter::GetBytes(uint16_t) metot


Belirtilen işaretsiz 16-bit tamsayı değerini bayt dizisine dönüştürür.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint16_t value)
```


### Parametreler

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **uint16_t** | Dönüştürülecek bir işaretsiz 16-bit tamsayı değeri |

### Dönüş Değeri

2 baytlık dizi, belirtilen değeri temsil eder

## BitConverter::GetBytes(uint32_t) metot


Belirtilen işaretsiz 32-bit tamsayı değerini bayt dizisine dönüştürür.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint32_t value)
```


### Parametreler

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **uint32_t** | Dönüştürülecek bir işaretsiz 32-bit tamsayı değeri |

### Dönüş Değeri

4 baytlık dizi, belirtilen değeri temsil eder

## BitConverter::GetBytes(uint64_t) metot


Belirtilen işaretsiz 64-bit tamsayı değerini bayt dizisine dönüştürür.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint64_t value)
```


### Parametreler

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **uint64_t** | Dönüştürülecek bir işaretsiz 64-bit tamsayı değeri |

### Dönüş Değeri

8 baytlık dizi, belirtilen değeri temsil eder

## BitConverter::GetBytes(float) metot


Belirtilen tek duyarlıklı kayan nokta değerini bayt dizisine dönüştürür.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(float value)
```


### Parametreler

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **float** | Dönüştürülecek bir kayan nokta değeri |

### Dönüş Değeri

4 baytlık dizi, belirtilen değeri temsil eder

## BitConverter::GetBytes(double) metot


Belirtilen çift duyarlıklı kayan nokta değerini bayt dizisine dönüştürür.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(double value)
```


### Parametreler

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **double** | Dönüştürülecek bir kayan nokta değeri |

### Dönüş Değeri

8 baytlık dizi, belirtilen değeri temsil eder

## Ayrıca Bakınız

* Tip Tanımı [ArrayPtr](../../arrayptr/)
* Sınıf [BitConverter](../)
* Ad alanı [System](../../)
* Kütüphane [Aspose.Slides](../../../)