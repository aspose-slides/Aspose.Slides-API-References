---
title: TypeInfo()
second_title: Aspose.Slides for C++ API Referansı
description: Varsayılan yapıcı (hiçbir tür ayarlanmadı).
type: docs
weight: 40
url: /tr/system/typeinfo/typeinfo/
---
## TypeInfo::TypeInfo() yapıcı

Varsayılan yapıcı (hiçbir tür ayarlanmadı).

```cpp
System::TypeInfo::TypeInfo()
```

## TypeInfo::TypeInfo(std::nullptr_t) yapıcı

Null nesne yapıcı (hiçbir tür ayarlanmadı).

```cpp
System::TypeInfo::TypeInfo(std::nullptr_t)
```

## TypeInfo::TypeInfo(const char_t *) yapıcı

Yapıcı.

```cpp
System::TypeInfo::TypeInfo(const char_t *name)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| name | const char_t * | Tür adı. |

## TypeInfo::TypeInfo(const char_t *, uint32_t) yapıcı

Yapıcı.

```cpp
System::TypeInfo::TypeInfo(const char_t *name, uint32_t hash)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| name | const char_t * | Tür adı. |
| hash | **uint32_t** | Tür adı karması. |

## TypeInfo::TypeInfo(const std::type_info\&) yapıcı

Yapıcı.

```cpp
System::TypeInfo::TypeInfo(const std::type_info &info)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| info | const std::type_info\& | Bir tür hakkında bilgi. |

## Ayrıca Bakınız

* Sınıf [TypeInfo](../)
* Ad Alanı [System](../../)
* Kütüphane [Aspose.Slides](../../../)