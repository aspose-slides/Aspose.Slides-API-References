---
title: ToUInt16()
second_title: Aspose.Slides for C++ API Referansı
description: Belirtilen boolean değeri eşdeğer 16 bit işaretsiz tamsayıya dönüştürür.
type: docs
weight: 144
url: /tr/system/convert/touint16/
---
## Convert::ToUInt16(bool) metod

Belirtilen boolean değeri eşdeğer 16 bit işaretsiz tamsayıya dönüştürür.

```cpp
static constexpr uint16_t System::Convert::ToUInt16(bool value)
```

## Convert::ToUInt16(uint8_t) metod

Belirtilen 8 bit işaretsiz tamsayıyı eşdeğer 16 bit işaretsiz tamsayıya dönüştürür.

```cpp
static constexpr uint16_t System::Convert::ToUInt16(uint8_t value)
```

## Convert::ToUInt16(int8_t) metod

Belirtilen 8 bit işaretli tamsayıyı eşdeğer 16 bit işaretsiz tamsayıya dönüştürür.

```cpp
static uint16_t System::Convert::ToUInt16(int8_t value)
```

## Convert::ToUInt16(uint16_t) metod

Belirtilen 16 bit işaretsiz tamsayıyı döndürür.

```cpp
static constexpr uint16_t System::Convert::ToUInt16(uint16_t value)
```

## Convert::ToUInt16(int16_t) metod

Belirtilen 16 bit işaretli tamsayıyı eşdeğer 16 bit işaretsiz tamsayıya dönüştürür.

```cpp
static uint16_t System::Convert::ToUInt16(int16_t value)
```

## Convert::ToUInt16(uint32_t) metod

Belirtilen 32 bit işaretsiz tamsayıyı eşdeğer 16 bit işaretsiz tamsayıya dönüştürür.

```cpp
static uint16_t System::Convert::ToUInt16(uint32_t value)
```

## Convert::ToUInt16(int32_t) metod

Belirtilen 32 bit işaretli tamsayıyı eşdeğer 16 bit işaretsiz tamsayıya dönüştürür.

```cpp
static uint16_t System::Convert::ToUInt16(int32_t value)
```

## Convert::ToUInt16(uint64_t) metod

Belirtilen 64 bit işaretsiz tamsayıyı eşdeğer 16 bit işaretsiz tamsayıya dönüştürür.

```cpp
static uint16_t System::Convert::ToUInt16(uint64_t value)
```

## Convert::ToUInt16(int64_t) metod

Belirtilen 64 bit işaretli tamsayıyı eşdeğer 16 bit işaretsiz tamsayıya dönüştürür.

```cpp
static uint16_t System::Convert::ToUInt16(int64_t value)
```

## Convert::ToUInt16(float) metod

Belirtilen float sayıyı eşdeğer 16 bit işaretsiz tamsayıya dönüştürür.

```cpp
static uint16_t System::Convert::ToUInt16(float value)
```

## Convert::ToUInt16(double) metod

Belirtilen double sayıyı eşdeğer 16 bit işaretsiz tamsayıya dönüştürür.

```cpp
static uint16_t System::Convert::ToUInt16(double value)
```

## Convert::ToUInt16(const Decimal\&) metod

Belirtilen ondalık sayıyı eşdeğer 16 bit işaretsiz tamsayıya dönüştürür.

```cpp
static uint16_t System::Convert::ToUInt16(const Decimal &value)
```

## Convert::ToUInt16(char_t) metod

Belirtilen unicode karakteri eşdeğer 16 bit işaretsiz tamsayıya dönüştürür.

```cpp
static constexpr uint16_t System::Convert::ToUInt16(char_t value)
```

## Convert::ToUInt16(DateTime) metod

Dönüştürme desteklenmez. Her zaman InvalidCastException fırlatır.

```cpp
static uint16_t System::Convert::ToUInt16(DateTime value)
```

## Convert::ToUInt16(std::nullptr_t) metod

Belirtilen null dizesini eşdeğer işaretsiz 16 bit tamsayı değerine dönüştürür.

```cpp
static constexpr uint16_t System::Convert::ToUInt16(std::nullptr_t)
```

### Return Value

Sıfır.

## Convert::ToUInt16(const char_t *) metod

Belirtilen sayının string temsiline sahip c-dizesini eşdeğer işaretsiz 16 bit tamsayı değerine dönüştürür.

```cpp
static uint16_t System::Convert::ToUInt16(const char_t *value)
```

### Argümanlar

| Parameter | Type | Description |
| --- | --- | --- |
| value | const char_t * | The c-string to convert |

### Return Value

Belirtilen c-dizesi tarafından temsil edilen sayıya eşit işaretsiz 16 bit tamsayı değeri

## Convert::ToUInt16(const String\&) metod

Belirtilen sayının string temsiline sahip dizeyi eşdeğer işaretsiz 16 bit tamsayı değerine dönüştürür.

```cpp
static uint16_t System::Convert::ToUInt16(const String &value)
```

### Argümanlar

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | The string to convert |

### Return Value

Belirtilen dize tarafından temsil edilen sayıya eşit işaretsiz 16 bit tamsayı değeri

## Convert::ToUInt16(const String\&, int) metod

Belirtilen tabanda sayının string temsiline sahip dizeyi eşdeğer işaretsiz 16 bit tamsayı değerine dönüştürür.

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, int from_base)
```

### Argümanlar

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | The string to convert |
| from_base | int | Dizenin temsil ettiği sayının tabanı |

### Return Value

Belirtilen dize tarafından temsil edilen sayıya eşit işaretsiz 16 bit tamsayı değeri

## Convert::ToUInt16(const String\&, const SharedPtr\<IFormatProvider\>\&) metod

Belirtilen sayının string temsiline sahip dizeyi sağlanan biçimlendirme bilgilerini kullanarak eşdeğer işaretsiz 16 bit tamsayı değerine dönüştürür.

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, const SharedPtr<IFormatProvider> &provider)
```

### Argümanlar

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | The string to convert |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | String biçim bilgilerini içeren bir nesneye işaretçi |

### Return Value

Belirtilen dize tarafından temsil edilen sayıya eşit işaretsiz 16 bit tamsayı değeri

## Convert::ToUInt16(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) metod




```cpp
static uint16_t System::Convert::ToUInt16(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToUInt16(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) metod




```cpp
static uint16_t System::Convert::ToUInt16(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToUInt16(const String\&, std::nullptr_t) metod




```cpp
static uint16_t System::Convert::ToUInt16(const String &value, std::nullptr_t)
```

## Convert::ToUInt16(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) metod

Belirtilen sayının string temsiline sahip dizeyi sağlanan biçimlendirme bilgileri ve sayı stili kullanarak eşdeğer işaretsiz 16 bit tamsayı değerine dönüştürür.

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```

### Argümanlar

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | The string to convert |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | Bir sayının string temsili için izin verilen stili belirten NumberStyles enum değerlerinin bitwise kombinasyonu |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | String biçim bilgilerini içeren bir nesneye işaretçi |

### Return Value

Belirtilen dize tarafından temsil edilen sayıya eşit işaretsiz 16 bit tamsayı değeri

## Convert::ToUInt16(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) metod




```cpp
static uint16_t System::Convert::ToUInt16(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToUInt16(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) metod




```cpp
static uint16_t System::Convert::ToUInt16(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToUInt16(const String\&, Globalization::NumberStyles, std::nullptr_t) metod 




```cpp
static uint16_t System::Convert::ToUInt16(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToUInt16(Enum) metod 




```cpp
template<typename Enum,typename> static uint16_t System::Convert::ToUInt16(Enum value)
```

## Convert::ToUInt16(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) metod

Belirtilen kutulanmış değeri eşdeğer işaretsiz 16 bit tamsayı değerine dönüştürür.

```cpp
static uint16_t System::Convert::ToUInt16(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```

### Argümanlar

| Parameter | Type | Description |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Değeri dönüştürmek için kutulayan nesneye yönelen paylaşımlı işaretçi |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Kutu içindeki değer tipi [String](../../string/) olduğunda kullanılacak string biçimi |

### Return Value

Belirtilen kutulanmış değere eşdeğer işaretsiz 16 bit tamsayı değeri

## İlgili

* Enum [NumberStyles](../../../system.globalization/numberstyles/)
* Typedef [SharedPtr](../../sharedptr/)
* Class [Decimal](../../decimal/)
* Class [DateTime](../../datetime/)
* Class [String](../../string/)
* Class [IFormatProvider](../../iformatprovider/)
* Class [CultureInfo](../../../system.globalization/cultureinfo/)
* Class [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* Class [Object](../../object/)
* Struct [Convert](../)
* Struct [Enum](../../enum/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)