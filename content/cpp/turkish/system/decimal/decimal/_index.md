---
title: Decimal()
second_title: Aspose.Slides for C++ API Referansı
description: 0'ı temsil eden bir örnek oluşturur.
type: docs
weight: 1
url: /tr/system/decimal/decimal/
---
## Decimal::Decimal() yapıcı

0'ı temsil eden bir örnek oluşturur.

```cpp
System::Decimal::Decimal()
```

## Decimal::Decimal(std::int8_t) yapıcı

Belirtilen değeri temsil eden bir örnek oluşturur.

```cpp
System::Decimal::Decimal(std::int8_t i)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| i | std::int8_t | [Decimal](../) nesnesi tarafından temsil edilecek 8 bitlik tamsayı değeri |

## Decimal::Decimal(std::int16_t) yapıcı

Belirtilen değeri temsil eden bir örnek oluşturur.

```cpp
System::Decimal::Decimal(std::int16_t i)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| i | std::int16_t | [Decimal](../) nesnesi tarafından temsil edilecek 16 bitlik tamsayı değeri |

## Decimal::Decimal(std::int32_t) yapıcı

Belirtilen değeri temsil eden bir örnek oluşturur.

```cpp
System::Decimal::Decimal(std::int32_t i)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| i | std::int32_t | [Decimal](../) nesnesi tarafından temsil edilecek 32 bitlik tamsayı değeri |

## Decimal::Decimal(std::int64_t) yapıcı

Belirtilen değeri temsil eden bir örnek oluşturur.

```cpp
System::Decimal::Decimal(std::int64_t i)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| i | std::int64_t | [Decimal](../) nesnesi tarafından temsil edilecek 64 bitlik tamsayı değeri |

## Decimal::Decimal(std::uint8_t) yapıcı

Belirtilen değeri temsil eden bir örnek oluşturur.

```cpp
System::Decimal::Decimal(std::uint8_t i)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| i | std::uint8_t | [Decimal](../) nesnesi tarafından temsil edilecek işaretsiz 8 bitlik tamsayı değeri |

## Decimal::Decimal(std::uint16_t) yapıcı

Belirtilen değeri temsil eden bir örnek oluşturur.

```cpp
System::Decimal::Decimal(std::uint16_t i)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| i | std::uint16_t | [Decimal](../) nesnesi tarafından temsil edilecek işaretsiz 16 bitlik tamsayı değeri |

## Decimal::Decimal(std::uint32_t) yapıcı

Belirtilen değeri temsil eden bir örnek oluşturur.

```cpp
System::Decimal::Decimal(std::uint32_t i)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| i | std::uint32_t | [Decimal](../) nesnesi tarafından temsil edilecek işaretsiz 32 bitlik tamsayı değeri |

## Decimal::Decimal(std::uint64_t) yapıcı

Belirtilen değeri temsil eden bir örnek oluşturur.

```cpp
System::Decimal::Decimal(std::uint64_t i)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| i | std::uint64_t | [Decimal](../) nesnesi tarafından temsil edilecek işaretsiz 64 bitlik tamsayı değeri |

## Decimal::Decimal(float) yapıcı

Belirtilen değeri temsil eden bir örnek oluşturur.

```cpp
System::Decimal::Decimal(float f)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| f | **float** | [Decimal](../) nesnesi tarafından temsil edilecek tek duyarlıklı kayan nokta değeri |

## Decimal::Decimal(double) yapıcı

Belirtilen değeri temsil eden bir örnek oluşturur.

```cpp
System::Decimal::Decimal(double d)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| d | **double** | [Decimal](../) nesnesi tarafından temsil edilecek çift duyarlıklı kayan nokta değeri |

## Decimal::Decimal(const std::string\&) yapıcı

std::string sınıfının bir örneği olarak belirtilen dize temsiline sahip bir değeri temsil eden bir örnek oluşturur.

```cpp
System::Decimal::Decimal(const std::string &str)
```

## Decimal::Decimal(int32_t, int32_t, int32_t, bool, uint8_t) yapıcı

Belirtilen bileşenlerden bir [Decimal](../) nesnesi oluşturur.

```cpp
System::Decimal::Decimal(int32_t lo, int32_t mid, int32_t hi, bool isNegative, uint8_t scale)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| lo | **int32_t** | Değerin düşük 32 biti |
| mid | **int32_t** | Değerin orta 32 biti |
| hi | **int32_t** | Değerin yüksek 32 biti |
| isNegative | **bool** | Değerin negatif olup olmadığını belirler |
| scale | **uint8_t** | 0 ile 28 arasında bir 10’un üssü |

## Decimal::Decimal(const Decimal\&) yapıcı

Belirtilen [Decimal](../) nesnesiyle aynı sayıyı temsil eden bir [Decimal](../) sınıfı örneği oluşturur.

```cpp
System::Decimal::Decimal(const Decimal &d)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| d | const [Decimal](../)\& | Değeri kopyalanacak bir [Decimal](../) nesnesi |

## Decimal::Decimal(const ArrayPtr\<int32_t\>\&) yapıcı

İkili temsili içeren bir tamsayı dizisinden bir [Decimal](../) sınıfı örneği oluşturur.

```cpp
System::Decimal::Decimal(const ArrayPtr<int32_t> &bits)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| bits | const [ArrayPtr](../../arrayptr/)\<**int32_t**\>\& | İkili temsili içeren bir tamsayı dizisi. |

## Decimal::Decimal(std::nullptr_t) yapıcı

Her zaman ArgumentNullException istisnasını fırlatır.

```cpp
System::Decimal::Decimal(std::nullptr_t bits)
```

## Decimal::Decimal(const number_type\&) yapıcı

Belirtilen değeri temsil eden bir [Decimal](../) sınıfı örneği oluşturur.

```cpp
System::Decimal::Decimal(const number_type &value)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | const [number_type](../number_type/)\& | Oluşturulan nesne tarafından temsil edilecek değere sabit referans |

## See Also

* Typedef [ArrayPtr](../../arrayptr/)
* Typedef [number_type](../number_type/)
* Sınıf [Decimal](../)
* Ad alanı [System](../../)
* Kütüphane [Aspose.Slides](../../../)