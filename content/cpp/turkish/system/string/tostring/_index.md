---
title: ToString()
second_title: Aspose.Slides for C++ API Referansı
description: Değer türü nesnelerinde ToString() çağrıldığında String sınıfını işlemek için sarmalayıcı.
type: docs
weight: 365
url: /tr/system/string/tostring/
---
## String::ToString() const metod

[String](../) sınıfını değer türü nesnelerinde [ToString()](./) çağrıldığında işlemek için sarmalayıcı.

```cpp
String System::String::ToString() const
```

### Dönüş Değeri

Geçerli dizenin bir kopyası. Geçerli dize bir null dize ise null dize olarak kabul edilir.

## String::ToString(const SharedPtr\<IFormatProvider\>\&) const metod

[String](../) sınıfını değer türü nesnelerinde [ToString()](./) çağrıldığında işlemek için sarmalayıcı.

```cpp
String System::String::ToString(const SharedPtr<IFormatProvider> &) const
```

### Dönüş Değeri

Geçerli dizenin bir kopyası. Geçerli dize bir null dize ise null dize olarak kabul edilir.

## İlgili

* Typedef [SharedPtr](../../sharedptr/)
* Sınıf [String](../)
* Sınıf [IFormatProvider](../../iformatprovider/)
* Ad alanı [System](../../)
* Kütüphane [Aspose.Slides](../../../)