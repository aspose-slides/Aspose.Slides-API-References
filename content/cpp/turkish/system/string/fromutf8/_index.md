---
title: FromUtf8()
second_title: Aspose.Slides C++ API Referansı
description: utf8 dizesinden String oluşturur.
type: docs
weight: 898
url: /tr/system/string/fromutf8/
---
## String::FromUtf8(const char *) metot

[String](../) oluşturur utf8 dizesinden.

```cpp
static String System::String::FromUtf8(const char *utf8)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| utf8 | const char * | utf8 kod sayfası kullanılarak kodlanmış null sonlu dizeye işaretçi. |

### Dönüş Değeri

[String](../) nesnesi, verilen dizeyi temsil eder.

## String::FromUtf8(const char *, int) metot

[String](../) oluşturur utf8 dizesinden.

```cpp
static String System::String::FromUtf8(const char *utf8, int len)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| utf8 | const char * | utf8 kod sayfası kullanılarak kodlanmış null sonlu dizeye işaretçi. |
| len | int | İşlenecek karakter sayısı. |

### Dönüş Değeri

[String](../) nesnesi, verilen dizeyi temsil eder.

## String::FromUtf8(const uint8_t *) metot

[String](../) oluşturur utf8 dizesinden.

```cpp
static String System::String::FromUtf8(const uint8_t *utf8)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| utf8 | const **uint8_t** * | utf8 kod sayfası kullanılarak kodlanmış null sonlu dizeye işaretçi. |

### Dönüş Değeri

[String](../) nesnesi, verilen dizeyi temsil eder.

## String::FromUtf8(const std::string\&) metot

[String](../) oluşturur utf8 dizesinden.

```cpp
static String System::String::FromUtf8(const std::string &utf8)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| utf8 | const std::string\& | utf8 kod sayfası kullanılarak kodlanmış dizeye işaretçi. |

### Dönüş Değeri

[String](../) nesnesi, verilen dizeyi temsil eder.

## Ayrıca Bakınız

* Sınıf [String](../)
* İsim Uzayı [System](../../)
* Kütüphane [Aspose.Slides](../../../)