---
title: WeakReference()
second_title: Aspose.Slides for C++ API Referansı
description: Varsayılan yapıcı.
type: docs
weight: 1
url: /tr/system/weakreference_tmpl_t__end_tmpl/weakreference/
---
## WeakReference< T >::WeakReference() yöntemi

Varsayılan yapıcı.

```cpp
System::WeakReference<T>::WeakReference()
```

## WeakReference< T >::WeakReference(std::nullptr_t) yöntemi

nullptr'tan yapıcı.

```cpp
System::WeakReference<T>::WeakReference(std::nullptr_t)
```

## WeakReference< T >::WeakReference(const SmartPtr\<T\>\&) yöntemi

WeakReference sınıfının yeni bir örneğini başlatır ve belirtilen nesneyi referans gösterir.

```cpp
System::WeakReference<T>::WeakReference(const SmartPtr<T> &data)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) saklamak için. |

## WeakReference< T >::WeakReference(const SmartPtr\<T\>\&, bool) yöntemi

WeakReference sınıfının yeni bir örneğini başlatır ve belirtilen nesneyi referans gösterir.

```cpp
System::WeakReference<T>::WeakReference(const SmartPtr<T> &data, bool trackResurrection)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) saklamak için. |
| trackResurrection | **bool** | Yok sayıldı. |

## Bakınız

* Sınıf [WeakReference< T >](../)
* Sınıf [SmartPtr](../../smartptr/)
* İsim Alanı [System](../../)
* Kütüphane [Aspose.Slides](../../../)