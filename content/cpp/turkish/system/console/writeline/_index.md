---
title: WriteLine()
second_title: Aspose.Slides for C++ API Referansı
description: Mevcut satır sonlandırıcısını standart çıktı akışına yazar.
type: docs
weight: 14
url: /tr/system/console/writeline/
---
## Console::WriteLine() metodu


Mevcut satır sonlandırıcısını standart çıktı akışına yazar.

```cpp
static void System::Console::WriteLine()
```

## Console::WriteLine(const SharedPtr\<T\>\&) metodu


Belirtilen nesnenin dize temsilini, ardından mevcut satır sonlandırıcısını, standart çıktı akışına yazar.

```cpp
template<class T> static void System::Console::WriteLine(const SharedPtr<T> &object)
```

### Şablon parametreleri

| Parametre | Açıklama |
| --- | --- |
| T | Çıktı verilecek nesnenin türü |

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| object | const [SharedPtr](../../sharedptr/)\<T\>\& | [Object](../../object/) çıktıya |

## Console::WriteLine(bool) metodu


bool değerinin dize temsilini, ardından mevcut satır sonlandırıcısını, standart çıktı akışına yazar.

```cpp
static void System::Console::WriteLine(bool value)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **bool** | Çıktı verilecek değer |

## Console::WriteLine(char_t) metodu


Belirtilen karakter değerini, ardından mevcut satır sonlandırıcısını, standart çıktı akışına yazar.

```cpp
static void System::Console::WriteLine(char_t value)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | char_t | Çıktı verilecek değer |

## Console::WriteLine(const ArrayPtr\<char_t\>\&) metodu


Belirtilen karakter dizisinin dize temsilini, ardından mevcut satır sonlandırıcısını, standart çıktı akışına yazar.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Çıktı verilecek dizi |

## Console::WriteLine(const Decimal\&) metodu


[Decimal](../../decimal/) değerinin dize temsilini, ardından mevcut satır sonlandırıcısını, standart çıktı akışına yazar.

```cpp
static void System::Console::WriteLine(const Decimal &value)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | Çıktı verilecek değer |

## Console::WriteLine(double) metodu


double hassasiyetli kayan nokta değerinin dize temsilini, ardından mevcut satır sonlandırıcısını, standart çıktı akışına yazar.

```cpp
static void System::Console::WriteLine(double value)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **double** | Çıktı verilecek değer |

## Console::WriteLine(float) metodu


single-hassasiyetli kayan nokta değerinin dize temsilini, ardından mevcut satır sonlandırıcısını, standart çıktı akışına yazar.

```cpp
static void System::Console::WriteLine(float value)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **float** | Çıktı verilecek değer |

## Console::WriteLine(int32_t) metodu


32-bit tam sayı değerinin dize temsilini, ardından mevcut satır sonlandırıcısını, standart çıktı akışına yazar.

```cpp
static void System::Console::WriteLine(int32_t value)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **int32_t** | Çıktı verilecek değer |

## Console::WriteLine(int64_t) metodu


64-bit tam sayı değerinin dize temsilini, ardından mevcut satır sonlandırıcısını, standart çıktı akışına yazar.

```cpp
static void System::Console::WriteLine(int64_t value)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **int64_t** | Çıktı verilecek değer |

## Console::WriteLine(const String\&) metodu


Belirtilen string nesnesini, ardından mevcut satır sonlandırıcısını, standart çıktı akışına yazar.

```cpp
static void System::Console::WriteLine(const String &value)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | const [String](../../string/)\& | Çıktı verilecek string nesnesi |

## Console::WriteLine(const char_t *) metodu


Belirtilen c-dizgisini, ardından mevcut satır sonlandırıcısını, standart çıktı akışına yazar.

```cpp
static void System::Console::WriteLine(const char_t *value)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | const char_t * | Çıktı verilecek c-dizi |

## Console::WriteLine(const TypeInfo\&) metodu


[TypeInfo](../../typeinfo/) değerinin dize temsilini, ardından mevcut satır sonlandırıcısını, standart çıktı akışına yazar.

```cpp
static void System::Console::WriteLine(const TypeInfo &value)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | Çıktı verilecek değer |

## Console::WriteLine(uint32_t) metodu


Unsigned 32-bit tam sayı değerinin dize temsilini, ardından mevcut satır sonlandırıcısını, standart çıktı akışına yazar.

```cpp
static void System::Console::WriteLine(uint32_t value)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **uint32_t** | Çıktı verilecek değer |

## Console::WriteLine(uint64_t) metodu


Unsigned 64-bit tam sayı değerinin dize temsilini, ardından mevcut satır sonlandırıcısını, standart çıktı akışına yazar.

```cpp
static void System::Console::WriteLine(uint64_t value)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | **uint64_t** | Çıktı verilecek değer |

## Console::WriteLine(const ArrayPtr\<char_t\>\&, int, int) metodu


Belirtilen karakter dizisinin belirtilen aralığının dize temsilini, ardından mevcut satır sonlandırıcısını, standart çıktı akışına yazar.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer, int index, int count)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Karakter dizisi |
| index | int | Dizide, çıktının başlayacağı indeks |
| count | int | Çıktı aralığındaki eleman sayısı |

## Console::WriteLine(const Exception\&) metodu


Belirtilen Exception nesnesinin dize temsilini, ardından mevcut satır sonlandırıcısını, standart çıktı akışına yazar.

```cpp
static void System::Console::WriteLine(const Exception &e)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| e | const [Exception](../../exception/)\& | Çıktı verilecek değer |

## Console::WriteLine(const String\&, Args\&&...) metodu


Belirtilen argümanların, belirtilen formata göre biçimlendirilmiş dize temsilini, ardından mevcut satır sonlandırıcısını, standart çıktı akışına yazar.

```cpp
template<class...> static void System::Console::WriteLine(const String &format, Args &&... args)
```

### Şablon parametreleri

| Parametre | Açıklama |
| --- | --- |
| The | çıktıya verilecek değerlerin tipleri |

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| format | const [String](../../string/)\& | Dize formatı |
| args | Args\&&... | Çıktı verilecek değerler |

## Console::WriteLine(const char *) metodu




```cpp
static void System::Console::WriteLine(const char *)=delete
```

## İlgili

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Typedef [Exception](../../exception/)
* Sınıf [Console](../)
* Sınıf [Decimal](../../decimal/)
* Sınıf [String](../../string/)
* Sınıf [TypeInfo](../../typeinfo/)
* Ad alanı [System](../../)
* Kütüphane [Aspose.Slides](../../../)