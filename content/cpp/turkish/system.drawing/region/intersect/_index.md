---
title: Intersect()
second_title: Aspose.Slides for C++ API Referansı
description: Geçerli nesne tarafından temsil edilen bölgeyi, bu bölge ile belirtilen dikdörtgenle tanımlanan bir bölgenin kesişim sonucu ile değiştirir.
type: docs
weight: 79
url: /tr/system.drawing/region/intersect/
---
## Region::Intersect(const RectangleF\&) metodu


Geçerli nesne tarafından temsil edilen bölgeyi, bu bölge ile belirtilen dikdörtgenle tanımlanan bir bölgenin kesişim sonucu ile değiştirir.

```cpp
void System::Drawing::Region::Intersect(const RectangleF &rect)
```


### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| rect | const [RectangleF](../../rectanglef/)\& | Bu bölgeyle kesişecek bir bölgeyi tanımlayan bir dikdörtgen |

## Region::Intersect(const Rectangle\&) metodu


Geçerli nesne tarafından temsil edilen bölgeyi, bu bölge ile belirtilen dikdörtgenle tanımlanan bir bölgenin kesişim sonucu ile değiştirir.

```cpp
void System::Drawing::Region::Intersect(const Rectangle &rect)
```


### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| rect | const [Rectangle](../../rectangle/)\& | Bu bölgeyle kesişecek bir bölgeyi tanımlayan bir dikdörtgen |

## Region::Intersect(const SharedPtr\<Drawing2D::GraphicsPath\>\&) metodu


Geçerli nesne tarafından temsil edilen bölgeyi, bu bölge ile belirtilen yolla tanımlanan bir bölgenin kesişim sonucu ile değiştirir.

```cpp
void System::Drawing::Region::Intersect(const SharedPtr<Drawing2D::GraphicsPath> &path)
```


### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| path | const [SharedPtr](../../../system/sharedptr/)\<[Drawing2D::GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)\>\& | Bu bölgeyle kesişecek bir bölgeyi tanımlayan bir yol |

## Region::Intersect(const SharedPtr\<Region\>\&) metodu


Geçerli nesne tarafından temsil edilen bölgeyi, bu bölge ile belirtilen bölgenin kesişim sonucu ile değiştirir.

```cpp
void System::Drawing::Region::Intersect(const SharedPtr<Region> &region)
```


### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| region | const [SharedPtr](../../../system/sharedptr/)\<[Region](../)\>\& | Bu bölgeyle kesişecek bir bölgeyi tanımlayan bir bölge |

## Ayrıca Bakınız

* Typedef [SharedPtr](../../../system/sharedptr/)
* Sınıf [RectangleF](../../rectanglef/)
* Sınıf [Region](../)
* Sınıf [Rectangle](../../rectangle/)
* Sınıf [GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)
* Ad alanı [System::Drawing](../../)
* Kütüphane [Aspose.Slides](../../../)