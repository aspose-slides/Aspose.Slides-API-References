---
title: FromArgb()
second_title: Aspose.Slides for C++ API Referansı
description: Belirtilen rengi temsil eden Color sınıfının bir örneğini oluşturur.
type: docs
weight: 235
url: /tr/system.drawing/color/fromargb/
---
## Color::FromArgb(int) metodu

Belirtilen rengi temsil eden [Color](../) sınıfının bir örneğini oluşturur.

```cpp
static Color System::Drawing::Color::FromArgb(int argb)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| argb | int | Oluşturulan nesne tarafından temsil edilecek rengin 32-bit ARGB değeri |

### Dönüş Değeri

Belirtilen rengi temsil eden bir nesne.

## Color::FromArgb(int, int, int, int) metodu

Belirtilen rengi temsil eden [Color](../) sınıfının bir örneğini oluşturur.

```cpp
static Color System::Drawing::Color::FromArgb(int alpha, int red, int green, int blue)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| alpha | int | Rengin alfa bileşeninin değeri |
| red | int | Rengin kırmızı bileşeninin değeri |
| green | int | Rengin yeşil bileşeninin değeri |
| blue | int | Rengin mavi bileşeninin değeri |

### Dönüş Değeri

Belirtilen rengi temsil eden bir nesne.

## Color::FromArgb(int, int, int) metodu

Alfa bileşeni 0xFF olarak ayarlanmış belirtilen rengi temsil eden [Color](../) sınıfının bir örneğini oluşturur.

```cpp
static Color System::Drawing::Color::FromArgb(int red, int green, int blue)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| red | int | Rengin kırmızı bileşeninin değeri |
| green | int | Rengin yeşil bileşeninin değeri |
| blue | int | Rengin mavi bileşeninin değeri |

### Dönüş Değeri

Belirtilen rengi temsil eden bir nesne.

## Color::FromArgb(int, Color) metodu

Belirtilen rengi temsil eden [Color](../) sınıfının bir örneğini oluşturur.

```cpp
static Color System::Drawing::Color::FromArgb(int alpha, Color base_color)
```

### Argümanlar

| Parametre | Tür | Açıklama |
| --- | --- | --- |
| alpha | int | Rengin alfa bileşeninin değeri |
| base_color | [Color](../) | Oluşturulan nesnenin temsil edeceği rengin kırmızı, yeşil ve mavi bileşenlerini temsil eden [Color](../) nesnesinin bir örneği |

### Dönüş Değeri

Belirtilen rengi temsil eden bir nesne.

## Ayrıca Bakınız

* Sınıf [Color](../)
* Ad alanı [System::Drawing](../../)
* Kütüphane [Aspose.Slides](../../../)