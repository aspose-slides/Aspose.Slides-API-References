---
title: FromArgb()
second_title: Aspose.Slides voor C++ API Referentie
description: Construeert een instantie van de klasse Color die de opgegeven kleur voorstelt.
type: docs
weight: 235
url: /nl/system.drawing/color/fromargb/
---
## Color::FromArgb(int) method

Construeert een instantie van de klasse [Color](../) die de opgegeven kleur voorstelt.

```cpp
static Color System::Drawing::Color::FromArgb(int argb)
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| argb | int | Een 32-bit ARGB-waarde van de kleur die wordt voorgesteld door het te construeren object |

### Retourwaarde

Een object dat de opgegeven kleur voorstelt.

## Color::FromArgb(int, int, int, int) method

Construeert een instantie van de klasse [Color](../) die de opgegeven kleur voorstelt.

```cpp
static Color System::Drawing::Color::FromArgb(int alpha, int red, int green, int blue)
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| alpha | int | Een waarde van de alfacomponent van de kleur |
| red | int | Een waarde van de rode component van de kleur |
| green | int | Een waarde van de groene component van de kleur |
| blue | int | Een waarde van de blauwe component van de kleur |

### Retourwaarde

Een object dat de opgegeven kleur voorstelt.

## Color::FromArgb(int, int, int) method

Construeert een instantie van de klasse [Color](../) die de opgegeven kleur voorstelt met de alfacomponent ingesteld op 0xFF.

```cpp
static Color System::Drawing::Color::FromArgb(int red, int green, int blue)
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| red | int | Een waarde van de rode component van de kleur |
| green | int | Een waarde van de groene component van de kleur |
| blue | int | Een waarde van de blauwe component van de kleur |

### Retourwaarde

Een object dat de opgegeven kleur voorstelt.

## Color::FromArgb(int, Color) method

Construeert een instantie van de klasse [Color](../) die de opgegeven kleur voorstelt.

```cpp
static Color System::Drawing::Color::FromArgb(int alpha, Color base_color)
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| alpha | int | Een waarde van de alfacomponent van de kleur |
| base_color | [Color](../) | Een instantie van een [Color](../) object dat de rode, groene en blauwe componenten van de kleur voorstelt die door het te creëren object wordt weergegeven |

### Retourwaarde

Een object dat de opgegeven kleur voorstelt.

## Zie ook

* Klasse [Color](../)
* Naamruimte [System::Drawing](../../)
* Bibliotheek [Aspose.Slides](../../../)