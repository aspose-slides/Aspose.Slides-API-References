---
title: begin()
second_title: Aspose.Slides voor C++ API-referentie
description: Haalt een iterator op die wijst naar het eerste element (indien aanwezig) van de collectie. Deze iterator kan niet worden gebruikt om een verwezen object te wijzigen omdat GetEnumerator() een kopie-object van T retourneert.
type: docs
weight: 378
url: /nl/system.collections.generic/ienumerable/begin/
---
## IEnumerable::begin() methode


Haalt een iterator op die wijst naar het eerste element (indien aanwezig) van de collectie. Deze iterator kan niet worden gebruikt om een verwezen object te wijzigen omdat [GetEnumerator()](../getenumerator/) een kopie-object van T retourneert.

```cpp
iterator System::Collections::Generic::IEnumerable<T>::begin()
```


### Retourwaarde

Een iterator die wijst naar het eerste element (indien aanwezig) van de collectie

## IEnumerable::begin() const methode


Haalt een iterator op die wijst naar het eerste element (indien aanwezig) van de const-gekwalificeerde instantie van de collectie.

```cpp
const_iterator System::Collections::Generic::IEnumerable<T>::begin() const
```


### Retourwaarde

Een iterator die wijst naar het eerste element (indien aanwezig) van de const-gekwalificeerde instantie van de collectie

## Zie ook

* Typedef [iterator](../iterator/)
* Typedef [const_iterator](../const_iterator/)
* Klasse [IEnumerable](../)
* Naamruimte [System::Collections::Generic](../../)
* Bibliotheek [Aspose.Slides](../../../)