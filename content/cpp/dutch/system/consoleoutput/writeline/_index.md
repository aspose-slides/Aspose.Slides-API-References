---
title: WriteLine()
second_title: Aspose.Slides voor C++ API-referentie
description: Schrijft de huidige regeleinde naar de uitvoerstroom die wordt vertegenwoordigd door het huidige object.
type: docs
weight: 27
url: /nl/system/consoleoutput/writeline/
---
## ConsoleOutput::WriteLine() methode

Schrijft de huidige regeleinde naar de uitvoerstroom die wordt vertegenwoordigd door het huidige object.

```cpp
void System::ConsoleOutput::WriteLine() override
```

## ConsoleOutput::WriteLine(const SharedPtr\<Object\>\&) methode

Schrijft de tekenreeksrepresentatie van het opgegeven object, gevolgd door de huidige regeleinde, naar de uitvoerstroom die wordt vertegenwoordigd door het huidige object.

```cpp
void System::ConsoleOutput::WriteLine(const SharedPtr<Object> &value) override
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Het object om uit te voeren |

## ConsoleOutput::WriteLine(bool) methode

Schrijft de tekenreeksrepresentatie van de opgegeven bool-waarde, gevolgd door de huidige regeleinde, naar de uitvoerstroom die wordt vertegenwoordigd door het huidige object.

```cpp
void System::ConsoleOutput::WriteLine(bool value) override
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | **bool** | De bool-waarde om uit te voeren |

## ConsoleOutput::WriteLine(char_t) methode

Schrijft de opgegeven tekenwaarde, gevolgd door de huidige regeleinde, naar de uitvoerstroom die wordt vertegenwoordigd door het huidige object.

```cpp
void System::ConsoleOutput::WriteLine(char_t value) override
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | char_t | De waarde om uit te voeren |

## ConsoleOutput::WriteLine(Decimal) methode

Schrijft de tekenreeksrepresentatie van [Decimal](../../decimal/)-waarde, gevolgd door de huidige regeleinde, naar de uitvoerstroom die wordt vertegenwoordigd door het huidige object.

```cpp
void System::ConsoleOutput::WriteLine(Decimal value) override
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | De waarde om uit te voeren |

## ConsoleOutput::WriteLine(double) methode

Schrijft de tekenreeksrepresentatie van een double-precisie zwevend-kommagetal, gevolgd door de huidige regeleinde, naar de uitvoerstroom die wordt vertegenwoordigd door het huidige object.

```cpp
void System::ConsoleOutput::WriteLine(double value) override
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | **double** | De waarde om uit te voeren |

## ConsoleOutput::WriteLine(int) methode

Schrijft de tekenreeksrepresentatie van een 32-bit geheel getal, gevolgd door de huidige regeleinde, naar de uitvoerstroom die wordt vertegenwoordigd door het huidige object.

```cpp
void System::ConsoleOutput::WriteLine(int value) override
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | int | De waarde om uit te voeren |

## ConsoleOutput::WriteLine(int64_t) methode

Schrijft de tekenreeksrepresentatie van een 64-bit geheel getal, gevolgd door de huidige regeleinde, naar de uitvoerstroom die wordt vertegenwoordigd door het huidige object.

```cpp
void System::ConsoleOutput::WriteLine(int64_t value) override
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | **int64_t** | De waarde om uit te voeren |

## ConsoleOutput::WriteLine(float) methode

Schrijft de tekenreeksrepresentatie van een single-precisie zwevend-kommagetal, gevolgd door de huidige regeleinde, naar de uitvoerstroom die wordt vertegenwoordigd door het huidige object.

```cpp
void System::ConsoleOutput::WriteLine(float value) override
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | **float** | De waarde om uit te voeren |

## ConsoleOutput::WriteLine(const String\&) methode

Schrijft het opgegeven tekenreeksobject, gevolgd door de huidige regeleinde, naar de uitvoerstroom die wordt vertegenwoordigd door het huidige object.

```cpp
void System::ConsoleOutput::WriteLine(const String &value) override
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | const [String](../../string/)\& | Het tekenreeksobject om uit te voeren |

## ConsoleOutput::WriteLine(uint32_t) methode

Schrijft de tekenreeksrepresentatie van een unsigned 32-bit geheel getal, gevolgd door de huidige regeleinde, naar de uitvoerstroom die wordt vertegenwoordigd door het huidige object.

```cpp
void System::ConsoleOutput::WriteLine(uint32_t value) override
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | **uint32_t** | De waarde om uit te voeren |

## ConsoleOutput::WriteLine(uint64_t) methode

Schrijft de tekenreeksrepresentatie van een unsigned 64-bit geheel getal, gevolgd door de huidige regeleinde, naar de uitvoerstroom die wordt vertegenwoordigd door het huidige object.

```cpp
void System::ConsoleOutput::WriteLine(uint64_t value) override
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | **uint64_t** | De waarde om uit te voeren |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&) methode

Schrijft de tekenreeksrepresentatie van de opgegeven tekenreeksarray, gevolgd door de huidige regeleinde, naar de uitvoerstroom die wordt vertegenwoordigd door het huidige object.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer) override
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | De array om uit te voeren |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&, int32_t, int32_t) methode

Schrijft de tekenreeksrepresentatie van een bereik van waarden van de opgegeven tekenreeksarray, gevolgd door de huidige regeleinde, naar de uitvoerstroom die wordt vertegenwoordigd door het huidige object.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | De array die de waarden bevat die moeten worden uitgegeven |
| index | **int32_t** | De index waarop het bereik van uit te voeren elementen begint |
| count | **int32_t** | Het aantal elementen in het bereik van uit te voeren elementen |

## ConsoleOutput::WriteLine(const char_t *) methode

Schrijft de opgegeven c-string, gevolgd door de huidige regeleinde, naar de uitvoerstroom die wordt vertegenwoordigd door het huidige object.

```cpp
void System::ConsoleOutput::WriteLine(const char_t *value) override
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | const char_t * | De c-string om uit te voeren |

## ConsoleOutput::WriteLine(const TypeInfo\&) methode

Schrijft de tekenreeksrepresentatie van het opgegeven [TypeInfo](../../typeinfo/)-object, gevolgd door de huidige regeleinde, naar de uitvoerstroom die wordt vertegenwoordigd door het huidige object.

```cpp
void System::ConsoleOutput::WriteLine(const TypeInfo &value) override
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | Het [TypeInfo](../../typeinfo/)-object om uit te voeren |

## ConsoleOutput::WriteLine(const char *) methode




```cpp
void System::ConsoleOutput::WriteLine(const char *)=delete
```

## Zie ook

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Klasse [ConsoleOutput](../)
* Klasse [Object](../../object/)
* Klasse [Decimal](../../decimal/)
* Klasse [String](../../string/)
* Klasse [TypeInfo](../../typeinfo/)
* Naamruimte [System](../../)
* Library [Aspose.Slides](../../../)