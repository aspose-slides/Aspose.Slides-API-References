---
title: Equals()
second_title: Aspose.Slides voor C++ API-referentie
description: Bepaalt of de waarden die worden vertegenwoordigd door het huidige object en het opgegeven object gelijk zijn.
type: docs
weight: 313
url: /nl/system/decimal/equals/
---
## Decimal::Equals(const Decimal\&) const method

Bepaalt of de waarden die worden vertegenwoordigd door het huidige object en het opgegeven object gelijk zijn.

```cpp
bool System::Decimal::Equals(const Decimal &d) const
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| d | const [Decimal](../)\& | Het [Decimal](../) object om het huidige object mee te vergelijken |

### Retourwaarde

Waar als de waarde die wordt weergegeven door het huidige en het opgegeven object gelijk is, anders - onwaar

## Decimal::Equals(const SharedPtr\<Object\>\&) const method

Bepaalt of de waarden die worden vertegenwoordigd door het huidige object en het opgegeven object gelijk zijn.

```cpp
bool System::Decimal::Equals(const SharedPtr<Object> &obj) const
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Het object om het huidige object mee te vergelijken |

### Retourwaarde

Waar als de waarde die wordt weergegeven door het huidige en het opgegeven object gelijk is, anders - onwaar

## Decimal::Equals(const Decimal\&, const Decimal\&) method

Bepaalt of de waarden die worden vertegenwoordigd door de opgegeven objecten gelijk zijn.

```cpp
static bool System::Decimal::Equals(const Decimal &d1, const Decimal &d2)
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| d1 | const [Decimal](../)\& | Het eerste te vergelijken element |
| d2 | const [Decimal](../)\& | Het tweede te vergelijken element |

### Retourwaarde

Waar als de waarden die worden weergegeven door **d1** en **d2** gelijk zijn, anders - onwaar

## Zie ook

* Typedef [SharedPtr](../../sharedptr/)
* Class [Decimal](../)
* Class [Object](../../object/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)