---
title: operator-()
second_title: Aspose.Slides voor C++ API-referentie
description: Retourneert een nieuw exemplaar van de Decimal-klasse dat een waarde vertegenwoordigt die het resultaat is van het aftrekken van de waarde die wordt weergegeven door het opgegeven object van de waarde die wordt weergegeven door het huidige object.
type: docs
weight: 92
url: /nl/system/decimal/operator_minus/
---
## Decimal::operator-(const Decimal\&) const methode


Geeft een nieuw exemplaar van de [Decimal](../) klasse terug die een waarde vertegenwoordigt die het resultaat is van het aftrekken van de waarde die wordt weergegeven door het opgegeven object van de waarde die wordt weergegeven door het huidige object.

```cpp
Decimal System::Decimal::operator-(const Decimal &d) const
```


### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| d | const [Decimal](../)\& | Het [Decimal](../) object dat de waarde vertegenwoordigt die moet worden afgetrokken |

### Retourwaarde

Een nieuw exemplaar van de [Decimal](../) klasse die een waarde vertegenwoordigt die het resultaat is van het aftrekken van de waarde die wordt weergegeven door het opgegeven object van de waarde die wordt weergegeven door het huidige object

## Decimal::operator-() const methode


Geeft een nieuw exemplaar van de [Decimal](../) klasse terug die een waarde vertegenwoordigt die het resultaat is van de negatie van de waarde die wordt weergegeven door het huidige object.

```cpp
Decimal System::Decimal::operator-() const
```

## Zie ook

* Klasse [Decimal](../)
* Naamruimte [System](../../)
* Bibliotheek [Aspose.Slides](../../../)