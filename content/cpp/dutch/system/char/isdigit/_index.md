---
title: IsDigit()
second_title: Aspose.Slides voor C++ API-referentie
description: Bepaalt of het teken op de opgegeven index in de opgegeven tekenbuffer als een decimale cijfer wordt geclassificeerd.
type: docs
weight: 79
url: /nl/system/char/isdigit/
---
## Char::IsDigit(const char_t *, int) methode

Bepaalt of het teken op de opgegeven index in de opgegeven tekenbuffer als een decimale cijfer wordt geclassificeerd.

```cpp
static bool System::Char::IsDigit(const char_t *str, int idx)
```

### Argumenten

| Parameter | Type | Description |
| --- | --- | --- |
| str | const char_t * | Pointer naar het begin van de tekenbuffer |
| idx | int | Een nulgebaseerde index in de opgegeven buffer van het te testen teken |

### Retourwaarde

True als het teken op de opgegeven index een decimale cijfer is, anders - false

## Char::IsDigit(const String\&, const int32_t) methode

Bepaalt of het teken op de opgegeven index in de opgegeven string als een decimale cijfer wordt geclassificeerd.

```cpp
static bool System::Char::IsDigit(const String &str, const int32_t idx)
```

### Argumenten

| Parameter | Type | Description |
| --- | --- | --- |
| str | const [String](../../string/)\& | Een string |
| idx | const **int32_t** | Een nulgebaseerde index in de opgegeven buffer van het te testen teken |

### Retourwaarde

True als het teken op de opgegeven index een decimale cijfer is, anders - false

## Char::IsDigit(char_t) methode

Bepaalt of het opgegeven teken als een decimale cijfer wordt geclassificeerd.

```cpp
static bool System::Char::IsDigit(char_t c)
```

### Argumenten

| Parameter | Type | Description |
| --- | --- | --- |
| c | char_t | Het te testen teken |

### Retourwaarde

True als het opgegeven teken een decimale cijfer is, anders - false

## Zie ook

* Klasse [Char](../)
* Klasse [String](../../string/)
* Naamruimte [System](../../)
* Bibliotheek [Aspose.Slides](../../../)