---
title: Delete()
second_title: Aspose.Slides voor C++ API-referentie
description: Verwijdert de map die wordt aangeduid door het pad dat wordt vertegenwoordigd door het huidige object, als de map leeg is.
type: docs
weight: 92
url: /nl/system.io/directoryinfo/delete/
---
## DirectoryInfo::Delete() methode

Verwijdert de map waarnaar wordt verwezen door het pad dat wordt vertegenwoordigd door het huidige object als de map leeg is.

```cpp
virtual void System::IO::DirectoryInfo::Delete() override
```

## DirectoryInfo::Delete(bool) methode

Verwijdert de map waarnaar wordt verwezen door het pad dat wordt vertegenwoordigd door het huidige object. Een parameter geeft aan of de inhoud van de map recursief moet worden verwijderd als de map niet leeg is.

```cpp
void System::IO::DirectoryInfo::Delete(bool recursive)
```

### Argumenten

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| recursive | **bool** | Geeft aan of de inhoud van de map recursief moet worden verwijderd als deze niet leeg is |

## Zie ook

* Klasse [DirectoryInfo](../)
* Naamruimte [System::IO](../../)
* Bibliotheek [Aspose.Slides](../../../)