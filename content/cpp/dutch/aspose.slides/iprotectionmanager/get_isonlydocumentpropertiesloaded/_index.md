---
title: get_IsOnlyDocumentPropertiesLoaded()
second_title: Aspose.Slides voor C++ API-referentie
description: Deze eigenschap is zinvol als het presentatiewerkbestand met een wachtwoord is beveiligd en de documenteigenschappen van dit bestand openbaar zijn. Een waarde van true betekent dat alleen documenteigenschappen worden geladen uit een versleuteld presentatiewerkbestand zonder gebruik van een wachtwoord. Een waarde van false betekent dat de volledige versleutelde presentatie wordt geladen met het juiste wachtwoord, niet alleen documenteigenschappen worden geladen. Als de presentatie niet versleuteld is, is de eigenschapswaarde altijd false. Als de documenteigenschappen van een versleuteld bestand niet openbaar zijn, is de eigenschapswaarde altijd false. Als PresentationEx.EncryptDocumentProperties true is, dan is de waarde van de IsOnlyDocumentPropertiesLoaded eigenschap altijd false. Alleen-lezen bool.
type: docs
weight: 40
url: /nl/aspose.slides/iprotectionmanager/get_isonlydocumentpropertiesloaded/
---
## IProtectionManager::get_IsOnlyDocumentPropertiesLoaded() methode

Deze eigenschap is zinvol als het presentatiewerkbestand met een wachtwoord is beveiligd en de documenteigenschappen van dit bestand openbaar zijn. Een waarde van true betekent dat alleen documenteigenschappen worden geladen uit een versleuteld presentatiewerkbestand zonder gebruik van een wachtwoord. Een waarde van false betekent dat de volledige versleutelde presentatie wordt geladen met het juiste wachtwoord, niet alleen de documenteigenschappen worden geladen. Als de presentatie niet versleuteld is, dan is de eigenschapswaarde altijd false. Als de documenteigenschappen van een versleuteld bestand niet openbaar zijn, dan is de eigenschapswaarde altijd false. Als PresentationEx.EncryptDocumentProperties true is, dan is de eigenschapswaarde van IsOnlyDocumentPropertiesLoaded altijd false. Alleen-lezen **bool**.

```cpp
virtual bool Aspose::Slides::IProtectionManager::get_IsOnlyDocumentPropertiesLoaded()=0
```

## Zie ook

* Klasse [IProtectionManager](../)
* Naamruimte [Aspose::Slides](../../)
* Bibliotheek [Aspose.Slides](../../../)