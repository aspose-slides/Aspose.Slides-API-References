---
title: idx_get()
second_title: Aspose.Slides pro C++ – referenční příručka API
description: Získá hodnotu hlavičky pomocí specifikované hlavičky požadavku.
type: docs
weight: 14
url: /cs/system.net/webheadercollection/idx_get/
---
## WebHeaderCollection::idx_get(HttpRequestHeader) metoda

Získá hodnotu hlavičky pomocí specifikované hlavičky požadavku.

```cpp
String System::Net::WebHeaderCollection::idx_get(HttpRequestHeader header)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| header | [HttpRequestHeader](../../httprequestheader/) | Hlavička. |

### Návratová hodnota

Hodnota hlavičky získaná pomocí specifikované hlavičky.

## WebHeaderCollection::idx_get(HttpResponseHeader) metoda

Získá hodnotu hlavičky pomocí specifikované hlavičky odezvy.

```cpp
String System::Net::WebHeaderCollection::idx_get(HttpResponseHeader header)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| header | [HttpResponseHeader](../../httpresponseheader/) | Hlavička. |

### Návratová hodnota

Hodnota hlavičky získaná pomocí specifikované hlavičky.

## WebHeaderCollection::idx_get(String) metoda

Získá hodnotu hlavičky pomocí specifikovaného názvu hlavičky.

```cpp
String System::Net::WebHeaderCollection::idx_get(String name)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| name | [String](../../../system/string/) | Název hlavičky. |

### Návratová hodnota

Hodnota hlavičky získaná pomocí specifikovaného názvu hlavičky.

## Viz také

* Enum [HttpRequestHeader](../../httprequestheader/)
* Enum [HttpResponseHeader](../../httpresponseheader/)
* Třída [String](../../../system/string/)
* Třída [WebHeaderCollection](../)
* Jmenný prostor [System::Net](../../)
* Library [Aspose.Slides](../../../)