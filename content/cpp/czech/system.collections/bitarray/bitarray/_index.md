---
title: BitArray()
second_title: Aspose.Slides pro C++ API Reference
description: Kopírovací konstruktor.
type: docs
weight: 14
url: /cs/system.collections/bitarray/bitarray/
---
## BitArray::BitArray(const bitset\&) konstruktor

Kopírovací konstruktor.

```cpp
System::Collections::BitArray::BitArray(const bitset &bits)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| bits | const [bitset](../bitset/)\& | Kontejner, ze kterého se kopírují data. |

## BitArray::BitArray(const BitArray\&) konstruktor

Kopírovací konstruktor.

```cpp
System::Collections::BitArray::BitArray(const BitArray &bits)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| bits | const [BitArray](../)\& | Kontejner, ze kterého se kopírují data. |

## BitArray::BitArray(const BitArrayPtr\&) konstruktor

Kopírovací konstruktor.

```cpp
System::Collections::BitArray::BitArray(const BitArrayPtr &array)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| array | const [BitArrayPtr](../../bitarrayptr/)\& | [BitArray](../) ke kopírování dat. |

## BitArray::BitArray(const System::ArrayPtr\<bool\>\&) konstruktor

Kopírovací konstruktor.

```cpp
System::Collections::BitArray::BitArray(const System::ArrayPtr<bool> &values)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| values | const [System::ArrayPtr](../../../system/arrayptr/)\<**bool**\>\& | Kontejner, ze kterého se po prvcích kopírují data. |

## BitArray::BitArray(const System::ArrayPtr\<uint8_t\>\&) konstruktor

Kopírovací konstruktor.

```cpp
System::Collections::BitArray::BitArray(const System::ArrayPtr<uint8_t> &bytes)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| bytes | const [System::ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\>\& | Kontejner, ze kterého se bitově kopírují data. |

## BitArray::BitArray(const System::ArrayPtr\<int\>\&) konstruktor

Kopírovací konstruktor.

```cpp
System::Collections::BitArray::BitArray(const System::ArrayPtr<int> &values)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| values | const [System::ArrayPtr](../../../system/arrayptr/)\<int\>\& | Kontejner, ze kterého se bitově kopírují data. |

## BitArray::BitArray(int, bool) konstruktor

Konstruktor vyplnění.

```cpp
System::Collections::BitArray::BitArray(int length, bool defaultValue=false)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| length | int | Počet prvků k vyplnění. |
| defaultValue | **bool** | Hodnota vyplnění. |

## Viz také

* Typedef [bitset](../bitset/)
* Typedef [ArrayPtr](../../../system/arrayptr/)
* Třída [BitArray](../)
* Třída [BitArrayPtr](../../bitarrayptr/)
* Jmenný prostor [System::Collections](../../)
* Knihovna [Aspose.Slides](../../../)