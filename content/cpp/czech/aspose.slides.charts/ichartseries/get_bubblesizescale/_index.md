---
title: get_BubbleSizeScale()
second_title: Aspose.Slides pro C++ API referenci
description: Určuje měřítkový faktor pro bublinový graf (může být mezi 0 a 300 procenty výchozí velikosti). Toto není vlastnost jen této série, ale všech sérií nadřazené skupiny sérií - jedná se o projekci odpovídající vlastnosti skupiny. Tato vlastnost je tedy pouze pro čtení. Použijte vlastnost ParentSeriesGroup pro přístup k nadřazené skupině sérií. Použijte get_ParentSeriesGroup()->get(set)_BubbleSizeScale() vlastnost pro čtení/zápis pro změnu hodnoty.
type: docs
weight: 612
url: /cs/aspose.slides.charts/ichartseries/get_bubblesizescale/
---
## IChartSeries::get_BubbleSizeScale() metoda


Určuje měřítkový faktor pro bublinový graf (může být mezi 0 a 300 procenty výchozí velikosti). Tato vlastnost není jen pro tuto sérii, ale pro všechny série nadřazené skupiny sérií – jedná se o projekci odpovídající vlastnosti skupiny. A tak je tato vlastnost pouze pro čtení. Použijte vlastnost ParentSeriesGroup pro přístup k nadřazené skupině sérií. Použijte [get_ParentSeriesGroup()](../get_parentseriesgroup/)->get(set)_BubbleSizeScale() vlastnost pro čtení/zápis pro změnu hodnoty.

```cpp
virtual int32_t Aspose::Slides::Charts::IChartSeries::get_BubbleSizeScale()=0
```

## Poznámky


Jedná se o projekci vlastnosti [get_ParentSeriesGroup()](../get_parentseriesgroup/)->get(set)_BubbleSizeScale(). 
## Viz také

* Třída [IChartSeries](../)
* Jmenný prostor [Aspose::Slides::Charts](../../)
* Knihovna [Aspose.Slides](../../../)