---
title: IndexOfAny()
second_title: Aspose.Slides pro C++ referenční příručka API
description: Najde index prvního výskytu kterékoliv ze dvou zadaných hodnot v ReadOnlySpan<T>
type: docs
weight: 157
url: /cs/system.memoryextensions/indexofany/
---
## System::MemoryExtensions::IndexOfAny(const ReadOnlySpan\<T\>\&, const T\&, const T\&) function

Najde index prvního výskytu kterékoliv ze dvou zadaných hodnot v ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const ReadOnlySpan<T> &span, const T &value0, const T &value1)
```

### Parametry šablony

| Parametr | Popis |
| --- | --- |
| T | Typ prvků v rozsahu |

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Rozsah, ve kterém se hledá |
| value0 | const T\& | První hodnota, kterou se hledá |
| value1 | const T\& | Druhá hodnota, kterou se hledá |

### Návratová hodnota

Index založený na nule prvního výskytu, nebo -1, pokud není nalezeno

## System::MemoryExtensions::IndexOfAny(const ReadOnlySpan\<T\>\&, const T\&, const T\&, const T\&) function

Najde index prvního výskytu kterékoliv ze tří zadaných hodnot v ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const ReadOnlySpan<T> &span, const T &value0, const T &value1, const T &value2)
```

### Parametry šablony

| Parametr | Popis |
| --- | --- |
| T | Typ prvků v rozsahu |

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Rozsah, ve kterém se hledá |
| value0 | const T\& | První hodnota, kterou se hledá |
| value1 | const T\& | Druhá hodnota, kterou se hledá |
| value2 | const T\& | Třetí hodnota, kterou se hledá |

### Návratová hodnota

Index založený na nule prvního výskytu, nebo -1, pokud není nalezeno

## System::MemoryExtensions::IndexOfAny(const Span\<T\>\&, const T\&, const T\&) function

Najde index prvního výskytu kterékoliv ze dvou zadaných hodnot v Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const Span<T> &span, const T &value0, const T &value1)
```

### Parametry šablony

| Parametr | Popis |
| --- | --- |
| T | Typ prvků v rozsahu |

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Rozsah, ve kterém se hledá |
| value0 | const T\& | První hodnota, kterou se hledá |
| value1 | const T\& | Druhá hodnota, kterou se hledá |

### Návratová hodnota

Index založený na nule prvního výskytu, nebo -1, pokud není nalezeno

## System::MemoryExtensions::IndexOfAny(const Span\<T\>\&, const T\&, const T\&, const T\&) function

Najde index prvního výskytu kterékoliv ze tří zadaných hodnot v Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const Span<T> &span, const T &value0, const T &value1, const T &value2)
```

### Parametry šablony

| Parametr | Popis |
| --- | --- |
| T | Typ prvků v rozsahu |

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Rozsah, ve kterém se hledá |
| value0 | const T\& | První hodnota, kterou se hledá |
| value1 | const T\& | Druhá hodnota, kterou se hledá |
| value2 | const T\& | Třetí hodnota, kterou se hledá |

### Návratová hodnota

Index založený na nule prvního výskytu, nebo -1, pokud není nalezeno

## System::MemoryExtensions::IndexOfAny(const ReadOnlySpan\<T\>\&, const ReadOnlySpan\<T\>\&) function

Najde index prvního výskytu jakékoli hodnoty ze span v jiném ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const ReadOnlySpan<T> &span, const ReadOnlySpan<T> &values)
```

### Parametry šablony

| Parametr | Popis |
| --- | --- |
| T | Typ prvků ve spanech |

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Rozsah, ve kterém se hledá |
| values | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Rozsah obsahující hodnoty, které se mají hledat |

### Návratová hodnota

Index založený na nule prvního výskytu, nebo -1, pokud není nalezeno

## System::MemoryExtensions::IndexOfAny(const Span\<T\>\&, const ReadOnlySpan\<T\>\&) function

Najde index prvního výskytu jakékoli hodnoty ze span v Span<T)

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const Span<T> &span, const ReadOnlySpan<T> &values)
```

### Parametry šablony

| Parametr | Popis |
| --- | --- |
| T | Typ prvků ve spanech |

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Rozsah, ve kterém se hledá |
| values | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Rozsah obsahující hodnoty, které se mají hledat |

### Návratová hodnota

Index založený na nule prvního výskytu, nebo -1, pokud není nalezeno

## Viz také

* Třída [ReadOnlySpan](../../system/readonlyspan/)
* Třída [Span](../../system/span/)
* Jmenný prostor [System::MemoryExtensions](../)
* Knihovna [Aspose.Slides](../../)