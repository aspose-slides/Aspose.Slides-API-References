---
title: IndexOfAnyExcept()
second_title: Aspose.Slides pro C++ – referenční příručka API
description: Najde index prvního prvku, který není roven zadané hodnotě v ReadOnlySpan<T>
type: docs
weight: 170
url: /cs/system.memoryextensions/indexofanyexcept/
---
## System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const T\&) funkce


Najde index prvního prvku, který není roven zadané hodnotě v ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan<T> &span, const T &value)
```


### Parametry šablony

| Parameter | Description |
| --- | --- |
| T | The type of elements in the span |

### Argumenty

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Rozsah, ve kterém se hledá |
| value | const T\& | Hodnota, která má být vyloučena ze vyhledávání |

### Návratová hodnota

Nulově založený index prvního neodpovídajícího prvku, nebo -1, pokud nebyl nalezen

## System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const T\&, const T\&) funkce


Najde index prvního prvku, který není roven žádné ze dvou zadaných hodnot v ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan<T> &span, const T &value0, const T &value1)
```


### Parametry šablony

| Parameter | Description |
| --- | --- |
| T | The type of elements in the span |

### Argumenty

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Rozsah, ve kterém se hledá |
| value0 | const T\& | První hodnota, která má být vyloučena ze vyhledávání |
| value1 | const T\& | Druhá hodnota, která má být vyloučena ze vyhledávání |

### Návratová hodnota

Nulově založený index prvního neodpovídajícího prvku, nebo -1, pokud nebyl nalezen

## System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const T\&, const T\&, const T\&) funkce


Najde index prvního prvku, který není roven žádné ze tří zadaných hodnot v ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan<T> &span, const T &value0, const T &value1, const T &value2)
```


### Parametry šablony

| Parameter | Description |
| --- | --- |
| T | The type of elements in the span |

### Argumenty

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Rozsah, ve kterém se hledá |
| value0 | const T\& | První hodnota, která má být vyloučena ze vyhledávání |
| value1 | const T\& | Druhá hodnota, která má být vyloučena ze vyhledávání |
| value2 | const T\& | Třetí hodnota, která má být vyloučena ze vyhledávání |

### Návratová hodnota

Nulově založený index prvního neodpovídajícího prvku, nebo -1, pokud nebyl nalezen

## System::MemoryExtensions::IndexOfAnyExcept(const Span\<T\>\&, const T\&) funkce


Najde index prvního prvku, který není roven zadané hodnotě v Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const Span<T> &span, const T &value)
```


### Parametry šablony

| Parameter | Description |
| --- | --- |
| T | The type of elements in the span |

### Argumenty

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Rozsah, ve kterém se hledá |
| value | const T\& | Hodnota, která má být vyloučena ze vyhledávání |

### Návratová hodnota

Nulově založený index prvního neodpovídajícího prvku, nebo -1, pokud nebyl nalezen

## System::MemoryExtensions::IndexOfAnyExcept(const Span\<T\>\&, const T\&, const T\&) funkce


Najde index prvního prvku, který není roven žádné ze dvou zadaných hodnot v Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const Span<T> &span, const T &value0, const T &value1)
```


### Parametry šablony

| Parameter | Description |
| --- | --- |
| T | The type of elements in the span |

### Argumenty

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Rozsah, ve kterém se hledá |
| value0 | const T\& | První hodnota, která má být vyloučena ze vyhledávání |
| value1 | const T\& | Druhá hodnota, která má být vyloučena ze vyhledávání |

### Návratová hodnota

Nulově založený index prvního neodpovídajícího prvku, nebo -1, pokud nebyl nalezen

## System::MemoryExtensions::IndexOfAnyExcept(const Span\<T\>\&, const T\&, const T\&, const T\&) funkce


Najde index prvního prvku, který není roven žádné ze tří zadaných hodnot v Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const Span<T> &span, const T &value0, const T &value1, const T &value2)
```


### Parametry šablony

| Parameter | Description |
| --- | --- |
| T | The type of elements in the span |

### Argumenty

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Rozsah, ve kterém se hledá |
| value0 | const T\& | První hodnota, která má být vyloučena ze vyhledávání |
| value1 | const T\& | Druhá hodnota, která má být vyloučena ze vyhledávání |
| value2 | const T\& | Třetí hodnota, která má být vyloučena ze vyhledávání |

### Návratová hodnota

Nulově založený index prvního neodpovídajícího prvku, nebo -1, pokud nebyl nalezen

## System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const ReadOnlySpan\<T\>\&) funkce


Najde index prvního prvku, který není roven žádné hodnotě ve spanu hodnot.

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan<T> &span, const ReadOnlySpan<T> &values)
```


### Parametry šablony

| Parameter | Description |
| --- | --- |
| T | The type of elements in the spans |

### Argumenty

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Rozsah, ve kterém se hledá |
| values | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Rozsah obsahující hodnoty, které mají být vyloučeny ze vyhledávání |

### Návratová hodnota

Nulově založený index prvního neodpovídajícího prvku, nebo -1, pokud nebyl nalezen

## System::MemoryExtensions::IndexOfAnyExcept(const Span\<T\>\&, const ReadOnlySpan\<T\>\&) funkce


Najde index prvního prvku, který není roven žádné hodnotě ve spanu hodnot v Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const Span<T> &span, const ReadOnlySpan<T> &values)
```


### Parametry šablony

| Parameter | Description |
| --- | --- |
| T | The type of elements in the spans |

### Argumenty

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Rozsah, ve kterém se hledá |
| values | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Rozsah obsahující hodnoty, které mají být vyloučeny ze vyhledávání |

### Návratová hodnota

Nulově založený index prvního neodpovídajícího prvku, nebo -1, pokud nebyl nalezen

## Viz také

* Třída [ReadOnlySpan](../../system/readonlyspan/)
* Třída [Span](../../system/span/)
* Jmenný prostor [System::MemoryExtensions](../)
* Knihovna [Aspose.Slides](../../)