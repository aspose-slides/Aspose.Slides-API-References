---
title: IsAmbiguousTime()
second_title: Aspose.Slides pro C++ referenční příručku API
description: Kontroluje, zda je zadané datum a čas nejednoznačný a může být mapován na více časů UTC.
type: docs
weight: 313
url: /cs/system/timezoneinfo/isambiguoustime/
---
## TimeZoneInfo::IsAmbiguousTime(DateTime) const metoda


Kontroluje, zda je zadané datum a čas nejednoznačný a může být mapován na více časů UTC.

```cpp
bool System::TimeZoneInfo::IsAmbiguousTime(DateTime date_time) const
```


### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| date_time | [DateTime](../../datetime/) | Datum a čas. |

### Návratová hodnota

True, pokud je date_time nejednoznačný.

## TimeZoneInfo::IsAmbiguousTime(const DateTimeOffset\&) const metoda


Kontroluje, zda je zadané datum a čas nejednoznačný a může být mapován na více časů UTC.

```cpp
bool System::TimeZoneInfo::IsAmbiguousTime(const DateTimeOffset &date_time_offset) const
```


### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| date_time_offset | const [DateTimeOffset](../../datetimeoffset/)\& | Datum a čas. |

### Návratová hodnota

True, pokud je date_time nejednoznačný.

## Viz také

* Třída [DateTime](../../datetime/)
* Třída [TimeZoneInfo](../)
* Třída [DateTimeOffset](../../datetimeoffset/)
* Jmenný prostor [System](../../)
* Knihovna [Aspose.Slides](../../../)