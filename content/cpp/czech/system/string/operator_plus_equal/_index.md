---
title: operator+=()
second_title: Aspose.Slides pro C++ – reference API
description: Operátor přiřazení konkatenace.
type: docs
weight: 287
url: /cs/system/string/operator_plus_equal/
---
## String::operator+=(char_t) metoda

Operátor přiřazení konkatenace.

```cpp
String & System::String::operator+=(char_t c)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| c | char_t | Znak, který se přidá na konec aktuálního řetězce. |

### Návratová hodnota

odkaz na sebe.

## String::operator+=(const String\&) metoda

Operátor přiřazení konkatenace.

```cpp
String & System::String::operator+=(const String &str)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| str | const [String](../)\& | [String](../) k přidání na konec aktuálního řetězce. |

### Návratová hodnota

odkaz na sebe.

## String::operator+=(double) metoda

Operátor přiřazení konkatenace.

```cpp
String & System::String::operator+=(double value)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| value | **double** | [Double](../../double/) k přidání na konec aktuálního řetězce. |

### Návratová hodnota

odkaz na sebe.

## String::operator+=(uint8_t) metoda

Operátor přiřazení konkatenace.

```cpp
String & System::String::operator+=(uint8_t value)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| value | **uint8_t** | [Byte](../../byte/) k přidání na konec aktuálního řetězce. |

### Návratová hodnota

odkaz na sebe.

## String::operator+=(int16_t) metoda

Operátor přiřazení konkatenace.

```cpp
String & System::String::operator+=(int16_t value)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| value | **int16_t** | Short k přidání na konec aktuálního řetězce. |

### Návratová hodnota

odkaz na sebe.

## String::operator+=(uint16_t) metoda

Operátor přiřazení konkatenace.

```cpp
String & System::String::operator+=(uint16_t value)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| value | **uint16_t** | Unsigned short k přidání na konec aktuálního řetězce. |

### Návratová hodnota

odkaz na sebe.

## String::operator+=(int32_t) metoda

Operátor přiřazení konkatenace.

```cpp
String & System::String::operator+=(int32_t value)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| value | **int32_t** | Int k přidání na konec aktuálního řetězce. |

### Návratová hodnota

odkaz na sebe.

## String::operator+=(uint32_t) metoda

Operátor přiřazení konkatenace.

```cpp
String & System::String::operator+=(uint32_t value)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| value | **uint32_t** | Unsigned int k přidání na konec aktuálního řetězce. |

### Návratová hodnota

odkaz na sebe.

## String::operator+=(int64_t) metoda

Operátor přiřazení konkatenace.

```cpp
String & System::String::operator+=(int64_t value)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| value | **int64_t** | Long k přidání na konec aktuálního řetězce. |

### Návratová hodnota

odkaz na sebe.

## String::operator+=(uint64_t) metoda

Operátor přiřazení konkatenace.

```cpp
String & System::String::operator+=(uint64_t value)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| value | **uint64_t** | Unsigned long k přidání na konec aktuálního řetězce. |

### Návratová hodnota

odkaz na sebe.

## String::operator+=(T) metoda

Operátor přiřazení konkatenace.

```cpp
template<typename T,std::enable_if_t< std::is_same< T, bool >::value > *> String & System::String::operator+=(T value)
```

### Parametry šablony

| Parametr | Popis |
| --- | --- |
| T | Typ hodnoty, který se má konkatenovat s řetězcem. Musí být bool |

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| value | T | [Boolean](../../boolean/) k přidání na konec aktuálního řetězce. |

### Návratová hodnota

odkaz na sebe.

## Viz také

* Třída [String](../)
* Jmenný prostor [System](../../)
* Knihovna [Aspose.Slides](../../../)