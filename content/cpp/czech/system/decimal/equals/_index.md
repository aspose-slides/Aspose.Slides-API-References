---
title: Equals()
second_title: Aspose.Slides pro C++ - API referenční příručka
description: Určuje, zda jsou hodnoty představované aktuálním objektem a zadaným objektem stejné.
type: docs
weight: 313
url: /cs/system/decimal/equals/
---
## Decimal::Equals(const Decimal\&) const metoda

Určuje, zda jsou hodnoty představované aktuálním objektem a zadaným objektem stejné.

```cpp
bool System::Decimal::Equals(const Decimal &d) const
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| d | const [Decimal](../)\& | Objekt [Decimal](../) pro porovnání s aktuálním objektem |

### Návratová hodnota

True pokud jsou hodnoty představované aktuálním a zadaným objektem stejné, jinak - false

## Decimal::Equals(const SharedPtr\<Object\>\&) const metoda

Určuje, zda jsou hodnoty představované aktuálním objektem a zadaným objektem stejné.

```cpp
bool System::Decimal::Equals(const SharedPtr<Object> &obj) const
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Objekt pro porovnání s aktuálním objektem |

### Návratová hodnota

True pokud jsou hodnoty představované aktuálním a zadaným objektem stejné, jinak - false

## Decimal::Equals(const Decimal\&, const Decimal\&) metoda

Určuje, zda jsou hodnoty představované zadanými objekty stejné.

```cpp
static bool System::Decimal::Equals(const Decimal &d1, const Decimal &d2)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| d1 | const [Decimal](../)\& | První porovnávaný |
| d2 | const [Decimal](../)\& | Druhý porovnávaný |

### Návratová hodnota

True pokud jsou hodnoty představované **d1** a **d2** stejné, jinak - false

## Viz také

* Typedef [SharedPtr](../../sharedptr/)
* Třída [Decimal](../)
* Třída [Object](../../object/)
* Jmenný prostor [System](../../)
* Knihovna [Aspose.Slides](../../../)