---
title: Equals()
second_title: Aspose.Slides pro C++ API Reference
description: Kontroluje, zda dva objekty DateTimeOffset představují stejný časový okamžik.
type: docs
weight: 365
url: /cs/system/datetimeoffset/equals/
---
## DateTimeOffset::Equals(const DateTimeOffset\&) const metoda


Kontroluje, zda dva [DateTimeOffset](../) objekty představují stejný časový okamžik.

```cpp
bool System::DateTimeOffset::Equals(const DateTimeOffset &other) const
```


### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/) k porovnání. |

### Návratová hodnota

true pokud je aktuální objekt roven **other**, jinak - false.

## DateTimeOffset::Equals(const SharedPtr\<Object\>\&) const metoda


Kontroluje, zda dva [DateTimeOffset](../) objekty představují stejný časový okamžik.

```cpp
bool System::DateTimeOffset::Equals(const SharedPtr<Object> &obj) const
```


### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/) k porovnání. |

### Návratová hodnota

true pokud je aktuální objekt roven **obj**, jinak - false.

## DateTimeOffset::Equals(const DateTimeOffset\&, const DateTimeOffset\&) metoda


Kontroluje, zda dva [DateTimeOffset](../) objekty představují stejný časový okamžik.

```cpp
static bool System::DateTimeOffset::Equals(const DateTimeOffset &first, const DateTimeOffset &second)
```


### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| first | const [DateTimeOffset](../)\& | První objekt k porovnání. |
| second | const [DateTimeOffset](../)\& | Druhý objekt k porovnání. |

### Návratová hodnota

true pokud je **first** roven **second**, jinak - false.

## Viz také

* Typedef [SharedPtr](../../sharedptr/)
* Třída [DateTimeOffset](../)
* Třída [Object](../../object/)
* Jmenný prostor [System](../../)
* Knihovna [Aspose.Slides](../../../)