---
title: WeakReference()
second_title: Aspose.Slides pro C++ – referenční příručka API
description: Výchozí konstruktor.
type: docs
weight: 1
url: /cs/system/weakreference_tmpl_t__end_tmpl/weakreference/
---
## WeakReference< T >::WeakReference() metoda

Výchozí konstruktor.

```cpp
System::WeakReference<T>::WeakReference()
```

## WeakReference< T >::WeakReference(std::nullptr_t) metoda

Konstruktor z nullptr.

```cpp
System::WeakReference<T>::WeakReference(std::nullptr_t)
```

## WeakReference< T >::WeakReference(const SmartPtr\<T\>\&) metoda

Inicializuje novou instanci třídy WeakReference, odkazující na zadaný objekt.

```cpp
System::WeakReference<T>::WeakReference(const SmartPtr<T> &data)
```

### Argumenty

| Parameter | Type | Description |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) k uložení. |

## WeakReference< T >::WeakReference(const SmartPtr\<T\>\&, bool) metoda

Inicializuje novou instanci třídy WeakReference, odkazující na zadaný objekt.

```cpp
System::WeakReference<T>::WeakReference(const SmartPtr<T> &data, bool trackResurrection)
```

### Argumenty

| Parameter | Type | Description |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) k uložení. |
| trackResurrection | **bool** | Ignored. |

## Viz také

* Třída [WeakReference< T >](../)
* Třída [SmartPtr](../../smartptr/)
* Jmenný prostor [System](../../)
* Knihovna [Aspose.Slides](../../../)