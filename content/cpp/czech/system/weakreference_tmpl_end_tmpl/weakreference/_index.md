---
title: WeakReference()
second_title: Aspose.Slides pro C++ – referenční příručka API
description: Výchozí konstruktor.
type: docs
weight: 1
url: /cs/system/weakreference_tmpl_end_tmpl/weakreference/
---
## WeakReference<>::WeakReference() metoda

Výchozí konstruktor.

```cpp
System::WeakReference<>::WeakReference()
```

## WeakReference<>::WeakReference(std::nullptr_t) metoda

Konstruktor z nullptr.

```cpp
System::WeakReference<>::WeakReference(std::nullptr_t)
```

## WeakReference<>::WeakReference(const SmartPtr\<Object\>\&) metoda

Inicializuje novou instanci třídy WeakReference, odkazující na zadaný objekt.

```cpp
System::WeakReference<>::WeakReference(const SmartPtr<Object> &data)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<[Object](../../object/)\>\& | [Object](../../object/) k uložení. |

## WeakReference<>::WeakReference(const SmartPtr\<Object\>\&, bool) metoda

Inicializuje novou instanci třídy WeakReference, odkazující na zadaný objekt.

```cpp
System::WeakReference<>::WeakReference(const SmartPtr<Object> &data, bool trackResurrection)
```

### Argumenty

| Parametr | Typ | Popis |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<[Object](../../object/)\>\& | [Object](../../object/) k uložení. |
| trackResurrection | **bool** | Ignorováno. |

## Viz také

* Třída [WeakReference<>](../)
* Třída [SmartPtr](../../smartptr/)
* Třída [Object](../../object/)
* Jmenný prostor [System](../../)
* Knihovna [Aspose.Slides](../../../)