---
title: IndexOfAny()
second_title: Aspose.Slides för C++ API-referens
description: Hittar indexet för den första förekomsten av någon av två specificerade värden i en ReadOnlySpan<T>
type: docs
weight: 157
url: /sv/system.memoryextensions/indexofany/
---
## System::MemoryExtensions::IndexOfAny(const ReadOnlySpan\<T\>\&, const T\&, const T\&) funktion

Hittar index för den första förekomsten av någon av två specificerade värden i en ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const ReadOnlySpan<T> &span, const T &value0, const T &value1)
```

### Mallparametrar

| Parameter | Beskrivning |
| --- | --- |
| T | Typen av element i intervallet |

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Intervallet att söka i |
| value0 | const T\& | Det första värdet att söka efter |
| value1 | const T\& | Det andra värdet att söka efter |

### Returvärde

Det nollbaserade indexet för den första förekomsten, eller -1 om det inte finns

## System::MemoryExtensions::IndexOfAny(const ReadOnlySpan\<T\>\&, const T\&, const T\&, const T\&) funktion

Hittar index för den första förekomsten av någon av tre specificerade värden i en ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const ReadOnlySpan<T> &span, const T &value0, const T &value1, const T &value2)
```

### Mallparametrar

| Parameter | Beskrivning |
| --- | --- |
| T | Typen av element i intervallet |

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Intervallet att söka i |
| value0 | const T\& | Det första värdet att söka efter |
| value1 | const T\& | Det andra värdet att söka efter |
| value2 | const T\& | Det tredje värdet att söka efter |

### Returvärde

Det nollbaserade indexet för den första förekomsten, eller -1 om det inte finns

## System::MemoryExtensions::IndexOfAny(const Span\<T\>\&, const T\&, const T\&) funktion

Hittar index för den första förekomsten av någon av två specificerade värden i en Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const Span<T> &span, const T &value0, const T &value1)
```

### Mallparametrar

| Parameter | Beskrivning |
| --- | --- |
| T | Typen av element i intervallet |

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Intervallet att söka i |
| value0 | const T\& | Det första värdet att söka efter |
| value1 | const T\& | Det andra värdet att söka efter |

### Returvärde

Det nollbaserade indexet för den första förekomsten, eller -1 om det inte finns

## System::MemoryExtensions::IndexOfAny(const Span\<T\>\&, const T\&, const T\&, const T\&) funktion

Hittar index för den första förekomsten av någon av tre specificerade värden i en Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const Span<T> &span, const T &value0, const T &value1, const T &value2)
```

### Mallparametrar

| Parameter | Beskrivning |
| --- | --- |
| T | Typen av element i intervallet |

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Intervallet att söka i |
| value0 | const T\& | Det första värdet att söka efter |
| value1 | const T\& | Det andra värdet att söka efter |
| value2 | const T\& | Det tredje värdet att söka efter |

### Returvärde

Det nollbaserade indexet för den första förekomsten, eller -1 om det inte finns

## System::MemoryExtensions::IndexOfAny(const ReadOnlySpan\<T\>\&, const ReadOnlySpan\<T\>\&) funktion

Hittar index för den första förekomsten av något värde från ett intervall i ett annat ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const ReadOnlySpan<T> &span, const ReadOnlySpan<T> &values)
```

### Mallparametrar

| Parameter | Beskrivning |
| --- | --- |
| T | Typen av element i intervallen |

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Intervallet att söka i |
| values | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Intervallet som innehåller värdena att söka efter |

### Returvärde

Det nollbaserade indexet för den första förekomsten, eller -1 om det inte finns

## System::MemoryExtensions::IndexOfAny(const Span\<T\>\&, const ReadOnlySpan\<T\>\&) funktion

Hittar index för den första förekomsten av något värde från ett intervall i en Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const Span<T> &span, const ReadOnlySpan<T> &values)
```

### Mallparametrar

| Parameter | Beskrivning |
| --- | --- |
| T | Typen av element i intervallen |

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Intervallet att söka i |
| values | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Intervallet som innehåller värdena att söka efter |

### Returvärde

Det nollbaserade indexet för den första förekomsten, eller -1 om det inte finns

## Se också

* Klass [ReadOnlySpan](../../system/readonlyspan/)
* Klass [Span](../../system/span/)
* Namnrymd [System::MemoryExtensions](../)
* Bibliotek [Aspose.Slides](../../)