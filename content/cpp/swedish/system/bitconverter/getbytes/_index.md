---
title: GetBytes()
second_title: Aspose.Slides för C++ API-referens
description: Konverterar det angivna booleska värdet till en bytearray.
type: docs
weight: 14
url: /sv/system/bitconverter/getbytes/
---
## BitConverter::GetBytes(bool) metod

Konverterar det angivna bool-värdet till en bytearray.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(bool value)
```

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | **bool** | Ett bool-värde att konvertera |

### Returvärde

1-byte array som representerar det angivna värdet

## BitConverter::GetBytes(char_t) metod

Konverterar det angivna char_t-värdet till en bytearray.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(char_t value)
```

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | char_t | Ett char_t-värde att konvertera |

### Returvärde

2-byte array som representerar det angivna värdet

## BitConverter::GetBytes(int16_t) metod

Konverterar det angivna 16-bit-heltal-värdet till en bytearray.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int16_t value)
```

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | **int16_t** | Ett 16-bit-heltal-värde att konvertera |

### Returvärde

2-byte array som representerar det angivna värdet

## BitConverter::GetBytes(int) metod

Konverterar det angivna 32-bit-heltal-värdet till en bytearray.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int value)
```

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | int | Ett 32-bit-heltal-värde att konvertera |

### Returvärde

4-byte array som representerar det angivna värdet

## BitConverter::GetBytes(int64_t) metod

Konverterar det angivna 64-bit-heltal-värdet till en bytearray.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int64_t value)
```

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | **int64_t** | Ett 64-bit-heltal-värde att konvertera |

### Returvärde

8-byte array som representerar det angivna värdet

## BitConverter::GetBytes(uint16_t) metod

Konverterar det angivna unsigned 16-bit-heltal-värdet till en bytearray.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint16_t value)
```

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | **uint16_t** | Ett unsigned 16-bit-heltal-värde att konvertera |

### Returvärde

2-byte array som representerar det angivna värdet

## BitConverter::GetBytes(uint32_t) metod

Konverterar det angivna unsigned 32-bit-heltal-värdet till en bytearray.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint32_t value)
```

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | **uint32_t** | Ett unsigned 32-bit-heltal-värde att konvertera |

### Returvärde

4-byte array som representerar det angivna värdet

## BitConverter::GetBytes(uint64_t) metod

Konverterar det angivna unsigned 64-bit-heltal-värdet till en bytearray.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint64_t value)
```

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | **uint64_t** | Ett unsigned 64-bit-heltal-värde att konvertera |

### Returvärde

8-byte array som representerar det angivna värdet

## BitConverter::GetBytes(float) metod

Konverterar det angivna enkel-precision flyttal-värdet till en bytearray.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(float value)
```

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | **float** | Ett flyttal-värde att konvertera |

### Returvärde

4-byte array som representerar det angivna värdet

## BitConverter::GetBytes(double) metod

Konverterar det angivna dubbel-precision flyttal-värdet till en bytearray.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(double value)
```

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | **double** | Ett flyttal-värde att konvertera |

### Returvärde

8-byte array som representerar det angivna värdet

## Se även

* Typedef [ArrayPtr](../../arrayptr/)
* Klass [BitConverter](../)
* Namnrymd [System](../../)
* Bibliotek [Aspose.Slides](../../../)