---
title: IsUpper()
second_title: Aspose.Slides för C++ API-referens
description: Avgör om tecknet på det angivna indexet i den angivna strängen är klassificerat som en stor bokstav.
type: docs
weight: 183
url: /sv/system/char/isupper/
---
## Char::IsUpper(const String\&, int) metod

Avgör om tecknet på det angivna indexet i den angivna strängen är klassificerat som en stor bokstav.

```cpp
static bool System::Char::IsUpper(const String &str, int idx)
```

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| str | const [String](../../string/)\& | Strängen som innehåller ett char. |
| idx | int | Ett nollbaserat index i den angivna strängen att testa |

### Returvärde

True om tecknet på det angivna indexet är en stor bokstav, annars - false

## Char::IsUpper(const char_t *, int) metod

Avgör om tecknet på det angivna indexet i den angivna teckenbufferten är klassificerat som en stor bokstav.

```cpp
static bool System::Char::IsUpper(const char_t *str, int idx)
```

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| str | const char_t * | Pekare till början av teckenbufferten |
| idx | int | Ett nollbaserat index i den angivna bufferten för tecknet att testa |

### Returvärde

True om tecknet på det angivna indexet är en stor bokstav, annars - false

## Char::IsUpper(char_t) metod

Avgör om det angivna tecknet är klassificerat som en stor bokstav.

```cpp
static bool System::Char::IsUpper(char_t c)
```

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| c | char_t | Tecknet att testa |

### Returvärde

True om det angivna tecknet är en stor bokstav, annars - false

## Se även

* Klass [String](../../string/)
* Klass [Char](../)
* Namnrymd [System](../../)
* Bibliotek [Aspose.Slides](../../../)