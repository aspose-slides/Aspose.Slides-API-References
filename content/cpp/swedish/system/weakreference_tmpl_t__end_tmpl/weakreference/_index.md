---
title: WeakReference()
second_title: Aspose.Slides för C++ API-referens
description: Standardkonstruktor.
type: docs
weight: 1
url: /sv/system/weakreference_tmpl_t__end_tmpl/weakreference/
---
## WeakReference< T >::WeakReference() metod

Standardkonstruktor.

```cpp
System::WeakReference<T>::WeakReference()
```

## WeakReference< T >::WeakReference(std::nullptr_t) metod

Konstruktor från nullptr.

```cpp
System::WeakReference<T>::WeakReference(std::nullptr_t)
```

## WeakReference< T >::WeakReference(const SmartPtr\<T\>\&) metod

Skapar en ny instans av WeakReference-klassen som refererar till det angivna objektet.

```cpp
System::WeakReference<T>::WeakReference(const SmartPtr<T> &data)
```

### Argument

| Parameter | Type | Description |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) att lagra. |

## WeakReference< T >::WeakReference(const SmartPtr\<T\>\&, bool) metod

Skapar en ny instans av WeakReference-klassen som refererar till det angivna objektet.

```cpp
System::WeakReference<T>::WeakReference(const SmartPtr<T> &data, bool trackResurrection)
```

### Argument

| Parameter | Type | Description |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) att lagra. |
| trackResurrection | **bool** | Ignoreras. |

## Se även

* Klass [WeakReference< T >](../)
* Klass [SmartPtr](../../smartptr/)
* Namnrymd [System](../../)
* Bibliotek [Aspose.Slides](../../../)