---
title: AddEllipse()
second_title: Aspose.Slides för C++ API-referens
description: Lägger till den angivna ellipsen till den bana som representeras av det aktuella objektet.
type: docs
weight: 196
url: /sv/system.drawing.drawing2d/graphicspath/addellipse/
---
## GraphicsPath::AddEllipse(float, float, float, float) metod

Lägger till den angivna ellipsen till den bana som representeras av det aktuella objektet.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddEllipse(float x, float y, float width, float height)
```

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| x | **float** | X-koordinaten för det övre vänstra hörnet på den rektangel som begränsar ellipsen som ska läggas till |
| y | **float** | Y-koordinaten för det övre vänstra hörnet på den rektangel som begränsar ellipsen som ska läggas till |
| width | **float** | Bredden på den rektangel som begränsar ellipsen som ska läggas till |
| height | **float** | Höjden på den rektangel som begränsar ellipsen som ska läggas till |

## GraphicsPath::AddEllipse(int, int, int, int) metod

Lägger till den angivna ellipsen till den bana som representeras av det aktuella objektet.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddEllipse(int x, int y, int width, int height)
```

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| x | int | X-koordinaten för det övre vänstra hörnet på den rektangel som begränsar ellipsen som ska läggas till |
| y | int | Y-koordinaten för det övre vänstra hörnet på den rektangel som begränsar ellipsen som ska läggas till |
| width | int | Bredden på den rektangel som begränsar ellipsen som ska läggas till |
| height | int | Höjden på den rektangel som begränsar ellipsen som ska läggas till |

## GraphicsPath::AddEllipse(const RectangleF\&) metod

Lägger till den angivna ellipsen till den bana som representeras av det aktuella objektet.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddEllipse(const RectangleF &rect)
```

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| rect | const [RectangleF](../../../system.drawing/rectanglef/)\& | Anger en rektangel som begränsar ellipsen som ska läggas till |

## GraphicsPath::AddEllipse(const Rectangle\&) metod

Lägger till den angivna ellipsen till den bana som representeras av det aktuella objektet.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddEllipse(const Rectangle &rect)
```

### Argument

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| rect | const [Rectangle](../../../system.drawing/rectangle/)\& | Anger en rektangel som begränsar ellipsen som ska läggas till |

## Se också

* Klass [GraphicsPath](../)
* Klass [RectangleF](../../../system.drawing/rectanglef/)
* Klass [Rectangle](../../../system.drawing/rectangle/)
* Namnrymd [System::Drawing::Drawing2D](../../)
* Bibliotek [Aspose.Slides](../../../)