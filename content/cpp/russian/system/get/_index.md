---
title: Get()
second_title: Aspose.Slides для C++ справочник API
description: Функция для получения N-го элемента заданного кортежа. Перегрузка для базового объекта.
type: docs
weight: 2367
url: /ru/system/get/
---
## System::Get(const SharedPtr\<Object\>\&) функция

Функция для получения N-го элемента кортежа. Перегрузка для базового объекта.

```cpp
template<std::size_t> auto System::Get(const SharedPtr<Object> &object)
```

### Параметры шаблона

| Параметр | Описание |
| --- | --- |
| N | индекс элемента. |

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| object | const [SharedPtr](../sharedptr/)\<[Object](../object/)\>\& | объект для осмотра. |

### Возвращаемое значение

значение N-го элемента кортежа, преобразованное к object.

## System::Get(const T\&) функция

Функция для получения N-го элемента кортежа. Перегрузка для объектов с методом Deconstruct.

```cpp
template<std::size_t,typename T> auto System::Get(const T &object)
```

### Параметры шаблона

| Параметр | Описание |
| --- | --- |
| N | индекс элемента. |
| T | тип проверяемого объекта. |

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| object | const T\& | объект для осмотра. |

### Возвращаемое значение

значение N-го элемента кортежа.

## System::Get(const SharedPtr\<T\>\&) функция

Функция для получения N-го элемента кортежа. Перегрузка для разделяемых указателей.

```cpp
template<std::size_t,typename T> auto System::Get(const SharedPtr<T> &pointer)
```

### Параметры шаблона

| Параметр | Описание |
| --- | --- |
| N | индекс элемента. |
| T | тип проверяемого объекта. |

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| object | const [SharedPtr](../sharedptr/)\<T\>\& | объект для осмотра. |

### Возвращаемое значение

значение N-го элемента кортежа.

## System::Get(const ValueTuple\<Args...\>\&) функция

Получает N-й элемент кортежа значений.

```cpp
template<std::size_t,typename...> auto System::Get(const ValueTuple<Args...> &tuple)
```

### Параметры шаблона

| Параметр | Описание |
| --- | --- |
| N | индекс элемента. |
| Args | элементы кортежа. |

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| tuple | const [ValueTuple](../valuetuple/)\<Args...\>\& | кортеж, из которого получить элемент. |

### Возвращаемое значение

значение N-го элемента кортежа.

## См. также

* Typedef [SharedPtr](../sharedptr/)
* Класс [Object](../object/)
* Класс [ValueTuple](../valuetuple/)
* Пространство имён [System](../)
* Библиотека [Aspose.Slides](../../)