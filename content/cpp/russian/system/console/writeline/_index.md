---
title: WriteLine()
second_title: Справочник API Aspose.Slides для C++
description: Выводит текущий разделитель строк в стандартный поток вывода.
type: docs
weight: 14
url: /ru/system/console/writeline/
---
## Console::WriteLine() метод


Выводит текущий разделитель строк в стандартный поток вывода.

```cpp
static void System::Console::WriteLine()
```

## Console::WriteLine(const SharedPtr\<T\>\&) метод


Выводит строковое представление указанного объекта, за которым следует текущий разделитель строк, в стандартный поток вывода.

```cpp
template<class T> static void System::Console::WriteLine(const SharedPtr<T> &object)
```


### Параметры шаблона

| Параметр | Описание |
| --- | --- |
| T | Тип выводимого объекта |

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| object | const [SharedPtr](../../sharedptr/)\<T\>\& | [Object](../../object/) для вывода |

## Console::WriteLine(bool) метод


Выводит строковое представление логического значения, за которым следует текущий разделитель строк, в стандартный поток вывода.

```cpp
static void System::Console::WriteLine(bool value)
```


### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| value | **bool** | Значение для вывода |

## Console::WriteLine(char_t) метод


Выводит указанный символьный параметр, за которым следует текущий разделитель строк, в стандартный поток вывода.

```cpp
static void System::Console::WriteLine(char_t value)
```


### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| value | char_t | Значение для вывода |

## Console::WriteLine(const ArrayPtr\<char_t\>\&) метод


Выводит строковое представление указанного массива символов, за которым следует текущий разделитель строк, в стандартный поток вывода.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer)
```


### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Массив для вывода |

## Console::WriteLine(const Decimal\&) метод


Выводит строковое представление значения [Decimal](../../decimal/), за которым следует текущий разделитель строк, в стандартный поток вывода.

```cpp
static void System::Console::WriteLine(const Decimal &value)
```


### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | Значение для вывода |

## Console::WriteLine(double) метод


Выводит строковое представление double-значения, за которым следует текущий разделитель строк, в стандартный поток вывода.

```cpp
static void System::Console::WriteLine(double value)
```


### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| value | **double** | Значение для вывода |

## Console::WriteLine(float) метод


Выводит строковое представление float-значения, за которым следует текущий разделитель строк, в стандартный поток вывода.

```cpp
static void System::Console::WriteLine(float value)
```


### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| value | **float** | Значение для вывода |

## Console::WriteLine(int32_t) метод


Выводит строковое представление 32-битного целого значения, за которым следует текущий разделитель строк, в стандартный поток вывода.

```cpp
static void System::Console::WriteLine(int32_t value)
```


### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| value | **int32_t** | Значение для вывода |

## Console::WriteLine(int64_t) метод


Выводит строковое представление 64-битного целого значения, за которым следует текущий разделитель строк, в стандартный поток вывода.

```cpp
static void System::Console::WriteLine(int64_t value)
```


### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| value | **int64_t** | Значение для вывода |

## Console::WriteLine(const String\&) метод


Выводит указанный объект строки, за которым следует текущий разделитель строк, в стандартный поток вывода.

```cpp
static void System::Console::WriteLine(const String &value)
```


### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| value | const [String](../../string/)\& | Объект строки для вывода |

## Console::WriteLine(const char_t *) метод


Выводит указанную C-строку, за которой следует текущий разделитель строк, в стандартный поток вывода.

```cpp
static void System::Console::WriteLine(const char_t *value)
```


### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| value | const char_t * | C-строка для вывода |

## Console::WriteLine(const TypeInfo\&) метод


Выводит строковое представление значения [TypeInfo](../../typeinfo/), за которым следует текущий разделитель строк, в стандартный поток вывода.

```cpp
static void System::Console::WriteLine(const TypeInfo &value)
```


### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | Значение для вывода |

## Console::WriteLine(uint32_t) метод


Выводит строковое представление беззнакового 32-битного целого значения, за которым следует текущий разделитель строк, в стандартный поток вывода.

```cpp
static void System::Console::WriteLine(uint32_t value)
```


### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| value | **uint32_t** | Значение для вывода |

## Console::WriteLine(uint64_t) метод


Выводит строковое представление беззнакового 64-битного целого значения, за которым следует текущий разделитель строк, в стандартный поток вывода.

```cpp
static void System::Console::WriteLine(uint64_t value)
```


### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| value | **uint64_t** | Значение для вывода |

## Console::WriteLine(const ArrayPtr\<char_t\>\&, int, int) метод


Выводит строковое представление указанного диапазона указанного массива символов, за которым следует текущий разделитель строк, в стандартный поток вывода.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer, int index, int count)
```


### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Массив символов |
| index | int | Индекс в массиве, с которого начинается диапазон вывода |
| count | int | Количество элементов в диапазоне вывода |

## Console::WriteLine(const Exception\&) метод


Выводит строковое представление указанного объекта Exception, за которым следует текущий разделитель строк, в стандартный поток вывода.

```cpp
static void System::Console::WriteLine(const Exception &e)
```


### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| e | const [Exception](../../exception/)\& | Значение для вывода |

## Console::WriteLine(const String\&, Args\&&...) метод


Выводит строковое представление указанных аргументов, отформатированных согласно указанному формату, за которым следует текущий разделитель строк, в стандартный поток вывода.

```cpp
template<class...> static void System::Console::WriteLine(const String &format, Args &&... args)
```


### Параметры шаблона

| Параметр | Описание |
| --- | --- |
| Типы | типы значений для вывода |

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| format | const [String](../../string/)\& | Формат строки |
| args | Args\&&... | Значения для вывода |

## Console::WriteLine(const char *) метод




```cpp
static void System::Console::WriteLine(const char *)=delete
```

## См. также

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Typedef [Exception](../../exception/)
* Class [Console](../)
* Class [Decimal](../../decimal/)
* Class [String](../../string/)
* Class [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)