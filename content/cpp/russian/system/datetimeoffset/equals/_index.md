---
title: Equals()
second_title: Справочник API Aspose.Slides для C++
description: Проверяет, представляют ли два объекта DateTimeOffset одну и ту же точку времени.
type: docs
weight: 365
url: /ru/system/datetimeoffset/equals/
---
## DateTimeOffset::Equals(const DateTimeOffset\&) const метод


Проверяет, представляют ли два [DateTimeOffset](../) объекта одну и ту же точку времени.

```cpp
bool System::DateTimeOffset::Equals(const DateTimeOffset &other) const
```


### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/) для сравнения. |

### Возвращаемое значение

true, если текущий объект равен **other**, иначе — false.

## DateTimeOffset::Equals(const SharedPtr\<Object\>\&) const метод


Проверяет, представляют ли два [DateTimeOffset](../) объекта одну и ту же точку времени.

```cpp
bool System::DateTimeOffset::Equals(const SharedPtr<Object> &obj) const
```


### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/) для сравнения. |

### Возвращаемое значение

true, если текущий объект равен **obj**, иначе — false.

## DateTimeOffset::Equals(const DateTimeOffset\&, const DateTimeOffset\&) метод


Проверяет, представляют ли два [DateTimeOffset](../) объекта одну и ту же точку времени.

```cpp
static bool System::DateTimeOffset::Equals(const DateTimeOffset &first, const DateTimeOffset &second)
```


### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| first | const [DateTimeOffset](../)\& | Первый объект для сравнения. |
| second | const [DateTimeOffset](../)\& | Второй объект для сравнения. |

### Возвращаемое значение

true, если **first** равен **second**, иначе — false.

## См. также

* Typedef [SharedPtr](../../sharedptr/)
* Класс [DateTimeOffset](../)
* Класс [Object](../../object/)
* Пространство имён [System](../../)
* Библиотека [Aspose.Slides](../../../)