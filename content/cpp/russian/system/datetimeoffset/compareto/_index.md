---
title: CompareTo()
second_title: Aspose.Slides для C++ справочник API
description: Сравнивает два объекта DateTimeOffset.
type: docs
weight: 352
url: /ru/system/datetimeoffset/compareto/
---
## DateTimeOffset::CompareTo(const DateTimeOffset\&) const метод

Сравнивает два объекта [DateTimeOffset](../).

```cpp
int System::DateTimeOffset::CompareTo(const DateTimeOffset &other) const
```

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/) для сравнения. |

### Возвращаемое значение

- 1 — если текущий объект раньше **other**, 1 — если текущий объект позже **other**, 0 — если текущий объект равен **other**.

## DateTimeOffset::CompareTo(const SharedPtr\<Object\>\&) const метод

Сравнивает два объекта [DateTimeOffset](../).

```cpp
int System::DateTimeOffset::CompareTo(const SharedPtr<Object> &obj) const
```

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/) для сравнения. |

### Возвращаемое значение

- 1 — если текущий объект раньше **obj**, 1 — если текущий объект позже **obj**, 0 — если текущий объект равен **obj**.

## См. также

* Typedef [SharedPtr](../../sharedptr/)
* Класс [DateTimeOffset](../)
* Класс [Object](../../object/)
* Пространство имён [System](../../)
* Library [Aspose.Slides](../../../)