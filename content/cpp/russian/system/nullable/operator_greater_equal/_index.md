---
title: operator>=()
second_title: Справочник API Aspose.Slides для C++
description: Всегда возвращает false.
type: docs
weight: 183
url: /ru/system/nullable/operator_greater_equal/
---
## Nullable::operator>=(std::nullptr_t) const метод


Всегда возвращает false.

```cpp
bool System::Nullable<T>::operator>=(std::nullptr_t) const
```


### Возвращаемое значение

Всегда – false

## Nullable::operator>=(const T1\&) const метод


Определяет, является ли значение, представленное текущим объектом, больше или равно значению, представленному указанным объектом, применяя [operator>=()](./) к этим значениям.

```cpp
template<typename T1> std::enable_if<!IsNullable<T1>::value, bool>::type System::Nullable<T>::operator>=(const T1 &other) const
```


### Параметры шаблона

| Параметр | Описание |
| --- | --- |
| T1 | Базовый тип значения, с которым сравнивается значение, представленное текущим объектом |

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| other | const T1\& | Константная ссылка на объект, с которым сравнивается текущий объект |

### Возвращаемое значение

True если значение, представленное текущим объектом, больше или равно значению, представленному указанным объектом, иначе - false

## Nullable::operator>=(const Nullable\<T1\>\&) const метод


Определяет, является ли значение, представленное текущим объектом, больше или равно значению, представленному указанным [Nullable](../) объектом, применяя [operator>=()](./) к этим значениям.

```cpp
template<typename T1> bool System::Nullable<T>::operator>=(const Nullable<T1> &other) const
```


### Параметры шаблона

| Параметр | Описание |
| --- | --- |
| T1 | Базовый тип [Nullable](../) объекта для сравнения |

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| other | const [Nullable](../)\<T1\>\& | Константная ссылка на [Nullable](../) объект для сравнения |

### Возвращаемое значение

True если значение, представленное текущим объектом, больше или равно значению, представленному указанным [Nullable](../) объектом, иначе - false

## См. также

* Класс [Nullable](../)
* Структура [IsNullable](../../isnullable/)
* Пространство имён [System](../../)
* Библиотека [Aspose.Slides](../../../)