---
title: FromAscii()
second_title: Aspose.Slides для C++ справочник API
description: Создает объект String из строки ASCII.
type: docs
weight: 950
url: /ru/system/string/fromascii/
---
## String::FromAscii(const char *) метод

Создает [String](../) из строки в кодировке ASCII.

```cpp
static String System::String::FromAscii(const char *asciiStr)
```

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| asciiStr | const char * | Указатель на нуль-терминированную строку, закодированную с помощью кодовой страницы ASCII. |

### Возвращаемое значение

[String](../) объект, представляющий переданную строку.

## String::FromAscii(const char *, int) метод

Создает [String](../) из строки в кодировке ASCII.

```cpp
static String System::String::FromAscii(const char *asciiStr, int len)
```

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| asciiStr | const char * | Указатель на строку, закодированную с помощью кодовой страницы ASCII. |
| len | int | Количество обрабатываемых символов. |

### Возвращаемое значение

[String](../) объект, представляющий переданную строку.

## String::FromAscii(const std::string\&) метод

Создает [String](../) из строки в кодировке ASCII.

```cpp
static String System::String::FromAscii(const std::string &asciiStr)
```

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| asciiStr | const std::string\& | Строка, закодированная в ASCII. |

### Возвращаемое значение

[String](../) объект, представляющий переданную строку.

## См. также

* Класс [String](../)
* Пространство имён [System](../../)
* Библиотека [Aspose.Slides](../../../)