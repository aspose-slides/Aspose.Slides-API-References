---
title: ToString()
second_title: Ссылка на API Aspose.Slides для C++
description: Обёртка для работы с классом String в контекстах, где ToString() вызывается для объектов типовых значений.
type: docs
weight: 365
url: /ru/system/string/tostring/
---
## String::ToString() const method


Обертка для работы с классом [String](../) в контекстах, где [ToString()](./) вызывается для объектов типовых значений.

```cpp
String System::String::ToString() const
```


### Возвращаемое значение

Копия текущей строки. Считается нулевой строкой, если текущая строка является нулевой строкой.

## String::ToString(const SharedPtr\<IFormatProvider\>\&) const method


Обертка для работы с классом [String](../) в контекстах, где [ToString()](./) вызывается для объектов типовых значений.

```cpp
String System::String::ToString(const SharedPtr<IFormatProvider> &) const
```


### Возвращаемое значение

Копия текущей строки. Считается нулевой строкой, если текущая строка является нулевой строкой.

## См. также

* Typedef [SharedPtr](../../sharedptr/)
* Класс [String](../)
* Класс [IFormatProvider](../../iformatprovider/)
* Пространство имен [System](../../)
* Библиотека [Aspose.Slides](../../../)