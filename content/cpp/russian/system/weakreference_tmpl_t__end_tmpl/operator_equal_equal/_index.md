---
title: operator==()
second_title: Aspose.Slides для C++ Справочник API
description: Проверяет, является ли ссылка на объект нулевой.
type: docs
weight: 53
url: /ru/system/weakreference_tmpl_t__end_tmpl/operator_equal_equal/
---
## WeakReference< T >::operator==(std::nullptr_t) const метод

Проверяет, является ли ссылка на объект нулевой.

```cpp
bool System::WeakReference<T>::operator==(std::nullptr_t) const
```

### Возвращаемое значение

True, если ссылка на объект равна null, иначе false.

## WeakReference< T >::operator==(const WeakReference\<T\>\&) const метод

Сравнивает объект, на который ссылаются, с другим экземпляром класса WeakReference.

```cpp
bool System::WeakReference<T>::operator==(const WeakReference<T> &other) const
```

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| other | const [WeakReference](../weakreference/)\<T\>\& | [Object](../../object/) сравнить с. |

### Возвращаемое значение

True, если сравниваемые объекты указывают на один и тот же объект, иначе false.

## См. также

* Метод [WeakReference](../weakreference/)
* Класс [WeakReference< T >](../)
* Пространство имён [System](../../)
* Библиотека [Aspose.Slides](../../../)