---
title: WeakReference()
second_title: Aspose.Slides для C++ справочник API
description: Конструктор по умолчанию.
type: docs
weight: 1
url: /ru/system/weakreference_tmpl_t__end_tmpl/weakreference/
---
## WeakReference< T >::WeakReference() метод

Конструктор по умолчанию.

```cpp
System::WeakReference<T>::WeakReference()
```

## WeakReference< T >::WeakReference(std::nullptr_t) метод

Конструктор из nullptr.

```cpp
System::WeakReference<T>::WeakReference(std::nullptr_t)
```

## WeakReference< T >::WeakReference(const SmartPtr\<T\>\&) метод

Инициализирует новый экземпляр класса WeakReference, ссылающийся на указанный объект.

```cpp
System::WeakReference<T>::WeakReference(const SmartPtr<T> &data)
```

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) для хранения. |

## WeakReference< T >::WeakReference(const SmartPtr\<T\>\&, bool) метод

Инициализирует новый экземпляр класса WeakReference, ссылающийся на указанный объект.

```cpp
System::WeakReference<T>::WeakReference(const SmartPtr<T> &data, bool trackResurrection)
```

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) для хранения. |
| trackResurrection | **bool** | Игнорируется. |

## См. также

* Класс [WeakReference< T >](../)
* Класс [SmartPtr](../../smartptr/)
* Пространство имён [System](../../)
* Library [Aspose.Slides](../../../)