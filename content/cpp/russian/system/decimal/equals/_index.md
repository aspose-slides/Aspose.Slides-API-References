---
title: Equals()
second_title: Aspose.Slides для C++ справка по API
description: Определяет, равны ли значения, представленные текущим объектом и указанным объектом.
type: docs
weight: 313
url: /ru/system/decimal/equals/
---
## Decimal::Equals(const Decimal\&) const method

Определяет, равны ли значения, представленные текущим объектом и указанным объектом.

```cpp
bool System::Decimal::Equals(const Decimal &d) const
```

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| d | const [Decimal](../)\& | Объект [Decimal](../) для сравнения с текущим объектом |

### Возвращаемое значение

True если значение, представленное текущим и указанным объектами, равно, иначе - false

## Decimal::Equals(const SharedPtr\<Object\>\&) const method

Определяет, равны ли значения, представленные текущим объектом и указанным объектом.

```cpp
bool System::Decimal::Equals(const SharedPtr<Object> &obj) const
```

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Объект для сравнения с текущим объектом |

### Возвращаемое значение

True если значение, представленное текущим и указанным объектами, равно, иначе - false

## Decimal::Equals(const Decimal\&, const Decimal\&) method

Определяет, равны ли значения, представленные указанными объектами.

```cpp
static bool System::Decimal::Equals(const Decimal &d1, const Decimal &d2)
```

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| d1 | const [Decimal](../)\& | Первый сравниваемый |
| d2 | const [Decimal](../)\& | Второй сравниваемый |

### Возвращаемое значение

True если значения **d1** и **d2** равны, иначе - false

## См. также

* Тип [SharedPtr](../../sharedptr/)
* Класс [Decimal](../)
* Класс [Object](../../object/)
* Пространство имен [System](../../)
* Библиотека [Aspose.Slides](../../../)