---
title: Exchange()
second_title: Справка по API Aspose.Slides для C++
description: "Обменивает значение переменной: сохраняет новое значение и возвращает значение переменной, которое было непосредственно перед сохранением."
type: docs
weight: 66
url: /ru/system.threading/interlocked/exchange/
---
## Interlocked::Exchange(T\&, T) метод


Обменивает значение переменной: сохраняет новое значение и возвращает значение переменной, которое было непосредственно перед сохранением.

```cpp
template<typename T> static std::enable_if<IsSupportedInt<T>, T>::type System::Threading::Interlocked::Exchange(T &location1, T value)
```


### Параметры шаблона

| Параметр | Описание |
| --- | --- |
| T | Тип переменной. |

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| location1 | T\& | Ссылка на переменную для изменения. |
| value | T | Значение для сохранения. |

### Возвращаемое значение

Значение переменной непосредственно перед изменением.

## Interlocked::Exchange(T\&, T) метод


Обменивает значение переменной: сохраняет новое значение и возвращает значение переменной, которое было непосредственно перед сохранением. Не реализовано.

```cpp
template<typename T> static std::enable_if<!IsSupportedInt<T>, T>::type System::Threading::Interlocked::Exchange(T &location1, T value)
```


### Параметры шаблона

| Параметр | Описание |
| --- | --- |
| T | Тип переменной. |

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| location1 | T\& | Ссылка на переменную для изменения. |
| value | T | Значение для сохранения. |

### Возвращаемое значение

Значение переменной непосредственно перед изменением.

## См. также

* Класс [Interlocked](../)
* Пространство имён [System::Threading](../../)
* Библиотека [Aspose.Slides](../../../)