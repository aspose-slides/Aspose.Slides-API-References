---
title: CompareExchange()
second_title: Aspose.Slides для C++ API Reference
description: "Обменивает значение переменной: проверяет, равно ли переменная конкретному значению, и сохраняет новое значение только если текущее значение совпадает с ожидаемым."
type: docs
weight: 79
url: /ru/system.threading/interlocked/compareexchange/
---
## Interlocked::CompareExchange(T\&, T, T) метод

Обменивает значение переменной: проверяет, равно ли переменная конкретному значению, и сохраняет новое значение только если текущее значение совпадает с ожидаемым.

```cpp
template<typename T> static std::enable_if<IsSupportedInt<T>, T>::type System::Threading::Interlocked::CompareExchange(T &location1, T value, T comparand)
```

### Параметры шаблона

| Параметр | Описание |
| --- | --- |
| T | Тип переменной. |

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| location1 | T\& | Ссылка на переменную для изменения. |
| value | T | Значение для сохранения. |
| comparand | T | Значение, с которым сравнивается значение переменной перед обменом. |

### Возвращаемое значение

Значение переменной в начале операции независимо от того, было ли оно изменено.

## Interlocked::CompareExchange(T\&, T, T) метод

Обменивает значение переменной: проверяет, равно ли переменная конкретному значению, и сохраняет новое значение только если текущее значение совпадает с ожидаемым. Не реализовано.

```cpp
template<typename T> static std::enable_if<!IsSupportedInt<T>, T>::type System::Threading::Interlocked::CompareExchange(T &location1, T value, T comparand)
```

### Параметры шаблона

| Параметр | Описание |
| --- | --- |
| T | Тип переменной. |

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| location1 | T\& | Ссылка на переменную для изменения. |
| value | T | Значение для сохранения. |
| comparand | T | Значение, с которым сравнивается значение переменной перед обменом. |

### Возвращаемое значение

Значение переменной в начале операции независимо от того, было ли оно изменено.

## Interlocked::CompareExchange(int32_t\&, int32_t, int32_t, bool\&) метод

Обменивает значение переменной: проверяет, равно ли переменная конкретному значению, и сохраняет новое значение только если текущее значение совпадает с ожидаемым.

```cpp
static int32_t System::Threading::Interlocked::CompareExchange(int32_t &location1, int32_t value, int32_t comparand, bool &succeeded)
```

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| location1 | **int32_t**\& | Ссылка на переменную для изменения. |
| value | **int32_t** | Значение для сохранения. |
| comparand | **int32_t** | Значение, с которым сравнивается значение переменной перед обменом. |
| succeeded | **bool**\& | Ссылка на переменную, которая устанавливается в true, если обмен произошёл, и в false в противном случае. |

### Возвращаемое значение

Значение переменной в начале операции независимо от того, было ли оно изменено.

## См. также

* Класс [Interlocked](../)
* Пространство имён [System::Threading](../../)
* Библиотека [Aspose.Slides](../../../)