---
title: NetworkToHostOrder()
second_title: Aspose.Slides для C++ справочник API
description: Преобразует указанный порядок байтов сети в соответствующий порядок байтов хоста.
type: docs
weight: 261
url: /ru/system.net/ipaddress/networktohostorder/
---
## IPAddress::NetworkToHostOrder(int64_t) метод


Преобразует указанный порядок байтов сети в соответствующий порядок байтов хоста.

```cpp
static int64_t System::Net::IPAddress::NetworkToHostOrder(int64_t network)
```


### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| network | **int64_t** | Значение, которое нужно преобразовать. |

### Возвращаемое значение

Соответствующий порядок байтов хоста.

## IPAddress::NetworkToHostOrder(int32_t) метод


Преобразует указанный порядок байтов сети в соответствующий порядок байтов хоста.

```cpp
static int32_t System::Net::IPAddress::NetworkToHostOrder(int32_t network)
```


### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| network | **int32_t** | Значение, которое нужно преобразовать. |

### Возвращаемое значение

Соответствующий порядок байтов хоста.

## IPAddress::NetworkToHostOrder(int16_t) метод


Преобразует указанный порядок байтов сети в соответствующий порядок байтов хоста.

```cpp
static int16_t System::Net::IPAddress::NetworkToHostOrder(int16_t network)
```


### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| network | **int16_t** | Значение, которое нужно преобразовать. |

### Возвращаемое значение

Соответствующий порядок байтов хоста.

## См. также

* Класс [IPAddress](../)
* Пространство имён [System::Net](../../)
* Библиотека [Aspose.Slides](../../../)