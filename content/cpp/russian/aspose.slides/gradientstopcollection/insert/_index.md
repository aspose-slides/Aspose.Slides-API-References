---
title: Insert()
second_title: Aspose.Slides для C++ справочник API
description: Создает новую точку градиента и вставляет её в указанный индекс коллекции.
type: docs
weight: 66
url: /ru/aspose.slides/gradientstopcollection/insert/
---
## GradientStopCollection::Insert(int32_t, float, System::Drawing::Color) метод

Создает новую точку градиента и вставляет её в указанный индекс в коллекцию.

```cpp
void Aspose::Slides::GradientStopCollection::Insert(int32_t index, float position, System::Drawing::Color color) override
```

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| index | **int32_t** | Индекс в коллекции, в который будет вставлена новая точка градиента. |
| position | **float** | Позиция новой точки градиента. |
| color | [System::Drawing::Color](../../../system.drawing/color/) | Цвет новой градиентной точки. |

## GradientStopCollection::Insert(int32_t, float, PresetColor) метод

Создает новую точку градиента и вставляет её в указанный индекс в коллекцию.

```cpp
void Aspose::Slides::GradientStopCollection::Insert(int32_t index, float position, PresetColor presetColor) override
```

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| index | **int32_t** | Индекс в коллекции, в который будет вставлена новая точка градиента. |
| position | **float** | Позиция новой точки градиента. |
| presetColor | [PresetColor](../../presetcolor/) | Цвет новой градиентной точки. |

## GradientStopCollection::Insert(int32_t, float, SchemeColor) метод

Создает новую точку градиента и вставляет её в указанный индекс в коллекцию.

```cpp
void Aspose::Slides::GradientStopCollection::Insert(int32_t index, float position, SchemeColor schemeColor) override
```

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| index | **int32_t** | Индекс в коллекции, в который будет вставлена новая точка градиента. |
| position | **float** | Позиция новой точки градиента. |
| schemeColor | [SchemeColor](../../schemecolor/) | Цвет новой градиентной точки. |

## См. также

* Enum [PresetColor](../../presetcolor/)
* Enum [SchemeColor](../../schemecolor/)
* Класс [Color](../../../system.drawing/color/)
* Класс [GradientStopCollection](../)
* Пространство имён [Aspose::Slides](../../)
* Library [Aspose.Slides](../../../)