---
title: IndexOfAnyExcept()
second_title: Aspose.Slides для C++ справка API
description: Находит индекс первого элемента, который не равен указанному значению в ReadOnlySpan<T>
type: docs
weight: 170
url: /ru/system.memoryextensions/indexofanyexcept/
---
## System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const T\&) функция


Находит индекс первого элемента, который не равен указанному значению в ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan<T> &span, const T &value)
```


### Параметры шаблона

| Параметр | Описание |
| --- | --- |
| T | Тип элементов в диапазоне |

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Диапазон для поиска |
| value | const T\& | Значение, которое следует исключить из поиска |

### Возвращаемое значение

Нулевой индекс первого несовпадающего элемента, или -1, если не найден

## System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const T\&, const T\&) функция


Находит индекс первого элемента, который не равен ни одному из двух указанных значений в ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan<T> &span, const T &value0, const T &value1)
```


### Параметры шаблона

| Параметр | Описание |
| --- | --- |
| T | Тип элементов в диапазоне |

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Диапазон для поиска |
| value0 | const T\& | Первое значение, которое следует исключить из поиска |
| value1 | const T\& | Второе значение, которое следует исключить из поиска |

### Возвращаемое значение

Нулевой индекс первого несовпадающего элемента, или -1, если не найден

## System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const T\&, const T\&, const T\&) функция


Находит индекс первого элемента, который не равен ни одному из трёх указанных значений в ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan<T> &span, const T &value0, const T &value1, const T &value2)
```


### Параметры шаблона

| Параметр | Описание |
| --- | --- |
| T | Тип элементов в диапазоне |

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Диапазон для поиска |
| value0 | const T\& | Первое значение, которое следует исключить из поиска |
| value1 | const T\& | Второе значение, которое следует исключить из поиска |
| value2 | const T\& | Третье значение, которое следует исключить из поиска |

### Возвращаемое значение

Нулевой индекс первого несовпадающего элемента, или -1, если не найден

## System::MemoryExtensions::IndexOfAnyExcept(const Span\<T\>\&, const T\&) функция


Находит индекс первого элемента, который не равен указанному значению в Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const Span<T> &span, const T &value)
```


### Параметры шаблона

| Параметр | Описание |
| --- | --- |
| T | Тип элементов в диапазоне |

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Диапазон для поиска |
| value | const T\& | Значение, которое следует исключить из поиска |

### Возвращаемое значение

Нулевой индекс первого несовпадающего элемента, или -1, если не найден

## System::MemoryExtensions::IndexOfAnyExcept(const Span\<T\>\&, const T\&, const T\&) функция


Находит индекс первого элемента, который не равен ни одному из двух указанных значений в Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const Span<T> &span, const T &value0, const T &value1)
```


### Параметры шаблона

| Параметр | Описание |
| --- | --- |
| T | Тип элементов в диапазоне |

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Диапазон для поиска |
| value0 | const T\& | Первое значение, которое следует исключить из поиска |
| value1 | const T\& | Второе значение, которое следует исключить из поиска |

### Возвращаемое значение

Нулевой индекс первого несовпадающего элемента, либо -1, если не найден

## System::MemoryExtensions::IndexOfAnyExcept(const Span\<T\>\&, const T\&, const T\&, const T\&) функция


Находит индекс первого элемента, который не равен ни одному из трёх указанных значений в Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const Span<T> &span, const T &value0, const T &value1, const T &value2)
```


### Параметры шаблона

| Параметр | Описание |
| --- | --- |
| T | Тип элементов в диапазоне |

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Диапазон для поиска |
| value0 | const T\& | Первое значение, которое следует исключить из поиска |
| value1 | const T\& | Второе значение, которое следует исключить из поиска |
| value2 | const T\& | Третье значение, которое следует исключить из поиска |

### Возвращаемое значение

Нулевой индекс первого несовпадающего элемента, или -1, если не найден

## System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const ReadOnlySpan\<T\>\&) функция


Находит индекс первого элемента, который не равен ни одному из значений в диапазоне значений.

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan<T> &span, const ReadOnlySpan<T> &values)
```


### Параметры шаблона

| Параметр | Описание |
| --- | --- |
| T | Тип элементов в диапазонах |

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Диапазон для поиска |
| values | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Диапазон, содержащий значения для исключения из поиска |

### Возвращаемое значение

Нулевой индекс первого несовпадающего элемента, или -1, если не найден

## System::MemoryExtensions::IndexOfAnyExcept(const Span\<T\>\&, const ReadOnlySpan\<T\>\&) функция


Находит индекс первого элемента, который не равен ни одному из значений в диапазоне значений в Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const Span<T> &span, const ReadOnlySpan<T> &values)
```


### Параметры шаблона

| Параметр | Описание |
| --- | --- |
| T | Тип элементов в диапазонах |

### Аргументы

| Параметр | Тип | Описание |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Диапазон для поиска |
| values | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Диапазон, содержащий значения для исключения из поиска |

### Возвращаемое значение

Нулевой индекс первого несовпадающего элемента, или -1, если не найден

## См. также

* Класс [ReadOnlySpan](../../system/readonlyspan/)
* Класс [Span](../../system/span/)
* Пространство имён [System::MemoryExtensions](../)
* Библиотека [Aspose.Slides](../../)