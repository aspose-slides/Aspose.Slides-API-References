---
title: GetOrCreateDataPointByIdx()
second_title: Aspose.Slides для C++ справочник API
description: "Если коллекция уже содержит точку данных с индексом index, то возвращает эту точку данных. Если коллекция не содержит точку данных с индексом index ==N (когда количество точек данных в этой коллекции меньше или равно N), то добавляет недостающие точки данных и возвращает последнюю (которая имеет запрошенный индекс). Например, индексы в коллекции: {0, 1, 2}, а запрашиваемый индекс — 5. Тогда метод добавляет недостающие точки данных: {0, 1, 2, 3, 4, 5}. И возвращает точку данных с индексом 5."
type: docs
weight: 131
url: /ru/aspose.slides.charts/ichartdatapointcollection/getorcreatedatapointbyidx/
---
## IChartDataPointCollection::GetOrCreateDataPointByIdx(uint32_t) метод

Если коллекция уже содержит точку данных с индексом *index*, то возвращает эту точку данных. Если коллекция не содержит точку данных с индексом *index* ==N (когда количество точек данных в этой коллекции меньше или равно N), то добавляет недостающие точки данных и возвращает последнюю (которая имеет запрошенный индекс). Например, индексы в коллекции: {0, 1, 2}, а запрашиваемый индекс — 5. Тогда метод добавляет недостающие точки данных: {0, 1, 2, 3, 4, 5}. И возвращает точку данных с индексом 5.

```cpp
virtual System::SharedPtr<IChartDataPoint> Aspose::Slides::Charts::IChartDataPointCollection::GetOrCreateDataPointByIdx(uint32_t index)=0
```

### Параметры

| Параметр | Тип | Описание |
| --- | --- | --- |
| index | **uint32_t** | Индекс. |

### Возвращаемое значение

Возвращает точку данных с запрошенным индексом.

## См. также

* Typedef [SharedPtr](../../../system/sharedptr/)
* Класс [IChartDataPoint](../../ichartdatapoint/)
* Класс [IChartDataPointCollection](../)
* Пространство имён [Aspose::Slides::Charts](../../)
* Библиотека [Aspose.Slides](../../../)