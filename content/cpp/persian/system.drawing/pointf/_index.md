---
title: PointF
second_title: مرجع API Aspose.Slides برای C++
description: "نمایشگر یک جفت مختصات X و Y نقطه‌ای با نقطه شناور تک‌دقت در یک صفحهٔ دوبعدی است. این نوع باید بر روی پشته تخصیص داده شود و به توابع به‌صورت مقدار یا ارجاع پاس داده شود. هرگز از کلاس System::SmartPtr برای مدیریت اشیاء این نوع استفاده نکنید."
type: docs
weight: 222
url: /fa/system.drawing/pointf/
---
## PointF کلاس

نمایشگر یک جفت مختصات X و Y نقطه‌ای با نقطه شناور تک‌دقت در یک صفحهٔ دوبعدی است. این نوع باید بر روی پشته تخصیص داده شود و به توابع به‌صورت مقدار یا ارجاع پاس داده شود. هرگز از کلاس [System::SmartPtr](../../system/smartptr/) برای مدیریت اشیاء این نوع استفاده نکنید.

```cpp
class PointF
```

## متدها

| Method | Description |
| --- | --- |
| static [PointF](./) [Add](./add/)(const [PointF](./)\&, const [SizeF](../sizef/)\&) | مقدارهای عرض و ارتفاع شیء [SizeF](../sizef/) مشخص‌شده را به مقادیر مختصات X و Y شیء [PointF](./) مشخص‌شده به‌طور متناظر اضافه می‌کند. |
| static [PointF](./) [Add](./add/)(const [PointF](./)\&, const [Size](../size/)\&) | مقدارهای عرض و ارتفاع شیء [Size](../size/) مشخص‌شده را به مقادیر مختصات X و Y شیء [PointF](./) مشخص‌شده به‌طور متناظر اضافه می‌کند. |
| **bool** [Equals](./equals/)(const [PointF](./)\&) const | تعیین می‌کند که آیا شیء جاری و شیء مشخص‌شده برابر هستند، یعنی همان جفت مقادیر مختصات X و Y را نشان می‌دهند. |
| **bool** [get_IsEmpty](./get_isempty/)() const | تعیین می‌کند که آیا هر دو مقدار مختصات X و Y برابر ۰ هستند. |
| **float** [get_X](./get_x/)() const | مقدار مختصات X را که توسط شیء جاری نمایندگی می‌شود، برمی‌گرداند. |
| **float** [get_Y](./get_y/)() const | مقدار مختصات Y را که توسط شیء جاری نمایندگی می‌شود، برمی‌گرداند. |
| int [GetHashCode](./gethashcode/)() const | کد هش برای شیء جاری را برمی‌گرداند. |
| **bool** [IsNull](./isnull/)() const | همیشه مقدار false را برمی‌گرداند. |
| explicit [operator bool](./operator_bool/)() | همیشه مقدار true را برمی‌گرداند. |
| [PointF](./pointf/)() | شیء جدیدی از نوع [PointF](./) می‌سازد و مقادیر مختصات X و Y آن را با ۰ مقداردهی اولیه می‌کند. |
| [PointF](./pointf/)(**float**, **float**) | شیء جدیدی از نوع [PointF](./) می‌سازد و آن را با مقادیر مشخص‌شده مقداردهی اولیه می‌کند. |
| [PointF](./pointf/)(const [SizeF](../sizef/)\&) | شیء جدیدی از نوع [PointF](./) می‌سازد و مقادیر مختصات X و Y آن را با مقادیر عرض و ارتفاع شیء [SizeF](../sizef/) مشخص‌شده به‌طور متناظر مقداردهی می‌کند. |
| void [set_X](./set_x/)(**float**) | مقدار مختصات X را که توسط شیء جاری نمایندگی می‌شود، تنظیم می‌کند. |
| void [set_Y](./set_y/)(**float**) | مقدار مختصات Y را که توسط شیء جاری نمایندگی می‌شود، تنظیم می‌کند. |
| static [PointF](./) [Subtract](./subtract/)(const [PointF](./)\&, const [SizeF](../sizef/)\&) | مقدارهای عرض و ارتفاع شیء [SizeF](../sizef/) مشخص‌شده را از مقادیر مختصات X و Y شیء [PointF](./) مشخص‌شده به‌طور متناظر کم می‌کند. |
| static [PointF](./) [Subtract](./subtract/)(const [PointF](./)\&, const [Size](../size/)\&) | مقدارهای عرض و ارتفاع شیء [Size](../size/) مشخص‌شده را از مقادیر مختصات X و Y شیء [PointF](./) مشخص‌شده به‌طور متناظر کم می‌کند. |
| [System::String](../../system/string/) [ToString](./tostring/)() const | نمایهٔ رشته‌ای جفت مقادیر مختصات X و Y که توسط شیء جاری نمایندگی می‌شود را برمی‌گرداند. |

## فیلدها

| Field | Description |
| --- | --- |
| static [Empty](./empty/) | یک نمونهٔ خالی از کلاس [PointF](./) که مقادیر مختصات X و Y آن برابر ۰ است. |

## موارد مرتبط

* فضای‌نام [System::Drawing](../)
* کتابخانه [Aspose.Slides](../../)