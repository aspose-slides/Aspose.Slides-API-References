---
title: Contains()
second_title: مرجع API Aspose.Slides برای C++
description: تشخیص می‌دهد آیا نقطهٔ مشخص شده در داخل مستطیلی که توسط شیء جاری نمایان شده است قرار دارد.
type: docs
weight: 248
url: /fa/system.drawing/rectangle/contains/
---
## Rectangle::Contains(int, int) const متد


تشخیص می‌دهد آیا نقطهٔ مشخص شده در داخل مستطیلی که توسط شیء جاری نشان داده می‌شود قرار دارد یا نه.

```cpp
bool System::Drawing::Rectangle::Contains(int x, int y) const
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| x | int | مختصات X نقطه برای بررسی |
| y | int | مختصات Y نقطه برای بررسی |

### مقدار بازگشت

True اگر نقطهٔ مشخص شده در مستطیلی که توسط شیء جاری نشان داده شده است قرار داشته باشد، در غیر این صورت - false

## Rectangle::Contains(const Point\&) const متد


تشخیص می‌دهد آیا نقطهٔ مشخص شده در داخل مستطیلی که توسط شیء جاری نشان داده می‌شود قرار دارد یا نه.

```cpp
bool System::Drawing::Rectangle::Contains(const Point &point) const
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| point | const [Point](../../point/)\& | نقطه‌ای برای بررسی |

### مقدار بازگشت

True اگر نقطهٔ مشخص شده در مستطیلی که توسط شیء جاری نشان داده شده است قرار داشته باشد، در غیر این صورت - false

## Rectangle::Contains(const Rectangle\&) const متد


تشخیص می‌دهد آیا مستطیل مشخص شده در داخل مستطیلی که توسط شیء جاری نشان داده می‌شود قرار دارد یا نه.

```cpp
bool System::Drawing::Rectangle::Contains(const Rectangle &rect) const
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| rect | const [Rectangle](../)\& | مستطیلی برای بررسی |

### مقدار بازگشت

True اگر مستطیل مشخص شده در مستطیلی که توسط شیء جاری نشان داده شده است قرار داشته باشد، در غیر این صورت - false

## موارد مرتبط

* کلاس [Rectangle](../)
* کلاس [Point](../../point/)
* فضای‌نام [System::Drawing](../../)
* کتابخانه [Aspose.Slides](../../../)