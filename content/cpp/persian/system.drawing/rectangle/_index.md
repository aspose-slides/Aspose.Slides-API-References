---
title: Rectangle
second_title: مرجع API Aspose.Slides برای C++
description: "نمایش ناحیهٔ مستطیلی از یک تصویر که به‌عنوان مختصات صحیح X و Y گوشهٔ بالا-چپ و عرض و ارتفاع آن تعریف می‌شود. این نوع باید روی پشته تخصیص یابد و به توابع به صورت مقدار یا ارجاع پاس شود. هرگز از کلاس System::SmartPtr برای مدیریت اشیای این نوع استفاده نکنید."
type: docs
weight: 235
url: /fa/system.drawing/rectangle/
---
## کلاس Rectangle

یک ناحیه مستطیلی از یک تصویر را که با مختصات صحیح X و Y گوشهٔ بالای چپ و عرض و ارتفاع آن تعریف می‌شود، نشان می‌دهد. این نوع باید در پشته تخصیص داده شود و به توابع به صورت مقدار یا ارجاع پاس داده شود. هرگز از کلاس [System::SmartPtr](../../system/smartptr/) برای مدیریت اشیاء این نوع استفاده نکنید.

```cpp
class Rectangle
```

## متدها

| متد | توضیح |
| --- | --- |
| static [Rectangle](./) [Ceiling](./ceiling/)(const [RectangleF](../rectanglef/)\&) | یک شیء [Rectangle](./) را از شیء [RectangleF](../rectanglef/) مشخص شده با گرد کردن مقادیر مکان و اندازهٔ شیء [RectangleF](../rectanglef/) به مقادیر صحیح بالاتر تولید می‌کند. |
| **bool** [Contains](./contains/)(int, int) const | تشخیص می‌دهد که آیا نقطهٔ مشخص شده درون مستطیلی که توسط شیء جاری نمایان شده است، قرار دارد یا نه. |
| **bool** [Contains](./contains/)(const [Point](../point/)\&) const | تشخیص می‌دهد که آیا نقطهٔ مشخص شده درون مستطیلی که توسط شیء جاری نمایان شده است، قرار دارد یا نه. |
| **bool** [Contains](./contains/)(const [Rectangle](./)\&) const | تشخیص می‌دهد که آیا مستطیل مشخص شده درون مستطیلی که توسط شیء جاری نمایان شده است، قرار دارد یا خیر. |
| **bool** [Equals](./equals/)(const [Rectangle](./)\&) const | تشخیص می‌دهد که آیا مستطیل‌های نمایان شده توسط شیء جاری و شیء مشخص شده یکسان هستند یا نه. |
| static [Rectangle](./) [FromLTRB](./fromltrb/)(int, int, int, int) | یک شیء جدید [Rectangle](./) ایجاد می‌کند که مستطیلی با مکان‌های لبهٔ مشخص شده را نشان می‌دهد. |
| int [get_Bottom](./get_bottom/)() const | مختصات y لبهٔ پایین مستطیلی که توسط شیء جاری نمایان شده است را برمی‌گرداند. |
| int [get_Height](./get_height/)() const | ارتفاع مستطیلی که توسط شیء جاری نمایان شده است را برمی‌گرداند. |
| **bool** [get_IsEmpty](./get_isempty/)() const | تشخیص می‌دهد که آیا مختصات X و Y گوشهٔ بالای چپ مستطیل نمایان شده توسط شیء جاری، همراه با عرض و ارتفاع آن، مقدار 0 دارند یا نه. |
| int [get_Left](./get_left/)() const | مختصات X لبهٔ چپ مستطیلی که توسط شیء جاری نمایان شده است را برمی‌گرداند. |
| [Point](../point/) [get_Location](./get_location/)() const | یک نمونه از کلاس [Point](../point/) را که مکان گوشهٔ بالای چپ مستطیل نمایان شده توسط شیء جاری را مشخص می‌کند، برمی‌گرداند. |
| int [get_Right](./get_right/)() const | مختصات X لبهٔ راست مستطیلی که توسط شیء جاری نمایان شده است را برمی‌گرداند. |
| [Size](../size/) [get_Size](./get_size/)() const | یک نمونه از کلاس [Size](../size/) را که عرض و ارتفاع مستطیل نمایان شده توسط شیء جاری را مشخص می‌کند، برمی‌گرداند. |
| int [get_Top](./get_top/)() const | مختصات Y لبهٔ بالای مستطیلی که توسط شیء جاری نمایان شده است را برمی‌گرداند. |
| int [get_Width](./get_width/)() const | عرض مستطیلی که توسط شیء جاری نمایان شده است را برمی‌گرداند. |
| int [get_X](./get_x/)() const | مختصات X گوشهٔ بالای چپ مستطیلی که توسط شیء جاری نمایان شده است را برمی‌گرداند. |
| int [get_Y](./get_y/)() const | مختصات Y گوشهٔ بالای چپ مستطیلی که توسط شیء جاری نمایان شده است را برمی‌گرداند. |
| int [GetHashCode](./gethashcode/)() const | یک کد هش از شیء جاری را برمی‌گرداند. |
| void [Inflate](./inflate/)(int, int) | عرض و ارتفاع مستطیل نمایان شده توسط شیء جاری را افزایش می‌دهد، در حالی که مکان مرکز هندسی مستطیل حفظ می‌شود. عرض و ارتفاع در هر دو جهت به مقدار مشخص شده افزایش می‌یابند. |
| void [Inflate](./inflate/)(const [Size](../size/)\&) | عرض و ارتفاع مستطیل نمایان شده توسط شیء جاری را افزایش می‌دهد، در حالی که مکان مرکز هندسی مستطیل حفظ می‌شود. عرض و ارتفاع به مقدارهای عرض و ارتفاع شیء اندازهٔ مشخص شده به طور متناظر در هر دو جهت افزایش می‌یابند. |
| static [Rectangle](./) [Inflate](./inflate/)(const [Rectangle](./)\&, int, int) | عرض و ارتفاع مستطیل نمایان شده توسط شیء مشخص شده را افزایش می‌دهد، در حالی که مکان مرکز هندسی مستطیل حفظ می‌شود. عرض و ارتفاع در هر دو جهت به مقادیر مشخص شده افزایش می‌یابند. |
| void [Intersect](./intersect/)(const [Rectangle](./)\&) | مستطیل نمایان شده توسط شیء جاری را با مستطیلی که از تقاطع آن با مستطیل نمایان شده توسط شیء مشخص شده به دست می‌آید، جایگزین می‌کند. |
| static [Rectangle](./) [Intersect](./intersect/)(const [Rectangle](./)\&, const [Rectangle](./)\&) | یک مستطیل که نتیجهٔ تقاطع مستطیل‌های مشخص شده است را برمی‌گرداند. |
| **bool** [IntersectsWith](./intersectswith/)(const [Rectangle](./)\&) | تشخیص می‌دهد که آیا مستطیل‌های نمایان شده توسط شیء جاری و شیء مشخص شده با هم تقاطع دارند یا نه. |
| void [Offset](./offset/)(const [Point](../point/)\&) | موقعیت مستطیل نمایان شده توسط شیء جاری را به مقدارهای مشخص شده جابجا می‌کند. |
| void [Offset](./offset/)(int, int) | موقعیت مستطیل نمایان شده توسط شیء جاری را به مقدارهای مشخص شده جابجا می‌کند. |
| [operator RectangleF](./operator_rectanglef/)() const | یک شیء [RectangleF](../rectanglef/) را که مستطیلی معادل مستطیل نمایان شده توسط شیء جاری است، برمی‌گرداند. |
| **bool** [operator!=](./operator_not_equal/)(std::nullptr_t) const | همیشه مقدار true را برمی‌گرداند. |
| **bool** [operator==](./operator_equal_equal/)(std::nullptr_t) const | همیشه مقدار false را برمی‌گرداند. |
| [Rectangle](./rectangle/)() | یک نمونهٔ جدید از شیء [Rectangle](./) ایجاد می‌کند که مستطیلی با مقادیر X و Y و عرض و ارتفاع برابر با 0 را نشان می‌دهد. |
| [Rectangle](./rectangle/)(int, int, int, int) | یک نمونهٔ جدید از شیء [Rectangle](./) می‌سازد که مستطیلی با مختصات مشخص شدهٔ گوشهٔ بالای چپ و عرض و ارتفاع دارد. |
| [Rectangle](./rectangle/)(const [Point](../point/)\&, const [Size](../size/)\&) | یک نمونهٔ جدید از شیء [Rectangle](./) می‌سازد که مستطیلی با مختصات گوشهٔ بالای چپ آن به عنوان یک نمونه از کلاس [Point](../point/) و عرض و ارتفاع آن به عنوان یک نمونه از کلاس [Size](../size/) مشخص می‌شود. |
| [Rectangle](./rectangle/)(const **System::Windows::Forms::Screen::Rectangle_**\&) | یک نمونهٔ جدید از شیء [Rectangle](./) می‌سازد که مستطیلی معادل مستطیل مشخص شده را نشان می‌دهد. |
| static [Rectangle](./) [Round](./round/)(const [RectangleF](../rectanglef/)\&) | یک شیء [Rectangle](./) را از شیء [RectangleF](../rectanglef/) مشخص شده با گرد کردن مقادیر مکان و اندازهٔ شیء [RectangleF](../rectanglef/) به نزدیک‌ترین عدد صحیح ایجاد می‌کند. |
| void [set_Height](./set_height/)(int) | ارتفاع مستطیل نمایان شده توسط شیء جاری را تنظیم می‌کند. |
| void [set_Location](./set_location/)([Point](../point/)) | موقعیت گوشهٔ بالای چپ مستطیلی که توسط شیء جاری نمایان شده است را تنظیم می‌کند. |
| void [set_Size](./set_size/)([Size](../size/)) | عرض و ارتفاع مستطیلی که توسط شیء جاری نمایان شده است را تنظیم می‌کند. |
| void [set_Width](./set_width/)(int) | عرض مستطیل نمایان شده توسط شیء جاری را تنظیم می‌کند. |
| void [set_X](./set_x/)(int) | مختصات X گوشهٔ بالای چپ مستطیلی که توسط شیء جاری نمایان شده است را تنظیم می‌کند. |
| void [set_Y](./set_y/)(int) | مختصات Y گوشهٔ بالای چپ مستطیلی که توسط شیء جاری نمایان شده است را تنظیم می‌کند. |
| [String](../../system/string/) [ToString](./tostring/)() const | نمایش متنی شیء جاری را برمی‌گرداند. |
| static [Rectangle](./) [Truncate](./truncate/)(const [RectangleF](../rectanglef/)\&) | یک شیء [Rectangle](./) را از شیء [RectangleF](../rectanglef/) مشخص شده با کوتاه کردن مقادیر مکان و اندازهٔ شیء [RectangleF](../rectanglef/) به مقدار صحیح پایین‌تر ایجاد می‌کند. |
| static [Rectangle](./) [Union](./union/)(const [Rectangle](./)\&, const [Rectangle](./)\&) | یک مستطیل که نتیجهٔ اتحاد مستطیل‌های مشخص شده است را برمی‌گرداند. |

## فیلدها

| فیلد | توضیح |
| --- | --- |
| static [Empty](./empty/) | یک مستطیل خالی؛ یعنی مستطیلی که مقادیر مکان و اندازهٔ آن صفر هستند. |

## مراجع

* فضای نام [System::Drawing](../)
* کتابخانه [Aspose.Slides](../../)