---
title: SizeF
second_title: Aspose.Slides برای مرجع API C++
description: "یک جفت مقدار شناور تک‌دقت که عرض و ارتفاع یک تصویر را نشان می‌دهند. این نوع باید بر روی پشته تخصیص یابد و به توابع به صورت مقدار یا ارجاع پاس داده شود. هرگز از کلاس System::SmartPtr برای مدیریت اشیاء این نوع استفاده نکنید."
type: docs
weight: 287
url: /fa/system.drawing/sizef/
---
## SizeF کلاس

یک جفت مقدار شناور تک‌دقت را که عرض و ارتفاع یک تصویر را نشان می‌دهند، نمایش می‌دهد. این نوع باید بر روی پشته اختصاص داده شود و به توابع به صورت مقدار یا ارجاع پاس داده شود. هرگز از کلاس [System::SmartPtr](../../system/smartptr/) برای مدیریت اشیاء این نوع استفاده نکنید.

```cpp
class SizeF
```

## متدها

| متد | توضیح |
| --- | --- |
| static [SizeF](./) [Add](./add/)(const [SizeF](./)\&, const [SizeF](./)\&) | یک شی جدید [SizeF](./) را بر می‌گرداند که مجموع اشیاء [SizeF](./) مشخص‌شده است، یعنی مقداری که عرض آن برابر با مجموع مقادیر عرض اشیاء مشخص‌شده و ارتفاع آن برابر با مجموع مقادیر ارتفاع اشیاء مشخص‌شده است. |
| **bool** [Equals](./equals/)(const [SizeF](./)\&) const | تعیین می‌کند که آیا شی جاری و شی مشخص‌شده برابر هستند، یعنی همان جفت مقادیر عرض و ارتفاع را نشان می‌دهند. |
| **float** [get_Height](./get_height/)() const | مقدار ارتفاعی که توسط شی جاری نشان داده می‌شود را بر می‌گرداند. |
| **bool** [get_IsEmpty](./get_isempty/)() const | تعیین می‌کند که آیا هر دو مقدار عرض و ارتفاع برابر با صفر هستند. |
| **float** [get_Width](./get_width/)() const | مقدار عرضی که توسط شی جاری نشان داده می‌شود را بر می‌گرداند. |
| **int32_t** [GetHashCode](./gethashcode/)() const | یک کد هش برای شی جاری بر می‌گرداند. |
| [operator PointF](./operator_pointf/)() const | شی جاری را به یک نمونه از شی [Point](../point/) تبدیل می‌کند به‌طوری که مختصات X و Y آن به‌صورت متناسب با مقادیر عرض و ارتفاع شی جاری مقداردهی شوند. |
| [SizeF](./)\& [operator+=](./operator_plus_equal/)(const [SizeF](./)\&) | مقادیر عرض و ارتفاع شی [SizeF](./) مشخص‌شده را به‌صورت متناسب به مقادیر عرض و ارتفاع شی جاری [SizeF](./) اضافه می‌کند. |
| void [set_Height](./set_height/)(**float**) | مقدار ارتفاعی که توسط شی جاری نشان داده می‌شود را تنظیم می‌کند. |
| void [set_Width](./set_width/)(**float**) | مقدار عرضی که توسط شی جاری نشان داده می‌شود را تنظیم می‌کند. |
| [SizeF](./sizef/)() | یک شی جدید [SizeF](./) ایجاد می‌کند و مقادیر عرض و ارتفاع آن را با 0 مقداردهی می‌کند. |
| [SizeF](./sizef/)(const [PointF](../pointf/)\&) | یک شی جدید [SizeF](./) ایجاد می‌کند و مقادیر عرض و ارتفاع آن را به‌صورت متناسب با مقادیر مختصات X و Y نقطه مشخص‌شده مقداردهی می‌کند. |
| [SizeF](./sizef/)(**float**, **float**) | یک شی جدید [SizeF](./) ایجاد می‌کند و آن را با مقدار مشخص‌شده مقداردهی می‌کند. |
| static [SizeF](./) [Subtract](./subtract/)(const [SizeF](./)\&, const [SizeF](./)\&) | یک شی جدید [SizeF](./) را بر می‌گرداند که نتیجهٔ تفریق **size2** از **size1** است، یعنی مقداری که عرض آن نتیجهٔ تفریق مقدار عرض **size2** از مقدار عرض **size1** و ارتفاع آن نتیجهٔ تفریق مقدار ارتفاع **size2** از مقدار ارتفاع **size1** می‌باشد. |
| [PointF](../pointf/) [ToPointF](./topointf/)() const | شی جاری را به یک نمونه از شی [Point](../point/) تبدیل می‌کند به‌طوری که مختصات X و Y آن با مقادیر عرض و ارتفاع شی جاری مقداردهی شوند. |
| [Size](../size/) [ToSize](./tosize/)() const | یک شی [Size](../size/) را از شی جاری [SizeF](./) ایجاد می‌کند با کاهش مقادیر عرض و ارتفاع شی [SizeF](./) به نزدیک‌ترین مقدار صحیح پایین‌تر. |
| [System::String](../../system/string/) [ToString](./tostring/)() const | نمایش رشته‌ای جفت مقادیر عرض و ارتفاع را که توسط شی جاری نشان داده می‌شود بر می‌گرداند. |

## فیلدها

| فیلد | توضیح |
| --- | --- |
| static [Empty](./empty/) | یک نمونهٔ خالی از کلاس [SizeF](./) که مقادیر عرض و ارتفاع آن ۰ هستند. |

## موارد مرتبط

* فضای نام [System::Drawing](../)
* کتابخانه [Aspose.Slides](../../)