---
title: "System::Drawing"
second_title: Aspose.Slides برای مرجع API C++
description: 
type: docs
weight: 482
url: /fa/system.drawing/
---
## کلاس‌ها

| کلاس | توضیح |
| --- | --- |
| [Bitmap](./bitmap/) | یک تصویر بیتی GDI+ را نشان می‌دهد. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را در پشته یا با استفاده از operator new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا می‌شود. همیشه این کلاس را در [System::SmartPtr](../system/smartptr/) اشاره‌گر بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [Brush](./brush/) | یک کلاس پایه برای کلاس‌هایی است که پرکننده‌های استفاده‌شده برای پر کردن داخلی شکل‌های گرافیکی را نشان می‌دهند. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را در پشته یا با استفاده از operator new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا می‌شود. همیشه این کلاس را در [System::SmartPtr](../system/smartptr/) اشاره‌گر بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [Brushes](./brushes/) | مجموعه‌ای از اشیاء پیش‌ساخته [SolidBrush](./solidbrush/) را فراهم می‌کند. این یک نوع ایستا بدون سرویس‌های نمونه است. شما هرگز نباید به هیچ وسیله‌ای نمونه‌ای از آن ایجاد کنید. |
| [CharacterRange](./characterrange/) | یک بازه از موقعیت‌های کاراکتر در یک رشته را نشان می‌دهد. این نوع باید در پشته تخصیص یابد و به توابع به صورت مقدار یا ارجاع پاس داده شود. هرگز از کلاس [System::SmartPtr](../system/smartptr/) برای مدیریت اشیاء این نوع استفاده نکنید. |
| [Color](./color/) | یک رنگ را نشان می‌دهد. این نوع باید در پشته تخصیص یابد و به توابع به صورت مقدار یا ارجاع پاس داده شود. هرگز از کلاس [System::SmartPtr](../system/smartptr/) برای مدیریت اشیاء این نوع استفاده نکنید. |
| [ColorTranslator](./colortranslator/) | ترجمه‌های رنگ را انجام می‌دهد. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را در پشته یا با استفاده از operator new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا می‌شود. همیشه این کلاس را در [System::SmartPtr](../system/smartptr/) اشاره‌گر بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [Font](./font/) | یک قالب خاص برای متن شامل صورت‌خط، اندازه و سبک را نشان می‌دهد. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را در پشته یا با استفاده از operator new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا می‌شود. همیشه این کلاس را در [System::SmartPtr](../system/smartptr/) اشاره‌گر بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [FontConverter](./fontconverter/) | اشیاء [Font](./font/) را از یک نوع داده به نوع دیگر تبدیل می‌کند. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را در پشته یا با استفاده از operator new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا می‌شود. همیشه این کلاس را در [System::SmartPtr](../system/smartptr/) اشاره‌گر بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [FontFamily](./fontfamily/) | یک گروه از نوع چهره‌ها که طراحی پایه مشابهی دارند را نشان می‌دهد. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را در پشته یا با استفاده از operator new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا می‌شود. همیشه این کلاس را در [System::SmartPtr](../system/smartptr/) اشاره‌گر بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [Graphics](./graphics/) | یک سطح رسم را نشان می‌دهد. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را در پشته یا با استفاده از operator new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا می‌شود. همیشه این کلاس را در [System::SmartPtr](../system/smartptr/) اشاره‌گر بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [Icon](./icon/) | یک نماد [Windows](../system.windows/) را نشان می‌دهد. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را در پشته یا با استفاده از operator new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا می‌شود. همیشه این کلاس را در [System::SmartPtr](../system/smartptr/) اشاره‌گر بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [Image](./image/) | یک کلاس پایه برای کلاس‌های [System::Drawing::Bitmap](./bitmap/) و System::Drawing::Metafile که عملکرد پایه را فراهم می‌کنند. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را در پشته یا با استفاده از operator new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا می‌شود. همیشه این کلاس را در [System::SmartPtr](../system/smartptr/) اشاره‌گر بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [ImageConverter](./imageconverter/) | اشیاء [Image](./image/) را از یک نوع داده به نوع دیگر تبدیل می‌کند. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را در پشته یا با استفاده از operator new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا می‌شود. همیشه این کلاس را در [System::SmartPtr](../system/smartptr/) اشاره‌گر بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [ImageFormatConverter](./imageformatconverter/) | اشیاء ImageFormat را از یک نوع داده به نوع دیگر تبدیل می‌کند. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را در پشته یا با استفاده از operator new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا می‌شود. همیشه این کلاس را در [System::SmartPtr](../system/smartptr/) اشاره‌گر بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [Pen](./pen/) | خواصی مانند رنگ، عرض و غیره خطوط و منحنی‌های ترسیم‌شده را نشان می‌دهد. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را در پشته یا با استفاده از operator new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا می‌شود. همیشه این کلاس را در [System::SmartPtr](../system/smartptr/) اشاره‌گر بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [Pens](./pens/) | مجموعه‌ای از اشیاء پیش‌ساخته [Pen](./pen/) را فراهم می‌کند. این یک نوع ایستا بدون سرویس‌های نمونه است. شما هرگز نباید به هیچ وسیله‌ای نمونه‌ای از آن ایجاد کنید. |
| [Point](./point/) | یک جفت مختصات صحیح X و Y یک نقطه روی صفحهٔ دو-بعدی را نشان می‌دهد. این نوع باید در پشته تخصیص یابد و به توابع به صورت مقدار یا ارجاع پاس داده شود. هرگز از کلاس [System::SmartPtr](../system/smartptr/) برای مدیریت اشیاء این نوع استفاده نکنید. |
| [PointF](./pointf/) | یک جفت مختصات نقطهٔ X و Y با دقت تک‌نقطه‌ای شناور یک نقطه روی صفحهٔ دو-بعدی را نشان می‌دهد. این نوع باید در پشته تخصیص یابد و به توابع به صورت مقدار یا ارجاع پاس داده شود. هرگز از کلاس [System::SmartPtr](../system/smartptr/) برای مدیریت اشیاء این نوع استفاده نکنید. |
| [Rectangle](./rectangle/) | یک ناحیهٔ مستطیلی از تصویر را که به عنوان مختصات صحیح X و Y گوشهٔ بالا-چپ و عرض و ارتفاع آن تعریف شده است، نشان می‌دهد. این نوع باید در پشته تخصیص یابد و به توابع به صورت مقدار یا ارجاع پاس داده شود. هرگز از کلاس [System::SmartPtr](../system/smartptr/) برای مدیریت اشیاء این نوع استفاده نکنید. |
| [RectangleF](./rectanglef/) | یک ناحیهٔ مستطیلی از تصویر را که به عنوان مختصات نقطهٔ شناور تک‌نقطه‌ای X و Y گوشهٔ بالا-چپ و عرض و ارتفاع آن تعریف شده است، نشان می‌دهد. این نوع باید در پشته تخصیص یابد و به توابع به صورت مقدار یا ارجاع پاس داده شود. هرگز از کلاس [System::SmartPtr](../system/smartptr/) برای مدیریت اشیاء این نوع استفاده نکنید. |
| [Region](./region/) | داخل یک شکل گرافیکی را نشان می‌دهد. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را در پشته یا با استفاده از operator new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا می‌شود. همیشه این کلاس را در [System::SmartPtr](../system/smartptr/) اشاره‌گر بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [Size](./size/) | یک جفت مقدار صحیح که عرض و ارتفاع یک تصویر را نشان می‌دهند، را نشان می‌دهد. این نوع باید در پشته تخصیص یابد و به توابع به صورت مقدار یا ارجاع پاس داده شود. هرگز از کلاس [System::SmartPtr](../system/smartptr/) برای مدیریت اشیاء این نوع استفاده نکنید. |
| [SizeF](./sizef/) | یک جفت مقدار شناور تک‌نقطه‌ای که عرض و ارتفاع یک تصویر را نشان می‌دهد، را نشان می‌دهد. این نوع باید در پشته تخصیص یابد و به توابع به صورت مقدار یا ارجاع پاس داده شود. هرگز از کلاس [System::SmartPtr](../system/smartptr/) برای مدیریت اشیاء این نوع استفاده نکنید. |
| [SolidBrush](./solidbrush/) | یک قلم‌مو با یک رنگ را نشان می‌دهد. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را در پشته یا با استفاده از operator new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا می‌شود. همیشه این کلاس را در [System::SmartPtr](../system/smartptr/) اشاره‌گر بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [StringFormat](./stringformat/) | اطلاعات چینش متن، دستکاری‌های نمایش و ویژگی‌های OpenType را در بر می‌گیرد. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را در پشته یا با استفاده از operator new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا می‌شود. همیشه این کلاس را در [System::SmartPtr](../system/smartptr/) اشاره‌گر بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [SystemColors](./systemcolors/) | کلاسی که مجموعه‌ای از اشیاء پیش‌ساخته [Color](./color/) را که نمایشگرهای [Windows](../system.windows/) رنگشان را نشان می‌دهند، فراهم می‌کند. این یک نوع ایستا بدون سرویس‌های نمونه است. شما هرگز نباید به هیچ وسیله‌ای نمونه‌ای از آن ایجاد کنید. |
| [SystemFonts](./systemfonts/) | یک مجموعه از اشیاء پیش‌ساخته [Font](./font/) را که قلم‌های مورد استفاده برای نمایش متن در عناصر نمایش [Windows](../system.windows/) را نشان می‌دهند، فراهم می‌کند. این یک نوع ایستا بدون سرویس‌های نمونه است. شما هرگز نباید به هیچ وسیله‌ای نمونه‌ای از آن ایجاد کنید. |
| [TextureBrush](./texturebrush/) | یک قلم‌مو که از یک تصویر برای پر کردن داخلی یک شکل استفاده می‌کند، را نشان می‌دهد. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را در پشته یا با استفاده از operator new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا می‌شود. همیشه این کلاس را در [System::SmartPtr](../system/smartptr/) اشاره‌گر بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
## توابع

| تابع | توضیح |
| --- | --- |
| std::ostream\& [operator<<](./operator_less_less/)(std::ostream\&, [Color](./color/)) | داده‌ها را با استفاده از رمزگذاری UTF-8 به جریان وارد می‌کند. |
| std::wostream\& [operator<<](./operator_less_less/)(std::wostream\&, [Color](./color/)) | داده‌ها را به جریان وارد می‌کند. |
| std::ostream\& [operator<<](./operator_less_less/)(std::ostream\&, const [Point](./point/)\&) | داده‌ها را با استفاده از رمزگذاری UTF-8 به جریان وارد می‌کند. |
| std::wostream\& [operator<<](./operator_less_less/)(std::wostream\&, const [Point](./point/)\&) | داده‌ها را به جریان وارد می‌کند. |
| std::ostream\& [operator<<](./operator_less_less/)(std::ostream\&, const [PointF](./pointf/)\&) | داده‌ها را با استفاده از رمزگذاری UTF-8 به جریان وارد می‌کند. |
| std::wostream\& [operator<<](./operator_less_less/)(std::wostream\&, const [PointF](./pointf/)\&) | داده‌ها را به جریان وارد می‌کند. |
| std::ostream\& [operator<<](./operator_less_less/)(std::ostream\&, const [Rectangle](./rectangle/)\&) | داده‌ها را با استفاده از رمزگذاری UTF-8 به جریان وارد می‌کند. |
| std::wostream\& [operator<<](./operator_less_less/)(std::wostream\&, const [Rectangle](./rectangle/)\&) | داده‌ها را به جریان وارد می‌کند. |
| std::ostream\& [operator<<](./operator_less_less/)(std::ostream\&, const [RectangleF](./rectanglef/)\&) | داده‌ها را با استفاده از رمزگذاری UTF-8 به جریان وارد می‌کند. |
| std::wostream\& [operator<<](./operator_less_less/)(std::wostream\&, const [RectangleF](./rectanglef/)\&) | داده‌ها را به جریان وارد می‌کند. |
## شمارش‌ها

| شمارش | توضیح |
| --- | --- |
| [ContentAlignment](./contentalignment/) | تطبیق محتوا را مشخص می‌کند. |
| [CopyPixelOperation](./copypixeloperation/) | نحوه ترکیب رنگ منبع در عملیات کپی پیکسل با رنگ مقصد برای تولید رنگ نهایی را مشخص می‌کند. |
| [FontStyle](./fontstyle/) | یک سبک قلم را نشان می‌دهد. |
| [GraphicsUnit](./graphicsunit/) | یک واحد اندازه‌گیری را نشان می‌دهد. |
| [KnownColor](./knowncolor/) | رنگ‌های شناخته‌شدهٔ سیستم را مشخص می‌کند. |
| [RotateFlipType](./rotatefliptype/) | نوع عملیات چرخش و/یا وارونه‌سازی را مشخص می‌کند. |
| [StringAlignment](./stringalignment/) | تطبیق رشته نسبت به مستطیل چینش آن را مشخص می‌کند. |
| [StringDigitSubstitute](./stringdigitsubstitute/) | نحوه جایگزینی ارقام در یک رشته بر اساس بومی یا زبان را مشخص می‌کند. |
| [StringFormatFlags](./stringformatflags/) | اطلاعات نمایش و چینش برای رشته‌های متن را مشخص می‌کند. |
| [StringTrimming](./stringtrimming/) | نحوه حذف کاراکترها از یک رشته‌ای که در شکل چینش جا نمی‌شود را مشخص می‌کند. |