---
title: Complement()
second_title: Aspose.Slides برای C++ - مرجع API
description: ناحیه‌ای که توسط شیء فعلی نمایان شده است را با بخشی از ناحیه‌ای که توسط مستطیل مشخص شده تعریف می‌شود و با این ناحیه تقاطع ندارد، جایگزین می‌کند.
type: docs
weight: 131
url: /fa/system.drawing/region/complement/
---
## Region::Complement(const RectangleF\&) متد

ناحیه‌ای را که توسط شیء فعلی نمایان شده است، با بخشی از ناحیه‌ای که توسط مستطیل مشخص شده تعریف می‌شود و با این ناحیه تقاطع ندارد، جایگزین می‌کند.

```cpp
void System::Drawing::Region::Complement(const RectangleF &rect)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| rect | const [RectangleF](../../rectanglef/)\& | مستطیلی که ناحیه‌ای برای مکمل تعریف می‌کند |

## Region::Complement(const Rectangle\&) متد

ناحیه‌ای را که توسط شیء فعلی نمایان شده است، با بخشی از ناحیه‌ای که توسط مستطیل مشخص شده تعریف می‌شود و با این ناحیه تقاطع ندارد، جایگزین می‌کند.

```cpp
void System::Drawing::Region::Complement(const Rectangle &rect)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| rect | const [Rectangle](../../rectangle/)\& | مستطیلی که ناحیه‌ای برای مکمل تعریف می‌کند |

## Region::Complement(const SharedPtr\<Drawing2D::GraphicsPath\>\&) متد

ناحیه‌ای را که توسط شیء فعلی نمایان شده است، با بخشی از ناحیه‌ای که توسط مسیر مشخص شده تعریف می‌شود و با این ناحیه تقاطع ندارد، جایگزین می‌کند.

```cpp
void System::Drawing::Region::Complement(const SharedPtr<Drawing2D::GraphicsPath> &path)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| path | const [SharedPtr](../../../system/sharedptr/)\<[Drawing2D::GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)\>\& | مسیری که ناحیه‌ای برای مکمل تعریف می‌کند |

## Region::Complement(const SharedPtr\<Region\>\&) متد

ناحیه‌ای را که توسط شیء فعلی نمایان شده است، با بخشی از ناحیه‌ای که توسط ناحیه مشخص شده تعریف می‌شود و با این ناحیه تقاطع ندارد، جایگزین می‌کند.

```cpp
void System::Drawing::Region::Complement(const SharedPtr<Region> &region)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| region | const [SharedPtr](../../../system/sharedptr/)\<[Region](../)\>\& | ناحیه‌ای برای مکمل |

## موارد مرتبط

* Typedef [SharedPtr](../../../system/sharedptr/)
* کلاس [RectangleF](../../rectanglef/)
* کلاس [Region](../)
* کلاس [Rectangle](../../rectangle/)
* کلاس [GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)
* فضای‌نام [System::Drawing](../../)
* کتابخانه [Aspose.Slides](../../../)