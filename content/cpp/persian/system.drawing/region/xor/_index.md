---
title: Xor()
second_title: مرجع API Aspose.Slides برای C++
description: منطقه‌ای که توسط شیء جاری نمایان شده است را با بخش‌های این منطقه و منطقه‌ای که توسط مستطیل مشخص‌شده تعریف می‌شود و تقاطع ندارند، جایگزین می‌کند.
type: docs
weight: 144
url: /fa/system.drawing/region/xor/
---
## Region::Xor(const RectangleF\&) متد

منطقه‌ای که توسط شیء جاری نمایان شده است را با بخش‌های این منطقه و منطقه‌ای که توسط مستطیل مشخص‌شده تعریف می‌شود و تقاطع ندارند، جایگزین می‌کند.

```cpp
void System::Drawing::Region::Xor(const RectangleF &rect)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| rect | const [RectangleF](../../rectanglef/)\& | مستطیلی که یک منطقه را برای اعمال عمل xor با منطقهٔ نمایان‌شده توسط شیء جاری تعریف می‌کند |

## Region::Xor(const Rectangle\&) متد

منطقه‌ای که توسط شیء جاری نمایان شده است را با بخش‌های این منطقه و منطقه‌ای که توسط مستطیل مشخص‌شده تعریف می‌شود و تقاطع ندارند، جایگزین می‌کند.

```cpp
void System::Drawing::Region::Xor(const Rectangle &rect)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| rect | const [Rectangle](../../rectangle/)\& | مستطیلی که یک منطقه را برای اعمال عمل xor با منطقهٔ نمایان‌شده توسط شیء جاری تعریف می‌کند |

## Region::Xor(const SharedPtr\<Drawing2D::GraphicsPath\>\&) متد

منطقه‌ای که توسط شیء جاری نمایان شده است را با بخش‌های این منطقه و مسیری که توسط مسیر مشخص‌شده تعریف می‌شود و تقاطع ندارند، جایگزین می‌کند.

```cpp
void System::Drawing::Region::Xor(const SharedPtr<Drawing2D::GraphicsPath> &path)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| path | const [SharedPtr](../../../system/sharedptr/)\<[Drawing2D::GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)\>\& | مسیری که یک منطقه را برای اعمال عمل xor با منطقهٔ نمایان‌شده توسط شیء جاری تعریف می‌کند |

## Region::Xor(const SharedPtr\<Region\>\&) متد

منطقه‌ای که توسط شیء جاری نمایان شده است را با بخش‌های این منطقه و منطقهٔ مشخص‌شده که تقاطع ندارند، جایگزین می‌کند.

```cpp
void System::Drawing::Region::Xor(const SharedPtr<Region> &region)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| region | const [SharedPtr](../../../system/sharedptr/)\<[Region](../)\>\& | منطقه‌ای برای اعمال عمل xor با منطقهٔ نمایان‌شده توسط شیء جاری |

## موارد مرتبط

* Typedef [SharedPtr](../../../system/sharedptr/)
* کلاس [RectangleF](../../rectanglef/)
* کلاس [Region](../)
* کلاس [Rectangle](../../rectangle/)
* کلاس [GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)
* فضای‌نام [System::Drawing](../../)
* کتابخانه [Aspose.Slides](../../../)