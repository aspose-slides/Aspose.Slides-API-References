---
title: Intersect()
second_title: مرجع API Aspose.Slides برای C++
description: منطقه‌ای که توسط شیء جاری نمایان شده است را با نتیجهٔ اشتراک این منطقه و منطقه‌ای که توسط مستطیل مشخص‌شده تعریف شده است، جایگزین می‌کند.
type: docs
weight: 79
url: /fa/system.drawing/region/intersect/
---
## Region::Intersect(const RectangleF\&) متد

منطقه‌ای که توسط شیء جاری نمایان شده است را با نتیجهٔ اشتراک این منطقه و منطقه‌ای که توسط مستطیل مشخص‌شده تعریف شده است، جایگزین می‌کند.

```cpp
void System::Drawing::Region::Intersect(const RectangleF &rect)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| rect | const [RectangleF](../../rectanglef/)\& | مستطیلی که منطقه‌ای برای اشتراک این منطقه با آن تعریف می‌کند |

## Region::Intersect(const Rectangle\&) متد

منطقه‌ای که توسط شیء جاری نمایان شده است را با نتیجهٔ اشتراک این منطقه و منطقه‌ای که توسط مستطیل مشخص‌شده تعریف شده است، جایگزین می‌کند.

```cpp
void System::Drawing::Region::Intersect(const Rectangle &rect)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| rect | const [Rectangle](../../rectangle/)\& | مستطیلی که منطقه‌ای برای اشتراک این منطقه با آن تعریف می‌کند |

## Region::Intersect(const SharedPtr\<Drawing2D::GraphicsPath\>\&) متد

منطقه‌ای که توسط شیء جاری نمایان شده است را با نتیجهٔ اشتراک این منطقه و منطقه‌ای که توسط مسیر مشخص‌شده تعریف شده است، جایگزین می‌کند.

```cpp
void System::Drawing::Region::Intersect(const SharedPtr<Drawing2D::GraphicsPath> &path)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| path | const [SharedPtr](../../../system/sharedptr/)\<[Drawing2D::GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)\>\& | مسیری که منطقه‌ای برای اشتراک این منطقه با آن تعریف می‌کند |

## Region::Intersect(const SharedPtr\<Region\>\&) متد

منطقه‌ای که توسط شیء جاری نمایان شده است را با نتیجهٔ اشتراک این منطقه و منطقهٔ مشخص‌شده، جایگزین می‌کند.

```cpp
void System::Drawing::Region::Intersect(const SharedPtr<Region> &region)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| region | const [SharedPtr](../../../system/sharedptr/)\<[Region](../)\>\& | منطقه‌ای برای اشتراک این منطقه با آن |

## موارد مرتبط

* Typedef [SharedPtr](../../../system/sharedptr/)
* کلاس [RectangleF](../../rectanglef/)
* کلاس [Region](../)
* کلاس [Rectangle](../../rectangle/)
* کلاس [GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)
* فضای نام [System::Drawing](../../)
* Library [Aspose.Slides](../../../)