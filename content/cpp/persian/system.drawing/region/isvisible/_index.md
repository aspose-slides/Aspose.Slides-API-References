---
title: IsVisible()
second_title: Aspose.Slides برای C++ مرجع API
description: تعیین می‌کند آیا نقطهٔ مشخص شده در ناحیه‌ای که توسط شیء فعلی نمایانده می‌شود، قرار دارد یا نه.
type: docs
weight: 196
url: /fa/system.drawing/region/isvisible/
---
## Region::IsVisible(const Point\&) const متد

تعیین می‌کند آیا نقطهٔ مشخص شده در ناحیه‌ای که توسط شیء فعلی نمایانده می‌شود، قرار دارد یا نه.

```cpp
bool System::Drawing::Region::IsVisible(const Point &point) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| point | const [Point](../../point/)\& | نقطه برای بررسی |

## Region::IsVisible(const PointF\&) const متد

تعیین می‌کند آیا نقطهٔ مشخص شده در ناحیه‌ای که توسط شیء فعلی نمایانده می‌شود، قرار دارد یا نه.

```cpp
bool System::Drawing::Region::IsVisible(const PointF &point) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| point | const [PointF](../../pointf/)\& | نقطه برای بررسی |

## Region::IsVisible(const Rectangle\&) متد

تعیین می‌کند آیا بخشی از مستطیل مشخص شده در ناحیه‌ای که توسط شیء فعلی نمایانده می‌شود، قرار دارد یا نه.

```cpp
bool System::Drawing::Region::IsVisible(const Rectangle &rect)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| rect | const [Rectangle](../../rectangle/)\& | مستطیل برای بررسی |

## Region::IsVisible(const RectangleF\&) متد

تعیین می‌کند آیا بخشی از مستطیل مشخص شده در ناحیه‌ای که توسط شیء فعلی نمایانده می‌شود، قرار دارد یا نه.

```cpp
bool System::Drawing::Region::IsVisible(const RectangleF &rect)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| rect | const [RectangleF](../../rectanglef/)\& | مستطیل برای بررسی |

## Region::IsVisible(const Point\&, const SharedPtr\<Graphics\>\&) const متد

تعیین می‌کند آیا نقطهٔ مشخص شده در ناحیه‌ای که توسط شیء فعلی نمایانده می‌شود، با استفاده از گرافیک مشخص شده، قرار دارد یا نه.

```cpp
bool System::Drawing::Region::IsVisible(const Point &point, const SharedPtr<Graphics> &graphics) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| point | const [Point](../../point/)\& | نقطه برای بررسی |
| graphics | const [SharedPtr](../../../system/sharedptr/)\<[Graphics](../../graphics/)\>\& | متن گرافیکی |

## Region::IsVisible(const PointF\&, const SharedPtr\<Graphics\>\&) const متد

تعیین می‌کند آیا نقطهٔ مشخص شده در ناحیه‌ای که توسط شیء فعلی نمایانده می‌شود، با استفاده از گرافیک مشخص شده، قرار دارد یا نه.

```cpp
bool System::Drawing::Region::IsVisible(const PointF &point, const SharedPtr<Graphics> &graphics) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| point | const [PointF](../../pointf/)\& | نقطه برای بررسی |
| graphics | const [SharedPtr](../../../system/sharedptr/)\<[Graphics](../../graphics/)\>\& | متن گرافیکی |

## Region::IsVisible(const Rectangle\&, const SharedPtr\<Graphics\>\&) متد

تعیین می‌کند آیا بخشی از مستطیل مشخص شده در ناحیه‌ای که توسط شیء فعلی نمایانده می‌شود، با استفاده از گرافیک مشخص شده، قرار دارد یا نه.

```cpp
bool System::Drawing::Region::IsVisible(const Rectangle &rect, const SharedPtr<Graphics> &graphics)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| rect | const [Rectangle](../../rectangle/)\& | مستطیل برای بررسی |
| graphics | const [SharedPtr](../../../system/sharedptr/)\<[Graphics](../../graphics/)\>\& | متن گرافیکی |

## Region::IsVisible(const RectangleF\&, const SharedPtr\<Graphics\>\&) متد

تعیین می‌کند آیا بخشی از مستطیل مشخص شده در ناحیه‌ای که توسط شیء فعلی نمایانده می‌شود، با استفاده از گرافیک مشخص شده، قرار دارد یا نه.

```cpp
bool System::Drawing::Region::IsVisible(const RectangleF &rect, const SharedPtr<Graphics> &graphics)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| rect | const [RectangleF](../../rectanglef/)\& | مستطیل برای بررسی |
| graphics | const [SharedPtr](../../../system/sharedptr/)\<[Graphics](../../graphics/)\>\& | متن گرافیکی |

## Region::IsVisible(float, float) const متد

تعیین می‌کند آیا نقطهٔ مشخص شده در ناحیه‌ای که توسط شیء فعلی نمایانده می‌شود، قرار دارد یا نه.

```cpp
bool System::Drawing::Region::IsVisible(float x, float y) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| x | **float** | مختصات X نقطه برای بررسی |
| y | **float** | مختصات Y نقطه برای بررسی |

## Region::IsVisible(float, float, const SharedPtr\<Graphics\>\&) const متد

تعیین می‌کند آیا نقطهٔ مشخص شده در ناحیه‌ای که توسط شیء فعلی نمایانده می‌شود، با استفاده از گرافیک مشخص شده، قرار دارد یا نه.

```cpp
bool System::Drawing::Region::IsVisible(float x, float y, const SharedPtr<Graphics> &graphics) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| x | **float** | مختصات X نقطه برای بررسی |
| y | **float** | مختصات Y نقطه برای بررسی |
| graphics | const [SharedPtr](../../../system/sharedptr/)\<[Graphics](../../graphics/)\>\& | متن گرافیکی |

## See Also

* Typedef [SharedPtr](../../../system/sharedptr/)
* کلاس [Point](../../point/)
* کلاس [Region](../)
* کلاس [PointF](../../pointf/)
* کلاس [Rectangle](../../rectangle/)
* کلاس [RectangleF](../../rectanglef/)
* کلاس [Graphics](../../graphics/)
* فضای‌نام [System::Drawing](../../)
* کتابخانه [Aspose.Slides](../../../)