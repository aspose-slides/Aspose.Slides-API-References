---
title: Union()
second_title: مرجع API Aspose.Slides برای C++
description: منطقه‌ای که توسط شیء جاری نمایانده شده است را با نتیجهٔ عمل الحاق این منطقه و منطقه‌ای که توسط مستطیل مشخص‌شده تعریف شده است، جایگزین می‌کند.
type: docs
weight: 53
url: /fa/system.drawing/region/union/
---
## Region::Union(const RectangleF\&) متد

منطقه‌ای که توسط شیء جاری نشان داده می‌شود را با نتیجهٔ عملیات الحاق این منطقه و منطقه‌ای که توسط مستطیل مشخص‌شده تعریف شده است، جایگزین می‌کند.

```cpp
void System::Drawing::Region::Union(const RectangleF &rect)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| rect | const [RectangleF](../../rectanglef/)\& | یک مستطیل که منطقه‌ای را تعریف می‌کند تا این منطقه با آن متحد شود |

## Region::Union(const Rectangle\&) متد

منطقه‌ای که توسط شیء جاری نشان داده می‌شود را با نتیجهٔ الحاق این منطقه و منطقه‌ای که توسط مستطیل مشخص‌شده تعریف شده است، جایگزین می‌کند.

```cpp
void System::Drawing::Region::Union(const Rectangle &rect)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| rect | const [Rectangle](../../rectangle/)\& | یک مستطیل که منطقه‌ای را تعریف می‌کند تا این منطقه با آن متحد شود |

## Region::Union(const SharedPtr\<Drawing2D::GraphicsPath\>\&) متد

منطقه‌ای که توسط شیء جاری نشان داده می‌شود را با نتیجهٔ الحاق این منطقه و منطقه‌ای که توسط مسیر مشخص‌شده تعریف شده است، جایگزین می‌کند.

```cpp
void System::Drawing::Region::Union(const SharedPtr<Drawing2D::GraphicsPath> &path)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| path | const [SharedPtr](../../../system/sharedptr/)\<[Drawing2D::GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)\>\& | یک مسیر که منطقه‌ای را تعریف می‌کند تا این منطقه با آن متحد شود |

## Region::Union(const SharedPtr\<Region\>\&) متد

منطقه‌ای که توسط شیء جاری نشان داده می‌شود را با نتیجهٔ الحاق این منطقه و منطقهٔ مشخص‌شده، جایگزین می‌کند.

```cpp
void System::Drawing::Region::Union(const SharedPtr<Region> &region)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| region | const [SharedPtr](../../../system/sharedptr/)\<[Region](../)\>\& | یک منطقه برای متحد کردن این منطقه با آن |

## موارد مرتبط

* Typedef [SharedPtr](../../../system/sharedptr/)
* کلاس [RectangleF](../../rectanglef/)
* کلاس [Region](../)
* کلاس [Rectangle](../../rectangle/)
* کلاس [GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)
* فضا‌نام [System::Drawing](../../)
* کتابخانه [Aspose.Slides](../../../)