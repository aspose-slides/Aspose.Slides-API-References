---
title: Region()
second_title: مرجع API Aspose.Slides برای C++
description: یک نمونه جدید از کلاس Region می‌سازد.
type: docs
weight: 1
url: /fa/system.drawing/region/region/
---
## Region::Region() سازنده


یک نمونه جدید از کلاس [Region](../) می‌سازد.

```cpp
System::Drawing::Region::Region()
```

## Region::Region(const RectangleF\&) سازنده


یک نمونه جدید از کلاس [Region](../) می‌سازد که نمایانگر ناحیه‌ای است که توسط مستطیل مشخص‌شده تعریف می‌شود.

```cpp
System::Drawing::Region::Region(const RectangleF &rect)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| rect | const [RectangleF](../../rectanglef/)\& | مستطیلی که منطقه را تعریف می‌کند |

## Region::Region(const Rectangle\&) سازنده


یک نمونه جدید از کلاس [Region](../) می‌سازد که نمایانگر ناحیه‌ای است که توسط مستطیل مشخص‌شده تعریف می‌شود.

```cpp
System::Drawing::Region::Region(const Rectangle &rect)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| rect | const [Rectangle](../../rectangle/)\& | مستطیلی که منطقه را تعریف می‌کند |

## Region::Region(const SharedPtr\<Drawing2D::GraphicsPath\>\&) سازنده


یک نمونه جدید از کلاس [Region](../) می‌سازد که ناحیه‌ای را که توسط مسیر مشخص‌شده تعریف می‌شود، نشان می‌دهد.

```cpp
System::Drawing::Region::Region(const SharedPtr<Drawing2D::GraphicsPath> &path)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| path | const [SharedPtr](../../../system/sharedptr/)\<[Drawing2D::GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)\>\& | مسیری که ناحیه را تعریف می‌کند |

## Region::Region(const SkPath\&) سازنده




```cpp
System::Drawing::Region::Region(const SkPath &path)
```

## Region::Region(const SharedPtr\<Drawing2D::RegionData\>\&) سازنده


یک نمونه جدید از کلاس [Region](../) می‌سازد که ناحیه‌ای را که توسط شیء RegionData مشخص‌شده تعریف می‌شود، نشان می‌دهد.

```cpp
System::Drawing::Region::Region(const SharedPtr<Drawing2D::RegionData> &region_data)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| region_data | const [SharedPtr](../../../system/sharedptr/)\<[Drawing2D::RegionData](../../../system.drawing.drawing2d/regiondata/)\>\& | شیء RegionData که ناحیه را تعریف می‌کند |

## مراجع

* Typedef [SharedPtr](../../../system/sharedptr/)
* کلاس [Region](../)
* کلاس [RectangleF](../../rectanglef/)
* کلاس [Rectangle](../../rectangle/)
* کلاس [GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)
* کلاس [RegionData](../../../system.drawing.drawing2d/regiondata/)
* فضای‌نام [System::Drawing](../../)
* کتابخانه [Aspose.Slides](../../../)