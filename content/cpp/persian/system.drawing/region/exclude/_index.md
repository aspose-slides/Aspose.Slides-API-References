---
title: Exclude()
second_title: مرجع API Aspose.Slides برای C++
description: منطقه‌ای که توسط شیء جاری نمایانده شده است را با نتیجهٔ حذف منطقه‌ای که توسط مستطیل مشخص‌شده تعریف شده است، جایگزین می‌کند.
type: docs
weight: 92
url: /fa/system.drawing/region/exclude/
---
## Region::Exclude(const RectangleF\&) متد


منطقه‌ای که توسط شیء جاری نمایانده شده است را با نتیجهٔ حذف منطقه‌ای که توسط مستطیل مشخص‌شده تعریف شده است، جایگزین می‌کند.

```cpp
void System::Drawing::Region::Exclude(const RectangleF &rect)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| rect | const [RectangleF](../../rectanglef/)\& | مستطیلی که منطقه‌ای برای حذف تعریف می‌کند |

## Region::Exclude(const Rectangle\&) متد


منطقه‌ای که توسط شیء جاری نمایانده شده است را با نتیجهٔ حذف منطقه‌ای که توسط مستطیل مشخص‌شده تعریف شده است، جایگزین می‌کند.

```cpp
void System::Drawing::Region::Exclude(const Rectangle &rect)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| rect | const [Rectangle](../../rectangle/)\& | مستطیلی که منطقه‌ای برای حذف تعریف می‌کند |

## Region::Exclude(const SharedPtr\<Drawing2D::GraphicsPath\>\&) متد


منطقه‌ای که توسط شیء جاری نمایانده شده است را با نتیجهٔ حذف منطقه‌ای که توسط مسیر مشخص‌شده تعریف شده است، جایگزین می‌کند.

```cpp
void System::Drawing::Region::Exclude(const SharedPtr<Drawing2D::GraphicsPath> &path)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| path | const [SharedPtr](../../../system/sharedptr/)\<[Drawing2D::GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)\>\& | مسیری که منطقه‌ای برای حذف تعریف می‌کند |

## Region::Exclude(const SharedPtr\<Region\>\&) متد


منطقه‌ای که توسط شیء جاری نمایانده شده است را با نتیجهٔ حذف منطقهٔ مشخص‌شده، جایگزین می‌کند.

```cpp
void System::Drawing::Region::Exclude(const SharedPtr<Region> &region)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| region | const [SharedPtr](../../../system/sharedptr/)\<[Region](../)\>\& | منطقه‌ای برای حذف |

## موارد مرتبط

* تعریف‌نوع [SharedPtr](../../../system/sharedptr/)
* کلاس [RectangleF](../../rectanglef/)
* کلاس [Region](../)
* کلاس [Rectangle](../../rectangle/)
* کلاس [GraphicsPath](../../../system.drawing.drawing2d/graphicspath/)
* فضای‌نام [System::Drawing](../../)
* کتابخانه [Aspose.Slides](../../../)