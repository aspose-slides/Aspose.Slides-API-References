---
title: RectangleF
second_title: مرجع API Aspose.Slides برای C++
description: "نمایشگر یک ناحیه مستطیلی از یک تصویر است که به‌صورت مختصات نقطه‌ای X و Y با دقت تک‌دقت شناور برای گوشهٔ بالا-چپ و عرض و ارتفاع آن تعریف می‌شود. این نوع باید بر روی پشته تخصیص یابد و به توابع به‌صورت مقدار یا مرجع پاس داده شود. هرگز از کلاس System::SmartPtr برای مدیریت اشیای این نوع استفاده نکنید."
type: docs
weight: 248
url: /fa/system.drawing/rectanglef/
---
## RectangleF کلاس

نمایشگر یک ناحیه مستطیلی از یک تصویر است که به‌صورت مختصات نقطه‌ای X و Y با دقت تک‌دقت شناور برای گوشهٔ بالا-چپ و عرض و ارتفاع آن تعریف می‌شود. این نوع باید بر روی پشته تخصیص یابد و به توابع به‌صورت مقدار یا مرجع پاس داده شود. هرگز از کلاس [System::SmartPtr](../../system/smartptr/) برای مدیریت اشیای این نوع استفاده نکنید.

```cpp
class RectangleF
```

## متدها

| متد | توضیح |
| --- | --- |
| **bool** [Contains](./contains/)(**float**, **float**) | تعیین می‌کند که آیا نقطهٔ مشخص‌شده در داخل مستطیلی که توسط شیء جاری نمایان شده‌است، قرار دارد یا خیر. |
| **bool** [Contains](./contains/)(const [PointF](../pointf/)\&) | تعیین می‌کند که آیا نقطهٔ مشخص‌شده در داخل مستطیلی که توسط شیء جاری نمایان شده‌است، قرار دارد یا خیر. |
| **bool** [Contains](./contains/)(const [RectangleF](./)\&) | تعیین می‌کند که آیا مستطیل مشخص‌شده در داخل مستطیلی که توسط شیء جاری نمایان شده‌است، قرار دارد یا خیر. |
| **bool** [Equals](./equals/)(const [RectangleF](./)\&) const | تعیین می‌کند که آیا مستطیل‌های نمایان‌شده توسط شیء جاری و شیء مشخص‌شده یکسان هستند یا نه. |
| static [RectangleF](./) [FromLTRB](./fromltrb/)(**float**, **float**, **float**, **float**) | یک شیء جدید [RectangleF](./) می‌سازد که یک مستطیل با موقعیت لبه‌های مشخص‌شده را نشان می‌دهد. |
| **float** [get_Bottom](./get_bottom/)() const | مختصات y لبهٔ پایین مستطیلی که توسط شیء جاری نمایان شده‌است را برمی‌گرداند. |
| **float** [get_Height](./get_height/)() const | ارتفاع مستطیلی که توسط شیء جاری نمایان شده‌است را برمی‌گرداند. |
| **bool** [get_IsEmpty](./get_isempty/)() const | تعیین می‌کند که آیا مختصات X و Y گوشهٔ بالا-چپ مستطیل نمایان‌شده توسط شیء جاری و همچنین عرض و ارتفاع آن مقدار صفر دارند یا نه. |
| **float** [get_Left](./get_left/)() const | مختصات X لبهٔ چپ مستطیلی که توسط شیء جاری نمایان شده‌است را برمی‌گرداند. |
| [PointF](../pointf/) [get_Location](./get_location/)() const | یک نمونه از کلاس [PointF](../pointf/) را برمی‌گرداند که مکان گوشهٔ بالا-چپ مستطیل نمایان‌شده توسط شیء جاری را مشخص می‌کند. |
| **float** [get_Right](./get_right/)() const | مختصات X لبهٔ راست مستطیلی که توسط شیء جاری نمایان شده‌است را برمی‌گرداند. |
| [SizeF](../sizef/) [get_Size](./get_size/)() const | یک نمونه از کلاس [SizeF](../sizef/) را برمی‌گرداند که عرض و ارتفاع مستطیلی که توسط شیء جاری نمایان شده‌است را مشخص می‌کند. |
| **float** [get_Top](./get_top/)() const | مختصات Y لبهٔ بالا مستطیلی که توسط شیء جاری نمایان شده‌است را برمی‌گرداند. |
| **float** [get_Width](./get_width/)() const | عرض مستطیلی که توسط شیء جاری نمایان شده‌است را برمی‌گرداند. |
| **float** [get_X](./get_x/)() const | مختصات X گوشهٔ بالا-چپ مستطیلی که توسط شیء جاری نمایان شده‌است را برمی‌گرداند. |
| **float** [get_Y](./get_y/)() const | مختصات Y گوشهٔ بالا-چپ مستطیلی که توسط شیء جاری نمایان شده‌است را برمی‌گرداند. |
| int [GetHashCode](./gethashcode/)() const | یک کد هش برای شیء جاری برمی‌گرداند. |
| void [Inflate](./inflate/)(**float**, **float**) | عرض و ارتفاع مستطیلی که توسط شیء جاری نمایان شده‌است را افزایش می‌دهد، در حالی که مکان مرکز هندسی مستطیل حفظ می‌شود. عرض و ارتفاع در هر دو جهت به مقدارهای مشخص‌شده افزایان می‌یابند. |
| void [Inflate](./inflate/)(const [SizeF](../sizef/)\&) | عرض و ارتفاع مستطیلی که توسط شیء جاری نمایان شده‌است را افزایش می‌دهد، در حالی که مکان مرکز هندسی مستطیل حفظ می‌شود. عرض و ارتفاع در هر دو جهت به مقادیر عرضه و ارتفاع شیء اندازهٔ مشخص‌شده به‌صورت متناظر افزایان می‌یابند. |
| static [RectangleF](./) [Inflate](./inflate/)(const [RectangleF](./)\&, **float**, **float**) | عرض و ارتفاع مستطیلی که توسط شیء مشخص‌شده نمایان شده‌است را افزایش می‌دهد، در حالی که مکان مرکز هندسی مستطیل حفظ می‌شود. عرض و ارتفاع در هر دو جهت به مقدارهای مشخص‌شده افزایان می‌یابند. |
| void [Intersect](./intersect/)(const [RectangleF](./)\&) | مستطیلی که توسط شیء جاری نمایان شده‌است را با مستطیلی که نتیجه تقاطع آن با مستطیل شیء مشخص‌شده است، جایگزین می‌کند. |
| static [RectangleF](./) [Intersect](./intersect/)(const [RectangleF](./)\&, const [RectangleF](./)\&) | یک مستطیل که نتیجهٔ تقاطع مستطیل‌های مشخص‌شده است را برمی‌گرداند. |
| **bool** [IntersectsWith](./intersectswith/)(const [RectangleF](./)\&) | تعیین می‌کند که آیا مستطیل‌های نمایان‌شده توسط شیء جاری و شیء مشخص‌شده تقاطع دارند یا نه. |
| void [Offset](./offset/)(const [PointF](../pointf/)\&) | موقعیت مستطیلی که توسط شیء جاری نمایان شده‌است را به مقدارهای مشخص‌شده جابجا می‌کند. |
| void [Offset](./offset/)(**float**, **float**) | موقعیت مستطیلی که توسط شیء جاری نمایان شده‌است را به مقدارهای مشخص‌شده جابجا می‌کند. |
| **bool** [operator!=](./operator_not_equal/)(std::nullptr_t) const | همیشه مقدار true را برمی‌گرداند. |
| **bool** [operator==](./operator_equal_equal/)(std::nullptr_t) const | همیشه مقدار false را برمی‌گرداند. |
|  [RectangleF](./rectanglef/)() | یک نمونهٔ جدید از شیء [RectangleF](./) می‌سازد که مستطیلی با مختصات X و Y و مقادیر عرض و ارتفاع صفر را نشان می‌دهد. |
|  [RectangleF](./rectanglef/)(**float**, **float**, **float**, **float**) | یک نمونهٔ جدید از شیء [RectangleF](./) می‌سازد که مستطیلی با مختصات مشخص‌شدهٔ گوشهٔ بالا-چپ و عرض و ارتفاع را نمایان می‌کند. |
|  [RectangleF](./rectanglef/)(const [PointF](../pointf/)\&, const [SizeF](../sizef/)\&) | یک نمونهٔ جدید از شیء [RectangleF](./) می‌سازد که مستطیلی با مختصات گوشهٔ بالا-چپ که به‌عنوان نمونه‌ای از کلاس [PointF](../pointf/) مشخص شده و عرض و ارتفاع آن به‌عنوان نمونه‌ای از کلاس [SizeF](../sizef/) تعیین می‌شود. |
| explicit  [RectangleF](./rectanglef/)(const [Rectangle](../rectangle/)\&) | یک نمونهٔ جدید از شیء [RectangleF](./) می‌سازد که مستطیلی معادل با مستطیل مشخص‌شده را نشان می‌دهد. |
| void [set_Height](./set_height/)(**float**) | ارتفاع مستطیلی که توسط شیء جاری نمایان شده‌است را تنظیم می‌کند. |
| void [set_Location](./set_location/)([PointF](../pointf/)) | مکان گوشهٔ بالا-چپ مستطیلی که توسط شیء جاری نمایان شده‌است را تنظیم می‌کند. |
| void [set_Size](./set_size/)([SizeF](../sizef/)) | عرض و ارتفاع مستطیلی که توسط شیء جاری نمایان شده‌است را تنظیم می‌کند. |
| void [set_Width](./set_width/)(**float**) | عرض مستطیلی که توسط شیء جاری نمایان شده‌است را تنظیم می‌کند. |
| void [set_X](./set_x/)(**float**) | مختصات X گوشهٔ بالا-چپ مستطیلی که توسط شیء جاری نمایان شده‌است را تنظیم می‌کند. |
| void [set_Y](./set_y/)(**float**) | مختصات Y گوشهٔ بالا-چپ مستطیلی که توسط شیء جاری نمایان شده‌است را تنظیم می‌کند. |
| [System::String](../../system/string/) [ToString](./tostring/)() const | نمایهٔ رشته‌ای شیء جاری را برمی‌گرداند. |
| static [RectangleF](./) [Union](./union/)(const [RectangleF](./)\&, const [RectangleF](./)\&) | یک مستطیل که نتیجهٔ اتحاد مستطیل‌های مشخص‌شده است را برمی‌گرداند. |

## فیلدها

| فیلد | توضیح |
| --- | --- |
| static [Empty](./empty/) | یک مستطیل خالی؛ یعنی مستطیلی که مقادیر مکان و اندازهٔ آن صفر است. |

## مراجع

* Namespace [System::Drawing](../)
* Library [Aspose.Slides](../../)