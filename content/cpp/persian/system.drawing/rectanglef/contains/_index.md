---
title: Contains()
second_title: مرجع API Aspose.Slides برای C++
description: مشخص می‌کند آیا نقطهٔ مشخص‌شده در داخل مستطیل نمایانده‌شده توسط شیء فعلی قرار دارد یا خیر.
type: docs
weight: 248
url: /fa/system.drawing/rectanglef/contains/
---
## RectangleF::Contains(float, float) متد


تعیین می‌کند آیا نقطه مشخص شده در داخل مستطیلی که توسط شیء جاری نمایانده شده است، واقع شده است یا خیر.

```cpp
bool System::Drawing::RectangleF::Contains(float x, float y)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| x | **float** | مختصات X نقطه‌ای که باید بررسی شود |
| y | **float** | مختصات Y نقطه‌ای که باید بررسی شود |

### مقدار بازگشت

True اگر نقطه مشخص شده در داخل مستطیل نمایانده شده توسط شیء جاری قرار داشته باشد، در غیر این صورت - false

## RectangleF::Contains(const PointF\&) متد


تعیین می‌کند آیا نقطه مشخص شده در داخل مستطیلی که توسط شیء جاری نمایانده شده است، واقع شده است یا خیر.

```cpp
bool System::Drawing::RectangleF::Contains(const PointF &point)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| point | const [PointF](../../pointf/)\& | نقطه‌ای برای بررسی |

### مقدار بازگشت

True اگر نقطه مشخص شده در داخل مستطیل نمایانده شده توسط شیء جاری قرار داشته باشد، در غیر این صورت - false

## RectangleF::Contains(const RectangleF\&) متد


تعیین می‌کند آیا مستطیل مشخص شده در داخل مستطیلی که توسط شیء جاری نمایانده شده است، واقع شده است یا خیر.

```cpp
bool System::Drawing::RectangleF::Contains(const RectangleF &rect)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| rect | const [RectangleF](../)\& | مستطیلی برای بررسی |

### مقدار بازگشت

True اگر مستطیل مشخص شده در داخل مستطیل نمایانده شده توسط شیء جاری قرار داشته باشد، در غیر این صورت - false

## موارد مرتبط

* کلاس [RectangleF](../)
* کلاس [PointF](../../pointf/)
* فضای نام [System::Drawing](../../)
* کتابخانه [Aspose.Slides](../../../)