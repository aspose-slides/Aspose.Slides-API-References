---
title: Point
second_title: مرجع API Aspose.Slides برای C++
description: "یک جفت مختصات عدد صحیح X و Y یک نقطه بر روی صفحهٔ دو-بعدی را نشان می‌دهد. این نوع باید بر روی پشته تخصیص یافته و به توابع به‌صورت مقدار یا با ارجاع پاس داده شود. هرگز از کلاس System::SmartPtr برای مدیریت اشیاء این نوع استفاده نکنید."
type: docs
weight: 209
url: /fa/system.drawing/point/
---
## کلاس Point

نمایش یک جفت مختصات عدد صحیح X و Y یک نقطه در صفحهٔ دو-بعدی. این نوع باید روی پشته تخصیص یافته و به توابع به‌صورت مقدار یا ارجاع پاس داده شود. هرگز از [System::SmartPtr](../../system/smartptr/) کلاس برای مدیریت اشیاء این نوع استفاده نکنید.

```cpp
class Point
```

## متدها

| متد | توضیح |
| --- | --- |
| static [Point](./) [Add](./add/)(const [Point](./)\&, const [Size](../size/)\&) | مقدارهای عرض و ارتفاع شیء [Size](../size/) مشخص‌شده را به ترتیب به مقادیر مختصات X و Y شیء [Point](./) مشخص‌شده اضافه می‌کند. |
| static [Point](./) [Ceiling](./ceiling/)(const [PointF](../pointf/)\&) | یک شیء [Point](./) را از شیء [PointF](../pointf/) مشخص‌شده با گرد کردن مقادیر مختصات X و Y شیء [PointF](../pointf/) به مقادیر صحیح بالاتر ایجاد می‌کند. |
| **bool** [Equals](./equals/)(const [Point](./)\&) const | تشخیص می‌دهد که آیا شیء فعلی و شیء مشخص‌شده برابر هستند، یعنی همان جفت مقادیر مختصات X و Y را نشان می‌دهند. |
| **bool** [get_IsEmpty](./get_isempty/)() const | تشخیص می‌دهد که آیا هر دو مقدار مختصات X و Y برابر با ۰ هستند. |
| int [get_X](./get_x/)() const | مقدار مختصات X که توسط شیء فعلی نمایان می‌شود را برمی‌گرداند. |
| int [get_Y](./get_y/)() const | مقدار مختصات Y که توسط شیء فعلی نمایان می‌شود را برمی‌گرداند. |
| int [GetHashCode](./gethashcode/)() const | یک کد هش برای شیء فعلی برمی‌گرداند. |
| size_t [getStdHash](./getstdhash/)() const | یک مقدار هش برای شیء فعلی برمی‌گرداند. |
| **bool** [IsNull](./isnull/)() const | همیشه مقدار نادرست (false) را برمی‌گرداند. |
| void [Offset](./offset/)(int, int) | مقادیر مختصات X و Y که توسط شیء فعلی نمایان می‌شود را با مقادیر مشخص‌شده جابجا می‌کند. |
| void [Offset](./offset/)([Point](./)) | مختصات X و Y که توسط شیء فعلی نمایان می‌شود را به ترتیب با مقادیر مختصات X و Y شیء [Point](./) مشخص‌شده جابجا می‌کند. |
|  [operator PointF](./operator_pointf/)() const | یک نمونه از شیء [PointF](../pointf/) را ساختار می‌دهد و آن را با مقادیر مختصات X و Y شیء [Point](./) فعلی مقداردهی اولیه می‌کند. |
|  [operator Size](./operator_size/)() const | یک نمونه از شیء [Size](../size/) را ساختار می‌دهد و مقادیر عرض و ارتفاع آن را به ترتیب با مقادیر مختصات X و Y که توسط شیء فعلی نمایان می‌شود مقداردهی اولیه می‌کند. |
|  [Point](./point/)() | یک شیء جدید [Point](./) را ساختار می‌دهد و مقادیر مختصات X و Y آن را با ۰ مقداردهی اولیه می‌کند. |
|  [Point](./point/)(int, int) | یک شیء جدید [Point](./) را ساختار می‌دهد و آن را با مقادیر مشخص‌شده مقداردهی اولیه می‌کند. |
|  [Point](./point/)(const [Size](../size/)\&) | یک شیء جدید [Point](./) را ساختار می‌دهد و مقادیر مختصات X و Y آن را به ترتیب با مقادیر عرض و ارتفاع شیء [SizeF](../sizef/) مشخص‌شده مقداردهی اولیه می‌کند. |
|  [Point](./point/)(int) | یک شیء جدید [Point](./) را ساختار می‌دهد و مقدار مختصات X آن را با مقدار تشکیل‌شده از 16 بیت بالایی عدد صحیح ۳۲-بیتی مشخص‌شده و مقدار مختصات Y آن را با مقدار تشکیل‌شده از 16 بیت پایینی همان عدد صحیح مقداردهی می‌کند. |
| static [Point](./) [Round](./round/)(const [PointF](../pointf/)\&) | یک شیء [Point](./) را از شیء [PointF](../pointf/) مشخص‌شده با گرد کردن مقادیر مختصات X و Y شیء [PointF](../pointf/) به نزدیک‌ترین مقادیر صحیح ایجاد می‌کند. |
| void [set_X](./set_x/)(int) | مقدار مختصات X که توسط شیء فعلی نمایان می‌شود را تنظیم می‌کند. |
| void [set_Y](./set_y/)(int) | مقدار مختصات Y که توسط شیء فعلی نمایان می‌شود را تنظیم می‌کند. |
| static [Point](./) [Subtract](./subtract/)(const [Point](./)\&, const [Size](../size/)\&) | مقدارهای عرض و ارتفاع شیء [Size](../size/) مشخص‌شده را به ترتیب از مقادیر مختصات X و Y شیء [Point](./) مشخص‌شده کم می‌کند. |
| [String](../../system/string/) [ToString](./tostring/)() const | نمایش متنی جفت مقادیر مختصات X و Y که توسط شیء فعلی نمایان می‌شود را برمی‌گرداند. |
| static [Point](./) [Truncate](./truncate/)(const [PointF](../pointf/)\&) | یک شیء [Point](./) را از شیء [PointF](../pointf/) مشخص‌شده با کوتاه کردن مقادیر مختصات X و Y شیء [PointF](../pointf/) به مقادیر صحیح پایین‌تر ایجاد می‌کند. |

## فیلدها

| فیلد | توضیح |
| --- | --- |
| static [Empty](./empty/) | یک نمونهٔ خالی از [Point](./) کلاس که مقادیر مختصات X و Y آن ۰ است. |

## موارد مرتبط

* فضای نام [System::Drawing](../)
* کتابخانه [Aspose.Slides](../../)