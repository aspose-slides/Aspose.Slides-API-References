---
title: FillEllipse()
second_title: مرجع API Aspose.Slides برای C++
description: محیط داخلی بیضی مشخص‌شده توسط مستطیل محاطی را با استفاده از براش تعیین‌شده پر می‌کند.
type: docs
weight: 378
url: /fa/system.drawing/graphics/fillellipse/
---
## Graphics::FillEllipse(const SharedPtr\<Brush\>\&, Rectangle) متد

محیط بیضی تعیین‌شده توسط مستطیل محاطی را با استفاده از براش مشخص‌شده پر می‌کند.

```cpp
void System::Drawing::Graphics::FillEllipse(const SharedPtr<Brush> &brush, Rectangle rect)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| brush | const [SharedPtr](../../../system/sharedptr/)\<[Brush](../../brush/)\>\& | یک شیء [Brush](../../brush/) برای پر کردن |
| rect | [Rectangle](../../rectangle/) | مستطیل محاطی که بیضی مورد پر کردن را تعریف می‌کند |

## Graphics::FillEllipse(const SharedPtr\<Brush\>\&, RectangleF) متد

محیط بیضی تعیین‌شده توسط مستطیل محاطی را با استفاده از براش مشخص‌شده پر می‌کند.

```cpp
void System::Drawing::Graphics::FillEllipse(const SharedPtr<Brush> &brush, RectangleF rect)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| brush | const [SharedPtr](../../../system/sharedptr/)\<[Brush](../../brush/)\>\& | یک شیء [Brush](../../brush/) برای پر کردن |
| rect | [RectangleF](../../rectanglef/) | مستطیل محاطی که بیضی مورد پر کردن را تعریف می‌کند |

## Graphics::FillEllipse(const SharedPtr\<Brush\>\&, int, int, int, int) متد

محیط بیضی تعیین‌شده توسط مستطیل محاطی را با استفاده از براش مشخص‌شده پر می‌کند.

```cpp
void System::Drawing::Graphics::FillEllipse(const SharedPtr<Brush> &brush, int x, int y, int width, int height)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| brush | const [SharedPtr](../../../system/sharedptr/)\<[Brush](../../brush/)\>\& | یک شیء [Brush](../../brush/) برای پر کردن |
| x | int | مختصات X گوشهٔ بالایی سمت چپ مستطیل محاطی |
| y | int | مختصات Y گوشهٔ بالایی سمت چپ مستطیل محاطی |
| width | int | عرض مستطیل محاطی |
| height | int | ارتفاع مستطیل محاطی |

## Graphics::FillEllipse(const SharedPtr\<Brush\>\&, float, float, float, float) متد

محیط بیضی تعیین‌شده توسط مستطیل محاطی را با استفاده از براش مشخص‌شده پر می‌کند.

```cpp
void System::Drawing::Graphics::FillEllipse(const SharedPtr<Brush> &brush, float x, float y, float width, float height)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| brush | const [SharedPtr](../../../system/sharedptr/)\<[Brush](../../brush/)\>\& | یک شیء [Brush](../../brush/) برای پر کردن |
| x | **float** | مختصات X گوشهٔ بالایی سمت چپ مستطیل محاطی |
| y | **float** | مختصات Y گوشهٔ بالایی سمت چپ مستطیل محاطی |
| width | **float** | عرض مستطیل محاطی |
| height | **float** | ارتفاع مستطیل محاطی |

## موارد مرتبط

* تعریف نوع [SharedPtr](../../../system/sharedptr/)
* کلاس [Brush](../../brush/)
* کلاس [Rectangle](../../rectangle/)
* کلاس [Graphics](../)
* کلاس [RectangleF](../../rectanglef/)
* فضای‌نام [System::Drawing](../../)
* کتابخانه [Aspose.Slides](../../../)