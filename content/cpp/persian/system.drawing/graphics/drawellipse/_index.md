---
title: DrawEllipse()
second_title: مرجع API Aspose.Slides برای C++
description: الپیس مشخص‌شده را با استفاده از قلم تعیین‌شده بر روی سطحی که توسط شیء جاری نشان داده می‌شود، رسم می‌کند.
type: docs
weight: 352
url: /fa/system.drawing/graphics/drawellipse/
---
## Graphics::DrawEllipse(const SharedPtr\<Pen\>\&, Rectangle) متد

الپیس موردنظر را با استفاده از قلم مشخص‌شده بر روی سطحی که توسط شیء جاری نمایان شده است، رسم می‌کند.

```cpp
void System::Drawing::Graphics::DrawEllipse(const SharedPtr<Pen> &pen, Rectangle rect)
```

### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| pen | const [SharedPtr](../../../system/sharedptr/)\<[Pen](../../pen/)\>\& | قلمی که برای رسم الپیس استفاده می‌شود |
| rect | [Rectangle](../../rectangle/) | مستطیلی که الپیس را تعریف می‌کند |

## Graphics::DrawEllipse(const SharedPtr\<Pen\>\&, RectangleF) متد

الپیس موردنظر را با استفاده از قلم مشخص‌شده بر روی سطحی که توسط شیء جاری نمایان شده است، رسم می‌کند.

```cpp
void System::Drawing::Graphics::DrawEllipse(const SharedPtr<Pen> &pen, RectangleF rect)
```

### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| pen | const [SharedPtr](../../../system/sharedptr/)\<[Pen](../../pen/)\>\& | قلمی که برای رسم الپیس استفاده می‌شود |
| rect | [RectangleF](../../rectanglef/) | مستطیلی که الپیس را تعریف می‌کند |

## Graphics::DrawEllipse(const SharedPtr\<Pen\>\&, int, int, int, int) متد

الپیس موردنظر را با استفاده از قلم مشخص‌شده بر روی سطحی که توسط شیء جاری نمایان شده است، رسم می‌کند.

```cpp
void System::Drawing::Graphics::DrawEllipse(const SharedPtr<Pen> &pen, int x, int y, int width, int height)
```

### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| pen | const [SharedPtr](../../../system/sharedptr/)\<[Pen](../../pen/)\>\& | قلمی که برای رسم الپیس استفاده می‌شود |
| x | int | مختصات X گوشهٔ بالا-چپ مستطیلی که الپیس را تعریف می‌کند |
| y | int | مختصات Y گوشهٔ بالا-چپ مستطیلی که الپیس را تعریف می‌کند |
| width | int | عرض مستطیلی که الپیس را تعریف می‌کند |
| height | int | ارتفاع مستطیلی که الپیس را تعریف می‌کند |

## Graphics::DrawEllipse(const SharedPtr\<Pen\>\&, float, float, float, float) متد

الپیس موردنظر را با استفاده از قلم مشخص‌شده بر روی سطحی که توسط شیء جاری نمایان شده است، رسم می‌کند.

```cpp
void System::Drawing::Graphics::DrawEllipse(const SharedPtr<Pen> &pen, float x, float y, float width, float height)
```

### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| pen | const [SharedPtr](../../../system/sharedptr/)\<[Pen](../../pen/)\>\& | قلمی که برای رسم الپیس استفاده می‌شود |
| x | **float** | مختصات X گوشهٔ بالا-چپ مستطیلی که الپیس را تعریف می‌کند |
| y | **float** | مختصات Y گوشهٔ بالا-چپ مستطیلی که الپیس را تعریف می‌کند |
| width | **float** | عرض مستطیلی که الپیس را تعریف می‌کند |
| height | **float** | ارتفاع مستطیلی که الپیس را تعریف می‌کند |

## موارد مرتبط

* Typedef [SharedPtr](../../../system/sharedptr/)
* کلاس [Pen](../../pen/)
* کلاس [Rectangle](../../rectangle/)
* کلاس [Graphics](../)
* کلاس [RectangleF](../../rectanglef/)
* فضای نام [System::Drawing](../../)
* کتابخانه [Aspose.Slides](../../../)