---
title: IsVisible()
second_title: Aspose.Slides برای مرجع API C++
description: تعیین می‌کند که آیا نقطهٔ مشخص‌شده در محدودهٔ قابل مشاهدهٔ کلیپ شیء Graphics فعلی قرار دارد یا خیر.
type: docs
weight: 963
url: /fa/system.drawing/graphics/isvisible/
---
## Graphics::IsVisible(Point) متد

تشخیص می‌دهد آیا نقطهٔ مشخص‌شده در محدودهٔ قابل مشاهدهٔ کلیپ شیء [Graphics](../) فعلی قرار دارد یا نه.

```cpp
bool System::Drawing::Graphics::IsVisible(Point point)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| point | [Point](../../point/) | نقطه برای بررسی |

### مقدار بازگشتی

در صورتی که **point** در محدودهٔ قابل مشاهدهٔ کلیپ شیء [Graphics](../) فعلی باشد، درست؛ در غیر این صورت - غلط

## Graphics::IsVisible(PointF) متد

پیاده‌سازی نشده.

```cpp
bool System::Drawing::Graphics::IsVisible(PointF point)
```

## Graphics::IsVisible(Rectangle) متد

پیاده‌سازی نشده.

```cpp
bool System::Drawing::Graphics::IsVisible(Rectangle rect)
```

## Graphics::IsVisible(RectangleF) متد

پیاده‌سازی نشده.

```cpp
bool System::Drawing::Graphics::IsVisible(RectangleF rect)
```

## Graphics::IsVisible(int32_t, int32_t) متد

پیاده‌سازی نشده.

```cpp
bool System::Drawing::Graphics::IsVisible(int32_t x, int32_t y)
```

## Graphics::IsVisible(float, float) متد

پیاده‌سازی نشده.

```cpp
bool System::Drawing::Graphics::IsVisible(float x, float y)
```

## Graphics::IsVisible(float, float, float, float) متد

پیاده‌سازی نشده.

```cpp
bool System::Drawing::Graphics::IsVisible(float x, float y, float width, float height)
```

## Graphics::IsVisible(int32_t, int32_t, int32_t, int32_t) متد

پیاده‌سازی نشده.

```cpp
bool System::Drawing::Graphics::IsVisible(int32_t x, int32_t y, int32_t width, int32_t height)
```

## موارد مرتبط

* کلاس [Point](../../point/)
* کلاس [Graphics](../)
* کلاس [PointF](../../pointf/)
* کلاس [Rectangle](../../rectangle/)
* کلاس [RectangleF](../../rectanglef/)
* فضای‌نام [System::Drawing](../../)
* کتابخانه [Aspose.Slides](../../../)