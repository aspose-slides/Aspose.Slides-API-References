---
title: FillRectangle()
second_title: مرجع API Aspose.Slides برای C++
description: مستطیل مشخص‌شده را با قلم‌مو مشخص‌شده پر می‌کند.
type: docs
weight: 326
url: /fa/system.drawing/graphics/fillrectangle/
---
## Graphics::FillRectangle(const SharedPtr\<Brush\>\&, float, float, float, float) متد

مستطیل مشخص‌شده را با brush مشخص پر می‌کند.

```cpp
void System::Drawing::Graphics::FillRectangle(const SharedPtr<Brush> &brush, float x, float y, float width, float height)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| brush | const [SharedPtr](../../../system/sharedptr/)\<[Brush](../../brush/)\>\& | یک شیء [Brush](../../brush/) برای پر کردن |
| x | **float** | مختصات X گوشهٔ بالای چپ مستطیلی که باید پر شود |
| y | **float** | مختصات Y گوشهٔ بالای چپ مستطیلی که باید پر شود |
| width | **float** | عرض مستطیلی که باید پر شود |
| height | **float** | ارتفاع مستطیلی که باید پر شود |

## Graphics::FillRectangle(const SharedPtr\<Brush\>\&, int, int, int, int) متد

مستطیل مشخص‌شده را با brush مشخص پر می‌کند.

```cpp
void System::Drawing::Graphics::FillRectangle(const SharedPtr<Brush> &brush, int x, int y, int width, int height)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| brush | const [SharedPtr](../../../system/sharedptr/)\<[Brush](../../brush/)\>\& | یک شیء [Brush](../../brush/) برای پر کردن |
| x | int | مختصات X گوشهٔ بالای چپ مستطیلی که باید پر شود |
| y | int | مختصات Y گوشهٔ بالای چپ مستطیلی که باید پر شود |
| width | int | عرض مستطیلی که باید پر شود |
| height | int | ارتفاع مستطیلی که باید پر شود |

## Graphics::FillRectangle(const SharedPtr\<Brush\>\&, Rectangle) متد

مستطیل مشخص‌شده را با brush مشخص پر می‌کند.

```cpp
void System::Drawing::Graphics::FillRectangle(const SharedPtr<Brush> &brush, Rectangle rect)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| brush | const [SharedPtr](../../../system/sharedptr/)\<[Brush](../../brush/)\>\& | یک شیء [Brush](../../brush/) برای پر کردن |
| rect | [Rectangle](../../rectangle/) | یک شیء [Rectangle](../../rectangle/) که مکان و اندازهٔ مستطیلی را که باید پر شود مشخص می‌کند |

## Graphics::FillRectangle(const SharedPtr\<Brush\>\&, RectangleF) متد

مستطیل مشخص‌شده را با brush مشخص پر می‌کند.

```cpp
void System::Drawing::Graphics::FillRectangle(const SharedPtr<Brush> &brush, RectangleF rect)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| brush | const [SharedPtr](../../../system/sharedptr/)\<[Brush](../../brush/)\>\& | یک شیء [Brush](../../brush/) برای پر کردن |
| rect | [RectangleF](../../rectanglef/) | یک شیء [RectangleF](../../rectanglef/) که مکان و اندازهٔ مستطیلی را که باید پر شود مشخص می‌کند |

## موارد مرتبط

* Typedef [SharedPtr](../../../system/sharedptr/)
* کلاس [Brush](../../brush/)
* کلاس [Graphics](../)
* کلاس [Rectangle](../../rectangle/)
* کلاس [RectangleF](../../rectanglef/)
* فضای‌نام [System::Drawing](../../)
* Library [Aspose.Slides](../../../)