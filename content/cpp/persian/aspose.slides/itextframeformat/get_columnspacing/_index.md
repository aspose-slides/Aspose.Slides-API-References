---
title: get_ColumnSpacing()
second_title: مرجع API Aspose.Slides برای C++
description: فضای بین ستون‌های متن در ناحیه متن (به نقاط) را برمی‌گرداند. این فقط وقتی که بیش از یک ستون موجود باشد اعمال می‌شود. این مقدار باید عددی مثبت باشد. در غیر این صورت، مقدار به صفر تنظیم می‌شود. مقدار به صورت double خوانده می‌شود.
type: docs
weight: 274
url: /fa/aspose.slides/itextframeformat/get_columnspacing/
---
## ITextFrameFormat::get_ColumnSpacing() متد

فضای بین ستون‌های متن در ناحیه متن (به نقاط) را برمی‌گرداند. این فقط وقتی که بیش از یک ستون موجود باشد اعمال می‌شود. این مقدار باید عددی مثبت باشد. در غیر این صورت، مقدار به صفر تنظیم می‌شود. خوانده می‌شود **double**.

```cpp
virtual double Aspose::Slides::ITextFrameFormat::get_ColumnSpacing()=0
```

## موارد مرتبط

* کلاس [ITextFrameFormat](../)
* فضای نام [Aspose::Slides](../../)
* کتابخانه [Aspose.Slides](../../../)