---
title: get_SpaceAfter()
second_title: Aspose.Slides برای C++ مرجع API
description: مقدار فضای پس از آخرین خط در یک پاراگراف بدون وراثت را برمی‌گرداند. یک مقدار مثبت درصد اندازهٔ قلم (فونت) را که فضای سفید باید باشد، تعیین می‌کند. یک مقدار منفی اندازهٔ فضای سفید را به واحد پوینت مشخص می‌کند. خواندن float.
type: docs
weight: 79
url: /fa/aspose.slides/paragraphformat/get_spaceafter/
---
## ParagraphFormat::get_SpaceAfter() متد

مقدار فضای پس از آخرین خط در یک پاراگراف بدون وراثت را برمی‌گرداند. یک مقدار مثبت درصد اندازهٔ فونت را که فضای سفید باید باشد، مشخص می‌کند. یک مقدار منفی اندازهٔ فضای سفید را به واحد پوینت مشخص می‌کند. خواندن **float**.

```cpp
float Aspose::Slides::ParagraphFormat::get_SpaceAfter() override
```

## موارد مرتبط

* کلاس [ParagraphFormat](../)
* فضای نام [Aspose::Slides](../../)
* کتابخانه [Aspose.Slides](../../../)