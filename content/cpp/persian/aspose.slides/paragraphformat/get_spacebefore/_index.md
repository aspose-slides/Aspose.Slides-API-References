---
title: get_SpaceBefore()
second_title: مرجع API Aspose.Slides برای C++
description: مقدار فضای قبل از اولین خط در یک پاراگراف بدون ارث‌بری را بر می‌گرداند. یک مقدار مثبت درصد اندازه قلم را که فضای سفید باید باشد، مشخص می‌کند. یک مقدار منفی اندازه فضای سفید را بر حسب point تعیین می‌کند. به صورت float خوانده می‌شود.
type: docs
weight: 53
url: /fa/aspose.slides/paragraphformat/get_spacebefore/
---
## ParagraphFormat::get_SpaceBefore() متد

مقدار فضای قبل از اولین خط در یک پاراگراف بدون ارث‌بری را برمی‌گرداند. یک مقدار مثبت درصد اندازه قلم را که فضای سفید باید باشد، تعیین می‌کند. یک مقدار منفی اندازه فضای سفید را بر حسب نقطه (point) مشخص می‌کند. خواندنی **float**.

```cpp
float Aspose::Slides::ParagraphFormat::get_SpaceBefore() override
```

## موارد مرتبط

* کلاس [ParagraphFormat](../)
* فضای نام [Aspose::Slides](../../)
* کتابخانه [Aspose.Slides](../../../)