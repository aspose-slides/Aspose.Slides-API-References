---
title: set_StretchOffsetTop()
second_title: مرجع API Aspose.Slides برای C++
description: لبهٔ بالایی مستطیل پرکننده را تنظیم می‌کند که با جابجایی درصدی از لبهٔ بالایی جعبه محدودکنندهٔ شکل تعریف می‌شود. درصد مثبت یک تو رفتگی را مشخص می‌کند، در حالی که درصد منفی یک برآمدگی را نشان می‌دهد. مقدار به صورت float نوشته می‌شود.
type: docs
weight: 209
url: /fa/aspose.slides/picturefillformat/set_stretchoffsettop/
---
## PictureFillFormat::set_StretchOffsetTop(float) متد

لبهٔ بالایی مستطیل پر‌کننده را که با یک درصد جابه‌جایی از لبهٔ بالایی جعبهٔ محدودهٔ شکل تعریف می‌شود، تنظیم می‌کند. یک درصد مثبت یک تو رفتگی را مشخص می‌کند، در حالی که یک درصد منفی یک برآمدگی را مشخص می‌کند. مقدار **float** نوشته شود.

```cpp
void Aspose::Slides::PictureFillFormat::set_StretchOffsetTop(float value) override
```

## موارد مرتبط

* کلاس [PictureFillFormat](../)
* فضای نام [Aspose::Slides](../../)
* کتابخانه [Aspose.Slides](../../../)