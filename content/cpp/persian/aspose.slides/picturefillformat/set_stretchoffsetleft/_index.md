---
title: set_StretchOffsetLeft()
second_title: Aspose.Slides برای C++ مرجع API
description: لبهٔ چپ مستطیل پر شدن را تنظیم می‌کند که با آفست درصدی از لبهٔ چپ جعبه مرزبند شکل تعریف می‌شود. درصد مثبت یک داخل‌گذاری را مشخص می‌کند، در حالی که درصد منفی یک خارج‌گذاری را مشخص می‌کند. مقدار float نوشته می‌شود.
type: docs
weight: 183
url: /fa/aspose.slides/picturefillformat/set_stretchoffsetleft/
---
## PictureFillFormat::set_StretchOffsetLeft(float) متد


لبهٔ چپ مستطیل پر شدن که با آفست درصدی از لبهٔ چپ باکس مرزبند شکل تعریف می‌شود تنظیم می‌کند. درصد مثبت یک داخل‌گذاری را مشخص می‌کند، در حالی که درصد منفی یک خارج‌گذاری را مشخص می‌کند. **float** نوشته می‌شود.

```cpp
void Aspose::Slides::PictureFillFormat::set_StretchOffsetLeft(float value) override
```

## موارد مرتبط

* کلاس [PictureFillFormat](../)
* فضای‌نام [Aspose::Slides](../../)
* کتابخانه [Aspose.Slides](../../../)