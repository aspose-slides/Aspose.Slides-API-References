---
title: set_AnchorPoint()
second_title: مرجع API Aspose.Slides برای C++
description: یک نقطه در فضای سه‌بعدی تنظیم می‌کند. این نقطه نقطه‌ای در فضا است که صفحه پس‌زمینه را ثابت می‌کند. نقطهٔ سه‌بعدی با آرایه‌ای از ۳ مقدار float که مختصات X، Y و Z را تعریف می‌کنند، نمایش داده می‌شود. بنویسید float[].
type: docs
weight: 40
url: /fa/aspose.slides/ibackdrop3dscene/set_anchorpoint/
---
## IBackdrop3DScene::set_AnchorPoint(System::ArrayPtr\<float\>) متد

یک نقطه در فضای 3D تنظیم می‌کند. این نقطه نقطه‌ای در فضا است که صفحه پس‌زمینه را ثابت می‌کند. نقطهٔ 3D با آرایه‌ای از ۳ مقدار **float** که مختصات X، Y و Z را تعریف می‌کنند، نمایش داده می‌شود. بنویسید **float**[].

```cpp
virtual void Aspose::Slides::IBackdrop3DScene::set_AnchorPoint(System::ArrayPtr<float> value)=0
```

## موارد مرتبط

* Typedef [ArrayPtr](../../../system/arrayptr/)
* Class [IBackdrop3DScene](../)
* Namespace [Aspose::Slides](../../)
* Library [Aspose.Slides](../../../)