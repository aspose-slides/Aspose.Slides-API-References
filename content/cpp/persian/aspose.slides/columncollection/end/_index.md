---
title: end()
second_title: مرجع API Aspose.Slides برای C++
description: یک iterator را برمی‌گرداند که درست بعد از آخرین عنصر (در صورت وجود) مجموعه قرار دارد.
type: docs
weight: 131
url: /fa/aspose.slides/columncollection/end/
---
## ColumnCollection::end() method

یک iterator را بر می‌گرداند که درست بعد از آخرین عنصر (در صورت وجود) مجموعه قرار دارد.

```cpp
iterator Aspose::Slides::ColumnCollection::end() noexcept
```

### مقدار بازگشتی

یک iterator را بر می‌گرداند که درست بعد از آخرین عنصر (در صورت وجود) مجموعه قرار دارد.

## ColumnCollection::end() const method

یک iterator را بر می‌گرداند که درست بعد از آخرین عنصر (در صورت وجود) نمونه const-qualified مجموعه قرار دارد.

```cpp
const_iterator Aspose::Slides::ColumnCollection::end() const noexcept
```

### مقدار بازگشتی

یک iterator را بر می‌گرداند که درست بعد از آخرین عنصر (در صورت وجود) نمونه const-qualified مجموعه قرار دارد.

## موارد مرتبط

* تعریف نوع [iterator](../iterator/)
* تعریف نوع [const_iterator](../const_iterator/)
* کلاس [ColumnCollection](../)
* فضای نام [Aspose::Slides](../../)
* کتابخانه [Aspose.Slides](../../../)