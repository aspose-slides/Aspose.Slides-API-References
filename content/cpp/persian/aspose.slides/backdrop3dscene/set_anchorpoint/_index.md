---
title: set_AnchorPoint()
second_title: Aspose.Slides برای C++ مرجع API
description: یک نقطه در فضای سه‌بعدی تنظیم می‌کند. این نقطه نقطه‌ای در فضا است که صفحه پس‌زمینه را ثابت می‌کند. نقطه سه‌بعدی با آرایه‌ای از ۳ مقدار float که مختصات X، Y و Z را تعریف می‌کند، نمایان می‌شود. float[] را بنویسید.
type: docs
weight: 40
url: /fa/aspose.slides/backdrop3dscene/set_anchorpoint/
---
## Backdrop3DScene::set_AnchorPoint(System::ArrayPtr\<float\>) متد

یک نقطه در فضای سه‌بعدی تنظیم می‌کند. این نقطه نقطه‌ای در فضا است که صفحه پس‌زمینه را ثابت می‌کند. نقطه سه‌بعدی توسط آرایه‌ای از ۳ مقدار float که مختصات X، Y و Z را تعریف می‌کنند، نمایش داده می‌شود. **float**[] را بنویسید.

```cpp
void Aspose::Slides::Backdrop3DScene::set_AnchorPoint(System::ArrayPtr<float> value) override
```

## موارد مرتبط

* تعریف‌نوع [ArrayPtr](../../../system/arrayptr/)
* کلاس [Backdrop3DScene](../)
* فضای نام [Aspose::Slides](../../)
* کتابخانه [Aspose.Slides](../../../)