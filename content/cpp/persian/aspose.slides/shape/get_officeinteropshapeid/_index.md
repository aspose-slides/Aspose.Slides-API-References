---
title: get_OfficeInteropShapeId()
second_title: Aspose.Slides برای مرجع API C++
description: "یک شناسهٔ یکتا با دامنهٔ اسلاید را برمی‌گرداند که برای طول عمر شکل ثابت می‌ماند و به PowerPoint یا کد اینترآپ اجازه می‌دهد که به‌صورت قابل اعتماد شکل را از هر نقطه‌ای از سند ارجاع دهد. فقط‌خواندنی uint32_t. همچنین ببینید Shape::get_UniqueId."
type: docs
weight: 430
url: /fa/aspose.slides/shape/get_officeinteropshapeid/
---
## Shape::get_OfficeInteropShapeId() متد


یک شناسهٔ یکتا با دامنهٔ اسلاید را برمی‌گرداند که برای طول عمر شکل ثابت می‌ماند و به PowerPoint یا کد اینترآپ اجازه می‌دهد که به‌صورت قابل اعتماد شکل را از هر نقطه‌ای از سند ارجاع دهد. فقط‌خواندنی **uint32_t**. همچنین ببینید [Shape::get_UniqueId](../get_uniqueid/).

```cpp
uint32_t Aspose::Slides::Shape::get_OfficeInteropShapeId() override
```

## موارد مرتبط

* کلاس [Shape](../)
* فضای‌نام [Aspose::Slides](../../)
* کتابخانه [Aspose.Slides](../../../)