---
title: get_MaxBlobsBytesInMemory()
second_title: مرجع API Aspose.Slides برای C++
description: تعریف حداکثر اندازه کل (به بایت) که تمام BLOBها می‌توانند در حافظه اشغال کنند. به طور پیش‌فرض، همه BLOBها در حافظه بارگذاری می‌شوند؛ تنها پس از رسیدن به این حد از مکانیزم‌های جایگزین (مانند فایل‌های موقت) استفاده می‌شود. نگه‌داشتن BLOBها در حافظه عملکرد را حداکثر می‌کند اما می‌تواند منجر به استفاده زیاد از حافظه شود. از این ویژگی برای تنظیم رفتار مطابق با محیط یا نیازهای خود استفاده کنید.
type: docs
weight: 79
url: /fa/aspose.slides/blobmanagementoptions/get_maxblobsbytesinmemory/
---
## BlobManagementOptions::get_MaxBlobsBytesInMemory() متد

حداکثر اندازه کل (به بایت) را که تمام BLOBها می‌توانند در حافظه اشغال کنند، تعریف می‌کند. به طور پیش‌فرض، تمام BLOBها در حافظه بارگذاری می‌شوند؛ تنها پس از رسیدن به این حد از مکانیزم‌های جایگزین (مانند فایل‌های موقت) استفاده می‌شود. نگهداری BLOBها در حافظه عملکرد را حداکثر می‌کند اما می‌تواند منجر به استفاده زیاد از حافظه شود. از این خصوصیت برای تنظیم رفتار مطابق با محیط یا نیازهای خود استفاده کنید.

```cpp
uint64_t Aspose::Slides::BlobManagementOptions::get_MaxBlobsBytesInMemory() override
```

## توضیحات

این مقدار در صورتی که [BlobManagementOptions::set_IsTemporaryFilesAllowed](../set_istemporaryfilesallowed/) روی false تنظیم شده باشد نادیده گرفته می‌شود، زیرا در آن حالت حافظه تنها مکان ذخیره‌سازی موجود است و محدود کردن استفاده از BLOB در حافظه تأثیری ندارد.

مقدار پیش‌فرض 629,145,600 بایت (600 مگابایت) است.

می‌توانید این خصوصیت را روی صفر تنظیم کنید، اما مقدار حداقل کمی از حافظه همچنان رزرو خواهد شد. 

## موارد مرتبط

* کلاس [BlobManagementOptions](../)
* فضای‌نام [Aspose::Slides](../../)
* کتابخانه [Aspose.Slides](../../../)