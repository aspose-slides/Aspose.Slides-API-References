---
title: get_ColumnSpacing()
second_title: مرجع API Aspose.Slides برای C++
description: فضای بین ستون‌های متنی در ناحیه متن را بر می‌گرداند (بر حسب پوینت). این فقط زمانی اعمال می‌شود که بیش از یک ستون موجود باشد. این مقدار باید عددی مثبت باشد. در غیر این صورت، مقدار به صفر تنظیم خواهد شد. قابل خواندن double.
type: docs
weight: 274
url: /fa/aspose.slides/textframeformat/get_columnspacing/
---
## TextFrameFormat::get_ColumnSpacing() متد

فضای بین ستون‌های متنی در ناحیه متن را بر می‌گرداند (بر حسب پوینت). این فقط زمانی اعمال می‌شود که بیش از یک ستون وجود داشته باشد. این مقدار باید یک عدد مثبت باشد. در غیر این صورت، مقدار به صفر تنظیم خواهد شد. قابل خواندن **double**.

```cpp
double Aspose::Slides::TextFrameFormat::get_ColumnSpacing() override
```

## موارد مرتبط

* کلاس [TextFrameFormat](../)
* فضای‌نام [Aspose::Slides](../../)
* کتابخانه [Aspose.Slides](../../../)