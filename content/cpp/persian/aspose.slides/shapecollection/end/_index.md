---
title: end()
second_title: Aspose.Slides برای C++ مرجع API
description: یک تکرارگر را برمی‌گرداند که درست پس از آخرین عنصر (در صورت وجود) مجموعه اشاره می‌کند.
type: docs
weight: 599
url: /fa/aspose.slides/shapecollection/end/
---
## ShapeCollection::end() متد

یک تکرارگر را برمی‌گرداند که درست پس از آخرین عنصر (در صورت وجود) مجموعه اشاره می‌کند.

```cpp
iterator Aspose::Slides::ShapeCollection::end() noexcept
```

### مقدار بازگشتی

یک تکرارگر که درست پس از آخرین عنصر (در صورت وجود) مجموعه اشاره می‌کند

## ShapeCollection::end() const متد

یک تکرارگر را برمی‌گرداند که درست پس از آخرین عنصر (در صورت وجود) نمونه‌ی const-صفت‌شده‌ی مجموعه اشاره می‌کند.

```cpp
const_iterator Aspose::Slides::ShapeCollection::end() const noexcept
```

### مقدار بازگشتی

یک تکرارگر که درست پس از آخرین عنصر (در صورت وجود) نمونه‌ی const-صفت‌شده‌ی مجموعه اشاره می‌کند

## موارد مرتبط

* نوع‌تعریف [iterator](../iterator/)
* نوع‌تعریف [const_iterator](../const_iterator/)
* کلاس [ShapeCollection](../)
* فضای‌نام [Aspose::Slides](../../)
* کتابخانه [Aspose.Slides](../../../)