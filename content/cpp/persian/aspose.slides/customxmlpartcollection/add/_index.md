---
title: Add()
second_title: Aspose.Slides برای C++ مرجع API
description: یک بخش XML سفارشی جدید اضافه می‌کند.
type: docs
weight: 53
url: /fa/aspose.slides/customxmlpartcollection/add/
---
## CustomXmlPartCollection::Add(System::String) متد

یک بخش XML سفارشی جدید اضافه می‌کند.

```cpp
System::SharedPtr<ICustomXmlPart> Aspose::Slides::CustomXmlPartCollection::Add(System::String xmlString) override
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| xmlString | [System::String](../../../system/string/) | رشتهٔ XML بخش جدیدی که باید اضافه شود. |

### مقدار بازگشت

بخش XML سفارشی ایجاد شده.

## CustomXmlPartCollection::Add(System::ArrayPtr\<uint8_t\>) متد

یک بخش XML سفارشی جدید اضافه می‌کند.

```cpp
System::SharedPtr<ICustomXmlPart> Aspose::Slides::CustomXmlPartCollection::Add(System::ArrayPtr<uint8_t> xmlData) override
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| xmlData | [System::ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | دادهٔ XML بخش جدیدی که باید اضافه شود. |

### مقدار بازگشت

بخش XML سفارشی ایجاد شده.

## CustomXmlPartCollection::Add(System::SharedPtr\<System::IO::Stream\>) متد

یک بخش XML سفارشی جدید اضافه می‌کند.

```cpp
System::SharedPtr<ICustomXmlPart> Aspose::Slides::CustomXmlPartCollection::Add(System::SharedPtr<System::IO::Stream> inputStream) override
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| inputStream | [System::SharedPtr](../../../system/sharedptr/)\<[System::IO::Stream](../../../system.io/stream/)\> | جریان ورودی با دادهٔ XML بخش جدیدی که باید اضافه شود. |

### مقدار بازگشت

بخش XML سفارشی ایجاد شده.

## موارد مشابه

* Typedef [SharedPtr](../../../system/sharedptr/)
* Typedef [ArrayPtr](../../../system/arrayptr/)
* Class [ICustomXmlPart](../../icustomxmlpart/)
* Class [String](../../../system/string/)
* Class [CustomXmlPartCollection](../)
* Class [Stream](../../../system.io/stream/)
* Namespace [Aspose::Slides](../../)
* Library [Aspose.Slides](../../../)