---
title: set_StretchOffsetBottom()
second_title: Aspose.Slides برای C++ مرجع API
description: لبهٔ پایین مستطیل پر شدن که با یک جابجایی درصدی از لبهٔ پایین جعبه محدود شکل تعریف می‌شود، تنظیم می‌کند. درصد مثبت یک داخل‌کاری را مشخص می‌کند، در حالی که درصد منفی یک خارج‌کاری را مشخص می‌کند. مقدار float را بنویسید.
type: docs
weight: 261
url: /fa/aspose.slides/ipicturefillformat/set_stretchoffsetbottom/
---
## IPictureFillFormat::set_StretchOffsetBottom(float) متد

لبهٔ پایین مستطیل پر شدن که با یک جابجایی درصدی از لبهٔ پایین جعبه محدود شکل تعریف می‌شود، تنظیم می‌کند. یک درصد مثبت یک داخل‌کاری را مشخص می‌کند، در حالی که یک درصد منفی یک خارج‌کاری را مشخص می‌کند. مقدار **float** را بنویسید.

```cpp
virtual void Aspose::Slides::IPictureFillFormat::set_StretchOffsetBottom(float value)=0
```

## موارد مرتبط

* کلاس [IPictureFillFormat](../)
* فضای نام [Aspose::Slides](../../)
* کتابخانه [Aspose.Slides](../../../)