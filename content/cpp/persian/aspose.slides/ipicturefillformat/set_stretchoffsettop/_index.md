---
title: set_StretchOffsetTop()
second_title: Aspose.Slides برای C++ مرجع API
description: لبه بالایی مستطیل پرشده را تنظیم می‌کند که با یک فاصله درصدی از لبه بالای جعبه محدود شکل تعریف می‌شود. یک درصد مثبت یک برش داخلی را مشخص می‌کند، در حالی که یک درصد منفی یک برش بیرونی را مشخص می‌کند. بنویسید float.
type: docs
weight: 209
url: /fa/aspose.slides/ipicturefillformat/set_stretchoffsettop/
---
## IPictureFillFormat::set_StretchOffsetTop(float) متد

لبه فوقانی مستطیل پرشده را تنظیم می‌کند که با یک فاصله درصدی از لبه فوقانی جعبه محاط شکل تعریف می‌شود. یک درصد مثبت یک برش داخلی را مشخص می‌کند، در حالی که یک درصد منفی یک برش بیرونی را مشخص می‌کند. بنویسید **float**.

```cpp
virtual void Aspose::Slides::IPictureFillFormat::set_StretchOffsetTop(float value)=0
```

## موارد مرتبط

* کلاس [IPictureFillFormat](../)
* فضای‌نام [Aspose::Slides](../../)
* کتابخانه [Aspose.Slides](../../../)