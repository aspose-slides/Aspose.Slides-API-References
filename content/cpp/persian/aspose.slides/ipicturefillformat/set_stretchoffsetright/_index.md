---
title: set_StretchOffsetRight()
second_title: مرجع API Aspose.Slides برای C++
description: لبهٔ راست مستطیل پر شدن را که با یک جابجایی درصدی از لبهٔ راست جعبهٔ محدودهٔ شکل تعریف می‌شود، تنظیم می‌کند. درصد مثبت یک تو رفتگی (inset) را مشخص می‌کند، در حالی که درصد منفی یک برون‌رفت (outset) را مشخص می‌کند. float را بنویسید.
type: docs
weight: 235
url: /fa/aspose.slides/ipicturefillformat/set_stretchoffsetright/
---
## IPictureFillFormat::set_StretchOffsetRight(float) متد

لبهٔ راست مستطیل پر شدن را که با یک جابجایی درصدی از لبهٔ راست جعبهٔ محدود کنندهٔ شکل تعریف می‌شود، تنظیم می‌کند. درصد مثبت یک تو رفتگی (inset) را مشخص می‌کند، در حالی که درصد منفی یک برون‌رفت (outset) را مشخص می‌کند. مقدار **float** را می‌نویسد.

```cpp
virtual void Aspose::Slides::IPictureFillFormat::set_StretchOffsetRight(float value)=0
```

## مراجع

* کلاس [IPictureFillFormat](../)
* فضای‌نام [Aspose::Slides](../../)
* کتابخانه [Aspose.Slides](../../../)