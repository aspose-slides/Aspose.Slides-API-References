---
title: Add()
second_title: مرجع API Aspose.Slides برای C++
description: یک بخش سفارشی XML جدید اضافه می‌کند.
type: docs
weight: 14
url: /fa/aspose.slides/icustomxmlpartcollection/add/
---
## ICustomXmlPartCollection::Add(System::ArrayPtr\<uint8_t\>) متد

یک بخش سفارشی XML جدید اضافه می‌کند.

```cpp
virtual System::SharedPtr<ICustomXmlPart> Aspose::Slides::ICustomXmlPartCollection::Add(System::ArrayPtr<uint8_t> xmlData)=0
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| xmlData | [System::ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | داده‌های xml بخش جدید که باید اضافه شود. |

### مقدار بازگشت

بخش سفارشی XML ایجاد شد.

## ICustomXmlPartCollection::Add(System::String) متد

یک بخش سفارشی XML جدید اضافه می‌کند.

```cpp
virtual System::SharedPtr<ICustomXmlPart> Aspose::Slides::ICustomXmlPartCollection::Add(System::String xmlString)=0
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| xmlString | [System::String](../../../system/string/) | رشته xml بخش جدید که باید اضافه شود. |

### مقدار بازگشت

بخش سفارشی XML ایجاد شد.

## ICustomXmlPartCollection::Add(System::SharedPtr\<System::IO::Stream\>) متد

یک بخش سفارشی XML جدید اضافه می‌کند.

```cpp
virtual System::SharedPtr<ICustomXmlPart> Aspose::Slides::ICustomXmlPartCollection::Add(System::SharedPtr<System::IO::Stream> inputStream)=0
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| inputStream | [System::SharedPtr](../../../system/sharedptr/)\<[System::IO::Stream](../../../system.io/stream/)\> | جریان ورودی حاوی داده‌های xml بخش جدید که باید اضافه شود. |

### مقدار بازگشت

بخش سفارشی XML ایجاد شد.

## موارد مرتبط

* تعریف نوع [SharedPtr](../../../system/sharedptr/)
* تعریف نوع [ArrayPtr](../../../system/arrayptr/)
* کلاس [ICustomXmlPart](../../icustomxmlpart/)
* کلاس [ICustomXmlPartCollection](../)
* کلاس [String](../../../system/string/)
* کلاس [Stream](../../../system.io/stream/)
* فضای نام [Aspose::Slides](../../)
* کتابخانه [Aspose.Slides](../../../)