---
title: Increment()
second_title: Aspose.Slides برای C++ مرجع API
description: مقدار را به‌صورت اتمی افزایش می‌دهد.
type: docs
weight: 1
url: /fa/system.threading/interlocked/increment/
---
## Interlocked::Increment(int32_t\&) متد

مقدار را به‌صورت اتمی افزایش می‌دهد.

```cpp
static int32_t System::Threading::Interlocked::Increment(int32_t &location)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| location | **int32_t**\& | مرجع متغیر برای افزایش. |

### مقدار بازگشتی

مقدار متغیر بلافاصله پس از افزایش.

## Interlocked::Increment(int64_t\&) متد

مقدار را به‌صورت اتمی افزایش می‌دهد.

```cpp
static int64_t System::Threading::Interlocked::Increment(int64_t &location)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| location | **int64_t**\& | مرجع متغیر برای افزایش. |

### مقدار بازگشتی

مقدار متغیر بلافاصله پس از افزایش.

## موارد مرتبط

* کلاس [Interlocked](../)
* فضای‌نام [System::Threading](../../)
* کتابخانه [Aspose.Slides](../../../)