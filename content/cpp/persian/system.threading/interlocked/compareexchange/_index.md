---
title: CompareExchange()
second_title: Aspose.Slides برای C++ مستندات API
description: "مقدار را به صورت مقایسه‌ای در متغیر تعویض می‌کند: بررسی می‌کند که آیا متغیر برابر با مقدار خاصی است و تنها در صورتی مقدار جدید را ذخیره می‌کند که مقدار ذخیره‌شده با مقدار مورد انتظار مطابقت داشته باشد."
type: docs
weight: 79
url: /fa/system.threading/interlocked/compareexchange/
---
## Interlocked::CompareExchange(T\&, T, T) متد

مقدار را در متغیر به‌صورت مقایسه‌ای تعویض می‌کند: بررسی می‌کند که آیا متغیر برابر با مقدار خاصی است و تنها در صورتی مقدار جدید را ذخیره می‌کند که مقدار ذخیره‌شده با مقدار مورد انتظار مطابقت داشته باشد.

```cpp
template<typename T> static std::enable_if<IsSupportedInt<T>, T>::type System::Threading::Interlocked::CompareExchange(T &location1, T value, T comparand)
```

### پارامترهای قالب

| پارامتر | توضیح |
| --- | --- |
| T | نوع متغیر. |

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| location1 | T\& | مرجع متغیر برای تغییر. |
| value | T | مقدار برای ذخیره. |
| comparand | T | مقداری که پیش از تعویض با مقدار متغیر مقایسه می‌شود. |

### مقدار برگشتی

مقدار متغیر در زمان شروع عملیات صرف‌نظر از اینکه تغییر کرده باشد یا نه.

## Interlocked::CompareExchange(T\&, T, T) متد

مقدار را در متغیر به‌صورت مقایسه‌ای تعویض می‌کند: بررسی می‌کند که آیا متغیر برابر با مقدار خاصی است و تنها در صورتی مقدار جدید را ذخیره می‌کند که مقدار ذخیره‌شده با مقدار مورد انتظار مطابقت داشته باشد. پیاده‌سازی نشده.

```cpp
template<typename T> static std::enable_if<!IsSupportedInt<T>, T>::type System::Threading::Interlocked::CompareExchange(T &location1, T value, T comparand)
```

### پارامترهای قالب

| پارامتر | توضیح |
| --- | --- |
| T | نوع متغیر. |

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| location1 | T\& | مرجع متغیر برای تغییر. |
| value | T | مقدار برای ذخیره. |
| comparand | T | مقداری که پیش از تعویض با مقدار متغیر مقایسه می‌شود. |

### مقدار برگشتی

مقدار متغیر در زمان شروع عملیات صرف‌نظر از اینکه تغییر کرده باشد یا نه.

## Interlocked::CompareExchange(int32_t\&, int32_t, int32_t, bool\&) متد

مقدار را در متغیر به‌صورت مقایسه‌ای تعویض می‌کند: بررسی می‌کند که آیا متغیر برابر با مقدار خاصی است و تنها در صورتی مقدار جدید را ذخیره می‌کند که مقدار ذخیره‌شده با مقدار مورد انتظار مطابقت داشته باشد.

```cpp
static int32_t System::Threading::Interlocked::CompareExchange(int32_t &location1, int32_t value, int32_t comparand, bool &succeeded)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| location1 | **int32_t**\& | مرجع متغیر برای تغییر. |
| value | **int32_t** | مقدار برای ذخیره. |
| comparand | **int32_t** | مقداری که پیش از تعویض با مقدار متغیر مقایسه می‌شود. |
| succeeded | **bool**\& | مرجع متغیری که در صورت انجام تعویض True و در غیر این صورت False می‌شود. |

### مقدار برگشتی

مقدار متغیر در زمان شروع عملیات صرف‌نظر از اینکه تغییر کرده باشد یا نه.

## موارد مرتبط

* کلاس [Interlocked](../)
* فضای نام [System::Threading](../../)
* کتابخانه [Aspose.Slides](../../../)