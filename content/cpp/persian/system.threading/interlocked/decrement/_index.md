---
title: Decrement()
second_title: مرجع API Aspose.Slides برای C++
description: مقدار را به‌صورت اتمی کاهش می‌دهد.
type: docs
weight: 14
url: /fa/system.threading/interlocked/decrement/
---
## Interlocked::Decrement(int32_t\&) متد

مقدار را به‌صورت اتمی کاهش می‌دهد.

```cpp
static int32_t System::Threading::Interlocked::Decrement(int32_t &location)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| location | **int32_t**\& | مرجع متغیر برای کاهش. |

### مقدار بازگشتی

مقدار متغیر بلافاصله پس از کاهش آن.

## Interlocked::Decrement(int64_t\&) متد

مقدار را به‌صورت اتمی کاهش می‌دهد.

```cpp
static int64_t System::Threading::Interlocked::Decrement(int64_t &location)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| location | **int64_t**\& | مرجع متغیر برای کاهش. |

### مقدار بازگشتی

مقدار متغیر بلافاصله پس از کاهش آن.

## موارد مرتبط

* کلاس [Interlocked](../)
* فضای نام [System::Threading](../../)
* کتابخانه [Aspose.Slides](../../../)