---
title: "System::Net::Http"
second_title: مرجع API Aspose.Slides برای C++
description: 
type: docs
weight: 677
url: /fa/system.net.http/
---
## کلاس‌ها

| کلاس | توضیح |
| --- | --- |
| [ByteArrayContent](./bytearraycontent/) | محتوای HTTP را به‌صورت آرایه بایت نمایش می‌دهد. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا نقص‌های اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای عبور به‌عنوان آرگومان به توابع استفاده کنید. |
| [Details_HttpRequestException](./details_httprequestexception/) | کلاس استثنای پایه توسط کلاس‌های [HttpClient](./httpclient/) و [HttpMessageHandler](./httpmessagehandler/) پرتاب می‌شود. هرگز نمونه‌های این کلاس را به‌صورت دستی ایجاد نکنید. به‌جای آن از کلاس HttpRequestException استفاده کنید. هرگز نمونه‌های کلاس HttpRequestException را در [System::SmartPtr](../system/smartptr/) بسته‌بندی نکنید. |
| [HttpClient](./httpclient/) | یک کلاس پایه برای یک کلاینت HTTP که درخواست‌ها را ارسال و پاسخ‌ها را دریافت می‌کند، نمایش می‌دهد. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا نقص‌های اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای عبور به‌عنوان آرگومان به توابع استفاده کنید. |
| [HttpClientHandler](./httpclienthandler/) | نمایندهٔ پردازشگر پیام پیش‌فرض استفاده‌شده توسط کلاس [HttpClient](./httpclient/) است. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا نقص‌های اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای عبور به‌عنوان آرگومان به توابع استفاده کنید. |
| [HttpContent](./httpcontent/) | محتوای یک موجودیت HTTP را نمایش می‌دهد. [Object](../system/object/) این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص داده شود. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا نقص‌های اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای عبور به‌عنوان آرگومان به توابع استفاده کنید. |
| [HttpMessageHandler](./httpmessagehandler/) | یک نوع پایه برای پردازشگرهای پیام HTTP را نمایش می‌دهد. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا نقص‌های اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای عبور به‌عنوان آرگومان به توابع استفاده کنید. |
| [HttpMessageInvoker](./httpmessageinvoker/) | به برنامه‌ها اجازه می‌دهد متد Send را در زنجیرهٔ پردازشگر HTTP فراخوانی کنند. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا نقص‌های اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای عبور به‌عنوان آرگومان به توابع استفاده کنید. |
| [HttpMethod](./httpmethod/) | یک متد HTTP را نمایش می‌دهد. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا نقص‌های اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای عبور به‌عنوان آرگومان به توابع استفاده کنید. |
| [HttpRequestMessage](./httprequestmessage/) | یک پیام درخواست HTTP را نمایش می‌دهد. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا نقص‌های اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای عبور به‌عنوان آرگومان به توابع استفاده کنید. |
| [HttpResponseMessage](./httpresponsemessage/) | یک پیام پاسخ HTTP را نمایش می‌دهد. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا نقص‌های اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای عبور به‌عنوان آرگومان به توابع استفاده کنید. |
| [HttpUtilities](./httputilities/) | متدهای کمکی را شامل می‌شود. |
| [StringContent](./stringcontent/) | محتوای HTTP را به‌صورت رشته نمایش می‌دهد. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا نقص‌های اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای عبور به‌عنوان آرگومان به توابع استفاده کنید. |

## توابع

| تابع | توضیح |
| --- | --- |
| **bool** [operator==](./operator_equal_equal/)([System::SharedPtr](../system/sharedptr/)\<[HttpMethod](./httpmethod/)\>, [System::SharedPtr](../system/sharedptr/)\<[HttpMethod](./httpmethod/)\>) |  |
| **bool** [operator!=](./operator_not_equal/)([System::SharedPtr](../system/sharedptr/)\<[HttpMethod](./httpmethod/)\>, [System::SharedPtr](../system/sharedptr/)\<[HttpMethod](./httpmethod/)\>) |  |

## شمارش‌ها

| شمارشی | توضیح |
| --- | --- |
| [HttpCompletionOption](./httpcompletionoption/) | نشان می‌دهد چه زمانی یک عملیات [HttpClient](./httpclient/) باید تکمیل شود. |
| [HttpParseResult](./httpparseresult/) | نتیجهٔ تجزیه را نشان می‌دهد. |

## تعریف‌نوع‌ها

| تعریف‌نوع | توضیح |
| --- | --- |
| [HttpRequestException](./httprequestexception/) |  |