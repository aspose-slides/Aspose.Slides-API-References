---
title: "System::Drawing::Imaging"
second_title: "Aspose.Slides برای C++ مرجع API"
description: 
type: docs
weight: 508
url: /fa/system.drawing.imaging/
---
## کلاس‌ها

| Class | Description |
| --- | --- |
| [BitmapData](./bitmapdata/) | نمایش‌دهندهٔ مجموعه‌ای از ویژگی‌های یک تصویر بیت‌مپ است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز یک نمونه از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گری [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال آن به توابع به عنوان آرگومان استفاده کنید. |
| [ColorMap](./colormap/) | نمایش‌دهندهٔ نقشه‌ای برای تبدیل رنگ‌ها است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز یک نمونه از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گری [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال آن به توابع به عنوان آرگومان استفاده کنید. |
| [ColorMatrix](./colormatrix/) | نمایش‌دهندهٔ ماتریس 5×5 که مختصات فضای رنگی RGBAW را شامل می‌شود است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز یک نمونه از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گری [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال آن به توابع به عنوان آرگومان استفاده کنید. |
| [ColorPalette](./colorpalette/) | نمایش‌دهندهٔ مجموعه‌ای از رنگ‌های 32-بیتی ARGB که پالت رنگی را تشکیل می‌دهند است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز یک نمونه از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گری [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال آن به توابع به عنوان آرگومان استفاده کنید. |
| [Encoder](./encoder/) | نمایش‌دهندهٔ GUIDی که با مجموعه‌ای از پارامترهای رمزگذار تصویر مرتبط است است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز یک نمونه از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گری [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال آن به توابع به عنوان آرگومان استفاده کنید. |
| [EncoderParameter](./encoderparameter/) | به عنوان یک محفظه برای انتقال مقادیر به رمزگذار تصویر عمل می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز یک نمونه از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گری [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال آن به توابع به عنوان آرگومان استفاده کنید. |
| [EncoderParameters](./encoderparameters/) | نمایش‌دهندهٔ آرایه‌ای از اشیاء [EncoderParameter](./encoderparameter/) است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز یک نمونه از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گری [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال آن به توابع به عنوان آرگومان استفاده کنید. |
| [FrameDimension](./framedimension/) | ویژگی‌هایی را فراهم می‌کند که ابعاد فریم تصویر را دریافت می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز یک نمونه از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گری [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال آن به توابع به عنوان آرگومان استفاده کنید. |
| [ImageAttributes](./imageattributes/) | اطلاعاتی در مورد نحوهٔ دستکاری رنگ‌های تصویر در حین رندرینگ ارائه می‌دهد. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز یک نمونه از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گری [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال آن به توابع به عنوان آرگومان استفاده کنید. |
| [ImageCodecInfo](./imagecodecinfo/) | اطلاعاتی دربارهٔ یک کدک تصویر فراهم می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز یک نمونه از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گری [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال آن به توابع به عنوان آرگومان استفاده کنید. |
| [ImageFormat](./imageformat/) | نمایش‌دهندهٔ فرمت فایل یک تصویر است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز یک نمونه از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گری [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال آن به توابع به عنوان آرگومان استفاده کنید. |
| [Metafile](./metafile/) | نمایش‌دهندهٔ یک متافایل گرافیکی است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز یک نمونه از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گری [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال آن به توابع به عنوان آرگومان استفاده کنید. |
| [MetafileHeader](./metafileheader/) | شامل ویژگی‌های مرتبط با یک متافایل گرافیکی است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز یک نمونه از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گری [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال آن به توابع به عنوان آرگومان استفاده کنید. |
| [PropertyItem](./propertyitem/) | نمایش‌دهندهٔ یک ویژگی متادیتا برای گنجاندن در یک فایل تصویر است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز یک نمونه از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اعتبارسنجی می‌شود. همیشه این کلاس را در اشاره‌گری [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال آن به توابع به عنوان آرگومان استفاده کنید. |
## شمارش‌ها

| Enum | Description |
| --- | --- |
| [ColorAdjustType](./coloradjusttype/) | مشخص می‌کند کدام اشیاء از اطلاعات تنظیم رنگ استفاده می‌کنند. |
| [ColorChannelFlag](./colorchannelflag/) | یک کانال رنگی در فضای رنگی CMYK را مشخص می‌کند. |
| [ColorMatrixFlag](./colormatrixflag/) | انواع تصویر و رنگی که تحت تنظیمات تنظیم رنگ و مقیاس‌گیری خاکستری یک شیء [ImageAttributes](./imageattributes/) قرار می‌گیرند را مشخص می‌کند. |
| [EmfPlusRecordType](./emfplusrecordtype/) | روش‌هایی را که می‌توان با یک متافایل برای خواندن و نوشتن دستورات گرافیکی استفاده کرد، نشان می‌دهد. |
| [EmfType](./emftype/) | انواع رکوردهایی را که در یک فایل EMF قرار می‌گیرند، مشخص می‌کند. |
| [EncoderParameterValueType](./encoderparametervaluetype/) | نوع مقداری را که توسط کلاس [EncoderParameter](./encoderparameter/) نمایان می‌شود، مشخص می‌کند. |
| [EncoderValue](./encodervalue/) | مقدار پارامتر پاس داده شده به یک رمزگذار تصویر JPEG یا TIFF را مشخص می‌کند. |
| [ImageFlags](./imageflags/) | ویژگی‌های دادهٔ پیکسل که توسط شیء [Image](../system.drawing/image/) نمایان می‌شود را توصیف می‌کند. |
| [ImageLockMode](./imagelockmode/) | ویژگی‌های ناحیه‌ای از تصویر که قفل شده است را مشخص می‌کند. |
| [MetafileFrameUnit](./metafileframeunit/) | واحد اندازه‌گیری مستطیل استفاده شده برای اندازه‌گذاری و موقعیت‌دهی به یک متافایل را مشخص می‌کند. |
| [MetafileType](./metafiletype/) | یک نوع متافایل گرافیکی را نمایش می‌دهد. |
| [PixelFormat](./pixelformat/) | قالب دادهٔ رنگی یک پیکسل را مشخص می‌کند. |
## typedefها

| Typedef | Description |
| --- | --- |
| [BitmapDataPtr](./bitmapdataptr/) | یک نام مستعار برای اشاره‌گر مشترک به یک نمونه از کلاس [BitmapData](./bitmapdata/) است. |
| [ColorMapPtr](./colormapptr/) | یک نام مستعار برای اشاره‌گر مشترک به یک نمونه از کلاس [ColorMap](./colormap/) است. |
| [ColorMatrixPtr](./colormatrixptr/) | یک نام مستعار برای اشاره‌گر مشترک به یک نمونه از کلاس [ColorMatrix](./colormatrix/) است. |
| [ColorPalettePtr](./colorpaletteptr/) | یک نام مستعار برای اشاره‌گر مشترک به یک نمونه از کلاس [ColorPalette](./colorpalette/) است. |
| [EncoderPtr](./encoderptr/) | یک نام مستعار برای اشاره‌گر مشترک به یک نمونه از کلاس [Encoder](./encoder/) است. |
| [EncoderParameterPtr](./encoderparameterptr/) | یک نام مستعار برای اشاره‌گر مشترک به یک نمونه از کلاس [EncoderParameter](./encoderparameter/) است. |
| [EncoderParametersPtr](./encoderparametersptr/) | یک نام مستعار برای اشاره‌گر مشترک به یک نمونه از کلاس [EncoderParameters](./encoderparameters/) است. |
| [FrameDimensionPtr](./framedimensionptr/) | یک نام مستعار برای اشاره‌گر مشترک به یک نمونه از کلاس [FrameDimension](./framedimension/) است. |
| [ImageAttributesPtr](./imageattributesptr/) | یک نام مستعار برای اشاره‌گر مشترک به یک نمونه از کلاس [ImageAttributes](./imageattributes/) است. |
| [ImageCodecInfoPtr](./imagecodecinfoptr/) | یک نام مستعار برای اشاره‌گر مشترک به یک نمونه از کلاس [ImageCodecInfo](./imagecodecinfo/) است. |
| [ImageFormatPtr](./imageformatptr/) | یک نام مستعار برای اشاره‌گر مشترک به یک نمونه از کلاس [ImageFormat](./imageformat/) است. |
| [PlayRecordCallback](./playrecordcallback/) | یک نماینده (delegate) که به عنوان پارامتر در شیء تابع EnumerateMetafileProc استفاده می‌شود. |