---
title: EncoderParameter()
second_title: مرجع API Aspose.Slides برای C++
description: یک نمونه جدید از کلاس EncoderParameter ایجاد می‌کند.
type: docs
weight: 1
url: /fa/system.drawing.imaging/encoderparameter/encoderparameter/
---
## EncoderParameter::EncoderParameter() سازنده

یک نمونه جدید از کلاس [EncoderParameter](../) ایجاد می‌کند.

```cpp
System::Drawing::Imaging::EncoderParameter::EncoderParameter()
```

## EncoderParameter::EncoderParameter(const SharedPtr\<Encoder\>\&, uint8_t, bool) سازنده

یک نمونه جدید از کلاس [EncoderParameter](../) ایجاد می‌کند.

```cpp
System::Drawing::Imaging::EncoderParameter::EncoderParameter(const SharedPtr<Encoder> &encoder, uint8_t value, bool undefined=false)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| encoder | const [SharedPtr](../../../system/sharedptr/)\<[Encoder](../../encoder/)\>\& | دسته‌بندی پارامتر را مشخص می‌کند |
| value | **uint8_t** | یک مقدار عدد صحیح ۸ بیتی بدون علامت که توسط شیء ایجادشده نمایان می‌شود |
| undefined | **bool** | اگر **value** باید به عنوان نوع تعریف‌نشده در نظر گرفته شود true است، در غیر اینصورت false |

## EncoderParameter::EncoderParameter(const SharedPtr\<Encoder\>\&, int16_t) سازنده

یک نمونه جدید از کلاس [EncoderParameter](../) ایجاد می‌کند.

```cpp
System::Drawing::Imaging::EncoderParameter::EncoderParameter(const SharedPtr<Encoder> &encoder, int16_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| encoder | const [SharedPtr](../../../system/sharedptr/)\<[Encoder](../../encoder/)\>\& | دسته‌بندی پارامتر را مشخص می‌کند |
| value | **int16_t** | یک مقدار عدد صحیح ۱۶ بیتی که توسط شیء ایجادشده نمایان می‌شود |

## EncoderParameter::EncoderParameter(const SharedPtr\<Encoder\>\&, int64_t) سازنده

یک نمونه جدید از کلاس [EncoderParameter](../) ایجاد می‌کند.

```cpp
System::Drawing::Imaging::EncoderParameter::EncoderParameter(const SharedPtr<Encoder> &encoder, int64_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| encoder | const [SharedPtr](../../../system/sharedptr/)\<[Encoder](../../encoder/)\>\& | دسته‌بندی پارامتر را مشخص می‌کند |
| value | **int64_t** | یک مقدار عدد صحیح ۶۴ بیتی که توسط شیء ایجادشده نمایان می‌شود |

## EncoderParameter::EncoderParameter(const SharedPtr\<Encoder\>\&, int32_t) سازنده

یک نمونه جدید از کلاس [EncoderParameter](../) ایجاد می‌کند.

```cpp
System::Drawing::Imaging::EncoderParameter::EncoderParameter(const SharedPtr<Encoder> &encoder, int32_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| encoder | const [SharedPtr](../../../system/sharedptr/)\<[Encoder](../../encoder/)\>\& | دسته‌بندی پارامتر را مشخص می‌کند |
| value | **int32_t** | یک مقدار عدد صحیح ۳۲ بیتی که توسط شیء ایجادشده نمایان می‌شود |

## EncoderParameter::EncoderParameter(const SharedPtr\<Encoder\>\&, int32_t, int32_t) سازنده

یک نمونه جدید از کلاس [EncoderParameter](../) ایجاد می‌کند که یک کسر را نشان می‌دهد.

```cpp
System::Drawing::Imaging::EncoderParameter::EncoderParameter(const SharedPtr<Encoder> &encoder, int32_t numerator, int32_t denominator)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| encoder | const [SharedPtr](../../../system/sharedptr/)\<[Encoder](../../encoder/)\>\& | دسته‌بندی پارامتر را مشخص می‌کند |
| numerator | **int32_t** | صورت کسر که توسط شیء ایجادشده نمایان می‌شود |
| denominator | **int32_t** | مخرج کسر که توسط شیء ایجادشده نمایان می‌شود |

## EncoderParameter::EncoderParameter(const SharedPtr\<Encoder\>\&, int64_t, int64_t) سازنده

یک نمونه جدید از کلاس [EncoderParameter](../) ایجاد می‌کند که یک بازه از مقادیر صحیح را نشان می‌دهد.

```cpp
System::Drawing::Imaging::EncoderParameter::EncoderParameter(const SharedPtr<Encoder> &encoder, int64_t rangebegin, int64_t rangeend)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| encoder | const [SharedPtr](../../../system/sharedptr/)\<[Encoder](../../encoder/)\>\& | دسته‌بندی پارامتر را مشخص می‌کند |
| rangebegin | **int64_t** | کوچک‌ترین مقدار در بازه |
| rangeend | **int64_t** | بزرگ‌ترین مقدار در بازه |

## EncoderParameter::EncoderParameter(const SharedPtr\<Encoder\>\&, int32_t, int32_t, int32_t, int32_t) سازنده

یک نمونه جدید از کلاس [EncoderParameter](../) ایجاد می‌کند که یک بازه از کسرها را نشان می‌دهد.

```cpp
System::Drawing::Imaging::EncoderParameter::EncoderParameter(const SharedPtr<Encoder> &encoder, int32_t numerator1, int32_t demoninator1, int32_t numerator2, int32_t demoninator2)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| encoder | const [SharedPtr](../../../system/sharedptr/)\<[Encoder](../../encoder/)\>\& | دسته‌بندی پارامتر را مشخص می‌کند |
| numerator1 | **int32_t** | صورت کوچک‌ترین کسر در بازه |
| numerator2 | **int32_t** | صورت بزرگ‌ترین کسر در بازه |
| demoninator1 | **int32_t** | مخرج کوچک‌ترین کسر در بازه |
| demoninator2 | **int32_t** | مخرج بزرگ‌ترین کسر در بازه |

## EncoderParameter::EncoderParameter(const SharedPtr\<Encoder\>\&, const String\&) سازنده

یک نمونه جدید از کلاس [EncoderParameter](../) ایجاد می‌کند.

```cpp
System::Drawing::Imaging::EncoderParameter::EncoderParameter(const SharedPtr<Encoder> &encoder, const String &value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| encoder | const [SharedPtr](../../../system/sharedptr/)\<[Encoder](../../encoder/)\>\& | دسته‌بندی پارامتر را مشخص می‌کند |
| value | const [String](../../../system/string/)\& | رشته‌ای که توسط شیء ایجادشده نمایان می‌شود |

## EncoderParameter::EncoderParameter(const SharedPtr\<Encoder\>\&, const ArrayPtr\<uint8_t\>\&, bool) سازنده

یک نمونه جدید از کلاس [EncoderParameter](../) ایجاد می‌کند که یک آرایه از مقادیر را نشان می‌دهد.

```cpp
System::Drawing::Imaging::EncoderParameter::EncoderParameter(const SharedPtr<Encoder> &encoder, const ArrayPtr<uint8_t> &value, bool undefined=false)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| encoder | const [SharedPtr](../../../system/sharedptr/)\<[Encoder](../../encoder/)\>\& | دسته‌بندی پارامتر را مشخص می‌کند |
| value | const [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\>\& | آرایه‌ای از مقادیر عدد صحیح ۸ بیتی بدون علامت که توسط شیء ایجادشده نمایان می‌شود |
| undefined | **bool** | اگر مقادیر موجود در آرایه **value** باید به عنوان نوع تعریف‌نشده در نظر گرفته شوند true است، در غیر اینصورت false |

## EncoderParameter::EncoderParameter(const SharedPtr\<Encoder\>\&, const ArrayPtr\<int16_t\>\&) سازنده

یک نمونه جدید از کلاس [EncoderParameter](../) ایجاد می‌کند که یک آرایه از مقادیر را نشان می‌دهد.

```cpp
System::Drawing::Imaging::EncoderParameter::EncoderParameter(const SharedPtr<Encoder> &encoder, const ArrayPtr<int16_t> &value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| encoder | const [SharedPtr](../../../system/sharedptr/)\<[Encoder](../../encoder/)\>\& | دسته‌بندی پارامتر را مشخص می‌کند |
| value | const [ArrayPtr](../../../system/arrayptr/)\<**int16_t**\>\& | آرایه‌ای از مقادیر عدد صحیح ۱۶ بیتی که توسط شیء ایجادشده نمایان می‌شود |

## EncoderParameter::EncoderParameter(const SharedPtr\<Encoder\>\&, const ArrayPtr\<int64_t\>\&) سازنده

یک نمونه جدید از کلاس [EncoderParameter](../) ایجاد می‌کند که یک آرایه از مقادیر را نشان می‌دهد.

```cpp
System::Drawing::Imaging::EncoderParameter::EncoderParameter(const SharedPtr<Encoder> &encoder, const ArrayPtr<int64_t> &value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| encoder | const [SharedPtr](../../../system/sharedptr/)\<[Encoder](../../encoder/)\>\& | دسته‌بندی پارامتر را مشخص می‌کند |
| value | const [ArrayPtr](../../../system/arrayptr/)\<**int64_t**\>\& | آرایه‌ای از مقادیر عدد صحیح ۶۴ بیتی که توسط شیء ایجادشده نمایان می‌شود |

## EncoderParameter::EncoderParameter(const SharedPtr\<Encoder\>\&, const ArrayPtr\<int32_t\>\&, const ArrayPtr\<int32_t\>\&) سازنده

یک نمونه جدید از کلاس [EncoderParameter](../) ایجاد می‌کند که یک آرایه از کسرها را نشان می‌دهد.

```cpp
System::Drawing::Imaging::EncoderParameter::EncoderParameter(const SharedPtr<Encoder> &encoder, const ArrayPtr<int32_t> &numerator, const ArrayPtr<int32_t> &denominator)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| encoder | const [SharedPtr](../../../system/sharedptr/)\<[Encoder](../../encoder/)\>\& | دسته‌بندی پارامتر را مشخص می‌کند |
| numerator | const [ArrayPtr](../../../system/arrayptr/)\<**int32_t**\>\& | آرایه‌ای از صورت‌های کسرها که توسط شیء ایجادشده نمایان می‌شود |
| denominator | const [ArrayPtr](../../../system/arrayptr/)\<**int32_t**\>\& | آرایه‌ای از مخرج‌های کسرها که توسط شیء ایجادشده نمایان می‌شود |

## EncoderParameter::EncoderParameter(const SharedPtr\<Encoder\>\&, const ArrayPtr\<int64_t\>\&, const ArrayPtr\<int64_t\>\&) سازنده

یک نمونه جدید از کلاس [EncoderParameter](../) ایجاد می‌کند که یک آرایه از بازه‌های عدد صحیح را نشان می‌دهد.

```cpp
System::Drawing::Imaging::EncoderParameter::EncoderParameter(const SharedPtr<Encoder> &encoder, const ArrayPtr<int64_t> &rangebegin, const ArrayPtr<int64_t> &rangeend)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| encoder | const [SharedPtr](../../../system/sharedptr/)\<[Encoder](../../encoder/)\>\& | دسته‌بندی پارامتر را مشخص می‌کند |
| rangebegin | const [ArrayPtr](../../../system/arrayptr/)\<**int64_t**\>\& | آرایه‌ای که شامل کوچک‌ترین مقادیر هر بازه است |
| rangeend | const [ArrayPtr](../../../system/arrayptr/)\<**int64_t**\>\& | آرایه‌ای که شامل بزرگ‌ترین مقادیر هر بازه است |

## EncoderParameter::EncoderParameter(const SharedPtr\<Encoder\>\&, const ArrayPtr\<int32_t\>\&, const ArrayPtr\<int32_t\>\&, const ArrayPtr\<int32_t\>\&, const ArrayPtr\<int32_t\>\&) سازنده

یک نمونه جدید از کلاس [EncoderParameter](../) ایجاد می‌کند که یک آرایه از بازه‌های کسرها را نشان می‌دهد.

```cpp
System::Drawing::Imaging::EncoderParameter::EncoderParameter(const SharedPtr<Encoder> &encoder, const ArrayPtr<int32_t> &numerator1, const ArrayPtr<int32_t> &denominator1, const ArrayPtr<int32_t> &numerator2, const ArrayPtr<int32_t> &denominator2)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| encoder | const [SharedPtr](../../../system/sharedptr/)\<[Encoder](../../encoder/)\>\& | دسته‌بندی پارامتر را مشخص می‌کند |
| numerator1 | const [ArrayPtr](../../../system/arrayptr/)\<**int32_t**\>\& | آرایه‌ای که شامل صورت‌های کوچک‌ترین کسرهای هر بازه است |
| numerator2 | const [ArrayPtr](../../../system/arrayptr/)\<**int32_t**\>\& | آرایه‌ای که شامل صورت‌های بزرگ‌ترین کسرهای هر بازه است |
| denominator1 | const [ArrayPtr](../../../system/arrayptr/)\<**int32_t**\>\& | آرایه‌ای که شامل مخرج‌های کوچک‌ترین کسرهای هر بازه است |
| denominator2 | const [ArrayPtr](../../../system/arrayptr/)\<**int32_t**\>\& | آرایه‌ای که شامل مخرج‌های بزرگ‌ترین کسرهای هر بازه است |

## EncoderParameter::EncoderParameter(const SharedPtr\<Encoder\>\&, int, EncoderParameterValueType, void *) سازنده

یک نمونه جدید از کلاس [EncoderParameter](../) ایجاد می‌کند که تعداد مشخصی از مقادیر از نوع مشخص را که از بافر مشخص خوانده می‌شوند، نشان می‌دهد.

```cpp
System::Drawing::Imaging::EncoderParameter::EncoderParameter(const SharedPtr<Encoder> &encoder, int numberValues, EncoderParameterValueType type, void *value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| encoder | const [SharedPtr](../../../system/sharedptr/)\<[Encoder](../../encoder/)\>\& | دسته‌بندی پارامتر را مشخص می‌کند |
| numberValues | int | تعداد مقادیر را مشخص می‌کند |
| type | [EncoderParameterValueType](../../encoderparametervaluetype/) | نوع مقادیر را مشخص می‌کند |
| value | void * | اشاره‌گری به بافر برای خواندن مقادیر از آن |

## مراجع

* Enum [EncoderParameterValueType](../../encoderparametervaluetype/)
* Typedef [SharedPtr](../../../system/sharedptr/)
* Typedef [ArrayPtr](../../../system/arrayptr/)
* Class [EncoderParameter](../)
* Class [Encoder](../../encoder/)
* Class [String](../../../system/string/)
* Namespace [System::Drawing::Imaging](../../)
* Library [Aspose.Slides](../../../)