---
title: operator[]()
second_title: مرجع API Aspose.Slides برای C++
description: عملگر دسترسی برای کار با تبدیل نوع کلید.
type: docs
weight: 14
url: /fa/system.collections.generic/dictionaryptr/operator[]/
---
## DictionaryPtr::operator[](const X\&) متد const

عملگر دسترسی برای کار با تبدیل نوع کلید.

```cpp
template<class X> V & System::Collections::Generic::DictionaryPtr<T, V>::operator[](const X &key) const
```

### پارامترهای قالب

| پارامتر | توضیح |
| --- | --- |
| X | نوع کلید منبع. |

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| key | const X\& | [Dictionary](../../dictionary/) کلید. |

### مقدار بازگشتی

مرجع به مقدار مربوط به کلید ارسال‌شده، موجود یا تازه ایجاد شده.

## DictionaryPtr::operator[](const T\&) متد const

عملگر دسترسی.

```cpp
V & System::Collections::Generic::DictionaryPtr<T, V>::operator[](const T &key) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| key | const T\& | [Dictionary](../../dictionary/) کلید. |

### مقدار بازگشتی

مرجع به مقدار مربوط به کلید ارسال‌شده، موجود یا تازه ایجاد شده.

## موارد مرتبط

* کلاس [DictionaryPtr](../)
* فضای‌نام [System::Collections::Generic](../../)
* کتابخانه [Aspose.Slides](../../../)