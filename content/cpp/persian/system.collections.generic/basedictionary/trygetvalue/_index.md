---
title: TryGetValue()
second_title: مرجع API Aspose.Slides برای C++
description: به دنبال مقدار کلیدی می‌گردد و در صورت یافتن آن را باز می‌گرداند.
type: docs
weight: 144
url: /fa/system.collections.generic/basedictionary/trygetvalue/
---
## BaseDictionary::TryGetValue(const key_t&, mapped_t&) const متد

به دنبال مقدار کلیدی می‌گردد و در صورت یافتن آن را باز می‌گرداند.

```cpp
bool System::Collections::Generic::BaseDictionary<Map>::TryGetValue(const key_t &key, mapped_t &value) const override
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| key | const key_t& | کلید برای جستجو. |
| value | mapped_t& | مرجع برای ذخیره مقدار یافت شده. |

### مقدار برگشتی

true اگر مقدار یافت شده و بازگردانده شود، false در غیر این صورت.

## موارد مرتبط

* کلاس [BaseDictionary](../)
* فضای‌نام [System::Collections::Generic](../../)
* کتابخانه [Aspose.Slides](../../../)