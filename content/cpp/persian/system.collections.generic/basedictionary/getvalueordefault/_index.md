---
title: GetValueOrDefault()
second_title: Aspose.Slides برای C++ مرجع API
description: اگر مقدار یافت شد، مقدار را برمی‌گرداند؛ در غیر این صورت Value() را برمی‌گرداند.
type: docs
weight: 196
url: /fa/system.collections.generic/basedictionary/getvalueordefault/
---
## BaseDictionary::GetValueOrDefault(const key_t\&) const متد

اگر مقدار پیدا شد، مقدار را برمی‌گرداند؛ در غیر این صورت **Value()** را برمی‌گرداند.

```cpp
mapped_t System::Collections::Generic::BaseDictionary<Map>::GetValueOrDefault(const key_t &key) const override
```
## BaseDictionary::GetValueOrDefault(const key_t\&, const mapped_t\&) const متد

اگر مقدار پیدا شد، مقدار را برمی‌گرداند؛ در غیر این صورت **defaultValue** را برمی‌گرداند.

```cpp
mapped_t System::Collections::Generic::BaseDictionary<Map>::GetValueOrDefault(const key_t &key, const mapped_t &defaultValue) const override
```
## موارد مرتبط

* کلاس [BaseDictionary](../)
* فضای نام [System::Collections::Generic](../../)
* کتابخانه [Aspose.Slides](../../../)