---
title: BaseDictionary()
second_title: مرجع API Aspose.Slides برای C++
description: ساختار داده خالی ایجاد می‌کند.
type: docs
weight: 14
url: /fa/system.collections.generic/basedictionary/basedictionary/
---
## BaseDictionary::BaseDictionary() سازنده

یک ساختار دادهٔ خالی ایجاد می‌کند.

```cpp
System::Collections::Generic::BaseDictionary<Map>::BaseDictionary()
```

## BaseDictionary::BaseDictionary(int, const Args\&...) سازنده

سازندهٔ انتقالی برای ارسال آرگومان‌ها به سازندهٔ نقشهٔ پایه.

```cpp
template<class...> System::Collections::Generic::BaseDictionary<Map>::BaseDictionary(int, const Args &... args)
```

### پارامترهای قالب

| پارامتر | توضیح |
| --- | --- |
| Args | نوع آرگومان‌ها برای ارسال به نقشه. |

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| args | int | آرگومان‌ها برای ارسال به نقشهٔ پایه. |

## BaseDictionary::BaseDictionary(BaseType *, const Args\&...) سازنده

سازندهٔ کپی.

```cpp
template<class...> System::Collections::Generic::BaseDictionary<Map>::BaseDictionary(BaseType *src, const Args &... args)
```

### پارامترهای قالب

| پارامتر | توضیح |
| --- | --- |
| Args | نوع آرگومان‌های سازندهٔ نقشه. |

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| src | [BaseType](../basetype/) * | برای کپی داده‌ها از [Object](../../../system/object/). |
| args | const Args\&... | آرگومان‌ها برای ارسال به سازندهٔ نقشهٔ پایه. |

## BaseDictionary::BaseDictionary(BaseType *) سازنده

سازندهٔ کپی.

```cpp
System::Collections::Generic::BaseDictionary<Map>::BaseDictionary(BaseType *src)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| src | [BaseType](../basetype/) * | برای کپی داده‌ها از [Object](../../../system/object/). |

## موارد مرتبط

* تعریف نوع [BaseType](../basetype/)
* کلاس [BaseDictionary](../)
* فضای نام [System::Collections::Generic](../../)
* کتابخانه [Aspose.Slides](../../../)