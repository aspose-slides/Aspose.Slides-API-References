---
title: ContainsValue()
second_title: مرجع API Aspose.Slides برای C++
description: بررسی می‌کند که آیا مقدار در دیکشنری موجود است. برای مقایسه مقادیر از عملگر == استفاده می‌کند.
type: docs
weight: 157
url: /fa/system.collections.generic/basedictionary/containsvalue/
---
## BaseDictionary::ContainsValue(const mapped_t\&) متد


بررسی می‌کند که آیا مقدار در دیکشنری وجود دارد یا نه. برای مقایسه مقادیر از عملگر == استفاده می‌کند.

```cpp
bool System::Collections::Generic::BaseDictionary<Map>::ContainsValue(const mapped_t &value)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const mapped_t\& | مقداری که باید جستجو شود. |

### مقدار بازگشت

true اگر مقدار یافت شود، در غیر این صورت false.

## موارد مرتبط

* کلاس [BaseDictionary](../)
* فضای نام [System::Collections::Generic](../../)
* کتابخانه [Aspose.Slides](../../../)