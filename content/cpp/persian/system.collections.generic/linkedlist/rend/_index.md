---
title: rend()
second_title: مرجع API Aspose.Slides برای C++
description: یک پیمایشگر معکوس برای عنصر غیرموجود قبل از شروع مجموعه برمی‌گرداند.
type: docs
weight: 300
url: /fa/system.collections.generic/linkedlist/rend/
---
## LinkedList::rend() متد

یک پیمایشگر معکوس برای عنصر غیرموجود پیش از شروع مجموعه برمی‌گرداند.

```cpp
reverse_iterator System::Collections::Generic::LinkedList<T>::rend() noexcept
```

### مقدار بازگشت

یک پیمایشگر معکوس که به عنصر نظری قبل از اولین عنصر در فهرست پیوندی اشاره می‌کند (آخرین در جهت معکوس).

## LinkedList::rend() const متد

یک پیمایشگر معکوس برای عنصر غیرموجود پیش از شروع مجموعهٔ ثابت‌صفت برمی‌گرداند.

```cpp
const_reverse_iterator System::Collections::Generic::LinkedList<T>::rend() const noexcept
```

### مقدار بازگشت

یک پیمایشگر معکوس که به عنصر نظری قبل از اولین عنصر در فهرست پیوندی ثابت‌صفت اشاره می‌کند (آخرین در جهت معکوس).

## موارد مرتبط

* نوع‌تعریف [reverse_iterator](../reverse_iterator/)
* نوع‌تعریف [const_reverse_iterator](../const_reverse_iterator/)
* کلاس [LinkedList](../)
* فضای‌نام [System::Collections::Generic](../../)
* کتابخانه [Aspose.Slides](../../../)