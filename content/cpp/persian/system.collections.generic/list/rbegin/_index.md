---
title: rbegin()
second_title: Aspose.Slides برای مرجع API C++
description: یک iterator معکوس به آخرین عنصر مجموعه (اولین در حالت معکوس) را دریافت می‌کند.
type: docs
weight: 79
url: /fa/system.collections.generic/list/rbegin/
---
## List::rbegin() متد

یک iterator معکوس به آخرین عنصر مجموعه (اولین در حالت معکوس) را دریافت می‌کند.

```cpp
reverse_iterator System::Collections::Generic::List<T>::rbegin() noexcept
```

### مقدار بازگشت

یک iterator معکوس که به آخرین عنصر لیست اشاره می‌کند (اولین در حالت معکوس).

## List::rbegin() const متد

یک iterator معکوس به آخرین عنصر مجموعه const-qualified (اولین در حالت معکوس) را دریافت می‌کند.

```cpp
const_reverse_iterator System::Collections::Generic::List<T>::rbegin() const noexcept
```

### مقدار بازگشت

یک iterator معکوس که به آخرین عنصر لیست const-qualified اشاره می‌کند (اولین در حالت معکوس).

## موارد مرتبط

* Typedef [reverse_iterator](../reverse_iterator/)
* Typedef [const_reverse_iterator](../const_reverse_iterator/)
* کلاس [List](../)
* فضای‌نام [System::Collections::Generic](../../)
* کتابخانه [Aspose.Slides](../../../)