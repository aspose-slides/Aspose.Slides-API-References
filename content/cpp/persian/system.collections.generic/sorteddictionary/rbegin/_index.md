---
title: rbegin()
second_title: Aspose.Slides برای C++ مرجع API
description: یک تکرارگر معکوس به آخرین عنصر مجموعه (اول در حالت معکوس) برمی‌گرداند.
type: docs
weight: 53
url: /fa/system.collections.generic/sorteddictionary/rbegin/
---
## SortedDictionary::rbegin() متد

یک تکرارگر معکوس به آخرین عنصر مجموعه (اول در حالت معکوس) برمی‌گرداند.

```cpp
reverse_iterator System::Collections::Generic::SortedDictionary<TKey, TValue>::rbegin() noexcept
```

### مقدار بازگشت

یک تکرارگر معکوس که به آخرین عنصر در نقشه اشاره می‌کند (اول در حالت معکوس).

## SortedDictionary::rbegin() const متد

یک تکرارگر معکوس به آخرین عنصر مجموعه با ویژگی const (اول در حالت معکوس) برمی‌گرداند.

```cpp
const_reverse_iterator System::Collections::Generic::SortedDictionary<TKey, TValue>::rbegin() const noexcept
```

### مقدار بازگشت

یک تکرارگر معکوس که به آخرین عنصر در نقشه با ویژگی const اشاره می‌کند (اول در حالت معکوس).

## موارد مرتبط

* تعریف نوع [reverse_iterator](../reverse_iterator/)
* تعریف نوع [const_reverse_iterator](../const_reverse_iterator/)
* کلاس [SortedDictionary](../)
* فضای‌نام [System::Collections::Generic](../../)
* کتابخانه [Aspose.Slides](../../../)