---
title: TryGetValue()
second_title: Aspose.Slides برای C++ مرجع API
description: به دنبال مقدار می‌گردد و در صورت یافتن آن را بازیابی می‌کند.
type: docs
weight: 40
url: /fa/system.collections.generic/idictionary/trygetvalue/
---
## IDictionary::TryGetValue(const TKey&, TValue&) const متد


به دنبال مقدار می‌گردد و در صورت پیدا شدن آن را بازیابی می‌کند.

```cpp
virtual bool System::Collections::Generic::IDictionary<TKey, TValue>::TryGetValue(const TKey &key, TValue &value) const =0
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| key | const TKey& | کلید برای جستجو. |
| value | TValue& | مرجع برای ذخیره مقدار بازیابی شده. |

### مقدار برگشت

در صورتی که مقدار یافت شده و بازیابی شده باشد True و در غیر این صورت false.

## موارد مرتبط

* کلاس [IDictionary](../)
* فضای نام [System::Collections::Generic](../../)
* کتابخانه [Aspose.Slides](../../../)