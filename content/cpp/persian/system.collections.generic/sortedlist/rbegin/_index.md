---
title: rbegin()
second_title: مرجع API Aspose.Slides برای C++
description: یک تکرارگر معکوس به آخرین عنصر مجموعه (اولین در حالت معکوس) برمی‌گرداند.
type: docs
weight: 92
url: /fa/system.collections.generic/sortedlist/rbegin/
---
## SortedList::rbegin() متد

یک تکرارگر معکوس به آخرین عنصر مجموعه (اولین در حالت معکوس) برمی‌گرداند.

```cpp
reverse_iterator System::Collections::Generic::SortedList<TKey, TValue>::rbegin() noexcept
```

### مقدار بازگشتی

یک تکرارگر معکوس که به آخرین عنصر نقشه اشاره می‌کند (اولین در حالت معکوس).

## SortedList::rbegin() const متد

یک تکرارگر معکوس به آخرین عنصر مجموعه‌ای با ویژگی const (اولین در حالت معکوس) برمی‌گرداند.

```cpp
const_reverse_iterator System::Collections::Generic::SortedList<TKey, TValue>::rbegin() const noexcept
```

### مقدار بازگشتی

یک تکرارگر معکوس که به آخرین عنصر نقشه‌ای با ویژگی const اشاره می‌کند (اولین در حالت معکوس).

## موارد مرتبط

* Typedef [reverse_iterator](../reverse_iterator/)
* Typedef [const_reverse_iterator](../const_reverse_iterator/)
* کلاس [SortedList](../)
* فضای‌نام [System::Collections::Generic](../../)
* Library [Aspose.Slides](../../../)