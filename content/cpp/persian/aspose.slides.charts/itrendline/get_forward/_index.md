---
title: get_Forward()
second_title: مرجع API Aspose.Slides برای C++
description: تعداد دسته‌ها (یا واحدها در یک نمودار پراکنده) را که خط روند پس از داده‌های سری مورد روند گسترش می‌یابد، مشخص می‌کند. در نمودارهای پراکنده و غیرپراکنده، مقدار می‌تواند هر مقدار غیر منفی باشد. خواندن double.
type: docs
weight: 105
url: /fa/aspose.slides.charts/itrendline/get_forward/
---
## ITrendline::get_Forward() متد

تعیین می‌کند تعداد دسته‌ها (یا واحدها در یک نمودار پراکنده) که خط روند پس از داده‌های سری که روند داده می‌شود، گسترش می‌یابد. در نمودارهای پراکنده و غیرپراکنده، مقدار می‌تواند هر مقدار غیر منفی باشد. خواندن **double**.

```cpp
virtual double Aspose::Slides::Charts::ITrendline::get_Forward()=0
```

## همچنین ببینید

* کلاس [ITrendline](../)
* فضای‌نام [Aspose::Slides::Charts](../../)
* Library [Aspose.Slides](../../../)