---
title: AddDataPointForPieSeries()
second_title: مرجع API Aspose.Slides برای C++
description: یک نقطه داده جدید می‌سازد و آن را به انتهای مجموعه اضافه می‌کند. برای سری‌هایی قابل استفاده است که chartType آن‌ها یکی از زیرنوع‌های Pie باشد (همچنین متد ChartTypeCharacterizer.IsChartTypePie(ChartType) را ببینید).
type: docs
weight: 222
url: /fa/aspose.slides.charts/ichartdatapointcollection/adddatapointforpieseries/
---
## IChartDataPointCollection::AddDataPointForPieSeries(System::SharedPtr\<IChartDataCell\>) متد

یک نقطه داده جدید ایجاد می‌کند و آن را به انتهای مجموعه اضافه می‌نماید. قابل استفاده برای سری‌هایی است که chartType آن‌ها یکی از زیرنوع‌های Pie است (همچنین [ChartTypeCharacterizer.IsChartTypePie(ChartType)](../../charttypecharacterizer/ischarttypepie/) متد را ببینید).

```cpp
virtual System::SharedPtr<IChartDataPoint> Aspose::Slides::Charts::IChartDataPointCollection::AddDataPointForPieSeries(System::SharedPtr<IChartDataCell> value)=0
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | [System::SharedPtr](../../../system/sharedptr/)\<[IChartDataCell](../../ichartdatacell/)\> | مقدار نقطه داده |

### مقدار بازگشت

یک نقطه داده جدید.

## IChartDataPointCollection::AddDataPointForPieSeries(double) متد

یک نقطه داده جدید ایجاد می‌کند و آن را به انتهای مجموعه اضافه می‌نماید. قابل استفاده برای سری‌هایی است که chartType آن‌ها یکی از زیرنوع‌های Pie است (همچنین [ChartTypeCharacterizer.IsChartTypePie(ChartType)](../../charttypecharacterizer/ischarttypepie/) متد را ببینید).

```cpp
virtual System::SharedPtr<IChartDataPoint> Aspose::Slides::Charts::IChartDataPointCollection::AddDataPointForPieSeries(double value)=0
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **double** | مقدار نقطه داده |

### مقدار بازگشت

یک نقطه داده جدید.

## موارد مرتبط

* تعریف نوع [SharedPtr](../../../system/sharedptr/)
* کلاس [IChartDataPoint](../../ichartdatapoint/)
* کلاس [IChartDataCell](../../ichartdatacell/)
* کلاس [IChartDataPointCollection](../)
* فضای نام [Aspose::Slides::Charts](../../)
* کتابخانه [Aspose.Slides](../../../)