---
title: IChartSeries
second_title: مرجع API Aspose.Slides برای C++
description: نمایانگر یک سری نمودار است.
type: docs
weight: 820
url: /fa/aspose.slides.charts/ichartseries/
---
## کلاس IChartSeries

نمایش یک سری نمودار است.

```cpp
class IChartSeries : public Aspose::Slides::Charts::IChartComponent
```

## متدها

| متد | توضیحات |
| --- | --- |
| virtual **bool** [Equals](../../system/object/equals/)([ptr](../../system/object/ptr/)) | شیء‌ها را با معنای C# [Object.Equals](../../system/object/equals/) مقایسه می‌کند. |
| static std::enable_if\<[IsSmartPtr](../../system/issmartptr/)\<T1\>::value\&&[IsSmartPtr](../../system/issmartptr/)\<T2\>::value, **bool**\>::type [Equals](../../system/object/equals/)(T1 const\&, T2 const\&) | شیء‌های نوع مرجع را به سبک C# مقایسه می‌کند. |
| static std::enable_if<\![IsSmartPtr](../../system/issmartptr/)\<T1\>::value\&&\![IsSmartPtr](../../system/issmartptr/)\<T2\>::value, **bool**\>::type [Equals](../../system/object/equals/)(T1 const\&, T2 const\&) | شیء‌های نوع مقدار را به سبک C# مقایسه می‌کند. |
| static **bool** [Equals](../../system/object/equals/)(**float** const\&, **float** const\&) | شبیه‌ساز مقایسه نقطه‌عبور به سبک C# است که در آن دو NaN برابر در نظر گرفته می‌شوند، حتی اگر بر طبق IEC 60559:1989 NaN برابر با هیچ مقداری، از جمله NaN، نیست. |
| static **bool** [Equals](../../system/object/equals/)(**double** const\&, **double** const\&) | شبیه‌ساز مقایسه نقطه‌عبور به سبک C# است که در آن دو NaN برابر در نظر گرفته می‌شوند، حتی اگر بر طبق IEC 60559:1989 NaN برابر با هیچ مقداری، از جمله NaN، نیست. |
| virtual **bool** [FastCast](../../system/object/fastcast/)(const Details::FastRttiBase\&, void **) const | فقط برای مقاصد داخلی. |
| virtual [ChartShapeType](../chartshapetype/) [get_Bar3DShape](./get_bar3dshape/)() | شکل یک سری نمودار میله‌ای 3-بعدی را تعیین می‌کند. تغییر مقدار این ویژگی می‌تواند باعث تغییر خودکار نوع سری شود. خواندنی [ChartShapeType](../chartshapetype/). |
| virtual [BubbleSizeRepresentationType](../bubblesizerepresentationtype/) [get_BubbleSizeRepresentation](./get_bubblesizerepresentation/)() | نحوهٔ نمایش مقادیر اندازه حباب‌ها در نمودار حبابی را تعیین می‌کند. این ویژگی نه تنها برای این سری بلکه برای تمام سری‌های گروه سری والد است - این یک بازتاب ویژگی مناسب گروه است. به همین دلیل این ویژگی فقط‌خواندنی است. برای دسترسی به گروه سری والد از ویژگی ParentSeriesGroup استفاده کنید. برای تغییر مقدار از ویژگی [get_ParentSeriesGroup()](./get_parentseriesgroup/)->get(set)_BubbleSizeRepresentation() خواندنی/قابل‌نوشتن استفاده کنید. |
| virtual **int32_t** [get_BubbleSizeScale](./get_bubblesizescale/)() | مقیاس‌ساز اندازه برای نمودار حبابی را تعیین می‌کند (می‌تواند بین 0 تا 300 درصد اندازه پیش‌فرض باشد). این ویژگی نه تنها برای این سری بلکه برای تمام سری‌های گروه سری والد است - این یک بازتاب ویژگی مناسب گروه است. به همین دلیل این ویژگی فقط‌خواندنی است. برای دسترسی به گروه سری والد از ویژگی ParentSeriesGroup استفاده کنید. برای تغییر مقدار از ویژگی [get_ParentSeriesGroup()](./get_parentseriesgroup/)->get(set)_BubbleSizeScale() خواندنی/قابل‌نوشتن استفاده کنید. |
| virtual [System::SharedPtr](../../system/sharedptr/)\<[IChart](../ichart/)\> [get_Chart](../ichartcomponent/get_chart/)() | نمودار را برمی‌گرداند. فقط‌خواندنی [IChart](../ichart/). |
| virtual [System::SharedPtr](../../system/sharedptr/)\<[IChartDataPoint](../ichartdatapoint/)\> [get_DataPoint](./get_datapoint/)(**int32_t**) | نقطه داده‌ای این سری را در ایندکس مشخص‌شده برمی‌گرداند. |
| virtual **int32_t** [get_DataPoint](./get_datapoint/)([System::SharedPtr](../../system/sharedptr/)\<[IChartDataPoint](../ichartdatapoint/)\>) |  |
| virtual [System::SharedPtr](../../system/sharedptr/)\<[IChartDataPointCollection](../ichartdatapointcollection/)\> [get_DataPoints](./get_datapoints/)() | مجموعه‌ای از نقاط دادهٔ این سری را برمی‌گرداند. فقط‌خواندنی [IChartDataPointCollection](../ichartdatapointcollection/). |
| virtual **uint8_t** [get_DoughnutHoleSize](./get_doughnutholesize/)() | اندازهٔ سوراخ در نمودار دونات را تعیین می‌کند (می‌تواند بین 10 تا 90 درصد اندازهٔ ناحیهٔ نمودار باشد). این ویژگی نه تنها برای این سری بلکه برای تمام سری‌های گروه سری والد است - این یک بازتاب ویژگی مناسب گروه است. به همین دلیل این ویژگی فقط‌خواندنی است. برای دسترسی به گروه سری والد از ویژگی ParentSeriesGroup استفاده کنید. برای تغییر مقدار از ویژگی [get_ParentSeriesGroup()](./get_parentseriesgroup/)->get(set)_DoughnutHoleSize() خواندنی/قابل‌نوشتن استفاده کنید. فقط‌خواندنی **uint8_t**. |
| virtual [System::SharedPtr](../../system/sharedptr/)\<[IErrorBarsFormat](../ierrorbarsformat/)\> [get_ErrorBarsXFormat](./get_errorbarsxformat/)() | نمایان‌گر نوارهای خطا (ErrorBars) برای سری با جهت X. |
| virtual [System::SharedPtr](../../system/sharedptr/)\<[IErrorBarsFormat](../ierrorbarsformat/)\> [get_ErrorBarsYFormat](./get_errorbarsyformat/)() | نمایان‌گر نوارهای خطا (ErrorBars) برای سری با جهت Y. |
| virtual **int32_t** [get_Explosion](./get_explosion/)() | فاصلهٔ یک برش پیت باز از مرکز نمودار دایره‌ای به‌صورت درصدی از قطر پیت بیان می‌شود. خواندنی **int32_t**. |
| virtual **uint16_t** [get_FirstSliceAngle](./get_firstsliceangle/)() | زاویهٔ اولین برش پیت یا دونات را به درجه (ساعتگرد از بالا، 0 تا 360 درجه) تعیین می‌کند. این ویژگی نه تنها برای این سری بلکه برای تمام سری‌های گروه سری والد است - این یک بازتاب ویژگی مناسب گروه است. به همین دلیل این ویژگی فقط‌خواندنی است. برای دسترسی به گروه سری والد از ویژگی ParentSeriesGroup استفاده کنید. برای تغییر مقدار از ویژگی [get_ParentSeriesGroup()](./get_parentseriesgroup/)->get(set)_FirstSliceAngle() خواندنی/قابل‌نوشتن استفاده کنید. فقط‌خواندنی **uint16_t**. |
| virtual [System::SharedPtr](../../system/sharedptr/)\<[IFormat](../iformat/)\> [get_Format](./get_format/)() | قالب یک سری را برمی‌گرداند. فقط‌خواندنی [IFormat](../iformat/). |
| virtual **int32_t** [get_GapDepth](./get_gapdepth/)() | فاصلهٔ بین سری‌های داده در نمودار 3-بعدی را به‌صورت درصدی از عرض علامت تعیین می‌کند. این ویژگی نه تنها برای این سری بلکه برای تمام سری‌های گروه سری والد است - این یک بازتاب ویژگی مناسب گروه است. به همین دلیل این ویژگی فقط‌خواندنی است. برای دسترسی به گروه سری والد از ویژگی ParentSeriesGroup استفاده کنید. برای تغییر مقدار از ویژگی [get_ParentSeriesGroup()](./get_parentseriesgroup/)->get(set)_GapDepth() خواندنی/قابل‌نوشتن استفاده کنید. فقط‌خواندنی **int32_t**. |
| virtual **int32_t** [get_GapWidth](./get_gapwidth/)() | فضای بین خوشه‌های میله یا ستون را به‌صورت درصدی از عرض میله یا ستون تعیین می‌کند. این ویژگی نه تنها برای این سری بلکه برای تمام سری‌های گروه سری والد است - این یک بازتاب ویژگی مناسب گروه است. به همین دلیل این ویژگی فقط‌خواندنی است. برای دسترسی به گروه سری والد از ویژگی ParentSeriesGroup استفاده کنید. برای تغییر مقدار از ویژگی [get_ParentSeriesGroup()](./get_parentseriesgroup/)->get(set)_GapWidth() خواندنی/قابل‌نوشتن استفاده کنید. فقط‌خواندنی **int32_t**. |
| virtual **bool** [get_HasSeriesLines](./get_hasserieslines/)() | تعیین می‌کند آیا خطوط سری برای این سری و سری‌های مرتبط وجود دارد یا خیر. این ویژگی نه تنها برای این سری بلکه برای تمام سری‌های گروه سری والد است - این یک بازتاب ویژگی مناسب گروه است. به همین دلیل این ویژگی فقط‌خواندنی است. برای دسترسی به گروه سری والد از ویژگی ParentSeriesGroup استفاده کنید. برای تغییر مقدار از ویژگی [get_ParentSeriesGroup()](./get_parentseriesgroup/)->get(set)_HasSeriesLines() خواندنی/قابل‌نوشتن استفاده کنید. برای قالب خطوط سری از ویژگی ParentSeriesGroup.SeriesLinesFormat استفاده کنید. فقط‌خواندنی **bool**. |
| virtual **bool** [get_HasUpDownBars](./get_hasupdownbars/)() | تعیین می‌کند آیا نمودار خطی یا سهام‌گذاری دارای میله‌های بالا/پایین است یا خیر. این ویژگی نه تنها برای این سری بلکه برای تمام سری‌های گروه سری والد است - این یک بازتاب ویژگی مناسب گروه است. به همین دلیل این ویژگی فقط‌خواندنی است. برای دسترسی به گروه سری والد از ویژگی ParentSeriesGroup استفاده کنید. برای تغییر مقدار از ویژگی [get_ParentSeriesGroup()](./get_parentseriesgroup/)->get_UpDownBars()->get(set)_HasUpDownBars() خواندنی/قابل‌نوشتن استفاده کنید. برای قالب میله‌های بالا/پایین از ویژگی [get_ParentSeriesGroup()](./get_parentseriesgroup/)->get_UpDownBars() استفاده کنید. فقط‌خواندنی **bool**. |
| virtual [System::SharedPtr](../../system/sharedptr/)\<[IColorFormat](../../aspose.slides/icolorformat/)\> [get_InvertedSolidFillColor](./get_invertedsolidfillcolor/)() | رنگ جامد معکوس برای سری را تعیین می‌کند. برای اعمال تنظیم رنگ، نوع پر (FillType) قالب سری را به [FillType::Solid](../../aspose.slides/filltype/) تنظیم کنید. خواندنی [IColorFormat](../../aspose.slides/icolorformat/). |
| virtual **bool** [get_InvertIfNegative](./get_invertifnegative/)() | تعیین می‌کند آیا میله، ستون یا سری حبابی در صورت منفی بودن مقدار، رنگ‌های خود را معکوس می‌کند. خواندنی **bool**. |
| virtual **bool** [get_IsColorVaried](./get_iscolorvaried/)() | تعیین می‌کند هر نشانگر داده در سری رنگ متفاوتی داشته باشد. این ویژگی نه تنها برای این سری بلکه برای تمام سری‌های گروه سری والد است - این یک بازتاب ویژگی مناسب گروه است. به همین دلیل این ویژگی فقط‌خواندنی است. برای دسترسی به گروه سری والد از ویژگی ParentSeriesGroup استفاده کنید. برای تغییر مقدار از ویژگی [get_ParentSeriesGroup()](./get_parentseriesgroup/)->get(set)_IsColorVaried() خواندنی/قابل‌نوشتن استفاده کنید. فقط‌خواندنی **bool**. |
| virtual [System::SharedPtr](../../system/sharedptr/)\<[IDataLabel](../idatalabel/)\> [get_Label](./get_label/)(**int32_t**) | برچسب داده‌ای برای نقطه دادهٔ این سری را در ایندکس مشخص‌شده برمی‌گرداند. |
| virtual [System::SharedPtr](../../system/sharedptr/)\<[IDataLabelCollection](../idatalabelcollection/)\> [get_Labels](./get_labels/)() | برچسب‌های یک سری را برمی‌گرداند. فقط‌خواندنی [IDataLabelCollection](../idatalabelcollection/). |
| virtual [System::SharedPtr](../../system/sharedptr/)\<[IMarker](../imarker/)\> [get_Marker](./get_marker/)() | نشانگر سری را برمی‌گرداند. فقط‌خواندنی [IMarker](../imarker/). |
| virtual [System::SharedPtr](../../system/sharedptr/)\<[IStringChartValue](../istringchartvalue/)\> [get_Name](./get_name/)() | نام سری را برمی‌گرداند. فقط‌خواندنی [IStringChartValue](../istringchartvalue/). |
| virtual [System::String](../../system/string/) [get_NumberFormatOfBubbleSizes](./get_numberformatofbubblesizes/)() | قالب عددی برای اندازه‌های حباب‌های سری را برمی‌گرداند. خواندنی [System::String](../../system/string/). |
| virtual [System::String](../../system/string/) [get_NumberFormatOfValues](./get_numberformatofvalues/)() | قالب عددی برای مقادیر سری را برمی‌گرداند. خواندنی [System::String](../../system/string/). |
| virtual [System::String](../../system/string/) [get_NumberFormatOfXValues](./get_numberformatofxvalues/)() | قالب عددی برای مقادیر x سری را برمی‌گرداند. خواندنی [System::String](../../system/string/). |
| virtual [System::String](../../system/string/) [get_NumberFormatOfYValues](./get_numberformatofyvalues/)() | قالب عددی برای مقادیر y سری را برمی‌گرداند. خواندنی [System::String](../../system/string/). |
| virtual **int32_t** [get_Order](./get_order/)() | ترتیب یک سری را برمی‌گرداند. خواندنی **int32_t**. |
| virtual **int8_t** [get_Overlap](./get_overlap/)() | میزان همپوشانی میله‌ها و ستون‌ها در نمودارهای دو-بعدی را به‌صورت درصد (از -100% تا 100%) تعیین می‌کند. این ویژگی نه تنها برای این سری بلکه برای تمام سری‌های گروه سری والد است - این یک بازتاب ویژگی مناسب گروه است. به همین دلیل این ویژگی فقط‌خواندنی است. برای تغییر مقدار از ویژگی [get_ParentSeriesGroup()](./get_parentseriesgroup/)->get(set)_Overlap() خواندنی/قابل‌نوشتن استفاده کنید. فقط‌خواندنی **int8_t**. |
| virtual [ParentLabelLayoutType](../parentlabellayouttype/) [get_ParentLabelLayout](./get_parentlabellayout/)() | نمایان‌گر چیدمان برچسب‌های دسته والد. فقط برای نمودارهای Treemap کاربرد دارد. |
| virtual [System::SharedPtr](../../system/sharedptr/)\<[IChartSeries](./)\> [get_ParentSeries](./get_parentseries/)(**int32_t**) | سری‌های نمودار را در گروه سری والد در ایندکس مشخص‌شده برمی‌گرداند. |
| virtual [System::SharedPtr](../../system/sharedptr/)\<[IChartSeriesGroup](../ichartseriesgroup/)\> [get_ParentSeriesGroup](./get_parentseriesgroup/)() | گروه سری والد را برمی‌گرداند. فقط‌خواندنی [IChartSeriesGroup](../ichartseriesgroup/). |
| virtual [PieSplitType](../piesplittype/) [get_PieSplitBy](./get_piesplitby/)() | تعیین می‌کند کدام نقاط داده در پای دوم یا میلهٔ دوم در نمودار pie-of-pie یا bar-of-pie قرار دارند. این ویژگی نه تنها برای این سری بلکه برای تمام سری‌های گروه سری والد است - این یک بازتاب ویژگی مناسب گروه است. به همین دلیل این ویژگی فقط‌خواندنی است. برای دسترسی به گروه سری والد از ویژگی ParentSeriesGroup استفاده کنید. برای تغییر مقدار از ویژگی [get_ParentSeriesGroup()](./get_parentseriesgroup/)->get(set)_PieSplitBy() خواندنی/قابل‌نوشتن استفاده کنید. فقط‌خواندنی [PieSplitType](../piesplittype/). |
| virtual [System::SharedPtr](../../system/sharedptr/)\<[IChartDataPoint](../ichartdatapoint/)\> [get_PieSplitCustomPoint](./get_piesplitcustompoint/)(**int32_t**) | اطلاعات تقسیم سفارشی برای نمودار pie-of-pie یا bar-of-pie با تقسیم سفارشی. نقطه داده‌ای را که باید در پای دوم یا میله دوم کشیده شود در ایندکس مشخص‌شده برمی‌گرداند. |
| virtual [System::SharedPtr](../../system/sharedptr/)\<[IPieSplitCustomPointCollection](../ipiesplitcustompointcollection/)\> [get_PieSplitCustomPoints](./get_piesplitcustompoints/)() | اطلاعات تقسیم سفارشی برای نمودار pie-of-pie یا bar-of-pie با تقسیم سفارشی. شامل نقاط داده‌ای است که باید در پای دوم یا میله دوم کشیده شوند. این ویژگی نه تنها برای این سری بلکه برای تمام سری‌های گروه سری والد است - این یک بازتاب ویژگی مناسب گروه است. فقط‌خواندنی [IPieSplitCustomPointCollection](../ipiesplitcustompointcollection/). |
| virtual **double** [get_PieSplitPosition](./get_piesplitposition/)() | مقداری که برای تعیین نقاط دادهٔ قرارگیری در پای دوم یا میله دوم در نمودار pie-of-pie یا bar-of-pie استفاده می‌شود. همراه با ویژگی PieSplitBy استفاده می‌شود. این ویژگی نه تنها برای این سری بلکه برای تمام سری‌های گروه سری والد است - این یک بازتاب ویژگی مناسب گروه است. به همین دلیل این ویژگی فقط‌خواندنی است. برای دسترسی به گروه سری والد از ویژگی ParentSeriesGroup استفاده کنید. برای تغییر مقدار از ویژگی [get_ParentSeriesGroup()](./get_parentseriesgroup/)->get(set)_PieSplitPosition() خواندنی/قابل‌نوشتن استفاده کنید. فقط‌خواندنی **double**. |
| virtual **bool** [get_PlotOnSecondAxis](./get_plotonsecondaxis/)() | نشان می‌دهد آیا این سری بر روی محور مقدار دوم رسم می‌شود. خواندنی **bool**. |
| virtual [System::SharedPtr](../../system/sharedptr/)\<[IPresentation](../../aspose.slides/ipresentation/)\> [get_Presentation](../../aspose.slides/ipresentationcomponent/get_presentation/)() | ارائه را برمی‌گرداند. فقط‌خواندنی [IPresentation](../../aspose.slides/ipresentation/). |
| virtual [QuartileMethodType](../quartilemethodtype/) [get_QuartileMethod](./get_quartilemethod/)() | نمایان‌گر روش چارک (quartile). فقط برای نمودارهای BoxAndWhisker کاربرد دارد. |
| virtual [System::SharedPtr](../../system/sharedptr/)\<[ILegendEntryProperties](../ilegendentryproperties/)\> [get_RelatedLegendEntry](./get_relatedlegendentry/)() | ورودی افسانه (legend) مربوط به این سری را برمی‌گرداند. فقط‌خواندنی [ILegendEntryProperties](../ilegendentryproperties/). |
| virtual **uint16_t** [get_SecondPieSize](./get_secondpiesize/)() | اندازهٔ پای دوم یا میلهٔ دوم در نمودار pie-of-pie یا bar-of-pie را به‌صورت درصدی از اندازهٔ اولین پای تعیین می‌کند (می‌تواند بین 5 تا 200 درصد باشد). این ویژگی نه تنها برای این سری بلکه برای تمام سری‌های گروه سری والد است - این یک بازتاب ویژگی مناسب گروه است. به همین دلیل این ویژگی فقط‌خواندنی است. برای دسترسی به گروه سری والد از ویژگی ParentSeriesGroup استفاده کنید. برای تغییر مقدار از ویژگی [get_ParentSeriesGroup()](./get_parentseriesgroup/)->get(set)_SecondPieSize() خواندنی/قابل‌نوشتن استفاده کنید. فقط‌خواندنی **uint16_t**. |
| virtual **bool** [get_ShowConnectorLines](./get_showconnectorlines/)() | نمایان‌گر خطوط اتصال (connector lines). فقط برای نمودارهای Waterfall کاربرد دارد. |
| virtual **bool** [get_ShowInnerPoints](./get_showinnerpoints/)() | نمایان‌گر نقاط داخلی. اگر نقاط داخلی در نمودار BoxAndWhisker نشان داده شوند، مقدار **true** برمی‌گردد. فقط برای نمودارهای BoxAndWhisker کاربرد دارد. خواندنی **bool**. |
| virtual **bool** [get_ShowMeanLine](./get_showmeanline/)() | نمایان‌گر نشانگرهای میانگین. اگر خط میانگین در نمودار BoxAndWhisker نشان داده شود، مقدار **true** برمی‌گردد. فقط برای نمودارهای BoxAndWhisker کاربرد دارد. خواندنی **bool**. |
| virtual **bool** [get_ShowMeanMarkers](./get_showmeanmarkers/)() | نمایان‌گر نشانگرهای میانگین. اگر نشانگرهای میانگین در نمودار BoxAndWhisker نشان داده شوند، مقدار **true** برمی‌گردد. فقط برای نمودارهای BoxAndWhisker کاربرد دارد. خواندنی **bool**. |
| virtual **bool** [get_ShowOutlierPoints](./get_showoutlierpoints/)() | نمایان‌گر نقاط دوردست (outlier). اگر نقاط دوردست در نمودار BoxAndWhisker نشان داده شوند، مقدار **true** برمی‌گردد. فقط برای نمودارهای BoxAndWhisker کاربرد دارد. خواندنی **bool**. |
| virtual [System::SharedPtr](../../system/sharedptr/)\<[IBaseSlide](../../aspose.slides/ibaseslide/)\> [get_Slide](../../aspose.slides/islidecomponent/get_slide/)() | اسلاید پایه را برمی‌گرداند. فقط‌خواندنی [IBaseSlide](../../aspose.slides/ibaseslide/). |
| virtual **bool** [get_Smooth](./get_smooth/)() | نمایان‌گر هموارسازی منحنی (curve smoothing). اگر هموارسازی منحنی برای نمودار خطی یا پراکنده فعال باشد، مقدار **true** برمی‌گردد. فقط برای نمودارهای خطی و پراکنده متصل به خطوط کاربرد دارد. خواندنی **bool**. |
| virtual [System::SharedPtr](../../system/sharedptr/)\<[ITrendline](../itrendline/)\> [get_TrendLine](./get_trendline/)(**int32_t**) | خط روند را در ایندکس مشخص‌شده برمی‌گرداند. |
| virtual [System::SharedPtr](../../system/sharedptr/)\<[ITrendlineCollection](../itrendlinecollection/)\> [get_TrendLines](./get_trendlines/)() | مجموعه‌ای از خطوط روند سری‌ها. فقط‌خواندنی [ITrendlineCollection](../itrendlinecollection/). |
| virtual [ChartType](../charttype/) [get_Type](./get_type/)() | نوع این سری را برمی‌گرداند. خواندنی [ChartType](../charttype/). |
| virtual [System::Drawing::Color](../../system.drawing/color/) [GetAutomaticSeriesColor](./getautomaticseriescolor/)() | رنگ خودکار سری را بر پایهٔ ایندکس سری و سبک نمودار برمی‌گرداند. این رنگ به‌صورت پیش‌فرض در صورت برابر بودن FillType با NotDefined استفاده می‌شود. |
| Detail::SmartPtrCounter * [GetCounter](../../system/object/getcounter/)() | ساختار دادهٔ شمارندهٔ مرجع مرتبط با شیء را دریافت می‌کند. |
| virtual **int32_t** [GetHashCode](../../system/object/gethashcode/)() const | مشابه متد C# [Object.GetHashCode()](../../system/object/gethashcode/). امکان هش‌سازی اشیاء سفارشی را فراهم می‌کند. |
| virtual const [TypeInfo](../../system/typeinfo/)\& [GetType](../../system/object/gettype/)() const | نوع واقعی شیء را دریافت می‌کند. مشابه فراخوانی C# [System.Object.GetType()](../../system/object/gettype/). |
| virtual **bool** [Is](../../system/object/is/)(const [TypeInfo](../../system/typeinfo/)\&) const | بررسی می‌کند آیا شیء یک نمونه از نوع توصیف‌شده توسط targetType است. مشابه عملگر C# `is`. |
| void [Lock](../../system/object/lock/)() | اجرای قفل (lock) در بیان C# lock(). مستقیماً صدا بزنید یا از شیء نگهدارنده [LockContext](../../system/lockcontext/) استفاده کنید. |
| virtual [ptr](../../system/object/ptr/) [MemberwiseClone](../../system/object/memberwiseclone/)() const | مشابه متد C# [Object.MemberwiseClone()](../../system/object/memberwiseclone/). امکان شبیه‌سازی (cloning) انواع سفارشی را فراهم می‌کند. |
|  [Object](../../system/object/object/)() | شیء را ایجاد می‌کند. تمام ساختارهای داخلی را مقداردهی اولیه می‌کند. |
|  [Object](../../system/object/object/)([Object](../../system/object/) const\&) | سازنده کپی. در واقع هیچ چیزی را کپی نمی‌کند، فقط شیء جدید را مقداردهی اولیه می‌کند و امکان ساخت کپی برای زیرکلاس‌ها را فراهم می‌کند. |
| [Object](../../system/object/)\& [operator=](../../system/object/operator_equal/)([Object](../../system/object/) const\&) | عملگر انتساب. در واقع هیچ چیزی را کپی نمی‌کند، فقط شیء جدید را مقداردهی اولیه می‌کند و امکان ساخت کپی برای زیرکلاس‌ها را فراهم می‌کند. |
| static **bool** [ReferenceEquals](../../system/object/referenceequals/)([ptr](../../system/object/ptr/) const\&, [ptr](../../system/object/ptr/) const\&) | اشیاء را با مرجع مقایسه می‌کند. |
| static std::enable_if<\![IsSmartPtr](../../system/issmartptr/)\<T\>::value, **bool**\>::type [ReferenceEquals](../../system/object/referenceequals/)(T const\&, T const\&) | اشیاء را با مرجع مقایسه می‌کند. |
| static std::enable_if<\![IsSmartPtr](../../system/issmartptr/)\<T\>::value, **bool**\>::type [ReferenceEquals](../../system/object/referenceequals/)(T const\&, std::nullptr_t) | مقایسه مرجع برای شیء نوع مقدار با nullptr. |
| **bool** [ReferenceEquals](../../system/object/referenceequals/)([String](../../system/string/) const\&, std::nullptr_t) | تخصصสำหรับ [Object::ReferenceEquals](../../system/object/referenceequals/) در حالتی که رشته و nullptr باشد. |
| **bool** [ReferenceEquals](../../system/object/referenceequals/)([String](../../system/string/) const\&, [String](../../system/string/) const\&) | تخصص برای [Object::ReferenceEquals](../../system/object/referenceequals/) در حالت رشته‌ها. |
| int [RemovedSharedRefs](../../system/object/removedsharedrefs/)(int) | تعداد مرجع مشترک را به مقدار مشخص‌شده کاهش می‌دهد. |
| virtual void [set_Bar3DShape](./set_bar3dshape/)([ChartShapeType](../chartshapetype/)) | شکل یک سری نمودار میله‌ای 3-بعدی را تعیین می‌کند. تغییر مقدار این ویژگی می‌تواند باعث تغییر خودکار نوع سری شود. نوشتنی [ChartShapeType](../chartshapetype/). |
| virtual void [set_Explosion](./set_explosion/)(**int32_t**) | فاصلهٔ یک برش پیت باز از مرکز نمودار دایره‌ای به‌صورت درصدی از قطر پیت بیان می‌شود. نوشتنی **int32_t**. |
| virtual void [set_InvertIfNegative](./set_invertifnegative/)(**bool**) | تعیین می‌کند آیا میله، ستون یا سری حبابی در صورت منفی بودن مقدار، رنگ‌های خود را معکوس می‌کند. نوشتنی **bool**. |
| virtual void [set_NumberFormatOfBubbleSizes](./set_numberformatofbubblesizes/)([System::String](../../system/string/)) | قالب عددی برای اندازه‌های حباب‌های سری را تنظیم می‌کند. نوشتنی [System::String](../../system/string/). |
| virtual void [set_NumberFormatOfValues](./set_numberformatofvalues/)([System::String](../../system/string/)) | قالب عددی برای مقادیر سری را تنظیم می‌کند. نوشتنی [System::String](../../system/string/). |
| virtual void [set_NumberFormatOfXValues](./set_numberformatofxvalues/)([System::String](../../system/string/)) | قالب عددی برای مقادیر x سری را تنظیم می‌کند. نوشتنی [System::String](../../system/string/). |
| virtual void [set_NumberFormatOfYValues](./set_numberformatofyvalues/)([System::String](../../system/string/)) | قالب عددی برای مقادیر y سری را تنظیم می‌کند. نوشتنی [System::String](../../system/string/). |
| virtual void [set_Order](./set_order/)(**int32_t**) | ترتیب یک سری را تنظیم می‌کند. نوشتنی **int32_t**. |
| virtual void [set_ParentLabelLayout](./set_parentlabellayout/)([ParentLabelLayoutType](../parentlabellayouttype/)) | نمایان‌گر چیدمان برچسب‌های دسته والد. فقط برای نمودارهای Treemap کاربرد دارد. |
| virtual void [set_PlotOnSecondAxis](./set_plotonsecondaxis/)(**bool**) | نشان می‌دهد آیا این سری بر روی محور مقدار دوم رسم می‌شود. نوشتنی **bool**. |
| virtual void [set_QuartileMethod](./set_quartilemethod/)([QuartileMethodType](../quartilemethodtype/)) | نمایان‌گر روش چارک (quartile). فقط برای نمودارهای BoxAndWhisker کاربرد دارد. |
| virtual void [set_ShowConnectorLines](./set_showconnectorlines/)(**bool**) | نمایان‌گر خطوط اتصال. فقط برای نمودارهای Waterfall کاربرد دارد. |
| virtual void [set_ShowInnerPoints](./set_showinnerpoints/)(**bool**) | نمایان‌گر نقاط داخلی. اگر نقاط داخلی در نمودار BoxAndWhisker نشان داده شوند، مقدار **true** برمی‌گردد. فقط برای نمودارهای BoxAndWhisker کاربرد دارد. نوشتنی **bool**. |
| virtual void [set_ShowMeanLine](./set_showmeanline/)(**bool**) | نمایان‌گر نشانگرهای میانگین. اگر خط میانگین در نمودار BoxAndWhisker نشان داده شود، مقدار **true** برمی‌گردد. فقط برای نمودارهای BoxAndWhisker کاربرد دارد. نوشتنی **bool**. |
| virtual void [set_ShowMeanMarkers](./set_showmeanmarkers/)(**bool**) | نمایان‌گر نشانگرهای میانگین. اگر نشانگرهای میانگین در نمودار BoxAndWhisker نشان داده شوند، مقدار **true** برمی‌گردد. فقط برای نمودارهای BoxAndWhisker کاربرد دارد. نوشتنی **bool**. |
| virtual void [set_ShowOutlierPoints](./set_showoutlierpoints/)(**bool**) | نمایان‌گر نقاط دوردست. اگر نقاط دوردست در نمودار BoxAndWhisker نشان داده شوند، مقدار **true** برمی‌گردد. فقط برای نمودارهای BoxAndWhisker کاربرد دارد. نوشتنی **bool**. |
| virtual void [set_Smooth](./set_smooth/)(**bool**) | نمایان‌گر هموارسازی منحنی. اگر هموارسازی منحنی برای نمودار خطی یا پراکنده فعال باشد، مقدار **true** برمی‌گردد. فقط برای نمودارهای خطی و پراکنده متصل به خطوط کاربرد دارد. نوشتنی **bool**. |
| virtual void [set_Type](./set_type/)([ChartType](../charttype/)) | نوع این سری را برمی‌گرداند. نوشتنی [ChartType](../charttype/). |
| virtual void [SetTemplateWeakPtr](../../system/object/settemplateweakptr/)(**uint32_t**) | آرگومان قالب n-ام را به یک اشاره‌گر ضعیف تنظیم می‌کند (نه قوی). امکان تغییر اشاره‌گرها در کانتینرها به حالت ضعیف را می‌دهد. |
| int [SharedCount](../../system/object/sharedcount/)() const | مقدار فعلی شمارندهٔ مرجع مشترک را دریافت می‌کند. |
| [Object](../../system/object/) * [SharedRefAdded](../../system/object/sharedrefadded/)() | شمارندهٔ مرجع مشترک را افزایش می‌دهد. نباید مستقیماً صدا زده شود؛ به‌جای آن از اشاره‌گرهای هوشمند یا ThisProtector استفاده کنید. |
| int [SharedRefRemovedSafe](../../system/object/sharedrefremovedsafe/)() | شمارندهٔ مرجع مشترک را کاهش داده و مقدار آن را برمی‌گرداند. نباید مستقیماً صدا زده شود؛ به‌جای آن از اشاره‌گرهای هوشمند یا ThisProtector استفاده کنید. |
| virtual [String](../../system/string/) [ToString](../../system/object/tostring/)() const | مشابه متد C# [Object.ToString()](../../system/object/tostring/). امکان تبدیل اشیاء سفارشی به رشته را فراهم می‌کند. |
| static const [TypeInfo](../../system/typeinfo/)\& [Type](../../system/object/type/)() | پیاده‌سازی عبارت C# typeof([System.Object](../../system/object/)). |
| void [Unlock](../../system/object/unlock/)() | اجرای بازگشایی (unlock) در بیان C# lock(). مستقیماً صدا بزنید یا از شیء نگهدارنده [LockContext](../../system/lockcontext/) استفاده کنید. |
| Detail::SmartPtrCounter * [WeakRefAdded](../../system/object/weakrefadded/)() | شمارندهٔ مرجع ضعیف را افزایش می‌دهد. نباید مستقیماً صدا زده شود؛ به‌جای آن از اشاره‌گرهای هوشمند یا ThisProtector استفاده کنید. |
| void [WeakRefRemoved](../../system/object/weakrefremoved/)() | شمارندهٔ مرجع ضعیف را کاهش می‌دهد. نباید مستقیماً صدا زده شود؛ به‌جای آن از اشاره‌گرهای هوشمند یا ThisProtector استفاده کنید. |
| virtual  [~Object](../../system/object/~object/)() | شیء را از بین می‌برد. تمام ساختارهای داخلی را آزاد می‌کند. |

## مراجع

* کلاس [IChartComponent](../ichartcomponent/)
* فضای‌نام [Aspose::Slides::Charts](../)
* کتابخانه [Aspose.Slides](../../)