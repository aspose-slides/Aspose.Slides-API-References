---
title: get_DoughnutHoleSize()
second_title: Aspose.Slides برای C++ مرجع API
description: اندازهٔ حفرهٔ داخل نمودار دونات را مشخص می‌کند (می‌تواند بین ۱۰ تا ۹۰ درصد از اندازهٔ ناحیهٔ ترسیم باشد). این ویژگی نه تنها متعلق به این سری است بلکه به تمام سری‌های گروه سری‌های والد نیز تعلق دارد - این یک بازتاب از ویژگی مناسب گروه است. بنابراین این ویژگی فقط‌خواندنی است. برای دسترسی به گروه سری‌های والد از ویژگی ParentSeriesGroup استفاده کنید. برای تغییر مقدار از ویژگی get_ParentSeriesGroup()->get(set)_DoughnutHoleSize() خواندن/نوشتن استفاده کنید. فقط‌خواندنی uint8_t.
type: docs
weight: 742
url: /fa/aspose.slides.charts/ichartseries/get_doughnutholesize/
---
## IChartSeries::get_DoughnutHoleSize() متد

اندازهٔ حفرهٔ داخل نمودار دونات را مشخص می‌کند (می‌تواند بین ۱۰ تا ۹۰ درصد از اندازهٔ ناحیهٔ ترسیم باشد). این ویژگی نه تنها متعلق به این سِری است بلکه به تمام سِری‌های گروه سطرهای والد نیز تعلق دارد – این یک بازتاب از ویژگی مناسب گروه است. بنابراین این ویژگی فقط-خواندنی است. برای دسترسی به گروه سطرهای والد از ویژگی ParentSeriesGroup استفاده کنید. برای تغییر مقدار از ویژگی [get_ParentSeriesGroup()](../get_parentseriesgroup/)->get(set)_DoughnutHoleSize() خواندن/نوشتن استفاده کنید. فقط-خواندنی **uint8_t**.

```cpp
virtual uint8_t Aspose::Slides::Charts::IChartSeries::get_DoughnutHoleSize()=0
```

## توضیحات

این بازتاب ویژگی [get_ParentSeriesGroup()](../get_parentseriesgroup/)->get(set)_DoughnutHoleSize() است.

## مراجع مرتبط

* کلاس [IChartSeries](../)
* فضای نام [Aspose::Slides::Charts](../../)
* کتابخانه [Aspose.Slides](../../../)