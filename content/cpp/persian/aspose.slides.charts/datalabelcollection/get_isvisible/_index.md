---
title: get_IsVisible()
second_title: مرجع API Aspose.Slides برای C++
description: مقدار False به این معنی است که برچسب داده به‌صورت پیش‌فرض قابل مشاهده نیست (و بنابراین تمام پرچم‌های Show* (ShowValue, ...) از ویژگی DefaultDataLabelFormat برابر false هستند). فقط‌خواندنی bool.
type: docs
weight: 14
url: /fa/aspose.slides.charts/datalabelcollection/get_isvisible/
---
## DataLabelCollection::get_IsVisible() روش

مقدار False به این معنی است که برچسب داده به‌صورت پیش‌فرض قابل مشاهده نیست (و بنابراین تمام پرچم‌های Show* (ShowValue, ...) از ویژگی DefaultDataLabelFormat برابر false هستند). فقط‌خواندنی **bool**.

```cpp
bool Aspose::Slides::Charts::DataLabelCollection::get_IsVisible() override
```

## توضیحات

اگر برچسب داده به‌صورت پیش‌فرض قابل مشاهده باشد می‌توانید آن را به‌صورت پیش‌فرض پنهان کنید با [Hide()](../hide/) متد. اما اگر برچسب داده به‌صورت پیش‌فرض قابل مشاهده نباشد (IsVisible مقدار false است) می‌توانید برچسب داده "visible 
by default" را با تنظیم پرچم‌های Show* (ShowValue, ...) از ویژگی DefaultDataLabelFormat به حالت true قابل مشاهده کنید.

## همچنین ببینید

* کلاس [DataLabelCollection](../)
* فضای‌نام [Aspose::Slides::Charts](../../)
* کتابخانه [Aspose.Slides](../../../)