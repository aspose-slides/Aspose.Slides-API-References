---
title: get_SecondPieSize()
second_title: Aspose.Slides برای C++ مرجع API
description: اندازهٔ پای دوم یا میلهٔ پای دوم در نمودار پای-پای یا میله-پای را به‌عنوان درصدی از اندازهٔ پای اول مشخص می‌کند (می‌تواند بین 5 تا 200 درصد باشد). این ویژگی نه فقط برای این سری بلکه برای تمام سری‌های گروه سری والد است - این یک پیش‌بینی از ویژگی مناسب گروه است. بنابراین این ویژگی فقط-قابل-خواندن است. برای دسترسی به گروه سری والد از ویژگی ParentSeriesGroup استفاده کنید. برای تغییر مقدار از ویژگی get_ParentSeriesGroup()->get(set)_SecondPieSize() که قابلیت خواندن/نوشتن دارد استفاده نمایید. فقط-قابل-خواندن uint16_t.
type: docs
weight: 703
url: /fa/aspose.slides.charts/chartseries/get_secondpiesize/
---
## ChartSeries::get_SecondPieSize() متد


اندازهٔ پای دوم یا میلهٔ پای دوم در نمودار پای-پای یا میله-پای را به‌عنوان درصدی از اندازهٔ پای اول مشخص می‌کند (مقدار می‌تواند بین 5 تا 200 درصد باشد). این ویژگی نه تنها متعلق به این سری بلکه به تمام سری‌های گروه سری والد تعلق دارد – این یک پیش‌بینی از ویژگی گروه مربوطه است. بنابراین این ویژگی فقط-قابل-خواندن است. برای دسترسی به گروه سری والد از ویژگی ParentSeriesGroup استفاده کنید. برای تغییر مقدار، از ویژگی [get_ParentSeriesGroup()](../get_parentseriesgroup/)->get(set)_SecondPieSize() که قابلیت خواندن/نوشتن دارد استفاده کنید. فقط-قابل-خواندن **uint16_t**.

```cpp
uint16_t Aspose::Slides::Charts::ChartSeries::get_SecondPieSize() override
```

## توضیحات


این یک پیش‌بینی از ویژگی [get_ParentSeriesGroup()](../get_parentseriesgroup/)->get(set)_SecondPieSize() است. 

## مراجع

* کلاس [ChartSeries](../)
* فضای‌نام [Aspose::Slides::Charts](../../)
* کتابخانه [Aspose.Slides](../../../)