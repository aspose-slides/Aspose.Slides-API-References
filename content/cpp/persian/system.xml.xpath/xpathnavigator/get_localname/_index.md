---
title: get_LocalName()
second_title: مرجع API Aspose.Slides برای C++
description: "هنگامی که در یک کلاس مشتق بازنویسی می‌شود، نام XPathNavigator::get_Name گرهٔ فعلی را بدون هیچ پیشوند فضای نامی دریافت می‌کند."
type: docs
weight: 144
url: /fa/system.xml.xpath/xpathnavigator/get_localname/
---
## XPathNavigator::get_LocalName() متد


هنگامی که در یک کلاس مشتق بازنویسی می‌شود، [XPathNavigator::get_Name](../get_name/) گرهٔ فعلی را بدون هیچ پیشوند فضای نامی دریافت می‌کند.

```cpp
virtual String System::Xml::XPath::XPathNavigator::get_LocalName()=0
```


### مقدار بازگشت

یک [String](../../../system/string/) که شامل نام محلی گرهٔ فعلی است، یا [String::Empty](../../../system/string/empty/) اگر گرهٔ فعلی نامی نداشته باشد (برای مثال گره‌های متن یا توضیح).

## همچنین ببینید

* کلاس [String](../../../system/string/)
* کلاس [XPathNavigator](../)
* فضای نام [System::Xml::XPath](../../)
* کتابخانه [Aspose.Slides](../../../)