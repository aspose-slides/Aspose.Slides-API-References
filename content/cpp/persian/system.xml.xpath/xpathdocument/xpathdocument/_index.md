---
title: XPathDocument()
second_title: مرجع API Aspose.Slides برای C++
description: یک نمونه جدید از کلاس XPathDocument را با داده‌های XML که در شیء XmlReader مشخص شده موجود است، مقداردهی اولیه می‌کند.
type: docs
weight: 1
url: /fa/system.xml.xpath/xpathdocument/xpathdocument/
---
## XPathDocument::XPathDocument(const SharedPtr\<XmlReader\>\&) سازنده


یک نمونه جدید از کلاس [XPathDocument](../) را با داده‌های XML که در شیء [XmlReader](../../../system.xml/xmlreader/) مشخص شده موجود است، مقداردهی اولیه می‌کند.

```cpp
System::Xml::XPath::XPathDocument::XPathDocument(const SharedPtr<XmlReader> &reader)
```


### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| reader | const [SharedPtr](../../../system/sharedptr/)\<[XmlReader](../../../system.xml/xmlreader/)\>\& | شیء [XmlReader](../../../system.xml/xmlreader/) که داده‌های XML را شامل می‌شود. |

## XPathDocument::XPathDocument(const SharedPtr\<XmlReader\>\&, XmlSpace) سازنده


یک نمونه جدید از کلاس [XPathDocument](../) را با داده‌های XML که در شیء [XmlReader](../../../system.xml/xmlreader/) مشخص شده موجود است و با کنترل فضای سفید مشخص شده مقداردهی می‌کند.

```cpp
System::Xml::XPath::XPathDocument::XPathDocument(const SharedPtr<XmlReader> &reader, XmlSpace space)
```


### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| reader | const [SharedPtr](../../../system/sharedptr/)\<[XmlReader](../../../system.xml/xmlreader/)\>\& | شیء [XmlReader](../../../system.xml/xmlreader/) که داده‌های XML را شامل می‌شود. |
| space | [XmlSpace](../../../system.xml/xmlspace/) | یک شیء XmlSpace. |

## XPathDocument::XPathDocument(const SharedPtr\<IO::TextReader\>\&) سازنده


یک نمونه جدید از کلاس [XPathDocument](../) را با داده‌های XML که در شیء TextReader مشخص شده موجود است، مقداردهی اولیه می‌کند.

```cpp
System::Xml::XPath::XPathDocument::XPathDocument(const SharedPtr<IO::TextReader> &textReader)
```


### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| textReader | const [SharedPtr](../../../system/sharedptr/)\<[IO::TextReader](../../../system.io/textreader/)\>\& | شیء TextReader که داده‌های XML را شامل می‌شود. |

## XPathDocument::XPathDocument(const SharedPtr\<IO::Stream\>\&) سازنده


یک نمونه جدید از کلاس [XPathDocument](../) را با داده‌های XML موجود در شیء Stream مشخص شده مقداردهی اولیه می‌کند.

```cpp
System::Xml::XPath::XPathDocument::XPathDocument(const SharedPtr<IO::Stream> &stream)
```


### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| stream | const [SharedPtr](../../../system/sharedptr/)\<[IO::Stream](../../../system.io/stream/)\>\& | شیء Stream که داده‌های XML را شامل می‌شود. |

## XPathDocument::XPathDocument(const String\&) سازنده


یک نمونه جدید از کلاس [XPathDocument](../) را با داده‌های XML موجود در فایل مشخص شده مقداردهی اولیه می‌کند.

```cpp
System::Xml::XPath::XPathDocument::XPathDocument(const String &uri)
```


### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| uri | const [String](../../../system/string/)\& | مسیر فایلی که داده‌های XML را شامل می‌شود. |

## XPathDocument::XPathDocument(const String\&, XmlSpace) سازنده


یک نمونه جدید از کلاس [XPathDocument](../) را با داده‌های XML موجود در فایلی که با تنظیمات کنترل فضای سفید مشخص شده است، مقداردهی اولیه می‌کند.

```cpp
System::Xml::XPath::XPathDocument::XPathDocument(const String &uri, XmlSpace space)
```


### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| uri | const [String](../../../system/string/)\& | مسیر فایلی که داده‌های XML را شامل می‌شود. |
| space | [XmlSpace](../../../system.xml/xmlspace/) | یک شیء XmlSpace. |

## موارد مرتبط

* Enum [XmlSpace](../../../system.xml/xmlspace/)
* Typedef [SharedPtr](../../../system/sharedptr/)
* Class [XmlReader](../../../system.xml/xmlreader/)
* Class [XPathDocument](../)
* Class [TextReader](../../../system.io/textreader/)
* Class [Stream](../../../system.io/stream/)
* Class [String](../../../system/string/)
* Namespace [System::Xml::XPath](../../)
* Library [Aspose.Slides](../../../)