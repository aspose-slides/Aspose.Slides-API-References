---
title: WriteLine()
second_title: مرجع API Aspose.Slides برای C++
description: پایان‌خط جاری را به جریان خروجی استاندارد می‌نویسد.
type: docs
weight: 14
url: /fa/system/console/writeline/
---
## Console::WriteLine() متد

پایان‌خط جاری را به جریان خروجی استاندارد می‌نویسد.

```cpp
static void System::Console::WriteLine()
```

## Console::WriteLine(const SharedPtr\<T\>\&) متد

نمایش رشته‌ای شیء مشخص شده به‌همراه پایان‌خط جاری را به جریان خروجی استاندارد می‌نویسد.

```cpp
template<class T> static void System::Console::WriteLine(const SharedPtr<T> &object)
```

### پارامترهای قالب

| پارامتر | توضیح |
| --- | --- |
| T | نوع شیء برای خروجی |

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| object | const [SharedPtr](../../sharedptr/)\<T\>\& | [Object](../../object/) برای خروجی |

## Console::WriteLine(bool) متد

نمایش رشته‌ای مقدار bool را به‌همراه پایان‌خط جاری به جریان خروجی استاندارد می‌نویسد.

```cpp
static void System::Console::WriteLine(bool value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **bool** | مقدار برای خروجی |

## Console::WriteLine(char_t) متد

مقدار کاراکتر مشخص شده را به‌همراه پایان‌خط جاری به جریان خروجی استاندارد می‌نویسد.

```cpp
static void System::Console::WriteLine(char_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | char_t | مقدار برای خروجی |

## Console::WriteLine(const ArrayPtr\<char_t\>\&) متد

نمایش رشته‌ای آرایه کاراکتر مشخص شده را به‌همراه پایان‌خط جاری به جریان خروجی استاندارد می‌نویسد.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | آرایه برای خروجی |

## Console::WriteLine(const Decimal\&) متد

نمایش رشته‌ای مقدار [Decimal](../../decimal/) را به‌همراه پایان‌خط جاری به جریان خروجی استاندارد می‌نویسد.

```cpp
static void System::Console::WriteLine(const Decimal &value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | مقدار برای خروجی |

## Console::WriteLine(double) متد

نمایش رشته‌ای مقدار عدد شناور دابل پرسیژن را به‌همراه پایان‌خط جاری به جریان خروجی استاندارد می‌نویسد.

```cpp
static void System::Console::WriteLine(double value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **double** | مقدار برای خروجی |

## Console::WriteLine(float) متد

نمایش رشته‌ای مقدار عدد شناور تک‌دقت را به‌همراه پایان‌خط جاری به جریان خروجی استاندارد می‌نویسد.

```cpp
static void System::Console::WriteLine(float value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **float** | مقدار برای خروجی |

## Console::WriteLine(int32_t) متد

نمایش رشته‌ای مقدار عدد صحیح ۳۲ بیتی را به‌همراه پایان‌خط جاری به جریان خروجی استاندارد می‌نویسد.

```cpp
static void System::Console::WriteLine(int32_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **int32_t** | مقدار برای خروجی |

## Console::WriteLine(int64_t) متد

نمایش رشته‌ای مقدار عدد صحیح ۶۴ بیتی را به‌همراه پایان‌خط جاری به جریان خروجی استاندارد می‌نویسد.

```cpp
static void System::Console::WriteLine(int64_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **int64_t** | مقدار برای خروجی |

## Console::WriteLine(const String\&) متد

شیء رشته‌ای مشخص شده را به‌همراه پایان‌خط جاری به جریان خروجی استاندارد می‌نویسد.

```cpp
static void System::Console::WriteLine(const String &value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | شیء رشته‌ای برای خروجی |

## Console::WriteLine(const char_t *) متد

رشتهٔ C مشخص شده را به‌همراه پایان‌خط جاری به جریان خروجی استاندارد می‌نویسد.

```cpp
static void System::Console::WriteLine(const char_t *value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const char_t * | رشتهٔ C برای خروجی |

## Console::WriteLine(const TypeInfo\&) متد

نمایش رشته‌ای مقدار [TypeInfo](../../typeinfo/) را به‌همراه پایان‌خط جاری به جریان خروجی استاندارد می‌نویسد.

```cpp
static void System::Console::WriteLine(const TypeInfo &value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | مقدار برای خروجی |

## Console::WriteLine(uint32_t) متد

نمایش رشته‌ای مقدار عدد صحیح بدون علامت ۳۲ بیتی را به‌همراه پایان‌خط جاری به جریان خروجی استاندارد می‌نویسد.

```cpp
static void System::Console::WriteLine(uint32_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **uint32_t** | مقدار برای خروجی |

## Console::WriteLine(uint64_t) متد

نمایش رشته‌ای مقدار عدد صحیح بدون علامت ۶۴ بیتی را به‌همراه پایان‌خط جاری به جریان خروجی استاندارد می‌نویسد.

```cpp
static void System::Console::WriteLine(uint64_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **uint64_t** | مقدار برای خروجی |

## Console::WriteLine(const ArrayPtr\<char_t\>\&, int, int) متد

نمایش رشته‌ای محدوده مشخص‌شده از آرایه کاراکتر مشخص شده را به‌همراه پایان‌خط جاری به جریان خروجی استاندارد می‌نویسد.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer, int index, int count)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | آرایه کاراکتر |
| index | int | شاخص در آرایه که بازه برای خروجی از آن شروع می‌شود |
| count | int | تعداد عناصر در بازه برای خروجی |

## Console::WriteLine(const Exception\&) متد

نمایش رشته‌ای شیء Exception مشخص شده را به‌همراه پایان‌خط جاری به جریان خروجی استاندارد می‌نویسد.

```cpp
static void System::Console::WriteLine(const Exception &e)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| e | const [Exception](../../exception/)\& | مقدار برای خروجی |

## Console::WriteLine(const String\&, Args\&&...) متد

نمایش رشته‌ای آرگومان‌های مشخص شده که با فرمت مشخص فرمت‌بندی شده‌اند را به‌همراه پایان‌خط جاری به جریان خروجی استاندارد می‌نویسد.

```cpp
template<class...> static void System::Console::WriteLine(const String &format, Args &&... args)
```

### پارامترهای قالب

| پارامتر | توضیح |
| --- | --- |
| این | انواع مقادیر برای خروجی |

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| format | const [String](../../string/)\& | قالب رشته |
| args | Args\&&... | مقادیر برای خروجی |

## Console::WriteLine(const char *) متد

```cpp
static void System::Console::WriteLine(const char *)=delete
```

## موارد مرتبط

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Typedef [Exception](../../exception/)
* Class [Console](../)
* Class [Decimal](../../decimal/)
* Class [String](../../string/)
* Class [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)