---
title: Write()
second_title: مرجع API Aspose.Slides برای C++
description: نمایش رشته‌ای از نمایهٔ شیء مشخص شده در جریان خروجی استاندارد.
type: docs
weight: 1
url: /fa/system/console/write/
---
## Console::Write(const SharedPtr\<T\>\&) متد

نمایش رشته‌ای از نمایهٔ شیء مشخص شده در جریان خروجی استاندارد.

```cpp
template<class T> static void System::Console::Write(const SharedPtr<T> &object)
```

### پارامترهای قالب

| پارامتر | توضیح |
| --- | --- |
| T | نوع شیء برای خروجی |

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| object | const [SharedPtr](../../sharedptr/)\<T\>\& | [Object](../../object/) برای خروجی |

## Console::Write(bool) متد

نمایش رشته‌ای از مقدار بولی در جریان خروجی استاندارد.

```cpp
static void System::Console::Write(bool value)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **bool** | مقدار برای خروجی |

## Console::Write(char_t) متد

نمایش مقدار کاراکتر مشخص شده در جریان خروجی استاندارد.

```cpp
static void System::Console::Write(char_t value)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | char_t | مقدار برای خروجی |

## Console::Write(const ArrayPtr\<char_t\>\&) متد

نمایش رشته‌ای از آرایهٔ کاراکتری مشخص شده در جریان خروجی استاندارد.

```cpp
static void System::Console::Write(const ArrayPtr<char_t> &buffer)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | آرایه برای خروجی |

## Console::Write(const Decimal\&) متد

نمایش رشته‌ای از مقدار [Decimal](../../decimal/) در جریان خروجی استاندارد.

```cpp
static void System::Console::Write(const Decimal &value)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | مقدار برای خروجی |

## Console::Write(double) متد

نمایش رشته‌ای از مقدار عدد شناور دقت دو برابر در جریان خروجی استاندارد.

```cpp
static void System::Console::Write(double value)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **double** | مقدار برای خروجی |

## Console::Write(float) متد

نمایش رشته‌ای از مقدار عدد شناور دقت تک برابر در جریان خروجی استاندارد.

```cpp
static void System::Console::Write(float value)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **float** | مقدار برای خروجی |

## Console::Write(int32_t) متد

نمایش رشته‌ای از مقدار عدد صحیح ۳۲ بیتی در جریان خروجی استاندارد.

```cpp
static void System::Console::Write(int32_t value)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **int32_t** | مقدار برای خروجی |

## Console::Write(int64_t) متد

نمایش رشته‌ای از مقدار عدد صحیح ۶۴ بیتی در جریان خروجی استاندارد.

```cpp
static void System::Console::Write(int64_t value)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **int64_t** | مقدار برای خروجی |

## Console::Write(const String\&) متد

نمایش شیء رشتهٔ مشخص شده در جریان خروجی استاندارد.

```cpp
static void System::Console::Write(const String &value)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | شیء رشته برای خروجی |

## Console::Write(const char_t *) متد

نمایش رشتهٔ C مشخص شده در جریان خروجی استاندارد.

```cpp
static void System::Console::Write(const char_t *value)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const char_t * | رشتهٔ C برای خروجی |

## Console::Write(const TypeInfo\&) متد

نمایش رشته‌ای از مقدار [TypeInfo](../../typeinfo/) در جریان خروجی استاندارد.

```cpp
static void System::Console::Write(const TypeInfo &value)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | مقدار برای خروجی |

## Console::Write(uint32_t) متد

نمایش رشته‌ای از مقدار عدد صحیح بدون علامت ۳۲ بیتی در جریان خروجی استاندارد.

```cpp
static void System::Console::Write(uint32_t value)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **uint32_t** | مقدار برای خروجی |

## Console::Write(uint64_t) متد

نمایش رشته‌ای از مقدار عدد صحیح بدون علامت ۶۴ بیتی در جریان خروجی استاندارد.

```cpp
static void System::Console::Write(uint64_t value)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **uint64_t** | مقدار برای خروجی |

## Console::Write(const ArrayPtr\<char_t\>\&, int32_t, int32_t) متد

نمایش رشته‌ای از بازهٔ مشخص‌شدهٔ آرایهٔ کاراکتری مشخص‌شده در جریان خروجی استاندارد.

```cpp
static void System::Console::Write(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | آرایهٔ کاراکتر |
| index | **int32_t** | شاخص در آرایه که بازهٔ خروجی از آن شروع می‌شود |
| count | **int32_t** | تعداد عناصر در بازهٔ خروجی |

## Console::Write(const String\&, Args\&&...) متد

نمایش رشته‌ای از آرگومان‌های مشخص‌شده که بر اساس قالب مشخص‌شده قالب‌بندی شده‌اند در جریان خروجی استاندارد.

```cpp
template<class...> static void System::Console::Write(const String &format, Args &&... args)
```

### پارامترهای قالب

| پارامتر | توضیح |
| --- | --- |
| The | انواع مقادیر برای خروجی |

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| format | const [String](../../string/)\& | قالب رشته |
| args | Args\&&... | مقادیر برای خروجی |

## Console::Write(const char *) متد

```cpp
static void System::Console::Write(const char *)=delete
```

## موارد مرتبط

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* کلاس [Console](../)
* کلاس [Decimal](../../decimal/)
* کلاس [String](../../string/)
* کلاس [TypeInfo](../../typeinfo/)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)