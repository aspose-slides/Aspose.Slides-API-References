---
title: WeakReference()
second_title: Aspose.Slides برای C++ مرجع API
description: سازنده پیش‌فرض.
type: docs
weight: 1
url: /fa/system/weakreference_tmpl_end_tmpl/weakreference/
---
## WeakReference<>::WeakReference() متد


سازنده پیش‌فرض.

```cpp
System::WeakReference<>::WeakReference()
```

## WeakReference<>::WeakReference(std::nullptr_t) متد


سازنده از nullptr.

```cpp
System::WeakReference<>::WeakReference(std::nullptr_t)
```

## WeakReference<>::WeakReference(const SmartPtr\<Object\>\&) متد


یک نمونه جدید از کلاس WeakReference را مقداردهی اولیه می‌کند که به شیء مشخص‌شده ارجاع می‌دهد.

```cpp
System::WeakReference<>::WeakReference(const SmartPtr<Object> &data)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<[Object](../../object/)\>\& | [Object](../../object/) برای ذخیره. |

## WeakReference<>::WeakReference(const SmartPtr\<Object\>\&, bool) متد


یک نمونه جدید از کلاس WeakReference را مقداردهی اولیه می‌کند که به شیء مشخص‌شده ارجاع می‌دهد.

```cpp
System::WeakReference<>::WeakReference(const SmartPtr<Object> &data, bool trackResurrection)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<[Object](../../object/)\>\& | [Object](../../object/) برای ذخیره. |
| trackResurrection | **bool** | نادیده گرفته شد. |

## موارد مرتبط

* کلاس [WeakReference<>](../)
* کلاس [SmartPtr](../../smartptr/)
* کلاس [Object](../../object/)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)