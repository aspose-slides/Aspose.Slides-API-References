---
title: FromUtf8()
second_title: مرجع API Aspose.Slides برای C++
description: یک String از رشتهٔ utf8 می‌سازد.
type: docs
weight: 898
url: /fa/system/string/fromutf8/
---
## String::FromUtf8(const char *) متد

[String](../) را از رشتهٔ utf8 می‌سازد.

```cpp
static String System::String::FromUtf8(const char *utf8)
```

### Arguments

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| utf8 | const char * | اشاره‌گری به رشته‌ای که با صفر خاتمه یافته و با صفحه کد utf8 رمزگذاری شده است. |

### مقدار بازگشت

[String](../) شیء‌ای که رشتهٔ ارسال‌شده را نشان می‌دهد.

## String::FromUtf8(const char *, int) متد

[String](../) را از رشتهٔ utf8 می‌سازد.

```cpp
static String System::String::FromUtf8(const char *utf8, int len)
```

### Arguments

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| utf8 | const char * | اشاره‌گری به رشته‌ای که با صفحه کد utf8 رمزگذاری شده است. |
| len | int | تعداد کاراکترهایی که باید پردازش شوند. |

### مقدار بازگشت

[String](../) شیء‌ای که رشتهٔ ارسال‌شده را نشان می‌دهد.

## String::FromUtf8(const uint8_t *) متد

[String](../) را از رشتهٔ utf8 می‌سازد.

```cpp
static String System::String::FromUtf8(const uint8_t *utf8)
```

### Arguments

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| utf8 | const **uint8_t** * | اشاره‌گری به رشته‌ای که با صفر خاتمه یافته و با صفحه کد utf8 رمزگذاری شده است. |

### مقدار بازگشت

[String](../) شیء‌ای که رشتهٔ ارسال‌شده را نشان می‌دهد.

## String::FromUtf8(const std::string\&) متد

[String](../) را از رشتهٔ utf8 می‌سازد.

```cpp
static String System::String::FromUtf8(const std::string &utf8)
```

### Arguments

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| utf8 | const std::string\& | اشاره‌گری به رشته‌ای که با صفحه کد utf8 رمزگذاری شده است. |

### مقدار بازگشت

[String](../) شیء‌ای که رشتهٔ ارسال‌شده را نشان می‌دهد.

## موارد مرتبط

* کلاس [String](../)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)