---
title: operator+=()
second_title: مرجع API Aspose.Slides برای C++
description: اپراتور انتساب ترکیبی.
type: docs
weight: 287
url: /fa/system/string/operator_plus_equal/
---
## String::operator+=(char_t) متد

اپراتور انتساب ترکیبی.

```cpp
String & System::String::operator+=(char_t c)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| c | char_t | کاراکتر برای افزودن به انتهای رشتهٔ جاری. |

### مقدار بازگشت

مرجع خود.

## String::operator+=(const String\&) متد

اپراتور انتساب ترکیبی.

```cpp
String & System::String::operator+=(const String &str)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| str | const [String](../)\& | [String](../) برای افزودن به انتهای رشتهٔ جاری. |

### مقدار بازگشت

مرجع خود.

## String::operator+=(double) متد

اپراتور انتساب ترکیبی.

```cpp
String & System::String::operator+=(double value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **double** | [Double](../../double/) برای افزودن به انتهای رشتهٔ جاری. |

### مقدار بازگشت

مرجع خود.

## String::operator+=(uint8_t) متد

اپراتور انتساب ترکیبی.

```cpp
String & System::String::operator+=(uint8_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **uint8_t** | [Byte](../../byte/) برای افزودن به انتهای رشتهٔ جاری. |

### مقدار بازگشت

مرجع خود.

## String::operator+=(int16_t) متد

اپراتور انتساب ترکیبی.

```cpp
String & System::String::operator+=(int16_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **int16_t** | عدد کوتاه برای افزودن به انتهای رشتهٔ جاری. |

### مقدار بازگشت

مرجع خود.

## String::operator+=(uint16_t) متد

اپراتور انتساب ترکیبی.

```cpp
String & System::String::operator+=(uint16_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **uint16_t** | عدد کوتاه بدون علامت برای افزودن به انتهای رشتهٔ جاری. |

### مقدار بازگشت

مرجع خود.

## String::operator+=(int32_t) متد

اپراتور انتساب ترکیبی.

```cpp
String & System::String::operator+=(int32_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **int32_t** | عدد صحیح برای افزودن به انتهای رشتهٔ جاری. |

### مقدار بازگشت

مرجع خود.

## String::operator+=(uint32_t) متد

اپراتور انتساب ترکیبی.

```cpp
String & System::String::operator+=(uint32_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **uint32_t** | عدد صحیح بدون علامت برای افزودن به انتهای رشتهٔ جاری. |

### مقدار بازگشت

مرجع خود.

## String::operator+=(int64_t) متد

اپراتور انتساب ترکیبی.

```cpp
String & System::String::operator+=(int64_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **int64_t** | عدد طولانی برای افزودن به انتهای رشتهٔ جاری. |

### مقدار بازگشت

مرجع خود.

## String::operator+=(uint64_t) متد

اپراتور انتساب ترکیبی.

```cpp
String & System::String::operator+=(uint64_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **uint64_t** | عدد طولانی بدون علامت برای افزودن به انتهای رشتهٔ جاری. |

### مقدار بازگشت

مرجع خود.

## String::operator+=(T) متد

اپراتور انتساب ترکیبی.

```cpp
template<typename T,std::enable_if_t< std::is_same< T, bool >::value > *> String & System::String::operator+=(T value)
```

### پارامترهای الگو

| پارامتر | توضیح |
| --- | --- |
| T | نوع مقدار برای ترکیب با رشته. باید bool باشد |

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | T | [Boolean](../../boolean/) برای افزودن به انتهای رشتهٔ جاری. |

### مقدار بازگشت

مرجع خود.

## موارد مرتبط

* کلاس [String](../)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)