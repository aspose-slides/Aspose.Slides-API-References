---
title: ToString()
second_title: مرجع API Aspose.Slides برای C++
description: پوشش برای مدیریت کلاس String در زمینه‌هایی که ToString() بر روی اشیاء نوع مقدار فراخوانی می‌شود.
type: docs
weight: 365
url: /fa/system/string/tostring/
---
## String::ToString() const متد

پوشش برای مدیریت کلاس [String](../) در زمینه‌هایی که [ToString()](./) بر روی اشیاء نوع مقدار فراخوانی می‌شود.

```cpp
String System::String::ToString() const
```

### مقدار بازگشت

نسخه‌ای از رشتهٔ فعلی. اگر رشتهٔ فعلی رشتهٔ تهی باشد، به عنوان رشتهٔ تهی درنظر گرفته می‌شود.

## String::ToString(const SharedPtr\<IFormatProvider\>\&) const متد

پوشش برای مدیریت کلاس [String](../) در زمینه‌هایی که [ToString()](./) بر روی اشیاء نوع مقدار فراخوانی می‌شود.

```cpp
String System::String::ToString(const SharedPtr<IFormatProvider> &) const
```

### مقدار بازگشت

نسخه‌ای از رشتهٔ فعلی. اگر رشتهٔ فعلی رشتهٔ تهی باشد، به عنوان رشتهٔ تهی درنظر گرفته می‌شود.

## موارد مرتبط

* Typedef [SharedPtr](../../sharedptr/)
* کلاس [String](../)
* کلاس [IFormatProvider](../../iformatprovider/)
* فضای‌نام [System](../../)
* Library [Aspose.Slides](../../../)