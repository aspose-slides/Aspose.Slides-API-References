---
title: TrimEnd()
second_title: Aspose.Slides برای C++ مرجع API
description: تمام کاراکترهای فضای خالی را از انتهای رشته حذف می‌کند.
type: docs
weight: 703
url: /fa/system/string/trimend/
---
## String::TrimEnd() const متد

تمام کاراکترهای فضای خالی را از انتهای رشته حذف می‌کند.

```cpp
String System::String::TrimEnd() const
```

### مقدار بازگشت

[String](../) بدون فضای خالی در ابتدا.

## String::TrimEnd(char_t) const متد

تمام وقوع‌های کاراکتر پاس‌خورده را از انتهای رشته حذف می‌کند.

```cpp
String System::String::TrimEnd(char_t ch) const
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| ch | char_t | نماد برای حذف. |

### مقدار بازگشت

نتیجه حذف.

## String::TrimEnd(const String\&) const متد

تمام وقوع‌های کاراکترهای پاس‌خورده را از انتهای رشته حذف می‌کند.

```cpp
String System::String::TrimEnd(const String &anyOf) const
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| anyOf | const [String](../)\& | [String](../) از کاراکترهای حذف. |

### مقدار بازگشت

[String](../) بدون کاراکترهای حذف شده.

## String::TrimEnd(const ArrayPtr\<char_t\>\&) const متد

تمام وقوع‌های کاراکترهای پاس‌خورده را از انتهای رشته حذف می‌کند.

```cpp
String System::String::TrimEnd(const ArrayPtr<char_t> &anyOf) const
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| anyOf | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | [Array](../../array/) از کاراکترهای حذف. |

### مقدار بازگشت

[String](../) بدون کاراکترهای حذف شده.

## همچنین ببینید

* تعریف نوع [ArrayPtr](../../arrayptr/)
* کلاس [String](../)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)