---
title: TrimStart()
second_title: Aspose.Slides برای C++ مرجع API
description: تمام کاراکترهای فضای سفید را از ابتدای رشته حذف می‌کند.
type: docs
weight: 690
url: /fa/system/string/trimstart/
---
## String::TrimStart() const متد

تمام کاراکترهای فضای سفید را از ابتدای رشته حذف می‌کند.

```cpp
String System::String::TrimStart() const
```

### مقدار بازگشت

[String](../) بدون فضای سفید در ابتداء.

## String::TrimStart(char_t) const متد

تمام رخدادهای کاراکتر ارسال‌شده را از ابتدای رشته حذف می‌کند.

```cpp
String System::String::TrimStart(char_t ch) const
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| ch | char_t | نماد برای حذف. |

### مقدار بازگشت

نتیجه حذف.

## String::TrimStart(const String\&) const متد

تمام رخدادهای کاراکترهای ارسال‌شده را از ابتدای رشته حذف می‌کند.

```cpp
String System::String::TrimStart(const String &anyOf) const
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| anyOf | const [String](../)\& | [String](../) کاراکترهای قابل حذف. |

### مقدار بازگشت

[String](../) بدون کاراکترهای حذف‌شده.

## String::TrimStart(const ArrayPtr\<char_t\>\&) const متد

تمام رخدادهای کاراکترهای ارسال‌شده را از ابتدای رشته حذف می‌کند.

```cpp
String System::String::TrimStart(const ArrayPtr<char_t> &anyOf) const
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| anyOf | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | [Array](../../array/) کاراکترهای قابل حذف. |

### مقدار بازگشت

[String](../) بدون کاراکترهای حذف‌شده.

## موارد مرتبط

* تعریف نوع [ArrayPtr](../../arrayptr/)
* کلاس [String](../)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)