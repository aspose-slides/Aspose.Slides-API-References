---
title: CompareOrdinal()
second_title: Aspose.Slides برای C++ API Reference
description: مقایسهٔ کمتر-برابر-بزرگ دو رشته با استفاده از حالت ترتیبی.
type: docs
weight: 833
url: /fa/system/string/compareordinal/
---
## String::CompareOrdinal(const String\&, const String\&) متد

مقایسهٔ کمتر-برابر-بزرگ دو رشته با استفاده از حالت ترتیبی.

```cpp
static int System::String::CompareOrdinal(const String &strA, const String &strB)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| strA | const [String](../)\& | رشتهٔ اول برای مقایسه. |
| strB | const [String](../)\& | رشتهٔ دوم برای مقایسه. |

### مقدار برگشتی

مقدار منفی اگر زیررشتهٔ اول کمتر از دوم باشد، صفر اگر برابر باشند، مقدار مثبت در غیر این صورت.

## String::CompareOrdinal(const String\&, int, const String\&, int, int) متد

مقایسهٔ کمتر-برابر-بزرگ دو رشته با استفاده از حالت ترتیبی.

```cpp
static int System::String::CompareOrdinal(const String &strA, int indexA, const String &strB, int indexB, int length)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| strA | const [String](../)\& | رشتهٔ اول برای مقایسه. |
| indexA | int | آغاز زیررشتهٔ اول. |
| strB | const [String](../)\& | رشتهٔ دوم برای مقایسه. |
| indexB | int | آغاز زیررشتهٔ دوم. |
| length | int | تعداد کاراکترهای مقایسه‌شده. |

### مقدار برگشتی

مقدار منفی اگر زیررشتهٔ اول کمتر از دوم باشد، صفر اگر برابر باشند، مقدار مثبت در غیر این صورت.

## موارد مرتبط

* کلاس [String](../)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)