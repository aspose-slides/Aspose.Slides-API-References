---
title: FromAscii()
second_title: Aspose.Slides برای مرجع API C++
description: یک شیء String را از رشتهٔ ASCII ایجاد می‌کند.
type: docs
weight: 950
url: /fa/system/string/fromascii/
---
## String::FromAscii(const char *) متد

یک [String](../) را از رشتهٔ ASCII ایجاد می‌کند.

```cpp
static String System::String::FromAscii(const char *asciiStr)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| asciiStr | const char * | اشاره‌گری به رشتهٔ خاتمه‌یافته با صفر که با کد‌صفحهٔ ASCII رمزگذاری شده است. |

### مقدار بازگشت

شیء [String](../) که نمایانگر رشتهٔ ارسال‌شده است.

## String::FromAscii(const char *, int) متد

یک [String](../) را از رشتهٔ ASCII ایجاد می‌کند.

```cpp
static String System::String::FromAscii(const char *asciiStr, int len)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| asciiStr | const char * | اشاره‌گری به رشتهٔ خاتمه‌یافته با صفر که با کد‌صفحهٔ ASCII رمزگذاری شده است. |
| len | int | تعداد کاراکترهایی که باید پردازش شوند. |

### مقدار بازگشت

شیء [String](../) که نمایانگر رشتهٔ ارسال‌شده است.

## String::FromAscii(const std::string\&) متد

یک [String](../) را از رشتهٔ ASCII ایجاد می‌کند.

```cpp
static String System::String::FromAscii(const std::string &asciiStr)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| asciiStr | const std::string\& | رشته‌ای رمزگذاری‌شده با ASCII. |

### مقدار بازگشت

شیء [String](../) که نمایانگر رشتهٔ ارسال‌شده است.

## موارد مرتبط

* کلاس [String](../)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)