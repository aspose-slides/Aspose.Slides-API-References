---
title: Concat()
second_title: مرجع API Aspose.Slides برای C++
description: رشته‌ها را به هم می‌چسباند.
type: docs
weight: 859
url: /fa/system/string/concat/
---
## String::Concat(const ArrayPtr\<String\>\&) متد

رشته‌ها را به هم می‌چسباند.

```cpp
static String System::String::Concat(const ArrayPtr<String> &parts)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| parts | const [ArrayPtr](../../arrayptr/)\<[String](../)\>\& | رشته‌ها برای الحاق. |

### مقدار بازگشت

[String](../) حاوی رشته‌های الحاق‌شده.

## String::Concat(const String\&, const String\&) متد

رشته‌ها را به هم می‌چسباند.

```cpp
static String System::String::Concat(const String &str0, const String &str1)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| str0 | const [String](../)\& | رشتهٔ اول برای الحاق. |
| str1 | const [String](../)\& | رشتهٔ دوم برای الحاق. |

### مقدار بازگشت

[String](../) حاوی رشته‌های الحاق‌شده.

## String::Concat(const String\&, const String\&, const String\&) متد

رشته‌ها را به هم می‌چسباند.

```cpp
static String System::String::Concat(const String &str0, const String &str1, const String &str2)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| str0 | const [String](../)\& | رشتهٔ اول برای الحاق. |
| str1 | const [String](../)\& | رشتهٔ دوم برای الحاق. |
| str2 | const [String](../)\& | رشتهٔ سوم برای الحاق. |

### مقدار بازگشت

[String](../) حاوی رشته‌های الحاق‌شده.

## String::Concat(const String\&, const String\&, const String\&, const String\&) متد

رشته‌ها را به هم می‌چسباند.

```cpp
static String System::String::Concat(const String &str0, const String &str1, const String &str2, const String &str3)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| str0 | const [String](../)\& | رشتهٔ اول برای الحاق. |
| str1 | const [String](../)\& | رشتهٔ دوم برای الحاق. |
| str2 | const [String](../)\& | رشتهٔ سوم برای الحاق. |
| str3 | const [String](../)\& | رشتهٔ چهارم برای الحاق. |

### مقدار بازگشت

[String](../) حاوی رشته‌های الحاق‌شده.

## موارد مرتبط

* تعریف‌نوع [ArrayPtr](../../arrayptr/)
* کلاس [String](../)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)