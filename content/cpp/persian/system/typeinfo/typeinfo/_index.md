---
title: TypeInfo()
second_title: مرجع API Aspose.Slides برای C++
description: سازنده پیش‌فرض (هیچ نوعی تنظیم نشده است).
type: docs
weight: 40
url: /fa/system/typeinfo/typeinfo/
---
## TypeInfo::TypeInfo() سازنده

سازنده پیش‌فرض (هیچ نوعی تنظیم نشده است).

```cpp
System::TypeInfo::TypeInfo()
```

## TypeInfo::TypeInfo(std::nullptr_t) سازنده

سازنده شیء خالی (هیچ نوعی تنظیم نشده است).

```cpp
System::TypeInfo::TypeInfo(std::nullptr_t)
```

## TypeInfo::TypeInfo(const char_t *) سازنده

سازنده.

```cpp
System::TypeInfo::TypeInfo(const char_t *name)
```

### Arguments

| Parameter | Type | Description |
| --- | --- | --- |
| name | const char_t * | نام نوع. |

## TypeInfo::TypeInfo(const char_t *, uint32_t) سازنده

سازنده.

```cpp
System::TypeInfo::TypeInfo(const char_t *name, uint32_t hash)
```

### Arguments

| Parameter | Type | Description |
| --- | --- | --- |
| name | const char_t * | نام نوع. |
| hash | **uint32_t** | هش نام نوع. |

## TypeInfo::TypeInfo(const std::type_info\&) سازنده

سازنده.

```cpp
System::TypeInfo::TypeInfo(const std::type_info &info)
```

### Arguments

| Parameter | Type | Description |
| --- | --- | --- |
| info | const std::type_info\& | اطلاعات درباره یک نوع. |

## همچنین ببینید

* کلاس [TypeInfo](../)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)