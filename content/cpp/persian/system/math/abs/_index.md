---
title: Abs()
second_title: مرجع API Aspose.Slides برای C++
description: مقدار مطلق مقدار مشخص‌شده را برمی‌گرداند.
type: docs
weight: 261
url: /fa/system/math/abs/
---
## Math::Abs(T) متد

مقدار مطلق مقدار مشخص‌شده را برمی‌گرداند.

```cpp
template<class T> static T System::Math::Abs(T value)
```

### پارامترهای قالب

| پارامتر | توضیح |
| --- | --- |
| T | نوع مقداری که متد به عنوان آرگومان می‌پذیرد |

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | T | یک مقدار از نوع عددی |

### مقدار بازگشتی

مقدار مطلق **value**

## Math::Abs(const Decimal\&) متد

مقدار مطلق مقداری که توسط شیء [Decimal](../../decimal/) مشخص‌شده نمایش داده می‌شود را برمی‌گرداند.

```cpp
static Decimal System::Math::Abs(const Decimal &d)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| d | const [Decimal](../../decimal/)\& | یک شیء [Decimal](../../decimal/) |

### مقدار بازگشتی

شیء [Decimal](../../decimal/) که مقدار مطلق مقداری که توسط **value** نمایش داده می‌شود را نشان می‌دهد.

## موارد مرتبط

* کلاس [Decimal](../../decimal/)
* ساختار [Math](../)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)