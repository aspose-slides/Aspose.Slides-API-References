---
title: Parse()
second_title: Aspose.Slides برای C++ مرجع API
description: رشتهٔ مشخص‌شده را به مقدار ثابت enum معادل تبدیل می‌کند.
type: docs
weight: 1
url: /fa/system/enumparsehelper/parse/
---
## EnumParseHelper::Parse(const String\&, bool) متد


رشتهٔ مشخص‌شده را به مقدار ثابت enum معادل تبدیل می‌کند.

```cpp
static E System::EnumParseHelper<E, G, Guard>::Parse(const String &str, bool ignoreCase=false)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| str | const [String](../../string/)\& | [String](../../string/) که به‌عنوان حاوی نام ثابت enum تفسیر می‌شود |
| ignoreCase | **bool** | مشخص می‌کند آیا هنگام تفسیر رشته باید به حروف بزرگ/کوچک توجه شود |

## مقدار برگشتی

ثابت enumeration که نام آن در **str** مشخص شده است

## موارد مرتبط

* کلاس [String](../../string/)
* ساختار [EnumParseHelper](../)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)