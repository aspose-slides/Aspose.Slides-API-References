---
title: SetTarget()
second_title: مرجع API Aspose.Slides برای C++
description: شی (هدف) را که توسط شی WeakReference فعلی ارجاع داده شده است، تنظیم می‌کند.
type: docs
weight: 27
url: /fa/system/weakreference_tmpl_t__end_tmpl/settarget/
---
## WeakReference< T >::SetTarget(const SmartPtr\<T\>\&) متد

شی (هدف) را که توسط شی WeakReference فعلی ارجاع داده شده است، تنظیم می‌کند.

```cpp
void System::WeakReference<T>::SetTarget(const SmartPtr<T> &data)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) برای تنظیم. |

## موارد مرتبط

* کلاس [SmartPtr](../../smartptr/)
* کلاس [WeakReference< T >](../)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)