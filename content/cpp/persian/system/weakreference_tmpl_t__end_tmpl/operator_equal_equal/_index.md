---
title: operator==()
second_title: مرجع API Aspose.Slides برای C++
description: بررسی می‌کند که آیا شیء مرجع تهی است.
type: docs
weight: 53
url: /fa/system/weakreference_tmpl_t__end_tmpl/operator_equal_equal/
---
## WeakReference< T >::operator==(std::nullptr_t) const متد

Checks if referenced object is null.

```cpp
bool System::WeakReference<T>::operator==(std::nullptr_t) const
```

### مقدار بازگشت

True اگر شیء مرجع تهی باشد، false در غیر این صورت.

## WeakReference< T >::operator==(const WeakReference\<T\>\&) const متد

Compares referenced object to another instance WeakReference class.

```cpp
bool System::WeakReference<T>::operator==(const WeakReference<T> &other) const
```

### آرگومان‌ها

| پارامتر | نوع | شرح |
| --- | --- | --- |
| other | const [WeakReference](../weakreference/)\<T\>\& | [Object](../../object/) مقایسه به. |

### مقدار بازگشت

True اگر اشیاء مقایسه‌شده به یک شیء اشاره داشته باشند، false در غیر این صورت.

## موارد مرتبط

* متد [WeakReference](../weakreference/)
* کلاس [WeakReference< T >](../)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)