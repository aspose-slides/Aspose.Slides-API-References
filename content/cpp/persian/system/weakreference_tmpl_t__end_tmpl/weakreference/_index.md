---
title: WeakReference()
second_title: مرجع API Aspose.Slides برای C++
description: سازنده پیش‌فرض.
type: docs
weight: 1
url: /fa/system/weakreference_tmpl_t__end_tmpl/weakreference/
---
## WeakReference< T >::WeakReference() روش

سازنده پیش فرض.

```cpp
System::WeakReference<T>::WeakReference()
```

## WeakReference< T >::WeakReference(std::nullptr_t) روش

سازنده از nullptr.

```cpp
System::WeakReference<T>::WeakReference(std::nullptr_t)
```

## WeakReference< T >::WeakReference(const SmartPtr\<T\>\&) روش

یک نمونه جدید از کلاس WeakReference را مقداردهی اولیه می‌کند که به شیء مشخص شده ارجاع می‌دهد.

```cpp
System::WeakReference<T>::WeakReference(const SmartPtr<T> &data)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) برای ذخیره. |

## WeakReference< T >::WeakReference(const SmartPtr\<T\>\&, bool) روش

یک نمونه جدید از کلاس WeakReference را مقداردهی اولیه می‌کند که به شیء مشخص شده ارجاع می‌دهد.

```cpp
System::WeakReference<T>::WeakReference(const SmartPtr<T> &data, bool trackResurrection)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) برای ذخیره. |
| trackResurrection | **bool** | نادیده گرفته می‌شود. |

## همچنین ببینید

* کلاس [WeakReference< T >](../)
* کلاس [SmartPtr](../../smartptr/)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)