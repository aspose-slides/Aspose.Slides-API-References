---
title: TryGetTarget()
second_title: Aspose.Slides برای مرجع API C++
description: شیء (هدف) را که توسط شیء WeakReference فعلی ارجاع داده شده است دریافت می‌کند.
type: docs
weight: 14
url: /fa/system/weakreference_tmpl_t__end_tmpl/trygettarget/
---
## WeakReference< T >::TryGetTarget(const SmartPtr\<T\>\&) const متد

شیء (هدف) مورد ارجاع توسط شیء WeakReference فعلی را دریافت می‌کند.

```cpp
bool System::WeakReference<T>::TryGetTarget(const SmartPtr<T> &data) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) مورد ارجاع توسط شیء WeakReference فعلی. |

### مقدار بازگشت

True if an operation succeded, false otherwise.

## موارد مرتبط

* کلاس [SmartPtr](../../smartptr/)
* کلاس [WeakReference< T >](../)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)