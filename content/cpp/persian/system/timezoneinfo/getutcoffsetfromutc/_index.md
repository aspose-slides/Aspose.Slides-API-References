---
title: GetUtcOffsetFromUtc()
second_title: مرجع API Aspose.Slides برای C++
description: تابع کمکی داخلی که مقدار انحراف UTC را برای یک زمان تاریخ-ساز UTC در یک منطقه زمانی مشخص بر می‌گرداند. برای استفاده داخلی.
type: docs
weight: 144
url: /fa/system/timezoneinfo/getutcoffsetfromutc/
---
## TimeZoneInfo::GetUtcOffsetFromUtc(DateTime, const TimeZoneInfoPtr\&) متد

تابع کمکی داخلی که مقدار انحراف UTC را برای یک UTC-datetime در یک منطقه زمانی مشخص بر می‌گرداند. برای استفاده داخلی.

```cpp
static TimeSpan System::TimeZoneInfo::GetUtcOffsetFromUtc(DateTime time, const TimeZoneInfoPtr &zone)
```
## TimeZoneInfo::GetUtcOffsetFromUtc(DateTime, const TimeZoneInfoPtr\&, bool\&, bool\&) متد

تابع کمکی داخلی که مقدار انحراف UTC را برای یک UTC-datetime در یک منطقه زمانی مشخص بر می‌گرداند. برای استفاده داخلی.

```cpp
static TimeSpan System::TimeZoneInfo::GetUtcOffsetFromUtc(DateTime time, const TimeZoneInfoPtr &zone, bool &is_daylight_savings, bool &is_ambiguous_local_dst)
```
## موارد مرتبط

* تعریف نوع [TimeZoneInfoPtr](../../timezoneinfoptr/)
* کلاس [TimeSpan](../../timespan/)
* کلاس [DateTime](../../datetime/)
* کلاس [TimeZoneInfo](../)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)