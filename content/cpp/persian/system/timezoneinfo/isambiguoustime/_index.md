---
title: IsAmbiguousTime()
second_title: Aspose.Slides برای C++ مرجع API
description: بررسی می‌کند که آیا تاریخ و زمان مشخص شده مبهم است و می‌تواند به زمان‌های متعدد UTC نگاشت شود.
type: docs
weight: 313
url: /fa/system/timezoneinfo/isambiguoustime/
---
## TimeZoneInfo::IsAmbiguousTime(DateTime) const متد


بررسی می‌کند که آیا تاریخ و زمان مشخص شده مبهم است و می‌تواند به زمان‌های متعدد UTC نگاشته شود.

```cpp
bool System::TimeZoneInfo::IsAmbiguousTime(DateTime date_time) const
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| date_time | [DateTime](../../datetime/) | تاریخ و زمان. |

### مقدار بازگشت

True اگر date_time مبهم باشد.

## TimeZoneInfo::IsAmbiguousTime(const DateTimeOffset\&) const متد


بررسی می‌کند که آیا تاریخ و زمان مشخص شده مبهم است و می‌تواند به زمان‌های متعدد UTC نگاشته شود.

```cpp
bool System::TimeZoneInfo::IsAmbiguousTime(const DateTimeOffset &date_time_offset) const
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| date_time_offset | const [DateTimeOffset](../../datetimeoffset/)\& | تاریخ و زمان. |

### مقدار بازگشت

True اگر date_time مبهم باشد.

## مراجع

* Class [DateTime](../../datetime/)
* Class [TimeZoneInfo](../)
* Class [DateTimeOffset](../../datetimeoffset/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)