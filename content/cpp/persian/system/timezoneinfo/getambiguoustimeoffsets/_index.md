---
title: GetAmbiguousTimeOffsets()
second_title: مرجع API Aspose.Slides برای C++
description: تاریخ‌ها و زمان‌های UTC را که می‌توان تاریخ و زمان مشخصی را به آن‌ها نگاشت کرد، دریافت می‌کند.
type: docs
weight: 261
url: /fa/system/timezoneinfo/getambiguoustimeoffsets/
---
## TimeZoneInfo::GetAmbiguousTimeOffsets(DateTime) const متد

تاریخ‌ها و زمان‌های UTC را که می‌توان تاریخ و زمان مشخصی را به آن‌ها نگاشت کرد، دریافت می‌کند.

```cpp
ArrayPtr<TimeSpan> System::TimeZoneInfo::GetAmbiguousTimeOffsets(DateTime date_time) const
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| date_time | [DateTime](../../datetime/) | تاریخ و زمان. |

### مقدار بازگشت

[Array](../../array/) از تاریخ‌ها و زمان‌های UTC.

## TimeZoneInfo::GetAmbiguousTimeOffsets(const DateTimeOffset\&) const متد

تاریخ‌ها و زمان‌های UTC را که می‌توان تاریخ و زمان مشخصی را به آن‌ها نگاشت کرد، دریافت می‌کند.

```cpp
ArrayPtr<TimeSpan> System::TimeZoneInfo::GetAmbiguousTimeOffsets(const DateTimeOffset &date_time_offset) const
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| date_time_offset | const [DateTimeOffset](../../datetimeoffset/)\& | تاریخ و زمان. |

### مقدار بازگشت

[Array](../../array/) از تاریخ‌ها و زمان‌های UTC.

## همچنین ببینید

* تایپ‌دِف [ArrayPtr](../../arrayptr/)
* کلاس [TimeSpan](../../timespan/)
* کلاس [DateTime](../../datetime/)
* کلاس [TimeZoneInfo](../)
* کلاس [DateTimeOffset](../../datetimeoffset/)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)