---
title: IsDaylightSavingTime()
second_title: Aspose.Slides برای C++ مرجع API
description: بررسی می‌کند که آیا تاریخ و زمان مشخص‌شده در بازهٔ زمان صرفه‌جویی در نور روز قرار دارد.
type: docs
weight: 326
url: /fa/system/timezoneinfo/isdaylightsavingtime/
---
## TimeZoneInfo::IsDaylightSavingTime(DateTime) const متد

بررسی می‌کند که آیا تاریخ و زمان مشخص‌شده در بازهٔ زمان صرفه‌جویی در نور روز قرار دارد.

```cpp
bool System::TimeZoneInfo::IsDaylightSavingTime(DateTime date_time) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| date_time | [DateTime](../../datetime/) | تاریخ و زمان. |

### مقدار بازگشتی

در صورتی که date_time زمان صرفه‌جویی در نور روز باشد، مقدار true برگردانده می‌شود.

## TimeZoneInfo::IsDaylightSavingTime(const DateTimeOffset\&) const متد

بررسی می‌کند که آیا تاریخ و زمان مشخص‌شده در بازهٔ زمان صرفه‌جویی در نور روز قرار دارد.

```cpp
bool System::TimeZoneInfo::IsDaylightSavingTime(const DateTimeOffset &date_time_offset) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| date_time_offset | const [DateTimeOffset](../../datetimeoffset/)\& | تاریخ و زمان. |

### مقدار بازگشتی

در صورتی که date_time زمان صرفه‌جویی در نور روز باشد، مقدار true برگردانده می‌شود.

## مراجع

* کلاس [DateTime](../../datetime/)
* کلاس [TimeZoneInfo](../)
* کلاس [DateTimeOffset](../../datetimeoffset/)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)