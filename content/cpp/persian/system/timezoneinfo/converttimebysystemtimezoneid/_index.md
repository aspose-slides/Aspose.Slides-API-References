---
title: ConvertTimeBySystemTimeZoneId()
second_title: مرجع API Aspose.Slides برای C++
description: زمان را به زمان در یک منطقه زمانی مشخص تبدیل می‌کند.
type: docs
weight: 53
url: /fa/system/timezoneinfo/converttimebysystemtimezoneid/
---
## TimeZoneInfo::ConvertTimeBySystemTimeZoneId(DateTime, const String\&) متد

[Convert](../../convert/) زمان را به زمان در یک منطقه زمانی مشخص تبدیل می‌کند.

```cpp
static DateTime System::TimeZoneInfo::ConvertTimeBySystemTimeZoneId(DateTime date_time, const String &destination_time_zone_id)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| date_time | [DateTime](../../datetime/) | تاریخ و زمان برای تبدیل. |
| destination_time_zone_id | const [String](../../string/)\& | شناسهٔ منطقه زمانی مقصد. |

### مقدار بازگشتی

تاریخ و زمان تبدیل‌شده.

## TimeZoneInfo::ConvertTimeBySystemTimeZoneId(const DateTimeOffset\&, const String\&) متد

[Convert](../../convert/) زمان را به زمان در یک منطقه زمانی مشخص تبدیل می‌کند.

```cpp
static DateTimeOffset System::TimeZoneInfo::ConvertTimeBySystemTimeZoneId(const DateTimeOffset &date_time_offset, const String &destination_time_zone_id)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| date_time_offset | const [DateTimeOffset](../../datetimeoffset/)\& | تاریخ و زمان برای تبدیل. |
| destination_time_zone_id | const [String](../../string/)\& | شناسهٔ منطقه زمانی مقصد. |

### مقدار بازگشتی

تاریخ و زمان تبدیل‌شده.

## TimeZoneInfo::ConvertTimeBySystemTimeZoneId(DateTime, const String\&, const String\&) متد

[Convert](../../convert/) زمان را به زمان در یک منطقه زمانی مشخص تبدیل می‌کند.

```cpp
static DateTime System::TimeZoneInfo::ConvertTimeBySystemTimeZoneId(DateTime date_time, const String &source_time_zone_id, const String &destination_time_zone_id)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| date_time | [DateTime](../../datetime/) | تاریخ و زمان برای تبدیل. |
| source_time_zone_id | const [String](../../string/)\& | شناسهٔ منطقه زمانی مبدأ. |
| destination_time_zone_id | const [String](../../string/)\& | شناسهٔ منطقه زمانی مقصد. |

### مقدار بازگشتی

تاریخ و زمان تبدیل‌شده.

## موارد مرتبط

* کلاس [DateTime](../../datetime/)
* کلاس [String](../../string/)
* کلاس [TimeZoneInfo](../)
* کلاس [DateTimeOffset](../../datetimeoffset/)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)