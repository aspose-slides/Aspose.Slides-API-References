---
title: Write()
second_title: مرجع API Aspose.Slides برای C++
description: نمایش نمایهٔ رشته‌ای مقدار bool مشخص‌شده به جریان خروجی که توسط شیء جاری نمایان شده است.
type: docs
weight: 14
url: /fa/system/consoleoutput/write/
---
## ConsoleOutput::Write(bool) متد

نمایش نمایهٔ رشته‌ای مقدار bool مشخص‌شده به جریان خروجی که توسط شیء جاری نمایان شده است.

```cpp
void System::ConsoleOutput::Write(bool value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **bool** | مقداری که باید خروجی داده شود |

## ConsoleOutput::Write(const SharedPtr\<Object\>\&) متد

نمایش نمایهٔ رشته‌ای شیء مشخص‌شده به جریان خروجی که توسط شیء جاری نمایان شده است.

```cpp
void System::ConsoleOutput::Write(const SharedPtr<Object> &value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | شیئی که باید خروجی داده شود |

## ConsoleOutput::Write(char_t) متد

نمایش مقدار کاراکتر مشخص‌شده به جریان خروجی که توسط شیء جاری نمایان شده است.

```cpp
void System::ConsoleOutput::Write(char_t value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | char_t | مقداری که باید خروجی داده شود |

## ConsoleOutput::Write(Decimal) متد

نمایش نمایهٔ رشته‌ای مقدار [Decimal](../../decimal/) به جریان خروجی که توسط شیء جاری نمایان شده است.

```cpp
void System::ConsoleOutput::Write(Decimal value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | مقداری که باید خروجی داده شود |

## ConsoleOutput::Write(double) متد

نمایش نمایهٔ رشته‌ای مقدار عدد شناور double به جریان خروجی که توسط شیء جاری نمایان شده است.

```cpp
void System::ConsoleOutput::Write(double value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **double** | مقداری که باید خروجی داده شود |

## ConsoleOutput::Write(int32_t) متد

نمایش نمایهٔ رشته‌ای مقدار عدد صحیح 32-بیتی به جریان خروجی که توسط شیء جاری نمایان شده است.

```cpp
void System::ConsoleOutput::Write(int32_t value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **int32_t** | مقداری که باید خروجی داده شود |

## ConsoleOutput::Write(int64_t) متد

نمایش نمایهٔ رشته‌ای مقدار عدد صحیح 64-بیتی به جریان خروجی که توسط شیء جاری نمایان شده است.

```cpp
void System::ConsoleOutput::Write(int64_t value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **int64_t** | مقداری که باید خروجی داده شود |

## ConsoleOutput::Write(float) متد

نمایش نمایهٔ رشته‌ای مقدار عدد شناور تک‌دقت (float) به جریان خروجی که توسط شیء جاری نمایان شده است.

```cpp
void System::ConsoleOutput::Write(float value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **float** | مقداری که باید خروجی داده شود |

## ConsoleOutput::Write(const String\&) متد

نمایش شیء رشته‌ای مشخص‌شده به جریان خروجی که توسط شیء جاری نمایان شده است.

```cpp
void System::ConsoleOutput::Write(const String &value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | شیء رشته‌ای که باید خروجی داده شود |

## ConsoleOutput::Write(uint32_t) متد

نمایش نمایهٔ رشته‌ای مقدار عدد صحیح بدون علامت 32-بیتی به جریان خروجی که توسط شیء جاری نمایان شده است.

```cpp
void System::ConsoleOutput::Write(uint32_t value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **uint32_t** | مقداری که باید خروجی داده شود |

## ConsoleOutput::Write(uint64_t) متد

نمایش نمایهٔ رشته‌ای مقدار عدد صحیح بدون علامت 64-بیتی به جریان خروجی که توسط شیء جاری نمایان شده است.

```cpp
void System::ConsoleOutput::Write(uint64_t value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **uint64_t** | مقداری که باید خروجی داده شود |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&) متد

نمایش نمایهٔ رشته‌ای آرایهٔ کاراکتری مشخص‌شده به جریان خروجی که توسط شیء جاری نمایان شده است.

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | آرایه‌ای که باید خروجی داده شود |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&, int32_t, int32_t) متد

نمایش نمایهٔ رشته‌ای بازه‌ای از مقادیر آرایهٔ کاراکتری مشخص‌شده به جریان خروجی که توسط شیء جاری نمایان شده است.

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | آرایه‌ای که مقادیر برای خروجی در آن قرار دارد |
| index | **int32_t** | اندیسی که بازهٔ عناصر برای خروجی از آن شروع می‌شود |
| count | **int32_t** | تعداد عناصری که در بازهٔ خروجی قرار دارند |

## ConsoleOutput::Write(const char_t *) متد

نمایش رشتهٔ C-به‌شکل مشخص‌شده به جریان خروجی که توسط شیء جاری نمایان شده است.

```cpp
void System::ConsoleOutput::Write(const char_t *value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const char_t * | رشتهٔ C-به‌شکل که باید خروجی داده شود |

## ConsoleOutput::Write(const TypeInfo\&) متد

نمایش نمایهٔ رشته‌ای شیء [TypeInfo](../../typeinfo/) مشخص‌شده به جریان خروجی که توسط شیء جاری نمایان شده است.

```cpp
void System::ConsoleOutput::Write(const TypeInfo &value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | شیء [TypeInfo](../../typeinfo/) که باید خروجی داده شود |

## ConsoleOutput::Write(const char *) متد




```cpp
void System::ConsoleOutput::Write(const char *)=delete
```

## موارد مرتبط

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Class [ConsoleOutput](../)
* Class [Object](../../object/)
* Class [Decimal](../../decimal/)
* Class [String](../../string/)
* Class [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)