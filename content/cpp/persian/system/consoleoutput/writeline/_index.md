---
title: WriteLine()
second_title: مرجع API Aspose.Slides برای C++
description: پایان‌خط فعلی را به جریان خروجی که توسط شیء فعلی نمایان شده است، می‌نویسد.
type: docs
weight: 27
url: /fa/system/consoleoutput/writeline/
---
## ConsoleOutput::WriteLine() متد

نمایانگر پایان خط فعلی را به جریان خروجی که توسط شیء جاری نمایان شده است، می‌نویسد.

```cpp
void System::ConsoleOutput::WriteLine() override
```

## ConsoleOutput::WriteLine(const SharedPtr\<Object\>\&) متد

نمایش رشته‌ای از شیء مشخص‌شده را به‌همراه نشانگر پایان خط فعلی به جریان خروجی که توسط شیء جاری نمایان شده است، می‌نویسد.

```cpp
void System::ConsoleOutput::WriteLine(const SharedPtr<Object> &value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | شیئی که باید خروجی داده شود |

## ConsoleOutput::WriteLine(bool) متد

نمایش رشته‌ای از مقدار بولی مشخص‌شده را به‌همراه نشانگر پایان خط فعلی به جریان خروجی که توسط شیء جاری نمایان شده است، می‌نویسد.

```cpp
void System::ConsoleOutput::WriteLine(bool value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **bool** | مقدار بولی که باید خروجی داده شود |

## ConsoleOutput::WriteLine(char_t) متد

مقدار کاراکتری مشخص‌شده را به‌همراه نشانگر پایان خط فعلی به جریان خروجی که توسط شیء جاری نمایان شده است، می‌نویسد.

```cpp
void System::ConsoleOutput::WriteLine(char_t value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | char_t | مقداری که باید خروجی داده شود |

## ConsoleOutput::WriteLine(Decimal) متد

نمایش رشته‌ای از مقدار [Decimal](../../decimal/) را به‌همراه نشانگر پایان خط فعلی به جریان خروجی که توسط شیء جاری نمایان شده است، می‌نویسد.

```cpp
void System::ConsoleOutput::WriteLine(Decimal value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | مقداری که باید خروجی داده شود |

## ConsoleOutput::WriteLine(double) متد

نمایش رشته‌ای از مقدار عدد اعشاری double را به‌همراه نشانگر پایان خط فعلی به جریان خروجی که توسط شیء جاری نمایان شده است، می‌نویسد.

```cpp
void System::ConsoleOutput::WriteLine(double value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **double** | مقداری که باید خروجی داده شود |

## ConsoleOutput::WriteLine(int) متد

نمایش رشته‌ای از مقدار عدد صحیح 32-بیتی را به‌همراه نشانگر پایان خط فعلی به جریان خروجی که توسط شیء جاری نمایان شده است، می‌نویسد.

```cpp
void System::ConsoleOutput::WriteLine(int value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int | مقداری که باید خروجی داده شود |

## ConsoleOutput::WriteLine(int64_t) متد

نمایش رشته‌ای از مقدار عدد صحیح 64-بیتی را به‌همراه نشانگر پایان خط فعلی به جریان خروجی که توسط شیء جاری نمایان شده است، می‌نویسد.

```cpp
void System::ConsoleOutput::WriteLine(int64_t value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **int64_t** | مقداری که باید خروجی داده شود |

## ConsoleOutput::WriteLine(float) متد

نمایش رشته‌ای از مقدار عدد اعشاری تک‌دقت (float) را به‌همراه نشانگر پایان خط فعلی به جریان خروجی که توسط شیء جاری نمایان شده است، می‌نویسد.

```cpp
void System::ConsoleOutput::WriteLine(float value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **float** | مقداری که باید خروجی داده شود |

## ConsoleOutput::WriteLine(const String\&) متد

شیء رشته‌ای مشخص‌شده را به‌همراه نشانگر پایان خط فعلی به جریان خروجی که توسط شیء جاری نمایان شده است، می‌نویسد.

```cpp
void System::ConsoleOutput::WriteLine(const String &value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | شیء رشته‌ای که باید خروجی داده شود |

## ConsoleOutput::WriteLine(uint32_t) متد

نمایش رشته‌ای از مقدار عدد صحیح بدون علامت 32-بیتی را به‌همراه نشانگر پایان خط فعلی به جریان خروجی که توسط شیء جاری نمایان شده است، می‌نویسد.

```cpp
void System::ConsoleOutput::WriteLine(uint32_t value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **uint32_t** | مقداری که باید خروجی داده شود |

## ConsoleOutput::WriteLine(uint64_t) متد

نمایش رشته‌ای از مقدار عدد صحیح بدون علامت 64-بیتی را به‌همراه نشانگر پایان خط فعلی به جریان خروجی که توسط شیء جاری نمایان شده است، می‌نویسد.

```cpp
void System::ConsoleOutput::WriteLine(uint64_t value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **uint64_t** | مقداری که باید خروجی داده شود |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&) متد

نمایش رشته‌ای از آرایهٔ کاراکتری مشخص‌شده را به‌همراه نشانگر پایان خط فعلی به جریان خروجی که توسط شیء جاری نمایان شده است، می‌نویسد.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | آرایه‌ای که باید خروجی داده شود |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&, int32_t, int32_t) متد

نمایش رشته‌ای از محدوده‌ای از مقادیر آرایهٔ کاراکتری مشخص‌شده را به‌همراه نشانگر پایان خط فعلی به جریان خروجی که توسط شیء جاری نمایان شده است، می‌نویسد.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | آرایه‌ای که شامل مقادیر برای خروجی است |
| index | **int32_t** | اندیسی که محدودهٔ عناصر برای خروجی از آن شروع می‌شود |
| count | **int32_t** | تعداد عناصر در محدودهٔ عناصری که باید خروجی داده شوند |

## ConsoleOutput::WriteLine(const char_t *) متد

رشتهٔ C مشخص‌شده را به‌همراه نشانگر پایان خط فعلی به جریان خروجی که توسط شیء جاری نمایان شده است، می‌نویسد.

```cpp
void System::ConsoleOutput::WriteLine(const char_t *value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const char_t * | رشته C که باید خروجی داده شود |

## ConsoleOutput::WriteLine(const TypeInfo\&) متد

نمایش رشته‌ای از شیء [TypeInfo](../../typeinfo/) مشخص‌شده را به‌همراه نشانگر پایان خط فعلی به جریان خروجی که توسط شیء جاری نمایان شده است، می‌نویسد.

```cpp
void System::ConsoleOutput::WriteLine(const TypeInfo &value) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | شیء [TypeInfo](../../typeinfo/) برای خروجی |

```cpp
void System::ConsoleOutput::WriteLine(const char *)=delete
```

## مراجع

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Class [ConsoleOutput](../)
* Class [Object](../../object/)
* Class [Decimal](../../decimal/)
* Class [String](../../string/)
* Class [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)