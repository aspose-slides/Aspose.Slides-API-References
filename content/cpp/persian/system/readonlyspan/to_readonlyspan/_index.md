---
title: to_ReadOnlySpan()
second_title: مرجع API Aspose.Slides برای C++
description: یک آرایه را به ReadOnlySpan تبدیل می‌کند.
type: docs
weight: 14
url: /fa/system/readonlyspan/to_readonlyspan/
---
## ReadOnlySpan::to_ReadOnlySpan(const typename BaseType::ArrayPtrT\&) متد

یک آرایه را به [ReadOnlySpan](../) تبدیل می‌کند.

```cpp
static ThisType System::ReadOnlySpan<T>::to_ReadOnlySpan(const typename BaseType::ArrayPtrT &array)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| array | const typename BaseType::ArrayPtrT\& | آرایه‌ای که باید تبدیل شود. |

### مقدار بازگشت

یک بازه فقط-خواند که نمایانگر آرایه است.

## موارد مرتبط

* کلاس [ReadOnlySpan](../)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)