---
title: GetEnvironmentVariable()
second_title: Aspose.Slides برای C++ مرجع API
description: مقدار متغیر محیطی مشخص‌شده که با پردازش جاری مرتبط است را برمی‌گرداند.
type: docs
weight: 287
url: /fa/system/environment/getenvironmentvariable/
---
## Environment::GetEnvironmentVariable(const String\&) متد

مقدار متغیر محیطی مشخص‌شده که با پردازش جاری مرتبط است را برمی‌گرداند.

```cpp
static String System::Environment::GetEnvironmentVariable(const String &variable)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| variable | const [String](../../string/)\& | رشته‌ای که نام متغیر برای بازیابی را دربردارد |

### مقدار بازگشتی

مقدار متغیر مشخص‌شده

## Environment::GetEnvironmentVariable(const String\&, EnvironmentVariableTarget) متد

مقدار متغیر محیطی مشخص‌شده را از مکان مشخص‌شده برمی‌گرداند.

```cpp
static String System::Environment::GetEnvironmentVariable(const String &variable, EnvironmentVariableTarget target)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| variable | const [String](../../string/)\& | رشته‌ای که نام متغیر برای بازیابی را دربردارد |
| target | [EnvironmentVariableTarget](../../environmentvariabletarget/) | مکان متغیر |

### مقدار بازگشتی

مقدار متغیر مشخص‌شده

## موارد مرتبط

* شمارشی [EnvironmentVariableTarget](../../environmentvariabletarget/)
* کلاس [String](../../string/)
* ساختار [Environment](../)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)