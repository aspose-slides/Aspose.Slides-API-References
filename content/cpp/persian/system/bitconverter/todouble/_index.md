---
title: ToDouble()
second_title: Aspose.Slides برای C++ مرجع API
description: هشت بایت را از آرایهٔ مشخص شده که از ایندکس مشخص‌شده شروع می‌شود، به مقدار ممیز شناور دوبل‌دقت تبدیل می‌کند.
type: docs
weight: 144
url: /fa/system/bitconverter/todouble/
---
## BitConverter::ToDouble(const System::ArrayPtr\<uint8_t\>\&, int) متد

هشت بایت را از آرایهٔ مشخص شده که از ایندکس مشخص‌شده شروع می‌شود، به مقدار ممیز شناور دوبل‌دقت تبدیل می‌کند.

```cpp
static double System::BitConverter::ToDouble(const System::ArrayPtr<uint8_t> &value, int startIndex)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [System::ArrayPtr](../../arrayptr/)\<**uint8_t**\>\& | [Array](../../array/) که حاوی بایت‌ها برای تبدیل است |
| startIndex | int | [Index](../../index/) در آرایه‌ای که بایت‌ها برای تبدیل از آن شروع می‌شود |

### مقدار بازگشت

مقدار ممیز شناور دوبل‌دقت حاصل از تبدیل

## BitConverter::ToDouble(const System::Details::ArrayView\<uint8_t\>\&, int) متد

هشت بایت را از آرایهٔ مشخص شده که از ایندکس مشخص‌شده شروع می‌شود، به مقدار ممیز شناور دوبل‌دقت تبدیل می‌کند.

```cpp
static double System::BitConverter::ToDouble(const System::Details::ArrayView<uint8_t> &value, int startIndex)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const System::Details::ArrayView\<**uint8_t**\>\& | ArrayView که حاوی بایت‌ها برای تبدیل است |
| startIndex | int | [Index](../../index/) در آرایه‌ای که بایت‌ها برای تبدیل از آن شروع می‌شود |

### مقدار بازگشت

مقدار ممیز شناور دوبل‌دقت حاصل از تبدیل

## موارد مرتبط

* Typedef [ArrayPtr](../../arrayptr/)
* Class [BitConverter](../)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)