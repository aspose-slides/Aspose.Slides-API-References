---
title: ToBoolean()
second_title: مرجع API Aspose.Slides برای C++
description: یک بایت از آرایهٔ مشخص‌شده که از اندیس مشخص‌شده شروع می‌شود را به مقدار بولی تبدیل می‌کند.
type: docs
weight: 27
url: /fa/system/bitconverter/toboolean/
---
## BitConverter::ToBoolean(const System::ArrayPtr\<uint8_t\>\&, int) متد

یک بایت از آرایهٔ مشخص شده که از اندیس مشخص‌شده شروع می‌شود را به مقدار بولی تبدیل می‌کند.

```cpp
static bool System::BitConverter::ToBoolean(const System::ArrayPtr<uint8_t> &value, int startIndex)
```

### پارامترها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | const [System::ArrayPtr](../../arrayptr/)\<**uint8_t**\>\& | [Array](../../array/) که شامل بایت‌های مورد تبدیل است |
| startIndex | int | [Index](../../index/) در آرایه‌ای که برای شروع گرفتن بایت‌ها جهت تبدیل استفاده می‌شود |

### مقدار بازگشتی

[Boolean](../../boolean/) مقدار حاصل از تبدیل

## BitConverter::ToBoolean(const System::Details::ArrayView\<uint8_t\>\&, int) متد

یک بایت از آرایهٔ مشخص شده که از اندیس مشخص‌شده شروع می‌شود را به مقدار بولی تبدیل می‌کند.

```cpp
static bool System::BitConverter::ToBoolean(const System::Details::ArrayView<uint8_t> &value, int startIndex)
```

### پارامترها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | const System::Details::ArrayView\<**uint8_t**\>\& | ArrayView که شامل بایت‌های مورد تبدیل است |
| startIndex | int | [Index](../../index/) در آرایه‌ای که برای شروع گرفتن بایت‌ها جهت تبدیل استفاده می‌شود |

### مقدار بازگشتی

[Boolean](../../boolean/) مقدار حاصل از تبدیل

## موارد مرتبط

* Typedef [ArrayPtr](../../arrayptr/)
* کلاس [BitConverter](../)
* فضای‌نام [System](../../)
* Library [Aspose.Slides](../../../)