---
title: ToInt32()
second_title: مرجع API Aspose.Slides برای C++
description: چهار بایت را از آرایهٔ مشخص‌شده که از اندیس مشخص‌شده شروع می‌شوند، به مقدار عدد صحیح ۳۲-بیتی تبدیل می‌کند.
type: docs
weight: 66
url: /fa/system/bitconverter/toint32/
---
## BitConverter::ToInt32(const System::ArrayPtr\<uint8_t\>\&, int) متد

چهار بایت از آرایۀ مشخص‌شده را که از اندیس مشخص‌شده شروع می‌شوند، به مقدار عدد صحیح ۳۲-بیتی تبدیل می‌کند.

```cpp
static int System::BitConverter::ToInt32(const System::ArrayPtr<uint8_t> &value, int startIndex)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [System::ArrayPtr](../../arrayptr/)\<**uint8_t**\>\& | [Array](../../array/) که حاوی بایت‌های قابل تبدیل است |
| startIndex | int | [Index](../../index/) در آرایه‌ای که باید از آن بایت‌ها برای تبدیل گرفته شود |

### مقدار بازگشت

عدد صحیح ۳۲-بیتی حاصل از تبدیل

## BitConverter::ToInt32(const System::Details::ArrayView\<uint8_t\>\&, int) متد

چهار بایت از آرایۀ مشخص‌شده را که از اندیس مشخص‌شده شروع می‌شوند، به مقدار عدد صحیح ۳۲-بیتی تبدیل می‌کند.

```cpp
static int System::BitConverter::ToInt32(const System::Details::ArrayView<uint8_t> &value, int startIndex)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const System::Details::ArrayView\<**uint8_t**\>\& | ArrayView که حاوی بایت‌های قابل تبدیل است |
| startIndex | int | [Index](../../index/) در آرایه‌ای که باید از آن بایت‌ها برای تبدیل گرفته شود |

### مقدار بازگشت

عدد صحیح ۳۲-بیتی حاصل از تبدیل

## همچنین ببینید

* Typedef [ArrayPtr](../../arrayptr/)
* Class [BitConverter](../)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)