---
title: ToUInt64()
second_title: Aspose.Slides برای C++ مرجع API
description: هشت بایت را از آرایهٔ مشخص‌شده، از ایندکس مشخص‌شده، به مقدار عدد صحیح بدون علامت 64 بیتی تبدیل می‌کند.
type: docs
weight: 118
url: /fa/system/bitconverter/touint64/
---
## BitConverter::ToUInt64(const System::ArrayPtr\<uint8_t\>\&, int) متد

هشت بایت را از آرایهٔ مشخص‌شده، از ایندکس مشخص‌شده، به مقدار عدد صحیح بدون علامت 64 بیتی تبدیل می‌کند.

```cpp
static uint64_t System::BitConverter::ToUInt64(const System::ArrayPtr<uint8_t> &value, int startIndex)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [System::ArrayPtr](../../arrayptr/)\<**uint8_t**\>\& | [Array](../../array/) که شامل بایت‌ها برای تبدیل است |
| startIndex | int | [Index](../../index/) در آرایه‌ای که تبدیل باید از آنجا شروع شود |

### مقدار بازگشتی

مقدار عدد صحیح بدون علامت 64 بیتی که از تبدیل حاصل می‌شود

## BitConverter::ToUInt64(const System::Details::ArrayView\<uint8_t\>\&, int) متد

هشت بایت را از آرایهٔ مشخص‌شده، از ایندکس مشخص‌شده، به مقدار عدد صحیح بدون علامت 64 بیتی تبدیل می‌کند.

```cpp
static uint64_t System::BitConverter::ToUInt64(const System::Details::ArrayView<uint8_t> &value, int startIndex)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const System::Details::ArrayView\<**uint8_t**\>\& | ArrayView که شامل بایت‌ها برای تبدیل است |
| startIndex | int | [Index](../../index/) در آرایه‌ای که تبدیل باید از آنجا شروع شود |

### مقدار بازگشتی

مقدار عدد صحیح بدون علامت 64 بیتی که از تبدیل حاصل می‌شود

## مراجع

* تعریف نوع [ArrayPtr](../../arrayptr/)
* کلاس [BitConverter](../)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)