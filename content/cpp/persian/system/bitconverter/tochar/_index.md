---
title: ToChar()
second_title: Aspose.Slides برای C++ مرجع API
description: دو بایت را از آرایهٔ مشخص شده که از ایندکس مشخص شده شروع می‌شود، به مقدار char_t تبدیل می‌کند.
type: docs
weight: 40
url: /fa/system/bitconverter/tochar/
---
## BitConverter::ToChar(const System::ArrayPtr\<uint8_t\>\&, int) متد

دو بایت را از آرایهٔ مشخص شده که از ایندکس مشخص شده شروع می‌شود به مقدار char_t تبدیل می‌کند.

```cpp
static char_t System::BitConverter::ToChar(const System::ArrayPtr<uint8_t> &value, int startIndex)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [System::ArrayPtr](../../arrayptr/)\<**uint8_t**\>\& | [Array](../../array/) که شامل بایت‌های قابل تبدیل است |
| startIndex | int | [Index](../../index/) در آرایه‌ای که تبدیل بایت‌ها از آن شروع می‌شود |

### مقدار بازگشت

char_t value resulting from conversion

## BitConverter::ToChar(const System::Details::ArrayView\<uint8_t\>\&, int) متد

دو بایت را از آرایهٔ مشخص شده که از ایندکس مشخص شده شروع می‌شود به مقدار char_t تبدیل می‌کند.

```cpp
static char_t System::BitConverter::ToChar(const System::Details::ArrayView<uint8_t> &value, int startIndex)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const System::Details::ArrayView\<**uint8_t**\>\& | ArrayView که شامل بایت‌های قابل تبدیل است |
| startIndex | int | [Index](../../index/) در آرایه‌ای که تبدیل بایت‌ها از آن شروع می‌شود |

### مقدار بازگشت

char_t value resulting from conversion

## همچنین ببینید

* Typedef [ArrayPtr](../../arrayptr/)
* Class [BitConverter](../)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)