---
title: ToInt64()
second_title: مرجع API Aspose.Slides برای C++
description: هشت بایت را از آرایهٔ مشخص‌شده که از ایندکس مشخص‌شده شروع می‌شود، به مقدار عدد صحیح ۶۴ بیتی تبدیل می‌کند.
type: docs
weight: 79
url: /fa/system/bitconverter/toint64/
---
## BitConverter::ToInt64(const System::ArrayPtr\<uint8_t\>\&, int) متد

هشت بایت را از آرایهٔ مشخص‌شده که از ایندکس مشخص‌شده شروع می‌شود، به مقدار عدد صحیح ۶۴ بیتی تبدیل می‌کند.

```cpp
static int64_t System::BitConverter::ToInt64(const System::ArrayPtr<uint8_t> &value, int startIndex)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [System::ArrayPtr](../../arrayptr/)\<**uint8_t**\>\& | [Array](../../array/) که شامل بایت‌های قابل تبدیل است |
| startIndex | int | [Index](../../index/) در آرایه‌ای که برای شروع دریافت بایت‌ها جهت تبدیل استفاده می‌شود |

### مقدار بازگشت

مقدار عدد صحیح ۶۴ بیتی حاصل از تبدیل

## BitConverter::ToInt64(const System::Details::ArrayView\<uint8_t\>\&, int) متد

هشت بایت را از آرایهٔ مشخص‌شده که از ایندکس مشخص‌شده شروع می‌شود، به مقدار عدد صحیح ۶۴ بیتی تبدیل می‌کند.

```cpp
static int64_t System::BitConverter::ToInt64(const System::Details::ArrayView<uint8_t> &value, int startIndex)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const System::Details::ArrayView\<**uint8_t**\>\& | ArrayView که شامل بایت‌های قابل تبدیل است |
| startIndex | int | [Index](../../index/) در آرایه‌ای که برای شروع دریافت بایت‌ها جهت تبدیل استفاده می‌شود |

### مقدار بازگشت

مقدار عدد صحیح ۶۴ بیتی حاصل از تبدیل

## موارد مرتبط

* Typedef [ArrayPtr](../../arrayptr/)
* Class [BitConverter](../)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)