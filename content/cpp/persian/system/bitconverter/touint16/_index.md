---
title: ToUInt16()
second_title: مرجع API Aspose.Slides برای C++
description: دو بایت را از آرایه مشخص‌شده که از ایندکس معین شروع می‌شود، به مقدار عدد صحیح بدون علامت ۱۶ بیتی تبدیل می‌کند.
type: docs
weight: 92
url: /fa/system/bitconverter/touint16/
---
## BitConverter::ToUInt16(const System::ArrayPtr\<uint8_t\>\&, int) متد

دو بایت را از آرایه مشخص‌شده که از ایندکس مشخص‌شده شروع می‌شود، به مقدار عدد صحیح بدون علامت ۱۶ بیتی تبدیل می‌کند.

```cpp
static uint16_t System::BitConverter::ToUInt16(const System::ArrayPtr<uint8_t> &value, int startIndex)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [System::ArrayPtr](../../arrayptr/)\<**uint8_t**\>\& | [Array](../../array/) که حاوی بایت‌های قابل تبدیل است |
| startIndex | int | [Index](../../index/) در آرایه‌ای که تبدیل بایت‌ها از آنجا آغاز می‌شود |

### مقدار بازگشتی

مقدار عدد صحیح بدون علامت ۱۶ بیتی حاصل از تبدیل

## BitConverter::ToUInt16(const System::Details::ArrayView\<uint8_t\>\&, int) متد

دو بایت را از آرایه مشخص‌شده که از ایندکس مشخص‌شده شروع می‌شود، به مقدار عدد صحیح بدون علامت ۱۶ بیتی تبدیل می‌کند.

```cpp
static uint16_t System::BitConverter::ToUInt16(const System::Details::ArrayView<uint8_t> &value, int startIndex)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const System::Details::ArrayView\<**uint8_t**\>\& | ArrayView که حاوی بایت‌های قابل تبدیل است |
| startIndex | int | [Index](../../index/) در آرایه‌ای که تبدیل بایت‌ها از آنجا آغاز می‌شود |

### مقدار بازگشتی

مقدار عدد صحیح بدون علامت ۱۶ بیتی حاصل از تبدیل

## موارد مرتبط

* Typedef [ArrayPtr](../../arrayptr/)
* Class [BitConverter](../)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)