---
title: GetBytes()
second_title: Aspose.Slides برای C++ مرجع API
description: مقدار بولی مشخص را به آرایه‌ای از بایت‌ها تبدیل می‌کند.
type: docs
weight: 14
url: /fa/system/bitconverter/getbytes/
---
## BitConverter::GetBytes(bool) متد

مقدار بولی مشخص را به آرایه‌ای از بایت‌ها تبدیل می‌کند.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(bool value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **bool** | مقدار بولی برای تبدیل |

### مقدار بازگشت

آرایه 1 بایتی نمایانگر مقدار مشخص

## BitConverter::GetBytes(char_t) متد

مقدار char_t مشخص را به آرایه‌ای از بایت‌ها تبدیل می‌کند.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(char_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | char_t | مقدار char_t برای تبدیل |

### مقدار بازگشت

آرایه 2 بایتی نمایانگر مقدار مشخص

## BitConverter::GetBytes(int16_t) متد

مقدار عدد صحیح 16 بیتی مشخص را به آرایه‌ای از بایت‌ها تبدیل می‌کند.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int16_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **int16_t** | عدد صحیح 16 بیتی برای تبدیل |

### مقدار بازگشت

آرایه 2 بایتی نمایانگر مقدار مشخص

## BitConverter::GetBytes(int) متد

مقدار عدد صحیح 32 بیتی مشخص را به آرایه‌ای از بایت‌ها تبدیل می‌کند.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int | عدد صحیح 32 بیتی برای تبدیل |

### مقدار بازگشت

آرایه 4 بایتی نمایانگر مقدار مشخص

## BitConverter::GetBytes(int64_t) متد

مقدار عدد صحیح 64 بیتی مشخص را به آرایه‌ای از بایت‌ها تبدیل می‌کند.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int64_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **int64_t** | عدد صحیح 64 بیتی برای تبدیل |

### مقدار بازگشت

آرایه 8 بایتی نمایانگر مقدار مشخص

## BitConverter::GetBytes(uint16_t) متد

مقدار عدد صحیح بدون علامت 16 بیتی مشخص را به آرایه‌ای از بایت‌ها تبدیل می‌کند.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint16_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **uint16_t** | عدد صحیح بدون علامت 16 بیتی برای تبدیل |

### مقدار بازگشت

آرایه 2 بایتی نمایانگر مقدار مشخص

## BitConverter::GetBytes(uint32_t) متد

مقدار عدد صحیح بدون علامت 32 بیتی مشخص را به آرایه‌ای از بایت‌ها تبدیل می‌کند.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint32_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **uint32_t** | عدد صحیح بدون علامت 32 بیتی برای تبدیل |

### مقدار بازگشت

آرایه 4 بایتی نمایانگر مقدار مشخص

## BitConverter::GetBytes(uint64_t) متد

مقدار عدد صحیح بدون علامت 64 بیتی مشخص را به آرایه‌ای از بایت‌ها تبدیل می‌کند.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint64_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **uint64_t** | عدد صحیح بدون علامت 64 بیتی برای تبدیل |

### مقدار بازگشت

آرایه 8 بایتی نمایانگر مقدار مشخص

## BitConverter::GetBytes(float) متد

مقدار عدد شناور تک‌دقتی (float) مشخص را به آرایه‌ای از بایت‌ها تبدیل می‌کند.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(float value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **float** | مقدار عدد شناور برای تبدیل |

### مقدار بازگشت

آرایه 4 بایتی نمایانگر مقدار مشخص

## BitConverter::GetBytes(double) متد

مقدار عدد شناور دو‌دقتی (double) مشخص را به آرایه‌ای از بایت‌ها تبدیل می‌کند.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(double value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **double** | مقدار عدد شناور برای تبدیل |

### مقدار بازگشت

آرایه 8 بایتی نمایانگر مقدار مشخص

## موارد مرتبط

* Typedef [ArrayPtr](../../arrayptr/)
* Class [BitConverter](../)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)