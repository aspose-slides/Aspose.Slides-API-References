---
title: ToUInt32()
second_title: Aspose.Slides برای C++ مرجع API
description: چهار بایت را از آرایهٔ مشخص‌شده که از ایندکس مشخص‌شده شروع می‌شود به مقدار عدد صحیح ۳۲ بیتی بدون علامت تبدیل می‌کند.
type: docs
weight: 105
url: /fa/system/bitconverter/touint32/
---
## BitConverter::ToUInt32(const System::ArrayPtr\<uint8_t\>\&, int) متد

چهار بایت را از آرایهٔ مشخص‌شده که از اندیس مشخص‌شده شروع می‌شود به مقدار عدد صحیح ۳۲ بیتی بدون علامت تبدیل می‌کند.

```cpp
static uint32_t System::BitConverter::ToUInt32(const System::ArrayPtr<uint8_t> &value, int startIndex)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [System::ArrayPtr](../../arrayptr/)\<**uint8_t**\>\& | [Array](../../array/) که حاوی بایت‌های برای تبدیل است |
| startIndex | int | [Index](../../index/) در آرایه که تبدیل بایت‌ها از آنجا آغاز می‌شود |

### مقدار بازگشتی

مقدار عدد صحیح ۳۲ بیتی بدون علامت حاصل از تبدیل

## BitConverter::ToUInt32(const System::Details::ArrayView\<uint8_t\>\&, int) متد

چهار بایت را از آرایهٔ مشخص‌شده که از اندیس مشخص‌شده شروع می‌شود به مقدار عدد صحیح ۳۲ بیتی بدون علامت تبدیل می‌کند.

```cpp
static uint32_t System::BitConverter::ToUInt32(const System::Details::ArrayView<uint8_t> &value, int startIndex)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const System::Details::ArrayView\<**uint8_t**\>\& | ArrayView که حاوی بایت‌های برای تبدیل است |
| startIndex | int | [Index](../../index/) در آرایه که تبدیل بایت‌ها از آنجا آغاز می‌شود |

### مقدار بازگشتی

مقدار عدد صحیح ۳۲ بیتی بدون علامت حاصل از تبدیل

## همچنین ببینید

* Typedef [ArrayPtr](../../arrayptr/)
* کلاس [BitConverter](../)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)