---
title: EqualsExact()
second_title: مرجع API Aspose.Slides برای C++
description: بررسی می‌کند آیا دو شیء DateTimeOffset نمایانگر یک نقطه زمانی یکسان هستند و جابه‌جایی یکسانی دارند.
type: docs
weight: 378
url: /fa/system/datetimeoffset/equalsexact/
---
## DateTimeOffset::EqualsExact(const DateTimeOffset\&) const متد

بررسی می‌کند آیا دو شیء [DateTimeOffset](../) نمایانگر یک نقطه زمانی یکسان هستند و جابه‌جایی یکسانی دارند.

```cpp
bool System::DateTimeOffset::EqualsExact(const DateTimeOffset &other) const
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/) برای مقایسه. |

### مقدار بازگشتی

true اگر شیء جاری دقیقاً برابر با **other** باشد، در غیر این صورت - false.

## DateTimeOffset::EqualsExact(const SharedPtr\<Object\>\&) const متد

بررسی می‌کند آیا دو شیء [DateTimeOffset](../) نمایانگر یک نقطه زمانی یکسان هستند و جابه‌جایی یکسانی دارند.

```cpp
bool System::DateTimeOffset::EqualsExact(const SharedPtr<Object> &obj) const
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/) برای مقایسه. |

### مقدار بازگشتی

true اگر شیء جاری دقیقاً برابر با **obj** باشد، در غیر این صورت - false.

## موارد مرتبط

* Typedef [SharedPtr](../../sharedptr/)
* کلاس [DateTimeOffset](../)
* کلاس [Object](../../object/)
* فضای‌نام [System](../../)
* Library [Aspose.Slides](../../../)