---
title: ToString()
second_title: مرجع API Aspose.Slides برای C++
description: شی جاری را با استفاده از قالب مشخص و ارائه‌کننده قالب به رشته تبدیل می‌کند.
type: docs
weight: 443
url: /fa/system/datetimeoffset/tostring/
---
## DateTimeOffset::ToString(const String\&, const SharedPtr\<IFormatProvider\>\&) const متد

شی جاری را با استفاده از قالب مشخص و ارائه‌کننده قالب به رشته تبدیل می‌کند.

```cpp
String System::DateTimeOffset::ToString(const String &format, const SharedPtr<IFormatProvider> &provider) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| format | const [String](../../string/)\& | رشته قالب. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | ارائه‌کننده قالب. |

### مقدار بازگشت

[String](../../string/) نمایش شی [DateTimeOffset](../) جاری.

## DateTimeOffset::ToString(const SharedPtr\<IFormatProvider\>\&) const متد

شی جاری را با استفاده از ارائه‌کننده قالب مشخص به رشته تبدیل می‌کند.

```cpp
String System::DateTimeOffset::ToString(const SharedPtr<IFormatProvider> &provider) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | ارائه‌کننده قالب. |

### مقدار بازگشت

[String](../../string/) نمایش شی [DateTimeOffset](../) جاری.

## DateTimeOffset::ToString(const String\&) const متد

شی جاری را با استفاده از قالب مشخص به رشته تبدیل می‌کند.

```cpp
String System::DateTimeOffset::ToString(const String &format) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| format | const [String](../../string/)\& | رشته قالب. |

### مقدار بازگشت

[String](../../string/) نمایش شی [DateTimeOffset](../) جاری.

## DateTimeOffset::ToString() const متد

شی جاری را به رشته تبدیل می‌کند.

```cpp
String System::DateTimeOffset::ToString() const
```

### مقدار بازگشت

[String](../../string/) نمایش شی [DateTimeOffset](../) جاری.

## موارد مرتبط

* تعریف‌نوع [SharedPtr](../../sharedptr/)
* کلاس [String](../../string/)
* کلاس [IFormatProvider](../../iformatprovider/)
* کلاس [DateTimeOffset](../)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)