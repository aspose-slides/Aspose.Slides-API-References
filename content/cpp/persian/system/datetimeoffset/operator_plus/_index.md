---
title: operator+()
second_title: مرجع API Aspose.Slides برای C++
description: یک نمونهٔ جدید از کلاس DateTimeOffset برمی‌گرداند که مقدار تاریخ و زمان را که مجموع مقداری که شیء فعلی نمایش می‌دهد و بازهٔ زمانی مشخص‌شده است، نشان می‌دهد.
type: docs
weight: 508
url: /fa/system/datetimeoffset/operator_plus/
---
## DateTimeOffset::operator+(TimeSpan) const متد

یک نمونهٔ جدید از کلاس [DateTimeOffset](../) برمی‌گرداند که مقدار تاریخ و زمان را که مجموع مقداری که شیء فعلی نمایش می‌دهد و بازهٔ زمانی مشخص‌شده است، نمایان می‌کند.

```cpp
DateTimeOffset System::DateTimeOffset::operator+(TimeSpan value) const
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | [TimeSpan](../../timespan/) | بازهٔ زمانی برای اضافه کردن به مقداری که شیء فعلی نمایش می‌دهد |

### مقدار بازگشت

یک نمونهٔ جدید از کلاس [DateTimeOffset](../) که مقدار تاریخ و زمان را که مجموع مقداری که شیء فعلی نمایش می‌دهد و **value** است، نمایان می‌کند.

## موارد مرتبط

* کلاس [DateTimeOffset](../)
* کلاس [TimeSpan](../../timespan/)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)