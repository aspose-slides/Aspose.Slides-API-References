---
title: operator-()
second_title: Aspose.Slides برای C++ مرجع API
description: یک نمونهٔ جدید از کلاس DateTimeOffset را برمی‌گرداند که مقدار تاریخ و زمان را نشان می‌دهد که نتیجهٔ تفریق بازهٔ زمانی مشخص‌شده از مقدار نمایان‌شده توسط شیء فعلی است.
type: docs
weight: 521
url: /fa/system/datetimeoffset/operator_minus/
---
## DateTimeOffset::operator-(TimeSpan) const متد

یک نمونهٔ جدید از کلاس [DateTimeOffset](../) را برمی‌گرداند که مقدار تاریخ و زمان را نشان می‌دهد که نتیجهٔ تفریق بازهٔ زمانی مشخص‌شده از مقدار نمایان‌شده توسط شیء فعلی است.

```cpp
DateTimeOffset System::DateTimeOffset::operator-(TimeSpan value) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | [TimeSpan](../../timespan/) | یک بازهٔ زمانی برای تفریق |

### مقدار بازگشتی

یک نمونهٔ جدید از کلاس [DateTimeOffset](../) که مقدار تاریخ و زمان را نشان می‌دهد که نتیجهٔ تفریق **value** از مقدار نمایان‌شده توسط شیء فعلی است.

## DateTimeOffset::operator-(const DateTimeOffset\&) const متد

یک نمونهٔ جدید از کلاس [TimeSpan](../../timespan/) را برمی‌گرداند که بازهٔ زمانی بین مقادیر تاریخ و زمان نمایان‌شده توسط شیء فعلی و شیء مشخص‌شده را نشان می‌دهد.

```cpp
TimeSpan System::DateTimeOffset::operator-(const DateTimeOffset &other) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | یک نمونه از کلاس [DateTime](../../datetime/) که یک انتهای بازهٔ محاسبه‌شده را نشان می‌دهد |

### مقدار بازگشتی

یک نمونه از کلاس [TimeSpan](../../timespan/) که بازهٔ زمانی بین مقادیر تاریخ و زمان نمایان‌شده توسط شیء فعلی و **other** را نشان می‌دهد.

## مراجع

* کلاس [DateTimeOffset](../)
* کلاس [TimeSpan](../../timespan/)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)