---
title: Equals()
second_title: مرجع API Aspose.Slides برای C++
description: بررسی می‌کند که آیا دو شیء DateTimeOffset نمایانگر یک نقطه زمانی هستند.
type: docs
weight: 365
url: /fa/system/datetimeoffset/equals/
---
## DateTimeOffset::Equals(const DateTimeOffset\&) const متد

بررسی می‌کند که آیا دو شیء [DateTimeOffset](../) نمایانگر یک نقطه زمانی هستند.

```cpp
bool System::DateTimeOffset::Equals(const DateTimeOffset &other) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/) برای مقایسه. |

### مقدار بازگشت

true اگر شیء جاری برابر با **other** باشد، در غیر این صورت - false.

## DateTimeOffset::Equals(const SharedPtr\<Object\>\&) const متد

بررسی می‌کند که آیا دو شیء [DateTimeOffset](../) نمایانگر یک نقطه زمانی هستند.

```cpp
bool System::DateTimeOffset::Equals(const SharedPtr<Object> &obj) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/) برای مقایسه. |

### مقدار بازگشت

true اگر شیء جاری برابر با **obj** باشد، در غیر این صورت - false.

## DateTimeOffset::Equals(const DateTimeOffset\&, const DateTimeOffset\&) متد

بررسی می‌کند که آیا دو شیء [DateTimeOffset](../) نمایانگر یک نقطه زمانی هستند.

```cpp
static bool System::DateTimeOffset::Equals(const DateTimeOffset &first, const DateTimeOffset &second)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| first | const [DateTimeOffset](../)\& | اولین شیء برای مقایسه. |
| second | const [DateTimeOffset](../)\& | دومین شیء برای مقایسه. |

### مقدار بازگشت

true اگر **first** برابر با **second** باشد، در غیر این صورت - false.

## موارد مرتبط

* Typedef [SharedPtr](../../sharedptr/)
* کلاس [DateTimeOffset](../)
* کلاس [Object](../../object/)
* فضازمان [System](../../)
* کتابخانه [Aspose.Slides](../../../)