---
title: operator>()
second_title: مرجع API Aspose.Slides برای C++
description: تعیین می‌کند آیا شیء جاری مقدار تاریخ و زمان را نمایان می‌کند که پس از مقداری باشد که توسط شیء DateTimeOffset مشخص شده نمایان می‌شود.
type: docs
weight: 573
url: /fa/system/datetimeoffset/operator_greater/
---
## DateTimeOffset::operator>(const DateTimeOffset\&) const متد

تعیین می‌کند آیا شیء جاری مقدار تاریخ و زمان را نمایان می‌کند که پس از مقداری باشد که توسط شیء [DateTimeOffset](../) مشخص شده نمایان می‌شود.

```cpp
bool System::DateTimeOffset::operator>(const DateTimeOffset &other) const
```

### Arguments

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | شیء [DateTimeOffset](../) برای مقایسهٔ شیء جاری با |

### Return Value

True اگر مقدار تاریخ و زمان نمایان‌شده توسط شیء جاری پس از مقداری که توسط **other** نمایان شده باشد، در غیر این صورت - false

## DateTimeOffset::operator>(std::nullptr_t) const متد




```cpp
constexpr bool System::DateTimeOffset::operator>(std::nullptr_t) const
```

## See Also

* کلاس [DateTimeOffset](../)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)