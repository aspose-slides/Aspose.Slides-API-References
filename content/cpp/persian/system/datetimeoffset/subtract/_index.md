---
title: Subtract()
second_title: Aspose.Slides برای C++ مرجع API
description: یک بازه زمانی مشخص را از شیء فعلی کم می‌کند.
type: docs
weight: 391
url: /fa/system/datetimeoffset/subtract/
---
## متد DateTimeOffset::Subtract(TimeSpan) const

یک بازه زمانی مشخص را از شیء فعلی کم می‌کند.

```cpp
DateTimeOffset System::DateTimeOffset::Subtract(TimeSpan value) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | [TimeSpan](../../timespan/) | مقدار برای کم کردن. |

### مقدار بازگشت

نتیجه عملیات.

## متد DateTimeOffset::Subtract(const DateTimeOffset\&) const

یک مقدار [DateTimeOffset](../) مشخص را از شیء فعلی کم می‌کند.

```cpp
TimeSpan System::DateTimeOffset::Subtract(const DateTimeOffset &value) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [DateTimeOffset](../)\& | مقدار برای کم کردن. |

### مقدار بازگشت

نتیجه عملیات.

## موارد مرتبط

* کلاس [DateTimeOffset](../)
* کلاس [TimeSpan](../../timespan/)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)