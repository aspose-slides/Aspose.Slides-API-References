---
title: Subtract()
second_title: Aspose.Slides برای C++ مرجع API
description: یک نمونه جدید از کلاس DateTime را برمی‌گرداند که مقدار تاریخ و زمان را نشان می‌دهد که نتیجه حذف بازه زمانی مشخص‌شده از مقداری است که شیء جاری نمایانگر آن است.
type: docs
weight: 326
url: /fa/system/datetime/subtract/
---
## DateTime::Subtract(TimeSpan) const متد

یک نمونه جدید از کلاس [DateTime](../) برمی‌گرداند که مقدار تاریخ و زمان حاصل از حذف بازه زمانی مشخص شده از مقداری که شیء جاری نمایانگر آن است را نشان می‌دهد.

```cpp
DateTime System::DateTime::Subtract(TimeSpan duration) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| duration | [TimeSpan](../../timespan/) | بازه زمانی برای حذف |

### مقدار بازگشت

یک نمونه جدید از کلاس [DateTime](../) برمی‌گرداند که مقدار تاریخ و زمان حاصل از حذف **duration** از مقداری که شیء جاری نمایانگر آن است را نشان می‌دهد.

## DateTime::Subtract(DateTime) const متد

یک نمونه از کلاس [TimeSpan](../../timespan/) برمی‌گرداند که بازه زمانی بین مقادیر تاریخ و زمان نمایانگر شیء جاری و شیء مشخص‌شده را نشان می‌دهد.

```cpp
constexpr TimeSpan System::DateTime::Subtract(DateTime value) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | [DateTime](../) | یک نمونه از کلاس [DateTime](../) که یکی از انتهای بازه محاسبه‌شده را علامت‌گذاری می‌کند |

### مقدار بازگشت

یک نمونه از کلاس [TimeSpan](../../timespan/) برمی‌گرداند که بازه زمانی بین مقادیر تاریخ و زمان نمایانگر شیء جاری و **value** را نشان می‌دهد.

## موارد مرتبط

* کلاس [DateTime](../)
* کلاس [TimeSpan](../../timespan/)
* فضا‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)