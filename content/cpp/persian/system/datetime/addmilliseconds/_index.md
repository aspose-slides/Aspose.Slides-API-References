---
title: AddMilliseconds()
second_title: Aspose.Slides برای C++ مرجع API
description: یک نمونهٔ جدید از کلاس DateTime را بر می‌گرداند که مقدار تاریخ و زمان را نشان می‌دهد و مجموع مقدار نمایان‌شده توسط شیء جاری و تعداد میلی‌ثانیهٔ مشخص‌شده است.
type: docs
weight: 300
url: /fa/system/datetime/addmilliseconds/
---
## DateTime::AddMilliseconds(double) const متد

باز می‌گرداند یک نمونهٔ جدید از کلاس [DateTime](../) که مقدار تاریخ و زمان را نشان می‌دهد که مجموع مقداری است که توسط شیء جاری نمایان می‌شود و تعداد میلی‌ثانیهٔ مشخص‌شده.

```cpp
DateTime System::DateTime::AddMilliseconds(double value) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **double** | تعداد میلی‌ثانیه‌هایی که باید اضافه شوند؛ value می‌تواند مثبت یا منفی باشد |

## مقدار بازگشتی

یک نمونهٔ جدید از کلاس [DateTime](../) که مقدار تاریخ و زمان را نشان می‌دهد که مجموع مقداری است که توسط شیء جاری نمایان می‌شود و **value** میلی‌ثانیه.

## مراجع

* کلاس [DateTime](../)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)