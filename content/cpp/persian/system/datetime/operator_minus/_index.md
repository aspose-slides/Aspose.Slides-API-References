---
title: operator-()
second_title: Aspose.Slides برای C++ مرجع API
description: یک نمونهٔ جدید از کلاس DateTime برمی‌گرداند که مقدار تاریخ و زمان را نشان می‌دهد که نتیجهٔ تفریق بازهٔ زمانی مشخص‌شده از مقداری است که توسط شیء فعلی نمایان شده است.
type: docs
weight: 651
url: /fa/system/datetime/operator_minus/
---
## DateTime::operator-(TimeSpan) const متد


یک نمونه جدید از کلاس [DateTime](../) برمی‌گرداند که مقدار تاریخ و زمان را نمایان می‌سازد که نتیجهٔ تفریق بازهٔ زمانی مشخص‌شده از مقداری است که توسط شیء فعلی نمایان شده است.

```cpp
DateTime System::DateTime::operator-(TimeSpan value) const
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | [TimeSpan](../../timespan/) | یک بازهٔ زمانی برای تفریق |

### مقدار بازگشت

یک نمونه جدید از کلاس [DateTime](../) برمی‌گرداند که مقدار تاریخ و زمان را نمایان می‌سازد که نتیجهٔ تفریق **value** از مقداری است که توسط شیء فعلی نمایان شده است.

## DateTime::operator-(DateTime) const متد


یک نمونه از کلاس [TimeSpan](../../timespan/) برمی‌گرداند که بازهٔ زمانی بین مقادیر تاریخ و زمان نمایان‌شده توسط شیء فعلی و شیء مشخص‌شده را نشان می‌دهد.

```cpp
constexpr TimeSpan System::DateTime::operator-(DateTime value) const
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | [DateTime](../) | یک نمونه از کلاس [DateTime](../) که یک سر انتهای بازهٔ محاسبه‌شده را نشان می‌دهد |

### مقدار بازگشت

یک نمونه از کلاس [TimeSpan](../../timespan/) برمی‌گرداند که بازهٔ زمانی بین مقادیر تاریخ و زمان نمایان‌شده توسط شیء فعلی و **value** را نشان می‌دهد.

## موارد مرتبط

* کلاس [DateTime](../)
* کلاس [TimeSpan](../../timespan/)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)