---
title: operator<=()
second_title: مرجع API Aspose.Slides برای C++
description: تعیین می‌کند که آیا شیء فعلی نمایانگر مقدار تاریخ و زمان است که پیش‌تر یا همان مقداری است که توسط شیء DateTime نمایان شده است.
type: docs
weight: 599
url: /fa/system/datetime/operator_less_equal/
---
## DateTime::operator<=(DateTime) const متد

تعیین می‌کند که آیا شیء فعلی نمایانگر مقدار تاریخ و زمان است که پیش‌تر یا همان مقدار باشد که توسط شیء [DateTime](../) مشخص شده نمایان می‌شود.

```cpp
constexpr bool System::DateTime::operator<=(DateTime other) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| other | [DateTime](../) | شیء [DateTime](../) برای مقایسهٔ شیء فعلی |

### مقدار بازگشت

در صورتی که مقدار تاریخ و زمان نمایان‌ساز توسط شیء فعلی پیش‌تر یا همان مقدار نمایان‌ساز **other** باشد، درست؛ در غیر این صورت - غلط

## DateTime::operator<=(std::nullptr_t) const متد




```cpp
constexpr bool System::DateTime::operator<=(std::nullptr_t) const
```

## موارد مرتبط

* کلاس [DateTime](../)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)