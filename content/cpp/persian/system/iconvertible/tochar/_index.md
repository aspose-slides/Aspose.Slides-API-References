---
title: ToChar()
second_title: مرجع API Aspose.Slides برای C++
description: مقدار این نمونه را به کاراکتر یونیکد معادل تبدیل می‌کند که از اطلاعات قالب‌بندی خاص فرهنگ مشخص‌شده استفاده می‌کند.
type: docs
weight: 27
url: /fa/system/iconvertible/tochar/
---
## IConvertible::ToChar(System::SharedPtr\<System::IFormatProvider\>) متد

مقدار این نمونه را به یک کاراکتر Unicode معادل تبدیل می‌کند که از اطلاعات قالب‌بندی خاص فرهنگ مشخص‌شده استفاده می‌کند.

```cpp
virtual char_t System::IConvertible::ToChar(System::SharedPtr<System::IFormatProvider> provider)=0
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| provider | [System::SharedPtr](../../sharedptr/)\<[System::IFormatProvider](../../iformatprovider/)\> | یک پیاده‌سازی رابط [System::IFormatProvider](../../iformatprovider/) که اطلاعات قالب‌بندی خاص فرهنگ را فراهم می‌کند. |

### مقدار بازگشت

یک کاراکتر Unicode معادل مقدار این نمونه.

## موارد مرتبط

* تعریف نوع [SharedPtr](../../sharedptr/)
* کلاس [IFormatProvider](../../iformatprovider/)
* کلاس [IConvertible](../)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)