---
title: ToString()
second_title: مرجع API Aspose.Slides برای C++
description: "مقدار این نمونه را به یک System::String معادل تبدیل می‌کند با استفاده از اطلاعات قالب‌بندی خاص فرهنگ مشخص شده."
type: docs
weight: 196
url: /fa/system/iconvertible/tostring/
---
## IConvertible::ToString(System::SharedPtr\<System::IFormatProvider\>) متد

مقدار این نمونه را به یک [System::String](../../string/) معادل تبدیل می‌کند با استفاده از اطلاعات قالب‌بندی خاص فرهنگ مشخص شده.

```cpp
virtual System::String System::IConvertible::ToString(System::SharedPtr<System::IFormatProvider> provider)=0
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| provider | [System::SharedPtr](../../sharedptr/)\<[System::IFormatProvider](../../iformatprovider/)\> | یک پیاده‌سازی رابط [System::IFormatProvider](../../iformatprovider/) که اطلاعات قالب‌بندی خاص فرهنگ را فراهم می‌کند. |

### مقدار بازگشت

یک نمونه [System::String](../../string/) معادل مقدار این نمونه.

## IConvertible::ToString() const متد

معادل متد [Object.ToString()](../../object/tostring/) در C#. امکان تبدیل اشیاء سفارشی به رشته را فراهم می‌کند.

```cpp
virtual String System::Object::ToString() const
```

### مقدار بازگشت

نمایش [String](../../string/) همان‌طوری که توسط کلاس نهایی ارائه می‌شود.

## موارد مرتبط

* Typedef [SharedPtr](../../sharedptr/)
* کلاس [String](../../string/)
* کلاس [IFormatProvider](../../iformatprovider/)
* کلاس [IConvertible](../)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)