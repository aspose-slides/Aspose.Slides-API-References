---
title: end()
second_title: مرجع API Aspose.Slides برای C++
description: دسترس‌گر برای متد end() از یک مجموعهٔ زیرین. فقط زمانی کامپایل می‌شود که SmartPtr_ نوع تخصصی باشد با متد end().
type: docs
weight: 391
url: /fa/system/smartptr/end/
---
## SmartPtr::end() متد

دسترس‌گر برای [end()](./) متد از یک مجموعهٔ زیرین. فقط در صورتی کامپایل می‌شود که SmartPtr_ نوع تخصصی باشد با [end()](./) متد.

```cpp
template<typename Q> auto System::SmartPtr<T>::end() -> decltype(std::declval<Q>().end())
```

### مقدار بازگشت

تکرارگر به انتهای مجموعه

## SmartPtr::end() const متد

دسترس‌گر برای [end()](./) متد از یک مجموعهٔ زیرین. فقط در صورتی کامپایل می‌شود که SmartPtr_ نوع تخصصی باشد با [end()](./) متد.

```cpp
template<typename Q> auto System::SmartPtr<T>::end() const -> decltype(std::declval<const Q>().end())
```

### مقدار بازگشت

تکرارگر به انتهای مجموعه

## موارد مرتبط

* کلاس [SmartPtr](../)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)