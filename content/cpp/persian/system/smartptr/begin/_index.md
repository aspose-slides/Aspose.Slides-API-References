---
title: begin()
second_title: Aspose.Slides برای مرجع API C++
description: دسترس‌پذیری برای متد begin() یک مجموعه زیرین. فقط در صورتی که SmartPtr_ نوع تخصصی باشد با متد begin() کامپایل می‌شود.
type: docs
weight: 378
url: /fa/system/smartptr/begin/
---
## SmartPtr::begin() متد

دسترس‌پذیری برای متد [begin()](./) یک مجموعه زیرین. فقط در صورتی کامپایل می‌شود که SmartPtr_ نوع تخصصی باشد با متد [begin()](./).

```cpp
template<typename Q> auto System::SmartPtr<T>::begin() -> decltype(std::declval<Q>().begin())
```

### مقدار بازگشتی

تکرارگر به ابتدای مجموعه

## SmartPtr::begin() const متد

دسترس‌پذیری برای متد [begin()](./) یک مجموعه زیرین. فقط در صورتی کامپایل می‌شود که SmartPtr_ نوع تخصصی باشد با متد [begin()](./).

```cpp
template<typename Q> auto System::SmartPtr<T>::begin() const -> decltype(std::declval<const Q>().begin())
```

### مقدار بازگشتی

تکرارگر به ابتدای مجموعه

## موارد مرتبط

* کلاس [SmartPtr](../)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)