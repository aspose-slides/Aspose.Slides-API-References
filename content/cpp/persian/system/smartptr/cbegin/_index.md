---
title: cbegin()
second_title: راهنمای API Aspose.Slides برای C++
description: دسترس‌گیر برای متد cbegin() یک مجموعهٔ زیرین. فقط در صورتی کامپایل می‌شود که SmartPtr_ نوع تخصصی باشد با متد cbegin().
type: docs
weight: 404
url: /fa/system/smartptr/cbegin/
---
## SmartPtr::cbegin() const متد

دسترس‌گیر برای متد [cbegin()](./) یک مجموعهٔ زیرین. فقط در صورتی کامپایل می‌شود که SmartPtr_ نوع تخصصی باشد با متد [cbegin()](./).

```cpp
template<typename Q> auto System::SmartPtr<T>::cbegin() const -> decltype(std::declval<const Q>().cbegin())
```

### مقدار بازگشت

تکرارگر به ابتدای مجموعه

## موارد مرتبط

* کلاس [SmartPtr](../)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)