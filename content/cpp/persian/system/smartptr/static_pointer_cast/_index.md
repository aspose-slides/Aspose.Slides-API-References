---
title: static_pointer_cast()
second_title: مرجع API Aspose.Slides برای C++
description: اشاره‌گر را به نوع متفاوتی با استفاده از static_cast بر روی شیء اشاره‌شده تبدیل می‌کند.
type: docs
weight: 248
url: /fa/system/smartptr/static_pointer_cast/
---
## SmartPtr::static_pointer_cast() const متد

اشاره‌گر را به نوع متفاوتی با استفاده از static_cast بر روی شیء اشاره‌شده تبدیل می‌کند.

```cpp
template<class Y> SmartPtr<Y> System::SmartPtr<T>::static_pointer_cast() const
```

### پارامترهای قالب

| پارامتر | توضیح |
| --- | --- |
| Y | نوع هدف شیء اشاره‌شده. |

### مقدار بازگشت

اشاره‌گری از نوع تغییر یافته که همیشه در حالت مشترک است.

## موارد مرتبط

* کلاس [SmartPtr](../)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)