---
title: cend()
second_title: Aspose.Slides برای مرجع API C++
description: دسترس‌دهنده برای متد cend() یک مجموعهٔ زیرین. فقط زمانی کامپایل می‌شود که SmartPtr_ یک نوع تخصصی با متد cend() باشد.
type: docs
weight: 417
url: /fa/system/smartptr/cend/
---
## SmartPtr::cend() const متد

دسترس‌دهنده برای [cend()](./) متد یک مجموعهٔ زیرین. فقط زمانی کامپایل می‌شود که SmartPtr_ یک نوع تخصصی با [cend()](./) متد باشد.

```cpp
template<typename Q> auto System::SmartPtr<T>::cend() const -> decltype(std::declval<const Q>().cend())
```

### مقدار بازگشت

تکرارگر تا انتهای مجموعه

## موارد مرتبط

* کلاس [SmartPtr](../)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)