---
title: Parse()
second_title: مرجع API Aspose.Slides برای C++
description: کاراکتر اول و تنها رشتهٔ مشخص‌شده را به مقدار char_t تبدیل می‌کند.
type: docs
weight: 326
url: /fa/system/char/parse/
---
## Char::Parse(const String\&) متد

کاراکتر اول و تنها رشتهٔ مشخص‌شده را به مقدار char_t تبدیل می‌کند.

```cpp
static char_t System::Char::Parse(const String &value)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته‌ای که باید تبدیل شود. |

### مقدار بازگشت

کاراکتر اول و تنها رشتهٔ مشخص‌شده، اگر دقیقاً یک کاراکتر طول داشته باشد.

## موارد مرتبط

* کلاس [String](../../string/)
* کلاس [Char](../)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)