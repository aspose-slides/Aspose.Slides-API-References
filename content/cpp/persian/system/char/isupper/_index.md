---
title: IsUpper()
second_title: Aspose.Slides برای C++ مرجع API
description: مشخص می‌کند که آیا کاراکتر در ایندکس مشخص‌شده در رشتهٔ مشخص‌شده به عنوان حرف بزرگ طبقه‌بندی می‌شود.
type: docs
weight: 183
url: /fa/system/char/isupper/
---
## Char::IsUpper(const String\&, int) متد

مشخص می‌کند که آیا کاراکتر در ایندکس مشخص‌شده در رشتهٔ مشخص‌شده به عنوان حرف بزرگ طبقه‌بندی می‌شود یا خیر.

```cpp
static bool System::Char::IsUpper(const String &str, int idx)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| str | const [String](../../string/)\& | رشته‌ای که کاراکتری در آن وجود دارد. |
| idx | int | ایندکس صفرمبنایی در رشتهٔ مشخص‌شده برای آزمون |

### مقدار بازگشت

درست اگر کاراکتر در ایندکس مشخص‌شده یک حرف بزرگ باشد، در غیر این صورت - نادرست

## Char::IsUpper(const char_t *, int) متد

مشخص می‌کند که آیا کاراکتر در ایندکس مشخص‌شده در بافر کاراکتری مشخص‌شده به عنوان حرف بزرگ طبقه‌بندی می‌شود یا خیر.

```cpp
static bool System::Char::IsUpper(const char_t *str, int idx)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| str | const char_t * | اشاره‌گری به ابتدای بافر کاراکتری |
| idx | int | ایندکس صفرمبنایی در بافر مشخص‌شده برای آزمون کاراکتر |

### مقدار بازگشت

درست اگر کاراکتر در ایندکس مشخص‌شده یک حرف بزرگ باشد، در غیر این صورت - نادرست

## Char::IsUpper(char_t) متد

مشخص می‌کند که آیا کاراکتر مشخص‌شده به عنوان حرف بزرگ طبقه‌بندی می‌شود یا خیر.

```cpp
static bool System::Char::IsUpper(char_t c)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| c | char_t | کاراکتری که آزمون می‌شود |

### مقدار بازگشت

درست اگر کاراکتر مشخص‌شده یک حرف بزرگ باشد، در غیر این صورت - نادرست

## موارد مرتبط

* کلاس [String](../../string/)
* کلاس [Char](../)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)