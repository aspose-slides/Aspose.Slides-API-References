---
title: operator!=()
second_title: Aspose.Slides برای C++ مرجع API
description: مشخص می‌کند که مجموعهٔ delegate خالی نیست.
type: docs
weight: 131
url: /fa/system/multicastdelegate_tmpl_returntype_lbrace_argumenttypes_dots_rbrace__end_tmpl/operator_not_equal/
---
## MulticastDelegate< ReturnType(ArgumentTypes...)>::operator!=(const std::nullptr_t\&) const متد

مشخص می‌کند که مجموعهٔ delegate خالی نیست.

```cpp
bool System::MulticastDelegate<ReturnType(ArgumentTypes...)>::operator!=(const std::nullptr_t &) const
```

### مقدار بازگشت

True اگر مجموعهٔ delegate خالی نباشد، در غیر این صورت - false

## MulticastDelegate< ReturnType(ArgumentTypes...)>::operator!=(const MulticastDelegate\&) const متد

مشخص می‌کند آیا دو نمونه از MulticastDelegate - شیء فعلی و شیء مشخص‌شده - نامساوی هستند.

```cpp
bool System::MulticastDelegate<ReturnType(ArgumentTypes...)>::operator!=(const MulticastDelegate &other) const
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| other | const [MulticastDelegate](../multicastdelegate/)\& | شیء MulticastDelegate برای مقایسه با |

### مقدار بازگشت

True اگر هر دو شیء نمایانگر همان مجموعهٔ delegates باشند، در غیر این صورت - false

## همچنین ببینید

* متد [MulticastDelegate](../multicastdelegate/)
* کلاس [MulticastDelegate< ReturnType(ArgumentTypes...)>](../)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)