---
title: operator==()
second_title: مرجع API Aspose.Slides برای C++
description: تعیین می‌کند که آیا مجموعهٔ نماینده خالی است.
type: docs
weight: 118
url: /fa/system/multicastdelegate_tmpl_returntype_lbrace_argumenttypes_dots_rbrace__end_tmpl/operator_equal_equal/
---
## MulticastDelegate< ReturnType(ArgumentTypes...)>::operator==(const std::nullptr_t\&) const method

تعیین می‌کند که آیا مجموعهٔ نماینده خالی است.

```cpp
bool System::MulticastDelegate<ReturnType(ArgumentTypes...)>::operator==(const std::nullptr_t &) const
```

### مقدار بازگشت

درست اگر مجموعهٔ نماینده خالی باشد، در غیر این صورت - غلط

## MulticastDelegate< ReturnType(ArgumentTypes...)>::operator==(const MulticastDelegate\&) const method

تعیین می‌کند که آیا دو نمونهٔ MulticastDelegate - شیء فعلی و شیء مشخص‌شده - برابر هستند یا خیر.

```cpp
bool System::MulticastDelegate<ReturnType(ArgumentTypes...)>::operator==(const MulticastDelegate &other) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| other | const [MulticastDelegate](../multicastdelegate/)\& | شیء MulticastDelegate برای مقایسه |

### مقدار بازگشت

درست اگر هر دو شیء نمایندهٔ یک مجموعه باشند، در غیر این صورت - غلط

## موارد مرتبط

* متد [MulticastDelegate](../multicastdelegate/)
* کلاس [MulticastDelegate< ReturnType(ArgumentTypes...)>](../)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)