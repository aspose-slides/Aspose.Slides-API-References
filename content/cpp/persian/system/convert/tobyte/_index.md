---
title: ToByte()
second_title: مرجع API Aspose.Slides برای C++
description: مقدار بولی مشخص‌شده را به یک عدد صحیح بدون علامت ۸ بیتی معادل تبدیل می‌کند.
type: docs
weight: 92
url: /fa/system/convert/tobyte/
---
## Convert::ToByte(bool) متد


مقدار بولی مشخص‌شده را به یک عدد صحیح بدون علامت ۸ بیتی معادل تبدیل می‌کند.

```cpp
static constexpr uint8_t System::Convert::ToByte(bool value)
```

## Convert::ToByte(uint8_t) متد


عدد صحیح بدون علامت ۸ بیتی مشخص‌شده را باز می‌گرداند.

```cpp
static constexpr uint8_t System::Convert::ToByte(uint8_t value)
```

## Convert::ToByte(int8_t) متد


عدد صحیح با علامت ۸ بیتی مشخص‌شده را به عدد صحیح بدون علامت ۸ بیتی معادل تبدیل می‌کند.

```cpp
static uint8_t System::Convert::ToByte(int8_t value)
```

## Convert::ToByte(uint16_t) متد


عدد صحیح بدون علامت ۱۶ بیتی مشخص‌شده را به عدد صحیح بدون علامت ۸ بیتی معادل تبدیل می‌کند.

```cpp
static uint8_t System::Convert::ToByte(uint16_t value)
```

## Convert::ToByte(int16_t) متد


عدد صحیح با علامت ۱۶ بیتی مشخص‌شده را به عدد صحیح بدون علامت ۸ بیتی معادل تبدیل می‌کند.

```cpp
static uint8_t System::Convert::ToByte(int16_t value)
```

## Convert::ToByte(uint32_t) متد


عدد صحیح بدون علامت ۳۲ بیتی مشخص‌شده را به عدد صحیح بدون علامت ۸ بیتی معادل تبدیل می‌کند.

```cpp
static uint8_t System::Convert::ToByte(uint32_t value)
```

## Convert::ToByte(int32_t) متد


عدد صحیح با علامت ۳۲ بیتی مشخص‌شده را به عدد صحیح بدون علامت ۸ بیتی معادل تبدیل می‌کند.

```cpp
static uint8_t System::Convert::ToByte(int32_t value)
```

## Convert::ToByte(uint64_t) متد


عدد صحیح بدون علامت ۶۴ بیتی مشخص‌شده را به عدد صحیح بدون علامت ۸ بیتی معادل تبدیل می‌کند.

```cpp
static uint8_t System::Convert::ToByte(uint64_t value)
```

## Convert::ToByte(int64_t) متد


عدد صحیح با علامت ۶۴ بیتی مشخص‌شده را به عدد صحیح بدون علامت ۸ بیتی معادل تبدیل می‌کند.

```cpp
static uint8_t System::Convert::ToByte(int64_t value)
```

## Convert::ToByte(float) متد


عدد شناور مشخص‌شده را به عدد صحیح بدون علامت ۸ بیتی معادل تبدیل می‌کند.

```cpp
static uint8_t System::Convert::ToByte(float value)
```

## Convert::ToByte(double) متد


عدد دوگانه مشخص‌شده را به عدد صحیح بدون علامت ۸ بیتی معادل تبدیل می‌کند.

```cpp
static uint8_t System::Convert::ToByte(double value)
```

## Convert::ToByte(const Decimal\&) متد


عدد اعشاری مشخص‌شده را به عدد صحیح بدون علامت ۸ بیتی معادل تبدیل می‌کند.

```cpp
static uint8_t System::Convert::ToByte(const Decimal &value)
```

## Convert::ToByte(char_t) متد


کاراکتر یونیکد مشخص‌شده را به عدد صحیح بدون علامت ۸ بیتی معادل تبدیل می‌کند.

```cpp
static uint8_t System::Convert::ToByte(char_t value)
```

## Convert::ToByte(DateTime) متد


تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException پرتاب می‌شود.

```cpp
static uint8_t System::Convert::ToByte(DateTime value)
```

## Convert::ToByte(std::nullptr_t) متد


رشته تهی مشخص‌شده را به مقدار عدد صحیح بدون علامت ۸ بیتی معادل تبدیل می‌کند.

```cpp
static constexpr uint8_t System::Convert::ToByte(std::nullptr_t)
```


### مقدار بازگشت

صفر.

## Convert::ToByte(const char_t *) متد


رشته C حاوی نمایش عددی رشته‌ای را به مقدار عدد صحیح بدون علامت ۸ بیتی معادل تبدیل می‌کند.

```cpp
static uint8_t System::Convert::ToByte(const char_t *value)
```


### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| value | const char_t * | رشته C برای تبدیل |

### مقدار بازگشت

مقدار عدد صحیح بدون علامت ۸ بیتی برابر با عدد نمایانگر در رشته C مشخص‌شده

## Convert::ToByte(const String\&) متد


رشته حاوی نمایش عددی رشته‌ای را به مقدار عدد صحیح بدون علامت ۸ بیتی معادل تبدیل می‌کند.

```cpp
static uint8_t System::Convert::ToByte(const String &value)
```


### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |

### مقدار بازگشت

مقدار عدد صحیح بدون علامت ۸ بیتی برابر با عدد نمایانگر در رشته مشخص‌شده

## Convert::ToByte(const String\&, int) متد


رشته حاوی نمایش عددی رشته‌ای در پایهٔ مشخص‌شده را به مقدار عدد صحیح بدون علامت ۸ بیتی معادل تبدیل می‌کند.

```cpp
static uint8_t System::Convert::ToByte(const String &value, int from_base)
```


### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |
| from_base | int | پایهٔ عددی که رشته نمایانگر آن است |

### مقدار بازگشت

مقدار عدد صحیح بدون علامت ۸ بیتی برابر با عدد نمایانگر در رشته مشخص‌شده

## Convert::ToByte(const String\&, const SharedPtr\<IFormatProvider\>\&) متد


رشته حاوی نمایش عددی رشته‌ای را با استفاده از اطلاعات قالب‌بندی ارائه‌شده به مقدار عدد صحیح بدون علامت ۸ بیتی معادل تبدیل می‌کند.

```cpp
static uint8_t System::Convert::ToByte(const String &value, const SharedPtr<IFormatProvider> &provider)
```


### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | اشاره‌گر به شیئی که اطلاعات قالب رشته را دارد |

### مقدار بازگشت

مقدار عدد صحیح بدون علامت ۸ بیتی برابر با عدد نمایانگر در رشته مشخص‌شده

## Convert::ToByte(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) متد




```cpp
static uint8_t System::Convert::ToByte(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToByte(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) متد




```cpp
static uint8_t System::Convert::ToByte(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToByte(const String\&, std::nullptr_t) متد




```cpp
static uint8_t System::Convert::ToByte(const String &value, std::nullptr_t)
```

## Convert::ToByte(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) متد


رشته حاوی نمایش عددی رشته‌ای را با استفاده از اطلاعات قالب‌بندی و سبک عددی ارائه‌شده به مقدار عدد صحیح بدون علامت ۸ بیتی معادل تبدیل می‌کند.

```cpp
static uint8_t System::Convert::ToByte(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```


### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | ترکیب بیتی مقادیر شمارش NumberStyles که سبک مجاز نمایش عدد در رشته را مشخص می‌کند |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | اشاره‌گر به شیئی که اطلاعات قالب رشته را دارد |

### مقدار بازگشت

مقدار عدد صحیح بدون علامت ۸ بیتی برابر با عدد نمایانگر در رشته مشخص‌شده

## Convert::ToByte(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) متد




```cpp
static uint8_t System::Convert::ToByte(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToByte(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) متد




```cpp
static uint8_t System::Convert::ToByte(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToByte(const String\&, Globalization::NumberStyles, std::nullptr_t) متد




```cpp
static uint8_t System::Convert::ToByte(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToByte(Enum) متد




```cpp
template<typename Enum,typename> static uint8_t System::Convert::ToByte(Enum value)
```

## Convert::ToByte(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) متد


مقدار جعبه‌شدهٔ مشخص‌شده را به مقدار عدد صحیح بدون علامت ۸ بیتی معادل تبدیل می‌کند.

```cpp
static uint8_t System::Convert::ToByte(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```


### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | اشاره‌گر مشترک به شیئی که مقدار جعبه‌شده برای تبدیل را دارد |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | قالب رشته‌ای که در صورت نوع مقدار جعبه‌شده [String](../../string/) استفاده می‌شود |

### مقدار بازگشت

یک مقدار عدد صحیح بدون علامت ۸ بیتی معادل مقدار جعبه‌شدهٔ مشخص‌شده

## موارد مرتبط

* شمارش [NumberStyles](../../../system.globalization/numberstyles/)
* تعریف نوع [SharedPtr](../../sharedptr/)
* کلاس [Decimal](../../decimal/)
* کلاس [DateTime](../../datetime/)
* کلاس [String](../../string/)
* کلاس [IFormatProvider](../../iformatprovider/)
* کلاس [CultureInfo](../../../system.globalization/cultureinfo/)
* کلاس [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* کلاس [Object](../../object/)
* ساختار [Convert](../)
* ساختار [Enum](../../enum/)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)