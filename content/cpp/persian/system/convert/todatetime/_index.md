---
title: ToDateTime()
second_title: مرجع API Aspose.Slides برای C++
description: تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException پرتاب می‌شود.
type: docs
weight: 248
url: /fa/system/convert/todatetime/
---
## Convert::ToDateTime(bool) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException پرتاب می‌شود.

```cpp
static DateTime System::Convert::ToDateTime(bool value)
```

## Convert::ToDateTime(uint8_t) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException پرتاب می‌شود.

```cpp
static DateTime System::Convert::ToDateTime(uint8_t value)
```

## Convert::ToDateTime(int8_t) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException پرتاب می‌شود.

```cpp
static DateTime System::Convert::ToDateTime(int8_t value)
```

## Convert::ToDateTime(uint16_t) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException پرتاب می‌شود.

```cpp
static DateTime System::Convert::ToDateTime(uint16_t value)
```

## Convert::ToDateTime(int16_t) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException پرتاب می‌شود.

```cpp
static DateTime System::Convert::ToDateTime(int16_t value)
```

## Convert::ToDateTime(uint32_t) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException پرتاب می‌شود.

```cpp
static DateTime System::Convert::ToDateTime(uint32_t value)
```

## Convert::ToDateTime(int32_t) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException پرتاب می‌شود.

```cpp
static DateTime System::Convert::ToDateTime(int32_t value)
```

## Convert::ToDateTime(uint64_t) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException پرتاب می‌شود.

```cpp
static DateTime System::Convert::ToDateTime(uint64_t value)
```

## Convert::ToDateTime(int64_t) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException پرتاب می‌شود.

```cpp
static DateTime System::Convert::ToDateTime(int64_t value)
```

## Convert::ToDateTime(float) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException پرتاب می‌شود.

```cpp
static DateTime System::Convert::ToDateTime(float value)
```

## Convert::ToDateTime(double) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException پرتاب می‌شود.

```cpp
static DateTime System::Convert::ToDateTime(double value)
```

## Convert::ToDateTime(const Decimal\&) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException پرتاب می‌شود.

```cpp
static DateTime System::Convert::ToDateTime(const Decimal &value)
```

## Convert::ToDateTime(char_t) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException پرتاب می‌شود.

```cpp
static DateTime System::Convert::ToDateTime(char_t value)
```

## Convert::ToDateTime(DateTime) متد

تاریخ و زمان مشخص شده را برمی‌گرداند.

```cpp
static constexpr DateTime System::Convert::ToDateTime(DateTime value)
```

## Convert::ToDateTime(const String\&) متد

رشتهٔ مشخص‌شده را به یک نمونه از کلاس [DateTime](../../datetime/) تبدیل می‌کند.

```cpp
static DateTime System::Convert::ToDateTime(const String &value)
```

### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشتهٔ مورد تبدیل |

### مقدار بازگشت

نمونه‌ای از کلاس [DateTime](../../datetime/) که اطلاعات تاریخ و زمان را که توسط رشتهٔ مشخص‌شده نشان داده شده است، نشان می‌دهد

## Convert::ToDateTime(const String\&, const SharedPtr\<IFormatProvider\>\&) متد

رشتهٔ مشخص‌شده را با استفاده از اطلاعات قالب‌بندی فراهم‌شده به یک نمونه از کلاس [DateTime](../../datetime/) تبدیل می‌کند.

```cpp
static DateTime System::Convert::ToDateTime(const String &value, const SharedPtr<IFormatProvider> &fp)
```

### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشتهٔ مورد تبدیل |
| fp | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | اشاره‌گری به شیئی که اطلاعات قالب رشته را شامل می‌شود |

### مقدار بازگشت

نمونه‌ای از کلاس [DateTime](../../datetime/) که اطلاعات تاریخ و زمان را که توسط رشتهٔ مشخص‌شده نشان داده شده است، نشان می‌دهد

## Convert::ToDateTime(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) متد

```cpp
static DateTime System::Convert::ToDateTime(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToDateTime(const String\&, const SharedPtr\<Globalization::DateTimeFormatInfo\>\&) متد

```cpp
static DateTime System::Convert::ToDateTime(const String &value, const SharedPtr<Globalization::DateTimeFormatInfo> &dtfi)
```

## Convert::ToDateTime(const String\&, std::nullptr_t) متد

```cpp
static DateTime System::Convert::ToDateTime(const String &value, std::nullptr_t)
```

## Convert::ToDateTime(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) متد

مقدار جعبه‌شدهٔ مشخص‌شده را به مقدار معادل [DateTime](../../datetime/) تبدیل می‌کند.

```cpp
static DateTime System::Convert::ToDateTime(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```

### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | اشاره‌گر مشترکی به شیئی که مقدار جعبه‌شده را شامل می‌شود |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | قالب رشته‌ای که در صورتی که نوع مقدار جعبه‌شده [String](../../string/) باشد، استفاده می‌شود |

### مقدار بازگشت

مقداری از نوع [DateTime](../../datetime/) معادل مقدار جعبه‌شدهٔ مشخص‌شده

## موارد مرتبط

* تعریف‌نوع [SharedPtr](../../sharedptr/)
* کلاس [DateTime](../../datetime/)
* کلاس [Decimal](../../decimal/)
* کلاس [String](../../string/)
* کلاس [IFormatProvider](../../iformatprovider/)
* کلاس [CultureInfo](../../../system.globalization/cultureinfo/)
* کلاس [DateTimeFormatInfo](../../../system.globalization/datetimeformatinfo/)
* کلاس [Object](../../object/)
* ساختار [Convert](../)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)