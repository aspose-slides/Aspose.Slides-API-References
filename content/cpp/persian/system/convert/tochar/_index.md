---
title: ToChar()
second_title: Aspose.Slides برای C++ مرجع API
description: تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException را پرتاب می‌کند.
type: docs
weight: 118
url: /fa/system/convert/tochar/
---
## Convert::ToChar(bool) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException را پرتاب می‌کند.

```cpp
static char_t System::Convert::ToChar(bool value)
```

## Convert::ToChar(uint8_t) متد

عدد صحیح بدون علامت 8 بیتی مشخص شده را به یک کاراکتر یونیکد معادل تبدیل می‌کند.

```cpp
static constexpr char_t System::Convert::ToChar(uint8_t value)
```

## Convert::ToChar(int8_t) متد

عدد صحیح علامت‌دار 8 بیتی مشخص شده را به یک کاراکتر یونیکد معادل تبدیل می‌کند.

```cpp
static char_t System::Convert::ToChar(int8_t value)
```

## Convert::ToChar(uint16_t) متد

عدد صحیح بدون علامت 16 بیتی مشخص شده را به یک کاراکتر یونیکد معادل تبدیل می‌کند.

```cpp
static constexpr char_t System::Convert::ToChar(uint16_t value)
```

## Convert::ToChar(int16_t) متد

عدد صحیح علامت‌دار 16 بیتی مشخص شده را به یک کاراکتر یونیکد معادل تبدیل می‌کند.

```cpp
static char_t System::Convert::ToChar(int16_t value)
```

## Convert::ToChar(uint32_t) متد

عدد صحیح بدون علامت 32 بیتی مشخص شده را به یک کاراکتر یونیکد معادل تبدیل می‌کند.

```cpp
static char_t System::Convert::ToChar(uint32_t value)
```

## Convert::ToChar(int32_t) متد

عدد صحیح علامت‌دار 32 بیتی مشخص شده را به یک کاراکتر یونیکد معادل تبدیل می‌کند.

```cpp
static char_t System::Convert::ToChar(int32_t value)
```

## Convert::ToChar(uint64_t) متد

عدد صحیح بدون علامت 64 بیتی مشخص شده را به یک کاراکتر یونیکد معادل تبدیل می‌کند.

```cpp
static char_t System::Convert::ToChar(uint64_t value)
```

## Convert::ToChar(int64_t) متد

عدد صحیح علامت‌دار 64 بیتی مشخص شده را به یک کاراکتر یونیکد معادل تبدیل می‌کند.

```cpp
static char_t System::Convert::ToChar(int64_t value)
```

## Convert::ToChar(float) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException را پرتاب می‌کند.

```cpp
static char_t System::Convert::ToChar(float value)
```

## Convert::ToChar(double) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException را پرتاب می‌کند.

```cpp
static char_t System::Convert::ToChar(double value)
```

## Convert::ToChar(const Decimal\&) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException را پرتاب می‌کند.

```cpp
static char_t System::Convert::ToChar(const Decimal &value)
```

## Convert::ToChar(char_t) متد

کاراکتر یونیکد مشخص شده را برمی‌گرداند.

```cpp
static constexpr char_t System::Convert::ToChar(char_t value)
```

## Convert::ToChar(DateTime) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException را پرتاب می‌کند.

```cpp
static char_t System::Convert::ToChar(DateTime value)
```

## Convert::ToChar(const char_t *) متد

نخستین و تنها کاراکتر رشته C مشخص شده را به مقدار char_t تبدیل می‌کند.

```cpp
static char_t System::Convert::ToChar(const char_t *value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const char_t * | رشته C برای تبدیل؛ انتظار می‌رود طول رشته C دقیقاً 1 کاراکتر باشد. |

### مقدار بازگشتی

اولین و تنها کاراکتر رشته C مشخص شده اگر دقیقاً 1 کاراکتر طول داشته باشد، در غیر این صورت - 0

## Convert::ToChar(const String\&) متد

نخستین و تنها کاراکتر رشته مشخص شده را به مقدار char_t تبدیل می‌کند.

```cpp
static char_t System::Convert::ToChar(const String &value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل؛ انتظار می‌رود طول رشته دقیقاً 1 کاراکتر باشد. |

### مقدار بازگشتی

اولین و تنها کاراکتر رشته مشخص شده اگر دقیقاً 1 کاراکتر طول داشته باشد، در غیر این صورت - 0

## Convert::ToChar(const String\&, const SharedPtr\<IFormatProvider\>\&) متد

نخستین و تنها کاراکتر رشته مشخص شده را به مقدار char_t تبدیل می‌کند.

```cpp
static char_t System::Convert::ToChar(const String &value, const SharedPtr<IFormatProvider> &)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل؛ انتظار می‌رود طول رشته دقیقاً 1 کاراکتر باشد. |

### مقدار بازگشتی

اولین و تنها کاراکتر رشته مشخص شده اگر دقیقاً 1 کاراکتر طول داشته باشد، در غیر این صورت - 0

## Convert::ToChar(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) متد

مقدار بسته‌بندی‌شده مشخص شده را به کاراکتر یونیکد معادل تبدیل می‌کند.

```cpp
static char_t System::Convert::ToChar(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | اشاره‌گر مشترک به شیئی که مقدار را بسته‌بندی می‌کند تا تبدیل شود. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | قالب رشته‌ای که در صورتی که نوع مقدار بسته‌بندی‌شده [String](../../string/) باشد، استفاده می‌شود. |

### مقدار بازگشتی

یک کاراکتر یونیکد معادل مقدار بسته‌بندی‌شده مشخص شده.

## موارد مرتبط

* Typedef [SharedPtr](../../sharedptr/)
* کلاس [Decimal](../../decimal/)
* کلاس [DateTime](../../datetime/)
* کلاس [String](../../string/)
* کلاس [IFormatProvider](../../iformatprovider/)
* کلاس [Object](../../object/)
* ساختار [Convert](../)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)