---
title: ToInt64()
second_title: مرجع API Aspose.Slides برای C++
description: مقدار بولی مشخص‌شده را به یک عدد صحیح ۶۴ بیتی معادل تبدیل می‌کند.
type: docs
weight: 183
url: /fa/system/convert/toint64/
---
## Convert::ToInt64(bool) متد

مقدار بولی مشخص‌شده را به یک عدد صحیح ۶۴ بیتی معادل تبدیل می‌کند.

```cpp
static constexpr int64_t System::Convert::ToInt64(bool value)
```

## Convert::ToInt64(uint8_t) متد

عدد صحیح ۸ بیتی بدون علامت مشخص‌شده را به یک عدد صحیح ۶۴ بیتی معادل تبدیل می‌کند.

```cpp
static constexpr int64_t System::Convert::ToInt64(uint8_t value)
```

## Convert::ToInt64(int8_t) متد

عدد صحیح ۸ بیتی با علامت مشخص‌شده را به یک عدد صحیح ۶۴ بیتی معادل تبدیل می‌کند.

```cpp
static constexpr int64_t System::Convert::ToInt64(int8_t value)
```

## Convert::ToInt64(uint16_t) متد

عدد صحیح ۱۶ بیتی بدون علامت مشخص‌شده را به یک عدد صحیح ۶۴ بیتی معادل تبدیل می‌کند.

```cpp
static constexpr int64_t System::Convert::ToInt64(uint16_t value)
```

## Convert::ToInt64(int16_t) متد

عدد صحیح ۱۶ بیتی با علامت مشخص‌شده را به یک عدد صحیح ۶۴ بیتی معادل تبدیل می‌کند.

```cpp
static constexpr int64_t System::Convert::ToInt64(int16_t value)
```

## Convert::ToInt64(uint32_t) متد

عدد صحیح ۳۲ بیتی بدون علامت مشخص‌شده را به یک عدد صحیح ۶۴ بیتی معادل تبدیل می‌کند.

```cpp
static constexpr int64_t System::Convert::ToInt64(uint32_t value)
```

## Convert::ToInt64(int32_t) متد

عدد صحیح ۳۲ بیتی با علامت مشخص‌شده را به یک عدد صحیح ۶۴ بیتی معادل تبدیل می‌کند.

```cpp
static constexpr int64_t System::Convert::ToInt64(int32_t value)
```

## Convert::ToInt64(uint64_t) متد

عدد صحیح ۶۴ بیتی بدون علامت مشخص‌شده را به یک عدد صحیح ۶۴ بیتی معادل تبدیل می‌کند.

```cpp
static int64_t System::Convert::ToInt64(uint64_t value)
```

## Convert::ToInt64(int64_t) متد

عدد صحیح ۶۴ بیتی مشخص‌شده را برمی‌گرداند.

```cpp
static constexpr int64_t System::Convert::ToInt64(int64_t value)
```

## Convert::ToInt64(float) متد

عدد شناور (float) مشخص‌شده را به یک عدد صحیح ۶۴ بیتی معادل تبدیل می‌کند.

```cpp
static int64_t System::Convert::ToInt64(float value)
```

## Convert::ToInt64(double) متد

عدد double مشخص‌شده را به یک عدد صحیح ۶۴ بیتی معادل تبدیل می‌کند.

```cpp
static int64_t System::Convert::ToInt64(double value)
```

## Convert::ToInt64(const Decimal\&) متد

عدد Decimal مشخص‌شده را به یک عدد صحیح ۶۴ بیتی معادل تبدیل می‌کند.

```cpp
static int64_t System::Convert::ToInt64(const Decimal &value)
```

## Convert::ToInt64(char_t) متد

کاراکتر یونیکد مشخص‌شده را به یک عدد صحیح ۶۴ بیتی معادل تبدیل می‌کند.

```cpp
static constexpr int64_t System::Convert::ToInt64(char_t value)
```

## Convert::ToInt64(DateTime) متد

تبدیل پشتیبانی نمی‌شود. همیشه یک InvalidCastException پرتاب می‌کند.

```cpp
static int64_t System::Convert::ToInt64(DateTime value)
```

## Convert::ToInt64(std::nullptr_t) متد

رشتهٔ null-string مشخص‌شده را به مقدار عدد صحیح ۶۴ بیتی معادل تبدیل می‌کند.

```cpp
static constexpr int64_t System::Convert::ToInt64(std::nullptr_t)
```

### مقدار بازگشتی

صفر.

## Convert::ToInt64(const char_t *) متد

رشتهٔ C-String حاوی نمای عددی را به مقدار عدد صحیح ۶۴ بیتی معادل تبدیل می‌کند.

```cpp
static int64_t System::Convert::ToInt64(const char_t *value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const char_t * | رشتهٔ C-String برای تبدیل |

### مقدار بازگشتی

مقدار عدد صحیح ۶۴ بیتی برابر با عددی که در رشتهٔ C-String مشخص‌شده نمایان شده است

## Convert::ToInt64(const String\&) متد

رشتهٔ حاوی نمای عددی را به مقدار عدد صحیح ۶۴ بیتی معادل تبدیل می‌کند.

```cpp
static int64_t System::Convert::ToInt64(const String &value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشتهٔ برای تبدیل |

### مقدار بازگشتی

مقدار عدد صحیح ۶۴ بیتی برابر با عددی که در رشتهٔ مشخص‌شده نمایان شده است

## Convert::ToInt64(const String\&, int) متد

رشتهٔ حاوی نمای عددی در پایهٔ مشخص‌شده را به مقدار عدد صحیح ۶۴ بیتی معادل تبدیل می‌کند.

```cpp
static int64_t System::Convert::ToInt64(const String &value, int from_base)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشتهٔ برای تبدیل |
| from_base | int | پایهٔ عدد نمایان‌شده در رشته |

### مقدار بازگشتی

مقدار عدد صحیح ۶۴ بیتی برابر با عددی که در رشتهٔ مشخص‌شده نمایان شده است

## Convert::ToInt64(const String\&, const SharedPtr\<IFormatProvider\>\&) متد

رشتهٔ حاوی نمای عددی را به مقدار عدد صحیح ۶۴ بیتی معادل تبدیل می‌کند با استفاده از اطلاعات قالب‌بندی فراهم‌شده.

```cpp
static int64_t System::Convert::ToInt64(const String &value, const SharedPtr<IFormatProvider> &provider)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشتهٔ برای تبدیل |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | اشاره‌گری به شیئی که شامل اطلاعات قالب‌بندی رشته است |

### مقدار بازگشتی

مقدار عدد صحیح ۶۴ بیتی برابر با عددی که در رشتهٔ مشخص‌شده نمایان شده است

## Convert::ToInt64(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) متد

```cpp
static int64_t System::Convert::ToInt64(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToInt64(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) متد

```cpp
static int64_t System::Convert::ToInt64(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToInt64(const String\&, std::nullptr_t) متد

```cpp
static int64_t System::Convert::ToInt64(const String &value, std::nullptr_t)
```

## Convert::ToInt64(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) متد

رشتهٔ حاوی نمای عددی را به مقدار عدد صحیح ۶۴ بیتی معادل تبدیل می‌کند با استفاده از اطلاعات قالب‌بندی و سبک عددی فراهم‌شده.

```cpp
static int64_t System::Convert::ToInt64(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشتهٔ برای تبدیل |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | ترکیبی بیتی از مقادیر enum NumberStyles که سبک مجاز نمایش عدد در رشته را مشخص می‌کند |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | اشاره‌گری به شیئی که شامل اطلاعات قالب‌بندی رشته است |

### مقدار بازگشتی

مقدار عدد صحیح ۶۴ بیتی برابر با عددی که در رشتهٔ مشخص‌شده نمایان شده است

## Convert::ToInt64(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) متد

```cpp
static int64_t System::Convert::ToInt64(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToInt64(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) متد

```cpp
static int64_t System::Convert::ToInt64(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToInt64(const String\&, Globalization::NumberStyles, std::nullptr_t) متد

```cpp
static int64_t System::Convert::ToInt64(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToInt64(Enum) متد

```cpp
template<typename Enum,typename> static int64_t System::Convert::ToInt64(Enum value)
```

## Convert::ToInt64(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) متد

مقدار بسته‌بندی‌شدهٔ مشخص‌شده را به مقدار عدد صحیح ۶۴ بیتی معادل تبدیل می‌کند.

```cpp
static int64_t System::Convert::ToInt64(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | اشاره‌گر مشترک به شیئی که مقدار بسته‌بندی‌شده برای تبدیل را نگه می‌دارد |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | قالب‌بندی رشته‌ای که در صورت برابر بودن نوع مقدار بسته‌بندی‌شده با [String](../../string/) استفاده می‌شود |

### مقدار بازگشتی

یک مقدار عدد صحیح ۶۴ بیتی معادل مقدار بسته‌بندی‌شدهٔ مشخص‌شده

## مراجع

* enum [NumberStyles](../../../system.globalization/numberstyles/)
* typedef [SharedPtr](../../sharedptr/)
* class [Decimal](../../decimal/)
* class [DateTime](../../datetime/)
* class [String](../../string/)
* class [IFormatProvider](../../iformatprovider/)
* class [CultureInfo](../../../system.globalization/cultureinfo/)
* class [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* class [Object](../../object/)
* struct [Convert](../)
* struct [Enum](../../enum/)
* namespace [System](../../)
* library [Aspose.Slides](../../../)