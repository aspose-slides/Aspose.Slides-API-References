---
title: ToSByte()
second_title: مرجع API Aspose.Slides برای C++
description: مقدار بولی مشخص‌شده را به یک عدد صحیح 8 بیتی علامت‌دار معادل تبدیل می‌کند.
type: docs
weight: 105
url: /fa/system/convert/tosbyte/
---
## Convert::ToSByte(bool) متد

مقدار بولی مشخص‌شده را به یک عدد صحیح 8 بیتی علامت‌دار معادل تبدیل می‌کند.

```cpp
static constexpr int8_t System::Convert::ToSByte(bool value)
```

## Convert::ToSByte(uint8_t) متد

عدد صحیح 8 بیتی بدون علامت مشخص‌شده را به یک عدد صحیح 8 بیتی علامت‌دار معادل تبدیل می‌کند.

```cpp
static int8_t System::Convert::ToSByte(uint8_t value)
```

## Convert::ToSByte(int8_t) متد

عدد صحیح 8 بیتی علامت‌دار مشخص‌شده را برمی‌گرداند.

```cpp
static constexpr int8_t System::Convert::ToSByte(int8_t value)
```

## Convert::ToSByte(uint16_t) متد

عدد صحیح 16 بیتی بدون علامت مشخص‌شده را به یک عدد صحیح 8 بیتی علامت‌دار معادل تبدیل می‌کند.

```cpp
static int8_t System::Convert::ToSByte(uint16_t value)
```

## Convert::ToSByte(int16_t) متد

عدد صحیح 16 بیتی علامت‌دار مشخص‌شده را به یک عدد صحیح 8 بیتی علامت‌دار معادل تبدیل می‌کند.

```cpp
static int8_t System::Convert::ToSByte(int16_t value)
```

## Convert::ToSByte(uint32_t) متد

عدد صحیح 32 بیتی بدون علامت مشخص‌شده را به یک عدد صحیح 8 بیتی علامت‌دار معادل تبدیل می‌کند.

```cpp
static int8_t System::Convert::ToSByte(uint32_t value)
```

## Convert::ToSByte(int32_t) متد

عدد صحیح 32 بیتی علامت‌دار مشخص‌شده را به یک عدد صحیح 8 بیتی علامت‌دار معادل تبدیل می‌کند.

```cpp
static int8_t System::Convert::ToSByte(int32_t value)
```

## Convert::ToSByte(uint64_t) متد

عدد صحیح 64 بیتی بدون علامت مشخص‌شده را به یک عدد صحیح 8 بیتی علامت‌دار معادل تبدیل می‌کند.

```cpp
static int8_t System::Convert::ToSByte(uint64_t value)
```

## Convert::ToSByte(int64_t) متد

عدد صحیح 64 بیتی علامت‌دار مشخص‌شده را به یک عدد صحیح 8 بیتی علامت‌دار معادل تبدیل می‌کند.

```cpp
static int8_t System::Convert::ToSByte(int64_t value)
```

## Convert::ToSByte(float) متد

عدد شناور (float) مشخص‌شده را به یک عدد صحیح 8 بیتی علامت‌دار معادل تبدیل می‌کند.

```cpp
static int8_t System::Convert::ToSByte(float value)
```

## Convert::ToSByte(double) متد

عدد ممیز شناور (double) مشخص‌شده را به یک عدد صحیح 8 بیتی علامت‌دار معادل تبدیل می‌کند.

```cpp
static int8_t System::Convert::ToSByte(double value)
```

## Convert::ToSByte(const Decimal\&) متد

عدد اعشاری (Decimal) مشخص‌شده را به یک عدد صحیح 8 بیتی علامت‌دار معادل تبدیل می‌کند.

```cpp
static int8_t System::Convert::ToSByte(const Decimal &value)
```

## Convert::ToSByte(char_t) متد

کاراکتر یونیکد مشخص‌شده را به یک عدد صحیح 8 بیتی علامت‌دار معادل تبدیل می‌کند.

```cpp
static int8_t System::Convert::ToSByte(char_t value)
```

## Convert::ToSByte(DateTime) متد

تبدیل پشتیبانی نمی‌شود. همیشه استثنای InvalidCastException پرتاب می‌شود.

```cpp
static int8_t System::Convert::ToSByte(DateTime value)
```

## Convert::ToSByte(std::nullptr_t) متد

رشته‌ی تهی (null-string) مشخص‌شده را به مقدار عدد صحیح 8 بیتی معادل تبدیل می‌کند.

```cpp
static constexpr int8_t System::Convert::ToSByte(std::nullptr_t)
```

### مقدار بازگشت

صفر.

## Convert::ToSByte(const char_t *) متد

رشته C-ی حاوی نمایش عددی موردنظر را به مقدار عدد صحیح 8 بیتی معادل تبدیل می‌کند.

```cpp
static int8_t System::Convert::ToSByte(const char_t *value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const char_t * | رشته C-ی‌ای که باید تبدیل شود |

### مقدار بازگشت

مقدار عدد صحیح 8 بیتی برابر با عددی که توسط رشته C-ی مشخص‌شده نمایش داده می‌شود

## Convert::ToSByte(const String\&) متد

رشته‌ای که حاوی نمایش عددی موردنظر است را به مقدار عدد صحیح 8 بیتی معادل تبدیل می‌کند.

```cpp
static int8_t System::Convert::ToSByte(const String &value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته‌ای که باید تبدیل شود |

### مقدار بازگشت

مقدار عدد صحیح 8 بیتی برابر با عددی که توسط رشته مشخص‌شده نمایش داده می‌شود

## Convert::ToSByte(const String\&, int) متد

رشته‌ای که حاوی نمایش عددی در پایهٔ مشخص‌شده است را به مقدار عدد صحیح 8 بیتی معادل تبدیل می‌کند.

```cpp
static int8_t System::Convert::ToSByte(const String &value, int from_base)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته‌ای که باید تبدیل شود |
| from_base | int | پایهٔ عددی که توسط رشته نمایش داده می‌شود |

### مقدار بازگشت

مقدار عدد صحیح 8 بیتی برابر با عددی که توسط رشته مشخص‌شده نمایش داده می‌شود

## Convert::ToSByte(const String\&, const SharedPtr\<IFormatProvider\>\&) متد

رشته‌ای که حاوی نمایش عددی است را با استفاده از اطلاعات قالب‌بندی ارائه‌شده، به مقدار عدد صحیح بدون علامت 8 بیتی معادل تبدیل می‌کند.

```cpp
static int8_t System::Convert::ToSByte(const String &value, const SharedPtr<IFormatProvider> &provider)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته‌ای که باید تبدیل شود |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | اشاره‌گری به شیئی که اطلاعات قالب‌بندی رشته را شامل می‌شود |

### مقدار بازگشت

مقدار عدد صحیح 8 بیتی برابر با عددی که توسط رشته مشخص‌شده نمایش داده می‌شود

## Convert::ToSByte(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) متد

```cpp
static int8_t System::Convert::ToSByte(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToSByte(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) متد

```cpp
static int8_t System::Convert::ToSByte(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToSByte(const String\&, std::nullptr_t) متد

```cpp
static int8_t System::Convert::ToSByte(const String &value, std::nullptr_t)
```

## Convert::ToSByte(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) متد

رشته‌ای که حاوی نمایش عددی است را با استفاده از اطلاعات قالب‌بندی و سبک عددی ارائه‌شده، به مقدار عدد صحیح 8 بیتی معادل تبدیل می‌کند.

```cpp
static int8_t System::Convert::ToSByte(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته‌ای که باید تبدیل شود |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | ترکیبی بیتی از مقادیر enum NumberStyles که سبک مجاز نمایش عدد را تعیین می‌کند |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | اشاره‌گری به شیئی که اطلاعات قالب‌بندی رشته را شامل می‌شود |

### مقدار بازگشت

مقدار عدد صحیح بدون علامت 8 بیتی برابر با عددی که توسط رشته مشخص‌شده نمایش داده می‌شود

## Convert::ToSByte(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) متد

```cpp
static int8_t System::Convert::ToSByte(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToSByte(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) متد

```cpp
static int8_t System::Convert::ToSByte(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToSByte(const String\&, Globalization::NumberStyles, std::nullptr_t) متد

```cpp
static int8_t System::Convert::ToSByte(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToSByte(Enum) متد

```cpp
template<typename Enum,typename> static int8_t System::Convert::ToSByte(Enum value)
```

## Convert::ToSByte(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) متد

مقدار جعبه‌بسته‌شدهٔ مشخص‌شده را به مقدار عدد صحیح 8 بیتی معادل تبدیل می‌کند.

```cpp
static int8_t System::Convert::ToSByte(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | اشاره‌گر اشتراکی به شیء‌ای که مقدار را بسته‌بندی کرده است |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | قالب‌بندی رشته‌ای که در صورت همخوانی نوع مقدار جعبه‌شده با [String](../../string/) استفاده می‌شود |

### مقدار بازگشت

یک مقدار عدد صحیح 8 بیتی معادل مقدار جعبه‌شدهٔ مشخص‌شده

## See Also

* Enum [NumberStyles](../../../system.globalization/numberstyles/)
* Typedef [SharedPtr](../../sharedptr/)
* Class [Decimal](../../decimal/)
* Class [DateTime](../../datetime/)
* Class [String](../../string/)
* Class [IFormatProvider](../../iformatprovider/)
* Class [CultureInfo](../../../system.globalization/cultureinfo/)
* Class [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* Class [Object](../../object/)
* Struct [Convert](../)
* Struct [Enum](../../enum/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)