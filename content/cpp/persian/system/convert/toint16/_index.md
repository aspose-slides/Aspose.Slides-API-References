---
title: ToInt16()
second_title: مرجع API Aspose.Slides برای C++
description: مقدار بولی مشخص شده را به یک عدد صحیح 16 بیتی معادل تبدیل می‌کند.
type: docs
weight: 131
url: /fa/system/convert/toint16/
---
## Convert::ToInt16(bool) متد

مقدار بولی مشخص شده را به یک عدد صحیح 16 بیتی معادل تبدیل می‌کند.

```cpp
static constexpr int16_t System::Convert::ToInt16(bool value)
```

## Convert::ToInt16(uint8_t) متد

عدد صحیح بدون علامت 8 بیتی مشخص شده را به یک عدد صحیح 16 بیتی معادل تبدیل می‌کند.

```cpp
static constexpr int16_t System::Convert::ToInt16(uint8_t value)
```

## Convert::ToInt16(int8_t) متد

عدد صحیح 8 بیتی دارای علامت مشخص شده را به یک عدد صحیح 16 بیتی معادل تبدیل می‌کند.

```cpp
static constexpr int16_t System::Convert::ToInt16(int8_t value)
```

## Convert::ToInt16(uint16_t) متد

عدد صحیح بدون علامت 16 بیتی مشخص شده را به عدد صحیح 16 بیتی معادل تبدیل می‌کند.

```cpp
static int16_t System::Convert::ToInt16(uint16_t value)
```

## Convert::ToInt16(int16_t) متد

عدد صحیح 16 بیتی مشخص شده را برمی‌گرداند.

```cpp
static constexpr int16_t System::Convert::ToInt16(int16_t value)
```

## Convert::ToInt16(uint32_t) متد

عدد صحیح بدون علامت 32 بیتی مشخص شده را به عدد صحیح 16 بیتی معادل تبدیل می‌کند.

```cpp
static int16_t System::Convert::ToInt16(uint32_t value)
```

## Convert::ToInt16(int32_t) متد

عدد صحیح 32 بیتی دارای علامت مشخص شده را به عدد صحیح 16 بیتی معادل تبدیل می‌کند.

```cpp
static int16_t System::Convert::ToInt16(int32_t value)
```

## Convert::ToInt16(uint64_t) متد

عدد صحیح بدون علامت 64 بیتی مشخص شده را به عدد صحیح 16 بیتی معادل تبدیل می‌کند.

```cpp
static int16_t System::Convert::ToInt16(uint64_t value)
```

## Convert::ToInt16(int64_t) متد

عدد صحیح 64 بیتی دارای علامت مشخص شده را به عدد صحیح 16 بیتی معادل تبدیل می‌کند.

```cpp
static int16_t System::Convert::ToInt16(int64_t value)
```

## Convert::ToInt16(float) متد

عدد شناور (float) مشخص شده را به عدد صحیح 16 بیتی معادل تبدیل می‌کند.

```cpp
static int16_t System::Convert::ToInt16(float value)
```

## Convert::ToInt16(double) متد

عدد دوچونگی (double) مشخص شده را به عدد صحیح 16 بیتی معادل تبدیل می‌کند.

```cpp
static int16_t System::Convert::ToInt16(double value)
```

## Convert::ToInt16(const Decimal\&) متد

عدد دسیمال (Decimal) مشخص شده را به عدد صحیح 16 بیتی معادل تبدیل می‌کند.

```cpp
static int16_t System::Convert::ToInt16(const Decimal &value)
```

## Convert::ToInt16(char_t) متد

کاراکتر یونیکد مشخص شده را به عدد صحیح 16 بیتی معادل تبدیل می‌کند.

```cpp
static int16_t System::Convert::ToInt16(char_t value)
```

## Convert::ToInt16(DateTime) متد

تبدیل پشتیبانی نمی‌شود. همیشه استثنای InvalidCastException را پرتاب می‌کند.

```cpp
static int16_t System::Convert::ToInt16(DateTime value)
```

## Convert::ToInt16(std::nullptr_t) متد

رشته null را به مقدار عدد صحیح 16 بیتی معادل تبدیل می‌کند.

```cpp
static constexpr int16_t System::Convert::ToInt16(std::nullptr_t)
```

### مقدار بازگشت

صفر.

## Convert::ToInt16(const char_t *) متد

c-string حاوی نمایش متنی عدد مشخص شده را به مقدار عدد صحیح 16 بیتی معادل تبدیل می‌کند.

```cpp
static int16_t System::Convert::ToInt16(const char_t *value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const char_t * | c-string برای تبدیل |

### مقدار بازگشت

مقدار عدد صحیح 16 بیتی برابر با عددی که توسط c-string مشخص شده نمایانده می‌شود

## Convert::ToInt16(const String\&) متد

رشته حاوی نمایش متنی عدد مشخص شده را به مقدار عدد صحیح 16 بیتی معادل تبدیل می‌کند.

```cpp
static int16_t System::Convert::ToInt16(const String &value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |

### مقدار بازگشت

مقدار عدد صحیح 16 بیتی برابر با عددی که توسط رشتهٔ مشخص شده نمایانده می‌شود

## Convert::ToInt16(const String\&, int) متد

رشته حاوی نمایش متنی عدد مشخص شده در پایهٔ داده‌شده را به مقدار عدد صحیح 16 بیتی معادل تبدیل می‌کند.

```cpp
static int16_t System::Convert::ToInt16(const String &value, int from_base)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |
| from_base | int | پایه عددی که توسط رشته نمایانده می‌شود |

### مقدار بازگشت

مقدار عدد صحیح 16 بیتی برابر با عددی که توسط رشتهٔ مشخص شده نمایانده می‌شود

## Convert::ToInt16(const String\&, const SharedPtr\<IFormatProvider\>\&) متد

رشته حاوی نمایش متنی عدد مشخص شده را به مقدار عدد صحیح 16 بیتی معادل تبدیل می‌کند با استفاده از اطلاعات قالب‌بندی ارائه‌شده.

```cpp
static int16_t System::Convert::ToInt16(const String &value, const SharedPtr<IFormatProvider> &provider)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | اشاره‌گری به شیئی که اطلاعات قالب‌بندی رشته را شامل می‌شود |

### مقدار بازگشت

مقدار عدد صحیح 16 بیتی برابر با عددی که توسط رشتهٔ مشخص شده نمایانده می‌شود

## Convert::ToInt16(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) متد




```cpp
static int16_t System::Convert::ToInt16(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToInt16(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) متد




```cpp
static int16_t System::Convert::ToInt16(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToInt16(const String\&, std::nullptr_t) متد




```cpp
static int16_t System::Convert::ToInt16(const String &value, std::nullptr_t)
```

## Convert::ToInt16(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) متد

رشته حاوی نمایش متنی عدد مشخص شده را به مقدار عدد صحیح 16 بیتی معادل تبدیل می‌کند با استفاده از اطلاعات قالب‌بندی و سبک عدد ارائه‌شده.

```cpp
static int16_t System::Convert::ToInt16(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | یک ترکیب بیتی از مقادیر enum NumberStyles که سبک مجاز نمایش عدد را مشخص می‌کند |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | اشاره‌گری به شیئی که اطلاعات قالب‌بندی رشته را شامل می‌شود |

### مقدار بازگشت

مقدار عدد صحیح 16 بیتی برابر با عددی که توسط رشتهٔ مشخص شده نمایانده می‌شود

## Convert::ToInt16(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) متد




```cpp
static int16_t System::Convert::ToInt16(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToInt16(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) متد




```cpp
static int16_t System::Convert::ToInt16(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToInt16(const String\&, Globalization::NumberStyles, std::nullptr_t) متد 




```cpp
static int16_t System::Convert::ToInt16(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToInt16(Enum) متد 




```cpp
template<typename Enum,typename> static int16_t System::Convert::ToInt16(Enum value)
```

## Convert::ToInt16(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) متد

مقدار بسته‌بندی‌شدهٔ مشخص‌شده را به مقدار عدد صحیح 16 بیتی معادل تبدیل می‌کند.

```cpp
static int16_t System::Convert::ToInt16(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | اشاره‌گر اشتراکی به شیئی که مقدار را بسته‌بندی می‌کند برای تبدیل |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | قالب رشته‌ای که در صورت نوع مقدار بسته‌بندی‌شده [String](../../string/) استفاده می‌شود |

### مقدار بازگشت

یک مقدار عدد صحیح 16 بیتی معادل مقدار بسته‌بندی‌شدهٔ مشخص شده

## موارد مرتبط

* Enum [NumberStyles](../../../system.globalization/numberstyles/)
* Typedef [SharedPtr](../../sharedptr/)
* Class [Decimal](../../decimal/)
* Class [DateTime](../../datetime/)
* Class [String](../../string/)
* Class [IFormatProvider](../../iformatprovider/)
* Class [CultureInfo](../../../system.globalization/cultureinfo/)
* Class [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* Class [Object](../../object/)
* Struct [Convert](../)
* Struct [Enum](../../enum/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)