---
title: ToInt32()
second_title: مرجع API Aspose.Slides برای C++
description: مقدار بولی مشخص‌شده را به عدد صحیح 32 بیتی دارای علامت معادل تبدیل می‌کند.
type: docs
weight: 157
url: /fa/system/convert/toint32/
---
## Convert::ToInt32(bool) متد

مقدار بولی مشخص‌شده را به عدد صحیح 32 بیتی دارای علامت معادل تبدیل می‌کند.

```cpp
static constexpr int System::Convert::ToInt32(bool value)
```

## Convert::ToInt32(uint8_t) متد

عدد صحیح 8 بیتی بدون علامت مشخص‌شده را به عدد صحیح 32 بیتی دارای علامت معادل تبدیل می‌کند.

```cpp
static constexpr int System::Convert::ToInt32(uint8_t value)
```

## Convert::ToInt32(int8_t) متد

عدد صحیح 8 بیتی دارای علامت مشخص‌شده را به عدد صحیح 32 بیتی دارای علامت معادل تبدیل می‌کند.

```cpp
static constexpr int System::Convert::ToInt32(int8_t value)
```

## Convert::ToInt32(uint16_t) متد

عدد صحیح 16 بیتی بدون علامت مشخص‌شده را به عدد صحیح 32 بیتی دارای علامت معادل تبدیل می‌کند.

```cpp
static constexpr int System::Convert::ToInt32(uint16_t value)
```

## Convert::ToInt32(int16_t) متد

عدد صحیح 16 بیتی دارای علامت مشخص‌شده را به عدد صحیح 32 بیتی دارای علامت معادل تبدیل می‌کند.

```cpp
static constexpr int System::Convert::ToInt32(int16_t value)
```

## Convert::ToInt32(uint32_t) متد

عدد صحیح 32 بیتی بدون علامت مشخص‌شده را به عدد صحیح 32 بیتی دارای علامت معادل تبدیل می‌کند.

```cpp
static int System::Convert::ToInt32(uint32_t value)
```

## Convert::ToInt32(int32_t) متد

عدد صحیح 32 بیتی مشخص‌شده را برمی‌گرداند.

```cpp
static constexpr int System::Convert::ToInt32(int32_t value)
```

## Convert::ToInt32(uint64_t) متد

عدد صحیح 64 بیتی بدون علامت مشخص‌شده را به عدد صحیح 32 بیتی دارای علامت معادل تبدیل می‌کند.

```cpp
static int System::Convert::ToInt32(uint64_t value)
```

## Convert::ToInt32(int64_t) متد

عدد صحیح 64 بیتی دارای علامت مشخص‌شده را به عدد صحیح 32 بیتی دارای علامت معادل تبدیل می‌کند.

```cpp
static int System::Convert::ToInt32(int64_t value)
```

## Convert::ToInt32(float) متد

عدد شناور (float) مشخص‌شده را به عدد صحیح 32 بیتی دارای علامت معادل تبدیل می‌کند.

```cpp
static int System::Convert::ToInt32(float value)
```

## Convert::ToInt32(double) متد

عدد دوبل (double) مشخص‌شده را به عدد صحیح 32 بیتی دارای علامت معادل تبدیل می‌کند.

```cpp
static int System::Convert::ToInt32(double value)
```

## Convert::ToInt32(const Decimal\&) متد

عدد اعشاری (Decimal) مشخص‌شده را به عدد صحیح 32 بیتی دارای علامت معادل تبدیل می‌کند.

```cpp
static int System::Convert::ToInt32(const Decimal &value)
```

## Convert::ToInt32(char_t) متد

کاراکتر یونیکد مشخص‌شده را به عدد صحیح 32 بیتی دارای علامت معادل تبدیل می‌کند.

```cpp
static constexpr int System::Convert::ToInt32(char_t value)
```

## Convert::ToInt32(DateTime) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException را پرتاب می‌کند.

```cpp
static int System::Convert::ToInt32(DateTime value)
```

## Convert::ToInt32(std::nullptr_t) متد

رشته‌ی تهی (null-string) مشخص‌شده را به مقدار عدد صحیح 32 بیتی معادل تبدیل می‌کند.

```cpp
static constexpr int System::Convert::ToInt32(std::nullptr_t)
```

### مقدار بازگشت

صفر.

## Convert::ToInt32(const char_t *) متد

رشته c-string شامل نمایهٔ عدد را به مقدار عدد صحیح 32 بیتی معادل تبدیل می‌کند.

```cpp
static int System::Convert::ToInt32(const char_t *value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const char_t * | رشته c برای تبدیل |

### مقدار بازگشت

مقدار عدد صحیح 32 بیتی برابر با عددی که توسط رشته c-string مشخص‌شده نمایانده شده است

## Convert::ToInt32(const String\&) متد

رشته‌ای که نمایهٔ عدد را شامل می‌شود را به مقدار عدد صحیح 32 بیتی معادل تبدیل می‌کند.

```cpp
static int System::Convert::ToInt32(const String &value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |

### مقدار بازگشت

مقدار عدد صحیح 32 بیتی برابر با عددی که توسط رشتهٔ مشخص‌شده نمایانده شده است

## Convert::ToInt32(const String\&, int) متد

رشته‌ای که نمایهٔ عدد را در پایهٔ مشخص‌شده دارد را به مقدار عدد صحیح 32 بیتی معادل تبدیل می‌کند.

```cpp
static int System::Convert::ToInt32(const String &value, int from_base)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |
| from_base | int | پایهٔ عددی که توسط رشته نمایانده شده است |

### مقدار بازگشت

مقدار عدد صحیح 32 بیتی برابر با عددی که توسط رشتهٔ مشخص‌شده نمایانده شده است

## Convert::ToInt32(const String\&, const SharedPtr\<IFormatProvider\>\&) متد

رشته‌ای که نمایهٔ عدد را شامل می‌شود را با استفاده از اطلاعات قالب‌بندی ارائه‌شده، به مقدار عدد صحیح 32 بیتی معادل تبدیل می‌کند.

```cpp
static int System::Convert::ToInt32(const String &value, const SharedPtr<IFormatProvider> &provider)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | اشاره‌گری به شیئی که اطلاعات قالب‌بندی رشته را در بر دارد |

### مقدار بازگشت

مقدار عدد صحیح 32 بیتی برابر با عددی که توسط رشتهٔ مشخص‌شده نمایانده شده است

## Convert::ToInt32(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) متد




```cpp
static int System::Convert::ToInt32(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToInt32(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) متد




```cpp
static int System::Convert::ToInt32(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToInt32(const String\&, std::nullptr_t) متد




```cpp
static int System::Convert::ToInt32(const String &value, std::nullptr_t)
```

## Convert::ToInt32(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) متد

رشته‌ای که نمایهٔ عدد را شامل می‌شود را با استفاده از اطلاعات قالب‌بندی و سبک عدد ارائه‌شده، به مقدار عدد صحیح 32 بیتی معادل تبدیل می‌کند.

```cpp
static int System::Convert::ToInt32(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | ترکیبی بیتی از مقادیر شمارش NumberStyles که سبک مجاز نمایهٔ عدد را مشخص می‌کند |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | اشاره‌گری به شیئی که اطلاعات قالب‌بندی رشته را در بر دارد |

### مقدار بازگشت

مقدار عدد صحیح 32 بیتی برابر با عددی که توسط رشتهٔ مشخص‌شده نمایانده شده است

## Convert::ToInt32(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) متد




```cpp
static int System::Convert::ToInt32(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToInt32(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) متد




```cpp
static int System::Convert::ToInt32(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToInt32(const String\&, Globalization::NumberStyles, std::nullptr_t) متد 




```cpp
static int System::Convert::ToInt32(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToInt32(Enum) متد 




```cpp
template<typename Enum,typename> static int32_t System::Convert::ToInt32(Enum value)
```

## Convert::ToInt32(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) متد

مقدار بسته‌بندی‌شدهٔ مشخص‌شده را به مقدار عدد صحیح 32 بیتی معادل تبدیل می‌کند.

```cpp
static int System::Convert::ToInt32(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | اشاره‌گر اشتراکی به شیئی که مقدار بسته‌بندی‌شده را برای تبدیل نگه می‌دارد |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | قالب‌بندی رشته‌ای که اگر نوع مقدار بسته‌بندی‌شده [String](../../string/) باشد، استفاده می‌شود |

### مقدار بازگشت

مقدار عدد صحیح 32 بیتی معادل مقدار بسته‌بندی‌شدهٔ مشخص‌شده

## مراجع مرتبط

* Enum [NumberStyles](../../../system.globalization/numberstyles/)
* Typedef [SharedPtr](../../sharedptr/)
* Class [Decimal](../../decimal/)
* Class [DateTime](../../datetime/)
* Class [String](../../string/)
* Class [IFormatProvider](../../iformatprovider/)
* Class [CultureInfo](../../../system.globalization/cultureinfo/)
* Class [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* Class [Object](../../object/)
* Struct [Convert](../)
* Struct [Enum](../../enum/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)