---
title: ToSingle()
second_title: Aspose.Slides برای C++ مرجع API
description: مقدار بولی مشخص شده را به یک عدد شناور تک‌دقت معادل تبدیل می‌کند.
type: docs
weight: 209
url: /fa/system/convert/tosingle/
---
## Convert::ToSingle(bool) متد

مقدار بولی مشخص شده را به یک عدد شناور تک‌دقت معادل تبدیل می‌کند.

```cpp
static constexpr float System::Convert::ToSingle(bool value)
```

## Convert::ToSingle(uint8_t) متد

عدد صحیح بدون علامت ۸ بیتی مشخص شده را به یک عدد شناور تک‌دقت معادل تبدیل می‌کند.

```cpp
static constexpr float System::Convert::ToSingle(uint8_t value)
```

## Convert::ToSingle(int8_t) متد

عدد صحیح علامت‌دار ۸ بیتی مشخص شده را به یک عدد شناور تک‌دقت معادل تبدیل می‌کند.

```cpp
static constexpr float System::Convert::ToSingle(int8_t value)
```

## Convert::ToSingle(uint16_t) متد

عدد صحیح بدون علامت ۱۶ بیتی مشخص شده را به یک عدد شناور تک‌دقت معادل تبدیل می‌کند.

```cpp
static constexpr float System::Convert::ToSingle(uint16_t value)
```

## Convert::ToSingle(int16_t) متد

عدد صحیح علامت‌دار ۱۶ بیتی مشخص شده را به یک عدد شناور تک‌دقت معادل تبدیل می‌کند.

```cpp
static constexpr float System::Convert::ToSingle(int16_t value)
```

## Convert::ToSingle(uint32_t) متد

عدد صحیح بدون علامت ۳۲ بیتی مشخص شده را به یک عدد شناور تک‌دقت معادل تبدیل می‌کند.

```cpp
static constexpr float System::Convert::ToSingle(uint32_t value)
```

## Convert::ToSingle(int32_t) متد

عدد صحیح علامت‌دار ۳۲ بیتی مشخص شده را به یک عدد شناور تک‌دقت معادل تبدیل می‌کند.

```cpp
static constexpr float System::Convert::ToSingle(int32_t value)
```

## Convert::ToSingle(uint64_t) متد

عدد صحیح بدون علامت ۶۴ بیتی مشخص شده را به یک عدد شناور تک‌دقت معادل تبدیل می‌کند.

```cpp
static constexpr float System::Convert::ToSingle(uint64_t value)
```

## Convert::ToSingle(int64_t) متد

عدد صحیح علامت‌دار ۶۴ بیتی مشخص شده را به یک عدد شناور تک‌دقت معادل تبدیل می‌کند.

```cpp
static constexpr float System::Convert::ToSingle(int64_t value)
```

## Convert::ToSingle(float) متد

عدد float مشخص شده را برمی‌گرداند.

```cpp
static constexpr float System::Convert::ToSingle(float value)
```

## Convert::ToSingle(double) متد

عدد دوچند precision مشخص شده را به یک عدد شناور تک‌دقت معادل تبدیل می‌کند.

```cpp
static constexpr float System::Convert::ToSingle(double value)
```

## Convert::ToSingle(const Decimal\&) متد

عدد Decimal مشخص شده را به یک عدد شناور تک‌دقت معادل تبدیل می‌کند.

```cpp
static float System::Convert::ToSingle(const Decimal &value)
```

## Convert::ToSingle(char_t) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException را پرتاب می‌کند.

```cpp
static float System::Convert::ToSingle(char_t value)
```

## Convert::ToSingle(DateTime) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException را پرتاب می‌کند.

```cpp
static float System::Convert::ToSingle(DateTime value)
```

## Convert::ToSingle(std::nullptr_t) متد

رشته تهی مشخص شده را به مقدار شناور تک‌دقت معادل تبدیل می‌کند.

```cpp
static constexpr float System::Convert::ToSingle(std::nullptr_t)
```

### مقدار بازگشتی

صفر.

## Convert::ToSingle(const char_t *) متد

رشته C شامل نمایش عددی را به مقدار شناور تک‌دقت معادل تبدیل می‌کند.

```cpp
static float System::Convert::ToSingle(const char_t *value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const char_t * | رشته C برای تبدیل |

### مقدار بازگشتی

مقدار شناور تک‌دقت برابر با عددی که توسط رشته C مشخص شده نمایش داده می‌شود.

## Convert::ToSingle(const String\&) متد

رشته شامل نمایش عددی را به مقدار شناور تک‌دقت معادل تبدیل می‌کند.

```cpp
static float System::Convert::ToSingle(const String &value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |

### مقدار بازگشتی

مقدار شناور تک‌دقت برابر با عددی که توسط رشته مشخص شده نمایش داده می‌شود.

## Convert::ToSingle(const String\&, const SharedPtr\<IFormatProvider\>\&) متد

رشته شامل نمایش عددی را با استفاده از اطلاعات قالب‌بندی ارائه‌شده به مقدار شناور تک‌دقت معادل تبدیل می‌کند.

```cpp
static float System::Convert::ToSingle(const String &value, const SharedPtr<IFormatProvider> &provider)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | یک اشاره‌گر به شیئی که اطلاعات قالب رشته را شامل می‌شود |

### مقدار بازگشتی

مقدار شناور تک‌دقت برابر با عددی که توسط رشته مشخص شده نمایش داده می‌شود.

## Convert::ToSingle(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) متد

```cpp
static float System::Convert::ToSingle(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToSingle(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) متد

```cpp
static float System::Convert::ToSingle(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToSingle(const String\&, std::nullptr_t) متد

```cpp
static float System::Convert::ToSingle(const String &value, std::nullptr_t)
```

## Convert::ToSingle(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) متد

رشته شامل نمایش عددی را با استفاده از اطلاعات قالب‌بندی و سبک عددی ارائه‌شده به مقدار شناور تک‌دقت معادل تبدیل می‌کند.

```cpp
static float System::Convert::ToSingle(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | ترکیب بیتی مقادیر enum NumberStyles که سبک مجاز نمایش عدد را مشخص می‌کند |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | یک اشاره‌گر به شیئی که اطلاعات قالب رشته را شامل می‌شود |

### مقدار بازگشتی

مقدار شناور تک‌دقت برابر با عددی که توسط رشته مشخص شده نمایش داده می‌شود.

## Convert::ToSingle(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) متد

```cpp
static float System::Convert::ToSingle(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToSingle(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) متد

```cpp
static float System::Convert::ToSingle(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToSingle(const String\&, Globalization::NumberStyles, std::nullptr_t) متد

```cpp
static float System::Convert::ToSingle(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToSingle(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) متد

مقدار بسته‌بندی‌شده مشخص شده را به مقدار شناور تک‌دقت تبدیل می‌کند.

```cpp
static float System::Convert::ToSingle(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | اشاره‌گر همگانی به شیئی که مقدار را محفظه‌سازی کرده برای تبدیل |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | قالب رشته‌ای که در صورت نوع مقدار محفظه‌سازی‌شده [String](../../string/) استفاده می‌شود |

### مقدار بازگشتی

یک مقدار شناور تک‌دقت معادل مقدار محفظه‌سازی‌شده مشخص شده

## موارد مرتبط

* Enum [NumberStyles](../../../system.globalization/numberstyles/)
* Typedef [SharedPtr](../../sharedptr/)
* Class [Decimal](../../decimal/)
* Class [DateTime](../../datetime/)
* Class [String](../../string/)
* Class [IFormatProvider](../../iformatprovider/)
* Class [CultureInfo](../../../system.globalization/cultureinfo/)
* Class [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* Class [Object](../../object/)
* Struct [Convert](../)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)