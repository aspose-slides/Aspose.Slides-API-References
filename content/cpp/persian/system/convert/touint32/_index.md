---
title: ToUInt32()
second_title: مرجع API Aspose.Slides برای C++
description: مقدار بولی مشخص‌شده را به یک عدد صحیح بدون علامت ۳۲-بیتی معادل تبدیل می‌کند.
type: docs
weight: 170
url: /fa/system/convert/touint32/
---
## Convert::ToUInt32(bool) متد

مقدار بولی مشخص‌شده را به یک عدد صحیح بدون علامت ۳۲-بیتی معادل تبدیل می‌کند.

```cpp
static constexpr uint32_t System::Convert::ToUInt32(bool value)
```

## Convert::ToUInt32(uint8_t) متد

عدد صحیح بدون علامت ۸-بیتی مشخص‌شده را به یک عدد صحیح بدون علامت ۳۲-بیتی معادل تبدیل می‌کند.

```cpp
static constexpr uint32_t System::Convert::ToUInt32(uint8_t value)
```

## Convert::ToUInt32(int8_t) متد

عدد صحیح علامت‌دار ۸-بیتی مشخص‌شده را به یک عدد صحیح بدون علامت ۳۲-بیتی معادل تبدیل می‌کند.

```cpp
static uint32_t System::Convert::ToUInt32(int8_t value)
```

## Convert::ToUInt32(uint16_t) متد

عدد صحیح بدون علامت ۱۶-بیتی مشخص‌شده را به یک عدد صحیح بدون علامت ۳۲-بیتی معادل تبدیل می‌کند.

```cpp
static constexpr uint32_t System::Convert::ToUInt32(uint16_t value)
```

## Convert::ToUInt32(int16_t) متد

عدد صحیح علامت‌دار ۱۶-بیتی مشخص‌شده را به یک عدد صحیح بدون علامت ۳۲-بیتی معادل تبدیل می‌کند.

```cpp
static uint32_t System::Convert::ToUInt32(int16_t value)
```

## Convert::ToUInt32(uint32_t) متد

عدد صحیح بدون علامت ۳۲-بیتی مشخص‌شده را باز می‌گرداند.

```cpp
static constexpr uint32_t System::Convert::ToUInt32(uint32_t value)
```

## Convert::ToUInt32(int32_t) متد

عدد صحیح علامت‌دار ۳۲-بیتی مشخص‌شده را به یک عدد صحیح بدون علامت ۳۲-بیتی معادل تبدیل می‌کند.

```cpp
static uint32_t System::Convert::ToUInt32(int32_t value)
```

## Convert::ToUInt32(uint64_t) متد

عدد صحیح بدون علامت ۶۴-بیتی مشخص‌شده را به یک عدد صحیح بدون علامت ۳۲-بیتی معادل تبدیل می‌کند.

```cpp
static uint32_t System::Convert::ToUInt32(uint64_t value)
```

## Convert::ToUInt32(int64_t) متد

عدد صحیح علامت‌دار ۶۴-بیتی مشخص‌شده را به یک عدد صحیح بدون علامت ۳۲-بیتی معادل تبدیل می‌کند.

```cpp
static uint32_t System::Convert::ToUInt32(int64_t value)
```

## Convert::ToUInt32(float) متد

عدد شناور (float) مشخص‌شده را به یک عدد صحیح بدون علامت ۳۲-بیتی معادل تبدیل می‌کند.

```cpp
static uint32_t System::Convert::ToUInt32(float value)
```

## Convert::ToUInt32(double) متد

عدد دوبل (double) مشخص‌شده را به یک عدد صحیح بدون علامت ۳۲-بیتی معادل تبدیل می‌کند.

```cpp
static uint32_t System::Convert::ToUInt32(double value)
```

## Convert::ToUInt32(const Decimal\&) متد

عدد دسیمال مشخص‌شده را به یک عدد صحیح بدون علامت ۳۲-بیتی معادل تبدیل می‌کند.

```cpp
static uint32_t System::Convert::ToUInt32(const Decimal &value)
```

## Convert::ToUInt32(char_t) متد

کاراکتر یونیکد مشخص‌شده را به یک عدد صحیح بدون علامت ۳۲-بیتی معادل تبدیل می‌کند.

```cpp
static constexpr uint32_t System::Convert::ToUInt32(char_t value)
```

## Convert::ToUInt32(DateTime) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException را پرتاب می‌کند.

```cpp
static uint32_t System::Convert::ToUInt32(DateTime value)
```

## Convert::ToUInt32(std::nullptr_t) متد

رشته تهی (null-string) مشخص‌شده را به مقدار معادل عدد صحیح بدون علامت ۳۲-بیتی تبدیل می‌کند.

```cpp
static constexpr uint32_t System::Convert::ToUInt32(std::nullptr_t)
```

### مقدار بازگشتی

صفر.

## Convert::ToUInt32(const char_t *) متد

c-string حاوی نمایش متنی یک عدد را به مقدار معادل عدد صحیح بدون علامت ۳۲-بیتی تبدیل می‌کند.

```cpp
static uint32_t System::Convert::ToUInt32(const char_t *value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const char_t * | c-string برای تبدیل |

### مقدار بازگشتی

مقدار عدد صحیح بدون علامت ۳۲-بیتی که برابر با عددی است که در c-string مشخص‌شده نمایش داده می‌شود

## Convert::ToUInt32(const String\&) متد

رشته حاوی نمایش متنی یک عدد را به مقدار معادل عدد صحیح بدون علامت ۳۲-بیتی تبدیل می‌کند.

```cpp
static uint32_t System::Convert::ToUInt32(const String &value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |

### مقدار بازگشتی

مقدار عدد صحیح بدون علامت ۳۲-بیتی که برابر با عددی است که در رشته مشخص‌شده نمایش داده می‌شود

## Convert::ToUInt32(const String\&, int) متد

رشته حاوی نمایش متنی یک عدد در پایهٔ مشخص‌شده را به مقدار معادل عدد صحیح بدون علامت ۳۲-بیتی تبدیل می‌کند.

```cpp
static uint32_t System::Convert::ToUInt32(const String &value, int from_base)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |
| from_base | int | پایهٔ عددی که در رشته نمایش داده شده است |

### مقدار بازگشتی

مقدار عدد صحیح بدون علامت ۳۲-بیتی که برابر با عددی است که در رشته مشخص‌شده نمایش داده می‌شود

## Convert::ToUInt32(const String\&, const SharedPtr\<IFormatProvider\>\&) متد

رشته حاوی نمایش متنی یک عدد را با استفاده از اطلاعات قالب‌بندی ارائه‌شده به مقدار معادل عدد صحیح بدون علامت ۳۲-بیتی تبدیل می‌کند.

```cpp
static uint32_t System::Convert::ToUInt32(const String &value, const SharedPtr<IFormatProvider> &provider)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | اشاره‌گری به شیئی که اطلاعات قالب‌بندی رشته را شامل می‌شود |

### مقدار بازگشتی

مقدار عدد صحیح بدون علامت ۳۲-بیتی که برابر با عددی است که در رشته مشخص‌شده نمایش داده می‌شود

## Convert::ToUInt32(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) متد

```cpp
static uint32_t System::Convert::ToUInt32(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToUInt32(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) متد

```cpp
static uint32_t System::Convert::ToUInt32(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToUInt32(const String\&, std::nullptr_t) متد

```cpp
static uint32_t System::Convert::ToUInt32(const String &value, std::nullptr_t)
```

## Convert::ToUInt32(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) متد

رشته حاوی نمایش متنی یک عدد را با استفاده از اطلاعات قالب‌بندی و سبک عدد ارائه‌شده به مقدار معادل عدد صحیح بدون علامت ۳۲-بیتی تبدیل می‌کند.

```cpp
static uint32_t System::Convert::ToUInt32(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | ترکیبی بیتی از مقادیر enum NumberStyles که سبک مجاز نمایش رشته‌ای عدد را مشخص می‌کند |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | اشاره‌گری به شیئی که اطلاعات قالب‌بندی رشته را شامل می‌شود |

### مقدار بازگشتی

مقدار عدد صحیح بدون علامت ۳۲-بیتی که برابر با عددی است که در رشته مشخص‌شده نمایش داده می‌شود

## Convert::ToUInt32(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) متد

```cpp
static uint32_t System::Convert::ToUInt32(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToUInt32(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) متد

```cpp
static uint32_t System::Convert::ToUInt32(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToUInt32(const String\&, Globalization::NumberStyles, std::nullptr_t) متد

```cpp
static uint32_t System::Convert::ToUInt32(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToUInt32(Enum) متد

```cpp
template<typename Enum,typename> static uint32_t System::Convert::ToUInt32(Enum value)
```

## Convert::ToUInt32(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) متد

مقدار بسته‌بندی‌شدهٔ مشخص‌شده را به مقدار معادل عدد صحیح بدون علامت ۳۲-بیتی تبدیل می‌کند.

```cpp
static uint32_t System::Convert::ToUInt32(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | SharedPtr شیئی که مقدار بسته‌بندی‌شده برای تبدیل را نگه می‌دارد |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | قالب‌بندی رشته‌ای که در صورت اینکه نوع مقدار بسته‌بندی‌شده [String](../../string/) باشد، استفاده می‌شود |

### مقدار بازگشتی

مقدار عدد صحیح بدون علامت ۳۲-بیتی معادل با مقدار بسته‌بندی‌شدهٔ مشخص‌شده

## موارد مرتبط

* Enum [NumberStyles](../../../system.globalization/numberstyles/)
* Typedef [SharedPtr](../../sharedptr/)
* کلاس [Decimal](../../decimal/)
* کلاس [DateTime](../../datetime/)
* کلاس [String](../../string/)
* کلاس [IFormatProvider](../../iformatprovider/)
* کلاس [CultureInfo](../../../system.globalization/cultureinfo/)
* کلاس [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* کلاس [Object](../../object/)
* Struct [Convert](../)
* Struct [Enum](../../enum/)
* فضای‌نام [System](../../)
* Library [Aspose.Slides](../../../)