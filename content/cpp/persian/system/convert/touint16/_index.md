---
title: ToUInt16()
second_title: مرجع API Aspose.Slides برای C++
description: مقدار بولی مشخص‌شده را به عدد صحیح بدون علامت 16 بیتی معادل تبدیل می‌کند.
type: docs
weight: 144
url: /fa/system/convert/touint16/
---
## Convert::ToUInt16(bool) متد

مقدار بولی مشخص‌شده را به عدد صحیح بدون علامت 16 بیتی معادل تبدیل می‌کند.

```cpp
static constexpr uint16_t System::Convert::ToUInt16(bool value)
```

## Convert::ToUInt16(uint8_t) متد

مقدار عدد صحیح بدون علامت 8 بیتی مشخص‌شده را به عدد صحیح بدون علامت 16 بیتی معادل تبدیل می‌کند.

```cpp
static constexpr uint16_t System::Convert::ToUInt16(uint8_t value)
```

## Convert::ToUInt16(int8_t) متد

مقدار عدد صحیح 8 بیتی علامت‌دار مشخص‌شده را به عدد صحیح بدون علامت 16 بیتی معادل تبدیل می‌کند.

```cpp
static uint16_t System::Convert::ToUInt16(int8_t value)
```

## Convert::ToUInt16(uint16_t) متد

عدد صحیح بدون علامت 16 بیتی مشخص‌شده را باز می‌گرداند.

```cpp
static constexpr uint16_t System::Convert::ToUInt16(uint16_t value)
```

## Convert::ToUInt16(int16_t) متد

مقدار عدد صحیح 16 بیتی علامت‌دار مشخص‌شده را به عدد صحیح بدون علامت 16 بیتی معادل تبدیل می‌کند.

```cpp
static uint16_t System::Convert::ToUInt16(int16_t value)
```

## Convert::ToUInt16(uint32_t) متد

مقدار عدد صحیح بدون علامت 32 بیتی مشخص‌شده را به عدد صحیح بدون علامت 16 بیتی معادل تبدیل می‌کند.

```cpp
static uint16_t System::Convert::ToUInt16(uint32_t value)
```

## Convert::ToUInt16(int32_t) متد

مقدار عدد صحیح 32 بیتی علامت‌دار مشخص‌شده را به عدد صحیح بدون علامت 16 بیتی معادل تبدیل می‌کند.

```cpp
static uint16_t System::Convert::ToUInt16(int32_t value)
```

## Convert::ToUInt16(uint64_t) متد

مقدار عدد صحیح بدون علامت 64 بیتی مشخص‌شده را به عدد صحیح بدون علامت 16 بیتی معادل تبدیل می‌کند.

```cpp
static uint16_t System::Convert::ToUInt16(uint64_t value)
```

## Convert::ToUInt16(int64_t) متد

مقدار عدد صحیح 64 بیتی علامت‌دار مشخص‌شده را به عدد صحیح بدون علامت 16 بیتی معادل تبدیل می‌کند.

```cpp
static uint16_t System::Convert::ToUInt16(int64_t value)
```

## Convert::ToUInt16(float) متد

عدد شناور مشخص‌شده را به عدد صحیح بدون علامت 16 بیتی معادل تبدیل می‌کند.

```cpp
static uint16_t System::Convert::ToUInt16(float value)
```

## Convert::ToUInt16(double) متد

عدد دابل مشخص‌شده را به عدد صحیح بدون علامت 16 بیتی معادل تبدیل می‌کند.

```cpp
static uint16_t System::Convert::ToUInt16(double value)
```

## Convert::ToUInt16(const Decimal\&) متد

عدد دسیمال مشخص‌شده را به عدد صحیح بدون علامت 16 بیتی معادل تبدیل می‌کند.

```cpp
static uint16_t System::Convert::ToUInt16(const Decimal &value)
```

## Convert::ToUInt16(char_t) متد

کاراکتر یونی‌کد مشخص‌شده را به عدد صحیح بدون علامت 16 بیتی معادل تبدیل می‌کند.

```cpp
static constexpr uint16_t System::Convert::ToUInt16(char_t value)
```

## Convert::ToUInt16(DateTime) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException را پرتاب می‌کند.

```cpp
static uint16_t System::Convert::ToUInt16(DateTime value)
```

## Convert::ToUInt16(std::nullptr_t) متد

رشته تهی مشخص‌شده را به مقدار عدد صحیح بدون علامت 16 بیتی معادل تبدیل می‌کند.

```cpp
static constexpr uint16_t System::Convert::ToUInt16(std::nullptr_t)
```

### مقدار بازگشتی

صفر.

## Convert::ToUInt16(const char_t *) متد

رشته C-ی مشخص‌شده که نمایانگر عددی است را به مقدار عدد صحیح بدون علامت 16 بیتی معادل تبدیل می‌کند.

```cpp
static uint16_t System::Convert::ToUInt16(const char_t *value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | const char_t * | رشته C- ای برای تبدیل |

### مقدار بازگشتی

مقدار عدد صحیح بدون علامت 16 بیتی برابر با عددی که توسط رشته C- ای مشخص‌شده نمایش داده می‌شود

## Convert::ToUInt16(const String\&) متد

رشته مشخص‌شده که نمایش عددی دارد را به مقدار عدد صحیح بدون علامت 16 بیتی معادل تبدیل می‌کند.

```cpp
static uint16_t System::Convert::ToUInt16(const String &value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |

### مقدار بازگشتی

مقدار عدد صحیح بدون علامت 16 بیتی برابر با عددی که توسط رشتهٔ مشخص‌شده نمایش داده می‌شود

## Convert::ToUInt16(const String\&, int) متد

رشته مشخص‌شده که نمایانگر عددی در پایهٔ داده‌شده است را به مقدار عدد صحیح بدون علامت 16 بیتی معادل تبدیل می‌کند.

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, int from_base)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |
| from_base | int | پایهٔ عددی که توسط رشته نمایان می‌شود |

### مقدار بازگشتی

مقدار عدد صحیح بدون علامت 16 بیتی برابر با عددی که توسط رشتهٔ مشخص‌شده نمایش داده می‌شود

## Convert::ToUInt16(const String\&, const SharedPtr\<IFormatProvider\>\&) متد

رشتهٔ مشخص‌شده را با استفاده از اطلاعات قالب‌بندی ارائه‌شده به مقدار عدد صحیح بدون علامت 16 بیتی معادل تبدیل می‌کند.

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, const SharedPtr<IFormatProvider> &provider)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | اشاره‌گری به شیئی که اطلاعات قالب‌بندی رشته را دارد |

### مقدار بازگشتی

مقدار عدد صحیح بدون علامت 16 بیتی برابر با عددی که توسط رشتهٔ مشخص‌شده نمایش داده می‌شود

## Convert::ToUInt16(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) متد

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToUInt16(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) متد

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToUInt16(const String\&, std::nullptr_t) متد

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, std::nullptr_t)
```

## Convert::ToUInt16(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) متد

رشتهٔ مشخص‌شده را با استفاده از اطلاعات قالب‌بندی و سبک عددی ارائه‌شده به مقدار عدد صحیح بدون علامت 16 بیتی معادل تبدیل می‌کند.

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | ترکیب بیتی مقادیر enum NumberStyles که سبک مجاز نمایش عدد به صورت رشته را مشخص می‌کند |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | اشاره‌گری به شیئی که اطلاعات قالب‌بندی رشته را دارد |

### مقدار بازگشتی

مقدار عدد صحیح بدون علامت 16 بیتی برابر با عددی که توسط رشتهٔ مشخص‌شده نمایش داده می‌شود

## Convert::ToUInt16(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) متد

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToUInt16(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) متد

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToUInt16(const String\&, Globalization::NumberStyles, std::nullptr_t) متد

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToUInt16(Enum) متد

```cpp
template<typename Enum,typename> static uint16_t System::Convert::ToUInt16(Enum value)
```

## Convert::ToUInt16(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) متد

مقدار بسته‌بندی‌شدهٔ مشخص‌شده را به مقدار عدد صحیح بدون علامت 16 بیتی معادل تبدیل می‌کند.

```cpp
static uint16_t System::Convert::ToUInt16(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | اشاره‌گر مشترک به شیئی که مقدار را بسته‌بندی می‌کند تا تبدیل شود |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | قالب رشته‌ای که در صورت نوع مقدار بسته‌بندی‌شده [String](../../string/) استفاده می‌شود |

### مقدار بازگشتی

مقدار عدد صحیح بدون علامت 16 بیتی معادل مقدار بسته‌بندی‌شدهٔ مشخص‌شده

## موارد مرتبط

* Enum [NumberStyles](../../../system.globalization/numberstyles/)
* Typedef [SharedPtr](../../sharedptr/)
* Class [Decimal](../../decimal/)
* Class [DateTime](../../datetime/)
* Class [String](../../string/)
* Class [IFormatProvider](../../iformatprovider/)
* Class [CultureInfo](../../../system.globalization/cultureinfo/)
* Class [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* Class [Object](../../object/)
* Struct [Convert](../)
* Struct [Enum](../../enum/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)