---
title: ToUInt64()
second_title: مرجع API Aspose.Slides برای C++
description: مقدار boolean مشخص‌شده را به یک عدد صحیح بدون علامت 64 بیتی معادل تبدیل می‌کند.
type: docs
weight: 196
url: /fa/system/convert/touint64/
---
## Convert::ToUInt64(bool) متد

مقدار boolean مشخص‌شده را به یک عدد صحیح بدون علامت 64 بیتی معادل تبدیل می‌کند.

```cpp
static constexpr uint64_t System::Convert::ToUInt64(bool value)
```

## Convert::ToUInt64(uint8_t) متد

عدد صحیح بدون علامت 8 بیتی مشخص‌شده را به یک عدد صحیح بدون علامت 64 بیتی معادل تبدیل می‌کند.

```cpp
static constexpr uint64_t System::Convert::ToUInt64(uint8_t value)
```

## Convert::ToUInt64(int8_t) متد

عدد صحیح علامت‌دار 8 بیتی مشخص‌شده را به یک عدد صحیح بدون علامت 64 بیتی معادل تبدیل می‌کند.

```cpp
static uint64_t System::Convert::ToUInt64(int8_t value)
```

## Convert::ToUInt64(uint16_t) متد

عدد صحیح بدون علامت 16 بیتی مشخص‌شده را به یک عدد صحیح بدون علامت 64 بیتی معادل تبدیل می‌کند.

```cpp
static constexpr uint64_t System::Convert::ToUInt64(uint16_t value)
```

## Convert::ToUInt64(int16_t) متد

عدد صحیح علامت‌دار 16 بیتی مشخص‌شده را به یک عدد صحیح بدون علامت 64 بیتی معادل تبدیل می‌کند.

```cpp
static uint64_t System::Convert::ToUInt64(int16_t value)
```

## Convert::ToUInt64(uint32_t) متد

عدد صحیح بدون علامت 32 بیتی مشخص‌شده را به یک عدد صحیح بدون علامت 64 بیتی معادل تبدیل می‌کند.

```cpp
static constexpr uint64_t System::Convert::ToUInt64(uint32_t value)
```

## Convert::ToUInt64(int32_t) متد

عدد صحیح علامت‌دار 32 بیتی مشخص‌شده را به یک عدد صحیح بدون علامت 64 بیتی معادل تبدیل می‌کند.

```cpp
static uint64_t System::Convert::ToUInt64(int32_t value)
```

## Convert::ToUInt64(uint64_t) متد

عدد صحیح بدون علامت 64 بیتی مشخص‌شده را برمی‌گرداند.

```cpp
static constexpr uint64_t System::Convert::ToUInt64(uint64_t value)
```

## Convert::ToUInt64(int64_t) متد

عدد صحیح علامت‌دار 64 بیتی مشخص‌شده را به یک عدد صحیح بدون علامت 64 بیتی معادل تبدیل می‌کند.

```cpp
static uint64_t System::Convert::ToUInt64(int64_t value)
```

## Convert::ToUInt64(float) متد

عدد شناور float مشخص‌شده را به یک عدد صحیح بدون علامت 64 بیتی معادل تبدیل می‌کند.

```cpp
static uint64_t System::Convert::ToUInt64(float value)
```

## Convert::ToUInt64(double) متد

عدد شناور double مشخص‌شده را به یک عدد صحیح بدون علامت 64 بیتی معادل تبدیل می‌کند.

```cpp
static uint64_t System::Convert::ToUInt64(double value)
```

## Convert::ToUInt64(const Decimal\&) متد

عدد اعشاری Decimal مشخص‌شده را به یک عدد صحیح بدون علامت 64 بیتی معادل تبدیل می‌کند.

```cpp
static uint64_t System::Convert::ToUInt64(const Decimal &value)
```

## Convert::ToUInt64(char_t) متد

کاراکتر یونیکد مشخص‌شده را به یک عدد صحیح بدون علامت 64 بیتی معادل تبدیل می‌کند.

```cpp
static constexpr uint64_t System::Convert::ToUInt64(char_t value)
```

## Convert::ToUInt64(DateTime) متد

تبدیل پشتیبانی نمی‌شود. همیشه InvalidCastException را برافراز می‌کند.

```cpp
static uint64_t System::Convert::ToUInt64(DateTime value)
```

## Convert::ToUInt64(std::nullptr_t) متد

رشتهٔ null-string مشخص‌شده را به مقدار عدد صحیح بدون علامت 64 بیتی معادل تبدیل می‌کند.

```cpp
static constexpr uint64_t System::Convert::ToUInt64(std::nullptr_t)
```

### مقدار بازگشتی

صفر.

## Convert::ToUInt64(const char_t *) متد

رشتهٔ c-string حاوی نمایش عددی یک مقدار را به مقدار عدد صحیح بدون علامت 64 بیتی معادل تبدیل می‌کند.

```cpp
static uint64_t System::Convert::ToUInt64(const char_t *value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const char_t * | رشتهٔ c-string برای تبدیل |

### مقدار بازگشتی

مقدار عدد صحیح بدون علامت 64 بیتی برابر با عددی که توسط رشتهٔ c-string مشخص‌شده نمایش داده شده است

## Convert::ToUInt64(const String\&) متد

رشتهٔ String حاوی نمایش عددی یک مقدار را به مقدار عدد صحیح بدون علامت 64 بیتی معادل تبدیل می‌کند.

```cpp
static uint64_t System::Convert::ToUInt64(const String &value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |

### مقدار بازگشتی

مقدار عدد صحیح بدون علامت 64 بیتی برابر با عددی که توسط رشتهٔ مشخص‌شده نمایش داده شده است

## Convert::ToUInt64(const String\&, int) متد

رشتهٔ String حاوی نمایش عددی یک مقدار در پایهٔ مشخص‌شده را به مقدار عدد صحیح بدون علامت 64 بیتی معادل تبدیل می‌کند.

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, int from_base)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |
| from_base | int | پایهٔ عددی که توسط رشته نمایش داده شده است |

### مقدار بازگشتی

مقدار عدد صحیح بدون علامت 64 بیتی برابر با عددی که توسط رشتهٔ مشخص‌شده نمایش داده شده است

## Convert::ToUInt64(const String\&, const SharedPtr\<IFormatProvider\>\&) متد

رشتهٔ String حاوی نمایش عددی یک مقدار را با استفاده از اطلاعات قالب‌بندی ارائه‌شده به مقدار عدد صحیح بدون علامت 64 بیتی معادل تبدیل می‌کند.

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, const SharedPtr<IFormatProvider> &provider)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | اشاره‌گری به شیئی که اطلاعات قالب‌بندی رشته را شامل می‌شود |

### مقدار بازگشتی

مقدار عدد صحیح بدون علامت 64 بیتی برابر با عددی که توسط رشتهٔ مشخص‌شده نمایش داده شده است

## Convert::ToUInt64(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) متد

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToUInt64(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) متد

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToUInt64(const String\&, std::nullptr_t) متد

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, std::nullptr_t)
```

## Convert::ToUInt64(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) متد

رشتهٔ String حاوی نمایش عددی یک مقدار را با استفاده از اطلاعات قالب‌بندی ارائه‌شده و سبک عددی معین به مقدار عدد صحیح بدون علامت 64 بیتی معادل تبدیل می‌کند.

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../string/)\& | رشته برای تبدیل |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | ترکیبی بیتی از مقادیر enum NumberStyles که سبک مجاز نمایش عددی رشته را مشخص می‌کند |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | اشاره‌گری به شیئی که اطلاعات قالب‌بندی رشته را شامل می‌شود |

### مقدار بازگشتی

مقدار عدد صحیح بدون علامت 64 بیتی برابر با عددی که توسط رشتهٔ مشخص‌شده نمایش داده شده است

## Convert::ToUInt64(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) متد

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToUInt64(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) متد

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToUInt64(const String\&, Globalization::NumberStyles, std::nullptr_t) متد

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToUInt64(Enum) متد

```cpp
template<typename Enum,typename> static uint64_t System::Convert::ToUInt64(Enum value)
```

## Convert::ToUInt64(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) متد

مقدار جعبه‌بندی‌شدهٔ مشخص‌شده را به مقدار عدد صحیح بدون علامت 64 بیتی معادل تبدیل می‌کند.

```cpp
static uint64_t System::Convert::ToUInt64(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | اشاره‌گر به شیء shared pointer که مقدار جعبه‌شده برای تبدیل را در خود دارد |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | قالب‌بندی رشته‌ای که در صورت تطابق نوع مقدار جعبه‌شده با [String](../../string/) استفاده می‌شود |

### مقدار بازگشتی

مقدار عدد صحیح بدون علامت 64 بیتی معادل مقدار جعبه‌شدهٔ مشخص‌شده

## موارد مرتبط

* Enum [NumberStyles](../../../system.globalization/numberstyles/)
* Typedef [SharedPtr](../../sharedptr/)
* Class [Decimal](../../decimal/)
* Class [DateTime](../../datetime/)
* Class [String](../../string/)
* Class [IFormatProvider](../../iformatprovider/)
* Class [CultureInfo](../../../system.globalization/cultureinfo/)
* Class [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* Class [Object](../../object/)
* Struct [Convert](../)
* Struct [Enum](../../enum/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)