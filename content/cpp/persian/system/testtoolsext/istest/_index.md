---
title: IsTest()
second_title: Aspose.Slides برای C++ مرجع API
description: وجود متد تست را بررسی می‌کند.
type: docs
weight: 1
url: /fa/system/testtoolsext/istest/
---
## TestToolsExt::IsTest(const char *, const char *, const char *) متد

وجود متد تست را بررسی می‌کند.

```cpp
static bool System::TestToolsExt::IsTest(const char *name_space, const char *class_name, const char *method_name)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| name_space | const char * | فضای نام برای جستجو. |
| class_name | const char * | کلاس برای جستجو. |
| method_name | const char * | متد برای جستجو. |

### مقدار بازگشت

True اگر متد تست ثبت شده باشد، false در غیر این صورت.

## TestToolsExt::IsTest(const char *, const char *) متد

وجود متد تست را بررسی می‌کند.

```cpp
static bool System::TestToolsExt::IsTest(const char *class_name, const char *method_name)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| class_name | const char * | کلاس برای جستجو. |
| method_name | const char * | متد برای جستجو. |

### مقدار بازگشت

True اگر متد تست ثبت شده باشد، false در غیر این صورت.

## موارد مرتبط

* ساختار [TestToolsExt](../)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)