---
title: operator!=()
second_title: Aspose.Slides برای مرجع API C++
description: تعیین می‌کند که آیا مقادیر نمایندهٔ شیء جاری و شیء مشخص‌شده برابر نیستند.
type: docs
weight: 157
url: /fa/system/decimal/operator_not_equal/
---
## Decimal::operator!=(const Decimal\&) const متد

تعیین می‌کند که آیا مقادیر نمایندهٔ شیء جاری و شیء مشخص‌شده برابر نیستند.

```cpp
bool System::Decimal::operator!=(const Decimal &d) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| d | const [Decimal](../)\& | شیء [Decimal](../) برای مقایسه با شیء جاری |

### مقدار بازگشت

درست در صورتی که مقدار نمایندهٔ شیء جاری و شیء مشخص‌شده برابر نباشد، در غیر این صورت - نادرست

## Decimal::operator!=(std::nullptr_t) const متد

تعیین می‌کند که آیا مقدار نمایندهٔ شیء جاری با ۰ متفاوت است.

```cpp
bool System::Decimal::operator!=(std::nullptr_t) const
```

### مقدار بازگشت

درست در صورتی که مقدار نمایندهٔ شیء جاری با ۰ متفاوت باشد، در غیر این صورت - نادرست

## موارد مرتبط

* کلاس [Decimal](../)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)