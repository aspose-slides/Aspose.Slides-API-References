---
title: operator-()
second_title: مرجع API Aspose.Slides برای C++
description: یک نمونهٔ جدید از کلاس Decimal برمی‌گرداند که مقداری را نشان می‌دهد که نتیجهٔ تفریق مقدار نمایان‌ساز شده توسط شیء مشخص‌شده از مقدار نمایان‌ساز شده توسط شیء جاری است.
type: docs
weight: 92
url: /fa/system/decimal/operator_minus/
---
## Decimal::operator-(const Decimal\&) const method

یک نمونهٔ جدید از کلاس [Decimal](../) را برمی‌گرداند که مقداری را نمایان می‌کند که نتیجهٔ تفریق مقدار نمایان‌ساز شده توسط شیء مشخص‌شده از مقدار نمایان‌ساز شده توسط شیء جاری است.

```cpp
Decimal System::Decimal::operator-(const Decimal &d) const
```

### Arguments

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| d | const [Decimal](../)\& | شیء [Decimal](../) که مقدار را برای تفریق نشان می‌دهد |

### Return Value

یک نمونهٔ جدید از کلاس [Decimal](../) را برمی‌گرداند که مقداری را نمایان می‌کند که نتیجهٔ تفریق مقدار نمایان‌ساز شده توسط شیء مشخص‌شده از مقدار نمایان‌ساز شده توسط شیء جاری است.

## Decimal::operator-() const method

یک نمونهٔ جدید از کلاس [Decimal](../) را برمی‌گرداند که مقداری را نمایان می‌کند که از منفی‌سازی مقدار نمایان‌ساز شده توسط شیء جاری به دست می‌آید.

```cpp
Decimal System::Decimal::operator-() const
```

## See Also

* کلاس [Decimal](../)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)