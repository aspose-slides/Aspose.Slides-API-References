---
title: Equals()
second_title: مرجع API Aspose.Slides برای C++
description: مشخص می‌کند که آیا مقادیر نمایان‌شده توسط شیء جاری و شیء مشخص‌شده برابر هستند یا خیر.
type: docs
weight: 313
url: /fa/system/decimal/equals/
---
## Decimal::Equals(const Decimal\&) const متد


مشخص می‌کند که آیا مقادیر نمایان‌شده توسط شیء جاری و شیء مشخص‌شده برابر هستند یا خیر.

```cpp
bool System::Decimal::Equals(const Decimal &d) const
```


### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| d | const [Decimal](../)\& | شیء [Decimal](../) برای مقایسه با شیء جاری |

### مقدار بازگشت

True if the value represented by the current and the specified objects are equal, otherwise - false

## Decimal::Equals(const SharedPtr\<Object\>\&) const متد


مشخص می‌کند که آیا مقادیر نمایان‌شده توسط شیء جاری و شیء مشخص‌شده برابر هستند یا خیر.

```cpp
bool System::Decimal::Equals(const SharedPtr<Object> &obj) const
```


### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | شیء برای مقایسه با شیء جاری |

### مقدار بازگشت

True if the value represented by the current and the specified objects are equal, otherwise - false

## Decimal::Equals(const Decimal\&, const Decimal\&) متد


مشخص می‌کند که آیا مقادیر نمایان‌شده توسط اشیاء مشخص‌شده برابر هستند یا خیر.

```cpp
static bool System::Decimal::Equals(const Decimal &d1, const Decimal &d2)
```


### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| d1 | const [Decimal](../)\& | مقایسه‌کننده اول |
| d2 | const [Decimal](../)\& | مقایسه‌کننده دوم |

### مقدار بازگشت

True if the values represented by **d1** and **d2** are equal, otherwise - false

## موارد مرتبط

* تعریف‌نوع [SharedPtr](../../sharedptr/)
* کلاس [Decimal](../)
* کلاس [Object](../../object/)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)