---
title: operator==()
second_title: Aspose.Slides برای مرجع API C++
description: تعیین می‌کند که آیا مقادیر نمایان‌شده توسط شیء جاری و شیء مشخص‌شده برابر هستند یا خیر.
type: docs
weight: 144
url: /fa/system/decimal/operator_equal_equal/
---
## Decimal::operator==(const Decimal\&) const method

تعیین می‌کند که آیا مقادیر نمایان‌شده توسط شیء جاری و شیء مشخص‌شده برابر هستند یا خیر.

```cpp
bool System::Decimal::operator==(const Decimal &d) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| d | const [Decimal](../)\& | شیء [Decimal](../) برای مقایسه با شیء جاری |

### مقدار بازگشت

True اگر مقدار نمایان‌شده توسط شیء جاری و شیء مشخص‌شده برابر باشد، در غیر این صورت - false

## Decimal::operator==(std::nullptr_t) const method

تعیین می‌کند که آیا مقدار نمایان‌شده توسط شیء جاری برابر ۰ است یا خیر.

```cpp
bool System::Decimal::operator==(std::nullptr_t) const
```

### مقدار بازگشت

True اگر مقدار نمایان‌شده توسط شیء جاری برابر ۰ باشد، در غیر این صورت - false

## موارد مرتبط

* کلاس [Decimal](../)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)