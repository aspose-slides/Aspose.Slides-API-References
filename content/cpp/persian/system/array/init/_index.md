---
title: Init()
second_title: Aspose.Slides برای مرجع API C++
description: آرایه‌ای که توسط شیء جاری نمایان می‌شود را با مقادیر آرایهٔ مشخص‌شده پر می‌کند.
type: docs
weight: 196
url: /fa/system/array/init/
---
## Array::Init(const T) متد

آرایه‌ای که توسط شیء جاری نمایان می‌شود را با مقادیر آرایهٔ مشخص‌شده پر می‌کند.

```cpp
ArrayPtr<T> System::Array<T>::Init(const T inits[])
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| inits | const T | آرایه‌ای که مقادیر از آن کپی می‌شود |

### مقدار بازگشت

یک اشاره‌گر مشترک که به شیء جاری اشاره می‌کند

## مراجع دیگر

* Typedef [ArrayPtr](../../arrayptr/)
* کلاس [Array](../)
* فضای نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)