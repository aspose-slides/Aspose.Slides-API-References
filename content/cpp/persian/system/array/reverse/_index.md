---
title: Reverse()
second_title: Aspose.Slides برای مرجع API C++
description: عناصر را در آرایه مشخص شده معکوس می‌کند.
type: docs
weight: 755
url: /fa/system/array/reverse/
---
## Array::Reverse(const ArrayPtr\<Type\>\&) متد

عناصر را در آرایه مشخص شده معکوس می‌کند.

```cpp
template<typename Type> static void System::Array<T>::Reverse(const ArrayPtr<Type> &arr)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| arr | const [ArrayPtr](../../arrayptr/)\<[Type](../../object/type/)\>\& | آرایه هدف |

## Array::Reverse(const ArrayPtr\<Type\>\&, int, int) متد

بخشی از عناصر را در آرایه مشخص شده معکوس می‌کند.

```cpp
template<typename Type> static void System::Array<T>::Reverse(const ArrayPtr<Type> &arr, int startIndex, int count)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| arr | const [ArrayPtr](../../arrayptr/)\<[Type](../../object/type/)\>\& | آرایه هدف |
| startIndex | int | [Index](../../index/) در آرایه که بازه برای معکوس شدن از آن شروع می‌شود |
| count | int | اندازهٔ بازه برای معکوس شدن |

## همچنین ببینید

* تعریف‌نوع [ArrayPtr](../../arrayptr/)
* متد [Type](../../object/type/)
* کلاس [Array](../)
* فضای‌نام [System](../../)
* کتابخانه [Aspose.Slides](../../../)