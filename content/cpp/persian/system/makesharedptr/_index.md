---
title: MakeSharedPtr()
second_title: مرجع API Aspose.Slides برای C++
description: پویینتر خام را به اشاره‌گر هوشمند تبدیل می‌کند.
type: docs
weight: 2900
url: /fa/system/makesharedptr/
---
## System::MakeSharedPtr(X *) تابع

پویینتر خام را به اشاره‌گر هوشمند تبدیل می‌کند.

```cpp
template<class X> SmartPtr<X> System::MakeSharedPtr(X *p)
```

### پارامترهای قالب

| پارامتر | توضیح |
| --- | --- |
| X | نوع اشاره‌گر. |

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| p | X * | پویینتر خام به شیء. |

### مقدار بازگشت

اشاره‌گر هوشمند مشترک به شیء.

## System::MakeSharedPtr(const X *) تابع

پویینتر خام را به اشاره‌گر هوشمند تبدیل می‌کند. نسخهٔ اضافه برای پویینترهای ثابت. به عنوان مثال هنگام استفاده از متغیر 'this' در متدهای C# که به صورت const ترجمه می‌شوند مفید است.

```cpp
template<class X> SmartPtr<X> System::MakeSharedPtr(const X *p)
```

### پارامترهای قالب

| پارامتر | توضیح |
| --- | --- |
| X | نوع اشاره‌گر. |

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| p | const X * | پویینتر خام به شیء. |

### مقدار بازگشت

اشاره‌گر هوشمند مشترک به شیء.

## موارد مرتبط

* کلاس [SmartPtr](../smartptr/)
* فضای نام [System](../)
* کتابخانه [Aspose.Slides](../../)