---
title: operator*()
second_title: مرجع API Aspose.Slides برای C++
description: یک نمونه جدید از کلاس Decimal را برمی‌گرداند که نمایانگر مقداری است که نتیجهٔ ضرب مقدار مشخص‌شده و مقداری است که توسط شیء Decimal مشخص‌شده نمایان می‌شود.
type: docs
weight: 2198
url: /fa/system/operator_star/
---
## System::operator*(const T\&, const Decimal\&) function


یک نمونهٔ جدید از کلاس [Decimal](../decimal/) باز می‌گرداند که نمایانگر مقداری است که نتیجهٔ ضرب مقدار مشخص‌شده و مقداری که توسط شیء [Decimal](../decimal/) مشخص‌شده نمایانده می‌شود.

```cpp
template<typename T,typename _> Decimal System::operator*(const T &x, const Decimal &d)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| x | const T\& | ضرب‌کنندهٔ اول |
| d | const [Decimal](../decimal/)\& | شیء [Decimal](../decimal/) که نمایانگر ضرب‌کنندهٔ دوم است |

### مقدار برگشتی

یک نمونهٔ جدید از کلاس [Decimal](../decimal/) که نمایانگر مقداری است که نتیجهٔ ضرب **x** و مقداری که توسط **d** نمایانده می‌شود.

## موارد مرتبط

* کلاس [Decimal](../decimal/)
* فضای نام [System](../)
* کتابخانه [Aspose.Slides](../../)