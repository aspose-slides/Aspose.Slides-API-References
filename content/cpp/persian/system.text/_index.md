---
title: "System::Text"
second_title: مرجع API Aspose.Slides برای C++
description: 
type: docs
weight: 976
url: /fa/system.text/
---
## کلاس‌ها

| کلاس | توضیح |
| --- | --- |
| [ASCIIEncoding](./asciiencoding/) | نمایش‌دهنده رمزگذاری ASCII است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [Decoder](./decoder/) | دنباله بایت‌های رمزگشایی را در دنباله کاراکترها محصور می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [DecoderExceptionFallback](./decoderexceptionfallback/) | استراتژی فالو بک پرتاب کننده استثنا را فراهم می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [DecoderExceptionFallbackBuffer](./decoderexceptionfallbackbuffer/) | [Buffer](../system/buffer/) برای استراتژی فالو بک پرتاب کننده استثنا در رمزگشایی. در واقع چیزی ذخیره نمی‌کند، اما به جای آن استثنا می‌اندازد. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [DecoderFallback](./decoderfallback/) | API فالو بک برای رسیدگی به خطای رمزگشایی را فراهم می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [DecoderFallbackBuffer](./decoderfallbackbuffer/) | بافر برای پیاده‌سازی فالو بک فراهم می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [DecoderReplacementFallback](./decoderreplacementfallback/) | استراتژی فالو بک جایگزینی نماد اشتباه با یک استاب را فراهم می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [DecoderReplacementFallbackBuffer](./decoderreplacementfallbackbuffer/) | [Buffer](../system/buffer/) برای جایگزینی استراتژی فالو بک رمزگشایی. |
| [Details_DecoderFallbackException](./details_decoderfallbackexception/) | استثنایی که توسط [DecoderExceptionFallback](./decoderexceptionfallback/) هنگام شکست رمزگشایی پرتاب می‌شود. هرگز نمونه‌های این کلاس را به صورت دستی ایجاد نکنید. به جای آن از کلاس DecoderFallbackException استفاده کنید. هرگز نمونه‌های کلاس DecoderFallbackException را در [System::SmartPtr](../system/smartptr/) بپیچانید. |
| [Details_EncoderFallbackException](./details_encoderfallbackexception/) | استثنایی که توسط [EncoderExceptionFallback](./encoderexceptionfallback/) هنگام شکست رمزگذاری پرتاب می‌شود. هرگز نمونه‌های این کلاس را به صورت دستی ایجاد نکنید. به جای آن از کلاس EncoderFallbackException استفاده کنید. هرگز نمونه‌های کلاس EncoderFallbackException را در [System::SmartPtr](../system/smartptr/) بپیچانید. |
| [Encoder](./encoder/) | دنباله کاراکترهای رمزگذاری را در دنباله بایت‌ها محصور می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [EncoderExceptionFallback](./encoderexceptionfallback/) | استراتژی فالو بک پرتاب کننده استثنا را فراهم می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [EncoderExceptionFallbackBuffer](./encoderexceptionfallbackbuffer/) | [Buffer](../system/buffer/) برای استراتژی فالو بک پرتاب کننده استثنا در رمزگذاری. در واقع چیزی ذخیره نمی‌کند، اما به جای آن استثنا می‌اندازد. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [EncoderFallback](./encoderfallback/) | API فالو بک برای رسیدگی به خطای رمزگذاری را فراهم می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [EncoderFallbackBuffer](./encoderfallbackbuffer/) | بافر برای پیاده‌سازی فالو بک فراهم می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [EncoderReplacementFallback](./encoderreplacementfallback/) | استراتژی فالو بک جایگزینی نماد اشتباه با یک استاب را فراهم می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [EncoderReplacementFallbackBuffer](./encoderreplacementfallbackbuffer/) | [Buffer](../system/buffer/) برای جایگزینی استراتژی فالو بک رمزگذاری. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [Encoding](./encoding/) | خدمات [Encoding](./encoding/). |
| [EncodingDecoder](./encodingdecoder/) | [Decoder](./decoder/) که از شیء رمزگذاری برای رمزگشایی استفاده می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [EncodingEncoder](./encodingencoder/) | [Encoder](./encoder/) که از شیء رمزگذاری برای رمزگذاری استفاده می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/اگر خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [EncodingInfo](./encodinginfo/) | اطلاعات مختصر درباره رمزگذاری. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [ICUDecoder](./icudecoder/) | [Decoder](./decoder/) که از ICU برای رمزگشایی استفاده می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [ICUEncoder](./icuencoder/) | [Encoder](./encoder/) که از ICU برای رمزگذاری استفاده می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [ICUEncoding](./icuencoding/) | پیاده‌سازی رمزگذاری مبتنی بر ICU. برای استفاده داخلی. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [Latin1Encoding](./latin1encoding/) | پشتیبانی از رمزگذاری Latin1. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [StringBuilder](./stringbuilder/) | [Buffer](../system/buffer/) برای تجمیع رشته به تدریج. این نوع می‌تواند یا روی پشته به عنوان نوع مقداری یا با استفاده از تابع [System::MakeObject()](../system/makeobject/) در heap اختصاص داده شود. پس از اختصاص شیء، هرگز این دو حالت استفاده را مخلوط نکنید: داشتن اشاره‌گرهای [SmartPtr](../system/smartptr/) به اشیاء اختصاص داده‌شده روی پشته به‌طور کامل ممنوع است. |
| [UnicodeEncoding](./unicodeencoding/) | رمزگذاری یونیکد. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [UTF32Encoding](./utf32encoding/) | رمزگذاری UTF-32. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [UTF7Encoding](./utf7encoding/) | رمزگذاری UTF-7. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [UTF8Encoding](./utf8encoding/) | رمزگذاری UTF-8. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اطمینان می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |

## شمارشی‌ها

| شمارشی | توضیح |
| --- | --- |
| [NormalizationForm](./normalizationform/) | نحوه نرمال‌سازی رشته‌ی یونیکد را تعریف می‌کند. |

## تعاریف نوع

| تعریف‌نوع | توضیح |
| --- | --- |
| [DecoderFallbackException](./decoderfallbackexception/) |  |
| [EncoderFallbackException](./encoderfallbackexception/) |  |
| [ICUEncodingPtr](./icuencodingptr/) | اشاره‌گر رمزگذاری ICU. |