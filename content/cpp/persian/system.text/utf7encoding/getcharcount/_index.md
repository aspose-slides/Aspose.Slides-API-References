---
title: GetCharCount()
second_title: ارجاع API Aspose.Slides برای C++
description: دریافت تعداد کاراکترهای مورد نیاز برای رمزگشایی یک بافر بایت.
type: docs
weight: 79
url: /fa/system.text/utf7encoding/getcharcount/
---
## UTF7Encoding::GetCharCount(ArrayPtr\<uint8_t\>, int, int) method

تعداد کاراکترهای مورد نیاز برای رمزگشایی یک بافر بایت را برمی‌گرداند.

```cpp
int System::Text::UTF7Encoding::GetCharCount(ArrayPtr<uint8_t> bytes, int index, int count) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | بایت‌های برای رمزگشایی. |
| index | int | آغاز برش. |
| count | int | اندازه برش. |

### مقدار بازگشت

تعداد کاراکترها.

## UTF7Encoding::GetCharCount(const uint8_t *, int) method

تعداد کاراکترهای مورد نیاز برای رمزگشایی یک بافر بایت را برمی‌گرداند.

```cpp
int System::Text::UTF7Encoding::GetCharCount(const uint8_t *bytes, int count) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| bytes | const **uint8_t** * | بایت‌های برای رمزگشایی. |
| count | int | تعداد بایت‌ها. |

### مقدار بازگشت

تعداد کاراکترها.

## UTF7Encoding::GetCharCount(ArrayPtr\<uint8_t\>, int, int) method

تعداد کاراکترهای مورد نیاز برای رمزگشایی یک بافر بایت را برمی‌گرداند.

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes, int index, int count)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | بایت‌های برای رمزگشایی. |
| index | int | آغاز برش. |
| count | int | اندازه برش. |

### مقدار بازگشت

تعداد کاراکترها.

## UTF7Encoding::GetCharCount(ArrayPtr\<uint8_t\>) method

تعداد کاراکترهای مورد نیاز برای رمزگشایی یک بافر بایت را برمی‌گرداند.

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | بایت‌های برای رمزگشایی. |

### مقدار بازگشت

تعداد کاراکترها.

## UTF7Encoding::GetCharCount(const uint8_t *, int) method

تعداد کاراکترهای مورد نیاز برای رمزگشایی یک بافر بایت را برمی‌گرداند.

```cpp
virtual int System::Text::Encoding::GetCharCount(const uint8_t *bytes, int count)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| bytes | const **uint8_t** * | بایت‌های برای رمزگشایی. |
| count | int | تعداد بایت‌ها. |

### مقدار بازگشت

تعداد کاراکترها.

## موارد مرتبط

* تعریف نوع [ArrayPtr](../../../system/arrayptr/)
* کلاس [UTF7Encoding](../)
* فضای نام [System::Text](../../)
* کتابخانه [Aspose.Slides](../../../)