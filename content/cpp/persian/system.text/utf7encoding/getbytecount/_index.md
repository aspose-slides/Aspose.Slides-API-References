---
title: GetByteCount()
second_title: مرجع API Aspose.Slides برای C++
description: تعداد نویسه‌های مورد نیاز برای رمزگذاری یک بافر کاراکتر را برمی‌گرداند.
type: docs
weight: 157
url: /fa/system.text/utf7encoding/getbytecount/
---
## UTF7Encoding::GetByteCount(const char_t *, int) متد

تعداد نویسه‌های مورد نیاز برای رمزگذاری یک بافر کاراکتر را برمی‌گرداند.

```cpp
int System::Text::UTF7Encoding::GetByteCount(const char_t *chars, int count) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| chars | const char_t * | بافر کاراکترها. |
| count | int | [Buffer](../../../system/buffer/) اندازه. |

### مقدار بازگشت

اندازه بافر مورد نیاز.

## UTF7Encoding::GetByteCount(ArrayPtr\<char_t\>, int, int) متد

تعداد نویسه‌های مورد نیاز برای رمزگذاری یک بافر کاراکتر را برمی‌گرداند.

```cpp
virtual int System::Text::Encoding::GetByteCount(ArrayPtr<char_t> chars, int index, int count)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| chars | [ArrayPtr](../../../system/arrayptr/)\<char_t\> | بافر کاراکترها. |
| index | int | شروع برش. |
| count | int | اندازه برش. |

### مقدار بازگشت

اندازه بافر مورد نیاز.

## UTF7Encoding::GetByteCount(System::Details::ArrayView\<char_t\>, int, int) متد

تعداد نویسه‌های مورد نیاز برای رمزگذاری یک بافر کاراکتر را برمی‌گرداند.

```cpp
virtual int System::Text::Encoding::GetByteCount(System::Details::ArrayView<char_t> chars, int index, int count)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| chars | System::Details::ArrayView\<char_t\> | بافر کاراکترها. |
| index | int | شروع برش. |
| count | int | اندازه برش. |

### مقدار بازگشت

اندازه بافر مورد نیاز.

## UTF7Encoding::GetByteCount(const System::Details::StackArray\<char_t, N\>\&, int, int) متد

تعداد نویسه‌های مورد نیاز برای رمزگذاری یک بافر کاراکتر را برمی‌گرداند.

```cpp
template<std::size_t> int System::Text::Encoding::GetByteCount(const System::Details::StackArray<char_t, N> &chars, int index, int count)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| chars | const System::Details::StackArray\<char_t, N\>\& | بافر کاراکترها. |
| index | int | شروع برش. |
| count | int | اندازه برش. |

### مقدار بازگشت

اندازه بافر مورد نیاز.

## UTF7Encoding::GetByteCount(const String\&) متد

تعداد نویسه‌های مورد نیاز برای رمزگذاری یک رشته را برمی‌گرداند.

```cpp
virtual int System::Text::Encoding::GetByteCount(const String &s)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| s | const [String](../../../system/string/)\& | [String](../../../system/string/) برای رمزگذاری. |

### مقدار بازگشت

اندازه بافر مورد نیاز.

## UTF7Encoding::GetByteCount(ArrayPtr\<char_t\>) متد

تعداد نویسه‌های مورد نیاز برای رمزگذاری یک بافر کاراکتر را برمی‌گرداند.

```cpp
virtual int System::Text::Encoding::GetByteCount(ArrayPtr<char_t> chars)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| chars | [ArrayPtr](../../../system/arrayptr/)\<char_t\> | بافر کاراکترها. |

### مقدار بازگشت

اندازه بافر مورد نیاز.

## UTF7Encoding::GetByteCount(const char_t *, int) متد

تعداد نویسه‌های مورد نیاز برای رمزگذاری یک بافر کاراکتر را برمی‌گرداند.

```cpp
virtual int System::Text::Encoding::GetByteCount(const char_t *chars, int count)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| chars | const char_t * | بافر کاراکترها. |
| count | int | [Buffer](../../../system/buffer/) اندازه. |

### مقدار بازگشت

اندازه بافر مورد نیاز.

## موارد مرتبط

* Typedef [ArrayPtr](../../../system/arrayptr/)
* Class [UTF7Encoding](../)
* Class [String](../../../system/string/)
* Namespace [System::Text](../../)
* Library [Aspose.Slides](../../../)