---
title: GetCharCount()
second_title: Aspose.Slides برای مرجع API C++
description: تعداد کاراکترهای مورد نیاز برای رمزگشایی یک بافر بایت را بر می‌گرداند.
type: docs
weight: 261
url: /fa/system.text/encoding/getcharcount/
---
## Encoding::GetCharCount(ArrayPtr\<uint8_t\>, int, int) متد


تعداد کاراکترهای مورد نیاز برای رمزگشایی یک بافر بایت را بر می‌گرداند.

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes, int index, int count)
```


### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | بایت‌های ورودی برای رمزگشایی. |
| index | int | شروع برش. |
| count | int | اندازه برش. |

### مقدار بازگشت

تعداد کاراکترها.

## Encoding::GetCharCount(ArrayPtr\<uint8_t\>) متد


تعداد کاراکترهای مورد نیاز برای رمزگشایی یک بافر بایت را بر می‌گرداند.

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes)
```


### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | بایت‌های ورودی برای رمزگشایی. |

### مقدار بازگشت

تعداد کاراکترها.

## Encoding::GetCharCount(const uint8_t *, int) متد


تعداد کاراکترهای مورد نیاز برای رمزگشایی یک بافر بایت را بر می‌گرداند.

```cpp
virtual int System::Text::Encoding::GetCharCount(const uint8_t *bytes, int count)
```


### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| bytes | const **uint8_t** * | بایت‌های ورودی برای رمزگشایی. |
| count | int | تعداد بایت‌ها. |

### مقدار بازگشت

تعداد کاراکترها.

## موارد مرتبط

* Typedef [ArrayPtr](../../../system/arrayptr/)
* Class [Encoding](../)
* Namespace [System::Text](../../)
* Library [Aspose.Slides](../../../)