---
title: Replace()
second_title: مرجع API Aspose.Slides برای C++
description: زیررشته را از طریق سازنده جایگزین می‌کند.
type: docs
weight: 196
url: /fa/system.text/stringbuilder/replace/
---
## StringBuilder::Replace(const String\&, const String\&) متد

زیررشته را از طریق سازنده جایگزین می‌کند.

```cpp
StringBuilder * System::Text::StringBuilder::Replace(const String &oldString, const String &newString)
```

### پارامترها

| Parameter | Type | Description |
| --- | --- | --- |
| oldString | const [String](../../../system/string/)\& | [String](../../../system/string/) برای جایگزینی. |
| newString | const [String](../../../system/string/)\& | رشته جایگزین. |

### مقدار بازگشت

این اشاره‌گر.

## StringBuilder::Replace(const String\&, const String\&, int, int) متد

زیررشته را در بازهٔ سازنده جایگزین می‌کند.

```cpp
StringBuilder * System::Text::StringBuilder::Replace(const String &oldString, const String &newString, int position, int count)
```

### پارامترها

| Parameter | Type | Description |
| --- | --- | --- |
| oldString | const [String](../../../system/string/)\& | [String](../../../system/string/) برای جایگزینی. |
| newString | const [String](../../../system/string/)\& | رشته جایگزین. |
| position | int | موقعیت شروع بازهٔ جایگزینی سازنده. |
| count | int | طول بازهٔ جایگزینی سازنده. |

### مقدار بازگشت

این اشاره‌گر.

## StringBuilder::Replace(char_t, char_t) متد

کاراکتر را از طریق سازنده جایگزین می‌کند.

```cpp
StringBuilder * System::Text::StringBuilder::Replace(char_t oldChar, char_t newChar)
```

### پارامترها

| Parameter | Type | Description |
| --- | --- | --- |
| oldChar | char_t | کاراکتر برای جایگزینی. |
| newChar | char_t | کاراکتر جایگزین. |

### مقدار بازگشت

این اشاره‌گر.

## StringBuilder::Replace(char_t, char_t, int, int) متد

کاراکتر را در بازهٔ سازنده جایگزین می‌کند.

```cpp
StringBuilder * System::Text::StringBuilder::Replace(char_t oldChar, char_t newChar, int startIndex, int count)
```

### پارامترها

| Parameter | Type | Description |
| --- | --- | --- |
| oldChar | char_t | کاراکتر برای جایگزینی. |
| newChar | char_t | کاراکتر جایگزین. |
| startIndex | int | موقعیت شروع بازهٔ جایگزینی سازنده. |
| count | int | طول بازهٔ جایگزینی سازنده. |

### مقدار بازگشت

این اشاره‌گر.

## موارد مرتبط

* کلاس [StringBuilder](../)
* کلاس [String](../../../system/string/)
* فضای نام [System::Text](../../)
* کتابخانه [Aspose.Slides](../../../)