---
title: GetCertHashString()
second_title: مرجع API Aspose.Slides برای C++
description: هش SHA1 را برای شیء فعلی به‌صورت رشتهٔ هگزادسیمال دریافت می‌کند.
type: docs
weight: 92
url: /fa/system.security.cryptography.x509certificates/x509certificate/getcerthashstring/
---
## X509Certificate::GetCertHashString() const متد

هش [SHA1](../../../system.security.cryptography/sha1/) را برای شیء فعلی به‌صورت رشتهٔ هگزادسیمال دریافت می‌کند.

```cpp
virtual String System::Security::Cryptography::X509Certificates::X509Certificate::GetCertHashString() const
```

### مقدار بازگشت

رشتهٔ هگزادسیمال.

## X509Certificate::GetCertHashString(const HashAlgorithmName\&) const متد

هش [SHA1](../../../system.security.cryptography/sha1/) را برای شیء فعلی به‌صورت رشتهٔ هگزادسیمال دریافت می‌کند.

```cpp
virtual String System::Security::Cryptography::X509Certificates::X509Certificate::GetCertHashString(const HashAlgorithmName &hash_algorithm) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| hash_algorithm | const [HashAlgorithmName](../../../system.security.cryptography/hashalgorithmname/)\& | نام الگوریتم هش. |

### مقدار بازگشت

رشتهٔ هگزادسیمال.

## موارد مرتبط

* کلاس [String](../../../system/string/)
* کلاس [X509Certificate](../)
* ساختار [HashAlgorithmName](../../../system.security.cryptography/hashalgorithmname/)
* فضای نام [System::Security::Cryptography::X509Certificates](../../)
* کتابخانه [Aspose.Slides](../../../)