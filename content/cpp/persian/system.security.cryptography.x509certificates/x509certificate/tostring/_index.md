---
title: ToString()
second_title: مرجع API Aspose.Slides برای C++
description: اطلاعات گواهی را به صورت متنی برمی‌گرداند.
type: docs
weight: 339
url: /fa/system.security.cryptography.x509certificates/x509certificate/tostring/
---
## X509Certificate::ToString(bool) const متد

اطلاعات گواهی را به صورت متنی برمی‌گرداند.

```cpp
virtual String System::Security::Cryptography::X509Certificates::X509Certificate::ToString(bool verbose) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| verbose | **bool** | true برای نمایش اطلاعات اضافی گواهی. |

### مقدار بازگشتی

اطلاعات گواهی.

## X509Certificate::ToString() const متد

اطلاعات گواهی را به صورت متنی برمی‌گرداند.

```cpp
String System::Security::Cryptography::X509Certificates::X509Certificate::ToString() const override
```

### مقدار بازگشتی

اطلاعات گواهی.

## موارد مرتبط

* کلاس [String](../../../system/string/)
* کلاس [X509Certificate](../)
* فضای‌نام [System::Security::Cryptography::X509Certificates](../../)
* کتابخانه [Aspose.Slides](../../../)