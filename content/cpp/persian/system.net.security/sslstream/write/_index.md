---
title: Write()
second_title: Aspose.Slides برای C++ مرجع API
description: آرایه بایت مشخص‌شده را به جریان می‌نویسد.
type: docs
weight: 404
url: /fa/system.net.security/sslstream/write/
---
## SslStream::Write(const ArrayPtr\<uint8_t\>\&) متد

آرایه بایت مشخص شده را به جریان می‌نویسد.

```cpp
void System::Net::Security::SslStream::Write(const ArrayPtr<uint8_t> &buffer)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\>\& | آرایه بایتی که باید نوشته شود. |

## SslStream::Write(const ArrayPtr\<uint8_t\>\&, int32_t, int32_t) متد

بخش زیرمجموعه مشخص شده‌ای از بایت‌ها را از آرایه بایت مشخص شده به جریان می‌نویسد.

```cpp
void System::Net::Security::SslStream::Write(const ArrayPtr<uint8_t> &buffer, int32_t offset, int32_t count) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\>\& | آرایه‌ای که بایت‌های مورد نوشتن را دربر دارد |
| offset | **int32_t** | یک اندیس صفر-پایه از عنصر در **buffer** که بخش زیرمجموعه برای نوشتن از آن شروع می‌شود |
| count | **int32_t** | تعداد عناصر در بخش زیرمجموعه‌ای که باید نوشته شود |

## SslStream::Write(const System::Details::ArrayView\<uint8_t\>\&) متد

آرایه بایت مشخص شده را به جریان می‌نویسد.

```cpp
void System::Net::Security::SslStream::Write(const System::Details::ArrayView<uint8_t> &buffer)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| buffer | const System::Details::ArrayView\<**uint8_t**\>\& | آرایه بایتی که باید نوشته شود. |

## SslStream::Write(const System::Details::ArrayView\<uint8_t\>\&, int32_t, int32_t) متد

بخش زیرمجموعه مشخص شده‌ای از بایت‌ها را از آرایه بایت مشخص شده به جریان می‌نویسد.

```cpp
void System::Net::Security::SslStream::Write(const System::Details::ArrayView<uint8_t> &buffer, int32_t offset, int32_t count) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| buffer | const System::Details::ArrayView\<**uint8_t**\>\& | آرایه‌ای که بایت‌های مورد نوشتن را دربر دارد |
| offset | **int32_t** | یک اندیس صفر-پایه از عنصر در **buffer** که بخش زیرمجموعه برای نوشتن از آن شروع می‌شود |
| count | **int32_t** | تعداد عناصر در بخش زیرمجموعه‌ای که باید نوشته شود |

## همچنین

* تعریف نوع [ArrayPtr](../../../system/arrayptr/)
* کلاس [SslStream](../)
* فضای‌نام [System::Net::Security](../../)
* کتابخانه [Aspose.Slides](../../../)