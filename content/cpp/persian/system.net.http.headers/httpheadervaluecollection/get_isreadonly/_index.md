---
title: get_IsReadOnly()
second_title: Aspose.Slides برای مرجع API C++
description: یک مقدار را برمی‌گرداند که نشان می‌دهد آیا مجموعهٔ فعلی فقط-خواندنی است.
type: docs
weight: 14
url: /fa/system.net.http.headers/httpheadervaluecollection/get_isreadonly/
---
## HttpHeaderValueCollection::get_IsReadOnly() متد

یک مقدار را برمی‌گرداند که نشان می‌دهد آیا مجموعهٔ فعلی فقط-خواندنی است.

```cpp
bool System::Net::Http::Headers::HttpHeaderValueCollection<T>::get_IsReadOnly()
```

### مقدار بازگشت

مقداری که نشان می‌دهد آیا مجموعهٔ فعلی فقط-خواندنی است.

## موارد مرتبط

* کلاس [HttpHeaderValueCollection](../)
* فضای نام [System::Net::Http::Headers](../../)
* کتابخانه [Aspose.Slides](../../../)