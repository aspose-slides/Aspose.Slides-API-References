---
title: get_FileNameStar()
second_title: مرجع API Aspose.Slides برای C++
description: مقداری را برمی‌گرداند که تعیین می‌کند چگونه نام‌های فایل برای ذخیره‌سازی بار پیام ساخته شوند. این مقدار زمانی استفاده می‌شود که موجودیت‌ها جدا شده و در فایل‌های جداگانه ذخیره شوند.
type: docs
weight: 92
url: /fa/system.net.http.headers/contentdispositionheadervalue/get_filenamestar/
---
## ContentDispositionHeaderValue::get_FileNameStar() متد

یک مقدار را برمی‌گرداند که تعیین می‌کند چگونگی ساخت نام‌های فایل برای ذخیره‌سازی بار پیام. این مقدار زمانی استفاده می‌شود که موجودیت‌ها جداسازی شده و در فایل‌های جداگانه ذخیره شوند.

```cpp
String System::Net::Http::Headers::ContentDispositionHeaderValue::get_FileNameStar()
```

### مقدار بازگشت

مقداری که تعیین می‌کند چطور نام‌های فایل برای ذخیره‌سازی بار پیام ساخته شوند.

## موارد مرتبط

* کلاس [String](../../../system/string/)
* کلاس [ContentDispositionHeaderValue](../)
* فضای‌نام [System::Net::Http::Headers](../../)
* کتابخانه [Aspose.Slides](../../../)