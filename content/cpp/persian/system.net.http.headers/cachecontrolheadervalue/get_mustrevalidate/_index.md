---
title: get_MustRevalidate()
second_title: Aspose.Slides برای C++ مرجع API
description: مقدار را برمی‌گرداند که تعیین می‌کند آیا سرور برای یک ورودی کش زمانی که منسوخ می‌شود، به اعتبارسنجی مجدد نیاز دارد.
type: docs
weight: 313
url: /fa/system.net.http.headers/cachecontrolheadervalue/get_mustrevalidate/
---
## CacheControlHeaderValue::get_MustRevalidate() متد


مقداری را برمی‌گرداند که تعیین می‌کند آیا سرور برای یک ورودی کش هنگامی که منسوخ می‌شود، به اعتبارسنجی مجدد نیاز دارد.

```cpp
bool System::Net::Http::Headers::CacheControlHeaderValue::get_MustRevalidate()
```


### مقدار بازگشتی

مقداری که تعیین می‌کند آیا سرور برای یک ورودی کش هنگامی که منسوخ می‌شود، به اعتبارسنجی مجدد نیاز دارد.

## موارد مرتبط

* کلاس [CacheControlHeaderValue](../)
* فضای نام [System::Net::Http::Headers](../../)
* کتابخانه [Aspose.Slides](../../../)