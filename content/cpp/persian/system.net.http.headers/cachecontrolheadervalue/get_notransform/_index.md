---
title: get_NoTransform()
second_title: مرجع API Aspose.Slides برای C++
description: مقداری را بر می‌گرداند که تعیین می‌کند آیا یک cache یا proxy نباید هیچ بخشی از بدنهٔ موجودیت را تغییر دهد.
type: docs
weight: 196
url: /fa/system.net.http.headers/cachecontrolheadervalue/get_notransform/
---
## CacheControlHeaderValue::get_NoTransform() متد

مقداری را برمی‌گرداند که تعیین می‌کند آیا یک cache یا proxy نباید هیچ بخشی از بدنهٔ موجودیت را تغییر دهد.

```cpp
bool System::Net::Http::Headers::CacheControlHeaderValue::get_NoTransform()
```

### مقدار بازگشتی

مقداری را برمی‌گرداند که تعیین می‌کند آیا یک cache یا proxy نباید هیچ بخشی از بدنهٔ موجودیت را تغییر دهد.

## موارد مرتبط

* کلاس [CacheControlHeaderValue](../)
* فضای‌نام [System::Net::Http::Headers](../../)
* کتابخانه [Aspose.Slides](../../../)