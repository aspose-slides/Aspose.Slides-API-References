---
title: get_Private()
second_title: Aspose.Slides برای C++ مرجع API
description: مقداری را برمی‌گرداند که تعیین می‌کند آیا پیام پاسخ HTTP یا بخشی از آن برای یک کاربر منفرد در نظر گرفته شده است و نباید توسط کش مشترک ذخیره شود.
type: docs
weight: 274
url: /fa/system.net.http.headers/cachecontrolheadervalue/get_private/
---
## CacheControlHeaderValue::get_Private() متد

مقداری را برمی‌گرداند که تعیین می‌کند آیا پیام پاسخ HTTP یا بخشی از آن برای یک کاربر منفرد در نظر گرفته شده است و نباید توسط کش مشترک ذخیره شود.

```cpp
bool System::Net::Http::Headers::CacheControlHeaderValue::get_Private()
```

### مقدار برگشتی

مقداری را برمی‌گرداند که تعیین می‌کند آیا پیام پاسخ HTTP یا بخشی از آن برای یک کاربر منفرد در نظر گرفته شده است و نباید توسط کش مشترک ذخیره شود.

## موارد مرتبط

* کلاس [CacheControlHeaderValue](../)
* فضای نام [System::Net::Http::Headers](../../)
* کتابخانه [Aspose.Slides](../../../)