---
title: set_MustRevalidate()
second_title: Aspose.Slides برای C++ مرجع API
description: مقداری که تعیین می‌کند آیا سرور هنگامیکه یک ورودی کش منقضی می‌شود، نیاز به اعتبارسنجی مجدد دارد، را تنظیم می‌کند.
type: docs
weight: 326
url: /fa/system.net.http.headers/cachecontrolheadervalue/set_mustrevalidate/
---
## CacheControlHeaderValue::set_MustRevalidate(bool) متد

Sets the value that determines if the server requires revalidation of a cache entry when the it becomes stale.

```cpp
void System::Net::Http::Headers::CacheControlHeaderValue::set_MustRevalidate(bool value)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **bool** | مقداری که باید تنظیم شود. |

## موارد مرتبط

* کلاس [CacheControlHeaderValue](../)
* فضای نام [System::Net::Http::Headers](../../)
* کتابخانه [Aspose.Slides](../../../)