---
title: get_SharedMaxAge()
second_title: Aspose.Slides برای C++ مرجع API
description: مقدار حداکثر سن مشترک بر حسب ثانیه را دریافت می‌کند که دستور 'max-age' در هدر 'Cache-Control' یا هدر 'Expires' را برای یک کش مشترک نادیده می‌گیرد.
type: docs
weight: 92
url: /fa/system.net.http.headers/cachecontrolheadervalue/get_sharedmaxage/
---
## CacheControlHeaderValue::get_SharedMaxAge() متد

مقدار حداکثر سن مشترک بر حسب ثانیه را دریافت می‌کند که دستور 'max-age' در هدر 'Cache-Control' یا هدر 'Expires' را برای یک کش مشترک نادیده می‌گیرد.

```cpp
Nullable<TimeSpan> System::Net::Http::Headers::CacheControlHeaderValue::get_SharedMaxAge()
```

### مقدار بازگشتی

مقدار حداکثر سن مشترک بر حسب ثانیه که دستور 'max-age' در هدر 'Cache-Control' یا هدر 'Expires' را برای یک کش مشترک نادیده می‌گیرد.

## موارد مرتبط

* کلاس [Nullable](../../../system/nullable/)
* کلاس [TimeSpan](../../../system/timespan/)
* کلاس [CacheControlHeaderValue](../)
* فضای نام [System::Net::Http::Headers](../../)
* کتابخانه [Aspose.Slides](../../../)