---
title: get_OnlyIfCached()
second_title: Aspose.Slides برای مرجع API C++
description: مقداری را دریافت می‌کند که تعیین می‌کند آیا کلاینت باید فقط از ورودی‌های کش‌شده استفاده کند.
type: docs
weight: 222
url: /fa/system.net.http.headers/cachecontrolheadervalue/get_onlyifcached/
---
## CacheControlHeaderValue::get_OnlyIfCached() متد


مقداری را که تعیین می‌کند آیا کلاینت باید فقط از ورودی‌های کش‌شده استفاده کند، دریافت می‌کند.

```cpp
bool System::Net::Http::Headers::CacheControlHeaderValue::get_OnlyIfCached()
```


### مقدار بازگشت

مقداری که تعیین می‌کند آیا کلاینت باید فقط از ورودی‌های کش‌شده استفاده کند.

## موارد مرتبط

* کلاس [CacheControlHeaderValue](../)
* فضای نام [System::Net::Http::Headers](../../)
* کتابخانه [Aspose.Slides](../../../)