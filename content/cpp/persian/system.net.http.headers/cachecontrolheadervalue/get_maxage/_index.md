---
title: get_MaxAge()
second_title: مرجع API Aspose.Slides برای C++
description: حداکثر مقدار سن بر حسب ثانیه را که زمان محدودی را مشخص می‌کند که مشتری پاسخ را می‌پذیرد، دریافت می‌کند.
type: docs
weight: 66
url: /fa/system.net.http.headers/cachecontrolheadervalue/get_maxage/
---
## CacheControlHeaderValue::get_MaxAge() متد


حداکثر مقدار سن بر حسب ثانیه را که زمان محدودی را مشخص می‌کند که مشتری پاسخ را می‌پذیرد، دریافت می‌کند.

```cpp
Nullable<TimeSpan> System::Net::Http::Headers::CacheControlHeaderValue::get_MaxAge()
```


### مقدار بازگشت

حداکثر مقدار سن بر حسب ثانیه را که زمان محدودی را مشخص می‌کند که مشتری پاسخ را می‌پذیرد، دریافت می‌کند.

## موارد مرتبط

* کلاس [Nullable](../../../system/nullable/)
* کلاس [TimeSpan](../../../system/timespan/)
* کلاس [CacheControlHeaderValue](../)
* فضای‌نام [System::Net::Http::Headers](../../)
* کتابخانه [Aspose.Slides](../../../)