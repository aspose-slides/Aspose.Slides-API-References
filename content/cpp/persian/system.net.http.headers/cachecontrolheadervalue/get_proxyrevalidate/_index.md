---
title: get_ProxyRevalidate()
second_title: Aspose.Slides برای C++ مرجع API
description: مقداری را دریافت می‌کند که تعیین می‌کند آیا سرور برای ورودی کش زمانی که برای کش‌های کارگزار مشترک قدیمی می‌شود، نیاز به بازاعتبارسازی دارد یا خیر.
type: docs
weight: 339
url: /fa/system.net.http.headers/cachecontrolheadervalue/get_proxyrevalidate/
---
## CacheControlHeaderValue::get_ProxyRevalidate() متد


مقداری را دریافت می‌کند که تعیین می‌کند آیا سرور برای ورود به کش زمانی که برای کش‌های کارگزار مشترک قدیمی می‌شود، نیاز به بازاعتبارسازی دارد یا خیر.

```cpp
bool System::Net::Http::Headers::CacheControlHeaderValue::get_ProxyRevalidate()
```


### مقدار بازگشت

مقداری که تعیین می‌کند آیا سرور برای ورود به کش زمانی که برای کش‌های کارگزار مشترک منقضی می‌شود، نیاز به بازاعتبارسازی دارد یا خیر.

## مراجع

* کلاس [CacheControlHeaderValue](../)
* فضای نام [System::Net::Http::Headers](../../)
* کتابخانه [Aspose.Slides](../../../)