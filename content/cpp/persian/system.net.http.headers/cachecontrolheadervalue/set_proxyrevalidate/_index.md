---
title: set_ProxyRevalidate()
second_title: مرجع API Aspose.Slides برای C++
description: مقدار را تنظیم می‌کند که تعیین می‌کند آیا سرور نیاز به اعتبارسنجی مجدد یک ورودی کش دارد زمانی که برای کش‌های مشترک کارگزار کاربر منقضی می‌شود.
type: docs
weight: 352
url: /fa/system.net.http.headers/cachecontrolheadervalue/set_proxyrevalidate/
---
## CacheControlHeaderValue::set_ProxyRevalidate(bool) متد

مقداری را تنظیم می‌کند که تعیین می‌کند آیا سرور وقتی ورودی کش برای کش‌های مشترک کاربرعامل منقضی می‌شود، به اعتبارسنجی مجدد نیاز دارد.

```cpp
void System::Net::Http::Headers::CacheControlHeaderValue::set_ProxyRevalidate(bool value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **bool** | مقداری که باید تنظیم شود. |

## موارد مرتبط

* کلاس [CacheControlHeaderValue](../)
* فضای نام [System::Net::Http::Headers](../../)
* کتابخانه [Aspose.Slides](../../../)