---
title: idx_get()
second_title: مرجع API Aspose.Slides برای C++
description: مقدار هدر را با استفاده از هدر درخواست مشخص‌شده دریافت می‌کند.
type: docs
weight: 14
url: /fa/system.net/webheadercollection/idx_get/
---
## WebHeaderCollection::idx_get(HttpRequestHeader) متد


مقدار هدر را با استفاده از هدر درخواست مشخص شده دریافت می‌کند.

```cpp
String System::Net::WebHeaderCollection::idx_get(HttpRequestHeader header)
```


### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| header | [HttpRequestHeader](../../httprequestheader/) | هدر. |

### مقدار بازگشتی

مقدار هدر را با استفاده از هدر مشخص شده دریافت می‌کند.

## WebHeaderCollection::idx_get(HttpResponseHeader) متد


مقدار هدر را با استفاده از هدر پاسخ مشخص شده دریافت می‌کند.

```cpp
String System::Net::WebHeaderCollection::idx_get(HttpResponseHeader header)
```


### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| header | [HttpResponseHeader](../../httpresponseheader/) | هدر. |

### مقدار بازگشتی

مقدار هدر را با استفاده از هدر مشخص شده دریافت می‌کند.

## WebHeaderCollection::idx_get(String) متد


مقدار هدر را با استفاده از نام هدر مشخص شده دریافت می‌کند.

```cpp
String System::Net::WebHeaderCollection::idx_get(String name)
```


### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| name | [String](../../../system/string/) | نام هدر. |

### مقدار بازگشتی

مقدار هدر را با استفاده از نام هدر مشخص شده دریافت می‌کند.

## همچنین ببینید

* Enum [HttpRequestHeader](../../httprequestheader/)
* Enum [HttpResponseHeader](../../httpresponseheader/)
* Class [String](../../../system/string/)
* Class [WebHeaderCollection](../)
* Namespace [System::Net](../../)
* Library [Aspose.Slides](../../../)