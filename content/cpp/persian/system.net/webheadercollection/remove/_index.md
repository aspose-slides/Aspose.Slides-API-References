---
title: Remove()
second_title: Aspose.Slides برای C++ مرجع API
description: سرصفحه را بر اساس نام سرصفحهٔ مشخص‌شده حذف می‌کند.
type: docs
weight: 40
url: /fa/system.net/webheadercollection/remove/
---
## WebHeaderCollection::Remove(String) متد

سرصفحه را با نام سرصفحهٔ مشخص‌شده حذف می‌کند.

```cpp
void System::Net::WebHeaderCollection::Remove(String name)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| name | [String](../../../system/string/) | نام سرصفحه. |

## WebHeaderCollection::Remove(HttpResponseHeader) متد

سرصفحهٔ پاسخ مشخص‌شده را حذف می‌کند.

```cpp
void System::Net::WebHeaderCollection::Remove(HttpResponseHeader header)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| header | [HttpResponseHeader](../../httpresponseheader/) | سرصفحه‌ای که باید حذف شود. |

## WebHeaderCollection::Remove(HttpRequestHeader) متد

سرصفحهٔ درخواست مشخص‌شده را حذف می‌کند.

```cpp
void System::Net::WebHeaderCollection::Remove(HttpRequestHeader header)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| header | [HttpRequestHeader](../../httprequestheader/) | سرصفحه‌ای که باید حذف شود. |

## موارد مرتبط

* Enum [HttpResponseHeader](../../httpresponseheader/)
* Enum [HttpRequestHeader](../../httprequestheader/)
* کلاس [String](../../../system/string/)
* کلاس [WebHeaderCollection](../)
* فضای‌نام [System::Net](../../)
* Library [Aspose.Slides](../../../)