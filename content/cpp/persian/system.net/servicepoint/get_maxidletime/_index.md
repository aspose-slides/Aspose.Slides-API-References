---
title: get_MaxIdleTime()
second_title: Aspose.Slides برای مرجع API C++
description: یک مقدار زمان به میلی‌ثانیه دریافت می‌کند که پس از آن یک اتصال بیکار بسته می‌شود.
type: docs
weight: 66
url: /fa/system.net/servicepoint/get_maxidletime/
---
## ServicePoint::get_MaxIdleTime() متد

زمانی به میلی‌ثانیه دریافت می‌کند که پس از آن یک اتصال بیکار بسته می‌شود.

```cpp
int32_t System::Net::ServicePoint::get_MaxIdleTime()
```

### مقدار بازگشت

یک مقدار زمان به میلی‌ثانیه که پس از آن یک اتصال بیکار بسته می‌شود.

## مراجع

* کلاس [ServicePoint](../)
* فضای‌نام [System::Net](../../)
* کتابخانه [Aspose.Slides](../../../)