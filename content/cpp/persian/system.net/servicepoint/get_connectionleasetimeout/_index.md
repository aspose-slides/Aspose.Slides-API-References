---
title: get_ConnectionLeaseTimeout()
second_title: Aspose.Slides برای C++ مرجع API
description: یک زمان‌به‌مسیر بر حسب میلی‌ثانیه دریافت می‌کند که پس از آن ServicePoint فعال بسته می‌شود.
type: docs
weight: 27
url: /fa/system.net/servicepoint/get_connectionleasetimeout/
---
## ServicePoint::get_ConnectionLeaseTimeout() متد

انقضایی را برحسب میلی‌ثانیه دریافت می‌کند که پس از آن [ServicePoint](../) فعال بسته می‌شود.

```cpp
int32_t System::Net::ServicePoint::get_ConnectionLeaseTimeout()
```

### مقدار بازگشت

یک انقضا برحسب میلی‌ثانیه که پس از آن [ServicePoint](../) فعال بسته می‌شود.

## موارد مرتبط

* کلاس [ServicePoint](../)
* فضای‌نام [System::Net](../../)
* کتابخانه [Aspose.Slides](../../../)