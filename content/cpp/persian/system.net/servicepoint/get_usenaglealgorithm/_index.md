---
title: get_UseNagleAlgorithm()
second_title: مرجع API Aspose.Slides برای C++
description: مقداری را برمی‌گرداند که نشان می‌دهد آیا الگوریتم Nagle توسط اتصالات مدیریت‌شده توسط نمونهٔ فعلی استفاده می‌شود.
type: docs
weight: 92
url: /fa/system.net/servicepoint/get_usenaglealgorithm/
---
## ServicePoint::get_UseNagleAlgorithm() متد

مقداری را برمی‌گرداند که نشان می‌دهد آیا الگوریتم Nagle توسط اتصالات مدیریت‌شده توسط نمونهٔ فعلی استفاده می‌شود یا خیر.

```cpp
bool System::Net::ServicePoint::get_UseNagleAlgorithm()
```

### مقدار بازگشت

مقداری که نشان می‌دهد آیا الگوریتم Nagle توسط اتصالات مدیریت‌شده توسط نمونهٔ فعلی استفاده می‌شود یا خیر.

## همچنین ببینید

* کلاس [ServicePoint](../)
* فضای‌نام [System::Net](../../)
* کتابخانه [Aspose.Slides](../../../)