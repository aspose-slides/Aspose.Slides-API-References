---
title: WebProxy()
second_title: مرجع API Aspose.Slides برای C++
description: یک نمونه جدید می‌سازد.
type: docs
weight: 131
url: /fa/system.net/webproxy/webproxy/
---
## WebProxy::WebProxy() سازنده


یک نمونه جدید می‌سازد.

```cpp
System::Net::WebProxy::WebProxy()
```

## WebProxy::WebProxy(System::SharedPtr\<Uri\>) سازنده


یک نمونه جدید می‌سازد.

```cpp
System::Net::WebProxy::WebProxy(System::SharedPtr<Uri> Address)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| Address | [System::SharedPtr](../../../system/sharedptr/)\<[Uri](../../../system/uri/)\> | آدرس سرور پروکسی. |

## WebProxy::WebProxy(System::SharedPtr\<Uri\>, bool) سازنده


یک نمونه جدید می‌سازد.

```cpp
System::Net::WebProxy::WebProxy(System::SharedPtr<Uri> Address, bool BypassOnLocal)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| Address | [System::SharedPtr](../../../system/sharedptr/)\<[Uri](../../../system/uri/)\> | آدرس سرور پروکسی. |
| BypassOnLocal | **bool** | مقداری که نشان می‌دهد آیا سرور پروکسی برای آدرس‌های محلی استفاده شود. |

## WebProxy::WebProxy(System::SharedPtr\<Uri\>, bool, System::ArrayPtr\<String\>) سازنده


یک نمونه جدید می‌سازد.

```cpp
System::Net::WebProxy::WebProxy(System::SharedPtr<Uri> Address, bool BypassOnLocal, System::ArrayPtr<String> BypassList)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| Address | [System::SharedPtr](../../../system/sharedptr/)\<[Uri](../../../system/uri/)\> | آدرس سرور پروکسی. |
| BypassOnLocal | **bool** | مقداری که نشان می‌دهد آیا سرور پروکسی برای آدرس‌های محلی استفاده شود. |
| BypassList | [System::ArrayPtr](../../../system/arrayptr/)\<[String](../../../system/string/)\> | فهرست آدرس‌هایی که از سرور پروکسی استفاده نمی‌کنند. |

## WebProxy::WebProxy(System::SharedPtr\<Uri\>, bool, System::ArrayPtr\<String\>, System::SharedPtr\<ICredentials\>) سازنده


یک نمونه جدید می‌سازد.

```cpp
System::Net::WebProxy::WebProxy(System::SharedPtr<Uri> Address, bool BypassOnLocal, System::ArrayPtr<String> BypassList, System::SharedPtr<ICredentials> Credentials)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| Address | [System::SharedPtr](../../../system/sharedptr/)\<[Uri](../../../system/uri/)\> | آدرس سرور پروکسی. |
| BypassOnLocal | **bool** | مقداری که نشان می‌دهد آیا سرور پروکسی برای آدرس‌های محلی استفاده شود. |
| BypassList | [System::ArrayPtr](../../../system/arrayptr/)\<[String](../../../system/string/)\> | فهرست آدرس‌هایی که از سرور پروکسی استفاده نمی‌کنند. |
| Credentials | [System::SharedPtr](../../../system/sharedptr/)\<[ICredentials](../../icredentials/)\> | اعتبارهایی که به سرور پروکسی برای احراز هویت ارسال می‌شوند. |

## WebProxy::WebProxy(String, int32_t) سازنده


یک نمونه جدید می‌سازد.

```cpp
System::Net::WebProxy::WebProxy(String Host, int32_t Port)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| Host | [String](../../../system/string/) | نام میزبان. |
| Port | **int32_t** | شماره پورت. |

## WebProxy::WebProxy(String) سازنده


یک نمونه جدید می‌سازد.

```cpp
System::Net::WebProxy::WebProxy(String Address)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| Address | [String](../../../system/string/) | آدرس سرور پروکسی. |

## WebProxy::WebProxy(String, bool) سازنده


یک نمونه جدید می‌سازد.

```cpp
System::Net::WebProxy::WebProxy(String Address, bool BypassOnLocal)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| Address | [String](../../../system/string/) | آدرس سرور پروکسی. |
| BypassOnLocal | **bool** | مقداری که نشان می‌دهد آیا سرور پروکسی برای آدرس‌های محلی استفاده شود. |

## WebProxy::WebProxy(String, bool, System::ArrayPtr\<String\>) سازنده


یک نمونه جدید می‌سازد.

```cpp
System::Net::WebProxy::WebProxy(String Address, bool BypassOnLocal, System::ArrayPtr<String> BypassList)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| Address | [String](../../../system/string/) | آدرس سرور پروکسی. |
| BypassOnLocal | **bool** | مقداری که نشان می‌دهد آیا سرور پروکسی برای آدرس‌های محلی استفاده شود. |
| BypassList | [System::ArrayPtr](../../../system/arrayptr/)\<[String](../../../system/string/)\> | فهرست آدرس‌هایی که از سرور پروکسی استفاده نمی‌کنند. |

## WebProxy::WebProxy(String, bool, System::ArrayPtr\<String\>, System::SharedPtr\<ICredentials\>) سازنده


یک نمونه جدید می‌سازد.

```cpp
System::Net::WebProxy::WebProxy(String Address, bool BypassOnLocal, System::ArrayPtr<String> BypassList, System::SharedPtr<ICredentials> Credentials)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| Address | [String](../../../system/string/) | آدرس سرور پروکسی. |
| BypassOnLocal | **bool** | مقداری که نشان می‌دهد آیا سرور پروکسی برای آدرس‌های محلی استفاده شود. |
| BypassList | [System::ArrayPtr](../../../system/arrayptr/)\<[String](../../../system/string/)\> | فهرست آدرس‌هایی که از سرور پروکسی استفاده نمی‌کنند. |
| Credentials | [System::SharedPtr](../../../system/sharedptr/)\<[ICredentials](../../icredentials/)\> | اعتبارهایی که به سرور پروکسی برای احراز هویت ارسال می‌شوند. |

## موارد مرتبط

* تعریف‌نوع [SharedPtr](../../../system/sharedptr/)
* تعریف‌نوع [ArrayPtr](../../../system/arrayptr/)
* کلاس [WebProxy](../)
* کلاس [Uri](../../../system/uri/)
* کلاس [String](../../../system/string/)
* کلاس [ICredentials](../../icredentials/)
* فضای‌نام [System::Net](../../)
* کتابخانه [Aspose.Slides](../../../)