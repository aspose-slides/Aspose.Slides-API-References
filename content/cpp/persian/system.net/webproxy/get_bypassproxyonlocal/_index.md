---
title: get_BypassProxyOnLocal()
second_title: Aspose.Slides برای C++ مرجع API
description: مقداری را برمی‌گرداند که نشان می‌دهد آیا سرور پروکسی باید برای آدرس‌های محلی استفاده شود.
type: docs
weight: 27
url: /fa/system.net/webproxy/get_bypassproxyonlocal/
---
## WebProxy::get_BypassProxyOnLocal() متد

مقداری را برمی‌گرداند که نشان می‌دهد آیا باید برای آدرس‌های محلی از سرور پروکسی استفاده شود.

```cpp
bool System::Net::WebProxy::get_BypassProxyOnLocal()
```

### مقدار برگشتی

مقداری که نشان می‌دهد آیا باید برای آدرس‌های محلی از سرور پروکسی استفاده شود.

## موارد مرتبط

* کلاس [WebProxy](../)
* فضای نام [System::Net](../../)
* کتابخانه [Aspose.Slides](../../../)