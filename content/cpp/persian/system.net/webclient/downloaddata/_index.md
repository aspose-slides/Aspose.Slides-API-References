---
title: DownloadData()
second_title: Aspose.Slides برای C++ مرجع API
description: منبع مشخص شده را به‌صورت آرایه بایتی دریافت می‌کند.
type: docs
weight: 79
url: /fa/system.net/webclient/downloaddata/
---
## WebClient::DownloadData(const String\&) const متد

منبع مشخص شده را به‌صورت آرایه بایتی دریافت می‌کند.

```cpp
ByteArrayPtr System::Net::WebClient::DownloadData(const String &address) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| address | const [String](../../../system/string/)\& | URI منبع. |

### مقدار بازگشتی

آرایه بایتی که منبع درخواستی را شامل می‌شود.

## WebClient::DownloadData(const SharedPtr\<Uri\>\&) const متد

منبع مشخص شده را به‌صورت آرایه بایتی دریافت می‌کند.

```cpp
ByteArrayPtr System::Net::WebClient::DownloadData(const SharedPtr<Uri> &address) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| address | const [SharedPtr](../../../system/sharedptr/)\<[Uri](../../../system/uri/)\>\& | URI منبع. |

### مقدار بازگشتی

آرایه بایتی که منبع درخواستی را شامل می‌شود.

## همچنین ببینید

* تعریف‌نوع [ByteArrayPtr](../../../system/bytearrayptr/)
* تعریف‌نوع [SharedPtr](../../../system/sharedptr/)
* کلاس [String](../../../system/string/)
* کلاس [WebClient](../)
* کلاس [Uri](../../../system/uri/)
* فضای‌نام [System::Net](../../)
* کتابخانه [Aspose.Slides](../../../)