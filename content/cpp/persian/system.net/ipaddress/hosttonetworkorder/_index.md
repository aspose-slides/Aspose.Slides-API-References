---
title: HostToNetworkOrder()
second_title: Aspose.Slides برای C++ API Reference
description: مقدار ترتیب بایت میزبان مشخص شده را به ترتیب بایت شبکهٔ مربوطه تبدیل می‌کند.
type: docs
weight: 248
url: /fa/system.net/ipaddress/hosttonetworkorder/
---
## IPAddress::HostToNetworkOrder(int64_t) متد

تبدیل ترتیب بایت میزبان مشخص شده به ترتیب بایت شبکهٔ مربوطه.

```cpp
static int64_t System::Net::IPAddress::HostToNetworkOrder(int64_t host)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| host | **int64_t** | مقداری که باید تبدیل شود. |

### مقدار برگشت

ترتیب بایت شبکهٔ مربوطه.

## IPAddress::HostToNetworkOrder(int32_t) متد

تبدیل ترتیب بایت میزبان مشخص شده به ترتیب بایت شبکهٔ مربوطه.

```cpp
static int32_t System::Net::IPAddress::HostToNetworkOrder(int32_t host)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| host | **int32_t** | مقداری که باید تبدیل شود. |

### مقدار برگشت

ترتیب بایت شبکهٔ مربوطه.

## IPAddress::HostToNetworkOrder(int16_t) متد

تبدیل ترتیب بایت میزبان مشخص شده به ترتیب بایت شبکهٔ مربوطه.

```cpp
static int16_t System::Net::IPAddress::HostToNetworkOrder(int16_t host)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| host | **int16_t** | مقداری که باید تبدیل شود. |

### مقدار برگشت

ترتیب بایت شبکهٔ مربوطه.

## موارد مرتبط

* کلاس [IPAddress](../)
* نام‌فضا [System::Net](../../)
* کتابخانه [Aspose.Slides](../../../)