---
title: NetworkToHostOrder()
second_title: Aspose.Slides برای C++ مرجع API
description: مقدار بایت ترتیب شبکهٔ مشخص‌شده را به ترتیب بایت میزبان متناظر تبدیل می‌کند.
type: docs
weight: 261
url: /fa/system.net/ipaddress/networktohostorder/
---
## IPAddress::NetworkToHostOrder(int64_t) متد

مقدار بایت ترتیب شبکهٔ مشخص‌شده را به ترتیب بایت میزبان متناظر تبدیل می‌کند.

```cpp
static int64_t System::Net::IPAddress::NetworkToHostOrder(int64_t network)
```

### Arguments

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| network | **int64_t** | مقدار مورد نظر برای تبدیل. |

### Return Value

ترتیب بایت میزبان متناظر.

## IPAddress::NetworkToHostOrder(int32_t) متد

مقدار بایت ترتیب شبکهٔ مشخص‌شده را به ترتیب بایت میزبان متناظر تبدیل می‌کند.

```cpp
static int32_t System::Net::IPAddress::NetworkToHostOrder(int32_t network)
```

### Arguments

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| network | **int32_t** | مقدار مورد نظر برای تبدیل. |

### Return Value

ترتیب بایت میزبان متناظر.

## IPAddress::NetworkToHostOrder(int16_t) متد

مقدار بایت ترتیب شبکهٔ مشخص‌شده را به ترتیب بایت میزبان متناظر تبدیل می‌کند.

```cpp
static int16_t System::Net::IPAddress::NetworkToHostOrder(int16_t network)
```

### Arguments

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| network | **int16_t** | مقدار مورد نظر برای تبدیل. |

### Return Value

ترتیب بایت میزبان متناظر.

## See Also

* کلاس [IPAddress](../)
* فضای نام [System::Net](../../)
* کتابخانه [Aspose.Slides](../../../)