---
title: "System::Net"
second_title: مرجع API Aspose.Slides برای C++
description: 
type: docs
weight: 651
url: /fa/system.net/
---
## کلاس‌ها

| کلاس | توضیح |
| --- | --- |
| [Cookie](./cookie/) | نمایش یک کوکی HTTP. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [CookieCollection](./cookiecollection/) | نمایش یک لیست از کوکی‌های مرتب شده. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [CookieComparer](./cookiecomparer/) | برای مقایسه نمونه‌های کلاس [Cookie](./cookie/) استفاده می‌شود. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [CookieContainer](./cookiecontainer/) | یک محفظه برای نمونه‌های کلاس CookieCollection فراهم می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [CookieParser](./cookieparser/) | برای تجزیهٔ یک سرصفحهٔ کوکی و ایجاد نمونه‌ای از کلاس [Cookie](./cookie/) استفاده می‌شود. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [CredentialCache](./credentialcache/) | ذخیره‌سازی اعتبارها را فراهم می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [Details_CookieException](./details_cookieexception/) | نمایانگر استثنایی است که وقتی اندازهٔ [CookieContainer](./cookiecontainer/) بزرگ‌تر از مقدار ویژگی MaxCookieSize باشد، پرتاب می‌شود. هرگز نمونه‌های این کلاس را به صورت دستی ایجاد نکنید. به جای آن از کلاس CookieException استفاده کنید. هرگز نمونه‌های کلاس CookieException را در [System::SmartPtr](../system/smartptr/) بپیچانید. |
| [Details_WebException](./details_webexception/) | نمایانگر استثنایی است که توسط [WebRequest](./webrequest/) وقتی خطایی رخ می‌دهد پرتاب می‌شود. هرگز نمونه‌های این کلاس را به صورت دستی ایجاد نکنید. به جای آن از کلاس WebException استفاده کنید. هرگز نمونه‌های کلاس WebException را در [System::SmartPtr](../system/smartptr/) بپیچانید. |
| [Dns](./dns/) | روش‌هایی برای کار با DNS فراهم می‌کند. |
| [DnsEndPoint](./dnsendpoint/) | اطلاعاتی که برنامه برای اتصال به سرویس از آن استفاده می‌کند را شامل می‌شود. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [EndPoint](./endpoint/) | کلاس انتزاعی شامل یک آدرس شبکه است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [FileWebRequest](./filewebrequest/) | پیاده‌سازی کلاس انتزاعی [WebRequest](./webrequest/) برای کار با سیستم پرونده‌ها را فراهم می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [FileWebResponse](./filewebresponse/) | پیاده‌سازی کلاس انتزاعی [WebResponse](./webresponse/) برای کار با سیستم پرونده‌ها را فراهم می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [FtpWebRequest](./ftpwebrequest/) | یک مشتری File Transfer Protocol (FTP) را پیاده‌سازی می‌کند. این کلاس ساختگی برای امکان‌پذیر ساختن ارتباط کد ترجمه‌شده با ارجاع‌های [FtpWebRequest](./ftpwebrequest/) است، اما اجرا نمی‌شود. هیچ عضو به‌طور صحیحی پیاده‌سازی نشده‌است. |
| [FtpWebResponse](./ftpwebresponse/) | کلاس ساختگی برای امکان‌پذیر ساختن ارتباط کد ترجمه‌شده با ارجاع‌های [FtpWebResponse](./ftpwebresponse/) است، اما اجرا نمی‌شود. هیچ عضو به‌طور صحیحی پیاده‌سازی نشده‌است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [HeaderVariantInfo](./headervariantinfo/) | برای تطبیق نام کوکی و ویژگی‌ها استفاده می‌شود. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [HttpKnownHeaderNames](./httpknownheadernames/) | مقادیر رشته‌ای نام‌های شناخته‌شدهٔ سرصفحه‌ها را شامل می‌شود. |
| [HttpRequestHeaderExtensions](./httprequestheaderextensions/) | روش‌های کمکی برای کار با مقادیر شمارندهٔ HttpRequestHeader را شامل می‌شود. |
| [HttpResponseHeaderExtensions](./httpresponseheaderextensions/) | روش‌های کمکی برای کار با مقادیر شمارندهٔ HttpResponseHeader را شامل می‌شود. |
| [HttpStatusDescription](./httpstatusdescription/) | روش‌های کمکی برای دریافت نمایهٔ رشته‌ای وضعیت HTTP را شامل می‌شود. |
| [HttpVersion](./httpversion/) | شامل نسخه‌های HTTP است. |
| [HttpWebRequest](./httpwebrequest/) | نمایانگر درخواست وب HTTP است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [HttpWebResponse](./httpwebresponse/) | نمایانگر پاسخ وب HTTP است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [ICredentials](./icredentials/) | رابط احراز هویت را فراهم می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [ICredentialsByHost](./icredentialsbyhost/) | رابط احراز هویت برای دریافت اعتبارها برای میزبان، پورت و نوع احراز هویت را فراهم می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [IPAddress](./ipaddress/) | نمایانگر آدرس IP است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [IPEndPoint](./ipendpoint/) | نمایانگر نقطه پایانی شبکه‌ای است که شامل یک آدرس IP و یک پورت می‌شود. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [IPHostEntry](./iphostentry/) | نمایانگر اطلاعات دربارهٔ آدرس میزبان اینترنتی است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [IWebProxy](./iwebproxy/) | این رابط برای پیاده‌سازی دسترسی پراکسی به کلاس [WebRequest](./webrequest/) استفاده می‌شود. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [IWebRequestCreate](./iwebrequestcreate/) | روش‌هایی برای ایجاد نمونه‌های کلاس [WebRequest](./webrequest/) فراهم می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [NetworkCredential](./networkcredential/) | اعتبارها برای طرح‌های احراز هویت مبتنی بر رمز عبور را فراهم می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [PathList](./pathlist/) | نمایانگر فهرست نمونه‌های کلاس [CookieCollection](./cookiecollection/) است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [ServicePoint](./servicepoint/) | مدیریت اتصال HTTP را فراهم می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [ServicePointManager](./servicepointmanager/) | مراحل طول عمر (ایجاد، نگهداری و حذف) نمونه‌های کلاس [ServicePoint](./servicepoint/) را مدیریت می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [SocketAddress](./socketaddress/) | برای ذخیره اطلاعات سریالی‌شده دربارهٔ نمونه‌های کلاس [EndPoint](./endpoint/) استفاده می‌شود. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [UriScheme](./urischeme/) | طرح URI را نشان می‌دهد. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [WebClient](./webclient/) | [WebClient](./webclient/) روش‌های مشترک برای ارسال و دریافت داده‌ها را فراهم می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [WebHeaderCollection](./webheadercollection/) | نمایانگر مجموعه سرصفحه‌های پروتکل است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [WebProxy](./webproxy/) | نمایانگر یک سرور پروکسی وب http است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [WebRequest](./webrequest/) | نمایانگر یک درخواست وب است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [WebRequestMethods](./webrequestmethods/) | ثابت‌های رشته‌ای درخواست‌های وب را نشان می‌دهد. |
| [WebResponse](./webresponse/) | نمایانگر یک پاسخ وب است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعایی می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچانید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |

## شمارنده‌ها

| شمارنده | توضیح |
| --- | --- |
| [AuthenticationSchemes](./authenticationschemes/) | انواع روش‌های احراز هویت را فهرست می‌کند. |
| [CookieVariant](./cookievariant/) | ویژگی‌های کوکی را فهرست می‌کند. |
| [DecompressionMethods](./decompressionmethods/) | الگوریتم فشرده‌سازی-فراگشایی را فهرست می‌کند. |
| [HttpRequestHeader](./httprequestheader/) | سرصفحه‌های درخواست را فهرست می‌کند. |
| [HttpResponseHeader](./httpresponseheader/) | سرصفحه‌های پاسخ http را فهرست می‌کند. |
| [HttpStatusCode](./httpstatuscode/) |  |
| [SecurityProtocolType](./securityprotocoltype/) | انواع پروتکل‌های امنیتی را فهرست می‌کند. |
| [TransportType](./transporttype/) | نوع انتقال مجاز برای سوکت را تعریف می‌کند. |
| [WebExceptionStatus](./webexceptionstatus/) | کدهای وضعیت کلاس WebException را فهرست می‌کند. |
| [WebHeaderCollectionType](./webheadercollectiontype/) | انواع مجموعه سرصفحه‌های پروتکل را فهرست می‌کند. |

## نوع‌تعاریف

| نوع‌تعریف | توضیح |
| --- | --- |
| [CookieException](./cookieexception/) |  |
| [BindIPEndPoint](./bindipendpoint/) |  |
| [WebException](./webexception/) |  |