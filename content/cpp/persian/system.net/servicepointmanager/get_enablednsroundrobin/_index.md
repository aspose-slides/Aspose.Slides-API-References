---
title: get_EnableDnsRoundRobin()
second_title: Aspose.Slides برای مرجع API C++
description: یک مقدار را برمی‌گرداند که نشان می‌دهد آیا حل DNS بین آدرس‌های IP قابل استفاده به صورت چرخشی انجام می‌شود.
type: docs
weight: 157
url: /fa/system.net/servicepointmanager/get_enablednsroundrobin/
---
## ServicePointManager::get_EnableDnsRoundRobin() متد

یک مقدار را برمی‌گرداند که نشان می‌دهد آیا حل DNS بین آدرس‌های IP قابل استفاده به‌صورت چرخشی انجام می‌شود.

```cpp
static bool System::Net::ServicePointManager::get_EnableDnsRoundRobin()
```

### مقدار بازگشتی

یک مقدار که نشان می‌دهد آیا حل DNS بین آدرس‌های IP قابل استفاده به‌صورت چرخشی انجام می‌شود.

## همچنین ببینید

* کلاس [ServicePointManager](../)
* فضای‌نام [System::Net](../../)
* کتابخانه [Aspose.Slides](../../../)