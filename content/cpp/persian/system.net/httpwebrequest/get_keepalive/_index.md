---
title: get_KeepAlive()
second_title: مرجع API Aspose.Slides برای C++
description: یک مقدار دریافت می‌کند که نشان می‌دهد آیا درخواست فعلی باید هدر 'Keep-Alive' را شامل شود.
type: docs
weight: 573
url: /fa/system.net/httpwebrequest/get_keepalive/
---
## HttpWebRequest::get_KeepAlive() متد

یک مقدار دریافت می‌کند که نشان می‌دهد آیا درخواست فعلی باید هدر 'Keep-Alive' را شامل شود.

```cpp
virtual bool System::Net::HttpWebRequest::get_KeepAlive()
```

### مقدار بازگشتی

مقداری که نشان می‌دهد آیا درخواست فعلی باید هدر 'Keep-Alive' را شامل شود.

## موارد مرتبط

* کلاس [HttpWebRequest](../)
* فضای نام [System::Net](../../)
* کتابخانه [Aspose.Slides](../../../)