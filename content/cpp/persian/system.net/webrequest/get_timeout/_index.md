---
title: get_Timeout()
second_title: Aspose.Slides برای C++ مرجع API
description: مقداری از زمان بر حسب میلی‌ثانیه که پس از آن درخواست منقضی می‌شود را برمی‌گرداند.
type: docs
weight: 443
url: /fa/system.net/webrequest/get_timeout/
---
## WebRequest::get_Timeout() متد

مقداری از زمان بر حسب میلی‌ثانیه که پس از آن درخواست منقضی می‌شود را باز می‌گرداند.

```cpp
virtual int32_t System::Net::WebRequest::get_Timeout()
```

### مقدار بازگشت

مقداری از زمان بر حسب میلی‌ثانیه که پس از آن درخواست منقضی می‌شود.

## موارد مرتبط

* کلاس [WebRequest](../)
* فضای نام [System::Net](../../)
* کتابخانه [Aspose.Slides](../../../)