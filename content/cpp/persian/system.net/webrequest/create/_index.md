---
title: Create()
second_title: مرجع API Aspose.Slides برای C++
description: یک نمونه جدید از کلاس WebRequest را با استفاده از URI مشخص شده ایجاد می‌کند.
type: docs
weight: 53
url: /fa/system.net/webrequest/create/
---
## WebRequest::Create(String) متد

یک نمونه جدید از کلاس [WebRequest](../) را با استفاده از URI مشخص شده ایجاد می‌کند.

```cpp
static System::SharedPtr<WebRequest> System::Net::WebRequest::Create(String requestUriString)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| requestUriString | [String](../../../system/string/) | URIی که برای ایجاد یک نمونه جدید از کلاس [WebRequest](../) استفاده می‌شود. |

### مقدار بازگشت

یک نمونه جدید از کلاس WebRequest ایجاد شده است.

## WebRequest::Create(System::SharedPtr\<Uri\>) متد

یک نمونه جدید از کلاس [WebRequest](../) را با استفاده از URI مشخص شده ایجاد می‌کند.

```cpp
static System::SharedPtr<WebRequest> System::Net::WebRequest::Create(System::SharedPtr<Uri> requestUri)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| requestUri | [System::SharedPtr](../../../system/sharedptr/)\<[Uri](../../../system/uri/)\> | URIی که برای ایجاد یک نمونه جدید از کلاس [WebRequest](../) استفاده می‌شود. |

### مقدار بازگشت

یک نمونه جدید از کلاس WebRequest ایجاد شده است.

## موارد مرتبط

* Typedef [SharedPtr](../../../system/sharedptr/)
* کلاس [WebRequest](../)
* کلاس [String](../../../system/string/)
* کلاس [Uri](../../../system/uri/)
* فضای‌نام [System::Net](../../)
* کتابخانه [Aspose.Slides](../../../)