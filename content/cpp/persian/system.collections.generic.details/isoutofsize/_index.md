---
title: IsOutOfSize()
second_title: Aspose.Slides برای مرجع API C++
description: بررسی می‌کند که آیا اندیس خارج از محدودهٔ مخزن است، شامل اندازهٔ مخزن.
type: docs
weight: 222
url: /fa/system.collections.generic.details/isoutofsize/
---
## System::Collections::Generic::Details::IsOutOfSize(int, const Container\&) تابع

بررسی می‌کند که آیا شاخص خارج از محدودهٔ مخزن است، شامل اندازهٔ مخزن.

```cpp
template<typename Container> bool System::Collections::Generic::Details::IsOutOfSize(int count, const Container &container)
```

### پارامترهای قالب

| پارامتر | توضیح |
| --- | --- |
| Container | نوع مخزن. |

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| count | int | [Index](../../system/index/). |
| container | const Container\& | مخزنی که شاخص در برابر آن بررسی می‌شود. |

### مقدار برگشتی

درست اگر شاخص خارج از محدودهٔ مخزن باشد، در غیر این صورت نادرست.

## System::Collections::Generic::Details::IsOutOfSize(std::int64_t, const Container\&) تابع

بررسی می‌کند که آیا شاخص خارج از محدودهٔ مخزن است، شامل اندازهٔ مخزن.

```cpp
template<typename Container> bool System::Collections::Generic::Details::IsOutOfSize(std::int64_t count, const Container &container)
```

### پارامترهای قالب

| پارامتر | توضیح |
| --- | --- |
| Container | نوع مخزن. |

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| count | std::int64_t | [Index](../../system/index/). |
| container | const Container\& | مخزنی که شاخص در برابر آن بررسی می‌شود. |

### مقدار برگشتی

درست اگر شاخص خارج از محدودهٔ مخزن باشد، در غیر این صورت نادرست.

## موارد مرتبط

* فضای نام [System::Collections::Generic::Details](../)
* کتابخانه [Aspose.Slides](../../)