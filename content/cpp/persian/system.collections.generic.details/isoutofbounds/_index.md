---
title: IsOutOfBounds()
second_title: مرجع API Aspose.Slides برای C++
description: بررسی می‌کند که آیا اندیس خارج از محدودهٔ کانتینر است، به‌جز اندازهٔ کانتینر.
type: docs
weight: 209
url: /fa/system.collections.generic.details/isoutofbounds/
---
## System::Collections::Generic::Details::IsOutOfBounds(int, const Container\&) تابع

بررسی می‌کند که آیا اندیس خارج از محدودهٔ کانتینر است، به‌جز اندازهٔ کانتینر.

```cpp
template<typename Container> bool System::Collections::Generic::Details::IsOutOfBounds(int idx, const Container &container)
```

### پارامترهای قالب

| پارامتر | توضیح |
| --- | --- |
| Container | نوع کانتینر. |

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| idx | int | [Index](../../system/index/). |
| container | const Container\& | کانتینری که اندیس نسبت به آن بررسی می‌شود. |

### مقدار بازگشتی

در صورتی که اندیس خارج از محدودهٔ کانتینر باشد true، در غیر این صورت false.

## System::Collections::Generic::Details::IsOutOfBounds(std::int64_t, const Container\&) تابع

بررسی می‌کند که آیا اندیس خارج از محدودهٔ کانتینر است، به‌جز اندازهٔ کانتینر.

```cpp
template<typename Container> bool System::Collections::Generic::Details::IsOutOfBounds(std::int64_t idx, const Container &container)
```

### پارامترهای قالب

| پارامتر | توضیح |
| --- | --- |
| Container | نوع کانتینر. |

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| idx | std::int64_t | [Index](../../system/index/). |
| container | const Container\& | کانتینری که اندیس نسبت به آن بررسی می‌شود. |

### مقدار بازگشتی

در صورتی که اندیس خارج از محدودهٔ کانتینر باشد true، در غیر این صورت false.

## موارد مرتبط

* فضای‌نام [System::Collections::Generic::Details](../)
* کتابخانه [Aspose.Slides](../../)