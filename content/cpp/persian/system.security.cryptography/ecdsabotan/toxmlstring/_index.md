---
title: ToXmlString()
second_title: Aspose.Slides برای C++ مرجع API
description: تمام پارامترها را در قالب XML صادر می‌کند. اجرا نشده است.
type: docs
weight: 157
url: /fa/system.security.cryptography/ecdsabotan/toxmlstring/
---
## ECDsaBotan::ToXmlString(bool) متد

تمام پارامترها را در قالب XML صادر می‌کند. اجرا نشده است.

```cpp
String System::Security::Cryptography::ECDsaBotan::ToXmlString(bool include_private_parameters) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| include_private_parameters | **bool** | درست برای صادر کردن هر دو پارامتر خصوصی و عمومی، غلط برای صادر کردن فقط پارامترهای عمومی. |

### مقدار بازگشت

پارامترهای کدگذاری‌شده با XML.

## ECDsaBotan::ToXmlString(ECKeyXmlFormat) متد

تمام پارامترها را در قالب XML صادر می‌کند.

```cpp
String System::Security::Cryptography::ECDsaBotan::ToXmlString(ECKeyXmlFormat format)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| format | [ECKeyXmlFormat](../../eckeyxmlformat/) | قالب رشته XML نتیجه. |

### مقدار بازگشت

پارامترهای کدگذاری‌شده با XML.

## موارد مرتبط

* Enum [ECKeyXmlFormat](../../eckeyxmlformat/)
* کلاس [String](../../../system/string/)
* کلاس [ECDsaBotan](../)
* فضای‌نام [System::Security::Cryptography](../../)
* Library [Aspose.Slides](../../../)