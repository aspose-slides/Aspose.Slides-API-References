---
title: Read()
second_title: مرجع API Aspose.Slides برای C++
description: داده‌ها را از جریان می‌خواند.
type: docs
weight: 14
url: /fa/system.security.cryptography/cryptostream/read/
---
## CryptoStream::Read(const ArrayPtr\<uint8_t\>\&, int32_t, int32_t) متد

داده‌ها را از جریان می‌خواند.

```cpp
int32_t System::Security::Cryptography::CryptoStream::Read(const ArrayPtr<uint8_t> &buffer, int32_t offset, int32_t count) override
```

### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\>\& | بافر مقصد داده. |
| offset | **int32_t** | افست در بافر مقصد. |
| count | **int32_t** | تعداد بایت‌های خوانده‌شده. |

### مقدار بازگشتی

تعداد بایت‌های خوانده شده واقعی.

## CryptoStream::Read(const System::Details::ArrayView\<uint8_t\>\&, int32_t, int32_t) متد

داده‌ها را از جریان می‌خواند.

```cpp
int32_t System::Security::Cryptography::CryptoStream::Read(const System::Details::ArrayView<uint8_t> &buffer, int32_t offset, int32_t count) override
```

### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| buffer | const System::Details::ArrayView\<**uint8_t**\>\& | بافر مقصد داده. |
| offset | **int32_t** | افست در بافر مقصد. |
| count | **int32_t** | تعداد بایت‌های خوانده‌شده. |

### مقدار بازگشتی

تعداد بایت‌های خوانده شده واقعی.

## موارد مرتبط

* تعریف نوع [ArrayPtr](../../../system/arrayptr/)
* کلاس [CryptoStream](../)
* فضای نام [System::Security::Cryptography](../../)
* کتابخانه [Aspose.Slides](../../../)