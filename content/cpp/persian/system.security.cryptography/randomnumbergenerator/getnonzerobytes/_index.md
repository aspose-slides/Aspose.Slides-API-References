---
title: GetNonZeroBytes()
second_title: Aspose.Slides برای C++ API Reference
description: عناصر آرایه موجود را با بایت‌های تصادفی غیر صفر پر می‌کند.
type: docs
weight: 27
url: /fa/system.security.cryptography/randomnumbergenerator/getnonzerobytes/
---
## RandomNumberGenerator::GetNonZeroBytes(ArrayPtr\<uint8_t\>) متد

عناصر آرایه موجود را با بایت‌های تصادفی غیر صفر پر می‌کند.

```cpp
virtual void System::Security::Cryptography::RandomNumberGenerator::GetNonZeroBytes(ArrayPtr<uint8_t> bytes)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | آرایه بایت‌ها برای پر کردن. |

## RandomNumberGenerator::GetNonZeroBytes(System::Details::ArrayView\<uint8_t\>) متد

عناصر نمای آرایه موجود را با بایت‌های تصادفی غیر صفر پر می‌کند.

```cpp
virtual void System::Security::Cryptography::RandomNumberGenerator::GetNonZeroBytes(System::Details::ArrayView<uint8_t> bytes)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| bytes | System::Details::ArrayView\<**uint8_t**\> | نمای آرایه بایت‌ها برای پر کردن. |

## RandomNumberGenerator::GetNonZeroBytes(System::Details::StackArray\<uint8_t, N\>\&) متد

عناصر آرایه پشته موجود را با بایت‌های تصادفی غیر صفر پر می‌کند.

```cpp
template<std::size_t> void System::Security::Cryptography::RandomNumberGenerator::GetNonZeroBytes(System::Details::StackArray<uint8_t, N> &bytes)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| bytes | System::Details::StackArray\<**uint8_t**, N\>\& | آرایه پشته بایت‌ها برای پر کردن. |

## موارد مرتبط

* تعریف‌نوع [ArrayPtr](../../../system/arrayptr/)
* کلاس [RandomNumberGenerator](../)
* فضای‌نام [System::Security::Cryptography](../../)
* کتابخانه [Aspose.Slides](../../../)