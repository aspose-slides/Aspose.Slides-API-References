---
title: get_IsFixedSize()
second_title: مرجع API Aspose.Slides برای C++
description: یک مقدار دریافت می‌کند که نشان می‌دهد آیا لیست اندازه ثابت دارد یا خیر.
type: docs
weight: 105
url: /fa/system.collections/ilist/get_isfixedsize/
---
## IList::get_IsFixedSize() const متد

یک مقدار دریافت می‌کند که نشان می‌دهد آیا لیست اندازه ثابت دارد یا خیر.

```cpp
virtual bool System::Collections::IList::get_IsFixedSize() const
```

### مقدار بازگشتی

در صورتی که لیست اندازه ثابت داشته باشد true، در غیر این صورت false.

## موارد مرتبط

* کلاس [IList](../)
* فضای نام [System::Collections](../../)
* کتابخانه [Aspose.Slides](../../../)