---
title: Delete()
second_title: Aspose.Slides برای C++ مرجع API
description: دایرکتوری‌ای که توسط مسیر نمایان‌گر شیء کنونی ارجاع داده می‌شود را حذف می‌کند اگر دایرکتوری خالی باشد.
type: docs
weight: 92
url: /fa/system.io/directoryinfo/delete/
---
## DirectoryInfo::Delete() متد

دایرکتوری‌ای که توسط مسیر نمایان‌گر شیء کنونی ارجاع داده می‌شود را حذف می‌کند اگر دایرکتوری خالی باشد.

```cpp
virtual void System::IO::DirectoryInfo::Delete() override
```

## DirectoryInfo::Delete(bool) متد

دایرکتوری‌ای که توسط مسیر نمایان‌گر شیء کنونی ارجاع داده می‌شود را حذف می‌کند. پارامتری مشخص می‌کند آیا محتوای دایرکتوری باید به‌صورت بازگشتی حذف شود اگر دایرکتوری خالی نباشد.

```cpp
void System::IO::DirectoryInfo::Delete(bool recursive)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| recursive | **bool** | مشخص می‌کند آیا محتوای دایرکتوری باید به‌صورت بازگشتی حذف شود اگر خالی نباشد |

## موارد مرتبط

* کلاس [DirectoryInfo](../)
* فضای‌نام [System::IO](../../)
* کتابخانه [Aspose.Slides](../../../)