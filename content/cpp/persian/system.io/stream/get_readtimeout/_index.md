---
title: get_ReadTimeout()
second_title: مرجع API Aspose.Slides برای C++
description: مقداری را به میلی‌ثانیه می‌گیرد که تعیین می‌کند جریان چه مدت زمان قبل از زمان‌پایان سعی می‌کند بخواند.
type: docs
weight: 326
url: /fa/system.io/stream/get_readtimeout/
---
## Stream::get_ReadTimeout() const متد

مقداری را به میلی‌ثانیه می‌گیرد که تعیین می‌کند جریان چه مدت زمان قبل از زمان‌پایان سعی می‌کند بخواند.

```cpp
virtual int System::IO::Stream::get_ReadTimeout() const
```

### مقدار بازگشت

مقداری به میلی‌ثانیه که تعیین می‌کند جریان چه مدت زمان قبل از زمان‌پایان سعی می‌کند بخواند.

## مراجع

* کلاس [Stream](../)
* فضای‌نام [System::IO](../../)
* کتابخانه [Aspose.Slides](../../../)