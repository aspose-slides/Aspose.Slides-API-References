---
title: ReadByte()
second_title: Aspose.Slides برای C++ مرجع API
description: اگر حالت بسته‌بندی باینری باشد، یک بایت از ذخیره‌سازی کاراکتر رمزگشایی‌شدهٔ آخر می‌خواند، در غیر این صورت یک کاراکتر از جریان می‌خواند و آن را به نوع uint8_t تبدیل می‌کند. پشتیبانی نمی‌شود!
type: docs
weight: 40
url: /fa/system.io/basicstdostreamwrapper/readbyte/
---
## BasicSTDOStreamWrapper::ReadByte() متد


اگر حالت بسته‌بندی باینری باشد، یک بایت از ذخیره‌سازی کاراکتر رمزگشایی‌شدهٔ آخر می‌خواند، در غیر این صورت یک کاراکتر از جریان می‌خواند و آن را به نوع **uint8_t** تبدیل می‌کند. پشتیبانی نمی‌شود!

```cpp
virtual int System::IO::BasicSTDOStreamWrapper<T, typename>::ReadByte() override
```


### مقدار بازگشت

یک مقدار عدد صحیح ۳۲ بیتی معادل نتیجهٔ خواندن

## موارد مرتبط

* کلاس [BasicSTDOStreamWrapper](../)
* فضای نام [System::IO](../../)
* کتابخانه [Aspose.Slides](../../../)