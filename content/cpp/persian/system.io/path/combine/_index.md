---
title: Combine()
second_title: Aspose.Slides برای C++ مرجع API
description: بخش‌های مسیر مشخص‌شده را در یک مسیر ترکیب می‌کند و در صورت نیاز، کاراکترهای جداکنندهٔ دایرکتوری را بین این بخش‌ها قرار می‌دهد.
type: docs
weight: 14
url: /fa/system.io/path/combine/
---
## Path::Combine(const ArrayPtr\<String\>\&) متد

قسمت‌های مسیر مشخص‌شده را در یک مسیر ترکیب می‌کند و در صورت نیاز، کاراکترهای جداکنندهٔ دایرکتوری را بین این بخش‌ها قرار می‌دهد.

```cpp
static String System::IO::Path::Combine(const ArrayPtr<String> &paths)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| paths | const [ArrayPtr](../../../system/arrayptr/)\<[String](../../../system/string/)\>\& | آرایه‌ای شامل بخش‌های مسیر برای ترکیب |

### مقدار بازگشتی

مسیر ترکیب‌شده

## Path::Combine(const String\&, const String\&) متد

دو بخش مسیر مشخص‌شده را در یک مسیر ترکیب می‌کند و در صورت نیاز، کاراکتر جداکنندهٔ دایرکتوری را بین این بخش‌ها قرار می‌دهد.

```cpp
static String System::IO::Path::Combine(const String &path1, const String &path2)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| path1 | const [String](../../../system/string/)\& | بخش مسیر اول |
| path2 | const [String](../../../system/string/)\& | بخش مسیر دوم |

### مقدار بازگشتی

مسیر ترکیب‌شده

## Path::Combine(const String\&, const String\&, const String\&) متد

سه بخش مسیر مشخص‌شده را در یک مسیر ترکیب می‌کند و در صورت نیاز، کاراکترهای جداکنندهٔ دایرکتوری را بین این بخش‌ها قرار می‌دهد.

```cpp
static String System::IO::Path::Combine(const String &path1, const String &path2, const String &path3)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| path1 | const [String](../../../system/string/)\& | بخش مسیر اول |
| path2 | const [String](../../../system/string/)\& | بخش مسیر دوم |
| path3 | const [String](../../../system/string/)\& | بخش مسیر سوم |

### مقدار بازگشتی

مسیر ترکیب‌شده

## Path::Combine(const String\&, const String\&, const String\&, const String\&) متد

چهار بخش مسیر مشخص‌شده را در یک مسیر ترکیب می‌کند و در صورت نیاز، کاراکترهای جداکنندهٔ دایرکتوری را بین این بخش‌ها قرار می‌دهد.

```cpp
static String System::IO::Path::Combine(const String &path1, const String &path2, const String &path3, const String &path4)
```

### آرگومان‌ها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| path1 | const [String](../../../system/string/)\& | بخش مسیر اول |
| path2 | const [String](../../../system/string/)\& | بخش مسیر دوم |
| path3 | const [String](../../../system/string/)\& | بخش مسیر سوم |
| path4 | const [String](../../../system/string/)\& | بخش مسیر چهارم |

### مقدار بازگشتی

مسیر ترکیب‌شده

## موارد مرتبط

* تعریف‌نوع [ArrayPtr](../../../system/arrayptr/)
* کلاس [String](../../../system/string/)
* کلاس [Path](../)
* فضای‌نام [System::IO](../../)
* کتابخانه [Aspose.Slides](../../../)