---
title: Write()
second_title: مرجع API Aspose.Slides برای C++
description: نمایش رشته‌ای شیء مشخص‌شده را به جریان می‌نویسد.
type: docs
weight: 105
url: /fa/system.io/textwriter/write/
---
## TextWriter::Write(const SharedPtr\<Object\>\&) متد

نمایش رشته‌ای شیء مشخص‌شده را به جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::Write(const SharedPtr<Object> &value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [SharedPtr](../../../system/sharedptr/)\<[Object](../../../system/object/)\>\& | شیء برای نوشتن |

## TextWriter::Write(bool) متد

نمایش رشته‌ای مقدار boolean مشخص‌شده را به جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::Write(bool value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **bool** | مقدار برای نوشتن |

## TextWriter::Write(char_t) متد

کاراکتر مشخص‌شده را به جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::Write(char_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | char_t | مقدار برای نوشتن |

## TextWriter::Write(Decimal) متد

نمایش رشته‌ای شیء [Decimal](../../../system/decimal/) مشخص‌شده را به جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::Write(Decimal value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | [Decimal](../../../system/decimal/) | شیء برای نوشتن |

## TextWriter::Write(double) متد

نمایش رشته‌ای مقدار عدد شناور دوبل پرسیژن مشخص‌شده را به جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::Write(double value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **double** | مقدار برای نوشتن |

## TextWriter::Write(int) متد

نمایش رشته‌ای مقدار عدد صحیح ۳۲ بیتی مشخص‌شده را به جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::Write(int value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int | مقدار برای نوشتن |

## TextWriter::Write(int64_t) متد

نمایش رشته‌ای مقدار عدد صحیح ۶۴ بیتی مشخص‌شده را به جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::Write(int64_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **int64_t** | مقدار برای نوشتن |

## TextWriter::Write(float) متد

نمایش رشته‌ای مقدار عدد شناور تک‌پرسیژن مشخص‌شده را به جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::Write(float value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **float** | مقدار برای نوشتن |

## TextWriter::Write(const String\&) متد

رشته‌ی مشخص‌شده را به جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::Write(const String &value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../../system/string/)\& | رشته برای نوشتن |

## TextWriter::Write(uint32_t) متد

نمایش رشته‌ای مقدار عدد صحیح بدون علامت ۳۲ بیتی مشخص‌شده را به جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::Write(uint32_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **uint32_t** | مقدار برای نوشتن |

## TextWriter::Write(uint64_t) متد

نمایش رشته‌ای مقدار عدد صحیح بدون علامت ۶۴ بیتی مشخص‌شده را به جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::Write(uint64_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **uint64_t** | مقدار برای نوشتن |

## TextWriter::Write(const ArrayPtr\<char_t\>\&) متد

تمام کاراکترهای آرایه‌ی مشخص‌شده را به جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::Write(const ArrayPtr<char_t> &buffer)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../../system/arrayptr/)\<char_t\>\& | آرایه شامل کاراکترهای برای نوشتن |

## TextWriter::Write(const ArrayPtr\<char_t\>\&, int32_t, int32_t) متد

بخش مشخص‌شده‌ای از کاراکترهای UTF-16 را از آرایه کاراکتری مشخص به جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::Write(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../../system/arrayptr/)\<char_t\>\& | آرایه شامل کاراکترهای برای نوشتن |
| index | **int32_t** | ایندکس مبتنی بر صفر از **buffer** که بازه‌ی زیرمجموعه برای نوشتن از آن شروع می‌شود |
| count | **int32_t** | تعداد کاراکترهای در بازه برای نوشتن؛ -1 به معنای اینکه بازه تا انتهای آرایه **buffer** ادامه دارد |

## TextWriter::Write(const char_t *) متد

رشته‌ی C مشخص‌شده را به جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::Write(const char_t *value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const char_t * | رشته‌ی C برای نوشتن |

## TextWriter::Write(const TypeInfo\&) متد

نمایش رشته‌ای شیء [TypeInfo](../../../system/typeinfo/) مشخص‌شده را به جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::Write(const TypeInfo &value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [TypeInfo](../../../system/typeinfo/)\& | شیء برای نوشتن |

## TextWriter::Write(const String\&, const TArgs\&...) متد

مقدارهای مشخص‌شده را با فرمت تعیین‌شده به جریان می‌نویسد.

```cpp
template<class...> void System::IO::TextWriter::Write(const String &format, const TArgs &... args)
```

### پارامترهای قالب

| پارامتر | توضیح |
| --- | --- |
| TArgs | فهرست انواع مقادیر برای نوشتن |

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| format | const [String](../../../system/string/)\& | قالب رشته |
| args | const TArgs\&... | مقدارهای برای نوشتن |

## موارد مرتبط

* Typedef [SharedPtr](../../../system/sharedptr/)
* Typedef [ArrayPtr](../../../system/arrayptr/)
* کلاس [Object](../../../system/object/)
* کلاس [TextWriter](../)
* کلاس [Decimal](../../../system/decimal/)
* کلاس [String](../../../system/string/)
* کلاس [TypeInfo](../../../system/typeinfo/)
* فضای‌نام [System::IO](../../)
* کتابخانه [Aspose.Slides](../../../)