---
title: WriteLine()
second_title: مرجع API Aspose.Slides برای C++
description: کاراکترهای خاتمه خط را در جریان می‌نویسد.
type: docs
weight: 118
url: /fa/system.io/textwriter/writeline/
---
## TextWriter::WriteLine() متد

کاراکترهای خاتمه خط را در جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::WriteLine()
```

## TextWriter::WriteLine(const SharedPtr\<Object\>\&) متد

نمایش رشته‌ای از شئ مشخص‌شده را به همراه کاراکترهای خاتمه خط در جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::WriteLine(const SharedPtr<Object> &value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [SharedPtr](../../../system/sharedptr/)\<[Object](../../../system/object/)\>\& | شئ‌ای که باید نوشته شود |

## TextWriter::WriteLine(bool) متد

نمایش رشته‌ای از مقدار بولی مشخص‌شده را به همراه کاراکترهای خاتمه خط در جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::WriteLine(bool value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **bool** | مقداری که باید نوشته شود |

## TextWriter::WriteLine(char_t) متد

کاراکتر مشخص‌شده را به همراه کاراکترهای خاتمه خط در جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::WriteLine(char_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | char_t | مقداری که باید نوشته شود |

## TextWriter::WriteLine(Decimal) متد

نمایش رشته‌ای از شئ [Decimal](../../../system/decimal/) مشخص‌شده را به همراه کاراکترهای خاتمه خط در جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::WriteLine(Decimal value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | [Decimal](../../../system/decimal/) | شئ‌ای که باید نوشته شود |

## TextWriter::WriteLine(double) متد

نمایش رشته‌ای از مقدار شناور دوتایی‌دقت مشخص‌شده را به همراه کاراکترهای خاتمه خط در جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::WriteLine(double value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **double** | مقداری که باید نوشته شود |

## TextWriter::WriteLine(int) متد

نمایش رشته‌ای از مقدار عدد صحیح ۳۲-بیتی مشخص‌شده را به همراه کاراکترهای خاتمه خط در جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::WriteLine(int value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int | مقداری که باید نوشته شود |

## TextWriter::WriteLine(int64_t) متد

نمایش رشته‌ای از مقدار عدد صحیح ۶۴-بیتی مشخص‌شده را به همراه کاراکترهای خاتمه خط در جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::WriteLine(int64_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **int64_t** | مقداری که باید نوشته شود |

## TextWriter::WriteLine(float) متد

نمایش رشته‌ای از مقدار شناور تک-دقت مشخص‌شده را به همراه کاراکترهای خاتمه خط در جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::WriteLine(float value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **float** | مقداری که باید نوشته شود |

## TextWriter::WriteLine(const String\&) متد

رشتهٔ مشخص‌شده را به همراه کاراکترهای خاتمه خط در جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::WriteLine(const String &value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [String](../../../system/string/)\& | رشته‌ای که باید نوشته شود |

## TextWriter::WriteLine(uint32_t) متد

نمایش رشته‌ای از مقدار عدد صحیح بدون علامت ۳۲-بیتی مشخص‌شده را به همراه کاراکترهای خاتمه خط در جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::WriteLine(uint32_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **uint32_t** | مقداری که باید نوشته شود |

## TextWriter::WriteLine(uint64_t) متد

نمایش رشته‌ای از مقدار عدد صحیح بدون علامت ۶۴-بیتی مشخص‌شده را به همراه کاراکترهای خاتمه خط در جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::WriteLine(uint64_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **uint64_t** | مقداری که باید نوشته شود |

## TextWriter::WriteLine(const ArrayPtr\<char_t\>\&) متد

تمام کاراکترهای موجود در آرایهٔ مشخص‌شده را به همراه کاراکترهای خاتمه خط در جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::WriteLine(const ArrayPtr<char_t> &buffer)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../../system/arrayptr/)\<char_t\>\& | آرایه‌ای که شامل کاراکترهای باید نوشته شوند |

## TextWriter::WriteLine(const ArrayPtr\<char_t\>\&, int32_t, int32_t) متد

بخش مشخص‌شده‌ای از کاراکترهای UTF-16 موجود در آرایهٔ کاراکتری مشخص‌شده را به همراه کاراکترهای خاتمه خط در جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::WriteLine(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../../system/arrayptr/)\<char_t\>\& | آرایه‌ای که شامل کاراکترهای باید نوشته شوند |
| index | **int32_t** | اندیس مبتنی بر صفر المان **buffer** که زیرمحدوده از آنجا شروع می‌شود |
| count | **int32_t** | تعداد کاراکترهای زیرمحدوده؛ -۱ به معنای ادامه تا انتهای آرایه **buffer** است |

## TextWriter::WriteLine(const char_t *) متد

رشتهٔ C مشخص‌شده را به همراه کاراکترهای خاتمه خط در جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::WriteLine(const char_t *value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const char_t * | رشتهٔ C که باید نوشته شود |

## TextWriter::WriteLine(const TypeInfo\&) متد

نمایش رشته‌ای از شئ [TypeInfo](../../../system/typeinfo/) مشخص‌شده را به همراه کاراکترهای خاتمه خط در جریان می‌نویسد.

```cpp
virtual void System::IO::TextWriter::WriteLine(const TypeInfo &value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | const [TypeInfo](../../../system/typeinfo/)\& | شئ‌ای که باید نوشته شود |

## TextWriter::WriteLine(const String\&, const TArgs\&...) متد

مقدارهای مشخص‌شده را بر اساس قالب مشخص‌شده قالب‌بندی کرده و به همراه کاراکترهای خاتمه خط در جریان می‌نویسد.

```cpp
template<class...> void System::IO::TextWriter::WriteLine(const String &format, const TArgs &... args)
```

### پارامترهای قالب

| پارامتر | توضیح |
| --- | --- |
| TArgs | فهرست انواع مقادیری که باید نوشته شوند |

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| format | const [String](../../../system/string/)\& | قالب رشته |
| args | const TArgs\&... | مقادیری که باید نوشته شوند |

## مراجع

* Typedef [SharedPtr](../../../system/sharedptr/)
* Typedef [ArrayPtr](../../../system/arrayptr/)
* کلاس [TextWriter](../)
* کلاس [Object](../../../system/object/)
* کلاس [Decimal](../../../system/decimal/)
* کلاس [String](../../../system/string/)
* کلاس [TypeInfo](../../../system/typeinfo/)
* فضای‌نام [System::IO](../../)
* Library [Aspose.Slides](../../../)