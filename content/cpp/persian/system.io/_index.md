---
title: "System::IO"
second_title: "راهنمای API Aspose.Slides برای C++"
description: 
type: docs
weight: 573
url: /fa/system.io/
---
## کلاس‌ها

| کلاس | توضیح |
| --- | --- |
| [BasicSTDIOStreamWrapper](./basicstdiostreamwrapper/) | نمایانگر یک بسته‌بندی شبیه [System.IO.Stream](./stream/) برای std::basic_iostream و اشیای مشتق‌شده آن است. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای assertion می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای ارسال به توابع به عنوان آرگومان استفاده کنید. |
| [BasicSTDIStreamWrapper](./basicstdistreamwrapper/) | نمایانگر یک بسته‌بندی شبیه [System.IO.Stream](./stream/) برای std::basic_istream و اشیای مشتق‌شده آن است. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای assertion می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای ارسال به توابع به عنوان آرگومان استفاده کنید. |
| [BasicSTDOStreamWrapper](./basicstdostreamwrapper/) | نمایانگر یک بسته‌بندی شبیه [System.IO.Stream](./stream/) برای std::basic_ostream و اشیای مشتق‌شده آن است. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای assertion می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای ارسال به توابع به عنوان آرگومان استفاده کنید. |
| [BasicSystemIOStreamBuf](./basicsystemiostreambuf/) | نمایانگر بافرئی است که جریان‌های شبیه [System::IO::Stream](./stream/) را در بر می‌گیرد و اجازه می‌دهد به عنوان بافر داخلی جریان‌های شبیه std::iostream استفاده شوند. |
| [BasicSystemIOStreamWrapper](./basicsystemiostreamwrapper/) | نمایانگر یک بسته‌بندی شبیه std::iostream است که از [BasicSystemIOStreamBuf](./basicsystemiostreambuf/) به‌عنوان بافر داخلی استفاده می‌کند. |
| [BasicSystemIStreamWrapper](./basicsystemistreamwrapper/) | نمایانگر یک بسته‌بندی شبیه std::istream است که از [BasicSystemIOStreamBuf](./basicsystemiostreambuf/) به‌عنوان بافر داخلی استفاده می‌کند. |
| [BasicSystemOStreamWrapper](./basicsystemostreamwrapper/) | نمایانگر یک بسته‌بندی شبیه std::ostream است که از [BasicSystemIOStreamBuf](./basicsystemiostreambuf/) به‌عنوان بافر داخلی استفاده می‌کند. |
| [BinaryReader](./binaryreader/) | نمایانگر یک خواننده است که انواع داده‌های اولیه را به‌عنوان داده‌های باینری در یک رمزگذاری خاص می‌خواند. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای assertion می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای ارسال به توابع به عنوان آرگومان استفاده کنید. |
| [BinaryWriter](./binarywriter/) | نمایانگر یک نویسنده است که مقادیر انواع داده‌های اولیه را به یک جریان بایتی می‌نویسد. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای assertion می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای ارسال به توابع به عنوان آرگومان استفاده کنید. |
| [BufferedStream](./bufferedstream/) | یک لایه بافرینگ در بالای یک جریان دیگر اضافه می‌کند. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای assertion می‌شود. همواره این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای ارسال به توابع به عنوان آرگومان بهره ببرید. |
| [Details_DirectoryNotFoundException](./details_directorynotfoundexception/) |  |
| [Details_DriveNotFoundException](./details_drivenotfoundexception/) |  |
| [Details_EndOfStreamException](./details_endofstreamexception/) |  |
| [Details_FileLoadException](./details_fileloadexception/) |  |
| [Details_FileNotFoundException](./details_filenotfoundexception/) | استثنایی است که هنگام تلاش برای دسترسی به فایلی که روی دیسک وجود ندارد و شکست می‌خورد، پرتاب می‌شود. هرگز نمونه‌های این کلاس را به‌صورت دستی ایجاد نکنید. به‌جای آن از کلاس FileNotFoundException استفاده کنید. هرگز نمونه‌های کلاس FileNotFoundException را در [System::SmartPtr](../system/smartptr/) بسته‌بندی نکنید. |
| [Details_InvalidDataException](./details_invaliddataexception/) |  |
| [Details_IOException](./details_ioexception/) |  |
| [Details_PathTooLongException](./details_pathtoolongexception/) |  |
| [Directory](./directory/) | شامل متدهایی برای دستکاری دایرکتوری‌ها است. این یک نوع ایستا بدون سرویس‌های نمونه است. هرگز نباید به هیچ وجه نمونه‌ای از آن ایجاد کنید. |
| [DirectoryInfo](./directoryinfo/) | نمایانگر یک مسیر سیستم فایل، دایرکتوری‌ای که با این مسیر اشاره می‌شود و متدهای نمونه‌ای برای دستکاری دایرکتوری‌ها فراهم می‌کند. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای assertion می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای ارسال به توابع به عنوان آرگومان استفاده کنید. |
| [File](./file/) | متدهایی برای دستکاری فایل‌ها فراهم می‌کند. این یک نوع ایستا بدون سرویس‌های نمونه است. هرگز نباید به هیچ وجه نمونه‌ای از آن ایجاد کنید. |
| [FileInfo](./fileinfo/) | نمایانگر مسیری به یک فایل و فایلی که با این مسیر اشاره می‌شود و متدهایی برای دستکاری آن فراهم می‌کند. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای assertion می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای ارسال به توابع به عنوان آرگومان استفاده کنید. |
| [FileStream](./filestream/) | نمایانگر یک جریان فایل که از عملیات خواندن و نوشتن همزمان و ناهمزمان پشتیبانی می‌کند. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای assertion می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای ارسال به توابع به عنوان آرگومان استفاده کنید. |
| [FileSystemInfo](./filesysteminfo/) | کلاس پایه برای [FileInfo](./fileinfo/) و [DirectoryInfo](./directoryinfo/) است. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای assertion می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای ارسال به توابع به عنوان آرگومان استفاده کنید. |
| [FileSystemInfoStat](./filesysteminfostat/) | نمایانگر اطلاعاتی درباره یک فایل یا دایرکتوری است. |
| [MemoryStream](./memorystream/) | نمایانگر یک جریان است که از حافظه می‌خواند و به آن می‌نویسد. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای assertion می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای ارسال به توابع به عنوان آرگومان استفاده کنید. |
| [Path](./path/) | متدهایی برای دستکاری مسیرها فراهم می‌کند. این یک نوع ایستا بدون سرویس‌های نمونه است. هرگز نباید به هیچ وجه نمونه‌ای از آن ایجاد کنید. |
| [STDIOStreamWrapperBase](./stdiostreamwrapperbase/) | کلاس پایه‌ای برای بسته‌بندی‌های شبیه [System.IO.Stream](./stream/) است. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای assertion می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای ارسال به توابع به عنوان آرگومان استفاده کنید. |
| [Stream](./stream/) | کلاس پایه‌ای برای انواع پیاده‌سازی‌های جریان است. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/ یا خطاهای assertion می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای ارسال به توابع به عنوان آرگومان استفاده کنید. |
| [StreamReader](./streamreader/) | نمایانگر یک خواننده است که کاراکترها را از یک جریان بایتی می‌خواند. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای assertion می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای ارسال به توابع به عنوان آرگومان استفاده کنید. |
| [StreamWriter](./streamwriter/) | نمایانگر یک نویسنده است که کاراکترها را به یک جریان بایتی می‌نویسد. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/ یا خطاهای assertion می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای ارسال به توابع به عنوان آرگومان استفاده کنید. |
| [StringReader](./stringreader/) | نمایانگر یک خواننده است که کاراکترها را از یک رشته می‌خواند. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای assertion می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای ارسال به توابع به عنوان آرگومان استفاده کنید. |
| [StringWriter](./stringwriter/) | پیاده‌سازی یک [TextWriter](./textwriter/) است که اطلاعات را به یک رشته می‌نویسد. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای assertion می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای ارسال به توابع به عنوان آرگومان استفاده کنید. |
| [TextReader](./textreader/) | کلاس پایه‌ای برای کلاس‌هایی است که خوانندگان را نشان می‌دهد که دنباله‌ای از کاراکترها را از منابع مختلف می‌خوانند. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای assertion می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای ارسال به توابع به عنوان آرگومان استفاده کنید. |
| [TextWriter](./textwriter/) | کلاس پایه‌ای برای کلاس‌هایی است که نویسندگان را نشان می‌دهد که دنباله‌ای از کاراکترها را به مقاصد مختلف می‌نویسند. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای assertion می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای ارسال به توابع به عنوان آرگومان استفاده کنید. |
| [UnmanagedMemoryStream](./unmanagedmemorystream/) | دسترسی به حافظه غیر مدیریت‌شده را فراهم می‌کند. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای assertion می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای ارسال به توابع به عنوان آرگومان استفاده کنید. |
## توابع

| تابع | توضیح |
| --- | --- |
| [SharedPtr](../system/sharedptr/)\<[Stream](./stream/)\> [WrapSTDIOStream](./wrapstdiostream/)(std::basic_istream\<char_type, traits_type\>\&, [STDIOStreamWrappingMode](./stdiostreamwrappingmode/)) | توابع پوشش‌دهنده برای جریان‌های شبیه std::basic_istream. |
| [SharedPtr](../system/sharedptr/)\<[Stream](./stream/)\> [WrapSTDIOStream](./wrapstdiostream/)(std::basic_ostream\<char_type, traits_type\>\&, [STDIOStreamWrappingMode](./stdiostreamwrappingmode/)) | توابع پوشش‌دهنده برای جریان‌های شبیه std::basic_ostream. |
| [SharedPtr](../system/sharedptr/)\<[Stream](./stream/)\> [WrapSTDIOStream](./wrapstdiostream/)(std::basic_iostream\<char_type, traits_type\>\&, [STDIOStreamWrappingMode](./stdiostreamwrappingmode/), [STDIOStreamPositionPreference](./stdiostreampositionpreference/)) | توابع پوشش‌دهنده برای جریان‌های شبیه std::basic_iostream. |
## شمارش‌ها

| شمارش | توضیح |
| --- | --- |
| [FileAccess](./fileaccess/) | نوع دسترسی را هنگام باز کردن فایل مشخص می‌کند. |
| [FileAttributes](./fileattributes/) | ویژگی‌های یک دایرکتوری یا یک فایل را نشان می‌دهد. |
| [FileMode](./filemode/) | نحوه باز کردن یک فایل را مشخص می‌کند. |
| [FileOptions](./fileoptions/) | گزینه‌های پیشرفته برای ایجاد شیء [FileStream](./filestream/) را نشان می‌دهد. |
| [FileShare](./fileshare/) | نوع دسترسی‌ای را که سایر اشیاء [FileStream](./filestream/) می‌توانند به فایلی که در حال باز شدن است داشته باشند، تعیین می‌کند. |
| [SearchOption](./searchoption/) | مشخص می‌کند جستجو فقط در دایرکتوری فعلی یا در دایرکتوری فعلی و تمام زیردایرکتوری‌های آن انجام شود. |
| [SeekOrigin](./seekorigin/) | موقعیت مرجع در جریان را نسبت به آن موقعیتی که باید به سمت آن جستجو شود، مشخص می‌کند. |
| [STDIOStreamWrappingMode](./stdiostreamwrappingmode/) | حالت عملیات I/O را که بسته‌بندها بر روی جریان‌های شبیه std::iostream انجام می‌دهند، تعیین می‌کند. |
| [STDIOStreamPositionPreference](./stdiostreampositionpreference/) | مشخص می‌کند کدام موقعیت در جریان به عنوان موقعیت مشترک خواندن و نوشتن ترجیح داده می‌شود زمانی که std::basic_iostream و اشیای مشتقه آن در زمان ایجاد بسته‌بند، موقعیت‌های خواندن و نوشتن متفاوتی داشته باشند. |
| [SystemIOStreamWrappingMode](./systemiostreamwrappingmode/) | حالت عملیات I/O را که بسته‌بندها بر روی جریان‌های شبیه [System::IO::Stream](./stream/) انجام می‌دهند، تعیین می‌کند. |
## تعریف‌های نوع

| تعریف | توضیح |
| --- | --- |
| [IOException](./ioexception/) |  |
| [EndOfStreamException](./endofstreamexception/) |  |
| [InvalidDataException](./invaliddataexception/) |  |
| [DirectoryNotFoundException](./directorynotfoundexception/) |  |
| [FileLoadException](./fileloadexception/) |  |
| [PathTooLongException](./pathtoolongexception/) |  |
| [DriveNotFoundException](./drivenotfoundexception/) |  |
| [BinaryWriterPtr](./binarywriterptr/) | یک نام دیگر برای یک اشاره‌گر مشترک به این کلاس. |
| [FileNotFoundException](./filenotfoundexception/) | استثنایی است که هنگام تلاش برای دسترسی به فایلی که روی دیسک وجود ندارد و شکست می‌خورد، پرتاب می‌شود. هرگز نمونه‌های کلاس FileNotFoundException را در [System::SmartPtr](../system/smartptr/) بسته‌بندی نکنید. |
| [STDIStreamWrapper](./stdistreamwrapper/) | تخصیص‌های خاص [BasicSTDIStreamWrapper](./basicstdistreamwrapper/) برای انواع کاراکتر char. |
| [STDWIStreamWrapper](./stdwistreamwrapper/) | تخصیص‌های خاص [BasicSTDIStreamWrapper](./basicstdistreamwrapper/) برای انواع کاراکتر **wchar_t**. |
| [STDOStreamWrapper](./stdostreamwrapper/) | تخصیص‌های خاص [BasicSTDOStreamWrapper](./basicstdostreamwrapper/) برای انواع کاراکتر char. |
| [STDWOStreamWrapper](./stdwostreamwrapper/) | تخصیص‌های خاص [BasicSTDOStreamWrapper](./basicstdostreamwrapper/) برای انواع کاراکتر **wchar_t**. |
| [STDIOStreamWrapper](./stdiostreamwrapper/) | تخصیص‌های خاص [BasicSTDIOStreamWrapper](./basicstdiostreamwrapper/) برای انواع کاراکتر char. |
| [STDWIOStreamWrapper](./stdwiostreamwrapper/) | تخصیص‌های خاص [BasicSTDIOStreamWrapper](./basicstdiostreamwrapper/) برای انواع کاراکتر **wchar_t**. |
| [SystemIStreamWrapper](./systemistreamwrapper/) | تخصیص‌های خاص [BasicSystemIStreamWrapper](./basicsystemistreamwrapper/) برای انواع کاراکتر char. |
| [SystemWIStreamWrapper](./systemwistreamwrapper/) | تخصیص‌های خاص [BasicSystemIStreamWrapper](./basicsystemistreamwrapper/) برای انواع کاراکتر **wchar_t**. |
| [SystemOStreamWrapper](./systemostreamwrapper/) | تخصیص‌های خاص [BasicSystemOStreamWrapper](./basicsystemostreamwrapper/) برای انواع کاراکتر char. |
| [SystemWOStreamWrapper](./systemwostreamwrapper/) | تخصیص‌های خاص [BasicSystemOStreamWrapper](./basicsystemostreamwrapper/) برای انواع کاراکتر **wchar_t**. |
| [SystemIOStreamWrapper](./systemiostreamwrapper/) | تخصیص‌های خاص [BasicSystemIOStreamWrapper](./basicsystemiostreamwrapper/) برای انواع کاراکتر char. |
| [SystemWIOStreamWrapper](./systemwiostreamwrapper/) | تخصیص‌های خاص [BasicSystemIOStreamWrapper](./basicsystemiostreamwrapper/) برای انواع کاراکتر **wchar_t**. |