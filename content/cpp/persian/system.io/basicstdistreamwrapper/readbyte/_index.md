---
title: ReadByte()
second_title: Aspose.Slides برای C++ مرجع API
description: اگر حالت بسته‌بندی باینری باشد، یک بایت واحد را از ذخیره‌سازی آخرین کاراکتر رمزگشایی‌شده می‌خواند، در غیر این صورت یک کاراکتر واحد را از جریان می‌خواند و به نوع uint8_t تبدیل می‌کند.
type: docs
weight: 40
url: /fa/system.io/basicstdistreamwrapper/readbyte/
---
## BasicSTDIStreamWrapper::ReadByte() متد


اگر حالت بسته‌بندی باینری باشد، یک بایت تنها را از ذخیره‌سازی کاراکترهای رمزگشایی‌شدهٔ آخر می‌خواند، در غیر این صورت یک کاراکتر تنها را از جریان می‌خواند و به نوع **uint8_t** تبدیل می‌کند.

```cpp
virtual int System::IO::BasicSTDIStreamWrapper<T, typename>::ReadByte() override
```


### مقدار بازگشت

مقدار عدد صحیح ۳۲ بیتی معادل نتیجهٔ خواندن

## مراجعه کنید

* کلاس [BasicSTDIStreamWrapper](../)
* فضای‌نام [System::IO](../../)
* کتابخانه [Aspose.Slides](../../../)