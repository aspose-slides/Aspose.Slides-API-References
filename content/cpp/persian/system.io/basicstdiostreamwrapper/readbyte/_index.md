---
title: ReadByte()
second_title: Aspose.Slides برای C++ مرجع API
description: اگر حالت بسته‌بندی باینری باشد، یک بایت از آخرین حافظهٔ کاراکترهای رمزگشایی‌شده می‌خواند، در غیر این صورت یک کاراکتر از جریان می‌خواند و به نوع uint8_t تبدیل می‌کند.
type: docs
weight: 40
url: /fa/system.io/basicstdiostreamwrapper/readbyte/
---
## BasicSTDIOStreamWrapper::ReadByte() متد


اگر حالت بسته‌بندی باینری باشد، یک بایت از آخرین حافظهٔ کاراکترهای رمزگشایی‌شده می‌خواند، در غیر این صورت یک کاراکتر از جریان می‌خواند و به نوع **uint8_t** تبدیل می‌کند.

```cpp
virtual int System::IO::BasicSTDIOStreamWrapper<T, typename>::ReadByte() override
```


### مقدار بازگشت

یک مقدار عددی ۳۲ بیتی معادل نتیجهٔ خواندن

## موارد مرتبط

* کلاس [BasicSTDIOStreamWrapper](../)
* فضای نام [System::IO](../../)
* کتابخانه [Aspose.Slides](../../../)