---
title: TransformVectors()
second_title: مرجع API Aspose.Slides برای C++
description: فقط مؤلفه‌های مقیاس و چرخش ماتریسی که توسط شیء فعلی نمایان می‌شود را بر نقاط مشخص‌شده اعمال می‌کند.
type: docs
weight: 209
url: /fa/system.drawing.drawing2d/matrix/transformvectors/
---
## Matrix::TransformVectors(const ArrayPtr\<Point\>\&) متد

فقط اجزای مقیاس و چرخش ماتریس نمایان‌شده توسط شیء فعلی را بر نقاط مشخص‌شده اعمال می‌کند.

```cpp
void System::Drawing::Drawing2D::Matrix::TransformVectors(const ArrayPtr<Point> &pts)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| pts | const [ArrayPtr](../../../system/arrayptr/)\<[Point](../../../system.drawing/point/)\>\& | آرایه‌ای حاوی نقاط برای تبدیل |

## Matrix::TransformVectors(const System::Details::ArrayView\<Point\>\&) متد

فقط اجزای مقیاس و چرخش ماتریس نمایان‌شده توسط شیء فعلی را بر نقاط مشخص‌شده اعمال می‌کند.

```cpp
void System::Drawing::Drawing2D::Matrix::TransformVectors(const System::Details::ArrayView<Point> &pts)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| pts | const System::Details::ArrayView\<[Point](../../../system.drawing/point/)\>\& | نمایی از آرایه حاوی نقاط برای تبدیل |

## Matrix::TransformVectors(const ArrayPtr\<PointF\>\&) متد

فقط اجزای مقیاس و چرخش ماتریس نمایان‌شده توسط شیء فعلی را بر نقاط مشخص‌شده اعمال می‌کند.

```cpp
void System::Drawing::Drawing2D::Matrix::TransformVectors(const ArrayPtr<PointF> &pts)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| pts | const [ArrayPtr](../../../system/arrayptr/)\<[PointF](../../../system.drawing/pointf/)\>\& | آرایه‌ای حاوی نقاط برای تبدیل |

## Matrix::TransformVectors(const System::Details::ArrayView\<PointF\>\&) متد

فقط اجزای مقیاس و چرخش ماتریس نمایان‌شده توسط شیء فعلی را بر نقاط مشخص‌شده اعمال می‌کند.

```cpp
void System::Drawing::Drawing2D::Matrix::TransformVectors(const System::Details::ArrayView<PointF> &pts)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| pts | const System::Details::ArrayView\<[PointF](../../../system.drawing/pointf/)\>\& | نمایی از آرایه حاوی نقاط برای تبدیل |

## موارد مرتبط

* تعریف‌نوع [ArrayPtr](../../../system/arrayptr/)
* کلاس [Point](../../../system.drawing/point/)
* کلاس [Matrix](../)
* کلاس [PointF](../../../system.drawing/pointf/)
* فضای‌نام [System::Drawing::Drawing2D](../../)
* کتابخانه [Aspose.Slides](../../../)