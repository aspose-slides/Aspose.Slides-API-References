---
title: TransformPoints()
second_title: Aspose.Slides برای C++ مستندات API
description: تبدیل هندسی تعریف‌شده توسط ماتریسی که توسط شیء فعلی نمایندگی می‌شود را بر نقاط مشخص‌شده اعمال می‌کند.
type: docs
weight: 196
url: /fa/system.drawing.drawing2d/matrix/transformpoints/
---
## Matrix::TransformPoints(const ArrayPtr\<Point\>\&) method

تبدیل هندسی تعریف‌شده توسط ماتریسی که توسط شیء فعلی نمایندگی می‌شود را بر نقاط مشخص‌شده اعمال می‌کند.

```cpp
void System::Drawing::Drawing2D::Matrix::TransformPoints(const ArrayPtr<Point> &pts)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| pts | const [ArrayPtr](../../../system/arrayptr/)\<[Point](../../../system.drawing/point/)\>\& | آرایه‌ای شامل نقاط برای تبدیل |

## Matrix::TransformPoints(const System::Details::ArrayView\<Point\>\&) method

تبدیل هندسی تعریف‌شده توسط ماتریسی که توسط شیء فعلی نمایندگی می‌شود را بر نقاط مشخص‌شده اعمال می‌کند.

```cpp
void System::Drawing::Drawing2D::Matrix::TransformPoints(const System::Details::ArrayView<Point> &pts)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| pts | const System::Details::ArrayView\<[Point](../../../system.drawing/point/)\>\& | نمایی از آرایه شامل نقاط برای تبدیل |

## Matrix::TransformPoints(const ArrayPtr\<PointF\>\&) method

تبدیل هندسی تعریف‌شده توسط ماتریسی که توسط شیء فعلی نمایندگی می‌شود را بر نقاط مشخص‌شده اعمال می‌کند.

```cpp
void System::Drawing::Drawing2D::Matrix::TransformPoints(const ArrayPtr<PointF> &pts)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| pts | const [ArrayPtr](../../../system/arrayptr/)\<[PointF](../../../system.drawing/pointf/)\>\& | آرایه‌ای شامل نقاط برای تبدیل |

## Matrix::TransformPoints(const System::Details::ArrayView\<PointF\>\&) method

تبدیل هندسی تعریف‌شده توسط ماتریسی که توسط شیء فعلی نمایندگی می‌شود را بر نقاط مشخص‌شده اعمال می‌کند.

```cpp
void System::Drawing::Drawing2D::Matrix::TransformPoints(const System::Details::ArrayView<PointF> &pts)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| pts | const System::Details::ArrayView\<[PointF](../../../system.drawing/pointf/)\>\& | نمایی از آرایه شامل نقاط برای تبدیل |

## موارد مرتبط

* Typedef [ArrayPtr](../../../system/arrayptr/)
* کلاس [Point](../../../system.drawing/point/)
* کلاس [Matrix](../)
* کلاس [PointF](../../../system.drawing/pointf/)
* فضای نام [System::Drawing::Drawing2D](../../)
* کتابخانه [Aspose.Slides](../../../)