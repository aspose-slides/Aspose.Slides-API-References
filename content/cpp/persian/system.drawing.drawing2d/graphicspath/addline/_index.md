---
title: AddLine()
second_title: مرجع API Aspose.Slides برای C++
description: خط مشخص‌شده را به مسیری که توسط شیء فعلی نمایانده می‌شود اضافه می‌کند.
type: docs
weight: 144
url: /fa/system.drawing.drawing2d/graphicspath/addline/
---
## GraphicsPath::AddLine(const Point&, const Point&) متد

خط مشخص‌شده را به مسیری که توسط شیء جاری نماینده است اضافه می‌کند.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddLine(const Point &pt1, const Point &pt2)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| pt1 | const [Point](../../../system.drawing/point/)\& | نقطه شروع خط برای اضافه کردن |
| pt2 | const [Point](../../../system.drawing/point/)\& | نقطه پایان خط برای اضافه کردن |

## GraphicsPath::AddLine(const PointF&, const PointF&) متد

خط مشخص‌شده را به مسیری که توسط شیء جاری نماینده است اضافه می‌کند.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddLine(const PointF &pt1, const PointF &pt2)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| pt1 | const [PointF](../../../system.drawing/pointf/)\& | نقطه شروع خط برای اضافه کردن |
| pt2 | const [PointF](../../../system.drawing/pointf/)\& | نقطه پایان خط برای اضافه کردن |

## GraphicsPath::AddLine(int, int, int, int) متد

خط مشخص‌شده را به مسیری که توسط شیء جاری نماینده است اضافه می‌کند.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddLine(int x1, int y1, int x2, int y2)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| x1 | int | مختصات X نقطه شروع خط برای اضافه کردن |
| y1 | int | مختصات Y نقطه شروع خط برای اضافه کردن |
| x2 | int | مختصات X نقطه پایان خط برای اضافه کردن |
| y2 | int | مختصات Y نقطه پایان خط برای اضافه کردن |

## GraphicsPath::AddLine(float, float, float, float) متد

خط مشخص‌شده را به مسیری که توسط شیء جاری نماینده است اضافه می‌کند.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddLine(float x1, float y1, float x2, float y2)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| x1 | **float** | مختصات X نقطه شروع خط برای اضافه کردن |
| y1 | **float** | مختصات Y نقطه شروع خط برای اضافه کردن |
| x2 | **float** | مختصات X نقطه پایان خط برای اضافه کردن |
| y2 | **float** | مختصات Y نقطه پایان خط برای اضافه کردن |

## موارد مرتبط

* کلاس [Point](../../../system.drawing/point/)
* کلاس [GraphicsPath](../)
* کلاس [PointF](../../../system.drawing/pointf/)
* فضای‌نام [System::Drawing::Drawing2D](../../)
* کتابخانه [Aspose.Slides](../../../)