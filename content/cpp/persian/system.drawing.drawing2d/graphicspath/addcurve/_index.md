---
title: AddCurve()
second_title: مرجع API Aspose.Slides برای C++
description: منحنی مشخص‌شده را به مسیری که توسط شیء فعلی نشان داده می‌شود اضافه می‌کند.
type: docs
weight: 274
url: /fa/system.drawing.drawing2d/graphicspath/addcurve/
---
## GraphicsPath::AddCurve(const ArrayPtr\<PointF\>\&, float) متد

منحنی مشخص‌شده را به مسیری که توسط شیء فعلی نشان داده می‌شود اضافه می‌کند.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddCurve(const ArrayPtr<PointF> &points, float tension=0.5)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| points | const [ArrayPtr](../../../system/arrayptr/)\<[PointF](../../../system.drawing/pointf/)\>\& | نقاطی که منحنی را مشخص می‌کنند |
| tension | **float** | مقدار خمیدگی منحنی بین نقاط کنترل را تعیین می‌کند |

## GraphicsPath::AddCurve(const ArrayPtr\<Point\>\&, float) متد

منحنی مشخص‌شده را به مسیری که توسط شیء فعلی نشان داده می‌شود اضافه می‌کند.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddCurve(const ArrayPtr<Point> &points, float tension=0.5)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| points | const [ArrayPtr](../../../system/arrayptr/)\<[Point](../../../system.drawing/point/)\>\& | نقاطی که منحنی را مشخص می‌کنند |
| tension | **float** | مقدار خمیدگی منحنی بین نقاط کنترل را تعیین می‌کند |

## GraphicsPath::AddCurve(const ArrayPtr\<PointF\>\&, int, int, float) متد

منحنی مشخص‌شده را به مسیری که توسط شیء فعلی نشان داده می‌شود اضافه می‌کند.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddCurve(const ArrayPtr<PointF> &points, int offset, int number_of_segments, float tension)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| points | const [ArrayPtr](../../../system/arrayptr/)\<[PointF](../../../system.drawing/pointf/)\>\& | نقاطی که منحنی را مشخص می‌کنند |
| offset | int | اندیس نقطه‌ای در **points** که به عنوان نقطه شروع منحنی استفاده می‌شود |
| number_of_segments | int | تعداد بخش‌هایی که برای رسم منحنی استفاده می‌شود |
| tension | **float** | مقدار خمیدگی منحنی بین نقاط کنترل را تعیین می‌کند |

## GraphicsPath::AddCurve(const ArrayPtr\<Point\>\&, int, int, float) متد

منحنی مشخص‌شده را به مسیری که توسط شیء فعلی نشان داده می‌شود اضافه می‌کند.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddCurve(const ArrayPtr<Point> &points, int offset, int number_of_segments, float tension)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| points | const [ArrayPtr](../../../system/arrayptr/)\<[Point](../../../system.drawing/point/)\>\& | نقاطی که منحنی را مشخص می‌کنند |
| offset | int | اندیس نقطه‌ای در **points** که به عنوان نقطه شروع منحنی استفاده می‌شود |
| number_of_segments | int | تعداد بخش‌هایی که برای رسم منحنی استفاده می‌شود |
| tension | **float** | مقدار خمیدگی منحنی بین نقاط کنترل را تعیین می‌کند |

## موارد مرتبط

* Typedef [ArrayPtr](../../../system/arrayptr/)
* Class [PointF](../../../system.drawing/pointf/)
* Class [GraphicsPath](../)
* Class [Point](../../../system.drawing/point/)
* Namespace [System::Drawing::Drawing2D](../../)
* Library [Aspose.Slides](../../../)