---
title: IsVisible()
second_title: مرجع API Aspose.Slides برای C++
description: مشخص می‌کند که آیا نقطهٔ مشخص‌شده در مسیری که توسط شیء فعلی نمایندگی می‌شود، قرار دارد یا خیر.
type: docs
weight: 417
url: /fa/system.drawing.drawing2d/graphicspath/isvisible/
---
## GraphicsPath::IsVisible(const PointF\&) متد

مشخص می‌کند که آیا نقطهٔ مشخص‌شده در مسیری که توسط شیء فعلی نمایندگی می‌شود، قرار دارد یا خیر.

```cpp
bool System::Drawing::Drawing2D::GraphicsPath::IsVisible(const PointF &point)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| point | const [PointF](../../../system.drawing/pointf/)\& | نقطهٔ مورد بررسی |

## GraphicsPath::IsVisible(float, float) متد

مشخص می‌کند که آیا نقطهٔ مشخص‌شده در مسیری که توسط شیء فعلی نمایندگی می‌شود، قرار دارد یا خیر.

```cpp
bool System::Drawing::Drawing2D::GraphicsPath::IsVisible(float x, float y)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| x | **float** | مختصات X نقطهٔ مورد بررسی |
| y | **float** | مختصات Y نقطهٔ مورد بررسی |

## موارد مرتبط

* کلاس [PointF](../../../system.drawing/pointf/)
* کلاس [GraphicsPath](../)
* فضای‌نام [System::Drawing::Drawing2D](../../)
* کتابخانه [Aspose.Slides](../../../)