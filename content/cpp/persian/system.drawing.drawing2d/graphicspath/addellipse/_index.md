---
title: AddEllipse()
second_title: مرجع API Aspose.Slides برای C++
description: الیس مشخص‌شده را به مسیری که توسط شیء فعلی نمایش داده می‌شود اضافه می‌کند.
type: docs
weight: 196
url: /fa/system.drawing.drawing2d/graphicspath/addellipse/
---
## GraphicsPath::AddEllipse(float, float, float, float) متد

الِیپس مشخص‌شده را به مسیری که توسط شیء فعلی نمایان شده است اضافه می‌کند.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddEllipse(float x, float y, float width, float height)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| x | **float** | مختصات X گوشهٔ بالایی سمت چپ مستطیلی که مرزهای الیپس را تعیین می‌کند |
| y | **float** | مختصات Y گوشهٔ بالایی سمت چپ مستطیلی که مرزهای الیپس را تعیین می‌کند |
| width | **float** | عرض مستطیلی که مرزهای الیپس را تعیین می‌کند |
| height | **float** | ارتفاع مستطیلی که مرزهای الیپس را تعیین می‌کند |

## GraphicsPath::AddEllipse(int, int, int, int) متد

الِیپس مشخص‌شده را به مسیری که توسط شیء فعلی نمایان شده است اضافه می‌کند.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddEllipse(int x, int y, int width, int height)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| x | int | مختصات X گوشهٔ بالایی سمت چپ مستطیلی که مرزهای الیپس را تعیین می‌کند |
| y | int | مختصات Y گوشهٔ بالایی سمت چپ مستطیلی که مرزهای الیپس را تعیین می‌کند |
| width | int | عرض مستطیلی که مرزهای الیپس را تعیین می‌کند |
| height | int | ارتفاع مستطیلی که مرزهای الیپس را تعیین می‌کند |

## GraphicsPath::AddEllipse(const RectangleF\&) متد

الِیپس مشخص‌شده را به مسیری که توسط شیء فعلی نمایان شده است اضافه می‌کند.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddEllipse(const RectangleF &rect)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| rect | const [RectangleF](../../../system.drawing/rectanglef/)\& | مستطیلی را که مرزهای الیپس مورد اضافه شدن را تعیین می‌کند، مشخص می‌کند |

## GraphicsPath::AddEllipse(const Rectangle\&) متد

الِیپس مشخص‌شده را به مسیری که توسط شیء فعلی نمایان شده است اضافه می‌کند.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddEllipse(const Rectangle &rect)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| rect | const [Rectangle](../../../system.drawing/rectangle/)\& | مستطیلی را که مرزهای الیپس مورد اضافه شدن را تعیین می‌کند، مشخص می‌کند |

## همچنین ببینید

* کلاس [GraphicsPath](../)
* کلاس [RectangleF](../../../system.drawing/rectanglef/)
* کلاس [Rectangle](../../../system.drawing/rectangle/)
* فضای‌نام [System::Drawing::Drawing2D](../../)
* کتابخانه [Aspose.Slides](../../../)