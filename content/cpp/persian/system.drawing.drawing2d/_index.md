---
title: "System::Drawing::Drawing2D"
second_title: مرجع API Aspose.Slides برای C++
description: 
type: docs
weight: 495
url: /fa/system.drawing.drawing2d/
---
## کلاس‌ها

| کلاس | توضیح |
| --- | --- |
| [AdjustableArrowCap](./adjustablearrowcap/) | یک سر خط شکل‌دار پیکان‌دار قابل تنظیم را نشان می‌دهد. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا اشکالات وارسی می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [Blend](./blend/) | یک الگوی ترکیب برای شیء [LinearGradientBrush](./lineargradientbrush/) را نشان می‌دهد. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا اشکالات وارسی می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [ColorBlend](./colorblend/) | آرایه‌هایی از رنگ‌ها و موقعیت‌ها را که برای درون‌یابی ترکیب رنگ در یک گرادیان چندرنگ استفاده می‌شوند، شامل می‌شود. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا اشکالات وارسی می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [CustomLineCap](./customlinecap/) | یک سر خط تعریف‌شده توسط کاربر را نشان می‌دهد. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا اشکالات وارسی می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [GraphicsContainer](./graphicscontainer/) | داده‌های داخلی یک محفظه گرافیکی را شامل می‌شود. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا اشکالات وارسی می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [GraphicsPath](./graphicspath/) | یک مجموعه‌ای از خطوط و منحنی‌های متصل را نشان می‌دهد. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا اشکالات وارسی می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [GraphicsState](./graphicsstate/) | وضعیت یک شیء [Graphics](../system.drawing/graphics/) را نشان می‌دهد. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا اشکالات وارسی می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [HatchBrush](./hatchbrush/) | یک براش مستطیلی با سبک هشت، رنگ پیش‌زمینه و رنگ پس‌زمینه را نشان می‌دهد. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا اشکالات وارسی می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [LinearGradientBrush](./lineargradientbrush/) | یک براش گرادیان خطی را نشان می‌دهد. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا اشکالات وارسی می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [Matrix](./matrix/) | یک ماتریس 3x3 که عملیات تبدیل را تعریف می‌کند، نشان می‌دهد. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا اشکالات وارسی می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [PathData](./pathdata/) | داده‌های گرافیکی که مسیر را نشان می‌دهند، شامل می‌شود. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا اشکالات وارسی می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [PathGradientBrush](./pathgradientbrush/) | یک براش که داخلی یک شیء [GraphicsPath](./graphicspath/) را با یک گرادیان پر می‌کند، نشان می‌دهد. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا اشکالات وارسی می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
| [RegionData](./regiondata/) | داده‌هایی که یک ناحیه را تعریف می‌کنند، شامل می‌شود. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا اشکالات وارسی می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای انتقال به توابع به عنوان آرگومان استفاده کنید. |
## شمارش‌ها

| شمارش | توضیح |
| --- | --- |
| [CombineMode](./combinemode/) | نحوه ترکیب نواحی برش را مشخص می‌کند. |
| [CompositingMode](./compositingmode/) | نحوه ترکیب رنگ‌های منبع و رنگ‌های پس‌زمینه را مشخص می‌کند. |
| [CompositingQuality](./compositingquality/) | سطح کیفیتی که در ترکیب استفاده می‌شود را مشخص می‌کند. |
| [CoordinateSpace](./coordinatespace/) | نحوه ارزیابی مختصات را مشخص می‌کند. |
| [DashCap](./dashcap/) | نوع سر انتهایی که در هر دو سر یک خط نقطه‌دار استفاده می‌شود را مشخص می‌کند. |
| [DashStyle](./dashstyle/) | سبک یک خط نقطه‌دار را مشخص می‌کند. |
| [FillMode](./fillmode/) | نحوه پر کردن داخلی یک مسیر بسته را مشخص می‌کند. |
| [FlushIntention](./flushintention/) | اینکه دستورات در پشته گرافیک بلافاصله خاتمه یابند یا هرچه زودتر اجرا شوند را مشخص می‌کند. |
| [HatchStyle](./hatchstyle/) | الگوهای براش [HatchBrush](./hatchbrush/) را مشخص می‌کند. |
| [InterpolationMode](./interpolationmode/) | الگوریتمی که هنگام چرخش یا مقیاس‌گذاری تصاویر استفاده می‌شود را مشخص می‌کند. |
| [LineCap](./linecap/) | سبک‌های سر قابل استفاده که یک شیء [Pen](../system.drawing/pen/) می‌تواند خط را به پایان برساند را نشان می‌دهد. |
| [LineJoin](./linejoin/) | نحوه اتصال خطوط یا منحنی‌های متوالی را مشخص می‌کند. |
| [LinearGradientMode](./lineargradientmode/) | یک جهت گرادیان خطی را نشان می‌دهد. |
| [MatrixOrder](./matrixorder/) | ترتیب عملیات تبدیل ماتریسی را مشخص می‌کند. |
| [PathPointType](./pathpointtype/) | نوع یک نقطه مسیر را مشخص می‌کند. |
| [PenAlignment](./penalignment/) | تراز یک شیء [Pen](../system.drawing/pen/) نسبت به یک خط مرجع انتزاعی با عرض صفر را مشخص می‌کند. |
| [PenType](./pentype/) | نوعی که یک شیء [Pen](../system.drawing/pen/) برای پر کردن استفاده می‌کند را مشخص می‌کند. |
| [PixelFormat](./pixelformat/) | قالب دادهٔ رنگ یک پیکسل را مشخص می‌کند. |
| [PixelOffsetMode](./pixeloffsetmode/) | نحوه جابجایی پیکسل‌ها هنگام رندر شدن را مشخص می‌کند. |
| [QualityMode](./qualitymode/) | کیفیت کلی رندرینگ را مشخص می‌کند. |
| [SmoothingMode](./smoothingmode/) | نوع صاف‌سازی (ضد لبه) که بر خطوط، منحنی‌ها و لبه‌های مناطق پر شده اعمال می‌شود را مشخص می‌کند. |
| [WrapMode](./wrapmode/) | نحوه تکرار یک بافت یا گرادیان زمانی که کوچکتر از ناحیهٔ پرشده باشد را مشخص می‌کند. |
## تعاریف نوع

| تعریف نوع | توضیح |
| --- | --- |
| [MatrixPtr](./matrixptr/) | یک نام مستعار برای یک اشاره‌گر مشترک به نمونه‌ای از کلاس [Matrix](./matrix/) است. |