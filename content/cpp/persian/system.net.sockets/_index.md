---
title: "System::Net::Sockets"
second_title: مرجع API Aspose.Slides برای C++
description: 
type: docs
weight: 729
url: /fa/system.net.sockets/
---
## کلاس‌ها

| کلاس | توضیح |
| --- | --- |
| [Details_SocketException](./details_socketexception/) | نشانگر استثناء‌ای است که هنگام وقوع خطای سوکت پرتاب می‌شود. هرگز نمونه‌های این کلاس را به‌صورت دستی ایجاد نکنید. به‌جای آن از کلاس SocketException استفاده کنید. هرگز نمونه‌های کلاس SocketException را در [System::SmartPtr](../system/smartptr/) بسته‌بندی نکنید. |
| [IPPacketInformation](./ippacketinformation/) | اطلاعات مربوط به بسته را نشان می‌دهد. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا باعث خطاهای زمان اجرا و/یا نقص‌های صحت‌سنجی می‌شود. همیشه این کلاس را در نشانگر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این نشانگر برای ارسال به توابع به‌عنوان آرگومان استفاده کنید. |
| [LingerOption](./lingeroption/) | مشخص می‌کند که آیا سوکت پس از فراخوانی متدهای Close() یا Close() متصل می‌ماند یا نه. همچنین بازه‌ی زمانی که سوکت در صورت ادامه ارسال داده‌ها متصل می‌ماند را تعیین می‌کند. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا باعث خطاهای زمان اجرا و/یا نقص‌های صحت‌سنجی می‌شود. همیشه این کلاس را در نشانگر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این نشانگر برای ارسال به توابع به‌عنوان آرگومان استفاده کنید. |
| [NetworkStream](./networkstream/) | جریان پایه داده‌ها برای دسترسی شبکه را فراهم می‌کند. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا باعث خطاهای زمان اجرا و/یا نقص‌های صحت‌سنجی می‌شود. همیشه این کلاس را در نشانگر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این نشانگر برای ارسال به توابع به‌عنوان آرگومان استفاده کنید. |
| [Socket](./socket/) | کلاس [Socket](./socket/) رابط سوکت‌های برکلی را پیاده‌سازی می‌کند. |
| [TcpClient](./tcpclient/) | نماینده‌ای برای سرویس‌های شبکه TCP است. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا باعث خطاهای زمان اجرا و/یا نقص‌های صحت‌سنجی می‌شود. همیشه این کلاس را در نشانگر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این نشانگر برای ارسال به توابع به‌عنوان آرگومان استفاده کنید. |
| [TcpListener](./tcplistener/) | یک شنونده برای سرویس‌های شبکه TCP است. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا باعث خطاهای زمان اجرا و/یا نقص‌های صحت‌سنجی می‌شود. همیشه این کلاس را در نشانگر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این نشانگر برای ارسال به توابع به‌عنوان آرگومان استفاده کنید. |
| [UdpClient](./udpclient/) | سرویس‌های شبکه پروتکل دیتاگرام کاربر (UDP) را فراهم می‌کند. اشیای این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا باعث خطاهای زمان اجرا و/یا نقص‌های صحت‌سنجی می‌شود. همیشه این کلاس را در نشانگر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این نشانگر برای ارسال به توابع به‌عنوان آرگومان استفاده کنید. |

## توابع

| تابع | توضیح |
| --- | --- |
| **bool** [operator==](./operator_equal_equal/)([IPPacketInformation](./ippacketinformation/), [IPPacketInformation](./ippacketinformation/)) |  |
| **bool** [operator!=](./operator_not_equal/)([IPPacketInformation](./ippacketinformation/), [IPPacketInformation](./ippacketinformation/)) |  |

## شمارش‌ها

| شمارش | توضیح |
| --- | --- |
| [SocketType](./sockettype/) | انواع سوکت را فهرست می‌کند. |
| [AddressFamily](./addressfamily/) | خانواده‌های آدرس را فهرست می‌کند. |
| [IOControlCode](./iocontrolcode/) | کدهای کنترل [IO](../system.io/) را فهرست می‌کند. |
| [ProtocolFamily](./protocolfamily/) | خانواده‌های پروتکل را فهرست می‌کند. |
| [ProtocolType](./protocoltype/) | انواع پروتکل را فهرست می‌کند. |
| [SelectMode](./selectmode/) | حالت نظرسنجی وضعیت سوکت را مشخص می‌کند. |
| [SocketError](./socketerror/) | انواع خطای سوکت را فهرست می‌کند. |
| [SocketFlags](./socketflags/) | مقادیر ثابت پیام‌های سوکت را فراهم می‌کند. |
| [SocketOptionLevel](./socketoptionlevel/) | سطوح گزینه سوکت برای کلاس '[Socket](./socket/)' را تعریف می‌کند. |
| [SocketOptionName](./socketoptionname/) | نام‌های گزینه سوکت برای کلاس [Socket](./socket/) را تعریف می‌کند. |
| [SocketShutdown](./socketshutdown/) | ثابت‌های مورد استفاده توسط متد [Socket.Shutdown](./socket/shutdown/) را تعریف می‌کند. |

## تعریف‌نوع‌ها

| تعریف‌نوع | توضیح |
| --- | --- |
| [SocketException](./socketexception/) |  |