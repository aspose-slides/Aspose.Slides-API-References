---
title: get_ExclusiveAddressUse()
second_title: Aspose.Slides برای مرجع API C++
description: مقداری را برمی‌گرداند که نشان می‌دهد آیا فقط یک فرآیند می‌تواند سوکت را به یک پورت متصل کند.
type: docs
weight: 170
url: /fa/system.net.sockets/socket/get_exclusiveaddressuse/
---
## Socket::get_ExclusiveAddressUse() متد

یک مقدار را می‌گیرد که نشان می‌دهد آیا فقط یک فرآیند می‌تواند سوکت را به پورت متصل کند.

```cpp
bool System::Net::Sockets::Socket::get_ExclusiveAddressUse()
```

### مقدار بازگشت

مقداری که نشان می‌دهد آیا فقط یک فرآیند می‌تواند سوکت را به پورت متصل کند.

## موارد مرتبط

* کلاس [Socket](../)
* فضای‌نام [System::Net::Sockets](../../)
* کتابخانه [Aspose.Slides](../../../)