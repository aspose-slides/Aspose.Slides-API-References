---
title: SetSocketOption()
second_title: مرجع API Aspose.Slides برای C++
description: گزینهٔ سوکت مشخص‌شده را به مقدار مشخص‌شده تنظیم می‌کند.
type: docs
weight: 716
url: /fa/system.net.sockets/socket/setsocketoption/
---
## Socket::SetSocketOption(SocketOptionLevel, SocketOptionName, int32_t) متد

گزینهٔ سوکت مشخص‌شده را به مقدار مشخص‌شده تنظیم می‌کند.

```cpp
void System::Net::Sockets::Socket::SetSocketOption(SocketOptionLevel optionLevel, SocketOptionName optionName, int32_t optionValue)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| optionLevel | [SocketOptionLevel](../../socketoptionlevel/) | سطح گزینهٔ سوکت. |
| optionName | [SocketOptionName](../../socketoptionname/) | نام گزینه‌ای که باید به‌روزرسانی شود. |
| optionValue | **int32_t** | مقداری که باید برای گزینهٔ مشخص‌شده تنظیم شود. |

## Socket::SetSocketOption(SocketOptionLevel, SocketOptionName, System::ArrayPtr\<uint8_t\>) متد

گزینهٔ سوکت مشخص‌شده را به مقدار مشخص‌شده تنظیم می‌کند.

```cpp
void System::Net::Sockets::Socket::SetSocketOption(SocketOptionLevel optionLevel, SocketOptionName optionName, System::ArrayPtr<uint8_t> optionValue)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| optionLevel | [SocketOptionLevel](../../socketoptionlevel/) | سطح گزینهٔ سوکت. |
| optionName | [SocketOptionName](../../socketoptionname/) | نام گزینه‌ای که باید به‌روزرسانی شود. |
| optionValue | [System::ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | مقداری که باید برای گزینهٔ مشخص‌شده تنظیم شود. |

## Socket::SetSocketOption(SocketOptionLevel, SocketOptionName, bool) متد

گزینهٔ سوکت مشخص‌شده را به مقدار مشخص‌شده تنظیم می‌کند.

```cpp
void System::Net::Sockets::Socket::SetSocketOption(SocketOptionLevel optionLevel, SocketOptionName optionName, bool optionValue)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| optionLevel | [SocketOptionLevel](../../socketoptionlevel/) | سطح گزینهٔ سوکت. |
| optionName | [SocketOptionName](../../socketoptionname/) | نام گزینه‌ای که باید به‌روزرسانی شود. |
| optionValue | **bool** | مقداری که باید برای گزینهٔ مشخص‌شده تنظیم شود. |

## Socket::SetSocketOption(SocketOptionLevel, SocketOptionName, System::SharedPtr\<Object\>) متد

گزینهٔ سوکت مشخص‌شده را به مقدار مشخص‌شده تنظیم می‌کند.

```cpp
void System::Net::Sockets::Socket::SetSocketOption(SocketOptionLevel optionLevel, SocketOptionName optionName, System::SharedPtr<Object> optionValue)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| optionLevel | [SocketOptionLevel](../../socketoptionlevel/) | سطح گزینهٔ سوکت. |
| optionName | [SocketOptionName](../../socketoptionname/) | نام گزینه‌ای که باید به‌روزرسانی شود. |
| optionValue | [System::SharedPtr](../../../system/sharedptr/)\<[Object](../../../system/object/)\> | مقداری که باید برای گزینهٔ مشخص‌شده تنظیم شود. |

## موارد مرتبط

* Enum [SocketOptionLevel](../../socketoptionlevel/)
* Enum [SocketOptionName](../../socketoptionname/)
* Typedef [ArrayPtr](../../../system/arrayptr/)
* Typedef [SharedPtr](../../../system/sharedptr/)
* Class [Socket](../)
* Class [Object](../../../system/object/)
* Namespace [System::Net::Sockets](../../)
* Library [Aspose.Slides](../../../)