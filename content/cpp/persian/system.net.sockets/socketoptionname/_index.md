---
title: SocketOptionName
second_title: مرجع API Aspose.Slides برای C++
description: نام‌های گزینه‌های سوکت را برای کلاس Socket تعریف می‌کند.
type: docs
weight: 248
url: /fa/system.net.sockets/socketoptionname/
---
## SocketOptionName enum

Defines socket option names for the [Socket](../socket/) class.

```cpp
enum class SocketOptionName
```

### مقادیر

| نام | مقدار | توضیح |
| --- | --- | --- |
| Debug | 1 | اطلاعات اشکال‌زدایی را ضبط می‌کند. |
| AcceptConnection | 2 | نشان می‌دهد که آیا سوکت برای دریافت اتصال ورودی در حالت گوش دادن است. |
| ReuseAddress | 4 | نشان می‌دهد که آیا سوکت می‌تواند به آدرسی که قبلاً در حال استفاده است متصل شود. |
| KeepAlive | 8 | پکت‌های «Keep-Alive» را برای اتصال سوکت فعال می‌کند. |
| DontRoute | 16 | نشان می‌دهد که آیا بسته مستقیماً به آدرس‌های رابط ارسال می‌شود. |
| Broadcast | 32 | نشان می‌دهد که آیا سوکت می‌تواند پیام‌های پخش را ارسال کند. |
| UseLoopback | 64 | در صورت امکان سخت‌افزار را دور می‌زند. |
| Linger | 128 | سیستم هنگام تلاش برای بسته شدن، فرآیند را مسدود می‌کند تا زمانی که قادر به ارسال داده باشد. |
| OutOfBandInline | 256 | داده‌های out-of-band را در جریان داده عادی دریافت می‌کند. |
| DontLinger | n/a | نشان می‌دهد که آیا سوکت بدون تاخیر بسته می‌شود. |
| ExclusiveAddressUse | n/a | سوکت آدرس متصل‌شده را به‌طور اختصاصی استفاده می‌کند. |
| SendBuffer | 4097 | اندازهٔ بافر ارسال را مشخص می‌کند. |
| ReceiveBuffer | 4098 | اندازهٔ بافر دریافت را مشخص می‌کند. |
| SendLowWater | 4099 | حداقل مقدار داده برای عملیات ارسال را مشخص می‌کند. |
| ReceiveLowWater | 4100 | حداقل مقدار داده برای عملیات دریافت را مشخص می‌کند. |
| SendTimeout | 4101 | زمان‌سربری برای عملیات‌های همزمان ارسال را مشخص می‌کند. |
| ReceiveTimeout | 4102 | زمان‌سربری برای عملیات‌های همزمان دریافت را مشخص می‌کند. |
| Error | 4103 | وضعیت خطا را برمی‌گرداند و پاک می‌کند. |
| Type | 4104 | یک نوع سوکت را برمی‌گرداند. |
| ReuseUnicastPort | 12295 | نشان می‌دهد که آیا سیستم باید تخصیص پورت موقت برای اتصالات خروجی را به تعویق بیندازد. |
| MaxConnections | 2147483647 | این گزینه پشتیبانی نمی‌شود. برای مشخص کردن حداکثر طول صف گوش دادن استفاده می‌شد. |
| IPOptions | 1 | گزینه IP را که باید به دیتاگرام‌های خروجی اضافه شود، مشخص می‌کند. |
| HeaderIncluded | 2 | سرصفحه به دیتاگرام‌های خروجی اضافه می‌شود. |
| TypeOfService | 3 | نوع سرویس فیلد هدر IP را تغییر می‌دهد. |
| IpTimeToLive | 4 | زمان عمر (TTL) IP. |
| MulticastInterface | 9 | رابط برای بسته‌های چندپخشی خروجی را تنظیم می‌کند. |
| MulticastTimeToLive | 10 | زمان عمر (TTL) چندپخشی IP. |
| MulticastLoopback | 11 | حلقه باز (loopback) چندپخشی IP. |
| AddMembership | 12 | عضویت در یک گروه IP را اضافه می‌کند. |
| DropMembership | 13 | عضویت در یک گروه IP را حذف می‌کند. |
| DontFragment | 14 | دیتاگرام‌های IP را تکه‌تکه نمی‌کند. |
| AddSourceMembership | 15 | به گروه/منبع IP ملحق می‌شود. |
| DropSourceMembership | 16 | عضویت گروه/منبع IP را حذف می‌کند. |
| BlockSource | 17 | منبع/گروه IP را مسدود می‌کند. |
| UnblockSource | 18 | مسدودیت منبع/گروه IP را لغو می‌کند. |
| PacketInformation | 19 | اطلاعات بسته برای IPv4 را دریافت می‌کند. |
| HopLimit | 21 | یک عدد صحیح شامل شمارش HOP بسته را تحویل می‌دهد. |
| IPProtectionLevel | 23 | محدودسازی یک سوکت IPv6 به دامنهٔ مشخص شده را فعال می‌کند. |
| IPv6Only | 27 | سوکت فقط برای ارسال و دریافت بسته‌های IPv6 محدود شده است. |
| NoDelay | 1 | الگوریتم Nagle برای ترکیب بسته‌های ارسال را غیرفعال می‌کند. |
| BsdUrgent | 2 | داده‌های اضطراری را همان‌طور که در RFC-1222 تعریف شده است، استفاده می‌کند. |
| Expedited | 2 | داده‌های سریع‌کار (expedited) را همان‌طور که در RFC-1222 تعریف شده است، استفاده می‌کند. |
| NoChecksum | 1 | دیتاگرام‌های UDP را با checksum برابر صفر ارسال می‌کند. |
| ChecksumCoverage | 20 | پوشش checksum UDP را تنظیم یا دریافت می‌کند. |
| UpdateAcceptContext | 28683 | یک سوکت مشتری را با همان خصوصیات سوکت شنونده به‌روز می‌کند. |
| UpdateConnectContext | 28688 | یک سوکت مشتری را با همان خصوصیات سوکت شنونده به‌روز می‌کند. |

## همچنین ببینید

* فضای‌نام [System::Net::Sockets](../)
* کتابخانه [Aspose.Slides](../../)