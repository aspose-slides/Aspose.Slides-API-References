---
title: get_ReadTimeout()
second_title: مرجع API Aspose.Slides برای C++
description: یک مقدار (به میلی‌ثانیه) برمی‌گرداند که تعیین می‌کند جریان تا چه مدت سعی می‌کند بخواند قبل از اینکه زمان‌سرب شود.
type: docs
weight: 53
url: /fa/system.net.sockets/networkstream/get_readtimeout/
---
## NetworkStream::get_ReadTimeout() const متد

یک مقدار (به میلی‌ثانیه) که تعیین می‌کند جریان تا چه مدت سعی می‌کند بخواند قبل از اینکه زمان‌سرب شود.

```cpp
int32_t System::Net::Sockets::NetworkStream::get_ReadTimeout() const override
```

### مقدار بازگشت

مقداری به میلی‌ثانیه که تعیین می‌کند جریان تا چه مدت سعی می‌کند بخواند قبل از اینکه زمان‌سرب شود.

## موارد مرتبط

* کلاس [NetworkStream](../)
* فضای نام [System::Net::Sockets](../../)
* کتابخانه [Aspose.Slides](../../../)