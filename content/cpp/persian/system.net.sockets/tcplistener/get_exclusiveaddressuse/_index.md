---
title: get_ExclusiveAddressUse()
second_title: مرجع API Aspose.Slides برای C++
description: مقدار را برمی‌گرداند که نشان می‌دهد آیا نمونهٔ فعلی تنها به یک مشتری اجازه استفاده از یک پورت را می‌دهد.
type: docs
weight: 27
url: /fa/system.net.sockets/tcplistener/get_exclusiveaddressuse/
---
## TcpListener::get_ExclusiveAddressUse() متد

دریافت مقداری که نشان می‌دهد آیا نمونهٔ فعلی تنها به یک مشتری اجازه استفاده از پورت را می‌دهد یا نه.

```cpp
bool System::Net::Sockets::TcpListener::get_ExclusiveAddressUse()
```

### مقدار بازگشت

مقداری که نشان می‌دهد آیا نمونهٔ فعلی تنها به یک مشتری اجازه استفاده از پورت را می‌دهد یا نه.

## موارد مرتبط

* کلاس [TcpListener](../)
* فضای‌نام [System::Net::Sockets](../../)
* کتابخانه [Aspose.Slides](../../../)