---
title: virtualizeEndConstIterator()
second_title: Aspose.Slides برای C++ مرجع API
description: یک تکرارگر دریافت می‌کند که درست پس از آخرین عنصر (در صورت وجود) از نمونهٔ ثابت‌صفت مجموعه را نشان می‌دهد.
type: docs
weight: 209
url: /fa/aspose.slides.animation/motionpath/virtualizeendconstiterator/
---
## MotionPath::virtualizeEndConstIterator() const متد

یک تکرارگر دریافت می‌کند که درست پس از آخرین عنصر (در صورت وجود) از نمونهٔ ثابت‌صفت مجموعه را نشان می‌دهد.

```cpp
virtualized_iterator * Aspose::Slides::Animation::MotionPath::virtualizeEndConstIterator() const override
```

### مقدار بازگشتی

یک تکرارگر که درست پس از آخرین عنصر (در صورت وجود) از نمونهٔ ثابت‌صفت مجموعه را نشان می‌دهد. پیاده‌سازی تکرارگر ثابت برای انتهای container را فراهم می‌کند.

شیء تکرارگر تازه ساخته شده.

## موارد مرتبط

* تعریف نوع [virtualized_iterator](../virtualized_iterator/)
* کلاس [MotionPath](../)
* فضای‌نام [Aspose::Slides::Animation](../../)
* کتابخانه [Aspose.Slides](../../../)