---
title: get_EnableUpaCheck()
second_title: مرجع API Aspose.Slides برای C++
description: مقداری را باز می‌گرداند که نشان می‌دهد آیا XmlSchemaSet باید برای تخلفات Unique Particle Attribution (UPA) بررسی کند.
type: docs
weight: 1
url: /fa/system.xml.schema/xmlschemacompilationsettings/get_enableupacheck/
---
## XmlSchemaCompilationSettings::get_EnableUpaCheck() متد


یک مقدار باز می‌گرداند که نشان می‌دهد آیا [XmlSchemaSet](../../xmlschemaset/) باید برای تخلفات Unique Particle Attribution (UPA) بررسی کند.

```cpp
bool System::Xml::Schema::XmlSchemaCompilationSettings::get_EnableUpaCheck()
```


### مقدار بازگشت

**true** اگر [XmlSchemaSet](../../xmlschemaset/) باید برای تخلفات Unique Particle Attribution (UPA) بررسی کند؛ در غیر اینصورت، **false**. مقدار پیش‌فرض **true** است.

## موارد مرتبط

* کلاس [XmlSchemaCompilationSettings](../)
* فضای نام [System::Xml::Schema](../../)
* کتابخانه [Aspose.Slides](../../../)