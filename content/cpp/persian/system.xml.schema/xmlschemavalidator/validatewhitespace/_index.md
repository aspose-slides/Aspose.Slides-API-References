---
title: ValidateWhitespace()
second_title: Aspose.Slides برای C++ مرجع API
description: بررسی می‌کند که آیا فضای سفید در رشتهٔ مشخص شده در زمینهٔ عنصر فعلی مجاز است و در صورتی که عنصر فعلی محتوی ساده داشته باشد، فضای سفید را برای اعتبارسنجی جمع‌آوری می‌کند.
type: docs
weight: 196
url: /fa/system.xml.schema/xmlschemavalidator/validatewhitespace/
---
## XmlSchemaValidator::ValidateWhitespace(const String\&) متد

اعتبارسنجی می‌کند که آیا فضای سفید در **string** مشخص شده در زمینه عنصر فعلی مجاز است و در صورتی که عنصر فعلی محتوی ساده داشته باشد، فضای سفید را برای اعتبارسنجی جمع‌آوری می‌کند.

```cpp
void System::Xml::Schema::XmlSchemaValidator::ValidateWhitespace(const String &elementValue)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| elementValue | const [String](../../../system/string/)\& | یک **string** فضای سفید برای اعتبارسنجی در زمینه عنصر فعلی. |

## XmlSchemaValidator::ValidateWhitespace(XmlValueGetter) متد

اعتبارسنجی می‌کند که آیا فضای سفید برگردانده شده توسط شیء XmlValueGetter مشخص شده در زمینه عنصر فعلی مجاز است و در صورتی که عنصر فعلی محتوی ساده داشته باشد، فضای سفید را برای اعتبارسنجی جمع‌آوری می‌کند.

```cpp
void System::Xml::Schema::XmlSchemaValidator::ValidateWhitespace(XmlValueGetter elementValue)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| elementValue | [XmlValueGetter](../../xmlvaluegetter/) | یک کال‌بک XmlValueGetter که برای انتقال مقدار فضای سفید به عنوان نوعی سازگار با نوع XML [Schema](../../) Definition Language (XSD) ویژگی استفاده می‌شود. |

## مراجع

* نوع تعریف [XmlValueGetter](../../xmlvaluegetter/)
* کلاس [String](../../../system/string/)
* کلاس [XmlSchemaValidator](../)
* فضای نام [System::Xml::Schema](../../)
* کتابخانه [Aspose.Slides](../../../)