---
title: set_IsDefault()
second_title: Aspose.Slides برای C++ مرجع API
description: مقدار را تنظیم می‌کند که نشان می‌دهد آیا این گره XML معتبر به عنوان نتیجهٔ اعمال مقدار پیش‌فرض در طول اعتبارسنجی XML Schema Definition Language (XSD) تنظیم شده است.
type: docs
weight: 40
url: /fa/system.xml.schema/xmlschemainfo/set_isdefault/
---
## XmlSchemaInfo::set_IsDefault(bool) متد

مقداری را تنظیم می‌کند که نشان می‌دهد آیا این گره XML اعتبارسنجی‌شده به عنوان نتیجه‌ی اعمال مقدار پیش‌فرض در طول اعتبارسنجی طرح‌بندی XML [Schema](../../) Definition Language (XSD) تنظیم شده است.

```cpp
void System::Xml::Schema::XmlSchemaInfo::set_IsDefault(bool value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **bool** | مقداری که باید تنظیم شود. |

## موارد مرتبط

* کلاس [XmlSchemaInfo](../)
* فضای‌نام [System::Xml::Schema](../../)
* کتابخانه [Aspose.Slides](../../../)