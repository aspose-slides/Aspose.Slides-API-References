---
title: "System::Xml::Serialization"
second_title: مرجع API Aspose.Slides برای C++
description: 
type: docs
weight: 1158
url: /fa/system.xml.serialization/
---
## کلاس‌ها

| کلاس | توضیح |
| --- | --- |
| [IXmlSerializable](./ixmlserializable/) | قالب‌بندی سفارشی برای سریال‌سازی و دیسریال‌سازی XML ارائه می‌دهد. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اعتبارسنجی می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [XmlAttributeOverrides](./xmlattributeoverrides/) | اجازه می‌دهد که ویژگی‌ها هنگام استفاده از [XmlSerializer](./xmlserializer/) برای سریال‌سازی یا دیسریال‌سازی یک شیء بازنویسی شوند. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اعتبارسنجی می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [XmlRootAttribute](./xmlrootattribute/) | هدف ویژگی را به عنوان عنصر ریشه XML علامت‌گذاری می‌کند و سریال‌سازی XML آن را کنترل می‌نماید. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اعتبارسنجی می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [XmlSerializationReader](./xmlserializationreader/) | کلاس سرویس که تجربه [XmlReader](../system.xml/xmlreader/) را بهبود می‌بخشد. |
| [XmlSerializationWriter](./xmlserializationwriter/) | کلاس سرویس که تجربه [XmlWriter](../system.xml/xmlwriter/) را بهبود می‌بخشد. |
| [XmlSerializer](./xmlserializer/) | سریال‌سازی و دیسریال‌سازی اشیاء به داخل و از اسناد XML را انجام می‌دهد. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای اعتبارسنجی می‌شود. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [XmlSerializerImplementation](./xmlserializerimplementation/) | کلاس داخلی برای استفاده با [XmlSerializer](./xmlserializer/). |
| [XmlSerializerNamespaces](./xmlserializernamespaces/) | فضاهای نام XML و پیشوندهایی را شامل می‌شود که [Serialization::XmlSerializer](./xmlserializer/) برای تولید نام‌های квалиفای شده در یک نمونه سند XML استفاده می‌کند. |