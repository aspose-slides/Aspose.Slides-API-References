---
title: GetHashCode()
second_title: مرجع API Aspose.Slides برای C++
description: "کد هش را برای نوع دلخواه دریافت می‌کند. برای این کار Object::GetHashCode() را فراخوانی می‌کند."
type: docs
weight: 1
url: /fa/system.runtime.compilerservices/runtimehelpers/gethashcode/
---
## RuntimeHelpers::GetHashCode(SmartPtr\<T\> const\&) متد

کد هش را برای نوع دلخواه دریافت می‌کند. برای این کار [Object::GetHashCode()](../../../system/object/gethashcode/) را فراخوانی می‌کند.

```cpp
template<typename T> static int System::Runtime::CompilerServices::RuntimeHelpers::GetHashCode(SmartPtr<T> const &obj)
```

### پارامترهای الگو

| پارامتر | توضیح |
| --- | --- |
| T | نوعی که کد هش برای آن دریافت می‌شود. |

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| obj | [SmartPtr](../../../system/smartptr/)\<T\> const\& | [Object](../../../system/object/) برای دریافت اطلاعات. |

### مقدار بازگشت

مقدار کد هش به‌عنوان محاسبه‌شده توسط پیاده‌سازی هدف.

## موارد مرتبط

* کلاس [SmartPtr](../../../system/smartptr/)
* کلاس [RuntimeHelpers](../)
* فضای نام [System::Runtime::CompilerServices](../../)
* کتابخانه [Aspose.Slides](../../../)