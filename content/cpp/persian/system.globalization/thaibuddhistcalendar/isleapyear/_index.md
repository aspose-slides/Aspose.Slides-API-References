---
title: IsLeapYear()
second_title: Aspose.Slides برای مرجع API C++
description: بررسی می‌کند که آیا سال کبیسه است.
type: docs
weight: 196
url: /fa/system.globalization/thaibuddhistcalendar/isleapyear/
---
## ThaiBuddhistCalendar::IsLeapYear(int, int) const متد

بررسی می‌کند که آیا سال کبیسه است.

```cpp
bool System::Globalization::ThaiBuddhistCalendar::IsLeapYear(int year, int era) const override
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| year | int | سال. |
| era | int | دوره. |

### مقدار بازگشت

True اگر سال مشخص شده کبیسه باشد، در غیر این صورت false.

## ThaiBuddhistCalendar::IsLeapYear(int) const متد

بررسی می‌کند که آیا سال کبیسه است.

```cpp
virtual bool System::Globalization::Calendar::IsLeapYear(int year) const
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| year | int | سال. |

### مقدار بازگشت

True اگر سال مشخص شده کبیسه باشد، در غیر این صورت false.

## ThaiBuddhistCalendar::IsLeapYear(int, int) const متد

بررسی می‌کند که آیا سال کبیسه است.

```cpp
virtual bool System::Globalization::Calendar::IsLeapYear(int year, int era) const=0
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| year | int | سال. |
| era | int | دوره. |

### مقدار بازگشت

True اگر سال مشخص شده کبیسه باشد، در غیر این صورت false.

## موارد مرتبط

* کلاس [ThaiBuddhistCalendar](../)
* فضای نام [System::Globalization](../../)
* کتابخانه [Aspose.Slides](../../../)