---
title: IsLeapDay()
second_title: مرجع API Aspose.Slides برای C++
description: بررسی می‌کند که آیا روز کبیسه است.
type: docs
weight: 222
url: /fa/system.globalization/thaibuddhistcalendar/isleapday/
---
## ThaiBuddhistCalendar::IsLeapDay(int, int, int, int) const method

روز مورد نظر آیا کبیسه است را بررسی می‌کند.

```cpp
bool System::Globalization::ThaiBuddhistCalendar::IsLeapDay(int year, int month, int day, int era) const override
```

### پارامترها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| year | int | سال. |
| month | int | ماه. |
| day | int | روز. |
| era | int | دوره. |

### مقدار بازگشتی

درست اگر تاریخ مشخص شده کبیسه باشد، در غیر این صورت نادرست.

## ThaiBuddhistCalendar::IsLeapDay(int, int, int) const method

روز مورد نظر آیا کبیسه است را بررسی می‌کند.

```cpp
virtual bool System::Globalization::Calendar::IsLeapDay(int year, int month, int day) const
```

### پارامترها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| year | int | سال. |
| month | int | ماه. |
| day | int | روز. |

### مقدار بازگشتی

درست اگر تاریخ مشخص شده کبیسه باشد، در غیر این صورت نادرست.

## ThaiBuddhistCalendar::IsLeapDay(int, int, int, int) const method

روز مورد نظر آیا کبیسه است را بررسی می‌کند.

```cpp
virtual bool System::Globalization::Calendar::IsLeapDay(int year, int month, int day, int era) const=0
```

### پارامترها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| year | int | سال. |
| month | int | ماه. |
| day | int | روز. |
| era | int | دوره. |

### مقدار بازگشتی

درست اگر تاریخ مشخص شده کبیسه باشد، در غیر این صورت نادرست.

## مراجع

* کلاس [ThaiBuddhistCalendar](../)
* فضای‌نام [System::Globalization](../../)
* کتابخانه [Aspose.Slides](../../../)