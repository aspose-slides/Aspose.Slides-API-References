---
title: IsLeapYear()
second_title: Aspose.Slides برای C++ مرجع API
description: بررسی می‌کند که آیا سال کبیسه است.
type: docs
weight: 92
url: /fa/system.globalization/umalquracalendar/isleapyear/
---
## UmAlQuraCalendar::IsLeapYear(int, int) const متد

بررسی می‌کند که آیا سال کبیسه است.

```cpp
bool System::Globalization::UmAlQuraCalendar::IsLeapYear(int year, int era) const override
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| year | int | سال. |
| era | int | دوره. |

### مقدار بازگشتی

اگر سال مشخص شده کبیسه باشد True، در غیر این صورت false.

## UmAlQuraCalendar::IsLeapYear(int) const متد

بررسی می‌کند که آیا سال کبیسه است.

```cpp
virtual bool System::Globalization::Calendar::IsLeapYear(int year) const
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| year | int | سال. |

### مقدار بازگشتی

اگر سال مشخص شده کبیسه باشد True، در غیر این صورت false.

## UmAlQuraCalendar::IsLeapYear(int, int) const متد

بررسی می‌کند که آیا سال کبیسه است.

```cpp
virtual bool System::Globalization::Calendar::IsLeapYear(int year, int era) const=0
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| year | int | سال. |
| era | int | دوره. |

### مقدار بازگشتی

اگر سال مشخص شده کبیسه باشد True، در غیر این صورت false.

## موارد مرتبط

* کلاس [UmAlQuraCalendar](../)
* فضای‌نام [System::Globalization](../../)
* کتابخانه [Aspose.Slides](../../../)