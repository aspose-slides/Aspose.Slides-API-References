---
title: IsLeapDay()
second_title: Aspose.Slides برای مرجع API C++
description: بررسی می‌کند که آیا روز کبیسه است یا خیر.
type: docs
weight: 157
url: /fa/system.globalization/juliancalendar/isleapday/
---
## JulianCalendar::IsLeapDay(int, int, int, int) const متد

بررسی می‌کند که آیا روز، روز کبیسه است یا خیر.

```cpp
bool System::Globalization::JulianCalendar::IsLeapDay(int year, int month, int day, int era) const override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| year | int | Year. |
| month | int | Month. |
| day | int | Day. |
| era | int | Era. |

### مقدار بازگشت

اگر تاریخ مشخص شده سال کبیسه باشد، True، در غیر این صورت false.

## JulianCalendar::IsLeapDay(int, int, int) const متد

بررسی می‌کند که آیا روز، روز کبیسه است یا خیر.

```cpp
virtual bool System::Globalization::Calendar::IsLeapDay(int year, int month, int day) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| year | int | Year. |
| month | int | Month. |
| day | int | Day. |

### مقدار بازگشت

اگر تاریخ مشخص شده سال کبیسه باشد، True، در غیر این صورت false.

## JulianCalendar::IsLeapDay(int, int, int, int) const متد

بررسی می‌کند که آیا روز، روز کبیسه است یا خیر.

```cpp
virtual bool System::Globalization::Calendar::IsLeapDay(int year, int month, int day, int era) const=0
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| year | int | Year. |
| month | int | Month. |
| day | int | Day. |
| era | int | Era. |

### مقدار بازگشت

اگر تاریخ مشخص شده سال کبیسه باشد، True، در غیر این صورت false.

## موارد مرتبط

* کلاس [JulianCalendar](../)
* فضای‌نام [System::Globalization](../../)
* کتابخانه [Aspose.Slides](../../../)