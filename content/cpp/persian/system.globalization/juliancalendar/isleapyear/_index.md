---
title: IsLeapYear()
second_title: Aspose.Slides برای مرجع API C++
description: بررسی می‌کند که آیا سال کبیسه است.
type: docs
weight: 131
url: /fa/system.globalization/juliancalendar/isleapyear/
---
## JulianCalendar::IsLeapYear(int, int) const method

بررسی می‌کند که آیا سال کبیسه است یا نه.

```cpp
bool System::Globalization::JulianCalendar::IsLeapYear(int year, int era) const override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| year | int | سال. |
| era | int | عصر. |

### مقدار بازگشت

درست اگر سال مشخص شده کبیسه باشد، و در غیر این صورت نادرست.

## JulianCalendar::IsLeapYear(int) const method

بررسی می‌کند که آیا سال کبیسه است یا نه.

```cpp
virtual bool System::Globalization::Calendar::IsLeapYear(int year) const
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| year | int | سال. |

### مقدار بازگشت

درست اگر سال مشخص شده کبیسه باشد، و در غیر این صورت نادرست.

## JulianCalendar::IsLeapYear(int, int) const method

بررسی می‌کند که آیا سال کبیسه است یا نه.

```cpp
virtual bool System::Globalization::Calendar::IsLeapYear(int year, int era) const=0
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| year | int | سال. |
| era | int | عصر. |

### مقدار بازگشت

درست اگر سال مشخص شده کبیسه باشد، و در غیر این صورت نادرست.

## موارد مرتبط

* Class [JulianCalendar](../)
* Namespace [System::Globalization](../../)
* Library [Aspose.Slides](../../../)