---
title: "System::Globalization"
second_title: مرجع API Aspose.Slides برای C++
description: 
type: docs
weight: 547
url: /fa/system.globalization/
---
## کلاس‌ها

| کلاس | توضیح |
| --- | --- |
| [Calendar](./calendar/) | [Calendar](./calendar/) که نحوه‌ی مدیریت، محاسبه، قالب‌بندی و ... تاریخ‌ها را تعریف می‌کند. عملیات Setter فقط برای اشیاء غیر-فقط-خواندنی فعال است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [CharUnicodeInfo](./charunicodeinfo/) | اطلاعاتی درباره کاراکتر یونیکد دریافت می‌کند. |
| [ChineseLunisolarCalendar](./chineselunisolarcalendar/) | تقویم قمری خورشیدی چینی. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [CompareInfo](./compareinfo/) | مقایسهٔ رشته‌ای حساس به فرهنگ. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [CultureInfo](./cultureinfo/) | مجموعه‌ای از مقادیر و الگوریتم‌های ویژهٔ فرهنگ. عملیات Setter فقط برای اشیاء غیر-فقط-خواندنی فعال است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [DateTimeFormatInfo](./datetimeformatinfo/) | مجموعه‌ای از پارامترهای قالب‌بندی تاریخ و زمان. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [DaylightTime](./daylighttime/) | دورهٔ زمان مهرنگاری تابستانی. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [Details_CultureNotFoundException](./details_culturenotfoundexception/) | CultureNotFoundException زمانی پرتاب می‌شود که سعی در ساخت فرهنگی باشد که در دسترس نیست. هرگز نمونه‌های این کلاس را به‌صورت دستی ایجاد نکنید. به جای آن از کلاس CultureNotFoundException استفاده کنید. هرگز نمونه‌های کلاس CultureNotFoundException را درون [System::SmartPtr](../system/smartptr/) بسته‌بندی نکنید. |
| [EastAsianLunisolarCalendar](./eastasianlunisolarcalendar/) | تقویم قمری خورشیدی شرق آسیا. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [GlobalizationExtensions](./globalizationextensions/) | روش‌های مرتبط با [Globalization](./). |
| [GregorianCalendar](./gregoriancalendar/) | تقویم گریگوری. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [HebrewCalendar](./hebrewcalendar/) | تقویم عبری. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [HijriCalendar](./hijricalendar/) | تقویم هجری. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [IdnMapping](./idnmapping/) | [IdnMapping](./idnmapping/) برای نگاشت نام‌ها به Punycode استفاده می‌شود. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [JapaneseCalendar](./japanesecalendar/) | تقویم ژاپنی. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [JapaneseLunisolarCalendar](./japaneselunisolarcalendar/) | تقویم قمری خورشیدی ژاپنی. پیاده‌سازی نشده. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [JulianCalendar](./juliancalendar/) | تقویم ژولین. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [KoreanCalendar](./koreancalendar/) | تقویم کره‌ای. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [KoreanLunisolarCalendar](./koreanlunisolarcalendar/) | تقویم قمری خورشیدی کره‌ای. پیاده‌سازی نشده. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [NumberFormatInfo](./numberformatinfo/) | اطلاعاتی دربارهٔ نحوهٔ قالب‌بندی اعداد را نگهداری می‌کند. عملیات Setter فقط برای اشیاء غیر-فقط-خواندنی فعال است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [PersianCalendar](./persiancalendar/) | تقویم فارسی. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [RegionInfo](./regioninfo/) | اطلاعاتی دربارهٔ منطقه ارائه می‌دهد. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [SortKey](./sortkey/) | نگاشت یک رشته به کلید مرتب‌سازی آن. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [SortVersion](./sortversion/) | اطلاعاتی دربارهٔ نسخهٔ یونیکد استفاده‌شده برای مقایسه و ترتیب‌بندی رشته‌ها ارائه می‌دهد. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [StringInfo](./stringinfo/) | جداساز برای پیمایش بخش‌های رشته. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [TaiwanCalendar](./taiwancalendar/) | تقویم تائیوان. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [TaiwanLunisolarCalendar](./taiwanlunisolarcalendar/) | تقویم قمری خورشیدی تائیوان. پیاده‌سازی نشده. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [TextElementEnumerator](./textelementenumerator/) | شمارنده برای پیمایش عناصر رشته (کاراکترها). اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [TextInfo](./textinfo/) | ویژگی‌های متنی خاص مکانیزه را تعریف می‌کند. عملیات Setter فقط برای اشیاء غیر-فقط-خواندنی فعال است. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [ThaiBuddhistCalendar](./thaibuddhistcalendar/) | تقویم بودایی تایلندی. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |
| [UmAlQuraCalendar](./umalquracalendar/) | تقویم عمر القُرا. پیاده‌سازی نشده. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) تخصیص یابند. هرگز نمونه‌ای از این نوع را روی پشته یا با عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا شکست‌های assert می‌شود. همیشه این کلاس را درون اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به توابع به عنوان آرگومان استفاده کنید. |

## شمارش‌ها

| شمارش | توضیح |
| --- | --- |
| [CalendarAlgorithmType](./calendaralgorithmtype/) | نوع [Calendar](./calendar/). |
| [CalendarWeekRule](./calendarweekrule/) | چگونگی تعیین اولین هفتهٔ سال. |
| [CompareOptions](./compareoptions/) | گزینه‌های مقایسهٔ [String](../system/string/). |
| [CultureTypes](./culturetypes/) | ورودی‌های بیت‌ماسک دسته‌های فرهنگ. |
| [DateTimeStyles](./datetimestyles/) | گزینه‌های قالب‌بندی تاریخ و زمان را تعریف می‌کند. پرچم‌های بیت. |
| [DigitShapes](./digitshapes/) | نمایش ارقام را مشخص می‌کند. |
| [GregorianCalendarTypes](./gregoriancalendartypes/) | انواع تقویم‌های گرگوری. |
| [NumberStyles](./numberstyles/) | سبک عددی مجاز هنگام تجزیه. |
| [TimeSpanStyles](./timespanstyles/) | گزینه‌های قالب‌بندی را مشخص می‌کند که تجزیهٔ رشته‌ای برای متدهایی که نمایهٔ زمان‌گذاری را به شیء Timespan تبدیل می‌کنند، سفارشی‌سازی می‌کند. |
| [UnicodeCategory](./unicodecategory/) | دستهٔ کاراکتر یونیکد. |

## تعریف‌های نوع

| تعریف نوع | توضیح |
| --- | --- |
| [CultureNotFoundException](./culturenotfoundexception/) |  |
| [CalendarPtr](./calendarptr/) | نوع اشاره‌گر [Calendar](./calendar/). |
| [ChineseLunisolarCalendarPtr](./chineselunisolarcalendarptr/) | نوع اشاره‌گر [ChineseLunisolarCalendar](./chineselunisolarcalendar/). |
| [CompareInfoPtr](./compareinfoptr/) | نوع اشاره‌گر [CompareInfo](./compareinfo/). |
| [CultureInfoPtr](./cultureinfoptr/) | نوع اشاره‌گر [CultureInfo](./cultureinfo/). |
| [DateTimeFormatInfoPtr](./datetimeformatinfoptr/) | اشاره‌گر اطلاعات [DateTimeFormatInfo](./datetimeformatinfo/). |
| [DaylightTimePtr](./daylighttimeptr/) | نوع اشاره‌گر [DaylightTime](./daylighttime/). |
| [EastAsianLunisolarCalendarPtr](./eastasianlunisolarcalendarptr/) | نوع اشاره‌گر [EastAsianLunisolarCalendar](./eastasianlunisolarcalendar/). |
| [GregorianCalendarPtr](./gregoriancalendarptr/) | نوع اشاره‌گر [GregorianCalendar](./gregoriancalendar/). |
| [HebrewCalendarPtr](./hebrewcalendarptr/) | نوع اشاره‌گر [HebrewCalendar](./hebrewcalendar/). |
| [HijriCalendarPtr](./hijricalendarptr/) | نوع اشاره‌گر [HijriCalendar](./hijricalendar/). |
| [IdnMappingPtr](./idnmappingptr/) | نوع اشاره‌گر [IdnMapping](./idnmapping/). |
| [JapaneseCalendarPtr](./japanesecalendarptr/) | نوع اشاره‌گر [JapaneseCalendar](./japanesecalendar/). |
| [JapaneseLunisolarCalendarPtr](./japaneselunisolarcalendarptr/) | نوع اشاره‌گر [JapaneseLunisolarCalendar](./japaneselunisolarcalendar/). |
| [JulianCalendarPtr](./juliancalendarptr/) | نوع اشاره‌گر [JulianCalendar](./juliancalendar/). |
| [KoreanCalendarPtr](./koreancalendarptr/) | نوع اشاره‌گر [KoreanCalendar](./koreancalendar/). |
| [KoreanLunisolarCalendarPtr](./koreanlunisolarcalendarptr/) | نوع اشاره‌گر [KoreanLunisolarCalendar](./koreanlunisolarcalendar/). |
| [NumberFormatInfoPtr](./numberformatinfoptr/) | اشاره‌گر [NumberFormatInfo](./numberformatinfo/). |
| [PersianCalendarPtr](./persiancalendarptr/) | نوع اشاره‌گر [PersianCalendar](./persiancalendar/). |
| [RegionInfoPtr](./regioninfoptr/) | نوع اشاره‌گر [RegionInfo](./regioninfo/). |
| [SortKeyPtr](./sortkeyptr/) | نوع اشاره‌گر [SortKey](./sortkey/). |
| [SortVersionPtr](./sortversionptr/) | نوع اشاره‌گر [SortVersion](./sortversion/). |
| [StringInfoPtr](./stringinfoptr/) | نوع اشاره‌گر [StringInfo](./stringinfo/). |
| [TaiwanCalendarPtr](./taiwancalendarptr/) | نوع اشاره‌گر [TaiwanCalendar](./taiwancalendar/). |
| [TaiwanLunisolarCalendarPtr](./taiwanlunisolarcalendarptr/) | نوع اشاره‌گر [TaiwanLunisolarCalendar](./taiwanlunisolarcalendar/). |
| [TextElementEnumeratorPtr](./textelementenumeratorptr/) | نوع اشاره‌گر [TextElementEnumerator](./textelementenumerator/). |
| [TextInfoPtr](./textinfoptr/) | نوع اشاره‌گر [TextInfo](./textinfo/). |
| [ThaiBuddhistCalendarPtr](./thaibuddhistcalendarptr/) | نوع اشاره‌گر [ThaiBuddhistCalendar](./thaibuddhistcalendar/). |
| [UmAlQuraCalendarPtr](./umalquracalendarptr/) | نوع اشاره‌گر [UmAlQuraCalendar](./umalquracalendar/). |