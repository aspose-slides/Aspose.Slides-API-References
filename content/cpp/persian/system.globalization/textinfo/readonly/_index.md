---
title: ReadOnly()
second_title: Aspose.Slides برای C++ API مرجع
description: نسخهٔ فقط‌خواندنی از فرهنگ را دریافت می‌کند.
type: docs
weight: 248
url: /fa/system.globalization/textinfo/readonly/
---
## TextInfo::ReadOnly(const TextInfoPtr\&) متد

یک نسخهٔ فقط‌خواندنی از فرهنگ را دریافت می‌کند.

```cpp
static TextInfoPtr System::Globalization::TextInfo::ReadOnly(const TextInfoPtr &text_info)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| text_info | const [TextInfoPtr](../../textinfoptr/)\& | [TextInfo](../) برای بسته شدن به صورت فقط‌خواندنی. |

### مقدار بازگشتی

اگر **text_info** فقط‌خواندنی باشد، همان شی را برمی‌گرداند؛ در غیر این صورت یک نسخهٔ فقط‌خواندنی ایجاد می‌کند.

## همچنین ببینید

* Typedef [TextInfoPtr](../../textinfoptr/)
* کلاس [TextInfo](../)
* فضای نام [System::Globalization](../../)
* کتابخانه [Aspose.Slides](../../../)