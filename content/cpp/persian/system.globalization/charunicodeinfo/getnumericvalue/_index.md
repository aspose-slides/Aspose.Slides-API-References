---
title: GetNumericValue()
second_title: Aspose.Slides برای مرجع API C++
description: مقدار عددی مرتبط با کاراکتر مشخص‌شده را دریافت می‌کند.
type: docs
weight: 27
url: /fa/system.globalization/charunicodeinfo/getnumericvalue/
---
## CharUnicodeInfo::GetNumericValue(char16_t) متد

دریافت مقدار عددی مرتبط با کاراکتر مشخص‌شده.

```cpp
static double System::Globalization::CharUnicodeInfo::GetNumericValue(char16_t ch)
```

### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| ch | char16_t | کاراکتر یونیکد. |

### مقدار بازگشت

مقدار عددی یا -1 اگر کاراکتر مشخص‌شده عددی نباشد.

## CharUnicodeInfo::GetNumericValue(const String\&, int) متد

دریافت مقدار عددی مرتبط با کاراکتر موجود در اندیس مشخص‌شدهٔ رشته.

```cpp
static double System::Globalization::CharUnicodeInfo::GetNumericValue(const String &str, int index)
```

### آرگومان‌ها

| Parameter | Type | Description |
| --- | --- | --- |
| str | const [String](../../../system/string/)\& | رشته‌ای که شامل کاراکتر یونیکد است. |
| index | int | اندیس کاراکتر یونیکد. |

### مقدار بازگشت

مقدار عددی یا -1 اگر کاراکتر مشخص‌شده عددی نباشد.

## مراجع

* کلاس [CharUnicodeInfo](../)
* کلاس [String](../../../system/string/)
* فضای نام [System::Globalization](../../)
* کتابخانه [Aspose.Slides](../../../)