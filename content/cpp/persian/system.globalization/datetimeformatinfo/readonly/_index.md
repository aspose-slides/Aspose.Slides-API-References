---
title: ReadOnly()
second_title: مرجع API Aspose.Slides برای C++
description: نسخه فقط-خواندنی فرمت‌کننده را دریافت می‌کند.
type: docs
weight: 859
url: /fa/system.globalization/datetimeformatinfo/readonly/
---
## DateTimeFormatInfo::ReadOnly(const DateTimeFormatInfoPtr\&) method


نسخه فقط-خواندنی فرمت‌کننده را دریافت می‌کند.

```cpp
static DateTimeFormatInfoPtr System::Globalization::DateTimeFormatInfo::ReadOnly(const DateTimeFormatInfoPtr &info)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| info | const [DateTimeFormatInfoPtr](../../datetimeformatinfoptr/)\& | فرمت‌کننده‌ای که نسخه فقط-خواندنی آن دریافت می‌شود. |

### مقدار بازگشتی

اگر **info** فقط-خواندنی باشد، همان را برمی‌گرداند؛ در غیر این صورت یک کپی از آن ایجاد می‌کند و به‌عنوان فقط-خواندنی علامت‌گذاری می‌کند.

## موارد مرتبط

* تعریف نوع [DateTimeFormatInfoPtr](../../datetimeformatinfoptr/)
* کلاس [DateTimeFormatInfo](../)
* فضای‌نام [System::Globalization](../../)
* کتابخانه [Aspose.Slides](../../../)