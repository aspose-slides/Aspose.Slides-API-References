---
title: IndexOf()
second_title: Aspose.Slides برای مرجع API C++
description: به دنبال زیررشته می‌گردد.
type: docs
weight: 92
url: /fa/system.globalization/compareinfo/indexof/
---
## CompareInfo::IndexOf(const String\&, const String\&, int, int) const متد

به دنبال زیررشته می‌گردد.

```cpp
virtual int System::Globalization::CompareInfo::IndexOf(const String &source, const String &value, int start_index, int count) const
```

### پارامترها

| Parameter | Type | Description |
| --- | --- | --- |
| source | const [String](../../../system/string/)\& | رشتهٔ منبع. |
| value | const [String](../../../system/string/)\& | مقدار جستجو. |
| start_index | int | شاخص شروع جستجو در رشتهٔ منبع. |
| count | int | تعداد کاراکترها در رشتهٔ منبع. |

### مقدار بازگشتی

[Index](../../../system/index/) مقدار جستجو در رشتهٔ منبع یا -1 اگر یافت نشود.

## CompareInfo::IndexOf(const String\&, const String\&, int, CompareOptions) const متد

به دنبال زیررشته می‌گردد. فقط حالت Ordinal پشتیبانی می‌شود.

```cpp
virtual int System::Globalization::CompareInfo::IndexOf(const String &source, const String &value, int start_index, CompareOptions options) const
```

### پارامترها

| Parameter | Type | Description |
| --- | --- | --- |
| source | const [String](../../../system/string/)\& | رشتهٔ منبع. |
| value | const [String](../../../system/string/)\& | مقدار جستجو. |
| start_index | int | شاخص شروع جستجو در رشتهٔ منبع. |
| options | [CompareOptions](../../compareoptions/) | گزینه‌های جستجو. |

### مقدار بازگشتی

[Index](../../../system/index/) مقدار جستجو در رشتهٔ منبع یا -1 اگر یافت نشود.

## CompareInfo::IndexOf(const String\&, const String\&, int, int, CompareOptions) const متد

به دنبال زیررشته می‌گردد. فقط حالت Ordinal پشتیبانی می‌شود.

```cpp
virtual int System::Globalization::CompareInfo::IndexOf(const String &source, const String &value, int start_index, int count, CompareOptions options) const
```

### پارامترها

| Parameter | Type | Description |
| --- | --- | --- |
| source | const [String](../../../system/string/)\& | رشتهٔ منبع. |
| value | const [String](../../../system/string/)\& | مقدار جستجو. |
| start_index | int | شاخص شروع جستجو در رشتهٔ منبع. |
| count | int | تعداد کاراکترها در رشتهٔ منبع. |
| options | [CompareOptions](../../compareoptions/) | گزینه‌های جستجو. |

### مقدار بازگشتی

[Index](../../../system/index/) مقدار جستجو در رشتهٔ منبع یا -1 اگر یافت نشود.

## CompareInfo::IndexOf(const String\&, char16_t, int, int, CompareOptions) const متد

به دنبال نویسهٔ مشخص شده می‌گردد. فقط حالت Ordinal پشتیبانی می‌شود.

```cpp
virtual int System::Globalization::CompareInfo::IndexOf(const String &source, char16_t value, int start_index, int count, CompareOptions options) const
```

### پارامترها

| Parameter | Type | Description |
| --- | --- | --- |
| source | const [String](../../../system/string/)\& | رشتهٔ منبع. |
| value | char16_t | نویسه. |
| start_index | int | شاخص شروع جستجو در رشتهٔ منبع. |
| count | int | تعداد کاراکترها در رشتهٔ منبع. |
| options | [CompareOptions](../../compareoptions/) | گزینه‌های جستجو. |

### مقدار بازگشتی

[Index](../../../system/index/) مقدار جستجو در رشتهٔ منبع یا -1 اگر یافت نشود.

## CompareInfo::IndexOf(const String\&, const String\&, int) const متد

به دنبال زیررشته می‌گردد.

```cpp
virtual int System::Globalization::CompareInfo::IndexOf(const String &source, const String &value, int start_index) const
```

### پارامترها

| Parameter | Type | Description |
| --- | --- | --- |
| source | const [String](../../../system/string/)\& | رشتهٔ منبع. |
| value | const [String](../../../system/string/)\& | مقدار جستجو. |
| start_index | int | شاخص شروع جستجو در رشتهٔ منبع. |

### مقدار بازگشتی

[Index](../../../system/index/) مقدار جستجو در رشتهٔ منبع یا -1 اگر یافت نشود.

## CompareInfo::IndexOf(const String\&, char16_t) const متد

به دنبال نویسهٔ مشخص شده می‌گردد.

```cpp
virtual int System::Globalization::CompareInfo::IndexOf(const String &source, char16_t value) const
```

### پارامترها

| Parameter | Type | Description |
| --- | --- | --- |
| source | const [String](../../../system/string/)\& | رشتهٔ منبع. |
| value | char16_t | نویسه. |

### مقدار بازگشتی

[Index](../../../system/index/) مقدار جستجو در رشتهٔ منبع یا -1 اگر یافت نشود.

## CompareInfo::IndexOf(const String\&, const String\&) const متد

به دنبال زیررشته می‌گردد.

```cpp
virtual int System::Globalization::CompareInfo::IndexOf(const String &source, const String &value) const
```

### پارامترها

| Parameter | Type | Description |
| --- | --- | --- |
| source | const [String](../../../system/string/)\& | رشتهٔ منبع. |
| value | const [String](../../../system/string/)\& | مقدار جستجو. |

### مقدار بازگشتی

[Index](../../../system/index/) مقدار جستجو در رشتهٔ منبع یا -1 اگر یافت نشود.

## CompareInfo::IndexOf(const String\&, char16_t, int, CompareOptions) const متد

به دنبال نویسهٔ مشخص شده می‌گردد. فقط حالت Ordinal پشتیبانی می‌شود.

```cpp
virtual int System::Globalization::CompareInfo::IndexOf(const String &source, char16_t value, int start_index, CompareOptions options) const
```

### پارامترها

| Parameter | Type | Description |
| --- | --- | --- |
| source | const [String](../../../system/string/)\& | رشتهٔ منبع. |
| value | char16_t | نویسه. |
| start_index | int | شاخص شروع جستجو در رشتهٔ منبع. |
| options | [CompareOptions](../../compareoptions/) | گزینه‌های جستجو. |

### مقدار بازگشتی

[Index](../../../system/index/) مقدار جستجو در رشتهٔ منبع یا -1 اگر یافت نشود.

## CompareInfo::IndexOf(const String\&, char16_t, int, int) const متد

به دنبال نویسهٔ مشخص شده می‌گردد.

```cpp
virtual int System::Globalization::CompareInfo::IndexOf(const String &source, char16_t value, int start_index, int count) const
```

### پارامترها

| Parameter | Type | Description |
| --- | --- | --- |
| source | const [String](../../../system/string/)\& | رشتهٔ منبع. |
| value | char16_t | نویسه. |
| start_index | int | شاخص شروع جستجو در رشتهٔ منبع. |
| count | int | تعداد کاراکترها در رشتهٔ منبع. |

### مقدار بازگشتی

[Index](../../../system/index/) مقدار جستجو در رشتهٔ منبع یا -1 اگر یافت نشود.

## CompareInfo::IndexOf(const String\&, char16_t, int) const متد

به دنبال نویسهٔ مشخص شده می‌گردد.

```cpp
virtual int System::Globalization::CompareInfo::IndexOf(const String &source, char16_t value, int start_index) const
```

### پارامترها

| Parameter | Type | Description |
| --- | --- | --- |
| source | const [String](../../../system/string/)\& | رشتهٔ منبع. |
| value | char16_t | نویسه. |
| start_index | int | شاخص شروع جستجو در رشتهٔ منبع. |

### مقدار بازگشتی

[Index](../../../system/index/) مقدار جستجو در رشتهٔ منبع یا -1 اگر یافت نشود.

## CompareInfo::IndexOf(const String\&, const String\&, CompareOptions) const متد

به دنبال زیررشته می‌گردد. فقط حالت Ordinal پشتیبانی می‌شود.

```cpp
virtual int System::Globalization::CompareInfo::IndexOf(const String &source, const String &value, CompareOptions options) const
```

### پارامترها

| Parameter | Type | Description |
| --- | --- | --- |
| source | const [String](../../../system/string/)\& | رشتهٔ منبع. |
| value | const [String](../../../system/string/)\& | مقدار جستجو. |
| options | [CompareOptions](../../compareoptions/) | گزینه‌های جستجو. |

### مقدار بازگشتی

[Index](../../../system/index/) مقدار جستجو در رشتهٔ منبع یا -1 اگر یافت نشود.

## CompareInfo::IndexOf(const String\&, char16_t, CompareOptions) const متد

به دنبال نویسهٔ مشخص شده می‌گردد. فقط حالت Ordinal پشتیبانی می‌شود.

```cpp
virtual int System::Globalization::CompareInfo::IndexOf(const String &source, char16_t value, CompareOptions options) const
```

### پارامترها

| Parameter | Type | Description |
| --- | --- | --- |
| source | const [String](../../../system/string/)\& | رشتهٔ منبع. |
| value | char16_t | نویسه. |
| options | [CompareOptions](../../compareoptions/) | گزینه‌های جستجو. |

### مقدار بازگشتی

[Index](../../../system/index/) مقدار جستجو در رشتهٔ منبع یا -1 اگر یافت نشود.

## همچنین ببینید

* Enum [CompareOptions](../../compareoptions/)
* Class [String](../../../system/string/)
* Class [CompareInfo](../)
* Namespace [System::Globalization](../../)
* Library [Aspose.Slides](../../../)