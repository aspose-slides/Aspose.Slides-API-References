---
title: IsPrefix()
second_title: Aspose.Slides برای C++ مرجع API
description: بررسی می‌کند که آیا رشتهٔ مشخص‌شده با پیشوند مشخص‌شده شروع می‌شود و از گزینه‌های مقایسهٔ مشخص‌شده استفاده می‌کند.
type: docs
weight: 105
url: /fa/system.globalization/compareinfo/isprefix/
---
## CompareInfo::IsPrefix(const String\&, const String\&, CompareOptions) const متد

بررسی می‌کند که آیا رشتهٔ مشخص‌شده با پیشوند مشخص‌شده شروع می‌شود و از گزینه‌های مقایسهٔ مشخص‌شده استفاده می‌کند.

```cpp
virtual bool System::Globalization::CompareInfo::IsPrefix(const String &source, const String &prefix, CompareOptions options) const
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| source | const [String](../../../system/string/)\& | رشتهٔ منبع. |
| prefix | const [String](../../../system/string/)\& | رشتهٔ پیشوند. |
| options | [CompareOptions](../../compareoptions/) | گزینه‌های مقایسه. |

### مقدار بازگشتی

True اگر رشته با پیشوند شروع شود؛ در غیر این صورت false.

## CompareInfo::IsPrefix(const String\&, const String\&) const متد

بررسی می‌کند که آیا رشتهٔ مشخص‌شده با پیشوند مشخص‌شده شروع می‌شود.

```cpp
virtual bool System::Globalization::CompareInfo::IsPrefix(const String &source, const String &prefix) const
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| source | const [String](../../../system/string/)\& | رشتهٔ منبع. |
| prefix | const [String](../../../system/string/)\& | رشتهٔ پیشوند. |

### مقدار بازگشتی

True اگر رشته با پیشوند شروع شود؛ در غیر این صورت false.

## موارد مرتبط

* enum [CompareOptions](../../compareoptions/)
* کلاس [String](../../../system/string/)
* کلاس [CompareInfo](../)
* فضای نام [System::Globalization](../../)
* کتابخانه [Aspose.Slides](../../../)