---
title: "System::Web::Services::Protocols"
second_title: مرجع API Aspose.Slides برای C++
description: 
type: docs
weight: 1080
url: /fa/system.web.services.protocols/
---
## کلاس‌ها

| کلاس | توضیح |
| --- | --- |
| [Details_SoapException](./details_soapexception/) | نمایشگر استثنایی است که هنگام فراخوانی متد بر روی SOAP و بروز خطا رخ می‌دهد. هرگز نمونه‌های این کلاس را به‌صورت دستی ایجاد نکنید. به‌جای آن از کلاس SoapException استفاده کنید. هرگز نمونه‌های کلاس SoapException را در [System::SmartPtr](../system/smartptr/) بسته‌بندی نکنید. |
| [HttpWebClientProtocol](./httpwebclientprotocol/) | این کلاس پایه در تمام پراکسی‌های کلاینت سرویس XML [Web](../system.web/) که از HTTP استفاده می‌کنند، به کار می‌رود. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از اپراتور new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا نقص‌های اطمینان می‌شود. همیشه این کلاس را در اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای ارسال به توابع به‌عنوان آرگومان استفاده کنید. |
| [InvokeCompletedEventArgs](./invokecompletedeventargs/) | یک نمونه از این کلاس به عنوان آرگومان به تفویض‌کننده InvokeCompletedEventHandler منتقل می‌شود. اشیاء این کلاس باید تنها با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از اپراتور new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا نقص‌های اطمینان می‌شود. همیشه این کلاس را در اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای ارسال به توابع به‌عنوان آرگومان استفاده کنید. |
| [SoapClientMessage](./soapclientmessage/) | نمایشگر داده‌های یک درخواست SOAP ارسال‌شده یا پاسخ SOAP دریافت‌شده است. اشیاء این کلاس باید فقط توسط تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از اپراتور new ایجاد نکنید، زیرا باعث خطاهای زمان اجرا و/یا نقص‌های اطمینان می‌شود. همیشه این کلاس را در اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای پاس دادن به توابع به‌عنوان آرگومان استفاده کنید. |
| [SoapDocumentMethodAttribute](./soapdocumentmethodattribute/) | مشخص می‌کند که همه پیام‌های SOAP که از متد عبور می‌کنند یا بازگردانده می‌شوند، از قالب‌بندی Document استفاده کنند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از اپراتور new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا نقص‌های اطمینان می‌شود. همیشه این کلاس را در اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای پاس دادن به توابع به‌عنوان آرگومان استفاده کنید. |
| [SoapDocumentServiceAttribute](./soapdocumentserviceattribute/) | قالب پیش‌فرض برای درخواست‌ها و پاسخ‌های SOAP را تنظیم می‌کند. اشیاء این کلاس باید فقط توسط تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از اپراتور new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا نقص‌های اطمینان می‌شود. همیشه این کلاس را در اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای پاس دادن به توابع به‌عنوان آرگومان استفاده کنید. |
| [SoapHeader](./soapheader/) | محتوای هدر SOAP را نمایان می‌سازد. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از اپراتور new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا نقص‌های اطمینان می‌شود. همیشه این کلاس را در اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای پاس دادن به توابع به‌عنوان آرگومان استفاده کنید. |
| [SoapHeaderAttribute](./soapheaderattribute/) | هدر SOAP را مشخص می‌کند که متد سرویس XML [Web](../system.web/) یا کلاینت سرویس XML [Web](../system.web/) می‌تواند پردازش کند. اشیاء این کلاس باید فقط توسط تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از اپراتور new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا نقص‌های اطمینان می‌شود. همیشه این کلاس را در اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای پاس دادن به توابع به‌عنوان آرگومان استفاده کنید. |
| [SoapHeaderCollection](./soapheadercollection/) | حاوی مجموعه‌ای از نمونه‌های کلاس [SoapHeader](./soapheader/) است. |
| [SoapHttpClientProtocol](./soaphttpclientprotocol/) | سرویس‌های پراکسی کلاینت باید هنگام استفاده از SOAP از این کلاس ارث‌بری کنند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از اپراتور new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا نقص‌های اطمینان می‌شود. همیشه این کلاس را در اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای پاس دادن به توابع به‌عنوان آرگومان استفاده کنید. |
| [SoapMessage](./soapmessage/) | نمایشگر پیام SOAP است. اشیاء این کلاس باید فقط توسط تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از اپراتور new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا نقص‌های اطمینان می‌شود. همیشه این کلاس را در اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای پاس دادن به توابع به‌عنوان آرگومان استفاده کنید. |
| [WebClientProtocol](./webclientprotocol/) | این کلاس پایه در تمام پراکسی‌های کلاینت سرویس XML [Web](../system.web/) که با ASP.NET ساخته شده‌اند، استفاده می‌شود. اشیاء این کلاس باید فقط توسط تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از اپراتور new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا نقص‌های اطمینان می‌شود. همیشه این کلاس را در اشاره‌گر [System::SmartPtr](../system/smartptr/) بسته‌بندی کنید و از این اشاره‌گر برای پاس دادن به توابع به‌عنوان آرگومان استفاده کنید. |

## انواع شمارشی

| نوع شمارشی | توضیح |
| --- | --- |
| [SoapHeaderDirection](./soapheaderdirection/) | جهت‌های هدر SOAP را فهرست می‌کند. |
| [SoapMessageStage](./soapmessagestage/) | مراحل پردازش پیام‌های SOAP را فهرست می‌کند. |
| [SoapParameterStyle](./soapparameterstyle/) | قالب‌های پارامترها در یک پیام SOAP را فهرست می‌کند. |
| [SoapProtocolVersion](./soapprotocolversion/) | نسخه‌های SOAP را فهرست می‌کند. |
| [SoapServiceRoutingStyle](./soapserviceroutingstyle/) | گزینه‌های مسیردهی پیام SOAP به سرویس XML [Web](../system.web/) را فهرست می‌کند. |

## تعریف نوع

| تعریف نوع | توضیح |
| --- | --- |
| [SoapException](./soapexception/) |  |