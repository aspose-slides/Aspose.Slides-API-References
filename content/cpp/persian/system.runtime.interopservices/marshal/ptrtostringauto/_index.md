---
title: PtrToStringAuto()
second_title: مرجع API Aspose.Slides برای C++
description: یک String مدیریت‌شده را از یک رشته بدون مدیریت که با صفر پایان می‌یابد، ایجاد می‌کند.
type: docs
weight: 287
url: /fa/system.runtime.interopservices/marshal/ptrtostringauto/
---
## Marshal::PtrToStringAuto(IntPtr) متد

یک [String](../../../system/string/) مدیریت‌شده را از یک رشته بدون مدیریت که با صفر پایان می‌یابد، ایجاد می‌کند.

```cpp
static String System::Runtime::InteropServices::Marshal::PtrToStringAuto(IntPtr ptr)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| ptr | IntPtr | اشاره‌گر به رشته بدون مدیریت. |

### مقدار بازگشت

یک رشته مدیریت‌شده.

## Marshal::PtrToStringAuto(IntPtr, int) متد

یک [String](../../../system/string/) مدیریت‌شده را از یک رشته بدون مدیریت ایجاد می‌کند.

```cpp
static String System::Runtime::InteropServices::Marshal::PtrToStringAuto(IntPtr ptr, int length)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| ptr | IntPtr | اشاره‌گر به رشته بدون مدیریت. |
| length | int | طول رشته بدون مدیریت. |

### مقدار بازگشت

یک رشته مدیریت‌شده.

## موارد مرتبط

* کلاس [String](../../../system/string/)
* کلاس [Marshal](../)
* فضای‌نام [System::Runtime::InteropServices](../../)
* کتابخانه [Aspose.Slides](../../../)