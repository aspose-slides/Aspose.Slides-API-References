---
title: PtrToStringUni()
second_title: Aspose.Slides برای C++ مرجع API
description: یک String مدیریت‌شده را از یک رشته یونیکد بدون مدیریت که با صفر خاتمه می‌یابد ایجاد می‌کند.
type: docs
weight: 300
url: /fa/system.runtime.interopservices/marshal/ptrtostringuni/
---
## Marshal::PtrToStringUni(IntPtr) متد

یک [String](../../../system/string/) مدیریت‌شده را از یک رشته یونیکد با پایان صفر بدون مدیریت ایجاد می‌کند.

```cpp
static String System::Runtime::InteropServices::Marshal::PtrToStringUni(IntPtr ptr)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| ptr | IntPtr | اشاره‌گر به رشته بدون مدیریت. |

### مقدار بازگشت

یک رشته مدیریت‌شده.

## Marshal::PtrToStringUni(IntPtr, int) متد

یک [String](../../../system/string/) مدیریت‌شده را از یک رشته یونیکد بدون مدیریت ایجاد می‌کند.

```cpp
static String System::Runtime::InteropServices::Marshal::PtrToStringUni(IntPtr ptr, int length)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| ptr | IntPtr | اشاره‌گر به رشته بدون مدیریت. |
| length | int | طول رشته بدون مدیریت. |

### مقدار بازگشت

یک رشته مدیریت‌شده.

## موارد مرتبط

* کلاس [String](../../../system/string/)
* کلاس [Marshal](../)
* فضای‌نام [System::Runtime::InteropServices](../../)
* کتابخانه [Aspose.Slides](../../../)