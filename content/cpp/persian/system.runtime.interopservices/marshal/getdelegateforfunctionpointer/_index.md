---
title: GetDelegateForFunctionPointer()
second_title: Aspose.Slides برای C++ مرجع API
description: یک اشاره‌گر تابع غیرمدیریت‌شده را به یک نماینده از نوع مشخص تبدیل می‌کند.
type: docs
weight: 14
url: /fa/system.runtime.interopservices/marshal/getdelegateforfunctionpointer/
---
## Marshal::GetDelegateForFunctionPointer(IntPtr) متد

یک اشاره‌گر توابع غیرمدیریت‌شده را به یک نماینده از نوع مشخص تبدیل می‌کند.

```cpp
template<typename TDelegate> static TDelegate System::Runtime::InteropServices::Marshal::GetDelegateForFunctionPointer(IntPtr ptr)
```

### پارامترهای قالب

| پارامتر | توضیح |
| --- | --- |
| TDelegate | نوع نماینده‌ای که باید برگردانده شود. |

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| ptr | IntPtr | اشاره‌گر توابع غیرمدیریت‌شده برای تبدیل. |

### مقدار بازگشت

یک نمونه از نوع نماینده مشخص‌شده.

## توضیحات

یک قالب اولیه برای پیاده‌سازی آینده

## موارد مرتبط

* کلاس [Marshal](../)
* فضای‌نام [System::Runtime::InteropServices](../../)
* کتابخانه [Aspose.Slides](../../../)