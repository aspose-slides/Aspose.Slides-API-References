---
title: StringToHGlobalAuto()
second_title: مرجع API Aspose.Slides برای C++
description: محتویات یک رشته مشخص را به حافظه unmanaged کپی می‌کند و در صورت نیاز به فرمت ANSI تبدیل می‌نماید.
type: docs
weight: 261
url: /fa/system.runtime.interopservices/marshal/stringtohglobalauto/
---
## Marshal::StringToHGlobalAuto(const String\&) متد

محتویات یک رشته مشخص را به حافظه unmanaged کپی می‌کند و در صورت نیاز به فرمت ANSI تبدیل می‌نماید.

```cpp
static IntPtr System::Runtime::InteropServices::Marshal::StringToHGlobalAuto(const String &s)
```

### پارامترها

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| s | const [String](../../../system/string/)\& | رشته‌ای که باید کپی شود. |

### مقدار بازگشتی

آدرس در حافظه غیرمدیریت شده.

## موارد مرتبط

* کلاس [String](../../../system/string/)
* کلاس [Marshal](../)
* فضای‌نام [System::Runtime::InteropServices](../../)
* کتابخانه [Aspose.Slides](../../../)