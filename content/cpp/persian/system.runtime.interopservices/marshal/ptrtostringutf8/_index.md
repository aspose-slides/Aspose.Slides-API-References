---
title: PtrToStringUTF8()
second_title: Aspose.Slides برای C++ مرجع API
description: یک String مدیریت‌شده از یک رشتهٔ UTF8 غیر مدیریتی با انتهای صفر ایجاد می‌کند.
type: docs
weight: 313
url: /fa/system.runtime.interopservices/marshal/ptrtostringutf8/
---
## Marshal::PtrToStringUTF8(IntPtr) متد

یک [String](../../../system/string/) مدیریت‌شده از یک رشتهٔ UTF8 غیر مدیریتی با انتهای صفر ایجاد می‌کند.

```cpp
static String System::Runtime::InteropServices::Marshal::PtrToStringUTF8(IntPtr ptr)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| ptr | IntPtr | اشاره‌گر به رشتهٔ غیر مدیریتی. |

### مقدار بازگشت

یک رشتهٔ مدیریت‌شده.

## Marshal::PtrToStringUTF8(IntPtr, int) متد

یک [String](../../../system/string/) مدیریت‌شده از یک رشتهٔ UTF8 غیر مدیریتی ایجاد می‌کند.

```cpp
static String System::Runtime::InteropServices::Marshal::PtrToStringUTF8(IntPtr ptr, int length)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| ptr | IntPtr | اشاره‌گر به رشتهٔ غیر مدیریتی. |
| length | int | طول رشتهٔ غیر مدیریتی. |

### مقدار بازگشت

یک رشتهٔ مدیریت‌شده.

## موارد مرتبط

* کلاس [String](../../../system/string/)
* کلاس [Marshal](../)
* فضای نام [System::Runtime::InteropServices](../../)
* کتابخانه [Aspose.Slides](../../../)