---
title: PtrToStringAnsi()
second_title: Aspose.Slides برای مرجع API C++
description: یک String مدیریت‌شده از یک رشته UTF8 صفرپایان‌دار غیرمدیریت‌شده ایجاد می‌کند.
type: docs
weight: 274
url: /fa/system.runtime.interopservices/marshal/ptrtostringansi/
---
## Marshal::PtrToStringAnsi(IntPtr) متد

یک [String](../../../system/string/) مدیریت‌شده از یک رشته UTF8 صفرپایان‌دار غیرمدیریت‌شده ایجاد می‌کند.

```cpp
static String System::Runtime::InteropServices::Marshal::PtrToStringAnsi(IntPtr ptr)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| ptr | IntPtr | اشاره‌گر به رشته غیرمدیریت‌شده. |

### مقدار بازگشت

یک string مدیریت‌شده.

## Marshal::PtrToStringAnsi(IntPtr, int) متد

یک [String](../../../system/string/) مدیریت‌شده از یک رشته UTF8 غیرمدیریت‌شده ایجاد می‌کند.

```cpp
static String System::Runtime::InteropServices::Marshal::PtrToStringAnsi(IntPtr ptr, int length)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| ptr | IntPtr | اشاره‌گر به رشته غیرمدیریت‌شده. |
| length | int | طول رشته غیرمدیریت‌شده. |

### مقدار بازگشت

یک string مدیریت‌شده.

## موارد مرتبط

* کلاس [String](../../../system/string/)
* کلاس [Marshal](../)
* فضای‌نام [System::Runtime::InteropServices](../../)
* کتابخانه [Aspose.Slides](../../../)