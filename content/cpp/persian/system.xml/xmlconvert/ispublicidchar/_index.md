---
title: IsPublicIdChar()
second_title: مرجع API Aspose.Slides برای C++
description: در صورتی که کاراکتر در آرگومان یک کاراکتر شناسه عمومی معتبر باشد، نمونهٔ کاراکتر پاس شده را برمی‌گرداند؛ در غیر این صورت nullptr.
type: docs
weight: 196
url: /fa/system.xml/xmlconvert/ispublicidchar/
---
## XmlConvert::IsPublicIdChar(char16_t) متد

در صورتی که کاراکتر ورودی یک کاراکتر شناسه عمومی معتبر باشد، نمونهٔ کاراکتر پاس شده را برمی‌گرداند، در غیر این صورت **nullptr** را باز می‌گرداند.

```cpp
static bool System::Xml::XmlConvert::IsPublicIdChar(char16_t ch)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| ch | char16_t | [Char](../../../system/char/) شی برای اعتبارسنجی. |

### مقدار بازگشتی

کاراکتر پاس شده در صورتی که کاراکتر یک شناسه عمومی معتبر باشد، در غیر این صورت **nullptr**.

## موارد مرتبط

* کلاس [XmlConvert](../)
* فضای نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)