---
title: VerifyXmlChars()
second_title: مرجع API Aspose.Slides برای C++
description: اگر تمام کاراکترها و کاراکترهای جفت‌سورگاتی در آرگومان رشته معتبر باشند، رشتهٔ ورودی بازگردانده می‌شود؛ در غیر این صورت یک XmlException با اطلاعات دربارهٔ اولین کاراکتر نامعتبر پرتاب می‌شود.
type: docs
weight: 105
url: /fa/system.xml/xmlconvert/verifyxmlchars/
---
## XmlConvert::VerifyXmlChars(const String\&) متد

اگر تمام کاراکترها و کاراکترهای جفت‌سورگاتی در آرگومان رشته معتبر باشند، رشتهٔ ورودی برگردانده می‌شود؛ در غیر این صورت یک XmlException با اطلاعات دربارهٔ اولین کاراکتر نامعتبر پرتاب می‌شود.

```cpp
static String System::Xml::XmlConvert::VerifyXmlChars(const String &content)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| content | const [String](../../../system/string/)\& | [String](../../../system/string/) که شامل کاراکترهایی برای تأیید است. |

### مقدار برگشت

اگر تمام کاراکترها و کاراکترهای جفت‌سورگاتی در آرگومان رشته معتبر باشند، رشتهٔ ورودی برگردانده می‌شود؛ در غیر این صورت یک XmlException با اطلاعات دربارهٔ اولین کاراکتر نامعتبر پرتاب می‌شود.

## مراجع

* کلاس [String](../../../system/string/)
* کلاس [XmlConvert](../)
* فضای نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)