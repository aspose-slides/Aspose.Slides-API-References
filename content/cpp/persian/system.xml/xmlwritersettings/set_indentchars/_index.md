---
title: set_IndentChars()
second_title: مرجع API Aspose.Slides برای C++
description: "رشته کاراکتری را تنظیم می‌کند که هنگام تورفتگی استفاده می‌شود. این تنظیم زمانی استفاده می‌شود که مقدار XmlWriterSettings::set_Indent برابر true باشد."
type: docs
weight: 144
url: /fa/system.xml/xmlwritersettings/set_indentchars/
---
## XmlWriterSettings::set_IndentChars(const String\&) متد

رشته کاراکتری را که هنگام تورفتگی استفاده می‌شود تنظیم می‌کند. این تنظیم زمانی استفاده می‌شود که مقدار [XmlWriterSettings::set_Indent](../set_indent/) به **true** تنظیم شده باشد.

```cpp
void System::Xml::XmlWriterSettings::set_IndentChars(const String &value)
```

### Arguments

| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | const [String](../../../system/string/)\& | مقداری که باید تنظیم شود. |

## موارد مرتبط

* کلاس [String](../../../system/string/)
* کلاس [XmlWriterSettings](../)
* فضای نام [System::Xml](../../)
* Library [Aspose.Slides](../../../)