---
title: ToString()
second_title: Aspose.Slides برای C++ مرجع API
description: مقدار رشته‌ای XmlQualifiedName را برمی‌گرداند.
type: docs
weight: 79
url: /fa/system.xml/xmlqualifiedname/tostring/
---
## XmlQualifiedName::ToString() const متد

مقدار رشته‌ای [XmlQualifiedName](../) را برمی‌گرداند.

```cpp
String System::Xml::XmlQualifiedName::ToString() const override
```

### مقدار بازگشتی

مقدار رشته‌ای [XmlQualifiedName](../) به قالب **namespace:localname**. اگر شیء نام‌فضایی تعریف نشده داشته باشد، این متد فقط نام محلی را برمی‌گرداند.

## XmlQualifiedName::ToString(const String\&, const String\&) متد

مقدار رشته‌ای [XmlQualifiedName](../) را برمی‌گرداند.

```cpp
static String System::Xml::XmlQualifiedName::ToString(const String &name, const String &ns)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| name | const [String](../../../system/string/)\& | نام شیء |
| ns | const [String](../../../system/string/)\& | نام‌فضای شیء |

### مقدار بازگشتی

مقدار رشته‌ای [XmlQualifiedName](../) به قالب **namespace:localname**. اگر شیء نام‌فضایی تعریف نشده داشته باشد، این متد فقط نام محلی را برمی‌گرداند.

## همچنین ببینید

* کلاس [String](../../../system/string/)
* کلاس [XmlQualifiedName](../)
* فضای‌نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)