---
title: get_CheckCharacters()
second_title: مرجع API Aspose.Slides برای C++
description: یک مقدار بازمی‌گرداند که نشان می‌دهد آیا باید بررسی کاراکترها انجام شود یا خیر.
type: docs
weight: 118
url: /fa/system.xml/xmlreadersettings/get_checkcharacters/
---
## XmlReaderSettings::get_CheckCharacters() متد

یک مقدار بازمی‌گرداند که نشان می‌دهد آیا باید بررسی کاراکترها انجام شود یا نه.

```cpp
bool System::Xml::XmlReaderSettings::get_CheckCharacters()
```

### مقدار بازگشت

**true** برای انجام بررسی کاراکترها؛ در غیر این صورت **false**. مقدار پیش‌فرض **true** است.

## مراجع

* کلاس [XmlReaderSettings](../)
* فضای نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)