---
title: set_MaxCharactersInDocument()
second_title: Aspose.Slides برای مرجع API C++
description: مقداری را تنظیم می‌کند که حداکثر تعداد کاراکترهای مجاز در یک سند XML را نشان می‌دهد. مقدار صفر (0) به این معنی است که هیچ محدودیتی برای اندازهٔ سند XML وجود ندارد. مقدار غیر صفر حداکثر اندازه را به کاراکترها مشخص می‌کند.
type: docs
weight: 157
url: /fa/system.xml/xmlreadersettings/set_maxcharactersindocument/
---
## XmlReaderSettings::set_MaxCharactersInDocument(int64_t) متد

یک مقدار را که نشانگر حداکثر تعداد کاراکترهای مجاز در یک سند XML است تنظیم می‌کند. مقدار صفر (0) به این معنی است که هیچ محدودیتی برای اندازه سند XML وجود ندارد. مقدار غیر صفر حداکثر اندازه را به کاراکتر مشخص می‌گردد.

```cpp
void System::Xml::XmlReaderSettings::set_MaxCharactersInDocument(int64_t value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | **int64_t** | مقداری که باید تنظیم شود. |

## همچنین ببینید

* کلاس [XmlReaderSettings](../)
* فضای نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)