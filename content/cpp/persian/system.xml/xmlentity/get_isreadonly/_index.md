---
title: get_IsReadOnly()
second_title: Aspose.Slides برای مرجع API C++
description: مقداری را برمی‌گرداند که نشان می‌دهد آیا گره فقط-خواندنی است یا خیر.
type: docs
weight: 1
url: /fa/system.xml/xmlentity/get_isreadonly/
---
## XmlEntity::get_IsReadOnly() متد

مقداری را برمی‌گرداند که نشان می‌دهد آیا گره فقط-خواندنی است یا خیر.

```cpp
bool System::Xml::XmlEntity::get_IsReadOnly() override
```

### مقدار بازگشت

**true** اگر گره فقط-خواندنی باشد؛ در غیر این صورت، **false**. چون گره‌های [XmlEntity](../) فقط-خواندنی هستند، این متد همیشه **true** برمی‌گرداند.

## موارد مرتبط

* کلاس [XmlEntity](../)
* فضای نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)