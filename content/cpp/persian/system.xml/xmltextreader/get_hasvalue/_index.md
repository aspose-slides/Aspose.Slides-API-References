---
title: get_HasValue()
second_title: مرجع API Aspose.Slides برای C++
description: "یک مقدار را برمی‌گرداند که نشان می‌دهد آیا گره‌ی فعلی می‌تواند یک XmlTextReader::get_Value غیر از String::Empty داشته باشد."
type: docs
weight: 66
url: /fa/system.xml/xmltextreader/get_hasvalue/
---
## XmlTextReader::get_HasValue() متد

یک مقدار را برمی‌گرداند که نشان می‌دهد آیا گره فعلی می‌تواند یک [XmlTextReader::get_Value](../get_value/) غیر از [String::Empty](../../../system/string/empty/) داشته باشد.

```cpp
bool System::Xml::XmlTextReader::get_HasValue() override
```

### مقدار بازگشت

**true** اگر گره‌ای که خواننده در حال حاضر روی آن موقعیت یافته می‌تواند یک **Value** داشته باشد؛ در غیر این صورت، **false**.

## موارد مرتبط

* کلاس [XmlTextReader](../)
* فضای‌نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)