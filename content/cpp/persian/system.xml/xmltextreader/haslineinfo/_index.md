---
title: HasLineInfo()
second_title: مرجع API Aspose.Slides برای C++
description: مقدار را برمی‌گرداند که نشان می‌دهد آیا کلاس می‌تواند اطلاعات خط را برگرداند.
type: docs
weight: 703
url: /fa/system.xml/xmltextreader/haslineinfo/
---
## XmlTextReader::HasLineInfo() متد

یک مقدار برمی‌گرداند که نشان می‌دهد آیا کلاس می‌تواند اطلاعات خط را برگرداند.

```cpp
bool System::Xml::XmlTextReader::HasLineInfo() override
```

### مقدار بازگشتی

**true** اگر کلاس بتواند اطلاعات خط را برگرداند؛ در غیر این صورت **false**.

## همچنین ببینید

* کلاس [XmlTextReader](../)
* فضای‌نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)