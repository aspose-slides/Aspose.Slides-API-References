---
title: LookupNamespace()
second_title: مرجع API Aspose.Slides برای C++
description: پیشوند یک فضای نام را در دامنهٔ عنصر جاری حل می‌کند.
type: docs
weight: 404
url: /fa/system.xml/xmlnodereader/lookupnamespace/
---
## XmlNodeReader::LookupNamespace(const String\&) method

پیشوند یک فضای نام را در دامنهٔ عنصر جاری حل می‌کند.

```cpp
String System::Xml::XmlNodeReader::LookupNamespace(const String &prefix) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| prefix | const [String](../../../system/string/)\& | پیشوندی که می‌خواهید URI فضای نام آن را حل کنید. برای مطابقت با فضای نام پیش‌فرض، یک رشتهٔ خالی را پاس کنید. این رشته نیازی به اتم‌سازی ندارد. |

### مقدار بازگشت

URI فضای نامی که پیشوند به آن نگاشته می‌شود یا **nullptr** اگر پیشوند مطابقی یافت نشد.

## مراجع

* کلاس [String](../../../system/string/)
* کلاس [XmlNodeReader](../)
* فضای‌نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)