---
title: get_IsReadOnly()
second_title: مرجع API Aspose.Slides برای C++
description: یک مقدار برمی‌گرداند که نشان می‌دهد آیا گره فقط‌خواندنی است.
type: docs
weight: 222
url: /fa/system.xml/xmlnode/get_isreadonly/
---
## XmlNode::get_IsReadOnly() متد

یک مقدار را برمی‌گرداند که نشان می‌دهد آیا گره فقط‌خواندنی است.

```cpp
virtual bool System::Xml::XmlNode::get_IsReadOnly()
```

### مقدار بازگشت

**true** اگر گره فقط‌خواندنی باشد؛ در غیر این صورت، **false**.

## موارد مرتبط

* کلاس [XmlNode](../)
* فضای نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)