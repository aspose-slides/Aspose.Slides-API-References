---
title: get_IsReadOnly()
second_title: Aspose.Slides برای مرجع API C++
description: یک مقدار برمی‌گرداند که نشان می‌دهد آیا گرهٔ فعلی فقط-خواندنی است یا خیر.
type: docs
weight: 170
url: /fa/system.xml/xmldocument/get_isreadonly/
---
## XmlDocument::get_IsReadOnly() method


یک مقدار برمی‌گرداند که نشان می‌دهد آیا گرهٔ فعلی فقط-خواندنی است یا خیر.

```cpp
bool System::Xml::XmlDocument::get_IsReadOnly() override
```


### مقدار بازگشتی

**true** اگر گرهٔ فعلی فقط-خواندنی باشد؛ در غیر این صورت **false**. [XmlDocument](../) گره‌ها همیشه **false** برمی‌گردانند.

## مشاهده کنید

* کلاس [XmlDocument](../)
* فضای نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)