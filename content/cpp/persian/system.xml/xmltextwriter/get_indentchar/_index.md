---
title: get_IndentChar()
second_title: مرجع API Aspose.Slides برای C++
description: "کاراکتری که برای تورفتگی استفاده می‌شود را برمی‌گرداند وقتی XmlTextWriter::set_Formatting روی Formatting::Indented تنظیم شده باشد."
type: docs
weight: 92
url: /fa/system.xml/xmltextwriter/get_indentchar/
---
## XmlTextWriter::get_IndentChar() متد

کاراکتری که برای تورفتگی استفاده می‌شود را برمی‌گرداند وقتی [XmlTextWriter::set_Formatting](../set_formatting/) روی [Formatting::Indented](../../formatting/) تنظیم شده باشد.

```cpp
char16_t System::Xml::XmlTextWriter::get_IndentChar()
```

### مقدار بازگشت

کاراکتری که برای تورفتگی استفاده می‌شود. مقدار پیش‌فرض یک فاصله است.

## موارد مرتبط

* کلاس [XmlTextWriter](../)
* فضای نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)