---
title: ReadElementContentAsDateTime()
second_title: Aspose.Slides برای C++ مرجع API
description: المان فعلی را می‌خواند و محتوا را به عنوان یک شیء DateTime برمی‌گرداند.
type: docs
weight: 495
url: /fa/system.xml/xmlreader/readelementcontentasdatetime/
---
## XmlReader::ReadElementContentAsDateTime() متد

المان فعلی را می‌خواند و محتوا را به عنوان یک شیء [DateTime](../../../system/datetime/) برمی‌گرداند.

```cpp
virtual DateTime System::Xml::XmlReader::ReadElementContentAsDateTime()
```

### مقدار بازگشت

محتوای عنصر به عنوان یک شیء [DateTime](../../../system/datetime/).

## XmlReader::ReadElementContentAsDateTime(String, String) متد

بررسی می‌کند که نام محلی و فضای‌نام URI مشخص‌شده با عنصر فعلی مطابقت دارد، سپس عنصر فعلی را می‌خواند و محتوا را به عنوان یک شیء [DateTime](../../../system/datetime/) برمی‌گرداند.

```cpp
virtual DateTime System::Xml::XmlReader::ReadElementContentAsDateTime(String localName, String namespaceURI)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| localName | [String](../../../system/string/) | نام محلی عنصر |
| namespaceURI | [String](../../../system/string/) | URI فضای‌نام عنصر |

### مقدار بازگشت

محتوای عنصر به عنوان یک شیء [DateTime](../../../system/datetime/).

## موارد مرتبط

* کلاس [DateTime](../../../system/datetime/)
* کلاس [XmlReader](../)
* کلاس [String](../../../system/string/)
* فضای‌نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)