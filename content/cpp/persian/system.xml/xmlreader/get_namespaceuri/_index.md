---
title: get_NamespaceURI()
second_title: مرجع API Aspose.Slides برای C++
description: زمانی که در یک کلاس مشتق‌شده بازنویسی می‌شود، URI فضای‌نام (همان‌طور که در مشخصات فضای‌نام W3C تعریف شده است) گره‌ای که خواننده بر روی آن قرار دارد را دریافت می‌کند.
type: docs
weight: 53
url: /fa/system.xml/xmlreader/get_namespaceuri/
---
## XmlReader::get_NamespaceURI() متد


زمانی که در یک کلاس مشتق‌شده بازنویسی می‌شود، URI فضای‌نام (همان‌طور که در مشخصات فضای‌نام W3C تعریف شده است) گره‌ای که خواننده بر روی آن قرار دارد را برمی‌گرداند.

```cpp
virtual String System::Xml::XmlReader::get_NamespaceURI()=0
```


### مقدار بازگشت

URI فضای‌نام گرهٔ فعلی؛ در غیر این صورت یک رشتهٔ خالی.

## موارد مرتبط

* کلاس [String](../../../system/string/)
* کلاس [XmlReader](../)
* فضای‌نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)