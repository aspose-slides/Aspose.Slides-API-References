---
title: ReadElementContentAsLong()
second_title: Aspose.Slides برای C++ مرجع API
description: عنصر فعلی را می‌خواند و محتوا را به عنوان یک عدد صحیح ۶۴ بیتی با علامت برمی‌گرداند.
type: docs
weight: 560
url: /fa/system.xml/xmlreader/readelementcontentaslong/
---
## XmlReader::ReadElementContentAsLong() متد

عنصر فعلی را می‌خواند و محتوا را به عنوان یک عدد صحیح ۶۴ بیتی با علامت برمی‌گرداند.

```cpp
virtual int64_t System::Xml::XmlReader::ReadElementContentAsLong()
```

### مقدار بازگشت

مقدار محتویات عنصر به عنوان یک عدد صحیح ۶۴ بیتی با علامت.

## XmlReader::ReadElementContentAsLong(String, String) متد

بررسی می‌کند که نام محلی و URI فضای نام مشخص‌شده با عنصر فعلی مطابقت داشته باشد، سپس عنصر فعلی را می‌خواند و محتوا را به عنوان یک عدد صحیح ۶۴ بیتی با علامت برمی‌گرداند.

```cpp
virtual int64_t System::Xml::XmlReader::ReadElementContentAsLong(String localName, String namespaceURI)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| localName | [String](../../../system/string/) | نام محلی عنصر. |
| namespaceURI | [String](../../../system/string/) | URI فضای نام عنصر. |

### مقدار بازگشت

مقدار محتویات عنصر به عنوان یک عدد صحیح ۶۴ بیتی با علامت.

## مراجع مرتبط

* کلاس [XmlReader](../)
* کلاس [String](../../../system/string/)
* فضای نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)