---
title: ReadElementContentAsDouble()
second_title: Aspose.Slides برای C++ مرجع API
description: عنصر جاری را می‌خواند و محتوا را به عنوان عدد شناور با دقت دوبل برمی‌گرداند.
type: docs
weight: 508
url: /fa/system.xml/xmlreader/readelementcontentasdouble/
---
## XmlReader::ReadElementContentAsDouble() متد

عنصر جاری را می‌خواند و محتوا را به عنوان عدد شناور دوبل برمی‌گرداند.

```cpp
virtual double System::Xml::XmlReader::ReadElementContentAsDouble()
```

### مقدار بازگشت

محتوای عنصر به عنوان عدد شناور دوبل.

## XmlReader::ReadElementContentAsDouble(String, String) متد

بررسی می‌کند که نام محلی و URI فضای‌نامی مشخص‌شده با نام و فضای‌نامی عنصر جاری مطابقت داشته باشد، سپس عنصر جاری را می‌خواند و محتوا را به عنوان عدد شناور دوبل برمی‌گرداند.

```cpp
virtual double System::Xml::XmlReader::ReadElementContentAsDouble(String localName, String namespaceURI)
```

### پارامترها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| localName | [String](../../../system/string/) | نام محلی عنصر. |
| namespaceURI | [String](../../../system/string/) | URI فضای‌نامی عنصر. |

### مقدار بازگشت

محتوای عنصر به عنوان عدد شناور دوبل.

## موارد مرتبط

* کلاس [XmlReader](../)
* کلاس [String](../../../system/string/)
* فضای‌نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)