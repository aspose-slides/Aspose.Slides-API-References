---
title: ReadElementContentAsString()
second_title: Aspose.Slides برای مرجع API C++
description: عنصر فعلی را می‌خواند و محتوا را به عنوان یک شیء String برمی‌گرداند.
type: docs
weight: 573
url: /fa/system.xml/xmlreader/readelementcontentasstring/
---
## XmlReader::ReadElementContentAsString() متد

عنصر فعلی را می‌خواند و محتوا را به عنوان یک شیء [String](../../../system/string/) برمی‌گرداند.

```cpp
virtual String System::Xml::XmlReader::ReadElementContentAsString()
```

### مقدار بازگشت

محتوای عنصر به عنوان یک شیء [String](../../../system/string/).

## XmlReader::ReadElementContentAsString(String, String) متد

بررسی می‌کند که نام محلی و URI فضای نام مشخص‌شده با عنصر فعلی مطابقت داشته باشد، سپس عنصر فعلی را می‌خواند و محتوا را به عنوان یک شیء [String](../../../system/string/) برمی‌گرداند.

```cpp
virtual String System::Xml::XmlReader::ReadElementContentAsString(String localName, String namespaceURI)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| localName | [String](../../../system/string/) | نام محلی عنصر. |
| namespaceURI | [String](../../../system/string/) | URI فضای نام عنصر. |

### مقدار بازگشت

محتوای عنصر به عنوان یک شیء [String](../../../system/string/).

## موارد مرتبط

* کلاس [String](../../../system/string/)
* کلاس [XmlReader](../)
* فضای نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)