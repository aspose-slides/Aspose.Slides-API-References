---
title: ReadElementContentAsBoolean()
second_title: Aspose.Slides برای مرجع API C++
description: عنصر جاری را می‌خواند و محتوا را به عنوان یک شیء Boolean برمی‌گرداند.
type: docs
weight: 482
url: /fa/system.xml/xmlreader/readelementcontentasboolean/
---
## XmlReader::ReadElementContentAsBoolean() متد

عنصر جاری را می‎خواند و محتوا را به صورت یک شیء [Boolean](../../../system/boolean/) برمی‌گرداند.

```cpp
virtual bool System::Xml::XmlReader::ReadElementContentAsBoolean()
```

### مقدار بازگشت

محتوای عنصر به صورت یک شیء [Boolean](../../../system/boolean/).

## XmlReader::ReadElementContentAsBoolean(String, String) متد

بررسی می‌کند که نام محلی و URI فضای‌نامی مشخص‌شده با عنصر جاری منطبق باشد، سپس عنصر جاری را می‌خواند و محتوا را به صورت یک شیء [Boolean](../../../system/boolean/) برمی‌گرداند.

```cpp
virtual bool System::Xml::XmlReader::ReadElementContentAsBoolean(String localName, String namespaceURI)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| localName | [String](../../../system/string/) | نام محلی عنصر. |
| namespaceURI | [String](../../../system/string/) | URI فضای‌نامی عنصر. |

### مقدار بازگشت

محتوای عنصر به صورت یک شیء [Boolean](../../../system/boolean/).

## موارد مرتبط

* کلاس [XmlReader](../)
* کلاس [String](../../../system/string/)
* فضای‌نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)