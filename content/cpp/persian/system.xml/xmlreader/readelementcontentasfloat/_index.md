---
title: ReadElementContentAsFloat()
second_title: مرجع API Aspose.Slides برای C++
description: عنصر فعلی را می‌خواند و محتوا را به‌عنوان عدد شناور تک‌دقت باز می‌گرداند.
type: docs
weight: 521
url: /fa/system.xml/xmlreader/readelementcontentasfloat/
---
## XmlReader::ReadElementContentAsFloat() متد


عنصر فعلی را می‌خواند و محتوا را به‌عنوان عدد شناور تک‌دقت باز می‌گرداند.

```cpp
virtual float System::Xml::XmlReader::ReadElementContentAsFloat()
```


### مقدار بازگشت

محتوای عنصر به‌عنوان عدد شناور تک‌دقت.

## XmlReader::ReadElementContentAsFloat(String, String) متد


بررسی می‌کند که نام محلی و URI فضای نام مشخص‌شده با عنصر فعلی مطابقت دارد، سپس عنصر فعلی را می‌خواند و محتوا را به‌عنوان عدد شناور تک‌دقت باز می‌گرداند.

```cpp
virtual float System::Xml::XmlReader::ReadElementContentAsFloat(String localName, String namespaceURI)
```


### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| localName | [String](../../../system/string/) | نام محلی عنصر. |
| namespaceURI | [String](../../../system/string/) | URI فضای نام عنصر. |

### مقدار بازگشت

محتوای عنصر به‌عنوان عدد شناور تک‌دقت.

## موارد مرتبط

* کلاس [XmlReader](../)
* کلاس [String](../../../system/string/)
* فضای نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)