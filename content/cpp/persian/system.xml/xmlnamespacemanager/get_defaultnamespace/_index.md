---
title: get_DefaultNamespace()
second_title: راهنمای API Aspose.Slides برای C++
description: آدرس URI فضای نام برای فضای نام پیش‌فرض را بر می‌گرداند.
type: docs
weight: 14
url: /fa/system.xml/xmlnamespacemanager/get_defaultnamespace/
---
## XmlNamespaceManager::get_DefaultNamespace() method

آدرس URI فضای نام برای فضای نام پیش‌فرض را باز می‌گرداند.

```cpp
virtual String System::Xml::XmlNamespaceManager::get_DefaultNamespace()
```

### مقدار بازگشتی

آدرس URI فضای نام برای فضای نام پیش‌فرض، یا یک رشته خالی اگر فضای نام پیش‌فرضی وجود نداشته باشد.

## موارد مرتبط

* کلاس [String](../../../system/string/)
* کلاس [XmlNamespaceManager](../)
* فضای نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)