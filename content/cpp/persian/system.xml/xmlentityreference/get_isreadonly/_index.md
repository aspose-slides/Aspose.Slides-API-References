---
title: get_IsReadOnly()
second_title: مرجع API Aspose.Slides برای C++
description: یک مقدار برمی‌گرداند که نشان می‌دهد آیا گره فقط-خواندنی است.
type: docs
weight: 66
url: /fa/system.xml/xmlentityreference/get_isreadonly/
---
## XmlEntityReference::get_IsReadOnly() متد

یک مقدار برمی‌گرداند که نشان می‌دهد آیا گره فقط-خواندنی است.

```cpp
bool System::Xml::XmlEntityReference::get_IsReadOnly() override
```

### مقدار بازگشت

**true** اگر گره فقط-خواندنی باشد؛ در غیر این صورت **false**. چون گره‌های [XmlEntityReference](../) فقط-خواندنی هستند، این متد همواره **true** را برمی‌گرداند.

## موارد مرتبط

* کلاس [XmlEntityReference](../)
* فضای‌نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)