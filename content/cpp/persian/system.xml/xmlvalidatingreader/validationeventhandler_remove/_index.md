---
title: ValidationEventHandler_remove()
second_title: Aspose.Slides برای C++ - مرجع API
description: یک هندلر رویداد را برای دریافت اطلاعات دربارهٔ خطاهای اعتبارسنجی تعریف نوع سند (DTD)، طرح‌واره XML-Data Reduced (XDR) و زبان تعریف طرح‌واره XML (XSD) حذف می‌کند.
type: docs
weight: 664
url: /fa/system.xml/xmlvalidatingreader/validationeventhandler_remove/
---
## XmlValidatingReader::ValidationEventHandler_remove(Args...) متد

یک هندلر رویداد را برای دریافت اطلاعات دربارهٔ تعریف نوع سند (DTD)، طرح‌واره XML-Data Reduced (XDR) و XML [Schema](../../../system.xml.schema/) زبان تعریف (XSD) خطاهای اعتبارسنجی حذف می‌کند.

```cpp
template<class...> void System::Xml::XmlValidatingReader::ValidationEventHandler_remove(Args... value)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | Args... | یک متد متصل یا ایستا یا نمونه ValidationEventHandler. |

## موارد مرتبط

* کلاس [XmlValidatingReader](../)
* فضای نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)