---
title: LookupNamespace()
second_title: مرجع API Aspose.Slides برای C++
description: پیشوند فضای نام را در محدوده عنصر جاری حل می‌کند.
type: docs
weight: 547
url: /fa/system.xml/xmlvalidatingreader/lookupnamespace/
---
## XmlValidatingReader::LookupNamespace(const String\&) متد

پیشوند فضای نام را در محدوده عنصر جاری حل می‌کند.

```cpp
String System::Xml::XmlValidatingReader::LookupNamespace(const String &prefix) override
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| prefix | const [String](../../../system/string/)\& | پیشوندی که می‌خواهید Uniform Resource Identifier (URI) فضای نام آن را حل کنید. برای تطبیق با فضای نام پیش‌فرض، یک رشته خالی پاس دهید. |

### مقدار بازگشت

URI فضای نامی که پیشوند به آن نگاشت می‌شود یا **nullptr** اگر پیشوند مطابقت‌دار یافت نشود.

## موارد مرتبط

* کلاس [String](../../../system/string/)
* کلاس [XmlValidatingReader](../)
* فضای نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)