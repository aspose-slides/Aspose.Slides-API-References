---
title: set_XmlResolver()
second_title: Aspose.Slides برای C++ مرجع API
description: XmlResolver را برای حل مراجع تعریف نوع سند خارجی (DTD) و مراجع مکان اسکیما تنظیم می‌کند. XmlResolver همچنین برای پردازش هر عنصر import یا include که در اسکیماهای زبان تعریف طرح XML (XSD) یافت می‌شود، استفاده می‌شود.
type: docs
weight: 378
url: /fa/system.xml/xmlvalidatingreader/set_xmlresolver/
---
## XmlValidatingReader::set_XmlResolver(const SharedPtr\<System::Xml::XmlResolver\>\&) متد

مقدار [XmlResolver](../../xmlresolver/) را که برای حل مراجع تعریف نوع سند خارجی (DTD) و مراجع مکان اسکیما استفاده می‌شود، تنظیم می‌کند. همچنین [XmlResolver](../../xmlresolver/) برای پردازش هر عنصر import یا include که در اسکیماهای زبان تعریف XML [Schema](../../../system.xml.schema/) (XSD) یافت می‌شود، به کار می‌رود.

```cpp
void System::Xml::XmlValidatingReader::set_XmlResolver(const SharedPtr<System::Xml::XmlResolver> &value)
```

### آرگومان‌ها

| پارامتر | نوع | شرح |
| --- | --- | --- |
| value | const [SharedPtr](../../../system/sharedptr/)\<[System::Xml::XmlResolver](../../xmlresolver/)\>\& | مقداری که باید تنظیم شود. |

## موارد مرتبط

* تعریف نوع [SharedPtr](../../../system/sharedptr/)
* کلاس [XmlResolver](../../xmlresolver/)
* کلاس [XmlValidatingReader](../)
* فضای نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)