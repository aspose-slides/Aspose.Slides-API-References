---
title: get_CanReadBinaryContent()
second_title: Aspose.Slides برای C++ مرجع API
description: یک مقدار برمی‌گرداند که نشان می‌دهد آیا XmlValidatingReader متدهای خواندن محتوای باینری را پیاده‌سازی می‌کند.
type: docs
weight: 248
url: /fa/system.xml/xmlvalidatingreader/get_canreadbinarycontent/
---
## XmlValidatingReader::get_CanReadBinaryContent() متد

یک مقدار را برمی‌گرداند که نشان می‌دهد آیا [XmlValidatingReader](../) متدهای خواندن محتوای باینری را پیاده‌سازی می‌کند.

```cpp
bool System::Xml::XmlValidatingReader::get_CanReadBinaryContent() override
```

### مقدار بازگشتی

**true** اگر متدهای خواندن محتوای باینری پیاده‌سازی شده باشند؛ در غیر این صورت **false**. کلاس [XmlValidatingReader](../) **true** را برمی‌گرداند.

## مراجع

* کلاس [XmlValidatingReader](../)
* فضای نام [System::Xml](../../)
* کتابخانه [Aspose.Slides](../../../)