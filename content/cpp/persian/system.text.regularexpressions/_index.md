---
title: "System::Text::RegularExpressions"
second_title: مرجع API Aspose.Slides برای C++
description: 
type: docs
weight: 989
url: /fa/system.text.regularexpressions/
---
## کلاس‌ها

| Class | Description |
| --- | --- |
| [Capture](./capture/) | نتیجهٔ مطابقت یک زیرعبارت واحد. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا خواهد شد. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به‌عنوان آرگومان به توابع استفاده کنید. |
| [CaptureCollection](./capturecollection/) | فهرست ضبط‌ها انجام‌شده توسط یک گروه ضبط‌کنندهٔ واحد. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا خواهد شد. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به‌عنوان آرگومان به توابع استفاده کنید. |
| [Group](./group/) | نتیجهٔ تطبیق انجام‌شده توسط یک گروه ضبط‌کنندهٔ واحد. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا خواهد شد. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به‌عنوان آرگومان به توابع استفاده کنید. |
| [GroupCollection](./groupcollection/) | فهرست گروه‌های ضبط در یک مطابقت منفرد. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا خواهد شد. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به‌عنوان آرگومان به توابع استفاده کنید. |
| [GroupCollectionPtr](./groupcollectionptr/) | [Group](./group/) اشاره‌گر مجموعه. این نوع یک اشاره‌گر برای مدیریت حذف شیء دیگر است. باید روی پشته اختصاص داده شود و به توابع یا به‌صورت مقدار یا به‌صورت مرجع ثابت (const) پاس داده شود. |
| [Match](./match/) | [Single](../system/single/) مطابقت عبارات منظم بر روی رشته. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا خواهد شد. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به‌عنوان آرگومان به توابع استفاده کنید. |
| [MatchCollection](./matchcollection/) | مجموعه‌ای از تطبیق‌ها که با اعمال مکرر عبارات منظم بر روی رشته انجام می‌شود. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا خواهد شد. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به‌عنوان آرگومان به توابع استفاده کنید. |
| [Regex](./regex/) | عبارت منظم که از سینتکس شبیه به C# پیروی می‌کند. اشیاء این کلاس باید فقط با استفاده از تابع [System::MakeObject()](../system/makeobject/) اختصاص داده شوند. هرگز نمونه‌ای از این نوع را روی پشته یا با استفاده از عملگر new ایجاد نکنید، زیرا منجر به خطاهای زمان اجرا و/یا خطاهای ادعا خواهد شد. همیشه این کلاس را در یک اشاره‌گر [System::SmartPtr](../system/smartptr/) بپیچید و از این اشاره‌گر برای عبور به‌عنوان آرگومان به توابع استفاده کنید. |

## توابع

| Function | Description |
| --- | --- |
| [ASPOSECPP_3RD_PARTY_UNCOPYBALE_TYPE_HOLDER](./asposecpp_3rd_party_uncopybale_type_holder/)(Detail::MatchHolder, MatchHolder, sizeof(Detail::DummyMatchHolder), Detail::DummyMatchHolder, MatchHolderAlias) | پوشش‌دهنده‌ای برای نگه داشتن کلاس MatchHolder بدون شامل کردن آن و همچنین PCRE2. |

## شمارش‌ها

| Enum | Description |
| --- | --- |
| [RegexOptions](./regexoptions/) | [Regex](./regex/) گزینه‌ها. |

## تعریف‌های‌نوع

| Typedef | Description |
| --- | --- |
| [UStringPtr](./ustringptr/) | UnicodeString مشترک برای جلوگیری از کپی‌کردن. |
| [CapturePtr](./captureptr/) | اشاره‌گر به شیء ضبط واحد. |
| [CaptureCollectionPtr](./capturecollectionptr/) | اشاره‌گر به مجموعهٔ ضبط. |
| [GroupPtr](./groupptr/) | اشاره‌گر به گروه. |
| [RegexPtr](./regexptr/) | [Regex](./regex/) اشاره‌گر. |
| [MatchPtr](./matchptr/) | [Match](./match/) اشاره‌گر. |
| [MatchCollectionPtr](./matchcollectionptr/) | [Match](./match/) اشاره‌گر مجموعه. |
| [MatchEvaluator](./matchevaluator/) | نوع نماینده برای ارزیابی مطابقت. |