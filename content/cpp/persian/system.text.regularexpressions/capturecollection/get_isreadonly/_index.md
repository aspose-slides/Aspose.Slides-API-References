---
title: get_IsReadOnly()
second_title: Aspose.Slides برای C++ مرجع API
description: مجموعه را به‌صورت فقط‌خواندنی علامت‌گذاری می‌کند.
type: docs
weight: 53
url: /fa/system.text.regularexpressions/capturecollection/get_isreadonly/
---
## CaptureCollection::get_IsReadOnly() const متد

مجموعه را به‌صورت فقط‌خواندنی علامت‌گذاری می‌کند.

```cpp
bool System::Text::RegularExpressions::CaptureCollection::get_IsReadOnly() const override
```

### مقدار برگشتی

همواره true را بازمی‌گرداند.

## موارد مرتبط

* کلاس [CaptureCollection](../)
* فضای‌نام [System::Text::RegularExpressions](../../)
* کتابخانه [Aspose.Slides](../../../)