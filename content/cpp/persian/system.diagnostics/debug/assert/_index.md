---
title: Assert()
second_title: Aspose.Slides برای مرجع API C++
description: شرط را بررسی کرده و در صورت شکست اطلاعات را ارسال می‌کند.
type: docs
weight: 14
url: /fa/system.diagnostics/debug/assert/
---
## Debug::Assert(bool) متد

شرط را بررسی کرده و در صورت شکست اطلاعات را ارسال می‌کند.

```cpp
static void System::Diagnostics::Debug::Assert(bool condition)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| condition | **bool** | مقدار شرط. |

## Debug::Assert(bool, const String\&) متد

شرط را بررسی کرده و در صورت شکست اطلاعات را ارسال می‌کند.

```cpp
static void System::Diagnostics::Debug::Assert(bool condition, const String &message)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| condition | **bool** | مقدار شرط. |
| message | const [String](../../../system/string/)\& | پیامی که در صورت شکست ادعا پر می‌شود. |

## Debug::Assert(bool, const char *) متد

شرط را بررسی کرده و در صورت شکست اطلاعات را ارسال می‌کند.

```cpp
static void System::Diagnostics::Debug::Assert(bool condition, const char *message)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| condition | **bool** | مقدار شرط. |
| message | const char * | پیامی که در صورت شکست ادعا پر می‌شود. |

## Debug::Assert(bool, const String\&, const String\&) متد

شرط را بررسی کرده و در صورت شکست اطلاعات را ارسال می‌کند.

```cpp
static void System::Diagnostics::Debug::Assert(bool condition, const String &message, const String &detailMessage)
```

### آرگومان‌ها

| پارامتر | نوع | توضیح |
| --- | --- | --- |
| condition | **bool** | مقدار شرط. |
| message | const [String](../../../system/string/)\& | پیامی که در صورت شکست ادعا پر می‌شود. |
| detailMessage | const [String](../../../system/string/)\& | پیام جزئیاتی که در صورت شکست ادعا پر می‌شود. |

## موارد مرتبط

* کلاس [String](../../../system/string/)
* ساختار [Debug](../)
* فضای‌نام [System::Diagnostics](../../)
* کتابخانه [Aspose.Slides](../../../)