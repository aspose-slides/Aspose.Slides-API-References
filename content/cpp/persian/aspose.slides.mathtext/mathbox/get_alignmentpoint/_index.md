---
title: get_AlignmentPoint()
second_title: Aspose.Slides برای مرجع API C++
description: "زمانی که مقدار true باشد، این شبیه‌ساز عملگر به عنوان نقطه‌ی ترازبندی عمل می‌کند؛ یعنی نقاط ترازبندی تعیین‌شده در معادلات دیگر می‌توانند با آن همراستا شوند. پیش‌فرض: false"
type: docs
weight: 92
url: /fa/aspose.slides.mathtext/mathbox/get_alignmentpoint/
---
## MathBox::get_AlignmentPoint() متد


زمانی که مقدار true باشد، این شبیه‌ساز عملگر به عنوان نقطه‌ی ترازبندی عمل می‌کند؛ یعنی نقاط ترازبندی تعیین‌شده در معادلات دیگر می‌توانند با آن همراستا شوند. پیش‌فرض: false

```cpp
bool Aspose::Slides::MathText::MathBox::get_AlignmentPoint() override
```

## توضیحات


مثال: 
```cpp
auto box = System::MakeObject<MathematicalText>(u"==")->ToBox();
box->set_AlignmentPoint(true);
```

## موارد مرتبط

* کلاس [MathBox](../)
* فضای‌نام [Aspose::Slides::MathText](../../)
* کتابخانه [Aspose.Slides](../../../)