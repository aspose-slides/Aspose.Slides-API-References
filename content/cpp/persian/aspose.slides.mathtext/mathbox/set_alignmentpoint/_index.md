---
title: set_AlignmentPoint()
second_title: "Aspose.Slides برای C++ مرجع API"
description: "زمانی که true باشد، این شبیه‌ساز اپراتور به عنوان نقطه‌ی تراز عمل می‌کند؛ به این معنی که نقاط تراز مشخص‌شده در معادلات دیگر می‌توانند با آن تراز شوند. پیش‌فرض: false"
type: docs
weight: 105
url: /fa/aspose.slides.mathtext/mathbox/set_alignmentpoint/
---
## MathBox::set_AlignmentPoint(bool) متد

زمانی که true باشد، این شبیه‌ساز اپراتور به عنوان نقطه‌ی تراز عمل می‌کند؛ یعنی نقاط تراز تعیین‌شده در معادلات دیگر می‌توانند با آن تراز شوند. پیش‌فرض: false

```cpp
void Aspose::Slides::MathText::MathBox::set_AlignmentPoint(bool value) override
```

## توضیحات

مثال:
```cpp
auto box = System::MakeObject<MathematicalText>(u"==")->ToBox();
box->set_AlignmentPoint(true);
```

## موارد مرتبط

* کلاس [MathBox](../)
* فضای‌نام [Aspose::Slides::MathText](../../)
* کتابخانه [Aspose.Slides](../../../)