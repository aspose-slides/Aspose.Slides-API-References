---
title: set_MaximumDistribution()
second_title: مستندات API Aspose.Slides برای C++
description: توزیع حداکثری وقتی مقدار true باشد، آرایه به حداکثر عرض عنصر حاوی (صفحه، ستون، سلول و غیره) تنظیم می‌شود.
type: docs
weight: 53
url: /fa/aspose.slides.mathtext/imatharray/set_maximumdistribution/
---
## IMathArray::set_MaximumDistribution(bool) متد

توزیع حداکثری وقتی مقدار true باشد، آرایه به حداکثر عرض عنصر حاوی (صفحه، ستون، سلول و غیره) تنظیم می‌شود.

```cpp
virtual void Aspose::Slides::MathText::IMathArray::set_MaximumDistribution(bool value)=0
```

## توضیحات

مثال:
```cpp
auto mathArray = System::MakeObject<MathArray>(System::MakeObject<MathematicalText>(u"item1"));
mathArray->set_MaximumDistribution(true);
```

## موارد مرتبط

* کلاس [IMathArray](../)
* فضای نام [Aspose::Slides::MathText](../../)
* کتابخانه [Aspose.Slides](../../../)