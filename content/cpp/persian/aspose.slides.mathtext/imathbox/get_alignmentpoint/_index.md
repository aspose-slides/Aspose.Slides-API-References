---
title: get_AlignmentPoint()
second_title: Aspose.Slides برای C++ مرجع API
description: "هنگام true، این شبیه‌ساز عملگر به‌عنوان نقطه تنظیم عمل می‌کند؛ یعنی نقاط تنظیم تعیین‌شده در معادلات دیگر می‌توانند با آن هم‌راستا شوند. پیش‌فرض: false"
type: docs
weight: 92
url: /fa/aspose.slides.mathtext/imathbox/get_alignmentpoint/
---
## IMathBox::get_AlignmentPoint() متد

هنگام true، این شبیه‌ساز عملگر به‌عنوان نقطه تنظیم استفاده می‌شود؛ یعنی نقاط تنظیم تعیین‌شده در معادلات دیگر می‌توانند با آن هم‌راستا شوند. پیش‌فرض: false

```cpp
virtual bool Aspose::Slides::MathText::IMathBox::get_AlignmentPoint()=0
```

## ملاحظات

مثال:
```cpp
auto box = System::MakeObject<MathematicalText>(u"==")->ToBox();
box->set_AlignmentPoint(true);
```

## موارد مرتبط

* کلاس [IMathBox](../)
* فضای‌نام [Aspose::Slides::MathText](../../)
* کتابخانه [Aspose.Slides](../../../)