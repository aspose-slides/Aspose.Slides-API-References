---
title: AddArc()
second_title: Riferimento API di Aspose.Slides per C++
description: Aggiunge l'arco ellittico specificato al percorso rappresentato dall'oggetto corrente.
type: docs
weight: 183
url: /it/system.drawing.drawing2d/graphicspath/addarc/
---
## GraphicsPath::AddArc(float, float, float, float, float, float) metodo


Aggiunge l'arco ellittico specificato al percorso rappresentato dall'oggetto corrente.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddArc(float x, float y, float width, float height, float startAngle, float sweepAngle)
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| x | **float** | La coordinata X dell'angolo superiore sinistro del rettangolo che delimita l'ellisse da cui è tratto l'arco |
| y | **float** | La coordinata Y dell'angolo superiore sinistro del rettangolo che delimita l'ellisse da cui è tratto l'arco |
| width | **float** | La larghezza del rettangolo che delimita l'ellisse da cui è tratto l'arco |
| height | **float** | L'altezza del rettangolo che delimita l'ellisse da cui è tratto l'arco |
| startAngle | **float** | Specifica l'angolo iniziale dell'arco in gradi, misurato in senso orario dall'asse X |
| sweepAngle | **float** | Specifica l'angolo compreso tra l'angolo iniziale e la fine dell'arco |

## GraphicsPath::AddArc(int, int, int, int, float, float) metodo


Aggiunge l'arco ellittico specificato al percorso rappresentato dall'oggetto corrente.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddArc(int x, int y, int width, int height, float startAngle, float sweepAngle)
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| x | int | La coordinata X dell'angolo superiore sinistro del rettangolo che delimita l'ellisse da cui è tratto l'arco |
| y | int | La coordinata Y dell'angolo superiore sinistro del rettangolo che delimita l'ellisse da cui è tratto l'arco |
| width | int | La larghezza del rettangolo che delimita l'ellisse da cui è tratto l'arco |
| height | int | L'altezza del rettangolo che delimita l'ellisse da cui è tratto l'arco |
| startAngle | **float** | Specifica l'angolo iniziale dell'arco in gradi, misurato in senso orario dall'asse X |
| sweepAngle | **float** | Specifica l'angolo compreso tra l'angolo iniziale e la fine dell'arco |

## GraphicsPath::AddArc(const RectangleF\&, float, float) metodo


Aggiunge l'arco ellittico specificato al percorso rappresentato dall'oggetto corrente.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddArc(const RectangleF &rect, float startAngle, float sweepAngle)
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| rect | const [RectangleF](../../../system.drawing/rectanglef/)\& | Il rettangolo che delimita l'ellisse da cui è tratto l'arco |
| startAngle | **float** | Specifica l'angolo iniziale dell'arco in gradi, misurato in senso orario dall'asse X |
| sweepAngle | **float** | Specifica l'angolo compreso tra l'angolo iniziale e la fine dell'arco |

## GraphicsPath::AddArc(const Rectangle\&, float, float) metodo


Aggiunge l'arco ellittico specificato al percorso rappresentato dall'oggetto corrente.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddArc(const Rectangle &rect, float startAngle, float sweepAngle)
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| rect | const [Rectangle](../../../system.drawing/rectangle/)\& | Il rettangolo che delimita l'ellisse da cui è tratto l'arco |
| startAngle | **float** | Specifica l'angolo iniziale dell'arco in gradi, misurato in senso orario dall'asse X |
| sweepAngle | **float** | Specifica l'angolo compreso tra l'angolo iniziale e la fine dell'arco |

## Vedi anche

* Classe [GraphicsPath](../)
* Classe [RectangleF](../../../system.drawing/rectanglef/)
* Classe [Rectangle](../../../system.drawing/rectangle/)
* Spazio dei nomi [System::Drawing::Drawing2D](../../)
* Library [Aspose.Slides](../../../)