---
title: operator[]()
second_title: Aspose.Slides per C++ Riferimento API
description: Operatore di accesso per lavorare con la conversione del tipo di chiave.
type: docs
weight: 14
url: /it/system.collections.generic/dictionaryptr/operator[]/
---
## DictionaryPtr::operator[](const X\&) const metodo

Operatore di accesso per lavorare con la conversione del tipo di chiave.

```cpp
template<class X> V & System::Collections::Generic::DictionaryPtr<T, V>::operator[](const X &key) const
```

### Parametri del modello

| Parametro | Descrizione |
| --- | --- |
| X | Tipo di chiave di origine. |

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| key | const X\& | [Dictionary](../../dictionary/) chiave. |

### Valore restituito

Riferimento al valore corrispondente alla chiave fornita, esistente o appena creato.

## DictionaryPtr::operator[](const T\&) const metodo

Operatore di accesso.

```cpp
V & System::Collections::Generic::DictionaryPtr<T, V>::operator[](const T &key) const
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| key | const T\& | [Dictionary](../../dictionary/) chiave. |

### Valore restituito

Riferimento al valore corrispondente alla chiave fornita, esistente o appena creato.

## Vedi anche

* Classe [DictionaryPtr](../)
* Spazio dei nomi [System::Collections::Generic](../../)
* Libreria [Aspose.Slides](../../../)