---
title: FromArgb()
second_title: Riferimento API di Aspose.Slides per C++
description: Costruisce un'istanza della classe Color che rappresenta il colore specificato.
type: docs
weight: 235
url: /it/system.drawing/color/fromargb/
---
## Color::FromArgb(int) metodo


Costruisce un'istanza della classe [Color](../) che rappresenta il colore specificato.

```cpp
static Color System::Drawing::Color::FromArgb(int argb)
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| argb | int | Un valore ARGB a 32 bit del colore da rappresentare dall'oggetto in fase di costruzione |

### Valore di ritorno

Un oggetto che rappresenta il colore specificato.

## Color::FromArgb(int, int, int, int) metodo


Costruisce un'istanza della classe [Color](../) che rappresenta il colore specificato.

```cpp
static Color System::Drawing::Color::FromArgb(int alpha, int red, int green, int blue)
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| alpha | int | Un valore del componente alfa del colore |
| red | int | Un valore del componente rosso del colore |
| green | int | Un valore del componente verde del colore |
| blue | int | Un valore del componente blu del colore |

### Valore di ritorno

Un oggetto che rappresenta il colore specificato.

## Color::FromArgb(int, int, int) metodo


Costruisce un'istanza della classe [Color](../) che rappresenta il colore con il componente alfa impostato a 0xFF.

```cpp
static Color System::Drawing::Color::FromArgb(int red, int green, int blue)
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| red | int | Un valore del componente rosso del colore |
| green | int | Un valore del componente verde del colore |
| blue | int | Un valore del componente blu del colore |

### Valore di ritorno

Un oggetto che rappresenta il colore specificato.

## Color::FromArgb(int, Color) metodo


Costruisce un'istanza della classe [Color](../) che rappresenta il colore specificato.

```cpp
static Color System::Drawing::Color::FromArgb(int alpha, Color base_color)
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| alpha | int | Un valore del componente alfa del colore |
| base_color | [Color](../) | Un'istanza dell'oggetto [Color](../) che rappresenta i componenti rosso, verde e blu del colore da rappresentare dall'oggetto creato |

### Valore di ritorno

Un oggetto che rappresenta il colore specificato.

## Vedi anche

* Classe [Color](../)
* Spazio dei nomi [System::Drawing](../../)
* Libreria [Aspose.Slides](../../../)