---
title: Equals()
second_title: Riferimento API di Aspose.Slides per C++
description: Determina se i valori rappresentati dall'oggetto corrente e dall'oggetto specificato sono uguali.
type: docs
weight: 313
url: /it/system/decimal/equals/
---
## Decimal::Equals(const Decimal\&) const metodo

Determina se i valori rappresentati dall'oggetto corrente e dall'oggetto specificato sono uguali.

```cpp
bool System::Decimal::Equals(const Decimal &d) const
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| d | const [Decimal](../)\& | L'oggetto [Decimal](../) da confrontare con l'oggetto corrente |

### Valore di ritorno

True se il valore rappresentato dall'oggetto corrente e dall'oggetto specificato è uguale, altrimenti - false

## Decimal::Equals(const SharedPtr\<Object\>\&) const metodo

Determina se i valori rappresentati dall'oggetto corrente e dall'oggetto specificato sono uguali.

```cpp
bool System::Decimal::Equals(const SharedPtr<Object> &obj) const
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | L'oggetto da confrontare con l'oggetto corrente |

### Valore di ritorno

True se il valore rappresentato dall'oggetto corrente e dall'oggetto specificato è uguale, altrimenti - false

## Decimal::Equals(const Decimal\&, const Decimal\&) metodo

Determina se i valori rappresentati dagli oggetti specificati sono uguali.

```cpp
static bool System::Decimal::Equals(const Decimal &d1, const Decimal &d2)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| d1 | const [Decimal](../)\& | Il primo comparando |
| d2 | const [Decimal](../)\& | Il secondo comparando |

### Valore di ritorno

True se i valori rappresentati da **d1** e **d2** sono uguali, altrimenti - false

## Vedi anche

* Typedef [SharedPtr](../../sharedptr/)
* Classe [Decimal](../)
* Classe [Object](../../object/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)