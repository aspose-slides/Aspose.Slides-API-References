---
title: WriteLine()
second_title: Riferimento API di Aspose.Slides per C++
description: Scrive il terminatore di riga corrente nello stream di output rappresentato dall'oggetto corrente.
type: docs
weight: 27
url: /it/system/consoleoutput/writeline/
---
## ConsoleOutput::WriteLine() metodo

Scrive il terminatore di riga corrente nello stream di output rappresentato dall'oggetto corrente.

```cpp
void System::ConsoleOutput::WriteLine() override
```

## ConsoleOutput::WriteLine(const SharedPtr\<Object\>\&) metodo

Scrive la rappresentazione testuale dell'oggetto specificato seguita dal terminatore di riga corrente nello stream di output rappresentato dall'oggetto corrente.

```cpp
void System::ConsoleOutput::WriteLine(const SharedPtr<Object> &value) override
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | L'oggetto da scrivere |

## ConsoleOutput::WriteLine(bool) metodo

Scrive la rappresentazione testuale del valore booleano specificato seguita dal terminatore di riga corrente nello stream di output rappresentato dall'oggetto corrente.

```cpp
void System::ConsoleOutput::WriteLine(bool value) override
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | **bool** | Il valore da scrivere |

## ConsoleOutput::WriteLine(char_t) metodo

Scrive il valore del carattere specificato seguito dal terminatore di riga corrente nello stream di output rappresentato dall'oggetto corrente.

```cpp
void System::ConsoleOutput::WriteLine(char_t value) override
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | char_t | Il valore da scrivere |

## ConsoleOutput::WriteLine(Decimal) metodo

Scrive la rappresentazione testuale del valore [Decimal](../../decimal/) seguito dal terminatore di riga corrente nello stream di output rappresentato dall'oggetto corrente.

```cpp
void System::ConsoleOutput::WriteLine(Decimal value) override
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | Il valore da scrivere |

## ConsoleOutput::WriteLine(double) metodo

Scrive la rappresentazione testuale del valore in virgola mobile a doppia precisione seguito dal terminatore di riga corrente nello stream di output rappresentato dall'oggetto corrente.

```cpp
void System::ConsoleOutput::WriteLine(double value) override
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | **double** | Il valore da scrivere |

## ConsoleOutput::WriteLine(int) metodo

Scrive la rappresentazione testuale del valore intero a 32 bit seguito dal terminatore di riga corrente nello stream di output rappresentato dall'oggetto corrente.

```cpp
void System::ConsoleOutput::WriteLine(int value) override
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | int | Il valore da scrivere |

## ConsoleOutput::WriteLine(int64_t) metodo

Scrive la rappresentazione testuale del valore intero a 64 bit seguito dal terminatore di riga corrente nello stream di output rappresentato dall'oggetto corrente.

```cpp
void System::ConsoleOutput::WriteLine(int64_t value) override
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | **int64_t** | Il valore da scrivere |

## ConsoleOutput::WriteLine(float) metodo

Scrive la rappresentazione testuale del valore in virgola mobile a precisione singola seguito dal terminatore di riga corrente nello stream di output rappresentato dall'oggetto corrente.

```cpp
void System::ConsoleOutput::WriteLine(float value) override
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | **float** | Il valore da scrivere |

## ConsoleOutput::WriteLine(const String\&) metodo

Scrive l'oggetto stringa specificato seguito dal terminatore di riga corrente nello stream di output rappresentato dall'oggetto corrente.

```cpp
void System::ConsoleOutput::WriteLine(const String &value) override
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | const [String](../../string/)\& | L'oggetto stringa da scrivere |

## ConsoleOutput::WriteLine(uint32_t) metodo

Scrive la rappresentazione testuale del valore intero senza segno a 32 bit seguito dal terminatore di riga corrente nello stream di output rappresentato dall'oggetto corrente.

```cpp
void System::ConsoleOutput::WriteLine(uint32_t value) override
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | **uint32_t** | Il valore da scrivere |

## ConsoleOutput::WriteLine(uint64_t) metodo

Scrive la rappresentazione testuale del valore intero senza segno a 64 bit seguito dal terminatore di riga corrente nello stream di output rappresentato dall'oggetto corrente.

```cpp
void System::ConsoleOutput::WriteLine(uint64_t value) override
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | **uint64_t** | Il valore da scrivere |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&) metodo

Scrive la rappresentazione testuale dell'array di caratteri specificato seguito dal terminatore di riga corrente nello stream di output rappresentato dall'oggetto corrente.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer) override
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | L'array da scrivere |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&, int32_t, int32_t) metodo

Scrive la rappresentazione testuale di un intervallo di valori dell'array di caratteri specificato, seguito dal terminatore di riga corrente, nello stream di output rappresentato dall'oggetto corrente.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | L'array contenente i valori da scrivere |
| index | **int32_t** | L'indice di inizio dell'intervallo di elementi da scrivere |
| count | **int32_t** | Il numero di elementi nell'intervallo da scrivere |

## ConsoleOutput::WriteLine(const char_t *) metodo

Scrive la stringa C specificata seguita dal terminatore di riga corrente nello stream di output rappresentato dall'oggetto corrente.

```cpp
void System::ConsoleOutput::WriteLine(const char_t *value) override
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | const char_t * | La stringa C da scrivere |

## ConsoleOutput::WriteLine(const TypeInfo\&) metodo

Scrive la rappresentazione testuale dell'oggetto [TypeInfo](../../typeinfo/) specificato seguito dal terminatore di riga corrente nello stream di output rappresentato dall'oggetto corrente.

```cpp
void System::ConsoleOutput::WriteLine(const TypeInfo &value) override
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | L'oggetto [TypeInfo](../../typeinfo/) da scrivere |

## ConsoleOutput::WriteLine(const char *) metodo




```cpp
void System::ConsoleOutput::WriteLine(const char *)=delete
```

## Vedi anche

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Classe [ConsoleOutput](../)
* Classe [Object](../../object/)
* Classe [Decimal](../../decimal/)
* Classe [String](../../string/)
* Classe [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)