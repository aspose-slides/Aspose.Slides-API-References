---
title: EqualsExact()
second_title: Riferimento API di Aspose.Slides per C++
description: Verifica se due oggetti DateTimeOffset rappresentano lo stesso punto temporale e hanno lo stesso offset.
type: docs
weight: 378
url: /it/system/datetimeoffset/equalsexact/
---
## DateTimeOffset::EqualsExact(const DateTimeOffset\&) const metodo

Verifica se due oggetti [DateTimeOffset](../) rappresentano lo stesso punto temporale e hanno lo stesso offset.

```cpp
bool System::DateTimeOffset::EqualsExact(const DateTimeOffset &other) const
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/) da confrontare. |

### Valore di ritorno

true se l'oggetto corrente è esattamente uguale a **other**, altrimenti - false.

## DateTimeOffset::EqualsExact(const SharedPtr\<Object\>\&) const metodo

Verifica se due oggetti [DateTimeOffset](../) rappresentano lo stesso punto temporale e hanno lo stesso offset.

```cpp
bool System::DateTimeOffset::EqualsExact(const SharedPtr<Object> &obj) const
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/) da confrontare. |

### Valore di ritorno

true se l'oggetto corrente è esattamente uguale a **obj**, altrimenti - false.

## Vedi anche

* Typedef [SharedPtr](../../sharedptr/)
* Classe [DateTimeOffset](../)
* Classe [Object](../../object/)
* Spazio dei nomi [System](../../)
* Libreria [Aspose.Slides](../../../)