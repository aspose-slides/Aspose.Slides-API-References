---
title: Equals()
second_title: Riferimento API di Aspose.Slides per C++
description: Verifica se due oggetti DateTimeOffset rappresentano lo stesso punto temporale.
type: docs
weight: 365
url: /it/system/datetimeoffset/equals/
---
## DateTimeOffset::Equals(const DateTimeOffset\&) const metodo


Verifica se due oggetti [DateTimeOffset](../) rappresentano lo stesso punto temporale.

```cpp
bool System::DateTimeOffset::Equals(const DateTimeOffset &other) const
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/) da confrontare. |

### Valore di ritorno

true se l'oggetto corrente è uguale a **other**, altrimenti - false.

## DateTimeOffset::Equals(const SharedPtr\<Object\>\&) const metodo


Verifica se due oggetti [DateTimeOffset](../) rappresentano lo stesso punto temporale.

```cpp
bool System::DateTimeOffset::Equals(const SharedPtr<Object> &obj) const
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/) da confrontare. |

### Valore di ritorno

true se l'oggetto corrente è uguale a **obj**, altrimenti - false.

## DateTimeOffset::Equals(const DateTimeOffset\&, const DateTimeOffset\&) metodo


Verifica se due oggetti [DateTimeOffset](../) rappresentano lo stesso punto temporale.

```cpp
static bool System::DateTimeOffset::Equals(const DateTimeOffset &first, const DateTimeOffset &second)
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| first | const [DateTimeOffset](../)\& | Primo oggetto da confrontare. |
| second | const [DateTimeOffset](../)\& | Secondo oggetto da confrontare. |

### Valore di ritorno

true se **first** è uguale a **second**, altrimenti - false.

## Vedi anche

* Typedef [SharedPtr](../../sharedptr/)
* Classe [DateTimeOffset](../)
* Classe [Object](../../object/)
* Spazio dei nomi [System](../../)
* Libreria [Aspose.Slides](../../../)