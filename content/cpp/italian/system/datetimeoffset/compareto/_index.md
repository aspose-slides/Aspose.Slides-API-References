---
title: CompareTo()
second_title: Riferimento API di Aspose.Slides per C++
description: Confronta due oggetti DateTimeOffset.
type: docs
weight: 352
url: /it/system/datetimeoffset/compareto/
---
## DateTimeOffset::CompareTo(const DateTimeOffset\&) const metodo

Confronta due oggetti [DateTimeOffset](../).

```cpp
int System::DateTimeOffset::CompareTo(const DateTimeOffset &other) const
```

### Argomenti

| Parameter | Type | Description |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/) da confrontare. |

### Valore di ritorno

- 1 - se l'oggetto corrente è precedente a **other**, 1 - se l'oggetto corrente è successivo a **other**, 0 - se l'oggetto corrente è uguale a **other**.

## DateTimeOffset::CompareTo(const SharedPtr\<Object\>\&) const metodo

Confronta due oggetti [DateTimeOffset](../).

```cpp
int System::DateTimeOffset::CompareTo(const SharedPtr<Object> &obj) const
```

### Argomenti

| Parameter | Type | Description |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/) da confrontare. |

### Valore di ritorno

- 1 - se l'oggetto corrente è precedente a **obj**, 1 - se l'oggetto corrente è successivo a **obj**, 0 - se l'oggetto corrente è uguale a **obj**.

## Vedi anche

* Typedef [SharedPtr](../../sharedptr/)
* Classe [DateTimeOffset](../)
* Classe [Object](../../object/)
* Spazio dei nomi [System](../../)
* Library [Aspose.Slides](../../../)