---
title: GetBytes()
second_title: Riferimento API di Aspose.Slides per C++
description: Converte il valore booleano specificato in un array di byte.
type: docs
weight: 14
url: /it/system/bitconverter/getbytes/
---
## BitConverter::GetBytes(bool) metodo


Converte il valore booleano specificato in un array di byte.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(bool value)
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | **bool** | Un valore booleano da convertire |

### Valore di ritorno

Array di 1 byte che rappresenta il valore specificato

## BitConverter::GetBytes(char_t) metodo


Converte il valore char_t specificato in un array di byte.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(char_t value)
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | char_t | Un valore char_t da convertire |

### Valore di ritorno

Array di 2 byte che rappresenta il valore specificato

## BitConverter::GetBytes(int16_t) metodo


Converte il valore intero a 16 bit specificato in un array di byte.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int16_t value)
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | **int16_t** | Un valore intero a 16 bit da convertire |

### Valore di ritorno

Array di 2 byte che rappresenta il valore specificato

## BitConverter::GetBytes(int) metodo


Converte il valore intero a 32 bit specificato in un array di byte.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int value)
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | int | Un valore intero a 32 bit da convertire |

### Valore di ritorno

Array di 4 byte che rappresenta il valore specificato

## BitConverter::GetBytes(int64_t) metodo


Converte il valore intero a 64 bit specificato in un array di byte.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int64_t value)
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | **int64_t** | Un valore intero a 64 bit da convertire |

### Valore di ritorno

Array di 8 byte che rappresenta il valore specificato

## BitConverter::GetBytes(uint16_t) metodo


Converte il valore intero senza segno a 16 bit specificato in un array di byte.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint16_t value)
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | **uint16_t** | Un valore intero senza segno a 16 bit da convertire |

### Valore di ritorno

Array di 2 byte che rappresenta il valore specificato

## BitConverter::GetBytes(uint32_t) metodo


Converte il valore intero senza segno a 32 bit specificato in un array di byte.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint32_t value)
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | **uint32_t** | Un valore intero senza segno a 32 bit da convertire |

### Valore di ritorno

Array di 4 byte che rappresenta il valore specificato

## BitConverter::GetBytes(uint64_t) metodo


Converte il valore intero senza segno a 64 bit specificato in un array di byte.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint64_t value)
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | **uint64_t** | Un valore intero senza segno a 64 bit da convertire |

### Valore di ritorno

Array di 8 byte che rappresenta il valore specificato

## BitConverter::GetBytes(float) metodo


Converte il valore a precisione singola specificato in un array di byte.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(float value)
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | **float** | Un valore a virgola mobile da convertire |

### Valore di ritorno

Array di 4 byte che rappresenta il valore specificato

## BitConverter::GetBytes(double) metodo


Converte il valore a doppia precisione specificato in un array di byte.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(double value)
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | **double** | Un valore a virgola mobile da convertire |

### Valore di ritorno

Array di 8 byte che rappresenta il valore specificato

## Vedi anche

* Typedef [ArrayPtr](../../arrayptr/)
* Classe [BitConverter](../)
* Spazio dei nomi [System](../../)
* Library [Aspose.Slides](../../../)