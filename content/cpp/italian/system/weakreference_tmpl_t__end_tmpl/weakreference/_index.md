---
title: WeakReference()
second_title: Riferimento API di Aspose.Slides per C++
description: Costruttore predefinito.
type: docs
weight: 1
url: /it/system/weakreference_tmpl_t__end_tmpl/weakreference/
---
## WeakReference< T >::WeakReference() metodo

Costruttore predefinito.

```cpp
System::WeakReference<T>::WeakReference()
```

## WeakReference< T >::WeakReference(std::nullptr_t) metodo

Costruttore da nullptr.

```cpp
System::WeakReference<T>::WeakReference(std::nullptr_t)
```

## WeakReference< T >::WeakReference(const SmartPtr\<T\>\&) metodo

Inizializza una nuova istanza della classe WeakReference, facendo riferimento all'oggetto specificato.

```cpp
System::WeakReference<T>::WeakReference(const SmartPtr<T> &data)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) da memorizzare. |

## WeakReference< T >::WeakReference(const SmartPtr\<T\>\&, bool) metodo

Inizializza una nuova istanza della classe WeakReference, facendo riferimento all'oggetto specificato.

```cpp
System::WeakReference<T>::WeakReference(const SmartPtr<T> &data, bool trackResurrection)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) da memorizzare. |
| trackResurrection | **bool** | Ignorato. |

## Vedi anche

* Classe [WeakReference< T >](../)
* Classe [SmartPtr](../../smartptr/)
* Namespace [System](../../)
* Libreria [Aspose.Slides](../../../)