---
title: WriteLine()
second_title: Riferimento API di Aspose.Slides per C++
description: Emette il terminatore di riga corrente sullo stream di output standard.
type: docs
weight: 14
url: /it/system/console/writeline/
---
## Console::WriteLine() metodo

Emette il terminatore di riga corrente sullo stream di output standard.

```cpp
static void System::Console::WriteLine()
```

## Console::WriteLine(const SharedPtr\<T\>\&) metodo

Emette la rappresentazione testuale dell'oggetto specificato seguita dal terminatore di riga corrente sullo stream di output standard.

```cpp
template<class T> static void System::Console::WriteLine(const SharedPtr<T> &object)
```

### Parametri del modello

| Parametro | Descrizione |
| --- | --- |
| T | Tipo dell'oggetto da emettere |

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| object | const [SharedPtr](../../sharedptr/)\<T\>\& | [Object](../../object/) da emettere |

## Console::WriteLine(bool) metodo

Emette la rappresentazione testuale del valore bool seguita dal terminatore di riga corrente sullo stream di output standard.

```cpp
static void System::Console::WriteLine(bool value)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | **bool** | Il valore da emettere |

## Console::WriteLine(char_t) metodo

Emette il valore di carattere specificato seguita dal terminatore di riga corrente sullo stream di output standard.

```cpp
static void System::Console::WriteLine(char_t value)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | char_t | Il valore da emettere |

## Console::WriteLine(const ArrayPtr\<char_t\>\&) metodo

Emette la rappresentazione testuale dell'array di caratteri specificato seguita dal terminatore di riga corrente sullo stream di output standard.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | L'array da emettere |

## Console::WriteLine(const Decimal\&) metodo

Emette la rappresentazione testuale del valore [Decimal](../../decimal/) seguita dal terminatore di riga corrente sullo stream di output standard.

```cpp
static void System::Console::WriteLine(const Decimal &value)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | Il valore da emettere |

## Console::WriteLine(double) metodo

Emette la rappresentazione testuale del valore in virgola mobile a doppia precisione seguita dal terminatore di riga corrente sullo stream di output standard.

```cpp
static void System::Console::WriteLine(double value)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | **double** | Il valore da emettere |

## Console::WriteLine(float) metodo

Emette la rappresentazione testuale del valore in virgola mobile a precisione singola seguita dal terminatore di riga corrente sullo stream di output standard.

```cpp
static void System::Console::WriteLine(float value)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | **float** | Il valore da emettere |

## Console::WriteLine(int32_t) metodo

Emette la rappresentazione testuale del valore intero a 32 bit seguita dal terminatore di riga corrente sullo stream di output standard.

```cpp
static void System::Console::WriteLine(int32_t value)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | **int32_t** | Il valore da emettere |

## Console::WriteLine(int64_t) metodo

Emette la rappresentazione testuale del valore intero a 64 bit seguita dal terminatore di riga corrente sullo stream di output standard.

```cpp
static void System::Console::WriteLine(int64_t value)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | **int64_t** | Il valore da emettere |

## Console::WriteLine(const String\&) metodo

Emette l'oggetto stringa specificato seguita dal terminatore di riga corrente sullo stream di output standard.

```cpp
static void System::Console::WriteLine(const String &value)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | const [String](../../string/)\& | L'oggetto stringa da emettere |

## Console::WriteLine(const char_t *) metodo

Emette la c-stringa specificata seguita dal terminatore di riga corrente sullo stream di output standard.

```cpp
static void System::Console::WriteLine(const char_t *value)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | const char_t * | La c-stringa da emettere |

## Console::WriteLine(const TypeInfo\&) metodo

Emette la rappresentazione testuale del valore [TypeInfo](../../typeinfo/) seguita dal terminatore di riga corrente sullo stream di output standard.

```cpp
static void System::Console::WriteLine(const TypeInfo &value)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | Il valore da emettere |

## Console::WriteLine(uint32_t) metodo

Emette la rappresentazione testuale del valore intero senza segno a 32 bit seguita dal terminatore di riga corrente sullo stream di output standard.

```cpp
static void System::Console::WriteLine(uint32_t value)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | **uint32_t** | Il valore da emettere |

## Console::WriteLine(uint64_t) metodo

Emette la rappresentazione testuale del valore intero senza segno a 64 bit seguita dal terminatore di riga corrente sullo stream di output standard.

```cpp
static void System::Console::WriteLine(uint64_t value)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | **uint64_t** | Il valore da emettere |

## Console::WriteLine(const ArrayPtr\<char_t\>\&, int, int) metodo

Emette la rappresentazione testuale dell'intervallo specificato dell'array di caratteri specificato seguita dal terminatore di riga corrente sullo stream di output standard.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer, int index, int count)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | L'array di caratteri |
| index | int | L'indice nell'array a cui inizia l'intervallo da emettere |
| count | int | Il numero di elementi nell'intervallo da emettere |

## Console::WriteLine(const Exception\&) metodo

Emette la rappresentazione testuale dell'oggetto Exception specificato seguita dal terminatore di riga corrente sullo stream di output standard.

```cpp
static void System::Console::WriteLine(const Exception &e)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| e | const [Exception](../../exception/)\& | Il valore da emettere |

## Console::WriteLine(const String\&, Args\&&...) metodo

Emette la rappresentazione testuale degli argomenti specificati formattati secondo il formato specificato seguita dal terminatore di riga corrente sullo stream di output standard.

```cpp
template<class...> static void System::Console::WriteLine(const String &format, Args &&... args)
```

### Parametri del modello

| Parametro | Descrizione |
| --- | --- |
| I | i tipi dei valori da emettere |

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| format | const [String](../../string/)\& | Il formato stringa |
| args | Args\&&... | I valori da emettere |

## Console::WriteLine(const char *) metodo




```cpp
static void System::Console::WriteLine(const char *)=delete
```

## Vedi anche

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Typedef [Exception](../../exception/)
* Classe [Console](../)
* Classe [Decimal](../../decimal/)
* Classe [String](../../string/)
* Classe [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)