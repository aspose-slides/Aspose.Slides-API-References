---
title: IsAmbiguousTime()
second_title: Riferimento API di Aspose.Slides per C++
description: Verifica se la data e l'ora specificate sono ambigue e possono essere associate a molte ore UTC.
type: docs
weight: 313
url: /it/system/timezoneinfo/isambiguoustime/
---
## TimeZoneInfo::IsAmbiguousTime(DateTime) const method


Verifica se la data e l'ora specificate sono ambigue e possono essere associate a molte ore UTC.

```cpp
bool System::TimeZoneInfo::IsAmbiguousTime(DateTime date_time) const
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| date_time | [DateTime](../../datetime/) | Data e ora. |

### Valore di ritorno

True se date_time è ambiguo.

## TimeZoneInfo::IsAmbiguousTime(const DateTimeOffset\&) const method


Verifica se la data e l'ora specificate sono ambigue e possono essere associate a molte ore UTC.

```cpp
bool System::TimeZoneInfo::IsAmbiguousTime(const DateTimeOffset &date_time_offset) const
```


### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| date_time_offset | const [DateTimeOffset](../../datetimeoffset/)\& | Data e ora. |

### Valore di ritorno

True se date_time è ambiguo.

## Vedi anche

* Classe [DateTime](../../datetime/)
* Classe [TimeZoneInfo](../)
* Classe [DateTimeOffset](../../datetimeoffset/)
* Namespace [System](../../)
* Libreria [Aspose.Slides](../../../)