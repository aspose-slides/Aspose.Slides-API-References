---
title: "System::Security::Cryptography::Xml"
second_title: Riferimento API di Aspose.Slides per C++
description: 
type: docs
weight: 872
url: /it/system.security.cryptography.xml/
---
## Classi

| Classe | Descrizione |
| --- | --- |
| [DataObject](./dataobject/) | Definisce un meccanismo indipendente dal formato per il trasferimento dei dati. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o errori di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [DSAKeyValue](./dsakeyvalue/) | Rappresenta la chiave privata [DSA](../system.security.cryptography/dsa/). Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o errori di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [KeyInfo](./keyinfo/) | Rappresenta l'elemento [KeyInfo](./keyinfo/) di una firma digitale XML o di una cifratura XML. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o errori di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [KeyInfoClause](./keyinfoclause/) | Tutte le implementazioni dei sottoggetti [KeyInfo](./keyinfo/) devono ereditare questa classe astratta. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o errori di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [KeyInfoName](./keyinfoname/) | Rappresenta il sottoggetto 'KeyName' di XMLDSIG o l'elemento '[KeyInfo](./keyinfo/)' della cifratura XML. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o errori di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [KeyInfoX509Data](./keyinfox509data/) | Rappresenta un elemento 'X509Data'. Contiene le informazioni del certificato X.509v3 relative alla chiave di validazione o di cifratura. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o errori di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [Reference](./reference/) | Facilita la creazione delle firme XML. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o errori di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [RSAKeyValue](./rsakeyvalue/) | Rappresenta l'elemento '[RSAKeyValue](./rsakeyvalue/)' della firma XML. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o errori di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [SignedInfo](./signedinfo/) | Rappresenta l'elemento '[SignedInfo](./signedinfo/)' della firma XML. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o errori di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [SignedXml](./signedxml/) | Utilizzato per la firma e la verifica XML. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o errori di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [Transform](./transform/) | Fornisce informazioni sulla trasformazione dei dati da parte del firmatario. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o errori di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [TransformChain](./transformchain/) | Rappresenta una catena ordinata di trasformazioni. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o errori di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [X509IssuerSerial](./x509issuerserial/) | Rappresenta l'elemento '[X509IssuerSerial](./x509issuerserial/)' della firma XML. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o errori di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [XmlDsigC14NTransform](./xmldsigc14ntransform/) | Rappresenta la trasformazione di canonicalizzazione XML C14N per una firma digitale senza commenti. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o errori di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [XmlDsigC14NWithCommentsTransform](./xmldsigc14nwithcommentstransform/) | Rappresenta la trasformazione di canonicalizzazione XML C14N per una firma digitale con commenti. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o errori di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [XmlDsigEnvelopedSignatureTransform](./xmldsigenvelopedsignaturetransform/) | Rappresenta la trasformazione di firma enveloped per la firma XML. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o errori di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [XmlDsigExcC14NTransform](./xmldsigexcc14ntransform/) | Rappresenta la trasformazione di canonicalizzazione XML C14N esclusiva per una firma digitale senza commenti. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o errori di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |