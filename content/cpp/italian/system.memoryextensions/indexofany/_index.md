---
title: IndexOfAny()
second_title: Aspose.Slides per C++ Riferimento API
description: Trova l'indice della prima occorrenza di uno qualsiasi dei due valori specificati in un ReadOnlySpan<T>
type: docs
weight: 157
url: /it/system.memoryextensions/indexofany/
---
## System::MemoryExtensions::IndexOfAny(const ReadOnlySpan\<T\>\&, const T\&, const T\&) funzione

Trova l'indice della prima occorrenza di uno qualsiasi dei due valori specificati in un ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const ReadOnlySpan<T> &span, const T &value0, const T &value1)
```


### Parametri del modello

| Parameter | Description |
| --- | --- |
| T | Il tipo di elementi nello span |

### Argomenti

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Lo span in cui cercare |
| value0 | const T\& | Il primo valore da cercare |
| value1 | const T\& | Il secondo valore da cercare |

### Valore di ritorno

L'indice basato su zero della prima occorrenza, o -1 se non trovato

## System::MemoryExtensions::IndexOfAny(const ReadOnlySpan\<T\>\&, const T\&, const T\&, const T\&) funzione

Trova l'indice della prima occorrenza di uno qualsiasi dei tre valori specificati in un ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const ReadOnlySpan<T> &span, const T &value0, const T &value1, const T &value2)
```


### Parametri del modello

| Parameter | Description |
| --- | --- |
| T | Il tipo di elementi nello span |

### Argomenti

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Lo span in cui cercare |
| value0 | const T\& | Il primo valore da cercare |
| value1 | const T\& | Il secondo valore da cercare |
| value2 | const T\& | Il terzo valore da cercare |

### Valore di ritorno

L'indice basato su zero della prima occorrenza, o -1 se non trovato

## System::MemoryExtensions::IndexOfAny(const Span\<T\>\&, const T\&, const T\&) funzione

Trova l'indice della prima occorrenza di uno qualsiasi dei due valori specificati in un Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const Span<T> &span, const T &value0, const T &value1)
```


### Parametri del modello

| Parameter | Description |
| --- | --- |
| T | Il tipo di elementi nello span |

### Argomenti

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Lo span in cui cercare |
| value0 | const T\& | Il primo valore da cercare |
| value1 | const T\& | Il secondo valore da cercare |

### Valore di ritorno

L'indice basato su zero della prima occorrenza, o -1 se non trovato

## System::MemoryExtensions::IndexOfAny(const Span\<T\>\&, const T\&, const T\&, const T\&) funzione

Trova l'indice della prima occorrenza di uno qualsiasi dei tre valori specificati in un Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const Span<T> &span, const T &value0, const T &value1, const T &value2)
```


### Parametri del modello

| Parameter | Description |
| --- | --- |
| T | Il tipo di elementi nello span |

### Argomenti

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Lo span in cui cercare |
| value0 | const T\& | Il primo valore da cercare |
| value1 | const T\& | Il secondo valore da cercare |
| value2 | const T\& | Il terzo valore da cercare |

### Valore di ritorno

L'indice basato su zero della prima occorrenza, o -1 se non trovato

## System::MemoryExtensions::IndexOfAny(const ReadOnlySpan\<T\>\&, const ReadOnlySpan\<T\>\&) funzione

Trova l'indice della prima occorrenza di qualsiasi valore contenuto in uno span all'interno di un altro ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const ReadOnlySpan<T> &span, const ReadOnlySpan<T> &values)
```


### Parametri del modello

| Parameter | Description |
| --- | --- |
| T | Il tipo di elementi negli span |

### Argomenti

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Lo span in cui cercare |
| values | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Lo span contenente i valori da cercare |

### Valore di ritorno

L'indice basato su zero della prima occorrenza, o -1 se non trovato

## System::MemoryExtensions::IndexOfAny(const Span\<T\>\&, const ReadOnlySpan\<T\>\&) funzione

Trova l'indice della prima occorrenza di qualsiasi valore contenuto in uno span all'interno di un Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const Span<T> &span, const ReadOnlySpan<T> &values)
```


### Parametri del modello

| Parameter | Description |
| --- | --- |
| T | Il tipo di elementi negli span |

### Argomenti

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | Lo span in cui cercare |
| values | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | Lo span contenente i valori da cercare |

### Valore di ritorno

L'indice basato su zero della prima occorrenza, o -1 se non trovato

## Vedi anche

* Classe [ReadOnlySpan](../../system/readonlyspan/)
* Classe [Span](../../system/span/)
* Namespace [System::MemoryExtensions](../)
* Libreria [Aspose.Slides](../../)