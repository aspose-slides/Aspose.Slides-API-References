---
title: Increment()
second_title: Riferimento API di Aspose.Slides per C++
description: Incrementa il valore in modo atomico.
type: docs
weight: 1
url: /it/system.threading/interlocked/increment/
---
## Interlocked::Increment(int32_t\&) metodo

Incrementa il valore in modo atomico.

```cpp
static int32_t System::Threading::Interlocked::Increment(int32_t &location)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| location | **int32_t**\& | Riferimento alla variabile da incrementare. |

### Valore restituito

Valore della variabile subito dopo l'incremento.

## Interlocked::Increment(int64_t\&) metodo

Incrementa il valore in modo atomico.

```cpp
static int64_t System::Threading::Interlocked::Increment(int64_t &location)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| location | **int64_t**\& | Riferimento alla variabile da incrementare. |

### Valore restituito

Valore della variabile subito dopo l'incremento.

## Vedi anche

* Classe [Interlocked](../)
* Spazio dei nomi [System::Threading](../../)
* Libreria [Aspose.Slides](../../../)