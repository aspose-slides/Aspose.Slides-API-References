---
title: "System::Net::Http::Headers"
second_title: Riferimento API di Aspose.Slides per C++
description: 
type: docs
weight: 690
url: /it/system.net.http.headers/
---
## Classi

| Classe | Descrizione |
| --- | --- |
| [AuthenticationHeaderValue](./authenticationheadervalue/) | Rappresenta i valori degli header 'Authorization', 'ProxyAuthorization', 'WWW-Authenticate' e 'Proxy-Authenticate'. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [CacheControlHeaderValue](./cachecontrolheadervalue/) | Rappresenta un valore dell'header 'Cache-Control'. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [ContentDispositionHeaderValue](./contentdispositionheadervalue/) | Rappresenta un valore dell'header 'Content-Disposition'. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [ContentRangeHeaderValue](./contentrangeheadervalue/) | Rappresenta un valore dell'header 'Content-Range'. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [EntityTagHeaderValue](./entitytagheadervalue/) | Rappresenta un valore dell'header 'Entity-Tag'. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [HttpContentHeaders](./httpcontentheaders/) | Rappresenta la collezione degli header 'Content'. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [HttpHeaders](./httpheaders/) | La collezione degli header HTTP. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [HttpHeaderValueCollection](./httpheadervaluecollection/) | Rappresenta la collezione dei valori degli header. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [HttpHeaderValueCollection< System::String >](./httpheadervaluecollection_tmpl_system_string__end_tmpl/) | La specializzazione parziale del template [HttpHeaderValueCollection](./httpheadervaluecollection/) per il tipo [String](../system/string/). Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [HttpRequestHeaders](./httprequestheaders/) | Rappresenta la collezione degli header 'Request'. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [HttpResponseHeaders](./httpresponseheaders/) | Rappresenta la collezione degli header 'Response'. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [MediaTypeHeaderValue](./mediatypeheadervalue/) | Rappresenta un tipo MIME nel valore dell'header 'Content-Type'. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [MediaTypeWithQualityHeaderValue](./mediatypewithqualityheadervalue/) | Rappresenta un tipo MIME con un fattore di qualità aggiuntivo nel valore dell'header 'Content-Type'. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [NameValueHeaderValue](./namevalueheadervalue/) | Rappresenta una coppia chiave/valore da utilizzare negli header. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [NameValueWithParametersHeaderValue](./namevaluewithparametersheadervalue/) | Rappresenta una coppia chiave/valore con parametri da utilizzare negli header. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [ObjectCollection](./objectcollection/) | Rappresenta la collezione degli oggetti. |
| [ProductHeaderValue](./productheadervalue/) | Rappresenta un token di prodotto nel valore dell'header 'User-Agent'. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [ProductInfoHeaderValue](./productinfoheadervalue/) | Rappresenta un prodotto o un commento nel valore dell'header 'User-Agent'. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [RangeConditionHeaderValue](./rangeconditionheadervalue/) | Rappresenta un valore dell'header 'If-Range'. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [RangeHeaderValue](./rangeheadervalue/) | Rappresenta un valore dell'header '[Range](../system/range/)'. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [RangeItemHeaderValue](./rangeitemheadervalue/) | Rappresenta un intervallo di byte nel valore dell'header '[Range](../system/range/)'. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [RetryConditionHeaderValue](./retryconditionheadervalue/) | Rappresenta un valore dell'header 'Retry-After'. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [StringWithQualityHeaderValue](./stringwithqualityheadervalue/) | Rappresenta un valore con una qualità aggiuntiva di un header di tipo stringa. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [TransferCodingHeaderValue](./transfercodingheadervalue/) | Rappresenta un valore dell'header 'Accept-Encoding'. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [TransferCodingWithQualityHeaderValue](./transfercodingwithqualityheadervalue/) | Rappresenta un valore con una qualità aggiuntiva dell'header 'Accept-Encoding'. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [ViaHeaderValue](./viaheadervalue/) | Rappresenta un valore dell'header 'Via'. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
| [WarningHeaderValue](./warningheadervalue/) | Rappresenta un valore dell'header 'Warning'. Gli oggetti di questa classe devono essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo sullo stack o usando l'operatore new, poiché causerà errori di runtime e/o fault di asserzione. Avvolgere sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usare questo puntatore per passarlo alle funzioni come argomento. |
## Typedef

| Typedef | Descrizione |
| --- | --- |
| [HeaderFunc](./headerfunc/) |  |