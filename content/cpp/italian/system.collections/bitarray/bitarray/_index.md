---
title: BitArray()
second_title: Riferimento API di Aspose.Slides per C++
description: Costruttore di copia.
type: docs
weight: 14
url: /it/system.collections/bitarray/bitarray/
---
## BitArray::BitArray(const bitset\&) costruttore

Costruttore di copia.

```cpp
System::Collections::BitArray::BitArray(const bitset &bits)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| bits | const [bitset](../bitset/)\& | Contenitore da cui copiare i dati. |

## BitArray::BitArray(const BitArray\&) costruttore

Costruttore di copia.

```cpp
System::Collections::BitArray::BitArray(const BitArray &bits)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| bits | const [BitArray](../)\& | Contenitore da cui copiare i dati. |

## BitArray::BitArray(const BitArrayPtr\&) costruttore

Costruttore di copia.

```cpp
System::Collections::BitArray::BitArray(const BitArrayPtr &array)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| array | const [BitArrayPtr](../../bitarrayptr/)\& | [BitArray](../) da cui copiare i dati. |

## BitArray::BitArray(const System::ArrayPtr\<bool\>\&) costruttore

Costruttore di copia.

```cpp
System::Collections::BitArray::BitArray(const System::ArrayPtr<bool> &values)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| values | const [System::ArrayPtr](../../../system/arrayptr/)\<**bool**\>\& | Contenitore da cui copiare i dati elemento per elemento. |

## BitArray::BitArray(const System::ArrayPtr\<uint8_t\>\&) costruttore

Costruttore di copia.

```cpp
System::Collections::BitArray::BitArray(const System::ArrayPtr<uint8_t> &bytes)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| bytes | const [System::ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\>\& | Contenitore da cui copiare i dati a livello di bit. |

## BitArray::BitArray(const System::ArrayPtr\<int\>\&) costruttore

Costruttore di copia.

```cpp
System::Collections::BitArray::BitArray(const System::ArrayPtr<int> &values)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| values | const [System::ArrayPtr](../../../system/arrayptr/)\<int\>\& | Contenitore da cui copiare i dati a livello di bit. |

## BitArray::BitArray(int, bool) costruttore

Costruttore di riempimento.

```cpp
System::Collections::BitArray::BitArray(int length, bool defaultValue=false)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| length | int | Numero di elementi da riempire. |
| defaultValue | **bool** | Valore di riempimento. |

## Vedi anche

* Typedef [bitset](../bitset/)
* Typedef [ArrayPtr](../../../system/arrayptr/)
* Classe [BitArray](../)
* Classe [BitArrayPtr](../../bitarrayptr/)
* Spazio dei nomi [System::Collections](../../)
* Library [Aspose.Slides](../../../)