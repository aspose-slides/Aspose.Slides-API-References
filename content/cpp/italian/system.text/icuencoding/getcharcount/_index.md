---
title: GetCharCount()
second_title: Riferimento API di Aspose.Slides per C++
description: Ottieni il numero di caratteri necessari per decodificare un buffer di byte.
type: docs
weight: 53
url: /it/system.text/icuencoding/getcharcount/
---
## ICUEncoding::GetCharCount(const uint8_t *, int) metodo


Ottieni il numero di caratteri necessari per decodificare un buffer di byte.

```cpp
int System::Text::ICUEncoding::GetCharCount(const uint8_t *bytes, int count) override
```


### Argomenti

| Parameter | Type | Description |
| --- | --- | --- |
| bytes | const **uint8_t** * | Byte da decodificare. |
| count | int | Conteggio dei byte. |

### Valore di ritorno

Numero di caratteri.

## ICUEncoding::GetCharCount(ArrayPtr\<uint8_t\>, int, int) metodo


Ottieni il numero di caratteri necessari per decodificare un buffer di byte.

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes, int index, int count)
```


### Argomenti

| Parameter | Type | Description |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | Byte da decodificare. |
| index | int | Inizio della sezione. |
| count | int | Dimensione della sezione. |

### Valore di ritorno

Numero di caratteri.

## ICUEncoding::GetCharCount(ArrayPtr\<uint8_t\>) metodo


Ottieni il numero di caratteri necessari per decodificare un buffer di byte.

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes)
```


### Argomenti

| Parameter | Type | Description |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | Byte da decodificare. |

### Valore di ritorno

Numero di caratteri.

## ICUEncoding::GetCharCount(const uint8_t *, int) metodo


Ottieni il numero di caratteri necessari per decodificare un buffer di byte.

```cpp
virtual int System::Text::Encoding::GetCharCount(const uint8_t *bytes, int count)
```


### Argomenti

| Parameter | Type | Description |
| --- | --- | --- |
| bytes | const **uint8_t** * | Byte da decodificare. |
| count | int | Conteggio dei byte. |

### Valore di ritorno

Numero di caratteri.

## Vedi anche

* Typedef [ArrayPtr](../../../system/arrayptr/)
* Classe [ICUEncoding](../)
* Spazio dei nomi [System::Text](../../)
* Library [Aspose.Slides](../../../)