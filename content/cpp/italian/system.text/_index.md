---
title: "System::Text"
second_title: Riferimento API di Aspose.Slides per C++
description: 
type: docs
weight: 976
url: /it/system.text/
---
## Classi

| Class | Description |
| --- | --- |
| [ASCIIEncoding](./asciiencoding/) | Rappresenta la codifica ASCII. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [Decoder](./decoder/) | Incapsula la decodifica di una sequenza di byte in una sequenza di caratteri. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [DecoderExceptionFallback](./decoderexceptionfallback/) | Fornisce una strategia di fallback che genera eccezioni. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [DecoderExceptionFallbackBuffer](./decoderexceptionfallbackbuffer/) | [Buffer](../system/buffer/) per strategia di fallback di decodifica che genera eccezioni. Non memorizza nulla in realtà, ma lancia eccezioni. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [DecoderFallback](./decoderfallback/) | Fornisce un'API di fallback per gestire gli errori di decodifica. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [DecoderFallbackBuffer](./decoderfallbackbuffer/) | Fornisce un buffer per l'implementazione di fallback. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [DecoderReplacementFallback](./decoderreplacementfallback/) | Fornisce una strategia di fallback che sostituisce il simbolo errato con un segnaposto. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [DecoderReplacementFallbackBuffer](./decoderreplacementfallbackbuffer/) | [Buffer](../system/buffer/) per la strategia di fallback di decodifica di sostituzione. |
| [Details_DecoderFallbackException](./details_decoderfallbackexception/) | Eccezione generata da [DecoderExceptionFallback](./decoderexceptionfallback/) quando la decodifica fallisce. Non creare istanze di questa classe manualmente. Usa invece la classe DecoderFallbackException. Non avvolgere mai le istanze della classe DecoderFallbackException in [System::SmartPtr](../system/smartptr/). |
| [Details_EncoderFallbackException](./details_encoderfallbackexception/) | Eccezione generata da [EncoderExceptionFallback](./encoderexceptionfallback/) quando la codifica fallisce. Non creare istanze di questa classe manualmente. Usa invece la classe EncoderFallbackException. Non avvolgere mai le istanze della classe EncoderFallbackException in [System::SmartPtr](../system/smartptr/). |
| [Encoder](./encoder/) | Incapsula la codifica di una sequenza di caratteri in una sequenza di byte. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [EncoderExceptionFallback](./encoderexceptionfallback/) | Fornisce una strategia di fallback che genera eccezioni. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [EncoderExceptionFallbackBuffer](./encoderexceptionfallbackbuffer/) | [Buffer](../system/buffer/) per strategia di fallback di codifica che genera eccezioni. Non memorizza nulla in realtà, ma lancia eccezioni. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [EncoderFallback](./encoderfallback/) | Fornisce un'API di fallback per gestire gli errori di codifica. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [EncoderFallbackBuffer](./encoderfallbackbuffer/) | Fornisce un buffer per l'implementazione di fallback. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [EncoderReplacementFallback](./encoderreplacementfallback/) | Fornisce una strategia di fallback che sostituisce il simbolo errato con un segnaposto. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [EncoderReplacementFallbackBuffer](./encoderreplacementfallbackbuffer/) | [Buffer](../system/buffer/) per la strategia di fallback di codifica di sostituzione. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [Encoding](./encoding/) | [Encoding](./encoding/) servizi. |
| [EncodingDecoder](./encodingdecoder/) | [Decoder](./decoder/) che utilizza un oggetto di codifica per la decodifica. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [EncodingEncoder](./encodingencoder/) | [Encoder](./encoder/) che utilizza un oggetto di codifica per la codifica. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [EncodingInfo](./encodinginfo/) | Brevi informazioni sulla codifica. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [ICUDecoder](./icudecoder/) | [Decoder](./decoder/) che utilizza ICU per la decodifica. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [ICUEncoder](./icuencoder/) | [Encoder](./encoder/) che utilizza ICU per la codifica. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [ICUEncoding](./icuencoding/) | Implementazione di codifica basata su ICU. SOLO PER USO INTERNO. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [Latin1Encoding](./latin1encoding/) | Supporto per la codifica Latin1. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [StringBuilder](./stringbuilder/) | [Buffer](../system/buffer/) per accumulare stringhe parte per parte. Questo tipo può essere allocato sia nello stack come tipo valore sia nell'heap usando la funzione [System::MakeObject()](../system/makeobject/). Una volta allocato l'oggetto, non mescolare questi due casi d'uso: avere puntatori [SmartPtr](../system/smartptr/) su oggetti allocati nello stack è strettamente proibito. |
| [UnicodeEncoding](./unicodeencoding/) | Codifica Unicode. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [UTF32Encoding](./utf32encoding/) | Codifica UTF-32. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [UTF7Encoding](./utf7encoding/) | Codifica UTF-7. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [UTF8Encoding](./utf8encoding/) | Codifica UTF-8. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fallimenti di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
## Enumerazioni

| Enum | Description |
| --- | --- |
| [NormalizationForm](./normalizationform/) | Definisce come normalizzare una stringa Unicode. |
## Definizioni di tipo

| Typedef | Description |
| --- | --- |
| [DecoderFallbackException](./decoderfallbackexception/) |  |
| [EncoderFallbackException](./encoderfallbackexception/) |  |
| [ICUEncodingPtr](./icuencodingptr/) | Puntatore di codifica ICU. |