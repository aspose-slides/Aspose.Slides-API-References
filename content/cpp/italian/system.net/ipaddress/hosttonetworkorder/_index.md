---
title: HostToNetworkOrder()
second_title: Riferimento API di Aspose.Slides per C++
description: Converte l'ordine dei byte host specificato nell'ordine dei byte di rete corrispondente.
type: docs
weight: 248
url: /it/system.net/ipaddress/hosttonetworkorder/
---
## IPAddress::HostToNetworkOrder(int64_t) metodo

Converte l'ordine dei byte host specificato nell'ordine dei byte di rete corrispondente.

```cpp
static int64_t System::Net::IPAddress::HostToNetworkOrder(int64_t host)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| host | **int64_t** | Il valore da convertire. |

### Valore di ritorno

L'ordine dei byte di rete corrispondente.

## IPAddress::HostToNetworkOrder(int32_t) metodo

Converte l'ordine dei byte host specificato nell'ordine dei byte di rete corrispondente.

```cpp
static int32_t System::Net::IPAddress::HostToNetworkOrder(int32_t host)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| host | **int32_t** | Il valore da convertire. |

### Valore di ritorno

L'ordine dei byte di rete corrispondente.

## IPAddress::HostToNetworkOrder(int16_t) metodo

Converte l'ordine dei byte host specificato nell'ordine dei byte di rete corrispondente.

```cpp
static int16_t System::Net::IPAddress::HostToNetworkOrder(int16_t host)
```

### Argomenti

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| host | **int16_t** | Il valore da convertire. |

### Valore di ritorno

L'ordine dei byte di rete corrispondente.

## Vedi anche

* Classe [IPAddress](../)
* Namespace [System::Net](../../)
* Libreria [Aspose.Slides](../../../)