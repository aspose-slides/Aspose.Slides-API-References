---
title: "System::Net"
second_title: Riferimento API di Aspose.Slides per C++
description: 
type: docs
weight: 651
url: /it/system.net/
---
## Classi

| Classe | Descrizione |
| --- | --- |
| [Cookie](./cookie/) | Rappresenta un cookie HTTP. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [CookieCollection](./cookiecollection/) | Rappresenta un elenco di cookie ordinati. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [CookieComparer](./cookiecomparer/) | Utilizzato per confrontare le istanze della classe [Cookie](./cookie/). Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [CookieContainer](./cookiecontainer/) | Fornisce un contenitore per le istanze della classe CookieCollection. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [CookieParser](./cookieparser/) | Utilizzato per analizzare un'intestazione cookie e creare un'istanza della classe [Cookie](./cookie/). Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [CredentialCache](./credentialcache/) | Fornisce lo storage delle credenziali. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [Details_CookieException](./details_cookieexception/) | Rappresenta un'eccezione sollevata quando la dimensione di [CookieContainer](./cookiecontainer/) è maggiore del valore della proprietà MaxCookieSize. Non creare manualmente istanze di questa classe. Usa invece la classe CookieException. Non avvolgere le istanze della classe CookieException in [System::SmartPtr](../system/smartptr/). |
| [Details_WebException](./details_webexception/) | Rappresenta l'eccezione sollevata da [WebRequest](./webrequest/) quando si verifica un errore. Non creare manualmente istanze di questa classe. Usa invece la classe WebException. Non avvolgere le istanze della classe WebException in [System::SmartPtr](../system/smartptr/). |
| [Dns](./dns/) | Fornisce metodi per lavorare con DNS. |
| [DnsEndPoint](./dnsendpoint/) | Contiene le informazioni utilizzate dall'applicazione per connettersi al servizio. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [EndPoint](./endpoint/) | La classe astratta contiene un indirizzo di rete. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [FileWebRequest](./filewebrequest/) | Fornisce l'implementazione della classe astratta [WebRequest](./webrequest/) per lavorare con il file system. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [FileWebResponse](./filewebresponse/) | Fornisce l'implementazione della classe astratta [WebResponse](./webresponse/) per lavorare con il file system. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [FtpWebRequest](./ftpwebrequest/) | Implementa un client File Transfer Protocol (FTP). Classe fittizia che consente di collegare il codice tradotto con riferimenti [FtpWebRequest](./ftpwebrequest/), ma non lo esegue. Non contiene membri implementati correttamente. |
| [FtpWebResponse](./ftpwebresponse/) | Classe fittizia che consente di collegare il codice tradotto con riferimenti [FtpWebResponse](./ftpwebresponse/), ma non lo esegue. Non contiene membri implementati correttamente. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [HeaderVariantInfo](./headervariantinfo/) | Utilizzato per confrontare il nome del cookie e la specifica. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [HttpKnownHeaderNames](./httpknownheadernames/) | Contiene i valori stringa dei nomi di intestazione noti. |
| [HttpRequestHeaderExtensions](./httprequestheaderextensions/) | Contiene metodi di utilità per lavorare con i valori di enumerazione HttpRequestHeader. |
| [HttpResponseHeaderExtensions](./httpresponseheaderextensions/) | Contiene metodi di utilità per lavorare con i valori di enumerazione HttpResponseHeader. |
| [HttpStatusDescription](./httpstatusdescription/) | Contiene i metodi di utilità per ottenere la rappresentazione stringa dello stato HTTP. |
| [HttpVersion](./httpversion/) | Contiene le versioni di HTTP. |
| [HttpWebRequest](./httpwebrequest/) | Rappresenta una richiesta web HTTP. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [HttpWebResponse](./httpwebresponse/) | Rappresenta una risposta web HTTP. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò proverà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [ICredentials](./icredentials/) | Fornisce l'interfaccia di autenticazione. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [ICredentialsByHost](./icredentialsbyhost/) | Fornisce l'interfaccia di autenticazione per recuperare le credenziali per un host, una porta e un tipo di autenticazione. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [IPAddress](./ipaddress/) | Rappresenta l'indirizzo IP. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [IPEndPoint](./ipendpoint/) | Rappresenta un endpoint di rete che contiene un indirizzo IP e una porta. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [IPHostEntry](./iphostentry/) | Rappresenta le informazioni su un indirizzo host Internet. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [IWebProxy](./iwebproxy/) | Questa interfaccia è usata per l'implementazione dell'accesso proxy alla classe [WebRequest](./webrequest/). Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [IWebRequestCreate](./iwebrequestcreate/) | Fornisce i metodi per creare le istanze della classe [WebRequest](./webrequest/). Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [NetworkCredential](./networkcredential/) | Fornisce credenziali per i meccanismi di autenticazione basati su password. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [PathList](./pathlist/) | Rappresenta l'elenco delle istanze della classe [CookieCollection](./cookiecollection/). Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [ServicePoint](./servicepoint/) | Fornisce la gestione delle connessioni HTTP. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [ServicePointManager](./servicepointmanager/) | Gestisce le fasi del ciclo di vita (creazione, mantenimento ed eliminazione) delle istanze della classe [ServicePoint](./servicepoint/). Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [SocketAddress](./socketaddress/) | Usato per memorizzare informazioni serializzate sulle istanze della classe [EndPoint](./endpoint/). Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [UriScheme](./urischeme/) | Rappresenta lo schema per l'URI. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [WebClient](./webclient/) | [WebClient](./webclient/) fornisce metodi comuni per l'invio e la ricezione dei dati. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [WebHeaderCollection](./webheadercollection/) | Rappresenta la raccolta delle intestazioni del protocollo. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [WebProxy](./webproxy/) | Rappresenta un server proxy web HTTP. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [WebRequest](./webrequest/) | Rappresenta una richiesta web. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
| [WebRequestMethods](./webrequestmethods/) | Rappresenta le costanti stringa delle richieste web. |
| [WebResponse](./webresponse/) | Rappresenta una risposta web. Gli oggetti di questa classe dovrebbero essere allocati solo usando la funzione [System::MakeObject()](../system/makeobject/). Non creare mai un'istanza di questo tipo nello stack o usando l'operatore new, poiché ciò provocherà errori di runtime e/o fault di asserzione. Avvolgi sempre questa classe in un puntatore [System::SmartPtr](../system/smartptr/) e usa questo puntatore per passarla alle funzioni come argomento. |
## Enumerazioni

| Enumerazione | Descrizione |
| --- | --- |
| [AuthenticationSchemes](./authenticationschemes/) | Enumera i meccanismi di autenticazione. |
| [CookieVariant](./cookievariant/) | Enumera le specifiche dei cookie. |
| [DecompressionMethods](./decompressionmethods/) | Enumera l'algoritmo di compressione-decompressione. |
| [HttpRequestHeader](./httprequestheader/) | Enumera le intestazioni di richiesta. |
| [HttpResponseHeader](./httpresponseheader/) | Enumera le intestazioni di risposta HTTP. |
| [HttpStatusCode](./httpstatuscode/) |  |
| [SecurityProtocolType](./securityprotocoltype/) | Enumera i tipi di protocollo di sicurezza. |
| [TransportType](./transporttype/) | Definisce il tipo di trasporto consentito per il socket. |
| [WebExceptionStatus](./webexceptionstatus/) | Enumera i codici di stato della classe WebException. |
| [WebHeaderCollectionType](./webheadercollectiontype/) | Enumera i tipi della raccolta di intestazioni del protocollo. |
## Typedef

| Typedef | Descrizione |
| --- | --- |
| [CookieException](./cookieexception/) |  |
| [BindIPEndPoint](./bindipendpoint/) |  |
| [WebException](./webexception/) |  |