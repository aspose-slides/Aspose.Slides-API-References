---
title: WeakReference()
second_title: Aspose.Slides C++ API hivatkozás
description: Alapértelmezett konstruktor.
type: docs
weight: 1
url: /hu/system/weakreference_tmpl_t__end_tmpl/weakreference/
---
## WeakReference< T >::WeakReference() metódus

Alapértelmezett konstruktor.

```cpp
System::WeakReference<T>::WeakReference()
```

## WeakReference< T >::WeakReference(std::nullptr_t) metódus

Konstruktor nullptr-ért.

```cpp
System::WeakReference<T>::WeakReference(std::nullptr_t)
```

## WeakReference< T >::WeakReference(const SmartPtr\<T\>\&) metódus

Új példányt hoz létre a WeakReference osztályból, amely a megadott objektumra hivatkozik.

```cpp
System::WeakReference<T>::WeakReference(const SmartPtr<T> &data)
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) a tároláshoz. |

## WeakReference< T >::WeakReference(const SmartPtr\<T\>\&, bool) metódus

Új példányt hoz létre a WeakReference osztályból, amely a megadott objektumra hivatkozik.

```cpp
System::WeakReference<T>::WeakReference(const SmartPtr<T> &data, bool trackResurrection)
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<T\>\& | [Object](../../object/) a tároláshoz. |
| trackResurrection | **bool** | Figyelmen kívül hagyva. |

## Lásd még

* Osztály [WeakReference< T >](../)
* Osztály [SmartPtr](../../smartptr/)
* Névtere [System](../../)
* Könyvtár [Aspose.Slides](../../../)