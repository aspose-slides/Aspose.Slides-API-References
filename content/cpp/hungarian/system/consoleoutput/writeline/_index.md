---
title: WriteLine()
second_title: Aspose.Slides C++ API referencia
description: Kiírja az aktuális sorvégjelet a jelenlegi objektum által képviselt kimeneti áramba.
type: docs
weight: 27
url: /hu/system/consoleoutput/writeline/
---
## ConsoleOutput::WriteLine() metódus

Kiírja az aktuális sorvégjelet a jelenlegi objektum által képviselt kimeneti áramba.

```cpp
void System::ConsoleOutput::WriteLine() override
```

## ConsoleOutput::WriteLine(const SharedPtr\<Object\>\&) metódus

Kiírja a megadott objektum karakterlánc-ábrázolását, majd az aktuális sorvégjelet a jelenlegi objektum által képviselt kimeneti áramba.

```cpp
void System::ConsoleOutput::WriteLine(const SharedPtr<Object> &value) override
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | A kimenetre írandó objektum |

## ConsoleOutput::WriteLine(bool) metódus

Kiírja a megadott bool érték karakterlánc-ábrázolását, majd az aktuális sorvégjelet a jelenlegi objektum által képviselt kimeneti áramba.

```cpp
void System::ConsoleOutput::WriteLine(bool value) override
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | **bool** | A kimenetre írandó érték |

## ConsoleOutput::WriteLine(char_t) metódus

Kiírja a megadott karakterértéket, majd az aktuális sorvégjelet a jelenlegi objektum által képviselt kimeneti áramba.

```cpp
void System::ConsoleOutput::WriteLine(char_t value) override
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | char_t | A kimenetre írandó érték |

## ConsoleOutput::WriteLine(Decimal) metódus

Kiírja a [Decimal](../../decimal/) érték karakterlánc-ábrázolását, majd az aktuális sorvégjelet a jelenlegi objektum által képviselt kimeneti áramba.

```cpp
void System::ConsoleOutput::WriteLine(Decimal value) override
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | A kimenetre írandó érték |

## ConsoleOutput::WriteLine(double) metódus

Kiírja a dupla pontosságú lebegőpontos érték karakterlánc-ábrázolását, majd az aktuális sorvégjelet a jelenlegi objektum által képviselt kimeneti áramba.

```cpp
void System::ConsoleOutput::WriteLine(double value) override
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | **double** | A kimenetre írandó érték |

## ConsoleOutput::WriteLine(int) metódus

Kiírja a 32 bites egész érték karakterlánc-ábrázolását, majd az aktuális sorvégjelet a jelenlegi objektum által képviselt kimeneti áramba.

```cpp
void System::ConsoleOutput::WriteLine(int value) override
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | int | A kimenetre írandó érték |

## ConsoleOutput::WriteLine(int64_t) metódus

Kiírja a 64 bites egész érték karakterlánc-ábrázolását, majd az aktuális sorvégjelet a jelenlegi objektum által képviselt kimeneti áramba.

```cpp
void System::ConsoleOutput::WriteLine(int64_t value) override
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | **int64_t** | A kimenetre írandó érték |

## ConsoleOutput::WriteLine(float) metódus

Kiírja az egyszeres pontosságú lebegőpontos érték karakterlánc-ábrázolását, majd az aktuális sorvégjelet a jelenlegi objektum által képviselt kimeneti áramba.

```cpp
void System::ConsoleOutput::WriteLine(float value) override
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | **float** | A kimenetre írandó érték |

## ConsoleOutput::WriteLine(const String\&) metódus

Kiírja a megadott string objektumot, majd az aktuális sorvégjelet a jelenlegi objektum által képviselt kimeneti áramba.

```cpp
void System::ConsoleOutput::WriteLine(const String &value) override
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | const [String](../../string/)\& | A kimenetre írandó string objektum |

## ConsoleOutput::WriteLine(uint32_t) metódus

Kiírja a 32 bites előjel nélküli egész érték karakterlánc-ábrázolását, majd az aktuális sorvégjelet a jelenlegi objektum által képviselt kimeneti áramba.

```cpp
void System::ConsoleOutput::WriteLine(uint32_t value) override
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | **uint32_t** | A kimenetre írandó érték |

## ConsoleOutput::WriteLine(uint64_t) metódus

Kiírja a 64 bites előjel nélküli egész érték karakterlánc-ábrázolását, majd az aktuális sorvégjelet a jelenlegi objektum által képviselt kimeneti áramba.

```cpp
void System::ConsoleOutput::WriteLine(uint64_t value) override
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | **uint64_t** | A kimenetre írandó érték |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&) metódus

Kiírja a megadott karaktertömb karakterlánc-ábrázolását, majd az aktuális sorvégjelet a jelenlegi objektum által képviselt kimeneti áramba.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer) override
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | A kimenetre írandó tömb |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&, int32_t, int32_t) metódus

Kiírja a megadott karaktertömb egy tartományának értékeinek karakterlánc-ábrázolását, majd az aktuális sorvégjelet a jelenlegi objektum által képviselt kimeneti áramba.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | A kimenetre írandó értékeket tartalmazó tömb |
| index | **int32_t** | Az index, ahol a kimenetre írandó elemek tartománya kezdődik |
| count | **int32_t** | A kimenetre írandó elemek tartományának elemeinek száma |

## ConsoleOutput::WriteLine(const char_t *) metódus

Kiírja a megadott C-karakterláncot, majd az aktuális sorvégjelet a jelenlegi objektum által képviselt kimeneti áramba.

```cpp
void System::ConsoleOutput::WriteLine(const char_t *value) override
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | const char_t * | A kimenetre írandó C-karakterlánc |

## ConsoleOutput::WriteLine(const TypeInfo\&) metódus

Kiírja a megadott [TypeInfo](../../typeinfo/) objektum karakterlánc-ábrázolását, majd az aktuális sorvégjelet a jelenlegi objektum által képviselt kimeneti áramba.

```cpp
void System::ConsoleOutput::WriteLine(const TypeInfo &value) override
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | A kimenetre írandó [TypeInfo](../../typeinfo/) objektum |

## ConsoleOutput::WriteLine(const char *) metódus




```cpp
void System::ConsoleOutput::WriteLine(const char *)=delete
```

## Lásd még

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Osztály [ConsoleOutput](../)
* Osztály [Object](../../object/)
* Osztály [Decimal](../../decimal/)
* Osztály [String](../../string/)
* Osztály [TypeInfo](../../typeinfo/)
* Névtér [System](../../)
* Könyvtár [Aspose.Slides](../../../)