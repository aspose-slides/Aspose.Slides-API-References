---
title: IsDigit()
second_title: Aspose.Slides C++ API hivatkozás
description: Megállapítja, hogy a megadott karakterpufferben a megadott indexű karakter decimális számjegynek minősül-e.
type: docs
weight: 79
url: /hu/system/char/isdigit/
---
## Char::IsDigit(const char_t *, int) metódus

Megállapítja, hogy a megadott karakterpufferben a megadott indexű karakter decimális számjegynek minősül-e.

```cpp
static bool System::Char::IsDigit(const char_t *str, int idx)
```

### Argumentumok

| Parameter | Type | Description |
| --- | --- | --- |
| str | const char_t * | Pointer to the beginning of the character buffer |
| idx | int | A zero-based index in the specified buffer of the character to test |

### Visszatérési érték

Igaz, ha a megadott indexű karakter decimális számjegy, egyébként - hamis

## Char::IsDigit(const String\&, const int32_t) metódus

Megállapítja, hogy a megadott karakterláncban a megadott indexű karakter decimális számjegynek minősül-e.

```cpp
static bool System::Char::IsDigit(const String &str, const int32_t idx)
```

### Argumentumok

| Parameter | Type | Description |
| --- | --- | --- |
| str | const [String](../../string/)\& | A string |
| idx | const **int32_t** | A zero-based index in the specified buffer of the character to test |

### Visszatérési érték

Igaz, ha a megadott indexű karakter decimális számjegy, egyébként - hamis

## Char::IsDigit(char_t) metódus

Megállapítja, hogy a megadott karakter decimális számjegynek minősül-e.

```cpp
static bool System::Char::IsDigit(char_t c)
```

### Argumentumok

| Parameter | Type | Description |
| --- | --- | --- |
| c | char_t | The character to test |

### Visszatérési érték

Igaz, ha a megadott indexű karakter decimális számjegy, egyébként - hamis

## Lásd még

* Osztály [Char](../)
* Osztály [String](../../string/)
* Névtér [System](../../)
* Könyvtár [Aspose.Slides](../../../)