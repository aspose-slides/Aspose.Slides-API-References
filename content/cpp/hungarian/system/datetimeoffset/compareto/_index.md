---
title: CompareTo()
second_title: Aspose.Slides C++ API referencia
description: Két DateTimeOffset objektumot hasonlít össze.
type: docs
weight: 352
url: /hu/system/datetimeoffset/compareto/
---
## DateTimeOffset::CompareTo(const DateTimeOffset\&) const metódus

Két [DateTimeOffset](../) objektumot hasonlít össze.

```cpp
int System::DateTimeOffset::CompareTo(const DateTimeOffset &other) const
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/) to compare. |

### Visszatérési érték

- 1 – ha a jelenlegi objektum korábbi, mint **other**, 1 – ha a jelenlegi objektum későbbi, mint **other**, 0 – ha a jelenlegi objektum egyenlő **other**-val.

## DateTimeOffset::CompareTo(const SharedPtr\<Object\>\&) const metódus

Két [DateTimeOffset](../) objektumot hasonlít össze.

```cpp
int System::DateTimeOffset::CompareTo(const SharedPtr<Object> &obj) const
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/) to compare. |

### Visszatérési érték

- 1 – ha a jelenlegi objektum korábbi, mint **obj**, 1 – ha a jelenlegi objektum későbbi, mint **obj**, 0 – ha a jelenlegi objektum egyenlő **obj**-val.

## Lásd még

* Typedef [SharedPtr](../../sharedptr/)
* Osztály [DateTimeOffset](../)
* Osztály [Object](../../object/)
* Névtér [System](../../)
* Library [Aspose.Slides](../../../)