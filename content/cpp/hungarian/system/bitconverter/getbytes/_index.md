---
title: GetBytes()
second_title: Aspose.Slides C++ API referenciája
description: Átalakítja a megadott logikai értéket egy bájttömbbé.
type: docs
weight: 14
url: /hu/system/bitconverter/getbytes/
---
## BitConverter::GetBytes(bool) metódus


Átalakítja a megadott bool értéket egy bájt tömbbé.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(bool value)
```


### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | **bool** | A konvertálandó boolean érték |

### Visszatérési érték

1 bájtos tömb, amely a megadott értéket képviseli

## BitConverter::GetBytes(char_t) metódus


Átalakítja a megadott char_t értéket egy bájt tömbbé.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(char_t value)
```


### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | char_t | A konvertálandó char_t érték |

### Visszatérési érték

2 bájtos tömb, amely a megadott értéket képviseli

## BitConverter::GetBytes(int16_t) metódus


Átalakítja a megadott 16-bit integer értéket egy bájt tömbbé.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int16_t value)
```


### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | **int16_t** | A konvertálandó 16 bites egész érték |

### Visszatérési érték

2 bájtos tömb, amely a megadott értéket képviseli

## BitConverter::GetBytes(int) metódus


Átalakítja a megadott 32-bit integer értéket egy bájt tömbbé.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int value)
```


### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | int | A konvertálandó 32 bites egész érték |

### Visszatérési érték

4 bájtos tömb, amely a megadott értéket képviseli

## BitConverter::GetBytes(int64_t) metódus


Átalakítja a megadott 64-bit integer értéket egy bájt tömbbé.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int64_t value)
```


### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | **int64_t** | A konvertálandó 64 bites egész érték |

### Visszatérési érték

8 bájtos tömb, amely a megadott értéket képviseli

## BitConverter::GetBytes(uint16_t) metódus


Átalakítja a megadott unsigned 16-bit integer értéket egy bájt tömbbé.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint16_t value)
```


### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | **uint16_t** | A konvertálandó 16 bites előjeles nélküli egész érték |

### Visszatérési érték

2 bájtos tömb, amely a megadott értéket képviseli

## BitConverter::GetBytes(uint32_t) metódus


Átalakítja a megadott unsigned 32-bit integer értéket egy bájt tömbbé.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint32_t value)
```


### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | **uint32_t** | A konvertálandó 32 bites előjeles nélküli egész érték |

### Visszatérési érték

4 bájtos tömb, amely a megadott értéket képviseli

## BitConverter::GetBytes(uint64_t) metódus


Átalakítja a megadott unsigned 64-bit integer értéket egy bájt tömbbé.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint64_t value)
```


### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | **uint64_t** | A konvertálandó 64 bites előjeles nélküli egész érték |

### Visszatérési érték

8 bájtos tömb, amely a megadott értéket képviseli

## BitConverter::GetBytes(float) metódus


Átalakítja a megadott egyetlen pontosságú lebegőpontos értéket egy bájt tömbbé.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(float value)
```


### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | **float** | A konvertálandó lebegőpontos érték |

### Visszatérési érték

4 bájtos tömb, amely a megadott értéket képviseli

## BitConverter::GetBytes(double) metódus


Átalakítja a megadott dupla pontosságú lebegőpontos értéket egy bájt tömbbé.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(double value)
```


### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | **double** | A konvertálandó lebegőpontos érték |

### Visszatérési érték

8 bájtos tömb, amely a megadott értéket képviseli

## Lásd még

* Typedef [ArrayPtr](../../arrayptr/)
* Class [BitConverter](../)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)