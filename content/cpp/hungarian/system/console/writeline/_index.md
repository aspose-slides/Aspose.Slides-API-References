---
title: WriteLine()
second_title: Aspose.Slides for C++ API referenciája
description: Kiírja az aktuális sortörést a szabványos kimeneti folyamra.
type: docs
weight: 14
url: /hu/system/console/writeline/
---
## Console::WriteLine() metódus

Kiírja az aktuális sortörést a szabványos kimeneti folyamra.

```cpp
static void System::Console::WriteLine()
```

## Console::WriteLine(const SharedPtr\<T\>\&) metódus

Kiírja a megadott objektum karakteres ábrázolását, majd az aktuális sortörést a szabványos kimeneti folyamra.

```cpp
template<class T> static void System::Console::WriteLine(const SharedPtr<T> &object)
```

### Sablonparaméterek

| Paraméter | Leírás |
| --- | --- |
| T | A kiírandó objektum típusa |

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| object | const [SharedPtr](../../sharedptr/)\<T\>\& | [Object](../../object/) kiírásra |

## Console::WriteLine(bool) metódus

Kiírja a bool érték karakteres ábrázolását, majd az aktuális sortörést a szabványos kimeneti folyamra.

```cpp
static void System::Console::WriteLine(bool value)
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | **bool** | A kiírandó érték |

## Console::WriteLine(char_t) metódus

Kiírja a megadott karakter értékét, majd az aktuális sortörést a szabványos kimeneti folyamra.

```cpp
static void System::Console::WriteLine(char_t value)
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | char_t | A kiírandó érték |

## Console::WriteLine(const ArrayPtr\<char_t\>\&) metódus

Kiírja a megadott karaktertömb karakteres ábrázolását, majd az aktuális sortörést a szabványos kimeneti folyamra.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer)
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | A kiírandó tömb |

## Console::WriteLine(const Decimal\&) metódus

Kiírja a [Decimal](../../decimal/) érték karakteres ábrázolását, majd az aktuális sortörést a szabványos kimeneti folyamra.

```cpp
static void System::Console::WriteLine(const Decimal &value)
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | A kiírandó érték |

## Console::WriteLine(double) metódus

Kiírja a dupla pontosságú lebegőpontos érték karakteres ábrázolását, majd az aktuális sortörést a szabványos kimeneti folyamra.

```cpp
static void System::Console::WriteLine(double value)
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | **double** | A kiírandó érték |

## Console::WriteLine(float) metódus

Kiírja az egyszeres pontosságú lebegőpontos érték karakteres ábrázolását, majd az aktuális sortörést a szabványos kimeneti folyamra.

```cpp
static void System::Console::WriteLine(float value)
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | **float** | A kiírandó érték |

## Console::WriteLine(int32_t) metódus

Kiírja a 32 bites egész szám érték karakteres ábrázolását, majd az aktuális sortörést a szabványos kimeneti folyamra.

```cpp
static void System::Console::WriteLine(int32_t value)
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | **int32_t** | A kiírandó érték |

## Console::WriteLine(int64_t) metódus

Kiírja a 64 bites egész szám érték karakteres ábrázolását, majd az aktuális sortörést a szabványos kimeneti folyamra.

```cpp
static void System::Console::WriteLine(int64_t value)
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | **int64_t** | A kiírandó érték |

## Console::WriteLine(const String\&) metódus

Kiírja a megadott karakterlánc objektumot, majd az aktuális sortörést a szabványos kimeneti folyamra.

```cpp
static void System::Console::WriteLine(const String &value)
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | const [String](../../string/)\& | A kiírandó karakterlánc objektum |

## Console::WriteLine(const char_t *) metódus

Kiírja a megadott C-karakterláncot, majd az aktuális sortörést a szabványos kimeneti folyamra.

```cpp
static void System::Console::WriteLine(const char_t *value)
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | const char_t * | A kiírandó C-karakterlánc |

## Console::WriteLine(const TypeInfo\&) metódus

Kiírja a [TypeInfo](../../typeinfo/) érték karakteres ábrázolását, majd az aktuális sortörést a szabványos kimeneti folyamra.

```cpp
static void System::Console::WriteLine(const TypeInfo &value)
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | A kiírandó érték |

## Console::WriteLine(uint32_t) metódus

Kiírja a 32 bites előjel nélküli egész szám érték karakteres ábrázolását, majd az aktuális sortörést a szabványos kimeneti folyamra.

```cpp
static void System::Console::WriteLine(uint32_t value)
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | **uint32_t** | A kiírandó érték |

## Console::WriteLine(uint64_t) metódus

Kiírja a 64 bites előjel nélküli egész szám érték karakteres ábrázolását, majd az aktuális sortörést a szabványos kimeneti folyamra.

```cpp
static void System::Console::WriteLine(uint64_t value)
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | **uint64_t** | A kiírandó érték |

## Console::WriteLine(const ArrayPtr\<char_t\>\&, int, int) metódus

Kiírja a megadott karaktertömb meghatározott tartományának karakteres ábrázolását, majd az aktuális sortörést a szabványos kimeneti folyamra.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer, int index, int count)
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | A karaktertömb |
| index | int | Az index a tömbben, ahol a kiírandó tartomány kezdődik |
| count | int | A kiírandó tartomány elemeinek száma |

## Console::WriteLine(const Exception\&) metódus

Kiírja a megadott Exception objektum karakteres ábrázolását, majd az aktuális sortörést a szabványos kimeneti folyamra.

```cpp
static void System::Console::WriteLine(const Exception &e)
```

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| e | const [Exception](../../exception/)\& | A kiírandó érték |

## Console::WriteLine(const String\&, Args\&&...) metódus

Kiírja a megadott argumentumok a megadott formátum szerint formázott karakteres ábrázolását, majd az aktuális sortörést a szabványos kimeneti folyamra.

```cpp
template<class...> static void System::Console::WriteLine(const String &format, Args &&... args)
```

### Sablonparaméterek

| Paraméter | Leírás |
| --- | --- |
| The | A kiírandó értékek típusai |

### Argumentumok

| Paraméter | Típus | Leírás |
| --- | --- | --- |
| format | const [String](../../string/)\& | A karakterlánc formátum |
| args | Args\&&... | A kiírandó értékek |

## Console::WriteLine(const char *) metódus




```cpp
static void System::Console::WriteLine(const char *)=delete
```

## Lásd még

* Típusdefiníció [SharedPtr](../../sharedptr/)
* Típusdefiníció [ArrayPtr](../../arrayptr/)
* Típusdefiníció [Exception](../../exception/)
* Osztály [Console](../)
* Osztály [Decimal](../../decimal/)
* Osztály [String](../../string/)
* Osztály [TypeInfo](../../typeinfo/)
* Névtér [System](../../)
* Könyvtár [Aspose.Slides](../../../)