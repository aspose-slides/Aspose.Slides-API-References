---
title: "System::Text"
second_title: Aspose.Slides para C++ Referência da API
description: 
type: docs
weight: 976
url: /pt/system.text/
---
## Classes

| Classe | Descrição |
| --- | --- |
| [ASCIIEncoding](./asciiencoding/) | Representa a codificação ASCII. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [Decoder](./decoder/) | Encapsula a sequência de bytes de decodificação em sequência de caracteres. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [DecoderExceptionFallback](./decoderexceptionfallback/) | Fornece estratégia de fallback que lança exceções. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [DecoderExceptionFallbackBuffer](./decoderexceptionfallbackbuffer/) | [Buffer](../system/buffer/) para estratégia de fallback de decodificação que lança exceções. Não armazena nada na prática, mas lança exceções. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [DecoderFallback](./decoderfallback/) | Fornece API de fallback para lidar com erro de decodificação. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [DecoderFallbackBuffer](./decoderfallbackbuffer/) | Fornece buffer para implementação de fallback. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [DecoderReplacementFallback](./decoderreplacementfallback/) | Fornece estratégia de fallback que substitui símbolo errado por um stub. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [DecoderReplacementFallbackBuffer](./decoderreplacementfallbackbuffer/) | [Buffer](../system/buffer/) para estratégia de fallback de substituição de decodificação. |
| [Details_DecoderFallbackException](./details_decoderfallbackexception/) | Exceção lançada por [DecoderExceptionFallback](./decoderexceptionfallback/) quando a decodificação falha. Nunca crie instâncias desta classe manualmente. Use a classe DecoderFallbackException em vez disso. Nunca encapsule as instâncias da classe DecoderFallbackException em [System::SmartPtr](../system/smartptr/). |
| [Details_EncoderFallbackException](./details_encoderfallbackexception/) | Exceção lançada por [EncoderExceptionFallback](./encoderexceptionfallback/) quando a codificação falha. Nunca crie instâncias desta classe manualmente. Use a classe EncoderFallbackException em vez disso. Nunca encapsule as instâncias da classe EncoderFallbackException em [System::SmartPtr](../system/smartptr/). |
| [Encoder](./encoder/) | Encapsula a sequência de caracteres de codificação em sequência de bytes. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [EncoderExceptionFallback](./encoderexceptionfallback/) | Fornece estratégia de fallback que lança exceções. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [EncoderExceptionFallbackBuffer](./encoderexceptionfallbackbuffer/) | [Buffer](../system/buffer/) para estratégia de fallback de codificação que lança exceções. Não armazena nada na prática, mas lança exceções. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [EncoderFallback](./encoderfallback/) | Fornece API de fallback para lidar com erro de codificação. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [EncoderFallbackBuffer](./encoderfallbackbuffer/) | Fornece buffer para implementação de fallback. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [EncoderReplacementFallback](./encoderreplacementfallback/) | Fornece estratégia de fallback que substitui símbolo errado por um stub. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [EncoderReplacementFallbackBuffer](./encoderreplacementfallbackbuffer/) | [Buffer](../system/buffer/) para estratégia de fallback de substituição de codificação. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [Encoding](./encoding/) | [Encoding](./encoding/) serviços. |
| [EncodingDecoder](./encodingdecoder/) | [Decoder](./decoder/) que usa objeto de codificação para decodificação. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [EncodingEncoder](./encodingencoder/) | [Encoder](./encoder/) que usa objeto de codificação para codificação. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [EncodingInfo](./encodinginfo/) | Informação breve sobre codificação. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [ICUDecoder](./icudecoder/) | [Decoder](./decoder/) que usa ICU para decodificação. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [ICUEncoder](./icuencoder/) | [Encoder](./encoder/) que usa ICU para codificação. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [ICUEncoding](./icuencoding/) | Implementação de codificação baseada em ICU. FOR INTERNAL USE. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [Latin1Encoding](./latin1encoding/) | Suporte a codificação Latin1. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [StringBuilder](./stringbuilder/) | [Buffer](../system/buffer/) para acumular string parte a parte. Este tipo pode ser alocado tanto na pilha como tipo valor quanto no heap usando a função [System::MakeObject()](../system/makeobject/). Uma vez que o objeto está alocado, nunca misture esses dois casos de uso: ter ponteiros [SmartPtr](../system/smartptr/) para objetos alocados na pilha é estritamente proibido. |
| [UnicodeEncoding](./unicodeencoding/) | Codificação Unicode. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [UTF32Encoding](./utf32encoding/) | Codificação UTF-32. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [UTF7Encoding](./utf7encoding/) | Codificação UTF-7. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [UTF8Encoding](./utf8encoding/) | Codificação UTF-8. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |

## Enumerações

| Enumeração | Descrição |
| --- | --- |
| [NormalizationForm](./normalizationform/) | Define como normalizar uma string Unicode. |

## Definições de Tipo

| Definição de Tipo | Descrição |
| --- | --- |
| [DecoderFallbackException](./decoderfallbackexception/) |  |
| [EncoderFallbackException](./encoderfallbackexception/) |  |
| [ICUEncodingPtr](./icuencodingptr/) | Ponteiro de codificação ICU. |