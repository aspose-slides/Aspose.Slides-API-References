---
title: EqualsExact()
second_title: Referência da API Aspose.Slides para C++
description: Verifica se dois objetos DateTimeOffset representam o mesmo ponto no tempo e têm o mesmo deslocamento.
type: docs
weight: 378
url: /pt/system/datetimeoffset/equalsexact/
---
## DateTimeOffset::EqualsExact(const DateTimeOffset\&) const método

Verifica se dois objetos [DateTimeOffset](../) representam o mesmo ponto no tempo e têm o mesmo deslocamento.

```cpp
bool System::DateTimeOffset::EqualsExact(const DateTimeOffset &other) const
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/) para comparar. |

### Valor de Retorno

true se o objeto atual for exatamente igual a **other**, caso contrário - false.

## DateTimeOffset::EqualsExact(const SharedPtr\<Object\>\&) const método

Verifica se dois objetos [DateTimeOffset](../) representam o mesmo ponto no tempo e têm o mesmo deslocamento.

```cpp
bool System::DateTimeOffset::EqualsExact(const SharedPtr<Object> &obj) const
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/) para comparar. |

### Valor de Retorno

true se o objeto atual for exatamente igual a **obj**, caso contrário - false.

## Veja Também

* Typedef [SharedPtr](../../sharedptr/)
* Classe [DateTimeOffset](../)
* Classe [Object](../../object/)
* Namespace [System](../../)
* Biblioteca [Aspose.Slides](../../../)