---
title: Equals()
second_title: Aspose.Slides para C++ Referência da API
description: Verifica se dois objetos DateTimeOffset representam o mesmo ponto no tempo.
type: docs
weight: 365
url: /pt/system/datetimeoffset/equals/
---
## DateTimeOffset::Equals(const DateTimeOffset\&) const método

Checks if two [DateTimeOffset](../) objects represents the same time point.

```cpp
bool System::DateTimeOffset::Equals(const DateTimeOffset &other) const
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/) para comparar. |

### Valor de Retorno

true se o objeto atual for igual a **other**, caso contrário - false.

## DateTimeOffset::Equals(const SharedPtr\<Object\>\&) const método

Checks if two [DateTimeOffset](../) objects represents the same time point.

```cpp
bool System::DateTimeOffset::Equals(const SharedPtr<Object> &obj) const
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/) para comparar. |

### Valor de Retorno

true se o objeto atual for igual a **obj**, caso contrário - false.

## DateTimeOffset::Equals(const DateTimeOffset\&, const DateTimeOffset\&) método

Checks if two [DateTimeOffset](../) objects represent the same time point.

```cpp
static bool System::DateTimeOffset::Equals(const DateTimeOffset &first, const DateTimeOffset &second)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| first | const [DateTimeOffset](../)\& | Primeiro objeto para comparar. |
| second | const [DateTimeOffset](../)\& | Segundo objeto para comparar. |

### Valor de Retorno

true se **first** for igual a **second**, caso contrário - false.

## Veja Também

* Typedef [SharedPtr](../../sharedptr/)
* Classe [DateTimeOffset](../)
* Classe [Object](../../object/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)