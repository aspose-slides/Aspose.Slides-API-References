---
title: CompareTo()
second_title: Referência da API Aspose.Slides para C++
description: Compara dois objetos DateTimeOffset.
type: docs
weight: 352
url: /pt/system/datetimeoffset/compareto/
---
## DateTimeOffset::CompareTo(const DateTimeOffset\&) const método

Compara dois [DateTimeOffset](../) objetos.

```cpp
int System::DateTimeOffset::CompareTo(const DateTimeOffset &other) const
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/) para comparar. |

### Valor de Retorno

- 1 - se o objeto atual for anterior a **other**, 1 - se o objeto atual for posterior a **other**, 0 - se o objeto atual for igual a **other**.

## DateTimeOffset::CompareTo(const SharedPtr\<Object\>\&) const método

Compara dois [DateTimeOffset](../) objetos.

```cpp
int System::DateTimeOffset::CompareTo(const SharedPtr<Object> &obj) const
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/) para comparar. |

### Valor de Retorno

- 1 - se o objeto atual for anterior a **obj**, 1 - se o objeto atual for posterior a **obj**, 0 - se o objeto atual for igual a **obj**.

## Ver Também

* Typedef [SharedPtr](../../sharedptr/)
* Classe [DateTimeOffset](../)
* Classe [Object](../../object/)
* Namespace [System](../../)
* Biblioteca [Aspose.Slides](../../../)