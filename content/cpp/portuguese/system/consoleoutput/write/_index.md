---
title: Write()
second_title: Referência da API Aspose.Slides para C++
description: Produz a representação em string do valor bool especificado no fluxo de saída representado pelo objeto atual.
type: docs
weight: 14
url: /pt/system/consoleoutput/write/
---
## ConsoleOutput::Write(bool) method

Produz a representação em string do valor bool especificado no fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::Write(bool value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **bool** | O valor a ser escrito |

## ConsoleOutput::Write(const SharedPtr\<Object\>\&) method

Produz a representação em string do objeto especificado no fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::Write(const SharedPtr<Object> &value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | O objeto a ser escrito |

## ConsoleOutput::Write(char_t) method

Produz o valor de caractere especificado no fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::Write(char_t value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | char_t | O valor a ser escrito |

## ConsoleOutput::Write(Decimal) method

Produz a representação em string do valor [Decimal](../../decimal/) no fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::Write(Decimal value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | O valor a ser escrito |

## ConsoleOutput::Write(double) method

Produz a representação em string de valor em ponto flutuante de precisão dupla no fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::Write(double value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **double** | O valor a ser escrito |

## ConsoleOutput::Write(int32_t) method

Produz a representação em string de valor inteiro de 32 bits no fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::Write(int32_t value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **int32_t** | O valor a ser escrito |

## ConsoleOutput::Write(int64_t) method

Produz a representação em string de valor inteiro de 64 bits no fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::Write(int64_t value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **int64_t** | O valor a ser escrito |

## ConsoleOutput::Write(float) method

Produz a representação em string de valor em ponto flutuante de precisão simples no fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::Write(float value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **float** | O valor a ser escrito |

## ConsoleOutput::Write(const String\&) method

Produz o objeto string especificado no fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::Write(const String &value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | const [String](../../string/)\& | O objeto string a ser escrito |

## ConsoleOutput::Write(uint32_t) method

Produz a representação em string de valor inteiro sem sinal de 32 bits no fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::Write(uint32_t value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **uint32_t** | O valor a ser escrito |

## ConsoleOutput::Write(uint64_t) method

Produz a representação em string de valor inteiro sem sinal de 64 bits no fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::Write(uint64_t value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **uint64_t** | O valor a ser escrito |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&) method

Produz a representação em string do array de caracteres especificado no fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | O array a ser escrito |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&, int32_t, int32_t) method

Produz a representação em string de um intervalo de valores do array de caracteres especificado no fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | O array contendo os valores a serem escritos |
| index | **int32_t** | O índice onde começa o intervalo de elementos a ser escrito |
| count | **int32_t** | O número de elementos no intervalo a ser escrito |

## ConsoleOutput::Write(const char_t *) method

Produz a c-string especificada no fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::Write(const char_t *value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | const char_t * | A c-string a ser escrita |

## ConsoleOutput::Write(const TypeInfo\&) method

Produz a representação em string do objeto [TypeInfo](../../typeinfo/) especificado no fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::Write(const TypeInfo &value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | O objeto [TypeInfo](../../typeinfo/) a ser escrito |

## ConsoleOutput::Write(const char *) method




```cpp
void System::ConsoleOutput::Write(const char *)=delete
```

## Ver Também

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Classe [ConsoleOutput](../)
* Classe [Object](../../object/)
* Classe [Decimal](../../decimal/)
* Classe [String](../../string/)
* Classe [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Biblioteca [Aspose.Slides](../../../)