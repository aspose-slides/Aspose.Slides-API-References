---
title: WriteLine()
second_title: Referência da API Aspose.Slides para C++
description: Emite o terminador de linha atual para o fluxo de saída representado pelo objeto atual.
type: docs
weight: 27
url: /pt/system/consoleoutput/writeline/
---
## ConsoleOutput::WriteLine() método

Emite o terminador de linha atual para o fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::WriteLine() override
```

## ConsoleOutput::WriteLine(const SharedPtr\<Object\>\&) método

Emite a representação em string do objeto especificado seguida do terminador de linha atual para o fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::WriteLine(const SharedPtr<Object> &value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | O objeto a ser emitido |

## ConsoleOutput::WriteLine(bool) método

Emite a representação em string do valor booleano especificado seguida do terminador de linha atual para o fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::WriteLine(bool value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **bool** | O objeto a ser emitido |

## ConsoleOutput::WriteLine(char_t) método

Emite o valor de caractere especificado seguido do terminador de linha atual para o fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::WriteLine(char_t value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | char_t | O valor a ser emitido |

## ConsoleOutput::WriteLine(Decimal) método

Emite a representação em string do valor [Decimal](../../decimal/) seguida do terminador de linha atual para o fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::WriteLine(Decimal value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | O valor a ser emitido |

## ConsoleOutput::WriteLine(double) método

Emite a representação em string de valor de ponto flutuante de precisão dupla seguida do terminador de linha atual para o fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::WriteLine(double value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **double** | O valor a ser emitido |

## ConsoleOutput::WriteLine(int) método

Emite a representação em string de valor inteiro de 32 bits seguida do terminador de linha atual para o fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::WriteLine(int value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | int | O valor a ser emitido |

## ConsoleOutput::WriteLine(int64_t) método

Emite a representação em string de valor inteiro de 64 bits seguida do terminador de linha atual para o fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::WriteLine(int64_t value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **int64_t** | O valor a ser emitido |

## ConsoleOutput::WriteLine(float) método

Emite a representação em string de valor de ponto flutuante de precisão simples seguida do terminador de linha atual para o fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::WriteLine(float value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **float** | O valor a ser emitido |

## ConsoleOutput::WriteLine(const String\&) método

Emite o objeto string especificado seguido do terminador de linha atual para o fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::WriteLine(const String &value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | const [String](../../string/)\& | O objeto string a ser emitido |

## ConsoleOutput::WriteLine(uint32_t) método

Emite a representação em string de valor inteiro sem sinal de 32 bits seguida do terminador de linha atual para o fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::WriteLine(uint32_t value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **uint32_t** | O valor a ser emitido |

## ConsoleOutput::WriteLine(uint64_t) método

Emite a representação em string de valor inteiro sem sinal de 64 bits seguida do terminador de linha atual para o fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::WriteLine(uint64_t value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **uint64_t** | O valor a ser emitido |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&) método

Emite a representação em string do array de caracteres especificado seguido do terminador de linha atual para o fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | O array a ser emitido |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&, int32_t, int32_t) método

Emite a representação em string de um intervalo de valores do array de caracteres especificado seguido do terminador de linha atual para o fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | O array contendo os valores a serem emitidos |
| index | **int32_t** | O índice onde o intervalo de elementos a ser emitido começa |
| count | **int32_t** | O número de elementos no intervalo de elementos a ser emitido |

## ConsoleOutput::WriteLine(const char_t *) método

Emite a c-string especificada seguida do terminador de linha atual para o fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::WriteLine(const char_t *value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | const char_t * | A c-string a ser emitida |

## ConsoleOutput::WriteLine(const TypeInfo\&) método

Emite a representação em string do objeto [TypeInfo](../../typeinfo/) especificado seguida do terminador de linha atual para o fluxo de saída representado pelo objeto atual.

```cpp
void System::ConsoleOutput::WriteLine(const TypeInfo &value) override
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | O objeto [TypeInfo](../../typeinfo/) a ser emitido |

## ConsoleOutput::WriteLine(const char *) método

```cpp
void System::ConsoleOutput::WriteLine(const char *)=delete
```

## Ver também

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Class [ConsoleOutput](../)
* Class [Object](../../object/)
* Class [Decimal](../../decimal/)
* Class [String](../../string/)
* Class [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)