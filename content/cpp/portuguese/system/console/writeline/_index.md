---
title: WriteLine()
second_title: Referência da API Aspose.Slides para C++
description: Produz o terminador de linha atual para o fluxo de saída padrão.
type: docs
weight: 14
url: /pt/system/console/writeline/
---
## Console::WriteLine() método

Produz o terminador de linha atual para o fluxo de saída padrão.

```cpp
static void System::Console::WriteLine()
```

## Console::WriteLine(const SharedPtr\<T\>\&) método

Produz a representação em string do objeto especificado seguida do terminador de linha atual para o fluxo de saída padrão.

```cpp
template<class T> static void System::Console::WriteLine(const SharedPtr<T> &object)
```

### Parâmetros do modelo

| Parâmetro | Descrição |
| --- | --- |
| T | Tipo do objeto a ser impresso |

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| object | const [SharedPtr](../../sharedptr/)\<T\>\& | [Object](../../object/) para saída |

## Console::WriteLine(bool) método

Produz a representação em string do valor bool seguida do terminador de linha atual para o fluxo de saída padrão.

```cpp
static void System::Console::WriteLine(bool value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **bool** | O valor para saída |

## Console::WriteLine(char_t) método

Produz o valor de caractere especificado seguido do terminador de linha atual para o fluxo de saída padrão.

```cpp
static void System::Console::WriteLine(char_t value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | char_t | O valor para saída |

## Console::WriteLine(const ArrayPtr\<char_t\>\&) método

Produz a representação em string do array de caracteres especificado seguida do terminador de linha atual para o fluxo de saída padrão.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | O array para saída |

## Console::WriteLine(const Decimal\&) método

Produz a representação em string do valor [Decimal](../../decimal/) seguida do terminador de linha atual para o fluxo de saída padrão.

```cpp
static void System::Console::WriteLine(const Decimal &value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | O valor para saída |

## Console::WriteLine(double) método

Produz a representação em string do valor de ponto flutuante de precisão dupla seguida do terminador de linha atual para o fluxo de saída padrão.

```cpp
static void System::Console::WriteLine(double value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **double** | O valor para saída |

## Console::WriteLine(float) método

Produz a representação em string do valor de ponto flutuante de precisão simples seguida do terminador de linha atual para o fluxo de saída padrão.

```cpp
static void System::Console::WriteLine(float value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **float** | O valor para saída |

## Console::WriteLine(int32_t) método

Produz a representação em string do valor inteiro de 32 bits seguida do terminador de linha atual para o fluxo de saída padrão.

```cpp
static void System::Console::WriteLine(int32_t value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **int32_t** | O valor para saída |

## Console::WriteLine(int64_t) método

Produz a representação em string do valor inteiro de 64 bits seguida do terminador de linha atual para o fluxo de saída padrão.

```cpp
static void System::Console::WriteLine(int64_t value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **int64_t** | O valor para saída |

## Console::WriteLine(const String\&) método

Produz o objeto string especificado seguido do terminador de linha atual para o fluxo de saída padrão.

```cpp
static void System::Console::WriteLine(const String &value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | const [String](../../string/)\& | O objeto string para saída |

## Console::WriteLine(const char_t *) método

Produz a c-string especificada seguida do terminador de linha atual para o fluxo de saída padrão.

```cpp
static void System::Console::WriteLine(const char_t *value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | const char_t * | A c-string para saída |

## Console::WriteLine(const TypeInfo\&) método

Produz a representação em string do valor [TypeInfo](../../typeinfo/) seguida do terminador de linha atual para o fluxo de saída padrão.

```cpp
static void System::Console::WriteLine(const TypeInfo &value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | O valor para saída |

## Console::WriteLine(uint32_t) método

Produz a representação em string do valor inteiro sem sinal de 32 bits seguida do terminador de linha atual para o fluxo de saída padrão.

```cpp
static void System::Console::WriteLine(uint32_t value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **uint32_t** | O valor para saída |

## Console::WriteLine(uint64_t) método

Produz a representação em string do valor inteiro sem sinal de 64 bits seguida do terminador de linha atual para o fluxo de saída padrão.

```cpp
static void System::Console::WriteLine(uint64_t value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **uint64_t** | O valor para saída |

## Console::WriteLine(const ArrayPtr\<char_t\>\&, int, int) método

Produz a representação em string do intervalo especificado do array de caracteres fornecido, seguido do terminador de linha atual para o fluxo de saída padrão.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer, int index, int count)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | O array de caracteres |
| index | int | O índice no array onde o intervalo a ser impresso começa |
| count | int | O número de elementos no intervalo a ser impresso |

## Console::WriteLine(const Exception\&) método

Produz a representação em string do objeto Exception especificado seguida do terminador de linha atual para o fluxo de saída padrão.

```cpp
static void System::Console::WriteLine(const Exception &e)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| e | const [Exception](../../exception/)\& | O valor para saída |

## Console::WriteLine(const String\&, Args\&&...) método

Produz a representação em string dos argumentos especificados formatados de acordo com o formato fornecido, seguida do terminador de linha atual para o fluxo de saída padrão.

```cpp
template<class...> static void System::Console::WriteLine(const String &format, Args &&... args)
```

### Parâmetros do modelo

| Parâmetro | Descrição |
| --- | --- |
| The | tipos dos valores a serem impressos |

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| format | const [String](../../string/)\& | O formato da string |
| args | Args\&&... | Os valores a serem impressos |

## Console::WriteLine(const char *) método




```cpp
static void System::Console::WriteLine(const char *)=delete
```

## Veja também

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Typedef [Exception](../../exception/)
* Class [Console](../)
* Class [Decimal](../../decimal/)
* Class [String](../../string/)
* Class [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)