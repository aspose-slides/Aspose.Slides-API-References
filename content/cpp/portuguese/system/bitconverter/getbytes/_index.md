---
title: GetBytes()
second_title: Referência da API Aspose.Slides para C++
description: Converte o valor booleano especificado em um array de bytes.
type: docs
weight: 14
url: /pt/system/bitconverter/getbytes/
---
## BitConverter::GetBytes(bool) método

Converte o valor booleano especificado em um array de bytes.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(bool value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **bool** | Um valor booleano a ser convertido |

### Valor de Retorno

Array de 1 byte representando o valor especificado

## BitConverter::GetBytes(char_t) método

Converte o valor char_t especificado em um array de bytes.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(char_t value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | char_t | Um valor char_t a ser convertido |

### Valor de Retorno

Array de 2 bytes representando o valor especificado

## BitConverter::GetBytes(int16_t) método

Converte o valor inteiro de 16 bits especificado em um array de bytes.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int16_t value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **int16_t** | Um valor inteiro de 16 bits a ser convertido |

### Valor de Retorno

Array de 2 bytes representando o valor especificado

## BitConverter::GetBytes(int) método

Converte o valor inteiro de 32 bits especificado em um array de bytes.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | int | Um valor inteiro de 32 bits a ser convertido |

### Valor de Retorno

Array de 4 bytes representando o valor especificado

## BitConverter::GetBytes(int64_t) método

Converte o valor inteiro de 64 bits especificado em um array de bytes.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int64_t value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **int64_t** | Um valor inteiro de 64 bits a ser convertido |

### Valor de Retorno

Array de 8 bytes representando o valor especificado

## BitConverter::GetBytes(uint16_t) método

Converte o valor inteiro sem sinal de 16 bits especificado em um array de bytes.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint16_t value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **uint16_t** | Um valor inteiro sem sinal de 16 bits a ser convertido |

### Valor de Retorno

Array de 2 bytes representando o valor especificado

## BitConverter::GetBytes(uint32_t) método

Converte o valor inteiro sem sinal de 32 bits especificado em um array de bytes.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint32_t value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **uint32_t** | Um valor inteiro sem sinal de 32 bits a ser convertido |

### Valor de Retorno

Array de 4 bytes representando o valor especificado

## BitConverter::GetBytes(uint64_t) método

Converte o valor inteiro sem sinal de 64 bits especificado em um array de bytes.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint64_t value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **uint64_t** | Um valor inteiro sem sinal de 64 bits a ser convertido |

### Valor de Retorno

Array de 8 bytes representando o valor especificado

## BitConverter::GetBytes(float) método

Converte o valor de ponto flutuante de precisão simples especificado em um array de bytes.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(float value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **float** | Um valor de ponto flutuante a ser convertido |

### Valor de Retorno

Array de 4 bytes representando o valor especificado

## BitConverter::GetBytes(double) método

Converte o valor de ponto flutuante de dupla precisão especificado em um array de bytes.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(double value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **double** | Um valor de ponto flutuante de dupla precisão a ser convertido |

### Valor de Retorno

Array de 8 bytes representando o valor especificado

## Veja Também

* Typedef [ArrayPtr](../../arrayptr/)
* Classe [BitConverter](../)
* Namespace [System](../../)
* Biblioteca [Aspose.Slides](../../../)