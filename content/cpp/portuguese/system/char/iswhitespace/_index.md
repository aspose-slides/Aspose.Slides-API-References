---
title: IsWhiteSpace()
second_title: Referência da API Aspose.Slides para C++
description: Determina se o caractere no índice especificado no buffer de caracteres especificado é classificado como um caractere de espaço em branco.
type: docs
weight: 157
url: /pt/system/char/iswhitespace/
---
## Char::IsWhiteSpace(const char_t *, int) método


Determina se o caractere no índice especificado no buffer de caracteres especificado é classificado como um caractere de espaço em branco.

```cpp
static bool System::Char::IsWhiteSpace(const char_t *str, int idx)
```


### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| str | const char_t * | Ponteiro para o início do buffer de caracteres |
| idx | int | Um índice baseado em zero no buffer especificado do caractere a ser testado |

### Valor de Retorno

True se o caractere no índice especificado for um caractere de espaço em branco, caso contrário - false

## Char::IsWhiteSpace(char_t) método


Determina se o caractere especificado é classificado como um caractere de espaço em branco.

```cpp
static bool System::Char::IsWhiteSpace(char_t c)
```


### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| c | char_t | O caractere a ser testado |

### Valor de Retorno

True se o caractere especificado for um caractere de espaço em branco, caso contrário - false

## Char::IsWhiteSpace(const String\&, int) método


Determina se o caractere no índice especificado na string especificada é classificado como um caractere de espaço em branco.

```cpp
static bool System::Char::IsWhiteSpace(const String &str, int index)
```


### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| str | const [String](../../string/)\& | Uma string |
| index | int | Um índice baseado em zero na string especificada |

### Valor de Retorno

True se o caractere no índice especificado for um caractere de espaço em branco, caso contrário - false

## Ver Também

* Classe [Char](../)
* Classe [String](../../string/)
* Namespace [System](../../)
* Biblioteca [Aspose.Slides](../../../)