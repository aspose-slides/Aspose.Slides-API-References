---
title: IsLower()
second_title: Aspose.Slides para C++ Referência da API
description: Determina se o caractere no índice especificado no buffer de caracteres especificado é classificado como uma letra minúscula.
type: docs
weight: 196
url: /pt/system/char/islower/
---
## Char::IsLower(const char_t *, int) método

Determina se o caractere no índice especificado no buffer de caracteres especificado é classificado como uma letra minúscula.

```cpp
static bool System::Char::IsLower(const char_t *str, int idx)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| str | const char_t * | Ponteiro para o início do buffer de caracteres |
| idx | int | Um índice baseado em zero no buffer especificado do caractere a ser testado |

### Valor de Retorno

True se o caractere no índice especificado for uma letra minúscula, caso contrário - false

## Char::IsLower(char_t) método

Determina se o caractere especificado é classificado como uma letra minúscula.

```cpp
static bool System::Char::IsLower(char_t c)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| c | char_t | O caractere a ser testado |

### Valor de Retorno

True se o caractere especificado for uma letra minúscula, caso contrário - false

## Char::IsLower(const String\&, int) método

Determina se o caractere no índice especificado na string especificada é classificado como uma letra minúscula.

```cpp
static bool System::Char::IsLower(const String &str, int idx)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| str | const [String](../../string/)\& | Uma string |
| idx | int | Um índice baseado em zero na string especificada a ser testado |

### Valor de Retorno

True se o caractere no índice especificado for uma letra minúscula, caso contrário - false

## Veja Também

* Classe [Char](../)
* Classe [String](../../string/)
* Namespace [System](../../)
* Biblioteca [Aspose.Slides](../../../)