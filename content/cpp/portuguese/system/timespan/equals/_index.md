---
title: Equals()
second_title: Aspose.Slides for C++ Referência da API
description: Determina se o intervalo de tempo representado pelo objeto atual é igual ao intervalo de tempo representado pelo objeto especificado.
type: docs
weight: 40
url: /pt/system/timespan/equals/
---
## TimeSpan::Equals(TimeSpan) const method

Determina se o intervalo de tempo representado pelo objeto atual é igual ao intervalo de tempo representado pelo objeto especificado.

```cpp
constexpr bool System::TimeSpan::Equals(TimeSpan value) const
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | [TimeSpan](../) | O objeto [TimeSpan](../) para comparar com o objeto atual |

### Valor de Retorno

true se o objeto atual e o objeto especificado representarem o mesmo intervalo de tempo, caso contrário - false

## TimeSpan::Equals(const SharedPtr\<Object\>\&) const method

Determina se o intervalo de tempo representado pelo objeto atual é igual ao intervalo de tempo representado pelo objeto especificado.

```cpp
bool System::TimeSpan::Equals(const SharedPtr<Object> &obj) const
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | O objeto [TimeSpan](../) para comparar com o objeto atual |

### Valor de Retorno

true se o objeto atual e o objeto especificado representarem o mesmo intervalo de tempo, caso contrário - false

## TimeSpan::Equals(TimeSpan, TimeSpan) method

Retorna true se os objetos especificados representarem o mesmo intervalo de tempo, caso contrário - false.

```cpp
static constexpr bool System::TimeSpan::Equals(TimeSpan a, TimeSpan b)
```

## Veja Também

* Typedef [SharedPtr](../../sharedptr/)
* Class [TimeSpan](../)
* Class [Object](../../object/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)