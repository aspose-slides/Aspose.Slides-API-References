---
title: "System::Net::Http::Headers"
second_title: Referência da API Aspose.Slides para C++
description: 
type: docs
weight: 690
url: /pt/system.net.http.headers/
---
## Classes

| Classe | Descrição |
| --- | --- |
| [AuthenticationHeaderValue](./authenticationheadervalue/) | Representa valores dos cabeçalhos 'Authorization', 'ProxyAuthorization', 'WWW-Authenticate' e 'Proxy-Authenticate'. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [CacheControlHeaderValue](./cachecontrolheadervalue/) | Representa um valor do cabeçalho 'Cache-Control'. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [ContentDispositionHeaderValue](./contentdispositionheadervalue/) | Representa um valor do cabeçalho 'Content-Disposition'. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [ContentRangeHeaderValue](./contentrangeheadervalue/) | Representa um valor do cabeçalho 'Content-Range'. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [EntityTagHeaderValue](./entitytagheadervalue/) | Representa um valor do cabeçalho 'Entity-Tag'. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [HttpContentHeaders](./httpcontentheaders/) | Representa a coleção dos cabeçalhos 'Content'. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [HttpHeaders](./httpheaders/) | A coleção dos cabeçalhos HTTP. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [HttpHeaderValueCollection](./httpheadervaluecollection/) | Representa a coleção dos valores dos cabeçalhos. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [HttpHeaderValueCollection< System::String >](./httpheadervaluecollection_tmpl_system_string__end_tmpl/) | A especialização parcial do modelo [HttpHeaderValueCollection](./httpheadervaluecollection/) para o tipo [String](../system/string/). Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [HttpRequestHeaders](./httprequestheaders/) | Representa a coleção dos cabeçalhos 'Request'. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [HttpResponseHeaders](./httpresponseheaders/) | Representa a coleção dos cabeçalhos 'Response'. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [MediaTypeHeaderValue](./mediatypeheadervalue/) | Representa um tipo MIME no valor do cabeçalho 'Content-Type'. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [MediaTypeWithQualityHeaderValue](./mediatypewithqualityheadervalue/) | Representa um tipo MIME com um fator de qualidade adicional no valor do cabeçalho 'Content-Type'. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [NameValueHeaderValue](./namevalueheadervalue/) | Representa um par chave/valor a ser usado nos cabeçalhos. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [NameValueWithParametersHeaderValue](./namevaluewithparametersheadervalue/) | Representa um par chave/valor com parâmetros a ser usado nos cabeçalhos. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [ObjectCollection](./objectcollection/) | Representa a coleção dos objetos. |
| [ProductHeaderValue](./productheadervalue/) | Representa um token de produto no valor do cabeçalho 'User-Agent'. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [ProductInfoHeaderValue](./productinfoheadervalue/) | Representa um produto ou um comentário no valor do cabeçalho 'User-Agent'. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [RangeConditionHeaderValue](./rangeconditionheadervalue/) | Representa um valor do cabeçalho 'If-Range'. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [RangeHeaderValue](./rangeheadervalue/) | Representa um valor do cabeçalho '[Range](../system/range/)'. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [RangeItemHeaderValue](./rangeitemheadervalue/) | Representa um intervalo de bytes no valor do cabeçalho '[Range](../system/range/)'. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [RetryConditionHeaderValue](./retryconditionheadervalue/) | Representa um valor do cabeçalho 'Retry-After'. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [StringWithQualityHeaderValue](./stringwithqualityheadervalue/) | Representa um valor com qualidade adicional de um cabeçalho string. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [TransferCodingHeaderValue](./transfercodingheadervalue/) | Representa um valor do cabeçalho 'Accept-Encoding'. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [TransferCodingWithQualityHeaderValue](./transfercodingwithqualityheadervalue/) | Representa um valor com qualidade adicional do cabeçalho 'Accept-Encoding'. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [ViaHeaderValue](./viaheadervalue/) | Representa um valor do cabeçalho 'Via'. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
| [WarningHeaderValue](./warningheadervalue/) | Representa um valor do cabeçalho 'Warning'. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo às funções como argumento. |
## Definições de tipo

| Definição de tipo | Descrição |
| --- | --- |
| [HeaderFunc](./headerfunc/) |  |