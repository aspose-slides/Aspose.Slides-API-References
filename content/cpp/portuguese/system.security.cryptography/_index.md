---
title: "System::Security::Cryptography"
second_title: Referência da API Aspose.Slides para C++
description: 
type: docs
weight: 833
url: /pt/system.security.cryptography/
---
## Classes

| Classe | Descrição |
| --- | --- |
| [AsnEncodedData](./asnencodeddata/) | Dados codificados em ASN.1. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [AsymmetricAlgorithm](./asymmetricalgorithm/) | Classe base abstrata para algoritmos de criptografia assimétrica. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [AsymmetricSignatureDeformatter](./asymmetricsignaturedeformatter/) | Classe base para deformatadores de assinatura assimétrica. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [AsymmetricSignatureFormatter](./asymmetricsignatureformatter/) | Classe base para formatadores de assinatura assimétrica. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [CryptoStream](./cryptostream/) | Implementação de fluxo que envolve um fluxo existente com uma função criptográfica. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [CspKeyContainerInfo](./cspkeycontainerinfo/) | Informação adicional sobre um par de chaves criptográficas. |
| [CspParameters](./cspparameters/) | Parâmetros do Provedor de Serviços Criptográficos. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [DeriveBytes](./derivebytes/) | Classe abstrata da qual todas as classes que derivam sequências de bytes de comprimento especificado herdam. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [Details_CryptographicException](./details_cryptographicexception/) |  |
| [Details_CryptographicUnexpectedOperationException](./details_cryptographicunexpectedoperationexception/) |  |
| [DSA](./dsa/) | Classe base para implementações do algoritmo [DSA](./dsa/). Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [DSACryptoServiceProvider](./dsacryptoserviceprovider/) | Algoritmo [DSA](./dsa/) em forma CSP. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [DSASignatureDeformatter](./dsasignaturedeformatter/) | Usado para verificar as assinaturas [DSA](./dsa/). Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [DSASignatureFormatter](./dsasignatureformatter/) | Usado para criar uma assinatura [DSA](./dsa/). Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [ECDsa](./ecdsa/) | Classe base para implementações do algoritmo [ECDsa](./ecdsa/). Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [ECDsaBotan](./ecdsabotan/) | Algoritmo [ECDsa](./ecdsa/) na forma Botan. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [FromBase64Transform](./frombase64transform/) | Converte a instância da classe [CryptoStream](./cryptostream/) de base 64. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [HashAlgorithm](./hashalgorithm/) | Classe base para algoritmos de hash. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [HMAC](./hmac/) | Todas as implementações de Código de Mensagem Baseado em Hash [Authentication](../system.security.authentication/) ([HMAC](./hmac/)) devem herdar desta classe abstrata. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [HMACSHA1](./hmacsha1/) | Código de Mensagem Baseado em Hash [Authentication](../system.security.authentication/) que usa a função de hash [SHA1](./sha1/). Não implementado. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [HMACSHA512](./hmacsha512/) | Código de Mensagem Baseado em Hash [Authentication](../system.security.authentication/) que usa a função de hash [SHA512](./sha512/). Implementado parcialmente. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [ICryptoTransform](./icryptotransform/) | Classe base de transformador criptográfico. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [ICspAsymmetricAlgorithm](./icspasymmetricalgorithm/) | Classe base de algoritmo assimétrico. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [KeySizes](./keysizes/) | Conjunto de tamanhos de chave aceitos por algoritmos simétricos. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [MD5](./md5/) | Algoritmo de hash [MD5](./md5/). Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [MD5CryptoServiceProvider](./md5cryptoserviceprovider/) | Algoritmo [MD5](./md5/) compatível com CSP. Não implementado. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [Oid](./oid/) | Identificador de objeto criptográfico. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [RandomNumberGenerator](./randomnumbergenerator/) | Classe abstrata da qual geradores de números aleatórios devem herdar. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [RC2](./rc2/) | Classe base de algoritmo simétrico [RC2](./rc2/). Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [RC2Managed](./rc2managed/) | Algoritmo gerenciado [RC2](./rc2/). Apenas os modos de cifra ECB, CFB e CBC são suportados. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [Rfc2898DeriveBytes](./rfc2898derivebytes/) | Implementa derivação de chave baseada em senha, PBKDF2. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [Rijndael](./rijndael/) | Classe base para o algoritmo [Rijndael](./rijndael/). Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [RijndaelManaged](./rijndaelmanaged/) | Algoritmo gerenciado [Rijndael](./rijndael/). Suporta apenas os modos ECB e CFB com preenchimento None e o modo CBC com preenchimentos None e Zeros. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [RIPEMD160](./ripemd160/) | Calcula o hash [RIPEMD160](./ripemd160/) para dados de entrada. Em C++ [RIPEMD160](./ripemd160/) não é uma classe abstrata. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instância deste tipo na pilha ou usando operator new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-la a funções como argumento. |
| [RNGCryptoServiceProvider](./rngcryptoserviceprovider/) | Gerador de números aleatórios que segue a noção CSP. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias desse tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre envolva esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [RSA](./rsa/) | Classe base para implementações do algoritmo [RSA](./rsa/). Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias desse tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre envolva esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [RSACryptoServiceProvider](./rsacryptoserviceprovider/) | Algoritmo [RSA](./rsa/) na forma CSP. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias desse tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre envolva esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [RSAEncryptionPadding](./rsaencryptionpadding/) | Modo de preenchimento e parâmetros para [RSA](./rsa/) criptografia ou descriptografia. |
| [RSAPKCS1SignatureDeformatter](./rsapkcs1signaturedeformatter/) | Classe para verificar a assinatura PKCS #1 v1.5 [RSA](./rsa/). Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias desse tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre envolva esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [RSAPKCS1SignatureFormatter](./rsapkcs1signatureformatter/) | Assina dados com uma assinatura PKCS #1 v1.5 [RSA](./rsa/). Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias desse tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre envolva esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [RSASignaturePadding](./rsasignaturepadding/) | Modo de preenchimento e parâmetros para operações de criação ou verificação de assinatura [RSA](./rsa/). |
| [SHA1](./sha1/) | Calcula o hash [SHA1](./sha1/) para dados de entrada. Em C++ [SHA1](./sha1/) não é uma classe abstrata. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias desse tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre envolva esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [SHA1CryptoServiceProvider](./sha1cryptoserviceprovider/) | [SHA1](./sha1/) implementação CSP. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias desse tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre envolva esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [SHA1Managed](./sha1managed/) | [SHA1](./sha1/) hasher gerenciado. Não implementado. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias desse tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre envolva esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [SHA256](./sha256/) | Calcula o hash [SHA256](./sha256/) para dados de entrada. Em C++ [SHA256](./sha256/) não é uma classe abstrata. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias desse tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre envolva esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [SHA256Managed](./sha256managed/) | [SHA256](./sha256/) hasher gerenciado. Não implementado. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias desse tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre envolva esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [SHA384](./sha384/) | Calcula o hash [SHA384](./sha384/) para dados de entrada. Em C++ [SHA384](./sha384/) não é uma classe abstrata. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias desse tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre envolva esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [SHA384Managed](./sha384managed/) | [SHA384](./sha384/) hasher gerenciado. Não implementado. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias desse tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre envolva esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [SHA512](./sha512/) | Calcula o hash [SHA512](./sha512/) para dados de entrada. Em C++ [SHA512](./sha512/) não é uma classe abstrata. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias desse tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre envolva esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [SHA512Managed](./sha512managed/) | [SHA384](./sha384/) hasher gerenciado. Não implementado. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias desse tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre envolva esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [SymmetricAlgorithm](./symmetricalgorithm/) | Algoritmo simétrico que usa a mesma chave para criptografia e descriptografia, classe base. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias desse tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre envolva esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [ToBase64Transform](./tobase64transform/) | Converte a instância da classe [CryptoStream](./cryptostream/) para base 64. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias desse tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre envolva esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [TripleDES](./tripledes/) | Classe base do algoritmo Triple [Data](../system.data/) Encryption Standard. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias desse tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre envolva esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [TripleDESManaged](./tripledesmanaged/) | [TripleDES](./tripledes/) implementação gerenciada. Suporta apenas os modos ECB e CFB com preenchimento None e o modo CBC com preenchimentos None, Zeros e PKCS7. Objetos desta classe devem ser alocados apenas usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias desse tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre envolva esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |

## Estruturas

| Estrutura | Descrição |
| --- | --- |
| [DSAParameters](./dsaparameters/) | [Data](../system.data/) estrutura de [DSA](./dsa/) parâmetros do algoritmo. Este tipo deve ser alocado na pilha e passado a funções por valor ou por referência. Nunca use a classe [System::SmartPtr](../system/smartptr/) para gerenciar objetos deste tipo. |
| [ECCurve](./eccurve/) | Uma curva elíptica. |
| [ECParameters](./ecparameters/) | Parâmetros de chave pública e privada de uma curva elíptica. |
| [ECPoint](./ecpoint/) | Ponto em uma curva elíptica. |
| [HashAlgorithmName](./hashalgorithmname/) | [String](../system/string/) representando o nome de um algoritmo de hash. Este tipo deve ser alocado na pilha e passado a funções por valor ou por referência. Nunca use a classe [System::SmartPtr](../system/smartptr/) para gerenciar objetos deste tipo. |
| [RSAParameters](./rsaparameters/) | [Data](../system.data/) estrutura de [RSA](./rsa/) parâmetros do algoritmo. Este tipo deve ser alocado na pilha e passado a funções por valor ou por referência. Nunca use a classe [System::SmartPtr](../system/smartptr/) para gerenciar objetos deste tipo. |

## Funções

| Função | Descrição |
| --- | --- |
| constexpr **bool** [operator==](./operator_equal_equal/)(std::nullptr_t, const [HashAlgorithmName](./hashalgorithmname/)\&) |  |
| **bool** [operator==](./operator_equal_equal/)(const [SharedPtr](../system/sharedptr/)\<[RSAEncryptionPadding](./rsaencryptionpadding/)\>\&, const [SharedPtr](../system/sharedptr/)\<[RSAEncryptionPadding](./rsaencryptionpadding/)\>\&) |  |
| **bool** [operator==](./operator_equal_equal/)(const [SharedPtr](../system/sharedptr/)\<[RSASignaturePadding](./rsasignaturepadding/)\>\&, const [SharedPtr](../system/sharedptr/)\<[RSASignaturePadding](./rsasignaturepadding/)\>\&) |  |
| constexpr **bool** [operator!=](./operator_not_equal/)(std::nullptr_t, const [HashAlgorithmName](./hashalgorithmname/)\&) |  |
| **bool** [operator!=](./operator_not_equal/)(const [SharedPtr](../system/sharedptr/)\<[RSAEncryptionPadding](./rsaencryptionpadding/)\>\&, const [SharedPtr](../system/sharedptr/)\<[RSAEncryptionPadding](./rsaencryptionpadding/)\>\&) |  |
| **bool** [operator!=](./operator_not_equal/)(const [SharedPtr](../system/sharedptr/)\<[RSASignaturePadding](./rsasignaturepadding/)\>\&, const [SharedPtr](../system/sharedptr/)\<[RSASignaturePadding](./rsasignaturepadding/)\>\&) |  |
| constexpr **bool** [operator<](./operator_less/)(std::nullptr_t, const [HashAlgorithmName](./hashalgorithmname/)\&) |  |
| constexpr **bool** [operator<=](./operator_less_equal/)(std::nullptr_t, const [HashAlgorithmName](./hashalgorithmname/)\&) |  |
| constexpr **bool** [operator>](./operator_greater/)(std::nullptr_t, const [HashAlgorithmName](./hashalgorithmname/)\&) |  |
| constexpr **bool** [operator>=](./operator_greater_equal/)(std::nullptr_t, const [HashAlgorithmName](./hashalgorithmname/)\&) |  |
| std::ostream\& [operator<<](./operator_less_less/)(std::ostream\&, const [HashAlgorithmName](./hashalgorithmname/)\&) | Insere dados no fluxo usando codificação UTF-8. |
| std::wostream\& [operator<<](./operator_less_less/)(std::wostream\&, const [HashAlgorithmName](./hashalgorithmname/)\&) | Insere dados no fluxo. |

## Enumeradores

| Enum | Descrição |
| --- | --- |
| [FromBase64TransformMode](./frombase64transformmode/) | Especifica se espaços em branco devem ser ignorados na transformação base 64. |
| [CipherMode](./ciphermode/) | Modo de cifra de bloco. |
| [CryptoStreamMode](./cryptostreammode/) | Direção [CryptoStream](./cryptostream/). |
| [ECKeyXmlFormat](./eckeyxmlformat/) | Formato XML para chaves de curvas elípticas. |
| [KeyNumber](./keynumber/) | Tipo de chave. |
| [OidGroup](./oidgroup/) | Identifica grupos OID [Windows](../system.windows/). |
| [PaddingMode](./paddingmode/) | Define como tratar mensagens que são mais curtas que o bloco exigido pelas operações criptográficas. |
| [RSAEncryptionPaddingMode](./rsaencryptionpaddingmode/) | Modo de preenchimento usado com operações de criptografia ou descriptografia [RSA](./rsa/). |
| [RSASignaturePaddingMode](./rsasignaturepaddingmode/) | Modo de preenchimento usado com operações de criação ou verificação de assinatura [RSA](./rsa/). |

## Definições de Tipo

| Typedef | Descrição |
| --- | --- |
| [CryptographicException](./cryptographicexception/) |  |
| [CryptographicUnexpectedOperationException](./cryptographicunexpectedoperationexception/) |  |