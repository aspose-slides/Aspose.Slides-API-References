---
title: AddLine()
second_title: Referência da API Aspose.Slides para C++
description: Adiciona a linha especificada ao caminho representado pelo objeto atual.
type: docs
weight: 144
url: /pt/system.drawing.drawing2d/graphicspath/addline/
---
## GraphicsPath::AddLine(const Point\&, const Point\&) método

Adiciona a linha especificada ao caminho representado pelo objeto atual.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddLine(const Point &pt1, const Point &pt2)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| pt1 | const [Point](../../../system.drawing/point/)\& | O ponto inicial da linha a ser adicionada |
| pt2 | const [Point](../../../system.drawing/point/)\& | O ponto final da linha a ser adicionada |

## GraphicsPath::AddLine(const PointF\&, const PointF\&) método

Adiciona a linha especificada ao caminho representado pelo objeto atual.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddLine(const PointF &pt1, const PointF &pt2)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| pt1 | const [PointF](../../../system.drawing/pointf/)\& | O ponto inicial da linha a ser adicionada |
| pt2 | const [PointF](../../../system.drawing/pointf/)\& | O ponto final da linha a ser adicionada |

## GraphicsPath::AddLine(int, int, int, int) método

Adiciona a linha especificada ao caminho representado pelo objeto atual.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddLine(int x1, int y1, int x2, int y2)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| x1 | int | A coordenada X do ponto inicial da linha a ser adicionada |
| y1 | int | A coordenada Y do ponto inicial da linha a ser adicionada |
| x2 | int | A coordenada X do ponto final da linha a ser adicionada |
| y2 | int | A coordenada Y do ponto final da linha a ser adicionada |

## GraphicsPath::AddLine(float, float, float, float) método

Adiciona a linha especificada ao caminho representado pelo objeto atual.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddLine(float x1, float y1, float x2, float y2)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| x1 | **float** | A coordenada X do ponto inicial da linha a ser adicionada |
| y1 | **float** | A coordenada Y do ponto inicial da linha a ser adicionada |
| x2 | **float** | A coordenada X do ponto final da linha a ser adicionada |
| y2 | **float** | A coordenada Y do ponto final da linha a ser adicionada |

## Veja Também

* Classe [Point](../../../system.drawing/point/)
* Classe [GraphicsPath](../)
* Classe [PointF](../../../system.drawing/pointf/)
* Espaço de nomes [System::Drawing::Drawing2D](../../)
* Biblioteca [Aspose.Slides](../../../)