---
title: DrawLine()
second_title: Referência da API Aspose.Slides for C++
description: Desenha a linha especificada usando a caneta especificada.
type: docs
weight: 456
url: /pt/system.drawing/graphics/drawline/
---
## Graphics::DrawLine(const SharedPtr\<Pen\>\&, Point, Point) método

Desenha a linha especificada usando a caneta especificada.

```cpp
void System::Drawing::Graphics::DrawLine(const SharedPtr<Pen> &pen, Point pt1, Point pt2)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| pen | const [SharedPtr](../../../system/sharedptr/)\<[Pen](../../pen/)\>\& | Um objeto [Pen](../../pen/) que especifica as propriedades gráficas da linha a ser desenhada |
| pt1 | [Point](../../point/) | O primeiro ponto que define a linha a ser desenhada |
| pt2 | [Point](../../point/) | O segundo ponto que define a linha a ser desenhada |

## Graphics::DrawLine(const SharedPtr\<Pen\>\&, PointF, PointF) método

Desenha a linha especificada usando a caneta especificada.

```cpp
void System::Drawing::Graphics::DrawLine(const SharedPtr<Pen> &pen, PointF pt1, PointF pt2)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| pen | const [SharedPtr](../../../system/sharedptr/)\<[Pen](../../pen/)\>\& | Um objeto [Pen](../../pen/) que especifica as propriedades gráficas da linha a ser desenhada |
| pt1 | [PointF](../../pointf/) | O primeiro ponto que define a linha a ser desenhada |
| pt2 | [PointF](../../pointf/) | O segundo ponto que define a linha a ser desenhada |

## Graphics::DrawLine(const SharedPtr\<Pen\>\&, int, int, int, int) método

Desenha a linha especificada usando a caneta especificada.

```cpp
void System::Drawing::Graphics::DrawLine(const SharedPtr<Pen> &pen, int x1, int y1, int x2, int y2)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| pen | const [SharedPtr](../../../system/sharedptr/)\<[Pen](../../pen/)\>\& | Um objeto [Pen](../../pen/) que especifica as propriedades gráficas da linha a ser desenhada |
| x1 | int | A coordenada X do primeiro ponto que define a linha a ser desenhada |
| y1 | int | A coordenada Y do primeiro ponto que define a linha a ser desenhada |
| x2 | int | A coordenada X do segundo ponto que define a linha a ser desenhada |
| y2 | int | A coordenada Y do segundo ponto que define a linha a ser desenhada |

## Graphics::DrawLine(const SharedPtr\<Pen\>\&, float, float, float, float) método

Desenha a linha especificada usando a caneta especificada.

```cpp
void System::Drawing::Graphics::DrawLine(const SharedPtr<Pen> &pen, float x1, float y1, float x2, float y2)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| pen | const [SharedPtr](../../../system/sharedptr/)\<[Pen](../../pen/)\>\& | Um objeto [Pen](../../pen/) que especifica as propriedades gráficas da linha a ser desenhada |
| x1 | **float** | A coordenada X do primeiro ponto que define a linha a ser desenhada |
| y1 | **float** | A coordenada Y do primeiro ponto que define a linha a ser desenhada |
| x2 | **float** | A coordenada X do segundo ponto que define a linha a ser desenhada |
| y2 | **float** | A coordenada Y do segundo ponto que define a linha a ser desenhada |

## Veja Também

* Typedef [SharedPtr](../../../system/sharedptr/)
* Classe [Pen](../../pen/)
* Classe [Point](../../point/)
* Classe [Graphics](../)
* Classe [PointF](../../pointf/)
* Namespace [System::Drawing](../../)
* Biblioteca [Aspose.Slides](../../../)