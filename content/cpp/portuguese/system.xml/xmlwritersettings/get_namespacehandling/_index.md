---
title: get_NamespaceHandling()
second_title: Aspose.Slides para C++ Referência da API
description: Retorna um valor que indica se o XmlWriter deve remover declarações de namespace duplicadas ao gravar conteúdo XML. O comportamento padrão é que o gravador emita todas as declarações de namespace que estão presentes no resolvedor de namespace do gravador.
type: docs
weight: 261
url: /pt/system.xml/xmlwritersettings/get_namespacehandling/
---
## XmlWriterSettings::get_NamespaceHandling() método

Retorna um valor que indica se o [XmlWriter](../../xmlwriter/) deve remover declarações de namespace duplicadas ao gravar conteúdo XML. O comportamento padrão é que o gravador emita todas as declarações de namespace que estão presentes no resolvedor de namespaces do gravador.

```cpp
System::Xml::NamespaceHandling System::Xml::XmlWriterSettings::get_NamespaceHandling()
```

### Valor de Retorno

A enumeração NamespaceHandling usada para especificar se deve remover declarações de namespace duplicadas no [XmlWriter](../../xmlwriter/).

## Veja Também

* Enum [NamespaceHandling](../../namespacehandling/)
* Classe [XmlWriterSettings](../)
* Espaço de nomes [System::Xml](../../)
* Library [Aspose.Slides](../../../)