---
title: WriteLine()
second_title: Referência da API Aspose.Slides para C++
description: Grava caracteres terminadores de linha no fluxo.
type: docs
weight: 118
url: /pt/system.io/textwriter/writeline/
---
## TextWriter::WriteLine() método

Grava caracteres terminadores de linha no fluxo.

```cpp
virtual void System::IO::TextWriter::WriteLine()
```

## TextWriter::WriteLine(const SharedPtr\<Object\>\&) método

Grava a representação em cadeia de caracteres do objeto especificado seguida pelos caracteres terminadores de linha no fluxo.

```cpp
virtual void System::IO::TextWriter::WriteLine(const SharedPtr<Object> &value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | const [SharedPtr](../../../system/sharedptr/)\<[Object](../../../system/object/)\>\& | O objeto a ser gravado |

## TextWriter::WriteLine(bool) método

Grava a representação em cadeia de caracteres do valor booleano especificado seguida pelos caracteres terminadores de linha no fluxo.

```cpp
virtual void System::IO::TextWriter::WriteLine(bool value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **bool** | O valor a ser gravado |

## TextWriter::WriteLine(char_t) método

Grava o caractere especificado seguido pelos caracteres terminadores de linha no fluxo.

```cpp
virtual void System::IO::TextWriter::WriteLine(char_t value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | char_t | O valor a ser gravado |

## TextWriter::WriteLine(Decimal) método

Grava a representação em cadeia de caracteres do objeto [Decimal](../../../system/decimal/) especificado seguida pelos caracteres terminadores de linha no fluxo.

```cpp
virtual void System::IO::TextWriter::WriteLine(Decimal value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | [Decimal](../../../system/decimal/) | O objeto a ser gravado |

## TextWriter::WriteLine(double) método

Grava a representação em cadeia de caracteres do valor de ponto flutuante de precisão dupla especificado seguida pelos caracteres terminadores de linha no fluxo.

```cpp
virtual void System::IO::TextWriter::WriteLine(double value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **double** | O valor a ser gravado |

## TextWriter::WriteLine(int) método

Grava a representação em cadeia de caracteres do valor inteiro de 32 bits especificado seguida pelos caracteres terminadores de linha no fluxo.

```cpp
virtual void System::IO::TextWriter::WriteLine(int value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | int | O valor a ser gravado |

## TextWriter::WriteLine(int64_t) método

Grava a representação em cadeia de caracteres do valor inteiro de 64 bits especificado seguida pelos caracteres terminadores de linha no fluxo.

```cpp
virtual void System::IO::TextWriter::WriteLine(int64_t value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **int64_t** | O valor a ser gravado |

## TextWriter::WriteLine(float) método

Grava a representação em cadeia de caracteres do valor de ponto flutuante de precisão simples especificado seguida pelos caracteres terminadores de linha no fluxo.

```cpp
virtual void System::IO::TextWriter::WriteLine(float value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **float** | O valor a ser gravado |

## TextWriter::WriteLine(const String\&) método

Grava a cadeia de caracteres especificada seguida pelos caracteres terminadores de linha no fluxo.

```cpp
virtual void System::IO::TextWriter::WriteLine(const String &value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | const [String](../../../system/string/)\& | A cadeia de caracteres a ser gravada |

## TextWriter::WriteLine(uint32_t) método

Grava a representação em cadeia de caracteres do valor inteiro sem sinal de 32 bits especificado seguida pelos caracteres terminadores de linha no fluxo.

```cpp
virtual void System::IO::TextWriter::WriteLine(uint32_t value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **uint32_t** | O valor a ser gravado |

## TextWriter::WriteLine(uint64_t) método

Grava a representação em cadeia de caracteres do valor inteiro sem sinal de 64 bits especificado seguida pelos caracteres terminadores de linha no fluxo.

```cpp
virtual void System::IO::TextWriter::WriteLine(uint64_t value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | **uint64_t** | O valor a ser gravado |

## TextWriter::WriteLine(const ArrayPtr\<char_t\>\&) método

Grava todos os caracteres do array especificado seguido pelos caracteres terminadores de linha no fluxo.

```cpp
virtual void System::IO::TextWriter::WriteLine(const ArrayPtr<char_t> &buffer)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../../system/arrayptr/)\<char_t\>\& | O array contendo os caracteres a serem gravados |

## TextWriter::WriteLine(const ArrayPtr\<char_t\>\&, int32_t, int32_t) método

Grava o subintervalo especificado de caracteres UTF-16 do array de caracteres especificado seguido pelos caracteres terminadores de linha no fluxo.

```cpp
virtual void System::IO::TextWriter::WriteLine(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../../system/arrayptr/)\<char_t\>\& | O array contendo os caracteres a serem gravados |
| index | **int32_t** | Um índice baseado em zero do **buffer** no qual o subintervalo a ser gravado começa |
| count | **int32_t** | O número de caracteres no subintervalo a ser gravado; -1 especifica que o subintervalo termina onde o array **buffer** termina |

## TextWriter::WriteLine(const char_t *) método

Grava a c-string especificada seguida pelos caracteres terminadores de linha no fluxo.

```cpp
virtual void System::IO::TextWriter::WriteLine(const char_t *value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | const char_t * | A c-string a ser gravada |

## TextWriter::WriteLine(const TypeInfo\&) método

Grava a representação em cadeia de caracteres do objeto [TypeInfo](../../../system/typeinfo/) especificado seguida pelos caracteres terminadores de linha no fluxo.

```cpp
virtual void System::IO::TextWriter::WriteLine(const TypeInfo &value)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | const [TypeInfo](../../../system/typeinfo/)\& | O objeto a ser gravado |

## TextWriter::WriteLine(const String\&, const TArgs\&...) método

Grava os valores especificados formatados de acordo com o formato especificado seguido pelos caracteres terminadores de linha no fluxo.

```cpp
template<class...> void System::IO::TextWriter::WriteLine(const String &format, const TArgs &... args)
```

### Parâmetros de modelo

| Parâmetro | Descrição |
| --- | --- |
| TArgs | A lista de tipos dos valores a serem gravados |

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| format | const [String](../../../system/string/)\& | O formato da cadeia de caracteres |
| args | const TArgs\&... | Os valores a serem gravados |

## Veja Também

* Typedef [SharedPtr](../../../system/sharedptr/)
* Typedef [ArrayPtr](../../../system/arrayptr/)
* Classe [TextWriter](../)
* Classe [Object](../../../system/object/)
* Classe [Decimal](../../../system/decimal/)
* Classe [String](../../../system/string/)
* Classe [TypeInfo](../../../system/typeinfo/)
* Espaço de nomes [System::IO](../../)
* Library [Aspose.Slides](../../../)