---
title: HostToNetworkOrder()
second_title: Referência da API Aspose.Slides para C++
description: Converte a ordem de bytes do host especificada para a ordem de bytes de rede correspondente.
type: docs
weight: 248
url: /pt/system.net/ipaddress/hosttonetworkorder/
---
## IPAddress::HostToNetworkOrder(int64_t) método

Converte a ordem de bytes do host especificada para a ordem de bytes de rede correspondente.

```cpp
static int64_t System::Net::IPAddress::HostToNetworkOrder(int64_t host)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| host | **int64_t** | O valor a ser convertido. |

### Valor de retorno

A ordem de bytes de rede correspondente.

## IPAddress::HostToNetworkOrder(int32_t) método

Converte a ordem de bytes do host especificada para a ordem de bytes de rede correspondente.

```cpp
static int32_t System::Net::IPAddress::HostToNetworkOrder(int32_t host)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| host | **int32_t** | O valor a ser convertido. |

### Valor de retorno

A ordem de bytes de rede correspondente.

## IPAddress::HostToNetworkOrder(int16_t) método

Converte a ordem de bytes do host especificada para a ordem de bytes de rede correspondente.

```cpp
static int16_t System::Net::IPAddress::HostToNetworkOrder(int16_t host)
```

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| host | **int16_t** | O valor a ser convertido. |

### Valor de retorno

A ordem de bytes de rede correspondente.

## Veja também

* Classe [IPAddress](../)
* Namespace [System::Net](../../)
* Biblioteca [Aspose.Slides](../../../)