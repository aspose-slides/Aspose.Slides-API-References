---
title: "System::Net"
second_title: Referência da API Aspose.Slides para C++
description: 
type: docs
weight: 651
url: /pt/system.net/
---
## Classes

| Classe | Descrição |
| --- | --- |
| [Cookie](./cookie/) | Representa um cookie HTTP. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [CookieCollection](./cookiecollection/) | Representa uma lista de cookies ordenados. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [CookieComparer](./cookiecomparer/) | Usado para comparar as instâncias da classe [Cookie](./cookie/). Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [CookieContainer](./cookiecontainer/) | Fornece um contêiner para as instâncias da classe CookieCollection. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [CookieParser](./cookieparser/) | Usado para analisar um cabeçalho de cookie e criar uma instância da classe [Cookie](./cookie/). Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [CredentialCache](./credentialcache/) | Fornece o armazenamento de credenciais. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [Details_CookieException](./details_cookieexception/) | Representa uma exceção lançada quando o tamanho de [CookieContainer](./cookiecontainer/) é maior que o valor da propriedade MaxCookieSize. Nunca crie instâncias desta classe manualmente. Use a classe CookieException em vez disso. Nunca encapsule as instâncias da classe CookieException em [System::SmartPtr](../system/smartptr/). |
| [Details_WebException](./details_webexception/) | Representa a exceção lançada por [WebRequest](./webrequest/) quando ocorre um erro. Nunca crie instâncias desta classe manualmente. Use a classe WebException em vez disso. Nunca encapsule as instâncias da classe WebException em [System::SmartPtr](../system/smartptr/). |
| [Dns](./dns/) | Fornece métodos para trabalhar com DNS. |
| [DnsEndPoint](./dnsendpoint/) | Contém informações usadas pela aplicação para conectar ao serviço. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [EndPoint](./endpoint/) | A classe abstrata contém um endereço de rede. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [FileWebRequest](./filewebrequest/) | Fornece implementação da classe abstrata [WebRequest](./webrequest/) para trabalhar com o sistema de arquivos. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [FileWebResponse](./filewebresponse/) | Fornece implementação da classe abstrata [WebResponse](./webresponse/) para trabalhar com o sistema de arquivos. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [FtpWebRequest](./ftpwebrequest/) | Implementa um cliente File Transfer Protocol (FTP). Classe fictícia que permite vincular o código traduzido com referências [FtpWebRequest](./ftpwebrequest/), mas não o executa. Não contém membros implementados corretamente. |
| [FtpWebResponse](./ftpwebresponse/) | Classe fictícia que permite vincular o código traduzido com referências [FtpWebResponse](./ftpwebresponse/), mas não o executa. Não contém membros implementados corretamente. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [HeaderVariantInfo](./headervariantinfo/) | Usado para corresponder o nome do cookie e a especificação. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [HttpKnownHeaderNames](./httpknownheadernames/) | Contém os valores de string dos nomes de cabeçalhos conhecidos. |
| [HttpRequestHeaderExtensions](./httprequestheaderextensions/) | Contém métodos utilitários para trabalhar com os valores da enumeração HttpRequestHeader. |
| [HttpResponseHeaderExtensions](./httpresponseheaderextensions/) | Contém métodos utilitários para trabalhar com os valores da enumeração HttpResponseHeader. |
| [HttpStatusDescription](./httpstatusdescription/) | Contém os métodos utilitários para obter a representação em string do status HTTP. |
| [HttpVersion](./httpversion/) | Contém versões do HTTP. |
| [HttpWebRequest](./httpwebrequest/) | Representa a requisição web HTTP. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [HttpWebResponse](./httpwebresponse/) | Representa a resposta web HTTP. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [ICredentials](./icredentials/) | Fornece a interface de autenticação. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [ICredentialsByHost](./icredentialsbyhost/) | Fornece a interface de autenticação para recuperar credenciais de um host, porta e tipo de autenticação. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [IPAddress](./ipaddress/) | Representa o endereço IP. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [IPEndPoint](./ipendpoint/) | Representa um ponto de extremidade de rede que contém um endereço IP e uma porta. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [IPHostEntry](./iphostentry/) | Representa informações sobre um endereço de host da Internet. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [IWebProxy](./iwebproxy/) | Esta interface usada para implementação de acesso proxy à classe [WebRequest](./webrequest/). Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [IWebRequestCreate](./iwebrequestcreate/) | Fornece os métodos para criar instâncias da classe [WebRequest](./webrequest/). Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [NetworkCredential](./networkcredential/) | Fornece credenciais para os esquemas de autenticação baseados em senha. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [PathList](./pathlist/) | Representa a lista das instâncias da classe [CookieCollection](./cookiecollection/). Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [ServicePoint](./servicepoint/) | Fornece gerenciamento de conexões HTTP. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [ServicePointManager](./servicepointmanager/) | Gerencia as fases do ciclo de vida (criação, manutenção e exclusão) das instâncias da classe [ServicePoint](./servicepoint/). Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [SocketAddress](./socketaddress/) | Usado para armazenar informações serializadas sobre as instâncias da classe [EndPoint](./endpoint/). Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [UriScheme](./urischeme/) | Representa o esquema para URI. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [WebClient](./webclient/) | [WebClient](./webclient/) fornece métodos comuns para enviar e receber dados. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [WebHeaderCollection](./webheadercollection/) | Representa a coleção dos cabeçalhos do protocolo. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [WebProxy](./webproxy/) | Representa um servidor proxy web http. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [WebRequest](./webrequest/) | Representa uma requisição web. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |
| [WebRequestMethods](./webrequestmethods/) | Representa as constantes de string das requisições web. |
| [WebResponse](./webresponse/) | Representa uma resposta web. Objetos desta classe devem ser alocados somente usando a função [System::MakeObject()](../system/makeobject/). Nunca crie instâncias deste tipo na pilha ou usando o operador new, pois isso resultará em erros de tempo de execução e/ou falhas de asserção. Sempre encapsule esta classe em um ponteiro [System::SmartPtr](../system/smartptr/) e use esse ponteiro para passá-lo a funções como argumento. |

## Enums

| Enum | Descrição |
| --- | --- |
| [AuthenticationSchemes](./authenticationschemes/) | Enumera os esquemas de autenticação. |
| [CookieVariant](./cookievariant/) | Enumera as especificações de cookie. |
| [DecompressionMethods](./decompressionmethods/) | Enumera o algoritmo de compressão-descompressão. |
| [HttpRequestHeader](./httprequestheader/) | Enumera os cabeçalhos de requisição. |
| [HttpResponseHeader](./httpresponseheader/) | Enumera os cabeçalhos de resposta http. |
| [HttpStatusCode](./httpstatuscode/) |  |
| [SecurityProtocolType](./securityprotocoltype/) | Enumera os tipos de protocolo de segurança. |
| [TransportType](./transporttype/) | Define o tipo de transporte permitido para o socket. |
| [WebExceptionStatus](./webexceptionstatus/) | Enumera os códigos de status da classe WebException. |
| [WebHeaderCollectionType](./webheadercollectiontype/) | Enumera os tipos da coleção de cabeçalhos de protocolo. |

## Typedefs

| Typedef | Descrição |
| --- | --- |
| [CookieException](./cookieexception/) |  |
| [BindIPEndPoint](./bindipendpoint/) |  |
| [WebException](./webexception/) |  |
