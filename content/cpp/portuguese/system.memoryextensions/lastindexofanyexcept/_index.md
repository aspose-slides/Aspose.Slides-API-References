---
title: LastIndexOfAnyExcept()
second_title: Referência da API Aspose.Slides para C++
description: Encontra a última ocorrência de qualquer elemento, exceto três valores especificados, dentro de um span.
type: docs
weight: 235
url: /pt/system.memoryextensions/lastindexofanyexcept/
---
## System::MemoryExtensions::LastIndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const T\&, const T\&, const T\&) função


Encontra a última ocorrência de qualquer elemento, exceto três valores especificados, dentro de um span.

```cpp
template<typename T> int32_t System::MemoryExtensions::LastIndexOfAnyExcept(const ReadOnlySpan<T> &span, const T &value0, const T &value1, const T &value2)
```


### Parâmetros de modelo

| Parameter | Description |
| --- | --- |
| T | O tipo de elementos no span |

### Argumentos

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | O span a ser pesquisado |
| value0 | const T\& | O primeiro valor a excluir |
| value1 | const T\& | O segundo valor a excluir |
| value2 | const T\& | O terceiro valor a excluir |

### Valor de retorno

O índice baseado em zero do último elemento não excluído, ou -1 se não encontrado

## System::MemoryExtensions::LastIndexOfAnyExcept(const Span\<T\>\&, const T\&, const T\&, const T\&) função


Encontra a última ocorrência de qualquer elemento, exceto três valores especificados, dentro de um span mutável.

```cpp
template<typename T> int32_t System::MemoryExtensions::LastIndexOfAnyExcept(const Span<T> &span, const T &value0, const T &value1, const T &value2)
```


### Parâmetros de modelo

| Parameter | Description |
| --- | --- |
| T | O tipo de elementos no span |

### Argumentos

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | O span a ser pesquisado |
| value0 | const T\& | O primeiro valor a excluir |
| value1 | const T\& | O segundo valor a excluir |
| value2 | const T\& | O terceiro valor a excluir |

### Valor de retorno

O índice baseado em zero do último elemento não excluído, ou -1 se não encontrado

## System::MemoryExtensions::LastIndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const T\&, const T\&) função


Encontra a última ocorrência de qualquer elemento, exceto dois valores especificados, dentro de um span.

```cpp
template<typename T> int32_t System::MemoryExtensions::LastIndexOfAnyExcept(const ReadOnlySpan<T> &span, const T &value0, const T &value1)
```


### Parâmetros de modelo

| Parameter | Description |
| --- | --- |
| T | O tipo de elementos no span |

### Argumentos

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | O span a ser pesquisado |
| value0 | const T\& | O primeiro valor a excluir |
| value1 | const T\& | O segundo valor a excluir |

### Valor de retorno

O índice baseado em zero do último elemento não excluído, ou -1 se não encontrado

## System::MemoryExtensions::LastIndexOfAnyExcept(const Span\<T\>\&, const T\&, const T\&) função


Encontra a última ocorrência de qualquer elemento, exceto dois valores especificados, dentro de um span mutável.

```cpp
template<typename T> int32_t System::MemoryExtensions::LastIndexOfAnyExcept(const Span<T> &span, const T &value0, const T &value1)
```


### Parâmetros de modelo

| Parameter | Description |
| --- | --- |
| T | O tipo de elementos no span |

### Argumentos

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | O span a ser pesquisado |
| value0 | const T\& | O primeiro valor a excluir |
| value1 | const T\& | O segundo valor a excluir |

### Valor de retorno

O índice baseado em zero do último elemento não excluído, ou -1 se não encontrado

## System::MemoryExtensions::LastIndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const T\&) função


Encontra a última ocorrência de qualquer elemento, exceto um valor especificado, dentro de um span.

```cpp
template<typename T> int32_t System::MemoryExtensions::LastIndexOfAnyExcept(const ReadOnlySpan<T> &span, const T &value)
```


### Parâmetros de modelo

| Parameter | Description |
| --- | --- |
| T | O tipo de elementos no span |

### Argumentos

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | O span a ser pesquisado |
| value | const T\& | O valor a excluir |

### Valor de retorno

O índice baseado em zero do último elemento não excluído, ou -1 se não encontrado

## System::MemoryExtensions::LastIndexOfAnyExcept(const Span\<T\>\&, const T\&) função


Encontra a última ocorrência de qualquer elemento, exceto um valor especificado, dentro de um span mutável.

```cpp
template<typename T> int32_t System::MemoryExtensions::LastIndexOfAnyExcept(const Span<T> &span, const T &value)
```


### Parâmetros de modelo

| Parameter | Description |
| --- | --- |
| T | O tipo de elementos no span |

### Argumentos

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | O span a ser pesquisado |
| value | const T\& | O valor a excluir |

### Valor de retorno

O índice baseado em zero do último elemento não excluído, ou -1 se não encontrado

## System::MemoryExtensions::LastIndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const ReadOnlySpan\<T\>\&) função


Encontra a última ocorrência de qualquer elemento, exceto valores de uma sequência, dentro de um span.

```cpp
template<typename T> int32_t System::MemoryExtensions::LastIndexOfAnyExcept(const ReadOnlySpan<T> &span, const ReadOnlySpan<T> &values)
```


### Parâmetros de modelo

| Parameter | Description |
| --- | --- |
| T | O tipo de elementos no span |

### Argumentos

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | O span a ser pesquisado |
| values | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | A sequência de valores a excluir |

### Valor de retorno

O índice baseado em zero do último elemento não excluído, ou -1 se não encontrado

## System::MemoryExtensions::LastIndexOfAnyExcept(const Span\<T\>\&, const ReadOnlySpan\<T\>\&) função


Encontra a última ocorrência de qualquer elemento, exceto valores de uma sequência, dentro de um span mutável.

```cpp
template<typename T> int32_t System::MemoryExtensions::LastIndexOfAnyExcept(const Span<T> &span, const ReadOnlySpan<T> &values)
```


### Parâmetros de modelo

| Parameter | Description |
| --- | --- |
| T | O tipo de elementos no span |

### Argumentos

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | O span a ser pesquisado |
| values | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | A sequência de valores a excluir |

### Valor de retorno

O índice baseado em zero do último elemento não excluído, ou -1 se não encontrado

## System::MemoryExtensions::LastIndexOfAnyExcept(const Span\<T\>\&, const Span\<T\>\&) função


Encontra a última ocorrência de qualquer elemento, exceto valores de uma sequência mutável, dentro de um span mutável.

```cpp
template<typename T> int32_t System::MemoryExtensions::LastIndexOfAnyExcept(const Span<T> &span, const Span<T> &values)
```


### Parâmetros de modelo

| Parameter | Description |
| --- | --- |
| T | O tipo de elementos no span |

### Argumentos

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | O span a ser pesquisado |
| values | const [Span](../../system/span/)\<T\>\& | A sequência de valores a excluir |

### Valor de retorno

O índice baseado em zero do último elemento não excluído, ou -1 se não encontrado

## Veja também

* Classe [ReadOnlySpan](../../system/readonlyspan/)
* Classe [Span](../../system/span/)
* Namespace [System::MemoryExtensions](../)
* Biblioteca [Aspose.Slides](../../)