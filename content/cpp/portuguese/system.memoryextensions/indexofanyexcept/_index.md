---
title: IndexOfAnyExcept()
second_title: Referência da API Aspose.Slides para C++
description: Encontra o índice do primeiro elemento que não é igual ao valor especificado em um ReadOnlySpan<T>
type: docs
weight: 170
url: /pt/system.memoryextensions/indexofanyexcept/
---
## System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const T\&) função


Encontra o índice do primeiro elemento que não é igual ao valor especificado em um ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan<T> &span, const T &value)
```


### Parâmetros de modelo

| Parâmetro | Descrição |
| --- | --- |
| T | O tipo dos elementos no span |

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | O span onde procurar |
| value | const T\& | O valor a ser excluído da pesquisa |

### Valor de retorno

O índice baseado em zero do primeiro elemento que não corresponde, ou -1 se não encontrado

## System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const T\&, const T\&) função


Encontra o índice do primeiro elemento que não é igual a nenhum dos dois valores especificados em um ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan<T> &span, const T &value0, const T &value1)
```


### Parâmetros de modelo

| Parâmetro | Descrição |
| --- | --- |
| T | O tipo dos elementos no span |

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | O span onde procurar |
| value0 | const T\& | O primeiro valor a ser excluído da pesquisa |
| value1 | const T\& | O segundo valor a ser excluído da pesquisa |

### Valor de retorno

O índice baseado em zero do primeiro elemento que não corresponde, ou -1 se não encontrado

## System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const T\&, const T\&, const T\&) função


Encontra o índice do primeiro elemento que não é igual a nenhum dos três valores especificados em um ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan<T> &span, const T &value0, const T &value1, const T &value2)
```


### Parâmetros de modelo

| Parâmetro | Descrição |
| --- | --- |
| T | O tipo dos elementos no span |

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | O span onde procurar |
| value0 | const T\& | O primeiro valor a ser excluído da pesquisa |
| value1 | const T\& | O segundo valor a ser excluído da pesquisa |
| value2 | const T\& | O terceiro valor a ser excluído da pesquisa |

### Valor de retorno

O índice baseado em zero do primeiro elemento que não corresponde, ou -1 se não encontrado

## System::MemoryExtensions::IndexOfAnyExcept(const Span\<T\>\&, const T\&) função


Encontra o índice do primeiro elemento que não é igual ao valor especificado em um Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const Span<T> &span, const T &value)
```


### Parâmetros de modelo

| Parâmetro | Descrição |
| --- | --- |
| T | O tipo dos elementos no span |

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | O span onde procurar |
| value | const T\& | O valor a ser excluído da pesquisa |

### Valor de retorno

O índice baseado em zero do primeiro elemento que não corresponde, ou -1 se não encontrado

## System::MemoryExtensions::IndexOfAnyExcept(const Span\<T\>\&, const T\&, const T\&) função


Encontra o índice do primeiro elemento que não é igual a nenhum dos dois valores especificados em um Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const Span<T> &span, const T &value0, const T &value1)
```


### Parâmetros de modelo

| Parâmetro | Descrição |
| --- | --- |
| T | O tipo dos elementos no span |

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | O span onde procurar |
| value0 | const T\& | O primeiro valor a ser excluído da pesquisa |
| value1 | const T\& | O segundo valor a ser excluído da pesquisa |

### Valor de retorno

O índice baseado em zero do primeiro elemento que não corresponde, ou -1 se não encontrado

## System::MemoryExtensions::IndexOfAnyExcept(const Span\<T\>\&, const T\&, const T\&, const T\&) função


Encontra o índice do primeiro elemento que não é igual a nenhum dos três valores especificados em um Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const Span<T> &span, const T &value0, const T &value1, const T &value2)
```


### Parâmetros de modelo

| Parâmetro | Descrição |
| --- | --- |
| T | O tipo dos elementos no span |

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | O span onde procurar |
| value0 | const T\& | O primeiro valor a ser excluído da pesquisa |
| value1 | const T\& | O segundo valor a ser excluído da pesquisa |
| value2 | const T\& | O terceiro valor a ser excluído da pesquisa |

### Valor de retorno

O índice baseado em zero do primeiro elemento que não corresponde, ou -1 se não encontrado

## System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const ReadOnlySpan\<T\>\&) função


Encontra o índice do primeiro elemento que não é igual a nenhum valor em um span de valores.

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const ReadOnlySpan<T> &span, const ReadOnlySpan<T> &values)
```


### Parâmetros de modelo

| Parâmetro | Descrição |
| --- | --- |
| T | O tipo dos elementos nos spans |

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | O span onde procurar |
| values | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | O span contendo valores a serem excluídos da pesquisa |

### Valor de retorno

O índice baseado em zero do primeiro elemento que não corresponde, ou -1 se não encontrado

## System::MemoryExtensions::IndexOfAnyExcept(const Span\<T\>\&, const ReadOnlySpan\<T\>\&) função


Encontra o índice do primeiro elemento que não é igual a nenhum valor em um span de valores em um Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAnyExcept(const Span<T> &span, const ReadOnlySpan<T> &values)
```


### Parâmetros de modelo

| Parâmetro | Descrição |
| --- | --- |
| T | O tipo dos elementos nos spans |

### Argumentos

| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | O span onde procurar |
| values | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | O span contendo valores a serem excluídos da pesquisa |

### Valor de retorno

O índice baseado em zero do primeiro elemento que não corresponde, ou -1 se não encontrado

## See Also

* Classe [ReadOnlySpan](../../system/readonlyspan/)
* Classe [Span](../../system/span/)
* Espaço de nomes [System::MemoryExtensions](../)
* Biblioteca [Aspose.Slides](../../)