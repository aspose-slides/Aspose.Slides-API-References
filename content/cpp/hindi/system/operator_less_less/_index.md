---
title: operator<<()
second_title: Aspose.Slides for C++ API संदर्भ
description: UTF-8 एन्कोडिंग का उपयोग करके डेटा को स्ट्रीम में डालें।
type: docs
weight: 2159
url: /hi/system/operator_less_less/
---
## System::operator<<(std::ostream\&, DateTime) फ़ंक्शन


UTF-8 एन्कोडिंग का उपयोग करके डेटा को स्ट्रीम में डालें।

```cpp
std::ostream & System::operator<<(std::ostream &stream, DateTime date_time)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::ostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| date_time | [DateTime](../datetime/) | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::wostream\&, DateTime) फ़ंक्शन


डेटा को स्ट्रीम में डालें।

```cpp
std::wostream & System::operator<<(std::wostream &stream, DateTime date_time)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::wostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| date_time | [DateTime](../datetime/) | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::ostream\&, DateTimeOffset) फ़ंक्शन


UTF-8 एन्कोडिंग का उपयोग करके डेटा को स्ट्रीम में डालें।

```cpp
std::ostream & System::operator<<(std::ostream &stream, DateTimeOffset value)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::ostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| value | [DateTimeOffset](../datetimeoffset/) | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::wostream\&, DateTimeOffset) फ़ंक्शन


डेटा को स्ट्रीम में डालें।

```cpp
std::wostream & System::operator<<(std::wostream &stream, DateTimeOffset value)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::wostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| value | [DateTimeOffset](../datetimeoffset/) | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::ostream\&, const Decimal\&) फ़ंक्शन


UTF-8 एन्कोडिंग का उपयोग करके डेटा को स्ट्रीम में डालें।

```cpp
std::ostream & System::operator<<(std::ostream &stream, const Decimal &decimal)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::ostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| decimal | const [Decimal](../decimal/)\& | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::wostream\&, const Decimal\&) फ़ंक्शन


डेटा को स्ट्रीम में डालें।

```cpp
std::wostream & System::operator<<(std::wostream &stream, const Decimal &decimal)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::wostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| decimal | const [Decimal](../decimal/)\& | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::ostream\&, const Exception\&) फ़ंक्शन


UTF-8 एन्कोडिंग का उपयोग करके डेटा को स्ट्रीम में डालें।

```cpp
std::ostream & System::operator<<(std::ostream &stream, const Exception &exception)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::ostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| exception | const [Exception](../exception/)\& | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::wostream\&, const Exception\&) फ़ंक्शन


डेटा को स्ट्रीम में डालें।

```cpp
std::wostream & System::operator<<(std::wostream &stream, const Exception &exception)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::wostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| exception | const [Exception](../exception/)\& | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::ostream\&, const Guid\&) फ़ंक्शन


UTF-8 एन्कोडिंग का उपयोग करके डेटा को स्ट्रीम में डालें।

```cpp
std::ostream & System::operator<<(std::ostream &stream, const Guid &guid)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::ostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| guid | const [Guid](../guid/)\& | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::wostream\&, const Guid\&) फ़ंक्शन


डेटा को स्ट्रीम में डालें।

```cpp
std::wostream & System::operator<<(std::wostream &stream, const Guid &guid)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::wostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| guid | const [Guid](../guid/)\& | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::ostream\&, const Nullable\<T\>\&) फ़ंक्शन


UTF-8 एन्कोडिंग का उपयोग करके डेटा को स्ट्रीम में डालें।

```cpp
template<typename T> std::ostream & System::operator<<(std::ostream &stream, const Nullable<T> &value)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::ostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| value | const [Nullable](../nullable/)\<T\>\& | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::wostream\&, const Nullable\<T\>\&) फ़ंक्शन


डेटा को स्ट्रीम में डालें।

```cpp
template<typename T> std::wostream & System::operator<<(std::wostream &stream, const Nullable<T> &value)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::wostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| value | const [Nullable](../nullable/)\<T\>\& | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::ostream\&, const System::Object\&) फ़ंक्शन


UTF-8 एन्कोडिंग का उपयोग करके डेटा को स्ट्रीम में डालें।

```cpp
std::ostream & System::operator<<(std::ostream &stream, const System::Object &object)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::ostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| object | const [System::Object](../object/)\& | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::wostream\&, const System::Object\&) फ़ंक्शन


डेटा को स्ट्रीम में डालें।

```cpp
std::wostream & System::operator<<(std::wostream &stream, const System::Object &object)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::wostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| object | const [System::Object](../object/)\& | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::ostream\&, const OperatingSystem\&) फ़ंक्शन


UTF-8 एन्कोडिंग का उपयोग करके डेटा को स्ट्रीम में डालें।

```cpp
std::ostream & System::operator<<(std::ostream &stream, const OperatingSystem &os)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::ostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| os | const [OperatingSystem](../operatingsystem/)\& | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::wostream\&, const OperatingSystem\&) फ़ंक्शन


डेटा को स्ट्रीम में डालें।

```cpp
std::wostream & System::operator<<(std::wostream &stream, const OperatingSystem &os)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::wostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| os | const [OperatingSystem](../operatingsystem/)\& | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::ostream\&, const SharedPtr\<T\>\&) फ़ंक्शन


UTF-8 एन्कोडिंग का उपयोग करके डेटा को स्ट्रीम में डालें।

```cpp
template<typename T> std::ostream & System::operator<<(std::ostream &stream, const SharedPtr<T> &object_ptr)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::ostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| object_ptr | const [SharedPtr](../sharedptr/)\<T\>\& | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::wostream\&, const SharedPtr\<T\>\&) फ़ंक्शन


डेटा को स्ट्रीम में डालें।

```cpp
template<typename T> std::wostream & System::operator<<(std::wostream &stream, const SharedPtr<T> &object_ptr)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::wostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| object_ptr | const [SharedPtr](../sharedptr/)\<T\>\& | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::ostream\&, const String\&) फ़ंक्शन


UTF-8 एन्कोडिंग का उपयोग करके स्ट्रिंग को आउटपुट स्ट्रीम में लिखता है।

```cpp
std::ostream & System::operator<<(std::ostream &os, const String &str)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| os | std::ostream\& | **basic_ostream** के **char** के साथ इंस्टैंसिएशन वाला आउटपुट स्ट्रीम ऑब्जेक्ट। |
| str | const [String](../string/)\& | आउटपुट स्ट्रीम को भेजी जाने वाली स्ट्रिंग। |

### रिटर्न मान

स्ट्रिंग जोड़ी गई आउटपुट स्ट्रीम।

## System::operator<<(std::wostream\&, const String\&) फ़ंक्शन


स्ट्रिंग को आउटपुट स्ट्रीम में लिखता है।

```cpp
std::wostream & System::operator<<(std::wostream &os, const String &str)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| os | std::wostream\& | **basic_ostream** के ****wchar_t**** के साथ इंस्टैंसिएशन वाला आउटपुट स्ट्रीम ऑब्जेक्ट। |
| str | const [String](../string/)\& | आउटपुट स्ट्रीम को भेजी जाने वाली स्ट्रिंग। |

### रिटर्न मान

स्ट्रिंग जोड़ी गई आउटपुट स्ट्रीम।

## System::operator<<(std::ostream\&, TimeSpan) फ़ंक्शन


UTF-8 एन्कोडिंग का उपयोग करके डेटा को स्ट्रीम में डालें।

```cpp
std::ostream & System::operator<<(std::ostream &stream, TimeSpan time_span)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::ostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| time_span | [TimeSpan](../timespan/) | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::wostream\&, TimeSpan) फ़ंक्शन


डेटा को स्ट्रीम में डालें।

```cpp
std::wostream & System::operator<<(std::wostream &stream, TimeSpan time_span)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::wostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| time_span | [TimeSpan](../timespan/) | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::ostream\&, const TypeInfo\&) फ़ंक्शन


UTF-8 एन्कोडिंग का उपयोग करके डेटा को स्ट्रीम में डालें।

```cpp
std::ostream & System::operator<<(std::ostream &stream, const TypeInfo &type_info)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::ostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| type_info | const [TypeInfo](../typeinfo/)\& | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::wostream\&, const TypeInfo\&) फ़ंक्शन


डेटा को स्ट्रीम में डालें।

```cpp
std::wostream & System::operator<<(std::wostream &stream, const TypeInfo &type_info)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::wostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| type_info | const [TypeInfo](../typeinfo/)\& | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::ostream\&, const Version\&) फ़ंक्शन


UTF-8 एन्कोडिंग का उपयोग करके डेटा को स्ट्रीम में डालें।

```cpp
std::ostream & System::operator<<(std::ostream &stream, const Version &version)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::ostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| version | const [Version](../version/)\& | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::wostream\&, const Version\&) फ़ंक्शन


डेटा को स्ट्रीम में डालें।

```cpp
std::wostream & System::operator<<(std::wostream &stream, const Version &version)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::wostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| version | const [Version](../version/)\& | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::ostream\&, const WeakPtr\<T\>\&) फ़ंक्शन


UTF-8 एन्कोडिंग का उपयोग करके डेटा को स्ट्रीम में डालें।

```cpp
template<typename T> std::ostream & System::operator<<(std::ostream &stream, const WeakPtr<T> &object_ptr)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::ostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| object_ptr | const [WeakPtr](../weakptr/)\<T\>\& | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## System::operator<<(std::wostream\&, const WeakPtr\<T\>\&) फ़ंक्शन


डेटा को स्ट्रीम में डालें।

```cpp
template<typename T> std::wostream & System::operator<<(std::wostream &stream, const WeakPtr<T> &object_ptr)
```


### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::wostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| object_ptr | const [WeakPtr](../weakptr/)\<T\>\& | [Data](../../system.data/) को डालने के लिए। |

### रिटर्न मान

**stream**।

## देखें

* Typedef [Exception](../exception/)
* Typedef [SharedPtr](../sharedptr/)
* Class [DateTime](../datetime/)
* Class [DateTimeOffset](../datetimeoffset/)
* Class [Decimal](../decimal/)
* Class [Guid](../guid/)
* Class [Nullable](../nullable/)
* Class [Object](../object/)
* Class [OperatingSystem](../operatingsystem/)
* Class [String](../string/)
* Class [TimeSpan](../timespan/)
* Class [TypeInfo](../typeinfo/)
* Class [Version](../version/)
* Class [WeakPtr](../weakptr/)
* Namespace [System](../)
* Library [Aspose.Slides](../../)