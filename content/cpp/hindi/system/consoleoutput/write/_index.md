---
title: Write()
second_title: Aspose.Slides के लिए C++ API संदर्भ
description: निर्दिष्ट bool मान का स्ट्रिंग प्रतिनिधित्व वर्तमान ऑब्जेक्ट द्वारा प्रतिनिधित्व किए गए आउटपुट स्ट्रीम में लिखता है।
type: docs
weight: 14
url: /hi/system/consoleoutput/write/
---
## ConsoleOutput::Write(bool) मेथड

निर्दिष्ट bool मान का स्ट्रिंग प्रतिनिधित्व वर्तमान वस्तु द्वारा प्रतिनिधित्व किए गए आउटपुट स्ट्रीम में लिखता है।

```cpp
void System::ConsoleOutput::Write(bool value) override
```

### आर्ग्युमेंट्स

| पैरामीटर | टाइप | विवरण |
| --- | --- | --- |
| value | **bool** | आउटपुट करने के लिए मान |

## ConsoleOutput::Write(const SharedPtr\<Object\>\&) मेथड

निर्दिष्ट ऑब्जेक्ट का स्ट्रिंग प्रतिनिधित्व वर्तमान वस्तु द्वारा प्रतिनिधित्व किए गए आउटपुट स्ट्रीम में लिखता है।

```cpp
void System::ConsoleOutput::Write(const SharedPtr<Object> &value) override
```

### आर्ग्युमेंट्स

| पैरामीटर | टाइप | विवरण |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | आउटपुट करने के लिए ऑब्जेक्ट |

## ConsoleOutput::Write(char_t) मेथड

निर्दिष्ट कैरेक्टर मान को वर्तमान वस्तु द्वारा प्रतिनिधित्व किए गए आउटपुट स्ट्रीम में लिखता है।

```cpp
void System::ConsoleOutput::Write(char_t value) override
```

### आर्ग्युमेंट्स

| पैरामीटर | टाइप | विवरण |
| --- | --- | --- |
| value | char_t | आउटपुट करने के लिए मान |

## ConsoleOutput::Write(Decimal) मेथड

वर्तमान वस्तु द्वारा प्रतिनिधित्व किए गए आउटपुट स्ट्रीम में [Decimal](../../decimal/) मान का स्ट्रिंग प्रतिनिधित्व लिखता है।

```cpp
void System::ConsoleOutput::Write(Decimal value) override
```

### आर्ग्युमेंट्स

| पैरामीटर | टाइप | विवरण |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | आउटपुट करने के लिए मान |

## ConsoleOutput::Write(double) मेथड

वर्तमान वस्तु द्वारा प्रतिनिधित्व किए गए आउटपुट स्ट्रीम में double-precision फ़्लोटिंग पॉइंट मान का स्ट्रिंग प्रतिनिधित्व लिखता है।

```cpp
void System::ConsoleOutput::Write(double value) override
```

### आर्ग्युमेंट्स

| पैरामीटर | टाइप | विवरण |
| --- | --- | --- |
| value | **double** | आउटपुट करने के लिए मान |

## ConsoleOutput::Write(int32_t) मेथड

वर्तमान वस्तु द्वारा प्रतिनिधित्व किए गए आउटपुट स्ट्रीम में 32-बिट इन्टीजर मान का स्ट्रिंग प्रतिनिधित्व लिखता है।

```cpp
void System::ConsoleOutput::Write(int32_t value) override
```

### आर्ग्युमेंट्स

| पैरामीटर | टाइप | विवरण |
| --- | --- | --- |
| value | **int32_t** | आउटपुट करने के लिए मान |

## ConsoleOutput::Write(int64_t) मेथड

वर्तमान वस्तु द्वारा प्रतिनिधित्व किए गए आउटपुट स्ट्रीम में 64-बिट इन्टीजर मान का स्ट्रिंग प्रतिनिधित्व लिखता है।

```cpp
void System::ConsoleOutput::Write(int64_t value) override
```

### आर्ग्युमेंट्स

| पैरामीटर | टाइप | विवरण |
| --- | --- | --- |
| value | **int64_t** | आउटपुट करने के लिए मान |

## ConsoleOutput::Write(float) मेथड

वर्तमान वस्तु द्वारा प्रतिनिधित्व किए गए आउटपुट स्ट्रीम में single-precision फ़्लोटिंग पॉइंट मान का स्ट्रिंग प्रतिनिधित्व लिखता है।

```cpp
void System::ConsoleOutput::Write(float value) override
```

### आर्ग्युमेंट्स

| पैरामीटर | टाइप | विवरण |
| --- | --- | --- |
| value | **float** | आउटपुट करने के लिए मान |

## ConsoleOutput::Write(const String\&) मेथड

निर्दिष्ट स्ट्रिंग ऑब्जेक्ट को वर्तमान वस्तु द्वारा प्रतिनिधित्व किए गए आउटपुट स्ट्रीम में लिखता है।

```cpp
void System::ConsoleOutput::Write(const String &value) override
```

### आर्ग्युमेंट्स

| पैरामीटर | टाइप | विवरण |
| --- | --- | --- |
| value | const [String](../../string/)\& | आउटपुट करने के लिए स्ट्रिंग ऑब्जेक्ट |

## ConsoleOutput::Write(uint32_t) मेथड

वर्तमान वस्तु द्वारा प्रतिनिधित्व किए गए आउटपुट स्ट्रीम में असंकेतित 32-बिट इन्टीजर मान का स्ट्रिंग प्रतिनिधित्व लिखता है।

```cpp
void System::ConsoleOutput::Write(uint32_t value) override
```

### आर्ग्युमेंट्स

| पैरामीटर | टाइप | विवरण |
| --- | --- | --- |
| value | **uint32_t** | आउटपुट करने के लिए मान |

## ConsoleOutput::Write(uint64_t) मेथड

वर्तमान वस्तु द्वारा प्रतिनिधित्व किए गए आउटपुट स्ट्रीम में असंकेतित 64-बिट इन्टीजर मान का स्ट्रिंग प्रतिनिधित्व लिखता है।

```cpp
void System::ConsoleOutput::Write(uint64_t value) override
```

### आर्ग्युमेंट्स

| पैरामीटर | टाइप | विवरण |
| --- | --- | --- |
| value | **uint64_t** | आउटपुट करने के लिए मान |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&) मेथड

निर्दिष्ट कैरेक्टर एरे का स्ट्रिंग प्रतिनिधित्व वर्तमान वस्तु द्वारा प्रतिनिधित्व किए गए आउटपुट स्ट्रीम में लिखता है।

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer) override
```

### आर्ग्युमेंट्स

| पैरामीटर | टाइप | विवरण |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | आउटपुट करने के लिए एरे |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&, int32_t, int32_t) मेथड

निर्दिष्ट कैरेक्टर एरे के मानों की एक रेंज का स्ट्रिंग प्रतिनिधित्व वर्तमान वस्तु द्वारा प्रतिनिधित्व किए गए आउटपुट स्ट्रीम में लिखता है।

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```

### आर्ग्युमेंट्स

| पैरामीटर | टाइप | विवरण |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | आउटपुट करने के लिए मानों वाली एरे |
| index | **int32_t** | इंडेक्स जहाँ आउटपुट की जाने वाली तत्वों की रेंज शुरू होती है |
| count | **int32_t** | आउटपुट की जाने वाली तत्वों की रेंज में तत्वों की संख्या |

## ConsoleOutput::Write(const char_t *) मेथड

निर्दिष्ट c-string को वर्तमान वस्तु द्वारा प्रतिनिधित्व किए गए आउटपुट स्ट्रीम में लिखता है।

```cpp
void System::ConsoleOutput::Write(const char_t *value) override
```

### आर्ग्युमेंट्स

| पैरामीटर | टाइप | विवरण |
| --- | --- | --- |
| value | const char_t * | आउटपुट करने के लिए c-string |

## ConsoleOutput::Write(const TypeInfo\&) मेथड

वर्तमान वस्तु द्वारा प्रतिनिधित्व किए गए आउटपुट स्ट्रीम में निर्दिष्ट [TypeInfo](../../typeinfo/) ऑब्जेक्ट का स्ट्रिंग प्रतिनिधित्व लिखता है।

```cpp
void System::ConsoleOutput::Write(const TypeInfo &value) override
```

### आर्ग्युमेंट्स

| पैरामीटर | टाइप | विवरण |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | आउटपुट करने के लिए [TypeInfo](../../typeinfo/) ऑब्जेक्ट |

## ConsoleOutput::Write(const char *) मेथड

```cpp
void System::ConsoleOutput::Write(const char *)=delete
```

## संबंधित देखें

* टाइपडिफ [SharedPtr](../../sharedptr/)
* टाइपडिफ [ArrayPtr](../../arrayptr/)
* क्लास [ConsoleOutput](../)
* क्लास [Object](../../object/)
* क्लास [Decimal](../../decimal/)
* क्लास [String](../../string/)
* क्लास [TypeInfo](../../typeinfo/)
* नेमस्पेस [System](../../)
* लाइब्रेरी [Aspose.Slides](../../../)