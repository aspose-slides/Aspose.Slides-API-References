---
title: WriteLine()
second_title: Aspose.Slides for C++ API संदर्भ
description: वर्तमान ऑब्जेक्ट द्वारा प्रतिनिधित्व किए गए आउटपुट स्ट्रीम पर वर्तमान लाइन टर्मिनेटर आउटपुट करता है।
type: docs
weight: 27
url: /hi/system/consoleoutput/writeline/
---
## ConsoleOutput::WriteLine() विधि

वर्तमान ऑब्जेक्ट द्वारा प्रतिनिधित्व किए गए आउटपुट स्ट्रीम पर वर्तमान लाइन टर्मिनेटर को आउटपुट करता है।

```cpp
void System::ConsoleOutput::WriteLine() override
```

## ConsoleOutput::WriteLine(const SharedPtr\<Object\>\&) विधि

निर्दिष्ट ऑब्जेक्ट का स्ट्रिंग प्रतिनिधित्व, उसके बाद वर्तमान लाइन टर्मिनेटर, आउटपुट स्ट्रीम पर आउटपुट करता है जो वर्तमान ऑब्जेक्ट द्वारा प्रतिनिधित्व किया गया है।

```cpp
void System::ConsoleOutput::WriteLine(const SharedPtr<Object> &value) override
```

### तर्क

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | आउटपुट करने के लिए ऑब्जेक्ट |

## ConsoleOutput::WriteLine(bool) विधि

निर्दिष्ट बूल मान का स्ट्रिंग प्रतिनिधित्व, उसके बाद वर्तमान लाइन टर्मिनेटर, आउटपुट स्ट्रीम पर आउटपुट करता है जो वर्तमान ऑब्जेक्ट द्वारा प्रतिनिधित्व किया गया है।

```cpp
void System::ConsoleOutput::WriteLine(bool value) override
```

### तर्क

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **bool** | आउटपुट करने के लिए मान |

## ConsoleOutput::WriteLine(char_t) विधि

निर्दिष्ट अक्षर मान को वर्तमान लाइन टर्मिनेटर के साथ आउटपुट स्ट्रीम पर आउटपुट करता है जो वर्तमान ऑब्जेक्ट द्वारा प्रतिनिधित्व किया गया है।

```cpp
void System::ConsoleOutput::WriteLine(char_t value) override
```

### तर्क

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | char_t | आउटपुट करने के लिए मान |

## ConsoleOutput::WriteLine(Decimal) विधि

[Decimal](../../decimal/) मान का स्ट्रिंग प्रतिनिधित्व, उसके बाद वर्तमान लाइन टर्मिनेटर, आउटपुट स्ट्रीम पर आउटपुट करता है जो वर्तमान ऑब्जेक्ट द्वारा प्रतिनिधित्व किया गया है।

```cpp
void System::ConsoleOutput::WriteLine(Decimal value) override
```

### तर्क

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | आउटपुट करने के लिए मान |

## ConsoleOutput::WriteLine(double) विधि

डबल-प्रिसीजन फ्लोटिंग-पॉइंट मान का स्ट्रिंग प्रतिनिधित्व, उसके बाद वर्तमान लाइन टर्मिनेटर, आउटपुट स्ट्रीम पर आउटपुट करता है जो वर्तमान ऑब्जेक्ट द्वारा प्रतिनिधित्व किया गया है।

```cpp
void System::ConsoleOutput::WriteLine(double value) override
```

### तर्क

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **double** | आउटपुट करने के लिए मान |

## ConsoleOutput::WriteLine(int) विधि

32-बिट इंटीज़र मान का स्ट्रिंग प्रतिनिधित्व, उसके बाद वर्तमान लाइन टर्मिनेटर, आउटपुट स्ट्रीम पर आउटपुट करता है जो वर्तमान ऑब्जेक्ट द्वारा प्रतिनिधित्व किया गया है।

```cpp
void System::ConsoleOutput::WriteLine(int value) override
```

### तर्क

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | int | आउटपुट करने के लिए मान |

## ConsoleOutput::WriteLine(int64_t) विधि

64-बिट इंटीज़र मान का स्ट्रिंग प्रतिनिधित्व, उसके बाद वर्तमान लाइन टर्मिनेटर, आउटपुट स्ट्रीम पर आउटपुट करता है जो वर्तमान ऑब्जेक्ट द्वारा प्रतिनिधित्व किया गया है।

```cpp
void System::ConsoleOutput::WriteLine(int64_t value) override
```

### तर्क

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **int64_t** | आउटपुट करने के लिए मान |

## ConsoleOutput::WriteLine(float) विधि

सिंगल-प्रिसीजन फ्लोटिंग-पॉइंट मान का स्ट्रिंग प्रतिनिधित्व, उसके बाद वर्तमान लाइन टर्मिनेटर, आउटपुट स्ट्रीम पर आउटपुट करता है जो वर्तमान ऑब्जेक्ट द्वारा प्रतिनिधित्व किया गया है।

```cpp
void System::ConsoleOutput::WriteLine(float value) override
```

### तर्क

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **float** | आउटपुट करने के लिए मान |

## ConsoleOutput::WriteLine(const String\&) विधि

निर्दिष्ट स्ट्रिंग ऑब्जेक्ट को वर्तमान लाइन टर्मिनेटर के साथ आउटपुट स्ट्रीम पर आउटपुट करता है जो वर्तमान ऑब्जेक्ट द्वारा प्रतिनिधित्व किया गया है।

```cpp
void System::ConsoleOutput::WriteLine(const String &value) override
```

### तर्क

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | const [String](../../string/)\& | आउटपुट करने के लिए स्ट्रिंग ऑब्जेक्ट |

## ConsoleOutput::WriteLine(uint32_t) विधि

अअनसाइन्ड 32-बिट इंटीज़र मान का स्ट्रिंग प्रतिनिधित्व, उसके बाद वर्तमान लाइन टर्मिनेटर, आउटपुट स्ट्रीम पर आउटपुट करता है जो वर्तमान ऑब्जेक्ट द्वारा प्रतिनिधित्व किया गया है।

```cpp
void System::ConsoleOutput::WriteLine(uint32_t value) override
```

### तर्क

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **uint32_t** | आउटपुट करने के लिए मान |

## ConsoleOutput::WriteLine(uint64_t) विधि

अअनसाइन्ड 64-बिट इंटीज़र मान का स्ट्रिंग प्रतिनिधित्व, उसके बाद वर्तमान लाइन टर्मिनेटर, आउटपुट स्ट्रीम पर आउटपुट करता है जो वर्तमान ऑब्जेक्ट द्वारा प्रतिनिधित्व किया गया है।

```cpp
void System::ConsoleOutput::WriteLine(uint64_t value) override
```

### तर्क

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **uint64_t** | आउटपुट करने के लिए मान |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&) विधि

निर्दिष्ट अक्षर एरे का स्ट्रिंग प्रतिनिधित्व, उसके बाद वर्तमान लाइन टर्मिनेटर, आउटपुट स्ट्रीम पर आउटपुट करता है जो वर्तमान ऑब्जेक्ट द्वारा प्रतिनिधित्व किया गया है।

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer) override
```

### तर्क

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | आउटपुट करने के लिए एरे |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&, int32_t, int32_t) विधि

निर्दिष्ट अक्षर एरे के मूल्य सीमा का स्ट्रिंग प्रतिनिधित्व, उसके बाद वर्तमान लाइन टर्मिनेटर, आउटपुट स्ट्रीम पर आउटपुट करता है जो वर्तमान ऑब्जेक्ट द्वारा प्रतिनिधित्व किया गया है।

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```

### तर्क

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | आउटपुट करने के लिए मानों वाले एरे |
| index | **int32_t** | आउटपुट करने के लिए तत्वों की रेंज की शुरुआत का सूचकांक |
| count | **int32_t** | आउटपुट करने के लिए तत्वों की रेंज में तत्वों की संख्या |

## ConsoleOutput::WriteLine(const char_t *) विधि

निर्दिष्ट C-स्ट्रींग को वर्तमान लाइन टर्मिनेटर के साथ आउटपुट स्ट्रीम पर आउटपुट करता है जो वर्तमान ऑब्जेक्ट द्वारा प्रतिनिधित्व किया गया है।

```cpp
void System::ConsoleOutput::WriteLine(const char_t *value) override
```

### तर्क

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | const char_t * | आउटपुट करने के लिए C-स्ट्रींग |

## ConsoleOutput::WriteLine(const TypeInfo\&) विधि

निर्दिष्ट [TypeInfo](../../typeinfo/) ऑब्जेक्ट का स्ट्रिंग प्रतिनिधित्व, उसके बाद वर्तमान लाइन टर्मिनेटर, आउटपुट स्ट्रीम पर आउटपुट करता है जो वर्तमान ऑब्जेक्ट द्वारा प्रतिनिधित्व किया गया है।

```cpp
void System::ConsoleOutput::WriteLine(const TypeInfo &value) override
```

### तर्क

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | [TypeInfo](../../typeinfo/) ऑब्जेक्ट को आउटपुट करने के लिए |

## ConsoleOutput::WriteLine(const char *) विधि




```cpp
void System::ConsoleOutput::WriteLine(const char *)=delete
```

## देखें

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Class [ConsoleOutput](../)
* Class [Object](../../object/)
* Class [Decimal](../../decimal/)
* Class [String](../../string/)
* Class [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)