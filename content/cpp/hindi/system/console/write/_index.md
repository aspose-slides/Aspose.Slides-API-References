---
title: Write()
second_title: C++ के लिए Aspose.Slides API संदर्भ
description: निर्दिष्ट ऑब्जेक्ट का स्ट्रिंग प्रतिनिधित्व मानक आउटपुट स्ट्रीम पर आउटपुट करता है।
type: docs
weight: 1
url: /hi/system/console/write/
---
## Console::Write(const SharedPtr\<T\>\&) मेथड

निर्दिष्ट ऑब्जेक्ट का स्ट्रिंग प्रतिनिधित्व मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
template<class T> static void System::Console::Write(const SharedPtr<T> &object)
```

### टेम्पलेट पैरामीटर

| पैरामीटर | विवरण |
| --- | --- |
| T | आउटपुट करने के लिए ऑब्जेक्ट का प्रकार |

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| object | const [SharedPtr](../../sharedptr/)\<T\>\& | [Object](../../object/) को आउटपुट करने के लिए |

## Console::Write(bool) मेथड

बूल मान की स्ट्रिंग प्रतिनिधित्व को मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
static void System::Console::Write(bool value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **bool** | आउटपुट करने के लिए मान |

## Console::Write(char_t) मेथड

निर्दिष्ट वर्ण मान को मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
static void System::Console::Write(char_t value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | char_t | आउटपुट करने के लिए मान |

## Console::Write(const ArrayPtr\<char_t\>\&) मेथड

निर्दिष्ट वर्ण एरे का स्ट्रिंग प्रतिनिधित्व मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
static void System::Console::Write(const ArrayPtr<char_t> &buffer)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | आउटपुट करने के लिए एरे |

## Console::Write(const Decimal\&) मेथड

[Decimal](../../decimal/) मान का स्ट्रिंग प्रतिनिधित्व मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
static void System::Console::Write(const Decimal &value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | आउटपुट करने के लिए मान |

## Console::Write(double) मेथड

डबल-प्रिसिजन फ्लोटिंग-पॉइंट मान का स्ट्रिंग प्रतिनिधित्व मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
static void System::Console::Write(double value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **double** | आउटपुट करने के लिए मान |

## Console::Write(float) मेथड

सिंगल-प्रिसिजन फ्लोटिंग-पॉइंट मान का स्ट्रिंग प्रतिनिधित्व मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
static void System::Console::Write(float value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **float** | आउटपुट करने के लिए मान |

## Console::Write(int32_t) मेथड

32-बिट इंटीजर मान का स्ट्रिंग प्रतिनिधित्व मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
static void System::Console::Write(int32_t value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **int32_t** | आउटपुट करने के लिए मान |

## Console::Write(int64_t) मेथड

64-बिट इंटीजर मान का स्ट्रिंग प्रतिनिधित्व मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
static void System::Console::Write(int64_t value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **int64_t** | आउटपुट करने के लिए मान |

## Console::Write(const String\&) मेथड

निर्दिष्ट स्ट्रिंग ऑब्जेक्ट को मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
static void System::Console::Write(const String &value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | const [String](../../string/)\& | आउटपुट करने के लिए स्ट्रिंग ऑब्जेक्ट |

## Console::Write(const char_t *) मेथड

निर्दिष्ट C-स्ट्रिंग को मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
static void System::Console::Write(const char_t *value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | const char_t * | आउटपुट करने के लिए C-स्ट्रिंग |

## Console::Write(const TypeInfo\&) मेथड

[TypeInfo](../../typeinfo/) मान का स्ट्रिंग प्रतिनिधित्व मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
static void System::Console::Write(const TypeInfo &value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | आउटपुट करने के लिए मान |

## Console::Write(uint32_t) मेथड

अस्पष्ट 32-बिट इंटीजर मान का स्ट्रिंग प्रतिनिधित्व मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
static void System::Console::Write(uint32_t value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **uint32_t** | आउटपुट करने के लिए मान |

## Console::Write(uint64_t) मेथड

अस्पष्ट 64-बिट इंटीजर मान का स्ट्रिंग प्रतिनिधित्व मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
static void System::Console::Write(uint64_t value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **uint64_t** | आउटपुट करने के लिए मान |

## Console::Write(const ArrayPtr\<char_t\>\&, int32_t, int32_t) मेथड

निर्दिष्ट वर्ण एरे की निर्दिष्ट रेंज का स्ट्रिंग प्रतिनिधित्व मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
static void System::Console::Write(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | वर्ण एरे |
| index | **int32_t** | उस एरे में वह सूचकांक जहाँ रेंज शुरू होती है |
| count | **int32_t** | रेंज में तत्वों की संख्या |

## Console::Write(const String\&, Args\&&...) मेथड

निर्दिष्ट फ़ॉर्मैट के अनुसार स्वरूपित निर्दिष्ट आर्ग्युमेंट्स का स्ट्रिंग प्रतिनिधित्व मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
template<class...> static void System::Console::Write(const String &format, Args &&... args)
```

### टेम्पलेट पैरामीटर

| पैरामीटर | विवरण |
| --- | --- |
| The | आउटपुट करने के लिए मानों के प्रकार |

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| format | const [String](../../string/)\& | स्ट्रिंग फ़ॉर्मैट |
| args | Args\&&... | आउटपुट करने के लिए मान |

## Console::Write(const char *) मेथड




```cpp
static void System::Console::Write(const char *)=delete
```

## देखें भी

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Class [Console](../)
* Class [Decimal](../../decimal/)
* Class [String](../../string/)
* Class [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)