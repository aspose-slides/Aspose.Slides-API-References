---
title: WriteLine()
second_title: Aspose.Slides for C++ API संदर्भ
description: वर्तमान लाइन टर्मिनेटर को मानक आउटपुट स्ट्रीम पर आउटपुट करता है।
type: docs
weight: 14
url: /hi/system/console/writeline/
---
## Console::WriteLine() विधि

Outputs the current line terminator to the standard output stream.

```cpp
static void System::Console::WriteLine()
```

## Console::WriteLine(const SharedPtr\<T\>\&) विधि

Outputs the string representation of the specified object followed by the current line terminator to the standard output stream.

```cpp
template<class T> static void System::Console::WriteLine(const SharedPtr<T> &object)
```

### टेम्पलेट पैरामीटर

| पैरामीटर | विवरण |
| --- | --- |
| T | ऑब्जेक्ट को आउटपुट करने का प्रकार |

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| object | const [SharedPtr](../../sharedptr/)\<T\>\& | [Object](../../object/) को आउटपुट करने के लिए |

## Console::WriteLine(bool) विधि

Outputs the string representation of bool value followed by the current line terminator to the standard output stream.

```cpp
static void System::Console::WriteLine(bool value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **bool** | आउटपुट करने का मान |

## Console::WriteLine(char_t) विधि

Outputs the specified character value followed by the current line terminator to the standard output stream.

```cpp
static void System::Console::WriteLine(char_t value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | char_t | आउटपुट करने का मान |

## Console::WriteLine(const ArrayPtr\<char_t\>\&) विधि

Outputs the string representation of the specified character array followed by the current line terminator to the standard output stream.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | आउटपुट करने के लिए एरे |

## Console::WriteLine(const Decimal\&) विधि

[Decimal](../../decimal/) मान का स्ट्रिंग रूपांकन, वर्तमान लाइन टर्मिनेटर के बाद, मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
static void System::Console::WriteLine(const Decimal &value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | आउटपुट करने का मान |

## Console::WriteLine(double) विधि

Outputs the string representation of double-precision floating-point value followed by the current line terminator to the standard output stream.

```cpp
static void System::Console::WriteLine(double value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **double** | आउटपुट करने का मान |

## Console::WriteLine(float) विधि

Outputs the string representation of single-precision floating-point value followed by the current line terminator to the standard output stream.

```cpp
static void System::Console::WriteLine(float value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **float** | आउटपुट करने का मान |

## Console::WriteLine(int32_t) विधि

Outputs the string representation of 32-bit integer value followed by the current line terminator to the standard output stream.

```cpp
static void System::Console::WriteLine(int32_t value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **int32_t** | आउटपुट करने का मान |

## Console::WriteLine(int64_t) विधि

Outputs the string representation of 64-bit integer value followed by the current line terminator to the standard output stream.

```cpp
static void System::Console::WriteLine(int64_t value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **int64_t** | आउटपुट करने का मान |

## Console::WriteLine(const String\&) विधि

निर्दिष्ट स्ट्रिंग ऑब्जेक्ट को वर्तमान लाइन टर्मिनेटर के बाद मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
static void System::Console::WriteLine(const String &value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | const [String](../../string/)\& | आउटपुट करने के लिए स्ट्रिंग ऑब्जेक्ट |

## Console::WriteLine(const char_t *) विधि

निर्दिष्ट c-स्ट्रिंग को वर्तमान लाइन टर्मिनेटर के बाद मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
static void System::Console::WriteLine(const char_t *value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | const char_t * | आउटपुट करने के लिए c-स्ट्रिंग |

## Console::WriteLine(const TypeInfo\&) विधि

[TypeInfo](../../typeinfo/) मान का स्ट्रिंग रूपांकन, वर्तमान लाइन टर्मिनेटर के बाद, मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
static void System::Console::WriteLine(const TypeInfo &value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | आउटपुट करने का मान |

## Console::WriteLine(uint32_t) विधि

अनसाइन्ड 32-बिट इंटेजर मान का स्ट्रिंग रूपांकन, वर्तमान लाइन टर्मिनेटर के बाद, मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
static void System::Console::WriteLine(uint32_t value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **uint32_t** | आउटपुट करने का मान |

## Console::WriteLine(uint64_t) विधि

अनसाइन्ड 64-बिट इंटेजर मान का स्ट्रिंग रूपांकन, वर्तमान लाइन टर्मिनेटर के बाद, मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
static void System::Console::WriteLine(uint64_t value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **uint64_t** | आउटपुट करने का मान |

## Console::WriteLine(const ArrayPtr\<char_t\>\&, int, int) विधि

निर्दिष्ट कैरेक्टर एरे की निर्दिष्ट रेंज का स्ट्रिंग रूपांकन, वर्तमान लाइन टर्मिनेटर के बाद, मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer, int index, int count)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | कैरेक्टर एरे |
| index | int | एरे में वह इंडेक्स जहाँ आउटपुट रेंज शुरू होती है |
| count | int | आउटपुट रेंज में तत्वों की संख्या |

## Console::WriteLine(const Exception\&) विधि

निर्दिष्ट Exception ऑब्जेक्ट का स्ट्रिंग रूपांकन, वर्तमान लाइन टर्मिनेटर के बाद, मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
static void System::Console::WriteLine(const Exception &e)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| e | const [Exception](../../exception/)\& | आउटपुट करने का मान |

## Console::WriteLine(const String\&, Args\&&...) विधि

निर्दिष्ट फ़ॉर्मेट के अनुसार स्वरूपित निर्दिष्ट तर्कों का स्ट्रिंग रूपांकन, वर्तमान लाइन टर्मिनेटर के बाद, मानक आउटपुट स्ट्रीम पर आउटपुट करता है।

```cpp
template<class...> static void System::Console::WriteLine(const String &format, Args &&... args)
```

### टेम्पलेट पैरामीटर

| पैरामीटर | विवरण |
| --- | --- |
| The | आउटपुट करने वाले मानों के प्रकार |

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| format | const [String](../../string/)\& | स्ट्रिंग फ़ॉर्मेट |
| args | Args\&&... | आउटपुट करने के मान |

## Console::WriteLine(const char *) विधि

```cpp
static void System::Console::WriteLine(const char *)=delete
```

## देखें भी

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Typedef [Exception](../../exception/)
* क्लास [Console](../)
* क्लास [Decimal](../../decimal/)
* क्लास [String](../../string/)
* क्लास [TypeInfo](../../typeinfo/)
* नेमस्पेस [System](../../)
* लाइब्रेरी [Aspose.Slides](../../../)