---
title: GetBytes()
second_title: Aspose.Slides for C++ API संदर्भ
description: निर्दिष्ट बूलियन मान को बाइट्स की एक श्रृंखला में परिवर्तित करता है।
type: docs
weight: 14
url: /hi/system/bitconverter/getbytes/
---
## BitConverter::GetBytes(bool) विधि

निर्दिष्ट बूलियन मान को बाइट्स की एक श्रृंखला में परिवर्तित करता है।

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(bool value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **bool** | परिवर्तित करने के लिए एक बूलियन मान |

### रिटर्न मान

1-बाइट ऐरे जो निर्दिष्ट मान को दर्शाता है

## BitConverter::GetBytes(char_t) विधि

निर्दिष्ट char_t मान को बाइट्स की एक श्रृंखला में परिवर्तित करता है।

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(char_t value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | char_t | परिवर्तित करने के लिए एक char_t मान |

### रिटर्न मान

2-बाइट ऐरे जो निर्दिष्ट मान को दर्शाता है

## BitConverter::GetBytes(int16_t) विधि

निर्दिष्ट 16-बिट पूर्णांक मान को बाइट्स की एक श्रृंखला में परिवर्तित करता है।

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int16_t value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **int16_t** | परिवर्तित करने के लिए एक 16-बिट पूर्णांक मान |

### रिटर्न मान

2-बाइट ऐरे जो निर्दिष्ट मान को दर्शाता है

## BitConverter::GetBytes(int) विधि

निर्दिष्ट 32-बिट पूर्णांक मान को बाइट्स की एक श्रृंखला में परिवर्तित करता है।

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | int | परिवर्तित करने के लिए एक 32-बिट पूर्णांक मान |

### रिटर्न मान

4-बाइट ऐरे जो निर्दिष्ट मान को दर्शाता है

## BitConverter::GetBytes(int64_t) विधि

निर्दिष्ट 64-बिट पूर्णांक मान को बाइट्स की एक श्रृंखला में परिवर्तित करता है।

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int64_t value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **int64_t** | परिवर्तित करने के लिए एक 64-बिट पूर्णांक मान |

### रिटर्न मान

8-बाइट ऐरे जो निर्दिष्ट मान को दर्शाता है

## BitConverter::GetBytes(uint16_t) विधि

निर्दिष्ट unsigned 16-बिट पूर्णांक मान को बाइट्स की एक श्रृंखला में परिवर्तित करता है।

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint16_t value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **uint16_t** | परिवर्तित करने के लिए एक unsigned 16-बिट पूर्णांक मान |

### रिटर्न मान

2-बाइट ऐरे जो निर्दिष्ट मान को दर्शाता है

## BitConverter::GetBytes(uint32_t) विधि

निर्दिष्ट unsigned 32-बिट पूर्णांक मान को बाइट्स की एक श्रृंखला में परिवर्तित करता है।

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint32_t value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **uint32_t** | परिवर्तित करने के लिए एक unsigned 32-बिट पूर्णांक मान |

### रिटर्न मान

4-बाइट ऐरे जो निर्दिष्ट मान को दर्शाता है

## BitConverter::GetBytes(uint64_t) विधि

निर्दिष्ट unsigned 64-बिट पूर्णांक मान को बाइट्स की एक श्रृंखला में परिवर्तित करता है।

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint64_t value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **uint64_t** | परिवर्तित करने के लिए एक unsigned 64-बिट पूर्णांक मान |

### रिटर्न मान

8-बाइट ऐरे जो निर्दिष्ट मान को दर्शाता है

## BitConverter::GetBytes(float) विधि

निर्दिष्ट सिंगल-प्रिसिशन फ़्लोटिंग-पॉइंट मान को बाइट्स की एक श्रृंखला में परिवर्तित करता है।

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(float value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **float** | परिवर्तित करने के लिए एक फ़्लोटिंग-पॉइंट मान |

### रिटर्न मान

4-बाइट ऐरे जो निर्दिष्ट मान को दर्शाता है

## BitConverter::GetBytes(double) विधि

निर्दिष्ट डबल-प्रिसिशन फ़्लोटिंग-पॉइंट मान को बाइट्स की एक श्रृंखला में परिवर्तित करता है।

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(double value)
```

### आर्ग्युमेंट्स

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | **double** | परिवर्तित करने के लिए एक फ़्लोटिंग-पॉइंट मान |

### रिटर्न मान

8-बाइट ऐरे जो निर्दिष्ट मान को दर्शाता है

## देखें

* Typedef [ArrayPtr](../../arrayptr/)
* क्लास [BitConverter](../)
* नामस्थान [System](../../)
* लाइब्रेरी [Aspose.Slides](../../../)