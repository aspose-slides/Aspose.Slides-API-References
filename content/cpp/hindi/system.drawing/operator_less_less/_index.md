---
title: operator<<()
second_title: Aspose.Slides for C++ API संदर्भ
description: UTF-8 एन्कोडिंग का उपयोग करके स्ट्रीम में डेटा डालें।
type: docs
weight: 365
url: /hi/system.drawing/operator_less_less/
---
## System::Drawing::operator<<(std::ostream\&, Color) फ़ंक्शन

UTF-8 एन्कोडिंग का उपयोग करके स्ट्रीम में डेटा डालें।

```cpp
std::ostream & System::Drawing::operator<<(std::ostream &stream, Color color)
```

### आर्ग्युमेंट्स

| परामिटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::ostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| color | [Color](../color/) | [Data](../../system.data/) डालने के लिए। |

### रिटर्न मान

**stream**.

## System::Drawing::operator<<(std::wostream\&, Color) फ़ंक्शन

स्ट्रीम में डेटा डालें।

```cpp
std::wostream & System::Drawing::operator<<(std::wostream &stream, Color color)
```

### आर्ग्युमेंट्स

| परामिटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::wostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| color | [Color](../color/) | [Data](../../system.data/) डालने के लिए। |

### रिटर्न मान

**stream**.

## System::Drawing::operator<<(std::ostream\&, const Point\&) फ़ंक्शन

UTF-8 एन्कोडिंग का उपयोग करके स्ट्रीम में डेटा डालें।

```cpp
std::ostream & System::Drawing::operator<<(std::ostream &stream, const Point &point)
```

### आर्ग्युमेंट्स

| परामिटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::ostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| point | const [Point](../point/)\& | [Data](../../system.data/) डालने के लिए। |

### रिटर्न मान

**stream**.

## System::Drawing::operator<<(std::wostream\&, const Point\&) फ़ंक्शन

स्ट्रीम में डेटा डालें।

```cpp
std::wostream & System::Drawing::operator<<(std::wostream &stream, const Point &point)
```

### आर्ग्युमेंट्स

| परामिटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::wostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| point | const [Point](../point/)\& | [Data](../../system.data/) डालने के लिए। |

### रिटर्न मान

**stream**.

## System::Drawing::operator<<(std::ostream\&, const PointF\&) फ़ंक्शन

UTF-8 एन्कोडिंग का उपयोग करके स्ट्रीम में डेटा डालें।

```cpp
std::ostream & System::Drawing::operator<<(std::ostream &stream, const PointF &point)
```

### आर्ग्युमेंट्स

| परामिटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::ostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| point | const [PointF](../pointf/)\& | [Data](../../system.data/) डालने के लिए। |

### रिटर्न मान

**stream**.

## System::Drawing::operator<<(std::wostream\&, const PointF\&) फ़ंक्शन

स्ट्रीम में डेटा डालें।

```cpp
std::wostream & System::Drawing::operator<<(std::wostream &stream, const PointF &point)
```

### आर्ग्युमेंट्स

| परामिटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::wostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| point | const [PointF](../pointf/)\& | [Data](../../system.data/) डालने के लिए। |

### रिटर्न मान

**stream**.

## System::Drawing::operator<<(std::ostream\&, const Rectangle\&) फ़ंक्शन

UTF-8 एन्कोडिंग का उपयोग करके स्ट्रीम में डेटा डालें।

```cpp
std::ostream & System::Drawing::operator<<(std::ostream &stream, const Rectangle &rectangle)
```

### आर्ग्युमेंट्स

| परामिटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::ostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| rectangle | const [Rectangle](../rectangle/)\& | [Data](../../system.data/) डालने के लिए। |

### रिटर्न मान

**stream**.

## System::Drawing::operator<<(std::wostream\&, const Rectangle\&) फ़ंक्शन

स्ट्रीम में डेटा डालें।

```cpp
std::wostream & System::Drawing::operator<<(std::wostream &stream, const Rectangle &rectangle)
```

### आर्ग्युमेंट्स

| परामिटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::wostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| rectangle | const [Rectangle](../rectangle/)\& | [Data](../../system.data/) डालने के लिए। |

### रिटर्न मान

**stream**.

## System::Drawing::operator<<(std::ostream\&, const RectangleF\&) फ़ंक्शन

UTF-8 एन्कोडिंग का उपयोग करके स्ट्रीम में डेटा डालें।

```cpp
std::ostream & System::Drawing::operator<<(std::ostream &stream, const RectangleF &rectangle)
```

### आर्ग्युमेंट्स

| परामिटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::ostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| rectangle | const [RectangleF](../rectanglef/)\& | [Data](../../system.data/) डालने के लिए। |

### रिटर्न मान

**stream**.

## System::Drawing::operator<<(std::wostream\&, const RectangleF\&) फ़ंक्शन

स्ट्रीम में डेटा डालें।

```cpp
std::wostream & System::Drawing::operator<<(std::wostream &stream, const RectangleF &rectangle)
```

### आर्ग्युमेंट्स

| परामिटर | प्रकार | विवरण |
| --- | --- | --- |
| stream | std::wostream\& | डेटा डालने के लिए आउटपुट स्ट्रीम। |
| rectangle | const [RectangleF](../rectanglef/)\& | [Data](../../system.data/) डालने के लिए। |

### रिटर्न मान

**stream**.

## देखें

* क्लास [Color](../color/)
* क्लास [Point](../point/)
* क्लास [PointF](../pointf/)
* क्लास [Rectangle](../rectangle/)
* क्लास [RectangleF](../rectanglef/)
* नेमस्पेस [System::Drawing](../)
* लाइब्रेरी [Aspose.Slides](../../)