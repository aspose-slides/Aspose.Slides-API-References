---
title: GetCharCount()
second_title: Aspose.Slides for C++ API संदर्भ
description: एक बाइट बफ़र को डिकोड करने के लिए आवश्यक वर्णों की संख्या प्राप्त करें।
type: docs
weight: 79
url: /hi/system.text/utf7encoding/getcharcount/
---
## UTF7Encoding::GetCharCount(ArrayPtr\<uint8_t\>, int, int) विधि

एक बाइट बफ़र को डिकोड करने के लिए आवश्यक वर्णों की संख्या प्राप्त करें।

```cpp
int System::Text::UTF7Encoding::GetCharCount(ArrayPtr<uint8_t> bytes, int index, int count) override
```

### आर्ग्यूमेंट्स

| Parameter | Type | Description |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | डिकोड करने के लिए बाइट्स। |
| index | int | स्लाइस की शुरुआत। |
| count | int | स्लाइस का आकार। |

### रिटर्न वैल्यू

वर्णों की संख्या।

## UTF7Encoding::GetCharCount(const uint8_t *, int) विधि

एक बाइट बफ़र को डिकोड करने के लिए आवश्यक वर्णों की संख्या प्राप्त करें।

```cpp
int System::Text::UTF7Encoding::GetCharCount(const uint8_t *bytes, int count) override
```

### आर्ग्यूमेंट्स

| Parameter | Type | Description |
| --- | --- | --- |
| bytes | const **uint8_t** * | डिकोड करने के लिए बाइट्स। |
| count | int | बाइट्स की संख्या। |

### रिटर्न वैल्यू

वर्णों की संख्या।

## UTF7Encoding::GetCharCount(ArrayPtr\<uint8_t\>, int, int) विधि

एक बाइट बफ़र को डिकोड करने के लिए आवश्यक वर्णों की संख्या प्राप्त करें।

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes, int index, int count)
```

### आर्ग्यूमेंट्स

| Parameter | Type | Description |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | डिकोड करने के लिए बाइट्स। |
| index | int | स्लाइस की शुरुआत। |
| count | int | स्लाइस का आकार। |

### रिटर्न वैल्यू

वर्णों की संख्या।

## UTF7Encoding::GetCharCount(ArrayPtr\<uint8_t\>) विधि

एक बाइट बफ़र को डिकोड करने के लिए आवश्यक वर्णों की संख्या प्राप्त करें।

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes)
```

### आर्ग्यूमेंट्स

| Parameter | Type | Description |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | डिकोड करने के लिए बाइट्स। |

### रिटर्न वैल्यू

वर्णों की संख्या।

## UTF7Encoding::GetCharCount(const uint8_t *, int) विधि

एक बाइट बफ़र को डिकोड करने के लिए आवश्यक वर्णों की संख्या प्राप्त करें।

```cpp
virtual int System::Text::Encoding::GetCharCount(const uint8_t *bytes, int count)
```

### आर्ग्यूमेंट्स

| Parameter | Type | Description |
| --- | --- | --- |
| bytes | const **uint8_t** * | डिकोड करने के लिए बाइट्स। |
| count | int | बाइट्स की संख्या। |

### रिटर्न वैल्यू

वर्णों की संख्या।

## संबंधित देखें

* टाइपडिफ [ArrayPtr](../../../system/arrayptr/)
* क्लास [UTF7Encoding](../)
* नेमस्पेस [System::Text](../../)
* लाइब्रेरी [Aspose.Slides](../../../)