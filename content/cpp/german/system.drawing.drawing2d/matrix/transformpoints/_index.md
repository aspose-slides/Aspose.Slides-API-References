---
title: TransformPoints()
second_title: Aspose.Slides für C++ API-Referenz
description: Wendet die geometrische Transformation an, die durch die von dem aktuellen Objekt repräsentierte Matrix definiert ist, auf die angegebenen Punkte an.
type: docs
weight: 196
url: /de/system.drawing.drawing2d/matrix/transformpoints/
---
## Matrix::TransformPoints(const ArrayPtr\<Point\>\&) Methode

Wendet die geometrische Transformation an, die durch die von dem aktuellen Objekt repräsentierte Matrix definiert ist, auf die angegebenen Punkte an.

```cpp
void System::Drawing::Drawing2D::Matrix::TransformPoints(const ArrayPtr<Point> &pts)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| pts | const [ArrayPtr](../../../system/arrayptr/)\<[Point](../../../system.drawing/point/)\>\& | Ein Array, das die zu transformierenden Punkte enthält |

## Matrix::TransformPoints(const System::Details::ArrayView\<Point\>\&) Methode

Wendet die geometrische Transformation an, die durch die von dem aktuellen Objekt repräsentierte Matrix definiert ist, auf die angegebenen Punkte an.

```cpp
void System::Drawing::Drawing2D::Matrix::TransformPoints(const System::Details::ArrayView<Point> &pts)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| pts | const System::Details::ArrayView\<[Point](../../../system.drawing/point/)\>\& | Ein Array-View, das die zu transformierenden Punkte enthält |

## Matrix::TransformPoints(const ArrayPtr\<PointF\>\&) Methode

Wendet die geometrische Transformation an, die durch die von dem aktuellen Objekt repräsentierte Matrix definiert ist, auf die angegebenen Punkte an.

```cpp
void System::Drawing::Drawing2D::Matrix::TransformPoints(const ArrayPtr<PointF> &pts)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| pts | const [ArrayPtr](../../../system/arrayptr/)\<[PointF](../../../system.drawing/pointf/)\>\& | Ein Array, das die zu transformierenden Punkte enthält |

## Matrix::TransformPoints(const System::Details::ArrayView\<PointF\>\&) Methode

Wendet die geometrische Transformation an, die durch die von dem aktuellen Objekt repräsentierte Matrix definiert ist, auf die angegebenen Punkte an.

```cpp
void System::Drawing::Drawing2D::Matrix::TransformPoints(const System::Details::ArrayView<PointF> &pts)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| pts | const System::Details::ArrayView\<[PointF](../../../system.drawing/pointf/)\>\& | Ein Array-View, das die zu transformierenden Punkte enthält |

## Siehe auch

* Typedef [ArrayPtr](../../../system/arrayptr/)
* Klasse [Point](../../../system.drawing/point/)
* Klasse [Matrix](../)
* Klasse [PointF](../../../system.drawing/pointf/)
* Namensraum [System::Drawing::Drawing2D](../../)
* Bibliothek [Aspose.Slides](../../../)