---
title: HostToNetworkOrder()
second_title: Aspose.Slides für C++ API-Referenz
description: Konvertiert die angegebene Host-Byte-Reihenfolge in die entsprechende Netzwerk-Byte-Reihenfolge.
type: docs
weight: 248
url: /de/system.net/ipaddress/hosttonetworkorder/
---
## IPAddress::HostToNetworkOrder(int64_t) Methode

Konvertiert die angegebene Host-Byte-Reihenfolge in die entsprechende Netzwerk-Byte-Reihenfolge.

```cpp
static int64_t System::Net::IPAddress::HostToNetworkOrder(int64_t host)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| host | **int64_t** | Der zu konvertierende Wert. |

### Rückgabewert

Die entsprechende Netzwerk-Byte-Reihenfolge.

## IPAddress::HostToNetworkOrder(int32_t) Methode

Konvertiert die angegebene Host-Byte-Reihenfolge in die entsprechende Netzwerk-Byte-Reihenfolge.

```cpp
static int32_t System::Net::IPAddress::HostToNetworkOrder(int32_t host)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| host | **int32_t** | Der zu konvertierende Wert. |

### Rückgabewert

Die entsprechende Netzwerk-Byte-Reihenfolge.

## IPAddress::HostToNetworkOrder(int16_t) Methode

Konvertiert die angegebene Host-Byte-Reihenfolge in die entsprechende Netzwerk-Byte-Reihenfolge.

```cpp
static int16_t System::Net::IPAddress::HostToNetworkOrder(int16_t host)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| host | **int16_t** | Der zu konvertierende Wert. |

### Rückgabewert

Die entsprechende Netzwerk-Byte-Reihenfolge.

## Siehe auch

* Klasse [IPAddress](../)
* Namensraum [System::Net](../../)
* Bibliothek [Aspose.Slides](../../../)