---
title: Equals()
second_title: Aspose.Slides für C++ API-Referenz
description: Bestimmt, ob die vom aktuellen Objekt und dem angegebenen Objekt dargestellten Werte gleich sind.
type: docs
weight: 313
url: /de/system/decimal/equals/
---
## Decimal::Equals(const Decimal\&) const Methode

Bestimmt, ob die vom aktuellen Objekt und dem angegebenen Objekt repräsentierten Werte gleich sind.

```cpp
bool System::Decimal::Equals(const Decimal &d) const
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| d | const [Decimal](../)\& | Das [Decimal](../)-Objekt, mit dem das aktuelle Objekt verglichen wird |

### Rückgabewert

True, wenn der vom aktuellen und dem angegebenen Objekt repräsentierte Wert gleich ist, andernfalls - false

## Decimal::Equals(const SharedPtr\<Object\>\&) const Methode

Bestimmt, ob die vom aktuellen Objekt und dem angegebenen Objekt repräsentierten Werte gleich sind.

```cpp
bool System::Decimal::Equals(const SharedPtr<Object> &obj) const
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Das Objekt, mit dem das aktuelle Objekt verglichen wird |

### Rückgabewert

True, wenn der vom aktuellen und dem angegebenen Objekt repräsentierte Wert gleich ist, andernfalls - false

## Decimal::Equals(const Decimal\&, const Decimal\&) Methode

Bestimmt, ob die von den angegebenen Objekten repräsentierten Werte gleich sind.

```cpp
static bool System::Decimal::Equals(const Decimal &d1, const Decimal &d2)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| d1 | const [Decimal](../)\& | Der erste Operand |
| d2 | const [Decimal](../)\& | Der zweite Operand |

### Rückgabewert

True, wenn die von **d1** und **d2** repräsentierten Werte gleich sind, andernfalls - false

## Siehe auch

* Typedef [SharedPtr](../../sharedptr/)
* Klasse [Decimal](../)
* Klasse [Object](../../object/)
* Namensraum [System](../../)
* Bibliothek [Aspose.Slides](../../../)