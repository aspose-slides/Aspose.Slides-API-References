---
title: Write()
second_title: Aspose.Slides für C++ API-Referenz
description: Gibt die Zeichenkettenrepräsentation des angegebenen bool-Werts an den Ausgabestream aus, der vom aktuellen Objekt repräsentiert wird.
type: docs
weight: 14
url: /de/system/consoleoutput/write/
---
## ConsoleOutput::Write(bool) Methode

Gibt die Zeichenkettenrepräsentation des angegebenen bool-Werts an den Ausgabestream aus, der vom aktuellen Objekt repräsentiert wird.

```cpp
void System::ConsoleOutput::Write(bool value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **bool** | Der auszugebende Wert |

## ConsoleOutput::Write(const SharedPtr\<Object\>\&) Methode

Gibt die Zeichenkettenrepräsentation des angegebenen Objekts an den Ausgabestream aus, der vom aktuellen Objekt repräsentiert wird.

```cpp
void System::ConsoleOutput::Write(const SharedPtr<Object> &value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Das auszugebende Objekt |

## ConsoleOutput::Write(char_t) Methode

Gibt den angegebenen Zeichenwert an den Ausgabestream aus, der vom aktuellen Objekt repräsentiert wird.

```cpp
void System::ConsoleOutput::Write(char_t value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | char_t | Der auszugebende Wert |

## ConsoleOutput::Write(Decimal) Methode

Gibt die Zeichenkettenrepräsentation des [Decimal](../../decimal/)-Werts an den Ausgabestream aus, der vom aktuellen Objekt repräsentiert wird.

```cpp
void System::ConsoleOutput::Write(Decimal value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | Der auszugebende Wert |

## ConsoleOutput::Write(double) Methode

Gibt die Zeichenkettenrepräsentation eines double-genauigkeits-Fließkommawertes an den Ausgabestream aus, der vom aktuellen Objekt repräsentiert wird.

```cpp
void System::ConsoleOutput::Write(double value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **double** | Der auszugebende Wert |

## ConsoleOutput::Write(int32_t) Methode

Gibt die Zeichenkettenrepräsentation eines 32-Bit-Ganzzahlwertes an den Ausgabestream aus, der vom aktuellen Objekt repräsentiert wird.

```cpp
void System::ConsoleOutput::Write(int32_t value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **int32_t** | Der auszugebende Wert |

## ConsoleOutput::Write(int64_t) Methode

Gibt die Zeichenkettenrepräsentation eines 64-Bit-Ganzzahlwertes an den Ausgabestream aus, der vom aktuellen Objekt repräsentiert wird.

```cpp
void System::ConsoleOutput::Write(int64_t value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **int64_t** | Der auszugebende Wert |

## ConsoleOutput::Write(float) Methode

Gibt die Zeichenkettenrepräsentation eines einfach-genauigkeits-Fließkommawertes an den Ausgabestream aus, der vom aktuellen Objekt repräsentiert wird.

```cpp
void System::ConsoleOutput::Write(float value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **float** | Der auszugebende Wert |

## ConsoleOutput::Write(const String\&) Methode

Gibt das angegebene String-Objekt an den Ausgabestream aus, der vom aktuellen Objekt repräsentiert wird.

```cpp
void System::ConsoleOutput::Write(const String &value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | const [String](../../string/)\& | Das auszugebende String-Objekt |

## ConsoleOutput::Write(uint32_t) Methode

Gibt die Zeichenkettenrepräsentation eines vorzeichenlosen 32-Bit-Ganzzahlwertes an den Ausgabestream aus, der vom aktuellen Objekt repräsentiert wird.

```cpp
void System::ConsoleOutput::Write(uint32_t value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **uint32_t** | Der auszugebende Wert |

## ConsoleOutput::Write(uint64_t) Methode

Gibt die Zeichenkettenrepräsentation eines vorzeichenlosen 64-Bit-Ganzzahlwertes an den Ausgabestream aus, der vom aktuellen Objekt repräsentiert wird.

```cpp
void System::ConsoleOutput::Write(uint64_t value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **uint64_t** | Der auszugebende Wert |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&) Methode

Gibt die Zeichenkettenrepräsentation des angegebenen Zeichen-Arrays an den Ausgabestream aus, der vom aktuellen Objekt repräsentiert wird.

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Das auszugebende Array |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&, int32_t, int32_t) Methode

Gibt die Zeichenkettenrepräsentation eines Wertebereichs des angegebenen Zeichen-Arrays an den Ausgabestream aus, der vom aktuellen Objekt repräsentiert wird.

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Das Array, das die auszugebenden Werte enthält |
| index | **int32_t** | Der Index, an dem der auszugebende Elementbereich beginnt |
| count | **int32_t** | Die Anzahl der Elemente im auszugebenden Bereich |

## ConsoleOutput::Write(const char_t *) Methode

Gibt den angegebenen C-String an den Ausgabestream aus, der vom aktuellen Objekt repräsentiert wird.

```cpp
void System::ConsoleOutput::Write(const char_t *value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | const char_t * | Der auszugebende C-String |

## ConsoleOutput::Write(const TypeInfo\&) Methode

Gibt die Zeichenkettenrepräsentation des angegebenen [TypeInfo](../../typeinfo/)-Objekts an den Ausgabestream aus, der vom aktuellen Objekt repräsentiert wird.

```cpp
void System::ConsoleOutput::Write(const TypeInfo &value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | Das [TypeInfo](../../typeinfo/)-Objekt, das ausgegeben wird |

## ConsoleOutput::Write(const char *) Methode

```cpp
void System::ConsoleOutput::Write(const char *)=delete
```

## Siehe auch

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Class [ConsoleOutput](../)
* Class [Object](../../object/)
* Class [Decimal](../../decimal/)
* Class [String](../../string/)
* Class [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)