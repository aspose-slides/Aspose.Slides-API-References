---
title: WriteLine()
second_title: Aspose.Slides für C++ API Referenz
description: Gibt das aktuelle Zeilenende an den vom aktuellen Objekt repräsentierten Ausgabestream aus.
type: docs
weight: 27
url: /de/system/consoleoutput/writeline/
---
## ConsoleOutput::WriteLine() Methode

Gibt das aktuelle Zeilenende an den vom aktuellen Objekt repräsentierten Ausgabestream aus.

```cpp
void System::ConsoleOutput::WriteLine() override
```

## ConsoleOutput::WriteLine(const SharedPtr\<Object\>\&) Methode

Gibt die Zeichenkettenrepräsentation des angegebenen Objekts gefolgt vom aktuellen Zeilenende an den vom aktuellen Objekt repräsentierten Ausgabestream aus.

```cpp
void System::ConsoleOutput::WriteLine(const SharedPtr<Object> &value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Das auszugebende Objekt |

## ConsoleOutput::WriteLine(bool) Methode

Gibt die Zeichenkettenrepräsentation des angegebenen bool-Wertes gefolgt vom aktuellen Zeilenende an den vom aktuellen Objekt repräsentierten Ausgabestream aus.

```cpp
void System::ConsoleOutput::WriteLine(bool value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **bool** | Das auszugebende Objekt |

## ConsoleOutput::WriteLine(char_t) Methode

Gibt den angegebenen Zeichenwert gefolgt vom aktuellen Zeilenende an den vom aktuellen Objekt repräsentierten Ausgabestream aus.

```cpp
void System::ConsoleOutput::WriteLine(char_t value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | char_t | Der auszugebende Wert |

## ConsoleOutput::WriteLine(Decimal) Methode

Gibt die Zeichenkettenrepräsentation des [Decimal](../../decimal/)-Wertes gefolgt vom aktuellen Zeilenende an den vom aktuellen Objekt repräsentierten Ausgabestream aus.

```cpp
void System::ConsoleOutput::WriteLine(Decimal value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | Der auszugebende Wert |

## ConsoleOutput::WriteLine(double) Methode

Gibt die Zeichenkettenrepräsentation des double-Präzisions-Gleitkommawertes gefolgt vom aktuellen Zeilenende an den vom aktuellen Objekt repräsentierten Ausgabestream aus.

```cpp
void System::ConsoleOutput::WriteLine(double value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **double** | Der auszugebende Wert |

## ConsoleOutput::WriteLine(int) Methode

Gibt die Zeichenkettenrepräsentation des 32-Bit-Ganzzahlwertes gefolgt vom aktuellen Zeilenende an den vom aktuellen Objekt repräsentierten Ausgabestream aus.

```cpp
void System::ConsoleOutput::WriteLine(int value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | int | Der auszugebende Wert |

## ConsoleOutput::WriteLine(int64_t) Methode

Gibt die Zeichenkettenrepräsentation des 64-Bit-Ganzzahlwertes gefolgt vom aktuellen Zeilenende an den vom aktuellen Objekt repräsentierten Ausgabestream aus.

```cpp
void System::ConsoleOutput::WriteLine(int64_t value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **int64_t** | Der auszugebende Wert |

## ConsoleOutput::WriteLine(float) Methode

Gibt die Zeichenkettenrepräsentation des Single-Precision-Gleitkommawertes gefolgt vom aktuellen Zeilenende an den vom aktuellen Objekt repräsentierten Ausgabestream aus.

```cpp
void System::ConsoleOutput::WriteLine(float value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **float** | Der auszugebende Wert |

## ConsoleOutput::WriteLine(const String\&) Methode

Gibt das angegebene Zeichenkettenobjekt gefolgt vom aktuellen Zeilenende an den vom aktuellen Objekt repräsentierten Ausgabestream aus.

```cpp
void System::ConsoleOutput::WriteLine(const String &value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | const [String](../../string/)\& | Das auszugebende Zeichenkettenobjekt |

## ConsoleOutput::WriteLine(uint32_t) Methode

Gibt die Zeichenkettenrepräsentation des vorzeichenlosen 32-Bit-Ganzzahlwertes gefolgt vom aktuellen Zeilenende an den vom aktuellen Objekt repräsentierten Ausgabestream aus.

```cpp
void System::ConsoleOutput::WriteLine(uint32_t value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **uint32_t** | Der auszugebende Wert |

## ConsoleOutput::WriteLine(uint64_t) Methode

Gibt die Zeichenkettenrepräsentation des vorzeichenlosen 64-Bit-Ganzzahlwertes gefolgt vom aktuellen Zeilenende an den vom aktuellen Objekt repräsentierten Ausgabestream aus.

```cpp
void System::ConsoleOutput::WriteLine(uint64_t value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **uint64_t** | Der auszugebende Wert |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&) Methode

Gibt die Zeichenkettenrepräsentation des angegebenen Zeichen-Arrays gefolgt vom aktuellen Zeilenende an den vom aktuellen Objekt repräsentierten Ausgabestream aus.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Das auszugebende Array |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&, int32_t, int32_t) Methode

Gibt die Zeichenkettenrepräsentation eines Bereichs von Werten des angegebenen Zeichen-Arrays gefolgt vom aktuellen Zeilenende an den vom aktuellen Objekt repräsentierten Ausgabestream aus.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Das Array, das die auszugebenden Werte enthält |
| index | **int32_t** | Der Index, an dem der auszugebende Bereich von Elementen beginnt |
| count | **int32_t** | Die Anzahl der auszugebenden Elemente im Bereich |

## ConsoleOutput::WriteLine(const char_t *) Methode

```cpp
void System::ConsoleOutput::WriteLine(const char_t *value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | const char_t * | Die auszugebende C-Zeichenkette |

## ConsoleOutput::WriteLine(const TypeInfo\&) Methode

Gibt die Zeichenkettenrepräsentation des angegebenen [TypeInfo](../../typeinfo/)-Objekts gefolgt vom aktuellen Zeilenende an den vom aktuellen Objekt repräsentierten Ausgabestream aus.

```cpp
void System::ConsoleOutput::WriteLine(const TypeInfo &value) override
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | Das [TypeInfo](../../typeinfo/) Objekt zum Ausgeben |

## ConsoleOutput::WriteLine(const char *) Methode

```cpp
void System::ConsoleOutput::WriteLine(const char *)=delete
```

## Siehe auch

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Klasse [ConsoleOutput](../)
* Klasse [Object](../../object/)
* Klasse [Decimal](../../decimal/)
* Klasse [String](../../string/)
* Klasse [TypeInfo](../../typeinfo/)
* Namensraum [System](../../)
* Bibliothek [Aspose.Slides](../../../)