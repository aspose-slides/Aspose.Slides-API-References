---
title: FromAscii()
second_title: Aspose.Slides für C++ API Referenz
description: Erstellt einen String aus einer ASCII-Zeichenfolge.
type: docs
weight: 950
url: /de/system/string/fromascii/
---
## String::FromAscii(const char *) Methode

Erstellt [String](../) aus einer ASCII-Zeichenfolge.

```cpp
static String System::String::FromAscii(const char *asciiStr)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| asciiStr | const char * | Zeiger auf nullterminierte Zeichenfolge, die mit dem ASCII-Codepage codiert ist. |

### Rückgabewert

[String](../) Objekt, das die übergebene Zeichenfolge darstellt.

## String::FromAscii(const char *, int) Methode

Erstellt [String](../) aus einer ASCII-Zeichenfolge.

```cpp
static String System::String::FromAscii(const char *asciiStr, int len)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| asciiStr | const char * | Zeiger auf eine Zeichenfolge, die mit dem ASCII-Codepage codiert ist. |
| len | int | Anzahl der zu verarbeitenden Zeichen. |

### Rückgabewert

[String](../) Objekt, das die übergebene Zeichenfolge darstellt.

## String::FromAscii(const std::string\&) Methode

Erstellt [String](../) aus einer ASCII-Zeichenfolge.

```cpp
static String System::String::FromAscii(const std::string &asciiStr)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| asciiStr | const std::string\& | ASCII-kodierte Zeichenfolge. |

### Rückgabewert

[String](../) Objekt, das die übergebene Zeichenfolge darstellt.

## Siehe auch

* Klasse [String](../)
* Namensraum [System](../../)
* Bibliothek [Aspose.Slides](../../../)