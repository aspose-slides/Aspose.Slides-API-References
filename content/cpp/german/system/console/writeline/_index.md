---
title: WriteLine()
second_title: Aspose.Slides für C++ API Referenz
description: Gibt das aktuelle Zeilenendezeichen an den Standardausgabestream aus.
type: docs
weight: 14
url: /de/system/console/writeline/
---
## Console::WriteLine() Methode

Gibt das aktuelle Zeilenendezeichen an den Standardausgabestream aus.

```cpp
static void System::Console::WriteLine()
```

## Console::WriteLine(const SharedPtr\<T\>\&) Methode

Gibt die Zeichenkettenrepräsentation des angegebenen Objekts aus, gefolgt vom aktuellen Zeilenendezeichen, an den Standardausgabestream aus.

```cpp
template<class T> static void System::Console::WriteLine(const SharedPtr<T> &object)
```

### Template-Parameter

| Parameter | Beschreibung |
| --- | --- |
| T | Typ des auszugebenden Objekts |

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| object | const [SharedPtr](../../sharedptr/)\<T\>\& | [Object](../../object/) auszugeben |

## Console::WriteLine(bool) Methode

Gibt die Zeichenkettenrepräsentation des bool-Werts aus, gefolgt vom aktuellen Zeilenendezeichen, an den Standardausgabestream.

```cpp
static void System::Console::WriteLine(bool value)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **bool** | Der auszugebende Wert |

## Console::WriteLine(char_t) Methode

Gibt den angegebenen Zeichenwert aus, gefolgt vom aktuellen Zeilenendezeichen, an den Standardausgabestream.

```cpp
static void System::Console::WriteLine(char_t value)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | char_t | Der auszugebende Wert |

## Console::WriteLine(const ArrayPtr\<char_t\>\&) Methode

Gibt die Zeichenkettenrepräsentation des angegebenen Zeichenarrays aus, gefolgt vom aktuellen Zeilenendezeichen, an den Standardausgabestream.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Das auszugebende Array |

## Console::WriteLine(const Decimal\&) Methode

Gibt die Zeichenkettenrepräsentation des [Decimal](../../decimal/) Wert aus, gefolgt vom aktuellen Zeilenendezeichen, an den Standardausgabestream.

```cpp
static void System::Console::WriteLine(const Decimal &value)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | Der auszugebende Wert |

## Console::WriteLine(double) Methode

Gibt die Zeichenkettenrepräsentation des double-Präzisions-Gleitkommawerts aus, gefolgt vom aktuellen Zeilenendezeichen, an den Standardausgabestream.

```cpp
static void System::Console::WriteLine(double value)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **double** | Der auszugebende Wert |

## Console::WriteLine(float) Methode

Gibt die Zeichenkettenrepräsentation des float-Präzisions-Gleitkommawerts aus, gefolgt vom aktuellen Zeilenendezeichen, an den Standardausgabestream.

```cpp
static void System::Console::WriteLine(float value)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **float** | Der auszugebende Wert |

## Console::WriteLine(int32_t) Methode

Gibt die Zeichenkettenrepräsentation des 32-Bit-Ganzzahlwerts aus, gefolgt vom aktuellen Zeilenendezeichen, an den Standardausgabestream.

```cpp
static void System::Console::WriteLine(int32_t value)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **int32_t** | Der auszugebende Wert |

## Console::WriteLine(int64_t) Methode

Gibt die Zeichenkettenrepräsentation des 64-Bit-Ganzzahlwerts aus, gefolgt vom aktuellen Zeilenendezeichen, an den Standardausgabestream.

```cpp
static void System::Console::WriteLine(int64_t value)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **int64_t** | Der auszugebende Wert |

## Console::WriteLine(const String\&) Methode

Gibt das angegebene String-Objekt aus, gefolgt vom aktuellen Zeilenendezeichen, an den Standardausgabestream.

```cpp
static void System::Console::WriteLine(const String &value)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | const [String](../../string/)\& | Das auszugebende String-Objekt |

## Console::WriteLine(const char_t *) Methode

Gibt die angegebene C-Zeichenkette aus, gefolgt vom aktuellen Zeilenendezeichen, an den Standardausgabestream.

```cpp
static void System::Console::WriteLine(const char_t *value)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | const char_t * | Die auszugebende C-Zeichenkette |

## Console::WriteLine(const TypeInfo\&) Methode

Gibt die Zeichenkettenrepräsentation des [TypeInfo](../../typeinfo/) Wert aus, gefolgt vom aktuellen Zeilenendezeichen, an den Standardausgabestream.

```cpp
static void System::Console::WriteLine(const TypeInfo &value)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | Der auszugebende Wert |

## Console::WriteLine(uint32_t) Methode

Gibt die Zeichenkettenrepräsentation des unsigned 32-Bit-Ganzzahlwerts aus, gefolgt vom aktuellen Zeilenendezeichen, an den Standardausgabestream.

```cpp
static void System::Console::WriteLine(uint32_t value)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **uint32_t** | Der auszugebende Wert |

## Console::WriteLine(uint64_t) Methode

Gibt die Zeichenkettenrepräsentation des unsigned 64-Bit-Ganzzahlwerts aus, gefolgt vom aktuellen Zeilenendezeichen, an den Standardausgabestream.

```cpp
static void System::Console::WriteLine(uint64_t value)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **uint64_t** | Der auszugebende Wert |

## Console::WriteLine(const ArrayPtr\<char_t\>\&, int, int) Methode

Gibt die Zeichenkettenrepräsentation des angegebenen Bereichs des angegebenen Zeichenarrays aus, gefolgt vom aktuellen Zeilenendezeichen, an den Standardausgabestream.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer, int index, int count)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Das Zeichenarray |
| index | int | Der Index im Array, an dem der auszugebende Bereich beginnt |
| count | int | Die Anzahl der Elemente im auszugebenden Bereich |

## Console::WriteLine(const Exception\&) Methode

Gibt die Zeichenkettenrepräsentation des angegebenen Exception-Objekts aus, gefolgt vom aktuellen Zeilenendezeichen, an den Standardausgabestream.

```cpp
static void System::Console::WriteLine(const Exception &e)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| e | const [Exception](../../exception/)\& | Der auszugebende Wert |

## Console::WriteLine(const String\&, Args\&&...) Methode

Gibt die Zeichenkettenrepräsentation der angegebenen Argumente, formatiert nach dem angegebenen Format, aus, gefolgt vom aktuellen Zeilenendezeichen, an den Standardausgabestream.

```cpp
template<class...> static void System::Console::WriteLine(const String &format, Args &&... args)
```

### Template-Parameter

| Parameter | Beschreibung |
| --- | --- |
| The | Typen der auszugebenden Werte |

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| format | const [String](../../string/)\& | Das Zeichenkettenformat |
| args | Args\&&... | Die auszugebenden Werte |

## Console::WriteLine(const char *) Methode

```cpp
static void System::Console::WriteLine(const char *)=delete
```

## Siehe auch

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Typedef [Exception](../../exception/)
* Class [Console](../)
* Class [Decimal](../../decimal/)
* Class [String](../../string/)
* Class [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)