---
title: GetBytes()
second_title: Aspose.Slides für C++ API Referenz
description: Konvertiert den angegebenen booleschen Wert in ein Byte-Array.
type: docs
weight: 14
url: /de/system/bitconverter/getbytes/
---
## BitConverter::GetBytes(bool) Methode

Konvertiert den angegebenen booleschen Wert in ein Byte-Array.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(bool value)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **bool** | Ein boolescher Wert zum Konvertieren |

### Rückgabewert

1-byte Array, das den angegebenen Wert darstellt

## BitConverter::GetBytes(char_t) Methode

Konvertiert den angegebenen char_t-Wert in ein Byte-Array.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(char_t value)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | char_t | Ein char_t-Wert zum Konvertieren |

### Rückgabewert

2-byte Array, das den angegebenen Wert darstellt

## BitConverter::GetBytes(int16_t) Methode

Konvertiert den angegebenen 16-Bit-Integerwert in ein Byte-Array.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int16_t value)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **int16_t** | Ein 16-Bit-Integerwert zum Konvertieren |

### Rückgabewert

2-byte Array, das den angegebenen Wert darstellt

## BitConverter::GetBytes(int) Methode

Konvertiert den angegebenen 32-Bit-Integerwert in ein Byte-Array.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int value)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | int | Ein 32-Bit-Integerwert zum Konvertieren |

### Rückgabewert

4-byte Array, das den angegebenen Wert darstellt

## BitConverter::GetBytes(int64_t) Methode

Konvertiert den angegebenen 64-Bit-Integerwert in ein Byte-Array.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int64_t value)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **int64_t** | Ein 64-Bit-Integerwert zum Konvertieren |

### Rückgabewert

8-byte Array, das den angegebenen Wert darstellt

## BitConverter::GetBytes(uint16_t) Methode

Konvertiert den angegebenen vorzeichenlosen 16-Bit-Integerwert in ein Byte-Array.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint16_t value)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **uint16_t** | Ein vorzeichenloser 16-Bit-Integerwert zum Konvertieren |

### Rückgabewert

2-byte Array, das den angegebenen Wert darstellt

## BitConverter::GetBytes(uint32_t) Methode

Konvertiert den angegebenen vorzeichenlosen 32-Bit-Integerwert in ein Byte-Array.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint32_t value)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **uint32_t** | Ein vorzeichenloser 32-Bit-Integerwert zum Konvertieren |

### Rückgabewert

4-byte Array, das den angegebenen Wert darstellt

## BitConverter::GetBytes(uint64_t) Methode

Konvertiert den angegebenen vorzeichenlosen 64-Bit-Integerwert in ein Byte-Array.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint64_t value)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **uint64_t** | Ein vorzeichenloser 64-Bit-Integerwert zum Konvertieren |

### Rückgabewert

8-byte Array, das den angegebenen Wert darstellt

## BitConverter::GetBytes(float) Methode

Konvertiert den angegebenen single-Precision-Gleitkommawert in ein Byte-Array.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(float value)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **float** | Ein Gleitkommawert zum Konvertieren |

### Rückgabewert

4-byte Array, das den angegebenen Wert darstellt

## BitConverter::GetBytes(double) Methode

Konvertiert den angegebenen double-Precision-Gleitkommawert in ein Byte-Array.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(double value)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | **double** | Ein Gleitkommawert zum Konvertieren |

### Rückgabewert

8-byte Array, das den angegebenen Wert darstellt

## Siehe auch

* Typedef [ArrayPtr](../../arrayptr/)
* Klasse [BitConverter](../)
* Namensraum [System](../../)
* Bibliothek [Aspose.Slides](../../../)