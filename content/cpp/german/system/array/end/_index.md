---
title: end()
second_title: Aspose.Slides für C++ API-Referenz
description: Gibt einen Iterator zurück, der auf das Element zeigt, das dem letzten Element des Containers folgt. Dieses Element dient als Platzhalter; der Versuch, darauf zuzugreifen, führt zu undefiniertem Verhalten.
type: docs
weight: 443
url: /de/system/array/end/
---
## Array::end() Methode

Gibt einen iterator zurück, der auf das Element zeigt, das dem letzten Element des Containers folgt. Dieses Element fungiert als Platzhalter; der Versuch, darauf zuzugreifen, führt zu undefiniertem Verhalten.

```cpp
iterator System::Array<T>::end() noexcept
```

### Rückgabewert

Ein iterator, der auf das theoretische Element zeigt, das nach dem letzten Element des Containers platziert ist.

## Array::end() const Methode

Gibt einen iterator zurück, der auf das Element zeigt, das dem letzten Element des const-qualifizierten Containers folgt. Dieses Element fungiert als Platzhalter; der Versuch, darauf zuzugreifen, führt zu undefiniertem Verhalten.

```cpp
const_iterator System::Array<T>::end() const noexcept
```

### Rückgabewert

Ein iterator, der auf das theoretische Element zeigt, das nach dem letzten Element des const-qualifizierten Containers platziert ist.

## Siehe auch

* Typedef [iterator](../iterator/)
* Typedef [const_iterator](../const_iterator/)
* Klasse [Array](../)
* Namensraum [System](../../)
* Bibliothek [Aspose.Slides](../../../)