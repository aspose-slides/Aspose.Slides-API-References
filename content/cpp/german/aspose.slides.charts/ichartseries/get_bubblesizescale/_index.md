---
title: get_BubbleSizeScale()
second_title: Aspose.Slides für C++ API-Referenz
description: Gibt den Skalierungsfaktor für das Blasendiagramm an (kann zwischen 0 und 300 Prozent der Standardgröße liegen). Diese Eigenschaft gilt nicht nur für diese Serie, sondern für alle Serien der übergeordneten Seriengruppe – dies ist die Projektion der entsprechenden Gruppeneigenschaft. Daher ist diese Eigenschaft schreibgeschützt. Verwenden Sie die Eigenschaft ParentSeriesGroup, um auf die übergeordnete Seriengruppe zuzugreifen. Verwenden Sie get_ParentSeriesGroup()->get(set)_BubbleSizeScale() Lese-/Schreib-Eigenschaft, um den Wert zu ändern.
type: docs
weight: 612
url: /de/aspose.slides.charts/ichartseries/get_bubblesizescale/
---
## IChartSeries::get_BubbleSizeScale() Methode

Gibt den Skalierungsfaktor für das Blasendiagramm an (kann zwischen 0 und 300 Prozent der Standardgröße liegen). Dies ist die Eigenschaft nicht nur dieser Serie, sondern aller Serien der übergeordneten Seriengruppe – dies ist die Projektion der entsprechenden Gruppeneigenschaft. Daher ist diese Eigenschaft schreibgeschützt. Verwenden Sie die Eigenschaft ParentSeriesGroup, um auf die übergeordnete Seriengruppe zuzugreifen. Verwenden Sie [get_ParentSeriesGroup()](../get_parentseriesgroup/)->get(set)_BubbleSizeScale() Lese-/Schreib-Eigenschaft, um den Wert zu ändern.

```cpp
virtual int32_t Aspose::Slides::Charts::IChartSeries::get_BubbleSizeScale()=0
```

## Anmerkungen

Dies ist die Projektion der Eigenschaft [get_ParentSeriesGroup()](../get_parentseriesgroup/)->get(set)_BubbleSizeScale().

## Siehe auch

* Klasse [IChartSeries](../)
* Namensraum [Aspose::Slides::Charts](../../)
* Bibliothek [Aspose.Slides](../../../)