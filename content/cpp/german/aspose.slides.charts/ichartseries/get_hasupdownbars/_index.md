---
title: get_HasUpDownBars()
second_title: Aspose.Slides für C++ API-Referenz
description: Bestimmt, ob ein Linien- oder Aktien-Diagramm Auf-/Ab-Balken hat. Dies ist die Eigenschaft nicht nur dieser Serie, sondern aller Serien der übergeordneten Seriengruppe - dies ist die Projektion der entsprechenden Gruppeneigenschaft. Daher ist diese Eigenschaft schreibgeschützt. Verwenden Sie die Eigenschaft ParentSeriesGroup, um auf die übergeordnete Seriengruppe zuzugreifen. Verwenden Sie get_ParentSeriesGroup()->get_UpDownBars()->get(set)_HasUpDownBars() Lese/Schreib-Eigenschaft, um den Wert zu ändern. Verwenden Sie die Eigenschaft get_ParentSeriesGroup()->get_UpDownBars() zur Formatierung von Auf-/Ab-Balken. Nur lesbar bool.
type: docs
weight: 625
url: /de/aspose.slides.charts/ichartseries/get_hasupdownbars/
---
## IChartSeries::get_HasUpDownBars() Methode

Bestimmt, ob ein Linien- oder Aktien-Diagramm Auf/Ab-Balken hat. Dies ist die Eigenschaft nicht nur dieser Serie, sondern aller Serien der übergeordneten Seriengruppe – dies ist die Projektion der entsprechenden Gruppeneigenschaft. Und daher ist diese Eigenschaft schreibgeschützt. Verwenden Sie die Eigenschaft ParentSeriesGroup, um auf die übergeordnete Seriengruppe zuzugreifen. Verwenden Sie [get_ParentSeriesGroup()](../get_parentseriesgroup/)->get_UpDownBars()->get(set)_HasUpDownBars() Lese/Schreib-Eigenschaft, um den Wert zu ändern. Verwenden Sie die Eigenschaft [get_ParentSeriesGroup()](../get_parentseriesgroup/)->get_UpDownBars() zur Formatierung von Auf/Ab-Balken. Nur lesbar **bool**.

```cpp
virtual bool Aspose::Slides::Charts::IChartSeries::get_HasUpDownBars()=0
```

## Hinweise

Dies ist die Projektion der Eigenschaft [get_ParentSeriesGroup()](../get_parentseriesgroup/)->get_UpDownBars()->get(set)_HasUpDownBars().

## Siehe auch

* Klasse [IChartSeries](../)
* Namensraum [Aspose::Slides::Charts](../../)
* Bibliothek [Aspose.Slides](../../../)