---
title: BitArray()
second_title: Aspose.Slides für C++ API-Referenz
description: Kopierkonstruktor.
type: docs
weight: 14
url: /de/system.collections/bitarray/bitarray/
---
## BitArray::BitArray(const bitset\&) Konstruktor

Kopierkonstruktor.

```cpp
System::Collections::BitArray::BitArray(const bitset &bits)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| bits | const [bitset](../bitset/)\& | Container, aus dem die Daten kopiert werden. |

## BitArray::BitArray(const BitArray\&) Konstruktor

Kopierkonstruktor.

```cpp
System::Collections::BitArray::BitArray(const BitArray &bits)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| bits | const [BitArray](../)\& | Container, aus dem die Daten kopiert werden. |

## BitArray::BitArray(const BitArrayPtr\&) Konstruktor

Kopierkonstruktor.

```cpp
System::Collections::BitArray::BitArray(const BitArrayPtr &array)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| array | const [BitArrayPtr](../../bitarrayptr/)\& | [BitArray](../) zum Kopieren von Daten. |

## BitArray::BitArray(const System::ArrayPtr\<bool\>\&) Konstruktor

Kopierkonstruktor.

```cpp
System::Collections::BitArray::BitArray(const System::ArrayPtr<bool> &values)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| values | const [System::ArrayPtr](../../../system/arrayptr/)\<**bool**\>\& | Container, aus dem die Daten elementweise kopiert werden. |

## BitArray::BitArray(const System::ArrayPtr\<uint8_t\>\&) Konstruktor

Kopierkonstruktor.

```cpp
System::Collections::BitArray::BitArray(const System::ArrayPtr<uint8_t> &bytes)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| bytes | const [System::ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\>\& | Container, aus dem die Daten bitweise kopiert werden. |

## BitArray::BitArray(const System::ArrayPtr\<int\>\&) Konstruktor

Kopierkonstruktor.

```cpp
System::Collections::BitArray::BitArray(const System::ArrayPtr<int> &values)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| values | const [System::ArrayPtr](../../../system/arrayptr/)\<int\>\& | Container, aus dem die Daten bitweise kopiert werden. |

## BitArray::BitArray(int, bool) Konstruktor

Füllkonstruktor.

```cpp
System::Collections::BitArray::BitArray(int length, bool defaultValue=false)
```

### Argumente

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| length | int | Anzahl der zu füllenden Elemente. |
| defaultValue | **bool** | Füllwert. |

## Siehe auch

* Typedef [bitset](../bitset/)
* Typedef [ArrayPtr](../../../system/arrayptr/)
* Klasse [BitArray](../)
* Klasse [BitArrayPtr](../../bitarrayptr/)
* Namensraum [System::Collections](../../)
* Bibliothek [Aspose.Slides](../../../)