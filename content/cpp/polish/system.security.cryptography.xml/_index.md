---
title: "System::Security::Cryptography::Xml"
second_title: "Referencja API Aspose.Slides dla C++"
description: 
type: docs
weight: 872
url: /pl/system.security.cryptography.xml/
---
## Klasy

| Klasa | Opis |
| --- | --- |
| [DataObject](./dataobject/) | Definiuje niezależny od formatu mechanizm transferu danych. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy wykonania i/lub naruszenia asercji. Zawsze opakuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i użyj tego wskaźnika do przekazywania jej jako argumentu do funkcji. |
| [DSAKeyValue](./dsakeyvalue/) | Reprezentuje [DSA](../system.security.cryptography/dsa/) klucz prywatny. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy wykonania i/lub naruszenia asercji. Zawsze opakuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i użyj tego wskaźnika do przekazywania jej jako argumentu do funkcji. |
| [KeyInfo](./keyinfo/) | Reprezentuje [KeyInfo](./keyinfo/) element podpisu cyfrowego XML lub szyfrowania XML. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy wykonania i/lub naruszenia asercji. Zawsze opakuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i użyj tego wskaźnika do przekazywania jej jako argumentu do funkcji. |
| [KeyInfoClause](./keyinfoclause/) | Wszystkie implementacje podelementów [KeyInfo](./keyinfo/) muszą dziedziczyć tę klasę abstrakcyjną. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy wykonania i/lub naruszenia asercji. Zawsze opakuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i użyj tego wskaźnika do przekazywania jej jako argumentu do funkcji. |
| [KeyInfoName](./keyinfoname/) | Reprezentuje podelement XMLDSIG 'KeyName' lub element XML szyfrowania '[KeyInfo](./keyinfo/)'. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy wykonania i/lub naruszenia asercji. Zawsze opakuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i użyj tego wskaźnika do przekazywania jej jako argumentu do funkcji. |
| [KeyInfoX509Data](./keyinfox509data/) | Reprezentuje element 'X509Data'. Zawiera informacje o certyfikacie X.509v3 związane z kluczem weryfikacji lub szyfrowania. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy wykonania i/lub naruszenia asercji. Zawsze opakuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i użyj tego wskaźnika do przekazywania jej jako argumentu do funkcji. |
| [Reference](./reference/) | Umożliwia tworzenie podpisów XML. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy wykonania i/lub naruszenia asercji. Zawsze opakuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i użyj tego wskaźnika do przekazywania jej jako argumentu do funkcji. |
| [RSAKeyValue](./rsakeyvalue/) | Reprezentuje element '[RSAKeyValue](./rsakeyvalue/)' podpisu XML. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy wykonania i/lub naruszenia asercji. Zawsze opakuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i użyj tego wskaźnika do przekazywania jej jako argumentu do funkcji. |
| [SignedInfo](./signedinfo/) | Reprezentuje element '[SignedInfo](./signedinfo/)' podpisu XML. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy wykonania i/lub naruszenia asercji. Zawsze opakuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i użyj tego wskaźnika do przekazywania jej jako argumentu do funkcji. |
| [SignedXml](./signedxml/) | Używany do podpisywania i weryfikacji XML. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy wykonania i/lub naruszenia asercji. Zawsze opakuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i użyj tego wskaźnika do przekazywania jej jako argumentu do funkcji. |
| [Transform](./transform/) | Dostarcza informacji o przekształcaniu danych przez podpisującego. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy wykonania i/lub naruszenia asercji. Zawsze opakuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i użyj tego wskaźnika do przekazywania jej jako argumentu do funkcji. |
| [TransformChain](./transformchain/) | Reprezentuje uporządkowany łańcuch przekształceń. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy wykonania i/lub naruszenia asercji. Zawsze opakuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i użyj tego wskaźnika do przekazywania jej jako argumentu do funkcji. |
| [X509IssuerSerial](./x509issuerserial/) | Reprezentuje element '[X509IssuerSerial](./x509issuerserial/)' podpisu XML. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy wykonania i/lub naruszenia asercji. Zawsze opakuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i użyj tego wskaźnika do przekazywania jej jako argumentu do funkcji. |
| [XmlDsigC14NTransform](./xmldsigc14ntransform/) | Reprezentuje transformację kanonizacji XML C14N dla podpisu cyfrowego bez komentarzy. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy wykonania i/lub naruszenia asercji. Zawsze opakuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i użyj tego wskaźnika do przekazywania jej jako argumentu do funkcji. |
| [XmlDsigC14NWithCommentsTransform](./xmldsigc14nwithcommentstransform/) | Reprezentuje transformację kanonizacji XML C14N dla podpisu cyfrowego z komentarzami. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy wykonania i/lub naruszenia asercji. Zawsze opakuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i użyj tego wskaźnika do przekazywania jej jako argumentu do funkcji. |
| [XmlDsigEnvelopedSignatureTransform](./xmldsigenvelopedsignaturetransform/) | Reprezentuje transformację podpisu osadzonego dla podpisu XML. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy wykonania i/lub naruszenia asercji. Zawsze opakuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i użyj tego wskaźnika do przekazywania jej jako argumentu do funkcji. |
| [XmlDsigExcC14NTransform](./xmldsigexcc14ntransform/) | Reprezentuje wyłączną transformację kanonizacji XML C14N dla podpisu cyfrowego bez komentarzy. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy wykonania i/lub naruszenia asercji. Zawsze opakuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i użyj tego wskaźnika do przekazywania jej jako argumentu do funkcji. |