---
title: WriteLine()
second_title: Aspose.Slides dla C++ – odniesienie API
description: Zapisuje znaki końca linii do strumienia.
type: docs
weight: 118
url: /pl/system.io/textwriter/writeline/
---
## TextWriter::WriteLine() metoda

Zapisuje znaki końca linii do strumienia.

```cpp
virtual void System::IO::TextWriter::WriteLine()
```

## TextWriter::WriteLine(const SharedPtr\<Object\>\&) metoda

Zapisuje reprezentację tekstową określonego obiektu, po czym zapisuje znaki końca linii do strumienia.

```cpp
virtual void System::IO::TextWriter::WriteLine(const SharedPtr<Object> &value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | const [SharedPtr](../../../system/sharedptr/)\<[Object](../../../system/object/)\>\& | Obiekt do zapisania |

## TextWriter::WriteLine(bool) metoda

Zapisuje reprezentację tekstową określonej wartości logicznej, po czym zapisuje znaki końca linii do strumienia.

```cpp
virtual void System::IO::TextWriter::WriteLine(bool value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **bool** | Wartość do zapisania |

## TextWriter::WriteLine(char_t) metoda

Zapisuje określony znak, po czym zapisuje znaki końca linii do strumienia.

```cpp
virtual void System::IO::TextWriter::WriteLine(char_t value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | char_t | Wartość do zapisania |

## TextWriter::WriteLine(Decimal) metoda

Zapisuje reprezentację tekstową określonego obiektu [Decimal](../../../system/decimal/), po czym zapisuje znaki końca linii do strumienia.

```cpp
virtual void System::IO::TextWriter::WriteLine(Decimal value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | [Decimal](../../../system/decimal/) | Obiekt do zapisania |

## TextWriter::WriteLine(double) metoda

Zapisuje reprezentację tekstową określonej liczby zmiennoprzecinkowej podwójnej precyzji, po czym zapisuje znaki końca linii do strumienia.

```cpp
virtual void System::IO::TextWriter::WriteLine(double value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **double** | Wartość do zapisania |

## TextWriter::WriteLine(int) metoda

Zapisuje reprezentację tekstową określonej 32-bitowej wartości całkowitej, po czym zapisuje znaki końca linii do strumienia.

```cpp
virtual void System::IO::TextWriter::WriteLine(int value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | int | Wartość do zapisania |

## TextWriter::WriteLine(int64_t) metoda

Zapisuje reprezentację tekstową określonej 64-bitowej wartości całkowitej, po czym zapisuje znaki końca linii do strumienia.

```cpp
virtual void System::IO::TextWriter::WriteLine(int64_t value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **int64_t** | Wartość do zapisania |

## TextWriter::WriteLine(float) metoda

Zapisuje reprezentację tekstową określonej liczby zmiennoprzecinkowej o pojedynczej precyzji, po czym zapisuje znaki końca linii do strumienia.

```cpp
virtual void System::IO::TextWriter::WriteLine(float value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **float** | Wartość do zapisania |

## TextWriter::WriteLine(const String\&) metoda

Zapisuje określony ciąg znaków, po czym zapisuje znaki końca linii do strumienia.

```cpp
virtual void System::IO::TextWriter::WriteLine(const String &value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | const [String](../../../system/string/)\& | Ciąg znaków do zapisania |

## TextWriter::WriteLine(uint32_t) metoda

Zapisuje reprezentację tekstową określonej 32-bitowej liczby całkowitej bez znaku, po czym zapisuje znaki końca linii do strumienia.

```cpp
virtual void System::IO::TextWriter::WriteLine(uint32_t value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **uint32_t** | Wartość do zapisania |

## TextWriter::WriteLine(uint64_t) metoda

Zapisuje reprezentację tekstową określonej 64-bitowej liczby całkowitej bez znaku, po czym zapisuje znaki końca linii do strumienia.

```cpp
virtual void System::IO::TextWriter::WriteLine(uint64_t value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **uint64_t** | Wartość do zapisania |

## TextWriter::WriteLine(const ArrayPtr\<char_t\>\&) metoda

Zapisuje wszystkie znaki z określonej tablicy, po czym zapisuje znaki końca linii do strumienia.

```cpp
virtual void System::IO::TextWriter::WriteLine(const ArrayPtr<char_t> &buffer)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../../system/arrayptr/)\<char_t\>\& | Tablica zawierająca znaki do zapisania |

## TextWriter::WriteLine(const ArrayPtr\<char_t\>\&, int32_t, int32_t) metoda

Zapisuje określony podzakres znaków UTF-16 z podanej tablicy znaków, po czym zapisuje znaki końca linii do strumienia.

```cpp
virtual void System::IO::TextWriter::WriteLine(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../../system/arrayptr/)\<char_t\>\& | Tablica zawierająca znaki do zapisania |
| index | **int32_t** | Indeks zerowy (liczony od 0) elementu w **buffer**, od którego podzakres do zapisu się zaczyna |
| count | **int32_t** | Liczba znaków w podzakresie do zapisu; -1 oznacza, że podzakres kończy się na końcu tablicy **buffer** |

## TextWriter::WriteLine(const char_t *) metoda

Zapisuje określony ciąg znaków typu C, po czym zapisuje znaki końca linii do strumienia.

```cpp
virtual void System::IO::TextWriter::WriteLine(const char_t *value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | const char_t * | Ciąg znaków typu C do zapisania |

## TextWriter::WriteLine(const TypeInfo\&) metoda

Zapisuje reprezentację tekstową określonego obiektu [TypeInfo](../../../system/typeinfo/), po czym zapisuje znaki końca linii do strumienia.

```cpp
virtual void System::IO::TextWriter::WriteLine(const TypeInfo &value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | const [TypeInfo](../../../system/typeinfo/)\& | Obiekt do zapisania |

## TextWriter::WriteLine(const String\&, const TArgs\&...) metoda

Zapisuje określone wartości sformatowane według podanego formatu, po czym zapisuje znaki końca linii do strumienia.

```cpp
template<class...> void System::IO::TextWriter::WriteLine(const String &format, const TArgs &... args)
```

### Parametry szablonu

| Parametr | Opis |
| --- | --- |
| TArgs | Lista typów wartości do zapisania |

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| format | const [String](../../../system/string/)\& | Format ciągu znaków |
| args | const TArgs\&... | Wartości do zapisania |

## Zobacz także

* Typedef [SharedPtr](../../../system/sharedptr/)
* Typedef [ArrayPtr](../../../system/arrayptr/)
* Class [TextWriter](../)
* Class [Object](../../../system/object/)
* Class [Decimal](../../../system/decimal/)
* Class [String](../../../system/string/)
* Class [TypeInfo](../../../system/typeinfo/)
* Namespace [System::IO](../../)
* Library [Aspose.Slides](../../../)