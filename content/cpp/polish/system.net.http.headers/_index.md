---
title: "System::Net::Http::Headers"
second_title: Aspose.Slides dla C++ – Referencja API
description: 
type: docs
weight: 690
url: /pl/system.net.http.headers/
---
## Klasy

| Klasa | Opis |
| --- | --- |
| [AuthenticationHeaderValue](./authenticationheadervalue/) | Reprezentuje wartości nagłówków 'Authorization', 'ProxyAuthorization', 'WWW-Authenticate' i 'Proxy-Authenticate'. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [CacheControlHeaderValue](./cachecontrolheadervalue/) | Reprezentuje wartość nagłówka 'Cache-Control'. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [ContentDispositionHeaderValue](./contentdispositionheadervalue/) | Reprezentuje wartość nagłówka 'Content-Disposition'. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [ContentRangeHeaderValue](./contentrangeheadervalue/) | Reprezentuje wartość nagłówka 'Content-Range'. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [EntityTagHeaderValue](./entitytagheadervalue/) | Reprezentuje wartość nagłówka 'Entity-Tag'. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [HttpContentHeaders](./httpcontentheaders/) | Reprezentuje kolekcję nagłówków 'Content'. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [HttpHeaders](./httpheaders/) | Kolekcja nagłówków HTTP. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [HttpHeaderValueCollection](./httpheadervaluecollection/) | Reprezentuje kolekcję wartości nagłówków. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [HttpHeaderValueCollection< System::String >](./httpheadervaluecollection_tmpl_system_string__end_tmpl/) | Częściowa specjalizacja szablonu [HttpHeaderValueCollection](./httpheadervaluecollection/) dla typu [String](../system/string/). Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [HttpRequestHeaders](./httprequestheaders/) | Reprezentuje kolekcję nagłówków 'Request'. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [HttpResponseHeaders](./httpresponseheaders/) | Reprezentuje kolekcję nagłówków 'Response'. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [MediaTypeHeaderValue](./mediatypeheadervalue/) | Reprezentuje typ MIME w wartości nagłówka 'Content-Type'. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [MediaTypeWithQualityHeaderValue](./mediatypewithqualityheadervalue/) | Reprezentuje typ MIME z dodatkowym współczynnikiem jakości w wartości nagłówka 'Content-Type'. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [NameValueHeaderValue](./namevalueheadervalue/) | Reprezentuje parę klucz/wartość do użycia w nagłówkach. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [NameValueWithParametersHeaderValue](./namevaluewithparametersheadervalue/) | Reprezentuje parę klucz/wartość z parametrami do użycia w nagłówkach. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [ObjectCollection](./objectcollection/) | Reprezentuje kolekcję obiektów. |
| [ProductHeaderValue](./productheadervalue/) | Reprezentuje token produktu w wartości nagłówka 'User-Agent'. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [ProductInfoHeaderValue](./productinfoheadervalue/) | Reprezentuje produkt lub komentarz w wartości nagłówka 'User-Agent'. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [RangeConditionHeaderValue](./rangeconditionheadervalue/) | Reprezentuje wartość nagłówka 'If-Range'. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [RangeHeaderValue](./rangeheadervalue/) | Reprezentuje wartość nagłówka '[Range](../system/range/)'. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [RangeItemHeaderValue](./rangeitemheadervalue/) | Reprezentuje zakres bajtów w wartości nagłówka '[Range](../system/range/)'. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [RetryConditionHeaderValue](./retryconditionheadervalue/) | Reprezentuje wartość nagłówka 'Retry-After'. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [StringWithQualityHeaderValue](./stringwithqualityheadervalue/) | Reprezentuje wartość z dodatkową jakością nagłówka tekstowego. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [TransferCodingHeaderValue](./transfercodingheadervalue/) | Reprezentuje wartość nagłówka 'Accept-Encoding'. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [TransferCodingWithQualityHeaderValue](./transfercodingwithqualityheadervalue/) | Reprezentuje wartość z dodatkową jakością nagłówka 'Accept-Encoding'. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [ViaHeaderValue](./viaheadervalue/) | Reprezentuje wartość nagłówka 'Via'. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [WarningHeaderValue](./warningheadervalue/) | Reprezentuje wartość nagłówka 'Warning'. Obiekty tej klasy powinny być przydzielane wyłącznie przy użyciu funkcji [System::MakeObject()](../system/makeobject/). Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operatora new, ponieważ spowoduje to błędy czasu wykonania i/lub błędy asercji. Zawsze opakowuj tę klasę w wskaźnik [System::SmartPtr](../system/smartptr/) i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
## Typedefy

| Definicja typu | Opis |
| --- | --- |
| [HeaderFunc](./headerfunc/) |  |