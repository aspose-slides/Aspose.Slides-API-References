---
title: "System::Data"
second_title: Aspose.Slides dla C++ – dokumentacja API
description: 
type: docs
weight: 430
url: /pl/system.data/
---
## Klasy

| Klasa | Opis |
| --- | --- |
| [DataColumn](./datacolumn/) | Opisuje kolumnę w danych. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu [System::MakeObject()](../system/makeobject/) funkcji. Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operator new, ponieważ spowoduje to błędy czasu wykonywania i/lub naruszenia asercji. Zawsze opakuj tę klasę w [System::SmartPtr](../system/smartptr/) wskaźnik i używaj tego wskaźnika do przekazywania go do funkcji jako argument. |
| [DataColumnCollection](./datacolumncollection/) | Zawiera kolekcję kolumn danych. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu [System::MakeObject()](../system/makeobject/) funkcji. Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operator new, ponieważ spowoduje to błędy czasu wykonywania i/lub naruszenia asercji. Zawsze opakuj tę klasę w [System::SmartPtr](../system/smartptr/) wskaźnik i używaj tego wskaźnika do przekazywania jej do funkcji jako argument. |
| [DataRelation](./datarelation/) | Opisuje relację rodzic-dziecko między tabelami. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu [System::MakeObject()](../system/makeobject/) funkcji. Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operator new, ponieważ spowoduje to błędy czasu wykonywania i/lub naruszenia asercji. Zawsze opakuj tę klasę w [System::SmartPtr](../system/smartptr/) wskaźnik i używaj tego wskaźnika do przekazywania jej do funkcji jako argument. |
| [DataRelationCollection](./datarelationcollection/) | Zbiór zależności między tabelami. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu [System::MakeObject()](../system/makeobject/) funkcji. Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operator new, ponieważ spowoduje to błędy czasu wykonywania i/lub naruszenia asercji. Zawsze opakuj tę klasę w [System::SmartPtr](../system/smartptr/) wskaźnik i używaj tego wskaźnika do przekazywania jej do funkcji jako argument. |
| [DataRow](./datarow/) | Wiersz w zestawie danych. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu [System::MakeObject()](../system/makeobject/) funkcji. Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operator new, ponieważ spowoduje to błędy czasu wykonywania i/lub naruszenia asercji. Zawsze opakuj tę klasę w [System::SmartPtr](../system/smartptr/) wskaźnik i używaj tego wskaźnika do przekazywania jej do funkcji jako argument. |
| [DataRowCollection](./datarowcollection/) | Zbiór wierszy danych. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu [System::MakeObject()](../system/makeobject/) funkcji. Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operator new, ponieważ spowoduje to błędy czasu wykonywania i/lub naruszenia asercji. Zawsze opakuj tę klasę w [System::SmartPtr](../system/smartptr/) wskaźnik i używaj tego wskaźnika do przekazywania jej do funkcji jako argument. |
| [DataRowView](./datarowview/) | Widok nad wierszem danych. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu [System::MakeObject()](../system/makeobject/) funkcji. Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operator new, ponieważ spowoduje to błędy czasu wykonywania i/lub naruszenia asercji. Zawsze opakuj tę klasę w [System::SmartPtr](../system/smartptr/) wskaźnik i używaj tego wskaźnika do przekazywania jej do funkcji jako argument. |
| [DataSet](./dataset/) | Zbiór danych przechowywanych w tabelach posiadających pewne relacje między sobą. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu [System::MakeObject()](../system/makeobject/) funkcji. Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operator new, ponieważ spowoduje to błędy czasu wykonywania i/lub naruszenia asercji. Zawsze opakuj tę klasę w [System::SmartPtr](../system/smartptr/) wskaźnik i używaj tego wskaźnika do przekazywania jej do funkcji jako argument. |
| [DataTable](./datatable/) | [Data](./) ustrukturyzowane w wierszach i kolumnach. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu [System::MakeObject()](../system/makeobject/) funkcji. Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operator new, ponieważ spowoduje to błędy czasu wykonywania i/lub naruszenia asercji. Zawsze opakuj tę klasę w [System::SmartPtr](../system/smartptr/) wskaźnik i używaj tego wskaźnika do przekazywania jej do funkcji jako argument. |
| [DataTableCollection](./datatablecollection/) | Zbiór tabel danych. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu [System::MakeObject()](../system/makeobject/) funkcji. Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operator new, ponieważ spowoduje to błędy czasu wykonywania i/lub naruszenia asercji. Zawsze opakuj tę klasę w [System::SmartPtr](../system/smartptr/) wskaźnik i używaj tego wskaźnika do przekazywania jej do funkcji jako argument. |
| [DataView](./dataview/) | Widok pracujący na tabeli. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu [System::MakeObject()](../system/makeobject/) funkcji. Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operator new, ponieważ spowoduje to błędy czasu wykonywania i/lub naruszenia asercji. Zawsze opakuj tę klasę w [System::SmartPtr](../system/smartptr/) wskaźnik i używaj tego wskaźnika do przekazywania jej do funkcji jako argument. |
| [IDataReader](./idatareader/) | Interfejs do odczytu kolejnych danych. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu [System::MakeObject()](../system/makeobject/) funkcji. Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operator new, ponieważ spowoduje to błędy czasu wykonywania i/lub naruszenia asercji. Zawsze opakuj tę klasę w [System::SmartPtr](../system/smartptr/) wskaźnik i używaj tego wskaźnika do przekazywania jej do funkcji jako argument. |
| [IDataRecord](./idatarecord/) | Interfejs do zapisu z kolumnami. Obiekty tej klasy powinny być alokowane wyłącznie przy użyciu [System::MakeObject()](../system/makeobject/) funkcji. Nigdy nie twórz instancji tego typu na stosie ani przy użyciu operator new, ponieważ spowoduje to błędy czasu wykonywania i/lub naruszenia asercji. Zawsze opakuj tę klasę w [System::SmartPtr](../system/smartptr/) wskaźnik i używaj tego wskaźnika do przekazywania jej do funkcji jako argument. |