---
title: set_OnlyLoadDocumentProperties()
second_title: Aspose.Slides dla C++ - dokumentacja API
description: Ta właściwość ma sens, jeśli plik prezentacji jest zabezpieczony hasłem. Wartość true oznacza, że z zaszyfrowanego pliku prezentacji należy załadować tylko właściwości dokumentu, a hasło ma być zignorowane. Wartość false oznacza, że cała zaszyfrowana prezentacja musi zostać załadowana przy użyciu właściwego hasła. Jeśli prezentacja nie jest zaszyfrowana, wartość właściwości jest zawsze ignorowana. Jeśli właściwości dokumentu zaszyfrowanego pliku nie są publiczne i wartość właściwości jest true, wtedy właściwości dokumentu nie mogą zostać załadowane i zostanie zgłoszony wyjątek. Zapisuje **bool**.
type: docs
weight: 144
url: /pl/aspose.slides/iloadoptions/set_onlyloaddocumentproperties/
---
## ILoadOptions::set_OnlyLoadDocumentProperties(bool) metoda

Ta właściwość ma sens, jeśli plik prezentacji jest chroniony hasłem. Wartość true oznacza, że z zaszyfrowanego pliku prezentacji należy załadować tylko właściwości dokumentu i hasło ma zostać zignorowane. Wartość false oznacza, że cała zaszyfrowana prezentacja musi zostać załadowana przy użyciu właściwego hasła. Jeśli prezentacja nie jest zaszyfrowana, wartość właściwości jest zawsze ignorowana. Jeśli właściwości dokumentu zaszyfrowanego pliku nie są publiczne i wartość właściwości jest true, wtedy właściwości dokumentu nie mogą zostać załadowane i zostanie zgłoszony wyjątek. Zapisuje **bool**.

```cpp
virtual void Aspose::Slides::ILoadOptions::set_OnlyLoadDocumentProperties(bool value)=0
```

## Zobacz także

* Klasa [ILoadOptions](../)
* Przestrzeń nazw [Aspose::Slides](../../)
* Biblioteka [Aspose.Slides](../../../)