---
title: Write()
second_title: Aspose.Slides dla C++ Referencja API
description: Wyświetla reprezentację tekstową określonej wartości bool na strumieniu wyjściowym reprezentowanym przez bieżący obiekt.
type: docs
weight: 14
url: /pl/system/consoleoutput/write/
---
## ConsoleOutput::Write(bool) metoda

Wyświetla reprezentację tekstową określonej wartości bool na strumieniu wyjściowym reprezentowanym przez bieżący obiekt.

```cpp
void System::ConsoleOutput::Write(bool value) override
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **bool** | Wartość do wypisania |

## ConsoleOutput::Write(const SharedPtr\<Object\>\&) metoda

Wyświetla reprezentację tekstową określonego obiektu na strumieniu wyjściowym reprezentowanym przez bieżący obiekt.

```cpp
void System::ConsoleOutput::Write(const SharedPtr<Object> &value) override
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Obiekt do wypisania |

## ConsoleOutput::Write(char_t) metoda

Wyświetla określoną wartość znaku na strumieniu wyjściowym reprezentowanym przez bieżący obiekt.

```cpp
void System::ConsoleOutput::Write(char_t value) override
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | char_t | Wartość do wypisania |

## ConsoleOutput::Write(Decimal) metoda

Wyświetla reprezentację tekstową wartości [Decimal](../../decimal/) na strumieniu wyjściowym reprezentowanym przez bieżący obiekt.

```cpp
void System::ConsoleOutput::Write(Decimal value) override
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | Wartość do wypisania |

## ConsoleOutput::Write(double) metoda

Wyświetla reprezentację tekstową wartości zmiennoprzecinkowej podwójnej precyzji na strumieniu wyjściowym reprezentowanym przez bieżący obiekt.

```cpp
void System::ConsoleOutput::Write(double value) override
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **double** | Wartość do wypisania |

## ConsoleOutput::Write(int32_t) metoda

Wyświetla reprezentację tekstową 32-bitowej wartości całkowitej na strumieniu wyjściowym reprezentowanym przez bieżący obiekt.

```cpp
void System::ConsoleOutput::Write(int32_t value) override
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **int32_t** | Wartość do wypisania |

## ConsoleOutput::Write(int64_t) metoda

Wyświetla reprezentację tekstową 64-bitowej wartości całkowitej na strumieniu wyjściowym reprezentowanym przez bieżący obiekt.

```cpp
void System::ConsoleOutput::Write(int64_t value) override
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **int64_t** | Wartość do wypisania |

## ConsoleOutput::Write(float) metoda

Wyświetla reprezentację tekstową wartości zmiennoprzecinkowej pojedynczej precyzji na strumieniu wyjściowym reprezentowanym przez bieżący obiekt.

```cpp
void System::ConsoleOutput::Write(float value) override
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **float** | Wartość do wypisania |

## ConsoleOutput::Write(const String\&) metoda

Wyświetla określony obiekt ciągu znaków na strumieniu wyjściowym reprezentowanym przez bieżący obiekt.

```cpp
void System::ConsoleOutput::Write(const String &value) override
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | const [String](../../string/)\& | Obiekt ciągu znaków do wypisania |

## ConsoleOutput::Write(uint32_t) metoda

Wyświetla reprezentację tekstową 32-bitowej liczby bez znaku na strumieniu wyjściowym reprezentowanym przez bieżący obiekt.

```cpp
void System::ConsoleOutput::Write(uint32_t value) override
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **uint32_t** | Wartość do wypisania |

## ConsoleOutput::Write(uint64_t) metoda

Wyświetla reprezentację tekstową 64-bitowej liczby bez znaku na strumieniu wyjściowym reprezentowanym przez bieżący obiekt.

```cpp
void System::ConsoleOutput::Write(uint64_t value) override
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **uint64_t** | Wartość do wypisania |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&) metoda

Wyświetla reprezentację tekstową określonej tablicy znaków na strumieniu wyjściowym reprezentowanym przez bieżący obiekt.

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer) override
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Tablica do wypisania |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&, int32_t, int32_t) metoda

Wyświetla reprezentację tekstową zakresu wartości określonej tablicy znaków na strumieniu wyjściowym reprezentowanym przez bieżący obiekt.

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Tablica zawierająca wartości do wypisania |
| index | **int32_t** | Indeks, od którego rozpoczyna się zakres elementów do wypisania |
| count | **int32_t** | Liczba elementów w zakresie elementów do wypisania |

## ConsoleOutput::Write(const char_t *) metoda

Wyświetla określony c-ciąg znaków na strumieniu wyjściowym reprezentowanym przez bieżący obiekt.

```cpp
void System::ConsoleOutput::Write(const char_t *value) override
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | const char_t * | C-ciąg znaków do wypisania |

## ConsoleOutput::Write(const TypeInfo\&) metoda

Wyświetla reprezentację tekstową określonego obiektu [TypeInfo](../../typeinfo/) na strumieniu wyjściowym reprezentowanym przez bieżący obiekt.

```cpp
void System::ConsoleOutput::Write(const TypeInfo &value) override
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | Obiekt [TypeInfo](../../typeinfo/) do wypisania |

## ConsoleOutput::Write(const char *) metoda




```cpp
void System::ConsoleOutput::Write(const char *)=delete
```

## Zobacz także

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Klasa [ConsoleOutput](../)
* Klasa [Object](../../object/)
* Klasa [Decimal](../../decimal/)
* Klasa [String](../../string/)
* Klasa [TypeInfo](../../typeinfo/)
* Przestrzeń nazw [System](../../)
* Biblioteka [Aspose.Slides](../../../)