---
title: WriteLine()
second_title: Aspose.Slides dla C++ – odniesienie API
description: Wysyła bieżący separator wierszy do standardowego strumienia wyjściowego.
type: docs
weight: 14
url: /pl/system/console/writeline/
---
## Console::WriteLine() metoda

Wypisuje bieżący znak końca wiersza na standardowy strumień wyjścia.

```cpp
static void System::Console::WriteLine()
```

## Console::WriteLine(const SharedPtr\<T\>\&) metoda

Wypisuje ciągową reprezentację określonego obiektu, po której następuje bieżący znak końca wiersza, na standardowy strumień wyjścia.

```cpp
template<class T> static void System::Console::WriteLine(const SharedPtr<T> &object)
```

### Parametry szablonu

| Parametr | Opis |
| --- | --- |
| T | Typ obiektu do wypisania |

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| object | const [SharedPtr](../../sharedptr/)\<T\>\& | [Object](../../object/) do wypisania |

## Console::WriteLine(bool) metoda

Wypisuje ciągową reprezentację wartości logicznej, po której następuje bieżący znak końca wiersza, na standardowy strumień wyjścia.

```cpp
static void System::Console::WriteLine(bool value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **bool** | Wartość do wypisania |

## Console::WriteLine(char_t) metoda

Wypisuje określoną wartość znaku, po której następuje bieżący znak końca wiersza, na standardowy strumień wyjścia.

```cpp
static void System::Console::WriteLine(char_t value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | char_t | Wartość do wypisania |

## Console::WriteLine(const ArrayPtr\<char_t\>\&) metoda

Wypisuje ciągową reprezentację określonej tablicy znaków, po której następuje bieżący znak końca wiersza, na standardowy strumień wyjścia.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Tablica do wypisania |

## Console::WriteLine(const Decimal\&) metoda

Wypisuje ciągową reprezentację wartości [Decimal](../../decimal/), po której następuje bieżący znak końca wiersza, na standardowy strumień wyjścia.

```cpp
static void System::Console::WriteLine(const Decimal &value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | Wartość do wypisania |

## Console::WriteLine(double) metoda

Wypisuje ciągową reprezentację wartości zmiennoprzecinkowej podwójnej precyzji, po której następuje bieżący znak końca wiersza, na standardowy strumień wyjścia.

```cpp
static void System::Console::WriteLine(double value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **double** | Wartość do wypisania |

## Console::WriteLine(float) metoda

Wypisuje ciągową reprezentację wartości zmiennoprzecinkowej pojedynczej precyzji, po której następuje bieżący znak końca wiersza, na standardowy strumień wyjścia.

```cpp
static void System::Console::WriteLine(float value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **float** | Wartość do wypisania |

## Console::WriteLine(int32_t) metoda

Wypisuje ciągową reprezentację 32-bitowej liczby całkowitej, po której następuje bieżący znak końca wiersza, na standardowy strumień wyjścia.

```cpp
static void System::Console::WriteLine(int32_t value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **int32_t** | Wartość do wypisania |

## Console::WriteLine(int64_t) metoda

Wypisuje ciągową reprezentację 64-bitowej liczby całkowitej, po której następuje bieżący znak końca wiersza, na standardowy strumień wyjścia.

```cpp
static void System::Console::WriteLine(int64_t value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **int64_t** | Wartość do wypisania |

## Console::WriteLine(const String\&) metoda

Wypisuje określony obiekt string, po którym następuje bieżący znak końca wiersza, na standardowy strumień wyjścia.

```cpp
static void System::Console::WriteLine(const String &value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | const [String](../../string/)\& | Obiekt string do wypisania |

## Console::WriteLine(const char_t *) metoda

Wypisuje określony ciąg znaków C, po którym następuje bieżący znak końca wiersza, na standardowy strumień wyjścia.

```cpp
static void System::Console::WriteLine(const char_t *value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | const char_t * | C-łańcuch do wypisania |

## Console::WriteLine(const TypeInfo\&) metoda

Wypisuje ciągową reprezentację wartości [TypeInfo](../../typeinfo/), po której następuje bieżący znak końca wiersza, na standardowy strumień wyjścia.

```cpp
static void System::Console::WriteLine(const TypeInfo &value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | Wartość do wypisania |

## Console::WriteLine(uint32_t) metoda

Wypisuje ciągową reprezentację 32-bitowej liczby całkowitej bez znaku, po której następuje bieżący znak końca wiersza, na standardowy strumień wyjścia.

```cpp
static void System::Console::WriteLine(uint32_t value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **uint32_t** | Wartość do wypisania |

## Console::WriteLine(uint64_t) metoda

Wypisuje ciągową reprezentację 64-bitowej liczby całkowitej bez znaku, po której następuje bieżący znak końca wiersza, na standardowy strumień wyjścia.

```cpp
static void System::Console::WriteLine(uint64_t value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **uint64_t** | Wartość do wypisania |

## Console::WriteLine(const ArrayPtr\<char_t\>\&, int, int) metoda

Wypisuje ciągową reprezentację określonego zakresu podanej tablicy znaków, po której następuje bieżący znak końca wiersza, na standardowy strumień wyjścia.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer, int index, int count)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Tablica znaków |
| index | int | Indeks w tablicy, od którego zaczyna się zakres do wypisania |
| count | int | Liczba elementów w zakresie do wypisania |

## Console::WriteLine(const Exception\&) metoda

Wypisuje ciągową reprezentację określonego obiektu Exception, po której następuje bieżący znak końca wiersza, na standardowy strumień wyjścia.

```cpp
static void System::Console::WriteLine(const Exception &e)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| e | const [Exception](../../exception/)\& | Wartość do wypisania |

## Console::WriteLine(const String\&, Args\&&...) metoda

Wypisuje ciągową reprezentację określonych argumentów sformatowaną zgodnie z podanym formatem, po której następuje bieżący znak końca wiersza, na standardowy strumień wyjścia.

```cpp
template<class...> static void System::Console::WriteLine(const String &format, Args &&... args)
```

### Parametry szablonu

| Parametr | Opis |
| --- | --- |
| Typy | typy wartości do wypisania |

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| format | const [String](../../string/)\& | Format ciągu |
| args | Args\&&... | Wartości do wypisania |

## Console::WriteLine(const char *) metoda

```cpp
static void System::Console::WriteLine(const char *)=delete
```

## Zobacz także

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Typedef [Exception](../../exception/)
* Class [Console](../)
* Class [Decimal](../../decimal/)
* Class [String](../../string/)
* Class [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)