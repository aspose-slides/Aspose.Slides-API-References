---
title: Equals()
second_title: Aspose.Slides dla C++ Dokumentacja API
description: Określa, czy wartości reprezentowane przez bieżący obiekt i określony obiekt są równe.
type: docs
weight: 313
url: /pl/system/decimal/equals/
---
## Decimal::Equals(const Decimal\&) const metoda


Określa, czy wartości reprezentowane przez bieżący obiekt i określony obiekt są równe.

```cpp
bool System::Decimal::Equals(const Decimal &d) const
```


### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| d | const [Decimal](../)\& | Obiekt [Decimal](../) służący do porównania bieżącego obiektu |

### Wartość zwracana

Prawda, jeśli wartość reprezentowana przez bieżące i określone obiekty jest równa, w przeciwnym razie - fałsz

## Decimal::Equals(const SharedPtr\<Object\>\&) const metoda


Określa, czy wartości reprezentowane przez bieżący obiekt i określony obiekt są równe.

```cpp
bool System::Decimal::Equals(const SharedPtr<Object> &obj) const
```


### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Obiekt służący do porównania bieżącego obiektu |

### Wartość zwracana

Prawda, jeśli wartość reprezentowana przez bieżące i określone obiekty jest równa, w przeciwnym razie - fałsz

## Decimal::Equals(const Decimal\&, const Decimal\&) metoda


Określa, czy wartości reprezentowane przez określone obiekty są równe.

```cpp
static bool System::Decimal::Equals(const Decimal &d1, const Decimal &d2)
```


### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| d1 | const [Decimal](../)\& | Pierwszy porównywany |
| d2 | const [Decimal](../)\& | Drugi porównywany |

### Wartość zwracana

Prawda, jeśli wartości reprezentowane przez **d1** i **d2** są równe, w przeciwnym razie - fałsz

## Zobacz także

* Typedef [SharedPtr](../../sharedptr/)
* Klasa [Decimal](../)
* Klasa [Object](../../object/)
* Przestrzeń nazw [System](../../)
* Biblioteka [Aspose.Slides](../../../)