---
title: WeakReference()
second_title: Aspose.Slides dla C++ – Dokumentacja API
description: Konstruktor domyślny.
type: docs
weight: 1
url: /pl/system/weakreference_tmpl_end_tmpl/weakreference/
---
## WeakReference<>::WeakReference() metoda

Konstruktor domyślny.

```cpp
System::WeakReference<>::WeakReference()
```

## WeakReference<>::WeakReference(std::nullptr_t) metoda

Konstruktor z nullptr.

```cpp
System::WeakReference<>::WeakReference(std::nullptr_t)
```

## WeakReference<>::WeakReference(const SmartPtr\<Object\>\&) metoda

Inicjalizuje nową instancję klasy WeakReference, odwołującą się do określonego obiektu.

```cpp
System::WeakReference<>::WeakReference(const SmartPtr<Object> &data)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<[Object](../../object/)\>\& | [Object](../../object/) do przechowywania. |

## WeakReference<>::WeakReference(const SmartPtr\<Object\>\&, bool) metoda

Inicjalizuje nową instancję klasy WeakReference, odwołującą się do określonego obiektu.

```cpp
System::WeakReference<>::WeakReference(const SmartPtr<Object> &data, bool trackResurrection)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| data | const [SmartPtr](../../smartptr/)\<[Object](../../object/)\>\& | [Object](../../object/) do przechowywania. |
| trackResurrection | **bool** | Ignorowane. |

## Zobacz także

* Klasa [WeakReference<>](../)
* Klasa [SmartPtr](../../smartptr/)
* Klasa [Object](../../object/)
* Przestrzeń nazw [System](../../)
* Biblioteka [Aspose.Slides](../../../)