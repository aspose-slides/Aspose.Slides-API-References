---
title: GetBytes()
second_title: Aspose.Slides dla C++ – odniesienie API
description: Konwertuje określoną wartość logiczną na tablicę bajtów.
type: docs
weight: 14
url: /pl/system/bitconverter/getbytes/
---
## BitConverter::GetBytes(bool) metoda

Konwertuje podaną wartość typu **bool** na tablicę bajtów.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(bool value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **bool** | Wartość bool do konwersji |

### Wartość zwracana

1-bajtowa tablica reprezentująca podaną wartość

## BitConverter::GetBytes(char_t) metoda

Konwertuje podaną wartość typu **char_t** na tablicę bajtów.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(char_t value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | char_t | Wartość char_t do konwersji |

### Wartość zwracana

2-bajtowa tablica reprezentująca podaną wartość

## BitConverter::GetBytes(int16_t) metoda

Konwertuje podaną 16-bitową wartość całkowitą typu **int16_t** na tablicę bajtów.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int16_t value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **int16_t** | 16-bitowa wartość całkowita do konwersji |

### Wartość zwracana

2-bajtowa tablica reprezentująca podaną wartość

## BitConverter::GetBytes(int) metoda

Konwertuje podaną 32-bitową wartość całkowitą typu **int** na tablicę bajtów.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | int | 32-bitowa wartość całkowita do konwersji |

### Wartość zwracana

4-bajtowa tablica reprezentująca podaną wartość

## BitConverter::GetBytes(int64_t) metoda

Konwertuje podaną 64-bitową wartość całkowitą typu **int64_t** na tablicę bajtów.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int64_t value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **int64_t** | 64-bitowa wartość całkowita do konwersji |

### Wartość zwracana

8-bajtowa tablica reprezentująca podaną wartość

## BitConverter::GetBytes(uint16_t) metoda

Konwertuje podaną 16-bitową wartość całkowitą bez znaku typu **uint16_t** na tablicę bajtów.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint16_t value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **uint16_t** | 16-bitowa wartość całkowita bez znaku do konwersji |

### Wartość zwracana

2-bajtowa tablica reprezentująca podaną wartość

## BitConverter::GetBytes(uint32_t) metoda

Konwertuje podaną 32-bitową wartość całkowitą bez znaku typu **uint32_t** na tablicę bajtów.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint32_t value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **uint32_t** | 32-bitowa wartość całkowita bez znaku do konwersji |

### Wartość zwracana

4-bajtowa tablica reprezentująca podaną wartość

## BitConverter::GetBytes(uint64_t) metoda

Konwertuje podaną 64-bitową wartość całkowitą bez znaku typu **uint64_t** na tablicę bajtów.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint64_t value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **uint64_t** | 64-bitowa wartość całkowita bez znaku do konwersji |

### Wartość zwracana

8-bajtowa tablica reprezentująca podaną wartość

## BitConverter::GetBytes(float) metoda

Konwertuje podaną wartość zmiennoprzecinkową pojedynczej precyzji typu **float** na tablicę bajtów.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(float value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **float** | Wartość zmiennoprzecinkowa do konwersji |

### Wartość zwracana

4-bajtowa tablica reprezentująca podaną wartość

## BitConverter::GetBytes(double) metoda

Konwertuje podaną wartość zmiennoprzecinkową podwójnej precyzji typu **double** na tablicę bajtów.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(double value)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| value | **double** | Wartość zmiennoprzecinkowa do konwersji |

### Wartość zwracana

8-bajtowa tablica reprezentująca podaną wartość

## Zobacz także

* Typedef [ArrayPtr](../../arrayptr/)
* Klasa [BitConverter](../)
* Przestrzeń nazw [System](../../)
* Biblioteka [Aspose.Slides](../../../)