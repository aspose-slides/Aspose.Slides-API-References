---
title: IsControl()
second_title: Aspose.Slides dla C++ – Dokumentacja API
description: Określa, czy znak podanym indeksem w określonym buforze znaków jest klasyfikowany jako znak kontrolny Unicode.
type: docs
weight: 66
url: /pl/system/char/iscontrol/
---
## Char::IsControl(const char_t *, int) metoda

Określa, czy znak pod wskazanym indeksem w podanym buforze znaków jest klasyfikowany jako znak kontrolny Unicode.

```cpp
static bool System::Char::IsControl(const char_t *str, int idx)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| str | const char_t * | Wskaźnik na początek bufora znaków |
| idx | int | Indeks zerowy w określonym buforze wskazujący na znak do sprawdzenia |

### Wartość zwracana

True jeśli znak pod wskazanym indeksem jest znakiem kontrolnym Unicode, w przeciwnym razie - false

## Char::IsControl(char_t) metoda

Określa, czy podany znak jest klasyfikowany jako znak kontrolny Unicode.

```cpp
static bool System::Char::IsControl(char_t c)
```

### Argumenty

| Parametr | Typ | Opis |
| --- | --- | --- |
| c | char_t | Badany znak |

### Wartość zwracana

True jeśli podany znak jest znakiem kontrolnym Unicode, w przeciwnym razie - false

## Zobacz także

* Klasa [Char](../)
* Przestrzeń nazw [System](../../)
* Biblioteka [Aspose.Slides](../../../)