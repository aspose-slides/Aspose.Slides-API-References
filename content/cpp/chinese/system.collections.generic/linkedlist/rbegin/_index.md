---
title: rbegin()
second_title: Aspose.Slides 的 C++ API 参考
description: 获取指向集合中最后一个元素的反向迭代器（反向的第一个）。
type: docs
weight: 287
url: /zh/system.collections.generic/linkedlist/rbegin/
---
## LinkedList::rbegin() 方法

获取指向集合中最后一个元素的反向迭代器（反向的第一个）。

```cpp
reverse_iterator System::Collections::Generic::LinkedList<T>::rbegin() noexcept
```

### 返回值

返回指向链表中最后一个元素的反向迭代器（反向的第一个）。

## LinkedList::rbegin() const 方法

获取指向 const 限定的集合中最后一个元素的反向迭代器（反向的第一个）。

```cpp
const_reverse_iterator System::Collections::Generic::LinkedList<T>::rbegin() const noexcept
```

### 返回值

返回指向 const 限定的链表中最后一个元素的反向迭代器（反向的第一个）。

## 参见

* Typedef [reverse_iterator](../reverse_iterator/)
* Typedef [const_reverse_iterator](../const_reverse_iterator/)
* Class [LinkedList](../)
* Namespace [System::Collections::Generic](../../)
* Library [Aspose.Slides](../../../)