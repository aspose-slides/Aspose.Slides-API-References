---
title: rbegin()
second_title: Aspose.Slides for C++ API 参考
description: 获取指向集合中最后一个元素（逆序中的第一个）的反向迭代器。
type: docs
weight: 92
url: /zh/system.collections.generic/sortedlist/rbegin/
---
## SortedList::rbegin() 方法

获取指向集合中最后一个元素（逆序中的第一个）的反向迭代器。

```cpp
reverse_iterator System::Collections::Generic::SortedList<TKey, TValue>::rbegin() noexcept
```

### 返回值

指向映射中最后一个元素（逆序中的第一个）的反向迭代器。

## SortedList::rbegin() const 方法

获取指向 const 限定集合中最后一个元素（逆序中的第一个）的反向迭代器。

```cpp
const_reverse_iterator System::Collections::Generic::SortedList<TKey, TValue>::rbegin() const noexcept
```

### 返回值

指向 const 限定映射中最后一个元素（逆序中的第一个）的反向迭代器。

## 另请参见

* 类型定义 [reverse_iterator](../reverse_iterator/)
* 类型定义 [const_reverse_iterator](../const_reverse_iterator/)
* 类 [SortedList](../)
* 命名空间 [System::Collections::Generic](../../)
* 库 [Aspose.Slides](../../../)