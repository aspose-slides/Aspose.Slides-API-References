---
title: end()
second_title: Aspose.Slides for C++ API 参考
description: 获取指向集合中最后一个元素（如果有）的迭代器，位置在该元素之后。
type: docs
weight: 92
url: /zh/aspose.slides.theme/effectstylecollection/end/
---
## EffectStyleCollection::end() 方法


获取指向集合中最后一个元素（如果有）的迭代器，位置在该元素之后。

```cpp
iterator Aspose::Slides::Theme::EffectStyleCollection::end() noexcept
```


### 返回值

指向集合中最后一个元素（如果有）的迭代器，位置在该元素之后

## EffectStyleCollection::end() const 方法


获取指向常量限定实例中最后一个元素（如果有）的迭代器，位置在该元素之后。

```cpp
const_iterator Aspose::Slides::Theme::EffectStyleCollection::end() const noexcept
```


### 返回值

指向常量限定实例中最后一个元素（如果有）的迭代器，位置在该元素之后

## 另请参见

* 类型定义 [iterator](../iterator/)
* 类型定义 [const_iterator](../const_iterator/)
* 类 [EffectStyleCollection](../)
* 命名空间 [Aspose::Slides::Theme](../../)
* 库 [Aspose.Slides](../../../)