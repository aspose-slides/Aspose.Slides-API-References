---
title: end()
second_title: Aspose.Slides for C++ API 参考
description: 获取指向集合中最后一个元素（如果有）之后的迭代器。
type: docs
weight: 92
url: /zh/aspose.slides.theme/fillformatcollection/end/
---
## FillFormatCollection::end() 方法

获取指向集合中最后一个元素（如果有）之后的迭代器。

```cpp
iterator Aspose::Slides::Theme::FillFormatCollection::end() noexcept
```

### 返回值

一个指向集合中最后一个元素（如果有）之后的迭代器

## FillFormatCollection::end() const 方法

获取指向集合的 const 限定实例中最后一个元素（如果有）之后的迭代器。

```cpp
const_iterator Aspose::Slides::Theme::FillFormatCollection::end() const noexcept
```

### 返回值

一个指向 const 限定实例中最后一个元素（如果有）之后的迭代器

## 另请参见

* 类型定义 [iterator](../iterator/)
* 类型定义 [const_iterator](../const_iterator/)
* 类 [FillFormatCollection](../)
* 命名空间 [Aspose::Slides::Theme](../../)
* 库 [Aspose.Slides](../../../)