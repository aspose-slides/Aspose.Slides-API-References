---
title: rbegin()
second_title: Aspose.Slides C++ API 参考
description: 获取指向集合中最后一个元素的逆向迭代器（逆向中的第一个）。
type: docs
weight: 209
url: /zh/system.collections.objectmodel/collection/rbegin/
---
## Collection::rbegin() 方法

获取指向集合中最后一个元素的逆向迭代器（逆向中的第一个）。

```cpp
reverse_iterator System::Collections::ObjectModel::Collection<T>::rbegin() noexcept
```

### 返回值

指向列表中最后一个元素的逆向迭代器（逆向中的第一个）。

## Collection::rbegin() const 方法

获取指向常量限定集合中最后一个元素的逆向迭代器（逆向中的第一个）。

```cpp
const_reverse_iterator System::Collections::ObjectModel::Collection<T>::rbegin() const noexcept
```

### 返回值

指向常量限定列表中最后一个元素的逆向迭代器（逆向中的第一个）。

## 另请参见

* 类型定义 [reverse_iterator](../reverse_iterator/)
* 类型定义 [const_reverse_iterator](../const_reverse_iterator/)
* 类 [Collection](../)
* 命名空间 [System::Collections::ObjectModel](../../)
* 库 [Aspose.Slides](../../../)