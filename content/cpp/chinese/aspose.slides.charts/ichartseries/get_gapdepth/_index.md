---
title: get_GapDepth()
second_title: Aspose.Slides for C++ API 参考
description: 返回数据系列在 3D 图表中之间的距离，作为标记宽度的百分比。该属性不仅属于本系列，还属于父系列组的所有系列——这是相应组属性的投影。因此该属性是只读的。使用 ParentSeriesGroup 属性访问父系列组。使用 get_ParentSeriesGroup()->get(set)_GapDepth() 读/写属性来更改值。只读 int32_t.
type: docs
weight: 651
url: /zh/aspose.slides.charts/ichartseries/get_gapdepth/
---
## IChartSeries::get_GapDepth() 方法

返回数据系列在 3D 图表中之间的距离，作为标记宽度的百分比。该属性不仅属于本系列，还属于父系列组的所有系列——这是相应组属性的投影。因此该属性是只读的。使用 ParentSeriesGroup 属性访问父系列组。使用 [get_ParentSeriesGroup()](../get_parentseriesgroup/)->get(set)_GapDepth() 读/写属性来更改值。只读 **int32_t**。

```cpp
virtual int32_t Aspose::Slides::Charts::IChartSeries::get_GapDepth()=0
```

## 备注

这是属性 [get_ParentSeriesGroup()](../get_parentseriesgroup/)->get(set)_GapDepth() 的投影。

## 另见

* 类 [IChartSeries](../)
* 命名空间 [Aspose::Slides::Charts](../../)
* 库 [Aspose.Slides](../../../)