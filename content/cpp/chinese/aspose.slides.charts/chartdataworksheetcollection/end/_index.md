---
title: end()
second_title: Aspose.Slides C++ API 参考
description: 获取指向集合中最后一个元素（如果有）之后的迭代器。
type: docs
weight: 92
url: /zh/aspose.slides.charts/chartdataworksheetcollection/end/
---
## ChartDataWorksheetCollection::end() 方法

获取指向集合中最后一个元素（如果有）之后的迭代器。

```cpp
iterator Aspose::Slides::Charts::ChartDataWorksheetCollection::end() noexcept
```

### 返回值

指向集合中最后一个元素（如果有）之后的迭代器

## ChartDataWorksheetCollection::end() const 方法

获取指向 const 限定实例的集合中最后一个元素（如果有）之后的迭代器。

```cpp
const_iterator Aspose::Slides::Charts::ChartDataWorksheetCollection::end() const noexcept
```

### 返回值

指向 const 限定实例的集合中最后一个元素（如果有）之后的迭代器

## 另见

* 类型定义 [iterator](../iterator/)
* 类型定义 [const_iterator](../const_iterator/)
* 类 [ChartDataWorksheetCollection](../)
* 命名空间 [Aspose::Slides::Charts](../../)
* 库 [Aspose.Slides](../../../)