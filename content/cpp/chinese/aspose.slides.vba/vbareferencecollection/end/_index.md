---
title: end()
second_title: Aspose.Slides for C++ API 参考
description: 获取指向集合中最后一个元素之后（如果有的话）的迭代器。
type: docs
weight: 105
url: /zh/aspose.slides.vba/vbareferencecollection/end/
---
## VbaReferenceCollection::end() 方法

获取指向集合中最后一个元素之后（如果有的话）的迭代器。

```cpp
iterator Aspose::Slides::Vba::VbaReferenceCollection::end() noexcept
```

### 返回值

一个指向集合中最后一个元素之后（如果有的话）的迭代器

## VbaReferenceCollection::end() const 方法

获取指向集合的 const 限定实例中最后一个元素之后（如果有的话）的迭代器。

```cpp
const_iterator Aspose::Slides::Vba::VbaReferenceCollection::end() const noexcept
```

### 返回值

一个指向集合的 const 限定实例中最后一个元素之后（如果有的话）的迭代器

## 另请参见

* 类型别名 [iterator](../iterator/)
* 类型别名 [const_iterator](../const_iterator/)
* 类 [VbaReferenceCollection](../)
* 命名空间 [Aspose::Slides::Vba](../../)
* 库 [Aspose.Slides](../../../)