---
title: operator<<()
second_title: Aspose.Slides C++ API 参考
description: 使用 UTF-8 编码将数据插入流。
type: docs
weight: 365
url: /zh/system.drawing/operator_less_less/
---
## System::Drawing::operator<<(std::ostream\&, Color) 函数

使用 UTF-8 编码将数据插入 stream。

```cpp
std::ostream & System::Drawing::operator<<(std::ostream &stream, Color color)
```

### 参数

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| stream | std::ostream\& | 输出 stream 用于插入数据。 |
| color | [Color](../color/) | [Data](../../system.data/) to insert. |

### 返回值

**stream**。

## System::Drawing::operator<<(std::wostream\&, Color) 函数

将数据插入 stream。

```cpp
std::wostream & System::Drawing::operator<<(std::wostream &stream, Color color)
```

### 参数

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| stream | std::wostream\& | 输出 stream 用于插入数据。 |
| color | [Color](../color/) | [Data](../../system.data/) to insert. |

### 返回值

**stream**。

## System::Drawing::operator<<(std::ostream\&, const Point\&) 函数

使用 UTF-8 编码将数据插入 stream。

```cpp
std::ostream & System::Drawing::operator<<(std::ostream &stream, const Point &point)
```

### 参数

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| stream | std::ostream\& | 输出 stream 用于插入数据。 |
| point | const [Point](../point/)\& | [Data](../../system.data/) to insert. |

### 返回值

**stream**。

## System::Drawing::operator<<(std::wostream\&, const Point\&) 函数

将数据插入 stream。

```cpp
std::wostream & System::Drawing::operator<<(std::wostream &stream, const Point &point)
```

### 参数

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| stream | std::wostream\& | 输出 stream 用于插入数据。 |
| point | const [Point](../point/)\& | [Data](../../system.data/) to insert. |

### 返回值

**stream**。

## System::Drawing::operator<<(std::ostream\&, const PointF\&) 函数

使用 UTF-8 编码将数据插入 stream。

```cpp
std::ostream & System::Drawing::operator<<(std::ostream &stream, const PointF &point)
```

### 参数

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| stream | std::ostream\& | 输出 stream 用于插入数据。 |
| point | const [PointF](../pointf/)\& | [Data](../../system.data/) to insert. |

### 返回值

**stream**。

## System::Drawing::operator<<(std::wostream\&, const PointF\&) 函数

将数据插入 stream。

```cpp
std::wostream & System::Drawing::operator<<(std::wostream &stream, const PointF &point)
```

### 参数

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| stream | std::wostream\& | 输出 stream 用于插入数据。 |
| point | const [PointF](../pointf/)\& | [Data](../../system.data/) to insert. |

### 返回值

**stream**。

## System::Drawing::operator<<(std::ostream\&, const Rectangle\&) 函数

使用 UTF-8 编码将数据插入 stream。

```cpp
std::ostream & System::Drawing::operator<<(std::ostream &stream, const Rectangle &rectangle)
```

### 参数

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| stream | std::ostream\& | 输出 stream 用于插入数据。 |
| rectangle | const [Rectangle](../rectangle/)\& | [Data](../../system.data/) to insert. |

### 返回值

**stream**。

## System::Drawing::operator<<(std::wostream\&, const Rectangle\&) 函数

将数据插入 stream。

```cpp
std::wostream & System::Drawing::operator<<(std::wostream &stream, const Rectangle &rectangle)
```

### 参数

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| stream | std::wostream\& | 输出 stream 用于插入数据。 |
| rectangle | const [Rectangle](../rectangle/)\& | [Data](../../system.data/) to insert. |

### 返回值

**stream**。

## System::Drawing::operator<<(std::ostream\&, const RectangleF\&) 函数

使用 UTF-8 编码将数据插入 stream。

```cpp
std::ostream & System::Drawing::operator<<(std::ostream &stream, const RectangleF &rectangle)
```

### 参数

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| stream | std::ostream\& | 输出 stream 用于插入数据。 |
| rectangle | const [RectangleF](../rectanglef/)\& | [Data](../../system.data/) to insert. |

### 返回值

**stream**。

## System::Drawing::operator<<(std::wostream\&, const RectangleF\&) 函数

将数据插入 stream。

```cpp
std::wostream & System::Drawing::operator<<(std::wostream &stream, const RectangleF &rectangle)
```

### 参数

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| stream | std::wostream\& | 输出 stream 用于插入数据。 |
| rectangle | const [RectangleF](../rectanglef/)\& | [Data](../../system.data/) to insert. |

### 返回值

**stream**。

## 参见

* 类 [Color](../color/)
* 类 [Point](../point/)
* 类 [PointF](../pointf/)
* 类 [Rectangle](../rectangle/)
* 类 [RectangleF](../rectanglef/)
* 命名空间 [System::Drawing](../)
* 库 [Aspose.Slides](../../)