---
title: DrawLine()
second_title: Aspose.Slides for C++ API 参考
description: 使用指定的笔绘制指定的线段。
type: docs
weight: 456
url: /zh/system.drawing/graphics/drawline/
---
## Graphics::DrawLine(const SharedPtr\<Pen\>\&, Point, Point) 方法

使用指定的笔绘制指定的线段。

```cpp
void System::Drawing::Graphics::DrawLine(const SharedPtr<Pen> &pen, Point pt1, Point pt2)
```

### 参数

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| pen | const [SharedPtr](../../../system/sharedptr/)\<[Pen](../../pen/)\>\& | 一个 [Pen](../../pen/) 对象，它指定要绘制的线的图形属性 |
| pt1 | [Point](../../point/) | 定义要绘制的线的第一个点 |
| pt2 | [Point](../../point/) | 定义要绘制的线的第二个点 |

## Graphics::DrawLine(const SharedPtr\<Pen\>\&, PointF, PointF) 方法

使用指定的笔绘制指定的线段。

```cpp
void System::Drawing::Graphics::DrawLine(const SharedPtr<Pen> &pen, PointF pt1, PointF pt2)
```

### 参数

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| pen | const [SharedPtr](../../../system/sharedptr/)\<[Pen](../../pen/)\>\& | 一个 [Pen](../../pen/) 对象，它指定要绘制的线的图形属性 |
| pt1 | [PointF](../../pointf/) | 定义要绘制的线的第一个点 |
| pt2 | [PointF](../../pointf/) | 定义要绘制的线的第二个点 |

## Graphics::DrawLine(const SharedPtr\<Pen\>\&, int, int, int, int) 方法

使用指定的笔绘制指定的线段。

```cpp
void System::Drawing::Graphics::DrawLine(const SharedPtr<Pen> &pen, int x1, int y1, int x2, int y2)
```

### 参数

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| pen | const [SharedPtr](../../../system/sharedptr/)\<[Pen](../../pen/)\>\& | 一个 [Pen](../../pen/) 对象，它指定要绘制的线的图形属性 |
| x1 | int | 定义要绘制的线的第一个点的X坐标 |
| y1 | int | 定义要绘制的线的第一个点的Y坐标 |
| x2 | int | 定义要绘制的线的第二个点的X坐标 |
| y2 | int | 定义要绘制的线的第二个点的Y坐标 |

## Graphics::DrawLine(const SharedPtr\<Pen\>\&, float, float, float, float) 方法

使用指定的笔绘制指定的线段。

```cpp
void System::Drawing::Graphics::DrawLine(const SharedPtr<Pen> &pen, float x1, float y1, float x2, float y2)
```

### 参数

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| pen | const [SharedPtr](../../../system/sharedptr/)\<[Pen](../../pen/)\>\& | 一个 [Pen](../../pen/) 对象，它指定要绘制的线的图形属性 |
| x1 | **float** | 定义要绘制的线的第一个点的X坐标 |
| y1 | **float** | 定义要绘制的线的第一个点的Y坐标 |
| x2 | **float** | 定义要绘制的线的第二个点的X坐标 |
| y2 | **float** | 定义要绘制的线的第二个点的Y坐标 |

## 另请参阅

* 类型定义 [SharedPtr](../../../system/sharedptr/)
* 类 [Pen](../../pen/)
* 类 [Point](../../point/)
* 类 [Graphics](../)
* 类 [PointF](../../pointf/)
* 命名空间 [System::Drawing](../../)
* 库 [Aspose.Slides](../../../)