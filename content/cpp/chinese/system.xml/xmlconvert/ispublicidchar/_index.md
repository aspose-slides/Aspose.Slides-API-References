---
title: IsPublicIdChar()
second_title: Aspose.Slides for C++ API 参考
description: 如果参数中的字符是有效的公共标识符字符，则返回传入的字符实例，否则返回 nullptr。
type: docs
weight: 196
url: /zh/system.xml/xmlconvert/ispublicidchar/
---
## XmlConvert::IsPublicIdChar(char16_t) 方法

返回传入的字符实例，如果参数中的字符是有效的公共标识符字符，否则返回 **nullptr**。

```cpp
static bool System::Xml::XmlConvert::IsPublicIdChar(char16_t ch)
```

### 参数

| Parameter | Type | Description |
| --- | --- | --- |
| ch | char16_t | [Char](../../../system/char/) 对象以进行验证。 |

### 返回值

传入的字符，如果字符是有效的公共标识符字符，否则返回 **nullptr**。

## 另见

* 类 [XmlConvert](../)
* 命名空间 [System::Xml](../../)
* 库 [Aspose.Slides](../../../)