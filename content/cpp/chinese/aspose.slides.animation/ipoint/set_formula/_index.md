---
title: set_Formula()
second_title: Aspose.Slides for C++ API 参考
description: "公式在 values、from、to、by 属性中可以由以下组成： 标准算术运算符： \\uFFFD+\\uFFFD, \\uFFFD-\\uFFFD, \\uFFFD*\\uFFFD, \\uFFFD/\\uFFFD, \\uFFFD^\\uFFFD, \\uFFFD\\uFFFD (mod) 常量： \\uFFFDpi\\uFFFD \\uFFFDe\\uFFFD 条件运算符： \\uFFFDabs\\uFFFD, \\uFFFDmin\\uFFFD, \\uFFFDmax\\uFFFD, \\uFFFD?\\uFFFD (if) 比较运算符： '==', '>=', '', '!=', '!' 三角运算符： \\uFFFDsin()\\uFFFD, \\uFFFDcos()\\uFFFD, \\uFFFDtan()\\uFFFD, \\uFFFDasin()\\uFFFD, \\uFFFDacos()\\uFFFD, \\uFFFDatan()\\uFFFD 自然对数 \\uFFFDln()\\uFFFD 属性引用（主机支持的属性）"
type: docs
weight: 66
url: /zh/aspose.slides.animation/ipoint/set_formula/
---
## IPoint::set_Formula(System::String) 方法

在 values、from、to、by 属性中的公式可以由以下组成： 标准算术运算符： \\uFFFD+\\uFFFD, \\uFFFD-\\uFFFD, \\uFFFD*\\uFFFD, \\uFFFD/\\uFFFD, \\uFFFD^\\uFFFD, \\uFFFD\\uFFFD (mod) 常量： \\uFFFDpi\\uFFFD \\uFFFDe\\uFFFD 条件运算符： \\uFFFDabs\\uFFFD, \\uFFFDmin\\uFFFD, \\uFFFDmax\\uFFFD, \\uFFFD?\\uFFFD (if) 比较运算符： '==', '>=', '', '!=', '!' 三角运算符： \\uFFFDsin()\\uFFFD, \\uFFFDcos()\\uFFFD, \\uFFFDtan()\\uFFFD, \\uFFFDasin()\\uFFFD, \\uFFFDacos()\\uFFFD, \\uFFFDatan()\\uFFFD 自然对数 \\uFFFDln()\\uFFFD 属性引用（主机支持的属性）

```cpp
virtual void Aspose::Slides::Animation::IPoint::set_Formula(System::String value)=0
```

## 备注

例如: "#ppt_x+(cos(-2*pi*(1-$))*-#ppt_x-sin(-2*pi*(1-$))*(1-#ppt_y))*(1-$)" 写入 [System::String](../../../system/string/)。

## 参见

* 类 [String](../../../system/string/)
* 类 [IPoint](../)
* 命名空间 [Aspose::Slides::Animation](../../)
* 库 [Aspose.Slides](../../../)