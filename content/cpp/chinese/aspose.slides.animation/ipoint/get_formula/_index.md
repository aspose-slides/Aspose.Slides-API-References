---
title: get_Formula()
second_title: Aspose.Slides for C++ API 参考
description: "公式（在 values、from、to、by 属性中）可以由以下组成：标准算术运算符：\\uFFFD+\\uFFFD, \\uFFFD-\\uFFFD, \\uFFFD*\\uFFFD, \\uFFFD/\\uFFFD, \\uFFFD^\\uFFFD, \\uFFFD\\uFFFD (mod) 常量：\\uFFFDpi\\uFFFD \\uFFFDe\\uFFFD 条件运算符：\\uFFFDabs\\uFFFD, \\uFFFDmin\\uFFFD, \\uFFFDmax\\uFFFD, \\uFFFD?\\uFFFD (if) 比较运算符：'==', '>=', '', '!=', '!' 三角函数运算符：\\uFFFDsin()\\uFFFD, \\uFFFDcos()\\uFFFD, \\uFFFDtan()\\uFFFD, \\uFFFDasin()\\uFFFD, \\uFFFDacos()\\uFFFD, \\uFFFDatan()\\uFFFD 自然对数 \\uFFFDln()\\uFFFD 属性引用（主机支持的属性）"
type: docs
weight: 53
url: /zh/aspose.slides.animation/ipoint/get_formula/
---
## IPoint::get_Formula() 方法

Formulas within values, from, to, by attributes can be made up of these: Standard arithmetic operators: \\uFFFD+\\uFFFD, \\uFFFD-\\uFFFD, \\uFFFD*\\uFFFD, \\uFFFD/\\uFFFD, \\uFFFD^\\uFFFD, \\uFFFD\\uFFFD (mod) Constants: \\uFFFDpi\\uFFFD \\uFFFDe\\uFFFD Conditional operators: \\uFFFDabs\\uFFFD, \\uFFFDmin\\uFFFD, \\uFFFDmax\\uFFFD, \\uFFFD?\\uFFFD (if) Comparison operators: '==', '>=', '', '!=', '!' Trigonometric operators: \\uFFFDsin()\\uFFFD, \\uFFFDcos()\\uFFFD, \\uFFFDtan()\\uFFFD, \\uFFFDasin()\\uFFFD, \\uFFFDacos()\\uFFFD, \\uFFFDatan()\\uFFFD Natural logarithm \\uFFFDln()\\uFFFD Property references (host supported properties)

```cpp
virtual System::String Aspose::Slides::Animation::IPoint::get_Formula()=0
```

## 备注

for example: "#ppt_x+(cos(-2*pi*(1-$))*-#ppt_x-sin(-2*pi*(1-$))*(1-#ppt_y))*(1-$)" Read [System::String](../../../system/string/). 

## 另见

* 类 [String](../../../system/string/)
* 类 [IPoint](../)
* 命名空间 [Aspose::Slides::Animation](../../)
* 库 [Aspose.Slides](../../../)