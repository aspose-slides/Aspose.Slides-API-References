---
title: rend()
second_title: Aspose.Slides for C++ API 参考
description: 返回一个逆向迭代器，指向已翻转容器中最后一个元素之后的元素。它对应于未翻转容器中第一个元素之前的元素。该元素充当占位符，尝试访问它会导致未定义行为。
type: docs
weight: 287
url: /zh/system.collections.specialized/stringcollection/rend/
---
## StringCollection::rend() 方法

返回一个逆向迭代器，指向已翻转容器中最后一个元素之后的元素。它对应于未翻转容器中第一个元素之前的元素。该元素充当占位符，尝试访问它会导致未定义行为。

```cpp
reverse_iterator System::Collections::Specialized::StringCollection::rend() noexcept
```

### 返回值

指向容器中第一个元素之前的理论元素的迭代器。

## StringCollection::rend() const 方法

返回一个逆向迭代器，指向已翻转容器中最后一个元素之后的元素。它对应于未翻转容器中第一个元素之前的元素。该元素充当占位符，尝试访问它会导致未定义行为。

```cpp
const_reverse_iterator System::Collections::Specialized::StringCollection::rend() const noexcept
```

### 返回值

指向 const 限定的容器中第一个元素之前的理论元素的迭代器。

## 另请参见

* 类型定义 [reverse_iterator](../reverse_iterator/)
* 类型定义 [const_reverse_iterator](../const_reverse_iterator/)
* 类 [StringCollection](../)
* 命名空间 [System::Collections::Specialized](../../)
* 库 [Aspose.Slides](../../../)