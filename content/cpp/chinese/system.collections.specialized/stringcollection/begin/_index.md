---
title: begin()
second_title: Aspose.Slides for C++ API 参考
description: 返回指向容器中第一个元素的迭代器。如果容器为空，返回的迭代器将等于 end()。
type: docs
weight: 209
url: /zh/system.collections.specialized/stringcollection/begin/
---
## StringCollection::begin() 方法

返回一个指向容器中第一个元素的迭代器。如果容器为空，返回的迭代器将等于 [end()](../end/)。

```cpp
iterator System::Collections::Specialized::StringCollection::begin() noexcept
```

### 返回值

指向容器中第一个元素的迭代器。

## StringCollection::begin() const 方法

返回一个指向 const 限定容器中第一个元素的迭代器。如果容器为空，返回的迭代器将等于 [end()](../end/)。

```cpp
const_iterator System::Collections::Specialized::StringCollection::begin() const noexcept
```

### 返回值

指向 const 限定容器中第一个元素的迭代器。

## 另见

* Typedef [iterator](../iterator/)
* Typedef [const_iterator](../const_iterator/)
* 类 [StringCollection](../)
* 命名空间 [System::Collections::Specialized](../../)
* Library [Aspose.Slides](../../../)