---
title: end()
second_title: Aspose.Slides for C++ API 参考
description: 返回一个迭代器，指向容器中最后一个元素之后的元素。该元素充当占位符；尝试访问它会导致未定义行为。
type: docs
weight: 235
url: /zh/system.collections.specialized/stringcollection/end/
---
## StringCollection::end() 方法

返回一个迭代器，指向容器中最后一个元素之后的元素。该元素作为占位符；尝试访问它会导致未定义行为。

```cpp
iterator System::Collections::Specialized::StringCollection::end() noexcept
```

### 返回值

指向理论上位于容器最后一个元素之后的元素的迭代器。

## StringCollection::end() const 方法

返回一个迭代器，指向 const 限定容器中最后一个元素之后的元素。该元素作为占位符；尝试访问它会导致未定义行为。

```cpp
const_iterator System::Collections::Specialized::StringCollection::end() const noexcept
```

### 返回值

指向理论上位于 const 限定容器最后一个元素之后的元素的迭代器。

## 另请参见

* 类型别名 [iterator](../iterator/)
* 类型别名 [const_iterator](../const_iterator/)
* 类 [StringCollection](../)
* 命名空间 [System::Collections::Specialized](../../)
* 库 [Aspose.Slides](../../../)