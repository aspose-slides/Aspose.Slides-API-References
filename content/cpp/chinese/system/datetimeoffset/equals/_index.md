---
title: Equals()
second_title: Aspose.Slides for C++ API 参考
description: 检查两个 DateTimeOffset 对象是否表示相同的时间点。
type: docs
weight: 365
url: /zh/system/datetimeoffset/equals/
---
## DateTimeOffset::Equals(const DateTimeOffset\&) const 方法

检查两个[DateTimeOffset](../)对象是否表示相同的时间点。

```cpp
bool System::DateTimeOffset::Equals(const DateTimeOffset &other) const
```

### 参数

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/)用于比较。 |

### 返回值

如果当前对象等于 **other**，则返回 true；否则返回 false。

## DateTimeOffset::Equals(const SharedPtr\<Object\>\&) const 方法

检查两个[DateTimeOffset](../)对象是否表示相同的时间点。

```cpp
bool System::DateTimeOffset::Equals(const SharedPtr<Object> &obj) const
```

### 参数

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/)用于比较。 |

### 返回值

如果当前对象等于 **obj**，则返回 true；否则返回 false。

## DateTimeOffset::Equals(const DateTimeOffset\&, const DateTimeOffset\&) 方法

检查两个[DateTimeOffset](../)对象是否表示相同的时间点。

```cpp
static bool System::DateTimeOffset::Equals(const DateTimeOffset &first, const DateTimeOffset &second)
```

### 参数

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| first | const [DateTimeOffset](../)\& | 要比较的第一个对象。 |
| second | const [DateTimeOffset](../)\& | 要比较的第二个对象。 |

### 返回值

如果 **first** 等于 **second**，则返回 true；否则返回 false。

## 另见

* 类型定义 [SharedPtr](../../sharedptr/)
* 类 [DateTimeOffset](../)
* 类 [Object](../../object/)
* 命名空间 [System](../../)
* 库 [Aspose.Slides](../../../)