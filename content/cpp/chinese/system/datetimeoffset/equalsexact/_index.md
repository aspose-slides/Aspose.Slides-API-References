---
title: EqualsExact()
second_title: Aspose.Slides C++ API 参考
description: 检查两个 DateTimeOffset 对象是否表示相同的时间点并具有相同的偏移量。
type: docs
weight: 378
url: /zh/system/datetimeoffset/equalsexact/
---
## DateTimeOffset::EqualsExact(const DateTimeOffset\&) const 方法

检查两个 [DateTimeOffset](../) 对象是否表示相同的时间点并具有相同的偏移量。

```cpp
bool System::DateTimeOffset::EqualsExact(const DateTimeOffset &other) const
```

### 参数

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/) 用于比较。 |

### 返回值

如果当前对象与 **other** 完全相等则返回 true，否则 - false。

## DateTimeOffset::EqualsExact(const SharedPtr\<Object\>\&) const 方法

检查两个 [DateTimeOffset](../) 对象是否表示相同的时间点并具有相同的偏移量。

```cpp
bool System::DateTimeOffset::EqualsExact(const SharedPtr<Object> &obj) const
```

### 参数

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/) 用于比较。 |

### 返回值

如果当前对象与 **obj** 完全相等则返回 true，否则 - false。

## 另见

* 类型定义 [SharedPtr](../../sharedptr/)
* 类 [DateTimeOffset](../)
* 类 [Object](../../object/)
* 命名空间 [System](../../)
* 库 [Aspose.Slides](../../../)