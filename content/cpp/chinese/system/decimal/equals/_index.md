---
title: Equals()
second_title: Aspose.Slides for C++ API 参考
description: 确定当前对象和指定对象表示的值是否相等。
type: docs
weight: 313
url: /zh/system/decimal/equals/
---
## Decimal::Equals(const Decimal\&) const 方法

确定当前对象和指定对象表示的值是否相等。

```cpp
bool System::Decimal::Equals(const Decimal &d) const
```

### 参数

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| d | const [Decimal](../)\& | 用于与当前对象比较的 [Decimal](../) 对象 |

### 返回值

如果当前对象和指定对象表示的值相等则返回 True，否则返回 false

## Decimal::Equals(const SharedPtr\<Object\>\&) const 方法

确定当前对象和指定对象表示的值是否相等。

```cpp
bool System::Decimal::Equals(const SharedPtr<Object> &obj) const
```

### 参数

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | 用于与当前对象比较的对象 |

### 返回值

如果当前对象和指定对象表示的值相等则返回 True，否则返回 false

## Decimal::Equals(const Decimal\&, const Decimal\&) 方法

确定指定对象表示的值是否相等。

```cpp
static bool System::Decimal::Equals(const Decimal &d1, const Decimal &d2)
```

### 参数

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| d1 | const [Decimal](../)\& | 第一个比较对象 |
| d2 | const [Decimal](../)\& | 第二个比较对象 |

### 返回值

如果 **d1** 和 **d2** 表示的值相等则返回 True，否则返回 false

## 另见

* 类型定义 [SharedPtr](../../sharedptr/)
* 类 [Decimal](../)
* 类 [Object](../../object/)
* 命名空间 [System](../../)
* 库 [Aspose.Slides](../../../)