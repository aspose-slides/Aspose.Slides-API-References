---
title: ToString()
second_title: Aspose.Slides for C++ API 参考
description: 在值类型对象上调用 ToString() 的上下文中，包装对 String 类的处理。
type: docs
weight: 365
url: /zh/system/string/tostring/
---
## String::ToString() const 方法

在值类型对象上调用 [ToString()](./) 的上下文中，包装对 [String](../) 类的处理。

```cpp
String System::String::ToString() const
```

### 返回值

当前字符串的副本。如果当前字符串为 null 字符串，则视为 null 字符串。

## String::ToString(const SharedPtr\<IFormatProvider\>\&) const 方法

在值类型对象上调用 [ToString()](./) 的上下文中，包装对 [String](../) 类的处理。

```cpp
String System::String::ToString(const SharedPtr<IFormatProvider> &) const
```

### 返回值

当前字符串的副本。如果当前字符串为 null 字符串，则视为 null 字符串。

## 另见

* 类型别名 [SharedPtr](../../sharedptr/)
* 类 [String](../)
* 类 [IFormatProvider](../../iformatprovider/)
* 命名空间 [System](../../)
* 库 [Aspose.Slides](../../../)