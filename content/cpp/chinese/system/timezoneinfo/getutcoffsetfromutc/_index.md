---
title: GetUtcOffsetFromUtc()
second_title: Aspose.Slides for C++ API 参考
description: 内部辅助函数，用于返回指定时区中 UTC 日期时间 的 UTC 偏移。仅供内部使用。
type: docs
weight: 144
url: /zh/system/timezoneinfo/getutcoffsetfromutc/
---
## TimeZoneInfo::GetUtcOffsetFromUtc(DateTime, const TimeZoneInfoPtr\&) 方法

内部辅助函数，用于返回指定时区中 UTC 日期时间 的 UTC 偏移。仅供内部使用。

```cpp
static TimeSpan System::TimeZoneInfo::GetUtcOffsetFromUtc(DateTime time, const TimeZoneInfoPtr &zone)
```
## TimeZoneInfo::GetUtcOffsetFromUtc(DateTime, const TimeZoneInfoPtr\&, bool\&, bool\&) 方法

内部辅助函数，用于返回指定时区中 UTC 日期时间 的 UTC 偏移。仅供内部使用。

```cpp
static TimeSpan System::TimeZoneInfo::GetUtcOffsetFromUtc(DateTime time, const TimeZoneInfoPtr &zone, bool &is_daylight_savings, bool &is_ambiguous_local_dst)
```
## 另见

* Typedef [TimeZoneInfoPtr](../../timezoneinfoptr/)
* 类 [TimeSpan](../../timespan/)
* 类 [DateTime](../../datetime/)
* 类 [TimeZoneInfo](../)
* 命名空间 [System](../../)
* Library [Aspose.Slides](../../../)