---
title: crend()
second_title: Aspose.Slides C++ API 参考
description: 返回一个反向迭代器，指向倒序容器中最后一个元素之后的元素。它对应于非倒序容器中第一个元素之前的元素。该元素充当占位符，尝试访问它会导致未定义行为。
type: docs
weight: 508
url: /zh/system/array/crend/
---
## Array::crend() const 方法

返回一个反向迭代器，指向倒序容器中最后一个元素之后的元素。它对应于非倒序容器中第一个元素之前的元素。该元素充当占位符，尝试访问它会导致未定义行为。

```cpp
const_reverse_iterator System::Array<T>::crend() const noexcept
```

### 返回值

一个迭代器，指向容器中第一个元素之前的理论 const 限定元素。

## 另请参阅

* 类型别名 [const_reverse_iterator](../const_reverse_iterator/)
* 类 [Array](../)
* 命名空间 [System](../../)
* 库 [Aspose.Slides](../../../)