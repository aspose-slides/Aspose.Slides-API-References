---
title: end()
second_title: Aspose.Slides for C++ API 参考
description: 获取指向集合中最后一个元素（如果有）的迭代器，位置在其之后。
type: docs
weight: 599
url: /zh/aspose.slides/shapecollection/end/
---
## ShapeCollection::end() 方法

获取指向集合中最后一个元素（如果有）的迭代器，位置在其之后。

```cpp
iterator Aspose::Slides::ShapeCollection::end() noexcept
```

### 返回值

指向集合中最后一个元素（如果有）的迭代器，位置在其之后

## ShapeCollection::end() const 方法

获取指向常量限定实例的集合中最后一个元素（如果有）的迭代器，位置在其之后。

```cpp
const_iterator Aspose::Slides::ShapeCollection::end() const noexcept
```

### 返回值

指向常量限定实例的集合中最后一个元素（如果有）的迭代器，位置在其之后

## 另见

* 类型定义 [iterator](../iterator/)
* 类型定义 [const_iterator](../const_iterator/)
* 类 [ShapeCollection](../)
* 命名空间 [Aspose::Slides](../../)
* 库 [Aspose.Slides](../../../)