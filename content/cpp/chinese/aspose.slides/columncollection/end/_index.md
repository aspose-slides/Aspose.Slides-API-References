---
title: end()
second_title: Aspose.Slides C++ API 参考
description: 获取指向集合中最后一个元素之后（如果有）的迭代器。
type: docs
weight: 131
url: /zh/aspose.slides/columncollection/end/
---
## ColumnCollection::end() 方法

获取指向集合中最后一个元素之后（如果有）的迭代器。

```cpp
iterator Aspose::Slides::ColumnCollection::end() noexcept
```

### 返回值

指向集合中最后一个元素之后（如果有）的迭代器

## ColumnCollection::end() const 方法

获取指向集合的 const 限定实例中最后一个元素之后（如果有）的迭代器。

```cpp
const_iterator Aspose::Slides::ColumnCollection::end() const noexcept
```

### 返回值

指向集合的 const 限定实例中最后一个元素之后（如果有）的迭代器

## 另请参见

* Typedef [iterator](../iterator/)
* Typedef [const_iterator](../const_iterator/)
* Class [ColumnCollection](../)
* Namespace [Aspose::Slides](../../)
* Library [Aspose.Slides](../../../)