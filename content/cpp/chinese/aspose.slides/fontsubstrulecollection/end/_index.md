---
title: end()
second_title: Aspose.Slides C++ API 参考
description: 获取指向集合中最后一个元素之后（如果有）的迭代器。
type: docs
weight: 131
url: /zh/aspose.slides/fontsubstrulecollection/end/
---
## FontSubstRuleCollection::end() 方法

获取指向集合中最后一个元素之后（若有）的迭代器。

```cpp
iterator Aspose::Slides::FontSubstRuleCollection::end() noexcept
```

### 返回值

指向集合中最后一个元素之后（若有）的迭代器

## FontSubstRuleCollection::end() const 方法

获取指向集合的 const 限定实例中最后一个元素之后（若有）的迭代器。

```cpp
const_iterator Aspose::Slides::FontSubstRuleCollection::end() const noexcept
```

### 返回值

指向集合的 const 限定实例中最后一个元素之后（若有）的迭代器

## 另见

* 类型定义 [iterator](../iterator/)
* 类型定义 [const_iterator](../const_iterator/)
* 类 [FontSubstRuleCollection](../)
* 命名空间 [Aspose::Slides](../../)
* 库 [Aspose.Slides](../../../)