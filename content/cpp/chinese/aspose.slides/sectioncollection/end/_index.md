---
title: end()
second_title: Aspose.Slides for C++ API 参考
description: 获取指向集合中最后一个元素之后（如果有）的迭代器。
type: docs
weight: 196
url: /zh/aspose.slides/sectioncollection/end/
---
## SectionCollection::end() method

获取指向集合中最后一个元素之后（如果有）的迭代器。

```cpp
iterator Aspose::Slides::SectionCollection::end() noexcept
```

### 返回值

一个指向集合中最后一个元素之后（如果有）的迭代器

## SectionCollection::end() const method

获取指向集合的 const 限定实例中最后一个元素之后（如果有）的迭代器。

```cpp
const_iterator Aspose::Slides::SectionCollection::end() const noexcept
```

### 返回值

一个指向集合的 const 限定实例中最后一个元素之后（如果有）的迭代器

## 另见

* Typedef [iterator](../iterator/)
* Typedef [const_iterator](../const_iterator/)
* 类 [SectionCollection](../)
* 命名空间 [Aspose::Slides](../../)
* Library [Aspose.Slides](../../../)