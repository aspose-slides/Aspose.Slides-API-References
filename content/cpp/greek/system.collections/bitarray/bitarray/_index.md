---
title: BitArray()
second_title: Aspose.Slides για C++ αναφορά API
description: Κατασκευαστής αντιγραφής.
type: docs
weight: 14
url: /el/system.collections/bitarray/bitarray/
---
## BitArray::BitArray(const bitset\&) κατασκευαστής

Κατασκευαστής αντιγραφής.

```cpp
System::Collections::BitArray::BitArray(const bitset &bits)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| bits | const [bitset](../bitset/)\& | Κοντέινερ για αντιγραφή δεδομένων. |

## BitArray::BitArray(const BitArray\&) κατασκευαστής

Κατασκευαστής αντιγραφής.

```cpp
System::Collections::BitArray::BitArray(const BitArray &bits)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| bits | const [BitArray](../)\& | Κοντέινερ για αντιγραφή δεδομένων. |

## BitArray::BitArray(const BitArrayPtr\&) κατασκευαστής

Κατασκευαστής αντιγραφής.

```cpp
System::Collections::BitArray::BitArray(const BitArrayPtr &array)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| array | const [BitArrayPtr](../../bitarrayptr/)\& | [BitArray](../) για αντιγραφή δεδομένων. |

## BitArray::BitArray(const System::ArrayPtr\<bool\>\&) κατασκευαστής

Κατασκευαστής αντιγραφής.

```cpp
System::Collections::BitArray::BitArray(const System::ArrayPtr<bool> &values)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| values | const [System::ArrayPtr](../../../system/arrayptr/)\<**bool**\>\& | Κοντέινερ για αντιγραφή δεδομένων στοιχείο-προς-στοιχείο. |

## BitArray::BitArray(const System::ArrayPtr\<uint8_t\>\&) κατασκευαστής

Κατασκευαστής αντιγραφής.

```cpp
System::Collections::BitArray::BitArray(const System::ArrayPtr<uint8_t> &bytes)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| bytes | const [System::ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\>\& | Κοντέινερ για αντιγραφή δεδομένων ανά bit. |

## BitArray::BitArray(const System::ArrayPtr\<int\>\&) κατασκευαστής

Κατασκευαστής αντιγραφής.

```cpp
System::Collections::BitArray::BitArray(const System::ArrayPtr<int> &values)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| values | const [System::ArrayPtr](../../../system/arrayptr/)\<int\>\& | Κοντέινερ για αντιγραφή δεδομένων ανά bit. |

## BitArray::BitArray(int, bool) κατασκευαστής

Κατασκευαστής γεμίσματος.

```cpp
System::Collections::BitArray::BitArray(int length, bool defaultValue=false)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| length | int | Αριθμός στοιχείων προς γέμισμα. |
| defaultValue | **bool** | Τιμή γέμισης. |

## Δείτε επίσης

* Typedef [bitset](../bitset/)
* Typedef [ArrayPtr](../../../system/arrayptr/)
* Κλάση [BitArray](../)
* Κλάση [BitArrayPtr](../../bitarrayptr/)
* Χώρος ονομάτων [System::Collections](../../)
* Βιβλιοθήκη [Aspose.Slides](../../../)