---
title: AddLine()
second_title: Αναφορά API Aspose.Slides για C++
description: Προσθέτει τη συγκεκριμένη γραμμή στη διαδρομή που αντιπροσωπεύεται από το τρέχον αντικείμενο.
type: docs
weight: 144
url: /el/system.drawing.drawing2d/graphicspath/addline/
---
## GraphicsPath::AddLine(const Point\&, const Point\&) μέθοδος

Προσθέτει την καθορισμένη γραμμή στη διαδρομή που αντιπροσωπεύεται από το τρέχον αντικείμενο.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddLine(const Point &pt1, const Point &pt2)
```

### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| pt1 | const [Point](../../../system.drawing/point/)\& | Το αρχικό σημείο της γραμμής που πρέπει να προστεθεί |
| pt2 | const [Point](../../../system.drawing/point/)\& | Το τελικό σημείο της γραμμής που πρέπει να προτεθεί |

## GraphicsPath::AddLine(const PointF\&, const PointF\&) μέθοδος

Προσθέτει την καθορισμένη γραμμή στη διαδρομή που αντιπροσωπεύεται από το τρέχον αντικείμενο.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddLine(const PointF &pt1, const PointF &pt2)
```

### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| pt1 | const [PointF](../../../system.drawing/pointf/)\& | Το αρχικό σημείο της γραμμής που πρέπει να προστεθεί |
| pt2 | const [PointF](../../../system.drawing/pointf/)\& | Το τελικό σημείο της γραμμής που πρέπει να προτεθεί |

## GraphicsPath::AddLine(int, int, int, int) μέθοδος

Προσθέτει την καθορισμένη γραμμή στη διαδρομή που αντιπροσωπεύεται από το τρέχον αντικείμενο.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddLine(int x1, int y1, int x2, int y2)
```

### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| x1 | int | Η X συντεταγμένη του αρχικού σημείου της γραμμής που πρέπει να προτεθεί |
| y1 | int | Η Y συντεταγμένη του αρχικού σημείου της γραμμής που πρέπει να προτεθεί |
| x2 | int | Η X συντεταγμένη του τελικού σημείου της γραμμής που πρέπει να προτεθεί |
| y2 | int | Η Y συντεταγμένη του τελικού σημείου της γραμμής που πρέπει να προτεθεί |

## GraphicsPath::AddLine(float, float, float, float) μέθοδος

Προσθέτει την καθορισμένη γραμμή στη διαδρομή που αντιπροσωπεύεται από το τρέχον αντικείμενο.

```cpp
void System::Drawing::Drawing2D::GraphicsPath::AddLine(float x1, float y1, float x2, float y2)
```

### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| x1 | **float** | Η X συντεταγμένη του αρχικού σημείου της γραμμής που πρέπει να προτεθεί |
| y1 | **float** | Η Y συντεταγμένη του αρχικού σημείου της γραμμής που πρέπει να προτεθεί |
| x2 | **float** | Η X συντεταγμένη του τελικού σημείου της γραμμής που πρέπει να προτεθεί |
| y2 | **float** | Η Y συντεταγμένη του τελικού σημείου της γραμμής που πρέπει να προτεθεί |

## Δείτε επίσης

* Κλάση [Point](../../../system.drawing/point/)
* Κλάση [GraphicsPath](../)
* Κλάση [PointF](../../../system.drawing/pointf/)
* Χώρος ονομάτων [System::Drawing::Drawing2D](../../)
* Βιβλιοθήκη [Aspose.Slides](../../../)