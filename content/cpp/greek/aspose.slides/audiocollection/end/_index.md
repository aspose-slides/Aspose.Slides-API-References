---
title: end()
second_title: Aspose.Slides για C++ API Αναφορά
description: Παίρνει iterator που δείχνει ακριβώς μετά το τελευταίο στοιχείο (αν υπάρχει) της συλλογής.
type: docs
weight: 105
url: /el/aspose.slides/audiocollection/end/
---
## AudioCollection::end() μέθοδος

Παίρνει iterator που δείχνει ακριβώς μετά το τελευταίο στοιχείο (αν υπάρχει) της συλλογής.

```cpp
iterator Aspose::Slides::AudioCollection::end() noexcept
```

### Τιμή Επιστροφής

Ένας iterator που δείχνει ακριβώς μετά το τελευταίο στοιχείο (αν υπάρχει) της συλλογής

## AudioCollection::end() const μέθοδος

Παίρνει iterator που δείχνει ακριβώς μετά το τελευταίο στοιχείο (αν υπάρχει) του const-προσδιορισμένου στιγμιότυπου της συλλογής.

```cpp
const_iterator Aspose::Slides::AudioCollection::end() const noexcept
```

### Τιμή Επιστροφής

Ένας iterator που δείχνει ακριβώς μετά το τελευταίο στοιχείο (αν υπάρχει) του const-προσδιορισμένου στιγμιότυπου της συλλογής

## Δείτε επίσης

* Typedef [iterator](../iterator/)
* Typedef [const_iterator](../const_iterator/)
* Κλάση [AudioCollection](../)
* Χώρος ονομάτων [Aspose::Slides](../../)
* Βιβλιοθήκη [Aspose.Slides](../../../)