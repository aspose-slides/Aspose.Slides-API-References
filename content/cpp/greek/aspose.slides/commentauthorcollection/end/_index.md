---
title: end()
second_title: Aspose.Slides για C++ API Αναφορά
description: Παίρνει έναν επαναλήπτη που δείχνει ακριβώς μετά το τελευταίο στοιχείο (εφόσον υπάρχει) της συλλογής.
type: docs
weight: 183
url: /el/aspose.slides/commentauthorcollection/end/
---
## CommentAuthorCollection::end() method

Παίρνει έναν επαναλήπτη που δείχνει ακριβώς μετά το τελευταίο στοιχείο (εφόσον υπάρχει) της συλλογής.

```cpp
iterator Aspose::Slides::CommentAuthorCollection::end() noexcept
```

### Τιμή Επιστροφής

Ένας επαναλήπτης που δείχνει ακριβώς μετά το τελευταίο στοιχείο (εφόσον υπάρχει) της συλλογής

## CommentAuthorCollection::end() const method

Παίρνει έναν επαναλήπτη που δείχνει ακριβώς μετά το τελευταίο στοιχείο (εφόσον υπάρχει) του αντικειμένου της συλλογής που είναι χαρακτηρισμένο ως const.

```cpp
const_iterator Aspose::Slides::CommentAuthorCollection::end() const noexcept
```

### Τιμή Επιστροφής

Ένας επαναλήπτης που δείχνει ακριβώς μετά το τελευταίο στοιχείο (εφόσον υπάρχει) του αντικειμένου της συλλογής που είναι χαρακτηρισμένο ως const.

## Δείτε επίσης

* Typedef [iterator](../iterator/)
* Typedef [const_iterator](../const_iterator/)
* Κλάση [CommentAuthorCollection](../)
* Χώρος ονομάτων [Aspose::Slides](../../)
* Library [Aspose.Slides](../../../)