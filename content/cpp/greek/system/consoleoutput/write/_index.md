---
title: Write()
second_title: Aspose.Slides για C++ Αναφορά API
description: Εξάγει την αναπαράσταση συμβολοσειράς της καθορισμένης τιμής bool στη ροή εξόδου που αντιπροσωπεύεται από το τρέχον αντικείμενο.
type: docs
weight: 14
url: /el/system/consoleoutput/write/
---
## ConsoleOutput::Write(bool) μέθοδος

Εξάγει την αναπαράσταση συμβολοσειράς της καθορισμένης τιμής **bool** στη ροή εξόδου που αντιπροσωπεύεται από το τρέχον αντικείμενο.

```cpp
void System::ConsoleOutput::Write(bool value) override
```

### Arguments

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **bool** | Η τιμή που θα εξαχθεί |

## ConsoleOutput::Write(const SharedPtr\<Object\>\&) μέθοδος

Εξάγει την αναπαράσταση συμβολοσειράς του καθορισμένου αντικειμένου στη ροή εξόδου που αντιπροσωπεύεται από το τρέχον αντικείμενο.

```cpp
void System::ConsoleOutput::Write(const SharedPtr<Object> &value) override
```

### Arguments

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Το αντικείμενο προς έξοδο |

## ConsoleOutput::Write(char_t) μέθοδος

Εξάγει την καθορισμένη τιμή χαρακτήρα στη ροή εξόδου που αντιπροσωπεύεται από το τρέχον αντικείμενο.

```cpp
void System::ConsoleOutput::Write(char_t value) override
```

### Arguments

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | char_t | Η τιμή που θα εξαχθεί |

## ConsoleOutput::Write(Decimal) μέθοδος

Εξάγει την αναπαράσταση συμβολοσειράς της τιμής [Decimal](../../decimal/) στη ροή εξόδου που αντιπροσωπεύεται από το τρέχον αντικείμενο.

```cpp
void System::ConsoleOutput::Write(Decimal value) override
```

### Arguments

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | Η τιμή που θα εξαχθεί |

## ConsoleOutput::Write(double) μέθοδος

Εξάγει την αναπαράσταση συμβολοσειράς της τιμής double-precision floating-point στη ροή εξόδου που αντιπροσωπεύεται από το τρέχον αντικείμενο.

```cpp
void System::ConsoleOutput::Write(double value) override
```

### Arguments

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **double** | Η τιμή που θα εξαχθεί |

## ConsoleOutput::Write(int32_t) μέθοδος

Εξάγει την αναπαράσταση συμβολοσειράς της 32-bit ακέραιας τιμής στη ροή εξόδου που αντιπροσωπεύεται από το τρέχον αντικείμενο.

```cpp
void System::ConsoleOutput::Write(int32_t value) override
```

### Arguments

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **int32_t** | Η τιμή που θα εξαχθεί |

## ConsoleOutput::Write(int64_t) μέθοδος

Εξάγει την αναπαράσταση συμβολοσειράς της 64-bit ακέραιας τιμής στη ροή εξόδου που αντιπροσωπεύεται από το τρέχον αντικείμενο.

```cpp
void System::ConsoleOutput::Write(int64_t value) override
```

### Arguments

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **int64_t** | Η τιμή που θα εξαχθεί |

## ConsoleOutput::Write(float) μέθοδος

Εξάγει την αναπαράσταση συμβολοσειράς της τιμής single-precision floating-point στη ροή εξόδου που αντιπροσωπεύεται από το τρέχον αντικείμενο.

```cpp
void System::ConsoleOutput::Write(float value) override
```

### Arguments

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **float** | Η τιμή που θα εξαχθεί |

## ConsoleOutput::Write(const String\&) μέθοδος

Εξάγει το καθορισμένο αντικείμενο συμβολοσειράς στη ροή εξόδου που αντιπροσωπεύεται από το τρέχον αντικείμενο.

```cpp
void System::ConsoleOutput::Write(const String &value) override
```

### Arguments

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Το αντικείμενο συμβολοσειράς προς έξοδο |

## ConsoleOutput::Write(uint32_t) μέθοδος

Εξάγει την αναπαράσταση συμβολοσειράς της αμεταβλητής 32-bit ακέραιας τιμής στη ροή εξόδου που αντιπροσωπεύεται από το τρέχον αντικείμενο.

```cpp
void System::ConsoleOutput::Write(uint32_t value) override
```

### Arguments

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **uint32_t** | Η τιμή που θα εξαχθεί |

## ConsoleOutput::Write(uint64_t) μέθοδος

Εξάγει την αναπαράσταση συμβολοσειράς της αμεταβλητής 64-bit ακέραιας τιμής στη ροή εξόδου που αντιπροσωπεύεται από το τρέχον αντικείμενο.

```cpp
void System::ConsoleOutput::Write(uint64_t value) override
```

### Arguments

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **uint64_t** | Η τιμή που θα εξαχθεί |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&) μέθοδος

Εξάγει την αναπαράσταση συμβολοσειράς του καθορισμένου πίνακα χαρακτήρων στη ροή εξόδου που αντιπροσωπεύεται από το τρέχον αντικείμενο.

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer) override
```

### Arguments

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Ο πίνακας προς έξοδο |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&, int32_t, int32_t) μέθοδος

Εξάγει την αναπαράσταση συμβολοσειράς μιας περιοχής τιμών του καθορισμένου πίνακα χαρακτήρων στη ροή εξόδου που αντιπροσωπεύεται από το τρέχον αντικείμενο.

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```

### Arguments

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Ο πίνακας που περιέχει τις τιμές προς έξοδο |
| index | **int32_t** | Ο δείκτης όπου αρχίζει η περιοχή των στοιχείων προς έξοδο |
| count | **int32_t** | Ο αριθμός των στοιχείων στην περιοχή των στοιχείων προς έξοδο |

## ConsoleOutput::Write(const char_t *) μέθοδος

Εξάγει το καθορισμένο c-string στη ροή εξόδου που αντιπροσωπεύεται από το τρέχον αντικείμενο.

```cpp
void System::ConsoleOutput::Write(const char_t *value) override
```

### Arguments

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const char_t * | Η c-string προς έξοδο |

## ConsoleOutput::Write(const TypeInfo\&) μέθοδος

Εξάγει την αναπαράσταση συμβολοσειράς του καθορισμένου αντικειμένου [TypeInfo](../../typeinfo/) στη ροή εξόδου που αντιπροσωπεύεται από το τρέχον αντικείμενο.

```cpp
void System::ConsoleOutput::Write(const TypeInfo &value) override
```

### Arguments

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | Το [TypeInfo](../../typeinfo/) αντικείμενο προς έξοδο |

## ConsoleOutput::Write(const char *) μέθοδος




```cpp
void System::ConsoleOutput::Write(const char *)=delete
```

## Δείτε επίσης

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Class [ConsoleOutput](../)
* Class [Object](../../object/)
* Class [Decimal](../../decimal/)
* Class [String](../../string/)
* Class [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)