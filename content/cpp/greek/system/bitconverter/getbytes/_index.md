---
title: GetBytes()
second_title: Αναφορά API του Aspose.Slides για C++
description: Μετατρέπει τη συγκεκριμένη τιμή boolean σε έναν πίνακα byte.
type: docs
weight: 14
url: /el/system/bitconverter/getbytes/
---
## BitConverter::GetBytes(bool) μέθοδος

Μετατρέπει τη συγκεκριμένη τιμή boolean σε έναν πίνακα byte.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(bool value)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **bool** | Μια τιμή boolean για μετατροπή |

### Τιμή Επιστροφής

Πίνακας 1-byte που αντιπροσωπεύει τη συγκεκριμένη τιμή

## BitConverter::GetBytes(char_t) μέθοδος

Μετατρέπει τη συγκεκριμένη τιμή char_t σε έναν πίνακα byte.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(char_t value)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | char_t | Μια τιμή char_t για μετατροπή |

### Τιμή Επιστροφής

Πίνακας 2-byte που αντιπροσωπεύει τη συγκεκριμένη τιμή

## BitConverter::GetBytes(int16_t) μέθοδος

Μετατρέπει τη συγκεκριμένη τιμή 16-bit integer σε έναν πίνακα byte.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int16_t value)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **int16_t** | Μια τιμή 16-bit integer για μετατροπή |

### Τιμή Επιστροφής

Πίνακας 2-byte που αντιπροσωπεύει τη συγκεκριμένη τιμή

## BitConverter::GetBytes(int) μέθοδος

Μετατρέπει τη συγκεκριμένη τιμή 32-bit integer σε έναν πίνακα byte.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int value)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | int | Μια τιμή 32-bit integer για μετατροπή |

### Τιμή Επιστροφής

Πίνακας 4-byte που αντιπροσωπεύει τη συγκεκριμένη τιμή

## BitConverter::GetBytes(int64_t) μέθοδος

Μετατρέπει τη συγκεκριμένη τιμή 64-bit integer σε έναν πίνακα byte.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(int64_t value)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **int64_t** | Μια τιμή 64-bit integer για μετατροπή |

### Τιμή Επιστροφής

Πίνακας 8-byte που αντιπροσωπεύει τη συγκεκριμένη τιμή

## BitConverter::GetBytes(uint16_t) μέθοδος

Μετατρέπει τη συγκεκριμένη τιμή unsigned 16-bit integer σε έναν πίνακα byte.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint16_t value)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **uint16_t** | Μια τιμή unsigned 16-bit integer για μετατροπή |

### Τιμή Επιστροφής

Πίνακας 2-byte που αντιπροσωπεύει τη συγκεκριμένη τιμή

## BitConverter::GetBytes(uint32_t) μέθοδος

Μετατρέπει τη συγκεκριμένη τιμή unsigned 32-bit integer σε έναν πίνακα byte.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint32_t value)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **uint32_t** | Μια τιμή unsigned 32-bit integer για μετατροπή |

### Τιμή Επιστροφής

Πίνακας 4-byte που αντιπροσωπεύει τη συγκεκριμένη τιμή

## BitConverter::GetBytes(uint64_t) μέθοδος

Μετατρέπει τη συγκεκριμένη τιμή unsigned 64-bit integer σε έναν πίνακα byte.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(uint64_t value)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **uint64_t** | Μια τιμή unsigned 64-bit integer για μετατροπή |

### Τιμή Επιστροφής

Πίνακας 8-byte που αντιπροσωπεύει τη συγκεκριμένη τιμή

## BitConverter::GetBytes(float) μέθοδος

Μετατρέπει τη συγκεκριμένη τιμή single-precision floating-point σε έναν πίνακα byte.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(float value)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **float** | Μια τιμή floating-point για μετατροπή |

### Τιμή Επιστροφής

Πίνακας 4-byte που αντιπροσωπεύει τη συγκεκριμένη τιμή

## BitConverter::GetBytes(double) μέθοδος

Μετατρέπει τη συγκεκριμένη τιμή double-precision floating-point σε έναν πίνακα byte.

```cpp
static System::ArrayPtr<uint8_t> System::BitConverter::GetBytes(double value)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **double** | Μια τιμή floating-point για μετατροπή |

### Τιμή Επιστροφής

Πίνακας 8-byte που αντιπροσωπεύει τη συγκεκριμένη τιμή

## Δείτε επίσης

* Typedef [ArrayPtr](../../arrayptr/)
* Κλάση [BitConverter](../)
* Χώρος ονομάτων [System](../../)
* Βιβλιοθήκη [Aspose.Slides](../../../)