---
title: ToSByte()
second_title: Aspose.Slides για C++ API Αναφορά
description: Μετατρέπει την καθορισμένη boolean τιμή σε ισοδύναμο 8-bit υπογεγραμμένο ακέραιο.
type: docs
weight: 105
url: /el/system/convert/tosbyte/
---
## Convert::ToSByte(bool) μέθοδος

Μετατρέπει την καθορισμένη boolean τιμή σε ισοδύναμο 8-bit υπογεγραμμένο ακέραιο.

```cpp
static constexpr int8_t System::Convert::ToSByte(bool value)
```

## Convert::ToSByte(uint8_t) μέθοδος

Μετατρέπει τον καθορισμένο 8-bit μη υπογεγραμμένο ακέραιο σε ισοδύναμο 8-bit υπογεγραμμένο ακέραιο.

```cpp
static int8_t System::Convert::ToSByte(uint8_t value)
```

## Convert::ToSByte(int8_t) μέθοδος

Επιστρέφει τον καθορισμένο 8-bit υπογεγραμμένο ακέραιο.

```cpp
static constexpr int8_t System::Convert::ToSByte(int8_t value)
```

## Convert::ToSByte(uint16_t) μέθοδος

Μετατρέπει τον καθορισμένο 16-bit μη υπογεγραμμένο ακέραιο σε ισοδύναμο 8-bit υπογεγραμμένο ακέραιο.

```cpp
static int8_t System::Convert::ToSByte(uint16_t value)
```

## Convert::ToSByte(int16_t) μέθοδος

Μετατρέπει τον καθορισμένο 16-bit υπογεγραμμένο ακέραιο σε ισοδύναμο 8-bit υπογεγραμμένο ακέραιο.

```cpp
static int8_t System::Convert::ToSByte(int16_t value)
```

## Convert::ToSByte(uint32_t) μέθοδος

Μετατρέπει τον καθορισμένο 32-bit μη υπογεγραμμένο ακέραιο σε ισοδύναμο 8-bit υπογεγραμμένο ακέραιο.

```cpp
static int8_t System::Convert::ToSByte(uint32_t value)
```

## Convert::ToSByte(int32_t) μέθοδος

Μετατρέπει τον καθορισμένο 32-bit υπογεγραμμένο ακέραιο σε ισοδύναμο 8-bit υπογεγραμμένο ακέραιο.

```cpp
static int8_t System::Convert::ToSByte(int32_t value)
```

## Convert::ToSByte(uint64_t) μέθοδος

Μετατρέπει τον καθορισμένο 64-bit μη υπογεγραμμένο ακέραιο σε ισοδύναμο 8-bit υπογεγραμμένο ακέραιο.

```cpp
static int8_t System::Convert::ToSByte(uint64_t value)
```

## Convert::ToSByte(int64_t) μέθοδος

Μετατρέπει τον καθορισμένο 64-bit υπογεγραμμένο ακέραιο σε ισοδύναμο 8-bit υπογεγραμμένο ακέραιο.

```cpp
static int8_t System::Convert::ToSByte(int64_t value)
```

## Convert::ToSByte(float) μέθοδος

Μετατρέπει τον καθορισμένο αριθμό float σε ισοδύναμο 8-bit υπογεγραμμένο ακέραιο.

```cpp
static int8_t System::Convert::ToSByte(float value)
```

## Convert::ToSByte(double) μέθοδος

Μετατρέπει τον καθορισμένο αριθμό double σε ισοδύναμο 8-bit υπογεγραμμένο ακέραιο.

```cpp
static int8_t System::Convert::ToSByte(double value)
```

## Convert::ToSByte(const Decimal\&) μέθοδος

Μετατρέπει τον καθορισμένο δεκαδικό αριθμό σε ισοδύναμο 8-bit υπογεγραμμένο ακέραιο.

```cpp
static int8_t System::Convert::ToSByte(const Decimal &value)
```

## Convert::ToSByte(char_t) μέθοδος

Μετατρέπει τον καθορισμένο unicode χαρακτήρα σε ισοδύναμο 8-bit υπογεγραμμένο ακέραιο.

```cpp
static int8_t System::Convert::ToSByte(char_t value)
```

## Convert::ToSByte(DateTime) μέθοδος

Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί InvalidCastException.

```cpp
static int8_t System::Convert::ToSByte(DateTime value)
```

## Convert::ToSByte(std::nullptr_t) μέθοδος

Μετατρέπει το καθορισμένο null-string στην ισοδύναμη 8-bit τιμή ακέραιου.

```cpp
static constexpr int8_t System::Convert::ToSByte(std::nullptr_t)
```

### Τιμή Επιστροφής

Μηδέν.

## Convert::ToSByte(const char_t *) μέθοδος

Μετατρέπει το καθορισμένο c-string που περιέχει την αναπαράσταση αριθμού σε ισοδύναμη 8-bit τιμή ακέραιου.

```cpp
static int8_t System::Convert::ToSByte(const char_t *value)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const char_t * | Το c-string που θα μετατραπεί |

### Τιμή Επιστροφής

Η 8-bit τιμή ακέραιου ίση με τον αριθμό που αναπαριστά το καθορισμένο c-string

## Convert::ToSByte(const String\&) μέθοδος

Μετατρέπει το καθορισμένο string που περιέχει την αναπαράσταση αριθμού σε ισοδύναμη 8-bit τιμή ακέραιου.

```cpp
static int8_t System::Convert::ToSByte(const String &value)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Το string που θα μετατραπεί |

### Τιμή Επιστροφής

Η 8-bit τιμή ακέραιου ίση με τον αριθμό που αναπαριστά το καθορισμένο string

## Convert::ToSByte(const String\&, int) μέθοδος

Μετατρέπει το καθορισμένο string που περιέχει την αναπαράσταση αριθμού στη συγκεκριμένη βάση σε ισοδύναμη 8-bit τιμή ακέραιου.

```cpp
static int8_t System::Convert::ToSByte(const String &value, int from_base)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Το string που θα μετατραπεί |
| from_base | int | Η βάση του αριθμού που αναπαριστά το string |

### Τιμή Επιστροφής

Η 8-bit τιμή ακέραιου ίση με τον αριθμό που αναπαριστά το καθορισμένο string

## Convert::ToSByte(const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος

Μετατρέπει το καθορισμένο string που περιέχει την αναπαράσταση αριθμού στην ισοδύναμη μη υπογεγραμμένη 8-bit τιμή ακέραιου χρησιμοποιώντας τις παρεχόμενες πληροφορίες μορφοποίησης.

```cpp
static int8_t System::Convert::ToSByte(const String &value, const SharedPtr<IFormatProvider> &provider)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Το string που θα μετατραπεί |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Ένας δείκτης σε αντικείμενο που περιέχει τις πληροφορίες μορφής του string |

### Τιμή Επιστροφής

Η 8-bit τιμή ακέραιου ίση με τον αριθμό που αναπαριστά το καθορισμένο string

## Convert::ToSByte(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος

```cpp
static int8_t System::Convert::ToSByte(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToSByte(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος

```cpp
static int8_t System::Convert::ToSByte(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToSByte(const String\&, std::nullptr_t) μέθοδος

```cpp
static int8_t System::Convert::ToSByte(const String &value, std::nullptr_t)
```

## Convert::ToSByte(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) μέθοδος

Μετατρέπει το καθορισμένο string που περιέχει την αναπαράσταση αριθμού στην ισοδύναμη 8-bit τιμή ακέραιου χρησιμοποιώντας τις παρεχόμενες πληροφορίες μορφοποίησης και το στυλ αριθμού.

```cpp
static int8_t System::Convert::ToSByte(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Το string που θα μετατραπεί |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | Συνδυασμός τιμών του enum NumberStyles που καθορίζει το επιτρεπόμενο στυλ της αναπαράστασης αριθμού |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Ένας δείκτης σε αντικείμενο που περιέχει τις πληροφορίες μορφής του string |

### Τιμή Επιστροφής

Η μη υπογεγραμμένη 8-bit τιμή ακέραιου ίση με τον αριθμό που αναπαριστά το καθορισμένο string

## Convert::ToSByte(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος

```cpp
static int8_t System::Convert::ToSByte(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToSByte(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος

```cpp
static int8_t System::Convert::ToSByte(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToSByte(const String\&, Globalization::NumberStyles, std::nullptr_t) μέθοδος

```cpp
static int8_t System::Convert::ToSByte(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToSByte(Enum) μέθοδος

```cpp
template<typename Enum,typename> static int8_t System::Convert::ToSByte(Enum value)
```

## Convert::ToSByte(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος

Μετατρέπει την καθορισμένη κελιοποιημένη τιμή στην ισοδύναμη 8-bit τιμή ακέραιου.

```cpp
static int8_t System::Convert::ToSByte(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Ο κοινόχρηστος δείκτης προς το αντικείμενο που κλειδώνει την τιμή για μετατροπή |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Η μορφή string που θα χρησιμοποιηθεί εάν ο τύπος της κελιοποιημένης τιμής είναι [String](../../string/) |

### Τιμή Επιστροφής

Μια 8-bit τιμή ακέραιου ισοδύναμη με την καθορισμένη κελιοποιημένη τιμή

## Δείτε επίσης

* Enum [NumberStyles](../../../system.globalization/numberstyles/)
* Typedef [SharedPtr](../../sharedptr/)
* Class [Decimal](../../decimal/)
* Class [DateTime](../../datetime/)
* Class [String](../../string/)
* Class [IFormatProvider](../../iformatprovider/)
* Class [CultureInfo](../../../system.globalization/cultureinfo/)
* Class [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* Class [Object](../../object/)
* Struct [Convert](../)
* Struct [Enum](../../enum/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)