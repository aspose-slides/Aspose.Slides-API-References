---
title: ToUInt64()
second_title: Αναφορά API του Aspose.Slides για C++
description: Μετατρέπει την καθορισμένη τιμή boolean σε ισοδύναμο 64-bit ακέραιο χωρίς πρόσημο.
type: docs
weight: 196
url: /el/system/convert/touint64/
---
## Convert::ToUInt64(bool) μέθοδος

Μετατρέπει την καθορισμένη τιμή boolean σε ισοδύναμο 64-bit ακέραιο χωρίς πρόσημο.

```cpp
static constexpr uint64_t System::Convert::ToUInt64(bool value)
```
## Convert::ToUInt64(uint8_t) μέθοδος

Μετατρέπει το καθορισμένο 8-bit unsigned integer σε ισοδύναμο 64-bit ακέραιο χωρίς πρόσημο.

```cpp
static constexpr uint64_t System::Convert::ToUInt64(uint8_t value)
```
## Convert::ToUInt64(int8_t) μέθοδος

Μετατρέπει το καθορισμένο 8-bit signed integer σε ισοδύναμο 64-bit ακέραιο χωρίς πρόσημο.

```cpp
static uint64_t System::Convert::ToUInt64(int8_t value)
```
## Convert::ToUInt64(uint16_t) μέθοδος

Μετατρέπει το καθορισμένο 16-bit unsigned integer σε ισοδύναμο 64-bit ακέραιο χωρίς πρόσημο.

```cpp
static constexpr uint64_t System::Convert::ToUInt64(uint16_t value)
```
## Convert::ToUInt64(int16_t) μέθοδος

Μετατρέπει το καθορισμένο 16-bit signed integer σε ισοδύναμο 64-bit ακέραιο χωρίς πρόσημο.

```cpp
static uint64_t System::Convert::ToUInt64(int16_t value)
```
## Convert::ToUInt64(uint32_t) μέθοδος

Μετατρέπει το καθορισμένο 32-bit unsigned integer σε ισοδύναμο 64-bit ακέραιο χωρίς πρόσημο.

```cpp
static constexpr uint64_t System::Convert::ToUInt64(uint32_t value)
```
## Convert::ToUInt64(int32_t) μέθοδος

Μετατρέπει το καθορισμένο 32-bit signed integer σε ισοδύναμο 64-bit ακέραιο χωρίς πρόσημο.

```cpp
static uint64_t System::Convert::ToUInt64(int32_t value)
```
## Convert::ToUInt64(uint64_t) μέθοδος

Επιστρέφει το καθορισμένο 64-bit unsigned integer.

```cpp
static constexpr uint64_t System::Convert::ToUInt64(uint64_t value)
```
## Convert::ToUInt64(int64_t) μέθοδος

Μετατρέπει το καθορισμένο 64-bit signed integer σε ισοδύναμο 64-bit ακέραιο χωρίς πρόσημο.

```cpp
static uint64_t System::Convert::ToUInt64(int64_t value)
```
## Convert::ToUInt64(float) μέθοδος

Μετατρέπει τον καθορισμένο αριθμό float σε ισοδύναμο 64-bit ακέραιο χωρίς πρόσημο.

```cpp
static uint64_t System::Convert::ToUInt64(float value)
```
## Convert::ToUInt64(double) μέθοδος

Μετατρέπει τον καθορισμένο αριθμό double σε ισοδύναμο 64-bit ακέραιο χωρίς πρόσημο.

```cpp
static uint64_t System::Convert::ToUInt64(double value)
```
## Convert::ToUInt64(const Decimal\&) μέθοδος

Μετατρέπει τον καθορισμένο δεκαδικό αριθμό σε ισοδύναμο 64-bit ακέραιο χωρίς πρόσημο.

```cpp
static uint64_t System::Convert::ToUInt64(const Decimal &value)
```
## Convert::ToUInt64(char_t) μέθοδος

Μετατρέπει τον καθορισμένο unicode χαρακτήρα σε ισοδύναμο 64-bit ακέραιο χωρίς πρόσημο.

```cpp
static constexpr uint64_t System::Convert::ToUInt64(char_t value)
```
## Convert::ToUInt64(DateTime) μέθοδος

Η μετατροπή δεν υποστηρίζεται. Εμφανίζει πάντα InvalidCastException.

```cpp
static uint64_t System::Convert::ToUInt64(DateTime value)
```
## Convert::ToUInt64(std::nullptr_t) μέθοδος

Μετατρέπει το καθορισμένο null-string στην ισοδύναμη τιμή unsigned 64-bit integer.

```cpp
static constexpr uint64_t System::Convert::ToUInt64(std::nullptr_t)
```

### Τιμή Επιστροφής

Μηδέν.

## Convert::ToUInt64(const char_t *) μέθοδος

Μετατρέπει το καθορισμένο c-string που περιέχει την αναπαράσταση αριθμού σε ισοδύναμη τιμή unsigned 64-bit integer.

```cpp
static uint64_t System::Convert::ToUInt64(const char_t *value)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const char_t * | Το c-string προς μετατροπή |

### Τιμή Επιστροφής

Η unsigned 64-bit integer τιμή ίση με τον αριθμό που αναπαρίσταται από το καθορισμένο c-string

## Convert::ToUInt64(const String\&) μέθοδος

Μετατρέπει την καθορισμένη συμβολοσειρά που περιέχει την αναπαράσταση αριθμού σε ισοδύναμη τιμή unsigned 64-bit integer.

```cpp
static uint64_t System::Convert::ToUInt64(const String &value)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Η συμβολοσειρά προς μετατροπή |

### Τιμή Επιστροφής

Η unsigned 64-bit integer τιμή ίση με τον αριθμό που αναπαρίσταται από την καθορισμένη συμβολοσειρά

## Convert::ToUInt64(const String\&, int) μέθοδος

Μετατρέπει την καθορισμένη συμβολοσειρά που περιέχει την αναπαράσταση αριθμού στη συγκεκριμένη βάση σε ισοδύναμη τιμή unsigned 64-bit integer.

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, int from_base)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Η συμβολοσειρά προς μετατροπή |
| from_base | int | Η βάση του αριθμού που αναπαρίσταται από τη συμβολοσειρά |

### Τιμή Επιστροφής

Η unsigned 64-bit integer τιμή ίση με τον αριθμό που αναπαρίσταται από την καθορισμένη συμβολοσειρά

## Convert::ToUInt64(const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος

Μετατρέπει την καθορισμένη συμβολοσειρά που περιέχει την αναπαράσταση αριθμού σε ισοδύναμη τιμή unsigned 64-bit integer χρησιμοποιώντας τις παρεχόμενες πληροφορίες μορφοποίησης.

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, const SharedPtr<IFormatProvider> &provider)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Η συμβολοσειρά προς μετατροπή |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Ένας δείκτης σε αντικείμενο που περιέχει τις πληροφορίες μορφοποίησης της συμβολοσειράς |

### Τιμή Επιστροφής

Η unsigned 64-bit integer τιμή ίση με τον αριθμό που αναπαρίσταται από την καθορισμένη συμβολοσειρά

## Convert::ToUInt64(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToUInt64(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToUInt64(const String\&, std::nullptr_t) μέθοδος

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, std::nullptr_t)
```

## Convert::ToUInt64(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) μέθοδος

Μετατρέπει την καθορισμένη συμβολοσειρά που περιέχει την αναπαράσταση αριθμού σε ισοδύναμη τιμή unsigned 64-bit integer χρησιμοποιώντας τις παρεχόμενες πληροφορίες μορφοποίησης και το στυλ αριθμού.

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Η συμβολοσειρά προς μετατροπή |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | Ένας συνδυασμός τιμών του enum NumberStyles που καθορίζει το επιτρεπτό στυλ της αναπαράστασης του αριθμού |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Ένας δείκτης σε αντικείμενο που περιέχει τις πληροφορίες μορφοποίησης της συμβολοσειράς |

### Τιμή Επιστροφής

Η unsigned 64-bit integer τιμή ίση με τον αριθμό που αναπαρίσταται από την καθορισμένη συμβολοσειρά

## Convert::ToUInt64(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToUInt64(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToUInt64(const String\&, Globalization::NumberStyles, std::nullptr_t) μέθοδος

```cpp
static uint64_t System::Convert::ToUInt64(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToUInt64(Enum) μέθοδος

```cpp
template<typename Enum,typename> static uint64_t System::Convert::ToUInt64(Enum value)
```

## Convert::ToUInt64(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος

Μετατρέπει την καθορισμένη τιμή boxed σε ισοδύναμη τιμή unsigned 64-bit integer.

```cpp
static uint64_t System::Convert::ToUInt64(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Ο shared pointer προς το αντικείμενο που περιέχει τη τιμή προς μετατροπή |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Η μορφή συμβολοσειράς που θα χρησιμοποιηθεί εάν ο τύπος της περιτυλιγμένης τιμής είναι [String](../../string/) |

### Τιμή Επιστροφής

Μια unsigned 64-bit integer τιμή ισοδύναμη με τη καθορισμένη τιμή boxed

## Δείτε επίσης

* Enum [NumberStyles](../../../system.globalization/numberstyles/)
* Typedef [SharedPtr](../../sharedptr/)
* Κλάση [Decimal](../../decimal/)
* Κλάση [DateTime](../../datetime/)
* Κλάση [String](../../string/)
* Κλάση [IFormatProvider](../../iformatprovider/)
* Κλάση [CultureInfo](../../../system.globalization/cultureinfo/)
* Κλάση [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* Κλάση [Object](../../object/)
* Δομή [Convert](../)
* Δομή [Enum](../../enum/)
* Χώρος ονομάτων [System](../../)
* Βιβλιοθήκη [Aspose.Slides](../../../)