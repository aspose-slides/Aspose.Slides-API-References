---
title: ToChar()
second_title: Aspose.Slides για C++ Αναφορά API
description: Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί την εξαίρεση InvalidCastException.
type: docs
weight: 118
url: /el/system/convert/tochar/
---
## Convert::ToChar(bool) μέθοδος

Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί την εξαίρεση InvalidCastException.

```cpp
static char_t System::Convert::ToChar(bool value)
```

## Convert::ToChar(uint8_t) μέθοδος

Μετατρέπει τον καθορισμένο 8-bit unsigned integer σε ισοδύναμο χαρακτήρα unicode.

```cpp
static constexpr char_t System::Convert::ToChar(uint8_t value)
```

## Convert::ToChar(int8_t) μέθοδος

Μετατρέπει τον καθορισμένο 8-bit signed integer σε ισοδύναμο χαρακτήρα unicode.

```cpp
static char_t System::Convert::ToChar(int8_t value)
```

## Convert::ToChar(uint16_t) μέθοδος

Μετατρέπει τον καθορισμένο 16-bit unsigned integer σε ισοδύναμο χαρακτήρα unicode.

```cpp
static constexpr char_t System::Convert::ToChar(uint16_t value)
```

## Convert::ToChar(int16_t) μέθοδος

Μετατρέπει τον καθορισμένο 16-bit signed integer σε ισοδύναμο χαρακτήρα unicode.

```cpp
static char_t System::Convert::ToChar(int16_t value)
```

## Convert::ToChar(uint32_t) μέθοδος

Μετατρέπει τον καθορισμένο 32-bit unsigned integer σε ισοδύναμο χαρακτήρα unicode.

```cpp
static char_t System::Convert::ToChar(uint32_t value)
```

## Convert::ToChar(int32_t) μέθοδος

Μετατρέπει τον καθορισμένο 32-bit signed integer σε ισοδύναμο χαρακτήρα unicode.

```cpp
static char_t System::Convert::ToChar(int32_t value)
```

## Convert::ToChar(uint64_t) μέθοδος

Μετατρέπει τον καθορισμένο 64-bit unsigned integer σε ισοδύναμο χαρακτήρα unicode.

```cpp
static char_t System::Convert::ToChar(uint64_t value)
```

## Convert::ToChar(int64_t) μέθοδος

Μετατρέπει τον καθορισμένο 64-bit signed integer σε ισοδύναμο χαρακτήρα unicode.

```cpp
static char_t System::Convert::ToChar(int64_t value)
```

## Convert::ToChar(float) μέθοδος

Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί την εξαίρεση InvalidCastException.

```cpp
static char_t System::Convert::ToChar(float value)
```

## Convert::ToChar(double) μέθοδος

Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί την εξαίρεση InvalidCastException.

```cpp
static char_t System::Convert::ToChar(double value)
```

## Convert::ToChar(const Decimal\&) μέθοδος

Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί την εξαίρεση InvalidCastException.

```cpp
static char_t System::Convert::ToChar(const Decimal &value)
```

## Convert::ToChar(char_t) μέθοδος

Επιστρέφει τον καθορισμένο χαρακτήρα unicode.

```cpp
static constexpr char_t System::Convert::ToChar(char_t value)
```

## Convert::ToChar(DateTime) μέθοδος

Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί την εξαίρεση InvalidCastException.

```cpp
static char_t System::Convert::ToChar(DateTime value)
```

## Convert::ToChar(const char_t *) μέθοδος

Μετατρέπει τον πρώτο και μοναδικό χαρακτήρα του καθορισμένου c-string σε τιμή char_t.

```cpp
static char_t System::Convert::ToChar(const char_t *value)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const char_t * | Το c-string προς μετατροπή· αναμένεται το c-string να είναι ακριβώς 1 χαρακτήρας μακρύ. |

### Τιμή Επιστροφής

Ο πρώτος και μοναδικός χαρακτήρας του καθορισμένου c-string εάν είναι ακριβώς 1 χαρακτήρας, διαφορετικά - 0

## Convert::ToChar(const String\&) μέθοδος

Μετατρέπει τον πρώτο και μοναδικό χαρακτήρα της καθορισμένης συμβολοσειράς σε τιμή char_t.

```cpp
static char_t System::Convert::ToChar(const String &value)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Η συμβολοσειρά προς μετατροπή· αναμένεται η συμβολοσειρά να είναι ακριβώς 1 χαρακτήρας μακρύ. |

### Τιμή Επιστροφής

Ο πρώτος και μοναδικός χαρακτήρας της καθορισμένης συμβολοσειράς εάν είναι ακριβώς 1 χαρακτήρας, διαφορετικά - 0

## Convert::ToChar(const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος

Μετατρέπει τον πρώτο και μοναδικό χαρακτήρα της καθορισμένης συμβολοσειράς σε τιμή char_t.

```cpp
static char_t System::Convert::ToChar(const String &value, const SharedPtr<IFormatProvider> &)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Η συμβολοσειρά προς μετατροπή· αναμένεται η συμβολοσειρά να είναι ακριβώς 1 χαρακτήρας μακρύ. |

### Τιμή Επιστροφής

Ο πρώτος και μοναδικός χαρακτήρας της καθορισμένης συμβολοσειράς εάν είναι ακριβώς 1 χαρακτήρας, διαφορετικά - 0

## Convert::ToChar(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος

Μετατρέπει την καθορισμένη περιεχυμένη τιμή σε ισοδύναμο χαρακτήρα unicode.

```cpp
static char_t System::Convert::ToChar(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Ο shared pointer στο αντικείμενο που περιέχει την τιμή προς μετατροπή |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Η μορφή συμβολοσειράς προς χρήση εάν ο τύπος της περιεχυμένης τιμής είναι [String](../../string/) |

### Τιμή Επιστροφής

Ένας χαρακτήρας unicode ισοδύναμος με την καθορισμένη περιεχυμένη τιμή

## Δείτε επίσης

* Typedef [SharedPtr](../../sharedptr/)
* Κλάση [Decimal](../../decimal/)
* Κλάση [DateTime](../../datetime/)
* Κλάση [String](../../string/)
* Κλάση [IFormatProvider](../../iformatprovider/)
* Κλάση [Object](../../object/)
* Δομή [Convert](../)
* Χώρος ονομάτων [System](../../)
* Library [Aspose.Slides](../../../)