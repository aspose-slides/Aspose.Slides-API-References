---
title: ToSingle()
second_title: Aspose.Slides για C++ Αναφορά API
description: Μετατρέπει την καθορισμένη λογική τιμή σε ισοδύναμο αριθμό κινητής υποδιαστολής μονής ακρίβειας.
type: docs
weight: 209
url: /el/system/convert/tosingle/
---
## Convert::ToSingle(bool) μέθοδος

Μετατρέπει την καθορισμένη λογική τιμή σε ισοδύναμο αριθμό κινητής υποδιαστολής μονής ακρίβειας.

```cpp
static constexpr float System::Convert::ToSingle(bool value)
```
## Convert::ToSingle(uint8_t) μέθοδος

Μετατρέπει το καθορισμένο 8-bit ακέραιο χωρίς πρόσημο σε ισοδύναμο αριθμό κινητής υποδιαστολής μονής ακρίβειας.

```cpp
static constexpr float System::Convert::ToSingle(uint8_t value)
```
## Convert::ToSingle(int8_t) μέθοδος

Μετατρέπει το καθορισμένο 8-bit ακέραιο με πρόσημο σε ισοδύναμο αριθμό κινητής υποδιαστολής μονής ακρίβειας.

```cpp
static constexpr float System::Convert::ToSingle(int8_t value)
```
## Convert::ToSingle(uint16_t) μέθοδος

Μετατρέπει το καθορισμένο 16-bit ακέραιο χωρίς πρόσημο σε ισοδύναμο αριθμό κινητής υποδιαστολής μονής ακρίβειας.

```cpp
static constexpr float System::Convert::ToSingle(uint16_t value)
```
## Convert::ToSingle(int16_t) μέθοδος

Μετατρέπει το καθορισμένο 16-bit ακέραιο με πρόσημο σε ισοδύναμο αριθμό κινητής υποδιαστολής μονής ακρίβειας.

```cpp
static constexpr float System::Convert::ToSingle(int16_t value)
```
## Convert::ToSingle(uint32_t) μέθοδος

Μετατρέπει το καθορισμένο 32-bit ακέραιο χωρίς πρόσημο σε ισοδύναμο αριθμό κινητής υποδιαστολής μονής ακρίβειας.

```cpp
static constexpr float System::Convert::ToSingle(uint32_t value)
```
## Convert::ToSingle(int32_t) μέθοδος

Μετατρέπει το καθορισμένο 32-bit ακέραιο με πρόσημο σε ισοδύναμο αριθμό κινητής υποδιαστολής μονής ακρίβειας.

```cpp
static constexpr float System::Convert::ToSingle(int32_t value)
```
## Convert::ToSingle(uint64_t) μέθοδος

Μετατρέπει το καθορισμένο 64-bit ακέραιο χωρίς πρόσημο σε ισοδύναμο αριθμό κινητής υποδιαστολής μονής ακρίβειας.

```cpp
static constexpr float System::Convert::ToSingle(uint64_t value)
```
## Convert::ToSingle(int64_t) μέθοδος

Μετατρέπει το καθορισμένο 64-bit ακέραιο με πρόσημο σε ισοδύναμο αριθμό κινητής υποδιαστολής μονής ακρίβειας.

```cpp
static constexpr float System::Convert::ToSingle(int64_t value)
```
## Convert::ToSingle(float) μέθοδος

Επιστρέφει τον καθορισμένο αριθμό float.

```cpp
static constexpr float System::Convert::ToSingle(float value)
```
## Convert::ToSingle(double) μέθοδος

Μετατρέπει τον καθορισμένο αριθμό διπλής ακρίβειας σε ισοδύναμο αριθμό κινητής υποδιαστολής μονής ακρίβειας.

```cpp
static constexpr float System::Convert::ToSingle(double value)
```
## Convert::ToSingle(const Decimal\&) μέθοδος

Μετατρέπει τον καθορισμένο δεκαδικό αριθμό σε ισοδύναμο αριθμό κινητής υποδιαστολής μονής ακρίβειας.

```cpp
static float System::Convert::ToSingle(const Decimal &value)
```
## Convert::ToSingle(char_t) μέθοδος

Η μετατροπή δεν υποστηρίζεται. Πάντα ρίχνει InvalidCastException.

```cpp
static float System::Convert::ToSingle(char_t value)
```
## Convert::ToSingle(DateTime) μέθοδος

Η μετατροπή δεν υποστηρίζεται. Πάντα ρίχνει InvalidCastException.

```cpp
static float System::Convert::ToSingle(DateTime value)
```
## Convert::ToSingle(std::nullptr_t) μέθοδος

Μετατρέπει το καθορισμένο null-string στην ισοδύναμη τιμή κινητής υποδιαστολής μονής ακρίβειας.

```cpp
static constexpr float System::Convert::ToSingle(std::nullptr_t)
```

### Τιμή Επιστροφής

Μηδέν.

## Convert::ToSingle(const char_t *) μέθοδος

Μετατρέπει το καθορισμένο c-string που περιέχει την αλφαριθμητική αναπαράσταση αριθμού στην ισοδύναμη τιμή κινητής υποδιαστολής μονής ακρίβειας.

```cpp
static float System::Convert::ToSingle(const char_t *value)
```

### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| value | const char_t * | Το c-string προς μετατροπή |

### Τιμή Επιστροφής

Η τιμή κινητής υποδιαστολής μονής ακρίβειας ισοδύναμη με τον αριθμό που αντιπροσωπεύεται από το καθορισμένο c-string

## Convert::ToSingle(const String\&) μέθοδος

Μετατρέπει το καθορισμένο string που περιέχει την αλφαριθμητική αναπαράσταση αριθμού στην ισοδύναμη τιμή κινητής υποδιαστολής μονής ακρίβειας.

```cpp
static float System::Convert::ToSingle(const String &value)
```

### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | Το string προς μετατροπή |

### Τιμή Επιστροφής

Η τιμή κινητής υποδιαστολής μονής ακρίβειας ισοδύναμη με τον αριθμό που αντιπροσωπεύεται από το καθορισμένο string

## Convert::ToSingle(const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος

Μετατρέπει το καθορισμένο string που περιέχει την αλφαριθμητική αναπαράσταση αριθμού στην ισοδύναμη τιμή κινητής υποδιαστολής μονής ακρίβειας χρησιμοποιώντας τις παρεχόμενες πληροφορίες μορφοποίησης.

```cpp
static float System::Convert::ToSingle(const String &value, const SharedPtr<IFormatProvider> &provider)
```

### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | Το string προς μετατροπή |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Δείκτης σε αντικείμενο που περιέχει τις πληροφορίες μορφοποίησης του string |

### Τιμή Επιστροφής

Η τιμή κινητής υποδιαστολής μονής ακρίβειας ισοδύναμη με τον αριθμό που αντιπροσωπεύεται από το καθορισμένο string

## Convert::ToSingle(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος

```cpp
static float System::Convert::ToSingle(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToSingle(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος

```cpp
static float System::Convert::ToSingle(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToSingle(const String\&, std::nullptr_t) μέθοδος

```cpp
static float System::Convert::ToSingle(const String &value, std::nullptr_t)
```

## Convert::ToSingle(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) μέθοδος

Μετατρέπει το καθορισμένο string που περιέχει την αλφαριθμητική αναπαράσταση αριθμού στην ισοδύναμη τιμή κινητής υποδιαστολής μονής ακρίβειας χρησιμοποιώντας τις παρεχόμενες πληροφορίες μορφοποίησης και το στυλ αριθμού.

```cpp
static float System::Convert::ToSingle(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```

### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | Το string προς μετατροπή |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | Συνδυασμός τιμών του enum NumberStyles που καθορίζει το επιτρεπτό στυλ της αλφαριθμητικής αναπαράστασης αριθμού |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Δείκτης σε αντικείμενο που περιέχει τις πληροφορίες μορφοποίησης του string |

### Τιμή Επιστροφής

Η τιμή κινητής υποδιαστολής μονής ακρίβειας ισοδύναμη με τον αριθμό που αντιπροσωπεύεται από το καθορισμένο string

## Convert::ToSingle(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος

```cpp
static float System::Convert::ToSingle(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToSingle(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος

```cpp
static float System::Convert::ToSingle(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToSingle(const String\&, Globalization::NumberStyles, std::nullptr_t) μέθοδος

```cpp
static float System::Convert::ToSingle(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToSingle(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος

Μετατρέπει την καθορισμένη τιμή σε πλαίσιο (boxed) σε τιμή κινητής υποδιαστολής μονής ακρίβειας.

```cpp
static float System::Convert::ToSingle(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```

### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Η κοινόχρηστη διεύθυνση (shared pointer) προς το αντικείμενο που περιέχει την τιμή προς μετατροπή |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Η μορφοποίηση string που θα χρησιμοποιηθεί εάν ο τύπος της τιμής σε πλαίσιο είναι [String](../../string/) |

### Τιμή Επιστροφής

Μια τιμή κινητής υποδιαστολής μονής ακρίβειας ισοδύναμη με την καθορισμένη τιμή σε πλαίσιο

## Δείτε επίσης

* Απαρίθμηση [NumberStyles](../../../system.globalization/numberstyles/)
* Typedef [SharedPtr](../../sharedptr/)
* Κλάση [Decimal](../../decimal/)
* Κλάση [DateTime](../../datetime/)
* Κλάση [String](../../string/)
* Κλάση [IFormatProvider](../../iformatprovider/)
* Κλάση [CultureInfo](../../../system.globalization/cultureinfo/)
* Κλάση [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* Κλάση [Object](../../object/)
* Δομή [Convert](../)
* ΧώροςΟνομάτων [System](../../)
* Βιβλιοθήκη [Aspose.Slides](../../../)