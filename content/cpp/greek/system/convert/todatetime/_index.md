---
title: ToDateTime()
second_title: Αναφορά API του Aspose.Slides για C++
description: Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί εξαίρεση InvalidCastException.
type: docs
weight: 248
url: /el/system/convert/todatetime/
---
## Convert::ToDateTime(bool) μέθοδος


Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί εξαίρεση InvalidCastException.

```cpp
static DateTime System::Convert::ToDateTime(bool value)
```

## Convert::ToDateTime(uint8_t) μέθοδος


Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί εξαίρεση InvalidCastException.

```cpp
static DateTime System::Convert::ToDateTime(uint8_t value)
```

## Convert::ToDateTime(int8_t) μέθοδος


Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί εξαίρεση InvalidCastException.

```cpp
static DateTime System::Convert::ToDateTime(int8_t value)
```

## Convert::ToDateTime(uint16_t) μέθοδος


Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί εξαίρεση InvalidCastException.

```cpp
static DateTime System::Convert::ToDateTime(uint16_t value)
```

## Convert::ToDateTime(int16_t) μέθοδος


Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί εξαίρεση InvalidCastException.

```cpp
static DateTime System::Convert::ToDateTime(int16_t value)
```

## Convert::ToDateTime(uint32_t) μέθοδος


Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί εξαίρεση InvalidCastException.

```cpp
static DateTime System::Convert::ToDateTime(uint32_t value)
```

## Convert::ToDateTime(int32_t) μέθοδος


Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί εξαίρεση InvalidCastException.

```cpp
static DateTime System::Convert::ToDateTime(int32_t value)
```

## Convert::ToDateTime(uint64_t) μέθοδος


Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί εξαίρεση InvalidCastException.

```cpp
static DateTime System::Convert::ToDateTime(uint64_t value)
```

## Convert::ToDateTime(int64_t) μέθοδος


Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί εξαίρεση InvalidCastException.

```cpp
static DateTime System::Convert::ToDateTime(int64_t value)
```

## Convert::ToDateTime(float) μέθοδος


Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί εξαίρεση InvalidCastException.

```cpp
static DateTime System::Convert::ToDateTime(float value)
```

## Convert::ToDateTime(double) μέθοδος


Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί εξαίρεση InvalidCastException.

```cpp
static DateTime System::Convert::ToDateTime(double value)
```

## Convert::ToDateTime(const Decimal\&) μέθοδος


Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί εξαίρεση InvalidCastException.

```cpp
static DateTime System::Convert::ToDateTime(const Decimal &value)
```

## Convert::ToDateTime(char_t) μέθοδος


Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί εξαίρεση InvalidCastException.

```cpp
static DateTime System::Convert::ToDateTime(char_t value)
```

## Convert::ToDateTime(DateTime) μέθοδος


Επιστρέφει την καθορισμένη ημερομηνία και ώρα.

```cpp
static constexpr DateTime System::Convert::ToDateTime(DateTime value)
```

## Convert::ToDateTime(const String\&) μέθοδος


Μετατρέπει τη συγκεκριμένη συμβολοσειρά σε ένα αντικείμενο της κλάσης [DateTime](../../datetime/).

```cpp
static DateTime System::Convert::ToDateTime(const String &value)
```


### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | Η συμβολοσειρά προς μετατροπή |

### Τιμή Επιστροφής

Ένα αντικείμενο της κλάσης [DateTime](../../datetime/) που αντιπροσωπεύει τις πληροφορίες ημερομηνίας και ώρας που αντιπροσωπεύει η καθορισμένη συμβολοσειρά

## Convert::ToDateTime(const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει τη συγκεκριμένη συμβολοσειρά σε ένα αντικείμενο της κλάσης [DateTime](../../datetime/) χρησιμοποιώντας τις παρεχόμενες πληροφορίες μορφοποίησης.

```cpp
static DateTime System::Convert::ToDateTime(const String &value, const SharedPtr<IFormatProvider> &fp)
```


### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | Η συμβολοσειρά προς μετατροπή |
| fp | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Δείκτης προς ένα αντικείμενο που περιέχει τις πληροφορίες μορφοποίησης συμβολοσειράς |

### Τιμή Επιστροφής

Ένα αντικείμενο της κλάσης [DateTime](../../datetime/) που αντιπροσωπεύει τις πληροφορίες ημερομηνίας και ώρας που αντιπροσωπεύει η καθορισμένη συμβολοσειρά

## Convert::ToDateTime(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος


```cpp
static DateTime System::Convert::ToDateTime(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToDateTime(const String\&, const SharedPtr\<Globalization::DateTimeFormatInfo\>\&) μέθοδος


```cpp
static DateTime System::Convert::ToDateTime(const String &value, const SharedPtr<Globalization::DateTimeFormatInfo> &dtfi)
```

## Convert::ToDateTime(const String\&, std::nullptr_t) μέθοδος


```cpp
static DateTime System::Convert::ToDateTime(const String &value, std::nullptr_t)
```

## Convert::ToDateTime(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη συσκευασμένη τιμή σε ισοδύναμη τιμή [DateTime](../../datetime/).

```cpp
static DateTime System::Convert::ToDateTime(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```


### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Ο κοινός δείκτης προς το αντικείμενο που συσκευάζει την τιμή προς μετατροπή |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Η μορφή συμβολοσειράς που θα χρησιμοποιηθεί εάν ο τύπος της συσκευασμένης τιμής είναι [String](../../string/) |

### Τιμή Επιστροφής

Μια τιμή [DateTime](../../datetime/) ισοδύναμη με την καθορισμένη συσκευασμένη τιμή

## See Also

* Typedef [SharedPtr](../../sharedptr/)
* Class [DateTime](../../datetime/)
* Class [Decimal](../../decimal/)
* Class [String](../../string/)
* Class [IFormatProvider](../../iformatprovider/)
* Class [CultureInfo](../../../system.globalization/cultureinfo/)
* Class [DateTimeFormatInfo](../../../system.globalization/datetimeformatinfo/)
* Class [Object](../../object/)
* Struct [Convert](../)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)