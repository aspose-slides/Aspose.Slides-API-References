---
title: ToInt64()
second_title: Αναφορά API του Aspose.Slides για C++
description: Μετατρέπει τη συγκεκριμένη λογική τιμή σε ισοδύναμο 64-bit υπογεγραμμένο ακέραιο.
type: docs
weight: 183
url: /el/system/convert/toint64/
---
## Convert::ToInt64(bool) method

Μετατρέπει τη συγκεκριμένη λογική τιμή σε ισοδύναμο 64-bit υπογεγραμμένο ακέραιο.

```cpp
static constexpr int64_t System::Convert::ToInt64(bool value)
```
## Convert::ToInt64(uint8_t) method

Μετατρέπει τον συγκεκριμένο 8-bit αμηνυμένο ακέραιο σε ισοδύναμο 64-bit υπογεγραμμένο ακέραιο.

```cpp
static constexpr int64_t System::Convert::ToInt64(uint8_t value)
```
## Convert::ToInt64(int8_t) method

Μετατρέπει τον συγκεκριμένο 8-bit υπογεγραμμένο ακέραιο σε ισοδύναμο 64-bit υπογεγραμμένο ακέραιο.

```cpp
static constexpr int64_t System::Convert::ToInt64(int8_t value)
```
## Convert::ToInt64(uint16_t) method

Μετατρέπει τον συγκεκριμένο 16-bit αμηνυμένο ακέραιο σε ισοδύναμο 64-bit υπογεγραμμένο ακέραιο.

```cpp
static constexpr int64_t System::Convert::ToInt64(uint16_t value)
```
## Convert::ToInt64(int16_t) method

Μετατρέπει τον συγκεκριμένο 16-bit υπογεγραμμένο ακέραιο σε ισοδύναμο 64-bit υπογεγραμμένο ακέραιο.

```cpp
static constexpr int64_t System::Convert::ToInt64(int16_t value)
```
## Convert::ToInt64(uint32_t) method

Μετατρέπει τον συγκεκριμένο 32-bit αμηνυμένο ακέραιο σε ισοδύναμο 64-bit υπογεγραμμένο ακέραιο.

```cpp
static constexpr int64_t System::Convert::ToInt64(uint32_t value)
```
## Convert::ToInt64(int32_t) method

Μετατρέπει τον συγκεκριμένο 32-bit υπογεγραμμένο ακέραιο σε ισοδύναμο 64-bit υπογεγραμμένο ακέραιο.

```cpp
static constexpr int64_t System::Convert::ToInt64(int32_t value)
```
## Convert::ToInt64(uint64_t) method

Μετατρέπει τον συγκεκριμένο 64-bit αμηνυμένο ακέραιο σε ισοδύναμο 64-bit υπογεγραμμένο ακέραιο.

```cpp
static int64_t System::Convert::ToInt64(uint64_t value)
```
## Convert::ToInt64(int64_t) method

Επιστρέφει τον συγκεκριμένο 64-bit υπογεγραμμένο ακέραιο.

```cpp
static constexpr int64_t System::Convert::ToInt64(int64_t value)
```
## Convert::ToInt64(float) method

Μετατρέπει τον συγκεκριμένο αριθμό κινητής υποδιαστολής (float) σε ισοδύναμο 64-bit υπογεγραμμένο ακέραιο.

```cpp
static int64_t System::Convert::ToInt64(float value)
```
## Convert::ToInt64(double) method

Μετατρέπει τον συγκεκριμένο αριθμό κινητής υποδιαστολής (double) σε ισοδύναμο 64-bit υπογεγραμμένο ακέραιο.

```cpp
static int64_t System::Convert::ToInt64(double value)
```
## Convert::ToInt64(const Decimal\&) method

Μετατρέπει τον συγκεκριμένο δεκαδικό αριθμό σε ισοδύναμο 64-bit υπογεγραμμένο ακέραιο.

```cpp
static int64_t System::Convert::ToInt64(const Decimal &value)
```
## Convert::ToInt64(char_t) method

Μετατρέπει τον συγκεκριμένο unicode χαρακτήρα σε ισοδύναμο 64-bit υπογεγραμμένο ακέραιο.

```cpp
static constexpr int64_t System::Convert::ToInt64(char_t value)
```
## Convert::ToInt64(DateTime) method

Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί InvalidCastException.

```cpp
static int64_t System::Convert::ToInt64(DateTime value)
```
## Convert::ToInt64(std::nullptr_t) method

Μετατρέπει τη συγκεκριμένη null-string στην ισοδύναμη 64-bit ακέραια τιμή.

```cpp
static constexpr int64_t System::Convert::ToInt64(std::nullptr_t)
```

### Τιμή Επιστροφής

Μηδέν.

## Convert::ToInt64(const char_t *) method

Μετατρέπει το συγκεκριμένο c-string που περιέχει την αναπαράσταση αριθμού σε ισοδύναμη 64-bit ακέραια τιμή.

```cpp
static int64_t System::Convert::ToInt64(const char_t *value)
```

### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| value | const char_t * | Το c-string για μετατροπή |

### Τιμή Επιστροφής

Η 64-bit ακέραια τιμή ίση με τον αριθμό που αναπαρίσταται από το συγκεκριμένο c-string

## Convert::ToInt64(const String\&) method

Μετατρέπει το συγκεκριμένο string που περιέχει την αναπαράσταση αριθμού σε ισοδύναμη 64-bit ακέραια τιμή.

```cpp
static int64_t System::Convert::ToInt64(const String &value)
```

### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | Το string για μετατροπή |

### Τιμή Επιστροφής

Η 64-bit ακέραια τιμή ίση με τον αριθμό που αναπαρίσταται από το συγκεκριμένο string

## Convert::ToInt64(const String\&, int) method

Μετατρέπει το συγκεκριμένο string που περιέχει την αναπαράσταση αριθμού στη συγκεκριμένη βάση σε ισοδύναμη 64-bit ακέραια τιμή.

```cpp
static int64_t System::Convert::ToInt64(const String &value, int from_base)
```

### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | Το string για μετατροπή |
| from_base | int | Η βάση του αριθμού που αναπαρίσταται από το string |

### Τιμή Επιστροφής

Η 64-bit ακέραια τιμή ίση με τον αριθμό που αναπαρίσταται από το συγκεκριμένο string

## Convert::ToInt64(const String\&, const SharedPtr\<IFormatProvider\>\&) method

Μετατρέπει το συγκεκριμένο string που περιέχει την αναπαράσταση αριθμού σε ισοδύναμη 64-bit ακέραια τιμή χρησιμοποιώντας τις παρεχόμενες πληροφορίες μορφοποίησης.

```cpp
static int64_t System::Convert::ToInt64(const String &value, const SharedPtr<IFormatProvider> &provider)
```

### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | Το string για μετατροπή |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Δείκτης σε αντικείμενο που περιέχει τις πληροφορίες μορφοποίησης του string |

### Τιμή Επιστροφής

Η 64-bit ακέραια τιμή ίση με τον αριθμό που αναπαρίσταται από το συγκεκριμένο string

## Convert::ToInt64(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) method

```cpp
static int64_t System::Convert::ToInt64(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToInt64(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) method

```cpp
static int64_t System::Convert::ToInt64(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToInt64(const String\&, std::nullptr_t) method

```cpp
static int64_t System::Convert::ToInt64(const String &value, std::nullptr_t)
```

## Convert::ToInt64(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) method

Μετατρέπει το συγκεκριμένο string που περιέχει την αναπαράσταση αριθμού σε ισοδύναμη 64-bit ακέραια τιμή χρησιμοποιώντας τις παρεχόμενες πληροφορίες μορφοποίησης και το στυλ αριθμού.

```cpp
static int64_t System::Convert::ToInt64(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```

### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| value | const [String](../../string/)\& | Το string για μετατροπή |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | Συνδυασμός τιμών του enum NumberStyles που καθορίζει το επιτρεπόμενο στυλ της αναπαράστασης αριθμού |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Δείκτης σε αντικείμενο που περιέχει τις πληροφορίες μορφοποίησης του string |

### Τιμή Επιστροφής

Η 64-bit ακέραια τιμή ίση με τον αριθμό που αναπαρίσταται από το συγκεκριμένο string

## Convert::ToInt64(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) method

```cpp
static int64_t System::Convert::ToInt64(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToInt64(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) method

```cpp
static int64_t System::Convert::ToInt64(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToInt64(const String\&, Globalization::NumberStyles, std::nullptr_t) method

```cpp
static int64_t System::Convert::ToInt64(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToInt64(Enum) method

```cpp
template<typename Enum,typename> static int64_t System::Convert::ToInt64(Enum value)
```

## Convert::ToInt64(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) method

Μετατρέπει την καθορισμένη αντικειμενοποιημένη τιμή σε ισοδύναμη 64-bit ακέραια τιμή.

```cpp
static int64_t System::Convert::ToInt64(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```

### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Ο shared pointer στο αντικείμενο που συσκευάζει την τιμή για μετατροπή |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Η μορφή string που θα χρησιμοποιηθεί αν ο τύπος της συσκευασμένης τιμής είναι [String](../../string/) |

### Τιμή Επιστροφής

Μια 64-bit ακέραια τιμή ισοδύναμη με την καθορισμένη αντικειμενοποιημένη τιμή

## Δείτε επίσης

* Enum [NumberStyles](../../../system.globalization/numberstyles/)
* Typedef [SharedPtr](../../sharedptr/)
* Κατηγορία [Decimal](../../decimal/)
* Κατηγορία [DateTime](../../datetime/)
* Κατηγορία [String](../../string/)
* Κατηγορία [IFormatProvider](../../iformatprovider/)
* Κατηγορία [CultureInfo](../../../system.globalization/cultureinfo/)
* Κατηγορία [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* Κατηγορία [Object](../../object/)
* Struct [Convert](../)
* Struct [Enum](../../enum/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)