---
title: ToDecimal()
second_title: Aspose.Slides για C++ Αναφορά API
description: Μετατρέπει την καθορισμένη τιμή boolean σε ένα ισοδύναμο δεκαδικό αριθμό.
type: docs
weight: 235
url: /el/system/convert/todecimal/
---
## Convert::ToDecimal(bool) μέθοδος

Μετατρέπει την καθορισμένη τιμή boolean σε ένα ισοδύναμο δεκαδικό αριθμό.

```cpp
static Decimal System::Convert::ToDecimal(bool value)
```
## Convert::ToDecimal(uint8_t) μέθοδος

Μετατρέπει τον καθορισμένο 8-bit unsigned integer σε ένα ισοδύναμο δεκαδικό αριθμό.

```cpp
static Decimal System::Convert::ToDecimal(uint8_t value)
```
## Convert::ToDecimal(int8_t) μέθοδος

Μετατρέπει τον καθορισμένο 8-bit signed integer σε ένα ισοδύναμο δεκαδικό αριθμό.

```cpp
static Decimal System::Convert::ToDecimal(int8_t value)
```
## Convert::ToDecimal(uint16_t) μέθοδος

Μετατρέπει τον καθορισμένο 16-bit unsigned integer σε ένα ισοδύναμο δεκαδικό αριθμό.

```cpp
static Decimal System::Convert::ToDecimal(uint16_t value)
```
## Convert::ToDecimal(int16_t) μέθοδος

Μετατρέπει τον καθορισμένο 16-bit signed integer σε ένα ισοδύναμο δεκαδικό αριθμό.

```cpp
static Decimal System::Convert::ToDecimal(int16_t value)
```
## Convert::ToDecimal(uint32_t) μέθοδος

Μετατρέπει τον καθορισμένο 32-bit unsigned integer σε ένα ισοδύναμο δεκαδικό αριθμό.

```cpp
static Decimal System::Convert::ToDecimal(uint32_t value)
```
## Convert::ToDecimal(int32_t) μέθοδος

Μετατρέπει τον καθορισμένο 32-bit signed integer σε ένα ισοδύναμο δεκαδικό αριθμό.

```cpp
static Decimal System::Convert::ToDecimal(int32_t value)
```
## Convert::ToDecimal(uint64_t) μέθοδος

Μετατρέπει τον καθορισμένο 64-bit unsigned integer σε ένα ισοδύναμο δεκαδικό αριθμό.

```cpp
static Decimal System::Convert::ToDecimal(uint64_t value)
```
## Convert::ToDecimal(int64_t) μέθοδος

Μετατρέπει τον καθορισμένο 64-bit signed integer σε ένα ισοδύναμο δεκαδικό αριθμό.

```cpp
static Decimal System::Convert::ToDecimal(int64_t value)
```
## Convert::ToDecimal(float) μέθοδος

Μετατρέπει τον καθορισμένο αριθμό τύπου float σε ένα ισοδύναμο δεκαδικό αριθμό.

```cpp
static Decimal System::Convert::ToDecimal(float value)
```
## Convert::ToDecimal(double) μέθοδος

Μετατρέπει τον καθορισμένο αριθμό τύπου double σε ένα ισοδύναμο δεκαδικό αριθμό.

```cpp
static Decimal System::Convert::ToDecimal(double value)
```
## Convert::ToDecimal(const Decimal\&) μέθοδος

Επιστρέφει το καθορισμένο δεκαδικό αριθμό.

```cpp
static Decimal System::Convert::ToDecimal(const Decimal &value)
```
## Convert::ToDecimal(char_t) μέθοδος

Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί InvalidCastException.

```cpp
static Decimal System::Convert::ToDecimal(char_t value)
```
## Convert::ToDecimal(DateTime) μέθοδος

Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί InvalidCastException.

```cpp
static Decimal System::Convert::ToDecimal(DateTime value)
```
## Convert::ToDecimal(std::nullptr_t) μέθοδος

Μετατρέπει την καθορισμένη μηδενική συμβολοσειρά στην ισοδύναμη [Decimal](../../decimal/) τιμή.

```cpp
static Decimal System::Convert::ToDecimal(std::nullptr_t)
```


### Return Value

Μηδέν.

## Convert::ToDecimal(const char_t *) μέθοδος

Μετατρέπει την καθορισμένη c-string που περιέχει την αναπαράσταση συμβολοσειράς ενός αριθμού στην ισοδύναμη [Decimal](../../decimal/) τιμή.

```cpp
static Decimal System::Convert::ToDecimal(const char_t *value)
```


### Arguments

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const char_t * | Η c-string προς μετατροπή |

### Return Value

Η [Decimal](../../decimal/) τιμή ίση με τον αριθμό που αντιπροσωπεύεται από την καθορισμένη c-string

## Convert::ToDecimal(const String\&) μέθοδος

Μετατρέπει την καθορισμένη συμβολοσειρά που περιέχει την αναπαράσταση συμβολοσειράς ενός αριθμού στην ισοδύναμη [Decimal](../../decimal/) τιμή.

```cpp
static Decimal System::Convert::ToDecimal(const String &value)
```


### Arguments

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Η συμβολοσειρά προς μετατροπή |

### Return Value

Η [Decimal](../../decimal/) τιμή ίση με τον αριθμό που αντιπροσωπεύεται από την καθορισμένη συμβολοσειρά

## Convert::ToDecimal(const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος

Μετατρέπει την καθορισμένη συμβολοσειρά που περιέχει την αναπαράσταση συμβολοσειράς ενός αριθμού στην ισοδύναμη [Decimal](../../decimal/) τιμή χρησιμοποιώντας τις παρεχόμενες πληροφορίες μορφοποίησης.

```cpp
static Decimal System::Convert::ToDecimal(const String &value, const SharedPtr<IFormatProvider> &provider)
```


### Arguments

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Η συμβολοσειρά προς μετατροπή |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Ένας δείκτης σε αντικείμενο που περιέχει τις πληροφορίες μορφοποίησης συμβολοσειράς |

### Return Value

Η [Decimal](../../decimal/) τιμή ίση με τον αριθμό που αντιπροσωπεύεται από την καθορισμένη συμβολοσειρά

## Convert::ToDecimal(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) μέθοδος

Μετατρέπει την καθορισμένη συμβολοσειρά που περιέχει την αναπαράσταση συμβολοσειράς ενός αριθμού στην ισοδύναμη [Decimal](../../decimal/) τιμή χρησιμοποιώντας τα καθορισμένα στυλ αριθμού και τις πληροφορίες μορφοποίησης.

```cpp
static Decimal System::Convert::ToDecimal(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```


### Arguments

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Η συμβολοσειρά προς μετατροπή |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | Ένας bitwise συνδυασμός τιμών του enum NumberStyles που καθορίζει το επιτρεπτό στυλ της συμβολοσειράς αναπαράστασης αριθμού |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Ένας δείκτης σε αντικείμενο που περιέχει τις πληροφορίες μορφοποίησης συμβολοσειράς |

### Return Value

Η [Decimal](../../decimal/) τιμή ίση με τον αριθμό που αντιπροσωπεύεται από την καθορισμένη συμβολοσειρά

## Convert::ToDecimal(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος

Μετατρέπει την καθορισμένη συσκευασμένη τιμή σε ισοδύναμη [Decimal](../../decimal/) τιμή.

```cpp
static Decimal System::Convert::ToDecimal(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```


### Arguments

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Ο κοινόχρηστος δείκτης στο αντικείμενο που συσκευάζει την τιμή προς μετατροπή |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Η μορφοποίηση συμβολοσειράς που θα χρησιμοποιηθεί εάν ο τύπος της συσκευασμένης τιμής είναι [String](../../string/) |

### Return Value

Μια [Decimal](../../decimal/) τιμή ισοδύναμη με την καθορισμένη συσκευασμένη τιμή

## Δείτε επίσης

* Enum [NumberStyles](../../../system.globalization/numberstyles/)
* Typedef [SharedPtr](../../sharedptr/)
* Class [Decimal](../../decimal/)
* Class [DateTime](../../datetime/)
* Class [String](../../string/)
* Class [IFormatProvider](../../iformatprovider/)
* Class [Object](../../object/)
* Struct [Convert](../)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)