---
title: ToUInt16()
second_title: Αναφορά API του Aspose.Slides για C++
description: Μετατρέπει την καθορισμένη λογική τιμή σε ισοδύναμο 16-bit μη υπογεγραμμένο ακέραιο.
type: docs
weight: 144
url: /el/system/convert/touint16/
---
## Convert::ToUInt16(bool) μέθοδος


Μετατρέπει την καθορισμένη λογική τιμή σε ισοδύναμο 16-bit μη υπογεγραμμένο ακέραιο.

```cpp
static constexpr uint16_t System::Convert::ToUInt16(bool value)
```

## Convert::ToUInt16(uint8_t) μέθοδος


Μετατρέπει τον καθορισμένο 8-bit μη υπογεγραμμένο ακέραιο σε ισοδύναμο 16-bit μη υπογεγραμμένο ακέραιο.

```cpp
static constexpr uint16_t System::Convert::ToUInt16(uint8_t value)
```

## Convert::ToUInt16(int8_t) μέθοδος


Μετατρέπει τον καθορισμένο 8-bit υπογεγραμμένο ακέραιο σε ισοδύναμο 16-bit μη υπογεγραμμένο ακέραιο.

```cpp
static uint16_t System::Convert::ToUInt16(int8_t value)
```

## Convert::ToUInt16(uint16_t) μέθοδος


Επιστρέφει τον καθορισμένο 16-bit μη υπογεγραμμένο ακέραιο.

```cpp
static constexpr uint16_t System::Convert::ToUInt16(uint16_t value)
```

## Convert::ToUInt16(int16_t) μέθοδος


Μετατρέπει τον καθορισμένο 16-bit υπογεγραμμένο ακέραιο σε ισοδύναμο 16-bit μη υπογεγραμμένο ακέραιο.

```cpp
static uint16_t System::Convert::ToUInt16(int16_t value)
```

## Convert::ToUInt16(uint32_t) μέθοδος


Μετατρέπει τον καθορισμένο 32-bit μη υπογεγραμμένο ακέραιο σε ισοδύναμο 16-bit μη υπογεγραμμένο ακέραιο.

```cpp
static uint16_t System::Convert::ToUInt16(uint32_t value)
```

## Convert::ToUInt16(int32_t) μέθοδος


Μετατρέπει τον καθορισμένο 32-bit υπογεγραμμένο ακέραιο σε ισοδύναμο 16-bit μη υπογεγραμμένο ακέραιο.

```cpp
static uint16_t System::Convert::ToUInt16(int32_t value)
```

## Convert::ToUInt16(uint64_t) μέθοδος


Μετατρέπει τον καθορισμένο 64-bit μη υπογεγραμμένο ακέραιο σε ισοδύναμο 16-bit μη υπογεγραμμένο ακέραιο.

```cpp
static uint16_t System::Convert::ToUInt16(uint64_t value)
```

## Convert::ToUInt16(int64_t) μέθοδος


Μετατρέπει τον καθορισμένο 64-bit υπογεγραμμένο ακέραιο σε ισοδύναμο 16-bit μη υπογεγραμμένο ακέραιο.

```cpp
static uint16_t System::Convert::ToUInt16(int64_t value)
```

## Convert::ToUInt16(float) μέθοδος


Μετατρέπει τον καθορισμένο αριθμό κινητής υποδιαστολής (float) σε ισοδύναμο 16-bit μη υπογεγραμμένο ακέραιο.

```cpp
static uint16_t System::Convert::ToUInt16(float value)
```

## Convert::ToUInt16(double) μέθοδος


Μετατρέπει τον καθορισμένο αριθμό double σε ισοδύναμο 16-bit μη υπογεγραμμένο ακέραιο.

```cpp
static uint16_t System::Convert::ToUInt16(double value)
```

## Convert::ToUInt16(const Decimal\&) μέθοδος


Μετατρέπει τον καθορισμένο δεκαδικό αριθμό σε ισοδύναμο 16-bit μη υπογεγραμμένο ακέραιο.

```cpp
static uint16_t System::Convert::ToUInt16(const Decimal &value)
```

## Convert::ToUInt16(char_t) μέθοδος


Μετατρέπει τον καθορισμένο unicode χαρακτήρα σε ισοδύναμο 16-bit μη υπογεγραμμένο ακέραιο.

```cpp
static constexpr uint16_t System::Convert::ToUInt16(char_t value)
```

## Convert::ToUInt16(DateTime) μέθοδος


Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί InvalidCastException.

```cpp
static uint16_t System::Convert::ToUInt16(DateTime value)
```

## Convert::ToUInt16(std::nullptr_t) μέθοδος


Μετατρέπει τη καθορισμένη συμβολοσειρά null σε ισοδύναμη τιμή 16-bit μη υπογεγραμμένου ακέραιου.

```cpp
static constexpr uint16_t System::Convert::ToUInt16(std::nullptr_t)
```


### Τιμή Επιστροφής

Μηδέν.

## Convert::ToUInt16(const char_t *) μέθοδος


Μετατρέπει τη καθορισμένη c-string που περιέχει τη αριθμητική αναπαράσταση σε ισοδύναμη τιμή 16-bit μη υπογεγραμμένου ακέραιου.

```cpp
static uint16_t System::Convert::ToUInt16(const char_t *value)
```


### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const char_t * | Το c-string που θα μετατραπεί |

### Τιμή Επιστροφής

Η τιμή 16-bit μη υπογεγραμμένου ακέραιου ίση με τον αριθμό που αναπαριστάται από τη συγκεκριμένη c-string

## Convert::ToUInt16(const String\&) μέθοδος


Μετατρέπει τη καθορισμένη συμβολοσειρά που περιέχει τη αριθμητική αναπαράσταση σε ισοδύναμη τιμή 16-bit μη υπογεγραμμένου ακέραιου.

```cpp
static uint16_t System::Convert::ToUInt16(const String &value)
```


### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Η συμβολοσειρά που θα μετατραπεί |

### Τιμή Επιστροφής

Η τιμή 16-bit μη υπογεγραμμένου ακέραιου ίση με τον αριθμό που αναπαριστάται από τη συγκεκριμένη συμβολοσειρά

## Convert::ToUInt16(const String\&, int) μέθοδος


Μετατρέπει τη καθορισμένη συμβολοσειρά που περιέχει τη αριθμητική αναπαράσταση σε καθορισμένη βάση σε ισοδύναμη τιμή 16-bit μη υπογεγραμμένου ακέραιου.

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, int from_base)
```


### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Η συμβολοσειρά που θα μετατραπεί |
| from_base | int | Η βάση του αριθμού που αναπαριστάται από τη συμβολοσειρά |

### Τιμή Επιστροφής

Η τιμή 16-bit μη υπογεγραμμένου ακέραιου ίση με τον αριθμό που αναπαριστάται από τη συγκεκριμένη συμβολοσειρά

## Convert::ToUInt16(const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει τη καθορισμένη συμβολοσειρά που περιέχει τη αριθμητική αναπαράσταση σε ισοδύναμη τιμή 16-bit μη υπογεγραμμένου ακέραιου χρησιμοποιώντας τις παρεχόμενες πληροφορίες μορφοποίησης.

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, const SharedPtr<IFormatProvider> &provider)
```


### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Η συμβολοσειρά που θα μετατραπεί |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Ένας δείκτης σε αντικείμενο που περιέχει τις πληροφορίες μορφοποίησης της συμβολοσειράς |

### Τιμή Επιστροφής

Η τιμή 16-bit μη υπογεγραμμένου ακέραιου ίση με τον αριθμό που αναπαριστάται από τη συγκεκριμένη συμβολοσειρά

## Convert::ToUInt16(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος




```cpp
static uint16_t System::Convert::ToUInt16(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToUInt16(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος




```cpp
static uint16_t System::Convert::ToUInt16(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToUInt16(const String\&, std::nullptr_t) μέθοδος




```cpp
static uint16_t System::Convert::ToUInt16(const String &value, std::nullptr_t)
```

## Convert::ToUInt16(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει τη καθορισμένη συμβολοσειρά που περιέχει τη αριθμητική αναπαράσταση σε ισοδύναμη τιμή 16-bit μη υπογεγραμμένου ακέραιου χρησιμοποιώντας τις παρεχόμενες πληροφορίες μορφοποίησης και το στυλ αριθμού.

```cpp
static uint16_t System::Convert::ToUInt16(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```


### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Η συμβολοσειρά που θα μετατραπεί |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | Ένας δυαδικός συνδυασμός τιμών του enum NumberStyles που καθορίζει το επιτρεπόμενο στυλ της αριθμητικής αναπαράστασης |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Ένας δείκτης σε αντικείμενο που περιέχει τις πληροφορίες μορφοποίησης της συμβολοσειράς |

### Τιμή Επιστροφής

Η τιμή 16-bit μη υπογεγραμμένου ακέραιου ίση με τον αριθμό που αναπαριστάται από τη συγκεκριμένη συμβολοσειρά

## Convert::ToUInt16(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος




```cpp
static uint16_t System::Convert::ToUInt16(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToUInt16(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος




```cpp
static uint16_t System::Convert::ToUInt16(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToUInt16(const String\&, Globalization::NumberStyles, std::nullptr_t) μέθοδος




```cpp
static uint16_t System::Convert::ToUInt16(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToUInt16(Enum) μέθοδος




```cpp
template<typename Enum,typename> static uint16_t System::Convert::ToUInt16(Enum value)
```

## Convert::ToUInt16(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη συσκευασμένη τιμή σε ισοδύναμη τιμή 16-bit μη υπογεγραμμένου ακέραιου.

```cpp
static uint16_t System::Convert::ToUInt16(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```


### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Ο shared pointer προς το αντικείμενο που συσκευάζει την τιμή προς μετατροπή |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Η μορφή συμβολοσειράς που θα χρησιμοποιηθεί αν ο τύπος της συσκευασμένης τιμής είναι [String](../../string/) |

### Τιμή Επιστροφής

Μία μη υπογεγραμμένη 16-bit ακέραια τιμή ισοδύναμη με την καθορισμένη συσκευασμένη τιμή

## Δείτε επίσης

* Enum [NumberStyles](../../../system.globalization/numberstyles/)
* Typedef [SharedPtr](../../sharedptr/)
* Class [Decimal](../../decimal/)
* Class [DateTime](../../datetime/)
* Class [String](../../string/)
* Class [IFormatProvider](../../iformatprovider/)
* Class [CultureInfo](../../../system.globalization/cultureinfo/)
* Class [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* Class [Object](../../object/)
* Struct [Convert](../)
* Struct [Enum](../../enum/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)