---
title: ToInt32()
second_title: Αναφορά API του Aspose.Slides για C++
description: Μετατρέπει την καθορισμένη λογική τιμή σε ένα ισοδύναμο 32-bit υπογεγραμμένο ακέραιο.
type: docs
weight: 157
url: /el/system/convert/toint32/
---
## Convert::ToInt32(bool) μέθοδος

Μετατρέπει την καθορισμένη λογική τιμή σε ένα ισοδύναμο 32-bit υπογεγραμμένο ακέραιο.

```cpp
static constexpr int System::Convert::ToInt32(bool value)
```

## Convert::ToInt32(uint8_t) μέθοδος

Μετατρέπει τον καθορισμένο 8-bit μη υπογεγραμμένο ακέραιο σε ένα ισοδύναμο 32-bit υπογεγραμμένο ακέραιο.

```cpp
static constexpr int System::Convert::ToInt32(uint8_t value)
```

## Convert::ToInt32(int8_t) μέθοδος

Μετατρέπει τον καθορισμένο 8-bit υπογεγραμμένο ακέραιο σε ένα ισοδύναμο 32-bit υπογεγραμμένο ακέραιο.

```cpp
static constexpr int System::Convert::ToInt32(int8_t value)
```

## Convert::ToInt32(uint16_t) μέθοδος

Μετατρέπει τον καθορισμένο 16-bit μη υπογεγραμμένο ακέραιο σε ένα ισοδύναμο 32-bit υπογεγραμμένο ακέραιο.

```cpp
static constexpr int System::Convert::ToInt32(uint16_t value)
```

## Convert::ToInt32(int16_t) μέθοδος

Μετατρέπει τον καθορισμένο 16-bit υπογεγραμμένο ακέραιο σε ένα ισοδύναμο 32-bit υπογεγραμμένο ακέραιο.

```cpp
static constexpr int System::Convert::ToInt32(int16_t value)
```

## Convert::ToInt32(uint32_t) μέθοδος

Μετατρέπει τον καθορισμένο 32-bit μη υπογεγραμμένο ακέραιο σε ένα ισοδύναμο 32-bit υπογεγραμμένο ακέραιο.

```cpp
static int System::Convert::ToInt32(uint32_t value)
```

## Convert::ToInt32(int32_t) μέθοδος

Επιστρέφει τον καθορισμένο 32-bit υπογεγραμμένο ακέραιο.

```cpp
static constexpr int System::Convert::ToInt32(int32_t value)
```

## Convert::ToInt32(uint64_t) μέθοδος

Μετατρέπει τον καθορισμένο 64-bit μη υπογεγραμμένο ακέραιο σε ένα ισοδύναμο 32-bit υπογεγραμμένο ακέραιο.

```cpp
static int System::Convert::ToInt32(uint64_t value)
```

## Convert::ToInt32(int64_t) μέθοδος

Μετατρέπει τον καθορισμένο 64-bit υπογεγραμμένο ακέραιο σε ένα ισοδύναμο 32-bit υπογεγραμμένο ακέραιο.

```cpp
static int System::Convert::ToInt32(int64_t value)
```

## Convert::ToInt32(float) μέθοδος

Μετατρέπει τον καθορισμένο αριθμό float σε ένα ισοδύναμο 32-bit υπογεγραμμένο ακέραιο.

```cpp
static int System::Convert::ToInt32(float value)
```

## Convert::ToInt32(double) μέθοδος

Μετατρέπει τον καθορισμένο αριθμό double σε ένα ισοδύναμο 32-bit υπογεγραμμένο ακέραιο.

```cpp
static int System::Convert::ToInt32(double value)
```

## Convert::ToInt32(const Decimal\&) μέθοδος

Μετατρέπει τον καθορισμένο δεκαδικό αριθμό σε ένα ισοδύναμο 32-bit υπογεγραμμένο ακέραιο.

```cpp
static int System::Convert::ToInt32(const Decimal &value)
```

## Convert::ToInt32(char_t) μέθοδος

Μετατρέπει τον καθορισμένο unicode χαρακτήρα σε ένα ισοδύναμο 32-bit υπογεγραμμένο ακέραιο.

```cpp
static constexpr int System::Convert::ToInt32(char_t value)
```

## Convert::ToInt32(DateTime) μέθοδος

Η μετατροπή δεν υποστηρίζεται. Πάντα προκαλεί InvalidCastException.

```cpp
static int System::Convert::ToInt32(DateTime value)
```

## Convert::ToInt32(std::nullptr_t) μέθοδος

Μετατρέπει τη συγκεκριμένη null-string στην ισοδύναμη τιμή 32-bit ακέραιου.

```cpp
static constexpr int System::Convert::ToInt32(std::nullptr_t)
```

### Return Value

Μηδέν.

## Convert::ToInt32(const char_t *) μέθοδος

Μετατρέπει τη συγκεκριμένη c-string που περιέχει τη συμβολοσειρά αναπαράστασης ενός αριθμού στην ισοδύναμη τιμή 32-bit ακέραιου.

```cpp
static int System::Convert::ToInt32(const char_t *value)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const char_t * | Η c-string προς μετατροπή |

### Return Value

Η τιμή 32-bit ακέραιου ίση με τον αριθμό που αναπαριστά η συγκεκριμένη c-string

## Convert::ToInt32(const String\&) μέθοδος

Μετατρέπει τη συγκεκριμένη συμβολοσειρά που περιέχει τη συμβολοσειρά αναπαράστασης ενός αριθμού στην ισοδύναμη τιμή 32-bit ακέραιου.

```cpp
static int System::Convert::ToInt32(const String &value)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Η συμβολοσειρά προς μετατροπή |

### Return Value

Η τιμή 32-bit ακέραιου ίση με τον αριθμό που αναπαριστά η συγκεκριμένη συμβολοσειρά

## Convert::ToInt32(const String\&, int) μέθοδος

Μετατρέπει τη συγκεκριμένη συμβολοσειρά που περιέχει τη συμβολοσειρά αναπαράστασης ενός αριθμού στη συγκεκριμένη βάση στην ισοδύναμη τιμή 32-bit ακέραιου.

```cpp
static int System::Convert::ToInt32(const String &value, int from_base)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Η συμβολοσειρά προς μετατροπή |
| from_base | int | Η βάση του αριθμού που αναπαριστά η συμβολοσειρά |

### Return Value

Η τιμή 32-bit ακέραιου ίση με τον αριθμό που αναπαριστά η συγκεκριμένη συμβολοσειρά

## Convert::ToInt32(const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος

Μετατρέπει τη συγκεκριμένη συμβολοσειρά που περιέχει τη συμβολοσειρά αναπαράστασης ενός αριθμού στην ισοδύναμη τιμή 32-bit ακέραιου χρησιμοποιώντας τις παρεχόμενες πληροφορίες μορφοποίησης.

```cpp
static int System::Convert::ToInt32(const String &value, const SharedPtr<IFormatProvider> &provider)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Η συμβολοσειρά προς μετατροπή |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Ένας δείκτης σε ένα αντικείμενο που περιέχει τις πληροφορίες μορφοποίησης της συμβολοσειράς |

### Return Value

Η τιμή 32-bit ακέραιου ίση με τον αριθμό που αναπαριστά η συγκεκριμένη συμβολοσειρά

## Convert::ToInt32(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος

```cpp
static int System::Convert::ToInt32(const String &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToInt32(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος

```cpp
static int System::Convert::ToInt32(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToInt32(const String\&, std::nullptr_t) μέθοδος

```cpp
static int System::Convert::ToInt32(const String &value, std::nullptr_t)
```

## Convert::ToInt32(const String\&, Globalization::NumberStyles, const SharedPtr\<IFormatProvider\>\&) μέθοδος

Μετατρέπει τη συγκεκριμένη συμβολοσειρά που περιέχει τη συμβολοσειρά αναπαράστασης ενός αριθμού στην ισοδύναμη τιμή 32-bit ακέραιου χρησιμοποιώντας τις παρεχόμενες πληροφορίες μορφοποίησης και το στυλ αριθμού.

```cpp
static int System::Convert::ToInt32(const String &value, Globalization::NumberStyles styles, const SharedPtr<IFormatProvider> &provider)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Η συμβολοσειρά προς μετατροπή |
| styles | [Globalization::NumberStyles](../../../system.globalization/numberstyles/) | Συνδυασμός τιμών του enum NumberStyles που καθορίζει το επιτρεπτό στυλ της συμβολοσειράς αριθμού |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Ένας δείκτης σε ένα αντικείμενο που περιέχει τις πληροφορίες μορφοποίησης της συμβολοσειράς |

### Return Value

Η τιμή 32-bit ακέραιου ίση με τον αριθμό που αναπαριστά η συγκεκριμένη συμβολοσειρά

## Convert::ToInt32(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος

```cpp
static int System::Convert::ToInt32(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToInt32(const String\&, Globalization::NumberStyles, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος

```cpp
static int System::Convert::ToInt32(const String &value, Globalization::NumberStyles styles, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToInt32(const String\&, Globalization::NumberStyles, std::nullptr_t) μέθοδος

```cpp
static int System::Convert::ToInt32(const String &value, Globalization::NumberStyles styles, std::nullptr_t=nullptr)
```

## Convert::ToInt32(Enum) μέθοδος

```cpp
template<typename Enum,typename> static int32_t System::Convert::ToInt32(Enum value)
```

## Convert::ToInt32(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος

Μετατρέπει την καθορισμένη τιμή σε κουτί (boxed) στην ισοδύναμη τιμή 32-bit ακέραιου.

```cpp
static int System::Convert::ToInt32(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Ο shared pointer στο αντικείμενο που περιέχει την τιμή για μετατροπή |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Η μορφή συμβολοσειράς που θα χρησιμοποιηθεί εάν ο τύπος της τιμής σε κουτί είναι [String](../../string/) |

### Return Value

Μια 32-bit ακέραια τιμή ισοδύναμη με την καθορισμένη τιμή σε κουτί

## Δείτε επίσης

* Enum [NumberStyles](../../../system.globalization/numberstyles/)
* Typedef [SharedPtr](../../sharedptr/)
* Class [Decimal](../../decimal/)
* Class [DateTime](../../datetime/)
* Class [String](../../string/)
* Class [IFormatProvider](../../iformatprovider/)
* Class [CultureInfo](../../../system.globalization/cultureinfo/)
* Class [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* Class [Object](../../object/)
* Struct [Convert](../)
* Struct [Enum](../../enum/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)