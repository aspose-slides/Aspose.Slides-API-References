---
title: ToString()
second_title: Aspose.Slides για C++ API Αναφορά
description: Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση.
type: docs
weight: 261
url: /el/system/convert/tostring/
---
## Convert::ToString(int8_t) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση.

```cpp
static String System::Convert::ToString(int8_t value)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **int8_t** | Η τιμή προς μετατροπή. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής

## Convert::ToString(uint8_t) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση.

```cpp
static String System::Convert::ToString(uint8_t value)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **uint8_t** | Η τιμή προς μετατροπή. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής

## Convert::ToString(int16_t) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση.

```cpp
static String System::Convert::ToString(int16_t value)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **int16_t** | Η τιμή προς μετατροπή. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής

## Convert::ToString(uint16_t) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση.

```cpp
static String System::Convert::ToString(uint16_t value)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **uint16_t** | Η τιμή προς μετατροπή. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής

## Convert::ToString(int32_t) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση.

```cpp
static String System::Convert::ToString(int32_t value)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **int32_t** | Η τιμή προς μετατροπή. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής

## Convert::ToString(uint32_t) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση.

```cpp
static String System::Convert::ToString(uint32_t value)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **uint32_t** | Η τιμή προς μετατροπή. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής

## Convert::ToString(int64_t) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση.

```cpp
static String System::Convert::ToString(int64_t value)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **int64_t** | Η τιμή προς μετατροπή. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής

## Convert::ToString(uint64_t) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση.

```cpp
static String System::Convert::ToString(uint64_t value)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **uint64_t** | Η τιμή προς μετατροπή. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής

## Convert::ToString(float) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση.

```cpp
static String System::Convert::ToString(float value)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **float** | Η τιμή προς μετατροπή. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής

## Convert::ToString(double) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση.

```cpp
static String System::Convert::ToString(double value)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **double** | Η τιμή προς μετατροπή. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής

## Convert::ToString(const Decimal\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση.

```cpp
static String System::Convert::ToString(const Decimal &value)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | Η τιμή προς μετατροπή. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(DateTime) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση.

```cpp
static String System::Convert::ToString(DateTime value)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | [DateTime](../../datetime/) | Η τιμή προς μετατροπή. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(int8_t, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή σε αλφαριθμητική χρησιμοποιώντας τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα.

```cpp
static String System::Convert::ToString(int8_t value, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **int8_t** | Η τιμή προς μετατροπή. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(int8_t, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος




```cpp
static String System::Convert::ToString(int8_t value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(int8_t, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος




```cpp
static String System::Convert::ToString(int8_t value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(int8_t, std::nullptr_t) μέθοδος




```cpp
static String System::Convert::ToString(int8_t value, std::nullptr_t)
```

## Convert::ToString(uint8_t, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή σε αλφαριθμητική χρησιμοποιώντας τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα.

```cpp
static String System::Convert::ToString(uint8_t value, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **uint8_t** | Η τιμή προς μετατροπή. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(uint8_t, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος




```cpp
static String System::Convert::ToString(uint8_t value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(uint8_t, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος




```cpp
static String System::Convert::ToString(uint8_t value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(uint8_t, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(uint8_t value, std::nullptr_t)
```

## Convert::ToString(int16_t, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή σε αλφαριθμητική χρησιμοποιώντας τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα.

```cpp
static String System::Convert::ToString(int16_t value, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **int16_t** | Η τιμή προς μετατροπή. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(int16_t, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος




```cpp
static String System::Convert::ToString(int16_t value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(int16_t, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(int16_t value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(int16_t, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(int16_t value, std::nullptr_t)
```

## Convert::ToString(uint16_t, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή σε αλφαριθμητική χρησιμοποιώντας τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα.

```cpp
static String System::Convert::ToString(uint16_t value, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **uint16_t** | Η τιμή προς μετατροπή. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(uint16_t, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(uint16_t value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(uint16_t, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(uint16_t value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(uint16_t, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(uint16_t value, std::nullptr_t)
```

## Convert::ToString(int32_t, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή σε αλφαριθμητική χρησιμοποιώντας τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα.

```cpp
static String System::Convert::ToString(int32_t value, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **int32_t** | Η τιμή προς μετατροπή. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(int32_t, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(int32_t value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(int32_t, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(int32_t value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(int32_t, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(int32_t value, std::nullptr_t)
```

## Convert::ToString(uint32_t, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή σε αλφαριθμητική χρησιμοποιώντας τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα.

```cpp
static String System::Convert::ToString(uint32_t value, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **uint32_t** | Η τιμή προς μετατροπή. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(uint32_t, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(uint32_t value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(uint32_t, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(uint32_t value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(uint32_t, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(uint32_t value, std::nullptr_t)
```

## Convert::ToString(int64_t, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή σε αλφαριθμητική χρησιμοποιώντας τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα.

```cpp
static String System::Convert::ToString(int64_t value, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **int64_t** | Η τιμή προς μετατροπή. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(int64_t, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(int64_t value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(int64_t, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(int64_t value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(int64_t, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(int64_t value, std::nullptr_t)
```

## Convert::ToString(uint64_t, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή σε αλφαριθμητική χρησιμοποιώντας τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα.

```cpp
static String System::Convert::ToString(uint64_t value, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **uint64_t** | Η τιμή προς μετατροπή. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(uint64_t, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(uint64_t value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(uint64_t, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(uint64_t value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(uint64_t, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(uint64_t value, std::nullptr_t)
```

## Convert::ToString(float, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή σε αλφαριθμητική χρησιμοποιώντας τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα.

```cpp
static String System::Convert::ToString(float value, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **float** | Η τιμή προς μετατροπή. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(float, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(float value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(float, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(float value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(float, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(float value, std::nullptr_t)
```

## Convert::ToString(double, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή σε αλφαριθμητική χρησιμοποιώντας τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα.

```cpp
static String System::Convert::ToString(double value, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **double** | Η τιμή προς μετατροπή. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(double, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος 




```cpp
static String System::Convert::We...
```

## Convert::ToString(double, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(double value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(double, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(double value, std::nullptr_t)
```

## Convert::ToString(const Decimal\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή σε αλφαριθμητική χρησιμοποιώντας τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα.

```cpp
static String System::Convert::ToString(const Decimal &value, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | Η τιμή προς μετατροπή. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(const Decimal\&, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(const Decimal &value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(const Decimal\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(const Decimal &value, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(const Decimal\&, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(const Decimal &value, std::nullptr_t)
```

## Convert::ToString(DateTime, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή σε αλφαριθμητική χρησιμοποιώντας τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα.

```cpp
static String System::Convert::ToString(DateTime value, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | [DateTime](../../datetime/) | Η τιμή προς μετατροπή. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(DateTime, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(DateTime value, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(DateTime, const SharedPtr\<Globalization::DateTimeFormatInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(DateTime value, const SharedPtr<Globalization::DateTimeFormatInfo> &dtfi)
```

## Convert::ToString(DateTime, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(DateTime value, std::nullptr_t)
```

## Convert::ToString(int8_t, const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση χρησιμοποιώντας την καθορισμένη μορφή συμβολοσειράς και τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα που παρέχονται από το αντικείμενο [IFormatProvider](../../iformatprovider/).

```cpp
static String System::Convert::ToString(int8_t value, const String &format, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **int8_t** | Η τιμή προς μετατροπή. |
| format | const [String](../../string/)\& | Η μορφή συμβολοσειράς. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(int8_t, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(int8_t value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(int8_t, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(int8_t value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(int8_t, const String\&, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(int8_t value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(uint8_t, const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση χρησιμοποιώντας την καθορισμένη μορφή συμβολοσειράς και τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα που παρέχονται από το αντικείμενο [IFormatProvider](../../iformatprovider/).

```cpp
static String System::Convert::ToString(uint8_t value, const String &format, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **uint8_t** | Η τιμή προς μετατροπή. |
| format | const [String](../../string/)\& | Η μορφή συμβολοσειράς. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(uint8_t, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(uint8_t value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(uint8_t, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(uint8_t value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(uint8_t, const String\&, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(uint8_t value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(int16_t, const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση χρησιμοποιώντας την καθορισμένη μορφή συμβολοσειράς και τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα που παρέχονται από το αντικείμενο [IFormatProvider](../../iformatprovider/).

```cpp
static String System::Convert::ToString(int16_t value, const String &format, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **int16_t** | Η τιμή προς μετατροπή. |
| format | const [String](../../string/)\& | Η μορφή συμβολοσειράς. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(int16_t, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(int16_t value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(int16_t, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(int16_t value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(int16_t, const String\&, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(int16_t value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(uint16_t, const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση χρησιμοποιώντας την καθορισμένη μορφή συμβολοσειράς και τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα που παρέχονται από το αντικείμενο [IFormatProvider](../../iformatprovider/).

```cpp
static String System::Convert::ToString(uint16_t value, const String &format, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **uint16_t** | Η τιμή προς μετατροπή. |
| format | const [String](../../string/)\& | Η μορφή συμβολοσειράς. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(uint16_t, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(uint16_t value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(uint16_t, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(uint16_t value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(uint16_t, const String\&, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(uint16_t value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(int32_t, const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση χρησιμοποιώντας την καθορισμένη μορφή συμβολοσειράς και τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα που παρέχονται από το αντικείμενο [IFormatProvider](../../iformatprovider/).

```cpp
static String System::Convert::ToString(int32_t value, const String &format, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **int32_t** | Η τιμή προς μετατροπή. |
| format | const [String](../../string/)\& | Η μορφή συμβολοσειράς. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(int32_t, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(int32_t value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(int32_t, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(int32_t value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(int32_t, const String\&, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(int32_t value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(uint32_t, const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση χρησιμοποιώντας την καθορισμένη μορφή συμβολοσειράς και τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα που παρέχονται από το αντικείμενο [IFormatProvider](../../iformatprovider/).

```cpp
static String System::Convert::ToString(uint32_t value, const String &format, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **uint32_t** | Η τιμή προς μετατροπή. |
| format | const [String](../../string/)\& | Η μορφή συμβολοσειράς. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(uint32_t, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(uint32_t value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(uint32_t, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(uint32_t value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(uint32_t, const String\&, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(uint32_t value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(int64_t, const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση χρησιμοποιώντας την καθορισμένη μορφή συμβολοσειράς και τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα που παρέχονται από το αντικείμενο [IFormatProvider](../../iformatprovider/).

```cpp
static String System::Convert::ToString(int64_t value, const String &format, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **int64_t** | Η τιμή προς μετατροπή. |
| format | const [String](../../string/)\& | Η μορφή συμβολοσειράς. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(int64_t, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(int64_t value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(int64_t, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(int64_t value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(int64_t, const String\&, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(int64_t value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(uint64_t, const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση χρησιμοποιώντας την καθορισμένη μορφή συμβολοσειράς και τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα που παρέχονται από το αντικείμενο [IFormatProvider](../../iformatprovider/).

```cpp
static String System::Convert::ToString(uint64_t value, const String &format, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **uint64_t** | Η τιμή προς μετατροπή. |
| format | const [String](../../string/)\& | Η μορφή συμβολοσειράς. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(uint64_t, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(uint64_t value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(uint64_t, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(uint64_t value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(uint64_t, const String\&, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(uint64_t value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(float, const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση χρησιμοποιώντας την καθορισμένη μορφή συμβολοσειράς και τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα που παρέχονται από το αντικείμενο [IFormatProvider](../../iformatprovider/).

```cpp
static String System::Convert::ToString(float value, const String &format, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **float** | Η τιμή προς μετατροπή. |
| format | const [String](../../string/)\& | Η μορφή συμβολοσειράς. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(float, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(float value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(float, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(float value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(float, const String\&, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(float value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(double, const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση χρησιμοποιώντας την καθορισμένη μορφή συμβολοσειράς και τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα που παρέχονται από το αντικείμενο [IFormatProvider](../../iformatprovider/).

```cpp
static String System::Convert::ToString(double value, const String &format, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **double** | Η τιμή προς μετατροπή. |
| format | const [String](../../string/)\& | Η μορφή συμβολοσειράς. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(double, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(double value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(double, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(double value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(double, const String\&, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(double value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(const Decimal\&, const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση χρησιμοποιώντας την καθορισμένη μορφή συμβολοσειράς και τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα που παρέχονται από το αντικείμενο [IFormatProvider](../../iformatprovider/).

```cpp
static String System::Convert::ToString(const Decimal &value, const String &format, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | Η τιμή προς μετατροπή. |
| format | const [String](../../string/)\& | Η μορφή συμβολοσειράς. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(const Decimal\&, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(const Decimal &value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(const Decimal\&, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(const Decimal &value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(const Decimal\&, const String\&, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(const Decimal &value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(DateTime, const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση χρησιμοποιώντας την καθορισμένη μορφή συμβολοσειράς και τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα που παρέχονται από το αντικείμενο [IFormatProvider](../../iformatprovider/).

```cpp
static String System::Convert::ToString(DateTime value, const String &format, const SharedPtr<IFormatProvider> &provider)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | [DateTime](../../datetime/) | Η τιμή προς μετατροπή. |
| format | const [String](../../string/)\& | Η μορφή συμβολοσειράς. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Το αντικείμενο [IFormatProvider](../../iformatprovider/) που παρέχει τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής.

## Convert::ToString(DateTime, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(DateTime value, const String &format, const SharedPtr<Globalization::CultureInfo> &culture)
```

## Convert::ToString(DateTime, const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος 




```cpp
static String System::Convert::ToString(DateTime value, const String &format, const SharedPtr<Globalization::NumberFormatInfo> &nfi)
```

## Convert::ToString(DateTime, const String\&, std::nullptr_t) μέθοδος 




```cpp
static String System::Convert::ToString(DateTime value, const String &format, std::nullptr_t=nullptr)
```

## Convert::ToString(const Guid\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή σε αλφαριθμητική.

```cpp
static String System::Convert::ToString(const Guid &value)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [Guid](../../guid/)\& | Η τιμή προς μετατροπή |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής

## Convert::ToString(const Guid\&, const String\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή σε αλφαριθμητική χρησιμοποιώντας την καθορισμένη μορφή συμβολοσειράς.

```cpp
static String System::Convert::ToString(const Guid &value, const String &format)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [Guid](../../guid/)\& | Η τιμή προς μετατροπή |
| format | const [String](../../string/)\& | Η μορφή συμβολοσειράς. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής

## Convert::ToString(const char_t(&), std::nullptr_t) μέθοδος


Μετατρέπει τον καθορισμένο πίνακα χαρακτήρων Unicode σε αλφαριθμητική.

```cpp
template<size_t> static String System::Convert::ToString(const char_t(&value)[N], std::nullptr_t=nullptr)
```


### Παράμετροι προτύπου

| Παράμετρος | Περιγραφή |
| --- | --- |
| N | Το μέγεθος του πίνακα. |

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const char_t(&) | Ο πίνακας προς μετατροπή. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση του καθορισμένου πίνακα.

## Convert::ToString(const char_t(&), const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει τον καθορισμένο πίνακα χαρακτήρων Unicode σε αλφαριθμητική χρησιμοποιώντας τις πληροφορίες μορφοποίησης ειδικές για την κουλτούρα που παρέχονται από το αντικείμενο [IFormatProvider](../../iformatprovider/).

```cpp
template<size_t> static String System::Convert::ToString(const char_t(&value)[N], const SharedPtr<IFormatProvider> &)
```


### Παράμετροι προτύπου

| Παράμετρος | Περιγραφή |
| --- | --- |
| N | Το μέγεθος του πίνακα. |

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const char_t(&) | Ο πίνακας προς μετατροπή. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση του καθορισμένου πίνακα.

## Convert::ToString(const char_t(&), const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος 




```cpp
template<size_t> static String System::Convert::ToString(const char_t(&value)[N], const SharedPtr<Globalization::CultureInfo> &)
```

## Convert::ToString(const String\&, std::nullptr_t) μέθοδος


Επιστρέφει την καθορισμένη τιμή· δεν εκτελείται μετατροπή.

```cpp
static String System::Convert::ToString(const String &value, std::nullptr_t=nullptr)
```

## Convert::ToString(const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Επιστρέφει την καθορισμένη τιμή· δεν εκτελείται μετατροπή.

```cpp
static
```

## Convert::ToString(const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος


Επιστρέφει την καθορισμένη τιμή· δεν εκτελείται μετατροπή.

```cpp
static String System::Convert::ToString(const String &value, const SharedPtr<Globalization::CultureInfo> &)
```

## Convert::ToString(const String\&, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος


Επιστρέφει την καθορισμένη τιμή· δεν εκτελείται μετατροπή.

```cpp
static String System::Convert::ToString(const String &value, const SharedPtr<Globalization::NumberFormatInfo> &)
```

## Convert::ToString(char_t, std::nullptr_t) μέθοδος


Επιστρέφει την καθορισμένη τιμή· δεν εκτελείται μετατροπή.

```cpp
static String System::Convert::ToString(char_t value, std::nullptr_t=nullptr)
```

## Convert::ToString(char_t, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Επιστρέφει την καθορισμένη τιμή· δεν εκτελείται μετατροπή.

```cpp
static String System::Convert::ToString(char_t value, const SharedPtr<IFormatProvider> &)
```

## Convert::ToString(char_t, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος


Επιστρέφει την καθορισμένη τιμή· δεν εκτελείται μετατροπή.

```cpp
static String System::Convert::ToString(char_t value, const SharedPtr<Globalization::CultureInfo> &)
```

## Convert::ToString(char_t, const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Επιστρέφει την καθορισμένη τιμή· δεν εκτελείται μετατροπή.

```cpp
static String System::Convert::ToString(char_t value, const String &, const SharedPtr<IFormatProvider> &)
```

## Convert::ToString(char_t, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος


Επιστρέφει την καθορισμένη τιμή· δεν εκτελείται μετατροπή.

```cpp
static String System::Convert::ToString(char_t value, const String &, const SharedPtr<Globalization::CultureInfo> &)
```

## Convert::ToString(char_t, const String\&, std::nullptr_t) μέθοδος


Επιστρέφει την καθορισμένη τιμή· δεν εκτελείται μετατροπή.

```cpp
static String System::Convert::ToString(char_t value, const String &, std::nullptr_t)
```

## Convert::ToString(bool, std::nullptr_t) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση.

```cpp
static String System::Convert::ToString(bool value, std::nullptr_t=nullptr)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **bool** | Η τιμή προς μετατροπή. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής

## Convert::ToString(bool, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση.

```cpp
static String System::Convert::ToString(bool value, const SharedPtr<IFormatProvider> &)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **bool** | Η τιμή προς μετατροπή. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής

## Convert::ToString(bool, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση.

```cpp
static String System::Convert::ToString(bool value, const SharedPtr<Globalization::CultureInfo> &)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **bool** | Η τιμή προς μετατροπή. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής

## Convert::ToString(bool, const SharedPtr\<Globalization::NumberFormatInfo\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση.

```cpp
static String System::Convert::ToString(bool value, const SharedPtr<Globalization::NumberFormatInfo> &)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **bool** | Η τιμή προς μετατροπή. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής

## Convert::ToString(bool, const String\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση.

```cpp
static String System::Convert::ToString(bool value, const String &, const SharedPtr<IFormatProvider> &)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **bool** | Η τιμή προς μετατροπή. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής

## Convert::ToString(bool, const String\&, const SharedPtr\<Globalization::CultureInfo\>\&) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση.

```cpp
static String System::Convert::ToString(bool value, const String &, const SharedPtr<Globalization::CultureInfo> &)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **bool** | Η τιμή προς μετατροπή. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής

## Convert::ToString(bool, const String\&, std::nullptr_t) μέθοδος


Μετατρέπει την καθορισμένη τιμή στην αλφαριθμητική της αναπαράσταση.

```cpp
static String System::Convert::ToString(bool value, const String &, std::nullptr_t)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **bool** | Η τιμή προς μετατροπή. |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής

## Convert::ToString(int8_t, int) μέθοδος


Μετατρέπει την καθορισμένη ακέραια τιμή στην αλφαριθμητική της αναπαράσταση στη καθορισμένη βάση.

```cpp
static String System::Convert::ToString(int8_t value, int to_base)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **int8_t** | Η τιμή προς μετατροπή |
| to_base | int | Η βάση στην οποία θα αναπαρασταθεί η καθορισμένη τιμή |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής στη καθορισμένη βάση

## Convert::ToString(int16_t, int) μέθοδος


Μετατρέπει την καθορισμένη ακέραια τιμή στην αλφαριθμητική της αναπαράσταση στη καθορισμένη βάση.

```cpp
static String System::Convert::ToString(int16_t value, int to_base)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **int16_t** | Η τιμή προς μετατροπή |
| to_base | int | Η βάση στην οποία θα αναπαρασταθεί η καθορισμένη τιμή |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής στη καθορισμένη βάση

## Convert::ToString(int32_t, int) μέθοδος


Μετατρέπει την καθορισμένη ακέραια τιμή στην αλφαριθμητική της αναπαράσταση στη καθορισμένη βάση.

```cpp
static String System::Convert::ToString(int32_t value, int to_base)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **int32_t** | Η τιμή προς μετατροπή |
| to_base | int | Η βάση στην οποία θα αναπαρασταθεί η καθορισμένη τιμή |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής στη καθορισμένη βάση

## Convert::ToString(int64_t, int) μέθοδος


Μετατρέπει την καθορισμένη ακέραια τιμή στην αλφαριθμητική της αναπαράσταση στη καθορισμένη βάση.

```cpp
static String System::Convert::ToString(int64_t value, int to_base)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **int64_t** | Η τιμή προς μετατροπή |
| to_base | int | Η βάση στην οποία θα αναπαρασταθεί η καθορισμένη τιμή |

### Τιμή Επιστροφής

Η αλφαριθμητική αναπαράσταση της καθορισμένης τιμής στη καθορισμένη βάση

## Convert::ToString(uint8_t, int) μέθοδος




```cpp
static String System::Convert::ToString(uint8_t value, int to_base)
```

## Convert::ToString(uint16_t, int) μέθοδος 




```cpp
static String System::Convert::ToString(uint16_t value, int to_base)
```

## Convert::ToString(uint32_t, int) μέθοδος 




```cpp
static String System::Convert::ToString(uint32_t value, int to_base)
```

## Convert::ToString(const SharedPtr\<Object\>\&, const SharedPtr\<IFormatProvider\>\&) μέθοδος


Μετατρέπει την καθορισμένη αντικειμενοποιημένη τιμή στην αλφαριθμητική της αναπαράσταση. Εάν ο τύπος της αντικειμενοποιημένης τιμής είναι [String](../../string/), η καθορισμένη μορφή συμβολοσειράς χρησιμοποιείται κατά τη μετατροπή.

```cpp
static String System::Convert::ToString(const SharedPtr<Object> &obj, const SharedPtr<IFormatProvider> &provider=nullptr)
```


### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | Ο κοινός δείκτης στο αντικείμενο που περιέχει την τιμή προς μετατροπή |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Η μορφή συμβολοσειράς που θα χρησιμοποιηθεί εάν ο τύπος της αντικειμενοποιημένης τιμής είναι [String](../../string/) |

### Τιμή Επιστροφής

Μια αλφαριθμητική που αντιπροσωπεύει την καθορισμένη αντικειμενοποιημένη τιμή

## See Also

* Typedef [SharedPtr](../../sharedptr/)
* Class [String](../../string/)
* Class [Decimal](../../decimal/)
* Class [DateTime](../../datetime/)
* Class [IFormatProvider](../../iformatprovider/)
* Class [CultureInfo](../../../system.globalization/cultureinfo/)
* Class [NumberFormatInfo](../../../system.globalization/numberformatinfo/)
* Class [DateTimeFormatInfo](../../../system.globalization/datetimeformatinfo/)
* Class [Guid](../../guid/)
* Class [Object](../../object/)
* Struct [Convert](../)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)