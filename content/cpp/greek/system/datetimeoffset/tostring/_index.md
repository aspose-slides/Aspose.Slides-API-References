---
title: ToString()
second_title: Aspose.Slides για C++ API Αναφορά
description: Μετατρέπει το τρέχον αντικείμενο σε συμβολοσειρά χρησιμοποιώντας το καθορισμένο μορφότυπο και τον παροχέα μορφοποίησης.
type: docs
weight: 443
url: /el/system/datetimeoffset/tostring/
---
## DateTimeOffset::ToString(const String\&, const SharedPtr\<IFormatProvider\>\&) const μέθοδος

Μετατρέπει το τρέχον αντικείμενο σε συμβολοσειρά χρησιμοποιώντας το καθορισμένο μορφότυπο και τον παροχέα μορφοποίησης.

```cpp
String System::DateTimeOffset::ToString(const String &format, const SharedPtr<IFormatProvider> &provider) const
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| format | const [String](../../string/)\& | Συμβολοσειρά μορφής. |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Παροχέας μορφοποίησης. |

### Τιμή Επιστροφής

[String](../../string/) αναπαράσταση του τρέχοντος [DateTimeOffset](../) αντικειμένου.

## DateTimeOffset::ToString(const SharedPtr\<IFormatProvider\>\&) const μέθοδος

Μετατρέπει το τρέχον αντικείμενο σε συμβολοσειρά χρησιμοποιώντας τον καθορισμένο παροχέα μορφοποίησης.

```cpp
String System::DateTimeOffset::ToString(const SharedPtr<IFormatProvider> &provider) const
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| provider | const [SharedPtr](../../sharedptr/)\<[IFormatProvider](../../iformatprovider/)\>\& | Παροχέας μορφοποίησης. |

### Τιμή Επιστροφής

[String](../../string/) αναπαράσταση του τρέχοντος [DateTimeOffset](../) αντικειμένου.

## DateTimeOffset::ToString(const String\&) const μέθοδος

Μετατρέπει το τρέχον αντικείμενο σε συμβολοσειρά χρησιμοποιώντας το καθορισμένο μορφότυπο.

```cpp
String System::DateTimeOffset::ToString(const String &format) const
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| format | const [String](../../string/)\& | Συμβολοσειρά μορφής. |

### Τιμή Επιστροφής

[String](../../string/) αναπαράσταση του τρέχοντος [DateTimeOffset](../) αντικειμένου.

## DateTimeOffset::ToString() const μέθοδος

Μετατρέπει το τρέχον αντικείμενο σε συμβολοσειρά.

```cpp
String System::DateTimeOffset::ToString() const
```

### Τιμή Επιστροφής

[String](../../string/) αναπαράσταση του τρέχοντος [DateTimeOffset](../) αντικειμένου.

## Δείτε επίσης

* Typedef [SharedPtr](../../sharedptr/)
* Class [String](../../string/)
* Class [IFormatProvider](../../iformatprovider/)
* Class [DateTimeOffset](../)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)