---
title: Equals()
second_title: Αναφορά API Aspose.Slides για C++
description: Ελέγχει εάν δύο αντικείμενα DateTimeOffset αντιπροσωπεύουν το ίδιο χρονικό σημείο.
type: docs
weight: 365
url: /el/system/datetimeoffset/equals/
---
## DateTimeOffset::Equals(const DateTimeOffset\&) const μέθοδος

Ελέγχει εάν δύο [DateTimeOffset](../) αντικείμενα αντιπροσωπεύουν το ίδιο χρονικό σημείο.

```cpp
bool System::DateTimeOffset::Equals(const DateTimeOffset &other) const
```

### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/) για σύγκριση. |

### Τιμή Επιστροφής

true εάν το τρέχον αντικείμενο είναι ίσο με **other**, διαφορετικά - false.

## DateTimeOffset::Equals(const SharedPtr\<Object\>\&) const μέθοδος

Ελέγχει εάν δύο [DateTimeOffset](../) αντικείμενα αντιπροσωπεύουν το ίδιο χρονικό σημείο.

```cpp
bool System::DateTimeOffset::Equals(const SharedPtr<Object> &obj) const
```

### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/) για σύγκριση. |

### Τιμή Επιστροφής

true εάν το τρέχον αντικείμενο είναι ίσο με **obj**, διαφορετικά - false.

## DateTimeOffset::Equals(const DateTimeOffset\&, const DateTimeOffset\&) μέθοδος

Ελέγχει εάν δύο [DateTimeOffset](../) αντικείμενα αντιπροσωπεύουν το ίδιο χρονικό σημείο.

```cpp
static bool System::DateTimeOffset::Equals(const DateTimeOffset &first, const DateTimeOffset &second)
```

### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| first | const [DateTimeOffset](../)\& | Πρώτο αντικείμενο για σύγκριση. |
| second | const [DateTimeOffset](../)\& | Δεύτερο αντικείμενο για σύγκριση. |

### Τιμή Επιστροφής

true εάν **first** είναι ίσον με **second**, διαφορετικά - false.

## Δείτε επίσης

* Typedef [SharedPtr](../../sharedptr/)
* Class [DateTimeOffset](../)
* Class [Object](../../object/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)