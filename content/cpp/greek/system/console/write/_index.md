---
title: Write()
second_title: Aspose.Slides για C++ Αναφορά API
description: Εξάγει την αναπαράσταση χαρακτήρων του καθορισμένου αντικειμένου στη ροή τυπικής εξόδου.
type: docs
weight: 1
url: /el/system/console/write/
---
## Console::Write(const SharedPtr\<T\>\&) μέθοδος

Εξάγει την αναπαράσταση χαρακτήρων του καθορισμένου αντικειμένου στη ροή τυπικής εξόδου.

```cpp
template<class T> static void System::Console::Write(const SharedPtr<T> &object)
```

### Παράμετροι προτύπου

| Παράμετρος | Περιγραφή |
| --- | --- |
| T | Τύπος του αντικειμένου για έξοδο |

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| object | const [SharedPtr](../../sharedptr/)\<T\>\& | [Object](../../object/) για έξοδο |

## Console::Write(bool) μέθοδος

Εξάγει την αναπαράσταση χαρακτήρων της τιμής bool στη ροή τυπικής εξόδου.

```cpp
static void System::Console::Write(bool value)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **bool** | Η τιμή για έξοδο |

## Console::Write(char_t) μέθοδος

Εξάγει την καθορισμένη τιμή χαρακτήρα στη ροή τυπικής εξόδου.

```cpp
static void System::Console::Write(char_t value)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | char_t | Η τιμή για έξοδο |

## Console::Write(const ArrayPtr\<char_t\>\&) μέθοδος

Εξάγει την αναπαράσταση χαρακτήρων του καθορισμένου πίνακα χαρακτήρων στη ροή τυπικής εξόδου.

```cpp
static void System::Console::Write(const ArrayPtr<char_t> &buffer)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Ο πίνακας για έξοδο |

## Console::Write(const Decimal\&) μέθοδος

Εξάγει την αναπαράσταση χαρακτήρων της τιμής [Decimal](../../decimal/) στη ροή τυπικής εξόδου.

```cpp
static void System::Console::Write(const Decimal &value)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | Η τιμή για έξοδο |

## Console::Write(double) μέθοδος

Εξάγει την αναπαράσταση χαρακτήρων της τιμής double-precision floating-point στη ροή τυπικής εξόδου.

```cpp
static void System::Console::Write(double value)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **double** | Η τιμή για έξοδο |

## Console::Write(float) μέθοδος

Εξάγει την αναπαράσταση χαρακτήρων της τιμής single-precision floating-point στη ροή τυπικής εξόδου.

```cpp
static void System::Console::Write(float value)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **float** | Η τιμή για έξοδο |

## Console::Write(int32_t) μέθοδος

Εξάγει την αναπαράσταση χαρακτήρων της τιμής 32-bit integer στη ροή τυπικής εξόδου.

```cpp
static void System::Console::Write(int32_t value)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **int32_t** | Η τιμή για έξοδο |

## Console::Write(int64_t) μέθοδος

Εξάγει την αναπαράσταση χαρακτήρων της τιμής 64-bit integer στη ροή τυπικής εξόδου.

```cpp
static void System::Console::Write(int64_t value)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **int64_t** | Η τιμή για έξοδο |

## Console::Write(const String\&) μέθοδος

Εξάγει το καθορισμένο αντικείμενο string στη ροή τυπικής εξόδου.

```cpp
static void System::Console::Write(const String &value)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [String](../../string/)\& | Το αντικείμενο string για έξοδο |

## Console::Write(const char_t *) μέθοδος

Εξάγει το καθορισμένο c-string στη ροή τυπικής εξόδου.

```cpp
static void System::Console::Write(const char_t *value)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const char_t * | Το c-string για έξοδο |

## Console::Write(const TypeInfo\&) μέθοδος

Εξάγει την αναπαράσταση χαρακτήρων της τιμής [TypeInfo](../../typeinfo/) στη ροή τυπικής εξόδου.

```cpp
static void System::Console::Write(const TypeInfo &value)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | Η τιμή για έξοδο |

## Console::Write(uint32_t) μέθοδος

Εξάγει την αναπαράσταση χαρακτήρων της τιμής unsigned 32-bit integer στη ροή τυπικής εξόδου.

```cpp
static void System::Console::Write(uint32_t value)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **uint32_t** | Η τιμή για έξοδο |

## Console::Write(uint64_t) μέθοδος

Εξάγει την αναπαράσταση χαρακτήρων της τιμής unsigned 64-bit integer στη ροή τυπικής εξόδου.

```cpp
static void System::Console::Write(uint64_t value)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | **uint64_t** | Η τιμή για έξοδο |

## Console::Write(const ArrayPtr\<char_t\>\&, int32_t, int32_t) μέθοδος

Εξάγει την αναπαράσταση χαρακτήρων του καθορισμένου εύρους του καθορισμένου πίνακα χαρακτήρων στη ροή τυπικής εξόδου.

```cpp
static void System::Console::Write(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | Ο πίνακας χαρακτήρων |
| index | **int32_t** | Ο δείκτης στον πίνακα από τον οποίο αρχίζει το εύρος για έξοδο |
| count | **int32_t** | Ο αριθμός των στοιχείων στο εύρος για έξοδο |

## Console::Write(const String\&, Args\&&...) μέθοδος

Εξάγει την αναπαράσταση χαρακτήρων των καθορισμένων ορισμάτων μορφοποιημένων σύμφωνα με τη καθορισμένη μορφή στη ροή τυπικής εξόδου.

```cpp
template<class...> static void System::Console::Write(const String &format, Args &&... args)
```

### Παράμετροι προτύπου

| Παράμετρος | Περιγραφή |
| --- | --- |
| Οι | τύποι των τιμών για έξοδο |

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| format | const [String](../../string/)\& | Η μορφή του string |
| args | Args\&&... | Οι τιμές για έξοδο |

## Console::Write(const char *) μέθοδος




```cpp
static void System::Console::Write(const char *)=delete
```

## Δείτε επίσης

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Class [Console](../)
* Class [Decimal](../../decimal/)
* Class [String](../../string/)
* Class [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)