---
title: GetHashCode()
second_title: Αναφορά API του Aspose.Slides για C++
description: Επιστρέφει έναν κωδικό κατακερματισμού για τη συγκεκριμένη τιμή κλίμακας.
type: docs
weight: 2445
url: /el/system/gethashcode/
---
## System::GetHashCode(const T\&) συνάρτηση


Επιστρέφει έναν κωδικό κατακερματισμού για τη συγκεκριμένη τιμή κλίμακας.

```cpp
template<typename T> std::enable_if<std::is_scalar<T>::value, int>::type System::GetHashCode(const T &obj)
```


### Παράμετροι προτύπου

| Παράμετρος | Περιγραφή |
| --- | --- |
| T | Ο τύπος της τιμής για την οποία η συνάρτηση δημιουργεί κωδικό κατακερματισμού |

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| obj | const T\& | Η τιμή για την οποία δημιουργείται κωδικός κατακερματισμού |

### Τιμή επιστροφής

Ο κωδικός κατακερματισμού που δημιουργείται για τη συγκεκριμένη τιμή

## System::GetHashCode(const T\&) συνάρτηση


Επιστρέφει έναν κωδικό κατακερματισμού για το συγκεκριμένο αντικείμενο.

```cpp
template<typename T> std::enable_if<!std::is_scalar<T>::value &&System::IsSmartPtr<T>::value, int>::type System::GetHashCode(const T &obj)
```


### Παράμετροι προτύπου

| Παράμετρος | Περιγραφή |
| --- | --- |
| T | Ο τύπος του αντικειμένου για το οποίο η συνάρτηση δημιουργεί κωδικό κατακερματισμού |

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| obj | const T\& | Το [SmartPtr](../smartptr/) που δείχνει στο αντικείμενο για το οποίο δημιουργείται κωδικός κατακερματισμού |

### Τιμή επιστροφής

Ο κωδικός κατακερματισμού που δημιουργείται για το συγκεκριμένο αντικείμενο

## System::GetHashCode(const T\&) συνάρτηση


Επιστρέφει έναν κωδικό κατακερματισμού για το συγκεκριμένο αντικείμενο που είναι εξαίρεση.

```cpp
template<typename T> std::enable_if<System::IsExceptionWrapper<T>::value, int>::type System::GetHashCode(const T &obj)
```


### Παράμετροι προτύπου

| Παράμετρος | Περιγραφή |
| --- | --- |
| T | Ο τύπος του αντικειμένου για το οποίο η συνάρτηση δημιουργεί κωδικό κατακερματισμού |

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| obj | const T\& | Το Exception Wrapper που περιέχει το αντικείμενο για το οποίο δημιουργείται κωδικός κατακερματισμού |

### Τιμή επιστροφής

Ο κωδικός κατακερματισμού που δημιουργείται για το συγκεκριμένο αντικείμενο

## System::GetHashCode(const T\&) συνάρτηση


Επιστρέφει έναν κωδικό κατακερματισμού για το συγκεκριμένο αντικείμενο που δεν είναι έξυπνος δείκτης ούτε εξαίρεση.

```cpp
template<typename T> std::enable_if<!std::is_scalar<T>::value &&!System::IsSmartPtr<T>::value &&!System::IsExceptionWrapper<T>::value, int>::type System::GetHashCode(const T &obj)
```


### Παράμετροι προτύπου

| Παράμετρος | Περιγραφή |
| --- | --- |
| T | Ο τύπος του αντικειμένου για το οποίο η συνάρτηση δημιουργεί κωδικό κατακερματισμού |

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| obj | const T\& | Μια σταθερή αναφορά στο αντικείμενο για το οποίο δημιουργείται κωδικός κατακερματισμού |

### Τιμή επιστροφής

Ο κωδικός κατακερματισμού που δημιουργείται για το συγκεκριμένο αντικείμενο

## System::GetHashCode(const std::thread::id\&) συνάρτηση


Εξειδίκευση για std::thread::id· Επιστρέφει τον κωδικό κατακερματισμού για το συγκεκριμένο αντικείμενο νήματος.

```cpp
int System::GetHashCode(const std::thread::id &id)
```

## Δείτε επίσης

* Δομή [IsSmartPtr](../issmartptr/)
* Δομή [IsExceptionWrapper](../isexceptionwrapper/)
* Χώρος ονομάτων [System](../)
* Βιβλιοθήκη [Aspose.Slides](../../)