---
title: FromAscii()
second_title: Αναφορά API του Aspose.Slides για C++
description: Δημιουργεί String από συμβολοσειρά ASCII.
type: docs
weight: 950
url: /el/system/string/fromascii/
---
## String::FromAscii(const char *) μέθοδος

Δημιουργεί [String](../) από συμβολοσειρά ASCII.

```cpp
static String System::String::FromAscii(const char *asciiStr)
```

### Arguments

| Parameter | Type | Description |
| --- | --- | --- |
| asciiStr | const char * | Δείκτης σε συμβολοσειρά που τελειώνει με μηδενικό χαρακτήρα και κωδικοποιείται με κωδικοσελίδα ASCII. |

### Return Value

[String](../) αντικείμενο που αντιπροσωπεύει τη δοσμένη συμβολοσειρά.

## String::FromAscii(const char *, int) μέθοδος

Δημιουργεί [String](../) από συμβολοσειρά ASCII.

```cpp
static String System::String::FromAscii(const char *asciiStr, int len)
```

### Arguments

| Parameter | Type | Description |
| --- | --- | --- |
| asciiStr | const char * | Δείκτης σε συμβολοσειρά που κωδικοποιείται με κωδικοσελίδα ASCII. |
| len | int | Αριθμός χαρακτήρων προς επεξεργασία. |

### Return Value

[String](../) αντικείμενο που αντιπροσωπεύει τη δοσμένη συμβολοσειρά.

## String::FromAscii(const std::string\&) μέθοδος

Δημιουργεί [String](../) από συμβολοσειρά ASCII.

```cpp
static String System::String::FromAscii(const std::string &asciiStr)
```

### Arguments

| Parameter | Type | Description |
| --- | --- | --- |
| asciiStr | const std::string\& | Συμβολοσειρά κωδικοποιημένη σε ASCII. |

### Return Value

[String](../) αντικείμενο που αντιπροσωπεύει τη δοσμένη συμβολοσειρά.

## Δείτε επίσης

* Κλάση [String](../)
* Χώρος ονομάτων [System](../../)
* Βιβλιοθήκη [Aspose.Slides](../../../)