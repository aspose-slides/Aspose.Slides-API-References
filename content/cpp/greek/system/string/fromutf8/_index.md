---
title: FromUtf8()
second_title: Aspose.Slides για C++ Αναφορά API
description: Δημιουργεί String από συμβολοσειρά utf8.
type: docs
weight: 898
url: /el/system/string/fromutf8/
---
## String::FromUtf8(const char *) μέθοδος

Δημιουργεί [String](../) από συμβολοσειρά utf8.

```cpp
static String System::String::FromUtf8(const char *utf8)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| utf8 | const char * | Δείκτης σε συμβολοσειρά null-terminated κωδικοποιημένη με κωδική σελίδα utf8. |

### Τιμή Επιστροφής

[String](../) αντικείμενο που αντιπροσωπεύει τη δοθείσα συμβολοσειρά.

## String::FromUtf8(const char *, int) μέθοδος

Δημιουργεί [String](../) από συμβολοσειρά utf8.

```cpp
static String System::String::FromUtf8(const char *utf8, int len)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| utf8 | const char * | Δείκτης σε συμβολοσειρά κωδικοποιημένη με κωδική σελίδα utf8. |
| len | int | Αριθμός χαρακτήρων προς επεξεργασία. |

### Τιμή Επιστροφής

[String](../) αντικείμενο που αντιπροσωπεύει τη δοθείσα συμβολοσειρά.

## String::FromUtf8(const uint8_t *) μέθοδος

Δημιουργεί [String](../) από συμβολοσειρά utf8.

```cpp
static String System::String::FromUtf8(const uint8_t *utf8)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| utf8 | const **uint8_t** * | Δείκτης σε συμβολοσειρά null-terminated κωδικοποιημένη με κωδική σελίδα utf8. |

### Τιμή Επιστροφής

[String](../) αντικείμενο που αντιπροσωπεύει τη δοθείσα συμβολοσειρά.

## String::FromUtf8(const std::string\&) μέθοδος

Δημιουργεί [String](../) από συμβολοσειρά utf8.

```cpp
static String System::String::FromUtf8(const std::string &utf8)
```

### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| utf8 | const std::string\& | Δείκτης σε συμβολοσειρά κωδικοποιημένη με κωδική σελίδα utf8. |

### Τιμή Επιστροφής

[String](../) αντικείμενο που αντιπροσωπεύει τη δοθείσα συμβολοσειρά.

## Δείτε επίσης

* Κλάση [String](../)
* Χώρος ονομάτων [System](../../)
* Βιβλιοθήκη [Aspose.Slides](../../../)