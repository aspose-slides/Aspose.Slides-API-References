---
title: Decimal()
second_title: Aspose.Slides για C++ API Αναφορά
description: Δημιουργεί ένα αντικείμενο που αντιπροσωπεύει το 0.
type: docs
weight: 1
url: /el/system/decimal/decimal/
---
## Decimal::Decimal() Κατασκευαστής

Δημιουργεί ένα αντικείμενο που αντιπροσωπεύει το 0.

```cpp
System::Decimal::Decimal()
```

## Decimal::Decimal(std::int8_t) Κατασκευαστής

Δημιουργεί ένα αντικείμενο που αντιπροσωπεύει την καθορισμένη τιμή.

```cpp
System::Decimal::Decimal(std::int8_t i)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| i | std::int8_t | Τιμή ακέραιου 8-bit που θα αντιπροσωπευθεί από το αντικείμενο [Decimal](../) που δημιουργείται |

## Decimal::Decimal(std::int16_t) Κατασκευαστής

Δημιουργεί ένα αντικείμενο που αντιπροσωπεύει την καθορισμένη τιμή.

```cpp
System::Decimal::Decimal(std::int16_t i)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| i | std::int16_t | Τιμή ακέραιου 16-bit που θα αντιπροσωπευθεί από το αντικείμενο [Decimal](../) που δημιουργείται |

## Decimal::Decimal(std::int32_t) Κατασκευαστής

Δημιουργεί ένα αντικείμενο που αντιπροσωπεύει την καθορισμένη τιμή.

```cpp
System::Decimal::Decimal(std::int32_t i)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| i | std::int32_t | Τιμή ακέραιου 32-bit που θα αντιπροσωπευθεί από το αντικείμενο [Decimal](../) που δημιουργείται |

## Decimal::Decimal(std::int64_t) Κατασκευαστής

Δημιουργεί ένα αντικείμενο που αντιπροσωπεύει την καθορισμένη τιμή.

```cpp
System::Decimal::Decimal(std::int64_t i)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| i | std::int64_t | Τιμή ακέραιου 64-bit που θα αντιπροσωπευθεί από το αντικείμενο [Decimal](../) που δημιουργείται |

## Decimal::Decimal(std::uint8_t) Κατασκευαστής

Δημιουργεί ένα αντικείμενο που αντιπροσωπεύει την καθορισμένη τιμή.

```cpp
System::Decimal::Decimal(std::uint8_t i)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| i | std::uint8_t | Τιμή αμερόληπτου 8-bit που θα αντιπροσωπευθεί από το αντικείμενο [Decimal](../) που δημιουργείται |

## Decimal::Decimal(std::uint16_t) Κατασκευαστής

Δημιουργεί ένα αντικείμενο που αντιπροσωπεύει την καθορισμένη τιμή.

```cpp
System::Decimal::Decimal(std::uint16_t i)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| i | std::uint16_t | Τιμή αμερόληπτου 16-bit που θα αντιπροσωπευθεί από το αντικείμενο [Decimal](../) που δημιουργείται |

## Decimal::Decimal(std::uint32_t) Κατασκευαστής

Δημιουργεί ένα αντικείμενο που αντιπροσωπεύει την καθορισμένη τιμή.

```cpp
System::Decimal::Decimal(std::uint32_t i)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| i | std::uint32_t | Τιμή αμερόληπτου 32-bit που θα αντιπροσωπευθεί από το αντικείμενο [Decimal](../) που δημιουργείται |

## Decimal::Decimal(std::uint64_t) Κατασκευαστής

Δημιουργεί ένα αντικείμενο που αντιπροσωπεύει την καθορισμένη τιμή.

```cpp
System::Decimal::Decimal(std::uint64_t i)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| i | std::uint64_t | Τιμή αμερόληπτου 64-bit που θα αντιπροσωπευθεί από το αντικείμενο [Decimal](../) που δημιουργείται |

## Decimal::Decimal(float) Κατασκευαστής

Δημιουργεί ένα αντικείμενο που αντιπροσωπεύει την καθορισμένη τιμή.

```cpp
System::Decimal::Decimal(float f)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| f | **float** | Η τιμή τύπου float μονής ακρίβειας που θα αντιπροσωπευθεί από το αντικείμενο [Decimal](../) που δημιουργείται |

## Decimal::Decimal(double) Κατασκευαστής

Δημιουργεί ένα αντικείμενο που αντιπροσωπεύει την καθορισμένη τιμή.

```cpp
System::Decimal::Decimal(double d)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| d | **double** | Η τιμή τύπου double διπλής ακρίβειας που θα αντιπροσωπευθεί από το αντικείμενο [Decimal](../) που δημιουργείται |

## Decimal::Decimal(const std::string\&) Κατασκευαστής

Δημιουργεί ένα αντικείμενο που αντιπροσωπεύει μια τιμή της οποίας η συμβολοσειρά αναπαράστασης έχει οριστεί ως παράδειγμα της κλάσης std::string.

```cpp
System::Decimal::Decimal(const std::string &str)
```

## Decimal::Decimal(int32_t, int32_t, int32_t, bool, uint8_t) Κατασκευαστής

Δημιουργεί ένα αντικείμενο [Decimal](../) από τα καθορισμένα στοιχεία.

```cpp
System::Decimal::Decimal(int32_t lo, int32_t mid, int32_t hi, bool isNegative, uint8_t scale)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| lo | **int32_t** | Τα χαμηλά 32 bits της τιμής |
| mid | **int32_t** | Τα μεσαία 32 bits της τιμής |
| hi | **int32_t** | Τα υψηλά 32 bits της τιμής |
| isNegative | **bool** | Καθορίζει αν η τιμή είναι αρνητική |
| scale | **uint8_t** | Μια δύναμη του 10 στο εύρος 0 έως 28 |

## Decimal::Decimal(const Decimal\&) Κατασκευαστής

Δημιουργεί ένα αντικείμενο της κλάσης [Decimal](../) που αντιπροσωπεύει τον ίδιο αριθμό με το καθορισμένο αντικείμενο [Decimal](../).

```cpp
System::Decimal::Decimal(const Decimal &d)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| d | const [Decimal](../)\& | Ένα αντικείμενο [Decimal](../) από το οποίο θα αντιγραφεί η τιμή |

## Decimal::Decimal(const ArrayPtr\<int32_t\>\&) Κατασκευαστής

Δημιουργεί ένα αντικείμενο της κλάσης [Decimal](../) από έναν ακέραιο πίνακα που περιέχει δυαδική αναπαράσταση.

```cpp
System::Decimal::Decimal(const ArrayPtr<int32_t> &bits)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| bits | const [ArrayPtr](../../arrayptr/)\<**int32_t**\>\& | Ένας ακέραιος πίνακας που περιέχει δυαδική αναπαράσταση. |

## Decimal::Decimal(std::nullptr_t) Κατασκευαστής

Πάντα ρίχνει ArgumentNullException.

```cpp
System::Decimal::Decimal(std::nullptr_t bits)
```

## Decimal::Decimal(const number_type\&) Κατασκευαστής

Δημιουργεί ένα αντικείμενο της κλάσης [Decimal](../) που αντιπροσωπεύει την καθορισμένη τιμή.

```cpp
System::Decimal::Decimal(const number_type &value)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | const [number_type](../number_type/)\& | Μια σταθερή αναφορά στην τιμή που θα αντιπροσωπευθεί από το αντικείμενο που δημιουργείται |

## Δείτε επίσης

* Typedef [ArrayPtr](../../arrayptr/)
* Typedef [number_type](../number_type/)
* Class [Decimal](../)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)