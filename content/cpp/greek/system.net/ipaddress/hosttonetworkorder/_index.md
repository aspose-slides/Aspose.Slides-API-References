---
title: HostToNetworkOrder()
second_title: Aspose.Slides για C++ Αναφορά API
description: Μετατρέπει τη συγκεκριμένη σειρά byte του κεντρικού υπολογιστή στην αντίστοιχη σειρά byte του δικτύου.
type: docs
weight: 248
url: /el/system.net/ipaddress/hosttonetworkorder/
---
## IPAddress::HostToNetworkOrder(int64_t) μέθοδος

Μετατρέπει τη συγκεκριμένη σειρά byte του υπολογιστή σε αντίστοιχη σειρά byte του δικτύου.

```cpp
static int64_t System::Net::IPAddress::HostToNetworkOrder(int64_t host)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| host | **int64_t** | Η τιμή προς μετατροπή. |

### Τιμή επιστροφής

Η αντίστοιχη σειρά byte του δικτύου.

## IPAddress::HostToNetworkOrder(int32_t) μέθοδος

Μετατρέπει τη συγκεκριμένη σειρά byte του υπολογιστή σε αντίστοιχη σειρά byte του δικτύου.

```cpp
static int32_t System::Net::IPAddress::HostToNetworkOrder(int32_t host)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| host | **int32_t** | Η τιμή προς μετατροπή. |

### Τιμή επιστροφής

Η αντίστοιχη σειρά byte του δικτύου.

## IPAddress::HostToNetworkOrder(int16_t) μέθοδος

Μετατρέπει τη συγκεκριμένη σειρά byte του υπολογιστή σε αντίστοιχη σειρά byte του δικτύου.

```cpp
static int16_t System::Net::IPAddress::HostToNetworkOrder(int16_t host)
```

### Ορίσματα

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| host | **int16_t** | Η τιμή προς μετατροπή. |

### Τιμή επιστροφής

Η αντίστοιχη σειρά byte του δικτύου.

## Δείτε επίσης

* Κλάση [IPAddress](../)
* Χώρος ονομάτων [System::Net](../../)
* Βιβλιοθήκη [Aspose.Slides](../../../)