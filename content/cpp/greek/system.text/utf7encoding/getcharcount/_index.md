---
title: GetCharCount()
second_title: Aspose.Slides για C++ API Αναφορά
description: Λαμβάνει τον αριθμό των χαρακτήρων που απαιτούνται για την αποκωδικοποίηση ενός byte buffer.
type: docs
weight: 79
url: /el/system.text/utf7encoding/getcharcount/
---
## UTF7Encoding::GetCharCount(ArrayPtr\<uint8_t\>, int, int) μέθοδος


Λαμβάνει τον αριθμό των χαρακτήρων που απαιτούνται για την αποκωδικοποίηση ενός byte buffer.

```cpp
int System::Text::UTF7Encoding::GetCharCount(ArrayPtr<uint8_t> bytes, int index, int count) override
```


### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | Bytes προς αποκωδικοποίηση. |
| index | int | Αρχή τμήματος. |
| count | int | Μέγεθος τμήματος. |

### Τιμή επιστροφής

Αριθμός χαρακτήρων.

## UTF7Encoding::GetCharCount(const uint8_t *, int) μέθοδος


Λαμβάνει τον αριθμό των χαρακτήρων που απαιτούνται για την αποκωδικοποίηση ενός byte buffer.

```cpp
int System::Text::UTF7Encoding::GetCharCount(const uint8_t *bytes, int count) override
```


### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| bytes | const **uint8_t** * | Bytes προς αποκωδικοποίηση. |
| count | int | Πλήθος bytes. |

### Τιμή επιστροφής

Αριθμός χαρακτήρων.

## UTF7Encoding::GetCharCount(ArrayPtr\<uint8_t\>, int, int) μέθοδος


Λαμβάνει τον αριθμό των χαρακτήρων που απαιτούνται για την αποκωδικοποίηση ενός byte buffer.

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes, int index, int count)
```


### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | Bytes προς αποκωδικοποίηση. |
| index | int | Αρχή τμήματος. |
| count | int | Μέγεθος τμήματος. |

### Τιμή επιστροφής

Αριθμός χαρακτήρων.

## UTF7Encoding::GetCharCount(ArrayPtr\<uint8_t\>) μέθοδος


Λαμβάνει τον αριθμό των χαρακτήρων που απαιτούνται για την αποκωδικοποίηση ενός byte buffer.

```cpp
virtual int System::Text::Encoding::GetCharCount(ArrayPtr<uint8_t> bytes)
```


### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| bytes | [ArrayPtr](../../../system/arrayptr/)\<**uint8_t**\> | Bytes προς αποκωδικοποίηση. |

### Τιμή επιστροφής

Αριθμός χαρακτήρων.

## UTF7Encoding::GetCharCount(const uint8_t *, int) μέθοδος


Λαμβάνει τον αριθμό των χαρακτήρων που απαιτούνται για την αποκωδικοποίηση ενός byte buffer.

```cpp
virtual int System::Text::Encoding::GetCharCount(const uint8_t *bytes, int count)
```


### Παράμετροι

| Parameter | Type | Description |
| --- | --- | --- |
| bytes | const **uint8_t** * | Bytes προς αποκωδικοποίηση. |
| count | int | Πλήθος bytes. |

### Τιμή επιστροφής

Αριθμός χαρακτήρων.

## Δείτε επίσης

* Typedef [ArrayPtr](../../../system/arrayptr/)
* Κλάση [UTF7Encoding](../)
* Χώρος ονομάτων [System::Text](../../)
* Βιβλιοθήκη [Aspose.Slides](../../../)