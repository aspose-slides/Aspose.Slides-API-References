---
title: GetByteCount()
second_title: Aspose.Slides για C++ API Αναφορά
description: Παρέχει τον αριθμό των χαρακτήρων που απαιτούνται για την κωδικοποίηση ενός buffer χαρακτήρων.
type: docs
weight: 157
url: /el/system.text/utf7encoding/getbytecount/
---
## UTF7Encoding::GetByteCount(const char_t *, int) μέθοδος

Παρέχει τον αριθμό των χαρακτήρων που απαιτούνται για την κωδικοποίηση ενός buffer χαρακτήρων.

```cpp
int System::Text::UTF7Encoding::GetByteCount(const char_t *chars, int count) override
```

### Arguments

| Parameter | Type | Description |
| --- | --- | --- |
| chars | const char_t * | Buffer χαρακτήρων. |
| count | int | [Buffer](../../../system/buffer/) μέγεθος. |

### Return Value

Απαιτούμενο μέγεθος buffer.

## UTF7Encoding::GetByteCount(ArrayPtr\<char_t\>, int, int) μέθοδος

Παρέχει τον αριθμό των χαρακτήρων που απαιτούνται για την κωδικοποίηση ενός buffer χαρακτήρων.

```cpp
virtual int System::Text::Encoding::GetByteCount(ArrayPtr<char_t> chars, int index, int count)
```

### Arguments

| Parameter | Type | Description |
| --- | --- | --- |
| chars | [ArrayPtr](../../../system/arrayptr/)\<char_t\> | Buffer χαρακτήρων. |
| index | int | Αρχή τμήματος. |
| count | int | Μέγεθος τμήματος. |

### Return Value

Απαιτούμενο μέγεθος buffer.

## UTF7Encoding::GetByteCount(System::Details::ArrayView\<char_t\>, int, int) μέθοδος

Παρέχει τον αριθμό των χαρακτήρων που απαιτούνται για την κωδικοποίηση ενός buffer χαρακτήρων.

```cpp
virtual int System::Text::Encoding::GetByteCount(System::Details::ArrayView<char_t> chars, int index, int count)
```

### Arguments

| Parameter | Type | Description |
| --- | --- | --- |
| chars | System::Details::ArrayView\<char_t\> | Buffer χαρακτήρων. |
| index | int | Αρχή τμήματος. |
| count | int | Μέγεθος τμήματος. |

### Return Value

Απαιτούμενο μέγεθος buffer.

## UTF7Encoding::GetByteCount(const System::Details::StackArray\<char_t, N\>\&, int, int) μέθοδος

Παρέχει τον αριθμό των χαρακτήρων που απαιτούνται για την κωδικοποίηση ενός buffer χαρακτήρων.

```cpp
template<std::size_t> int System::Text::Encoding::GetByteCount(const System::Details::StackArray<char_t, N> &chars, int index, int count)
```

### Arguments

| Parameter | Type | Description |
| --- | --- | --- |
| chars | const System::Details::StackArray\<char_t, N\>\& | Buffer χαρακτήρων. |
| index | int | Αρχή τμήματος. |
| count | int | Μέγεθος τμήματος. |

### Return Value

Απαιτούμενο μέγεθος buffer.

## UTF7Encoding::GetByteCount(const String\&) μέθοδος

Παρέχει τον αριθμό των χαρακτήρων που απαιτούνται για την κωδικοποίηση μιας συμβολοσειράς.

```cpp
virtual int System::Text::Encoding::GetByteCount(const String &s)
```

### Arguments

| Parameter | Type | Description |
| --- | --- | --- |
| s | const [String](../../../system/string/)\& | [String](../../../system/string/) για κωδικοποίηση. |

### Return Value

Απαιτούμενο μέγεθος buffer.

## UTF7Encoding::GetByteCount(ArrayPtr\<char_t\>) μέθοδος

Παρέχει τον αριθμό των χαρακτήρων που απαιτούνται για την κωδικοποίηση ενός buffer χαρακτήρων.

```cpp
virtual int System::Text::Encoding::GetByteCount(ArrayPtr<char_t> chars)
```

### Arguments

| Parameter | Type | Description |
| --- | --- | --- |
| chars | [ArrayPtr](../../../system/arrayptr/)\<char_t\> | Buffer χαρακτήρων. |

### Return Value

Απαιτούμενο μέγεθος buffer.

## UTF7Encoding::GetByteCount(const char_t *, int) μέθοδος

Παρέχει τον αριθμό των χαρακτήρων που απαιτούνται για την κωδικοποίηση ενός buffer χαρακτήρων.

```cpp
virtual int System::Text::Encoding::GetByteCount(const char_t *chars, int count)
```

### Arguments

| Parameter | Type | Description |
| --- | --- | --- |
| chars | const char_t * | Buffer χαρακτήρων. |
| count | int | [Buffer](../../../system/buffer/) μέγεθος. |

### Return Value

Απαιτούμενο μέγεθος buffer.

## Δείτε επίσης

* Typedef [ArrayPtr](../../../system/arrayptr/)
* Κλάση [UTF7Encoding](../)
* Κλάση [String](../../../system/string/)
* Περιοχή ονομάτων [System::Text](../../)
* Βιβλιοθήκη [Aspose.Slides](../../../)