---
title: end()
second_title: Αναφορά API Aspose.Slides για C++
description: Αποκτά έναν επαναλήπτη που δείχνει ακριβώς μετά το τελευταίο στοιχείο (εάν υπάρχει) της συλλογής.
type: docs
weight: 92
url: /el/aspose.slides.charts/chartdataworksheetcollection/end/
---
## ChartDataWorksheetCollection::end() μέθοδος

Αποκτά επαναλήπτη που δείχνει ακριβώς μετά το τελευταίο στοιχείο (αν υπάρχει) της συλλογής.

```cpp
iterator Aspose::Slides::Charts::ChartDataWorksheetCollection::end() noexcept
```

### Τιμή Επιστροφής

Ένας επαναλήπτης που δείχνει ακριβώς μετά το τελευταίο στοιχείο (αν υπάρχει) της συλλογής

## ChartDataWorksheetCollection::end() const μέθοδος

Αποκτά επαναλήπτη που δείχνει ακριβώς μετά το τελευταίο στοιχείο (αν υπάρχει) της const-qualified παρουσίας της συλλογής.

```cpp
const_iterator Aspose::Slides::Charts::ChartDataWorksheetCollection::end() const noexcept
```

### Τιμή Επιστροφής

Ένας επαναλήπτης που δείχνει ακριβώς μετά το τελευταίο στοιχείο (αν υπάρχει) της const-qualified παρουσίας της συλλογής

## Δείτε επίσης

* Typedef [iterator](../iterator/)
* Typedef [const_iterator](../const_iterator/)
* Τάξη [ChartDataWorksheetCollection](../)
* Χώρος ονομάτων [Aspose::Slides::Charts](../../)
* Library [Aspose.Slides](../../../)