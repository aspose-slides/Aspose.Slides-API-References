---
title: Export()
second_title: Aspose.Slides για C++ API Αναφορά
description: Εξάγει το τρέχον αντικείμενο σε έναν πίνακα byte χρησιμοποιώντας το καθορισμένο μορφότυπο. ΔΕΝ ΥΠΑΡΧΕΙ ΥΛΟΠΟΙΗΣΗ.
type: docs
weight: 287
url: /el/system.security.cryptography.x509certificates/x509certificate/export/
---
## X509Certificate::Export(X509ContentType) const μέθοδος

Εξάγει το τρέχον αντικείμενο σε έναν πίνακα byte χρησιμοποιώντας το καθορισμένο μορφότυπο. ΔΕΝ ΥΠΑΡΧΕΙ ΥΛΟΠΟΙΗΣΗ.

```cpp
virtual ByteArrayPtr System::Security::Cryptography::X509Certificates::X509Certificate::Export(X509ContentType content_type) const
```


### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| content_type | [X509ContentType](../../x509contenttype/) | Καθορίζει πώς να μορφοποιούνται τα δεδομένα εξόδου. |

### Τιμή Επιστροφής

Ένας πίνακας byte που αντιπροσωπεύει το τρέχον αντικείμενο.

## X509Certificate::Export(X509ContentType, const SecureStringPtr\&) const μέθοδος

Εξάγει το τρέχον αντικείμενο σε έναν πίνακα byte χρησιμοποιώντας το καθορισμένο μορφότυπο. ΔΕΝ ΥΠΑΡΧΕΙ ΥΛΟΠΟΙΗΣΗ.

```cpp
virtual ByteArrayPtr System::Security::Cryptography::X509Certificates::X509Certificate::Export(X509ContentType content_type, const SecureStringPtr &password) const
```


### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| content_type | [X509ContentType](../../x509contenttype/) | Καθορίζει πώς να μορφοποιούνται τα δεδομένα εξόδου. |
| password | const [SecureStringPtr](../../../system.security/securestringptr/)\& | Ο κωδικός πρόσβασης που απαιτείται για την πρόσβαση στα δεδομένα του πιστοποιητικού. |

### Τιμή Επιστροφής

Ένας πίνακας byte που αντιπροσωπεύει το τρέχον αντικείμενο.

## X509Certificate::Export(X509ContentType, const String\&) const μέθοδος

Εξάγει το τρέχον αντικείμενο σε έναν πίνακα byte χρησιμοποιώντας το καθορισμένο μορφότυπο. ΔΕΝ ΥΠΑΡΧΕΙ ΥΛΟΠΟΙΗΣΗ.

```cpp
virtual ByteArrayPtr System::Security::Cryptography::X509Certificates::X509Certificate::Export(X509ContentType content_type, const String &password) const
```


### Παράμετροι

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| content_type | [X509ContentType](../../x509contenttype/) | Καθορίζει πώς να μορφοποιούνται τα δεδομένα εξόδου. |
| password | const [String](../../../system/string/)\& | Ο κωδικός πρόσβασης που απαιτείται για την πρόσβαση στα δεδομένα του πιστοποιητικού. |

### Τιμή Επιστροφής

Ένας πίνακας byte που αντιπροσωπεύει το τρέχον αντικείμενο.

## Δείτε επίσης

* Απαρίθμηση [X509ContentType](../../x509contenttype/)
* Δήλωση τύπου [ByteArrayPtr](../../../system/bytearrayptr/)
* Δήλωση τύπου [SecureStringPtr](../../../system.security/securestringptr/)
* Κλάση [X509Certificate](../)
* Κλάση [String](../../../system/string/)
* Χώρος ονομάτων [System::Security::Cryptography::X509Certificates](../../)
* Βιβλιοθήκη [Aspose.Slides](../../../)