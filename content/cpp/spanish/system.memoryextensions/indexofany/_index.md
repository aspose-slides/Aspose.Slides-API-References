---
title: IndexOfAny()
second_title: Referencia de la API de Aspose.Slides para C++
description: Encuentra el índice de la primera aparición de cualquiera de los dos valores especificados en un ReadOnlySpan<T>
type: docs
weight: 157
url: /es/system.memoryextensions/indexofany/
---
## System::MemoryExtensions::IndexOfAny(const ReadOnlySpan\<T\>\&, const T\&, const T\&) función


Encuentra el índice de la primera aparición de cualquiera de los dos valores especificados en un ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const ReadOnlySpan<T> &span, const T &value0, const T &value1)
```


### Parámetros de plantilla

| Parámetro | Descripción |
| --- | --- |
| T | El tipo de los elementos en el span |

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | El span en el que buscar |
| value0 | const T\& | El primer valor que se busca |
| value1 | const T\& | El segundo valor que se busca |

### Valor devuelto

El índice basado en cero de la primera aparición, o -1 si no se encuentra

## System::MemoryExtensions::IndexOfAny(const ReadOnlySpan\<T\>\&, const T\&, const T\&, const T\&) función


Encuentra el índice de la primera aparición de cualquiera de los tres valores especificados en un ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const ReadOnlySpan<T> &span, const T &value0, const T &value1, const T &value2)
```


### Parámetros de plantilla

| Parámetro | Descripción |
| --- | --- |
| T | El tipo de los elementos en el span |

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | El span en el que buscar |
| value0 | const T\& | El primer valor que se busca |
| value1 | const T\& | El segundo valor que se busca |
| value2 | const T\& | El tercer valor que se busca |

### Valor devuelto

El índice basado en cero de la primera aparición, o -1 si no se encuentra

## System::MemoryExtensions::IndexOfAny(const Span\<T\>\&, const T\&, const T\&) función


Encuentra el índice de la primera aparición de cualquiera de los dos valores especificados en un Span<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const Span<T> &span, const T &value0, const T &value1)
```


### Parámetros de plantilla

| Parámetro | Descripción |
| --- | --- |
| T | El tipo de los elementos en el span |

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | El span en el que buscar |
| value0 | const T\& | El primer valor que se busca |
| value1 | const T\& | El segundo valor que se busca |

### Valor devuelto

El índice basado en cero de la primera aparición, o -1 si no se encuentra

## System::MemoryExtensions::IndexOfAny(const Span\<T\>\&, const T\&, const T\&, const T\&) función


Encuentra el índice de la primera aparición de cualquiera de los tres valores especificados en un Span<T)

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const Span<T> &span, const T &value0, const T &value1, const T &value2)
```


### Parámetros de plantilla

| Parámetro | Descripción |
| --- | --- |
| T | El tipo de los elementos en el span |

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | El span en el que buscar |
| value0 | const T\& | El primer valor que se busca |
| value1 | const T\& | El segundo valor que se busca |
| value2 | const T\& | El tercer valor que se busca |

### Valor devuelto

El índice basado en cero de la primera aparición, o -1 si no se encuentra

## System::MemoryExtensions::IndexOfAny(const ReadOnlySpan\<T\>\&, const ReadOnlySpan\<T\>\&) función


Encuentra el índice de la primera aparición de cualquier valor de un span en otro ReadOnlySpan<T>

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const ReadOnlySpan<T> &span, const ReadOnlySpan<T> &values)
```


### Parámetros de plantilla

| Parámetro | Descripción |
| --- | --- |
| T | El tipo de los elementos en los spans |

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | El span en el que buscar |
| values | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | El span que contiene los valores a buscar |

### Valor devuelto

El índice basado en cero de la primera aparición, o -1 si no se encuentra

## System::MemoryExtensions::IndexOfAny(const Span\<T\>\&, const ReadOnlySpan\<T\>\&) función


Encuentra el índice de la primera aparición de cualquier valor de un span en un Span<T)

```cpp
template<typename T> int32_t System::MemoryExtensions::IndexOfAny(const Span<T> &span, const ReadOnlySpan<T> &values)
```


### Parámetros de plantilla

| Parámetro | Descripción |
| --- | --- |
| T | El tipo de los elementos en los spans |

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | El span en el que buscar |
| values | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | El span que contiene los valores a buscar |

### Valor devuelto

El índice basado en cero de la primera aparición, o -1 si no se encuentra

## Ver también

* Clase [ReadOnlySpan](../../system/readonlyspan/)
* Clase [Span](../../system/span/)
* Espacio de nombres [System::MemoryExtensions](../)
* Biblioteca [Aspose.Slides](../../)