---
title: LastIndexOfAnyExcept()
second_title: Referencia de API de Aspose.Slides para C++
description: Encuentra la última aparición de cualquier elemento, excepto tres valores especificados, dentro de una secuencia.
type: docs
weight: 235
url: /es/system.memoryextensions/lastindexofanyexcept/
---
## System::MemoryExtensions::LastIndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const T\&, const T\&, const T\&) función


Encuentra la última aparición de cualquier elemento, excepto tres valores especificados, dentro de una secuencia.

```cpp
template<typename T> int32_t System::MemoryExtensions::LastIndexOfAnyExcept(const ReadOnlySpan<T> &span, const T &value0, const T &value1, const T &value2)
```


### Parámetros de plantilla

| Parameter | Description |
| --- | --- |
| T | The type of elements in the span |

### Argumentos

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | The span to search within |
| value0 | const T\& | The first value to exclude |
| value1 | const T\& | The second value to exclude |
| value2 | const T\& | The third value to exclude |

### Valor devuelto

The zero-based index of the last non-excluded element, or -1 if not found

## System::MemoryExtensions::LastIndexOfAnyExcept(const Span\<T\>\&, const T\&, const T\&, const T\&) función


Encuentra la última aparición de cualquier elemento, excepto tres valores especificados, dentro de una secuencia mutable.

```cpp
template<typename T> int32_t System::MemoryExtensions::LastIndexOfAnyExcept(const Span<T> &span, const T &value0, const T &value1, const T &value2)
```


### Parámetros de plantilla

| Parameter | Description |
| --- | --- |
| T | The type of elements in the span |

### Argumentos

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | The span to search within |
| value0 | const T\& | The first value to exclude |
| value1 | const T\& | The second value to exclude |
| value2 | const T\& | The third value to exclude |

### Valor devuelto

The zero-based index of the last non-excluded element, or -1 if not found

## System::MemoryExtensions::LastIndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const T\&, const T\&) función


Encuentra la última aparición de cualquier elemento, excepto dos valores especificados, dentro de una secuencia.

```cpp
template<typename T> int32_t System::MemoryExtensions::LastIndexOfAnyExcept(const ReadOnlySpan<T> &span, const T &value0, const T &value1)
```


### Parámetros de plantilla

| Parameter | Description |
| --- | --- |
| T | The type of elements in the span |

### Argumentos

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | The span to search within |
| value0 | const T\& | The first value to exclude |
| value1 | const T\& | The second value to exclude |

### Valor devuelto

The zero-based index of the last non-excluded element, or -1 if not found

## System::MemoryExtensions::LastIndexOfAnyExcept(const Span\<T\>\&, const T\&, const T\&) función


Encuentra la última aparición de cualquier elemento, excepto dos valores especificados, dentro de una secuencia mutable.

```cpp
template<typename T> int32_t System::MemoryExtensions::LastIndexOfAnyExcept(const Span<T> &span, const T &value0, const T &value1)
```


### Parámetros de plantilla

| Parameter | Description |
| --- | --- |
| T | The type of elements in the span |

### Argumentos

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | The span to search within |
| value0 | const T\& | The first value to exclude |
| value1 | const T\& | The second value to exclude |

### Valor devuelto

The zero-based index of the last non-excluded element, or -1 if not found

## System::MemoryExtensions::LastIndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const T\&) función


Encuentra la última aparición de cualquier elemento, excepto un valor especificado, dentro de una secuencia.

```cpp
template<typename T> int32_t System::MemoryExtensions::LastIndexOfAnyExcept(const ReadOnlySpan<T> &span, const T &value)
```


### Parámetros de plantilla

| Parameter | Description |
| --- | --- |
| T | The type of elements in the span |

### Argumentos

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | The span to search within |
| value | const T\& | The value to exclude |

### Valor devuelto

The zero-based index of the last non-excluded element, or -1 if not found

## System::MemoryExtensions::LastIndexOfAnyExcept(const Span\<T\>\&, const T\&) función


Encuentra la última aparición de cualquier elemento, excepto un valor especificado, dentro de una secuencia mutable.

```cpp
template<typename T> int32_t System::MemoryExtensions::LastIndexOfAnyExcept(const Span<T> &span, const T &value)
```


### Parámetros de plantilla

| Parameter | Description |
| --- | --- |
| T | The type of elements in the span |

### Argumentos

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | The span to search within |
| value | const T\& | The value to exclude |

### Valor devuelto

The zero-based index of the last non-excluded element, or -1 if not found

## System::MemoryExtensions::LastIndexOfAnyExcept(const ReadOnlySpan\<T\>\&, const ReadOnlySpan\<T\>\&) función


Encuentra la última aparición de cualquier elemento, excepto valores de una secuencia, dentro de una secuencia.

```cpp
template<typename T> int32_t System::MemoryExtensions::LastIndexOfAnyExcept(const ReadOnlySpan<T> &span, const ReadOnlySpan<T> &values)
```


### Parámetros de plantilla

| Parameter | Description |
| --- | --- |
| T | The type of elements in the span |

### Argumentos

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | The span to search within |
| values | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | The sequence of values to exclude |

### Valor devuelto

The zero-based index of the last non-excluded element, or -1 if not found

## System::MemoryExtensions::LastIndexOfAnyExcept(const Span\<T\>\&, const ReadOnlySpan\<T\>\&) función


Encuentra la última aparición de cualquier elemento, excepto valores de una secuencia, dentro de una secuencia mutable.

```cpp
template<typename T> int32_t System::MemoryExtensions::LastIndexOfAnyExcept(const Span<T> &span, const ReadOnlySpan<T> &values)
```


### Parámetros de plantilla

| Parameter | Description |
| --- | --- |
| T | The type of elements in the span |

### Argumentos

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | The span to search within |
| values | const [ReadOnlySpan](../../system/readonlyspan/)\<T\>\& | The sequence of values to exclude |

### Valor devuelto

The zero-based index of the last non-excluded element, or -1 if not found

## System::MemoryExtensions::LastIndexOfAnyExcept(const Span\<T\>\&, const Span\<T\>\&) función


Encuentra la última aparición de cualquier elemento, excepto valores de una secuencia mutable, dentro de una secuencia mutable.

```cpp
template<typename T> int32_t System::MemoryExtensions::LastIndexOfAnyExcept(const Span<T> &span, const Span<T> &values)
```


### Parámetros de plantilla

| Parameter | Description |
| --- | --- |
| T | The type of elements in the span |

### Argumentos

| Parameter | Type | Description |
| --- | --- | --- |
| span | const [Span](../../system/span/)\<T\>\& | The span to search within |
| values | const [Span](../../system/span/)\<T\>\& | The sequence of values to exclude |

### Valor devuelto

The zero-based index of the last non-excluded element, or -1 if not found

## Ver también

* Clase [ReadOnlySpan](../../system/readonlyspan/)
* Clase [Span](../../system/span/)
* Espacio de nombres [System::MemoryExtensions](../)
* Biblioteca [Aspose.Slides](../../)