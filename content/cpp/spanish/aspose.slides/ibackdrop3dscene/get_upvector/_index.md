---
title: get_UpVector()
second_title: Referencia de API de Aspose.Slides para C++
description: Devuelve un vector que representa la dirección arriba. Para ser más precisos, este atributo define un vector que representa la dirección arriba en relación con la cara del plano del telón de fondo. El vector está representado por una matriz de 3 valores float que definen las coordenadas X, Y y Z. Leer float[].
type: docs
weight: 53
url: /es/aspose.slides/ibackdrop3dscene/get_upvector/
---
## IBackdrop3DScene::get_UpVector() método

Devuelve un vector que representa la dirección arriba. Para ser más precisos, este atributo define un vector que representa la dirección arriba en relación con la cara del plano del telón de fondo. El vector está representado por una matriz de 3 valores **float** que definen las coordenadas X, Y y Z. Leer **float**[].

```cpp
virtual System::ArrayPtr<float> Aspose::Slides::IBackdrop3DScene::get_UpVector()=0
```

## Ver también

* Typedef [ArrayPtr](../../../system/arrayptr/)
* Clase [IBackdrop3DScene](../)
* Espacio de nombres [Aspose::Slides](../../)
* Biblioteca [Aspose.Slides](../../../)