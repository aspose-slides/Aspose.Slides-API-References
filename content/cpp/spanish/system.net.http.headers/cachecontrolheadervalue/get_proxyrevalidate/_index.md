---
title: get_ProxyRevalidate()
second_title: Referencia de API de Aspose.Slides para C++
description: Obtiene el valor que determina si el servidor requiere la revalidación de una entrada de caché cuando se vuelve obsoleta para las cachés de agentes de usuario compartidas.
type: docs
weight: 339
url: /es/system.net.http.headers/cachecontrolheadervalue/get_proxyrevalidate/
---
## CacheControlHeaderValue::get_ProxyRevalidate() método

Obtiene el valor que determina si el servidor requiere la revalidación de una entrada de caché cuando se vuelve obsoleta para las cachés de agentes de usuario compartidas.

```cpp
bool System::Net::Http::Headers::CacheControlHeaderValue::get_ProxyRevalidate()
```

### Valor de retorno

El valor que determina si el servidor requiere la revalidación de una entrada de caché cuando se vuelve obsoleta para las cachés de agentes de usuario compartidas.

## Ver también

* Clase [CacheControlHeaderValue](../)
* Espacio de nombres [System::Net::Http::Headers](../../)
* Biblioteca [Aspose.Slides](../../../)