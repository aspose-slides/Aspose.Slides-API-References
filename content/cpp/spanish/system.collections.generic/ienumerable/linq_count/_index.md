---
title: LINQ_Count()
second_title: Referencia de la API de Aspose.Slides para C++
description: Devuelve el número de elementos en la secuencia (calculado mediante conteo directo).
type: docs
weight: 118
url: /es/system.collections.generic/ienumerable/linq_count/
---
## IEnumerable::LINQ_Count() método

Devuelve el número de elementos en la secuencia (calculado mediante conteo directo).

```cpp
int System::Collections::Generic::IEnumerable<T>::LINQ_Count()
```

### Valor devuelto

Número de elementos en la secuencia.

## IEnumerable::LINQ_Count(const Func\<T, bool\>\&) método

Devuelve el número de elementos en la secuencia que cumplen la condición especificada.

```cpp
int System::Collections::Generic::IEnumerable<T>::LINQ_Count(const Func<T, bool> &predicate)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| predicate | const [Func](../../../system/func/)\<T, **bool**\>\& | Una función para probar cada elemento con una condición. |

### Valor devuelto

Número de elementos en la secuencia que cumplen la condición especificada.

## Ver también

* Clase [IEnumerable](../)
* Clase [Func](../../../system/func/)
* Espacio de nombres [System::Collections::Generic](../../)
* Biblioteca [Aspose.Slides](../../../)