---
title: WriteLine()
second_title: Referencia de la API de Aspose.Slides para C++
description: Escribe los caracteres de terminación de línea en el flujo.
type: docs
weight: 118
url: /es/system.io/textwriter/writeline/
---
## TextWriter::WriteLine() método

Escribe los caracteres de terminación de línea en el flujo.

```cpp
virtual void System::IO::TextWriter::WriteLine()
```

## TextWriter::WriteLine(const SharedPtr\<Object\>\&) método

Escribe la representación en cadena del objeto especificado seguida de los caracteres de terminación de línea en el flujo.

```cpp
virtual void System::IO::TextWriter::WriteLine(const SharedPtr<Object> &value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | const [SharedPtr](../../../system/sharedptr/)\<[Object](../../../system/object/)\>\& | El objeto a escribir |

## TextWriter::WriteLine(bool) método

Escribe la representación en cadena del valor booleano especificado seguida de los caracteres de terminación de línea en el flujo.

```cpp
virtual void System::IO::TextWriter::WriteLine(bool value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **bool** | El valor a escribir |

## TextWriter::WriteLine(char_t) método

Escribe el carácter especificado seguido de los caracteres de terminación de línea en el flujo.

```cpp
virtual void System::IO::TextWriter::WriteLine(char_t value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | char_t | El valor a escribir |

## TextWriter::WriteLine(Decimal) método

Escribe la representación en cadena del objeto [Decimal](../../../system/decimal/) especificado seguida de los caracteres de terminación de línea en el flujo.

```cpp
virtual void System::IO::TextWriter::WriteLine(Decimal value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | [Decimal](../../../system/decimal/) | El objeto a escribir |

## TextWriter::WriteLine(double) método

Escribe la representación en cadena del valor de punto flotante de doble precisión especificado seguido de los caracteres de terminación de línea en el flujo.

```cpp
virtual void System::IO::TextWriter::WriteLine(double value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **double** | El valor a escribir |

## TextWriter::WriteLine(int) método

Escribe la representación en cadena del valor entero de 32 bits especificado seguido de los caracteres de terminación de línea en el flujo.

```cpp
virtual void System::IO::TextWriter::WriteLine(int value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | int | El valor a escribir |

## TextWriter::WriteLine(int64_t) método

Escribe la representación en cadena del valor entero de 64 bits especificado seguido de los caracteres de terminación de línea en el flujo.

```cpp
virtual void System::IO::TextWriter::WriteLine(int64_t value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **int64_t** | El valor a escribir |

## TextWriter::WriteLine(float) método

Escribe la representación en cadena del valor de punto flotante de precisión simple especificado seguido de los caracteres de terminación de línea en el flujo.

```cpp
virtual void System::IO::TextWriter::WriteLine(float value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **float** | El valor a escribir |

## TextWriter::WriteLine(const String\&) método

Escribe la cadena especificada seguida de los caracteres de terminación de línea en el flujo.

```cpp
virtual void System::IO::TextWriter::WriteLine(const String &value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | const [String](../../../system/string/)\& | La cadena a escribir |

## TextWriter::WriteLine(uint32_t) método

Escribe la representación en cadena del valor entero sin signo de 32 bits especificado seguido de los caracteres de terminación de línea en el flujo.

```cpp
virtual void System::IO::TextWriter::WriteLine(uint32_t value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **uint32_t** | El valor a escribir |

## TextWriter::WriteLine(uint64_t) método

Escribe la representación en cadena del valor entero sin signo de 64 bits especificado seguido de los caracteres de terminación de línea en el flujo.

```cpp
virtual void System::IO::TextWriter::WriteLine(uint64_t value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **uint64_t** | El valor a escribir |

## TextWriter::WriteLine(const ArrayPtr\<char_t\>\&) método

Escribe todos los caracteres del array especificado seguido de los caracteres de terminación de línea en el flujo.

```cpp
virtual void System::IO::TextWriter::WriteLine(const ArrayPtr<char_t> &buffer)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../../system/arrayptr/)\<char_t\>\& | El array que contiene los caracteres a escribir |

## TextWriter::WriteLine(const ArrayPtr\<char_t\>\&, int32_t, int32_t) método

Escribe el subrango especificado de caracteres UTF-16 del array de caracteres especificado seguido de los caracteres de terminación de línea en el flujo.

```cpp
virtual void System::IO::TextWriter::WriteLine(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../../system/arrayptr/)\<char_t\>\& | El array que contiene los caracteres a escribir |
| index | **int32_t** | Un índice basado en 0 del elemento en **buffer** donde comienza el subrango a escribir |
| count | **int32_t** | El número de caracteres en el subrango a escribir; -1 especifica que el subrango termina donde finaliza el array **buffer** |

## TextWriter::WriteLine(const char_t *) método

Escribe la cadena C especificada seguida de los caracteres de terminación de línea en el flujo.

```cpp
virtual void System::IO::TextWriter::WriteLine(const char_t *value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | const char_t * | La cadena C a escribir |

## TextWriter::WriteLine(const TypeInfo\&) método

Escribe la representación en cadena del objeto [TypeInfo](../../../system/typeinfo/) especificado seguido de los caracteres de terminación de línea en el flujo.

```cpp
virtual void System::IO::TextWriter::WriteLine(const TypeInfo &value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | const [TypeInfo](../../../system/typeinfo/)\& | El objeto a escribir |

## TextWriter::WriteLine(const String\&, const TArgs\&...) método

Escribe los valores especificados formateados según el formato especificado seguido de los caracteres de terminación de línea en el flujo.

```cpp
template<class...> void System::IO::TextWriter::WriteLine(const String &format, const TArgs &... args)
```

### Parámetros de plantilla

| Parámetro | Descripción |
| --- | --- |
| TArgs | La lista de tipos de valores a escribir |

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| format | const [String](../../../system/string/)\& | El formato de cadena |
| args | const TArgs\&... | Los valores a escribir |

## Ver también

* Typedef [SharedPtr](../../../system/sharedptr/)
* Typedef [ArrayPtr](../../../system/arrayptr/)
* Clase [TextWriter](../)
* Clase [Object](../../../system/object/)
* Clase [Decimal](../../../system/decimal/)
* Clase [String](../../../system/string/)
* Clase [TypeInfo](../../../system/typeinfo/)
* Espacio de nombres [System::IO](../../)
* Library [Aspose.Slides](../../../)