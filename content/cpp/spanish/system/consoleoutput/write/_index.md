---
title: Write()
second_title: Referencia de API de Aspose.Slides para C++
description: Devuelve la representación en cadena del valor bool especificado al flujo de salida representado por el objeto actual.
type: docs
weight: 14
url: /es/system/consoleoutput/write/
---
## ConsoleOutput::Write(bool) método


Devuelve la representación en cadena del valor bool especificado al flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::Write(bool value) override
```


### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **bool** | El valor a escribir |

## ConsoleOutput::Write(const SharedPtr\<Object\>\&) método


Devuelve la representación en cadena del objeto especificado al flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::Write(const SharedPtr<Object> &value) override
```


### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | El objeto a escribir |

## ConsoleOutput::Write(char_t) método


Devuelve el carácter especificado al flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::Write(char_t value) override
```


### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | char_t | El valor a escribir |

## ConsoleOutput::Write(Decimal) método


Devuelve la representación en cadena del valor [Decimal](../../decimal/) al flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::Write(Decimal value) override
```


### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | El valor a escribir |

## ConsoleOutput::Write(double) método


Devuelve la representación en cadena del valor de punto flotante de doble precisión al flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::Write(double value) override
```


### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **double** | El valor a escribir |

## ConsoleOutput::Write(int32_t) método


Devuelve la representación en cadena del valor entero de 32 bits al flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::Write(int32_t value) override
```


### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **int32_t** | El valor a escribir |

## ConsoleOutput::Write(int64_t) método


Devuelve la representación en cadena del valor entero de 64 bits al flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::Write(int64_t value) override
```


### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **int64_t** | El valor a escribir |

## ConsoleOutput::Write(float) método


Devuelve la representación en cadena del valor de punto flotante de precisión simple al flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::Write(float value) override
```


### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **float** | El valor a escribir |

## ConsoleOutput::Write(const String\&) método


Devuelve el objeto string especificado al flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::Write(const String &value) override
```


### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | const [String](../../string/)\& | El objeto string a escribir |

## ConsoleOutput::Write(uint32_t) método


Devuelve la representación en cadena del valor entero sin signo de 32 bits al flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::Write(uint32_t value) override
```


### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **uint32_t** | El valor a escribir |

## ConsoleOutput::Write(uint64_t) método


Devuelve la representación en cadena del valor entero sin signo de 64 bits al flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::Write(uint64_t value) override
```


### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **uint64_t** | El valor a escribir |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&) método


Devuelve la representación en cadena del arreglo de caracteres especificado al flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer) override
```


### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | El arreglo a escribir |

## ConsoleOutput::Write(const ArrayPtr\<char_t\>\&, int32_t, int32_t) método


Devuelve la representación en cadena de un rango de valores del arreglo de caracteres especificado al flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::Write(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```


### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | El arreglo que contiene los valores a escribir |
| index | **int32_t** | El índice donde comienza el rango de elementos a escribir |
| count | **int32_t** | El número de elementos en el rango de elementos a escribir |

## ConsoleOutput::Write(const char_t *) método


Devuelve la c-string especificada al flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::Write(const char_t *value) override
```


### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | const char_t * | La c-string a escribir |

## ConsoleOutput::Write(const TypeInfo\&) método


Devuelve la representación en cadena del objeto [TypeInfo](../../typeinfo/) especificado al flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::Write(const TypeInfo &value) override
```


### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | El objeto [TypeInfo](../../typeinfo/) a escribir |

## ConsoleOutput::Write(const char *) método




```cpp
void System::ConsoleOutput::Write(const char *)=delete
```

## Ver también

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Clase [ConsoleOutput](../)
* Clase [Object](../../object/)
* Clase [Decimal](../../decimal/)
* Clase [String](../../string/)
* Clase [TypeInfo](../../typeinfo/)
* Espacio de nombres [System](../../)
* Biblioteca [Aspose.Slides](../../../)