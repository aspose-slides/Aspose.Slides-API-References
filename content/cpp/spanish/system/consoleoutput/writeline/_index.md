---
title: WriteLine()
second_title: Referencia de la API de Aspose.Slides para C++
description: Escribe el terminador de línea actual en el flujo de salida representado por el objeto actual.
type: docs
weight: 27
url: /es/system/consoleoutput/writeline/
---
## ConsoleOutput::WriteLine() método

Escribe el terminador de línea actual en el flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::WriteLine() override
```

## ConsoleOutput::WriteLine(const SharedPtr\<Object\>\&) método

Escribe la representación en cadena del objeto especificado seguida del terminador de línea actual en el flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::WriteLine(const SharedPtr<Object> &value) override
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | El objeto a imprimir |

## ConsoleOutput::WriteLine(bool) método

Escribe la representación en cadena del valor booleano especificado seguida del terminador de línea actual en el flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::WriteLine(bool value) override
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **bool** | El valor a imprimir |

## ConsoleOutput::WriteLine(char_t) método

Escribe el carácter especificado seguido del terminador de línea actual en el flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::WriteLine(char_t value) override
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | char_t | El valor a imprimir |

## ConsoleOutput::WriteLine(Decimal) método

Escribe la representación en cadena del valor [Decimal](../../decimal/) seguida del terminador de línea actual en el flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::WriteLine(Decimal value) override
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | [Decimal](../../decimal/) | El valor a imprimir |

## ConsoleOutput::WriteLine(double) método

Escribe la representación en cadena del valor de coma flotante de doble precisión seguido del terminador de línea actual en el flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::WriteLine(double value) override
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **double** | El valor a imprimir |

## ConsoleOutput::WriteLine(int) método

Escribe la representación en cadena del valor entero de 32 bits seguido del terminador de línea actual en el flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::WriteLine(int value) override
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | int | El valor a imprimir |

## ConsoleOutput::WriteLine(int64_t) método

Escribe la representación en cadena del valor entero de 64 bits seguido del terminador de línea actual en el flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::WriteLine(int64_t value) override
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **int64_t** | El valor a imprimir |

## ConsoleOutput::WriteLine(float) método

Escribe la representación en cadena del valor de coma flotante de precisión simple seguido del terminador de línea actual en el flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::WriteLine(float value) override
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **float** | El valor a imprimir |

## ConsoleOutput::WriteLine(const String\&) método

Escribe el objeto cadena especificado seguido del terminador de línea actual en el flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::WriteLine(const String &value) override
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | const [String](../../string/)\& | El objeto cadena a imprimir |

## ConsoleOutput::WriteLine(uint32_t) método

Escribe la representación en cadena del valor entero sin signo de 32 bits seguido del terminador de línea actual en el flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::WriteLine(uint32_t value) override
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **uint32_t** | El valor a imprimir |

## ConsoleOutput::WriteLine(uint64_t) método

Escribe la representación en cadena del valor entero sin signo de 64 bits seguido del terminador de línea actual en el flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::WriteLine(uint64_t value) override
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **uint64_t** | El valor a imprimir |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&) método

Escribe la representación en cadena del arreglo de caracteres especificado seguido del terminador de línea actual en el flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer) override
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | El arreglo a imprimir |

## ConsoleOutput::WriteLine(const ArrayPtr\<char_t\>\&, int32_t, int32_t) método

Escribe la representación en cadena de un rango de valores del arreglo de caracteres especificado seguido del terminador de línea actual en el flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::WriteLine(const ArrayPtr<char_t> &buffer, int32_t index, int32_t count) override
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | El arreglo que contiene los valores a imprimir |
| index | **int32_t** | El índice en el que comienza el rango de elementos a imprimir |
| count | **int32_t** | El número de elementos en el rango de elementos a imprimir |

## ConsoleOutput::WriteLine(const char_t *) método

Escribe la cadena C especificada seguida del terminador de línea actual en el flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::WriteLine(const char_t *value) override
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | const char_t * | La cadena C a imprimir |

## ConsoleOutput::WriteLine(const TypeInfo\&) método

Escribe la representación en cadena del objeto [TypeInfo](../../typeinfo/) especificado seguido del terminador de línea actual en el flujo de salida representado por el objeto actual.

```cpp
void System::ConsoleOutput::WriteLine(const TypeInfo &value) override
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | El objeto [TypeInfo](../../typeinfo/) a imprimir |

## ConsoleOutput::WriteLine(const char *) método




```cpp
void System::ConsoleOutput::WriteLine(const char *)=delete
```

## Ver también

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Clase [ConsoleOutput](../)
* Clase [Object](../../object/)
* Clase [Decimal](../../decimal/)
* Clase [String](../../string/)
* Clase [TypeInfo](../../typeinfo/)
* Espacio de nombres [System](../../)
* Library [Aspose.Slides](../../../)