---
title: WriteLine()
second_title: Referencia de la API de Aspose.Slides para C++
description: Emite el terminador de línea actual al flujo de salida estándar.
type: docs
weight: 14
url: /es/system/console/writeline/
---
## Console::WriteLine() método

Emite el terminador de línea actual al flujo de salida estándar.

```cpp
static void System::Console::WriteLine()
```

## Console::WriteLine(const SharedPtr\<T\>\&) método

Emite la representación en cadena del objeto especificado seguida del terminador de línea actual al flujo de salida estándar.

```cpp
template<class T> static void System::Console::WriteLine(const SharedPtr<T> &object)
```

### Parámetros de plantilla

| Parámetro | Descripción |
| --- | --- |
| T | Tipo del objeto a emitir |

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| object | const [SharedPtr](../../sharedptr/)\<T\>\& | [Object](../../object/) para emitir |

## Console::WriteLine(bool) método

Emite la representación en cadena del valor **bool** seguido del terminador de línea actual al flujo de salida estándar.

```cpp
static void System::Console::WriteLine(bool value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **bool** | El valor a emitir |

## Console::WriteLine(char_t) método

Emite el valor de carácter especificado seguido del terminador de línea actual al flujo de salida estándar.

```cpp
static void System::Console::WriteLine(char_t value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | char_t | El valor a emitir |

## Console::WriteLine(const ArrayPtr\<char_t\>\&) método

Emite la representación en cadena del arreglo de caracteres especificado seguido del terminador de línea actual al flujo de salida estándar.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | El arreglo a emitir |

## Console::WriteLine(const Decimal\&) método

Emite la representación en cadena del valor [Decimal](../../decimal/) seguido del terminador de línea actual al flujo de salida estándar.

```cpp
static void System::Console::WriteLine(const Decimal &value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | const [Decimal](../../decimal/)\& | El valor a emitir |

## Console::WriteLine(double) método

Emite la representación en cadena del valor de punto flotante de doble precisión seguido del terminador de línea actual al flujo de salida estándar.

```cpp
static void System::Console::WriteLine(double value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **double** | El valor a emitir |

## Console::WriteLine(float) método

Emite la representación en cadena del valor de punto flotante de precisión simple seguido del terminador de línea actual al flujo de salida estándar.

```cpp
static void System::Console::WriteLine(float value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **float** | El valor a emitir |

## Console::WriteLine(int32_t) método

Emite la representación en cadena del valor entero de 32 bits seguido del terminador de línea actual al flujo de salida estándar.

```cpp
static void System::Console::WriteLine(int32_t value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **int32_t** | El valor a emitir |

## Console::WriteLine(int64_t) método

Emite la representación en cadena del valor entero de 64 bits seguido del terminador de línea actual al flujo de salida estándar.

```cpp
static void System::Console::WriteLine(int64_t value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **int64_t** | El valor a emitir |

## Console::WriteLine(const String\&) método

Emite el objeto cadena especificado seguido del terminador de línea actual al flujo de salida estándar.

```cpp
static void System::Console::WriteLine(const String &value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | const [String](../../string/)\& | El objeto cadena a emitir |

## Console::WriteLine(const char_t *) método

Emite la cadena C especificada seguida del terminador de línea actual al flujo de salida estándar.

```cpp
static void System::Console::WriteLine(const char_t *value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | const char_t * | La cadena C a emitir |

## Console::WriteLine(const TypeInfo\&) método

Emite la representación en cadena del valor [TypeInfo](../../typeinfo/) seguido del terminador de línea actual al flujo de salida estándar.

```cpp
static void System::Console::WriteLine(const TypeInfo &value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | const [TypeInfo](../../typeinfo/)\& | El valor a emitir |

## Console::WriteLine(uint32_t) método

Emite la representación en cadena del valor entero sin signo de 32 bits seguido del terminador de línea actual al flujo de salida estándar.

```cpp
static void System::Console::WriteLine(uint32_t value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **uint32_t** | El valor a emitir |

## Console::WriteLine(uint64_t) método

Emite la representación en cadena del valor entero sin signo de 64 bits seguido del terminador de línea actual al flujo de salida estándar.

```cpp
static void System::Console::WriteLine(uint64_t value)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | **uint64_t** | El valor a emitir |

## Console::WriteLine(const ArrayPtr\<char_t\>\&, int, int) método

Emite la representación en cadena del rango especificado del arreglo de caracteres especificado seguido del terminador de línea actual al flujo de salida estándar.

```cpp
static void System::Console::WriteLine(const ArrayPtr<char_t> &buffer, int index, int count)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| buffer | const [ArrayPtr](../../arrayptr/)\<char_t\>\& | El arreglo de caracteres |
| index | int | El índice en el arreglo donde comienza el rango a emitir |
| count | int | La cantidad de elementos en el rango a emitir |

## Console::WriteLine(const Exception\&) método

Emite la representación en cadena del objeto **Exception** especificado seguido del terminador de línea actual al flujo de salida estándar.

```cpp
static void System::Console::WriteLine(const Exception &e)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| e | const [Exception](../../exception/)\& | El valor a emitir |

## Console::WriteLine(const String\&, Args\&&...) método

Emite la representación en cadena de los argumentos especificados formateados según el formato especificado seguido del terminador de línea actual al flujo de salida estándar.

```cpp
template<class...> static void System::Console::WriteLine(const String &format, Args &&... args)
```

### Parámetros de plantilla

| Parámetro | Descripción |
| --- | --- |
| The | tipos de los valores a emitir |

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| format | const [String](../../string/)\& | El formato de cadena |
| args | Args\&&... | Los valores a emitir |

## Console::WriteLine(const char *) método




```cpp
static void System::Console::WriteLine(const char *)=delete
```

## See Also

* Typedef [SharedPtr](../../sharedptr/)
* Typedef [ArrayPtr](../../arrayptr/)
* Typedef [Exception](../../exception/)
* Class [Console](../)
* Class [Decimal](../../decimal/)
* Class [String](../../string/)
* Class [TypeInfo](../../typeinfo/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)