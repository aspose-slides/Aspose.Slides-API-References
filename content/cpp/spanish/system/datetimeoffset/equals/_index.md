---
title: Equals()
second_title: Referencia de la API de Aspose.Slides para C++
description: Comprueba si dos objetos DateTimeOffset representan el mismo punto de tiempo.
type: docs
weight: 365
url: /es/system/datetimeoffset/equals/
---
## DateTimeOffset::Equals(const DateTimeOffset\&) const método

Comprueba si dos [DateTimeOffset](../) objetos representan el mismo punto de tiempo.

```cpp
bool System::DateTimeOffset::Equals(const DateTimeOffset &other) const
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| other | const [DateTimeOffset](../)\& | [Object](../../object/) para comparar. |

### Valor de retorno

true si el objeto actual es igual a **other**, de lo contrario - false.

## DateTimeOffset::Equals(const SharedPtr\<Object\>\&) const método

Comprueba si dos [DateTimeOffset](../) objetos representan el mismo punto de tiempo.

```cpp
bool System::DateTimeOffset::Equals(const SharedPtr<Object> &obj) const
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| obj | const [SharedPtr](../../sharedptr/)\<[Object](../../object/)\>\& | [Object](../../object/) para comparar. |

### Valor de retorno

true si el objeto actual es igual a **obj**, de lo contrario - false.

## DateTimeOffset::Equals(const DateTimeOffset\&, const DateTimeOffset\&) método

Comprueba si dos [DateTimeOffset](../) objetos representan el mismo punto de tiempo.

```cpp
static bool System::DateTimeOffset::Equals(const DateTimeOffset &first, const DateTimeOffset &second)
```

### Argumentos

| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| first | const [DateTimeOffset](../)\& | Primer objeto a comparar. |
| second | const [DateTimeOffset](../)\& | Segundo objeto a comparar. |

### Valor de retorno

true si **first** es igual a **second**, de lo contrario - false.

## Ver también

* Typedef [SharedPtr](../../sharedptr/)
* Clase [DateTimeOffset](../)
* Clase [Object](../../object/)
* Espacio de nombres [System](../../)
* Biblioteca [Aspose.Slides](../../../)