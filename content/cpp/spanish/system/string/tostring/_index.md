---
title: ToString()
second_title: Referencia de API de Aspose.Slides para C++
description: Envoltorio para manejar la clase String en contextos donde se llama a ToString() sobre objetos de tipo valor.
type: docs
weight: 365
url: /es/system/string/tostring/
---
## String::ToString() const método

Envoltorio para manejar la clase [String](../) en contextos donde se llama a [ToString()](./) sobre objetos de tipo valor.

```cpp
String System::String::ToString() const
```

### Valor devuelto

Copia de la cadena actual. Se considera una cadena nula si la cadena actual es una cadena nula.

## String::ToString(const SharedPtr\<IFormatProvider\>\&) const método

Envoltorio para manejar la clase [String](../) en contextos donde se llama a [ToString()](./) sobre objetos de tipo valor.

```cpp
String System::String::ToString(const SharedPtr<IFormatProvider> &) const
```

### Valor devuelto

Copia de la cadena actual. Se considera una cadena nula si la cadena actual es una cadena nula.

## Ver también

* Typedef [SharedPtr](../../sharedptr/)
* Class [String](../)
* Class [IFormatProvider](../../iformatprovider/)
* Namespace [System](../../)
* Library [Aspose.Slides](../../../)