---
title: MoveToElement()
second_title: Referencia de la API de Aspose.Slides para C++
description: Se desplaza al elemento que contiene el nodo de atributo actual.
type: docs
weight: 495
url: /es/system.xml/xmlvalidatingreader/movetoelement/
---
## XmlValidatingReader::MoveToElement() método

Se desplaza al elemento que contiene el nodo de atributo actual.

```cpp
bool System::Xml::XmlValidatingReader::MoveToElement() override
```

### Valor devuelto

**true** si el lector está posicionado en un atributo (el lector se desplaza al elemento que posee el atributo); **false** si el lector no está posicionado en un atributo (la posición del lector no cambia).

## Ver también

* Clase [XmlValidatingReader](../)
* Espacio de nombres [System::Xml](../../)
* Biblioteca [Aspose.Slides](../../../)