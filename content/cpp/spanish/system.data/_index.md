---
title: "System::Data"
second_title: Referencia de API de Aspose.Slides para C++
description: 
type: docs
weight: 430
url: /es/system.data/
---
## Clases

| Clase | Descripción |
| --- | --- |
| [DataColumn](./datacolumn/) | Describe la columna en los datos. Los objetos de esta clase solo deben asignarse utilizando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use este puntero para pasarlo a funciones como argumento. |
| [DataColumnCollection](./datacolumncollection/) | Encapsula una colección de columnas de datos. Los objetos de esta clase solo deben asignarse utilizando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use este puntero para pasarlo a funciones como argumento. |
| [DataRelation](./datarelation/) | Describe la relación padre-hijo entre tablas. Los objetos de esta clase solo deben asignarse utilizando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use este puntero para pasarlo a funciones como argumento. |
| [DataRelationCollection](./datarelationcollection/) | Colección de dependencias entre tablas. Los objetos de esta clase solo deben asignarse utilizando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use este puntero para pasarlo a funciones como argumento. |
| [DataRow](./datarow/) | Fila en el conjunto de datos. Los objetos de esta clase solo deben asignarse utilizando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use este puntero para pasarlo a funciones como argumento. |
| [DataRowCollection](./datarowcollection/) | Colección de filas de datos. Los objetos de esta clase solo deben asignarse utilizando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use este puntero para pasarlo a funciones como argumento. |
| [DataRowView](./datarowview/) | Vista sobre la fila de datos. Los objetos de esta clase solo deben asignarse utilizando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use este puntero para pasarlo a funciones como argumento. |
| [DataSet](./dataset/) | Conjunto de datos que se almacenan en tablas con algunas relaciones entre ellas. Los objetos de esta clase solo deben asignarse utilizando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use este puntero para pasarlo a funciones como argumento. |
| [DataTable](./datatable/) | [Data](./) estructurado en filas y columnas. Los objetos de esta clase solo deben asignarse utilizando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use este puntero para pasarlo a funciones como argumento. |
| [DataTableCollection](./datatablecollection/) | Colección de tablas de datos. Los objetos de esta clase solo deben asignarse utilizando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use este puntero para pasarlo a funciones como argumento. |
| [DataView](./dataview/) | Vista que trabaja sobre la tabla. Los objetos de esta clase solo deben asignarse utilizando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use este puntero para pasarlo a funciones como argumento. |
| [IDataReader](./idatareader/) | Interfaz para leer datos consecuentes de. Los objetos de esta clase solo deben asignarse utilizando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use este puntero para pasarlo a funciones como argumento. |
| [IDataRecord](./idatarecord/) | Interfaz para registrar con columnas. Los objetos de esta clase solo deben asignarse utilizando la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila o usando el operador new, ya que provocará errores de ejecución y/o fallas de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use este puntero para pasarlo a funciones como argumento. |