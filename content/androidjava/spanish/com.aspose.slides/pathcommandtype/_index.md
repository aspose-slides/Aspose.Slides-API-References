---
title: PathCommandType
second_title: Aspose.Slides para Android a través de la referencia de API Java
description: Representa los comandos de ruta gráfica
type: docs
url: /es/com.aspose.slides/pathcommandtype/
---
**Herencia:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class PathCommandType extends System.Enum
```

Representa los comandos de ruta gráfica
## Campos

| Campo | Descripción |
| --- | --- |
| [Close](#Close) | Cierra la ruta geométrica |
| [MoveTo](#MoveTo) | Establece la posición del siguiente punto de la ruta geométrica |
| [LineTo](#LineTo) | Añade una línea a la ruta geométrica |
| [ArcTo](#ArcTo) | Añade el arco especificado a la ruta geométrica. |
| [QuadBezierTo](#QuadBezierTo) | Añade una curva Bezier cuadrática a la ruta geométrica |
| [CubicBezierTo](#CubicBezierTo) | Añade una curva Bezier cúbica a la ruta geométrica |
### Close {#Close}
```
public static final byte Close
```

Cierra la ruta geométrica

### MoveTo {#MoveTo}
```
public static final byte MoveTo
```

Establece la posición del siguiente punto de la ruta geométrica

### LineTo {#LineTo}
```
public static final byte LineTo
```

Añade una línea a la ruta geométrica

### ArcTo {#ArcTo}
```
public static final byte ArcTo
```

Añade el arco especificado a la ruta geométrica.

### QuadBezierTo {#QuadBezierTo}
```
public static final byte QuadBezierTo
```

Añade una curva Bezier cuadrática a la ruta geométrica

### CubicBezierTo {#CubicBezierTo}
```
public static final byte CubicBezierTo
```

Añade una curva Bezier cúbica a la ruta geométrica