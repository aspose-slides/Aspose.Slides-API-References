---
title: NormalViewRestoredProperties
second_title: Aspose.Slides para Android a través de la API de Java
description: Especifica el dimensionado de la región de la diapositiva ancho cuando es hijo de restoredTop alto cuando es hijo de restoredLeft de la vista normal cuando la región tiene un tamaño restaurado variable, ni minimizada ni maximizada.
type: docs
url: /es/com.aspose.slides/normalviewrestoredproperties/
---
**Herencia:**
java.lang.Object

**Todas las interfaces implementadas:**
[com.aspose.slides.INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)
```
public class NormalViewRestoredProperties implements INormalViewRestoredProperties
```

Especifica el dimensionado de la región de la diapositiva ((ancho cuando es hijo de restoredTop, alto cuando es hijo de restoredLeft) de la vista normal, cuando la región tiene un tamaño restaurado variable (ni minimizada ni maximizada)).

## Métodos

| Método | Descripción |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | Especifica el tamaño de la región de la diapositiva (ancho cuando es hijo de RestoredTop, alto cuando es hijo de RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | Especifica el tamaño de la región de la diapositiva (ancho cuando es hijo de RestoredTop, alto cuando es hijo de RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | Especifica si el tamaño de la región de contenido lateral debe compensar el nuevo tamaño al redimensionar la ventana que contiene la vista dentro de la aplicación Lectura/escritura booleano. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | Especifica si el tamaño de la región de contenido lateral debe compensar el nuevo tamaño al redimensionar la ventana que contiene la vista dentro de la aplicación Lectura/escritura booleano. |

### getDimensionSize() {#getDimensionSize--}
```
public final float getDimensionSize()
```

Especifica el tamaño de la región de la diapositiva (ancho cuando es hijo de RestoredTop, alto cuando es hijo de RestoredLeft). Lectura/escritura float.

**Devuelve:**
float

### setDimensionSize(float value) {#setDimensionSize-float-}
```
public final void setDimensionSize(float value)
```

Especifica el tamaño de la región de la diapositiva (ancho cuando es hijo de RestoredTop, alto cuando es hijo de RestoredLeft). Lectura/escritura float.

**Parámetros:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public final boolean getAutoAdjust()
```

Especifica si el tamaño de la región de contenido lateral debe compensar el nuevo tamaño al redimensionar la ventana que contiene la vista dentro de la aplicación Lectura/escritura booleano.

**Devuelve:**
boolean

### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public final void setAutoAdjust(boolean value)
```

Especifica si el tamaño de la región de contenido lateral debe compensar el nuevo tamaño al redimensionar la ventana que contiene la vista dentro de la aplicación Lectura/escritura booleano.

**Parámetros:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| value | boolean |  |