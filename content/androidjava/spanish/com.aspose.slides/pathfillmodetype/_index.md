---
title: PathFillModeType
second_title: Aspose.Slides para Android mediante la referencia de API Java
description: Especifica la forma en que una ruta debe ser rellenada
type: docs
url: /es/com.aspose.slides/pathfillmodetype/
---
**Herencia:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class PathFillModeType extends System.Enum
```

Especifica la forma en que una ruta debe ser rellenada
## Campos

| Campo | Descripción |
| --- | --- |
| [None](#None) | Sin relleno de ruta. Esto especifica que la ruta correspondiente no debe tener relleno. |
| [Normal](#Normal) | Relleno de ruta normal. Esto especifica que la ruta correspondiente debe tener un color sombreado normalmente aplicado a su relleno. |
| [Lighten](#Lighten) | Relleno de ruta aclarado. Esto especifica que la ruta correspondiente debe tener un color ligeramente sombreado aplicado a su relleno. |
| [LightenLess](#LightenLess) | Relleno de ruta ligeramente aclarado. Esto especifica que la ruta correspondiente debe tener un color un poco más claro aplicado a su relleno. |
| [Darken](#Darken) | Relleno de ruta oscurecido. Esto especifica que la ruta correspondiente debe tener un color más oscuro aplicado a su relleno. |
| [DarkenLess](#DarkenLess) | Relleno de ruta ligeramente oscurecido. Esto especifica que la ruta correspondiente debe tener un color un poco más oscuro aplicado a su relleno. |
### None {#None}
```
public static final byte None
```

Sin relleno de ruta. Esto especifica que la ruta correspondiente no debe tener relleno.

### Normal {#Normal}
```
public static final byte Normal
```

Relleno de ruta normal. Esto especifica que la ruta correspondiente debe tener un color sombreado normalmente aplicado a su relleno.

### Lighten {#Lighten}
```
public static final byte Lighten
```

Relleno de ruta aclarado. Esto especifica que la ruta correspondiente debe tener un color ligeramente sombreado aplicado a su relleno.

### LightenLess {#LightenLess}
```
public static final byte LightenLess
```

Relleno de ruta ligeramente aclarado. Esto especifica que la ruta correspondiente debe tener un color un poco más claro aplicado a su relleno.

### Darken {#Darken}
```
public static final byte Darken
```

Relleno de ruta oscurecido. Esto especifica que la ruta correspondiente debe tener un color más oscuro aplicado a su relleno.

### DarkenLess {#DarkenLess}
```
public static final byte DarkenLess
```

Relleno de ruta ligeramente oscurecido. Esto especifica que la ruta correspondiente debe tener un color un poco más oscuro aplicado a su relleno.