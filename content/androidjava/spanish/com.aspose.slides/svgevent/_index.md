---
title: SvgEvent
second_title: Aspose.Slides para Android a través de la referencia de API Java
description: Representa opciones para la forma SVG.
type: docs
url: /es/com.aspose.slides/svgevent/
---
**Herencia:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class SvgEvent extends System.Enum
```

Representa opciones para la forma SVG.

## Campos

| Campo | Descripción |
| --- | --- |
| [OnFocusIn](#OnFocusIn) | Ocurre cuando un elemento recibe el foco, como cuando un texto se selecciona. |
| [OnFocusOut](#OnFocusOut) | Ocurre cuando un elemento pierde el foco, como cuando un texto deja de estar seleccionado. |
| [OnActivate](#OnActivate) | Ocurre cuando un elemento se activa, por ejemplo, mediante un clic del ratón o una pulsación de tecla. |
| [OnClick](#OnClick) | Ocurre cuando se hace clic con el botón del dispositivo apuntador sobre un elemento. |
| [OnMouseDown](#OnMouseDown) | Ocurre cuando se pulsa el botón del dispositivo apuntador sobre un elemento. |
| [OnMouseUp](#OnMouseUp) | Ocurre cuando se suelta el botón del dispositivo apuntador sobre un elemento. |
| [OnMouseOver](#OnMouseOver) | Ocurre cuando el dispositivo apuntador se mueve sobre un elemento. |
| [OnMouseMove](#OnMouseMove) | Ocurre cuando el dispositivo apuntador se mueve mientras está sobre un elemento. |
| [OnMouseOut](#OnMouseOut) | Ocurre cuando el dispositivo apuntador se aleja de un elemento. |
| [OnLoad](#OnLoad) | Ocurre cuando el agente de usuario ha analizado completamente el elemento y sus descendientes y todos los recursos referenciados requeridos para renderizarlo. |
| [OnUnload](#OnUnload) | Ocurre cuando la implementación del DOM elimina un documento de una ventana o marco. |
| [OnAbort](#OnAbort) | Ocurre cuando la carga de la página se detiene antes de que un elemento haya podido cargarse completamente. |
| [OnError](#OnError) | Ocurre cuando un elemento no se carga correctamente o cuando ocurre un error durante la ejecución del script. |
| [OnResize](#OnResize) | Ocurre cuando la vista del documento se está redimensionando. |
| [OnScroll](#OnScroll) | Ocurre cuando la vista del documento se desplaza a lo largo del eje X, Y o ambos. |
| [OnZoom](#OnZoom) | Ocurre cuando se está cambiando el nivel de zoom de la vista del documento. |
| [OnBegin](#OnBegin) | Ocurre cuando un elemento de animación comienza. |
| [OnEnd](#OnEnd) | Ocurre cuando un elemento de animación termina. |
| [OnRepeat](#OnRepeat) | Ocurre cuando un elemento de animación se repite. |

### OnFocusIn {#OnFocusIn}
```
public static final int OnFocusIn
```

Ocurre cuando un elemento recibe el foco, como cuando un texto se selecciona.

### OnFocusOut {#OnFocusOut}
```
public static final int OnFocusOut
```

Ocurre cuando un elemento pierde el foco, como cuando un texto deja de estar seleccionado.

### OnActivate {#OnActivate}
```
public static final int OnActivate
```

Ocurre cuando un elemento se activa, por ejemplo, mediante un clic del ratón o una pulsación de tecla.

### OnClick {#OnClick}
```
public static final int OnClick
```

Ocurre cuando se hace clic con el botón del dispositivo apuntador sobre un elemento.

### OnMouseDown {#OnMouseDown}
```
public static final int OnMouseDown
```

Ocurre cuando se pulsa el botón del dispositivo apuntador sobre un elemento.

### OnMouseUp {#OnMouseUp}
```
public static final int OnMouseUp
```

Ocurre cuando se suelta el botón del dispositivo apuntador sobre un elemento.

### OnMouseOver {#OnMouseOver}
```
public static final int OnMouseOver
```

Ocurre cuando el dispositivo apuntador se mueve sobre un elemento.

### OnMouseMove {#OnMouseMove}
```
public static final int OnMouseMove
```

Ocurre cuando el dispositivo apuntador se mueve mientras está sobre un elemento.

### OnMouseOut {#OnMouseOut}
```
public static final int OnMouseOut
```

Ocurre cuando el dispositivo apuntador se aleja de un elemento.

### OnLoad {#OnLoad}
```
public static final int OnLoad
```

Ocurre cuando el agente de usuario ha analizado completamente el elemento y sus descendientes y todos los recursos referenciados requeridos para renderizarlo.

### OnUnload {#OnUnload}
```
public static final int OnUnload
```

Ocurre cuando la implementación del DOM elimina un documento de una ventana o marco. Only applicable to outermost svg elements.

### OnAbort {#OnAbort}
```
public static final int OnAbort
```

Ocurre cuando la carga de la página se detiene antes de que un elemento haya podido cargarse completamente.

### OnError {#OnError}
```
public static final int OnError
```

Ocurre cuando un elemento no se carga correctamente o cuando ocurre un error durante la ejecución del script.

### OnResize {#OnResize}
```
public static final int OnResize
```

Ocurre cuando la vista del documento se está redimensionando. Only applicable to outermost svg elements.

### OnScroll {#OnScroll}
```
public static final int OnScroll
```

Ocurre cuando la vista del documento se desplaza a lo largo del eje X, Y o ambos. Only applicable to outermost svg elements.

### OnZoom {#OnZoom}
```
public static final int OnZoom
```

Ocurre cuando se está cambiando el nivel de zoom de la vista del documento. Only applicable to outermost svg elements.

### OnBegin {#OnBegin}
```
public static final int OnBegin
```

Ocurre cuando un elemento de animación comienza.

### OnEnd {#OnEnd}
```
public static final int OnEnd
```

Ocurre cuando un elemento de animación termina.

### OnRepeat {#OnRepeat}
```
public static final int OnRepeat
```

Ocurre cuando un elemento de animación se repite.