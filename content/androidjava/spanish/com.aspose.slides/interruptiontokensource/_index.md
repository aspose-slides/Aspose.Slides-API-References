---
title: InterruptionTokenSource
second_title: Aspose.Slides para Android mediante la referencia de la API Java
description: Representa la fuente de .
type: docs
url: /es/com.aspose.slides/interruptiontokensource/
---
**Herencia:**
java.lang.Object
```
public class InterruptionTokenSource
```

Representa la fuente de [InterruptionToken](../../com.aspose.slides/interruptiontoken).
## Constructores

| Constructor | Descripción |
| --- | --- |
| [InterruptionTokenSource()](#InterruptionTokenSource--) | Crea un nuevo [InterruptionTokenSource](../../com.aspose.slides/interruptiontokensource). |
## Métodos

| Método | Descripción |
| --- | --- |
| [getToken()](#getToken--) | Devuelve un nuevo token enlazado a este [InterruptionTokenSource](../../com.aspose.slides/interruptiontokensource). |
| [isInterruptionRequested()](#isInterruptionRequested--) | Devuelve true si se solicitó interrupción, false en caso contrario. |
| [interrupt()](#interrupt--) | Inicializa la solicitud de interrupción. |
### InterruptionTokenSource() {#InterruptionTokenSource--}
```
public InterruptionTokenSource()
```

Crea un nuevo [InterruptionTokenSource](../../com.aspose.slides/interruptiontokensource).

### getToken() {#getToken--}
```
public final InterruptionToken getToken()
```

Devuelve un nuevo token enlazado a este [InterruptionTokenSource](../../com.aspose.slides/interruptiontokensource).

**Devuelve:**
[InterruptionToken](../../com.aspose.slides/interruptiontoken)
### isInterruptionRequested() {#isInterruptionRequested--}
```
public final boolean isInterruptionRequested()
```

Devuelve true si se solicitó interrupción, false en caso contrario.

**Devuelve:**
boolean
### interrupt() {#interrupt--}
```
public final void interrupt()
```

Inicializa la solicitud de interrupción.