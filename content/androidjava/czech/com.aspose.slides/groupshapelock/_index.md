---
title: GroupShapeLock
second_title: Aspose.Slides pro Android pomocí Java API Reference
description: Určuje, které operace jsou na nadřazeném GroupShape zakázány.
type: docs
url: /cs/com.aspose.slides/groupshapelock/
---
**Dědičnost:**
java.lang.Object, [com.aspose.slides.BaseShapeLock](../../com.aspose.slides/baseshapelock)

**Všechny implementované rozhraní:**
[com.aspose.slides.IGroupShapeLock](../../com.aspose.slides/igroupshapelock)
```
public class GroupShapeLock extends BaseShapeLock implements IGroupShapeLock
```

Určuje, které operace jsou na nadřazeném GroupShape zakázány.
## Metody

| Metoda | Popis |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | Určuje, zda je přidání tohoto tvaru do skupiny zakázáno. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | Určuje, zda je přidání tohoto tvaru do skupiny zakázáno. |
| [getUngroupingLocked()](#getUngroupingLocked--) | Určuje, zda je rozdělení tohoto groupshape zakázáno. |
| [setUngroupingLocked(boolean value)](#setUngroupingLocked-boolean-) | Určuje, zda je rozdělení tohoto groupshape zakázáno. |
| [getSelectLocked()](#getSelectLocked--) | Určuje, zda je výběr tohoto tvaru zakázán. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | Určuje, zda je výběr tohoto tvaru zakázán. |
| [getRotationLocked()](#getRotationLocked--) | Určuje, zda je změna úhlu otočení tohoto tvaru zakázána. |
| [setRotationLocked(boolean value)](#setRotationLocked-boolean-) | Určuje, zda je změna úhlu otočení tohoto tvaru zakázána. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | Určuje, zda má tvar zachovat poměr stran při změně velikosti. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | Určuje, zda má tvar zachovat poměr stran při změně velikosti. |
| [getPositionLocked()](#getPositionLocked--) | Určuje, zda je pohyb tohoto tvaru zakázán. |
| [setPositionLocked(boolean value)](#setPositionLocked-boolean-) | Určuje, zda je pohyb tohoto tvaru zakázán. |
| [getSizeLocked()](#getSizeLocked--) | Určuje, zda je změna velikosti tohoto tvaru zakázána. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | Určuje, zda je změna velikosti tohoto tvaru zakázána. |
### getGroupingLocked() {#getGroupingLocked--}
```
public boolean getGroupingLocked()
```

Určuje, zda je přidání tohoto tvaru do skupiny zakázáno. Boolean s možností čtení a zápisu.

**Vrací:**
boolean
### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public void setGroupingLocked(boolean value)
```

Určuje, zda je přidání tohoto tvaru do skupiny zakázáno. Boolean s možností čtení a zápisu.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |
### getUngroupingLocked() {#getUngroupingLocked--}
```
public boolean getUngroupingLocked()
```

Určuje, zda je rozdělení tohoto groupshape zakázáno. Boolean s možností čtení a zápisu.

**Vrací:**
boolean
### setUngroupingLocked(boolean value) {#setUngroupingLocked-boolean-}
```
public void setUngroupingLocked(boolean value)
```

Určuje, zda je rozdělení tohoto groupshape zakázáno. Boolean s možností čtení a zápisu.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |
### getSelectLocked() {#getSelectLocked--}
```
public boolean getSelectLocked()
```

Určuje, zda je výběr tohoto tvaru zakázán. Boolean s možností čtení a zápisu.

**Vrací:**
boolean
### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public void setSelectLocked(boolean value)
```

Určuje, zda je výběr tohoto tvaru zakázán. Boolean s možností čtení a zápisu.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |
### getRotationLocked() {#getRotationLocked--}
```
public boolean getRotationLocked()
```

Určuje, zda je změna úhlu otočení tohoto tvaru zakázána. Boolean s možností čtení a zápisu.

**Vrací:**
boolean
### setRotationLocked(boolean value) {#setRotationLocked-boolean-}
```
public void setRotationLocked(boolean value)
```

Určuje, zda je změna úhlu otočení tohoto tvaru zakázána. Boolean s možností čtení a zápisu.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |
### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public boolean getAspectRatioLocked()
```

Určuje, zda má tvar zachovat poměr stran při změně velikosti. Boolean s možností čtení a zápisu.

**Vrací:**
boolean
### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public void setAspectRatioLocked(boolean value)
```

Určuje, zda má tvar zachovat poměr stran při změně velikosti. Boolean s možností čtení a zápisu.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |
### getPositionLocked() {#getPositionLocked--}
```
public boolean getPositionLocked()
```

Určuje, zda je pohyb tohoto tvaru zakázán. Boolean s možností čtení a zápisu.

**Vrací:**
boolean
### setPositionLocked(boolean value) {#setPositionLocked-boolean-}
```
public void setPositionLocked(boolean value)
```

Určuje, zda je pohyb tohoto tvaru zakázán. Boolean s možností čtení a zápisu.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |
### getSizeLocked() {#getSizeLocked--}
```
public boolean getSizeLocked()
```

Určuje, zda je změna velikosti tohoto tvaru zakázána. Boolean s možností čtení a zápisu.

**Vrací:**
boolean
### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public void setSizeLocked(boolean value)
```

Určuje, zda je změna velikosti tohoto tvaru zakázána. Boolean s možností čtení a zápisu.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |