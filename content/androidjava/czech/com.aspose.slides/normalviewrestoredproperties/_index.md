---
title: NormalViewRestoredProperties
second_title: Aspose.Slides pro Android pomocí Java API Reference
description: Určuje velikost oblasti snímku (šířka, když je podřízená restoredTop, výška, když je podřízená restoredLeft) normálního zobrazení, když je oblast proměnné obnovitelné velikosti (ne minimalizovaná, ani maximalizovaná).
type: docs
url: /cs/com.aspose.slides/normalviewrestoredproperties/
---
**Dědičnost:**
java.lang.Object

**Všechny implementované rozhraní:**
[com.aspose.slides.INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)
```
public class NormalViewRestoredProperties implements INormalViewRestoredProperties
```

Určuje velikost oblasti snímku ((šířka, když je podřízená restoredTop, výška, když je podřízená restoredLeft) normálního zobrazení, když je oblast proměnné obnovitelné velikosti (ne minimalizovaná, ani maximalizovaná)).
## Metody

| Metoda | Popis |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | Určuje velikost oblasti snímku (šířka, když je podřízená RestoredTop, výška, když je podřízená RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | Určuje velikost oblasti snímku (šířka, když je podřízená RestoredTop, výška, když je podřízená RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | Určuje, zda má velikost oblasti postranního obsahu kompenzovat novou velikost při změně velikosti okna obsahujícího zobrazení v aplikaci Read/write boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | Určuje, zda má velikost oblasti postranního obsahu kompenzovat novou velikost při změně velikosti okna obsahujícího zobrazení v aplikaci Read/write boolean. |
### getDimensionSize() {#getDimensionSize--}
```
public final float getDimensionSize()
```

Určuje velikost oblasti snímku (šířka, když je podřízená RestoredTop, výška, když je podřízená RestoredLeft). Read/write float.

**Vrací:**
float
### setDimensionSize(float value) {#setDimensionSize-float-}
```
public final void setDimensionSize(float value)
```

Určuje velikost oblasti snímku (šířka, když je podřízená RestoredTop, výška, když je podřízená RestoredLeft). Read/write float.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | float |  |
### getAutoAdjust() {#getAutoAdjust--}
```
public final boolean getAutoAdjust()
```

Určuje, zda má velikost oblasti postranního obsahu kompenzovat novou velikost při změně velikosti okna obsahujícího zobrazení v aplikaci Read/write boolean.

**Vrací:**
boolean
### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public final void setAutoAdjust(boolean value)
```

Určuje, zda má velikost oblasti postranního obsahu kompenzovat novou velikost při změně velikosti okna obsahujícího zobrazení v aplikaci Read/write boolean.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |