---
title: Placeholder
second_title: Aspose.Slides pro Android přes referenci API Java
description: Představuje zástupný prvek na snímku.
type: docs
url: /cs/com.aspose.slides/placeholder/
---
**Dědičnost:**
java.lang.Object

**Všechny implementované rozhraní:**
[com.aspose.slides.IPlaceholder](../../com.aspose.slides/iplaceholder)
```
public class Placeholder implements IPlaceholder
```

Představuje zástupný prvek na snímku.
## Metody

| Metoda | Popis |
| --- | --- |
| [getOrientation()](#getOrientation--) | Získá nebo nastaví orientaci zástupného prvku. |
| [getSize()](#getSize--) | Získá nebo nastaví velikost zástupného prvku. |
| [getType()](#getType--) | Získá nebo nastaví typ zástupného prvku. |
| [getIndex()](#getIndex--) | Získá nebo nastaví index zástupného prvku. |
### getOrientation() {#getOrientation--}
```
public byte getOrientation()
```

Získá nebo nastaví orientaci zástupného prvku. Pouze pro čtení [Orientation](../../com.aspose.slides/orientation).

**Vrací:**
byte
### getSize() {#getSize--}
```
public byte getSize()
```

Získá nebo nastaví velikost zástupného prvku. Pouze pro čtení [PlaceholderSize](../../com.aspose.slides/placeholdersize).

**Vrací:**
byte
### getType() {#getType--}
```
public byte getType()
```

Získá nebo nastaví typ zástupného prvku. Pouze pro čtení [PlaceholderType](../../com.aspose.slides/placeholdertype).

**Vrací:**
byte
### getIndex() {#getIndex--}
```
public long getIndex()
```

Získá nebo nastaví index zástupného prvku. Pouze pro čtení long.

**Vrací:**
long