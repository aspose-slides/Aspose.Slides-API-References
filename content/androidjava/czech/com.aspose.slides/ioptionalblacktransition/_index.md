---
title: IOptionalBlackTransition
second_title: Aspose.Slides pro Android přes Java API Reference
description: Volitelný černý efekt přechodu snímku.
type: docs
url: /cs/com.aspose.slides/ioptionalblacktransition/
---
**Všechny implementované rozhraní:**
[com.aspose.slides.ITransitionValueBase](../../com.aspose.slides/itransitionvaluebase)
```
public interface IOptionalBlackTransition extends ITransitionValueBase
```

Volitelný černý efekt přechodu snímku.
## Metody

| Metoda | Popis |
| --- | --- |
| [getFromBlack()](#getFromBlack--) | Tento atribut určuje, zda přechod začne z černé obrazovky (a pak přejde na nový snímek přes černou). |
| [setFromBlack(boolean value)](#setFromBlack-boolean-) | Tento atribut určuje, zda přechod začne z černé obrazovky (a pak přejde na nový snímek přes černou). |
### getFromBlack() {#getFromBlack--}
```
public abstract boolean getFromBlack()
```

Tento atribut určuje, zda přechod začne z černé obrazovky (a pak přejde na nový snímek přes černou). Číst/zapisovat boolean.

**Vrací:**
boolean
### setFromBlack(boolean value) {#setFromBlack-boolean-}
```
public abstract void setFromBlack(boolean value)
```

Tento atribut určuje, zda přechod začne z černé obrazovky (a pak přejde na nový snímek přes černou). Číst/zapisovat boolean.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |