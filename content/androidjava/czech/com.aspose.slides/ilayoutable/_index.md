---
title: ILayoutable
second_title: Aspose.Slides pro Android přes Java API Reference
description: Určuje přesnou polohu prvku grafu.
type: docs
url: /cs/com.aspose.slides/ilayoutable/
---
**Všechna implementovaná rozhraní:**
[com.aspose.slides.IChartComponent](../../com.aspose.slides/ichartcomponent)
```
public interface ILayoutable extends IChartComponent
```

Určuje přesnou polohu prvku grafu.
## Metody

| Metoda | Popis |
| --- | --- |
| [getX()](#getX--) | Určuje x-polohu (levý okraj) prvku grafu jako podíl šířky grafu. |
| [setX(float value)](#setX-float-) | Určuje x-polohu (levý okraj) prvku grafu jako podíl šířky grafu. |
| [getY()](#getY--) | Určuje horní část prvku grafu jako podíl výšky grafu. |
| [setY(float value)](#setY-float-) | Určuje horní část prvku grafu jako podíl výšky grafu. |
| [getWidth()](#getWidth--) | Určuje šířku prvku grafu jako podíl šířky grafu. |
| [setWidth(float value)](#setWidth-float-) | Určuje šířku prvku grafu jako podíl šířky grafu. |
| [getHeight()](#getHeight--) | Určuje výšku prvku grafu jako podíl výšky grafu. |
| [setHeight(float value)](#setHeight-float-) | Určuje výšku prvku grafu jako podíl výšky grafu. |
| [getRight()](#getRight--) | Získá pravou stranu prvku grafu jako podíl šířky grafu. |
| [getBottom()](#getBottom--) | Získá horní část prvku grafu jako podíl výšky grafu. |
### getX() {#getX--}
```
public abstract float getX()
```

Určuje x-polohu (levý okraj) prvku grafu jako podíl šířky grafu. Čtení/zápis float.

**Vrací:**
float
### setX(float value) {#setX-float-}
```
public abstract void setX(float value)
```

Určuje x-polohu (levý okraj) prvku grafu jako podíl šířky grafu. Čtení/zápis float.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | float |  |

### getY() {#getY--}
```
public abstract float getY()
```

Určuje horní část prvku grafu jako podíl výšky grafu. Čtení/zápis float.

**Vrací:**
float
### setY(float value) {#setY-float-}
```
public abstract void setY(float value)
```

Určuje horní část prvku grafu jako podíl výšky grafu. Čtení/zápis float.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | float |  |

### getWidth() {#getWidth--}
```
public abstract float getWidth()
```

Určuje šířku prvku grafu jako podíl šířky grafu. Čtení/zápis float.

**Vrací:**
float
### setWidth(float value) {#setWidth-float-}
```
public abstract void setWidth(float value)
```

Určuje šířku prvku grafu jako podíl šířky grafu. Čtení/zápis float.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | float |  |

### getHeight() {#getHeight--}
```
public abstract float getHeight()
```

Určuje výšku prvku grafu jako podíl výšky grafu. Čtení/zápis float.

**Vrací:**
float
### setHeight(float value) {#setHeight-float-}
```
public abstract void setHeight(float value)
```

Určuje výšku prvku grafu jako podíl výšky grafu. Čtení/zápis float.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | float |  |

### getRight() {#getRight--}
```
public abstract float getRight()
```

Získá pravou stranu prvku grafu jako podíl šířky grafu. Pouze pro čtení float.

**Vrací:**
float
### getBottom() {#getBottom--}
```
public abstract float getBottom()
```

Získá horní část prvku grafu jako podíl výšky grafu. Pouze pro čtení float.

**Vrací:**
float