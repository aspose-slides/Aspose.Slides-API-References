---
title: BaseSlideHeaderFooterManager
second_title: Aspose.Slides pro Android přes Java API Reference
description: Představuje správce, který obsahuje chování placeholderů zápatí, data a času a číslování stránek pro všechny typy snímků.
type: docs
url: /cs/com.aspose.slides/baseslideheaderfootermanager/
---
**Dědičnost:**
java.lang.Object, [com.aspose.slides.BaseHeaderFooterManager](../../com.aspose.slides/baseheaderfootermanager)
```
public abstract class BaseSlideHeaderFooterManager extends BaseHeaderFooterManager
```

Představuje správce, který obsahuje chování placeholderů zápatí, data a času a číslování stránek pro všechny typy snímků.
## Metody

| Metoda | Popis |
| --- | --- |
| [isFooterVisible()](#isFooterVisible--) | Získá hodnotu, která označuje, že je přítomen placeholder zápatí. |
| [isSlideNumberVisible()](#isSlideNumberVisible--) | Získá hodnotu, která označuje, že je přítomen placeholder čísla stránky. |
| [isDateTimeVisible()](#isDateTimeVisible--) | Získá hodnotu, která označuje, že je přítomen placeholder data a času. |
| [setFooterVisibility(boolean isVisible)](#setFooterVisibility-boolean-) | Mění viditelnost placeholderu zápatí snímku. |
| [setSlideNumberVisibility(boolean isVisible)](#setSlideNumberVisibility-boolean-) | Mění viditelnost placeholderu čísla stránky snímku. |
| [setDateTimeVisibility(boolean isVisible)](#setDateTimeVisibility-boolean-) | Mění viditelnost placeholderu data a času snímku. |
| [setFooterText(String text)](#setFooterText-java.lang.String-) | Nastaví text do placeholderu zápatí snímku. |
| [setDateTimeText(String text)](#setDateTimeText-java.lang.String-) | Nastaví text do placeholderu data a času snímku. |
### isFooterVisible() {#isFooterVisible--}
```
public final boolean isFooterVisible()
```


Získá hodnotu, která označuje, že je přítomen placeholder zápatí. Čte boolean.

**Vrací:**
boolean
### isSlideNumberVisible() {#isSlideNumberVisible--}
```
public final boolean isSlideNumberVisible()
```


Získá hodnotu, která označuje, že je přítomen placeholder čísla stránky. Čte boolean.

**Vrací:**
boolean
### isDateTimeVisible() {#isDateTimeVisible--}
```
public final boolean isDateTimeVisible()
```


Získá hodnotu, která označuje, že je přítomen placeholder data a času. Čte boolean.

**Vrací:**
boolean
### setFooterVisibility(boolean isVisible) {#setFooterVisibility-boolean-}
```
public final void setFooterVisibility(boolean isVisible)
```


Mění viditelnost placeholderu zápatí snímku.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| isVisible | boolean | true – zviditelní placeholder zápatí, jinak – jej skryje. |

### setSlideNumberVisibility(boolean isVisible) {#setSlideNumberVisibility-boolean-}
```
public final void setSlideNumberVisibility(boolean isVisible)
```


Mění viditelnost placeholderu čísla stránky snímku.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| isVisible | boolean | true – zviditelní placeholder čísla stránky, jinak – jej skryje. |

### setDateTimeVisibility(boolean isVisible) {#setDateTimeVisibility-boolean-}
```
public final void setDateTimeVisibility(boolean isVisible)
```


Mění viditelnost placeholderu data a času snímku.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| isVisible | boolean | true – zviditelní placeholder data a času, jinak – jej skryje. |

### setFooterText(String text) {#setFooterText-java.lang.String-}
```
public final void setFooterText(String text)
```


Nastaví text do placeholderu zápatí snímku.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| text | java.lang.String | Text k nastavení. |

### setDateTimeText(String text) {#setDateTimeText-java.lang.String-}
```
public final void setDateTimeText(String text)
```


Nastaví text do placeholderu data a času snímku.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| text | java.lang.String | Text k nastavení. |