---
title: IScaleEffect
second_title: Aspose.Slides pro Android prostřednictvím Java API
description: Reprezentuje efekt měřítka animace.
type: docs
url: /cs/com.aspose.slides/iscaleeffect/
---
**Všechny implementované rozhraní:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IScaleEffect extends IBehavior
```

Reprezentuje efekt měřítka animace.
## Metody

| Metoda | Popis |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Určuje, zda by měl být obsah zvětšen. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Určuje, zda by měl být obsah zvětšen. |
| [getFrom()](#getFrom--) | Určuje souřadnici x/y, odkud se má animace spustit (v procentech). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | Určuje souřadnici x/y, odkud se má animace spustit (v procentech). |
| [getTo()](#getTo--) | Určuje cílovou polohu pro efekt měřítka animace (v procentech). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | Určuje cílovou polohu pro efekt měřítka animace (v procentech). |
| [getBy()](#getBy--) | Popisuje relativní hodnotu posunu pro animaci (v procentech). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | Popisuje relativní hodnotu posunu pro animaci (v procentech). |
### getZoomContent() {#getZoomContent--}
```
public abstract byte getZoomContent()
```


Určuje, zda by měl být obsah zvětšen. Čtení/zápis [NullableBool](../../com.aspose.slides/nullablebool).

**Vrací:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public abstract void setZoomContent(byte value)
```


Určuje, zda by měl být obsah zvětšen. Čtení/zápis [NullableBool](../../com.aspose.slides/nullablebool).

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public abstract PointF getFrom()
```


Určuje souřadnici x/y, odkud se má animace spustit (v procentech). Čtení/zápis android.graphics.PointF.

**Vrací:**
android.graphics.PointF
### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public abstract void setFrom(PointF value)
```


Určuje souřadnici x/y, odkud se má animace spustit (v procentech). Čtení/zápis android.graphics.PointF.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public abstract PointF getTo()
```


Určuje cílovou polohu pro efekt měřítka animace (v procentech). Čtení/zápis android.graphics.PointF.

**Vrací:**
android.graphics.PointF
### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public abstract void setTo(PointF value)
```


Určuje cílovou polohu pro efekt měřítka animace (v procentech). Čtení/zápis android.graphics.PointF.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public abstract PointF getBy()
```


Popisuje relativní hodnotu posunu pro animaci (v procentech). Čtení/zápis android.graphics.PointF.

**Vrací:**
android.graphics.PointF
### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public abstract void setBy(PointF value)
```


Popisuje relativní hodnotu posunu pro animaci (v procentech). Čtení/zápis android.graphics.PointF.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | android.graphics.PointF |  |