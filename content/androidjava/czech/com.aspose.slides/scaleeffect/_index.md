---
title: ScaleEffect
second_title: Aspose.Slides pro Android přes Java API Reference
description: Reprezentuje efekt měřítka animace.
type: docs
url: /cs/com.aspose.slides/scaleeffect/
---
**Dědičnost:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**Všechny implementované rozhraní:**
[com.aspose.slides.IScaleEffect](../../com.aspose.slides/iscaleeffect)
```
public class ScaleEffect extends Behavior implements IScaleEffect
```

Reprezentuje efekt měřítka animace.
## Konstruktory

| Konstruktor | Popis |
| --- | --- |
| [ScaleEffect()](#ScaleEffect--) |  |
## Metody

| Metoda | Popis |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Určuje, zda by měl být obsah zvětšen. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Určuje, zda by měl být obsah zvětšen. |
| [getFrom()](#getFrom--) | Určuje souřadnici x/y, odkud má animace začít (v procentech). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | Určuje souřadnici x/y, odkud má animace začít (v procentech). |
| [getTo()](#getTo--) | Určuje cílovou polohu pro efekt měřítka animace (v procentech). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | Určuje cílovou polohu pro efekt měřítka animace (v procentech). |
| [getBy()](#getBy--) | Popisuje relativní hodnotu offsetu pro animaci (v procentech). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | Popisuje relativní hodnotu offsetu pro animaci (v procentech). |
### ScaleEffect() {#ScaleEffect--}
```
public ScaleEffect()
```

### getZoomContent() {#getZoomContent--}
```
public final byte getZoomContent()
```

Určuje, zda by měl být obsah zvětšen. Číst/zapisovat [NullableBool](../../com.aspose.slides/nullablebool).

**Vrací:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public final void setZoomContent(byte value)
```

Určuje, zda by měl být obsah zvětšen. Číst/zapisovat [NullableBool](../../com.aspose.slides/nullablebool).

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public final PointF getFrom()
```

Určuje souřadnici x/y, odkud má animace začít (v procentech). Číst/zapisovat android.graphics.PointF.

**Vrací:**
android.graphics.PointF
### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public final void setFrom(PointF value)
```

Určuje souřadnici x/y, odkud má animace začít (v procentech). Číst/zapisovat android.graphics.PointF.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public final PointF getTo()
```

Určuje cílovou polohu pro efekt měřítka animace (v procentech). Číst/zapisovat android.graphics.PointF.

**Vrací:**
android.graphics.PointF
### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public final void setTo(PointF value)
```

Určuje cílovou polohu pro efekt měřítka animace (v procentech). Číst/zapisovat android.graphics.PointF.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public final PointF getBy()
```

Popisuje relativní hodnotu offsetu pro animaci (v procentech). Číst/zapisovat android.graphics.PointF.

**Vrací:**
android.graphics.PointF
### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public final void setBy(PointF value)
```

Popisuje relativní hodnotu offsetu pro animaci (v procentech). Číst/zapisovat android.graphics.PointF.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | android.graphics.PointF |  |