---
title: INormalViewRestoredProperties
second_title: Aspose.Slides pro Android přes Java API Reference
description: Určuje rozměry oblasti snímku ((šířka, pokud je dítětem restoredTop, výška, pokud je dítětem restoredLeft) normálního zobrazení, když je oblast o proměnných obnovitelných rozměrech (ne minimalizovaná ani maximalizovaná)).
type: docs
url: /cs/com.aspose.slides/inormalviewrestoredproperties/
---```
public interface INormalViewRestoredProperties
```

Určuje rozměry oblasti snímku ((šířka, pokud je dítětem restoredTop, výška, pokud je dítětem restoredLeft) normálního zobrazení, když je oblast o proměnných obnovitelných rozměrech (ne minimalizovaná ani maximalizovaná)).
## Metody

| Metoda | Popis |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | Určuje velikost oblasti snímku (šířka, pokud je dítětem RestoredTop, výška, pokud je dítětem RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | Určuje velikost oblasti snímku (šířka, pokud je dítětem RestoredTop, výška, pokud je dítětem RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | Určuje, zda má velikost oblasti postranního obsahu kompenzovat novou velikost při změně velikosti okna obsahujícího zobrazení v aplikaci. Číst/Zapisovat boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | Určuje, zda má velikost oblasti postranního obsahu kompenzovat novou velikost při změně velikosti okna obsahujícího zobrazení v aplikaci. Číst/Zapisovat boolean. |
### getDimensionSize() {#getDimensionSize--}
```
public abstract float getDimensionSize()
```

Určuje velikost oblasti snímku (šířka, pokud je dítětem RestoredTop, výška, pokud je dítětem RestoredLeft). Číst/Zapisovat float.

**Vrací:**
float
### setDimensionSize(float value) {#setDimensionSize-float-}
```
public abstract void setDimensionSize(float value)
```

Určuje velikost oblasti snímku (šířka, pokud je dítětem RestoredTop, výška, pokud je dítětem RestoredLeft). Číst/Zapisovat float.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public abstract boolean getAutoAdjust()
```

Určuje, zda má velikost oblasti postranního obsahu kompenzovat novou velikost při změně velikosti okna obsahujícího zobrazení v aplikaci. Číst/Zapisovat boolean.

**Vrací:**
boolean
### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public abstract void setAutoAdjust(boolean value)
```

Určuje, zda má velikost oblasti postranního obsahu kompenzovat novou velikost při změně velikosti okna obsahujícího zobrazení v aplikaci. Číst/Zapisovat boolean.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |