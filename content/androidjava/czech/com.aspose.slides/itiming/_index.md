---
title: ITiming
second_title: Aspose.Slides for Android via Java API Reference
description: Represents animation timing.
type: docs
url: /cs/com.aspose.slides/itiming/
---```
public interface ITiming
```

Představuje načasování animace.
## Metody

| Metoda | Popis |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | Popisuje procento trvání zrychlujícího chování efektu. |
| [setAccelerate(float value)](#setAccelerate-float-) | Popisuje procento trvání zrychlujícího chování efektu. |
| [getDecelerate()](#getDecelerate--) | Popisuje procento trvání zpomalujícího chování efektu. |
| [setDecelerate(float value)](#setDecelerate-float-) | Popisuje procento trvání zpomalujícího chování efektu. |
| [getAutoReverse()](#getAutoReverse--) | Popisuje, zda se má automaticky přehrát animace v opačném směru po přehrání vpřed. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | Popisuje, zda se má automaticky přehrát animace v opačném směru po přehrání vpřed. |
| [getDuration()](#getDuration--) | Popisuje dobu trvání animačního efektu. |
| [setDuration(float value)](#setDuration-float-) | Popisuje dobu trvání animačního efektu. |
| [getRepeatCount()](#getRepeatCount--) | Popisuje počet opakování efektu. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | Popisuje počet opakování efektu. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | Tento atribut určuje, zda se efekt bude opakovat až do konce snímku. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | Tento atribut určuje, zda se efekt bude opakovat až do konce snímku. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | Tento atribut určuje, zda se efekt bude opakovat až do dalšího kliknutí. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | Tento atribut určuje, zda se efekt bude opakovat až do dalšího kliknutí. |
| [getRepeatDuration()](#getRepeatDuration--) | Popisuje počet opakování efektu. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | Popisuje počet opakování efektu. |
| [getRestart()](#getRestart--) | Určuje, zda se má efekt po dokončení restartovat. |
| [setRestart(int value)](#setRestart-int-) | Určuje, zda se má efekt po dokončení restartovat. |
| [getSpeed()](#getSpeed--) | Určuje procento, o které se má načasování zrychlit (nebo zpomalit). |
| [setSpeed(float value)](#setSpeed-float-) | Určuje procento, o které se má načasování zrychlit (nebo zpomalit). |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | Popisuje dobu zpoždění po spoušti. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | Popisuje dobu zpoždění po spoušti. |
| [getTriggerType()](#getTriggerType--) | Popisuje typ spouštěče. |
| [setTriggerType(int value)](#setTriggerType-int-) | Popisuje typ spouštěče. |
| [getRewind()](#getRewind--) | Tento atribut určuje, zda se efekt po přehrání přetočí zpět. |
| [setRewind(boolean value)](#setRewind-boolean-) | Tento atribut určuje, zda se efekt po přehrání přetočí zpět. |
### getAccelerate() {#getAccelerate--}
```
public abstract float getAccelerate()
```


Popisuje procento trvání zrychlujícího chování efektu. Čtení/Zápis float.

**Vrací:**
float
### setAccelerate(float value) {#setAccelerate-float-}
```
public abstract void setAccelerate(float value)
```


Popisuje procento trvání zrychlujícího chování efektu. Čtení/Zápis float.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public abstract float getDecelerate()
```


Popisuje procento trvání zpomalujícího chování efektu. Čtení/Zápis float.

**Vrací:**
float
### setDecelerate(float value) {#setDecelerate-float-}
```
public abstract void setDecelerate(float value)
```


Popisuje procento trvání zpomalujícího chování efektu. Čtení/Zápis float.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public abstract boolean getAutoReverse()
```


Popisuje, zda se má automaticky přehrát animace v opačném směru po přehrání vpřed. Čtení/Zápis boolean.

**Vrací:**
boolean
### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public abstract void setAutoReverse(boolean value)
```


Popisuje, zda se má automaticky přehrát animace v opačném směru po přehrání vpřed. Čtení/Zápis boolean.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public abstract float getDuration()
```


Popisuje dobu trvání animačního efektu. Čtení/Zápis float.

**Vrací:**
float
### setDuration(float value) {#setDuration-float-}
```
public abstract void setDuration(float value)
```


Popisuje dobu trvání animačního efektu. Čtení/Zápis float.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public abstract float getRepeatCount()
```


Popisuje počet opakování efektu. Čtení/Zápis float.

**Vrací:**
float
### setRepeatCount(float value) {#setRepeatCount-float-}
```
public abstract void setRepeatCount(float value)
```


Popisuje počet opakování efektu. Čtení/Zápis float.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public abstract boolean getRepeatUntilEndSlide()
```


Tento atribut určuje, zda se efekt bude opakovat až do konce snímku. Čtení/Zápis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Získat sekvenci efektů pro první snímek
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Získat první efekt hlavní sekvence.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Změnit načasování/opakování efektu na "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Vrací:**
boolean
### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public abstract void setRepeatUntilEndSlide(boolean value)
```


Tento atribut určuje, zda se efekt bude opakovat až do konce snímku. Čtení/Zápis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Získat sekvenci efektů pro první snímek
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Získat první efekt hlavní sekvence.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Změnit načasování/opakování efektu na "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public abstract boolean getRepeatUntilNextClick()
```


Tento atribut určuje, zda se efekt bude opakovat až do dalšího kliknutí. Čtení/Zápis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Získat sekvenci efektů pro první snímek
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Získat první efekt hlavní sekvence.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Změnit načasování/opakování efektu na "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Vrací:**
boolean
### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public abstract void setRepeatUntilNextClick(boolean value)
```


Tento atribut určuje, zda se efekt bude opakovat až do dalšího kliknutí. Čtení/Zápis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Získat sekvenci efektů pro první snímek
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Získat první efekt hlavní sekvence.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Změnit načasování/opakování efektu na "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public abstract float getRepeatDuration()
```


Popisuje počet opakování efektu. Čtení/Zápis float.

**Vrací:**
float
### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public abstract void setRepeatDuration(float value)
```


Popisuje počet opakování efektu. Čtení/Zápis float.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public abstract int getRestart()
```


Určuje, zda se má efekt po dokončení restartovat. Čtení/Zápis [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Vrací:**
int
### setRestart(int value) {#setRestart-int-}
```
public abstract void setRestart(int value)
```


Určuje, zda se má efekt po dokončení restartovat. Čtení/Zápis [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | int |  |

### getSpeed() {#getSpeed--}
```
public abstract float getSpeed()
```


Určuje procento, o které se má načasování zrychlit (nebo zpomalit). Čtení/Zápis float.

**Vrací:**
float
### setSpeed(float value) {#setSpeed-float-}
```
public abstract void setSpeed(float value)
```


Určuje procento, o které se má načasování zrychlit (nebo zpomalit). Čtení/Zápis float.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public abstract float getTriggerDelayTime()
```


Popisuje dobu zpoždění po spoušti. Čtení/Zápis float.

**Vrací:**
float
### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public abstract void setTriggerDelayTime(float value)
```


Popisuje dobu zpoždění po spoušti. Čtení/Zápis float.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public abstract int getTriggerType()
```


Popisuje typ spouštěče. Čtení/Zápis [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Vrací:**
int
### setTriggerType(int value) {#setTriggerType-int-}
```
public abstract void setTriggerType(int value)
```


Popisuje typ spouštěče. Čtení/Zápis [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public abstract boolean getRewind()
```


Tento atribut určuje, zda se efekt po přehrání přetočí zpět. Čtení/Zápis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Získat sekvenci efektů pro první snímek
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Získat první efekt hlavní sekvence.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Zapnout načasování/rewind efektu.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Vrací:**
boolean
### setRewind(boolean value) {#setRewind-boolean-}
```
public abstract void setRewind(boolean value)
```


Tento atribut určuje, zda se efekt po přehrání přetočí zpět. Čtení/Zápis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Získat sekvenci efektů pro první snímek
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Získat první efekt hlavní sekvence.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Zapnout načasování/rewind efektu.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |