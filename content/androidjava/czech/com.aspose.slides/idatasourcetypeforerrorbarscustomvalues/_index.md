---
title: IDataSourceTypeForErrorBarsCustomValues
second_title: Aspose.Slides for Android via Java API Reference
description: Určuje typy hodnot v seznamu vlastností ChartDataPoint.ErrorBarsCustomValues
type: docs
url: /cs/com.aspose.slides/idatasourcetypeforerrorbarscustomvalues/
---```
public interface IDataSourceTypeForErrorBarsCustomValues
```

Určuje typy hodnot v seznamu vlastností ChartDataPoint.ErrorBarsCustomValues
## Metody

| Metoda | Popis |
| --- | --- |
| [getDataSourceTypeForXMinusValues()](#getDataSourceTypeForXMinusValues--) | Určuje, zda je vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble v objektu vlastnosti XMinus datových bodů pro vlastní hodnoty chybových úseček aktuální. |
| [setDataSourceTypeForXMinusValues(int value)](#setDataSourceTypeForXMinusValues-int-) | Určuje, zda je vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble v objektu vlastnosti XMinus datových bodů pro vlastní hodnoty chybových úseček aktuální. |
| [getDataSourceTypeForXPlusValues()](#getDataSourceTypeForXPlusValues--) | Určuje, zda je vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble v objektu vlastnosti XPlus datových bodů pro vlastní hodnoty chybových úseček aktuální. |
| [setDataSourceTypeForXPlusValues(int value)](#setDataSourceTypeForXPlusValues-int-) | Určuje, zda je vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble v objektu vlastnosti XPlus datových bodů pro vlastní hodnoty chybových úseček aktuální. |
| [getDataSourceTypeForYMinusValues()](#getDataSourceTypeForYMinusValues--) | Určuje, zda je vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble v objektu vlastnosti YMinus datových bodů pro vlastní hodnoty chybových úseček aktuální. |
| [setDataSourceTypeForYMinusValues(int value)](#setDataSourceTypeForYMinusValues-int-) | Určuje, zda je vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble v objektu vlastnosti YMinus datových bodů pro vlastní hodnoty chybových úseček aktuální. |
| [getDataSourceTypeForYPlusValues()](#getDataSourceTypeForYPlusValues--) | Určuje, zda je vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble v objektu vlastnosti YPlus datových bodů pro vlastní hodnoty chybových úseček aktuální. |
| [setDataSourceTypeForYPlusValues(int value)](#setDataSourceTypeForYPlusValues-int-) | Určuje, zda je vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble v objektu vlastnosti YPlus datových bodů pro vlastní hodnoty chybových úseček aktuální. |
### getDataSourceTypeForXMinusValues() {#getDataSourceTypeForXMinusValues--}
```
public abstract int getDataSourceTypeForXMinusValues()
```

Určuje, zda je vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble v objektu vlastnosti XMinus datových bodů pro vlastní hodnoty chybových úseček aktuální. Jinak řečeno určuje typ hodnoty vlastnosti ChartDataPoint.ErrorBarsCustomValues.XMinus.Data. Čtení/zápis [DataSourceType](../../com.aspose.slides/datasourcetype).

**Vrací:**
int
### setDataSourceTypeForXMinusValues(int value) {#setDataSourceTypeForXMinusValues-int-}
```
public abstract void setDataSourceTypeForXMinusValues(int value)
```

Určuje, zda je vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble v objektu vlastnosti XMinus datových bodů pro vlastní hodnoty chybových úseček aktuální. Jinak řečeno určuje typ hodnoty vlastnosti ChartDataPoint.ErrorBarsCustomValues.XMinus.Data. Čtení/zápis [DataSourceType](../../com.aspose.slides/datasourcetype).

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | int |  |

### getDataSourceTypeForXPlusValues() {#getDataSourceTypeForXPlusValues--}
```
public abstract int getDataSourceTypeForXPlusValues()
```

Určuje, zda je vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble v objektu vlastnosti XPlus datových bodů pro vlastní hodnoty chybových úseček aktuální. Jinak řečeno určuje typ hodnoty vlastnosti ChartDataPoint.ErrorBarsCustomValues.XPlus.Data. Čtení/zápis [DataSourceType](../../com.aspose.slides/datasourcetype).

**Vrací:**
int
### setDataSourceTypeForXPlusValues(int value) {#setDataSourceTypeForXPlusValues-int-}
```
public abstract void setDataSourceTypeForXPlusValues(int value)
```

Určuje, zda je vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble v objektu vlastnosti XPlus datových bodů pro vlastní hodnoty chybových úseček aktuální. Jinak řečeno určuje typ hodnoty vlastnosti ChartDataPoint.ErrorBarsCustomValues.XPlus.Data. Čtení/zápis [DataSourceType](../../com.aspose.slides/datasourcetype).

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | int |  |

### getDataSourceTypeForYMinusValues() {#getDataSourceTypeForYMinusValues--}
```
public abstract int getDataSourceTypeForYMinusValues()
```

Určuje, zda je vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble v objektu vlastnosti YMinus datových bodů pro vlastní hodnoty chybových úseček aktuální. Jinak řečeno určuje typ hodnoty vlastnosti ChartDataPointEx.ErrorBarsCustomValues.YMinus.Data. Čtení/zápis [DataSourceType](../../com.aspose.slides/datasourcetype).

**Vrací:**
int
### setDataSourceTypeForYMinusValues(int value) {#setDataSourceTypeForYMinusValues-int-}
```
public abstract void setDataSourceTypeForYMinusValues(int value)
```

Určuje, zda je vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble v objektu vlastnosti YMinus datových bodů pro vlastní hodnoty chybových úseček aktuální. Jinak řečeno určuje typ hodnoty vlastnosti ChartDataPointEx.ErrorBarsCustomValues.YMinus.Data. Čtení/zápis [DataSourceType](../../com.aspose.slides/datasourcetype).

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | int |  |

### getDataSourceTypeForYPlusValues() {#getDataSourceTypeForYPlusValues--}
```
public abstract int getDataSourceTypeForYPlusValues()
```

Určuje, zda je vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble v objektu vlastnosti YPlus datových bodů pro vlastní hodnoty chybových úseček aktuální. Jinak řečeno určuje typ hodnoty vlastnosti ChartDataPointEx.ErrorBarsCustomValues.YPlus.Data. Čtení/zápis [DataSourceType](../../com.aspose.slides/datasourcetype).

**Vrací:**
int
### setDataSourceTypeForYPlusValues(int value) {#setDataSourceTypeForYPlusValues-int-}
```
public abstract void setDataSourceTypeForYPlusValues(int value)
```

Určuje, zda je vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble v objektu vlastnosti YPlus datových bodů pro vlastní hodnoty chybových úseček aktuální. Jinak řečeno určuje typ hodnoty vlastnosti ChartDataPointEx.ErrorBarsCustomValues.YPlus.Data. Čtení/zápis [DataSourceType](../../com.aspose.slides/datasourcetype).

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | int |  |