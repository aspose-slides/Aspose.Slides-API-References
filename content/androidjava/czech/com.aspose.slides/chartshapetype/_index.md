---
title: ChartShapeType
second_title: Aspose.Slides pro Android přes Java API Reference
description: Reprezentuje tvar grafu.
type: docs
url: /cs/com.aspose.slides/chartshapetype/
---
**Dědičnost:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class ChartShapeType extends System.Enum
```

Reprezentuje tvar grafu.
## Pole

| Pole | Popis |
| --- | --- |
| [NotDefined](#NotDefined) | Tvar grafu není definován. |
| [Box](#Box) | Určuje, že graf bude vykreslen v podobě krabice. |
| [Cone](#Cone) | Určuje, že graf bude vykreslen jako kužel, přičemž základna kužele leží na podlaze a špička kužele je na vrcholu datového markeru. |
| [ConeToMax](#ConeToMax) | Určuje, že graf bude vykreslen zkrácenými kužely tak, že špička kužele bude představovat maximální hodnotu dat. |
| [Cylinder](#Cylinder) | Určuje, že graf bude vykreslen jako válec. |
| [Pyramid](#Pyramid) | Určuje, že graf bude vykreslen jako pravoúhlá pyramida, přičemž základna pyramidy leží na podlaze a špička pyramidy je na vrcholu datového markeru. |
| [PyramidToMaximum](#PyramidToMaximum) | Určuje, že graf bude vykreslen zkrácenými kužely tak, že špička kužele bude představovat maximální hodnotu dat. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```

Tvar grafu není definován.

### Box {#Box}
```
public static final int Box
```

Určuje, že graf bude vykreslen v podobě krabice.

### Cone {#Cone}
```
public static final int Cone
```

Určuje, že graf bude vykreslen jako kužel, přičemž základna kužele leží na podlaze a špička kužele je na vrcholu datového markeru.

### ConeToMax {#ConeToMax}
```
public static final int ConeToMax
```

Určuje, že graf bude vykreslen zkrácenými kužely tak, že špička kužele bude představovat maximální hodnotu dat.

### Cylinder {#Cylinder}
```
public static final int Cylinder
```

Určuje, že graf bude vykreslen jako válec.

### Pyramid {#Pyramid}
```
public static final int Pyramid
```

Určuje, že graf bude vykreslen jako pravoúhlá pyramida, přičemž základna pyramidy leží na podlaze a špička pyramidy je na vrcholu datového markeru.

### PyramidToMaximum {#PyramidToMaximum}
```
public static final int PyramidToMaximum
```

Určuje, že graf bude vykreslen zkrácenými kužely tak, že špička kužele bude představovat maximální hodnotu dat.