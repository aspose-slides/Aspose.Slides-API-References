---
title: IHSLEffectiveData
second_title: Aspose.Slides สำหรับ Android ผ่านการอ้างอิง Java API
description: แสดงผลกระทบของ Hue/Saturation/Luminance.
type: docs
url: /th/com.aspose.slides/ihsleffectivedata/
---
**ส่วนต่อประสานที่ดำเนินการทั้งหมด:**
[com.aspose.slides.IEffectEffectiveData](../../com.aspose.slides/ieffecteffectivedata)
```
public interface IHSLEffectiveData extends IEffectEffectiveData
```

แทนผลกระทบของ Hue/Saturation/Luminance. ค่า hue, saturation, และ luminance สามารถปรับได้โดยอิงจากค่าปัจจุบันของแต่ละค่า.
## เมธอด

| เมธอด | คำอธิบาย |
| --- | --- |
| [getHue()](#getHue--) | ส่งคืนค่าเปอร์เซ็นต์ของ hue. |
| [getSaturation()](#getSaturation--) | ส่งคืนค่าเปอร์เซ็นต์ของ saturation. |
| [getLuminance()](#getLuminance--) | ส่งคืนค่าเปอร์เซ็นต์ของ luminance. |
### getHue() {#getHue--}
```
public abstract float getHue()
```

ส่งคืนค่าเปอร์เซ็นต์ของ hue. float แบบอ่านอย่างเดียว.

**ส่งคืน:**
float
### getSaturation() {#getSaturation--}
```
public abstract float getSaturation()
```

ส่งคืนค่าเปอร์เซ็นต์ของ saturation. float แบบอ่านอย่างเดียว.

**ส่งคืน:**
float
### getLuminance() {#getLuminance--}
```
public abstract float getLuminance()
```

ส่งคืนค่าเปอร์เซ็นต์ของ luminance. float แบบอ่านอย่างเดียว.

**ส่งคืน:**
float