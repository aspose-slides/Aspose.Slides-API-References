---
title: FieldType
second_title: Aspose.Slides สำหรับ Android ผ่านการอ้างอิง API Java
description: เป็นตัวแทนของประเภทของฟิลด์.
type: docs
url: /th/com.aspose.slides/fieldtype/
---
**การสืบทอด:**
java.lang.Object

**อินเทอร์เฟซที่ทำการใช้งานทั้งหมด:**
[com.aspose.slides.IFieldType](../../com.aspose.slides/ifieldtype)
```
public final class FieldType implements IFieldType
```

Represents a type of field. This value determines which text will be set to the field portion when it will be updated.

## ตัวสร้าง

| คอนสตรัคเตอร์ | คำอธิบาย |
| --- | --- |
| [FieldType(String str)](#FieldType-java.lang.String-) | เริ่มต้นอินสแตนซ์ใหม่ของ class FieldType. |
## เมธอด

| เมธอด | คำอธิบาย |
| --- | --- |
| [getInternalString()](#getInternalString--) | คืนค่าชื่อภายในของอ็อบเจ็กต์ FieldType นี้. |
| [setInternalString(String value)](#setInternalString-java.lang.String-) | คืนค่าชื่อภายในของอ็อบเจ็กต์ FieldType นี้. |
| [equals(Object obj)](#equals-java.lang.Object-) | ตรวจสอบว่าฟิลด์นี้เท่ากับฟิลด์อื่นหรือไม่. |
| [hashCode()](#hashCode--) | คืนค่า hashcode ของอ็อบเจ็กต์นี้. |
| [op_Equality(FieldType a, FieldType b)](#op-Equality-com.aspose.slides.FieldType-com.aspose.slides.FieldType-) | ตรวจสอบว่าอ็อบเจ็กต์ FieldType สองตัวเท่ากันหรือไม่. |
| [op_Inequality(FieldType a, FieldType b)](#op-Inequality-com.aspose.slides.FieldType-com.aspose.slides.FieldType-) | ตรวจสอบว่าอ็อบเจ็กต์ FieldType สองตัวไม่เท่ากันหรือไม่. |
| [getSlideNumber()](#getSlideNumber--) | หมายเลขสไลด์ปัจจุบัน. |
| [getFooter()](#getFooter--) | ส่วนท้ายของสไลด์. |
| [getHeader()](#getHeader--) | ส่วนหัวของสไลด์. |
| [getDateTime()](#getDateTime--) | วันที่และเวลาปัจจุบันในรูปแบบวันที่และเวลามาตรฐานสำหรับแอปพลิเคชันการเรนเดอร์. |
| [getDateTime1()](#getDateTime1--) | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้เป็นแรก (MM/DD/YYYY for english). |
| [getDateTime2()](#getDateTime2--) | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่สอง (Day, Month DD, YYYY for english). |
| [getDateTime3()](#getDateTime3--) | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่สาม (DD Month YYYY for english). |
| [getDateTime4()](#getDateTime4--) | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่สี่ (Month DD, YYYY for english). |
| [getDateTime5()](#getDateTime5--) | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่ห้า (DD-Mon-YY for english). |
| [getDateTime6()](#getDateTime6--) | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่หก (Month YY for english). |
| [getDateTime7()](#getDateTime7--) | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่เจ็ด (Mon-YY for english). |
| [getDateTime8()](#getDateTime8--) | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่แปด (MM/DD/YYYY hh:mm AM/PM for english). |
| [getDateTime9()](#getDateTime9--) | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่เก้า (MM/DD/YYYY hh:mm:ss AM/PM for english). |
| [getDateTime10()](#getDateTime10--) | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่สิบ (hh:mm for english). |
| [getDateTime11()](#getDateTime11--) | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่สิบเอ็ด (hh:mm:ss for english). |
| [getDateTime12()](#getDateTime12--) | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่สิบสอง (hh:mm AM/PM for english). |
| [getDateTime13()](#getDateTime13--) | วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่สิบสาม (hh:mm:ss AM/PM for english). |
### FieldType(String str) {#FieldType-java.lang.String-}
```
public FieldType(String str)
```

เริ่มต้นอินสแตนซ์ใหม่ของ class FieldType.

**พารามิเตอร์:**
| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| str | java.lang.String |  |
### getInternalString() {#getInternalString--}
```
public final String getInternalString()
```

คืนค่าชื่อภายในของอ็อบเจ็กต์ FieldType นี้. อ่าน/เขียน String.

**คืนค่า:**
java.lang.String
### setInternalString(String value) {#setInternalString-java.lang.String-}
```
public final void setInternalString(String value)
```

คืนค่าชื่อภายในของอ็อบเจ็กต์ FieldType นี้. อ่าน/เขียน String.

**พารามิเตอร์:**
| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | java.lang.String |  |
### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```

ตรวจสอบว่าฟิลด์นี้เท่ากับฟิลด์อื่นหรือไม่.

**พารามิเตอร์:**
| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| obj | java.lang.Object | ฟิลด์ที่จะเปรียบเทียบ. |

**คืนค่า:**
boolean - True หากฟิลด์เท่ากัน.
### hashCode() {#hashCode--}
```
public int hashCode()
```

คืนค่า hashcode ของอ็อบเจ็กต์นี้.

**คืนค่า:**
int - Hashcode int.
### op_Equality(FieldType a, FieldType b) {#op-Equality-com.aspose.slides.FieldType-com.aspose.slides.FieldType-}
```
public static boolean op_Equality(FieldType a, FieldType b)
```

ตรวจสอบว่าอ็อบเจ็กต์ FieldType สองตัวเท่ากันหรือไม่.

**พารามิเตอร์:**
| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| a | [FieldType](../../com.aspose.slides/fieldtype) | FieldType แรกที่จะเปรียบเทียบ. |
| b | [FieldType](../../com.aspose.slides/fieldtype) | FieldType ที่สองที่จะเปรียบเทียบ. |

**คืนค่า:**
boolean - True หากอ็อบเจ็กต์ FieldType เท่ากัน.
### op_Inequality(FieldType a, FieldType b) {#op-Inequality-com.aspose.slides.FieldType-com.aspose.slides.FieldType-}
```
public static boolean op_Inequality(FieldType a, FieldType b)
```

ตรวจสอบว่าอ็อบเจ็กต์ FieldType สองตัวไม่เท่ากันหรือไม่.

**พารามิเตอร์:**
| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| a | [FieldType](../../com.aspose.slides/fieldtype) | FieldType แรกที่จะเปรียบเทียบ. |
| b | [FieldType](../../com.aspose.slides/fieldtype) | FieldType ที่สองที่จะเปรียบเทียบ. |

**คืนค่า:**
boolean - True หากอ็อบเจ็กต์ FieldType ไม่เท่ากัน.
### getSlideNumber() {#getSlideNumber--}
```
public static FieldType getSlideNumber()
```

หมายเลขสไลด์ปัจจุบัน. อ่านอย่างเดียว [FieldType](../../com.aspose.slides/fieldtype).

**คืนค่า:**
[FieldType](../../com.aspose.slides/fieldtype)
### getFooter() {#getFooter--}
```
public static FieldType getFooter()
```

ส่วนท้ายของสไลด์. อ่านอย่างเดียว [FieldType](../../com.aspose.slides/fieldtype).

**คืนค่า:**
[FieldType](../../com.aspose.slides/fieldtype)
### getHeader() {#getHeader--}
```
public static FieldType getHeader()
```

ส่วนหัวของสไลด์. อ่านอย่างเดียว [FieldType](../../com.aspose.slides/fieldtype).

**คืนค่า:**
[FieldType](../../com.aspose.slides/fieldtype)
### getDateTime() {#getDateTime--}
```
public static FieldType getDateTime()
```

วันที่และเวลาปัจจุบันในรูปแบบวันที่และเวลามาตรฐานสำหรับแอปพลิเคชันการเรนเดอร์. อ่านอย่างเดียว [FieldType](../../com.aspose.slides/fieldtype).

**คืนค่า:**
[FieldType](../../com.aspose.slides/fieldtype)
### getDateTime1() {#getDateTime1--}
```
public static FieldType getDateTime1()
```

วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้เป็นแรก (MM/DD/YYYY for english). อ่านอย่างเดียว [FieldType](../../com.aspose.slides/fieldtype).

**คืนค่า:**
[FieldType](../../com.aspose.slides/fieldtype)
### getDateTime2() {#getDateTime2--}
```
public static FieldType getDateTime2()
```

วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่สอง (Day, Month DD, YYYY for english). อ่านอย่างเดียว [FieldType](../../com.aspose.slides/fieldtype).

**คืนค่า:**
[FieldType](../../com.aspose.slides/fieldtype)
### getDateTime3() {#getDateTime3--}
```
public static FieldType getDateTime3()
```

วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่สาม (DD Month YYYY for english). อ่านอย่างเดียว [FieldType](../../com.aspose.slides/fieldtype).

**คืนค่า:**
[FieldType](../../com.aspose.slides/fieldtype)
### getDateTime4() {#getDateTime4--}
```
public static FieldType getDateTime4()
```

วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่สี่ (Month DD, YYYY for english). อ่านอย่างเดียว [FieldType](../../com.aspose.slides/fieldtype).

**คืนค่า:**
[FieldType](../../com.aspose.slides/fieldtype)
### getDateTime5() {#getDateTime5--}
```
public static FieldType getDateTime5()
```

วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่ห้า (DD-Mon-YY for english). อ่านอย่างเดียว [FieldType](../../com.aspose.slides/fieldtype).

**คืนค่า:**
[FieldType](../../com.aspose.slides/fieldtype)
### getDateTime6() {#getDateTime6--}
```
public static FieldType getDateTime6()
```

วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่หก (Month YY for english). อ่านอย่างเดียว [FieldType](../../com.aspose.slides/fieldtype).

**คืนค่า:**
[FieldType](../../com.aspose.slides/fieldtype)
### getDateTime7() {#getDateTime7--}
```
public static FieldType getDateTime7()
```

วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่เจ็ด (Mon-YY for english). อ่านอย่างเดียว [FieldType](../../com.aspose.slides/fieldtype).

**คืนค่า:**
[FieldType](../../com.aspose.slides/fieldtype)
### getDateTime8() {#getDateTime8--}
```
public static FieldType getDateTime8()
```

วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่แปด (MM/DD/YYYY hh:mm AM/PM for english). อ่านอย่างเดียว [FieldType](../../com.aspose.slides/fieldtype).

**คืนค่า:**
[FieldType](../../com.aspose.slides/fieldtype)
### getDateTime9() {#getDateTime9--}
```
public static FieldName getDateTime9()
```

วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่เก้า (MM/DD/YYYY hh:mm:ss AM/PM for english). อ่านอย่างเดียว [FieldType](../../com.aspose.slides/fieldtype).

**คืนค่า:**
[FieldType](../../com.aspose.slides/fieldtype)
### getDateTime10() {#getDateTime10--}
```
public static FieldType getDateTime10()
```

วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่สิบ (hh:mm for english). อ่านอย่างเดียว [FieldType](../../com.aspose.slides/fieldtype).

**คืนค่า:**
[FieldType](../../com.aspose.slides/fieldtype)
### getDateTime11() {#getDateTime11--}
```
public static FieldType getDateTime11()
```

วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่สิบเอ็ด (hh:mm:ss for english). อ่านอย่างเดียว [FieldType](../../com.aspose.slides/fieldtype).

**คืนค่า:**
[FieldType](../../com.aspose.slides/fieldtype)
### getDateTime12() {#getDateTime12--}
```
public static FieldType getDateTime12()
```

วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่สิบสอง (hh:mm AM/PM for english). อ่านอย่างเดียว [FieldType](../../com.aspose.slides/fieldtype).

**คืนค่า:**
[FieldType](../../com.aspose.slides/fieldtype)
### getDateTime13() {#getDateTime13--}
```
public static FieldType getDateTime13()
```

วันที่และเวลาปัจจุบันในรูปแบบที่กำหนดไว้ที่สิบสาม (hh:mm:ss AM/PM for english). อ่านอย่างเดียว [FieldType](../../com.aspose.slides/fieldtype).

**คืนค่า:**
[FieldType](../../com.aspose.slides/fieldtype)