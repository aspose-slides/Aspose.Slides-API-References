---
title: GradientStopEffectiveData
second_title: Aspose.Slides for Android ผ่านการอ้างอิง Java API
description: อ็อบเจ็กต์ที่ไม่เปลี่ยนแปลงซึ่งเป็นตัวแทนของจุดหยุดไล่ระดับสี.
type: docs
url: /th/com.aspose.slides/gradientstopeffectivedata/
---
**การสืบทอด:**
java.lang.Object

**อินเทอร์เฟซที่ Implement ทั้งหมด:**
com.aspose.slides.IEffectiveData, [com.aspose.slides.IGradientStopEffectiveData](../../com.aspose.slides/igradientstopeffectivedata)
```
public class GradientStopEffectiveData implements IEffectiveData, IGradientStopEffectiveData
```

อ็อบเจ็กต์ไม่เปลี่ยนแปลงที่เป็นตัวแทนของจุดหยุดไล่ระดับสี.
## เมธอด

| เมธอด | คำอธิบาย |
| --- | --- |
| [getPosition()](#getPosition--) | คืนค่าตำแหน่ง (0..1) ของจุดหยุดไล่ระดับสี. |
| [getColor()](#getColor--) | คืนค่าสีของจุดหยุดไล่ระดับสี. |
| [equals(Object obj)](#equals-java.lang.Object-) | กำหนดว่าข้อมูล [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) ที่ระบุเทียบกับ [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) ปัจจุบันเท่ากันหรือไม่. |
| [hashCode()](#hashCode--) |  |
### getPosition() {#getPosition--}
```
public final float getPosition()
```


คืนค่าตำแหน่ง (0..1) ของจุดหยุดไล่ระดับสี. อ่านอย่างเดียว float.

**คืนค่า:**
float
### getColor() {#getColor--}
```
public final Integer getColor()
```


คืนค่าสีของจุดหยุดไล่ระดับสี. อ่านอย่างเดียว java.lang.Integer.

**คืนค่า:**
java.lang.Integer
### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```


กำหนดว่าข้อมูล [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) ที่ระบุเทียบกับ [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) ปัจจุบันเท่ากันหรือไม่.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| obj | java.lang.Object | [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) ที่จะเปรียบเทียบ. |

**คืนค่า:**
boolean - true หากวัตถุเท่ากัน; มิฉะนั้น false.
### hashCode() {#hashCode--}
```
public int hashCode()
```




**คืนค่า:**
int