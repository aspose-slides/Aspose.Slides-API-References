---
title: IDataTable
second_title: Aspose.Slides สำหรับ Android ผ่านอ้างอิง API ของ Java
description: แสดงคุณสมบัติของตารางข้อมูล.
type: docs
url: /th/com.aspose.slides/idatatable/
---
**อินเทอร์เฟซที่นำไปใช้ทั้งหมด:**
[com.aspose.slides.IFormattedTextContainer](../../com.aspose.slides/iformattedtextcontainer)
```
public interface IDataTable extends IFormattedTextContainer
```

แสดงคุณสมบัติของตารางข้อมูล.
## เมธอด

| เมธอด | คำอธิบาย |
| --- | --- |
| [getFormat()](#getFormat--) | ส่งคืนสไตล์ของเส้น, การเติมและเอฟเฟกต์ของอ็อบเจ็กต์. |
| [hasBorderHorizontal()](#hasBorderHorizontal--) | เป็นจริงถ้าตารางข้อมูลแผนภูมิมีเส้นขอบเซลล์แนวนอน. |
| [setBorderHorizontal(boolean value)](#setBorderHorizontal-boolean-) | เป็นจริงถ้าตารางข้อมูลแผนภูมิมีเส้นขอบเซลล์แนวนอน. |
| [hasBorderOutline()](#hasBorderOutline--) | เป็นจริงถ้าตารางข้อมูลแผนภูมิมีเส้นขอบรอบนอก. |
| [setBorderOutline(boolean value)](#setBorderOutline-boolean-) | เป็นจริงถ้าตารางข้อมูลแผนภูมิมีเส้นขอบรอบนอก. |
| [hasBorderVertical()](#hasBorderVertical--) | เป็นจริงถ้าตารางข้อมูลแผนภูมิมีเส้นขอบเซลล์แนวตั้ง. |
| [setBorderVertical(boolean value)](#setBorderVertical-boolean-) | เป็นจริงถ้าตารางข้อมูลแผนภูมิมีเส้นขอบเซลล์แนวตั้ง. |
| [getShowLegendKey()](#getShowLegendKey--) | เป็นจริงถ้าคีย์คำอธิบายของป้ายข้อมูลมองเห็นได้. |
| [setShowLegendKey(boolean value)](#setShowLegendKey-boolean-) | เป็นจริงถ้าคีย์คำอธิบายของป้ายข้อมูลมองเห็นได้. |
### getFormat() {#getFormat--}
```
public abstract IFormat getFormat()
```


ส่งคืนสไตล์ของเส้น, การเติมและเอฟเฟกต์ของอ็อบเจ็กต์. อ่านได้เท่านั้น [IFormat](../../com.aspose.slides/iformat).

**ส่งคืน:**
[IFormat](../../com.aspose.slides/iformat)
### hasBorderHorizontal() {#hasBorderHorizontal--}
```
public abstract boolean hasBorderHorizontal()
```


เป็นจริงถ้าตารางข้อมูลแผนภูมิมีเส้นขอบเซลล์แนวนอน. อ่าน/เขียน boolean.

**ส่งคืน:**
boolean
### setBorderHorizontal(boolean value) {#setBorderHorizontal-boolean-}
```
public abstract void setBorderHorizontal(boolean value)
```


เป็นจริงถ้าตารางข้อมูลแผนภูมิมีเส้นขอบเซลล์แนวนอน. อ่าน/เขียน boolean.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | boolean |  |
### hasBorderOutline() {#hasBorderOutline--}
```
public abstract boolean hasBorderOutline()
```


เป็นจริงถ้าตารางข้อมูลแผนภูมิมีเส้นขอบรอบนอก. อ่าน/เขียน boolean.

**ส่งคืน:**
boolean
### setBorderOutline(boolean value) {#setBorderOutline-boolean-}
```
public abstract void setBorderOutline(boolean value)
```


เป็นจริงถ้าตารางข้อมูลแผนภูมิมีเส้นขอบรอบนอก. อ่าน/เขียน boolean.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | boolean |  |
### hasBorderVertical() {#hasBorderVertical--}
```
public abstract boolean hasBorderVertical()
```


เป็นจริงถ้าตารางข้อมูลแผนภูมิมีเส้นขอบเซลล์แนวตั้ง. อ่าน/เขียน boolean.

**ส่งคืน:**
boolean
### setBorderVertical(boolean value) {#setBorderVertical-boolean-}
```
public abstract void setBorderVertical(boolean value)
```


เป็นจริงถ้าตารางข้อมูลแผนภูมิมีเส้นขอบเซลล์แนวตั้ง. อ่าน/เขียน boolean.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | boolean |  |
### getShowLegendKey() {#getShowLegendKey--}
```
public abstract boolean getShowLegendKey()
```


เป็นจริงถ้าคีย์คำอธิบายของป้ายข้อมูลมองเห็นได้. อ่าน/เขียน boolean.

**ส่งคืน:**
boolean
### setShowLegendKey(boolean value) {#setShowLegendKey-boolean-}
```
public abstract void setShowLegendKey(boolean value)
```


เป็นจริงถ้าคีย์คำอธิบายของป้ายข้อมูลมองเห็นได้. อ่าน/เขียน boolean.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | boolean |  |