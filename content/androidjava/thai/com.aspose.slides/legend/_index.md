---
title: Legend
second_title: Aspose.Slides สำหรับ Android ผ่าน Java API Reference
description: แสดงคุณสมบัติของ legend ในแผนภูมิ
type: docs
url: /th/com.aspose.slides/legend/
---
**Inheritance:**
java.lang.Object, com.aspose.slides.DomObject

**All Implemented Interfaces:**
[com.aspose.slides.ILegend](../../com.aspose.slides/ilegend)
```
public class Legend extends DomObject<Chart> implements ILegend
```

แสดงคุณสมบัติของ legend ในแผนภูมิ
## เมธอด

| Method | Description |
| --- | --- |
| [getX()](#getX--) | คืนค่า หรือ ตั้งค่าพิกัด x ของ legend เป็นสัดส่วนของความกว้างของแผนภูมิ |
| [setX(float value)](#setX-float-) | คืนค่า หรือ ตั้งค่าพิกัด x ของ legend เป็นสัดส่วนของความกว้างของแผนภูมิ |
| [getY()](#getY--) | คืนค่า หรือ ตั้งค่าพิกัด y ของ legend เป็นสัดส่วนของความสูงของแผนภูมิ |
| [setY(float value)](#setY-float-) | คืนค่า หรือ ตั้งค่าพิกัด y ของ legend เป็นสัดส่วนของความสูงของแผนภูมิ |
| [getWidth()](#getWidth--) | คืนค่า หรือ ตั้งค่าความกว้างของ legend เป็นสัดส่วนของความกว้างของแผนภูมิ |
| [setWidth(float value)](#setWidth-float-) | คืนค่า หรือ ตั้งค่าความกว้างของ legend เป็นสัดส่วนของความกว้างของแผนภูมิ |
| [getHeight()](#getHeight--) | คืนค่า หรือ ตั้งค่าความสูงของ legend เป็นสัดส่วนของความสูงของแผนภูมิ |
| [setHeight(float value)](#setHeight-float-) | คืนค่า หรือ ตั้งค่าความสูงของ legend เป็นสัดส่วนของความสูงของแผนภูมิ |
| [getRight()](#getRight--) | ด้านขวา |
| [getBottom()](#getBottom--) | ด้านล่าง |
| [getOverlay()](#getOverlay--) | กำหนดว่ากราฟิกอื่น ๆ ของแผนภูมิจะอนุญาตให้ทับซ้อน legend หรือไม่ |
| [setOverlay(boolean value)](#setOverlay-boolean-) | กำหนดว่ากราฟิกอื่น ๆ ของแผนภูมิจะอนุญาตให้ทับซ้อน legend หรือไม่ |
| [getTextFormat()](#getTextFormat--) | ฟอร์แมตข้อความ |
| [getPosition()](#getPosition--) | ระบุตำแหน่งของ legend บนแผนภูมิ |
| [setPosition(int value)](#setPosition-int-) | ระบุตำแหน่งของ legend บนแผนภูมิ |
| [getFormat()](#getFormat--) | คืนค่าฟอร์แมตของ legend |
| [getChart()](#getChart--) | คืนค่าแผนภูมิ |
| [getEntries()](#getEntries--) | รับรายการ legend |
| [getActualX()](#getActualX--) | ระบุตำแหน่ง x จริง (ซ้าย) ขององค์ประกอบแผนภูมิสัมพันธ์กับมุมซ้ายบนของแผนภูมิ |
| [getActualY()](#getActualY--) | ระบุตำแหน่งบนจริงขององค์ประกอบแผนภูมิสัมพันธ์กับมุมซ้ายบนของแผนภูมิ |
| [getActualWidth()](#getActualWidth--) | ระบุความกว้างจริงขององค์ประกอบแผนภูมิ |
| [getActualHeight()](#getActualHeight--) | ระบุความสูงจริงขององค์ประกอบแผนภูมิ |
| [getSlide()](#getSlide--) | คืนสไลด์แม่ของ FillFormat |
| [getPresentation()](#getPresentation--) | คืนพรีเซนเทชันแม่ของ FillFormat |
### getX() {#getX--}
```
public final float getX()
```

คืนค่า หรือ ตั้งค่าพิกัด x ของ legend เป็นสัดส่วนของความกว้างของแผนภูมิ อ่าน/เขียน float.

**คืนค่า:**
float
### setX(float value) {#setX-float-}
```
public final void setX(float value)
```

คืนค่า หรือ ตั้งค่าพิกัด x ของ legend เป็นสัดส่วนของความกว้างของแผนภูมิ อ่าน/เขียน float.

**พารามิเตอร์:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getY() {#getY--}
```
public final float getY()
```

คืนค่า หรือ ตั้งค่าพิกัด y ของ legend เป็นสัดส่วนของความสูงของแผนภูมิ อ่าน/เขียน float.

**คืนค่า:**
float
### setY(float value) {#setY-float-}
```
public final void setY(float value)
```

คืนค่า หรือ ตั้งค่าพิกัด y ของ legend เป็นสัดส่วนของความสูงของแผนภูมิ อ่าน/เขียน float.

**พารามิเตอร์:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getWidth() {#getWidth--}
```
public final float getWidth()
```

คืนค่า หรือ ตั้งค่าความกว้างของ legend เป็นสัดส่วนของความกว้างของแผนภูมิ อ่าน/เขียน float.

**คืนค่า:**
float
### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```

คืนค่า หรือ ตั้งค่าความกว้างของ legend เป็นสัดส่วนของความกว้างของแผนภูมิ อ่าน/เขียน float.

**พารามิเตอร์:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getHeight() {#getHeight--}
```
public final float getHeight()
```

คืนค่า หรือ ตั้งค่าความสูงของ legend เป็นสัดส่วนของความสูงของแผนภูมิ อ่าน/เขียน float.

**คืนค่า:**
float
### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```

คืนค่า หรือ ตั้งค่าความสูงของ legend เป็นสัดส่วนของความสูงของแผนภูมิ อ่าน/เขียน float.

**พารามิเตอร์:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getRight() {#getRight--}
```
public final float getRight()
```

ด้านขวา อ่านอย่างเดียว float.

**คืนค่า:**
float
### getBottom() {#getBottom--}
```
public final float getBottom()
```

ด้านล่าง อ่านอย่างเดียว float.

**คืนค่า:**
float
### getOverlay() {#getOverlay--}
```
public final boolean getOverlay()
```

กำหนดว่ากราฟิกอื่น ๆ ของแผนภูมิจะอนุญาตให้ทับซ้อน legend หรือไม่ อ่าน/เขียน boolean.

**คืนค่า:**
boolean
### setOverlay(boolean value) {#setOverlay-boolean-}
```
public final void setOverlay(boolean value)
```

กำหนดว่ากราฟิกอื่น ๆ ของแผนภูมิจะอนุญาตให้ทับซ้อน legend หรือไม่ อ่าน/เขียน boolean.

**พารามิเตอร์:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getTextFormat() {#getTextFormat--}
```
public final IChartTextFormat getTextFormat()
```

ฟอร์แมตข้อความ อ่านอย่างเดียว [IChartTextFormat](../../com.aspose.slides/icharttextformat).

**คืนค่า:**
[IChartTextFormat](../../com.aspose.slides/icharttextformat)
### getPosition() {#getPosition--}
```
public final int getPosition()
```

ระบุตำแหน่งของ legend บนแผนภูมิ ค่าที่ไม่เป็น NaN ของคุณสมบัติ X, Y, Width, Height จะทับผลของคุณสมบัตินี้ อ่าน/เขียน [LegendPositionType](../../com.aspose.slides/legendpositiontype).

**คืนค่า:**
int
### setPosition(int value) {#setPosition-int-}
```
public final void setPosition(int value)
```

ระบุตำแหน่งของ legend บนแผนภูมิ ค่าที่ไม่เป็น NaN ของคุณสมบัติ X, Y, Width, Height จะทับผลของคุณสมบัตินี้ อ่าน/เขียน [LegendPositionType](../../com.aspose.slides/legendpositiontype).

**พารามิเตอร์:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | int |  |

### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

คืนค่าฟอร์แมตของ legend อ่านอย่างเดียว [IFormat](../../com.aspose.slides/iformat).

**คืนค่า:**
[IFormat](../../com.aspose.slides/iformat)
### getChart() {#getChart--}
```
public final IChart getChart()
```

คืนค่าแผนภูมิ อ่านอย่างเดียว [IChart](../../com.aspose.slides/ichart).

**คืนค่า:**
[IChart](../../com.aspose.slides/ichart)
### getEntries() {#getEntries--}
```
public final ILegendEntryCollection getEntries()
```

รับรายการ legend อ่านอย่างเดียว [ILegendEntryCollection](../../com.aspose.slides/ilegendentrycollection).

**คืนค่า:**
[ILegendEntryCollection](../../com.aspose.slides/ilegendentrycollection)
### getActualX() {#getActualX--}
```
public final float getActualX()
```

ระบุตำแหน่ง x จริง (ซ้าย) ขององค์ประกอบแผนภูมิสัมพันธ์กับมุมซ้ายบนของแผนภูมิ เรียกเมธอด IChart.validateChartLayout() ก่อนเพื่อรับค่าจริง อ่าน float.

**คืนค่า:**
float
### getActualY() {#getActualY--}
```
public final float getActualY()
```

ระบุตำแหน่งบนจริงขององค์ประกอบแผนภูมิสัมพันธ์กับมุมซ้ายบนของแผนภูมิ เรียกเมธอด IChart.validateChartLayout() ก่อนเพื่อรับค่าจริง อ่าน float.

**คืนค่า:**
float
### getActualWidth() {#getActualWidth--}
```
public final float getActualWidth()
```

ระบุความกว้างจริงขององค์ประกอบแผนภูมิ เรียกเมธอด IChart.validateChartLayout() ก่อนเพื่อรับค่าจริง อ่าน float.

**คืนค่า:**
float
### getActualHeight() {#getActualHeight--}
```
public final float getActualHeight()
```

ระบุความสูงจริงขององค์ประกอบแผนภูมิ เรียกเมธอด IChart.validateChartLayout() ก่อนเพื่อรับค่าจริง อ่าน float.

**คืนค่า:**
float
### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

คืนสไลด์แม่ของ FillFormat อ่านอย่างเดียว [BaseSlide](../../com.aspose.slides/baseslide).

**คืนค่า:**
[IBaseSlide](../../com.aspose.slides/ibaseslide)
### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

คืนพรีเซนเทชันแม่ของ FillFormat อ่านอย่างเดียว [IPresentation](../../com.aspose.slides/ipresentation).

**คืนค่า:**
[IPresentation](../../com.aspose.slides/ipresentation)