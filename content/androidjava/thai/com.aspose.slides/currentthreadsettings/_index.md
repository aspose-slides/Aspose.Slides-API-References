---
title: CurrentThreadSettings
second_title: Aspose.Slides สำหรับ Android ผ่านการอ้างอิง Java API
description: คลาสเสริมที่อนุญาตให้กำหนดค่า Locale เริ่มต้นสำหรับเธรดปัจจุบัน
type: docs
url: /th/com.aspose.slides/currentthreadsettings/
---
**การสืบทอด:**
java.lang.Object
```
public class CurrentThreadSettings
```

คลาสเสริมที่อนุญาตให้กำหนด default Locale สำหรับเธรดปัจจุบัน
## ตัวสร้าง

| ตัวสร้าง | คำอธิบาย |
| --- | --- |
| [CurrentThreadSettings()](#CurrentThreadSettings--) |  |
## เมธอด

| เมธอด | คำอธิบาย |
| --- | --- |
| [getLocale()](#getLocale--) | ส่งคืน default Locale สำหรับเธรดปัจจุบัน. |
| [setLocale(Locale locale)](#setLocale-java.util.Locale-) | ตั้งค่า default Locale สำหรับเธรดปัจจุบัน. |
| [setLocale(String localeName)](#setLocale-java.lang.String-) | ตั้งค่า default Locale สำหรับเธรดปัจจุบัน. |
### CurrentThreadSettings() {#CurrentThreadSettings--}
```
public CurrentThreadSettings()
```


### getLocale() {#getLocale--}
```
public static final Locale getLocale()
```


ส่งคืน default Locale สำหรับเธรดปัจจุบัน.

**ส่งคืน:**
java.util.Locale - Locale ปัจจุบัน
### setLocale(Locale locale) {#setLocale-java.util.Locale-}
```
public static final void setLocale(Locale locale)
```


ตั้งค่า default Locale สำหรับเธรดปัจจุบัน.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| locale | java.util.Locale | Locale เริ่มต้นสำหรับเธรดปัจจุบัน. |

### setLocale(String localeName) {#setLocale-java.lang.String-}
```
public static final void setLocale(String localeName)
```


ตั้งค่า default Locale สำหรับเธรดปัจจุบัน.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| localeName | java.lang.String | Locale เริ่มต้นสำหรับเธรดปัจจุบัน. |