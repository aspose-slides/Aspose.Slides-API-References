---
title: MarkerStyleType
second_title: Aspose.Slides สำหรับ Android ผ่าน Java API อ้างอิง
description: กำหนดรูปแบบของเครื่องหมายบนจุดข้อมูลของแผนภูมิ.
type: docs
url: /th/com.aspose.slides/markerstyletype/
---
**การสืบทอด:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class MarkerStyleType extends System.Enum
```

กำหนดรูปแบบของเครื่องหมายบนจุดข้อมูลของแผนภูมิ.
## ฟิลด์

| ฟิลด์ | คำอธิบาย |
| --- | --- |
| [NotDefined](#NotDefined) |  |
| [Circle](#Circle) | ระบุให้วาดวงกลมที่จุดข้อมูลแต่ละจุด. |
| [Dash](#Dash) | ระบุให้วาดเส้นประที่จุดข้อมูลแต่ละจุด. |
| [Diamond](#Diamond) | ระบุให้วาดรูปเพชรที่จุดข้อมูลแต่ละจุด. |
| [Dot](#Dot) | ระบุให้วาดจุดที่จุดข้อมูลแต่ละจุด. |
| [None](#None) | ระบุว่าไม่มีสิ่งใดถูกวาดที่จุดข้อมูลแต่ละจุด. |
| [Picture](#Picture) | ระบุให้วาดรูปภาพที่จุดข้อมูลแต่ละจุด. |
| [Plus](#Plus) | ระบุให้วาดเครื่องหมายบวกที่จุดข้อมูลแต่ละจุด. |
| [Square](#Square) | ระบุให้วาดสี่เหลี่ยมที่จุดข้อมูลแต่ละจุด. |
| [Star](#Star) | ระบุให้วาดดาวที่จุดข้อมูลแต่ละจุด. |
| [Triangle](#Triangle) | ระบุให้วาดสามเหลี่ยมที่จุดข้อมูลแต่ละจุด. |
| [X](#X) | ระบุให้วาดสัญลักษณ์ X ที่จุดข้อมูลแต่ละจุด. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```




### Circle {#Circle}
```
public static final int Circle
```

ระบุให้วาดวงกลมที่จุดข้อมูลแต่ละจุด.

### Dash {#Dash}
```
public static final int Dash
```

ระบุให้วาดเส้นประที่จุดข้อมูลแต่ละจุด.

### Diamond {#Diamond}
```
public static final int Diamond
```

ระบุให้วาดรูปเพชรที่จุดข้อมูลแต่ละจุด.

### Dot {#Dot}
```
public static final int Dot
```

ระบุให้วาดจุดที่จุดข้อมูลแต่ละจุด.

### None {#None}
```
public static final int None
```

ระบุว่าไม่มีสิ่งใดถูกวาดที่จุดข้อมูลแต่ละจุด.

### Picture {#Picture}
```
public static final int Picture
```

ระบุให้วาดรูปภาพที่จุดข้อมูลแต่ละจุด.

### Plus {#Plus}
```
public static final int Plus
```

ระบุให้วาดเครื่องหมายบวกที่จุดข้อมูลแต่ละจุด.

### Square {#Square}
```
public static final int Square
```

ระบุให้วาดสี่เหลี่ยมที่จุดข้อมูลแต่ละจุด.

### Star {#Star}
```
public static final int Star
```

ระบุให้วาดดาวที่จุดข้อมูลแต่ละจุด.

### Triangle {#Triangle}
```
public static final int Triangle
```

ระบุให้วาดสามเหลี่ยมที่จุดข้อมูลแต่ละจุด.

### X {#X}
```
public static final int X
```

ระบุให้วาดสัญลักษณ์ X ที่จุดข้อมูลแต่ละจัด.