---
title: IColorOffset
second_title: Aspose.Slides for Android via Java API Reference
description: Represent color offset.
type: docs
url: /th/com.aspose.slides/icoloroffset/
---```
public interface IColorOffset
```

แทนค่าการออฟเซตของสี.
## เมธอด

| เมธอด | คำอธิบาย |
| --- | --- |
| [getValue0()](#getValue0--) | กำหนดค่าตัวแรกของออฟเซต. |
| [setValue0(float value)](#setValue0-float-) | กำหนดค่าตัวแรกของออฟเซต. |
| [getValue1()](#getValue1--) | กำหนดค่าตัวที่สองของออฟเซต. |
| [setValue1(float value)](#setValue1-float-) | กำหนดค่าตัวที่สองของออฟเซต. |
| [getValue2()](#getValue2--) | กำหนดค่าตัวที่สามของออฟเซต. |
| [setValue2(float value)](#setValue2-float-) | กำหนดค่าตัวที่สามของออฟเซต. |
### getValue0() {#getValue0--}
```
public abstract float getValue0()
```

กำหนดค่าตัวแรกของออฟเซต. อ่าน/เขียน float.

**คืนค่า:**
float
### setValue0(float value) {#setValue0-float-}
```
public abstract void setValue0(float value)
```

กำหนดค่าตัวแรกของออฟเซต. อ่าน/เขียน float.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | float |  |

### getValue1() {#getValue1--}
```
public abstract float getValue1()
```

กำหนดค่าตัวที่สองของออฟเซต. อ่าน/เขียน float.

**คืนค่า:**
float
### setValue1(float value) {#setValue1-float-}
```
public abstract void setValue1(float value)
```

กำหนดค่าตัวที่สองของออฟเซต. อ่าน/เขียน float.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | float |  |

### getValue2() {#getValue2--}
```
public abstract float getValue2()
```

กำหนดค่าตัวที่สามของออฟเซต. อ่าน/เขียน float.

**คืนค่า:**
float
### setValue2(float value) {#setValue2-float-}
```
public abstract void setValue2(float value)
```

กำหนดค่าตัวที่สามของออฟเซต. อ่าน/เขียน float.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | float |  |