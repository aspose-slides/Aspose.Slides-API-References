---
title: IColorChangeEffectiveData
second_title: Aspose.Slides สำหรับ Android ผ่านการอ้างอิง Java API
description: อ็อบเจกต์ไม่เปลี่ยนแปลงซึ่งเป็นตัวแทนของเอฟเฟกต์การเปลี่ยนสี.
type: docs
url: /th/com.aspose.slides/icolorchangeeffectivedata/
---
**ส่วนต่อประสานที่ใช้งานทั้งหมด:**
[com.aspose.slides.IEffectEffectiveData](../../com.aspose.slides/ieffecteffectivedata)
```
public interface IColorChangeEffectiveData extends IEffectEffectiveData
```

อ็อบเจกต์ที่ไม่เปลี่ยนแปลงซึ่งเป็นตัวแทนของเอฟเฟกต์การเปลี่ยน Color Change. อินสแตนซ์ของ FromColor จะถูกแทนที่ด้วยอินสแตนซ์ของ ToColor.
## เมธอด

| เมธอด | คำอธิบาย |
| --- | --- |
| [getFromColor()](#getFromColor--) | Color ที่จะถูกแทนที่. |
| [getToColor()](#getToColor--) | Color ที่จะแทนที่. |
| [getUseAlpha()](#getUseAlpha--) | คืนค่า boolean ที่ระบุว่าควรใช้ส่วน alpha หรือไม่. |
### getFromColor() {#getFromColor--}
```
public abstract Integer getFromColor()
```

Color ที่จะถูกแทนที่. อ่านอย่างเดียว java.lang.Integer.

**คืนค่า:**
java.lang.Integer
### getToColor() {#getToColor--}
```
public abstract Integer getToColor()
```

Color ที่จะแทนที่. อ่านอย่างเดียว java.lang.Integer.

**คืนค่า:**
java.lang.Integer
### getUseAlpha() {#getUseAlpha--}
```
public abstract boolean getUseAlpha()
```

คืนค่า boolean ที่ระบุว่าควรใช้ส่วน alpha หรือไม่. อ่านอย่างเดียว boolean.

**คืนค่า:**
boolean