---
title: DrawingGuide
second_title: Aspose.Slides สำหรับ Android ผ่านการอ้างอิง API ของ Java
description: แทนแนวทางวาดที่ปรับได้.
type: docs
url: /th/com.aspose.slides/drawingguide/
---
**Inheritance:**
java.lang.Object

**All Implemented Interfaces:**
[com.aspose.slides.IDrawingGuide](../../com.aspose.slides/idrawingguide)
```
public final class DrawingGuide implements IDrawingGuide
```

แทนการแสดงแนวทางวาดที่ปรับได้
## เมธอด

| เมธอด | คำอธิบาย |
| --- | --- |
| [getOrientation()](#getOrientation--) | คืนค่า หรือ ตั้งค่าทิศทางของแนวทางวาด |
| [setOrientation(byte value)](#setOrientation-byte-) | คืนค่า หรือ ตั้งค่าทิศทางของแนวทางวาด |
| [getPosition()](#getPosition--) | คืนค่า หรือ ตั้งค่าตำแหน่งของแนวทางวาดเป็นจุดจากมุมบนซ้ายของสไลด์ |
| [setPosition(float value)](#setPosition-float-) | คืนค่า หรือ ตั้งค่าตำแหน่งของแนวทางวาดเป็นจุดจากมุมบนซ้ายของสไลด์ |
| [getColor()](#getColor--) | คืนค่า หรือ ตั้งค่าสีของแนวทางวาด |
| [setColor(Integer value)](#setColor-java.lang.Integer-) | คืนค่า หรือ ตั้งค่าสีของแนวทางวาด |
### getOrientation() {#getOrientation--}
```
public final byte getOrientation()
```

คืนค่า หรือ ตั้งค่าทิศทางของแนวทางวาด. อ่าน/เขียน [Orientation](../../com.aspose.slides/orientation).

**คืนค่า:**
byte
### setOrientation(byte value) {#setOrientation-byte-}
```
public final void setOrientation(byte value)
```

คืนค่า หรือ ตั้งค่าทิศทางของแนวทางวาด. อ่าน/เขียน [Orientation](../../com.aspose.slides/orientation).

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | byte |  |

### getPosition() {#getPosition--}
```
public final float getPosition()
```

คืนค่า หรือ ตั้งค่าตำแหน่งของแนวทางวาดเป็นจุดจากมุมบนซ้ายของสไลด์. อ่าน/เขียน float.

--------------------

ค่าช่วงทั่วไปอยู่ระหว่างศูนย์ถึงความสูงของสไลด์สำหรับแนวทางแนวนอนและระหว่างศูนย์ถึงความกว้างของสไลด์สำหรับแนวทางแนวตั้ง

**คืนค่า:**
float
### setPosition(float value) {#setPosition-float-}
```
public final void setPosition(float value)
```

คืนค่า หรือ ตั้งค่าตำแหน่งของแนวทางวาดเป็นจุดจากมุมบนซ้ายของสไลด์. อ่าน/เขียน float.

--------------------

ค่าช่วงทั่วไปอยู่ระหว่างศูนย์ถึงความสูงของสไลด์สำหรับแนวทางแนวนอนและระหว่างศูนย์ถึงความกว้างของสไลด์สำหรับแนวทางแนวตั้ง

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | float |  |

### getColor() {#getColor--}
```
public final Integer getColor()
```

คืนค่า หรือ ตั้งค่าสีของแนวทางวาด. อ่าน/เขียน java.lang.Integer.

**คืนค่า:**
java.lang.Integer
### setColor(Integer value) {#setColor-java.lang.Integer-}
```
public final void setColor(Integer value)
```

คืนค่า หรือ ตั้งค่าสีของแนวทางวาด. อ่าน/เขียน java.lang.Integer.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | java.lang.Integer |  |