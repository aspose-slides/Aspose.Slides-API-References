---
title: SvgEvent
second_title: Aspose.Slides สำหรับ Android ผ่าน Java API Reference
description: แสดงตัวเลือกสำหรับรูปทรง SVG.
type: docs
url: /th/com.aspose.slides/svgevent/
---
**การสืบทอด:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class SvgEvent extends System.Enum
```

แสดงตัวเลือกสำหรับรูปทรง SVG.
## ฟิลด์

| ฟิลด์ | คำอธิบาย |
| --- | --- |
| [OnFocusIn](#OnFocusIn) | เกิดขึ้นเมื่อองค์ประกอบได้รับโฟกัส เช่น เมื่อข้อความถูกเลือก |
| [OnFocusOut](#OnFocusOut) | เกิดขึ้นเมื่อองค์ประกอบสูญเสียโฟกัส เช่น เมื่อข้อความไม่ได้เลือก |
| [OnActivate](#OnActivate) | เกิดขึ้นเมื่อองค์ประกอบถูกกระตุ้น เช่น ผ่านการคลิกเมาส์หรือการกดแป้นพิมพ์ |
| [OnClick](#OnClick) | เกิดขึ้นเมื่อปุ่มอุปกรณ์ชี้ถูกคลิกบนองค์ประกอบ |
| [OnMouseDown](#OnMouseDown) | เกิดขึ้นเมื่อปุ่มอุปกรณ์ชี้ถูกกดบนองค์ประกอบ |
| [OnMouseUp](#OnMouseUp) | เกิดขึ้นเมื่อปุ่มอุปกรณ์ชี้ถูกปล่อยบนองค์ประกอบ |
| [OnMouseOver](#OnMouseOver) | เกิดขึ้นเมื่ออุปกรณ์ชี้เคลื่อนย้ายเข้าสู่องค์ประกอบ |
| [OnMouseMove](#OnMouseMove) | เกิดขึ้นเมื่ออุปกรณ์ชี้เคลื่อนย้ายขณะอยู่เหนือองค์ประกอบ |
| [OnMouseOut](#OnMouseOut) | เกิดขึ้นเมื่ออุปกรณ์ชี้เคลื่อนย้ายออกจากองค์ประกอบ |
| [OnLoad](#OnLoad) | เกิดขึ้นเมื่อเอเจนต์ผู้ใช้ทำการแยกวิเคราะห์องค์ประกอบและสืบทอดของมันรวมถึงทรัพยากรที่อ้างอิงทั้งหมดที่จำเป็นต่อการแสดงผลอย่างครบถ้วน |
| [OnUnload](#OnUnload) | เกิดขึ้นเมื่อการนำไปใช้ของ DOM ลบเอกสารออกจากหน้าต่างหรือกรอบ |
| [OnAbort](#OnAbort) | เกิดขึ้นเมื่อการโหลดหน้าเว็บถูกหยุดก่อนที่องค์ประกอบจะโหลดครบถ้วน |
| [OnError](#OnError) | เกิดขึ้นเมื่อองค์ประกอบไม่โหลดอย่างถูกต้องหรือเมื่อเกิดข้อผิดพลาดระหว่างการทำงานของสคริปต์ |
| [OnResize](#OnResize) | เกิดขึ้นเมื่อมุมมองเอกสารถูกปรับขนาด |
| [OnScroll](#OnScroll) | เกิดขึ้นเมื่อมุมมองเอกสารถูกเลื่อนตามแกน X หรือ Y หรือทั้งสองแกน |
| [OnZoom](#OnZoom) | เกิดขึ้นเมื่อระดับการซูมของมุมมองเอกสารถูกเปลี่ยนแปลง |
| [OnBegin](#OnBegin) | เกิดขึ้นเมื่อองค์ประกอบแอนิเมชันเริ่มต้น |
| [OnEnd](#OnEnd) | เกิดขึ้นเมื่อองค์ประกอบแอนิเมชันสิ้นสุด |
| [OnRepeat](#OnRepeat) | เกิดขึ้นเมื่อองค์ประกอบแอนิเมชันทำซ้ำ |
### OnFocusIn {#OnFocusIn}
```
public static final int OnFocusIn
```

เกิดขึ้นเมื่อองค์ประกอบได้รับโฟกัส เช่น เมื่อข้อความถูกเลือก

### OnFocusOut {#OnFocusOut}
```
public static final int OnFocusOut
```

เกิดขึ้นเมื่อองค์ประกอบสูญเสียโฟกัส เช่น เมื่อข้อความไม่ได้เลือก

### OnActivate {#OnActivate}
```
public static final int OnActivate
```

เกิดขึ้นเมื่อองค์ประกอบถูกกระตุ้น เช่น ผ่านการคลิกเมาส์หรือการกดแป้นพิมพ์

### OnClick {#OnClick}
```
public static final int OnClick
```

เกิดขึ้นเมื่อปุ่มอุปกรณ์ชี้ถูกคลิกบนองค์ประกอบ

### OnMouseDown {#OnMouseDown}
```
public static final int OnMouseDown
```

เกิดขึ้นเมื่อปุ่มอุปกรณ์ชี้ถูกกดบนองค์ประกอบ

### OnMouseUp {#OnMouseUp}
```
public static final int OnMouseUp
```

เกิดขึ้นเมื่อปุ่มอุปกรณ์ชี้ถูกปล่อยบนองค์ประกอบ

### OnMouseOver {#OnMouseOver}
```
public static final int OnMouseOver
```

เกิดขึ้นเมื่ออุปกรณ์ชี้เคลื่อนย้ายเข้าสู่องค์ประกอบ

### OnMouseMove {#OnMouseMove}
```
public static final int OnMouseMove
```

เกิดขึ้นเมื่ออุปกรณ์ชี้เคลื่อนย้ายขณะอยู่เหนือองค์ประกอบ

### OnMouseOut {#OnMouseOut}
```
public static final int OnMouseOut
```

เกิดขึ้นเมื่ออุปกรณ์ชี้เคลื่อนย้ายออกจากองค์ประกอบ

### OnLoad {#OnLoad}
```
public static final int OnLoad
```

เกิดขึ้นเมื่อเอเจนต์ผู้ใช้ทำการแยกวิเคราะห์องค์ประกอบและสืบทอดของมันรวมถึงทรัพยากรที่อ้างอิงทั้งหมดที่จำเป็นต่อการแสดงผลอย่างครบถ้วน

### OnUnload {#OnUnload}
```
public static final int OnUnload
```

เกิดขึ้นเมื่อการนำไปใช้ของ DOM ลบเอกสารออกจากหน้าต่างหรือกรอบ ซึ่งใช้ได้เฉพาะกับองค์ประกอบ SVG ที่อยู่ในระดับนอกสุด

### OnAbort {#OnAbort}
```
public static final int OnAbort
```

เกิดขึ้นเมื่อการโหลดหน้าเว็บถูกหยุดก่อนที่องค์ประกอบจะโหลดครบถ้วน

### OnError {#OnError}
```
public static final int OnError
```

เกิดขึ้นเมื่อองค์ประกอบไม่โหลดอย่างถูกต้องหรือเมื่อเกิดข้อผิดพลาดระหว่างการทำงานของสคริปต์

### OnResize {#OnResize}
```
public static final int OnResize
```

เกิดขึ้นเมื่อมุมมองเอกสารถูกปรับขนาด ซึ่งใช้ได้เฉพาะกับองค์ประกอบ SVG ที่อยู่ในระดับนอกสุด

### OnScroll {#OnScroll}
```
public static final int OnScroll
```

เกิดขึ้นเมื่อมุมมองเอกสารถูกเลื่อนตามแกน X หรือ Y หรือทั้งสองแกน ซึ่งใช้ได้เฉพาะกับองค์ประกอบ SVG ที่อยู่ในระดับนอกสุด

### OnZoom {#OnZoom}
```
public static final int OnZoom
```

เกิดขึ้นเมื่อระดับการซูมของมุมมองเอกสารถูกเปลี่ยนแปลง ซึ่งใช้ได้เฉพาะกับองค์ประกอบ SVG ที่อยู่ในระดับนอกสุด

### OnBegin {#OnBegin}
```
public static final int OnBegin
```

เกิดขึ้นเมื่อองค์ประกอบแอนิเมชันเริ่มต้น

### OnEnd {#OnEnd}
```
public static final int OnEnd
```

เกิดขึ้นเมื่อองค์ประกอบแอนิเมชันสิ้นสุด

### OnRepeat {#OnRepeat}
```
public static final int OnRepeat
```

เกิดขึ้นเมื่อองค์ประกอบแอนิเมชันทำซ้ำ