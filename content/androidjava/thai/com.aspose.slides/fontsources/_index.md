---
title: FontSources
second_title: Aspose.Slides สำหรับ Android ผ่านการอ้างอิง API ของ Java
description: จัดหาไฟล์และแหล่งข้อมูลหน่วยความจำสำหรับฟอนต์ภายนอก.
type: docs
url: /th/com.aspose.slides/fontsources/
---
**Inheritance:**  
การสืบทอด:

**All Implemented Interfaces:**  
อินเทอร์เฟซที่ทำการ Implement ทั้งหมด:
[com.aspose.slides.IFontSources](../../com.aspose.slides/ifontsources)
```
public class FontSources implements IFontSources
```

จัดหาไฟล์และแหล่งข้อมูลหน่วยความจำสำหรับฟอนต์ภายนอก.
## Constructors

| Constructor | Description |
| --- | --- |
| [FontSources()](#FontSources--) | สร้างตัวเลือกฟอนต์เริ่มต้นใหม่. |

## Methods

| Method | Description |
| --- | --- |
| [getFontFolders()](#getFontFolders--) | โฟลเดอร์ที่บรรจุไฟล์ฟอนต์. |
| [setFontFolders(String[] value)](#setFontFolders-java.lang.String---) | โฟลเดอร์ที่บรรจุไฟล์ฟอนต์. |
| [getMemoryFonts()](#getMemoryFonts--) | คอลเลกชันของฟอนต์ที่แสดงเป็นอาร์เรย์ของไบต์. |
| [setMemoryFonts(byte[][] value)](#setMemoryFonts-byte-----) | คอลเลกชันของฟอนต์ที่แสดงเป็นอาร์เรย์ของไบต์. |
### FontSources() {#FontSources--}
```
public FontSources()
```

สร้างตัวเลือกฟอนต์เริ่มต้นใหม่.

### getFontFolders() {#getFontFolders--}
```
public final String[] getFontFolders()
```

โฟลเดอร์ที่บรรจุไฟล์ฟอนต์. ไฟล์ฟอนต์ทั้งหมดที่อยู่ในโฟลเดอร์เหล่านี้จะถูกรวมในคอลเลกชัน โฟลเดอร์ที่ค้นหาแบบเรียกซ้ำ

**Returns:**  
java.lang.String[]

### setFontFolders(String[] value) {#setFontFolders-java.lang.String---}
```
public final void setFontFolders(String[] value)
```

โฟลเดอร์ที่บรรจุไฟล์ฟอนต์. ไฟล์ฟอนต์ทั้งหมดที่อยู่ในโฟลเดอร์เหล่านี้จะถูกรวมในคอลเลกชัน โฟลเดอร์ที่ค้นหาแบบเรียกซ้ำ

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | java.lang.String[] |  |

### getMemoryFonts() {#getMemoryFonts--}
```
public final byte[][] getMemoryFonts()
```

คอลเลกชันของฟอนต์ที่แสดงเป็นอาร์เรย์ของไบต์.

**Returns:**  
byte[][]

### setMemoryFonts(byte[][] value) {#setMemoryFonts-byte-----}
```
public final void setMemoryFonts(byte[][] value)
```

คอลเลกชันของฟอนต์ที่แสดงเป็นอาร์เรย์ของไบต์.

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | byte[][] |  |