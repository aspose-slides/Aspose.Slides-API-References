---
title: PieSplitType
second_title: Aspose.Slides สำหรับ Android ผ่านการอ้างอิง API ของ Java
description: แสดงประเภทของจุดแบ่งในพายหรือแถบที่สองบนแผนภูมิแบบ pie-of-pie หรือ bar-of-pie
type: docs
url: /th/com.aspose.slides/piesplittype/
---
**การสืบทอด:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class PieSplitType extends System.Enum
```

แสดงประเภทของจุดแบ่งในพายหรือแถบที่สองบนแผนภูมิแบบ pie-of-pie หรือ bar-of-pie chart.
## ฟิลด์

| ฟิลด์ | คำอธิบาย |
| --- | --- |
| [Default](#Default) | ระบุว่าจุดข้อมูลจะถูกแยกโดยใช้กลไกเริ่มต้นสำหรับประเภทแผนภูมินี้ |
| [Custom](#Custom) | ระบุว่าจุดข้อมูลจะถูกแยกระหว่างพายและแผนภูมิที่สองตามค่าการแยกแบบกำหนดเอง |
| [ByPercentage](#ByPercentage) | ระบุว่าจุดข้อมูลจะถูกแยกระหว่างพายและแผนภูมิที่สองโดยวางจุดที่มีเปอร์เซ็นต์น้อยกว่าตำแหน่งแยก (Split Position) ไว้ในแผนภูมิที่สอง |
| [ByPos](#ByPos) | ระบุว่าจุดข้อมูลจะถูกแยกระหว่างพายและแผนภูมิที่สองโดยวางตำแหน่งแยกสุดท้ายของจุดข้อมูลไว้ในแผนภูมิที่สอง |
| [ByValue](#ByValue) | ระบุว่าจุดข้อมูลจะถูกแยกระหว่างพายและแผนภูมิที่สองโดยวางจุดข้อมูลที่มีค่าน้อยกว่าตำแหน่งแยก (Split Position) ไว้ในแผนภูมิที่สอง |
### ค่าเริ่มต้น {#Default}
```
public static final int Default
```


ระบุว่าจุดข้อมูลจะถูกแยกโดยใช้กลไกเริ่มต้นสำหรับประเภทแผนภูมินี้

### กำหนดเอง {#Custom}
```
public static final int Custom
```


ระบุว่าจุดข้อมูลจะถูกแยกระหว่างพายและแผนภูมิที่สองตามค่าการแยกแบบกำหนดเอง

### ตามเปอร์เซ็นต์ {#ByPercentage}
```
public static final int ByPercentage
```


ระบุว่าจุดข้อมูลจะถูกแยกระหว่างพายและแผนภูมิที่สองโดยวางจุดที่มีเปอร์เซ็นต์น้อยกว่าตำแหน่งแยก (Split Position) ไว้ในแผนภูมิที่สอง

### ตามตำแหน่ง {#ByPos}
```
public static final int ByPos
```


ระบุว่าจุดข้อมูลจะถูกแยกระหว่างพายและแผนภูมิที่สองโดยวางตำแหน่งแยกสุดท้ายของจุดข้อมูลไว้ในแผนภูมิที่สอง

### ตามค่า {#ByValue}
```
public static final int ByValue
```


ระบุว่าจุดข้อมูลจะถูกแยกระหว่างพายและแผนภูมิที่สองโดยวางจุดข้อมูลที่มีค่าน้อยกว่าตำแหน่งแยก (Split Position) ไว้ในแผนภูมิที่สอง