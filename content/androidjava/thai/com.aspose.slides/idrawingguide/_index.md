---
title: IDrawingGuide
second_title: Aspose.Slides for Android via Java API Reference
description: แสดงถึงไกด์การวาดที่ปรับได้.
type: docs
url: /th/com.aspose.slides/idrawingguide/
---```
public interface IDrawingGuide
```

แสดงถึงไกด์การวาดที่ปรับได้.
## เมธอด

| เมธอด | คำอธิบาย |
| --- | --- |
| [getOrientation()](#getOrientation--) | ส่งคืนหรือกำหนดการวางแนวของไกด์การวาด. |
| [setOrientation(byte value)](#setOrientation-byte-) | ส่งคืนหรือกำหนดการวางแนวของไกด์การวาด. |
| [getPosition()](#getPosition--) | ส่งคืนหรือกำหนดตำแหน่งของไกด์การวาดในหน่วยจุดจากมุมบนซ้ายของสไลด์. |
| [setPosition(float value)](#setPosition-float-) | ส่งคืนหรือกำหนดตำแหน่งของไกด์การวาดในหน่วยจุดจากมุมบนซ้ายของสไลด์. |
| [getColor()](#getColor--) | ส่งคืนหรือกำหนดสีของไกด์การวาด. |
| [setColor(Integer value)](#setColor-java.lang.Integer-) | ส่งคืนหรือกำหนดสีของไกด์การวาด. |
### getOrientation() {#getOrientation--}
```
public abstract byte getOrientation()
```

ส่งคืนหรือกำหนดการวางแนวของไกด์การวาด. อ่าน/เขียน [Orientation](../../com.aspose.slides/orientation).

**ผลลัพธ์:**
byte
### setOrientation(byte value) {#setOrientation-byte-}
```
public abstract void setOrientation(byte value)
```

ส่งคืนหรือกำหนดการวางแนวของไกด์การวาด. อ่าน/เขียน [Orientation](../../com.aspose.slides/orientation).

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | byte |  |

### getPosition() {#getPosition--}
```
public abstract float getPosition()
```

ส่งคืนหรือกำหนดตำแหน่งของไกด์การวาดในหน่วยจุดจากมุมบนซ้ายของสไลด์. อ่าน/เขียน float.

--------------------

ช่วงค่าที่ปกติอยู่ระหว่างศูนย์ถึงความสูงของสไลด์สำหรับไกด์แนวนอนและระหว่างศูนย์ถึงความกว้างของสไลด์สำหรับไกด์แนวตั้ง.

**ผลลัพธ์:**
float
### setPosition(float value) {#setPosition-float-}
```
public abstract void setPosition(float value)
```

ส่งคืนหรือกำหนดตำแหน่งของไกด์การวาดในหน่วยจุดจากมุมบนซ้ายของสไลด์. อ่าน/เขียน float.

--------------------

ช่วงค่าที่ปกติอยู่ระหว่างศูนย์ถึงความสูงของสไลด์สำหรับไกด์แนวนอนและระหว่างศูนย์ถึงความกว้างของสไลด์สำหรับไกด์แนวตั้ง.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | float |  |

### getColor() {#getColor--}
```
public abstract Integer getColor()
```

ส่งคืนหรือกำหนดสีของไกด์การวาด. อ่าน/เขียน java.lang.Integer.

**ผลลัพธ์:**
java.lang.Integer
### setColor(Integer value) {#setColor-java.lang.Integer-}
```
public abstract void setColor(Integer value)
```

ส่งคืนหรือกำหนดสีของไกด์การวาด. อ่าน/เขียน java.lang.Integer.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | java.lang.Integer |  |