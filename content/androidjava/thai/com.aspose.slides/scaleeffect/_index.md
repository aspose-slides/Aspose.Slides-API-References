---
title: ScaleEffect
second_title: Aspose.Slides สำหรับ Android ผ่านการอ้างอิง Java API
description: แสดงถึงเอฟเฟ็กต์การปรับขนาดการเคลื่อนไหว.
type: docs
url: /th/com.aspose.slides/scaleeffect/
---
**การสืบทอด:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**อินเทอร์เฟซที่ทำการนำมาใช้ทั้งหมด:**
[com.aspose.slides.IScaleEffect](../../com.aspose.slides/iscaleeffect)
```
public class ScaleEffect extends Behavior implements IScaleEffect
```

แสดงถึงเอฟเฟ็กต์การปรับขนาดการเคลื่อนไหว.
## คอนสตรัคเตอร์

| คอนสตรัคเตอร์ | คำอธิบาย |
| --- | --- |
| [ScaleEffect()](#ScaleEffect--) |  |
## เมธอด

| เมธอด | คำอธิบาย |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | กำหนดว่าคอนเทนต์ควรซูมหรือไม่. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | กำหนดว่าคอนเทนต์ควรซูมหรือไม่. |
| [getFrom()](#getFrom--) | ระบุตำแหน่งพิกัด x/y ที่จะเริ่มการเคลื่อนไหวจาก (เป็นเปอร์เซ็น). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | ระบุตำแหน่งพิกัด x/y ที่จะเริ่มการเคลื่อนไหวจาก (เป็นเปอร์เซ็น). |
| [getTo()](#getTo--) | ระบุตำแหน่งเป้าหมายสำหรับเอฟเฟ็กต์การปรับขนาดการเคลื่อนไหว (เป็นเปอร์เซ็น). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | ระบุตำแหน่งเป้าหมายสำหรับเอฟเฟ็กต์การปรับขนาดการเคลื่อนไหว (เป็นเปอร์เซ็น). |
| [getBy()](#getBy--) | อธิบายค่าการออฟเซ็ตสัมพัทธ์สำหรับการเคลื่อนไหว (เป็นเปอร์เซ็น). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | อธิบายค่าการออฟเซ็ตสัมพัทธ์สำหรับการเคลื่อนไหว (เป็นเปอร์เซ็น). |
### ScaleEffect() {#ScaleEffect--}
```
public ScaleEffect()
```


### getZoomContent() {#getZoomContent--}
```
public final byte getZoomContent()
```


กำหนดว่าคอนเทนต์ควรซูมหรือไม่. อ่าน/เขียน [NullableBool](../../com.aspose.slides/nullablebool).

**คืนค่า:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public final void setZoomContent(byte value)
```


กำหนดว่าคอนเทนต์ควรซูมหรือไม่. อ่าน/เขียน [NullableBool](../../com.aspose.slides/nullablebool).

**พารามิเตอร์:**
| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public final PointF getFrom()
```


ระบุตำแหน่งพิกัด x/y ที่จะเริ่มการเคลื่อนไหวจาก (เป็นเปอร์เซ็น). อ่าน/เขียน android.graphics.PointF.

**คืนค่า:**
android.graphics.PointF
### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public final void setFrom(PointF value)
```


ระบุตำแหน่งพิกัด x/y ที่จะเริ่มการเคลื่อนไหวจาก (เป็นเปอร์เซ็น). อ่าน/เขียน android.graphics.PointF.

**พารามิเตอร์:**
| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public final PointF getTo()
```


ระบุตำแหน่งเป้าหมายสำหรับเอฟเฟ็กต์การปรับขนาดการเคลื่อนไหว (เป็นเปอร์เซ็น). อ่าน/เขียน android.graphics.PointF.

**คืนค่า:**
android.graphics.PointF
### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public final void setTo(PointF value)
```


ระบุตำแหน่งเป้าหมายสำหรับเอฟเฟ็กต์การปรับขนาดการเคลื่อนไหว (เป็นเปอร์เซ็น). อ่าน/เขียน android.graphics.PointF.

**พารามิเตอร์:**
| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public final PointF getBy()
```


อธิบายค่าการออฟเซ็ตสัมพัทธ์สำหรับการเคลื่อนไหว (เป็นเปอร์เซ็น). อ่าน/เขียน android.graphics.PointF.

**คืนค่า:**
android.graphics.PointF
### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public final void setBy(PointF value)
```


อธิบายค่าการออฟเซ็ตสัมพัทธ์สำหรับการเคลื่อนไหว (เป็นเปอร์เซ็น). อ่าน/เขียน android.graphics.PointF.

**พารามิเตอร์:**
| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | android.graphics.PointF |  |