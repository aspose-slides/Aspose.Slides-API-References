---
title: MathSpacingRules
second_title: Aspose.Slides สำหรับ Android ผ่าน Java API Reference
description: ประเภทของช่องว่างแนวนอนระหว่างคอลัมน์ของเมทริกซ์
type: docs
url: /th/com.aspose.slides/mathspacingrules/
---
**การสืบทอด:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class MathSpacingRules extends System.Enum
```

ประเภทของช่องว่าง (ระยะห่างตามแนวนอน) ระหว่างคอลัมน์ของเมทริกซ์
## ฟิลด์

| ฟิลด์ | คำอธิบาย |
| --- | --- |
| [SingleSpacingGap](#SingleSpacingGap) | เดียว (1 em สำหรับคอลัมน์หรือ 1 บรรทัดสำหรับแถว) |
| [OneAndHalfSpacingGap](#OneAndHalfSpacingGap) | หนึ่งและครึ่ง (1.5 em สำหรับคอลัมน์หรือ 1.5 บรรทัดสำหรับแถว) |
| [DoubleSpacingGap](#DoubleSpacingGap) | ช่องว่างคู่ (2 em สำหรับคอลัมน์หรือ 2 บรรทัดสำหรับแถว) |
| [Exactly](#Exactly) | ค่าที่แม่นยำ สำหรับคอลัมน์ พึ่งพาค่าของ ColumnGap วัดเป็น twips สำหรับแถว พึ่งพาค่าของ RowGap วัดเป็น twips |
| [Multiple](#Multiple) | หลายค่า สำหรับคอลัมน์ พึ่งพค่าของ ColumnGap วัดเป็นช่วง 0.5 em สำหรับแถว พึ่งพค่าของ RowGap วัดเป็นบรรทัด |
### SingleSpacingGap {#SingleSpacingGap}
```
public static final int SingleSpacingGap
```

เดียว (1 em สำหรับคอลัมน์หรือ 1 บรรทัดสำหรับแถว)

### OneAndHalfSpacingGap {#OneAndHalfSpacingGap}
```
public static final int OneAndHalfSpacingGap
```

หนึ่งและครึ่ง (1.5 em สำหรับคอลัมน์หรือ 1.5 บรรทัดสำหรับแถว)

### DoubleSpacingGap {#DoubleSpacingGap}
```
public static final int DoubleSpacingGap
```

ช่องว่างคู่ (2 em สำหรับคอลัมน์หรือ 2 บรรทัดสำหรับแถว)

### Exactly {#Exactly}
```
public static final int Exactly
```

ค่าที่แม่นยำ สำหรับคอลัมน์ พึ่งพาค่าของ ColumnGap วัดเป็น twips สำหรับแถว พึ่งพาค่าของ RowGap วัดเป็น twips

### Multiple {#Multiple}
```
public static final int Multiple
```

หลายค่า สำหรับคอลัมน์ พึ่งพค่าของ ColumnGap วัดเป็นช่วง 0.5 em สำหรับแถว พึ่งพค่าของ RowGap วัดเป็นบรรทัด