---
title: TextHighlightingOptions
second_title: Aspose.Slides for Android の Java API 参照
description: テキストフレーム内のテキストをハイライトするために使用できるオプションを表します。
type: docs
url: /ja/com.aspose.slides/texthighlightingoptions/
---
**継承:**
java.lang.Object

**実装されているすべてのインターフェイス:**
[com.aspose.slides.ITextHighlightingOptions](../../com.aspose.slides/itexthighlightingoptions)
```
public class TextHighlightingOptions implements ITextHighlightingOptions
```

テキスト フレーム内のテキストをハイライトするために使用できるオプションを表します。

## コンストラクタ

| コンストラクタ | 説明 |
| --- | --- |
| [TextHighlightingOptions()](#TextHighlightingOptions--) | デフォルトのテキストハイライトオプションを新しく作成します。 |

## メソッド

| メソッド | 説明 |
| --- | --- |
| [getCaseSensitive()](#getCaseSensitive--) | true を設定すると大文字と小文字を区別した検索を使用し、false の場合はそれ以外です。 |
| [setCaseSensitive(boolean value)](#setCaseSensitive-boolean-) | true を設定すると大文字と小文字を区別した検索を使用し、false の場合はそれ以外です。 |
| [getWholeWordsOnly()](#getWholeWordsOnly--) | true を設定すると完全一致する単語のみを対象とし、false の場合はそれ以外です。 |
| [setWholeWordsOnly(boolean value)](#setWholeWordsOnly-boolean-) | true を設定すると完全一致する単語のみを対象とし、false の場合はそれ以外です。 |

### TextHighlightingOptions() {#TextHighlightingOptions--}
```
public TextHighlightingOptions()
```

デフォルトのテキストハイライトオプションを新しく作成します。

### getCaseSensitive() {#getCaseSensitive--}
```
public final boolean getCaseSensitive()
```

true を設定すると大文字と小文字を区別した検索を使用し、false の場合はそれ以外です。 読み書き可能な boolean。

**戻り値:**
boolean

### setCaseSensitive(boolean value) {#setCaseSensitive-boolean-}
```
public final void setCaseSensitive(boolean value)
```

true を設定すると大文字と小文字を区別した検索を使用し、false の場合はそれ以外です。 読み書き可能な boolean。

**パラメータ:**
| パラメータ | 型 | 説明 |
| --- | --- | --- |
| value | boolean |  |

### getWholeWordsOnly() {#getWholeWordsOnly--}
```
public final boolean getWholeWordsOnly()
```

true を設定すると完全一致する単語のみを対象とし、false の場合はそれ以外です。 読み書き可能な boolean。

**戻り値:**
boolean

### setWholeWordsOnly(boolean value) {#setWholeWordsOnly-boolean-}
```
public final void setWholeWordsOnly(boolean value)
```

true を設定すると完全一致する単語のみを対象とし、false の場合はそれ以外です。 読み書き可能な boolean。

**パラメータ:**
| パラメータ | 型 | 説明 |
| --- | --- | --- |
| value | boolean |  |