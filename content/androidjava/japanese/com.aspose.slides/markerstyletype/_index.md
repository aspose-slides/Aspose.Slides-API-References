---
title: MarkerStyleType
second_title: Java API リファレンスによる Android 用 Aspose.Slides
description: チャートのデータポイントにおけるマーカーの形状を決定します。
type: docs
url: /ja/com.aspose.slides/markerstyletype/
---
**継承:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class MarkerStyleType extends System.Enum
```

チャートのデータポイントのマーカーの形状を決定します。
## フィールド

| フィールド | 説明 |
| --- | --- |
| [NotDefined](#NotDefined) |  |
| [Circle](#Circle) | 各データポイントで円が描画されることを指定します。 |
| [Dash](#Dash) | 各データポイントで破線が描画されることを指定します。 |
| [Diamond](#Diamond) | 各データポイントでダイヤモンドが描画されることを指定します。 |
| [Dot](#Dot) | 各データポイントで点が描画されることを指定します。 |
| [None](#None) | 各データポイントで何も描画されないことを指定します。 |
| [Picture](#Picture) | 各データポイントで画像が描画されることを指定します。 |
| [Plus](#Plus) | 各データポイントでプラス記号が描画されることを指定します。 |
| [Square](#Square) | 各データポイントで正方形が描画されることを指定します。 |
| [Star](#Star) | 各データポイントで星形が描画されることを指定します。 |
| [Triangle](#Triangle) | 各データポイントで三角形が描画されることを指定します。 |
| [X](#X) | 各データポイントで X が描画されることを指定します。 |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```




### Circle {#Circle}
```
public static final int Circle
```


各データポイントで円が描画されることを指定します。

### Dash {#Dash}
```
public static final int Dash
```


各データポイントで破線が描画されることを指定します。

### Diamond {#Diamond}
```
public static final int Diamond
```


各データポイントでダイヤモンドが描画されることを指定します。

### Dot {#Dot}
```
public static final int Dot
```


各データポイントで点が描画されることを指定します。

### None {#None}
```
public static final int None
```


各データポイントで何も描画されないことを指定します。

### Picture {#Picture}
```
public static final int Picture
```


各データポイントで画像が描画されることを指定します。

### Plus {#Plus}
```
public static final int Plus
```


各データポイントでプラス記号が描画されることを指定します。

### Square {#Square}
```
public static final int Square
```


各データポイントで正方形が描画されることを指定します。

### Star {#Star}
```
public static final int Star
```


各データポイントで星形が描画されることを指定します。

### Triangle {#Triangle}
```
public static final int Triangle
```


各データポイントで三角形が描画されることを指定します。

### X {#X}
```
public static final int X
```


各データポイントで X が描画されることを指定します。