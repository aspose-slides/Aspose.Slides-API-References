---
title: DisplayUnitType
second_title: Java API リファレンスによる Aspose.Slides for Android
description: 表示されるデータの多重性を決定します。
type: docs
url: /ja/com.aspose.slides/displayunittype/
---
**継承:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class DisplayUnitType extends System.Enum
```

表示されるデータの多重性を決定します。
## フィールド

| フィールド | 説明 |
| --- | --- |
| [None](#None) | 値はそのまま表示されます。 |
| [Hundreds](#Hundreds) | チャート上の値は 100 で除算されます。 |
| [Thousands](#Thousands) | チャート上の値は 1,000 で除算されます。 |
| [TenThousands](#TenThousands) | チャート上の値は 10,000 で除算されます。 |
| [HundredThousands](#HundredThousands) | チャート上の値は 100,000 で除算されます。 |
| [Millions](#Millions) | チャート上の値は 1,000,000 で除算されます。 |
| [TenMillions](#TenMillions) | チャート上の値は 10,000,000 で除算されます。 |
| [HundredMillions](#HundredMillions) | チャート上の値は 100,000,000 で除算されます。 |
| [Billions](#Billions) | チャート上の値は 1,000,000,000 で除算されます。 |
| [Trillions](#Trillions) | チャート上の値は 1,000,000,000,000 で除算されます。 |
| [CustomValue](#CustomValue) | チャート上の値はカスタム値で除算されます。 |
### None {#None}
```
public static final int None
```

値はそのまま表示されます。

### Hundreds {#Hundreds}
```
public static final int Hundreds
```

チャート上の値は 100 で除算されます。

### Thousands {#Thousands}
```
public static final int Thousands
```

チャート上の値は 1,000 で除算されます。

### TenThousands {#TenThousands}
```
public static final int TenThousands
```

チャート上の値は 10,000 で除算されます。

### HundredThousands {#HundredThousands}
```
public static final int HundredThousands
```

チャート上の値は 100,000 で除算されます。

### Millions {#Millions}
```
public static final int Millions
```

チャート上の値は 1,000,000 で除算されます。

### TenMillions {#TenMillions}
```
public static final int TenMillions
```

チャート上の値は 10,000,000 で除算されます。

### HundredMillions {#HundredMillions}
```
public static final int HundredMillions
```

チャート上の値は 100,000,000 で除算されます。

### Billions {#Billions}
```
public static final int Billions
```

チャート上の値は 1,000,000,000 で除算されます。

### Trillions {#Trillions}
```
public static final int Trillions
```

チャート上の値は 1,000,000,000,000 で除算されます。

### CustomValue {#CustomValue}
```
public static final int CustomValue
```

チャート上の値はカスタム値で除算されます。