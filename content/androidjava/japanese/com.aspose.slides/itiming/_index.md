---
title: ITiming
second_title: Aspose.Slides for Android via Java API Reference
description: アニメーションのタイミングを表します。
type: docs
url: /ja/com.aspose.slides/itiming/
---```
public interface ITiming
```

アニメーションのタイミングを表します。

## メソッド

| Method | Description |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | 加速動作効果の期間に対するパーセンテージを示します。 |
| [setAccelerate(float value)](#setAccelerate-float-) | 加速動作効果の期間に対するパーセンテージを示します。 |
| [getDecelerate()](#getDecelerate--) | 減速動作効果の期間に対するパーセンテージを示します。 |
| [setDecelerate(float value)](#setDecelerate-float-) | 減速動作効果の期間に対するパーセンテージを示します。 |
| [getAutoReverse()](#getAutoReverse--) | 前方向に再生した後、自動的に逆方向でアニメーションを再生するかどうかを示します。 |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | 前方向に再生した後、自動的に逆方向でアニメーションを再生するかどうかを示します。 |
| [getDuration()](#getDuration--) | アニメーション効果の期間を示します。 |
| [setDuration(float value)](#setDuration-float-) | アニメーション効果の期間を示します。 |
| [getRepeatCount()](#getRepeatCount--) | 効果を繰り返す回数を示します。 |
| [setRepeatCount(float value)](#setRepeatCount-float-) | 効果を繰り返す回数を示します。 |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | この属性は、スライドの終了まで効果が繰り返されるかどうかを指定します。 |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | この属性は、スライドの終了まで効果が繰り返されるかどうかを指定します。 |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | この属性は、次のクリックまで効果が繰り返されるかどうかを指定します。 |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | この属性は、次のクリックまで効果が繰り返されるかどうかを指定します。 |
| [getRepeatDuration()](#getRepeatDuration--) | 効果を繰り返す回数を示します。 |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | 効果を繰り返す回数を示します。 |
| [getRestart()](#getRestart--) | 効果が完了した後に再開するかどうかを指定します。 |
| [setRestart(int value)](#setRestart-int-) | 効果が完了した後に再開するかどうかを指定します。 |
| [getSpeed()](#getSpeed--) | タイミングを速める（または遅くする）割合を指定します。 |
| [setSpeed(float value)](#setSpeed-float-) | タイミングを速める（または遅くする）割合を指定します。 |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | トリガー後の遅延時間を示します。 |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | トリガー後の遅延時間を示します。 |
| [getTriggerType()](#getTriggerType--) | トリガータイプを示します。 |
| [setTriggerType(int value)](#setTriggerType-int-) | トリガータイプを示します。 |
| [getRewind()](#getRewind--) | この属性は、再生が完了したときに効果を巻き戻すかどうかを指定します。 |
| [setRewind(boolean value)](#setRewind-boolean-) | この属性は、再生が完了したときに効果を巻き戻すかどうかを指定します。 |

### getAccelerate() {#getAccelerate--}
```
public abstract float getAccelerate()
```

加速動作効果の期間に対するパーセンテージを示します。読み取り/書き込み float。

**戻り値:**
float

### setAccelerate(float value) {#setAccelerate-float-}
```
public abstract void setAccelerate(float value)
```

加速動作効果の期間に対するパーセンテージを示します。読み取り/書き込み float。

**パラメーター:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public abstract float getDecelerate()
```

減速動作効果の期間に対するパーセンテージを示します。読み取り/書き込み float。

**戻り値:**
float

### setDecelerate(float value) {#setDecelerate-float-}
```
public abstract void setDecelerate(float value)
```

減速動作効果の期間に対するパーセンテージを示します。読み取り/書き込み float。

**パラメーター:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public abstract boolean getAutoReverse()
```

前方向に再生した後、自動的に逆方向でアニメーションを再生するかどうかを示します。読み取り/書き込み boolean。

**戻り値:**
boolean

### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public abstract void setAutoReverse(boolean value)
```

前方向に再生した後、自動的に逆方向でアニメーションを再生するかどうかを示します。読み取り/書き込み boolean。

**パラメーター:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public abstract float getDuration()
```

アニメーション効果の期間を示します。読み取り/書き込み float。

**戻り値:**
float

### setDuration(float value) {#setDuration-float-}
```
public abstract void setDuration(float value)
```

アニメーション効果の期間を示します。読み取り/書き込み float。

**パラメーター:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public abstract float getRepeatCount()
```

効果を繰り返す回数を示します。読み取り/書き込み float。

**戻り値:**
float

### setRepeatCount(float value) {#setRepeatCount-float-}
```
public abstract void setRepeatCount(float value)
```

効果を繰り返す回数を示します。読み取り/書き込み float。

**パラメーター:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public abstract boolean getRepeatUntilEndSlide()
```

この属性は、スライドの終了まで効果が繰り返されるかどうかを指定します。読み取り/書き込み boolean。

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // 最初のスライドのエフェクトシーケンスを取得
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // メインシーケンスの最初のエフェクトを取得。
>      IEffect effect = effectsSequence.get_Item(0);
>      // エフェクトのタイミング/繰り返しを「スライドの最後まで」に変更
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**戻り値:**
boolean

### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public abstract void setRepeatUntilEndSlide(boolean value)
```

この属性は、スライドの終了まで効果が繰り返されるかどうかを指定します。読み取り/書き込み boolean。

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // 最初のスライドのエフェクトシーケンスを取得
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // メインシーケンスの最初のエフェクトを取得。
>      IEffect effect = effectsSequence.get_Item(0);
>      // エフェクトのタイミング/繰り返しを "スライドの最後まで" に変更
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**パラメーター:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public abstract boolean getRepeatUntilNextClick()
```

この属性は、次のクリックまで効果が繰り返されるかどうかを指定します。読み取り/書き込み boolean。

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // 最初のスライドのエフェクトシーケンスを取得
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // メインシーケンスの最初のエフェクトを取得。
>      IEffect effect = effectsSequence.get_Item(0);
>      // エフェクトのタイミング/繰り返しを "次のクリックまで" に変更
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**戻り値:**
boolean

### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public abstract void setRepeatUntilNextClick(boolean value)
```

この属性は、次のクリックまで効果が繰り返されるかどうかを指定します。読み取り/書き込み boolean。

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // 最初のスライドのエフェクトシーケンスを取得
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // メインシーケンスの最初のエフェクトを取得。
>      IEffect effect = effectsSequence.get_Item(0);
>      // エフェクトのタイミング/繰り返しを "次のクリックまで" に変更
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**パラメーター:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public abstract float getRepeatDuration()
```

効果を繰り返す回数を示します。読み取り/書き込み float。

**戻り値:**
float

### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public abstract void setRepeatDuration(float value)
```

効果を繰り返す回数を示します。読み取り/書き込み float。

**パラメーター:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public abstract int getRestart()
```

効果が完了した後に再開するかどうかを指定します。読み取り/書き込み [EffectRestartType](../../com.aspose.slides/effectrestarttype)。

**戻り値:**
int

### setRestart(int value) {#setRestart-int-}
```
public abstract void setRestart(int value)
```

効果が完了した後に再開するかどうかを指定します。読み取り/書き込み [EffectRestartType](../../com.aspose.slides/effectrestarttype)。

**パラメーター:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | int |  |

### getSpeed() {#getSpeed--}
```
public abstract float getSpeed()
```

タイミングを速める（または遅くする）割合を指定します。読み取り/書き込み float。

**戻り値:**
float

### setSpeed(float value) {#setSpeed-float-}
```
public abstract void setSpeed(float value)
```

タイミングを速める（または遅くする）割合を指定します。読み取り/書き込み float。

**パラメーター:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public abstract float getTriggerDelayTime()
```

トリガー後の遅延時間を示します。読み取り/書き込み float。

**戻り値:**
float

### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public abstract void setTriggerDelayTime(float value)
```

トリガー後の遅延時間を示します。読み取り/書き込み float。

**パラメーター:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public abstract int getTriggerType()
```

トリガータイプを示します。読み取り/書き込み [EffectTriggerType](../../com.aspose.slides/effecttriggertype)。

**戻り値:**
int

### setTriggerType(int value) {#setTriggerType-int-}
```
public abstract void setTriggerType(int value)
```

トリガータイプを示します。読み取り/書き込み [EffectTriggerType](../../com.aspose.slides/effecttriggertype)。

**パラメーター:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public abstract boolean getRewind()
```

この属性は、再生が完了したときに効果を巻き戻すかどうかを指定します。読み取り/書き込み boolean。

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // 最初のスライドのエフェクトシーケンスを取得
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // メインシーケンスの最初のエフェクトを取得。
>      IEffect effect = effectsSequence.get_Item(0);
>      // エフェクトのタイミング/リワインドを有効にする。
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**戻り値:**
boolean

### setRewind(boolean value) {#setRewind-boolean-}
```
public abstract void setRewind(boolean value)
```

この属性は、再生が完了したときに効果を巻き戻すかどうかを指定します。読み取り/書き込み boolean。

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // 最初のスライドのエフェクトシーケンスを取得
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // メインシーケンスの最初のエフェクトを取得。
>      IEffect effect = effectsSequence.get_Item(0);
>      // エフェクトのタイミング/リワインドを有効にする。
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**パラメーター:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |