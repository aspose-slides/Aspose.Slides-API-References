---
title: ErrorBarType
second_title: Android 用 Aspose.Slides の Java API リファレンス
description: エラーバーの種類を表します
type: docs
url: /ja/com.aspose.slides/errorbartype/
---
**継承:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class ErrorBarType extends System.Enum
```

エラーバーの種類を表します
## フィールド

| フィールド | 説明 |
| --- | --- |
| [Both](#Both) | エラーバーが正方向と負方向の両方に表示されることを指定します。 |
| [Minus](#Minus) | エラーバーが負方向のみに表示されることを指定します。 |
| [Plus](#Plus) | エラーバーが正方向のみに表示されることを指定します。 |
### Both {#Both}
```
public static final int Both
```

エラーバーが正方向と負方向の両方に表示されることを指定します。

### Minus {#Minus}
```
public static final int Minus
```

エラーバーが負方向のみに表示されることを指定します。

### Plus {#Plus}
```
public static final int Plus
```

エラーバーが正方向のみに表示されることを指定します。