---
title: ChartShapeType
second_title: Aspose.Slides for Android 用 Java API リファレンス
description: チャートの形状を表します。
type: docs
url: /ja/com.aspose.slides/chartshapetype/
---
**継承:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class ChartShapeType extends System.Enum
```

チャートの形状を表します。

## フィールド

| フィールド | 説明 |
| --- | --- |
| [NotDefined](#NotDefined) | チャートの形状は定義されていません。 |
| [Box](#Box) | チャートはボックス形状で描画されることを指定します。 |
| [Cone](#Cone) | チャートは円錐として描画され、円錐の底部が床にあり、円錐の先端がデータマーカーの上部にあります。 |
| [ConeToMax](#ConeToMax) | チャートは切頭円錐で描画され、円錐の先端が最大データ値になるように指定します。 |
| [Cylinder](#Cylinder) | チャートは円柱として描画されることを指定します。 |
| [Pyramid](#Pyramid) | チャートは長方形のピラミッドとして描画され、ピラミッドの底部が床にあり、ピラミッドの先端がデータマーカーの上部にあります。 |
| [PyramidToMaximum](#PyramidToMaximum) | チャートは切頭円錐で描画され、円錐の先端が最大データ値になるように指定します。 |

### NotDefined {#NotDefined}
```
public static final int NotDefined
```

チャートの形状は定義されていません。

### Box {#Box}
```
public static final int Box
```

チャートはボックス形状で描画されることを指定します。

### Cone {#Cone}
```
public static final int Cone
```

チャートは円錐として描画され、円錐の底部が床にあり、円錐の先端がデータマーカーの上部にあります。

### ConeToMax {#ConeToMax}
```
public static final int ConeToMax
```

チャートは切頭円錐で描画され、円錐の先端が最大データ値になるように指定します。

### Cylinder {#Cylinder}
```
public static final int Cylinder
```

チャートは円柱として描画されることを指定します。

### Pyramid {#Pyramid}
```
public static final int Pyramid
```

チャートは長方形のピラミッドとして描画され、ピラミッドの底部が床にあり、ピラミッドの先端がデータマーカーの上部にあります。

### PyramidToMaximum {#PyramidToMaximum}
```
public static final int PyramidToMaximum
```

チャートは切頭円錐で描画され、円錐の先端が最大データ値になるように指定します。