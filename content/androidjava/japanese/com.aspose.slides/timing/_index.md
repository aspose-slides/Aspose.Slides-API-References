---
title: Timing
second_title: Java API リファレンスによる Android 用 Aspose.Slides
description: アニメーションのタイミングを表します。
type: docs
url: /ja/com.aspose.slides/timing/
---
**継承:**
java.lang.Object

**実装されたすべてのインターフェイス:**
[com.aspose.slides.ITiming](../../com.aspose.slides/itiming), com.aspose.slides.IDOMObject
```
public class Timing implements ITiming, IDOMObject
```

アニメーションのタイミングを表します。
## メソッド

| メソッド | 説明 |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | 加速動作効果の期間のパーセンテージを示します。 |
| [setAccelerate(float value)](#setAccelerate-float-) | 加速動作効果の期間のパーセンテージを示します。 |
| [getDecelerate()](#getDecelerate--) | 減速動作効果の期間のパーセンテージを示します。 |
| [setDecelerate(float value)](#setDecelerate-float-) | 減速動作効果の期間のパーセンテージを示します。 |
| [getAutoReverse()](#getAutoReverse--) | 前方向に再生した後、自動的に逆方向にアニメーションを再生するかどうかを示します。 |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | 前方向に再生した後、自動的に逆方向にアニメーションを再生するかどうかを示します。 |
| [getDuration()](#getDuration--) | アニメーション効果の期間を示します。 |
| [setDuration(float value)](#setDuration-float-) | アニメーション効果の期間を示します。 |
| [getRepeatCount()](#getRepeatCount--) | 効果を繰り返す回数を示します。 |
| [setRepeatCount(float value)](#setRepeatCount-float-) | 効果を繰り返す回数を示します。 |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | この属性は、効果がスライドの最後まで繰り返されるかどうかを指定します。 |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | この属性は、効果がスライドの最後まで繰り返されるかどうかを指定します。 |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | この属性は、効果が次のクリックまで繰り返されるかどうかを指定します。 |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | この属性は、効果が次のクリックまで繰り返されるかどうかを指定します。 |
| [getRepeatDuration()](#getRepeatDuration--) | 効果を繰り返す回数を示します。 |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | 効果を繰り返す回数を示します。 |
| [getRestart()](#getRestart--) | 効果が完了した後に再起動するかどうかを指定します。 |
| [setRestart(int value)](#setRestart-int-) | 効果が完了した後に再起動するかどうかを指定します。 |
| [getRewind()](#getRewind--) | この属性は、再生が完了したときに効果を巻き戻すかどうかを指定します。 |
| [setRewind(boolean value)](#setRewind-boolean-) | この属性は、再生が完了したときに効果を巻き戻すかどうかを指定します。 |
| [getSpeed()](#getSpeed--) | タイミングを速める（または遅くする）パーセンテージを指定します。 |
| [setSpeed(float value)](#setSpeed-float-) | タイミングを速める（または遅くする）パーセンテージを指定します。 |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | トリガー後の遅延時間を示します。 |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | トリガー後の遅延時間を示します。 |
| [getTriggerType()](#getTriggerType--) | トリガータイプを示します。 |
| [setTriggerType(int value)](#setTriggerType-int-) | トリガータイプを示します。 |
| [getParent_Immediate()](#getParent-Immediate--) |  |

### getAccelerate() {#getAccelerate--}
```
public final float getAccelerate()
```

加速動作効果の期間のパーセンテージを示します。読み取り/書き込み可能 float.

**返り値:**
float
### setAccelerate(float value) {#setAccelerate-float-}
```
public final void setAccelerate(float value)
```

加速動作効果の期間のパーセンテージを示します。読み取り/書き込み可能 float.

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public final float getDecelerate()
```

減速動作効果の期間のパーセンテージを示します。読み取り/書き込み可能 float.

**返り値:**
float
### setDecelerate(float value) {#setDecelerate-float-}
```
public final void setDecelerate(float value)
```

減速動作効果の期間のパーセンテージを示します。読み取り/書き込み可能 float.

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public final boolean getAutoReverse()
```

前方向に再生した後、自動的に逆方向にアニメーションを再生するかどうかを示します。読み取り/書き込み可能 boolean。

**返り値:**
boolean
### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public final void setAutoReverse(boolean value)
```

前方向に再生した後、自動的に逆方向にアニメーションを再生するかどうかを示します。読み取り/書き込み可能 boolean。

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public final float getDuration()
```

アニメーション効果の期間を示します。読み取り/書き込み可能 float。

**返り値:**
float
### setDuration(float value) {#setDuration-float-}
```
public final void setDuration(float value)
```

アニメーション効果の期間を示します。読み取り/書き込み可能 float。

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public final float getRepeatCount()
```

効果を繰り返す回数を示します。読み取り/書き込み可能 float。

**返り値:**
float
### setRepeatCount(float value) {#setRepeatCount-float-}
```
public final void setRepeatCount(float value)
```

効果を繰り返す回数を示します。読み取り/書き込み可能 float。

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public final boolean getRepeatUntilEndSlide()
```

この属性は、効果がスライドの最後まで繰り返されるかどうかを指定します。読み取り/書き込み可能 boolean。

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Get the effects sequence for the first slide
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Get the first effect of main sequence.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Change the effect Timing/Repeat to "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**返り値:**
boolean
### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public final void setRepeatUntilEndSlide(boolean value)
```

この属性は、効果がスライドの最後まで繰り返されるかどうかを指定します。読み取り/書き込み可能 boolean。

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // 最初のスライドのエフェクトシーケンスを取得する
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // メインシーケンスの最初のエフェクトを取得する。
>      IEffect effect = effectsSequence.get_Item(0);
>      // エフェクトのタイミング/リピートを「Until End of Slide」に変更する
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public final boolean getRepeatUntilNextClick()
```

この属性は、効果が次のクリックまで繰り返されるかどうかを指定します。読み取り/書き込み可能 boolean。

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // 最初のスライドのエフェクトシーケンスを取得する
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // メインシーケンスの最初のエフェクトを取得する。
>      IEffect effect = effectsSequence.get_Item(0);
>      // エフェクトのタイミング/リピートを「Until Next Click」に変更する
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**返り値:**
boolean
### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public final void setRepeatUntilNextClick(boolean value)
```

この属性は、効果が次のクリックまで繰り返されるかどうかを指定します。読み取り/書き込み可能 boolean。

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // 最初のスライドのエフェクトシーケンスを取得する
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // メインシーケンスの最初のエフェクトを取得する。
>      IEffect effect = effectsSequence.get_Item(0);
>      // エフェクトのタイミング/リピートを「Until Next Click」に変更する
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public final float getRepeatDuration()
```

効果を繰り返す回数を示します。読み取り/書き込み可能 float。

**返り値:**
float
### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public final void setRepeatDuration(float value)
```

効果を繰り返す回数を示します。読み取り/書き込み可能 float。

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public final int getRestart()
```

効果が完了した後に再起動するかどうかを指定します。読み取り/書き込み可能 [EffectRestartType](../../com.aspose.slides/effectrestarttype)。

**返り値:**
int
### setRestart(int value) {#setRestart-int-}
```
public final void setRestart(int value)
```

効果が完了した後に再起動するかどうかを指定します。読み取り/書き込み可能 [EffectRestartType](../../com.aspose.slides/effectrestarttype)。

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public final boolean getRewind()
```

この属性は、再生が完了したときに効果を巻き戻すかどうかを指定します。読み取り/書き込み可能 boolean。

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Get the effects sequence for the first slide
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Get the first effect of main sequence.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Turn the effect Timing/Rewind on.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**返り値:**
boolean
### setRewind(boolean value) {#setRewind-boolean-}
```
public final void setRewind(boolean value)
```

この属性は、再生が完了したときに効果を巻き戻すかどうかを指定します。読み取り/書き込み可能 boolean。

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // 最初のスライドのエフェクトシーケンスを取得する
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // メインシーケンスの最初のエフェクトを取得する。
>      IEffect effect = effectsSequence.get_Item(0);
>      // エフェクトのタイミング/リワインドをオンにする。
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | boolean |  |

### getSpeed() {#getSpeed--}
```
public final float getSpeed()
```

タイミングを速める（または遅くする）パーセンテージを指定します。読み取り/書き込み可能 float。

**返り値:**
float
### setSpeed(float value) {#setSpeed-float-}
```
public final void setSpeed(float value)
```

タイミングを速める（または遅くする）パーセンテージを指定します。読み取り/書き込み可能 float。

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public final float getTriggerDelayTime()
```

トリガー後の遅延時間を示します。読み取り/書き込み可能 float。

**返り値:**
float
### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public final void setTriggerDelayTime(float value)
```

トリガー後の遅延時間を示します。読み取り/書き込み可能 float。

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public final int getTriggerType()
```

トリガータイプを示します。読み取り/書き込み可能 [EffectTriggerType](../../com.aspose.slides/effecttriggertype)。

**返り値:**
int
### setTriggerType(int value) {#setTriggerType-int-}
```
public final void setTriggerType(int value)
```

トリガータイプを示します。読み取り/書き込み可能 [EffectTriggerType](../../com.aspose.slides/effecttriggertype)。

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | int |  |

### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

Parent_Immediate オブジェクトを返します。読み取り専用 IDOMObject。

**返り値:**
com.aspose.slides.IDOMObject