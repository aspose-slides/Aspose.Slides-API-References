---
title: Timing
second_title: Aspose.Slides voor Android via Java API-referentie
description: Stelt animatietiming voor.
type: docs
url: /nl/com.aspose.slides/timing/
---
**Erfenis:**
java.lang.Object

**Alle geïmplementeerde interfaces:**
[com.aspose.slides.ITiming](../../com.aspose.slides/itiming), com.aspose.slides.IDOMObject
```
public class Timing implements ITiming, IDOMObject
```

Stelt animatietiming voor.

## Methoden

| Methode | Beschrijving |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | Beschrijft het percentage van de duur van het versnellen-gedrag effect. |
| [setAccelerate(float value)](#setAccelerate-float-) | Beschrijft het percentage van de duur van het versnellen-gedrag effect. |
| [getDecelerate()](#getDecelerate--) | Beschrijft het percentage van de duur van het vertragen-gedrag effect. |
| [setDecelerate(float value)](#setDecelerate-float-) | Beschrijft het percentage van de duur van het vertragen-gedrag effect. |
| [getAutoReverse()](#getAutoReverse--) | Beschrijft of de animatie automatisch in omgekeerde richting moet worden afgespeeld na afspelen in de voorwaartse richting. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | Beschrijft of de animatie automatisch in omgekeerde richting moet worden afgespeeld na afspelen in de voorwaartse richting. |
| [getDuration()](#getDuration--) | Beschrijft de duur van het animatie-effect. |
| [setDuration(float value)](#setDuration-float-) | Beschrijft de duur van het animatie-effect. |
| [getRepeatCount()](#getRepeatCount--) | Beschrijft het aantal keer dat het effect moet worden herhaald. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | Beschrijft het aantal keer dat het effect moet worden herhaald. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | Dit attribuut specificeert of het effect zal herhalen tot het einde van de dia. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | Dit attribuut specificeert of het effect zal herhalen tot het einde van de dia. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | Dit attribuut specificeert of het effect zal herhalen tot de volgende klik. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | Dit attribuut specificeert of het effect zal herhalen tot de volgende klik. |
| [getRepeatDuration()](#getRepeatDuration--) | Beschrijft het aantal keer dat het effect moet worden herhaald. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | Beschrijft het aantal keer dat het effect moet worden herhaald. |
| [getRestart()](#getRestart--) | Specificeert of een effect moet herstarten na voltooiing. |
| [setRestart(int value)](#setRestart-int-) | Specificeert of een effect moet herstarten na voltooiing. |
| [getRewind()](#getRewind--) | Dit attribuut specificeert of het effect terugspoelt wanneer het afspelen voltooid is. |
| [setRewind(boolean value)](#setRewind-boolean-) | Dit attribuut specificeert of het effect terugspoelt wanneer het afspelen voltooid is. |
| [getSpeed()](#getSpeed--) | Specificeert het percentage waarmee de timing moet worden versneld (of vertraagd). |
| [setSpeed(float value)](#setSpeed-float-) | Specificeert het percentage waarmee de timing moet worden versneld (of vertraagd). |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | Beschrijft de vertragingstijd na de trigger. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | Beschrijft de vertragingstijd na de trigger. |
| [getTriggerType()](#getTriggerType--) | Beschrijft het type trigger. |
| [setTriggerType(int value)](#setTriggerType-int-) | Beschrijft het type trigger. |
| [getParent_Immediate()](#getParent-Immediate--) |  |

### getAccelerate() {#getAccelerate--}
```
public final float getAccelerate()
```

Beschrijft het percentage van de duur van het versnellen-gedrag effect. Lezen/schrijven float.

**Retourneert:**
float
### setAccelerate(float value) {#setAccelerate-float-}
```
public final void setAccelerate(float value)
```

Beschrijft het percentage van de duur van het versnellen-gedrag effect. Lezen/schrijven float.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public final float getDecelerate()
```

Beschrijft het percentage van de duur van het vertragen-gedrag effect. Lezen/schrijven float.

**Retourneert:**
float
### setDecelerate(float value) {#setDecelerate-float-}
```
public final void setDecelerate(float value)
```

Beschrijft het percentage van de duur van het vertragen-gedrag effect. Lezen/schrijven float.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public final boolean getAutoReverse()
```

Beschrijft of de animatie automatisch in omgekeerde richting moet worden afgespeeld na afspelen in de voorwaartse richting. Lezen/schrijven boolean.

**Retourneert:**
boolean
### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public final void setAutoReverse(boolean value)
```

Beschrijft of de animatie automatisch in omgekeerde richting moet worden afgespeeld na afspelen in de voorwaartse richting. Lezen/schrijven boolean.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public final float getDuration()
```

Beschrijft de duur van het animatie-effect. Lezen/schrijven float.

**Retourneert:**
float
### setDuration(float value) {#setDuration-float-}
```
public final void setDuration(float value)
```

Beschrijft de duur van het animatie-effect. Lezen/schrijven float.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public final float getRepeatCount()
```

Beschrijft het aantal keer dat het effect moet worden herhaald. Lezen/schrijven float.

**Retourneert:**
float
### setRepeatCount(float value) {#setRepeatCount-float-}
```
public final void setRepeatCount(float value)
```

Beschrijft het aantal keer dat het effect moet worden herhaald. Lezen/schrijven float.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public final boolean getRepeatUntilEndSlide()
```

Dit attribuut specificeert of het effect zal herhalen tot het einde van de dia. Lezen/schrijven boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Haal de effectensequentie op voor de eerste dia
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Haal het eerste effect van de hoofdsequentie op.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Verander de Timing/Repeat van het effect naar "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Retourneert:**
boolean
### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public final void setRepeatUntilEndSlide(boolean value)
```

Dit attribuut specificeert of het effect zal herhalen tot het einde van de dia. Lezen/schrijven boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Haal de effectensequentie op voor de eerste dia
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Haal het eerste effect van de hoofdsequentie op.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Verander de Timing/Repeat van het effect naar "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public final boolean getRepeatUntilNextClick()
```

Dit attribuut specificeert of het effect zal herhalen tot de volgende klik. Lezen/schrijven boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Haal de effectensequentie op voor de eerste dia
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Haal het eerste effect van de hoofdsequentie op.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Verander de Timing/Repeat van het effect naar "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Retourneert:**
boolean
### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public final void setRepeatUntilNextClick(boolean value)
```

Dit attribuut specificeert of het effect zal herhalen tot de volgende klik. Lezen/schrijven boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Haal de effectensequentie op voor de eerste dia
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Haal het eerste effect van de hoofdsequentie op.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Verander de Timing/Repeat van het effect naar "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public final float getRepeatDuration()
```

Beschrijft het aantal keer dat het effect moet worden herhaald. Lezen/schrijven float.

**Retourneert:**
float
### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public final void setRepeatDuration(float value)
```

Beschrijft het aantal keer dat het effect moet worden herhaald. Lezen/schrijven float.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public final int getRestart()
```

Specificeert of een effect moet herstarten na voltooiing. Lezen/schrijven [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Retourneert:**
int
### setRestart(int value) {#setRestart-int-}
```
public final void setRestart(int value)
```

Specificeert of een effect moet herstarten na voltooiing. Lezen/schrijven [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public final boolean getRewind()
```

Dit attribuut specificeert of het effect terugspoelt wanneer het afspelen voltooid is. Lezen/schrijven boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Haal de effectensequentie op voor de eerste dia
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Haal het eerste effect van de hoofdsequentie op.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Zet de Timing/Rewind van het effect aan.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Retourneert:**
boolean
### setRewind(boolean value) {#setRewind-boolean-}
```
public final void setRewind(boolean value)
```

Dit attribuut specificeert of het effect terugspoelt wanneer het afspelen voltooid is. Lezen/schrijven boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Haal de effectensequentie op voor de eerste dia
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Haal het eerste effect van de hoofdsequentie op.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Zet de Timing/Rewind van het effect aan.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | boolean |  |

### getSpeed() {#getSpeed--}
```
public final float getSpeed()
```

Specificeert het percentage waarmee de timing moet worden versneld (of vertraagd). Lezen/schrijven float.

**Retourneert:**
float
### setSpeed(float value) {#setSpeed-float-}
```
public final void setSpeed(float value)
```

Specificeert het percentage waarmee de timing moet worden versneld (of vertraagd). Lezen/schrijven float.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public final float getTriggerDelayTime()
```

Beschrijft de vertragingstijd na de trigger. Lezen/schrijven float.

**Retourneert:**
float
### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public final void setTriggerDelayTime(float value)
```

Beschrijft de vertragingstijd na de trigger. Lezen/schrijven float.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public final int getTriggerType()
```

Beschrijft het type trigger. Lezen/schrijven [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Retourneert:**
int
### setTriggerType(int value) {#setTriggerType-int-}
```
public final void setTriggerType(int value)
```

Beschrijft het type trigger. Lezen/schrijven [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | int |  |

### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

Retourneert Parent_Immediate object. Alleen-lezen IDOMObject.

**Retourneert:**
com.aspose.slides.IDOMObject