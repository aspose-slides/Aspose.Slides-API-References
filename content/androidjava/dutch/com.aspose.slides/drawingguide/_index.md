---
title: DrawingGuide
second_title: Aspose.Slides voor Android via Java API-referentie
description: Stelt een aanpasbare tekengids voor.
type: docs
url: /nl/com.aspose.slides/drawingguide/
---
**Overerving:**
java.lang.Object

**Alle geïmplementeerde interfaces:**
[com.aspose.slides.IDrawingGuide](../../com.aspose.slides/idrawingguide)
```
public final class DrawingGuide implements IDrawingGuide
```

Stelt een aanpasbare tekengids voor.
## Methoden

| Methode | Beschrijving |
| --- | --- |
| [getOrientation()](#getOrientation--) | Geeft de oriëntatie van de tekengids terug of stelt deze in. |
| [setOrientation(byte value)](#setOrientation-byte-) | Geeft de oriëntatie van de tekengids terug of stelt deze in. |
| [getPosition()](#getPosition--) | Geeft de positie van de tekengids terug of stelt deze in, in punten vanaf de boven-linkerhoek van de dia. |
| [setPosition(float value)](#setPosition-float-) | Geeft de positie van de tekengids terug of stelt deze in, in punten vanaf de boven-linkerhoek van de dia. |
| [getColor()](#getColor--) | Geeft de kleur van de tekengids terug of stelt deze in. |
| [setColor(Integer value)](#setColor-java.lang.Integer-) | Geeft de kleur van de tekengids terug of stelt deze in. |
### getOrientation() {#getOrientation--}
```
public final byte getOrientation()
```

Geeft de oriëntatie van de tekengids terug of stelt deze in. Lezen/schrijven [Orientation](../../com.aspose.slides/orientation).

**Retour:**
byte
### setOrientation(byte value) {#setOrientation-byte-}
```
public final void setOrientation(byte value)
```

Geeft de oriëntatie van de tekengids terug of stelt deze in. Lezen/schrijven [Orientation](../../com.aspose.slides/orientation).

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | byte |  |

### getPosition() {#getPosition--}
```
public final float getPosition()
```

Geeft de positie van de tekengids terug of stelt deze in, in punten vanaf de boven-linkerhoek van de dia. Lezen/schrijven float.

--------------------

Het typische waardebereik is van nul tot de slide-hoogte voor een horizontale gids en van nul tot de slide-breedte voor een verticale gids.

**Retour:**
float
### setPosition(float value) {#setPosition-float-}
```
public final void setPosition(float value)
```

Geeft de positie van de tekengids terug of stelt deze in, in punten vanaf de boven-linkerhoek van de dia. Lezen/schrijven float.

--------------------

Het typische waardebereik is van nul tot de slide-hoogte voor een horizontale gids en van nul tot de slide-breedte voor een verticale gids.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | float |  |

### getColor() {#getColor--}
```
public final Integer getColor()
```

Geeft de kleur van de tekengids terug of stelt deze in. Lezen/schrijven java.lang.Integer.

**Retour:**
java.lang.Integer
### setColor(Integer value) {#setColor-java.lang.Integer-}
```
public final void setColor(Integer value)
```

Geeft de kleur van de tekengids terug of stelt deze in. Lezen/schrijven java.lang.Integer.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | java.lang.Integer |  |