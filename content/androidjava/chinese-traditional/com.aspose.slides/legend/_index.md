---
title: Legend
second_title: Aspose.Slides for Android via Java API 參考
description: 代表圖表圖例屬性。
type: docs
url: /zh-hant/com.aspose.slides/legend/
---
**繼承:**  
java.lang.Object, com.aspose.slides.DomObject

**所有已實作的介面:**  
[com.aspose.slides.ILegend](../../com.aspose.slides/ilegend)  
```
public class Legend extends DomObject<Chart> implements ILegend
```

表示圖例的屬性。
## 方法

| 方法 | 說明 |
| --- | --- |
| [getX()](#getX--) | 返回或設定圖例的 x 座標，作為圖表寬度的比例。 |
| [setX(float value)](#setX-float-) | 返回或設定圖例的 x 座標，作為圖表寬度的比例。 |
| [getY()](#getY--) | 返回或設定圖例的 y 座標，作為圖表高度的比例。 |
| [setY(float value)](#setY-float-) | 返回或設定圖例的 y 座標，作為圖表高度的比例。 |
| [getWidth()](#getWidth--) | 返回或設定圖例的寬度，作為圖表寬度的比例。 |
| [setWidth(float value)](#setWidth-float-) | 返回或設定圖例的寬度，作為圖表寬度的比例。 |
| [getHeight()](#getHeight--) | 返回或設定圖例的高度，作為圖表高度的比例。 |
| [setHeight(float value)](#setHeight-float-) | 返回或設定圖例的高度，作為圖表高度的比例。 |
| [getRight()](#getRight--) | 右側。 |
| [getBottom()](#getBottom--) | 底部。 |
| [getOverlay()](#getOverlay--) | 判斷是否允許其他圖表元素覆蓋圖例。 |
| [setOverlay(boolean value)](#setOverlay-boolean-) | 判斷是否允許其他圖表元素覆蓋圖例。 |
| [getTextFormat()](#getTextFormat--) | 文字格式。 |
| [getPosition()](#getPosition--) | 指定圖例在圖表上的位置。 |
| [setPosition(int value)](#setPosition-int-) | 指定圖例在圖表上的位置。 |
| [getFormat()](#getFormat--) | 返回圖例的格式。 |
| [getChart()](#getChart--) | 返回圖表。 |
| [getEntries()](#getEntries--) | 取得圖例項目。 |
| [getActualX()](#getActualX--) | 指定圖表元素相對於圖表左上角的實際 x 位置（左）。 |
| [getActualY()](#getActualY--) | 指定圖表元素相對於圖表左上角的實際頂部位置。 |
| [getActualWidth()](#getActualWidth--) | 指定圖表元素的實際寬度。 |
| [getActualHeight()](#getActualHeight--) | 指定圖表元素的實際高度。 |
| [getSlide()](#getSlide--) | 返回 FillFormat 的父投影片。 |
| [getPresentation()](#getPresentation--) | 返回 FillFormat 的父簡報。 |

### getX() {#getX--}
```
public final float getX()
```

返回或設定圖例的 x 座標，作為圖表寬度的比例。可讀寫 float.

**傳回:**  
float

### setX(float value) {#setX-float-}
```
public final void setX(float value)
```

返回或設定圖例的 x 座標，作為圖表寬度的比例。可讀寫 float.

**參數:**  
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | float |  |

### getY() {#getY--}
```
public final float getY()
```

返回或設定圖例的 y 座標，作為圖表高度的比例。可讀寫 float.

**傳回:**  
float

### setY(float value) {#setY-float-}
```
public final void setY(float value)
```

返回或設定圖例的 y 座標，作為圖表高度的比例。可讀寫 float.

**參數:**  
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | float |  |

### getWidth() {#getWidth--}
```
public final float getWidth()
```

返回或設定圖例的寬度，作為圖表寬度的比例。可讀寫 float.

**傳回:**  
float

### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```

返回或設定圖例的寬度，作為圖表寬度的比例。可讀寫 float.

**參數:**  
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | float |  |

### getHeight() {#getHeight--}
```
public final float getHeight()
```

返回或設定圖例的高度，作為圖表高度的比例。可讀寫 float.

**傳回:**  
float

### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```

返回或設定圖例的高度，作為圖表高度的比例。可讀寫 float.

**參數:**  
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | float |  |

### getRight() {#getRight--}
```
public final float getRight()
```

右側。唯讀 float。

**傳回:**  
float

### getBottom() {#getBottom--}
```
public final float getBottom()
```

底部。唯讀 float。

**傳回:**  
float

### getOverlay() {#getOverlay--}
```
public final boolean getOverlay()
```

判斷是否允許其他圖表元素覆蓋圖例。可讀寫 boolean。

**傳回:**  
boolean

### setOverlay(boolean value) {#setOverlay-boolean-}
```
public final void setOverlay(boolean value)
```

判斷是否允許其他圖表元素覆蓋圖例。可讀寫 boolean。

**參數:**  
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | boolean |  |

### getTextFormat() {#getTextFormat--}
```
public final IChartTextFormat getTextFormat()
```

文字格式。唯讀 [IChartTextFormat](../../com.aspose.slides/icharttextformat)。

**傳回:**  
[IChartTextFormat](../../com.aspose.slides/icharttextformat)

### getPosition() {#getPosition--}
```
public final int getPosition()
```

指定圖例在圖表上的位置。X、Y、Width、Height 屬性的非 NaN 值將覆寫此屬性的效果。可讀寫 [LegendPositionType](../../com.aspose.slides/legendpositiontype)。

**傳回:**  
int

### setPosition(int value) {#setPosition-int-}
```
public final void setPosition(int value)
```

指定圖例在圖表上的位置。X、Y、Width、Height 屬性的非 NaN 值將覆寫此屬性的效果。可讀寫 [LegendPositionType](../../com.aspose.slides/legendpositiontype)。

**參數:**  
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | int |  |

### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

返回圖例的格式。唯讀 [IFormat](../../com.aspose.slides/iformat)。

**傳回:**  
[IFormat](../../com.aspose.slides/iformat)

### getChart() {#getChart--}
```
public final IChart getChart()
```

返回圖表。唯讀 [IChart](../../com.aspose.slides/ichart)。

**傳回:**  
[IChart](../../com.aspose.slides/ichart)

### getEntries() {#getEntries--}
```
public final ILegendEntryCollection getEntries()
```

取得圖例項目。唯讀 [ILegendEntryCollection](../../com.aspose.slides/ilegendentrycollection)。

**傳回:**  
[ILegendEntryCollection](../../com.aspose.slides/ilegendentrycollection)

### getActualX() {#getActualX--}
```
public final float getActualX()
```

指定圖表元素相對於圖表左上角的實際 x 位置（左）。在取得實際值之前，請先呼叫 IChart.validateChartLayout() 方法。唯讀 float。

**傳回:**  
float

### getActualY() {#getActualY--}
```
public final float getActualY()
```

指定圖表元素相對於圖表左上角的實際頂部位置。在取得實際值之前，請先呼叫 IChart.validateChartLayout() 方法。唯讀 float。

**傳回:**  
float

### getActualWidth() {#getActualWidth--}
```
public final float getActualWidth()
```

指定圖表元素的實際寬度。在取得實際值之前，請先呼叫 IChart.validateChartLayout() 方法。唯讀 float。

**傳回:**  
float

### getActualHeight() {#getActualHeight--}
```
public final float getActualHeight()
```

指定圖表元素的實際高度。在取得實際值之前，請先呼叫 IChart.validateChartLayout() 方法。唯讀 float。

**傳回:**  
float

### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

返回 FillFormat 的父投影片。唯讀 [BaseSlide](../../com.aspose.slides/baseslide)。

**傳回:**  
[IBaseSlide](../../com.aspose.slides/ibaseslide)

### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

返回 FillFormat 的父簡報。唯讀 [IPresentation](../../com.aspose.slides/ipresentation)。

**傳回:**  
[IPresentation](../../com.aspose.slides/ipresentation)