---
title: ILayoutable
second_title: Aspose.Slides for Android via Java API 參考
description: 指定圖表元素的精確位置。
type: docs
url: /zh-hant/com.aspose.slides/ilayoutable/
---
**所有已實作的介面：**
[com.aspose.slides.IChartComponent](../../com.aspose.slides/ichartcomponent)
```
public interface ILayoutable extends IChartComponent
```

指定圖表元素的精確位置。
## 方法

| 方法 | 說明 |
| --- | --- |
| [getX()](#getX--) | 指定圖表元素的 x 位置（左側），以圖表寬度的比例表示。 |
| [setX(float value)](#setX-float-) | 指定圖表元素的 x 位置（左側），以圖表寬度的比例表示。 |
| [getY()](#getY--) | 指定圖表元素的上緣，以圖表高度的比例表示。 |
| [setY(float value)](#setY-float-) | 指定圖表元素的上緣，以圖表高度的比例表示。 |
| [getWidth()](#getWidth--) | 指定圖表元素的寬度，以圖表寬度的比例表示。 |
| [setWidth(float value)](#setWidth-float-) | 指定圖表元素的寬度，以圖表寬度的比例表示。 |
| [getHeight()](#getHeight--) | 指定圖表元素的高度，以圖表高度的比例表示。 |
| [setHeight(float value)](#setHeight-float-) | 指定圖表元素的高度，以圖表高度的比例表示。 |
| [getRight()](#getRight--) | 取得圖表元素的右側，以圖表寬度的比例表示。 |
| [getBottom()](#getBottom--) | 取得圖表元素的上緣，以圖表高度的比例表示。 |
### getX() {#getX--}
```
public abstract float getX()
```

指定圖表元素的 x 位置（左側），以圖表寬度的比例表示。 可讀寫 float。

**傳回：**
float
### setX(float value) {#setX-float-}
```
public abstract void setX(float value)
```

指定圖表元素的 x 位置（左側），以圖表寬度的比例表示。 可讀寫 float。

**參數：**
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | float |  |

### getY() {#getY--}
```
public abstract float getY()
```

指定圖表元素的上緣，以圖表高度的比例表示。 可讀寫 float。

**傳回：**
float
### setY(float value) {#setY-float-}
```
public abstract void setY(float value)
```

指定圖表元素的上緣，以圖表高度的比例表示。 可讀寫 float。

**參數：**
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | float |  |

### getWidth() {#getWidth--}
```
public abstract float getWidth()
```

指定圖表元素的寬度，以圖表寬度的比例表示。 可讀寫 float。

**傳回：**
float
### setWidth(float value) {#setWidth-float-}
```
public abstract void setWidth(float value)
```

指定圖表元素的寬度，以圖表寬度的比例表示。 可讀寫 float。

**參數：**
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | float |  |

### getHeight() {#getHeight--}
```
public abstract float getHeight()
```

指定圖表元素的高度，以圖表高度的比例表示。 可讀寫 float。

**傳回：**
float
### setHeight(float value) {#setHeight-float-}
```
public abstract void setHeight(float value)
```

指定圖表元素的高度，以圖表高度的比例表示。 可讀寫 float。

**參數：**
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | float |  |

### getRight() {#getRight--}
```
public abstract float getRight()
```

取得圖表元素的右側，以圖表寬度的比例表示。 唯讀 float。

**傳回：**
float
### getBottom() {#getBottom--}
```
public abstract float getBottom()
```

取得圖表元素的上緣，以圖表高度的比例表示。 唯讀 float。

**傳回：**
float