---
title: ChartPlotArea
second_title: Aspose.Slides for Android 透過 Java API 參考
description: 表示圖表應繪製的矩形區域。
type: docs
url: /zh-hant/com.aspose.slides/chartplotarea/
---
**繼承:**
java.lang.Object, com.aspose.slides.DomObject

**已實作的介面:**
[com.aspose.slides.IChartPlotArea](../../com.aspose.slides/ichartplotarea)
```
public class ChartPlotArea extends DomObject<Chart> implements IChartPlotArea
```

表示圖表應繪製的矩形區域。
## 方法

| 方法 | 描述 |
| --- | --- |
| [getFormat()](#getFormat--) | 傳回圖表區域的格式。 |
| [getX()](#getX--) | 以圖表寬度的比例（0 到 1）傳回或設定圖表區域邊界框左上角的 x 座標。 |
| [setX(float value)](#setX-float-) | 以圖表寬度的比例（0 到 1）傳回或設定圖表區域邊界框左上角的 x 座標。 |
| [getY()](#getY--) | 以圖表高度的比例（0 到 1）傳回或設定圖表區域邊界框左上角的 y 座標。 |
| [setY(float value)](#setY-float-) | 以圖表高度的比例（0 到 1）傳回或設定圖表區域邊界框左上角的 y 座標。 |
| [getWidth()](#getWidth--) | 以圖表寬度的比例（0 到 1）傳回或設定圖表區域邊界框的寬度。 |
| [setWidth(float value)](#setWidth-float-) | 以圖表寬度的比例（0 到 1）傳回或設定圖表區域邊界框的寬度。 |
| [getHeight()](#getHeight--) | 以圖表高度的比例（0 到 1）傳回或設定圖表區域邊界框的高度。 |
| [setHeight(float value)](#setHeight-float-) | 以圖表高度的比例（0 到 1）傳回或設定圖表區域邊界框的高度。 |
| [getRight()](#getRight--) | Right. |
| [getBottom()](#getBottom--) | Bottom. |
| [getChart()](#getChart--) | Chart. |
| [isLocationAutocalculated()](#isLocationAutocalculated--) | 定義如何計算位置：true – 自動計算；由 X、Y、Width、Height 屬性定義。 |
| [getLayoutTargetType()](#getLayoutTargetType--) | 若手動定義圖表區域的版面，此屬性指定是依內部（不含座標軸及座標軸標籤）還是外部（含座標軸及座標軸標籤）進行版面配置。 |
| [setLayoutTargetType(int value)](#setLayoutTargetType-int-) | 若手動定義圖表區域的版面，此屬性指定是依內部（不含座標軸及座標軸標籤）還是外部（含座標軸及座標軸標籤）進行版面配置。 |
| [getActualX()](#getActualX--) | 指定圖表元素相對於圖表左上角的實際 x 位置（左）。 |
| [getActualY()](#getActualY--) | 指定圖表元素相對於圖表左上角的實際上緣位置。 |
| [getActualWidth()](#getActualWidth--) | 指定圖表元素的實際寬度。 |
| [getActualHeight()](#getActualHeight--) | 指定圖表元素的實際高度。 |
| [getSlide()](#getSlide--) | 傳回 FillFormat 的父投影片。 |
| [getPresentation()](#getPresentation--) | 傳回 FillFormat 的父簡報。 |
### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```


傳回圖表區域的格式。 唯讀 [IFormat](../../com.aspose.slides/iformat)。

**傳回值:**
[IFormat](../../com.aspose.slides/iformat)
### getX() {#getX--}
```
public final float getX()
```


以圖表寬度的比例（0 到 1）傳回或設定圖表區域邊界框左上角的 x 座標。 讀寫 float。

**傳回值:**
float
### setX(float value) {#setX-float-}
```
public final void setX(float value)
```


以圖表寬度的比例（0 到 1）傳回或設定圖表區域邊界框左上角的 x 座標。 讀寫 float。

**參數:**
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | float |  |

### getY() {#getY--}
```
public final float getY()
```


以圖表高度的比例（0 到 1）傳回或設定圖表區域邊界框左上角的 y 座標。 讀寫 float。

**傳回值:**
float
### setY(float value) {#setY-float-}
```
public final void setY(float value)
```


以圖表高度的比例（0 到 1）傳回或設定圖表區域邊界框左上角的 y 座標。 讀寫 float。

**參數:**
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | float |  |

### getWidth() {#getWidth--}
```
public final float getWidth()
```


以圖表寬度的比例（0 到 1）傳回或設定圖表區域邊界框的寬度。 讀寫 float。

**傳回值:**
float
### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```


以圖表寬度的比例（0 到 1）傳回或設定圖表區域邊界框的寬度。 讀寫 float。

**參數:**
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | float |  |

### getHeight() {#getHeight--}
```
public final float getHeight()
```


以圖表高度的比例（0 到 1）傳回或設定圖表區域邊界框的高度。 讀寫 float。

**傳回值:**
float
### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```


以圖表高度的比例（0 到 1）傳回或設定圖表區域邊界框的高度。 讀寫 float。

**參數:**
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | float |  |

### getRight() {#getRight--}
```
public final float getRight()
```


Right. 唯讀 float。

**傳回值:**
float
### getBottom() {#getBottom--}
```
public final float getBottom()
```


Bottom. 唯讀 float。

**傳回值:**
float
### getChart() {#getChart--}
```
public final IChart getChart()
```


Chart. 唯讀 [IChart](../../com.aspose.slides/ichart)。

**傳回值:**
[IChart](../../com.aspose.slides/ichart)
### isLocationAutocalculated() {#isLocationAutocalculated--}
```
public final boolean isLocationAutocalculated()
```


定義如何計算位置：true – 自動計算；由 X、Y、Width、Height 屬性定義。 唯讀 boolean。

**傳回值:**
boolean
### getLayoutTargetType() {#getLayoutTargetType--}
```
public final int getLayoutTargetType()
```


若手動定義圖表區域的版面，此屬性指定是依內部（不含座標軸及座標軸標籤）還是外部（含座標軸及座標軸標籤）進行版面配置。 讀寫 [LayoutTargetType](../../com.aspose.slides/layouttargettype)(\#getLayoutTargetType.getLayoutTargetType/\#setLayoutTargetType(int).setLayoutTargetType(int))。

--------------------

> ```
> Presentation presentation = new Presentation();
>   try
>   {
>       ISlide slide = presentation.getSlides().get_Item(0);
>       IChart chart = slide.getShapes().addChart(ChartType.ClusteredColumn, 20, 100, 600, 400);
>       chart.getPlotArea().setX(0.2f);
>       chart.getPlotArea().setY(0.2f);
>       chart.getPlotArea().setWidth(0.7f);
>       chart.getPlotArea().setHeight(0.7f);
>       chart.getPlotArea().setLayoutTargetType(LayoutTargetType.Inner);
>       ...
>   } finally {
>       if (presentation != null) presentation.dispose();
>   }
> ```

**傳回值:**
int
### setLayoutTargetType(int value) {#setLayoutTargetType-int-}
```
public final void setLayoutTargetType(int value)
```


若手動定義圖表區域的版面，此屬性指定是依內部（不含座標軸及座標軸標籤）還是外部（含座標軸及座標軸標籤）進行版面配置。 讀寫 [LayoutTargetType](../../com.aspose.slides/layouttargettype)(\#getLayoutTargetType.getLayoutTargetType/\#setLayoutTargetType(int).setLayoutTargetType(int))。

--------------------

> ```
> Presentation presentation = new Presentation();
>   try
>   {
>       ISlide slide = presentation.getSlides().get_Item(0);
>       IChart chart = slide.getShapes().addChart(ChartType.ClusteredColumn, 20, 100, 600, 400);
>       chart.getPlotArea().setX(0.2f);
>       chart.getPlotArea().setY(0.2f);
>       chart.getPlotArea().setWidth(0.7f);
>       chart.getPlotArea().setHeight(0.7f);
>       chart.getPlotArea().setLayoutTargetType(LayoutTargetType.Inner);
>       ...
>   } finally {
>       if (presentation != null) presentation.dispose();
>   }
> ```

**參數:**
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | int |  |

### getActualX() {#getActualX--}
```
public final float getActualX()
```


指定圖表元素相對於圖表左上角的實際 x 位置（左）。在取得實際值前，請先呼叫 IChart.ValidateChartLayout() 方法。 讀 float。

**傳回值:**
float
### getActualY() {#getActualY--}
```
public final float getActualY()
```


指定圖表元素相對於圖表左上角的實際上緣位置。在取得實際值前，請先呼叫 IChart.ValidateChartLayout() 方法。 讀 float。

**傳回值:**
float
### getActualWidth() {#getActualWidth--}
```
public final float getActualWidth()
```


指定圖表元素的實際寬度。在取得實際值前，請先呼叫 IChart.ValidateChartLayout() 方法。 讀 float。

**傳回值:**
float
### getActualHeight() {#getActualHeight--}
```
public final float getActualHeight()
```


指定圖表元素的實際高度。在取得實際值前，請先呼叫 IChart.ValidateChartLayout() 方法。 讀 float。

**傳回值:**
float
### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```


傳回 FillFormat 的父投影片。 唯讀 [BaseSlide](../../com.aspose.slides/baseslide)。

**傳回值:**
[IBaseSlide](../../com.aspose.slides/ibaseslide)
### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```


傳回 FillFormat 的父簡報。 唯讀 [IPresentation](../../com.aspose.slides/ipresentation)。

**傳回值:**
[IPresentation](../../com.aspose.slides/ipresentation)