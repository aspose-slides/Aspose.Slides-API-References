---
title: NormalViewRestoredProperties
second_title: Aspose.Slides for Android 透過 Java API 參考
description: 指定投影片區域的尺寸（在 normal view 中，當為 restoredTop 的子項時為寬度，當為 restoredLeft 的子項時為高度），當區域具有可變的還原大小（既未最小化亦未最大化）時。
type: docs
url: /zh-hant/com.aspose.slides/normalviewrestoredproperties/
---
**繼承關係:**
java.lang.Object

**全部實作的介面:**
[com.aspose.slides.INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)
```
public class NormalViewRestoredProperties implements INormalViewRestoredProperties
```

指定投影片區域的大小（在 normal view 中，當為 restoredTop 的子項時為寬度，當為 restoredLeft 的子項時為高度），當區域具有可變的還原大小（既未最小化亦未最大化）時。

## 方法

| 方法 | 說明 |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | 指定投影片區域的大小（當為 RestoredTop 的子項時為寬度，當為 RestoredLeft 的子項時為高度）。 |
| [setDimensionSize(float value)](#setDimensionSize-float-) | 指定投影片區域的大小（當為 RestoredTop 的子項時為寬度，當為 RestoredLeft 的子項時為高度）。 |
| [getAutoAdjust()](#getAutoAdjust--) | 指定在調整應用程式內視圖所屬視窗大小時，側邊內容區域的大小是否應補償新尺寸。可讀寫 boolean。 |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | 指定在調整應用程式內視圖所屬視窗大小時，側邊內容區域的大小是否應補償新尺寸。可讀寫 boolean。 |
### getDimensionSize() {#getDimensionSize--}
```
public final float getDimensionSize()
```

指定投影片區域的大小（當為 RestoredTop 的子項時為寬度，當為 RestoredLeft 的子項時為高度）。可讀寫 float。

**回傳值:**
float
### setDimensionSize(float value) {#setDimensionSize-float-}
```
public final void setDimensionSize(float value)
```

指定投影片區域的大小（當為 RestoredTop 的子項時為寬度，當為 RestoredLeft 的子項時為高度）。可讀寫 float。

**參數:**
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public final boolean getAutoAdjust()
```

指定在調整應用程式內視圖所屬視窗大小時，側邊內容區域的大小是否應補償新尺寸。可讀寫 boolean。

**回傳值:**
boolean
### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public final void setAutoAdjust(boolean value)
```

指定在調整應用程式內視圖所屬視窗大小時，側邊內容區域的大小是否應補償新尺寸。可讀寫 boolean。

**參數:**
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | boolean |  |