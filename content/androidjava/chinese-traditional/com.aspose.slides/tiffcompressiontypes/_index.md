---
title: TiffCompressionTypes
second_title: Aspose.Slides for Android 透過 Java API 參考
description: 提供控制簡報以 TIFF 格式壓縮方式的選項。
type: docs
url: /zh-hant/com.aspose.slides/tiffcompressiontypes/
---
**繼承:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class TiffCompressionTypes extends System.Enum
```

提供控制呈現檔以 TIFF 格式壓縮方式的選項。
## 欄位

| 欄位 | 說明 |
| --- | --- |
| [Default](#Default) | 指定預設的壓縮方案 (LZW)。 |
| [None](#None) | 指定不使用壓縮。 |
| [CCITT3](#CCITT3) | 指定 CCITT3 壓縮方案。 |
| [CCITT4](#CCITT4) | 指定 CCITT4 壓縮方案。 |
| [LZW](#LZW) | 指定 LZW 壓縮方案 (預設)。 |
| [RLE](#RLE) | 指定 RLE 壓縮方案。 |
### Default {#Default}
```
public static final int Default
```

指定預設的壓縮方案 (LZW)。

### None {#None}
```
public static final int None
```

指定不使用壓縮。

### CCITT3 {#CCITT3}
```
public static final int CCITT3
```

指定 CCITT3 壓縮方案。

### CCITT4 {#CCITT4}
```
public static final int CCITT4
```

指定 CCITT4 壓縮方案。

### LZW {#LZW}
```
public static final int LZW
```

指定 LZW 壓縮方案 (預設)。

### RLE {#RLE}
```
public static final int RLE
```

指定 RLE 壓縮方案。