---
title: TextSearchOptions
second_title: Aspose.Slides 用於 Android 的 Java API 參考
description: 表示可用於在簡報投影片或 TextFrame 中搜尋文字的選項。
type: docs
url: /zh-hant/com.aspose.slides/textsearchoptions/
---
**Inheritance:**
java.lang.Object

**All Implemented Interfaces:**
[com.aspose.slides.ITextSearchOptions](../../com.aspose.slides/itextsearchoptions)
```
public class TextSearchOptions implements ITextSearchOptions
```

表示可用於在 Presentation、Slide 或 TextFrame 中搜尋文字的選項。

## 建構函式

| 建構式 | 說明 |
| --- | --- |
| [TextSearchOptions()](#TextSearchOptions--) | 建立新的預設文字搜尋選項。 |

## 方法

| 方法 | 說明 |
| --- | --- |
| [getCaseSensitive()](#getCaseSensitive--) | 設定為 true 以使用區分大小寫的搜尋，否則為 false。 |
| [setCaseSensitive(boolean value)](#setCaseSensitive-boolean-) | 設定為 true 以使用區分大小寫的搜尋，否則為 false。 |
| [getWholeWordsOnly()](#getWholeWordsOnly--) | 設定為 true 以僅匹配完整單詞，否則為 false。 |
| [setWholeWordsOnly(boolean value)](#setWholeWordsOnly-boolean-) | 設定為 true 以僅匹配完整單詞，否則為 false。 |
| [getIncludeNotes()](#getIncludeNotes--) | 設定為 true 以在執行文字搜尋、取代或突顯操作時，包含投影片備註中的文字（[NotesSlide](../../com.aspose.slides/notesslide)）。 |
| [setIncludeNotes(boolean value)](#setIncludeNotes-boolean-) | 設定為 true 以在執行文字搜尋、取代或突顯操作時，包含投影片備註中的文字（[NotesSlide](../../com.aspose.slides/notesslide)）。 |

### TextSearchOptions() {#TextSearchOptions--}
```
public TextSearchOptions()
```

建立新的預設文字搜尋選項。

### getCaseSensitive() {#getCaseSensitive--}
```
public final boolean getCaseSensitive()
```

設定為 true 以使用區分大小寫的搜尋，否則為 false。讀/寫  boolean 。

**返回值:**
boolean

### setCaseSensitive(boolean value) {#setCaseSensitive-boolean-}
```
public final void setCaseSensitive(boolean value)
```

設定為 true 以使用區分大小寫的搜尋，否則為 false。讀/寫  boolean 。

**參數:**
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | boolean |  |

### getWholeWordsOnly() {#getWholeWordsOnly--}
```
public final boolean getWholeWordsOnly()
```

設定為 true 以僅匹配完整單詞，否則為 false。讀/寫  boolean 。

**返回值:**
boolean

### setWholeWordsOnly(boolean value) {#setWholeWordsOnly-boolean-}
```
public final void setWholeWordsOnly(boolean value)
```

設定為 true 以僅匹配完整單詞，否則為 false。讀/寫  boolean 。

**參數:**
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | boolean |  |

### getIncludeNotes() {#getIncludeNotes--}
```
public final boolean getIncludeNotes()
```

設定為 true 以在執行文字搜尋、取代或突顯操作時，包含投影片備註中的文字（[NotesSlide](../../com.aspose.slides/notesslide)）。預設值為 false。

**返回值:**
boolean

### setIncludeNotes(boolean value) {#setIncludeNotes-boolean-}
```
public final void setIncludeNotes(boolean value)
```

設定為 true 以在執行文字搜尋、取代或突顯操作時，包含投影片備註中的文字（[NotesSlide](../../com.aspose.slides/notesslide)）。預設值為 false。

**參數:**
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | boolean |  |