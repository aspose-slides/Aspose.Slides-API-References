---
title: SvgEvent
second_title: Aspose.Slides for Android 的 Java API 參考
description: 表示 SVG 圖形的選項。
type: docs
url: /zh-hant/com.aspose.slides/svgevent/
---
**繼承:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class SvgEvent extends System.Enum
```

表示 SVG 圖形的選項。  
## Fields

| Field | Description |
| --- | --- |
| [OnFocusIn](#OnFocusIn) | 當元素取得焦點時發生，例如文字被選取時。 |
| [OnFocusOut](#OnFocusOut) | 當元素失去焦點時發生，例如文字被取消選取時。 |
| [OnActivate](#OnActivate) | 當元素被啟用時發生，例如透過滑鼠點擊或按鍵操作。 |
| [OnClick](#OnClick) | 當指向裝置的按鈕在元素上被點擊時發生。 |
| [OnMouseDown](#OnMouseDown) | 當指向裝置的按鈕在元素上被按下時發生。 |
| [OnMouseUp](#OnMouseUp) | 當指向裝置的按鈕在元素上被釋放時發生。 |
| [OnMouseOver](#OnMouseOver) | 當指向裝置移動到元素上時發生。 |
| [OnMouseMove](#OnMouseMove) | 當指向裝置在元素上移動時發生。 |
| [OnMouseOut](#OnMouseOut) | 當指向裝置離開元素時發生。 |
| [OnLoad](#OnLoad) | 當使用者代理已完整解析該元素及其所有子元素與所有參考的資源，且可渲染時發生。 |
| [OnUnload](#OnUnload) | 當 DOM 實作從視窗或框架中移除文件時發生。 |
| [OnAbort](#OnAbort) | 當在元素完全載入之前，頁面載入被停止時發生。 |
| [OnError](#OnError) | 當元素未正確載入或腳本執行期間發生錯誤時發生。 |
| [OnResize](#OnResize) | 當文件檢視正在調整大小時發生。 |
| [OnScroll](#OnScroll) | 當文件檢視在 X 軸、Y 軸或兩者上平移時發生。 |
| [OnZoom](#OnZoom) | 當文件檢視的縮放等級變更時發生。 |
| [OnBegin](#OnBegin) | 當動畫元素開始時發生。 |
| [OnEnd](#OnEnd) | 當動畫元素結束時發生。 |
| [OnRepeat](#OnRepeat) | 當動畫元素重複時發生。 |
### OnFocusIn {#OnFocusIn}
```
public static final int OnFocusIn
```

發生於元素取得焦點時，例如文字被選取時。

### OnFocusOut {#OnFocusOut}
```
public static final int OnFocusOut
```

發生於元素失去焦點時，例如文字被取消選取時。

### OnActivate {#OnActivate}
```
public static final int OnActivate
```

發生於元素被啟用時，例如透過滑鼠點擊或按鍵操作。

### OnClick {#OnClick}
```
public static final int OnClick
```

發生於指向裝置的按鈕在元素上被點擊時。

### OnMouseDown {#OnMouseDown}
```
public static final int OnMouseDown
```

發生於指向裝置的按鈕在元素上被按下時。

### OnMouseUp {#OnMouseUp}
```
public static final int OnMouseUp
```

發生於指向裝置的按鈕在元素上被釋放時。

### OnMouseOver {#OnMouseOver}
```
public static final int OnMouseOver
```

發生於指向裝置移動到元素上時。

### OnMouseMove {#OnMouseMove}
```
public static final int OnMouseMove
```

發生於指向裝置在元素上移動時。

### OnMouseOut {#OnMouseOut}
```
public static final int OnMouseOut
```

發生於指向裝置離開元素時。

### OnLoad {#OnLoad}
```
public static final int OnLoad
```

發生於使用者代理已完整解析該元素及其所有子元素與所有參考的資源，且可渲染時。

### OnUnload {#OnUnload}
```
public static final int OnUnload
```

發生於 DOM 實作從視窗或框架中移除文件時。僅適用於最外層的 svg 元素。

### OnAbort {#OnAbort}
```
public static final int OnAbort
```

發生於在元素完全載入之前，頁面載入被停止時。

### OnError {#OnError}
```
public static final int OnError
```

發生於元素未正確載入或腳本執行期間發生錯誤時。

### OnResize {#OnResize}
```
public static final int OnResize
```

發生於文件檢視正在調整大小時。僅適用於最外層的 svg 元素。

### OnScroll {#OnScroll}
```
public static final int OnScroll
```

發生於文件檢視在 X 軸、Y 軸或兩者上平移時。僅適用於最外層的 svg 元素。

### OnZoom {#OnZoom}
```
public static final int OnZoom
```

發生於文件檢視的縮放等級變更時。僅適用於最外層的 svg 元素。

### OnBegin {#OnBegin}
```
public static final int OnBegin
```

發生於動畫元素開始時。

### OnEnd {#OnEnd}
```
public static final int OnEnd
```

發生於動畫元素結束時。

### OnRepeat {#OnRepeat}
```
public static final int OnRepeat
```

發生於動畫元素重複時。