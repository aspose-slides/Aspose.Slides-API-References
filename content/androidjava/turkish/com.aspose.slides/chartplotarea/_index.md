---
title: ChartPlotArea
second_title: Aspose.Slides for Android için Java API Referansı
description: Grafiğin çizileceği dikdörtgeni temsil eder.
type: docs
url: /tr/com.aspose.slides/chartplotarea/
---
**Kalıtım:**
java.lang.Object, com.aspose.slides.DomObject

**Uygulanan Tüm Arayüzler:**
[com.aspose.slides.IChartPlotArea](../../com.aspose.slides/ichartplotarea)
```
public class ChartPlotArea extends DomObject<Chart> implements IChartPlotArea
```

Grafiğin çizileceği dikdörtgeni temsil eder.
## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [getFormat()](#getFormat--) | Bir alanın biçimini döndürür. |
| [getX()](#getX--) | Grafiğin genişliğine göre (0 ile 1 arasında) alan sınırlama kutusunun sol üst köşesinin x koordinatını döndürür veya ayarlar. |
| [setX(float value)](#setX-float-) | Grafiğin genişliğine göre (0 ile 1 arasında) alan sınırlama kutusunun sol üst köşesinin x koordinatını döndürür veya ayarlar. |
| [getY()](#getY--) | Grafiğin yüksekliğine göre (0 ile 1 arasında) alan sınırlama kutusunun sol üst köşesinin y koordinatını döndürür veya ayarlar. |
| [setY(float value)](#setY-float-) | Grafiğin yüksekliğine göre (0 ile 1 arasında) alan sınırlama kutusunun sol üst köşesinin y koordinatını döndürür veya ayarlar. |
| [getWidth()](#getWidth--) | Grafiğin genişliğine göre (0 ile 1 arasında) alan sınırlama kutusunun genişliğini döndürür veya ayarlar. |
| [setWidth(float value)](#setWidth-float-) | Grafiğin genişliğine göre (0 ile 1 arasında) alan sınırlama kutusunun genişliğini döndürür veya ayarlar. |
| [getHeight()](#getHeight--) | Grafiğin yüksekliğine göre (0 ile 1 arasında) alan sınırlama kutusunun yüksekliğini döndürür veya ayarlar. |
| [setHeight(float value)](#setHeight-float-) | Grafiğin yüksekliğine göre (0 ile 1 arasında) alan sınırlama kutusunun yüksekliğini döndürür veya ayarlar. |
| [getRight()](#getRight--) | Sağ. |
| [getBottom()](#getBottom--) | Alt. |
| [getChart()](#getChart--) | Grafik. |
| [isLocationAutocalculated()](#isLocationAutocalculated--) | Konumun nasıl hesaplanacağını tanımlar: true \\u2013 otomatik olarak hesaplanır; X, Y, Width, Height özellikleriyle tanımlanır. |
| [getLayoutTargetType()](#getLayoutTargetType--) | Alan düzeni manuel olarak tanımlanmışsa, bu özellik alanın iç kısmına (ekseni ve eksen etiketlerini içermez) mı yoksa dış kısmına (ekseni ve eksen etiketlerini içerir) mı yerleştirileceğini belirtir. |
| [setLayoutTargetType(int value)](#setLayoutTargetType-int-) | Alan düzeni manuel olarak tanımlanmışsa, bu özellik alanın iç kısmına (ekseni ve eksen etiketlerini içermez) mı yoksa dış kısmına (ekseni ve eksen etiketlerini içerir) mı yerleştirileceğini belirtir. |
| [getActualX()](#getActualX--) | Grafiğin sol üst köşesine göre grafik öğesinin gerçek x konumunu (sol) belirtir. |
| [getActualY()](#getActualY--) | Grafiğin sol üst köşesine göre grafik öğesinin gerçek üst konumunu belirtir. |
| [getActualWidth()](#getActualWidth--) | Grafik öğesinin gerçek genişliğini belirtir. |
| [getActualHeight()](#getActualHeight--) | Grafik öğesinin gerçek yüksekliğini belirtir. |
| [getSlide()](#getSlide--) | Bir FillFormat'ın üst slaytını döndürür. |
| [getPresentation()](#getPresentation--) | Bir FillFormat'ın üst sunumunu döndürür. |
### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

Bir alanın biçimini döndürür. Yalnızca okuma [IFormat](../../com.aspose.slides/iformat).

**Döndürür:**
[IFormat](../../com.aspose.slides/iformat)
### getX() {#getX--}
```
public final float getX()
```

Grafiğin genişliğine göre (0 ile 1 arasında) alan sınırlama kutusunun sol üst köşesinin x koordinatını döndürür veya ayarlar. Okuma/yazma float.

**Döndürür:**
float
### setX(float value) {#setX-float-}
```
public final void setX(float value)
```

Grafiğin genişliğine göre (0 ile 1 arasında) alan sınırlama kutusunun sol üst köşesinin x koordinatını döndürür veya ayarlar. Okuma/yazma float.

**Parametreler:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getY() {#getY--}
```
public final float getY()
```

Grafiğin yüksekliğine göre (0 ile 1 arasında) alan sınırlama kutusunun sol üst köşesinin y koordinatını döndürür veya ayarlar. Okuma/yazma float.

**Döndürür:**
float
### setY(float value) {#setY-float-}
```
public final void setY(float value)
```

Grafiğin yüksekliğine göre (0 ile 1 arasında) alan sınırlama kutusunun sol üst köşesinin y koordinatını döndürür veya ayarlar. Okuma/yazma float.

**Parametreler:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getWidth() {#getWidth--}
```
public final float getWidth()
```

Grafiğin genişliğine göre (0 ile 1 arasında) alan sınırlama kutusunun genişliğini döndürür veya ayarlar. Okuma/yazma float.

**Döndürür:**
float
### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```

Grafiğin genişliğine göre (0 ile 1 arasında) alan sınırlama kutusunun genişliğini döndürür veya ayarlar. Okuma/yazma float.

**Parametreler:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getHeight() {#getHeight--}
```
public final float getHeight()
```

Grafiğin yüksekliğine göre (0 ile 1 arasında) alan sınırlama kutusunun yüksekliğini döndürür veya ayarlar. Okuma/yazma float.

**Döndürür:**
float
### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```

Grafiğin yüksekliğine göre (0 ile 1 arasında) alan sınırlama kutusunun yüksekliğini döndürür veya ayarlar. Okuma/yazma float.

**Parametreler:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getRight() {#getRight--}
```
public final float getRight()
```

Sağ. Yalnızca okuma float.

**Döndürür:**
float
### getBottom() {#getBottom--}
```
public final float getBottom()
```

Alt. Yalnızca okuma float.

**Döndürür:**
float
### getChart() {#getChart--}
```
public final IChart getChart()
```

Grafik. Yalnızca okuma [IChart](../../com.aspose.slides/ichart).

**Döndürür:**
[IChart](../../com.aspose.slides/ichart)
### isLocationAutocalculated() {#isLocationAutocalculated--}
```
public final boolean isLocationAutocalculated()
```

Konumun nasıl hesaplanacağını tanımlar: true \\u2013 otomatik olarak hesaplanır; X, Y, Width, Height özellikleriyle tanımlanır. Yalnızca okuma boolean.

**Döndürür:**
boolean
### getLayoutTargetType() {#getLayoutTargetType--}
```
public final int getLayoutTargetType()
```

Alan düzeni manuel olarak tanımlanmışsa, bu özellik alanın iç kısmına (ekseni ve eksen etiketlerini içermez) mı yoksa dış kısmına (ekseni ve eksen etiketlerini içerir) mı yerleştirileceğini belirtir. Okuma/yazma [LayoutTargetType](../../com.aspose.slides/layouttargettype)(\#getLayoutTargetType.getLayoutTargetType/\#setLayoutTargetType(int).setLayoutTargetType(int)).

--------------------

> ```
> Presentation presentation = new Presentation();
>   try
>   {
>       ISlide slide = presentation.getSlides().get_Item(0);
>       IChart chart = slide.getShapes().addChart(ChartType.ClusteredColumn, 20, 100, 600, 400);
>       chart.getPlotArea().setX(0.2f);
>       chart.getPlotArea().setY(0.2f);
>       chart.getPlotArea().setWidth(0.7f);
>       chart.getPlotArea().setHeight(0.7f);
>       chart.getPlotArea().setLayoutTargetType(LayoutTargetType.Inner);
>       ...
>   } finally {
>       if (presentation != null) presentation.dispose();
>   }
> ```

**Döndürür:**
int
### setLayoutTargetType(int value) {#setLayoutTargetType-int-}
```
public final void setLayoutTargetType(int value)
```

Alan düzeni manuel olarak tanımlanmışsa, bu özellik alanın iç kısmına (ekseni ve eksen etiketlerini içermez) mı yoksa dış kısmına (ekseni ve eksen etiketlerini içerir) mı yerleştirileceğini belirtir. Okuma/yazma [LayoutTargetType](../../com.aspose.slides/layouttargettype)(\#getLayoutTargetType.getLayoutTargetType/\#setLayoutTargetType(int).setLayoutTargetType(int)).

--------------------

> ```
> Presentation presentation = new Presentation();
>   try
>   {
>       ISlide slide = presentation.getSlides().get_Item(0);
>       IChart chart = slide.getShapes().addChart(ChartType.ClusteredColumn, 20, 100, 600, 400);
>       chart.getPlotArea().setX(0.2f);
>       chart.getPlotArea().setY(0.2f);
>       chart.getPlotArea().setWidth(0.7f);
>       chart.getPlotArea().setHeight(0.7f);
>       chart.getPlotArea().setLayoutTargetType(LayoutTargetType.Inner);
>       ...
>   } finally {
>       if (presentation != null) presentation.dispose();
>   }
> ```


**Parametreler:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | int |  |
### getActualX() {#getActualX--}
```
public final float getActualX()
```

Grafiğin sol üst köşesine göre grafik öğesinin gerçek x konumunu (sol) belirtir. Gerçek değerleri almak için IChart.ValidateChartLayout() metodunu çağırın. Okuma float.

**Döndürür:**
float
### getActualY() {#getActualY--}
```
public final float getActualY()
```

Grafiğin sol üst köşesine göre grafik öğesinin gerçek üst konumunu belirtir. Gerçek değerleri almak için IChart.ValidateChartLayout() metodunu çağırın. Okuma float.

**Döndürür:**
float
### getActualWidth() {#getActualWidth--}
```
public final float getActualWidth()
```

Grafik öğesinin gerçek genişliğini belirtir. Gerçek değerleri almak için IChart.ValidateChartLayout() metodunu çağırın. Okuma float.

**Döndürür:**
float
### getActualHeight() {#getActualHeight--}
```
public final float getActualHeight()
```

Grafik öğesinin gerçek yüksekliğini belirtir. Gerçek değerleri almak için IChart.ValidateChartLayout() metodunu çağırın. Okuma float.

**Döndürür:**
float
### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

Bir FillFormat'ın üst slaytını döndürür. Yalnızca okuma [BaseSlide](../../com.aspose.slides/baseslide).

**Döndürür:**
[IBaseSlide](../../com.aspose.slides/ibaseslide)
### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

Bir FillFormat'ın üst sunumunu döndürür. Yalnızca okuma [IPresentation](../../com.aspose.slides/ipresentation).

**Döndürür:**
[IPresentation](../../com.aspose.slides/ipresentation)