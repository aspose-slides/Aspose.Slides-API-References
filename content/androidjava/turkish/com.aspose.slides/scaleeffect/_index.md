---
title: ScaleEffect
second_title: Java API Referansı aracılığıyla Android için Aspose.Slides
description: Animasyon ölçek etkisini temsil eder.
type: docs
url: /tr/com.aspose.slides/scaleeffect/
---
**Kalıtım:**  
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**Uygulanan Tüm Arabirimler:**  
[com.aspose.slides.IScaleEffect](../../com.aspose.slides/iscaleeffect)  
```
public class ScaleEffect extends Behavior implements IScaleEffect
```

Animasyon ölçek etkisini temsil eder.

## Yapıcılar

| Yapıcı | Açıklama |
| --- | --- |
| [ScaleEffect()](#ScaleEffect--) |  |

## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | İçeriğin yakınlaştırılması gerekip gerekmediğini belirler. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | İçeriğin yakınlaştırılması gerekip gerekmediğini belirler. |
| [getFrom()](#getFrom--) | Animasyonun başlatılacağı x/y koordinatını (yüzde olarak) belirler. |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | Animasyonun başlatılacağı x/y koordinatını (yüzde olarak) belirler. |
| [getTo()](#getTo--) | Animasyon ölçek etkisi için hedef konumu (yüzde olarak) belirler. |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | Animasyon ölçek etkisi için hedef konumu (yüzde olarak) belirler. |
| [getBy()](#getBy--) | Animasyon için göreli offset değerini (yüzde olarak) açıklar. |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | Animasyon için göreli offset değerini (yüzde olarak) açıklar. |

### ScaleEffect() {#ScaleEffect--}
```
public ScaleEffect()
```

### getZoomContent() {#getZoomContent--}
```
public final byte getZoomContent()
```

İçeriğin yakınlaştırılması gerekip gerekmediğini belirler. Okunur/yazılır [NullableBool](../../com.aspose.slides/nullablebool).

**Döndürür:**  
byte

### setZoomContent(byte value) {#setZoomContent-byte-}
```
public final void setZoomContent(byte value)
```

İçeriğin yakınlaştırılması gerekip gerekmediğini belirler. Okunur/yazılır [NullableBool](../../com.aspose.slides/nullablebool).

**Parametreler:**  
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public final PointF getFrom()
```

Animasyonun başlatılacağı x/y koordinatını (yüzde olarak) belirler. Okunur/yazılır android.graphics.PointF.

**Döndürür:**  
android.graphics.PointF

### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public final void setFrom(PointF value)
```

Animasyonun başlatılacağı x/y koordinatını (yüzde olarak) belirler. Okunur/yazılır android.graphics.PointF.

**Parametreler:**  
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public final PointF getTo()
```

Animasyon ölçek etkisi için hedef konumu (yüzde olarak) belirler. Okunur/yazılır android.graphics.PointF.

**Döndürür:**  
android.graphics.PointF

### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public final void setTo(PointF value)
```

Animasyon ölçek etkisi için hedef konumu (yüzde olarak) belirler. Okunur/yazılır android.graphics.PointF.

**Parametreler:**  
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public final PointF getBy()
```

Animasyon için göreli offset değerini (yüzde olarak) açıklar. Okunur/yazılır android.graphics.PointF.

**Döndürür:**  
android.graphics.PointF

### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public final void setBy(PointF value)
```

Animasyon için göreli offset değerini (yüzde olarak) açıklar. Okunur/yazılır android.graphics.PointF.

**Parametreler:**  
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | android.graphics.PointF |  |