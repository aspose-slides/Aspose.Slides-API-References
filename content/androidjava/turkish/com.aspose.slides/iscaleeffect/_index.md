---
title: IScaleEffect
second_title: Aspose.Slides for Android Java API Referansı
description: Animasyon ölçek etkisini temsil eder.
type: docs
url: /tr/com.aspose.slides/iscaleeffect/
---
**Tüm Uygulanan Arabirimler:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IScaleEffect extends IBehavior
```

Animasyon ölçek etkisini temsil eder.
## Yöntemler

| Method | Description |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | İçeriğin yakınlaştırılması gerekip gerekmediğini belirler. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | İçeriğin yakınlaştırılması gerekip gerekmediğini belirler. |
| [getFrom()](#getFrom--) | Animasyonu başlatmak için x/y koordinatını (yüzde olarak) belirtir. |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | Animasyonu başlatmak için x/y koordinatını (yüzde olarak) belirtir. |
| [getTo()](#getTo--) | Animasyon ölçek etkisi için hedef konumu (yüzde olarak) belirtir. |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | Animasyon ölçek etkisi için hedef konumu (yüzde olarak) belirtir. |
| [getBy()](#getBy--) | Animasyon için göreceli offset değerini (yüzde olarak) açıklar. |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | Animasyon için göreceli offset değerini (yüzde olarak) açıklar. |
### getZoomContent() {#getZoomContent--}
```
public abstract byte getZoomContent()
```

İçeriğin yakınlaştırılması gerekip gerekmediğini belirler. Okuma/yazma [NullableBool](../../com.aspose.slides/nullablebool).

**Döndürür:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public abstract void setZoomContent(byte value)
```

İçeriğin yakınlaştırılması gerekip gerekmediğini belirler. Okuma/yazma [NullableBool](../../com.aspose.slides/nullablebool).

**Parametreler:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | byte |  |
### getFrom() {#getFrom--}
```
public abstract PointF getFrom()
```

Animasyonu başlatmak için x/y koordinatını (yüzde olarak) belirtir. Okuma/yazma android.graphics.PointF.

**Döndürür:**
android.graphics.PointF
### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public abstract void setFrom(PointF value)
```

Animasyonu başlatmak için x/y koordinatını (yüzde olarak) belirtir. Okuma/yazma android.graphics.PointF.

**Parametreler:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | android.graphics.PointF |  |
### getTo() {#getTo--}
```
public abstract PointF getTo()
```

Animasyon ölçek etkisi için hedef konumu (yüzde olarak) belirtir. Okuma/yazma android.graphics.PointF.

**Döndürür:**
android.graphics.PointF
### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public abstract void setTo(PointF value)
```

Animasyon ölçek etkisi için hedef konumu (yüzde olarak) belirtir. Okuma/yazma android.graphics.PointF.

**Parametreler:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | android.graphics.PointF |  |
### getBy() {#getBy--}
```
public abstract PointF getBy()
```

Animasyon için göreceli offset değerini (yüzde olarak) açıklar. Okuma/yazma android.graphics.PointF.

**Döndürür:**
android.graphics.PointF
### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public abstract void setBy(PointF value)
```

Animasyon için göreceli offset değerini (yüzde olarak) açıklar. Okuma/yazma android.graphics.PointF.

**Parametreler:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | android.graphics.PointF |  |