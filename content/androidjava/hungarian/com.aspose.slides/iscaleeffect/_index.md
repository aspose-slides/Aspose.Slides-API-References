---
title: IScaleEffect
second_title: Aspose.Slides Androidra Java API referencia szerint
description: Az animáció méretarány hatását képviseli.
type: docs
url: /hu/com.aspose.slides/iscaleeffect/
---
**Az összes megvalósított interfész:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IScaleEffect extends IBehavior
```

Az animáció méretarány hatását képviseli.
## Metódusok

| Metódus | Leírás |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Meghatározza, hogy a tartalomnak nagyítania kell-e. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Meghatározza, hogy a tartalomnak nagyítania kell-e. |
| [getFrom()](#getFrom--) | Megadja az x/y koordinátát, ahonnan a animáció indul (százalékban). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | Megadja az x/y koordinátát, ahonnan a animáció indul (százalékban). |
| [getTo()](#getTo--) | Megadja a célhelyet az animáció méretezési hatásához (százalékban). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | Megadja a célhelyet az animáció méretezési hatásához (százalékban). |
| [getBy()](#getBy--) | Leírja az animáció relatív eltolási értékét (százalékban). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | Leírja az animáció relatív eltolási értékét (százalékban). |
### getZoomContent() {#getZoomContent--}
```
public abstract byte getZoomContent()
```

Meghatározza, hogy a tartalomnak nagyítania kell-e. Olvasás/írás [NullableBool](../../com.aspose.slides/nullablebool).

**Visszatérési érték:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public abstract void setZoomContent(byte value)
```

Meghatározza, hogy a tartalomnak nagyítania kell-e. Olvasás/írás [NullableBool](../../com.aspose.slides/nullablebool).

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public abstract PointF getFrom()
```

Megadja az x/y koordinátát, ahonnan a animáció indul (százalékban). Olvasás/írás android.graphics.PointF.

**Visszatérési érték:**
android.graphics.PointF
### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public abstract void setFrom(PointF value)
```

Megadja az x/y koordinátát, ahonnan a animáció indul (százalékban). Olvasás/írás android.graphics.PointF.

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public abstract PointF getTo()
```

Megadja a célhelyet az animáció méretezési hatásához (százalékban). Olvasás/írás android.graphics.PointF.

**Visszatérési érték:**
android.graphics.PointF
### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public abstract void setTo(PointF value)
```

Megadja a célhelyet az animáció méretezési hatásához (százalékban). Olvasás/írás android.graphics.PointF.

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public abstract PointF getBy()
```

Leírja az animáció relatív eltolási értékét (százalékban). Olvasás/írás android.graphics.PointF.

**Visszatérési érték:**
android.graphics.PointF
### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public abstract void setBy(PointF value)
```

Leírja az animáció relatív eltolási értékét (százalékban). Olvasás/írás android.graphics.PointF.

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | android.graphics.PointF |  |