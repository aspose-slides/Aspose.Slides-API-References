---
title: ScaleEffect
second_title: Aspose.Slides for Android Java API referenciája
description: Az animáció méretezési hatását ábrázolja.
type: docs
url: /hu/com.aspose.slides/scaleeffect/
---
**Öröklés:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**Az összes megvalósított interfész:**
[com.aspose.slides.IScaleEffect](../../com.aspose.slides/iscaleeffect)
```
public class ScaleEffect extends Behavior implements IScaleEffect
```

Az animáció méretezési hatását ábrázolja.
## Konstruktorok

| Konstruktor | Leírás |
| --- | --- |
| [ScaleEffect()](#ScaleEffect--) |  |
## Metódusok

| Metódus | Leírás |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Meghatározza, hogy a tartalomnak nagyítania kell-e. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Meghatározza, hogy a tartalomnak nagyítania kell-e. |
| [getFrom()](#getFrom--) | Megad egy x/y koordinátát, ahonnan az animáció indul (százalékban). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | Megad egy x/y koordinátát, ahonnan az animáció indul (százalékban). |
| [getTo()](#getTo--) | Megadja az animáció méretezési hatásának célhelyét (százalékban). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | Megadja az animáció méretezési hatásának célhelyét (százalékban). |
| [getBy()](#getBy--) | Leírja az animáció relatív eltolás értékét (százalékban). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | Leírja az animáció relatív eltolás értékét (százalékban). |
### ScaleEffect() {#ScaleEffect--}
```
public ScaleEffect()
```


### getZoomContent() {#getZoomContent--}
```
public final byte getZoomContent()
```


Meghatározza, hogy a tartalomnak nagyítania kell-e. Olvasás/írás [NullableBool](../../com.aspose.slides/nullablebool).

**Visszatérési érték:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public final void setZoomContent(byte value)
```


Meghatározza, hogy a tartalomnak nagyítania kell-e. Olvasás/írás [NullableBool](../../com.aspose.slides/nullablebool).

**Paraméterek:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public final PointF getFrom()
```


Megad egy x/y koordinátát, ahonnan az animáció indul (százalékban). Olvasás/írás android.graphics.PointF.

**Visszatérési érték:**
android.graphics.PointF
### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public final void setFrom(PointF value)
```


Megad egy x/y koordinátát, ahonnan az animáció indul (százalékban). Olvasás/írás android.graphics.PointF.

**Paraméterek:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public final PointF getTo()
```


Megadja az animáció méretezési hatásának célhelyét (százalékban). Olvasás/írás android.graphics.PointF.

**Visszatérési érték:**
android.graphics.PointF
### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public final void setTo(PointF value)
```


Megadja az animáció méretezési hatásának célhelyét (százalékban). Olvasás/írás android.graphics.PointF.

**Paraméterek:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public final PointF getBy()
```


Leírja az animáció relatív eltolás értékét (százalékban). Olvasás/írás android.graphics.PointF.

**Visszatérési érték:**
android.graphics.PointF
### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public final void setBy(PointF value)
```


Leírja az animáció relatív eltolás értékét (százalékban). Olvasás/írás android.graphics.PointF.

**Paraméterek:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | android.graphics.PointF |  |