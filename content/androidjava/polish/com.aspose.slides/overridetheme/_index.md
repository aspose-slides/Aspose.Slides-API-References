---
title: OverrideTheme
second_title: Aspose.Slides dla Androida – dokumentacja API Java
description: Reprezentuje nadpisywaną tematykę.
type: docs
url: /pl/com.aspose.slides/overridetheme/
---
**Dziedziczenie:**  
java.lang.Object, [com.aspose.slides.Theme](../../com.aspose.slides/theme)

**Wszystkie zaimplementowane interfejsy:**  
[com.aspose.slides.IOverrideTheme](../../com.aspose.slides/ioverridetheme)  
```
public final class OverrideTheme extends Theme implements IOverrideTheme
```

Reprezentuje nadpisywaną tematykę.
## Metody

| Metoda | Opis |
| --- | --- |
| [initColorScheme()](#initColorScheme--) | Inicjalizuj ColorScheme nowym obiektem do nadpisywania ColorScheme w InheritedTheme. |
| [initColorSchemeFrom(IColorScheme colorScheme)](#initColorSchemeFrom-com.aspose.slides.IColorScheme-) | Inicjalizuj ColorScheme nowym obiektem do nadpisywania ColorScheme w InheritedTheme. |
| [initColorSchemeFromInherited()](#initColorSchemeFromInherited--) | Inicjalizuj ColorScheme nowym obiektem do nadpisywania ColorScheme w InheritedTheme. |
| [initFontScheme()](#initFontScheme--) | Inicjalizuj FontScheme nowym obiektem do nadpisywania FontScheme w InheritedTheme. |
| [initFontSchemeFrom(IFontScheme fontScheme)](#initFontSchemeFrom-com.aspose.slides.IFontScheme-) | Inicjalizuj FontScheme nowym obiektem do nadpisywania FontScheme w InheritedTheme. |
| [initFontSchemeFromInherited()](#initFontSchemeFromInherited--) | Inicjalizuj FontScheme nowym obiektem do nadpisywania FontScheme w InheritedTheme. |
| [initFormatScheme()](#initFormatScheme--) | Inicjalizuj FormatScheme nowym obiektem do nadpisywania FormatScheme w InheritedTheme. |
| [initFormatSchemeFrom(IFormatScheme formatScheme)](#initFormatSchemeFrom-com.aspose.slides.IFormatScheme-) | Inicjalizuj FormatScheme nowym obiektem do nadpisywania FormatScheme w InheritedTheme. |
| [initFormatSchemeFromInherited()](#initFormatSchemeFromInherited--) | Inicjalizuj FormatScheme nowym obiektem do nadpisywania FormatScheme w InheritedTheme. |
| [getColorScheme()](#getColorScheme--) | Zwraca schemę kolorów. |
| [getFontScheme()](#getFontScheme--) | Zwraca schemę czcionek. |
| [getFormatScheme()](#getFormatScheme--) | Zwraca schemę formatu kształtu. |
| [isEmpty()](#isEmpty--) | Prawdziwa wartość oznacza, że ColorScheme, FontScheme, FormatScheme jest null i wszelkie nadpisywanie tym obiektem tematu jest wyłączone. |
| [clear()](#clear--) | Ustaw ColorScheme, FontScheme, FormatScheme na null, aby wyłączyć wszelkie nadpisywanie tym obiektem tematu. |
| [getVersion()](#getVersion--) |  |
### initColorScheme() {#initColorScheme--}
```
public final void initColorScheme()
```

Inicjalizuj ColorScheme nowym obiektem do nadpisywania ColorScheme w InheritedTheme.

### initColorSchemeFrom(IColorScheme colorScheme) {#initColorSchemeFrom-com.aspose.slides.IColorScheme-}
```
public final void initColorSchemeFrom(IColorScheme colorScheme)
```

Inicjalizuj ColorScheme nowym obiektem do nadpisywania ColorScheme w InheritedTheme.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| colorScheme | [IColorScheme](../../com.aspose.slides/icolorscheme) | Dane do zainicjowania. |

### initColorSchemeFromInherited() {#initColorSchemeFromInherited--}
```
public final void initColorSchemeFromInherited()
```

Inicjalizuj ColorScheme nowym obiektem do nadpisywania ColorScheme w InheritedTheme. I zainicjuj dane tego nowego obiektu danymi ColorScheme w InheritedTheme.

### initFontScheme() {#initFontScheme--}
```
public final void initFontScheme()
```

Inicjalizuj FontScheme nowym obiektem do nadpisywania FontScheme w InheritedTheme.

### initFontSchemeFrom(IFontScheme fontScheme) {#initFontSchemeFrom-com.aspose.slides.IFontScheme-}
```
public final void initFontSchemeFrom(IFontScheme fontScheme)
```

Inicjalizuj FontScheme nowym obiektem do nadpisywania FontScheme w InheritedTheme.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| fontScheme | [IFontScheme](../../com.aspose.slides/ifontscheme) | Dane do zainicjowania. |

### initFontSchemeFromInherited() {#initFontSchemeFromInherited--}
```
public final void initFontSchemeFromInherited()
```

Inicjalizuj FontScheme nowym obiektem do nadpisywania FontScheme w InheritedTheme. I zainicjuj dane tego nowego obiektu danymi FontScheme w InheritedTheme.

### initFormatScheme() {#initFormatScheme--}
```
public final void initFormatScheme()
```

Inicjalizuj FormatScheme nowym obiektem do nadpisywania FormatScheme w InheritedTheme.

### initFormatSchemeFrom(IFormatScheme formatScheme) {#initFormatSchemeFrom-com.aspose.slides.IFormatScheme-}
```
public final void initFormatSchemeFrom(IFormatScheme formatScheme)
```

Inicjalizuj FormatScheme nowym obiektem do nadpisywania FormatScheme w InheritedTheme.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| formatScheme | [IFormatScheme](../../com.aspose.slides/iformatscheme) | Dane do zainicjowania. |

### initFormatSchemeFromInherited() {#initFormatSchemeFromInherited--}
```
public final void initFormatSchemeFromInherited()
```

Inicjalizuj FormatScheme nowym obiektem do nadpisywania FormatScheme w InheritedTheme. I zainicjuj dane tego nowego obiektu danymi FormatScheme w InheritedTheme.

### getColorScheme() {#getColorScheme--}
```
public IColorScheme getColorScheme()
```

Zwraca schemę kolorów. tylko do odczytu [IColorScheme](../../com.aspose.slides/icolorscheme).

**Zwraca:**
[IColorScheme](../../com.aspose.slides/icolorscheme)
### getFontScheme() {#getFontScheme--}
```
public IFontScheme getFontScheme()
```

Zwraca schemę czcionek. tylko do odczytu [IFontScheme](../../com.aspose.slides/ifontscheme).

**Zwraca:**
[IFontScheme](../../com.aspose.slides/ifontscheme)
### getFormatScheme() {#getFormatScheme--}
```
public IFormatScheme getFormatScheme()
```

Zwraca schemę formatu kształtu. tylko do odczytu [IFormatScheme](../../com.aspose.slides/iformatscheme).

**Zwraca:**
[IFormatScheme](../../com.aspose.slides/iformatscheme)
### isEmpty() {#isEmpty--}
```
public final boolean isEmpty()
```

Prawdziwa wartość oznacza, że ColorScheme, FontScheme, FormatScheme jest null i wszelkie nadpisywanie tym obiektem tematu jest wyłączone. tylko do odczytu boolean.

**Zwraca:**
boolean
### clear() {#clear--}
```
public final void clear()
```

Ustaw ColorScheme, FontScheme, FormatScheme na null, aby wyłączyć wszelkie nadpisywanie tym obiektem tematu.

### getVersion() {#getVersion--}
```
public long getVersion()
```

Wersja. tylko do odczytu long.

**Zwraca:**
long