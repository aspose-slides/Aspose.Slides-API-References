---
title: LegendDataLabelPosition
second_title: Aspose.Slides dla Androida – dokumentacja API Java
description: Określa pozycję etykiet danych.
type: docs
url: /pl/com.aspose.slides/legenddatalabelposition/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class LegendDataLabelPosition extends System.Enum
```

Określa pozycję etykiet danych.
## Pola

| Pole | Opis |
| --- | --- |
| [NotDefined](#NotDefined) | Pozycja nie jest określona. |
| [Bottom](#Bottom) | Określa, że etykiety danych powinny być wyświetlane pod znacznikiem danych. |
| [BestFit](#BestFit) | Określa, że etykiety danych powinny być wyświetlane w najlepszej pozycji. |
| [Center](#Center) | Określa, że etykiety danych powinny być wyświetlane wyśrodkowane względem znacznika danych. |
| [InsideBase](#InsideBase) | Określa, że etykiety danych powinny być wyświetlane wewnątrz podstawy znacznika danych. |
| [InsideEnd](#InsideEnd) | Określa, że etykiety danych powinny być wyświetlane wewnątrz końca znacznika danych. |
| [Left](#Left) | Określa, że etykiety danych powinny być wyświetlane po lewej stronie znacznika danych. |
| [OutsideEnd](#OutsideEnd) | Określa, że etykiety danych powinny być wyświetlane na zewnątrz końca znacznika danych. |
| [Right](#Right) | Określa, że etykiety danych powinny być wyświetlane po prawej stronie znacznika danych. |
| [Top](#Top) | Określa, że etykiety danych powinny być wyświetlane nad znacznikiem danych. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```


Pozycja nie jest określona.

### Bottom {#Bottom}
```
public static final int Bottom
```


Określa, że etykiety danych powinny być wyświetlane pod znacznikiem danych.

### BestFit {#BestFit}
```
public static final int BestFit
```


Określa, że etykiety danych powinny być wyświetlane w najlepszej pozycji.

### Center {#Center}
```
public static final int Center
```


Określa, że etykiety danych powinny być wyświetlane wyśrodkowane względem znacznika danych.

### InsideBase {#InsideBase}
```
public static final int InsideBase
```


Określa, że etykiety danych powinny być wyświetlane wewnątrz podstawy znacznika danych.

### InsideEnd {#InsideEnd}
```
public static final int InsideEnd
```


Określa, że etykiety danych powinny być wyświetlane wewnątrz końca znacznika danych.

### Left {#Left}
```
public static final int Left
```


Określa, że etykiety danych powinny być wyświetlane po lewej stronie znacznika danych.

### OutsideEnd {#OutsideEnd}
```
public static final int OutsideEnd
```


Określa, że etykiety danych powinny być wyświetlane na zewnątrz końca znacznika danych.

### Right {#Right}
```
public static final int Right
```


Określa, że etykiety danych powinny być wyświetlane po prawej stronie znacznika danych.

### Top {#Top}
```
public static final int Top
```


Określa, że etykiety danych powinny być wyświetlane nad znacznikiem danych.