---
title: PieSplitType
second_title: Aspose.Slides dla Androida – odwołanie do Java API
description: Reprezentuje typ punktów podziału w drugim wykresie kołowym lub słupkowym w wykresie kołowy-pod-wykresem lub słupkowy-pod-wykresem.
type: docs
url: /pl/com.aspose.slides/piesplittype/
---
**Dziedziczenie:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class PieSplitType extends System.Enum
```

Reprezentuje typ punktów podziału w drugiej części wykresu kołowego lub słupkowego w wykresie wykres-pod-wykresem lub słupkowym-w-wykresem kołowym.
## Pola

| Pole | Opis |
| --- | --- |
| [Default](#Default) | Określa, że punkty danych zostaną podzielone przy użyciu domyślnego mechanizmu dla tego typu wykresu. |
| [Custom](#Custom) | Określa, że punkty danych zostaną podzielone pomiędzy wykres kołowy a drugi wykres zgodnie z wartościami Custom Split. |
| [ByPercentage](#ByPercentage) | Określa, że punkty danych zostaną podzielone pomiędzy wykres kołowy a drugi wykres, umieszczając punkty o procencie mniejszym niż Split Position percent w drugim wykresie. |
| [ByPos](#ByPos) | Określa, że punkty danych zostaną podzielone pomiędzy wykres kołowy a drugi wykres, umieszczając ostatnią Split Position punktów danych w drugim wykresie. |
| [ByValue](#ByValue) | Określa, że punkty danych zostaną podzielone pomiędzy wykres kołowy a drugi wykres, umieszczając punkty danych o wartości mniejszej niż Split Position w drugim wykresie. |
### Default {#Default}
```
public static final int Default
```

Określa, że punkty danych zostaną podzielone przy użyciu domyślnego mechanizmu dla tego typu wykresu.

### Custom {#Custom}
```
public static final int Custom
```

Określa, że punkty danych zostaną podzielone pomiędzy wykres kołowy a drugi wykres zgodnie z wartościami Custom Split.

### ByPercentage {#ByPercentage}
```
public static final int ByPercentage
```

Określa, że punkty danych zostaną podzielone pomiędzy wykres kołowy a drugi wykres, umieszczając punkty o procencie mniejszym niż Split Position percent w drugim wykresie.

### ByPos {#ByPos}
```
public static final int ByPos
```

Określa, że punkty danych zostaną podzielone pomiędzy wykres kołowy a drugi wykres, umieszczając ostatnią Split Position punktów danych w drugim wykresie.

### ByValue {#ByValue}
```
public static final int ByValue
```

Określa, że punkty danych zostaną podzielone pomiędzy wykres kołowy a drugi wykres, umieszczając punkty danych o wartości mniejszej niż Split Position w drugim wykresie.