---
title: ScaleEffect
second_title: Aspose.Slides dla Androida - referencja API Java
description: Reprezentuje efekt skalowania animacji.
type: docs
url: /pl/com.aspose.slides/scaleeffect/
---
**Dziedziczenie:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**Wszystkie zaimplementowane interfejsy:**
[com.aspose.slides.IScaleEffect](../../com.aspose.slides/iscaleeffect)
```
public class ScaleEffect extends Behavior implements IScaleEffect
```

Reprezentuje efekt skalowania animacji.
## Konstruktory

| Konstruktor | Opis |
| --- | --- |
| [ScaleEffect()](#ScaleEffect--) |  |
## Metody

| Metoda | Opis |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Określa, czy zawartość powinna być powiększana. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Określa, czy zawartość powinna być powiększana. |
| [getFrom()](#getFrom--) | Określa współrzędną x/y, z której rozpoczyna się animacja (w procentach). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | Określa współrzędną x/y, z której rozpoczyna się animacja (w procentach). |
| [getTo()](#getTo--) | Określa docelową lokalizację efektu skalowania animacji (w procentach). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | Określa docelową lokalizację efektu skalowania animacji (w procentach). |
| [getBy()](#getBy--) | Opisuje względną wartość przesunięcia dla animacji (w procentach). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | Opisuje względną wartość przesunięcia dla animacji (w procentach). |
### ScaleEffect() {#ScaleEffect--}
```
public ScaleEffect()
```


### getZoomContent() {#getZoomContent--}
```
public final byte getZoomContent()
```


Określa, czy zawartość powinna być powiększana. Odczyt/zapis [NullableBool](../../com.aspose.slides/nullablebool).

**Zwraca:**  
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public final void setZoomContent(byte value)
```


Określa, czy zawartość powinna być powiększana. Odczyt/zapis [NullableBool](../../com.aspose.slides/nullablebool).

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public final PointF getFrom()
```


Określa współrzędną x/y, z której rozpoczyna się animacja (w procentach). Odczyt/zapis android.graphics.PointF.

**Zwraca:**  
android.graphics.PointF
### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public final void setFrom(PointF value)
```


Określa współrzędną x/y, z której rozpoczyna się animacja (w procentach). Odczyt/zapis android.graphics.PointF.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public final PointF getTo()
```


Określa docelową lokalizację efektu skalowania animacji (w procentach). Odczyt/zapis android.graphics.PointF.

**Zwraca:**  
android.graphics.PointF
### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public final void setTo(PointF value)
```


Określa docelową lokalizację efektu skalowania animacji (w procentach). Odczyt/zapis android.graphics.PointF.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public final PointF getBy()
```


Opisuje względną wartość przesunięcia dla animacji (w procentach). Odczyt/zapis android.graphics.PointF.

**Zwraca:**  
android.graphics.PointF
### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public final void setBy(PointF value)
```


Opisuje względną wartość przesunięcia dla animacji (w procentach). Odczyt/zapis android.graphics.PointF.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | android.graphics.PointF |  |