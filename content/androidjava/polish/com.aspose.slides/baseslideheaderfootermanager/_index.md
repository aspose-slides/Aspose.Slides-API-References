---
title: BaseSlideHeaderFooterManager
second_title: Aspose.Slides dla Androida poprzez odniesienie API Java
description: Reprezentuje menedżera, który kontroluje zachowanie znaków zastępczych stopki, daty-czasu i numeru strony we wszystkich typach slajdów.
type: docs
url: /pl/com.aspose.slides/baseslideheaderfootermanager/
---
**Dziedziczenie:**
java.lang.Object, [com.aspose.slides.BaseHeaderFooterManager](../../com.aspose.slides/baseheaderfootermanager)
```
public abstract class BaseSlideHeaderFooterManager extends BaseHeaderFooterManager
```

Reprezentuje menedżera, który kontroluje zachowanie znaków zastępczych stopki, daty-czasu i numeru strony we wszystkich typach slajdów.
## Metody

| Metoda | Opis |
| --- | --- |
| [isFooterVisible()](#isFooterVisible--) | Zwraca wartość wskazującą, że znak zastępczy stopki jest obecny. Odczyt boolean. |
| [isSlideNumberVisible()](#isSlideNumberVisible--) | Zwraca wartość wskazującą, że znak zastępczy numeru strony jest obecny. Odczyt boolean. |
| [isDateTimeVisible()](#isDateTimeVisible--) | Zwraca wartość wskazującą, że znak zastępczy daty-czasu jest obecny. Odczyt boolean. |
| [setFooterVisibility(boolean isVisible)](#setFooterVisibility-boolean-) | Zmienia widoczność znaku zastępczego stopki slajdu. |
| [setSlideNumberVisibility(boolean isVisible)](#setSlideNumberVisibility-boolean-) | Zmienia widoczność znaku zastępczego numeru strony slajdu. |
| [setDateTimeVisibility(boolean isVisible)](#setDateTimeVisibility-boolean-) | Zmienia widoczność znaku zastępczego daty-czasu slajdu. |
| [setFooterText(String text)](#setFooterText-java.lang.String-) | Ustawia tekst w znaku zastępczym stopki slajdu. |
| [setDateTimeText(String text)](#setDateTimeText-java.lang.String-) | Ustawia tekst w znaku zastępczym daty-czasu slajdu. |
### isFooterVisible() {#isFooterVisible--}
```
public final boolean isFooterVisible()
```

Zwraca wartość wskazującą, że znak zastępczy stopki jest obecny. Odczyt boolean.

**Zwraca:**
boolean
### isSlideNumberVisible() {#isSlideNumberVisible--}
```
public final boolean isSlideNumberVisible()
```

Zwraca wartość wskazującą, że znak zastępczy numeru strony jest obecny. Odczyt boolean.

**Zwraca:**
boolean
### isDateTimeVisible() {#isDateTimeVisible--}
```
public final boolean isDateTimeVisible()
```

Zwraca wartość wskazującą, że znak zastępczy daty-czasu jest obecny. Odczyt boolean.

**Zwraca:**
boolean
### setFooterVisibility(boolean isVisible) {#setFooterVisibility-boolean-}
```
public final void setFooterVisibility(boolean isVisible)
```

Zmienia widoczność znaku zastępczego stopki slajdu.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| isVisible | boolean | true - sprawia, że znak zastępczy stopki jest widoczny, w przeciwnym razie - ukrywa go. |

### setSlideNumberVisibility(boolean isVisible) {#setSlideNumberVisibility-boolean-}
```
public final void setSlideNumberVisibility(boolean isVisible)
```

Zmienia widoczność znaku zastępczego numeru strony slajdu.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| isVisible | boolean | true - sprawia, że znak zastępczy numeru strony jest widoczny, w przeciwnym razie - ukrywa go. |

### setDateTimeVisibility(boolean isVisible) {#setDateTimeVisibility-boolean-}
```
public final void setDateTimeVisibility(boolean isVisible)
```

Zmienia widoczność znaku zastępczego daty-czasu slajdu.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| isVisible | boolean | true - sprawia, że znak zastępczy daty-czasu jest widoczny, w przeciwnym razie - ukrywa go. |

### setFooterText(String text) {#setFooterText-java.lang.String-}
```
public final void setFooterText(String text)
```

Ustawia tekst w znaku zastępczym stopki slajdu.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| text | java.lang.String | Tekst do ustawienia. |

### setDateTimeText(String text) {#setDateTimeText-java.lang.String-}
```
public final void setDateTimeText(String text)
```

Ustawia tekst w znaku zastępczym daty-czasu slajdu.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| text | java.lang.String | Tekst do ustawienia. |