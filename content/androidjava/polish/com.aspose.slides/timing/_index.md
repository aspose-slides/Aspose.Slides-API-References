---
title: Timing
second_title: Aspose.Slides dla Androida – odwołanie do API w Javie
description: Reprezentuje synchronizację animacji.
type: docs
url: /pl/com.aspose.slides/timing/
---
**Dziedziczenie:**
java.lang.Object

**Wszystkie zaimplementowane interfejsy:**
[com.aspose.slides.ITiming](../../com.aspose.slides/itiming), com.aspose.slides.IDOMObject
```
public class Timing implements ITiming, IDOMObject
```

Reprezentuje synchronizację animacji.
## Metody

| Metoda | Opis |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | Opisuje procent czasu trwania efektu przyspieszania. |
| [setAccelerate(float value)](#setAccelerate-float-) | Opisuje procent czasu trwania efektu przyspieszania. |
| [getDecelerate()](#getDecelerate--) | Opisuje procent czasu trwania efektu zwalniania. |
| [setDecelerate(float value)](#setDecelerate-float-) | Opisuje procent czasu trwania efektu zwalniania. |
| [getAutoReverse()](#getAutoReverse--) | Opisuje, czy animację należy automatycznie odtwarzać w odwrotnym kierunku po odtworzeniu w kierunku do przodu. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | Opisuje, czy animację należy automatycznie odtwarzać w odwrotnym kierunku po odtworzeniu w kierunku do przodu. |
| [getDuration()](#getDuration--) | Opisuje czas trwania efektu animacji. |
| [setDuration(float value)](#setDuration-float-) | Opisuje czas trwania efektu animacji. |
| [getRepeatCount()](#getRepeatCount--) | Opisuje liczbę powtórzeń efektu. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | Opisuje liczbę powtórzeń efektu. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | Ten atrybut określa, czy efekt będzie powtarzany aż do końca slajdu. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | Ten atrybut określa, czy efekt będzie powtarzany aż do końca slajdu. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | Ten atrybut określa, czy efekt będzie powtarzany aż do następnego kliknięcia. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | Ten atrybut określa, czy efekt będzie powtarzany aż do następnego kliknięcia. |
| [getRepeatDuration()](#getRepeatDuration--) | Opisuje liczbę powtórzeń efektu. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | Opisuje liczbę powtórzeń efektu. |
| [getRestart()](#getRestart--) | Określa, czy efekt ma zostać uruchomiony ponownie po zakończeniu. |
| [setRestart(int value)](#setRestart-int-) | Określa, czy efekt ma zostać uruchomiony ponownie po zakończeniu. |
| [getRewind()](#getRewind--) | Ten atrybut określa, czy efekt zostanie przewinięty po zakończeniu odtwarzania. |
| [setRewind(boolean value)](#setRewind-boolean-) | Ten atrybut określa, czy efekt zostanie przewinięty po zakończeniu odtwarzania. |
| [getSpeed()](#getSpeed--) | Określa procent, o jaki należy przyspieszyć (lub spowolnić) synchronizację. |
| [setSpeed(float value)](#setSpeed-float-) | Określa procent, o jaki należy przyspieszyć (lub spowolnić) synchronizację. |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | Opisuje czas opóźnienia po wyzwoleniu. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | Opisuje czas opóźnienia po wyzwoleniu. |
| [getTriggerType()](#getTriggerType--) | Opisuje typ wyzwalacza. |
| [setTriggerType(int value)](#setTriggerType-int-) | Opisuje typ wyzwalacza. |
| [getParent_Immediate()](#getParent-Immediate--) |  |
### getAccelerate() {#getAccelerate--}
```
public final float getAccelerate()
```

Opisuje procent czasu trwania efektu przyspieszania. Odczyt/zapis float.

**Zwraca:**
float
### setAccelerate(float value) {#setAccelerate-float-}
```
public final void setAccelerate(float value)
```

Opisuje procent czasu trwania efektu przyspieszania. Odczyt/zapis float.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public final float getDecelerate()
```

Opisuje procent czasu trwania efektu zwalniania. Odczyt/zapis float.

**Zwraca:**
float
### setDecelerate(float value) {#setDecelerate-float-}
```
public final void setDecelerate(float value)
```

Opisuje procent czasu trwania efektu zwalniania. Odczyt/zapis float.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public final boolean getAutoReverse()
```

Opisuje, czy animację należy automatycznie odtwarzać w odwrotnym kierunku po odtworzeniu w kierunku do przodu. Odczyt/zapis boolean.

**Zwraca:**
boolean
### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public final void setAutoReverse(boolean value)
```

Opisuje, czy animację należy automatycznie odtwarzać w odwrotnym kierunku po odtworzeniu w kierunku do przodu. Odczyt/zapis boolean.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public final float getDuration()
```

Opisuje czas trwania efektu animacji. Odczyt/zapis float.

**Zwraca:**
float
### setDuration(float value) {#setDuration-float-}
```
public final void setDuration(float value)
```

Opisuje czas trwania efektu animacji. Odczyt/zapis float.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public final float getRepeatCount()
```

Opisuje liczbę powtórzeń efektu. Odczyt/zapis float.

**Zwraca:**
float
### setRepeatCount(float value) {#setRepeatCount-float-}
```
public final void setRepeatCount(float value)
```

Opisuje liczbę powtórzeń efektu. Odczyt/zapis float.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public final boolean getRepeatUntilEndSlide()
```

Ten atrybut określa, czy efekt będzie powtarzany aż do końca slajdu. Odczyt/zapis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Pobierz sekwencję efektów dla pierwszego slajdu
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Pobierz pierwszy efekt głównej sekwencji.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Zmień synchronizację/powtarzanie efektu na "Do końca slajdu"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Zwraca:**
boolean
### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public final void setRepeatUntilEndSlide(boolean value)
```

Ten atrybut określa, czy efekt będzie powtarzany aż do końca slajdu. Odczyt/zapis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Pobierz sekwencję efektów dla pierwszego slajdu
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Pobierz pierwszy efekt głównej sekwencji.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Zmień synchronizację/powtórzenie efektu na "Do końca slajdu"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public final boolean getRepeatUntilNextClick()
```

Ten atrybut określa, czy efekt będzie powtarzany aż do następnego kliknięcia. Odczyt/zapis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Pobierz sekwencję efektów dla pierwszego slajdu
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Pobierz pierwszy efekt głównej sekwencji.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Zmień synchronizację/powtórzenie efektu na "Do następnego kliknięcia"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Zwraca:**
boolean
### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public final void setRepeatUntilNextClick(boolean value)
```

Ten atrybut określa, czy efekt będzie powtarzany aż do następnego kliknięcia. Odczyt/zapis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Pobierz sekwencję efektów dla pierwszego slajdu
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Pobierz pierwszy efekt głównej sekwencji.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Zmień synchronizację/powtórzenie efektu na "Do następnego kliknięcia"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public final float getRepeatDuration()
```

Opisuje liczbę powtórzeń efektu. Odczyt/zapis float.

**Zwraca:**
float
### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public final void setRepeatDuration(float value)
```

Opisuje liczbę powtórzeń efektu. Odczyt/zapis float.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public final int getRestart()
```

Określa, czy efekt ma zostać uruchomiony ponownie po zakończeniu. Odczyt/zapis [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Zwraca:**
int
### setRestart(int value) {#setRestart-int-}
```
public final void setRestart(int value)
```

Określa, czy efekt ma zostać uruchomiony ponownie po zakończeniu. Odczyt/zapis [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public final boolean getRewind()
```

Ten atrybut określa, czy efekt zostanie przewinięty po zakończeniu odtwarzania. Odczyt/zapis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Pobierz sekwencję efektów dla pierwszego slajdu
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Pobierz pierwszy efekt głównej sekwencji.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Włącz synchronizację/odtwarzanie wsteczne efektu.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Zwraca:**
boolean
### setRewind(boolean value) {#setRewind-boolean-}
```
public final void setRewind(boolean value)
```

Ten atrybut określa, czy efekt zostanie przewinięty po zakończeniu odtwarzania. Odczyt/zapis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Pobierz sekwencję efektów dla pierwszego slajdu
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Pobierz pierwszy efekt głównej sekwencji.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Włącz synchronizację/odtwarzanie wsteczne efektu.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | boolean |  |

### getSpeed() {#getSpeed--}
```
public final float getSpeed()
```

Określa procent, o jaki należy przyspieszyć (lub spowolnić) synchronizację. Odczyt/zapis float.

**Zwraca:**
float
### setSpeed(float value) {#setSpeed-float-}
```
public final void setSpeed(float value)
```

Określa procent, o jaki należy przyspieszyć (lub spowolnić) synchronizację. Odczyt/zapis float.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public final float getTriggerDelayTime()
```

Opisuje czas opóźnienia po wyzwoleniu. Odczyt/zapis float.

**Zwraca:**
float
### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public final void setTriggerDelayTime(float value)
```

Opisuje czas opóźnienia po wyzwoleniu. Odczyt/zapis float.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public final int getTriggerType()
```

Opisuje typ wyzwalacza. Odczyt/zapis [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Zwraca:**
int
### setTriggerType(int value) {#setTriggerType-int-}
```
public final void setTriggerType(int value)
```

Opisuje typ wyzwalacza. Odczyt/zapis [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | int |  |

### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

Zwraca obiekt Parent_Immediate. Tylko do odczytu IDOMObject.

**Zwraca:**
com.aspose.slides.IDOMObject