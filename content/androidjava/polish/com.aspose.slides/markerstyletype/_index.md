---
title: MarkerStyleType
second_title: Aspose.Slides dla Androida - odniesienie API Java
description: Określa formę znacznika na punkcie danych wykresu.
type: docs
url: /pl/com.aspose.slides/markerstyletype/
---
**Dziedziczenie:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class MarkerStyleType extends System.Enum
```

Określa formę znacznika na punkcie danych wykresu.
## Pola

| Pole | Opis |
| --- | --- |
| [NotDefined](#NotDefined) |  |
| [Circle](#Circle) | Określa, że ma być rysowane koło przy każdym punkcie danych. |
| [Dash](#Dash) | Określa, że ma być rysowana kreska przy każdym punkcie danych. |
| [Diamond](#Diamond) | Określa, że ma być rysowany romb przy każdym punkcie danych. |
| [Dot](#Dot) | Określa, że ma być rysowana kropka przy każdym punkcie danych. |
| [None](#None) | Określa, że nic nie ma być rysowane przy każdym punkcie danych. |
| [Picture](#Picture) | Określa, że ma być rysowany obraz przy każdym punkcie danych. |
| [Plus](#Plus) | Określa, że ma być rysowany znak plus przy każdym punkcie danych. |
| [Square](#Square) | Określa, że ma być rysowany kwadrat przy każdym punkcie danych. |
| [Star](#Star) | Określa, że ma być rysowana gwiazda przy każdym punkcie danych. |
| [Triangle](#Triangle) | Określa, że ma być rysowany trójkąt przy każdym punkcie danych. |
| [X](#X) | Określa, że ma być rysowane X przy każdym punkcie danych. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```




### Circle {#Circle}
```
public static final int Circle
```

Określa, że ma być rysowane koło przy każdym punkcie danych.

### Dash {#Dash}
```
public static final int Dash
```

Określa, że ma być rysowana kreska przy każdym punkcie danych.

### Diamond {#Diamond}
```
public static final int Diamond
```

Określa, że ma być rysowany romb przy każdym punkcie danych.

### Dot {#Dot}
```
public static final int Dot
```

Określa, że ma być rysowana kropka przy każdym punkcie danych.

### None {#None}
```
public static final int None
```

Określa, że nic nie ma być rysowane przy każdym punkcie danych.

### Picture {#Picture}
```
public static final int Picture
```

Określa, że ma być rysowany obraz przy każdym punkcie danych.

### Plus {#Plus}
```
public static final int Plus
```

Określa, że ma być rysowany znak plus przy każdym punkcie danych.

### Square {#Square}
```
public static final int Square
```

Określa, że ma być rysowany kwadrat przy każdym punkcie danych.

### Star {#Star}
```
public static final int Star
```

Określa, że ma być rysowana gwiazda przy każdym punkcie danych.

### Triangle {#Triangle}
```
public static final int Triangle
```

Określa, że ma być rysowany trójkąt przy każdym punkcie danych.

### X {#X}
```
public static final int X
```

Określa, że ma być rysowane X przy każdym punkcie danych.