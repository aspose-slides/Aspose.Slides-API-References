---
title: INormalViewRestoredProperties
second_title: Aspose.Slides for Android via Java API Reference
description: Specifies the sizing of the slide region width when a child of restoredTop height when a child of restoredLeft of the normal view when the region is of a variable restored sizeneither minimized nor maximized.
type: docs
url: /pl/com.aspose.slides/inormalviewrestoredproperties/
---
```
public interface INormalViewRestoredProperties
```

Określa rozmiar regionu slajdu ((szerokość, gdy jest dzieckiem restoredTop, wysokość, gdy jest dzieckiem restoredLeft) widoku normalnego, gdy region ma zmienny rozmiar przywrócony (nie zminimalizowany ani zmaksymalizowany)).
## Metody

| Metoda | Opis |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | Określa rozmiar regionu slajdu (szerokość, gdy jest dzieckiem RestoredTop, wysokość, gdy jest dzieckiem RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | Określa rozmiar regionu slajdu (szerokość, gdy jest dzieckiem RestoredTop, wysokość, gdy jest dzieckiem RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | Określa, czy rozmiar regionu zawartości bocznej powinien kompensować nowy rozmiar przy zmianie rozmiaru okna zawierającego widok w aplikacji Read/write boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | Określa, czy rozmiar regionu zawartości bocznej powinien kompensować nowy rozmiar przy zmianie rozmiaru okna zawierającego widok w aplikacji Read/write boolean. |
### getDimensionSize() {#getDimensionSize--}
```
public abstract float getDimensionSize()
```

Określa rozmiar regionu slajdu (szerokość, gdy jest dzieckiem RestoredTop, wysokość, gdy jest dzieckiem RestoredLeft). Read/write float.

**Zwraca:**
float
### setDimensionSize(float value) {#setDimensionSize-float-}
```
public abstract void setDimensionSize(float value)
```

Określa rozmiar regionu slajdu (szerokość, gdy jest dzieckiem RestoredTop, wysokość, gdy jest dzieckiem RestoredLeft). Read/write float.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public abstract boolean getAutoAdjust()
```

Określa, czy rozmiar regionu zawartości bocznej powinien kompensować nowy rozmiar przy zmianie rozmiaru okna zawierającego widok w aplikacji Read/write boolean.

**Zwraca:**
boolean
### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public abstract void setAutoAdjust(boolean value)
```

Określa, czy rozmiar regionu zawartości bocznej powinien kompensować nowy rozmiar przy zmianie rozmiaru okna zawierającego widok w aplikacji Read/write boolean.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | boolean |  |