---
title: WarningType
second_title: Aspose.Slides dla Androida przez API Java
description: Reprezentuje typ ostrzeżenia.
type: docs
url: /pl/com.aspose.slides/warningtype/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class WarningType extends System.Enum
```

Reprezentuje typ ostrzeżenia.

## Pola

| Pole | Opis |
| --- | --- |
| [SourceFileCorruption](#SourceFileCorruption) | W wykryto problem w dokumencie źródłowym, co bardzo prawdopodobnie uniemożliwi otwarcie dokumentu po zapisaniu w jego pierwotnym formacie. |
| [DataLoss](#DataLoss) | Tekst, wykres, obraz lub inne dane będą całkowicie nieobecne zarówno w drzewie dokumentu po załadowaniu, jak i w utworzonym dokumencie po zapisaniu. |
| [MajorFormattingLoss](#MajorFormattingLoss) | Znaczna utrata formatowania. |
| [MinorFormattingLoss](#MinorFormattingLoss) | Drobna utrata formatowania. |
| [CompatibilityIssue](#CompatibilityIssue) | Jest to znany problem, który uniemożliwi otwarcie dokumentu przez niektóre agenty użytkownika lub ich wcześniejsze wersje. |
| [UnexpectedContent](#UnexpectedContent) | Nie udało się rozpoznać niektórych treści w dokumencie źródłowym (np. |

### SourceFileCorruption {#SourceFileCorruption}
```
public static final int SourceFileCorruption
```

W wykryto problem w dokumencie źródłowym, co bardzo prawdopodobnie uniemożliwi otwarcie dokumentu po zapisaniu w jego pierwotnym formacie.

### DataLoss {#DataLoss}
```
public static final int DataLoss
```

Tekst, wykres, obraz lub inne dane będą całkowicie nieobecne zarówno w drzewie dokumentu po załadowaniu, jak i w utworzonym dokumencie po zapisaniu.

### MajorFormattingLoss {#MajorFormattingLoss}
```
public static final int MajorFormattingLoss
```

Znaczna utrata formatowania.

### MinorFormattingLoss {#MinorFormattingLoss}
```
public static final int MinorFormattingLoss
```

Drobna utrata formatowania.

### CompatibilityIssue {#CompatibilityIssue}
```
public static final int CompatibilityIssue
```

Jest to znany problem, który uniemożliwi otwarcie dokumentu przez niektóre agenty użytkownika lub ich wcześniejsze wersje.

### UnexpectedContent {#UnexpectedContent}
```
public static final int UnexpectedContent
```

Nie udało się rozpoznać niektórych treści w dokumencie źródłowym (np.