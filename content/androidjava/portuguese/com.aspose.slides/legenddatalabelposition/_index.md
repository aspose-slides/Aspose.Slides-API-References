---
title: LegendDataLabelPosition
second_title: Aspose.Slides para Android via Referência da API Java
description: Determina a posição dos rótulos de dados.
type: docs
url: /pt/com.aspose.slides/legenddatalabelposition/
---
**Herança:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class LegendDataLabelPosition extends System.Enum
```

Determina a posição dos rótulos de dados.
## Campos

| Campo | Descrição |
| --- | --- |
| [NotDefined](#NotDefined) | A posição não está definida. |
| [Bottom](#Bottom) | Especifica que os rótulos de dados devem ser exibidos abaixo do marcador de dados. |
| [BestFit](#BestFit) | Especifica que os rótulos de dados devem ser exibidos na melhor posição. |
| [Center](#Center) | Especifica que os rótulos de dados devem ser exibidos centralizados no marcador de dados. |
| [InsideBase](#InsideBase) | Especifica que os rótulos de dados devem ser exibidos dentro da base do marcador de dados. |
| [InsideEnd](#InsideEnd) | Especifica que os rótulos de dados devem ser exibidos dentro da extremidade do marcador de dados. |
| [Left](#Left) | Especifica que os rótulos de dados devem ser exibidos à esquerda do marcador de dados. |
| [OutsideEnd](#OutsideEnd) | Especifica que os rótulos de dados devem ser exibidos fora da extremidade do marcador de dados. |
| [Right](#Right) | Especifica que os rótulos de dados devem ser exibidos à direita do marcador de dados. |
| [Top](#Top) | Especifica que os rótulos de dados devem ser exibidos acima do marcador de dados. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```

A posição não está definida.

### Bottom {#Bottom}
```
public static final int Bottom
```

Especifica que os rótulos de dados devem ser exibidos abaixo do marcador de dados.

### BestFit {#BestFit}
```
public static final int BestFit
```

Especifica que os rótulos de dados devem ser exibidos na melhor posição.

### Center {#Center}
```
public static final int Center
```

Especifica que os rótulos de dados devem ser exibidos centralizados no marcador de dados.

### InsideBase {#InsideBase}
```
public static final int InsideBase
```

Especifica que os rótulos de dados devem ser exibidos dentro da base do marcador de dados.

### InsideEnd {#InsideEnd}
```
public static final int InsideEnd
```

Especifica que os rótulos de dados devem ser exibidos dentro da extremidade do marcador de dados.

### Left {#Left}
```
public static final int Left
```

Especifica que os rótulos de dados devem ser exibidos à esquerda do marcador de dados.

### OutsideEnd {#OutsideEnd}
```
public static final int OutsideEnd
```

Especifica que os rótulos de dados devem ser exibidos fora da extremidade do marcador de dados.

### Right {#Right}
```
public static final int Right
```

Especifica que os rótulos de dados devem ser exibidos à direita do marcador de dados.

### Top {#Top}
```
public static final int Top
```

Especifica que os rótulos de dados devem ser exibidos acima do marcador de dados.