---
title: IScaleEffect
second_title: Aspose.Slides para Android via Referência da API Java
description: Representa o efeito de escala de animação.
type: docs
url: /pt/com.aspose.slides/iscaleeffect/
---
**Todas as interfaces implementadas:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IScaleEffect extends IBehavior
```

Representa o efeito de escala de animação.
## Métodos

| Método | Descrição |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Determina se um conteúdo deve ser ampliado. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Determina se um conteúdo deve ser ampliado. |
| [getFrom()](#getFrom--) | Especifica uma coordenada x/y para iniciar a animação (em porcentagem). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | Especifica uma coordenada x/y para iniciar a animação (em porcentagem). |
| [getTo()](#getTo--) | Especifica a localização de destino para um efeito de escala de animação (em porcentagem). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | Especifica a localização de destino para um efeito de escala de animação (em porcentagem). |
| [getBy()](#getBy--) | Descreve o valor de deslocamento relativo para a animação (em porcentagem). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | Descreve o valor de deslocamento relativo para a animação (em porcentagem). |
### getZoomContent() {#getZoomContent--}
```
public abstract byte getZoomContent()
```


Determina se um conteúdo deve ser ampliado. Leitura/Gravação [NullableBool](../../com.aspose.slides/nullablebool).

**Retorno:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public abstract void setZoomContent(byte value)
```


Determina se um conteúdo deve ser ampliado. Leitura/Gravação [NullableBool](../../com.aspose.slides/nullablebool).

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public abstract PointF getFrom()
```


Especifica uma coordenada x/y para iniciar a animação (em porcentagem). Leitura/Gravação android.graphics.PointF.

**Retorno:**
android.graphics.PointF
### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public abstract void setFrom(PointF value)
```


Especifica uma coordenada x/y para iniciar a animação (em porcentagem). Leitura/Gravação android.graphics.PointF.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public abstract PointF getTo()
```


Especifica a localização de destino para um efeito de escala de animação (em porcentagem). Leitura/Gravação android.graphics.PointF.

**Retorno:**
android.graphics.PointF
### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public abstract void setTo(PointF value)
```


Especifica a localização de destino para um efeito de escala de animação (em porcentagem). Leitura/Gravação android.graphics.PointF.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public abstract PointF getBy()
```


Descreve o valor de deslocamento relativo para a animação (em porcentagem). Leitura/Gravação android.graphics.PointF.

**Retorno:**
android.graphics.PointF
### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public abstract void setBy(PointF value)
```


Descreve o valor de deslocamento relativo para a animação (em porcentagem). Leitura/Gravação android.graphics.PointF.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | android.graphics.PointF |  |