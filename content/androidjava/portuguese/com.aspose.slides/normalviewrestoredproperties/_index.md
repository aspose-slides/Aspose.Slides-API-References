---
title: NormalViewRestoredProperties
second_title: Aspose.Slides para Android via Referência da API Java
description: Especifica o dimensionamento da região do slide largura quando filha de restoredTop altura quando filha de restoredLeft da visualização normal quando a região tem um tamanho restaurado variável, nem minimizada nem maximizada.
type: docs
url: /pt/com.aspose.slides/normalviewrestoredproperties/
---
**Herança:**
java.lang.Object

**Todas as Interfaces Implementadas:**
[com.aspose.slides.INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)
```
public class NormalViewRestoredProperties implements INormalViewRestoredProperties
```

Especifica o dimensionamento da região do slide ((largura quando filha de restoredTop, altura quando filha de restoredLeft) da visualização normal, quando a região tem um tamanho restaurado variável (nem minimizada nem maximizada).
## Métodos

| Método | Descrição |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | Especifica o tamanho da região do slide (largura quando filha de RestoredTop, altura quando filha de RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | Especifica o tamanho da região do slide (largura quando filha de RestoredTop, altura quando filha de RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | Especifica se o tamanho da região de conteúdo lateral deve compensar o novo tamanho ao redimensionar a janela que contém a visualização dentro da aplicação booleano leitura/gravação. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | Especifica se o tamanho da região de conteúdo lateral deve compensar o novo tamanho ao redimensionar a janela que contém a visualização dentro da aplicação booleano leitura/gravação. |
### getDimensionSize() {#getDimensionSize--}
```
public final float getDimensionSize()
```

Especifica o tamanho da região do slide (largura quando filha de RestoredTop, altura quando filha de RestoredLeft). float leitura/gravação.

**Retorna:**
float
### setDimensionSize(float value) {#setDimensionSize-float-}
```
public final void setDimensionSize(float value)
```

Especifica o tamanho da região do slide (largura quando filha de RestoredTop, altura quando filha de RestoredLeft). float leitura/gravação.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public final boolean getAutoAdjust()
```

Especifica se o tamanho da região de conteúdo lateral deve compensar o novo tamanho ao redimensionar a janela que contém a visualização dentro da aplicação booleano leitura/gravação.

**Retorna:**
boolean
### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public final void setAutoAdjust(boolean value)
```

Especifica se o tamanho da região de conteúdo lateral deve compensar o novo tamanho ao redimensionar a janela que contém a visualização dentro da aplicação booleano leitura/gravação.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | boolean |  |