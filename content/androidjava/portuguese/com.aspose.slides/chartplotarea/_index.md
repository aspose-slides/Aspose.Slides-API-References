---
title: ChartPlotArea
second_title: Aspose.Slides para Android via Referência da API Java
description: Representa o retângulo onde o gráfico deve ser plotado.
type: docs
url: /pt/com.aspose.slides/chartplotarea/
---
**Herança:**
java.lang.Object, com.aspose.slides.DomObject

**Todas as Interfaces Implementadas:**
[com.aspose.slides.IChartPlotArea](../../com.aspose.slides/ichartplotarea)
```
public class ChartPlotArea extends DomObject<Chart> implements IChartPlotArea
```

Representa o retângulo onde o gráfico deve ser plotado.
## Métodos

| Método | Descrição |
| --- | --- |
| [getFormat()](#getFormat--) | Retorna o formato de uma área de plotagem. |
| [getX()](#getX--) | Retorna ou define a coordenada x do canto superior esquerdo da caixa delimitadora da área de plotagem como uma fração da largura do gráfico (de 0 a 1). |
| [setX(float value)](#setX-float-) | Retorna ou define a coordenada x do canto superior esquerdo da caixa delimitadora da área de plotagem como uma fração da largura do gráfico (de 0 a 1). |
| [getY()](#getY--) | Retorna ou define a coordenada y do canto superior esquerdo da caixa delimitadora da área de plotagem como uma fração da altura do gráfico (de 0 a 1). |
| [setY(float value)](#setY-float-) | Retorna ou define a coordenada y do canto superior esquerdo da caixa delimitadora da área de plotagem como uma fração da altura do gráfico (de 0 a 1). |
| [getWidth()](#getWidth--) | Retorna ou define a largura de uma caixa delimitadora da área de plotagem como uma fração da largura do gráfico (de 0 a 1). |
| [setWidth(float value)](#setWidth-float-) | Retorna ou define a largura de uma caixa delimitadora da área de plotagem como uma fração da largura do gráfico (de 0 a 1). |
| [getHeight()](#getHeight--) | Retorna ou define a altura de uma caixa delimitadora da área de plotagem como uma fração da altura do gráfico (de 0 a 1). |
| [setHeight(float value)](#setHeight-float-) | Retorna ou define a altura de uma caixa delimitadora da área de plotagem como uma fração da altura do gráfico (de 0 a 1). |
| [getRight()](#getRight--) | Direita. |
| [getBottom()](#getBottom--) | Inferior. |
| [getChart()](#getChart--) | Gráfico. |
| [isLocationAutocalculated()](#isLocationAutocalculated--) | Define como a localização deve ser calculada: true \\u2013 calculado automaticamente; definido pelas propriedades X, Y, Width, Height. |
| [getLayoutTargetType()](#getLayoutTargetType--) | Se o layout da área de plotagem for definido manualmente, esta propriedade especifica se o layout da área de plotagem deve ser feito por seu interior (não incluindo eixo e rótulos de eixo) ou por seu exterior (incluindo eixo e rótulos de eixo). |
| [setLayoutTargetType(int value)](#setLayoutTargetType-int-) | Se o layout da área de plotagem for definido manualmente, esta propriedade especifica se o layout da área de plotagem deve ser feito por seu interior (não incluindo eixo e rótulos de eixo) ou por seu exterior (incluindo eixo e rótulos de eixo). |
| [getActualX()](#getActualX--) | Especifica a localização real x (esquerda) do elemento do gráfico em relação ao canto superior esquerdo do gráfico. |
| [getActualY()](#getActualY--) | Especifica o topo real do elemento do gráfico em relação ao canto superior esquerdo do gráfico. |
| [getActualWidth()](#getActualWidth--) | Especifica a largura real do elemento do gráfico. |
| [getActualHeight()](#getActualHeight--) | Especifica a altura real do elemento do gráfico. |
| [getSlide()](#getSlide--) | Retorna o slide pai de um FillFormat. |
| [getPresentation()](#getPresentation--) | Retorna a apresentação pai de um FillFormat. |
### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

Retorna o formato de uma área de plotagem. Somente leitura [IFormat](../../com.aspose.slides/iformat).

**Retorna:**
[IFormat](../../com.aspose.slides/iformat)
### getX() {#getX--}
```
public final float getX()
```

Retorna ou define a coordenada x do canto superior esquerdo da caixa delimitadora da área de plotagem como uma fração da largura do gráfico (de 0 a 1). Leitura/gravação float.

**Retorna:**
float
### setX(float value) {#setX-float-}
```
public final void setX(float value)
```

Retorna ou define a coordenada x do canto superior esquerdo da caixa delimitadora da área de plotagem como uma fração da largura do gráfico (de 0 a 1). Leitura/gravação float.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | float |  |
### getY() {#getY--}
```
public final float getY()
```

Retorna ou define a coordenada y do canto superior esquerdo da caixa delimitadora da área de plotagem como uma fração da altura do gráfico (de 0 a 1). Leitura/gravação float.

**Retorna:**
float
### setY(float value) {#setY-float-}
```
public final void setY(float value)
```

Retorna ou define a coordenada y do canto superior esquerdo da caixa delimitadora da área de plotagem como uma fração da altura do gráfico (de 0 a 1). Leitura/gravação float.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | float |  |
### getWidth() {#getWidth--}
```
public final float getWidth()
```

Retorna ou define a largura de uma caixa delimitadora da área de plotagem como uma fração da largura do gráfico (de 0 a 1). Leitura/gravação float.

**Retorna:**
float
### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```

Retorna ou define a largura de uma caixa delimitadora da área de plotagem como uma fração da largura do gráfico (de 0 a 1). Leitura/gravação float.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | float |  |
### getHeight() {#getHeight--}
```
public final float getHeight()
```

Retorna ou define a altura de uma caixa delimitadora da área de plotagem como uma fração da altura do gráfico (de 0 a 1). Leitura/gravação float.

**Retorna:**
float
### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```

Retorna ou define a altura de uma caixa delimitadora da área de plotagem como uma fração da altura do gráfico (de 0 a 1). Leitura/gravação float.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | float |  |
### getRight() {#getRight--}
```
public final float getRight()
```

Direita. Somente leitura float.

**Retorna:**
float
### getBottom() {#getBottom--}
```
public final float getBottom()
```

Inferior. Somente leitura float.

**Retorna:**
float
### getChart() {#getChart--}
```
public final IChart getChart()
```

Gráfico. Somente leitura [IChart](../../com.aspose.slides/ichart).

**Retorna:**
[IChart](../../com.aspose.slides/ichart)
### isLocationAutocalculated() {#isLocationAutocalculated--}
```
public final boolean isLocationAutocalculated()
```

Define como a localização deve ser calculada: true \\u2013 calculado automaticamente; definido pelas propriedades X, Y, Width, Height. Somente leitura boolean.

**Retorna:**
boolean
### getLayoutTargetType() {#getLayoutTargetType--}
```
public final int getLayoutTargetType()
```

Se o layout da área de plotagem for definido manualmente, esta propriedade especifica se o layout da área de plotagem deve ser feito por seu interior (não incluindo eixo e rótulos de eixo) ou por seu exterior (incluindo eixo e rótulos de eixo). Leitura/gravação [LayoutTargetType](../../com.aspose.slides/layouttargettype)(\#getLayoutTargetType.getLayoutTargetType/\#setLayoutTargetType(int).setLayoutTargetType(int)).

--------------------

> ```
> Presentation presentation = new Presentation();
>   try
>   {
>       ISlide slide = presentation.getSlides().get_Item(0);
>       IChart chart = slide.getShapes().addChart(ChartType.ClusteredColumn, 20, 100, 600, 400);
>       chart.getPlotArea().setX(0.2f);
>       chart.getPlotArea().setY(0.2f);
>       chart.getPlotArea().setWidth(0.7f);
>       chart.getPlotArea().setHeight(0.7f);
>       chart.getPlotArea().setLayoutTargetType(LayoutTargetType.Inner);
>       ...
>   } finally {
>       if (presentation != null) presentation.dispose();
>   }
> ```


**Retorna:**
int
### setLayoutTargetType(int value) {#setLayoutTargetType-int-}
```
public final void setLayoutTargetType(int value)
```

Se o layout da área de plotagem for definido manualmente, esta propriedade especifica se o layout da área de plotagem deve ser feito por seu interior (não incluindo eixo e rótulos de eixo) ou por seu exterior (incluindo eixo e rótulos de eixo). Leitura/gravação [LayoutTargetType](../../com.aspose.slides/layouttargettype)(\#getLayoutTargetType.getLayoutTargetType/\#setLayoutTargetType(int).setLayoutTargetType(int)).

--------------------

> ```
> Presentation presentation = new Presentation();
>   try
>   {
>       ISlide slide = presentation.getSlides().get_Item(0);
>       IChart chart = slide.getShapes().addChart(ChartType.ClusteredColumn, 20, 100, 600, 400);
>       chart.getPlotArea().setX(0.2f);
>       chart.getPlotArea().setY(0.2f);
>       chart.getPlotArea().setWidth(0.7f);
>       chart.getPlotArea().setHeight(0.7f);
>       chart.getPlotArea().setLayoutTargetType(LayoutTargetType.Inner);
>       ...
>   } finally {
>       if (presentation != null) presentation.dispose();
>   }
> ```

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | int |  |
### getActualX() {#getActualX--}
```
public final float getActualX()
```

Especifica a localização real x (esquerda) do elemento do gráfico em relação ao canto superior esquerdo do gráfico. Chame o método IChart.ValidateChartLayout() antes para obter valores reais. Leitura float.

**Retorna:**
float
### getActualY() {#getActualY--}
```
public final float getActualY()
```

Especifica o topo real do elemento do gráfico em relação ao canto superior esquerdo do gráfico. Chame o método IChart.ValidateChartLayout() antes para obter valores reais. Leitura float.

**Retorna:**
float
### getActualWidth() {#getActualWidth--}
```
public final float getActualWidth()
```

Especifica a largura real do elemento do gráfico. Chame o método IChart.ValidateChartLayout() antes para obter valores reais. Leitura float.

**Retorna:**
float
### getActualHeight() {#getActualHeight--}
```
public final float getActualHeight()
```

Especifica a altura real do elemento do gráfico. Chame o método IChart.ValidateChartLayout() antes para obter valores reais. Leitura float.

**Retorna:**
float
### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

Retorna o slide pai de um FillFormat. Somente leitura [BaseSlide](../../com.aspose.slides/baseslide).

**Retorna:**
[IBaseSlide](../../com.aspose.slides/ibaseslide)
### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

Retorna a apresentação pai de um FillFormat. Somente leitura [IPresentation](../../com.aspose.slides/ipresentation).

**Retorna:**
[IPresentation](../../com.aspose.slides/ipresentation)