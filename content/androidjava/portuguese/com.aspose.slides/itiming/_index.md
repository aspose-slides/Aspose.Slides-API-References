---
title: ITiming
second_title: Aspose.Slides for Android via Java API Reference
description: Represents animation timing.
type: docs
url: /pt/com.aspose.slides/itiming/
---```
public interface ITiming
```

Representa o tempo de animação.
## Métodos

| Método | Descrição |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | Descreve a porcentagem do efeito de aceleração da duração. |
| [setAccelerate(float value)](#setAccelerate-float-) | Descreve a porcentagem do efeito de aceleração da duração. |
| [getDecelerate()](#getDecelerate--) | Descreve a porcentagem do efeito de desaceleração da duração. |
| [setDecelerate(float value)](#setDecelerate-float-) | Descreve a porcentagem do efeito de desaceleração da duração. |
| [getAutoReverse()](#getAutoReverse--) | Descreve se a animação deve ser reproduzida automaticamente em sentido reverso após a reprodução no sentido direto. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | Descreve se a animação deve ser reproduzida automaticamente em sentido reverso após a reprodução no sentido direto. |
| [getDuration()](#getDuration--) | Descreve a duração do efeito de animação. |
| [setDuration(float value)](#setDuration-float-) | Descreve a duração do efeito de animação. |
| [getRepeatCount()](#getRepeatCount--) | Descreve o número de vezes que o efeito deve ser repetido. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | Descreve o número de vezes que o efeito deve ser repetido. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | Este atributo especifica se o efeito será repetido até o final do slide. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | Este atributo especifica se o efeito será repetido até o final do slide. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | Este atributo especifica se o efeito será repetido até o próximo clique. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | Este atributo especifica se o efeito será repetido até o próximo clique. |
| [getRepeatDuration()](#getRepeatDuration--) | Descreve o número de vezes que o efeito deve ser repetido. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | Descreve o número de vezes que o efeito deve ser repetido. |
| [getRestart()](#getRestart--) | Especifica se um efeito deve reiniciar após ser concluído. |
| [setRestart(int value)](#setRestart-int-) | Especifica se um efeito deve reiniciar após ser concluído. |
| [getSpeed()](#getSpeed--) | Especifica a porcentagem para acelerar (ou desacelerar) o tempo. |
| [setSpeed(float value)](#setSpeed-float-) | Especifica a porcentagem para acelerar (ou desacelerar) o tempo. |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | Descreve o tempo de atraso após o gatilho. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | Descreve o tempo de atraso após o gatilho. |
| [getTriggerType()](#getTriggerType--) | Descreve o tipo de gatilho. |
| [setTriggerType(int value)](#setTriggerType-int-) | Descreve o tipo de gatilho. |
| [getRewind()](#getRewind--) | Este atributo especifica se o efeito será rebobinado ao terminar a reprodução. |
| [setRewind(boolean value)](#setRewind-boolean-) | Este atributo especifica se o efeito será rebobinado ao terminar a reprodução. |
### getAccelerate() {#getAccelerate--}
```
public abstract float getAccelerate()
```


Descreve a porcentagem do efeito de aceleração da duração. Leitura/gravação float.

**Retorno:**
float
### setAccelerate(float value) {#setAccelerate-float-}
```
public abstract void setAccelerate(float value)
```


Descreve a porcentagem do efeito de aceleração da duração. Leitura/gravação float.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public abstract float getDecelerate()
```


Descreve a porcentagem do efeito de desaceleração da duração. Leitura/gravação float.

**Retorno:**
float
### setDecelerate(float value) {#setDecelerate-float-}
```
public abstract void setDecelerate(float value)
```


Descreve a porcentagem do efeito de desaceleração da duração. Leitura/gravação float.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public abstract boolean getAutoReverse()
```


Descreve se a animação deve ser reproduzida automaticamente em sentido reverso após a reprodução no sentido direto. Leitura/gravação boolean.

**Retorno:**
boolean
### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public abstract void setAutoReverse(boolean value)
```


Descreve se a animação deve ser reproduzida automaticamente em sentido reverso após a reprodução no sentido direto. Leitura/gravação boolean.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public abstract float getDuration()
```


Descreve a duração do efeito de animação. Leitura/gravação float.

**Retorno:**
float
### setDuration(float value) {#setDuration-float-}
```
public abstract void setDuration(float value)
```


Descreve a duração do efeito de animação. Leitura/gravação float.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public abstract float getRepeatCount()
```


Descreve o número de vezes que o efeito deve ser repetido. Leitura/gravação float.

**Retorno:**
float
### setRepeatCount(float value) {#setRepeatCount-float-}
```
public abstract void setRepeatCount(float value)
```


Descreve o número de vezes que o efeito deve ser repetido. Leitura/gravação float.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public abstract boolean getRepeatUntilEndSlide()
```


Este atributo especifica se o efeito será repetido até o final do slide. Leitura/gravação boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtenha a sequência de efeitos do primeiro slide
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtenha o primeiro efeito da sequência principal.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Altere o Tempo/Repetição do efeito para "Até o fim do slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Retorno:**
boolean
### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public abstract void setRepeatUntilEndSlide(boolean value)
```


Este atributo especifica se o efeito será repetido até o final do slide. Leitura/gravação boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtenha a sequência de efeitos para o primeiro slide
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtenha o primeiro efeito da sequência principal.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Altere o Tempo/Repetição do efeito para "Até o fim do slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public abstract boolean getRepeatUntilNextClick()
```


Este atributo especifica se o efeito será repetido até o próximo clique. Leitura/gravação boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtenha a sequência de efeitos para o primeiro slide
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtenha o primeiro efeito da sequência principal.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Altere o Tempo/Repetição do efeito para "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Retorno:**
boolean
### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public abstract void setRepeatUntilNextClick(boolean value)
```


Este atributo especifica se o efeito será repetido até o próximo clique. Leitura/gravação boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtenha a sequência de efeitos para o primeiro slide
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtenha o primeiro efeito da sequência principal.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Altere o Tempo/Repetição do efeito para "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public abstract float getRepeatDuration()
```


Descreve o número de vezes que o efeito deve ser repetido. Leitura/gravação float.

**Retorno:**
float
### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public abstract void setRepeatDuration(float value)
```


Descreve o número de vezes que o efeito deve ser repetido. Leitura/gravação float.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public abstract int getRestart()
```


Especifica se um efeito deve reiniciar após ser concluído. Leitura/gravação [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Retorno:**
int
### setRestart(int value) {#setRestart-int-}
```
public abstract void setRestart(int value)
```


Especifica se um efeito deve reiniciar após ser concluído. Leitura/gravação [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | int |  |

### getSpeed() {#getSpeed--}
```
public abstract float getSpeed()
```


Especifica a porcentagem para acelerar (ou desacelerar) o tempo. Leitura/gravação float.

**Retorno:**
float
### setSpeed(float value) {#setSpeed-float-}
```
public abstract void setSpeed(float value)
```


Especifica a porcentagem para acelerar (ou desacelerar) o tempo. Leitura/gravação float.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public abstract float getTriggerDelayTime()
```


Descreve o tempo de atraso após o gatilho. Leitura/gravação float.

**Retorno:**
float
### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public abstract void setTriggerDelayTime(float value)
```


Descreve o tempo de atraso após o gatilho. Leitura/gravação float.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public abstract int getTriggerType()
```


Descreve o tipo de gatilho. Leitura/gravação [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Retorno:**
int
### setTriggerType(int value) {#setTriggerType-int-}
```
public abstract void setTriggerType(int value)
```


Descreve o tipo de gatilho. Leitura/gravação [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public abstract boolean getRewind()
```


Este atributo especifica se o efeito será rebobinado ao terminar a reprodução. Leitura/gravação boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtenha a sequência de efeitos para o primeiro slide
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtenha o primeiro efeito da sequência principal.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ative o Tempo/Rebobinar do efeito.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Retorno:**
boolean
### setRewind(boolean value) {#setRewind-boolean-}
```
public abstract void setRewind(boolean value)
```


Este atributo especifica se o efeito será rebobinado ao terminar a reprodução. Leitura/gravação boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtenha a sequência de efeitos para o primeiro slide
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtenha o primeiro efeito da sequência principal.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ative o Tempo/Rebobinar do efeito.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | boolean |  |