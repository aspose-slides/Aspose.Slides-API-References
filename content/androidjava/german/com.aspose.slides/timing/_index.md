---
title: Timing
second_title: Aspose.Slides für Android über Java API Referenz
description: Stellt die Animationszeit dar.
type: docs
url: /de/com.aspose.slides/timing/
---
**Vererbung:**
java.lang.Object

**Alle implementierten Schnittstellen:**
[com.aspose.slides.ITiming](../../com.aspose.slides/itiming), com.aspose.slides.IDOMObject
```
public class Timing implements ITiming, IDOMObject
```

Stellt die Animationszeit dar.
## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | Beschreibt den Prozentsatz der Beschleunigung über die Dauer. |
| [setAccelerate(float value)](#setAccelerate-float-) | Beschreibt den Prozentsatz der Beschleunigung über die Dauer. |
| [getDecelerate()](#getDecelerate--) | Beschreibt den Prozentsatz der Verzögerung über die Dauer. |
| [setDecelerate(float value)](#setDecelerate-float-) | Beschreibt den Prozentsatz der Verzögerung über die Dauer. |
| [getAutoReverse()](#getAutoReverse--) | Beschreibt, ob die Animation automatisch in umgekehrter Richtung abgespielt wird, nachdem sie vorwärts abgespielt wurde. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | Beschreibt, ob die Animation automatisch in umgekehrter Richtung abgespielt wird, nachdem sie vorwärts abgespielt wurde. |
| [getDuration()](#getDuration--) | Beschreibt die Dauer des Animationseffekts. |
| [setDuration(float value)](#setDuration-float-) | Beschreibt die Dauer des Animationseffekts. |
| [getRepeatCount()](#getRepeatCount--) | Beschreibt die Anzahl der Wiederholungen des Effekts. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | Beschreibt die Anzahl der Wiederholungen des Effekts. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | Dieses Attribut gibt an, ob der Effekt bis zum Ende der Folie wiederholt wird. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | Dieses Attribut gibt an, ob der Effekt bis zum Ende der Folie wiederholt wird. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | Dieses Attribut gibt an, ob der Effekt bis zum nächsten Klick wiederholt wird. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | Dieses Attribut gibt an, ob der Effekt bis zum nächsten Klick wiederholt wird. |
| [getRepeatDuration()](#getRepeatDuration--) | Beschreibt die Anzahl der Wiederholungen des Effekts. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | Beschreibt die Anzahl der Wiederholungen des Effekts. |
| [getRestart()](#getRestart--) | Gibt an, ob ein Effekt nach Abschluss neu gestartet werden soll. |
| [setRestart(int value)](#setRestart-int-) | Gibt an, ob ein Effekt nach Abschluss neu gestartet werden soll. |
| [getRewind()](#getRewind--) | Dieses Attribut gibt an, ob der Effekt nach dem Abspielen zurückgespult wird. |
| [setRewind(boolean value)](#setRewind-boolean-) | Dieses Attribut gibt an, ob der Effekt nach dem Abspielen zurückgespult wird. |
| [getSpeed()](#getSpeed--) | Gibt den Prozentsatz an, um den die Zeitbeschleunigung (oder -verlangsamung) erfolgt. |
| [setSpeed(float value)](#setSpeed-float-) | Gibt den Prozentsatz an, um den die Zeitbeschleunigung (oder -verlangsamung) erfolgt. |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | Beschreibt die Verzögerungszeit nach dem Auslöser. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | Beschreibt die Verzögerungszeit nach dem Auslöser. |
| [getTriggerType()](#getTriggerType--) | Beschreibt den Auslösertyp. |
| [setTriggerType(int value)](#setTriggerType-int-) | Beschreibt den Auslösertyp. |
| [getParent_Immediate()](#getParent-Immediate--) |  |

### getAccelerate() {#getAccelerate--}
```
public final float getAccelerate()
```

Beschreibt den Prozentsatz der Beschleunigung über die Dauer. Lese-/Schreib float.

**Rückgabewert:**
float
### setAccelerate(float value) {#setAccelerate-float-}
```
public final void setAccelerate(float value)
```

Beschreibt den Prozentsatz der Beschleunigung über die Dauer. Lese-/Schreib float.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public final float getDecelerate()
```

Beschreibt den Prozentsatz der Verzögerung über die Dauer. Lese-/Schreib float.

**Rückgabewert:**
float
### setDecelerate(float value) {#setDecelerate-float-}
```
public final void setDecelerate(float value)
```

Beschreibt den Prozentsatz der Verzögerung über die Dauer. Lese-/Schreib float.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public final boolean getAutoReverse()
```

Beschreibt, ob die Animation automatisch in umgekehrter Richtung abgespielt wird, nachdem sie vorwärts abgespielt wurde. Lese-/Schreib boolean.

**Rückgabewert:**
boolean
### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public final void setAutoReverse(boolean value)
```

Beschreibt, ob die Animation automatisch in umgekehrter Richtung abgespielt wird, nachdem sie vorwärts abgespielt wurde. Lese-/Schreib boolean.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public final float getDuration()
```

Beschreibt die Dauer des Animationseffekts. Lese-/Schreib float.

**Rückgabewert:**
float
### setDuration(float value) {#setDuration-float-}
```
public final void setDuration(float value)
```

Beschreibt die Dauer des Animationseffekts. Lese-/Schreib float.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public final float getRepeatCount()
```

Beschreibt die Anzahl der Wiederholungen des Effekts. Lese-/Schreib float.

**Rückgabewert:**
float
### setRepeatCount(float value) {#setRepeatCount-float-}
```
public final void setRepeatCount(float value)
```

Beschreibt die Anzahl der Wiederholungen des Effekts. Lese-/Schreib float.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public final boolean getRepeatUntilEndSlide()
```

Dieses Attribut gibt an, ob der Effekt bis zum Ende der Folie wiederholt wird. Lese-/Schreib boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Holt die Effektsequenz für die erste Folie
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Holt den ersten Effekt der Hauptsequenz.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ändert das Timing/Wiederholung des Effekts auf "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Rückgabewert:**
boolean
### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public final void setRepeatUntilEndSlide(boolean value)
```

Dieses Attribut gibt an, ob der Effekt bis zum Ende der Folie wiederholt wird. Lese-/Schreib boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Holt die Effektsequenz für die erste Folie
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Holt den ersten Effekt der Hauptsequenz.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ändert das Timing/Wiederholung des Effekts auf "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public final boolean getRepeatUntilNextClick()
```

Dieses Attribut gibt an, ob der Effekt bis zum nächsten Klick wiederholt wird. Lese-/Schreib boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Holt die Effektsequenz für die erste Folie
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Holt den ersten Effekt der Hauptsequenz.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ändert das Timing/Wiederholung des Effekts auf "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Rückgabewert:**
boolean
### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public final void setRepeatUntilNextClick(boolean value)
```

Dieses Attribut gibt an, ob der Effekt bis zum nächsten Klick wiederholt wird. Lese-/Schreib boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Holt die Effektsequenz für die erste Folie
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Holt den ersten Effekt der Hauptsequenz.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ändert das Timing/Wiederholung des Effekts auf "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public final float getRepeatDuration()
```

Beschreibt die Anzahl der Wiederholungen des Effekts. Lese-/Schreib float.

**Rückgabewert:**
float
### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public final void setRepeatDuration(float value)
```

Beschreibt die Anzahl der Wiederholungen des Effekts. Lese-/Schreib float.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public final int getRestart()
```

Gibt an, ob ein Effekt nach Abschluss neu gestartet werden soll. Lese-/Schreib [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Rückgabewert:**
int
### setRestart(int value) {#setRestart-int-}
```
public final void setRestart(int value)
```

Gibt an, ob ein Effekt nach Abschluss neu gestartet werden soll. Lese-/Schreib [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public final boolean getRewind()
```

Dieses Attribut gibt an, ob der Effekt nach dem Abspielen zurückgespult wird. Lese-/Schreib boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Holt die Effektsequenz für die erste Folie
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Holt den ersten Effekt der Hauptsequenz.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Schaltet das Timing/Rewind des Effekts ein.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Rückgabewert:**
boolean
### setRewind(boolean value) {#setRewind-boolean-}
```
public final void setRewind(boolean value)
```

Dieses Attribut gibt an, ob der Effekt nach dem Abspielen zurückgespult wird. Lese-/Schreib boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Holt die Effektsequenz für die erste Folie
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Holt den ersten Effekt der Hauptsequenz.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Schaltet das Timing/Rewind des Effekts ein.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | boolean |  |

### getSpeed() {#getSpeed--}
```
public final float getSpeed()
```

Gibt den Prozentsatz an, um den die Zeitbeschleunigung (oder -verlangsamung) erfolgt. Lese-/Schreib float.

**Rückgabewert:**
float
### setSpeed(float value) {#setSpeed-float-}
```
public final void setSpeed(float value)
```

Gibt den Prozentsatz an, um den die Zeitbeschleunigung (oder -verlangsamung) erfolgt. Lese-/Schreib float.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public final float getTriggerDelayTime()
```

Beschreibt die Verzögerungszeit nach dem Auslöser. Lese-/Schreib float.

**Rückgabewert:**
float
### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public final void setTriggerDelayTime(float value)
```

Beschreibt die Verzögerungszeit nach dem Auslöser. Lese-/Schreib float.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public final int getTriggerType()
```

Beschreibt den Auslösertyp. Lese-/Schreib [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Rückgabewert:**
int
### setTriggerType(int value) {#setTriggerType-int-}
```
public final void setTriggerType(int value)
```

Beschreibt den Auslösertyp. Lese-/Schreib [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | int |  |

### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

Gibt das Parent_Immediate-Objekt zurück. Nur lesen IDOMObject.

**Rückgabewert:**
com.aspose.slides.IDOMObject