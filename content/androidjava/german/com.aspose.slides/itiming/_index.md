---
title: ITiming
second_title: Aspose.Slides for Android via Java API Reference
description: Represents animation timing.
type: docs
url: /de/com.aspose.slides/itiming/
---```
public interface ITiming
```

Stellt die Animationszeit dar.
## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | Beschreibt den prozentualen Anteil der Beschleunigungsdauer des Effekts. |
| [setAccelerate(float value)](#setAccelerate-float-) | Beschreibt den prozentualen Anteil der Beschleunigungsdauer des Effekts. |
| [getDecelerate()](#getDecelerate--) | Beschreibt den prozentualen Anteil der Verzögerungsdauer des Effekts. |
| [setDecelerate(float value)](#setDecelerate-float-) | Beschreibt den prozentualen Anteil der Verzögerungsdauer des Effekts. |
| [getAutoReverse()](#getAutoReverse--) | Beschreibt, ob die Animation nach dem Vorwärtsabspielen automatisch in umgekehrter Richtung abgespielt wird. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | Beschreibt, ob die Animation nach dem Vorwärtsabspielen automatisch in umgekehrter Richtung abgespielt wird. |
| [getDuration()](#getDuration--) | Beschreibt die Dauer des Animationseffekts. |
| [setDuration(float value)](#setDuration-float-) | Beschreibt die Dauer des Animationseffekts. |
| [getRepeatCount()](#getRepeatCount--) | Beschreibt, wie oft der Effekt wiederholt werden soll. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | Beschreibt, wie oft der Effekt wiederholt werden soll. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | Dieses Attribut gibt an, ob der Effekt bis zum Ende der Folie wiederholt wird. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | Dieses Attribut gibt an, ob der Effekt bis zum Ende der Folie wiederholt wird. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | Dieses Attribut gibt an, ob der Effekt bis zum nächsten Klick wiederholt wird. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | Dieses Attribut gibt an, ob der Effekt bis zum nächsten Klick wiederholt wird. |
| [getRepeatDuration()](#getRepeatDuration--) | Beschreibt, wie oft der Effekt wiederholt werden soll. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | Beschreibt, wie oft der Effekt wiederholt werden soll. |
| [getRestart()](#getRestart--) | Gibt an, ob ein Effekt nach Abschluss neu gestartet werden soll. |
| [setRestart(int value)](#setRestart-int-) | Gibt an, ob ein Effekt nach Abschluss neu gestartet werden soll. |
| [getSpeed()](#getSpeed--) | Gibt den Prozentsatz an, um den die Zeitbeschleunigung (oder -verlangsamung) erfolgen soll. |
| [setSpeed(float value)](#setSpeed-float-) | Gibt den Prozentsatz an, um den die Zeitbeschleunigung (oder -verlangsamung) erfolgen soll. |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | Beschreibt die Verzögerungszeit nach dem Auslöser. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | Beschreibt die Verzögerungszeit nach dem Auslöser. |
| [getTriggerType()](#getTriggerType--) | Beschreibt den Auslösertyp. |
| [setTriggerType(int value)](#setTriggerType-int-) | Beschreibt den Auslösertyp. |
| [getRewind()](#getRewind--) | Dieses Attribut gibt an, ob der Effekt nach dem Abspielen zurückgespult wird. |
| [setRewind(boolean value)](#setRewind-boolean-) | Dieses Attribut gibt an, ob der Effekt nach dem Abspielen zurückgespult wird. |
### getAccelerate() {#getAccelerate--}
```
public abstract float getAccelerate()
```


Beschreibt den prozentualen Anteil der Beschleunigungsdauer des Effekts. Lesen/Schreiben float.

**Rückgabe:**
float
### setAccelerate(float value) {#setAccelerate-float-}
```
public abstract void setAccelerate(float value)
```


Beschreibt den prozentualen Anteil der Beschleunigungsdauer des Effekts. Lesen/Schreiben float.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public abstract float getDecelerate()
```


Beschreibt den prozentualen Anteil der Verzögerungsdauer des Effekts. Lesen/Schreiben float.

**Rückgabe:**
float
### setDecelerate(float value) {#setDecelerate-float-}
```
public abstract void setDecelerate(float value)
```


Beschreibt den prozentualen Anteil der Verzögerungsdauer des Effekts. Lesen/Schreiben float.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public abstract boolean getAutoReverse()
```


Beschreibt, ob die Animation nach dem Vorwärtsabspielen automatisch in umgekehrter Richtung abgespielt wird. Lesen/Schreiben boolean.

**Rückgabe:**
boolean
### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public abstract void setAutoReverse(boolean value)
```


Beschreibt, ob die Animation nach dem Vorwärtsabspielen automatisch in umgekehrter Richtung abgespielt wird. Lesen/Schreiben boolean.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public abstract float getDuration()
```


Beschreibt die Dauer des Animationseffekts. Lesen/Schreiben float.

**Rückgabe:**
float
### setDuration(float value) {#setDuration-float-}
```
public abstract void setDuration(float value)
```


Beschreibt die Dauer des Animationseffekts. Lesen/Schreiben float.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public abstract float getRepeatCount()
```


Beschreibt, wie oft der Effekt wiederholt werden soll. Lesen/Schreiben float.

**Rückgabe:**
float
### setRepeatCount(float value) {#setRepeatCount-float-}
```
public abstract void setRepeatCount(float value)
```


Beschreibt, wie oft der Effekt wiederholt werden soll. Lesen/Schreiben float.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public abstract boolean getRepeatUntilEndSlide()
```


Dieses Attribut gibt an, ob der Effekt bis zum Ende der Folie wiederholt wird. Lesen/Schreiben boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Erhalte die Effektsequenz für die erste Folie
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Erhalte den ersten Effekt der Hauptsequenz.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ändere das Timing/Wiederholung des Effekts auf "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Rückgabe:**
boolean
### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public abstract void setRepeatUntilEndSlide(boolean value)
```


Dieses Attribut gibt an, ob der Effekt bis zum Ende der Folie wiederholt wird. Lesen/Schreiben boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Erhalte die Effektsequenz für die erste Folie
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Erhalte den ersten Effekt der Hauptsequenz.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ändere das Timing/Wiederholung des Effekts auf "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public abstract boolean getRepeatUntilNextClick()
```


Dieses Attribut gibt an, ob der Effekt bis zum nächsten Klick wiederholt wird. Lesen/Schreiben boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Erhalte die Effektsequenz für die erste Folie
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Erhalte den ersten Effekt der Hauptsequenz.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ändere das Timing/Wiederholung des Effekts auf "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Rückgabe:**
boolean
### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public abstract void setRepeatUntilNextClick(boolean value)
```


Dieses Attribut gibt an, ob der Effekt bis zum nächsten Klick wiederholt wird. Lesen/Schreiben boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Erhalte die Effektsequenz für die erste Folie
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Erhalte den ersten Effekt der Hauptsequenz.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ändere das Timing/Wiederholung des Effekts auf "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public abstract float getRepeatDuration()
```


Beschreibt, wie oft der Effekt wiederholt werden soll. Lesen/Schreiben float.

**Rückgabe:**
float
### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public abstract void setRepeatDuration(float value)
```


Beschreibt, wie oft der Effekt wiederholt werden soll. Lesen/Schreiben float.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public abstract int getRestart()
```


Gibt an, ob ein Effekt nach Abschluss neu gestartet werden soll. Lesen/Schreiben [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Rückgabe:**
int
### setRestart(int value) {#setRestart-int-}
```
public abstract void setRestart(int value)
```


Gibt an, ob ein Effekt nach Abschluss neu gestartet werden soll. Lesen/Schreiben [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | int |  |

### getSpeed() {#getSpeed--}
```
public abstract float getSpeed()
```


Gibt den Prozentsatz an, um den die Zeitbeschleunigung (oder -verlangsamung) erfolgen soll. Lesen/Schreiben float.

**Rückgabe:**
float
### setSpeed(float value) {#setSpeed-float-}
```
public abstract void setSpeed(float value)
```


Gibt den Prozentsatz an, um den die Zeitbeschleunigung (oder -verlangsamung) erfolgen soll. Lesen/Schreiben float.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public abstract float getTriggerDelayTime()
```


Beschreibt die Verzögerungszeit nach dem Auslöser. Lesen/Schreiben float.

**Rückgabe:**
float
### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public abstract void setTriggerDelayTime(float value)
```


Beschreibt die Verzögerungszeit nach dem Auslöser. Lesen/Schreiben float.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public abstract int getTriggerType()
```


Beschreibt den Auslösertyp. Lesen/Schreiben [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Rückgabe:**
int
### setTriggerType(int value) {#setTriggerType-int-}
```
public abstract void setTriggerType(int value)
```


Beschreibt den Auslösertyp. Lesen/Schreiben [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public abstract boolean getRewind()
```


Dieses Attribut gibt an, ob der Effekt nach dem Abspielen zurückgespult wird. Lesen/Schreiben boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Erhalte die Effektsequenz für die erste Folie
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Erhalte den ersten Effekt der Hauptsequenz.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Aktiviere das Timing/Rewind des Effekts.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Rückgabe:**
boolean
### setRewind(boolean value) {#setRewind-boolean-}
```
public abstract void setRewind(boolean value)
```


Dieses Attribut gibt an, ob der Effekt nach dem Abspielen zurückgespult wird. Lesen/Schreiben boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Erhalte die Effektsequenz für die erste Folie
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Erhalte den ersten Effekt der Hauptsequenz.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Aktiviere das Timing/Rewind des Effekts.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | boolean |  |