---
title: NormalViewRestoredProperties
second_title: Aspose.Slides für Android über Java API-Referenz
description: Gibt die Größe des Folienbereichs an (Breite, wenn ein Kind von restoredTop, Höhe, wenn ein Kind von restoredLeft) der Normalansicht, wenn der Bereich eine variable wiederhergestellte Größe hat, weder minimiert noch maximiert.
type: docs
url: /de/com.aspose.slides/normalviewrestoredproperties/
---
**Vererbung:**
java.lang.Object

**Alle implementierten Schnittstellen:**
[com.aspose.slides.INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)
```
public class NormalViewRestoredProperties implements INormalViewRestoredProperties
```

Gibt die Größe des Folienbereichs an ((Breite, wenn ein Kind von restoredTop, Höhe, wenn ein Kind von restoredLeft) der Normalansicht, wenn der Bereich eine variable wiederhergestellte Größe hat (weder minimiert noch maximiert)).
## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | Gibt die Größe des Folienbereichs an (Breite, wenn ein Kind von RestoredTop, Höhe, wenn ein Kind von RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | Gibt die Größe des Folienbereichs an (Breite, wenn ein Kind von RestoredTop, Höhe, wenn ein Kind von RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | Gibt an, ob die Größe des Seiteninhaltsbereichs die neue Größe beim Ändern der Fenstergröße, das die Ansicht enthält, ausgleichen soll Lesen/Schreiben boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | Gibt an, ob die Größe des Seiteninhaltsbereichs die neue Größe beim Ändern der Fenstergröße, das die Ansicht enthält, ausgleichen soll Lesen/Schreiben boolean. |
### getDimensionSize() {#getDimensionSize--}
```
public final float getDimensionSize()
```

Gibt die Größe des Folienbereichs an (Breite, wenn ein Kind von RestoredTop, Höhe, wenn ein Kind von RestoredLeft). Lesen/Schreiben float.

**Rückgabe:**
float
### setDimensionSize(float value) {#setDimensionSize-float-}
```
public final void setDimensionSize(float value)
```

Gibt die Größe des Folienbereichs an (Breite, wenn ein Kind von RestoredTop, Höhe, wenn ein Kind von RestoredLeft). Lesen/Schreiben float.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public final boolean getAutoAdjust()
```

Gibt an, ob die Größe des Seiteninhaltsbereichs die neue Größe beim Ändern der Fenstergröße, das die Ansicht enthält, ausgleichen soll Lesen/Schreiben boolean.

**Rückgabe:**
boolean
### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public final void setAutoAdjust(boolean value)
```

Gibt an, ob die Größe des Seiteninhaltsbereichs die neue Größe beim Ändern der Fenstergröße, das die Ansicht enthält, ausgleichen soll Lesen/Schreiben boolean.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | boolean |  |