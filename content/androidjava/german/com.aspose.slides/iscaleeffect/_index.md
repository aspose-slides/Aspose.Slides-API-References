---
title: IScaleEffect
second_title: Aspose.Slides für Android über Java-API-Referenz
description: Stellt einen Animationsskalaeffekt dar.
type: docs
url: /de/com.aspose.slides/iscaleeffect/
---
**Alle implementierten Schnittstellen:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IScaleEffect extends IBehavior
```

Stellt einen Animationsskalaeffekt dar.
## Methoden

| Method | Description |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Bestimmt, ob ein Inhalt gezoomt werden soll. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Bestimmt, ob ein Inhalt gezoomt werden soll. |
| [getFrom()](#getFrom--) | Gibt eine x/y-Koordinate an, von der die Animation startet (in Prozent). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | Gibt eine x/y-Koordinate an, von der die Animation startet (in Prozent). |
| [getTo()](#getTo--) | Gibt den Zielort für einen Animationsskalaeffekt an (in Prozent). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | Gibt den Zielort für einen Animationsskalaeffekt an (in Prozent). |
| [getBy()](#getBy--) | Beschreibt den relativen Versatzwert für die Animation (in Prozent). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | Beschreibt den relativen Versatzwert für die Animation (in Prozent). |
### getZoomContent() {#getZoomContent--}
```
public abstract byte getZoomContent()
```

Bestimmt, ob ein Inhalt gezoomt werden soll. Lesen/Schreiben [NullableBool](../../com.aspose.slides/nullablebool).

**Rückgabe:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public abstract void setZoomContent(byte value)
```

Bestimmt, ob ein Inhalt gezoomt werden soll. Lesen/Schreiben [NullableBool](../../com.aspose.slides/nullablebool).

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | byte |  |
### getFrom() {#getFrom--}
```
public abstract PointF getFrom()
```

Gibt eine x/y-Koordinate an, von der die Animation startet (in Prozent). Lesen/Schreiben android.graphics.PointF.

**Rückgabe:**
android.graphics.PointF
### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public abstract void setFrom(PointF value)
```

Gibt eine x/y-Koordinate an, von der die Animation startet (in Prozent). Lesen/Schreiben android.graphics.PointF.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | android.graphics.PointF |  |
### getTo() {#getTo--}
```
public abstract PointF getTo()
```

Gibt den Zielort für einen Animationsskalaeffekt an (in Prozent). Lesen/Schreiben android.graphics.PointF.

**Rückgabe:**
android.graphics.PointF
### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public abstract void setTo(PointF value)
```

Gibt den Zielort für einen Animationsskalaeffekt an (in Prozent). Lesen/Schreiben android.graphics.PointF.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | android.graphics.PointF |  |
### getBy() {#getBy--}
```
public abstract PointF getBy()
```

Beschreibt den relativen Versatzwert für die Animation (in Prozent). Lesen/Schreiben android.graphics.PointF.

**Rückgabe:**
android.graphics.PointF
### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public abstract void setBy(PointF value)
```

Beschreibt den relativen Versatzwert für die Animation (in Prozent). Lesen/Schreiben android.graphics.PointF.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | android.graphics.PointF |  |