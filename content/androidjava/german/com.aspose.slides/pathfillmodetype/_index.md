---
title: PathFillModeType
second_title: Aspose.Slides für Android über die Java API Referenz
description: Gibt an, wie ein Pfad gefüllt werden soll
type: docs
url: /de/com.aspose.slides/pathfillmodetype/
---
**Vererbung:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class PathFillModeType extends System.Enum
```

Gibt an, wie ein Pfad gefüllt werden soll
## Felder

| Feld | Beschreibung |
| --- | --- |
| [None](#None) | Kein Pfadfüllung. Dies gibt an, dass der entsprechende Pfad keine Füllung hat. |
| [Normal](#Normal) | Normale Pfadfüllung. Dies gibt an, dass der entsprechende Pfad mit einer normal schattierten Farbe gefüllt wird. |
| [Lighten](#Lighten) | Aufgehellte Pfadfüllung. Dies gibt an, dass der entsprechende Pfad mit einer leicht schattierten Farbe gefüllt wird. |
| [LightenLess](#LightenLess) | Weniger aufgehellte Pfadfüllung. Dies gibt an, dass der entsprechende Pfad mit einer etwas helleren schattierten Farbe gefüllt wird. |
| [Darken](#Darken) | Verdunkelte Pfadfüllung. Dies gibt an, dass der entsprechende Pfad mit einer dunkleren schattierten Farbe gefüllt wird. |
| [DarkenLess](#DarkenLess) | Weniger verdunkelte Pfadfüllung. Dies gibt an, dass der entsprechende Pfad mit einer etwas dunkleren schattierten Farbe gefüllt wird. |
### None {#None}
```
public static final byte None
```

Kein Pfadfüllung. Dies gibt an, dass der entsprechende Pfad keine Füllung hat.

### Normal {#Normal}
```
public static final byte Normal
```

Normale Pfadfüllung. Dies gibt an, dass der entsprechende Pfad mit einer normal schattierten Farbe gefüllt wird.

### Lighten {#Lighten}
```
public static final byte Lighten
```

Aufgehellte Pfadfüllung. Dies gibt an, dass der entsprechende Pfad mit einer leicht schattierten Farbe gefüllt wird.

### LightenLess {#LightenLess}
```
public static final byte LightenLess
```

Weniger aufgehellte Pfadfüllung. Dies gibt an, dass der entsprechende Pfad mit einer etwas helleren schattierten Farbe gefüllt wird.

### Darken {#Darken}
```
public static final byte Darken
```

Verdunkelte Pfadfüllung. Dies gibt an, dass der entsprechende Pfad mit einer dunkleren schattierten Farbe gefüllt wird.

### DarkenLess {#DarkenLess}
```
public static final byte DarkenLess
```

Weniger verdunkelte Pfadfüllung. Dies gibt an, dass der entsprechende Pfad mit einer etwas dunkleren schattierten Farbe gefüllt wird.