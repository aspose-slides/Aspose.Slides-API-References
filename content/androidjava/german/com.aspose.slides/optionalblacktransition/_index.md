---
title: OptionalBlackTransition
second_title: Aspose.Slides für Android über die Java API-Referenz
description: Optionaler schwarzer Folienübergangseffekt.
type: docs
url: /de/com.aspose.slides/optionalblacktransition/
---
**Vererbung:**
java.lang.Object, [com.aspose.slides.TransitionValueBase](../../com.aspose.slides/transitionvaluebase)

**Alle implementierten Schnittstellen:**
[com.aspose.slides.IOptionalBlackTransition](../../com.aspose.slides/ioptionalblacktransition)
```
public class OptionalBlackTransition extends TransitionValueBase implements IOptionalBlackTransition
```

Optionaler schwarzer Folienübergangseffekt.
## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getFromBlack()](#getFromBlack--) | Dieses Attribut gibt an, ob der Übergang von einem schwarzen Bildschirm startet (und dann die neue Folie über Schwarz überblendet). |
| [setFromBlack(boolean value)](#setFromBlack-boolean-) | Dieses Attribut gibt an, ob der Übergang von einem schwarzen Bildschirm startet (und dann die neue Folie über Schwarz überblendet). |
### getFromBlack() {#getFromBlack--}
```
public final boolean getFromBlack()
```

Dieses Attribut gibt an, ob der Übergang von einem schwarzen Bildschirm startet (und dann die neue Folie über Schwarz überblendet). Lese-/Schreibzugriff boolean.

**Rückgabewert:**
boolean
### setFromBlack(boolean value) {#setFromBlack-boolean-}
```
public final void setFromBlack(boolean value)
```

Dieses Attribut gibt an, ob der Übergang von einem schwarzen Bildschirm startet (und dann die neue Folie über Schwarz überblendet). Lese-/Schreibzugriff boolean.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | boolean |  |