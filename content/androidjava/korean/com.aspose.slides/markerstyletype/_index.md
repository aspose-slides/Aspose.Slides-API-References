---
title: MarkerStyleType
second_title: Aspose.Slides for Android용 Java API 레퍼런스
description: 차트 데이터 포인트에 표시될 마커의 형태를 결정합니다.
type: docs
url: /ko/com.aspose.slides/markerstyletype/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class MarkerStyleType extends System.Enum
```

Determines form of marker on chart's data point.
## 필드

| 필드 | 설명 |
| --- | --- |
| [NotDefined](#NotDefined) |  |
| [Circle](#Circle) | 각 데이터 포인트에 원이 그려지도록 지정합니다. |
| [Dash](#Dash) | 각 데이터 포인트에 대시가 그려지도록 지정합니다. |
| [Diamond](#Diamond) | 각 데이터 포인트에 다이아몬드가 그려지도록 지정합니다. |
| [Dot](#Dot) | 각 데이터 포인트에 점이 그려지도록 지정합니다. |
| [None](#None) | 각 데이터 포인트에 아무것도 그려지지 않도록 지정합니다. |
| [Picture](#Picture) | 각 데이터 포인트에 그림이 그려지도록 지정합니다. |
| [Plus](#Plus) | 각 데이터 포인트에 플러스 기호가 그려지도록 지정합니다. |
| [Square](#Square) | 각 데이터 포인트에 사각형이 그려지도록 지정합니다. |
| [Star](#Star) | 각 데이터 포인트에 별이 그려지도록 지정합니다. |
| [Triangle](#Triangle) | 각 데이터 포인트에 삼각형이 그려지도록 지정합니다. |
| [X](#X) | 각 데이터 포인트에 X가 그려지도록 지정합니다. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```




### Circle {#Circle}
```
public static final int Circle
```


각 데이터 포인트에 원이 그려지도록 지정합니다.

### Dash {#Dash}
```
public static final int Dash
```


각 데이터 포인트에 대시가 그려지도록 지정합니다.

### Diamond {#Diamond}
```
public static final int Diamond
```


각 데이터 포인트에 다이아몬드가 그려지도록 지정합니다.

### Dot {#Dot}
```
public static final int Dot
```


각 데이터 포인트에 점이 그려지도록 지정합니다.

### None {#None}
```
public static final int None
```


각 데이터 포인트에 아무것도 그려지지 않도록 지정합니다.

### Picture {#Picture}
```
public static final int Picture
```


각 데이터 포인트에 그림이 그려지도록 지정합니다.

### Plus {#Plus}
```
public static final int Plus
```


각 데이터 포인트에 플러스 기호가 그려지도록 지정합니다.

### Square {#Square}
```
public static final int Square
```


각 데이터 포인트에 사각형이 그려지도록 지정합니다.

### Star {#Star}
```
public static final int Star
```


각 데이터 포인트에 별이 그려지도록 지정합니다.

### Triangle {#Triangle}
```
public static final int Triangle
```


각 데이터 포인트에 삼각형이 그려지도록 지정합니다.

### X {#X}
```
public static final int X
```


각 데이터 포인트에 X가 그려지도록 지정합니다.