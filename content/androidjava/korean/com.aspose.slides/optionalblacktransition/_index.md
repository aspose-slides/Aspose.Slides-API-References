---
title: OptionalBlackTransition
second_title: Java API 레퍼런스를 통한 Android용 Aspose.Slides
description: 옵션 검은 슬라이드 전환 효과.
type: docs
url: /ko/com.aspose.slides/optionalblacktransition/
---
**상속:**
java.lang.Object, [com.aspose.slides.TransitionValueBase](../../com.aspose.slides/transitionvaluebase)

**구현된 모든 인터페이스:**
[com.aspose.slides.IOptionalBlackTransition](../../com.aspose.slides/ioptionalblacktransition)
```
public class OptionalBlackTransition extends TransitionValueBase implements IOptionalBlackTransition
```

옵션 검은 슬라이드 전환 효과.
## 메서드

| 메서드 | 설명 |
| --- | --- |
| [getFromBlack()](#getFromBlack--) | This attribute specifies if the transition will start from a black screen (and then transition the new slide over black). Read/write boolean. |
| [setFromBlack(boolean value)](#setFromBlack-boolean-) | This attribute specifies if the transition will start from a black screen (and then transition the new slide over black). Read/write boolean. |
### getFromBlack() {#getFromBlack--}
```
public final boolean getFromBlack()
```


이 속성은 전환이 검은 화면에서 시작되는지 여부를 지정합니다(그런 다음 새 슬라이드를 검은 화면 위로 전환합니다). 읽기/쓰기 boolean.

**반환값:**
boolean
### setFromBlack(boolean value) {#setFromBlack-boolean-}
```
public final void setFromBlack(boolean value)
```


이 속성은 전환이 검은 화면에서 시작되는지 여부를 지정합니다(그런 다음 새 슬라이드를 검은 화면 위로 전환합니다). 읽기/쓰기 boolean.

**매개변수:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | boolean |  |