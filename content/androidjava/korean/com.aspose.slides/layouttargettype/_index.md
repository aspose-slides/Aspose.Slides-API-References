---
title: LayoutTargetType
second_title: Aspose.Slides Android용 Java API 참조
description: 플롯 영역 레이아웃을 수동으로 정의한 경우, 이 속성은 축 및 축 레이블을 제외한 내부에 플롯 영역을 레이아웃할지, 축 및 축 레이블을 포함한 외부에 레이아웃할지를 지정합니다.
type: docs
url: /ko/com.aspose.slides/layouttargettype/
---
**상속:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class LayoutTargetType extends System.Enum
```

플롯 영역의 레이아웃을 수동으로 정의한 경우, 이 속성은 플롯 영역을 내부(축과 축 레이블을 제외)로 레이아웃할지 외부(축과 축 레이블을 포함)로 레이아웃할지를 지정합니다.

## 필드

| 필드 | 설명 |
| --- | --- |
| [Inner](#Inner) | 플롯 영역 크기가 플롯 영역의 크기를 결정하도록 지정하며, 눈금 표시와 축 레이블은 포함하지 않습니다. |
| [Outer](#Outer) | 플롯 영역 크기가 플롯 영역, 눈금 표시 및 축 레이블의 크기를 결정하도록 지정합니다. |

### 내부 {#Inner}
```
public static final int Inner
```

플롯 영역 크기가 플롯 영역의 크기를 결정하도록 지정하며, 눈금 표시와 축 레이블은 포함하지 않습니다.

### 외부 {#Outer}
```
public static final int Outer
```

플롯 영역 크기가 플롯 영역, 눈금 표시 및 축 레이블의 크기를 결정하도록 지정합니다.