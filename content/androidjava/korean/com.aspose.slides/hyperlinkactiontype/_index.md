---
title: HyperlinkActionType
second_title: Aspose.Slides for Android용 Java API 참조
description: 하이퍼링크 동작 유형을 나타냅니다.
type: docs
url: /ko/com.aspose.slides/hyperlinkactiontype/
---
**상속:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class HyperlinkActionType extends System.Enum
```

하이퍼링크 동작 유형을 나타냅니다.
## 필드

| 필드 | 설명 |
| --- | --- |
| [Unknown](#Unknown) | 인식되지 않은 동작 유형입니다. |
| [NoAction](#NoAction) | 동작이 없습니다. |
| [Hyperlink](#Hyperlink) | 일반 하이퍼링크입니다. |
| [JumpFirstSlide](#JumpFirstSlide) | 프레젠테이션의 첫 번째 슬라이드로 이동합니다. |
| [JumpPreviousSlide](#JumpPreviousSlide) | 이전 슬라이드로 이동합니다. |
| [JumpNextSlide](#JumpNextSlide) | 다음 슬라이드로 이동합니다. |
| [JumpLastSlide](#JumpLastSlide) | 프레젠테이션의 마지막 슬라이드로 이동합니다. |
| [JumpEndShow](#JumpEndShow) | 슬라이드 쇼의 끝으로 이동합니다. |
| [JumpLastViewedSlide](#JumpLastViewedSlide) | 마지막으로 본 슬라이드로 이동합니다. |
| [JumpSpecificSlide](#JumpSpecificSlide) | 특정 슬라이드로 이동합니다. ([IHyperlink.getTargetSlide](../../com.aspose.slides/ihyperlink\#getTargetSlide)) 속성으로 지정된 슬라이드입니다. |
| [StartCustomSlideShow](#StartCustomSlideShow) | 맞춤 슬라이드 쇼를 시작합니다. |
| [OpenFile](#OpenFile) | 참조된 파일을 엽니다. |
| [OpenPresentation](#OpenPresentation) | 참조된 프레젠테이션을 엽니다. |
| [StartStopMedia](#StartStopMedia) | 미디어 파일 재생을 시작/중지합니다. |
| [StartMacro](#StartMacro) | 매크로 스크립트 실행을 시작합니다. |
| [StartProgram](#StartProgram) | 프로그램을 시작합니다. |
### Unknown {#Unknown}
```
public static final int Unknown
```

인식되지 않은 동작 유형입니다.

### NoAction {#NoAction}
```
public static final int NoAction
```

동작이 없습니다.

### Hyperlink {#Hyperlink}
```
public static final int Hyperlink
```

일반 하이퍼링크입니다.

### JumpFirstSlide {#JumpFirstSlide}
```
public static final int JumpFirstSlide
```

프레젠테이션의 첫 번째 슬라이드로 이동합니다.

### JumpPreviousSlide {#JumpPreviousSlide}
```
public static final int JumpPreviousSlide
```

이전 슬라이드로 이동합니다.

### JumpNextSlide {#JumpNextSlide}
```
public static final int JumpNextSlide
```

다음 슬라이드로 이동합니다.

### JumpLastSlide {#JumpLastSlide}
```
public static final int JumpLastSlide
```

프레젠테이션의 마지막 슬라이드로 이동합니다.

### JumpEndShow {#JumpEndShow}
```
public static final int JumpEndShow
```

슬라이드 쇼의 끝으로 이동합니다.

### JumpLastViewedSlide {#JumpLastViewedSlide}
```
public static final int JumpLastViewedSlide
```

마지막으로 본 슬라이드로 이동합니다.

### JumpSpecificSlide {#JumpSpecificSlide}
```
public static final int JumpSpecificSlide
```

특정 슬라이드로 이동합니다. ([IHyperlink.getTargetSlide](../../com.aspose.slides/ihyperlink\#getTargetSlide)) 속성으로 지정된 슬라이드입니다.

### StartCustomSlideShow {#StartCustomSlideShow}
```
public static final int StartCustomSlideShow
```

맞춤 슬라이드 쇼를 시작합니다.

### OpenFile {#OpenFile}
```
public static final int OpenFile
```

참조된 파일을 엽니다.

### OpenPresentation {#OpenPresentation}
```
public static final int OpenPresentation
```

참조된 프레젠테이션을 엽니다.

### StartStopMedia {#StartStopMedia}
```
public static final int StartStopMedia
```

미디어 파일 재생을 시작/중지합니다.

### StartMacro {#StartMacro}
```
public static final int StartMacro
```

매크로 스크립트 실행을 시작합니다.

### StartProgram {#StartProgram}
```
public static final int StartProgram
```

프로그램을 시작합니다.