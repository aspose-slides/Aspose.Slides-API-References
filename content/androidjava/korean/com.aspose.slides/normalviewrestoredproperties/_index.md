---
title: NormalViewRestoredProperties
second_title: Java API 레퍼런스를 통한 Android용 Aspose.Slides
description: 슬라이드 영역의 크기를 지정합니다. 일반 보기에서 복원된 Top의 자식인 경우 너비, 복원된 Left의 자식인 경우 높이입니다. 영역이 가변 복원 크기(최소화되지도, 최대화되지도 않음)인 경우.
type: docs
url: /ko/com.aspose.slides/normalviewrestoredproperties/
---
**Inheritance:**
java.lang.Object

**All Implemented Interfaces:**
[com.aspose.slides.INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)
```
public class NormalViewRestoredProperties implements INormalViewRestoredProperties
```

슬라이드 영역의 크기를 지정합니다 ((restoredTop의 자식인 경우 너비, restoredLeft의 자식인 경우 높이) 일반 보기에서, 영역이 가변 복원 크기(최소화되지도, 최대화되지도 않음)인 경우).

## Methods

| 메서드 | 설명 |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | 슬라이드 영역의 크기를 지정합니다 (RestoredTop의 자식인 경우 너비, RestoredLeft의 자식인 경우 높이). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | 슬라이드 영역의 크기를 지정합니다 (RestoredTop의 자식인 경우 너비, RestoredLeft의 자식인 경우 높이). |
| [getAutoAdjust()](#getAutoAdjust--) | 창이 애플리케이션 내에서 보기를 포함하는 경우 크기 조정 시 사이드 콘텐츠 영역의 크기가 새 크기를 보정하도록 지정합니다. 읽기/쓰기 boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | 창이 애플리케이션 내에서 보기를 포함하는 경우 크기 조정 시 사이드 콘텐츠 영역의 크기가 새 크기를 보정하도록 지정합니다. 읽기/쓰기 boolean. |

### getDimensionSize() {#getDimensionSize--}
```
public final float getDimensionSize()
```

슬라이드 영역의 크기를 지정합니다 (RestoredTop의 자식인 경우 너비, RestoredLeft의 자식인 경우 높이). 읽기/쓰기 float.

**반환값:**
float

### setDimensionSize(float value) {#setDimensionSize-float-}
```
public final void setDimensionSize(float value)
```

슬라이드 영역의 크기를 지정합니다 (RestoredTop의 자식인 경우 너비, RestoredLeft의 자식인 경우 높이). 읽기/쓰기 float.

**매개변수:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public final boolean getAutoAdjust()
```

창이 애플리케이션 내에서 보기를 포함하는 경우 크기 조정 시 사이드 콘텐츠 영역의 크기가 새 크기를 보정하도록 지정합니다. 읽기/쓰기 boolean.

**반환값:**
boolean

### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public final void setAutoAdjust(boolean value)
```

창이 애플리케이션 내에서 보기를 포함하는 경우 크기 조정 시 사이드 콘텐츠 영역의 크기가 새 크기를 보정하도록 지정합니다. 읽기/쓰기 boolean.

**매개변수:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | boolean |  |