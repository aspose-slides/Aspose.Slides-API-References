---
title: SmartArtLayoutType
second_title: Java API 참조를 통한 Android용 Aspose.Slides
description: SmartArt 다이어그램의 레이아웃 유형을 나타냅니다.
type: docs
url: /ko/com.aspose.slides/smartartlayouttype/
---
**Inheritance:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class SmartArtLayoutType extends System.Enum
```

SmartArt 다이어그램의 레이아웃 유형을 나타냅니다.

## 필드

| 필드 | 설명 |
| --- | --- |
| [AccentProcess](#AccentProcess) | 진행, 타임라인 또는 작업, 프로세스, 워크플로우의 순차적 단계를 표시하는 데 사용합니다. |
| [AccentedPicture](#AccentedPicture) | 중앙의 사진 아이디어와 측면의 관련 아이디어를 표시하는 데 사용합니다. |
| [AlternatingFlow](#AlternatingFlow) | 작업, 프로세스 또는 워크플로우의 정보 그룹 또는 순차적 단계를 표시하는 데 사용합니다. |
| [AlternatingHexagons](#AlternatingHexagons) | 상호 연결된 아이디어 시리즈를 나타내는 데 사용합니다. |
| [AlternatingPictureBlocks](#AlternatingPictureBlocks) | 위에서 아래로 그림 시리즈를 표시하는 데 사용합니다. |
| [AlternatingPictureCircles](#AlternatingPictureCircles) | 텍스트가 포함된 그림 세트를 표시하는 데 사용합니다. |
| [ArrowRibbon](#ArrowRibbon) | 관련 혹은 대비되는 개념을 연결된 형태로 표시하는 데 사용합니다(예: 반대 힘). |
| [AscendingPictureAccentProcess](#AscendingPictureAccentProcess) | 설명 텍스트가 포함된 상승하는 그림 시리즈를 표시하는 데 사용합니다. |
| [Balance](#Balance) | 두 아이디어 간의 관계를 비교하거나 표시하는 데 사용합니다. |
| [BasicBendingProcess](#BasicBendingProcess) | 작업, 프로세스 또는 워크플로우의 진행 또는 순차적 단계를 표시하는 데 사용합니다. |
| [BasicBlockList](#BasicBlockList) | 비순차적이거나 그룹화된 정보 블록을 표시하는 데 사용합니다. |
| [BasicChevronProcess](#BasicChevronProcess) | 진행, 타임라인, 순차적 단계 또는 움직임/방향을 강조하는 데 사용합니다. |
| [BasicCycle](#BasicCycle) | 순환 흐름에서 단계, 작업 또는 이벤트의 연속적인 순서를 나타내는 데 사용합니다. |
| [BasicMatrix](#BasicMatrix) | 사분면에서 구성 요소와 전체의 관계를 표시하는 데 사용합니다. |
| [BasicPie](#BasicPie) | 개별 부품이 전체를 형성하는 방식을 표시하는 데 사용합니다. |
| [BasicProcess](#BasicProcess) | 작업, 프로세스 또는 워크플로우의 진행 또는 순차적 단계를 표시하는 데 사용합니다. |
| [BasicPyramid](#BasicPyramid) | 가장 큰 구성 요소가 아래에 있고 위로 좁혀지는 비례, 상호 연결 또는 계층 관계를 표시합니다. |
| [BasicRadial](#BasicRadial) | 순환에서 중앙 아이디어와의 관계를 표시하는 데 사용합니다. |
| [BasicTarget](#BasicTarget) | 포함, 단계 구분 또는 계층 관계를 표시하는 데 사용합니다. |
| [BasicTimeline](#BasicTimeline) | 작업, 프로세스 또는 워크플로우의 순차적 단계 또는 타임라인 정보를 표시하는 데 사용합니다. |
| [BasicVenn](#BasicVenn) | 겹치거나 상호 연결된 관계를 표시하는 데 사용합니다. |
| [BendingPictureAccentList](#BendingPictureAccentList) | 비순차적이거나 그룹화된 정보 블록을 표시하는 데 사용합니다. |
| [BendingPictureBlocks](#BendingPictureBlocks) | 그림 시리즈를 표시하는 데 사용합니다. |
| [BendingPictureCaption](#BendingPictureCaption) | 순차적인 그림 시리즈를 표시하는 데 사용합니다. |
| [BendingPictureCaptionList](#BendingPictureCaptionList) | 그림 시리즈를 표시하는 데 사용합니다. |
| [BendingPictureSemiTransparentText](#BendingPictureSemiTransparentText) | 그림 시리즈를 표시하는 데 사용합니다. |
| [BlockCycle](#BlockCycle) | 순환 흐름에서 단계, 작업 또는 이벤트의 연속적인 순서를 나타내는 데 사용합니다. |
| [BubblePictureList](#BubblePictureList) | 그림 시리즈를 표시하는 데 사용합니다. |
| [CaptionedPictures](#CaptionedPictures) | 여러 수준의 텍스트가 포함된 그림을 표시하는 데 사용합니다. |
| [ChevronList](#ChevronList) | 전체 워크플로우를 구성하는 여러 프로세스를 통한 진행을 표시하는 데 사용합니다. |
| [CircleAccentTimeline](#CircleAccentTimeline) | 이벤트 시리즈나 타임라인 정보를 표시하는 데 사용합니다. |
| [CircleArrowProcess](#CircleArrowProcess) | 각 항목에 지원 텍스트가 있는 순차 항목을 표시하는 데 사용합니다. |
| [CirclePictureHierarchy](#CirclePictureHierarchy) | 조직 내 계층적 정보 또는 보고 관계를 표시하는 데 사용합니다. |
| [CircleRelationship](#CircleRelationship) | 중앙 아이디어와의 관계를 표시하는 데 사용합니다. |
| [CircularBendingProcess](#CircularBendingProcess) | 작업, 프로세스 또는 워크플로우에서 길고 비선형적인 순서 또는 단계를 표시하는 데 사용합니다. |
| [CircularPictureCallout](#CircularPictureCallout) | 중앙 아이디어와 부 아이디어 또는 관련 항목을 표시하는 데 사용합니다. |
| [ClosedChevronProcess](#ClosedChevronProcess) | 진행, 타임라인 또는 순차적 단계를 표시하고 움직임/방향을 강조하는 데 사용합니다. |
| [ContinuousArrowProcess](#ContinuousArrowProcess) | 타임라인 또는 작업, 프로세스, 워크플로우의 순차적 단계를 표시하는 데 사용합니다. |
| [ContinuousBlockProcess](#ContinuousBlockProcess) | 작업, 프로세스 또는 워크플로우의 진행 또는 순차적 단계를 표시하는 데 사용합니다. |
| [ContinuousCycle](#ContinuousCycle) | 순환 흐름에서 단계, 작업 또는 이벤트의 연속적인 순서를 나타내는 데 사용합니다. |
| [ContinuousPictureList](#ContinuousPictureList) | 상호 연결된 정보 그룹을 표시하는 데 사용합니다. |
| [ConvergingArrows](#ConvergingArrows) | 중앙 점으로 수렴하는 아이디어 또는 개념을 표시하는 데 사용합니다. |
| [ConvergingRadial](#ConvergingRadial) | 순환에서 중앙 아이디어에 대한 개념 또는 구성 요소의 관계를 표시합니다. |
| [CounterbalanceArrows](#CounterbalanceArrows) | 두 대립되는 아이디어 또는 개념을 표시하는 데 사용합니다. |
| [CycleMatrix](#CycleMatrix) | 순환 진행에서 중앙 아이디어와의 관계를 표시하는 데 사용합니다. |
| [DescendingBlockList](#DescendingBlockList) | 관련 아이디어 그룹 또는 정보 목록을 표시하는 데 사용합니다. |
| [DescendingProcess](#DescendingProcess) | 하강하는 이벤트 시리즈를 표시하는 데 사용합니다. |
| [DetailedProcess](#DetailedProcess) | 많은 양의 레벨 2 텍스트와 함께 단계별 진행을 표시하는 데 사용합니다. |
| [DivergingArrows](#DivergingArrows) | 중앙 소스에서 외부로 진행되는 아이디어 또는 개념을 표시하는 데 사용합니다. |
| [DivergingRadial](#DivergingRadial) | 순환에서 중앙 아이디어와의 관계를 표시합니다. |
| [Equation](#Equation) | 계획이나 결과를 나타내는 순차적 단계 또는 작업을 표시합니다. |
| [FramedTextPicture](#FramedTextPicture) | 레벨 1 텍스트가 프레임에 표시된 그림을 표시합니다. |
| [Funnel](#Funnel) | 정보 필터링 또는 부분이 전체로 병합되는 방식을 표시합니다. |
| [Gear](#Gear) | 맞물린 아이디어를 표시하는 데 사용합니다. |
| [GridMatrix](#GridMatrix) | 두 축을 따라 개념을 배치하는 데 사용합니다. |
| [GroupedList](#GroupedList) | 정보 그룹 및 하위 그룹, 또는 작업, 프로세스, 워크플로우의 단계와 하위 단계를 표시합니다. |
| [HalfCircleOrganizationChart](#HalfCircleOrganizationChart) | 조직 내 계층적 정보 또는 보고 관계를 표시합니다. |
| [HexagonCluster](#HexagonCluster) | 설명 텍스트가 포함된 그림을 표시합니다. |
| [Hierarchy](#Hierarchy) | 위에서 아래로 진행되는 계층 관계를 표시합니다. |
| [HierarchyList](#HierarchyList) | 그룹 간에 진행되는 계층 관계를 표시합니다. |
| [HorizontalBulletList](#HorizontalBulletList) | 비순차적이거나 그룹화된 정보 목록을 표시합니다. |
| [HorizontalHierarchy](#HorizontalHierarchy) | 가로로 진행되는 계층 관계를 표시합니다. |
| [HorizontalLabeledHierarchy](#HorizontalLabeledHierarchy) | 가로로 진행되고 계층적으로 그룹화된 계층 관계를 표시합니다. |
| [HorizontalMultiLevelHierarchy](#HorizontalMultiLevelHierarchy) | 가로로 진행되는 방대한 계층 정보를 표시합니다. |
| [HorizontalOrganizationChart](#HorizontalOrganizationChart) | 가로로 진행되는 계층 정보 또는 조직 내 보고 관계를 표시합니다. |
| [HorizontalPictureList](#HorizontalPictureList) | 관련된 그림에 강조를 두고 비순차적이거나 그룹화된 정보를 표시합니다. |
| [IncreasingArrowsProcess](#IncreasingArrowsProcess) | 프로세스의 순차적 및 겹치는 단계를 표시합니다. |
| [IncreasingCircleProcess](#IncreasingCircleProcess) | 원 내부가 단계마다 커지는 일련의 단계를 표시합니다. |
| [InvertedPyramid](#InvertedPyramid) | 가장 큰 구성 요소가 위에 있고 아래로 좁혀지는 비례, 상호 연결 또는 계층 관계를 표시합니다. |
| [LabeledHierarchy](#LabeledHierarchy) | 위에서 아래로 진행되고 계층적으로 그룹화된 관계를 표시합니다. |
| [LinearVenn](#LinearVenn) | 순서에서 겹치는 관계를 표시합니다. |
| [LinedList](#LinedList) | 카테고리와 하위 카테고리로 나뉜 대량의 텍스트를 표시합니다. |
| [MultidirectionalCycle](#MultidirectionalCycle) | 어느 방향으로든 발생할 수 있는 연속적인 단계 시퀀스를 나타냅니다. |
| [NameandTitleOrganizationChart](#NameandTitleOrganizationChart) | 조직 내 계층 정보 또는 보고 관계를 표시합니다. |
| [NestedTarget](#NestedTarget) | 포함 관계를 표시합니다. |
| [NondirectionalCycle](#NondirectionalCycle) | 순환 흐름에서 연속적인 단계 시퀀스를 나타냅니다. |
| [OpposingArrows](#OpposingArrows) | 두 대립되는 아이디어 또는 중앙점에서 갈라지는 아이디어를 표시합니다. |
| [OpposingIdeas](#OpposingIdeas) | 두 대립되거나 대비되는 아이디어를 표시합니다. |
| [OrganizationChart](#OrganizationChart) | 조직 내 계층 정보 또는 보고 관계를 표시합니다. |
| [PhasedProcess](#PhasedProcess) | 프로세스의 세 단계를 표시합니다. |
| [PictureAccentBlocks](#PictureAccentBlocks) | 코너에서 시작하는 블록 형태의 그림 그룹을 표시합니다. |
| [PictureAccentList](#PictureAccentList) | 그룹화되거나 관련된 정보를 표시합니다. |
| [PictureAccentProcess](#PictureAccentProcess) | 작업, 프로세스 또는 워크플로우의 순차적 단계를 표시합니다. |
| [PictureCaptionList](#PictureCaptionList) | 비순차적이거나 그룹화된 정보 블록을 표시합니다. |
| [PictureGrid](#PictureGrid) | 정사각형 격자에 배치된 그림을 표시합니다. |
| [PictureLineup](#PictureLineup) | 나란히 배치된 그림 시리즈를 표시합니다. |
| [PictureStrips](#PictureStrips) | 레벨 1 텍스트가 각 그림 옆에 있는 위에서 아래로 그림 시리즈를 표시합니다. |
| [PieProcess](#PieProcess) | 파이 슬라이스가 최대 7개의 형태로 커지는 프로세스 단계를 표시합니다. |
| [PlusandMinus](#PlusandMinus) | 두 아이디어의 장단점을 표시합니다. |
| [ProcessArrows](#ProcessArrows) | 프로세스나 워크플로우를 설명하는 정보를 표시합니다. |
| [ProcessList](#ProcessList) | 여러 그룹의 정보 또는 작업, 프로세스, 워크플로우의 단계와 하위 단계를 표시합니다. |
| [PyramidList](#PyramidList) | 비례, 상호 연결 또는 계층 관계를 표시합니다. |
| [RadialCluster](#RadialCluster) | 중앙 아이디어 또는 주제와 관련된 데이터를 표시합니다. |
| [RadialCycle](#RadialCycle) | 중앙 아이디어와의 관계를 표시합니다. |
| [RadialList](#RadialList) | 순환에서 중앙 아이디어와의 관계를 표시합니다. |
| [RadialVenn](#RadialVenn) | 겹치는 관계와 순환에서 중앙 아이디어와의 관계를 모두 표시합니다. |
| [RandomToResultProcess](#RandomToResultProcess) | 일련의 단계 를 통해 여러 혼돈 아이디어가 하나의 통합된 목표 또는 아이디어로 연결되는 과정을 표시합니다. |
| [RepeatingBendingProcess](#RepeatingBendingProcess) | 작업, 프로세스 또는 워크플로우의 진행 또는 순차적 단계를 표시합니다. |
| [ReverseList](#ReverseList) | 두 항목 사이를 전환하는 데 사용합니다. |
| [SegmentedCycle](#SegmentedCycle) | 순환 흐름에서 단계, 작업 또는 이벤트의 연속적인 시퀀스를 표시합니다. |
| [SegmentedProcess](#SegmentedProcess) | 작업, 프로세스 또는 워크플로우의 진행 또는 순차적 단계를 표시합니다. |
| [SegmentedPyramid](#SegmentedPyramid) | 포함, 비례 또는 상호 연결 관계를 표시합니다. |
| [SnapshotPictureList](#SnapshotPictureList) | 설명 텍스트가 포함된 그림을 표시합니다. |
| [SpiralPicture](#SpiralPicture) | 중심으로 나선형으로 배열된 최대 다섯 개의 그림과 레벨 1 캡션을 표시합니다. |
| [SquareAccentList](#SquareAccentList) | 카테고리별로 나뉜 정보 목록을 표시합니다. |
| [StackedList](#StackedList) | 작업, 프로세스, 워크플로우의 정보 그룹 또는 단계를 표시합니다. |
| [StackedVenn](#StackedVenn) | 겹치는 관계를 표시합니다. |
| [StaggeredProcess](#StaggeredProcess) | 단계별 하향 진행을 표시합니다. |
| [StepDownProcess](#StepDownProcess) | 다단계 및 하위 단계가 포함된 하강 프로세스를 표시합니다. |
| [StepUpProcess](#StepUpProcess) | 상승하는 단계 또는 정보 목록을 표시합니다. |
| [SubStepProcess](#SubStepProcess) | 각 레벨 1 텍스트 사이에 하위 단계가 포함된 다단계 프로세스를 표시합니다. |
| [TableHierarchy](#TableHierarchy) | 위에서 아래로 구축된 정보 그룹과 각 그룹 내 계층을 표시합니다. |
| [TableList](#TableList) | 동등한 가치를 가진 그룹화된 또는 관련된 정보를 표시합니다. |
| [TargetList](#TargetList) | 상호 연관되거나 겹치는 정보를 표시합니다. |
| [TextCycle](#TextCycle) | 순환 흐름에서 단계, 작업, 이벤트의 연속적인 시퀀스를 나타냅니다. |
| [TitlePictureLineup](#TitlePictureLineup) | 각기 제목과 설명이 있는 그림 시리즈를 표시합니다. |
| [TitledMatrix](#TitledMatrix) | 전체에 대한 사분면 관계를 표시합니다. |
| [TitledPictureAccentList](#TitledPictureAccentList) | 레벨 2 텍스트마다 강조 그림이 있는 정보 목록을 표시합니다. |
| [TitledPictureBlocks](#TitledPictureBlocks) | 그림 시리즈를 표시합니다. |
| [TrapezoidList](#TrapezoidList) | 동등한 가치를 가진 그룹화된 또는 관련된 정보를 표시합니다. |
| [UpwardArrow](#UpwardArrow) | 작업, 프로세스, 워크플로우에서 상승하는 진행이나 단계를 표시합니다. |
| [VerticalAccentList](#VerticalAccentList) | 정보 목록을 표시합니다. |
| [VerticalArrowList](#VerticalArrowList) | 공통 목표를 향해 진행되는 작업, 프로세스, 워크플로우의 진행 또는 순차적 단계를 표시합니다. |
| [VerticalBendingProcess](#VerticalBendingProcess) | 작업, 프로세스, 워크플로우의 진행 또는 순차적 단계를 표시합니다. |
| [VerticalBlockList](#VerticalBlockList) | 작업, 프로세스, 워크플로우의 정보 그룹 또는 단계를 표시합니다. |
| [VerticalBoxList](#VerticalBoxList) | 특히 레벨 2 텍스트가 많은 여러 정보 그룹을 표시합니다. |
| [VerticalBulletList](#VerticalBulletList) | 비순차적이거나 그룹화된 정보 블록을 표시합니다. |
| [VerticalChevronList](#VerticalChevronList) | 작업, 프로세스, 워크플로우의 진행 또는 순차적 단계를 표시하거나 움직임/방향을 강조합니다. |
| [VerticalCircleList](#VerticalCircleList) | 순차적이거나 그룹화된 데이터를 표시합니다. |
| [VerticalCurvedList](#VerticalCurvedList) | 곡선 형태의 정보 목록을 표시합니다. |
| [VerticalEquation](#VerticalEquation) | 계획이나 결과를 나타내는 순차적 단계 또는 작업을 표시합니다. |
| [VerticalPictureAccentList](#VerticalPictureAccentList) | 비순차적이거나 그룹화된 정보 블록을 표시합니다. |
| [VerticalPictureList](#VerticalPictureList) | 비순차적이거나 그룹화된 정보 블록을 표시합니다. |
| [VerticalProcess](#VerticalProcess) | 위에서 아래로 진행되는 작업, 프로세스, 워크플로우의 진행 또는 순차적 단계를 표시합니다. |
| [Custom](#Custom) | 사용자 지정 레이아웃 템플릿이 적용된 SmartArt 다이어그램을 나타냅니다. |
| [PictureOrganizationChart](#PictureOrganizationChart) | 조직 내 계층 정보 또는 보고 관계를 그림과 함께 표시합니다. |
### AccentProcess {#AccentProcess}
```
public static final int AccentProcess
```

작업, 프로세스 또는 워크플로우의 진행, 타임라인 또는 순차적 단계를 표시하는 데 사용합니다. 레벨 1 및 레벨 2 텍스트 모두를 설명하는 데 적합합니다.

### AccentedPicture {#AccentedPicture}
```
public static final int AccentedPicture
```

중앙의 사진 아이디어와 측면의 관련 아이디어를 표시하는 데 사용합니다. 상단 레벨 1 텍스트가 중앙 그림 위에 나타납니다. 다른 레벨 1 도형에 대한 텍스트는 작은 원형 그림 옆에 나타납니다. 텍스트가 없는 경우에도 이 레이아웃은 잘 동작합니다.

### AlternatingFlow {#AlternatingFlow}
```
public static final int AlternatingFlow
```

작업, 프로세스 또는 워크플로우의 정보 그룹 또는 순차적 단계를 표시합니다. 정보 그룹 간의 상호 작용 또는 관계를 강조합니다.

### AlternatingHexagons {#AlternatingHexagons}
```
public static final int AlternatingHexagons
```

상호 연결된 아이디어 시리즈를 나타내는 데 사용합니다. 레벨 1 텍스트는 육각형 안에, 레벨 2 텍스트는 도형 외부에 표시됩니다.

### AlternatingPictureBlocks {#AlternatingPictureBlocks}
```
public static final int AlternatingPictureBlocks
```

위에서 아래로 그림 시리즈를 표시합니다. 텍스트는 그림의 오른쪽 또는 왼쪽에 번갈아 가며 표시됩니다.

### AlternatingPictureCircles {#AlternatingPictureCircles}
```
public static final int AlternatingPictureCircles
```

텍스트가 포함된 그림 세트를 표시합니다. 해당 텍스트는 그림이 좌우로 교차하면서 중앙 원에 표시됩니다.

### ArrowRibbon {#ArrowRibbon}
```
public static final int ArrowRibbon
```

관련 혹은 대비되는 개념을 연결된 형태로 표시합니다(예: 반대 힘). 레벨 1 텍스트의 앞 두 줄이 화살표 텍스트에 사용됩니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### AscendingPictureAccentProcess {#AscendingPictureAccentProcess}
```
public static final int AscendingPictureAccentProcess
```

설명 텍스트가 포함된 상승하는 그림 시리즈를 표시합니다. 텍스트가 적을 때 가장 효과적입니다.

### Balance {#Balance}
```
public static final int Balance
```

두 아이디어 간의 관계를 비교하거나 표시합니다. 레벨 1 텍스트의 첫 두 줄이 중앙점 양쪽 상단에 해당하는 텍스트에 매핑됩니다. 레벨 2 텍스트는 각 측면에 네 개의 도형으로 제한됩니다. 레벨 2 텍스트가 가장 많이 포함된 쪽으로 균형이 기울어집니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### BasicBendingProcess {#BasicBendingProcess}
```
public static final int BasicBendingProcess
```

작업, 프로세스 또는 워크플로우의 진행 또는 순차적 단계를 표시합니다. 도형의 가로·세로 표시 공간을 최대화합니다.

### BasicBlockList {#BasicBlockList}
```
public static final int BasicBlockList
```

비순차적이거나 그룹화된 정보 블록을 표시합니다. 도형의 가로·세로 표시 공간을 최대화합니다.

### BasicChevronProcess {#BasicChevronProcess}
```
public static final int BasicChevronProcess
```

진행, 타임라인, 순차적 단계 또는 움직임/방향을 강조하는 데 사용합니다. 레벨 1 텍스트는 화살표 도형 안에, 레벨 2 텍스트는 화살표 도형 아래에 표시됩니다.

### BasicCycle {#BasicCycle}
```
public static final int BasicCycle
```

순환 흐름에서 단계, 작업 또는 이벤트의 연속적인 시퀀스를 나타냅니다. 연결 화살표보다 단계 자체를 강조합니다. 레벨 1 텍스트만 있을 때 가장 효과적입니다.

### BasicMatrix {#BasicMatrix}
```
public static final int BasicMatrix
```

사분면에서 구성 요소와 전체의 관계를 표시합니다. 레벨 1 텍스트의 처음 네 줄이 사분면에 배치됩니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### BasicPie {#BasicPie}
```
public static final int BasicPie
```

개별 부품이 전체를 형성하는 방식을 표시합니다. 레벨 1 텍스트의 처음 일곱 줄이 고르게 배분된 파이 조각에 매핑됩니다. 상단 레벨 1 텍스트 도형은 파이 외부에 강조 표시됩니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### BasicProcess {#BasicProcess}
```
public static final int BasicProcess
```

작업, 프로세스 또는 워크플로우의 진행 또는 순차적 단계를 표시합니다.

### BasicPyramid {#BasicPyramid}
```
public static final int BasicPyramid
```

가장 큰 구성 요소가 아래에 있고 위로 좁혀지는 비례, 상호 연결 또는 계층 관계를 표시합니다. 레벨 1 텍스트는 피라미드 구획에, 레벨 2 텍스트는 각 구획 옆 도형에 표시됩니다.

### BasicRadial {#BasicRadial}
```
public static final int BasicRadial
```

순환에서 중앙 아이디어와의 관계를 표시합니다. 레벨 1 텍스트 첫 줄이 중앙 도형에, 레벨 2 텍스트는 주변 원형 도형에 매핑됩니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### BasicTarget {#BasicTarget}
```
public static final int BasicTarget
```

포함, 단계 구분 또는 계층 관계를 표시합니다. 레벨 1 텍스트 처음 다섯 줄이 원과 연결됩니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### BasicTimeline {#BasicTimeline}
```
public static final int BasicTimeline
```

작업, 프로세스 또는 워크플로우의 순차적 단계 또는 타임라인 정보를 표시합니다. 레벨 1 및 레벨 2 텍스트 모두에 적합합니다.

### BasicVenn {#BasicVenn}
```
public static final int BasicVenn
```

겹치거나 상호 연결된 관계를 표시합니다. 레벨 1 텍스트 처음 일곱 줄이 원에 매핑됩니다. 레벨 1 텍스트가 네 줄 이하이면 텍스트가 원 안에, 그보다 많으면 원 밖에 표시됩니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### BendingPictureAccentList {#BendingPictureAccentList}
```
public static final int BendingPictureAccentList
```

비순차적이거나 그룹화된 정보 블록을 표시합니다. 작은 원형 도형은 그림을 포함하도록 설계되었습니다. 레벨 1 및 레벨 2 텍스트 모두에 적합하며 도형의 가로·세로 공간을 최대화합니다.

### BendingPictureBlocks {#BendingPictureBlocks}
```
public static final int BendingPictureBlocks
```

그림 시리즈를 표시합니다. 아래 코너를 차지하는 박스에 작은 양의 텍스트를 넣을 수 있습니다.

### BendingPictureCaption {#BendingPictureCaption}
```
public static final int BendingPictureCaption
```

순차적인 그림 시리즈를 표시합니다. 아래 코너를 차지하는 박스에 작은 양의 텍스트를 넣을 수 있습니다.

### BendingPictureCaptionList {#BendingPictureCaptionList}
```
public static final int BendingPictureCaptionList
```

그림 시리즈를 표시합니다. 제목과 설명이 각 그림 아래의 말풍선 도형에 나타납니다.

### BendingPictureSemiTransparentText {#BendingPictureSemiTransparentText}
```
public static final int BendingPictureSemiTransparentText
```

그림 시리즈를 표시합니다. 반투명 박스가 그림 하단을 가리고 모든 레벨의 텍스트를 포함합니다.

### BlockCycle {#BlockCycle}
```
public static final int BlockCycle
```

순환 흐름에서 단계, 작업, 이벤트의 연속적인 시퀀스를 나타냅니다. 단계 자체를 강조하고 화살표는 부차적입니다.

### BubblePictureList {#BubblePictureList}
```
public static final int BubblePictureList
```

그림 시리즈를 표시합니다. 최대 8개의 레벨 1 그림을 포함할 수 있습니다. 사용되지 않은 텍스트와 그림은 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다. 텍스트가 적을 때 가장 효과적입니다.

### CaptionedPictures {#CaptionedPictures}
```
public static final int CaptionedPictures
```

다중 레벨 텍스트가 포함된 그림을 표시합니다. 적은 양의 레벨 1 텍스트와 중간 양의 레벨 2 텍스트에 가장 적합합니다.

### ChevronList {#ChevronList}
```
public static final int ChevronList
```

전체 워크플로우를 구성하는 여러 프로세스의 진행을 표시합니다. 대비되는 프로세스도 표시 가능합니다. 레벨 1 텍스트는 좌측 첫 화살표 도형에, 레벨 2 텍스트는 각 레벨 1 도형에 대한 가로 하위 단계에 매핑됩니다.

### CircleAccentTimeline {#CircleAccentTimeline}
```
public static final int CircleAccentTimeline
```

이벤트 시리즈 또는 타임라인 정보를 표시합니다. 레벨 1 텍스트는 큰 원형 도형 옆에, 레벨 2 텍스트는 작은 원형 도형 옆에 나타납니다.

### CircleArrowProcess {#CircleArrowProcess}
```
public static final int CircleArrowProcess
```

각 항목에 대한 지원 텍스트가 있는 순차적 항목을 표시합니다. 레벨 1 텍스트가 적을 때 가장 적합합니다.

### CirclePictureHierarchy {#CirclePictureHierarchy}
```
public static final int CirclePictureHierarchy
```

조직 내 계층 정보 또는 보고 관계를 표시합니다. 그림은 원 안에, 해당 텍스트는 그림 옆에 나타납니다.

### CircleRelationship {#CircleRelationship}
```
public static final int CircleRelationship
```

중앙 아이디어와의 관계 또는 중앙 아이디어에서의 관계를 표시합니다. 레벨 2 텍스트는 비순차적으로 추가되며 최대 다섯 항목으로 제한됩니다. 레벨 1 항목은 하나만 있을 수 있습니다.

### CircularBendingProcess {#CircularBendingProcess}
```
public static final int CircularBendingProcess
```

길거나 비선형적인 작업, 프로세스, 워크플로우의 순서를 표시합니다. 레벨 1 텍스트만 있을 때 가장 효과적이며 도형의 가로·세로 공간을 최대화합니다.

### CircularPictureCallout {#CircularPictureCallout}
```
public static final int CircularPictureCallout
```

중앙 아이디어와 부아이디어 또는 관련 항목을 표시합니다. 첫 번째 그림의 텍스트가 그림 하단을 차지하고, 다른 레벨 1 도형에 대한 텍스트는 작은 원형 그림 옆에 나타납니다. 텍스트가 없어도 잘 동작합니다.

### ClosedChevronProcess {#ClosedChevronProcess}
```
public static final int ClosedChevronProcess
```

진행, 타임라인 또는 순차적 단계와 함께 움직임/방향을 강조합니다. 시작 도형의 정보를 강조하는 데 사용할 수 있습니다. 레벨 1 텍스트만 있을 때 가장 효과적입니다.

### ContinuousArrowProcess {#ContinuousArrowProcess}
```
public static final int ContinuousArrowProcess
```

타임라인 또는 순차적 단계를 표시합니다. 레벨 1 텍스트가 화살표 도형 안에, 레벨 2 텍스트가 화살표 도형 밖에 나타납니다. 레벨 1 텍스트가 주로 사용됩니다.

### ContinuousBlockProcess {#ContinuousBlockProcess}
```
public static final int ContinuousBlockProcess
```

진행 또는 순차적 단계를 표시합니다. 최소한의 레벨 1·레벨 2 텍스트에 가장 적합합니다.

### ContinuousCycle {#ContinuousCycle}
```
public static final int ContinuousCycle
```

순환 흐름에서 단계, 작업, 이벤트의 연속적인 시퀀스를 나타냅니다. 모든 구성 요소 간의 연결을 강조합니다. 레벨 1 텍스트만 있을 때 가장 효과적입니다.

### ContinuousPictureList {#ContinuousPictureList}
```
public static final int ContinuousPictureList
```

상호 연결된 정보 그룹을 표시합니다. 원형 도형은 그림을 포함하도록 설계되었습니다.

### ConvergingArrows {#ConvergingArrows}
```
public static final int ConvergingArrows
```

중앙점으로 수렴하는 아이디어 또는 개념을 표시합니다. 레벨 1 텍스트만 있을 때 가장 효과적입니다.

### ConvergingRadial {#ConvergingRadial}
```
public static final int ConvergingRadial
```

순환에서 중앙 아이디어에 대한 개념 또는 구성 요소의 관계를 표시합니다. 레벨 1 텍스트 첫 줄이 중앙 원형 도형에, 레벨 2 텍스트는 주변 사각형 도형에 매핑됩니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### CounterbalanceArrows {#CounterbalanceArrows}
```
public static final int CounterbalanceArrows
```

두 대립되는 아이디어 또는 개념을 표시합니다. 레벨 1 텍스트 첫 두 줄이 각각 화살표에 매핑되며 레벨 2 텍스트와 잘 어울립니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### CycleMatrix {#CycleMatrix}
```
public static final int CycleMatrix
```

순환 진행에서 중앙 아이디어와의 관계를 표시합니다. 레벨 1 텍스트 처음 네 줄이 파이 또는 웨지 도형에, 레벨 2 텍스트는 해당 도형 옆의 사각형에 표시됩니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### DescendingBlockList {#DescendingBlockList}
```
public static final int DescendingBlockList
```

관련 아이디어 또는 정보 목록을 그룹화합니다. 텍스트 도형이 높이가 순차적으로 감소하고 레벨 1 텍스트가 수직으로 표시됩니다.

### DescendingProcess {#DescendingProcess}
```
public static final int DescendingProcess
```

하강하는 이벤트 시리즈를 표시합니다. 레벨 1 텍스트 첫 번째가 화살표 상단에, 마지막은 화살표 하단에 표시됩니다. 처음 일곱 개의 레벨 1 항목만 표시됩니다. 텍스트가 적당히 있을 때 가장 효과적입니다.

### DetailedProcess {#DetailedProcess}
```
public static final int DetailedProcess
```

많은 양의 레벨 2 텍스트와 함께 단계별 진행을 표시합니다.

### DivergingArrows {#DivergingArrows}
```
public static final int DivergingArrows
```

중앙 소스에서 외부로 진행되는 아이디어 또는 개념을 표시합니다. 레벨 1 텍스트만 있을 때 가장 효과적입니다.

### DivergingRadial {#DivergingRadial}}
```
public static final int DivergingRadial
```

순환에서 중앙 아이디어에 대한 관계를 표시합니다. 레벨 1 텍스트 첫 줄이 중앙 원형 도형에 매핑됩니다. 주변 원을 강조하고 중앙 아이디어는 부차적입니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### Equation {#Equation}
```
public static final int Equation
```

계획이나 결과를 나타내는 순차적 단계 또는 작업을 표시합니다. 마지막 레벨 1 텍스트 줄이 등호(=) 뒤에 나타납니다. 레벨 1 텍스트만 있을 때 가장 효과적입니다.

### FramedTextPicture {#FramedTextPicture}
```
public static final int FramedTextPicture
```

레벨 1 텍스트가 프레임 안에 표시되는 그림을 보여줍니다.

### Funnel {#Funnel}
```
public static final int Funnel
```

정보 필터링 또는 부분이 전체로 병합되는 방식을 표시합니다. 최종 결과를 강조합니다. 최대 네 줄의 레벨 1 텍스트를 포함할 수 있으며, 마지막 줄은 퍼널 아래에, 나머지는 원형 도형에 매핑됩니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### Gear {#Gear}
```
public static final int Gear
```

맞물린 아이디어를 표시합니다. 레벨 1 텍스트 처음 세 줄이 기어 도형에, 대응하는 레벨 2 텍스트는 기어 옆 사각형에 나타납니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### GridMatrix {#GridMatrix}
```
public static final int GridMatrix
```

두 축을 따라 개념을 배치합니다. 전체보다 개별 구성 요소를 강조합니다. 레벨 1 텍스트 처음 네 줄이 사분면에 표시됩니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### GroupedList {#GroupedList}
```
public static final int GroupedList
```

정보 그룹 및 하위 그룹, 또는 작업, 프로세스, 워크플로우의 단계와 하위 단계를 표시합니다. 레벨 1 텍스트는 최상위 가로 도형에, 레벨 2 텍스트는 각 상위 도형 아래의 세로 하위 단계에 매핑됩니다. 하위 그룹·하위 단계·계층 정보·다중 목록 강조에 적합합니다.

### HalfCircleOrganizationChart {#HalfCircleOrganizationChart}
```
public static final int HalfCircleOrganizationChart
```

조직 내 계층 정보 또는 보고 관계를 표시합니다. 보조 도형 및 Org Chart hanging 레이아웃이 지원됩니다.

### HexagonCluster {#HexagonCluster}
```
public static final int HexagonCluster
```

연관된 설명 텍스트가 있는 그림을 표시합니다. 작은 육각형이 그림·텍스트 쌍을 나타냅니다. 텍스트가 적을 때 가장 효과적입니다.

### Hierarchy {#Hierarchy}
```
public static final int Hierarchy
```

위에서 아래로 진행되는 계층 관계를 표시합니다.

### HierarchyList {#HierarchyList}
```
public static final int HierarchyList
```

그룹 간에 진행되는 계층 관계를 표시합니다. 정보 그룹 또는 목록에도 사용할 수 있습니다.

### HorizontalBulletList {#HorizontalBulletList}
```
public static final int HorizontalBulletList
```

비순차적이거나 그룹화된 정보 목록을 표시합니다. 텍스트가 많을 때 적합하며 모든 텍스트가 동일한 강조 수준을 가지고 방향은 암시되지 않습니다.

### HorizontalHierarchy {#HorizontalHierarchy}
```
public static final int HorizontalHierarchy
```

가로로 진행되는 계층 관계를 표시합니다. 의사결정 트리에 적합합니다.

### HorizontalLabeledHierarchy {#HorizontalLabeledHierarchy}
```
public static final int HorizontalLabeledHierarchy
```

가로로 진행되고 계층적으로 그룹화된 관계를 표시합니다. 레벨 1(헤딩) 텍스트를 강조합니다. 첫 번째 레벨 1 텍스트 줄이 계층 시작 도형에, 두 번째 이후 줄은 높은 사각형 상단에 표시됩니다.

### HorizontalMultiLevelHierarchy {#HorizontalMultiLevelHierarchy}
```
public static final int HorizontalMultiLevelHierarchy
```

많은 양의 계층 정보를 가로로 진행하면서 표시합니다. 계층 상단이 수직으로 표시됩니다. 다수 레벨의 계층을 지원합니다.

### HorizontalOrganizationChart {#HorizontalOrganizationChart}
```
public static final int HorizontalOrganizationChart
```

가로 방향으로 조직의 계층 정보 또는 보고 관계를 표시합니다. 보조 도형 및 Org Chart hanging 레이아웃이 지원됩니다.

### HorizontalPictureList {#HorizontalPictureList}
```
public static final int HorizontalPictureList
```

관련 그림에 강조를 두고 비순차적·그룹화된 정보를 표시합니다. 상단 도형은 그림을 포함하도록 설계되었습니다.

### IncreasingArrowsProcess {#IncreasingArrowsProcess}
```
public static final int IncreasingArrowsProcess
```

프로세스에서 순차적이며 겹치는 단계를 표시합니다. 레벨 1 항목은 최대 다섯 개이며 레벨 2는 많은 텍스트를 포함할 수 있습니다.

### IncreasingCircleProcess {#IncreasingCircleProcess}
```
public static final int IncreasingCircleProcess
```

단계마다 원 내부가 커지는 일련의 단계를 표시합니다. 레벨 1 단계는 최대 일곱 개이며 레벨 2 항목은 무제한입니다. 레벨 2 텍스트가 많을 때 적합합니다.

### InvertedPyramid {#InvertedPyramid}
```
public static final int InvertedPyramid
```

가장 큰 구성 요소가 위에 있고 아래로 좁혀지는 비례, 상호 연결 또는 계층 관계를 표시합니다. 레벨 1 텍스트는 피라미드 구획에, 레벨 2 텍스트는 각 구획 옆 도형에 나타납니다.

### LabeledHierarchy {#LabeledHierarchy}
```
public static final int LabeledHierarchy
```

위에서 아래로 진행되고 계층적으로 그룹화된 관계를 표시합니다. 레벨 1(헤딩) 텍스트를 강조합니다. 첫 번째 레벨 1 텍스트는 계층 시작 도형에, 이후 모든 레벨 1 텍스트는 긴 사각형 왼쪽에 표시됩니다.

### LinearVenn {#LinearVenn}
```
public static final int LinearVenn
```

시퀀스 내 겹치는 관계를 표시합니다. 레벨 1 텍스트만 있을 때 가장 효과적입니다.

### LinedList {#LinedList}
```
public static final int LinedList
```

카테고리와 하위 카테고리로 나뉜 대량의 텍스트를 표시합니다. 여러 레벨의 텍스트에 적합하며 같은 수준의 텍스트는 선으로 구분됩니다.

### MultidirectionalCycle {#MultidirectionalCycle}
```
public static final int MultidirectionalCycle
```

어느 방향으로든 발생할 수 있는 연속적인 단계 시퀀스를 나타냅니다.

### NameandTitleOrganizationChart {#NameandTitleOrganizationChart}
```
public static final int NameandTitleOrganizationChart
```

조직 내 계층 정보 또는 보고 관계를 표시합니다. 제목 상자에 텍스트를 입력하려면 작은 사각형 도형에 직접 입력합니다. 보조 도형 및 Org Chart hanging 레이아웃이 지원됩니다.

### NestedTarget {#NestedTarget}
```
public static final int NestedTarget
```

포함 관계를 표시합니다. 레벨 1 텍스트 처음 세 줄이 도형 왼쪽 상단 텍스트에, 레벨 2 텍스트는 작은 도형에 매핑됩니다. 레벨 2 텍스트가 최소일 때 가장 효과적이며, 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### NondirectionalCycle {#NondirectionalCycle}
```
public static final int NondirectionalCycle
```

어느 방향으로든 발생할 수 있는 연속적인 단계 시퀀스를 표시합니다. 모든 도형이 동일한 중요도를 가지며, 방향을 표시할 필요가 없을 때 적합합니다.

### OpposingArrows {#OpposingArrows}
```
public static final int OpposingArrows
```

두 대립되는 아이디어 또는 중앙점에서 갈라지는 아이디어를 표시합니다. 레벨 1 텍스트 처음 두 줄이 각각 화살표에 매핑됩니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### OpposingIdeas {#OpposingIdeas}
```
public static final int OpposingIdeas
```

두 대립되거나 대비되는 아이디어를 표시합니다. 레벨 1 항목이 하나 또는 두 개일 수 있으며, 각 레벨 1 텍스트는 다중 하위 수준을 포함할 수 있습니다. 텍스트가 많을 때 적합합니다.

### OrganizationChart {#OrganizationChart}
```
public static final int OrganizationChart
```

조직 내 계층 정보 또는 보고 관계를 표시합니다. 보조 도형 및 Org Chart hanging 레이아웃이 지원됩니다.

### PhasedProcess {#PhasedProcess}
```
public static final int PhasedProcess
```

프로세스의 세 단계를 표시합니다. 레벨 1 항목은 최대 세 개이며, 첫 두 레벨 1 항목은 각각 네 개의 레벨 2 항목을, 세 번째 레벨 1 항목은 무제한 레벨 2 항목을 포함할 수 있습니다. 텍스트가 적을 때 가장 효과적입니다.

### PictureAccentBlocks {#PictureAccentBlocks}
```
public static final int PictureAccentBlocks
```

코너에서 시작하는 블록 형태의 그림 그룹을 표시합니다. 대응 텍스트가 수직으로 표시됩니다. 제목 또는 부제 슬라이드의 강조 또는 문서 구분에 적합합니다.

### PictureAccentList {#PictureAccentList}
```
public static final int PictureAccentList
```

그룹화되거나 관련된 정보를 표시합니다. 상단 코너의 작은 도형은 그림을 포함하도록 설계되었습니다. 레벨 2 텍스트가 레벨 1 텍스트보다 강조되며, 레벨 2 텍스트가 많은 경우에 적합합니다.

### PictureAccentProcess {#PictureAccentProcess}
```
public static final int PictureAccentProcess
```

작업, 프로세스 또는 워크플로우의 순차적 단계를 표시합니다. 배경의 사각형 도형은 그림을 포함하도록 설계되었습니다.

### PictureCaptionList {#PictureCaptionList}
```
public static final int PictureCaptionList
```

비순차적이거나 그룹화된 정보 블록을 표시합니다. 상단 도형은 그림을 포함하도록 설계되었으며, 그림이 텍스트보다 강조됩니다. 짧은 텍스트 캡션이 있는 그림에 적합합니다.

### PictureGrid {#PictureGrid}
```
public static final int PictureGrid
```

정사각형 격자에 배치된 그림을 표시합니다. 레벨 1 텍스트가 적을 때 가장 좋으며, 그림 위에 표시됩니다.

### PictureLineup {#PictureLineup}
```
public static final int PictureLineup
```

나란히 배치된 그림 시리즈를 표시합니다. 레벨 1 텍스트가 그림 상단을 덮으며, 레벨 2 텍스트는 그림 아래에 나타납니다.

### PictureStrips {#PictureStrips}
```
public static final int PictureStrips
```

위에서 아래로 그림 시리즈를 표시하고 각 그림 옆에 레벨 1 텍스트를 배치합니다.

### PieProcess {#PieProcess}
```
public static final int PieProcess
```

프로세스 단계가 각각 파이 조각이 커지는 형태로 표시됩니다. 최대 일곱 개의 도형까지 가능하며, 레벨 1 텍스트는 수직으로 표시됩니다.

### PlusandMinus {#PlusandMinus}
```
public static final int PlusandMinus
```

두 아이디어의 장단점을 표시합니다. 각 레벨 1 텍스트는 다중 하위 수준을 포함할 수 있습니다. 텍스트가 많을 때 적합하며, 레벨 1 항목은 두 개로 제한됩니다.

### ProcessArrows {#ProcessArrows}
```
public static final int ProcessArrows
```

프로세스 또는 워크플로우를 설명하는 정보를 표시합니다. 레벨 1 텍스트는 원형 도형에, 레벨 2 텍스트는 화살표 도형에 나타납니다. 텍스트가 최소일 때 가장 효과적이며 움직임/방향을 강조합니다.

### ProcessList {#ProcessList}
```
public static final int ProcessList
```

여러 정보 그룹 또는 작업, 프로세스, 워크플로우의 단계와 하위 단계를 표시합니다. 레벨 1 텍스트는 최상위 가로 도형에, 레벨 2 텍스트는 각 상위 도형 아래의 세로 하위 단계에 매핑됩니다.

### PyramidList {#PyramidList}
```
public static final int PyramidList
```

비례, 상호 연결 또는 계층 관계를 표시합니다. 텍스트는 피라미드 배경 위의 사각형 도형에 표시됩니다.

### RadialCluster {#RadialCluster}
```
public static final int RadialCluster
```

중심 아이디어 또는 주제와 관련된 데이터를 표시합니다. 레벨 1 텍스트는 중앙에, 레벨 2 텍스트는 주변 도형에 표시됩니다. 최대 일곱 개의 레벨 2 도형을 포함할 수 있습니다. 텍스트가 적을 때 가장 효과적이며, 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### RadialCycle {#RadialCycle}
```
public static final int RadialCycle
```

중앙 아이디어와의 관계를 표시합니다. 중앙 원에 대한 레벨 1 텍스트와 외부 원에 대한 레벨 2 텍스트를 모두 강조합니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### RadialList {#RadialList}
```
public static final int RadialList
```

중앙 아이디어와의 관계를 순환에서 표시합니다. 중심 도형에 그림을 넣을 수 있습니다. 레벨 1 텍스트는 작은 원에, 관련 레벨 2 텍스트는 작은 원 옆에 나타납니다.

### RadialVenn {#RadialVenn}
```
public static final int RadialVenn
```

겹치는 관계와 순환에서 중앙 아이디어와의 관계를 모두 표시합니다. 레벨 1 텍스트 첫 줄이 중앙 도형에, 레벨 2 텍스트는 주변 원형 도형에 매핑됩니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### RandomToResultProcess {#RandomToResultProcess}
```
public static final int RandomToResultProcess
```

일련의 단계 를 통해 여러 혼돈 아이디어가 통합된 목표 또는 아이디어로 이어지는 과정을 표시합니다. 여러 레벨 1 텍스트 항목을 지원하지만 첫 번째와 마지막 레벨 1 도형은 고정됩니다. 레벨 1 텍스트가 적고 레벨 2 텍스트가 중간 정도일 때 가장 효과적입니다.

### RepeatingBendingProcess {#RepeatingBendingProcess}
```
public static final int RepeatingBendingProcess
```

작업, 프로세스 또는 워크플로우의 진행 또는 순차적 단계를 표시합니다. 도형의 가로·세로 공간을 최대화합니다.

### ReverseList {#ReverseList}
```
public static final int ReverseList
```

두 항목 사이를 전환하는 데 사용합니다. 텍스트의 앞 두 항목만 표시되며 각 항목에 많은 텍스트를 포함할 수 있습니다. 두 항목 간의 변화를 보여주거나 순서 전환에 적합합니다.

### SegmentedCycle {#SegmentedCycle}
```
public static final int SegmentedCycle
```

원형 흐름에서 단계, 작업 또는 이벤트의 순차적 시퀀스를 표시합니다. 상호 연결된 조각을 강조합니다. 레벨 1 텍스트 처음 일곱 줄이 웨지 또는 파이 도형에 매핑됩니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### SegmentedProcess {#SegmentedProcess}
```
public static final int SegmentedProcess
```

작업, 프로세스 또는 워크플로우의 진행 또는 순차적 단계를 표시합니다. 레벨 2 텍스트가 강조되며, 각 줄이 별도의 도형에 표시됩니다.

### SegmentedPyramid {#SegmentedPyramid}
```
public static final int SegmentedPyramid
```

포함, 비례 또는 상호 연결 관계를 표시합니다. 레벨 1 텍스트 처음 아홉 줄이 삼각형 도형에 표시됩니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다. 레벨 1 텍스트만 있을 때 가장 효과적입니다.

### SnapshotPictureList {#SnapshotPictureList}
```
public static final int SnapshotPictureList
```

설명 텍스트가 포함된 그림을 표시합니다. 레벨 2 텍스트는 정보 목록을 표시할 수 있습니다. 많은 양의 텍스트에 적합합니다.

### SpiralPicture {#SpiralPicture}
```
public static final int SpiralPicture
```

중심으로 나선형으로 배치된 최대 다섯 개의 그림과 해당 레벨 1 캡션을 표시합니다.

### SquareAccentList {#SquareAccentList}
```
public static final int SquareAccentList
```

카테고리별로 나뉜 정보 목록을 표시합니다. 레벨 2 텍스트는 작은 정사각형 도형 옆에 나타납니다. 레벨 2 텍스트가 많을 때 적합합니다.

### StackedList {#StackedList}
```
public static final int StackedList
```

작업, 프로세스, 워크플로우의 정보 그룹 또는 단계를 표시합니다. 원형 도형에 레벨 1 텍스트가, 대응하는 사각형에 레벨 2 텍스트가 들어갑니다. 세부 사항이 많고 레벨 1 텍스트가 최소일 때 적합합니다.

### StackedVenn {#StackedVenn}}
```
public static final int StackedVenn
```

겹치는 관계를 표시합니다. 성장 또는 단계 구분을 강조하는 데 좋습니다. 레벨 1 텍스트만 있을 때 가장 효과적이며, 첫 일곱 줄이 원형 도형에 매핑됩니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### StaggeredProcess {#StaggeredProcess}
```
public static final int StaggeredProcess
```

단계별 하향 진행을 표시합니다. 레벨 1 텍스트 처음 다섯 줄이 사각형에 매핑됩니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### StepDownProcess {#StepDownProcess}
```
public static final int StepDownProcess
```

다단계 및 하위 단계가 포함된 하강 프로세스를 표시합니다. 텍스트가 적을 때 가장 효과적입니다.

### StepUpProcess {#StepUpProcess}
```
public static final int StepUpProcess
```

상승하는 단계 시리즈 또는 정보 목록을 표시합니다.

### SubStepProcess {#SubStepProcess}
```
public static final int SubStepProcess
```

레벨 1 텍스트 사이에 하위 단계가 포함된 다단계 프로세스를 표시합니다. 텍스트가 적을 때 가장 효과적이며, 레벨 1 단계는 최대 일곱 개이며 각 단계는 무제한 하위 단계를 가질 수 있습니다.

### TableHierarchy {#TableHierarchy}
```
public static final int TableHierarchy
```

위에서 아래로 구축된 정보 그룹 및 각 그룹 내 계층을 표시합니다. 이 레이아웃은 연결 선을 포함하지 않습니다.

### TableList {#TableList}
```
public static final int TableList
```

동등한 가치를 가진 그룹화된 또는 관련된 정보를 표시합니다. 첫 레벨 1 텍스트 줄이 최상위 도형에 매핑되고, 해당 레벨 2 텍스트가 후속 목록에 사용됩니다.

### TargetList {#TargetList}
```
public static final int TableList
```

상호 연관되거나 겹치는 정보를 표시합니다. 레벨 1 텍스트 처음 일곱 줄이 사각형 도형에 나타납니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다. 레벨 1·레벨 2 텍스트 모두에 적합합니다.

### TextCycle {#TextCycle}
```
public static final int TextCycle
```

순환 흐름에서 단계, 작업, 이벤트의 연속적인 시퀀스를 나타냅니다. 단계 자체보다 화살표·흐름을 강조합니다. 레벨 1 텍스트만 있을 때 가장 효과적입니다.

### TitlePictureLineup {#TitlePictureLineup}
```
public static final int TitlePictureLineup
```

각각 고유한 제목과 설명을 가진 그림 시리즈를 표시합니다. 레벨 1 텍스트는 그림 위 박스에, 레벨 2 텍스트는 그림 아래에 나타납니다.

### TitledMatrix {#TitledMatrix}
```
public static final int TitledMatrix
```

네 사분면과 전체와의 관계를 표시합니다. 레벨 1 텍스트 첫 줄이 중앙 도형에, 레벨 2 텍스트 처음 네 줄이 사분면에 나타납니다. 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### TitledPictureAccentList {#TitledPictureAccentList}
```
public static final int TitledPictureAccentList
```

각 레벨 2 텍스트에 강조 그림이 있는 정보 목록을 표시합니다. 레벨 1 텍스트는 목록 상단 별도 박스에 표시됩니다.

### TitledPictureBlocks {#TitledPictureBlocks}
```
public static final int TitledPictureBlocks
```

그림 시리즈를 표시합니다. 레벨 1 텍스트는 각 그림 위에, 레벨 2 텍스트는 옆에 약간 겹치게 표시됩니다.

### TrapezoidList {#TrapezoidList}
```
public static final int TrapezoidList
```

동등한 가치를 가진 그룹화·관련 정보를 표시합니다. 텍스트가 많을 때 적합합니다.

### UpwardArrow {#UpwardArrow}
```
public static final int UpwardArrow
```

작업, 프로세스 또는 워크플로우에서 상승하는 진행 또는 단계를 표시합니다. 레벨 1 텍스트 처음 다섯 줄이 화살표의 각 점에 매핑됩니다. 텍스트가 최소일 때 가장 효과적이며, 사용되지 않은 텍스트는 보이지 않지만 레이아웃을 전환하면 사용할 수 있습니다.

### VerticalAccentList {#VerticalAccentList}
```
public static final int VerticalAccentList
```

정보 목록을 표시합니다. 레벨 2 텍스트는 수직 셰브론 위의 사각형 도형에 나타납니다. 레벨 2 텍스트가 레벨 1 텍스트보다 강조되며, 중간 양의 레벨 2 텍스트에 적합합니다.

### VerticalArrowList {#VerticalArrowList}
```
public static final int VerticalArrowList
```

작업, 프로세스 또는 워크플로우에서 공통 목표를 향해 진행하거나 순차적 단계를 표시합니다. 정보의 글머리표 목록에 적합합니다.

### VerticalBendingProcess {#VerticalBendingProcess}
```
public static final int VerticalBendingProcess
```

작업, 프로세스 또는 워크플로우의 진행 또는 순차적 단계를 표시합니다. 도형의 가로·세로 공간을 최대화하며, 도형 간 상호 관계를 방향·움직임보다 강조합니다.

### VerticalBlockList {#VerticalBlockList}
```
public static final int VerticalBlockList
```

작업, 프로세스, 워크플로우의 정보 그룹 또는 단계를 표시합니다. 레벨 2 텍스트가 많을 때 적합하며, 주요 요점과 여러 하위 요점을 가진 텍스트에 좋습니다.

### VerticalBoxList {#VerticalBoxList}
```
public static final int VerticalBoxList
```

특히 레벨 2 텍스트가 많은 여러 정보 그룹을 표시합니다. 글머리표 목록에 적합합니다.

### VerticalBulletList {#VerticalBulletList}
```
public static final int VerticalBulletList
```

비순차적이거나 그룹화된 정보 블록을 표시합니다. 긴 헤딩이나 최상위 정보가 있는 목록에 적합합니다.

### VerticalChevronList {#VerticalChevronList}
```
public static final int VerticalChevronList
```

작업, 프로세스 또는 워크플로우의 진행 또는 순차적 단계를 표시하거나 움직임·방향을 강조합니다. 레벨 2 텍스트가 레벨 1 텍스트보다 강조되며, 레벨 2 텍스트가 많은 경우에 좋습니다.

### VerticalCircleList {#VerticalCircleList}
```
public static final int VerticalCircleList
```

순차적이거나 그룹화된 데이터를 표시합니다. 레벨 1 텍스트는 큰 원형 도형 옆에 표시되며, 하위 레벨 텍스트는 작은 원형 도형으로 구분됩니다.

### VerticalCurvedList {#VerticalCurvedList}
```
public static final int VerticalCurvedList
```

곡선 형태의 정보 목록을 표시합니다. 강조 원형 도형에 그림을 채우면 그림을 추가할 수 있습니다.

### VerticalEquation {#VerticalEquation}
```
public static final int VerticalEquation
```

계획이나 결과를 나타내는 순차적 단계 또는 작업을 표시합니다. 마지막 레벨 1 텍스트 줄이 화살표 뒤에 나타납니다. 레벨 1 텍스트만 있을 때 가장 효과적입니다.

### VerticalPictureAccentList {#VerticalPictureAccentList}
```
public static final int VerticalPictureAccentList
```

비순차적이거나 그룹화된 정보 블록을 표시합니다. 작은 원은 그림을 포함하도록 설계되었습니다.

### VerticalPictureList {#VerticalPictureList}
```
public static final int VerticalPictureList
```

비순차적이거나 그룹화된 정보 블록을 표시합니다. 왼쪽의 작은 도형은 그림을 포함하도록 설계되었습니다.

### VerticalProcess {#VerticalProcess}
```
public static final int VerticalProcess
```

작업, 프로세스 또는 워크플로우의 위에서 아래로 진행 또는 순차적 단계를 표시합니다. 수직 공간이 제한되어 있어 레벨 1 텍스트에 가장 적합합니다.

### Custom {#Custom}
```
public static final int Custom
```

사용자 지정 레이아웃 템플릿이 적용된 SmartArt 다이어그램을 나타냅니다.

### PictureOrganizationChart {#PictureOrganizationChart}
```
public static final int PictureOrganizationChart
```

조직 내 계층 정보 또는 보고 관계를 그림과 함께 표시합니다. 보조 도형 및 Org Chart hanging 레이아웃이 지원됩니다.