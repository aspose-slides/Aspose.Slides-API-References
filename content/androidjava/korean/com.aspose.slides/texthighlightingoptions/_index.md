---
title: TextHighlightingOptions
second_title: Java API 참조를 통한 Aspose.Slides for Android
description: 텍스트 프레임에서 텍스트를 강조 표시하는 데 사용할 수 있는 옵션을 나타냅니다.
type: docs
url: /ko/com.aspose.slides/texthighlightingoptions/
---
**상속:**  
java.lang.Object

**모든 구현된 인터페이스:**  
[com.aspose.slides.ITextHighlightingOptions](../../com.aspose.slides/itexthighlightingoptions)  
```
public class TextHighlightingOptions implements ITextHighlightingOptions
```

텍스트 프레임에서 텍스트를 강조 표시하는 데 사용할 수 있는 옵션을 나타냅니다.
## 생성자

| 생성자 | 설명 |
| --- | --- |
| [TextHighlightingOptions()](#TextHighlightingOptions--) | 새 기본 텍스트 강조 옵션을 생성합니다. |
## 메서드

| 메서드 | 설명 |
| --- | --- |
| [getCaseSensitive()](#getCaseSensitive--) | 대소문자 구분 검색을 사용하려면 true로 설정하고, 그렇지 않으면 false로 설정합니다. |
| [setCaseSensitive(boolean value)](#setCaseSensitive-boolean-) | 대소문자 구분 검색을 사용하려면 true로 설정하고, 그렇지 않으면 false로 설정합니다. |
| [getWholeWordsOnly()](#getWholeWordsOnly--) | 전체 단어만 일치시키려면 true로 설정하고, 그렇지 않으면 false로 설정합니다. |
| [setWholeWordsOnly(boolean value)](#setWholeWordsOnly-boolean-) | 전체 단어만 일치시키려면 true로 설정하고, 그렇지 않으면 false로 설정합니다. |
### TextHighlightingOptions() {#TextHighlightingOptions--}
```
public TextHighlightingOptions()
```

새 기본 텍스트 강조 옵션을 생성합니다.

### getCaseSensitive() {#getCaseSensitive--}
```
public final boolean getCaseSensitive()
```

대소문자 구분 검색을 사용하려면 true로 설정하고, 그렇지 않으면 false로 설정합니다. 읽기/쓰기 boolean.

**반환값:**  
boolean
### setCaseSensitive(boolean value) {#setCaseSensitive-boolean-}
```
public final void setCaseSensitive(boolean value)
```

대소문자 구분 검색을 사용하려면 true로 설정하고, 그렇지 않으면 false로 설정합니다. 읽기/쓰기 boolean.

**매개변수:**  
| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| value | boolean |  |
### getWholeWordsOnly() {#getWholeWordsOnly--}
```
public final boolean getWholeWordsOnly()
```

전체 단어만 일치시키려면 true로 설정하고, 그렇지 않으면 false로 설정합니다. 읽기/쓰기 boolean.

**반환값:**  
boolean
### setWholeWordsOnly(boolean value) {#setWholeWordsOnly-boolean-}
```
public final void setWholeWordsOnly(boolean value)
```

전체 단어만 일치시키려면 true로 설정하고, 그렇지 않으면 false로 설정합니다. 읽기/쓰기 boolean.

**매개변수:**  
| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| value | boolean |  |