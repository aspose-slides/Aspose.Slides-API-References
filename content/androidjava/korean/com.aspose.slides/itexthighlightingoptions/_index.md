---
title: ITextHighlightingOptions
second_title: Aspose.Slides for Android via Java API Reference
description: 텍스트 프레임의 텍스트를 강조 표시하는 데 사용할 수 있는 옵션을 나타냅니다.
type: docs
url: /ko/com.aspose.slides/itexthighlightingoptions/
---```
public interface ITextHighlightingOptions
```

텍스트 프레임의 텍스트를 강조 표시하는 데 사용할 수 있는 옵션을 나타냅니다.
## 메서드

| Method | Description |
| --- | --- |
| [getCaseSensitive()](#getCaseSensitive--) | 대소문자를 구분하는 검색을 사용하려면 true를 설정하고, 그렇지 않으면 false를 설정합니다. |
| [setCaseSensitive(boolean value)](#setCaseSensitive-boolean-) | 대소문자를 구분하는 검색을 사용하려면 true를 설정하고, 그렇지 않으면 false를 설정합니다. |
| [getWholeWordsOnly()](#getWholeWordsOnly--) | 전체 단어만 일치시키려면 true를 설정하고, 그렇지 않으면 false를 설정합니다. |
| [setWholeWordsOnly(boolean value)](#setWholeWordsOnly-boolean-) | 전체 단어만 일치시키려면 true를 설정하고, 그렇지 않으면 false를 설정합니다. |
### getCaseSensitive() {#getCaseSensitive--}
```
public abstract boolean getCaseSensitive()
```

대소문자를 구분하는 검색을 사용하려면 true를 설정하고, 그렇지 않으면 false를 설정합니다. 읽기/쓰기 boolean.

**반환:**
boolean
### setCaseSensitive(boolean value) {#setCaseSensitive-boolean-}
```
public abstract void setCaseSensitive(boolean value)
```

대소문자를 구분하는 검색을 사용하려면 true를 설정하고, 그렇지 않으면 false를 설정합니다. 읽기/쓰기 boolean.

**매개변수:**
| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| value | boolean |  |

### getWholeWordsOnly() {#getWholeWordsOnly--}
```
public abstract boolean getWholeWordsOnly()
```

전체 단어만 일치시키려면 true를 설정하고, 그렇지 않으면 false를 설정합니다. 읽기/쓰기 boolean.

**반환:**
boolean
### setWholeWordsOnly(boolean value) {#setWholeWordsOnly-boolean-}
```
public abstract void setWholeWordsOnly(boolean value)
```

전체 단어만 일치시키려면 true를 설정하고, 그렇지 않으면 false를 설정합니다. 읽기/쓰기 boolean.

**매개변수:**
| 매개변수 | 유형 | 설명 |
| --- | --- | --- |
| value | boolean |  |