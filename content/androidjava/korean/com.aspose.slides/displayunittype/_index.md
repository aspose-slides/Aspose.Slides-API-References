---
title: DisplayUnitType
second_title: Java API 레퍼런스를 통한 Android용 Aspose.Slides
description: 표시된 데이터의 배수를 결정합니다.
type: docs
url: /ko/com.aspose.slides/displayunittype/
---
**상속:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class DisplayUnitType extends System.Enum
```

표시된 데이터의 배수를 결정합니다.
## 필드

| 필드 | 설명 |
| --- | --- |
| [None](#None) | 값이 있는 그대로 표시됩니다. |
| [Hundreds](#Hundreds) | 차트의 값이 100으로 나누어짐을 지정합니다. |
| [Thousands](#Thousands) | 차트의 값이 1,000으로 나누어짐을 지정합니다. |
| [TenThousands](#TenThousands) | 차트의 값이 10,000으로 나누어짐을 지정합니다. |
| [HundredThousands](#HundredThousands) | 차트의 값이 100,000으로 나누어짐을 지정합니다. |
| [Millions](#Millions) | 차트의 값이 1,000,000으로 나누어짐을 지정합니다. |
| [TenMillions](#TenMillions) | 차트의 값이 10,000,000으로 나누어짐을 지정합니다. |
| [HundredMillions](#HundredMillions) | 차트의 값이 100,000,000으로 나누어짐을 지정합니다. |
| [Billions](#Billions) | 차트의 값이 1,000,000,000으로 나누어짐을 지정합니다. |
| [Trillions](#Trillions) | 차트의 값이 1,000,000,000,000으로 나누어짐을 지정합니다. |
| [CustomValue](#CustomValue) | 차트의 값이 사용자 지정 값으로 나누어짐을 지정합니다. |
### None {#None}
```
public static final int None
```


값이 있는 그대로 표시됩니다.

### Hundreds {#Hundreds}
```
public static final int Hundreds
```


차트의 값이 100으로 나누어짐을 지정합니다.

### Thousands {#Thousands}
```
public static final int Thousands
```


차트의 값이 1,000으로 나누어짐을 지정합니다.

### TenThousands {#TenThousands}
```
public static final int TenThousands
```


차트의 값이 10,000으로 나누어짐을 지정합니다.

### HundredThousands {#HundredThousands}
```
public static final int HundredThousands
```


차트의 값이 100,000으로 나누어짐을 지정합니다.

### Millions {#Millions}
```
public static final int Millions
```


차트의 값이 1,000,000으로 나누어짐을 지정합니다.

### TenMillions {#TenMillions}
```
public static final int TenMillions
```


차트의 값이 10,000,000으로 나누어짐을 지정합니다.

### HundredMillions {#HundredMillions}
```
public static final int HundredMillions
```


차트의 값이 100,000,000으로 나누어짐을 지정합니다.

### Billions {#Billions}
```
public static final int Billings
```


차트의 값이 1,000,000,000으로 나누어짐을 지정합니다.

### Trillions {#Trillions}
```
public static final int Trillions
```


차트의 값이 1,000,000,000,000으로 나누어짐을 지정합니다.

### CustomValue {#CustomValue}
```
public static final int CustomValue
```


차트의 값이 사용자 지정 값으로 나누어짐을 지정합니다.