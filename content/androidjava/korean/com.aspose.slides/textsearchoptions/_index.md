---
title: TextSearchOptions
second_title: Aspose.Slides for Android Java API 레퍼런스
description: Presentation Slide 또는 TextFrame에서 텍스트를 검색하는 데 사용할 수 있는 옵션을 나타냅니다.
type: docs
url: /ko/com.aspose.slides/textsearchoptions/
---
**상속:**  
java.lang.Object

**구현된 모든 인터페이스:**  
[com.aspose.slides.ITextSearchOptions](../../com.aspose.slides/itextsearchoptions)  
```
public class TextSearchOptions implements ITextSearchOptions
```

Presentation, Slide 또는 TextFrame에서 텍스트를 검색하는 데 사용할 수 있는 옵션을 나타냅니다.

## 생성자

| 생성자 | 설명 |
| --- | --- |
| [TextSearchOptions()](#TextSearchOptions--) | 새 기본 텍스트 검색 옵션을 생성합니다. |

## 메서드

| 메서드 | 설명 |
| --- | --- |
| [getCaseSensitive()](#getCaseSensitive--) | 대소문자를 구분하는 검색을 사용하려면 true로 설정하고, 그렇지 않으면 false로 설정합니다. |
| [setCaseSensitive(boolean value)](#setCaseSensitive-boolean-) | 대소문자를 구분하는 검색을 사용하려면 true로 설정하고, 그렇지 않으면 false로 설정합니다. |
| [getWholeWordsOnly()](#getWholeWordsOnly--) | 전체 단어만 일치하도록 하려면 true로 설정하고, 그렇지 않으면 false로 설정합니다. |
| [setWholeWordsOnly(boolean value)](#setWholeWordsOnly-boolean-) | 전체 단어만 일치하도록 하려면 true로 설정하고, 그렇지 않으면 false로 설정합니다. |
| [getIncludeNotes()](#getIncludeNotes--) | 텍스트 검색, 교체 또는 강조 작업을 수행할 때 슬라이드 노트에 포함된 텍스트([NotesSlide](../../com.aspose.slides/notesslide))를 포함하려면 true로 설정합니다. |
| [setIncludeNotes(boolean value)](#setIncludeNotes-boolean-) | 텍스트 검색, 교체 또는 강조 작업을 수행할 때 슬라이드 노트에 포함된 텍스트([NotesSlide](../../com.aspose.slides/notesslide))를 포함하려면 true로 설정합니다. |

### TextSearchOptions() {#TextSearchOptions--}
```
public TextSearchOptions()
```

새 기본 텍스트 검색 옵션을 생성합니다.

### getCaseSensitive() {#getCaseSensitive--}
```
public final boolean getCaseSensitive()
```

대소문자를 구분하는 검색을 사용하려면 true로 설정하고, 그렇지 않으면 false로 설정합니다. 읽기/쓰기  boolean .

**반환값:**  
boolean

### setCaseSensitive(boolean value) {#setCaseSensitive-boolean-}
```
public final void setCaseSensitive(boolean value)
```

대소문자를 구분하는 검색을 사용하려면 true로 설정하고, 그렇지 않으면 false로 설정합니다. 읽기/쓰기  boolean .

**매개변수:**  
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | boolean |  |

### getWholeWordsOnly() {#getWholeWordsOnly--}
```
public final boolean getWholeWordsOnly()
```

전체 단어만 일치하도록 하려면 true로 설정하고, 그렇지 않으면 false로 설정합니다. 읽기/쓰기  boolean .

**반환값:**  
boolean

### setWholeWordsOnly(boolean value) {#setWholeWordsOnly-boolean-}
```
public final void setWholeWordsOnly(boolean value)
```

전체 단어만 일치하도록 하려면 true로 설정하고, 그렇지 않으면 false로 설정합니다. 읽기/쓰기  boolean .

**매개변수:**  
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | boolean |  |

### getIncludeNotes() {#getIncludeNotes--}
```
public final boolean getIncludeNotes()
```

텍스트 검색, 교체 또는 강조 작업을 수행할 때 슬라이드 노트에 포함된 텍스트([NotesSlide](../../com.aspose.slides/notesslide))를 포함하려면 true로 설정합니다. 기본값은 false입니다.

**반환값:**  
boolean

### setIncludeNotes(boolean value) {#setIncludeNotes-boolean-}
```
public final void setIncludeNotes(boolean value)
```

텍스트 검색, 교체 또는 강조 작업을 수행할 때 슬라이드 노트에 포함된 텍스트([NotesSlide](../../com.aspose.slides/notesslide))를 포함하려면 true로 설정합니다. 기본값은 false입니다.

**매개변수:**  
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | boolean |  |