---
title: PictureFrameLock
second_title: Java API 참조를 통한 Android용 Aspose.Slides
description: 부모 PictureFrame에서 비활성화된 작업이 무엇인지 결정합니다.
type: docs
url: /ko/com.aspose.slides/pictureframelock/
---
**상속:**
java.lang.Object, [com.aspose.slides.BaseShapeLock](../../com.aspose.slides/baseshapelock)

**구현된 모든 인터페이스:**
[com.aspose.slides.IPictureFrameLock](../../com.aspose.slides/ipictureframelock)
```
public class PictureFrameLock extends BaseShapeLock implements IPictureFrameLock
```

부모 PictureFrame에서 비활성화된 작업을 결정합니다.
## 메서드

| 메서드 | 설명 |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | 이 모양을 그룹에 추가하는 것이 금지되었는지 여부를 결정합니다. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | 이 모양을 그룹에 추가하는 것이 금지되었는지 여부를 결정합니다. |
| [getSelectLocked()](#getSelectLocked--) | 이 모양을 선택하는 것이 금지되었는지 여부를 결정합니다. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | 이 모양을 선택하는 것이 금지되었는지 여부를 결정합니다. |
| [getRotationLocked()](#getRotationLocked--) | 이 모양의 회전 각을 변경하는 것이 금지되었는지 여부를 결정합니다. |
| [setRotationLocked(boolean value)](#setRotationLocked-boolean-) | 이 모양의 회전 각을 변경하는 것이 금지되었는지 여부를 결정합니다. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | 크기 조정 시 모양이 종횡비를 유지해야 하는지 여부를 결정합니다. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | 크기 조정 시 모양이 종횡비를 유지해야 하는지 여부를 결정합니다. |
| [getPositionLocked()](#getPositionLocked--) | 이 모양을 이동하는 것이 금지되었는지 여부를 결정합니다. |
| [setPositionLocked(boolean value)](#setPositionLocked-boolean-) | 이 모양을 이동하는 것이 금지되었는지 여부를 결정합니다. |
| [getSizeLocked()](#getSizeLocked--) | 이 모양의 크기 조정이 금지되었는지 여부를 결정합니다. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | 이 모양의 크기 조정이 금지되었는지 여부를 결정합니다. |
| [getEditPointsLocked()](#getEditPointsLocked--) | 이 모양의 윤곽을 직접 변경하는 것이 금지되었는지 여부를 결정합니다. |
| [setEditPointsLocked(boolean value)](#setEditPointsLocked-boolean-) | 이 모양의 윤곽을 직접 변경하는 것이 금지되었는지 여부를 결정합니다. |
| [getAdjustHandlesLocked()](#getAdjustHandlesLocked--) | 조정값을 변경하는 것이 금지되었는지 여부를 결정합니다. |
| [setAdjustHandlesLocked(boolean value)](#setAdjustHandlesLocked-boolean-) | 조정값을 변경하는 것이 금지되었는지 여부를 결정합니다. |
| [getArrowheadsLocked()](#getArrowheadsLocked--) | 화살표 머리를 변경하는 것이 금지되었는지 여부를 결정합니다. |
| [setArrowheadsLocked(boolean value)](#setArrowheadsLocked-boolean-) | 화살표 머리를 변경하는 것이 금지되었는지 여부를 결정합니다. |
| [getShapeTypeLocked()](#getShapeTypeLocked--) | 모양 유형을 변경하는 것이 금지되었는지 여부를 결정합니다. |
| [setShapeTypeLocked(boolean value)](#setShapeTypeLocked-boolean-) | 모양 유형을 변경하는 것이 금지되었는지 여부를 결정합니다. |
| [getCropLocked()](#getCropLocked--) | 이미지 자르기가 금지되었는지 여부를 결정합니다. |
| [setCropLocked(boolean value)](#setCropLocked-boolean-) | 이미지 자르기가 금지되었는지 여부를 결정합니다. |
### getGroupingLocked() {#getGroupingLocked--}
```
public boolean getGroupingLocked()
```


이 모양을 그룹에 추가하는 것이 금지되었는지 여부를 결정합니다. 읽기/쓰기 boolean.

**반환:**
boolean
### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public void setGroupingLocked(boolean value)
```


이 모양을 그룹에 추가하는 것이 금지되었는지 여부를 결정합니다. 읽기/쓰기 boolean.

**매개변수:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | boolean |  |

### getSelectLocked() {#getSelectLocked--}
```
public boolean getSelectLocked()
```


이 모양을 선택하는 것이 금지되었는지 여부를 결정합니다. 읽기/쓰기 boolean.

**반환:**
boolean
### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public void setSelectLocked(boolean value)
```


이 모양을 선택하는 것이 금지되었는지 여부를 결정합니다. 읽기/쓰기 boolean.

**매개변수:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | boolean |  |

### getRotationLocked() {#getRotationLocked--}
```
public boolean getRotationLocked()
```


이 모양의 회전 각을 변경하는 것이 금지되었는지 여부를 결정합니다. 읽기/쓰기 boolean.

**반환:**
boolean
### setRotationLocked(boolean value) {#setRotationLocked-boolean-}
```
public void setRotationLocked(boolean value)
```


이 모양의 회전 각을 변경하는 것이 금지되었는지 여부를 결정합니다. 읽기/쓰기 boolean.

**매개변수:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | boolean |  |

### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public boolean getAspectRatioLocked()
```


크기 조정 시 모양이 종횡비를 유지해야 하는지 여부를 결정합니다. 읽기/쓰기 boolean.

**반환:**
boolean
### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public void setAspectRatioLocked(boolean value)
```


크기 조정 시 모양이 종횡비를 유지해야 하는지 여부를 결정합니다. 읽기/쓰기 boolean.

**매개변수:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | boolean |  |

### getPositionLocked() {#getPositionLocked--}
```
public boolean getPositionLocked()
```


이 모양을 이동하는 것이 금지되었는지 여부를 결정합니다. 읽기/쓰기 boolean.

**반환:**
boolean
### setPositionLocked(boolean value) {#setPositionLocked-boolean-}
```
public void setPositionLocked(boolean value)
```


이 모양을 이동하는 것이 금지되었는지 여부를 결정합니다. 읽기/쓰기 boolean.

**매개변수:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | boolean |  |

### getSizeLocked() {#getSizeLocked--}
```
public boolean getSizeLocked()
```


이 모양의 크기 조정이 금지되었는지 여부를 결정합니다. 읽기/쓰기 boolean.

**반환:**
boolean
### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public void setSizeLocked(boolean value)
```


이 모양의 크기 조정이 금지되었는지 여부를 결정합니다. 읽기/쓰기 boolean.

**매개변수:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | boolean |  |

### getEditPointsLocked() {#getEditPointsLocked--}
```
public boolean getEditPointsLocked()
```


이 모양의 윤곽을 직접 변경하는 것이 금지되었는지 여부를 결정합니다. 읽기/쓰기 boolean.

**반환:**
boolean
### setEditPointsLocked(boolean value) {#setEditPointsLocked-boolean-}
```
public void setEditPointsLocked(boolean value)
```


이 모양의 윤곽을 직접 변경하는 것이 금지되었는지 여부를 결정합니다. 읽기/쓰기 boolean.

**매개변수:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | boolean |  |

### getAdjustHandlesLocked() {#getAdjustHandlesLocked--}
```
public boolean getAdjustHandlesLocked()
```


조정값을 변경하는 것이 금지되었는지 여부를 결정합니다. 읽기/쓰기 boolean.

**반환:**
boolean
### setAdjustHandlesLocked(boolean value) {#setAdjustHandlesLocked-boolean-}
```
public void setAdjustHandlesLocked(boolean value)
```


조정값을 변경하는 것이 금지되었는지 여부를 결정합니다. 읽기/쓰기 boolean.

**매개변수:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | boolean |  |

### getArrowheadsLocked() {#getArrowheadsLocked--}
```
public boolean getArrowheadsLocked()
```


화살표 머리를 변경하는 것이 금지되었는지 여부를 결정합니다. 읽기/쓰기 boolean.

**반환:**
boolean
### setArrowheadsLocked(boolean value) {#setArrowheadsLocked-boolean-}
```
public void setArrowheadsLocked(boolean value)
```


화살표 머리를 변경하는 것이 금지되었는지 여부를 결정합니다. 읽기/쓰기 boolean.

**매개변수:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | boolean |  |

### getShapeTypeLocked() {#getShapeTypeLocked--}
```
public boolean getShapeTypeLocked()
```


모양 유형을 변경하는 것이 금지되었는지 여부를 결정합니다. 읽기/쓰기 boolean.

**반환:**
boolean
### setShapeTypeLocked(boolean value) {#setShapeTypeLocked-boolean-}
```
public void setShapeTypeLocked(boolean value)
```


모양 유형을 변경하는 것이 금지되었는지 여부를 결정합니다. 읽기/쓰기 boolean.

**매개변수:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | boolean |  |

### getCropLocked() {#getCropLocked--}
```
public boolean getCropLocked()
```


이미지 자르기가 금지되었는지 여부를 결정합니다. 읽기/쓰기 boolean.

**반환:**
boolean
### setCropLocked(boolean value) {#setCropLocked-boolean-}
```
public void setCropLocked(boolean value)
```


이미지 자르기가 금지되었는지 여부를 결정합니다. 읽기/쓰기 boolean.

**매개변수:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | boolean |  |