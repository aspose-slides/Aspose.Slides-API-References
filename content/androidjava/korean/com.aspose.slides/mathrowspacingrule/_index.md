---
title: MathRowSpacingRule
second_title: Java API 레퍼런스를 통한 Android용 Aspose.Slides
description: 행렬 또는 배열에서 열 사이의 수직 간격 유형
type: docs
url: /ko/com.aspose.slides/mathrowspacingrule/
---
**상속:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class MathRowSpacingRule extends System.Enum
```

행렬 또는 배열에서 열 사이의 수직 간격 유형
## 필드

| 필드 | 설명 |
| --- | --- |
| [SingleLineGap](#SingleLineGap) | 단일 행 간격 |
| [OneAndAHalfLineGap](#OneAndAHalfLineGap) | 한 줄 반 간격 |
| [TwoLineGap](#TwoLineGap) | 두 줄 간격 |
| [Exactly](#Exactly) | 정확히 (RowSpacing 속성값에 따라, 포인트 단위 측정) |
| [Multiple](#Multiple) | 다중 (RowSpacing 속성값에 따라, 라인 단위 측정) |
### SingleLineGap {#SingleLineGap}
```
public static final int SingleLineGap
```

단일 행 간격

### OneAndAHalfLineGap {#OneAndAHalfLineGap}
```
public static final int OneAndAHalfLineGap
```

한 줄 반 간격

### TwoLineGap {#TwoLineGap}
```
public static final int TwoLineGap
```

두 줄 간격

### Exactly {#Exactly}
```
public static final int Exactly
```

정확히 (RowSpacing 속성값에 따라, 포인트 단위 측정)

### Multiple {#Multiple}
```
public static final int Multiple
```

다중 (RowSpacing 속성값에 따라, 라인 단위 측정)