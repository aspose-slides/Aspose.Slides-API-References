---
title: IMarker
second_title: Aspose.Slides per Android via Riferimento API Java
description: Rappresenta il marcatore di un grafico.
type: docs
url: /it/com.aspose.slides/imarker/
---```
public interface IMarker
```

Rappresenta il marcatore di un grafico.
## Metodi

| Metodo | Descrizione |
| --- | --- |
| [getSymbol()](#getSymbol--) | Rappresenta lo stile del marcatore in un grafico a linee, a dispersione o a radar. |
| [setSymbol(int value)](#setSymbol-int-) | Rappresenta lo stile del marcatore in un grafico a linee, a dispersione o a radar. |
| [getFormat()](#getFormat--) | Ottiene il riempimento del marcatore. |
| [getSize()](#getSize--) | Rappresenta la dimensione del marcatore in un grafico a linee, a dispersione o a radar. |
| [setSize(int value)](#setSize-int-) | Rappresenta la dimensione del marcatore in un grafico a linee, a dispersione o a radar. |
### getSymbol() {#getSymbol--}
```
public abstract int getSymbol()
```

Rappresenta lo stile del marcatore in un grafico a linee, a dispersione o a radar. Lettura/Scrittura [MarkerStyleType](../../com.aspose.slides/markerstyletype).

**Restituisce:**
int
### setSymbol(int value) {#setSymbol-int-}
```
public abstract void setSymbol(int value)
```

Rappresenta lo stile del marcatore in un grafico a linee, a dispersione o a radar. Lettura/Scrittura [MarkerStyleType](../../com.aspose.slides/markerstyletype).

**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | int |  |

### getFormat() {#getFormat--}
```
public abstract IFormat getFormat()
```

Ottiene il riempimento del marcatore. Solo lettura [IFormat](../../com.aspose.slides/iformat).

**Restituisce:**
[IFormat](../../com.aspose.slides/iformat)
### getSize() {#getSize--}
```
public abstract int getSize()
```

Rappresenta la dimensione del marcatore in un grafico a linee, a dispersione o a radar. Lettura/Scrittura int.

**Restituisce:**
int
### setSize(int value) {#setSize-int-}
```
public abstract void setSize(int value)
```

Rappresenta la dimensione del marcatore in un grafico a linee, a dispersione o a radar. Lettura/Scrittura int.

**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | int |  |