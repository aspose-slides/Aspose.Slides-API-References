---
title: ScaleEffect
second_title: Riferimento API Java per Aspose.Slides per Android
description: Rappresenta l'effetto di scala dell'animazione.
type: docs
url: /it/com.aspose.slides/scaleeffect/
---
**Ereditarietà:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**Tutte le interfacce implementate:**
[com.aspose.slides.IScaleEffect](../../com.aspose.slides/iscaleeffect)
```
public class ScaleEffect extends Behavior implements IScaleEffect
```

Rappresenta l'effetto di scala dell'animazione.
## Costruttori

| Costruttore | Descrizione |
| --- | --- |
| [ScaleEffect()](#ScaleEffect--) |  |
## Metodi

| Metodo | Descrizione |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Determina se un contenuto dovrebbe essere ingrandito. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Determina se un contenuto dovrebbe essere ingrandito. |
| [getFrom()](#getFrom--) | Specifica una coordinata x/y da cui avviare l'animazione (in percentuale). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | Specifica una coordinata x/y da cui avviare l'animazione (in percentuale). |
| [getTo()](#getTo--) | Specifica la posizione di destinazione per un effetto di scala dell'animazione (in percentuale). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | Specifica la posizione di destinazione per un effetto di scala dell'animazione (in percentuale). |
| [getBy()](#getBy--) | descrive il valore di offset relativo per l'animazione (in percentuale). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | descrive il valore di offset relativo per l'animazione (in percentuale). |
### ScaleEffect() {#ScaleEffect--}
```
public ScaleEffect()
```


### getZoomContent() {#getZoomContent--}
```
public final byte getZoomContent()
```


Determina se un contenuto dovrebbe essere ingrandito. Lettura/Scrittura [NullableBool](../../com.aspose.slides/nullablebool).

**Restituisce:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public final void setZoomContent(byte value)
```


Determina se un contenuto dovrebbe essere ingrandito. Lettura/Scrittura [NullableBool](../../com.aspose.slides/nullablebool).

**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public final PointF getFrom()
```


Specifică una coordinata x/y da cui avviare l'animazione (in percentuale). Lettura/Scrittura android.graphics.PointF.

**Restituisce:**
android.graphics.PointF
### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public final void setFrom(PointF value)
```


Specifică una coordinata x/y da cui avviare l'animazione (in percentuale). Lettura/Scrittura android.graphics.PointF.

**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public final PointF getTo()
```


Specifică la posizione di destinazione per un effetto di scala dell'animazione (in percentuale). Lettura/Scrittura android.graphics.PointF.

**Restituisce:**
android.graphics.PointF
### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public final void setTo(PointF value)
```


Specifică la posizione di destinazione per un effetto di scala dell'animazione (in percentuale). Lettura/Scrittura android.graphics.PointF.

**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public final PointF getBy()
```


descrive il valore di offset relativo per l'animazione (in percentuale). Lettura/Scrittura android.graphics.PointF.

**Restituisce:**
android.graphics.PointF
### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public final void setBy(PointF value)
```


descrive il valore di offset relativo per l'animazione (in percentuale). Lettura/Scrittura android.graphics.PointF.

**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | android.graphics.PointF |  |