---
title: ILayoutable
second_title: Aspose.Slides per Android tramite Riferimento API Java
description: Specifica la posizione esatta di un elemento del grafico.
type: docs
url: /it/com.aspose.slides/ilayoutable/
---
**Tutte le interfacce implementate:**
[com.aspose.slides.IChartComponent](../../com.aspose.slides/ichartcomponent)
```
public interface ILayoutable extends IChartComponent
```

Specifica la posizione esatta di un elemento del grafico.
## Metodi

| Metodo | Descrizione |
| --- | --- |
| [getX()](#getX--) | Specifica la posizione x (sinistra) dell'elemento del grafico come frazione della larghezza del grafico. |
| [setX(float value)](#setX-float-) | Specifica la posizione x (sinistra) dell'elemento del grafico come frazione della larghezza del grafico. |
| [getY()](#getY--) | Specifica la parte superiore dell'elemento del grafico come frazione dell'altezza del grafico. |
| [setY(float value)](#setY-float-) | Specifica la parte superiore dell'elemento del grafico come frazione dell'altezza del grafico. |
| [getWidth()](#getWidth--) | Specifica la larghezza dell'elemento del grafico come frazione della larghezza del grafico. |
| [setWidth(float value)](#setWidth-float-) | Specifica la larghezza dell'elemento del grafico come frazione della larghezza del grafico. |
| [getHeight()](#getHeight--) | Specifica l'altezza dell'elemento del grafico come frazione dell'altezza del grafico. |
| [setHeight(float value)](#setHeight-float-) | Specifica l'altezza dell'elemento del grafico come frazione dell'altezza del grafico. |
| [getRight()](#getRight--) | Restituisce il lato destro dell'elemento del grafico come frazione della larghezza del grafico. |
| [getBottom()](#getBottom--) | Restituisce la parte superiore dell'elemento del grafico come frazione dell'altezza del grafico. |
### getX() {#getX--}
```
public abstract float getX()
```

Specifica la posizione x (sinistra) dell'elemento del grafico come frazione della larghezza del grafico. Lettura/Scrittura float.

**Restituisce:**
float
### setX(float value) {#setX-float-}
```
public abstract void setX(float value)
```

Specifica la posizione x (sinistra) dell'elemento del grafico come frazione della larghezza del grafico. Lettura/Scrittura float.

**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | float |  |
### getY() {#getY--}
```
public abstract float getY()
```

Specifica la parte superiore dell'elemento del grafico come frazione dell'altezza del grafico. Lettura/Scrittura float.

**Restituisce:**
float
### setY(float value) {#setY-float-}
```
public abstract void setY(float value)
```

Specifica la parte superiore dell'elemento del grafico come frazione dell'altezza del grafico. Lettura/Scrittura float.

**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | float |  |
### getWidth() {#getWidth--}
```
public abstract float getWidth()
```

Specifică (Specifica) la larghezza dell'elemento del grafico come frazione della larghezza del grafico. Lettura/Scrittura float.

**Restituisce:**
float
### setWidth(float value) {#setWidth-float-}
```
public abstract void setWidth(float value)
```

Specifică (Specifica) la larghezza dell'elemento del grafico come frazione della larghezza del grafico. Lettura/Scrittura float.

**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | float |  |
### getHeight() {#getHeight--}
```
public abstract float getHeight()
```

Specifică (Specifica) l'altezza dell'elemento del grafico come frazione dell'altezza del grafico. Lettura/Scrittura float.

**Restituisce:**
float
### setHeight(float value) {#setHeight-float-}
```
public abstract void setHeight(float value)
```

Specifică (Specifica) l'altezza dell'elemento del grafico come frazione dell'altezza del grafico. Lettura/Scrittura float.

**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | float |  |
### getRight() {#getRight--}
```
public abstract float getRight()
```

Restituisce il lato destro dell'elemento del grafico come frazione della larghezza del grafico. Sola lettura float.

**Restituisce:**
float
### getBottom() {#getBottom--}
```
public abstract float getBottom()
```

Restituisce la parte superiore dell'elemento del grafico come frazione dell'altezza del grafico. Sola lettura float.

**Restituisce:**
float