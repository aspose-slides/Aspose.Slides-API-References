---
title: IPictureFillFormatEffectiveData
second_title: Riferimento API Java di Aspose.Slides per Android
description: Oggetto immutabile che contiene le proprietà del riempimento dell'immagine.
type: docs
url: /it/com.aspose.slides/ipicturefillformateffectivedata/
---
**Tutte le interfacce implementate:**
[com.aspose.slides.IFillParamSource](../../com.aspose.slides/ifillparamsource)
```
public interface IPictureFillFormatEffectiveData extends IFillParamSource
```

Oggetto immutabile che contiene le proprietà del riempimento dell'immagine.

--------------------

Questa interfaccia è usata come parte di [IFillFormatEffectiveData](../../com.aspose.slides/ifillformateffectivedata).
## Metodi

| Metodo | Descrizione |
| --- | --- |
| [getDpi()](#getDpi--) | Restituisce i dpi usati per riempire un'immagine. |
| [getPictureFillMode()](#getPictureFillMode--) | Restituisce la modalità di riempimento dell'immagine. |
| [getPicture()](#getPicture--) | Restituisce l'immagine. |
| [getCropLeft()](#getCropLeft--) | Restituisce la percentuale della larghezza reale dell'immagine che viene ritagliata a sinistra dell'immagine. |
| [getCropTop()](#getCropTop--) | Restituisce la percentuale dell'altezza reale dell'immagine che viene ritagliata in alto dell'immagine. |
| [getCropRight()](#getCropRight--) | Restituisce la percentuale della larghezza reale dell'immagine che viene ritagliata a destra dell'immagine. |
| [getCropBottom()](#getCropBottom--) | Restituisce la percentuale dell'altezza reale dell'immagine che viene ritagliata in basso dell'immagine. |
### getDpi() {#getDpi--}
```
public abstract int getDpi()
```

Restituisce i dpi usati per riempire un'immagine. Solo lettura int.

**Restituisce:**  
int
### getPictureFillMode() {#getPictureFillMode--}
```
public abstract int getPictureFillMode()
```

Restituisce la modalità di riempimento dell'immagine. Solo lettura [PictureFillMode](../../com.aspose.slides/picturefillmode).

**Restituisce:**  
int
### getPicture() {#getPicture--}
```
public abstract IPictureEffectiveData getPicture()
```

Restituisce l'immagine. Solo lettura [IPictureEffectiveData](../../com.aspose.slides/ipictureeffectivedata).

**Restituisce:**  
[IPictureEffectiveData](../../com.aspose.slides/ipictureeffectivedata)
### getCropLeft() {#getCropLeft--}
```
public abstract float getCropLeft()
```

Restituisce la percentuale della larghezza reale dell'immagine che viene ritagliata a sinistra dell'immagine. Solo lettura float.

**Restituisce:**  
float
### getCropTop() {#getCropTop--}
```
public abstract float getCropTop()
```

Restituisce la percentuale dell'altezza reale dell'immagine che viene ritagliata in alto dell'immagine. Solo lettura float.

**Restituisce:**  
float
### getCropRight() {#getCropRight--}
```
public abstract float getCropRight()
```

Restituisce la percentuale della larghezza reale dell'immagine che viene ritagliata a destra dell'immagine. Solo lettura float.

**Restituisce:**  
float
### getCropBottom() {#getCropBottom--}
```
public abstract float getCropBottom()
```

Restituisce la percentuale dell'altezza reale dell'immagine che viene ritagliata in basso dell'immagine. Solo lettura float.

**Restituisce:**  
float