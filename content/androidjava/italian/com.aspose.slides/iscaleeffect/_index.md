---
title: IScaleEffect
second_title: Aspose.Slides per Android tramite Riferimento API Java
description: Rappresenta l'effetto di scala dell'animazione.
type: docs
url: /it/com.aspose.slides/iscaleeffect/
---
**Tutte le interfacce implementate:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IScaleEffect extends IBehavior
```

Rappresenta l'effetto di scala dell'animazione.
## Metodi

| Metodo | Descrizione |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Determina se un contenuto deve essere ingrandito. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Determina se un contenuto deve essere ingrandito. |
| [getFrom()](#getFrom--) | Specifica una coordinata x/y da cui avviare l'animazione (in percentuali). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | Specifica una coordinata x/y da cui avviare l'animazione (in percentuali). |
| [getTo()](#getTo--) | Specifica la posizione di destinazione per un effetto di scala dell'animazione (in percentuali). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | Specifica la posizione di destinazione per un effetto di scala dell'animazione (in percentuali). |
| [getBy()](#getBy--) | descrive il valore di offset relativo per l'animazione (in percentuali). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | descrive il valore di offset relativo per l'animazione (in percentuali). |
### getZoomContent() {#getZoomContent--}
```
public abstract byte getZoomContent()
```


Determina se un contenuto deve essere ingrandito. Lettura/Scrittura [NullableBool](../../com.aspose.slides/nullablebool).

**Restituisce:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public abstract void setZoomContent(byte value)
```


Determina se un contenuto deve essere ingrandito. Lettura/Scrittura [NullableBool](../../com.aspose.slides/nullablebool).

**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public abstract PointF getFrom()
```


Specifica una coordinata x/y da cui avviare l'animazione (in percentuali). Lettura/Scrittura android.graphics.PointF.

**Restituisce:**
android.graphics.PointF
### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public abstract void setFrom(PointF value)
```


Specifica una coordinata x/y da cui avviare l'animazione (in percentuali). Lettura/Scrittura android.graphics.PointF.

**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public abstract PointF getTo()
```


Specifica la posizione di destinazione per un effetto di scala dell'animazione (in percentuali). Lettura/Scrittura android.graphics.PointF.

**Restituisce:**
android.graphics.PointF
### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public abstract void setTo(PointF value)
```


Specifica la posizione di destinazione per un effetto di scala dell'animazione (in percentuali). Lettura/Scrittura android.graphics.PointF.

**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public abstract PointF getBy()
```


descrive il valore di offset relativo per l'animazione (in percentuali). Lettura/Scrittura android.graphics.PointF.

**Restituisce:**
android.graphics.PointF
### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public abstract void setBy(PointF value)
```


descrive il valore di offset relativo per l'animazione (in percentuali). Lettura/Scrittura android.graphics.PointF.

**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | android.graphics.PointF |  |