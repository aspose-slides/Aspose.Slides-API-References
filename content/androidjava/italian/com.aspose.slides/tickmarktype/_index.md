---
title: TickMarkType
second_title: Aspose.Slides per Android tramite Riferimento API Java
description: Rappresenta il tipo di segno di spunta per l'asse specificato.
type: docs
url: /it/com.aspose.slides/tickmarktype/
---
**Ereditarietà:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class TickMarkType extends System.Enum
```

Rappresenta il tipo di segno di spunta per l'asse specificato.
## Campi

| Campo | Descrizione |
| --- | --- |
| [Cross](#Cross) | Specifica che i segni di spunta attraversano l'asse. |
| [Inside](#Inside) | Specifica che i segni di spunta sono all'interno dell'area del grafico. |
| [None](#None) | Specifica che non ci sono segni di spunta. |
| [Outside](#Outside) | Specifica che i segni di spunta sono al di fuori dell'area del grafico. |
### Attraversa {#Cross}
```
public static final int Cross
```

Specifică che i segni di spunta attraversano l'asse.

### Interno {#Inside}
```
public static final int Inside
```

Specifică che i segni di spunta sono all'interno dell'area del grafico.

### Nessuno {#None}
```
public static final int None
```

Specifică che non ci sono segni di spunta.

### Esterno {#Outside}
```
public static final int Outside
```

Specifică che i segni di spunta sono al di fuori dell'area del grafico.