---
title: NormalViewRestoredProperties
second_title: Aspose.Slides per Android tramite Riferimento API Java
description: Specifica le dimensioni della regione della diapositiva (larghezza quando è figlio di restoredTop, altezza quando è figlio di restoredLeft) della vista normale, quando la regione ha una dimensione variabile ripristinata (né minimizzata né massimizzata).
type: docs
url: /it/com.aspose.slides/normalviewrestoredproperties/
---
**Eredità:**
java.lang.Object

**Tutte le interfacce implementate:**
[com.aspose.slides.INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)
```
public class NormalViewRestoredProperties implements INormalViewRestoredProperties
```

Specifica le dimensioni della regione della diapositiva ((larghezza quando è figlio di restoredTop, altezza quando è figlio di restoredLeft) della vista normale, quando la regione è di una dimensione variabile ripristinata (né minimizzata né massimizzata)).
## Metodi

| Metodo | Descrizione |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | Specifica la dimensione della regione della diapositiva (larghezza quando è figlio di RestoredTop, altezza quando è figlio di RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | Specifica la dimensione della regione della diapositiva (larghezza quando è figlio di RestoredTop, altezza quando è figlio di RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | Specifica se la dimensione della regione del contenuto laterale deve compensare la nuova dimensione ridimensionando la finestra che contiene la vista nell'applicazione Lettura/scrittura boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | Specifica se la dimensione della regione del contenuto laterale deve compensare la nuova dimensione ridimensionando la finestra che contiene la vista nell'applicazione Lettura/scrittura boolean. |
### getDimensionSize() {#getDimensionSize--}
```
public final float getDimensionSize()
```

Specifică la dimensione della regione della diapositiva (larghezza quando è figlio di RestoredTop, altezza quando è figlio di RestoredLeft). Lettura/scrittura float.

**Restituisce:**
float
### setDimensionSize(float value) {#setDimensionSize-float-}
```
public final void setDimensionSize(float value)
```

Specifică la dimensione della regione della diapositiva (larghezza quando è figlio di RestoredTop, altezza quando è figlio di RestoredLeft). Lettura/scrittura float.

**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public final boolean getAutoAdjust()
```

Specifică se la dimensione della regione del contenuto laterale deve compensare la nuova dimensione ridimensionando la finestra che contiene la vista nell'applicazione Lettura/scrittura boolean.

**Restituisce:**
boolean
### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public final void setAutoAdjust(boolean value)
```

Specifică se la dimensione della regione del contenuto laterale deve compensare la nuova dimensione ridimensionando la finestra che contiene la vista nell'applicazione Lettura/scrittura boolean.

**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | boolean |  |