---
title: ChartPlotArea
second_title: Riferimento API Java di Aspose.Slides per Android
description: Rappresenta il rettangolo in cui deve essere tracciato il grafico.
type: docs
url: /it/com.aspose.slides/chartplotarea/
---
**Ereditarietà:**
java.lang.Object, com.aspose.slides.DomObject

**Tutte le interfacce implementate:**
[com.aspose.slides.IChartPlotArea](../../com.aspose.slides/ichartplotarea)
```
public class ChartPlotArea extends DomObject<Chart> implements IChartPlotArea
```

Rappresenta il rettangolo in cui deve essere tracciato il grafico.
## Metodi

| Metodo | Descrizione |
| --- | --- |
| [getFormat()](#getFormat--) | Restituisce il formato di un'area del grafico. |
| [getX()](#getX--) | Restituisce o imposta la coordinata x dell'angolo in alto a sinistra del riquadro dell'area del grafico come frazione della larghezza del grafico (da 0 a 1). |
| [setX(float value)](#setX-float-) | Restituisce o imposta la coordinata x dell'angolo in alto a sinistra del riquadro dell'area del grafico come frazione della larghezza del grafico (da 0 a 1). |
| [getY()](#getY--) | Restituisce o imposta la coordinata y dell'angolo in alto a sinistra del riquadro dell'area del grafico come frazione dell'altezza del grafico (da 0 a 1). |
| [setY(float value)](#setY-float-) | Restituisce o imposta la coordinata y dell'angolo in alto a sinistra del riquadro dell'area del grafico come frazione dell'altezza del grafico (da 0 a 1). |
| [getWidth()](#getWidth--) | Restituisce o imposta la larghezza del riquadro dell'area del grafico come frazione della larghezza del grafico (da 0 a 1). |
| [setWidth(float value)](#setWidth-float-) | Restituisce o imposta la larghezza del riquadro dell'area del grafico come frazione della larghezza del grafico (da 0 a 1). |
| [getHeight()](#getHeight--) | Restituisce o imposta l'altezza del riquadro dell'area del grafico come frazione dell'altezza del grafico (da 0 a 1). |
| [setHeight(float value)](#setHeight-float-) | Restituisce o imposta l'altezza del riquadro dell'area del grafico come frazione dell'altezza del grafico (da 0 a 1). |
| [getRight()](#getRight--) | Destra. |
| [getBottom()](#getBottom--) | Inferiore. |
| [getChart()](#getChart--) | Grafico. |
| [isLocationAutocalculated()](#isLocationAutocalculated--) | Definisce come la posizione dovrebbe essere calcolata: true \u2013 calcolata automaticamente; definita dalle proprietà X, Y, Width, Height. |
| [getLayoutTargetType()](#getLayoutTargetType--) | Se il layout dell'area del grafico è definito manualmente, questa proprietà specifica se il layout deve avvenire interno (escludendo assi e etichette) o esterno (includendo assi e etichette). |
| [setLayoutTargetType(int value)](#setLayoutTargetType-int-) | Se il layout dell'area del grafico è definito manualmente, questa proprietà specifica se il layout deve avvenire interno (escludendo assi e etichette) o esterno (includendo assi e etichette). |
| [getActualX()](#getActualX--) | Specifica la posizione x effettiva (sinistra) dell'elemento del grafico rispetto all'angolo in alto a sinistra del grafico. |
| [getActualY()](#getActualY--) | Specifica la parte superiore effettiva dell'elemento del grafico rispetto all'angolo in alto a sinistra del grafico. |
| [getActualWidth()](#getActualWidth--) | Specifica la larghezza effettiva dell'elemento del grafico. |
| [getActualHeight()](#getActualHeight--) | Specifica l'altezza effettiva dell'elemento del grafico. |
| [getSlide()](#getSlide--) | Restituisce la diapositiva genitore di un FillFormat. |
| [getPresentation()](#getPresentation--) | Restituisce la presentazione genitore di un FillFormat. |
### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

Restituisce il formato di un'area del grafico. Solo lettura [IFormat](../../com.aspose.slides/iformat).

**Restituisce:**
[IFormat](../../com.aspose.slides/iformat)
### getX() {#getX--}
```
public final float getX()
```

Restituisce o imposta la coordinata x dell'angolo in alto a sinistra del riquadro dell'area del grafico come frazione della larghezza del grafico (da 0 a 1). Lettura/Scrittura float.

**Restituisce:**
float
### setX(float value) {#setX-float-}
```
public final void setX(float value)
```

Restituisce o imposta la coordinata x dell'angolo in alto a sinistra del riquadro dell'area del grafico come frazione della larghezza del grafico (da 0 a 1). Lettura/Scrittura float.

**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | float |  |
### getY() {#getY--}
```
public final float getY()
```

Restituisce o imposta la coordinata y dell'angolo in alto a sinistra del riquadro dell'area del grafico come frazione dell'altezza del grafico (da 0 a 1). Lettura/Scrittura float.

**Restituisce:**
float
### setY(float value) {#setY-float-}
```
public final void setY(float value)
```

Restituisce o imposta la coordinata y dell'angolo in alto a sinistra del riquadro dell'area del grafico come frazione dell'altezza del grafico (da 0 a 1). Lettura/Scrittura float.

**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | float |  |
### getWidth() {#getWidth--}
```
public final float getWidth()
```

Restituisce o imposta la larghezza del riquadro dell'area del grafico come frazione della larghezza del grafico (da 0 a 1). Lettura/Scrittura float.

**Restituisce:**
float
### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```

Restituisce o imposta la larghezza del riquadro dell'area del grafico come frazione della larghezza del grafico (da 0 a 1). Lettura/Scrittura float.

**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | float |  |
### getHeight() {#getHeight--}
```
public final float getHeight()
```

Restituisce o imposta l'altezza del riquadro dell'area del grafico come frazione dell'altezza del grafico (da 0 a 1). Lettura/Scrittura float.

**Restituisce:**
float
### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```

Restituisce o imposta l'altezza del riquadro dell'area del grafico come frazione dell'altezza del grafico (da 0 a 1). Lettura/Scrittura float.

**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | float |  |
### getRight() {#getRight--}
```
public final float getRight()
```

Destra. Solo lettura float.

**Restituisce:**
float
### getBottom() {#getBottom--}
```
public final float getBottom()
```

Inferiore. Solo lettura float.

**Restituisce:**
float
### getChart() {#getChart--}
```
public final IChart getChart()
```

Grafico. Solo lettura [IChart](../../com.aspose.slides/ichart).

**Restituisce:**
[IChart](../../com.aspose.slides/ichart)
### isLocationAutocalculated() {#isLocationAutocalculated--}
```
public final boolean isLocationAutocalculated()
```

Definisce come la posizione dovrebbe essere calcolata: true \u2013 calcolata automaticamente; definita dalle proprietà X, Y, Width, Height. Booleano solo lettura.

**Restituisce:**
boolean
### getLayoutTargetType() {#getLayoutTargetType--}
```
public final int getLayoutTargetType()
```

Se il layout dell'area del grafico è definito manualmente, questa proprietà specifica se il layout deve avvenire interno (escludendo assi e etichette) o esterno (includendo assi e etichette). Lettura/Scrittura [LayoutTargetType](../../com.aspose.slides/layouttargettype)(\#getLayoutTargetType.getLayoutTargetType/\#setLayoutTargetType(int).setLayoutTargetType(int)).

--------------------

> ```
> Presentation presentation = new Presentation();
>   try
>   {
>       ISlide slide = presentation.getSlides().get_Item(0);
>       IChart chart = slide.getShapes().addChart(ChartType.ClusteredColumn, 20, 100, 600, 400);
>       chart.getPlotArea().setX(0.2f);
>       chart.getPlotArea().setY(0.2f);
>       chart.getPlotArea().setWidth(0.7f);
>       chart.getPlotArea().setHeight(0.7f);
>       chart.getPlotArea().setLayoutTargetType(LayoutTargetType.Inner);
>       ...
>   } finally {
>       if (presentation != null) presentation.dispose();
>   }
> ```


**Restituisce:**
int
### setLayoutTargetType(int value) {#setLayoutTargetType-int-}
```
public final void setLayoutTargetType(int value)
```

Se il layout dell'area del grafico è definito manualmente, questa proprietà specifica se il layout deve avvenire interno (escludendo assi e etichette) o esterno (includendo assi e etichette). Lettura/Scrittura [LayoutTargetType](../../com.aspose.slides/layouttargettype)(\#getLayoutTargetType.getLayoutTargetType/\#setLayoutTargetType(int).setLayoutTargetType(int)).

--------------------

> ```
> Presentation presentation = new Presentation();
>   try
>   {
>       ISlide slide = presentation.getSlides().get_Item(0);
>       IChart chart = slide.getShapes().addChart(ChartType.ClusteredColumn, 20, 100, 600, 400);
>       chart.getPlotArea().setX(0.2f);
>       chart.getPlotArea().setY(0.2f);
>       chart.getPlotArea().setWidth(0.7f);
>       chart.getPlotArea().setHeight(0.7f);
>       chart.getPlotArea().setLayoutTargetType(LayoutTargetType.Inner);
>       ...
>   } finally {
>       if (presentation != null) presentation.dispose();
>   }
> ```


**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | int |  |
### getActualX() {#getActualX--}
```
public final float getActualX()
```

Specifică la locație reală x (stânga) a elementului grafic în raport cu colțul din stânga sus al graficului. Chiamare il metodo IChart.ValidateChartLayout() prima per ottenere i valori effettivi. Float di sola lettura.

**Restituisce:**
float
### getActualY() {#getActualY--}
```
public final float getActualY()
```

Specifică partea superioară reală a elementului grafic în raport cu colțul din stânga sus al graficului. Chiamare il metodo IChart.ValidateChartLayout() prima per ottenere i valori effettivi. Float di sola lettura.

**Restituisce:**
float
### getActualWidth() {#getActualWidth--}
```
public final float getActualWidth()
```

Specifică lărgimea reală a elementului grafic. Chiamare il metodo IChart.ValidateChartLayout() prima per ottenere i valori effettivi. Float di sola lettura.

**Restituisce:**
float
### getActualHeight() {#getActualHeight--}
```
public final float getActualHeight()
```

Specifică înălțimea reală a elementului grafic. Chiamare il metodo IChart.ValidateChartLayout() prima per ottenere i valori effettivi. Float di sola lettura.

**Restituisce:**
float
### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

Restituisce la diapositiva genitore di un FillFormat. Solo lettura [BaseSlide](../../com.aspose.slides/baseslide).

**Restituisce:**
[IBaseSlide](../../com.aspose.slides/ibaseslide)
### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

Restituisce la presentazione genitore di un FillFormat. Solo lettura [IPresentation](../../com.aspose.slides/ipresentation).

**Restituisce:**
[IPresentation](../../com.aspose.slides/ipresentation)