---
title: ISetEffect
second_title: Aspose.Slides لنظام Android عبر مرجع API جافا
description: يمثّل تأثيرًا مجموعة لسلوك الرسوم المتحركة.
type: docs
url: /ar/com.aspose.slides/iseteffect/
---
**جميع الواجهات المُطبقة:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface ISetEffect extends IBehavior
```

يمثل تأثيرًا مجموعة لسلوك الرسوم المتحركة.
## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getTo()](#getTo--) | يحدد السمة المحددة لتأثير بعد تأثير الرسوم المتحركة. |
| [setTo(Object value)](#setTo-java.lang.Object-) | يحدد السمة المحددة لتأثير بعد تأثير الرسوم المتحركة. |
### getTo() {#getTo--}
```
public abstract Object getTo()
```

يحدد السمة المحددة لتأثير بعد تأثير الرسوم المتحركة. يمثل قيمة نقطية. القيم المسموح بها: bool, ColorFormat, float, int, string. قراءة/كتابة Object.

**الإرجاع:**
java.lang.Object
### setTo(Object value) {#setTo-java.lang.Object-}
```
public abstract void setTo(Object value)
```

يحدد السمة المحددة لتأثير بعد تأثير الرسوم المتحركة. يمثل قيمة نقطية. القيم المسموح بها: bool, ColorFormat, float, int, string. قراءة/كتابة Object.

**المعلمات:**
| المعلمة | النوع | الوصف |
| --- | --- | --- |
| value | java.lang.Object |  |