---
title: IMotionCmdPath
second_title: Aspose.Slides for Android via Java API Reference
description: Represent one command of a path.
type: docs
url: /ar/com.aspose.slides/imotioncmdpath/
---```
public interface IMotionCmdPath
```

يمثل أمرًا واحدًا في مسار.
## الطرق

| Method | Description |
| --- | --- |
| [getPoints()](#getPoints--) | يحدد نقاط الأمر. |
| [setPoints(PointF[] value)](#setPoints-android.graphics.PointF---) | يحدد نقاط الأمر. |
| [getCommandType()](#getCommandType--) | يحدد نوع الأمر. |
| [setCommandType(int value)](#setCommandType-int-) | يحدد نوع الأمر. |
| [isRelative()](#isRelative--) | يحدد إحداثيات الأمر نسبية أم لا. |
| [setRelative(boolean value)](#setRelative-boolean-) | يحدد إحداثيات الأمر نسبية أم لا. |
| [getPointsType()](#getPointsType--) | يحدد نوع نقاط الأمر قراءة/كتابة [MotionPathPointsType](../../com.aspose.slides/motionpathpointstype). |
| [setPointsType(int value)](#setPointsType-int-) | يحدد نوع نقاط الأمر قراءة/كتابة [MotionPathPointsType](../../com.aspose.slides/motionpathpointstype). |
### getPoints() {#getPoints--}
```
public abstract PointF[] getPoints()
```

يحدد نقاط الأمر. قراءة/كتابة android.graphics.PointF[].

**الإرجاع:**
android.graphics.PointF[]
### setPoints(PointF[] value) {#setPoints-android.graphics.PointF---}
```
public abstract void setPoints(PointF[] value)
```

يحدد نقاط الأمر. قراءة/كتابة android.graphics.PointF[].

**المعاملات:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| value | android.graphics.PointF[] |  |
### getCommandType() {#getCommandType--}
```
public abstract int getCommandType()
```

يحدد نوع الأمر. قراءة/كتابة [MotionCommandPathType](../../com.aspose.slides/motioncommandpathtype).

**الإرجاع:**
int
### setCommandType(int value) {#setCommandType-int-}
```
public abstract void setCommandType(int value)
```

يحدد نوع الأمر. قراءة/كتابة [MotionCommandPathType](../../com.aspose.slides/motioncommandpathtype).

**المعاملات:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| value | int |  |
### isRelative() {#isRelative--}
```
public abstract boolean isRelative()
```

يحدد إحداثيات الأمر نسبية أم لا. قراءة/كتابة boolean.

**الإرجاع:**
boolean
### setRelative(boolean value) {#setRelative-boolean-}
```
public abstract void setRelative(boolean value)
```

يحدد إحداثيات الأمر نسبية أم لا. قراءة/كتابة boolean.

**المعاملات:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| value | boolean |  |
### getPointsType() {#getPointsType--}
```
public abstract int getPointsType()
```

يحدد نوع نقاط الأمر قراءة/كتابة [MotionPathPointsType](../../com.aspose.slides/motionpathpointstype).

**الإرجاع:**
int
### setPointsType(int value) {#setPointsType-int-}
```
public abstract void setPointsType(int value)
```

يحدد نوع نقاط الأمر قراءة/كتابة [MotionPathPointsType](../../com.aspose.slides/motionpathpointstype).

**المعاملات:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| value | int |  |