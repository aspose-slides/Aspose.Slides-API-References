---
title: RotationEffect
second_title: Aspose.Slides لنظام Android عبر مرجع API للـ Java
description: يمثل سلوك دوران التأثير.
type: docs
url: /ar/com.aspose.slides/rotationeffect/
---
**الوراثة:**  
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**جميع الواجهات التي تم تنفيذها:**  
[com.aspose.slides.IRotationEffect](../../com.aspose.slides/irotationeffect)  
```
public class RotationEffect extends Behavior implements IRotationEffect
```

يمثل سلوك دوران التأثير.
## المنشئات

| المنشئ | الوصف |
| --- | --- |
| [RotationEffect()](#RotationEffect--) |  |
## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getFrom()](#getFrom--) | يصف القيمة الابتدائية للرسوم المتحركة. قراءة/كتابة float. |
| [setFrom(float value)](#setFrom-float-) | يصف القيمة الابتدائية للرسوم المتحركة. قراءة/كتابة float. |
| [getTo()](#getTo--) | يصف القيمة النهائية للرسوم المتحركة. قراءة/كتابة float. |
| [setTo(float value)](#setTo-float-) | يصف القيمة النهائية للرسوم المتحركة. قراءة/كتابة float. |
| [getBy()](#getBy--) | يصف قيمة الإزاحة النسبية للرسوم المتحركة. قراءة/كتابة float. |
| [setBy(float value)](#setBy-float-) | يصف قيمة الإزاحة النسبية للرسوم المتحركة. قراءة/كتابة float. |
### RotationEffect() {#RotationEffect--}
```
public RotationEffect()
```

### getFrom() {#getFrom--}
```
public final float getFrom()
```

يصف القيمة الابتدائية للرسوم المتحركة. قراءة/كتابة float.

**القيمة المرجعة:**  
float
### setFrom(float value) {#setFrom-float-}
```
public final void setFrom(float value)
```

يصف القيمة الابتدائية للرسوم المتحركة. قراءة/كتابة float.

**المعلمات:**  
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getTo() {#getTo--}
```
public final float getTo()
```

يصف القيمة النهائية للرسوم المتحركة. قراءة/كتابة float.

**القيمة المرجعة:**  
float
### setTo(float value) {#setTo-float-}
```
public final void setTo(float value)
```

يصف القيمة النهائية للرسوم المتحركة. قراءة/كتابة float.

**المعلمات:**  
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getBy() {#getBy--}
```
public final float getBy()
```

يصف قيمة الإزاحة النسبية للرسوم المتحركة. قراءة/كتابة float.

**القيمة المرجعة:**  
float
### setBy(float value) {#setBy-float-}
```
public final void setBy(float value)
```

يصف قيمة الإزاحة النسبية للرسوم المتحركة. قراءة/كتابة float.

**المعلمات:**  
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |