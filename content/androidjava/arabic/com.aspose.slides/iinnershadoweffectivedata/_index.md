---
title: IInnerShadowEffectiveData
second_title: Aspose.Slides لنظام Android عبر مرجع API لجافا
description: كائن ثابت يمثل تأثير الظل الداخلي.
type: docs
url: /ar/com.aspose.slides/iinnershadoweffectivedata/
---
**جميع الواجهات المُنفذة:**
[com.aspose.slides.IEffectEffectiveData](../../com.aspose.slides/ieffecteffectivedata)
```
public interface IInnerShadowEffectiveData extends IEffectEffectiveData
```

كائن ثابت يمثل تأثير الظل الداخلي.
## طرق

| الطريقة | الوصف |
| --- | --- |
| [getBlurRadius()](#getBlurRadius--) | نصف قطر الضبابية. |
| [getDirection()](#getDirection--) | اتجاه الظل. |
| [getDistance()](#getDistance--) | مسافة الظل. |
| [getShadowColor()](#getShadowColor--) | لون الظل. |
### getBlurRadius() {#getBlurRadius--}
```
public abstract double getBlurRadius()
```

نصف قطر الضبابية. للقراءة فقط double.

**الإرجاع:**
double
### getDirection() {#getDirection--}
```
public abstract float getDirection()
```

اتجاه الظل. للقراءة فقط float.

**الإرجاع:**
float
### getDistance() {#getDistance--}
```
public abstract double getDistance()
```

مسافة الظل. للقراءة فقط double.

**الإرجاع:**
double
### getShadowColor() {#getShadowColor--}
```
public abstract Integer getShadowColor()
```

لون الظل. للقراءة فقط java.lang.Integer.

**الإرجاع:**
java.lang.Integer