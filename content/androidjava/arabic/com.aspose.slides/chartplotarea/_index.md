---
title: ChartPlotArea
second_title: Aspose.Slides لنظام Android عبر مرجع API لجافا
description: يمثل المستطيل حيث يجب رسم المخطط.
type: docs
url: /ar/com.aspose.slides/chartplotarea/
---
**الوراثة:**  
java.lang.Object, com.aspose.slides.DomObject

**جميع الواجهات المنفذة:**  
[com.aspose.slides.IChartPlotArea](../../com.aspose.slides/ichartplotarea)  
```
public class ChartPlotArea extends DomObject<Chart> implements IChartPlotArea
```

يمثل المستطيل حيث يجب رسم المخطط.  
## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getFormat()](#getFormat--) | يعيد تنسيق منطقة الرسم. |
| [getX()](#getX--) | يعيد أو يضبط الإحداثي X للزاوية العلوية اليسرى لمربع حدود منطقة الرسم كنسبة من عرض المخطط (من 0 إلى 1). |
| [setX(float value)](#setX-float-) | يعيد أو يضبط الإحداثي X للزاوية العلوية اليسرى لمربع حدود منطقة الرسم كنسبة من عرض المخطط (من 0 إلى 1). |
| [getY()](#getY--) | يعيد أو يضبط الإحداثي Y للزاوية العلوية اليسرى لمربع حدود منطقة الرسم كنسبة من ارتفاع المخطط (من 0 إلى 1). |
| [setY(float value)](#setY-float-) | يعيد أو يضبط الإحداثي Y للزاوية العلوية اليسرى لمربع حدود منطقة الرسم كنسبة من ارتفاع المخطط (من 0 إلى 1). |
| [getWidth()](#getWidth--) | يعيد أو يضبط عرض مربع حدود منطقة الرسم كنسبة من عرض المخطط (من 0 إلى 1). |
| [setWidth(float value)](#setWidth-float-) | يعيد أو يضبط عرض مربع حدود منطقة الرسم كنسبة من عرض المخطط (من 0 إلى 1). |
| [getHeight()](#getHeight--) | يعيد أو يضبط ارتفاع مربع حدود منطقة الرسم كنسبة من ارتفاع المخطط (من 0 إلى 1). |
| [setHeight(float value)](#setHeight-float-) | يعيد أو يضبط ارتفاع مربع حدود منطقة الرسم كنسبة من ارتفاع المخطط (من 0 إلى 1). |
| [getRight()](#getRight--) | اليمين. |
| [getBottom()](#getBottom--) | الأسفل. |
| [getChart()](#getChart--) | المخطط. |
| [isLocationAutocalculated()](#isLocationAutocalculated--) | يحدد كيفية حساب الموقع: true – يُحسب تلقائيًا؛ يُحدد بواسطة الخصائص X, Y, Width, Height. |
| [getLayoutTargetType()](#getLayoutTargetType--) | إذا تم تعريف تخطيط منطقة الرسم يدويًا فإن هذه الخاصية تحدد ما إذا كان يتم تخطيط المنطقة من الداخل (باستثناء المحور وعناوين المحاور) أو من الخارج (متضمنًا المحور وعناوين المحاور). |
| [setLayoutTargetType(int value)](#setLayoutTargetType-int-) | إذا تم تعريف تخطيط منطقة الرسم يدويًا فإن هذه الخاصية تحدد ما إذا كان يتم تخطيط المنطقة من الداخل (باستثناء المحور وعناوين المحاور) أو من الخارج (متضمنًا المحور وعناوين المحاور). |
| [getActualX()](#getActualX--) | يحدد الموقع الفعلي X (اليسار) لعنصر المخطط بالنسبة للزاوية اليسرى العليا للمخطط. |
| [getActualY()](#getActualY--) | يحدد أعلى العنصر الفعلي للمخطط بالنسبة للزاوية اليسرى العليا للمخطط. |
| [getActualWidth()](#getActualWidth--) | يحدد العرض الفعلي لعنصر المخطط. |
| [getActualHeight()](#getActualHeight--) | يحدد الارتفاع الفعلي لعنصر المخطط. |
| [getSlide()](#getSlide--) | يعيد الشريحة الأصلية لكائن FillFormat. |
| [getPresentation()](#getPresentation--) | يعيد العرض التقديمي الأصلي لكائن FillFormat. |
### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

يعيد تنسيق منطقة الرسم. قراءة فقط [IFormat](../../com.aspose.slides/iformat).

**الإرجاع:**  
[IFormat](../../com.aspose.slides/iformat)
### getX() {#getX--}
```
public final float getX()
```

يعيد أو يضبط الإحداثي X للزاوية العلوية اليسرى لمربع حدود منطقة الرسم كنسبة من عرض المخطط (من 0 إلى 1). قراءة/كتابة float.

**الإرجاع:**  
float
### setX(float value) {#setX-float-}
```
public final void setX(float value)
```

يعيد أو يضبط الإحداثي X للزاوية العلوية اليسرى لمربع حدود منطقة الرسم كنسبة من عرض المخطط (من 0 إلى 1). قراءة/كتابة float.

**المعلمات:**  
| المعلمة | النوع | الوصف |
| --- | --- | --- |
| value | float |  |
### getY() {#getY--}
```
public final float getY()
```

يعيد أو يضبط الإحداثي Y للزاوية العلوية اليسرى لمربع حدود منطقة الرسم كنسبة من ارتفاع المخطط (من 0 إلى 1). قراءة/كتابة float.

**الإرجاع:**  
float
### setY(float value) {#setY-float-}
```
public final void setY(float value)
```

يعيد أو يضبط الإحداثي Y للزاوية العلوية اليسرى لمربع حدود منطقة الرسم كنسبة من ارتفاع المخطط (من 0 إلى 1). قراءة/كتابة float.

**المعلمات:**  
| المعلمة | النوع | الوصف |
| --- | --- | --- |
| value | float |  |
### getWidth() {#getWidth--}
```
public final float getWidth()
```

يعيد أو يضبط عرض مربع حدود منطقة الرسم كنسبة من عرض المخطط (من 0 إلى 1). قراءة/كتابة float.

**الإرجاع:**  
float
### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```

يعيد أو يضبط عرض مربع حدود منطقة الرسم كنسبة من عرض المخطط (من 0 إلى 1). قراءة/كتابة float.

**المعلمات:**  
| المعلمة | النوع | الوصف |
| --- | --- | --- |
| value | float |  |
### getHeight() {#getHeight--}
```
public final float getHeight()
```

يعيد أو يضبط ارتفاع مربع حدود منطقة الرسم كنسبة من ارتفاع المخطط (من 0 إلى 1). قراءة/كتابة float.

**الإرجاع:**  
float
### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```

يعيد أو يضبط ارتفاع مربع حدود منطقة الرسم كنسبة من ارتفاع المخطط (من 0 إلى 1). قراءة/كتابة float.

**المعلمات:**  
| المعلمة | النوع | الوصف |
| --- | --- | --- |
| value | float |  |
### getRight() {#getRight--}
```
public final float getRight()
```

اليمين. قراءة فقط float.

**الإرجاع:**  
float
### getBottom() {#getBottom--}
```
public final float getBottom()
```

الأسفل. قراءة فقط float.

**الإرجاع:**  
float
### getChart() {#getChart--}
```
public final IChart getChart()
```

المخطط. قراءة فقط [IChart](../../com.aspose.slides/ichart).

**الإرجاع:**  
[IChart](../../com.aspose.slides/ichart)
### isLocationAutocalculated() {#isLocationAutocalculated--}
```
public final boolean isLocationAutocalculated()
```

يحدد كيفية حساب الموقع: true – يُحسب تلقائيًا؛ يُحدد بواسطة الخصائص X, Y, Width, Height. قراءة فقط boolean.

**الإرجاع:**  
boolean
### getLayoutTargetType() {#getLayoutTargetType--}
```
public final int getLayoutTargetType()
```

إذا تم تعريف تخطيط منطقة الرسم يدويًا فإن هذه الخاصية تحدد ما إذا كان يتم تخطيط المنطقة من الداخل (باستثناء المحور وعناوين المحاور) أو من الخارج (متضمنًا المحور وعناوين المحاور). قراءة/كتابة [LayoutTargetType](../../com.aspose.slides/layouttargettype)(\#getLayoutTargetType.getLayoutTargetType/\#setLayoutTargetType(int).setLayoutTargetType(int)).

--------------------

> ```
> Presentation presentation = new Presentation();
>   try
>   {
>       ISlide slide = presentation.getSlides().get_Item(0);
>       IChart chart = slide.getShapes().addChart(ChartType.ClusteredColumn, 20, 100, 600, 400);
>       chart.getPlotArea().setX(0.2f);
>       chart.getPlotArea().setY(0.2f);
>       chart.getPlotArea().setWidth(0.7f);
>       chart.getPlotArea().setHeight(0.7f);
>       chart.getPlotArea().setLayoutTargetType(LayoutTargetType.Inner);
>       ...
>   } finally {
>       if (presentation != null) presentation.dispose();
>   }
> ```

**الإرجاع:**  
int
### setLayoutTargetType(int value) {#setLayoutTargetType-int-}
```
public final void setLayoutTargetType(int value)
```

إذا تم تعريف تخطيط منطقة الرسم يدويًا فإن هذه الخاصية تحدد ما إذا كان يتم تخطيط المنطقة من الداخل (باستثناء المحور وعناوين المحاور) أو من الخارج (متضمنًا المحور وعناوين المحاور). قراءة/كتابة [LayoutTargetType](../../com.aspose.slides/layouttargettype)(\#getLayoutTargetType.getLayoutTargetType/\#setLayoutTargetType(int).setLayoutTargetType(int)).

--------------------

> ```
> Presentation presentation = new Presentation();
>   try
>   {
>       ISlide slide = presentation.getSlides().get_Item(0);
>       IChart chart = slide.getShapes().addChart(ChartType.ClusteredColumn, 20, 100, 600, 400);
>       chart.getPlotArea().setX(0.2f);
>       chart.getPlotArea().setY(0.2f);
>       chart.getPlotArea().setWidth(0.7f);
>       chart.getPlotArea().setHeight(0.7f);
>       chart.getPlotArea().setLayoutTargetType(LayoutTargetType.Inner);
>       ...
>   } finally {
>       if (presentation != null) presentation.dispose();
>   }
> ```

**المعلمات:**  
| المعلمة | النوع | الوصف |
| --- | --- | --- |
| value | int |  |
### getActualX() {#getActualX--}
```
public final float getActualX()
```

يحدد الموقع الفعلي X (اليسار) لعنصر المخطط بالنسبة للزاوية اليسرى العليا للمخطط. استدعِ الدالة IChart.ValidateChartLayout() مسبقًا للحصول على القيم الفعلية. قراءة float.

**الإرجاع:**  
float
### getActualY() {#getActualY--}
```
public final float getActualY()
```

يحدد أعلى العنصر الفعلي للمخطط بالنسبة للزاوية اليسرى العليا للمخطط. استدعِ الدالة IChart.ValidateChartLayout() مسبقًا للحصول على القيم الفعلية. قراءة float.

**الإرجاع:**  
float
### getActualWidth() {#getActualWidth--}
```
public final float getActualWidth()
```

يحدد العرض الفعلي لعنصر المخطط. استدعِ الدالة IChart.ValidateChartLayout() مسبقًا للحصول على القيم الفعلية. قراءة float.

**الإرجاع:**  
float
### getActualHeight() {#getActualHeight--}
```
public final float getActualHeight()
```

يحدد الارتفاع الفعلي لعنصر المخطط. استدعِ الدالة IChart.ValidateChartLayout() مسبقًا للحصول على القيم الفعلية. قراءة float.

**الإرجاع:**  
float
### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

يعيد الشريحة الأصلية لكائن FillFormat. قراءة فقط [BaseSlide](../../com.aspose.slides/baseslide).

**الإرجاع:**  
[IBaseSlide](../../com.aspose.slides/ibaseslide)
### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

يعيد العرض التقديمي الأصلي لكائن FillFormat. قراءة فقط [IPresentation](../../com.aspose.slides/ipresentation).

**الإرجاع:**  
[IPresentation](../../com.aspose.slides/ipresentation)