---
title: CommandEffect
second_title: Aspose.Slides لأندرويد عبر مرجع API للغة Java
description: يمثل تأثير أمر لسلوك الرسوم المتحركة.
type: docs
url: /ar/com.aspose.slides/commandeffect/
---
**الوراثة:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**جميع الواجهات المنفذة:**
[com.aspose.slides.ICommandEffect](../../com.aspose.slides/icommandeffect)
```
public class CommandEffect extends Behavior implements ICommandEffect
```

يمثل تأثير الأمر لسلوك الرسوم المتحركة.

## المنشئين

| المُنشئ | الوصف |
| --- | --- |
| [CommandEffect()](#CommandEffect--) | ينشئ مثيلاً جديداً. |

## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getType()](#getType--) | يعرّف نوع تأثير الأمر للسلوك. |
| [setType(byte value)](#setType-byte-) | يعرّف نوع تأثير الأمر للسلوك. |
| [getCommandString()](#getCommandString--) | يعرّف سلسلة الأمر. |
| [setCommandString(String value)](#setCommandString-java.lang.String-) | يعرّف سلسلة الأمر. |
| [getShapeTarget()](#getShapeTarget--) | يعرّف الهدف الشكل لتأثير الأمر. |
| [setShapeTarget(IShape value)](#setShapeTarget-com.aspose.slides.IShape-) | يعرّف الهدف الشكل لتأثير الأمر. |

### CommandEffect() {#CommandEffect--}
```
public CommandEffect()
```

ينشئ مثيلاً جديداً.

### getType() {#getType--}
```
public final byte getType()
```

يعرّف نوع تأثير الأمر للسلوك. قراءة/كتابة [CommandEffectType](../../com.aspose.slides/commandeffecttype).

**الإرجاع:**
byte

### setType(byte value) {#setType-byte-}
```
public final void setType(byte value)
```

يعرّف نوع تأثير الأمر للسلوك. قراءة/كتابة [CommandEffectType](../../com.aspose.slides/commandeffecttype).

**المعلمات:**
| المعلمة | النوع | الوصف |
| --- | --- | --- |
| value | byte |  |

### getCommandString() {#getCommandString--}
```
public final String getCommandString()
```

يعرّف سلسلة الأمر. قراءة/كتابة String.

**الإرجاع:**
java.lang.String

### setCommandString(String value) {#setCommandString-java.lang.String-}
```
public final void setCommandString(String value)
```

يعرّف سلسلة الأمر. قراءة/كتابة String.

**المعلمات:**
| المعلمة | النوع | الوصف |
| --- | --- | --- |
| value | java.lang.String |  |

### getShapeTarget() {#getShapeTarget--}
```
public final IShape getShapeTarget()
```

يعرّف الهدف الشكل لتأثير الأمر. قراءة/كتابة [IShape](../../com.aspose.slides/ishape).

**الإرجاع:**
[IShape](../../com.aspose.slides/ishape)

### setShapeTarget(IShape value) {#setShapeTarget-com.aspose.slides.IShape-}
```
public final void setShapeTarget(IShape value)
```

يعرّف الهدف الشكل لتأثير الأمر. قراءة/كتابة [IShape](../../com.aspose.slides/ishape).

**المعلمات:**
| المعلمة | النوع | الوصف |
| --- | --- | --- |
| value | [IShape](../../com.aspose.slides/ishape) |  |