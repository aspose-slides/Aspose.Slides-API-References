---
title: SmartArtLayoutType
second_title: Aspose.Slides لنظام Android عبر مرجع API جافا
description: يمثل نوع تخطيط مخطط SmartArt.
type: docs
url: /ar/com.aspose.slides/smartartlayouttype/
---
**الوراثة:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class SmartArtLayoutType extends System.Enum
```

يمثل نوع تخطيط مخطط SmartArt.
## الحقول

| الحقل | الوصف |
| --- | --- |
| [AccentProcess](#AccentProcess) | يُستخدم لعرض تقدم، أو مخطط زمني، أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. |
| [AccentedPicture](#AccentedPicture) | يُستخدم لعرض فكرة مركزية تصويرية مع أفكار ذات صلة على الجانب. |
| [AlternatingFlow](#AlternatingFlow) | يُستخدم لعرض مجموعات من المعلومات أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. |
| [AlternatingHexagons](#AlternatingHexagons) | يُستخدم لتمثيل سلسلة من الأفكار المترابطة. |
| [AlternatingPictureBlocks](#AlternatingPictureBlocks) | يُستخدم لعرض سلسلة من الصور من الأعلى إلى الأسفل. |
| [AlternatingPictureCircles](#AlternatingPictureCircles) | يُستخدم لعرض مجموعة من الصور مع نص. |
| [ArrowRibbon](#ArrowRibbon) | يُستخدم لعرض مفاهيم ذات صلة أو متقابلة مع بعض الارتباط، مثل القوى المتعارضة. |
| [AscendingPictureAccentProcess](#AscendingPictureAccentProcess) | يُستخدم لعرض سلسلة من الصور المتصاعدة مع نص وصفي. |
| [Balance](#Balance) | يُستخدم للمقارنة أو إظهار العلاقة بين فكرتين. |
| [BasicBendingProcess](#BasicBendingProcess) | يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. |
| [BasicBlockList](#BasicBlockList) | يُستخدم لعرض كتل معلومات غير متسلسلة أو مجمّعة. |
| [BasicChevronProcess](#BasicChevronProcess) | يُستخدم لعرض تقدم؛ أو مخطط زمني؛ أو خطوات متسلسلة في مهمة أو عملية أو سير عمل؛ أو لتأكيد الحركة أو الاتجاه. |
| [BasicCycle](#BasicCycle) | يُستخدم لتمثيل تسلسل مستمر للمراحل أو المهام أو الأحداث في تدفق دائري. |
| [BasicMatrix](#BasicMatrix) | يُستخدم لإظهار علاقة المكونات بالكل في أرباع. |
| [BasicPie](#BasicPie) | يُستخدم لإظهار كيف تشكل الأجزاء الفردية كلًا. |
| [BasicProcess](#BasicProcess) | يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. |
| [BasicPyramid](#BasicPyramid) | يُستخدم لإظهار علاقات نسبية أو مترابطة أو هرمية مع أكبر مكون في الأسفل ويتناقص صعودًا. |
| [BasicRadial](#BasicRadial) | يُستخدم لإظهار العلاقة بفكرة مركزية في دورة. |
| [BasicTarget](#BasicTarget) | يُستخدم لإظهار التضمين أو التدرجات أو العلاقات الهرمية. |
| [BasicTimeline](#BasicTimeline) | يُستخدم لعرض خطوات متسلسلة في مهمة أو عملية أو سير عمل، أو لعرض معلومات مخطط زمني. |
| [BasicVenn](#BasicVenn) | يُستخدم لعرض علاقات متداخلة أو مترابطة. |
| [BendingPictureAccentList](#BendingPictureAccentList) | يُستخدم لعرض كتل معلومات غير متسلسلة أو مجمّعة. |
| [BendingPictureBlocks](#BendingPictureBlocks) | يُستخدم لعرض سلسلة من الصور. |
| [BendingPictureCaption](#BendingPictureCaption) | يُستخدم لعرض سلسلة متسلسلة من الصور. |
| [BendingPictureCaptionList](#BendingPictureCaptionList) | يُستخدم لعرض سلسلة من الصور. |
| [BendingPictureSemiTransparentText](#BendingPictureSemiTransparentText) | يُستخدم لعرض سلسلة من الصور. |
| [BlockCycle](#BlockCycle) | يُستخدم لتمثيل تسلسل مستمر للمراحل أو المهام أو الأحداث في تدفق دائري. |
| [BubblePictureList](#BubblePictureList) | يُستخدم لعرض سلسلة من الصور. |
| [CaptionedPictures](#CaptionedPictures) | يُستخدم لعرض صور مع مستويات متعددة من النص. |
| [ChevronList](#ChevronList) | يُستخدم لعرض تقدم عبر عدة عمليات تشكل سير عمل شامل. |
| [CircleAccentTimeline](#CircleAccentTimeline) | يُستخدم لعرض سلسلة من الأحداث أو معلومات مخطط زمني. |
| [CircleArrowProcess](#CircleArrowProcess) | يُستخدم لعرض عناصر متسلسلة مع نص داعم لكل عنصر. |
| [CirclePictureHierarchy](#CirclePictureHierarchy) | يُستخدم لعرض معلومات هرمية أو علاقات تقارير في منظمة. |
| [CircleRelationship](#CircleRelationship) | يُستخدم لإظهار العلاقة مع أو من فكرة مركزية. |
| [CircularBendingProcess](#CircularBendingProcess) | يُستخدم لعرض تسلسل طويل أو غير خطي أو خطوات في مهمة أو عملية أو سير عمل. |
| [CircularPictureCallout](#CircularPictureCallout) | يُستخدم لعرض فكرة مركزية وأفكار فرعية أو عناصر ذات صلة. |
| [ClosedChevronProcess](#ClosedChevronProcess) | يُستخدم لعرض تقدم؛ أو مخطط زمني؛ أو خطوات متسلسلة في مهمة أو عملية أو سير عمل؛ أو لتأكيد الحركة أو الاتجاه. |
| [ContinuousArrowProcess](#ContinuousArrowProcess) | يُستخدم لعرض مخطط زمني أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. |
| [ContinuousBlockProcess](#ContinuousBlockProcess) | يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. |
| [ContinuousCycle](#ContinuousCycle) | يُستخدم لتمثيل تسلسل مستمر للمراحل أو المهام أو الأحداث في تدفق دائري. |
| [ContinuousPictureList](#ContinuousPictureList) | يُستخدم لعرض مجموعات من المعلومات المترابطة. |
| [ConvergingArrows](#ConvergingArrows) | يُستخدم لعرض أفكار أو مفاهيم تتقارب إلى نقطة مركزية. |
| [ConvergingRadial](#ConvergingRadial) | يُستخدم لإظهار علاقات المفاهيم أو المكونات بفكرة مركزية في دورة. |
| [CounterbalanceArrows](#CounterbalanceArrows) | يُستخدم لعرض فكرتين أو مفهومين متعارضين. |
| [CycleMatrix](#CycleMatrix) | يُستخدم لإظهار العلاقة بفكرة مركزية في تطور دوري. |
| [DescendingBlockList](#DescendingBlockList) | يُستخدم لعرض مجموعات من الأفكار ذات الصلة أو قوائم معلومات. |
| [DescendingProcess](#DescendingProcess) | يُستخدم لعرض سلسلة متناقصة من الأحداث. |
| [DetailedProcess](#DetailedProcess) | يُستخدم مع كميات كبيرة من النص من المستوى الثاني لإظهار تقدم عبر المراحل. |
| [DivergingArrows](#DivergingArrows) | يُستخدم لعرض أفكار أو مفاهيم تتطور خارجيًا من مصدر مركزي. |
| [DivergingRadial](#DivergingRadial) | يُستخدم لإظهار علاقات بفكرة مركزية في دورة. |
| [Equation](#Equation) | يُستخدم لعرض خطوات أو مهام متسلسلة تصف خطة أو نتيجة. |
| [FramedTextPicture](#FramedTextPicture) | يُستخدم لعرض صور مع نص المستوى الأول المقابل معروض في إطار. |
| [Funnel](#Funnel) | يُستخدم لإظهار تصفية المعلومات أو كيف تندمج الأجزاء لتشكل كلًا. |
| [Gear](#Gear) | يُستخدم لعرض أفكار متشابكة. |
| [GridMatrix](#GridMatrix) | يُستخدم لإظهار وضع المفاهيم على محوريْن. |
| [GroupedList](#GroupedList) | يُستخدم لعرض مجموعات ومجموعات فرعية من المعلومات، أو خطوات وخطوات فرعية في مهمة أو عملية أو سير عمل. |
| [HalfCircleOrganizationChart](#HalfCircleOrganizationChart) | يُستخدم لعرض معلومات هرمية أو علاقات تقارير في منظمة. |
| [HexagonCluster](#HexagonCluster) | يُستخدم لعرض صور مع نص وصفي مرتبط. |
| [Hierarchy](#Hierarchy) | يُستخدم لعرض علاقات هرمية تتطور من الأعلى إلى الأسفل. |
| [HierarchyList](#HierarchyList) | يُستخدم لعرض علاقات هرمية تتطور عبر المجموعات. |
| [HorizontalBulletList](#HorizontalBulletList) | يُستخدم لعرض قوائم معلومات غير متسلسلة أو مجمّعة. |
| [HorizontalHierarchy](#HorizontalHierarchy) | يُستخدم لعرض علاقات هرمية تتطور أفقيًا. |
| [HorizontalLabeledHierarchy](#HorizontalLabeledHierarchy) | يُستخدم لعرض علاقات هرمية تتطور أفقيًا ومجمّعة هرميًا. |
| [HorizontalMultiLevelHierarchy](#HorizontalMultiLevelHierarchy) | يُستخدم لعرض كميات كبيرة من المعلومات الهرمية تتطور أفقيًا. |
| [HorizontalOrganizationChart](#HorizontalOrganizationChart) | يُستخدم لعرض معلومات هرمية أفقيًا أو علاقات تقارير في منظمة. |
| [HorizontalPictureList](#HorizontalPictureList) | يُستخدم لعرض معلومات غير متسلسلة أو مجمّعة مع التركيز على الصور ذات الصلة. |
| [IncreasingArrowsProcess](#IncreasingArrowsProcess) | يُستخدم لعرض خطوات متسلسلة ومتشابكة في عملية. |
| [IncreasingCircleProcess](#IncreasingCircleProcess) | يُستخدم لعرض سلسلة من الخطوات، حيث يزداد داخل الدائرة مع كل خطوة. |
| [InvertedPyramid](#InvertedPyramid) | يُستخدم لإظهار علاقات نسبية أو مترابطة أو هرمية مع أكبر مكون في الأعلى ويتناقص نحو الأسفل. |
| [LabeledHierarchy](#LabeledHierarchy) | يُستخدم لعرض علاقات هرمية تتطور من الأعلى إلى الأسفل ومجمّعة هرميًا. |
| [LinearVenn](#LinearVenn) | يُستخدم لعرض علاقات متداخلة في تسلسل. |
| [LinedList](#LinedList) | يُستخدم لعرض كميات كبيرة من النص مقسّمة إلى فئات وفئات فرعية. |
| [MultidirectionalCycle](#MultidirectionalCycle) | يُستخدم لتمثيل تسلسل مستمر للمراحل أو المهام أو الأحداث يمكن أن يحدث في أي اتجاه. |
| [NameandTitleOrganizationChart](#NameandTitleOrganizationChart) | يُستخدم لعرض معلومات هرمية أو علاقات تقارير في منظمة. |
| [NestedTarget](#NestedTarget) | يُستخدم لعرض علاقات التضمين. |
| [NondirectionalCycle](#NondirectionalCycle) | يُستخدم لتمثيل تسلسل مستمر للمراحل أو المهام أو الأحداث في تدفق دائري. |
| [OpposingArrows](#OpposingArrows) | يُستخدم لعرض فكرتين متعارضتين، أو أفكار تتباعد عن نقطة مركزية. |
| [OpposingIdeas](#OpposingIdeas) | يُستخدم لعرض فكرتين متعارضتين أو متناقضتين. |
| [OrganizationChart](#OrganizationChart) | يُستخدم لعرض معلومات هرمية أو علاقات تقارير في منظمة. |
| [PhasedProcess](#PhasedProcess) | يُستخدم لعرض ثلاث مراحل من عملية. |
| [PictureAccentBlocks](#PictureAccentBlocks) | يُستخدم لعرض مجموعة من الصور في كتل تبدأ من الزاوية. |
| [PictureAccentList](#PictureAccentList) | يُستخدم لعرض معلومات مجمّعة أو ذات صلة. |
| [PictureAccentProcess](#PictureAccentProcess) | يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. |
| [PictureCaptionList](#PictureCaptionList) | يُستخدم لعرض كتل معلومات غير متسلسلة أو مجمّعة. |
| [PictureGrid](#PictureGrid) | يُستخدم لعرض صور مرتبة على شبكة مربعة. |
| [PictureLineup](#PictureLineup) | يُستخدم لعرض سلسلة من الصور جنبًا إلى جنب. |
| [PictureStrips](#PictureStrips) | يُستخدم لعرض سلسلة من الصور من الأعلى إلى الأسفل مع نص المستوى الأول بجانب كل صورة. |
| [PieProcess](#PieProcess) | يُستخدم لعرض خطوات في عملية مع زيادة حجم كل شريحة من الدائرة حتى سبعة أشكال. |
| [PlusandMinus](#PlusandMinus) | يُستخدم لعرض مزايا وعيوب فكرتين. |
| [ProcessArrows](#ProcessArrows) | يُستخدم لعرض معلومات توضح عملية أو سير عمل. |
| [ProcessList](#ProcessList) | يُستخدم لعرض مجموعات متعددة من المعلومات أو خطوات وخطوات فرعية في مهمة أو عملية أو سير عمل. |
| [PyramidList](#PyramidList) | يُستخدم لإظهار علاقات نسبية أو مترابطة أو هرمية. |
| [RadialCluster](#RadialCluster) | يُستخدم لعرض بيانات ترتبط بفكرة أو موضوع مركزي. |
| [RadialCycle](#RadialCycle) | يُستخدم لإظهار العلاقة بفكرة مركزية. |
| [RadialList](#RadialList) | يُستخدم لإظهار علاقات بفكرة مركزية في دورة. |
| [RadialVenn](#RadialVenn) | يُستخدم لعرض كل من العلاقات المتداخلة والعلاقة بفكرة مركزية في دورة. |
| [RandomToResultProcess](#RandomToResultProcess) | يُستخدم، من خلال سلسلة من الخطوات، لتوضيح كيف يمكن لعدة أفكار فوضوية أن تؤدي إلى هدف أو فكرة موحدة. |
| [RepeatingBendingProcess](#RepeatingBendingProcess) | يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. |
| [ReverseList](#ReverseList) | يُستخدم لتغيير بين عنصرين. |
| [SegmentedCycle](#SegmentedCycle) | يُستخدم لعرض تقدم أو سلسلة من المراحل أو المهام أو الأحداث في تدفق دائري. |
| [SegmentedProcess](#SegmentedProcess) | يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. |
| [SegmentedPyramid](#SegmentedPyramid) | يُستخدم لإظهار التضمين أو العلاقات النسبية أو المترابطة. |
| [SnapshotPictureList](#SnapshotPictureList) | يُستخدم لعرض صور مع نص توضيحي. |
| [SpiralPicture](#SpiralPicture) | يُستخدم لعرض سلسلة من خمس صور كحد أقصى مع عناوين المستوى الأول المقابلة التي تتشكل على شكل حلزوني نحو المركز. |
| [SquareAccentList](#SquareAccentList) | يُستخدم لعرض قوائم معلومات مقسّمة إلى فئات. |
| [StackedList](#StackedList) | يُستخدم لعرض مجموعات معلومات أو خطوات في مهمة أو عملية أو سير عمل. |
| [StackedVenn](#StackedVenn) | يُستخدم لعرض علاقات متداخلة. |
| [StaggeredProcess](#StaggeredProcess) | يُستخدم لعرض تقدم تنازلي عبر المراحل. |
| [StepDownProcess](#StepDownProcess) | يُستخدم لعرض عملية تنازلية مع خطوات متعددة وخطوات فرعية. |
| [StepUpProcess](#StepUpProcess) | يُستخدم لعرض سلسلة تصاعدية من الخطوات أو قوائم معلومات. |
| [SubStepProcess](#SubStepProcess) | يُستخدم لعرض عملية متعددة الخطوات مع خطوات فرعية بين كل نص من المستوى الأول. |
| [TableHierarchy](#TableHierarchy) | يُستخدم لعرض مجموعات معلومات مبنية من الأعلى إلى الأسفل، والهرميات داخل كل مجموعة. |
| [TableList](#TableList) | يُستخدم لعرض معلومات مجمّعة أو ذات صلة ذات قيمة متساوية. |
| [TargetList](#TargetList) | يُستخدم لعرض معلومات مترابطة أو متداخلة. |
| [TextCycle](#TextCycle) | يُستخدم لتمثيل تسلسل مستمر للمراحل أو المهام أو الأحداث في تدفق دائري. |
| [TitlePictureLineup](#TitlePictureLineup) | يُستخدم لعرض سلسلة من الصور التي لها كل منها عنوانها ووصفها. |
| [TitledMatrix](#TitledMatrix) | يُستخدم لإظهار علاقات الأرباع الأربعة بالكل. |
| [TitledPictureAccentList](#TitledPictureAccentList) | يُستخدم لعرض قوائم معلومات مع صورة مميزة لكل نص من المستوى الثاني. |
| [TitledPictureBlocks](#TitledPictureBlocks) | يُستخدم لعرض سلسلة من الصور. |
| [TrapezoidList](#TrapezoidList) | يُستخدم لعرض معلومات مجمّعة أو ذات صلة ذات قيمة متساوية. |
| [UpwardArrow](#UpwardArrow) | يُستخدم لعرض تقدم أو خطوات تتجه صعودًا في مهمة أو عملية أو سير عمل. |
| [VerticalAccentList](#VerticalAccentList) | يُستخدم لعرض قوائم معلومات. |
| [VerticalArrowList](#VerticalArrowList) | يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل تتجه نحو هدف مشترك. |
| [VerticalBendingProcess](#VerticalBendingProcess) | يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. |
| [VerticalBlockList](#VerticalBlockList) | يُستخدم لعرض مجموعات معلومات أو خطوات في مهمة أو عملية أو سير عمل. |
| [VerticalBoxList](#VerticalBoxList) | يُستخدم لعرض عدة مجموعات من المعلومات، خصوصًا المجموعات التي تحتوي على كميات كبيرة من نص المستوى الثاني. |
| [VerticalBulletList](#VerticalBulletList) | يُستخدم لعرض كتل معلومات غير متسلسلة أو مجمّعة. |
| [VerticalChevronList](#VerticalChevronList) | يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل؛ أو لتأكيد الحركة أو الاتجاه. |
| [VerticalCircleList](#VerticalCircleList) | يُستخدم لعرض بيانات متسلسلة أو مجمّعة. |
| [VerticalCurvedList](#VerticalCurvedList) | يُستخدم لعرض قائمة معلومات منحنية. |
| [VerticalEquation](#VerticalEquation) | يُستخدم لعرض خطوات أو مهام متسلسلة تصف خطة أو نتيجة. |
| [VerticalPictureAccentList](#VerticalPictureAccentList) | يُستخدم لعرض كتل معلومات غير متسلسلة أو مجمّعة. |
| [VerticalPictureList](#VerticalPictureList) | يُستخدم لعرض كتل معلومات غير متسلسلة أو مجمّعة. |
| [VerticalProcess](#VerticalProcess) | يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل من الأعلى إلى الأسفل. |
| [Custom](#Custom) | يمثل مخطط SmartArt مع قالب تخطيط مخصص |
| [PictureOrganizationChart](#PictureOrganizationChart) | يُستخدم لعرض معلومات هرمية أو علاقات تقارير في منظمة، مع صور مقابلة. |
### AccentProcess {#AccentProcess}
```
public static final int AccentProcess
```

يُستخدم لعرض تقدم، أو مخطط زمني، أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. يعمل بشكل جيد لتوضيح النص من المستوى الأول والمستوى الثاني معًا.

### AccentedPicture {#AccentedPicture}
```
public static final int AccentedPicture
```

يُستخدم لعرض فكرة مركزية تصويرية مع أفكار ذات صلة على الجانب. يظهر النص الأعلى من المستوى الأول فوق الصورة المركزية. يظهر النص المقابل للأشكال الأخرى من المستوى الأول بجانب الصور الدائرية الصغيرة. هذا التخطيط يعمل أيضًا جيدًا بدون نص.

### AlternatingFlow {#AlternatingFlow}
```
public static final int AlternatingFlow
```

يُستخدم لعرض مجموعات من المعلومات أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. يبرز التفاعل أو العلاقات بين مجموعات المعلومات.

### AlternatingHexagons {#AlternatingHexagons}
```
public static final int AlternatingHexagons
```

يُستخدم لتمثيل سلسلة من الأفكار المترابطة. يظهر النص من المستوى الأول داخل السداسيات. يظهر النص من المستوى الثاني خارج الأشكال.

### AlternatingPictureBlocks {#AlternatingPictureBlocks}
```
public static final int AlternatingPictureBlocks
```

يُستخدم لعرض سلسلة من الصور من الأعلى إلى الأسفل. يظهر النص بالتناوب على اليمين أو اليسار من الصورة.

### AlternatingPictureCircles {#AlternatingPictureCircles}
```
public static final int AlternatingPictureCircles
```

يُستخدم لعرض مجموعة من الصور مع نص. يظهر النص المقابل في الدوائر المركزية مع تبديل الصور من اليسار إلى اليمين.

### ArrowRibbon {#ArrowRibbon}
```
public static final int ArrowRibbon
```

يُستخدم لعرض مفاهيم ذات صلة أو متقابلة مع بعض الارتباط، مثل القوى المتعارضة. تُستخدم السطران الأولان من نص المستوى الأول للنص داخل الأسهم. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### AscendingPictureAccentProcess {#AscendingPictureAccentProcess}
```
public static final int AscendingPictureAccentProcess
```

يُستخدم لعرض سلسلة من الصور المتصاعدة مع نص وصفي. يعمل بشكل أفضل مع كمية قليلة من النص.

### Balance {#Balance}
```
public static final int Balance
```

يُستخدم للمقارنة أو إظهار العلاقة بين فكرتين. كل من السطرين الأولين من نص المستوى الأول يتطابق مع نص في أعلى أحد جانبي نقطة المركز. يبرز نص المستوى الثاني، الذي يقتصر على أربعة أشكال على كل جانب من نقطة المركز. تميل الموازنة إلى الجانب الذي يحتوي على أكبر عدد من الأشكال التي تحتوي على نص المستوى الثاني. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### BasicBendingProcess {#BasicBendingProcess}
```
public static final int BasicBendingProcess
```

يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. يضاعف مساحة العرض الأفقية والعمودية للأشكال.

### BasicBlockList {#BasicBlockList}
```
public static final int BasicBlockList
```

يُستخدم لعرض كتل معلومات غير متسلسلة أو مجمّعة. يضاعف مساحة العرض الأفقية والعمودية للأشكال.

### BasicChevronProcess {#BasicChevronProcess}
```
public static final int BasicChevronProcess
```

يُستخدم لعرض تقدم؛ أو مخطط زمني؛ أو خطوات متسلسلة في مهمة أو عملية أو سير عمل؛ أو لتأكيد الحركة أو الاتجاه. يظهر نص المستوى الأول داخل شكل السهم بينما يظهر نص المستوى الثاني أسفل أشكال السهم.

### BasicCycle {#BasicCycle}
```
public static final int BasicCycle
```

يُستخدم لتمثيل تسلسل مستمر للمراحل أو المهام أو الأحداث في تدفق دائري. يبرز المراحل أو الخطوات بدلاً من الأسهم أو التدفق المتصل. يعمل بشكل أفضل مع نص المستوى الأول فقط.

### BasicMatrix {#BasicMatrix}
```
public static final int BasicMatrix
```

يُستخدم لإظهار علاقة المكونات بالكل في أرباع. تظهر الأسطر الأربع الأولى من نص المستوى الأول في الأرباع. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### BasicPie {#BasicPie}
```
public static final int BasicPie
```

يُستخدم لإظهار كيف تشكل الأجزاء الفردية كلًا. تتطابق الأسطر السبعة الأولى من نص المستوى الأول مع شرائح الفطيرة المتوزعة بالتساوي. يظهر شكل نص المستوى الأول الأعلى خارج الفطيرة لبروز إضافي. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### BasicProcess {#BasicProcess}
```
public static final int BasicProcess
```

يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل.

### BasicPyramid {#BasicPyramid}
```
public static final int BasicPyramid
```

يُستخدم لإظهار علاقات نسبية أو مترابطة أو هرمية مع أكبر مكون في الأسفل ويتناقص صعودًا. يظهر نص المستوى الأول في قطاعات الهرم ويظهر نص المستوى الثاني في الأشكال المجاورة لكل قطاع.

### BasicRadial {#BasicRadial}
```
public static final int BasicRadial
```

يُستخدم لإظهار العلاقة بفكرة مركزية في دورة. يتطابق السطر الأول من نص المستوى الأول مع الشكل المركزي، ويتطابق نص المستوى الثاني مع الأشكال الدائرية المحيطة. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### BasicTarget {#BasicTarget}
```
public static final int BasicTarget
```

يُستخدم لعرض علاقات التضمين أو التدرجات أو العلاقات الهرمية. تتطابق الأسطر الخمسة الأولى من نص المستوى الأول مع دائرة. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### BasicTimeline {#BasicTimeline}
```
public static final int BasicTimeline
```

يُستخدم لعرض خطوات متسلسلة في مهمة أو عملية أو سير عمل، أو لعرض معلومات مخطط زمني. يعمل بشكل جيد مع نصي المستوى الأول والمستوى الثاني.

### BasicVenn {#BasicVenn}
```
public static final int BasicVenn
```

يُستخدم لعرض علاقات متداخلة أو مترابطة. تتطابق الأسطر السبعة الأولى من نص المستوى الأول مع دائرة. إذا كان هناك أربعة أسطر أو أقل من نص المستوى الأول، يكون النص داخل الدوائر؛ إذا كان هناك أكثر من أربعة أسطر، يكون النص خارج الدوائر. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### BendingPictureAccentList {#BendingPictureAccentList}
```
public static final int BendingPictureAccentList
```

يُستخدم لعرض كتل معلومات غير متسلسلة أو مجمّعة. تم تصميم الأشكال الدائرية الصغيرة لاحتواء الصور. يعمل بشكل جيد لتوضيح النص من المستوى الأول والمستوى الثاني معًا. يضاعف مساحة العرض الأفقية والعمودية للأشكال.

### BendingPictureBlocks {#BendingPictureBlocks}
```
public static final int BendingPictureBlocks
```

يُستخدم لعرض سلسلة من الصور. يمكن للصندوق الذي يغطي الزاوية السفلية أن يحتوي على كمية قليلة من النص.

### BendingPictureCaption {#BendingPictureCaption}
```
public static final int BendingPictureCaption
```

يُستخدم لعرض سلسلة متسلسلة من الصور. يمكن للصندوق الذي يغطي الزاوية السفلية أن يحتوي على كمية قليلة من النص.

### BendingPictureCaptionList {#BendingPictureCaptionList}
```
public static final int BendingPictureCaptionList
```

يُستخدم لعرض سلسلة من الصور. يظهر العنوان والوصف في شكل توضيحي تحت كل صورة.

### BendingPictureSemiTransparentText {#BendingPictureSemiTransparentText}
```
public static final int BendingPictureSemiTransparentText
```

يُستخدم لعرض سلسلة من الصور. يغطي صندوق شبه شفاف الجزء السفلي من الصورة ويحتوي على جميع مستويات النص.

### BlockCycle {#BlockCycle}
```
public static final int BlockCycle
```

يُستخدم لتمثيل تسلسل مستمر للمراحل أو المهام أو الأحداث في تدفق دائري. يبرز المراحل أو الخطوات بدلاً من الأسهم أو التدفق المتصل.

### BubblePictureList {#BubblePictureList}
```
public static final int BubblePictureList
```

يُستخدم لعرض سلسلة من الصور. يمكن أن يحتوي على ما يصل إلى ثماني صور من المستوى الأول. لا يظهر النص أو الصور غير المستخدمة، لكنها تبقى متاحة إذا تم تغيير التخطيط. يعمل بشكل أفضل مع كمية قليلة من النص.

### CaptionedPictures {#CaptionedPictures}
```
public static final int CaptionedPictures
```

يُستخدم لعرض صور مع مستويات متعددة من النص. يعمل بشكل أفضل مع كمية قليلة من نص المستوى الأول وكمية متوسطة من نص المستوى الثاني.

### ChevronList {#ChevronList}
```
public static final int ChevronList
```

يُستخدم لعرض تقدم عبر عدة عمليات تشكل سير عمل شامل. يعمل أيضًا على توضيح عمليات متقابلة. يتطابق نص المستوى الأول مع أول شكل سهم على اليسار، بينما يتطابق نص المستوى الثاني مع الخطوات الفرعية الأفقية لكل شكل يحتوي على نص المستوى الأول.

### CircleAccentTimeline {#CircleAccentTimeline}
```
public static final int CircleAccentTimeline
```

يُستخدم لعرض سلسلة من الأحداث أو معلومات مخطط زمني. يظهر نص المستوى الأول بجوار الأشكال الدائرية الأكبر. يظهر نص المستوى الثاني بجوار الأشكال الدائرية الأصغر.

### CircleArrowProcess {#CircleArrowProcess}
```
public static final int CircleArrowProcess
```

يُستخدم لعرض عناصر متسلسلة مع نص داعم لكل عنصر. يعمل هذا المخطط بشكل أفضل مع كمية قليلة من نص المستوى الأول.

### CirclePictureHierarchy {#CirclePictureHierarchy}
```
public static final int CirclePictureHierarchy
```

يُستخدم لعرض معلومات هرمية أو علاقات تقارير في منظمة. تظهر الصور في دوائر والنص المقابل يظهر بجوار الصور.

### CircleRelationship {#CircleRelationship}
```
public static final int CircleRelationship
```

يُستخدم لإظهار العلاقة مع أو من فكرة مركزية. يضاف نص المستوى الثاني غير المتسلسل ويقتصر على خمسة عناصر. يمكن أن يكون هناك عنصر واحد فقط من المستوى الأول.

### CircularBendingProcess {#CircularBendingProcess}
```
public static final int CircularBendingProcess
```

يُستخدم لعرض تسلسل طويل أو غير خطي أو خطوات في مهمة أو عملية أو سير عمل. يعمل بشكل أفضل مع نص المستوى الأول فقط. يضاعف مساحة العرض الأفقية والعمودية للأشكال.

### CircularPictureCallout {#CircularPictureCallout}
```
public static final int CircularPictureCallout
```

يُستخدم لعرض فكرة مركزية وأفكار فرعية أو عناصر ذات صلة. يغطي النص للصورة الأولى الجزء السفلي من الصورة. يظهر النص المقابل للأشكال الأخرى من المستوى الأول بجانب الصور الدائرية الصغيرة. يعمل هذا المخطط أيضًا جيدًا بدون نص.

### ClosedChevronProcess {#ClosedChevronProcess}
```
public static final int ClosedChevronProcess
```

يُستخدم لعرض تقدم، أو مخطط زمني، أو خطوات متسلسلة في مهمة أو عملية أو سير عمل؛ أو لتأكيد الحركة أو الاتجاه. يمكن استخدامه لتأكيد المعلومات في الشكل الأول. يعمل بشكل أفضل مع نص المستوى الأول فقط.

### ContinuousArrowProcess {#ContinuousArrowProcess}
```
public static final int ContinuousArrowProcess
```

يُستخدم لعرض مخطط زمني أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. يعمل بشكل أفضل مع نص المستوى الأول لأن كل سطر من نص المستوى الأول يظهر داخل شكل السهم. يظهر نص المستوى الثاني خارج شكل السهم.

### ContinuousBlockProcess {#ContinuousBlockProcess}
```
public static final int ContinuousBlockProcess
```

يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. يعمل بشكل أفضل مع نصي المستوى الأول والمستوى الثاني القليلين.

### ContinuousCycle {#ContinuousCycle}
```
public static final int ContinuousCycle
```

يُستخدم لتمثيل تسلسل مستمر للمراحل أو المهام أو الأحداث في تدفق دائري. يبرز الاتصال بين جميع المكونات. يعمل بشكل أفضل مع نص المستوى الأول فقط.

### ContinuousPictureList {#ContinuousPictureList}
```
public static final int ContinuousPictureList
```

يُستخدم لعرض مجموعات من المعلومات المترابطة. تم تصميم الأشكال الدائرية لاحتواء الصور.

### ConvergingArrows {#ConvergingArrows}
```
public static final int ConvergingArrows
```

يُستخدم لعرض أفكار أو مفاهيم تتقارب إلى نقطة مركزية. يعمل بشكل أفضل مع نص المستوى الأول فقط.

### ConvergingRadial {#ConvergingRadial}
```
public ... 
```

يُستخدم لإظهار علاقات المفاهيم أو المكونات بفكرة مركزية في دورة. يتطابق السطر الأول من نص المستوى الأول مع الشكل الدائري المركزي، وتطابق أسطر نص المستوى الثاني مع الأشكال المستطيلة المحيطة. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### CounterbalanceArrows {#CounterbalanceArrows}
```
public static final int CounterbalanceArrows
```

يُستخدم لعرض فكرتين أو مفهومين متعارضين. يتطابق السطران الأولان من نص المستوى الأول مع سهمين ويعملان جيدًا مع نص المستوى الثاني. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### CycleMatrix {#CycleMatrix}
```
public static final int CycleMatrix
```

يُستخدم لإظهار العلاقة بفكرة مركزية في تطور دوري. يتطابق الأسطر الأربعة الأولى من نص المستوى الأول مع شريحة أو شكل فطيرة، ويظهر نص المستوى الثاني في شكل مستطيل بجانب الشريحة أو الشكل الفطيري. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### DescendingBlockList {#DescendingBlockList}
```
public static final int DescendingBlockList
```

يُستخدم لعرض مجموعات من الأفكار ذات الصلة أو قوائم معلومات. تقل ارتفاع أشكال النص بشكل متسلسلة، ويظهر نص المستوى الأول عموديًا.

### DescendingProcess {#DescendingProcess}
```
public static final int DescendingProcess
```

يُستخدم لعرض سلسلة تنازلية من الأحداث. يكون نص المستوى الأول الأول في أعلى السهم، والنص الأخير من المستوى الأول في أسفل السهم. تظهر فقط أول سبعة عناصر من المستوى الأول. يعمل بشكل أفضل مع كمية صغيرة إلى متوسطة من النص.

### DetailedProcess {#DetailedProcess}
```
public static final int DetailedProcess
```

يُستخدم مع كميات كبيرة من نص المستوى الثاني لعرض تقدم عبر المراحل.

### DivergingArrows {#DivergingArrows}
```
public static final int DivergingArrows
```

يُستخدم لعرض أفكار أو مفاهيم تتطور خارجيًا من مصدر مركزي. يعمل بشكل أفضل مع نص المستوى الأول فقط.

### DivergingRadial {#DivergingRadial}
```
public static final int DivergingRadial
```

يُستخدم لإظهار العلاقات بفكرة مركزية في دورة. يتطابق السطر الأول من نص المستوى الأول مع الشكل الدائري المركزي. يبرز الدوائر المحيطة بدلاً من الفكرة المركزية. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### Equation {#Equation}
```
public static final int Equation
```

يُستخدم لعرض خطوات أو مهام متسلسلة تصف خطة أو نتيجة. يظهر السطر الأخير من نص المستوى الأول بعد علامة المساواة (=). يعمل بشكل أفضل مع نص المستوى الأول فقط.

### FramedTextPicture {#FramedTextPicture}
```
public static final int FramedTextPicture
```

يُستخدم لعرض صور مع نص المستوى الأول المقابل معروض في إطار.

### Funnel {#Funnel}
```
public static final int Funnel
```

يُستخدم لإظهار تصفية المعلومات أو كيف تندمج الأجزاء لتشكل كلًا. يبرز النتيجة النهائية. يمكن أن يحتوي على ما يصل إلى أربعة أسطر من نص المستوى الأول؛ يظهر السطر الرابع أسفل القمع وتطابق الأسطر الأخرى مع شكل دائري. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### Gear {#Gear}
```
public static final int Gear
```

يُستخدم لعرض أفكار متشابكة. تتطابق الأسطر الثلاثة الأولى من نص المستوى الأول مع شكل تروس، وتظهر نصوص المستوى الثاني المقابلة في مستطيلات بجوار شكل الترس. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### GridMatrix {#GridMatrix}
```
public static final int GridMatrix
```

يُستخدم لإظهار وضع المفاهيم على محوريْن. يبرز المكونات الفردية بدلاً من الكل. تظهر الأسطر الأربعة الأولى من نص المستوى الأول في الأرباع. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### GroupedList {#GroupedList}
```
public static final int GroupedList
```

يُستخدم لعرض مجموعات ومجموعات فرعية من المعلومات، أو خطوات وخطوات فرعية في مهمة أو عملية أو سير عمل. يتطابق نص المستوى الأول مع الأشكال الأفقية العليا، ويتطابق نص المستوى الثاني مع الخطوات الفرعية العمودية تحت كل شكل علوي ذي صلة. يعمل جيدًا لتأكيد المجموعات الفرعية أو الخطوات الفرعية، المعلومات الهرمية، أو قوائم متعددة من المعلومات.

### HalfCircleOrganizationChart {#HalfCircleOrganizationChart}
```
public static final int HalfCircleOrganizationChart
```

يُستخدم لعرض معلومات هرمية أو علاقات تقارير في منظمة. تتوفر الأشكال المساعدة وتخطيطات مخطط المنظمة المعلقة مع هذا التخطيط.

### HexagonCluster {#HexagonCluster}
```
public static final int HexagonCluster
```

يُستخدم لعرض صور مع نص وصفي مقترن. تشير السداسيات الصغيرة إلى زوج الصورة والنص. يعمل بشكل أفضل مع كمية قليلة من النص.

### Hierarchy {#Hierarchy}
```
public static final int Hierarchy
```

يُستخدم لعرض علاقات هرمية تتطور من الأعلى إلى الأسفل.

### HierarchyList {#HierarchyList}
```
public static final int HierarchyList
```

يُستخدم لعرض علاقات هرمية تتطور عبر المجموعات. يمكن أيضًا استخدامها لتجميع أو سرد معلومات.

### HorizontalBulletList {#HorizontalBulletList}
```
public static final int HorizontalBulletList
```

يُستخدم لعرض قوائم معلومات غير متسلسلة أو مجمّعة. يعمل جيدًا مع كميات كبيرة من النص. جميع النصوص لها نفس مستوى التركيز، ولا يُستدل على اتجاه.

### HorizontalHierarchy {#HorizontalHierarchy}
```
public static final int HorizontalHierarchy
```

يُستخدم لعرض علاقات هرمية تتطور أفقيًا. يعمل جيدًا لأشجار اتخاذ القرار.

### HorizontalLabeledHierarchy {#HorizontalLabeledHierarchy}
```
public static final int HorizontalLabeledHierarchy
```

يُستخدم لعرض علاقات هرمية تتطور أفقيًا ومجمّعة هرميًا. يبرز النص أو عنوان المستوى الأول. يظهر السطر الأول من نص المستوى الأول في الشكل في بداية الهرم، وتظهر السطران الثاني وما يليهما من نص المستوى الأول أعلى المستطيلات الطويلة.

### HorizontalMultiLevelHierarchy {#HorizontalMultiLevelHierarchy}
```
public static final int HorizontalMultiLevelHierarchy
```

يُستخدم لعرض كميات كبيرة من المعلومات الهرمية تتطور أفقيًا. يُعرض أعلى الهرم عموديًا. يدعم هذا التخطيط مستويات متعددة في الهرم.

### HorizontalOrganizationChart {#HorizontalOrganizationChart}
```
public static final int HorizontalOrganizationChart
```

يُستخدم لعرض معلومات هرمية أفقيًا أو علاقات تقارير في منظمة. تتوفر الشكل المساعد وتخطيطات مخطط المنظمة المعلقة مع هذا التخطيط.

### HorizontalPictureList {#HorizontalPictureList}
```
public static final int HorizontalPictureList
```

يُستخدم لعرض معلومات غير متسلسلة أو مجمّعة مع التركيز على الصور ذات الصلة. تم تصميم الأشكال العلوية لاحتواء الصور.

### IncreasingArrowsProcess {#IncreasingArrowsProcess}
```
public static final int IncreasingArrowsProcess
```

يُستخدم لعرض خطوات وتداخلات متسلسلة في عملية. يقتصر على خمسة عناصر من المستوى الأول. يمكن لنص المستوى الثاني أن يحتوي على كميات كبيرة من النص.

### IncreasingCircleProcess {#IncreasingCircleProcess}
```
public static final int IncreasingCircleProcess
```

يُستخدم لعرض سلسلة من الخطوات، حيث يزداد داخل الدائرة مع كل خطوة. يقتصر على سبعة خطوات من المستوى الأول ولكن لا حدود لعناصر المستوى الثاني. يعمل جيدًا مع كميات كبيرة من نص المستوى الثاني.

### InvertedPyramid {#InvertedPyramid}
```
public static final int InvertedPyramid
```

يُستخدم لإظهار علاقات نسبية أو مترابطة أو هرمية مع أكبر مكون في الأعلى ويتناقص نحو الأسفل. يظهر نص المستوى الأول في قطاعات الهرم ويظهر نص المستوى الثاني في الأشكال المجاورة لكل قطاع.

### LabeledHierarchy {#LabeledHierarchy}
```
public static final int LabeledHierarchy
```

يُستخدم لعرض علاقات هرمية تتطور من الأعلى إلى الأسفل ومجمّعة هرميًا. يبرز النص أو عنوان المستوى الأول. يظهر السطر الأول من نص المستوى الأول في الشكل في بداية الهرم، وتظهر جميع الأسطر اللاحقة من نص المستوى الأول إلى يسار المستطيلات الطويلة.

### LinearVenn {#LinearVenn}
```
public static final int LinearVenn
```

يُستخدم لعرض علاقات متداخلة في تسلسل. يعمل بشكل أفضل مع نص المستوى الأول فقط.

### LinedList {#LinedList}
```
public static final int LinedList
```

يُستخدم لعرض كميات كبيرة من النص مقسّمة إلى فئات وفئات فرعية. يعمل جيدًا مع مستويات متعددة من النص. يتم فصل النص على نفس المستوى بخطوط.

### MultidirectionalCycle {#MultidirectionalCycle}
```
public static final int MultidirectionalCycle
```

يُستخدم لتمثيل تسلسل مستمر للمراحل أو المهام أو الأحداث التي يمكن أن تحدث في أي اتجاه.

### NameandTitleOrganizationChart {#NameandTitleOrganizationChart}
```
public static final int NameandTitleOrganizationChart
```

يُستخدم لعرض معلومات هرمية أو علاقات تقارير في منظمة. لإدخال نص في صندوق العنوان، اكتب مباشرة في الشكل المستطيل الأصغر. تتوفر الشكل المساعد وتخطيطات مخطط المنظمة المعلقة مع هذا التخطيط.

### NestedTarget {#NestedTarget}
```
public static final int NestedTarget
```

يُستخدم لعرض علاقات التضمين. تتطابق الأسطر الثلاثة الأولى من نص المستوى الأول مع النص العلوي الأيسر في الأشكال، ويتطابق نص المستوى الثاني مع الأشكال الأصغر. يعمل بشكل أفضل مع عدد قليل من أسطر نص المستوى الثاني. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### NondirectionalCycle {#NondirectionalCycle}
```
public static final int NondirectionalCycle
```

يُستخدم لتمثيل تسلسل مستمر للمراحل أو المهام أو الأحداث في تدفق دائري. لكل شكل نفس مستوى الأهمية. يعمل جيدًا عندما لا يلزم الإشارة إلى الاتجاه.

### OpposingArrows {#OpposingArrows}
```
public static final int OpposingArrows
```

يُستخدم لعرض فكرتين متعارضتين، أو أفكار تتباعد عن نقطة مركزية. يتطابق السطران الأولان من نص المستوى الأول مع سهمين. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### OpposingIdeas {#OpposingIdeas}
```
public static final int OpposingIdeas
```

يُستخدم لعرض فكرتين متعارضتين أو متناقضتين. يمكن أن يكون هناك عنصر أو عنصران من المستوى الأول. يمكن لكل نص من المستوى الأول أن يحتوي على مستويات فرعية متعددة. يعمل جيدًا مع كميات كبيرة من النص.

### OrganizationChart {#OrganizationChart}
```
public static final int OrganizationChart
```

يُستخدم لعرض معلومات هرمية أو علاقات تقارير في منظمة. تتوفر الشكل المساعد وتخطيطات مخطط المنظمة المعلقة مع هذا التخطيط.

### PhasedProcess {#PhasedProcess}
```
public static final int PhasedProcess
```

يُستخدم لعرض ثلاث مراحل من عملية. يقتصر على ثلاثة عناصر من المستوى الأول. يمكن لكل من العنصرين الأولين من المستوى الأول أن يحتوي كل منهما على أربعة عناصر من المستوى الثاني، ويمكن للعنصر الثالث من المستوى الأول أن يحتوي على عدد غير محدود من عناصر المستوى الثاني. يعمل بشكل أفضل مع كمية قليلة من النص.

### PictureAccentBlocks {#PictureAccentBlocks}
```
public static final int PictureAccentBlocks
```

يُستخدم لعرض مجموعة من الصور في كتل تبدأ من الزاوية. يظهر النص المقابل عموديًا. يعمل جيدًا كتأكيد على العنوان أو العناوين الفرعية للشرائح أو لفواصل أقسام المستند.

### PictureAccentList {#PictureAccentList}
```
public static final int PictureAccentList
```

يُستخدم لعرض معلومات مجمّعة أو ذات صلة. تم تصميم الأشكال الصغيرة في الزوايا العليا لاحتواء الصور. يبرز نص المستوى الثاني على نص المستوى الأول، وهو اختيار جيد لكميات كبيرة من نص المستوى الثاني.

### PictureAccentProcess {#PictureAccentProcess}
```
public static final int PictureAccentProcess
```

يُستخدم لعرض خطوات متسلسلة في مهمة أو عملية أو سير عمل. تم تصميم الأشكال المستطيلة في الخلفية لاحتواء الصور.

### PictureCaptionList {#PictureCaptionList}
```
public static final int PictureCaptionList
```

يُستخدم لعرض كتل معلومات غير متسلسلة أو مجمّعة. تم تصميم الأشكال العليا لاحتواء الصور وتُبرز الصور على النص. يعمل جيدًا للصور مع تعليقات نصية قصيرة.

### PictureGrid {#PictureGrid}
```
public static final int PictureGrid
```

يُستخدم لعرض صور مرتبة على شبكة مربعة. الأفضل مع كمية قليلة من نص المستوى الأول، الذي يظهر فوق الصورة.

### PictureLineup {#PictureLineup}
```
public static final int PictureLineup
```

يُستخدم لعرض سلسلة من الصور جنبًا إلى جنب. يغطي نص المستوى الأول أعلى الصورة. يظهر نص المستوى الثاني أسفل الصورة.

### PictureStrips {#PictureStrips}
```
public static final int PictureStrips
```

يُستخدم لعرض سلسلة من الصور من الأعلى إلى الأسفل مع نص المستوى الأول بجانب كل منها.

### PieProcess {#PieProcess}
```
public static final int PictureStrips
```

يُستخدم لعرض خطوات في عملية مع زيادة حجم كل شريحة فطيرة حتى سبعة أشكال. يعرض نص المستوى الأول عموديًا.

### PlusandMinus {#PlusandMinus}
```
public static final int PlusandMinus
```

يُستخدم لعرض مزايا وعيوب فكرتين. يمكن أن يحتوي كل نص من المستوى الأول على مستويات فرعية متعددة. يعمل جيدًا مع كميات كبيرة من النص. يقتصر على عنصرين من المستوى الأول.

### ProcessArrows {#ProcessArrows}
```
public static final int ProcessArrows
```

يُستخدم لعرض معلومات توضح عملية أو سير عمل. يظهر نص المستوى الأول في الأشكال الدائرية ويظهر نص المستوى الثاني في أشكال السهم. يعمل بشكل أفضل مع نص قليل لتأكيد الحركة أو الاتجاه.

### ProcessList {#ProcessList}
```
public static final int ProcessList
```

يُستخدم لعرض مجموعات متعددة من المعلومات أو خطوات وخطوات فرعية في مهمة أو عملية أو سير عمل. يتطابق نص المستوى الأول مع الأشكال الأفقية العليا، ويتطابق نص المستوى الثاني مع الخطوات الفرعية العمودية تحت كل شكل علوي ذو صلة.

### PyramidList {#PyramidList}
```
public static final int PyramidList
```

يُستخدم لإظهار علاقات نسبية أو مترابطة أو هرمية. يظهر النص في الأشكال المستطيلة أعلى خلفية الهرم.

### RadialCluster {#RadialCluster}
```
public static final int RadialCluster
```

يُستخدم لعرض بيانات ترتبط بفكرة أو موضوع مركزي. يظهر نص المستوى الأول الأعلى في المركز. يظهر نص المستوى الثاني في الأشكال المحيطة. يمكن أن يحتوي على ما يصل إلى سبعة أشكال من المستوى الثاني. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط. يعمل بشكل أفضل مع كمية قليلة من النص.

### RadialCycle {#RadialCycle}
```
public static final int RadialCycle
```

يُستخدم لإظهار العلاقة بفكرة مركزية. يبرز كل من المعلومات في الدائرة المركزية وكيف تساهم المعلومات في الحلقة الخارجية من الدوائر في الفكرة المركزية. يتطابق السطر الأول من نص المستوى الأول مع الدائرة المركزية، ويتطابق نص المستوى الثاني مع الحلقة الخارجية من الدوائر. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### RadialList {#RadialList}
```
public static final int RadialList
```

يُستخدم لإظهار علاقات بفكرة مركزية في دورة. يمكن أن يحتوي الشكل المركزي على صورة. يظهر نص المستوى الأول في الدوائر الصغيرة وأي نص مرتبط من المستوى الثاني يظهر إلى جانب الدوائر الصغيرة.

### RadialVenn {#RadialVenn}
```
public static final int RadialVenn
```

يُستخدم لعرض كل من العلاقات المتداخلة والعلاقة بفكرة مركزية في دورة. يتطابق السطر الأول من نص المستوى الأول مع الشكل المركزي وتطابق أسطر نص المستوى الثاني مع الأشكال الدائرية المحيطة. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### RandomToResultProcess {#RandomToResultProcess}
```
public static final int RandomToResultProcess
```

يُستخدم، من خلال سلسلة من الخطوات، لتوضيح كيف يمكن لعدة أفكار فوضوية أن تؤدي إلى هدف أو فكرة موحدة. يدعم عناصر متعددة من نص المستوى الأول، لكن الشكلين الأول والأخير من المستوى الأول ثابتان. يعمل بشكل أفضل مع كمية قليلة من نص المستوى الأول وكميات متوسطة من نص المستوى الثاني.

### RepeatingBendingProcess {#RepeatingBendingProcess}
```
public static final int RepeatingBendingProcess
```

يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. يضاعف مساحة العرض الأفقية والعمودية للأشكال.

### ReverseList {#ReverseList}
```
public static final int ReverseList
```

يُستخدم لتغيير بين عنصرين. يتم عرض العنصرين الأولين من النص فقط، ويمكن لكل عنصر أن يحتوي على كمية كبيرة من النص. يعمل جيدًا لعرض تغيير بين عنصرين أو تحول في الترتيب.

### SegmentedCycle {#SegmentedCycle}
```
public static final int SegmentedCycle
```

يُستخدم لعرض تقدم أو تسلسل من المراحل أو المهام أو الأحداث في تدفق دائري. يبرز القطع المتصلة. تتطابق الأسطر السبعة الأولى من نص المستوى الأول مع شريحة أو شكل فطيرة. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### SegmentedProcess {#SegmentedProcess}
```
public static final int SegmentedProcess
```

يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. يبرز نص المستوى الثاني، حيث يظهر كل سطر في شكل منفصل.

### SegmentedPyramid {#SegmentedPyramid}
```
public static final int SegmentedPyramid
```

يُستخدم لعرض علاقات التضمين أو النسبية أو المترابطة. تظهر الأسطر التسعة الأولى من نص المستوى الأول في الأشكال المثلثية. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط. يعمل بشكل أفضل مع نص المستوى الأول فقط.

### SnapshotPictureList {#SnapshotPictureList}
```
public static final int SnapshotPictureList
```

يُستخدم لعرض صور مع نص توضيحي. يمكن لنص المستوى الثاني عرض قوائم معلومات. يعمل جيدًا مع كمية كبيرة من النص.

### SpiralPicture {#SpiralPicture}
```
public static final int SpiralPicture
```

يُستخدم لعرض سلسلة تصل إلى خمس صور مع عناوين المستوى الأول المقابلة التي تتشكل حلزونيًا نحو المركز.

### SquareAccentList {#SquareAccentList}
```
public static final int SquareAccentList
```

يُستخدم لعرض قوائم معلومات مقسّمة إلى فئات. يظهر نص المستوى الثاني بجانب شكل مربع صغير. يعمل جيدًا مع كميات كبيرة من نص المستوى الثاني.

### StackedList {#StackedList}
```
public static final int StackedList
```

يُستخدم لعرض مجموعات معلومات أو خطوات في مهمة أو عملية أو سير عمل. تحتوي الأن shapes الدائرية على نص المستوى الأول، وتحتوي المستطيلات المقابلة على نص المستوى الثاني. يعمل جيدًا لتفاصيل عديدة ونص المستوى الأول القليل.

### StackedVenn {#StackedVenn}
```
public static final int StackedVenn
```

يُستخدم لعرض علاقات متداخلة. خيار جيد لتأكيد النمو أو التدرج. يعمل بشكل أفضل مع نص المستوى الأول فقط. تتطابق الأسطر السبعة الأولى من نص المستوى الأول مع شكل دائري. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### StaggeredProcess {#StaggeredProcess}
```
public static final int StaggeredProcess
```

يُستخدم لعرض تقدم تنازلي عبر المراحل. يتطابق كل من الأسطر الخمسة الأولى من نص المستوى الأول مع مستطيل. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### StepDownProcess {#StepDownProcess}
```
public static final int StepDownProcess
```

يُستخدم لعرض عملية تنازلية مع خطوات متعددة وخطوات فرعية. يعمل بشكل أفضل مع كميات قليلة من النص.

### StepUpProcess {#StepUpProcess}
```
public static final int StepUpProcess
```

يُستخدم لعرض سلسلة تصاعدية من الخطوات أو قوائم معلومات.

### SubStepProcess {#SubStepProcess}
```
public static final int SubStepProcess
```

يُستخدم لعرض عملية متعددة الخطوات مع خطوات فرعية بين كل نص من المستوى الأول. يعمل بشكل أفضل مع كمية قليلة من النص ويقتصر على سبعة خطوات من المستوى الأول. يمكن لكل خطوة من المستوى الأول أن تحتوي على عدد غير محدود من الخطوات الفرعية.

### TableHierarchy {#TableHierarchy}
```
public static final int TableHierarchy
```

يُستخدم لعرض مجموعات معلومات مبنية من الأعلى إلى الأسفل، والهرميات داخل كل مجموعة. لا يحتوي هذا التخطيط على خطوط وصل.

### TableList {#TableList}
```
public static final int TableList
```

يُستخدم لعرض معلومات مجمّعة أو ذات صلة ذات قيمة متساوية. يتطابق السطر الأول من نص المستوى الأول مع الشكل الأعلى وتُستخدم نصوص المستوى الثاني للقوائم اللاحقة.

### TargetList {#TargetList}
```
public static final int TargetList
```

يُستخدم لعرض معلومات مترابطة أو متداخلة. تظهر الأسطر السبعة الأولى من نص المستوى الأول في الشكل المستطيل. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط. يعمل جيدًا مع نصي المستوى الأول والمستوى الثاني معًا.

### TextCycle {#TextCycle}
```
public static final int TextCycle
```

يُستخدم لتمثيل تسلسل مستمر للمراحل أو المهام أو الأحداث في تدفق دائري. يبرز الأسهم أو التدفق بدلاً من المراحل أو الخطوات. يعمل بشكل أفضل مع نص المستوى الأول فقط.

### TitlePictureLineup {#TitlePictureLineup}
```
public static final int TitlePictureLineup
```

يُستخدم لعرض سلسلة من الصور التي لها كل منها عنوانها ووصفها. يظهر نص المستوى الأول في الصندوق أعلى الصورة. يظهر نص المستوى الثاني أسفل الصورة.

### TitledMatrix {#TitledMatrix}
```
public static final int TitledMatrix
```

يُستخدم لإظهار علاقات الأرباع الأربعة بالكل. يتطابق السطر الأول من نص المستوى الأول مع الشكل المركزي، وتظهر الأسطر الأربعة الأولى من نص المستوى الثاني في الأرباع. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### TitledPictureAccentList {#TitledPictureAccentList}
```
public static final int TitledPictureAccentList
```

يُستخدم لعرض قوائم معلومات مع صورة مميزة لكل نص من المستوى الثاني. يُعرض نص المستوى الأول في صندوق منفصل أعلى القائمة.

### TitledPictureBlocks {#TitledPictureBlocks}
```
public static final int TitledPictureBlocks
```

يُستخدم لعرض سلسلة من الصور. يظهر نص المستوى الأول فوق كل صورة. يظهر نص المستوى الثاني إلى الجانب ويتداخل قليلاً مع كل صورة.

### TrapezoidList {#TrapezoidList}
```
public static final int TrapezoidList
```

يُستخدم لعرض معلومات مجمّعة أو ذات صلة ذات قيمة متساوية. يعمل جيدًا مع كميات كبيرة من النص.

### UpwardArrow {#UpwardArrow}
```
public static final int UpwardArrow
```

يُستخدم لعرض تقدم أو خطوات تتجه صعودًا في مهمة أو عملية أو سير عمل. يتطابق كل من الأسطر الخمسة الأولى من نص المستوى الأول مع نقطة على السهم. يعمل بشكل أفضل مع نص قليل. لا يظهر النص غير المستخدم، لكنه يبقى متاحًا إذا تم تغيير التخطيط.

### VerticalAccentList {#VerticalAccentList}
```
public static final int VerticalAccentList
```

يُستخدم لعرض قوائم معلومات. يظهر نص المستوى الثاني في الأشكال المستطيلة فوق الشُعَاب العمودية. يبرز نص المستوى الثاني على نص المستوى الأول، وهو اختيار جيد لكميات متوسطة من نص المستوى الثاني.

### VerticalArrowList {#VerticalArrowList}
```
public static final int VerticalArrowList
```

يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل تتجه نحو هدف مشترك. يعمل جيدًا لقوائم معلومات نقطية.

### VerticalBendingProcess {#VerticalBendingProcess}
```
public static final int VerticalBendingProcess
```

يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. يضاعف مساحة العرض الأفقية والعمودية للأشكال. يضع مزيدًا من التركيز على العلاقات المتبادلة بين الأشكال بدلاً من الاتجاه أو الحركة.

### VerticalBlockList {#VerticalBlockList}
```
public static final int VerticalBlockList
```

يُستخدم لعرض مجموعات معلومات أو خطوات في مهمة أو عملية أو سير عمل. يعمل جيدًا مع كميات كبيرة من نص المستوى الثاني. خيار جيد للنص مع نقطة رئيسية وعدة نقاط فرعية.

### VerticalBoxList {#VerticalBoxList}
```
public static final int VerticalBoxList
```

يُستخدم لعرض عدة مجموعات من المعلومات، خصوصًا المجموعات التي تحتوي على كميات كبيرة من نص المستوى الثاني. خيار جيد لقوائم معلومات نقطية.

### VerticalBulletList {#VerticalBulletList}
```
public static final int VerticalBulletList
```

يُستخدم لعرض كتل معلومات غير متسلسلة أو مجمّعة. يعمل جيدًا للقوائم ذات عناوين طويلة أو معلومات المستوى الأعلى.

### VerticalChevronList {#VerticalChevronList}
```
public static final int VerticalChevronList
```

يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل؛ أو لتأكيد الحركة أو الاتجاه. يبرز نص المستوى الثاني على نص المستوى الأول، وهو اختيار جيد لكميات كبيرة من نص المستوى الثاني.

### VerticalCircleList {#VerticalCircleList}
```
public static final int VerticalCircleList
```

يُستخدم لعرض بيانات متسلسلة أو مُجمّعة. يعمل بشكل أفضل لنص المستوى الأول، الذي يُظهر بجوار شكل دائري كبير. تُفصل المستويات الأدنى من النص بأشكال دائرية أصغر.

### VerticalCurvedList {#VerticalCurvedList}
```
public static final int VerticalCurvedList
```

يُستخدم لعرض قائمة معلومات منحنية. لإضافة صور إلى أشكال الدائرة المميزة، طبّق تعبئة صورة.

### VerticalEquation {#VerticalEquation}
```
public static final int VerticalEquation
```

يُستخدم لعرض خطوات أو مهام متسلسلة تصف خطة أو نتيجة. يظهر السطر الأخير من نص المستوى الأول بعد السهم. يعمل بشكل أفضل مع نص المستوى الأول فقط.

### VerticalPictureAccentList {#VerticalPictureAccentList}
```
public static final int VerticalPictureAccentList
```

يُستخدم لعرض كتل معلومات غير متسلسلة أو مجمّعة. تم تصميم الدوائر الصغيرة لاحتواء الصور.

### VerticalPictureList {#VerticalPictureList}
```
public static final int VerticalPictureList
```

يُستخدم لعرض كتل معلومات غير متسلسلة أو مجمّعة. تم تصميم الأشكال الصغيرة على اليسار لاحتواء الصور.

### VerticalProcess {#VerticalProcess}
```
public static final int VerticalProcess
```

يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل من الأعلى إلى الأسفل. يعمل بشكل أفضل مع نص المستوى الأول، لأن المساحة العمودية محدودة.

### Custom {#Custom}
```
public static final int Custom
```

يمثل مخطط SmartArt مع قالب تخطيط مخصص

### PictureOrganizationChart {#PictureOrganizationChart}
```
public static final int PictureOrganizationChart
```

يُستخدم لعرض معلومات هرمية أو علاقات تقارير في منظمة، مع صور مقابلة. تتوفر الشكل المساعد وتخطيطات مخطط المنظمة المعلقة مع هذا التخطيط.