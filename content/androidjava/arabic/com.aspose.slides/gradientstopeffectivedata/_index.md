---
title: GradientStopEffectiveData
second_title: Aspose.Slides لأجهزة Android عبر مرجع API جافا
description: كائن غير قابل للتغيير يمثل نقطة التدرج.
type: docs
url: /ar/com.aspose.slides/gradientstopeffectivedata/
---
**الوراثة:**
java.lang.Object

**جميع الواجهات المنفذة:**
com.aspose.slides.IEffectiveData, [com.aspose.slides.IGradientStopEffectiveData](../../com.aspose.slides/igradientstopeffectivedata)
```
public class GradientStopEffectiveData implements IEffectiveData, IGradientStopEffectiveData
```

كائن غير قابل للتغيير يمثل نقطة التدرج.
## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getPosition()](#getPosition--) | يرجع موضع (0..1) نقطة التدرج. |
| [getColor()](#getColor--) | يرجع اللون لنقطة التدرج. |
| [equals(Object obj)](#equals-java.lang.Object-) | يحدد ما إذا كان [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) المحدد يساوي [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) الحالي. |
| [hashCode()](#hashCode--) |  |
### getPosition() {#getPosition--}
```
public final float getPosition()
```

يرجع موضع (0..1) نقطة التدرج. قراءة فقط float.

**الإرجاع:**
float
### getColor() {#getColor--}
```
public final Integer getColor()
```

يرجع لون نقطة التدرج. قراءة فقط java.lang.Integer.

**الإرجاع:**
java.lang.Integer
### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```

يحدد ما إذا كان [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) المحدد يساوي [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) الحالي.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| obj | java.lang.Object | الـ [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) للمقارنة. |

**الإرجاع:**
boolean - true إذا كانت الكائنات متساوية؛ وإلا false.
### hashCode() {#hashCode--}
```
public int hashCode()
```

**الإرجاع:**
int