---
title: Placeholder
second_title: Aspose.Slides لنظام Android عبر مرجع API لـ Java
description: يمثل عنصرًا نائبًا على شريحة.
type: docs
url: /ar/com.aspose.slides/placeholder/
---
**الوراثة:**
java.lang.Object

**كل الواجهات المُطبقة:**
[com.aspose.slides.IPlaceholder](../../com.aspose.slides/iplaceholder)
```
public class Placeholder implements IPlaceholder
```

يمثل عنصرًا نائبًا على شريحة.
## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getOrientation()](#getOrientation--) | يحصل أو يحدد اتجاه العنصر النائب. |
| [getSize()](#getSize--) | يحصل أو يحدد حجم العنصر النائب. |
| [getType()](#getType--) | يحصل أو يحدد نوع العنصر النائب. |
| [getIndex()](#getIndex--) | يحصل أو يحدد فهرس العنصر النائب. |
### getOrientation() {#getOrientation--}
```
public byte getOrientation()
```

يحصل أو يحدد اتجاه العنصر النائب. للقراءة فقط [Orientation](../../com.aspose.slides/orientation).

**الإرجاع:**
byte
### getSize() {#getSize--}
```
public byte getSize()
```

يحصل أو يحدد حجم العنصر النائب. للقراءة فقط [PlaceholderSize](../../com.aspose.slides/placeholdersize).

**الإرجاع:**
byte
### getType() {#getType--}
```
public byte getType()
```

يحصل أو يحدد نوع العنصر النائب. للقراءة فقط [PlaceholderType](../../com.aspose.slides/placeholdertype).

**الإرجاع:**
byte
### getIndex() {#getIndex--}
```
public long getIndex()
```

يحصل أو يحدد فهرس العنصر النائب. للقراءة فقط long.

**الإرجاع:**
long