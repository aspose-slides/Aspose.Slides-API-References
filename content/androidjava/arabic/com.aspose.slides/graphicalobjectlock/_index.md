---
title: GraphicalObjectLock
second_title: Aspose.Slides لنظام Android عبر مرجع API لجافا
description: يحدد أي العمليات معطلة على الكائن الأصلي GraphicalObject.
type: docs
url: /ar/com.aspose.slides/graphicalobjectlock/
---
**الوراثة:**
java.lang.Object, [com.aspose.slides.BaseShapeLock](../../com.aspose.slides/baseshapelock)

**جميع الواجهات المنفذة:**
[com.aspose.slides.IGraphicalObjectLock](../../com.aspose.slides/igraphicalobjectlock)
```
public class GraphicalObjectLock extends BaseShapeLock implements IGraphicalObjectLock
```

يحدد أي العمليات معطلة على كائن GraphicalObject الأصلي.
## الطرق

| Method | Description |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | يحدد ما إذا كان إضافة هذا الشكل إلى مجموعة محظوراً. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | يحدد ما إذا كان إضافة هذا الشكل إلى مجموعة محظوراً. |
| [getDrilldownLocked()](#getDrilldownLocked--) | يحدد ما إذا كان اختيار الأشكال الفرعية لهذا الكائن محظوراً. |
| [setDrilldownLocked(boolean value)](#setDrilldownLocked-boolean-) | يحدد ما إذا كان اختيار الأشكال الفرعية لهذا الكائن محظوراً. |
| [getSelectLocked()](#getSelectLocked--) | يحدد ما إذا كان اختيار هذا الشكل محظوراً. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | يحدد ما إذا كان اختيار هذا الشكل محظوراً. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | يحدد ما إذا كان الشكل يجب أن يحافظ على نسبة العرض إلى الارتفاع عند تغيير الحجم. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | يحدد ما إذا كان الشكل يجب أن يحافظ على نسبة العرض إلى الارتفاع عند تغيير الحجم. |
| [getPositionLocked()](#getPositionLocked--) | يحدد ما إذا كان نقل هذا الشكل محظوراً. |
| [setPositionLocked(boolean value)](#setPositionLocked-boolean-) | يحدد ما إذا كان نقل هذا الشكل محظوراً. |
| [getSizeLocked()](#getSizeLocked--) | يحدد ما إذا كان تغيير حجم هذا الشكل محظوراً. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | يحدد ما إذا كان تغيير حجم هذا الشكل محظوراً. |
### getGroupingLocked() {#getGroupingLocked--}
```
public boolean getGroupingLocked()
```

يحدد ما إذا كان إضافة هذا الشكل إلى مجموعة محظوراً. قراءة/كتابة boolean.

**القيمة المرجعة:**
boolean
### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public void setGroupingLocked(boolean value)
```

يحدد ما إذا كان إضافة هذا الشكل إلى مجموعة محظوراً. قراءة/كتابة boolean.

**المعلمات:**
| المعلمة | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getDrilldownLocked() {#getDrilldownLocked--}
```
public boolean getDrilldownLocked()
```

يحدد ما إذا كان اختيار الأشكال الفرعية لهذا الكائن محظوراً. قراءة/كتابة boolean.

**القيمة المرجعة:**
boolean
### setDrilldownLocked(boolean value) {#setDrilldownLocked-boolean-}
```
public void setDrilldownLocked(boolean value)
```

يحدد ما إذا كان اختيار الأشكال الفرعية لهذا الكائن محظوراً. قراءة/كتابة boolean.

**المعلمات:**
| المعلمة | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getSelectLocked() {#getSelectLocked--}
```
public boolean getSelectLocked()
```

يحدد ما إذا كان اختيار هذا الشكل محظوراً. قراءة/كتابة boolean.

**القيمة المرجعة:**
boolean
### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public void setSelectLocked(boolean value)
```

يحدد ما إذا كان اختيار هذا الشكل محظوراً. قراءة/كتابة boolean.

**المعلمات:**
| المعلمة | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public boolean getAspectRatioLocked()
```

يحدد ما إذا كان الشكل يجب أن يحافظ على نسبة العرض إلى الارتفاع عند تغيير الحجم. قراءة/كتابة boolean.

**القيمة المرجعة:**
boolean
### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public void setAspectRatioLocked(boolean value)
```

يحدد ما إذا كان الشكل يجب أن يحافظ على نسبة العرض إلى الارتفاع عند تغيير الحجم. قراءة/كتابة boolean.

**المعلمات:**
| المعلمة | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getPositionLocked() {#getPositionLocked--}
```
public boolean getPositionLocked()
```

يحدد ما إذا كان نقل هذا الشكل محظوراً. قراءة/كتابة boolean.

**القيمة المرجعة:**
boolean
### setPositionLocked(boolean value) {#setPositionLocked-boolean-}
```
public void setPositionLocked(boolean value)
```

يحدد ما إذا كان نقل هذا الشكل محظوراً. قراءة/كتابة boolean.

**المعلمات:**
| المعلمة | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getSizeLocked() {#getSizeLocked--}
```
public boolean getSizeLocked()
```

يحدد ما إذا كان تغيير حجم هذا الشكل محظوراً. قراءة/كتابة boolean.

**القيمة المرجعة:**
boolean
### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public void setSizeLocked(boolean value)
```

يحدد ما إذا كان تغيير حجم هذا الشكل محظوراً. قراءة/كتابة boolean.

**المعلمات:**
| المعلمة | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |