---
title: BulletFormat
second_title: Aspose.Slides لنظام Android عبر مرجع API جافا
description: يمثل خصائص تنسيق تعداد الفقرات.
type: docs
url: /ar/com.aspose.slides/bulletformat/
---
**الوراثة:**
java.lang.Object, [com.aspose.slides.PVIObject](../../com.aspose.slides/pviobject)

**جميع الواجهات المُطبقة:**
[com.aspose.slides.IBulletFormat](../../com.aspose.slides/ibulletformat)
```
public final class BulletFormat extends PVIObject implements IBulletFormat
```

يمثل خصائص تنسيق تعداد الفقرات.
## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getType()](#getType--) | تُعيد أو تُعيِّن نوع التعداد لفقرة لا وراثة لها. |
| [setType(byte value)](#setType-byte-) | تُعيد أو تُعيِّن نوع التعداد لفقرة لا وراثة لها. |
| [getChar()](#getChar--) | تُعيد أو تُعيِّن حرف التعداد لفقرة لا وراثة لها. |
| [setChar(char value)](#setChar-char-) | تُعيد أو تُعيِّن حرف التعداد لفقرة لا وراثة لها. |
| [getFont()](#getFont--) | تُعيد أو تُعيِّن خط التعداد لفقرة لا وراثة لها. |
| [setFont(IFontData value)](#setFont-com.aspose.slides.IFontData-) | تُعيد أو تُعيِّن خط التعداد لفقرة لا وراثة لها. |
| [getHeight()](#getHeight--) | تُعيد أو تُعيِّن ارتفاع التعداد لفقرة لا وراثة لها. |
| [setHeight(float value)](#setHeight-float-) | تُعيد أو تُعيِّن ارتفاع التعداد لفقرة لا وراثة لها. |
| [getColor()](#getColor--) | تُعيد تنسيق اللون لتعداد فقرة لا وراثة لها. |
| [getNumberedBulletStartWith()](#getNumberedBulletStartWith--) | تُعيد أو تُعيِّن الرقم الأول المستخدم لمجموعة تعداد مرقّم لا وراثة لها. |
| [setNumberedBulletStartWith(short value)](#setNumberedBulletStartWith-short-) | تُعيد أو تُعيِّن الرقم الأول المستخدم لمجموعة تعداد مرقّم لا وراثة لها. |
| [getNumberedBulletStyle()](#getNumberedBulletStyle--) | تُعيد أو تُعيِّن نمط تعداد مرقّم لا وراثة لها. |
| [setNumberedBulletStyle(byte value)](#setNumberedBulletStyle-byte-) | تُعيد أو تُعيِّن نمط تعداد مرقّم لا وراثة لها. |
| [isBulletHardColor()](#isBulletHardColor--) | يحدّد ما إذا كان للتعداد لون خاص به أو يرثه من الجزء الأول في الفقرة. |
| [setBulletHardColor(byte value)](#setBulletHardColor-byte-) | يحدّد ما إذا كان للتعداد لون خاص به أو يرثه من الجزء الأول في الفقرة. |
| [isBulletHardFont()](#isBulletHardFont--) | يحدّد ما إذا كان للتعداد خط خاص به أو يرثه من الجزء الأول في الفقرة. |
| [setBulletHardFont(byte value)](#setBulletHardFont-byte-) | يحدّد ما إذا كان للتعداد خط خاص به أو يرثه من الجزء الأول في الفقرة. |
| [getPicture()](#getPicture--) | تُعيد الصورة المستخدمة كتعداد في فقرة لا وراثة لها. |
| [applyDefaultParagraphIndentsShifts()](#applyDefaultParagraphIndentsShifts--) | يضبط الإزاحات غير الصفرية الافتراضية للمسافة البادئة الفعّالة للفقرة والهوامش اليسرى عندما يكون التعداد مفعَّلاً (كما في PowerPoint عند تفعيل تعداد الفقرة/الترقيم). |
| [getEffective()](#getEffective--) | يحصل على بيانات تنسيق التعداد الفعّال مع تطبيق الوراثة. |
| [getVersion()](#getVersion--) |  |
### getType() {#getType--}
```
public final byte getType()
```


تُعيد أو تُعيِّن نوع التعداد لفقرة لا وراثة لها. قراءة/كتابة [BulletType](../../com.aspose.slides/bullettype).

**الإرجاع:**
byte
### setType(byte value) {#setType-byte-}
```
public final void setType(byte value)
```


تُعيد أو تُعيِّن نوع التعداد لفقرة لا وراثة لها. قراءة/كتابة [BulletType](../../com.aspose.slides/bullettype).

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | byte |  |

### getChar() {#getChar--}
```
public final char getChar()
```


تُعيد أو تُعيِّن حرف التعداد لفقرة لا وراثة لها. قراءة/كتابة  char .

**الإرجاع:**
char
### setChar(char value) {#setChar-char-}
```
public final void setChar(char value)
```


تُعيد أو تُعيِّن حرف التعداد لفقرة لا وراثة لها. قراءة/كتابة  char .

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | char |  |

### getFont() {#getFont--}
```
public final IFontData getFont()
```


تُعيد أو تُعيِّن خط التعداد لفقرة لا وراثة لها. قراءة/كتابة [IFontData](../../com.aspose.slides/ifontdata).

**الإرجاع:**
[IFontData](../../com.aspose.slides/ifontdata)
### setFont(IFontData value) {#setFont-com.aspose.slides.IFontData-}
```
public final void setFont(IFontData value)
```


تُعيد أو تُعيِّن خط التعداد لفقرة لا وراثة لها. قراءة/كتابة [IFontData](../../com.aspose.slides/ifontdata).

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | [IFontData](../../com.aspose.slides/ifontdata) |  |

### getHeight() {#getHeight--}
```
public final float getHeight()
```


تُعيد أو تُعيِّن ارتفاع التعداد لفقرة لا وراثة لها. القيمة Float.NaN تعني أن التعداد يرث الارتفاع من الجزء الأول في الفقرة. قراءة/كتابة  float .

--------------------

القيمة السالبة للارتفاع تعني أن الارتفاع يُعطى بالنقاط والقيمة الإيجابية تعني أن الارتفاع نسبة مئوية من النص المحيط.

**الإرجاع:**
float
### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```


تُعيد أو تُعيِّن ارتفاع التعداد لفقرة لا وراثة لها. القيمة Float.NaN تعني أن التعداد يرث الارتفاع من الجزء الأول في الفقرة. قراءة/كتابة  float .

--------------------

القيمة السالبة للارتفاع تعني أن الارتفاع يُعطى بالنقاط والقيمة الإيجابية تعني أن الارتفاع نسبة مئوية من النص المحيط.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getColor() {#getColor--}
```
public final IColorFormat getColor()
```


تُعيد تنسيق اللون لتعداد فقرة لا وراثة لها. للقراءة فقط [IColorFormat](../../com.aspose.slides/icolorformat).

**الإرجاع:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getNumberedBulletStartWith() {#getNumberedBulletStartWith--}
```
public final short getNumberedBulletStartWith()
```


تُعيد أو تُعيِّن الرقم الأول المستخدم لمجموعة تعداد مرقّم لا وراثة لها. قراءة/كتابة  short .

**الإرجاع:**
short
### setNumberedBulletStartWith(short value) {#setNumberedBulletStartWith-short-}
```
public final void setNumberedBulletStartWith(short value)
```


تُعيد أو تُعيِّن الرقم الأول المستخدم لمجموعة تعداد مرقّم لا وراثة لها. قراءة/كتابة  short .

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | short |  |

### getNumberedBulletStyle() {#getNumberedBulletStyle--}
```
public final byte getNumberedBulletStyle()
```


تُعيد أو تُعيِّن نمط تعداد مرقّم لا وراثة لها. قراءة/كتابة [NumberedBulletStyle](../../com.aspose.slides/numberedbulletstyle).

**الإرجاع:**
byte
### setNumberedBulletStyle(byte value) {#setNumberedBulletStyle-byte-}
```
public final void setNumberedBulletStyle(byte value)
```


تُعيد أو تُعيِّن نمط تعداد مرقّم لا وراثة لها. قراءة/كتابة [NumberedBulletStyle](../../com.aspose.slides/numberedbulletstyle).

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | byte |  |

### isBulletHardColor() {#isBulletHardColor--}
```
public final byte isBulletHardColor()
```


يحدّد ما إذا كان للتعداد لون خاص به أو يرثه من الجزء الأول في الفقرة. **NullableBool.True** إذا كان للتعداد لون خاص و **NullableBool.False** إذا كان يرث اللون من الجزء الأول في الفقرة. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**الإرجاع:**
byte
### setBulletHardColor(byte value) {#setBulletHardColor-byte-}
```
public final void setBulletHardColor(byte value)
```


يحدّد ما إذا كان للتعداد لون خاص به أو يرثه من الجزء الأول في الفقرة. **NullableBool.True** إذا كان للتعداد لون خاص و **NullableBool.False** إذا كان يرث اللون من الجزء الأول في الفقرة. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | byte |  |

### isBulletHardFont() {#isBulletHardFont--}
```
public final byte isBulletHardFont()
```


يحدّد ما إذا كان للتعداد خط خاص به أو يرثه من الجزء الأول في الفقرة. **NullableBool.True** إذا كان للتعداد خط خاص و **NullableBool.False** إذا كان يرث الخط من الجزء الأول في الفقرة. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**الإرجاع:**
byte
### setBulletHardFont(byte value) {#setBulletHardFont-byte-}
```
public final void setBulletHardFont(byte value)
```


يحدّد ما إذا كان للتعداد خط خاص به أو يرثه من الجزء الأول في الفقرة. **NullableBool.True** إذا كان للتعداد خط خاص و **NullableBool.False** إذا كان يرث الخط من الجزء الأول في الفقرة. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | byte |  |

### getPicture() {#getPicture--}
```
public final ISlidesPicture getPicture()
```


تُعيد الصورة المستخدمة كتعداد في فقرة لا وراثة لها. للقراءة فقط [ISlidesPicture](../../com.aspose.slides/islidespicture).

**الإرجاع:**
[ISlidesPicture](../../com.aspose.slides/islidespicture)
### applyDefaultParagraphIndentsShifts() {#applyDefaultParagraphIndentsShifts--}
```
public final void applyDefaultParagraphIndentsShifts()
```


يضبط الإزاحات غير الصفرية الافتراضية للمسافة البادئة الفعّالة للفقرة والهوامش اليسرى عندما يكون التعداد مفعَّلاً (كما في PowerPoint عند تفعيل تعداد الفقرة/الت

رقيم). إذا تم تعطيل التعداد فسيتم فقط إعادة تعيين المسافة البادئة والهوامش اليسرى للفقرة (كما في PowerPoint عند تعطيل تعداد الفقرة/الت

رقيم). تُطبق إزاحات الفراغات بالنسبة إلى سياق التعداد الحالي — IBulletFormat.Type، .NumberedBulletStyle و FontHeight للجزء الأول. تُطبق الإزاحات غير الصفرية على المسافة البادئة والهوامش اليسرى الفعّالة للفقرة الحالية (لتصبح القيم النتيجة قيمًا محلية).

### getEffective() {#getEffective--}
```
public final IBulletFormatEffectiveData getEffective()
```


يحصل على بيانات تنسيق التعداد الفعّال مع تطبيق الوراثة.

--------------------

> ```
> This example demonstrates getting some effective bullet format properties.
>  
>  Presentation pres = new Presentation("MyPresentation.pptx");
>  try {
>      IAutoShape shape = (IAutoShape)pres.getSlides().get_Item(0).getShapes().get_Item(0);
>      IBulletFormatEffectiveData effectiveBulletFormat = shape.getTextFrame().getParagraphs().get_Item(0).getParagraphFormat().getBullet().getEffective();
>      System.out.println("Bullet type: " + effectiveBulletFormat.getType());
>      if (effectiveBulletFormat.getType() == BulletType.Numbered)
>      {
>          System.out.println("Numbered style: " + effectiveBulletFormat.getNumberedBulletStyle());
>          System.out.println("Starting number: " + effectiveBulletFormat.getNumberedBulletStartWith());
>      }
>  } finally {
>      if (pres != null) pres.dispose();
>  }
> ```


**الإرجاع:**
[IBulletFormatEffectiveData](../../com.aspose.slides/ibulletformateffectivedata) - A [IBulletFormatEffectiveData](../../com.aspose.slides/ibulletformateffectivedata).
### getVersion() {#getVersion--}
```
public long getVersion()
```


الإصدار. للقراءة فقط long.

**الإرجاع:**
long