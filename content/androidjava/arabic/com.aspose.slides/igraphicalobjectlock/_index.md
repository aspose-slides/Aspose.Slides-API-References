---
title: IGraphicalObjectLock
second_title: Aspose.Slides لنظام Android عبر مرجع API لجافا
description: يحدد العمليات التي تم تعطيلها على كائن GraphicalObject الأب.
type: docs
url: /ar/com.aspose.slides/igraphicalobjectlock/
---
**جميع الواجهات المنفذة:**
[com.aspose.slides.IBaseShapeLock](../../com.aspose.slides/ibaseshapelock)
```
public interface IGraphicalObjectLock extends IBaseShapeLock
```

يحدد العمليات التي تم تعطيلها على كائن GraphicalObject الأب.
## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | يحدد ما إذا كان إضافة هذا الشكل إلى مجموعة محظورًا. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | يحدد ما إذا كان إضافة هذا الشكل إلى مجموعة محظورًا. |
| [getDrilldownLocked()](#getDrilldownLocked--) | يحدد ما إذا كان اختيار الأشكال الفرعية لهذا الكائن محظورًا. |
| [setDrilldownLocked(boolean value)](#setDrilldownLocked-boolean-) | يحدد ما إذا كان اختيار الأشكال الفرعية لهذا الكائن محظورًا. |
| [getSelectLocked()](#getSelectLocked--) | يحدد ما إذا كان اختيار هذا الشكل محظورًا. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | يحدد ما إذا كان اختيار هذا الشكل محظورًا. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | يحدد ما إذا كان يجب على الشكل الحفاظ على نسبة العرض إلى الارتفاع عند تغيير الحجم. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | يحدد ما إذا كان يجب على الشكل الحفاظ على نسبة العرض إلى الارتفاع عند تغيير الحجم. |
| [getPositionLocked()](#getPositionLocked--) | يحدد ما إذا كان نقل هذا الشكل محظورًا. |
| [setPositionLocked(boolean value)](#setPositionLocked-boolean-) | يحدد ما إذا كان نقل هذا الشكل محظورًا. |
| [getSizeLocked()](#getSizeLocked--) | يحدد ما إذا كان تغيير حجم هذا الشكل محظورًا. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | يحدد ما إذا كان تغيير حجم هذا الشكل محظورًا. |
### getGroupingLocked() {#getGroupingLocked--}
```
public abstract boolean getGroupingLocked()
```

يحدد ما إذا كان إضافة هذا الشكل إلى مجموعة محظورًا. قراءة/كتابة boolean.

**الإرجاع:**
boolean
### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public abstract void setGroupingLocked(boolean value)
```

يحدد ما إذا كان إضافة هذا الشكل إلى مجموعة محظورًا. قراءة/كتابة boolean.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getDrilldownLocked() {#getDrilldownLocked--}
```
public abstract boolean getDrilldownLocked()
```

يحدد ما إذا كان اختيار الأشكال الفرعية لهذا الكائن محظورًا. قراءة/كتابة boolean.

**الإرجاع:**
boolean
### setDrilldownLocked(boolean value) {#setDrilldownLocked-boolean-}
```
public abstract void setDrilldownLocked(boolean value)
```

يحدد ما إذا كان اختيار الأشكال الفرعية لهذا الكائن محظورًا. قراءة/كتابة boolean.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getSelectLocked() {#getSelectLocked--}
```
public abstract boolean getSelectLocked()
```

يحدد ما إذا كان اختيار هذا الشكل محظورًا. قراءة/كتابة boolean.

**الإرجاع:**
boolean
### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public abstract void setSelectLocked(boolean value)
```

يحدد ما إذا كان اختيار هذا الشكل محظورًا. قراءة/كتابة boolean.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public abstract boolean getAspectRatioLocked()
```

يحدد ما إذا كان يجب على الشكل الحفاظ على نسبة العرض إلى الارتفاع عند تغيير الحجم. قراءة/كتابة boolean.

**الإرجاع:**
boolean
### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public abstract void setAspectRatioLocked(boolean value)
```

يحدد ما إذا كان يجب على الشكل الحفاظ على نسبة العرض إلى الارتفاع عند تغيير الحجم. قراءة/كتابة boolean.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getPositionLocked() {#getPositionLocked--}
```
public abstract boolean getPositionLocked()
```

يحدد ما إذا كان نقل هذا الشكل محظورًا. قراءة/كتابة boolean.

**الإرجاع:**
boolean
### setPositionLocked(boolean value) {#setPositionLocked-boolean-}
```
public abstract void setPositionLocked(boolean value)
```

يحدد ما إذا كان نقل هذا الشكل محظورًا. قراءة/كتابة boolean.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getSizeLocked() {#getSizeLocked--}
```
public abstract boolean getSizeLocked()
```

يحدد ما إذا كان تغيير حجم هذا الشكل محظورًا. قراءة/كتابة boolean.

**الإرجاع:**
boolean
### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public abstract void setSizeLocked(boolean value)
```

يحدد ما إذا كان تغيير حجم هذا الشكل محظورًا. قراءة/كتابة boolean.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |