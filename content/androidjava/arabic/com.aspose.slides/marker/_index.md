---
title: Marker
second_title: Aspose.Slides لنظام Android عبر مرجع Java API
description: يمثل علامة من نوع chert.
type: docs
url: /ar/com.aspose.slides/marker/
---
**الوراثة:**
java.lang.Object

**جميع الواجهات التي تم تنفيذها:**
[com.aspose.slides.IMarker](../../com.aspose.slides/imarker), com.aspose.slides.IDOMObject
```
public class Marker implements IMarker, IDOMObject
```

يمثل علامة من نوع chert.
## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getSymbol()](#getSymbol--) | يمثل نمط العلامة في مخطط خطي أو مخطط مبعثر أو مخطط رادار. |
| [setSymbol(int value)](#setSymbol-int-) | يمثل نمط العلامة في مخطط خطي أو مخطط مبعثر أو مخطط رادار. |
| [getFormat()](#getFormat--) | الحصول على أو ضبط تعبئة العلامة. |
| [getSize()](#getSize--) | يمثل حجم العلامة في مخطط خطي أو مخطط مبعثر أو مخطط رادار. |
| [setSize(int value)](#setSize-int-) | يمثل حجم العلامة في مخطط خطي أو مخطط مبعثر أو مخطط رادار. |
| [getParent_Immediate()](#getParent-Immediate--) |  |

### getSymbol() {#getSymbol--}
```
public final int getSymbol()
```

يمثل نمط العلامة في مخطط خطي أو مخطط مبعثر أو مخطط رادار. قراءة/كتابة [MarkerStyleType](../../com.aspose.slides/markerstyletype).

**الإرجاع:**
int

### setSymbol(int value) {#setSymbol-int-}
```
public final void setSymbol(int value)
```

يمثل نمط العلامة في مخطط خطي أو مخطط مبعثر أو مخطط رادار. قراءة/كتابة [MarkerStyleType](../../com.aspose.slides/markerstyletype).

**المعلمات:**
| المعلمة | النوع | الوصف |
| --- | --- | --- |
| value | int |  |

### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

الحصول على أو ضبط تعبئة العلامة. قراءة فقط [IFormat](../../com.aspose.slides/iformat).

**الإرجاع:**
[IFormat](../../com.aspose.slides/iformat)

### getSize() {#getSize--}
```
public final int getSize()
```

يمثل حجم العلامة في مخطط خطي أو مخطط مبعثر أو مخطط رادار. قراءة/كتابة int.

**الإرجاع:**
int

### setSize(int value) {#setSize-int-}
```
public final void setSize(int value)
```

يمثل حجم العلامة في مخطط خطي أو مخطط مبعثر أو مخطط رادار. قراءة/كتابة int.

**المعلمات:**
| المعلمة | النوع | الوصف |
| --- | --- | --- |
| value | int |  |

### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

يرجع كائن Parent_Immediate. قراءة فقط IDOMObject.

**الإرجاع:**
com.aspose.slides.IDOMObject