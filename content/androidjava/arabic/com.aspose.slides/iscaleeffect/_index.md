---
title: IScaleEffect
second_title: Aspose.Slides لنظام Android عبر مرجع API جافا
description: يمثل تأثير مقياس الرسوم المتحركة.
type: docs
url: /ar/com.aspose.slides/iscaleeffect/
---
**جميع الواجهات المنفذة:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IScaleEffect extends IBehavior
```

يمثل تأثير مقياس الرسوم المتحركة.
## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | يحدد ما إذا كان يجب تكبير المحتوى. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | يحدد ما إذا كان يجب تكبير المحتوى. |
| [getFrom()](#getFrom--) | يحدد إحداثيات x/y لبدء الرسوم المتحركة من (بالنسب المئوية). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | يحدد إحداثيات x/y لبدء الرسوم المتحركة من (بالنسب المئوية). |
| [getTo()](#getTo--) | يحدد الموقع المستهدف لتأثير مقياس الرسوم المتحركة (بالنسب المئوية). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | يحدد الموقع المستهدف لتأثير مقياس الرسوم المتحركة (بالنسب المئوية). |
| [getBy()](#getBy--) | يصف قيمة الإزاحة النسبية للرسوم المتحركة (بالنسب المئوية). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | يصف قيمة الإزاحة النسبية للرسوم المتحركة (بالنسب المئوية). |

### getZoomContent() {#getZoomContent--}
```
public abstract byte getZoomContent()
```

يحدد ما إذا كان يجب تكبير المحتوى. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**القيمة المرجعة:**
byte

### setZoomContent(byte value) {#setZoomContent-byte-}
```
public abstract void setZoomContent(byte value)
```

يحدد ما إذا كان يجب تكبير المحتوى. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public abstract PointF getFrom()
```

يحدد إحداثيات x/y لبدء الرسوم المتحركة من (بالنسب المئوية). قراءة/كتابة android.graphics.PointF.

**القيمة المرجعة:**
android.graphics.PointF

### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public abstract void setFrom(PointF value)
```

يحدد إحداثيات x/y لبدء الرسوم المتحركة من (بالنسب المئوية). قراءة/كتابة android.graphics.PointF.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public abstract PointF getTo()
```

يحدد الموقع المستهدف لتأثير مقياس الرسوم المتحركة (بالنسب المئوية). قراءة/كتابة android.graphics.PointF.

**القيمة المرجعة:**
android.graphics.PointF

### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public abstract void setTo(PointF value)
```

يحدد الموقع المستهدف لتأثير مقياس الرسوم المتحركة (بالنسب المئوية). قراءة/كتابة android.graphics.PointF.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public abstract PointF getBy()
```

يصف قيمة الإزاحة النسبية للرسوم المتحركة (بالنسب المئوية). قراءة/كتابة android.graphics.PointF.

**القيمة المرجعة:**
android.graphics.PointF

### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public abstract void setBy(PointF value)
```

يصف قيمة الإزاحة النسبية للرسوم المتحركة (بالنسب المئوية). قراءة/كتابة android.graphics.PointF.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | android.graphics.PointF |  |