---
title: AutoShapeLock
second_title: Aspose.Slides لنظام Android عبر مرجع واجهة برمجة تطبيقات Java
description: يحدد أي العمليات معطلة على الـ AutoshapeEx الأب.
type: docs
url: /ar/com.aspose.slides/autoshapelock/
---
**الوراثة:**
java.lang.Object, [com.aspose.slides.BaseShapeLock](../../com.aspose.slides/baseshapelock)

**جميع الواجهات المنفذة:**
[com.aspose.slides.IAutoShapeLock](../../com.aspose.slides/iautoshapelock)
```
public class AutoShapeLock extends BaseShapeLock implements IAutoShapeLock
```

يحدد أي العمليات معطلة على الـ AutoshapeEx الأب.

## الأساليب

| الطريقة | الوصف |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | يحدد ما إذا كان إضافة هذا الشكل إلى مجموعة ممنوعًا. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | يحدد ما إذا كان إضافة هذا الشكل إلى مجموعة ممنوعًا. |
| [getSelectLocked()](#getSelectLocked--) | يحدد ما إذا كان اختيار هذا الشكل ممنوعًا. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | يحدد ما إذا كان اختيار هذا الشكل ممنوعًا. |
| [getRotateLocked()](#getRotateLocked--) | يحدد ما إذا كان تغيير زاوية الدوران لهذا الشكل ممنوعًا. |
| [setRotateLocked(boolean value)](#setRotateLocked-boolean-) | يحدد ما إذا كان تغيير زاوية الدوران لهذا الشكل ممنوعًا. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | يحدد ما إذا كان يجب على الشكل الحفاظ على نسبة العرض إلى الارتفاع عند تغيير الحجم. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | يحدد ما إذا كان يجب على الشكل الحفاظ على نسبة العرض إلى الارتفاع عند تغيير الحجم. |
| [getPositionLocked()](#getPositionLocked--) | يحدد ما إذا كان نقل هذا الشكل ممنوعًا. |
| [setPositionLocked(boolean value)](#setPositionLocked-boolean-) | يحدد ما إذا كان نقل هذا الشكل ممنوعًا. |
| [getSizeLocked()](#getSizeLocked--) | يحدد ما إذا كان تعديل حجم هذا الشكل ممنوعًا. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | يحدد ما إذا كان تعديل حجم هذا الشكل ممنوعًا. |
| [getEditPointsLocked()](#getEditPointsLocked--) | يحدد ما إذا كان تعديل مباشر للحدود الخارجية لهذا الشكل ممنوعًا. |
| [setEditPointsLocked(boolean value)](#setEditPointsLocked-boolean-) | يحدد ما إذا كان تعديل مباشر للحدود الخارجية لهذا الشكل ممنوعًا. |
| [getAdjustHandlesLocked()](#getAdjustHandlesLocked--) | يحدد ما إذا كان تعديل قيم الضبط ممنوعًا. |
| [setAdjustHandlesLocked(boolean value)](#setAdjustHandlesLocked-boolean-) | يحدد ما إذا كان تعديل قيم الضبط ممنوعًا. |
| [getArrowheadsLocked()](#getArrowheadsLocked--) | يحدد ما إذا كان تعديل رؤوس السهام ممنوعًا. |
| [setArrowheadsLocked(boolean value)](#setArrowheadsLocked-boolean-) | يحدد ما إذا كان تعديل رؤوس السهام ممنوعًا. |
| [getShapeTypeLocked()](#getShapeTypeLocked--) | يحدد ما إذا كان تغيير نوع الشكل ممنوعًا. |
| [setShapeTypeLocked(boolean value)](#setShapeTypeLocked-boolean-) | يحدد ما إذا كان تغيير نوع الشكل ممنوعًا. |
| [getTextLocked()](#getTextLocked--) | يحدد ما إذا كان تحرير النص ممنوعًا. |
| [setTextLocked(boolean value)](#setTextLocked-boolean-) | يحدد ما إذا كان تحرير النص ممنوعًا. |

### getGroupingLocked() {#getGroupingLocked--}
```
public boolean getGroupingLocked()
```

يحدد ما إذا كان إضافة هذا الشكل إلى مجموعة ممنوعًا. منطقية قابلة للقراءة والكتابة.

**الإرجاع:**
boolean

### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public void setGroupingLocked(boolean value)
```

يحدد ما إذا كان إضافة هذا الشكل إلى مجموعة ممنوعًا. منطقية قابلة للقراءة والكتابة.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getSelectLocked() {#getSelectLocked--}
```
public boolean getSelectLocked()
```

يحدد ما إذا كان اختيار هذا الشكل ممنوعًا. منطقية قابلة للقراءة والكتابة.

**الإرجاع:**
boolean

### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public void setSelectLocked(boolean value)
```

يحدد ما إذا كان اختيار هذا الشكل ممنوعًا. منطقية قابلة للقراءة والكتابة.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getRotateLocked() {#getRotateLocked--}
```
public boolean getRotateLocked()
```

يحدد ما إذا كان تغيير زاوية الدوران لهذا الشكل ممنوعًا. منطقية قابلة للقراءة والكتابة.

**الإرجاع:**
boolean

### setRotateLocked(boolean value) {#setRotateLocked-boolean-}
```
public void setRotateLocked(boolean value)
```

يحدد ما إذا كان تغيير زاوية الدوران لهذا الشكل ممنوعًا. منطقية قابلة للقراءة والكتابة.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public boolean getAspectRatioLocked()
```

يحدد ما إذا كان يجب على الشكل الحفاظ على نسبة العرض إلى الارتفاع عند تغيير الحجم. منطقية قابلة للقراءة والكتابة.

**الإرجاع:**
boolean

### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public void setAspectRatioLocked(boolean value)
```

يحدد ما إذا كان يجب على الشكل الحفاظ على نسبة العرض إلى الارتفاع عند تغيير الحجم. منطقية قابلة للقراءة والكتابة.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getPositionLocked() {#getPositionLocked--}
```
public boolean getPositionLocked()
```

يحدد ما إذا كان نقل هذا الشكل ممنوعًا. منطقية قابلة للقراءة والكتابة.

**الإرجاع:**
boolean

### setPositionLocked(boolean value) {#setPositionLocked-boolean-}
```
public void setPositionLocked(boolean value)
```

يحدد ما إذا كان نقل هذا الشكل ممنوعًا. منطقية قابلة للقراءة والكتابة.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getSizeLocked() {#getSizeLocked--}
```
public boolean getSizeLocked()
```

يحدد ما إذا كان تعديل حجم هذا الشكل ممنوعًا. منطقية قابلة للقراءة والكتابة.

**الإرجاع:**
boolean

### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public void setSizeLocked(boolean value)
```

يحدد ما إذا كان تعديل حجم هذا الشكل ممنوعًا. منطقية قابلة للقراءة والكتابة.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getEditPointsLocked() {#getEditPointsLocked--}
```
public boolean getEditPointsLocked()
```

يحدد ما إذا كان تعديل مباشر للحدود الخارجية لهذا الشكل ممنوعًا. منطقية قابلة للقراءة والكتابة.

**الإرجاع:**
boolean

### setEditPointsLocked(boolean value) {#setEditPointsLocked-boolean-}
```
public void setEditPointsLocked(boolean value)
```

يحدد ما إذا كان تعديل مباشر للحدود الخارجية لهذا الشكل ممنوعًا. منطقية قابلة للقراءة والكتابة.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getAdjustHandlesLocked() {#getAdjustHandlesLocked--}
```
public boolean getAdjustHandlesLocked()
```

يحدد ما إذا كان تعديل قيم الضبط ممنوعًا. منطقية قابلة للقراءة والكتابة.

**الإرجاع:**
boolean

### setAdjustHandlesLocked(boolean value) {#setAdjustHandlesLocked-boolean-}
```
public void setAdjustHandlesLocked(boolean value)
```

يحدد ما إذا كان تعديل قيم الضبط ممنوعًا. منطقية قابلة للقراءة والكتابة.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getArrowheadsLocked() {#getArrowheadsLocked--}
```
public boolean getArrowheadsLocked()
```

يحدد ما إذا كان تعديل رؤوس السهام ممنوعًا. منطقية قابلة للقراءة والكتابة.

**الإرجاع:**
boolean

### setArrowheadsLocked(boolean value) {#setArrowheadsLocked-boolean-}
```
public void setArrowheadsLocked(boolean value)
```

يحدد ما إذا كان تعديل رؤوس السهام ممنوعًا. منطقية قابلة للقراءة والكتابة.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getShapeTypeLocked() {#getShapeTypeLocked--}
```
public boolean getShapeTypeLocked()
```

يحدد ما إذا كان تغيير نوع الشكل ممنوعًا. منطقية قابلة للقراءة والكتابة.

**الإرجاع:**
boolean

### setShapeTypeLocked(boolean value) {#setShapeTypeLocked-boolean-}
```
public void setShapeTypeLocked(boolean value)
```

يحدد ما إذا كان تغيير نوع الشكل ممنوعًا. منطقية قابلة للقراءة والكتابة.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getTextLocked() {#getTextLocked--}
```
public boolean getTextLocked()
```

يحدد ما إذا كان تحرير النص ممنوعًا. منطقية قابلة للقراءة والكتابة.

**الإرجاع:**
boolean

### setTextLocked(boolean value) {#setTextLocked-boolean-}
```
public void setTextLocked(boolean value)
```

يحدد ما إذا كان تحرير النص مم

نوًا. منطقية قابلة للقراءة والكتابة.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |