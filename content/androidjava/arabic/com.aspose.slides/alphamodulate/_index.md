---
title: AlphaModulate
second_title: Aspose.Slides لنظام Android عبر مرجع API Java
description: يمثل تأثير تعديل ألفا.
type: docs
url: /ar/com.aspose.slides/alphamodulate/
---
**الوراثة:**  
java.lang.Object, [com.aspose.slides.PVIObject](../../com.aspose.slides/pviobject), [com.aspose.slides.ImageTransformOperation](../../com.aspose.slides/imagetransformoperation)

**كل الواجهات المنفذة:**  
[com.aspose.slides.IAlphaModulate](../../com.aspose.slides/ialphamodulate), com.aspose.slides.IVisualEffect  
```
public final class AlphaModulate extends ImageTransformOperation implements IAlphaModulate, IVisualEffect
```

يمثل تأثير تعديل ألفا. يتم ضرب قيم ألفا (الشفافية) في نسبة ثابتة. يحدد حاوية التأثير تأثيرًا يحتوي على قيم ألفا لتعديلها.

## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getEffective()](#getEffective--) | يحصل على بيانات تأثير تعديل ألفا الفعّالة مع تطبيق الوراثة. |
| [equals(Object obj)](#equals-java.lang.Object-) | يحدّد ما إذا كان [AlphaModulate](../../com.aspose.slides/alphamodulate) المحدد يساوي [AlphaModulate](../../com.aspose.slides/alphamodulate) الحالي. |
| [hashCode()](#hashCode--) | يعمل كدالة تجزئة لنوع معين. |

### getEffective() {#getEffective--}
```
public final IAlphaModulateEffectiveData getEffective()
```

يحصل على بيانات تأثير تعديل ألفا الفعّالة مع تطبيق الوراثة.

**الإرجاع:**  
[IAlphaModulateEffectiveData](../../com.aspose.slides/ialphamodulateeffectivedata) - [IAlphaModulateEffectiveData](../../com.aspose.slides/ialphamodulateeffectivedata)

### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```

يحدّد ما إذا كان [AlphaModulate](../../com.aspose.slides/alphamodulate) المحدد يساوي [AlphaModulate](../../com.aspose.slides/alphamodulate) الحالي.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| obj | java.lang.Object | ال[AlphaModulate](../../com.aspose.slides/alphamodulate) للمقارنة. |

**الإرجاع:**  
boolean - صحيح إذا كانت الكائنات متساوية؛ وإلا خاطئ.

### hashCode() {#hashCode--}
```
public int hashCode()
```

يعمل كدالة تجزئة لنوع معين.

**الإرجاع:**  
int - رمز تجزئة للكائن الحالي.