---
title: ScaleEffect
second_title: Aspose.Slides لنظام Android عبر مرجع API جافا
description: يمثل تأثير مقياس الرسوم المتحركة.
type: docs
url: /ar/com.aspose.slides/scaleeffect/
---
**Inheritance:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**All Implemented Interfaces:**
[com.aspose.slides.IScaleEffect](../../com.aspose.slides/iscaleeffect)
```
public class ScaleEffect extends Behavior implements IScaleEffect
```

يمثل تأثير مقياس الرسوم المتحركة.
## Constructors

| Constructor | Description |
| --- | --- |
| [ScaleEffect()](#ScaleEffect--) |  |
## Methods

| Method | Description |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | يحدد ما إذا كان يجب تكبير المحتوى. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | يحدد ما إذا كان يجب تكبير المحتوى. |
| [getFrom()](#getFrom--) | يحدد إحداثي x/y لبدء الرسوم المتحركة من (بنسبة مئوية). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | يحدد إحداثي x/y لبدء الرسوم المتحركة من (بنسبة مئوية). |
| [getTo()](#getTo--) | يحدد الموقع الهدف لتأثير مقياس الرسوم المتحركة (بنسبة مئوية). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | يحدد الموقع الهدف لتأثير مقياس الرسوم المتحركة (بنسبة مئوية). |
| [getBy()](#getBy--) | يصف قيمة الإزاحة النسبية للرسوم المتحركة (بنسبة مئوية). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | يصف قيمة الإزاحة النسبية للرسوم المتحركة (بنسبة مئوية). |
### ScaleEffect() {#ScaleEffect--}
```
public ScaleEffect()
```


### getZoomContent() {#getZoomContent--}
```
public final byte getZoomContent()
```


يحدد ما إذا كان يجب تكبير المحتوى. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**الإرجاع:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public final void setZoomContent(byte value)
```


يحدد ما إذا كان يجب تكبير المحتوى. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**المعاملات:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public final PointF getFrom()
```


يحدد إحداثي x/y لبدء الرسوم المتحركة من (بنسبة مئوية). قراءة/كتابة android.graphics.PointF.

**الإرجاع:**
android.graphics.PointF
### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public final void setFrom(PointF value)
```


يحدد إحداثي x/y لبدء الرسوم المتحركة من (بنسبة مئوية). قراءة/كتابة android.graphics.PointF.

**المعاملات:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public final PointF getTo()
```


يحدد الموقع الهدف لتأثير مقياس الرسوم المتحركة (بنسبة مئوية). قراءة/كتابة android.graphics.PointF.

**الإرجاع:**
android.graphics.PointF
### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public final void setTo(PointF value)
```


يحدد الموقع الهدف لتأثير مقياس الرسوم المتحركة (بنسبة مئوية). قراءة/كتابة android.graphics.PointF.

**المعاملات:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public final PointF getBy()
```


يصف قيمة الإزاحة النسبية للرسوم المتحركة (بنسبة مئوية). قراءة/كتابة android.graphics.PointF.

**الإرجاع:**
android.graphics.PointF
### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public final void setBy(PointF value)
```


يصف قيمة الإزاحة النسبية للرسوم المتحركة (بنسبة مئوية). قراءة/كتابة android.graphics.PointF.

**المعاملات:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | android.graphics.PointF |  |