---
title: IOleEmbeddedDataInfo
second_title: Aspose.Slides لنظام Android عبر مرجع API لجافا
description: يمثل معلومات البيانات المضمنة لكائن OLE.
type: docs
url: /ar/com.aspose.slides/ioleembeddeddatainfo/
---```
public interface IOleEmbeddedDataInfo
```

يمثل معلومات البيانات المضمنة لكائن OLE.
## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getEmbeddedFileData()](#getEmbeddedFileData--) | إرجاع بيانات الملف لكائن OLE المضمن قراءة فقط byte[]. |
| [getEmbeddedFileExtension()](#getEmbeddedFileExtension--) | إرجاع امتداد الملف لكائن OLE المضمن الحالي قراءة فقط String. |
### getEmbeddedFileData() {#getEmbeddedFileData--}
```
public abstract byte[] getEmbeddedFileData()
```


إرجاع بيانات الملف لكائن OLE المضمن قراءة فقط byte[].

**الإرجاع:**
byte[]
### getEmbeddedFileExtension() {#getEmbeddedFileExtension--}
```
public abstract String getEmbeddedFileExtension()
```


إرجاع امتداد الملف لكائن OLE المضمن الحالي قراءة فقط String.

**الإرجاع:**
java.lang.String