---
title: MarkerStyleType
second_title: Aspose.Slides لنظام Android عبر مرجع API لجافا
description: يحدد شكل العلامة على نقطة البيانات في المخططات.
type: docs
url: /ar/com.aspose.slides/markerstyletype/
---
**الوراثة:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class MarkerStyleType extends System.Enum
```

يحدد شكل العلامة على نقطة البيانات في المخطط.
## الحقول

| الحقل | الوصف |
| --- | --- |
| [NotDefined](#NotDefined) |  |
| [Circle](#Circle) | يحدد أنه يجب رسم دائرة عند كل نقطة بيانات. |
| [Dash](#Dash) | يحدد أنه يجب رسم شَرطَة عند كل نقطة بيانات. |
| [Diamond](#Diamond) | يحدد أنه يجب رسم ماسة عند كل نقطة بيانات. |
| [Dot](#Dot) | يحدد أنه يجب رسم نقطة عند كل نقطة بيانات. |
| [None](#None) | يحدد أنه لا يُرسم شيء عند كل نقطة بيانات. |
| [Picture](#Picture) | يحدد أنه يجب رسم صورة عند كل نقطة بيانات. |
| [Plus](#Plus) | يحدد أنه يجب رسم علامة زائد عند كل نقطة بيانات. |
| [Square](#Square) | يحدد أنه يجب رسم مربع عند كل نقطة بيانات. |
| [Star](#Star) | يحدد أنه يجب رسم نجمة عند كل نقطة بيانات. |
| [Triangle](#Triangle) | يحدد أنه يجب رسم مثلث عند كل نقطة بيانات. |
| [X](#X) | يحدد أنه يجب رسم حرف X عند كل نقطة بيانات. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```




### Circle {#Circle}
```
public static final int Circle
```

يحدد أنه يجب رسم دائرة عند كل نقطة بيانات.

### Dash {#Dash}
```
public static final int Dash
```

يحدد أنه يجب رسم شَرطَة عند كل نقطة بيانات.

### Diamond {#Diamond}
```
public static final int Diamond
```

يحدد أنه يجب رسم ماسة عند كل نقطة بيانات.

### Dot {#Dot}
```
public static final int Dot
```

يحدد أنه يجب رسم نقطة عند كل نقطة بيانات.

### None {#None}
```
public static final int None
```

يحدد أنه لا يُرسم شيء عند كل نقطة بيانات.

### Picture {#Picture}
```
public static final int Picture
```

يحدد أنه يجب رسم صورة عند كل نقطة بيانات.

### Plus {#Plus}
```
public static final int Plus
```

يحدد أنه يجب رسم علامة زائد عند كل نقطة بيانات.

### Square {#Square}
```
public static final int Square
```

يحدد أنه يجب رسم مربع عند كل نقطة بيانات.

### Star {#Star}
```
public static final int Star
```

يحدد أنه يجب رسم نجمة عند كل نقطة بيانات.

### Triangle {#Triangle}
```
public static final int Triangle
```

يحدد أنه يجب رسم مثلث عند كل نقطة بيانات.

### X {#X}
```
public static final int X
```

يحدد أنه يجب رسم حرف X عند كل نقطة بيانات.