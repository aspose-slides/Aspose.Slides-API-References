---
title: MotionEffect
second_title: Aspose.Slides لنظام Android عبر مرجع API لجافا
description: يمثل سلوك تأثير الحركة للتأثير.
type: docs
url: /ar/com.aspose.slides/motioneffect/
---
**الوراثة:**  
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**جميع الواجهات المنفذة:**  
[com.aspose.slides.IMotionEffect](../../com.aspose.slides/imotioneffect)
```
public class MotionEffect extends Behavior implements IMotionEffect
```

تمثل سلوك تأثير الحركة للتأثير.

## المنشئات

| المنشئ | الوصف |
| --- | --- |
| [MotionEffect()](#MotionEffect--) |  |

## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getFrom()](#getFrom--) | يحدد إحداثي x/y لبدء الرسوم المتحركة من (بالنسب المئوية). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | يحدد إحداثي x/y لبدء الرسوم المتحركة من (بالنسب المئوية). |
| [getTo()](#getTo--) | يحدد الموقع الهدف لتأثير حركة الرسوم المتحركة (بالنسب المئوية). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | يحدد الموقع الهدف لتأثير حركة الرسوم المتحركة (بالنسب المئوية). |
| [getBy()](#getBy--) | يصف قيمة الإزاحة النسبية للرسوم المتحركة (بالنسب المئوية). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | يصف قيمة الإزاحة النسبية للرسوم المتحركة (بالنسب المئوية). |
| [getRotationCenter()](#getRotationCenter--) | يصف مركز الدوران المستخدم لتدوير مسار الحركة بزاوية X. |
| [setRotationCenter(PointF value)](#setRotationCenter-android.graphics.PointF-) | يصف مركز الدوران المستخدم لتدوير مسار الحركة بزاوية X. |
| [getOrigin()](#getOrigin--) | يحدد ما هو أصل مسار الحركة بالنسبة إلى مثل تخطيط الشريحة أو الأصل. |
| [setOrigin(int value)](#setOrigin-int-) | يحدد ما هو أصل مسار الحركة بالنسبة إلى مثل تخطيط الشريحة أو الأصل. |
| [getPath()](#getPath--) | يحدد الشكل الأولي للمسار متبوعًا بالإحداثيات لحركة الرسوم المتحركة. |
| [setPath(IMotionPath value)](#setPath-com.aspose.slides.IMotionPath-) | يحدد الشكل الأولي للمسار متبوعًا بالإحداثيات لحركة الرسوم المتحركة. |
| [getPathEditMode()](#getPathEditMode--) | يحدد كيف يتحرك مسار الحركة عندما يتم نقل الشكل. |
| [setPathEditMode(int value)](#setPathEditMode-int-) | يحدد كيف يتحرك مسار الحركة عندما يتم نقل الشكل. |
| [getAngle()](#getAngle--) | يصف الزاوية النسبية لمسار الحركة. |
| [setAngle(float value)](#setAngle-float-) | يصف الزاوية النسبية لمسار الحركة. |

### MotionEffect() {#MotionEffect--}
```
public MotionEffect()
```

### getFrom() {#getFrom--}
```
public final PointF getFrom()
```

يحدد إحداثي x/y لبدء الرسوم المتحركة من (بالنسب المئوية). قراءة/كتابة android.graphics.PointF.

**القيمة المرجعة:**  
android.graphics.PointF

### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public final void setFrom(PointF value)
```

يحدد إحداثي x/y لبدء الرسوم المتحركة من (بالنسب المئوية). قراءة/كتابة android.graphics.PointF.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public final PointF getTo()
```

يحدد الموقع الهدف لتأثير حركة الرسوم المتحركة (بالنسب المئوية). قراءة/كتابة android.graphics.PointF.

**القيمة المرجعة:**  
android.graphics.PointF

### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public final void setTo(PointF value)
```

يحدد الموقع الهدف لتأثير حركة الرسوم المتحركة (بالنسب المئوية). قراءة/كتابة android.graphics.PointF.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public final PointF getBy()
```

يصف قيمة الإزاحة النسبية للرسوم المتحركة (بالنسب المئوية). قراءة/كتابة android.graphics.PointF.

**القيمة المرجعة:**  
android.graphics.PointF

### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public final void setBy(PointF value)
```

يصف قيمة الإزاحة النسبية للرسوم المتحركة (بالنسب المئوية). قراءة/كتابة android.graphics.PointF.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getRotationCenter() {#getRotationCenter--}
```
public final PointF getRotationCenter()
```

يصف مركز الدوران المستخدم لتدوير مسار الحركة بزاوية X. قراءة/كتابة android.graphics.PointF.

**القيمة المرجعة:**  
android.graphics.PointF

### setRotationCenter(PointF value) {#setRotationCenter-android.graphics.PointF-}
```
public final void setRotationCenter(PointF value)
```

يصف مركز الدوران المستخدم لتدوير مسار الحركة بزاوية X. قراءة/كتابة android.graphics.PointF.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getOrigin() {#getOrigin--}
```
public final int getOrigin()
```

يحدد ما هو أصل مسار الحركة بالنسبة إلى مثل تخطيط الشريحة أو الأصل. قراءة/كتابة [MotionOriginType](../../com.aspose.slides/motionorigintype).

**القيمة المرجعة:**  
int

### setOrigin(int value) {#setOrigin-int-}
```
public final void setOrigin(int value)
```

يحدد ما هو أصل مسار الحركة بالنسبة إلى مثل تخطيط الشريحة أو الأصل. قراءة/كتابة [MotionOriginType](../../com.aspose.slides/motionorigintype).

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | int |  |

### getPath() {#getPath--}
```
public final IMotionPath getPath()
```

يحدد الشكل الأولي للمسار متبوعًا بالإحداثيات لحركة الرسوم المتحركة. قراءة/كتابة [IMotionPath](../../com.aspose.slides/imotionpath).

**القيمة المرجعة:**  
[IMotionPath](../../com.aspose.slides/imotionpath)

### setPath(IMotionPath value) {#setPath-com.aspose.slides.IMotionPath-}
```
public final void setPath(IMotionPath value)
```

يحدد الشكل الأولي للمسار متبعًا بالإحداثيات لحركة الرسوم المتحركة. قراءة/كتابة [IMotionPath](../../com.aspose.slides/imotionpath).

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | [IMotionPath](../../com.aspose.slides/imotionpath) |  |

### getPathEditMode() {#getPathEditMode--}
```
public final int getPathEditMode()
```

يحدد كيف يتحرك مسار الحركة عندما يتم نقل الشكل. قراءة/كتابة [MotionPathEditMode](../../com.aspose.slides/motionpatheditmode).

**القيمة المرجعة:**  
int

### setPathEditMode(int value) {#setPathEditMode-int-}
```
public final void setPathEditMode(int value)
```

يحدد كيف يتحرك مسار الحركة عندما يتم نقل الشكل. قراءة/كتابة [MotionPathEditMode](../../com.aspose.slides/motionpatheditmode).

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | int |  |

### getAngle() {#getAngle--}
```
public final float getAngle()
```

يصف الزاوية النسبية لمسار الحركة. قراءة/كتابة float.

**القيمة المرجعة:**  
float

### setAngle(float value) {#setAngle-float-}
```
public final void setAngle(float value)
```

يصف الزاوية النسبية لمسار الحركة. قراءة/كتابة float.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |