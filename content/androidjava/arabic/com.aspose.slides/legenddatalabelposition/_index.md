---
title: LegendDataLabelPosition
second_title: Aspose.Slides لنظام Android عبر مرجع API للجاوة
description: يحدد موضع تسميات البيانات.
type: docs
url: /ar/com.aspose.slides/legenddatalabelposition/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class LegendDataLabelPosition extends System.Enum
```

يحدد موضع تسمية البيانات.
## Fields

| الحقل | الوصف |
| --- | --- |
| [NotDefined](#NotDefined) | الموضع غير معرف. |
| [Bottom](#Bottom) | يحدد أن تُعرض تسمية البيانات أسفل علامة البيانات. |
| [BestFit](#BestFit) | يحدد أن تُعرض تسمية البيانات في أفضل موضع. |
| [Center](#Center) | يحدد أن تُعرض تسمية البيانات مركزة على علامة البيانات. |
| [InsideBase](#InsideBase) | يحدد أن تُعرض تسمية البيانات داخل قاعدة علامة البيانات. |
| [InsideEnd](#InsideEnd) | يحدد أن تُعرض تسمية البيانات داخل نهاية علامة البيانات. |
| [Left](#Left) | يحدد أن تُعرض تسمية البيانات إلى يسار علامة البيانات. |
| [OutsideEnd](#OutsideEnd) | يحدد أن تُعرض تسمية البيانات خارج نهاية علامة البيانات. |
| [Right](#Right) | يحدد أن تُعرض تسمية البيانات إلى يمين علامة البيانات. |
| [Top](#Top) | يحدد أن تُعرض تسمية البيانات فوق علامة البيانات. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```


الموضع غير معرف.

### Bottom {#Bottom}
```
public static final int Bottom
```


يحدد أن تُعرض تسمية البيانات أسفل علامة البيانات.

### BestFit {#BestFit}
```
public static final int BestFit
```


يحدد أن تُعرض تسمية البيانات في أفضل موضع.

### Center {#Center}
```
public static final int Center
```


يحدد أن تُعرض تسمية البيانات مركزة على علامة البيانات.

### InsideBase {#InsideBase}
```
public static final int InsideBase
```


يحدد أن تُعرض تسمية البيانات داخل قاعدة علامة البيانات.

### InsideEnd {#InsideEnd}
```
public static final int InsideEnd
```


يحدد أن تُعرض تسمية البيانات داخل نهاية علامة البيانات.

### Left {#Left}
```
public static final int Left
```


يحدد أن تُعرض تسمية البيانات إلى يسار علامة البيانات.

### OutsideEnd {#OutsideEnd}
```
public static final int OutsideEnd
```


يحدد أن تُعرض تسمية البيانات خارج نهاية علامة البيانات.

### Right {#Right}
```
public static final int Right
```


يحدد أن تُعرض تسمية البيانات إلى يمين علامة البيانات.

### Top {#Top}
```
public static final int Top
```


يحدد أن تُعرض تسمية البيانات فوق علامة البيانات.