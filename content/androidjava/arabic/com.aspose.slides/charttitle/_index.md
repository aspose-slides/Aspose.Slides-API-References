---
title: ChartTitle
second_title: Aspose.Slides for Android عبر مرجع API للـ Java
description: يمثّل خصائص عنوان المخطط.
type: docs
url: /ar/com.aspose.slides/charttitle/
---
**الوراثة:**
java.lang.Object

**جميع الواجهات المنفذة:**
[com.aspose.slides.IChartTitle](../../com.aspose.slides/icharttitle), com.aspose.slides.IDOMObject
```
public class ChartTitle implements IChartTitle, IDOMObject
```

يمثّل خصائص عنوان المخطط.
## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getX()](#getX--) | تُعيد أو تُعيّن إحداثي x للعنوان كنسبة من عرض المخطط. |
| [setX(float value)](#setX-float-) | تُعيد أو تُعيّن إحداثي x للعنوان كنسبة من عرض المخطط. |
| [getY()](#getY--) | تُعيد أو تُعيّن إحداثي y للعنوان كنسبة من ارتفاع المخطط. |
| [setY(float value)](#setY-float-) | تُعيد أو تُعيّن إحداثي y للعنوان كنسبة من ارتفاع المخطط. |
| [getWidth()](#getWidth--) | تُعيد أو تُعيّن عرض العنوان كنسبة من عرض المخطط. |
| [setWidth(float value)](#setWidth-float-) | تُعيد أو تُعيّن عرض العنوان كنسبة من عرض المخطط. |
| [getHeight()](#getHeight--) | تُعيد أو تُعيّن ارتفاع العنوان كنسبة من ارتفاع المخطط. |
| [setHeight(float value)](#setHeight-float-) | تُعيد أو تُعيّن ارتفاع العنوان كنسبة من ارتفاع المخطط. |
| [getRight()](#getRight--) | يمين. |
| [getBottom()](#getBottom--) | أسفل. |
| [getOverlay()](#getOverlay--) | يحدّد ما إذا كان يُسمح لعناصر المخطط الأخرى بالتداخل مع العنوان. |
| [setOverlay(boolean value)](#setOverlay-boolean-) | يحدّد ما إذا كان يُسمح لعناصر المخطط الأخرى بالتداخل مع العنوان. |
| [getFormat()](#getFormat--) | تُعيد أنماط التعبئة، الخط، والتأثير للعنوان. |
| [addTextFrameForOverriding(String text)](#addTextFrameForOverriding-java.lang.String-) | تتهيء TextFrameForOverriding بالنص في المعامل "text". |
| [getTextFrameForOverriding()](#getTextFrameForOverriding--) | يمكن أن يحتوي على نص غني منسّق. |
| [getTextFormat()](#getTextFormat--) | تُعيد تنسيق النص. |
| [getActualX()](#getActualX--) | يحدّد الموقع الفعلي لـ x (اليسار) لعنصر المخطط بالنسبة إلى الركن العلوي الأيسر للمخطط. |
| [getActualY()](#getActualY--) | يحدّد أعلى العنصر الفعلي للمخطط بالنسبة إلى الركن العلوي الأيسر للمخطط. |
| [getActualWidth()](#getActualWidth--) | يحدّد العرض الفعلي لعنصر المخطط. |
| [getActualHeight()](#getActualHeight--) | يحدّد الارتفاع الفعلي لعنصر المخطط. |
| [getParent_Immediate()](#getParent-Immediate--) |  |
| [getChart()](#getChart--) | تُعيد المخطط الأب. |
| [getSlide()](#getSlide--) | تُعيد الشريحة الأب لـ FillFormat. |
| [getPresentation()](#getPresentation--) | تُعيد العرض التقديمي الأب لـ FillFormat. |
### getX() {#getX--}
```
public final float getX()
```


تُعيد أو تُعيّن إحداثي x للعنوان كنسبة من عرض المخطط. قراءة/كتابة float.

**القيمة المرجعة:**
float
### setX(float value) {#setX-float-}
```
public final void setX(float value)
```


تُعيد أو تُعيّن إحداثي x للعنوان كنسبة من عرض المخطط. قراءة/كتابة float.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getY() {#getY--}
```
public final float getY()
```


تُعيد أو تُعيّن إحداثي y للعنوان كنسبة من ارتفاع المخطط. قراءة/كتابة float.

**القيمة المرجعة:**
float
### setY(float value) {#setY-float-}
```
public final void setY(float value)
```


تُعيد أو تُعيّن إحداثي y للعنوان كنسبة من ارتفاع المخطط. قراءة/كتابة float.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getWidth() {#getWidth--}
```
public final float getWidth()
```


تُعيد أو تُعيّن عرض العنوان كنسبة من عرض المخطط. قراءة/كتابة float.

**القيمة المرجعة:**
float
### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```


تُعيد أو تُعيّن عرض العنوان كنسبة من عرض المخطط. قراءة/كتابة float.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getHeight() {#getHeight--}
```
public final float getHeight()
```


تُعيد أو تُعيّن ارتفاع العنوان كنسبة من ارتفاع المخطط. قراءة/كتابة float.

**القيمة المرجعة:**
float
### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```


تُعيد أو تُعيّن ارتفاع العنوان كنسبة من ارتفاع المخطط. قراءة/كتابة float.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getRight() {#getRight--}
```
public final float getRight()
```


يمين. قراءة فقط float.

**القيمة المرجعة:**
float
### getBottom() {#getBottom--}
```
public final float getBottom()
```


أسفل. قراءة فقط float.

**القيمة المرجعة:**
float
### getOverlay() {#getOverlay--}
```
public final boolean getOverlay()
```


يحدّد ما إذا كان يُسمح لعناصر المخطط الأخرى بالتداخل مع العنوان. قراءة/كتابة boolean.

**القيمة المرجعة:**
boolean
### setOverlay(boolean value) {#setOverlay-boolean-}
```
public final void setOverlay(boolean value)
```


يحدّد ما إذا كان يُسمح لعناصر المخطط الأخرى بالتداخل مع العنوان. قراءة/كتابة boolean.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```


تُعيد أنماط التعبئة، الخط، والتأثير للعنوان. قراءة فقط [IFormat](../../com.aspose.slides/iformat).

**القيمة المرجعة:**
[IFormat](../../com.aspose.slides/iformat)
### addTextFrameForOverriding(String text) {#addTextFrameForOverriding-java.lang.String-}
```
public final ITextFrame addTextFrameForOverriding(String text)
```


تهيئة TextFrameForOverriding بالنص في المعامل "text". إذا كان TextFrameForOverriding مُهيئًا مسبقًا فسيتم فقط تغيير نصه.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| text | java.lang.String | النص لإطار النص الجديد. |

**القيمة المرجعة:**
[ITextFrame](../../com.aspose.slides/itextframe)
### getTextFrameForOverriding() {#getTextFrameForOverriding--}
```
public final ITextFrame getTextFrameForOverriding()
```


يمكن أن يحتوي على نص غني منسّق. إذا لم يكن هذا الخاصية فارغًا فإن قيمة النص المنسّق هذه تتجاوز النص المُولد تلقائيًا. النص المُولد تلقائيًا هو خاصية ضمنية لتسمية البيانات، تسمية وحدة العرض للمحور القيمي، عنوان المحور، عنوان المخطط، تسمية خط الاتجاه. النص المُولد تلقائيًا يُنسق باستخدام خاصية IFormattedTextContainer.TextFormat. قراءة فقط [ITextFrame](../../com.aspose.slides/itextframe).

**القيمة المرجعة:**
[ITextFrame](../../com.aspose.slides/itextframe)
### getTextFormat() {#getTextFormat--}
```
public final IChartTextFormat getTextFormat()
```


تُعيد تنسيق النص. قراءة فقط [IChartTextFormat](../../com.aspose.slides/icharttextformat).

**القيمة المرجعة:**
[IChartTextFormat](../../com.aspose.slides/icharttextformat)
### getActualX() {#getActualX--}
```
public final float getActualX()
```


يحدّد الموقع الفعلي لـ x (اليسار) لعنصر المخطط بالنسبة إلى الركن العلوي الأيسر للمخطط. استدعِ الدالة IChart.validateChartLayout() أولاً للحصول على القيم الفعلية. قراءة float.

**القيمة المرجعة:**
float
### getActualY() {#getActualY--}
```
public final float getActualY()
```


يحدّد أعلى العنصر الفعلي للمخطط بالنسبة إلى الركن العلوي الأيسر للمخطط. استدعِ الدالة IChart.validateChartLayout() أولاً للحصول على القيم الفعلية. قراءة float.

**القيمة المرجعة:**
float
### getActualWidth() {#getActualWidth--}
```
public final float getActualWidth()
```


يحدّد العرض الفعلي لعنصر المخطط. استدعِ الدالة IChart.validateChartLayout() أولاً للحصول على القيم الفعلية. قراءة float.

**القيمة المرجعة:**
float
### getActualHeight() {#getActualHeight--}
```
public final float getActualHeight()
```


يحدّد الارتفاع الفعلي لعنصر المخطط. استدعِ الدالة IChart.validateChartLayout() أولاً للحصول على القيم الفعلية. قراءة float.

**القيمة المرجعة:**
float
### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```


تُعيد كائن Parent_Immediate. قراءة فقط IDOMObject.

**القيمة المرجعة:**
com.aspose.slides.IDOMObject
### getChart() {#getChart--}
```
public final IChart getChart()
```


تُعيد المخطط الأب. قراءة فقط [IChart](../../com.aspose.slides/ichart).

**القيمة المرجعة:**
[IChart](../../com.aspose.slides/ichart)
### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```


تُعيد الشريحة الأب لـ FillFormat. قراءة فقط [BaseSlide](../../com.aspose.slides/baseslide).

**القيمة المرجعة:**
[IBaseSlide](../../com.aspose.slides/ibaseslide)
### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```


تُعيد العرض التقديمي الأب لـ FillFormat. قراءة فقط [IPresentation](../../com.aspose.slides/ipresentation).

**القيمة المرجعة:**
[IPresentation](../../com.aspose.slides/ipresentation)