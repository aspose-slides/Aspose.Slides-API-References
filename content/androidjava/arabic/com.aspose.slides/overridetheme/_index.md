---
title: OverrideTheme
second_title: Aspose.Slides لنظام Android عبر مرجع API لجافا
description: يمثل سمة متجاوزة.
type: docs
url: /ar/com.aspose.slides/overridetheme/
---
**الوراثة:**  
java.lang.Object, [com.aspose.slides.Theme](../../com.aspose.slides/theme)

**جميع الواجهات المُطبقة:**  
[com.aspose.slides.IOverrideTheme](../../com.aspose.slides/ioverridetheme)  
```
public final class OverrideTheme extends Theme implements IOverrideTheme
```

يمثل سمة متجاوزة.

## الطرق

| الطريقة | الوصف |
| --- | --- |
| [initColorScheme()](#initColorScheme--) | تهيئة ColorScheme بكائن جديد لتجاوز ColorScheme الخاص بـ InheritedTheme. |
| [initColorSchemeFrom(IColorScheme colorScheme)](#initColorSchemeFrom-com.aspose.slides.IColorScheme-) | تهيئة ColorScheme بكائن جديد لتجاوز ColorScheme الخاص بـ InheritedTheme. |
| [initColorSchemeFromInherited()](#initColorSchemeFromInherited--) | تهيئة ColorScheme بكائن جديد لتجاوز ColorScheme الخاص بـ InheritedTheme. |
| [initFontScheme()](#initFontScheme--) | تهيئة FontScheme بكائن جديد لتجاوز FontScheme الخاص بـ InheritedTheme. |
| [initFontSchemeFrom(IFontScheme fontScheme)](#initFontSchemeFrom-com.aspose.slides.IFontScheme-) | تهيئة FontScheme بكائن جديد لتجاوز FontScheme الخاص بـ InheritedTheme. |
| [initFontSchemeFromInherited()](#initFontSchemeFromInherited--) | تهيئة FontScheme بكائن جديد لتجاوز FontScheme الخاص بـ InheritedTheme. |
| [initFormatScheme()](#initFormatScheme--) | تهيئة FormatScheme بكائن جديد لتجاوز FormatScheme الخاص بـ InheritedTheme. |
| [initFormatSchemeFrom(IFormatScheme formatScheme)](#initFormatSchemeFrom-com.aspose.slides.IFormatScheme-) | تهيئة FormatScheme بكائن جديد لتجاوز FormatScheme الخاص بـ InheritedTheme. |
| [initFormatSchemeFromInherited()](#initFormatSchemeFromInherited--) | تهيئة FormatScheme بكائن جديد لتجاوز FormatScheme الخاص بـ InheritedTheme. |
| [getColorScheme()](#getColorScheme--) | تُرجع مخطط الألوان. |
| [getFontScheme()](#getFontScheme--) | تُرجع مخطط الخطوط. |
| [getFormatScheme()](#getFormatScheme--) | تُرجع مخطط تنسيق الشكل. |
| [isEmpty()](#isEmpty--) | القيمة الصادقة تعني أن ColorScheme و FontScheme و FormatScheme قيمهم null وأي تجاوز بهذا الكائن غير مفعل. |
| [clear()](#clear--) | اضبط ColorScheme و FontScheme و FormatScheme إلى null لتعطيل أي تجاوز بهذا الكائن. |
| [getVersion()](#getVersion--) |  |

### initColorScheme() {#initColorScheme--}
```
public final void initColorScheme()
```

تهيئة ColorScheme بكائن جديد لتجاوز ColorScheme الخاص بـ InheritedTheme.

### initColorSchemeFrom(IColorScheme colorScheme) {#initColorSchemeFrom-com.aspose.slides.IColorScheme-}
```
public final void initColorSchemeFrom(IColorScheme colorScheme)
```

تهيئة ColorScheme بكائن جديد لتجاوز ColorScheme الخاص بـ InheritedTheme.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| colorScheme | [IColorScheme](../../com.aspose.slides/icolorscheme) | البيانات للتهيئة منها. |

### initColorSchemeFromInherited() {#initColorSchemeFromInherited--}
```
public final void initColorSchemeFromInherited()
```

تهيئة ColorScheme بكائن جديد لتجاوز ColorScheme الخاص بـ InheritedTheme. وتم تهيئة بيانات هذا الكائن الجديد ببيانات ColorScheme الخاص بـ InheritedTheme.

### initFontScheme() {#initFontScheme--}
```
public final void initFontScheme()
```

تهيئة FontScheme بكائن جديد لتجاوز FontScheme الخاص بـ InheritedTheme.

### initFontSchemeFrom(IFontScheme fontScheme) {#initFontSchemeFrom-com.aspose.slides.IFontScheme-}
```
public final void initFontSchemeFrom(IFontScheme fontScheme)
```

تهيئة FontScheme بكائن جديد لتجاوز FontScheme الخاص بـ InheritedTheme.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| fontScheme | [IFontScheme](../../com.aspose.slides/ifontscheme) | البيانات للتهيئة منها. |

### initFontSchemeFromInherited() {#initFontSchemeFromInherited--}
```
public final void initFontSchemeFromInherited()
```

تهيئة FontScheme بكائن جديد لتجاوز FontScheme الخاص بـ InheritedTheme. وتم تهيئة بيانات هذا الكائن الجديد ببيانات FontScheme الخاص بـ InheritedTheme.

### initFormatScheme() {#initFormatScheme--}
```
public final void initFormatScheme()
```

تهيئة FormatScheme بكائن جديد لتجاوز FormatScheme الخاص بـ InheritedTheme.

### initFormatSchemeFrom(IFormatScheme formatScheme) {#initFormatSchemeFrom-com.aspose.slides.IFormatScheme-}
```
public final void initFormatSchemeFrom(IFormatScheme formatScheme)
```

تهيئة FormatScheme بكائن جديد لتجاوز FormatScheme الخاص بـ InheritedTheme.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| formatScheme | [IFormatScheme](../../com.aspose.slides/iformatscheme) | البيانات للتهيئة منها. |

### initFormatSchemeFromInherited() {#initFormatSchemeFromInherited--}
```
public final void initFormatSchemeFromInherited()
```

تهيئة FormatScheme بكائن جديد لتجاوز FormatScheme الخاص بـ InheritedTheme. وتم تهيئة بيانات هذا الكائن الجديد ببيانات FormatScheme الخاص بـ InheritedTheme.

### getColorScheme() {#getColorScheme--}
```
public IColorScheme getColorScheme()
```

تُرجع مخطط الألوان. للقراءة فقط [IColorScheme](../../com.aspose.slides/icolorscheme).

**القيمة المرجعة:**
[IColorScheme](../../com.aspose.slides/icolorscheme)

### getFontScheme() {#getFontScheme--}
```
public IFontScheme getFontScheme()
```

تُرجع مخطط الخطوط. للقراءة فقط [IFontScheme](../../com.aspose.slides/ifontscheme).

**القيمة المرجعة:**
[IFontScheme](../../com.aspose.slides/ifontscheme)

### getFormatScheme() {#getFormatScheme--}
```
public IFormatScheme getFormatScheme()
```

تُرجع مخطط تنسيق الشكل. للقراءة فقط [IFormatScheme](../../com.aspose.slides/iformatscheme).

**القيمة المرجعة:**
[IFormatScheme](../../com.aspose.slides/iformatscheme)

### isEmpty() {#isEmpty--}
```
public final boolean isEmpty()
```

القيمة الصادقة تعني أن ColorScheme و FontScheme و FormatScheme قيمهم null وأي تجاوز بهذا الكائن غير مفعل. للقراءة فقط boolean.

**القيمة المرجعة:**
boolean

### clear() {#clear--}
```
public final void clear()
```

اضبط ColorScheme و FontScheme و FormatScheme إلى null لتعطيل أي تجاوز بهذا الكائن.

### getVersion() {#getVersion--}
```
public long getVersion()
```

الإصدار. للقراءة فقط long.

**القيمة المرجعة:**
long