---
title: AdjustValue
second_title: Aspose.Slides لنظام Android عبر مرجع API للـ Java
description: يمثل قيمة تعديل الأشكال الهندسية.
type: docs
url: /ar/com.aspose.slides/adjustvalue/
---
**Inheritance:**
java.lang.Object

**All Implemented Interfaces:**
[com.aspose.slides.IAdjustValue](../../com.aspose.slides/iadjustvalue)
```
public class AdjustValue implements IAdjustValue
```

يمثل قيمة تعديل الشكل الهندسي. هذه القيم تؤثر على شكل الشكل.
## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getRawValue()](#getRawValue--) | يرجع أو يضبط قيمة الضبط "كما هو". |
| [setRawValue(long value)](#setRawValue-long-) | يرجع أو يضبط قيمة الضبط "كما هو". |
| [getAngleValue()](#getAngleValue--) | يرجع أو يضبط القيمة، مع تفسيرها كزاوية بالدرجات. |
| [setAngleValue(float value)](#setAngleValue-float-) | يرجع أو يضبط القيمة، مع تفسيرها كزاوية بالدرجات. |
| [getName()](#getName--) | يرجع اسم هذه القيمة الضبطية. |
| [getType()](#getType--) | يرجع نوع تعديل الشكل. |
### getRawValue() {#getRawValue--}
```
public final long getRawValue()
```

يرجع أو يضبط قيمة الضبط "كما هو". قراءة/كتابة long.

**Returns:**
long
### setRawValue(long value) {#setRawValue-long-}
```
public final void setRawValue(long value)
```

يرجع أو يضبط قيمة الضبط "كما هو". قراءة/كتابة long.

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | long |  |

### getAngleValue() {#getAngleValue--}
```
public final float getAngleValue()
```

يرجع أو يضبط القيمة، مع تفسيرها كزاوية بالدرجات. قراءة/كتابة float.

**Returns:**
float
### setAngleValue(float value) {#setAngleValue-float-}
```
public final void setAngleValue(float value)
```

يرجع أو يضبط القيمة، مع تفسيرها كزاوية بالدرجات. قراءة/كتابة float.

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getName() {#getName--}
```
public final String getName()
```

يرجع اسم هذه القيمة الضبطية. قراءة فقط String.

**Returns:**
java.lang.String
### getType() {#getType--}
```
public final int getType()
```

يرجع نوع تعديل الشكل. قراءة فقط [ShapeAdjustmentType](../../com.aspose.slides/shapeadjustmenttype).

**Returns:**
int