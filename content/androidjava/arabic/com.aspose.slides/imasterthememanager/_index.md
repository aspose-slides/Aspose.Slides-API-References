---
title: IMasterThemeManager
second_title: Aspose.Slides for Android عبر مرجع API Java
description: يتيح الوصول إلى نمط العرض الرئيسي.
type: docs
url: /ar/com.aspose.slides/imasterthememanager/
---
**جميع الواجهات المنفذة:**
[com.aspose.slides.IThemeManager](../../com.aspose.slides/ithememanager)
```
public interface IMasterThemeManager extends IThemeManager
```

يوفر الوصول إلى نمط العرض الرئيسي.
## الطرق

| طريقة | الوصف |
| --- | --- |
| [isOverrideThemeEnabled()](#isOverrideThemeEnabled--) | يحدد ما إذا كان OverrideTheme يتجاوز النمط الفعّال الموروث (Presentation.MasterTheme) أم لا. |
| [setOverrideThemeEnabled(boolean value)](#setOverrideThemeEnabled-boolean-) | يحدد ما إذا كان OverrideTheme يتجاوز النمط الفعّال الموروث (Presentation.MasterTheme) أم لا. |
| [getOverrideTheme()](#getOverrideTheme--) | يعيد كائن النمط المتجاوز. |
| [setOverrideTheme(IMasterTheme value)](#setOverrideTheme-com.aspose.slides.IMasterTheme-) | يعيد كائن النمط المتجاوز. |
### isOverrideThemeEnabled() {#isOverrideThemeEnabled--}
```
public abstract boolean isOverrideThemeEnabled()
```

يحدد ما إذا كان OverrideTheme يتجاوز النمط الفعّال الموروث (Presentation.MasterTheme) أم لا. قراءة/كتابة boolean.

**القيمة المرجعة:**
boolean
### setOverrideThemeEnabled(boolean value) {#setOverrideThemeEnabled-boolean-}
```
public abstract void setOverrideThemeEnabled(boolean value)
```

يحدد ما إذا كان OverrideTheme يتجاوز النمط الفعّال الموروث (Presentation.MasterTheme) أم لا. قراءة/كتابة boolean.

**الوسائط:**
| معامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |
### getOverrideTheme() {#getOverrideTheme--}
```
public abstract IMasterTheme getOverrideTheme()
```

يعيد كائن النمط المتجاوز. قراءة/كتابة [IMasterTheme](../../com.aspose.slides/imastertheme).

**القيمة المرجعة:**
[IMasterTheme](../../com.aspose.slides/imastertheme)
### setOverrideTheme(IMasterTheme value) {#setOverrideTheme-com.aspose.slides.IMasterTheme-}
```
public abstract void setOverrideTheme(IMasterTheme value)
```

يعيد كائن النمط المتجاوز. قراءة/كتابة [IMasterTheme](../../com.aspose.slides/imastertheme).

**الوسائط:**
| معامل | النوع | الوصف |
| --- | --- | --- |
| value | [IMasterTheme](../../com.aspose.slides/imastertheme) |  |