---
title: WorkbookType
second_title: Aspose.Slides لنظام Android عبر مرجع API Java
description: يحدد نوع ملف المصنف Open XML.
type: docs
url: /ar/com.aspose.slides/workbooktype/
---
**Inheritance:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class WorkbookType extends System.Enum
```

يحدد نوع ملف المصنف Open XML.
## الحقول

| الحقل | الوصف |
| --- | --- |
| [NotDefined](#NotDefined) | نوع المصنف غير معرف. |
| [Workbook](#Workbook) | مصنف Excel (\*.xlsx). |
| [WorkbookMacro](#WorkbookMacro) | مصنف Excel مفعّل بالماكرو (\*.xlsm). |
| [Template](#Template) | قالب Excel (\*.xltx). |
| [TemplateMacro](#TemplateMacro) | قالب Excel مفعّل بالماكرو (\*.xltm). |
| [WorkbookBinaryMacro](#WorkbookBinaryMacro) | مصنف Excel الثنائي المفعّل بالماكرو (\*.xlsb). |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```

نوع المصنف غير معرف.

### Workbook {#Workbook}
```
public static final int Workbook
```

مصنف Excel (\*.xlsx).

### WorkbookMacro {#WorkbookMacro}
```
public static final int WorkbookMacro
```

مصنف Excel مفعّل بالماكرو (\*.xlsm).

### Template {#Template}
```
public static final int Template
```

قالب Excel (\*.xltx).

### TemplateMacro {#TemplateMacro}
```
public static final int TemplateMacro
```

قالب Excel مفعّل بالماكرو (\*.xltm).

### WorkbookBinaryMacro {#WorkbookBinaryMacro}
```
public static final int WorkbookBinaryMacro
```

مصنف Excel الثنائي المفعّل بالماكرو (\*.xlsb).