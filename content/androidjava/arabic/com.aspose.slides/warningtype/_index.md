---
title: WarningType
second_title: Aspose.Slides لـ Android عبر مرجع API جافا
description: يمثل نوعًا من التحذير.
type: docs
url: /ar/com.aspose.slides/warningtype/
---
**الوراثة:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class WarningType extends System.Enum
```

تمثّل نوعًا من التحذيرات.
## الحقول

| الحقل | الوصف |
| --- | --- |
| [SourceFileCorruption](#SourceFileCorruption) | تم اكتشاف مشكلة في مستند المصدر تجعل من المحتمل جداً أن المستند لن يكون قابلاً للفتح إذا تم حفظه بصيغته الأصلية. |
| [DataLoss](#DataLoss) | ستُفقد النصوص/المخططات/الصور أو أي بيانات أخرى بالكامل إما من شجرة المستند بعد التحميل أو من المستند المُنشأ بعد الحفظ. |
| [MajorFormattingLoss](#MajorFormattingLoss) | فقدان كبير للتنسيق. |
| [MinorFormattingLoss](#MinorFormattingLoss) | فقدان طفيف للتنسيق. |
| [CompatibilityIssue](#CompatibilityIssue) | هذه مشكلة معروفة ستمنع فتح المستند بواسطة بعض عُملاء المستخدم أو الإصدارات السابقة من عُملاء المستخدم. |
| [UnexpectedContent](#UnexpectedContent) | بعض المحتوى في مستند المصدر لم يتم التعرف عليه (أي غير مدعوم)، وقد يؤدي ذلك إلى مشاكل أو إلى فقدان البيانات/التنسيق أو لا يؤدي إلى ذلك. |
### SourceFileCorruption {#SourceFileCorruption}
```
public static final int SourceFileCorruption
```

تم اكتشاف مشكلة في مستند المصدر تجعل من المحتمل جداً أن المستند لن يكون قابلاً للفتح إذا تم حفظه بصيغته الأصلية.

### DataLoss {#DataLoss}
```
public static final int DataLoss
```

ستُفقد النصوص/المخططات/الصور أو أي بيانات أخرى بالكامل إما من شجرة المستند بعد التحميل أو من المستند المُنشأ بعد الحفظ.

### MajorFormattingLoss {#MajorFormattingLoss}
```
public static final int MajorFormattingLoss
```

فقدان كبير للتنسيق.

### MinorFormattingLoss {#MinorFormattingLoss}
```
public static final int MinorFormattingLoss
```

فقدان طفيف للتنسيق.

### CompatibilityIssue {#CompatibilityIssue}
```
public static final int CompatibilityIssue
```

هذه مشكلة معروفة ستمنع فتح المستند بواسطة بعض عُملاء المستخدم أو الإصدارات السابقة من عُملاء المستخدم.

### UnexpectedContent {#UnexpectedContent}
```
public static final int UnexpectedContent
```

بعض المحتوى في مستند المصدر لم يتم التعرف عليه (أي غير مدعوم)، وقد يؤدي ذلك إلى مشاكل أو إلى فقدان البيانات/التنسيق أو لا يؤدي إلى ذلك.