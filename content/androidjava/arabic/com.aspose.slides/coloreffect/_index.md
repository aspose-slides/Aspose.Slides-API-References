---
title: ColorEffect
second_title: Aspose.Slides لنظام Android عبر مرجع API Java
description: يمثل تأثير لون لسلوك الرسوم المتحركة.
type: docs
url: /ar/com.aspose.slides/coloreffect/
---
**الوراثة:**  
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**جميع الواجهات المُنفذة:**  
[com.aspose.slides.IColorEffect](../../com.aspose.slides/icoloreffect)  
```
public class ColorEffect extends Behavior implements IColorEffect
```

يمثل تأثير لون لسلوك الرسوم المتحركة.

## المُنشئات

| المُنشئ | الوصف |
| --- | --- |
| [ColorEffect()](#ColorEffect--) | ينشئ مثيلًا جديدًا. |

## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getFrom()](#getFrom--) | هذه القيمة تُستخدم لتحديد اللون الابتدائي للسلوك. |
| [setFrom(IColorFormat value)](#setFrom-com.aspose.slides.IColorFormat-) | هذه القيمة تُستخدم لتحديد اللون الابتدائي للسلوك. |
| [getTo()](#getTo--) | يوصف اللون الناتج لتغيير لون الرسوم المتحركة. |
| [setTo(IColorFormat value)](#setTo-com.aspose.slides.IColorFormat-) | يوصف اللون الناتج لتغيير لون الرسوم المتحركة. |
| [getBy()](#getBy--) | يصف قيمة الإزاحة النسبية للرسوم المتحركة اللونية. |
| [setBy(IColorOffset value)](#setBy-com.aspose.slides.IColorOffset-) | يصف قيمة الإزاحة النسبية للرسوم المتحركة اللونية. |
| [getColorSpace()](#getColorSpace--) | يمثل مساحة اللون للسلوك. |
| [setColorSpace(int value)](#setColorSpace-int-) | يمثل مساحة اللون للسلوك. |
| [getDirection()](#getDirection--) | يحدد الاتجاه الذي يتم فيه تدوير درجة اللون حول عجلة الألوان. |
| [setDirection(int value)](#setDirection-int-) | يحدد الاتجاه الذي يتم فيه تدوير درجة اللون حول عجلة الألوان. |

### ColorEffect() {#ColorEffect--}
```
public ColorEffect()
```

ينشئ مثيلًا جديدًا.

### getFrom() {#getFrom--}
```
public final IColorFormat getFrom()
```

هذه القيمة تُستخدم لتحديد اللون الابتدائي للسلوك. قراءة/كتابة [IColorFormat](../../com.aspose.slides/icolorformat).

**القيمة المرجعة:**
[IColorFormat](../../com.aspose.slides/icolorformat)

### setFrom(IColorFormat value) {#setFrom-com.aspose.slides.IColorFormat-}
```
public final void setFrom(IColorFormat value)
```

هذه القيمة تُستخدم لتحديد اللون الابتدائي للسلوك. قراءة/كتابة [IColorFormat](../../com.aspose.slides/icolorformat).

**المُعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | [IColorFormat](../../com.aspose.slides/icolorformat) |  |

### getTo() {#getTo--}
```
public final IColorFormat getTo()
```

يوصف اللون الناتج لتغيير لون الرسوم المتحركة. قراءة/كتابة [IColorFormat](../../com.aspose.slides/icolorformat).

**القيمة المرجعة:**
[IColorFormat](../../com.aspose.slides/icolorformat)

### setTo(IColorFormat value) {#setTo-com.aspose.slides.IColorFormat-}
```
public final void setTo(IColorFormat value)
```

يوصف اللون الناتج لتغيير لون الرسوم المتحركة. قراءة/كتابة [IColorFormat](../../com.aspose.slides/icolorformat).

**المُعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | [IColorFormat](../../com.aspose.slides/icolorformat) |  |

### getBy() {#getBy--}
```
public final IColorOffset getBy()
```

يصف قيمة الإزاحة النسبية للرسوم المتحركة اللونية. قراءة/كتابة [IColorOffset](../../com.aspose.slides/icoloroffset).

**القيمة المرجعة:**
[IColorOffset](../../com.aspose.slides/icoloroffset)

### setBy(IColorOffset value) {#setBy-com.aspose.slides.IColorOffset-}
```
public final void setBy(IColorOffset value)
```

يصف قيمة الإزاحة النسبية للرسوم المتحركة اللونية. قراءة/كتابة [IColorOffset](../../com.aspose.slides/icoloroffset).

**المُعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | [IColorOffset](../../com.aspose.slides/icoloroffset) |  |

### getColorSpace() {#getColorSpace--}
```
public final int getColorSpace()
```

يمثل مساحة اللون للسلوك. قراءة/كتابة [ColorSpace](../../com.aspose.slides/colorspace).

**القيمة المرجعة:**
int

### setColorSpace(int value) {#setColorSpace-int-}
```
public final void setColorSpace(int value)
```

يمثل مساحة اللون للسلوك. قراءة/كتابة [ColorSpace](../../com.aspose.slides/colorspace).

**المُعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | int |  |

### getDirection() {#getDirection--}
```
public final int getDirection()
```

يحدد الاتجاه الذي يتم فيه تدوير درجة اللون حول عجلة الألوان. قراءة/كتابة [ColorDirection](../../com.aspose.slides/colordirection).

**القيمة المرجعة:**
int

### setDirection(int value) {#setDirection-int-}
```
public final void setDirection(int value)
```

يحدد الاتجاه الذي يتم فيه تدوير درجة اللون حول عجلة الألوان. قراءة/كتابة [ColorDirection](../../com.aspose.slides/colordirection).

**المُعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | int |  |