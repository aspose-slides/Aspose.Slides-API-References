---
title: ChartShapeType
second_title: Aspose.Slides لنظام Android عبر مرجع API لجافا
description: يمثل شكلًا للمخطط.
type: docs
url: /ar/com.aspose.slides/chartshapetype/
---
**الوراثة:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class ChartShapeType extends System.Enum
```

يمثل شكلًا للمخطط.
## الحقول

| الحقل | الوصف |
| --- | --- |
| [NotDefined](#NotDefined) | شكل المخطط غير معرف. |
| [Box](#Box) | يحدد أن يتم رسم المخطط بشكل صندوق. |
| [Cone](#Cone) | يحدد أن يتم رسم المخطط على شكل مخروط، بحيث يكون قاعدة المخروط على الأرض وقمة المخروط فوق علامة البيانات. |
| [ConeToMax](#ConeToMax) | يحدد أن يتم رسم المخطط باستخدام مخاريط مقصوصة بحيث تكون قمة المخروط هي قيمة البيانات القصوى. |
| [Cylinder](#Cylinder) | يحدد أن يتم رسم المخطط على شكل أسطوانة. |
| [Pyramid](#Pyramid) | يحدد أن يتم رسم المخطط على شكل هرم مستطيل، بحيث تكون قاعدة الهرم على الأرض وقمة الهرم فوق علامة البيانات. |
| [PyramidToMaximum](#PyramidToMaximum) | يحدد أن يتم رسم المخطط باستخدام مخاريط مقصوصة بحيث تكون قمة المخروط هي قيمة البيانات القصوى. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```


شكل المخطط غير معرف.

### Box {#Box}
```
public static final int Box
```


يحدد أن يتم رسم المخطط بشكل صندوق.

### Cone {#Cone}
```
public static final int Cone
```


يحدد أن يتم رسم المخطط على شكل مخروط، بحيث يكون قاعدة المخروط على الأرض وقمة المخروط فوق علامة البيانات.

### ConeToMax {#ConeToMax}
```
public static final int ConeToMax
```


يحدد أن يتم رسم المخطط باستخدام مخاريط مقصوصة بحيث تكون قمة المخروط هي قيمة البيانات القصوى.

### Cylinder {#Cylinder}
```
public static final int Cylinder
```


يحدد أن يتم رسم المخطط على شكل أسطوانة.

### Pyramid {#Pyramid}
```
public static final int Pyramid
```


يحدد أن يتم رسم المخطط على شكل هرم مستطيل، بحيث تكون قاعدة الهرم على الأرض وقمة الهرم فوق علامة البيانات.

### PyramidToMaximum {#PyramidToMaximum}
```
public static final int PyramidToMaximum
```


يحدد أن يتم رسم المخطط باستخدام مخاريط مقصوصة بحيث تكون قمة المخروط هي قيمة البيانات القصوى.