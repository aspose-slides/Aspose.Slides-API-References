---
title: IOuterShadowEffectiveData
second_title: Aspose.Slides for Android 通过 Java API 参考
description: 不可变对象，表示外部阴影效果。
type: docs
url: /zh/com.aspose.slides/ioutershadoweffectivedata/
---
**已实现接口:**
[com.aspose.slides.IEffectEffectiveData](../../com.aspose.slides/ieffecteffectivedata)
```
public interface IOuterShadowEffectiveData extends IEffectEffectiveData
```

不可变对象，表示外部阴影效果。
## 方法

| 方法 | 描述 |
| --- | --- |
| [getBlurRadius()](#getBlurRadius--) | 模糊半径。 |
| [getDirection()](#getDirection--) | 阴影方向。 |
| [getDistance()](#getDistance--) | 阴影距离。 |
| [getShadowColor()](#getShadowColor--) | 阴影颜色。 |
| [getRectangleAlign()](#getRectangleAlign--) | 矩形对齐。 |
| [getSkewHorizontal()](#getSkewHorizontal--) | 指定水平倾斜角度（以度为单位）。 |
| [getSkewVertical()](#getSkewVertical--) | 指定垂直倾斜角度（以度为单位）。 |
| [getRotateShadowWithShape()](#getRotateShadowWithShape--) | 指定当形状旋转时阴影是否随形状旋转。 |
| [getScaleHorizontal()](#getScaleHorizontal--) | 指定水平缩放因子，负值缩放会导致翻转。 |
| [getScaleVertical()](#getScaleVertical--) | 指定垂直缩放因子，负值缩放会导致翻转。 |
### getBlurRadius() {#getBlurRadius--}
```
public abstract double getBlurRadius()
```

模糊半径。只读 double.

返回:
double
### getDirection() {#getDirection--}
```
public abstract float getDirection()
```

阴影方向。只读 float.

返回:
float
### getDistance() {#getDistance--}
```
public abstract double getDistance()
```

阴影距离。只读 double.

返回:
double
### getShadowColor() {#getShadowColor--}
```
public abstract Integer getShadowColor()
```

阴影颜色。只读 java.lang.Integer.

返回:
java.lang.Integer
### getRectangleAlign() {#getRectangleAlign--}
```
public abstract byte getRectangleAlign()
```

矩形对齐。只读 [RectangleAlignment](../../com.aspose.slides/rectanglealignment).

返回:
byte
### getSkewHorizontal() {#getSkewHorizontal--}
```
public abstract double getSkewHorizontal()
```

指定水平倾斜角度（以度为单位）。只读 double.

返回:
double
### getSkewVertical() {#getSkewVertical--}
```
public abstract double getSkewVertical()
```

指定垂直倾斜角度（以度为单位）。只读 double.

返回:
double
### getRotateShadowWithShape() {#getRotateShadowWithShape--}
```
public abstract boolean getRotateShadowWithShape()
```

指定当形状旋转时阴影是否随形状旋转。只读 boolean.

返回:
boolean
### getScaleHorizontal() {#getScaleHorizontal--}
```
public abstract double getScaleHorizontal()
```

指定水平缩放因子，负值缩放会导致翻转。只读 double.

返回:
double
### getScaleVertical() {#getScaleVertical--}
```
public abstract double getScaleVertical()
```

指定垂直缩放因子，负值缩放会导致翻转。只读 double.

返回:
double