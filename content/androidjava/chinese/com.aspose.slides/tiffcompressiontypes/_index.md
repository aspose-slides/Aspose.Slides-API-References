---
title: TiffCompressionTypes
second_title: Aspose.Slides for Android via Java API 参考
description: 提供用于控制演示文稿在 TIFF 格式下压缩方式的选项。
type: docs
url: /zh/com.aspose.slides/tiffcompressiontypes/
---
**继承:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class TiffCompressionTypes extends System.Enum
```

提供用于控制演示文稿在 TIFF 格式下压缩方式的选项。

## 字段

| 字段 | 描述 |
| --- | --- |
| [Default](#Default) | 指定默认的压缩方案 (LZW)。 |
| [None](#None) | 指定不进行压缩。 |
| [CCITT3](#CCITT3) | 指定 CCITT3 压缩方案。 |
| [CCITT4](#CCITT4) | 指定 CCITT4 压缩方案。 |
| [LZW](#LZW) | 指定 LZW 压缩方案 (默认)。 |
| [RLE](#RLE) | 指定 RLE 压缩方案。 |

### Default {#Default}
```
public static final int Default
```

指定默认的压缩方案 (LZW)。

### None {#None}
```
public static final int None
```

指定不进行压缩。

### CCITT3 {#CCITT3}
```
public static final int CCITT3
```

指定 CCITT3 压缩方案。

### CCITT4 {#CCITT4}
```
public static final int CCITT4
```

指定 CCITT4 压缩方案。

### LZW {#LZW}
```
public static final int LZW
```

指定 LZW 压缩方案 (默认)。

### RLE {#RLE}
```
public static final int RLE
```

指定 RLE 压缩方案。