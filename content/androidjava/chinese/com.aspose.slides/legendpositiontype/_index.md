---
title: LegendPositionType
second_title: Aspose.Slides for Android via Java API 参考
description: 确定图表中图例的位置。
type: docs
url: /zh/com.aspose.slides/legendpositiontype/
---
**继承：**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class LegendPositionType extends System.Enum
```

确定图表中图例的位置。

## 字段

| 字段 | 描述 |
| --- | --- |
| [Bottom](#Bottom) | 指定图例应绘制在图表的底部。 |
| [Left](#Left) | 指定图例应绘制在图表的左侧。 |
| [Right](#Right) | 指定图例应绘制在图表的右侧。 |
| [Top](#Top) | 指定图例应绘制在图表的顶部。 |
| [TopRight](#TopRight) | 指定图例应绘制在图表的右上角。 |

### Bottom {#Bottom}
```
public static final int Bottom
```

指定图例应绘制在图表的底部。

### Left {#Left}
```
public static final int Left
```

指定图例应绘制在图表的左侧。

### Right {#Right}
```
public static final int Right
```

指定图例应绘制在图表的右侧。

### Top {#Top}
```
public static final int Top
```

指定图例应绘制在图表的顶部。

### TopRight {#TopRight}
```
public static final int TopRight
```

指定图例应绘制在图表的右上角。