---
title: MathSpacingRules
second_title: Aspose.Slides for Android 的 Java API 参考
description: 矩阵列之间的水平间距类型
type: docs
url: /zh/com.aspose.slides/mathspacingrules/
---
**继承:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class MathSpacingRules extends System.Enum
```

矩阵列之间的间隙（水平间距）的类型
## 字段

| 字段 | 描述 |
| --- | --- |
| [SingleSpacingGap](#SingleSpacingGap) | 单个（列为 1 em，行为 1 行） |
| [OneAndHalfSpacingGap](#OneAndHalfSpacingGap) | 一个半（列为 1.5 em，行为 1.5 行） |
| [DoubleSpacingGap](#DoubleSpacingGap) | 双倍间距（列为 2 em，行为 2 行） |
| [Exactly](#Exactly) | 精确值 对列使用 ColumnGap 的值，以 twips 为单位 对行使用 RowGap 的值，以 twips 为单位 |
| [Multiple](#Multiple) | 多重 对列使用 ColumnGap 的值，以 0.5 em 为增量 对行使用 RowGap 的值，以行数为单位 |
### SingleSpacingGap {#SingleSpacingGap}
```
public static final int SingleSpacingGap
```

单个（列为 1 em，行为 1 行）

### OneAndHalfSpacingGap {#OneAndHalfSpacingGap}
```
public static final int OneAndHalfSpacingGap
```

一个半（列为 1.5 em，行为 1.5 行）

### DoubleSpacingGap {#DoubleSpacingGap}
```
public static final int DoubleSpacingGap
```

双倍间距（列为 2 em，行为 2 行）

### Exactly {#Exactly}
```
public static final int Exactly
```

精确值 对列使用 ColumnGap 的值，以 twips 为单位 对行使用 RowGap 的值，以 twips 为单位

### Multiple {#Multiple}
```
public static final int Multiple
```

多重 对列使用 ColumnGap 的值，以 0.5 em 为增量 对行使用 RowGap 的值，以行数为单位