---
title: ScaleEffect
second_title: Aspose.Slides for Android via Java API 参考
description: 表示动画缩放效果。
type: docs
url: /zh/com.aspose.slides/scaleeffect/
---
**继承:**  
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**所有实现的接口:**  
[com.aspose.slides.IScaleEffect](../../com.aspose.slides/iscaleeffect)  
```
public class ScaleEffect extends Behavior implements IScaleEffect
```

表示动画缩放效果。

## 构造函数

| 构造函数 | 描述 |
| --- | --- |
| [ScaleEffect()](#ScaleEffect--) |  |

## 方法

| 方法 | 描述 |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | 确定是否应对内容进行缩放。 |
| [setZoomContent(byte value)](#setZoomContent-byte-) | 确定是否应对内容进行缩放。 |
| [getFrom()](#getFrom--) | 指定动画开始的 x/y 坐标（以百分比表示）。 |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | 指定动画开始的 x/y 坐标（以百分比表示）。 |
| [getTo()](#getTo--) | 指定动画缩放效果的目标位置（以百分比表示）。 |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | 指定动画缩放效果的目标位置（以百分比表示）。 |
| [getBy()](#getBy--) | 描述动画的相对偏移值（以百分比表示）。 |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | 描述动画的相对偏移值（以百分比表示）。 |

### ScaleEffect() {#ScaleEffect--}
```
public ScaleEffect()
```

### getZoomContent() {#getZoomContent--}
```
public final byte getZoomContent()
```

确定是否应对内容进行缩放。读写 [NullableBool](../../com.aspose.slides/nullablebool)。

**返回:**  
byte

### setZoomContent(byte value) {#setZoomContent-byte-}
```
public final void setZoomContent(byte value)
```

确定是否应对内容进行缩放。读写 [NullableBool](../../com.aspose.slides/nullablebool)。

**参数:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public final PointF getFrom()
```

指定动画开始的 x/y 坐标（以百分比表示）。读写 android.graphics.PointF。

**返回:**  
android.graphics.PointF

### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public final void setFrom(PointF value)
```

指定动画开始的 x/y 坐标（以百分比表示）。读写 android.graphics.PointF。

**参数:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public final PointF getTo()
```

指定动画缩放效果的目标位置（以百分比表示）。读写 android.graphics.PointF。

**返回:**  
android.graphics.PointF

### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public final void setTo(PointF value)
```

指定动画缩放效果的目标位置（以百分比表示）。读写 android.graphics.PointF。

**参数:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public final PointF getBy()
```

描述动画的相对偏移值（以百分比表示）。读写 android.graphics.PointF。

**返回:**  
android.graphics.PointF

### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public final void setBy(PointF value)
```

描述动画的相对偏移值（以百分比表示）。读写 android.graphics.PointF。

**参数:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| value | android.graphics.PointF |  |