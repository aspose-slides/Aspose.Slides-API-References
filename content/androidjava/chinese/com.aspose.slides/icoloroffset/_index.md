---
title: IColorOffset
second_title: Aspose.Slides for Android via Java API 参考文档
description: 表示颜色偏移。
type: docs
url: /zh/com.aspose.slides/icoloroffset/
---```
public interface IColorOffset
```

表示颜色偏移。
## 方法

| 方法 | 描述 |
| --- | --- |
| [getValue0()](#getValue0--) | 定义偏移的第一个值。 |
| [setValue0(float value)](#setValue0-float-) | 定义偏移的第一个值。 |
| [getValue1()](#getValue1--) | 定义偏移的第二个值。 |
| [setValue1(float value)](#setValue1-float-) | 定义偏移的第二个值。 |
| [getValue2()](#getValue2--) | 定义偏移的第三个值。 |
| [setValue2(float value)](#setValue2-float-) | 定义偏移的第三个值。 |
### getValue0() {#getValue0--}
```
public abstract float getValue0()
```

定义偏移的第一个值。读取/写入 float。

**返回:**  
float
### setValue0(float value) {#setValue0-float-}
```
public abstract void setValue0(float value)
```

定义偏移的第一个值。读取/写入 float。

**参数:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| value | float |  |
### getValue1() {#getValue1--}
```
public abstract float getValue1()
```

定义偏移的第二个值。读取/写入 float。

**返回:**  
float
### setValue1(float value) {#setValue1-float-}
```
public abstract void setValue1(float value)
```

定义偏移的第二个值。读取/写入 float。

**参数:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| value | float |  |
### getValue2() {#getValue2--}
```
public abstract float getValue2()
```

定义偏移的第三个值。读取/写入 float。

**返回:**  
float
### setValue2(float value) {#setValue2-float-}
```
public abstract void setValue2(float value)
```

定义偏移的第三个值。读取/写入 float。

**参数:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| value | float |  |