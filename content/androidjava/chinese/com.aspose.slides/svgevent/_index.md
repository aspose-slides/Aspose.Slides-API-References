---
title: SvgEvent
second_title: Aspose.Slides for Android via Java API 参考文档
description: 表示 SVG 形状的选项。
type: docs
url: /zh/com.aspose.slides/svgevent/
---
**继承:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class SvgEvent extends System.Enum
```

表示 SVG 形状的选项。

## 字段

| 字段 | 描述 |
| --- | --- |
| [OnFocusIn](#OnFocusIn) | 当元素获得焦点时触发，例如当文本被选中时。 |
| [OnFocusOut](#OnFocusOut) | 当元素失去焦点时触发，例如当文本被取消选择时。 |
| [OnActivate](#OnActivate) | 当元素被激活时触发，例如通过鼠标点击或按键。 |
| [OnClick](#OnClick) | 当指针设备按钮在元素上被点击时触发。 |
| [OnMouseDown](#OnMouseDown) | 当指针设备按钮在元素上被按下时触发。 |
| [OnMouseUp](#OnMouseUp) | 当指针设备按钮在元素上被释放时触发。 |
| [OnMouseOver](#OnMouseOver) | 当指针设备移动到元素上时触发。 |
| [OnMouseMove](#OnMouseMove) | 当指针设备在元素上移动时触发。 |
| [OnMouseOut](#OnMouseOut) | 当指针设备离开元素时触发。 |
| [OnLoad](#OnLoad) | 当用户代理已完全解析元素及其子元素以及渲染所需的所有引用资源时触发。 |
| [OnUnload](#OnUnload) | 当 DOM 实现从窗口或框架中移除文档时触发。 |
| [OnAbort](#OnAbort) | 当页面加载在元素完全加载之前被停止时触发。 |
| [OnError](#OnError) | 当元素未正确加载或脚本执行期间出现错误时触发。 |
| [OnResize](#OnResize) | 当文档视图正在调整大小时触发。 |
| [OnScroll](#OnScroll) | 当文档视图沿 X、Y 或两轴移动时触发。 |
| [OnZoom](#OnZoom) | 当文档视图的缩放级别改变时触发。 |
| [OnBegin](#OnBegin) | 当动画元素开始时触发。 |
| [OnEnd](#OnEnd) | 当动画元素结束时触发。 |
| [OnRepeat](#OnRepeat) | 当动画元素重复时触发。 |

### OnFocusIn {#OnFocusIn}
```
public static final int OnFocusIn
```

当元素获得焦点时触发，例如当文本被选中时。

### OnFocusOut {#OnFocusOut}
```
public static final int OnFocusOut
```

当元素失去焦点时触发，例如当文本被取消选择时。

### OnActivate {#OnActivate}
```
public static final int OnActivate
```

当元素被激活时触发，例如通过鼠标点击或按键。

### OnClick {#OnClick}
```
public static final int OnClick
```

当指针设备按钮在元素上被点击时触发。

### OnMouseDown {#OnMouseDown}
```
public static final int OnMouseDown
```

当指针设备按钮在元素上被按下时触发。

### OnMouseUp {#OnMouseUp}
```
public static final int OnMouseUp
```

当指针设备按钮在元素上被释放时触发。

### OnMouseOver {#OnMouseOver}
```
public static final int OnMouseOver
```

当指针设备移动到元素上时触发。

### OnMouseMove {#OnMouseMove}
```
public static final int OnMouseMove
```

当指针设备在元素上移动时触发。

### OnMouseOut {#OnMouseOut}
```
public static final int OnMouseOut
```

当指针设备离开元素时触发。

### OnLoad {#OnLoad}
```
public static final int OnLoad
```

当用户代理已完全解析元素及其子元素以及渲染所需的所有引用资源时触发。

### OnUnload {#OnUnload}
```
public static final int OnUnload
```

当 DOM 实现从窗口或框架中移除文档时触发。仅适用于最外层的 svg 元素。

### OnAbort {#OnAbort}
```
public static final int OnAbort
```

当页面加载在元素完全加载之前被停止时触发。

### OnError {#OnError}
```
public static final int OnError
```

当元素未正确加载或脚本执行期间出现错误时触发。

### OnResize {#OnResize}
```
public static final int OnResize
```

当文档视图正在调整大小时触发。仅适用于最外层的 svg 元素。

### OnScroll {#OnScroll}
```
public static final int OnScroll
```

当文档视图沿 X、Y 或两轴移动时触发。仅适用于最外层的 svg 元素。

### OnZoom {#OnZoom}
```
public static final int OnZoom
```

当文档视图的缩放级别改变时触发。仅适用于最外层的 svg 元素。

### OnBegin {#OnBegin}
```
public static final int OnBegin
```

当动画元素开始时触发。

### OnEnd {#OnEnd}
```
public static final int OnEnd
```

当动画元素结束时触发。

### OnRepeat {#OnRepeat}
```
public static final int OnRepeat
```

当动画元素重复时触发。