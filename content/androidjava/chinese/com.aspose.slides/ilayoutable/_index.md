---
title: ILayoutable
second_title: Aspose.Slides for Android via Java API 参考
description: 指定图表元素的精确位置。
type: docs
url: /zh/com.aspose.slides/ilayoutable/
---
**所有实现的接口：**
[com.aspose.slides.IChartComponent](../../com.aspose.slides/ichartcomponent)
```
public interface ILayoutable extends IChartComponent
```

指定图表元素的精确位置。
## 方法

| 方法 | 描述 |
| --- | --- |
| [getX()](#getX--) | 指定图表元素的 x 位置（左）为图表宽度的比例。 |
| [setX(float value)](#setX-float-) | 指定图表元素的 x 位置（左）为图表宽度的比例。 |
| [getY()](#getY--) | 指定图表元素的顶部为图表高度的比例。 |
| [setY(float value)](#setY-float-) | 指定图表元素的顶部为图表高度的比例。 |
| [getWidth()](#getWidth--) | 指定图表元素的宽度为图表宽度的比例。 |
| [setWidth(float value)](#setWidth-float-) | 指定图表元素的宽度为图表宽度的比例。 |
| [getHeight()](#getHeight--) | 指定图表元素的高度为图表高度的比例。 |
| [setHeight(float value)](#setHeight-float-) | 指定图表元素的高度为图表高度的比例。 |
| [getRight()](#getRight--) | 获取图表元素的右侧位置，为图表宽度的比例。 |
| [getBottom()](#getBottom--) | 获取图表元素的顶部，为图表高度的比例。 |
### getX() {#getX--}
```
public abstract float getX()
```

指定图表元素的 x 位置（左）为图表宽度的比例。可读写 float。

**返回值：**
float
### setX(float value) {#setX-float-}
```
public abstract void setX(float value)
```

指定图表元素的 x 位置（左）为图表宽度的比例。可读写 float。

**参数：**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| value | float |  |

### getY() {#getY--}
```
public abstract float getY()
```

指定图表元素的顶部为图表高度的比例。可读写 float。

**返回值：**
float
### setY(float value) {#setY-float-}
```
public abstract void setY(float value)
```

指定图表元素的顶部为图表高度的比例。可读写 float。

**参数：**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| value | float |  |

### getWidth() {#getWidth--}
```
public abstract float getWidth()
```

指定图表元素的宽度为图表宽度的比例。可读写 float。

**返回值：**
float
### setWidth(float value) {#setWidth-float-}
```
public abstract void setWidth(float value)
```

指定图表元素的宽度为图表宽度的比例。可读写 float。

**参数：**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| value | float |  |

### getHeight() {#getHeight--}
```
public abstract float getHeight()
```

指定图表元素的高度为图表高度的比例。可读写 float。

**返回值：**
float
### setHeight(float value) {#setHeight-float-}
```
public abstract void setHeight(float value)
```

指定图表元素的高度为图表高度的比例。可读写 float。

**参数：**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| value | float |  |

### getRight() {#getRight--}
```
public abstract float getRight()
```

获取图表元素的右侧位置，为图表宽度的比例。只读 float。

**返回值：**
float
### getBottom() {#getBottom--}
```
public abstract float getBottom()
```

获取图表元素的顶部，为图表高度的比例。只读 float。

**返回值：**
float