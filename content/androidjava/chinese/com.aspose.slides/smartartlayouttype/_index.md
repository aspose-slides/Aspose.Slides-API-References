---
title: SmartArtLayoutType
second_title: Aspose.Slides for Android 的 Java API 参考
description: 表示 SmartArt 图表的布局类型。
type: docs
url: /zh/com.aspose.slides/smartartlayouttype/
---
**继承:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class SmartArtLayoutType extends System.Enum
```

表示 SmartArt 图表的布局类型。
## 字段

| 字段 | 描述 |
| --- | --- |
| [AccentProcess](#AccentProcess) | 用于展示任务、流程或工作流中的进展、时间线或顺序步骤。 |
| [AccentedPicture](#AccentedPicture) | 用于展示中心的摄影概念以及旁边的相关想法。 |
| [AlternatingFlow](#AlternatingFlow) | 用于展示任务、流程或工作流中的信息组或顺序步骤。 |
| [AlternatingHexagons](#AlternatingHexagons) | 用于表示一系列相互关联的想法。 |
| [AlternatingPictureBlocks](#AlternatingPictureBlocks) | 用于从上到下展示一系列图片。 |
| [AlternatingPictureCircles](#AlternatingPictureCircles) | 用于展示带文字的一组图片。 |
| [ArrowRibbon](#ArrowRibbon) | 用于展示具有某种关联的相关或对立概念，例如对立力量。 |
| [AscendingPictureAccentProcess](#AscendingPictureAccentProcess) | 用于展示带说明文字的递增图片序列。 |
| [Balance](#Balance) | 用于比较或展示两个想法之间的关系。 |
| [BasicBendingProcess](#BasicBendingProcess) | 用于展示任务、流程或工作流中的进展或顺序步骤。 |
| [BasicBlockList](#BasicBlockList) | 用于展示非顺序或分组的信息块。 |
| [BasicChevronProcess](#BasicChevronProcess) | 用于展示进展、时间线、顺序步骤，或强调运动或方向。 |
| [BasicCycle](#BasicCycle) | 用于表示循环流程中阶段、任务或事件的连续序列。 |
| [BasicMatrix](#BasicMatrix) | 用于在四象限中展示组件与整体的关系。 |
| [BasicPie](#BasicPie) | 用于展示各个部分如何构成整体。 |
| [BasicProcess](#BasicProcess) | 用于展示任务、流程或工作流中的进展或顺序步骤。 |
| [BasicPyramid](#BasicPyramid) | 用于展示底部最大、向上收窄的比例、相互关联或层级关系。 |
| [BasicRadial](#BasicRadial) | 用于展示循环中与中心思想的关系。 |
| [BasicTarget](#BasicTarget) | 用于展示包含层次或等级关系的包含关系。 |
| [BasicTimeline](#BasicTimeline) | 用于展示任务、流程或工作流中的顺序步骤或时间线信息。 |
| [BasicVenn](#BasicVenn) | 用于展示重叠或相互关联的关系。 |
| [BendingPictureAccentList](#BendingPictureAccentList) | 用于展示非顺序或分组的信息块。 |
| [BendingPictureBlocks](#BendingPictureBlocks) | 用于展示一系列图片。 |
| [BendingPictureCaption](#BendingPictureCaption) | 用于展示顺序的图片系列。 |
| [BendingPictureCaptionList](#BendingPictureCaptionList) | 用于展示一系列图片。 |
| [BendingPictureSemiTransparentText](#BendingPictureSemiTransparentText) | 用于展示一系列图片。 |
| [BlockCycle](#BlockCycle) | 用于表示循环流程中阶段、任务或事件的连续序列。 |
| [BubblePictureList](#BubblePictureList) | 用于展示一系列图片。 |
| [CaptionedPictures](#CaptionedPictures) | 用于展示带多层文本的图片。 |
| [ChevronList](#ChevronList) | 用于展示构成整体工作流的多个过程的进展。 |
| [CircleAccentTimeline](#CircleAccentTimeline) | 用于展示一系列事件或时间线信息。 |
| [CircleArrowProcess](#CircleArrowProcess) | 用于展示每个项目都有配套说明文本的顺序项。 |
| [CirclePictureHierarchy](#CirclePictureHierarchy) | 用于展示组织内部的层级信息或汇报关系。 |
| [CircleRelationship](#CircleRelationship) | 用于展示与中心思想的关系或来源。 |
| [CircularBendingProcess](#CircularBendingProcess) | 用于展示任务、流程或工作流中的长链或非线性序列。 |
| [CircularPictureCallout](#CircularPictureCallout) | 用于展示中心思想及子思想或相关项目。 |
| [ClosedChevronProcess](#ClosedChevronProcess) | 用于展示进展、时间线或顺序步骤，或强调运动或方向。 |
| [ContinuousArrowProcess](#ContinuousArrowProcess) | 用于展示任务、流程或工作流中的时间线或顺序步骤。 |
| [ContinuousBlockProcess](#ContinuousBlockProcess) | 用于展示任务、流程或工作流中的进展或顺序步骤。 |
| [ContinuousCycle](#ContinuousCycle) | 用于表示循环流程中阶段、任务或事件的连续序列。 |
| [ContinuousPictureList](#ContinuousPictureList) | 用于展示相互关联的信息组。 |
| [ConvergingArrows](#ConvergingArrows) | 用于展示汇聚到中心点的想法或概念。 |
| [ConvergingRadial](#ConvergingRadial) | 用于展示概念或组件相对于循环中心思想的关系。 |
| [CounterbalanceArrows](#CounterbalanceArrows) | 用于展示两种对立的想法或概念。 |
| [CycleMatrix](#CycleMatrix) | 用于展示与循环进程中中心思想的关系。 |
| [DescendingBlockList](#DescendingBlockList) | 用于展示相关想法的分组或信息列表。 |
| [DescendingProcess](#DescendingProcess) | 用于展示递减的事件序列。 |
| [DetailedProcess](#DetailedProcess) | 用于在大量二级文本的情况下展示阶段性进展。 |
| [DivergingArrows](#DivergingArrows) | 用于展示从中心源向外扩散的想法或概念。 |
| [DivergingRadial](#DivergingRadial) | 用于展示循环中与中心思想的关系。 |
| [Equation](#Equation) | 用于展示计划或结果的顺序步骤或任务。 |
| [FramedTextPicture](#FramedTextPicture) | 用于在框架中展示对应一级文本的图片。 |
| [Funnel](#Funnel) | 用于展示信息的筛选或部件如何合并为整体。 |
| [Gear](#Gear) | 用于展示相互啮合的想法。 |
| [GridMatrix](#GridMatrix) | 用于展示概念在两个轴上的放置。 |
| [GroupedList](#GroupedList) | 用于展示信息的分组与子分组，或任务、流程中的步骤与子步骤。 |
| [HalfCircleOrganizationChart](#HalfCircleOrganizationChart) | 用于展示组织内部的层级信息或汇报关系。 |
| [HexagonCluster](#HexagonCluster) | 用于展示带说明文字的图片。 |
| [Hierarchy](#Hierarchy) | 用于展示从上到下递进的层级关系。 |
| [HierarchyList](#HierarchyList) | 用于展示跨组的层级关系。 |
| [HorizontalBulletList](#HorizontalBulletList) | 用于展示非顺序或分组的信息列表。 |
| [HorizontalHierarchy](#HorizontalHierarchy) | 用于展示横向递进的层级关系。 |
| [HorizontalLabeledHierarchy](#HorizontalLabeledHierarchy) | 用于展示横向递进且层级分组的层级关系。 |
| [HorizontalMultiLevelHierarchy](#HorizontalMultiLevelHierarchy) | 用于展示大量横向递进的层级信息。 |
| [HorizontalOrganizationChart](#HorizontalOrganizationChart) | 用于展示横向递进的层级信息或组织的汇报关系。 |
| [HorizontalPictureList](#HorizontalPictureList) | 用于在强调相关图片的情况下展示非顺序或分组信息。 |
| [IncreasingArrowsProcess](#IncreasingArrowsProcess) | 用于展示过程中的顺序与重叠步骤。 |
| [IncreasingCircleProcess](#IncreasingCircleProcess) | 用于展示一系列步骤，圆圈内部随每步增大。 |
| [InvertedPyramid](#InvertedPyramid) | 用于展示顶部最大、向下收窄的比例、相互关联或层级关系。 |
| [LabeledHierarchy](#LabeledHierarchy) | 用于展示从上到下递进且层级分组的层级关系。 |
| [LinearVenn](#LinearVenn) | 用于展示序列中的重叠关系。 |
| [LinedList](#LinedList) | 用于展示分为类别和子类别的大量文本。 |
| [MultidirectionalCycle](#MultidirectionalCycle) | 用于表示可以任意方向发生的阶段、任务或事件的连续序列。 |
| [NameandTitleOrganizationChart](#NameandTitleOrganizationChart) | 用于展示组织内部的层级信息或汇报关系。 |
| [NestedTarget](#NestedTarget) | 用于展示包含关系。 |
| [NondirectionalCycle](#NondirectionalCycle) | 用于表示循环流程中阶段、任务或事件的连续序列。 |
| [OpposingArrows](#OpposingArrows) | 用于展示两种对立或从中心点分开的想法。 |
| [OpposingIdeas](#OpposingIdeas) | 用于展示两种对立或对比的想法。 |
| [OrganizationChart](#OrganizationChart) | 用于展示组织内部的层级信息或汇报关系。 |
| [PhasedProcess](#PhasedProcess) | 用于展示过程的三个阶段。 |
| [PictureAccentBlocks](#PictureAccentBlocks) | 用于从角落开始以块状展示一组图片。 |
| [PictureAccentList](#PictureAccentList) | 用于展示已分组或相关的信息。 |
| [PictureAccentProcess](#PictureAccentProcess) | 用于展示任务、流程或工作流中的顺序步骤。 |
| [PictureCaptionList](#PictureCaptionList) | 用于展示非顺序或分组的信息块。 |
| [PictureGrid](#PictureGrid) | 用于在方形网格上展示图片。 |
| [PictureLineup](#PictureLineup) | 用于并排展示一系列图片。 |
| [PictureStrips](#PictureStrips) | 用于从上到下展示一系列图片，左侧配一级文本。 |
| [PieProcess](#PieProcess) | 用于在过程的每个饼形块中逐步增大，最多七个形状。 |
| [PlusandMinus](#PlusandMinus) | 用于展示两个想法的利弊。 |
| [ProcessArrows](#ProcessArrows) | 用于展示说明过程或工作流的信息。 |
| [ProcessList](#ProcessList) | 用于展示信息组或任务、流程的步骤与子步骤。 |
| [PyramidList](#PyramidList) | 用于展示比例、相互关联或层级关系。 |
| [RadialCluster](#RadialCluster) | 用于展示与中心思想或主题相关的数据。 |
| [RadialCycle](#RadialCycle) | 用于展示与中心思想的关系。 |
| [RadialList](#RadialList) | 用于展示循环中与中心思想的关系。 |
| [RadialVenn](#RadialVenn) | 用于同时展示重叠关系及循环中与中心思想的关系。 |
| [RandomToResultProcess](#RandomToResultProcess) | 通过一系列步骤展示多个混乱想法如何汇聚为统一目标。 |
| [RepeatingBendingProcess](#RepeatingBendingProcess) | 用于展示任务、流程或工作流中的进展或顺序步骤。 |
| [ReverseList](#ReverseList) | 用于在两个项目之间切换。 |
| [SegmentedCycle](#SegmentedCycle) | 用于展示循环流程中阶段、任务或事件的进展或序列。 |
| [SegmentedProcess](#SegmentedProcess) | 用于展示任务、流程或工作流中的进展或顺序步骤。 |
| [SegmentedPyramid](#SegmentedPyramid) | 用于展示包含、比例或相互关联的关系。 |
| [SnapshotPictureList](#SnapshotPictureList) | 用于展示带说明文字的图片。 |
| [SpiralPicture](#SpiralPicture) | 用于展示最多五张图片的螺旋式排列，配相应一级标题。 |
| [SquareAccentList](#SquareAccentList) | 用于按类别划分信息列表。 |
| [StackedList](#StackedList) | 用于展示信息组或任务、流程的步骤。 |
| [StackedVenn](#StackedVenn) | 用于展示重叠关系。 |
| [StaggeredProcess](#StaggeredProcess) | 用于展示向下递进的阶段。 |
| [StepDownProcess](#StepDownProcess) | 用于展示多步且带子步骤的递减过程。 |
| [StepUpProcess](#StepUpProcess) | 用于展示递增的步骤或信息列表。 |
| [SubStepProcess](#SubStepProcess) | 用于在每个一级文本之间展示子步骤的多步骤过程。 |
| [TableHierarchy](#TableHierarchy) | 用于自上而下构建信息组及其层级。 |
| [TableList](#TableList) | 用于展示价值相等的已分组或相关信息。 |
| [TargetList](#TargetList) | 用于展示相互关联或重叠的信息。 |
| [TextCycle](#TextCycle) | 用于表示循环流程中阶段、任务或事件的连续序列。 |
| [TitlePictureLineup](#TitlePictureLineup) | 用于展示每张图片都有独立标题和描述的系列。 |
| [TitledMatrix](#TitledMatrix) | 用于展示四象限与整体的关系。 |
| [TitledPictureAccentList](#TitledPictureAccentList) | 用于为每个二级文本配以强调图片的列表。 |
| [TitledPictureBlocks](#TitledPictureBlocks) | 用于展示一系列图片。 |
| [TrapezoidList](#TrapezoidList) | 用于展示价值相等的已分组或相关信息。 |
| [UpwardArrow](#UpwardArrow) | 用于展示任务、流程或工作流中向上趋势的进展或步骤。 |
| [VerticalAccentList](#VerticalAccentList) | 用于展示信息列表。 |
| [VerticalArrowList](#VerticalArrowList) | 用于展示向共同目标前进的进展或顺序步骤。 |
| [VerticalBendingProcess](#VerticalBendingProcess) | 用于展示任务、流程或工作流中的进展或顺序步骤。 |
| [VerticalBlockList](#VerticalBlockList) | 用于展示信息组或任务、流程的步骤。 |
| [VerticalBoxList](#VerticalBoxList) | 用于展示多个信息组，尤其是二级文本较多的组。 |
| [VerticalBulletList](#VerticalBulletList) | 用于展示非顺序或分组的信息块。 |
| [VerticalChevronList](#VerticalChevronList) | 用于展示任务、流程或工作流中的进展或顺序步骤，或强调运动或方向。 |
| [VerticalCircleList](#VerticalCircleList) | 用于展示顺序或分组数据。 |
| [VerticalCurvedList](#VerticalCurvedList) | 用于展示弧形信息列表。 |
| [VerticalEquation](#VerticalEquation) | 用于展示计划或结果的顺序步骤或任务。 |
| [VerticalPictureAccentList](#VerticalPictureAccentList) | 用于展示非顺序或分组的信息块。 |
| [VerticalPictureList](#VerticalPictureList) | 用于展示非顺序或分组的信息块。 |
| [VerticalProcess](#VerticalProcess) | 用于从上到下展示任务、流程或工作流的进展或顺序步骤。 |
| [Custom](#Custom) | 表示具有自定义布局模板的 SmartArt 图表 |
| [PictureOrganizationChart](#PictureOrganizationChart) | 用于展示带对应图片的组织层级信息或汇报关系。 |
### AccentProcess {#AccentProcess}
```
public static final int AccentProcess
```

用于展示任务、流程或工作流中的进展、时间线或顺序步骤。适用于说明一级和二级文本。

### AccentedPicture {#AccentedPicture}
```
public static final int AccentedPicture
```

用于展示中心的摄影概念以及旁边的相关想法。顶部一级文本显示在中心图片上。其他一级形状的相应文本显示在小圆形图片旁边。此布局在没有文本时也能很好使用。

### AlternatingFlow {#AlternatingFlow}
```
public static final int AlternatingFlow
```

用于展示信息组或任务、流程的顺序步骤。强调信息组之间的交互或关系。

### AlternatingHexagons {#AlternatingHexagons}
```
public static final int AlternatingHexagons
```

用于表示相互关联的想法。一级文本显示在六边形内部，二级文本显示在形状外部。

### AlternatingPictureBlocks {#AlternatingPictureBlocks}
```
public static final int AlternatingPictureBlocks
```

用于从上到下展示一系列图片。文本交替显示在图片的右侧或左侧。

### AlternatingPictureCircles {#AlternatingPictureCircles}
```
public static final int AlternatingPictureCircles
```

用于展示带文本的一组图片。对应文本显示在中心圆圈内，图片左右交替排列。

### ArrowRibbon {#ArrowRibbon}
```
public static final int ArrowRibbon
```

用于展示具有某种关联的相关或对立概念，例如对立力量。一级文本的前两行用于箭头中的文字。未使用的文本不显示，但在切换布局时仍可用。

### AscendingPictureAccentProcess {#AscendingPictureAccentProcess}
```
public static final int AscendingPictureAccentProcess
```

用于展示带说明文字的递增图片系列。最适合少量文本。

### Balance {#Balance}
```
public static final int Balance
```

用于比较或展示两个想法之间的关系。前两行一级文本对应中心点两侧顶部的文字。强调二级文本，二级文本限每侧四个形状。平衡倾向于二级文本形状更多的一侧。未使用的文本不显示，但在切换布局时仍可用。

### BasicBendingProcess {#BasicBendingProcess}
```
public static final int BasicBendingProcess
```

用于展示任务、流程或工作流中的进展或顺序步骤。最大化形状的水平和垂直显示空间。

### BasicBlockList {#BasicBlockList}
```
public static final int BasicBlockList
```

用于展示非顺序或分组的信息块。最大化形状的水平和垂直显示空间。

### BasicChevronProcess {#BasicChevronProcess}
```
public static final int BasicChevronProcess
```

用于展示进展、时间线、顺序步骤或强调运动或方向。一级文本显示在箭头形状内部，二级文本显示在箭头形状下方。

### BasicCycle {#BasicCycle}
```
public static final int BasicCycle
```

用于表示循环流程中阶段、任务或事件的连续序列。强调阶段或步骤，而非连接箭头或流向。最适合仅使用一级文本。

### BasicMatrix {#BasicMatrix}
```
public static final int BasicMatrix
```

用于展示四象限中组件与整体的关系。前四行一级文本出现在象限中。未使用的文本不显示，但在切换布局时仍可用。

### BasicPie {#BasicPie}
```
public static final int BasicPie
```

用于展示各部分如何构成整体。前七行一级文本对应均匀分布的楔形或饼形。顶部一级文本形状位于饼形之外以示强调。未使用的文本不显示，但在切换布局时仍可用。

### BasicProcess {#BasicProcess}
```
public static final int BasicProcess
```

用于展示任务、流程或工作流中的进展或顺序步骤。

### BasicPyramid {#BasicPyramid}
```
public static final int BasicPyramid
```

用于展示底部最大、向上收窄的比例、相互关联或层级关系。一级文本显示在金字塔段落中，二级文本显示在每段旁边的形状中。

### BasicRadial {#BasicRadial}
```
public static final int BasicRadial
```

用于展示循环中与中心思想的关系。第一行一级文本对应中心形状，其二级文本对应周围圆形。未使用的文本不显示，但在切换布局时仍可用。

### BasicTarget {#BasicTarget}
```
public static final int BasicTarget
```

用于展示包含、递进或层级关系。前五行一级文本关联一个圆圈。未使用的文本不显示，但在切换布局时仍可用。

### BasicTimeline {#BasicTimeline}
```
public static final int BasicTimeline
```

用于展示任务、流程或工作流中的顺序步骤，或展示时间线信息。适用于一级和二级文本。

### BasicVenn {#BasicVenn}
```
public static final int BasicVenn
```

用于展示重叠或相互关联的关系。前七行一级文本对应一个圆形。若一级文本不超过四行，文本放在圆形内部；超过四行则放在圆形外部。未使用的文本不显示，但在切换布局时仍可用。

### BendingPictureAccentList {#BendingPictureAccentList}
```
public static final int BendingPictureAccentList
```

用于展示非顺序或分组的信息块。小圆形设计用于容纳图片。适用于说明一级和二级文本。最大化形状的水平和垂直显示空间。

### BendingPictureBlocks {#BendingPictureBlocks}
```
public static final int BendingPictureBlocks
```

用于展示一系列图片。覆盖底部角落的框可容纳少量文本。

### BendingPictureCaption {#BendingPictureCaption}
```
public static final int BendingPictureCaption
```

用于展示顺序图片系列。覆盖底部角落的框可容纳少量文本。

### BendingPictureCaptionList {#BendingPictureCaptionList}
```
public static final int BendingPictureCaptionList
```

用于展示一系列图片。标题和描述出现在每张图片下方的标注形状中。

### BendingPictureSemiTransparentText {#BendingPictureSemiTransparentText}
```
public static final int BendingPictureSemiTransparentText
```

用于展示一系列图片。半透明框覆盖图片下部并容纳所有层级文本。

### BlockCycle {#BlockCycle}
```
public static final int BlockCycle
```

用于表示循环流程中阶段、任务或事件的连续序列。强调阶段或步骤，而非连接箭头或流向。

### BubblePictureList {#BubblePictureList}
```
public static final int BubblePictureList
```

用于展示一系列图片。可容纳最多八张一级图片。未使用的文本和图片不显示，但在切换布局时仍可用。最适合少量文本。

### CaptionedPictures {#CaptionedPictures}
```
public static final int CaptionedPictures
```

用于展示带多层文本的图片。最适合少量一级文本和中等量二级文本。

### ChevronList {#ChevronList}
```
public static final int ChevronList
```

用于展示构成整体工作流的多个过程的进展。也适用于说明对比过程。一级文本对应左侧的第一个箭头形状，二级文本对应每个包含一级文本的形状的水平子步骤。

### CircleAccentTimeline {#CircleAccentTimeline}
```
public static final int CircleAccentTimeline
```

用于展示一系列事件或时间线信息。一级文本显示在较大圆形旁边，二级文本显示在较小圆形旁边。

### CircleArrowProcess {#CircleArrowProcess}
```
public static final int CircleArrowProcess
```

用于展示每个项目都有配套说明文本的顺序项。此图表最适合少量一级文本。

### CirclePictureHierarchy {#CirclePictureHierarchy}
```
public static final int CirclePictureHierarchy
```

用于展示组织层级信息或汇报关系。图片显示在圆形中，相应文本显示在图片旁边。

### CircleRelationship {#CircleRelationship}
```
public static final int CircleRelationship
```

用于展示与中心思想的关系或来源。二级文本非顺序添加，限五项。只能有一项一级文本。

### CircularBendingProcess {#CircularBendingProcess}
```
public static final int CircularBendingProcess
```

用于展示任务、流程或工作流中的长链或非线性序列。最适合仅使用一级文本。最大化形状的水平和垂直显示空间。

### CircularPictureCallout {#CircularPictureCallout}
```
public static final int CircularPictureCallout
```

用于展示中心思想及子思想或相关项目。第一张图片的文本覆盖图片下部。其他一级形状的相应文本显示在小圆形图片旁。此图在没有文本时也能很好使用。

### ClosedChevronProcess {#ClosedChevronProcess}
```
public static final int ClosedChevronProcess
```

用于展示进展、时间线或顺序步骤，或强调运动或方向。可用于强调起始形状的信息。最适合仅使用一级文本。

### ContinuousArrowProcess {#ContinuousArrowProcess}
```
public static final int ContinuousArrowProcess
```

用于展示任务、流程或工作流中的时间线或顺序步骤。最适合仅使用一级文本，因为每行一级文本出现在箭头内部。二级文本出现在箭头外部。

### ContinuousBlockProcess {#ContinuousBlockProcess}
```
public static final int ContinuousBlockProcess
```

用于展示任务、流程或工作流中的进展或顺序步骤。最适合最少的一级和二级文本。

### ContinuousCycle {#ContinuousCycle}
```
public static final int ContinuousCycle
```

用于表示循环流程中阶段、任务或事件的连续序列。强调所有组件之间的连接。最适合仅使用一级文本。

### ContinuousPictureList {#ContinuousPictureList}
```
public static final int ContinuousPictureList
```

用于展示相互关联的信息组。圆形设计用于容纳图片。

### ConvergingArrows {#ConvergingArrows}
```
public static final int ConvergingArrows
```

用于展示汇聚到中心点的想法或概念。最适合仅使用一级文本。

### ConvergingRadial {#ConvergingRadial}
```
public static final int ConvergingRadial
```

用于展示概念或组件相对于循环中心思想的关系。第一行一级文本对应中心圆形，二级文本对应周围矩形。未使用的文本不显示，但在切换布局时仍可用。

### CounterbalanceArrows {#CounterbalanceArrows}
```
public static final int CounterbalanceArrows
```

用于展示两种对立的想法或概念。前两行一级文本对应箭头，配合二级文本使用效果好。未使用的文本不显示，但在切换布局时仍可用。

### CycleMatrix {#CycleMatrix}
```
public static final int CycleMatrix
```

用于展示循环进程中与中心思想的关系。前四行一级文本对应楔形或饼形，二级文本显示在楔形或饼形旁的矩形中。未使用的文本不显示，但在切换布局时仍可用。

### DescendingBlockList {#DescendingBlockList}
```
public static final int DescendingBlockList
```

用于展示相关想法或信息列表的分组。文本形状按高度递减，一级文本垂直显示。

### DescendingProcess {#DescendingProcess}
```
public static final int DescendingProcess
```

用于展示递减的事件序列。首个一级文本位于箭头顶部，最后一级文本位于箭头底部。仅显示前七个一级项目。最适合少量至中等文本。

### DetailedProcess {#DetailedProcess}}
```
public static final int DetailedProcess
```

用于在大量二级文本的情况下展示阶段性进展。

### DivergingArrows {#DivergingArrows}
```
public static final int DivergingArrows
```

用于展示从中心源向外扩散的想法或概念。最适合仅使用一级文本。

### DivergingRadial {#DivergingRadial}
```
public static final int DivergingRadial
```

用于展示循环中与中心思想的关系。第一行一级文本对应中心圆形。强调外围圆形而非中心思想。未使用的文本不显示，但在切换布局时仍可用。

### Equation {#Equation}
```
public static final int Equation
```

用于展示计划或结果的顺序步骤或任务。等号（=）后显示最后一行一级文本。最适合仅使用一级文本。

### FramedTextPicture {#FramedTextPicture}
```
public static final int FramedTextPicture
```

用于在框架中展示对应一级文本的图片。

### Funnel {#Funnel}
```
public static final int Funnel
```

用于展示信息的过滤或部件如何合并为整体。强调最终结果。可容纳最多四行一级文本；第四行以下显示在漏斗下方，其他行对应圆形。未使用的文本不显示，但在切换布局时仍可用。

### Gear {#Gear}
```
public static final int Gear
```

用于展示相互啮合的想法。前三行一级文本对应齿轮形状，其二级文本出现在齿轮旁的矩形中。未使用的文本不显示，但在切换布局时仍可用。

### GridMatrix {#GridMatrix}
```
public static final int GridMatrix
```

用于展示概念在两个轴上的放置。强调单个组件而非整体。前四行一级文本出现在象限中。未使用的文本不显示，但在切换布局时仍可用。

### GroupedList {#GroupedList}
```
public static final int GroupedList
```

用于展示信息的分组与子分组，或任务、流程的步骤与子步骤。一级文本对应顶部水平形状，二级文本对应每个相关顶部形状下的垂直子步骤。适用于强调子分组、层级信息或多列表信息。

### HalfCircleOrganizationChart {#HalfCircleOrganizationChart}
```
public static final int HalfCircleOrganizationChart
```

用于展示组织层级信息或汇报关系。此布局提供助理形状和组织图悬挂布局。

### HexagonCluster {#HexagonCluster}
```
public static final int HexagonCluster
```

用于展示带说明文字的图片。小六边形指示图片与文字的配对。最适合少量文本。

### Hierarchy {#Hierarchy}
```
public static final int Hierarchy
```

用于展示自上而下递进的层级关系。

### HierarchyList {#HierarchyList}
```
public static final int HierarchyList
```

用于展示跨组递进的层级关系。也可用于分组或列出信息。

### HorizontalBulletList {#HorizontalBulletList}
```
public static final int HorizontalBulletList
```

用于展示非顺序或分组的信息列表。适合大量文本。所有文本具有相同强调层级，且不暗示方向。

### HorizontalHierarchy {#HorizontalHierarchy}
```
public static final int HorizontalHierarchy
```

用于展示横向递进的层级关系。适用于决策树。

### HorizontalLabeledHierarchy {#HorizontalLabeledHierarchy}
```
public static final int HorizontalLabeledHierarchy
```

用于展示横向递进且层级分组的层级关系。强调标题或一级文本。第一行一级文本出现在层级起始形状中，其余一级文本出现在高矩形顶部。

### HorizontalMultiLevelHierarchy {#HorizontalMultiLevelHierarchy}
```
public static final int HorizontalMultiLevelHierarchy
```

用于展示大量横向递进的层级信息。层级顶部垂直显示。此布局支持多层级。

### HorizontalOrganizationChart {#HorizontalOrganizationChart}
```
public static final int HorizontalOrganizationChart
```

用于展示组织横向层级信息或汇报关系。此布局提供助理形状和组织图悬挂布局。

### HorizontalPictureList {#HorizontalPictureList}
```
public static final int HorizontalPictureList
```

用于展示强调相关图片的非顺序或分组信息。顶部形状设计用于容纳图片。

### IncreasingArrowsProcess {#IncreasingArrowsProcess}
```
public static final int IncreasingArrowsProcess
```

用于展示过程中的顺序和重叠步骤。限五个一级项目。二级文本可容纳大量文字。

### IncreasingCircleProcess {#IncreasingCircleProcess}
```
public static final int IncreasingCircleProcess
```

用于展示一系列步骤，圆内部随每步增大。限七个一级步骤，二级项目不限。适合大量二级文本。

### InvertedPyramid {#InvertedPyramid}
```
public static final int InvertedPyramid
```

用于展示顶部最大、向下收窄的比例、相互关联或层级关系。一级文本显示在金字塔段落中，二级文本显示在每段旁的形状中。

### LabeledHierarchy {#LabeledHierarchy}
```
public static final int LabeledHierarchy
```

用于展示自上而下递进且层级分组的层级关系。强调标题或一级文本。第一行一级文本出现在层级起始形状中，所有后续一级文本出现在长矩形左侧。

### LinearVenn {#LinearVenn}
```
public static final int LinearVenn
```

用于展示序列中的重叠关系。最适合仅使用一级文本。

### LinedList {#LinedList}
```
public static final int LinedList
```

用于将大量文本分为类别和子类别。适合多层级文本。相同层级的文本由线条分隔。

### MultidirectionalCycle {#MultidirectionalCycle}
```
public static final int MultidirectionalCycle
```

用于表示可在任意方向发生的持续阶段、任务或事件序列。

### NameandTitleOrganizationChart {#NameandTitleOrganizationChart}
```
public static final int NameandTitleOrganizationChart
```

用于展示组织层级信息或汇报关系。要在标题框中输入文本，直接在较小的矩形形状中键入。此布局提供助理形状和组织图悬挂布局。

### NestedTarget {#NestedTarget}
```
public static final int NestedTarget
```

用于展示包含关系。前三行一级文本对应形状左上方的文本，二级文本对应较小形状。最适合最少的二级文本行。未使用的文本不显示，但在切换布局时仍可用。

### NondirectionalCycle {#NondirectionalCycle}
```
public static final int NondirectionalCycle
```

用于表示循环流程中阶段、任务或事件的持续序列。每个形状具有相同的重要性。适用于不需指示方向的情况。

### OpposingArrows {#OpposingArrows}
```
public static final int OpposingArrows
```

用于展示两种对立的想法或从中心点分开的想法。前两行一级文本对应箭头。未使用的文本不显示，但在切换布局时仍可用。

### OpposingIdeas {#OpposingIdeas}
```
public static final int OpposingIdeas
```

用于展示两种对立或对比的想法。可有一或两项一级文本。每个一级文本可包含多个子层级。适合大量文本。

### OrganizationChart {#OrganizationChart}
```
public static final int OrganizationChart
```

用于展示组织层级信息或汇报关系。此布局提供助理形状和组织图悬挂布局。

### PhasedProcess {#PhasedProcess}
```
public static final int PhasedProcess
```

用于展示过程的三个阶段。限三项一级文本。前两项一级文本各可包含四项二级文本，第三项一级文本可包含无限二级文本。最适合少量文本。

### PictureAccentBlocks {#PictureAccentBlocks}
```
public static final int PictureAccentBlocks
```

用于从角落开始以块状展示一组图片。对应文本垂直显示。适合作为标题或副标题幻灯片的强调，或文档章节分隔。

### PictureAccentList {#PictureAccentList}
```
public static final int PictureAccentList
```

用于展示已分组或相关的信息。左上角的小形状设计用于容纳图片。强调二级文本胜过一级文本，适合大量二级文本。

### PictureAccentProcess {#PictureAccentProcess}
```
public static final int PictureAccentProcess
```

用于展示任务、流程或工作流中的顺序步骤。背景矩形设计用于容纳图片。

### PictureCaptionList {#PictureCaptionList}
```
public static final int PictureCaptionList
```

用于展示非顺序或分组的信息块。顶部形状设计用于容纳图片，图片相对于文本更受强调。适合带简短标题的图片。

### PictureGrid {#PictureGrid}
```
public static final int PictureGrid
```

用于在方形网格上展示图片。最适合少量一级文本，文本显示在图片上方。

### PictureLineup {#PictureLineup}
```
public static final int PictureLineup
```

用于并排展示一系列图片。一级文本覆盖图片顶部，二级文本显示在图片下方。

### PictureStrips {#PictureStrips}
```
public static final int PictureStrips
```

用于从上到下展示一系列图片，一级文本位于每张图片旁边。

### PieProcess {#PieProcess}
```
public static final int PieProcess
```

用于展示每个饼块逐渐增大的过程步骤，最多七个形状。一级文本垂直显示。

### PlusandMinus {#PlusandMinus}
```
public static final int PlusandMinus
```

用于展示两种想法的利弊。每个一级文本可包含多个子层级。适合大量文本。限两个一级项目。

### ProcessArrows {#ProcessArrows}
```
public static final int PlusandMinus
```

用于展示说明过程或工作流的信息。一级文本出现在圆形，二级文本出现在箭头形状。最适合最少文本并强调运动或方向。

### ProcessList {#ProcessList}
```
public static final int ProcessList
```

用于展示信息组或任务、流程步骤及子步骤。一级文本对应顶部水平形状，二级文本对应每个相关顶部形状下的垂直子步骤。

### PyramidList {#PyramidList}
```
public static final int PyramidList
```

用于展示比例、相互关联或层级关系。文本显示在金字塔背景顶部的矩形形状中。

### RadialCluster {#RadialCluster}
```
public static final int RadialCluster
```

用于展示与中心思想或主题相关的数据。顶部一级文本显示在中心，二级文本显示在周围形状。可容纳最多七个二级形状。未使用的文本不显示，但在切换布局时仍可用。最适合少量文本。

### RadialCycle {#RadialCycle}
```
public static final int RadialCycle
```

用于展示与中心思想的关系。强调中心圆形的信息以及外围圆环对中心思想的贡献。第一行一级文本对应中心圆形，其二级文本对应外围圆环。未使用的文本不显示，但在切换布局时仍可用。

### RadialList {#RadialList}
```
public static final int RadialList
```

用于展示循环中与中心思想的关系。中心形状可容纳图片。一级文本出现在较小圆形，任何相关二级文本显示在小圆形侧边。

### RadialVenn {#RadialVenn}
```
public static final int RadialVenn
```

用于同时展示重叠关系及循环中与中心思想的关系。第一行一级文本对应中心形状，二级文本对应周围圆形。未使用的文本不显示，但在切换布局时仍可用。

### RandomToResultProcess {#RandomToResultProcess}
```
public static final int RandomToResultProcess
```

通过一系列步骤展示多个混乱想法如何汇聚为统一目标。支持多个一级文本项目，但首尾对应形状固定。最适合少量一级文本和中等二级文本。

### RepeatingBendingProcess {#RepeatingBendingProcess}
```
public static final int RepeatingBendingProcess
```

用于展示任务、流程或工作流中的进展或顺序步骤。最大化形状的水平和垂直显示空间。

### ReverseList {#ReverseList}
```
public static final int ReverseList
```

用于在两个项目之间切换。仅显示前两项文本，每项可容纳大量文本。适合展示两项之间的变化或顺序移动。

### SegmentedCycle {#SegmentedCycle}
```
public static final int SegmentedCycle
```

用于展示循环中的阶段、任务或事件的进展或序列。强调相互连接的部分。前七行一级文本对应楔形或饼形。未使用的文本不显示，但在切换布局时仍可用。

### SegmentedProcess {#SegmentedProcess}
```
public static final int SegmentedProcess
```

用于展示任务、流程或工作流中的进展或顺序步骤。强调二级文本，因为每行出现在单独的形状中。

### SegmentedPyramid {#SegmentedPyramid}
```
public static final int SegmentedPyramid
```

用于展示包含、比例或相互关联的关系。前九行一级文本出现在三角形形状中。未使用的文本不显示，但在切换布局时仍可用。仅适用于一级文本。

### SnapshotPictureList {#SnapshotPictureList}
```
public static final int SnapshotPictureList
```

用于展示带说明文字的图片。二级文本可显示信息列表。适合大量文本。

### SpiralPicture {#SpiralPicture}
```
public static final int SpiralPicture
```

用于展示最多五张图片，配相应一级标题，螺旋式向中心排列。

### SquareAccentList {#SquareAccentList}
```
public static final int SquareAccentList
```

用于按类别划分信息列表。二级文本出现在小方形旁。适合大量二级文本。

### StackedList {#StackedList}
```
public static final int StackedList
```

用于展示信息组或任务、流程的步骤。圆形包含一级文本，矩形包含二级文本。适合大量细节和最少一级文本。

### StackedVenn {#StackedVenn}
```
public static final int StackedVenn
```

用于展示重叠关系。强调增长或递进。最适合仅使用一级文本。前七行一级文本对应圆形。未使用的文本不显示，但在切换布局时仍可用。

### StaggeredProcess {#StaggeredProcess}
```
public static final int StaggeredProcess
```

用于展示向下递进的阶段。前五行一级文本对应矩形。未使用的文本不显示，但在切换布局时仍可用。

### StepDownProcess {#StepDownProcess}
```
public static final int StepDownProcess
```

用于展示多步骤且带子步骤的递减过程。最适合少量文本。

### StepUpProcess {#StepUpProcess}
```
public static final int StepUpProcess
```

用于展示递增的步骤或信息列表。

### SubStepProcess {#SubStepProcess}
```
public static final int SubStepProcess
```

用于在每个一级文本之间添加子步骤的多步骤过程。最适合少量文本，限七个一级步骤，每个一级步骤可拥有无限子步骤。

### TableHierarchy {#TableHierarchy}
```
public static final int TableHierarchy
```

用于展示从上到下构建的信息组及其层级。此布局不含连接线。

### TableList {#TableList}
```
public static final int TableList
```

用于展示价值相等的已分组或相关信息。首行一级文本对应顶部形状，其二级文本用于后续列表。

### TargetList {#TargetList}
```
public static final int TargetList
```

用于展示相互关联或重叠的信息。前七行一级文本出现在矩形形状中。未使用的文本不显示，但在切换布局时仍可用。适合同时使用一级和二级文本。

### TextCycle {#TextCycle}
```
public static final int TextCycle
```

用于表示循环流程中阶段、任务或事件的持续序列。强调箭头或流向，而非阶段或步骤。最适合仅使用一级文本。

### TitlePictureLineup {#TitlePictureLineup}
```
public static final int TitlePictureLineup
```

用于展示每张图片都有独立标题和描述的系列。一级文本显示在图片上方的框中，二级文本显示在图片下方。

### TitledMatrix {#TitledMatrix}
```
public static final int TitledMatrix
```

用于展示四象限与整体的关系。第一行一级文本对应中心形状，前四行二级文本出现在象限中。未使用的文本不显示，但在切换布局时仍可用。

### TitledPictureAccentList {#TitledPictureAccentList}
```
public static final int TitledPictureAccentList
```

用于为每个二级文本配以强调图片的信息列表。一级文本显示在列表顶部的单独框中。

### TitledPictureBlocks {#TitledPictureBlocks}
```
public static final int TitledPictureBlocks
```

用于展示一系列图片。一级文本显示在每张图片上方，二级文本显示在侧面并略微重叠每张图片。

### TrapezoidList {#TrapezoidList}
```
public static final int TrapezoidList
```

用于展示价值相等的已分组或相关信息。适合大量文本。

### UpwardArrow {#UpwardArrow}
```
public static final int UpwardArrow
```

用于展示任务、流程或工作流中向上趋势的进展或步骤。前五行一级文本对应箭头上的点。最适合最少文本。未使用的文本不显示，但在切换布局时仍可用。

### VerticalAccentList {#VerticalAccentList}
```
public static final int VerticalAccentList
```

用于展示信息列表。二级文本出现在垂直箭头形状上的矩形中。强调二级文本胜过一级文本，适合中等量二级文本。

### VerticalArrowList {#VerticalArrowList}
```
public static final int VerticalArrowList
```

用于展示任务、流程或工作流中朝向共同目标前进的进展或顺序步骤。适合项目符号信息列表。

### VerticalBendingProcess {#VerticalBendingProcess}
```
public static final int VerticalBendingProcess
```

用于展示任务、流程或工作流中的进展或顺序步骤。最大化形状的水平和垂直显示空间。更强调形状之间的相互关系，而非方向或运动。

### VerticalBlockList {#VerticalBlockList}
```
public static final int VerticalBlockList
```

用于展示信息组或任务、流程的步骤。适合大量二级文本。适合有主点和多个子点的文本。

### VerticalBoxList {#VerticalBoxList}
```
public static final int VerticalBoxList
```

用于展示多个信息组，尤其是二级文本大量的组。适合项目符号信息列表。

### VerticalBulletList {#VerticalBulletList}
```
public static final int VerticalBulletList
```

用于展示非顺序或分组的信息块。适合标题或顶层信息较长的列表。

### VerticalChevronList {#VerticalChevronList}
```
public static final int VerticalChevronList
```

用于展示任务、流程或工作流中的进展或顺序步骤，或强调运动或方向。强调二级文本胜过一级文本，适合大量二级文本。

### VerticalCircleList {#VerticalCircleList}
```
public static final int VerticalCircleList
```

用于展示顺序或分组的数据。最适合仅使用一级文本，显示在大圆形旁。低层级文本用小圆形分隔。

### VerticalCurvedList {#VerticalCurvedList}
```
public static final int VerticalCurvedList
```

用于展示弧形信息列表。若要在强调圆形中加入图片，请使用图片填充。

### VerticalEquation {#VerticalEquation}
```
public static final int VerticalEquation
```

用于展示计划或结果的顺序步骤或任务。最后一行一级文本出现在箭头后。最适合仅使用一级文本。

### VerticalPictureAccentList {#VerticalPictureAccentList}
```
public static final int VerticalPictureAccentList
```

用于展示非顺序或分组的信息块。小圆形设计用于容纳图片。

### VerticalPictureList {#VerticalPictureList}
```
public static final int VerticalPictureList
```

用于展示非顺序或分组的信息块。左侧的小形状设计用于容纳图片。

### VerticalProcess {#VerticalProcess}
```
public static final int VerticalProcess
```

用于从上到下展示任务、流程或工作流的进展或顺序步骤。最适合仅使用一级文本，因为垂直空间有限。

### Custom {#Custom}
```
public static final int Custom
```

表示具有自定义布局模板的 SmartArt 图表

### PictureOrganizationChart {#PictureOrganizationChart}
```
public static final int PictureOrganizationChart
```

用于展示带对应图片的组织层级信息或汇报关系。此布局提供助理形状和组织图悬挂布局。