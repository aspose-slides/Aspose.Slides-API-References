---
title: MarkerStyleType
second_title: Aspose.Slides for Android via Java API 参考
description: 确定图表数据点上标记的形状。
type: docs
url: /zh/com.aspose.slides/markerstyletype/
---
**继承:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class MarkerStyleType extends System.Enum
```

确定图表数据点上标记的形状。
## 字段

| 字段 | 描述 |
| --- | --- |
| [NotDefined](#NotDefined) |  |
| [Circle](#Circle) | 指定在每个数据点上绘制圆形。 |
| [Dash](#Dash) | 指定在每个数据点上绘制短划线。 |
| [Diamond](#Diamond) | 指定在每个数据点上绘制菱形。 |
| [Dot](#Dot) | 指定在每个数据点上绘制点。 |
| [None](#None) | 指定在每个数据点上不绘制任何内容。 |
| [Picture](#Picture) | 指定在每个数据点上绘制图片。 |
| [Plus](#Plus) | 指定在每个数据点上绘制加号。 |
| [Square](#Square) | 指定在每个数据点上绘制正方形。 |
| [Star](#Star) | 指定在每个数据点上绘制星形。 |
| [Triangle](#Triangle) | 指定在每个数据点上绘制三角形。 |
| [X](#X) | 指定在每个数据点上绘制 X。 |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```




### Circle {#Circle}
```
public static final int Circle
```

指定在每个数据点上绘制圆形。

### Dash {#Dash}
```
public static final int Dash
```

指定在每个数据点上绘制短划线。

### Diamond {#Diamond}
```
public static final int Diamond
```

指定在每个数据点上绘制菱形。

### Dot {#Dot}
```
public static final int Dot
```

指定在每个数据点上绘制点。

### None {#None}
```
public static final int None
```

指定在每个数据点上不绘制任何内容。

### Picture {#Picture}
```
public static final int Picture
```

指定在每个数据点上绘制图片。

### Plus {#Plus}
```
public static final int Plus
```

指定在每个数据点上绘制加号。

### Square {#Square}
```
public static final int Square
```

指定在每个数据点上绘制正方形。

### Star {#Star}
```
public static final int Star
```

指定在每个数据点上绘制星形。

### Triangle {#Triangle}
```
public static final int Triangle
```

指定在每个数据点上绘制三角形。

### X {#X}
```
public static final int X
```

指定在每个数据点上绘制 X。