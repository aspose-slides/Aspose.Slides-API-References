---
title: ImageFormat
second_title: Aspose.Slides για Android μέσω αναφοράς API Java
description: Αναπαριστά τη μορφή αρχείου της εικόνας.
type: docs
url: /el/com.aspose.slides/imageformat/
---
**Κληρονομικότητα:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class ImageFormat extends System.Enum
```

Αναπαριστά τη μορφή αρχείου της εικόνας.
## Πεδία

| Πεδίο | Περιγραφή |
| --- | --- |
| [MemoryBmp](#MemoryBmp) | Αναπαριστά τη μορφή ενός bitmap στη μνήμη. |
| [Bmp](#Bmp) | Αναπαριστά τη μορφή της εικόνας bitmap (BMP). |
| [Emf](#Emf) | Αναπαριστά τη μορφή της βελτιωμένης μετααρχεία (EMF) εικόνας. |
| [Wmf](#Wmf) | Αναπαριστά τη μορφή του μετααρχείου Windows (WMF) εικόνας. |
| [Gif](#Gif) | Αναπαριστά τη μορφή της εικόνας Graphics Interchange Format (GIF). |
| [Jpeg](#Jpeg) | Αναπαριστά τη μορφή της εικόνας Joint Photographic Experts Group (JPEG). |
| [Png](#Png) | Αναπαριστά τη μορφή της εικόνας W3C Portable Network Graphics (PNG). |
| [Tiff](#Tiff) | Αναπαριστά τη μορφή της εικόνας Tagged Image File Format (TIFF). |
| [Exif](#Exif) | Αναπαριστά τη μορφή του αρχείου Exchangeable Image File (Exif). |
| [Icon](#Icon) | Αναπαριστά τη μορφή της εικόνας εικονιδίου Windows. |
### MemoryBmp {#MemoryBmp}
```
public static final int MemoryBmp
```

Αναπαριστά τη μορφή ενός bitmap στη μνήμη.

### Bmp {#Bmp}
```
public static final int Bmp
```

Αναπαριστά τη μορφή της εικόνας bitmap (BMP).

### Emf {#Emf}
```
public static final int Emf
```

Αναπαριστά τη μορφή της βελτιωμένης μετααρχεία (EMF) εικόνας.

### Wmf {#Wmf}
```
public static final int Wmf
```

Αναπαριστά τη μορφή του μετααρχείου Windows (WMF) εικόνας.

### Gif {#Gif}
```
public static final int Gif
```

Αναπαριστά τη μορφή της εικόνας Graphics Interchange Format (GIF).

### Jpeg {#Jpeg}
```
public static final int Jpeg
```

Αναπαριστά τη μορφή της εικόνας Joint Photographic Experts Group (JPEG).

### Png {#Png}
```
public static final int Png
```

Αναπαριστά τη μορφή της εικόνας W3C Portable Network Graphics (PNG).

### Tiff {#Tiff}
```
public static final int Tiff
```

Αναπαριστά τη μορφή της εικόνας Tagged Image File Format (TIFF).

### Exif {#Exif}
```
public static final int Exif
```

Αναπαριστά τη μορφή του αρχείου Exchangeable Image File (Exif).

### Icon {#Icon}
```
public static final int Icon
```

Αναπαριστά τη μορφή της εικόνας εικονιδίου Windows.