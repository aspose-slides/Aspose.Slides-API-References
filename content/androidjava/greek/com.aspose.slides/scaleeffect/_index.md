---
title: ScaleEffect
second_title: Aspose.Slides για Android μέσω Java API Αναφορά
description: Αντιπροσωπεύει το εφέ κλιμάκωσης της κίνησης.
type: docs
url: /el/com.aspose.slides/scaleeffect/
---
**Κληρονόμηση:**  
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**Όλες οι Υλοποιημένες Διεπαφές:**  
[com.aspose.slides.IScaleEffect](../../com.aspose.slides/iscaleeffect)  
```
public class ScaleEffect extends Behavior implements IScaleEffect
```

Αντιπροσωπεύει το εφέ κλιμάκωσης της κίνησης.

## Κατασκευαστές

| Κατασκευαστής | Περιγραφή |
| --- | --- |
| [ScaleEffect()](#ScaleEffect--) |  |

## Μέθοδοι

| Μέθοδος | Περιγραφή |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Καθορίζει εάν ένα περιεχόμενο πρέπει να ζουμάρει. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Καθορίζει εάν ένα περιεχόμενο πρέπει να ζουμάρει. |
| [getFrom()](#getFrom--) | Καθορίζει συντεταγμένη x/y από την οποία ξεκινά η κίνηση (σε ποσοστά). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | Καθορίζει συντεταγμένη x/y από την οποία ξεκινά η κίνηση (σε ποσοστά). |
| [getTo()](#getTo--) | Καθορίζει την τελική θέση για το εφέ κλιμάκωσης της κίνησης (σε ποσοστά). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | Καθορίζει την τελική θέση για το εφέ κλιμάκωσης της κίνησης (σε ποσοστά). |
| [getBy()](#getBy--) | Περιγράφει τη σχετική τιμή μετατόπισης για την κίνηση (σε ποσοστά). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | Περιγράφει τη σχετική τιμή μετατόπισης για την κίνηση (σε ποσοστά). |

### ScaleEffect() {#ScaleEffect--}
```
public ScaleEffect()
```

### getZoomContent() {#getZoomContent--}
```
public final byte getZoomContent()
```

Καθορίζει εάν ένα περιεχόμενο πρέπει να ζουμάρει. Ανάγνωση/εγγραφή [NullableBool](../../com.aspose.slides/nullablebool).

**Επιστρέφει:**
byte

### setZoomContent(byte value) {#setZoomContent-byte-}
```
public final void setZoomContent(byte value)
```

Καθορίζει εάν ένα περιεχόμενο πρέπει να ζουμάρει. Ανάγνωση/εγγραφή [NullableBool](../../com.aspose.slides/nullablebool).

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public final PointF getFrom()
```

Καθορίζει συντεταγμένη x/y από την οποία ξεκινά η κίνηση (σε ποσοστά). Ανάγνωση/εγγραφή android.graphics.PointF.

**Επιστρέφει:**
android.graphics.PointF

### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public final void setFrom(PointF value)
```

Καθορίζει συντεταγμένη x/y από την οποία ξεκινά η κίνηση (σε ποσοστά). Ανάγνωση/εγγραφή android.graphics.PointF.

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public final PointF getTo()
```

Καθορίζει την τελική θέση για το εφέ κλιμάκωσης της κίνησης (σε ποσοστά). Ανάγνωση/εγγραφή android.graphics.PointF.

**Επιστρέφει:**
android.graphics.PointF

### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public final void setTo(PointF value)
```

Καθορίζει την τελική θέση για το εφέ κλιμάκωσης της κίνησης (σε ποσοστά). Ανάγνωση/εγγραφή android.graphics.PointF.

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public final PointF getBy()
```

Περιγράφει τη σχετική τιμή μετατόπισης για την κίνηση (σε ποσοστά). Ανάγνωση/εγγραφή android.graphics.PointF.

**Επιστρέφει:**
android.graphics.PointF

### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public final void setBy(PointF value)
```

Περιγράφει τη σχετική τιμή μετατόπισης για την κίνηση (σε ποσοστά). Ανάγνωση/εγγραφή android.graphics.PointF.

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | android.graphics.PointF |  |