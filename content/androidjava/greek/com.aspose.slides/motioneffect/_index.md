---
title: MotionEffect
second_title: Aspose.Slides για Android μέσω Java API Αναφορά
description: Αναπαριστά τη συμπεριφορά του εφέ κίνησης.
type: docs
url: /el/com.aspose.slides/motioneffect/
---
**Κληρονομικότητα:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**Όλες οι Υλοποιημένες Διεπαφές:**
[com.aspose.slides.IMotionEffect](../../com.aspose.slides/imotioneffect)
```
public class MotionEffect extends Behavior implements IMotionEffect
```

Αναπαριστά τη συμπεριφορά εφέ κίνησης του εφέ.
## Κατασκευαστές

| Κατασκευαστής | Περιγραφή |
| --- | --- |
| [MotionEffect()](#MotionEffect--) |  |
## Μέθοδοι

| Μέθοδος | Περιγραφή |
| --- | --- |
| [getFrom()](#getFrom--) | Καθορίζει μια συντεταγμένη x/y από την οποία ξεκινά η κίνηση της παρουσίασης (σε ποσοστά). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | Καθορίζει μια συντεταγμένη x/y από την οποία ξεκινά η κίνηση της παρουσίασης (σε ποσοστά). |
| [getTo()](#getTo--) | Καθορίζει τη στοχευμένη θέση για ένα εφέ κίνησης παρουσίασης (σε ποσοστά). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | Καθορίζει τη στοχευμένη θέση για ένα εφέ κίνησης παρουσίασης (σε ποσοστά). |
| [getBy()](#getBy--) | Περιγράφει τη σχετική τιμή μετατόπισης για την παρουσίαση (σε ποσοστά). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | Περιγράφει τη σχετική τιμή μετατόπισης για την παρουσίαση (σε ποσοστά). |
| [getRotationCenter()](#getRotationCenter--) | Περιγράφει το κέντρο περιστροφής που χρησιμοποιείται για την περιστροφή μιας διαδρομής κίνησης κατά γωνία X. |
| [setRotationCenter(PointF value)](#setRotationCenter-android.graphics.PointF-) | Περιγράφει το κέντρο περιστροφής που χρησιμοποιείται για την περιστροφή μιας διαδρομής κίνησης κατά γωνία X. |
| [getOrigin()](#getOrigin--) | Καθορίζει τι είναι η προέλευση της διαδρομής κίνησης σε σχέση με κάτι, όπως η διάταξη της διαφάνειας ή ο γονέας. |
| [setOrigin(int value)](#setOrigin-int-) | Καθορίζει τι είναι η προέλευση της διαδρομής κίνησης σε σχέση με κάτι, όπως η διάταξη της διαφάνειας ή ο γονέας. |
| [getPath()](#getPath--) | Καθορίζει το βασικό στοιχείο διαδρομής που ακολουθείται από συντεταγμένες για την κίνηση της παρουσίασης. |
| [setPath(IMotionPath value)](#setPath-com.aspose.slides.IMotionPath-) | Καθορίζει το βασικό στοιχείο διαδρομής που ακολουθείται από συντεταγμένες για την κίνηση της παρουσίασης. |
| [getPathEditMode()](#getPathEditMode--) | Καθορίζει πώς κινείται η διαδρομή κίνησης όταν το σχήμα μετακινείται. |
| [setPathEditMode(int value)](#setPathEditMode-int-) | Καθορίζει πώς κινείται η διαδρομή κίνησης όταν το σχήμα μετακινείται. |
| [getAngle()](#getAngle--) | Περιγράφει τη σχετική γωνία της διαδρομής κίνησης. |
| [setAngle(float value)](#setAngle-float-) | Περιγράφει τη σχετική γωνία της διαδρομής κίνησης. |
### MotionEffect() {#MotionEffect--}
```
public MotionEffect()
```

### getFrom() {#getFrom--}
```
public final PointF getFrom()
```

Καθορίζει μια συντεταγμένη x/y από την οποία ξεκινά η κίνηση της παρουσίασης (σε ποσοστά). Ανάγνωση/εγγραφή android.graphics.PointF.

**Επιστρέφει:**
android.graphics.PointF
### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public final void setFrom(PointF value)
```

Καθορίζει μια συντεταγμένη x/y από την οποία ξεκινά η κίνηση της παρουσίασης (σε ποσοστά). Ανάγνωση/εγγραφή android.graphics.PointF.

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public final PointF getTo()
```

Καθορίζει τη στοχευμένη θέση για ένα εφέ κίνησης παρουσίασης (σε ποσοστά). Ανάγνωση/εγγραφή android.graphics.PointF.

**Επιστρέφει:**
android.graphics.PointF
### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public final void setTo(PointF value)
```

Καθορίζει τη στοχευμένη θέση για ένα εφέ κίνησης παρουσίασης (σε ποσοστά). Ανάγνωση/εγγραφή android.graphics.PointF.

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public final PointF getBy()
```

Περιγράφει τη σχετική τιμή μετατόπισης για την παρουσίαση (σε ποσοστά). Ανάγνωση/εγγραφή android.graphics.PointF.

**Επιστρέφει:**
android.graphics.PointF
### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public final void setBy(PointF value)
```

Περιγράφει τη σχετική τιμή μετατόπισης για την παρουσίαση (σε ποσοστά). Ανάγνωση/εγγραφή android.graphics.PointF.

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getRotationCenter() {#getRotationCenter--}
```
public final PointF getRotationCenter()
```

Περιγράφει το κέντρο περιστροφής που χρησιμοποιείται για την περιστροφή μιας διαδρομής κίνησης κατά γωνία X. Ανάγνωση/εγγραφή android.graphics.PointF.

**Επιστρέφει:**
android.graphics.PointF
### setRotationCenter(PointF value) {#setRotationCenter-android.graphics.PointF-}
```
public final void setRotationCenter(PointF value)
```

Περιγράφει το κέντρο περιστροφής που χρησιμοποιείται για την περιστροφή μιας διαδρομής κίνησης κατά γωνία X. Ανάγνωση/εγγραφή android.graphics.PointF.

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getOrigin() {#getOrigin--}
```
public final int getOrigin()
```

Καθορίζει τι είναι η προέλευση της διαδρομής κίνησης σε σχέση με κάτι, όπως η διάταξη της διαφάνειας ή ο γονέας. Ανάγνωση/εγγραφή [MotionOriginType](../../com.aspose.slides/motionorigintype).

**Επιστρέφει:**
int
### setOrigin(int value) {#setOrigin-int-}
```
public final void setOrigin(int value)
```

Καθορίζει τι είναι η προέλευση της διαδρομής κίνησης σε σχέση με κάτι, όπως η διάταξη της διαφάνειας ή ο γονέας. Ανάγνωση/εγγραφή [MotionOriginType](../../com.aspose.slides/motionorigintype).

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | int |  |

### getPath() {#getPath--}
```
public final IMotionPath getPath()
```

Καθορίζει το βασικό στοιχείο διαδρομής που ακολουθείται από συντεταγμένες για την κίνηση της παρουσίασης. Ανάγνωση/εγγραφή [IMotionPath](../../com.aspose.slides/imotionpath).

**Επιστρέφει:**
[IMotionPath](../../com.aspose.slides/imotionpath)
### setPath(IMotionPath value) {#setPath-com.aspose.slides.IMotionPath-}
```
public final void setPath(IMotionPath value)
```

Καθορίζει το βασικό στοιχείο διαδρομής που ακολουθείται από συντεταγμένες για την κίνηση της παρουσίασης. Ανάγνωση/εγγραφή [IMotionPath](../../com.aspose.slides/imotionpath).

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | [IMotionPath](../../com.aspose.slides/imotionpath) |  |

### getPathEditMode() {#getPathEditMode--}
```
public final int getPathEditMode()
```

Καθορίζει πώς κινείται η διαδρομή κίνησης όταν το σχήμα μετακινείται. Ανάγνωση/εγγραφή [MotionPathEditMode](../../com.aspose.slides/motionpatheditmode).

**Επιστρέφει:**
int
### setPathEditMode(int value) {#setPathEditMode-int-}
```
public final void setPathEditMode(int value)
```

Καθορίζει πώς κινείται η διαδρομή κίνησης όταν το σχήμα μετακινείται. Ανάγνωση/εγγραφή [MotionPathEditMode](../../com.aspose.slides/motionpatheditmode).

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | int |  |

### getAngle() {#getAngle--}
```
public final float getAngle()
```

Περιγράφει τη σχετική γωνία της διαδρομής κίνησης. Ανάγνωση/εγγραφή float.

**Επιστρέφει:**
float
### setAngle(float value) {#setAngle-float-}
```
public final void setAngle(float value)
```

Περιγράφει τη σχετική γωνία της διαδρομής κίνησης. Ανάγνωση/εγγραφή float.

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | float |  |