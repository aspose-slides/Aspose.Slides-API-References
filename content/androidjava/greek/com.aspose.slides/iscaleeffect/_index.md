---
title: IScaleEffect
second_title: Aspose.Slides για Android μέσω αναφοράς Java API
description: Αναπαριστά το εφέ κλίμακας κίνησης.
type: docs
url: /el/com.aspose.slides/iscaleeffect/
---
**Όλες οι Υλοποιημένες Διασυνδέσεις:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IScaleEffect extends IBehavior
```

Αναπαριστά το εφέ κλίμακας κίνησης.
## Μέθοδοι

| Μέθοδος | Περιγραφή |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Καθορίζει εάν ένα περιεχόμενο πρέπει να ζουμάρεται. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Καθορίζει εάν ένα περιεχόμενο πρέπει να ζουμάρεται. |
| [getFrom()](#getFrom--) | Καθορίζει μια συντεταγμένη x/y από την οποία ξεκινά η κίνηση (σε ποσοστά). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | Καθορίζει μια συντεταγμένη x/y από την οποία ξεκινά η κίνηση (σε ποσοστά). |
| [getTo()](#getTo--) | Καθορίζει τη θέση στόχο για το εφέ κλίμακας κίνησης (σε ποσοστά). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | Καθορίζει τη θέση στόχο για το εφέ κλίμακας κίνησης (σε ποσοστά). |
| [getBy()](#getBy--) | Περιγράφει τη σχετική τιμή μετατόπισης για την κίνηση (σε ποσοστά). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | Περιγράφει τη σχετική τιμή μετατόπισης για την κίνηση (σε ποσοστά). |
### getZoomContent() {#getZoomContent--}
```
public abstract byte getZoomContent()
```

Καθορίζει εάν ένα περιεχόμενο πρέπει να ζουμάρεται. Ανάγνωση/Εγγραφή [NullableBool](../../com.aspose.slides/nullablebool).

**Επιστρέφει:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public abstract void setZoomContent(byte value)
```

Καθορίζει εάν ένα περιεχόμενο πρέπει να ζουμάρεται. Ανάγνωση/Εγγραφή [NullableBool](../../com.aspose.slides/nullablebool).

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | byte |  |
### getFrom() {#getFrom--}
```
public abstract PointF getFrom()
```

Καθορίζει μια συντεταγμένη x/y από την οποία ξεκινά η κίνηση (σε ποσοστά). Ανάγνωση/Εγγραφή android.graphics.PointF.

**Επιστρέφει:**
android.graphics.PointF
### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public abstract void setFrom(PointF value)
```

Καθορίζει μια συντεταγμένη x/y από την οποία ξεκινά η κίνηση (σε ποσοστά). Ανάγνωση/Εγγραφή android.graphics.PointF.

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | android.graphics.PointF |  |
### getTo() {#getTo--}
```
public abstract PointF getTo()
```

Καθορίζει τη θέση στόχο για το εφέ κλίμακας κίνησης (σε ποσοστά). Ανάγνωση/Εγγραφή android.graphics.PointF.

**Επιστρέφει:**
android.graphics.PointF
### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public abstract void setTo(PointF value)
```

Καθορίζει τη θέση στόχο για το εφέ κλίμακας κίνησης (σε ποσοστά). Ανάγνωση/Εγγραφή android.graphics.PointF.

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | android.graphics.PointF |  |
### getBy() {#getBy--}
```
public abstract PointF getBy()
```

Περιγράφει τη σχετική τιμή μετατόπισης για την κίνηση (σε ποσοστά). Ανάγνωση/Εγγραφή android.graphics.PointF.

**Επιστρέφει:**
android.graphics.PointF
### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public abstract void setBy(PointF value)
```

Περιγράφει τη σχετική τιμή μετατόπισης για την κίνηση (σε ποσοστά). Ανάγνωση/Εγγραφή android.graphics.PointF.

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | android.graphics.PointF |  |