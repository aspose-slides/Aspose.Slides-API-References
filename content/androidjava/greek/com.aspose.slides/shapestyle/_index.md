---
title: ShapeStyle
second_title: Aspose.Slides για Android μέσω αναφοράς Java API
description: Αναπαριστά τη αναφορά στυλ των σχημάτων.
type: docs
url: /el/com.aspose.slides/shapestyle/
---
**Κληρονομικότητα:**
java.lang.Object, com.aspose.slides.DomObject

**Όλες οι Υλοποιημένες Διεπαφές:**
[com.aspose.slides.IShapeStyle](../../com.aspose.slides/ishapestyle)
```
public class ShapeStyle extends DomObject<Shape> implements IShapeStyle
```

Αναπαριστά την αναφορά στυλ του σχήματος.
## Μέθοδοι

| Μέθοδος | Περιγραφή |
| --- | --- |
| [getLineColor()](#getLineColor--) | Επιστρέφει το χρώμα περιγράμματος του σχήματος. |
| [getLineStyleIndex()](#getLineStyleIndex--) | Επιστρέφει ή ορίζει το δείκτη στήλης της γραμμής σε έναν πίνακα στυλ. |
| [setLineStyleIndex(int value)](#setLineStyleIndex-int-) | Επιστρέφει ή ορίζει το δείκτη στήλης της γραμμής σε έναν πίνακα στυλ. |
| [getFillColor()](#getFillColor--) | Επιστρέφει το χρώμα γεμίσματος του σχήματος. |
| [getFillStyleIndex()](#getFillStyleIndex--) | Επιστρέφει ή ορίζει το δείκτη στήλης γεμίσματος του σχήματος σε πίνακες στυλ. |
| [setFillStyleIndex(short value)](#setFillStyleIndex-short-) | Επιστρέφει ή ορίζει το δείκτη στήλης γεμίσματος του σχήματος σε πίνακες στυλ. |
| [getEffectColor()](#getEffectColor--) | Επιστρέφει το χρώμα εφέ του σχήματος. |
| [getEffectStyleIndex()](#getEffectStyleIndex--) | Επιστρέφει ή ορίζει το δείκτη στήλης εφέ του σχήματος σε έναν πίνακα στυλ. |
| [setEffectStyleIndex(long value)](#setEffectStyleIndex-long-) | Επιστρέφει ή ορίζει το δείκτη στήλης εφέ του σχήματος σε έναν πίνακα στυλ. |
| [getFontColor()](#getFontColor--) | Επιστρέφει το χρώμα γραμματοσειράς του σχήματος. |
| [getFontCollectionIndex()](#getFontCollectionIndex--) | Επιστρέφει ή ορίζει το δείκτη γραμματοσειράς του σχήματος σε μια συλλογή γραμματοσειρών. |
| [setFontCollectionIndex(byte value)](#setFontCollectionIndex-byte-) | Επιστρέφει ή ορίζει το δείκτη γραμματοσειράς του σχήματος σε μια συλλογή γραμματοσειρών. |
### getLineColor() {#getLineColor--}
```
public final IColorFormat getLineColor()
```

Επιστρέφει το χρώμα περιγράμματος του σχήματος. Μόνο για ανάγνωση [IColorFormat](../../com.aspose.slides/icolorformat).

**Επιστρέφει:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getLineStyleIndex() {#getLineStyleIndex--}
```
public final int getLineStyleIndex()
```

Επιστρέφει ή ορίζει το δείκτη στήλης της γραμμής σε έναν πίνακα στυλ. Ανάγνωση/εγγραφή int.

**Επιστρέφει:**
int
### setLineStyleIndex(int value) {#setLineStyleIndex-int-}
```
public final void setLineStyleIndex(int value)
```

Επιστρέφει ή ορίζει το δείκτη στήλης της γραμμής σε έναν πίνακα στυλ. Ανάγνωση/εγγραφή int.

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | int |  |
### getFillColor() {#getFillColor--}
```
public final IColorFormat getFillColor()
```

Επιστρέφει το χρώμα γεμίσματος του σχήματος. Μόνο για ανάγνωση [IColorFormat](../../com.aspose.slides/icolorformat).

**Επιστρέφει:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getFillStyleIndex() {#getFillStyleIndex--}
```
public final short getFillStyleIndex()
```

Επιστρέφει ή ορίζει το δείκτη στήλης γεμίσματος του σχήματος σε πίνακες στυλ. 0 σημαίνει κανένα γέμισμα, θετική τιμή - δείκτης στα στυλ γεμίσματος του θέματος, αρνητική τιμή - δείκτης στα στυλ υποβάθρου του θέματος. Ανάγνωση/εγγραφή short.

**Επιστρέφει:**
short
### setFillStyleIndex(short value) {#setFillStyleIndex-short-}
```
public final void setFillStyleIndex(short value)
```

Επιστρέφει ή ορίζει το δείκτη στήλης γεμίσματος του σχήματος σε πίνακες στυλ. 0 σημαίνει κανένα γέμισμα, θετική τιμή - δείκτης στα στυλ γεμίσματος του θέματος, αρνητική τιμή - δείκτης στα στυλ υποβάθρου του θέματος. Ανάγνωση/εγγραφή short.

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | short |  |
### getEffectColor() {#getEffectColor--}
```
public final IColorFormat getEffectColor()
```

Επιστρέφει το χρώμα εφέ του σχήματος. Μόνο για ανάγνωση [IColorFormat](../../com.aspose.slides/icolorformat).

**Επιστρέφει:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getEffectStyleIndex() {#getEffectStyleIndex--}
```
public final long getEffectStyleIndex()
```

Επιστρέφει ή ορίζει το δείκτη στήλης εφέ του σχήματος σε έναν πίνακα στυλ. Ανάγνωση/εγγραφή long.

**Επιστρέφει:**
long
### setEffectStyleIndex(long value) {#setEffectStyleIndex-long-}
```
public final void setEffectStyleIndex(long value)
```

Επιστρέφει ή ορίζει το δείκτη στήλης εφέ του σχήματος σε έναν πίνακα στυλ. Ανάγνωση/εγγραφή long.

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | long |  |
### getFontColor() {#getFontColor--}
```
public final IColorFormat getFontColor()
```

Επιστρέφει το χρώμα γραμματοσειράς του σχήματος. Μόνο για ανάγνωση [IColorFormat](../../com.aspose.slides/icolorformat).

**Επιστρέφει:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getFontCollectionIndex() {#getFontCollectionIndex--}
```
public final byte getFontCollectionIndex()
```

Επιστρέφει ή ορίζει το δείκτη γραμματοσειράς του σχήματος σε μια συλλογή γραμματοσειρών. Ανάγνωση/εγγραφή [FontCollectionIndex](../../com.aspose.slides/fontcollectionindex).

**Επιστρέφει:**
byte
### setFontCollectionIndex(byte value) {#setFontCollectionIndex-byte-}
```
public final void setFontCollectionIndex(byte value)
```

Επιστρέφει ή ορίζει το δείκτη γραμματοσειράς του σχήματος σε μια συλλογή γραμματοσειρών. Ανάγνωση/εγγραφή [FontCollectionIndex](../../com.aspose.slides/fontcollectionindex).

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | byte |  |