---
title: IFilterEffect
second_title: Aspose.Slides για Android μέσω Αναφοράς Java API
description: Αντιπροσωπεύει το εφέ φίλτρου της συμπεριφοράς.
type: docs
url: /el/com.aspose.slides/ifiltereffect/
---
**Όλες οι Υλοποιημένες Διεπαφές:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IFilterEffect extends IBehavior
```

Αντιπροσωπεύει το εφέ φίλτρου της συμπεριφοράς.
## Μέθοδοι

| Μέθοδος | Περιγραφή |
| --- | --- |
| [getReveal()](#getReveal--) | Αντιπροσωπεύει ότι το εφέ με τη συμπεριφορά πρέπει να αποκαλύπτεται (εισόδου/εξόδου) Ανάγνωση/Εγγραφή [FilterEffectRevealType](../../com.aspose.slides/filtereffectrevealtype). |
| [setReveal(int value)](#setReveal-int-) | Αντιπροσωπεύει ότι το εφέ με τη συμπεριφορά πρέπει να αποκαλύπτεται (εισόδου/εξόδου) Ανάγνωση/Εγγραφή [FilterEffectRevealType](../../com.aspose.slides/filtereffectrevealtype). |
| [getType()](#getType--) | Αντιπροσωπεύει τον τύπο του εφέ φίλτρου. |
| [setType(int value)](#setType-int-) | Αντιπροσωπεύει τον τύπο του εφέ φίλτρου. |
| [getSubtype()](#getSubtype--) | Αντιπροσωπεύει το υποτύπο του εφέ φίλτρου. |
| [setSubtype(int value)](#setSubtype-int-) | Αντιπροσωπεύει το υποτύπο του εφέ φίλτρου. |
### getReveal() {#getReveal--}
```
public abstract int getReveal()
```

Αντιπροσωπεύει ότι το εφέ με τη συμπεριφορά πρέπει να αποκαλύπτεται (εισόδου/εξόδου) Ανάγνωση/Εγγραφή [FilterEffectRevealType](../../com.aspose.slides/filtereffectrevealtype).

**Επιστρέφει:**
int
### setReveal(int value) {#setReveal-int-}
```
public abstract void setReveal(int value)
```

Αντιπροσωπεύει ότι το εφέ με τη συμπεριφορά πρέπει να αποκαλύπτεται (εισόδου/εξόδου) Ανάγνωση/Εγγραφή [FilterEffectRevealType](../../com.aspose.slides/filtereffectrevealtype).

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | int |  |
### getType() {#getType--}
```
public abstract int getType()
```

Αντιπροσωπεύει τον τύπο του εφέ φίλτρου. Ανάγνωση/Εγγραφή [FilterEffectType](../../com.aspose.slides/filtereffecttype).

**Επιστρέφει:**
int
### setType(int value) {#setType-int-}
```
public abstract void setType(int value)
```

Αντιπροσωπεύει τον τύπο του εφέ φίλτρου. Ανάγνωση/Εγγραφή [FilterEffectType](../../com.aspose.slides/filtereffecttype).

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | int |  |
### getSubtype() {#getSubtype--}
```
public abstract int getSubtype()
```

Αντιπροσωπεύει το υποτύπο του εφέ φίλτρου. Ανάγνωση/Εγγραφή [FilterEffectSubtype](../../com.aspose.slides/filtereffectsubtype).

**Επιστρέφει:**
int
### setSubtype(int value) {#setSubtype-int-}
```
public abstract void setSubtype(int value)
```

Αντιπροσωπεύει το υποτύπο του εφέ φίλτρου. Ανάγνωση/Εγγραφή [FilterEffectSubtype](../../com.aspose.slides/filtereffectsubtype).

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | int |  |