---
title: MarkerStyleType
second_title: Aspose.Slides για Android μέσω Αναφοράς API Java
description: Καθορίζει τη μορφή του δείκτη στα δεδομένα των διαγραμμάτων.
type: docs
url: /el/com.aspose.slides/markerstyletype/
---
**Κληρονομιά:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class MarkerStyleType extends System.Enum
```

Καθορίζει τη μορφή του δείκτη στο σημείο δεδομένων του διαγράμματος.
## Πεδία

| Field | Description |
| --- | --- |
| [NotDefined](#NotDefined) |  |
| [Circle](#Circle) | Καθορίζει ότι θα σχεδιαστεί ένας κύκλος σε κάθε σημείο δεδομένων. |
| [Dash](#Dash) | Καθορίζει ότι θα σχεδιαστεί μια παύλα σε κάθε σημείο δεδομένων. |
| [Diamond](#Diamond) | Καθορίζει ότι θα σχεδιαστεί ένας ρόμβος σε κάθε σημείο δεδομένων. |
| [Dot](#Dot) | Καθορίζει ότι θα σχεδιαστεί μια τελεία σε κάθε σημείο δεδομένων. |
| [None](#None) | Καθορίζει ότι δεν θα σχεδιαστεί τίποτα σε κάθε σημείο δεδομένων. |
| [Picture](#Picture) | Καθορίζει ότι θα σχεδιαστεί μια εικόνα σε κάθε σημείο δεδομένων. |
| [Plus](#Plus) | Καθορίζει ότι θα σχεδιαστεί ένα σύμβολο συν (+) σε κάθε σημείο δεδομένων. |
| [Square](#Square) | Καθορίζει ότι θα σχεδιαστεί ένα τετράγωνο σε κάθε σημείο δεδομένων. |
| [Star](#Star) | Καθορίζει ότι θα σχεδιαστεί ένα αστέρι σε κάθε σημείο δεδομένων. |
| [Triangle](#Triangle) | Καθορίζει ότι θα σχεδιαστεί ένα τρίγωνο σε κάθε σημείο δεδομένων. |
| [X](#X) | Καθορίζει ότι θα σχεδιαστεί ένα X σε κάθε σημείο δεδομένων. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```




### Circle {#Circle}
```
public static final int Circle
```


Καθορίζει ότι θα σχεδιαστεί ένας κύκλος σε κάθε σημείο δεδομένων.

### Dash {#Dash}
```
public static final int Dash
```


Καθορίζει ότι θα σχεδιαστεί μια παύλα σε κάθε σημείο δεδομένων.

### Diamond {#Diamond}
```
public static final int Diamond
```


Καθορίζει ότι θα σχεδιαστεί ένας ρόμβος σε κάθε σημείο δεδομένων.

### Dot {#Dot}
```
public static final int Dot
```


Καθορίζει ότι θα σχεδιαστεί μια τελεία σε κάθε σημείο δεδομένων.

### None {#None}
```
public static final int None
```


Καθορίζει ότι δεν θα σχεδιαστεί τίποτα σε κάθε σημείο δεδομένων.

### Picture {#Picture}
```
public static final int Picture
```


Καθορίζει ότι θα σχεδιαστεί μια εικόνα σε κάθε σημείο δεδομένων.

### Plus {#Plus}
```
public static final int Plus
```


Καθορίζει ότι θα σχεδιαστεί ένα σύμβολο συν (+) σε κάθε σημείο δεδομένων.

### Square {#Square}
```
public static final int Square
```


Καθορίζει ότι θα σχεδιαστεί ένα τετράγωνο σε κάθε σημείο δεδομένων.

### Star {#Star}
```
public static final int Star
```


Καθορίζει ότι θα σχεδιαστεί ένα αστέρι σε κάθε σημείο δεδομένων.

### Triangle {#Triangle}
```
public static final int Triangle
```


Καθορίζει ότι θα σχεδιαστεί ένα τρίγωνο σε κάθε σημείο δεδομένων.

### X {#X}
```
public static final int X
```


Καθορίζει ότι θα σχεδιαστεί ένα X σε κάθε σημία δεδομένων.