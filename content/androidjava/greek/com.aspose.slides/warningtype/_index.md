---
title: WarningType
second_title: Aspose.Slides για Android μέσω αναφοράς API Java
description: Αντιπροσωπεύει έναν τύπο προειδοποίησης.
type: docs
url: /el/com.aspose.slides/warningtype/
---
**Κληρονομικότητα:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class WarningType extends System.Enum
```

Αντιπροσωπεύει έναν τύπο προειδοποίησης.
## Πεδία

| Field | Description |
| --- | --- |
| [SourceFileCorruption](#SourceFileCorruption) | Ένα ζήτημα εντοπίστηκε στο πηγαίο έγγραφο, το οποίο κάνει πολύ πιθανό ότι το έγγραφο δεν θα μπορεί να ανοίξει εάν αποθηκευτεί στην αρχική του μορφή. |
| [DataLoss](#DataLoss) | Κείμενο/γράφημα/εικόνα ή άλλα δεδομένα θα λείπουν εντελώς είτε από το δέντρο του εγγράφου μετά τη φόρτωση, είτε από το δημιουργημένο έγγραφο μετά την αποθήκευση. |
| [MajorFormattingLoss](#MajorFormattingLoss) | Σημαντική απώλεια μορφοποίησης. |
| [MinorFormattingLoss](#MinorFormattingLoss) | Μικρή απώλεια μορφοποίησης. |
| [CompatibilityIssue](#CompatibilityIssue) | Αυτή είναι γνωστή προβληματική κατάσταση που θα εμποδίσει το άνοιγμα του εγγράφου από ορισμένους χρήστες-πράκτορες ή από προηγούμενες εκδόσεις τους. |
| [UnexpectedContent](#UnexpectedContent) | Κάποιο περιεχόμενο στο πηγαίο έγγραφο δεν αναγνωρίστηκε (π.χ. |

### SourceFileCorruption {#SourceFileCorruption}
```
public static final int SourceFileCorruption
```

Ένα ζήτημα εντοπίστηκε στο πηγαίο έγγραφο, το οποίο κάνει πολύ πιθανό ότι το έγγραφο δεν θα μπορεί να ανοίξει εάν αποθηκευτεί στην αρχική του μορφή.

### DataLoss {#DataLoss}
```
public static final int DataLoss
```

Κείμενο/γράφημα/εικόνα ή άλλα δεδομένα θα λείπουν εντελώς είτε από το δέντρο του εγγράφου μετά τη φόρτωση, είτε από το δημιουργημένο έγγραφο μετά την αποθήκευση.

### MajorFormattingLoss {#MajorFormattingLoss}
```
public static final int MajorFormattingLoss
```

Σημαντική απώλεια μορφοποίησης.

### MinorFormattingLoss {#MinorFormattingLoss}
```
public static final int MinorFormattingLoss
```

Μικρή απώλεια μορφοποίησης.

### CompatibilityIssue {#CompatibilityIssue}
```
public static final int CompatibilityIssue
```

Αυτή είναι γνωστή προβληματική κατάσταση που θα εμποδίσει το άνοιγμα του εγγράφου από ορισμένους χρήστες-πράκτορες ή από προηγούμενες εκδόσεις τους.

### UnexpectedContent {#UnexpectedContent}
```
public static final int UnexpectedContent
```

Κάποιο περιεχόμενο στο πηγαίο έγγραφο δεν αναγνωρίστηκε (π.χ. είναι μη υποστηριζόμενο), αυτό μπορεί ή όχι να προκαλέσει προβλήματα ή να οδηγήσει σε απώλεια δεδομένων/μορφοποίησης.