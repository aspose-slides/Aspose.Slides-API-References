---
title: AfterAnimationType
second_title: Aspose.Slides για Android μέσω Java API Αναφορά
description: Αναπαριστά τον τύπο μετά το animation ενός εφέ animation.
type: docs
url: /el/com.aspose.slides/afteranimationtype/
---
**Κληρονομικότητα:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class AfterAnimationType extends System.Enum
```

Αναπαριστά τον τύπο μετά το animation ενός εφέ animation.

## Πεδία

| Πεδίο | Περιγραφή |
| --- | --- |
| [DoNotDim](#DoNotDim) | Μην μειώνεται η φωτεινότητα μετά το animation. |
| [Color](#Color) | Χρώμα μετά το animation. |
| [HideAfterAnimation](#HideAfterAnimation) | Απόκρυψη μετά το animation. |
| [HideOnNextMouseClick](#HideOnNextMouseClick) | Απόκρυψη με το επόμενο κλικ του ποντικιού μετά το animation. |

### DoNotDim {#DoNotDim}
```
public static final int DoNotDim
```

Μην μειώνεται η φωτεινότητα μετά το animation.

### Color {#Color}
```
public static final int Color
```

Χρώμα μετά το animation.

### HideAfterAnimation {#HideAfterAnimation}
```
public static final int HideAfterAnimation
```

Απόκρυψη μετά το animation.

### HideOnNextMouseClick {#HideOnNextMouseClick}
```
public static final int HideOnNextMouseClick
```

Απόκρυψη με το επόμενο κλικ του ποντικιού μετά το animation.