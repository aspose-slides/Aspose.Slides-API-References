---
title: Placeholder
second_title: Aspose.Slides για Android μέσω Java API Αναφορά
description: Αναπαριστά ένα placeholder σε μια διαφάνεια.
type: docs
url: /el/com.aspose.slides/placeholder/
---
**Κληρονομικότητα:**
java.lang.Object

**Όλες οι Υλοποιημένες Διεπαφές:**
[com.aspose.slides.IPlaceholder](../../com.aspose.slides/iplaceholder)
```
public class Placeholder implements IPlaceholder
```

Αναπαριστά ένα placeholder σε μια διαφάνεια.

## Μέθοδοι

| Μέθοδος | Περιγραφή |
| --- | --- |
| [getOrientation()](#getOrientation--) | Λαμβάνει ή ορίζει τον προσανατολισμό ενός placeholder. |
| [getSize()](#getSize--) | Λαμβάνει ή ορίζει το μέγεθος ενός placeholder. |
| [getType()](#getType--) | Λαμβάνει ή ορίζει τον τύπο ενός placeholder. |
| [getIndex()](#getIndex--) | Λαμβάνει ή ορίζει το ευρετήριο ενός placeholder. |

### getOrientation() {#getOrientation--}
```
public byte getOrientation()
```

Λαμβάνει ή ορίζει τον προσανατολισμό ενός placeholder. Μόνο για ανάγνωση [Orientation](../../com.aspose.slides/orientation).

**Επιστρέφει:**
byte

### getSize() {#getSize--}
```
public byte getSize()
```

Λαμβάνει ή ορίζει το μέγεθος ενός placeholder. Μόνο για ανάγνωση [PlaceholderSize](../../com.aspose.slides/placeholdersize).

**Επιστρέφει:**
byte

### getType() {#getType--}
```
public byte getType()
```

Λαμβάνει ή ορίζει τον τύπο ενός placeholder. Μόνο για ανάγνωση [PlaceholderType](../../com.aspose.slides/placeholdertype).

**Επιστρέφει:**
byte

### getIndex() {#getIndex--}
```
public long getIndex()
```

Λαμβάνει ή ορίζει το ευρετήριο ενός placeholder. Μόνο για ανάγνωση long.

**Επιστρέφει:**
long