---
title: ErrorBarValueType
second_title: Aspose.Slides για Android μέσω Java API Reference
description: Αναπαριστά τον τύπο της τιμής της γραμμής σφάλματος
type: docs
url: /el/com.aspose.slides/errorbarvaluetype/
---
**Κληρονομικότητα:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class ErrorBarValueType extends System.Enum
```

Αντιπροσωπεύει τον τύπο της τιμής της γραμμής σφάλματος
## Πεδία

| Πεδίο | Περιγραφή |
| --- | --- |
| [Custom](#Custom) | Καθορίζει ότι το μήκος των γραμμών σφάλματος πρέπει να καθορίζεται από τα στοιχεία Plus και Minus. |
| [Fixed](#Fixed) | Καθορίζει ότι το μήκος των γραμμών σφάλματος πρέπει να είναι η σταθερή τιμή που προσδιορίζεται από το Error Bar Value. |
| [Percentage](#Percentage) | Καθορίζει ότι το μήκος των γραμμών σφάλματος πρέπει να είναι το ποσοστό Error Bar Value των δεδομένων. |
| [StandardDeviation](#StandardDeviation) | Καθορίζει ότι το μήκος των γραμμών σφάλματος πρέπει να είναι οι τυπικές αποκλίσεις Error Bar Value των δεδομένων. |
| [StandardError](#StandardError) | Καθορίζει ότι το μήκος των γραμμών σφάλματος πρέπει να είναι τα τυπικά σφάλματα Error Bar Value των δεδομένων. |
### Custom {#Custom}
```
public static final int Custom
```

Καθορίζει ότι το μήκος των γραμμών σφάλματος πρέπει να καθορίζεται από τα στοιχεία Plus και Minus.

### Fixed {#Fixed}
```
public static final int Fixed
```

Καθορίζει ότι το μήκος των γραμμών σφάλματος πρέπει να είναι η σταθερή τιμή που προσδιορίζεται από το Error Bar Value.

### Percentage {#Percentage}
```
public static final int Percentage
```

Καθορίζει ότι το μήκος των γραμμών σφάλματος πρέπει να είναι το ποσοστό Error Bar Value των δεδομένων.

### StandardDeviation {#StandardDeviation}
```
public static final int StandardDeviation
```

Καθορίζει ότι το μήκος των γραμμών σφάλματος πρέπει να είναι οι τυπικές αποκλίσεις Error Bar Value των δεδομένων.

### StandardError {#StandardError}
```
public static final int StandardError
```

Καθορίζει ότι το μήκος των γραμμών σφάλματος πρέπει να είναι τα τυπικά σφάλματα Error Bar Value των δεδομένων.