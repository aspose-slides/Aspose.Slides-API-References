---
title: IColorChangeEffectiveData
second_title: Aspose.Slides για Android μέσω αναφοράς Java API
description: Αμετάβλητο αντικείμενο που αντιπροσωπεύει την εφέ Αλλαγής Χρώματος.
type: docs
url: /el/com.aspose.slides/icolorchangeeffectivedata/
---
**Όλες οι Υλοποιημένες Διεπαφές:**
[com.aspose.slides.IEffectEffectiveData](../../com.aspose.slides/ieffecteffectivedata)
```
public interface IColorChangeEffectiveData extends IEffectEffectiveData
```

Αμετάβλητο αντικείμενο που αντιπροσωπεύει την εφέ Αλλαγής Χρώματος. Τα στιγμιότυπα του FromColor αντικαθίστανται με τα στιγμιότυπα του ToColor.
## Μέθοδοι

| Μέθοδος | Περιγραφή |
| --- | --- |
| [getFromColor()](#getFromColor--) | Χρώμα που θα αντικατασταθεί. |
| [getToColor()](#getToColor--) | Χρώμα που θα αντικαταστήσει. |
| [getUseAlpha()](#getUseAlpha--) | Επιστρέφει μια boolean τιμή που καθορίζει αν θα χρησιμοποιηθεί το component alpha. |
### getFromColor() {#getFromColor--}
```
public abstract Integer getFromColor()
```

Χρώμα που θα αντικατασταθεί. Μόνο ανάγνωση java.lang.Integer.

**Επιστρέφει:**
java.lang.Integer
### getToColor() {#getToColor--}
```
public abstract Integer getToColor()
```

Χρώμα που θα αντικαταστήσει. Μόνο ανάγνωση java.lang.Integer.

**Επιστρέφει:**
java.lang.Integer
### getUseAlpha() {#getUseAlpha--}
```
public abstract boolean getUseAlpha()
```

Επιστρέφει μια boolean τιμή που καθορίζει αν θα χρησιμοποιηθεί το component alpha. Μόνο ανάγνωση boolean.

**Επιστρέφει:**
boolean