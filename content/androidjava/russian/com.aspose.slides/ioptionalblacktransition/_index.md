---
title: IOptionalBlackTransition
second_title: Aspose.Slides для Android через справку Java API
description: Необязательный чёрный эффект перехода слайда.
type: docs
url: /ru/com.aspose.slides/ioptionalblacktransition/
---
**Все реализованные интерфейсы:**
[com.aspose.slides.ITransitionValueBase](../../com.aspose.slides/itransitionvaluebase)
```
public interface IOptionalBlackTransition extends ITransitionValueBase
```

Необязательный чёрный эффект перехода слайда.
## Методы

| Метод | Описание |
| --- | --- |
| [getFromBlack()](#getFromBlack--) | Этот атрибут указывает, будет ли переход начинаться с чёрного экрана (а затем переходить к новому слайду поверх чёрного). |
| [setFromBlack(boolean value)](#setFromBlack-boolean-) | Этот атрибут указывает, будет ли переход начинаться с чёрного экрана (а затем переходить к новому слайду поверх чёрного). |
### getFromBlack() {#getFromBlack--}
```
public abstract boolean getFromBlack()
```

Этот атрибут указывает, будет ли переход начинаться с чёрного экрана (а затем переходить к новому слайду поверх чёрного). Чтение/запись boolean.

**Возвращаемое значение:**
boolean
### setFromBlack(boolean value) {#setFromBlack-boolean-}
```
public abstract void setFromBlack(boolean value)
```

Этот атрибут указывает, будет ли переход начинаться с чёрного экрана (а затем переходить к новому слайду поверх чёрного). Чтение/запись boolean.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | boolean |  |