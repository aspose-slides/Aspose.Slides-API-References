---
title: TextSearchOptions
second_title: Aspose.Slides для Android через Java API Reference
description: Представляет параметры, которые могут использоваться для поиска текста в объекте Presentation, Slide или TextFrame.
type: docs
url: /ru/com.aspose.slides/textsearchoptions/
---
**Наследование:**
java.lang.Object

**Все реализованные интерфейсы:**
[com.aspose.slides.ITextSearchOptions](../../com.aspose.slides/itextsearchoptions)
```
public class TextSearchOptions implements ITextSearchOptions
```

Представляет параметры, которые могут использоваться для поиска текста в объекте Presentation, Slide или TextFrame.

## Конструкторы

| Конструктор | Описание |
| --- | --- |
| [TextSearchOptions()](#TextSearchOptions--) | Создаёт новые параметры поиска текста по умолчанию. |

## Методы

| Метод | Описание |
| --- | --- |
| [getCaseSensitive()](#getCaseSensitive--) | Устанавливает true для поиска с учётом регистра, иначе false. |
| [setCaseSensitive(boolean value)](#setCaseSensitive-boolean-) | Устанавливает true для поиска с учётом регистра, иначе false. |
| [getWholeWordsOnly()](#getWholeWordsOnly--) | Устанавливает true для совпадения только целых слов, иначе false. |
| [setWholeWordsOnly(boolean value)](#setWholeWordsOnly-boolean-) | Устанавливает true для совпадения только целых слов, иначе false. |
| [getIncludeNotes()](#getIncludeNotes--) | Устанавливает true для включения текста, содержащегося в заметках слайда ([NotesSlide](../../com.aspose.slides/notesslide)) при выполнении поиска текста, замену или подсветку. |
| [setIncludeNotes(boolean value)](#setIncludeNotes-boolean-) | Устанавливает true для включения текста, содержащегося в заметках слайда ([NotesSlide](../../com.aspose.slides/notesslide)) при выполнении поиска текста, замену или подсветку. |

### TextSearchOptions() {#TextSearchOptions--}
```
public TextSearchOptions()
```

Создаёт новые параметры поиска текста по умолчанию.

### getCaseSensitive() {#getCaseSensitive--}
```
public final boolean getCaseSensitive()
```

Устанавливает true для поиска с учётом регистра, иначе false. Чтение/запись  boolean .

**Возвращаемое значение:**
boolean

### setCaseSensitive(boolean value) {#setCaseSensitive-boolean-}
```
public final void setCaseSensitive(boolean value)
```

Устанавливает true для поиска с учётом регистра, иначе false. Чтение/запись  boolean .

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | boolean |  |

### getWholeWordsOnly() {#getWholeWordsOnly--}
```
public final boolean getWholeWordsOnly()
```

Устанавливает true для совпадения только целых слов, иначе false. Чтение/запись  boolean .

**Возвращаемое значение:**
boolean

### setWholeWordsOnly(boolean value) {#setWholeWordsOnly-boolean-}
```
public final void setWholeWordsOnly(boolean value)
```

Устанавливает true для совпадения только целых слов, иначе false. Чтение/запись  boolean .

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | boolean |  |

### getIncludeNotes() {#getIncludeNotes--}
```
public final boolean getIncludeNotes()
```

Устанавливает true для включения текста, содержащегося в заметках слайда ([NotesSlide](../../com.aspose.slides/notesslide)) при выполнении поиска текста, замену или подсветку. Значение по умолчанию — false.

**Возвращаемое значение:**
boolean

### setIncludeNotes(boolean value) {#setIncludeNotes-boolean-}
```
public final void setIncludeNotes(boolean value)
```

Устанавливает true для включения текста, содержащегося в заметках слайда ([NotesSlide](../../com.aspose.slides/notesslide)) при выполнении поиска текста, замену или подсветку. Значение по умолчанию — false.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | boolean |  |