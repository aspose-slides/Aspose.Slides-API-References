---
title: ScaleEffect
second_title: Справочник API Aspose.Slides для Android на Java
description: Представляет эффект масштабирования анимации.
type: docs
url: /ru/com.aspose.slides/scaleeffect/
---
**Наследование:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**Все реализованные интерфейсы:**
[com.aspose.slides.IScaleEffect](../../com.aspose.slides/iscaleeffect)
```
public class ScaleEffect extends Behavior implements IScaleEffect
```

Представляет эффект масштабирования анимации.
## Конструкторы

| Конструктор | Описание |
| --- | --- |
| [ScaleEffect()](#ScaleEffect--) |  |
## Методы

| Метод | Описание |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Определяет, следует ли масштабировать контент. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Определяет, следует ли масштабировать контент. |
| [getFrom()](#getFrom--) | Указывает координату x/y, с которой начинается анимация (в процентах). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | Указывает координату x/y, с которой начинается анимация (в процентах). |
| [getTo()](#getTo--) | Указывает целевое расположение для эффекта масштабирования анимации (в процентах). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | Указывает целевое расположение для эффекта масштабирования анимации (в процентах). |
| [getBy()](#getBy--) | Описывает относительное значение смещения для анимации (в процентах). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | Описывает относительное значение смещения для анимации (в процентах). |
### ScaleEffect() {#ScaleEffect--}
```
public ScaleEffect()
```

### getZoomContent() {#getZoomContent--}
```
public final byte getZoomContent()
```

Определяет, следует ли масштабировать контент. Чтение/запись [NullableBool](../../com.aspose.slides/nullablebool).

**Возвращает:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public final void setZoomContent(byte value)
```

Определяет, следует ли масштабировать контент. Чтение/запись [NullableBool](../../com.aspose.slides/nullablebool).

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public final PointF getFrom()
```

Указывает координату x/y, с которой начинается анимация (в процентах). Чтение/запись android.graphics.PointF.

**Возвращает:**
android.graphics.PointF
### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public final void setFrom(PointF value)
```

Указывает координату x/y, с которой начинается анимация (в процентах). Чтение/запись android.graphics.PointF.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public final PointF getTo()
```

Указывает целевое расположение для эффекта масштабирования анимации (в процентах). Чтение/запись android.graphics.PointF.

**Возвращает:**
android.graphics.PointF
### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public final void setTo(PointF value)
```

Указывает целевое расположение для эффекта масштабирования анимации (в процентах). Чтение/запись android.graphics.PointF.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public final PointF getBy()
```

Описывает относительное значение смещения для анимации (в процентах). Чтение/запись android.graphics.PointF.

**Возвращает:**
android.graphics.PointF
### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public final void setBy(PointF value)
```

Описывает относительное значение смещения для анимации (в процентах). Чтение/запись android.graphics.PointF.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | android.graphics.PointF |  |