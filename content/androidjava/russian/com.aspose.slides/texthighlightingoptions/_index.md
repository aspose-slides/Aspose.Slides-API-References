---
title: TextHighlightingOptions
second_title: Aspose.Slides для Android через Java API Справка
description: Представляет параметры, которые могут использоваться для выделения текста в текстовом фрейме.
type: docs
url: /ru/com.aspose.slides/texthighlightingoptions/
---
**Наследование:**
java.lang.Object

**Все реализованные интерфейсы:**
[com.aspose.slides.ITextHighlightingOptions](../../com.aspose.slides/itexthighlightingoptions)
```
public class TextHighlightingOptions implements ITextHighlightingOptions
```

Представляет параметры, которые могут использоваться для выделения текста в текстовом фрейме.
## Конструкторы

| Конструктор | Описание |
| --- | --- |
| [TextHighlightingOptions()](#TextHighlightingOptions--) | Создаёт новые параметры выделения текста по умолчанию. |
## Методы

| Метод | Описание |
| --- | --- |
| [getCaseSensitive()](#getCaseSensitive--) | Установите true, чтобы использовать поиск с учётом регистра, иначе false. |
| [setCaseSensitive(boolean value)](#setCaseSensitive-boolean-) | Установите true, чтобы использовать поиск с учётом регистра, иначе false. |
| [getWholeWordsOnly()](#getWholeWordsOnly--) | Установите true, чтобы находить только целые слова, иначе false. |
| [setWholeWordsOnly(boolean value)](#setWholeWordsOnly-boolean-) | Установите true, чтобы находить только целые слова, иначе false. |
### TextHighlightingOptions() {#TextHighlightingOptions--}
```
public TextHighlightingOptions()
```

Создаёт новые параметры выделения текста по умолчанию.

### getCaseSensitive() {#getCaseSensitive--}
```
public final boolean getCaseSensitive()
```

Установите true, чтобы использовать поиск с учётом регистра, иначе false. Чтение/запись boolean.

**Возвращаемое значение:**
boolean
### setCaseSensitive(boolean value) {#setCaseSensitive-boolean-}
```
public final void setCaseSensitive(boolean value)
```

Установите true, чтобы использовать поиск с учётом регистра, иначе false. Чтение/запись boolean.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | boolean |  |

### getWholeWordsOnly() {#getWholeWordsOnly--}
```
public final boolean getWholeWordsOnly()
```

Установите true, чтобы находить только целые слова, иначе false. Чтение/запись boolean.

**Возвращаемое значение:**
boolean
### setWholeWordsOnly(boolean value) {#setWholeWordsOnly-boolean-}
```
public final void setWholeWordsOnly(boolean value)
```

Установите true, чтобы находить только целые слова, иначе false. Чтение/запись boolean.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | boolean |  |