---
title: OptionalBlackTransition
second_title: Aspose.Slides для Android через справку Java API
description: Опциональный эффект перехода с чёрного слайда.
type: docs
url: /ru/com.aspose.slides/optionalblacktransition/
---
**Наследование:**
java.lang.Object, [com.aspose.slides.TransitionValueBase](../../com.aspose.slides/transitionvaluebase)

**Все реализованные интерфейсы:**
[com.aspose.slides.IOptionalBlackTransition](../../com.aspose.slides/ioptionalblacktransition)
```
public class OptionalBlackTransition extends TransitionValueBase implements IOptionalBlackTransition
```

Опциональный эффект перехода с чёрного слайда.
## Методы

| Метод | Описание |
| --- | --- |
| [getFromBlack()](#getFromBlack--) | Этот атрибут указывает, начнётся ли переход с чёрного экрана (а затем новый слайд будет переходить поверх чёрного). |
| [setFromBlack(boolean value)](#setFromBlack-boolean-) | Этот атрибут указывает, начнётся ли переход с чёрного экрана (а затем новый слайд будет переходить поверх чёрного). |
### getFromBlack() {#getFromBlack--}
```
public final boolean getFromBlack()
```

Этот атрибут указывает, начнётся ли переход с чёрного экрана (а затем новый слайд будет переходить поверх чёрного). Чтение/запись boolean.

**Возвращаемое значение:**
boolean
### setFromBlack(boolean value) {#setFromBlack-boolean-}
```
public final void setFromBlack(boolean value)
```

Этот атрибут указывает, начнётся ли переход с чёрного экрана (а затем новый слайд будет переходить поверх чёрного). Чтение/запись boolean.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | boolean |  |