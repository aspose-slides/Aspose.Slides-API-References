---
title: Timing
second_title: Aspose.Slides для Android через справочник API Java
description: Представляет тайминг анимации.
type: docs
url: /ru/com.aspose.slides/timing/
---
**Наследование:**
java.lang.Object

**Все реализованные интерфейсы:**
[com.aspose.slides.ITiming](../../com.aspose.slides/itiming), com.aspose.slides.IDOMObject
```
public class Timing implements ITiming, IDOMObject
```

Представляет тайминг анимации.
## Методы

| Метод | Описание |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | Описывает процент длительности ускоряющего поведения эффекта. |
| [setAccelerate(float value)](#setAccelerate-float-) | Описывает процент длительности ускоряющего поведения эффекта. |
| [getDecelerate()](#getDecelerate--) | Описывает процент длительности замедляющего поведения эффекта. |
| [setDecelerate(float value)](#setDecelerate-float-) | Описывает процент длительности замедляющего поведения эффекта. |
| [getAutoReverse()](#getAutoReverse--) | Описывает, следует ли автоматически воспроизводить анимацию в обратном направлении после воспроизведения в прямом направлении. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | Описывает, следует ли автоматически воспроизводить анимацию в обратном направлении после воспроизведения в прямом направлении. |
| [getDuration()](#getDuration--) | Описывает длительность эффекта анимации. |
| [setDuration(float value)](#setDuration-float-) | Описывает длительность эффекта анимации. |
| [getRepeatCount()](#getRepeatCount--) | Описывает количество повторений эффекта. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | Описывает количество повторений эффекта. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | Этот атрибут указывает, будет ли эффект повторяться до конца слайда. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | Этот атрибут указывает, будет ли эффект повторяться до конца слайда. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | Этот атрибут указывает, будет ли эффект повторяться до следующего клика. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | Этот атрибут указывает, будет ли эффект повторяться до следующего клика. |
| [getRepeatDuration()](#getRepeatDuration--) | Описывает количество повторений эффекта. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | Описывает количество повторений эффекта. |
| [getRestart()](#getRestart--) | Указывает, следует ли перезапускать эффект после завершения. |
| [setRestart(int value)](#setRestart-int-) | Указывает, следует ли перезапускать эффект после завершения. |
| [getRewind()](#getRewind--) | Этот атрибут указывает, будет ли эффект перематываться после завершения воспроизведения. |
| [setRewind(boolean value)](#setRewind-boolean-) | Этот атрибут указывает, будет ли эффект перематываться после завершения воспроизведения. |
| [getSpeed()](#getSpeed--) | Указывает процент, на который нужно ускорить (или замедлить) тайминг. |
| [setSpeed(float value)](#setSpeed-float-) | Указывает процент, на который нужно ускорить (или замедлить) тайминг. |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | Описывает время задержки после триггера. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | Описывает время задержки после триггера. |
| [getTriggerType()](#getTriggerType--) | Описывает тип триггера. |
| [setTriggerType(int value)](#setTriggerType-int-) | Описывает тип триггера. |
| [getParent_Immediate()](#getParent-Immediate--) |  |

### getAccelerate() {#getAccelerate--}
```
public final float getAccelerate()
```

Описывает процент длительности ускоряющего поведения эффекта. Чтение/запись float.

**Возвращает:**
float
### setAccelerate(float value) {#setAccelerate-float-}
```
public final void setAccelerate(float value)
```

Описывает процент длительности ускоряющего поведения эффекта. Чтение/запись float.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public final float getDecelerate()
```

Описывает процент длительности замедляющего поведения эффекта. Чтение/запись float.

**Возвращает:**
float
### setDecelerate(float value) {#setDecelerate-float-}
```
public final void setDecelerate(float value)
```

Описывает процент длительности замедляющего поведения эффекта. Чтение/запись float.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public final boolean getAutoReverse()
```

Описывает, следует ли автоматически воспроизводить анимацию в обратном направлении после воспроизведения в прямом направлении. Чтение/запись boolean.

**Возвращает:**
boolean
### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public final void setAutoReverse(boolean value)
```

Описывает, следует ли автоматически воспроизводить анимацию в обратном направлении после воспроизведения в прямом направлении. Чтение/запись boolean.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public final float getDuration()
```

Описывает длительность эффекта анимации. Чтение/запись float.

**Возвращает:**
float
### setDuration(float value) {#setDuration-float-}
```
public final void setDuration(float value)
```

Описывает длительность эффекта анимации. Чтение/запись float.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public final float getRepeatCount()
```

Описывает количество повторений эффекта. Чтение/запись float.

**Возвращает:**
float
### setRepeatCount(float value) {#setRepeatCount-float-}
```
public final void setRepeatCount(float value)
```

Описывает количество повторений эффекта. Чтение/запись float.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public final boolean getRepeatUntilEndSlide()
```

Этот атрибут указывает, будет ли эффект повторяться до конца слайда. Чтение/запись boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Получить последовательность эффектов для первого слайда
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Получить первый эффект основной последовательности.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Изменить тайминг/повтор эффекта на "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Возвращает:**
boolean
### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public final void setRepeatUntilEndSlide(boolean value)
```

Этот атрибут указывает, будет ли эффект повторяться до конца слайда. Чтение/запись boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Получить последовательность эффектов для первого слайда
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Получить первый эффект основной последовательности.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Изменить тайминг/повтор эффекта на "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public final boolean getRepeatUntilNextClick()
```

Этот атрибут указывает, будет ли эффект повторяться до следующего клика. Чтение/запись boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Получить последовательность эффектов для первого слайда
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Получить первый эффект основной последовательности.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Изменить тайминг/повтор эффекта на "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Возвращает:**
boolean
### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public final void setRepeatUntilNextClick(boolean value)
```

Этот атрибут указывает, будет ли эффект повторяться до следующего клика. Чтение/запись boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Получить последовательность эффектов для первого слайда
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Получить первый эффект основной последовательности.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Изменить тайминг/повтор эффекта на "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public final float getRepeatDuration()
```

Описывает количество повторений эффекта. Чтение/запись float.

**Возвращает:**
float
### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public final void setRepeatDuration(float value)
```

Описывает количество повторений эффекта. Чтение/запись float.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public final int getRestart()
```

Указывает, следует ли перезапускать эффект после завершения. Чтение/запись [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Возвращает:**
int
### setRestart(int value) {#setRestart-int-}
```
public final void setRestart(int value)
```

Указывает, следует ли перезапускать эффект после завершения. Чтение/запись [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public final boolean getRewind()
```

Этот атрибут указывает, будет ли эффект перематываться после завершения воспроизведения. Чтение/запись boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Получить последовательность эффектов для первого слайда
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Получить первый эффект основной последовательности.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Включить тайминг/перемотку эффекта.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Возвращает:**
boolean
### setRewind(boolean value) {#setRewind-boolean-}
```
public final void setRewind(boolean value)
```

Этот атрибут указывает, будет ли эффект перематываться после завершения воспроизведения. Чтение/запись boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Получить последовательность эффектов для первого слайда
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Получить первый эффект основной последовательности.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Включить тайминг/перемотку эффекта.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | boolean |  |

### getSpeed() {#getSpeed--}
```
public final float getSpeed()
```

Указывает процент, на который нужно ускорить (или замедлить) тайминг. Чтение/запись float.

**Возвращает:**
float
### setSpeed(float value) {#setSpeed-float-}
```
public final void setSpeed(float value)
```

Указывает процент, на который нужно ускорить (или замедлить) тайминг. Чтение/запись float.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public final float getTriggerDelayTime()
```

Описывает время задержки после триггера. Чтение/запись float.

**Возвращает:**
float
### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public final void setTriggerDelayTime(float value)
```

Описывает время задержки после триггера. Чтение/запись float.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public final int getTriggerType()
```

Описывает тип триггера. Чтение/запись [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Возвращает:**
int
### setTriggerType(int value) {#setTriggerType-int-}
```
public final void setTriggerType(int value)
```

Описывает тип триггера. Чтение/запись [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | int |  |

### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

Возвращает объект Parent_Immediate. Только чтение IDOMObject.

**Возвращает:**
com.aspose.slides.IDOMObject