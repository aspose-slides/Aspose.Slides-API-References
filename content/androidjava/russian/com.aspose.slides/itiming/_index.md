---
title: ITiming
second_title: Aspose.Slides for Android via Java API Reference
description: Представляет тайминг анимации.
type: docs
url: /ru/com.aspose.slides/itiming/
---```
public interface ITiming
```

Представляет тайминг анимации.
## Методы

| Метод | Описание |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | Описывает процент продолжительности ускоряющего поведения эффекта. |
| [setAccelerate(float value)](#setAccelerate-float-) | Описывает процент продолжительности ускоряющего поведения эффекта. |
| [getDecelerate()](#getDecelerate--) | Описывает процент продолжительности замедляющего поведения эффекта. |
| [setDecelerate(float value)](#setDecelerate-float-) | Описывает процент продолжительности замедляющего поведения эффекта. |
| [getAutoReverse()](#getAutoReverse--) | Описывает, следует ли автоматически воспроизводить анимацию в обратном направлении после воспроизведения в прямом направлении. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | Описывает, следует ли автоматически воспроизводить анимацию в обратном направлении после воспроизведения в прямом направлении. |
| [getDuration()](#getDuration--) | Описывает длительность эффекта анимации. |
| [setDuration(float value)](#setDuration-float-) | Описывает длительность эффекта анимации. |
| [getRepeatCount()](#getRepeatCount--) | Описывает количество повторов эффекта. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | Описывает количество повторов эффекта. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | Этот атрибут указывает, будет ли эффект повторяться до конца слайда. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | Этот атрибут указывает, будет ли эффект повторяться до конца слайда. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | Этот атрибут указывает, будет ли эффект повторяться до следующего щелчка. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | Этот атрибут указывает, будет ли эффект повторяться до следующего щелчка. |
| [getRepeatDuration()](#getRepeatDuration--) | Описывает количество повторов эффекта. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | Описывает количество повторов эффекта. |
| [getRestart()](#getRestart--) | Указывает, следует ли перезапустить эффект после завершения. |
| [setRestart(int value)](#setRestart-int-) | Указывает, следует ли перезапустить эффект после завершения. |
| [getSpeed()](#getSpeed--) | Указывает процент, на который следует ускорить (или замедлить) тайминг. |
| [setSpeed(float value)](#setSpeed-float-) | Указывает процент, на который следует ускорить (или замедлить) тайминг. |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | Описывает задержку после триггера. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | Описывает задержку после триггера. |
| [getTriggerType()](#getTriggerType--) | Описывает тип триггера. |
| [setTriggerType(int value)](#setTriggerType-int-) | Описывает тип триггера. |
| [getRewind()](#getRewind--) | Этот атрибут указывает, будет ли эффект перемотываться назад после завершения воспроизведения. |
| [setRewind(boolean value)](#setRewind-boolean-) | Этот атрибут указывает, будет ли эффект перемотываться назад после завершения воспроизведения. |
### getAccelerate() {#getAccelerate--}
```
public abstract float getAccelerate()
```


Описывает процент продолжительности ускоряющего поведения эффекта. Чтение/запись float.

**Возвращаемое значение:**
float
### setAccelerate(float value) {#setAccelerate-float-}
```
public abstract void setAccelerate(float value)
```


Описывает процент продолжительности ускоряющего поведения эффекта. Чтение/запись float.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public abstract float getDecelerate()
```


Описывает процент продолжительности замедляющего поведения эффекта. Чтение/запись float.

**Возвращаемое значение:**
float
### setDecelerate(float value) {#setDecelerate-float-}
```
public abstract void setDecelerate(float value)
```


Описывает процент продолжительности замедляющего поведения эффекта. Чтение/запись float.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public abstract boolean getAutoReverse()
```


Описывает, следует ли автоматически воспроизводить анимацию в обратном направлении после воспроизведения в прямом направлении. Чтение/запись boolean.

**Возвращаемое значение:**
boolean
### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public abstract void setAutoReverse(boolean value)
```


Описывает, следует ли автоматически воспроизводить анимацию в обратном направлении после воспроизведения в прямом направлении. Чтение/запись boolean.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public abstract float getDuration()
```


Описывает длительность эффекта анимации. Чтение/запись float.

**Возвращаемое значение:**
float
### setDuration(float value) {#setDuration-float-}
```
public abstract void setDuration(float value)
```


Описывает длительность эффекта анимации. Чтение/запись float.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public abstract float getRepeatCount()
```


Описывает количество повторов эффекта. Чтение/запись float.

**Возвращаемое значение:**
float
### setRepeatCount(float value) {#setRepeatCount-float-}
```
public abstract void setRepeatCount(float value)
```


Описывает количество повторов эффекта. Чтение/запись float.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public abstract boolean getRepeatUntilEndSlide()
```


Этот атрибут указывает, будет ли эффект повторяться до конца слайда. Чтение/запись boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Получить последовательность эффектов для первого слайда
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Получить первый эффект основной последовательности.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Изменить тайминг/повтор эффекта на "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Возвращаемое значение:**
boolean
### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public abstract void setRepeatUntilEndSlide(boolean value)
```


Этот атрибут указывает, будет ли эффект повторяться до конца слайда. Чтение/запись boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Получить последовательность эффектов для первого слайда
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Получить первый эффект основной последовательности.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Изменить тайминг/повтор эффекта на "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public abstract boolean getRepeatUntilNextClick()
```


Этот атрибут указывает, будет ли эффект повторяться до следующего щелчка. Чтение/запись boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Получить последовательность эффектов для первого слайда
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Получить первый эффект основной последовательности.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Изменить тайминг/повтор эффекта на "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Возвращаемое значение:**
boolean
### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public abstract void setRepeatUntilNextClick(boolean value)
```


Этот атрибут указывает, будет ли эффект повторяться до следующего щелчка. Чтение/запись boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Получить последовательность эффектов для первого слайда
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Получить первый эффект основной последовательности.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Изменить тайминг/повтор эффекта на "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public abstract float getRepeatDuration()
```


Описывает количество повторов эффекта. Чтение/запись float.

**Возвращаемое значение:**
float
### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public abstract void setRepeatDuration(float value)
```


Описывает количество повторов эффекта. Чтение/запись float.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public abstract int getRestart()
```


Указывает, следует ли перезапустить эффект после завершения. Чтение/запись [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Возвращаемое значение:**
int
### setRestart(int value) {#setRestart-int-}
```
public abstract void setRestart(int value)
```


Указывает, следует ли перезапустить эффект после завершения. Чтение/запись [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | int |  |

### getSpeed() {#getSpeed--}
```
public abstract float getSpeed()
```


Указывает процент, на который следует ускорить (или замедлить) тайминг. Чтение/запись float.

**Возвращаемое значение:**
float
### setSpeed(float value) {#setSpeed-float-}
```
public abstract void setSpeed(float value)
```


Указывает процент, на который следует ускорить (или замедлить) тайминг. Чтение/запись float.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public abstract float getTriggerDelayTime()
```


Описывает задержку после триггера. Чтение/запись float.

**Возвращаемое значение:**
float
### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public abstract void setTriggerDelayTime(float value)
```


Описывает задержку после триггера. Чтение/запись float.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public abstract int getTriggerType()
```


Описывает тип триггера. Чтение/запись [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Возвращаемое значение:**
int
### setTriggerType(int value) {#setTriggerType-int-}
```
public abstract void setTriggerType(int value)
```


Описывает тип триггера. Чтение/запись [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public abstract boolean getRewind()
```


Этот атрибут указывает, будет ли эффект перемотываться назад после завершения воспроизведения. Чтение/запись boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Получить последовательность эффектов для первого слайда
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Получить первый эффект основной последовательности.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Включить тайминг/перемотку эффекта.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Возвращаемое значение:**
boolean
### setRewind(boolean value) {#setRewind-boolean-}
```
public abstract void setRewind(boolean value)
```


Этот атрибут указывает, будет ли эффект перемотываться назад после завершения воспроизведения. Чтение/запись boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Получить последовательность эффектов для первого слайда
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Получить первый эффект основной последовательности.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Включить тайминг/перемотку эффекта.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | boolean |  |