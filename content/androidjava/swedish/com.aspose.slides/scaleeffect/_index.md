---
title: ScaleEffect
second_title: Aspose.Slides för Android via Java API-referens
description: Representerar en animerad skalningseffekt.
type: docs
url: /sv/com.aspose.slides/scaleeffect/
---
**Arv:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**Alla implementerade gränssnitt:**
[com.aspose.slides.IScaleEffect](../../com.aspose.slides/iscaleeffect)
```
public class ScaleEffect extends Behavior implements IScaleEffect
```

Representerar en animerad skalningseffekt.
## Konstruktörer

| Konstruktor | Beskrivning |
| --- | --- |
| [ScaleEffect()](#ScaleEffect--) |  |
## Metoder

| Metod | Beskrivning |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Avgör om ett innehåll ska förstoras. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Avgör om ett innehåll ska förstoras. |
| [getFrom()](#getFrom--) | Anger en x/y-koordinat för att starta animationen från (i procent). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | Anger en x/y-koordinat för att starta animationen från (i procent). |
| [getTo()](#getTo--) | Anger målpositionen för en animerad skalningseffekt (i procent). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | Anger målpositionen för en animerad skalningseffekt (i procent). |
| [getBy()](#getBy--) | Beskriver det relativa offsetvärdet för animationen (i procent). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | Beskriver det relativa offsetvärdet för animationen (i procent). |
### ScaleEffect() {#ScaleEffect--}
```
public ScaleEffect()
```


### getZoomContent() {#getZoomContent--}
```
public final byte getZoomContent()
```


Avgör om ett innehåll ska förstoras. Läs/skriv [NullableBool](../../com.aspose.slides/nullablebool).

**Returnerar:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public final void setZoomContent(byte value)
```


Avgör om ett innehåll ska förstoras. Läs/skriv [NullableBool](../../com.aspose.slides/nullablebool).

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public final PointF getFrom()
```


Anger en x/y-koordinat för att starta animationen från (i procent). Läs/skriv android.graphics.PointF.

**Returnerar:**
android.graphics.PointF
### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public final void setFrom(PointF value)
```


Anger en x/y-koordinat för att starta animationen från (i procent). Läs/skriv android.graphics.PointF.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public final PointF getTo()
```


Anger målpositionen för en animerad skalningseffekt (i procent). Läs/skriv android.graphics.PointF.

**Returnerar:**
android.graphics.PointF
### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public final void setTo(PointF value)
```


Anger målpositionen för en animerad skalningseffekt (i procent). Läs/skriv android.graphics.PointF.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public final PointF getBy()
```


Beskriver det relativa offsetvärdet för animationen (i procent). Läs/skriv android.graphics.PointF.

**Returnerar:**
android.graphics.PointF
### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public final void setBy(PointF value)
```


Beskriver det relativa offsetvärdet för animationen (i procent). Läs/skriv android.graphics.PointF.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | android.graphics.PointF |  |