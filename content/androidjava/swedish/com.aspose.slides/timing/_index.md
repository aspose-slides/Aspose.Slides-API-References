---
title: Timing
second_title: Aspose.Slides för Android via Java API-referens
description: Representerar animeringstiming.
type: docs
url: /sv/com.aspose.slides/timing/
---
**Inheritance:**
java.lang.Object

**All Implemented Interfaces:**
[com.aspose.slides.ITiming](../../com.aspose.slides/itiming), com.aspose.slides.IDOMObject
```
public class Timing implements ITiming, IDOMObject
```

Representerar animeringstiming.
## Metoder

| Metod | Beskrivning |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | Beskriver procentandelen av varaktigheten för accelerationsbeteendeeffekten. |
| [setAccelerate(float value)](#setAccelerate-float-) | Beskriver procentandelen av varaktigheten för accelerationsbeteendeeffekten. |
| [getDecelerate()](#getDecelerate--) | Beskriver procentandelen av varaktigheten för retardationsbeteendeeffekten. |
| [setDecelerate(float value)](#setDecelerate-float-) | Beskriver procentandelen av varaktigheten för retardationsbeteendeeffekten. |
| [getAutoReverse()](#getAutoReverse--) | Beskriver huruvida animationen ska spelas automatiskt i omvänd riktning efter att ha spelats framåt. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | Beskriver huruvida animationen ska spelas automatiskt i omvänd riktning efter att ha spelats framåt. |
| [getDuration()](#getDuration--) | Beskriver varaktigheten av animationseffekten. |
| [setDuration(float value)](#setDuration-float-) | Beskriver varaktigheten av animationseffekten. |
| [getRepeatCount()](#getRepeatCount--) | Beskriver hur många gånger effekten ska upprepas. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | Beskriver hur många gånger effekten ska upprepas. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | Detta attribut anger om effekten ska upprepas tills sliden är slut. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | Detta attribut anger om effekten ska upprepas tills sliden är slut. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | Detta attribut anger om effekten ska upprepas tills nästa klick. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | Detta attribut anger om effekten ska upprepas tills nästa klick. |
| [getRepeatDuration()](#getRepeatDuration--) | Beskriver hur många gånger effekten ska upprepas. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | Beskriver hur många gånger effekten ska upprepas. |
| [getRestart()](#getRestart--) | Anger om en effekt ska startas om efter avslut. |
| [setRestart(int value)](#setRestart-int-) | Anger om en effekt ska startas om efter avslut. |
| [getRewind()](#getRewind--) | Detta attribut anger om effekten ska spolas tillbaka när den är klar. |
| [setRewind(boolean value)](#setRewind-boolean-) | Detta attribut anger om effekten ska spolas tillbaka när den är klar. |
| [getSpeed()](#getSpeed--) | Anger den procentandel med vilken timingen ska accelereras (eller decelereras). |
| [setSpeed(float value)](#setSpeed-float-) | Anger den procentandel med vilken timingen ska accelereras (eller decelereras). |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | Beskriver fördröjningstid efter utlösning. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | Beskriver fördröjningstid efter utlösning. |
| [getTriggerType()](#getTriggerType--) | Beskriver utlösningstyp. |
| [setTriggerType(int value)](#setTriggerType-int-) | Beskriver utlösningstyp. |
| [getParent_Immediate()](#getParent-Immediate--) |  |
### getAccelerate() {#getAccelerate--}
```
public final float getAccelerate()
```


Beskriver procentandelen av varaktigheten för accelerationsbeteendeeffekten. Read/write float.

**Returnerar:**
float
### setAccelerate(float value) {#setAccelerate-float-}
```
public final void setAccelerate(float value)
```


Beskriver procentandelen av varaktigheten för accelerationsbeteendeeffekten. Read/write float.

**Parametrar:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public final float getDecelerate()
```


Beskriver procentandelen av varaktigheten för retardationsbeteendeeffekten. Read/write float.

**Returnerar:**
float
### setDecelerate(float value) {#setDecelerate-float-}
```
public final void setDecelerate(float value)
```


Beskriver procentandelen av varaktigheten för retardationsbeteendeeffekten. Read/write float.

**Parametrar:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public final boolean getAutoReverse()
```


Beskriver huruvida animationen ska spelas automatiskt i omvänd riktning efter att ha spelats framåt. Read/write boolean.

**Returnerar:**
boolean
### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public final void setAutoReverse(boolean value)
```


Beskriver huruvida animationen ska spelas automatiskt i omvänd riktning efter att ha spelats framåt. Read/write boolean.

**Parametrar:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public final float getDuration()
```


Beskriver varaktigheten av animationseffekten. Read/write float.

**Returnerar:**
float
### setDuration(float value) {#setDuration-float-}
```
public final void setDuration(float value)
```


Beskriver varaktigheten av animationseffekten. Read/write float.

**Parametrar:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public final float getRepeatCount()
```


Beskriver hur många gånger effekten ska upprepas. Read/write float.

**Returnerar:**
float
### setRepeatCount(float value) {#setRepeatCount-float-}
```
public final void setRepeatCount(float value)
```


Beskriver hur många gånger effekten ska upprepas. Read/write float.

**Parametrar:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public final boolean getRepeatUntilEndSlide()
```


Detta attribut anger om effekten ska upprepas tills sliden är slut. Read/write boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Hämta effektsekvensen för den första sliden
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Hämta den första effekten i huvudsekvensen.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ändra effekten Timing/Repeat till "Till slutet av sliden"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Returnerar:**
boolean
### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public final void setRepeatUntilEndSlide(boolean value)
```


Detta attribut anger om effekten ska upprepas tills sliden är slut. Read/write boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Hämta effektsekvensen för den första sliden
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Hämta den första effekten i huvudsekvensen.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ändra effekten Timing/Repeat till "Till slutet av sliden"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parametrar:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public final boolean getRepeatUntilNextClick()
```


Detta attribut anger om effekten ska upprepas tills nästa klick. Read/write boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Hämta effektsekvensen för den första sliden
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Hämta den första effekten i huvudsekvensen.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ändra effekten Timing/Repeat till "Till nästa klick"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Returnerar:**
boolean
### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public final void setRepeatUntilNextClick(boolean value)
```


Detta attribut anger om effekten ska upprepas tills nästa klick. Read/write boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Hämta effektsekvensen för den första sliden
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Hämta den första effekten i huvudsekvensen.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ändra effekten Timing/Repeat till "Till nästa klick"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parametrar:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public final float getRepeatDuration()
```


Beskriver hur många gånger effekten ska upprepas. Read/write float.

**Returnerar:**
float
### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public final void setRepeatDuration(float value)
```


Beskriver hur många gånger effekten ska upprepas. Read/write float.

**Parametrar:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public final int getRestart()
```


Anger om en effekt ska startas om efter avslut. Read/write [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Returnerar:**
int
### setRestart(int value) {#setRestart-int-}
```
public final void setRestart(int value)
```


Anger om en effekt ska startas om efter avslut. Read/write [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Parametrar:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public final boolean getRewind()
```


Detta attribut anger om effekten ska spolas tillbaka när den är klar. Read/write boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Hämta effektsekvensen för den första sliden
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Hämta den första effekten i huvudsekvensen.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Aktivera effekten Timing/Rewind.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Returnerar:**
boolean
### setRewind(boolean value) {#setRewind-boolean-}
```
public final void setRewind(boolean value)
```


Detta attribut anger om effekten ska spolas tillbaka när den är klar. Read/write boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Hämta effektsekvensen för den första sliden
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Hämta den första effekten i huvudsekvensen.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Aktivera effekten Timing/Rewind.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parametrar:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getSpeed() {#getSpeed--}
```
public final float getSpeed()
```


Anger den procentandel med vilken timingen ska accelereras (eller decelereras). Read/write float.

**Returnerar:**
float
### setSpeed(float value) {#setSpeed-float-}
```
public final void setSpeed(float value)
```


Anger den procentandel med vilken timingen ska accelereras (eller decelereras). Read/write float.

**Parametrar:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public final float getTriggerDelayTime()
```


Beskriver fördröjningstid efter utlösning. Read/write float.

**Returnerar:**
float
### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public final void setTriggerDelayTime(float value)
```


Beskriver fördröjningstid efter utlösning. Read/write float.

**Parametrar:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public final int getTriggerType()
```


Beskriver utlösningstyp. Read/write [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Returnerar:**
int
### setTriggerType(int value) {#setTriggerType-int-}
```
public final void setTriggerType(int value)
```


Beskriver utlösningstyp. Read/write [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Parametrar:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | int |  |

### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```


Returnerar Parent_Immediate-objektet. Read-only IDOMObject.

**Returnerar:**
com.aspose.slides.IDOMObject