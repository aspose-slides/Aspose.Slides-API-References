---
title: INormalViewRestoredProperties
second_title: Aspose.Slides for Android via Java API Reference
description: Anger storleken på bildområdets ((bredd när ett barn till restoredTop, höjd när ett barn till restoredLeft) av normalläget, när området har en variabel återställd storlek (varken minimerad eller maximerad).
type: docs
url: /sv/com.aspose.slides/inormalviewrestoredproperties/
---```
public interface INormalViewRestoredProperties
```

Anger storleken på bildområdets ((bredd när ett barn till restoredTop, höjd när ett barn till restoredLeft) av normalläget, när området har en variabel återställd storlek (varken minimerad eller maximerad).
## Metoder

| Metod | Beskrivning |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | Anger storleken på bildområdet (bredd när ett barn till RestoredTop, höjd när ett barn till RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | Anger storleken på bildområdet (bredd när ett barn till RestoredTop, höjd när ett barn till RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | Anger om storleken på sidoinnehållsområdet ska kompensera för den nya storleken när fönstret som innehåller vyn i applikationen ändras storlek Läs/skriv boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | Anger om storleken på sidoinnehållsområdet ska kompensera för den nya storleken när fönstret som innehåller vyn i applikationen ändras storlek Läs/skriv boolean. |
### getDimensionSize() {#getDimensionSize--}
```
public abstract float getDimensionSize()
```

Anger storleken på bildområdet (bredd när ett barn till RestoredTop, höjd när ett barn till RestoredLeft). Läs/skriv float.

**Returnerar:**
float
### setDimensionSize(float value) {#setDimensionSize-float-}
```
public abstract void setDimensionSize(float value)
```

Anger storleken på bildområdet (bredd när ett barn till RestoredTop, höjd när ett barn till RestoredLeft). Läs/skriv float.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public abstract boolean getAutoAdjust()
```

Anger om storleken på sidoinnehållsområdet ska kompensera för den nya storleken när fönstret som innehåller vyn i applikationen ändras storlek Läs/skriv boolean.

**Returnerar:**
boolean
### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public abstract void setAutoAdjust(boolean value)
```

Anger om storleken på sidoinnehållsområdet ska kompensera för den nya storleken när fönstret som innehåller vyn i applikationen ändras storlek Läs/skriv boolean.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | boolean |  |