---
title: ITiming
second_title: Aspose.Slides for Android via Java API Reference
description: Represents animation timing.
type: docs
url: /sv/com.aspose.slides/itiming/
---```
public interface ITiming
```

Representerar animationens timing.
## Metoder

| Metod | Beskrivning |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | Beskriver procentandelen av varaktigheten för accelerationsbeteendeeffekten. |
| [setAccelerate(float value)](#setAccelerate-float-) | Beskriver procentandelen av varaktigheten för accelerationsbeteendeeffekten. |
| [getDecelerate()](#getDecelerate--) | Beskriver procentandelen av varaktigheten för deaccelerationsbeteendeeffekten. |
| [setDecelerate(float value)](#setDecelerate-float-) | Beskriver procentandelen av varaktigheten för deaccelerationsbeteendeeffekten. |
| [getAutoReverse()](#getAutoReverse--) | Beskriver om animationen automatiskt ska spelas upp i omvänd riktning efter att ha spelats i framåtriktning. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | Beskriver om animationen automatiskt ska spelas upp i omvänd riktning efter att ha spelats i framåtriktning. |
| [getDuration()](#getDuration--) | Beskriver varaktigheten av animationseffekten. |
| [setDuration(float value)](#setDuration-float-) | Beskriver varaktigheten av animationseffekten. |
| [getRepeatCount()](#getRepeatCount--) | Beskriver hur många gånger effekten bör upprepas. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | Beskriver hur många gånger effekten bör upprepas. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | Detta attribut anger om effekten ska upprepas tills slutet av bilden. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | Detta attribut anger om effekten ska upprepas tills slutet av bilden. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | Detta attribut anger om effekten ska upprepas tills nästa klick. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | Detta attribut anger om effekten ska upprepas tills nästa klick. |
| [getRepeatDuration()](#getRepeatDuration--) | Beskriver hur många gånger effekten bör upprepas. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | Beskriver hur många gånger effekten bör upprepas. |
| [getRestart()](#getRestart--) | Anger om en effekt ska startas om efter slutförande. |
| [setRestart(int value)](#setRestart-int-) | Anger om en effekt ska startas om efter slutförande. |
| [getSpeed()](#getSpeed--) | Anger den procentandel med vilken timingen ska accelereras (eller bromsas). |
| [setSpeed(float value)](#setSpeed-float-) | Anger den procentandel med vilken timingen ska accelereras (eller bromsas). |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | Beskriver fördröjningstid efter utlösare. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | Beskriver fördröjningstid efter utlösare. |
| [getTriggerType()](#getTriggerType--) | Beskriver utlösartyp. |
| [setTriggerType(int value)](#setTriggerType-int-) | Beskriver utlösartyp. |
| [getRewind()](#getRewind--) | Detta attribut anger om effekten ska spolas tillbaka när den har spelats färdigt. |
| [setRewind(boolean value)](#setRewind-boolean-) | Detta attribut anger om effekten ska spolas tillbaka när den har spelats färdigt. |
### getAccelerate() {#getAccelerate--}
```
public abstract float getAccelerate()
```

Beskriver procentandelen av varaktigheten för accelerationsbeteendeeffekten. Läs/skriv float.

**Returnerar:**
float
### setAccelerate(float value) {#setAccelerate-float-}
```
public abstract void setAccelerate(float value)
```

Beskriver procentandelen av varaktigheten för accelerationsbeteendeeffekten. Läs/skriv float.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | float |  |
### getDecelerate() {#getDecelerate--}
```
public abstract float getDecelerate()
```

Beskriver procentandelen av varaktigheten för deaccelerationsbeteendeeffekten. Läs/skriv float.

**Returnerar:**
float
### setDecelerate(float value) {#setDecelerate-float-}
```
public abstract void setDecelerate(float value)
```

Beskriver procentandelen av varaktigheten för deaccelerationsbeteendeeffekten. Läs/skriv float.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | float |  |
### getAutoReverse() {#getAutoReverse--}
```
public abstract boolean getAutoReverse()
```

Beskriver om animationen automatiskt ska spelas upp i omvänd riktning efter att ha spelats i framåtriktning. Läs/skriv boolean.

**Returnerar:**
boolean
### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public abstract void setAutoReverse(boolean value)
```

Beskriver om animationen automatiskt ska spelas upp i omvänd riktning efter att ha spelats i framåtriktning. Läs/skriv boolean.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | boolean |  |
### getDuration() {#getDuration--}
```
public abstract float getDuration()
```

Beskriver varaktigheten av animationseffekten. Läs/skriv float.

**Returnerar:**
float
### setDuration(float value) {#setDuration-float-}
```
public abstract void setDuration(float value)
```

Beskriver varaktigheten av animationseffekten. Läs/skriv float.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | float |  |
### getRepeatCount() {#getRepeatCount--}
```
public abstract float getRepeatCount()
```

Beskriver hur många gånger effekten bör upprepas. Läs/skriv float.

**Returnerar:**
float
### setRepeatCount(float value) {#setRepeatCount-float-}
```
public abstract void setRepeatCount(float value)
```

Beskriver hur många gånger effekten bör upprepas. Läs/skriv float.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | float |  |
### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public abstract boolean getRepeatUntilEndSlide()
```

Detta attribut anger om effekten ska upprepas tills slutet av bilden. Läs/skriv boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Hämta effektsekvensen för den första bilden
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Hämta den första effekten i huvudsekvensen.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ändra effektens Timing/Repeat till "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Returnerar:**
boolean
### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public abstract void setRepeatUntilEndSlide(boolean value)
```

Detta attribut anger om effekten ska upprepas tills slutet av bilden. Läs/skriv boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Hämta effektsekvensen för den första bilden
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Hämta den första effekten i huvudsekvensen.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ändra effektens Timing/Repeat till "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | boolean |  |
### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public abstract boolean getRepeatUntilNextClick()
```

Detta attribut anger om effekten ska upprepas tills nästa klick. Läs/skriv boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Hämta effektsekvensen för den första bilden
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Hämta den första effekten i huvudsekvensen.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ändra effektens Timing/Repeat till "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Returnerar:**
boolean
### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public abstract void setRepeatUntilNextClick(boolean value)
```

Detta attribut anger om effekten ska upprepas tills nästa klick. Läs/skriv boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Hämta effektsekvensen för den första bilden
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Hämta den första effekten i huvudsekvensen.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ändra effektens Timing/Repeat till "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | boolean |  |
### getRepeatDuration() {#getRepeatDuration--}
```
public abstract float getRepeatDuration()
```

Beskriver hur många gånger effekten bör upprepas. Läs/skriv float.

**Returnerar:**
float
### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public abstract void setRepeatDuration(float value)
```

Beskriver hur många gånger effekten bör upprepas. Läs/skriv float.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | float |  |
### getRestart() {#getRestart--}
```
public abstract int getRestart()
```

Anger om en effekt ska startas om efter slutförande. Läs/skriv [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Returnerar:**
int
### setRestart(int value) {#setRestart-int-}
```
public abstract void setRestart(int value)
```

Anger om en effekt ska startas om efter slutförande. Läs/skriv [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | int |  |
### getSpeed() {#getSpeed--}
```
public abstract float getSpeed()
```

Anger den procentandel med vilken timingen ska accelereras (eller bromsas). Läs/skriv float.

**Returnerar:**
float
### setSpeed(float value) {#setSpeed-float-}
```
public abstract void setSpeed(float value)
```

Anger den procentandel med vilken timingen ska accelereras (eller bromsas). Läs/skriv float.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | float |  |
### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public abstract float getTriggerDelayTime()
```

Beskriver fördröjningstid efter utlösare. Läs/skriv float.

**Returnerar:**
float
### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public abstract void setTriggerDelayTime(float value)
```

Beskriver fördröjningstid efter utlösare. Läs/skriv float.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | float |  |
### getTriggerType() {#getTriggerType--}
```
public abstract int getTriggerType()
```

Beskriver utlösartyp. Läs/skriv [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Returnerar:**
int
### setTriggerType(int value) {#setTriggerType-int-}
```
public abstract void setTriggerType(int value)
```

Beskriver utlösartyp. Läs/skriv [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | int |  |
### getRewind() {#getRewind--}
```
public abstract boolean getRewind()
```

Detta attribut anger om effekten ska spolas tillbaka när den har spelats färdigt. Läs/skriv boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Hämta effektsekvensen för den första bilden
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Hämta den första effekten i huvudsekvensen.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Aktivera effektens Timing/Rewind.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Returnerar:**
boolean
### setRewind(boolean value) {#setRewind-boolean-}
```
public abstract void setRewind(boolean value)
```

Detta attribut anger om effekten ska spolas tillbaka när den har spelats färdigt. Läs/skriv boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Hämta effektsekvensen för den första bilden
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Hämta den första effekten i huvudsekvensen.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Aktivera effektens Timing/Rewind.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | boolean |  |