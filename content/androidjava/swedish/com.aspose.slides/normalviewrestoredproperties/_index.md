---
title: NormalViewRestoredProperties
second_title: Aspose.Slides för Android via Java API-referens
description: Anger storleken på bildområdet (bredd när det är ett barn till restoredTop, höjd när det är ett barn till restoredLeft) i normalvyn, när området har en variabel återställd storlek (varken minimerad eller maximerad).
type: docs
url: /sv/com.aspose.slides/normalviewrestoredproperties/
---
**Arv:**
java.lang.Object

**Alla implementerade gränssnitt:**
[com.aspose.slides.INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)
```
public class NormalViewRestoredProperties implements INormalViewRestoredProperties
```

Anger storleken på bildområdet ((bredd när det är ett barn till restoredTop, höjd när det är ett barn till restoredLeft) i normalvyn, när området har en variabel återställd storlek (varken minimerad eller maximerad)).
## Metoder

| Metod | Beskrivning |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | Anger storleken på bildområdet (bredd när det är ett barn till RestoredTop, höjd när det är ett barn till RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | Anger storleken på bildområdet (bredd när det är ett barn till RestoredTop, höjd när det är ett barn till RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | Anger huruvida storleken på sidoinnehållsområdet ska kompensera för den nya storleken när fönstret som innehåller vyn i applikationen ändras storlek Läs/skriv boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | Anger huruvida storleken på sidoinnehållsområdet ska kompensera för den nya storleken när fönstret som innehåller vyn i applikationen ändras storlek Läs/skriv boolean. |
### getDimensionSize() {#getDimensionSize--}
```
public final float getDimensionSize()
```

Anger storleken på bildområdet (bredd när det är ett barn till RestoredTop, höjd när det är ett barn till RestoredLeft). Läs/skriv float.

**Returnerar:**
float
### setDimensionSize(float value) {#setDimensionSize-float-}
```
public final void setDimensionSize(float value)
```

Anger storleken på bildområdet (bredd när det är ett barn till RestoredTop, höjd när det är ett barn till RestoredLeft). Läs/skriv float.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public final boolean getAutoAdjust()
```

Anger huruvida storleken på sidoinnehållsområdet ska kompensera för den nya storleken när fönstret som innehåller vyn i applikationen ändras storlek Läs/skriv boolean.

**Returnerar:**
boolean
### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public final void setAutoAdjust(boolean value)
```

Anger huruvida storleken på sidoinnehållsområdet ska kompensera för den nya storleken när fönstret som innehåller vyn i applikationen ändras storlek Läs/skriv boolean.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | boolean |  |