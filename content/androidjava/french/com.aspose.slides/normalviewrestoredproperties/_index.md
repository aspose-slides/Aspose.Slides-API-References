---
title: NormalViewRestoredProperties
second_title: Référence de l'API Java Aspose.Slides pour Android
description: Spécifie la taille de la région de diapositive, largeur lorsqu’elle est un enfant de restoredTop, hauteur lorsqu’elle est un enfant de restoredLeft, de la vue normale lorsque la région a une taille restaurée variable (ni réduite ni agrandie).
type: docs
url: /fr/com.aspose.slides/normalviewrestoredproperties/
---
**Héritage :**
java.lang.Object

**Toutes les interfaces implémentées :**
[com.aspose.slides.INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)
```
public class NormalViewRestoredProperties implements INormalViewRestoredProperties
```

Spécifie la taille de la région de diapositive ((largeur lorsqu’elle est un enfant de restoredTop, hauteur lorsqu’elle est un enfant de restoredLeft) de la vue normale, lorsque la région est d’une taille restaurée variable (ni réduite ni agrandie)).

## Méthodes

| Méthode | Description |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | Spécifie la taille de la région de diapositive (largeur lorsqu’elle est un enfant de RestoredTop, hauteur lorsqu’elle est un enfant de RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | Spécifie la taille de la région de diapositive (largeur lorsqu’elle est un enfant de RestoredTop, hauteur lorsqu’elle est un enfant de RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | Spécifie si la taille de la région de contenu latéral doit compenser la nouvelle taille lors du redimensionnement de la fenêtre contenant la vue dans l’application Read/write boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | Spécifie si la taille de la région de contenu latéral doit compenser la nouvelle taille lors du redimensionnement de la fenêtre contenant la vue dans l’application Read/write boolean. |

### getDimensionSize() {#getDimensionSize--}
```
public final float getDimensionSize()
```

Spécifie la taille de la région de diapositive (largeur lorsqu’elle est un enfant de RestoredTop, hauteur lorsqu’elle est un enfant de RestoredLeft). Read/write float.

**Renvoie :**
float

### setDimensionSize(float value) {#setDimensionSize-float-}
```
public final void setDimensionSize(float value)
```

Spécifie la taille de la région de diapositive (largeur lorsqu’elle est un enfant de RestoredTop, hauteur lorsqu’elle est un enfant de RestoredLeft). Read/write float.

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public final boolean getAutoAdjust()
```

Spécifie si la taille de la région de contenu latéral doit compenser la nouvelle taille lors du redimensionnement de la fenêtre contenant la vue dans l’application Read/write boolean.

**Renvoie :**
boolean

### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public final void setAutoAdjust(boolean value)
```

Spécifie si la taille de la région de contenu latéral doit compenser la nouvelle taille lors du redimensionnement de la fenêtre contenant la vue dans l’application Read/write boolean.

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | boolean |  |