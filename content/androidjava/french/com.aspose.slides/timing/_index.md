---
title: Timing
second_title: Aspose.Slides pour Android via la référence API Java
description: Représente le timing d'animation.
type: docs
url: /fr/com.aspose.slides/timing/
---
**Inheritance:** java.lang.Object

**All Implemented Interfaces:** [com.aspose.slides.ITiming](../../com.aspose.slides/itiming), com.aspose.slides.IDOMObject
```
public class Timing implements ITiming, IDOMObject
```

Représente le timing d'animation.
## Méthodes

| Méthode | Description |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | Décrit le pourcentage de la durée de l'effet d'accélération du comportement. |
| [setAccelerate(float value)](#setAccelerate-float-) | Décrit le pourcentage de la durée de l'effet d'accélération du comportement. |
| [getDecelerate()](#getDecelerate--) | Décrit le pourcentage de la durée de l'effet de décélération du comportement. |
| [setDecelerate(float value)](#setDecelerate-float-) | Décrit le pourcentage de la durée de l'effet de décélération du comportement. |
| [getAutoReverse()](#getAutoReverse--) | Décrit si l'animation doit être jouée automatiquement en sens inverse après l'avoir jouée dans le sens avant. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | Décrit si l'animation doit être jouée automatiquement en sens inverse après l'avoir jouée dans le sens avant. |
| [getDuration()](#getDuration--) | Décrit la durée de l'effet d'animation. |
| [setDuration(float value)](#setDuration-float-) | Décrit la durée de l'effet d'animation. |
| [getRepeatCount()](#getRepeatCount--) | Décrit le nombre de fois que l'effet doit se répéter. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | Décrit le nombre de fois que l'effet doit se répéter. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | Cet attribut spécifie si l'effet se répétera jusqu'à la fin de la diapositive. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | Cet attribut spécifie si l'effet se répétera jusqu'à la fin de la diapositive. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | Cet attribut spécifie si l'effet se répétera jusqu'au clic suivant. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | Cet attribut spécifie si l'effet se répétera jusqu'au clic suivant. |
| [getRepeatDuration()](#getRepeatDuration--) | Décrit le nombre de fois que l'effet doit se répéter. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | Décrit le nombre de fois que l'effet doit se répéter. |
| [getRestart()](#getRestart--) | Spécifie si un effet doit redémarrer après son achèvement. |
| [setRestart(int value)](#setRestart-int-) | Spécifie si un effet doit redémarrer après son achèvement. |
| [getRewind()](#getRewind--) | Cet attribut spécifie si l'effet reviendra en arrière une fois la lecture terminée. |
| [setRewind(boolean value)](#setRewind-boolean-) | Cet attribut spécifie si l'effet reviendra en arrière une fois la lecture terminée. |
| [getSpeed()](#getSpeed--) | Spécifie le pourcentage d'accélération (ou de ralentissement) du timing. |
| [setSpeed(float value)](#setSpeed-float-) | Spécifie le pourcentage d'accélération (ou de ralentissement) du timing. |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | Décrit le temps de retard après le déclencheur. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | Décrit le temps de retard après le déclencheur. |
| [getTriggerType()](#getTriggerType--) | Décrit le type de déclencheur. |
| [setTriggerType(int value)](#setTriggerType-int-) | Décrit le type de déclencheur. |
| [getParent_Immediate()](#getParent-Immediate--) |  |

### getAccelerate() {#getAccelerate--}
```
public final float getAccelerate()
```

Décrit le pourcentage de la durée de l'effet d'accélération du comportement. Lecture/écriture float.

**Returns:**
float

### setAccelerate(float value) {#setAccelerate-float-}
```
public final void setAccelerate(float value)
```

Décrit le pourcentage de la durée de l'effet d'accélération du comportement. Lecture/écriture float.

**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public final float getDecelerate()
```

Décrit le pourcentage de la durée de l'effet de décélération du comportement. Lecture/écriture float.

**Returns:**
float

### setDecelerate(float value) {#setDecelerate-float-}
```
public final void setDecelerate(float value)
```

Décrit le pourcentage de la durée de l'effet de décélération du comportement. Lecture/écriture float.

**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public final boolean getAutoReverse()
```

Décrit si l'animation doit être jouée automatiquement en sens inverse après l'avoir jouée dans le sens avant. Lecture/écriture boolean.

**Returns:**
boolean

### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public final void setAutoReverse(boolean value)
```

Décrit si l'animation doit être jouée automatiquement en sens inverse après l'avoir jouée dans le sens avant. Lecture/écriture boolean.

**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public final float getDuration()
```

Décrit la durée de l'effet d'animation. Lecture/écriture float.

**Returns:**
float

### setDuration(float value) {#setDuration-float-}
```
public final void setDuration(float value)
```

Décrit la durée de l'effet d'animation. Lecture/écriture float.

**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public final float getRepeatCount()
```

Décrit le nombre de fois que l'effet doit se répéter. Lecture/écriture float.

**Returns:**
float

### setRepeatCount(float value) {#setRepeatCount-float-}
```
public final void setRepeatCount(float value)
```

Décrit le nombre de fois que l'effet doit se répéter. Lecture/écriture float.

**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public final boolean getRepeatUntilEndSlide()
```

Cet attribut spécifie si l'effet se répétera jusqu'à la fin de la diapositive. Lecture/écriture boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtenir la séquence d'effets pour la première diapositive
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtenir le premier effet de la séquence principale.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Modifier le Timing/Repeat de l'effet à "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
>```
```
public final void setRepeatUntilEndSlide(boolean value)
```

This attribute specifies if the effect will repeat until the end of the slide. Read/write boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtenir la séquence d'effets pour la première diapositive
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtenir le premier effet de la séquence principale.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Modifier le Timing/Repeat de l'effet à "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public final boolean getRepeatUntilNextClick()
```

This attribute specifies if the effect will repeat until the next click. Read/write boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtenir la séquence d'effets pour la première diapositive
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtenir le premier effet de la séquence principale.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Modifier le Timing/Repeat de l'effet à "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Returns:**
boolean
### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public final void setRepeatUntilNextClick(boolean value)
```
This attribute specifies if the effect will repeat until the next click. Read/write boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtenir la séquence d'effets pour la première diapositive
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtenir le premier effet de la séquence principale.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Modifier le Timing/Repeat de l'effet à "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public final float getRepeatDuration()
```

Describes the number of times the effect should repeat. Read/write float.

**Returns:**
float
### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public final void setRepeatDuration(float value)
```

Describes the number of times the effect should repeat. Read/write float.

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public final int getRestart()
```

Specifies if a effect is to restart after complete. Read/write [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Returns:**
int
### setRestart(int value) {#setRestart-int-}
```
public final void setRestart(int value)
```

Specifies if a effect is to restart after complete. Read/write [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public final boolean getRewind()
```
This attribute specifies if the effect will rewind when done playing. Read/write boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtenir la séquence d'effets pour la première diapositive
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtenir le premier effet de la séquence principale.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Activer le Timing/Rewind de l'effet.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```
**Returns:**
boolean
### setRewind(boolean value) {#setRewind-boolean-}
```
public final void setRewind(boolean value)
```

This attribute specifies if the effect will rewind when done playing. Read/write boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtenir la séquence d'effets pour la première diapositive
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtenir le premier effet de la séquence principale.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Activer le Timing/Rewind de l'effet.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```
**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getSpeed() {#getSpeed--}
```
public final float getSpeed()
```

Specifies the percentage by which to speed up (or slow down) the timing. Read/write float.

**Returns:**
float
### setSpeed(float value) {#setSpeed-float-}
```
public final void setSpeed(float value)
```

Specifies the percentage by which to speed up (or slow down) the timing. Read/write float.

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public final float getTriggerDelayTime()
```

Describes delay time after trigger. Read/write float.

**Returns:**
float
### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public final void setTriggerDelayTime(float value)
```

Describes delay time after trigger. Read/write float.

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public final int getTriggerType()
```

Describes trigger type. Read/write [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Returns:**
int
### setTriggerType(int value) {#setTriggerType-int-}
```
public final void setTriggerType(int value)
```

Décrit le type de déclencheur. Lecture/écriture [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | int |  |

### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()


Renvoie Parent_Immediate objet. Lecture seule IDOMObject.

**Returns:**
com.aspose.slides.IDOMObject