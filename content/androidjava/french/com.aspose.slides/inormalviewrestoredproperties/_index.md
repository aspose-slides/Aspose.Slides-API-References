---
title: INormalViewRestoredProperties
second_title: Aspose.Slides for Android via Java API Reference
description: Spécifie la taille de la région de diapositive (largeur lorsqu'elle est enfant de RestoredTop, hauteur lorsqu'elle est enfant de RestoredLeft) de la vue normale, lorsque la région a une taille restaurée variable (ni minimisée ni maximisée).
type: docs
url: /fr/com.aspose.slides/inormalviewrestoredproperties/
---```
public interface INormalViewRestoredProperties
```

Spécifie la taille de la région de diapositive ((largeur lorsqu'elle est enfant de RestoredTop, hauteur lorsqu'elle est enfant de RestoredLeft) de la vue normale, lorsque la région a une taille restaurée variable (ni minimisée ni maximisée).

## Méthodes

| Méthode | Description |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | Spécifie la taille de la région de diapositive (largeur lorsqu'elle est enfant de RestoredTop, hauteur lorsqu'elle est enfant de RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | Spécifie la taille de la région de diapositive (largeur lorsqu'elle est enfant de RestoredTop, hauteur lorsqu'elle est enfant de RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | Spécifie si la taille de la région de contenu latéral doit compenser la nouvelle taille lors du redimensionnement de la fenêtre contenant la vue dans l'application Lecture/Écriture booléen. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | Spécifie si la taille de la région de contenu latéral doit compenser la nouvelle taille lors du redimensionnement de la fenêtre contenant la vue dans l'application Lecture/Écriture booléen. |

### getDimensionSize() {#getDimensionSize--}
```
public abstract float getDimensionSize()
```

Spécifie la taille de la région de diapositive (largeur lorsqu'elle est enfant de RestoredTop, hauteur lorsqu'elle est enfant de RestoredLeft). Lecture/Écriture flottant.

**Renvoie :**  
float

### setDimensionSize(float value) {#setDimensionSize-float-}
```
public abstract void setDimensionSize(float value)
```

Spécifie la taille de la région de diapositive (largeur lorsqu'elle est enfant de RestoredTop, hauteur lorsqu'elle est enfant de RestoredLeft). Lecture/Écriture flottant.

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public abstract boolean getAutoAdjust()
```

Spécifie si la taille de la région de contenu latéral doit compenser la nouvelle taille lors du redimensionnement de la fenêtre contenant la vue dans l'application Lecture/Écriture booléen.

**Renvoie :**  
boolean

### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public abstract void setAutoAdjust(boolean value)
```

Spécifie si la taille de la région de contenu latéral doit compenser la nouvelle taille lors du redimensionnement de la fenêtre contenant la vue dans l'application Lecture/Écriture booléen.

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | boolean |  |