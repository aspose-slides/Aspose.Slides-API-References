---
title: Marker
second_title: Référence de l'API Aspose.Slides pour Android via Java
description: Représente le marqueur d'un graphique.
type: docs
url: /fr/com.aspose.slides/marker/
---
**Héritage:**  
java.lang.Object

**Toutes les interfaces implémentées:**  
[com.aspose.slides.IMarker](../../com.aspose.slides/imarker), com.aspose.slides.IDOMObject  
```
public class Marker implements IMarker, IDOMObject
```

Représente le marqueur d’un graphique.

## Méthodes

| Méthode | Description |
| --- | --- |
| [getSymbol()](#getSymbol--) | Représente le style du marqueur dans un graphique linéaire, un graphique en nuage de points ou un graphique radar. |
| [setSymbol(int value)](#setSymbol-int-) | Représente le style du marqueur dans un graphique linéaire, un graphique en nuage de points ou un graphique radar. |
| [getFormat()](#getFormat--) | Obtient ou définit le remplissage du marqueur. |
| [getSize()](#getSize--) | Représente la taille du marqueur dans un graphique linéaire, un graphique en nuage de points ou un graphique radar. |
| [setSize(int value)](#setSize-int-) | Représente la taille du marqueur dans un graphique linéaire, un graphique en nuage de points ou un graphique radar. |
| [getParent_Immediate()](#getParent-Immediate--) |  |

### getSymbol() {#getSymbol--}
```
public final int getSymbol()
```

Représente le style du marqueur dans un graphique linéaire, un graphique en nuage de points ou un graphique radar. Lecture/écriture [MarkerStyleType](../../com.aspose.slides/markerstyletype).

**Renvoie:**  
int

### setSymbol(int value) {#setSymbol-int-}
```
public final void setSymbol(int value)
```

Représente le style du marqueur dans un graphique linéaire, un graphique en nuage de points ou un graphique radar. Lecture/écriture [MarkerStyleType](../../com.aspose.slides/markerstyletype).

**Paramètres:**  
| Paramètre | Type | Description |
| --- | --- | --- |
| value | int |  |

### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

Obtient ou définit le remplissage du marqueur. Lecture seule [IFormat](../../com.aspose.slides/iformat).

**Renvoie:**  
[IFormat](../../com.aspose.slides/iformat)

### getSize() {#getSize--}
```
public final int getSize()
```

Représente la taille du marqueur dans un graphique linéaire, un graphique en nuage de points ou un graphique radar. Lecture/écriture int.

**Renvoie:**  
int

### setSize(int value) {#setSize-int-}
```
public final void setSize(int value)
```

Représente la taille du marqueur dans un graphique linéaire, un graphique en nuage de points ou un graphique radar. Lecture/écriture int.

**Paramètres:**  
| Paramètre | Type | Description |
| --- | --- | --- |
| value | int |  |

### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

Renvoie l’objet Parent_Immediate. Lecture seule IDOMObject.

**Renvoie:**  
com.aspose.slides.IDOMObject