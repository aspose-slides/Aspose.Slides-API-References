---
title: AdjustValue
second_title: Référence API Java Aspose.Slides pour Android
description: Représente la valeur d'ajustement d'une forme géométrique.
type: docs
url: /fr/com.aspose.slides/adjustvalue/
---
**Héritage:**  
java.lang.Object

**Toutes les interfaces implémentées:**  
[com.aspose.slides.IAdjustValue](../../com.aspose.slides/iadjustvalue)
```
public class AdjustValue implements IAdjustValue
```

Représente la valeur d'ajustement d'une forme géométrique. Ces valeurs affectent la forme.

## Méthodes

| Méthode | Description |
| --- | --- |
| [getRawValue()](#getRawValue--) | Renvoie ou définit la valeur d'ajustement "telle quelle". |
| [setRawValue(long value)](#setRawValue-long-) | Renvoie ou définit la valeur d'ajustement "telle quelle". |
| [getAngleValue()](#getAngleValue--) | Renvoie ou définit la valeur, en l'interprétant comme un angle en degrés. |
| [setAngleValue(float value)](#setAngleValue-float-) | Renvoie ou définit la valeur, en l'interprétant comme un angle en degrés. |
| [getName()](#getName--) | Renvoie le nom de cette valeur d'ajustement. |
| [getType()](#getType--) | Renvoie le type de l'ajustement de la forme. |

### getRawValue() {#getRawValue--}
```
public final long getRawValue()
```

Renvoie ou définit la valeur d'ajustement "telle quelle". Lecture/écriture long.

**Renvoie :**  
long

### setRawValue(long value) {#setRawValue-long-}
```
public final void setRawValue(long value)
```

Renvoie ou définit la valeur d'ajustement "telle quelle". Lecture/écriture long.

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | long |  |

### getAngleValue() {#getAngleValue--}
```
public final float getAngleValue()
```

Renvoie ou définit la valeur, en l'interprétant comme un angle en degrés. Lecture/écriture float.

**Renvoie :**  
float

### setAngleValue(float value) {#setAngleValue-float-}
```
public final void setAngleValue(float value)
```

Renvoie ou définit la valeur, en l'interprétant comme un angle en degrés. Lecture/écriture float.

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | float |  |

### getName() {#getName--}
```
public final String getName()
```

Renvoie un nom de cette valeur d'ajustement. Lecture seule String.

**Renvoie :**  
java.lang.String

### getType() {#getType--}
```
public final int getType()
```

Renvoie le type de l'ajustement de la forme. Lecture seule [ShapeAdjustmentType](../../com.aspose.slides/shapeadjustmenttype).

**Renvoie :**  
int