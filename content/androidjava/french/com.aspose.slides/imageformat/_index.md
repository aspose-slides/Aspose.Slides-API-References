---
title: ImageFormat
second_title: Aspose.Slides pour Android via la référence de l'API Java
description: Représente le format de fichier de l'image.
type: docs
url: /fr/com.aspose.slides/imageformat/
---
**Héritage:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum  
```
public final class ImageFormat extends System.Enum
```

Représente le format de fichier de l'image.

## Champs

| Champ | Description |
| --- | --- |
| [MemoryBmp](#MemoryBmp) | Représente le format d'un bitmap en mémoire. |
| [Bmp](#Bmp) | Représente le format d'image bitmap (BMP). |
| [Emf](#Emf) | Représente le format d'image métafichier enrichi (EMF). |
| [Wmf](#Wmf) | Représente le format d'image métafichier Windows (WMF). |
| [Gif](#Gif) | Représente le format d'image Graphics Interchange Format (GIF). |
| [Jpeg](#Jpeg) | Représente le format d'image Joint Photographic Experts Group (JPEG). |
| [Png](#Png) | Représente le format d'image W3C Portable Network Graphics (PNG). |
| [Tiff](#Tiff) | Représente le format d'image Tagged Image File Format (TIFF). |
| [Exif](#Exif) | Représente le format Exchangeable Image File (Exif). |
| [Icon](#Icon) | Représente le format d'image icône Windows. |

### MemoryBmp {#MemoryBmp}
```
public static final int MemoryBmp
```

Représente le format d'un bitmap en mémoire.

### Bmp {#Bmp}
```
public static final int Bmp
```

Représente le format d'image bitmap (BMP).

### Emf {#Emf}
```
public static final int Emf
```

Représente le format d'image métafichier enrichi (EMF).

### Wmf {#Wmf}
```
public static final int Wmf
```

Représente le format d'image métafichier Windows (WMF).

### Gif {#Gif}
```
public static final int Gif
```

Représente le format d'image Graphics Interchange Format (GIF).

### Jpeg {#Jpeg}
```
public static final int Jpeg
```

Représente le format d'image Joint Photographic Experts Group (JPEG).

### Png {#Png}
```
public static final int Png
```

Représente le format d'image W3C Portable Network Graphics (PNG).

### Tiff {#Tiff}
```
public static final int Tiff
```

Représente le format d'image Tagged Image File Format (TIFF).

### Exif {#Exif}
```
public static final int Exif
```

Représente le format Exchangeable Image File (Exif).

### Icon {#Icon}
```
public static final int Icon
```

Représente le format d'image icône Windows.