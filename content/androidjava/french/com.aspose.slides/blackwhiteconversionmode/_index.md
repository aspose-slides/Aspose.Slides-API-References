---
title: BlackWhiteConversionMode
second_title: Aspose.Slides pour Android via la référence de l'API Java
description: Fournit des options qui contrôlent la façon dont les images des diapositives seront converties en images bitonales.
type: docs
url: /fr/com.aspose.slides/blackwhiteconversionmode/
---
**Héritage :**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class BlackWhiteConversionMode extends System.Enum
```

Fournit des options qui contrôlent la façon dont les images des diapositives seront converties en images binaires.
## Champs

| Champ | Description |
| --- | --- |
| [Default](#Default) | Spécifie aucun algorithme de conversion. |
| [Dithering](#Dithering) | Spécifie l'algorithme de tramage (Floyd-Steinberg). |
| [DitheringFloydSteinberg](#DitheringFloydSteinberg) | Spécifie l'algorithme de tramage Floyd-Steinberg. |
| [Auto](#Auto) | Spécifie l'algorithme de seuillage calculé automatiquement (Otsu). |
| [AutoOtsu](#AutoOtsu) | Spécifie l'algorithme de seuillage Otsu calculé automatiquement. |
| [Threshold25](#Threshold25) | Spécifie l'algorithme de seuillage statique (25%). |
| [Threshold50](#Threshold50) | Spécifie l'algorithme de seuillage statique (50%). |
| [Threshold75](#Threshold75) | Spécifie l'algorithme de seuillage statique (75%). |
### Par défaut {#Default}
```
public static final int Default
```

Spécifie aucun algorithme de conversion. L'algorithme implémenté dans le codec TIFF sera utilisé. (Par défaut)

### Tramage {#Dithering}
```
public static final int Dithering
```

Spécifie l'algorithme de tramage (Floyd-Steinberg).

### DitheringFloydSteinberg {#DitheringFloydSteinberg}
```
public static final int DitheringFloydSteinberg
```

Spécifie l'algorithme de tramage Floyd-Steinberg.

### Auto {#Auto}
```
public static final int Auto
```

Spécifie l'algorithme de seuillage calculé automatiquement (Otsu).

### AutoOtsu {#AutoOtsu}
```
public static final int AutoOtsu
```

Spécifie l'algorithme de seuillage Otsu calculé automatiquement.

### Threshold25 {#Threshold25}
```
public static final int Threshold25
```

Spécifie l'algorithme de seuillage statique (25%).

### Threshold50 {#Threshold50}
```
public static final int Threshold50
```

Spécifie l'algorithme de seuillage statique (50%).

### Threshold75 {#Threshold75}
```
public static final int Threshold75
```

Spécifie l'algorithme de seuillage statique (75%).