---
title: IScaleEffect
second_title: Aspose.Slides pour Android via la référence d'API Java
description: Représente l'effet d'échelle d'animation.
type: docs
url: /fr/com.aspose.slides/iscaleeffect/
---
**Toutes les interfaces implémentées :**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IScaleEffect extends IBehavior
```

Représente l'effet d'échelle d'animation.
## Méthodes

| Méthode | Description |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Détermine si un contenu doit être zoomé. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Détermine si un contenu doit être zoomé. |
| [getFrom()](#getFrom--) | Spécifie une coordonnée x/y à partir de laquelle démarrer l'animation (en pourcentage). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | Spécifie une coordonnée x/y à partir de laquelle démarrer l'animation (en pourcentage). |
| [getTo()](#getTo--) | Spécifie l'emplacement cible pour un effet d'échelle d'animation (en pourcentage). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | Spécifie l'emplacement cible pour un effet d'échelle d'animation (en pourcentage). |
| [getBy()](#getBy--) | Décrit la valeur de décalage relatif pour l'animation (en pourcentage). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | Décrit la valeur de décalage relatif pour l'animation (en pourcentage). |

### getZoomContent() {#getZoomContent--}
```
public abstract byte getZoomContent()
```

Détermine si un contenu doit être zoomé. Lecture/écriture [NullableBool](../../com.aspose.slides/nullablebool).

**Renvoie :**  
byte

### setZoomContent(byte value) {#setZoomContent-byte-}
```
public abstract void setZoomContent(byte value)
```

Détermine si un contenu doit être zoomé. Lecture/écriture [NullableBool](../../com.aspose.slides/nullablebool).

**Paramètres :**  
| Paramètre | Type | Description |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public abstract PointF getFrom()
```

Spécifie une coordonnée x/y à partir de laquelle démarrer l'animation (en pourcentage). Lecture/écriture android.graphics.PointF.

**Renvoie :**  
android.graphics.PointF

### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public abstract void setFrom(PointF value)
```

Spécifie une coordonnée x/y à partir de laquelle démarrer l'animation (en pourcentage). Lecture/écriture android.graphics.PointF.

**Paramètres :**  
| Paramètre | Type | Description |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public abstract PointF getTo()
```

Spécifie l'emplacement cible pour un effet d'échelle d'animation (en pourcentage). Lecture/écriture android.graphics.PointF.

**Renvoie :**  
android.graphics.PointF

### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public abstract void setTo(PointF value)
```

Spécifie l'emplacement cible pour un effet d'échelle d'animation (en pourcentage). Lecture/écriture android.graphics.PointF.

**Paramètres :**  
| Paramètre | Type | Description |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public abstract PointF getBy()
```

Décrit la valeur de décalage relatif pour l'animation (en pourcentage). Lecture/écriture android.graphics.PointF.

**Renvoie :**  
android.graphics.PointF

### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public abstract void setBy(PointF value)
```

Décrit la valeur de décalage relatif pour l'animation (en pourcentage). Lecture/écriture android.graphics.PointF.

**Paramètres :**  
| Paramètre | Type | Description |
| --- | --- | --- |
| value | android.graphics.PointF |  |