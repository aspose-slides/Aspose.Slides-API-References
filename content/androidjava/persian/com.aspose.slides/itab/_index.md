---
title: ITab
second_title: مرجع API جاوا Aspose.Slides برای اندروید
description: نمایانگر یک تب برای متن است.
type: docs
url: /fa/com.aspose.slides/itab/
---
**همه اینترفیس‌های پیاده‌سازی شده:**
java.lang.Comparable
```
public interface ITab extends Comparable
```

نمایانگر تب برای متن.

## متدها

| متد | توضیح |
| --- | --- |
| [getPosition()](#getPosition--) | Returns or sets position of a tab. |
| [setPosition(double value)](#setPosition-double-) | Returns or sets position of a tab. |
| [getAlignment()](#getAlignment--) | Returns or sets align style of a tab. |
| [setAlignment(int value)](#setAlignment-int-) | Returns or sets align style of a tab. |
### getPosition() {#getPosition--}
```
public abstract double getPosition()
```

مقدار یا موقعیت یک تب را برمی‌گرداند یا تنظیم می‌کند. اختصاص این ویژگی می‌تواند شاخص تب را در مجموعه تغییر دهد و Enumerator را نامعتبر کند. خواندنی/قابل نوشتن double.

**بازگشت:**
double
### setPosition(double value) {#setPosition-double-}
```
public abstract void setPosition(double value)
```

مقدار یا موقعیت یک تب را برمی‌گرداند یا تنظیم می‌کند. اختصاص این ویژگی می‌تواند شاخص تب را در مجموعه تغییر دهد و Enumerator را نامعتبر کند. خواندنی/قابل نوشتن double.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | double |  |

### getAlignment() {#getAlignment--}
```
public abstract int getAlignment()
```

سبک تراز یک تب را برمی‌گرداند یا تنظیم می‌کند. خواندنی/قابل نوشتن [TabAlignment](../../com.aspose.slides/tabalignment).

**بازگشت:**
int
### setAlignment(int value) {#setAlignment-int-}
```
public abstract void setAlignment(int value)
```

سبک تراز یک تب را برمی‌گرداند یا تنظیم می‌کند. خواندنی/قابل نوشتن [TabAlignment](../../com.aspose.slides/tabalignment).

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | int |  |