---
title: NotesPositions
second_title: مرجع API جاوا برای Aspose.Slides برای اندروید
description: قانونی را که برای رندر کردن یادداشت‌ها در سند صادر شده استفاده می‌شود، نمایندگی می‌کند
type: docs
url: /fa/com.aspose.slides/notespositions/
---
**ارث‌بری:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class NotesPositions extends System.Enum
```

قانونی را که برای رندر کردن یادداشت‌ها در سند صادر شده به کار می‌رود، نمایندگی می‌کند
## فیلدها

| فیلد | توضیح |
| --- | --- |
| [None](#None) | مشخص می‌کند که یادداشت‌ها به‌طور کامل نمایش داده نمی‌شوند. |
| [BottomFull](#BottomFull) | مشخص می‌کند که یادداشت‌ها به‌صورت کامل با استفاده از صفحات اضافه در صورت نیاز نمایش داده می‌شوند. |
| [BottomTruncated](#BottomTruncated) | مشخص می‌کند که یادداشت‌ها تنها در یک صفحه نمایش داده می‌شوند. |
### None {#None}
```
public static final int None
```

مشخص می‌کند که یادداشت‌ها به‌طور کامل نمایش داده نمی‌شوند.

### BottomFull {#BottomFull}
```
public static final int BottomFull
```

مشخص می‌کند که یادداشت‌ها به‌صورت کامل با استفاده از صفحات اضافه در صورت نیاز نمایش داده می‌شوند.

### BottomTruncated {#BottomTruncated}
```
public static final int BottomTruncated
```

مشخص می‌کند که یادداشت‌ها تنها در یک صفحه نمایش داده می‌شوند.