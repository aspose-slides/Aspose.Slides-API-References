---
title: ImageTransformOperationFactory
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: امکان ایجاد عملیات تبدیل تصویر را فراهم می‌کند
type: docs
url: /fa/com.aspose.slides/imagetransformoperationfactory/
---
**ارث‌بری:**
java.lang.Object

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IImageTransformOperationFactory](../../com.aspose.slides/iimagetransformoperationfactory)
```
public class ImageTransformOperationFactory implements IImageTransformOperationFactory
```

اجازۀ ایجاد عملیات تبدیل تصویر

--------------------

برای سازگاری با COM.
## سازنده‌ها

| سازنده | توضیح |
| --- | --- |
| [ImageTransformOperationFactory()](#ImageTransformOperationFactory--) |  |
## متدها

| متد | توضیح |
| --- | --- |
| [createAlphaBiLevel(float threshold)](#createAlphaBiLevel-float-) | یک اثر Alpha BiLevel ایجاد می‌کند. |
| [createAlphCeiling()](#createAlphCeiling--) | یک اثر Alpha Ceiling ایجاد می‌کند. |
| [createAlphaFloor()](#createAlphaFloor--) | یک اثر Alpha floor ایجاد می‌کند. |
| [createAlphaInverse()](#createAlphaInverse--) | یک اثر Alpha inverse ایجاد می‌کند. |
| [createAlphaModulate()](#createAlphaModulate--) | یک اثر Alpha modulate ایجاد می‌کند. |
| [createAlphaModulateFixed(float amount)](#createAlphaModulateFixed-float-) | یک اثر Alpha modulate fixed ایجاد می‌کند. |
| [createAlphaReplace(float alpha)](#createAlphaReplace-float-) | یک اثر Alpha replace ایجاد می‌کند. |
| [createBiLevel(float threshold)](#createBiLevel-float-) | یک اثر BiLevel ایجاد می‌کند. |
| [createBlur(double radius, boolean grow)](#createBlur-double-boolean-) | یک اثر Blur ایجاد می‌کند. |
| [createColorChange()](#createColorChange--) | یک اثر Color change ایجاد می‌کند. |
| [createColorReplace()](#createColorReplace--) | یک اثر Color replace ایجاد می‌کند. |
| [createDuotone()](#createDuotone--) | یک اثر Duotone ایجاد می‌کند. |
| [createFillOverlay()](#createFillOverlay--) | یک اثر Fill overlay ایجاد می‌کند. |
| [createGrayScale()](#createGrayScale--) | یک اثر Gray scale ایجاد می‌کند. |
| [createHSL(float hue, float saturation, float luminance)](#createHSL-float-float-float-) | یک اثر Hue Saturation Luminance ایجاد می‌کند. |
| [createLuminance(float brightness, float contrast)](#createLuminance-float-float-) | یک اثر Luminance ایجاد می‌کند. |
| [createTint(float hue, float amount)](#createTint-float-float-) | یک اثر Tint ایجاد می‌کند. |
### ImageTransformOperationFactory() {#ImageTransformOperationFactory--}
```
public ImageTransformOperationFactory()
```


### createAlphaBiLevel(float threshold) {#createAlphaBiLevel-float-}
```
public final IAlphaBiLevel createAlphaBiLevel(float threshold)
```


یک اثر Alpha BiLevel ایجاد می‌کند.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| threshold | float | آستانه. |

**باز می‌گرداند:**
[IAlphaBiLevel](../../com.aspose.slides/ialphabilevel) - اثر Alpha BiLevel.
### createAlphCeiling() {#createAlphCeiling--}
```
public final IAlphaCeiling createAlphCeiling()
```


یک اثر Alpha Ceiling ایجاد می‌کند.

**باز می‌گرداند:**
[IAlphaCeiling](../../com.aspose.slides/ialphaceiling) - اثر Alpha Ceiling.
### createAlphaFloor() {#createAlphaFloor--}
```
public final IAlphaFloor createAlphaFloor()
```


یک اثر Alpha floor ایجاد می‌کند.

**باز می‌گرداند:**
[IAlphaFloor](../../com.aspose.slides/ialphafloor) - اثر Alpha floor.
### createAlphaInverse() {#createAlphaInverse--}
```
public final IAlphaInverse createAlphaInverse()
```


یک اثر Alpha inverse ایجاد می‌کند.

**باز می‌گرداند:**
[IAlphaInverse](../../com.aspose.slides/ialphainverse) - اثر Alpha inverst.
### createAlphaModulate() {#createAlphaModulate--}
```
public final IAlphaModulate createAlphaModulate()
```


یک اثر Alpha modulate ایجاد می‌کند.

**باز می‌گرداند:**
[IAlphaModulate](../../com.aspose.slides/ialphamodulate) - اثر Alpha modulate.
### createAlphaModulateFixed(float amount) {#createAlphaModulateFixed-float-}
```
public final IAlphaModulateFixed createAlphaModulateFixed(float amount)
```


یک اثر Alpha modulate fixed ایجاد می‌کند.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| amount | float | مقدار. |

**باز می‌گرداند:**
[IAlphaModulateFixed](../../com.aspose.slides/ialphamodulatefixed) - اثر Alpha modulate fixed.
### createAlphaReplace(float alpha) {#createAlphaReplace-float-}
```
public final IAlphaReplace createAlphaReplace(float alpha)
```


یک اثر Alpha replace ایجاد می‌کند.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| alpha | float | Alpha |

**باز می‌گرداند:**
[IAlphaReplace](../../com.aspose.slides/ialphareplace) - اثر Alpha replace.
### createBiLevel(float threshold) {#createBiLevel-float-}
```
public final IBiLevel createBiLevel(float threshold)
```


یک اثر BiLevel ایجاد می‌کند.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| threshold | float | آستانه. |

**باز می‌گرداند:**
[IBiLevel](../../com.aspose.slides/ibilevel) - اثر BiLevel.
### createBlur(double radius, boolean grow) {#createBlur-double-boolean-}
```
public final IBlur createBlur(double radius, boolean grow)
```


یک اثر Blur ایجاد می‌کند.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| radius | double | شعاع. |
| grow | boolean | رشد. |

**باز می‌گرداند:**
[IBlur](../../com.aspose.slides/iblur) - اثر Blur.
### createColorChange() {#createColorChange--}
```
public final IColorChange createColorChange()
```


یک اثر Color change ایجاد می‌کند.

**باز می‌گرداند:**
[IColorChange](../../com.aspose.slides/icolorchange) - اثر Color change.
### createColorReplace() {#createColorReplace--}
```
public final IColorReplace createColorReplace()
```


یک اثر Color replace ایجاد می‌کند.

**باز می‌گرداند:**
[IColorReplace](../../com.aspose.slides/icolorreplace) - اثر Color replace.
### createDuotone() {#createDuotone--}
```
public final IDuotone createDuotone()
```


یک اثر Duotone ایجاد می‌کند.

**باز می‌گرداند:**
[IDuotone](../../com.aspose.slides/iduotone) - اثر Duotone.
### createFillOverlay() {#createFillOverlay--}
```
public final IFillOverlay createFillOverlay()
```


یک اثر Fill overlay ایجاد می‌کند.

**باز می‌گرداند:**
[IFillOverlay](../../com.aspose.slides/ifilloverlay) - اثر Fill overlay.
### createGrayScale() {#createGrayScale--}
```
public final IGrayScale createGrayScale()
```


یک اثر Gray scale ایجاد می‌کند.

**باز می‌گرداند:**
[IGrayScale](../../com.aspose.slides/igrayscale) - اثر Gray scale.
### createHSL(float hue, float saturation, float luminance) {#createHSL-float-float-float-}
```
public final IHSL createHSL(float hue, float saturation, float luminance)
```


یک اثر Hue Saturation Luminance ایجاد می‌کند.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| hue | float | Hue. |
| saturation | float | Saturation. |
| luminance | float | Luminance. |

**باز می‌گرداند:**
[IHSL](../../com.aspose.slides/ihsl) - اثر HSL.
### createLuminance(float brightness, float contrast) {#createLuminance-float-float-}
```
public final ILuminance createLuminance(float brightness, float contrast)
```


یک اثر Luminance ایجاد می‌کند.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| brightness | float | روشنایی. |
| contrast | float | کنتراست. |

**باز می‌گرداند:**
[ILuminance](../../com.aspose.slides/iluminance) - اثر Luminance.
### createTint(float hue, float amount) {#createTint-float-float-}
```
public final ITint createTint(float hue, float amount)
```


یک اثر Tint ایجاد می‌کند.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| hue | float | Hue. |
| amount | float | مقدار. |

**باز می‌گرداند:**
[ITint](../../com.aspose.slides/itint) - اثر Tint.