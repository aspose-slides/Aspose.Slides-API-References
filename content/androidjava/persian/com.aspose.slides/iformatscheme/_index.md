---
title: IFormatScheme
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: قالب‌های تعریف‌شده توسط تم را برای اشکال ذخیره می‌کند.
type: docs
url: /fa/com.aspose.slides/iformatscheme/
---
**همه رابط‌های پیاده‌سازی شده:**
[com.aspose.slides.ISlideComponent](../../com.aspose.slides/islidecomponent)
```
public interface IFormatScheme extends ISlideComponent
```

قالب‌های تعریف‌شده توسط تم را برای اشکال ذخیره می‌کند.
## متدها

| متد | توضیح |
| --- | --- |
| [getFillStyles()](#getFillStyles--) | یک مجموعه از سبک‌های پر تم تعریف‌شده را برمی‌گرداند. |
| [getLineStyles()](#getLineStyles--) | یک مجموعه از سبک‌های خط تم تعریف‌شده را برمی‌گرداند. |
| [getEffectStyles()](#getEffectStyles--) | یک مجموعه از سبک‌های اثر تم تعریف‌شده را برمی‌گرداند. |
| [getBackgroundFillStyles()](#getBackgroundFillStyles--) | یک مجموعه از سبک‌های پر پس‌زمینه تم تعریف‌شده را برمی‌گرداند. |
### getFillStyles() {#getFillStyles--}
```
public abstract IFillFormatCollection getFillStyles()
```

یک مجموعه از سبک‌های پر تم تعریف‌شده را برمی‌گرداند. فقط‌خواندنی [IFillFormatCollection](../../com.aspose.slides/ifillformatcollection).

**باز می‌گرداند:**
[IFillFormatCollection](../../com.aspose.slides/ifillformatcollection)
### getLineStyles() {#getLineStyles--}
```
public abstract ILineFormatCollection getLineStyles()
```

یک مجموعه از سبک‌های خط تم تعریف‌شده را برمی‌گرداند. فقط‌خواندنی [ILineFormatCollection](../../com.aspose.slides/ilineformatcollection).

**باز می‌گرداند:**
[ILineFormatCollection](../../com.aspose.slides/ilineformatcollection)
### getEffectStyles() {#getEffectStyles--}
```
public abstract IEffectStyleCollection getEffectStyles()
```

یک مجموعه از سبک‌های اثر تم تعریف‌شده را برمی‌گرداند. فقط‌خواندنی [IEffectStyleCollection](../../com.aspose.slides/ieffectstylecollection).

**باز می‌گرداند:**
[IEffectStyleCollection](../../com.aspose.slides/ieffectstylecollection)
### getBackgroundFillStyles() {#getBackgroundFillStyles--}
```
public abstract IFillFormatCollection getBackgroundFillStyles()
```

یک مجموعه از سبک‌های پر پس‌زمینه تم تعریف‌شده را برمی‌گرداند. فقط‌خواندنی [IFillFormatCollection](../../com.aspose.slides/ifillformatcollection).

**باز می‌گرداند:**
[IFillFormatCollection](../../com.aspose.slides/ifillformatcollection)