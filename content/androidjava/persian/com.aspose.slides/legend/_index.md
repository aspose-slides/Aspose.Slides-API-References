---
title: Legend
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: خواص افسانه نمودارها را نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/legend/
---
**ارث‌بری:**
java.lang.Object, com.aspose.slides.DomObject

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.ILegend](../../com.aspose.slides/ilegend)
```
public class Legend extends DomObject<Chart> implements ILegend
```

خواص افسانه نمودار را نشان می‌دهد.
## متدها

| متد | توضیح |
| --- | --- |
| [getX()](#getX--) | مقدار یا تنظیم مختصات x یک افسانه به‌صورت کسری از عرض نمودار. |
| [setX(float value)](#setX-float-) | مقدار یا تنظیم مختصات x یک افسانه به‌صورت کسری از عرض نمودار. |
| [getY()](#getY--) | مقدار یا تنظیم مختصات y یک افسانه به‌صورت کسری از ارتفاع نمودار. |
| [setY(float value)](#setY-float-) | مقدار یا تنظیم مختصات y یک افسانه به‌صورت کسری از ارتفاع نمودار. |
| [getWidth()](#getWidth--) | مقدار یا تنظیم عرض یک افسانه به‌صورت کسری از عرض نمودار. |
| [setWidth(float value)](#setWidth-float-) | مقدار یا تنظیم عرض یک افسانه به‌صورت کسری از عرض نمودار. |
| [getHeight()](#getHeight--) | مقدار یا تنظیم ارتفاع یک افسانه به‌صورت کسری از ارتفاع نمودار. |
| [setHeight(float value)](#setHeight-float-) | مقدار یا تنظیم ارتفاع یک افسانه به‌صورت کسری از ارتفاع نمودار. |
| [getRight()](#getRight--) | راست. |
| [getBottom()](#getBottom--) | پایین. |
| [getOverlay()](#getOverlay--) | مشخص می‌کند که آیا عناصر دیگر نمودار اجازهٔ همپوشانی با افسانه را داشته باشند. |
| [setOverlay(boolean value)](#setOverlay-boolean-) | مشخص می‌کند که آیا عناصر دیگر نمودار اجازهٔ همپوشانی با افسانه را داشته باشند. |
| [getTextFormat()](#getTextFormat--) | قالب متن. |
| [getPosition()](#getPosition--) | موقعیت افسانه را روی نمودار مشخص می‌کند. |
| [setPosition(int value)](#setPosition-int-) | موقعیت افسانه را روی نمودار مشخص می‌کند. |
| [getFormat()](#getFormat--) | قالب یک افسانه را برمی‌گرداند. |
| [getChart()](#getChart--) | نمودار را برمی‌گرداند. |
| [getEntries()](#getEntries--) | ورودی‌های افسانه را دریافت می‌کند. |
| [getActualX()](#getActualX--) | موقعیت واقعی x (چپ) عنصر نمودار را نسبت به گوشهٔ بالایی چپ نمودار مشخص می‌کند. |
| [getActualY()](#getActualY--) | بالای واقعی عنصر نمودار را نسبت به گوشهٔ بالایی چپ نمودار مشخص می‌کند. |
| [getActualWidth()](#getActualWidth--) | عرض واقعی عنصر نمودار را مشخص می‌کند. |
| [getActualHeight()](#getActualHeight--) | ارتفاع واقعی عنصر نمودار را مشخص می‌کند. |
| [getSlide()](#getSlide--) | اسلاید والد یک FillFormat را برمی‌گرداند. |
| [getPresentation()](#getPresentation--) | ارائهٔ والد یک FillFormat را برمی‌گرداند. |

### getX() {#getX--}
```
public final float getX()
```

مقدار یا تنظیم مختصات x یک افسانه به‌صورت کسری از عرض نمودار. خواندنی/نوشتنی float.

**بازگشت:**
float
### setX(float value) {#setX-float-}
```
public final void setX(float value)
```

مقدار یا تنظیم مختصات x یک افسانه به‌صورت کسری از عرض نمودار. خواندنی/نوشتنی float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getY() {#getY--}
```
public final float getY()
```

مقدار یا تنظیم مختصات y یک افسانه به‌صورت کسری از ارتفاع نمودار. خواندنی/نوشتنی float.

**بازگشت:**
float
### setY(float value) {#setY-float-}
```
public final void setY(float value)
```

مقدار یا تنظیم مختصات y یک افسانه به‌صورت کسری از ارتفاع نمودار. خواندنی/نوشتنی float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getWidth() {#getWidth--}
```
public final float getWidth()
```

مقدار یا تنظیم عرض یک افسانه به‌صورت کسری از عرض نمودار. خواندنی/نوشتنی float.

**بازگشت:**
float
### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```

مقدار یا تنظیم عرض یک افسانه به‌صورت کسری از عرض نمودار. خواندنی/نوشتنی float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getHeight() {#getHeight--}
```
public final float getHeight()
```

مقدار یا تنظیم ارتفاع یک افسانه به‌صورت کسری از ارتفاع نمودار. خواندنی/نوشتنی float.

**بازگشت:**
float
### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```

مقدار یا تنظیم ارتفاع یک افسانه به‌صورت کسری از ارتفاع نمودار. خواندنی/نوشتنی float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getRight() {#getRight--}
```
public final float getRight()
```

راست. فقط-خواندنی float.

**بازگشت:**
float
### getBottom() {#getBottom--}
```
public final float getBottom()
```

پایین. فقط-خواندنی float.

**بازگشت:**
float
### getOverlay() {#getOverlay--}
```
public final boolean getOverlay()
```

مشخص می‌کند که آیا عناصر دیگر نمودار اجازهٔ همپوشانی با افسانه را داشته باشند. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setOverlay(boolean value) {#setOverlay-boolean-}
```
public final void setOverlay(boolean value)
```

مشخص می‌کند که آیا عناصر دیگر نمودار اجازهٔ همپوشانی با افسانه را داشته باشند. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getTextFormat() {#getTextFormat--}
```
public final IChartTextFormat getTextFormat()
```

قالب متن. فقط-خواندنی [IChartTextFormat](../../com.aspose.slides/icharttextformat).

**بازگشت:**
[IChartTextFormat](../../com.aspose.slides/icharttextformat)
### getPosition() {#getPosition--}
```
public final int getPosition()
```

موقعیت افسانه را روی نمودار مشخص می‌کند. مقادیر غیر NaN ویژگی‌های X، Y، Width، Height اثر این ویژگی را نادیده می‌گیرند. خواندنی/نوشتنی [LegendPositionType](../../com.aspose.slides/legendpositiontype).

**بازگشت:**
int
### setPosition(int value) {#setPosition-int-}
```
public final void setPosition(int value)
```

موقعیت افسانه را روی نمودار مشخص می‌کند. مقادیر غیر NaN ویژگی‌های X، Y، Width، Height اثر این ویژگی را نادیده می‌گیرند. خواندنی/نوشتنی [LegendPositionType](../../com.aspose.slides/legendpositiontype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

قالب یک افسانه را برمی‌گرداند. فقط-خواندنی [IFormat](../../com.aspose.slides/iformat).

**بازگشت:**
[IFormat](../../com.aspose.slides/iformat)
### getChart() {#getChart--}
```
public final IChart getChart()
```

نمودار را برمی‌گرداند. فقط-خواندنی [IChart](../../com.aspose.slides/ichart).

**بازگشت:**
[IChart](../../com.aspose.slides/ichart)
### getEntries() {#getEntries--}
```
public final ILegendEntryCollection getEntries()
```

ورودی‌های افسانه را دریافت می‌کند. فقط-خواندنی [ILegendEntryCollection](../../com.aspose.slides/ilegendentrycollection).

**بازگشت:**
[ILegendEntryCollection](../../com.aspose.slides/ilegendentrycollection)
### getActualX() {#getActualX--}
```
public final float getActualX()
```

موقعیت واقعی x (چپ) عنصر نمودار را نسبت به گوشهٔ بالایی چپ نمودار مشخص می‌کند. برای دریافت مقادیر واقعی متد IChart.validateChartLayout() را پیش از فراخوانی اجرا کنید. خواندنی float.

**بازگشت:**
float
### getActualY() {#getActualY--}
```
public final float getActualY()
```

بالای واقعی عنصر نمودار را نسبت به گوشهٔ بالایی چپ نمودار مشخص می‌کند. برای دریافت مقادیر واقعی متد IChart.validateChartLayout() را پیش از فراخوانی اجرا کنید. خواندنی float.

**بازگشت:**
float
### getActualWidth() {#getActualWidth--}
```
public final float getActualWidth()
```

عرض واقعی عنصر نمودار را مشخص می‌کند. برای دریافت مقادیر واقعی متد IChart.validateChartLayout() را پیش از فراخوانی اجرا کنید. خواندنی float.

**بازگشت:**
float
### getActualHeight() {#getActualHeight--}
```
public final float getActualHeight()
```

ارتفاع واقعی عنصر نمودار را مشخص می‌کند. برای دریافت مقادیر واقعی متد IChart.validateChartLayout() را پیش از فراخوانی اجرا کنید. خواندنی float.

**بازگشت:**
float
### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

اسلاید والد یک FillFormat را برمی‌گرداند. فقط-خواندنی [BaseSlide](../../com.aspose.slides/baseslide).

**بازگشت:**
[IBaseSlide](../../com.aspose.slides/ibaseslide)
### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

ارائهٔ والد یک FillFormat را برمی‌گرداند. فقط-خواندنی [IPresentation](../../com.aspose.slides/ipresentation).

**بازگشت:**
[IPresentation](../../com.aspose.slides/ipresentation)