---
title: Timing
second_title: Aspose.Slides برای Android از طریق مرجع API Java
description: نمایانگر زمان‌بندی انیمیشن.
type: docs
url: /fa/com.aspose.slides/timing/
---
**Inheritance:**
java.lang.Object

**All Implemented Interfaces:**
[com.aspose.slides.ITiming](../../com.aspose.slides/itiming), com.aspose.slides.IDOMObject
```
public class Timing implements ITiming, IDOMObject
```

Represents animation timing.
## متدها

| متد | توضیح |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | درصد رفتار شتابی مدت زمان را توصیف می‌کند. |
| [setAccelerate(float value)](#setAccelerate-float-) | درصد رفتار شتابی مدت زمان را توصیف می‌کند. |
| [getDecelerate()](#getDecelerate--) | درصد رفتار کاهشی مدت زمان را توصیف می‌کند. |
| [setDecelerate(float value)](#setDecelerate-float-) | درصد رفتار کاهشی مدت زمان را توصیف می‌کند. |
| [getAutoReverse()](#getAutoReverse--) | تعیین می‌کند که آیا انیمیشن پس از پخش در جهت پیش به صورت خودکار به صورت معکوس پخش شود یا خیر. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | تعیین می‌کند که آیا انیمیشن پس از پخش در جهت پیش به صورت خودکار به صورت معکوس پخش شود یا خیر. |
| [getDuration()](#getDuration--) | مدت زمان اثر انیمیشن را توصیف می‌کند. |
| [setDuration(float value)](#setDuration-float-) | مدت زمان اثر انیمیشن را توصیف می‌کند. |
| [getRepeatCount()](#getRepeatCount--) | تعداد دفعات تکرار اثر را توصیف می‌کند. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | تعداد دفعات تکرار اثر را توصیف می‌کند. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | این ویژگی مشخص می‌کند که آیا اثر تا پایان اسلاید تکرار شود یا خیر. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | این ویژگی مشخص می‌کند که آیا اثر تا پایان اسلاید تکرار شود یا خیر. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | این ویژگی مشخص می‌کند که آیا اثر تا کلیک بعدی تکرار شود یا خیر. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | این ویژگی مشخص می‌کند که آیا اثر تا کلیک بعدی تکرار شود یا خیر. |
| [getRepeatDuration()](#getRepeatDuration--) | تعداد دفعات تکرار اثر را توصیف می‌کند. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | تعداد دفعات تکرار اثر را توصیف می‌کند. |
| [getRestart()](#getRestart--) | مشخص می‌کند که آیا اثر پس از اتمام مجدداً آغاز شود یا خیر. |
| [setRestart(int value)](#setRestart-int-) | مشخص می‌کند که آیا اثر پس از اتمام مجدداً آغاز شود یا خیر. |
| [getRewind()](#getRewind--) | این ویژگی مشخص می‌کند که آیا اثر پس از اتمام پخش به عقب برگردد یا خیر. |
| [setRewind(boolean value)](#setRewind-boolean-) | این ویژگی مشخص می‌کند که آیا اثر پس از اتمام پخش به عقب برگردد یا خیر. |
| [getSpeed()](#getSpeed--) | درصدی که زمان‌بندی باید سرعت یا کند شود را توصیف می‌کند. |
| [setSpeed(float value)](#setSpeed-float-) | درصدی که زمان‌بندی باید سرعت یا کند شود را توصیف می‌کند. |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | زمان تأخیر پس از فعال‌ساز را توصیف می‌کند. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | زمان تأخیر پس از فعال‌ساز را توصیف می‌کند. |
| [getTriggerType()](#getTriggerType--) | نوع فعال‌ساز را توصیف می‌کند. |
| [setTriggerType(int value)](#setTriggerType-int-) | نوع فعال‌ساز را توصیف می‌کند. |
| [getParent_Immediate()](#getParent-Immediate--) |  |

### getAccelerate() {#getAccelerate--}
```
public final float getAccelerate()
```

درصد رفتار شتابی مدت زمان را توصیف می‌کند. قابل خواندن/نوشتن float.

**بازگشت:**
float

### setAccelerate(float value) {#setAccelerate-float-}
```
public final void setAccelerate(float value)
```

درصد رفتار شتابی مدت زمان را توصیف می‌کند. قابل خواندن/نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public final float getDecelerate()
```

درصد رفتار کاهشی مدت زمان را توصیف می‌کند. قابل خواندن/نوشتن float.

**بازگشت:**
float

### setDecelerate(float value) {#setDecelerate-float-}
```
public final void setDecelerate(float value)
```

درصد رفتار کاهشی مدت زمان را توصیف می‌کند. قابل خواندن/نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public final boolean getAutoReverse()
```

تعیین می‌کند که آیا انیمیشن پس از پخش در جهت پیش به صورت خودکار به صورت معکوس پخش شود یا خیر. قابل خواندن/نوشتن boolean.

**بازگشت:**
boolean

### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public final void setAutoReverse(boolean value)
```

تعیین می‌کند که آیا انیمیشن پس از پخش در جهت پیش به صورت خودکار به صورت معکوس پخش شود یا خیر. قابل خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public final float getDuration()
```

مدت زمان اثر انیمیشن را توصیف می‌کند. قابل خواندن/نوشتن float.

**بازگشت:**
float

### setDuration(float value) {#setDuration-float-}
```
public final void setDuration(float value)
```

مدت زمان اثر انیمیشن را توصیف می‌کند. قابل خواندن/نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public final float getRepeatCount()
```

تعداد دفعات تکرار اثر را توصیف می‌کند. قابل خواندن/نوشتن float.

**بازگشت:**
float

### setRepeatCount(float value) {#setRepeatCount-float-}
```
public final void setRepeatCount(float value)
```

تعداد دفعات تکرار اثر را توصیف می‌کند. قابل خواندن/نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public final boolean getRepeatUntilEndSlide()
```

این ویژگی مشخص می‌کند که آیا اثر تا پایان اسلاید تکرار شود یا خیر. قابل خواندن/نوشتن boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت توالی افکت‌ها برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین افکت از توالی اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // تغییر زمان‌بندی/تکرار افکت به "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**بازگشت:**
boolean

### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public final void setRepeatUntilEndSlide(boolean value)
```

این ویژگی مشخص می‌کند که آیا اثر تا پایان اسلاید تکرار شود یا خیر. قابل خواندن/نوشتن boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت توالی افکت‌ها برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین افکت از توالی اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // تغییر زمان‌بندی/تکرار افکت به "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public final boolean getRepeatUntilNextClick()
```

این ویژگی مشخص می‌کند که آیا اثر تا کلیک بعدی تکرار شود یا خیر. قابل خواندن/نوشتن boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت توالی افکت‌ها برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین افکت از توالی اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // تغییر زمان‌بندی/تکرار افکت به "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**بازگشت:**
boolean

### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public final void setRepeatUntilNextClick(boolean value)
```

این ویژگی مشخص می‌کند که آیا اثر تا کلیک بعدی تکرار شود یا خیر. قابل خواندن/نوشتن boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت توالی افکت‌ها برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین افکت از توالی اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // تغییر زمان‌بندی/تکرار افکت به "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public final float getRepeatDuration()
```

تعداد دفعات تکرار اثر را توصیف می‌کند. قابل خواندن/نوشتن float.

**بازگشت:**
float

### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public final void setRepeatDuration(float value)
```

تعداد دفعات تکرار اثر را توصیف می‌کند. قابل خواندن/نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public final int getRestart()
```

مشخص می‌کند که آیا اثر پس از اتمام مجدداً آغاز شود یا خیر. قابل خواندن/نوشتن [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**بازگشت:**
int

### setRestart(int value) {#setRestart-int-}
```
public final void setRestart(int value)
```

مشخص می‌کند که آیا اثر پس از اتمام مجدداً آغاز شود یا خیر. قابل خواندن/نوشتن [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public final boolean getRewind()
```

این ویژگی مشخص می‌کند که آیا اثر پس از پایان پخش به عقب برگردد یا خیر. قابل خواندن/نوشتن boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت توالی افکت‌ها برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین افکت از توالی اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // فعال‌سازی Timing/Rewind افکت.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**بازگشت:**
boolean

### setRewind(boolean value) {#setRewind-boolean-}
```
public final void setRewind(boolean value)
```

این ویژگی مشخص می‌کند که آیا اثر پس از پایان پخش به عقب برگردد یا خیر. قابل خواندن/نوشتن boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت توالی افکت‌ها برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین افکت از توالی اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // فعال‌سازی Timing/Rewind افکت.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getSpeed() {#getSpeed--}
```
public final float getSpeed()
```

درصدی که زمان‌بندی باید سرعت یا کند شود را توصیف می‌کند. قابل خواندن/نوشتن float.

**بازگشت:**
float

### setSpeed(float value) {#setSpeed-float-}
```
public final void setSpeed(float value)
```

درصدی که زمان‌بندی باید سرعت یا کند شود را توصیف می‌کند. قابل خواندن/نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public final float getTriggerDelayTime()
```

زمان تأخیر پس از فعال‌ساز را توصیف می‌کند. قابل خواندن/نوشتن float.

**بازگشت:**
float

### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public final void setTriggerDelayTime(float value)
```

زمان تأخیر پس از فعال‌ساز را توصیف می‌کند. قابل خواندن/نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public final int getTriggerType()
```

نوع فعال‌ساز را توصیف می‌کند. قابل خواندن/نوشتن [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**بازگشت:**
int

### setTriggerType(int value) {#setTriggerType-int-}
```
public final void setTriggerType(int value)
```

نوع فعال‌ساز را توصیف می‌کند. قابل خواندن/نوشتن [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

Returns Parent\_Immediate object. تنها-خواندنی IDOMObject.

**بازگشت:**
com.aspose.slides.IDOMObject