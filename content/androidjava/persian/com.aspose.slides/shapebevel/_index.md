---
title: ShapeBevel
second_title: Aspose.Slides برای اندروید از طریق مرجع API جاوا
description: خواص برجستگی سطح اصلی شکل‌ها را شامل می‌شود.
type: docs
url: /fa/com.aspose.slides/shapebevel/
---
**ارث‌بری:**
java.lang.Object, [com.aspose.slides.PVIObject](../../com.aspose.slides/pviobject)

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IShapeBevel](../../com.aspose.slides/ishapebevel)
```
public final class ShapeBevel extends PVIObject implements IShapeBevel
```

خواص سطح اصلی شکل را شامل می‌شود.
## سازنده‌ها

| سازنده | توضیح |
| --- | --- |
| [ShapeBevel(boolean bIsTopBevel)](#ShapeBevel-boolean-) | یک نمونه جدید ایجاد می‌کند. |
## متدها

| متد | توضیح |
| --- | --- |
| [getVersion()](#getVersion--) |  |
| [equals(Object obj)](#equals-java.lang.Object-) |  |
| [getWidth()](#getWidth--) | عرض برجستگی. |
| [setWidth(double value)](#setWidth-double-) | عرض برجستگی. |
| [getHeight()](#getHeight--) | ارتفاع برجستگی. |
| [setHeight(double value)](#setHeight-double-) | ارتفاع برجستگی. |
| [getBevelType()](#getBevelType--) | نوع برجستگی. |
| [setBevelType(int value)](#setBevelType-int-) | نوع برجستگی. |
### ShapeBevel(boolean bIsTopBevel) {#ShapeBevel-boolean-}
```
public ShapeBevel(boolean bIsTopBevel)
```

یک نمونه جدید ایجاد می‌کند.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| bIsTopBevel | boolean | شیب بالایی است. |
### getVersion() {#getVersion--}
```
public long getVersion()
```

نسخه. فقط-خواندنی long.

**برگشت:**
long
### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```

با شیء مشخص‌شده مقایسه می‌کند.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| obj | java.lang.Object |  |

**برگشت:**
boolean
### getWidth() {#getWidth--}
```
public final double getWidth()
```

عرض برجستگی. قابل خواندن/نوشتن double.

**برگشت:**
double
### setWidth(double value) {#setWidth-double-}
```
public final void setWidth(double value)
```

عرض برجستگی. قابل خواندن/نوشتن double.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | double |  |
### getHeight() {#getHeight--}
```
public final double getHeight()
```

ارتفاع برجستگی. قابل خواندن/نوشتن double.

**برگشت:**
double
### setHeight(double value) {#setHeight-double-}
```
public final void setHeight(double value)
```

ارتفاع برجستگی. قابل خواندن/نوشتن double.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | double |  |
### getBevelType() {#getBevelType--}
```
public final int getBevelType()
```

نوع برجستگی. قابل خواندن/نوشتن [BevelPresetType](../../com.aspose.slides/bevelpresettype).

**برگشت:**
int
### setBevelType(int value) {#setBevelType-int-}
```
public final void setBevelType(int value)
```

نوع برجستگی. قابل خواندن/نوشتن [BevelPresetType](../../com.aspose.slides/bevelpresettype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |