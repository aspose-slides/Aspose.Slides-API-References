---
title: IShapeFrame
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: خواص قاب‌های شکل را نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/ishapeframe/
---
**تمام اینترفیس‌های پیاده‌سازی‌شده:**
com.aspose.slides.IGenericCloneable
```
public interface IShapeFrame extends IGenericCloneable<IShapeFrame>
```

خواص قاب شکل را نشان می‌دهد.
## متدها

| متد | توضیح |
| --- | --- |
| [getX()](#getX--) | مختصات X گوشه بالای چپ یک قاب را باز می‌گرداند. |
| [getY()](#getY--) | مختصات Y گوشه بالای چپ یک قاب را باز می‌گرداند. |
| [getWidth()](#getWidth--) | عرض یک قاب را باز می‌گرداند. |
| [getHeight()](#getHeight--) | ارتفاع یک قاب را باز می‌گرداند. |
| [getRotation()](#getRotation--) | تعداد درجه‌های چرخش یک قاب حول محور z را باز می‌گرداند. |
| [getCenterX()](#getCenterX--) | مختصات X مرکز یک قاب را باز می‌گرداند. |
| [getCenterY()](#getCenterY--) | مختصات Y مرکز یک قاب را باز می‌گرداند. |
| [getFlipH()](#getFlipH--) | تشخیص می‌دهد آیا یک قاب به صورت افقی وارونه شده است یا خیر. |
| [getFlipV()](#getFlipV--) | تشخیص می‌دهد آیا یک قاب به صورت عمودی وارونه شده است یا خیر. |
| [getRectangle()](#getRectangle--) | مختصات یک قاب را باز می‌گرداند. |
### getX() {#getX--}
```
public abstract float getX()
```


مختصات X گوشه بالای چپ یک قاب را باز می‌گرداند. فقط خواندنی float.

**باز می‌گرداند:**
float
### getY() {#getY--}
```
public abstract float getY()
```


مختصات Y گوشه بالای چپ یک قاب را باز می‌گرداند. فقط خواندنی float.

**باز می‌گرداند:**
float
### getWidth() {#getWidth--}
```
public abstract float getWidth()
```


عرض یک قاب را باز می‌گرداند. فقط خواندنی float.

**باز می‌گرداند:**
float
### getHeight() {#getHeight--}
```
public abstract float getHeight()
```


ارتفاع یک قاب را باز می‌گرداند. فقط خواندنی float.

**باز می‌گرداند:**
float
### getRotation() {#getRotation--}
```
public abstract float getRotation()
```


تعداد درجه‌های چرخش یک قاب حول محور z را باز می‌گرداند. مقدار مثبت نشان‌دهنده چرخش ساعت‌گرد؛ مقدار منفی نشان‌دهنده چرخش پادساعت‌گرد است. فقط خواندنی float.

**باز می‌گرداند:**
float
### getCenterX() {#getCenterX--}
```
public abstract float getCenterX()
```


مختصات X مرکز یک قاب را باز می‌گرداند. فقط خواندنی float.

**باز می‌گرداند:**
float
### getCenterY() {#getCenterY--}
```
public abstract float getCenterY()
```


مختصات Y مرکز یک قاب را باز می‌گرداند. فقط خواندنی float.

**باز می‌گرداند:**
float
### getFlipH() {#getFlipH--}
```
public abstract byte getFlipH()
```


تشخیص می‌دهد آیا یک قاب به صورت افقی وارونه شده است یا خیر. فقط خواندنی [NullableBool](../../com.aspose.slides/nullablebool).

**باز می‌گرداند:**
byte
### getFlipV() {#getFlipV--}
```
public abstract byte getFlipV()
```


تشخیص می‌دهد آیا یک قاب به صورت عمودی وارونه شده است یا خیر. فقط خواندنی [NullableBool](../../com.aspose.slides/nullablebool).

**باز می‌گرداند:**
byte
### getRectangle() {#getRectangle--}
```
public abstract RectF getRectangle()
```


مختصات یک قاب را باز می‌گرداند. فقط خواندنی android.graphics.RectF.

**باز می‌گرداند:**
android.graphics.RectF