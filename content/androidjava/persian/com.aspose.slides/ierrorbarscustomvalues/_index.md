---
title: IErrorBarsCustomValues
second_title: Aspose.Slides for Android via Java API Reference
description: مقدارهای نوارهای خطا را مشخص می‌کند.
type: docs
url: /fa/com.aspose.slides/ierrorbarscustomvalues/
---```
public interface IErrorBarsCustomValues
```

مقدارهای نوارهای خطا را مشخص می‌کند. این باید فقط زمانی استفاده شود که نوع مقدار نوارهای خطا سفارشی (Custom) باشد.
## Methods

| Method | Description |
| --- | --- |
| [getXMinus()](#getXMinus--) | مقدار نوار خطا را در جهت منفی مشخص می‌کند. |
| [getYMinus()](#getYMinus--) | مقدار نوار خطا را در جهت منفی مشخص می‌کند. |
| [getXPlus()](#getXPlus--) | مقدار نوار خطا را در جهت مثبت مشخص می‌کند. |
| [getYPlus()](#getYPlus--) | مقدار نوار خطا را در جهت مثبت مشخص می‌کند. |
### getXMinus() {#getXMinus--}
```
public abstract IDoubleChartValue getXMinus()
```


مقدار نوار خطا را در جهت منفی مشخص می‌کند. در صورتیکه نوع مقدار نوارهای خطا سفارشی (Custom) باشد و ErrorBarsXFormat مجاز باشد، در دسترس است. در هر مورد دیگر این ویژگی مقدار null برمی‌گرداند. فقط-خواندنی [IDoubleChartValue](../../com.aspose.slides/idoublechartvalue).

**Returns:**  
[IDoubleChartValue](../../com.aspose.slides/idoublechartvalue)
### getYMinus() {#getYMinus--}
```
public abstract IDoubleChartValue getYMinus()
```


مقدار نوار خطا را در جهت منفی مشخص می‌کند. در صورتیکه نوع مقدار نوارهای خطا سفارشی (Custom) باشد و ErrorBarsYFormat مجاز باشد، در دسترس است. در هر مورد دیگر این ویژگی مقدار null برمی‌گرداند. فقط-خواندنی [IDoubleChartValue](../../com.aspose.slides/idoublechartvalue).

**Returns:**  
[IDoubleChartValue](../../com.aspose.slides/idoublechartvalue)
### getXPlus() {#getXPlus--}
```
public abstract IDoubleChartValue getXPlus()
```


مقدار نوار خطا را در جهت مثبت مشخص می‌کند. در صورتیکه نوع مقدار نوارهای خطا سفارشی (Custom) باشد و ErrorBarsXFormat مجاز باشد، در دسترس است. در هر مورد دیگر این ویژگی مقدار null برمی‌گرداند. فقط-خواندنی [IDoubleChartValue](../../com.aspose.slides/idoublechartvalue).

**Returns:**  
[IDoubleChartValue](../../com.aspose.slides/idoublechartvalue)
### getYPlus() {#getYPlus--}
```
public abstract IDoubleChartValue getYPlus()
```


مقدار نوار خطا را در جهت مثبت مشخص می‌کند. در صورتیکه نوع مقدار نوارهای خطا سفارشی (Custom) باشد و ErrorBarsYFormat مجاز باشد، در دسترس است. در هر مورد دیگر این ویژگی مقدار null برمی‌گرداند. فقط-خواندنی [IDoubleChartValue](../../com.aspose.slides/idoublechartvalue).

**Returns:**  
[IDoubleChartValue](../../com.aspose.slides/idoublechartvalue)