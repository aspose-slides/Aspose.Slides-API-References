---
title: SvgEvent
second_title: Aspose.Slides برای اندروید از طریق API جاوا
description: نمایانگر گزینه‌های شکل SVG.
type: docs
url: /fa/com.aspose.slides/svgevent/
---
**ارث‌بری:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class SvgEvent extends System.Enum
```

نمایانگر گزینه‌های شکل SVG.
## فیلدها

| فیلد | توضیح |
| --- | --- |
| [OnFocusIn](#OnFocusIn) | در زمانی که یک عنصر فوکوس دریافت می‌کند، مثلاً زمانی که متنی انتخاب می شود. |
| [OnFocusOut](#OnFocusOut) | در زمانی که یک عنصر فوکوس خود را از دست می‌دهد، مثلاً زمانی که متن انتخاب شده لغو می‌شود. |
| [OnActivate](#OnActivate) | در زمانی که یک عنصر فعال می‌شود، برای مثال از طریق کلیک ماوس یا فشار کلید. |
| [OnClick](#OnClick) | در زمانی که دکمهٔ دستگاه اشاره‌کننده بر روی یک عنصر کلیک می‌شود. |
| [OnMouseDown](#OnMouseDown) | در زمانی که دکمهٔ دستگاه اشاره‌کننده بر روی یک عنصر فشار داده می‌شود. |
| [OnMouseUp](#OnMouseUp) | در زمانی که دکمهٔ دستگاه اشاره‌کننده بر روی یک عنصر رها می‌شود. |
| [OnMouseOver](#OnMouseOver) | در زمانی که دستگاه اشاره‌کننده بر روی یک عنصر حرکت می‌کند. |
| [OnMouseMove](#OnMouseMove) | در زمانی که دستگاه اشاره‌کننده در حالی که بر روی یک عنصر است، حرکت می‌کند. |
| [OnMouseOut](#OnMouseOut) | در زمانی که دستگاه اشاره‌کننده از یک عنصر دور می‌شود. |
| [OnLoad](#OnLoad) | در زمانی که عامل کاربر تمام عنصر و فرزندان آن و تمام منابع ارجاع داده شده مورد نیاز برای رندر شدن را به‌طور کامل تجزیه کرده است. |
| [OnUnload](#OnUnload) | در زمانی که پیاده‌سازی DOM یک سند را از یک پنجره یا فریم حذف می‌کند. |
| [OnAbort](#OnAbort) | در زمانی که بارگذاری صفحه قبل از اینکه یک عنصر به‌طور کامل بارگذاری شود، متوقف می‌شود. |
| [OnError](#OnError) | در زمانی که یک عنصر به‌درستی بارگذاری نمی‌شود یا خطایی هنگام اجرای اسکریپت رخ می‌دهد. |
| [OnResize](#OnResize) | در زمانی که نمای سند در حال تغییر اندازه است. |
| [OnScroll](#OnScroll) | در زمانی که نمای سند در محور X یا Y یا هر دو جابجا می‌شود. |
| [OnZoom](#OnZoom) | در زمانی که سطح بزرگ‌نمایی نمای سند در حال تغییر است. |
| [OnBegin](#OnBegin) | در زمانی که عنصر انیمیشن آغاز می‌شود. |
| [OnEnd](#OnEnd) | در زمانی که عنصر انیمیشن به پایان می‌رسد. |
| [OnRepeat](#OnRepeat) | در زمانی که عنصر انیمیشن تکرار می‌شود. |
### OnFocusIn {#OnFocusIn}
```
public static final int OnFocusIn
```

در زمانی که یک عنصر فوکوس دریافت می‌کند، مثلاً زمانی که متنی انتخاب می شود.

### OnFocusOut {#OnFocusOut}
```
public static final int OnFocusOut
```

در زمانی که یک عنصر فوکوس خود را از دست می‌دهد، مثلاً زمانی که متن انتخاب شده لغو می‌شود.

### OnActivate {#OnActivate}
```
public static final int OnActivate
```

در زمانی که یک عنصر فعال می‌شود، برای مثال از طریق کلیک ماوس یا فشار کلید.

### OnClick {#OnClick}
```
public static final int OnClick
```

در زمانی که دکمهٔ دستگاه اشاره‌کننده بر روی یک عنصر کلیک می‌شود.

### OnMouseDown {#OnMouseDown}
```
public static final int OnMouseDown
```

در زمانی که دکمهٔ دستگاه اشاره‌کننده بر روی یک عنصر فشار داده می‌شود.

### OnMouseUp {#OnMouseUp}
```
public static final int OnMouseUp
```

در زمانی که دکمهٔ دستگاه اشاره‌کننده بر روی یک عنصر رها می‌شود.

### OnMouseOver {#OnMouseOver}
```
public static final int OnMouseOver
```

در زمانی که دستگاه اشاره‌کننده بر روی یک عنصر حرکت می‌کند.

### OnMouseMove {#OnMouseMove}
```
public static final int OnMouseMove
```

در زمانی که دستگاه اشاره‌کننده در حالی که بر روی یک عنصر است، حرکت می‌کند.

### OnMouseOut {#OnMouseOut}
```
public static final int OnMouseOut
```

در زمانی که دستگاه اشاره‌کننده از یک عنصر دور می‌شود.

### OnLoad {#OnLoad}
```
public static final int OnLoad
```

در زمانی که عامل کاربر تمام عنصر و فرزندان آن و تمام منابع ارجاع داده شده مورد نیاز برای رندر شدن را به‌طور کامل تجزیه کرده است.

### OnUnload {#OnUnload}
```
public static final int OnUnload
```

در زمانی که پیاده‌سازی DOM یک سند را از یک پنجره یا فریم حذف می‌کند. Only applicable to outermost svg elements.

### OnAbort {#OnAbort}
```
public static final int OnAbort
```

در زمانی که بارگذاری صفحه قبل از اینکه یک عنصر به‌طور کامل بارگذاری شود، متوقف می‌شود.

### OnError {#OnError}
```
public static final int OnError
```

در زمانی که یک عنصر به‌درستی بارگذاری نمی‌شود یا خطایی هنگام اجرای اسکریپت رخ می‌دهد.

### OnResize {#OnResize}
```
public static final int OnResize
```

در زمانی که نمای سند در حال تغییر اندازه است. Only applicable to outermost svg elements.

### OnScroll {#OnScroll}
```
public static final int OnScroll
```

در زمانی که نمای سند در محور X یا Y یا هر دو جابجا می‌شود. Only applicable to outermost svg elements.

### OnZoom {#OnZoom}
```
public static final int OnZoom
```

در زمانی که سطح بزرگ‌نمایی نمای سند در حال تغییر است. Only applicable to outermost svg elements.

### OnBegin {#OnBegin}
```
public static final int OnBegin
```

در زمانی که عنصر انیمیشن آغاز می‌شود.

### OnEnd {#OnEnd}
```
public static final int OnEnd
```

در زمانی که عنصر انیمیشن به پایان می‌رسد.

### OnRepeat {#OnRepeat}
```
public static final int OnRepeat
```

در زمانی که عنصر انیمیشن تکرار می‌شود.