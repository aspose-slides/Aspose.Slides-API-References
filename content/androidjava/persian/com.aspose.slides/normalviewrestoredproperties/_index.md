---
title: NormalViewRestoredProperties
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: اندازهٔ ناحیهٔ اسلاید را مشخص می‌کند؛ عرض وقتی فرزند restoredTop، ارتفاع وقتی فرزند restoredLeft در نمای معمولی، زمانی که ناحیه دارای اندازهٔ متغیر restored باشد (نه حداقل‌شده نه حداکثر‌شده).
type: docs
url: /fa/com.aspose.slides/normalviewrestoredproperties/
---
**وراثت:**
java.lang.Object

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)
```
public class NormalViewRestoredProperties implements INormalViewRestoredProperties
```

ابعاد ناحیهٔ اسلاید را مشخص می‌کند ((عرض وقتی فرزند restoredTop، ارتفاع وقتی فرزند restoredLeft) در نمای معمولی، وقتی ناحیه دارای اندازهٔ متغیر restored است (نه حداقل‌شده نه حداکثر‌شده)).

## متدها

| متد | توضیح |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | اندازهٔ ناحیهٔ اسلاید را مشخص می‌کند (عرض وقتی فرزند RestoredTop، ارتفاع وقتی فرزند RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | اندازهٔ ناحیهٔ اسلاید را مشخص می‌کند (عرض وقتی فرزند RestoredTop، ارتفاع وقتی فرزند RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | مشخص می‌کند آیا اندازهٔ ناحیهٔ محتویات جانبی باید برای اندازهٔ جدید جبران کند هنگام تغییر اندازهٔ پنجرهٔ حاوی نما داخل برنامه Read/write boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | مشخص می‌کند آیا اندازهٔ ناحیهٔ محتویات جانبی باید برای اندازهٔ جدید جبران کند هنگام تغییر اندازهٔ پنجرهٔ حاوی نما داخل برنامه Read/write boolean. |

### getDimensionSize() {#getDimensionSize--}
```
public final float getDimensionSize()
```

اندازهٔ ناحیهٔ اسلاید را مشخص می‌کند (عرض وقتی فرزند RestoredTop، ارتفاع وقتی فرزند RestoredLeft). Read/write float.

**بازگشت:**
float

### setDimensionSize(float value) {#setDimensionSize-float-}
```
public final void setDimensionSize(float value)
```

اندازهٔ ناحیهٔ اسلاید را مشخص می‌کند (عرض وقتی فرزند RestoredTop، ارتفاع وقتی فرزند RestoredLeft). Read/write float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public final boolean getAutoAdjust()
```

مشخص می‌کند آیا اندازهٔ ناحیهٔ محتویات جانبی باید برای اندازهٔ جدید جبران کند هنگام تغییر اندازهٔ پنجرهٔ حاوی نمای داخل برنامه Read/write boolean.

**بازگشت:**
boolean

### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public final void setAutoAdjust(boolean value)
```

مشخص می‌کند آیا اندازهٔ ناحیهٔ محتویات جانبی باید برای اندازهٔ جدید جبران کند هنگام تغییر اندازهٔ پنجرهٔ حاوی نمای داخل برنامه Read/write boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |