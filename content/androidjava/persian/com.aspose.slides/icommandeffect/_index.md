---
title: ICommandEffect
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: یک اثر فرمان برای رفتار انیمیشن را نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/icommandeffect/
---
**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface ICommandEffect extends IBehavior
```

یک اثر فرمان برای رفتار انیمیشن را نشان می‌دهد.
## متدها

| متد | توضیح |
| --- | --- |
| [getType()](#getType--) | نوع اثر فرمان رفتار را تعریف می‌کند. |
| [setType(byte value)](#setType-byte-) | نوع اثر فرمان رفتار را تعریف می‌کند. |
| [getCommandString()](#getCommandString--) | رشتهٔ فرمان را تعریف می‌کند. |
| [setCommandString(String value)](#setCommandString-java.lang.String-) | رشتهٔ فرمان را تعریف می‌کند. |
| [getShapeTarget()](#getShapeTarget--) | هدف شکل اثر فرمان را تعریف می‌کند. |
| [setShapeTarget(IShape value)](#setShapeTarget-com.aspose.slides.IShape-) | هدف شکل اثر فرمان را تعریف می‌کند. |
### getType() {#getType--}
```
public abstract byte getType()
```

نوع اثر فرمان رفتار را تعریف می‌کند. خواندنی/نوشتنی [CommandEffectType](../../com.aspose.slides/commandeffecttype).

**بازگشت:**
byte
### setType(byte value) {#setType-byte-}
```
public abstract void setType(byte value)
```

نوع اثر فرمان رفتار را تعریف می‌کند. خواندنی/نوشتنی [CommandEffectType](../../com.aspose.slides/commandeffecttype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | byte |  |

### getCommandString() {#getCommandString--}
```
public abstract String getCommandString()
```

رشتهٔ فرمان را تعریف می‌کند. خواندنی/نوشتنی String.

**بازگشت:**
java.lang.String
### setCommandString(String value) {#setCommandString-java.lang.String-}
```
public abstract void setCommandString(String value)
```

رشتهٔ فرمان را تعریف می‌کند. خواندنی/نوشتنی String.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | java.lang.String |  |

### getShapeTarget() {#getShapeTarget--}
```
public abstract IShape getShapeTarget()
```

هدف شکل اثر فرمان را تعریف می‌کند. خواندنی/نوشتنی [IShape](../../com.aspose.slides/ishape).

**بازگشت:**
[IShape](../../com.aspose.slides/ishape)
### setShapeTarget(IShape value) {#setShapeTarget-com.aspose.slides.IShape-}
```
public abstract void setShapeTarget(IShape value)
```

هدف شکل اثر فرمان را تعریف می‌کند. خواندنی/نوشتنی [IShape](../../com.aspose.slides/ishape).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | [IShape](../../com.aspose.slides/ishape) |  |