---
title: ConnectorLock
second_title: Aspose.Slides برای اندروید از طریق API جاوا
description: تعیین می‌کند کدام عملیات‌ها در Connector والد غیرفعال هستند.
type: docs
url: /fa/com.aspose.slides/connectorlock/
---
**ارث بری:**
java.lang.Object, [com.aspose.slides.BaseShapeLock](../../com.aspose.slides/baseshapelock)

**تمام رابط‌های پیاده‌سازی شده:**
[com.aspose.slides.IConnectorLock](../../com.aspose.slides/iconnectorlock)
```
public class ConnectorLock extends BaseShapeLock implements IConnectorLock
```

تعیین می‌کند کدام عملیات‌ها در اتصال‌دهنده والد غیرفعال هستند.
## Methods

| Method | Description |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | تعیین می‌کند آیا افزودن این شکل به یک گروه ممنوع است. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | تعیین می‌کند آیا افزودن این شکل به یک گروه ممنوع است. |
| [getSelectLocked()](#getSelectLocked--) | تعیین می‌کند آیا انتخاب این شکل ممنوع است. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | تعیین می‌کند آیا انتخاب این شکل ممنوع است. |
| [getRotateLocked()](#getRotateLocked--) | تعیین می‌کند آیا تغییر زاویه چرخش این شکل ممنوع است. |
| [setRotateLocked(boolean value)](#setRotateLocked-boolean-) | تعیین می‌کند آیا تغییر زاویه چرخش این شکل ممنوع است. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | تعیین می‌کند آیا شکل باید نسبت تصویری را در هنگام تغییر اندازه حفظ کند. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | تعیین می‌کند آیا شکل باید نسبت تصویری را در هنگام تغییر اندازه حفظ کند. |
| [getPositionMove()](#getPositionMove--) | تعیین می‌کند آیا جابجایی این شکل ممنوع است. |
| [setPositionMove(boolean value)](#setPositionMove-boolean-) | تعیین می‌کند آیا جابجایی این شکل ممنوع است. |
| [getSizeLocked()](#getSizeLocked--) | تعیین می‌کند آیا تغییر اندازه این شکل ممنوع است. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | تعیین می‌کند آیا تغییر اندازه این شکل ممنوع است. |
| [getEditPointsLocked()](#getEditPointsLocked--) | تعیین می‌کند آیا تغییر مستقیم خطوط خارجی این شکل ممنوع است. |
| [setEditPointsLocked(boolean value)](#setEditPointsLocked-boolean-) | تعیین می‌کند آیا تغییر مستقیم خطوط خارجی این شکل ممنوع است. |
| [getAdjustHandlesLocked()](#getAdjustHandlesLocked--) | تعیین می‌کند آیا تغییر مقدارهای تنظیمات ممنوع است. |
| [setAdjustHandlesLocked(boolean value)](#setAdjustHandlesLocked-boolean-) | تعیین می‌کند آیا تغییر مقدارهای تنظیمات ممنوع است. |
| [getArrowheadsLocked()](#getArrowheadsLocked--) | تعیین می‌کند آیا تغییر سرهای فلش ممنوح است. |
| [setArrowheadsLocked(boolean value)](#setArrowheadsLocked-boolean-) | تعیین می‌کند آیا تغییر سرهای فلش ممنوح است. |
| [getShapeTypeLocked()](#getShapeTypeLocked--) | تعیین می‌کند آیا تغییر نوع شکل ممنوع است. |
| [setShapeTypeLocked(boolean value)](#setShapeTypeLocked-boolean-) | تعیین می‌کند آیا تغییر نوع شکل ممنوع است. |
### getGroupingLocked() {#getGroupingLocked--}
```
public boolean getGroupingLocked()
```


تعیین می‌کند آیا افزودن این شکل به یک گروه ممنوع است. قابلیت خواندن/نوشتن بولی.

**بازگشت:**
boolean
### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public void setGroupingLocked(boolean value)
```


تعیین می‌کند آیا افزودن این شکل به یک گروه ممنوع است. قابلیت خواندن/نوشتن بولی.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getSelectLocked() {#getSelectLocked--}
```
public boolean getSelectLocked()
```


تعیین می‌کند آیا انتخاب این شکل ممنوع است. قابلیت خواندن/نوشتن بولی.

**بازگشت:**
boolean
### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public void setSelectLocked(boolean value)
```


تعیین می‌کند آیا انتخاب این شکل ممنوع است. قابلیت خواندن/نوشتن بولی.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getRotateLocked() {#getRotateLocked--}
```
public boolean getRotateLocked()
```


تعیین می‌کند آیا تغییر زاویه چرخش این شکل ممنوع است. قابلیت خواندن/نوشتن بولی.

**بازگشت:**
boolean
### setRotateLocked(boolean value) {#setRotateLocked-boolean-}
```
public void setRotateLocked(boolean value)
```


تعیین می‌کند آیا تغییر زاویه چرخش این شکل ممنوع است. قابلیت خواندن/نوشتن بولی.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public boolean getAspectRatioLocked()
```


تعیین می‌کند آیا شکل باید نسبت تصویری را در هنگام تغییر اندازه حفظ کند. قابلیت خواندن/نوشتن بولی.

**بازگشت:**
boolean
### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public void setAspectRatioLocked(boolean value)
```


تعیین می‌کند آیا شکل باید نسبت تصویری را در هنگام تغییر اندازه حفظ کند. قابلیت خواندن/نوشتن بولی.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getPositionMove() {#getPositionMove--}
```
public boolean getPositionMove()
```


تعیین می‌کند آیا جابجایی این شکل ممنوع است. قابلیت خواندن/نوشتن بولی.

**بازگشت:**
boolean
### setPositionMove(boolean value) {#setPositionMove-boolean-}
```
public void setPositionMove(boolean value)
```


تعیین می‌کند آیا جابجایی این شکل ممنوع است. قابلیت خواندن/نوشتن بولی.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getSizeLocked() {#getSizeLocked--}
```
public boolean getSizeLocked()
```


تعیین می‌کند آیا تغییر اندازه این شکل ممنوع است. قابلیت خواندن/نوشتن بولی.

**بازگشت:**
boolean
### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public void setSizeLocked(boolean value)
```


تعیین می‌کند آیا تغییر اندازه این شکل ممنوع است. قابلیت خواندن/نوشتن بولی.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getEditPointsLocked() {#getEditPointsLocked--}
```
public boolean getEditPointsLocked()
```


تعیین می‌کند آیا تغییر مستقیم خطوط خارجی این شکل ممنوع است. قابلیت خواندن/نوشتن بولی.

**بازگشت:**
boolean
### setEditPointsLocked(boolean value) {#setEditPointsLocked-boolean-}
```
public void setEditPointsLocked(boolean value)
```


تعیین می‌کند آیا تغییر مستقیم خطوط خارجی این شکل ممنوع است. قابلیت خواندن/نوشتن بولی.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getAdjustHandlesLocked() {#getAdjustHandlesLocked--}
```
public boolean getAdjustHandlesLocked()
```


تعیین می‌کند آیا تغییر مقدارهای تنظیمات ممنوع است. قابلیت خواندن/نوشتن بولی.

**بازگشت:**
boolean
### setAdjustHandlesLocked(boolean value) {#setAdjustHandlesLocked-boolean-}
```
public void setAdjustHandlesLocked(boolean value)
```


تعیین می‌کند آیا تغییر مقدارهای تنظیمات ممنوع است. قابلیت خواندن/نوشتن بولی.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getArrowheadsLocked() {#getArrowheadsLocked--}
```
public boolean getArrowheadsLocked()
```


تعیین می‌کند آیا تغییر سرهای فلش ممنوح است. قابلیت خواندن/نوشتن بولی.

**بازگشت:**
boolean
### setArrowheadsLocked(boolean value) {#setArrowheadsLocked-boolean-}
```
public void setArrowheadsLocked(boolean value)
```


تعیین می‌کند آیا تغییر سرهای فلش ممنوح است. قابلیت خواندن/نوشتن بولی.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getShapeTypeLocked() {#getShapeTypeLocked--}
```
public boolean getShapeTypeLocked()
```


تعیین می‌کند آیا تغییر نوع شکل ممنوع است. قابلیت خواندن/نوشتن بولی.

**بازگشت:**
boolean
### setShapeTypeLocked(boolean value) {#setShapeTypeLocked-boolean-}
```
public void setShapeTypeLocked(boolean value)
```


تعیین می‌کند آیا تغییر نوع شکل ممنوع است. قابلیت خواندن/نوشتن بولی.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |