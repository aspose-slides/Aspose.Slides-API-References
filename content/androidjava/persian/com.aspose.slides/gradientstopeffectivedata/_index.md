---
title: GradientStopEffectiveData
second_title: Aspose.Slides برای اندروید از طریق مرجع API جاوا
description: شیء غیرقابل تغییر که یک نقطه گرادیان را نمایندگی می‌کند.
type: docs
url: /fa/com.aspose.slides/gradientstopeffectivedata/
---
**وراثت:**  
java.lang.Object

**همه رابط‌های پیاده‌سازی‌شده:**  
com.aspose.slides.IEffectiveData, [com.aspose.slides.IGradientStopEffectiveData](../../com.aspose.slides/igradientstopeffectivedata)
```
public class GradientStopEffectiveData implements IEffectiveData, IGradientStopEffectiveData
```

شیء غیرقابل تغییر که یک نقطه گرادیان را نمایندگی می‌کند.

## متدها

| متد | توضیح |
| --- | --- |
| [getPosition()](#getPosition--) | موقعیت (0..1) یک نقطه گرادیان را باز می‌گرداند. |
| [getColor()](#getColor--) | رنگ یک نقطه گرادیان را باز می‌گرداند. |
| [equals(Object obj)](#equals-java.lang.Object-) | تعیین می‌کند آیا [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) مشخص شده برابر با [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) فعلی است یا خیر. |
| [hashCode()](#hashCode--) |  |

### getPosition() {#getPosition--}
```
public final float getPosition()
```

موقعیت (0..1) یک نقطه گرادیان را باز می‌گرداند. فقط-خواندنی float.

**بازگشت:**  
float

### getColor() {#getColor--}
```
public final Integer getColor()
```

رنگ یک نقطه گرادیان را باز می‌گرداند. فقط-خواندنی java.lang.Integer.

**بازگشت:**  
java.lang.Integer

### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```

تعیین می‌کند آیا [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) مشخص شده برابر با [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) فعلی است یا خیر.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| obj | java.lang.Object | [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) برای مقایسه. |

**بازگشت:**  
boolean - true اگر اشیا برابر باشند؛ در غیر این صورت false.

### hashCode() {#hashCode--}
```
public int hashCode()
```

**بازگشت:**  
int