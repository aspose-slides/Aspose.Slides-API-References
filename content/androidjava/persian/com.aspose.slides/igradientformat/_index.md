---
title: IGradientFormat
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: نمایش یک فرمت گرادیان.
type: docs
url: /fa/com.aspose.slides/igradientformat/
---
**همهٔ رابط‌های پیاده‌سازی شده:**
[com.aspose.slides.IFillParamSource](../../com.aspose.slides/ifillparamsource)
```
public interface IGradientFormat extends IFillParamSource
```

نمایش یک فرمت گرادیان.
## متدها

| Method | Description |
| --- | --- |
| [getTileFlip()](#getTileFlip--) | مقدار یا تنظیم حالت چرخش برای یک گرادیان. |
| [setTileFlip(int value)](#setTileFlip-int-) | مقدار یا تنظیم حالت چرخش برای یک گرادیان. |
| [getGradientDirection()](#getGradientDirection--) | مقدار یا تنظیم سبک یک گرادیان. |
| [setGradientDirection(int value)](#setGradientDirection-int-) | مقدار یا تنظیم سبک یک گرادیان. |
| [getLinearGradientAngle()](#getLinearGradientAngle--) | مقدار یا تنظیم زاویهٔ یک گرادیان. |
| [setLinearGradientAngle(float value)](#setLinearGradientAngle-float-) | مقدار یا تنظیم زاویهٔ یک گرادیان. |
| [getLinearGradientScaled()](#getLinearGradientScaled--) | تعیین می‌کند آیا یک گرادیان مقیاس‌بندی شده است یا خیر. |
| [setLinearGradientScaled(byte value)](#setLinearGradientScaled-byte-) | تعیین می‌کند آیا یک گرادیان مقیاس‌بندی شده است یا خیر. |
| [getGradientShape()](#getGradientShape--) | مقدار یا تنظیم شکل یک گرادیان. |
| [setGradientShape(byte value)](#setGradientShape-byte-) | مقدار یا تنظیم شکل یک گرادیان. |
| [getGradientStops()](#getGradientStops--) | مجموعه‌ای از نقاط توقف گرادیان را باز می‌گرداند. |
### getTileFlip() {#getTileFlip--}
```
public abstract int getTileFlip()
```

مقدار یا تنظیم حالت چرخش برای یک گرادیان. خواندنی/نوشتنی [TileFlip](../../com.aspose.slides/tileflip).

**باز می‌گرداند:**
int
### setTileFlip(int value) {#setTileFlip-int-}
```
public abstract void setTileFlip(int value)
```

مقدار یا تنظیم حالت چرخش برای یک گرادیان. خواندنی/نوشتنی [TileFlip](../../com.aspose.slides/tileflip).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |
### getGradientDirection() {#getGradientDirection--}
```
public abstract int getGradientDirection()
```

مقدار یا تنظیم سبک یک گرادیان. خواندنی/نوشتنی [GradientDirection](../../com.aspose.slides/gradientdirection).

**باز می‌گرداند:**
int
### setGradientDirection(int value) {#setGradientDirection-int-}
```
public abstract void setGradientDirection(int value)
```

مقدار یا تنظیم سبک یک گرادیان. خواندنی/نوشتنی [GradientDirection](../../com.aspose.slides/gradientdirection).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |
### getLinearGradientAngle() {#getLinearGradientAngle--}
```
public abstract float getLinearGradientAngle()
```

مقدار یا تنظیم زاویهٔ یک گرادیان. خواندنی/نوشتنی float.

**باز می‌گرداند:**
float
### setLinearGradientAngle(float value) {#setLinearGradientAngle-float-}
```
public abstract void setLinearGradientAngle(float value)
```

مقدار یا تنظیم زاویهٔ یک گرادیان. خواندنی/نوشتنی float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |
### getLinearGradientScaled() {#getLinearGradientScaled--}
```
public abstract byte getLinearGradientScaled()
```

تعیین می‌کند آیا یک گرادیان مقیاس‌بندی شده است یا خیر. خواندنی/نوشتنی [NullableBool](../../com.aspose.slides/nullablebool).

**باز می‌گرداند:**
byte
### setLinearGradientScaled(byte value) {#setLinearGradientScaled-byte-}
```
public abstract void setLinearGradientScaled(byte value)
```

تعیین می‌کند آیا یک گرادیان مقیاس‌بندی شده است یا خیر. خواندنی/نوشتنی [NullableBool](../../com.aspose.slides/nullablebool).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | byte |  |
### getGradientShape() {#getGradientShape--}
```
public abstract byte getGradientShape()
```

مقدار یا تنظیم شکل یک گرادیان. خواندنی/نوشتنی [GradientShape](../../com.aspose.slides/gradientshape).

**باز می‌گرداند:**
byte
### setGradientShape(byte value) {#setGradientShape-byte-}
```
public abstract void setGradientShape(byte value)
```

مقدار یا تنظیم شکل یک گرادیان. خواندنی/نوشتنی [GradientShape](../../com.aspose.slides/gradientshape).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | byte |  |
### getGradientStops() {#getGradientStops--}
```
public abstract IGradientStopCollection getGradientStops()
```

مجموعه‌ای از نقاط توقف گرادیان را باز می‌گرداند. فقط-خواندنی [IGradientStopCollection](../../com.aspose.slides/igradientstopcollection).

**باز می‌گرداند:**
[IGradientStopCollection](../../com.aspose.slides/igradientstopcollection)