---
title: BehaviorFactory
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: اجازه می‌دهد تا افکت‌های انیمیشن ایجاد شود
type: docs
url: /fa/com.aspose.slides/behaviorfactory/
---
**وراثت:**
java.lang.Object

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IBehaviorFactory](../../com.aspose.slides/ibehaviorfactory)
```
public class BehaviorFactory implements IBehaviorFactory
```

اجازه می‌دهد تا افکت‌های انیمیشن ایجاد شود

--------------------

برای سازگاری با COM.
## سازنده‌ها

| سازنده | توضیح |
| --- | --- |
| [BehaviorFactory()](#BehaviorFactory--) |  |
## متدها

| متد | توضیح |
| --- | --- |
| [createColorEffect()](#createColorEffect--) | یک افکت رنگی ایجاد می‌کند. |
| [createCommandEffect()](#createCommandEffect--) | یک افکت دستوری ایجاد می‌کند. |
| [createFilterEffect()](#createFilterEffect--) | یک افکت فیلتر ایجاد می‌کند. |
| [createMotionEffect()](#createMotionEffect--) | یک افکت حرکتی ایجاد می‌کند. |
| [createPropertyEffect()](#createPropertyEffect--) | یک افکت ویژگی ایجاد می‌کند. |
| [createRotationEffect()](#createRotationEffect--) | یک افکت چرخش ایجاد می‌کند. |
| [createScaleEffect()](#createScaleEffect--) | یک افکت مقیاس ایجاد می‌کند. |
| [createSetEffect()](#createSetEffect--) | یک افکت مجموعه ایجاد می‌کند. |
### BehaviorFactory() {#BehaviorFactory--}
```
public BehaviorFactory()
```

### createColorEffect() {#createColorEffect--}
```
public final IColorEffect createColorEffect()
```

یک افکت رنگی ایجاد می‌کند.

**باز می‌گردد:**
[IColorEffect](../../com.aspose.slides/icoloreffect) - افکت رنگ
### createCommandEffect() {#createCommandEffect--}
```
public final ICommandEffect createCommandEffect()
```

یک افکت دستوری ایجاد می‌کند.

**باز می‌گردد:**
[ICommandEffect](../../com.aspose.slides/icommandeffect) - افکت دستوری
### createFilterEffect() {#createFilterEffect--}
```
public final IFilterEffect createFilterEffect()
```

یک افکت فیلتر ایجاد می‌کند.

**باز می‌گردد:**
[IFilterEffect](../../com.aspose.slides/ifiltereffect) - افکت فیلتر
### createMotionEffect() {#createMotionEffect--}
```
public final IMotionEffect createMotionEffect()
```

یک افکت حرکتی ایجاد می‌کند.

**باز می‌گردد:**
[IMotionEffect](../../com.aspose.slides/imotioneffect) - افکت حرکتی
### createPropertyEffect() {#createPropertyEffect--}
```
public final IPropertyEffect createPropertyEffect()
```

یک افکت ویژگی ایجاد می‌کند.

**باز می‌گردد:**
[IPropertyEffect](../../com.aspose.slides/ipropertyeffect) - افکت ویژگی
### createRotationEffect() {#createRotationEffect--}
```
public final IRotationEffect createRotationEffect()
```

یک افکت چرخش ایجاد می‌کند.

**باز می‌گردد:**
[IRotationEffect](../../com.aspose.slides/irotationeffect) - افکت چرخش
### createScaleEffect() {#createScaleEffect--}
```
public final IScaleEffect createScaleEffect()
```

یک افکت مقیاس ایجاد می‌کند.

**باز می‌گردد:**
[IScaleEffect](../../com.aspose.slides/iscaleeffect) - افکت مقیاس
### createSetEffect() {#createSetEffect--}
```
public final ISetEffect createSetEffect()
```

یک افکت مجموعه ایجاد می‌کند.

**باز می‌گردد:**
[ISetEffect](../../com.aspose.slides/iseteffect) - افکت مجموعه