---
title: IMotionCmdPath
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: نمایش یک فرمان از یک مسیر.
type: docs
url: /fa/com.aspose.slides/imotioncmdpath/
---```
public interface IMotionCmdPath
```

نمایش یک فرمان از یک مسیر.

## متدها

| Method | Description |
| --- | --- |
| [getPoints()](#getPoints--) | نقاط فرمان را مشخص می‌کند. |
| [setPoints(PointF[] value)](#setPoints-android.graphics.PointF---) | نقاط فرمان را مشخص می‌کند. |
| [getCommandType()](#getCommandType--) | نوع فرمان را مشخص می‌کند. |
| [setCommandType(int value)](#setCommandType-int-) | نوع فرمان را مشخص می‌کند. |
| [isRelative()](#isRelative--) | تعیین می‌کند که مختصات فرمان نسبی هستند یا نه. |
| [setRelative(boolean value)](#setRelative-boolean-) | تعیین می‌کند که مختصات فرمان نسبی هستند یا نه. |
| [getPointsType()](#getPointsType--) | نوع نقاط فرمان را مشخص می‌کند خواندن/نوشتن [MotionPathPointsType](../../com.aspose.slides/motionpathpointstype). |
| [setPointsType(int value)](#setPointsType-int-) | نوع نقاط فرمان را مشخص می‌کند خواندن/نوشتن [MotionPathPointsType](../../com.aspose.slides/motionpathpointstype). |

### getPoints() {#getPoints--}
```
public abstract PointF[] getPoints()
```

نقاط فرمان را مشخص می‌کند. خواندن/نوشتن android.graphics.PointF[].

**بازگشت:**
android.graphics.PointF[]

### setPoints(PointF[] value) {#setPoints-android.graphics.PointF---}
```
public abstract void setPoints(PointF[] value)
```

نقاط فرمان را مشخص می‌کند. خواندن/نوشتن android.graphics.PointF[].

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | android.graphics.PointF[] |  |

### getCommandType() {#getCommandType--}
```
public abstract int getCommandType()
```

نوع فرمان را مشخص می‌کند. خواندن/نوشتن [MotionCommandPathType](../../com.aspose.slides/motioncommandpathtype).

**بازگشت:**
int

### setCommandType(int value) {#setCommandType-int-}
```
public abstract void setCommandType(int value)
```

نوع فرمان را مشخص می‌کند. خواندن/نوشتن [MotionCommandPathType](../../com.aspose.slides/motioncommandpathtype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### isRelative() {#isRelative--}
```
public abstract boolean isRelative()
```

تعیین می‌کند که مختصات فرمان نسبی هستند یا نه. خواندن/نوشتن boolean.

**بازگشت:**
boolean

### setRelative(boolean value) {#setRelative-boolean-}
```
public abstract void setRelative(boolean value)
```

تعیین می‌کند که مختصات فرمان نسبی هستند یا نه. خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getPointsType() {#getPointsType--}
```
public abstract int getPointsType()
```

نوع نقاط فرمان را مشخص می‌کند خواندن/نوشتن [MotionPathPointsType](../../com.aspose.slides/motionpathpointstype).

**بازگشت:**
int

### setPointsType(int value) {#setPointsType-int-}
```
public abstract void setPointsType(int value)
```

نوع نقاط فرمان را مشخص می‌کند خواندن/نوشتن [MotionPathPointsType](../../com.aspose.slides/motionpathpointstype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |