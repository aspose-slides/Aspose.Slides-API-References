---
title: FormatScheme
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: قالب‌های تعریف‌شده توسط تم را برای شکل‌ها ذخیره می‌کند.
type: docs
url: /fa/com.aspose.slides/formatscheme/
---
**ارث‌بری:**
java.lang.Object

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IFormatScheme](../../com.aspose.slides/iformatscheme), com.aspose.slides.IDOMObject
```
public class FormatScheme implements IFormatScheme, IDOMObject
```

قالب‌های تعریف‌شده توسط تم برای شکل‌ها را ذخیره می‌کند.
## متدها

| متد | توضیح |
| --- | --- |
| [getFillStyles()](#getFillStyles--) | یک مجموعه از سبک‌های پر شده تعریف‌شده توسط تم را برمی‌گرداند. |
| [getLineStyles()](#getLineStyles--) | یک مجموعه از سبک‌های خط تعریف‌شده توسط تم را برمی‌گرداند. |
| [getEffectStyles()](#getEffectStyles--) | یک مجموعه از سبک‌های افکت تعریف‌شده توسط تم را برمی‌گرداند. |
| [getBackgroundFillStyles()](#getBackgroundFillStyles--) | یک مجموعه از سبک‌های پر پس‌زمینه تعریف‌شده توسط تم را برمی‌گرداند. |
| [getSlide()](#getSlide--) | اسلاید والد را برمی‌گرداند. |
| [getPresentation()](#getPresentation--) | ارائهٔ والد را برمی‌گرداند. |
| [getParent_Immediate()](#getParent-Immediate--) |  |
### getFillStyles() {#getFillStyles--}
```
public final IFillFormatCollection getFillStyles()
```

یک مجموعه از سبک‌های پر شده تعریف‌شده توسط تم را برمی‌گرداند. فقط-خواندنی [IFillFormatCollection](../../com.aspose.slides/ifillformatcollection).

**بازگشت:**
[IFillFormatCollection](../../com.aspose.slides/ifillformatcollection)
### getLineStyles() {#getLineStyles--}
```
public final ILineFormatCollection getLineStyles()
```

یک مجموعه از سبک‌های خط تعریف‌شده توسط تم را برمی‌گرداند. فقط-خواندنی [ILineFormatCollection](../../com.aspose.slides/ilineformatcollection).

**بازگشت:**
[ILineFormatCollection](../../com.aspose.slides/ilineformatcollection)
### getEffectStyles() {#getEffectStyles--}
```
public final IEffectStyleCollection getEffectStyles()
```

یک مجموعه از سبک‌های افکت تعریف‌شده توسط تم را برمی‌گرداند. فقط-خواندنی [IEffectStyleCollection](../../com.aspose.slides/ieffectstylecollection).

**بازگشت:**
[IEffectStyleCollection](../../com.aspose.slides/ieffectstylecollection)
### getBackgroundFillStyles() {#getBackgroundFillStyles--}
```
public final IFillFormatCollection getBackgroundFillStyles()
```

یک مجموعه از سبک‌های پر پس‌زمینه تعریف‌شده توسط تم را برمی‌گرداند. فقط-خواندنی [IFillFormatCollection](../../com.aspose.slides/ifillformatcollection).

**بازگشت:**
[IFillFormatCollection](../../com.aspose.slides/ifillformatcollection)
### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

اسلاید والد را برمی‌گرداند. فقط-خواندنی [IBaseSlide](../../com.aspose.slides/ibaseslide).

**بازگشت:**
[IBaseSlide](../../com.aspose.slides/ibaseslide)
### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

ارائهٔ والد را برمی‌گرداند. فقط-خواندنی [IPresentation](../../com.aspose.slides/ipresentation).

**بازگشت:**
[IPresentation](../../com.aspose.slides/ipresentation)
### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

یک شیء Parent_Immediate را برمی‌گرداند. فقط-خواندنی IDOMObject.

**بازگشت:**
com.aspose.slides.IDOMObject