---
title: IScaleEffect
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: نمایش اثر مقیاس انیمیشن.
type: docs
url: /fa/com.aspose.slides/iscaleeffect/
---
**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IScaleEffect extends IBehavior
```

نمایش اثر مقیاس انیمیشن.
## متدها

| متد | توضیح |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | تعیین می‌کند که آیا محتوا باید بزرگ‌نمایی شود. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | تعیین می‌کند که آیا محتوا باید بزرگ‌نمایی شود. |
| [getFrom()](#getFrom--) | مختصات x/y را برای شروع انیمیشن مشخص می‌کند (در درصد). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | مختصات x/y را برای شروع انیمیشن مشخص می‌کند (در درصد). |
| [getTo()](#getTo--) | مکان هدف برای اثر مقیاس‌گذاری انیمیشن را مشخص می‌کند (در درصد). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | مکان هدف برای اثر مقیاس‌گذاری انیمیشن را مشخص می‌کند (در درصد). |
| [getBy()](#getBy--) | مقدار جابجایی نسبی برای انیمیشن را توصیف می‌کند (در درصد). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | مقدار جابجایی نسبی برای انیمیشن را توصیف می‌کند (در درصد). |
### getZoomContent() {#getZoomContent--}
```
public abstract byte getZoomContent()
```

تعیین می‌کند که آیا محتوا باید بزرگ‌نمایی شود. خواندنی/نوشتنی [NullableBool](../../com.aspose.slides/nullablebool).

**بازگشت:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public abstract void setZoomContent(byte value)
```

تعیین می‌کند که آیا محتوا باید بزرگ‌نمایی شود. خواندنی/نوشتنی [NullableBool](../../com.aspose.slides/nullablebool).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public abstract PointF getFrom()
```

مختصات x/y را برای شروع انیمیشن مشخص می‌کند (در درصد). خواندنی/نوشتنی android.graphics.PointF.

**بازگشت:**
android.graphics.PointF
### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public abstract void setFrom(PointF value)
```

مختصات x/y را برای شروع انیمیشن مشخص می‌کند (در درصد). خواندنی/نوشتنی android.graphics.PointF.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public abstract PointF getTo()
```

مکان هدف برای اثر مقیاس‌گذاری انیمیشن را مشخص می‌کند (در درصد). خواندنی/نوشتنی android.graphics.PointF.

**بازگشت:**
android.graphics.PointF
### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public abstract void setTo(PointF value)
```

مکان هدف برای اثر مقیاس‌گذاری انیمیشن را مشخص می‌کند (در درصد). خواندنی/نوشتنی android.graphics.PointF.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public abstract PointF getBy()
```

مقدار جابجایی نسبی برای انیمیشن را توصیف می‌کند (در درصد). خواندنی/نوشتنی android.graphics.PointF.

**بازگشت:**
android.graphics.PointF
### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public abstract void setBy(PointF value)
```

مقدار جابجایی نسبی برای انیمیشن را توصیف می‌کند (در درصد). خواندنی/نوشتنی android.graphics.PointF.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | android.graphics.PointF |  |