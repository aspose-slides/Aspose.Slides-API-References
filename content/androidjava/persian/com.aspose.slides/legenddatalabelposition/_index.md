---
title: LegendDataLabelPosition
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: موقعیت برچسب‌های داده را تعیین می‌کند.
type: docs
url: /fa/com.aspose.slides/legenddatalabelposition/
---
**وراثت:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class LegendDataLabelPosition extends System.Enum
```

موقعیت برچسب‌های داده را تعیین می‌کند.
## فیلدها

| Field | توضیح |
| --- | --- |
| [NotDefined](#NotDefined) | موقعیت تعریف نشده است. |
| [Bottom](#Bottom) | مشخص می‌کند که برچسب‌های داده باید زیر نشانگر داده نمایش داده شوند. |
| [BestFit](#BestFit) | مشخص می‌کند که برچسب‌های داده باید در بهترین موقعیت نمایش داده شوند. |
| [Center](#Center) | مشخص می‌کند که برچسب‌های داده باید به صورت مرکز بر روی نشانگر داده نمایش داده شوند. |
| [InsideBase](#InsideBase) | مشخص می‌کند که برچسب‌های داده باید در پایه نشانگر داده داخلی نمایش داده شوند. |
| [InsideEnd](#InsideEnd) | مشخص می‌کند که برچسب‌های داده باید در انتهای نشانگر داده داخلی نمایش داده شوند. |
| [Left](#Left) | مشخص می‌کند که برچسب‌های داده باید در سمت چپ نشانگر داده نمایش داده شوند. |
| [OutsideEnd](#OutsideEnd) | مشخص می‌کند که برچسب‌های داده باید خارج از انتهای نشانگر داده نمایش داده شوند. |
| [Right](#Right) | مشخص می‌کند که برچسب‌های داده باید در سمت راست نشانگر داده نمایش داده شوند. |
| [Top](#Top) | مشخص می‌کند که برچسب‌های داده باید بالای نشانگر داده نمایش داده شوند. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```

موقعیت تعریف نشده است.

### Bottom {#Bottom}
```
public static final int Bottom
```

مشخص می‌کند که برچسب‌های داده باید زیر نشانگر داده نمایش داده شوند.

### BestFit {#BestFit}
```
public static final int BestFit
```

مشخص می‌کند که برچسب‌های داده باید در بهترین موقعیت نمایش داده شوند.

### Center {#Center}
```
public static final int Center
```

مشخص می‌کند که برچسب‌های داده باید به صورت مرکز بر روی نشانگر داده نمایش داده شوند.

### InsideBase {#InsideBase}
```
public static final int InsideBase
```

مشخص می‌کند که برچسب‌های داده باید در پایه نشانگر داده داخلی نمایش داده شوند.

### InsideEnd {#InsideEnd}
```
public static final int InsideEnd
```

مشخص می‌کند که برچسب‌های داده باید در انتهای نشانگر داده داخلی نمایش داده شوند.

### Left {#Left}
```
public static final int Left
```

مشخص می‌کند که برچسب‌های داده باید در سمت چپ نشانگر داده نمایش داده شوند.

### OutsideEnd {#OutsideEnd}
```
public static final int OutsideEnd
```

مشخص می‌کند که برچسب‌های داده باید خارج از انتهای نشانگر داده نمایش داده شوند.

### Right {#Right}
```
public static final int Right
```

مشخص می‌کند که برچسب‌های داده باید در سمت راست نشانگر داده نمایش داده شوند.

### Top {#Top}
```
public static final int Top
```

مشخص می‌کند که برچسب‌های داده باید بالای نشانگر داده نمایش داده شوند.