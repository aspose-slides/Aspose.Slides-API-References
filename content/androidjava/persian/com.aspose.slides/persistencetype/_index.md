---
title: PersistenceType
second_title: Aspose.Slides برای اندروید از طریق مرجع API جاوا
description: روشی را که برای ذخیره‌سازی ویژگی‌های کنترل ActiveX استفاده می‌شود، مشخص می‌کند.
type: docs
url: /fa/com.aspose.slides/persistencetype/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class PersistenceType extends System.Enum
```

روشی را که برای ذخیره‌سازی ویژگی‌های کنترل ActiveX استفاده می‌شود، مشخص می‌کند.

## فیلدها

| فیلد | توضیح |
| --- | --- |
| [NotDefined](#NotDefined) | شناسه Persistence مشخص نشده است. |
| [PersistPropertyBag](#PersistPropertyBag) | مشخص می‌کند که کنترل ActiveX با استفاده از persistence مبتنی بر property-bag ذخیره می‌شود. |
| [PersistStream](#PersistStream) | مشخص می‌کند که کنترل ActiveX با استفاده از persistence مبتنی بر stream که از مقداردهی اولیه به وضعیت پیش‌فرض پشتیبانی نمی‌کند، ذخیره می‌شود. |
| [PersistStreamInit](#PersistStreamInit) | مشخص می‌کند که کنترل ActiveX با استفاده از persistence مبتنی بر stream که از مقداردهی اولیه به وضعیت پیش‌فرض پشتیبانی می‌کند، ذخیره می‌شود. |
| [PersistStorage](#PersistStorage) | مشخص می‌کند که کنترل ActiveX با استفاده از persistence مبتنی بر storage ذخیره می‌شود. |

### NotDefined {#NotDefined}
```
public static final int NotDefined
```

شناسه Persistence مشخص نشده است.

### PersistPropertyBag {#PersistPropertyBag}
```
public static final int PersistPropertyBag
```

مشخص می‌کند که کنترل ActiveX با استفاده از persistence مبتنی بر property-bag ذخیره می‌شود. persistence مبتنی بر property-bag کنترل ActiveX را با مجموعه‌ای از جفت‌های نام و مقدار که داده‌های ذخیره‌شده توسط کنترل ActiveX را مشخص می‌کنند، ذخیره می‌کند.

### PersistStream {#PersistStream}
```
public static final int PersistStream
```

مشخص می‌کند که کنترل ActiveX با استفاده از persistence مبتنی بر stream که از مقداردهی اولیه به وضعیت پیش‌فرض پشتیبانی نمی‌کند، ذخیره می‌شود.

### PersistStreamInit {#PersistStreamInit}
```
public static final int PersistStreamInit
```

مشخص می‌کند که کنترل ActiveX با استفاده از persistence مبتنی بر stream که از مقداردهی اولیه به وضعیت پیش‌فرض پشتیبانی می‌کند، ذخیره می‌شود.

### PersistStorage {#PersistStorage}
```
public static final int PersistStorage
```

مشخص می‌کند که کنترل ActiveX با استفاده از persistence مبتنی بر storage ذخیره می‌شود.