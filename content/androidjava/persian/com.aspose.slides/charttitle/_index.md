---
title: ChartTitle
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: خواص عنوان نمودار را نمایان می‌کند.
type: docs
url: /fa/com.aspose.slides/charttitle/
---
**وراثت:**  
java.lang.Object

**تمام رابط‌های پیاده‌سازی‌شده:**  
[com.aspose.slides.IChartTitle](../../com.aspose.slides/icharttitle), com.aspose.slides.IDOMObject  
```
public class ChartTitle implements IChartTitle, IDOMObject
```

خواص عنوان نمودار را نمایش می‌دهد.

## متدها

| متد | توضیح |
| --- | --- |
| [getX()](#getX--) | مختصات x یک عنوان را به عنوان کسری از عرض نمودار برمی‌گرداند یا تنظیم می‌کند. |
| [setX(float value)](#setX-float-) | مختصات x یک عنوان را به عنوان کسری از عرض نمودار برمی‌گرداند یا تنظیم می‌کند. |
| [getY()](#getY--) | مختصات y یک عنوان را به عنوان کسری از ارتفاع نمودار برمی‌گرداند یا تنظیم می‌کند. |
| [setY(float value)](#setY-float-) | مختصات y یک عنوان را به عنوان کسری از ارتفاع نمودار برمی‌گرداند یا تنظیم می‌کند. |
| [getWidth()](#getWidth--) | عرض یک عنوان را به عنوان کسری از عرض نمودار برمی‌گرداند یا تنظیم می‌کند. |
| [setWidth(float value)](#setWidth-float-) | عرض یک عنوان را به عنوان کسری از عرض نمودار برمی‌گرداند یا تنظیم می‌کند. |
| [getHeight()](#getHeight--) | ارتفاع یک عنوان را به عنوان کسری از ارتفاع نمودار برمی‌گرداند یا تنظیم می‌کند. |
| [setHeight(float value)](#setHeight-float-) | ارتفاع یک عنوان را به عنوان کسری از ارتفاع نمودار برمی‌گرداند یا تنظیم می‌کند. |
| [getRight()](#getRight--) | راست. |
| [getBottom()](#getBottom--) | پایین. |
| [getOverlay()](#getOverlay--) | تعیین می‌کند آیا عناصر دیگر نمودار اجازه همپوشانی با عنوان را داشته باشند یا نه. |
| [setOverlay(boolean value)](#setOverlay-boolean-) | تعیین می‌کند آیا عناصر دیگر نمودار اجازه همپوشانی با عنوان را داشته باشند یا نه. |
| [getFormat()](#getFormat--) | پر، خط و سبک‌های افکتی عنوان را باز می‌گرداند. |
| [addTextFrameForOverriding(String text)](#addTextFrameForOverriding-java.lang.String-) | TextFrameForOverriding را با متن پارامتر "text" مقداردهی اولیه می‌کند. |
| [getTextFrameForOverriding()](#getTextFrameForOverriding--) | می‌تواند شامل متنی با قالب‌بندی غنی باشد. |
| [getTextFormat()](#getTextFormat--) | قالب متن را باز می‌گرداند. |
| [getActualX()](#getActualX--) | مکان واقعی x (چپ) عنصر نمودار را نسبت به گوشه بالای چپ نمودار مشخص می‌کند. |
| [getActualY()](#getActualY--) | بالای واقعی عنصر نمودار را نسبت به گوشه بالای چپ نمودار مشخص می‌کند. |
| [getActualWidth()](#getActualWidth--) | عرض واقعی عنصر نمودار را مشخص می‌کند. |
| [getActualHeight()](#getActualHeight--) | ارتفاع واقعی عنصر نمودار را مشخص می‌کند. |
| [getParent_Immediate()](#getParent-Immediate--) |  |
| [getChart()](#getChart--) | نمودار والد را باز می‌گرداند. |
| [getSlide()](#getSlide--) | اسلاید والد FillFormat را باز می‌گرداند. |
| [getPresentation()](#getPresentation--) | ارائه والد FillFormat را باز می‌گرداند. |

### getX() {#getX--}
```
public final float getX()
```

مختصات x یک عنوان را به عنوان کسری از عرض نمودار برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن float.

**باز می‌گرداند:**
float

### setX(float value) {#setX-float-}
```
public final void setX(float value)
```

مختصات x یک عنوان را به عنوان کسری از عرض نمودار برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getY() {#getY--}
```
public final float getY()
```

مختصات y یک عنوان را به عنوان کسری از ارتفاع نمودار برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن float.

**باز می‌گرداند:**
float

### setY(float value) {#setY-float-}
```
public final void setY(float value)
```

مختصات y یک عنوان را به عنوان کسری از ارتفاع نمودار برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getWidth() {#getWidth--}
```
public final float getWidth()
```

عرض یک عنوان را به عنوان کسری از عرض نمودار برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن float.

**باز می‌گرداند:**
float

### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```

عرض یک عنوان را به عنوان کسری از عرض نمودار برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getHeight() {#getHeight--}
```
public final float getHeight()
```

ارتفاع یک عنوان را به عنوان کسری از ارتفاع نمودار برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن float.

**باز می‌گرداند:**
float

### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```

ارتفاع یک عنوان را به عنوان کسری از ارتفاع نمودار برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getRight() {#getRight--}
```
public final float getRight()
```

راست. فقط خواندنی float.

**باز می‌گرداند:**
float

### getBottom() {#getBottom--}
```
public final float getBottom()
```

پایین. فقط خواندنی float.

**باز می‌گرداند:**
float

### getOverlay() {#getOverlay--}
```
public final boolean getOverlay()
```

تعیین می‌کند آیا عناصر دیگر نمودار اجازه همپوشانی با عنوان را داشته باشند. قابل خواندن/نوشتن boolean.

**باز می‌گرداند:**
boolean

### setOverlay(boolean value) {#setOverlay-boolean-}
```
public final void setOverlay(boolean value)
```

تعیین می‌کند آیا عناصر دیگر نمودار اجازه همپوشانی با عنوان را داشته باشند. قابل خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

پر، خط و سبک‌های افکتی عنوان را باز می‌گرداند. فقط خواندنی [IFormat](../../com.aspose.slides/iformat).

**باز می‌گرداند:**
[IFormat](../../com.aspose.slides/iformat)

### addTextFrameForOverriding(String text) {#addTextFrameForOverriding-java.lang.String-}
```
public final ITextFrame addTextFrameForOverriding(String text)
```

TextFrameForOverriding را با متن پارامتر "text" مقداردهی اولیه می‌کند. اگر TextFrameForOverriding از قبل مقداردهی شده باشد، متن آن را به سادگی تغییر می‌دهد.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| text | java.lang.String | متن برای یک TextFrameForOverriding جدید. |

**باز می‌گرداند:**
[ITextFrame](../../com.aspose.slides/itextframe)

### getTextFrameForOverriding() {#getTextFrameForOverriding--}
```
public final ITextFrame getTextFrameForOverriding()
```

می‌تواند شامل متنی با قالب‌بندی غنی باشد. اگر این ویژگی مقدار null نداشته باشد، این مقدار متنی قالب‌بندی‌شده متن تولید خودکار را بازنویسی می‌کند. متن تولید خودکار یک ویژگی ضمنی ... متن تولید خودکار با ویژگی IFormattedTextContainer.TextFormat قالب‌بندی می‌شود. فقط خواندنی [ITextFrame](../../com.aspose.slides/itextframe).

**باز می‌گرداند:**
[ITextFrame](../../com.aspose.slides/itextframe)

### getTextFormat() {#getTextFormat--}
```
public final IChartTextFormat getTextFormat()
```

قالب متن را باز می‌گرداند. فقط خواندنی [IChartTextFormat](../../com.aspose.slides/icharttextformat).

**باز می‌گرداند:**
[IChartTextFormat](../../com.aspose.slides/icharttextformat)

### getActualX() {#getActualX--}
```
public final float getActualX()
```

مکان واقعی x (چپ) عنصر نمودار را نسبت به گوشه بالای چپ نمودار مشخص می‌کند. قبل از دریافت مقادیر واقعی، متد IChart.validateChartLayout() را صدا بزنید. فقط خواندنی float.

**باز می‌گرداند:**
float

### getActualY() {#getActualY--}
```
public final float getActualY()
```

بالای واقعی عنصر نمودار را نسبت به گوشه بالای چپ نمودار مشخص می‌کند. قبل از دریافت مقادیر واقعی، متد IChart.validateChartLayout() را صدا بزنید. فقط خواندنی float.

**باز می‌گرداند:**
float

### getActualWidth() {#getActualWidth--}
```
public final float getActualWidth()
```

عرض واقعی عنصر نمودار را مشخص می‌کند. قبل از دریافت مقادیر واقعی، متد IChart.validateChartLayout() را صدا بزنید. فقط خواندنی float.

**باز می‌گرداند:**
float

### getActualHeight() {#getActualHeight--}
```
public final float getActualHeight()
```

ارتفاع واقعی عنصر نمودار را مشخص می‌کند. قبل از دریافت مقادیر واقعی، متد IChart.validateChartLayout() را صدا بزنید. فقط خواندنی float.

**باز می‌گرداند:**
float

### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

شی Parent_Immediate را باز می‌گرداند. فقط خواندنی IDOMObject.

**باز می‌گرداند:**
com.aspose.slides.IDOMObject

### getChart() {#getChart--}
```
public final IChart getChart()
```

نمودار والد را باز می‌گرداند. فقط خواندنی [IChart](../../com.aspose.slides/ichart).

**باز می‌گرداند:**
[IChart](../../com.aspose.slides/ichart)

### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

اسلاید والد FillFormat را باز می‌گرداند. فقط خواندنی [BaseSlide](../../com.aspose.slides/baseslide).

**باز می‌گرداند:**
[IBaseSlide](../../com.aspose.slides/ibaseslide)

### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

ارائه والد FillFormat را باز می‌گرداند. فقط خواندنی [IPresentation](../../com.aspose.slides/ipresentation).

**باز می‌گرداند:**
[IPresentation](../../com.aspose.slides/ipresentation)