---
title: ShapeAdjustmentType
second_title: Aspose.Slides برای اندروید از طریق مرجع API جاوا
description: انواع مختلف مقادیر تنظیم شکل را مشخص می‌کند.
type: docs
url: /fa/com.aspose.slides/shapeadjustmenttype/
---
**وراثت:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class ShapeAdjustmentType extends System.Enum
```

انواع مختلف مقادیر تنظیم شکل را مشخص می‌کند.
## فیلدها

| Field | Description |
| --- | --- |
| [Custom](#Custom) | نوع سفارشی (تنظیم ناشناخته) |
| [CornerSize](#CornerSize) | اندازهٔ گوشه‌ها را کنترل می‌کند. |
| [LeftUpCorner](#LeftUpCorner) | گوشهٔ بالا چپ را کنترل می‌کند. |
| [RightUpCorner](#RightUpCorner) | گوشهٔ بالا راست را کنترل می‌کند. |
| [UpCorners](#UpCorners) | گوشه‌های بالایی را کنترل می‌کند. |
| [DownCorners](#DownCorners) | گوشه‌های پایینی را کنترل می‌کند. |
| [Thickness](#Thickness) | ضخامت شکل را کنترل می‌کند. |
| [ArrowTailThickness](#ArrowTailThickness) | ضخامت دم فلش را کنترل می‌کند. |
| [ArrowheadLength](#ArrowheadLength) | طول سر فلش را کنترل می‌کند. |
| [ArrowheadWidth](#ArrowheadWidth) | عرض سر فلش را کنترل می‌کند. |
| [Radius](#Radius) | اندازهٔ شعاع شکل یا قسمت آن را کنترل می‌کند. |
| [Depth](#Depth) | عمق دندانه‌های چرخ دنده یا اثر بیول را کنترل می‌کند. |
| [ShapePartOffset](#ShapePartOffset) | جابجایی یک قسمت از شکل نسبت به قسمت دیگر را کنترل می‌کند. |
| [Angle](#Angle) | زاویه شکل یا قسمت آن را کنترل می‌کند. |
| [StartAngle](#StartAngle) | زاویهٔ شروع برای اشکال کیک و قوس. |
| [EndAngle](#EndAngle) | زاویهٔ انتها برای اشکال کیک و قوس. |
| [StartPointX](#StartPointX) | موقعیت X نقطهٔ شروع فراخوان را کنترل می‌کند. |
| [StartPointY](#StartPointY) | موقعیت Y نقطهٔ شروع فراخوان را کنترل می‌کند. |
| [ConnectorPoint1X](#ConnectorPoint1X) | موقعیت افقی نقطهٔ تنظیم زاویهٔ فراخوان را کنترل می‌کند. |
| [ConnectorPoint1Y](#ConnectorPoint1Y) | موقعیت عمودی نقطهٔ تنظیم زاویهٔ فراخوان را کنترل می‌کند. |
| [ConnectorPoint2X](#ConnectorPoint2X) | موقعیت افقی نقطهٔ تنظیم زاویهٔ فراخوان را کنترل می‌کند. |
| [ConnectorPoint2Y](#ConnectorPoint2Y) | موقعیت عمودی نقطهٔ تنظیم زاویهٔ فراخوان را کنترل می‌کند. |
| [ConnectorBendPositionX](#ConnectorBendPositionX) | موقعیت افقی خم در کانکتور را کنترل می‌کند. |
| [ConnectorBendPositionY](#ConnectorBendPositionY) | موقعیت عمودی خم در کانکتور را کنترل می‌کند. |
| [AttachY](#AttachY) | نقطهٔ اتصال عمودی شکل را کنترل می‌کند. |
| [AttachX](#AttachX) | نقطهٔ اتصال افقی شکل را کنترل می‌کند. |
| [CalloutHeight](#CalloutHeight) | اندازهٔ عمودی فراخوان را کنترل می‌کند. |
| [CalloutWidth](#CalloutWidth) | اندازهٔ افقی فراخوان را کنترل می‌کند. |
| [CalloutSize](#CalloutSize) | اندازهٔ فراخوان را کنترل می‌کند. |
| [Curvature](#Curvature) | انحنای یک خم، براکت، فلش منحنی یا شکل. |
| [HorizontalThickness](#HorizontalThickness) | ضخامت افقی شکل گوشه را کنترل می‌کند. |
| [VerticalThickness](#VerticalThickness) | ضخامت عمودی شکل گوشه را کنترل می‌کند. |
| [VerticalSize](#VerticalSize) | اندازهٔ عمودی شکل را کنترل می‌کند. |
| [HorizontalSize](#HorizontalSize) | اندازهٔ افقی شکل را کنترل می‌کند. |
| [Top](#Top) | قسمت بالایی یک شکل را کنترل می‌کند. |
### Custom {#Custom}
```
public static final int Custom
```

نوع سفارشی (تنظیم ناشناخته)

### CornerSize {#CornerSize}
```
public static final int CornerSize
```

اندازهٔ گوشه‌ها را کنترل می‌کند.

### LeftUpCorner {#LeftUpCorner}
```
public static final int LeftUpCorner
```

گوشهٔ بالا چپ را کنترل می‌کند.

### RightUpCorner {#RightUpCorner}
```
public static final int RightUpCorner
```

گوشهٔ بالا راست را کنترل می‌کند.

### UpCorners {#UpCorners}
```
public static final int UpCorners
```

گوشه‌های بالایی را کنترل می‌کند.

### DownCorners {#DownCorners}
```
public static final int DownCorners
```

گوشه‌های پایینی را کنترل می‌کند.

### Thickness {#Thickness}
```
public static final int Thickness
```

ضخامت شکل را کنترل می‌کند.

### ArrowTailThickness {#ArrowTailThickness}
```
public static final int ArrowTailThickness
```

ضخامت دم فلش را کنترل می‌کند.

### ArrowheadLength {#ArrowheadLength}
```
public static final int ArrowheadLength
```

طول سر فلش را کنترل می‌کند.

### ArrowheadWidth {#ArrowheadWidth}
```
public static final int ArrowheadWidth
```

عرض سر فلش را کنترل می‌کند.

### Radius {#Radius}
```
public static final int Radius
```

اندازهٔ شعاع شکل یا قسمت آن را کنترل می‌کند.

### Depth {#Depth}
```
public static final int Depth
```

عمق دندانه‌های چرخ دنده یا اثر بیول را کنترل می‌کند.

### ShapePartOffset {#ShapePartOffset}
```
public static final int ShapePartOffset
```

جابجایی یک قسمت از شکل نسبت به قسمت دیگر را کنترل می‌کند.

### Angle {#Angle}
```
public static final int Angle
```

زاویه شکل یا قسمت آن را کنترل می‌کند.

### StartAngle {#StartAngle}
```
public static final int StartAngle
```

زاویهٔ شروع برای اشکال کیک و قوس.

### EndAngle {#EndAngle}
```
public static final int EndAngle
```

زاویهٔ انتها برای اشکال کیک و قوس.

### StartPointX {#StartPointX}
```
public static final int StartPointX
```

موقعیت X نقطهٔ شروع فراخوان را کنترل می‌کند.

### StartPointY {#StartPointY}
```
public static final int StartPointY
```

موقعیت Y نقطهٔ شروع فراخوان را کنترل می‌کند.

### ConnectorPoint1X {#ConnectorPoint1X}
```
public static final int ConnectorPoint1X
```

موقعیت افقی نقطهٔ تنظیم زاویهٔ فراخوان را کنترل می‌کند.

### ConnectorPoint1Y {#ConnectorPoint1Y}
```
public static final int ConnectorPoint1Y
```

موقعیت عمودی نقطهٔ تنظیم زاویهٔ فراخوان را کنترل می‌کند.

### ConnectorPoint2X {#ConnectorPoint2X}
```
public static final int ConnectorPoint2X
```

موقعیت افقی نقطهٔ تنظیم زاویهٔ فراخوان را کنترل می‌کند.

### ConnectorPoint2Y {#ConnectorPoint2Y}
```
public static final int ConnectorPoint2Y
```

موقعیت عمودی نقطهٔ تنظیم زاویهٔ فراخوان را کنترل می‌کند.

### ConnectorBendPositionX {#ConnectorBendPositionX}
```
public static final int ConnectorBendPositionX
```

موقعیت افقی خم در کانکتور را کنترل می‌کند.

### ConnectorBendPositionY {#ConnectorBendPositionY}
```
public static final int ConnectorBendPositionY
```

موقعیت عمودی خم در کانکتور را کنترل می‌کند.

### AttachY {#AttachY}
```
public static final int AttachY
```

نقطهٔ اتصال عمودی شکل را کنترل می‌کند.

### AttachX {#AttachX}
```
public static final int AttachX
```

نقطهٔ اتصال افقی شکل را کنترل می‌کند.

### CalloutHeight {#CalloutHeight}
```
public static final int CalloutHeight
```

اندازهٔ عمودی فراخوان را کنترل می‌کند.

### CalloutWidth {#CalloutWidth}
```
public static final int CalloutWidth
```

اندازهٔ افقی فراخوان را کنترل می‌کند.

### CalloutSize {#CalloutSize}
```
public static final int CalloutSize
```

اندازهٔ فراخوان را کنترل می‌کند.

### Curvature {#Curvature}
```
public static final int Curvature
```

انحنای یک خم، براکت، فلش منحنی یا شکل.

### HorizontalThickness {#HorizontalThickness}
```
public static final int HorizontalThickness
```

ضخامت افقی شکل گوشه را کنترل می‌کند.

### VerticalThickness {#VerticalThickness}
```
public static final int VerticalThickness
```

ضخامت عمودی شکل گوشه را کنترل می‌کند.

### VerticalSize {#VerticalSize}
```
public static final int VerticalSize
```

اندازهٔ عمودی شکل را کنترل می‌کند.

### HorizontalSize {#HorizontalSize}
```
public static final int HorizontalSize
```

اندازهٔ افقی شکل را کنترل می‌کند.

### Top {#Top}
```
public static final int Top
```

قسمت بالایی یک شکل را کنترل می‌کند.