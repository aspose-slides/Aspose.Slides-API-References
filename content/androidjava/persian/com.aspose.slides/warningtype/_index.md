---
title: WarningType
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: یک نوع هشدار را نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/warningtype/
---
**ارث‌بری:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class WarningType extends System.Enum
```

یک نوع هشدار را نشان می‌دهد.
## فیلدها

| فیلد | توضیح |
| --- | --- |
| [SourceFileCorruption](#SourceFileCorruption) | یک مشکل در سند منبع شناسایی شده است که به‌طوری محتمل است سند پس از ذخیره در قالب اصلی خود قابل باز شدن نباشد. |
| [DataLoss](#DataLoss) | متن/نمودار/تصویر یا سایر داده‌ها به‌صورت کامل از درخت سند پس از بارگذاری یا سند ایجاد شده پس از ذخیره‌سازی حذف خواهند شد. |
| [MajorFormattingLoss](#MajorFormattingLoss) | از دست رفتن بزرگ قالب‌بندی. |
| [MinorFormattingLoss](#MinorFormattingLoss) | از دست رفتن جزئی قالب‌بندی. |
| [CompatibilityIssue](#CompatibilityIssue) | این یک مشکل شناخته شده است که باز شدن سند را توسط برخی عوامل کاربری یا نسخه‌های قبلی عوامل کاربری منع می‌کند. |
| [UnexpectedContent](#UnexpectedContent) | برخی محتواها در سند منبع تشخیص داده نشدند (مثلاً |
### SourceFileCorruption {#SourceFileCorruption}
```
public static final int SourceFileCorruption
```


یک مشکل در سند منبع شناسایی شده است که به‌طوری محتمل است سند پس از ذخیره در قالب اصلی خود قابل باز شدن نباشد.

### DataLoss {#DataLoss}
```
public static final int DataLoss
```


متن/نمودار/تصویر یا سایر داده‌ها به‌صورت کامل از درخت سند پس از بارگذاری یا سند ایجاد شده پس از ذخیره‌سازی حذف خواهند شد.

### MajorFormattingLoss {#MajorFormattingLoss}
```
public static final int MajorFormattingLoss
```


از دست رفتن بزرگ قالب‌بندی.

### MinorFormattingLoss {#MinorFormattingLoss}
```
public static final int MinorFormattingLoss
```


از دست رفتن جزئی قالب‌بندی.

### CompatibilityIssue {#CompatibilityIssue}
```
public static final int CompatibilityIssue
```


این یک مشکل شناخته شده است که باز شدن سند را توسط برخی عوامل کاربری یا نسخه‌های قبلی عوامل کاربری منع می‌کند.

### UnexpectedContent {#UnexpectedContent}
```
public static final int UnexpectedContent
```


برخی محتواها در سند منبع قابل تشخیص نبودند (به‌عنوان مثال پشتیبانی نمی‌شوند)، این ممکن است منجر به بروز مشکلات یا از دست رفتن داده‌ها/قالب‌بندی شود یا نشود.