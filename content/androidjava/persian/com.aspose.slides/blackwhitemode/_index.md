---
title: BlackWhiteMode
second_title: Aspose.Slides برای اندروید از طریق مرجع API جاوا
description: تعیین می‌کند که شکل رنگی چگونه باید به سیاه و سفید تبدیل شود.
type: docs
url: /fa/com.aspose.slides/blackwhitemode/
---
**وراثت:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class BlackWhiteMode extends System.Enum
```

تعیین می‌کند که شکل رنگی چگونه به سیاه و سفید تبدیل شود.
## فیلدها

| Field | Description |
| --- | --- |
| [NotDefined](#NotDefined) | ویژگی تنظیم نشده است |
| [Color](#Color) | شی با رنگ‌بندی معمولی رندر می‌شود |
| [Automatic](#Automatic) | شی با رنگ‌بندی خودکار رندر می‌شود |
| [Gray](#Gray) | شی با رنگ‌بندی خاکستری رندر می‌شود |
| [LightGray](#LightGray) | شی با رنگ‌بندی خاکستری روشن رندر می‌شود |
| [InverseGray](#InverseGray) | شی با رنگ‌بندی خاکستری معکوس رندر می‌شود |
| [GrayWhite](#GrayWhite) | شی با رنگ‌بندی خاکستری و سفید رندر می‌شود |
| [BlackGray](#BlackGray) | شی با رنگ‌بندی سیاه و خاکستری رندر می‌شود |
| [BlackWhite](#BlackWhite) | شی با رنگ‌بندی سیاه و سفید رندر می‌شود |
| [Black](#Black) | شی فقط با رنگ‌بندی سیاه رندر می‌شود |
| [White](#White) | شی با رنگ‌بندی سفید رندر می‌شود |
| [Hidden](#Hidden) | شی رندر نمی‌شود |
### NotDefined {#NotDefined}
```
public static final byte NotDefined
```

ویژگی تنظیم نشده است

### Color {#Color}
```
public static final byte Color
```

شی با رنگ‌بندی معمولی رندر می‌شود

### Automatic {#Automatic}
```
public static final byte Automatic
```

شی با رنگ‌بندی خودکار رندر می‌شود

### Gray {#Gray}
```
public static final byte Gray
```

شی با رنگ‌بندی خاکستری رندر می‌شود

### LightGray {#LightGray}
```
public static final byte LightGray
```

شی با رنگ‌بندی خاکستری روشن رندر می‌شود

### InverseGray {#InverseGray}
```
public static final byte InverseGray
```

شی با رنگ‌بندی خاکستری معکوس رندر می‌شود

### GrayWhite {#GrayWhite}
```
public static final byte GrayWhite
```

شی با رنگ‌بندی خاکستری و سفید رندر می‌شود

### BlackGray {#BlackGray}
```
public static final byte BlackGray
```

شی با رنگ‌بندی سیاه و خاکستری رندر می‌شود

### BlackWhite {#BlackWhite}
```
public static final byte BlackWhite
```

شی با رنگ‌بندی سیاه و سفید رندر می‌شود

### Black {#Black}
```
public static final byte Black
```

شی فقط با رنگ‌بندی سیاه رندر می‌شود

### White {#White}
```
public static final byte White
```

شی با رنگ‌بندی سفید رندر می‌شود

### Hidden {#Hidden}
```
public static final byte Hidden
```

شی رندر نمی‌شود