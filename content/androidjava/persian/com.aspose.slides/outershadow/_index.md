---
title: OuterShadow
second_title: Aspose.Slides برای اندروید از طریق مرجع API جاوا
description: نمایانگر یک اثر سایه خارجی است.
type: docs
url: /fa/com.aspose.slides/outershadow/
---
**ارث‌بری:**  
java.lang.Object

**تمام رابط‌های پیاده‌سازی شده:**  
[com.aspose.slides.IOuterShadow](../../com.aspose.slides/ioutershadow), com.aspose.slides.IVisualEffect, com.aspose.slides.IDOMObject, com.aspose.slides.IPVIObject, java.lang.Cloneable  
```
public final class OuterShadow implements IOuterShadow, IVisualEffect, IDOMObject, IPVIObject, Cloneable
```

نمایانگر یک اثر سایه خارجی است.

## متدها

| متد | توضیح |
| --- | --- |
| [getBlurRadius()](#getBlurRadius--) | شعاع تاری، به نقطه. |
| [setBlurRadius(double value)](#setBlurRadius-double-) | شعاع تاری، به نقطه. |
| [getDirection()](#getDirection--) | جهت سایه، به درجه. |
| [setDirection(float value)](#setDirection-float-) | جهت سایه، به درجه. |
| [getDistance()](#getDistance--) | فاصله سایه از شی، به نقطه. |
| [setDistance(double value)](#setDistance-double-) | فاصله سایه از شی، به نقطه. |
| [getShadowColor()](#getShadowColor--) | رنگ سایه. |
| [getRectangleAlign()](#getRectangleAlign--) | تراز مستطیل. |
| [setRectangleAlign(byte value)](#setRectangleAlign-byte-) | تراز مستطیل. |
| [getSkewHorizontal()](#getSkewHorizontal--) | زاویه انحراف افقی، به درجه. |
| [setSkewHorizontal(double value)](#setSkewHorizontal-double-) | زاویه انحراف افقی، به درجه. |
| [getSkewVertical()](#getSkewVertical--) | زاویه انحراف عمودی، به درجه. |
| [setSkewVertical(double value)](#setSkewVertical-double-) | زاویه انحراف عمودی، به درجه. |
| [getRotateShadowWithShape()](#getRotateShadowWithShape--) | نشان می‌دهد آیا سایه همراه با شکل می‌چرخد یا خیر. |
| [setRotateShadowWithShape(boolean value)](#setRotateShadowWithShape-boolean-) | نشان می‌دهد آیا سایه همراه با شکل می‌چرخد یا خیر. |
| [getScaleHorizontal()](#getScaleHorizontal--) | ضریب مقیاس افقی، به درصد اندازه اصلی. |
| [setScaleHorizontal(double value)](#setScaleHorizontal-double-) | ضریب مقیاس افقی، به درصد اندازه اصلی. |
| [getScaleVertical()](#getScaleVertical--) | ضریب مقیاس عمودی، به درصد اندازه اصلی. |
| [setScaleVertical(double value)](#setScaleVertical-double-) | ضریب مقیاس عمودی، به درصد اندازه اصلی. |
| [getEffective()](#getEffective--) | داده‌های مؤثر اثر سایه خارجی را با اعمال ارث‌بری دریافت می‌کند. |
| [getParent_Immediate()](#getParent-Immediate--) |  |
| [getVersion()](#getVersion--) |  |
| [getParent_IPresentationComponent()](#getParent-IPresentationComponent--) |  |
| [equals(Object obj)](#equals-java.lang.Object-) | تعیین می‌کند آیا [OuterShadow](../../com.aspose.slides/outershadow) مشخص شده برابر با [OuterShadow](../../com.aspose.slides/outershadow) فعلی است. |
| [hashCode()](#hashCode--) | به عنوان تابع هش برای یک نوع خاص عمل می‌کند. |

### getBlurRadius() {#getBlurRadius--}
```
public final double getBlurRadius()
```

شعاع تاری، به نقطه. مقدار پیش‌فرض - 0 pt. خواندنی/نوشتنی double.

**بازگرداندن:**  
double

### setBlurRadius(double value) {#setBlurRadius-double-}
```
public final void setBlurRadius(double value)
```

شعاع تاری، به نقطه. مقدار پیش‌فرض - 0 pt. خواندنی/نوشتنی double.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | double |  |

### getDirection() {#getDirection--}
```
public final float getDirection()
```

جهت سایه، به درجه. مقدار پیش‌فرض - 0 � (چپ به راست). خواندنی/نوشتنی float.

**بازگرداندن:**  
float

### setDirection(float value) {#setDirection-float-}
```
public final void setDirection(float value)
```

جهت سایه، به درجه. مقدار پیش‌فرض - 0 � (چپ به راست). خواندنی/نوشتنی float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getDistance() {#getDistance--}
```
public final double getDistance()
```

فاصله سایه از شی، به نقطه. مقدار پیش‌فرض - 0 pt. خواندنی/نوشتنی double.

**بازگرداندن:**  
double

### setDistance(double value) {#setDistance-double-}
```
public final void setDistance(double value)
```

فاصله سایه از شی، به نقطه. مقدار پیش‌فرض - 0 pt. خواندنی/نوشتنی double.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | double |  |

### getShadowColor() {#getShadowColor--}
```
public final IColorFormat getShadowColor()
```

رنگ سایه. مقدار پیش‌فرض - سیاه خودکار (وابسته به تم). فقط خواندنی [IColorFormat](../../com.aspose.slides/icolorformat).

**بازگرداندن:**  
[IColorFormat](../../com.aspose.slides/icolorformat)

### getRectangleAlign() {#getRectangleAlign--}
```
public final byte getRectangleAlign()
```

تراز مستطیل. مقدار پیش‌فرض - [RectangleAlignment.Bottom](../../com.aspose.slides/rectanglealignment\#Bottom). خواندنی/نوشتنی [RectangleAlignment](../../com.aspose.slides/rectanglealignment).

**بازگرداندن:**  
byte

### setRectangleAlign(byte value) {#setRectangleAlign-byte-}
```
public final void setRectangleAlign(byte value)
```

تراز مستطیل. مقدار پیش‌فرض - [RectangleAlignment.Bottom](../../com.aspose.slides/rectanglealignment\#Bottom). خواندنی/نوشتنی [RectangleAlignment](../../com.aspose.slides/rectanglealignment).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | byte |  |

### getSkewHorizontal() {#getSkewHorizontal--}
```
public final double getSkewHorizontal()
```

زاویه انحراف افقی، به درجه. مقدار پیش‌فرض - 0 �. خواندنی/نوشتنی double.

**بازگرداندن:**  
double

### setSkewHorizontal(double value) {#setSkewHorizontal-double-}
```
public final void setSkewHorizontal(double value)
```

زاویه انحراف افقی، به درجه. مقدار پیش‌فرض - 0 �. خواندنی/نوشتنی double.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | double |  |

### getSkewVertical() {#getSkewVertical--}
```
public final double getSkewVertical()
```

زاویه انحراف عمودی، به درجه. مقدار پیش‌فرض - 0 �. خواندنی/نوشتنی double.

**بازگرداندن:**  
double

### setSkewVertical(double value) {#setSkewVertical-double-}
```
public final void setSkewVertical(double value)
```

زاویه انحراف عمودی، به درجه. مقدار پیش‌فرض - 0 �. خواندنی/نوشتنی double.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | double |  |

### getRotateShadowWithShape() {#getRotateShadowWithShape--}
```
public final boolean getRotateShadowWithShape()
```

نشان می‌دهد آیا سایه همراه با شکل می‌چرخد یا خیر. مقدار پیش‌فرض - true. خواندنی/نوشتنی boolean.

**بازگرداندن:**  
boolean

### setRotateShadowWithShape(boolean value) {#setRotateShadowWithShape-boolean-}
```
public final void setRotateShadowWithShape(boolean value)
```

نشان می‌دهد آیا سایه همراه با شکل می‌چرخد یا خیر. مقدار پیش‌فرض - true. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getScaleHorizontal() {#getScaleHorizontal--}
```
public final double getScaleHorizontal()
```

ضریب مقیاس افقی، به درصد اندازه اصلی. مقیاس منفی باعث وارون شدن می‌شود. مقدار پیش‌فرض - 100 %. خواندنی/نوشتنی double.

**بازگرداندن:**  
double

### setScaleHorizontal(double value) {#setScaleHorizontal-double-}
```
public final void setScaleHorizontal(double value)
```

ضریب مقیاس افقی، به درصد اندازه اصلی. مقیاس منفی باعث وارون شدن می‌شود. مقدار پیش‌فرض - 100 %. خواندنی/نوشتنی double.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | double |  |

### getScaleVertical() {#getScaleVertical--}
```
public final double getScaleVertical()
```

ضریب مقیاس عمودی، به درصد اندازه اصلی. مقیاس منفی باعث وارون شدن می‌شود. مقدار پیش‌فرض - 100 %. خواندنی/نوشتنی double.

**بازگرداندن:**  
double

### setScaleVertical(double value) {#setScaleVertical-double-}
```
public final void setScaleVertical(double value)
```

ضریب مقیاس عمودی، به درصد اندازه اصلی. مقیاس منفی باعث وارون شدن می‌شود. مقدار پیش‌فرض - 100 %. خواندنی/نوشتنی double.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | double |  |

### getEffective() {#getEffective--}
```
public final IOuterShadowEffectiveData getEffective()
```

داده‌های مؤثر اثر سایه خارجی را با اعمال ارث‌بری دریافت می‌کند.

**بازگرداندن:**  
[IOuterShadowEffectiveData](../../com.aspose.slides/ioutershadoweffectivedata) - یک [IOuterShadowEffectiveData](../../com.aspose.slides/ioutershadoweffectivedata).

### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

شی Parent_Immediate را برمی‌گرداند. فقط خواندنی IDOMObject.

**بازگرداندن:**  
com.aspose.slides.IDOMObject

### getVersion() {#getVersion--}
```
public final long getVersion()
```

نسخه. فقط خواندنی long.

**بازگرداندن:**  
long

### getParent_IPresentationComponent() {#getParent-IPresentationComponent--}
```
public final IPresentationComponent getParent_IPresentationComponent()
```

شی Parent IPresentationComponent را برمی‌گرداند. فقط خواندنی [IPresentationComponent](../../com.aspose.slides/ipresentationcomponent).

**بازگرداندن:**  
[IPresentationComponent](../../com.aspose.slides/ipresentationcomponent)

### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```

تعیین می‌کند آیا [OuterShadow](../../com.aspose.slides/outershadow) مشخص شده برابر با [OuterShadow](../../com.aspose.slides/outershadow) فعلی است.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| obj | java.lang.Object | [OuterShadow](../../com.aspose.slides/outershadow) برای مقایسه. |

**بازگرداندن:**  
boolean - true اگر اشیا برابر باشند؛ در غیر این صورت false.

### hashCode() {#hashCode--}
```
public int hashCode()
```

به عنوان تابع هش برای یک نوع خاص عمل می‌کند.

**بازگرداندن:**  
int - یک کد هش برای شی جاری.