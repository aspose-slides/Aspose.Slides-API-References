---
title: ILayoutable
second_title: Aspose.Slides برای اندروید از طریق مرجع API جاوا
description: موقعیت دقیق یک عنصر نمودار را مشخص می‌کند.
type: docs
url: /fa/com.aspose.slides/ilayoutable/
---
**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IChartComponent](../../com.aspose.slides/ichartcomponent)
```
public interface ILayoutable extends IChartComponent
```

موقعیت دقیق یک عنصر نمودار را مشخص می‌کند.
## متدها

| متد | توضیح |
| --- | --- |
| [getX()](#getX--) | موقعیت x (چپ) عنصر نمودار را به عنوان یک نسبت از عرض نمودار مشخص می‌کند. |
| [setX(float value)](#setX-float-) | موقعیت x (چپ) عنصر نمودار را به عنوان یک نسبت از عرض نمودار مشخص می‌کند. |
| [getY()](#getY--) | موقعیت بالای عنصر نمودار را به عنوان یک نسبت از ارتفاع نمودار مشخص می‌کند. |
| [setY(float value)](#setY-float-) | موقعیت بالای عنصر نمودار را به عنوان یک نسبت از ارتفاع نمودار مشخص می‌کند. |
| [getWidth()](#getWidth--) | عرض عنصر نمودار را به عنوان یک نسبت از عرض نمودار مشخص می‌کند. |
| [setWidth(float value)](#setWidth-float-) | عرض عنصر نمودار را به عنوان یک نسبت از عرض نمودار مشخص می‌کند. |
| [getHeight()](#getHeight--) | ارتفاع عنصر نمودار را به عنوان یک نسبت از ارتفاع نمودار مشخص می‌کند. |
| [setHeight(float value)](#setHeight-float-) | ارتفاع عنصر نمودار را به عنوان یک نسبت از ارتفاع نمودار مشخص می‌کند. |
| [getRight()](#getRight--) | دریافت می‌کند right عنصر نمودار به عنوان یک نسبت از عرض نمودار. |
| [getBottom()](#getBottom--) | دریافت می‌کند top عنصر نمودار به عنوان یک نسبت از ارتفاع نمودار. |
### getX() {#getX--}
```
public abstract float getX()
```

موقعیت x (چپ) عنصر نمودار را به عنوان یک نسبت از عرض نمودار مشخص می‌کند. قابلیت خواندن/نوشتن، float.

**بازگشت:**  
float
### setX(float value) {#setX-float-}
```
public abstract void setX(float value)
```

موقعیت x (چپ) عنصر نمودار را به عنوان یک نسبت از عرض نمودار مشخص می‌کند. قابلیت خواندن/نوشتن، float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |
### getY() {#getY--}
```
public abstract float getY()
```

موقعیت بالای عنصر نمودار را به عنوان یک نسبت از ارتفاع نمودار مشخص می‌کند. قابلیت خواندن/نوشتن، float.

**بازگشت:**  
float
### setY(float value) {#setY-float-}
```
public abstract void setY(float value)
```

موقعیت بالای عنصر نمودار را به عنوان یک نسبت از ارتفاع نمودار مشخص می‌کند. قابلیت خواندن/نوشتن، float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |
### getWidth() {#getWidth--}
```
public abstract float getWidth()
```

عرض عنصر نمودار را به عنوان یک نسبت از عرض نمودار مشخص می‌کند. قابلیت خواندن/نوشتن، float.

**بازگشت:**  
float
### setWidth(float value) {#setWidth-float-}
```
public abstract void setWidth(float value)
```

عرض عنصر نمودار را به عنوان یک نسبت از عرض نمودار مشخص می‌کند. قابلیت خواندن/نوشتن، float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |
### getHeight() {#getHeight--}
```
public abstract float getHeight()
```

ارتفاع عنصر نمودار را به عنوان یک نسبت از ارتفاع نمودار مشخص می‌کند. قابلیت خواندن/نوشتن، float.

**بازگشت:**  
float
### setHeight(float value) {#setHeight-float-}
```
public abstract void setHeight(float value)
```

ارتفاع عنصر نمودار را به عنوان یک نسبت از ارتفاع نمودار مشخص می‌کند. قابلیت خواندن/نوشتن، float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |
### getRight() {#getRight--}
```
public abstract float getRight()
```

دریافت می‌کند right عنصر نمودار به عنوان یک نسبت از عرض نمودار. فقط خواندنی، float.

**بازگشت:**  
float
### getBottom() {#getBottom--}
```
public abstract float getBottom()
```

دریافت می‌کند top عنصر نمودار به عنوان یک نسبت از ارتفاع نمودار. فقط خواندنی، float.

**بازگشت:**  
float