---
title: DisplayUnitType
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: تعیین‌کننده تعداد داده‌های نمایش داده شده.
type: docs
url: /fa/com.aspose.slides/displayunittype/
---
**ارث‌بری:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class DisplayUnitType extends System.Enum
```

تعدد داده‌های نمایش داده شده را تعیین می‌کند.
## فیلدها

| فیلد | توضیح |
| --- | --- |
| [None](#None) | مقادیر همان‌گونه نمایش داده می‌شوند. |
| [Hundreds](#Hundreds) | مشخص می‌کند که مقادیر در نمودار باید بر ۱۰۰ تقسیم شوند. |
| [Thousands](#Thousands) | مشخص می‌کند که مقادیر در نمودار باید بر ۱٬۰۰۰ تقسیم شوند. |
| [TenThousands](#TenThousands) | مشخص می‌کند که مقادیر در نمودار باید بر ۱۰٬۰۰۰ تقسیم شوند. |
| [HundredThousands](#HundredThousands) | مشخص می‌کند که مقادیر در نمودار باید بر ۱۰۰٬۰۰۰ تقسیم شوند. |
| [Millions](#Millions) | مشخص می‌کند که مقادیر در نمودار باید بر ۱٬۰۰۰٬۰۰۰ تقسیم شوند. |
| [TenMillions](#TenMillions) | مشخص می‌کند که مقادیر در نمودار باید بر ۱۰٬۰۰۰٬۰۰۰ تقسیم شوند. |
| [HundredMillions](#HundredMillions) | مشخص می‌کند که مقادیر در نمودار باید بر ۱۰۰٬۰۰۰٬۰۰۰ تقسیم شوند. |
| [Billions](#Billions) | مشخص می‌کند که مقادیر در نمودار باید بر ۱٬۰۰۰٬۰۰۰٬۰۰۰ تقسیم شوند. |
| [Trillions](#Trillions) | مشخص می‌کند که مقادیر در نمودار باید بر ۱٬۰۰۰٬۰۰۰٬۰۰۰٬۰۰۰ تقسیم شوند. |
| [CustomValue](#CustomValue) | مشخص می‌کند که مقادیر در نمودار باید بر مقدار سفارشی تقسیم شوند. |
### None {#None}
```
public static final int None
```

مقادیر همان‌گونه نمایش داده می‌شوند.

### Hundreds {#Hundreds}
```
public static final int Hundreds
```

مشخص می‌کند که مقادیر در نمودار باید بر ۱۰۰ تقسیم شوند.

### Thousands {#Thousands}
```
public static final int Thousands
```

مشخص می‌کند که مقادیر در نمودار باید بر ۱٬۰۰۰ تقسیم شوند.

### TenThousands {#TenThousands}
```
public static final int TenThousands
```

مشخص می‌کند که مقادیر در نمودار باید بر ۱۰٬۰۰۰ تقسیم شوند.

### HundredThousands {#HundredThousands}
```
public static final int HundredThousands
```

مشخص می‌کند که مقادیر در نمودار باید بر ۱۰۰٬۰۰۰ تقسیم شوند.

### Millions {#Millions}
```
public static final int Millions
```

مشخص می‌کند که مقادیر در نمودار باید بر ۱٬۰۰۰٬۰۰۰ تقسیم شوند.

### TenMillions {#TenMillions}
```
public static final int TenMillions
```

مشخص می‌کند که مقادیر در نمودار باید بر ۱۰٬۰۰۰٬۰۰۰ تقسیم شوند.

### HundredMillions {#HundredMillions}
```
public static final int HundredMillions
```

مشخص می‌کند که مقادیر در نمودار باید بر ۱۰۰٬۰۰۰٬۰۰۰ تقسیم شوند.

### Billions {#Billions}
```
public static final int Billions
```

مشخص می‌کند که مقادیر در نمودار باید بر ۱٬۰۰۰٬۰۰۰٬۰۰۰ تقسیم شوند.

### Trillions {#Trillions}
```
public static final int Trillions
```

مشخص می‌کند که مقادیر در نمودار باید بر ۱٬۰۰۰٬۰۰۰٬۰۰۰٬۰۰۰ تقسیم شوند.

### CustomValue {#CustomValue}
```
public static final int CustomValue
```

مشخص می‌کند که مقادیر در نمودار باید بر مقدار سفارشی تقسیم شوند.