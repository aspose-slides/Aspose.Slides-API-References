---
title: TrendlineCollection
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: یک مجموعه از Trendline را نشان می‌دهد
type: docs
url: /fa/com.aspose.slides/trendlinecollection/
---
**وراثت:**  
java.lang.Object, com.aspose.slides.DomObject

**تمام رابط‌های پیاده‌سازی‌شده:**  
[com.aspose.slides.ITrendlineCollection](../../com.aspose.slides/itrendlinecollection)  
```
public class TrendlineCollection extends DomObject<ChartSeries> implements ITrendlineCollection
```

یک مجموعه از Trendline را نشان می‌دهد
## متدها

| متد | توضیح |
| --- | --- |
| [get_Item(int index)](#get-Item-int-) | عنصر مورد نظر در اندیس مشخص‌شده را دریافت می‌کند. |
| [add(int trendlineType)](#add-int-) | Trendline جدید را در انتهای یک مجموعه اضافه می‌کند و آن را برمی‌گرداند. |
| [remove(ITrendline value)](#remove-com.aspose.slides.ITrendline-) | مقدار مشخص‌شده را حذف می‌کند. |
| [iterator()](#iterator--) | یک enumerator که مجموعه را پیمایش می‌کند بازمی‌گرداند. |
| [iteratorJava()](#iteratorJava--) | یک java iterator برای کل مجموعه بازمی‌گرداند. |
| [getCount()](#getCount--) | تعداد عناصری که واقعاً در مجموعه موجود هستند را دریافت می‌کند. |

### get_Item(int index) {#get-Item-int-}
```
public final ITrendline get_Item(int index)
```

عنصر مورد نظر در اندیس مشخص‌شده را دریافت می‌کند. فقط خواندنی [Trendline](../../com.aspose.slides/trendline).

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| index | int |  |

**بازگشت:**  
[ITrendline](../../com.aspose.slides/itrendline)

### add(int trendlineType) {#add-int-}
```
public final ITrendline add(int trendlineType)
```

Trendline جدید را در انتهای یک مجموعه اضافه می‌کند و آن را برمی‌گرداند.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| trendlineType | int |  |

**بازگشت:**  
[ITrendline](../../com.aspose.slides/itrendline)

### remove(ITrendline value) {#remove-com.aspose.slides.ITrendline-}
```
public final void remove(ITrendline value)
```

مقدار مشخص‌شده را حذف می‌کند.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | [ITrendline](../../com.aspose.slides/itrendline) |  |

### iterator() {#iterator--}
```
public final System.Collections.Generic.IGenericEnumerator<ITrendline> iterator()
```

یک enumerator که مجموعه را پیمایش می‌کند بازمی‌گرداند.

**بازگشت:**  
com.aspose.ms.System.Collections.Generic.IGenericEnumerator<com.aspose.slides.ITrendline> - یک IGenericEnumerator که می‌تواند برای پیمایش مجموعه استفاده شود.

### iteratorJava() {#iteratorJava--}
```
public final System.Collections.Generic.IGenericEnumerator<ITrendline> iteratorJava()
```

یک java iterator برای کل مجموعه بازمی‌گرداند.

**بازگشت:**  
com.aspose.ms.System.Collections.Generic.IGenericEnumerator<com.aspose.slides.ITrendline> - یک java.util.Iterator برای کل مجموعه.

### getCount() {#getCount--}
```
public final int getCount()
```

تعداد عناصری که واقعاً در مجموعه موجود هستند را دریافت می‌کند. فقط خواندنی int.

**بازگشت:**  
int