---
title: VideoFrame
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: یک کلیپ ویدئویی را بر روی اسلاید نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/videoframe/
---
**وراثت:**  
java.lang.Object, [com.aspose.slides.Shape](../../com.aspose.slides/shape), [com.aspose.slides.GeometryShape](../../com.aspose.slides/geometryshape), [com.aspose.slides.PictureFrame](../../com.aspose.slides/pictureframe)

**تمام رابط‌های پیاده‌سازی شده:**  
[com.aspose.slides.IVideoFrame](../../com.aspose.slides/ivideoframe)  
```
public class VideoFrame extends PictureFrame implements IVideoFrame
```

نمایش یک کلیپ ویدئویی بر روی اسلاید.
## متدها

| متد | توضیح |
| --- | --- |
| [getRewindVideo()](#getRewindVideo--) | مشخص می‌کند آیا یک ویدئو به‌صورت خودکار پس از پایان پخش به نقطه شروع باز می‌گردد. |
| [setRewindVideo(boolean value)](#setRewindVideo-boolean-) | مشخص می‌کند آیا یک ویدئو به‌صورت خودکار پس از پایان پخش به نقطه شروع باز می‌گردد. |
| [getPlayLoopMode()](#getPlayLoopMode--) | مشخص می‌کند آیا یک ویدئو به صورت حلقه‌ای پخش می‌شود. |
| [setPlayLoopMode(boolean value)](#setPlayLoopMode-boolean-) | مشخص می‌کند آیا یک ویدئو به صورت حلقه‌ای پخش می‌شود. |
| [getHideAtShowing()](#getHideAtShowing--) | مشخص می‌کند آیا یک VideoFrame مخفی است. |
| [setHideAtShowing(boolean value)](#setHideAtShowing-boolean-) | مشخص می‌کند آیا یک VideoFrame مخفی است. |
| [getVolume()](#getVolume--) | باز می‌گرداند یا تنظیم می‌کند حجم صدا. |
| [setVolume(int value)](#setVolume-int-) | باز می‌گرداند یا تنظیم می‌کند حجم صدا. |
| [getPlayMode()](#getPlayMode--) | باز می‌گرداند یا تنظیم می‌کند حالت پخش ویدئو. |
| [setPlayMode(int value)](#setPlayMode-int-) | باز می‌گرداند یا تنظیم می‌کند حالت پخش ویدئو. |
| [getFullScreenMode()](#getFullScreenMode--) | مشخص می‌کند آیا یک ویدئو در حالت تمام‌صفحه نمایش داده می‌شود. |
| [setFullScreenMode(boolean value)](#setFullScreenMode-boolean-) | مشخص می‌کند آیا یک ویدئو در حالت تمام‌صفحه نمایش داده می‌شود. |
| [getLinkPathLong()](#getLinkPathLong--) | باز می‌گرداند یا تنظیم می‌کند نام فایل ویدئویی که به یک VideoFrame لینک شده است. |
| [setLinkPathLong(String value)](#setLinkPathLong-java.lang.String-) | باز می‌گرداند یا تنظیم می‌کند نام فایل ویدئویی که به یک VideoFrame لینک شده است. |
| [getEmbeddedVideo()](#getEmbeddedVideo--) | باز می‌گرداند یا تنظیم می‌کند شیء ویدئوی جاسازی‌شده. |
| [setEmbeddedVideo(IVideo value)](#setEmbeddedVideo-com.aspose.slides.IVideo-) | باز می‌گرداند یا تنظیم می‌کند شیء ویدئوی جاسازی‌شده. |
| [getTrimFromStart()](#getTrimFromStart--) | برش شروع [ms] |
| [setTrimFromStart(float value)](#setTrimFromStart-float-) | برش شروع [ms] |
| [getTrimFromEnd()](#getTrimFromEnd--) | برش پایان [ms] |
| [setTrimFromEnd(float value)](#setTrimFromEnd-float-) | برش پایان [ms] |
| [getCaptionTracks()](#getCaptionTracks--) | مجموعه‌ای از زیرنویس‌های بسته مربوط به VideoFrame را برمی‌گرداند. |

### getRewindVideo() {#getRewindVideo--}
```
public final boolean getRewindVideo()
```

مشخص می‌کند آیا یک ویدئو به‌صورت خودکار پس از پایان پخش به نقطه شروع باز می‌گردد.

خواندنی/قابل نوشتن boolean.

**بازگشت:**  
boolean
### setRewindVideo(boolean value) {#setRewindVideo-boolean-}
```
public final void setRewindVideo(boolean value)
```

مشخص می‌کند آیا یک ویدئو به‌صورت خودکار پس از پایان پخش به نقطه شروع باز می‌گردد.

خواندنی/قابل نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getPlayLoopMode() {#getPlayLoopMode--}
```
public final boolean getPlayLoopMode()
```

مشخص می‌کند آیا یک ویدئو به صورت حلقه‌ای پخش می‌شود.

خواندنی/قابل نوشتن boolean.

**بازگشت:**  
boolean
### setPlayLoopMode(boolean value) {#setPlayLoopMode-boolean-}
```
public final void setPlayLoopMode(boolean value)
```

مشخص می‌کند آیا یک ویدئو به صورت حلقه‌ای پخش می‌شود.

خواندنی/قابل نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getHideAtShowing() {#getHideAtShowing--}
```
public final boolean getHideAtShowing()
```

مشخص می‌کند آیا یک VideoFrame مخفی است.

خواندنی/قابل نوشتن boolean.

**بازگشت:**  
boolean
### setHideAtShowing(boolean value) {#setHideAtShowing-boolean-}
```
public final void setHideAtShowing(boolean value)
```

مشخص می‌کند آیا یک VideoFrame مخفی است.

خواندنی/قابل نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getVolume() {#getVolume--}
```
public final int getVolume()
```

باز می‌گرداند یا تنظیم می‌کند حجم صدا.

خواندنی/قابل نوشتن [AudioVolumeMode](../../com.aspose.slides/audiovolumemode).

**بازگشت:**  
int
### setVolume(int value) {#setVolume-int-}
```
public final void setVolume(int value)
```

باز می‌گرداند یا تنظیم می‌کند حجم صدا.

خواندنی/قابل نوشتن [AudioVolumeMode](../../com.aspose.slides/audiovolumemode).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getPlayMode() {#getPlayMode--}
```
public final int getPlayMode()
```

باز می‌گرداند یا تنظیم می‌کند حالت پخش ویدئو.

خواندنی/قابل نوشتن [VideoPlayModePreset](../../com.aspose.slides/videoplaymodepreset).

**بازگشت:**  
int
### setPlayMode(int value) {#setPlayMode-int-}
```
public final void setPlayMode(int value)
```

باز می‌گرداند یا تنظیم می‌کند حالت پخش ویدئو.

خواندنی/قابل نوشتن [VideoPlayModePreset](../../com.aspose.slides/videoplaymodepreset).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getFullScreenMode() {#getFullScreenMode--}
```
public final boolean getFullScreenMode()
```

مشخص می‌کند آیا یک ویدئو در حالت تمام‌صفحه نمایش داده می‌شود.

خواندنی/قابل نوشتن boolean.

**بازگشت:**  
boolean
### setFullScreenMode(boolean value) {#setFullScreenMode-boolean-}
```
public final void setFullScreenMode(boolean value)
```

مشخص می‌کند آیا یک ویدئو در حالت تمام‌صفحه نمایش داده می‌شود.

خواندنی/قابل نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getLinkPathLong() {#getLinkPathLong--}
```
public final String getLinkPathLong()
```

باز می‌گرداند یا تنظیم می‌کند نام فایل ویدئویی که به یک VideoFrame لینک شده است.

خواندنی/قابل نوشتن String.

**بازگشت:**  
java.lang.String
### setLinkPathLong(String value) {#setLinkPathLong-java.lang.String-}
```
public final void setLinkPathLong(String value)
```

باز می‌گرداند یا تنظیم می‌کند نام فایل ویدئویی که به یک VideoFrame لینک شده است.

خواندنی/قابل نوشتن String.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | java.lang.String |  |

### getEmbeddedVideo() {#getEmbeddedVideo--}
```
public final IVideo getEmbeddedVideo()
```

باز می‌گرداند یا تنظیم می‌کند شیء ویدئوی جاسازی‌شده.

خواندنی/قابل نوشتن [IVideo](../../com.aspose.slides/ivideo).

**بازگشت:**  
[IVideo](../../com.aspose.slides/ivideo)
### setEmbeddedVideo(IVideo value) {#setEmbeddedVideo-com.aspose.slides.IVideo-}
```
public final void setEmbeddedVideo(IVideo value)
```

باز می‌گرداند یا تنظیم می‌کند شیء ویدئوی جاسازی‌شده.

خواندنی/قابل نوشتن [IVideo](../../com.aspose.slides/ivideo).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | [IVideo](../../com.aspose.slides/ivideo) |  |

### getTrimFromStart() {#getTrimFromStart--}
```
public final float getTrimFromStart()
```

برش شروع [ms]

--------------------

> ```
> Example:
>  
>  Presentation pres = new Presentation();
>  try {
>      ISlide slide = pres.getSlides().get_Item(0);
>      IVideo video = pres.getVideos().addVideo(videoData);
>      IVideoFrame videoFrame = slide.getShapes().addVideoFrame(0, 0, 100, 100, video);
>      //تنظیم زمان شروع برش 1 ثانیه
>      videoFrame.setTrimFromStart(1000f);
>      //تنظیم زمان پایان برش 2 ثانیه
>      videoFrame.setTrimFromEnd(2000f);
>  } finally {
>      if (pres != null) pres.dispose();
>  }
> ```

**بازگشت:**  
float
### setTrimFromStart(float value) {#setTrimFromStart-float-}
```
public final void setTrimFromStart(float value)
```

برش شروع [ms]

--------------------

> ```
> Example:
>  
>  Presentation pres = new Presentation();
>  try {
>      ISlide slide = pres.getSlides().get_Item(0);
>      IVideo video = pres.getVideos().addVideo(videoData);
>      IVideoFrame videoFrame = slide.getShapes().addVideoFrame(0, 0, 100, 100, video);
>      // تنظیم زمان شروع برش 1 ثانیه
>      videoFrame.setTrimFromStart(1000f);
>      // تنظیم زمان پایان برش 2 ثانیه
>      videoFrame.setTrimFromEnd(2000f);
>  } finally {
>      if (pres != null) pres.dispose();
>  }
> ```

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getTrimFromEnd() {#getTrimFromEnd--}
```
public final float getTrimFromEnd()
```

برش پایان [ms]

**بازگشت:**  
float
### setTrimFromEnd(float value) {#setTrimFromEnd-float-}
```
public final void setTrimFromEnd(float value)
```

برش پایان [ms]

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getCaptionTracks() {#getCaptionTracks--}
```
public final ICaptionsCollection getCaptionTracks()
```

مجموعه‌ای از زیرنویس‌های بسته مربوط به VideoFrame را برمی‌گرداند. این ویژگی فقط-خواندنی است و یک [ICaptionsCollection](../../com.aspose.slides/icaptionscollection) حاوی تمام مسیرهای زیرنویس را برمی‌گرداند.

--------------------

> ```
> Example:
>   
>  Presentation pres = new Presentation("video with captions.pptx");
>  try {
>      for (IShape shape : pres.getSlides().get_Item(0).getShapes())
>      {
>          if (!(shape instanceof IVideoFrame))
>              continue;
>          IVideoFrame videoFrame = (IVideoFrame) shape;
>          for (ICaptions captionTrack : videoFrame.getCaptionTracks())
>          {
>              // داده‌های دودویی زیرنویس را استخراج می‌کند و آن‌ها را در فایل ذخیره می‌نماید
>              FileOutputStream fos = new FileOutputStream(captionTrack.getCaptionId() + ".vtt");
>              fos.write(captionTrack.getBinaryData());
>              fos.close();
>          }
>      }
>  } finally {
>      if (pres != null) pres.dispose();
>  }
> ```

**بازگشت:**  
[ICaptionsCollection](../../com.aspose.slides/icaptionscollection)