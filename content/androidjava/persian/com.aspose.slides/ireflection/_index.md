---
title: IReflection
second_title: Aspose.Slides برای اندروید از طریق مرجع API جاوا
description: نمایانگر یک اثر انعکاس است.
type: docs
url: /fa/com.aspose.slides/ireflection/
---
**تمام رابط‌های پیاده‌سازی شده:**  
[com.aspose.slides.IImageTransformOperation](../../com.aspose.slides/iimagetransformoperation), com.aspose.slides.IAccessiblePVIObject
```
public interface IReflection extends IImageTransformOperation, IAccessiblePVIObject<IReflectionEffectiveData>
```

نمایانگر یک اثر انعکاس است.
## متدها

| Method | Description |
| --- | --- |
| [getStartPosAlpha()](#getStartPosAlpha--) | موقعیت شروع (در امتداد دامنه گرادیان آلفا) مقدار آلفای شروع (درصد) را مشخص می‌کند. |
| [setStartPosAlpha(float value)](#setStartPosAlpha-float-) | موقعیت شروع (در امتداد دامنه گرادیان آلفا) مقدار آلفای شروع (درصد) را مشخص می‌کند. |
| [getEndPosAlpha()](#getEndPosAlpha--) | موقعیت پایان (در امتداد دامنه گرادیان آلفا) مقدار آلفای پایان (درصد) را مشخص می‌کند. |
| [setEndPosAlpha(float value)](#setEndPosAlpha-float-) | موقعیت پایان (در امتداد دامنه گرادیان آلفا) مقدار آلفای پایان (درصد) را مشخص می‌کند. |
| [getFadeDirection()](#getFadeDirection--) | جهت جابجایی انعکاس را مشخص می‌کند. |
| [setFadeDirection(float value)](#setFadeDirection-float-) | جهت جابجایی انعکاس را مشخص می‌کند. |
| [getStartReflectionOpacity()](#getStartReflectionOpacity--) | شفافیت شروع انعکاس. |
| [setStartReflectionOpacity(float value)](#setStartReflectionOpacity-float-) | شفافیت شروع انعکاس. |
| [getEndReflectionOpacity()](#getEndReflectionOpacity--) | شفافیت پایان انعکاس. |
| [setEndReflectionOpacity(float value)](#setEndReflectionOpacity-float-) | شفافیت پایان انعکاس. |
| [getBlurRadius()](#getBlurRadius--) | شعاع تاری. |
| [setBlurRadius(double value)](#setBlurRadius-double-) | شعاع تاری. |
| [getDirection()](#getDirection--) | جهت انعکاس. |
| [setDirection(float value)](#setDirection-float-) | جهت انعکاس. |
| [getDistance()](#getDistance--) | فاصله انعکاس. |
| [setDistance(double value)](#setDistance-double-) | فاصله انعکاس. |
| [getRectangleAlign()](#getRectangleAlign--) | تراز مستطیل. |
| [setRectangleAlign(byte value)](#setRectangleAlign-byte-) | تراز مستطیل. |
| [getSkewHorizontal()](#getSkewHorizontal--) | زاویهٔ انحراف افقی را مشخص می‌کند. |
| [setSkewHorizontal(double value)](#setSkewHorizontal-double-) | زاویهٔ انحراف افقی را مشخص می‌کند. |
| [getSkewVertical()](#getSkewVertical--) | زاویهٔ انحراف عمودی را مشخص می‌کند. |
| [setSkewVertical(double value)](#setSkewVertical-double-) | زاویهٔ انحراف عمودی را مشخص می‌کند. |
| [getRotateShadowWithShape()](#getRotateShadowWithShape--) | مشخص می‌کند که آیا انعکاس باید همراه شکل چرخانده شود، در صورت چرخاندن شکل. |
| [setRotateShadowWithShape(boolean value)](#setRotateShadowWithShape-boolean-) | مشخص می‌کند که آیا انعکاس باید همراه شکل چرخانده شود، در صورت چرخاندن شکل. |
| [getScaleHorizontal()](#getScaleHorizontal--) | عامل مقیاس‌گذاری افقی را مشخص می‌کند، مقیاس منفی باعث وارون شدن می‌شود. |
| [setScaleHorizontal(double value)](#setScaleHorizontal-double-) | عامل مقیاس‌گذاری افقی را مشخص می‌کند، مقیاس منفی باعث وارون شدن می‌شود. |
| [getScaleVertical()](#getScaleVertical--) | عامل مقیاس‌گذاری عمودی را مشخص می‌کند، مقیاس منفی باعث وارون شدن می‌شود. |
| [setScaleVertical(double value)](#setScaleVertical-double-) | عامل مقیاس‌گذاری عمودی را مشخص می‌کند، مقیاس منفی باعث وارون شدن می‌شود. |

### getStartPosAlpha() {#getStartPosAlpha--}
```
public abstract float getStartPosAlpha()
```

موقعیت شروع (در امتداد دامنه گرادیان آلفا) مقدار آلفای شروع (درصد) را مشخص می‌کند. قابل خواندن/نوشتن float.

**باز می‌گردد:**
float
### setStartPosAlpha(float value) {#setStartPosAlpha-float-}
```
public abstract void setStartPosAlpha(float value)
```

موقعیت شروع (در امتداد دامنه گرادیان آلفا) مقدار آلفای شروع (درصد) را مشخص می‌کند. قابل خواندن/نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getEndPosAlpha() {#getEndPosAlpha--}
```
public abstract float getEndPosAlpha()
```

موقعیت پایان (در امتداد دامنه گرادیان آلفا) مقدار آلفای پایان (درصد) را مشخص می‌کند. قابل خواندن/نوشتن float.

**باز می‌گردد:**
float
### setEndPosAlpha(float value) {#setEndPosAlpha-float-}
```
public abstract void setEndPosAlpha(float value)
```

موقعیت پایان (در امتداد دامنه گرادیان آلفا) مقدار آلفای پایان (درصد) را مشخص می‌کند. قابل خواندن/نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getFadeDirection() {#getFadeDirection--}
```
public abstract float getFadeDirection()
```

جهت جابجایی انعکاس را مشخص می‌کند. (زاویه) قابل خواندن/نوشتن float.

**باز می‌گردد:**
float
### setFadeDirection(float value) {#setFadeDirection-float-}
```
public abstract void setFadeDirection(float value)
```

جهت جابجایی انعکاس را مشخص می‌کند. (زاویه) قابل خواندن/نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getStartReflectionOpacity() {#getStartReflectionOpacity--}
```
public abstract float getStartReflectionOpacity()
```

شفافیت شروع انعکاس. (درصد) قابل خواندن/نوشتن float.

**باز می‌گردد:**
float
### setStartReflectionOpacity(float value) {#setStartReflectionOpacity-float-}
```
public abstract void setStartReflectionOpacity(float value)
```

شفافیت شروع انعکاس. (درصد) قابل خواندن/نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getEndReflectionOpacity() {#getEndReflectionOpacity--}
```
public abstract float getEndReflectionOpacity()
```

شفافیت پایان انعکاس. (درصد) قابل خواندن/نوشتن float.

**باز می‌گردد:**
float
### setEndReflectionOpacity(float value) {#setEndReflectionOpacity-float-}
```
public abstract void setEndReflectionOpacity(float value)
```

شفافیت پایان انعکاس. (درصد) قابل خواندن/نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getBlurRadius() {#getBlurRadius--}
```
public abstract double getBlurRadius()
```

شعاع تاری. قابل خواندن/نوشتن double.

**باز می‌گردد:**
double
### setBlurRadius(double value) {#setBlurRadius-double-}
```
public abstract void setBlurRadius(double value)
```

شعاع تاری. قابل خواندن/نوشتن double.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | double |  |

### getDirection() {#getDirection--}
```
public abstract float getDirection()
```

جهت انعکاس. قابل خواندن/نوشتن float.

**باز می‌گردد:**
float
### setDirection(float value) {#setDirection-float-}
```
public abstract void setDirection(float value)
```

جهت انعکاس. قابل خواندن/نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getDistance() {#getDistance--}
```
public abstract double getDistance()
```

فاصله انعکاس. قابل خواندن/نوشتن double.

**باز می‌گردد:**
double
### setDistance(double value) {#setDistance-double-}
```
public abstract void setDistance(double value)
```

فاصله انعکاس. قابل خواندن/نوشتن double.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | double |  |

### getRectangleAlign() {#getRectangleAlign--}
```
public abstract byte getRectangleAlign()
```

تراز مستطیل. قابل خواندن/نوشتن [RectangleAlignment](../../com.aspose.slides/rectanglealignment).

**باز می‌گردد:**
byte
### setRectangleAlign(byte value) {#setRectangleAlign-byte-}
```
public abstract void setRectangleAlign(byte value)
```

تراز مستطیل. قابل خواندن/نوشتن [RectangleAlignment](../../com.aspose.slides/rectanglealignment).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | byte |  |

### getSkewHorizontal() {#getSkewHorizontal--}
```
public abstract double getSkewHorizontal()
```

زاویهٔ انحراف افقی را مشخص می‌کند. قابل خواندن/نوشتن double.

**باز می‌گردد:**
double
### setSkewHorizontal(double value) {#setSkewHorizontal-double-}
```
public abstract void setSkewHorizontal(double value)
```

زاویهٔ انحراف افقی را مشخص می‌کند. قابل خواندن/نوشتن double.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | double |  |

### getSkewVertical() {#getSkewVertical--}
```
public abstract double getSkewVertical()
```

زاویهٔ انحراف عمودی را مشخص می‌کند. قابل خواندن/نوشتن double.

**باز می‌گردد:**
double
### setSkewVertical(double value) {#setSkewVertical-double-}
```
public abstract void setSkewVertical(double value)
```

زاویهٔ انحراف عمودی را مشخص می‌کند. قابل خواندن/نوشتن double.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | double |  |

### getRotateShadowWithShape() {#getRotateShadowWithShape--}
```
public abstract boolean getRotateShadowWithShape()
```

مشخص می‌کند که آیا انعکاس باید همراه شکل چرخانده شود، در صورت چرخاندن شکل. قابل خواندن/نوشتن boolean.

**باز می‌گردد:**
boolean
### setRotateShadowWithShape(boolean value) {#setRotateShadowWithShape-boolean-}
```
public abstract void setRotateShadowWithShape(boolean value)
```

مشخص می‌کند که آیا انعکاس باید همراه شکل چرخانده شود، در صورت چرخاندن شکل. قابل خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getScaleHorizontal() {#getScaleHorizontal--}
```
public abstract double getScaleHorizontal()
```

عامل مقیاس‌گذاری افقی را مشخص می‌کند، مقیاس منفی باعث وارون شدن می‌شود. (درصد) قابل خواندن/نوشتن double.

**باز می‌گردد:**
double
### setScaleHorizontal(double value) {#setScaleHorizontal-double-}
```
public abstract void setScaleHorizontal(double value)
```

عامل مقیاس‌گذاری افقی را مشخص می‌کند، مقیاس منفی باعث وارون شدن می‌شود. (درصد) قابل خواندن/نوشتن double.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | double |  |

### getScaleVertical() {#getScaleVertical--}
```
public abstract double getScaleVertical()
```

عامل مقیاس‌گذاری عمودی را مشخص می‌کند، مقیاس منفی باعث وارون شدن می‌شود. (درصد) قابل خواندن/نوشتن double.

**باز می‌گردد:**
double
### setScaleVertical(double value) {#setScaleVertical-double-}
```
public abstract void setScaleVertical(double value)
```

عامل مقیاس‌گذاری عمودی را مشخص می‌کند، مقیاس منفی باعث وارون شدن می‌شود. (درصد) قابل خواندن/نوشتن double.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | double |  |