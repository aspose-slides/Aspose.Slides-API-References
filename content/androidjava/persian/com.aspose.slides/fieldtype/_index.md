---
title: FieldType
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: نمایانگر یک نوع فیلد است.
type: docs
url: /fa/com.aspose.slides/fieldtype/
---
**ارث‌بری:**
java.lang.Object

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IFieldType](../../com.aspose.slides/ifieldtype)
```
public final class FieldType implements IFieldType
```

یک نوع فیلد را نمایان می‌کند. این مقدار تعیین می‌کند که هنگام به‌روزرسانی، کدام متن در بخش فیلد تنظیم شود.

## سازنده‌ها

| سازنده | توضیح |
| --- | --- |
| [FieldType(String str)](#FieldType-java.lang.String-) | یک نمونه جدید از کلاس FieldType را مقداردهی اولیه می‌کند. |

## متدها

| متد | توضیح |
| --- | --- |
| [getInternalString()](#getInternalString--) | نام داخلی این شیء FieldType را بازمی‌گرداند. |
| [setInternalString(String value)](#setInternalString-java.lang.String-) | نام داخلی این شیء FieldType را بازمی‌گرداند. |
| [equals(Object obj)](#equals-java.lang.Object-) | بررسی می‌کند که آیا این فیلد با فیلد دیگری برابر است یا خیر. |
| [hashCode()](#hashCode--) | کد هش این شیء را بازمی‌گرداند. |
| [op_Equality(FieldType a, FieldType b)](#op-Equality-com.aspose.slides.FieldType-com.aspose.slides.FieldType-) | بررسی می‌کند که آیا دو شیء FieldType برابر هستند یا خیر. |
| [op_Inequality(FieldType a, FieldType b)](#op-Inequality-com.aspose.slides.FieldType-com.aspose.slides.FieldType-) | بررسی می‌کند که آیا دو شیء FieldType نامساوی هستند یا خیر. |
| [getSlideNumber()](#getSlideNumber--) | شماره اسلاید فعلی. |
| [getFooter()](#getFooter--) | پاورقی اسلاید. |
| [getHeader()](#getHeader--) | سرصفحه اسلاید. |
| [getDateTime()](#getDateTime--) | تاریخ و زمان فعلی در قالب پیش‌فرض تاریخ-زمان برای برنامه رندر. |
| [getDateTime1()](#getDateTime1--) | تاریخ و زمان فعلی در اولین قالب پیش‌ تعریف‌شده (MM/DD/YYYY برای انگلیسی). |
| [getDateTime2()](#getDateTime2--) | تاریخ و زمان فعلی در دومین قالب پیش‌ تعریف‌شده (Day, Month DD, YYYY برای انگلیسی). |
| [getDateTime3()](#getDateTime3--) | تاریخ و زمان فعلی در سومین قالب پیش‌ تعریف‌شده (DD Month YYYY برای انگلیسی). |
| [getDateTime4()](#getDateTime4--) | تاریخ و زمان فعلی در چهارمین قالب پیش‌ تعریف‌شده (Month DD, YYYY برای انگلیسی). |
| [getDateTime5()](#getDateTime5--) | تاریخ و زمان فعلی در پنجمین قالب پیش‌ تعریف‌شده (DD-Mon-YY برای انگلیسی). |
| [getDateTime6()](#getDateTime6--) | تاریخ و زمان فعلی در ششمین قالب پیش‌ تعریف‌شده (Month YY برای انگلیسی). |
| [getDateTime7()](#getDateTime7--) | تاریخ و زمان فعلی در هفتمین قالب پیش‌ تعریف‌شده (Mon-YY برای انگلیسی). |
| [getDateTime8()](#getDateTime8--) | تاریخ و زمان فعلی در هشتمین قالب پیش‌ تعریف‌شده (MM/DD/YYYY hh:mm AM/PM برای انگلیسی). |
| [getDateTime9()](#getDateTime9--) | تاریخ و زمان فعلی در نهمین قالب پیش‌ تعریف‌شده (MM/DD/YYYY hh:mm:ss AM/PM برای انگلیسی). |
| [getDateTime10()](#getDateTime10--) | تاریخ و زمان فعلی در دهمین قالب پیش‌ تعریف‌شده (hh:mm برای انگلیسی). |
| [getDateTime11()](#getDateTime11--) | تاریخ و زمان فعلی در یازدهمین قالب پیش‌ تعریف‌شده (hh:mm:ss برای انگلیسی). |
| [getDateTime12()](#getDateTime12--) | تاریخ و زمان فعلی در دوازدهمین قالب پیش‌ تعریف‌شده (hh:mm AM/PM برای انگلیسی). |
| [getDateTime13()](#getDateTime13--) | تاریخ و زمان فعلی در سیزدهمین قالب پیش‌ تعریف‌شده (hh:mm:ss AM/PM برای انگلیسی). |

### FieldType(String str) {#FieldType-java.lang.String-}
```
public FieldType(String str)
```

یک نمونه جدید از کلاس FieldType را مقداردهی اولیه می‌کند.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| str | java.lang.String |  |

### getInternalString() {#getInternalString--}
```
public final String getInternalString()
```

نام داخلی این شیء FieldType را بازمی‌گرداند. خواندن/نوشتن String.

**مقدار بازگشتی:**
java.lang.String

### setInternalString(String value) {#setInternalString-java.lang.String-}
```
public final void setInternalString(String value)
```

نام داخلی این شیء FieldType را بازمی‌گرداند. خواندن/نوشتن String.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | java.lang.String |  |

### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```

بررسی می‌کند که آیا این فیلد با مورد دیگر برابر است یا خیر.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| obj | java.lang.Object | فیلد برای مقایسه. |

**مقدار بازگشتی:**
boolean - درست اگر فیلدها برابر باشند.

### hashCode() {#hashCode--}
```
public int hashCode()
```

کد هش این شیء را بازمی‌گرداند.

**مقدار بازگشتی:**
int - کد هش به‌صورت int.

### op_Equality(FieldType a, FieldType b) {#op-Equality-com.aspose.slides.FieldType-com.aspose.slides.FieldType-}
```
public static boolean op_Equality(FieldType a, FieldType b)
```

بررسی می‌کند که آیا دو شیء FieldType برابر هستند یا خیر.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| a | [FieldType](../../com.aspose.slides/fieldtype) | اولین FieldType برای مقایسه. |
| b | [FieldType](../../com.aspose.slides/fieldtype) | دومین FieldType برای مقایسه. |

**مقدار بازگشتی:**
boolean - درست اگر اشیاء FieldType برابر باشند.

### op_Inequality(FieldType a, FieldType b) {#op-Inequality-com.aspose.slides.FieldType-com.aspose.slides.FieldType-}
```
public static boolean op_Inequality(FieldType a, FieldType b)
```

بررسی می‌کند که آیا دو شیء FieldType نامساوی هستند یا خیر.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| a | [FieldType](../../com.aspose.slides/fieldtype) | اولین FieldType برای مقایسه. |
| b | [FieldType](../../com.aspose.slides/fieldtype) | دومین FieldType برای مقایسه. |

**مقدار بازگشتی:**
boolean - درست اگر اشیاء FieldType برابر نباشند.

### getSlideNumber() {#getSlideNumber--}
```
public static FieldType getSlideNumber()
```

شماره اسلاید فعلی. فقط خواندنی [FieldType](../../com.aspose.slides/fieldtype).

**مقدار بازگشتی:**
[FieldType](../../com.aspose.slides/fieldtype)

### getFooter() {#getFooter--}
```
public static FieldType getFooter()
```

پاورقی اسلاید. فقط خواندنی [FieldType](../../com.aspose.slides/fieldtype).

**مقدار بازگشتی:**
[FieldType](../../com.aspose.slides/fieldtype)

### getHeader() {#getHeader--}
```
public static FieldType getHeader()
```

سرصفحه اسلاید. فقط خواندنی [FieldType](../../com.aspose.slides/fieldtype).

**مقدار بازگشتی:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime() {#getDateTime--}
```
public static FieldType getDateTime()
```

تاریخ و زمان فعلی در قالب پیش‌فرض تاریخ-زمان برای برنامه رندر. فقط خواندنی [FieldType](../../com.aspose.slides/fieldtype).

**مقدار بازگشتی:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime1() {#getDateTime1--}
```
public static FieldName getDateTime1()
```

تاریخ و زمان فعلی در اولین قالب پیش‌ تعریف‌شده (MM/DD/YYYY برای انگلیسی). فقط خواندنی [FieldType](../../com.aspose.slides/fieldtype).

**مقدار بازگشتی:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime2() {#getDateTime2--}
```
public static FieldType getDateTime2()
```

تاریخ و زمان فعلی در دومین قالب پیش‌ تعریف‌شده (Day, Month DD, YYYY برای انگلیسی). فقط خواندنی [FieldType](../../com.aspose.slides/fieldtype).

**مقدار بازگشتی:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime3() {#getDateTime3--}
```
public static FieldType getDateTime3()
```

تاریخ و زمان فعلی در سومین قالب پیش‌ تعریف‌شده (DD Month YYYY برای انگلیسی). فقط خواندنی [FieldType](../../com.aspose.slides/fieldtype).

**مقدار بازگشتی:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime4() {#getDateTime4--}
```
public static FieldType getDateTime4()
```

تاریخ و زمان فعلی در چهارمین قالب پیش‌ تعریف‌شده (Month DD, YYYY برای انگلیسی). فقط خواندنی [FieldType](../../com.aspose.slides/fieldtype).

**مقدار بازگشتی:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime5() {#getDateTime5--}
```
public static FieldType getDateTime5()
```

تاریخ و زمان فعلی در پنجمین قالب پیش‌ تعریف‌شده (DD-Mon-YY برای انگلیسی). فقط خواندنی [FieldType](../../com.aspose.slides/fieldtype).

**مقدار بازگشتی:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime6() {#getDateTime6--}
```
public static FieldType getDateTime6()
```

تاریخ و زمان فعلی در ششمین قالب پیش‌ تعریف‌شده (Month YY برای انگلیسی). فقط خواندنی [FieldType](../../com.aspose.slides/fieldtype).

**مقدار بازگشتی:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime7() {#getDateTime7--}
```
public static FieldType getDateTime7()
```

تاریخ و زمان فعلی در هفتمین قالب پیش‌ تعریف‌شده (Mon-YY برای انگلیسی). فقط خواندنی [FieldType](../../com.aspose.slides/fieldtype).

**مقدار بازگشتی:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime8() {#getDateTime8--}
```
public static FieldType getDateTime8()
```

تاریخ و زمان فعلی در هشتمین قالب پیش‌ تعریف‌شده (MM/DD/YYYY hh:mm AM/PM برای انگلیسی). فقط خواندنی [FieldType](../../com.aspose.slides/fieldtype).

**مقدار بازگشتی:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime9() {#getDateTime9--}
```
public static FieldType getDateTime9()
```

تاریخ و زمان فعلی در نهمین قالب پیش‌ تعریف‌شده (MM/DD/YYYY hh:mm:ss AM/PM برای انگلیسی). فقط خواندنی [FieldType](../../com.aspose.slides/fieldtype).

**مقدار بازگشتی:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime10() {#getDateTime10--}
```
public static FieldType getDateTime10()
```

تاریخ و زمان فعلی در دهمین قالب پیش‌ تعریف‌شده (hh:mm برای انگلیسی). فقط خواندنی [FieldType](../../com.aspose.slides/fieldtype).

**مقدار بازگشتی:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime11() {#getDateTime11--}
```
public static FieldType getDateTime11()
```

تاریخ و زمان فعلی در یازدهمین قالب پیش‌ تعریف‌شده (hh:mm:ss برای انگلیسی). فقط خواندنی [FieldType](../../com.aspose.slides/fieldtype).

**مقدار بازگشتی:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime12() {#getDateTime12--}
```
public static FieldType getDateTime12()
```

تاریخ و زمان فعلی در دوازدهمین قالب پیش‌ تعریف‌شده (hh:mm AM/PM برای انگلیسی). فقط خواندنی [FieldType](../../com.aspose.slides/fieldtype).

**مقدار بازگشتی:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime13() {#getDateTime13--}
```
public static FieldType getDateTime13()
```

تاریخ و زمان فعلی در سیزدهمین قالب پیش‌ تعریف‌شده (hh:mm:ss AM/PM برای انگلیسی). فقط خواندنی [FieldType](../../com.aspose.slides/fieldtype).

**مقدار بازگشتی:**
[FieldType](../../com.aspose.slides/fieldtype)