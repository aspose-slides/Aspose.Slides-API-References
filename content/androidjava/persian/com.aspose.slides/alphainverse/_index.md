---
title: AlphaInverse
second_title: Aspose.Slides برای Android از طریق مرجع API Java
description: نمایانگر یک اثر معکوس الفا است.
type: docs
url: /fa/com.aspose.slides/alphainverse/
---
**Inheritance:**  
وراثت:

[com.aspose.slides.PVIObject](../../com.aspose.slides/pviobject), [com.aspose.slides.ImageTransformOperation](../../com.aspose.slides/imagetransformoperation)

**All Implemented Interfaces:**  
تمام رابط‌های پیاده‌سازی‌شده:  
[com.aspose.slides.IAlphaInverse](../../com.aspose.slides/ialphainverse), com.aspose.slides.IVisualEffect  
```
public final class AlphaInverse extends ImageTransformOperation implements IAlphaInverse, IVisualEffect
```

نمایانگر یک اثر معکوس الفا است. مقادیر الفا (شفافیت) با کم کردن از 100٪ معکوس می‌شوند.

## Methods
## متدها

| Method | Description |
| --- | --- |
| [getEffective()](#getEffective--) | داده‌های اثر معکوس الفای مؤثر را با اعمال وراثت دریافت می‌کند. |
| [getVersion()](#getVersion--) |  |
| [equals(Object obj)](#equals-java.lang.Object-) | تعیین می‌کند که آیا [AlphaInverse](../../com.aspose.slides/alphainverse) مشخص‌شده با [AlphaInverse](../../com.aspose.slides/alphainverse) فعلی برابر است یا خیر. |
| [hashCode()](#hashCode--) | به‌عنوان تابع هش برای یک نوع خاص عمل می‌کند. |

### getEffective() {#getEffective--}
```
public final IAlphaInverseEffectiveData getEffective()
```

داده‌های اثر معکوس الفای مؤثر را با اعمال وراثت دریافت می‌کند.

**Returns:**  
بازگشت:  
[IAlphaInverseEffectiveData](../../com.aspose.slides/ialphainverseeffectivedata) - A [IAlphaInverseEffectiveData](../../com.aspose.slides/ialphainverseeffectivedata).

### getVersion() {#getVersion--}
```
public long getVersion()
```

نسخه. فقط-خواندنی long.

**Returns:**  
بازگشت:  
long

### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```

تعیین می‌کند که آیا [AlphaInverse](../../com.aspose.slides/alphainverse) مشخص‌شده با [AlphaInverse](../../com.aspose.slides/alphainverse) فعلی برابر است یا خیر.

**Parameters:**  
پارامترها:
| Parameter | Type | Description |
| --- | --- | --- |
| obj | java.lang.Object | [AlphaInverse](../../com.aspose.slides/alphainverse) برای مقایسه. |

**Returns:**  
بازگشت:  
boolean - اگر اشیا برابر باشند true؛ در غیر این صورت false.

### hashCode() {#hashCode--}
```
public int hashCode()
```

به‌عنوان تابع هش برای یک نوع خاص عمل می‌کند.

**Returns:**  
بازگشت:  
int - یک کد هش برای شیء فعلی.