---
title: PathCommandType
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: نمایش‌دهندهٔ دستورات مسیر گرافیک
type: docs
url: /fa/com.aspose.slides/pathcommandtype/
---
**ارث‌بری:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class PathCommandType extends System.Enum
```

نمایش‌دهندهٔ دستورات مسیر گرافیک
## فیلدها

| فیلد | توضیح |
| --- | --- |
| [Close](#Close) | مسیر هندسی را می‌بندد |
| [MoveTo](#MoveTo) | موقعیت نقطهٔ بعدی مسیر هندسی را تنظیم می‌کند |
| [LineTo](#LineTo) | خطی را به مسیر هندسی اضافه می‌کند |
| [ArcTo](#ArcTo) | قوس مشخص‌شده را به مسیر هندسی اضافه می‌کند. |
| [QuadBezierTo](#QuadBezierTo) | منحنی بزیه‌ی درجهٔ دو را به مسیر هندسی اضافه می‌کند |
| [CubicBezierTo](#CubicBezierTo) | منحنی بزیه‌ی درجهٔ سه را به مسیر هندسی اضافه می‌کند |
### Close {#Close}
```
public static final byte Close
```

مسیر هندسی را می‌بندد

### MoveTo {#MoveTo}
```
public static final byte MoveTo
```

موقعیت نقطهٔ بعدی مسیر هندسی را تنظیم می‌کند

### LineTo {#LineTo}
```
public static final byte LineTo
```

خطی را به مسیر هندسی اضافه می‌کند

### ArcTo {#ArcTo}
```
public static final byte ArcTo
```

قوس مشخص‌شده را به مسیر هندسی اضافه می‌کند.

### QuadBezierTo {#QuadBezierTo}
```
public static final byte QuadBezierTo
```

منحنی بزیه‌ی درجهٔ دو را به مسیر هندسی اضافه می‌کند

### CubicBezierTo {#CubicBezierTo}
```
public static final byte CubicBezierTo
```

منحنی بزیه‌ی درجهٔ سه را به مسیر هندسی اضافه می‌کند