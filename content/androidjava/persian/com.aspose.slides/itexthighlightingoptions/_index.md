---
title: ITextHighlightingOptions
second_title: Aspose.Slides for Android via Java API Reference
description: Represents options which can be used to highlight text in text frame.
type: docs
url: /fa/com.aspose.slides/itexthighlightingoptions/
---```
public interface ITextHighlightingOptions
```

نمایش گزینه‌هایی که می‌توان برای برجسته‌سازی متن در فریم متن استفاده کرد.
## متدها

| متد | توضیح |
| --- | --- |
| [getCaseSensitive()](#getCaseSensitive--) | در صورتی که true تنظیم شود، جستجوی حساس به حروف استفاده می‌شود؛ در غیر این صورت false. |
| [setCaseSensitive(boolean value)](#setCaseSensitive-boolean-) | در صورتی که true تنظیم شود، جستجوی حساس به حروف استفاده می‌شود؛ در غیر این صورت false. |
| [getWholeWordsOnly()](#getWholeWordsOnly--) | در صورتی که true تنظیم شود، فقط کلمات کامل مطابقت داده می‌شوند؛ در غیر این صورت false. |
| [setWholeWordsOnly(boolean value)](#setWholeWordsOnly-boolean-) | در صورتی که true تنظیم شود، فقط کلمات کامل مطابقت داده می‌شوند؛ در غیر این صورت false. |
### getCaseSensitive() {#getCaseSensitive--}
```
public abstract boolean getCaseSensitive()
```


در صورتی که true تنظیم شود، جستجوی حساس به حروف استفاده می‌شود؛ در غیر این صورت false. خواندنی/نوشتنی boolean.

**باز می‌گردد:**
boolean
### setCaseSensitive(boolean value) {#setCaseSensitive-boolean-}
```
public abstract void setCaseSensitive(boolean value)
```


در صورتی که true تنظیم شود، جستجوی حساس به حروف استفاده می‌شود؛ در غیر این صورت false. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getWholeWordsOnly() {#getWholeWordsOnly--}
```
public abstract boolean getWholeWordsOnly()
```


در صورتی که true تنظیم شود، فقط کلمات کامل مطابقت داده می‌شوند؛ در غیر این صورت false. خواندنی/نوشتنی boolean.

**باز می‌گردد:**
boolean
### setWholeWordsOnly(boolean value) {#setWholeWordsOnly-boolean-}
```
public abstract void setWholeWordsOnly(boolean value)
```


در صورتی که true تنظیم شود، فقط کلمات کامل مطابقت داده می‌شوند؛ در غیر این صورت false. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |