---
title: INotesSlideManager
second_title: Aspose.Slides for Android via Java API Reference
description: مدیر اسلاید یادداشت‌ها.
type: docs
url: /fa/com.aspose.slides/inotesslidemanager/
---```
public interface INotesSlideManager
```

مدیر اسلاید یادداشت‌ها.
## متدها

| متد | توضیح |
| --- | --- |
| [getNotesSlide()](#getNotesSlide--) | اسلاید یادداشت‌ها را برای اسلاید فعلی برمی‌گرداند. |
| [addNotesSlide()](#addNotesSlide--) | اسلاید یادداشت‌ها را برای اسلاید فعلی برمی‌گرداند و در صورت عدم وجود، یک اسلاید جدید ایجاد می‌کند. |
| [removeNotesSlide()](#removeNotesSlide--) | اسلاید یادداشت‌های اسلاید فعلی را حذف می‌کند. |
### getNotesSlide() {#getNotesSlide--}
```
public abstract INotesSlide getNotesSlide()
```


اسلاید یادداشت‌ها را برای اسلاید فعلی برمی‌گرداند. اگر اسلاید یادداشت نداشته باشد null برمی‌گرداند. فقط-خواندنی [INotesSlide](../../com.aspose.slides/inotesslide).

**برگشت:**
[INotesSlide](../../com.aspose.slides/inotesslide)
### addNotesSlide() {#addNotesSlide--}
```
public abstract INotesSlide addNotesSlide()
```


اسلاید یادداشت‌ها را برای اسلاید فعلی برمی‌گرداند و در صورت عدم وجود، یک اسلاید جدید ایجاد می‌کند.

**برگشت:**
[INotesSlide](../../com.aspose.slides/inotesslide) - [INotesSlide](../../com.aspose.slides/inotesslide) برای این اسلاید.
### removeNotesSlide() {#removeNotesSlide--}
```
public abstract void removeNotesSlide()
```


اسلاید یادداشت‌های اسلاید فعلی را حذف می‌کند.