---
title: GroupShapeLock
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: تعیین می‌کند کدام عملیات‌ها بر روی GroupShape والد غیرفعال هستند.
type: docs
url: /fa/com.aspose.slides/groupshapelock/
---
**ارث‌بری:**
java.lang.Object, [com.aspose.slides.BaseShapeLock](../../com.aspose.slides/baseshapelock)

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IGroupShapeLock](../../com.aspose.slides/igroupshapelock)
```
public class GroupShapeLock extends BaseShapeLock implements IGroupShapeLock
```

تعیین می‌کند کدام عملیات‌ها در GroupShape والد غیرفعال هستند.
## متدها

| متد | توضیح |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | تعیین می‌کند آیا افزودن این شکل به یک گروه ممنوع است یا نه. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | تعیین می‌کند آیا افزودن این شکل به یک گروه ممنوع است یا نه. |
| [getUngroupingLocked()](#getUngroupingLocked--) | تعیین می‌کند آیا تقسیم این GroupShape ممنوع است یا نه. |
| [setUngroupingLocked(boolean value)](#setUngroupingLocked-boolean-) | تعیین می‌کند آیا تقسیم این GroupShape ممنوع است یا نه. |
| [getSelectLocked()](#getSelectLocked--) | تعیین می‌کند آیا انتخاب این شکل ممنوع است یا نه. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | تعیین می‌کند آیا انتخاب این شکل ممنوع است یا نه. |
| [getRotationLocked()](#getRotationLocked--) | تعیین می‌کند آیا تغییر زاویه چرخش این شکل ممنوع است یا نه. |
| [setRotationLocked(boolean value)](#setRotationLocked-boolean-) | تعیین می‌کند آیا تغییر زاویه چرخش این شکل ممنوع است یا نه. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | تعیین می‌کند آیا شکل باید نسبت عرض به ارتفاع را هنگام تغییر اندازه حفظ کند. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | تعیین می‌کند آیا شکل باید نسبت عرض به ارتفاع را هنگام تغییر اندازه حفظ کند. |
| [getPositionLocked()](#getPositionLocked--) | تعیین می‌کند آیا جابه‌جایی این شکل ممنوع است یا نه. |
| [setPositionLocked(boolean value)](#setPositionLocked-boolean-) | تعیین می‌کند آیا جابه‌جایی این شکل ممنوع است یا نه. |
| [getSizeLocked()](#getSizeLocked--) | تعیین می‌کند آیا تغییر اندازه این شکل ممنوع است یا نه. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | تعیین می‌کند آیا تغییر اندازه این شکل ممنوع است یا نه. |
### getGroupingLocked() {#getGroupingLocked--}
```
public boolean getGroupingLocked()
```

تعیین می‌کند آیا افزودن این شکل به یک گروه ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public void setGroupingLocked(boolean value)
```

تعیین می‌کند آیا افزودن این شکل به یک گروه ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getUngroupingLocked() {#getUngroupingLocked--}
```
public boolean getUngroupingLocked()
```

تعیین می‌کند آیا تقسیم این GroupShape ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setUngroupingLocked(boolean value) {#setUngroupingLocked-boolean-}
```
public void setUngroupingLocked(boolean value)
```

تعیین می‌کند آیا تقسیم این GroupShape ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getSelectLocked() {#getSelectLocked--}
```
public boolean getSelectLocked()
```

تعیین می‌کند آیا انتخاب این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public void setSelectLocked(boolean value)
```

تعیین می‌کند آیا انتخاب این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getRotationLocked() {#getRotationLocked--}
```
public boolean getRotationLocked()
```

تعیین می‌کند آیا تغییر زاویه چرخش این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setRotationLocked(boolean value) {#setRotationLocked-boolean-}
```
public void setRotationLocked(boolean value)
```

تعیین می‌کند آیا تغییر زاویه چرخش این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public boolean getAspectRatioLocked()
```

تعیین می‌کند آیا شکل باید نسبت عرض به ارتفاع را هنگام تغییر اندازه حفظ کند. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public void setAspectRatioLocked(boolean value)
```

تعیین می‌کند آیا شکل باید نسبت عرض به ارتفاع را هنگام تغییر اندازه حفظ کند. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getPositionLocked() {#getPositionLocked--}
```
public boolean getPositionLocked()
```

تعیین می‌کند آیا جابه‌جایی این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setPositionLocked(boolean value) {#setPositionLocked-boolean-}
```
public void setPositionLocked(boolean value)
```

تعیین می‌کند آیا جابه‌جایی این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getSizeLocked() {#getSizeLocked--}
```
public boolean getSizeLocked()
```

تعیین می‌کند آیا تغییر اندازه این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public void setSizeLocked(boolean value)
```

تعیین می‌کند آیا تغییر اندازه این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |