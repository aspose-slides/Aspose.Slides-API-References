---
title: ChartWall
second_title: Aspose.Slides برای اندروید از طریق مرجع API جاوا
description: دیوارها را در نمودارهای سه‌بعدی نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/chartwall/
---
**وراثت:**
java.lang.Object, com.aspose.slides.DomObject

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IChartWall](../../com.aspose.slides/ichartwall)
```
public class ChartWall extends DomObject<Chart> implements IChartWall
```

دیوارها را در چارت‌های سه‌بعدی نشان می‌دهد.
## متدها

| متد | توضیح |
| --- | --- |
| [getThickness()](#getThickness--) | ضخامت دیوارها را به عنوان درصدی از بزرگ‌ترین بعد حجم نمودار بر می‌گرداند یا تنظیم می‌کند. |
| [setThickness(int value)](#setThickness-int-) | ضخامت دیوارها را به عنوان درصدی از بزرگ‌ترین بعد حجم نمودار بر می‌گرداند یا تنظیم می‌کند. |
| [getFormat()](#getFormat--) | پر کردن دیوار، خط، اثر و سبک‌های سه‌بعدی را بر می‌گرداند. |
| [getPictureType()](#getPictureType--) | نوع تصویر را بر می‌گرداند یا تنظیم می‌کند. |
| [setPictureType(int value)](#setPictureType-int-) | نوع تصویر را بر می‌گرداند یا تنظیم می‌کند. |
### getThickness() {#getThickness--}
```
public final int getThickness()
```

ضخامت دیوارها را به عنوان درصدی از بزرگ‌ترین بعد حجم نمودار بر می‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن int.

**بازگشت:**
int
### setThickness(int value) {#setThickness-int-}
```
public final void setThickness(int value)
```

ضخامت دیوارها را به عنوان درصدی از بزرگ‌ترین بعد حجم نمودار بر می‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن int.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |
### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

پر کردن دیوار، خط، اثر و سبک‌های سه‌بعدی را بر می‌گرداند. فقط خواندنی [IFormat](../../com.aspose.slides/iformat).

**بازگشت:**
[IFormat](../../com.aspose.slides/iformat)
### getPictureType() {#getPictureType--}
```
public final int getPictureType()
```

نوع تصویر را بر می‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن [PictureType](../../com.aspose.slides/picturetype).

**بازگشت:**
int
### setPictureType(int value) {#setPictureType-int-}
```
public final void setPictureType(int value)
```

نوع تصویر را بر می‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن [PictureType](../../com.aspose.slides/picturetype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |