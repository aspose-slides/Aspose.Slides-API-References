---
title: ITiming
second_title: Aspose.Slides for Android via Java API Reference
description: Represents animation timing.
type: docs
url: /fa/com.aspose.slides/itiming/
---```
public interface ITiming
```

نمایش زمان‌بندی انیمیشن.
## متدها

| متد | توضیح |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | درصد رفتار تسریع مدت زمان اثر را توصیف می‌کند. |
| [setAccelerate(float value)](#setAccelerate-float-) | درصد رفتار تسریع مدت زمان اثر را توصیف می‌کند. |
| [getDecelerate()](#getDecelerate--) | درصد رفتار کاهش سرعت مدت زمان اثر را توصیف می‌کند. |
| [setDecelerate(float value)](#setDecelerate-float-) | درصد رفتار کاهش سرعت مدت زمان اثر را توصیف می‌کند. |
| [getAutoReverse()](#getAutoReverse--) | توضیح می‌دهد که آیا پس از پخش در جهت جلو به‌طور خودکار انیمیشن به‌طرف عقب پخش شود. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | توضیح می‌دهد که آیا پس از پخش در جهت جلو به‌طور خودکار انیمیشن به‌طرف عقب پخش شود. |
| [getDuration()](#getDuration--) | مدت زمان اثر انیمیشن را توصیف می‌کند. |
| [setDuration(float value)](#setDuration-float-) | مدت زمان اثر انیمیشن را توصیف می‌کند. |
| [getRepeatCount()](#getRepeatCount--) | تعداد دفعات تکرار اثر را توصیف می‌کند. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | تعداد دفعات تکرار اثر را توصیف می‌کند. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | این ویژگی مشخص می‌کند که آیا اثر تا پایان اسلاید تکرار شود. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | این ویژگی مشخص می‌کند که آیا اثر تا پایان اسلاید تکرار شود. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | این ویژگی مشخص می‌کند که آیا اثر تا کلیک بعدی تکرار شود. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | این ویژگی مشخص می‌کند که آیا اثر تا کلیک بعدی تکرار شود. |
| [getRepeatDuration()](#getRepeatDuration--) | تعداد دفعات تکرار اثر را توصیف می‌کند. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | تعداد دفعات تکرار اثر را توصیف می‌کند. |
| [getRestart()](#getRestart--) | مشخص می‌کند که آیا اثر پس از تکمیل دوباره شروع شود. |
| [setRestart(int value)](#setRestart-int-) | مشخص می‌کند که آیا اثر پس از تکمیل دوباره شروع شود. |
| [getSpeed()](#getSpeed--) | درصدی که زمان‌بندی باید تسریع (یا کند شود) را مشخص می‌کند. |
| [setSpeed(float value)](#setSpeed-float-) | درصدی که زمان‌بندی باید تسریع (یا کند شود) را مشخص می‌کند. |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | زمان تاخیر پس از فعال‌ساز را توصیف می‌کند. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | زمان تاخیر پس از فعال‌ساز را توصیف می‌کند. |
| [getTriggerType()](#getTriggerType--) | نوع فعال‌ساز را توصیف می‌کند. |
| [setTriggerType(int value)](#setTriggerType-int-) | نوع فعال‌ساز را توصیف می‌کند. |
| [getRewind()](#getRewind--) | این ویژگی مشخص می‌کند که آیا اثر پس از اتمام پخش، به عقب بازگردد. |
| [setRewind(boolean value)](#setRewind-boolean-) | این ویژگی مشخص می‌کند که آیا اثر پس از اتمام پخش، به عقب بازگردد. |
### getAccelerate() {#getAccelerate--}
```
public abstract float getAccelerate()
```

درصد رفتار تسریع مدت زمان اثر را توصیف می‌کند. خواند/نوشت float.

**Returns:**
float
### setAccelerate(float value) {#setAccelerate-float-}
```
public abstract void setAccelerate(float value)
```

درصد رفتار تسریع مدت زمان اثر را توصیف می‌کند. خواند/نوشت float.

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getDecelerate() {#getDecelerate--}
```
public abstract float getDecelerate()
```

درصد رفتار کاهش سرعت مدت زمان اثر را توصیف می‌کند. خواند/نوشت float.

**Returns:**
float
### setDecelerate(float value) {#setDecelerate-float-}
```
public abstract void setDecelerate(float value)
```

درصد رفتار کاهش سرعت مدت زمان اثر را توصیف می‌کند. خواند/نوشت float.

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getAutoReverse() {#getAutoReverse--}
```
public abstract boolean getAutoReverse()
```

توضیح می‌دهد که آیا پس از پخش در جهت جلو به‌طور خودکار انیمیشن به‌طرف عقب پخش شود. خواند/نوشت boolean.

**Returns:**
boolean
### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public abstract void setAutoReverse(boolean value)
```

توضیح می‌دهد که آیا پس از پخش در جهت جلو به‌طور خودکار انیمیشن به‌طرف عقب پخش شود. خواند/نوشت boolean.

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |
### getDuration() {#getDuration--}
```
public abstract float getDuration()
```

مدت زمان اثر انیمیشن را توصیف می‌کند. خواند/نوشت float.

**Returns:**
float
### setDuration(float value) {#setDuration-float-}
```
public abstract void setDuration(float value)
```

مدت زمان اثر انیمیشن را توصیف می‌کند. خواند/نوشت float.

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getRepeatCount() {#getRepeatCount--}
```
public abstract float getRepeatCount()
```

تعداد دفعات تکرار اثر را توصیف می‌کند. خواند/نوشت float.

**Returns:**
float
### setRepeatCount(float value) {#setRepeatCount-float-}
```
public abstract void setRepeatCount(float value)
```

تعداد دفعات تکرار اثر را توصیف می‌کند. خواند/نوشت float.

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public abstract boolean getRepeatUntilEndSlide()
```

این ویژگی مشخص می‌کند که آیا اثر تا پایان اسلاید تکرار شود. خواند/نوشت boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت توالی افکت‌ها برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین افکت از توالی اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // تغییر زمان‌بندی/تکرار افکت به "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Returns:**
boolean
### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public abstract void setRepeatUntilEndSlide(boolean value)
```

این ویژگی مشخص می‌کند که آیا اثر تا پایان اسلاید تکرار شود. خواند/نوشت boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت توالی افکت‌ها برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین افکت از توالی اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // تغییر زمان‌بندی/تکرار افکت به "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |
### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public abstract boolean getRepeatUntilNextClick()
```

این ویژگی مشخص می‌کند که آیا اثر تا کلیک بعدی تکرار شود. خواند/نوشت boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت توالی افکت‌ها برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین افکت از توالی اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // تغییر زمان‌بندی/تکرار افکت به "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Returns:**
boolean
### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public abstract void setRepeatUntilNextClick(boolean value)
```

این ویژگی مشخص می‌کند که آیا اثر تا کلیک بعدی تکرار شود. خواند/نوشت boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت توالی افکت‌ها برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین افکت از توالی اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // تغییر زمان‌بندی/تکرار افکت به "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |
### getRepeatDuration() {#getRepeatDuration--}
```
public abstract float getRepeatDuration()
```

تعداد دفعات تکرار اثر را توصیف می‌کند. خواند/نوشت float.

**Returns:**
float
### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public abstract void setRepeatDuration(float value)
```

تعداد دفعات تکرار اثر را توصیف می‌کند. خواند/نوشت float.

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getRestart() {#getRestart--}
```
public abstract int getRestart()
```

مشخص می‌کند که آیا اثر پس از تکمیل دوباره شروع شود. خواند/نوشت [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Returns:**
int
### setRestart(int value) {#setRestart-int-}
```
public abstract void setRestart(int value)
```

مشخص می‌کند که آیا اثر پس از تکمیل دوباره شروع شود. خواند/نوشت [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | int |  |
### getSpeed() {#getSpeed--}
```
public abstract float getSpeed()
```

درصدی که زمان‌بندی باید تسریع (یا کند شود) را مشخص می‌کند. خواند/نوشت float.

**Returns:**
float
### setSpeed(float value) {#setSpeed-float-}
```
public abstract void setSpeed(float value)
```

درصدی که زمان‌بندی باید تسریع (یا کند شود) را مشخص می‌کند. خواند/نوشت float.

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public abstract float getTriggerDelayTime()
```

زمان تاخیر پس از فعال‌ساز را توصیف می‌کند. خواند/نوشت float.

**Returns:**
float
### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public abstract void setTriggerDelayTime(float value)
```

زمان تاخیر پس از فعال‌ساز را توصیف می‌کند. خواند/نوشت float.

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getTriggerType() {#getTriggerType--}
```
public abstract int getTriggerType()
```

نوع فعال‌ساز را توصیف می‌کند. خواند/نوشت [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Returns:**
int
### setTriggerType(int value) {#setTriggerType-int-}
```
public abstract void setTriggerType(int value)
```

نوع فعال‌ساز را توصیف می‌کند. خواند/نوشت [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | int |  |
### getRewind() {#getRewind--}
```
public abstract boolean getRewind()
```

این ویژگی مشخص می‌کند که آیا اثر پس از اتمام پخش، به عقب بازگردد. خواند/نوشت boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت توالی افکت‌ها برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین افکت از توالی اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // فعال‌سازی زمان‌بندی/بازگرداندن افکت.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Returns:**
boolean
### setRewind(boolean value) {#setRewind-boolean-}
```
public abstract void setRewind(boolean value)
```

این ویژگی مشخص می‌کند که آیا اثر پس از اتمام پخش، به عقب بازگردد. خواند/نوشت boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت توالی افکت‌ها برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین افکت از توالی اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // فعال‌سازی زمان‌بندی/بازگرداندن افکت.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |