---
title: GradientFormat
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: یک قالب گرادیان را نمایش می‌دهد.
type: docs
url: /fa/com.aspose.slides/gradientformat/
---
**ارث‌بری:**
java.lang.Object, [com.aspose.slides.PVIObject](../../com.aspose.slides/pviobject)

**تمام رابط‌های پیاده‌سازی شده:**
[com.aspose.slides.IGradientFormat](../../com.aspose.slides/igradientformat)
```
public final class GradientFormat extends PVIObject implements IGradientFormat
```

یک قالب گرادیان را نمایش می‌دهد.

## متدها

| متد | توضیح |
| --- | --- |
| [getVersion()](#getVersion--) |  |
| [getTileFlip()](#getTileFlip--) | مقدار یا تنظیم حالت چرخش برای یک گرادیان. |
| [setTileFlip(int value)](#setTileFlip-int-) | مقدار یا تنظیم حالت چرخش برای یک گرادیان. |
| [getGradientDirection()](#getGradientDirection--) | مقدار یا تنظیم سبک یک گرادیان. |
| [setGradientDirection(int value)](#setGradientDirection-int-) | مقدار یا تنظیم سبک یک گرادیان. |
| [getLinearGradientAngle()](#getLinearGradientAngle--) | مقدار یا تنظیم زاویه یک گرادیان. |
| [setLinearGradientAngle(float value)](#setLinearGradientAngle-float-) | مقدار یا تنظیم زاویه یک گرادیان. |
| [getLinearGradientScaled()](#getLinearGradientScaled--) | مشخص می‌کند آیا یک گرادیان مقیاس‌بندی شده است. |
| [setLinearGradientScaled(byte value)](#setLinearGradientScaled-byte-) | مشخص می‌کند آیا یک گرادیان مقیاس‌بندی شده است. |
| [getGradientShape()](#getGradientShape--) | مقدار یا تنظیم شکل یک گرادیان. |
| [setGradientShape(byte value)](#setGradientShape-byte-) | مقدار یا تنظیم شکل یک گرادیان. |
| [getGradientStops()](#getGradientStops--) | مجموعه‌ای از توقف‌های گرادیان را باز می‌گرداند. |

### getVersion() {#getVersion--}
```
public long getVersion()
```

نسخه. فقط‌خواندنی long.

**بازمی‌گرداند:**
long

### getTileFlip() {#getTileFlip--}
```
public final int getTileFlip()
```

مقدار یا تنظیم حالت چرخش برای یک گرادیان. خواندن/نوشتن [TileFlip](../../com.aspose.slides/tileflip).

**بازمی‌گرداند:**
int

### setTileFlip(int value) {#setTileFlip-int-}
```
public final void setTileFlip(int value)
```

مقدار یا تنظیم حالت چرخش برای یک گرادیان. خواندن/نوشتن [TileFlip](../../com.aspose.slides/tileflip).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getGradientDirection() {#getGradientDirection--}
```
public final int getGradientDirection()
```

مقدار یا تنظیم سبک یک گرادیان. خواندن/نوشتن [GradientDirection](../../com.aspose.slides/gradientdirection).

**بازمی‌گرداند:**
int

### setGradientDirection(int value) {#setGradientDirection-int-}
```
public final void setGradientDirection(int value)
```

مقدار یا تنظیم سبک یک گرادیان. خواندن/نوشتن [GradientDirection](../../com.aspose.slides/gradientdirection).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getLinearGradientAngle() {#getLinearGradientAngle--}
```
public final float getLinearGradientAngle()
```

مقدار یا تنظیم زاویه یک گرادیان. خواندن/نوشتن float.

**بازمی‌گرداند:**
float

### setLinearGradientAngle(float value) {#setLinearGradientAngle-float-}
```
public final void setLinearGradientAngle(float value)
```

مقدار یا تنظیم زاویه یک گرادیان. خواندن/نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getLinearGradientScaled() {#getLinearGradientScaled--}
```
public final byte getLinearGradientScaled()
```

مشخص می‌کند آیا یک گرادیان مقیاس‌بندی شده است. خواندن/نوشتن [NullableBool](../../com.aspose.slides/nullablebool).

**بازمی‌گرداند:**
byte

### setLinearGradientScaled(byte value) {#setLinearGradientScaled-byte-}
```
public final void setLinearGradientScaled(byte value)
```

مشخص می‌کند آیا یک گرادیان مقیاس‌بندی شده است. خواندن/نوشتن [NullableBool](../../com.aspose.slides/nullablebool).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | byte |  |

### getGradientShape() {#getGradientShape--}
```
public final byte getGradientShape()
```

مقدار یا تنظیم شکل یک گرادیان. خواندن/نوشتن [GradientShape](../../com.aspose.slides/gradientshape).

**بازمی‌گرداند:**
byte

### setGradientShape(byte value) {#setGradientShape-byte-}
```
public final void setGradientShape(byte value)
```

مقدار یا تنظیم شکل یک گرادیان. خواندن/نوشتن [GradientShape](../../com.aspose.slides/gradientshape).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | byte |  |

### getGradientStops() {#getGradientStops--}
```
public final IGradientStopCollection getGradientStops()
```

مجموعه‌ای از توقف‌های گرادیان را باز می‌گرداند. فقط‌خواندنی [IGradientStopCollection](../../com.aspose.slides/igradientstopcollection).

**بازمی‌گرداند:**
[IGradientStopCollection](../../com.aspose.slides/igradientstopcollection)