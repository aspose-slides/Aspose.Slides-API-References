---
title: IGroupShapeLock
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: تعیین می‌کند کدام عملیات‌ها بر روی GroupShape والد غیرفعال هستند.
type: docs
url: /fa/com.aspose.slides/igroupshapelock/
---
**All Implemented Interfaces:**
[com.aspose.slides.IBaseShapeLock](../../com.aspose.slides/ibaseshapelock)
```
public interface IGroupShapeLock extends IBaseShapeLock
```

تعیین می‌کند کدام عملیات‌ها روی GroupShape والد غیرفعال هستند.
## متدها

| Method | Description |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | تعیین می‌کند آیا افزودن این شکل به یک گروه ممنوع است. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | تعیین می‌کند آیا افزودن این شکل به یک گروه ممنوع است. |
| [getUngroupingLocked()](#getUngroupingLocked--) | تعیین می‌کند آیا تقسیم این گروه‌شکل ممنوع است. |
| [setUngroupingLocked(boolean value)](#setUngroupingLocked-boolean-) | تعیین می‌کند آیا تقسیم این گروه‌شکل ممنوع است. |
| [getSelectLocked()](#getSelectLocked--) | تعیین می‌کند آیا انتخاب این شکل ممنوع است. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | تعیین می‌کند آیا انتخاب این شکل ممنوع است. |
| [getRotationLocked()](#getRotationLocked--) | تعیین می‌کند آیا تغییر زاویه چرخش این شکل ممنوع است. |
| [setRotationLocked(boolean value)](#setRotationLocked-boolean-) | تعیین می‌کند آیا تغییر زاویه چرخش این شکل ممنوع است. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | تعیین می‌کند آیا شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | تعیین می‌کند آیا شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. |
| [getPositionLocked()](#getPositionLocked--) | تعیین می‌کند آیا جابه‌جایی این شکل ممنوع است. |
| [setPositionLocked(boolean value)](#setPositionLocked-boolean-) | تعیین می‌کند آیا جابه‌جایی این شکل ممنوع است. |
| [getSizeLocked()](#getSizeLocked--) | تعیین می‌کند آیا تغییر اندازه این شکل ممنوع است. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | تعیین می‌کند آیا تغییر اندازه این شکل ممنوع است. |
### getGroupingLocked() {#getGroupingLocked--}
```
public abstract boolean getGroupingLocked()
```


تعیین می‌کند آیا افزودن این شکل به یک گروه ممنوع است. بولی خواندنی/قابل نوشتن.

**بازگشت:**
boolean
### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public abstract void setGroupingLocked(boolean value)
```


تعیین می‌کند آیا افزودن این شکل به یک گروه ممنوع است. بولی خواندنی/قابل نوشتن.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getUngroupingLocked() {#getUngroupingLocked--}
```
public abstract boolean getUngroupingLocked()
```


تعیین می‌کند آیا تقسیم این گروه‌شکل ممنوع است. بولی خواندنی/قابل نوشتن.

**بازگشت:**
boolean
### setUngroupingLocked(boolean value) {#setUngroupingLocked-boolean-}
```
public abstract void setUngroupingLocked(boolean value)
```


تعیین می‌کند آیا تقسیم این گروه‌شکل ممنوع است. بولی خواندنی/قابل نوشتن.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getSelectLocked() {#getSelectLocked--}
```
public abstract boolean getSelectLocked()
```


تعیین می‌کند آیا انتخاب این شکل ممنوع است. بولی خواندنی/قابل نوشتن.

**بازگشت:**
boolean
### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public abstract void setSelectLocked(boolean value)
```


تعیین می‌کند آیا انتخاب این شکل ممنوع است. بولی خواندنی/قابل نوشتن.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getRotationLocked() {#getRotationLocked--}
```
public abstract boolean getRotationLocked()
```


تعیین می‌کند آیا تغییر زاویه چرخش این شکل ممنوع است. بولی خواندنی/قابل نوشتن.

**بازگشت:**
boolean
### setRotationLocked(boolean value) {#setRotationLocked-boolean-}
```
public abstract void setRotationLocked(boolean value)
```


تعیین می‌کند آیا تغییر زاویه چرخش این شکل ممنوع است. بولی خواندنی/قابل نوشتن.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public abstract boolean getAspectRatioLocked()
```


تعیین می‌کند آیا شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. بولی خواندنی/قابل نوشتن.

**بازگشت:**
boolean
### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public abstract void setAspectRatioLocked(boolean value)
```


تعیین می‌کند آیا شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. بولی خواندنی/قابل نوشتن.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getPositionLocked() {#getPositionLocked--}
```
public abstract boolean getPositionLocked()
```


تعیین می‌کند آیا جابه‌جایی این شکل ممنوع است. بولی خواندنی/قابل نوشتن.

**بازگشت:**
boolean
### setPositionLocked(boolean value) {#setPositionLocked-boolean-}
```
public abstract void setPositionLocked(boolean value)
```


تعیین می‌کند آیا جابه‌جایی این شکل ممنوع است. بولی خواندنی/قابل نوشتن.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getSizeLocked() {#getSizeLocked--}
```
public abstract boolean getSizeLocked()
```


تعیین می‌کند آیا تغییر اندازه این شکل ممنوع است. بولی خواندنی/قابل نوشتن.

**بازگشت:**
boolean
### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public abstract void setSizeLocked(boolean value)
```


تعیین می‌کند آیا تغییر اندازه این شکل ممنوع است. بولی خواندنی/قابل نوشتن.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |