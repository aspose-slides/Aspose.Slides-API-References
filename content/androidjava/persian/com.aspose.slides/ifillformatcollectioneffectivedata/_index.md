---
title: IFillFormatCollectionEffectiveData
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: شیء غیرقابل تغییر که یک مجموعه‌ی فقط‌خواندنی از قالب‌های پرکننده مؤثر را نمایندگی می‌کند.
type: docs
url: /fa/com.aspose.slides/ifillformatcollectioneffectivedata/
---
**تمام رابط‌های پیاده‌سازی‌شده:**  
com.aspose.slides.IGenericCollection
```
public interface IFillFormatCollectionEffectiveData extends IGenericCollection<IFillFormatEffectiveData>
```

شیء غیرقابل تغییر که یک مجموعه‌ی فقط‌خواندنی از قالب‌های پرکننده مؤثر را نمایندگی می‌کند.

--------------------

این رابط به‌عنوان بخشی از [IFormatSchemeEffectiveData](../../com.aspose.slides/iformatschemeeffectivedata) استفاده می‌شود.
## متدها

| متد | توضیح |
| --- | --- |
| [get_Item(int index)](#get-Item-int-) | عنصری را در اندیس مشخص‌شده دریافت می‌کند. |
### get_Item(int index) {#get-Item-int-}
```
public abstract IFillFormatEffectiveData get_Item(int index)
```

عنصری را در اندیس مشخص‌شده دریافت می‌کند. فقط‌خواندنی [IFillFormatEffectiveData](../../com.aspose.slides/ifillformateffectivedata).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| index | int |  |

**بازگشت:**
[IFillFormatEffectiveData](../../com.aspose.slides/ifillformateffectivedata)