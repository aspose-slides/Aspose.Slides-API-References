---
title: BaseSlideHeaderFooterManager
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: نمایش‌دهنده‌ی مدیر که رفتار placeholderهای فوتر، تاریخ-زمان و شماره صفحه را برای تمام انواع اسلاید نگه می‌دارد.
type: docs
url: /fa/com.aspose.slides/baseslideheaderfootermanager/
---
**ارث‌بری:**  
java.lang.Object, [com.aspose.slides.BaseHeaderFooterManager](../../com.aspose.slides/baseheaderfootermanager)
```
public abstract class BaseSlideHeaderFooterManager extends BaseHeaderFooterManager
```

مدیری را نشان می‌دهد که رفتار placeholderهای فوتر، تاریخ-زمان و شماره صفحه را برای تمام انواع اسلاید نگه می‌دارد.

## متدها

| متد | توضیح |
| --- | --- |
| [isFooterVisible()](#isFooterVisible--) | مقداری را برمی‌گرداند که نشان می‌دهد یک placeholder فوتر موجود است. |
| [isSlideNumberVisible()](#isSlideNumberVisible--) | مقداری را برمی‌گرداند که نشان می‌دهد یک placeholder شماره صفحه موجود است. |
| [isDateTimeVisible()](#isDateTimeVisible--) | مقداری را برمی‌گرداند که نشان می‌دهد یک placeholder تاریخ-زمان موجود است. |
| [setFooterVisibility(boolean isVisible)](#setFooterVisibility-boolean-) | قابلیت مشاهده placeholder فوتر اسلاید را تغییر می‌دهد. |
| [setSlideNumberVisibility(boolean isVisible)](#setSlideNumberVisibility-boolean-) | قابلیت مشاهده placeholder شماره صفحه اسلاید را تغییر می‌دهد. |
| [setDateTimeVisibility(boolean isVisible)](#setDateTimeVisibility-boolean-) | قابلیت مشاهده placeholder تاریخ-زمان اسلاید را تغییر می‌دهد. |
| [setFooterText(String text)](#setFooterText-java.lang.String-) | متنی را برای placeholder فوتر اسلاید تنظیم می‌کند. |
| [setDateTimeText(String text)](#setDateTimeText-java.lang.String-) | متنی را برای placeholder تاریخ-زمان اسلاید تنظیم می‌کند. |

### isFooterVisible() {#isFooterVisible--}
```
public final boolean isFooterVisible()
```

مقداری را برمی‌گرداند که نشان می‌دهد یک placeholder فوتر موجود است. خواندن boolean.

**بازگشت:**  
boolean

### isSlideNumberVisible() {#isSlideNumberVisible--}
```
public final boolean isSlideNumberVisible()
```

مقداری را برمی‌گرداند که نشان می‌دهد یک placeholder شماره صفحه موجود است. خواندن boolean.

**بازگشت:**  
boolean

### isDateTimeVisible() {#isDateTimeVisible--}
```
public final boolean isDateTimeVisible()
```

مقداری را برمی‌گرداند که نشان می‌دهد یک placeholder تاریخ-زمان موجود است. خواندن boolean.

**بازگشت:**  
boolean

### setFooterVisibility(boolean isVisible) {#setFooterVisibility-boolean-}
```
public final void setFooterVisibility(boolean isVisible)
```

قابلیت مشاهده placeholder فوتر اسلاید را تغییر می‌دهد.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - placeholder فوتر را قابل مشاهده می‌کند، در غیر این صورت - آن را مخفی می‌کند. |

### setSlideNumberVisibility(boolean isVisible) {#setSlideNumberVisibility-boolean-}
```
public final void setSlideNumberVisibility(boolean isVisible)
```

قابلیت مشاهده placeholder شماره صفحه اسلاید را تغییر می‌دهد.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - placeholder شماره صفحه را قابل مشاهده می‌کند، در غیر این صورت - آن را مخفی می‌کند. |

### setDateTimeVisibility(boolean isVisible) {#setDateTimeVisibility-boolean-}
```
public final void setDateTimeVisibility(boolean isVisible)
```

قابلیت مشاهده placeholder تاریخ-زمان اسلاید را تغییر می‌دهد.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - placeholder تاریخ-زمان را قابل مشاهده می‌کند، در غیر این صورت - آن را مخفی می‌کند. |

### setFooterText(String text) {#setFooterText-java.lang.String-}
```
public final void setFooterText(String text)
```

متنی را برای placeholder فوتر اسلاید تنظیم می‌کند.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| text | java.lang.String | متنی برای تنظیم. |

### setDateTimeText(String text) {#setDateTimeText-java.lang.String-}
```
public final void setDateTimeText(String text)
```

متنی را برای placeholder تاریخ-زمان اسلاید تنظیم می‌کند.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| text | java.lang.String | متنی برای تنظیم. |