---
title: IMathPhantom
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: یک شیء ریاضی فانتوم ltmphantgt را نمایان می‌کند که چیدمان عنصر فرزند خود را تحت تأثیر قرار می‌دهد بدون اینکه لزوماً نمایش داده شود.
type: docs
url: /fa/com.aspose.slides/imathphantom/
---
**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IMathElement](../../com.aspose.slides/imathelement)
```
public interface IMathPhantom extends IMathElement
```

یک شیء ریاضی فانتوم (<m:phant>) را نمایان می‌کند که چیدمان عنصر فرزند خود را تحت تأثیر قرار می‌دهد بدون آنکه لزوماً نمایش داده شود. فانتوم می‌تواند عبارت پایه خود را مخفی کند در حالی که عرض، ارتفاع یا عمق آن را برای تراز فرمول‌ها یا رزرو فضا حفظ می‌کند. نمایش و رفتار هندسی توسط ویژگی‌هایی مانند Show، ZeroWid، ZeroAsc، ZeroDesc و Transp کنترل می‌شود.

--------------------

> ```
> Example:
>  
>  IMathPhantom phantom = new MathPhantom(new MathematicalText("1/2"));
>  phantom.setShow(false);          // مخفی کردن محتوا
>  phantom.setZeroWidth(false);     // نگه داشتن عرض
>  ```

## متدها

| متد | توضیح |
| --- | --- |
| [getBase()](#getBase--) | آرگومان پایه |
| [getShow()](#getShow--) | مقداری را دریافت یا تنظیم می‌کند که نشان می‌دهد آیا عنصر پایه نمایش داده می‌شود یا خیر. |
| [setShow(boolean value)](#setShow-boolean-) | مقداری را دریافت یا تنظیم می‌کند که نشان می‌دهد آیا عنصر پایه نمایش داده می‌شود یا خیر. |
| [getZeroWidth()](#getZeroWidth--) | مقداری را دریافت یا تنظیم می‌کند که نشان می‌دهد آیا عرض عنصر پایه باید به عنوان صفر در نظر گرفته شود یا خیر. |
| [setZeroWidth(boolean value)](#setZeroWidth-boolean-) | مقداری را دریافت یا تنظیم می‌کند که نشان می‌دهد آیا عرض عنصر پایه باید به عنوان صفر در نظر گرفته شود یا خیر. |
| [getZeroAsc()](#getZeroAsc--) | مقداری را دریافت یا تنظیم می‌کند که نشان می‌دهد آیا ارتفاع صعودی (ارتفاع بالای خط پایه) عنصر پایه باید به عنوان صفر در نظر گرفته شود یا خیر. |
| [setZeroAsc(boolean value)](#setZeroAsc-boolean-) | مقداری را دریافت یا تنظیم می‌کند که نشان می‌دهد آیا ارتفاع صعودی (ارتفاع بالای خط پایه) عنصر پایه باید به عنوان صفر در نظر گرفته شود یا خیر. |
| [getZeroDesc()](#getZeroDesc--) | مقداری را دریافت یا تنظیم می‌کند که نشان می‌دهد آیا کاهش (عمق زیر خط پایه) عنصر پایه باید به عنوان صفر در نظر گرفته شود یا خیر. |
| [setZeroDesc(boolean value)](#setZeroDesc-boolean-) | مقداری را دریافت یا تنظیم می‌کند که نشان می‌دهد آیا کاهش (عمق زیر خط پایه) عنصر پایه باید به عنوان صفر در نظر گرفته شود یا خیر. |
| [getTransp()](#getTransp--) | مقداری را دریافت یا تنظیم می‌کند که نشان می‌دهد آیا فانتوم برای قوانین فاصله‌گذاری مبتنی بر کلاس شفاف است یا خیر. |
| [setTransp(boolean value)](#setTransp-boolean-) | مقداری را دریافت یا تنظیم می‌کند که نشان می‌دهد آیا فانتوم برای قوانین فاصله‌گذاری مبتنی بر کلاس شفاف است یا خیر. |
### getBase() {#getBase--}
```
public abstract IMathElement getBase()
```


آرگومان پایه

--------------------

> ```
> Example:
>  
>  MathPhantom mathBar = new MathPhantom(new MathematicalText("x"));
>  IMathElement base = mathBar.getBase();
>  ```


**بازمی‌گردد:**
[IMathElement](../../com.aspose.slides/imathelement)
### getShow() {#getShow--}
```
public abstract boolean getShow()
```


مقداری را دریافت یا تنظیم می‌کند که نشان می‌دهد آیا عنصر پایه نمایش داده می‌شود یا خیر.

--------------------

زمانی که مقدار false باشد، عنصر پایه مخفی می‌شود اما ممکن است بسته به تنظیمات دیگر فانتوم همچنان فضایی را اشغال کند. معادل ویژگی OMML m:show.

**بازمی‌گردد:**
boolean
### setShow(boolean value) {#setShow-boolean-}
```
public abstract void setShow(boolean value)
```


مقداری را دریافت یا تنظیم می‌کند که نشان می‌دهد آیا عنصر پایه نمایش داده می‌شود یا خیر.

--------------------

زمانی که مقدار false باشد، عنصر پایه مخفی می‌شود اما ممکن است بسته به تنظیمات دیگر فانتوم همچنان فضایی را اشغال کند. معادل ویژگی OMML m:show.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getZeroWidth() {#getZeroWidth--}
```
public abstract boolean getZeroWidth()
```


مقداری را دریافت یا تنظیم می‌کند که نشان می‌دهد آیا عرض عنصر پایه باید به عنوان صفر در نظر گرفته شود یا خیر.

--------------------

زمانی که مقدار true باشد، فانتوم برای پایه خود فضای افقی رزرو نمی‌کند. معادل ویژگی OMML m:zeroWid.

**بازمی‌گردد:**
boolean
### setZeroWidth(boolean value) {#setZeroWidth-boolean-}
```
public abstract void setZeroWidth(boolean value)
```


مقداری را دریافت یا تنظیم می‌کند که نشان می‌دهد آیا عرض عنصر پایه باید به عنوان صفر در نظر گرفته شود یا خیر.

--------------------

زمانی که مقدار true باشد، فانتوم برای پایه خود فضای افقی رزرو نمی‌کند. معادل ویژگی OMML m:zeroWid.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getZeroAsc() {#getZeroAsc--}
```
public abstract boolean getZeroAsc()
```


مقداری را دریافت یا تنظیم می‌کند که نشان می‌دهد آیا ارتفاع صعودی (ارتفاع بالای خط پایه) عنصر پایه باید به عنوان صفر در نظر گرفته شود یا خیر.

--------------------

زمانی که مقدار true باشد، فانتوم baseline خط ریاضی اطراف را بالا نمی‌برد. معادل ویژگی OMML m:zeroAsc.

**بازمی‌گردد:**
boolean
### setZeroAsc(boolean value) {#setZeroAsc-boolean-}
```
public abstract void setZeroAsc(boolean value)
```


مقداری را دریافت یا تنظیم می‌کند که نشان می‌دهد آیا ارتفاع صعودی (ارتفاع بالای خط پایه) عنصر پایه باید به عنوان صفر در نظر گرفته شود یا خیر.

--------------------

زمانی که مقدار true باشد، فانتوم baseline خط ریاضی اطراف را بالا نمی‌برد. معادل ویژگی OMML m:zeroAsc.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getZeroDesc() {#getZeroDesc--}
```
public abstract boolean getZeroDesc()
```


مقداری را دریافت یا تنظیم می‌کند که نشان می‌دهد آیا کاهش (عمق زیر خط پایه) عنصر پایه باید به عنوان صفر در نظر گرفته شود یا خیر.

--------------------

زمانی که مقدار true باشد، فانتوم baseline خط ریاضی اطراف را پایین نمی‌آورد. معادل ویژگی OMML m:zeroDesc.

**بازمی‌گردد:**
boolean
### setZeroDesc(boolean value) {#setZeroDesc-boolean-}
```
public abstract void setZeroDesc(boolean value)
```


مقداری را دریافت یا تنظیم می‌کند که نشان می‌دهد آیا کاهش (عمق زیر خط پایه) عنصر پایه باید به عنوان صفر در نظر گرفته شود یا خیر.

--------------------

زمانی که مقدار true باشد، فانتوم baseline خط ریاضی اطراف را پایین نمی‌آورد. معادل ویژگی OMML m:zeroDesc.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getTransp() {#getTransp--}
```
public abstract boolean getTransp()
```


مقداری را دریافت یا تنظیم می‌کند که نشان می‌دهد آیا فانتوم برای قوانین فاصله‌گذاری مبتنی بر کلاس شفاف است یا خیر.

--------------------

زمانی که مقدار true باشد، عملگرها و نمادهای داخل فانتوم همچنان بر فاصله‌گذاری ریاضی اطراف فانتوم تأثیر می‌گذارند (همانند قابل مشاهده بودن). زمانی که مقدار false باشد، قوانین فاصله‌گذاری مبتنی بر کلاس نادیده گرفته می‌شوند. معادل ویژگی OMML m:transp.

**بازمی‌گردد:**
boolean
### setTransp(boolean value) {#setTransp-boolean-}
```
public abstract void setTransp(boolean value)
```


مقداری را دریافت یا تنظیم می‌کند که نشان می‌دهد آیا فانتوم برای قوانین فاصله‌گذاری مبتنی بر کلاس شفاف است یا خیر.

--------------------

زمانی که مقدار true باشد، عملگرها و نمادهای داخل فانتوم همچنان بر فاصله‌گذاری ریاضی اطراف فانتوم تأثیر می‌گذارند (همانند قابل مشاهده بودن). زمانی که مقدار false باشد، قوانین فاصله‌گذاری مبتنی بر کلاس نادیده گرفته می‌شوند. معادل ویژگی OMML m:transp.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |