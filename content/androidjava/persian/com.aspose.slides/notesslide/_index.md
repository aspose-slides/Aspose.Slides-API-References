---
title: NotesSlide
second_title: Aspose.Slides برای اندروید از طریق مرجع API جاوا
description: یک اسلاید یادداشت در یک ارائه را نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/notesslide/
---
**ارث‌بری:**  
java.lang.Object, [com.aspose.slides.BaseSlide](../../com.aspose.slides/baseslide)

**تمام واسط‌های پیاده‌سازی شده:**  
[com.aspose.slides.INotesSlide](../../com.aspose.slides/inotesslide)  
```
public class NotesSlide extends BaseSlide implements INotesSlide
```

یک اسلاید یادداشت در یک ارائه را نشان می‌دهد.

## متدها

| متد | توضیح |
| --- | --- |
| [getHeaderFooterManager()](#getHeaderFooterManager--) | بازگرداندن مدیر HeaderFooter اسلاید یادداشت‌ها. |
| [getNotesTextFrame()](#getNotesTextFrame--) | بازگرداندن یک TextFrame حاوی متن یادداشت‌ها در صورتی که موجود باشد. |
| [getThemeManager()](#getThemeManager--) | بازگرداندن مدیر تم overriding. |
| [getParentSlide()](#getParentSlide--) | بازگرداندن اسلاید والد. |
| [getShowMasterShapes()](#getShowMasterShapes--) | تعیین می‌کند آیا اشکال روی اسلاید master بر روی اسلایدها نمایش داده شوند یا نه. |
| [setShowMasterShapes(boolean value)](#setShowMasterShapes-boolean-) | تعیین می‌کند آیا اشکال روی اسلاید master بر روی اسلایدها نمایش داده شوند یا نه. |

### getHeaderFooterManager() {#getHeaderFooterManager--}
```
public final INotesSlideHeaderFooterManager getHeaderFooterManager()
```

بازگرداندن مدیر HeaderFooter اسلاید یادداشت‌ها. فقط-خواندنی [INotesSlideHeaderFooterManager](../../com.aspose.slides/inotesslideheaderfootermanager).

**بازگشت:**  
[INotesSlideHeaderFooterManager](../../com.aspose.slides/inotesslideheaderfootermanager)

### getNotesTextFrame() {#getNotesTextFrame--}
```
public final ITextFrame getNotesTextFrame()
```

بازگرداندن یک TextFrame حاوی متن یادداشت‌ها در صورتی که موجود باشد. فقط-خواندنی [ITextFrame](../../com.aspose.slides/itextframe).

**بازگشت:**  
[ITextFrame](../../com.aspose.slides/itextframe)

### getThemeManager() {#getThemeManager--}
```
public final IOverrideThemeManager getThemeManager()
```

بازگرداندن مدیر تم overriding. فقط-خواندنی [IOverrideThemeManager](../../com.aspose.slides/ioverridethememanager).

**بازگشت:**  
[IOverrideThemeManager](../../com.aspose.slides/ioverridethememanager)

### getParentSlide() {#getParentSlide--}
```
public final ISlide getParentSlide()
```

بازگرداندن اسلاید والد. فقط-خواندنی [ISlide](../../com.aspose.slides/islide).

**بازگشت:**  
[ISlide](../../com.aspose.slides/islide)

### getShowMasterShapes() {#getShowMasterShapes--}
```
public boolean getShowMasterShapes()
```

تعیین می‌کند آیا اشکال روی اسلاید master بر روی اسلایدها نمایش داده شوند یا نه. قابل-خواندن/نوشتن boolean.

**بازگشت:**  
boolean

### setShowMasterShapes(boolean value) {#setShowMasterShapes-boolean-}
```
public void setShowMasterShapes(boolean value)
```

تعیین می‌کند آیا اشکال روی اسلاید master بر روی اسلایدها نمایش داده شوند یا نه. قابل-خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |