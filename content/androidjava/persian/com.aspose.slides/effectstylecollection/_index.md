---
title: EffectStyleCollection
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: یک مجموعه از سبک‌های اثر را نمایندگی می‌کند.
type: docs
url: /fa/com.aspose.slides/effectstylecollection/
---
**ارث‌بری:**  
java.lang.Object, com.aspose.slides.DomObject

**تمام رابط‌های پیاده‌سازی‌شده:**  
[com.aspose.slides.IEffectStyleCollection](../../com.aspose.slides/ieffectstylecollection)  
```
public final class EffectStyleCollection extends DomObject<FormatScheme> implements IEffectStyleCollection
```

نمایشی از یک مجموعه از سبک‌های اثر.

## متدها

| متد | توضیح |
| --- | --- |
| [get_Item(int index)](#get-Item-int-) | عنصری را در موقعیت مشخص بازمی‌گرداند. |
| [iterator()](#iterator--) | یک شمارنده را بازمی‌گرداند که از مجموعه عبور می‌کند. |
| [iteratorJava()](#iteratorJava--) | یک iterator جاوا برای کل مجموعه بازمی‌گرداند. |
| [size()](#size--) | تعداد عناصری در مجموعه را بازمی‌گرداند. |
| [copyTo(System.Array array, int index)](#copyTo-com.aspose.ms.System.Array-int-) | تمام عناصر مجموعه را به آرایه مشخص شده کپی می‌کند. |
| [isSynchronized()](#isSynchronized--) | یک مقدار را بازمی‌گرداند که نشان می‌دهد آیا دسترسی به مجموعه همگام‌سازی شده است (ایمن برای رشته‌ها). |
| [getSyncRoot()](#getSyncRoot--) | یک ریشهٔ همگام‌سازی را بازمی‌گرداند. |

### get_Item(int index) {#get-Item-int-}
```
public final IEffectStyle get_Item(int index)
```

عنصری را در موقعیت مشخص بازمی‌گرداند. فقط‌خواندنی [EffectStyle](../../com.aspose.slides/effectstyle).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| index | int | موقعیت عنصر. |

**بازگشت:**
[IEffectStyle](../../com.aspose.slides/ieffectstyle) - عنصر در موقعیت مشخص.

### iterator() {#iterator--}
```
public final System.Collections.Generic.IGenericEnumerator<IEffectStyle> iterator()
```

یک شمارنده را بازمی‌گرداند که از مجموعه عبور می‌کند.

**بازگشت:**
com.aspose.ms.System.Collections.Generic.IGenericEnumerator<com.aspose.slides.IEffectStyle> - یک IGenericEnumerator که می‌تواند برای عبور از مجموعه استفاده شود.

### iteratorJava() {#iteratorJava--}
```
public final System.Collections.Generic.IGenericEnumerator<IEffectStyle> iteratorJava()
```

یک iterator جاوا برای کل مجموعه بازمی‌گرداند.

**بازگشت:**
com.aspose.ms.System.Collections.Generic.IGenericEnumerator<com.aspose.slides.IEffectStyle> - یک java.util.Iterator برای کل مجموعه.

### size() {#size--}
```
public final int size()
```

تعداد عناصری در مجموعه را بازمی‌گرداند. فقط‌خواندنی int، فقط‌خواندنی int.

**بازگشت:**
int

### copyTo(System.Array array, int index) {#copyTo-com.aspose.ms.System.Array-int-}
```
public final void copyTo(System.Array array, int index)
```

تمام عناصر مجموعه را به آرایه مشخص شده کپی می‌کند.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| array | com.aspose.ms.System.Array | آرایه هدف. |
| index | int | اندیس شروع در آرایه هدف. |

### isSynchronized() {#isSynchronized--}
```
public final boolean isSynchronized()
```

یک مقدار را بازمی‌گرداند که نشان می‌دهد آیا دسترسی به مجموعه همگام‌سازی شده است (ایمن برای رشته‌ها). فقط‌خواندنی boolean.

**بازگشت:**
boolean

### getSyncRoot() {#getSyncRoot--}
```
public final Object getSyncRoot()
```

یک ریشهٔ همگام‌سازی را بازمی‌گرداند. فقط‌خواندنی Object.

**بازگشت:**
java.lang.Object