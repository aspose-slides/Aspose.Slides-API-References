---
title: TextHighlightingOptions
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: نمایش می‌دهد گزینه‌هایی که می‌توان برای برجسته‌سازی متن در قاب متن استفاده کرد.
type: docs
url: /fa/com.aspose.slides/texthighlightingoptions/
---
**ارث‌بری:**
java.lang.Object

**تمام رابط‌های پیاده‌سازی شده:**
[com.aspose.slides.ITextHighlightingOptions](../../com.aspose.slides/itexthighlightingoptions)
```
public class TextHighlightingOptions implements ITextHighlightingOptions
```

نمایش می‌دهد گزینه‌هایی که می‌توان برای برجسته‌سازی متن در قاب متن استفاده کرد.

## سازنده‌ها

| سازنده | توضیح |
| --- | --- |
| [TextHighlightingOptions()](#TextHighlightingOptions--) | یک گزینه جدید برجسته‌سازی متن پیش‌فرض ایجاد می‌کند. |

## متدها

| متد | توضیح |
| --- | --- |
| [getCaseSensitive()](#getCaseSensitive--) | اگر true تنظیم شود برای استفاده از جستجوی حساس به حروف بزرگ و کوچک، در غیر این صورت false. |
| [setCaseSensitive(boolean value)](#setCaseSensitive-boolean-) | اگر true تنظیم شود برای استفاده از جستجوی حساس به حروف بزرگ و کوچک، در غیر این صورت false. |
| [getWholeWordsOnly()](#getWholeWordsOnly--) | اگر true تنظیم شود فقط کلمات کامل را مطابقت می‌دهد، در غیر این صورت false. |
| [setWholeWordsOnly(boolean value)](#setWholeWordsOnly-boolean-) | اگر true تنظیم شود فقط کلمات کامل را مطابقت می‌دهد، در غیر این صورت false. |

### TextHighlightingOptions() {#TextHighlightingOptions--}
```
public TextHighlightingOptions()
```

یک گزینه جدید برجسته‌سازی متن پیش‌فرض ایجاد می‌کند.

### getCaseSensitive() {#getCaseSensitive--}
```
public final boolean getCaseSensitive()
```

اگر true تنظیم شود برای استفاده از جستجوی حساس به حروف بزرگ و کوچک، در غیر این صورت false. خواندن/نوشتن بولی.

**بازگشت:**
boolean

### setCaseSensitive(boolean value) {#setCaseSensitive-boolean-}
```
public final void setCaseSensitive(boolean value)
```

اگر true تنظیم شود برای استفاده از جستجوی حساس به حروف بزرگ و کوچک، در غیر این صورت false. خواندن/نوشتن بولی.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getWholeWordsOnly() {#getWholeWordsOnly--}
```
public final boolean getWholeWordsOnly()
```

اگر true تنظیم شود فقط کلمات کامل را مطابقت می‌دهد، در غیر این صورت false. خواندن/نوشتن بولی.

**بازگشت:**
boolean

### setWholeWordsOnly(boolean value) {#setWholeWordsOnly-boolean-}
```
public final void setWholeWordsOnly(boolean value)
```

اگر true تنظیم شود فقط کلمات کامل را مطابقت می‌دهد، در غیر این صورت false. خواندن/نوشتن بولی.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |