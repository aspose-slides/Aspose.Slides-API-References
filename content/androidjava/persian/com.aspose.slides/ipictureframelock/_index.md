---
title: IPictureFrameLock
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: تعیین می‌کند که چه عملیات‌هایی در PictureFrameEx والد غیرفعال هستند.
type: docs
url: /fa/com.aspose.slides/ipictureframelock/
---
**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IBaseShapeLock](../../com.aspose.slides/ibaseshapelock)
```
public interface IPictureFrameLock extends IBaseShapeLock
```

تعیین می‌کند که چه عملیات‌هایی در PictureFrameEx والد غیرفعال هستند.
## متدها

| متد | توضیح |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | تعیین می‌کند که افزودن این شکل به یک گروه ممنوع است. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | تعیین می‌کند که افزودن این شکل به یک گروه ممنوع است. |
| [getSelectLocked()](#getSelectLocked--) | تعیین می‌کند که انتخاب این شکل ممنوع است. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | تعیین می‌کند که انتخاب این شکل ممنوع است. |
| [getRotationLocked()](#getRotationLocked--) | تعیین می‌کند که تغییر زاویه چرخش این شکل ممنوع است. |
| [setRotationLocked(boolean value)](#setRotationLocked-boolean-) | تعیین می‌کند که تغییر زاویه چرخش این شکل ممنوع است. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | تعیین می‌کند که آیا شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | تعیین می‌کند که آیا شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. |
| [getPositionLocked()](#getPositionLocked--) | تعیین می‌کند که حرکت این شکل ممنوع است. |
| [setPositionLocked(boolean value)](#setPositionLocked-boolean-) | تعیین می‌کند که حرکت این شکل ممنوع است. |
| [getSizeLocked()](#getSizeLocked--) | تعیین می‌کند که تغییر اندازه این شکل ممنوع است. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | تعیین می‌کند که تغییر اندازه این شکل ممنوع است. |
| [getEditPointsLocked()](#getEditPointsLocked--) | تعیین می‌کند که تغییر مستقیم کانتور این شکل ممنوع است. |
| [setEditPointsLocked(boolean value)](#setEditPointsLocked-boolean-) | تعیین می‌کند که تغییر مستقیم کانتور این شکل ممنوع است. |
| [getAdjustHandlesLocked()](#getAdjustHandlesLocked--) | تعیین می‌کند که تغییر مقادیر تنظیم ممنوع است. |
| [setAdjustHandlesLocked(boolean value)](#setAdjustHandlesLocked-boolean-) | تعیین می‌کند که تغییر مقادیر تنظیم ممنوع است. |
| [getArrowheadsLocked()](#getArrowheadsLocked--) | تعیین می‌کند که تغییر سرهای پیکان ممنوع است. |
| [setArrowheadsLocked(boolean value)](#setArrowheadsLocked-boolean-) | تعیین می‌کند که تغییر سرهای پیکان ممنوع است. |
| [getShapeTypeLocked()](#getShapeTypeLocked--) | تعیین می‌کند که تغییر نوع شکل ممنوع است. |
| [setShapeTypeLocked(boolean value)](#setShapeTypeLocked-boolean-) | تعیین می‌کند که تغییر نوع شکل ممنوع است. |
| [getCropLocked()](#getCropLocked--) | تعیین می‌کند که بریدن تصویر ممنوع است. |
| [setCropLocked(boolean value)](#setCropLocked-boolean-) | تعیین می‌کند که بریدن تصویر ممنوع است. |
### getGroupingLocked() {#getGroupingLocked--}
```
public abstract boolean getGroupingLocked()
```

تعیین می‌کند که افزودن این شکل به یک گروه ممنوع است. قابل خواندن/نوشتن boolean.

**باز می‌گردد:**
boolean
### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public abstract void setGroupingLocked(boolean value)
```

تعیین می‌کند که افزودن این شکل به یک گروه ممنوع است. قابل خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getSelectLocked() {#getSelectLocked--}
```
public abstract boolean getSelectLocked()
```

تعیین می‌کند که انتخاب این شکل ممنوع است. قابل خواندن/نوشتن boolean.

**باز می‌گردد:**
boolean
### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public abstract void setSelectLocked(boolean value)
```

تعیین می‌کند که انتخاب این شکل ممنوع است. قابل خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getRotationLocked() {#getRotationLocked--}
```
public abstract boolean getRotationLocked()
```

تعیین می‌کند که تغییر زاویه چرخش این شکل ممنوع است. قابل خواندن/نوشتن boolean.

**باز می‌گردد:**
boolean
### setRotationLocked(boolean value) {#setRotationLocked-boolean-}
```
public abstract void setRotationLocked(boolean value)
```

تعیین می‌کند که تغییر زاویه چرخش این شکل ممنوع است. قابل خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public abstract boolean getAspectRatioLocked()
```

تعیین می‌کند که آیا شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. قابل خواندن/نوشتن boolean.

**باز می‌گردد:**
boolean
### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public abstract void setAspectRatioLocked(boolean value)
```

تعیین می‌کند که آیا شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. قابل خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getPositionLocked() {#getPositionLocked--}
```
public abstract boolean getPositionLocked()
```

تعیین می‌کند که حرکت این شکل ممنوع است. قابل خواندن/نوشتن boolean.

**باز می‌گردد:**
boolean
### setPositionLocked(boolean value) {#setPositionLocked-boolean-}
```
public abstract void setPositionLocked(boolean value)
```

تعیین می‌کند که حرکت این شکل ممنوع است. قابل خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getSizeLocked() {#getSizeLocked--}
```
public abstract boolean getSizeLocked()
```

تعیین می‌کند که تغییر اندازه این شکل ممنوع است. قابل خواندن/نوشتن boolean.

**باز می‌گردد:**
boolean
### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public abstract void setSizeLocked(boolean value)
```

تعیین می‌کند که تغییر اندازه این شکل ممنوع است. قابل خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getEditPointsLocked() {#getEditPointsLocked--}
```
public abstract boolean getEditPointsLocked()
```

تعیین می‌کند که تغییر مستقیم کانتور این شکل ممنوع است. قابل خواندن/نوشتن boolean.

**باز می‌گردد:**
boolean
### setEditPointsLocked(boolean value) {#setEditPointsLocked-boolean-}
```
public abstract void setEditPointsLocked(boolean value)
```

تعیین می‌کند که تغییر مستقیم کانتور این شکل ممنوع است. قابل خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getAdjustHandlesLocked() {#getAdjustHandlesLocked--}
```
public abstract boolean getAdjustHandlesLocked()
```

تعیین می‌کند که تغییر مقادیر تنظیم ممنوع است. قابل خواندن/نوشتن boolean.

**باز می‌گردد:**
boolean
### setAdjustHandlesLocked(boolean value) {#setAdjustHandlesLocked-boolean-}
```
public abstract void setAdjustHandlesLocked(boolean value)
```

تعیین می‌کند که تغییر مقادیر تنظیم ممنوع است. قابل خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getArrowheadsLocked() {#getArrowheadsLocked--}
```
public abstract boolean getArrowheadsLocked()
```

تعیین می‌کند که تغییر سرهای پیکان ممنوع است. قابل خواندن/نوشتن boolean.

**باز می‌گردد:**
boolean
### setArrowheadsLocked(boolean value) {#setArrowheadsLocked-boolean-}
```
public abstract void setArrowheadsLocked(boolean value)
```

تعیین می‌کند که تغییر سرهای پیکان ممنوع است. قابل خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getShapeTypeLocked() {#getShapeTypeLocked--}
```
public abstract boolean getShapeTypeLocked()
```

تعیین می‌کند که تغییر نوع شکل ممنوع است. قابل خواندن/نوشتن boolean.

**باز می‌گردد:**
boolean
### setShapeTypeLocked(boolean value) {#setShapeTypeLocked-boolean-}
```
public abstract void setShapeTypeLocked(boolean value)
```

تعیین می‌کند که تغییر نوع شکل ممنوع است. قابل خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getCropLocked() {#getCropLocked--}
```
public abstract boolean getCropLocked()
```

تعیین می‌کند که بریدن تصویر ممنوع است. قابل خواندن/نوشتن boolean.

**باز می‌گردد:**
boolean
### setCropLocked(boolean value) {#setCropLocked-boolean-}
```
public abstract void setCropLocked(boolean value)
```

تعیین می‌کند که بریدن تصویر ممنوع است. قابل خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |