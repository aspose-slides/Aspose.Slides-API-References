---
title: DataTable
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: خواص جدول داده را نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/datatable/
---
**وراثت:**  
java.lang.Object, com.aspose.slides.DomObject

**تمام رابط‌های اجرا شده:**  
[com.aspose.slides.IDataTable](../../com.aspose.slides/idatatable)  
```
public class DataTable extends DomObject<Chart> implements IDataTable
```

خواص جدول داده را نشان می‌دهد.

## متدها

| متد | توضیح |
| --- | --- |
| [getFormat()](#getFormat--) | خط، پر کردن و سبک‌های اثر یک شی را برمی‌گرداند. |
| [hasBorderHorizontal()](#hasBorderHorizontal--) | True اگر جدول داده‌های نمودار حاشیه افقی سلول داشته باشد. |
| [setBorderHorizontal(boolean value)](#setBorderHorizontal-boolean-) | True اگر جدول داده‌های نمودار حاشیه افقی سلول داشته باشد. |
| [hasBorderOutline()](#hasBorderOutline--) | True اگر جدول داده‌های نمودار حاشیه پیرامونی داشته باشد. |
| [setBorderOutline(boolean value)](#setBorderOutline-boolean-) | True اگر جدول داده‌های نمودار حاشیه پیرامونی داشته باشد. |
| [hasBorderVertical()](#hasBorderVertical--) | True اگر جدول داده‌های نمودار حاشیه عمودی سلول داشته باشد. |
| [setBorderVertical(boolean value)](#setBorderVertical-boolean-) | True اگر جدول داده‌های نمودار حاشیه عمودی سلول داشته باشد. |
| [getShowLegendKey()](#getShowLegendKey--) | True اگر کلید افسانه‌برچسب داده قابل مشاهده باشد. |
| [setShowLegendKey(boolean value)](#setShowLegendKey-boolean-) | True اگر کلید افسانه‌برچسب داده قابل مشاهده باشد. |
| [getChart()](#getChart--) | نمودار را برمی‌گرداند. |
| [getTextFormat()](#getTextFormat--) | قالب متن را برمی‌گرداند. |
| [getSlide()](#getSlide--) | صفحه اسلاید والد یک FillFormat را برمی‌گرداند. |
| [getPresentation()](#getPresentation--) | ارائه والد یک FillFormat را برمی‌گرداند. |

### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

خط، پر کردن و سبک‌های اثر یک شی را برمی‌گرداند. فقط‌خواندنی [IFormat](../../com.aspose.slides/iformat).

**بازگشت:**  
[IFormat](../../com.aspose.slides/iformat)

### hasBorderHorizontal() {#hasBorderHorizontal--}
```
public final boolean hasBorderHorizontal()
```

True اگر جدول داده‌های نمودار حاشیه افقی سلول داشته باشد. خواندنی/نوشتنی boolean.

**بازگشت:**  
boolean

### setBorderHorizontal(boolean value) {#setBorderHorizontal-boolean-}
```
public final void setBorderHorizontal(boolean value)
```

True اگر جدول داده‌های نمودار حاشیه افقی سلول داشته باشد. خواندنی/نوشتنی boolean.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### hasBorderOutline() {#hasBorderOutline--}
```
public final boolean hasBorderOutline()
```

True اگر جدول داده‌های نمودار حاشیه پیرامونی داشته باشد. خواندنی/نوشتنی boolean.

**بازگشت:**  
boolean

### setBorderOutline(boolean value) {#setBorderOutline-boolean-}
```
public final void setBorderOutline(boolean value)
```

True اگر جدول داده‌های نمودار حاشیه پیرامونی داشته باشد. خواندنی/نوشتنی boolean.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### hasBorderVertical() {#hasBorderVertical--}
```
public final boolean hasBorderVertical()
```

True اگر جدول داده‌های نمودار حاشیه عمودی سلول داشته باشد. خواندنی/نوشتنی boolean.

**بازگشت:**  
boolean

### setBorderVertical(boolean value) {#setBorderVertical-boolean-}
```
public final void setBorderVertical(boolean value)
```

True اگر جدول داده‌های نمودار حاشیه عمودی سلول داشته باشد. خواندنی/نوشتنی boolean.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getShowLegendKey() {#getShowLegendKey--}
```
public final boolean getShowLegendKey()
```

True اگر کلید افسانه‌برچسب داده قابل مشاهده باشد. خواندنی/نوشتنی boolean.

**بازگشت:**  
boolean

### setShowLegendKey(boolean value) {#setShowLegendKey-boolean-}
```
public final void setShowLegendKey(boolean value)
```

True اگر کلید افسانه‌برچسب داده قابل مشاهده باشد. خواندنی/نوشتنی boolean.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getChart() {#getChart--}
```
public final IChart getChart()
```

نمودار را برمی‌گرداند. فقط‌خواندنی [IChart](../../com.aspose.slides/ichart).

**بازگشت:**  
[IChart](../../com.aspose.slides/ichart)

### getTextFormat() {#getTextFormat--}
```
public final IChartTextFormat getTextFormat()
```

قالب متن را برمی‌گرداند. فقط‌خواندنی [IChartTextFormat](../../com.aspose.slides/icharttextformat).

**بازگشت:**  
[IChartTextFormat](../../com.aspose.slides/icharttextformat)

### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

صفحه اسلاید والد یک FillFormat را برمی‌گرداند. فقط‌خواندنی [BaseSlide](../../com.aspose.slides/baseslide).

**بازگشت:**  
[IBaseSlide](../../com.aspose.slides/ibaseslide)

### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

ارائه والد یک FillFormat را برمی‌گرداند. فقط‌خواندنی [IPresentation](../../com.aspose.slides/ipresentation).

**بازگشت:**  
[IPresentation](../../com.aspose.slides/ipresentation)