---
title: INormalViewRestoredProperties
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: اندازه‌گذاری ناحیه اسلاید (عرض وقتی فرزند RestoredTop، ارتفاع وقتی فرزند RestoredLeft) در نمای عادی، هنگامی که ناحیه دارای اندازه متغیر Restored است (نه کم‌شده نه بزرگ‌شده).
type: docs
url: /fa/com.aspose.slides/inormalviewrestoredproperties/
---
```
public interface INormalViewRestoredProperties
```

اندازۀ ناحیه اسلاید (عرض وقتی فرزند RestoredTop، ارتفاع وقتی فرزند RestoredLeft) را در نمای عادی مشخص می‌کند، وقتی ناحیه دارای اندازهٔ متغیر Restored است (نه کم‌شده نه بزرگ‌شده).

## متدها

| متد | توضیح |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | اندازهٔ ناحیه اسلاید را مشخص می‌کند (عرض وقتی فرزند RestoredTop، ارتفاع وقتی فرزند RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | اندازهٔ ناحیه اسلاید را مشخص می‌کند (عرض وقتی فرزند RestoredTop، ارتفاع وقتی فرزند RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | مشخص می‌کند که آیا اندازهٔ ناحیهٔ محتوا باید برای اندازهٔ جدید جبران کند هنگام تغییر اندازهٔ پنجره‌ای که نما را در برنامه دربر می‌گیرد. قابل خواندن/نوشتن boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | مشخص می‌کند که آیا اندازهٔ ناحیهٔ محتوا باید برای اندازهٔ جدید جبران کند هنگام تغییر اندازهٔ پنجره‌ای که نما را در برنامه دربر می‌گیرد. قابل خواندن/نوشتن boolean. |

### getDimensionSize() {#getDimensionSize--}
```
public abstract float getDimensionSize()
```

اندازهٔ ناحیه اسلاید را مشخص می‌کند (عرض وقتی فرزند RestoredTop، ارتفاع وقتی فرزند RestoredLeft). قابل خواندن/نوشتن float.

**بازگرداندن:**
float

### setDimensionSize(float value) {#setDimensionSize-float-}
```
public abstract void setDimensionSize(float value)
```

اندازهٔ ناحیه اسلاید را مشخص می‌کند (عرض وقتی فرزند RestoredTop، ارتفاع وقتی فرزند RestoredLeft). قابل خواندن/نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public abstract boolean getAutoAdjust()
```

مشخص می‌کند که آیا اندازهٔ ناحیهٔ محتوا باید برای اندازهٔ جدید جبران کند هنگام تغییر اندازهٔ پنجره‌ای که نما را در برنامه دربر می‌گیرد. قابل خواندن/نوشتن boolean.

**بازگرداندن:**
boolean

### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public abstract void setAutoAdjust(boolean value)
```

مشخص می‌کند که آیا اندازهٔ ناحیهٔ محتوا باید برای اندازهٔ جدید جبران کند هنگام تغییر اندازهٔ پنجره‌ای که نما را در برنامه دربر می‌گیرد. قابل خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |