---
title: DataSourceTypeForErrorBarsCustomValues
second_title: مرجع API جاوا Aspose.Slides برای اندروید
description: انواع مقادیر را در فهرست ویژگی‌های ChartDataPoint.ErrorBarsCustomValues مشخص می‌کند
type: docs
url: /fa/com.aspose.slides/datasourcetypeforerrorbarscustomvalues/
---
**وراثت:**
java.lang.Object

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IDataSourceTypeForErrorBarsCustomValues](../../com.aspose.slides/idatasourcetypeforerrorbarscustomvalues)
```
public class DataSourceTypeForErrorBarsCustomValues implements IDataSourceTypeForErrorBarsCustomValues
```

انواع مقادیر را در فهرست ویژگی‌های ChartDataPoint.ErrorBarsCustomValues مشخص می‌کند
## سازنده‌ها

| سازنده | توضیح |
| --- | --- |
| [DataSourceTypeForErrorBarsCustomValues()](#DataSourceTypeForErrorBarsCustomValues--) |  |
## متدها

| متد | توضیح |
| --- | --- |
| [getDataSourceTypeForXMinusValues()](#getDataSourceTypeForXMinusValues--) | مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء ویژگی XMinus نقاط داده برای مقادیر سفارشی نوارهای خطا واقعی است. |
| [setDataSourceTypeForXMinusValues(int value)](#setDataSourceTypeForXMinusValues-int-) | مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء ویژگی XMinus نقاط داده برای مقادیر سفارشی نوارهای خطا واقعی است. |
| [getDataSourceTypeForXPlusValues()](#getDataSourceTypeForXPlusValues--) | مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء ویژگی XPlus نقاط داده برای مقادیر سفارشی نوارهای خطا واقعی است. |
| [setDataSourceTypeForXPlusValues(int value)](#setDataSourceTypeForXPlusValues-int-) | مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء ویژگی XPlus نقاط داده برای مقادیر سفارشی نوارهای خطا واقعی است. |
| [getDataSourceTypeForYMinusValues()](#getDataSourceTypeForYMinusValues--) | مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء ویژگی YMinus نقاط داده برای مقادیر سفارشی نوارهای خطا واقعی است. |
| [setDataSourceTypeForYMinusValues(int value)](#setDataSourceTypeForYMinusValues-int-) | مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء ویژگی YMinus نقاط داده برای مقادیر سفارشی نوارهای خطا واقعی است. |
| [getDataSourceTypeForYPlusValues()](#getDataSourceTypeForYPlusValues--) | مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء ویژگی YPlus نقاط داده برای مقادیر سفارشی نوارهای خطا واقعی است. |
| [setDataSourceTypeForYPlusValues(int value)](#setDataSourceTypeForYPlusValues-int-) | مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء ویژگی YPlus نقاط داده برای مقادیر سفارشی نوارهای خطا واقعی است. |
### DataSourceTypeForErrorBarsCustomValues() {#DataSourceTypeForErrorBarsCustomValues--}
```
public DataSourceTypeForErrorBarsCustomValues()
```


### getDataSourceTypeForXMinusValues() {#getDataSourceTypeForXMinusValues--}
```
public final int getDataSourceTypeForXMinusValues()
```


مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء ویژگی XMinus نقاط داده برای مقادیر سفارشی نوارهای خطا واقعی است. به عبارت دیگر، نوع مقدار ویژگی ChartDataPoint.ErrorBarsCustomValues.XMinus.Data را تعیین می‌کند. خواندنی/نوشتنی [DataSourceType](../../com.aspose.slides/datasourcetype).

**بازگشت:**
int
### setDataSourceTypeForXMinusValues(int value) {#setDataSourceTypeForXMinusValues-int-}
```
public final void setDataSourceTypeForXMinusValues(int value)
```


مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء ویژگی XMinus نقاط داده برای مقادیر سفارشی نوارهای خطا واقعی است. به عبارت دیگر، نوع مقدار ویژگی ChartDataPoint.ErrorBarsCustomValues.XMinus.Data را تعیین می‌کند. خواندنی/نوشتنی [DataSourceType](../../com.aspose.slides/datasourcetype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getDataSourceTypeForXPlusValues() {#getDataSourceTypeForXPlusValues--}
```
public final int getDataSourceTypeForXPlusValues()
```


مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء ویژگی XPlus نقاط داده برای مقادیر سفارشی نوارهای خطا واقعی است. به عبارت دیگر، نوع مقدار ویژگی ChartDataPoint.ErrorBarsCustomValues.XPlus.Data را تعیین می‌کند. خواندنی/نوشتنی [DataSourceType](../../com.aspose.slides/datasourcetype).

**بازگشت:**
int
### setDataSourceTypeForXPlusValues(int value) {#setDataSourceTypeForXPlusValues-int-}
```
public final void setDataSourceTypeForXPlusValues(int value)
```


مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء ویژگی XPlus نقاط داده برای مقادیر سفارشی نوارهای خطا واقعی است. به عبارت دیگر، نوع مقدار ویژگی ChartDataPoint.ErrorBarsCustomValues.XPlus.Data را تعیین می‌کند. خواندنی/نوشتنی [DataSourceType](../../com.aspose.slides/datasourcetype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getDataSourceTypeForYMinusValues() {#getDataSourceTypeForYMinusValues--}
```
public final int getDataSourceTypeForYMinusValues()
```


مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء ویژگی YMinus نقاط داده برای مقادیر سفارشی نوارهای خطا واقعی است. به عبارت دیگر، نوع مقدار ویژگی ChartDataPointEx.ErrorBarsCustomValues.YMinus.Data را تعیین می‌کند. خواندنی/نوشتنی [DataSourceType](../../com.aspose.slides/datasourcetype).

**بازگشت:**
int
### setDataSourceTypeForYMinusValues(int value) {#setDataSourceTypeForYMinusValues-int-}
```
public final void setDataSourceTypeForYMinusValues(int value)
```


مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء ویژگی YMinus نقاط داده برای مقادیر سفارشی نوارهای خطا واقعی است. به عبارت دیگر، نوع مقدار ویژگی ChartDataPointEx.ErrorBarsCustomValues.YMinus.Data را تعیین می‌کند. خواندنی/نوشتنی [DataSourceType](../../com.aspose.slides/datasourcetype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getDataSourceTypeForYPlusValues() {#getDataSourceTypeForYPlusValues--}
```
public final int getDataSourceTypeForYPlusValues()
```


مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء ویژگی YPlus نقاط داده برای مقادیر سفارشی نوارهای خطا واقعی است. به عبارت دیگر، نوع مقدار ویژگی ChartDataPointEx.ErrorBarsCustomValues.YPlus.Data را تعیین می‌کند. خواندنی/نوشتنی [DataSourceType](../../com.aspose.slides/datasourcetype).

**بازگشت:**
int
### setDataSourceTypeForYPlusValues(int value) {#setDataSourceTypeForYPlusValues-int-}
```
public final void setDataSourceTypeForYPlusValues(int value)
```


مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء ویژگی YPlus نقاط داده برای مقادیر سفارشی نوارهای خطا واقعی است. به عبارت دیگر، نوع مقدار ویژگی ChartDataPointEx.ErrorBarsCustomValues.YPlus.Data را تعیین می‌کند. خواندنی/نوشتنی [DataSourceType](../../com.aspose.slides/datasourcetype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |