---
title: IAutoShapeLock
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: تعیین می‌کند کدام عملیات‌ها در AutoshapeEx والد غیرفعال هستند.
type: docs
url: /fa/com.aspose.slides/iautoshapelock/
---
**تمام رابط‌های پیاده‌سازی شده:**
[com.aspose.slides.IBaseShapeLock](../../com.aspose.slides/ibaseshapelock)
```
public interface IAutoShapeLock extends IBaseShapeLock
```

تعیین می‌کند کدام عملیات‌ها در AutoshapeEx والد غیرفعال هستند.
## متدها

| متد | توضیح |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | تعیین می‌کند افزودن این شکل به یک گروه ممنوع باشد. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | تعیین می‌کند افزودن این شکل به یک گروه ممنوع باشد. |
| [getSelectLocked()](#getSelectLocked--) | تعیین می‌کند انتخاب این شکل ممنوع باشد. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | تعیین می‌کند انتخاب این شکل ممنوع باشد. |
| [getRotateLocked()](#getRotateLocked--) | تعیین می‌کند تغییر زاویه چرخش این شکل ممنوع باشد. |
| [setRotateLocked(boolean value)](#setRotateLocked-boolean-) | تعیین می‌کند تغییر زاویه چرخش این شکل ممنوع باشد. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | تعیین می‌کند حفظ نسبت ابعاد هنگام تغییر اندازه برای این شکل لازم باشد. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | تعیین می‌کند حفظ نسبت ابعاد هنگام تغییر اندازه برای این شکل لازم باشد. |
| [getPositionLocked()](#getPositionLocked--) | تعیین می‌کند جابه‌جایی این شکل ممنوع باشد. |
| [setPositionLocked(boolean value)](#setPositionLocked-boolean-) | تعیین می‌کند جابه‌جایی این شکل ممنوع باشد. |
| [getSizeLocked()](#getSizeLocked--) | تعیین می‌کند تغییر اندازه این شکل ممنوع باشد. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | تعیین می‌کند تغییر اندازه این شکل ممنوع باشد. |
| [getEditPointsLocked()](#getEditPointsLocked--) | تعیین می‌کند تغییر مستقیم کانتور این شکل ممنوع باشد. |
| [setEditPointsLocked(boolean value)](#setEditPointsLocked-boolean-) | تعیین می‌کند تغییر مستقیم کانتور این شکل ممنوع باشد. |
| [getAdjustHandlesLocked()](#getAdjustHandlesLocked--) | تعیین می‌کند تغییر مقادیر تنظیمات ممنوع باشد. |
| [setAdjustHandlesLocked(boolean value)](#setAdjustHandlesLocked-boolean-) | تعیین می‌کند تغییر مقادیر تنظیمات ممنوع باشد. |
| [getArrowheadsLocked()](#getArrowheadsLocked--) | تعیین می‌کند تغییر سرهای فلش ممنوع باشد. |
| [setArrowheadsLocked(boolean value)](#setArrowheadsLocked-boolean-) | تعیین می‌کند تغییر سرهای فلش ممنوع باشد. |
| [getShapeTypeLocked()](#getShapeTypeLocked--) | تعیین می‌کند تغییر نوع شکل ممنوع باشد. |
| [setShapeTypeLocked(boolean value)](#setShapeTypeLocked-boolean-) | تعیین می‌کند تغییر نوع شکل ممنوع باشد. |
| [getTextLocked()](#getTextLocked--) | تعیین می‌کند ویرایش متن ممنوع باشد. |
| [setTextLocked(boolean value)](#setTextLocked-boolean-) | تعیین می‌کند ویرایش متن ممنوع باشد. |
### getGroupingLocked() {#getGroupingLocked--}
```
public abstract boolean getGroupingLocked()
```

تعیین می‌کند افزودن این شکل به یک گروه ممنوع باشد. بولی قابل خواندن و نوشتن.

**بازگشت:**
boolean
### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public abstract void setGroupingLocked(boolean value)
```

تعیین می‌کند افزودن این شکل به یک گروه ممنوع باشد. بولی قابل خواندن و نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getSelectLocked() {#getSelectLocked--}
```
public abstract boolean getSelectLocked()
```

تعیین می‌کند انتخاب این شکل ممنوع باشد. بولی قابل خواندن و نوشتن.

**بازگشت:**
boolean
### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public abstract void setSelectLocked(boolean value)
```

تعیین می‌کند انتخاب این شکل ممنوع باشد. بولی قابل خواندن و نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getRotateLocked() {#getRotateLocked--}
```
public abstract boolean getRotateLocked()
```

تعیین می‌کند تغییر زاویه چرخش این شکل ممنوع باشد. بولی قابل خواندن و نوشتن.

**بازگشت:**
boolean
### setRotateLocked(boolean value) {#setRotateLocked-boolean-}
```
public abstract void setRotateLocked(boolean value)
```

تعیین می‌کند تغییر زاویه چرخش این شکل ممنوع باشد. بولی قابل خواندن و نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public abstract boolean getAspectRatioLocked()
```

تعیین می‌کند حفظ نسبت ابعاد هنگام تغییر اندازه برای این شکل لازم باشد. بولی قابل خواندن و نوشتن.

**بازگشت:**
boolean
### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public abstract void setAspectRatioLocked(boolean value)
```

تعیین می‌کند حفظ نسبت ابعاد هنگام تغییر اندازه برای این شکل لازم باشد. بولی قابل خواندن و نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getPositionLocked() {#getPositionLocked--}
```
public abstract boolean getPositionLocked()
```

تعیین می‌کند جابه‌جایی این شکل ممنوع باشد. بولی قابل خواندن و نوشتن.

**بازگشت:**
boolean
### setPositionLocked(boolean value) {#setPositionLocked-boolean-}
```
public abstract void setPositionLocked(boolean value)
```

تعیین می‌کند جابه‌جایی این شکل ممنوع باشد. بولی قابل خواندن و نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getSizeLocked() {#getSizeLocked--}
```
public abstract boolean getSizeLocked()
```

تعیین می‌کند تغییر اندازه این شکل ممنوع باشد. بولی قابل خواندن و نوشتن.

**بازگشت:**
boolean
### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public abstract void setSizeLocked(boolean value)
```

تعیین می‌کند تغییر اندازه این شکل ممنوع باشد. بولی قابل خواندن و نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getEditPointsLocked() {#getEditPointsLocked--}
```
public abstract boolean getEditPointsLocked()
```

تعیین می‌کند تغییر مستقیم کانتور این شکل ممنوع باشد. بولی قابل خواندن و نوشتن.

**بازگشت:**
boolean
### setEditPointsLocked(boolean value) {#setEditPointsLocked-boolean-}
```
public abstract void setEditPointsLocked(boolean value)
```

تعیین می‌کند تغییر مستقیم کانتور این شکل ممنوع باشد. بولی قابل خواندن و نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getAdjustHandlesLocked() {#getAdjustHandlesLocked--}
```
public abstract boolean getAdjustHandlesLocked()
```

تعیین می‌کند تغییر مقادیر تنظیمات ممنوع باشد. بولی قابل خواندن و نوشتن.

**بازگشت:**
boolean
### setAdjustHandlesLocked(boolean value) {#setAdjustHandlesLocked-boolean-}
```
public abstract void setAdjustHandlesLocked(boolean value)
```

تعیین می‌کند تغییر مقادیر تنظیمات ممنوع باشد. بولی قابل خواندن و نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getArrowheadsLocked() {#getArrowheadsLocked--}
```
public abstract boolean getArrowheadsLocked()
```

تعیین می‌کند تغییر سرهای فلش ممنوع باشد. بولی قابل خواندن و نوشتن.

**بازگشت:**
boolean
### setArrowheadsLocked(boolean value) {#setArrowheadsLocked-boolean-}
```
public abstract void setArrowheadsLocked(boolean value)
```

تعیین می‌کند تغییر سرهای فلش ممنوع باشد. بولی قابل خواندن و نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getShapeTypeLocked() {#getShapeTypeLocked--}
```
public abstract boolean getShapeTypeLocked()
```

تعیین می‌کند تغییر نوع شکل ممنوع باشد. بولی قابل خواندن و نوشتن.

**بازگشت:**
boolean
### setShapeTypeLocked(boolean value) {#setShapeTypeLocked-boolean-}
```
public abstract void setShapeTypeLocked(boolean value)
```

تعیید می‌کند تغییر نوع شکل ممنوع باشد. بولی قابل خواندن و نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getTextLocked() {#getTextLocked--}
```
public abstract boolean getTextLocked()
```

تعیین می‌کند ویرایش متن ممنوع باشد. بولی قابل خواندن و نوشتن.

**بازگشت:**
boolean
### setTextLocked(boolean value) {#setTextLocked-boolean-}
```
public abstract void setTextLocked(boolean value)
```

تعیین می‌کند ویرایش متن ممنوع باشد. بولی قابل خواندن و نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |