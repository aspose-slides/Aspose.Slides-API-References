---
title: IGraphicalObjectLock
second_title: Aspose.Slides برای اندروید از طریق مرجع API جاوا
description: تعیین می‌کند کدام عملیات‌ها بر روی شی گرافیکی والد غیرفعال هستند.
type: docs
url: /fa/com.aspose.slides/igraphicalobjectlock/
---
**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IBaseShapeLock](../../com.aspose.slides/ibaseshapelock)
```
public interface IGraphicalObjectLock extends IBaseShapeLock
```

تعیین می‌کند که کدام عملیات‌ها روی شیء گرافیکی والد غیرفعال هستند.
## متدها

| متد | توضیح |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | تعیین می‌کند آیا افزودن این شکل به یک گروه ممنوع است یا خیر. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | تعیین می‌کند آیا افزودن این شکل به یک گروه ممنوع است یا خیر. |
| [getDrilldownLocked()](#getDrilldownLocked--) | تعیین می‌کند آیا انتخاب زیرشکل‌های این شیء ممنوع است یا خیر. |
| [setDrilldownLocked(boolean value)](#setDrilldownLocked-boolean-) | تعیین می‌کند آیا انتخاب زیرشکل‌های این شیء ممنوع است یا خیر. |
| [getSelectLocked()](#getSelectLocked--) | تعیین می‌کند آیا انتخاب این شکل ممنوع است یا خیر. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | تعیین می‌کند آیا انتخاب این شکل ممنوع است یا خیر. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | تعیین می‌کند آیا شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند یا خیر. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | تعیین می‌کند آیا شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند یا خیر. |
| [getPositionLocked()](#getPositionLocked--) | تعیین می‌کند آیا جابجایی این شکل ممنوع است یا خیر. |
| [setPositionLocked(boolean value)](#setPositionLocked-boolean-) | تعیین می‌کند آیا جابجایی این شکل ممنوع است یا خیر. |
| [getSizeLocked()](#getSizeLocked--) | تعیین می‌کند آیا تغییر اندازه این شکل ممنوع است یا خیر. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | تعیین می‌کند آیا تغییر اندازه این شکل ممنوع است یا خیر. |
### getGroupingLocked() {#getGroupingLocked--}
```
public abstract boolean getGroupingLocked()
```

تعیین می‌کند آیا افزودن این شکل به یک گروه ممنوع است یا خیر. بولی قابل خواندن/نوشتن.

**بازگرداندن:**
boolean
### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public abstract void setGroupingLocked(boolean value)
```

تعیین می‌کند آیا افزودن این شکل به یک گروه ممنوع است یا خیر. بولی قابل خواندن/نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |
### getDrilldownLocked() {#getDrilldownLocked--}
```
public abstract boolean getDrilldownLocked()
```

تعیین می‌کند آیا انتخاب زیرشکل‌های این شیء ممنوع است یا خیر. بولی قابل خواندن/نوشتن.

**بازگرداندن:**
boolean
### setDrilldownLocked(boolean value) {#setDrilldownLocked-boolean-}
```
public abstract void setDrilldownLocked(boolean value)
```

تعیین می‌کند آیا انتخاب زیرشکل‌های این شیء ممنوع است یا خیر. بولی قابل خواندن/نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |
### getSelectLocked() {#getSelectLocked--}
```
public abstract boolean getSelectLocked()
```

تعیین می‌کند آیا انتخاب این شکل ممنوع است یا خیر. بولی قابل خواندن/نوشتن.

**بازگرداندن:**
boolean
### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public abstract void setSelectLocked(boolean value)
```

تعیین می‌کند آیا انتخاب این شکل ممنوع است یا خیر. بولی قابل خواندن/نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |
### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public abstract boolean getAspectRatioLocked()
```

تعیین می‌کند آیا شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند یا خیر. بولی قابل خواندن/نوشتن.

**بازگرداندن:**
boolean
### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public abstract void setAspectRatioLocked(boolean value)
```

تعیین می‌کند آیا شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند یا خیر. بولی قابل خواندن/نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |
### getPositionLocked() {#getPositionLocked--}
```
public abstract boolean getPositionLocked()
```

تعیین می‌کند آیا جابجایی این شکل ممنوع است یا خیر. بولی قابل خواندن/نوشتن.

**بازگرداندن:**
boolean
### setPositionLocked(boolean value) {#setPositionLocked-boolean-}
```
public abstract void setPositionLocked(boolean value)
```

تعیین می‌کند آیا جابجایی این شکل ممنوع است یا خیر. بولی قابل خواندن/نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |
### getSizeLocked() {#getSizeLocked--}
```
public abstract boolean getSizeLocked()
```

تعیین می‌کند آیا تغییر اندازه این شکل ممنوع است یا خیر. بولی قابل خواندن/نوشتن.

**بازگرداندن:**
boolean
### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public abstract void setSizeLocked(boolean value)
```

تعیین می‌کند آیا تغییر اندازه این شکل ممنوع است یا خیر. بولی قابل خواندن/نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |