---
title: ITextSearchOptions
second_title: Aspose.Slides for Android via Java API Reference
description: گزینه‌هایی را که می‌توان برای جستجوی متن در یک ارائه، اسلاید یا فریم متنی استفاده کرد، نمایان می‌کند.
type: docs
url: /fa/com.aspose.slides/itextsearchoptions/
---```
public interface ITextSearchOptions
```

گزینه‌هایی را که می‌توان برای جستجوی متن در یک ارائه، اسلاید یا فریم متنی استفاده کرد، نمایان می‌کند.
## متدها

| متد | توضیح |
| --- | --- |
| [getCaseSensitive()](#getCaseSensitive--) | اگر true تنظیم شود، جستجوی حساس به حروف انجام می‌شود، در غیر این صورت false. |
| [setCaseSensitive(boolean value)](#setCaseSensitive-boolean-) | اگر true تنظیم شود، جستجوی حساس به حروف انجام می‌شود، در غیر این صورت false. |
| [getWholeWordsOnly()](#getWholeWordsOnly--) | اگر true تنظیم شود، فقط کلمات کامل مطابقت داده می‌شوند، در غیر این صورت false. |
| [setWholeWordsOnly(boolean value)](#setWholeWordsOnly-boolean-) | اگر true تنظیم شود، فقط کلمات کامل مطابقت داده می‌شوند، در غیر این صورت false. |
| [getIncludeNotes()](#getIncludeNotes--) | اگر true تنظیم شود، متن موجود در یادداشت‌های اسلاید هنگام جستجو، جایگزینی یا هایلایت کردن در نظر گرفته می‌شود. |
| [setIncludeNotes(boolean value)](#setIncludeNotes-boolean-) | اگر true تنظیم شود، متن موجود در یادداشت‌های اسلاید هنگام جستجو، جایگزینی یا هایلایت کردن در نظر گرفته می‌شود. |
### getCaseSensitive() {#getCaseSensitive--}
```
public abstract boolean getCaseSensitive()
```

اگر true تنظیم شود، جستجوی حساس به حروف انجام می‌شود، در غیر این صورت false. خواندنی/نوشتنی boolean.

**بازگشت:**  
boolean
### setCaseSensitive(boolean value) {#setCaseSensitive-boolean-}
```
public abstract void setCaseSensitive(boolean value)
```

اگر true تنظیم شود، جستجوی حساس به حروف انجام می‌شود، در غیر این صورت false. خواندنی/نوشتنی boolean.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |
### getWholeWordsOnly() {#getWholeWordsOnly--}
```
public abstract boolean getWholeWordsOnly()
```

اگر true تنظیم شود، فقط کلمات کامل مطابقت داده می‌شوند، در غیر این صورت false. خواندنی/نوشتنی boolean.

**بازگشت:**  
boolean
### setWholeWordsOnly(boolean value) {#setWholeWordsOnly-boolean-}
```
public abstract void setWholeWordsOnly(boolean value)
```

اگر true تنظیم شود، فقط کلمات کامل مطابقت داده می‌شوند، در غیر این صورت false. خواندنی/نوشتنی boolean.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |
### getIncludeNotes() {#getIncludeNotes--}
```
public abstract boolean getIncludeNotes()
```

اگر true تنظیم شود، متن موجود در یادداشت‌های اسلاید هنگام جستجو، جایگزینی یا هایلایت کردن در نظر گرفته می‌شود. مقدار پیش‌فرض false است.

**بازگشت:**  
boolean
### setIncludeNotes(boolean value) {#setIncludeNotes-boolean-}
```
public abstract void setIncludeNotes(boolean value)
```

اگر true تنظیم شود، متن موجود در یادداشت‌های اسلاید هنگام جستجو، جایگزینی یا هایلایت کردن در نظر گرفته می‌شود. مقدار پیش‌فرض false است.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |