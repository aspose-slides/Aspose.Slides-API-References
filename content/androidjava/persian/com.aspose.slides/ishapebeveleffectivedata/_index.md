---
title: IShapeBevelEffectiveData
second_title: Aspose.Slides برای Android از طریق Java API Reference
description: شیء غیرقابل تغییر که ویژگی‌های رفع صورت شکل مؤثر را شامل می‌شود.
type: docs
url: /fa/com.aspose.slides/ishapebeveleffectivedata/
---```
public interface IShapeBevelEffectiveData
```

شیء غیرقابل تغییر که ویژگی‌های رفع صورت شکل مؤثر را شامل می‌شود.

--------------------

این رابط به عنوان بخشی از [IThreeDFormatEffectiveData](../../com.aspose.slides/ithreedformateffectivedata) استفاده می‌شود.
## متدها

| متد | توضیح |
| --- | --- |
| [getWidth()](#getWidth--) | عرض برش. |
| [getHeight()](#getHeight--) | ارتفاع برش. |
| [getBevelType()](#getBevelType--) | نوع برش. |
### getWidth() {#getWidth--}
```
public abstract double getWidth()
```

عرض برش. فقط خواندنی double.

**بازمی‌گرداند:**  
double
### getHeight() {#getHeight--}
```
public abstract double getHeight()
```

ارتفاع برش. فقط خواندنی double.

**بازمی‌گرداند:**  
double
### getBevelType() {#getBevelType--}
```
public abstract int getBevelType()
```

نوع برش. فقط خواندنی [BevelPresetType](../../com.aspose.slides/bevelpresettype).

**بازمی‌گرداند:**  
int