---
title: MathSpacingRules
second_title: Aspose.Slides برای اندروید از طریق مرجع API جاوا
description: انواع فاصلهٔ افقی بین ستون‌های یک ماتریس
type: docs
url: /fa/com.aspose.slides/mathspacingrules/
---
**ارث‌بری:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class MathSpacingRules extends System.Enum
```

انواع فاصله (فاصلهٔ افقی) بین ستون‌های یک ماتریس
## Fields

| Field | Description |
| --- | --- |
| [SingleSpacingGap](#SingleSpacingGap) | یک‌تک (1 em برای ستون‌ها یا 1 خط برای ردیف‌ها) |
| [OneAndHalfSpacingGap](#OneAndHalfSpacingGap) | یک و نیم (1.5 em برای ستون‌ها یا 1.5 خط برای ردیف‌ها) |
| [DoubleSpacingGap](#DoubleSpacingGap) | فاصلهٔ دو برابر (2 em برای ستون‌ها یا 2 خط برای ردیف‌ها) |
| [Exactly](#Exactly) | مقدار دقیق برای ستون‌ها، به مقدار ColumnGap اتکا می‌کند، که بر حسب twips اندازه‌گیری می‌شود؛ برای ردیف‌ها، به مقدار RowGap اتکا می‌کند، که بر حسب twips اندازه‌گیری می‌شود |
| [Multiple](#Multiple) | چندگانه برای ستون‌ها، به مقدار ColumnGap اتکا می‌کند، که بر حسب افزایش‌های 0.5 em اندازه‌گیری می‌شود؛ برای ردیف‌ها، به مقدار RowGap اتکا می‌کند، که بر حسب خط اندازه‌گیری می‌شود |
### SingleSpacingGap {#SingleSpacingGap}
```
public static final int SingleSpacingGap
```

یک‌تک (1 em برای ستون‌ها یا 1 خط برای ردیف‌ها)

### OneAndHalfSpacingGap {#OneAndHalfSpacingGap}
```
public static final int OneAndHalfSpacingGap
```

یک و نیم (1.5 em برای ستون‌ها یا 1.5 خط برای ردیف‌ها)

### DoubleSpacingGap {#DoubleSpacingGap}
```
public static final int DoubleSpacingGap
```

فاصلهٔ دو برابر (2 em برای ستون‌ها یا 2 خط برای ردیف‌ها)

### Exactly {#Exactly}
```
public static final int Exactly
```

مقدار دقیق برای ستون‌ها، به مقدار ColumnGap اتکا می‌کند، که بر حسب twips اندازه‌گیری می‌شود؛ برای ردیف‌ها، به مقدار RowGap اتکا می‌کند، که بر حسب twips اندازه‌گیری می‌شود

### Multiple {#Multiple}
```
public static final int Multiple
```

چندگانه برای ستون‌ها، به مقدار ColumnGap اتکا می‌کند، که بر حسب افزایش‌های 0.5 em اندازه‌گیری می‌شود؛ برای ردیف‌ها، به مقدار RowGap اتکا می‌کند، که بر حسب خط اندازه‌گیری می‌شود