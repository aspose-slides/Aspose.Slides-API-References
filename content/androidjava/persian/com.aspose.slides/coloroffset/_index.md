---
title: ColorOffset
second_title: مرجع API جاوا برای Aspose.Slides برای اندروید
description: نمایش جابجایی رنگ.
type: docs
url: /fa/com.aspose.slides/coloroffset/
---
**ارث‌بری:**
java.lang.Object

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IColorOffset](../../com.aspose.slides/icoloroffset)
```
public class ColorOffset implements IColorOffset
```

نمایش offset رنگ.
## سازندگان

| سازنده | توضیح |
| --- | --- |
| [ColorOffset()](#ColorOffset--) |  |
## متدها

| متد | توضیح |
| --- | --- |
| [getValue0()](#getValue0--) | مقدار اول offset را تعریف می‌کند. |
| [setValue0(float value)](#setValue0-float-) | مقدار اول offset را تعریف می‌کند. |
| [getValue1()](#getValue1--) | مقدار دوم offset را تعریف می‌کند. |
| [setValue1(float value)](#setValue1-float-) | مقدار دوم offset را تعریف می‌کند. |
| [getValue2()](#getValue2--) | مقدار سوم offset را تعریف می‌کند. |
| [setValue2(float value)](#setValue2-float-) | مقدار سوم offset را تعریف می‌کند. |
### ColorOffset() {#ColorOffset--}
```
public ColorOffset()
```

### getValue0() {#getValue0--}
```
public final float getValue0()
```

مقدار اول offset را تعریف می‌کند. float خواندنی/نوشتنی.

**بازگشت:**
float
### setValue0(float value) {#setValue0-float-}
```
public final void setValue0(float value)
```

مقدار اول offset را تعریف می‌کند. float خواندنی/نوشتنی.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getValue1() {#getValue1--}
```
public final float getValue1()
```

مقدار دوم offset را تعریف می‌کند. float خواندنی/نوشتنی.

**بازگشت:**
float
### setValue1(float value) {#setValue1-float-}
```
public final void setValue1(float value)
```

مقدار دوم offset را تعریف می‌کند. float خواندنی/نوشتنی.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getValue2() {#getValue2--}
```
public final float getValue2()
```

مقدار سوم offset را تعریف می‌کند. float خواندنی/نوشتنی.

**بازگشت:**
float
### setValue2(float value) {#setValue2-float-}
```
public final void setValue2(float value)
```

مقدار سوم offset را تعریف می‌کند. float خواندنی/نوشتنی.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |