---
title: IGradientFormatEffectiveData
second_title: Aspose.Slides برای اندروید از طریق مرجع API جاوا
description: شیء غیرقابل تغییر که شامل خصوصیات مؤثر پر کردن گرادیان است.
type: docs
url: /fa/com.aspose.slides/igradientformateffectivedata/
---
**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IFillParamSource](../../com.aspose.slides/ifillparamsource)
```
public interface IGradientFormatEffectiveData extends IFillParamSource
```

شیء غیرقابل تغییر که شامل خصوصیات مؤثر پر کردن گرادیان است.

--------------------

این رابط به عنوان بخشی از [IFillFormatEffectiveData](../../com.aspose.slides/ifillformateffectivedata) و [ILineFillFormatEffectiveData](../../com.aspose.slides/ilinefillformateffectivedata) استفاده می‌شود.
## متدها

| متد | توضیح |
| --- | --- |
| [getTileFlip()](#getTileFlip--) | حالت چرخاندن یک گرادیان را برمی‌گرداند. |
| [getGradientDirection()](#getGradientDirection--) | سبک یک گرادیان را برمی‌گرداند. |
| [getLinearGradientAngle()](#getLinearGradientAngle--) | زاویه یک گرادیان را برمی‌گرداند. |
| [getLinearGradientScaled()](#getLinearGradientScaled--) | تعیین می‌کند آیا گرادیان مقیاس‌دار است یا خیر. |
| [getGradientShape()](#getGradientShape--) | شکل یک گرادیان را برمی‌گرداند. |
| [getGradientStops()](#getGradientStops--) | مجموعه نقاط توقف گرادیان را برمی‌گرداند. |
### getTileFlip() {#getTileFlip--}
```
public abstract int getTileFlip()
```

حالت چرخاندن یک گرادیان را برمی‌گرداند. فقط‌قابل‌خواندن [TileFlip](../../com.aspose.slides/tileflip).

**بازگشت:**
int
### getGradientDirection() {#getGradientDirection--}
```
public abstract int getGradientDirection()
```

سبک یک گرادیان را برمی‌گرداند. فقط‌قابل‌خواندن [GradientDirection](../../com.aspose.slides/gradientdirection).

**بازگشت:**
int
### getLinearGradientAngle() {#getLinearGradientAngle--}
```
public abstract float getLinearGradientAngle()
```

زاویه یک گرادیان را برمی‌گرداند. فقط‌قابل‌خواندن float.

**بازگشت:**
float
### getLinearGradientScaled() {#getLinearGradientScaled--}
```
public abstract boolean getLinearGradientScaled()
```

تعیین می‌کند آیا گرادیان مقیاس‌دار است یا خیر. فقط‌قابل‌خواندن boolean.

**بازگشت:**
boolean
### getGradientShape() {#getGradientShape--}
```
public abstract byte getGradientShape()
```

شکل یک گرادیان را برمی‌گرداند. فقط‌قابل‌خواندن [GradientShape](../../com.aspose.slides/gradientshape).

**بازگشت:**
byte
### getGradientStops() {#getGradientStops--}
```
public abstract IGradientStopCollectionEffectiveData getGradientStops()
```

مجموعه نقاط توقف گرادیان را برمی‌گرداند. فقط‌قابل‌خواندن [IGradientStopCollectionEffectiveData](../../com.aspose.slides/igradientstopcollectioneffectivedata).

**بازگشت:**
[IGradientStopCollectionEffectiveData](../../com.aspose.slides/igradientstopcollectioneffectivedata)