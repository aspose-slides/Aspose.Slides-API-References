---
title: DataSourceType
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: انواع منبع داده.
type: docs
url: /fa/com.aspose.slides/datasourcetype/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class DataSourceType extends System.Enum
```

انواع منبع داده.
## فیلدها

| فیلد | توضیح |
| --- | --- |
| [Worksheet](#Worksheet) | از اشیای CellXls برای ذخیره XValue، YValue، Value یا BubbleSize نقطه داده در worksheet استفاده کنید. |
| [StringLiterals](#StringLiterals) | از متغیر رشته با دسترسی سریع برای ذخیره XValue، YValue، Value یا BubbleSize نقطه داده استفاده کنید. |
| [DoubleLiterals](#DoubleLiterals) | از متغیر double با دسترسی سریع برای ذخیره XValue، YValue، Value یا BubbleSize نقطه داده استفاده کنید. |
### Worksheet {#Worksheet}
```
public static final int Worksheet
```

از اشیای CellXls برای ذخیره XValue، YValue، Value یا BubbleSize نقطه داده در worksheet استفاده کنید.

### StringLiterals {#StringLiterals}
```
public static final int StringLiterals
```

از متغیر رشته با دسترسی سریع برای ذخیره XValue، YValue، Value یا BubbleSize نقطه داده استفاده کنید. Worksheet برای ذخیره مقادیر استفاده نمی‌شود.

### DoubleLiterals {#DoubleLiterals}
```
public static final int DoubleLiterals
```

از متغیر double با دسترسی سریع برای ذخیره XValue، YValue، Value یا BubbleSize نقطه داده استفاده کنید. Worksheet برای ذخیره مقادیر استفاده نمی‌شود.