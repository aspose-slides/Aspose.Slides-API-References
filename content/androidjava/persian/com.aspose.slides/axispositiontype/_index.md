---
title: AxisPositionType
second_title: Aspose.Slides برای Android از طریق مرجع API Java
description: موقعیت محور را تعیین می‌کند.
type: docs
url: /fa/com.aspose.slides/axispositiontype/
---
**ارث‌بری:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class AxisPositionType extends System.Enum
```

موقعیت محور را تعیین می‌کند.
## فیلدها

| فیلد | توضیح |
| --- | --- |
| [Bottom](#Bottom) | مشخص می‌کند که محور در پایین ناحیه نمودار نمایش داده شود. |
| [Left](#Left) | مشخص می‌کند که محور در چپ ناحیه نمودار نمایش داده شود. |
| [Right](#Right) | مشخص می‌کند که محور در راست ناحیه نمودار نمایش داده شود. |
| [Top](#Top) | مشخص می‌کند که محور در بالا ناحیه نمودار نمایش داده شود. |
### پایین {#Bottom}
```
public static final int Bottom
```

مشخص می‌کند که محور در پایین ناحیه نمودار نمایش داده شود.

### چپ {#Left}
```
public static final int Left
```

مشخص می‌کند که محور در چپ ناحیه نمودار نمایش داده شود.

### راست {#Right}
```
public static final int Right
```

مشخص می‌کند که محور در راست ناحیه نمودار نمایش داده شود.

### بالا {#Top}
```
public static final int Top
```

مشخص می‌کند که محور در بالا ناحیه نمودار نمایش داده شود.