---
title: TextSearchOptions
second_title: Aspose.Slides برای اندروید از طریق مرجع API جاوا
description: نمایانگر گزینه‌هایی است که می‌توان برای جستجوی متن در یک اسلاید ارائه یا TextFrame استفاده کرد.
type: docs
url: /fa/com.aspose.slides/textsearchoptions/
---
**وراثت:**
java.lang.Object

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.ITextSearchOptions](../../com.aspose.slides/itextsearchoptions)
```
public class TextSearchOptions implements ITextSearchOptions
```

نمایانگر گزینه‌هایی است که می‌توان برای جستجوی متن در یک Presentation، Slide یا TextFrame استفاده کرد.

## سازنده‌ها

| سازنده | توضیح |
| --- | --- |
| [TextSearchOptions()](#TextSearchOptions--) | گزینه‌های پیش‌فرض جستجوی متن را ایجاد می‌کند. |

## متدها

| متد | توضیح |
| --- | --- |
| [getCaseSensitive()](#getCaseSensitive--) | در صورتی که true باشد، از جستجوی حساس به حروف استفاده می‌شود؛ در غیر این صورت false. |
| [setCaseSensitive(boolean value)](#setCaseSensitive-boolean-) | در صورتی که true باشد، از جستجوی حساس به حروف استفاده می‌شود؛ در غیر این صورت false. |
| [getWholeWordsOnly()](#getWholeWordsOnly--) | اگر true باشد، فقط کلمات کامل مطابقت می‌کنند؛ در غیر این صورت false. |
| [setWholeWordsOnly(boolean value)](#setWholeWordsOnly-boolean-) | اگر true باشد، فقط کلمات کامل مطابقت می‌کنند؛ در غیر این صورت false. |
| [getIncludeNotes()](#getIncludeNotes--) | اگر true باشد، متن موجود در یادداشت‌های اسلاید ([NotesSlide](../../com.aspose.slides/notesslide)) هنگام انجام عملیات جستجو، جایگزینی یا برجسته‌سازی شامل می‌شود. |
| [setIncludeNotes(boolean value)](#setIncludeNotes-boolean-) | اگر true باشد، متن موجود در یادداشت‌های اسلاید ([NotesSlide](../../com.aspose.slides/notesslide)) هنگام انجام عملیات جستجو، جایگزینی یا برجسته‌سازی شامل می‌شود. |

### TextSearchOptions() {#TextSearchOptions--}
```
public TextSearchOptions()
```

گزینه‌های پیش‌فرض جستجوی متن را ایجاد می‌کند.

### getCaseSensitive() {#getCaseSensitive--}
```
public final boolean getCaseSensitive()
```

اگر true باشد، از جستجوی حساس به حروف استفاده می‌شود؛ در غیر این صورت false. خواندنی/نوشتنی  boolean .

**بازگشت:**
boolean

### setCaseSensitive(boolean value) {#setCaseSensitive-boolean-}
```
public final void setCaseSensitive(boolean value)
```

اگر true باشد، از جستجوی حساس به حروف استفاده می‌شود؛ در غیر این صورت false. خواندنی/نوشتنی  boolean .

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getWholeWordsOnly() {#getWholeWordsOnly--}
```
public final boolean getWholeWordsOnly()
```

اگر true باشد، فقط کلمات کامل مطابقت می‌کنند؛ در غیر این صورت false. خواندنی/نوشتنی  boolean .

**بازگشت:**
boolean

### setWholeWordsOnly(boolean value) {#setWholeWordsOnly-boolean-}
```
public final void setWholeWordsOnly(boolean value)
```

اگر true باشد، فقط کلمات کامل مطابقت می‌کنند؛ در غیر این صورت false. خواندنی/نوشتنی  boolean .

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getIncludeNotes() {#getIncludeNotes--}
```
public final boolean getIncludeNotes()
```

اگر true باشد، متن موجود در یادداشت‌های اسلاید ([NotesSlide](../../com.aspose.slides/notesslide)) هنگام انجام عملیات جستجو، جایگزینی یا برجسته‌سازی شامل می‌شود. مقدار پیش‌فرض false است.

**بازگشت:**
boolean

### setIncludeNotes(boolean value) {#setIncludeNotes-boolean-}
```
public final void setIncludeNotes(boolean value)
```

اگر true باشد، متن موجود در یادداشت‌های اسلاید ([NotesSlide](../../com.aspose.slides/notesslide)) هنگام انجام عملیات جستجو، جایگزینی یا برجسته‌سازی شامل می‌شود. مقدار پیش‌فرض false است.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |