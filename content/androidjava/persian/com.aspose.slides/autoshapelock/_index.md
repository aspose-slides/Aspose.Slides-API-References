---
title: AutoShapeLock
second_title: Aspose.Slides برای Android از طریق مرجع API Java
description: مشخص می‌کند کدام عملیات‌ها بر روی AutoshapeEx والد غیرفعال هستند.
type: docs
url: /fa/com.aspose.slides/autoshapelock/
---
**وراثت:**  
java.lang.Object, [com.aspose.slides.BaseShapeLock](../../com.aspose.slides/baseshapelock)

**تمام رابط‌های پیاده‌سازی‌شده:**  
[com.aspose.slides.IAutoShapeLock](../../com.aspose.slides/iautoshapelock)  
```
public class AutoShapeLock extends BaseShapeLock implements IAutoShapeLock
```

مشخص می‌کند که کدام عملیات‌ها بر روی AutoshapeEx والد غیرفعال هستند.
## متدها

| متد | توضیح |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | مشخص می‌کند که افزودن این شکل به یک گروه ممنوع است یا نه. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | مشخص می‌کند که افزودن این شکل به یک گروه ممنوع است یا نه. |
| [getSelectLocked()](#getSelectLocked--) | مشخص می‌کند که انتخاب این شکل ممنوع است یا نه. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | مشخص می‌کند که انتخاب این شکل ممنوع است یا نه. |
| [getRotateLocked()](#getRotateLocked--) | مشخص می‌کند که تغییر زاویه چرخش این شکل ممنوع است یا نه. |
| [setRotateLocked(boolean value)](#setRotateLocked-boolean-) | مشخص می‌کند که تغییر زاویه چرخش این شکل ممنوع است یا نه. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | مشخص می‌کند که این شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند یا نه. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | مشخص می‌کند که این شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند یا نه. |
| [getPositionLocked()](#getPositionLocked--) | مشخص می‌کند که جابجایی این شکل ممنوع است یا نه. |
| [setPositionLocked(boolean value)](#setPositionLocked-boolean-) | مشخص می‌کند که جابجایی این شکل ممنوع است یا نه. |
| [getSizeLocked()](#getSizeLocked--) | مشخص می‌کند که تغییر اندازه این شکل ممنوع است یا نه. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | مشخص می‌کند که تغییر اندازه این شکل ممنوع است یا نه. |
| [getEditPointsLocked()](#getEditPointsLocked--) | مشخص می‌کند که تغییر مستقیم contour این شکل ممنوع است یا نه. |
| [setEditPointsLocked(boolean value)](#setEditPointsLocked-boolean-) | مشخص می‌کند که تغییر مستقیم contour این شکل ممنوع است یا نه. |
| [getAdjustHandlesLocked()](#getAdjustHandlesLocked--) | مشخص می‌کند که تغییر مقادیر adjust ممنوع است یا نه. |
| [setAdjustHandlesLocked(boolean value)](#setAdjustHandlesLocked-boolean-) | مشخص می‌کند که تغییر مقادیر adjust ممنوع است یا نه. |
| [getArrowheadsLocked()](#getArrowheadsLocked--) | مشخص می‌کند که تغییر arrowheads ممنوع است یا نه. |
| [setArrowheadsLocked(boolean value)](#setArrowheadsLocked-boolean-) | مشخص می‌کند که تغییر arrowheads ممنوع است یا نه. |
| [getShapeTypeLocked()](#getShapeTypeLocked--) | مشخص می‌کند که تغییر نوع شکل ممنوع است یا نه. |
| [setShapeTypeLocked(boolean value)](#setShapeTypeLocked-boolean-) | مشخص می‌کند که تغییر نوع شکل ممنوع است یا نه. |
| [getTextLocked()](#getTextLocked--) | مشخص می‌کند که ویرایش متن ممنوع است یا نه. |
| [setTextLocked(boolean value)](#setTextLocked-boolean-) | مشخص می‌کند که ویرایش متن ممنوع است یا نه. |

### getGroupingLocked() {#getGroupingLocked--}
```
public boolean getGroupingLocked()
```

مشخص می‌کند که افزودن این شکل به یک گروه ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگرداندن:**  
boolean
### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public void setGroupingLocked(boolean value)
```

مشخص می‌کند که افزودن این شکل به یک گروه ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getSelectLocked() {#getSelectLocked--}
```
public boolean getSelectLocked()
```

مشخص می‌کند که انتخاب این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگرداندن:**  
boolean
### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public void setSelectLocked(boolean value)
```

مشخص می‌کند که انتخاب این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getRotateLocked() {#getRotateLocked--}
```
public boolean getRotateLocked()
```

مشخص می‌کند که تغییر زاویه چرخش این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگرداندن:**  
boolean
### setRotateLocked(boolean value) {#setRotateLocked-boolean-}
```
public void setRotateLocked(boolean value)
```

مشخص می‌کند که تغییر زاویه چرخش این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public boolean getAspectRatioLocked()
```

مشخص می‌کند که این شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند یا نه. خواندنی/نوشتنی boolean.

**بازگرداندن:**  
boolean
### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public void setAspectRatioLocked(boolean value)
```

مشخص می‌کند که این شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getPositionLocked() {#getPositionLocked--}
```
public boolean getPositionLocked()
```

مشخص می‌کند که جابجایی این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگرداندن:**  
boolean
### setPositionLocked(boolean value) {#setPositionLocked-boolean-}
```
public void setPositionLocked(boolean value)
```

مشخص می‌کند که جابجایی این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getSizeLocked() {#getSizeLocked--}
```
public boolean getSizeLocked()
```

مشخص می‌کند که تغییر اندازه این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگرداندن:**  
boolean
### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public void setSizeLocked(boolean value)
```

مشخص می‌کند که تغییر اندازه این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getEditPointsLocked() {#getEditPointsLocked--}
```
public boolean getEditPointsLocked()
```

مشخص می‌کند که تغییر مستقیم contour این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگرداندن:**  
boolean
### setEditPointsLocked(boolean value) {#setEditPointsLocked-boolean-}
```
public void setEditPointsLocked(boolean value)
```

مشخص می‌کند که تغییر مستقیم contour این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getAdjustHandlesLocked() {#getAdjustHandlesLocked--}
```
public boolean getAdjustHandlesLocked()
```

مشخص می‌کند که تغییر مقادیر adjust ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگرداندن:**  
boolean
### setAdjustHandlesLocked(boolean value) {#setAdjustHandlesLocked-boolean-}
```
public void setAdjustHandlesLocked(boolean value)
```

مشخص می‌کند که تغییر مقادیر adjust ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getArrowheadsLocked() {#getArrowheadsLocked--}
```
public boolean getArrowheadsLocked()
```

مشخص می‌کند که تغییر arrowheads ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگرداندن:**  
boolean
### setArrowheadsLocked(boolean value) {#setArrowheadsLocked-boolean-}
```
public void setArrowheadsLocked(boolean value)
```

مشخص می‌کند که تغییر arrowheads ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getShapeTypeLocked() {#getShapeTypeLocked--}
```
public boolean getShapeTypeLocked()
```

مشخص می‌کند که تغییر نوع شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگرداندن:**  
boolean
### setShapeTypeLocked(boolean value) {#setShapeTypeLocked-boolean-}
```
public void setShapeTypeLocked(boolean value)
```

مشخص می‌کند که تغییر نوع شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getTextLocked() {#getTextLocked--}
```
public boolean getTextLocked()
```

مشخص می‌کند که ویرایش متن ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگرداندن:**  
boolean
### setTextLocked(boolean value) {#setTextLocked-boolean-}
```
public void setTextLocked(boolean value)
```

مشخص می‌کند که ویرایش متن ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |