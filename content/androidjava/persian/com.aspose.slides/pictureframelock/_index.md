---
title: PictureFrameLock
second_title: Aspose.Slides برای Android از طریق مستندات API جاوا
description: مشخص می‌کند کدام عملیات‌ها بر روی PictureFrame والد غیرفعال هستند.
type: docs
url: /fa/com.aspose.slides/pictureframelock/
---
**وراثت:**  
java.lang.Object, [com.aspose.slides.BaseShapeLock](../../com.aspose.slides/baseshapelock)

**همه رابط‌های پیاده‌سازی‌شده:**  
[com.aspose.slides.IPictureFrameLock](../../com.aspose.slides/ipictureframelock)  
```
public class PictureFrameLock extends BaseShapeLock implements IPictureFrameLock
```

تشخیص می‌دهد که کدام عملیات‌ها روی PictureFrame والد غیرفعال هستند.

## متدها

| متد | توضیح |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | تشخیص می‌دهد که افزودن این شکل به یک گروه ممنوع است یا نه. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | تشخیص می‌دهد که افزودن این شکل به یک گروه ممنوع است یا نه. |
| [getSelectLocked()](#getSelectLocked--) | تشخیص می‌دهد که انتخاب این شکل ممنوع است یا نه. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | تشخیص می‌دهد که انتخاب این شکل ممنوع است یا نه. |
| [getRotationLocked()](#getRotationLocked--) | تشخیص می‌دهد که تغییر زاویه چرخش این شکل ممنوع است یا نه. |
| [setRotationLocked(boolean value)](#setRotationLocked-boolean-) | تشخیص می‌دهد که تغییر زاویه چرخش این شکل ممنوع است یا نه. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | تشخیص می‌دهد که آیا شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند یا نه. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | تشخیص می‌دهد که آیا شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند یا نه. |
| [getPositionLocked()](#getPositionLocked--) | تشخیص می‌دهد که جابجایی این شکل ممنوع است یا نه. |
| [setPositionLocked(boolean value)](#setPositionLocked-boolean-) | تشخیص می‌دهد که جابجایی این شکل ممنوع است یا نه. |
| [getSizeLocked()](#getSizeLocked--) | تشخیص می‌دهد که تغییر اندازه این شکل ممنوع است یا نه. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | تشخیص می‌دهد که تغییر اندازه این شکل ممنوع است یا نه. |
| [getEditPointsLocked()](#getEditPointsLocked--) | تشخیص می‌دهد که تغییر مستقیم contour این شکل ممنوع است یا نه. |
| [setEditPointsLocked(boolean value)](#setEditPointsLocked-boolean-) | تشخیص می‌دهد که تغییر مستقیم contour این شکل ممنوع است یا نه. |
| [getAdjustHandlesLocked()](#getAdjustHandlesLocked--) | تشخیص می‌دهد که تغییر مقادیر adjust ممنوع است یا نه. |
| [setAdjustHandlesLocked(boolean value)](#setAdjustHandlesLocked-boolean-) | تشخیص می‌دهد که تغییر مقادیر adjust ممنوع است یا نه. |
| [getArrowheadsLocked()](#getArrowheadsLocked--) | تشخیص می‌دهد که تغییر arrowheads ممنوع است یا نه. |
| [setArrowheadsLocked(boolean value)](#setArrowheadsLocked-boolean-) | تشخیص می‌دهد که تغییر arrowheads ممنوع است یا نه. |
| [getShapeTypeLocked()](#getShapeTypeLocked--) | تشخیص می‌دهد که تغییر نوع شکل ممنوع است یا نه. |
| [setShapeTypeLocked(boolean value)](#setShapeTypeLocked-boolean-) | تشخیص می‌دهد که تغییر نوع شکل ممنوع است یا نه. |
| [getCropLocked()](#getCropLocked--) | تشخیص می‌دهد که برش تصویر ممنوع است یا نه. |
| [setCropLocked(boolean value)](#setCropLocked-boolean-) | تشخیص می‌دهد که برش تصویر ممنوع است یا نه. |

### getGroupingLocked() {#getGroupingLocked--}
```
public boolean getGroupingLocked()
```

تشخیص می‌دهد که افزودن این شکل به یک گروه ممنوع است یا نه. خواندن/نوشتن boolean.

**بازگشت:**  
boolean

### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public void setGroupingLocked(boolean value)
```

تشخیص می‌دهد که افزودن این شکل به یک گروه ممنوع است یا نه. خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getSelectLocked() {#getSelectLocked--}
```
public boolean getSelectLocked()
```

تشخیص می‌دهد که انتخاب این شکل ممنوع است یا نه. خواندن/نوشتن boolean.

**بازگشت:**  
boolean

### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public void setSelectLocked(boolean value)
```

تشخیص می‌دهد که انتخاب این شکل ممنوع است یا نه. خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getRotationLocked() {#getRotationLocked--}
```
public boolean getRotationLocked()
```

تشخیص می‌دهد که تغییر زاویه چرخش این شکل ممنوع است یا نه. خواندن/نوشتن boolean.

**بازگشت:**  
boolean

### setRotationLocked(boolean value) {#setRotationLocked-boolean-}
```
public void setRotationLocked(boolean value)
```

تشخیص می‌دهد که تغییر زاویه چرخش این شکل ممنوع است یا نه. خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public boolean getAspectRatioLocked()
```

تشخیص می‌دهد که آیا شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند یا نه. خواندن/نوشتن boolean.

**بازگشت:**  
boolean

### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public void setAspectRatioLocked(boolean value)
```

تشخیص می‌دهد که آیا شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند یا نه. خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getPositionLocked() {#getPositionLocked--}
```
public boolean getPositionLocked()
```

تشخیص می‌دهد که جابجایی این شکل ممنوع است یا نه. خواندن/نوشتن boolean.

**بازگشت:**  
boolean

### setPositionLocked(boolean value) {#setPositionLocked-boolean-}
```
public void setPositionLocked(boolean value)
```

تشخیص می‌دهد که جابجایی این شکل ممنوع است یا نه. خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getSizeLocked() {#getSizeLocked--}
```
public boolean getSizeLocked()
```

تشخیص می‌دهد که تغییر اندازه این شکل ممنوع است یا نه. خواندن/نوشتن boolean.

**بازگشت:**  
boolean

### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public void setSizeLocked(boolean value)
```

تشخیص می‌دهد که تغییر اندازه این شکل ممنوع است یا نه. خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getEditPointsLocked() {#getEditPointsLocked--}
```
public boolean getEditPointsLocked()
```

تشخیص می‌دهد که تغییر مستقیم contour این شکل ممنوع است یا نه. خواندن/نوشتن boolean.

**بازگشت:**  
boolean

### setEditPointsLocked(boolean value) {#setEditPointsLocked-boolean-}
```
public void setEditPointsLocked(boolean value)
```

تشخیص می‌دهد که تغییر مستقیم contour این شکل ممنوع است یا نه. خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getAdjustHandlesLocked() {#getAdjustHandlesLocked--}
```
public boolean getAdjustHandlesLocked()
```

تشخیص می‌دهد که تغییر مقادیر adjust ممنوع است یا نه. خواندن/نوشتن boolean.

**بازگشت:**  
boolean

### setAdjustHandlesLocked(boolean value) {#setAdjustHandlesLocked-boolean-}
```
public void setAdjustHandlesLocked(boolean value)
```

تشخیص می‌دهد که تغییر مقادیر adjust ممنوع است یا نه. خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getArrowheadsLocked() {#getArrowheadsLocked--}
```
public boolean getArrowheadsLocked()
```

تشخیص می‌دهد که تغییر arrowheads ممنوع است یا نه. خواندن/نوشتن boolean.

**بازگشت:**  
boolean

### setArrowheadsLocked(boolean value) {#setArrowheadsLocked-boolean-}
```
public void setArrowheadsLocked(boolean value)
```

تشخیص می‌دهد که تغییر arrowheads ممنوع است یا نه. خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getShapeTypeLocked() {#getShapeTypeLocked--}
```
public boolean getShapeTypeLocked()
```

تشخیص می‌دهد که تغییر نوع شکل ممنوع است یا نه. خواندن/نوشتن boolean.

**بازگشت:**  
boolean

### setShapeTypeLocked(boolean value) {#setShapeTypeLocked-boolean-}
```
public void setShapeTypeLocked(boolean value)
```

تشخیص می‌دهد که تغییر نوع شکل ممنوع است یا نه. خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getCropLocked() {#getCropLocked--}
```
public boolean getCropLocked()
```

تشخیص می‌دهد که برش تصویر ممنوع است یا نه. خواندن/نوشتن boolean.

**بازگشت:**  
boolean

### setCropLocked(boolean value) {#setCropLocked-boolean-}
```
public void setCropLocked(boolean value)
```

تشخیص می‌دهد که برش تصویر ممنوع است یا نه. خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |