---
title: SmartArtLayoutType
second_title: Aspose.Slides برای Android – مرجع API جاوا
description: نمایانگر نوع چیدمان یک نمودار SmartArt است.
type: docs
url: /fa/com.aspose.slides/smartartlayouttype/
---
**ارث‌بری:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class SmartArtLayoutType extends System.Enum
```

نمایانگر نوع آرایش یک نمودار SmartArt است.
## فیلدها

| فیلد | توضیح |
| --- | --- |
| [AccentProcess](#AccentProcess) | از آن برای نمایش پیشرفت، خط زمانی یا مراحل متوالی در یک کار، فرآیند یا جریان کاری استفاده می‌شود. |
| [AccentedPicture](#AccentedPicture) | از آن برای نمایش یک ایدهٔ عکاسی مرکزی با ایده‌های مرتبط در کنار آن استفاده می‌شود. |
| [AlternatingFlow](#AlternatingFlow) | از آن برای نمایش گروه‌های اطلاعات یا مراحل متوالی در یک کار، فرآیند یا جریان کاری استفاده می‌شود. |
| [AlternatingHexagons](#AlternatingHexagons) | از آن برای نمایش مجموعه‌ای از ایده‌های مرتبط استفاده می‌شود. |
| [AlternatingPictureBlocks](#AlternatingPictureBlocks) | از آن برای نمایش مجموعه‌ای از تصاویر از بالا به پایین استفاده می‌شود. |
| [AlternatingPictureCircles](#AlternatingPictureCircles) | از آن برای نمایش مجموعه‌ای از تصاویر همراه با متن استفاده می‌شود. |
| [ArrowRibbon](#ArrowRibbon) | از آن برای نمایش مفاهیم مرتبط یا متضاد با برخی ارتباط‌ها، مانند نیروهای مخالف، استفاده می‌شود. |
| [AscendingPictureAccentProcess](#AscendingPictureAccentProcess) | از آن برای نمایش یک مجموعه صعودی از تصاویر با متن توصیفی استفاده می‌شود. |
| [Balance](#Balance) | از آن برای مقایسه یا نمایش رابطه بین دو ایده استفاده می‌شود. |
| [BasicBendingProcess](#BasicBendingProcess) | از آن برای نمایش پیشرفت یا مراحل متوالی در یک کار، فرآیند یا جریان کاری استفاده می‌شود. |
| [BasicBlockList](#BasicBlockList) | از آن برای نمایش بلوک‌های اطلاعاتی غیرمتوالی یا گروه‌بندی شده استفاده می‌شود. |
| [BasicChevronProcess](#BasicChevronProcess) | از آن برای نمایش پیشرفت؛ خط زمانی؛ مراحل متوالی در یک کار، فرآیند یا جریان کاری؛ یا برای تأکید بر حرکت یا جهت استفاده می‌شود. |
| [BasicCycle](#BasicCycle) | از آن برای نمایش یک سلسلهٔ مداوم از مراحل، وظایف یا رویدادها در یک جریان چرخه‌ای استفاده می‌شود. |
| [BasicMatrix](#BasicMatrix) | از آن برای نمایش رابطهٔ اجزا به یک کل در چهار قسمت استفاده می‌شود. |
| [BasicPie](#BasicPie) | از آن برای نمایش اینکه چگونه بخش‌های فردی یک کل را تشکیل می‌دهند استفاده می‌شود. |
| [BasicProcess](#BasicProcess) | از آن برای نمایش پیشرفت یا مراحل متوالی در یک کار، فرآیند یا جریان کاری استفاده می‌شود. |
| [BasicPyramid](#BasicPyramid) | از آن برای نمایش روابط نسبی، مرتبط یا سلسله‌مراتبی با بزرگ‌ترین جزء در پایین و به سمت بالا باریک شدن استفاده می‌شود. |
| [BasicRadial](#BasicRadial) | از آن برای نمایش رابطه به یک ایدهٔ مرکزی در یک چرخه استفاده می‌شود. |
| [BasicTarget](#BasicTarget) | از آن برای نمایش محتوا، درجات یا روابط سلسله‌مراتبی استفاده می‌شود. |
| [BasicTimeline](#BasicTimeline) | از آن برای نمایش مراحل متوالی در یک کار، فرآیند یا جریان کاری یا برای نمایش اطلاعات خط زمانی استفاده می‌شود. |
| [BasicVenn](#BasicVenn) | از آن برای نمایش روابط متقاطع یا مرتبط استفاده می‌شود. |
| [BendingPictureAccentList](#BendingPictureAccentList) | از آن برای نمایش بلوک‌های اطلاعاتی غیرمتوالی یا گروه‌بندی شده استفاده می‌شود. |
| [BendingPictureBlocks](#BendingPictureBlocks) | از آن برای نمایش مجموعه‌ای از تصاویر استفاده می‌شود. |
| [BendingPictureCaption](#BendingPictureCaption) | از آن برای نمایش یک سری متوالی از تصاویر استفاده می‌شود. |
| [BendingPictureCaptionList](#BendingPictureCaptionList) | از آن برای نمایش مجموعه‌ای از تصاویر استفاده می‌شود. |
| [BendingPictureSemiTransparentText](#BendingPictureSemiTransparentText) | از آن برای نمایش مجموعه‌ای از تصاویر استفاده می‌شود. |
| [BlockCycle](#BlockCycle) | از آن برای نمایش یک سلسلهٔ مداوم از مراحل، وظایف یا رویدادها در یک جریان چرخه‌ای استفاده می‌شود. |
| [BubblePictureList](#BubblePictureList) | از آن برای نمایش مجموعه‌ای از تصاویر استفاده می‌شود. |
| [CaptionedPictures](#CaptionedPictures) | از آن برای نمایش تصاویر با سطوح مختلف متن استفاده می‌شود. |
| [ChevronList](#ChevronList) | از آن برای نمایش پیشرفت از طریق چندین فرآیند که یک جریان کاری کلی را تشکیل می‌دهند استفاده می‌شود. |
| [CircleAccentTimeline](#CircleAccentTimeline) | از آن برای نمایش مجموعه‌ای از رویدادها یا اطلاعات خط زمانی استفاده می‌شود. |
| [CircleArrowProcess](#CircleArrowProcess) | از آن برای نمایش آیتم‌های متوالی با متن پشتیبان برای هر آیتم استفاده می‌شود. |
| [CirclePictureHierarchy](#CirclePictureHierarchy) | از آن برای نمایش اطلاعات سلسله‌مراتبی یا روابط گزارش‌دهی در یک سازمان استفاده می‌شود. |
| [CircleRelationship](#CircleRelationship) | از آن برای نمایش رابطه به یا از یک ایدهٔ مرکزی استفاده می‌شود. |
| [CircularBendingProcess](#CircularBendingProcess) | از آن برای نمایش یک توالی طولانی یا غیرخطی از مراحل یا گام‌ها در یک کار، فرآیند یا جریان کاری استفاده می‌شود. |
| [CircularPictureCallout](#CircularPictureCallout) | از آن برای نمایش یک ایدهٔ مرکزی و زیرایده‌ها یا آیتم‌های مرتبط استفاده می‌شود. |
| [ClosedChevronProcess](#ClosedChevronProcess) | از آن برای نمایش پیشرفت، خط زمانی یا مراحل متوالی در یک کار، فرآیند یا جریان کاری یا برای تأکید بر حرکت یا جهت استفاده می‌شود. |
| [ContinuousArrowProcess](#ContinuousArrowProcess) | از آن برای نمایش خط زمانی یا مراحل متوالی در یک کار، فرآیند یا جریان کاری استفاده می‌شود. |
| [ContinuousBlockProcess](#ContinuousBlockProcess) | از آن برای نمایش پیشرفت یا مراحل متوالی در یک کار، فرآیند یا جریان کاری استفاده می‌شود. |
| [ContinuousCycle](#ContinuousCycle) | از آن برای نمایش یک سلسلهٔ مداوم از مراحل، وظایف یا رویدادها در یک جریان چرخه‌ای استفاده می‌شود. |
| [ContinuousPictureList](#ContinuousPictureList) | از آن برای نمایش گروه‌های اطلاعاتی مرتبط استفاده می‌شود. |
| [ConvergingArrows](#ConvergingArrows) | از آن برای نمایش ایده‌ها یا مفاهیمی که به یک نقطهٔ مرکزی همگرا می‌شوند استفاده می‌شود. |
| [ConvergingRadial](#ConvergingRadial) | از آن برای نمایش روابط مفاهیم یا اجزا به یک ایدهٔ مرکزی در یک چرخه استفاده می‌شود. |
| [CounterbalanceArrows](#CounterbalanceArrows) | از آن برای نمایش دو ایده یا مفهوم مخالف استفاده می‌شود. |
| [CycleMatrix](#CycleMatrix) | از آن برای نمایش رابطه به یک ایدهٔ مرکزی در یک پیشرفت چرخه‌ای استفاده می‌شود. |
| [DescendingBlockList](#DescendingBlockList) | از آن برای نمایش گروه‌های ایده‌های مرتبط یا فهرست‌های اطلاعات استفاده می‌شود. |
| [DescendingProcess](#DescendingProcess) | از آن برای نمایش یک سلسلهٔ نزولی از رویدادها استفاده می‌شود. |
| [DetailedProcess](#DetailedProcess) | برای متون سطح ۲ بزرگ استفاده می‌شود تا پیشرفت از طریق مراحل را نشان دهد. |
| [DivergingArrows](#DivergingArrows) | از آن برای نمایش ایده‌ها یا مفاهیمی که از منبع مرکزی به سمت بیرون پیشرفت می‌کنند استفاده می‌شود. |
| [DivergingRadial](#DivergingRadial) | از آن برای نمایش روابط به یک ایدهٔ مرکزی در یک چرخه استفاده می‌شود. |
| [Equation](#Equation) | از آن برای نمایش مراحل یا وظایفی که برنامه یا نتیجه‌ای را نشان می‌دهند استفاده می‌شود. |
| [FramedTextPicture](#FramedTextPicture) | از آن برای نمایش تصاویر با متن سطح ۱ متناظر در یک فریم استفاده می‌شود. |
| [Funnel](#Funnel) | از آن برای نمایش فیلتر کردن اطلاعات یا چگونگی ترکیب بخش‌ها به یک کل استفاده می‌شود. |
| [Gear](#Gear) | از آن برای نمایش ایده‌های درهم‌تنید استفاده می‌شود. |
| [GridMatrix](#GridMatrix) | از آن برای نمایش مکان‌گذاری مفاهیم در طول دو محور استفاده می‌شود. |
| [GroupedList](#GroupedList) | از آن برای نمایش گروه‌ها و زیرگروه‌های اطلاعات یا گام‌ها و زیرگام‌ها در یک کار، فرآیند یا جریان کاری استفاده می‌شود. |
| [HalfCircleOrganizationChart](#HalfCircleOrganizationChart) | از آن برای نمایش اطلاعات سلسله‌مراتبی یا روابط گزارش‌دهی در یک سازمان استفاده می‌شود. |
| [HexagonCluster](#HexagonCluster) | از آن برای نمایش تصاویر با متن توصیفی مربوطه استفاده می‌شود. |
| [Hierarchy](#Hierarchy) | از آن برای نمایش روابط سلسله‌مراتبی که از بالا به پایین پیش می‌روند استفاده می‌شود. |
| [HierarchyList](#HierarchyList) | از آن برای نمایش روابط سلسله‌مراتبی که از گروه به گروه پیش می‌روند استفاده می‌شود. |
| [HorizontalBulletList](#HorizontalBulletList) | از آن برای نمایش فهرست‌های اطلاعاتی غیرمتوالی یا گروه‌بندی شده استفاده می‌شود. |
| [HorizontalHierarchy](#HorizontalHierarchy) | از آن برای نمایش روابط سلسله‌مراتبی که به صورت افقی پیش می‌روند استفاده می‌شود. |
| [HorizontalLabeledHierarchy](#HorizontalLabeledHierarchy) | از آن برای نمایش روابط سلسله‌مراتبی که به صورت افقی پیش می‌روند و به‌صورت گروهی سلسله‌مراتبی هستند استفاده می‌شود. |
| [HorizontalMultiLevelHierarchy](#HorizontalMultiLevelHierarchy) | از آن برای نمایش مقادیر بزرگ اطلاعات سلسله‌مراتبی که به صورت افقی پیش می‌روند استفاده می‌شود. |
| [HorizontalOrganizationChart](#HorizontalOrganizationChart) | از آن برای نمایش اطلاعات سلسله‌مراتبی به‌صورت افقی یا روابط گزارش‌دهی در یک سازمان استفاده می‌شود. |
| [HorizontalPictureList](#HorizontalPictureList) | از آن برای نمایش اطلاعات غیرمتوالی یا گروه‌بندی شده با تأکید بر تصاویر مرتبط استفاده می‌شود. |
| [IncreasingArrowsProcess](#IncreasingArrowsProcess) | از آن برای نمایش گام‌های متوالی و همپوشان در یک فرآیند استفاده می‌شود. |
| [IncreasingCircleProcess](#IncreasingCircleProcess) | از آن برای نمایش یک سری گام‌ها که درون دایره با هر گام افزایش می‌یابد استفاده می‌شود. |
| [InvertedPyramid](#InvertedPyramid) | از آن برای نمایش روابط نسبی، مرتبط یا سلسله‌مراتبی با بزرگ‌ترین جزء در بالا و باریک شدن به سمت پایین استفاده می‌شود. |
| [LabeledHierarchy](#LabeledHierarchy) | از آن برای نمایش روابط سلسله‌مراتبی که از بالا به پایین پیش می‌روند و به‌صورت گروهی سلسله‌مراتبی هستند استفاده می‌شود. |
| [LinearVenn](#LinearVenn) | از آن برای نمایش روابط همپوشان در یک توالی استفاده می‌شود. |
| [LinedList](#LinedList) | از آن برای نمایش مقادیر بزرگ متن تقسیم‌شده به دسته‌ها و زیردسته‌ها استفاده می‌شود. |
| [MultidirectionalCycle](#MultidirectionalCycle) | از آن برای نمایش یک سلسلهٔ مداوم از مراحل، وظایف یا رویدادهایی که می‌توانند در هر جهت رخ دهند استفاده می‌شود. |
| [NameandTitleOrganizationChart](#NameandTitleOrganizationChart) | از آن برای نمایش اطلاعات سلسله‌مراتبی یا روابط گزارش‌دهی در یک سازمان استفاده می‌شود. |
| [NestedTarget](#NestedTarget) | از آن برای نمایش روابط حاوی‌سازی استفاده می‌شود. |
| [NondirectionalCycle](#NondirectionalCycle) | از آن برای نمایش یک سلسلهٔ مداوم از مراحل، وظایف یا رویدادها در یک جریان چرخه‌ای استفاده می‌شود. |
| [OpposingArrows](#OpposingArrows) | از آن برای نمایش دو ایدهٔ مخالف یا ایده‌هایی که از نقطهٔ مرکزی متفاوت می‌شوند استفاده می‌شود. |
| [OpposingIdeas](#OpposingIdeas) | از آن برای نمایش دو ایدهٔ مخالف یا متضاد استفاده می‌شود. |
| [OrganizationChart](#OrganizationChart) | از آن برای نمایش اطلاعات سلسله‌مراتبی یا روابط گزارش‌دهی در یک سازمان استفاده می‌شود. |
| [PhasedProcess](#PhasedProcess) | از آن برای نمایش سه فاز یک فرآیند استفاده می‌شود. |
| [PictureAccentBlocks](#PictureAccentBlocks) | از آن برای نمایش یک گروه از تصاویر در بلوک‌ها که از گوشه شروع می‌شوند استفاده می‌شود. |
| [PictureAccentList](#PictureAccentList) | از آن برای نمایش اطلاعات گروه‌بندی‌شده یا مرتبط استفاده می‌شود. |
| [PictureAccentProcess](#PictureAccentProcess) | از آن برای نمایش گام‌های متوالی در یک کار، فرآیند یا جریان کاری استفاده می‌شود. |
| [PictureCaptionList](#PictureCaptionList) | از آن برای نمایش بلوک‌های اطلاعاتی غیرمتوالی یا گروه‌بندی شده استفاده می‌شود. |
| [PictureGrid](#PictureGrid) | از آن برای نمایش تصاویر چینش‌یافته روی یک شبکهٔ مربعی استفاده می‌شود. |
| [PictureLineup](#PictureLineup) | از آن برای نمایش یک سری از تصاویر در کنار هم استفاده می‌شود. |
| [PictureStrips](#PictureStrips) | از آن برای نمایش یک سری از تصاویر از بالا به پایین با متن سطح ۱ در کنار هر کدام استفاده می‌شود. |
| [PieProcess](#PieProcess) | از آن برای نمایش گام‌های یک فرآیند که هر برش پای‌کیک با افزایش اندازه تا هفت شکل رشد می‌کند استفاده می‌شود. |
| [PlusandMinus](#PlusandMinus) | از آن برای نمایش مزایا و معایب دو ایده استفاده می‌شود. |
| [ProcessArrows](#ProcessArrows) | از آن برای نمایش اطلاعاتی که یک فرآیند یا جریان کاری را نشان می‌دهد استفاده می‌شود. |
| [ProcessList](#ProcessList) | از آن برای نمایش گروه‌های متعدد اطلاعات یا گام‌ها و زیرگام‌ها در یک کار، فرآیند یا جریان کاری استفاده می‌شود. |
| [PyramidList](#PyramidList) | از آن برای نمایش روابط نسبی، مرتبط یا سلسله‌مراتبی استفاده می‌شود. |
| [RadialCluster](#RadialCluster) | از آن برای نمایش داده‌هایی که به یک ایدهٔ مرکزی یا تم مرتبط هستند استفاده می‌شود. |
| [RadialCycle](#RadialCycle) | از آن برای نمایش رابطه به یک ایدهٔ مرکزی استفاده می‌شود. |
| [RadialList](#RadialList) | از آن برای نمایش روابط به یک ایدهٔ مرکزی در یک چرخه استفاده می‌شود. |
| [RadialVenn](#RadialVenn) | از آن برای نمایش همپوشانی روابط و رابطه به یک ایدهٔ مرکزی در یک چرخه استفاده می‌شود. |
| [RandomToResultProcess](#RandomToResultProcess) | از آن برای نشان دادن این‌که از طریق یک سری گام‌ها، چگونه ایده‌های آشوب‌مانند می‌توانند به یک هدف یا ایدهٔ واحد منجر شوند استفاده می‌شود. |
| [RepeatingBendingProcess](#RepeatingBendingProcess) | از آن برای نمایش پیشرفت یا گام‌های متوالی در یک کار، فرآیند یا جریان کاری استفاده می‌شود. |
| [ReverseList](#ReverseList) | از آن برای جابه‌جایی بین دو آیتم استفاده می‌شود. |
| [SegmentedCycle](#SegmentedCycle) | از آن برای نمایش پیشرفت یا یک توالی از مراحل، وظایف یا رویدادها در یک جریان چرخه‌ای استفاده می‌شود. |
| [SegmentedProcess](#SegmentedProcess) | از آن برای نمایش پیشرفت یا گام‌های متوالی در یک کار، فرآیند یا جریان کاری استفاده می‌شود. |
| [SegmentedPyramid](#SegmentedPyramid) | از آن برای نمایش روابط حاوی‌سازی، نسبی یا مرتبط استفاده می‌شود. |
| [SnapshotPictureList](#SnapshotPictureList) | از آن برای نمایش تصاویر با متن توضیحی استفاده می‌شود. |
| [SpiralPicture](#SpiralPicture) | از آن برای نمایش یک سری تا پنج تصویر با عنوان‌های سطح ۱ متناظر که به مرکز می‌پیچند استفاده می‌شود. |
| [SquareAccentList](#SquareAccentList) | از آن برای نمایش فهرست‌های اطلاعات تقسیم‌شده به دسته‌ها استفاده می‌شود. |
| [StackedList](#StackedList) | از آن برای نمایش گروه‌های اطلاعات یا گام‌ها در یک کار، فرآیند یا جریان کاری استفاده می‌شود. |
| [StackedVenn](#StackedVenn) | از آن برای نمایش روابط همپوشان استفاده می‌شود. |
| [StaggeredProcess](#StaggeredProcess) | از آن برای نمایش پیشرفت نزولی از طریق مراحل استفاده می‌شود. |
| [StepDownProcess](#StepDownProcess) | از آن برای نمایش یک فرآیند نزولی با چندین گام و زیرگام استفاده می‌شود. |
| [StepUpProcess](#StepUpProcess) | از آن برای نمایش یک سری صعودی از گام‌ها یا فهرست‌های اطلاعات استفاده می‌شود. |
| [SubStepProcess](#SubStepProcess) | از آن برای نمایش یک فرآیند چندگامی با زیرگام‌ها بین هر متن سطح ۱ استفاده می‌شود. |
| [TableHierarchy](#TableHierarchy) | از آن برای نمایش گروه‌های اطلاعاتی ساخته‌شده از بالا به پایین و سلسله‌مراتب‌های درون هر گروه استفاده می‌شود. |
| [TableList](#TableList) | از آن برای نمایش اطلاعات گروه‌بندی‌شده یا مرتبط با ارزش برابر استفاده می‌شود. |
| [TargetList](#TargetList) | از آن برای نمایش اطلاعات مرتبط یا همپوشان استفاده می‌شود. |
| [TextCycle](#TextCycle) | از آن برای نمایش یک سلسلهٔ مداوم از مراحل، وظایف یا رویدادها در یک جریان چرخه‌ای استفاده می‌شود. |
| [TitlePictureLineup](#TitlePictureLineup) | از آن برای نمایش یک سری تصاویر که هر کدام عنوان و توضیح خود را دارند استفاده می‌شود. |
| [TitledMatrix](#TitledMatrix) | از آن برای نمایش روابط چهار ربع به یک کل استفاده می‌شود. |
| [TitledPictureAccentList](#TitledPictureAccentList) | از آن برای نمایش فهرست‌های اطلاعات با یک تصویر برجسته برای هر متن سطح ۲ استفاده می‌شود. |
| [TitledPictureBlocks](#TitledPictureBlocks) | از آن برای نمایش یک سری تصاویر استفاده می‌شود. |
| [TrapezoidList](#TrapezoidList) | از آن برای نمایش اطلاعات گروه‌بندی‌شده یا مرتبط با ارزش برابر استفاده می‌شود. |
| [UpwardArrow](#UpwardArrow) | از آن برای نمایش پیشرفت یا گام‌هایی که در یک کار، فرآیند یا جریان کاری به سمت بالا تمایل دارند استفاده می‌شود. |
| [VerticalAccentList](#VerticalAccentList) | از آن برای نمایش فهرست‌های اطلاعات استفاده می‌شود. |
| [VerticalArrowList](#VerticalArrowList) | از آن برای نمایش پیشرفت یا گام‌های متوالی در یک کار، فرآیند یا جریان کاری که به سمت هدف مشترک حرکت می‌کند استفاده می‌شود. |
| [VerticalBendingProcess](#VerticalBendingProcess) | از آن برای نمایش پیشرفت یا گام‌های متوالی در یک کار، فرآیند یا جریان کاری استفاده می‌شود. |
| [VerticalBlockList](#VerticalBlockList) | از آن برای نمایش گروه‌های اطلاعات یا گام‌ها در یک کار، فرآیند یا جریان کاری استفاده می‌شود. |
| [VerticalBoxList](#VerticalBoxList) | از آن برای نمایش چندین گروه اطلاعات، به‌ویژه گروه‌هایی با متن سطح ۲ زیاد استفاده می‌شود. |
| [VerticalBulletList](#VerticalBulletList) | از آن برای نمایش بلوک‌های اطلاعاتی غیرمتوالی یا گروه‌بندی شده استفاده می‌شود. |
| [VerticalChevronList](#VerticalChevronList) | از آن برای نمایش پیشرفت یا گام‌های متوالی در یک کار، فرآیند یا جریان کاری یا برای تأکید بر حرکت یا جهت استفاده می‌شود. |
| [VerticalCircleList](#VerticalCircleList) | از آن برای نمایش داده‌های متوالی یا گروه‌بندی شده استفاده می‌شود. |
| [VerticalCurvedList](#VerticalCurvedList) | از آن برای نمایش یک فهرست منحنی از اطلاعات استفاده می‌شود. |
| [VerticalEquation](#VerticalEquation) | از آن برای نمایش گام‌های متوالی یا وظایفی که برنامه یا نتیجه‌ای را نشان می‌دهند استفاده می‌شود. |
| [VerticalPictureAccentList](#VerticalPictureAccentList) | از آن برای نمایش بلوک‌های اطلاعاتی غیرمتوالی یا گروه‌بندی شده استفاده می‌شود. |
| [VerticalPictureList](#VerticalPictureList) | از آن برای نمایش بلوک‌های اطلاعاتی غیرمتوالی یا گروه‌بندی شده استفاده می‌شود. |
| [VerticalProcess](#VerticalProcess) | از آن برای نمایش پیشرفت یا گام‌های متوالی در یک کار، فرآیند یا جریان کاری از بالا به پایین استفاده می‌شود. |
| [Custom](#Custom) | نمایانگر نمودار SmartArt با قالب چیدمان سفارشی است |
| [PictureOrganizationChart](#PictureOrganizationChart) | از آن برای نمایش اطلاعات سلسله‌مراتبی یا روابط گزارش‌دهی در یک سازمان، با تصاویر متناظر استفاده می‌شود. |
### AccentProcess {#AccentProcess}
```
public static final int AccentProcess
```

از آن برای نمایش پیشرفت، خط زمانی یا گام‌های متوالی در یک کار، فرآیند یا جریان کاری استفاده می‌شود. برای نمایش همزمان متن سطح ۱ و سطح ۲ مناسب است.

### AccentedPicture {#AccentedPicture}
```
public static final int AccentedPicture
```

از آن برای نمایش یک ایدهٔ عکاسی مرکزی با ایده‌های مرتبط در کنار آن استفاده می‌شود. متن سطح ۱ بالای تصویر مرکزی ظاهر می‌شود. متن متناظر برای اشکال سطح ۱ دیگر در کنار تصاویر دایره‌ای کوچک قرار می‌گیرد. این چیدمان حتی بدون متن نیز کار می‌کند.

### AlternatingFlow {#AlternatingFlow}
```
public static final int AlternatingFlow
```

از آن برای نمایش گروه‌های اطلاعاتی یا گام‌های متوالی در یک کار، فرآیند یا جریان کاری استفاده می‌شود. بر تعامل یا روابط بین گروه‌های اطلاعاتی تأکید دارد.

### AlternatingHexagons {#AlternatingHexagons}
```
public static final int AlternatingHexagons
```

از آن برای نمایش یک سری ایده‌های مرتبط استفاده می‌شود. متن سطح ۱ داخل پنج‌ضلعی‌ها ظاهر می‌شود. متن سطح ۲ خارج از اشکال قرار می‌گیرد.

### AlternatingPictureBlocks {#AlternatingPictureBlocks}
```
public static final int AlternatingPictureBlocks
```

از آن برای نمایش یک سری تصاویر از بالا به پایین استفاده می‌شود. متن به‌صورت متناوب در سمت راست یا چپ تصویر ظاهر می‌شود.

### AlternatingPictureCircles {#AlternatingPictureCircles}
```
public static final int AlternatingPictureCircles
```

از آن برای نمایش مجموعه‌ای از تصاویر با متن استفاده می‌شود. متن متناظر در دایره‌های مرکزی ظاهر می‌شود و تصاویر به‌صورت چپ-راست جایگزین می‌شوند.

### ArrowRibbon {#ArrowRibbon}
```
public static final int ArrowRibbon
```

از آن برای نمایش مفاهیم مرتبط یا متضاد با برخی ارتباط‌ها، مانند نیروهای مخالف، استفاده می‌شود. دو خط اول متن سطح ۱ برای متن در پیکان‌ها استفاده می‌شود. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### AscendingPictureAccentProcess {#AscendingPictureAccentProcess}
```
public static final int AscendingPictureAccentProcess
```

از آن برای نمایش یک سری صعودی از تصاویر با متن توصیفی استفاده می‌شود. برای متن کم‌حجم بهترین است.

### Balance {#Balance}
```
public static final int Balance
```

از آن برای مقایسه یا نمایش رابطه بین دو ایده استفاده می‌شود. هر یک از دو خط اول متن سطح ۱ متنی در بالای یک طرف نقطهٔ مرکزی دارد. بر متن سطح ۲ تأکید می‌شود که به‌چهار شکل در هر طرف محدود است. تعادل به طرفی که بیشترین شکل‌های شامل متن سطح ۲ را دارد گرایش می‌یابد. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### BasicBendingProcess {#BasicBendingProcess}
```
public static final int BasicBendingProcess
```

از آن برای نمایش پیشرفت یا گام‌های متوالی در یک کار، فرآیند یا جریان کاری استفاده می‌شود. فضای افقی و عمودی نمایش اشکال را به حداکثر می‌رساند.

### BasicBlockList {#BasicBlockList}
```
public static final int BasicBlockList
```

از آن برای نمایش بلوک‌های اطلاعاتی غیرمتوالی یا گروه‌بندی شده استفاده می‌شود. فضای افقی و عمودی نمایش اشکال را به حداکثر می‌رساند.

### BasicChevronProcess {#BasicChevronProcess}
```
public static final int BasicChevronProcess
```

از آن برای نمایش پیشرفت؛ خط زمانی؛ گام‌های متوالی در یک کار، فرآیند یا جریان کاری؛ یا برای تأکید بر حرکت یا جهت استفاده می‌شود. متن سطح ۱ داخل شکل پیکان ظاهر می‌شود در حالی که متن سطح ۲ زیر اشکال پیکان قرار می‌گیرد.

### BasicCycle {#BasicCycle}
```
public static final int BasicCycle
```

از آن برای نمایش یک توالی مداوم از مراحل، وظایف یا رویدادها در یک جریان چرخه‌ای استفاده می‌شود. بر مراحل یا گام‌ها تأکید دارد نه بر پیکان‌های متصل یا جریان. برای صرفاً متن سطح ۱ مناسب‌ترین است.

### BasicMatrix {#BasicMatrix}
```
public static final int BasicMatrix
```

از آن برای نمایش رابطه اجزا به یک کل در چهار ربع استفاده می‌شود. چهار خط اول متن سطح ۱ در ربع‌ها ظاهر می‌شود. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### BasicPie {#BasicPie}
```
public static final int BasicPie
```

از آن برای نمایش چگونگی تشکیل یک کل از بخش‌های فردی استفاده می‌شود. هفت خط اول متن سطح ۱ متناظر با قطعات مساوی پیت یا دایره هستند. متن سطح ۱ بالایی خارج از بقیه پیت برای تأکید ظاهر می‌شود. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### BasicProcess {#BasicProcess}
```
public static final int BasicProcess
```

از آن برای نمایش پیشرفت یا گام‌های متوالی در یک کار، فرآیند یا جریان کاری استفاده می‌شود.

### BasicPyramid {#BasicPyramid}
```
public static final int BasicPyramid
```

از آن برای نمایش روابط نسبی، مرتبط یا سلسله‌مراتبی با بزرگ‌ترین جزء در پایین و باریک شدن به سمت بالا استفاده می‌شود. متن سطح ۱ در بخش‌های هرم ظاهر می‌شود و متن سطح ۲ در اشکال کنار هر بخش.

### BasicRadial {#BasicRadial}
```
public static final int BasicRadial
```

از آن برای نمایش رابطه به یک ایدهٔ مرکزی در یک چرخه استفاده می‌شود. خط اول متن سطح ۱ متناظر با شکل مرکزی است و متن سطح ۲ آن به اشکال دایره‌ای اطراف مربوط می‌شود. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### BasicTarget {#BasicTarget}
```
public static final int BasicTarget
```

از آن برای نمایش روابط حاوی‌سازی، درجات یا روابط سلسله‌مراتبی استفاده می‌شود. پنج خط اول متن سطح ۱ به یک دایره مرتبط هستند. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### BasicTimeline {#BasicTimeline}
```
public static final int BasicTimeline
```

از آن برای نمایش گام‌های متوالی در یک کار، فرآیند یا جریان کاری یا برای نمایش اطلاعات خط زمانی استفاده می‌شود. برای متن‌های سطح ۱ و سطح ۲ هر دو مناسب است.

### BasicVenn {#BasicVenn}
```
public static final int BasicVenn
```

از آن برای نمایش روابط همپوشان یا مرتبط استفاده می‌شود. هفت خط اول متن سطح ۱ متناظر با یک دایره هستند. اگر چهار خط یا کمتر وجود داشته باشد، متن داخل دایره‌ها قرار می‌گیرد؛ اگر بیشتر باشد، متن خارج دایره‌ها قرار می‌گیرد. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### BendingPictureAccentList {#BendingPictureAccentList}
```
public static final int BendingPictureAccentList
```

از آن برای نمایش بلوک‌های اطلاعاتی غیرمتوالی یا گروه‌بندی شده استفاده می‌شود. اشکال دایره‌ای کوچک برای شامل کردن تصاویر طراحی شده‌اند. برای نمایش همزمان متن سطح ۱ و سطح ۲ مناسب است. فضای افقی و عمودی نمایش اشکال را به حداکثر می‌رساند.

### BendingPictureBlocks {#BendingPictureBlocks}
```
public static final int BendingPictureBlocks
```

از آن برای نمایش یک سری تصاویر استفاده می‌شود. جعبهٔ پوشانندهٔ گوشهٔ پایین می‌تواند متن کمی را شامل شود.

### BendingPictureCaption {#BendingPictureCaption}
```
public static final int BendingPictureCaption
```

از آن برای نمایش یک سری متوالی از تصاویر استفاده می‌شود. جعبهٔ پوشانندهٔ گوشهٔ پایین می‌تواند متن کمی را شامل شود.

### BendingPictureCaptionList {#BendingPictureCaptionList}
```
public static final int BendingPictureCaptionList
```

از آن برای نمایش یک سری تصاویر استفاده می‌شود. عنوان و توضیح در یک شکل حاشیه‌دار زیر هر تصویر ظاهر می‌شود.

### BendingPictureSemiTransparentText {#BendingPictureSemiTransparentText}
```
public static final int BendingPictureSemiTransparentText
```

از آن برای نمایش یک سری تصاویر استفاده می‌شود. جعبه‌ای نیمه‌شفاف بخش پایین تصویر را می‌پوشاند و تمام سطوح متن را در بر می‌گیرد.

### BlockCycle {#BlockCycle}
```
public static final int BlockCycle
```

از آن برای نمایش یک توالی مداوم از مراحل، وظایف یا رویدادها در یک جریان چرخه‌ای استفاده می‌شود. بر مراحل یا گام‌ها تأکید دارد نه بر پیکان‌های متصل یا جریان.

### BubblePictureList {#BubblePictureList}
```
public static final int BubblePictureList
```

از آن برای نمایش یک سری تصاویر استفاده می‌شود. می‌تواند تا هشت تصویر سطح ۱ را در بر بگیرد. متن و تصاویر استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند. برای متن کم‌حجم بهترین است.

### CaptionedPictures {#CaptionedPictures}
```
public static final int CaptionedPictures
```

از آن برای نمایش تصاویر با سطوح مختلف متن استفاده می‌شود. برای متن کم‌حجم سطح ۱ و متن متوسط سطح ۲ مناسب‌ترین است.

### ChevronList {#ChevronList}
```
public static final int ChevronList
```

از آن برای نمایش پیشرفت از طریق چندین فرآیند که یک جریان کاری کلی را تشکیل می‌دهند استفاده می‌شود. همچنین برای نشان دادن فرآیندهای متضاد مناسب است. متن سطح ۱ به اولین شکل پیکان چپ متناظر است، در حالی که متن سطح ۲ به گام‌های افقی زیر هر شکلی که متن سطح ۱ دارد متناظر می‌شود.

### CircleAccentTimeline {#CircleAccentTimeline}
```
public static final int CircleAccentTimeline
```

از آن برای نمایش یک سری رویدادها یا اطلاعات خط زمانی استفاده می‌شود. متن سطح ۱ در کنار اشکال دایره‌ای بزرگتر ظاهر می‌شود. متن سطح ۲ در کنار اشکال دایره‌ای کوچکتر ظاهر می‌شود.

### CircleArrowProcess {#CircleArrowProcess}}
```
public static final int CircleArrowProcess
```

از آن برای نمایش آیتم‌های متوالی با متن پشتیبان برای هر آیتم استفاده می‌شود. این نمودار برای متن‌های کم‌حجم سطح ۱ بهترین است.

### CirclePictureHierarchy {#CirclePictureHierarchy}
```
public static final int CirclePictureHierarchy
```

از آن برای نمایش اطلاعات سلسله‌مراتبی یا روابط گزارش‌دهی در یک سازمان استفاده می‌شود. تصاویر در دایره‌ها ظاهر می‌شوند و متن متناظر در کنار تصاویر قرار می‌گیرد.

### CircleRelationship {#CircleRelationship}
```
public static final int CircleRelationship
```

از آن برای نمایش رابطه به یا از یک ایدهٔ مرکزی استفاده می‌شود. متن سطح ۲ به‌صورت غیرمتوالی افزوده می‌شود و به پنج آیتم محدود است. تنها می‌تواند یک آیتم سطح ۱ وجود داشته باشد.

### CircularBendingProcess {#CircularBendingProcess}
```
public static final int CircularBendingProcess
```

از آن برای نمایش یک توالی طولانی یا غیرخطی از گام‌ها در یک کار، فرآیند یا جریان کاری استفاده می‌شود. فقط برای متن سطح ۱ مناسب است. فضای افقی و عمودی نمایش اشکال را به حداکثر می‌رساند.

### CircularPictureCallout {#CircularPictureCallout}
```
public static final int CircularPictureCallout
```

از آن برای نمایش ایدهٔ مرکزی و زیرایده‌ها یا آیتم‌های مرتبط استفاده می‌شود. متن اولین تصویر بخش پایین تصویر را می‌پوشاند. متن متناظر برای دیگر اشکال سطح ۱ در کنار تصاویر دایره‌ای کوچک ظاهر می‌شود. این نمودار حتی بدون متن نیز کار می‌کند.

### ClosedChevronProcess {#ClosedChevronProcess}
```
public static final int ClosedChevronProcess
```

از آن برای نمایش پیشرفت، خط زمانی یا گام‌های متوالی در یک کار، فرآیند یا جریان کاری یا برای تأکید بر حرکت یا جهت استفاده می‌شود. می‌تواند برای تأکید بر اطلاعات در شکل شروعی استفاده شود. برای صرفاً متن سطح ۱ مناسب‌ترین است.

### ContinuousArrowProcess {#ContinuousArrowProcess}
```
public static final int ContinuousArrowProcess
```

از آن برای نمایش خط زمانی یا گام‌های متوالی در یک کار، فرآیند یا جریان کاری استفاده می‌شود. برای متن سطح ۱ مناسب است زیرا هر خط متن سطح ۱ داخل شکل پیکان ظاهر می‌شود. متن سطح ۲ خارج از شکل پیکان قرار می‌گیرد.

### ContinuousBlockProcess {#ContinuousBlockProcess}
```
public static final int ContinuousBlockProcess
```

از آن برای نمایش پیشرفت یا گام‌های متوالی در یک کار، فرآیند یا جریان کاری استفاده می‌شود. برای متون کمینه سطح ۱ و سطح ۲ بهترین است.

### ContinuousCycle {#ContinuousCycle}
```
public static final int ContinuousCycle
```

از آن برای نمایش توالی مداوم از مراحل، وظایف یا رویدادها در یک جریان چرخه‌ای استفاده می‌شود. بر ارتباط بین تمام اجزا تأکید دارد. برای صرفاً متن سطح ۱ مناسب‌ترین است.

### ContinuousPictureList {#ContinuousPictureList}
```
public static final int ContinuousPictureList
```

از آن برای نمایش گروه‌های اطلاعاتی مرتبط استفاده می‌شود. اشکال دایره‌ای برای شامل کردن تصاویر طراحی شده‌اند.

### ConvergingArrows {#ConvergingArrows}
```
public static final int ConvergingArrows
```

از آن برای نمایش ایده‌ها یا مفاهیمی که به یک نقطهٔ مرکزی همگرا می‌شوند استفاده می‌شود. برای صرفاً متن سطح ۱ مناسب‌ترین است.

### ConvergingRadial {#ConvergingRadial}
```
public static final int ConvergingRadial
```

از آن برای نمایش روابط مفاهیم یا اجزا به یک ایدهٔ مرکزی در یک چرخه استفاده می‌شود. خط اول متن سطح ۱ متناظر با شکل دایره‌ای مرکزی است و خطوط متن سطح ۲ به اشکال مستطیل اطراف مربوط می‌شود. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### CounterbalanceArrows {#CounterbalanceArrows}
```
public static final int CounterbalanceArrows
```

از آن برای نمایش دو ایده یا مفهوم مخالف استفاده می‌شود. هر یک از دو خط اول متن سطح ۱ متناظر با یک پیکان است و با متن سطح ۲ کار می‌کند. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### CycleMatrix {#CycleMatrix}
```
public static final int CycleMatrix
```

از آن برای نمایش رابطه به یک ایدهٔ مرکزی در یک پیشرفت چرخه‌ای استفاده می‌شود. هر یک از چهار خط اول متن سطح ۱ متناظر با یک قطعه یا شکل پیت هستند و متن سطح ۲ در یک شکل مستطیلی کنار قطعه یا شکل پیت ظاهر می‌شود. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### DescendingBlockList {#DescendingBlockList}
```
public static final int DescendingBlockList
```

از آن برای نمایش گروه‌های ایده‌های مرتبط یا فهرست‌های اطلاعات استفاده می‌شود. اشکال متنی به‌صورت ترتیبی ارتفاعشان کاهش می‌یابد و متن سطح ۱ به صورت عمودی نمایش داده می‌شود.

### DescendingProcess {#DescendingProcess}
```
public static final int DescendingProcess
```

از آن برای نمایش یک سری نزولی از رویدادها استفاده می‌شود. اولین متن سطح ۱ در بالای پیکان و آخرین متن سطح ۱ در پایین پیکان ظاهر می‌شود. فقط هفت آیتم سطح ۱ اول ظاهر می‌شوند. برای متن‌های کم تا متوسط بهترین است.

### DetailedProcess {#DetailedProcess}
```
public static final int DetailedProcess
```

با متن‌های زیاد سطح ۲ برای نمایش پیشرفت از طریق مراحل استفاده می‌شود.

### DivergingArrows {#DivergingArrows}
```
public static final int DivergingArrows
```

از آن برای نمایش ایده‌ها یا مفاهیمی که از منبع مرکزی به سمت بیرون پیشرفت می‌کنند استفاده می‌شود. برای صرفاً متن سطح ۱ مناسب‌ترین است.

### DivergingRadial {#DivergingRadial}
```
public static final int DivergingRadial
```

از آن برای نمایش روابط به یک ایدهٔ مرکزی در یک چرخه استفاده می‌شود. اولین خط متن سطح ۱ متناظر با شکل دایره‌ای مرکزی است. بر دایره‌های اطراف به‌جای ایدهٔ مرکزی تأکید دارد. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### Equation {#Equation}
```
public static final int Equation
```

از آن برای نمایش گام‌های متوالی یا وظایفی که برنامه یا نتیجه‌ای را نشان می‌دهند استفاده می‌شود. آخرین خط متن سطح ۱ پس از علامت برابر (=) ظاهر می‌شود. برای صرفاً متن سطح ۱ مناسب‌ترین است.

### FramedTextPicture {#FramedTextPicture}
```
public static final int FramedTextPicture
```

از آن برای نمایش تصاویر با متن سطح ۱ متناظر داخل یک فریم استفاده می‌شود.

### Funnel {#Funnel}
```
public static final int Funnel
```

از آن برای نمایش فیلتر کردن اطلاعات یا چگونگی ترکیب بخش‌ها به یک کل استفاده می‌شود. نتایج نهایی را برجسته می‌کند. می‌تواند تا چهار خط متن سطح ۱ داشته باشد؛ خط آخر این چهار خط زیر قیف ظاهر می‌شود و سایر خطوط به یک شکل دایره‌ای متناظر هستند. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### Gear {#Gear}
```
public static final int Gear
```

از آن برای نمایش ایده‌های درهم‌تنید استفاده می‌شود. هر یک از سه خط اول متن سطح ۱ متناظر با یک شکل دنده است و متن سطح ۲ متناظر در مستطیل‌های کناری دنده ظاهر می‌شود. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### GridMatrix {#GridMatrix}
```
public static final int GridMatrix
```

از آن برای نمایش مکان‌گذاری مفاهیم در طول دو محور استفاده می‌شود. بر اجزای فردی به‌جای کل تأکید دارد. چهار خط اول متن سطح ۱ در ربع‌ها ظاهر می‌شود. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### GroupedList {#GroupedList}
```
public static final int GroupedList
```

از آن برای نمایش گروه‌ها و زیرگروه‌های اطلاعات یا گام‌ها و زیرگام‌ها در یک کار، فرآیند یا جریان کاری استفاده می‌شود. متن سطح ۱ به اشکال افقی سطح بالا متناظر است و متن سطح ۲ به زیرگام‌های عمودی زیر هر شکل بالایی مرتبط متناظر می‌شود. برای تأکید بر زیرگروه‌ها یا زیرگام‌ها، اطلاعات سلسله‌مراتبی یا فهرست‌های متعدد اطلاعات مناسب است.

### HalfCircleOrganizationChart {#HalfCircleOrganizationChart}
```
public static final int HalfCircleOrganizationChart
```

از آن برای نمایش اطلاعات سلسله‌مراتبی یا روابط گزارش‌دهی در یک سازمان استفاده می‌شود. اشکال دستیار و طرح‌های hanging Org Chart با این چیدمان در دسترس هستند.

### HexagonCluster {#HexagonCluster}
```
public static final int HexagonCluster
```

از آن برای نمایش تصاویر با متن توصیفی مرتبط استفاده می‌شود. شش‌ضلعی‌های کوچک تصویر و جفت متن را نشان می‌دهند. برای متن کم‌حجم بهترین است.

### Hierarchy {#Hierarchy}
```
public static final int Hierarchy
```

از آن برای نمایش روابط سلسله‌مراتبی که از بالا به پایین پیش می‌روند استفاده می‌شود.

### HierarchyList {#HierarchyList}
```
public static final int HierarchyList
```

از آن برای نمایش روابط سلسله‌مراتبی که از گروه به گروه پیش می‌روند استفاده می‌شود. می‌تواند برای گروه‌بندی یا فهرست اطلاعات نیز به کار رود.

### HorizontalBulletList {#HorizontalBulletList}
```
public static final int HorizontalBulletList
```

از آن برای نمایش فهرست‌های غیرمتوالی یا گروه‌بندی شده اطلاعات استفاده می‌شود. برای متون بزرگ مناسب است. تمام متن‌ها دارای سطح یکسانی از تأکید هستند و جهت‌گیریی نشان داده نمی‌شود.

### HorizontalHierarchy {#HorizontalHierarchy}
```
public static final int HorizontalHierarchy
```

از آن برای نمایش روابط سلسله‌مراتبی که به صورت افقی پیش می‌روند استفاده می‌شود. برای درخت‌های تصمیم‌گیری مناسب است.

### HorizontalLabeledHierarchy {#HorizontalLabeledHierarchy}
```
public static final int HorizontalLabeledHierarchy
```

از آن برای نمایش روابط سلسله‌مراتبی که به صورت افقی پیش می‌روند و به‌صورت گروهی سلسله‌مراتبی هستند استفاده می‌شود. بر عنوان یا متن سطح ۱ تأکید دارد. اولین خط متن سطح ۱ در شکلی که در ابتدای سلسله‌مراتب قرار دارد ظاهر می‌شود و خط دوم و تمام خطوط بعدی در بالای مستطیل‌های بلند ظاهر می‌شوند.

### HorizontalMultiLevelHierarchy {#HorizontalMultiLevelHierarchy}
```
public static final int HorizontalMultiLevelHierarchy
```

از آن برای نمایش مقادیر بزرگ اطلاعات سلسله‌مراتبی که به صورت افقی پیش می‌روند استفاده می‌شود. بالای سلسله‌مراتب به صورت عمودی نمایش داده می‌شود. این چیدمان از سطوح متعدد در سلسله‌مراتب پشتیبانی می‌کند.

### HorizontalOrganizationChart {#HorizontalOrganizationChart}
```
public static final int HorizontalOrganizationChart
```

از آن برای نمایش اطلاعات سلسله‌مراتبی به صورت افقی یا روابط گزارش‌دهی در یک سازمان استفاده می‌شود. اشکال دستیار و طرح‌های hanging Org Chart با این چیدمان در دسترس هستند.

### HorizontalPictureList {#HorizontalPictureList}
```
public static final int HorizontalPictureList
```

از آن برای نمایش اطلاعات غیرمتوالی یا گروه‌بندی شده با تأکید بر تصاویر مرتبط استفاده می‌شود. اشکال بالایی برای شامل شدن تصاویر طراحی شده‌اند.

### IncreasingArrowsProcess {#IncreasingArrowsProcess}
```
public static final int IncreasingArrowsProcess
```

از آن برای نمایش گام‌های متوالی و همپوشان در یک فرآیند استفاده می‌شود. به پنج آیتم سطح ۱ محدود است. سطح ۲ می‌تواند متن‌های زیاد داشته باشد.

### IncreasingCircleProcess {#IncreasingCircleProcess}
```
public static final int IncreasingCircleProcess
```

از آن برای نمایش یک سری گام‌ها که درون دایره با هر گام بزرگ‌تر می‌شود استفاده می‌شود. به هفت گام سطح ۱ محدود است ولی آیتم‌های سطح ۲ نامحدود. برای متن‌های زیاد سطح ۲ مناسب است.

### InvertedPyramid {#InvertedPyramid}
```
public static final int InvertedPyramid
```

از آن برای نمایش روابط نسبی، مرتبط یا سلسله‌مراتبی با بزرگ‌ترین جزء در بالا و باریک شدن به سمت پایین استفاده می‌شود. متن سطح ۱ در بخش‌های هرم ظاهر می‌شود و متن سطح ۲ در اشکال کنار هر بخش.

### LabeledHierarchy {#LabeledHierarchy}
```
public static final int LabeledHierarchy
```

از آن برای نمایش روابط سلسله‌مراتبی که از بالا به پایین پیش می‌روند و به‌صورت گروهی سلسله‌مراتبی هستند استفاده می‌شود. بر عنوان یا متن سطح ۱ تأکید دارد. اولین خط متن سطح ۱ در شکلی که در ابتدای سلسله‌مراتب قرار دارد ظاهر می‌شود و تمام خطوط بعدی در سمت چپ مستطیل‌های بلند ظاهر می‌شوند.

### LinearVenn {#LinearVenn}
```
public static final int LinearVenn
```

از آن برای نمایش روابط همپوشان در یک توالی استفاده می‌شود. برای صرفاً متن سطح ۱ مناسب‌ترین است.

### LinedList {#LinedList}
```
public static final int LinedList
```

از آن برای نمایش مقادیر زیاد متن تقسیم‌شده به دسته‌ها و زیردسته‌ها استفاده می‌شود. برای سطوح متعدد متن مناسب است. متن‌های همسطح با خطوط جدا شده‌اند.

### MultidirectionalCycle {#MultidirectionalCycle}
```
public static final int MultidirectionalCycle
```

از آن برای نمایش یک توالی مداوم از مراحل، وظایف یا رویدادهایی که می‌توانند در هر جهت رخ دهند استفاده می‌شود.

### NameandTitleOrganizationChart {#NameandTitleOrganizationChart}
```
public static final int NameandTitleOrganizationChart
```

از آن برای نمایش اطلاعات سلسله‌مراتبی یا روابط گزارش‌دهی در یک سازمان استفاده می‌شود. برای وارد کردن متن در جعبه عنوان، مستقیماً در شکل مستطیلی کوچکتر تایپ کنید. اشکال دستیار و طرح‌های hanging Org Chart با این چیدمان در دسترس هستند.

### NestedTarget {#NestedTarget}
```
public static final int NestedTarget
```

از آن برای نمایش روابط حاوی‌سازی استفاده می‌شود. هر یک از سه خط اول متن سطح ۱ به متن بالا-چپ در اشکال متناظر است و متن سطح ۲ به اشکال کوچک‌تر مربوط می‌شود. برای حداقل خطوط متن سطح ۲ بهترین است. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### NondirectionalCycle {#NondirectionalCycle}
```
public static final int NondirectionalCycle
```

از آن برای نمایش توالی مداوم از مراحل، وظایف یا رویدادها در یک جریان چرخه‌ای استفاده می‌شود. هر شکل همان سطح اهمیت را دارد. برای زمانی که نیازی به نشان دادن جهت نیست مناسب است.

### OpposingArrows {#OpposingArrows}
```
public static final int OpposingArrows
```

از آن برای نمایش دو ایدهٔ مخالف یا ایده‌هایی که از نقطهٔ مرکزی متفاوت می‌شوند استفاده می‌شود. هر یک از دو خط اول متن سطح ۱ به یک پیکان متناظر است. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### OpposingIdeas {#OpposingIdeas}
```
public static final int OpposingIdeas
```

از آن برای نمایش دو ایدهٔ مخالف یا متضاد استفاده می‌شود. می‌تواند یک یا دو آیتم سطح ۱ داشته باشد. هر متن سطح ۱ می‌تواند زیرسطوح متعددی داشته باشد. برای متون زیاد مناسب است.

### OrganizationChart {#OrganizationChart}
```
public static final int OrganizationChart
```

از آن برای نمایش اطلاعات سلسله‌مراتبی یا روابط گزارش‌دهی در یک سازمان استفاده می‌شود. اشکال دستیار و طرح‌های hanging Org Chart با این چیدمان در دسترس هستند.

### PhasedProcess {#PhasedProcess}
```
public static final int PhasedProcess
```

از آن برای نمایش سه فاز یک فرآیند استفاده می‌شود. به سه آیتم سطح ۱ محدود است. دو آیتم سطح ۱ اول می‌توانند هر کدام چهار آیتم سطح ۲ داشته باشند و آیتم سوم می‌تواند تعداد نامحدودی آیتم سطح ۲ داشته باشد. برای متن کم مناسب است.

### PictureAccentBlocks {#PictureAccentBlocks}
```
public static final int PictureAccentBlocks
```

از آن برای نمایش یک گروه از تصاویر در بلوک‌ها که از گوشه شروع می‌شوند استفاده می‌شود. متن متناظر به صورت عمودی نمایش داده می‌شود. برای افزودن تأکید در اسلایدهای عنوان یا زیرعنوان یا برای تقسیم بخش‌های سند مناسب است.

### PictureAccentList {#PictureAccentList}
```
public static final int PictureAccentList
```

از آن برای نمایش اطلاعات گروه‌بندی‌شده یا مرتبط استفاده می‌شود. اشکال کوچک در گوشه‌های بالا برای شامل کردن تصاویر طراحی شده‌اند. بر متن سطح ۲ نسبت به سطح ۱ تأکید دارد و برای متن‌های زیاد سطح ۲ گزینهٔ خوبی است.

### PictureAccentProcess {#PictureAccentProcess}
```
public static final int PictureAccentProcess
```

از آن برای نمایش گام‌های متوالی در یک کار، فرآیند یا جریان کاری استفاده می‌شود. اشکال مستطیلی پس‌زمینه برای شامل کردن تصاویر طراحی شده‌اند.

### PictureCaptionList {#PictureCaptionList}
```
public static final int PictureCaptionList
```

از آن برای نمایش بلوک‌های اطلاعاتی غیرمتوالی یا گروه‌بندی شده استفاده می‌شود. اشکال بالا برای شامل کردن تصاویر طراحی شده‌اند و تصاویر نسبت به متن برجسته می‌شوند. برای تصاویر با عنوان‌های کوتاه مناسب است.

### PictureGrid {#PictureGrid}
```
public static final int PictureGrid
```

از آن برای نمایش تصاویر چینش‌یافته روی یک شبکهٔ مربعی استفاده می‌شود. برای متن سطح ۱ کم‌حجم که بالای تصویر ظاهر می‌شود بهترین است.

### PictureLineup {#PictureLineup}
```
public static final int PictureLineup
```

از آن برای نمایش یک سری تصاویر در کنار هم استفاده می‌شود. متن سطح ۱ بالای تصویر را می‌پوشاند. متن سطح ۲ زیر تصویر ظاهر می‌شود.

### PictureStrips {#PictureStrips}
```
public static final int PictureStrips
```

از آن برای نمایش یک سری از تصاویر از بالا به پایین با متن سطح ۱ در کنار هر کدام استفاده می‌شود.

### PieProcess {#PieProcess}
```
public static final int PieProcess
```

از آن برای نمایش گام‌های یک فرآیند که هر برش پیت با افزایش اندازه تا هفت شکل رشد می‌کند استفاده می‌شود. متن سطح ۱ به صورت عمودی نمایش داده می‌شود.

### PlusandMinus {#PlusandMinus}
```
public static final int PlusandMinus
```

از آن برای نمایش مزایا و معایب دو ایده استفاده می‌شود. هر متن سطح ۱ می‌تواند زیرسطوح متعددی داشته باشد. برای متون زیاد مناسب است. به دو آیتم سطح ۱ محدود است.

### ProcessArrows {#ProcessArrows}
```
public static final int ProcessArrows
```

از آن برای نمایش اطلاعاتی که یک فرآیند یا جریان کاری را نشان می‌دهد استفاده می‌شود. متن سطح ۱ در اشکال دایره‌ای و متن سطح ۲ در اشکال پیکان ظاهر می‌شود. برای متن کم و تأکید بر حرکت یا جهت بهترین است.

### ProcessList {#ProcessList}
```
public static final int ProcessList
```

از آن برای نمایش گروه‌های متعدد اطلاعات یا گام‌ها و زیرگام‌ها در یک کار، فرآیند یا جریان کاری استفاده می‌شود. متن سطح ۱ به اشکال افقی بالایی متناظر است و متن سطح ۲ به زیرگام‌های عمودی زیر هر شکل بالایی مرتبط متناظر می‌شود.

### PyramidList {#PyramidList}
```
public static final int PyramidList
```

از آن برای نمایش روابط نسبی، مرتبط یا سلسله‌مراتبی استفاده می‌شود. متن در اشکال مستطیلی بالای پس‌زمینهٔ هرم ظاهر می‌شود.

### RadialCluster {#RadialCluster}
```
public static final int RadialCluster
```

از آن برای نمایش داده‌هایی که به یک ایدهٔ مرکزی یا تم مرتبط هستند استفاده می‌شود. متن سطح ۱ بالا در مرکز ظاهر می‌شود. متن سطح ۲ در اشکال اطراف ظاهر می‌شود. می‌تواند تا هفت شکل سطح ۲ داشته باشد. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند. برای متن کم بهترین است.

### RadialCycle {#RadialCycle}
```
public static final int RadialCycle
```

از آن برای نمایش رابطه به یک ایدهٔ مرکزی استفاده می‌شود. هم اطلاعات در دایرهٔ مرکزی و هم چگونگی مشارکت اطلاعات در حلقهٔ بیرونی دایره‌ها به ایدهٔ مرکزی تأکید دارد. اولین خط متن سطح ۱ متناظر با دایرهٔ مرکزی است و متن سطح ۲ آن به حلقهٔ بیرونی دایره‌ها متناظر است. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### RadialList {#RadialList}
```
public static final int RadialList
```

از آن برای نمایش روابط به یک ایدهٔ مرکزی در یک چرخه استفاده می‌شود. شکل مرکزی می‌تواند تصویر داشته باشد. متن سطح ۱ در دایره‌های کوچک‌تر ظاهر می‌شود و هر متن مرتبط سطح ۲ در کنار دایره‌های کوچک ظاهر می‌شود.

### RadialVenn {#RadialVenn}
```
public static final int RadialVenn
```

از آن برای نمایش هر دو رابطه همپوشان و رابطه به یک ایدهٔ مرکزی در یک چرخه استفاده می‌شود. اولین خط متن سطح ۱ متناظر با شکل مرکزی است و خطوط متن سطح ۲ متناظر با اشکال دایره‌ای اطراف هستند. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### RandomToResultProcess {#RandomToResultProcess}
```
public static final int RandomToResultProcess
```

از آن برای نشان دادن این‌که از طریق یک سری گام‌ها، چگونه ایده‌های آشوب‌مانند می‌توانند به یک هدف یا ایدهٔ واحد منجر شوند استفاده می‌شود. از چندین متن سطح ۱ پشتیبانی می‌کند، اما اولین و آخرین اشکال متن سطح ۱ ثابت هستند. برای متن کم سطح ۱ و متن متوسط سطح ۲ بهترین است.

### RepeatingBendingProcess {#RepeatingBendingProcess}
```
public static final int RepeatingBendingProcess
```

از آن برای نمایش پیشرفت یا گام‌های متوالی در یک کار، فرآیند یا جریان کاری استفاده می‌شود. فضای افقی و عمودی نمایش اشکال را به حداکثر می‌رساند.

### ReverseList {#ReverseList}
```
public static final int ReverseList
```

از آن برای جابه‌جایی بین دو آیتم استفاده می‌شود. تنها دو آیتم اول متن نمایش داده می‌شوند و هر آیتم می‌تواند متن زیادی داشته باشد. برای نشان دادن تغییر بین دو آیتم یا جابه‌جایی در ترتیب مناسب است.

### SegmentedCycle {#SegmentedCycle}
```
public static final int SegmentedCycle
```

از آن برای نمایش پیشرفت یا توالی از مراحل، وظایف یا رویدادها در یک جریان چرخه‌ای استفاده می‌شود. بر قطعات متقابل تأکید دارد. هر یک از هفت خط اول متن سطح ۱ به یک قطعه یا شکل پیت متناظر است. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### SegmentedProcess {#SegmentedProcess}
```
public static final int SegmentedProcess
```

از آن برای نمایش پیشرفت یا گام‌های متوالی در یک کار، فرآیند یا جریان کاری استفاده می‌شود. بر متن سطح ۲ تأکید دارد، چرا که هر خط در یک شکل جداگانه ظاهر می‌شود.

### SegmentedPyramid {#SegmentedPyramid}
```
public static final int SegmentedPyramid
```

از آن برای نمایش روابط حاوی‌سازی، نسبی یا مرتبط استفاده می‌شود. نه خط اول متن سطح ۱ در اشکال مثلثی ظاهر می‌شود. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند. فقط برای متن سطح ۱ مناسب است.

### SnapshotPictureList {#SnapshotPictureList}
```
public static final int SnapshotPictureList
```

از آن برای نمایش تصاویر با متن توضیحی استفاده می‌شود. متن سطح ۲ می‌تواند فهرست‌های اطلاعاتی نمایش دهد. برای مقدار زیاد متن مناسب است.

### SpiralPicture {#SpiralPicture}
```
public static final int SpiralPicture
```

از آن برای نمایش حداکثر پنج تصویر با عنوان‌های سطح ۱ متناظر که به مرکز می‌پیچند استفاده می‌شود.

### SquareAccentList {#SquareAccentList}
```
public static final int SquareAccentList
```

از آن برای نمایش فهرست‌های اطلاعاتی تقسیم‌شده به دسته‌ها استفاده می‌شود. متن سطح ۲ در کنار یک شکل مربع کوچک ظاهر می‌شود. برای متن‌های زیاد سطح ۲ مناسب است.

### StackedList {#StackedList}
```
public static final int StackedList
```

از آن برای نمایش گروه‌های اطلاعات یا گام‌ها در یک کار، فرآیند یا جریان کاری استفاده می‌شود. اشکال دایره‌ای شامل متن سطح ۱ هستند و مستطیل‌های متناظر شامل متن سطح ۲ هستند. برای جزئیات فراوان و متن سطح ۱ کم مناسب است.

### StackedVenn {#StackedVenn}
```
public static final int StackedVenn
```

از آن برای نمایش روابط همپوشان استفاده می‌شود. گزینهٔ مناسبی برای تأکید بر رشد یا درجات است. برای صرفاً متن سطح ۱ بهترین است. هفت خط اول متن سطح ۱ به یک شکل دایره‌ای متناظر است. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### StaggeredProcess {#StaggeredProcess}
```
public static final int StaggeredProcess
```

از آن برای نمایش پیشرفت نزولی از طریق مراحل استفاده می‌شود. هر یک از پنج خط اول متن سطح ۱ به یک مستطیل متناظر است. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### StepDownProcess {#StepDownProcess}
```
public static final int StepDownProcess
```

از آن برای نمایش یک فرآیند نزولی با گام‌ها و زیرگام‌های متعدد استفاده می‌شود. برای متن کم بهترین است.

### StepUpProcess {#StepUpProcess}
```
public static final int StepUpProcess
```

از آن برای نمایش یک سری صعودی از گام‌ها یا فهرست‌های اطلاعات استفاده می‌شود.

### SubStepProcess {#SubStepProcess}
```
public static final int SubStepProcess
```

از آن برای نمایش یک فرآیند چندگامی با زیرگام‌ها بین هر متن سطح ۱ استفاده می‌شود. برای متن کم بهترین است و به هفت گام سطح ۱ محدود است. هر گام سطح ۱ می‌تواند زیرگام‌های نامحدودی داشته باشد.

### TableHierarchy {#TableHierarchy}
```
public static final int TableHierarchy
```

از آن برای نمایش گروه‌های اطلاعاتی ساخته‌شده از بالا به پایین و سلسله‌مراتب‌های درون هر گروه استفاده می‌شود. این چیدمان خطوط اتصال ندارد.

### TableList {#TableList}
```
public static final int TableList
```

از آن برای نمایش اطلاعات گروه‌بندی‌شده یا مرتبط با ارزش برابر استفاده می‌شود. اولین خط متن سطح ۱ به شکل بالایی متناظر است و متن سطح ۲ برای فهرست‌های بعدی استفاده می‌شود.

### TargetList {#TargetList}
```
public static final int TableList
```

از آن برای نمایش اطلاعات مرتبط یا همپوشان استفاده می‌شود. هر یک از هفت خط اول متن سطح ۱ در شکل مستطیلی ظاهر می‌شود. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند. برای هر دو متن سطح ۱ و سطح ۲ مناسب است.

### TextCycle {#TextCycle}
```
public static final int TextCycle
```

از آن برای نمایش یک توالی مداوم از مراحل، وظایف یا رویدادها در یک جریان چرخه‌ای استفاده می‌شود. بر پیکان‌ها یا جریان به‌جای مراحل یا گام‌ها تأکید دارد. برای صرفاً متن سطح ۱ بهترین است.

### TitlePictureLineup {#TitlePictureLineup}
```
public static final int TitlePictureLineup
```

از آن برای نمایش یک سری تصاویر که هر کدام عنوان و توضیح خود را دارند استفاده می‌شود. متن سطح ۱ در جعبه بالای تصویر ظاهر می‌شود. متن سطح ۲ زیر تصویر ظاهر می‌شود.

### TitledMatrix {#TitledMatrix}
```
public static final int TitledMatrix
```

از آن برای نمایش روابط چهار ربع به یک کل استفاده می‌شود. اولین خط متن سطح ۱ به شکل مرکزی متناظر است و چهار خط اول متن سطح ۲ در ربع‌ها ظاهر می‌شوند. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### TitledPictureAccentList {#TitledPictureAccentList}
```
public static final int TitledPictureAccentList
```

از آن برای نمایش فهرست‌های اطلاعاتی با تصویر برجسته برای هر متن سطح ۲ استفاده می‌شود. متن سطح ۱ در جعبه‌ای جداگانه در بالای فهرست نمایش داده می‌شود.

### TitledPictureBlocks {#TitledPictureBlocks}
```
public static final int TitledPictureBlocks
```

از آن برای نمایش یک سری تصاویر استفاده می‌شود. متن سطح ۱ بالای هر تصویر ظاهر می‌شود. متن سطح ۲ در کنار تصویر و کمی همپوشانی دارد.

### TrapezoidList {#TrapezoidList}
```
public static final int TrapezoidList
```

از آن برای نمایش اطلاعات گروه‌بندی‌شده یا مرتبط با ارزش برابر استفاده می‌شود. برای متون زیاد مناسب است.

### UpwardArrow {#UpwardArrow}
```
public static final int UpwardArrow
```

از آن برای نمایش پیشرفت یا گام‌هایی که به سمت بالا تمایل دارند در یک کار، فرآیند یا جریان کاری استفاده می‌شود. هر یک از پنج خط اول متن سطح ۱ به یک نقطه روی پیکان متناظر است. برای متن کمینه بهترین است. متن‌های استفاده‌نشده ظاهر نمی‌شوند ولی در صورت تغییر چیدمان در دسترس هستند.

### VerticalAccentList {#VerticalAccentList}
```
public static final int VerticalAccentList
```

از آن برای نمایش فهرست‌های اطلاعاتی استفاده می‌شود. متن سطح ۲ در اشکال مستطیلی بر روی شِیارهای عمودی ظاهر می‌شود. بر متن سطح ۲ نسبت به سطح ۱ تأکید دارد و برای متن‌های متوسط سطح ۲ مناسب است.

### VerticalArrowList {#VerticalArrowList}
```
public static final int VerticalArrowList
```

از آن برای نمایش پیشرفت یا گام‌های متوالی در یک کار، فرآیند یا جریان کاری که به سمت هدف مشترک حرکت می‌کند استفاده می‌شود. برای فهرست‌های بولت‌دار اطلاعات مناسب است.

### VerticalBendingProcess {#VerticalBendingProcess}
```
public static final int VerticalBendingProcess
```

از آن برای نمایش پیشرفت یا گام‌های متوالی در یک کار، فرآیند یا جریان کاری استفاده می‌شود. فضای افقی و عمودی نمایش اشکال را به حداکثر می‌رساند. بر روابط متقابل میان اشکال بیشتر از جهت یا حرکت تأکید دارد.

### VerticalBlockList {#VerticalBlockList}
```
public static final int VerticalBlockList
```

از آن برای نمایش گروه‌های اطلاعات یا گام‌ها در یک کار، فرآیند یا جریان کاری استفاده می‌شود. برای متن زیاد سطح ۲ مناسب است. گزینهٔ خوبی برای متنی با نکتهٔ اصلی و زیرنکات متعدد است.

### VerticalBoxList {#VerticalBoxList}
```
public static final int VerticalBoxList
```

از آن برای نمایش چندین گروه اطلاعاتی، به‌ویژه گروه‌های با متن زیاد سطح ۲ استفاده می‌شود. گزینهٔ خوبی برای فهرست‌های بولت‌دار اطلاعات است.

### VerticalBulletList {#VerticalBulletList}
```
public static final int VerticalBulletList
```

از آن برای نمایش بلوک‌های اطلاعاتی غیرمتوالی یا گروه‌بندی شده استفاده می‌شود. برای فهرست‌های با عنوان‌های طولانی یا اطلاعات سطح بالا مناسب است.

### VerticalChevronList {#VerticalChevronList}
```
public static final int VerticalChevronList
```

از آن برای نمایش پیشرفت یا گام‌های متوالی در یک کار، فرآیند یا جریان کاری یا برای تأکید بر حرکت یا جهت استفاده می‌شود. بر متن سطح ۲ نسبت به سطح ۱ تأکید دارد و برای متن زیاد سطح ۲ مناسب است.

### VerticalCircleList {#VerticalCircleList}
```
public static final int VerticalCircleList
```

از آن برای نمایش داده‌های متوالی یا گروه‌بندی شده استفاده می‌شود. بهترین برای متن سطح ۱ است که در کنار یک شکل دایره‌ای بزرگ ظاهر می‌شود. سطوح پایین‌تر متن با اشکال دایره‌ای کوچکتر جدا می‌شوند.

### VerticalCurvedList {#VerticalCurvedList}
```
public static final int VerticalCurvedList
```

از آن برای نمایش فهرست منحنی اطلاعات استفاده می‌شود. برای افزودن تصاویر به اشکال دایره‌ای برجسته، پرکردن تصویر را اعمال کنید.

### VerticalEquation {#VerticalEquation}
```
public static final int VerticalEquation
```

از آن برای نمایش گام‌های متوالی یا وظایفی که برنامه یا نتیجه‌ای را نشان می‌دهند استفاده می‌شود. آخرین خط متن سطح ۱ پس از پیکان ظاهر می‌شود. برای صرفاً متن سطح ۱ بهترین است.

### VerticalPictureAccentList {#VerticalPictureAccentList}
```
public static final int VerticalPictureAccentList
```

از آن برای نمایش بلوک‌های اطلاعاتی غیرمتوالی یا گروه‌بندی شده استفاده می‌شود. دایره‌های کوچک برای شامل کردن تصاویر طراحی شده‌اند.

### VerticalPictureList {#VerticalPictureList}
```
public static final int VerticalPictureList
```

از آن برای نمایش بلوک‌های اطلاعاتی غیرمتوالی یا گروه‌بندی شده استفاده می‌شود. اشکال کوچک در سمت چپ برای شامل کردن تصاویر طراحی شده‌اند.

### VerticalProcess {#VerticalProcess}
```
public static final int VerticalProcess
```

از آن برای نمایش پیشرفت یا گام‌های متوالی در یک کار، فرآیند یا جریان کاری از بالا به پایین استفاده می‌شود. برای متن سطح ۱ بهترین است چون فضای عمودی محدود است.

### Custom {#Custom}
```
public static final int Custom
```

نمایانگر یک نمودار SmartArt با قالب چیدمان سفارشی است

### PictureOrganizationChart {#PictureOrganizationChart}
```
public static final int PictureOrganizationChart
```

از آن برای نمایش اطلاعات سلسله‌مراتبی یا روابط گزارش‌دهی در یک سازمان، با تصاویر متناظر استفاده می‌شود. اشکال دستیار و طرح‌های hanging Org Chart با این چیدمان در دسترس هستند.