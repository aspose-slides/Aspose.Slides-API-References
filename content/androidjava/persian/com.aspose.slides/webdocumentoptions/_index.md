---
title: WebDocumentOptions
second_title: Aspose.Slides برای اندروید از طریق مرجع API جاوا
description: یک مجموعه گزینه‌ها برای ذخیره‌سازی را نمایان می‌کند.
type: docs
url: /fa/com.aspose.slides/webdocumentoptions/
---
**Inheritance:**
java.lang.Object
```
public class WebDocumentOptions
```

نماد مجموعه‌ای از گزینه‌ها برای ذخیره‌سازی [WebDocument](../../com.aspose.slides/webdocument) است.
## سازنده‌ها

| سازنده | توضیح |
| --- | --- |
| [WebDocumentOptions()](#WebDocumentOptions--) |  |
## متدها

| متد | توضیح |
| --- | --- |
| [getTemplateEngine()](#getTemplateEngine--) | موتور قالب‌ها را باز می‌گرداند یا تنظیم می‌کند. |
| [setTemplateEngine(ITemplateEngine value)](#setTemplateEngine-com.aspose.slides.ITemplateEngine-) | موتور قالب‌ها را باز می‌گرداند یا تنظیم می‌کند. |
| [getOutputSaver()](#getOutputSaver--) | ذخیره‌کننده خروجی را باز می‌گرداند یا تنظیم می‌کند. |
| [setOutputSaver(IOutputSaver value)](#setOutputSaver-com.aspose.slides.IOutputSaver-) | ذخیره‌کننده خروجی را باز می‌گرداند یا تنظیم می کند. |
| [getEmbedImages()](#getEmbedImages--) | گزینه‌ی جاسازی تصاویر را باز می‌گرداند یا تنظیم می‌کند. |
| [setEmbedImages(boolean value)](#setEmbedImages-boolean-) | گزینه‌ی جاسازی تصاویر را باز می‌گرداند یا تنظیم می‌کند. |
| [getAnimateTransitions()](#getAnimateTransitions--) | گزینه‌ی انیمیشن انتقال‌ها را باز می‌گرداند یا تنظیم می‌کند. |
| [setAnimateTransitions(boolean value)](#setAnimateTransitions-boolean-) | گزینه‌ی انیمیشن انتقال‌ها را باز می‌گرداند یا تنظیم می‌کند. |
| [getAnimateShapes()](#getAnimateShapes--) | گزینه‌ی انیمیشن اشکال را باز می‌گرداند یا تنظیم می‌کند. |
| [setAnimateShapes(boolean value)](#setAnimateShapes-boolean-) | گزینه‌ی انیمیشن اشکال را باز می‌گرداند یا تنظیم می‌کند. |
### WebDocumentOptions() {#WebDocumentOptions--}
```
public WebDocumentOptions()
```


### getTemplateEngine() {#getTemplateEngine--}
```
public final ITemplateEngine getTemplateEngine()
```

موتور قالب‌ها را باز می‌گرداند یا تنظیم می‌کند. خواندنی/نوشتنی [ITemplateEngine](../../com.aspose.slides/itemplateengine).

**بازگشت:**
[ITemplateEngine](../../com.aspose.slides/itemplateengine)
### setTemplateEngine(ITemplateEngine value) {#setTemplateEngine-com.aspose.slides.ITemplateEngine-}
```
public final void setTemplateEngine(ITemplateEngine value)
```

موتور قالب‌ها را باز می‌گرداند یا تنظیم می‌کند. خواندنی/نوشتنی [ITemplateEngine](../../com.aspose.slides/itemplateengine).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | [ITemplateEngine](../../com.aspose.slides/itemplateengine) |  |

### getOutputSaver() {#getOutputSaver--}
```
public final IOutputSaver getOutputSaver()
```

ذخیره‌کننده خروجی را باز می‌گرداند یا تنظیم می‌کند. خواندنی/نوشتنی [IOutputSaver](../../com.aspose.slides/ioutputsaver).

**بازگشت:**
[IOutputSaver](../../com.aspose.slides/ioutputsaver)
### setOutputSaver(IOutputSaver value) {#setOutputSaver-com.aspose.slides.IOutputSaver-}
```
public final void setOutputSaver(IOutputSaver value)
```

ذخیره‌کننده خروجی را باز می‌گرداند یا تنظیم می‌کند. خواندنی/نوشتنی [IOutputSaver](../../com.aspose.slides/ioutputsaver).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | [IOutputSaver](../../com.aspose.slides/ioutputsaver) |  |

### getEmbedImages() {#getEmbedImages--}
```
public final boolean getEmbedImages()
```

گزینه‌ی جاسازی تصاویر را باز می‌گرداند یا تنظیم می‌کند. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setEmbedImages(boolean value) {#setEmbedImages-boolean-}
```
public final void setEmbedImages(boolean value)
```

گزینه‌ی جاسازی تصاویر را باز می‌گرداند یا تنظیم می‌کند. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getAnimateTransitions() {#getAnimateTransitions--}
```
public final boolean getAnimateTransitions()
```

گزینه‌ی انیمیشن انتقال‌ها را باز می‌گرداند یا تنظیم می‌کند. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setAnimateTransitions(boolean value) {#setAnimateTransitions-boolean-}
```
public final void setAnimateTransitions(boolean value)
```

گزینه‌ی انیمیشن انتقال‌ها را باز می‌گرداند یا تنظیم می‌کند. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getAnimateShapes() {#getAnimateShapes--}
```
public final boolean getAnimateShapes()
```

گزینه‌ی انیمیشن اشکال را باز می‌گرداند یا تنظیم می‌کند. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setAnimateShapes(boolean value) {#setAnimateShapes-boolean-}
```
public final void setAnimateShapes(boolean value)
```

گزینه‌ی انیمیشن اشکال را باز می‌گرداند یا تنظیم می‌کند. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |