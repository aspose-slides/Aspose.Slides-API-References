---
title: IMasterNotesSlideManager
second_title: Aspose.Slides for Android via Java API Reference
description: مدیر اسلاید یادداشت‌های اصلی.
type: docs
url: /fa/com.aspose.slides/imasternotesslidemanager/
---```
public interface IMasterNotesSlideManager
```

مدیر اسلاید یادداشت‌های اصلی.
## متدها

| متد | توضیح |
| --- | --- |
| [getMasterNotesSlide()](#getMasterNotesSlide--) | اگر یک مستر وجود داشته باشد، مستری برای تمام اسلایدهای یادداشت‌های این ارائه برمی‌گرداند، در غیر این صورت مقدار null را برمی‌گرداند. |
| [setDefaultMasterNotesSlide()](#setDefaultMasterNotesSlide--) | مستر پیش‌فرض اسلاید یادداشت برای اسلاید یادداشت مرتبط را تنظیم می‌کند. |
| [removeMasterNotesSlide()](#removeMasterNotesSlide--) | مستر اسلاید یادداشت را حذف می‌کند. |
### getMasterNotesSlide() {#getMasterNotesSlide--}
```
public abstract IMasterNotesSlide getMasterNotesSlide()
```

اگر یک مستر وجود داشته باشد، مستری برای تمام اسلایدهای یادداشت‌های این ارائه برمی‌گرداند، در غیر این صورت مقدار null را برمی‌گرداند. فقط‌خواندنی [IMasterNotesSlide](../../com.aspose.slides/imasternotesslide).

**بازگشت:**
[IMasterNotesSlide](../../com.aspose.slides/imasternotesslide)
### setDefaultMasterNotesSlide() {#setDefaultMasterNotesSlide--}
```
public abstract IMasterNotesSlide setDefaultMasterNotesSlide()
```

مستری پیش‌فرض برای اسلاید یادداشت‌های مرتبط تنظیم می‌کند.

**بازگشت:**
[IMasterNotesSlide](../../com.aspose.slides/imasternotesslide) - مستر پیش‌فرض اسلاید یادداشت [IMasterNotesSlide](../../com.aspose.slides/imasternotesslide)
### removeMasterNotesSlide() {#removeMasterNotesSlide--}
```
public abstract void removeMasterNotesSlide()
```

مستری اسلاید یادداشت را حذف می‌کند.