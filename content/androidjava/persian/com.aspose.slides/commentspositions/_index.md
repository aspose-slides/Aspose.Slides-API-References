---
title: CommentsPositions
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: قاعده‌ای را که نظرات را در سند صادر شده رندر می‌کند نشان می‌دهد
type: docs
url: /fa/com.aspose.slides/commentspositions/
---
**ارث‌برداری:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class CommentsPositions extends System.Enum
```

نمایانگر قاعده رندر نظرات در سند صادر شده است.
## فیلدها

| فیلد | توضیح |
| --- | --- |
| [None](#None) | مشخص می‌کند که نظرات باید به‌طور کلی نمایش داده نشوند. |
| [Bottom](#Bottom) | مشخص می‌کند که نظرات باید در پایین صفحه نمایش داده شوند. |
| [Right](#Right) | مشخص می‌کند که نظرات باید در سمت راست صفحه نمایش داده شوند. |
### None {#None}
```
public static final int None
```

مشخص می‌کند که نظرات باید به‌طور کلی نمایش داده نشوند.

### Bottom {#Bottom}
```
public static final int Bottom
```

مشخص می‌کند که نظرات باید در پایین صفحه نمایش داده شوند.

### Right {#Right}
```
public static final int Right
```

مشخص می‌کند که نظرات باید در سمت راست صفحه نمایش داده شوند.