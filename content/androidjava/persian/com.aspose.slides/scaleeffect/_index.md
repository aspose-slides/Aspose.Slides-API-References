---
title: ScaleEffect
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: نمایانگر اثر مقیاس‌گذاری انیمیشن.
type: docs
url: /fa/com.aspose.slides/scaleeffect/
---
**ارث‌بری:**  
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**تمام رابط‌های پیاده‌سازی‌شده:**  
[com.aspose.slides.IScaleEffect](../../com.aspose.slides/iscaleeffect)  
```
public class ScaleEffect extends Behavior implements IScaleEffect
```

نمایانگر اثر مقیاس‌گذاری انیمیشن است.

## سازندگان

| سازنده | توضیح |
| --- | --- |
| [ScaleEffect()](#ScaleEffect--) |  |

## متدها

| متد | توضیح |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | تعیین می‌کند آیا محتوا باید بزرگنمایی شود. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | تعیین می‌کند آیا محتوا باید بزرگنمایی شود. |
| [getFrom()](#getFrom--) | مختصات x/y را برای شروع انیمیشن تعیین می‌کند (به درصد). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | مختصات x/y را برای شروع انیمیشن تعیین می‌کند (به درصد). |
| [getTo()](#getTo--) | موقعیت هدف برای اثر مقیاس‌گذاری انیمیشن را تعیین می‌کند (به درصد). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | موقعیت هدف برای اثر مقیاس‌گذاری انیمیشن را تعیین می‌کند (به درصد). |
| [getBy()](#getBy--) | مقدار جابجایی نسبی برای انیمیشن را توصیف می‌کند (به درصد). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | مقدار جابجایی نسبی برای انیمیشن را توصیف می‌کند (به درصد). |

### ScaleEffect() {#ScaleEffect--}
```
public ScaleEffect()
```

### getZoomContent() {#getZoomContent--}
```
public final byte getZoomContent()
```

تعیین می‌کند آیا محتوا باید بزرگنمایی شود. قابل‌خواندن/قابل‌نوشتن [NullableBool](../../com.aspose.slides/nullablebool).

**بازگشت:**  
byte

### setZoomContent(byte value) {#setZoomContent-byte-}
```
public final void setZoomContent(byte value)
```

تعیین می‌کند آیا محتوا باید بزرگنمایی شود. قابل‌خواندن/قابل‌نوشتن [NullableBool](../../com.aspose.slides/nullablebool).

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public final PointF getFrom()
```

مختصات x/y را برای شروع انیمیشن تعیین می‌کند (به درصد). قابل‌خواندن/قابل‌نوشتن android.graphics.PointF.

**بازگشت:**  
android.graphics.PointF

### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public final void setFrom(PointF value)
```

مختصات x/y را برای شروع انیمیشن تعیین می‌کند (به درصد). قابل‌خواندن/قابل‌نوشتن android.graphics.PointF.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public final PointF getTo()
```

موقعیت هدف برای اثر مقیاس‌گذاری انیمیشن را تعیین می‌کند (به درصد). قابل‌خواندن/قابل‌نوشتن android.graphics.PointF.

**بازگشت:**  
android.graphics.PointF

### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public final void setTo(PointF value)
```

موقعیت هدف برای اثر مقیاس‌گذاری انیمیشن را تعیین می‌کند (به درصد). قابل‌خواندن/قابل‌نوشتن android.graphics.PointF.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public final PointF getBy()
```

مقدار جابجایی نسبی برای انیمیشن را توصیف می‌کند (به درصد). قابل‌خواندن/قابل‌نوشتن android.graphics.PointF.

**بازگشت:**  
android.graphics.PointF

### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public final void setBy(PointF value)
```

مقدار جابجایی نسبی برای انیمیشن را توصیف می‌کند (به درصد). قابل‌خواندن/قابل‌نوشتن android.graphics.PointF.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | android.graphics.PointF |  |