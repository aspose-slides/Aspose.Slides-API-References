---
title: CommentAuthor
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: نمایانگر نویسندهٔ نظرات است.
type: docs
url: /fa/com.aspose.slides/commentauthor/
---
**وراثت:**
java.lang.Object

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.ICommentAuthor](../../com.aspose.slides/icommentauthor), com.aspose.slides.IDOMObject
```
public final class CommentAuthor implements ICommentAuthor, IDOMObject
```

نمایانگر نویسندهٔ نظرات است.
## متدها

| متد | توضیح |
| --- | --- |
| [getName()](#getName--) | نام نویسنده را برمی‌گرداند یا تنظیم می‌کند. |
| [setName(String value)](#setName-java.lang.String-) | نام نویسنده را برمی‌گرداند یا تنظیم می‌کند. |
| [getInitials()](#getInitials--) | حروف اول نویسنده را برمی‌گرداند یا تنظیم می‌کند. |
| [setInitials(String value)](#setInitials-java.lang.String-) | حروف اول نویسنده را برمی‌گرداند یا تنظیم می‌کند. |
| [getComments()](#getComments--) | مجموعه نظراتی که این نویسنده ثبت کرده است را برمی‌گرداند. |
| [remove()](#remove--) | نویسنده را از مجموعه والد حذف می‌کند. |
| [getParent_Immediate()](#getParent-Immediate--) |  |

### getName() {#getName--}
```
public final String getName()
```

نام نویسنده را برمی‌گرداند یا تنظیم می‌کند. خواندنی/قابل نوشتن String.

**بازگشت:**
java.lang.String
### setName(String value) {#setName-java.lang.String-}
```
public final void setName(String value)
```

نام نویسنده را برمی‌گرداند یا تنظیم می‌کند. خواندنی/قابل نوشتن String.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | java.lang.String |  |

### getInitials() {#getInitials--}
```
public final String getInitials()
```

حروف اول نویسنده را برمی‌گرداند یا تنظیم می‌کند. خواندنی/قابل نوشتن String.

**بازگشت:**
java.lang.String
### setInitials(String value) {#setInitials-java.lang.String-}
```
public final void setInitials(String value)
```

حروف اول نویسنده را برمی‌گرداند یا تنظیم می‌کند. خواندنی/قابل نوشتن String.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | java.lang.String |  |

### getComments() {#getComments--}
```
public final ICommentCollection getComments()
```

مجموعه نظراتی که این نویسنده ثبت کرده است را برمی‌گرداند. فقط‌خواندنی [ICommentCollection](../../com.aspose.slides/icommentcollection).

**بازگشت:**
[ICommentCollection](../../com.aspose.slides/icommentcollection)
### remove() {#remove--}
```
public final void remove()
```

نویسنده را از مجموعه والد حذف می‌کند.

### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

شیء Parent_Immediate را برمی‌گرداند. فقط‌خواندنی IDOMObject.

**بازگشت:**
com.aspose.slides.IDOMObject