---
title: MasterTheme
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: نمایانگر یک تم اصلی است.
type: docs
url: /fa/com.aspose.slides/mastertheme/
---
**ارث‌بری:**  
java.lang.Object, [com.aspose.slides.Theme](../../com.aspose.slides/theme)

**همهٔ رابط‌های پیاده‌سازی‌شده:**  
[com.aspose.slides.IMasterTheme](../../com.aspose.slides/imastertheme)
```
public final class MasterTheme extends Theme implements IMasterTheme
```

نمایانگر یک تم اصلی است.
## متدها

| متد | توضیح |
| --- | --- |
| [getColorScheme()](#getColorScheme--) | طرح رنگی را باز می‌گرداند. |
| [getFontScheme()](#getFontScheme--) | طرح قلم را باز می‌گرداند. |
| [getFormatScheme()](#getFormatScheme--) | طرح فرمت شکل را باز می‌گرداند. |
| [getExtraColorSchemes()](#getExtraColorSchemes--) | مجموعهٔ طرح‌های رنگی اضافی را باز می‌گرداند. |
| [getName()](#getName--) | نام یک تم را باز می‌گرداند. |
| [setName(String value)](#setName-java.lang.String-) | نام یک تم را باز می‌گرداند. |
| [getVersion()](#getVersion--) |  |

### getColorScheme() {#getColorScheme--}
```
public IColorScheme getColorScheme()
```

طرح رنگی را باز می‌گرداند. فقط‌خواندنی [IColorScheme](../../com.aspose.slides/icolorscheme).

**باز می‌گرداند:**  
[IColorScheme](../../com.aspose.slides/icolorscheme)

### getFontScheme() {#getFontScheme--}
```
public IFontScheme getFontScheme()
```

طرح قلم را باز می‌گرداند. فقط‌خواندنی [IFontScheme](../../com.aspose.slides/ifontscheme).

**باز می‌گرداند:**  
[IFontScheme](../../com.aspose.slides/ifontscheme)

### getFormatScheme() {#getFormatScheme--}
```
public IFormatScheme getFormatScheme()
```

طرح فرمت شکل را باز می‌گرداند. فقط‌خواندنی [IFormatScheme](../../com.aspose.slides/iformatscheme).

**باز می‌گرداند:**  
[IFormatScheme](../../com.aspose.slides/iformatscheme)

### getExtraColorSchemes() {#getExtraColorSchemes--}
```
public final IExtraColorSchemeCollection getExtraColorSchemes()
```

مجموعهٔ طرح‌های رنگی اضافی را باز می‌گرداند. این طرح‌ها بر ظاهر ارائه تأثیری ندارند، می‌توانند به‌عنوان طرح رنگ اصلی برای یک اسلاید انتخاب شوند. فقط‌خواندنی [IExtraColorSchemeCollection](../../com.aspose.slides/iextracolorschemecollection).

**باز می‌گرداند:**  
[IExtraColorSchemeCollection](../../com.aspose.slides/iextracolorschemecollection)

### getName() {#getName--}
```
public final String getName()
```

نام یک تم را باز می‌گرداند. قابل‌نوشتن String.

**باز می‌گرداند:**  
java.lang.String

### setName(String value) {#setName-java.lang.String-}
```
public final void setName(String value)
```

نام یک تم را باز می‌گرداند. قابل‌نوشتن String.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | java.lang.String |  |

### getVersion() {#getVersion--}
```
public long getVersion()
```

نسخه. فقط‌خواندنی long.

**باز می‌گرداند:**  
long