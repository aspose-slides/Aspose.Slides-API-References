---
title: IPictureFillFormatEffectiveData
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: شیء غیرقابل تغییر که ویژگی‌های پر کردن تصویر را شامل می‌شود.
type: docs
url: /fa/com.aspose.slides/ipicturefillformateffectivedata/
---
**تمام رابط‌های پیاده‌سازی شده:**
[com.aspose.slides.IFillParamSource](../../com.aspose.slides/ifillparamsource)
```
public interface IPictureFillFormatEffectiveData extends IFillParamSource
```

شیء غیرقابل تغییر که ویژگی‌های پر کردن تصویر را شامل می‌شود.

--------------------

این رابط به‌عنوان بخشی از [IFillFormatEffectiveData](../../com.aspose.slides/ifillformateffectivedata) استفاده می‌شود.
## روش‌ها

| متد | توضیح |
| --- | --- |
| [getDpi()](#getDpi--) | dpi ای را که برای پر کردن تصویر استفاده می‌شود برمی‌گرداند. |
| [getPictureFillMode()](#getPictureFillMode--) | حالت پر کردن تصویر را برمی‌گرداند. |
| [getPicture()](#getPicture--) | تصویر را برمی‌گرداند. |
| [getCropLeft()](#getCropLeft--) | تعداد درصدهای عرض واقعی تصویر که از سمت چپ تصویر برش خورده‌اند را برمی‌گرداند. |
| [getCropTop()](#getCropTop--) | تعداد درصدهای ارتفاع واقعی تصویر که از بالای تصویر برش خورده‌اند را برمی‌گرداند. |
| [getCropRight()](#getCropRight--) | تعداد درصدهای عرض واقعی تصویر که از سمت راست تصویر برش خورده‌اند را برمی‌گرداند. |
| [getCropBottom()](#getCropBottom--) | تعداد درصدهای ارتفاع واقعی تصویر که از پایین تصویر برش خورده‌اند را برمی‌گرداند. |
### getDpi() {#getDpi--}
```
public abstract int getDpi()
```

dpi ای را که برای پر کردن تصویر استفاده می‌شود برمی‌گرداند. فقط‌قابل‌خواندن int.

**بازگرداندن:**
int
### getPictureFillMode() {#getPictureFillMode--}
```
public abstract int getPictureFillMode()
```

حالت پر کردن تصویر را برمی‌گرداند. فقط‌قابل‌خواندن [PictureFillMode](../../com.aspose.slides/picturefillmode).

**بازگرداندن:**
int
### getPicture() {#getPicture--}
```
public abstract IPictureEffectiveData getPicture()
```

تصویر را برمی‌گرداند. فقط‌قابل‌خواندن [IPictureEffectiveData](../../com.aspose.slides/ipictureeffectivedata).

**بازگرداندن:**
[IPictureEffectiveData](../../com.aspose.slides/ipictureeffectivedata)
### getCropLeft() {#getCropLeft--}
```
public abstract float getCropLeft()
```

تعداد درصدهای عرض واقعی تصویر که از سمت چپ تصویر برش خورده‌اند را برمی‌گرداند. فقط‌قابل‌خواندن float.

**بازگرداندن:**
float
### getCropTop() {#getCropTop--}
```
public abstract float getCropTop()
```

تعداد درصدهای ارتفاع واقعی تصویر که از بالای تصویر برش خورده‌اند را برمی‌گرداند. فقط‌قابل‌خواندن float.

**بازگرداندن:**
float
### getCropRight() {#getCropRight--}
```
public abstract float getCropRight()
```

تعداد درصدهای عرض واقعی تصویر که از سمت راست تصویر برش خورده‌اند را برمی‌گرداند. فقط‌قابل‌خواندن float.

**بازگرداندن:**
float
### getCropBottom() {#getCropBottom--}
```
public abstract float getCropBottom()
```

تعداد درصدهای ارتفاع واقعی تصویر که از پایین تصویر برش خورده‌اند را برمی‌گرداند. فقط‌قابل‌خواندن float.

**بازگرداندن:**
float