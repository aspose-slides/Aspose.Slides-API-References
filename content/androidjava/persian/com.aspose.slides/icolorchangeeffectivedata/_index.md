---
title: IColorChangeEffectiveData
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: شیء تغییرناپذیر که یک اثر تغییر رنگ را نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/icolorchangeeffectivedata/
---
**تمام اینترفیس‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IEffectEffectiveData](../../com.aspose.slides/ieffecteffectivedata)
```
public interface IColorChangeEffectiveData extends IEffectEffectiveData
```

شیء تغییرناپذیر که یک اثر تغییر رنگ را نشان می‌دهد. نمونه‌های FromColor با نمونه‌های ToColor جایگزین می‌شوند.
## متدها

| متد | توضیح |
| --- | --- |
| [getFromColor()](#getFromColor--) | رنگی که جایگزین خواهد شد. |
| [getToColor()](#getToColor--) | رنگی که جایگزین می‌کند. |
| [getUseAlpha()](#getUseAlpha--) | مقدار بولی‌ای را برمی‌گرداند که تعیین می‌کند آیا مؤلفه آلفا باید استفاده شود. |
### getFromColor() {#getFromColor--}
```
public abstract Integer getFromColor()
```

رنگی که جایگزین خواهد شد. فقط خواندنی java.lang.Integer.

**بازگشت:**
java.lang.Integer
### getToColor() {#getToColor--}
```
public abstract Integer getToColor()
```

رنگی که جایگزین می‌کند. فقط خواندنی java.lang.Integer.

**بازگشت:**
java.lang.Integer
### getUseAlpha() {#getUseAlpha--}
```
public abstract boolean getUseAlpha()
```

مقدار بولی‌ای را برمی‌گرداند که تعیین می‌کند آیا مؤلفه آلفا باید استفاده شود. فقط خواندنی boolean.

**بازگشت:**
boolean