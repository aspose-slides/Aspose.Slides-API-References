---
title: Placeholder
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: نمایانگر یک جای‌گیر روی اسلاید است.
type: docs
url: /fa/com.aspose.slides/placeholder/
---
**ارث‌بری:**
java.lang.Object

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IPlaceholder](../../com.aspose.slides/iplaceholder)
```
public class Placeholder implements IPlaceholder
```

یک جای‌گیر را روی اسلاید نشان می‌دهد.

## متدها

| متد | توضیح |
| --- | --- |
| [getOrientation()](#getOrientation--) | دریافت یا تنظیم جهت یک جای‌گیر. |
| [getSize()](#getSize--) | دریافت یا تنظیم اندازه یک جای‌گیر. |
| [getType()](#getType--) | دریافت یا تنظیم نوع یک جای‌گیر. |
| [getIndex()](#getIndex--) | دریافت یا تنظیم اندیس یک جای‌گیر. |

### getOrientation() {#getOrientation--}
```
public byte getOrientation()
```

دریافت یا تنظیم جهت یک جای‌گیر. فقط‌خواندنی [Orientation](../../com.aspose.slides/orientation).

**بازگشت:**
byte

### getSize() {#getSize--}
```
public byte getSize()
```

دریافت یا تنظیم اندازه یک جای‌گیر. فقط‌خواندنی [PlaceholderSize](../../com.aspose.slides/placeholdersize).

**بازگشت:**
byte

### getType() {#getType--}
```
public byte getType()
```

دریافت یا تنظیم نوع یک جای‌گیر. فقط‌خواندنی [PlaceholderType](../../com.aspose.slides/placeholdertype).

**بازگشت:**
byte

### getIndex() {#getIndex--}
```
public long getIndex()
```

دریافت یا تنظیم اندیس یک جای‌گیر. فقط‌خواندنی long.

**بازگشت:**
long