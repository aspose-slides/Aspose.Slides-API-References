---
title: SlideShowTransition
second_title: Aspose.Slides برای اندروید از طریق مرجع API جاوا
description: نمایانگر انتقال اسلاید نمایش.
type: docs
url: /fa/com.aspose.slides/slideshowtransition/
---
**ارث‌بری:**
java.lang.Object, com.aspose.slides.DomObject

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.ISlideShowTransition](../../com.aspose.slides/islideshowtransition)
```
public class SlideShowTransition extends DomObject<BaseSlide> implements ISlideShowTransition
```

نمایانگر انتقال اسلاید نمایش.

## متدها

| متد | توضیح |
| --- | --- |
| [getSound()](#getSound--) | داده‌های صوتی توکار را بازمی‌گرداند یا تنظیم می‌کند. |
| [setSound(IAudio value)](#setSound-com.aspose.slides.IAudio-) | داده‌های صوتی توکار را بازمی‌گرداند یا تنظیم می‌کند. |
| [getSoundMode()](#getSoundMode--) | حالت صدا برای انتقال اسلاید را تنظیم یا بازمی‌گرداند. |
| [setSoundMode(int value)](#setSoundMode-int-) | حالت صدا برای انتقال اسلاید را تنظیم یا بازمی‌گرداند. |
| [getSoundLoop()](#getSoundLoop--) | این ویژگی مشخص می‌کند که آیا صدا تا وقوع رویداد صوتی بعدی در اسلایدشو به‌صورت حلقه‌ای ادامه خواهد یافت یا نه. |
| [setSoundLoop(boolean value)](#setSoundLoop-boolean-) | این ویژگی مشخص می‌کند که آیا صدا تا وقوع رویداد صوتی بعدی در اسلایدشو به‌صورت حلقه‌ای ادامه خواهد یافت یا نه. |
| [getAdvanceOnClick()](#getAdvanceOnClick--) | مشخص می‌کند که آیا کلیک ماوس اسلاید را به جلو می‌برد یا نه. |
| [setAdvanceOnClick(boolean value)](#setAdvanceOnClick-boolean-) | مشخص می‌کند که آیا کلیک ماوس اسلاید را به جلو می‌برد یا نه. |
| [getAdvanceAfter()](#getAdvanceAfter--) | این ویژگی مشخص می‌کند که آیا اسلایدشو پس از زمان معینی به اسلاید بعدی می‌رود یا نه. |
| [setAdvanceAfter(boolean value)](#setAdvanceAfter-boolean-) | این ویژگی مشخص می‌کند که آیا اسلایدشو پس از زمان معینی به اسلاید بعدی می‌رود یا نه. |
| [getAdvanceAfterTime()](#getAdvanceAfterTime--) | زمانی را بر حسب میلی‌ثانیه مشخص می‌کند که پس از آن انتقال باید آغاز شود. |
| [setAdvanceAfterTime(long value)](#setAdvanceAfterTime-long-) | زمانی را بر حسب میلی‌ثانیه مشخص می‌کند که پس از آن انتقال باید آغاز شود. |
| [getSpeed()](#getSpeed--) | سرعت انتقالی که هنگام انتقال از اسلاید جاری به اسلاید بعدی استفاده می‌شود را مشخص می‌کند. |
| [setSpeed(int value)](#setSpeed-int-) | سرعت انتقالی که هنگام انتقال از اسلاید جاری به اسلاید بعدی استفاده می‌شود را مشخص می‌کند. |
| [getValue()](#getValue--) | مقدار انتقال اسلاید نمایش. |
| [getType()](#getType--) | نوع انتقال. |
| [setType(int value)](#setType-int-) | نوع انتقال. |
| [getSoundIsBuiltIn()](#getSoundIsBuiltIn--) | مشخص می‌کند که آیا این صدا یک صدای داخلی است یا نه. |
| [setSoundIsBuiltIn(boolean value)](#setSoundIsBuiltIn-boolean-) | مشخص می‌کند که آیا این صدا یک صدای داخلی است یا نه. |
| [getSoundName()](#getSoundName--) | نام قابل خواندن برای صدا در انتقال را مشخص می‌کند. |
| [setSoundName(String value)](#setSoundName-java.lang.String-) | نام قابل خواندن برای صدا در انتقال را مشخص می‌کند. |
| [getDuration()](#getDuration--) | مدت زمان اثر انتقال اسلاید را در میلی‌ثانیه دریافت یا تنظیم می‌کند. خواندنی/قابل نوشتن int. |
| [setDuration(int value)](#setDuration-int-) | مدت زمان اثر انتقال اسلاید را در میلی‌ثانیه دریافت یا تنظیم می‌کند. خواندنی/قابل نوشتن int. |
| [equals(Object obj)](#equals-java.lang.Object-) | تعیین می‌کند که آیا دو نمونهٔ SlideShowTransition برابر هستند یا نه. |
| [hashCode()](#hashCode--) | به‌عنوان یک تابع هش برای یک نوع خاص عمل می‌کند که برای استفاده در الگوریتم‌های هش و ساختارهای داده‌ای مانند جدول هش مناسب است. |

### getSound() {#getSound--}
```
public final IAudio getSound()
```

داده‌های صوتی توکار را بازمی‌گرداند یا تنظیم می‌کند. خواندنی/قابل نوشتن [IAudio](../../com.aspose.slides/iaudio).

**بازگشت:**
[IAudio](../../com.aspose.slides/iaudio)

### setSound(IAudio value) {#setSound-com.aspose.slides.IAudio-}
```
public final void setSound(IAudio value)
```

داده‌های صوتی توکار را بازمی‌گرداند یا تنظیم می‌کند. خواندنی/قابل نوشتن [IAudio](../../com.aspose.slides/iaudio).

**پارامترها:**
| پارامتر | Type | Description |
| --- | --- | --- |
| value | [IAudio](../../com.aspose.slides/iaudio) |  |

### getSoundMode() {#getSoundMode--}
```
public final int getSoundMode()
```

حالت صدا برای انتقال اسلاید را تنظیم یا بازمی‌گرداند. خواندنی/قابل نوشتن [TransitionSoundMode](../../com.aspose.slides/transitionsoundmode).

**بازگشت:**
int

### setSoundMode(int value) {#setSoundMode-int-}
```
public final void setSoundMode(int value)
```

حالت صدا برای انتقال اسلاید را تنظیم یا بازمی‌گرداند. خواندنی/قابل نوشتن [TransitionSoundMode](../../com.aspose.slides/transitionsoundmode).

**پارامترها:**
| پارامتر | Type | Description |
| --- | --- | --- |
| value | int |  |

### getSoundLoop() {#getSoundLoop--}
```
public final boolean getSoundLoop()
```

این ویژگی مشخص می‌کند که آیا صدا تا وقوع رویداد صوتی بعدی در اسلایدشو به‌صورت حلقه‌ای ادامه خواهد یافت یا نه. خواندنی/قابل نوشتن boolean.

**بازگشت:**
boolean

### setSoundLoop(boolean value) {#setSoundLoop-boolean-}
```
public final void setSoundLoop(boolean value)
```

این ویژگی مشخص می‌کند که آیا صدا تا وقوع رویداد صوتی بعدی در اسلایدشو به‌صورت حلقه‌ای ادامه خواهد یافت یا نه. خواندنی/قابل نوشتن boolean.

**پارامترها:**
| پارامتر | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getAdvanceOnClick() {#getAdvanceOnClick--}
```
public final boolean getAdvanceOnClick()
```

مشخص می‌کند که آیا کلیک ماوس اسلاید را به جلو می‌برد یا نه. اگر این ویژگی مشخص نشده باشد مقدار true فرض می‌شود. خواندنی/قابل نوشتن boolean.

**بازگشت:**
boolean

### setAdvanceOnClick(boolean value) {#setAdvanceOnClick-boolean-}
```
public final void setAdvanceOnClick(boolean value)
```

مشخص می‌کند که آیا کلیک ماوس اسلاید را به جلو می‌برد یا نه. اگر این ویژگی مشخص نشده باشد مقدار true فرض می‌شود. خواندنی/قابل نوشتن boolean.

**پارامترها:**
| پارامتر | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getAdvanceAfter() {#getAdvanceAfter--}
```
public final boolean getAdvanceAfter()
```

این ویژگی مشخص می‌کند که آیا اسلایدشو پس از زمان معینی به اسلاید بعدی می‌رود یا نه. خواندنی/قابل نوشتن boolean.

--------------------

> ```
> Presentation pres = new Presentation("demo.pptx");
>  try {
>      // دریافت اولین انتقال اسلاید
>      ISlideShowTransition slideTransition = pres.getSlides().get_Item(0).getSlideShowTransition();
> 
>      // بررسی اینکه آیا پرچم Advance Slide After فعال است
>      if (slideTransition.getAdvanceAfter())
>      {
>          // دریافت مقدار زمان پیشروی اسلاید بعدی
>          long advanceAfterTime = slideTransition.getAdvanceAfterTime();
>      }
>  } finally {
>      if (pres != null) pres.dispose();
>  }
> ```

**بازگشت:**
boolean

### setAdvanceAfter(boolean value) {#setAdvanceAfter-boolean-}
```
public final void setAdvanceAfter(boolean value)
```

این ویژگی مشخص می‌کند که آیا اسلایدشو پس از زمان معینی به اسلاید بعدی می‌رود یا نه. خواندنی/قابل نوشتن boolean.

--------------------

> ```
> Presentation pres = new Presentation("demo.pptx");
>  try {
>      // دریافت اولین انتقال اسلاید
>      ISlideShowTransition slideTransition = pres.getSlides().get_Item(0).getSlideShowTransition();
> 
>      // بررسی اینکه آیا پرچم Advance Slide After فعال است
>      if (slideTransition.getAdvanceAfter())
>      {
>          // دریافت مقدار زمان پیشروی اسلاید بعدی
>          long advanceAfterTime = slideTransition.getAdvanceAfterTime();
>      }
>  } finally {
>      if (pres != null) pres.dispose();
>  }
> ```

**پارامترها:**
| پارامتر | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getAdvanceAfterTime() {#getAdvanceAfterTime--}
```
public final long getAdvanceAfterTime()
```

زمانی را بر حسب میلی‌ثانیه مشخص می‌کند که پس از آن انتقال باید آغاز شود. این تنظیم می‌تواند همراه با ویژگی advClick استفاده شود. اگر این ویژگی مشخص نشده باشد فرض می‌شود که هیچ پیشروی خودکار انجام نمی‌شود. خواندنی/قابل نوشتن long.

**بازگشت:**
long

### setAdvanceAfterTime(long value) {#setAdvanceAfterTime-long-}
```
public final void setAdvanceAfterTime(long value)
```

زمانی را بر حسب میلی‌ثانیه مشخص می‌کند که پس از آن انتقال باید آغاز شود. این تنظیم می‌تواند همراه با ویژگی advClick استفاده شود. اگر این ویژگی مشخص نشده باشد فرض می‌شود که هیچ پیشروی خودکار انجام نمی‌شود. خواندنی/قابل نوشتن long.

**پارامترها:**
| پارامتر | Type | Description |
| --- | --- | --- |
| value | long |  |

### getSpeed() {#getSpeed--}
```
public final int getSpeed()
```

سرعت انتقالی که هنگام انتقال از اسلاید جاری به اسلاید بعدی استفاده می‌شود را مشخص می‌کند. خواندنی/قابل نوشتن [TransitionSpeed](../../com.aspose.slides/transitionspeed).

**بازگشت:**
int

### setSpeed(int value) {#setSpeed-int-}
```
public final void setSpeed(int value)
```

سرعت انتقالی که هنگام انتقال از اسلاید جاری به اسلاید بعدی استفاده می‌شود را مشخص می‌کند. خواندنی/قابل نوشتن [TransitionSpeed](../../com.aspose.slides/transitionspeed).

**پارامترها:**
| پارامتر | Type | Description |
| --- | --- | --- |
| value | int |  |

### getValue() {#getValue--}
```
public final ITransitionValueBase getValue()
```

مقدار انتقال اسلاید نمایش. فقط-خواندنی [ITransitionValueBase](../../com.aspose.slides/itransitionvaluebase).

**بازگشت:**
[ITransitionValueBase](../../com.aspose.slides/itransitionvaluebase)

### getType() {#getType--}
```
public final int getType()
```

نوع انتقال. خواندنی/قابل نوشتن [TransitionType](../../com.aspose.slides/transitiontype).

**بازگشت:**
int

### setType(int value) {#setType-int-}
```
public final void setType(int value)
```

نوع انتقال. خواندنی/قابل نوشتن [TransitionType](../../com.aspose.slides/transitiontype).

**پارامترها:**
| پارامتر | Type | Description |
| --- | --- | --- |
| value | int |  |

### getSoundIsBuiltIn() {#getSoundIsBuiltIn--}
```
public final boolean getSoundIsBuiltIn()
```

مشخص می‌کند که آیا این صدا یک صدای داخلی است یا نه. اگر این ویژگی برابر true تنظیم شود، برنامهٔ تولید‌کننده هشدار می‌دهد تا ویژگی name را که برای این صدا در فهرست صداهای داخلی مشخص شده است بررسی کند و سپس نام سفارشی یا رابط کاربری مناسب ارائه دهد. خواندنی/قابل نوشتن boolean.

**بازگشت:**
boolean

### setSoundIsBuiltIn(boolean value) {#setSoundIsBuiltIn-boolean-}
```
public final void setSoundIsBuiltIn(boolean value)
```

مشخص می‌کند که آیا این صدا یک صدای داخلی است یا نه. اگر این ویژگی برابر true تنظیم شود، برنامهٔ تولید‌کننده هشدار می‌دهد تا ویژگی name را که برای این صدا در فهرست صداهای داخلی مشخص شده است بررسی کند و سپس نام سفارشی یا رابط کاربری مناسب ارائه دهد. خواندنی/قابل نوشتن boolean.

**پارامترها:**
| پارامتر | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getSoundName() {#getSoundName--}
```
public final String getSoundName()
```

نام قابل خواندن برای صدا در انتقال را مشخص می‌کند. ویژگی Sound (\#getSound.getSound/\#setSound(IAudio).setSound(IAudio)) باید برای دریافت یا تنظیم نام صدا اختصاص داده شود. خواندنی/قابل نوشتن String.

**بازگشت:**
java.lang.String

### setSoundName(String value) {#setSoundName-java.lang.String-}
```
public final void setSoundName(String value)
```

نام قابل خواندن برای صدا در انتقال را مشخص می‌کند. ویژگی Sound (\#getSound.getSound/\#setSound(IAudio).setSound(IAudio)) باید برای دریافت یا تنظیم نام صدا اختصاص داده شود. خواندنی/قابل نوشتن String.

**پارامترها:**
| پارامتر | Type | Description |
| --- | --- | --- |
| value | java.lang.String |  |

### getDuration() {#getDuration--}
```
public final int getDuration()
```

مدت زمان اثر انتقال اسلاید را در میلی‌ثانیه دریافت یا تنظیم می‌کند. خواندنی/قابل نوشتن int.

--------------------

متقابل با ویژگی p14:dur عنصر p:transition در طرح PresentationML است. اگر تنظیم نشود، مدت زمان به‌صورت خودکار بر اساس ویژگی \#getSpeed.getSpeed/\#setSpeed(int).setSpeed(int) و نوع انتقال تعیین می‌شود.

**بازگشت:**
int

### setDuration(int value) {#setDuration-int-}
```
public final void setDuration(int value)
```

مدت زمان اثر انتقال اسلاید را در میلی‌ثانیه دریافت یا تنظیم می‌کند. خواندنی/قابل نوشتن int.

--------------------

متقابل با ویژگی p14:dur عنصر p:transition در طرح PresentationML است. اگر تنظیم نشود، مدت زمان به‌صورت خودکار بر اساس ویژگی \#getSpeed.getSpeed/\#setSpeed(int).setSpeed(int) و نوع انتقال تعیین می‌شود.

**پارامترها:**
| پارامتر | Type | Description |
| --- | --- | --- |
| value | int |  |

### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```

تعیین می‌کند که آیا دو نمونهٔ SlideShowTransition برابر هستند یا نه. خواندنی/قابل نوشتن boolean.

**پارامترها:**
| پارامتر | Type | Description |
| --- | --- | --- |
| obj | java.lang.Object | SlideShowTransition برای مقایسه با SlideShowTransition فعلی. |

**بازگشت:**
boolean -  **true**  اگر SlideShowTransition مشخص‌شده برابر با SlideShowTransition فعلی باشد؛ در غیر این صورت  **false** .

### hashCode() {#hashCode--}
```
public int hashCode()
```

به‌عنوان یک تابع هش برای یک نوع خاص عمل می‌کند که برای استفاده در الگوریتم‌های هش و ساختارهای داده‌ای مانند جدول هش مناسب است.

**بازگشت:**
int - 23454

--------------------

بازنویسی شده برای خوشایند کردن کامپایلر. همیشه مقدار ثابت را بازمی‌گرداند زیرا شی قابل تغییر است.