---
title: CrossesType
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: مشخص می‌کند که محور در کجا عبور می‌کند.
type: docs
url: /fa/com.aspose.slides/crossestype/
---
**ارث‌بری:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class CrossesType extends System.Enum
```

مشخص می‌کند که محور در کجا عبور می‌کند.
## فیلدها

| فیلد | توضیح |
| --- | --- |
| [AxisCrossesAtZero](#AxisCrossesAtZero) | محور دسته‌بندی در نقطه صفر محور مقدار عبور می‌کند (در صورت امکان)، یا در مقدار حداقل (اگر حداقل بزرگتر از صفر باشد) یا در مقدار حداکثر (اگر حداکثر کمتر از صفر باشد). |
| [Maximum](#Maximum) | محور در مقدار حداکثر عبور می‌کند. |
| [Custom](#Custom) | مقدار سفارشی از ویژگی CrossAt |
### AxisCrossesAtZero {#AxisCrossesAtZero}
```
public static final int AxisCrossesAtZero
```

محور دسته‌بندی در نقطه صفر محور مقدار عبور می‌کند (در صورت امکان)، یا در مقدار حداقل (اگر حداقل بزرگتر از صفر باشد) یا در مقدار حداکثر (اگر حداکثر کمتر از صفر باشد).

### Maximum {#Maximum}
```
public static final int Maximum
```

محور در مقدار حداکثر عبور می‌کند.

### Custom {#Custom}
```
public static final int Custom
```

مقدار سفارشی از ویژگی CrossAt