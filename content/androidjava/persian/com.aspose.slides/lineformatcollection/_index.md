---
title: LineFormatCollection
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: نمایانگر مجموعه‌ای از سبک‌های خط.
type: docs
url: /fa/com.aspose.slides/lineformatcollection/
---
**ارث‌بری:**
java.lang.Object, com.aspose.slides.DomObject

**همه اینترفیس‌های پیاده‌سازی‌شده:**
[com.aspose.slides.ILineFormatCollection](../../com.aspose.slides/ilineformatcollection)
```
public final class LineFormatCollection extends DomObject<FormatScheme> implements ILineFormatCollection
```

نمایانگر مجموعه‌ای از سبک‌های خط است.
## متدها

| Method | Description |
| --- | --- |
| [get_Item(int index)](#get-Item-int-) | عنصری را که در شاخص مشخص شده است دریافت می‌کند. |
| [iterator()](#iterator--) | یک شمارنده را برمی‌گرداند که بر روی مجموعه تکرار می‌کند. |
| [iteratorJava()](#iteratorJava--) | یک تکرارکنندهٔ جاوا برای کل مجموعه برمی‌گرداند. |
| [size()](#size--) | تعداد عناصری که واقعاً در مجموعه موجود هستند را دریافت می‌کند. |
| [copyTo(System.Array array, int index)](#copyTo-com.aspose.ms.System.Array-int-) | همهٔ عناصر را از مجموعه به آرایهٔ مشخص شده کپی می‌کند. |
| [isSynchronized()](#isSynchronized--) | مقداری را برمی‌گرداند که نشان می‌دهد دسترسی به مجموعه همگام‌سازی شده است (ایمن نسبت به رشته‌ها). |
| [getSyncRoot()](#getSyncRoot--) | یک ریشهٔ همگام‌سازی را برمی‌گرداند. |
### get_Item(int index) {#get-Item-int-}
```
public final ILineFormat get_Item(int index)
```

عنصری را که در شاخص مشخص شده است دریافت می‌کند. فقط قابل خواندن [ILineFormat](../../com.aspose.slides/ilineformat).

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| index | int |  |

**باز می‌گرداند:**
[ILineFormat](../../com.aspose.slides/ilineformat)
### iterator() {#iterator--}
```
public final System.Collections.Generic.IGenericEnumerator<ILineFormat> iterator()
```

یک شمارنده را برمی‌گرداند که بر روی مجموعه تکرار می‌کند.

**باز می‌گرداند:**
com.aspose.ms.System.Collections.Generic.IGenericEnumerator<com.aspose.slides.ILineFormat> - یک IGenericEnumerator که می‌توان از آن برای تکرار در مجموعه استفاده کرد.
### iteratorJava() {#iteratorJava--}
```
public final System.Collections.Generic.IGenericEnumerator<ILineFormat> iteratorJava()
```

یک تکرارکنندهٔ جاوا برای کل مجموعه برمی‌گرداند.

**باز می‌گرداند:**
com.aspose.ms.System.Collections.Generic.IGenericEnumerator<com.aspose.slides.ILineFormat> - یک java.util.Iterator برای کل مجموعه.
### size() {#size--}
```
public final int size()
```

تعداد عناصری که واقعاً در مجموعه موجود هستند را دریافت می‌کند. int فقط قابل خواندن.

**باز می‌گرداند:**
int
### copyTo(System.Array array, int index) {#copyTo-com.aspose.ms.System.Array-int-}
```
public final void copyTo(System.Array array, int index)
```

همهٔ عناصر را از مجموعه به آرایهٔ مشخص شده کپی می‌کند.

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| array | com.aspose.ms.System.Array | آرایه هدف. |
| index | int | شاخص شروع در آرایه هدف. |

### isSynchronized() {#isSynchronized--}
```
public final boolean isSynchronized()
```

مقداری را برمی‌گرداند که نشان می‌دهد دسترسی به مجموعه همگام‌سازی شده است (ایمن نسبت به رشته‌ها). boolean فقط قابل خواندن.

**باز می‌گرداند:**
boolean
### getSyncRoot() {#getSyncRoot--}
```
public final Object getSyncRoot()
```

یک ریشهٔ همگام‌سازی را برمی‌گرداند. Object فقط قابل خواندن.

**باز می‌گرداند:**
java.lang.Object