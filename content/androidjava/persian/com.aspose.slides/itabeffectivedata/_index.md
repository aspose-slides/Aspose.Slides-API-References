---
title: ITabEffectiveData
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: شیء غیرقابل تغییر که ویژگی‌های توقف تب‌بندی متن‌های مؤثر را شامل می‌شود.
type: docs
url: /fa/com.aspose.slides/itabeffectivedata/
---
**تمام اینترفیس‌های پیاده‌سازی شده:**
java.lang.Comparable
```
public interface ITabEffectiveData extends Comparable
```

شیء غیرقابل تغییر که ویژگی‌های توقف تب‌بندی متن مؤثر را شامل می‌شود.

--------------------

این اینترفیس به عنوان بخشی از [IParagraphFormatEffectiveData](../../com.aspose.slides/iparagraphformateffectivedata) استفاده می‌شود.
## متدها

| متد | توضیح |
| --- | --- |
| [getPosition()](#getPosition--) | موقعیت یک تب را برمی‌گرداند. |
| [getAlignment()](#getAlignment--) | سبک تراز یک تب را برمی‌گرداند. |
### getPosition() {#getPosition--}
```
public abstract double getPosition()
```


موقعیت یک تب را برمی‌گرداند. اختصاص این ویژگی می‌تواند شاخص تب را در مجموعه تغییر دهد و Enumerator را نامعتبر کند. فقط خواندنی double.

**باز می‌گرداند:**
double
### getAlignment() {#getAlignment--}
```
public abstract int getAlignment()
```


سبک تراز یک تب را برمی‌گرداند. فقط خواندنی [TabAlignment](../../com.aspose.slides/tabalignment).

**باز می‌گرداند:**
int