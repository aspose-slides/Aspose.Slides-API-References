---
title: IBaseTableFormatEffectiveData
second_title: Aspose.Slides for Android via Java API Reference
description: رابط پایه برای اشیای غیرقابل تغییر که دارای ویژگی‌های قالب‌بندی جدول مؤثر هستند.
type: docs
url: /fa/com.aspose.slides/ibasetableformateffectivedata/
---```
public interface IBaseTableFormatEffectiveData
```

رابط پایه برای اشیای غیرقابل تغییر که دارای ویژگی‌های قالب‌بندی جدول مؤثر هستند.
## متدها

| متد | توضیح |
| --- | --- |
| [getFillFormat()](#getFillFormat--) | مقدار مؤثر قالب پر کردن را برمی‌گرداند. |
| [getBorderLeft()](#getBorderLeft--) | مقدار مؤثر قالب خط حاشیه چپ را برمی‌گرداند. |
| [getBorderTop()](#getBorderTop--) | مقدار مؤثر قالب خط حاشیه بالایی را برمی‌گرداند. |
| [getBorderRight()](#getBorderRight--) | مقدار مؤثر قالب خط حاشیه راست را برمی‌گرداند. |
| [getBorderBottom()](#getBorderBottom--) | مقدار مؤثر قالب خط حاشیه پایین را برمی‌گرداند. |
| [getBorderDiagonalDown()](#getBorderDiagonalDown--) | مقدار مؤثر قالب خط قطری پایین را برمی‌گرداند. |
| [getBorderDiagonalUp()](#getBorderDiagonalUp--) | مقدار مؤثر قالب خط قطری بالا را برمی‌گرداند. |
### getFillFormat() {#getFillFormat--}
```
public abstract IFillFormatEffectiveData getFillFormat()
```


مقدار مؤثر قالب پر کردن را برمی‌گرداند. فقط خواندنی [IFillFormatEffectiveData](../../com.aspose.slides/ifillformateffectivedata).

**بازگشت:**
[IFillFormatEffectiveData](../../com.aspose.slides/ifillformateffectivedata)
### getBorderLeft() {#getBorderLeft--}
```
public abstract ILineFormatEffectiveData getBorderLeft()
```


مقدار مؤثر قالب خط حاشیه چپ را برمی‌گرداند. فقط خواندنی [ILineFormatEffectiveData](../../com.aspose.slides/ilineformateffectivedata).

**بازگشت:**
[ILineFormatEffectiveData](../../com.aspose.slides/ilineformateffectivedata)
### getBorderTop() {#getBorderTop--}
```
public abstract ILineFormatEffectiveData getBorderTop()
```


مقدار مؤثر قالب خط حاشیه بالایی را برمی‌گرداند. فقط خواندنی [ILineFormatEffectiveData](../../com.aspose.slides/ilineformateffectivedata).

**بازگشت:**
[ILineFormatEffectiveData](../../com.aspose.slides/ilineformateffectivedata)
### getBorderRight() {#getBorderRight--}
```
public abstract ILineFormatEffectiveData getBorderRight()
```


مقدار مؤثر قالب خط حاشیه راست را برمی‌گرداند. فقط خواندنی [ILineFormatEffectiveData](../../com.aspose.slides/ilineformateffectivedata).

**بازگشت:**
[ILineFormatEffectiveData](../../com.aspose.slides/ilineformateffectivedata)
### getBorderBottom() {#getBorderBottom--}
```
public abstract ILineFormatEffectiveData getBorderBottom()
```


مقدار مؤثر قالب خط حاشیه پایین را برمی‌گرداند. فقط خواندنی [ILineFormatEffectiveData](../../com.aspose.slides/ilineformateffectivedata).

**بازگشت:**
[ILineFormatEffectiveData](../../com.aspose.slides/ilineformateffectivedata)
### getBorderDiagonalDown() {#getBorderDiagonalDown--}
```
public abstract ILineFormatEffectiveData getBorderDiagonalDown()
```


مقدار مؤثر قالب خط قطری پایین را برمی‌گرداند. فقط خواندنی [ILineFormatEffectiveData](../../com.aspose.slides/ilineformateffectivedata).

**بازگشت:**
[ILineFormatEffectiveData](../../com.aspose.slides/ilineformateffectivedata)
### getBorderDiagonalUp() {#getBorderDiagonalUp--}
```
public abstract ILineFormatEffectiveData getBorderDiagonalUp()
```


مقدار مؤثر قالب خط قطری بالا را برمی‌گرداند. فقط خواندنی [ILineFormatEffectiveData](../../com.aspose.slides/ilineformateffectivedata).

**بازگشت:**
[ILineFormatEffectiveData](../../com.aspose.slides/ilineformateffectivedata)