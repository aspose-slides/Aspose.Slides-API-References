---
title: PresentationHeaderFooterManager
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: مدیری را نشان می‌دهد که رفتار تمام مکان‌دارهای پاصقحه، تاریخ-زمان و شماره صفحهٔ ارائه را در بر می‌گیرد.
type: docs
url: /fa/com.aspose.slides/presentationheaderfootermanager/
---
**ارث‌بری:**
java.lang.Object, [com.aspose.slides.BaseHeaderFooterManager](../../com.aspose.slides/baseheaderfootermanager)

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IPresentationHeaderFooterManager](../../com.aspose.slides/ipresentationheaderfootermanager)
```
public class PresentationHeaderFooterManager extends BaseHeaderFooterManager implements IPresentationHeaderFooterManager
```

نمایندهٔ مدیری است که رفتار تمام مکان‌دارهای پاصقحه، تاریخ-زمان و شماره صفحهٔ ارائه را در بر می‌گیرد.
## متدها

| متد | توضیح |
| --- | --- |
| [setAllHeadersVisibility(boolean isVisible)](#setAllHeadersVisibility-boolean-) | تمام مکان‌دارهای سرصفحه را تغییر می‌دهد، از جمله مستر یادداشت‌ها، اسلایدهای یادداشت و مستر جزوه. |
| [setAllFootersVisibility(boolean isVisible)](#setAllFootersVisibility-boolean-) | تمام مکان‌دارهای پاصقحه را تغییر می‌دهد، از جمله اسلایدهای مستر، اسلایدهای طرح‌بندی، اسلایدها، مستر یادداشت‌ها، اسلایدهای یادداشت و مستر جزوه. |
| [setAllSlideNumbersVisibility(boolean isVisible)](#setAllSlideNumbersVisibility-boolean-) | تمام مکان‌دارهای شماره صفحه را تغییر می‌دهد، از جمله اسلایدهای مستر، اسلایدهای طرح‌بندی، اسلایدها، مستر یادداشت‌ها، اسلایدهای یادداشت و مستر جزوه. |
| [setAllDateTimesVisibility(boolean isVisible)](#setAllDateTimesVisibility-boolean-) | تمام مکان‌دارهای تاریخ-زمان را تغییر می‌دهد، از جمله اسلایدهای مستر، اسلایدهای طرح‌بندی، اسلایدها، مستر یادداشت‌ها، اسلایدهای یادداشت و مستر جزوه. |
| [setAllHeadersText(String text)](#setAllHeadersText-java.lang.String-) | متن را برای تمام مکان‌دارهای سرصفحه تنظیم می‌کند، از جمله مستر یادداشت‌ها، اسلایدهای یادداشت و مستر جزوه. |
| [setAllFootersText(String text)](#setAllFootersText-java.lang.String-) | متن را برای تمام مکان‌دارهای پاصقحه تنظیم می‌کند، از جمله اسلایدهای مستر، اسلایدهای طرح‌بندی، اسلایدها، مستر یادداشت‌ها، اسلایدهای یادداشت و مستر جزوه. |
| [setAllDateTimesText(String text)](#setAllDateTimesText-java.lang.String-) | متن را برای تمام مکان‌دارهای تاریخ-زمان تنظیم می‌کند، از جمله اسلایدهای مستر، اسلایدهای طرح‌بندی، اسلایدها، مستر یادداشت‌ها، اسلایدهای یادداشت و مستر جزوه. |
| [setVisibilityOnAllTitleSlides(boolean isVisible)](#setVisibilityOnAllTitleSlides-boolean-) | نمایش مکان‌دارهای پاصقحه، تاریخ-زمان و شماره صفحه را برای تمام اسلایدهای عنوان و اولین اسلاید طرح‌بندی تغییر می‌دهد. |

### setAllHeadersVisibility(boolean isVisible) {#setAllHeadersVisibility-boolean-}
```
public final void setAllHeadersVisibility(boolean isVisible)
```

تمام مکان‌دارهای سرصفحه را تغییر می‌دهد، از جمله مستر یادداشت‌ها، اسلایدهای یادداشت و مستر جزوه.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - مکان‌دارهای سرصفحه را قابل مشاهده می‌کند، در غیر این صورت آن‌ها را مخفی می‌سازد. |

### setAllFootersVisibility(boolean isVisible) {#setAllFootersVisibility-boolean-}
```
public final void setAllFootersVisibility(boolean isVisible)
```

تمام مکان‌دارهای پاصقحه را تغییر می‌دهد، از جمله اسلایدهای مستر، اسلایدهای طرح‌بندی، اسلایدها، مستر یادداشت‌ها، اسلایدهای یادداشت و مستر جزوه.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - مکان‌دارهای پاصقحه را قابل مشاهده می‌کند، در غیر این صورت آن‌ها را مخفی می‌سازد. |

### setAllSlideNumbersVisibility(boolean isVisible) {#setAllSlideNumbersVisibility-boolean-}
```
public final void setAllSlideNumbersVisibility(boolean isVisible)
```

تمام مکان‌دارهای شماره صفحه را تغییر می‌دهد، از جمله اسلایدهای مستر، اسلایدهای طرح‌بندی، اسلایدها، مستر یادداشت‌ها، اسلایدهای یادداشت و مستر جزوه.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - مکان‌دارهای شماره صفحه را قابل مشاهده می‌کند، در غیر این صورت آن‌ها را مخفی می‌سازد. |

### setAllDateTimesVisibility(boolean isVisible) {#setAllDateTimesVisibility-boolean-}
```
public final void setAllDateTimesVisibility(boolean isVisible)
```

تمام مکان‌دارهای تاریخ-زمان را تغییر می‌دهد، از جمله اسلایدهای مستر، اسلایدهای طرح‌بندی، اسلایدها، مستر یادداشت‌ها، اسلایدهای یادداشت و مستر جزوه.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - مکان‌دارهای تاریخ-زمان را قابل مشاهده می‌کند، در غیر این صورت آن‌ها را مخفی می‌سازد. |

### setAllHeadersText(String text) {#setAllHeadersText-java.lang.String-}
```
public final void setAllHeadersText(String text)
```

متن را برای تمام مکان‌دارهای سرصفحه تنظیم می‌کند، از جمله مستر یادداشت‌ها، اسلایدهای یادداشت و مستر جزوه.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| text | java.lang.String | متنی که باید تنظیم شود. |

### setAllFootersText(String text) {#setAllFootersText-java.lang.String-}
```
public final void setAllFootersText(String text)
```

متن را برای تمام مکان‌دارهای پاصقحه تنظیم می‌کند، از جمله اسلایدهای مستر، اسلایدهای طرح‌بندی، اسلایدها، مستر یادداشت‌ها، اسلایدهای یادداشت و مستر جزوه.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| text | java.lang.String | متنی که باید تنظیم شود. |

### setAllDateTimesText(String text) {#setAllDateTimesText-java.lang.String-}
```
public final void setAllDateTimesText(String text)
```

متن را برای تمام مکان‌دارهای تاریخ-زمان تنظیم می‌کند، از جمله اسلایدهای مستر، اسلایدهای طرح‌بندی، اسلایدها، مستر یادداشت‌ها، اسلایدهای یادداشت و مستر جزوه.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| text | java.lang.String | متنی که باید تنظیم شود. |

### setVisibilityOnAllTitleSlides(boolean isVisible) {#setVisibilityOnAllTitleSlides-boolean-}
```
public final void setVisibilityOnAllTitleSlides(boolean isVisible)
```

نمایش مکان‌دارهای پاصقحه، تاریخ-زمان و شماره صفحه را برای تمام اسلایدهای عنوان و اولین اسلاید طرح‌بندی تغییر می‌دهد. اسلایدهای عنوان – اسلایدهایی بر پایه اولین اسلاید طرح‌بندی (بدون درنظر گرفتن نوع این طرح‌بندی اولیه).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - مکان‌دارها را قابل مشاهده می‌کند، در غیر این صورت آن‌ها را مخفی می‌سازد. |