---
title: FillType
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: نوع پرکننده داخلی اشیای بصری مختلف را مشخص می‌کند.
type: docs
url: /fa/com.aspose.slides/filltype/
---
**وراثت:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class FillType extends System.Enum
```

نوع پرکننده داخلی اشیای بصری مختلف را مشخص می‌کند.

## فیلدها

| فیلد | توضیح |
| --- | --- |
| [NotDefined](#NotDefined) | نوع پرکننده تعریف نشده است. |
| [NoFill](#NoFill) | هیچ پرکننده‌ای اعمال نشده است. |
| [Solid](#Solid) | با رنگ ثابت پر شده است. |
| [Gradient](#Gradient) | پرکننده به صورت گرادیان است. |
| [Pattern](#Pattern) | الگوی تکراری برای پر کردن شیء استفاده می‌شود. |
| [Picture](#Picture) | یک تصویر واحد برای پر کردن شیء استفاده می‌شود. |
| [Group](#Group) | شیء بصری ویژگی‌های پرکننده را از گروه به ارث می‌برد. |

### NotDefined {#NotDefined}
```
public static final byte NotDefined
```

نوع پرکننده تعریف نشده است.

### NoFill {#NoFill}
```
public static final byte NoFill
```

هیچ پرکننده‌ای اعمال نشده است.

### Solid {#Solid}
```
public static final byte Solid
```

با رنگ ثابت پر شده است.

### Gradient {#Gradient}
```
public static final byte Gradient
```

پرکننده به صورت گرادیان است.

### Pattern {#Pattern}
```
public static final byte Pattern
```

الگوی تکراری برای پر کردن شیء استفاده می‌شود.

### Picture {#Picture}
```
public static final byte Picture
```

یک تصویر واحد برای پر کردن شیء استفاده می‌شود.

### Group {#Group}
```
public static final byte Group
```

شیء بصری ویژگی‌های پرکننده را از گروه به ارث می‌برد.