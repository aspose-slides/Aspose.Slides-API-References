---
title: Marker
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: نمایشگر نشانگر یک چرت.
type: docs
url: /fa/com.aspose.slides/marker/
---
**ارث‌بندی:**
java.lang.Object

**تمام رابط‌های پیاده‌سازی شده:**
[com.aspose.slides.IMarker](../../com.aspose.slides/imarker), com.aspose.slides.IDOMObject
```
public class Marker implements IMarker, IDOMObject
```

نمایشگر نشانگر یک chert.

## متدها

| متد | توضیح |
| --- | --- |
| [getSymbol()](#getSymbol--) | سبک نشانگر را در یک نمودار خطی، نمودار پراکنش یا نمودار راداری نمایش می‌دهد. |
| [setSymbol(int value)](#setSymbol-int-) | سبک نشانگر را در یک نمودار خطی، نمودار پراکنش یا نمودار راداری نمایش می‌دهد. |
| [getFormat()](#getFormat--) | پرکننده نشانگر را دریافت یا تنظیم می‌کند. |
| [getSize()](#getSize--) | اندازه نشانگر را در یک نمودار خطی، نمودار پراکنش یا نمودار راداری نمایش می‌دهد. |
| [setSize(int value)](#setSize-int-) | اندازه نشانگر را در یک نمودار خطی، نمودار پراکنش یا نمودار راداری نمایش می‌دهد. |
| [getParent_Immediate()](#getParent-Immediate--) |  |

### getSymbol() {#getSymbol--}
```
public final int getSymbol()
```

سبک نشانگر را در یک نمودار خطی، نمودار پراکنش یا نمودار راداری نمایش می‌دهد. خواندنی/قابل‌نوشتن [MarkerStyleType](../../com.aspose.slides/markerstyletype).

**باز می‌گرداند:**
int

### setSymbol(int value) {#setSymbol-int-}
```
public final void setSymbol(int value)
```

سبک نشانگر را در یک نمودار خطی، نمودار پراکنش یا نمودار راداری نمایش می‌دهد. خواندنی/قابل‌نوشتن [MarkerStyleType](../../com.aspose.slides/markerstyletype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

پرکننده نشانگر را دریافت یا تنظیم می‌کند. فقط‌خواندنی [IFormat](../../com.aspose.slides/iformat).

**باز می‌گرداند:**
[IFormat](../../com.aspose.slides/iformat)

### getSize() {#getSize--}
```
public final int getSize()
```

اندازه نشانگر را در یک نمودار خطی، نمودار پراکنش یا نمودار راداری نمایش می‌دهد. خواندنی/قابل‌نوشتن int.

**باز می‌گرداند:**
int

### setSize(int value) {#setSize-int-}
```
public final void setSize(int value)
```

اندازه نشانگر را در یک نمودار خطی، نمودار پراکنش یا نمودار راداری نمایش می‌دهد. خواندنی/قابل‌نوشتن int.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

شیء Parent_Immediate را باز می‌گرداند. فقط‌خواندنی IDOMObject.

**باز می‌گرداند:**
com.aspose.slides.IDOMObject