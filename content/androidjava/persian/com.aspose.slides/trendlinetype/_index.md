---
title: TrendlineType
second_title: Aspose.Slides برای Android از طریق ارجاع API جاوا
description: نمایانگر نوع خط روند
type: docs
url: /fa/com.aspose.slides/trendlinetype/
---
**وراثت:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class TrendlineType extends System.Enum
```

نمایش نوع خط روند
## فیلدها

| فیلد | توضیح |
| --- | --- |
| [Exponential](#Exponential) | مشخص می‌کند که خط روند باید یک منحنی نمایی به شکل داشته باشد. |
| [Linear](#Linear) | مشخص می‌کند که خط روند باید یک خط به شکل داشته باشد. |
| [Logarithmic](#Logarithmic) | مشخص می‌کند که خط روند باید یک منحنی لگاریتمی به شکل داشته باشد، که در آن log لگاریتم طبیعی است. |
| [MovingAverage](#MovingAverage) | مشخص می‌کند که خط روند باید یک میانگین متحرک با دوره Period باشد. |
| [Polynomial](#Polynomial) | مشخص می‌کند که خط روند باید یک منحنی چندجمله‌ای به ترتیب Order به شکل داشته باشد. |
| [Power](#Power) | مشخص می‌کند که خط روند باید یک منحنی توان به شکل داشته باشد. |
### نمایی {#Exponential}
```
public static final int Exponential
```

مشخص می‌کند که خط روند باید یک منحنی نمایی به شکل داشته باشد.

### خطی {#Linear}
```
public static final int Linear
```

مشخص می‌کند که خط روند باید یک خط به شکل داشته باشد.

### لگاریتمی {#Logarithmic}
```
public static final int Logarithmic
```

مشخص می‌کند که خط روند باید یک منحنی لگاریتمی به شکل داشته باشد، که در آن log لگاریتم طبیعی است.

### میانگین‌متحرک {#MovingAverage}
```
public static final int MovingAverage
```

مشخص می‌کند که خط روند باید یک میانگین متحرک با دوره Period باشد.

### چندجمله‌ای {#Polynomial}
```
public static final int Polynomial
```

مشخص می‌کند که خط روند باید یک منحنی چندجمله‌ای به ترتیب Order به شکل داشته باشد.

### توان {#Power}
```
public static final int Power
```

مشخص می‌کند که خط روند باید یک منحنی توان به شکل داشته باشد.