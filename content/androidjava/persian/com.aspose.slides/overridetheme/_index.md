---
title: OverrideTheme
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: یک تم بازنویسی را نمایش می‌دهد.
type: docs
url: /fa/com.aspose.slides/overridetheme/
---
**وراثت:**
java.lang.Object, [com.aspose.slides.Theme](../../com.aspose.slides/theme)

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IOverrideTheme](../../com.aspose.slides/ioverridetheme)
```
public final class OverrideTheme extends Theme implements IOverrideTheme
```

یک تم بازنویسی را نمایش می‌دهد.
## روش‌ها

| متد | توضیح |
| --- | --- |
| [initColorScheme()](#initColorScheme--) | راه‌اندازی ColorScheme با شی جدید برای بازنویسی ColorScheme از InheritedTheme. |
| [initColorSchemeFrom(IColorScheme colorScheme)](#initColorSchemeFrom-com.aspose.slides.IColorScheme-) | راه‌اندازی ColorScheme با شی جدید برای بازنویسی ColorScheme از InheritedTheme. |
| [initColorSchemeFromInherited()](#initColorSchemeFromInherited--) | راه‌اندازی ColorScheme با شی جدید برای بازنویسی ColorScheme از InheritedTheme. |
| [initFontScheme()](#initFontScheme--) | راه‌اندازی FontScheme با شی جدید برای بازنویسی FontScheme از InheritedTheme. |
| [initFontSchemeFrom(IFontScheme fontScheme)](#initFontSchemeFrom-com.aspose.slides.IFontScheme-) | راه‌اندازی FontScheme با شی جدید برای بازنویسی FontScheme از InheritedTheme. |
| [initFontSchemeFromInherited()](#initFontSchemeFromInherited--) | راه‌اندازی FontScheme با شی جدید برای بازنویسی FontScheme از InheritedTheme. |
| [initFormatScheme()](#initFormatScheme--) | راه‌اندازی FormatScheme با شی جدید برای بازنویسی FormatScheme از InheritedTheme. |
| [initFormatSchemeFrom(IFormatScheme formatScheme)](#initFormatSchemeFrom-com.aspose.slides.IFormatScheme-) | راه‌اندازی FormatScheme با شی جدید برای بازنویسی FormatScheme از InheritedTheme. |
| [initFormatSchemeFromInherited()](#initFormatSchemeFromInherited--) | راه‌اندازی FormatScheme با شی جدید برای بازنویسی FormatScheme از InheritedTheme. |
| [getColorScheme()](#getColorScheme--) | ColorScheme را باز می‌گرداند. |
| [getFontScheme()](#getFontScheme--) | FontScheme را باز می‌گرداند. |
| [getFormatScheme()](#getFormatScheme--) | FormatScheme را باز می‌گرداند. |
| [isEmpty()](#isEmpty--) | مقدار true به این معنی است که ColorScheme، FontScheme، FormatScheme مقدار null دارند و هرگونه بازنویسی با این شی تم غیرفعال است. |
| [clear()](#clear--) | برای غیرفعال کردن هرگونه بازنویسی با این شی تم، ColorScheme، FontScheme و FormatScheme را به null تنظیم کنید. |
| [getVersion()](#getVersion--) |  |
### initColorScheme() {#initColorScheme--}
```
public final void initColorScheme()
```

راه‌اندازی ColorScheme با شی جدید برای بازنویسی ColorScheme از InheritedTheme.

### initColorSchemeFrom(IColorScheme colorScheme) {#initColorSchemeFrom-com.aspose.slides.IColorScheme-}
```
public final void initColorSchemeFrom(IColorScheme colorScheme)
```

راه‌اندازی ColorScheme با شی جدید برای بازنویسی ColorScheme از InheritedTheme.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| colorScheme | [IColorScheme](../../com.aspose.slides/icolorscheme) | داده برای مقداردهی اولیه. |

### initColorSchemeFromInherited() {#initColorSchemeFromInherited--}
```
public final void initColorSchemeFromInherited()
```

راه‌اندازی ColorScheme با شی جدید برای بازنویسی ColorScheme از InheritedTheme. و مقداردهی داده‌های این شی جدید با داده‌های ColorScheme از InheritedTheme.

### initFontScheme() {#initFontScheme--}
```
public final void initFontScheme()
```

راه‌اندازی FontScheme با شی جدید برای بازنویسی FontScheme از InheritedTheme.

### initFontSchemeFrom(IFontScheme fontScheme) {#initFontSchemeFrom-com.aspose.slides.IFontScheme-}
```
public final void initFontSchemeFrom(IFontScheme fontScheme)
```

راه‌اندازی FontScheme با شی جدید برای بازنویسی FontScheme از InheritedTheme.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| fontScheme | [IFontScheme](../../com.aspose.slides/ifontscheme) | داده برای مقداردهی اولیه. |

### initFontSchemeFromInherited() {#initFontSchemeFromInherited--}
```
public final void initFontSchemeFromInherited()
```

راه‌اندازی FontScheme با شی جدید برای بازنویسی FontScheme از InheritedTheme. و مقداردهی داده‌های این شی جدید با داده‌های FontScheme از InheritedTheme.

### initFormatScheme() {#initFormatScheme--}
```
public final void initFormatScheme()
```

راه‌اندازی FormatScheme با شی جدید برای بازنویسی FormatScheme از InheritedTheme.

### initFormatSchemeFrom(IFormatScheme formatScheme) {#initFormatSchemeFrom-com.aspose.slides.IFormatScheme-}
```
public final void initFormatSchemeFrom(IFormatScheme formatScheme)
```

راه‌اندازی FormatScheme با شی جدید برای بازنویسی FormatScheme از InheritedTheme.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| formatScheme | [IFormatScheme](../../com.aspose.slides/iformatscheme) | داده برای مقداردهی اولیه. |

### initFormatSchemeFromInherited() {#initFormatSchemeFromInherited--}
```
public final void initFormatSchemeFromInherited()
```

راه‌اندازی FormatScheme با شی جدید برای بازنویسی FormatScheme از InheritedTheme. و مقداردهی داده‌های این شی جدید با داده‌های FormatScheme از InheritedTheme.

### getColorScheme() {#getColorScheme--}
```
public IColorScheme getColorScheme()
```

ColorScheme را باز می‌گرداند. فقط-خواندنی [IColorScheme](../../com.aspose.slides/icolorscheme).

**باز می‌گرداند:**
[IColorScheme](../../com.aspose.slides/icolorscheme)
### getFontScheme() {#getFontScheme--}
```
public IFontScheme getFontScheme()
```

FontScheme را باز می‌گرداند. فقط-خواندنی [IFontScheme](../../com.aspose.slides/ifontscheme).

**باز می‌گرداند:**
[IFontScheme](../../com.aspose.slides/ifontscheme)
### getFormatScheme() {#getFormatScheme--}
```
public IFormatScheme getFormatScheme()
```

FormatScheme را باز می‌گرداند. فقط-خواندنی [IFormatScheme](../../com.aspose.slides/iformatscheme).

**باز می‌گرداند:**
[IFormatScheme](../../com.aspose.slides/iformatscheme)
### isEmpty() {#isEmpty--}
```
public final boolean isEmpty()
```

مقدار true به این معنی است که ColorScheme، FontScheme، FormatScheme مقدار null دارند و هرگونه بازنویسی با این شی تم غیرفعال است. فقط-خواندنی boolean.

**باز می‌گرداند:**
boolean
### clear() {#clear--}
```
public final void clear()
```

برای غیرفعال کردن هرگونه بازنویسی با این شی تم، ColorScheme، FontScheme و FormatScheme را به null تنظیم کنید.

### getVersion() {#getVersion--}
```
public long getVersion()
```

نسخه. فقط-خواندنی long.

**باز می‌گرداند:**
long