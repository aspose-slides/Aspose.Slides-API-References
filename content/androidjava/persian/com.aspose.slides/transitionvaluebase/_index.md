---
title: TransitionValueBase
second_title: Aspose.Slides برای اندروید از طریق مرجع API جاوا
description: کلاس پایه برای افکت‌های انتقال اسلاید.
type: docs
url: /fa/com.aspose.slides/transitionvaluebase/
---
**ارث‌بری:**
java.lang.Object

**همه واسط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.ITransitionValueBase](../../com.aspose.slides/itransitionvaluebase)
```
public class TransitionValueBase implements ITransitionValueBase
```

کلاس پایه برای افکت‌های انتقال اسلاید.
## متدها

| متد | توضیحات |
| --- | --- |
| [equals(Object obj)](#equals-java.lang.Object-) | تعیین می‌کند که آیا دو نمونهٔ TransitionValueBase برابر هستند یا خیر. |
| [hashCode()](#hashCode--) | به عنوان یک تابع هش برای یک نوع خاص عمل می‌کند و برای استفاده در الگوریتم‌های هش و ساختارهای داده مانند جدول هش مناسب است. |
### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```


تعیین می‌کند که آیا دو نمونهٔ TransitionValueBase برابر هستند یا خیر.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| obj | java.lang.Object | TransitionValueBase برای مقایسه با TransitionValueBase جاری. |

**بازگرداندن:**
boolean - **true** اگر TransitionValueBase مشخص‌شده برابر با TransitionValueBase جاری باشد؛ در غیر این صورت، **false**.
### hashCode() {#hashCode--}
```
public int hashCode()
```


به عنوان یک تابع هش برای یک نوع خاص عمل می‌کند و برای استفاده در الگوریتم‌های هش و ساختارهای داده مانند جدول هش مناسب است.

**بازگرداندن:**
int - 23454

--------------------

تغییردهی شده برای خوشحال کردن کامپایلر. همیشه مقدار ثابت برمی‌گرداند زیرا شیء قابل تغییر است.