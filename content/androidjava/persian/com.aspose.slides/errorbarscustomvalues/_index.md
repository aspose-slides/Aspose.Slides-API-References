---
title: ErrorBarsCustomValues
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: مقادیر میله‌های خطا را مشخص می‌کند.
type: docs
url: /fa/com.aspose.slides/errorbarscustomvalues/
---
**ارث‌برداری:**  
java.lang.Object, com.aspose.slides.DomObject

**تمام رابط‌های پیاده‌سازی‌شده:**  
[com.aspose.slides.IErrorBarsCustomValues](../../com.aspose.slides/ierrorbarscustomvalues)  
```
public class ErrorBarsCustomValues extends DomObject<ChartDataPoint> implements IErrorBarsCustomValues
```

مقدارهای میله خطا را مشخص می‌کند. این فقط زمانی استفاده می‌شود که نوع مقدار میله‌های خطا سفارشی باشد.

## متدها

| متد | توضیح |
| --- | --- |
| [getXMinus()](#getXMinus--) | مقدار میله خطا را در جهت منفی مشخص می‌کند. |
| [getYMinus()](#getYMinus--) | مقدار میله خطا را در جهت منفی مشخص می‌کند. |
| [getXPlus()](#getXPlus--) | مقدار میله خطا را در جهت مثبت مشخص می‌کند. |
| [getYPlus()](#getYPlus--) | مقدار میله خطا را در جهت مثبت مشخص می‌کند. |

### getXMinus() {#getXMinus--}
```
public final IDoubleChartValue getXMinus()
```

مقدار میله خطا را در جهت منفی مشخص می‌کند. در صورتی که نوع مقدار میله‌های خطا سفارشی باشد و ErrorBarsXFormat مجاز باشد، در دسترس است. در هر حالت دیگر این ویژگی null برمی‌گرداند. فقط‌خواندنی [IDoubleChartValue](../../com.aspose.slides/idoublechartvalue).

**بازگشت:**  
[IDoubleChartValue](../../com.aspose.slides/idoublechartvalue)

### getYMinus() {#getYMinus--}
```
public final IDoubleChartValue getYMinus()
```

مقدار میله خطا را در جهت منفی مشخص می‌کند. در صورتی که نوع مقدار میله‌های خطا سفارشی باشد و ErrorBarsYFormat مجاز باشد، در دسترس است. در هر حالت دیگر این ویژگی null برمی‌گرداند. فقط‌خواندنی [IDoubleChartValue](../../com.aspose.slides/idoublechartvalue).

**بازگشت:**  
[IDoubleChartValue](../../com.aspose.slides/idoublechartvalue)

### getXPlus() {#getXPlus--}
```
public final IDoubleChartValue getXPlus()
```

مقدار میله خطا را در جهت مثبت مشخص می‌کند. در صورتی که نوع مقدار میله‌های خطا سفارشی باشد و ErrorBarsXFormat مجاز باشد، در دسترس است. در هر حالت دیگر این ویژگی null برمی‌گرداند. فقط‌خواندنی [IDoubleChartValue](../../com.aspose.slides/idoublechartvalue).

**بازگشت:**  
[IDoubleChartValue](../../com.aspose.slides/idoublechartvalue)

### getYPlus() {#getYPlus--}
```
public final IDoubleChartValue getYPlus()
```

مقدار میله خطا را در جهت مثبت مشخص می‌کند. در صورتی که نوع مقدار میله‌های خطا سفارشی باشد و ErrorBarsYFormat مجاز باشد، در دسترس است. در هر حالت دیگر این ویژگی null برمی‌گرداند. فقط‌خواندنی [IDoubleChartValue](../../com.aspose.slides/idoublechartvalue).

**بازگشت:**  
[IDoubleChartValue](../../com.aspose.slides/idoublechartvalue)