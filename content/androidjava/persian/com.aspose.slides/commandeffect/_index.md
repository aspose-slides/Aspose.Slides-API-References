---
title: CommandEffect
second_title: Aspose.Slides برای Android از طریق مرجع API جاوا
description: نمایانگر یک اثر فرمان برای رفتار انیمیشن است.
type: docs
url: /fa/com.aspose.slides/commandeffect/
---
**ارث‌بری:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.ICommandEffect](../../com.aspose.slides/icommandeffect)
```
public class CommandEffect extends Behavior implements ICommandEffect
```

نمایانگر یک اثر فرمان برای رفتار انیمیشن است.
## سازنده‌ها

| سازنده | توضیحات |
| --- | --- |
| [CommandEffect()](#CommandEffect--) | یک نمونه جدید ایجاد می‌کند. |
## متدها

| متد | توضیحات |
| --- | --- |
| [getType()](#getType--) | نوع اثر فرمان رفتار را تعریف می‌کند. |
| [setType(byte value)](#setType-byte-) | نوع اثر فرمان رفتار را تعریف می‌کند. |
| [getCommandString()](#getCommandString--) | رشته فرمان را تعریف می‌کند. |
| [setCommandString(String value)](#setCommandString-java.lang.String-) | رشته فرمان را تعریف می‌کند. |
| [getShapeTarget()](#getShapeTarget--) | هدف شکل اثر فرمان را تعریف می‌کند. |
| [setShapeTarget(IShape value)](#setShapeTarget-com.aspose.slides.IShape-) | هدف شکل اثر فرمان را تعریف می‌کند. |
### CommandEffect() {#CommandEffect--}
```
public CommandEffect()
```


یک نمونه جدید ایجاد می‌کند.

### getType() {#getType--}
```
public final byte getType()
```


نوع اثر فرمان رفتار را تعریف می‌کند. خواندنی/نوشتنی [CommandEffectType](../../com.aspose.slides/commandeffecttype).

**بازگشت:**
byte
### setType(byte value) {#setType-byte-}
```
public final void setType(byte value)
```


نوع اثر فرمان رفتار را تعریف می‌کند. خواندنی/نوشتنی [CommandEffectType](../../com.aspose.slides/commandeffecttype).

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | byte |  |

### getCommandString() {#getCommandString--}
```
public final String getCommandString()
```


رشته فرمان را تعریف می‌کند. خواندنی/نوشتنی String.

**بازگشت:**
java.lang.String
### setCommandString(String value) {#setCommandString-java.lang.String-}
```
public final void setCommandString(String value)
```


رشته فرمان را تعریف می‌کند. خواندنی/نوشتنی String.

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | java.lang.String |  |

### getShapeTarget() {#getShapeTarget--}
```
public final IShape getShapeTarget()
```


هدف شکل اثر فرمان را تعریف می‌کند. خواندنی/نوشتنی [IShape](../../com.aspose.slides/ishape).

**بازگشت:**
[IShape](../../com.aspose.slides/ishape)
### setShapeTarget(IShape value) {#setShapeTarget-com.aspose.slides.IShape-}
```
public final void setShapeTarget(IShape value)
```


هدف شکل اثر فرمان را تعریف می‌کند. خواندنی/نوشتنی [IShape](../../com.aspose.slides/ishape).

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | [IShape](../../com.aspose.slides/ishape) |  |