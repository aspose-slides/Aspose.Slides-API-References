---
title: OleEmbeddedDataInfo
second_title: Aspose.Slides untuk Android melalui Referensi API Java
description: Mewakili informasi data yang disematkan untuk objek OLE.
type: docs
url: /id/com.aspose.slides/oleembeddeddatainfo/
---
**Warisan:**
java.lang.Object

**Semua Antarmuka yang Diimplementasikan:**
[com.aspose.slides.IOleEmbeddedDataInfo](../../com.aspose.slides/ioleembeddeddatainfo)
```
public class OleEmbeddedDataInfo implements IOleEmbeddedDataInfo
```

Mewakili info data yang disematkan untuk objek OLE.

## Konstruktor

| Konstruktor | Deskripsi |
| --- | --- |
| [OleEmbeddedDataInfo()](#OleEmbeddedDataInfo--) | Membuat info data yang disematkan baru untuk objek OLE. |
| [OleEmbeddedDataInfo(byte[] embeddedFileData, String embeddedFileExtension)](#OleEmbeddedDataInfo-byte---java.lang.String-) | Membuat sebuah instance baru dari info data yang disematkan untuk objek OLE. |

## Metode

| Metode | Deskripsi |
| --- | --- |
| [getEmbeddedFileData()](#getEmbeddedFileData--) | Mengembalikan data file dari objek OLE yang disematkan Hanya baca byte[]. |
| [getEmbeddedFileExtension()](#getEmbeddedFileExtension--) | Mengembalikan ekstensi file untuk objek OLE yang disematkan saat ini Hanya baca String. |

### OleEmbeddedDataInfo() {#OleEmbeddedDataInfo--}
```
public OleEmbeddedDataInfo()
```

Membuat info data yang disematkan baru untuk objek OLE.

### OleEmbeddedDataInfo(byte[] embeddedFileData, String embeddedFileExtension) {#OleEmbeddedDataInfo-byte---java.lang.String-}
```
public OleEmbeddedDataInfo(byte[] embeddedFileData, String embeddedFileExtension)
```

Membuat sebuah instance baru dari info data yang disematkan untuk objek OLE.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| embeddedFileData | byte[] | Data file dari objek OLE yang disematkan byte[]. |
| embeddedFileExtension | java.lang.String | Ekstensi file untuk objek OLE yang disematkan saat ini String. |

### getEmbeddedFileData() {#getEmbeddedFileData--}
```
public final byte[] getEmbeddedFileData()
```

Mengembalikan data file dari objek OLE yang disematkan Hanya baca byte[].

**Mengembalikan:**
byte[]
### getEmbeddedFileExtension() {#getEmbeddedFileExtension--}
```
public final String getEmbeddedFileExtension()
```

Mengembalikan ekstensi file untuk objek OLE yang disematkan saat ini Hanya baca String.

**Mengembalikan:**
java.lang.String