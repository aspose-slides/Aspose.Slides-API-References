---
title: NormalViewRestoredProperties
second_title: Aspose.Slides untuk Android via Referensi API Java
description: Menentukan ukuran area slide (lebar ketika menjadi anak restoredTop, tinggi ketika menjadi anak restoredLeft) pada tampilan normal, ketika area tersebut memiliki ukuran restored yang variabel (tidak diminimalkan maupun dimaksimalkan).
type: docs
url: /id/com.aspose.slides/normalviewrestoredproperties/
---
**Warisan:**
java.lang.Object

**Semua Antarmuka yang Diimplementasikan:**
[com.aspose.slides.INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)
```
public class NormalViewRestoredProperties implements INormalViewRestoredProperties
```

Menentukan ukuran area slide ((lebar ketika menjadi anak dari restoredTop, tinggi ketika menjadi anak dari restoredLeft) pada tampilan normal, ketika area memiliki ukuran restored yang variabel (tidak diminimalkan maupun dimaksimalkan)).

## Metode

| Metode | Deskripsi |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | Menentukan ukuran area slide (lebar ketika menjadi anak RestoredTop, tinggi ketika menjadi anak RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | Menentukan ukuran area slide (lebar ketika menjadi anak RestoredTop, tinggi ketika menjadi anak RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | Menentukan apakah ukuran area konten samping harus menyesuaikan dengan ukuran baru saat mengubah ukuran jendela yang berisi tampilan dalam aplikasi Read/write boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | Menentukan apakah ukuran area konten samping harus menyesuaikan dengan ukuran baru saat mengubah ukuran jendela yang berisi tampilan dalam aplikasi Read/write boolean. |

### getDimensionSize() {#getDimensionSize--}
```
public final float getDimensionSize()
```

Menentukan ukuran area slide (lebar ketika menjadi anak RestoredTop, tinggi ketika menjadi anak RestoredLeft). Read/write float.

**Mengembalikan:**
float

### setDimensionSize(float value) {#setDimensionSize-float-}
```
public final void setDimensionSize(float value)
```

Menentukan ukuran area slide (lebar ketika menjadi anak RestoredTop, tinggi ketika menjadi anak RestoredLeft). Read/write float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public final boolean getAutoAdjust()
```

Menentukan apakah ukuran area konten samping harus menyesuaikan dengan ukuran baru saat mengubah ukuran jendela yang berisi tampilan dalam aplikasi Read/write boolean.

**Mengembalikan:**
boolean

### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public final void setAutoAdjust(boolean value)
```

Menentukan apakah ukuran area konten samping harus menyesuaikan dengan ukuran baru saat mengubah ukuran jendela yang berisi tampilan dalam aplikasi Read/write boolean.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | boolean |  |