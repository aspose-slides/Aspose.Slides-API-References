---
title: Timing
second_title: Aspose.Slides untuk Android via Referensi API Java
description: Mewakili timing animasi.
type: docs
url: /id/com.aspose.slides/timing/
---
**Pewarisan:**
java.lang.Object

**Semua Antarmuka yang Diimplementasikan:**
[com.aspose.slides.ITiming](../../com.aspose.slides/itiming), com.aspose.slides.IDOMObject
```
public class Timing implements ITiming, IDOMObject
```

Mewakili timing animasi.
## Metode

| Metode | Deskripsi |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | Menjelaskan persentase durasi efek perilaku percepatan. |
| [setAccelerate(float value)](#setAccelerate-float-) | Menjelaskan persentase durasi efek perilaku percepatan. |
| [getDecelerate()](#getDecelerate--) | Menjelaskan persentase durasi efek perilaku perlambatan. |
| [setDecelerate(float value)](#setDecelerate-float-) | Menjelaskan persentase durasi efek perilaku perlambatan. |
| [getAutoReverse()](#getAutoReverse--) | Menjelaskan apakah animasi secara otomatis diputar mundur setelah diputar maju. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | Menjelaskan apakah animasi secara otomatis diputar mundur setelah diputar maju. |
| [getDuration()](#getDuration--) | Menjelaskan durasi efek animasi. |
| [setDuration(float value)](#setDuration-float-) | Menjelaskan durasi efek animasi. |
| [getRepeatCount()](#getRepeatCount--) | Menjelaskan jumlah kali efek harus diulang. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | Menjelaskan jumlah kali efek harus diulang. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | Atribut ini menentukan apakah efek akan diulang sampai akhir slide. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | Atribut ini menentukan apakah efek akan diulang sampai akhir slide. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | Atribut ini menentukan apakah efek akan diulang sampai klik berikutnya. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | Atribut ini menentukan apakah efek akan diulang sampai klik berikutnya. |
| [getRepeatDuration()](#getRepeatDuration--) | Menjelaskan jumlah kali efek harus diulang. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | Menjelaskan jumlah kali efek harus diulang. |
| [getRestart()](#getRestart--) | Menentukan apakah efek harus dimulai ulang setelah selesai. |
| [setRestart(int value)](#setRestart-int-) | Menentukan apakah efek harus dimulai ulang setelah selesai. |
| [getRewind()](#getRewind--) | Atribut ini menentukan apakah efek akan diputar ulang ketika selesai diputar. |
| [setRewind(boolean value)](#setRewind-boolean-) | Atribut ini menentukan apakah efek akan diputar ulang ketika selesai diputar. |
| [getSpeed()](#getSpeed--) | Menentukan persentase percepatan (atau perlambatan) timing. |
| [setSpeed(float value)](#setSpeed-float-) | Menentukan persentase percepatan (atau perlambatan) timing. |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | Menjelaskan waktu tunda setelah pemicu. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | Menjelaskan waktu tunda setelah pemicu. |
| [getTriggerType()](#getTriggerType--) | Menjelaskan tipe pemicu. |
| [setTriggerType(int value)](#setTriggerType-int-) | Menjelaskan tipe pemicu. |
| [getParent_Immediate()](#getParent-Immediate--) |  |

### getAccelerate() {#getAccelerate--}
```
public final float getAccelerate()
```

Menjelaskan persentase durasi efek perilaku percepatan. Baca/tulis float.

**Mengembalikan:**
float

### setAccelerate(float value) {#setAccelerate-float-}
```
public final void setAccelerate(float value)
```

Menjelaskan persentase durasi efek perilaku percepatan. Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public final float getDecelerate()
```

Menjelaskan persentase durasi efek perilaku perlambatan. Baca/tulis float.

**Mengembalikan:**
float

### setDecelerate(float value) {#setDecelerate-float-}
```
public final void setDecelerate(float value)
```

Menjelaskan persentase durasi efek perilaku perlambatan. Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public final boolean getAutoReverse()
```

Menjelaskan apakah animasi secara otomatis diputar mundur setelah diputar maju. Baca/tulis boolean.

**Mengembalikan:**
boolean

### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public final void setAutoReverse(boolean value)
```

Menjelaskan apakah animasi secara otomatis diputar mundur setelah diputar maju. Baca/tulis boolean.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public final float getDuration()
```

Menjelaskan durasi efek animasi. Baca/tulis float.

**Mengembalikan:**
float

### setDuration(float value) {#setDuration-float-}
```
public final void setDuration(float value)
```

Menjelaskan durasi efek animasi. Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public final float getRepeatCount()
```

Menjelaskan jumlah kali efek harus diulang. Baca/tulis float.

**Mengembalikan:**
float

### setRepeatCount(float value) {#setRepeatCount-float-}
```
public final void setRepeatCount(float value)
```

Menjelaskan jumlah kali efek harus diulang. Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public final boolean getRepeatUntilEndSlide()
```

Atribut ini menentukan apakah efek akan diulang sampai akhir slide. Baca/tulis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Dapatkan urutan efek untuk slide pertama
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Dapatkan efek pertama dari urutan utama.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ubah Timing/Repeat efek menjadi "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Mengembalikan:**
boolean

### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public final void setRepeatUntilEndSlide(boolean value)
```

Atribut ini menentukan apakah efek akan diulang sampai akhir slide. Baca/tulis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Dapatkan urutan efek untuk slide pertama
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Dapatkan efek pertama dari urutan utama.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ubah Timing/Repeat efek menjadi "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public final boolean getRepeatUntilNextClick()
```

Atribut ini menentukan apakah efek akan diulang sampai klik berikutnya. Baca/tulis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Dapatkan urutan efek untuk slide pertama
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Dapatkan efek pertama dari urutan utama.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ubah Timing/Repeat efek menjadi "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Mengembalikan:**
boolean

### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public final void setRepeatUntilNextClick(boolean value)
```

Atribut ini menentukan apakah efek akan diulang sampai klik berikutnya. Baca/tulis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Dapatkan urutan efek untuk slide pertama
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Dapatkan efek pertama dari urutan utama.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ubah Timing/Repeat efek menjadi "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public final float getRepeatDuration()
```

Menjelaskan jumlah kali efek harus diulang. Baca/tulis float.

**Mengembalikan:**
float

### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public final void setRepeatDuration(float value)
```

Menjelaskan jumlah kali efek harus diulang. Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public final int getRestart()
```

Menentukan apakah efek harus dimulai ulang setelah selesai. Baca/tulis [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Mengembalikan:**
int

### setRestart(int value) {#setRestart-int-}
```
public final void setRestart(int value)
```

Menentukan apakah efek harus dimulai ulang setelah selesai. Baca/tulis [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public final boolean getRewind()
```

Atribut ini menentukan apakah efek akan diputar ulang ketika selesai diputar. Baca/tulis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Dapatkan urutan efek untuk slide pertama
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Dapatkan efek pertama dari urutan utama.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Aktifkan Timing/Rewind efek.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Mengembalikan:**
boolean

### setRewind(boolean value) {#setRewind-boolean-}
```
public final void setRewind(boolean value)
```

Atribut ini menentukan apakah efek akan diputar ulang ketika selesai diputar. Baca/tulis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Dapatkan urutan efek untuk slide pertama
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Dapatkan efek pertama dari urutan utama.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Aktifkan Timing/Rewind efek.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | boolean |  |

### getSpeed() {#getSpeed--}
```
public final float getSpeed()
```

Menentukan persentase percepatan (atau perlambatan) timing. Baca/tulis float.

**Mengembalikan:**
float

### setSpeed(float value) {#setSpeed-float-}
```
public final void setSpeed(float value)
```

Menentukan persentase percepatan (atau perlambatan) timing. Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public final float getTriggerDelayTime()
```

Menjelaskan waktu tunda setelah pemicu. Baca/tulis float.

**Mengembalikan:**
float

### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public final void setTriggerDelayTime(float value)
```

Menjelaskan waktu tunda setelah pemicu. Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public final int getTriggerType()
```

Menjelaskan tipe pemicu. Baca/tulis [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Mengembalikan:**
int

### setTriggerType(int value) {#setTriggerType-int-}
```
public final void setTriggerType(int value)
```

Menjelaskan tipe pemicu. Baca/tulis [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | int |  |

### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

Mengembalikan objek Parent_Immediate. Hanya-baca IDOMObject.

**Mengembalikan:**
com.aspose.slides.IDOMObject