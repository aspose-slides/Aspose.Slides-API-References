---
title: ITiming
second_title: Aspose.Slides for Android via Java API Reference
description: Mewakili timing animasi.
type: docs
url: /id/com.aspose.slides/itiming/
---```
public interface ITiming
```

Mewakili timing animasi.
## Metode

| Metode | Deskripsi |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | Menjelaskan persentase efek perilaku percepatan durasi. |
| [setAccelerate(float value)](#setAccelerate-float-) | Menjelaskan persentase efek perilaku percepatan durasi. |
| [getDecelerate()](#getDecelerate--) | Menjelaskan persentase efek perilaku perlambatan durasi. |
| [setDecelerate(float value)](#setDecelerate-float-) | Menjelaskan persentase efek perilaku perlambatan durasi. |
| [getAutoReverse()](#getAutoReverse--) | Menjelaskan apakah animasi secara otomatis diputar mundur setelah diputar maju. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | Menjelaskan apakah animasi secara otomatis diputar mundur setelah diputar maju. |
| [getDuration()](#getDuration--) | Menjelaskan durasi efek animasi. |
| [setDuration(float value)](#setDuration-float-) | Menjelaskan durasi efek animasi. |
| [getRepeatCount()](#getRepeatCount--) | Menjelaskan jumlah kali efek harus diulang. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | Menjelaskan jumlah kali efek harus diulang. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | Atribut ini menentukan apakah efek akan diulang hingga akhir slide. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | Atribut ini menentukan apakah efek akan diulang hingga akhir slide. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | Atribut ini menentukan apakah efek akan diulang hingga klik berikutnya. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | Atribut ini menentukan apakah efek akan diulang hingga klik berikutnya. |
| [getRepeatDuration()](#getRepeatDuration--) | Menjelaskan jumlah kali efek harus diulang. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | Menjelaskan jumlah kali efek harus diulang. |
| [getRestart()](#getRestart--) | Menentukan apakah efek harus dimulai kembali setelah selesai. |
| [setRestart(int value)](#setRestart-int-) | Menentukan apakah efek harus dimulai kembali setelah selesai. |
| [getSpeed()](#getSpeed--) | Menentukan persentase untuk mempercepat (atau memperlambat) timing. |
| [setSpeed(float value)](#setSpeed-float-) | Menentukan persentase untuk mempercepat (atau memperlambat) timing. |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | Menjelaskan waktu jeda setelah pemicu. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | Menjelaskan waktu jeda setelah pemicu. |
| [getTriggerType()](#getTriggerType--) | Menjelaskan jenis pemicu. |
| [setTriggerType(int value)](#setTriggerType-int-) | Menjelaskan jenis pemicu. |
| [getRewind()](#getRewind--) | Atribut ini menentukan apakah efek akan diputar kembali setelah selesai dimainkan. |
| [setRewind(boolean value)](#setRewind-boolean-) | Atribut ini menentukan apakah efek akan diputar kembali setelah selesai dimainkan. |
### getAccelerate() {#getAccelerate--}
```
public abstract float getAccelerate()
```

Menjelaskan persentase efek perilaku percepatan durasi. Baca/tulis float.

**Mengembalikan:**
float
### setAccelerate(float value) {#setAccelerate-float-}
```
public abstract void setAccelerate(float value)
```

Menjelaskan persentase efek perilaku percepatan durasi. Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |
### getDecelerate() {#getDecelerate--}
```
public abstract float getDecelerate()
```

Menjelaskan persentase efek perilaku perlambatan durasi. Baca/tulis float.

**Mengembalikan:**
float
### setDecelerate(float value) {#setDecelerate-float-}
```
public abstract void setDecelerate(float value)
```

Menjelaskan persentase efek perilaku perlambatan durasi. Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |
### getAutoReverse() {#getAutoReverse--}
```
public abstract boolean getAutoReverse()
```

Menjelaskan apakah animasi secara otomatis diputar mundur setelah diputar maju. Baca/tulis boolean.

**Mengembalikan:**
boolean
### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public abstract void setAutoReverse(boolean value)
```

Menjelaskan apakah animasi secara otomatis diputar mundur setelah diputar maju. Baca/tulis boolean.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | boolean |  |
### getDuration() {#getDuration--}
```
public abstract float getDuration()
```

Menjelaskan durasi efek animasi. Baca/tulis float.

**Mengembalikan:**
float
### setDuration(float value) {#setDuration-float-}
```
public abstract void setDuration(float value)
```

Menjelaskan durasi efek animasi. Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |
### getRepeatCount() {#getRepeatCount--}
```
public abstract float getRepeatCount()
```

Menjelaskan jumlah kali efek harus diulang. Baca/tulis float.

**Mengembalikan:**
float
### setRepeatCount(float value) {#setRepeatCount-float-}
```
public abstract void setRepeatCount(float value)
```

Menjelaskan jumlah kali efek harus diulang. Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |
### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public abstract boolean getRepeatUntilEndSlide()
```

Atribut ini menentukan apakah efek akan diulang hingga akhir slide. Baca/tulis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Dapatkan urutan efek untuk slide pertama
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Dapatkan efek pertama dari urutan utama.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ubah Timing/Ulangi efek menjadi "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Mengembalikan:**
boolean
### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public abstract void setRepeatUntilEndSlide(boolean value)
```

Atribut ini menentukan apakah efek akan diulang hingga akhir slide. Baca/tulis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Dapatkan urutan efek untuk slide pertama
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Dapatkan efek pertama dari urutan utama.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ubah Timing/Ulangi efek menjadi "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | boolean |  |
### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public abstract boolean getRepeatUntilNextClick()
```

Atribut ini menentukan apakah efek akan diulang hingga klik berikutnya. Baca/tulis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Dapatkan urutan efek untuk slide pertama
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Dapatkan efek pertama dari urutan utama.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ubah Timing/Ulangi efek menjadi "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Mengembalikan:**
boolean
### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public abstract void setRepeatUntilNextClick(boolean value)
```

Atribut ini menentukan apakah efek akan diulang hingga klik berikutnya. Baca/tulis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Dapatkan urutan efek untuk slide pertama
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Dapatkan efek pertama dari urutan utama.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ubah Timing/Ulangi efek menjadi "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | boolean |  |
### getRepeatDuration() {#getRepeatDuration--}
```
public abstract float getRepeatDuration()
```

Menjelaskan jumlah kali efek harus diulang. Baca/tulis float.

**Mengembalikan:**
float
### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public abstract void setRepeatDuration(float value)
```

Menjelaskan jumlah kali efek harus diulang. Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |
### getRestart() {#getRestart--}
```
public abstract int getRestart()
```

Menentukan apakah efek harus dimulai kembali setelah selesai. Baca/tulis [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Mengembalikan:**
int
### setRestart(int value) {#setRestart-int-}
```
public abstract void setRestart(int value)
```

Menentukan apakah efek harus dimulai kembali setelah selesai. Baca/tulis [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | int |  |
### getSpeed() {#getSpeed--}
```
public abstract float getSpeed()
```

Menentukan persentase untuk mempercepat (atau memperlambat) timing. Baca/tulis float.

**Mengembalikan:**
float
### setSpeed(float value) {#setSpeed-float-}
```
public abstract void setSpeed(float value)
```

Menentukan persentase untuk mempercepat (atau memperlambat) timing. Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |
### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public abstract float getTriggerDelayTime()
```

Menjelaskan waktu jeda setelah pemicu. Baca/tulis float.

**Mengembalikan:**
float
### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public abstract void setTriggerDelayTime(float value)
```

Menjelaskan waktu jeda setelah pemicu. Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |
### getTriggerType() {#getTriggerType--}
```
public abstract int getTriggerType()
```

Menjelaskan jenis pemicu. Baca/tulis [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Mengembalikan:**
int
### setTriggerType(int value) {#setTriggerType-int-}
```
public abstract void setTriggerType(int value)
```

Menjelaskan jenis pemicu. Baca/tulis [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | int |  |
### getRewind() {#getRewind--}
```
public abstract boolean getRewind()
```

Atribut ini menentukan apakah efek akan diputar kembali setelah selesai dimainkan. Baca/tulis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Dapatkan urutan efek untuk slide pertama
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Dapatkan efek pertama dari urutan utama.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Aktifkan Timing/Rewind efek.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Mengembalikan:**
boolean
### setRewind(boolean value) {#setRewind-boolean-}
```
public abstract void setRewind(boolean value)
```

Atribut ini menentukan apakah efek akan diputar kembali setelah selesai dimainkan. Baca/tulis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Dapatkan urutan efek untuk slide pertama
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Dapatkan efek pertama dari urutan utama.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Aktifkan Timing/Rewind efek.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | boolean |  |