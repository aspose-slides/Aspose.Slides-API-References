---
title: IScaleEffect
second_title: Aspose.Slides untuk Android via Referensi API Java
description: Mewakili efek skala animasi.
type: docs
url: /id/com.aspose.slides/iscaleeffect/
---
**Semua Antarmuka yang Diimplementasikan:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IScaleEffect extends IBehavior
```

Mewakili efek skala animasi.
## Metode

| Metode | Deskripsi |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Menentukan apakah konten harus diperbesar. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Menentukan apakah konten harus diperbesar. |
| [getFrom()](#getFrom--) | Menentukan koordinat x/y untuk memulai animasi (dalam persen). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | Menentukan koordinat x/y untuk memulai animasi (dalam persen). |
| [getTo()](#getTo--) | Menentukan lokasi target untuk efek skala animasi (dalam persen). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | Menentukan lokasi target untuk efek skala animasi (dalam persen). |
| [getBy()](#getBy--) | Menjelaskan nilai offset relatif untuk animasi (dalam persen). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | Menjelaskan nilai offset relatif untuk animasi (dalam persen). |
### getZoomContent() {#getZoomContent--}
```
public abstract byte getZoomContent()
```

Menentukan apakah konten harus diperbesar. Baca/tulis [NullableBool](../../com.aspose.slides/nullablebool).

**Mengembalikan:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public abstract void setZoomContent(byte value)
```

Menentukan apakah konten harus diperbesar. Baca/tulis [NullableBool](../../com.aspose.slides/nullablebool).

**Parameter:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | byte |  |
### getFrom() {#getFrom--}
```
public abstract PointF getFrom()
```

Menentukan koordinat x/y untuk memulai animasi (dalam persen). Baca/tulis android.graphics.PointF.

**Mengembalikan:**
android.graphics.PointF
### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public abstract void setFrom(PointF value)
```

Menentukan koordinat x/y untuk memulai animasi (dalam persen). Baca/tulis android.graphics.PointF.

**Parameter:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | android.graphics.PointF |  |
### getTo() {#getTo--}
```
public abstract PointF getTo()
```

Menentukan lokasi target untuk efek skala animasi (dalam persen). Baca/tulis android.graphics.PointF.

**Mengembalikan:**
android.graphics.PointF
### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public abstract void setTo(PointF value)
```

Menentukan lokasi target untuk efek skala animasi (dalam persen). Baca/tulis android.graphics.PointF.

**Parameter:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | android.graphics.PointF |  |
### getBy() {#getBy--}
```
public abstract PointF getBy()
```

Menjelaskan nilai offset relatif untuk animasi (dalam persen). Baca/tulis android.graphics.PointF.

**Mengembalikan:**
android.graphics.PointF
### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public abstract void setBy(PointF value)
```

Menjelaskan nilai offset relatif untuk animasi (dalam persen). Baca/tulis android.graphics.PointF.

**Parameter:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | android.graphics.PointF |  |