---
title: IPictureFillFormatEffectiveData
second_title: Aspose.Slides untuk Android via Referensi API Java
description: Objek tak dapat diubah yang berisi properti pengisian gambar.
type: docs
url: /id/com.aspose.slides/ipicturefillformateffectivedata/
---
**Semua Antarmuka yang Diimplementasikan:**
[com.aspose.slides.IFillParamSource](../../com.aspose.slides/ifillparamsource)
```
public interface IPictureFillFormatEffectiveData extends IFillParamSource
```

Objek tak dapat diubah yang berisi properti pengisian gambar.

--------------------

Antarmuka ini digunakan sebagai bagian dari [IFillFormatEffectiveData](../../com.aspose.slides/ifillformateffectivedata).
## Metode

| Metode | Deskripsi |
| --- | --- |
| [getDpi()](#getDpi--) | Mengembalikan dpi yang digunakan untuk mengisi gambar. |
| [getPictureFillMode()](#getPictureFillMode--) | Mengembalikan mode pengisian gambar. |
| [getPicture()](#getPicture--) | Mengembalikan gambar. |
| [getCropLeft()](#getCropLeft--) | Mengembalikan persentase lebar gambar asli yang dipotong dari sisi kiri gambar. |
| [getCropTop()](#getCropTop--) | Mengembalikan persentase tinggi gambar asli yang dipotong dari bagian atas gambar. |
| [getCropRight()](#getCropRight--) | Mengembalikan persentase lebar gambar asli yang dipotong dari sisi kanan gambar. |
| [getCropBottom()](#getCropBottom--) | Mengembalikan persentase tinggi gambar asli yang dipotong dari bagian bawah gambar. |
### getDpi() {#getDpi--}
```
public abstract int getDpi()
```


Mengembalikan dpi yang digunakan untuk mengisi gambar. Hanya-baca int.

**Mengembalikan:**
int
### getPictureFillMode() {#getPictureFillMode--}
```
public abstract int getPictureFillMode()
```


Mengembalikan mode pengisian gambar. Hanya-baca [PictureFillMode](../../com.aspose.slides/picturefillmode).

**Mengembalikan:**
int
### getPicture() {#getPicture--}
```
public abstract IPictureEffectiveData getPicture()
```


Mengembalikan gambar. Hanya-baca [IPictureEffectiveData](../../com.aspose.slides/ipictureeffectivedata).

**Mengembalikan:**
[IPictureEffectiveData](../../com.aspose.slides/ipictureeffectivedata)
### getCropLeft() {#getCropLeft--}
```
public abstract float getCropLeft()
```


Mengembalikan persentase lebar gambar asli yang dipotong dari sisi kiri gambar. Hanya-baca float.

**Mengembalikan:**
float
### getCropTop() {#getCropTop--}
```
public abstract float getCropTop()
```


Mengembalikan persentase tinggi gambar asli yang dipotong dari bagian atas gambar. Hanya-baca float.

**Mengembalikan:**
float
### getCropRight() {#getCropRight--}
```
public abstract float getCropRight()
```


Mengembalikan persentase lebar gambar asli yang dipotong dari sisi kanan gambar. Hanya-baca float.

**Mengembalikan:**
float
### getCropBottom() {#getCropBottom--}
```
public abstract float getCropBottom()
```


Mengembalikan persentase tinggi gambar asli yang dipotong dari bagian bawah gambar. Hanya-baca float.

**Mengembalikan:**
float