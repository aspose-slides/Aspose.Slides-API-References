---
title: LegendDataLabelPosition
second_title: Aspose.Slides untuk Android melalui Referensi API Java
description: Menentukan posisi label data.
type: docs
url: /id/com.aspose.slides/legenddatalabelposition/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class LegendDataLabelPosition extends System.Enum
```

Menentukan posisi label data.
## Fields

| Field | Description |
| --- | --- |
| [NotDefined](#NotDefined) | Posisi tidak ditentukan. |
| [Bottom](#Bottom) | Menentukan bahwa label data akan ditampilkan di bawah penanda data. |
| [BestFit](#BestFit) | Menentukan bahwa label data akan ditampilkan pada posisi terbaik. |
| [Center](#Center) | Menentukan bahwa label data akan ditampilkan di tengah penanda data. |
| [InsideBase](#InsideBase) | Menentukan bahwa label data akan ditampilkan di dalam dasar penanda data. |
| [InsideEnd](#InsideEnd) | Menentukan bahwa label data akan ditampilkan di dalam ujung penanda data. |
| [Left](#Left) | Menentukan bahwa label data akan ditampilkan di sebelah kiri penanda data. |
| [OutsideEnd](#OutsideEnd) | Menentukan bahwa label data akan ditampilkan di luar ujung penanda data. |
| [Right](#Right) | Menentukan bahwa label data akan ditampilkan di sebelah kanan penanda data. |
| [Top](#Top) | Menentukan bahwa label data akan ditampilkan di atas penanda data. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```

Posisi tidak ditentukan.

### Bottom {#Bottom}
```
public static final int Bottom
```

Menentukan bahwa label data akan ditampilkan di bawah penanda data.

### BestFit {#BestFit}
```
public static final int BestFit
```

Menentukan bahwa label data akan ditampilkan pada posisi terbaik.

### Center {#Center}
```
public static final int Center
```

Menentukan bahwa label data akan ditampilkan di tengah penanda data.

### InsideBase {#InsideBase}
```
public static final int InsideBase
```

Menentukan bahwa label data akan ditampilkan di dalam dasar penanda data.

### InsideEnd {#InsideEnd}
```
public static final int InsideEnd
```

Menentukan bahwa label data akan ditampilkan di dalam ujung penanda data.

### Left {#Left}
```
public static final int Left
```

Menentukan bahwa label data akan ditampilkan di sebelah kiri penanda data.

### OutsideEnd {#OutsideEnd}
```
public static final int OutsideEnd
```

Menentukan bahwa label data akan ditampilkan di luar ujung penanda data.

### Right {#Right}
```
public static final int Right
```

Menentukan bahwa label data akan ditampilkan di sebelah kanan penanda data.

### Top {#Top}
```
public static final int Top
```

Menentukan bahwa label data akan ditampilkan di atas penanda data.