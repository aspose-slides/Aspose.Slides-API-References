---
title: FormatScheme
second_title: Aspose.Slides untuk Android via Referensi API Java
description: Menyimpan format yang didefinisikan tema untuk shapes.
type: docs
url: /id/com.aspose.slides/formatscheme/
---
**Pewarisan:**
java.lang.Object

**Semua Antarmuka yang Diimplementasikan:**
[com.aspose.slides.IFormatScheme](../../com.aspose.slides/iformatscheme), com.aspose.slides.IDOMObject
```
public class FormatScheme implements IFormatScheme, IDOMObject
```

Menyimpan format yang didefinisikan tema untuk shapes.
## Metode

| Metode | Deskripsi |
| --- | --- |
| [getFillStyles()](#getFillStyles--) | Mengembalikan koleksi gaya isi yang didefinisikan tema. |
| [getLineStyles()](#getLineStyles--) | Mengembalikan koleksi gaya garis yang didefinisikan tema. |
| [getEffectStyles()](#getEffectStyles--) | Mengembalikan koleksi gaya efek yang didefinisikan tema. |
| [getBackgroundFillStyles()](#getBackgroundFillStyles--) | Mengembalikan koleksi gaya isian latar belakang yang didefinisikan tema. |
| [getSlide()](#getSlide--) | Mengembalikan slide induk. |
| [getPresentation()](#getPresentation--) | Mengembalikan presentasi induk. |
| [getParent_Immediate()](#getParent-Immediate--) |  |
### getFillStyles() {#getFillStyles--}
```
public final IFillFormatCollection getFillStyles()
```

Mengembalikan koleksi gaya isi yang didefinisikan tema. Hanya-baca [IFillFormatCollection](../../com.aspose.slides/ifillformatcollection).

**Mengembalikan:**
[IFillFormatCollection](../../com.aspose.slides/ifillformatcollection)
### getLineStyles() {#getLineStyles--}
```
public final ILineFormatCollection getLineStyles()
```

Mengembalikan koleksi gaya garis yang didefinisikan tema. Hanya-baca [ILineFormatCollection](../../com.aspose.slides/ilineformatcollection).

**Mengembalikan:**
[ILineFormatCollection](../../com.aspose.slides/ilineformatcollection)
### getEffectStyles() {#getEffectStyles--}
```
public final IEffectStyleCollection getEffectStyles()
```

Mengembalikan koleksi gaya efek yang didefinisikan tema. Hanya-baca [IEffectStyleCollection](../../com.aspose.slides/ieffectstylecollection).

**Mengembalikan:**
[IEffectStyleCollection](../../com.aspose.slides/ieffectstylecollection)
### getBackgroundFillStyles() {#getBackgroundFillStyles--}
```
public final IFillFormatCollection getBackgroundFillStyles()
```

Mengembalikan koleksi gaya isian latar belakang yang didefinisikan tema. Hanya-baca [IFillFormatCollection](../../com.aspose.slides/ifillformatcollection).

**Mengembalikan:**
[IFillFormatCollection](../../com.aspose.slides/ifillformatcollection)
### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

Mengembalikan slide induk. Hanya-baca [IBaseSlide](../../com.aspose.slides/ibaseslide).

**Mengembalikan:**
[IBaseSlide](../../com.aspose.slides/ibaseslide)
### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

Mengembalikan presentasi induk. Hanya-baca [IPresentation](../../com.aspose.slides/ipresentation).

**Mengembalikan:**
[IPresentation](../../com.aspose.slides/ipresentation)
### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

Mengembalikan objek Parent_Immediate. Hanya-baca IDOMObject.

**Mengembalikan:**
com.aspose.slides.IDOMObject