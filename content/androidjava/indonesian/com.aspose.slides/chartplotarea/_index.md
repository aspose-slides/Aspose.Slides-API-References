---
title: ChartPlotArea
second_title: Aspose.Slides untuk Android melalui Referensi API Java
description: Merepresentasikan persegi panjang tempat diagram harus dipetakan.
type: docs
url: /id/com.aspose.slides/chartplotarea/
---
**Pewarisan:**
java.lang.Object, com.aspose.slides.DomObject

**Semua Antarmuka yang Diimplementasikan:**
[com.aspose.slides.IChartPlotArea](../../com.aspose.slides/ichartplotarea)
```
public class ChartPlotArea extends DomObject<Chart> implements IChartPlotArea
```

Mewakili persegi panjang tempat diagram harus dipetakan.
## Metode

| Metode | Deskripsi |
| --- | --- |
| [getFormat()](#getFormat--) | Mengembalikan format area plot. |
| [getX()](#getX--) | Mengembalikan atau mengatur koordinat x dari sudut kiri atas kotak pembatas area plot sebagai fraksi lebar chart (dari 0 sampai 1). |
| [setX(float value)](#setX-float-) | Mengembalikan atau mengatur koordinat x dari sudut kiri atas kotak pembatas area plot sebagai fraksi lebar chart (dari 0 sampai 1). |
| [getY()](#getY--) | Mengembalikan atau mengatur koordinat y dari sudut kiri atas kotak pembatas area plot sebagai fraksi tinggi chart (dari 0 sampai 1). |
| [setY(float value)](#setY-float-) | Mengembalikan atau mengatur koordinat y dari sudut kiri atas kotak pembatas area plot sebagai fraksi tinggi chart (dari 0 sampai 1). |
| [getWidth()](#getWidth--) | Mengembalikan atau mengatur lebar kotak pembatas area plot sebagai fraksi lebar chart (dari 0 sampai 1). |
| [setWidth(float value)](#setWidth-float-) | Mengembalikan atau mengatur lebar kotak pembatas area plot sebagai fraksi lebar chart (dari 0 sampai 1). |
| [getHeight()](#getHeight--) | Mengembalikan atau mengatur tinggi kotak pembatas area plot sebagai fraksi tinggi chart (dari 0 sampai 1). |
| [setHeight(float value)](#setHeight-float-) | Mengembalikan atau mengatur tinggi kotak pembatas area plot sebagai fraksi tinggi chart (dari 0 sampai 1). |
| [getRight()](#getRight--) | Kanan. |
| [getBottom()](#getBottom--) | Bawah. |
| [getChart()](#getChart--) | Diagram. |
| [isLocationAutocalculated()](#isLocationAutocalculated--) | Mendefinisikan bagaimana lokasi harus dihitung: true \\u2013 dihitung secara otomatis; ditentukan oleh properti X, Y, Width, Height. |
| [getLayoutTargetType()](#getLayoutTargetType--) | Jika tata letak area plot ditentukan secara manual, properti ini menentukan apakah menata area plot dari dalam (tidak termasuk sumbu dan label sumbu) atau dari luar (termasuk sumbu dan label sumbu). |
| [setLayoutTargetType(int value)](#setLayoutTargetType-int-) | Jika tata letak area plot ditentukan secara manual, properti ini menentukan apakah menata area plot dari dalam (tidak termasuk sumbu dan label sumbu) atau dari luar (termasuk sumbu dan label sumbu). |
| [getActualX()](#getActualX--) | Menentukan lokasi x aktual (kiri) dari elemen diagram relatif terhadap pojok kiri atas diagram. |
| [getActualY()](#getActualY--) | Menentukan posisi atas aktual dari elemen diagram relatif terhadap pojok kiri atas diagram. |
| [getActualWidth()](#getActualWidth--) | Menentukan lebar aktual elemen diagram. |
| [getActualHeight()](#getActualHeight--) | Menentukan tinggi aktual elemen diagram. |
| [getSlide()](#getSlide--) | Mengembalikan slide induk dari FillFormat. |
| [getPresentation()](#getPresentation--) | Mengembalikan presentasi induk dari FillFormat. |

### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

Mengembalikan format area plot. Hanya-baca [IFormat](../../com.aspose.slides/iformat).

**Mengembalikan:**
[IFormat](../../com.aspose.slides/iformat)
### getX() {#getX--}
```
public final float getX()
```

Mengembalikan atau mengatur koordinat x dari sudut kiri atas kotak pembatas area plot sebagai fraksi lebar chart (dari 0 sampai 1). Baca/tulis float.

**Mengembalikan:**
float
### setX(float value) {#setX-float-}
```
public final void setX(float value)
```

Mengembalikan atau mengatur koordinat x dari sudut kiri atas kotak pembatas area plot sebagai fraksi lebar chart (dari 0 sampai 1). Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |
### getY() {#getY--}
```
public final float getY()
```

Mengembalikan atau mengatur koordinat y dari sudut kiri atas kotak pembatas area plot sebagai fraksi tinggi chart (dari 0 sampai 1). Baca/tulis float.

**Mengembalikan:**
float
### setY(float value) {#setY-float-}
```
public final void setY(float value)
```

Mengembalikan atau mengatur koordinat y dari sudut kiri atas kotak pembatas area plot sebagai fraksi tinggi chart (dari 0 sampai 1). Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |
### getWidth() {#getWidth--}
```
public final float getWidth()
```

Mengembalikan atau mengatur lebar kotak pembatas area plot sebagai fraksi lebar chart (dari 0 sampai 1). Baca/tulis float.

**Mengembalikan:**
float
### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```

Mengembalikan atau mengatur lebar kotak pembatas area plot sebagai fraksi lebar chart (dari 0 sampai 1). Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |
### getHeight() {#getHeight--}
```
public final float getHeight()
```

Mengembalikan atau mengatur tinggi kotak pembatas area plot sebagai fraksi tinggi chart (dari 0 sampai 1). Baca/tulis float.

**Mengembalikan:**
float
### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```

Mengembalikan atau mengatur tinggi kotak pembatas area plot sebagai fraksi tinggi chart (dari 0 sampai 1). Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |
### getRight() {#getRight--}
```
public final float getRight()
```

Kanan. Hanya-baca float.

**Mengembalikan:**
float
### getBottom() {#getBottom--}
```
public final float getBottom()
```

Bawah. Hanya-baca float.

**Mengembalikan:**
float
### getChart() {#getChart--}
```
public final IChart getChart()
```

Diagram. Hanya-baca [IChart](../../com.aspose.slides/ichart).

**Mengembalikan:**
[IChart](../../com.aspose.slides/ichart)
### isLocationAutocalculated() {#isLocationAutocalculated--}
```
public final boolean isLocationAutocalculated()
```

Mendefinisikan bagaimana lokasi harus dihitung: true \\u2013 dihitung secara otomatis; ditentukan oleh properti X, Y, Width, Height. Hanya-baca boolean.

**Mengembalikan:**
boolean
### getLayoutTargetType() {#getLayoutTargetType--}
```
public final int getLayoutTargetType()
```

Jika tata letak area plot ditentukan secara manual, properti ini menentukan apakah menata area plot dari dalam (tidak termasuk sumbu dan label sumbu) atau dari luar (termasuk sumbu dan label sumbu). Baca/tulis [LayoutTargetType](../../com.aspose.slides/layouttargettype)(\#getLayoutTargetType.getLayoutTargetType/\#setLayoutTargetType(int).setLayoutTargetType(int)).

--------------------

> ```
> Presentation presentation = new Presentation();
>   try
>   {
>       ISlide slide = presentation.getSlides().get_Item(0);
>       IChart chart = slide.getShapes().addChart(ChartType.ClusteredColumn, 20, 100, 600, 400);
>       chart.getPlotArea().setX(0.2f);
>       chart.getPlotArea().setY(0.2f);
>       chart.getPlotArea().setWidth(0.7f);
>       chart.getPlotArea().setHeight(0.7f);
>       chart.getPlotArea().setLayoutTargetType(LayoutTargetType.Inner);
>       ...
>   } finally {
>       if (presentation != null) presentation.dispose();
>   }
> ```

**Mengembalikan:**
int
### setLayoutTargetType(int value) {#setLayoutTargetType-int-}
```
public final void setLayoutTargetType(int value)
```

Jika tata letak area plot ditentukan secara manual, properti ini menentukan apakah menata area plot dari dalam (tidak termasuk sumbu dan label sumbu) atau dari luar (termasuk sumbu dan label sumbu). Baca/tulis [LayoutTargetType](../../com.aspose.slides/layouttargettype)(\#getLayoutTargetType.getLayoutTargetType/\#setLayoutTargetType(int).setLayoutTargetType(int)).

--------------------

> ```
> Presentation presentation = new Presentation();
>   try
>   {
>       ISlide slide = presentation.getSlides().get_Item(0);
>       IChart chart = slide.getShapes().addChart(ChartType.ClusteredColumn, 20, 100, 600, 400);
>       chart.getPlotArea().setX(0.2f);
>       chart.getPlotArea().setY(0.2f);
>       chart.getPlotArea().setWidth(0.7f);
>       chart.getPlotArea().setHeight(0.7f);
>       chart.getPlotArea().setLayoutTargetType(LayoutTargetType.Inner);
>       ...
>   } finally {
>       if (presentation != null) presentation.dispose();
>   }
> ```

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | int |  |
### getActualX() {#getActualX--}
```
public final float getActualX()
```

Menentukan lokasi x aktual (kiri) dari elemen diagram relatif terhadap pojok kiri atas diagram. Panggil metode IChart.ValidateChartLayout() terlebih dahulu untuk mendapatkan nilai aktual. Baca float.

**Mengembalikan:**
float
### getActualY() {#getActualY--}
```
public final float getActualY()
```

Menentukan posisi atas aktual dari elemen diagram relatif terhadap pojok kiri atas diagram. Panggil metode IChart.ValidateChartLayout() terlebih dahulu untuk mendapatkan nilai aktual. Baca float.

**Mengembalikan:**
float
### getActualWidth() {#getActualWidth--}
```
public final float getActualWidth()
```

Menentukan lebar aktual elemen diagram. Panggil metode IChart.ValidateChartLayout() terlebih dahulu untuk mendapatkan nilai aktual. Baca float.

**Mengembalikan:**
float
### getActualHeight() {#getActualHeight--}
```
public final float getActualHeight()
```

Menentukan tinggi aktual elemen diagram. Panggil metode IChart.ValidateChartLayout() terlebih dahulu untuk mendapatkan nilai aktual. Baca float.

**Mengembalikan:**
float
### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

Mengembalikan slide induk dari FillFormat. Hanya-baca [BaseSlide](../../com.aspose.slides/baseslide).

**Mengembalikan:**
[IBaseSlide](../../com.aspose.slides/ibaseslide)
### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

Mengembalikan presentasi induk dari FillFormat. Hanya-baca [IPresentation](../../com.aspose.slides/ipresentation).

**Mengembalikan:**
[IPresentation](../../com.aspose.slides/ipresentation)