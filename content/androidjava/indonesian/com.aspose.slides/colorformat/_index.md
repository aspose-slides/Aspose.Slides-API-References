---
title: ColorFormat
second_title: Referensi API Java Aspose.Slides untuk Android
description: Mewakili warna yang digunakan dalam presentasi.
type: docs
url: /id/com.aspose.slides/colorformat/
---
**Pewarisan:**
java.lang.Object, [com.aspose.slides.PVIObject](../../com.aspose.slides/pviobject)

**Semua Antarmuka yang Diimplementasikan:**
[com.aspose.slides.IColorFormat](../../com.aspose.slides/icolorformat)
```
public final class ColorFormat extends PVIObject implements IColorFormat
```

Mewakili warna yang digunakan dalam presentasi.
## Metode

| Metode | Deskripsi |
| --- | --- |
| [getColorType()](#getColorType--) | Mengembalikan atau menetapkan metode definisi warna. |
| [setColorType(int value)](#setColorType-int-) | Mengembalikan atau menetapkan metode definisi warna. |
| [getColor()](#getColor--) | Mengembalikan warna hasil (dengan semua transformasi warna diterapkan). |
| [setColor(Integer value)](#setColor-java.lang.Integer-) | Mengembalikan warna hasil (dengan semua transformasi warna diterapkan). |
| [getPresetColor()](#getPresetColor--) | Mengembalikan atau menetapkan preset warna. |
| [setPresetColor(int value)](#setPresetColor-int-) | Mengembalikan atau menetapkan preset warna. |
| [getSystemColor()](#getSystemColor--) | Mengembalikan atau menetapkan warna yang diidentifikasi oleh tabel warna sistem. |
| [setSystemColor(int value)](#setSystemColor-int-) | Mengembalikan atau menetapkan warna yang diidentifikasi oleh tabel warna sistem. |
| [getSchemeColor()](#getSchemeColor--) | Mengembalikan atau menetapkan warna yang diidentifikasi oleh skema warna. |
| [setSchemeColor(int value)](#setSchemeColor-int-) | Mengembalikan atau menetapkan warna yang diidentifikasi oleh skema warna. |
| [getR()](#getR--) | Mengembalikan atau menetapkan komponen merah sebuah warna. |
| [setR(byte value)](#setR-byte-) | Mengembalikan atau menetapkan komponen merah sebuah warna. |
| [getG()](#getG--) | Mengembalikan atau menetapkan komponen hijau sebuah warna. |
| [setG(byte value)](#setG-byte-) | Mengembalikan atau menetapkan komponen hijau sebuah warna. |
| [getB()](#getB--) | Mengembalikan atau menetapkan komponen biru sebuah warna. |
| [setB(byte value)](#setB-byte-) | Mengembalikan atau menetapkan komponen biru sebuah warna. |
| [getFloatR()](#getFloatR--) | Mengembalikan atau menetapkan komponen merah sebuah warna. |
| [setFloatR(float value)](#setFloatR-float-) | Mengembalikan atau menetapkan komponen merah sebuah warna. |
| [getFloatG()](#getFloatG--) | Mengembalikan atau menetapkan komponen hijau sebuah warna. |
| [setFloatG(float value)](#setFloatG-float-) | Mengembalikan atau menetapkan komponen hijau sebuah warna. |
| [getFloatB()](#getFloatB--) | Mengembalikan atau menetapkan komponen biru sebuah warna. |
| [setFloatB(float value)](#setFloatB-float-) | Mengembalikan atau menetapkan komponen biru sebuah warna. |
| [getHue()](#getHue--) | Mengembalikan atau menetapkan komponen hue sebuah warna dalam representasi HSL. |
| [setHue(float value)](#setHue-float-) | Mengembalikan atau menetapkan komponen hue sebuah warna dalam representasi HSL. |
| [getSaturation()](#getSaturation--) | Mengembalikan atau menetapkan komponen saturasi sebuah warna dalam representasi HSL. |
| [setSaturation(float value)](#setSaturation-float-) | Mengembalikan atau menetapkan komponen saturasi sebuah warna dalam representasi HSL. |
| [getLuminance()](#getLuminance--) | Mengembalikan atau menetapkan komponen luminansi sebuah warna dalam representasi HSL. |
| [setLuminance(float value)](#setLuminance-float-) | Mengembalikan atau menetapkan komponen luminansi sebuah warna dalam representasi HSL. |
| [getColorTransform()](#getColorTransform--) | Mengembalikan koleksi transformasi warna yang diterapkan pada sebuah warna. |
| [toString(int format)](#toString-int-) | Mengembalikan String yang mewakili format warna saat ini. |
| [copyFrom(IColorFormat color)](#copyFrom-com.aspose.slides.IColorFormat-) | Menyalin format warna dari "color". |
| [equals(Object obj)](#equals-java.lang.Object-) | Memeriksa kesamaan dengan objek yang ditentukan. |
| [hashCode()](#hashCode--) | Mengembalikan kode hash. |
| [getVersion()](#getVersion--) |  |
| [getParent_ISlideComponent()](#getParent-ISlideComponent--) |  |
| [getParent_IPresentationComponent()](#getParent-IPresentationComponent--) |  |

### getColorType() {#getColorType--}
```
public final int getColorType()
```

Mengembalikan atau menetapkan metode definisi warna. Baca/tulis [ColorType](../../com.aspose.slides/colortype).

**Mengembalikan:**
int

### setColorType(int value) {#setColorType-int-}
```
public final void setColorType(int value)
```

Mengembalikan atau menetapkan metode definisi warna. Baca/tulis [ColorType](../../com.aspose.slides/colortype).

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | int |  |

### getColor() {#getColor--}
```
public final Integer getColor()
```

Mengembalikan warna hasil (dengan semua transformasi warna diterapkan). Menetapkan warna RGB dan menghapus semua transformasi warna. Baca/tulis java.lang.Integer.

**Mengembalikan:**
java.lang.Integer

### setColor(java.lang.Integer value) {#setColor-java.lang.Integer-}
```
public final void setColor(Integer value)
```

Mengembalikan warna hasil (dengan semua transformasi warna diterapkan). Menetapkan warna RGB dan menghapus semua transformasi warna. Baca/tulis java.lang.Integer.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | java.lang.Integer |  |

### getPresetColor() {#getPresetColor--}
```
public final int getPresetColor()
```

Mengembalikan atau menetapkan preset warna. Baca/tulis [PresetColor](../../com.aspose.slides/presetcolor).

**Mengembalikan:**
int

### setPresetColor(int value) {#setPresetColor-int-}
```
public final void setPresetColor(int value)
```

Mengembalikan atau menetapkan preset warna. Baca/tulis [PresetColor](../../com.aspose.slides/presetcolor).

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | int |  |

### getSystemColor() {#getSystemColor--}
```
public final int getSystemColor()
```

Mengembalikan atau menetapkan warna yang diidentifikasi oleh tabel warna sistem. Baca/tulis [SystemColor](../../com.aspose.slides/systemcolor).

**Mengembalikan:**
int

### setSystemColor(int value) {#setSystemColor-int-}
```
public final void setSystemColor(int value)
```

Mengembalikan atau menetapkan warna yang diidentifikasi oleh tabel warna sistem. Baca/tulis [SystemColor](../../com.aspose.slides/systemcolor).

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | int |  |

### getSchemeColor() {#getSchemeColor--}
```
public final int getSchemeColor()
```

Mengembalikan atau menetapkan warna yang diidentifikasi oleh skema warna. Baca/tulis [SchemeColor](../../com.aspose.slides/schemecolor).

**Mengembalikan:**
int

### setSchemeColor(int value) {#setSchemeColor-int-}
```
public final void setSchemeColor(int value)
```

Mengembalikan atau menetapkan warna yang diidentifikasi oleh skema warna. Baca/tulis [SchemeColor](../../com.aspose.slides/schemecolor).

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | int |  |

### getR() {#getR--}
```
public final byte getR()
```

Mengembalikan atau menetapkan komponen merah sebuah warna. Semua transformasi warna diabaikan. Baca/tulis byte.

**Mengembalikan:**
byte

### setR(byte value) {#setR-byte-}
```
public final void setR(byte value)
```

Mengembalikan atau menetapkan komponen merah sebuah warna. Semua transformasi warna diabaikan. Baca/tulis byte.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | byte |  |

### getG() {#getG--}
```
public final byte getG()
```

Mengembalikan atau menetapkan komponen hijau sebuah warna. Semua transformasi warna diabaikan.

**Mengembalikan:**
byte

### setG(byte value) {#setG-byte-}
```
public final void setG(byte value)
```

Mengembalikan atau menetapkan komponen hijau sebuah warna. Semua transformasi warna diabaikan.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | byte |  |

### getB() {#getB--}
```
public final byte getB()
```

Mengembalikan atau menetapkan komponen biru sebuah warna. Semua transformasi warna diabaikan. Baca/tulis byte.

**Mengembalikan:**
byte

### setB(byte value) {#setB-byte-}
```
public final void setB(byte value)
```

Mengembalikan atau menetapkan komponen biru sebuah warna. Semua transformasi warna diabaikan. Baca/tulis byte.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | byte |  |

### getFloatR() {#getFloatR--}
```
public final float getFloatR()
```

Mengembalikan atau menetapkan komponen merah sebuah warna. Semua transformasi warna diabaikan. Baca/tulis float.

**Mengembalikan:**
float

### setFloatR(float value) {#setFloatR-float-}
```
public final void setFloatR(float value)
```

Mengembalikan atau menetapkan komponen merah sebuah warna. Semua transformasi warna diabaikan. Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |

### getFloatG() {#getFloatG--}
```
public final float getFloatG()
```

Mengembalikan atau menetapkan komponen hijau sebuah warna. Semua transformasi warna diabaikan. Baca/tulis float.

**Mengembalikan:**
float

### setFloatG(float value) {#setFloatG-float-}
```
public final void setFloatG(float value)
```

Mengembalikan atau menetapkan komponen hijau sebuah warna. Semua transformasi warna diabaikan. Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |

### getFloatB() {#getFloatB--}
```
public final float getFloatB()
```

Mengembalikan atau menetapkan komponen biru sebuah warna. Semua transformasi warna diabaikan. Baca/tulis float.

**Mengembalikan:**
float

### setFloatB(float value) {#setFloatB-float-}
```
public final void setFloatB(float value)
```

Mengembalikan atau menetapkan komponen biru sebuah warna. Semua transformasi warna diabaikan. Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |

### getHue() {#getHue--}
```
public final float getHue()
```

Mengembalikan atau menetapkan komponen hue sebuah warna dalam representasi HSL. Semua transformasi warna diabaikan. Baca/tulis float.

**Mengembalikan:**
float

### setHue(float value) {#setHue-float-}
```
public final void setHue(float value)
```

Mengembalikan atau menetapkan komponen hue sebuah warna dalam representasi HSL. Semua transformasi warna diabaikan. Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |

### getSaturation() {#getSaturation--}
```
public final float getSaturation()
```

Mengembalikan atau menetapkan komponen saturasi sebuah warna dalam representasi HSL. Semua transformasi warna diabaikan. Baca/tulis float.

**Mengembalikan:**
float

### setSaturation(float value) {#setSaturation-float-}
```
public final void setSaturation(float value)
```

Mengembalikan atau menetapkan komponen saturasi sebuah warna dalam representasi HSL. Semua transformasi warna diabaikan. Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |

### getLuminance() {#getLuminance--}
```
public final float getLuminance()
```

Mengembalikan atau menetapkan komponen luminansi sebuah warna dalam representasi HSL. Semua transformasi warna diabaikan. Baca/tulis float.

**Mengembalikan:**
float

### setLuminance(float value) {#setLuminance-float-}
```
public final void setLuminance(float value)
```

Mengembalikan atau menetapkan komponen luminansi sebuah warna dalam representasi HSL. Semua transformasi warna diabaikan. Baca/tulis float.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| value | float |  |

### getColorTransform() {#getColorTransform--}
```
public final IColorOperationCollection getColorTransform()
```

Mengembalikan koleksi transformasi warna yang diterapkan pada sebuah warna. Baca-saja [IColorOperationCollection](../../com.aspose.slides/icoloroperationcollection).

**Mengembalikan:**
[IColorOperationCollection](../../com.aspose.slides/icoloroperationcollection)

### toString(int format) {#toString-int-}
```
public final String toString(int format)
```

Mengembalikan String yang mewakili format warna saat ini.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| format | int | Jenis format string warna. |

**Mengembalikan:**
java.lang.String - String yang mewakili format warna saat ini.

### copyFrom(IColorFormat color) {#copyFrom-com.aspose.slides.IColorFormat-}
```
public final void copyFrom(IColorFormat color)
```

Menyalin format warna dari "color".

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| color | [IColorFormat](../../com.aspose.slides/icolorformat) |  |

### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```

Memeriksa kesamaan dengan objek yang ditentukan.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| obj | java.lang.Object | Objek. |

**Mengembalikan:**
boolean - Benar jika objek sama, jika tidak false.

### hashCode() {#hashCode--}
```
public int hashCode()
```

Mengembalikan kode hash.

**Mengembalikan:**
int - Kode hash.

### getVersion() {#getVersion--}
```
public long getVersion()
```

Versi. Baca-saja long.

**Mengembalikan:**
long

### getParent_ISlideComponent() {#getParent-ISlideComponent--}
```
public final ISlideComponent getParent_ISlideComponent()
```

**Mengembalikan:**
[ISlideComponent](../../com.aspose.slides/islidecomponent)

### getParent_IPresentationComponent() {#getParent-IPresentationComponent--}
```
public final IPresentationComponent getParent_IPresentationComponent()
```

Mengembalikan IPresentationComponent induk. Baca-saja [IPresentationComponent](../../com.aspose.slides/ipresentationcomponent).

**Mengembalikan:**
[IPresentationComponent](../../com.aspose.slides/ipresentationcomponent)