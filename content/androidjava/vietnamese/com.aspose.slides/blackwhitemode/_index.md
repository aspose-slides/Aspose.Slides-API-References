---
title: BlackWhiteMode
second_title: Aspose.Slides cho Android thông qua Tham chiếu API Java
description: Xác định cách hình dạng có màu sẽ được chuyển đổi thành đen và trắng.
type: docs
url: /vi/com.aspose.slides/blackwhitemode/
---
**Kế thừa:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class BlackWhiteMode extends System.Enum
```

Xác định cách hình dạng có màu sẽ được chuyển đổi thành đen và trắng.
## Trường

| Trường | Mô tả |
| --- | --- |
| [NotDefined](#NotDefined) | Thuộc tính chưa được thiết lập |
| [Color](#Color) | Đối tượng được hiển thị với màu bình thường |
| [Automatic](#Automatic) | Đối tượng được hiển thị với màu tự động |
| [Gray](#Gray) | Đối tượng được hiển thị với màu xám |
| [LightGray](#LightGray) | Đối tượng được hiển thị với màu xám nhạt |
| [InverseGray](#InverseGray) | Đối tượng được hiển thị với màu xám ngược |
| [GrayWhite](#GrayWhite) | Đối tượng được hiển thị với màu xám và trắng |
| [BlackGray](#BlackGray) | Đối tượng được hiển thị với màu đen và xám |
| [BlackWhite](#BlackWhite) | Đối tượng được hiển thị với màu đen và trắng |
| [Black](#Black) | Đối tượng chỉ được hiển thị với màu đen |
| [White](#White) | Đối tượng được hiển thị với màu trắng |
| [Hidden](#Hidden) | Đối tượng không được hiển thị |
### NotDefined {#NotDefined}
```
public static final byte NotDefined
```

Thuộc tính chưa được thiết lập

### Color {#Color}
```
public static final byte Color
```

Đối tượng được hiển thị với màu bình thường

### Automatic {#Automatic}
```
public static final byte Automatic
```

Đối tượng được hiển thị với màu tự động

### Gray {#Gray}
```
public static final byte Gray
```

Đối tượng được hiển thị với màu xám

### LightGray {#LightGray}
```
public static final byte LightGray
```

Đối tượng được hiển thị với màu xám nhạt

### InverseGray {#InverseGray}
```
public static final byte InverseGray
```

Đối tượng được hiển thị với màu xám ngược

### GrayWhite {#GrayWhite}
```
public static final byte GrayWhite
```

Đối tượng được hiển thị với màu xám và trắng

### BlackGray {#BlackGray}
```
public static final byte BlackGray
```

Đối tượng được hiển thị với màu đen và xám

### BlackWhite {#BlackWhite}
```
public static final byte BlackWhite
```

Đối tượng được hiển thị với màu đen và trắng

### Black {#Black}
```
public static final byte Black
```

Đối tượng chỉ được hiển thị với màu đen

### White {#White}
```
public static final byte White
```

Đối tượng được hiển thị với màu trắng

### Hidden {#Hidden}
```
public static final byte Hidden
```

Đối tượng không được hiển thị