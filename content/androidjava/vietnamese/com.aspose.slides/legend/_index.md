---
title: Legend
second_title: Aspose.Slides for Android thông qua Tham chiếu API Java
description: Biểu diễn các thuộc tính legend của biểu đồ.
type: docs
url: /vi/com.aspose.slides/legend/
---
**Kế thừa:**
java.lang.Object, com.aspose.slides.DomObject

**Tất cả các giao diện được triển khai:**
[com.aspose.slides.ILegend](../../com.aspose.slides/ilegend)
```
public class Legend extends DomObject<Chart> implements ILegend
```

Biểu diễn các thuộc tính legend của chart.
## Phương thức

| Phương thức | Mô tả |
| --- | --- |
| [getX()](#getX--) | Trả về hoặc đặt tọa độ x của một legend dưới dạng phần tỷ của chiều rộng chart. |
| [setX(float value)](#setX-float-) | Trả về hoặc đặt tọa độ x của một legend dưới dạng phần tỷ của chiều rộng chart. |
| [getY()](#getY--) | Trả về hoặc đặt tọa độ y của một legend dưới dạng phần tỷ của chiều cao chart. |
| [setY(float value)](#setY-float-) | Trả về hoặc đặt tọa độ y của một legend dưới dạng phần tỷ của chiều cao chart. |
| [getWidth()](#getWidth--) | Trả về hoặc đặt chiều rộng của một legend dưới dạng phần tỷ của chiều rộng chart. |
| [setWidth(float value)](#setWidth-float-) | Trả về hoặc đặt chiều rộng của một legend dưới dạng phần tỷ của chiều rộng chart. |
| [getHeight()](#getHeight--) | Trả về hoặc đặt chiều cao của một legend dưới dạng phần tỷ của chiều cao chart. |
| [setHeight(float value)](#setHeight-float-) | Trả về hoặc đặt chiều cao của một legend dưới dạng phần tỷ của chiều cao chart. |
| [getRight()](#getRight--) | Right. |
| [getBottom()](#getBottom--) | Bottom. |
| [getOverlay()](#getOverlay--) | Xác định liệu các phần tử chart khác có được phép chồng lên legend hay không. |
| [setOverlay(boolean value)](#setOverlay-boolean-) | Xác định liệu các phần tử chart khác có được phép chồng lên legend hay không. |
| [getTextFormat()](#getTextFormat--) | Định dạng văn bản. |
| [getPosition()](#getPosition--) | Xác định vị trí của legend trên chart. |
| [setPosition(int value)](#setPosition-int-) | Xác định vị trí của legend trên chart. |
| [getFormat()](#getFormat--) | Trả về định dạng của một legend. |
| [getChart()](#getChart--) | Trả về chart. |
| [getEntries()](#getEntries--) | Lấy các mục legend. |
| [getActualX()](#getActualX--) | Xác định vị trí x thực tế (trái) của phần tử chart so với góc trên trái của chart. Gọi phương thức IChart.validateChartLayout() trước để lấy các giá trị thực tế. Đọc float. |
| [getActualY()](#getActualY--) | Xác định vị trí y thực tế (trên) của phần tử chart so với góc trên trái của chart. Gọi phương thức IChart.validateChartLayout() trước để lấy các giá trị thực tế. Đọc float. |
| [getActualWidth()](#getActualWidth--) | Xác định chiều rộng thực tế của phần tử chart. Gọi phương thức IChart.validateChartLayout() trước để lấy các giá trị thực tế. Đọc float. |
| [getActualHeight()](#getActualHeight--) | Xác định chiều cao thực tế của phần tử chart. Gọi phương thức IChart.validateChartLayout() trước để lấy các giá trị thực tế. Đọc float. |
| [getSlide()](#getSlide--) | Trả về slide cha của một FillFormat. |
| [getPresentation()](#getPresentation--) | Trả về presentation cha của một FillFormat. |
### getX() {#getX--}
```
public final float getX()
```


Trả về hoặc đặt tọa độ x của một legend dưới dạng phần tỷ của chiều rộng chart. Đọc/ghi float.

**Trả về:**
float
### setX(float value) {#setX-float-}
```
public final void setX(float value)
```


Trả về hoặc đặt tọa độ x của một legend dưới dạng phần tỷ của chiều rộng chart. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |
### getY() {#getY--}
```
public final float getY()
```


Trả về hoặc đặt tọa độ y của một legend dưới dạng phần tỷ của chiều cao chart. Đọc/ghi float.

**Trả về:**
float
### setY(float value) {#setY-float-}
```
public final void setY(float value)
```


Trả về hoặc đặt tọa độ y của một legend dưới dạng phần tỷ của chiều cao chart. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |
### getWidth() {#getWidth--}
```
public final float getWidth()
```


Trả về hoặc đặt chiều rộng của một legend dưới dạng phần tỷ của chiều rộng chart. Đọc/ghi float.

**Trả về:**
float
### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```


Trả về hoặc đặt chiều rộng của một legend dưới dạng phần tỷ của chiều rộng chart. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |
### getHeight() {#getHeight--}
```
public final float getHeight()
```


Trả về hoặc đặt chiều cao của một legend dưới dạng phần tỷ của chiều cao chart. Đọc/ghi float.

**Trả về:**
float
### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```


Trả về hoặc đặt chiều cao của một legend dưới dạng phần tỷ của chiều cao chart. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |
### getRight() {#getRight--}
```
public final float getRight()
```


Right. Chỉ đọc float.

**Trả về:**
float
### getBottom() {#getBottom--}
```
public final float getBottom()
```


Bottom. Chỉ đọc float.

**Trả về:**
float
### getOverlay() {#getOverlay--}
```
public final boolean getOverlay()
```


Xác định liệu các phần tử chart khác có được phép chồng lên legend hay không. Đọc/ghi boolean.

**Trả về:**
boolean
### setOverlay(boolean value) {#setOverlay-boolean-}
```
public final void setOverlay(boolean value)
```


Xác định liệu các phần tử chart khác có được phép chồng lên legend hay không. Đọc/ghi boolean.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |
### getTextFormat() {#getTextFormat--}
```
public final IChartTextFormat getTextFormat()
```


Định dạng văn bản. Chỉ đọc [IChartTextFormat](../../com.aspose.slides/icharttextformat).

**Trả về:**
[IChartTextFormat](../../com.aspose.slides/icharttextformat)
### getPosition() {#getPosition--}
```
public final int getPosition()
```


Xác định vị trí của legend trên chart. Giá trị không phải NaN của các thuộc tính X, Y, Width, Height sẽ ghi đè hiệu lực của thuộc tính này. Đọc/ghi [LegendPositionType](../../com.aspose.slides/legendpositiontype).

**Trả về:**
int
### setPosition(int value) {#setPosition-int-}
```
public final void setPosition(int value)
```


Xác định vị trí của legend trên chart. Giá trị không phải NaN của các thuộc tính X, Y, Width, Height sẽ ghi đè hiệu lực của thuộc tính này. Đọc/ghi [LegendPositionType](../../com.aspose.slides/legendpositiontype).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | int |  |
### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```


Trả về định dạng của một legend. Chỉ đọc [IFormat](../../com.aspose.slides/iformat).

**Trả về:**
[IFormat](../../com.aspose.slides/iformat)
### getChart() {#getChart--}
```
public final IChart getChart()
```


Trả về chart. Chỉ đọc [IChart](../../com.aspose.slides/ichart).

**Trả về:**
[IChart](../../com.aspose.slides/ichart)
### getEntries() {#getEntries--}
```
public final ILegendEntryCollection getEntries()
```


Lấy các mục legend. Chỉ đọc [ILegendEntryCollection](../../com.aspose.slides/ilegendentrycollection).

**Trả về:**
[ILegendEntryCollection](../../com.aspose.slides/ilegendentrycollection)
### getActualX() {#getActualX--}
```
public final float getActualX()
```


Xác định vị trí x thực tế (trái) của phần tử chart so với góc trên trái của chart. Gọi phương thức IChart.validateChartLayout() trước để lấy các giá trị thực tế. Đọc float.

**Trả về:**
float
### getActualY() {#getActualY--}
```
public final float getActualY()
```


Xác định vị trí y thực tế (trên) của phần tử chart so với góc trên trái của chart. Gọi phương thức IChart.validateChartLayout() trước để lấy các giá trị thực tế. Đọc float.

**Trả về:**
float
### getActualWidth() {#getActualWidth--}
```
public final float getActualWidth()
```


Xác định chiều rộng thực tế của phần tử chart. Gọi phương thức IChart.validateChartLayout() trước để lấy các giá trị thực tế. Đọc float.

**Trả về:**
float
### getActualHeight() {#getActualHeight--}
```
public final float getActualHeight()
```


Xác định chiều cao thực tế của phần tử chart. Gọi phương thức IChart.validateChartLayout() trước để lấy các giá trị thực tế. Đọc float.

**Trả về:**
float
### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```


Trả về slide cha của một FillFormat. Chỉ đọc [BaseSlide](../../com.aspose.slides/baseslide).

**Trả về:**
[IBaseSlide](../../com.aspose.slides/ibaseslide)
### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```


Trả về presentation cha của một FillFormat. Chỉ đọc [IPresentation](../../com.aspose.slides/ipresentation).

**Trả về:**
[IPresentation](../../com.aspose.slides/ipresentation)