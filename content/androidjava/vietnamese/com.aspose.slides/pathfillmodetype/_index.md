---
title: PathFillModeType
second_title: Aspose.Slides cho Android qua Tham chiếu API Java
description: Xác định cách mà một đường dẫn nên được tô màu
type: docs
url: /vi/com.aspose.slides/pathfillmodetype/
---
**Kế thừa:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class PathFillModeType extends System.Enum
```

Xác định cách mà một đường dẫn nên được tô màu
## Trường

| Trường | Mô tả |
| --- | --- |
| [None](#None) | No Path Fill Điều này chỉ ra rằng đường dẫn tương ứng không có màu nền. |
| [Normal](#Normal) | Normal Path Fill Điều này chỉ ra rằng đường dẫn tương ứng sẽ có một màu được làm bóng thường áp dụng cho phần nền của nó. |
| [Lighten](#Lighten) | Lighten Path Fill Điều này chỉ ra rằng đường dẫn tương ứng sẽ có một màu được làm bóng nhẹ áp dụng cho phần nền của nó. |
| [LightenLess](#LightenLess) | Lighten Path Fill Less Điều này chỉ ra rằng đường dẫn tương ứng sẽ có một màu hơi nhạt hơn được áp dụng cho phần nền của nó. |
| [Darken](#Darken) | Darken Path Fill Điều này chỉ ra rằng đường dẫn tương ứng sẽ có một màu tối hơn được áp dụng cho phần nền của nó. |
| [DarkenLess](#DarkenLess) | Darken Path Fill Less Điều này chỉ ra rằng đường dẫn tương ứng sẽ có một màu hơi tối hơn được áp dụng cho phần nền của nó. |
### None {#None}
```
public static final byte None
```

No Path Fill Điều này chỉ ra rằng đường dẫn tương ứng không có màu nền.

### Normal {#Normal}
```
public static final byte Normal
```

Normal Path Fill Điều này chỉ ra rằng đường dẫn tương ứng sẽ có một màu được làm bóng thường áp dụng cho phần nền của nó.

### Lighten {#Lighten}
```
public static final byte Lighten
```

Lighten Path Fill Điều này chỉ ra rằng đường dẫn tương ứng sẽ có một màu được làm bóng nhẹ áp dụng cho phần nền của nó.

### LightenLess {#LightenLess}
```
public static final byte LightenLess
```

Lighten Path Fill Less Điều này chỉ ra rằng đường dẫn tương ứng sẽ có một màu hơi nhạt hơn được áp dụng cho phần nền của nó.

### Darken {#Darken}
```
public static final byte Darken
```

Darken Path Fill Điều này chỉ ra rằng đường dẫn tương ứng sẽ có một màu tối hơn được áp dụng cho phần nền của nó.

### DarkenLess {#DarkenLess}
```
public static final byte DarkenLess
```

Darken Path Fill Less Điều này chỉ ra rằng đường dẫn tương ứng sẽ có một màu hơi tối hơn được áp dụng cho phần nền của nó.