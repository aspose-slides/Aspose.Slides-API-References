---
title: IScaleEffect
second_title: Aspose.Slides cho Android qua Tham chiếu API Java
description: Biểu diễn hiệu ứng thu phóng hoạt ảnh.
type: docs
url: /vi/com.aspose.slides/iscaleeffect/
---
**Tất cả các giao diện được triển khai:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IScaleEffect extends IBehavior
```

Biểu diễn hiệu ứng thu phóng hoạt ảnh.
## Các phương thức

| Phương thức | Mô tả |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Xác định liệu nội dung có nên được thu phóng hay không. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Xác định liệu nội dung có nên được thu phóng hay không. |
| [getFrom()](#getFrom--) | Xác định tọa độ x/y để bắt đầu hiệu ứng (theo phần trăm). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | Xác định tọa độ x/y để bắt đầu hiệu ứng (theo phần trăm). |
| [getTo()](#getTo--) | Xác định vị trí mục tiêu cho hiệu ứng thu phóng hoạt ảnh (theo phần trăm). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | Xác định vị trí mục tiêu cho hiệu ứng thu phóng hoạt ảnh (theo phần trăm). |
| [getBy()](#getBy--) | Mô tả giá trị độ lệch tương đối cho hiệu ứng (theo phần trăm). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | Mô tả giá trị độ lệch tương đối cho hiệu ứng (theo phần trăm). |
### getZoomContent() {#getZoomContent--}
```
public abstract byte getZoomContent()
```

Xác định liệu nội dung có nên được thu phóng hay không. Đọc/ghi [NullableBool](../../com.aspose.slides/nullablebool).

**Trả về:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public abstract void setZoomContent(byte value)
```

Xác định liệu nội dung có nên được thu phóng hay không. Đọc/ghi [NullableBool](../../com.aspose.slides/nullablebool).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public abstract PointF getFrom()
```

Xác định tọa độ x/y để bắt đầu hiệu ứng (theo phần trăm). Đọc/ghi android.graphics.PointF.

**Trả về:**
android.graphics.PointF
### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public abstract void setFrom(PointF value)
```

Xác định tọa độ x/y để bắt đầu hiệu ứng (theo phần trăm). Đọc/ghi android.graphics.PointF.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public abstract PointF getTo()
```

Xác định vị trí mục tiêu cho hiệu ứng thu phóng hoạt ảnh (theo phần trăm). Đọc/ghi android.graphics.PointF.

**Trả về:**
android.graphics.PointF
### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public abstract void setTo(PointF value)
```

Xác định vị trí mục tiêu cho hiệu ứng thu phóng hoạt ảnh (theo phần trăm). Đọc/ghi android.graphics.PointF.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public abstract PointF getBy()
```

Mô tả giá trị độ lệch tương đối cho hiệu ứng (theo phần trăm). Đọc/ghi android.graphics.PointF.

**Trả về:**
android.graphics.PointF
### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public abstract void setBy(PointF value)
```

Mô tả giá trị độ lệch tương đối cho hiệu ứng (theo phần trăm). Đọc/ghi android.graphics.PointF.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | android.graphics.PointF |  |