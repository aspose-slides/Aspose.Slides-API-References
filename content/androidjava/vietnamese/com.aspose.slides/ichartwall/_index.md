---
title: IChartWall
second_title: Aspose.Slides for Android via Java API Reference
description: Biểu diễn tường trên biểu đồ 3D.
type: docs
url: /vi/com.aspose.slides/ichartwall/
---```
public interface IChartWall
```

Biểu diễn tường trên biểu đồ 3D.
## Phương thức

| Phương thức | Mô tả |
| --- | --- |
| [getThickness()](#getThickness--) | Trả về hoặc đặt độ dày tường dưới dạng phần trăm của chiều lớn nhất của thể tích biểu đồ. |
| [setThickness(int value)](#setThickness-int-) | Trả về hoặc đặt độ dày tường dưới dạng phần trăm của chiều lớn nhất của thể tích biểu đồ. |
| [getFormat()](#getFormat--) | Trả về màu nền tường, đường viền, hiệu ứng, kiểu 3D. |
| [getPictureType()](#getPictureType--) | Trả về hoặc đặt loại hình ảnh. |
| [setPictureType(int value)](#setPictureType-int-) | Trả về hoặc đặt loại hình ảnh. |
### getThickness() {#getThickness--}
```
public abstract int getThickness()
```

Trả về hoặc đặt độ dày tường dưới dạng phần trăm của chiều lớn nhất của thể tích biểu đồ. Đọc/ghi int.

**Giá trị trả về:**
int
### setThickness(int value) {#setThickness-int-}
```
public abstract void setThickness(int value)
```

Trả về hoặc đặt độ dày tường dưới dạng phần trăm của chiều lớn nhất của thể tích biểu đồ. Đọc/ghi int.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | int |  |
### getFormat() {#getFormat--}
```
public abstract IFormat getFormat()
```

Trả về màu nền tường, đường viền, hiệu ứng, kiểu 3D. Chỉ đọc [IFormat](../../com.aspose.slides/iformat).

**Giá trị trả về:**
[IFormat](../../com.aspose.slides/iformat)
### getPictureType() {#getPictureType--}
```
public abstract int getPictureType()
```

Trả về hoặc đặt loại hình ảnh. Đọc/ghi [PictureType](../../com.aspose.slides/picturetype)(\#getPictureType.getPictureType/\#setPictureType(int).setPictureType(int)).

**Giá trị trả về:**
int
### setPictureType(int value) {#setPictureType-int-}
```
public abstract void setPictureType(int value)
```

Trả về hoặc đặt loại hình ảnh. Đọc/ghi [PictureType](../../com.aspose.slides/picturetype)(\#getPictureType.getPictureType/\#setPictureType(int).setPictureType(int)).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | int |  |