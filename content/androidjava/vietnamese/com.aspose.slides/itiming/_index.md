---
title: ITiming
second_title: Aspose.Slides for Android via Java API Reference
description: Represents animation timing.
type: docs
url: /vi/com.aspose.slides/itiming/
---```
public interface ITiming
```

Biểu diễn thời gian hoạt ảnh.
## Phương thức

| Phương thức | Mô tả |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | Mô tả phần trăm thời lượng của hiệu ứng hành vi tăng tốc. |
| [setAccelerate(float value)](#setAccelerate-float-) | Mô tả phần trăm thời lượng của hiệu ứng hành vi tăng tốc. |
| [getDecelerate()](#getDecelerate--) | Mô tả phần trăm thời lượng của hiệu ứng hành vi giảm tốc. |
| [setDecelerate(float value)](#setDecelerate-float-) | Mô tả phần trăm thời lượng của hiệu ứng hành vi giảm tốc. |
| [getAutoReverse()](#getAutoReverse--) | Mô tả việc tự động phát hoạt ảnh ngược lại sau khi đã phát theo hướng tiến. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | Mô tả việc tự động phát hoạt ảnh ngược lại sau khi đã phát theo hướng tiến. |
| [getDuration()](#getDuration--) | Mô tả thời lượng của hiệu ứng hoạt ảnh. |
| [setDuration(float value)](#setDuration-float-) | Mô tả thời lượng của hiệu ứng hoạt ảnh. |
| [getRepeatCount()](#getRepeatCount--) | Mô tả số lần hiệu ứng nên lặp lại. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | Mô tả số lần hiệu ứng nên lặp lại. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | Thuộc tính này xác định liệu hiệu ứng có lặp lại đến khi kết thúc slide hay không. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | Thuộc tính này xác định liệu hiệu ứng có lặp lại đến khi kết thúc slide hay không. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | Thuộc tính này xác định liệu hiệu ứng có lặp lại cho đến lần nhấp tiếp theo hay không. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | Thuộc tính này xác định liệu hiệu ứng có lặp lại cho đến lần nhấp tiếp theo hay không. |
| [getRepeatDuration()](#getRepeatDuration--) | Mô tả số lần hiệu ứng nên lặp lại. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | Mô tả số lần hiệu ứng nên lặp lại. |
| [getRestart()](#getRestart--) | Xác định xem một hiệu ứng có được khởi động lại sau khi hoàn thành hay không. |
| [setRestart(int value)](#setRestart-int-) | Xác định xem một hiệu ứng có được khởi động lại sau khi hoàn thành hay không. |
| [getSpeed()](#getSpeed--) | Xác định phần trăm để tăng tốc (hoặc làm chậm) thời gian. |
| [setSpeed(float value)](#setSpeed-float-) | Xác định phần trăm để tăng tốc (hoặc làm chậm) thời gian. |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | Mô tả thời gian trì hoãn sau khi kích hoạt. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | Mô tả thời gian trì hoãn sau khi kích hoạt. |
| [getTriggerType()](#getTriggerType--) | Mô tả loại kích hoạt. |
| [setTriggerType(int value)](#setTriggerType-int-) | Mô tả loại kích hoạt. |
| [getRewind()](#getRewind--) | Thuộc tính này xác định liệu hiệu ứng có được tua lại khi phát xong hay không. |
| [setRewind(boolean value)](#setRewind-boolean-) | Thuộc tính này xác định liệu hiệu ứng có được tua lại khi phát xong hay không. |
### getAccelerate() {#getAccelerate--}
```
public abstract float getAccelerate()
```


Mô tả phần trăm thời lượng của hiệu ứng hành vi tăng tốc. Đọc/ghi float.

**Trả về:**
float
### setAccelerate(float value) {#setAccelerate-float-}
```
public abstract void setAccelerate(float value)
```


Mô tả phần trăm thời lượng của hiệu ứng hành vi tăng tốc. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public abstract float getDecelerate()
```


Mô tả phần trăm thời lượng của hiệu ứng hành vi giảm tốc. Đọc/ghi float.

**Trả về:**
float
### setDecelerate(float value) {#setDecelerate-float-}
```
public abstract void setDecelerate(float value)
```


Mô tả phần trăm thời lượng của hiệu ứng hành vi giảm tốc. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public abstract boolean getAutoReverse()
```


Mô tả việc tự động phát hoạt ảnh ngược lại sau khi đã phát theo hướng tiến. Đọc/ghi boolean.

**Trả về:**
boolean
### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public abstract void setAutoReverse(boolean value)
```


Mô tả việc tự động phát hoạt ảnh ngược lại sau khi đã phát theo hướng tiến. Đọc/ghi boolean.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public abstract float getDuration()
```


Mô tả thời lượng của hiệu ứng hoạt ảnh. Đọc/ghi float.

**Trả về:**
float
### setDuration(float value) {#setDuration-float-}
```
public abstract void setDuration(float value)
```


Mô tả thời lượng của hiệu ứng hoạt ảnh. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public abstract float getRepeatCount()
```


Mô tả số lần hiệu ứng nên lặp lại. Đọc/ghi float.

**Trả về:**
float
### setRepeatCount(float value) {#setRepeatCount-float-}
```
public abstract void setRepeatCount(float value)
```


Mô tả số lần hiệu ứng nên lặp lại. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public abstract boolean getRepeatUntilEndSlide()
```


Thuộc tính này xác định liệu hiệu ứng có lặp lại đến khi kết thúc slide hay không. Đọc/ghi boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Lấy chuỗi hiệu ứng cho slide đầu tiên
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Lấy hiệu ứng đầu tiên của chuỗi chính.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Thay đổi Timing/Repeat của hiệu ứng thành "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Trả về:**
boolean
### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public abstract void setRepeatUntilEndSlide(boolean value)
```


Thuộc tính này xác định liệu hiệu ứng có lặp lại đến khi kết thúc slide hay không. Đọc/ghi boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Lấy chuỗi hiệu ứng cho slide đầu tiên
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Lấy hiệu ứng đầu tiên của chuỗi chính.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Thay đổi Timing/Repeat của hiệu ứng thành "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public abstract boolean getRepeatUntilNextClick()
```


Thuộc tính này xác định liệu hiệu ứng có lặp lại cho đến lần nhấp tiếp theo hay không. Đọc/ghi boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Lấy chuỗi hiệu ứng cho slide đầu tiên
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Lấy hiệu ứng đầu tiên của chuỗi chính.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Thay đổi Timing/Repeat của hiệu ứng thành "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Trả về:**
boolean
### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public abstract void setRepeatUntilNextClick(boolean value)
```


Thuộc tính này xác định liệu hiệu ứng có lặp lại cho đến lần nhấp tiếp theo hay không. Đọc/ghi boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Lấy chuỗi hiệu ứng cho slide đầu tiên
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Lấy hiệu ứng đầu tiên của chuỗi chính.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Thay đổi Timing/Repeat của hiệu ứng thành "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public abstract float getRepeatDuration()
```


Mô tả số lần hiệu ứng nên lặp lại. Đọc/ghi float.

**Trả về:**
float
### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public abstract void setRepeatDuration(float value)
```


Mô tả số lần hiệu ứng nên lặp lại. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public abstract int getRestart()
```


Xác định xem một hiệu ứng có được khởi động lại sau khi hoàn thành hay không. Đọc/ghi [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Trả về:**
int
### setRestart(int value) {#setRestart-int-}
```
public abstract void setRestart(int value)
```


Xác định xem một hiệu ứng có được khởi động lại sau khi hoàn thành hay không. Đọc/ghi [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | int |  |

### getSpeed() {#getSpeed--}
```
public abstract float getSpeed()
```


Xác định phần trăm để tăng tốc (hoặc làm chậm) thời gian. Đọc/ghi float.

**Trả về:**
float
### setSpeed(float value) {#setSpeed-float-}
```
public abstract void setSpeed(float value)
```


Xác định phần trăm để tăng tốc (hoặc làm chậm) thời gian. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public abstract float getTriggerDelayTime()
```


Mô tả thời gian trì hoãn sau khi kích hoạt. Đọc/ghi float.

**Trả về:**
float
### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public abstract void setTriggerDelayTime(float value)
```


Mô tả thời gian trì hoãn sau khi kích hoạt. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public abstract int getTriggerType()
```


Mô tả loại kích hoạt. Đọc/ghi [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Trả về:**
int
### setTriggerType(int value) {#setTriggerType-int-}
```
public abstract void setTriggerType(int value)
```


Mô tả loại kích hoạt. Đọc/ghi [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public abstract boolean getRewind()
```


Thuộc tính này xác định liệu hiệu ứng có được tua lại khi phát xong hay không. Đọc/ghi boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Lấy chuỗi hiệu ứng cho slide đầu tiên
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Lấy hiệu ứng đầu tiên của chuỗi chính.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Bật Timing/Rewind cho hiệu ứng.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Trả về:**
boolean
### setRewind(boolean value) {#setRewind-boolean-}
```
public abstract void setRewind(boolean value)
```


Thuộc tính này xác định liệu hiệu ứng có được tua lại khi phát xong hay không. Đọc/ghi boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Lấy chuỗi hiệu ứng cho slide đầu tiên
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Lấy hiệu ứng đầu tiên của chuỗi chính.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Bật Timing/Rewind cho hiệu ứng.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |