---
title: IChartParagraphFormat
second_title: Aspose.Slides cho Android thông qua Tham chiếu API Java
description: Đại diện cho các thuộc tính định dạng đoạn văn của biểu đồ.
type: docs
url: /vi/com.aspose.slides/ichartparagraphformat/
---```
public interface IChartParagraphFormat
```

Đại diện cho các thuộc tính định dạng đoạn văn của biểu đồ.

## Phương thức

| Method | Description |
| --- | --- |
| [getAlignment()](#getAlignment--) | Trả về hoặc đặt căn chỉnh văn bản trong một đoạn. |
| [setAlignment(int value)](#setAlignment-int-) | Trả về hoặc đặt căn chỉnh văn bản trong một đoạn. |
| [getSpaceWithin()](#getSpaceWithin--) | Trả về hoặc đặt khoảng cách giữa các đường cơ sở trong một đoạn. |
| [setSpaceWithin(float value)](#setSpaceWithin-float-) | Trả về hoặc đặt khoảng cách giữa các đường cơ sở trong một đoạn. |
| [getSpaceBefore()](#getSpaceBefore--) | Trả về hoặc đặt khoảng cách trước dòng đầu tiên trong một đoạn. |
| [setSpaceBefore(float value)](#setSpaceBefore-float-) | Trả về hoặc đặt khoảng cách trước dòng đầu tiên trong một đoạn. |
| [getSpaceAfter()](#getSpaceAfter--) | Trả về hoặc đặt khoảng cách sau dòng cuối cùng trong một đoạn. |
| [setSpaceAfter(float value)](#setSpaceAfter-float-) | Trả về hoặc đặt khoảng cách sau dòng cuối cùng trong một đoạn. |
| [getEastAsianLineBreak()](#getEastAsianLineBreak--) | Xác định xem ngắt dòng Đông Á có được sử dụng trong một đoạn hay không. |
| [setEastAsianLineBreak(byte value)](#setEastAsianLineBreak-byte-) | Xác định xem ngắt dòng Đông Á có được sử dụng trong một đoạn hay không. |
| [getRightToLeft()](#getRightToLeft--) | Xác định xem viết từ phải sang trái có được sử dụng trong một đoạn hay không. |
| [setRightToLeft(byte value)](#setRightToLeft-byte-) | Xác định xem viết từ phải sang trái có được sử dụng trong một đoạn hay không. |
| [getLatinLineBreak()](#getLatinLineBreak--) | Xác định xem ngắt dòng La-tinh có được sử dụng trong một đoạn hay không. |
| [setLatinLineBreak(byte value)](#setLatinLineBreak-byte-) | Xác định xem ngắt dòng La-tinh có được sử dụng trong một đoạn hay không. |
| [getHangingPunctuation()](#getHangingPunctuation--) | Xác định xem dấu câu treo được sử dụng trong một đoạn hay không. |
| [setHangingPunctuation(byte value)](#setHangingPunctuation-byte-) | Xác định xem dấu câu treo được sử dụng trong một đoạn hay không. |
| [getMarginLeft()](#getMarginLeft--) | Trả về hoặc đặt lề trái trong một đoạn. |
| [setMarginLeft(float value)](#setMarginLeft-float-) | Trả về hoặc đặt lề trái trong một đoạn. |
| [getMarginRight()](#getMarginRight--) | Trả về hoặc đặt lề phải trong một đoạn. |
| [setMarginRight(float value)](#setMarginRight-float-) | Trả về hoặc đặt lề phải trong một đoạn. |
| [getIndent()](#getIndent--) | Trả về hoặc đặt Độ thụt dòng đầu/Độ thụt treo của đoạn. |
| [setIndent(float value)](#setIndent-float-) | Trả về hoặc đặt Độ thụt dòng đầu/Độ thụt treo của đoạn. |
| [getDefaultTabSize()](#getDefaultTabSize--) | Trả về hoặc đặt kích thước tab mặc định. |
| [setDefaultTabSize(float value)](#setDefaultTabSize-float-) | Trả về hoặc đặt kích thước tab mặc định. |
| [getTabs()](#getTabs--) | Trả về các tab của một đoạn. |
| [getFontAlignment()](#getFontAlignment--) | Trả về hoặc đặt căn chỉnh phông chữ trong một đoạn. |
| [setFontAlignment(int value)](#setFontAlignment-int-) | Trả về hoặc đặt căn chỉnh phông chữ trong một đoạn. |

### getAlignment() {#getAlignment--}
```
public abstract int getAlignment()
```

Trả về hoặc đặt căn chỉnh văn bản trong một đoạn. Đọc/ghi [TextAlignment](../../com.aspose.slides/textalignment).

**Trả về:**
int

### setAlignment(int value) {#setAlignment-int-}
```
public abstract void setAlignment(int value)
```

Trả về hoặc đặt căn chỉnh văn bản trong một đoạn. Đọc/ghi [TextAlignment](../../com.aspose.slides/textalignment).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | int |  |

### getSpaceWithin() {#getSpaceWithin--}
```
public abstract float getSpaceWithin()
```

Trả về hoặc đặt khoảng cách giữa các đường cơ sở trong một đoạn. Đọc/ghi float.

**Trả về:**
float

### setSpaceWithin(float value) {#setSpaceWithin-float-}
```
public abstract void setSpaceWithin(float value)
```

Trả về hoặc đặt khoảng cách giữa các đường cơ sở trong một đoạn. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getSpaceBefore() {#getSpaceBefore--}
```
public abstract float getSpaceBefore()
```

Trả về hoặc đặt khoảng cách trước dòng đầu tiên trong một đoạn. Đọc/ghi float.

**Trả về:**
float

### setSpaceBefore(float value) {#setSpaceBefore-float-}
```
public abstract void setSpaceBefore(float value)
```

Trả về hoặc đặt khoảng cách trước dòng đầu tiên trong một đoạn. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getSpaceAfter() {#getSpaceAfter--}
```
public abstract float getSpaceAfter()
```

Trả về hoặc đặt khoảng cách sau dòng cuối cùng trong một đoạn. Đọc/ghi float.

**Trả về:**
float

### setSpaceAfter(float value) {#setSpaceAfter-float-}
```
public abstract void setSpaceAfter(float value)
```

Trả về hoặc đặt khoảng cách sau dòng cuối cùng trong một đoạn. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getEastAsianLineBreak() {#getEastAsianLineBreak--}
```
public abstract byte getEastAsianLineBreak()
```

Xác định xem ngắt dòng Đông Á có được sử dụng trong một đoạn hay không. Đọc/ghi [NullableBool](../../com.aspose.slides/nullablebool).

**Trả về:**
byte

### setEastAsianLineBreak(byte value) {#setEastAsianLineBreak-byte-}
```
public abstract void setEastAsianLineBreak(byte value)
```

Xác định xem ngắt dòng Đông Á có được sử dụng trong một đoạn hay không. Đọc/ghi [NullableBool](../../com.aspose.slides/nullablebool).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | byte |  |

### getRightToLeft() {#getRightToLeft--}
```
public abstract byte getRightToLeft()
```

Xác định xem viết từ phải sang trái có được sử dụng trong một đoạn hay không. Đọc/ghi [NullableBool](../../com.aspose.slides/nullablebool).

**Trả về:**
byte

### setRightToLeft(byte value) {#setRightToLeft-byte-}
```
public abstract void setRightToLeft(byte value)
```

Xác định xem viết từ phải sang trái có được sử dụng trong một đoạn hay không. Đọc/ghi [NullableBool](../../com.aspose.slides/nullablebool).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | byte |  |

### getLatinLineBreak() {#getLatinLineBreak--}
```
public abstract byte getLatinLineBreak()
```

Xác định xem ngắt dòng La-tinh có được sử dụng trong một đoạn hay không. Đọc/ghi [NullableBool](../../com.aspose.slides/nullablebool).

**Trả về:**
byte

### setLatinLineBreak(byte value) {#setLatinLineBreak-byte-}
```
public abstract void setLatinLineBreak(byte value)
```

Xác định xem ngắt dòng La-tinh có được sử dụng trong một đoạn hay không. Đọc/ghi [NullableBool](../../com.aspose.slides/nullablebool).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | byte |  |

### getHangingPunctuation() {#getHangingPunctuation--}
```
public abstract byte getHangingPunctuation()
```

Xác định xem dấu câu treo được sử dụng trong một đoạn hay không. Đọc/ghi [NullableBool](../../com.aspose.slides/nullablebool).

**Trả về:**
byte

### setHangingPunctuation(byte value) {#setHangingPunctuation-byte-}
```
public abstract void setHangingPunctuation(byte value)
```

Xác định xem dấu câu treo được sử dụng trong một đoạn hay không. Đọc/ghi [NullableBool](../../com.aspose.slides/nullablebool).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | byte |  |

### getMarginLeft() {#getMarginLeft--}
```
public abstract float getMarginLeft()
```

Trả về hoặc đặt lề trái trong một đoạn. Đọc/ghi float.

**Trả về:**
float

### setMarginLeft(float value) {#setMarginLeft-float-}
```
public abstract void setMarginLeft(float value)
```

Trả về hoặc đặt lề trái trong một đoạn. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getMarginRight() {#getMarginRight--}
```
public abstract float getMarginRight()
```

Trả về hoặc đặt lề phải trong một đoạn. Đọc/ghi float.

**Trả về:**
float

### setMarginRight(float value) {#setMarginRight-float-}
```
public abstract void setMarginRight(float value)
```

Trả về hoặc đặt lề phải trong một đoạn. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getIndent() {#getIndent--}
```
public abstract float getIndent()
```

Trả về hoặc đặt Độ thụt dòng đầu/Độ thụt treo của đoạn. Độ thụt treo có thể được định nghĩa bằng giá trị âm. Đọc/ghi float.

**Trả về:**
float

### setIndent(float value) {#setIndent-float-}
```
public abstract void setIndent(float value)
```

Trả về hoặc đặt Độ thụt dòng đầu/Độ thụt treo của đoạn. Độ thụt treo có thể được định nghĩa bằng giá trị âm. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getDefaultTabSize() {#getDefaultTabSize--}
```
public abstract float getDefaultTabSize()
```

Trả về hoặc đặt kích thước tab mặc định. Đọc/ghi float.

**Trả về:**
float

### setDefaultTabSize(float value) {#setDefaultTabSize-float-}
```
public abstract void setDefaultTabSize(float value)
```

Trả về hoặc đặt kích thước tab mặc định. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getTabs() {#getTabs--}
```
public abstract ITabCollection getTabs()
```

Trả về các tab của một đoạn. Chỉ đọc [ITabCollection](../../com.aspose.slides/itabcollection).

**Trả về:**
[ITabCollection](../../com.aspose.slides/itabcollection)

### getFontAlignment() {#getFontAlignment--}
```
public abstract int getFontAlignment()
```

Trả về hoặc đặt căn chỉnh phông chữ trong một đoạn. Đọc/ghi [FontAlignment](../../com.aspose.slides/fontalignment).

**Trả về:**
int

### setFontAlignment(int value) {#setFontAlignment-int-}
```
public abstract void setFontAlignment(int value)
```

Trả về hoặc đặt căn chỉnh phông chữ trong một đoạn. Đọc/ghi [FontAlignment](../../com.aspose.slides/fontalignment).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | int |  |