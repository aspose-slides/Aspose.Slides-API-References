---
title: ScaleEffect
second_title: Aspose.Slides cho Android qua Tham chiếu API Java
description: Đại diện cho hiệu ứng thu phóng hoạt hình.
type: docs
url: /vi/com.aspose.slides/scaleeffect/
---
**Kế thừa:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**Tất cả các giao diện được triển khai:**
[com.aspose.slides.IScaleEffect](../../com.aspose.slides/iscaleeffect)
```
public class ScaleEffect extends Behavior implements IScaleEffect
```

Đại diện cho hiệu ứng thu phóng hoạt hình.
## Hàm tạo

| Hàm tạo | Mô tả |
| --- | --- |
| [ScaleEffect()](#ScaleEffect--) |  |
## Phương thức

| Phương thức | Mô tả |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Xác định liệu nội dung có nên được phóng to hay không. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Xác định liệu nội dung có nên được phóng to hay không. |
| [getFrom()](#getFrom--) | Xác định tọa độ x/y để bắt đầu hoạt hình (theo phần trăm). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | Xác định tọa độ x/y để bắt đầu hoạt hình (theo phần trăm). |
| [getTo()](#getTo--) | Xác định vị trí mục tiêu cho hiệu ứng thu phóng hoạt hình (theo phần trăm). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | Xác định vị trí mục tiêu cho hiệu ứng thu phóng hoạt hình (theo phần trăm). |
| [getBy()](#getBy--) | Mô tả giá trị độ lệch tương đối cho hoạt hình (theo phần trăm). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | Mô tả giá trị độ lệch tương đối cho hoạt hình (theo phần trăm). |
### ScaleEffect() {#ScaleEffect--}
```
public ScaleEffect()
```


### getZoomContent() {#getZoomContent--}
```
public final byte getZoomContent()
```


Xác định liệu nội dung có nên được phóng to hay không. Đọc/ghi [NullableBool](../../com.aspose.slides/nullablebool).

**Giá trị trả về:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public final void setZoomContent(byte value)
```


Xác định liệu nội dung có nên được phóng to hay không. Đọc/ghi [NullableBool](../../com.aspose.slides/nullablebool).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public final PointF getFrom()
```


Xác định tọa độ x/y để bắt đầu hoạt hình (theo phần trăm). Đọc/ghi android.graphics.PointF.

**Giá trị trả về:**
android.graphics.PointF
### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public final void setFrom(PointF value)
```


Xác định tọa độ x/y để bắt đầu hoạt hình (theo phần trăm). Đọc/ghi android.graphics.PointF.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public final PointF getTo()
```


Xác định vị trí mục tiêu cho hiệu ứng thu phóng hoạt hình (theo phần trăm). Đọc/ghi android.graphics.PointF.

**Giá trị trả về:**
android.graphics.PointF
### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public final void setTo(PointF value)
```


Xác định vị trí mục tiêu cho hiệu ứng thu phóng hoạt hình (theo phần trăm). Đọc/ghi android.graphics.PointF.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public final PointF getBy()
```


Mô tả giá trị độ lệch tương đối cho hoạt hình (theo phần trăm). Đọc/ghi android.graphics.PointF.

**Giá trị trả về:**
android.graphics.PointF
### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public final void setBy(PointF value)
```


Mô tả giá trị độ lệch tương đối cho hoạt hình (theo phần trăm). Đọc/ghi android.graphics.PointF.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | android.graphics.PointF |  |