---
title: IPlaceholder
second_title: Aspose.Slides for Android via Java API Reference
description: Represents a placeholder on a slide.
type: docs
url: /vi/com.aspose.slides/iplaceholder/
---```
public interface IPlaceholder
```

Biểu diễn một trình giữ chỗ trên một slide.
## Phương thức

| Phương thức | Mô tả |
| --- | --- |
| [getOrientation()](#getOrientation--) | Lấy hướng của một trình giữ chỗ. |
| [getSize()](#getSize--) | Lấy kích thước của một trình giữ chỗ. |
| [getType()](#getType--) | Lấy loại của một trình giữ chỗ. |
| [getIndex()](#getIndex--) | Lấy chỉ mục của một trình giữ chỗ. |
### getOrientation() {#getOrientation--}
```
public abstract byte getOrientation()
```

Lấy hướng của một trình giữ chỗ. Chỉ đọc [Orientation](../../com.aspose.slides/orientation).

**Trả về:**
byte
### getSize() {#getSize--}
```
public abstract byte getSize()
```

Lấy kích thước của một trình giữ chỗ. Chỉ đọc [PlaceholderSize](../../com.aspose.slides/placeholdersize).

**Trả về:**
byte
### getType() {#getType--}
```
public abstract byte getType()
```

Lấy loại của một trình giữ chỗ. Chỉ đọc [PlaceholderType](../../com.aspose.slides/placeholdertype).

**Trả về:**
byte
### getIndex() {#getIndex--}
```
public abstract long getIndex()
```

Lấy chỉ mục của một trình giữ chỗ. Chỉ đọc long.

**Trả về:**
long