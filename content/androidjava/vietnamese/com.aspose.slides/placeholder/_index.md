---
title: Placeholder
second_title: Aspose.Slides cho Android thông qua Tham khảo API Java
description: Biểu diễn một trình giữ chỗ trên slide.
type: docs
url: /vi/com.aspose.slides/placeholder/
---
**Inheritance:**
java.lang.Object

**All Implemented Interfaces:**
[com.aspose.slides.IPlaceholder](../../com.aspose.slides/iplaceholder)
```
public class Placeholder implements IPlaceholder
```

Biểu diễn một trình giữ chỗ trên slide.
## Phương thức

| Method | Description |
| --- | --- |
| [getOrientation()](#getOrientation--) | Lấy hoặc đặt hướng của một trình giữ chỗ. |
| [getSize()](#getSize--) | Lấy hoặc đặt kích thước của một trình giữ chỗ. |
| [getType()](#getType--) | Lấy hoặc đặt loại của một trình giữ chỗ. |
| [getIndex()](#getIndex--) | Lấy hoặc đặt chỉ số của một trình giữ chỗ. |
### getOrientation() {#getOrientation--}
```
public byte getOrientation()
```

Lấy hoặc đặt hướng của một trình giữ chỗ. Chỉ đọc [Orientation](../../com.aspose.slides/orientation).

**Trả về:**
byte
### getSize() {#getSize--}
```
public byte getSize()
```

Lấy hoặc đặt kích thước của một trình giữ chỗ. Chỉ đọc [PlaceholderSize](../../com.aspose.slides/placeholdersize).

**Trả về:**
byte
### getType() {#getType--}
```
public byte getType()
```

Lấy hoặc đặt loại của một trình giữ chỗ. Chỉ đọc [PlaceholderType](../../com.aspose.slides/placeholdertype).

**Trả về:**
byte
### getIndex() {#getIndex--}
```
public long getIndex()
```

Lấy hoặc đặt chỉ số của một trình giữ chỗ. Chỉ đọc long.

**Trả về:**
long