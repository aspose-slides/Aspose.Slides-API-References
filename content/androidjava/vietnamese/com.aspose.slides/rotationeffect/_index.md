---
title: RotationEffect
second_title: Aspose.Slides cho Android qua Tham chiếu API Java
description: Mô tả hành vi quay của hiệu ứng.
type: docs
url: /vi/com.aspose.slides/rotationeffect/
---
**Inheritance:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**All Implemented Interfaces:**
[com.aspose.slides.IRotationEffect](../../com.aspose.slides/irotationeffect)
```
public class RotationEffect extends Behavior implements IRotationEffect
```

Mô tả hành vi quay của hiệu ứng.
## Hàm tạo

| Hàm tạo | Mô tả |
| --- | --- |
| [RotationEffect()](#RotationEffect--) |  |
## Phương thức

| Phương thức | Mô tả |
| --- | --- |
| [getFrom()](#getFrom--) | Mô tả giá trị bắt đầu cho hoạt ảnh. Đọc/ghi float. |
| [setFrom(float value)](#setFrom-float-) | Mô tả giá trị bắt đầu cho hoạt ảnh. Đọc/ghi float. |
| [getTo()](#getTo--) | Mô tả giá trị kết thúc cho hoạt ảnh. Đọc/ghi float. |
| [setTo(float value)](#setTo-float-) | Mô tả giá trị kết thúc cho hoạt ảnh. Đọc/ghi float. |
| [getBy()](#getBy--) | Mô tả giá trị độ lệch tương đối cho hoạt ảnh. Đọc/ghi float. |
| [setBy(float value)](#setBy-float-) | Mô tả giá trị độ lệch tương đối cho hoạt ảnh. Đọc/ghi float. |
### RotationEffect() {#RotationEffect--}
```
public RotationEffect()
```


### getFrom() {#getFrom--}
```
public final float getFrom()
```

Mô tả giá trị bắt đầu cho hoạt ảnh. Đọc/ghi float.

**Trả về:**
float
### setFrom(float value) {#setFrom-float-}
```
public final void setFrom(float value)
```

Mô tả giá trị bắt đầu cho hoạt ảnh. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getTo() {#getTo--}
```
public final float getTo()
```

Mô tả giá trị kết thúc cho hoạt ảnh. Đọc/ghi float.

**Trả về:**
float
### setTo(float value) {#setTo-float-}
```
public final void setTo(float value)
```

Mô tả giá trị kết thúc cho hoạt ảnh. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getBy() {#getBy--}
```
public final float getBy()
```

Mô tả giá trị độ lệch tương đối cho hoạt ảnh. Đọc/ghi float.

**Trả về:**
float
### setBy(float value) {#setBy-float-}
```
public final void setBy(float value)
```

Mô tả giá trị độ lệch tương đối cho hoạt ảnh. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |