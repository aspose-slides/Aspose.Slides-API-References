---
title: FillType
second_title: Aspose.Slides cho Android qua Tham chiếu API Java
description: Xác định loại nền bên trong của các đối tượng hình ảnh khác nhau.
type: docs
url: /vi/com.aspose.slides/filltype/
---
**Kế thừa:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class FillType extends System.Enum
```

Xác định loại nền bên trong của các đối tượng hình ảnh khác nhau.

## Trường

| Trường | Mô tả |
| --- | --- |
| [NotDefined](#NotDefined) | Kiểu nền chưa được xác định. |
| [NoFill](#NoFill) | Không có nền được áp dụng. |
| [Solid](#Solid) | Được điền bằng màu đồng nhất. |
| [Gradient](#Gradient) | Nền là dạng gradient. |
| [Pattern](#Pattern) | Mẫu lặp lại được sử dụng để điền vào đối tượng. |
| [Picture](#Picture) | Một hình ảnh duy nhất được sử dụng để điền vào đối tượng. |
| [Group](#Group) | Đối tượng hình ảnh kế thừa các thuộc tính nền từ nhóm. |

### NotDefined {#NotDefined}
```
public static final byte NotDefined
```

Kiểu nền chưa được xác định.

### NoFill {#NoFill}
```
public static final byte NoFill
```

Không có nền được áp dụng.

### Solid {#Solid}
```
public static final byte Solid
```

Được điền bằng màu đồng nhất.

### Gradient {#Gradient}
```
public static final byte Gradient
```

Nền là dạng gradient.

### Pattern {#Pattern}
```
public static final byte Pattern
```

Mẫu lặp lại được sử dụng để điền vào đối tượng.

### Picture {#Picture}
```
public static final byte Picture
```

Một hình ảnh duy nhất được sử dụng để điền vào đối tượng.

### Group {#Group}
```
public static final byte Group
```

Đối tượng hình ảnh kế thừa các thuộc tính nền từ nhóm.