---
title: ShapeStyle
second_title: Aspose.Slides cho Android thông qua Tham chiếu API Java
description: Tham chiếu kiểu dáng của hình dạng.
type: docs
url: /vi/com.aspose.slides/shapestyle/
---
**Kế thừa:**
java.lang.Object, com.aspose.slides.DomObject

**Tất cả các giao diện được triển khai:**
[com.aspose.slides.IShapeStyle](../../com.aspose.slides/ishapestyle)
```
public class ShapeStyle extends DomObject<Shape> implements IShapeStyle
```

Tham chiếu kiểu dáng của hình dạng.
## Phương thức

| Method | Description |
| --- | --- |
| [getLineColor()](#getLineColor--) | Trả về màu viền của hình dạng. |
| [getLineStyleIndex()](#getLineStyleIndex--) | Trả về hoặc đặt chỉ số cột của đường trong ma trận kiểu. |
| [setLineStyleIndex(int value)](#setLineStyleIndex-int-) | Trả về hoặc đặt chỉ số cột của đường trong ma trận kiểu. |
| [getFillColor()](#getFillColor--) | Trả về màu nền của hình dạng. |
| [getFillStyleIndex()](#getFillStyleIndex--) | Trả về hoặc đặt chỉ số cột nền của hình dạng trong các ma trận kiểu. |
| [setFillStyleIndex(short value)](#setFillStyleIndex-short-) | Trả về hoặc đặt chỉ số cột nền của hình dạng trong các ma trận kiểu. |
| [getEffectColor()](#getEffectColor--) | Trả về màu hiệu ứng của hình dạng. |
| [getEffectStyleIndex()](#getEffectStyleIndex--) | Trả về hoặc đặt chỉ số cột hiệu ứng của hình dạng trong ma trận kiểu. |
| [setEffectStyleIndex(long value)](#setEffectStyleIndex-long-) | Trả về hoặc đặt chỉ số cột hiệu ứng của hình dạng trong ma trận kiểu. |
| [getFontColor()](#getFontColor--) | Trả về màu phông chữ của hình dạng. |
| [getFontCollectionIndex()](#getFontCollectionIndex--) | Trả về hoặc đặt chỉ số phông chữ của hình dạng trong bộ sưu tập phông chữ. |
| [setFontCollectionIndex(byte value)](#setFontCollectionIndex-byte-) | Trả về hoặc đặt chỉ số phông chữ của hình dạng trong bộ sưu tập phông chữ. |
### getLineColor() {#getLineColor--}
```
public final IColorFormat getLineColor()
```

Trả về màu viền của hình dạng. Chỉ đọc [IColorFormat](../../com.aspose.slides/icolorformat).

**Trả về:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getLineStyleIndex() {#getLineStyleIndex--}
```
public final int getLineStyleIndex()
```

Trả về hoặc đặt chỉ số cột của đường trong ma trận kiểu. Đọc/ghi int.

**Trả về:**
int
### setLineStyleIndex(int value) {#setLineStyleIndex-int-}
```
public final void setLineStyleIndex(int value)
```

Trả về hoặc đặt chỉ số cột của đường trong ma trận kiểu. Đọc/ghi int.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | int |  |
### getFillColor() {#getFillColor--}
```
public final IColorFormat getFillColor()
```

Trả về màu nền của hình dạng. Chỉ đọc [IColorFormat](../../com.aspose.slides/icolorformat).

**Trả về:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getFillStyleIndex() {#getFillStyleIndex--}
```
public final short getFillStyleIndex()
```

Trả về hoặc đặt chỉ số cột nền của hình dạng trong các ma trận kiểu. 0 có nghĩa là không có nền, giá trị dương - chỉ số trong các kiểu nền của chủ đề, giá trị âm - chỉ số trong các kiểu nền nền của chủ đề. Đọc/ghi short.

**Trả về:**
short
### setFillStyleIndex(short value) {#setFillStyleIndex-short-}
```
public final void setFillStyleIndex(short value)
```

Trả về hoặc đặt chỉ số cột nền của hình dạng trong các ma trận kiểu. 0 có nghĩa là không có nền, giá trị dương - chỉ số trong các kiểu nền của chủ đề, giá trị âm - chỉ số trong các kiểu nền nền của chủ đề. Đọc/ghi short.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | short |  |
### getEffectColor() {#getEffectColor--}
```
public final IColorFormat getEffectColor()
```

Trả về màu hiệu ứng của hình dạng. Chỉ đọc [IColorFormat](../../com.aspose.slides/icolorformat).

**Trả về:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getEffectStyleIndex() {#getEffectStyleIndex--}
```
public final long getEffectStyleIndex()
```

Trả về hoặc đặt chỉ số cột hiệu ứng của hình dạng trong ma trận kiểu. Đọc/ghi long.

**Trả về:**
long
### setEffectStyleIndex(long value) {#setEffectStyleIndex-long-}
```
public final void setEffectStyleIndex(long value)
```

Trả về hoặc đặt chỉ số cột hiệu ứng của hình dạng trong ma trận kiểu. Đọc/ghi long.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | long |  |
### getFontColor() {#getFontColor--}
```
public final IColorFormat getFontColor()
```

Trả về màu phông chữ của hình dạng. Chỉ đọc [IColorFormat](../../com.aspose.slides/icolorformat).

**Trả về:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getFontCollectionIndex() {#getFontCollectionIndex--}
```
public final byte getFontCollectionIndex()
```

Trả về hoặc đặt chỉ số phông chữ của hình dạng trong bộ sưu tập phông chữ. Đọc/ghi [FontCollectionIndex](../../com.aspose.slides/fontcollectionindex).

**Trả về:**
byte
### setFontCollectionIndex(byte value) {#setFontCollectionIndex-byte-}
```
public final void setFontCollectionIndex(byte value)
```

Trả về hoặc đặt chỉ số phông chữ của hình dạng trong bộ sưu tập phông chữ. Đọc/ghi [FontCollectionIndex](../../com.aspose.slides/fontcollectionindex).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | byte |  |