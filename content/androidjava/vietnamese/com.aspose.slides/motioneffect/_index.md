---
title: MotionEffect
second_title: Tham khảo API Java Aspose.Slides cho Android
description: Đại diện cho hành vi hiệu ứng chuyển động của hiệu ứng.
type: docs
url: /vi/com.aspose.slides/motioneffect/
---
**Kế thừa:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**Tất cả các giao diện được thực thi:**
[com.aspose.slides.IMotionEffect](../../com.aspose.slides/imotioneffect)
```
public class MotionEffect extends Behavior implements IMotionEffect
```

Đại diện cho hành vi hiệu ứng chuyển động của hiệu ứng.

## Các hàm tạo

| Hàm tạo | Mô tả |
| --- | --- |
| [MotionEffect()](#MotionEffect--) |  |

## Các phương thức

| Phương thức | Mô tả |
| --- | --- |
| [getFrom()](#getFrom--) | Chỉ định tọa độ x/y để bắt đầu hoạt ảnh (theo phần trăm). |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | Chỉ định tọa độ x/y để bắt đầu hoạt ảnh (theo phần trăm). |
| [getTo()](#getTo--) | Chỉ định vị trí mục tiêu cho hiệu ứng chuyển động hoạt ảnh (theo phần trăm). |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | Chỉ định vị trí mục tiêu cho hiệu ứng chuyển động hoạt ảnh (theo phần trăm). |
| [getBy()](#getBy--) | Mô tả giá trị offset tương đối cho hoạt ảnh (theo phần trăm). |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | Mô tả giá trị offset tương đối cho hoạt ảnh (theo phần trăm). |
| [getRotationCenter()](#getRotationCenter--) | Mô tả trung tâm quay được sử dụng để quay đường chuyển động theo góc X. |
| [setRotationCenter(PointF value)](#setRotationCenter-android.graphics.PointF-) | Mô tả trung tâm quay được sử dụng để quay đường chuyển động theo góc X. |
| [getOrigin()](#getOrigin--) | Chỉ định nguồn gốc của đường chuyển động tương đối với bố cục slide hoặc phần tử cha. |
| [setOrigin(int value)](#setOrigin-int-) | Chỉ định nguồn gốc của đường chuyển động tương đối với bố cục slide hoặc phần tử cha. |
| [getPath()](#getPath--) | Chỉ định hình nguyên thủy của đường đi kèm với tọa độ cho chuyển động hoạt ảnh. |
| [setPath(IMotionPath value)](#setPath-com.aspose.slides.IMotionPath-) | Chỉ định hình nguyên thủy của đường đi kèm với tọa độ cho chuyển động hoạt ảnh. |
| [getPathEditMode()](#getPathEditMode--) | Chỉ định cách đường chuyển động di chuyển khi hình dạng được di chuyển. |
| [setPathEditMode(int value)](#setPathEditMode-int-) | Chỉ định cách đường chuyển động di chuyển khi hình dạng được di chuyển. |
| [getAngle()](#getAngle--) | Mô tả góc tương đối của đường chuyển động. |
| [setAngle(float value)](#setAngle-float-) | Mô tả góc tương đối của đường chuyển động. |

### MotionEffect() {#MotionEffect--}
```
public MotionEffect()
```

### getFrom() {#getFrom--}
```
public final PointF getFrom()
```

Chỉ định tọa độ x/y để bắt đầu hoạt ảnh (theo phần trăm). Đọc/ghi android.graphics.PointF.

**Trả về:**
android.graphics.PointF

### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public final void setFrom(PointF value)
```

Chỉ định tọa độ x/y để bắt đầu hoạt ảnh (theo phần trăm). Đọc/ghi android.graphics.PointF.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getTo() {#getTo--}
```
public final PointF getTo()
```

Chỉ định vị trí mục tiêu cho hiệu ứng chuyển động hoạt ảnh (theo phần trăm). Đọc/ghi android.graphics.PointF.

**Trả về:**
android.graphics.PointF

### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public final void setTo(PointF value)
```

Chỉ định vị trí mục tiêu cho hiệu ứng chuyển động hoạt ảnh (theo phần trăm). Đọc/ghi android.graphics.PointF.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getBy() {#getBy--}
```
public final PointF getBy()
```

Mô tả giá trị offset tương đối cho hoạt ảnh (theo phần trăm). Đọc/ghi android.graphics.PointF.

**Trả về:**
android.graphics.PointF

### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public final void setBy(PointF value)
```

Mô tả giá trị offset tương đối cho hoạt ảnh (theo phần trăm). Đọc/ghi android.graphics.PointF.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getRotationCenter() {#getRotationCenter--}
```
public final PointF getRotationCenter()
```

Mô tả trung tâm quay được sử dụng để quay đường chuyển động theo góc X. Đọc/ghi android.graphics.PointF.

**Trả về:**
android.graphics.PointF

### setRotationCenter(PointF value) {#setRotationCenter-android.graphics.PointF-}
```
public final void setRotationCenter(PointF value)
```

Mô tả trung tâm quay được sử dụng để quay đường chuyển động theo góc X. Đọc/ghi android.graphics.PointF.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | android.graphics.PointF |  |

### getOrigin() {#getOrigin--}
```
public final int getOrigin()
```

Chỉ định nguồn gốc của đường chuyển động tương đối với bố cục slide hoặc phần tử cha. Đọc/ghi [MotionOriginType](../../com.aspose.slides/motionorigintype).

**Trả về:**
int

### setOrigin(int value) {#setOrigin-int-}
```
public final void setOrigin(int value)
```

Chỉ định nguồn gốc của đường chuyển động tương đối với bố cục slide hoặc phần tử cha. Đọc/ghi [MotionOriginType](../../com.aspose.slides/motionorigintype).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | int |  |

### getPath() {#getPath--}
```
public final IMotionPath getPath()
```

Chỉ định hình nguyên thủy của đường đi kèm với tọa độ cho chuyển động hoạt ảnh. Đọc/ghi [IMotionPath](../../com.aspose.slides/imotionpath).

**Trả về:**
[IMotionPath](../../com.aspose.slides/imotionpath)

### setPath(IMotionPath value) {#setPath-com.aspose.slides.IMotionPath-}
```
public final void setPath(IMotionPath value)
```

Chỉ định hình nguyên thủy của đường đi kèm với tọa độ cho chuyển động hoạt ảnh. Đọc/ghi [IMotionPath](../../com.aspose.slides/imotionpath).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | [IMotionPath](../../com.aspose.slides/imotionpath) |  |

### getPathEditMode() {#getPathEditMode--}
```
public final int getPathEditMode()
```

Chỉ định cách đường chuyển động di chuyển khi hình dạng được di chuyển. Đọc/ghi [MotionPathEditMode](../../com.aspose.slides/motionpatheditmode).

**Trả về:**
int

### setPathEditMode(int value) {#setPathEditMode-int-}
```
public final void setPathEditMode(int value)
```

Chỉ định cách đường chuyển động di chuyển khi hình dạng được di chuyển. Đọc/ghi [MotionPathEditMode](../../com.aspose.slides/motionpatheditmode).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | int |  |

### getAngle() {#getAngle--}
```
public final float getAngle()
```

Mô tả góc tương đối của đường chuyển động. Đọc/ghi float.

**Trả về:**
float

### setAngle(float value) {#setAngle-float-}
```
public final void setAngle(float value)
```

Mô tả góc tương đối của đường chuyển động. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |