---
title: TextSearchOptions
second_title: Aspose.Slides cho Android qua Tham chiếu API Java
description: Đại diện cho các tùy chọn có thể được sử dụng để tìm kiếm văn bản trong Presentation Slide hoặc TextFrame.
type: docs
url: /vi/com.aspose.slides/textsearchoptions/
---
**Kế thừa:**
java.lang.Object

**Tất cả các giao diện được triển khai:**
[com.aspose.slides.ITextSearchOptions](../../com.aspose.slides/itextsearchoptions)
```
public class TextSearchOptions implements ITextSearchOptions
```

Đại diện cho các tùy chọn có thể được sử dụng để tìm kiếm văn bản trong Presentation, Slide hoặc TextFrame.

## Hàm tạo

| Constructor | Mô tả |
| --- | --- |
| [TextSearchOptions()](#TextSearchOptions--) | Tạo các tùy chọn tìm kiếm văn bản mặc định mới. |

## Phương thức

| Phương thức | Mô tả |
| --- | --- |
| [getCaseSensitive()](#getCaseSensitive--) | Đặt true để sử dụng tìm kiếm phân biệt chữ hoa/thường, false - ngược lại. |
| [setCaseSensitive(boolean value)](#setCaseSensitive-boolean-) | Đặt true để sử dụng tìm kiếm phân biệt chữ hoa/thường, false - ngược lại. |
| [getWholeWordsOnly()](#getWholeWordsOnly--) | Đặt true để chỉ khớp toàn bộ từ, false - ngược lại. |
| [setWholeWordsOnly(boolean value)](#setWholeWordsOnly-boolean-) | Đặt true để chỉ khớp toàn bộ từ, false - ngược lại. |
| [getIncludeNotes()](#getIncludeNotes--) | Đặt true để bao gồm văn bản có trong ghi chú slide ([NotesSlide](../../com.aspose.slides/notesslide)) khi thực hiện các thao tác tìm kiếm văn bản, thay thế hoặc tô sáng. |
| [setIncludeNotes(boolean value)](#setIncludeNotes-boolean-) | Đặt true để bao gồm văn bản có trong ghi chú slide ([NotesSlide](../../com.aspose.slides/notesslide)) khi thực hiện các thao tác tìm kiếm văn bản, thay thế hoặc tô sáng. |

### TextSearchOptions() {#TextSearchOptions--}
```
public TextSearchOptions()
```

Tạo các tùy chọn tìm kiếm văn bản mặc định mới.

### getCaseSensitive() {#getCaseSensitive--}
```
public final boolean getCaseSensitive()
```

Đặt true để sử dụng tìm kiếm phân biệt chữ hoa/thường, false - ngược lại. Đọc/ghi  boolean .

**Trả về:**
boolean

### setCaseSensitive(boolean value) {#setCaseSensitive-boolean-}
```
public final void setCaseSensitive(boolean value)
```

Đặt true để sử dụng tìm kiếm phân biệt chữ hoa/thường, false - ngược lại. Đọc/ghi  boolean .

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |

### getWholeWordsOnly() {#getWholeWordsOnly--}
```
public final boolean getWholeWordsOnly()
```

Đặt true để chỉ khớp toàn bộ từ, false - ngược lại. Đọc/ghi  boolean .

**Trả về:**
boolean

### setWholeWordsOnly(boolean value) {#setWholeWordsOnly-boolean-}
```
public final void setWholeWordsOnly(boolean value)
```

Đặt true để chỉ khớp toàn bộ từ, false - ngược lại. Đọc/ghi  boolean .

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |

### getIncludeNotes() {#getIncludeNotes--}
```
public final boolean getIncludeNotes()
```

Đặt true để bao gồm văn bản có trong ghi chú slide ([NotesSlide](../../com.aspose.slides/notesslide)) khi thực hiện các thao tác tìm kiếm văn bản, thay thế hoặc tô sáng. Giá trị mặc định là false.

**Trả về:**
boolean

### setIncludeNotes(boolean value) {#setIncludeNotes-boolean-}
```
public final void setIncludeNotes(boolean value)
```

Đặt true để bao gồm văn bản có trong ghi chú slide ([NotesSlide](../../com.aspose.slides/notesslide)) khi thực hiện các thao tác tìm kiếm văn bản, thay thế hoặc tô sáng. Giá trị mặc định là false.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |