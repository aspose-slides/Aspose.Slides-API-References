---
title: IDataTable
second_title: Aspose.Slides cho Android qua Tham chiếu API Java
description: Biểu diễn các thuộc tính của bảng dữ liệu.
type: docs
url: /vi/com.aspose.slides/idatatable/
---
**Tất cả các giao diện đã thực hiện:**
[com.aspose.slides.IFormattedTextContainer](../../com.aspose.slides/iformattedtextcontainer)
```
public interface IDataTable extends IFormattedTextContainer
```

Biểu diễn các thuộc tính của bảng dữ liệu.
## Phương thức

| Phương thức | Mô tả |
| --- | --- |
| [getFormat()](#getFormat--) | Trả về các kiểu đường, tô và hiệu ứng của đối tượng. |
| [hasBorderHorizontal()](#hasBorderHorizontal--) | Đúng nếu bảng dữ liệu biểu đồ có viền ô ngang. |
| [setBorderHorizontal(boolean value)](#setBorderHorizontal-boolean-) | Đúng nếu bảng dữ liệu biểu đồ có viền ô ngang. |
| [hasBorderOutline()](#hasBorderOutline--) | Đúng nếu bảng dữ liệu biểu đồ có viền bao quanh. |
| [setBorderOutline(boolean value)](#setBorderOutline-boolean-) | Đúng nếu bảng dữ liệu biểu đồ có viền bao quanh. |
| [hasBorderVertical()](#hasBorderVertical--) | Đúng nếu bảng dữ liệu biểu đồ có viền ô dọc. |
| [setBorderVertical(boolean value)](#setBorderVertical-boolean-) | Đúng nếu bảng dữ liệu biểu đồ có viền ô dọc. |
| [getShowLegendKey()](#getShowLegendKey--) | Đúng nếu khóa chú giải nhãn dữ liệu được hiển thị. |
| [setShowLegendKey(boolean value)](#setShowLegendKey-boolean-) | Đúng nếu khóa chú giải nhãn dữ liệu được hiển thị. |
### getFormat() {#getFormat--}
```
public abstract IFormat getFormat()
```

Trả về các kiểu đường, tô và hiệu ứng của đối tượng. Chỉ đọc [IFormat](../../com.aspose.slides/iformat).

**Trả về:**
[IFormat](../../com.aspose.slides/iformat)
### hasBorderHorizontal() {#hasBorderHorizontal--}
```
public abstract boolean hasBorderHorizontal()
```

Đúng nếu bảng dữ liệu biểu đồ có viền ô ngang. Đọc/ghi boolean.

**Trả về:**
boolean
### setBorderHorizontal(boolean value) {#setBorderHorizontal-boolean-}
```
public abstract void setBorderHorizontal(boolean value)
```

Đúng nếu bảng dữ liệu biểu đồ có viền ô ngang. Đọc/ghi boolean.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |

### hasBorderOutline() {#hasBorderOutline--}
```
public abstract boolean hasBorderOutline()
```

Đúng nếu bảng dữ liệu biểu đồ có viền bao quanh. Đọc/ghi boolean.

**Trả về:**
boolean
### setBorderOutline(boolean value) {#setBorderOutline-boolean-}
```
public abstract void setBorderOutline(boolean value)
```

Đúng nếu bảng dữ liệu biểu đồ có viền bao quanh. Đọc/ghi boolean.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |

### hasBorderVertical() {#hasBorderVertical--}
```
public abstract boolean hasBorderVertical()
```

Đúng nếu bảng dữ liệu biểu đồ có viền ô dọc. Đọc/ghi boolean.

**Trả về:**
boolean
### setBorderVertical(boolean value) {#setBorderVertical-boolean-}
```
public abstract void setBorderVertical(boolean value)
```

Đúng nếu bảng dữ liệu biểu đồ có viền ô dọc. Đọc/ghi boolean.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |

### getShowLegendKey() {#getShowLegendKey--}
```
public abstract boolean getShowLegendKey()
```

Đúng nếu khóa chú giải nhãn dữ liệu được hiển thị. Đọc/ghi boolean.

**Trả về:**
boolean
### setShowLegendKey(boolean value) {#setShowLegendKey-boolean-}
```
public abstract void setShowLegendKey(boolean value)
```

Đúng nếu khóa chú giải nhãn dữ liệu được hiển thị. Đọc/ghi boolean.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |