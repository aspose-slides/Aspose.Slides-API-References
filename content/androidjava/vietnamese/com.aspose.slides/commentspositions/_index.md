---
title: CommentsPositions
second_title: Aspose.Slides cho Android qua Tham chiếu API Java
description: Đại diện cho quy tắc hiển thị bình luận vào tài liệu xuất khẩu
type: docs
url: /vi/com.aspose.slides/commentspositions/
---
**Kế thừa:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class CommentsPositions extends System.Enum
```

Đại diện cho quy tắc hiển thị bình luận vào tài liệu xuất khẩu
## Các trường

| Trường | Mô tả |
| --- | --- |
| [None](#None) | Chỉ định rằng bình luận không được hiển thị hoàn toàn. |
| [Bottom](#Bottom) | Chỉ định rằng bình luận sẽ được hiển thị ở cuối trang. |
| [Right](#Right) | Chỉ định rằng bình luận sẽ được hiển thị ở phía bên phải của trang. |
### Không {#None}
```
public static final int None
```

Chỉ định rằng bình luận không được hiển thị hoàn toàn.

### Dưới {#Bottom}
```
public static final int Bottom
```

Chỉ định rằng bình luận sẽ được hiển thị ở cuối trang.

### Bên phải {#Right}
```
public static final int Right
```

Chỉ định rằng bình luận sẽ được hiển thị ở phía bên phải của trang.