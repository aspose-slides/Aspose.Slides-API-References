---
title: MarkerStyleType
second_title: Aspose.Slides cho Android thông qua Tham chiếu API Java
description: Xác định dạng của dấu đánh dấu trên điểm dữ liệu của biểu đồ.
type: docs
url: /vi/com.aspose.slides/markerstyletype/
---
**Kế thừa:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class MarkerStyleType extends System.Enum
```

Xác định dạng của dấu đánh dấu trên điểm dữ liệu của biểu đồ.
## Trường

| Trường | Mô tả |
| --- | --- |
| [NotDefined](#NotDefined) |  |
| [Circle](#Circle) | Xác định sẽ vẽ một vòng tròn tại mỗi điểm dữ liệu. |
| [Dash](#Dash) | Xác định sẽ vẽ một dấu gạch ngang tại mỗi điểm dữ liệu. |
| [Diamond](#Diamond) | Xác định sẽ vẽ một hình thoi tại mỗi điểm dữ liệu. |
| [Dot](#Dot) | Xác định sẽ vẽ một dấu chấm tại mỗi điểm dữ liệu. |
| [None](#None) | Xác định sẽ không vẽ gì tại mỗi điểm dữ liệu. |
| [Picture](#Picture) | Xác định sẽ vẽ một hình ảnh tại mỗi điểm dữ liệu. |
| [Plus](#Plus) | Xác định sẽ vẽ một dấu cộng tại mỗi điểm dữ liệu. |
| [Square](#Square) | Xác định sẽ vẽ một hình vuông tại mỗi điểm dữ liệu. |
| [Star](#Star) | Xác định sẽ vẽ một ngôi sao tại mỗi điểm dữ liệu. |
| [Triangle](#Triangle) | Xác định sẽ vẽ một hình tam giác tại mỗi điểm dữ liệu. |
| [X](#X) | Xác định sẽ vẽ một ký tự X tại mỗi điểm dữ liệu. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```




### Circle {#Circle}
```
public static final int Circle
```


Xác định sẽ vẽ một vòng tròn tại mỗi điểm dữ liệu.

### Dash {#Dash}
```
public static final int Dash
```


Xác định sẽ vẽ một dấu gạch ngang tại mỗi điểm dữ liệu.

### Diamond {#Diamond}
```
public static final int Diamond
```


Xác định sẽ vẽ một hình thoi tại mỗi điểm dữ liệu.

### Dot {#Dot}
```
public static final int Dot
```


Xác định sẽ vẽ một dấu chấm tại mỗi điểm dữ liệu.

### None {#None}
```
public static final int None
```


Xác định sẽ không vẽ gì tại mỗi điểm dữ liệu.

### Picture {#Picture}
```
public static final int Picture
```


Xác định sẽ vẽ một hình ảnh tại mỗi điểm dữ liệu.

### Plus {#Plus}
```
public static final int Plus
```


Xác định sẽ vẽ một dấu cộng tại mỗi điểm dữ liệu.

### Square {#Square}
```
public static final int Square
```


Xác định sẽ vẽ một hình vuông tại mỗi điểm dữ liệu.

### Star {#Star}
```
public static final int Star
```


Xác định sẽ vẽ một ngôi sao tại mỗi điểm dữ liệu.

### Triangle {#Triangle}
```
public static final int Triangle
```


Xác định sẽ vẽ một hình tam giác tại mỗi điểm dữ liệu.

### X {#X}
```
public static final int X
```


Xác định sẽ vẽ một ký tự X tại mỗi điểm dữ liệu.