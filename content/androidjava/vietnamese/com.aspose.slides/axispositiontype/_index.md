---
title: AxisPositionType
second_title: Aspose.Slides cho Android qua Tham chiếu API Java
description: Xác định vị trí của trục.
type: docs
url: /vi/com.aspose.slides/axispositiontype/
---
**Kế thừa:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class AxisPositionType extends System.Enum
```

Xác định vị trí của trục.
## Trường

| Trường | Mô tả |
| --- | --- |
| [Bottom](#Bottom) | Chỉ định rằng trục sẽ được hiển thị ở phía dưới của vùng biểu đồ. |
| [Left](#Left) | Chỉ định rằng trục sẽ được hiển thị ở phía trái của vùng biểu đồ. |
| [Right](#Right) | Chỉ định rằng trục sẽ được hiển thị ở phía phải của vùng biểu đồ. |
| [Top](#Top) | Chỉ định rằng trục sẽ được hiển thị ở phía trên của vùng biểu đồ. |
### Dưới {#Bottom}
```
public static final int Bottom
```

Chỉ định rằng trục sẽ được hiển thị ở phía dưới của vùng biểu đồ.

### Trái {#Left}
```
public static final int Left
```

Chỉ định rằng trục sẽ được hiển thị ở phía trái của vùng biểu đồ.

### Phải {#Right}
```
public static final int Right
```

Chỉ định rằng trục sẽ được hiển thị ở phía phải của vùng biểu đồ.

### Trên {#Top}
```
public static final int Top
```

Chỉ định rằng trục sẽ được hiển thị ở phía trên của vùng biểu đồ.