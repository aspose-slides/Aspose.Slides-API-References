---
title: Timing
second_title: Tham chiếu API Java cho Aspose.Slides trên Android
description: Biểu diễn thời gian hoạt ảnh.
type: docs
url: /vi/com.aspose.slides/timing/
---
**Kế thừa:**
java.lang.Object

**Tất cả giao diện được triển khai:**
[com.aspose.slides.ITiming](../../com.aspose.slides/itiming), com.aspose.slides.IDOMObject
```
public class Timing implements ITiming, IDOMObject
```

Biểu diễn thời gian hoạt ảnh.
## Phương thức

| Phương thức | Mô tả |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | Mô tả tỷ lệ phần trăm của hiệu ứng hành vi tăng tốc trong thời lượng. |
| [setAccelerate(float value)](#setAccelerate-float-) | Mô tả tỷ lệ phần trăm của hiệu ứng hành vi tăng tốc trong thời lượng. |
| [getDecelerate()](#getDecelerate--) | Mô tả tỷ lệ phần trăm của hiệu ứng hành vi giảm tốc trong thời lượng. |
| [setDecelerate(float value)](#setDecelerate-float-) | Mô tả tỷ lệ phần trăm của hiệu ứng hành vi giảm tốc trong thời lượng. |
| [getAutoReverse()](#getAutoReverse--) | Mô tả việc tự động phát hoạt ảnh ngược lại sau khi đã phát theo hướng tiến. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | Mô tả việc tự động phát hoạt ảnh ngược lại sau khi đã phát theo hướng tiến. |
| [getDuration()](#getDuration--) | Mô tả thời lượng của hiệu ứng hoạt ảnh. |
| [setDuration(float value)](#setDuration-float-) | Mô tả thời lượng của hiệu ứng hoạt ảnh. |
| [getRepeatCount()](#getRepeatCount--) | Mô tả số lần hiệu ứng nên lặp lại. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | Mô tả số lần hiệu ứng nên lặp lại. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | Thuộc tính này chỉ định liệu hiệu ứng có lặp lại cho đến khi kết thúc slide hay không. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | Thuộc tính này chỉ định liệu hiệu ứng có lặp lại cho đến khi kết thúc slide hay không. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | Thuộc tính này chỉ định liệu hiệu ứng có lặp lại cho đến lần nhấn tiếp theo hay không. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | Thuộc tính này chỉ định liệu hiệu ứng có lặp lại cho đến lần nhấn tiếp theo hay không. |
| [getRepeatDuration()](#getRepeatDuration--) | Mô tả số lần hiệu ứng nên lặp lại. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | Mô tả số lần hiệu ứng nên lặp lại. |
| [getRestart()](#getRestart--) | Chỉ định liệu một hiệu ứng có nên khởi động lại sau khi hoàn thành hay không. |
| [setRestart(int value)](#setRestart-int-) | Chỉ định liệu một hiệu ứng có nên khởi động lại sau khi hoàn thành hay không. |
| [getRewind()](#getRewind--) | Thuộc tính này chỉ định liệu hiệu ứng có tua lại khi phát xong hay không. |
| [setRewind(boolean value)](#setRewind-boolean-) | Thuộc tính này chỉ định liệu hiệu ứng có tua lại khi phát xong hay không. |
| [getSpeed()](#getSpeed--) | Chỉ định tỷ lệ phần trăm để tăng (hoặc giảm) tốc độ thời gian. |
| [setSpeed(float value)](#setSpeed-float-) | Chỉ định tỷ lệ phần trăm để tăng (hoặc giảm) tốc độ thời gian. |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | Mô tả thời gian trễ sau khi kích hoạt. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | Mô tả thời gian trễ sau khi kích hoạt. |
| [getTriggerType()](#getTriggerType--) | Mô tả loại kích hoạt. |
| [setTriggerType(int value)](#setTriggerType-int-) | Mô tả loại kích hoạt. |
| [getParent_Immediate()](#getParent-Immediate--) |  |

### getAccelerate() {#getAccelerate--}
```
public final float getAccelerate()
```

Mô tả tỷ lệ phần trăm của hiệu ứng hành vi tăng tốc trong thời lượng. Đọc/ghi float.

**Trả về:**
float

### setAccelerate(float value) {#setAccelerate-float-}
```
public final void setAccelerate(float value)
```

Mô tả tỷ lệ phần trăm của hiệu ứng hành vi tăng tốc trong thời lượng. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public final float getDecelerate()
```

Mô tả tỷ lệ phần trăm của hiệu ứng hành vi giảm tốc trong thời lượng. Đọc/ghi float.

**Trả về:**
float

### setDecelerate(float value) {#setDecelerate-float-}
```
public final void setDecelerate(float value)
```

Mô tả tỷ lệ phần trăm của hiệu ứng hành vi giảm tốc trong thời lượng. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public final boolean getAutoReverse()
```

Mô tả việc tự động phát hoạt ảnh ngược lại sau khi đã phát theo hướng tiến. Đọc/ghi boolean.

**Trả về:**
boolean

### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public final void setAutoReverse(boolean value)
```

Mô tả việc tự động phát hoạt ảnh ngược lại sau khi đã phát theo hướng tiến. Đọc/ghi boolean.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public final float getDuration()
```

Mô tả thời lượng của hiệu ứng hoạt ảnh. Đọc/ghi float.

**Trả về:**
float

### setDuration(float value) {#setDuration-float-}
```
public final void setDuration(float value)
```

Mô tả thời lượng của hiệu ứng hoạt ảnh. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public final float getRepeatCount()
```

Mô tả số lần hiệu ứng nên lặp lại. Đọc/ghi float.

**Trả về:**
float

### setRepeatCount(float value) {#setRepeatCount-float-}
```
public final void setRepeatCount(float value)
```

Mô tả số lần hiệu ứng nên lặp lại. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public final boolean getRepeatUntilEndSlide()
```

Thuộc tính này chỉ định liệu hiệu ứng có lặp lại cho đến khi kết thúc slide hay không. Đọc/ghi boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Lấy chuỗi hiệu ứng cho slide đầu tiên
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Lấy hiệu ứng đầu tiên của chuỗi chính.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Thay đổi Timing/Repeat của hiệu ứng thành "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Trả về:**
boolean

### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public final void setRepeatUntilEndSlide(boolean value)
```

Thuộc tính này chỉ định liệu hiệu ứng có lặp lại cho đến khi kết thúc slide hay không. Đọc/ghi boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Lấy chuỗi hiệu ứng cho slide đầu tiên
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Lấy hiệu ứng đầu tiên của chuỗi chính.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Thay đổi Timing/Repeat của hiệu ứng thành "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public final boolean getRepeatUntilNextClick()
```

Thuộc tính này chỉ định liệu hiệu ứng có lặp lại cho đến lần nhấn tiếp theo hay không. Đọc/ghi boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Lấy chuỗi hiệu ứng cho slide đầu tiên
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Lấy hiệu ứng đầu tiên của chuỗi chính.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Thay đổi Timing/Repeat của hiệu ứng thành "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Trả về:**
boolean

### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public final void setRepeatUntilNextClick(boolean value)
```

Thuộc tính này chỉ định liệu hiệu ứng có lặp lại cho đến lần nhấn tiếp theo hay không. Đọc/ghi boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Lấy chuỗi hiệu ứng cho slide đầu tiên
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Lấy hiệu ứng đầu tiên của chuỗi chính.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Thay đổi Timing/Repeat của hiệu ứng thành "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public final float getRepeatDuration()
```

Mô tả số lần hiệu ứng nên lặp lại. Đọc/ghi float.

**Trả về:**
float

### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public final void setRepeatDuration(float value)
```

Mô tả số lần hiệu ứng nên lặp lại. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public final int getRestart()
```

Chỉ định liệu một hiệu ứng có nên khởi động lại sau khi hoàn thành. Đọc/ghi [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Trả về:**
int

### setRestart(int value) {#setRestart-int-}
```
public final void setRestart(int value)
```

Chỉ định liệu một hiệu ứng có nên khởi động lại sau khi hoàn thành. Đọc/ghi [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public final boolean getRewind()
```

Thuộc tính này chỉ định liệu hiệu ứng có tua lại khi phát xong hay không. Đọc/ghi boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Lấy chuỗi hiệu ứng cho slide đầu tiên
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Lấy hiệu ứng đầu tiên của chuỗi chính.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Bật tính năng Timing/Rewind của hiệu ứng.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Trả về:**
boolean

### setRewind(boolean value) {#setRewind-boolean-}
```
public final void setRewind(boolean value)
```

Thuộc tính này chỉ định liệu hiệu ứng có tua lại khi phát xong hay không. Đọc/ghi boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Lấy chuỗi hiệu ứng cho slide đầu tiên
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Lấy hiệu ứng đầu tiên của chuỗi chính.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Bật tính năng Timing/Rewind của hiệu ứng.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |

### getSpeed() {#getSpeed--}
```
public final float getSpeed()
```

Chỉ định tỷ lệ phần trăm để tăng (hoặc giảm) tốc độ thời gian. Đọc/ghi float.

**Trả về:**
float

### setSpeed(float value) {#setSpeed-float-}
```
public final void setSpeed(float value)
```

Chỉ định tỷ lệ phần trăm để tăng (hoặc giảm) tốc độ thời gian. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public final float getTriggerDelayTime()
```

Mô tả thời gian trễ sau khi kích hoạt. Đọc/ghi float.

**Trả về:**
float

### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public final void setTriggerDelayTime(float value)
```

Mô tả thời gian trễ sau khi kích hoạt. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public final int getTriggerType()
```

Mô tả loại kích hoạt. Đọc/ghi [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Trả về:**
int

### setTriggerType(int value) {#setTriggerType-int-}
```
public final void setTriggerType(int value)
```

Mô tả loại kích hoạt. Đọc/ghi [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | int |  |

### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

Trả về đối tượng Parent_Immediate. Chỉ-đọc IDOMObject.

**Trả về:**
com.aspose.slides.IDOMObject