---
title: Luminance
second_title: Aspose.Slides cho Android qua Tham chiếu API Java
description: Đại diện cho hiệu ứng Độ sáng.
type: docs
url: /vi/com.aspose.slides/luminance/
---
**Kế thừa:**
java.lang.Object, [com.aspose.slides.PVIObject](../../com.aspose.slides/pviobject), [com.aspose.slides.ImageTransformOperation](../../com.aspose.slides/imagetransformoperation)

**Tất cả các giao diện được triển khai:**
[com.aspose.slides.ILuminance](../../com.aspose.slides/iluminance), com.aspose.slides.IVisualEffect
```
public final class Luminance extends ImageTransformOperation implements ILuminance, IVisualEffect
```

Đại diện cho hiệu ứng Độ sáng. Độ sáng dịch tuyến tính tất cả các màu gần hơn tới màu trắng hoặc màu đen. Độ tương phản thay đổi tỉ lệ các màu để chúng gần nhau hơn hoặc cách nhau hơn.
## Phương thức

| Phương thức | Mô tả |
| --- | --- |
| [getEffective()](#getEffective--) | Lấy dữ liệu hiệu ứng Độ sáng thực tế với kế thừa đã được áp dụng. |
| [equals(Object obj)](#equals-java.lang.Object-) | Xác định xem [Luminance](../../com.aspose.slides/luminance) được chỉ định có bằng với [Luminance](../../com.aspose.slides/luminance) hiện tại hay không. |
| [hashCode()](#hashCode--) | Đóng vai trò là hàm băm cho một kiểu cụ thể. |
### getEffective() {#getEffective--}
```
public final ILuminanceEffectiveData getEffective()
```


Lấy dữ liệu hiệu ứng Độ sáng thực tế với kế thừa đã được áp dụng.

**Giá trị trả về:**
[ILuminanceEffectiveData](../../com.aspose.slides/iluminanceeffectivedata) - Một [ILuminanceEffectiveData](../../com.aspose.slides/iluminanceeffectivedata).
### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```


Xác định xem [Luminance](../../com.aspose.slides/luminance) được chỉ định có bằng với [Luminance](../../com.aspose.slides/luminance) hiện tại hay không.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| obj | java.lang.Object | Đối tượng [Luminance](../../com.aspose.slides/luminance) để so sánh. |

**Giá trị trả về:**
boolean - true nếu các đối tượng bằng nhau; ngược lại, false.
### hashCode() {#hashCode--}
```
public int hashCode()
```


Đóng vai trò là hàm băm cho một kiểu cụ thể.

**Giá trị trả về:**
int - Một mã băm cho đối tượng hiện tại.