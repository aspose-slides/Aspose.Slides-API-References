---
title: IOuterShadowEffectiveData
second_title: Aspose.Slides cho Android thông qua Tham chiếu API Java
description: Đối tượng bất biến đại diện cho hiệu ứng bóng ngoài.
type: docs
url: /vi/com.aspose.slides/ioutershadoweffectivedata/
---
**Tất cả các giao diện được triển khai:**
[com.aspose.slides.IEffectEffectiveData](../../com.aspose.slides/ieffecteffectivedata)
```
public interface IOuterShadowEffectiveData extends IEffectEffectiveData
```

Đối tượng bất biến đại diện cho hiệu ứng bóng ngoài.
## Phương thức

| Phương thức | Mô tả |
| --- | --- |
| [getBlurRadius()](#getBlurRadius--) | Bán kính làm mờ. |
| [getDirection()](#getDirection--) | Hướng của bóng. |
| [getDistance()](#getDistance--) | Khoảng cách của bóng. |
| [getShadowColor()](#getShadowColor--) | Màu của bóng. |
| [getRectangleAlign()](#getRectangleAlign--) | Căn hình chữ nhật. |
| [getSkewHorizontal()](#getSkewHorizontal--) | Xác định góc nghiêng ngang (độ). |
| [getSkewVertical()](#getSkewVertical--) | Xác định góc nghiêng dọc (độ). |
| [getRotateShadowWithShape()](#getRotateShadowWithShape--) | Xác định liệu bóng có quay cùng hình dạng nếu hình dạng bị quay hay không. |
| [getScaleHorizontal()](#getScaleHorizontal--) | Xác định hệ số co dọc chiều ngang, hệ số âm gây lật. |
| [getScaleVertical()](#getScaleVertical--) | Xác định hệ số co dọc chiều dọc, hệ số âm gây lật. |
### getBlurRadius() {#getBlurRadius--}
```
public abstract double getBlurRadius()
```

Bán kính làm mờ. Chỉ đọc double.

**Trả về:**
double
### getDirection() {#getDirection--}
```
public abstract float getDirection()
```

Hướng của bóng. Chỉ đọc float.

**Trả về:**
float
### getDistance() {#getDistance--}
```
public abstract double getDistance()
```

Khoảng cách của bóng. Chỉ đọc double.

**Trả về:**
double
### getShadowColor() {#getShadowColor--}
```
public abstract Integer getShadowColor()
```

Màu của bóng. Chỉ đọc java.lang.Integer.

**Trả về:**
java.lang.Integer
### getRectangleAlign() {#getRectangleAlign--}
```
public abstract byte getRectangleAlign()
```

Căn hình chữ nhật. Chỉ đọc [RectangleAlignment](../../com.aspose.slides/rectanglealignment).

**Trả về:**
byte
### getSkewHorizontal() {#getSkewHorizontal--}
```
public abstract double getSkewHorizontal()
```

Xác định góc nghiêng ngang (độ). Chỉ đọc double.

**Trả về:**
double
### getSkewVertical() {#getSkewVertical--}
```
public abstract double getSkewVertical()
```

Xác định góc nghiêng dọc (độ). Chỉ đọc double.

**Trả về:**
double
### getRotateShadowWithShape() {#getRotateShadowWithShape--}
```
public abstract boolean getRotateShadowWithShape()
```

Xác định liệu bóng có quay cùng hình dạng nếu hình dạng bị quay hay không. Chỉ đọc boolean.

**Trả về:**
boolean
### getScaleHorizontal() {#getScaleHorizontal--}
```
public abstract double getScaleHorizontal()
```

Xác định hệ số co dọc chiều ngang, hệ số âm gây lật. Chỉ đọc double.

**Trả về:**
double
### getScaleVertical() {#getScaleVertical--}
```
public abstract double getScaleVertical()
```

Xác định hệ số co dọc chiều dọc, hệ số âm gây lật. Chỉ đọc double.

**Trả về:**
double