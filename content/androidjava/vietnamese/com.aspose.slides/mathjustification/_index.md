---
title: MathJustification
second_title: Aspose.Slides cho Android qua Tham chiếu API Java
description: Xác định cách căn chỉnh của đoạn văn toán học, một chuỗi các thể hiện liền kề của văn bản toán học trong cùng một đoạn
type: docs
url: /vi/com.aspose.slides/mathjustification/
---
**Kế thừa:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class MathJustification extends System.Enum
```

Xác định cách căn chỉnh của đoạn văn toán học (một chuỗi các thể hiện liền kề của văn bản toán học trong cùng một đoạn)

## Fields

| Field | Description |
| --- | --- |
| [LeftJustified](#LeftJustified) | Căn lề trái cho văn bản toán học |
| [RightJustified](#RightJustified) | Căn lề phải cho văn bản toán học |
| [Centered](#Centered) | Căn lề trung tâm cho văn bản toán học |
| [CenteredAsGroup](#CenteredAsGroup) | Canh giữa trên trang toàn bộ nhóm khối toán học như một đơn vị duy nhất. |

### LeftJustified {#LeftJustified}
```
public static final int LeftJustified
```

Căn lề trái cho văn bản toán học

### RightJustified {#RightJustified}
```
public static final int RightJustified
```

Căn lề phải cho văn bản toán học

### Centered {#Centered}
```
public static final int Centered
```

Căn lề trung tâm cho văn bản toán học

### CenteredAsGroup {#CenteredAsGroup}
```
public static final int CenteredAsGroup
```

Canh giữa trên trang toàn bộ nhóm khối toán học như một đơn vị duy nhất. Điều này có nghĩa là các thể hiện của văn bản toán học có thể được căn chỉnh với nhau, nhưng toàn bộ nhóm văn bản toán học được canh giữa như một khối.