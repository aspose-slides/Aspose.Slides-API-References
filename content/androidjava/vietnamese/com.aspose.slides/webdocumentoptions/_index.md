---
title: WebDocumentOptions
second_title: Aspose.Slides cho Android qua Tham chiếu API Java
description: Biểu diễn một bộ tùy chọn cho việc lưu.
type: docs
url: /vi/com.aspose.slides/webdocumentoptions/
---
**Inheritance:**
java.lang.Object
```
public class WebDocumentOptions
```

Biểu diễn một bộ tùy chọn cho việc lưu [WebDocument](../../com.aspose.slides/webdocument).

## Hàm tạo

| Hàm tạo | Mô tả |
| --- | --- |
| [WebDocumentOptions()](#WebDocumentOptions--) |  |

## Phương thức

| Phương thức | Mô tả |
| --- | --- |
| [getTemplateEngine()](#getTemplateEngine--) | Trả về hoặc đặt công cụ mẫu. |
| [setTemplateEngine(ITemplateEngine value)](#setTemplateEngine-com.aspose.slides.ITemplateEngine-) | Trả về hoặc đặt công cụ mẫu. |
| [getOutputSaver()](#getOutputSaver--) | Trả về hoặc đặt trình lưu đầu ra. |
| [setOutputSaver(IOutputSaver value)](#setOutputSaver-com.aspose.slides.IOutputSaver-) | Trả về hoặc đặt trình lưu đầu ra. |
| [getEmbedImages()](#getEmbedImages--) | Trả về hoặc đặt tùy chọn nhúng hình ảnh. |
| [setEmbedImages(boolean value)](#setEmbedImages-boolean-) | Trả về hoặc đặt tùy chọn nhúng hình ảnh. |
| [getAnimateTransitions()](#getAnimateTransitions--) | Trả về hoặc đặt tùy chọn hoạt ảnh chuyển tiếp. |
| [setAnimateTransitions(boolean value)](#setAnimateTransitions-boolean-) | Trả về hoặc đặt tùy chọn hoạt ảnh chuyển tiếp. |
| [getAnimateShapes()](#getAnimateShapes--) | Trả về hoặc đặt tùy chọn hoạt ảnh hình dạng. |
| [setAnimateShapes(boolean value)](#setAnimateShapes-boolean-) | Trả về hoặc đặt tùy chọn hoạt ảnh hình dạng. |

### WebDocumentOptions() {#WebDocumentOptions--}
```
public WebDocumentOptions()
```

### getTemplateEngine() {#getTemplateEngine--}
```
public final ITemplateEngine getTemplateEngine()
```

Trả về hoặc đặt công cụ mẫu. Đọc/ghi [ITemplateEngine](../../com.aspose.slides/itemplateengine).

**Giá trị trả về:**
[ITemplateEngine](../../com.aspose.slides/itemplateengine)

### setTemplateEngine(ITemplateEngine value) {#setTemplateEngine-com.aspose.slides.ITemplateEngine-}
```
public final void setTemplateEngine(ITemplateEngine value)
```

Trả về hoặc đặt công cụ mẫu. Đọc/ghi [ITemplateEngine](../../com.aspose.slides/itemplateengine).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | [ITemplateEngine](../../com.aspose.slides/itemplateengine) |  |

### getOutputSaver() {#getOutputSaver--}
```
public final IOutputSaver getOutputSaver()
```

Trả về hoặc đặt trình lưu đầu ra. Đọc/ghi [IOutputSaver](../../com.aspose.slides/ioutputsaver).

**Giá trị trả về:**
[IOutputSaver](../../com.aspose.slides/ioutputsaver)

### setOutputSaver(IOutputSaver value) {#setOutputSaver-com.aspose.slides.IOutputSaver-}
```
public final void setOutputSaver(IOutputSaver value)
```

Trả về hoặc đặt trình lưu đầu ra. Đọc/ghi [IOutputSaver](../../com.aspose.slides/ioutputsaver).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | [IOutputSaver](../../com.aspose.slides/ioutputsaver) |  |

### getEmbedImages() {#getEmbedImages--}
```
public final boolean getEmbedImages()
```

Trả về hoặc đặt tùy chọn nhúng hình ảnh. Đọc/ghi boolean.

**Giá trị trả về:**
boolean

### setEmbedImages(boolean value) {#setEmbedImages-boolean-}
```
public final void setEmbedImages(boolean value)
```

Trả về hoặc đặt tùy chọn nhúng hình ảnh. Đọc/ghi boolean.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |

### getAnimateTransitions() {#getAnimateTransitions--}
```
public final boolean getAnimateTransitions()
```

Trả về hoặc đặt tùy chọn hoạt ảnh chuyển tiếp. Đọc/ghi boolean.

**Giá trị trả về:**
boolean

### setAnimateTransitions(boolean value) {#setAnimateTransitions-boolean-}
```
public final void setAnimateTransitions(boolean value)
```

Trả về hoặc đặt tùy chọn hoạt ảnh chuyển tiếp. Đọc/ghi boolean.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |

### getAnimateShapes() {#getAnimateShapes--}
```
public final boolean getAnimateShapes()
```

Trả về hoặc đặt tùy chọn hoạt ảnh hình dạng. Đọc/ghi boolean.

**Giá trị trả về:**
boolean

### setAnimateShapes(boolean value) {#setAnimateShapes-boolean-}
```
public final void setAnimateShapes(boolean value)
```

Trả về hoặc đặt tùy chọn hoạt ảnh hình dạng. Đọc/ghi boolean.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |