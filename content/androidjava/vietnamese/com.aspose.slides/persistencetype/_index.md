---
title: PersistenceType
second_title: Aspose.Slides cho Android qua Tham chiếu API Java
description: Chỉ định phương pháp được sử dụng để lưu trữ các thuộc tính của điều khiển ActiveX.
type: docs
url: /vi/com.aspose.slides/persistencetype/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class PersistenceType extends System.Enum
```

Chỉ định phương pháp được sử dụng để lưu trữ các thuộc tính của điều khiển ActiveX.
## Trường

| Field | Description |
| --- | --- |
| [NotDefined](#NotDefined) | ID lưu trữ không được chỉ định. |
| [PersistPropertyBag](#PersistPropertyBag) | Chỉ định rằng điều khiển ActiveX được lưu trữ bằng cách sử dụng persistence dựa trên property-bag. |
| [PersistStream](#PersistStream) | Chỉ định rằng điều khiển ActiveX được lưu trữ bằng cách sử dụng persistence dựa trên luồng mà không hỗ trợ khởi tạo điều khiển ActiveX về trạng thái mặc định. |
| [PersistStreamInit](#PersistStreamInit) | Chỉ định rằng điều khiển ActiveX được lưu trữ bằng cách sử dụng persistence dựa trên luồng hỗ trợ khởi tạo điều khiển ActiveX về trạng thái mặc định. |
| [PersistStorage](#PersistStorage) | Chỉ định rằng điều khiển ActiveX được lưu trữ bằng cách sử dụng persistence dựa trên lưu trữ. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```


ID lưu trữ không được chỉ định.

### PersistPropertyBag {#PersistPropertyBag}
```
public static final int PersistPropertyBag
```


Chỉ định rằng điều khiển ActiveX được lưu trữ bằng cách sử dụng persistence dựa trên property-bag. Persistence dựa trên property-bag lưu trữ một điều khiển ActiveX thông qua một tập hợp các cặp tên và giá trị xác định dữ liệu được lưu trữ bởi điều khiển ActiveX.

### PersistStream {#PersistStream}
```
public static final int PersistStream
```


Chỉ định rằng điều khiển ActiveX được lưu trữ bằng cách sử dụng persistence dựa trên luồng mà không hỗ trợ khởi tạo điều khiển ActiveX về trạng thái mặc định.

### PersistStreamInit {#PersistStreamInit}
```
public static final int PersistStreamInit
```


Chỉ định rằng điều khiển ActiveX được lưu trữ bằng cách sử dụng persistence dựa trên luồng hỗ trợ khởi tạo điều khiển ActiveX về trạng thái mặc định.

### PersistStorage {#PersistStorage}
```
public static final int PersistStorage
```


Chỉ định rằng điều khiển ActiveX được lưu trữ bằng cách sử dụng persistence dựa trên lưu trữ.