---
title: IColorChangeEffectiveData
second_title: Aspose.Slides cho Android qua Tham chiếu API Java
description: Đối tượng bất biến đại diện cho một hiệu ứng thay đổi màu.
type: docs
url: /vi/com.aspose.slides/icolorchangeeffectivedata/
---
**Tất cả các giao diện được thực thi:**
[com.aspose.slides.IEffectEffectiveData](../../com.aspose.slides/ieffecteffectivedata)
```
public interface IColorChangeEffectiveData extends IEffectEffectiveData
```

Đối tượng bất biến đại diện cho hiệu ứng Color Change. Các thể hiện của FromColor sẽ được thay thế bằng các thể hiện của ToColor.
## Phương thức

| Phương thức | Mô tả |
| --- | --- |
| [getFromColor()](#getFromColor--) | Màu sẽ được thay thế. |
| [getToColor()](#getToColor--) | Màu sẽ thay thế. |
| [getUseAlpha()](#getUseAlpha--) | Trả về một giá trị boolean xác định có nên sử dụng thành phần alpha hay không. |
### getFromColor() {#getFromColor--}
```
public abstract Integer getFromColor()
```

Màu sẽ được thay thế. Chỉ đọc java.lang.Integer.

**Trả về:**
java.lang.Integer
### getToColor() {#getToColor--}
```
public abstract Integer getToColor()
```

Màu sẽ thay thế. Chỉ đọc java.lang.Integer.

**Trả về:**
java.lang.Integer
### getUseAlpha() {#getUseAlpha--}
```
public abstract boolean getUseAlpha()
```

Trả về một giá trị boolean xác định có nên sử dụng thành phần alpha hay không. Chỉ đọc boolean.

**Trả về:**
boolean