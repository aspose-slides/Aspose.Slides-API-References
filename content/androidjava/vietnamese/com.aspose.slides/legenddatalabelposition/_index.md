---
title: LegendDataLabelPosition
second_title: Tham khảo API Java của Aspose.Slides cho Android
description: Xác định vị trí của nhãn dữ liệu.
type: docs
url: /vi/com.aspose.slides/legenddatalabelposition/
---
**Kế thừa:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class LegendDataLabelPosition extends System.Enum
```

Xác định vị trí của nhãn dữ liệu.
## Trường

| Trường | Mô tả |
| --- | --- |
| [NotDefined](#NotDefined) | Vị trí không được xác định. |
| [Bottom](#Bottom) | Chỉ định rằng nhãn dữ liệu sẽ được hiển thị bên dưới dấu dữ liệu. |
| [BestFit](#BestFit) | Chỉ định rằng nhãn dữ liệu sẽ được hiển thị ở vị trí tốt nhất. |
| [Center](#Center) | Chỉ định rằng nhãn dữ liệu sẽ được hiển thị ở trung tâm dấu dữ liệu. |
| [InsideBase](#InsideBase) | Chỉ định rằng nhãn dữ liệu sẽ được hiển thị bên trong đáy của dấu dữ liệu. |
| [InsideEnd](#InsideEnd) | Chỉ định rằng nhãn dữ liệu sẽ được hiển thị bên trong phần cuối của dấu dữ liệu. |
| [Left](#Left) | Chỉ định rằng nhãn dữ liệu sẽ được hiển thị phía trái của dấu dữ liệu. |
| [OutsideEnd](#OutsideEnd) | Chỉ định rằng nhãn dữ liệu sẽ được hiển thị bên ngoài phần cuối của dấu dữ liệu. |
| [Right](#Right) | Chỉ định rằng nhãn dữ liệu sẽ được hiển thị phía bên phải của dấu dữ liệu. |
| [Top](#Top) | Chỉ định rằng nhãn dữ liệu sẽ được hiển thị phía trên của dấu dữ liệu. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```

Vị trí không được xác định.

### Bottom {#Bottom}
```
public static final int Bottom
```

Chỉ định rằng nhãn dữ liệu sẽ được hiển thị bên dưới dấu dữ liệu.

### BestFit {#BestFit}
```
public static final int BestFit
```

Chỉ định rằng nhãn dữ liệu sẽ được hiển thị ở vị trí tốt nhất.

### Center {#Center}
```
public static final int Center
```

Chỉ định rằng nhãn dữ liệu sẽ được hiển thị ở trung tâm dấu dữ liệu.

### InsideBase {#InsideBase}
```
public static final int InsideBase
```

Chỉ định rằng nhãn dữ liệu sẽ được hiển thị bên trong đáy của dấu dữ liệu.

### InsideEnd {#InsideEnd}
```
public static final int InsideEnd
```

Chỉ định rằng nhãn dữ liệu sẽ được hiển thị bên trong phần cuối của dấu dữ liệu.

### Left {#Left}
```
public static final int Left
```

Chỉ định rằng nhãn dữ liệu sẽ được hiển thị phía trái của dấu dữ liệu.

### OutsideEnd {#OutsideEnd}
```
public static final int OutsideEnd
```

Chỉ định rằng nhãn dữ liệu sẽ được hiển thị bên ngoài phần cuối của dấu dữ liệu.

### Right {#Right}
```
public static final int Right
```

Chỉ định rằng nhãn dữ liệu sẽ được hiển thị phía bên phải của dấu dữ liệu.

### Top {#Top}
```
public static final int Top
```

Chỉ định rằng nhãn dữ liệu sẽ được hiển thị phía trên của dấu dữ liệu.