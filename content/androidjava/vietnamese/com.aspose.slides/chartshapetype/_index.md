---
title: ChartShapeType
second_title: Aspose.Slides cho Android qua Tham chiếu API Java
description: Biểu diễn một hình dạng của biểu đồ.
type: docs
url: /vi/com.aspose.slides/chartshapetype/
---
**Kế thừa:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class ChartShapeType extends System.Enum
```

Biểu diễn một hình dạng của biểu đồ.
## Trường

| Trường | Mô tả |
| --- | --- |
| [NotDefined](#NotDefined) | Hình dạng biểu đồ chưa được xác định. |
| [Box](#Box) | Xác định biểu đồ sẽ được vẽ với hình dạng hộp. |
| [Cone](#Cone) | Xác định biểu đồ sẽ được vẽ dưới dạng hình nón, với đáy của nón đặt trên nền và đỉnh của nón ở trên đầu điểm dữ liệu. |
| [ConeToMax](#ConeToMax) | Xác định biểu đồ sẽ được vẽ với các hình nón cắt ngắn sao cho đỉnh của nón sẽ là giá trị dữ liệu tối đa. |
| [Cylinder](#Cylinder) | Xác định biểu đồ sẽ được vẽ dưới dạng hình trụ. |
| [Pyramid](#Pyramid) | Xác định biểu đồ sẽ được vẽ dưới dạng kim tự tháp chữ nhật, với đáy của kim tự tháp đặt trên nền và đỉnh của kim tự tháp ở trên đầu điểm dữ liệu. |
| [PyramidToMaximum](#PyramidToMaximum) | Xác định biểu đồ sẽ được vẽ với các hình nón cắt ngắn sao cho đỉnh của nón sẽ là giá trị dữ liệu tối đa. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```


Hình dạng biểu đồ chưa được xác định.

### Box {#Box}
```
public static final int Box
```


Xác định biểu đồ sẽ được vẽ với hình dạng hộp.

### Cone {#Cone}
```
public static final int Cone
```


Xác định biểu đồ sẽ được vẽ dưới dạng hình nón, với đáy của nón đặt trên nền và đỉnh của nón ở trên đầu điểm dữ liệu.

### ConeToMax {#ConeToMax}
```
public static final int ConeToMax
```


Xác định biểu đồ sẽ được vẽ với các hình nón cắt ngắn sao cho đỉnh của nón sẽ là giá trị dữ liệu tối đa.

### Cylinder {#Cylinder}
```
public static final int Cylinder
```


Xác định biểu đồ sẽ được vẽ dưới dạng hình trụ.

### Pyramid {#Pyramid}
```
public static final int Pyramid
```


Xác định biểu đồ sẽ được vẽ dưới dạng kim tự tháp chữ nhật, với đáy của kim tự tháp đặt trên nền và đỉnh của kim tự tháp ở trên đầu điểm dữ liệu.

### PyramidToMaximum {#PyramidToMaximum}
```
public static final int PyramidToMaximum
```


Xác định biểu đồ sẽ được vẽ với các hình nón cắt ngắn sao cho đỉnh của nón sẽ là giá trị dữ liệu tối đa.