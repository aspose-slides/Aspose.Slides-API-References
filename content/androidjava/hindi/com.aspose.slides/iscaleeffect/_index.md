---
title: IScaleEffect
second_title: Aspose.Slides for Android के लिए Java API संदर्भ
description: ऐनिमेशन स्केल प्रभाव का प्रतिनिधित्व करता है।
type: docs
url: /hi/com.aspose.slides/iscaleeffect/
---
**All Implemented Interfaces:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IScaleEffect extends IBehavior
```

Represents animation scale effect. => **ऐनिमेशन स्केल प्रभाव का प्रतिनिधित्व करता है।**

## Methods

| Method | Description |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | निर्धारित करता है कि सामग्री को ज़ूम किया जाना चाहिए या नहीं। |
| [setZoomContent(byte value)](#setZoomContent-byte-) | निर्धारित करता है कि सामग्री को ज़ूम किया जाना चाहिए या नहीं। |
| [getFrom()](#getFrom--) | ऐनिमेशन को (प्रतिशत में) शुरू करने के लिए x/y निर्देशांक निर्दिष्ट करता है। |
| [setFrom(PointF value)](#setFrom-android.graphics.PointF-) | ऐनिमेशन को (प्रतिशत में) शुरू करने के लिए x/y निर्देशांक निर्दिष्ट करता है। |
| [getTo()](#getTo--) | ऐनिमेशन स्केल प्रभाव के लिए लक्ष्य स्थान (प्रतिशत में) निर्दिष्ट करता है। |
| [setTo(PointF value)](#setTo-android.graphics.PointF-) | ऐनिमेशन स्केल प्रभाव के लिए लक्ष्य स्थान (प्रतिशत में) निर्दिष्ट करता है। |
| [getBy()](#getBy--) | ऐनिमेशन के लिए सापेक्ष ऑफ़सेट मान (प्रतिशत में) का वर्णन करता है। |
| [setBy(PointF value)](#setBy-android.graphics.PointF-) | ऐनिमेशन के लिए सापेक्ष ऑफ़सेट मान (प्रतिशत में) का वर्णन करता है। |
### getZoomContent() {#getZoomContent--}
```
public abstract byte getZoomContent()
```

निर्धारित करता है कि सामग्री को ज़ूम किया जाना चाहिए या नहीं। पढ़ें/लिखें [NullableBool](../../com.aspose.slides/nullablebool).

**Returns:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public abstract void setZoomContent(byte value)
```

निर्धारित करता है कि सामग्री को ज़ूम किया जाना चाहिए या नहीं। पढ़ें/लिखें [NullableBool](../../com.aspose.slides/nullablebool).

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | byte |  |
### getFrom() {#getFrom--}
```
public abstract PointF getFrom()
```

ऐनिमेशन को (प्रतिशत में) शुरू करने के लिए x/y निर्देशांक निर्दिष्ट करता है। पढ़ें/लिखें android.graphics.PointF.

**Returns:**
android.graphics.PointF
### setFrom(PointF value) {#setFrom-android.graphics.PointF-}
```
public abstract void setFrom(PointF value)
```

ऐनिमेशन को (प्रतिशत में) शुरू करने के लिए x/y निर्देशांक निर्दिष्ट करता है। पढ़ें/लिखें android.graphics.PointF.

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | android.graphics.PointF |  |
### getTo() {#getTo--}
```
public abstract PointF getTo()
```

ऐनिमेशन स्केल प्रभाव के लिए लक्ष्य स्थान (प्रतिशत में) निर्दिष्ट करता है। पढ़ें/लिखें android.graphics.PointF.

**Returns:**
android.graphics.PointF
### setTo(PointF value) {#setTo-android.graphics.PointF-}
```
public abstract void setTo(PointF value)
```

ऐनिमेशन स्केल प्रभाव के लिए लक्ष्य स्थान (प्रतिशत में) निर्दिष्ट करता है। पढ़ें/लिखें android.graphics.PointF.

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | android.graphics.PointF |  |
### getBy() {#getBy--}
```
public abstract PointF getBy()
```

ऐनिमेशन के लिए सापेक्ष ऑफ़सेट मान (प्रतिशत में) का वर्णन करता है। पढ़ें/लिखें android.graphics.PointF.

**Returns:**
android.graphics.PointF
### setBy(PointF value) {#setBy-android.graphics.PointF-}
```
public abstract void setBy(PointF value)
```

ऐनिमेशन के लिए सापेक्ष ऑफ़सेट मान (प्रतिशत में) का वर्णन करता है। पढ़ें/लिखें android.graphics.PointF.

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | android.graphics.PointF |  |