---
title: ImageFormat
second_title: Aspose.Slides for Android द्वारा Java API Reference
description: छवि के फ़ाइल फ़ॉर्मेट को दर्शाता है।
type: docs
url: /hi/com.aspose.slides/imageformat/
---
**विरासत:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class ImageFormat extends System.Enum
```

छवि के फ़ाइल फ़ॉर्मेट को दर्शाता है।
## फ़ील्ड

| फ़ील्ड | विवरण |
| --- | --- |
| [MemoryBmp](#MemoryBmp) | स्मृति में एक बिटमैप का प्रारूप दर्शाता है। |
| [Bmp](#Bmp) | बिटमैप (BMP) चित्र फ़ॉर्मेट को दर्शाता है। |
| [Emf](#Emf) | एन्हैंस्ड मेटाफाइल (EMF) चित्र फ़ॉर्मेट को दर्शाता है। |
| [Wmf](#Wmf) | विंडोज़ मेटाफाइल (WMF) चित्र फ़ॉर्मेट को दर्शाता है। |
| [Gif](#Gif) | ग्राफ़िक्स इंटरचेन्ज फ़ॉर्मेट (GIF) चित्र फ़ॉर्मेट को दर्शाता है। |
| [Jpeg](#Jpeg) | जॉइंट फोटोग्राफ़िक एक्सपर्ट्स ग्रुप (JPEG) चित्र फ़ॉर्मेट को दर्शाता है। |
| [Png](#Png) | डब्ल्यू3सी पोर्टेबल नेटवर्क ग्राफ़िक्स (PNG) चित्र फ़ॉर्मेट को दर्शाता है। |
| [Tiff](#Tiff) | टैग्ड इमेज फ़ाइल फ़ॉर्मेट (TIFF) चित्र फ़ॉर्मेट को दर्शाता है। |
| [Exif](#Exif) | एक्सचेंजेबल इमेज फ़ाइल (Exif) फ़ॉर्मेट को दर्शाता है। |
| [Icon](#Icon) | विंडोज़ आइकन चित्र फ़ॉर्मेट को दर्शाता है। |
### MemoryBmp {#MemoryBmp}
```
public static final int MemoryBmp
```

स्मृति में एक बिटमैप का प्रारूप दर्शाता है।

### Bmp {#Bmp}
```
public static final int Bmp
```

बिटमैप (BMP) चित्र फ़ॉर्मेट को दर्शाता है।

### Emf {#Emf}
```
public static final int Emf
```

एन्हैंस्ड मेटाफाइल (EMF) चित्र फ़ॉर्मेट को दर्शाता है।

### Wmf {#Wmf}
```
public static final int Wmf
```

विंडोज़ मेटाफाइल (WMF) चित्र फ़ॉर्मेट को दर्शाता है।

### Gif {#Gif}
```
public static final int Gif
```

ग्राफ़िक्स इंटरचेन्ज फ़ॉर्मेट (GIF) चित्र फ़ॉर्मेट को दर्शाता है।

### Jpeg {#Jpeg}
```
public static final int Jpeg
```

जॉइंट फोटोग्राफ़िक एक्सपर्ट्स ग्रुप (JPEG) चित्र फ़ॉर्मेट को दर्शाता है।

### Png {#Png}
```
public static final int Png
```

डब्ल्यू3सी पोर्टेबल नेटवर्क ग्राफ़िक्स (PNG) चित्र फ़ॉर्मेट को दर्शाता है।

### Tiff {#Tiff}
```
public static final int Tiff
```

टैग्ड इमेज फ़ाइल फ़ॉर्मेट (TIFF) चित्र फ़ॉर्मेट को दर्शाता है।

### Exif {#Exif}
```
public static final int Exif
```

एक्सचेंजेबल इमेज फ़ाइल (Exif) फ़ॉर्मेट को दर्शाता है।

### Icon {#Icon}
```
public static final int Icon
```

विंडोज़ आइकन चित्र फ़ॉर्मेट को दर्शाता है।