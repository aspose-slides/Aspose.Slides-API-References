---
title: INormalViewRestoredProperties
second_title: Aspose.Slides for Java API Reference
description: Meghatározza a diaterület méretét (szélesség, ha a restoredTop gyermekeként, magasság, ha a restoredLeft gyermekeként) a normál nézetben, amikor a terület egy változó visszaállított méretű (sem minimalizált, sem maximalizált).
type: docs
url: /hu/com.aspose.slides/inormalviewrestoredproperties/
---```
public interface INormalViewRestoredProperties
```

Meghatározza a diaterület méretét ((szélesség, ha a restoredTop gyermekeként, magasság, ha a restoredLeft gyermekeként) a normál nézetben, amikor a terület egy változó visszaállított méretű (sem minimalizált, sem maximalizált)).
## Metódusok

| Metódus | Leírás |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | Meghatározza a diaterület méretét (szélesség, ha a RestoredTop gyermekeként, magasság, ha a RestoredLeft gyermekeként). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | Meghatározza a diaterület méretét (szélesség, ha a RestoredTop gyermekeként, magasság, ha a RestoredLeft gyermekeként). |
| [getAutoAdjust()](#getAutoAdjust--) | Meghatározza, hogy a tartalomoldali terület mérete kompenzálja-e az új méretet az alkalmazásban a nézetet tartalmazó ablak átméretezésekor. Read/write boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | Meghatározza, hogy a tartalomoldali terület mérete kompenzálja-e az új méretet az alkalmazásban a nézetet tartalmazó ablak átméretezésekor. Read/write boolean. |
### getDimensionSize() {#getDimensionSize--}
```
public abstract float getDimensionSize()
```

Meghatározza a diaterület méretét (szélesség, ha a RestoredTop gyermekeként, magasság, ha a RestoredLeft gyermekeként). Read/write float.

**Visszatérési érték:**
float
### setDimensionSize(float value) {#setDimensionSize-float-}
```
public abstract void setDimensionSize(float value)
```

Meghatározza a diaterület méretét (szélesség, ha a RestoredTop gyermekeként, magasság, ha a RestoredLeft gyermekeként). Read/write float.

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public abstract boolean getAutoAdjust()
```

Meghatározza, hogy a tartalomoldali terület mérete kompenzálja-e az új méretet az alkalmazásban a nézetet tartalmazó ablak átméretezésekor. Read/write boolean.

**Visszatérési érték:**
boolean
### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public abstract void setAutoAdjust(boolean value)
```

Meghatározza, hogy a tartalomoldali terület mérete kompenzálja-e az új méretet az alkalmazásban a nézetet tartalmazó ablak átméretezésekor. Read/write boolean.

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | boolean |  |