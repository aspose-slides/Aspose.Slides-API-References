---
title: ITiming
second_title: Aspose.Slides for Java API Reference
description: Az animáció időzítését reprezentálja.
type: docs
url: /hu/com.aspose.slides/itiming/
---```
public interface ITiming
```

Az animáció időzítését reprezentálja.
## Metódusok

| Method | Description |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | Leírja a gyorsulási viselkedés időtartamának százalékos arányát. |
| [setAccelerate(float value)](#setAccelerate-float-) | Leírja a gyorsulási viselkedés időtartamának százalékos arányát. |
| [getDecelerate()](#getDecelerate--) | Leírja a lassulási viselkedés időtartamának százalékos arányát. |
| [setDecelerate(float value)](#setDecelerate-float-) | Leírja a lassulási viselkedés időtartamának százalékos arányát. |
| [getAutoReverse()](#getAutoReverse--) | Leírja, hogy a animációt automatikusan lejátsza-e visszafelé, miután előre lett lejátszva. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | Leírja, hogy a animációt automatikusan lejátsza-e visszafelé, miután előre lett lejátszva. |
| [getDuration()](#getDuration--) | Leírja az animáció hatásának időtartamát. |
| [setDuration(float value)](#setDuration-float-) | Leírja az animáció hatásának időtartamát. |
| [getRepeatCount()](#getRepeatCount--) | Leírja, hogy a hatást hányszor kell ismételni. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | Leírja, hogy a hatást hányszor kell ismételni. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | Ez az attribútum megadja, hogy a hatás a diák végéig ismétlődik-e. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | Ez az attribútum megadja, hogy a hatás a diák végéig ismétlődik-e. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | Ez az attribútum megadja, hogy a hatás a következő kattintásig ismétlődik-e. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | Ez az attribútum megadja, hogy a hatás a következő kattintásig ismétlődik-e. |
| [getRepeatDuration()](#getRepeatDuration--) | Leírja, hogy a hatást hányszor kell ismételni. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | Leírja, hogy a hatást hányszor kell ismételni. |
| [getRestart()](#getRestart--) | Megadja, hogy egy hatás újraindul-e a befejezés után. |
| [setRestart(int value)](#setRestart-int-) | Megadja, hogy egy hatás újraindul-e a befejezés után. |
| [getSpeed()](#getSpeed--) | Megadja, hogy a timing mennyivel gyorsul (vagy lassul) százalékban. |
| [setSpeed(float value)](#setSpeed-float-) | Megadja, hogy a timing mennyivel gyorsul (vagy lassul) százalékban. |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | Leírja a trigger utáni késleltetési időt. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | Leírja a trigger utáni késleltetési időt. |
| [getTriggerType()](#getTriggerType--) | Leírja a trigger típusát. |
| [setTriggerType(int value)](#setTriggerType-int-) | Leírja a trigger típusát. |
| [getRewind()](#getRewind--) | Ez az attribútum megadja, hogy a hatás lejátszása után visszatekerődik-e. |
| [setRewind(boolean value)](#setRewind-boolean-) | Ez az attribútum megadja, hogy a hatás lejátszása után visszatekerődik-e. |

### getAccelerate() {#getAccelerate--}
```
public abstract float getAccelerate()
```

Leírja a gyorsulási viselkedés időtartamának százalékos arányát. Olvasás/írás float.

**Visszatérési érték:**
float

### setAccelerate(float value) {#setAccelerate-float-}
```
public abstract void setAccelerate(float value)
```

Leírja a gyorsulási viselkedés időtartamának százalékos arányát. Olvasás/írás float.

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public abstract float getDecelerate()
```

Leírja a lassulási viselkedés időtartamának százalékos arányát. Olvasás/írás float.

**Visszatérési érték:**
float

### setDecelerate(float value) {#setDecelerate-float-}
```
public abstract void setDecelerate(float value)
```

Leírja a lassulási viselkedés időtartamának százalékos arányát. Olvasás/írás float.

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public abstract boolean getAutoReverse()
```

Leírja, hogy a animációt automatikusan lejátsza-e visszafelé, miután előre lett lejátszva. Olvasás/írás boolean.

**Visszatérési érték:**
boolean

### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public abstract void setAutoReverse(boolean value)
```

Leírja, hogy a animációt automatikusan lejátsza-e visszafelé, miután előre lett lejátszva. Olvasás/írás boolean.

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public abstract float getDuration()
```

Leírja az animáció hatásának időtartamát. Olvasás/írás float.

**Visszatérési érték:**
float

### setDuration(float value) {#setDuration-float-}
```
public abstract void setDuration(float value)
```

Leírja az animáció hatásának időtartamát. Olvasás/írás float.

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public abstract float getRepeatCount()
```

Leírja, hogy a hatást hányszor kell ismételni. Olvasás/írás float.

**Visszatérési érték:**
float

### setRepeatCount(float value) {#setRepeatCount-float-}
```
public abstract void setRepeatCount(float value)
```

Leírja, hogy a hatást hányszor kell ismételni. Olvasás/írás float.

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public abstract boolean getRepeatUntilEndSlide()
```

Ez az attribútum megadja, hogy a hatás a diák végéig ismétlődik-e. Olvasás/írás boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Az első dia effektus sorozatát kapja meg
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // A fő sorozat első effektusát kapja meg.
>      IEffect effect = effectsSequence.get_Item(0);
>      // A effektus időzítését/ismétlését "Until End of Slide" értékre állítja
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Visszatérési érték:**
boolean

### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public abstract void setRepeatUntilEndSlide(boolean value)
```

Ez az attribútum megadja, hogy a hatás a diák végéig ismétlődik-e. Olvasás/írás boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Az első dia effektus sorozatát kapja meg
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // A fő sorozat első effektusát kapja meg.
>      IEffect effect = effectsSequence.get_Item(0);
>      // A effektus időzítését/ismétlését "Until End of Slide" értékre állítja
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public abstract boolean getRepeatUntilNextClick()
```

Ez az attribútum megadja, hogy a hatás a következő kattintásig ismétlődik-e. Olvasás/írás boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Az első dia effektus sorozatát kapja meg
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // A fő sorozat első effektusát kapja meg.
>      IEffect effect = effectsSequence.get_Item(0);
>      // A effektus időzítését/ismétlését "Until Next Click" értékre állítja
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Visszatérési érték:**
boolean

### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public abstract void setRepeatUntilNextClick(boolean value)
```

Ez az attribútum megadja, hogy a hatás a következő kattintásig ismétlődik-e. Olvasás/írás boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Az első dia effektus sorozatát kapja meg
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // A fő sorozat első effektusát kapja meg.
>      IEffect effect = effectsSequence.get_Item(0);
>      // A effektus időzítését/ismétlését "Until Next Click" értékre állítja
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public abstract float getRepeatDuration()
```

Leírja, hogy a hatást hányszor kell ismételni. Olvasás/írás float.

**Visszatérési érték:**
float

### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public abstract void setRepeatDuration(float value)
```

Leírja, hogy a hatást hányszor kell ismételni. Olvasás/írás float.

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public abstract int getRestart()
```

Megadja, hogy egy hatás újraindul-e a befejezés után. Olvasás/írás [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Visszatérési érték:**
int

### setRestart(int value) {#setRestart-int-}
```
public abstract void setRestart(int value)
```

Megadja, hogy egy hatás újraindul-e a befejezés után. Olvasás/írás [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | int |  |

### getSpeed() {#getSpeed--}
```
public abstract float getSpeed()
```

Megadja, hogy a timing mennyivel gyorsul (vagy lassul) százalékban. Olvasás/írás float.

**Visszatérési érték:**
float

### setSpeed(float value) {#setSpeed-float-}
```
public abstract void setSpeed(float value)
```

Megadja, hogy a timing mennyivel gyorsul (vagy lassul) százalékban. Olvasás/írás float.

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public abstract float getTriggerDelayTime()
```

Leírja a trigger utáni késleltetési időt. Olvasás/írás float.

**Visszatérési érték:**
float

### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public abstract void setTriggerDelayTime(float value)
```

Leírja a trigger utáni késleltetési időt. Olvasás/írás float.

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public abstract int getTriggerType()
```

Leírja a trigger típusát. Olvasás/írás [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Visszatérési érték:**
int

### setTriggerType(int value) {#setTriggerType-int-}
```
public abstract void setTriggerType(int value)
```

Leírja a trigger típusát. Olvasás/írás [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public abstract boolean getRewind()
```

Ez az attribútum megadja, hogy a hatás lejátszása után visszatekerődik-e. Olvasás/írás boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Az első dia effektus sorozatát kapja meg
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // A fő sorozat első effektusát kapja meg.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Bekapcsolja a effektus Timing/Rewind-et
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Visszatérési érték:**
boolean

### setRewind(boolean value) {#setRewind-boolean-}
```
public abstract void setRewind(boolean value)
```

Ez az attribútum megadja, hogy a hatás lejátszása után visszatekerődik-e. Olvasás/írás boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Az első dia effektus sorozatát kapja meg
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // A fő sorozat első effektusát kapja meg.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Bekapcsolja a effektus Timing/Rewind-et
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | boolean |  |