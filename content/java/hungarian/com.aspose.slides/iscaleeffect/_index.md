---
title: IScaleEffect
second_title: Aspose.Slides Java API hivatkozás
description: Az animáció méretezési hatását képviseli.
type: docs
url: /hu/com.aspose.slides/iscaleeffect/
---
**Minden megvalósított interfész:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IScaleEffect extends IBehavior
```

Az animáció méretezési hatását képviseli.
## Módszerek

| Módszer | Leírás |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Meghatározza, hogy egy tartalmat nagyítani kell-e. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Meghatározza, hogy egy tartalmat nagyítani kell-e. |
| [getFrom()](#getFrom--) | Megadja az animáció kiindulási x/y koordinátáját (százalékban). |
| [setFrom(Point2D.Float value)](#setFrom-java.awt.geom.Point2D.Float-) | Megadja az animáció kiindulási x/y koordinátáját (százalékban). |
| [getTo()](#getTo--) | Megadja az animáció méretezési hatásának célhelyét (százalékban). |
| [setTo(Point2D.Float value)](#setTo-java.awt.geom.Point2D.Float-) | Megadja az animáció méretezési hatásának célhelyét (százalékban). |
| [getBy()](#getBy--) | Leírja a relatív eltolás értékét az animációhoz (százalékban). |
| [setBy(Point2D.Float value)](#setBy-java.awt.geom.Point2D.Float-) | Leírja a relatív eltolás értékét az animációhoz (százalékban). |
### getZoomContent() {#getZoomContent--}
```
public abstract byte getZoomContent()
```

Meghatározza, hogy egy tartalmat nagyítani kell-e. Olvasás/írás [NullableBool](../../com.aspose.slides/nullablebool).

**Visszatér:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public abstract void setZoomContent(byte value)
```

Meghatározza, hogy egy tartalmat nagyítani kell-e. Olvasás/írás [NullableBool](../../com.aspose.slides/nullablebool).

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | byte |  |
### getFrom() {#getFrom--}
```
public abstract Point2D.Float getFrom()
```

Megadja az animáció kiindulási x/y koordinátáját (százalékban). Olvasás/írás java.awt.geom.Point2D.Float.

**Visszatér:**
java.awt.geom.Point2D.Float
### setFrom(Point2D.Float value) {#setFrom-java.awt.geom.Point2D.Float-}
```
public abstract void setFrom(Point2D.Float value)
```

Megadja az animáció kiindulási x/y koordinátáját (százalékban). Olvasás/írás java.awt.geom.Point2D.Float.

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |
### getTo() {#getTo--}
```
public abstract Point2D.Float getTo()
```

Megadja az animáció méretezési hatásának célhelyét (százalékban). Olvasás/írás java.awt.geom.Point2D.Float.

**Visszatér:**
java.awt.geom.Point2D.Float
### setTo(Point2D.Float value) {#setTo-java.awt.geom.Point2D.Float-}
```
public abstract void setTo(Point2D.Float value)
```

Megadja az animáció méretezési hatásának célhelyét (százalékban). Olvasás/írás java.awt.geom.Point2D.Float.

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |
### getBy() {#getBy--}
```
public abstract Point2D.Float getBy()
```

Leírja a relatív eltolás értékét az animációhoz (százalékban). Olvasás/írás java.awt.geom.Point2D.Float.

**Visszatér:**
java.awt.geom.Point2D.Float
### setBy(Point2D.Float value) {#setBy-java.awt.geom.Point2D.Float-}
```
public abstract void setBy(Point2D.Float value)
```

Leírja a relatív eltolás értékét az animációhoz (százalékban). Olvasás/írás java.awt.geom.Point2D.Float.

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |