---
title: ChartPlotArea
second_title: Aspose.Slides Java API referencia
description: A diagram megjelenítéséhez használt téglalapot reprezentálja.
type: docs
url: /hu/com.aspose.slides/chartplotarea/
---
**Öröklődés:**
java.lang.Object, com.aspose.slides.DomObject

**Minden implementált interfész:**
[com.aspose.slides.IChartPlotArea](../../com.aspose.slides/ichartplotarea)
```
public class ChartPlotArea extends DomObject<Chart> implements IChartPlotArea
```

A diagram megjelenítési téglalapját képviseli.
## Módszerek

| Módszer | Leírás |
| --- | --- |
| [getFormat()](#getFormat--) | Visszaadja a diagram terület formátumát. |
| [getX()](#getX--) | Visszaadja vagy beállítja a diagram szélességének arányában (0-tól 1-ig) a diagram terület körülhatároló keretének bal felső sarkának x koordinátáját. |
| [setX(float value)](#setX-float-) | Visszaadja vagy beállítja a diagram szélességének arányában (0-tól 1-ig) a diagram terület körülhatároló keretének bal felső sarkának x koordinátáját. |
| [getY()](#getY--) | Visszaadja vagy beállítja a diagram magasságának arányában (0-tól 1-ig) a diagram terület körülhatároló keretének bal felső sarkának y koordinátáját. |
| [setY(float value)](#setY-float-) | Visszaadja vagy beállítja a diagram magasságának arányában (0-tól 1-ig) a diagram terület körülhatároló keretének bal felső sarkának y koordinátáját. |
| [getWidth()](#getWidth--) | Visszaadja vagy beállítja a diagram szélességének arányában (0-tól 1-ig) a diagram terület körülhatároló keretének szélességét. |
| [setWidth(float value)](#setWidth-float-) | Visszaadja vagy beállítja a diagram szélességének arányában (0-tól 1-ig) a diagram terület körülhatároló keretének szélességét. |
| [getHeight()](#getHeight--) | Visszaadja vagy beállítja a diagram magasságának arányában (0-tól 1-ig) a diagram terület körülhatároló keretének magasságát. |
| [setHeight(float value)](#setHeight-float-) | Visszaadja vagy beállítja a diagram magasságának arányában (0-tól 1-ig) a diagram terület körülhatároló keretének magasságát. |
| [getRight()](#getRight--) | Jobb. |
| [getBottom()](#getBottom--) | Alsó. |
| [getChart()](#getChart--) | Diagram. |
| [isLocationAutocalculated()](#isLocationAutocalculated--) | Meghatározza, hogyan kell kiszámítani a helyet: true – automatikusan számítva; az X, Y, Width, Height tulajdonságok határozzák meg. |
| [getLayoutTargetType()](#getLayoutTargetType--) | Ha a diagram terület elrendezését manuálisan határozzák meg, ez a tulajdonság azt adja meg, hogy a területet belülről (a tengelyek és tengelycímkék nélkül) vagy kívülről (tengelyekkel és tengelycímkékkel együtt) kell-e elrendezni. |
| [setLayoutTargetType(int value)](#setLayoutTargetType-int-) | Ha a diagram terület elrendezését manuálisan határozzák meg, ez a tulajdonság azt adja meg, hogy a területet belülről (a tengelyek és tengelycímkék nélkül) vagy kívülről (tengelyekkel és tengelycímkékkel együtt) kell-e elrendezni. |
| [getActualX()](#getActualX--) | Megadja a diagram elem tényleges x helyzetét (bal) a diagram bal felső sarkához képest. |
| [getActualY()](#getActualY--) | Megadja a diagram elem tényleges felső helyzetét a diagram bal felső sarkához képest. |
| [getActualWidth()](#getActualWidth--) | Megadja a diagram elem tényleges szélességét. |
| [getActualHeight()](#getActualHeight--) | Megadja a diagram elem tényleges magasságát. |
| [getSlide()](#getSlide--) | Visszaadja egy FillFormat szülő diáját. |
| [getPresentation()](#getPresentation--) | Visszaadja egy FillFormat szülő prezentációját. |

### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

Visszaadja a diagram terület formátumát. Csak olvasható [IFormat](../../com.aspose.slides/iformat).

**Visszatér:**
[IFormat](../../com.aspose.slides/iformat)

### getX() {#getX--}
```
public final float getX()
```

Visszaadja vagy beállítja a diagram szélességének arányában (0-tól 1-ig) a diagram terület körülhatároló keretének bal felső sarkának x koordinátáját. Olvasás/írás float.

**Visszatér:**
float

### setX(float value) {#setX-float-}
```
public final void setX(float value)
```

Visszaadja vagy beállítja a diagram szélességének arányában (0-tól 1-ig) a diagram terület körülhatároló keretének bal felső sarkának x koordinátáját. Olvasás/írás float.

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | float |  |

### getY() {#getY--}
```
public final float getY()
```

Visszaadja vagy beállítja a diagram magasságának arányában (0-tól 1-ig) a diagram terület körülhatároló keretének bal felső sarkának y koordinátáját. Olvasás/írás float.

**Visszatér:**
float

### setY(float value) {#setY-float-}
```
public final void setY(float value)
```

Visszaadja vagy beállítja a diagram magasságának arányában (0-tól 1-ig) a diagram terület körülhatároló keretének bal felső sarkának y koordinátáját. Olvasás/írás float.

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | float |  |

### getWidth() {#getWidth--}
```
public final float getWidth()
```

Visszaadja vagy beállítja a diagram szélességének arányában (0-tól 1-ig) a diagram terület körülhatároló keretének szélességét. Olvasás/írás float.

**Visszatér:**
float

### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```

Visszaadja vagy beállítja a diagram szélességének arányában (0-tól 1-ig) a diagram terület körülhatároló keretének szélességét. Olvasás/írás float.

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | float |  |

### getHeight() {#getHeight--}
```
public final float getHeight()
```

Visszaadja vagy beállítja a diagram magasságának arányában (0-tól 1-ig) a diagram terület körülhatároló keretének magasságát. Olvasás/írás float.

**Visszatér:**
float

### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```

Visszaadja vagy beállítja a diagram magasságának arányában (0-tól 1-ig) a diagram terület körülhatároló keretének magasságát. Olvasás/írás float.

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | float |  |

### getRight() {#getRight--}
```
public final float getRight()
```

Jobb. Csak olvasható float.

**Visszatér:**
float

### getBottom() {#getBottom--}
```
public final float getBottom()
```

Alsó. Csak olvasható float.

**Visszatér:**
float

### getChart() {#getChart--}
```
public final IChart getChart()
```

Diagram. Csak olvasható [IChart](../../com.aspose.slides/ichart).

**Visszatér:**
[IChart](../../com.aspose.slides/ichart)

### isLocationAutocalculated() {#isLocationAutocalculated--}
```
public final boolean isLocationAutocalculated()
```

Meghatározza, hogyan kell kiszámítani a helyet: true – automatikusan számítva; az X, Y, Width, Height tulajdonságok határozzák meg. Csak olvasható boolean.

**Visszatér:**
boolean

### getLayoutTargetType() {#getLayoutTargetType--}
```
public final int getLayoutTargetType()
```

Ha a diagram terület elrendezését manuálisan határozzák meg, ez a tulajdonság azt adja meg, hogy a területet belülről (a tengelyek és tengelycímkék nélkül) vagy kívülről (tengelyekkel és tengelycímkékkel együtt) kell-e elrendezni. Olvasás/írás [LayoutTargetType](../../com.aspose.slides/layouttargettype)(\#getLayoutTargetType.getLayoutTargetType/\#setLayoutTargetType(int).setLayoutTargetType(int)).

--------------------

> ```
> Presentation presentation = new Presentation();
>   try
>   {
>       ISlide slide = presentation.getSlides().get_Item(0);
>       IChart chart = slide.getShapes().addChart(ChartType.ClusteredColumn, 20, 100, 600, 400);
>       chart.getPlotArea().setX(0.2f);
>       chart.getPlotArea().setY(0.2f);
>       chart.getPlotArea().setWidth(0.7f);
>       chart.getPlotArea().setHeight(0.7f);
>       chart.getPlotArea().setLayoutTargetType(LayoutTargetType.Inner);
>       ...
>   } finally {
>       if (presentation != null) presentation.dispose();
>   }
> ```


**Visszatér:**
int

### setLayoutTargetType(int value) {#setLayoutTargetType-int-}
```
public final void setLayoutTargetType(int value)
```

Ha a diagram terület elrendezését manuálisan határozzák meg, ez a tulajdonság azt adja meg, hogy a területet belülről (a tengelyek és tengelycímkék nélkül) vagy kívülről (tengelyekkel és tengelycímkékkel együtt) kell-e elrendezni. Olvasás/írás [LayoutTargetType](../../com.aspose.slides/layouttargettype)(\#getLayoutTargetType.getLayoutTargetType/\#setLayoutTargetType(int).setLayoutTargetType(int)).

--------------------

> ```
> Presentation presentation = new Presentation();
>   try
>   {
>       ISlide slide = presentation.getSlides().get_Item(0);
>       IChart chart = slide.getShapes().addChart(ChartType.ClusteredColumn, 20, 100, 600, 400);
>       chart.getPlotArea().setX(0.2f);
>       chart.getPlotArea().setY(0.2f);
>       chart.getPlotArea().setWidth(0.7f);
>       chart.getPlotArea().setHeight(0.7f);
>       chart.getPlotArea().setLayoutTargetType(LayoutTargetType.Inner);
>       ...
>   } finally {
>       if (presentation != null) presentation.dispose();
>   }
> ```

**Paraméterek:**
| Paraméter | Típus | Leírás |
| --- | --- | --- |
| value | int |  |

### getActualX() {#getActualX--}
```
public final float getActualX()
```

Megadja a diagram elem tényleges x helyzetét (bal) a diagram bal felső sarkához képest. Hívja meg az IChart.ValidateChartLayout() metódust, mielőtt a tényleges értékeket lekérné. Olvasás float.

**Visszatér:**
float

### getActualY() {#getActualY--}
```
public final float getActualY()
```

Megadja a diagram elem tényleges felső helyzetét a diagram bal felső sarkához képest. Hívja meg az IChart.ValidateChartLayout() metódust, mielőtt a tényleges értékeket lekérné. Olvasás float.

**Visszatér:**
float

### getActualWidth() {#getActualWidth--}
```
public final float getActualWidth()
```

Megadja a diagram elem tényleges szélességét. Hívja meg az IChart.ValidateChartLayout() metódust, mielőtt a tényleges értékeket lekérné. Olvasás float.

**Visszatér:**
float

### getActualHeight() {#getActualHeight--}
```
public final float getActualHeight()
```

Megadja a diagram elem tényleges magasságát. Hívja meg az IChart.ValidateChartLayout() metódust, mielőtt a tényleges értékeket lekérné. Olvasás float.

**Visszatér:**
float

### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

Visszaadja egy FillFormat szülő diáját. Csak olvasható [BaseSlide](../../com.aspose.slides/baseslide).

**Visszatér:**
[IBaseSlide](../../com.aspose.slides/ibaseslide)

### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

Visszaadja egy FillFormat szülő prezentációját. Csak olvasható [IPresentation](../../com.aspose.slides/ipresentation).

**Visszatér:**
[IPresentation](../../com.aspose.slides/ipresentation)