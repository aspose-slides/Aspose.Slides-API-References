---
title: SensitivityLabelContentType
second_title: Referência da API Aspose.Slides para Java
description: Representa os tipos de marcação de conteúdo que devem ser aplicados a um documento.
type: docs
url: /pt/com.aspose.slides/sensitivitylabelcontenttype/
---
**Herança:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class SensitivityLabelContentType extends System.Enum
```

Representa os tipos de marcação de conteúdo que devem ser aplicados a um documento.
## Campos

| Campo | Descrição |
| --- | --- |
| [None](#None) | Indica que o rótulo é aplicado por padrão ou automaticamente. |
| [Header](#Header) | Indica que o rótulo foi aplicado manualmente. |
| [Footer](#Footer) | Indica que o rótulo foi aplicado manualmente. |
| [Watermark](#Watermark) | Indica que o rótulo foi aplicado manualmente. |
| [Encryption](#Encryption) | Indica que o rótulo foi aplicado manualmente. |
### None {#None}
```
public static final int None
```

Indica que o rótulo é aplicado por padrão ou automaticamente.

### Header {#Header}
```
public static final int Header
```

Indica que o rótulo foi aplicado manualmente. Use para qualquer rótulo de sensibilidade que foi aplicado diretamente pelo usuário.

### Footer {#Footer}
```
public static final int Footer
```

Indica que o rótulo foi aplicado manualmente. Use para qualquer rótulo de sensibilidade que foi aplicado diretamente pelo usuário.

### Watermark {#Watermark}
```
public static final int Watermark
```

Indica que o rótulo foi aplicado manualmente. Use para qualquer rótulo de sensibilidade que foi aplicado diretamente pelo usuário.

### Encryption {#Encryption}
```
public static final int Encryption
```

Indica que o rótulo foi aplicado manualmente. Use para qualquer rótulo de sensibilidade que foi aplicado diretamente pelo usuário.