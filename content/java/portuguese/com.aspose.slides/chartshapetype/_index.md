---
title: ChartShapeType
second_title: Referência da API Aspose.Slides para Java
description: Representa uma forma de gráfico.
type: docs
url: /pt/com.aspose.slides/chartshapetype/
---
**Herança:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class ChartShapeType extends System.Enum
```

Representa uma forma de gráfico.
## Campos

| Campo | Descrição |
| --- | --- |
| [NotDefined](#NotDefined) | A forma do gráfico não está definida. |
| [Box](#Box) | Especifica que o gráfico deve ser desenhado com uma forma de caixa. |
| [Cone](#Cone) | Especifica que o gráfico deve ser desenhado como um cone, com a base do cone no chão e o ponto do cone no topo do marcador de dados. |
| [ConeToMax](#ConeToMax) | Especifica que o gráfico deve ser desenhado com cones truncados de modo que o ponto do cone seja o valor máximo dos dados. |
| [Cylinder](#Cylinder) | Especifica que o gráfico deve ser desenhado como um cilindro. |
| [Pyramid](#Pyramid) | Especifica que o gráfico deve ser desenhado como uma pirâmide retangular, com a base da pirâmide no chão e o ponto da pirâmide no topo do marcador de dados. |
| [PyramidToMaximum](#PyramidToMaximum) | Especifica que o gráfico deve ser desenhado com cones truncados de modo que o ponto do cone seja o valor máximo dos dados. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```

A forma do gráfico não está definida.

### Box {#Box}
```
public static final int Box
```

Especifica que o gráfico deve ser desenhado com uma forma de caixa.

### Cone {#Cone}
```
public static final int Cone
```

Especifica que o gráfico deve ser desenhado como um cone, com a base do cone no chão e o ponto do cone no topo do marcador de dados.

### ConeToMax {#ConeToMax}
```
public static final int ConeToMax
```

Especifica que o gráfico deve ser desenhado com cones truncados de modo que o ponto do cone seja o valor máximo dos dados.

### Cylinder {#Cylinder}
```
public static final int Cylinder
```

Especifica que o gráfico deve ser desenhado como um cilindro.

### Pyramid {#Pyramid}
```
public static final int Pyramid
```

Especifica que o gráfico deve ser desenhado como uma pirâmide retangular, com a base da pirâmide no chão e o ponto da pirâmide no topo do marcador de dados.

### PyramidToMaximum {#PyramidToMaximum}
```
public static final int PyramidToMaximum
```

Especifica que o gráfico deve ser desenhado com cones truncados de modo que o ponto do cone seja o valor máximo dos dados.