---
title: Timing
second_title: Aspose.Slides para Java – Referência da API
description: Representa o tempo da animação.
type: docs
url: /pt/com.aspose.slides/timing/
---
**Herança:**
java.lang.Object

**Todas as Interfaces Implementadas:**
[com.aspose.slides.ITiming](../../com.aspose.slides/itiming), com.aspose.slides.IDOMObject
```
public class Timing implements ITiming, IDOMObject
```

Representa o tempo da animação.
## Métodos

| Método | Descrição |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | Descreve a porcentagem da duração do efeito de aceleração de comportamento. |
| [setAccelerate(float value)](#setAccelerate-float-) | Descreve a porcentagem da duração do efeito de aceleração de comportamento. |
| [getDecelerate()](#getDecelerate--) | Descreve a porcentagem da duração do efeito de desaceleração de comportamento. |
| [setDecelerate(float value)](#setDecelerate-float-) | Descreve a porcentagem da duração do efeito de desaceleração de comportamento. |
| [getAutoReverse()](#getAutoReverse--) | Descreve se a animação deve ser reproduzida automaticamente ao contrário após ser reproduzida na direção direta. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | Descreve se a animação deve ser reproduzida automaticamente ao contrário após ser reproduzida na direção direta. |
| [getDuration()](#getDuration--) | Descreve a duração do efeito da animação. |
| [setDuration(float value)](#setDuration-float-) | Descreve a duração do efeito da animação. |
| [getRepeatCount()](#getRepeatCount--) | Descreve o número de vezes que o efeito deve ser repetido. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | Descreve o número de vezes que o efeito deve ser repetido. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | Este atributo especifica se o efeito será repetido até o final do slide. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | Este atributo especifica se o efeito será repetido até o final do slide. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | Este atributo especifica se o efeito será repetido até o próximo clique. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | Este atributo especifica se o efeito será repetido até o próximo clique. |
| [getRepeatDuration()](#getRepeatDuration--) | Descreve o número de vezes que o efeito deve ser repetido. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | Descreve o número de vezes que o efeito deve ser repetido. |
| [getRestart()](#getRestart--) | Especifica se um efeito deve reiniciar após a conclusão. |
| [setRestart(int value)](#setRestart-int-) | Especifica se um efeito deve reiniciar após a conclusão. |
| [getRewind()](#getRewind--) | Este atributo especifica se o efeito será rebobinado ao terminar de reproduzir. |
| [setRewind(boolean value)](#setRewind-boolean-) | Este atributo especifica se o efeito será rebobinado ao terminar de reproduzir. |
| [getSpeed()](#getSpeed--) | Especifica a porcentagem para acelerar (ou desacelerar) o tempo. |
| [setSpeed(float value)](#setSpeed-float-) | Especifica a porcentagem para acelerar (ou desacelerar) o tempo. |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | Descreve o tempo de atraso após o gatilho. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | Descreve o tempo de atraso após o gatilho. |
| [getTriggerType()](#getTriggerType--) | Descreve o tipo de gatilho. |
| [setTriggerType(int value)](#setTriggerType-int-) | Descreve o tipo de gatilho. |
| [getParent_Immediate()](#getParent-Immediate--) |  |

### getAccelerate() {#getAccelerate--}
```
public final float getAccelerate()
```

Descreve a porcentagem da duração do efeito de aceleração de comportamento. Leitura/gravação float.

**Retorna:**
float

### setAccelerate(float value) {#setAccelerate-float-}
```
public final void setAccelerate(float value)
```

Descreve a porcentagem da duração do efeito de aceleração de comportamento. Leitura/gravação float.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public final float getDecelerate()
```

Descreve a porcentagem da duração do efeito de desaceleração de comportamento. Leitura/gravação float.

**Retorna:**
float

### setDecelerate(float value) {#setDecelerate-float-}
```
public final void setDecelerate(float value)
```

Descreve a porcentagem da duração do efeito de desaceleração de comportamento. Leitura/gravação float.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public final boolean getAutoReverse()
```

Descreve se a animação deve ser reproduzida automaticamente ao contrário após ser reproduzida na direção direta. Leitura/gravação boolean.

**Retorna:**
boolean

### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public final void setAutoReverse(boolean value)
```

Descreve se a animação deve ser reproduzida automaticamente ao contrário após ser reproduzida na direção direta. Leitura/gravação boolean.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public final float getDuration()
```

Descreve a duração do efeito da animação. Leitura/gravação float.

**Retorna:**
float

### setDuration(float value) {#setDuration-float-}
```
public final void setDuration(float value)
```

Descreve a duração do efeito da animação. Leitura/gravação float.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public final float getRepeatCount()
```

Descreve o número de vezes que o efeito deve ser repetido. Leitura/gravação float.

**Retorna:**
float

### setRepeatCount(float value) {#setRepeatCount-float-}
```
public final void setRepeatCount(float value)
```

Descreve o número de vezes que o efeito deve ser repetido. Leitura/gravação float.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public final boolean getRepeatUntilEndSlide()
```

Este atributo especifica se o efeito será repetido até o final do slide. Leitura/gravação boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtém a sequência de efeitos para o primeiro slide
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtém o primeiro efeito da sequência principal.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Altera o Timing/Repeat do efeito para "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Retorna:**
boolean

### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public final void setRepeatUntilEndSlide(boolean value)
```

Este atributo especifica se o efeito será repetido até o final do slide. Leitura/gravação boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obter a sequência de efeitos para o primeiro slide
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obter o primeiro efeito da sequência principal.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Alterar o Timing/Repeat do efeito para "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public final boolean getRepeatUntilNextClick()
```

Este atributo especifica se o efeito será repetido até o próximo clique. Leitura/gravação boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtém a sequência de efeitos para o primeiro slide
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtém o primeiro efeito da sequência principal.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Altera o Timing/Repeat do efeito para "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Retorna:**
boolean

### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public final void setRepeatUntilNextClick(boolean value)
```

Este atributo especifica se o efeito será repetido até o próximo clique. Leitura/gravação boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtém a sequência de efeitos para o primeiro slide
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtém o primeiro efeito da sequência principal.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Altera o Timing/Repeat do efeito para "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public final float getRepeatDuration()
```

Descreve o número de vezes que o efeito deve ser repetido. Leitura/gravação float.

**Retorna:**
float

### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public final void setRepeatDuration(float value)
```

Descreve o número de vezes que o efeito deve ser repetido. Leitura/gravação float.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public final int getRestart()
```

Especifica se um efeito deve reiniciar após a conclusão. Leitura/gravação [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Retorna:**
int

### setRestart(int value) {#setRestart-int-}
```
public final void setRestart(int value)
```

Especifica se um efeito deve reiniciar após a conclusão. Leitura/gravação [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public final boolean getRewind()
```

Este atributo especifica se o efeito será rebobinado ao terminar de reproduzir. Leitura/gravação boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtém a sequência de efeitos para o primeiro slide
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtém o primeiro efeito da sequência principal.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ativa o Timing/Rewind do efeito.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Retorna:**
boolean

### setRewind(boolean value) {#setRewind-boolean-}
```
public final void setRewind(boolean value)
```

Este atributo especifica se o efeito será rebobinado ao terminar de reproduzir. Leitura/gravação boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtém a sequência de efeitos para o primeiro slide
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtém o primeiro efeito da sequência principal.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ativa o Timing/Rewind do efeito.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | boolean |  |

### getSpeed() {#getSpeed--}
```
public final float getSpeed()
```

Especifica a porcentagem para acelerar (ou desacelerar) o tempo. Leitura/gravação float.

**Retorna:**
float

### setSpeed(float value) {#setSpeed-float-}
```
public final void setSpeed(float value)
```

Especifica a porcentagem para acelerar (ou desacelerar) o tempo. Leitura/gravação float.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public final float getTriggerDelayTime()
```

Descreve o tempo de atraso após o gatilho. Leitura/gravação float.

**Retorna:**
float

### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public final void setTriggerDelayTime(float value)
```

Descreve o tempo de atraso após o gatilho. Leitura/gravação float.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public final int getTriggerType()
```

Descreve o tipo de gatilho. Leitura/gravação [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Retorna:**
int

### setTriggerType(int value) {#setTriggerType-int-}
```
public final void setTriggerType(int value)
```

Descreve o tipo de gatilho. Leitura/gravação [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | int |  |

### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

Retorna objeto Parent_Immediate. Somente leitura IDOMObject.

**Retorna:**
com.aspose.slides.IDOMObject