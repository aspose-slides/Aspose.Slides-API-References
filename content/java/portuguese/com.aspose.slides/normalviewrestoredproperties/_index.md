---
title: NormalViewRestoredProperties
second_title: Referência da API Aspose.Slides para Java
description: Especifica o dimensionamento da região do slide (largura quando é filho de restoredTop, altura quando é filho de restoredLeft) da visualização normal, quando a região possui um tamanho restaurado variável, nem minimizado nem maximizado.
type: docs
url: /pt/com.aspose.slides/normalviewrestoredproperties/
---
**Herança:**
java.lang.Object

**Todas as interfaces implementadas:**
[com.aspose.slides.INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)
```
public class NormalViewRestoredProperties implements INormalViewRestoredProperties
```

Especifica o dimensionamento da região do slide ((largura quando é filho de restoredTop, altura quando é filho de restoredLeft) da visualização normal, quando a região tem um tamanho restaurado variável (nem minimizado nem maximizado)).
## Métodos

| Método | Descrição |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | Especifica o tamanho da região do slide (largura quando é filho de RestoredTop, altura quando é filho de RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | Especifica o tamanho da região do slide (largura quando é filho de RestoredTop, altura quando é filho de RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | Especifica se o tamanho da região de conteúdo lateral deve compensar o novo tamanho ao redimensionar a janela que contém a visualização dentro do aplicativo Read/write boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | Especifica se o tamanho da região de conteúdo lateral deve compensar o novo tamanho ao redimensionar a janela que contém a visualização dentro do aplicativo Read/write boolean. |
### getDimensionSize() {#getDimensionSize--}
```
public final float getDimensionSize()
```


Especifica o tamanho da região do slide (largura quando é filho de RestoredTop, altura quando é filho de RestoredLeft). Read/write float.

**Retorna:**
float
### setDimensionSize(float value) {#setDimensionSize-float-}
```
public final void setDimensionSize(float value)
```


Especifica o tamanho da região do slide (largura quando é filho de RestoredTop, altura quando é filho de RestoredLeft). Read/write float.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public final boolean getAutoAdjust()
```


Especifica se o tamanho da região de conteúdo lateral deve compensar o novo tamanho ao redimensionar a janela que contém a visualização dentro do aplicativo Read/write boolean.

**Retorna:**
boolean
### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public final void setAutoAdjust(boolean value)
```


Especifica se o tamanho da região de conteúdo lateral deve compensar o novo tamanho ao redimensionar a janela que contém a visualização dentro do aplicativo Read/write boolean.

**Parâmetros:**
| Parâmetro | Tipo | Descrição |
| --- | --- | --- |
| value | boolean |  |