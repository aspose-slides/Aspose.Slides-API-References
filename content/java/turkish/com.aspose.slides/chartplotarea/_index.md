---
title: ChartPlotArea
second_title: Aspose.Slides for Java API Referansı
description: Grafiğin çizileceği dikdörtgeni temsil eder.
type: docs
url: /tr/com.aspose.slides/chartplotarea/
---
**Kalıtım:**
java.lang.Object, com.aspose.slides.DomObject

**Uygulanan Tüm Arabirimler:**
[com.aspose.slides.IChartPlotArea](../../com.aspose.slides/ichartplotarea)
```
public class ChartPlotArea extends DomObject<Chart> implements IChartPlotArea
```

Grafiğin çizileceği dikdörtgeni temsil eder.
## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [getFormat()](#getFormat--) | Grafik alanının biçimini döndürür. |
| [getX()](#getX--) | Grafik alanı sınırlayıcı kutusunun sol üst köşesinin x koordinatını, grafiğin genişliğinin bir kesri (0 ile 1 arasında) olarak döndürür veya ayarlar. |
| [setX(float value)](#setX-float-) | Grafik alanı sınırlayıcı kutusunun sol üst köşesinin x koordinatını, grafiğin genişliğinin bir kesri (0 ile 1 arasında) olarak döndürür veya ayarlar. |
| [getY()](#getY--) | Grafik alanı sınırlayıcı kutusunun sol üst köşesinin y koordinatını, grafiğin yüksekliğinin bir kesri (0 ile 1 arasında) olarak döndürür veya ayarlar. |
| [setY(float value)](#setY-float-) | Grafik alanı sınırlayıcı kutusunun sol üst köşesinin y koordinatını, grafiğin yüksekliğinin bir kesri (0 ile 1 arasında) olarak döndürür veya ayarlar. |
| [getWidth()](#getWidth--) | Grafik alanı sınırlayıcı kutusunun genişliğini, grafiğin genişliğinin bir kesri (0 ile 1 arasında) olarak döndürür veya ayarlar. |
| [setWidth(float value)](#setWidth-float-) | Grafik alanı sınırlayıcı kutusunun genişliğini, grafiğin genişliğinin bir kesri (0 ile 1 arasında) olarak döndürür veya ayarlar. |
| [getHeight()](#getHeight--) | Grafik alanı sınırlayıcı kutusunun yüksekliğini, grafiğin yüksekliğinin bir kesri (0 ile 1 arasında) olarak döndürür veya ayarlar. |
| [setHeight(float value)](#setHeight-float-) | Grafik alanı sınırlayıcı kutusunun yüksekliğini, grafiğin yüksekliğinin bir kesri (0 ile 1 arasında) olarak döndürür veya ayarlar. |
| [getRight()](#getRight--) | Sağ. |
| [getBottom()](#getBottom--) | Alt. |
| [getChart()](#getChart--) | Grafik. |
| [isLocationAutocalculated()](#isLocationAutocalculated--) | Konumun nasıl hesaplanacağını tanımlar: true – otomatik olarak hesaplanır; X, Y, Width, Height özellikleriyle tanımlanır. |
| [getLayoutTargetType()](#getLayoutTargetType--) | Eğer grafik alanının yerleşimi elle tanımlanmışsa bu özellik, grafik alanının içinde (eksen ve etiketler hariç) mi yoksa dışında (eksen ve etiketler dahil) mi yerleştirileceğini belirtir. |
| [setLayoutTargetType(int value)](#setLayoutTargetType-int-) | Eğer grafik alanının yerleşimi elle tanımlanmışsa bu özellik, grafik alanının içinde (eksen ve etiketler hariç) mi yoksa dışında (eksen ve etiketler dahil) mi yerleştirileceğini belirtir. |
| [getActualX()](#getActualX--) | Grafik öğesinin sol üst köşeye göre gerçek x konumunu (sol) belirtir. |
| [getActualY()](#getActualY--) | Grafik öğesinin sol üst köşeye göre gerçek üst konumunu belirtir. |
| [getActualWidth()](#getActualWidth--) | Grafik öğesinin gerçek genişliğini belirtir. |
| [getActualHeight()](#getActualHeight--) | Grafik öğesinin gerçek yüksekliğini belirtir. |
| [getSlide()](#getSlide--) | FillFormat'ın üst slaytını döndürür. |
| [getPresentation()](#getPresentation--) | FillFormat'ın üst sunumunu döndürür. |

### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

Grafik alanının biçimini döndürür. Salt-okunur [IFormat](../../com.aspose.slides/iformat).

**Döndürür:**
[IFormat](../../com.aspose.slides/iformat)

### getX() {#getX--}
```
public final float getX()
```

Grafik alanı sınırlayıcı kutusunun sol üst köşesinin x koordinatını, grafiğin genişliğinin bir kesri (0 ile 1 arasında) olarak döndürür veya ayarlar. Okunur/yazılabilir float.

**Döndürür:**
float

### setX(float value) {#setX-float-}
```
public final void setX(float value)
```

Grafik alanı sınırlayıcı kutusunun sol üst köşesinin x koordinatını, grafiğin genişliğinin bir kesri (0 ile 1 arasında) olarak döndürür veya ayarlar. Okunur/yazılabilir float.

**Parametreler:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | float |  |

### getY() {#getY--}
```
public final float getY()
```

Grafik alanı sınırlayıcı kutusunun sol üst köşesinin y koordinatını, grafiğin yüksekliğinin bir kesri (0 ile 1 arasında) olarak döndürür veya ayarlar. Okunur/yazılabilir float.

**Döndürür:**
float

### setY(float value) {#setY-float-}
```
public final void setY(float value)
```

Grafik alanı sınırlayıcı kutusunun sol üst köşesinin y koordinatını, grafiğin yüksekliğinin bir kesri (0 ile 1 arasında) olarak döndürür veya ayarlar. Okunur/yazılabilir float.

**Parametreler:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | float |  |

### getWidth() {#getWidth--}
```
public final float getWidth()
```

Grafik alanı sınırlayıcı kutusunun genişliğini, grafiğin genişliğinin bir kesri (0 ile 1 arasında) olarak döndürür veya ayarlar. Okunur/yazılabilir float.

**Döndürür:**
float

### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```

Grafik alanı sınırlayıcı kutusunun genişliğini, grafiğin genişliğinin bir kesri (0 ile 1 arasında) olarak döndürür veya ayarlar. Okunur/yazılabilir float.

**Parametreler:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | float |  |

### getHeight() {#getHeight--}
```
public final float getHeight()
```

Grafik alanı sınırlayıcı kutusunun yüksekliğini, grafiğin yüksekliğinin bir kesri (0 ile 1 arasında) olarak döndürür veya ayarlar. Okunur/yazılabilir float.

**Döndürür:**
float

### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```

Grafik alanı sınırlayıcı kutusunun yüksekliğini, grafiğin yüksekliğinin bir kesri (0 ile 1 arasında) olarak döndürür veya ayarlar. Okunur/yazılabilir float.

**Parametreler:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | float |  |

### getRight() {#getRight--}
```
public final float getRight()
```

Sağ. Salt-okunur float.

**Döndürür:**
float

### getBottom() {#getBottom--}
```
public final float getBottom()
```

Alt. Salt-okunur float.

**Döndürür:**
float

### getChart() {#getChart--}
```
public final IChart getChart()
```

Grafik. Salt-okunur [IChart](../../com.aspose.slides/ichart).

**Döndürür:**
[IChart](../../com.aspose.slides/ichart)

### isLocationAutocalculated() {#isLocationAutocalculated--}
```
public final boolean isLocationAutocalculated()
```

Konumun nasıl hesaplanacağını tanımlar: true – otomatik olarak hesaplanır; X, Y, Width, Height özellikleriyle tanımlanır. Salt-okunur boolean.

**Döndürür:**
boolean

### getLayoutTargetType() {#getLayoutTargetType--}
```
public final int getLayoutTargetType()
```

Eğer grafik alanının yerleşimi elle tanımlanmışsa bu özellik, grafik alanını içinde (eksen ve etiketler hariç) mi yoksa dışında (eksen ve etiketler dahil) mi yerleştirileceğini belirtir. Okunur/yazılabilir [LayoutTargetType](../../com.aspose.slides/layouttargettype)(\#getLayoutTargetType.getLayoutTargetType/\#setLayoutTargetType(int).setLayoutTargetType(int)).

--------------------

> ```
> Presentation presentation = new Presentation();
>   try
>   {
>       ISlide slide = presentation.getSlides().get_Item(0);
>       IChart chart = slide.getShapes().addChart(ChartType.ClusteredColumn, 20, 100, 600, 400);
>       chart.getPlotArea().setX(0.2f);
>       chart.getPlotArea().setY(0.2f);
>       chart.getPlotArea().setWidth(0.7f);
>       chart.getPlotArea().setHeight(0.7f);
>       chart.getPlotArea().setLayoutTargetType(LayoutTargetType.Inner);
>       ...
>   } finally {
>       if (presentation != null) presentation.dispose();
>   }
> ```

**Döndürür:**
int

### setLayoutTargetType(int value) {#setLayoutTargetType-int-}
```
public final void setLayoutTargetType(int value)
```

Eğer grafik alanının yerleşimi elle tanımlanmışsa bu özellik, grafik alanını içinde (eksen ve etiketler hariç) mi yoksa dışında (eksen ve etiketler dahil) mi yerleştirileceğini belirtir. Okunur/yazılabilir [LayoutTargetType](../../com.aspose.slides/layouttargettype)(\#getLayoutTargetType.getLayoutTargetType/\#setLayoutTargetType(int).setLayoutTargetType(int)).

--------------------

> ```
> Presentation presentation = new Presentation();
>   try
>   {
>       ISlide slide = presentation.getSlides().get_Item(0);
>       IChart chart = slide.getShapes().addChart(ChartType.ClusteredColumn, 20, 100, 600, 400);
>       chart.getPlotArea().setX(0.2f);
>       chart.getPlotArea().setY(0.2f);
>       chart.getPlotArea().setWidth(0.7f);
>       chart.getPlotArea().setHeight(0.7f);
>       chart.getPlotArea().setLayoutTargetType(LayoutTargetType.Inner);
>       ...
>   } finally {
>       if (presentation != null) presentation.dispose();
>   }
> ```

**Parametreler:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | int |  |

### getActualX() {#getActualX--}
```
public final float getActualX()
```

Grafik öğesinin sol üst köşeye göre gerçek x konumunu (sol) belirtir. Gerçek değerleri almak için önce IChart.ValidateChartLayout() metodunu çağırın. Okunur float.

**Döndürür:**
float

### getActualY() {#getActualY--}
```
public final float getActualY()
```

Grafik öğesinin sol üst köşeye göre gerçek üst konumunu belirtir. Gerçek değerleri almak için önce IChart.ValidateChartLayout() metodunu çağırın. Okunur float.

**Döndürür:**
float

### getActualWidth() {#getActualWidth--}
```
public final float getActualWidth()
```

Grafik öğesinin gerçek genişliğini belirtir. Gerçek değerleri almak için önce IChart.ValidateChartLayout() metodunu çağırın. Okunur float.

**Döndürür:**
float

### getActualHeight() {#getActualHeight--}
```
public final float getActualHeight()
```

Grafik öğesinin gerçek yüksekliğini belirtir. Gerçek değerleri almak için önce IChart.ValidateChartLayout() metodunu çağırın. Okunur float.

**Döndürür:**
float

### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

FillFormat'ın üst slaytını döndürür. Salt-okunur [BaseSlide](../../com.aspose.slides/baseslide).

**Döndürür:**
[IBaseSlide](../../com.aspose.slides/ibaseslide)

### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

FillFormat'ın üst sunumunu döndürür. Salt-okunur [IPresentation](../../com.aspose.slides/ipresentation).

**Döndürür:**
[IPresentation](../../com.aspose.slides/ipresentation)