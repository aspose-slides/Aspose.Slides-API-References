---
title: IScaleEffect
second_title: Aspose.Slides for Java API Referansı
description: Animasyon ölçek etkisini temsil eder.
type: docs
url: /tr/com.aspose.slides/iscaleeffect/
---
**Tüm Gerçekleştirilen Arabirimler:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IScaleEffect extends IBehavior
```

Animasyon ölçek etkisini temsil eder.
## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Bir içeriğin yakınlaştırılıp yakınlaştırılmayacağını belirler. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Bir içeriğin yakınlaştırılıp yakınlaştırılmayacağını belirler. |
| [getFrom()](#getFrom--) | Animasyonun başlatılacağı x/y koordinatını (yüzde olarak) belirtir. |
| [setFrom(Point2D.Float value)](#setFrom-java.awt.geom.Point2D.Float-) | Animasyonun başlatılacağı x/y koordinatını (yüzde olarak) belirtir. |
| [getTo()](#getTo--) | Animasyon ölçek etkisi için hedef konumu (yüzde olarak) belirtir. |
| [setTo(Point2D.Float value)](#setTo-java.awt.geom.Point2D.Float-) | Animasyon ölçek etkisi için hedef konumu (yüzde olarak) belirtir. |
| [getBy()](#getBy--) | Animasyon için göreceli offset değerini (yüzde olarak) açıklar. |
| [setBy(Point2D.Float value)](#setBy-java.awt.geom.Point2D.Float-) | Animasyon için göreceli offset değerini (yüzde olarak) açıklar. |
### getZoomContent() {#getZoomContent--}
```
public abstract byte getZoomContent()
```

Bir içeriğin yakınlaştırılıp yakınlaştırılmayacağını belirler. Okunur/yazılabilir [NullableBool](../../com.aspose.slides/nullablebool).

**Döndürür:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public abstract void setZoomContent(byte value)
```

Bir içeriğin yakınlaştırılıp yakınlaştırılmayacağını belirler. Okunur/yazılabilir [NullableBool](../../com.aspose.slides/nullablebool).

**Parametreler:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public abstract Point2D.Float getFrom()
```

Animasyonun başlatılacağı x/y koordinatını (yüzde olarak) belirtir. Okunur/yazılabilir java.awt.geom.Point2D.Float.

**Döndürür:**
java.awt.geom.Point2D.Float
### setFrom(Point2D.Float value) {#setFrom-java.awt.geom.Point2D.Float-}
```
public abstract void setFrom(Point2D.Float value)
```

Animasyonun başlatılacağı x/y koordinatını (yüzde olarak) belirtir. Okunur/yazılabilir java.awt.geom.Point2D.Float.

**Parametreler:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getTo() {#getTo--}
```
public abstract Point2D.Float getTo()
```

Animasyon ölçek etkisi için hedef konumu (yüzde olarak) belirtir. Okunur/yazılabilir java.awt.geom.Point2D.Float.

**Döndürür:**
java.awt.geom.Point2D.Float
### setTo(Point2D.Float value) {#setTo-java.awt.geom.Point2D.Float-}
```
public abstract void setTo(Point2D.Float value)
```

Animasyon ölçek etkisi için hedef konumu (yüzde olarak) belirtir. Okunur/yazılabilir java.awt.geom.Point2D.Float.

**Parametreler:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getBy() {#getBy--}
```
public abstract Point2D.Float getBy()
```

Animasyon için göreceli offset değerini (yüzde olarak) açıklar. Okunur/yazılabilir java.awt.geom.Point2D.Float.

**Döndürür:**
java.awt.geom.Point2D.Float
### setBy(Point2D.Float value) {#setBy-java.awt.geom.Point2D.Float-}
```
public abstract void setBy(Point2D.Float value)
```

Animasyon için göreceli offset değerini (yüzde olarak) açıklar. Okunur/yazılabilir java.awt.geom.Point2D.Float.

**Parametreler:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |