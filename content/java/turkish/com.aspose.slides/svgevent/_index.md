---
title: SvgEvent
second_title: Aspose.Slides for Java API Referansı
description: SVG şekli için seçenekleri temsil eder.
type: docs
url: /tr/com.aspose.slides/svgevent/
---
**Kalıtım:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class SvgEvent extends System.Enum
```

SVG şekli için seçenekleri temsil eder.
## Alanlar

| Alan | Açıklama |
| --- | --- |
| [OnFocusIn](#OnFocusIn) | Bir öğe odaklandığında oluşur, örneğin bir metin seçildiğinde. |
| [OnFocusOut](#OnFocusOut) | Bir öğe odağını kaybettiğinde oluşur, örneğin bir metin seçimi kaldırıldığında. |
| [OnActivate](#OnActivate) | Bir öğe etkinleştirildiğinde oluşur, örneğin fare tıklaması veya tuşa basma yoluyla. |
| [OnClick](#OnClick) | İşaretleme cihazı düğmesi bir öğe üzerinde tıklandığında oluşur. |
| [OnMouseDown](#OnMouseDown) | İşaretleme cihazı düğmesi bir öğe üzerinde basıldığında oluşur. |
| [OnMouseUp](#OnMouseUp) | İşaretleme cihazı düğmesi bir öğe üzerinde serbest bırakıldığında oluşur. |
| [OnMouseOver](#OnMouseOver) | İşaretleme cihazı bir öğe üzerine hareket ettiğinde oluşur. |
| [OnMouseMove](#OnMouseMove) | İşaretleme cihazı bir öğe üzerinde iken hareket ettiğinde oluşur. |
| [OnMouseOut](#OnMouseOut) | İşaretleme cihazı bir öğeden uzaklaştığında oluşur. |
| [OnLoad](#OnLoad) | Kullanıcı aracısı öğeyi, alt öğelerini ve onu renderlemek için gereken tüm başvurulan kaynakları tamamen ayrıştırdığında oluşur. |
| [OnUnload](#OnUnload) | DOM uygulaması bir belgeyi bir pencereden veya çerçeveden kaldırdığında oluşur. |
| [OnAbort](#OnAbort) | Bir öğenin tamamen yüklenmesine izin verilmeden sayfa yüklemesi durdurulduğunda oluşur. |
| [OnError](#OnError) | Bir öğe düzgün yüklenmediğinde veya betik yürütmesi sırasında bir hata oluştuğunda oluşur. |
| [OnResize](#OnResize) | Bir belge görünümü yeniden boyutlandırıldığında oluşur. |
| [OnScroll](#OnScroll) | Bir belge görünümü X, Y ya da her iki eksen boyunca kaydırıldığında oluşur. |
| [OnZoom](#OnZoom) | Bir belge görünümünün yakınlaştırma seviyesi değiştirildiğinde oluşur. |
| [OnBegin](#OnBegin) | Bir animasyon öğesi başladığında oluşur. |
| [OnEnd](#OnEnd) | Bir animasyon öğesi bittiğinde oluşur. |
| [OnRepeat](#OnRepeat) | Bir animasyon öğesi tekrarlandığında oluşur. |
### OnFocusIn {#OnFocusIn}
```
public static final int OnFocusIn
```

Bir öğe odaklandığında oluşur, örneğin bir metin seçildiğinde.

### OnFocusOut {#OnFocusOut}
```
public static final int OnFocusOut
```

Bir öğe odağını kaybettiğinde oluşur, örnekle bir metin seçimi kaldırıldığında.

### OnActivate {#OnActivate}
```
public static final int OnActivate
```

Bir öğe etkinleştirildiğinde oluşur, örnekle fare tıklaması veya tuşa basma yoluyla.

### OnClick {#OnClick}
```
public static final int OnClick
```

İşaretleme cihazı düğmesi bir öğe üzerinde tıklandığında oluşur.

### OnMouseDown {#OnMouseDown}
```
public static final int OnMouseDown
```

İşaretleme cihazı düğmesi bir öğe üzerinde basıldığında oluşur.

### OnMouseUp {#OnMouseUp}
```
public static final int OnMouseUp
```

İşaretleme cihazı düğmesi bir öğe üzerinde serbest bırakıldığında oluşur.

### OnMouseOver {#OnMouseOver}
```
public static final int OnMouseOver
```

İşaretleme cihazı bir öğe üzerine hareket ettiğinde oluşur.

### OnMouseMove {#OnMouseMove}
```
public static final int OnMouseMove
```

İşaretleme cihazı bir öğe üzerinde iken hareket ettiğinde oluşur.

### OnMouseOut {#OnMouseOut}
```
public static final int OnMouseOut
```

İşaretleme cihazı bir öğeden uzaklaştığında oluşur.

### OnLoad {#OnLoad}
```
public static final int OnLoad
```

Kullanıcı aracısı öğeyi, alt öğelerini ve onu renderlemek için gereken tüm başvurulan kaynakları tamamen ayrıştırdığında oluşur.

### OnUnload {#OnUnload}
```
public static final int OnUnload
```

DOM uygulaması bir belgeyi bir pencereden veya çerçeveden kaldırdığında oluşur. Only applicable to outermost svg elements.

### OnAbort {#OnAbort}
```
public static final int OnAbort
```

Bir öğenin tamamen yüklenmesine izin verilmeden sayfa yüklemesi durdurulduğunda oluşur.

### OnError {#OnError}
```
public static final int OnError
```

Bir öğe düzgün yüklenmediğinde veya betik yürütmesi sırasında bir hata oluştuğunda oluşur.

### OnResize {#OnResize}
```
public static final int OnResize
```

Bir belge görünümü yeniden boyutlandırıldığında oluşur. Only applicable to outermost svg elements.

### OnScroll {#OnScroll}
```
public static final int OnScroll
```

Bir belge görünümü X, Y ya da her iki eksen boyunca kaydırıldığında oluşur. Only applicable to outermost svg elements.

### OnZoom {#OnZoom}
```
public static final int OnZoom
```

Bir belge görünümünün yakınlaştırma seviyesi değiştirildiğinde oluşur. Only applicable to outermost svg elements.

### OnBegin {#OnBegin}
```
public static final int OnBegin
```

Bir animasyon öğesi başladığında oluşur.

### OnEnd {#OnEnd}
```
public static final int OnEnd
```

Bir animasyon öğesi bittiğinde oluşur.

### OnRepeat {#OnRepeat}
```
public static final int OnRepeat
```

Bir animasyon öğesi tekrarlandığında oluşur.