---
title: IChartParagraphFormat
second_title: Aspose.Slides for Java API संदर्भ
description: एक चार्ट के पैराग्राफ फ़ॉर्मेटिंग गुणों को प्रदर्शित करता है।
type: docs
url: /hi/com.aspose.slides/ichartparagraphformat/
---```
public interface IChartParagraphFormat
```

एक चार्ट के पैराग्राफ फ़ॉर्मेटिंग गुणों को प्रदर्शित करता है।
## मेथड्स

| विधि | विवरण |
| --- | --- |
| [getAlignment()](#getAlignment--) | एक पैराग्राफ में पाठ संरेखण को लौटाता है या सेट करता है। |
| [setAlignment(int value)](#setAlignment-int-) | एक पैराग्राफ में पाठ संरेखण को लौटाता है या सेट करता है। |
| [getSpaceWithin()](#getSpaceWithin--) | एक पैराग्राफ में बेस लाइनों के बीच स्थान की मात्रा को लौटाता है या सेट करता है। |
| [setSpaceWithin(float value)](#setSpaceWithin-float-) | एक पैराग्राफ में बेस लाइनों के बीच स्थान की मात्रा को लौटाता है या सेट करता है। |
| [getSpaceBefore()](#getSpaceBefore--) | एक पैराग्राफ में पहली लाइन से पहले स्थान की मात्रा को लौटाता है या सेट करता है। |
| [setSpaceBefore(float value)](#setSpaceBefore-float-) | एक पैराग्राफ में पहली लाइन से पहले स्थान की मात्रा को लौटाता है या सेट करता है। |
| [getSpaceAfter()](#getSpaceAfter--) | एक पैराग्राफ में अंतिम लाइन के बाद स्थान की मात्रा को लौटाता है या सेट करता है। |
| [setSpaceAfter(float value)](#setSpaceAfter-float-) | एक पैराग्राफ में अंतिम लाइन के बाद स्थान की मात्रा को लौटाता है या सेट करता है। |
| [getEastAsianLineBreak()](#getEastAsianLineBreak--) | निश्चित करता है कि क्या पैराग्राफ में ईस्ट एशियन लाइन ब्रेक उपयोग किया जाता है। |
| [setEastAsianLineBreak(byte value)](#setEastAsianLineBreak-byte-) | निश्चित करता है कि क्या पैराग्राफ में ईस्ट एशियन लाइन ब्रेक उपयोग किया जाता है। |
| [getRightToLeft()](#getRightToLeft--) | निश्चित करता है कि क्या पैराग्राफ में दाएं से बाएं लेखन उपयोग किया जाता है। |
| [setRightToLeft(byte value)](#setRightToLeft-byte-) | निश्चित करता है कि क्या पैराग्राफ में दाएं से बाएं लेखन उपयोग किया जाता है। |
| [getLatinLineBreak()](#getLatinLineBreak--) | निश्चित करता है कि क्या पैराग्राफ में लैटिन लाइन ब्रेक उपयोग किया जाता है। |
| [setLatinLineBreak(byte value)](#setLatinLineBreak-byte-) | निश्चित करता है कि क्या पैराग्राफ में लैटिन लाइन ब्रेक उपयोग किया जाता है। |
| [getHangingPunctuation()](#getHangingPunctuation--) | निश्चित करता है कि क्या पैराग्राफ में हैंगिंग पंक्चुएशन उपयोग किया जाता है। |
| [setHangingPunctuation(byte value)](#setHangingPunctuation-byte-) | निश्चित करता है कि क्या पैराग्राफ में हैंगिंग पंक्चरशन उपयोग किया जाता है। |
| [getMarginLeft()](#getMarginLeft--) | एक पैराग्राफ में बायां मार्जिन को लौटाता है या सेट करता है। |
| [setMarginLeft(float value)](#setMarginLeft-float-) | एक पैराग्राफ में बायां मार्जिन को लौटाता है या सेट करता है। |
| [getMarginRight()](#getMarginRight--) | एक पैराग्राफ में दायां मार्जिन को लौटाता है या सेट करता है। |
| [setMarginRight(float value)](#setMarginRight-float-) | एक पैराग्राफ में दायां मार्जिन को लौटाता है या सेट करता है। |
| [getIndent()](#getIndent--) | पैराग्राफ की पहली लाइन इंडेंट/हैंगिंग इंडेंट को लौटाता है या सेट करता है। |
| [setIndent(float value)](#setIndent-float-) | पैराग्राफ की पहली लाइन इंडेंट/हैंगिंग इंडेंट को लौटाता है या सेट करता है। |
| [getDefaultTabSize()](#getDefaultTabSize--) | डिफ़ॉल्ट टैबुलेशन आकार को लौटाता है या सेट करता है। |
| [setDefaultTabSize(float value)](#setDefaultTabSize-float-) | डिफ़ॉल्ट टैबुलेशन आकार को लौटाता है या सेट करता है। |
| [getTabs()](#getTabs--) | एक पैराग्राफ की टैबुलेशन को लौटाता है। |
| [getFontAlignment()](#getFontAlignment--) | एक पैराग्राफ में फ़ॉन्ट संरेखण को लौटाता है या सेट करता है। |
| [setFontAlignment(int value)](#setFontAlignment-int-) | एक पैराग्राफ में फ़ॉन्ट संरेखण को लौटाता है या सेट करता है। |
### getAlignment() {#getAlignment--}
```
public abstract int getAlignment()
```

एक पैराग्राफ में पाठ संरेखण को लौटाता है या सेट करता है। पढ़ें/लिखें [TextAlignment](../../com.aspose.slides/textalignment)।

**रिटर्न:**
int
### setAlignment(int value) {#setAlignment-int-}
```
public abstract void setAlignment(int value)
```

एक पैराग्राफ में पाठ संरेखण को लौटाता है या सेट करता है। पढ़ें/लिखें [TextAlignment](../../com.aspose.slides/textalignment)।

**पैरामीटर:**
| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | int |  |
### getSpaceWithin() {#getSpaceWithin--}
```
public abstract float getSpaceWithin()
```

एक पैराग्राफ में बेस लाइनों के बीच स्थान की मात्रा को लौटाता है या सेट करता है। पढ़ें/लिखें float।

**रिटर्न:**
float
### setSpaceWithin(float value) {#setSpaceWithin-float-}
```
public abstract void setSpaceWithin(float value)
```

एक पैराग्राफ में बेस लाइनों के बीच स्थान की मात्रा को लौटाता है या सेट करता है। पढ़ें/लिखें float।

**पैरामीटर:**
| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | float |  |
### getSpaceBefore() {#getSpaceBefore--}
```
public abstract float getSpaceBefore()
```

एक पैराग्राफ में पहली लाइन से पहले स्थान की मात्रा को लौटाता है या सेट करता है। पढ़ें/लिखें float।

**रिटर्न:**
float
### setSpaceBefore(float value) {#setSpaceBefore-float-}
```
public abstract void setSpaceBefore(float value)
```

एक पैराग्राफ में पहली लाइन से पहले स्थान की मात्रा को लौटाता है या सेट करता है। पढ़ें/लिखें float।

**पैरामीटर:**
| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | float |  |
### getSpaceAfter() {#getSpaceAfter--}
```
public abstract float getSpaceAfter()
```

एक पैराग्राफ में अंतिम लाइन के बाद स्थान की मात्रा को लौटाता है या सेट करता है। पढ़ें/लिखें float।

**रिटर्न:**
float
### setSpaceAfter(float value) {#setSpaceAfter-float-}
```
public abstract void setSpaceAfter(float value)
```

एक पैराग्राफ में अंतिम लाइन के बाद स्थान की मात्रा को लौटाता है या सेट करता है। पढ़ें/लिखें float।

**पैरामीटर:**
| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | float |  |
### getEastAsianLineBreak() {#getEastAsianLineBreak--}
```
public abstract byte getEastAsianLineBreak()
```

निश्चित करता है कि क्या पैराग्राफ में ईस्ट एशियन लाइन ब्रेक उपयोग किया जाता है। पढ़ें/लिखें [NullableBool](../../com.aspose.slides/nullablebool)।

**रिटर्न:**
byte
### setEastAsianLineBreak(byte value) {#setEastAsianLineBreak-byte-}
```
public abstract void setEastAsianLineBreak(byte value)
```

निश्चित करता है कि क्या पैराग्राफ में ईस्ट एशियन लाइन ब्रेक उपयोग किया जाता है। पढ़ें/लिखें [NullableBool](../../com.aspose.slides/nullablebool)।

**पैरामीटर:**
| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | byte |  |
### getRightToLeft() {#getRightToLeft--}
```
public abstract byte getRightToLeft()
```

निश्चित करता है कि क्या पैराग्राफ में दाएं से बाएं लेखन उपयोग किया जाता है। पढ़ें/लिखें [NullableBool](../../com.aspose.slides/nullablebool)।

**रिटर्न:**
byte
### setRightToLeft(byte value) {#setRightToLeft-byte-}
```
public abstract void setRightToLeft(byte value)
```

निश्चित करता है कि क्या पैराग्राफ में दाएं से बाएं लेखन उपयोग किया जाता है। पढ़ें/लिखें [NullableBool](../../com.aspose.slides/nullablebool)।

**पैरामीटर:**
| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | byte |  |
### getLatinLineBreak() {#getLatinLineBreak--}
```
public abstract byte getLatinLineBreak()
```

निश्चित करता है कि क्या पैराग्राफ में लैटिन लाइन ब्रेक उपयोग किया जाता है। पढ़ें/लिखें [NullableBool](../../com.aspose.slides/nullablebool)।

**रिटर्न:**
byte
### setLatinLineBreak(byte value) {#setLatinLineBreak-byte-}
```
public abstract void setLatinLineBreak(byte value)
```

निश्चित करता है कि क्या पैराग्राफ में लैटिन लाइन ब्रेक उपयोग किया जाता है। पढ़ें/लिखें [NullableBool](../../com.aspose.slides/nullablebool)।

**पैरामीटर:**
| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | byte |  |
### getHangingPunctuation() {#getHangingPunctuation--}
```
public abstract byte getHangingPunctuation()
```

निश्चित करता है कि क्या पैराग्राफ में हैंगिंग पंक्चुएशन उपयोग किया जाता है। पढ़ें/लिखें [NullableBool](../../com.aspose.slides/nullablebool)।

**रिटर्न:**
byte
### setHangingPunctuation(byte value) {#setHangingPunctuation-byte-}
```
public abstract void setHangingPunctuation(byte value)
```

निश्चित करता है कि क्या पैराग्राफ में हैंगिंग पंक्चुएशन उपयोग किया जाता है। पढ़ें/लिखें [NullableBool](../../com.aspose.slides/nullablebool)।

**पैरामीटर:**
| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | byte |  |
### getMarginLeft() {#getMarginLeft--}
```
public abstract float getMarginLeft()
```

एक पैराग्राफ में बायां मार्जिन को लौटाता है या सेट करता है। पढ़ें/लिखें float।

**रिटर्न:**
float
### setMarginLeft(float value) {#setMarginLeft-float-}
```
public abstract void setMarginLeft(float value)
```

एक पैराग्राफ में बायां मार्जिन को लौटाता है या सेट करता है। पढ़ें/लिखें float।

**पैरामीटर:**
| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | float |  |
### getMarginRight() {#getMarginRight--}
```
public abstract float getMarginRight()
```

एक पैराग्राफ में दायां मार्जिन को लौटाता है या सेट करता है। पढ़ें/लिखें float।

**रिटर्न:**
float
### setMarginRight(float value) {#setMarginRight-float-}
```
public abstract void setMarginRight(float value)
```

एक पैराग्राफ में दायां मार्जिन को लौटाता है या सेट करता है। पढ़ें/लिखें float।

**पैरामीटर:**
| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | float |  |
### getIndent() {#getIndent--}
```
public abstract float getIndent()
```

पैराग्राफ की पहली लाइन इंडेंट/हैंगिंग इंडेंट को लौटाता है या सेट करता है। हैंगिंग इंडेंट नकारात्मक मानों से परिभाषित किया जा सकता है। पढ़ें/लिखें float।

**रिटर्न:**
float
### setIndent(float value) {#setIndent-float-}
```
public abstract void setIndent(float value)
```

पैराग्राफ की पहली लाइन इंडेंट/हैंगिंग इंडेंट को लौटाता है या सेट करता है। हैंगिंग इंडेंट नकारात्मक मानों से परिभाषित किया जा सकता है। पढ़ें/लिखें float।

**पैरामीटर:**
| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | float |  |
### getDefaultTabSize() {#getDefaultTabSize--}
```
public abstract float getDefaultTabSize()
```

डिफ़ॉल्ट टैबुलेशन आकार को लौटाता है या सेट करता है। पढ़ें/लिखें float।

**रिटर्न:**
float
### setDefaultTabSize(float value) {#setDefaultTabSize-float-}
```
public abstract void setDefaultTabSize(float value)
```

डिफ़ॉल्ट टैबुलेशन आकार को लौटाता है या सेट करता है। पढ़ें/लिखें float।

**पैरामीटर:**
| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | float |  |
### getTabs() {#getTabs--}
```
public abstract ITabCollection getTabs()
```

एक पैराग्राफ की टैबुलेशन को लौटाता है। केवल-पढ़ने योग्य [ITabCollection](../../com.aspose.slides/itabcollection)।

**रिटर्न:**
[ITabCollection](../../com.aspose.slides/itabcollection)
### getFontAlignment() {#getFontAlignment--}
```
public abstract int getFontAlignment()
```

एक पैराग्राफ में फ़ॉन्ट संरेखण को लौटाता है या सेट करता है। पढ़ें/लिखें [FontAlignment](../../com.aspose.slides/fontalignment)।

**रिटर्न:**
int
### setFontAlignment(int value) {#setFontAlignment-int-}
```
public abstract void setFontAlignment(int value)
```

एक पैराग्राफ में फ़ॉन्ट संरेखण को लौटाता है या सेट करता है। पढ़ें/लिखें [FontAlignment](../../com.aspose.slides/fontalignment)।

**पैरामीटर:**
| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| value | int |  |