---
title: AnimateTextType
second_title: Aspose.Slides जावा के लिए API रेफ़रेंस
description: एक एनीमेशन इफ़ेक्ट के एनीमेट टेक्स्ट प्रकार को दर्शाता है।
type: docs
url: /hi/com.aspose.slides/animatetexttype/
---
**विरासत:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class AnimateTextType extends System.Enum
```

एक एनीमेशन इफ़ेक्ट के एनीमेट टेक्स्ट प्रकार को दर्शाता है।
## फ़ील्ड्स

| फ़ील्ड | विवरण |
| --- | --- |
| [AllAtOnce](#AllAtOnce) | सभी टेक्स्ट को एक साथ एनीमेट करें। |
| [ByWord](#ByWord) | टेक्स्ट को शब्द-वार एनीमेट करें। |
| [ByLetter](#ByLetter) | टेक्स्ट को अक्षर-वार एनीमेट करें। |
### AllAtOnce {#AllAtOnce}
```
public static final int AllAtOnce
```

सभी टेक्स्ट को एक साथ एनीमेट करें।

### ByWord {#ByWord}
```
public static final int ByWord
```

टेक्स्ट को शब्द-वार एनीमेट करें।

### ByLetter {#ByLetter}
```
public static final int ByLetter
```

टेक्स्ट को अक्षर-वार एनीमेट करें।