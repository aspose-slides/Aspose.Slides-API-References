---
title: MarkerStyleType
second_title: Αναφορά API Aspose.Slides για Java
description: Καθορίζει τη μορφή του δείκτη σε σημείο δεδομένων του διαγράμματος.
type: docs
url: /el/com.aspose.slides/markerstyletype/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class MarkerStyleType extends System.Enum
```

Καθορίζει τη μορφή του δείκτη σε σημείο δεδομένων του διαγράμματος.
## Πεδία

| Πεδίο | Περιγραφή |
| --- | --- |
| [NotDefined](#NotDefined) |  |
| [Circle](#Circle) | Καθορίζει ότι θα σχεδιαστεί κύκλος σε κάθε σημείο δεδομένων. |
| [Dash](#Dash) | Καθορίζει ότι θα σχεδιαστεί παύλα σε κάθε σημείο δεδομένων. |
| [Diamond](#Diamond) | Καθορίζει ότι θα σχεδιαστεί διαμάντι σε κάθε σημείο δεδομένων. |
| [Dot](#Dot) | Καθορίζει ότι θα σχεδιαστεί σημείο σε κάθε σημείο δεδομένων. |
| [None](#None) | Καθορίζει ότι δεν θα σχεδιαστεί τίποτα σε κάθε σημείο δεδομένων. |
| [Picture](#Picture) | Καθορίζει ότι θα σχεδιαστεί εικόνα σε κάθε σημείο δεδομένων. |
| [Plus](#Plus) | Καθορίζει ότι θα σχεδιαστεί σύμβολο συν σε κάθε σημείο δεδομένων. |
| [Square](#Square) | Καθορίζει ότι θα σχεδιαστεί τετράγωνο σε κάθε σημείο δεδομένων. |
| [Star](#Star) | Καθορίζει ότι θα σχεδιαστεί αστέρι σε κάθε σημείο δεδομένων. |
| [Triangle](#Triangle) | Καθορίζει ότι θα σχεδιαστεί τρίγωνο σε κάθε σημείο δεδομένων. |
| [X](#X) | Καθορίζει ότι θα σχεδιαστεί X σε κάθε σημείο δεδομένων. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```




### Circle {#Circle}
```
public static final int Circle
```

Καθορίζει ότι θα σχεδιαστεί κύκλος σε κάθε σημείο δεδομένων.

### Dash {#Dash}
```
public static final int Dash
```

Καθορίζει ότι θα σχεδιαστεί παύλα σε κάθε σημείο δεδομένων.

### Diamond {#Diamond}
```
public static final int Diamond
```

Καθορίζει ότι θα σχεδιαστεί διαμάντι σε κάθε σημείο δεδομένων.

### Dot {#Dot}
```
public static final int Dot
```

Καθορίζει ότι θα σχεδιαστεί σημείο σε κάθε σημείο δεδομένων.

### None {#None}
```
public static final int None
```

Καθορίζει ότι δεν θα σχεδιαστεί τίποτα σε κάθε σημείο δεδομένων.

### Picture {#Picture}
```
public static final int Picture
```

Καθορίζει ότι θα σχεδιαστεί εικόνα σε κάθε σημείο δεδομένων.

### Plus {#Plus}
```
public static final int Plus
```

Καθορίζει ότι θα σχεδιαστεί σύμβολο συν σε κάθε σημείο δεδομένων.

### Square {#Square}
```
public static final int Square
```

Καθορίζει ότι θα σχεδιαστεί τετράγωνο σε κάθε σημείο δεδομένων.

### Star {#Star}
```
public static final int Star
```

Καθορίζει ότι θα σχεδιαστεί αστέρι σε κάθε σημείο δεδομένων.

### Triangle {#Triangle}
```
public static final int Triangle
```

Καθορίζει ότι θα σχεδιαστεί τρίγωνο σε κάθε σημείο δεδομένων.

### X {#X}
```
public static final int X
```

Καθορίζει ότι θα σχεδιαστεί X σε κάθε σημείο δεδομένων.