---
title: MotionEffect
second_title: Aspose.Slides για την τεκμηρίωση API Java
description: Αναπαριστά τη συμπεριφορά του εφέ κίνησης.
type: docs
url: /el/com.aspose.slides/motioneffect/
---
**Κληρονόμηση:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**Όλες οι υλοποιημένες διεπαφές:**
[com.aspose.slides.IMotionEffect](../../com.aspose.slides/imotioneffect)
```
public class MotionEffect extends Behavior implements IMotionEffect
```

Αναπαριστά τη συμπεριφορά του εφέ κίνησης του εφέ.
## Κατασκευαστές

| Κατασκευαστής | Περιγραφή |
| --- | --- |
| [MotionEffect()](#MotionEffect--) |  |
## Μέθοδοι

| Μέθοδος | Περιγραφή |
| --- | --- |
| [getFrom()](#getFrom--) | Καθορίζει ένα σημείο x/y από το οποίο ξεκινά η κίνηση (σε ποσοστά). |
| [setFrom(Point2D.Float value)](#setFrom-java.awt.geom.Point2D.Float-) | Καθορίζει ένα σημείο x/y από το οποίο ξεκινά η κίνηση (σε ποσοστά). |
| [getTo()](#getTo--) | Καθορίζει τη θέση στόχο για ένα εφέ κίνησης (σε ποσοστά). |
| [setTo(Point2D.Float value)](#setTo-java.awt.geom.Point2D.Float-) | Καθορίζει τη θέση στόχο για ένα εφέ κίνησης (σε ποσοστά). |
| [getBy()](#getBy--) | Περιγράφει τη σχετική τιμή μετατόπισης για την κίνηση (σε ποσοστά). |
| [setBy(Point2D.Float value)](#setBy-java.awt.geom.Point2D.Float-) | Περιγράφει τη σχετική τιμή μετατόπισης για την κίνηση (σε ποσοστά). |
| [getRotationCenter()](#getRotationCenter--) | Περιγράφει το κέντρο περιστροφής που χρησιμοποιείται για την περιστροφή μιας διαδρομής κίνησης κατά γωνία X. |
| [setRotationCenter(Point2D.Float value)](#setRotationCenter-java.awt.geom.Point2D.Float-) | Περιγράφει το κέντρο περιστροφής που χρησιμοποιείται για την περιστροφή μιας διαδρομής κίνησης κατά γωνία X. |
| [getOrigin()](#getOrigin--) | Καθορίζει τι είναι η προέλευση της διαδρομής κίνησης σε σχέση με κάτι όπως η διάταξη της διαφάνειας ή το γονικό αντικείμενο. |
| [setOrigin(int value)](#setOrigin-int-) | Καθορίζει τι είναι η προέλευση της διαδρομής κίνησης σε σχέση με κάτι όπως η διάταξη της διαφάνειας ή το γονικό αντικείμενο. |
| [getPath()](#getPath--) | Καθορίζει το βασικό στοιχείο της διαδρομής ακολουθούμενο από συντεταγμένες για την κίνηση του animation. |
| [setPath(IMotionPath value)](#setPath-com.aspose.slides.IMotionPath-) | Καθορίζει το βασικό στοιχείο της διαδρομής ακολουθούμενο από συντεταγμένες για την κίνηση του animation. |
| [getPathEditMode()](#getPathEditMode--) | Καθορίζει πώς κινείται η διαδρομή κίνησης όταν το σχήμα μετακινείται. |
| [setPathEditMode(int value)](#setPathEditMode-int-) | Καθορίζει πώς κινείται η διαδρομή κίνησης όταν το σχήμα μετακινείται. |
| [getAngle()](#getAngle--) | Περιγράφει τη σχετική γωνία της διαδρομής κίνησης. |
| [setAngle(float value)](#setAngle-float-) | Περιγράφει τη σχετική γωνία της διαδρομής κίνησης. |
### MotionEffect() {#MotionEffect--}
```
public MotionEffect()
```

### getFrom() {#getFrom--}
```
public final Point2D.Float getFrom()
```

Καθορίζει ένα σημείο x/y από το οποίο ξεκινά η κίνηση (σε ποσοστά). Ανάγνωση/εγγραφή java.awt.geom.Point2D.Float.

**Επιστρέφει:**
java.awt.geom.Point2D.Float
### setFrom(Point2D.Float value) {#setFrom-java.awt.geom.Point2D.Float-}
```
public final void setFrom(Point2D.Float value)
```

Καθορίζει ένα σημείο x/y από το οποίο ξεκινά η κίνηση (σε ποσοστά). Ανάγνωση/εγγραφή java.awt.geom.Point2D.Float.

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getTo() {#getTo--}
```
public final Point2D.Float getTo()
```

Καθορίζει τη θέση στόχο για ένα εφέ κίνησης (σε ποσοστά). Ανάγνωση/εγγραφή java.awt.geom.Point2D.Float.

**Επιστρέφει:**
java.awt.geom.Point2D.Float
### setTo(Point2D.Float value) {#setTo-java.awt.geom.Point2D.Float-}
```
public final void setTo(Point2D.Float value)
```

Καθορίζει τη θέση στόχο για ένα εφέ κίνησης (σε ποσοστά). Ανάγνωση/εγγραφή java.awt.geom.Point2D.Float.

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getBy() {#getBy--}
```
public final Point2D.Float getBy()
```

Περιγράφει τη σχετική τιμή μετατόπισης για την κίνηση (σε ποσοστά). Ανάγνωση/εγγραφή java.awt.geom.Point2D.Float.

**Επιστρέφει:**
java.awt.geom.Point2D.Float
### setBy(Point2D.Float value) {#setBy-java.awt.geom.Point2D.Float-}
```
public final void setBy(Point2D.Float value)
```

Περιγράφει τη σχετική τιμή μετατόπισης για την κίνηση (σε ποσοστά). Ανάγνωση/εγγραφή java.awt.geom.Point2D.Float.

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getRotationCenter() {#getRotationCenter--}
```
public final Point2D.Float getRotationCenter()
```

Περιγράφει το κέντρο περιστροφής που χρησιμοποιείται για την περιστροφή μιας διαδρομής κίνησης κατά γωνία X. Ανάγνωση/εγγραφή java.awt.geom.Point2D.Float.

**Επιστρέφει:**
java.awt.geom.Point2D.Float
### setRotationCenter(Point2D.Float value) {#setRotationCenter-java.awt.geom.Point2D.Float-}
```
public final void setRotationCenter(Point2D.Float value)
```

Περιγράφει το κέντρο περιστροφής που χρησιμοποιείται για την περιστροφή μιας διαδρομής κίνησης κατά γωνία X. Ανάγνωση/εγγραφή java.awt.geom.Point2D.Float.

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getOrigin() {#getOrigin--}
```
public final int getOrigin()
```

Καθορίζει τι είναι η προέλευση της διαδρομής κίνησης σε σχέση με κάτι όπως η διάταξη της διαφάνειας ή το γονικό αντικείμενο. Ανάγνωση/εγγραφή [MotionOriginType](../../com.aspose.slides/motionorigintype).

**Επιστρέφει:**
int
### setOrigin(int value) {#setOrigin-int-}
```
public final void setOrigin(int value)
```

Καθορίζει τι είναι η προέλευση της διαδρομής κίνησης σε σχέση με κάτι όπως η διάταξη της διαφάνειας ή το γονικό αντικείμενο. Ανάγνωση/εγγραφή [MotionOriginType](../../com.aspose.slides/motionorigintype).

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | int |  |

### getPath() {#getPath--}
```
public final IMotionPath getPath()
```

Καθορίζει το βασικό στοιχείο της διαδρομής ακολουθούμενο από συντεταγμένες για την κίνηση του animation. Ανάγνωση/εγγραφή [IMotionPath](../../com.aspose.slides/imotionpath).

**Επιστέφει:**
[IMotionPath](../../com.aspose.slides/imotionpath)
### setPath(IMotionPath value) {#setPath-com.aspose.slides.IMotionPath-}
```
public final void setPath(IMotionPath value)
```

Καθορίζει το βασικό στοιχείο της διαδρομής ακολουθούμενο από συντεταγμένες για την κίνηση του animation. Ανάγνωση/εγγραφή [IMotionPath](../../com.aspose.slides/imotionpath).

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | [IMotionPath](../../com.aspose.slides/imotionpath) |  |

### getPathEditMode() {#getPathEditMode--}
```
public final int getPathEditMode()
```

Καθορίζει πώς κινείται η διαδρομή κίνησης όταν το σχήμα μετακινείται. Ανάγνωση/εγγραφή [MotionPathEditMode](../../com.aspose.slides/motionpatheditmode).

**Επιστέφει:**
int
### setPathEditMode(int value) {#setPathEditMode-int-}
```
public final void setPathEditMode(int value)
```

Καθορίζει πώς κινείται η διαδρομή κίνησης όταν το σχήμα μετακινείται. Ανάγνωση/εγγραφή [MotionPathEditMode](../../com.aspose.slides/motionpatheditmode).

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | int |  |

### getAngle() {#getAngle--}
```
public final float getAngle()
```

Περιγράφει τη σχετική γωνία της διαδρομής κίνησης. Ανάγνωση/εγγραφή float.

**Επιστέφει:**
float
### setAngle(float value) {#setAngle-float-}
```
public final void setAngle(float value)
```

Περιγράφει τη σχετική γωνία της διαδρομής κίνησης. Ανάγνωση/εγγραφή float.

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | float |  |