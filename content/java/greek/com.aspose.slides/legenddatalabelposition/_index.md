---
title: LegendDataLabelPosition
second_title: Αναφορά API Aspose.Slides για Java
description: Καθορίζει τη θέση των ετικετών δεδομένων.
type: docs
url: /el/com.aspose.slides/legenddatalabelposition/
---
**Κληρονομικότητα:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class LegendDataLabelPosition extends System.Enum
```

Καθορίζει τη θέση των ετικετών δεδομένων.
## Πεδία

| Πεδίο | Περιγραφή |
| --- | --- |
| [NotDefined](#NotDefined) | Η θέση δεν έχει οριστεί. |
| [Bottom](#Bottom) | Καθορίζει ότι οι ετικέτες δεδομένων πρέπει να εμφανίζονται κάτω από το σημάδι δεδομένων. |
| [BestFit](#BestFit) | Καθορίζει ότι οι ετικέτες δεδομένων πρέπει να εμφανίζονται στην καλύτερη θέση. |
| [Center](#Center) | Καθορίζει ότι οι ετικέτες δεδομένων πρέπει να εμφανίζονται κεντραρισμένες στο σημάδι δεδομένων. |
| [InsideBase](#InsideBase) | Καθορίζει ότι οι ετικέτες δεδομένων πρέπει να εμφανίζονται μέσα στη βάση του σημείου δεδομένων. |
| [InsideEnd](#InsideEnd) | Καθορίζει ότι οι ετικέτες δεδομένων πρέπει να εμφανίζονται στο τέλος του σημείου δεδομένων. |
| [Left](#Left) | Καθορίζει ότι οι ετικέτες δεδομένων πρέπει να εμφανίζονται στα αριστερά του σημείου δεδομένων. |
| [OutsideEnd](#OutsideEnd) | Καθορίζει ότι οι ετικέτες δεδομένων πρέπει να εμφανίζονται εκτός του τέλους του σημείου δεδομένων. |
| [Right](#Right) | Καθορίζει ότι οι ετικέτες δεδομένων πρέπει να εμφανίζονται στα δεξιά του σημείου δεδομένων. |
| [Top](#Top) | Καθορίζει ότι οι ετικέτες δεδομένων πρέπει να εμφανίζονται πάνω από το σημείο δεδομένων. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```


Η θέση δεν έχει οριστεί.

### Bottom {#Bottom}
```
public static final int Bottom
```


Καθορίζει ότι οι ετικέτες δεδομένων πρέπει να εμφανίζονται κάτω από το σημάδι δεδομένων.

### BestFit {#BestFit}
```
public static final int BestFit
```


Καθορίζει ότι οι ετικέτες δεδομένων πρέπει να εμφανίζονται στην καλύτερη θέση.

### Center {#Center}
```
public static final int Center
```


Καθορίζει ότι οι ετικέτες δεδομένων πρέπει να εμφανίζονται κεντραρισμένες στο σημάδι δεδομένων.

### InsideBase {#InsideBase}
```
public static final int InsideBase
```


Καθορίζει ότι οι ετικέτες δεδομένων πρέπει να εμφανίζονται μέσα στη βάση του σημείου δεδομένων.

### InsideEnd {#InsideEnd}
```
public static final int InsideEnd
```


Καθορίζει ότι οι ετικέτες δεδομένων πρέπει να εμφανίζονται στο τέλος του σημείου δεδομένων.

### Left {#Left}
```
public static final int Left
```


Καθορίζει ότι οι ετικέτες δεδομένων πρέπει να εμφανίζονται στα αριστερά του σημείου δεδομένων.

### OutsideEnd {#OutsideEnd}
```
public static final int OutsideEnd
```


Καθορίζει ότι οι ετικέτες δεδομένων πρέπει να εμφανίζονται εκτός του τέλους του σημείου δεδομένων.

### Right {#Right}
```
public static final int Right
```


Καθορίζει ότι οι ετικέτες δεδομένων πρέπει να εμφανίζονται στα δεξιά του σημείου δεδομένων.

### Top {#Top}
```
public static final int Top
```


Καθορίζει ότι οι ετικέτες δεδομένων πρέπει να εμφανίζονται πάνω από το σημείο δεδομένων.