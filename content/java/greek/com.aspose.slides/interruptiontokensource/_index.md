---
title: InterruptionTokenSource
second_title: Aspose.Slides για την αναφορά API της Java
description: Αντιπροσωπεύει την πηγή του .
type: docs
url: /el/com.aspose.slides/interruptiontokensource/
---
**Κληρονομικότητα:**
java.lang.Object
```
public class InterruptionTokenSource
```

Αντιπροσωπεύει την πηγή του [InterruptionToken](../../com.aspose.slides/interruptiontoken).
## Κατασκευαστές

| Constructor | Περιγραφή |
| --- | --- |
| [InterruptionTokenSource()](#InterruptionTokenSource--) | Δημιουργεί ένα νέο [InterruptionTokenSource](../../com.aspose.slides/interruptiontokensource). |
## Μέθοδοι

| Method | Περιγραφή |
| --- | --- |
| [getToken()](#getToken--) | Επιστρέφει ένα νέο διακριτικό που συνδέεται με αυτό το [InterruptionTokenSource](../../com.aspose.slides/interruptiontokensource). |
| [isInterruptionRequested()](#isInterruptionRequested--) | Επιστρέφει true αν έχει ζητηθεί διακοπή, false διαφορετικά. |
| [interrupt()](#interrupt--) | Αρχικοποιεί αίτημα για διακοπή. |
### InterruptionTokenSource() {#InterruptionTokenSource--}
```
public InterruptionTokenSource()
```


Δημιουργεί ένα νέο [InterruptionTokenSource](../../com.aspose.slides/interruptiontokensource).

### getToken() {#getToken--}
```
public final InterruptionToken getToken()
```


Επιστρέφει ένα νέο διακριτικό που συνδέεται με αυτό το [InterruptionTokenSource](../../com.aspose.slides/interruptiontokensource).

**Επιστρέφει:**
[InterruptionToken](../../com.aspose.slides/interruptiontoken)
### isInterruptionRequested() {#isInterruptionRequested--}
```
public final boolean isInterruptionRequested()
```


Επιστρέφει true αν έχει ζητηθεί διακοπή, false διαφορετικά.

**Επιστρέφει:**
boolean
### interrupt() {#interrupt--}
```
public final void interrupt()
```


Αρχικοποιεί αίτημα για διακοπή.