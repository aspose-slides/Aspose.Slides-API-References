---
title: GradientFormat
second_title: Αναφορά API του Aspose.Slides για Java
description: Αναπαριστά μορφή διαβάθμισης.
type: docs
url: /el/com.aspose.slides/gradientformat/
---
**Κληρονομικότητα:**
java.lang.Object, [com.aspose.slides.PVIObject](../../com.aspose.slides/pviobject)

**Όλες οι Υλοποιημένες Διεπαφές:**
[com.aspose.slides.IGradientFormat](../../com.aspose.slides/igradientformat)
```
public final class GradientFormat extends PVIObject implements IGradientFormat
```

Αναπαριστά μορφή διαβάθμισης.
## Μέθοδοι

| Μέθοδος | Περιγραφή |
| --- | --- |
| [getVersion()](#getVersion--) |  |
| [getTileFlip()](#getTileFlip--) | Επιστρέφει ή ορίζει τη λειτουργία ανάστροφης για μια διαβάθμιση. |
| [setTileFlip(int value)](#setTileFlip-int-) | Επιστρέφει ή ορίζει τη λειτουργία ανάστροφης για μια διαβάθμιση. |
| [getGradientDirection()](#getGradientDirection--) | Επιστρέφει ή ορίζει το στυλ μιας διαβάθμισης. |
| [setGradientDirection(int value)](#setGradientDirection-int-) | Επιστρέφει ή ορίζει το στυλ μιας διαβάθμισης. |
| [getLinearGradientAngle()](#getLinearGradientAngle--) | Επιστρέφει ή ορίζει τη γωνία μιας διαβάθμισης. |
| [setLinearGradientAngle(float value)](#setLinearGradientAngle-float-) | Επιστρέφει ή ορίζει τη γωνία μιας διαβάθμισης. |
| [getLinearGradientScaled()](#getLinearGradientScaled--) | Καθορίζει εάν μια διαβάθμιση είναι κλιμακωμένη. |
| [setLinearGradientScaled(byte value)](#setLinearGradientScaled-byte-) | Καθορίζει εάν μια διαβάθμιση είναι κλιμακωμένη. |
| [getGradientShape()](#getGradientShape--) | Επιστρέφει ή ορίζει το σχήμα μιας διαβάθμισης. |
| [setGradientShape(byte value)](#setGradientShape-byte-) | Επιστρέφει ή ορίζει το σχήμα μιας διαβάθμισης. |
| [getGradientStops()](#getGradientStops--) | Επιστρέφει τη συλλογή των σημείων διαβάθμισης. |
### getVersion() {#getVersion--}
```
public long getVersion()
```

Έκδοση. Μόνο για ανάγνωση long.

**Επιστρέφει:**
long
### getTileFlip() {#getTileFlip--}
```
public final int getTileFlip()
```

Επιστρέφει ή ορίζει τη λειτουργία ανάστροφης για μια διαβάθμιση. Αναγν/εγγρ [TileFlip](../../com.aspose.slides/tileflip).

**Επιστρέφει:**
int
### setTileFlip(int value) {#setTileFlip-int-}
```
public final void setTileFlip(int value)
```

Επιστρέφει ή ορίζει τη λειτουργία ανάστροφης για μια διαβάθμιση. Αναγν/εγγρ [TileFlip](../../com.aspose.slides/tileflip).

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | int |  |

### getGradientDirection() {#getGradientDirection--}
```
public final int getGradientDirection()
```

Επιστρέφει ή ορίζει το στυλ μιας διαβάθμισης. Αναγν/εγγρ [GradientDirection](../../com.aspose.slides/gradientdirection).

**Επιστρέφει:**
int
### setGradientDirection(int value) {#setGradientDirection-int-}
```
public final void setGradientDirection(int value)
```

Επιστρέφει ή ορίζει το στυλ μιας διαβάθμισης. Αναγν/εγγρ [GradientDirection](../../com.aspose.slides/gradientdirection).

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | int |  |

### getLinearGradientAngle() {#getLinearGradientAngle--}
```
public final float getLinearGradientAngle()
```

Επιστρέφει ή ορίζει τη γωνία μιας διαβάθμισης. Αναγν/εγγρ float.

**Επιστρέφει:**
float
### setLinearGradientAngle(float value) {#setLinearGradientAngle-float-}
```
public final void setLinearGradientAngle(float value)
```

Επιστρέφει ή ορίζει τη γωνία μιας διαβάθμισης. Αναγν/εγγρ float.

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | float |  |

### getLinearGradientScaled() {#getLinearGradientScaled--}
```
public final byte getLinearGradientScaled()
```

Καθορίζει εάν μια διαβάθμιση είναι κλιμακωμένη. Αναγν/εγγρ [NullableBool](../../com.aspose.slides/nullablebool).

**Επιστρέφει:**
byte
### setLinearGradientScaled(byte value) {#setLinearGradientScaled-byte-}
```
public final void setLinearGradientScaled(byte value)
```

Καθορίζει εάν μια διαβάθμιση είναι κλιμακωμένη. Αναγν/εγγρ [NullableBool](../../com.aspose.slides/nullablebool).

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | byte |  |

### getGradientShape() {#getGradientShape--}
```
public final byte getGradientShape()
```

Επιστρέφει ή ορίζει το σχήμα μιας διαβάθμισης. Αναγν/εγγρ [GradientShape](../../com.aspose.slides/gradientshape).

**Επιστρέφει:**
byte
### setGradientShape(byte value) {#setGradientShape-byte-}
```
public final void setGradientShape(byte value)
```

Επιστρέφει ή ορίζει το σχήμα μιας διαβάθμισης. Αναγν/εγγρ [GradientShape](../../com.aspose.slides/gradientshape).

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | byte |  |

### getGradientStops() {#getGradientStops--}
```
public final IGradientStopCollection getGradientStops()
```

Επιστρέφει τη συλλογή των σημείων διαβάθμισης. Μόνο για ανάγνωση [IGradientStopCollection](../../com.aspose.slides/igradientstopcollection).

**Επιστρέφει:**
[IGradientStopCollection](../../com.aspose.slides/igradientstopcollection)