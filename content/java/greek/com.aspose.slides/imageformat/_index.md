---
title: ImageFormat
second_title: Aspose.Slides για την αναφορά API Java
description: Αναπαριστά τη μορφή αρχείου της εικόνας.
type: docs
url: /el/com.aspose.slides/imageformat/
---
**Κληρονομικότητα:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class ImageFormat extends System.Enum
```

Αναπαριστά τη μορφή αρχείου της εικόνας.

## Πεδία

| Πεδίο | Περιγραφή |
| --- | --- |
| [MemoryBmp](#MemoryBmp) | Αναπαριστά τη μορφή ενός bitmap στη μνήμη. |
| [Bmp](#Bmp) | Αναπαριστά τη μορφή εικόνας bitmap (BMP). |
| [Emf](#Emf) | Αναπαριστά τη μορφή εικόνας βελτιωμένου μεταφαίκολου (EMF). |
| [Wmf](#Wmf) | Αναπαριστά τη μορφή εικόνας μεταφαίκολου Windows (WMF). |
| [Gif](#Gif) | Αναπαριστά τη μορφή εικόνας Graphics Interchange Format (GIF). |
| [Jpeg](#Jpeg) | Αναπαριστά τη μορφή εικόνας Joint Photographic Experts Group (JPEG). |
| [Png](#Png) | Αναπαριστά τη μορφή εικόνας W3C Portable Network Graphics (PNG). |
| [Tiff](#Tiff) | Αναπαριστά τη μορφή εικόνας Tagged Image File Format (TIFF). |
| [Exif](#Exif) | Αναπαριστά τη μορφή Exchangeable Image File (Exif). |
| [Icon](#Icon) | Αναπαριστά τη μορφή εικόνας εικονιδίου Windows. |

### MemoryBmp {#MemoryBmp}
```
public static final int MemoryBmp
```

Αναπαριστά τη μορφή ενός bitmap στη μνήμη.

### Bmp {#Bmp}
```
public static final int Bmp
```

Αναπαριστά τη μορφή εικόνας bitmap (BMP).

### Emf {#Emf}
```
public static final int Emf
```

Αναπαριστά τη μορφή εικόνας βελτιωμένου μεταφαίκολου (EMF).

### Wmf {#Wmf}
```
public static final int Wmf
```

Αναπαριστά τη μορφή εικόνας μεταφαίκολου Windows (WMF).

### Gif {#Gif}
```
public static final int Gif
```

Αναπαριστά τη μορφή εικόνας Graphics Interchange Format (GIF).

### Jpeg {#Jpeg}
```
public static final int Jpeg
```

Αναπαριστά τη μορφή εικόνας Joint Photographic Experts Group (JPEG).

### Png {#Png}
```
public static final int Png
```

Αναπαριστά τη μορφή εικόνας W3C Portable Network Graphics (PNG).

### Tiff {#Tiff}
```
public static final int Tiff
```

Αναπαριστά τη μορφή εικόνας Tagged Image File Format (TIFF).

### Exif {#Exif}
```
public static final int Exif
```

Αναπαριστά τη μορφή Exchangeable Image File (Exif).

### Icon {#Icon}
```
public static final int Icon
```

Αναπαριστά τη μορφή εικόνας εικονιδίου Windows.