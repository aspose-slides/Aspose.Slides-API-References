---
title: ISetEffect
second_title: Aspose.Slides για την Αναφορά API της Java
description: Αντιπροσωπεύει ένα σύνολο εφέ για μια συμπεριφορά κίνησης.
type: docs
url: /el/com.aspose.slides/iseteffect/
---
**Όλες οι υλοποιημένες διεπαφές:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface ISetEffect extends IBehavior
```

Αντιπροσωπεύει ένα σύνολο εφέ για μια συμπεριφορά κίνησης.
## Μέθοδοι

| Μέθοδος | Περιγραφή |
| --- | --- |
| [getTo()](#getTo--) | Καθορίζει το συγκεκριμένο χαρακτηριστικό ενός εφέ μετά από ένα εφέ κίνησης. |
| [setTo(Object value)](#setTo-java.lang.Object-) | Καθορίζει το συγκεκριμένο χαρακτηριστικό ενός εφέ μετά από ένα εφέ κίνησης. |
### getTo() {#getTo--}
```
public abstract Object getTo()
```

Καθορίζει το συγκεκριμένο χαρακτηριστικό ενός εφέ μετά από ένα εφέ κίνησης. Αντιπροσωπεύει τιμή σημείου. Μόνο: bool, ColorFormat, float, int, string. Ανάγνωση/εγγραφή Object.

**Επιστρέφει:**
java.lang.Object
### setTo(Object value) {#setTo-java.lang.Object-}
```
public abstract void setTo(Object value)
```

Καθορίζει το συγκεκριμένο χαρακτηριστικό ενός εφέ μετά από ένα εφέ κίνησης. Αντιπροσωπεύει τιμή σημείου. Μόνο: bool, ColorFormat, float, int, string. Ανάγνωση/εγγραφή Object.

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | java.lang.Object |  |