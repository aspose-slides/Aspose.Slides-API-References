---
title: Placeholder
second_title: Αναφορά API του Aspose.Slides για Java
description: Αναπαριστά έναν placeholder σε μια διαφάνεια.
type: docs
url: /el/com.aspose.slides/placeholder/
---
**Inheritance:**
java.lang.Object

**All Implemented Interfaces:**
[com.aspose.slides.IPlaceholder](../../com.aspose.slides/iplaceholder)
```
public class Placeholder implements IPlaceholder
```

Αναπαριστά έναν placeholder σε μια διαφάνεια.
## Μέθοδοι

| Μέθοδος | Περιγραφή |
| --- | --- |
| [getOrientation()](#getOrientation--) | Αποκτά ή ορίζει τον προσανατολισμό ενός placeholder. |
| [getSize()](#getSize--) | Αποκτά ή ορίζει το μέγεθος ενός placeholder. |
| [getType()](#getType--) | Αποκτά ή ορίζει τον τύπο ενός placeholder. |
| [getIndex()](#getIndex--) | Αποκτά ή ορίζει το ευρετήριο ενός placeholder. |
### getOrientation() {#getOrientation--}
```
public byte getOrientation()
```

Αποκτά ή ορίζει τον προσανατολισμό ενός placeholder. Μόνο-ανάγνωση [Orientation](../../com.aspose.slides/orientation).

**Επιστρέφει:**
byte
### getSize() {#getSize--}
```
public byte getSize()
```

Αποκτά ή ορίζει το μέγεθος ενός placeholder. Μόνο-ανάγνωση [PlaceholderSize](../../com.aspose.slides/placeholdersize).

**Επιστρέφει:**
byte
### getType() {#getType--}
```
public byte getType()
```

Αποκτά ή ορίζει τον τύπο ενός placeholder. Μόνο-ανάγνωση [PlaceholderType](../../com.aspose.slides/placeholdertype).

**Επιστρέφει:**
byte
### getIndex() {#getIndex--}
```
public long getIndex()
```

Αποκτά ή ορίζει το ευρετήριο ενός placeholder. Μόνο-ανάγνωση long.

**Επιστρέφει:**
long