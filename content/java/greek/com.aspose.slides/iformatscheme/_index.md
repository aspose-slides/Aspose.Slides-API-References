---
title: IFormatScheme
second_title: Aspose.Slides για την Αναφορά API της Java
description: Αποθηκεύει μορφές που ορίζονται από το θέμα για τα σχήματα.
type: docs
url: /el/com.aspose.slides/iformatscheme/
---
**Όλες οι υλοποιημένες διεπαφές:**
[com.aspose.slides.ISlideComponent](../../com.aspose.slides/islidecomponent)
```
public interface IFormatScheme extends ISlideComponent
```

Αποθηκεύει μορφές που ορίζονται από το θέμα για τα σχήματα.
## Μέθοδοι

| Μέθοδος | Περιγραφή |
| --- | --- |
| [getFillStyles()](#getFillStyles--) | Επιστρέφει μια συλλογή στυλ γεμίσματος που ορίζονται από το θέμα. |
| [getLineStyles()](#getLineStyles--) | Επιστρέφει μια συλλογή στυλ γραμμής που ορίζονται από το θέμα. |
| [getEffectStyles()](#getEffectStyles--) | Επιστρέφει μια συλλογή στυλ εφέ που ορίζονται από το θέμα. |
| [getBackgroundFillStyles()](#getBackgroundFillStyles--) | Επιστρέφει μια συλλογή στυλ γεμίσματος φόντου που ορίζονται από το θέμα. |
### getFillStyles() {#getFillStyles--}
```
public abstract IFillFormatCollection getFillStyles()
```

Επιστρέφει μια συλλογή στυλ γεμίσματος που ορίζονται από το θέμα. Μόνο για ανάγνωση [IFillFormatCollection](../../com.aspose.slides/ifillformatcollection).

**Επιστρέφει:**
[IFillFormatCollection](../../com.aspose.slides/ifillformatcollection)
### getLineStyles() {#getLineStyles--}
```
public abstract ILineFormatCollection getLineStyles()
```

Επιστρέφει μια συλλογή στυλ γραμμής που ορίζονται από το θέμα. Μόνο για ανάγνωση [ILineFormatCollection](../../com.aspose.slides/ilineformatcollection).

**Επιστρέφει:**
[ILineFormatCollection](../../com.aspose.slides/ilineformatcollection)
### getEffectStyles() {#getEffectStyles--}
```
public abstract IEffectStyleCollection getEffectStyles()
```

Επιστρέφει μια συλλογή στυλ εφέ που ορίζονται από το θέμα. Μόνο για ανάγνωση [IEffectStyleCollection](../../com.aspose.slides/ieffectstylecollection).

**Επιστρέφει:**
[IEffectStyleCollection](../../com.aspose.slides/ieffectstylecollection)
### getBackgroundFillStyles() {#getBackgroundFillStyles--}
```
public abstract IFillFormatCollection getBackgroundFillStyles()
```

Επιστρέφει μια συλλογή στυλ γεμίσματος φόντου που ορίζονται από το θέμα. Μόνο για ανάγνωση [IFillFormatCollection](../../com.aspose.slides/ifillformatcollection).

**Επιστρέφει:**
[IFillFormatCollection](../../com.aspose.slides/ifillformatcollection)