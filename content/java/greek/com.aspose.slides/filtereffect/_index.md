---
title: FilterEffect
second_title: Aspose.Slides για την Αναφορά API της Java
description: Αναπαριστά το εφέ φίλτρου της συμπεριφοράς.
type: docs
url: /el/com.aspose.slides/filtereffect/
---
**Κληρονομικότητα:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**Όλες οι Υλοποιημένες Διεπαφές:**
[com.aspose.slides.IFilterEffect](../../com.aspose.slides/ifiltereffect)
```
public class FilterEffect extends Behavior implements IFilterEffect
```

Αναπαριστά το εφέ φίλτρου της συμπεριφοράς.
## Κατασκευαστές

| Κατασκευαστής | Περιγραφή |
| --- | --- |
| [FilterEffect()](#FilterEffect--) | Προεπιλεγμένος κατασκευαστής. |
## Μέθοδοι

| Μέθοδος | Περιγραφή |
| --- | --- |
| [getReveal()](#getReveal--) | Αναπαριστά ότι το εφέ με τη συμπεριφορά πρέπει να αποκαλύπτεται (in/out) Ανάγνωση/εγγραφή [FilterEffectRevealType](../../com.aspose.slides/filtereffectrevealtype). |
| [setReveal(int value)](#setReveal-int-) | Αναπαριστά ότι το εφέ με τη συμπεριφορά πρέπει να αποκαλύπτεται (in/out) Ανάγνωση/εγγραφή [FilterEffectRevealType](../../com.aspose.slides/filtereffectrevealtype). |
| [getType()](#getType--) | Αναπαριστά τύπο φίλτρου εφέ. |
| [setType(int value)](#setType-int-) | Αναπαριστά τύπο φίλτρου εφέ. |
| [getSubtype()](#getSubtype--) | Αναπαριστά υποτύπο φίλτρου εφέ. |
| [setSubtype(int value)](#setSubtype-int-) | Αναπαριστά υποτύπο φίλτρου εφέ. |
### FilterEffect() {#FilterEffect--}
```
public FilterEffect()
```

Προεπιλεγμένος κατασκευαστής.

### getReveal() {#getReveal--}
```
public final int getReveal()
```

Αναπαριστά ότι το εφέ με τη συμπεριφορά πρέπει να αποκαλύπτεται (in/out) Ανάγνωση/εγγραφή [FilterEffectRevealType](../../com.aspose.slides/filtereffectrevealtype).

**Επιστρέφει:**
int
### setReveal(int value) {#setReveal-int-}
```
public final void setReveal(int value)
```

Αναπαριστά ότι το εφέ με τη συμπεριφορά πρέπει να αποκαλύπτεται (in/out) Ανάγνωση/εγγραφή [FilterEffectRevealType](../../com.aspose.slides/filtereffectrevealtype).

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | int |  |

### getType() {#getType--}
```
public final int getType()
```

Αναπαριστά τύπο φίλτρου εφέ. Ανάγνωση/εγγραφή [FilterEffectType](../../com.aspose.slides/filtereffecttype).

**Επιστρέφει:**
int
### setType(int value) {#setType-int-}
```
public final void setType(int value)
```

Αναπαριστά τύπο φίλτρου εφέ. Ανάγνωση/εγγραφή [FilterEffectType](../../com.aspose.slides/filtereffecttype).

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | int |  |

### getSubtype() {#getSubtype--}
```
public final int getSubtype()
```

Αναπαριστά υποτύπο φίλτρου εφέ. Ανάγνωση/εγγραφή [FilterEffectSubtype](../../com.aspose.slides/filtereffectsubtype).

**Επιστρέφει:**
int
### setSubtype(int value) {#setSubtype-int-}
```
public final void setSubtype(int value)
```

Αναπαριστά υποτύπο φίλτρου εφέ. Ανάγνωση/εγγραφή [FilterEffectSubtype](../../com.aspose.slides/filtereffectsubtype).

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | int |  |