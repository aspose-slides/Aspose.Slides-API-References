---
title: SvgEvent
second_title: Αναφορά API του Aspose.Slides για Java
description: Αναπαριστά επιλογές για σχήμα SVG.
type: docs
url: /el/com.aspose.slides/svgevent/
---
**Κληρονομικότητα:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class SvgEvent extends System.Enum
```

Αναπαριστά επιλογές για σχήμα SVG.
## Πεδία

| Πεδίο | Περιγραφή |
| --- | --- |
| [OnFocusIn](#OnFocusIn) | Συμβαίνει όταν ένα στοιχείο λαμβάνει εστίαση, όπως όταν ένα κείμενο επιλέγεται. |
| [OnFocusOut](#OnFocusOut) | Συμβαίνει όταν ένα στοιχείο χάνει εστίαση, όπως όταν ένα κείμενο απεπιλέγεται. |
| [OnActivate](#OnActivate) | Συμβαίνει όταν ένα στοιχείο ενεργοποιείται, για παράδειγμα μέσω κλικ του ποντικιού ή πίεσης πλήκτρου. |
| [OnClick](#OnClick) | Συμβαίνει όταν το κουμπί της συσκευής κατάδειξης κάνει κλικ πάνω σε ένα στοιχείο. |
| [OnMouseDown](#OnMouseDown) | Συμβαίνει όταν το κουμπί της συσκευής κατάδειξης πιέζεται πάνω σε ένα στοιχείο. |
| [OnMouseUp](#OnMouseUp) | Συμβαίνει όταν το κουμπί της συσκευής κατάδειξης αφήνεται πάνω σε ένα στοιχείο. |
| [OnMouseOver](#OnMouseOver) | Συμβαίνει όταν η συσκευή κατάδειξης μετακινείται πάνω σε ένα στοιχείο. |
| [OnMouseMove](#OnMouseMove) | Συμβαίνει όταν η συσκευή κατάδειξης μετακινείται ενώ βρίσκεται πάνω σε ένα στοιχείο. |
| [OnMouseOut](#OnMouseOut) | Συμβαίνει όταν η συσκευή κατάδειξης απομακρύνεται από ένα στοιχείο. |
| [OnLoad](#OnLoad) | Συμβαίνει όταν ο πράκτορας χρήστη έχει επεξεργαστεί πλήρως το στοιχείο και τους απογόνους του καθώς και όλους τους αναφερθέντες πόρους, που απαιτούνται για την απόδοσή του. |
| [OnUnload](#OnUnload) | Συμβαίνει όταν η υλοποίηση DOM αφαιρεί ένα έγγραφο από ένα παράθυρο ή πλαίσιο. |
| [OnAbort](#OnAbort) | Συμβαίνει όταν η φόρτωση της σελίδας διακόπτεται πριν επιτραπεί η πλήρης φόρτωση ενός στοιχείου. |
| [OnError](#OnError) | Συμβαίνει όταν ένα στοιχείο δεν φορτώνεται σωστά ή όταν προκύπτει σφάλμα κατά την εκτέλεση του σεναρίου. |
| [OnResize](#OnResize) | Συμβαίνει όταν η προβολή του εγγράφου αλλάζει μέγεθος. |
| [OnScroll](#OnScroll) | Συμβαίνει όταν η προβολή του εγγράφου μετακινείται κατά μήκος του άξονα X ή Y ή και των δύο. |
| [OnZoom](#OnZoom) | Συμβαίνει όταν το επίπεδο ζουμ της προβολής του εγγράφου αλλάζει. |
| [OnBegin](#OnBegin) | Συμβαίνει όταν ένα στοιχείο animation αρχίζει. |
| [OnEnd](#OnEnd) | Συμβαίνει όταν ένα στοιχείο animation λήγει. |
| [OnRepeat](#OnRepeat) | Συμβαίνει όταν ένα στοιχείο animation επαναλαμβάνεται. |
### OnFocusIn {#OnFocusIn}
```
public static final int OnFocusIn
```

Συμβαίνει όταν ένα στοιχείο λαμβάνει εστίαση, όπως όταν ένα κείμενο επιλέγεται.

### OnFocusOut {#OnFocusOut}
```
public static final int OnFocusOut
```

Συμβαίνει όταν ένα στοιχείο χάνει εστίαση, όπως όταν ένα κείμενο απεπιλέγεται.

### OnActivate {#OnActivate}
```
public static final int OnActivate
```

Συμβαίνει όταν ένα στοιχείο ενεργοποιείται, για παράδειγμα μέσω κλικ του ποντικιού ή πίεσης πλήκτρου.

### OnClick {#OnClick}
```
public static final int OnClick
```

Συμβαίνει όταν το κουμπί της συσκευής κατάδειξης κάνει κλικ πάνω σε ένα στοιχείο.

### OnMouseDown {#OnMouseDown}
```
public static final int OnMouseDown
```

Συμβαίνει όταν το κουμπί της συσκευής κατάδειξης πιέζεται πάνω σε ένα στοιχείο.

### OnMouseUp {#OnMouseUp}
```
public static final int OnMouseUp
```

Συμβαίνει όταν το κουμπί της συσκευής κατάδειξης αφήνεται πάνω σε ένα στοιχείο.

### OnMouseOver {#OnMouseOver}
```
public static final int OnMouseOver
```

Συμβαίνει όταν η συσκευή κατάδειξης μετακινείται πάνω σε ένα στοιχείο.

### OnMouseMove {#OnMouseMove}
```
public static final int OnMouseMove
```

Συμβαίνει όταν η συσκευή κατάδειξης μετακινείται ενώ βρίσκεται πάνω σε ένα στοιχείο.

### OnMouseOut {#OnMouseOut}
```
public static final int OnMouseOut
```

Συμβαίνει όταν η συσκευή κατάδειξης απομακρύνεται από ένα στοιχείο.

### OnLoad {#OnLoad}
```
public static final int OnLoad
```

Συμβαίνει όταν ο πράκτορας χρήστη έχει επεξεργαστεί πλήρως το στοιχείο και τους απογόνους του καθώς και όλους τους αναφερθέντες πόρους, που απαιτούνται για την απόδοσή του.

### OnUnload {#OnUnload}
```
public static final int OnUnload
```

Συμβαίνει όταν η υλοποίηση DOM αφαιρεί ένα έγγραφο από ένα παράθυρο ή πλαίσιο. Only applicable to outermost svg elements.

### OnAbort {#OnAbort}
```
public static final int OnAbort
```

Συμβαίνει όταν η φόρτωση της σελίδας διακόπτεται πριν επιτραπεί η πλήρης φόρτωση ενός στοιχείου.

### OnError {#OnError}
```
public static final int OnError
```

Συμβαίνει όταν ένα στοιχείο δεν φορτώνεται σωστά ή όταν προκύπτει σφάλμα κατά την εκτέλεση του σεναρίου.

### OnResize {#OnResize}
```
public static final int OnResize
```

Συμβαίνει όταν η προβολή του εγγράφου αλλάζει μέγεθος. Only applicable to outermost svg elements.

### OnScroll {#OnScroll}
```
public static final int OnScroll
```

Συμβαίνει όταν η προβολή του εγγράφου μετακινείται κατά μήκος του άξονα X ή Y ή και των δύο. Only applicable to outermost svg elements.

### OnZoom {#OnZoom}
```
public static final int OnZoom
```

Συμβαίνει όταν το επίπεδο ζουμ της προβολής του εγγράφου αλλάζει. Only applicable to outermost svg elements.

### OnBegin {#OnBegin}
```
public static final int OnBegin
```

Συμβαίνει όταν ένα στοιχείο animation αρχίζει.

### OnEnd {#OnEnd}
```
public static final int OnEnd
```

Συμβαίνει όταν ένα στοιχείο animation λήγει.

### OnRepeat {#OnRepeat}
```
public static final int OnRepeat
```

Συμβαίνει όταν ένα στοιχείο animation επαναλαμβάνεται.