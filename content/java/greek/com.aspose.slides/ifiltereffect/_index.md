---
title: IFilterEffect
second_title: Αναφορά API Aspose.Slides για Java
description: Αναπαριστά το εφέ φίλτρου της συμπεριφοράς.
type: docs
url: /el/com.aspose.slides/ifiltereffect/
---
**Όλες οι Υλοποιημένες Διεπαφές:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IFilterEffect extends IBehavior
```

Αναπαριστά το φίλτρο εφέ της συμπεριφοράς.
## Μέθοδοι

| Μέθοδος | Περιγραφή |
| --- | --- |
| [getReveal()](#getReveal--) | Αναπαριστά το εφέ με τη συμπεριφορά που πρέπει να αποκαλύψει (in/out) Ανάγνωση/Εγγραφή [FilterEffectRevealType](../../com.aspose.slides/filtereffectrevealtype). |
| [setReveal(int value)](#setReveal-int-) | Αναπαριστά το εφέ με τη συμπεριφορά που πρέπει να αποκαλύψει (in/out) Ανάγνωση/Εγγραφή [FilterEffectRevealType](../../com.aspose.slides/filtereffectrevealtype). |
| [getType()](#getType--) | Αναπαριστά τον τύπο του φίλτρου εφέ. |
| [setType(int value)](#setType-int-) | Αναπαριστά τον τύπο του φίλτρου εφέ. |
| [getSubtype()](#getSubtype--) | Αναπαριστά τον υποτύπο του φίλτρου εφέ. |
| [setSubtype(int value)](#setSubtype-int-) | Αναπαριστά τον υποτύπο του φίλτρου εφέ. |
### getReveal() {#getReveal--}
```
public abstract int getReveal()
```

Αναπαριστά το εφέ με τη συμπεριφορά που πρέπει να αποκαλύψει (in/out) Ανάγνωση/Εγγραφή [FilterEffectRevealType](../../com.aspose.slides/filtereffectrevealtype).

**Επιστρέφει:**
int
### setReveal(int value) {#setReveal-int-}
```
public abstract void setReveal(int value)
```

Αναπαριστά το εφέ με τη συμπεριφορά που πρέπει να αποκαλύψει (in/out) Ανάγνωση/Εγγραφή [FilterEffectRevealType](../../com.aspose.slides/filtereffectrevealtype).

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | int |  |
### getType() {#getType--}
```
public abstract int getType()
```

Αναπαριστά τον τύπο του φίλτρου εφέ. Ανάγνωση/Εγγραφή [FilterEffectType](../../com.aspose.slides/filtereffecttype).

**Επιστρέφει:**
int
### setType(int value) {#setType-int-}
```
public abstract void setType(int value)
```

Αναπαριστά τον τύπο του φίλτρου εφέ. Ανάγνωση/Εγγραφή [FilterEffectType](../../com.aspose.slides/filtereffecttype).

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | int |  |
### getSubtype() {#getSubtype--}
```
public abstract int getSubtype()
```

Αναπαριστά τον υποτύπο του φίλτρου εφέ. Ανάγνωση/Εγγραφή [FilterEffectSubtype](../../com.aspose.slides/filtereffectsubtype).

**Επιστρέφει:**
int
### setSubtype(int value) {#setSubtype-int-}
```
public abstract void setSubtype(int value)
```

Αναπαριστά τον υποτύπο του φίλτρου εφέ. Ανάγνωση/Εγγραφή [FilterEffectSubtype](../../com.aspose.slides/filtereffectsubtype).

**Παράμετροι:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| value | int |  |