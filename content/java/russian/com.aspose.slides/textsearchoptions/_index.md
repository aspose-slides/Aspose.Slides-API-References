---
title: TextSearchOptions
second_title: Aspose.Slides для Java API Reference
description: Представляет параметры, которые можно использовать для поиска текста в Presentation Slide или TextFrame.
type: docs
url: /ru/com.aspose.slides/textsearchoptions/
---
**Наследование:**
java.lang.Object

**Все реализованные интерфейсы:**
[com.aspose.slides.ITextSearchOptions](../../com.aspose.slides/itextsearchoptions)
```
public class TextSearchOptions implements ITextSearchOptions
```

Представляет параметры, которые можно использовать для поиска текста в Presentation, Slide или TextFrame.
## Конструкторы

| Конструктор | Описание |
| --- | --- |
| [TextSearchOptions()](#TextSearchOptions--) | Создаёт новые параметры поиска текста по умолчанию. |
## Методы

| Метод | Описание |
| --- | --- |
| [getCaseSensitive()](#getCaseSensitive--) | Установите true, чтобы использовать поиск с учётом регистра, false — иначе. |
| [setCaseSensitive(boolean value)](#setCaseSensitive-boolean-) | Установите true, чтобы использовать поиск с учётом регистра, false — иначе. |
| [getWholeWordsOnly()](#getWholeWordsOnly--) | Установите true, чтобы сопоставлять только целые слова, false — иначе. |
| [setWholeWordsOnly(boolean value)](#setWholeWordsOnly-boolean-) | Установите true, чтобы сопоставлять только целые слова, false — иначе. |
| [getIncludeNotes()](#getIncludeNotes--) | Установите true, чтобы включить текст, содержащийся в заметках слайдов ([NotesSlide](../../com.aspose.slides/notesslide)) при выполнении поиска текста, замены или выделения. |
| [setIncludeNotes(boolean value)](#setIncludeNotes-boolean-) | Установите true, чтобы включить текст, содержащийся в заметках слайдов ([NotesSlide](../../com.aspose.slides/notesslide)) при выполнении поиска текста, замены или выделения. |
### TextSearchOptions() {#TextSearchOptions--}
```
public TextSearchOptions()
```


Создаёт новые параметры поиска текста по умолчанию.

### getCaseSensitive() {#getCaseSensitive--}
```
public final boolean getCaseSensitive()
```


Установите true, чтобы использовать поиск с учётом регистра, false — иначе. Чтение/запись  boolean .

**Возвращает:**
boolean
### setCaseSensitive(boolean value) {#setCaseSensitive-boolean-}
```
public final void setCaseSensitive(boolean value)
```


Установите true, чтобы использовать поиск с учётом регистра, false — иначе. Чтение/запись  boolean .

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | boolean |  |

### getWholeWordsOnly() {#getWholeWordsOnly--}
```
public final boolean getWholeWordsOnly()
```


Установите true, чтобы сопоставлять только целые слова, false — иначе. Чтение/запись  boolean .

**Возвращает:**
boolean
### setWholeWordsOnly(boolean value) {#setWholeWordsOnly-boolean-}
```
public final void setWholeWordsOnly(boolean value)
```


Установите true, чтобы сопоставлять только целые слова, false — иначе. Чтение/запись  boolean .

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | boolean |  |

### getIncludeNotes() {#getIncludeNotes--}
```
public final boolean getIncludeNotes()
```


Установите true, чтобы включить текст, содержащийся в заметках слайдов ([NotesSlide](../../com.aspose.slides/notesslide)) при выполнении поиска текста, замены или выделения. Значение по умолчанию — false.

**Возвращает:**
boolean
### setIncludeNotes(boolean value) {#setIncludeNotes-boolean-}
```
public final void setIncludeNotes(boolean value)
```


Установите true, чтобы включить текст, содержащийся в заметках слайдов ([NotesSlide](../../com.aspose.slides/notesslide)) при выполнении поиска текста, замены или выделения. Значение по умолчанию — false.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | boolean |  |