---
title: IOptionalBlackTransition
second_title: Справочник API Aspose.Slides для Java
description: Опциональный черный эффект перехода слайда.
type: docs
url: /ru/com.aspose.slides/ioptionalblacktransition/
---
**Все реализованные интерфейсы:**
[com.aspose.slides.ITransitionValueBase](../../com.aspose.slides/itransitionvaluebase)
```
public interface IOptionalBlackTransition extends ITransitionValueBase
```

Опциональный черный эффект перехода слайда.
## Методы

| Метод | Описание |
| --- | --- |
| [getFromBlack()](#getFromBlack--) | Этот атрибут указывает, будет ли переход начинаться с черного экрана (а затем переходить к новому слайду поверх черного). |
| [setFromBlack(boolean value)](#setFromBlack-boolean-) | Этот атрибут указывает, будет ли переход начинаться с черного экрана (а затем переходить к новому слайду поверх черного). |
### getFromBlack() {#getFromBlack--}
```
public abstract boolean getFromBlack()
```


Этот атрибут указывает, будет ли переход начинаться с черного экрана (а затем переходить к новому слайду поверх черного). Чтение/запись boolean.

**Возвращаемое значение:**
boolean
### setFromBlack(boolean value) {#setFromBlack-boolean-}
```
public abstract void setFromBlack(boolean value)
```


Этот атрибут указывает, будет ли переход начинаться с черного экрана (а затем переходить к новому слайду поверх черного). Чтение/запись boolean.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | boolean |  |