---
title: INormalViewRestoredProperties
second_title: Aspose.Slides для Java API Reference
description: Указывает размер региона слайда (ширина, когда дочерний элемент RestoredTop, высота, когда дочерний элемент RestoredLeft) в обычном представлении, когда регион имеет переменный восстановленный размер (не свернут и не развернут).
type: docs
url: /ru/com.aspose.slides/inormalviewrestoredproperties/
---```
public interface INormalViewRestoredProperties
```

Указывает размер региона слайда (ширина, когда дочерний элемент RestoredTop, высота, когда дочерний элемент RestoredLeft). Read/write float.

## Методы

| Метод | Описание |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | Указывает размер региона слайда (ширина, когда дочерний элемент RestoredTop, высота, когда дочерний элемент RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | Указывает размер региона слайда (ширина, когда дочерний элемент RestoredTop, высота, когда дочерний элемент RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | Указывает, должна ли область бокового содержимого компенсировать новый размер при изменении размера окна, содержащего представление в приложении. Чтение/запись boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | Указывает, должна ли область бокового содержимого компенсировать новый размер при изменении размера окна, содержащего представление в приложении. Чтение/запись boolean. |
### getDimensionSize() {#getDimensionSize--}
```
public abstract float getDimensionSize()
```

Указывает размер региона слайда (ширина, когда дочерний элемент RestoredTop, высота, когда дочерний элемент RestoredLeft). Чтение/запись float.

**Возвращаемое значение:**
float
### setDimensionSize(float value) {#setDimensionSize-float-}
```
public abstract void setDimensionSize(float value)
```

Указывает размер региона слайда (ширина, когда дочерний элемент RestoredTop, высота, когда дочерний элемент RestoredLeft). Чтение/запись float.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public abstract boolean getAutoAdjust()
```

Указывает, должна ли область бокового содержимого компенсировать новый размер при изменении размера окна, содержащего представление в приложении. Чтение/запись boolean.

**Возвращаемое значение:**
boolean
### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public abstract void setAutoAdjust(boolean value)
```

Указывает, должна ли область бокового содержимого компенсировать новый размер при изменении размера окна, содержащего представление в приложении. Чтение/запись boolean.

**Параметры:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| value | boolean |  |