---
title: ITiming
second_title: Aspose.Slides for Java API Reference
description: Представляет тайминг анимации.
type: docs
url: /ru/com.aspose.slides/itiming/
---```
public interface ITiming
```

Представляет тайминг анимации.
## Методы

| Метод | Описание |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | Описывает процент длительности ускоряющего поведения эффекта. |
| [setAccelerate(float value)](#setAccelerate-float-) | Описывает процент длительности ускоряющего поведения эффекта. |
| [getDecelerate()](#getDecelerate--) | Описывает процент длительности замедляющего поведения эффекта. |
| [setDecelerate(float value)](#setDecelerate-float-) | Описывает процент длительности замедляющего поведения эффекта. |
| [getAutoReverse()](#getAutoReverse--) | Описывает, следует ли автоматически воспроизводить анимацию в обратном направлении после её воспроизведения вперёд. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | Описывает, следует ли автоматически воспроизводить анимацию в обратном направлении после её воспроизведения вперёд. |
| [getDuration()](#getDuration--) | Описывает длительность эффекта анимации. |
| [setDuration(float value)](#setDuration-float-) | Описывает длительность эффекта анимации. |
| [getRepeatCount()](#getRepeatCount--) | Описывает количество повторений эффекта. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | Описывает количество повторений эффекта. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | Этот атрибут определяет, будет ли эффект повторяться до окончания слайда. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | Этот атрибут определяет, будет ли эффект повторяться до окончания слайда. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | Этот атрибут определяет, будет ли эффект повторяться до следующего клика. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | Этот атрибут определяет, будет ли эффект повторяться до следующего клика. |
| [getRepeatDuration()](#getRepeatDuration--) | Описывает количество повторений эффекта. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | Описывает количество повторений эффекта. |
| [getRestart()](#getRestart--) | Определяет, следует ли перезапустить эффект после завершения. |
| [setRestart(int value)](#setRestart-int-) | Определяет, следует ли перезапустить эффект после завершения. |
| [getSpeed()](#getSpeed--) | Определяет процент ускорения (или замедления) тайминга. |
| [setSpeed(float value)](#setSpeed-float-) | Определяет процент ускорения (или замедления) тайминга. |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | Описывает задержку после триггера. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | Описывает задержку после триггера. |
| [getTriggerType()](#getTriggerType--) | Описывает тип триггера. |
| [setTriggerType(int value)](#setTriggerType-int-) | Описывает тип триггера. |
| [getRewind()](#getRewind--) | Этот атрибут определяет, будет ли эффект откатываться после воспроизведения. |
| [setRewind(boolean value)](#setRewind-boolean-) | Этот атрибут определяет, будет ли эффект откатываться после воспроизведения. |
### getAccelerate() {#getAccelerate--}
```
public abstract float getAccelerate()
```

Описывает процент длительности ускоряющего поведения эффекта. Чтение/запись float.

**Возвращаемое значение:**
float
### setAccelerate(float value) {#setAccelerate-float-}
```
public abstract void setAccelerate(float value)
```

Описывает процент длительности ускоряющего поведения эффекта. Чтение/запись float.

**Параметры:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getDecelerate() {#getDecelerate--}
```
public abstract float getDecelerate()
```

Описывает процент длительности замедляющего поведения эффекта. Чтение/запись float.

**Возвращаемое значение:**
float
### setDecelerate(float value) {#setDecelerate-float-}
```
public abstract void setDecelerate(float value)
```

Описывает процент длительности замедляющего поведения эффекта. Чтение/запись float.

**Параметры:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getAutoReverse() {#getAutoReverse--}
```
public abstract boolean getAutoReverse()
```

Описывает, следует ли автоматически воспроизводить анимацию в обратном направлении после её воспроизведения вперёд. Чтение/запись boolean.

**Возвращаемое значение:**
boolean
### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public abstract void setAutoReverse(boolean value)
```

Описывает, следует ли автоматически воспроизводить анимацию в обратном направлении после её воспроизведения вперёд. Чтение/запись boolean.

**Параметры:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |
### getDuration() {#getDuration--}
```
public abstract float getDuration()
```

Описывает длительность эффекта анимации. Чтение/запись float.

**Возвращаемое значение:**
float
### setDuration(float value) {#setDuration-float-}
```
public abstract void setDuration(float value)
```

Описывает длительность эффекта анимации. Чтение/запись float.

**Параметры:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getRepeatCount() {#getRepeatCount--}
```
public abstract float getRepeatCount()
```

Описывает количество повторений эффекта. Чтение/запись float.

**Возвращаемое значение:**
float
### setRepeatCount(float value) {#setRepeatCount-float-}
```
public abstract void setRepeatCount(float value)
```

Описывает количество повторений эффекта. Чтение/запись float.

**Параметры:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public abstract boolean getRepeatUntilEndSlide()
```

Этот атрибут определяет, будет ли эффект повторяться до окончания слайда. Чтение/запись boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Get the effects sequence for the first slide
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Get the first effect of main sequence.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Change the effect Timing/Repeat to "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Возвращаемое значение:**
boolean
### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public abstract void setRepeatUntilEndSlide(boolean value)
```

Этот атрибут определяет, будет ли эффект повторяться до окончания слайда. Чтение/запись boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Get the effects sequence for the first slide
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Get the first effect of main sequence.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Change the effect Timing/Repeat to "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Параметры:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |
### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public abstract boolean getRepeatUntilNextClick()
```

Этот атрибут определяет, будет ли эффект повторяться до следующего клика. Чтение/запись boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Получить последовательность эффектов для первого слайда
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Получить первый эффект основной последовательности.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Изменить тайминг/повтор эффекта на "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Возвращаемое значение:**
boolean
### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public abstract void setRepeatUntilNextClick(boolean value)
```

Этот атрибут определяет, будет ли эффект повторяться до следующего клика. Чтение/запись boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Получить последовательность эффектов для первого слайда
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Получить первый эффект основной последовательности.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Изменить тайминг/повтор эффекта на "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Параметры:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |
### getRepeatDuration() {#getRepeatDuration--}
```
public abstract float getRepeatDuration()
```

Описывает количество повторений эффекта. Чтение/запись float.

**Возвращаемое значение:**
float
### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public abstract void setRepeatDuration(float value)
```

Описывает количество повторений эффекта. Чтение/запись float.

**Параметры:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getRestart() {#getRestart--}
```
public abstract int getRestart()
```

Определяет, следует ли перезапустить эффект после завершения. Чтение/запись [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Возвращаемое значение:**
int
### setRestart(int value) {#setRestart-int-}
```
public abstract void setRestart(int value)
```

Определяет, следует ли перезапустить эффект после завершения. Чтение/запись [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Параметры:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | int |  |
### getSpeed() {#getSpeed--}
```
public abstract float getSpeed()
```

Определяет процент ускорения (или замедления) тайминга. Чтение/запись float.

**Возвращаемое значение:**
float
### setSpeed(float value) {#setSpeed-float-}
```
public abstract void setSpeed(float value)
```

Определяет процент ускорения (или замедления) тайминга. Чтение/запись float.

**Параметры:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public abstract float getTriggerDelayTime()
```

Описывает задержку после триггера. Чтение/запись float.

**Возвращаемое значение:**
float
### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public abstract void setTriggerDelayTime(float value)
```

Описывает задержку после триггера. Чтение/запись float.

**Параметры:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getTriggerType() {#getTriggerType--}
```
public abstract int getTriggerType()
```

Описывает тип триггера. Чтение/запись [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Возвращаемое значение:**
int
### setTriggerType(int value) {#setTriggerType-int-}
```
public abstract void setTriggerType(int value)
```

Описывает тип триггера. Чтение/запись [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Параметры:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | int |  |
### getRewind() {#getRewind--}
```
public abstract boolean getRewind()
```

Этот атрибут определяет, будет ли эффект откатываться после воспроизведения. Чтение/запись boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Get the effects sequence for the first slide
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Get the first effect of main sequence.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Turn the effect Timing/Rewind on.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Возвращаемое значение:**
boolean
### setRewind(boolean value) {#setRewind-boolean-}
```
public abstract void setRewind(boolean value)
```

Этот атрибут определяет, будет ли эффект откатываться после воспроизведения. Чтение/запись boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Get the effects sequence for the first slide
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Get the first effect of main sequence.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Turn the effect Timing/Rewind on.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Параметры:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |