---
title: IColorOffset
second_title: Aspose.Slides for Java API Reference
description: แสดงการออฟเซ็ตสี.
type: docs
url: /th/com.aspose.slides/icoloroffset/
---```
public interface IColorOffset
```

แสดงการออฟเซ็ตสี.
## เมธอด

| เมธอด | คำอธิบาย |
| --- | --- |
| [getValue0()](#getValue0--) | กำหนดค่าตำแหน่งออฟเซ็ตแรก. |
| [setValue0(float value)](#setValue0-float-) | กำหนดค่าตำแหน่งออฟเซ็ตแรก. |
| [getValue1()](#getValue1--) | กำหนดค่าตำแหน่งออฟเซ็ตที่สอง. |
| [setValue1(float value)](#setValue1-float-) | กำหนดค่าตำแหน่งออฟเซ็ตที่สอง. |
| [getValue2()](#getValue2--) | กำหนดค่าตำแหน่งออฟเซ็ตที่สาม. |
| [setValue2(float value)](#setValue2-float-) | กำหนดค่าตำแหน่งออฟเซ็ตที่สาม. |
### getValue0() {#getValue0--}
```
public abstract float getValue0()
```

กำหนดค่าตำแหน่งออฟเซ็ตแรก. อ่าน/เขียน float.

**คืนค่า:**
float
### setValue0(float value) {#setValue0-float-}
```
public abstract void setValue0(float value)
```

กำหนดค่าตำแหน่งออฟเซ็ตแรก. อ่าน/เขียน float.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | float |  |

### getValue1() {#getValue1--}
```
public abstract float getValue1()
```

กำหนดค่าตำแหน่งออฟเซ็ตที่สอง. อ่าน/เขียน float.

**คืนค่า:**
float
### setValue1(float value) {#setValue1-float-}
```
public abstract void setValue1(float value)
```

กำหนดค่าตำแหน่งออฟเซ็ตที่สอง. อ่าน/เขียน float.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | float |  |

### getValue2() {#getValue2--}
```
public abstract float getValue2()
```

กำหนดค่าตำแหน่งออฟเซ็ตที่สาม. อ่าน/เขียน float.

**คืนค่า:**
float
### setValue2(float value) {#setValue2-float-}
```
public abstract void setValue2(float value)
```

กำหนดค่าตำแหน่งออฟเซ็ตที่สาม. อ่าน/เขียน float.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | float |  |