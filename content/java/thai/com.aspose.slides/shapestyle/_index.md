---
title: ShapeStyle
second_title: อ้างอิง API ของ Aspose.Slides สำหรับ Java
description: อ้างอิงสไตล์ของรูปร่าง.
type: docs
url: /th/com.aspose.slides/shapestyle/
---
**การสืบทอด:**
java.lang.Object, com.aspose.slides.DomObject

**ทั้งหมดของอินเทอร์เฟซที่ทำการ Implement:**
[com.aspose.slides.IShapeStyle](../../com.aspose.slides/ishapestyle)
```
public class ShapeStyle extends DomObject<Shape> implements IShapeStyle
```

เป็นการอ้างอิงสไตล์ของรูปทรง
## วิธีการ

| เมธอด | คำอธิบาย |
| --- | --- |
| [getLineColor()](#getLineColor--) | ส่งคืนสีโครงของรูปทรง. |
| [getLineStyleIndex()](#getLineStyleIndex--) | ส่งคืนหรือกำหนดดัชนีคอลัมน์ของเส้นในเมทริกซ์สไตล์. |
| [setLineStyleIndex(int value)](#setLineStyleIndex-int-) | ส่งคืนหรือกำหนดดัชนีคอลัมน์ของเส้นในเมทริกซ์สไตล์. |
| [getFillColor()](#getFillColor--) | ส่งคืนสีเติมของรูปทรง. |
| [getFillStyleIndex()](#getFillStyleIndex--) | ส่งคืนหรือกำหนดดัชนีคอลัมน์การเติมของรูปทรงในเมทริกซ์สไตล์. |
| [setFillStyleIndex(short value)](#setFillStyleIndex-short-) | ส่งคืนหรือกำหนดดัชนีคอลัมน์การเติมของรูปทรงในเมทริกซ์สไตล์. |
| [getEffectColor()](#getEffectColor--) | ส่งคืนสีเอฟเฟกต์ของรูปทรง. |
| [getEffectStyleIndex()](#getEffectStyleIndex--) | ส่งคืนหรือกำหนดดัชนีคอลัมน์เอฟเฟกต์ของรูปทรงในเมทริกซ์สไตล์. |
| [setEffectStyleIndex(long value)](#setEffectStyleIndex-long-) | ส่งคืนหรือกำหนดดัชนีคอลัมน์เอฟเฟกต์ของรูปทรงในเมทริกซ์สไตล์. |
| [getFontColor()](#getFontColor--) | ส่งคืนสีฟอนต์ของรูปทรง. |
| [getFontCollectionIndex()](#getFontCollectionIndex--) | ส่งคืนหรือกำหนดดัชนีฟอนต์ของรูปทรงในคอลเลกชันฟอนต์. |
| [setFontCollectionIndex(byte value)](#setFontCollectionIndex-byte-) | ส่งคืนหรือกำหนดดัชนีฟอนต์ของรูปทรงในคอลเลกชันฟอนต์. |
### getLineColor() {#getLineColor--}
```
public final IColorFormat getLineColor()
```

ส่งคืนสีโครงของรูปทรง อ่านอย่างเดียว [IColorFormat](../../com.aspose.slides/icolorformat).

**ค่าที่ส่งกลับ:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getLineStyleIndex() {#getLineStyleIndex--}
```
public final int getLineStyleIndex()
```

ส่งคืนหรือกำหนดดัชนีคอลัมน์ของเส้นในเมทริกซ์สไตล์ อ่านและเขียน int.

**ค่าที่ส่งกลับ:**
int
### setLineStyleIndex(int value) {#setLineStyleIndex-int-}
```
public final void setLineStyleIndex(int value)
```

ส่งคืนหรือกำหนดดัชนีคอลัมน์ของเส้นในเมทริกซ์สไตล์ อ่านและเขียน int.

**พารามิเตอร์:**
| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | int |  |
### getFillColor() {#getFillColor--}
```
public final IColorFormat getFillColor()
```

ส่งคืนสีเติมของรูปทรง อ่านอย่างเดียว [IColorFormat](../../com.aspose.slides/icolorformat).

**ค่าที่ส่งกลับ:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getFillStyleIndex() {#getFillStyleIndex--}
```
public final short getFillStyleIndex()
```

ส่งคืนหรือกำหนดดัชนีคอลัมน์การเติมของรูปทรงในเมทริกซ์สไตล์ 0 หมายถึงไม่มีการเติม, ค่าบวก - ดัชนีในสไตล์การเติมของธีม, ค่าลบ - ดัชนีในสไตล์พื้นหลังของธีม อ่านและเขียน short.

**ค่าที่ส่งกลับ:**
short
### setFillStyleIndex(short value) {#setFillStyleIndex-short-}
```
public final void setFillStyleIndex(short value)
```

ส่งคืนหรือกำหนดดัชนีคอลัมน์การเติมของรูปทรงในเมทริกซ์สไตล์ 0 หมายถึงไม่มีการเติม, ค่าบวก - ดัชนีในสไตล์การเติมของธีม, ค่าลบ - ดัชนีในสไตล์พื้นหลังของธีม อ่านและเขียน short.

**พารามิเตอร์:**
| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | short |  |
### getEffectColor() {#getEffectColor--}
```
public final IColorFormat getEffectColor()
```

ส่งคืนสีเอฟเฟกต์ของรูปทรง อ่านอย่างเดียว [IColorFormat](../../com.aspose.slides/icolorformat).

**ค่าที่ส่งกลับ:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getEffectStyleIndex() {#getEffectStyleIndex--}
```
public final long getEffectStyleIndex()
```

ส่งคืนหรือกำหนดดัชนีคอลัมน์เอฟเฟกต์ของรูปทรงในเมทริกซ์สไตล์ อ่านและเขียน long.

**ค่าที่ส่งกลับ:**
long
### setEffectStyleIndex(long value) {#setEffectStyleIndex-long-}
```
public final void setEffectStyleIndex(long value)
```

ส่งคืนหรือกำหนดดัชนีคอลัมน์เอฟเฟกต์ของรูปทรงในเมทริกซ์สไตล์ อ่านและเขียน long.

**พารามิเตอร์:**
| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | long |  |
### getFontColor() {#getFontColor--}
```
public final IColorFormat getFontColor()
```

ส่งคืนสีฟอนต์ของรูปทรง อ่านอย่างเดียว [IColorFormat](../../com.aspose.slides/icolorformat).

**ค่าที่ส่งกลับ:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getFontCollectionIndex() {#getFontCollectionIndex--}
```
public final byte getFontCollectionIndex()
```

ส่งคืนหรือกำหนดดัชนีฟอนต์ของรูปทรงในคอลเลกชันฟอนต์ อ่านและเขียน [FontCollectionIndex](../../com.aspose.slides/fontcollectionindex).

**ค่าที่ส่งกลับ:**
byte
### setFontCollectionIndex(byte value) {#setFontCollectionIndex-byte-}
```
public final void setFontCollectionIndex(byte value)
```

ส่งคืนหรือกำหนดดัชนีฟอนต์ของรูปทรงในคอลเลกชันฟอนต์ อ่านและเขียน [FontCollectionIndex](../../com.aspose.slides/fontcollectionindex).

**พารามิเตอร์:**
| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | byte |  |