---
title: RotationEffect
second_title: อ้างอิง API ของ Aspose.Slides สำหรับ Java
description: แสดงพฤติกรรมการหมุนของเอฟเฟกต์.
type: docs
url: /th/com.aspose.slides/rotationeffect/
---
**การสืบทอด:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**อินเทอร์เฟซที่นำไปใช้ทั้งหมด:**
[com.aspose.slides.IRotationEffect](../../com.aspose.slides/irotationeffect)
```
public class RotationEffect extends Behavior implements IRotationEffect
```

แสดงพฤติกรรมการหมุนของเอฟเฟกต์.
## คอนสตรัคเตอร์

| คอนสตรัคเตอร์ | คำอธิบาย |
| --- | --- |
| [RotationEffect()](#RotationEffect--) |  |
## เมธอด

| เมธอด | คำอธิบาย |
| --- | --- |
| [getFrom()](#getFrom--) | อธิบายค่าตั้งต้นสำหรับแอนิเมชัน. |
| [setFrom(float value)](#setFrom-float-) | อธิบายค่าตั้งต้นสำหรับแอนิเมชัน. |
| [getTo()](#getTo--) | อธิบายค่าปลายสำหรับแอนิเมชัน. |
| [setTo(float value)](#setTo-float-) | อธิบายค่าปลายสำหรับแอนิเมชัน. |
| [getBy()](#getBy--) | อธิบายค่าการเยื้องเชิงสัมพัทธ์สำหรับแอนิเมชัน. |
| [setBy(float value)](#setBy-float-) | อธิบายค่าการเยื้องเชิงสัมพัทธ์สำหรับแอนิเมชัน. |
### RotationEffect() {#RotationEffect--}
```
public RotationEffect()
```


### getFrom() {#getFrom--}
```
public final float getFrom()
```


อธิบายค่าตั้งต้นสำหรับแอนิเมชัน. อ่าน/เขียน float.

**คืนค่า:**
float
### setFrom(float value) {#setFrom-float-}
```
public final void setFrom(float value)
```


อธิบายค่าตั้งต้นสำหรับแอนิเมชัน. อ่าน/เขียน float.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | float |  |

### getTo() {#getTo--}
```
public final float getTo()
```


อธิบายค่าปลายสำหรับแอนิเมชัน. อ่าน/เขียน float.

**คืนค่า:**
float
### setTo(float value) {#setTo-float-}
```
public final void setTo(float value)
```


อธิบายค่าปลายสำหรับแอนิเมชัน. อ่าน/เขียน float.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | float |  |

### getBy() {#getBy--}
```
public final float getBy()
```


อธิบายค่าการเยื้องเชิงสัมพัทธ์สำหรับแอนิเมชัน. อ่าน/เขียน float.

**คืนค่า:**
float
### setBy(float value) {#setBy-float-}
```
public final void setBy(float value)
```


อธิบายค่าการเยื้องเชิงสัมพัทธ์สำหรับแอนิเมชัน. อ่าน/เขียน float.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | float |  |