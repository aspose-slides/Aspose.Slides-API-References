---
title: TextHighlightingOptions
second_title: Aspose.Slides สำหรับการอ้างอิง API ของ Java
description: แสดงตัวเลือกที่สามารถใช้เพื่อเน้นข้อความในกรอบข้อความ.
type: docs
url: /th/com.aspose.slides/texthighlightingoptions/
---
**การสืบทอด:**
java.lang.Object

**All Implemented Interfaces:**
[com.aspose.slides.ITextHighlightingOptions](../../com.aspose.slides/itexthighlightingoptions)
```
public class TextHighlightingOptions implements ITextHighlightingOptions
```

แสดงตัวเลือกที่สามารถใช้เพื่อเน้นข้อความในกรอบข้อความ
## ตัวสร้าง

| ตัวสร้าง | คำอธิบาย |
| --- | --- |
| [TextHighlightingOptions()](#TextHighlightingOptions--) | สร้างตัวเลือกการเน้นข้อความเริ่มต้นใหม่ |

## เมธอด

| เมธอด | คำอธิบาย |
| --- | --- |
| [getCaseSensitive()](#getCaseSensitive--) | ตั้งค่าเป็น true เพื่อใช้การค้นหาแยกแยะตัวพิมพ์ใหญ่-เล็ก, false - มิฉะนั้น |
| [setCaseSensitive(boolean value)](#setCaseSensitive-boolean-) | ตั้งค่าเป็น true เพื่อใช้การค้นหาแยกแยะตัวพิมพ์ใหญ่-เล็ก, false - มิฉะนั้น |
| [getWholeWordsOnly()](#getWholeWordsOnly--) | ตั้งค่าเป็น true เพื่อจับตรงคำทั้งหมด, false - มิฉะนั้น |
| [setWholeWordsOnly(boolean value)](#setWholeWordsOnly-boolean-) | ตั้งค่าเป็น true เพื่อจับตรงคำทั้งหมด, false - มิฉะนั้น |
### TextHighlightingOptions() {#TextHighlightingOptions--}
```
public TextHighlightingOptions()
```

สร้างตัวเลือกการเน้นข้อความเริ่มต้นใหม่

### getCaseSensitive() {#getCaseSensitive--}
```
public final boolean getCaseSensitive()
```

ตั้งค่าเป็น true เพื่อใช้การค้นหาแยกแยะตัวพิมพ์ใหญ่-เล็ก, false - มิฉะนั้น อ่าน/เขียน boolean

**คืนค่า:**
boolean
### setCaseSensitive(boolean value) {#setCaseSensitive-boolean-}
```
public final void setCaseSensitive(boolean value)
```

ตั้งค่าเป็น true เพื่อใช้การค้นหาแยกแยะตัวพิมพ์ใหญ่-เล็ก, false - มิฉะนั้น อ่าน/เขียน boolean

**พารามิเตอร์:**
| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | boolean |  |

### getWholeWordsOnly() {#getWholeWordsOnly--}
```
public final boolean getWholeWordsOnly()
```

ตั้งค่าเป็น true เพื่อจับตรงคำทั้งหมด, false - มิฉะนั้น อ่าน/เขียน boolean

**คืนค่า:**
boolean
### setWholeWordsOnly(boolean value) {#setWholeWordsOnly-boolean-}
```
public final void setWholeWordsOnly(boolean value)
```

ตั้งค่าเป็น true เพื่อจับตรงคำทั้งหมด, false - มิฉะนั้น อ่าน/เขียน boolean

**พารามิเตอร์:**
| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | boolean |  |