---
title: PieSplitType
second_title: Aspose.Slides สำหรับ Java API Reference
description: แสดงประเภทของจุดแบ่งในพายหรือแถบที่สองบนแผนภูมิแบบ pie-of-pie หรือ bar-of-pie
type: docs
url: /th/com.aspose.slides/piesplittype/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class PieSplitType extends System.Enum
```

แสดงประเภทของจุดแบ่งในพายหรือแถบที่สองบนแผนภูมิแบบ pie-of-pie หรือ bar-of-pie

## Fields

| ฟิลด์ | คำอธิบาย |
| --- | --- |
| [Default](#Default) | ระบุว่าจุดข้อมูลจะถูกแยกโดยใช้กลไกเริ่มต้นสำหรับประเภทแผนภูมินี้ |
| [Custom](#Custom) | ระบุว่าจุดข้อมูลจะถูกแยกระหว่างพายและแผนภูมิที่สองตามค่า Custom Split |
| [ByPercentage](#ByPercentage) | ระบุว่าจุดข้อมูลจะถูกแยกระหว่างพายและแผนภูมิที่สองโดยใส่จุดที่มีเปอร์เซ็นต์น้อยกว่า Split Position เปอร์เซ็นต์ในแผนภูมิที่สอง |
| [ByPos](#ByPos) | ระบุว่าจุดข้อมูลจะถูกแยกระหว่างพายและแผนภูมิที่สองโดยใส่ตำแหน่ง Split Position สุดท้ายของจุดข้อมูลในแผนภูมิที่สอง |
| [ByValue](#ByValue) | ระบุว่าจุดข้อมูลจะถูกแยกระหว่างพายและแผนภูมิที่สองโดยใส่จุดข้อมูลที่มีค่าน้อยกว่า Split Position ในแผนภูมิที่สอง |

### Default {#Default}
```
public static final int Default
```

ระบุว่าจุดข้อมูลจะถูกแยกโดยใช้กลไกเริ่มต้นสำหรับประเภทแผนภูมินี้

### Custom {#Custom}
```
public static final int Custom
```

ระบุว่าจุดข้อมูลจะถูกแยกระหว่างพายและแผนภูมิที่สองตามค่า Custom Split

### ByPercentage {#ByPercentage}
```
public static final int ByPercentage
```

ระบุว่าจุดข้อมูลจะถูกแยกระหว่างพายและแผนภูมิที่สองโดยใส่จุดที่มีเปอร์เซ็นต์น้อยกว่า Split Position เปอร์เซ็นต์ในแผนภูมิที่สอง

### ByPos {#ByPos}
```
public static final int ByPos
```

ระบุว่าจุดข้อมูลจะถูกแยกระหว่างพายและแผนภูมิที่สองโดยใส่ตำแหน่ง Split Position สุดท้ายของจุดข้อมูลในแผนภูมิที่สอง

### ByValue {#ByValue}
```
public static final int ByValue
```

ระบุว่จุดข้อมูลจะถูกแยกระหว่างพายและแผนภูมิที่สองโดยใส่จุดข้อมูลที่มีค่าน้อยกว่า Split Position ในแผนภูมิที่สอง