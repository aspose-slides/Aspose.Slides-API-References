---
title: GradientStopEffectiveData
second_title: เอกสารอ้างอิง API ของ Aspose.Slides สำหรับ Java
description: อ็อบเจกต์ที่ไม่สามารถเปลี่ยนแปลงได้ซึ่งแสดงถึงจุดไล่ระดับสี.
type: docs
url: /th/com.aspose.slides/gradientstopeffectivedata/
---
**การสืบทอด:**
java.lang.Object

**อินเทอร์เฟซที่นำไปใช้ทั้งหมด:**
com.aspose.slides.IEffectiveData, [com.aspose.slides.IGradientStopEffectiveData](../../com.aspose.slides/igradientstopeffectivedata)
```
public class GradientStopEffectiveData implements IEffectiveData, IGradientStopEffectiveData
```

อ็อบเจกต์ที่ไม่สามารถเปลี่ยนแปลงได้ซึ่งแสดงถึงจุดไล่ระดับสี.
## วิธีการ

| เมธอด | คำอธิบาย |
| --- | --- |
| [getPosition()](#getPosition--) | ส่งคืนตำแหน่ง (0..1) ของจุดไล่ระดับสี. |
| [getColor()](#getColor--) | ส่งคืนสีของจุดไล่ระดับสี. |
| [equals(Object obj)](#equals-java.lang.Object-) | ตรวจสอบว่า [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) ที่ระบุเท่ากับ [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) ปัจจุบันหรือไม่. |
| [hashCode()](#hashCode--) |  |

### getPosition() {#getPosition--}
```
public final float getPosition()
```

ส่งคืนตำแหน่ง (0..1) ของจุดไล่ระดับสี. อ่านอย่างเดียว float.

**ส่งคืน:**
float

### getColor() {#getColor--}
```
public final Color getColor()
```

ส่งคืนสีของจุดไล่ระดับสี. อ่านอย่างเดียว java.awt.Color.

**ส่งคืน:**
java.awt.Color

### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```

ตรวจสอบว่า [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) ที่ระบุเท่ากับ [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) ปัจจุบันหรือไม่.

**พารามิเตอร์:**
| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| obj | java.lang.Object | [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) ที่จะเปรียบเทียบ. |

**ส่งคืน:**
boolean - true หากวัตถุเท่ากัน; มิฉะนั้น false.

### hashCode() {#hashCode--}
```
public int hashCode()
```

**ส่งคืน:**
int