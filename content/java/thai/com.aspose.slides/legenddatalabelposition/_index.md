---
title: LegendDataLabelPosition
second_title: อ้างอิง API Aspose.Slides สำหรับ Java
description: กำหนดตำแหน่งของป้ายข้อมูล.
type: docs
url: /th/com.aspose.slides/legenddatalabelposition/
---
**การสืบทอด:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class LegendDataLabelPosition extends System.Enum
```

กำหนดตำแหน่งของป้ายข้อมูล.
## ฟิลด์

| ฟิลด์ | คำอธิบาย |
| --- | --- |
| [NotDefined](#NotDefined) | ตำแหน่งไม่ได้กำหนด. |
| [Bottom](#Bottom) | ระบุว่าป้ายข้อมูลจะถูกแสดงด้านล่างของเครื่องหมายข้อมูล. |
| [BestFit](#BestFit) | ระบุว่าป้ายข้อมูลจะถูกแสดงในตำแหน่งที่ดีที่สุด. |
| [Center](#Center) | ระบุว่าป้ายข้อมูลจะถูกแสดงตรงกลางของเครื่องหมายข้อมูล. |
| [InsideBase](#InsideBase) | ระบุว่าป้ายข้อมูลจะถูกแสดงภายในฐานของเครื่องหมายข้อมูล. |
| [InsideEnd](#InsideEnd) | ระบุว่าป้ายข้อมูลจะถูกแสดงภายในส่วนสุดของเครื่องหมายข้อมูล. |
| [Left](#Left) | ระบุว่าป้ายข้อมูลจะถูกแสดงทางซ้ายของเครื่องหมายข้อมูล. |
| [OutsideEnd](#OutsideEnd) | ระบุว่าป้ายข้อมูลจะถูกแสดงอยู่นอกส่วนสุดของเครื่องหมายข้อมูล. |
| [Right](#Right) | ระบุว่าป้ายข้อมูลจะถูกแสดงทางขวาของเครื่องหมายข้อมูล. |
| [Top](#Top) | ระบุว่าป้ายข้อมูลจะถูกแสดงด้านบนของเครื่องหมายข้อมูล. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```

ตำแหน่งไม่ได้กำหนด.

### Bottom {#Bottom}
```
public static final int Bottom
```

ระบุว่าป้ายข้อมูลจะถูกแสดงด้านล่างของเครื่องหมายข้อมูล.

### BestFit {#BestFit}
```
public static final int BestFit
```

ระบุว่าป้ายข้อมูลจะถูกแสดงในตำแหน่งที่ดีที่สุด.

### Center {#Center}
```
public static final int Center
```

ระบุว่าป้ายข้อมูลจะถูกแสดงตรงกลางของเครื่องหมายข้อมูล.

### InsideBase {#InsideBase}
```
public static final int InsideBase
```

ระบุว่าป้ายข้อมูลจะถูกแสดงภายในฐานของเครื่องหมายข้อมูล.

### InsideEnd {#InsideEnd}
```
public static final int InsideEnd
```

ระบุว่าป้ายข้อมูลจะถูกแสดงภายในส่วนสุดของเครื่องหมายข้อมูล.

### Left {#Left}
```
public static final int Left
```

ระบุว่าป้ายข้อมูลจะถูกแสดงทางซ้ายของเครื่องหมายข้อมูล.

### OutsideEnd {#OutsideEnd}
```
public static final int OutsideEnd
```

ระบุว่าป้ายข้อมูลจะถูกแสดงอยู่นอกส่วนสุดของเครื่องหมายข้อมูล.

### Right {#Right}
```
public static final int Right
```

ระบุว่าป้ายข้อมูลจะถูกแสดงทางขวาของเครื่องหมายข้อมูล.

### Top {#Top}
```
public static final int Top
```

ระบุว่าป้ายข้อมูลจะถูกแสดงด้านบนของเครื่องหมายข้อมูล.