---
title: ColorOffset
second_title: อ้างอิง API ของ Aspose.Slides สำหรับ Java
description: แสดงออฟเซ็ตสี.
type: docs
url: /th/com.aspose.slides/coloroffset/
---
**Inheritance:**
การสืบทอด:

**All Implemented Interfaces:**
[com.aspose.slides.IColorOffset](../../com.aspose.slides/icoloroffset)
```
public class ColorOffset implements IColorOffset
```

Represent color offset.
แสดงออฟเซ็ตสี.
## Constructors
## ตัวสร้าง

| Constructor | Description |
| --- | --- |
| [ColorOffset()](#ColorOffset--) |  |

## Methods
## เมธอด

| Method | Description |
| --- | --- |
| [getValue0()](#getValue0--) | กำหนดค่าตำแหน่งแรกของออฟเซ็ต. |
| [setValue0(float value)](#setValue0-float-) | กำหนดค่าตำแหน่งแรกของออฟเซ็ต. |
| [getValue1()](#getValue1--) | กำหนดค่าตำแหน่งที่สองของออฟเซ็ต. |
| [setValue1(float value)](#setValue1-float-) | กำหนดค่าตำแหน่งที่สองของออฟเซ็ต. |
| [getValue2()](#getValue2--) | กำหนดค่าตำแหน่งที่สามของออฟเซ็ต. |
| [setValue2(float value)](#setValue2-float-) | กำหนดค่าตำแหน่งที่สามของออฟเซ็ต. |
### ColorOffset() {#ColorOffset--}
```
public ColorOffset()
```


### getValue0() {#getValue0--}
```
public final float getValue0()
```


กำหนดค่าตำแหน่งแรกของออฟเซ็ต. อ่าน/เขียน float.

**Returns:**
คืนค่า:
float
### setValue0(float value) {#setValue0-float-}
```
public final void setValue0(float value)
```


กำหนดค่าตำแหน่งแรกของออฟเซ็ต. อ่าน/เขียน float.

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getValue1() {#getValue1--}
```
public final float getValue1()
```


กำหนดค่าตำแหน่งที่สองของออฟเซ็ต. อ่าน/เขียน float.

**Returns:**
คืนค่า:
float
### setValue1(float value) {#setValue1-float-}
```
public final void setValue1(float value)
```


กำหนดค่าตำแหน่งที่สองของออฟเซ็ต. อ่าน/เขียน float.

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getValue2() {#getValue2--}
```
public final float getValue2()
```


กำหนดค่าตำแหน่งที่สามของออฟเซ็ต. อ่าน/เขียน float.

**Returns:**
คืนค่า:
float
### setValue2(float value) {#setValue2-float-}
```
public final void setValue2(float value)
```


กำหนดค่าตำแหน่งที่สามของออฟเซ็ต. อ่าน/เขียน float.

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |