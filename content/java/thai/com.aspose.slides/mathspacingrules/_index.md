---
title: MathSpacingRules
second_title: Aspose.Slides สำหรับการอ้างอิง API ของ Java
description: ประเภทของช่องว่างแนวนอนระหว่างคอลัมน์ของเมทริกซ์
type: docs
url: /th/com.aspose.slides/mathspacingrules/
---
**การสืบทอด:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class MathSpacingRules extends System.Enum
```

ประเภทของช่องว่าง (ช่องแนวนอน) ระหว่างคอลัมน์ของเมทริกซ์
## ฟิลด์

| ฟิลด์ | คำอธิบาย |
| --- | --- |
| [SingleSpacingGap](#SingleSpacingGap) | ช่องว่างเดี่ยว (1 em สำหรับคอลัมน์หรือ 1 บรรทัดสำหรับแถว) |
| [OneAndHalfSpacingGap](#OneAndHalfSpacingGap) | หนึ่งและครึ่ง (1.5 em สำหรับคอลัมน์หรือ 1.5 บรรทัดสำหรับแถว) |
| [DoubleSpacingGap](#DoubleSpacingGap) | ช่องว่างสองเท่า (2 em สำหรับคอลัมน์หรือ 2 บรรทัดสำหรับแถว) |
| [Exactly](#Exactly) | ค่าที่แน่นอน สำหรับคอลัมน์ จะอ้างอิงค่าของ ColumnGap หน่วยเป็น twips สำหรับแถว จะอ้างอิงค่ของ RowGap หน่วยเป็น twips |
| [Multiple](#Multiple) | หลายค่า สำหรับคอลัมน์ จะอ้างอิงค่าของ ColumnGap หน่วยเป็นการเพิ่ม 0.5 em สำหรับแถว จะอ้างอิงค่าของ RowGap หน่วยเป็นบรรทัด |

### SingleSpacingGap {#SingleSpacingGap}
```
public static final int SingleSpacingGap
```

ช่องว่างเดี่ยว (1 em สำหรับคอลัมน์หรือ 1 บรรทัดสำหรับแถว)

### OneAndHalfSpacingGap {#OneAndHalfSpacingGap}
```
public static final int OneAndHalfSpacingGap
```

หนึ่งและครึ่ง (1.5 em สำหรับคอลัมน์หรือ 1.5 บรรทัดสำหรับแถว)

### DoubleSpacingGap {#DoubleSpacingGap}
```
public static final int DoubleSpacingGap
```

ช่องว่างสองเท่า (2 em สำหรับคอลัมน์หรือ 2 บรรทัดสำหรับแถว)

### Exactly {#Exactly}
```
public static final int Exactly
```

ค่าที่แน่นอน สำหรับคอลัมน์ จะอ้างอิงค่าของ ColumnGap หน่วยเป็น twips สำหรับแถว จะอ้างอิงค่าของ RowGap หน่วยเป็น twips

### Multiple {#Multiple}
```
public static final int Multiple
```

หลายค่า สำหรับคอลัมน์ จะอ้างอิงค่าของ ColumnGap หน่วยเป็นการเพิ่ม 0.5 em สำหรับแถว จะอ้างอิงค่าของ RowGap หน่วยเป็นบรรทัด