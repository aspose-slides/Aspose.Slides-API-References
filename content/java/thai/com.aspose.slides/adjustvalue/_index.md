---
title: AdjustValue
second_title: อ้างอิง API ของ Aspose.Slides สำหรับ Java
description: เป็นค่าการปรับแต่งของรูปทรงเรขาคณิต
type: docs
url: /th/com.aspose.slides/adjustvalue/
---
**การสืบทอด:**
java.lang.Object

**ส่วนต่อประสานที่ทำการใช้งานทั้งหมด:**
[com.aspose.slides.IAdjustValue](../../com.aspose.slides/iadjustvalue)
```
public class AdjustValue implements IAdjustValue
```

เป็นค่าการปรับแต่งของรูปทรงเรขาคณิต ค่าดังกล่าวมีผลต่อรูปทรงของ shape.
## เมธอด

| เมธอด | คำอธิบาย |
| --- | --- |
| [getRawValue()](#getRawValue--) | รับหรือกำหนดค่าการปรับแต่ง "ตามที่เป็น". |
| [setRawValue(long value)](#setRawValue-long-) | รับหรือกำหนดค่าการปรับแต่ง "ตามที่เป็น". |
| [getAngleValue()](#getAngleValue--) | รับหรือกำหนดค่า โดยตีความเป็นมุมในหน่วยองศา. |
| [setAngleValue(float value)](#setAngleValue-float-) | รับหรือกำหนดค่า โดยตีความเป็นมุมในหน่วยองศา. |
| [getName()](#getName--) | รับชื่อของค่าการปรับแต่งนี้. |
| [getType()](#getType--) | รับประเภทของการปรับแต่ง shape. |
### getRawValue() {#getRawValue--}
```
public final long getRawValue()
```

รับหรือกำหนดค่าการปรับแต่ง "ตามที่เป็น". อ่าน/เขียน long.

**ผลลัพธ์:**
long
### setRawValue(long value) {#setRawValue-long-}
```
public final void setRawValue(long value)
```

รับหรือกำหนดค่าการปรับแต่ง "ตามที่เป็น". อ่าน/เขียน long.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | long |  |
### getAngleValue() {#getAngleValue--}
```
public final float getAngleValue()
```

รับหรือกำหนดค่าโดยตีความเป็นมุมในหน่วยองศา. อ่าน/เขียน float.

**ผลลัพธ์:**
float
### setAngleValue(float value) {#setAngleValue-float-}
```
public final void setAngleValue(float value)
```

รับหรือกำหนดค่าโดยตีความเป็นมุมในหน่วยองศา. อ่าน/เขียน float.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | float |  |
### getName() {#getName--}
```
public final String getName()
```

รับชื่อของค่าการปรับแต่งนี้. อ่านอย่างเดียว String.

**ผลลัพธ์:**
java.lang.String
### getType() {#getType--}
```
public final int getType()
```

รับประเภทของการปรับแต่ง shape. อ่านอย่างเดียว [ShapeAdjustmentType](../../com.aspose.slides/shapeadjustmenttype).

**ผลลัพธ์:**
int