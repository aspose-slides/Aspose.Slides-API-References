---
title: FormatScheme
second_title: Aspose.Slides สำหรับ Java เอกสารอ้างอิง API
description: จัดเก็บรูปแบบที่กำหนดโดยธีมสำหรับรูปร่าง.
type: docs
url: /th/com.aspose.slides/formatscheme/
---
**การสืบทอด:**
java.lang.Object

**อินเทอร์เฟซที่นำไปใช้ทั้งหมด:**
[com.aspose.slides.IFormatScheme](../../com.aspose.slides/iformatscheme), com.aspose.slides.IDOMObject
```
public class FormatScheme implements IFormatScheme, IDOMObject
```

จัดเก็บรูปแบบที่กำหนดโดยธีมสำหรับรูปร่าง.
## เมธอด

| เมธอด | คำอธิบาย |
| --- | --- |
| [getFillStyles()](#getFillStyles--) | ส่งคืนคอลเลกชันของสไตล์การเติมที่กำหนดโดยธีม. |
| [getLineStyles()](#getLineStyles--) | ส่งคืนคอลเลกชันของสไตล์เส้นที่กำหนดโดยธีม. |
| [getEffectStyles()](#getEffectStyles--) | ส่งคืนคอลเลกชันของสไตล์เอฟเฟกต์ที่กำหนดโดยธีม. |
| [getBackgroundFillStyles()](#getBackgroundFillStyles--) | ส่งคืนคอลเลกชันของสไตล์การเติมพื้นหลังที่กำหนดโดยธีม. |
| [getSlide()](#getSlide--) | ส่งคืนสไลด์แม่. |
| [getPresentation()](#getPresentation--) | ส่งคืนการนำเสนอแม่. |
| [getParent_Immediate()](#getParent-Immediate--) |  |
### getFillStyles() {#getFillStyles--}}
```
public final IFillFormatCollection getFillStyles()
```

ส่งคืนคอลเลกชันของสไตล์การเติมที่กำหนดโดยธีม. อ่านอย่างเดียว [IFillFormatCollection](../../com.aspose.slides/ifillformatcollection).

**คืนค่า:**
[IFillFormatCollection](../../com.aspose.slides/ifillformatcollection)
### getLineStyles() {#getLineStyles--}}
```
public final ILineFormatCollection getLineStyles()
```

ส่งคืนคอลเลกชันของสไตล์เส้นที่กำหนดโดยธีม. อ่านอย่างเดียว [ILineFormatCollection](../../com.aspose.slides/ilineformatcollection).

**คืนค่า:**
[ILineFormatCollection](../../com.aspose.slides/ilineformatcollection)
### getEffectStyles() {#getEffectStyles--}}
```
public final IEffectStyleCollection getEffectStyles()
```

ส่งคืนคอลเลกชันของสไตล์เอฟเฟกต์ที่กำหนดโดยธีม. อ่านอย่างเดียว [IEffectStyleCollection](../../com.aspose.slides/ieffectstylecollection).

**คืนค่า:**
[IEffectStyleCollection](../../com.aspose.slides/ieffectstylecollection)
### getBackgroundFillStyles() {#getBackgroundFillStyles--}}
```
public final IFillFormatCollection getBackgroundFillStyles()
```

ส่งคืนคอลเลกชันของสไตล์การเติมพื้นหลังที่กำหนดโดยธีม. อ่านอย่างเดียว [IFillFormatCollection](../../com.aspose.slides/ifillformatcollection).

**คืนค่า:**
[IFillFormatCollection](../../com.aspose.slides/ifillformatcollection)
### getSlide() {#getSlide--}}
```
public final IBaseSlide getSlide()
```

ส่งคืนสไลด์แม่. อ่านอย่างเดียว [IBaseSlide](../../com.aspose.slides/ibaseslide).

**คืนค่า:**
[IBaseSlide](../../com.aspose.slides/ibaseslide)
### getPresentation() {#getPresentation--}}
```
public final IPresentation getPresentation()
```

ส่งคืนการนำเสนอแม่. อ่านอย่างเดียว [IPresentation](../../com.aspose.slides/ipresentation).

**คืนค่า:**
[IPresentation](../../com.aspose.slides/ipresentation)
### getParent_Immediate() {#getParent-Immediate--}}
```
public final IDOMObject getParent_Immediate()
```

ส่งคืนอ็อบเจกต์ Parent_Immediate. อ่านอย่างเดียว IDOMObject.

**คืนค่า:**
com.aspose.slides.IDOMObject