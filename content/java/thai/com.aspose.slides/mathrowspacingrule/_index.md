---
title: MathRowSpacingRule
second_title: เอกสารอ้างอิง API ของ Aspose.Slides สำหรับ Java
description: ประเภทของช่องว่างในแนวตั้งระหว่างคอลัมน์ในเมทริกซ์หรืออาร์เรย์
type: docs
url: /th/com.aspose.slides/mathrowspacingrule/
---
**การสืบทอด:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class MathRowSpacingRule extends System.Enum
```

ประเภทของช่องว่างในแนวตั้งระหว่างคอลัมน์ในเมทริกซ์หรืออาร์เรย์
## ฟิลด์

| ฟิลด์ | คำอธิบาย |
| --- | --- |
| [SingleLineGap](#SingleLineGap) | ช่องว่างบรรทัดเดียว |
| [OneAndAHalfLineGap](#OneAndAHalfLineGap) | ช่องว่างบรรทัดหนึ่งและครึ่ง |
| [TwoLineGap](#TwoLineGap) | ช่องว่างสองบรรทัด |
| [Exactly](#Exactly) | พอดี (พึ่งพาค่าของคุณสมบัติ RowSpacing วัดเป็นจุด) |
| [Multiple](#Multiple) | หลายค่า (พึ่งพาค่าของคุณสมบัติ RowSpacing วัดเป็นบรรทัด) |
### SingleLineGap {#SingleLineGap}
```
public static final int SingleLineGap
```

ช่องว่างบรรทัดเดียว

### OneAndAHalfLineGap {#OneAndAHalfLineGap}
```
public static final int OneAndAHalfLineGap
```

ช่องว่างบรรทัดหนึ่งและครึ่ง

### TwoLineGap {#TwoLineGap}
```
public static final int TwoLineGap
```

ช่องว่างสองบรรทัด

### Exactly {#Exactly}
```
public static final int Exactly
```

พอดี (พึ่งพาค่าของคุณสมบัติ RowSpacing วัดเป็นจุด)

### Multiple {#Multiple}
```
public static final int Multiple
```

หลายค่า (พึ่งพาค่าของคุณสมบัติ RowSpacing วัดเป็นบรรทัด)