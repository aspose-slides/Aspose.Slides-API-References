---
title: IFilterEffect
second_title: การอ้างอิง API ของ Aspose.Slides สำหรับ Java
description: แสดงเอฟเฟกต์ของฟิลเตอร์ที่มีพฤติกรรม
type: docs
url: /th/com.aspose.slides/ifiltereffect/
---
**อินเทอร์เฟซที่นำไปใช้งานทั้งหมด:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IFilterEffect extends IBehavior
```

แสดงเอฟเฟ็กต์ของฟิลเตอร์ที่มีพฤติกรรม。

## เมธอด

| เมธอด | คำอธิบาย |
| --- | --- |
| [getReveal()](#getReveal--) | แสดงว่าเอฟเฟกต์ที่มีพฤติกรรมต้องเปิดเผย (เข้า/ออก) อ่าน/เขียน [FilterEffectRevealType](../../com.aspose.slides/filtereffectrevealtype). |
| [setReveal(int value)](#setReveal-int-) | แสดงว่าเอฟเฟกต์ที่มีพฤติกรรมต้องเปิดเผย (เข้า/ออก) อ่าน/เขียน [FilterEffectRevealType](../../com.aspose.slides/filtereffectrevealtype). |
| [getType()](#getType--) | แสดงประเภทของเอฟเฟ็กต์ฟิลเตอร์. |
| [setType(int value)](#setType-int-) | แสดงประเภทของเอฟเฟ็กต์ฟิลเตอร์. |
| [getSubtype()](#getSubtype--) | แสดงชนิดย่อยของเอฟเฟ็กต์ฟิลเตอร์. |
| [setSubtype(int value)](#setSubtype-int-) | แสดงชนิดย่อยของเอฟเฟ็กต์ฟิลเตอร์. |

### getReveal() {#getReveal--}
```
public abstract int getReveal()
```

แสดงว่าเอฟเฟกต์ที่มีพฤติกรรมต้องเปิดเผย (เข้า/ออก) อ่าน/เขียน [FilterEffectRevealType](../../com.aspose.slides/filtereffectrevealtype).

**คืนค่า:**
int

### setReveal(int value) {#setReveal-int-}
```
public abstract void setReveal(int value)
```

แสดงว่าเอฟเฟกต์ที่มีพฤติกรรมต้องเปิดเผย (เข้า/ออก) อ่าน/เขียน [FilterEffectRevealType](../../com.aspose.slides/filtereffectrevealtype).

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | int |  |

### getType() {#getType--}
```
public abstract int getType()
```

แสดงประเภทของเอฟเฟ็กต์ฟิลเตอร์. อ่าน/เขียน [FilterEffectType](../../com.aspose.slides/filtereffecttype).

**คืนค่า:**
int

### setType(int value) {#setType-int-}
```
public abstract void setType(int value)
```

แสดงประเภทของเอฟ็กต์ฟิลเตอร์. อ่าน/เขียน [FilterEffectType](../../com.aspose.slides/filtereffecttype).

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | int |  |

### getSubtype() {#getSubtype--}
```
public abstract int getSubtype()
```

แสดงชนิดย่อยของเอฟเฟ็กต์ฟิลเตอร์. อ่าน/เขียน [FilterEffectSubtype](../../com.aspose.slides/filtereffectsubtype).

**คืนค่า:**
int

### setSubtype(int value) {#setSubtype-int-}
```
public abstract void setSubtype(int value)
```

แสดงชนิดย่อยของเอฟเฟ็กต์ฟิลเตอร์. อ่าน/เขียน [FilterEffectSubtype](../../com.aspose.slides/filtereffectsubtype).

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | int |  |