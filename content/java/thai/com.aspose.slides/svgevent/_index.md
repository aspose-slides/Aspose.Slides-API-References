---
title: SvgEvent
second_title: Aspose.Slides สำหรับอ้างอิง API ของ Java
description: แสดงตัวเลือกสำหรับรูปทรง SVG.
type: docs
url: /th/com.aspose.slides/svgevent/
---
**การสืบทอด:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class SvgEvent extends System.Enum
```

แสดงตัวเลือกสำหรับรูปทรง SVG.
## ฟิลด์

| ฟิลด์ | คำอธิบาย |
| --- | --- |
| [OnFocusIn](#OnFocusIn) | เกิดขึ้นเมื่อองค์ประกอบได้รับโฟกัส เช่นเมื่อข้อความถูกเลือก. |
| [OnFocusOut](#OnFocusOut) | เกิดขึ้นเมื่อองค์ประกอบสูญเสียโฟกัส เช่นเมื่อข้อความถูกยกเลิกการเลือก. |
| [OnActivate](#OnActivate) | เกิดขึ้นเมื่อองค์ประกอบถูกเปิดใช้งาน เช่นผ่านการคลิกเมาส์หรือการกดคีย์. |
| [OnClick](#OnClick) | เกิดขึ้นเมื่อปุ่มของอุปกรณ์ชี้ถูกคลิกบนองค์ประกอบ. |
| [OnMouseDown](#OnMouseDown) | เกิดขึ้นเมื่อปุ่มของอุปกรณ์ชี้ถูกกดบนองค์ประกอบ. |
| [OnMouseUp](#OnMouseUp) | เกิดขึ้นเมื่อปุ่มของอุปกรณ์ชี้ถูกปล่อยบนองค์ประกอบ. |
| [OnMouseOver](#OnMouseOver) | เกิดขึ้นเมื่ออุปกรณ์ชี้ถูกย้ายไปบนองค์ประกอบ. |
| [OnMouseMove](#OnMouseMove) | เกิดขึ้นเมื่ออุปกรณ์ชี้ถูกย้ายขณะอยู่บนองค์ประกอบ. |
| [OnMouseOut](#OnMouseOut) | เกิดขึ้นเมื่ออุปกรณ์ชี้ถูกย้ายออกจากองค์ประกอบ. |
| [OnLoad](#OnLoad) | เกิดขึ้นเมื่อเอเจนต์ผู้ใช้ได้ทำการพาร์สองค์ประกอบและบุตรหลานของมันรวมถึงทรัพยากรที่อ้างอิงทั้งหมดที่จำเป็นต่อการแสดงผลอย่างสมบูรณ์. |
| [OnUnload](#OnUnload) | เกิดขึ้นเมื่อการดำเนินการ DOM ลบเอกสารออกจากหน้าต่างหรือเฟรม. |
| [OnAbort](#OnAbort) | เกิดขึ้นเมื่อการโหลดหน้าเว็บหยุดก่อนที่องค์ประกอบจะโหลดเสร็จสมบูรณ์. |
| [OnError](#OnError) | เกิดขึ้นเมื่อองค์ประกอบไม่โหลดอย่างถูกต้อง หรือเมื่อเกิดข้อผิดพลาดระหว่างการทำงานของสคริปต์. |
| [OnResize](#OnResize) | เกิดขึ้นเมื่อมุมมองเอกสารกำลังปรับขนาด. |
| [OnScroll](#OnScroll) | เกิดขึ้นเมื่อมุมมองเอกสารถูกเคลื่อนย้ายตามแกน X หรือ Y หรือทั้งสองแกน. |
| [OnZoom](#OnZoom) | เกิดขึ้นเมื่อระดับการซูมของมุมมองเอกสารกำลังเปลี่ยนแปลง. |
| [OnBegin](#OnBegin) | เกิดขึ้นเมื่อองค์ประกอบแอนิเมชันเริ่มต้น. |
| [OnEnd](#OnEnd) | เกิดขึ้นเมื่อองค์ประกอบแอนิเมชันจบลง. |
| [OnRepeat](#OnRepeat) | เกิดขึ้นเมื่อองค์ประกอบแอนิเมชันทำซ้ำ. |
### OnFocusIn {#OnFocusIn}
```
public static final int OnFocusIn
```

เกิดขึ้นเมื่อองค์ประกอบได้รับโฟกัส เช่นเมื่อข้อความถูกเลือก.

### OnFocusOut {#OnFocusOut}
```
public static final int OnFocusOut
```

เกิดขึ้นเมื่อองค์ประกอบสูญเสียโฟกัส เช่นเมื่อข้อความถูกยกเลิกการเลือก.

### OnActivate {#OnActivate}
```
public static final int OnActivate
```

เกิดขึ้นเมื่อองค์ประกอบถูกเปิดใช้งาน เช่นผ่านการคลิกเมาส์หรือการกดคีย์.

### OnClick {#OnClick}
```
public static final int OnClick
```

เกิดขึ้นเมื่อปุ่มของอุปกรณ์ชี้ถูกคลิกบนองค์ประกอบ.

### OnMouseDown {#OnMouseDown}
```
public static final int OnMouseDown
```

เกิดขึ้นเมื่อปุ่มของอุปกรณ์ชี้ถูกกดบนองค์ประกอบ.

### OnMouseUp {#OnMouseUp}
```
public static final int OnMouseUp
```

เกิดขึ้นเมื่อปุ่มของอุปกรณ์ชี้ถูกปล่อยบนองค์ประกอบ.

### OnMouseOver {#OnMouseOver}
```
public static final int OnMouseOver
```

เกิดขึ้นเมื่ออุปกรณ์ชี้ถูกย้ายไปบนองค์ประกอบ.

### OnMouseMove {#OnMouseMove}
```
public static final int OnMouseMove
```

เกิดขึ้นเมื่ออุปกรณ์ชี้ถูกย้ายขณะอยู่บนองค์ประกอบ.

### OnMouseOut {#OnMouseOut}
```
public static final int OnMouseOut
```

เกิดขึ้นเมื่ออุปกรณ์ชี้ถูกย้ายออกจากองค์ประกอบ.

### OnLoad {#OnLoad}
```
public static final int OnLoad
```

เกิดขึ้นเมื่อเอเจนต์ผู้ใช้ได้ทำการพาร์สองค์ประกอบและบุตรหลานของมันรวมถึงทรัพยากรที่อ้างอิงทั้งหมดที่จำเป็นต่อการแสดงผลอย่างสมบูรณ์.

### OnUnload {#OnUnload}
```
public static final int OnUnload
```

เกิดขึ้นเมื่อการดำเนินการ DOM ลบเอกสารออกจากหน้าต่างหรือเฟรม. Only applicable to outermost svg elements.

### OnAbort {#OnAbort}
```
public static final int OnAbort
```

เกิดขึ้นเมื่อการโหลดหน้าเว็บหยุดก่อนที่องค์ประกอบจะโหลดเสร็จสมบูรณ์.

### OnError {#OnError}
```
public static final int OnError
```

เกิดขึ้นเมื่อองค์ประกอบไม่โหลดอย่างถูกต้อง หรือเมื่อเกิดข้อผิดพลาดระหว่างการทำงานของสคริปต์.

### OnResize {#OnResize}
```
public static final int OnResize
```

เกิดขึ้นเมื่อมุมมองเอกสารกำลังปรับขนาด. Only applicable to outermost svg elements.

### OnScroll {#OnScroll}
```
public static final int OnScroll
```

เกิดขึ้นเมื่อมุมมองเอกสารถูกเคลื่อนย้ายตามแกน X หรือ Y หรือทั้งสองแกน. Only applicable to outermost svg elements.

### OnZoom {#OnZoom}
```
public static final int OnZoom
```

เกิดขึ้นเมื่อระดับการซูมของมุมมองเอกสารกำลังเปลี่ยนแปลง. Only applicable to outermost svg elements.

### OnBegin {#OnBegin}
```
public static final int OnBegin
```

เกิดขึ้นเมื่อองค์ประกอบแอนิเมชันเริ่มต้น.

### OnEnd {#OnEnd}
```
public static final int OnEnd
```

เกิดขึ้นเมื่อองค์ประกอบแอนิเมชันจบลง.

### OnRepeat {#OnRepeat}
```
public static final int OnRepeat
```

เกิดขึ้นเมื่อองค์ประกอบแอนิเมชันทำซ้ำ.