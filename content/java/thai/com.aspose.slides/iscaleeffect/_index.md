---
title: IScaleEffect
second_title: อ้างอิง API Aspose.Slides for Java
description: แสดงผลการปรับขนาดการเคลื่อนไหว.
type: docs
url: /th/com.aspose.slides/iscaleeffect/
---
**ส่วนติดต่อที่ใช้งานทั้งหมด:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IScaleEffect extends IBehavior
```

แสดงผลการปรับขนาดการเคลื่อนไหว.
## เมธอด

| เมธอด | คำอธิบาย |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | กำหนดว่าคอนเทนต์ควรจะซูมหรือไม่. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | กำหนดว่าคอนเทนต์ควรจะซูมหรือไม่. |
| [getFrom()](#getFrom--) | ระบุพิกัด x/y ที่เริ่มการเคลื่อนไหวจาก (เป็นเปอร์เซ็นต์). |
| [setFrom(Point2D.Float value)](#setFrom-java.awt.geom.Point2D.Float-) | ระบุพิกัด x/y ที่เริ่มการเคลื่อนไหวจาก (เป็นเปอร์เซ็นต์). |
| [getTo()](#getTo--) | ระบุตำแหน่งเป้าหมายสำหรับเอฟเฟกต์การปรับขนาดการเคลื่อนไหว (เป็นเปอร์เซ็นต์). |
| [setTo(Point2D.Float value)](#setTo-java.awt.geom.Point2D.Float-) | ระบุตำแหน่งเป้าหมายสำหรับเอฟเฟกต์การปรับขนาดการเคลื่อนไหว (เป็นเปอร์เซ็นต์). |
| [getBy()](#getBy--) | อธิบายค่าการออฟเซ็ตสัมพัทธ์สำหรับการเคลื่อนไหว (เป็นเปอร์เซ็นต์). |
| [setBy(Point2D.Float value)](#setBy-java.awt.geom.Point2D.Float-) | อธิบายค่าการออฟเซ็ตสัมพัทธ์สำหรับการเคลื่อนไหว (เป็นเปอร์เซ็นต์). |
### getZoomContent() {#getZoomContent--}
```
public abstract byte getZoomContent()
```

กำหนดว่าคอนเทนต์ควรจะซูมหรือไม่. อ่าน/เขียน [NullableBool](../../com.aspose.slides/nullablebool).

**คืนค่า:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public abstract void setZoomContent(byte value)
```

กำหนดว่าคอนเทนต์ควรจะซูมหรือไม่. อ่าน/เขียน [NullableBool](../../com.aspose.slides/nullablebool).

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public abstract Point2D.Float getFrom()
```

ระบุพิกัด x/y ที่เริ่มการเคลื่อนไหวจาก (เป็นเปอร์เซ็นต์). อ่าน/เขียน java.awt.geom.Point2D.Float.

**คืนค่า:**
java.awt.geom.Point2D.Float
### setFrom(Point2D.Float value) {#setFrom-java.awt.geom.Point2D.Float-}
```
public abstract void setFrom(Point2D.Float value)
```

ระบุพิกัด x/y ที่เริ่มการเคลื่อนไหวจาก (เป็นเปอร์เซ็นต์). อ่าน/เขียน java.awt.geom.Point2D.Float.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getTo() {#getTo--}
```
public abstract Point2D.Float getTo()
```

ระบุตำแหน่งเป้าหมายสำหรับเอฟเฟกต์การปรับขนาดการเคลื่อนไหว (เป็นเปอร์เซ็นต์). อ่าน/เขียน java.awt.geom.Point2D.Float.

**คืนค่า:**
java.awt.geom.Point2D.Float
### setTo(Point2D.Float value) {#setTo-java.awt.geom.Point2D.Float-}
```
public abstract void setTo(Point2D.Float value)
```

ระบุตำแหน่งเป้าหมายสำหรับเอฟเฟกต์การปรับขนาดการเคลื่อนไหว (เป็นเปอร์เซ็นต์). อ่าน/เขียน java.awt.geom.Point2D.Float.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getBy() {#getBy--}
```
public abstract Point2D.Float getBy()
```

อธิบายค่าการออฟเซ็ตสัมพัทธ์สำหรับการเคลื่อนไหว (เป็นเปอร์เซ็นต์). อ่าน/เขียน java.awt.geom.Point2D.Float.

**คืนค่า:**
java.awt.geom.Point2D.Float
### setBy(Point2D.Float value) {#setBy-java.awt.geom.Point2D.Float-}
```
public abstract void setBy(Point2D.Float value)
```

อธิบายค่าการออฟเซ็ตสัมพัทธ์สำหรับการเคลื่อนไหว (เป็นเปอร์เซ็นต์). อ่าน/เขียน java.awt.geom.Point2D.Float.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |