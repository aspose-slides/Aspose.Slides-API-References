---
title: IDataTable
second_title: อ้างอิง API ของ Aspose.Slides สำหรับ Java
description: แสดงคุณสมบัติของตารางข้อมูล.
type: docs
url: /th/com.aspose.slides/idatatable/
---
**ส่วนต่อประสานที่ทำการใช้งานทั้งหมด:**
[com.aspose.slides.IFormattedTextContainer](../../com.aspose.slides/iformattedtextcontainer)
```
public interface IDataTable extends IFormattedTextContainer
```

แสดงคุณสมบัติของตารางข้อมูล.
## Methods

| Method | Description |
| --- | --- |
| [getFormat()](#getFormat--) | ส่งคืนรูปแบบเส้น, การเติม และเอฟเฟกต์ของวัตถุ. |
| [hasBorderHorizontal()](#hasBorderHorizontal--) | จริงถ้าตารางข้อมูลแผนภูมิมีขอบเซลล์แนวนอน. |
| [setBorderHorizontal(boolean value)](#setBorderHorizontal-boolean-) | จริงถ้าตารางข้อมูลแผนภูมิมีขอบเซลล์แนวนอน. |
| [hasBorderOutline()](#hasBorderOutline--) | จริงถ้าตารางข้อมูลแผนภูมิมีขอบรอบ. |
| [setBorderOutline(boolean value)](#setBorderOutline-boolean-) | จริงถ้าตารางข้อมูลแผนภูมิมีขอบรอบ. |
| [hasBorderVertical()](#hasBorderVertical--) | จริงถ้าตารางข้อมูลแผนภูมิมีขอบเซลล์แนวตั้ง. |
| [setBorderVertical(boolean value)](#setBorderVertical-boolean-) | จริงถ้าตารางข้อมูลแผนภูมิมีขอบเซลล์แนวตั้ง. |
| [getShowLegendKey()](#getShowLegendKey--) | จริงหากคีย์คำอธิบายป้ายกำกับข้อมูลมองเห็นได้. |
| [setShowLegendKey(boolean value)](#setShowLegendKey-boolean-) | จริงหากคีย์คำอธิบายป้ายกำกับข้อมูลมองเห็นได้. |
### getFormat() {#getFormat--}
```
public abstract IFormat getFormat()
```

ส่งคืนรูปแบบเส้น, การเติม และเอฟเฟกต์ของวัตถุ. Read-only [IFormat](../../com.aspose.slides/iformat).

**คืนค่า:**
[IFormat](../../com.aspose.slides/iformat)
### hasBorderHorizontal() {#hasBorderHorizontal--}
```
public abstract boolean hasBorderHorizontal()
```

จริงถ้าตารางข้อมูลแผนภูมิมีขอบเซลล์แนวนอน. Read/write boolean.

**คืนค่า:**
boolean
### setBorderHorizontal(boolean value) {#setBorderHorizontal-boolean-}
```
public abstract void setBorderHorizontal(boolean value)
```

จริงถ้าตารางข้อมูลแผนภูมิมีขอบเซลล์แนวนอน. Read/write boolean.

**พารามิเตอร์:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### hasBorderOutline() {#hasBorderOutline--}
```
public abstract boolean hasBorderOutline()
```

จริงถ้าตารางข้อมูลแผนภูมิมีขอบรอบ. Read/write boolean.

**คืนค่า:**
boolean
### setBorderOutline(boolean value) {#setBorderOutline-boolean-}
```
public abstract void setBorderOutline(boolean value)
```

จริงถ้าตารางข้อมูลแผนภูมิมีขอบรอบ. Read/write boolean.

**พารามิเตอร์:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### hasBorderVertical() {#hasBorderVertical--}
```
public abstract boolean hasBorderVertical()
```

จริงถ้าตารางข้อมูลแผนภูมิมีขอบเซลล์แนวตั้ง. Read/write boolean.

**คืนค่า:**
boolean
### setBorderVertical(boolean value) {#setBorderVertical-boolean-}
```
public abstract void setBorderVertical(boolean value)
```

จริงถ้าตารางข้อมูลแผนภูมิมีขอบเซลล์แนวตั้ง. Read/write boolean.

**พารามิเตอร์:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getShowLegendKey() {#getShowLegendKey--}
```
public abstract boolean getShowLegendKey()
```

จริงหากคีย์คำอธิบายป้ายกำกับข้อมูลมองเห็นได้. Read/write boolean.

**คืนค่า:**
boolean
### setShowLegendKey(boolean value) {#setShowLegendKey-boolean-}
```
public abstract void setShowLegendKey(boolean value)
```

จริงหากคีย์คำอธิบายป้ายกำกับข้อมูลมองเห็นได้. Read/write boolean.

**พารามิเตอร์:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |