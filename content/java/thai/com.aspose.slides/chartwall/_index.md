---
title: ChartWall
second_title: อ้างอิง API ของ Aspose.Slides สำหรับ Java
description: แทนกำแพงบนแผนภูมิ 3 มิติ.
type: docs
url: /th/com.aspose.slides/chartwall/
---
**การสืบทอด:**  
java.lang.Object, com.aspose.slides.DomObject

**อินเทอร์เฟซที่นำไปใช้ทั้งหมด:**  
[com.aspose.slides.IChartWall](../../com.aspose.slides/ichartwall)  
```
public class ChartWall extends DomObject<Chart> implements IChartWall
```

แทนกำแพงบนแผนภูมิ 3 มิติ.

## เมธอด

| เมธอด | คำอธิบาย |
| --- | --- |
| [getThickness()](#getThickness--) | คืนค่า หรือ กำหนดความหนาของกำแพงเป็นเปอร์เซ็นต์ของมิติที่ใหญ่ที่สุดของปริมาณการแสดงผล |
| [setThickness(int value)](#setThickness-int-) | คืนค่า หรือ กำหนดความหนาของกำแพงเป็นเปอร์เซ็นต์ของมิติที่ใหญ่ที่สุดของปริมาณการแสดงผล |
| [getFormat()](#getFormat--) | คืนค่าการเติมกำแพง, เส้น, เอฟเฟ็กต์, สไตล์ 3 มิติ |
| [getPictureType()](#getPictureType--) | คืนค่าหรือกำหนดประเภทของรูปภาพ |
| [setPictureType(int value)](#setPictureType-int-) | คืนค่าหรือกำหนดประเภทของรูปภาพ |

### getThickness() {#getThickness--}
```
public final int getThickness()
```

คืนค่า หรือ กำหนดความหนาของกำแพงเป็นเปอร์เซ็นต์ของมิติที่ใหญ่ที่สุดของปริมาณการแสดงผล. อ่าน/เขียน int.

**คืนค่า:**  
int

### setThickness(int value) {#setThickness-int-}
```
public final void setThickness(int value)
```

คืนค่า หรือ กำหนดความหนาของกำแพงเป็นเปอร์เซ็นต์ของมิติที่ใหญ่ที่สุดของปริมาณการแสดงผล. อ่าน/เขียน int.

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | int |  |

### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

คืนค่าการเติมกำแพง, เส้น, เอฟเฟ็กต์, สไตล์ 3 มิติ. อ่านอย่างเดียว [IFormat](../../com.aspose.slides/iformat).

**คืนค่า:**  
[IFormat](../../com.aspose.slides/iformat)

### getPictureType() {#getPictureType--}
```
public final int getPictureType()
```

คืนค่าหรือกำหนดประเภทของรูปภาพ. อ่าน/เขียน [PictureType](../../com.aspose.slides/picturetype).

**คืนค่า:**  
int

### setPictureType(int value) {#setPictureType-int-}
```
public final void setPictureType(int value)
```

คืนค่าหรือกำหนดประเภทของรูปภาพ. อ่าน/เขียน [PictureType](../../com.aspose.slides/picturetype).

**พารามิเตอร์:**
| พารามิเตอร์ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| value | int |  |