---
title: Legend
second_title: อ้างอิง API ของ Aspose.Slides สำหรับ Java
description: แสดงคุณสมบัติตำนานของแผนภูมิ.
type: docs
url: /th/com.aspose.slides/legend/
---
**การสืบทอด:**  
java.lang.Object, com.aspose.slides.DomObject

**อินเทอร์เฟซที่ทำการใช้งานทั้งหมด:**  
[com.aspose.slides.ILegend](../../com.aspose.slides/ilegend)  
```
public class Legend extends DomObject<Chart> implements ILegend
```

แสดงคุณสมบัติตำนานของแผนภูมิ.
## เมธอด

| เมธอด | คำอธิบาย |
| --- | --- |
| [getX()](#getX--) | คืนค่า หรือกำหนดค่าพิกัด x ของตำนานเป็นส่วนหนึ่งของความกว้างของแผนภูมิ. |
| [setX(float value)](#setX-float-) | คืนค่า หรือกำหนดค่าพิกัด x ของตำนานเป็นส่วนหนึ่งของความกว้างของแผนภูมิ. |
| [getY()](#getY--) | คืนค่า หรือกำหนดค่าพิกัด y ของตำนานเป็นส่วนหนึ่งของความสูงของแผนภูมิ. |
| [setY(float value)](#setY-float-) | คืนค่า หรือกำหนดค่าพิกัด y ของตำนานเป็นส่วนหนึ่งของความสูงของแผนภูมิ. |
| [getWidth()](#getWidth--) | คืนค่า หรือกำหนดค่าความกว้างของตำนานเป็นส่วนหนึ่งของความกว้างของแผนภูมิ. |
| [setWidth(float value)](#setWidth-float-) | คืนค่า หรือกำหนดค่าความกว้างของตำนานเป็นส่วนหนึ่งของความกว้างของแผนภูมิ. |
| [getHeight()](#getHeight--) | คืนค่า หรือกำหนดค่าความสูงของตำนานเป็นส่วนหนึ่งของความสูงของแผนภูมิ. |
| [setHeight(float value)](#setHeight-float-) | คืนค่า หรือกำหนดค่าความสูงของตำนานเป็นส่วนหนึ่งของความสูงของแผนภูมิ. |
| [getRight()](#getRight--) | ด้านขวา. |
| [getBottom()](#getBottom--) | ด้านล่าง. |
| [getOverlay()](#getOverlay--) | กำหนดว่ารายการแผนภูมิอื่น ๆ จะอนุญาตให้ทับซ้อนตำนานหรือไม่. |
| [setOverlay(boolean value)](#setOverlay-boolean-) | กำหนดว่ารายการแผนภูมิอื่น ๆ จะอนุญาตให้ทับซ้อนตำนานหรือไม่. |
| [getTextFormat()](#getTextFormat--) | รูปแบบข้อความ. |
| [getPosition()](#getPosition--) | ระบุตำแหน่งของตำนานบนแผนภูมิ. |
| [setPosition(int value)](#setPosition-int-) | ระบุตำแหน่งของตำนานบนแผนภูมิ. |
| [getFormat()](#getFormat--) | คืนค่ารูปแบบของตำนาน. |
| [getChart()](#getChart--) | คืนค่าแผนภูมิ. |
| [getEntries()](#getEntries--) | รับรายการตำนาน. |
| [getActualX()](#getActualX--) | ระบุตำแหน่ง x จริง (ซ้าย) ขององค์ประกอบแผนภูมิสัมพันธ์กับมุมซ้ายบนของแผนภูมิ. |
| [getActualY()](#getActualY--) | ระบุตำแหน่งบนจริงขององค์ประกอบแผนภูมิสัมพันธ์กับมุมซ้ายบนของแผนภูมิ. |
| [getActualWidth()](#getActualWidth--) | ระบุความกว้างจริงขององค์ประกอบแผนภูมิ. |
| [getActualHeight()](#getActualHeight--) | ระบุความสูงจริงขององค์ประกอบแผนภูมิ. |
| [getSlide()](#getSlide--) | คืนค่า slide พาเรนต์ของ FillFormat. |
| [getPresentation()](#getPresentation--) | คืนค่า presentation พาเรนต์ของ FillFormat. |
### getX() {#getX--}
```
public final float getX()
```

คืนค่า หรือกำหนดค่าพิกัด x ของตำนานเป็นส่วนหนึ่งของความกว้างของแผนภูมิ. อ่าน/เขียน float.

**คืนค่า:**  
float
### setX(float value) {#setX-float-}
```
public final void setX(float value)
```

คืนค่า หรือกำหนดค่าพิกัด x ของตำนานเป็นส่วนหนึ่งของความกว้างของแผนภูมิ. อ่าน/เขียน float.

**พารามิเตอร์:**
| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | float |  |

### getY() {#getY--}
```
public final float getY()
```

คืนค่า หรือกำหนดค่าพิกัด y ของตำนานเป็นส่วนหนึ่งของความสูงของแผนภูมิ. อ่าน/เขียน float.

**คืนค่า:**  
float
### setY(float value) {#setY-float-}
```
public final void setY(float value)
```

คืนค่า หรือกำหนดค่าพิกัด y ของตำนานเป็นส่วนหนึ่งของความสูงของแผนภูมิ. อ่าน/เขียน float.

**พารามิเตอร์:**
| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | float |  |

### getWidth() {#getWidth--}
```
public final float getWidth()
```

คืนค่า หรือกำหนดค่าความกว้างของตำนานเป็นส่วนหนึ่งของความกว้างของแผนภูมิ. อ่าน/เขียน float.

**คืนค่า:**  
float
### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```

คืนค่า หรือกำหนดค่าความกว้างของตำนานเป็นส่วนหนึ่งของความกว้างของแผนภูมิ. อ่าน/เขียน float.

**พารามิเตอร์:**
| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | float |  |

### getHeight() {#getHeight--}
```
public final float getHeight()
```

คืนค่า หรือกำหนดค่าความสูงของตำนานเป็นส่วนหนึ่งของความสูงของแผนภูมิ. อ่าน/เขียน float.

**คืนค่า:**  
float
### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```

คืนค่า หรือกำหนดค่าความสูงของตำนานเป็นส่วนหนึ่งของความสูงของแผนภูมิ. อ่าน/เขียน float.

**พารามิเตอร์:**
| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | float |  |

### getRight() {#getRight--}
```
public final float getRight()
```

ด้านขวา. อ่านอย่างเดียว float.

**คืนค่า:**  
float
### getBottom() {#getBottom--}
```
public final float getBottom()
```

ด้านล่าง. อ่านอย่างเดียว float.

**คืนค่า:**  
float
### getOverlay() {#getOverlay--}
```
public final boolean getOverlay()
```

กำหนดว่ารายการแผนภูมิอื่น ๆ จะอนุญาตให้ทับซ้อนตำนานหรือไม่. อ่าน/เขียน boolean.

**คืนค่า:**  
boolean
### setOverlay(boolean value) {#setOverlay-boolean-}
```
public final void setOverlay(boolean value)
```

กำหนดว่ารายการแผนภูมิอื่น ๆ จะอนุญาตให้ทับซ้อนตำนานหรือไม่. อ่าน/เขียน boolean.

**พารามิเตอร์:**
| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | boolean |  |

### getTextFormat() {#getTextFormat--}
```
public final IChartTextFormat getTextFormat()
```

รูปแบบข้อความ. อ่านอย่างเดียว [IChartTextFormat](../../com.aspose.slides/icharttextformat).

**คืนค่า:**  
[IChartTextFormat](../../com.aspose.slides/icharttextformat)
### getPosition() {#getPosition--}
```
public final int getPosition()
```

ระบุตำแหน่งของตำนานบนแผนภูมิ. ค่าที่ไม่เป็น NaN ของคุณสมบัติ X, Y, Width, Height จะครอบคลุมผลของคุณสมบัตินี้. อ่าน/เขียน [LegendPositionType](../../com.aspose.slides/legendpositiontype).

**คืนค่า:**  
int
### setPosition(int value) {#setPosition-int-}
```
public final void setPosition(int value)
```

ระบุตำแหน่งของตำนานบนแผนภูมิ. ค่าที่ไม่เป็น NaN ของคุณสมบัติ X, Y, Width, Height จะครอบคลุมผลของคุณสมบัตินี้. อ่าน/เขียน [LegendPositionType](../../com.aspose.slides/legendpositiontype).

**พารามิเตอร์:**
| พารามิเตอร์ | ชนิด | คำอธิบาย |
| --- | --- | --- |
| value | int |  |

### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

คืนค่ารูปแบบของตำนาน. อ่านอย่างเดียว [IFormat](../../com.aspose.slides/iformat).

**คืนค่า:**  
[IFormat](../../com.aspose.slides/iformat)
### getChart() {#getChart--}
```
public final IChart getChart()
```

คืนค่าแผนภูมิ. อ่านอย่างเดียว [IChart](../../com.aspose.slides/ichart).

**คืนค่า:**  
[IChart](../../com.aspose.slides/ichart)
### getEntries() {#getEntries--}
```
public final ILegendEntryCollection getEntries()
```

รับรายการตำนาน. อ่านอย่างเดียว [ILegendEntryCollection](../../com.aspose.slides/ilegendentrycollection).

**คืนค่า:**  
[ILegendEntryCollection](../../com.aspose.slides/ilegendentrycollection)
### getActualX() {#getActualX--}
```
public final float getActualX()
```

ระบุตำแหน่ง x จริง (ซ้าย) ขององค์ประกอบแผนภูมิสัมพันธ์กับมุมซ้ายบนของแผนภูมิ. เรียกเมธอด IChart.validateChartLayout() ก่อนเพื่อรับค่าจริง. อ่าน float.

**คืนค่า:**  
float
### getActualY() {#getActualY--}
```
public final float getActualY()
```

ระบุตำแหน่งบนจริงขององค์ประกอบแผนภูมิสัมพันธ์กับมุมซ้ายบนของแผนภูมิ. เรียกเมธอด IChart.validateChartLayout() ก่อนเพื่อรับค่าจริง. อ่าน float.

**คืนค่า:**  
float
### getActualWidth() {#getActualWidth--}
```
public final float getActualWidth()
```

ระบุความกว้างจริงขององค์ประกอบแผนภูมิ. เรียกเมธอด IChart.validateChartLayout() ก่อนเพื่อรับค่าจริง. อ่าน float.

**คืนค่า:**  
float
### getActualHeight() {#getActualHeight--}
```
public final float getActualHeight()
```

ระบุความสูงจริงขององค์ประกอบแผนภูมิ. เรียกเมธอด IChart.validateChartLayout() ก่อนเพื่อรับค่าจริง. อ่าน float.

**คืนค่า:**  
float
### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

คืนค่า slide พาเรนต์ของ FillFormat. อ่านอย่างเดียว [BaseSlide](../../com.aspose.slides/baseslide).

**คืนค่า:**  
[IBaseSlide](../../com.aspose.slides/ibaseslide)
### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

คืนค่า presentation พาเรนต์ของ FillFormat. อ่านอย่างเดียว [IPresentation](../../com.aspose.slides/ipresentation).

**คืนค่า:**  
[IPresentation](../../com.aspose.slides/ipresentation)