---
title: MarkerStyleType
second_title: Aspose.Slides for Java API リファレンス
description: チャートのデータポイントのマーカーの形状を決定します。
type: docs
url: /ja/com.aspose.slides/markerstyletype/
---
**継承:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class MarkerStyleType extends System.Enum
```

チャートのデータポイントのマーカーの形状を決定します。
## フィールド

| Field | Description |
| --- | --- |
| [NotDefined](#NotDefined) |  |
| [Circle](#Circle) | 各データポイントに円が描画されることを指定します。 |
| [Dash](#Dash) | 各データポイントに破線が描画されることを指定します。 |
| [Diamond](#Diamond) | 各データポイントに菱形が描画されることを指定します。 |
| [Dot](#Dot) | 各データポイントに点が描画されることを指定します。 |
| [None](#None) | 各データポイントに何も描画しないことを指定します。 |
| [Picture](#Picture) | 各データポイントに画像が描画されることを指定します。 |
| [Plus](#Plus) | 各データポイントにプラス記号が描画されることを指定します。 |
| [Square](#Square) | 各データポイントに四角形が描画されることを指定します。 |
| [Star](#Star) | 各データポイントに星形が描画されることを指定します。 |
| [Triangle](#Triangle) | 各データポイントに三角形が描画されることを指定します。 |
| [X](#X) | 各データポイントにXが描画されることを指定します。 |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```




### Circle {#Circle}
```
public static final int Circle
```


各データポイントに円が描画されることを指定します。

### Dash {#Dash}
```
public static final int Dash
```


各データポイントに破線が描画されることを指定します。

### Diamond {#Diamond}
```
public static final int Diamond
```


各データポイントに菱形が描画されることを指定します。

### Dot {#Dot}
```
public static final int Dot
```


各データポイントに点が描画されることを指定します。

### None {#None}
```
public static final int None
```


各データポイントに何も描画しないことを指定します。

### Picture {#Picture}
```
public static final int Picture
```


各データポイントに画像が描画されることを指定します。

### Plus {#Plus}
```
public static final int Plus
```


各データポイントにプラス記号が描画されることを指定します。

### Square {#Square}
```
public static final int Square
```


各データポイントに四角形が描画されることを指定します。

### Star {#Star}
```
public static final int Star
```


各データポイントに星形が描画されることを指定します。

### Triangle {#Triangle}
```
public static final int Triangle
```


各データポイントに三角形が描画されることを指定します。

### X {#X}
```
public static final int X
```


各データポイントにXが描画されることを指定します。