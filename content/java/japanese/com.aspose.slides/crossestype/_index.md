---
title: CrossesType
second_title: Aspose.Slides for Java API リファレンス
description: 軸が交差する位置を決定します。
type: docs
url: /ja/com.aspose.slides/crossestype/
---
**継承:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class CrossesType extends System.Enum
```

軸が交差する位置を決定します。

## フィールド

| フィールド | 説明 |
| --- | --- |
| [AxisCrossesAtZero](#AxisCrossesAtZero) | カテゴリ軸は、可能であれば値軸のゼロ点、最小値（最小値がゼロより大きい場合）、または最大値（最大値がゼロ未満の場合）で交差します。 |
| [Maximum](#Maximum) | 軸は最大値で交差します。 |
| [Custom](#Custom) | プロパティ CrossAt からのカスタム値 |

### AxisCrossesAtZero {#AxisCrossesAtZero}
```
public static final int AxisCrossesAtZero
```

カテゴリ軸は、可能であれば値軸のゼロ点、最小値（最小値がゼロより大きい場合）、または最大値（最大値がゼロ未満の場合）で交差します。

### Maximum {#Maximum}
```
public static final int Maximum
```

軸は最大値で交差します。

### Custom {#Custom}
```
public static final int Custom
```

プロパティ CrossAt からのカスタム値