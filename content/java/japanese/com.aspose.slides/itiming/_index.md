---
title: ITiming
second_title: Aspose.Slides for Java API Reference
description: アニメーションのタイミングを表します。
type: docs
url: /ja/com.aspose.slides/itiming/
---```
public interface ITiming
```

アニメーションのタイミングを表します。
## メソッド

| メソッド | 説明 |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | 加速動作効果の期間のパーセンテージを表します。 |
| [setAccelerate(float value)](#setAccelerate-float-) | 加速動作効果の期間のパーセンテージを表します。 |
| [getDecelerate()](#getDecelerate--) | 減速動作効果の期間のパーセンテージを表します。 |
| [setDecelerate(float value)](#setDecelerate-float-) | 減速動作効果の期間のパーセンテージを表します。 |
| [getAutoReverse()](#getAutoReverse--) | 正方向で再生した後に、アニメーションを自動的に逆方向で再生するかどうかを表します。 |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | 正方向で再生した後に、アニメーションを自動的に逆方向で再生するかどうかを表します。 |
| [getDuration()](#getDuration--) | アニメーション効果の期間を表します。 |
| [setDuration(float value)](#setDuration-float-) | アニメーション効果の期間を表します。 |
| [getRepeatCount()](#getRepeatCount--) | 効果が繰り返す回数を表します。 |
| [setRepeatCount(float value)](#setRepeatCount-float-) | 効果が繰り返す回数を表します。 |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | この属性は、効果がスライドの最後まで繰り返すかどうかを指定します。 |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | この属性は、効果がスライドの最後まで繰り返すかどうかを指定します。 |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | この属性は、効果が次のクリックまで繰り返すかどうかを指定します。 |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | この属性は、効果が次のクリックまで繰り返すかどうかを指定します。 |
| [getRepeatDuration()](#getRepeatDuration--) | 効果が繰り返す回数を表します。 |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | 効果が繰り返す回数を表します。 |
| [getRestart()](#getRestart--) | 効果が完了した後に再開するかどうかを指定します。 |
| [setRestart(int value)](#setRestart-int-) | 効果が完了した後に再開するかどうかを指定します。 |
| [getSpeed()](#getSpeed--) | タイミングを加速（または減速）させるパーセンテージを指定します。 |
| [setSpeed(float value)](#setSpeed-float-) | タイミングを加速（または減速）させるパーセンテージを指定します。 |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | トリガー後の遅延時間を表します。 |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | トリガー後の遅延時間を表します。 |
| [getTriggerType()](#getTriggerType--) | トリガーの種類を表します。 |
| [setTriggerType(int value)](#setTriggerType-int-) | トリガーの種類を表します。 |
| [getRewind()](#getRewind--) | この属性は、効果が完了した後に巻き戻すかどうかを指定します。 |
| [setRewind(boolean value)](#setRewind-boolean-) | この属性は、効果が完了した後に巻き戻すかどうかを指定します。 |

### getAccelerate() {#getAccelerate--}
```
public abstract float getAccelerate()
```

加速動作効果の期間のパーセンテージを表します。読み取り/書き込み float.

**戻り値:**
float

### setAccelerate(float value) {#setAccelerate-float-}
```
public abstract void setAccelerate(float value)
```

加速動作効果の期間のパーセンテージを表します。読み取り/書き込み float.

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public abstract float getDecelerate()
```

減速動作効果の期間のパーセンテージを表します。読み取り/書き込み float.

**戻り値:**
float

### setDecelerate(float value) {#setDecelerate-float-}
```
public abstract void setDecelerate(float value)
```

減速動作効果の期間のパーセンテージを表します。読み取り/書き込み float.

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public abstract boolean getAutoReverse()
```

正方向で再生した後に、アニメーションを自動的に逆方向で再生するかどうかを表します。読み取り/書き込み boolean.

**戻り値:**
boolean

### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public abstract void setAutoReverse(boolean value)
```

正方向で再生した後に、アニメーションを自動的に逆方向で再生するかどうかを表します。読み取り/書き込み boolean.

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public abstract float getDuration()
```

アニメーション効果の期間を表します。読み取り/書き込み float.

**戻り値:**
float

### setDuration(float value) {#setDuration-float-}
```
public abstract void setDuration(float value)
```

アニメーション効果の期間を表します。読み取り/書き込み float.

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public abstract float getRepeatCount()
```

効果が繰り返す回数を表します。読み取り/書き込み float.

**戻り値:**
float

### setRepeatCount(float value) {#setRepeatCount-float-}
```
public abstract void setRepeatCount(float value)
```

効果が繰り返す回数を表します。読み取り/書き込み float.

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public abstract boolean getRepeatUntilEndSlide()
```

この属性は、効果がスライドの最後まで繰り返すかどうかを指定します。読み取り/書き込み boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // 最初のスライドのエフェクトシーケンスを取得する
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // メインシーケンスの最初のエフェクトを取得する。
>      IEffect effect = effectsSequence.get_Item(0);
>      // エフェクトのタイミング/リピートを「スライドの最後まで」に変更する
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**戻り値:**
boolean

### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public abstract void setRepeatUntilEndSlide(boolean value)
```

この属性は、効果がスライドの最後まで繰り返すかどうかを指定します。読み取り/書き込み boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // 最初のスライドのエフェクトシーケンスを取得する
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // メインシーケンスの最初のエフェクトを取得する。
>      IEffect effect = effectsSequence.get_Item(0);
>      // エフェクトのタイミング/リピートを「スライドの最後まで」に変更する
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public abstract boolean getRepeatUntilNextClick()
```

この属性は、効果が次のクリックまで繰り返すかどうかを指定します。読み取り/書き込み boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // 最初のスライドのエフェクトシーケンスを取得する
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // メインシーケンスの最初のエフェクトを取得する。
>      IEffect effect = effectsSequence.get_Item(0);
>      // エフェクトのタイミング/リピートを「次のクリックまで」に変更する
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**戻り値:**
boolean

### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public abstract void setRepeatUntilNextClick(boolean value)
```

この属性は、効果が次のクリックまで繰り返すかどうかを指定します。読み取り/書き込み boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // 最初のスライドのエフェクトシーケンスを取得する
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // メインシーケンスの最初のエフェクトを取得する。
>      IEffect effect = effectsSequence.get_Item(0);
>      // エフェクトのタイミング/リピートを「次のクリックまで」に変更する
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public abstract float getRepeatDuration()
```

効果が繰り返す回数を表します。読み取り/書き込み float.

**戻り値:**
float

### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public abstract void setRepeatDuration(float value)
```

効果が繰り返す回数を表します。読み取り/書き込み float.

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public abstract int getRestart()
```

効果が完了した後に再開するかどうかを指定します。読み取り/書き込み [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**戻り値:**
int

### setRestart(int value) {#setRestart-int-}
```
public abstract void setRestart(int value)
```

効果が完了した後に再開するかどうかを指定します。読み取り/書き込み [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | int |  |

### getSpeed() {#getSpeed--}
```
public abstract float getSpeed()
```

タイミングを加速（または減速）させるパーセンテージを指定します。読み取り/書き込み float.

**戻り値:**
float

### setSpeed(float value) {#setSpeed-float-}
```
public abstract void setSpeed(float value)
```

タイミングを加速（または減速）させるパーセンテージを指定します。読み取り/書き込み float.

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public abstract float getTriggerDelayTime()
```

トリガー後の遅延時間を表します。読み取り/書き込み float.

**戻り値:**
float

### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public abstract void setTriggerDelayTime(float value)
```

トリガー後の遅延時間を表します。読み取り/書き込み float.

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public abstract int getTriggerType()
```

トリガーの種類を表します。読み取り/書き込み [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**戻り値:**
int

### setTriggerType(int value) {#setTriggerType-int-}
```
public abstract void setTriggerType(int value)
```

トリガーの種類を表します。読み取り/書き込み [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public abstract boolean getRewind()
```

この属性は、効果が完了した後に巻き戻すかどうかを指定します。読み取り/書き込み boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // 最初のスライドのエフェクトシーケンスを取得する
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // メインシーケンスの最初のエフェクトを取得する。
>      IEffect effect = effectsSequence.get_Item(0);
>      // エフェクトのタイミング/リワインドをオンにする。
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**戻り値:**
boolean

### setRewind(boolean value) {#setRewind-boolean-}
```
public abstract void setRewind(boolean value)
```

この属性は、効果が完了した後に巻き戻すかどうかを指定します。読み取り/書き込み boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // 最初のスライドのエフェクトシーケンスを取得する
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // メインシーケンスの最初のエフェクトを取得する。
>      IEffect effect = effectsSequence.get_Item(0);
>      // エフェクトのタイミング/リワインドをオンにする。
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | boolean |  |