---
title: DisplayUnitType
second_title: Aspose.Slides for Java API リファレンス
description: 表示データの多重度を決定します。
type: docs
url: /ja/com.aspose.slides/displayunittype/
---
**継承:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class DisplayUnitType extends System.Enum
```

表示データの多重度を決定します。
## フィールド

| Field | 説明 |
| --- | --- |
| [None](#None) | 値はそのまま表示されます。 |
| [Hundreds](#Hundreds) | チャート上の値を 100 で除算することを指定します。 |
| [Thousands](#Thousands) | チャート上の値を 1,000 で除算することを指定します。 |
| [TenThousands](#TenThousands) | チャート上の値を 10,000 で除算することを指定します。 |
| [HundredThousands](#HundredThousands) | チャート上の値を 100,000 で除算することを指定します。 |
| [Millions](#Millions) | チャート上の値を 1,000,000 で除算することを指定します。 |
| [TenMillions](#TenMillions) | チャート上の値を 10,000,000 で除算することを指定します。 |
| [HundredMillions](#HundredMillions) | チャート上の値を 100,000,000 で除算することを指定します。 |
| [Billions](#Billions) | チャート上の値を 1,000,000,000 で除算することを指定します。 |
| [Trillions](#Trillions) | チャート上の値を 1,000,000,000,000 で除算することを指定します。 |
| [CustomValue](#CustomValue) | チャート上の値をカスタム値で除算することを指定します。 |
### None {#None}
```
public static final int None
```


値はそのまま表示されます。

### Hundreds {#Hundreds}
```
public static final int Hundreds
```


チャート上の値を 100 で除算することを指定します。

### Thousands {#Thousands}
```
public static final int Thousands
```


チャート上の値を 1,000 で除算することを指定します。

### TenThousands {#TenThousands}
```
public static final int TenThousands
```


チャート上の値を 10,000 で除算することを指定します。

### HundredThousands {#HundredThousands}
```
public static final int HundredThousands
```


チャート上の値を 100,000 で除算することを指定します。

### Millions {#Millions}
```
public static final int Millions
```


チャート上の値を 1,000,000 で除算することを指定します。

### TenMillions {#TenMillions}
```
public static final int TenMillions
```


チャート上の値を 10,000,000 で除算することを指定します。

### HundredMillions {#HundredMillions}
```
public static final int HundredMillions
```


チャート上の値を 100,000,000 で除算することを指定します。

### Billions {#Billions}
```
public static final int Billions
```


チャート上の値を 1,000,000,000 で除算することを指定します。

### Trillions {#Trillions}
```
public static final int Trillions
```


チャート上の値を 1,000,000,000,000 で除算することを指定します。

### CustomValue {#CustomValue}
```
public static final int CustomValue
```


チャート上の値をカスタム値で除算することを指定します。