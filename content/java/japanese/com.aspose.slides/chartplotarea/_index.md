---
title: ChartPlotArea
second_title: Aspose.Slides for Java APIリファレンス
description: チャートがプロットされるべき矩形を表します。
type: docs
url: /ja/com.aspose.slides/chartplotarea/
---
**Inheritance:**
java.lang.Object, com.aspose.slides.DomObject

**All Implemented Interfaces:**
[com.aspose.slides.IChartPlotArea](../../com.aspose.slides/ichartplotarea)
```
public class ChartPlotArea extends DomObject<Chart> implements IChartPlotArea
```

チャートがプロットされるべき矩形を表します。

## メソッド

| メソッド | 説明 |
| --- | --- |
| [getFormat()](#getFormat--) | プロット領域の形式を返します。 |
| [getX()](#getX--) | チャートの幅に対する割合（0 から 1）として、プロット領域バウンディングボックスの左上隅の x 座標を取得または設定します。 |
| [setX(float value)](#setX-float-) | チャートの幅に対する割合（0 から 1）として、プロット領域バウンディングボックスの左上隅の x 座標を取得または設定します。 |
| [getY()](#getY--) | チャートの高さに対する割合（0 から 1）として、プロット領域バウンディングボックスの左上隅の y 座標を取得または設定します。 |
| [setY(float value)](#setY-float-) | チャートの高さに対する割合（0 から 1）として、プロット領域バウンディングボックスの左上隅の y 座標を取得または設定します。 |
| [getWidth()](#getWidth--) | チャートの幅に対する割合（0 から 1）として、プロット領域バウンディングボックスの幅を取得または設定します。 |
| [setWidth(float value)](#setWidth-float-) | チャートの幅に対する割合（0 から 1）として、プロット領域バウンディングボックスの幅を取得または設定します。 |
| [getHeight()](#getHeight--) | チャートの高さに対する割合（0 から 1）として、プロット領域バウンディングボックスの高さを取得または設定します。 |
| [setHeight(float value)](#setHeight-float-) | チャートの高さに対する割合（0 から 1）として、プロット領域バウンディングボックスの高さを取得または設定します。 |
| [getRight()](#getRight--) | 右。 |
| [getBottom()](#getBottom--) | 下。 |
| [getChart()](#getChart--) | チャート。 |
| [isLocationAutocalculated()](#isLocationAutocalculated--) | 位置の計算方法を定義します: true \u2013 自動的に計算されます; X、Y、Width、Height プロパティで定義されます。 |
| [getLayoutTargetType()](#getLayoutTargetType--) | プロット領域のレイアウトが手動で定義されている場合、このプロパティは領域の内部（軸および軸ラベルを除く）でレイアウトするか、外部（軸および軸ラベルを含む）でレイアウトするかを指定します。 |
| [setLayoutTargetType(int value)](#setLayoutTargetType-int-) | プロット領域のレイアウトが手動で定義されている場合、このプロパティは領域の内部（軸および軸ラベルを除く）でレイアウトするか、外部（軸および軸ラベルを含む）でレイアウトするかを指定します。 |
| [getActualX()](#getActualX--) | チャート要素の実際の x 位置（左）を、チャートの左上隅に対して指定します。 |
| [getActualY()](#getActualY--) | チャート要素の実際の上端を、チャートの左上隅に対して指定します。 |
| [getActualWidth()](#getActualWidth--) | チャート要素の実際の幅を指定します。 |
| [getActualHeight()](#getActualHeight--) | チャート要素の実際の高さを指定します。 |
| [getSlide()](#getSlide--) | FillFormat の親スライドを返します。 |
| [getPresentation()](#getPresentation--) | FillFormat の親プレゼンテーションを返します。 |

### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

プロット領域の形式を返します。読み取り専用 [IFormat](../../com.aspose.slides/iformat)。

**戻り値:**
[IFormat](../../com.aspose.slides/iformat)

### getX() {#getX--}
```
public final float getX()
```

チャートの幅に対する割合（0 から 1）として、プロット領域バウンディングボックスの左上隅の x 座標を取得または設定します。読み書き可能 float。

**戻り値:**
float

### setX(float value) {#setX-float-}
```
public final void setX(float value)
```

チャートの幅に対する割合（0 から 1）として、プロット領域バウンディングボックスの左上隅の x 座標を取得または設定します。読み書き可能 float。

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getY() {#getY--}
```
public final float getY()
```

チャートの高さに対する割合（0 から 1）として、プロット領域バウンディングボックスの左上隅の y 座標を取得または設定します。読み書き可能 float。

**戻り値:**
float

### setY(float value) {#setY-float-}
```
public final void setY(float value)
```

チャートの高さに対する割合（0 から 1）として、プロット領域バウンディングボックスの左上隅の y 座標を取得または設定します。読み書き可能 float。

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getWidth() {#getWidth--}
```
public final float getWidth()
```

チャートの幅に対する割合（0 から 1）として、プロット領域バウンディングボックスの幅を取得または設定します。読み書き可能 float。

**戻り値:**
float

### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```

チャートの幅に対する割合（0 から 1）として、プロット領域バウンディングボックスの幅を取得または設定します。読み書き可能 float。

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getHeight() {#getHeight--}
```
public final float getHeight()
```

チャートの高さに対する割合（0 から 1）として、プロット領域バウンディングボックスの高さを取得または設定します。読み書き可能 float。

**戻り値:**
float

### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```

チャートの高さに対する割合（0 から 1）として、プロット領域バウンディングボックスの高さを取得または設定します。読み書き可能 float。

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getRight() {#getRight--}
```
public final float getRight()
```

右。読み取り専用 float。

**戻り値:**
float

### getBottom() {#getBottom--}
```
public final float getBottom()
```

下。読み取り専用 float。

**戻り値:**
float

### getChart() {#getChart--}
```
public final IChart getChart()
```

チャート。読み取り専用 [IChart](../../com.aspose.slides/ichart)。

**戻り値:**
[IChart](../../com.aspose.slides/ichart)

### isLocationAutocalculated() {#isLocationAutocalculated--}
```
public final boolean isLocationAutocalculated()
```

位置の計算方法を定義します: true \u2013 自動的に計算されます; X、Y、Width、Height プロパティで定義されます。読み取り専用 boolean。

**戻り値:**
boolean

### getLayoutTargetType() {#getLayoutTargetType--}
```
public final int getLayoutTargetType()
```

プロット領域のレイアウトが手動で定義されている場合、このプロパティは領域の内部（軸および軸ラベルを除く）でレイアウトするか、外部（軸および軸ラベルを含む）でレイアウトするかを指定します。読み書き可能 [LayoutTargetType](../../com.aspose.slides/layouttargettype)(\#getLayoutTargetType.getLayoutTargetType/\#setLayoutTargetType(int).setLayoutTargetType(int))。

--------------------

> ```
> Presentation presentation = new Presentation();
>   try
>   {
>       ISlide slide = presentation.getSlides().get_Item(0);
>       IChart chart = slide.getShapes().addChart(ChartType.ClusteredColumn, 20, 100, 600, 400);
>       chart.getPlotArea().setX(0.2f);
>       chart.getPlotArea().setY(0.2f);
>       chart.getPlotArea().setWidth(0.7f);
>       chart.getPlotArea().setHeight(0.7f);
>       chart.getPlotArea().setLayoutTargetType(LayoutTargetType.Inner);
>       ...
>   } finally {
>       if (presentation != null) presentation.dispose();
>   }
> ```

**戻り値:**
int

### setLayoutTargetType(int value) {#setLayoutTargetType-int-}
```
public final void setLayoutTargetType(int value)
```

プロット領域のレイアウトが手動で定義されている場合、このプロパティは領域の内部（軸および軸ラベルを除く）でレイアウトするか、外部（軸および軸ラベルを含む）でレイアウトするかを指定します。読み書き可能 [LayoutTargetType](../../com.aspose.slides/layouttargettype)(\#getLayoutTargetType.getLayoutTargetType/\#setLayoutTargetType(int).setLayoutTargetType(int))。

--------------------

> ```
> Presentation presentation = new Presentation();
>   try
>   {
>       ISlide slide = presentation.getSlides().get_Item(0);
>       IChart chart = slide.getShapes().addChart(ChartType.ClusteredColumn, 20, 100, 600, 400);
>       chart.getPlotArea().setX(0.2f);
>       chart.getPlotArea().setY(0.2f);
>       chart.getPlotArea().setWidth(0.7f);
>       chart.getPlotArea().setHeight(0.7f);
>       chart.getPlotArea().setLayoutTargetType(LayoutTargetType.Inner);
>       ...
>   } finally {
>       if (presentation != null) presentation.dispose();
>   }
> ```

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | int |  |

### getActualX() {#getActualX--}
```
public final float getActualX()
```

チャート要素の実際の x 位置（左）を、チャートの左上隅に対して指定します。実際の値を取得するには、事前に IChart.ValidateChartLayout() メソッドを呼び出してください。読み取り専用 float。

**戻り値:**
float

### getActualY() {#getActualY--}
```
public final float getActualY()
```

チャート要素の実際の上端を、チャートの左上隅に対して指定します。実際の値を取得するには、事前に IChart.ValidateChartLayout() メソッドを呼び出してください。読み取り専用 float。

**戻り値:**
float

### getActualWidth() {#getActualWidth--}
```
public final float getActualWidth()
```

チャート要素の実際の幅を指定します。実際の値を取得するには、事前に IChart.ValidateChartLayout() メソッドを呼び出してください。読み取り専用 float。

**戻り値:**
float

### getActualHeight() {#getActualHeight--}
```
public final float getActualHeight()
```

チャート要素の実際の高さを指定します。実際の値を取得するには、事前に IChart.ValidateChartLayout() メソッドを呼び出してください。読み取り専用 float。

**戻り値:**
float

### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

FillFormat の親スライドを返します。読み取り専用 [BaseSlide](../../com.aspose.slides/baseslide)。

**戻り値:**
[IBaseSlide](../../com.aspose.slides/ibaseslide)

### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

FillFormat の親プレゼンテーションを返します。読み取り専用 [IPresentation](../../com.aspose.slides/ipresentation)。

**戻り値:**
[IPresentation](../../com.aspose.slides/ipresentation)