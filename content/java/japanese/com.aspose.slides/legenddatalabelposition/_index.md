---
title: LegendDataLabelPosition
second_title: Aspose.Slides for Java API リファレンス
description: データラベルの位置を決定します。
type: docs
url: /ja/com.aspose.slides/legenddatalabelposition/
---
**継承:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class LegendDataLabelPosition extends System.Enum
```

データラベルの位置を決定します。
## フィールド

| フィールド | 説明 |
| --- | --- |
| [NotDefined](#NotDefined) | 位置は未定義です。 |
| [Bottom](#Bottom) | データラベルをデータマーカーの下に表示することを指定します。 |
| [BestFit](#BestFit) | データラベルを最適な位置に表示することを指定します。 |
| [Center](#Center) | データラベルをデータマーカーの中央に表示することを指定します。 |
| [InsideBase](#InsideBase) | データラベルをデータマーカーの基部内に表示することを指定します。 |
| [InsideEnd](#InsideEnd) | データラベルをデータマーカーの端部内に表示することを指定します。 |
| [Left](#Left) | データラベルをデータマーカーの左側に表示することを指定します。 |
| [OutsideEnd](#OutsideEnd) | データラベルをデータマーカーの端部外側に表示することを指定します。 |
| [Right](#Right) | データラベルをデータマーカーの右側に表示することを指定します。 |
| [Top](#Top) | データラベルをデータマーカーの上に表示することを指定します。 |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```

位置は未定義です。

### Bottom {#Bottom}
```
public static final int Bottom
```

データラベルをデータマーカーの下に表示することを指定します。

### BestFit {#BestFit}
```
public static final int BestFit
```

データラベルを最適な位置に表示することを指定します。

### Center {#Center}
```
public static final int Center
```

データラベルをデータマーカーの中央に表示することを指定します。

### InsideBase {#InsideBase}
```
public static final int InsideBase
```

データラベルをデータマーカーの基部内に表示することを指定します。

### InsideEnd {#InsideEnd}
```
public static final int InsideEnd
```

データラベルをデータマーカーの端部内に表示することを指定します。

### Left {#Left}
```
public static final int Left
```

データラベルをデータマーカーの左側に表示することを指定します。

### OutsideEnd {#OutsideEnd}
```
public static final int OutsideEnd
```

データラベルをデータマーカーの端部外側に表示することを指定します。

### Right {#Right}
```
public static final int Right
```

データラベルをデータマーカーの右側に表示することを指定します。

### Top {#Top}
```
public static final int Top
```

データラベルをデータマーカーの上に表示することを指定します。