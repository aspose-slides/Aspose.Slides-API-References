---
title: ILayoutable
second_title: Aspose.Slides for Java API リファレンス
description: チャート要素の正確な位置を指定します。
type: docs
url: /ja/com.aspose.slides/ilayoutable/
---
**実装されているすべてのインターフェイス:**
[com.aspose.slides.IChartComponent](../../com.aspose.slides/ichartcomponent)
```
public interface ILayoutable extends IChartComponent
```

チャート要素の正確な位置を指定します。

## メソッド

| メソッド | 説明 |
| --- | --- |
| [getX()](#getX--) | チャート要素の x 位置（左）を、チャートの幅の割合として指定します。 |
| [setX(float value)](#setX-float-) | チャート要素の x 位置（左）を、チャートの幅の割合として指定します。 |
| [getY()](#getY--) | チャート要素の上端を、チャートの高さの割合として指定します。 |
| [setY(float value)](#setY-float-) | チャート要素の上端を、チャートの高さの割合として指定します。 |
| [getWidth()](#getWidth--) | チャート要素の幅を、チャートの幅の割合として指定します。 |
| [setWidth(float value)](#setWidth-float-) | チャート要素の幅を、チャートの幅の割合として指定します。 |
| [getHeight()](#getHeight--) | チャート要素の高さを、チャートの高さの割合として指定します。 |
| [setHeight(float value)](#setHeight-float-) | チャート要素の高さを、チャートの高さの割合として指定します。 |
| [getRight()](#getRight--) | チャート要素の右端を、チャートの幅の割合として取得します。 |
| [getBottom()](#getBottom--) | チャート要素の上端を、チャートの高さの割合として取得します。 |

### getX() {#getX--}
```
public abstract float getX()
```

チャート要素の x 位置（左）を、チャートの幅の割合として指定します。 読み書き可能な float。

**戻り値:**
float

### setX(float value) {#setX-float-}
```
public abstract void setX(float value)
```

チャート要素の x 位置（左）を、チャートの幅の割合として指定します。 読み書き可能な float。

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getY() {#getY--}
```
public abstract float getY()
```

チャート要素の上端を、チャートの高さの割合として指定します。 読み書き可能な float。

**戻り値:**
float

### setY(float value) {#setY-float-}
```
public abstract void setY(float value)
```

チャート要素の上端を、チャートの高さの割合として指定します。 読み書き可能な float。

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getWidth() {#getWidth--}
```
public abstract float getWidth()
```

チャート要素の幅を、チャートの幅の割合として指定します。 読み書き可能な float。

**戻り値:**
float

### setWidth(float value) {#setWidth-float-}
```
public abstract void setWidth(float value)
```

チャート要素の幅を、チャートの幅の割合として指定します。 読み書き可能な float。

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getHeight() {#getHeight--}
```
public abstract float getHeight()
```

チャート要素の高さを、チャートの高さの割合として指定します。 読み書き可能な float。

**戻り値:**
float

### setHeight(float value) {#setHeight-float-}
```
public abstract void setHeight(float value)
```

チャート要素の高さを、チャートの高さの割合として指定します。 読み書き可能な float。

**パラメーター:**
| パラメーター | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getRight() {#getRight--}
```
public abstract float getRight()
```

チャート要素の右端を、チャートの幅の割合として取得します。 読み取り専用 float。

**戻り値:**
float

### getBottom() {#getBottom--}
```
public abstract float getBottom()
```

チャート要素の上端を、チャートの高さの割合として取得します。 読み取り専用 float。

**戻り値:**
float