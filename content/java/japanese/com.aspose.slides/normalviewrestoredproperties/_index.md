---
title: NormalViewRestoredProperties
second_title: Aspose.Slides for Java APIリファレンス
description: スライド領域のサイズを指定します。通常ビューにおいて、restoredTop の子の場合は幅、restoredLeft の子の場合は高さを表し、領域が可変の復元サイズ（最小化でも最大化でもない）です。
type: docs
url: /ja/com.aspose.slides/normalviewrestoredproperties/
---
**継承:**
java.lang.Object

**実装されているすべてのインターフェイス:**
[com.aspose.slides.INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)
```
public class NormalViewRestoredProperties implements INormalViewRestoredProperties
```

スライド領域のサイズ指定（通常ビューの restoredTop の子の場合は幅、restoredLeft の子の場合は高さ）で、領域が可変の復元サイズ（最小化でも最大化でもない）である場合です。

## メソッド

| メソッド | 説明 |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | スライド領域のサイズを指定します（RestoredTop の子の場合は幅、RestoredLeft の子の場合は高さ）。 |
| [setDimensionSize(float value)](#setDimensionSize-float-) | スライド領域のサイズを指定します（RestoredTop の子の場合は幅、RestoredLeft の子の場合は高さ）。 |
| [getAutoAdjust()](#getAutoAdjust--) | ウィンドウ内のビューを含むアプリケーションでサイズ変更時にサイドコンテンツ領域のサイズが新しいサイズに合わせて補正されるかどうかを指定します。読み書き可能な boolean。 |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | ウィンドウ内のビューを含むアプリケーションでサイズ変更時にサイドコンテンツ領域のサイズが新しいサイズに合わせて補正されるかどうかを指定します。読み書き可能な boolean。 |
### getDimensionSize() {#getDimensionSize--}
```
public final float getDimensionSize()
```


スライド領域のサイズを指定します（RestoredTop の子の場合は幅、RestoredLeft の子の場合は高さ）。読み書き可能な float。

**戻り値:**
float
### setDimensionSize(float value) {#setDimensionSize-float-}
```
public final void setDimensionSize(float value)
```


スライド領域のサイズを指定します（RestoredTop の子の場合は幅、RestoredLeft の子の場合は高さ）。読み書き可能な float。

**パラメータ:**
| パラメータ | 型 | 説明 |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public final boolean getAutoAdjust()
```


ウィンドウ内のビューを含むアプリケーションでサイズ変更時にサイドコンテンツ領域のサイズが新しいサイズに合わせて補正されるかどうかを指定します。読み書き可能な boolean。

**戻り値:**
boolean
### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public final void setAutoAdjust(boolean value)
```


ウィンドウ内のビューを含むアプリケーションでサイズ変更時にサイドコンテンツ領域のサイズが新しいサイズに合わせて補正されるかどうかを指定します。読み書き可能な boolean。

**パラメータ:**
| パラメータ | 型 | 説明 |
| --- | --- | --- |
| value | boolean |  |