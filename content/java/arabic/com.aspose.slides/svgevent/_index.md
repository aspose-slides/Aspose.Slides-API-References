---
title: SvgEvent
second_title: Aspose.Slides لواجهة برمجة التطبيقات لجافا
description: يمثل خيارات لشكل SVG.
type: docs
url: /ar/com.aspose.slides/svgevent/
---
**الوراثة:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class SvgEvent extends System.Enum
```

يمثل خيارات لشكل SVG.
## الحقول

| الحقل | الوصف |
| --- | --- |
| [OnFocusIn](#OnFocusIn) | يحدث عندما يحصل العنصر على التركيز، مثل عندما يتم تحديد نص. |
| [OnFocusOut](#OnFocusOut) | يحدث عندما يفقد العنصر التركيز، مثل عندما يتم إلغاء تحديد نص. |
| [OnActivate](#OnActivate) | يحدث عندما يتم تنشيط العنصر، على سبيل المثال، عبر نقرة الفأرة أو ضغط مفتاح. |
| [OnClick](#OnClick) | يحدث عندما يتم النقر بزر جهاز الإشارة فوق عنصر. |
| [OnMouseDown](#OnMouseDown) | يحدث عندما يتم الضغط على زر جهاز الإشارة فوق عنصر. |
| [OnMouseUp](#OnMouseUp) | يحدث عندما يتم تحرير زر جهاز الإشارة فوق عنصر. |
| [OnMouseOver](#OnMouseOver) | يحدث عندما يتم تحريك جهاز الإشارة إلى عنصر. |
| [OnMouseMove](#OnMouseMove) | يحدث عندما يتم تحريك جهاز الإشارة بينما هو فوق عنصر. |
| [OnMouseOut](#OnMouseOut) | يحدث عندما يتم تحريك جهاز الإشارة بعيدًا عن عنصر. |
| [OnLoad](#OnLoad) | يحدث عندما يكون عميل المستخدم قد عالج بالكامل العنصر وسلالته وكل الموارد المشار إليها المطلوبة لعرضه. |
| [OnUnload](#OnUnload) | يحدث عندما يزيل تنفيذ DOM وثيقة من نافذة أو إطار. |
| [OnAbort](#OnAbort) | يحدث عندما يتوقف تحميل الصفحة قبل أن يُسمح للعنصر بالتحميل بالكامل. |
| [OnError](#OnError) | يحدث عندما لا يتم تحميل العنصر بشكل صحيح أو عندما يحدث خطأ أثناء تنفيذ النص. |
| [OnResize](#OnResize) | يحدث عندما يتم تغيير حجم عرض المستند. |
| [OnScroll](#OnScroll) | يحدث عندما يتم تحريك عرض المستند على طول المحور X أو Y أو كليهما. |
| [OnZoom](#OnZoom) | يحدث عندما يتم تغيير مستوى تكبير عرض المستند. |
| [OnBegin](#OnBegin) | يحدث عندما يبدأ عنصر الرسوم المتحركة. |
| [OnEnd](#OnEnd) | يحدث عندما ينتهي عنصر الرسوم المتحركة. |
| [OnRepeat](#OnRepeat) | يحدث عندما يتكرر عنصر الرسوم المتحركة. |
### OnFocusIn {#OnFocusIn}
```
public static final int OnFocusIn
```

يحدث عندما يحصل العنصر على التركيز، مثل عندما يتم تحديد نص.

### OnFocusOut {#OnFocusOut}
```
public static final int OnFocusOut
```

يحدث عندما يفقد العنصر التركيز، مثل عندما يتم إلغاء تحديد نص.

### OnActivate {#OnActivate}
```
public static final int OnActivate
```

يحدث عندما يتم تنشيط العنصر، على سبيل المثال، عبر نقرة الفأرة أو ضغط مفتاح.

### OnClick {#OnClick}
```
public static final int OnClick
```

يحدث عندما يتم النقر بزر جهاز الإشارة فوق عنصر.

### OnMouseDown {#OnMouseDown}
```
public static final int OnMouseDown
```

يحدث عندما يتم الضغط على زر جهاز الإشارة فوق عنصر.

### OnMouseUp {#OnMouseUp}
```
public static final int OnMouseUp
```

يحدث عندما يتم تحرير زر جهاز الإشارة فوق عنصر.

### OnMouseOver {#OnMouseOver}
```
public static final int OnMouseOver
```

يحدث عندما يتم تحريك جهاز الإشارة إلى عنصر.

### OnMouseMove {#OnMouseMove}
```
public static final int OnMouseMove
```

يحدث عندما يتم تحريك جهاز الإشارة بينما هو فوق عنصر.

### OnMouseOut {#OnMouseOut}
```
public static final int OnMouseOut
```

يحدث عندما يتم تحريك جهاز الإشارة بعيدًا عن عنصر.

### OnLoad {#OnLoad}
```
public static final int OnLoad
```

يحدث عندما يكون عميل المستخدم قد عالج بالكامل العنصر وسلالته وكل الموارد المشار إليها المطلوبة لعرضه.

### OnUnload {#OnUnload}
```
public static final int OnUnload
```

يحدث عندما يزيل تنفيذ DOM وثيقة من نافذة أو إطار. فقط قابل للتطبيق على عناصر svg الخارجية.

### OnAbort {#OnAbort}
```
public static final int OnAbort
```

يحدث عندما يتوقف تحميل الصفحة قبل أن يُسمح للعنصر بالتحميل بالكامل.

### OnError {#OnError}
```
public static final int OnError
```

يحدث عندما لا يتم تحميل العنصر بشكل صحيح أو عندما يحدث خطأ أثناء تنفيذ النص.

### OnResize {#OnResize}
```
public static final int OnResize
```

يحدث عندما يتم تغيير حجم عرض المستند. فقط قابل للتطبيق على عناصر svg الخارجية.

### OnScroll {#OnScroll}
```
public static final int OnScroll
```

يحدث عندما يتم تحريك عرض المستند على طول المحور X أو Y أو كليهما. فقط قابل للتطبيق على عناصر svg الخارجية.

### OnZoom {#OnZoom}
```
public static final int OnZoom
```

يحدث عندما يتم تغيير مستوى تكبير عرض المستند. فقط قابل للتطبيق على عناصر svg الخارجية.

### OnBegin {#OnBegin}
```
public static final int OnBegin
```

يحدث عندما يبدأ عنصر الرسوم المتحركة.

### OnEnd {#OnEnd}
```
public static final int OnEnd
```

يحدث عندما ينتهي عنصر الرسوم المتحركة.

### OnRepeat {#OnRepeat}
```
public static final int OnRepeat
```

يحدث عندما يتكرر عنصر الرسوم المتحركة.