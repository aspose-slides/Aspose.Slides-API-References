---
title: IMotionCmdPath
second_title: Aspose.Slides for Java API Reference
description: يمثل أمرًا واحدًا في مسار.
type: docs
url: /ar/com.aspose.slides/imotioncmdpath/
---```
public interface IMotionCmdPath
```

يمثل أمرًا واحدًا في مسار.

## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getPoints()](#getPoints--) | يحدد نقاط الأمر. |
| [setPoints(Point2D.Float[] value)](#setPoints-java.awt.geom.Point2D.Float---) | يحدد نقاط الأمر. |
| [getCommandType()](#getCommandType--) | يحدد نوع الأمر. |
| [setCommandType(int value)](#setCommandType-int-) | يحدد نوع الأمر. |
| [isRelative()](#isRelative--) | تحديد ما إذا كانت إحداثيات الأمر نسبية أم لا. |
| [setRelative(boolean value)](#setRelative-boolean-) | تحديد ما إذا كانت إحداثيات الأمر نسبية أم لا. |
| [getPointsType()](#getPointsType--) | يحدد نوع نقاط الأمر للقراءة/الكتابة [MotionPathPointsType](../../com.aspose.slides/motionpathpointstype). |
| [setPointsType(int value)](#setPointsType-int-) | يحدد نوع نقاط الأمر للقراءة/الكتابة [MotionPathPointsType](../../com.aspose.slides/motionpathpointstype). |

### getPoints() {#getPoints--}
```
public abstract Point2D.Float[] getPoints()
```

يحدد نقاط الأمر. للقراءة/الكتابة java.awt.geom.Point2D.Float[].

**القيمة المرجعة:**
java.awt.geom.Point2D.Float[]

### setPoints(Point2D.Float[] value) {#setPoints-java.awt.geom.Point2D.Float---}
```
public abstract void setPoints(Point2D.Float[] value)
```

يحدد نقاط الأمر. للقراءة/الكتابة java.awt.geom.Point2D.Float[].

**المعلمات:**
| معلمة | النوع | الوصف |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float[] |  |

### getCommandType() {#getCommandType--}
```
public abstract int getCommandType()
```

يحدد نوع الأمر. للقراءة/الكتابة [MotionCommandPathType](../../com.aspose.slides/motioncommandpathtype).

**القيمة المرجعة:**
int

### setCommandType(int value) {#setCommandType-int-}
```
public abstract void setCommandType(int value)
```

يحدد نوع الأمر. للقراءة/الكتابة [MotionCommandPathType](../../com.aspose.slides/motioncommandpathtype).

**المعلمات:**
| معلمة | النوع | الوصف |
| --- | --- | --- |
| value | int |  |

### isRelative() {#isRelative--}
```
public abstract boolean isRelative()
```

تحديد ما إذا كانت إحداثيات الأمر نسبية أم لا. للقراءة/الكتابة boolean.

**القيمة المرجعة:**
boolean

### setRelative(boolean value) {#setRelative-boolean-}
```
public abstract void setRelative(boolean value)
```

تحديد ما إذا كانت إحداثيات الأمر نسبية أم لا. للقراءة/الكتابة boolean.

**المعلمات:**
| معلمة | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getPointsType() {#getPointsType--}
```
public abstract int getPointsType()
```

يحدد نوع نقاط الأمر للقراءة/الكتابة [MotionPathPointsType](../../com.aspose.slides/motionpathpointstype).

**القيمة المرجعة:**
int

### setPointsType(int value) {#setPointsType-int-}
```
public abstract void setPointsType(int value)
```

يحدد نوع نقاط الأمر للقراءة/الكتابة [MotionPathPointsType](../../com.aspose.slides/motionpathpointstype).

**المعلمات:**
| معلمة | النوع | الوصف |
| --- | --- | --- |
| value | int |  |