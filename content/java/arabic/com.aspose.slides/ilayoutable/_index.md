---
title: ILayoutable
second_title: مرجع API الخاص بـ Aspose.Slides للـ Java
description: يحدد الموضع الدقيق لعنصر المخطط.
type: docs
url: /ar/com.aspose.slides/ilayoutable/
---
**جميع الواجهات المنفذة:**
[com.aspose.slides.IChartComponent](../../com.aspose.slides/ichartcomponent)
```
public interface ILayoutable extends IChartComponent
```

يحدد الموضع الدقيق لعنصر المخطط.
## الطرق

| طريقة | الوصف |
| --- | --- |
| [getX()](#getX--) | يحدد موضع x (اليسار) لعنصر المخطط كنسبة من عرض المخطط. |
| [setX(float value)](#setX-float-) | يحدد موضع x (اليسار) لعنصر المخطط كنسبة من عرض المخطط. |
| [getY()](#getY--) | يحدد أعلى عنصر المخطط كنسبة من ارتفاع المخطط. |
| [setY(float value)](#setY-float-) | يحدد أعلى عنصر المخطط كنسبة من ارتفاع المخطط. |
| [getWidth()](#getWidth--) | يحدد عرض عنصر المخطط كنسبة من عرض المخطط. |
| [setWidth(float value)](#setWidth-float-) | يحدد عرض عنصر المخطط كنسبة من عرض المخطط. |
| [getHeight()](#getHeight--) | يحدد ارتفاع عنصر المخطط كنسبة من ارتفاع المخطط. |
| [setHeight(float value)](#setHeight-float-) | يحدد ارتفاع عنصر المخطط كنسبة من ارتفاع المخطط. |
| [getRight()](#getRight--) | يحصل على الجانب الأيمن لعنصر المخطط كنسبة من عرض المخطط. |
| [getBottom()](#getBottom--) | يحصل على أعلى عنصر المخطط كنسبة من ارتفاع المخطط. |
### getX() {#getX--}
```
public abstract float getX()
```

يحدد موضع x (اليسار) لعنصر المخطط كنسبة من عرض المخطط. قابل للقراءة/الكتابة float.

**الإرجاع:**
float
### setX(float value) {#setX-float-}
```
public abstract void setX(float value)
```

يحدد موضع x (اليسار) لعنصر المخطط كنسبة من عرض المخطط. قابل للقراءة/الكتابة float.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getY() {#getY--}
```
public abstract float getY()
```

يحدد أعلى عنصر المخطط كنسبة من ارتفاع المخطط. قابل للقراءة/الكتابة float.

**الإرجاع:**
float
### setY(float value) {#setY-float-}
```
public abstract void setY(float value)
```

يحدد أعلى عنصر المخطط كنسبة من ارتفاع المخطط. قابل للقراءة/الكتابة float.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getWidth() {#getWidth--}
```
public abstract float getWidth()
```

يحدد عرض عنصر المخطط كنسبة من عرض المخطط. قابل للقراءة/الكتابة float.

**الإرجاع:**
float
### setWidth(float value) {#setWidth-float-}
```
public abstract void setWidth(float value)
```

يحدد عرض عنصر المخطط كنسبة من عرض المخطط. قابل للقراءة/الكتابة float.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getHeight() {#getHeight--}
```
public abstract float getHeight()
```

يحدد ارتفاع عنصر المخطط كنسبة من ارتفاع المخطط. قابل للقراءة/الكتابة float.

**الإرجاع:**
float
### setHeight(float value) {#setHeight-float-}
```
public abstract void setHeight(float value)
```

يحدد ارتفاع عنصر المخطط كنسبة من ارتفاع المخطط. قابل للقراءة/الكتابة float.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getRight() {#getRight--}
```
public abstract float getRight()
```

يحصل على الجانب الأيمن لعنصر المخطط كنسبة من عرض المخطط. للقراءة فقط float.

**الإرجاع:**
float
### getBottom() {#getBottom--}
```
public abstract float getBottom()
```

يحصل على أعلى عنصر المخطط كنسبة من ارتفاع المخطط. للقراءة فقط float.

**الإرجاع:**
float