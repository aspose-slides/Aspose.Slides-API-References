---
title: Legend
second_title: مرجع Aspose.Slides لواجهة برمجة تطبيقات Java
description: يمثل خصائص وسيلة إيضاح المخططات.
type: docs
url: /ar/com.aspose.slides/legend/
---
**الوراثة:**  
java.lang.Object, com.aspose.slides.DomObject

**جميع الواجهات المنفذة:**  
[com.aspose.slides.ILegend](../../com.aspose.slides/ilegend)  
```
public class Legend extends DomObject<Chart> implements ILegend
```

يمثل خصائص وسيلة إيضاح المخطط.
## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getX()](#getX--) | إرجاع أو ضبط إحداثي X للوسيلة كنسبة من عرض المخطط. |
| [setX(float value)](#setX-float-) | إرجاع أو ضبط إحداثي X للوسيلة كنسبة من عرض المخطط. |
| [getY()](#getY--) | إرجاع أو ضبط إحداثي Y للوسيلة كنسبة من ارتفاع المخطط. |
| [setY(float value)](#setY-float-) | إرجاع أو ضبط إحداثي Y للوسيلة كنسبة من ارتفاع المخطط. |
| [getWidth()](#getWidth--) | إرجاع أو ضبط عرض الوسيلة كنسبة من عرض المخطط. |
| [setWidth(float value)](#setWidth-float-) | إرجاع أو ضبط عرض الوسيلة كنسبة من عرض المخطط. |
| [getHeight()](#getHeight--) | إرجاع أو ضبط ارتفاع الوسيلة كنسبة من ارتفاع المخطط. |
| [setHeight(float value)](#setHeight-float-) | إرجاع أو ضبط ارتفاع الوسيلة كنسبة من ارتفاع المخطط. |
| [getRight()](#getRight--) | يمين. |
| [getBottom()](#getBottom--) | أسفل. |
| [getOverlay()](#getOverlay--) | تحديد ما إذا كان يسمح لعناصر المخطط الأخرى بتغطية الوسيلة. |
| [setOverlay(boolean value)](#setOverlay-boolean-) | تحديد ما إذا كان يسمح لعناصر المخطط الأخرى بتغطية الوسيلة. |
| [getTextFormat()](#getTextFormat--) | تنسيق النص. |
| [getPosition()](#getPosition--) | تحديد موضع الوسيلة على المخطط. |
| [setPosition(int value)](#setPosition-int-) | تحديد موضع الوسيلة على المخطط. |
| [getFormat()](#getFormat--) | إرجاع تنسيق الوسيلة. |
| [getChart()](#getChart--) | إرجاع المخطط. |
| [getEntries()](#getEntries--) | إحضار مدخلات الوسيلة. |
| [getActualX()](#getActualX--) | تحديد الموقع الفعلي لإحداثي X (اليسار) لعنصر المخطط نسبة إلى الزاوية اليسرى العليا للمخطط. |
| [getActualY()](#getActualY--) | تحديد أعلى العنصر الفعلي للمخطط نسبة إلى الزاوية اليسرى العليا للمخطط. |
| [getActualWidth()](#getActualWidth--) | تحديد العرض الفعلي لعنصر المخطط. |
| [getActualHeight()](#getActualHeight--) | تحديد الارتفاع الفعلي لعنصر المخطط. |
| [getSlide()](#getSlide--) | إرجاع الشريحة الأم لـ FillFormat. |
| [getPresentation()](#getPresentation--) | إرجاع العرض الأم لـ FillFormat. |

### getX() {#getX--}
```
public final float getX()
```

إرجاع أو ضبط إحداثي X للوسيلة كنسبة من عرض المخطط. قراءة/كتابة float.

**الإرجاع:**  
float
### setX(float value) {#setX-float-}
```
public final void setX(float value)
```

إرجاع أو ضبط إحداثي X للوسيلة كنسبة من عرض المخطط. قراءة/كتابة float.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getY() {#getY--}
```
public final float getY()
```

إرجاع أو ضبط إحداثي Y للوسيلة كنسبة من ارتفاع المخطط. قراءة/كتابة float.

**الإرجاع:**  
float
### setY(float value) {#setY-float-}
```
public final void setY(float value)
```

إرجاع أو ضبط إحداثي Y للوسيلة كنسبة من ارتفاع المخطط. قراءة/كتابة float.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getWidth() {#getWidth--}
```
public final float getWidth()
```

إرجاع أو ضبط عرض الوسيلة كنسبة من عرض المخطط. قراءة/كتابة float.

**الإرجاع:**  
float
### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```

إرجاع أو ضبط عرض الوسيلة كنسبة من عرض المخطط. قراءة/كتابة float.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getHeight() {#getHeight--}
```
public final float getHeight()
```

إرجاع أو ضبط ارتفاع الوسيلة كنسبة من ارتفاع المخطط. قراءة/كتابة float.

**الإرجاع:**  
float
### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```

إرجاع أو ضبط ارتفاع الوسيلة كنسبة من ارتفاع المخطط. قراءة/كتابة float.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getRight() {#getRight--}
```
public final float getRight()
```

يمين. قراءة فقط float.

**الإرجاع:**  
float
### getBottom() {#getBottom--}
```
public final float getBottom()
```

أسفل. قراءة فقط float.

**الإرجاع:**  
float
### getOverlay() {#getOverlay--}
```
public final boolean getOverlay()
```

تحديد ما إذا كان يسمح لعناصر المخطط الأخرى بتغطية الوسيلة. قراءة/كتابة boolean.

**الإرجاع:**  
boolean
### setOverlay(boolean value) {#setOverlay-boolean-}
```
public final void setOverlay(boolean value)
```

تحديد ما إذا كان يسمح لعناصر المخطط الأخرى بتغطية الوسيلة. قراءة/كتابة boolean.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getTextFormat() {#getTextFormat--}
```
public final IChartTextFormat getTextFormat()
```

تنسيق النص. قراءة فقط [IChartTextFormat](../../com.aspose.slides/icharttextformat).

**الإرجاع:**  
[IChartTextFormat](../../com.aspose.slides/icharttextformat)
### getPosition() {#getPosition--}
```
public final int getPosition()
```

تحديد موضع الوسيلة على المخطط. القيم غير NaN للخصائص X و Y و Width و Height تتجاوز تأثير هذه الخاصية. قراءة/كتابة [LegendPositionType](../../com.aspose.slides/legendpositiontype).

**الإرجاع:**  
int
### setPosition(int value) {#setPosition-int-}
```
public final void setPosition(int value)
```

تحديد موضع الوسيلة على المخطط. القيم غير NaN للخصائص X و Y و Width و Height تتجاوز تأثير هذه الخاصية. قراءة/كتابة [LegendPositionType](../../com.aspose.slides/legendpositiontype).

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | int |  |

### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

إرجاع تنسيق الوسيلة. قراءة فقط [IFormat](../../com.aspose.slides/iformat).

**الإرجاع:**  
[IFormat](../../com.aspose.slides/iformat)
### getChart() {#getChart--}
```
public final IChart getChart()
```

إرجاع المخطط. قراءة فقط [IChart](../../com.aspose.slides/ichart).

**الإرجاع:**  
[IChart](../../com.aspose.slides/ichart)
### getEntries() {#getEntries--}
```
public final ILegendEntryCollection getEntries()
```

إحضار مدخلات الوسيلة. قراءة فقط [ILegendEntryCollection](../../com.aspose.slides/ilegendentrycollection).

**الإرجاع:**  
[ILegendEntryCollection](../../com.aspose.slides/ilegendentrycollection)
### getActualX() {#getActualX--}
```
public final float getActualX()
```

تحديد الموقع الفعلي لإحداثي X (اليسار) لعنصر المخطط نسبة إلى الزاوية اليسرى العليا للمخطط. استدعاء الطريقة IChart.validateChartLayout() قبل للحصول على القيم الفعلية. قراءة float.

**الإرجاع:**  
float
### getActualY() {#getActualY--}
```
public final float getActualY()
```

تحديد أعلى العنصر الفعلي للمخطط نسبة إلى الزاوية اليسرى العليا للمخطط. استدعاء الطريقة IChart.validateChartLayout() قبل للحصول على القيم الفعلية. قراءة float.

**الإرجاع:**  
float
### getActualWidth() {#getActualWidth--}
```
public final float getActualWidth()
```

تحديد العرض الفعلي لعنصر المخطط. استدعاء الطريقة IChart.validateChartLayout() قبل للحصول على القيم الفعلية. قراءة float.

**الإرجاع:**  
float
### getActualHeight() {#getActualHeight--}
```
public final float getActualHeight()
```

تحديد الارتفاع الفعلي لعنصر المخطط. استدعاء الطريقة IChart.validateChartLayout() قبل للحصول على القيم الفعلية. قراءة float.

**الإرجاع:**  
float
### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

إرجاع الشريحة الأم لـ FillFormat. قراءة فقط [BaseSlide](../../com.aspose.slides/baseslide).

**الإرجاع:**  
[IBaseSlide](../../com.aspose.slides/ibaseslide)
### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

إرجاع العرض الأم لـ FillFormat. قراءة فقط [IPresentation](../../com.aspose.slides/ipresentation).

**الإرجاع:**  
[IPresentation](../../com.aspose.slides/ipresentation)