---
title: IBrightnessContrastEffectiveData
second_title: Aspose.Slides لواجهة برمجة التطبيقات لجافا
description: كائن غير قابل للتغيير يمثل تأثير BrightnessContrast.
type: docs
url: /ar/com.aspose.slides/ibrightnesscontrasteffectivedata/
---
**كل الواجهات المنفذة:**
[com.aspose.slides.IEffectEffectiveData](../../com.aspose.slides/ieffecteffectivedata)
```
public interface IBrightnessContrastEffectiveData extends IEffectEffectiveData
```

كائن غير قابل للتغيير يمثل تأثير BrightnessContrast. يضبط السطوع والتباين
## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getBrightness()](#getBrightness--) | يعيد السطوع. |
| [getContrast()](#getContrast--) | يعيد التباين. |
### getBrightness() {#getBrightness--}
```
public abstract float getBrightness()
```

يعيد السطوع. قراءة فقط float .

**الإرجاع:**  
float
### getContrast() {#getContrast--}
```
public abstract float getContrast()
```

يعيد التباين. قراءة فقط float .

**الإرجاع:**  
float