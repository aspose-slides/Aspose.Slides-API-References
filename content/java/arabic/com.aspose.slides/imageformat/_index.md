---
title: ImageFormat
second_title: مرجع API Aspose.Slides للغة Java
description: يمثل تنسيق ملف الصورة.
type: docs
url: /ar/com.aspose.slides/imageformat/
---
**الوراثة:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class ImageFormat extends System.Enum
```

يمثل تنسيق ملف الصورة.
## الحقول

| الحقل | الوصف |
| --- | --- |
| [MemoryBmp](#MemoryBmp) | يمثل تنسيق البت ماب في الذاكرة. |
| [Bmp](#Bmp) | يمثل تنسيق صورة البت ماب (BMP). |
| [Emf](#Emf) | يمثل تنسيق ملف الميتا المحسّن (EMF). |
| [Wmf](#Wmf) | يمثل تنسيق ملف الميتا ويندوز (WMF). |
| [Gif](#Gif) | يمثل تنسيق تبادل الرسومات (GIF). |
| [Jpeg](#Jpeg) | يمثل تنسيق مجموعة خبراء التصوير الفوتوغرافي المشتركة (JPEG). |
| [Png](#Png) | يمثل تنسيق الرسوميات الشبكية القابلة للنقل (PNG) الخاص بـ W3C. |
| [Tiff](#Tiff) | يمثل تنسيق ملف الصورة المعلم (TIFF). |
| [Exif](#Exif) | يمثل تنسيق ملف الصورة القابل للتبادل (Exif). |
| [Icon](#Icon) | يمثل تنسيق أيقونة ويندوز. |
### MemoryBmp {#MemoryBmp}
```
public static final int MemoryBmp
```

يمثل تنسيق البت ماب في الذاكرة.

### Bmp {#Bmp}
```
public static final int Bmp
```

يمثل تنسيق صورة البت ماب (BMP).

### Emf {#Emf}
```
public static final int Emf
```

يمثل تنسيق ملف الميتا المحسّن (EMF).

### Wmf {#Wmf}
```
public static final int Wmf
```

يمثل تنسيق ملف الميتا ويندوز (WMF).

### Gif {#Gif}
```
public static final int Gif
```

يمثل تنسيق تبادل الرسومات (GIF).

### Jpeg {#Jpeg}
```
public static final int Jpeg
```

يمثل تنسيق مجموعة خبراء التصوير الفوتوغرافي المشتركة (JPEG).

### Png {#Png}
```
public static final int Png
```

يمثل تنسيق الرسوميات الشبكية القابلة للنقل (PNG) الخاص بـ W3C.

### Tiff {#Tiff}
```
public static final int Tiff
```

يمثل تنسيق ملف الصورة المعلم (TIFF).

### Exif {#Exif}
```
public static final int Exif
```

يمثل تنسيق ملف الصورة القابل للتبادل (Exif).

### Icon {#Icon}
```
public static final int Icon
```

يمثل تنسيق أيقونة ويندوز.