---
title: ChartPlotArea
second_title: مرجع API لـ Aspose.Slides للـ Java
description: يمثل المستطيل الذي يجب رسم المخطط فيه.
type: docs
url: /ar/com.aspose.slides/chartplotarea/
---
**الوراثة:**
java.lang.Object, com.aspose.slides.DomObject

**جميع الواجهات المنفذة:**
[com.aspose.slides.IChartPlotArea](../../com.aspose.slides/ichartplotarea)
```
public class ChartPlotArea extends DomObject<Chart> implements IChartPlotArea
```

يمثل المستطيل حيث يجب رسم الـChart.
## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getFormat()](#getFormat--) | يرجع تنسيق مساحة الرسم. |
| [getX()](#getX--) | يرجع أو يحدد إحداثي x للزاوية العلوية اليسرى لمربع الحدود الخاص بمنطقة الرسم كنسبة من عرض الـChart (من 0 إلى 1). |
| [setX(float value)](#setX-float-) | يرجع أو يحدد إحداثي x للزاوية العلوية اليسرى لمربع الحدود الخاص بمنطقة الرسم كنسبة من عرض الـChart (من 0 إلى 1). |
| [getY()](#getY--) | يرجع أو يحدد إحداثي y للزاوية العلوية اليسرى لمربع الحدود الخاص بمنطقة الرسم كنسبة من ارتفاع الـChart (من 0 إلى 1). |
| [setY(float value)](#setY-float-) | يرجع أو يحدد إحداثي y للزاوية العلوية اليسرى لمربع الحدود الخاص بمنطقة الرسم كنسبة من ارتفاع الـChart (من 0 إلى 1). |
| [getWidth()](#getWidth--) | يرجع أو يحدد عرض مربع الحدود الخاص بمنطقة الرسم كنسبة من عرض الـChart (من 0 إلى 1). |
| [setWidth(float value)](#setWidth-float-) | يرجع أو يحدد عرض مربع الحدود الخاص بمنطقة الرسم كنسبة من عرض الـChart (من 0 إلى 1). |
| [getHeight()](#getHeight--) | يرجع أو يحدد ارتفاع مربع الحدود الخاص بمنطقة الرسم كنسبة من ارتفاع الـChart (من 0 إلى 1). |
| [setHeight(float value)](#setHeight-float-) | يرجع أو يحدد ارتفاع مربع الحدود الخاص بمنطقة الرسم كنسبة من ارتفاع الـChart (من 0 إلى 1). |
| [getRight()](#getRight--) | Right. |
| [getBottom()](#getBottom--) | Bottom. |
| [getChart()](#getChart--) | Chart. |
| [isLocationAutocalculated()](#isLocationAutocalculated--) | Defines how location should be calculated: true – calculated automatically; defined by the X, Y, Width, Height properties. |
| [getLayoutTargetType()](#getLayoutTargetType--) | If layout of the plot area defined manually this property specifies whether to layout the plot area by its inside (not including axis and axis labels) or outside (including axis and axis labels). |
| [setLayoutTargetType(int value)](#setLayoutTargetType-int-) | If layout of the plot area defined manually this property specifies whether to layout the plot area by its inside (not including axis and axis labels) or outside (including axis and axis labels). |
| [getActualX()](#getActualX--) | Specifies actual x location (left) of the chart element relative to the left top corner of the chart. |
| [getActualY()](#getActualY--) | Specifies actual top of the chart element relative to the left top corner of the chart. |
| [getActualWidth()](#getActualWidth--) | Specifies actual width of the chart element. |
| [getActualHeight()](#getActualHeight--) | Specifies actual height of the chart element. |
| [getSlide()](#getSlide--) | Returns the parent slide of a FillFormat. |
| [getPresentation()](#getPresentation--) | Returns the parent presentation of a FillFormat. |

### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

يرجع تنسيق مساحة الرسم. للقراءة فقط [IFormat](../../com.aspose.slides/iformat).

**الإرجاع:**
[IFormat](../../com.aspose.slides/iformat)
### getX() {#getX--}
```
public final float getX()
```

يرجع أو يحدد إحداثي x للزاوية العلوية اليسرى لمربع الحدود الخاص بمنطقة الرسم كنسبة من عرض الـChart (من 0 إلى 1). float للقراءة والكتابة.

**الإرجاع:**
float
### setX(float value) {#setX-float-}
```
public final void setX(float value)
```

يرجع أو يحدد إحداثي x للزاوية العلوية اليسرى لمربع الحدود الخاص بمنطقة الرسم كنسبة من عرض الـChart (من 0 إلى 1). float للقراءة والكتابة.

**المعلمات:**
| المعلمة | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getY() {#getY--}
```
public final float getY()
```

يرجع أو يحدد إحداثي y للزاوية العلوية اليسرى لمربع الحدود الخاص بمنطقة الرسم كنسبة من ارتفاع الـChart (من 0 إلى 1). float للقراءة والكتابة.

**الإرجاع:**
float
### setY(float value) {#setY-float-}
```
public final void setY(float value)
```

يرجع أو يحدد إحداثي y للزاوية العلوية اليسرى لمربع الحدود الخاص بمنطقة الرسم كنسبة من ارتفاع الـChart (من 0 إلى 1). float للقراءة والكتابة.

**المعلمات:**
| المعلمة | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getWidth() {#getWidth--}
```
public final float getWidth()
```

يرجع أو يحدد عرض مربع الحدود الخاص بمنطقة الرسم كنسبة من عرض الـChart (من 0 إلى 1). float للقراءة والكتابة.

**الإرجاع:**
float
### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```

يرجع أو يحدد عرض مربع الحدود الخاص بمنطقة الرسم كنسبة من عرض الـChart (من 0 إلى 1). float للقراءة والكتابة.

**المعلمات:**
| المعلمة | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getHeight() {#getHeight--}
```
public final float getHeight()
```

يرجع أو يحدد ارتفاع مربع الحدود الخاص بمنطقة الرسم كنسبة من ارتفاع الـChart (من 0 إلى 1). float للقراءة والكتابة.

**الإرجاع:**
float
### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```

يرجع أو يحدد ارتفاع مربع الحدود الخاص بمنطقة الرسم كنسبة من ارتفاع الـChart (من 0 إلى 1). float للقراءة والكتابة.

**المعلمات:**
| المعلمة | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getRight() {#getRight--}
```
public final float getRight()
```

Right. float للقراءة فقط.

**الإرجاع:**
float
### getBottom() {#getBottom--}
```
public final float getBottom()
```

Bottom. float للقراءة فقط.

**الإرجاع:**
float
### getChart() {#getChart--}
```
public final IChart getChart()
```

Chart. للقراءة فقط [IChart](../../com.aspose.slides/ichart).

**الإرجاع:**
[IChart](../../com.aspose.slides/ichart)
### isLocationAutocalculated() {#isLocationAutocalculated--}
```
public final boolean isLocationAutocalculated()
```

Defines how location should be calculated: true – calculated automatically; defined by the X, Y, Width, Height properties. boolean للقراءة فقط.

**الإرجاع:**
boolean
### getLayoutTargetType() {#getLayoutTargetType--}
```
public final int getLayoutTargetType()
```

If layout of the plot area defined manually this property specifies whether to layout the plot area by its inside (not including axis and axis labels) or outside (including axis and axis labels). float للقراءة والكتابة [LayoutTargetType](../../com.aspose.slides/layouttargettype)(\#getLayoutTargetType.getLayoutTargetType/\#setLayoutTargetType(int).setLayoutTargetType(int)).

--------------------

> ```
> Presentation presentation = new Presentation();
>   try
>   {
>       ISlide slide = presentation.getSlides().get_Item(0);
>       IChart chart = slide.getShapes().addChart(ChartType.ClusteredColumn, 20, 100, 600, 400);
>       chart.getPlotArea().setX(0.2f);
>       chart.getPlotArea().setY(0.2f);
>       chart.getPlotArea().setWidth(0.7f);
>       chart.getPlotArea().setHeight(0.7f);
>       chart.getPlotArea().setLayoutTargetType(LayoutTargetType.Inner);
>       ...
>   } finally {
>       if (presentation != null) presentation.dispose();
>   }
> ```

**الإرجاع:**
int
### setLayoutTargetType(int value) {#setLayoutTargetType-int-}
```
public final void setLayoutTargetType(int value)
```

If layout of the plot area defined manually this property specifies whether to layout the plot area by its inside (not including axis and axis labels) or outside (including axis and axis labels). float للقراءة والكتابة [LayoutTargetType](../../com.aspose.slides/layouttargettype)(\#getLayoutTargetType.getLayoutTargetType/\#setLayoutTargetType(int).setLayoutTargetType(int)).

--------------------

> ```
> Presentation presentation = new Presentation();
>   try
>   {
>       ISlide slide = presentation.getSlides().get_Item(0);
>       IChart chart = slide.getShapes().addChart(ChartType.ClusteredColumn, 20, 100, 600, 400);
>       chart.getPlotArea().setX(0.2f);
>       chart.getPlotArea().setY(0.2f);
>       chart.getPlotArea().setWidth(0.7f);
>       chart.getPlotArea().setHeight(0.7f);
>       chart.getPlotArea().setLayoutTargetType(LayoutTargetType.Inner);
>       ...
>   } finally {
>       if (presentation != null) presentation.dispose();
>   }
> ```

**المعلمات:**
| المعلمة | النوع | الوصف |
| --- | --- | --- |
| value | int |  |

### getActualX() {#getActualX--}
```
public final float getActualX()
```

Specifies actual x location (left) of the chart element relative to the left top corner of the chart. Call method IChart.ValidateChartLayout() before to get actual values. float للقراءة.

**الإرجاع:**
float
### getActualY() {#getActualY--}
```
public final float getActualY()
```

Specifies actual top of the chart element relative to the left top corner of the chart. Call method IChart.ValidateChartLayout() before to get actual values. float للقراءة.

**الإرجاع:**
float
### getActualWidth() {#getActualWidth--}
```
public final float getActualWidth()
```

Specifies actual width of the chart element. Call method IChart.ValidateChartLayout() before to get actual values. float للقراءة.

**الإرجاع:**
float
### getActualHeight() {#getActualHeight--}
```
public final float getActualHeight()
```

Specifies actual height of the chart element. Call method IChart.ValidateChartLayout() before to get actual values. float للقراءة.

**الإرجاع:**
float
### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

Returns the parent slide of a FillFormat. float للقراءة فقط [BaseSlide](../../com.aspose.slides/baseslide).

**الإرجاع:**
[IBaseSlide](../../com.aspose.slides/ibaseslide)
### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

Returns the parent presentation of a FillFormat. float للقراءة فقط [IPresentation](../../com.aspose.slides/ipresentation).

**الإرجاع:**
[IPresentation](../../com.aspose.slides/ipresentation)