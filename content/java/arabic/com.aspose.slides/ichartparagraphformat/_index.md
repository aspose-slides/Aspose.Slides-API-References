---
title: IChartParagraphFormat
second_title: Aspose.Slides for Java API Reference
description: يمثل خصائص تنسيق الفقرة في مخطط.
type: docs
url: /ar/com.aspose.slides/ichartparagraphformat/
---```
public interface IChartParagraphFormat
```

يمثل خصائص تنسيق الفقرة في مخطط.
## الطرق

| طريقة | الوصف |
| --- | --- |
| [getAlignment()](#getAlignment--) | تُرجع أو تُعيّن محاذاة النص في الفقرة. |
| [setAlignment(int value)](#setAlignment-int-) | تُرجع أو تُعيّن محاذاة النص في الفقرة. |
| [getSpaceWithin()](#getSpaceWithin--) | تُرجع أو تُعيّن مقدار المسافة بين الخطوط القاعدية في الفقرة. |
| [setSpaceWithin(float value)](#setSpaceWithin-float-) | تُرجع أو تُعيّن مقدار المسافة بين الخطوط القاعدية في الفقرة. |
| [getSpaceBefore()](#getSpaceBefore--) | تُرجع أو تُعيّن مقدار المسافة قبل السطر الأول في الفقرة. |
| [setSpaceBefore(float value)](#setSpaceBefore-float-) | تُرجع أو تُعيّن مقدار المسافة قبل السطر الأول في الفقرة. |
| [getSpaceAfter()](#getSpaceAfter--) | تُرجع أو تُعيّن مقدار المسافة بعد السطر الأخير في الفقرة. |
| [setSpaceAfter(float value)](#setSpaceAfter-float-) | تُرجع أو تُعيّن مقدار المسافة بعد السطر الأخير في الفقرة. |
| [getEastAsianLineBreak()](#getEastAsianLineBreak--) | تحدد ما إذا كان فاصل السطر الآسيوي الشرقي يُستخدم في الفقرة. |
| [setEastAsianLineBreak(byte value)](#setEastAsianLineBreak-byte-) | تحدد ما إذا كان فاصل السطر الآسيوي الشرقي يُستخدم في الفقرة. |
| [getRightToLeft()](#getRightToLeft--) | تحدد ما إذا كان الكتابة من اليمين إلى اليسار مستخدمة في الفقرة. |
| [setRightToLeft(byte value)](#setRightToLeft-byte-) | تحدد ما إذا كان الكتابة من اليمين إلى اليسار مستخدمة في الفقرة. |
| [getLatinLineBreak()](#getLatinLineBreak--) | تحدد ما إذا كان فاصل السطر اللاتيني يُستخدم في الفقرة. |
| [setLatinLineBreak(byte value)](#setLatinLineBreak-byte-) | تحدد ما إذا كان فاصل السطر اللاتيني يُستخدم في الفقرة. |
| [getHangingPunctuation()](#getHangingPunctuation--) | تحدد ما إذا كانت علامات الترقيم المتدلية تُستخدم في الفقرة. |
| [setHangingPunctuation(byte value)](#setHangingPunctuation-byte-) | تحدد ما إذا كانت علامات الترقيم المتدلية تُستخدم في الفقرة. |
| [getMarginLeft()](#getMarginLeft--) | تُرجع أو تُعيّن الهامش الأيسر في الفقرة. |
| [setMarginLeft(float value)](#setMarginLeft-float-) | تُرجع أو تُعيّن الهامش الأيسر في الفقرة. |
| [getMarginRight()](#getMarginRight--) | تُرجع أو تُعيّن الهامش الأيمن في الفقرة. |
| [setMarginRight(float value)](#setMarginRight-float-) | تُرجع أو تُعيّن الهامش الأيمن في الفقرة. |
| [getIndent()](#getIndent--) | تُرجع أو تُعيّن إزاحة السطر الأول/الإزاحة المتدلية للفقرة. |
| [setIndent(float value)](#setIndent-float-) | تُرجع أو تُعيّن إزاحة السطر الأول/الإزاحة المتدلية للفقرة. |
| [getDefaultTabSize()](#getDefaultTabSize--) | تُرجع أو تُعيّن حجم التبويب الافتراضي. |
| [setDefaultTabSize(float value)](#setDefaultTabSize-float-) | تُرجع أو تُعيّن حجم التبويب الافتراضي. |
| [getTabs()](#getTabs--) | تُرجع علامات التبويب للفقرة. |
| [getFontAlignment()](#getFontAlignment--) | تُرجع أو تُعيّن محاذاة الخط في الفقرة. |
| [setFontAlignment(int value)](#setFontAlignment-int-) | تُرجع أو تُعيّن محاذاة الخط في الفقرة. |

### getAlignment() {#getAlignment--}
```
public abstract int getAlignment()
```

تُرجع أو تُعيّن محاذاة النص في الفقرة. قراءة/كتابة [TextAlignment](../../com.aspose.slides/textalignment).

**القيمة المرجعة:**
int

### setAlignment(int value) {#setAlignment-int-}
```
public abstract void setAlignment(int value)
```

تُرجع أو تُعيّن محاذاة النص في الفقرة. قراءة/كتابة [TextAlignment](../../com.aspose.slides/textalignment).

**المعلمات:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| value | int |  |

### getSpaceWithin() {#getSpaceWithin--}
```
public abstract float getSpaceWithin()
```

تُرجع أو تُعيّن مقدار المسافة بين الخطوط القاعدية في الفقرة. قراءة/كتابة float.

**القيمة المرجعة:**
float

### setSpaceWithin(float value) {#setSpaceWithin-float-}
```
public abstract void setSpaceWithin(float value)
```

تُرجع أو تُعيّن مقدار المسافة بين الخطوط القاعدية في الفقرة. قراءة/كتابة float.

**المعلمات:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getSpaceBefore() {#getSpaceBefore--}
```
public abstract float getSpaceBefore()
```

تُرجع أو تُعيّن مقدار المسافة قبل السطر الأول في الفقرة. قراءة/كتابة float.

**القيمة المرجعة:**
float

### setSpaceBefore(float value) {#setSpaceBefore-float-}
```
public abstract void setSpaceBefore(float value)
```

تُرجع أو تُعيّن مقدار المسافة قبل السطر الأول في الفقرة. قراءة/كتابة float.

**المعلمات:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getSpaceAfter() {#getSpaceAfter--}
```
public abstract float getSpaceAfter()
```

تُرجع أو تُعيّن مقدار المسافة بعد السطر الأخير في الفقرة. قراءة/كتابة float.

**القيمة المرجعة:**
float

### setSpaceAfter(float value) {#setSpaceAfter-float-}
```
public abstract void setSpaceAfter(float value)
```

تُرجع أو تُعيّن مقدار المسافة بعد السطر الأخير في الفقرة. قراءة/كتابة float.

**المعلمات:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getEastAsianLineBreak() {#getEastAsianLineBreak--}
```
public abstract byte getEastAsianLineBreak()
```

تحدد ما إذا كان فاصل السطر الآسيوي الشرقي يُستخدم في الفقرة. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**القيمة المرجعة:**
byte

### setEastAsianLineBreak(byte value) {#setEastAsianLineBreak-byte-}
```
public abstract void setEastAsianLineBreak(byte value)
```

تحدد ما إذا كان فاصل السطر الآسيوي الشرقي يُستخدم في الفقرة. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**المعلمات:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| value | byte |  |

### getRightToLeft() {#getRightToLeft--}
```
public abstract byte getRightToLeft()
```

تحدد ما إذا كان الكتابة من اليمين إلى اليسار مستخدمة في الفقرة. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**القيمة المرجعة:**
byte

### setRightToLeft(byte value) {#setRightToLeft-byte-}
```
public abstract void setRightToLeft(byte value)
```

تحدد ما إذا كان الكتابة من اليمين إلى اليسار مستخدمة في الفقرة. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**المعلمات:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| value | byte |  |

### getLatinLineBreak() {#getLatinLineBreak--}
```
public abstract byte getLatinLineBreak()
```

تحدد ما إذا كان فاصل السطر اللاتيني يُستخدم في الفقرة. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**القيمة المرجعة:**
byte

### setLatinLineBreak(byte value) {#setLatinLineBreak-byte-}
```
public abstract void setLatinLineBreak(byte value)
```

تحدد ما إذا كان فاصل السطر اللاتيني يُستخدم في الفقرة. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**المعلمات:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| value | byte |  |

### getHangingPunctuation() {#getHangingPunctuation--}
```
public abstract byte getHangingPunctuation()
```

تحدد ما إذا كانت علامات الترقيم المتدلية تُستخدم في الفقرة. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**القيمة المرجعة:**
byte

### setHangingPunctuation(byte value) {#setHangingPunctuation-byte-}
```
public abstract void setHangingPunctuation(byte value)
```

تحدد ما إذا كانت علامات الترقيم المتدلية تُستخدم في الفقرة. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**المعلمات:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| value | byte |  |

### getMarginLeft() {#getMarginLeft--}
```
public abstract float getMarginLeft()
```

تُرجع أو تُعيّن الهامش الأيسر في الفقرة. قراءة/كتابة float.

**القيمة المرجعة:**
float

### setMarginLeft(float value) {#setMarginLeft-float-}
```
public abstract void setMarginLeft(float value)
```

تُرجع أو تُعيّن الهامش الأيسر في الفقرة. قراءة/كتابة float.

**المعلمات:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getMarginRight() {#getMarginRight--}
```
public abstract float getMarginRight()
```

تُرجع أو تُعيّن الهامش الأيمن في الفقرة. قراءة/كتابة float.

**القيمة المرجعة:**
float

### setMarginRight(float value) {#setMarginRight-float-}
```
public abstract void setMarginRight(float value)
```

تُرجع أو تُعيّن الهامش الأيمن في الفقرة. قراءة/كتابة float.

**المعلمات:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getIndent() {#getIndent--}
```
public abstract float getIndent()
```

تُرجع أو تُعيّن إزاحة السطر الأول/الإزاحة المتدلية للفقرة. يمكن تعريف الإزاحة المتدلية بقيم سالبة. قراءة/كتابة float.

**القيمة المرجعة:**
float

### setIndent(float value) {#setIndent-float-}
```
public abstract void setIndent(float value)
```

تُرجع أو تُعيّن إزاحة السطر الأول/الإزاحة المتدلية للفقرة. يمكن تعريف الإزاحة المتدلية بقيم سالبة. قراءة/كتابة float.

**المعلمات:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getDefaultTabSize() {#getDefaultTabSize--}
```
public abstract float getDefaultTabSize()
```

تُرجع أو تُعيّن حجم التبويب الافتراضي. قراءة/كتابة float.

**القيمة المرجعة:**
float

### setDefaultTabSize(float value) {#setDefaultTabSize-float-}
```
public abstract void setDefaultTabSize(float value)
```

تُرجع أو تُعيّن حجم التبويب الافتراضي. قراءة/كتابة float.

**المعلمات:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getTabs() {#getTabs--}
```
public abstract ITabCollection getTabs()
```

تُرجع علامات التبويب للفقرة. قراءة فقط [ITabCollection](../../com.aspose.slides/itabcollection).

**القيمة المرجعة:**
[ITabCollection](../../com.aspose.slides/itabcollection)

### getFontAlignment() {#getFontAlignment--}
```
public abstract int getFontAlignment()
```

تُرجع أو تُعيّن محاذاة الخط في الفقرة. قراءة/كتابة [FontAlignment](../../com.aspose.slides/fontalignment).

**القيمة المرجعة:**
int

### setFontAlignment(int value) {#setFontAlignment-int-}
```
public abstract void setFontAlignment(int value)
```

تُرجع أو تُعيّن محاذاة الخط في الفقرة. قراءة/كتابة [FontAlignment](../../com.aspose.slides/fontalignment).

**المعلمات:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| value | int |  |