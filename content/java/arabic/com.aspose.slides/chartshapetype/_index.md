---
title: ChartShapeType
second_title: مرجع API لـ Aspose.Slides للغة Java
description: يمثل شكلاً للمخطط.
type: docs
url: /ar/com.aspose.slides/chartshapetype/
---
**الوراثة:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class ChartShapeType extends System.Enum
```

يمثل شكلًا للمخطط.
## الحقول

| الحقل | الوصف |
| --- | --- |
| [NotDefined](#NotDefined) | شكل المخطط غير معرف. |
| [Box](#Box) | يحدد أن يتم رسم المخطط بشكل صندوق. |
| [Cone](#Cone) | يحدد أن يتم رسم المخطط على شكل مخروط، يكون قاعدة المخروط على الأرض ونقطة المخروط في أعلى علامة البيانات. |
| [ConeToMax](#ConeToMax) | يحدد أن يتم رسم المخطط باستخدام مخاريط مقصوصة بحيث تكون نقطة المخروط هي القيمة القصوى للبيانات. |
| [Cylinder](#Cylinder) | يحدد أن يتم رسم المخطط على شكل أسطوانة. |
| [Pyramid](#Pyramid) | يحدد أن يتم رسم المخطط على شكل هرم مستطيل، تكون قاعدة الهرم على الأرض ونقطة الهرم في أعلى علامة البيانات. |
| [PyramidToMaximum](#PyramidToMaximum) | يحدد أن يتم رسم المخطط باستخدام مخاريط مقصوصة بحيث تكون نقطة المخروط هي القيمة القصوى للبيانات. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```

شكل المخطط غير معرف.

### Box {#Box}
```
public static final int Box
```

يحدد أن يتم رسم المخطط بشكل صندوق.

### Cone {#Cone}
```
public static final int Cone
```

يحدد أن يتم رسم المخطط على شكل مخروط، يكون قاعدة المخروط على الأرض ونقطة المخروط في أعلى علامة البيانات.

### ConeToMax {#ConeToMax}
```
public static final int ConeToMax
```

يحدد أن يتم رسم المخطط باستخدام مخاريط مقصوصة بحيث تكون نقطة المخروط هي القيمة القصوى للبيانات.

### Cylinder {#Cylinder}
```
public static final int Cylinder
```

يحدد أن يتم رسم المخطط على شكل أسطوانة.

### Pyramid {#Pyramid}
```
public static final int Pyramid
```

يحدد أن يتم رسم المخطط على شكل هرم مستطيل، تكون قاعدة الهرم على الأرض ونقطة الهرم في أعلى علامة البيانات.

### PyramidToMaximum {#PyramidToMaximum}
```
public static final int PyramidToMaximum
```

يحدد أن يتم رسم المخطط باستخدام مخاريط مقصوصة بحيث تكون نقطة المخروط هي القيمة القصوى للبيانات.