---
title: ChartTitle
second_title: مرجع Aspose.Slides لواجهة برمجة تطبيقات Java
description: يمثل خصائص عنوان المخطط.
type: docs
url: /ar/com.aspose.slides/charttitle/
---
**الوراثة:**  
java.lang.Object

**جميع الواجهات المطبقة:**  
[com.aspose.slides.IChartTitle](../../com.aspose.slides/icharttitle), com.aspose.slides.IDOMObject  
```
public class ChartTitle implements IChartTitle, IDOMObject
```

يمثل خصائص عنوان المخطط.

## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getX()](#getX--) | يعيد أو يعيّن إحداثي x للعنوان كنسبة من عرض المخطط. |
| [setX(float value)](#setX-float-) | يعيد أو يعيّن إحداثي x للعنوان كنسبة من عرض المخطط. |
| [getY()](#getY--) | يعيد أو يعيّن إحداثي y للعنوان كنسبة من ارتفاع المخطط. |
| [setY(float value)](#setY-float-) | يعيد أو يعيّن إحداثي y للعنوان كنسبة من ارتفاع المخطط. |
| [getWidth()](#getWidth--) | يعيد أو يعيّن عرض العنوان كنسبة من عرض المخطط. |
| [setWidth(float value)](#setWidth-float-) | يعيد أو يعيّن عرض العنوان كنسبة من عرض المخطط. |
| [getHeight()](#getHeight--) | يعيد أو يعيّن ارتفاع العنوان كنسبة من ارتفاع المخطط. |
| [setHeight(float value)](#setHeight-float-) | يعيد أو يعيّن ارتفاع العنوان كنسبة من ارتفاع المخطط. |
| [getRight()](#getRight--) | يمين. |
| [getBottom()](#getBottom--) | أسفل. |
| [getOverlay()](#getOverlay--) | يحدد ما إذا كان يُسمح لعناصر المخطط الأخرى بتغطية العنوان. |
| [setOverlay(boolean value)](#setOverlay-boolean-) | يحدد ما إذا كان يُسمح لعناصر المخطط الأخرى بتغطية العنوان. |
| [getFormat()](#getFormat--) | يعيد أنماط التعبئة والخط والتأثير للعنوان. |
| [addTextFrameForOverriding(String text)](#addTextFrameForOverriding-java.lang.String-) | تهيئة TextFrameForOverriding بالنص الموجود في المعامل "text". |
| [getTextFrameForOverriding()](#getTextFrameForOverriding--) | يمكن أن يحتوي على نص منسق غني. |
| [getTextFormat()](#getTextFormat--) | يعيد تنسيق النص. |
| [getActualX()](#getActualX--) | يحدد الموقع الفعلي لإحداثي x (يسار) لعنصر المخطط بالنسبة إلى الزاوية اليسرى العليا للمخطط. |
| [getActualY()](#getActualY--) | يحدد الجزء العلوي الفعلي لعنصر المخطط بالنسبة إلى الزاوية اليسرى العليا للمخطط. |
| [getActualWidth()](#getActualWidth--) | يحدد العرض الفعلي لعنصر المخطط. |
| [getActualHeight()](#getActualHeight--) | يحدد الارتفاع الفعلي لعنصر المخطط. |
| [getParent_Immediate()](#getParent-Immediate--) |  |
| [getChart()](#getChart--) | يعيد المخطط الأصلي. |
| [getSlide()](#getSlide--) | يعيد الشريحة الأصلية لـ FillFormat. |
| [getPresentation()](#getPresentation--) | يعيد العرض الأصلي لـ FillFormat. |

### getX() {#getX--}
```
public final float getX()
```

يعيد أو يعيّن إحداثي x للعنوان كنسبة من عرض المخطط. قراءة/كتابة float.

**القيمة المرتجعة:**  
float

### setX(float value) {#setX-float-}
```
public final void setX(float value)
```

يعيد أو يعيّن إحداثي x للعنوان كنسبة من عرض المخطط. قراءة/كتابة float.

**الوسائط:**  
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getY() {#getY--}
```
public final float getY()
```

يعيد أو يعيّن إحداثي y للعنوان كنسبة من ارتفاع المخطط. قراءة/كتابة float.

**القيمة المرتجعة:**  
float

### setY(float value) {#setY-float-}
```
public final void setY(float value)
```

يعيد أو يعيّن إحداثي y للعنوان كنسبة من ارتفاع المخطط. قراءة/كتابة float.

**الوسائط:**  
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getWidth() {#getWidth--}
```
public final float getWidth()
```

يعيد أو يعيّن عرض العنوان كنسبة من عرض المخطط. قراءة/كتابة float.

**القيمة المرتجعة:**  
float

### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```

يعيد أو يعيّن عرض العنوان كنسبة من عرض المخطط. قراءة/كتابة float.

**الوسائط:**  
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getHeight() {#getHeight--}
```
public final float getHeight()
```

يعيد أو يعيّن ارتفاع العنوان كنسبة من ارتفاع المخطط. قراءة/كتابة float.

**القيمة المرتجعة:**  
float

### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```

يعيد أو يعيّن ارتفاع العنوان كنسبة من ارتفاع المخطط. قراءة/كتابة float.

**الوسائط:**  
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getRight() {#getRight--}
```
public final float getRight()
```

يمين. قراءة فقط float.

**القيمة المرتجعة:**  
float

### getBottom() {#getBottom--}
```
public final float getBottom()
```

أسفل. قراءة فقط float.

**القيمة المرتجعة:**  
float

### getOverlay() {#getOverlay--}
```
public final boolean getOverlay()
```

يحدد ما إذا كان يُسمح لعناصر المخطط الأخرى بتغطية العنوان. قراءة/كتابة boolean.

**القيمة المرتجعة:**  
boolean

### setOverlay(boolean value) {#setOverlay-boolean-}
```
public final void setOverlay(boolean value)
```

يحدد ما إذا كان يُسمح لعناصر المخطط الأخرى بتغطية العنوان. قراءة/كتابة boolean.

**الوسائط:**  
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

يعيد أنماط التعبئة والخط والتأثير للعنوان. قراءة فقط [IFormat](../../com.aspose.slides/iformat).

**القيمة المرتجعة:**  
[IFormat](../../com.aspose.slides/iformat)

### addTextFrameForOverriding(String text) {#addTextFrameForOverriding-java.lang.String-}
```
public final ITextFrame addTextFrameForOverriding(String text)
```

تهيئة TextFrameForOverriding بالنص الموجود في المعامل "text". إذا كان TextFrameForOverriding مهيئًا بالفعل فسيتم فقط تغيير نصه.

**الوسائط:**  
| المعامل | النوع | الوصف |
| --- | --- | --- |
| text | java.lang.String | النص لإطار TextFrameForOverriding الجديد. |

**القيمة المرتجعة:**  
[ITextFrame](../../com.aspose.slides/itextframe)

### getTextFrameForOverriding() {#getTextFrameForOverriding--}
```
public final ITextFrame getTextFrameForOverriding()
```

يمكن أن يحتوي على نص منسق غني. إذا كانت هذه الخاصية غير فارغة فإن هذا النص المنسق يتجاوز النص المولّد تلقائيًا. النص المولّد تلقائيًا هو خاصية ضمنية لتسمية البيانات، تسمية وحدة العرض لمحور القيمة، عنوان المحور، عنوان المخطط، تسمية الخط الاتجاهي. يتم تنسيق النص المولّد تلقائيًا باستخدام الخاصية IFormattedTextContainer.TextFormat. قراءة فقط [ITextFrame](../../com.aspose.slides/itextframe).

**القيمة المرتجعة:**  
[ITextFrame](../../com.aspose.slides/itextframe)

### getTextFormat() {#getTextFormat--}
```
public final IChartTextFormat getTextFormat()
```

يعيد تنسيق النص. قراءة فقط [IChartTextFormat](../../com.aspose.slides/icharttextformat).

**القيمة المرتجعة:**  
[IChartTextFormat](../../com.aspose.slides/icharttextformat)

### getActualX() {#getActualX--}
```
public final float getActualX()
```

يحدد الموقع الفعلي لإحداثي x (يسار) لعنصر المخطط بالنسبة إلى الزاوية اليسرى العليا للمخطط. استدعِ الطريقة IChart.validateChartLayout() مسبقًا للحصول على القيم الفعلية. قراءة float.

**القيمة المرتجعة:**  
float

### getActualY() {#getActualY--}
```
public final float getActualY()
```

يحدد الجزء العلوي الفعلي لعنصر المخطط بالنسبة إلى الزاوية اليسرى العليا للمخطط. استدعِ الطريقة IChart.validateChartLayout() مسبقًا للحصول على القيم الفعلية. قراءة float.

**القيمة المرتجعة:**  
float

### getActualWidth() {#getActualWidth--}
```
public final float getActualWidth()
```

يحدد العرض الفعلي لعنصر المخطط. استدعِ الطريقة IChart.validateChartLayout() مسبقًا للحصول على القيم الفعلية. قراءة float.

**القيمة المرتجعة:**  
float

### getActualHeight() {#getActualHeight--}
```
public final float getActualHeight()
```

يحدد الارتفاع الفعلي لعنصر المخطط. استدعِ الطريقة IChart.validateChartLayout() مسبقًا للحصول على القيم الفعلية. قراءة float.

**القيمة المرتجعة:**  
float

### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

يعيد كائن Parent_Immediate. قراءة فقط IDOMObject.

**القيمة المرتجعة:**  
com.aspose.slides.IDOMObject

### getChart() {#getChart--}
```
public final IChart getChart()
```

يعيد المخطط الأصلي. قراءة فقط [IChart](../../com.aspose.slides/ichart).

**القيمة المرتجعة:**  
[IChart](../../com.aspose.slides/ichart)

### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

يعيد الشريحة الأصلية لـ FillFormat. قراءة فقط [BaseSlide](../../com.aspose.slides/baseslide).

**القيمة المرتجعة:**  
[IBaseSlide](../../com.aspose.slides/ibaseslide)

### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

يعيد العرض الأصلي لـ FillFormat. قراءة فقط [IPresentation](../../com.aspose.slides/ipresentation).

**القيمة المرتجعة:**  
[IPresentation](../../com.aspose.slides/ipresentation)