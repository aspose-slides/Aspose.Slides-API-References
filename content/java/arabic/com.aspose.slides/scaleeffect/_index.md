---
title: ScaleEffect
second_title: مرجع Aspose.Slides لواجهة برمجة تطبيقات Java
description: يمثل تأثير مقياس الرسوم المتحركة.
type: docs
url: /ar/com.aspose.slides/scaleeffect/
---
**الوراثة:**  
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**جميع الواجهات المنفذة:**  
[com.aspose.slides.IScaleEffect](../../com.aspose.slides/iscaleeffect)  
```
public class ScaleEffect extends Behavior implements IScaleEffect
```

يمثل تأثير مقياس الرسوم المتحركة.

## المنشئات

| المنشئ | الوصف |
| --- | --- |
| [ScaleEffect()](#ScaleEffect--) |  |

## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | يحدد ما إذا يجب تكبير المحتوى. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | يحدد ما إذا يجب تكبير المحتوى. |
| [getFrom()](#getFrom--) | يحدد إحداثيات x/y لبدء الرسوم المتحركة من (بالنسبة المئوية). |
| [setFrom(Point2D.Float value)](#setFrom-java.awt.geom.Point2D.Float-) | يحدد إحداثيات x/y لبدء الرسوم المتحركة من (بالنسبة المئوية). |
| [getTo()](#getTo--) | يحدد الموقع المستهدف لتأثير مقياس الرسوم المتحركة (بالنسبة المئوية). |
| [setTo(Point2D.Float value)](#setTo-java.awt.geom.Point2D.Float-) | يحدد الموقع المستهدف لتأثير مقياس الرسوم المتحركة (بالنسبة المئوية). |
| [getBy()](#getBy--) | يصف قيمة الإزاحة النسبية للرسوم المتحركة (بالنسبة المئوية). |
| [setBy(Point2D.Float value)](#setBy-java.awt.geom.Point2D.Float-) | يصف قيمة الإزاحة النسبية للرسوم المتحركة (بالنسبة المئوية). |

### ScaleEffect() {#ScaleEffect--}
```
public ScaleEffect()
```

### getZoomContent() {#getZoomContent--}
```
public final byte getZoomContent()
```

يحدد ما إذا يجب تكبير المحتوى. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**القيمة المرجعة:**  
byte

### setZoomContent(byte value) {#setZoomContent-byte-}
```
public final void setZoomContent(byte value)
```

يحدد ما إذا يجب تكبير المحتوى. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public final Point2D.Float getFrom()
```

يحدد إحداثيات x/y لبدء الرسوم المتحركة من (بالنسبة المئوية). قراءة/كتابة java.awt.geom.Point2D.Float.

**القيمة المرجعة:**  
java.awt.geom.Point2D.Float

### setFrom(Point2D.Float value) {#setFrom-java.awt.geom.Point2D.Float-}
```
public final void setFrom(Point2D.Float value)
```

يحدد إحداثيات x/y لبدء الرسوم المتحركة من (بالنسبة المئوية). قراءة/كتابة java.awt.geom.Point2D.Float.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getTo() {#getTo--}
```
public final Point2D.Float getTo()
```

يحدد الموقع المستهدف لتأثير مقياس الرسوم المتحركة (بالنسبة المئوية). قراءة/كتابة java.awt.geom.Point2D.Float.

**القيمة المرجعة:**  
java.awt.geom.Point2D.Float

### setTo(Point2D.Float value) {#setTo-java.awt.geom.Point2D.Float-}
```
public final void setTo(Point2D.Float value)
```

يحدد الموقع المستهدف لتأثير مقياس الرسوم المتحركة (بالنسبة المئوية). قراءة/كتابة java.awt.geom.Point2D.Float.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getBy() {#getBy--}
```
public final Point2D.Float getBy()
```

يصف قيمة الإزاحة النسبية للرسوم المتحركة (بالنسبة المئوية). قراءة/كتابة java.awt.geom.Point2D.Float.

**القيمة المرجعة:**  
java.awt.geom.Point2D.Float

### setBy(Point2D.Float value) {#setBy-java.awt.geom.Point2D.Float-}
```
public final void setBy(Point2D.Float value)
```

يصف قيمة الإزاحة النسبية للرسوم المتحركة (بالنسبة المئوية). قراءة/كتابة java.awt.geom.Point2D.Float.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |