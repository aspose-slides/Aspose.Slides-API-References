---
title: Placeholder
second_title: مرجع API Aspose.Slides لـ Java
description: يمثل عنصرًا نائبًا على شريحة.
type: docs
url: /ar/com.aspose.slides/placeholder/
---
**الوراثة:**
java.lang.Object

**جميع الواجهات المنفذة:**
[com.aspose.slides.IPlaceholder](../../com.aspose.slides/iplaceholder)
```
public class Placeholder implements IPlaceholder
```

يمثل عنصرًا نائبًا على شريحة.
## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getOrientation()](#getOrientation--) | يحصل أو يضبط اتجاه العنصر النائب. |
| [getSize()](#getSize--) | يحصل أو يضبط حجم العنصر النائب. |
| [getType()](#getType--) | يحصل أو يضبط نوع العنصر النائب. |
| [getIndex()](#getIndex--) | يحصل أو يضبط فهرس العنصر النائب. |
### getOrientation() {#getOrientation--}
```
public byte getOrientation()
```

يحصل أو يضبط اتجاه العنصر النائب. للقراءة فقط [Orientation](../../com.aspose.slides/orientation).

**القيمة المرجعة:**
byte
### getSize() {#getSize--}
```
public byte getSize()
```

يحصل أو يضبط حجم العنصر النائب. للقراءة فقط [PlaceholderSize](../../com.aspose.slides/placeholdersize).

**القيمة المرجعة:**
byte
### getType() {#getType--}
```
public byte getType()
```

يحصل أو يضبط نوع العنصر النائب. للقراءة فقط [PlaceholderType](../../com.aspose.slides/placeholdertype).

**القيمة المرجعة:**
byte
### getIndex() {#getIndex--}
```
public long getIndex()
```

يحصل أو يضبط فهرس العنصر النائب. للقراءة فقط long.

**القيمة المرجعة:**
long