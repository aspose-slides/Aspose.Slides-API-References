---
title: MotionEffect
second_title: مرجع Aspose.Slides لواجهة برمجة تطبيقات Java
description: يمثل سلوك تأثير الحركة للتأثير.
type: docs
url: /ar/com.aspose.slides/motioneffect/
---
**الوراثة:**  
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**جميع الواجهات المُنفذة:**  
[com.aspose.slides.IMotionEffect](../../com.aspose.slides/imotioneffect)  
```
public class MotionEffect extends Behavior implements IMotionEffect
```

تمثّل سلوك تأثير الحركة للتأثير.

## البُنى

| المنشئ | الوصف |
| --- | --- |
| [MotionEffect()](#MotionEffect--) |  |

## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getFrom()](#getFrom--) | يحدد إحداثي x/y لبدء الحركة (بنسبة مئوية). |
| [setFrom(Point2D.Float value)](#setFrom-java.awt.geom.Point2D.Float-) | يحدد إحداثي x/y لبدء الحركة (بنسبة مئوية). |
| [getTo()](#getTo--) | يحدد الموقع الهدف لتأثير الحركة (بنسبة مئوية). |
| [setTo(Point2D.Float value)](#setTo-java.awt.geom.Point2D.Float-) | يحدد الموقع الهدف لتأثير الحركة (بنسبة مئوية). |
| [getBy()](#getBy--) | يصف قيمة الإزاحة النسبية للحركة (بنسبة مئوية). |
| [setBy(Point2D.Float value)](#setBy-java.awt.geom.Point2D.Float-) | يصف قيمة الإزاحة النسبية للحركة (بنسبة مئوية). |
| [getRotationCenter()](#getRotationCenter--) | يصف مركز الدوران المستخدم لتدوير مسار الحركة بزاوية X. |
| [setRotationCenter(Point2D.Float value)](#setRotationCenter-java.awt.geom.Point2D.Float-) | يصف مركز الدوران المستخدم لتدوير مسار الحركة بزاوية X. |
| [getOrigin()](#getOrigin--) | يحدد ما هو أصل مسار الحركة بالنسبة إلى مثل تخطيط الشريحة أو العنصر الأصل. |
| [setOrigin(int value)](#setOrigin-int-) | يحدد ما هو أصل مسار الحركة بالنسبة إلى مثل تخطيط الشريحة أو العنصر الأصل. |
| [getPath()](#getPath--) | يحدد primitive للمسار متبوعًا بالإحداثيات للحركة. |
| [setPath(IMotionPath value)](#setPath-com.aspose.slides.IMotionPath-) | يحدد primitive للمسار متبعًا بالإحداثيات للحركة. |
| [getPathEditMode()](#getPathEditMode--) | يحدد كيفية تحرك مسار الحركة عند تحريك الشكل. |
| [setPathEditMode(int value)](#setPathEditMode-int-) | يحدد كيفية تحرك مسار الحركة عند تحريك الشكل. |
| [getAngle()](#getAngle--) | يصف الزاوية النسبية لمسار الحركة. |
| [setAngle(float value)](#setAngle-float-) | يصف الزاوية النسبية لمسار الحركة. |

### MotionEffect() {#MotionEffect--}
```
public MotionEffect()
```

### getFrom() {#getFrom--}
```
public final Point2D.Float getFrom()
```

يحدد إحداثي x/y لبدء الحركة (بنسبة مئوية). قراءة/كتابة java.awt.geom.Point2D.Float.

**الإرجاع:**  
java.awt.geom.Point2D.Float

### setFrom(Point2D.Float value) {#setFrom-java.awt.geom.Point2D.Float-}
```
public final void setFrom(Point2D.Float value)
```

يحدد إحداثي x/y لبدء الحركة (بنسبة مئوية). قراءة/كتابة java.awt.geom.Point2D.Float.

**المعلمات:**  
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getTo() {#getTo--}
```
public final Point2D.Float getTo()
```

يحدد الموقع الهدف لتأثير الحركة (بنسبة مئوية). قراءة/كتابة java.awt.geom.Point2D.Float.

**الإرجاع:**  
java.awt.geom.Point2D.Float

### setTo(Point2D.Float value) {#setTo-java.awt.geom.Point2D.Float-}
```
public final void setTo(Point2D.Float value)
```

يحدد الموقع الهدف لتأثير الحركة (بنسبة مئوية). قراءة/كتابة java.awt.geom.Point2D.Float.

**المعلمات:**  
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getBy() {#getBy--}
```
public final Point2D.Float getBy()
```

يصف قيمة الإزاحة النسبية للحركة (بنسبة مئوية). قراءة/كتابة java.awt.geom.Point2D.Float.

**الإرجاع:**  
java.awt.geom.Point2D.Float

### setBy(Point2D.Float value) {#setBy-java.awt.geom.Point2D.Float-}
```
public final void setBy(Point2D.Float value)
```

يصف قيمة الإزاحة النسبية للحركة (بنسبة مئوية). قراءة/كتابة java.awt.geom.Point2D.Float.

**المعلمات:**  
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getRotationCenter() {#getRotationCenter--}
```
public final Point2D.Float getRotationCenter()
```

يصف مركز الدوران المستخدم لتدوير مسار الحركة بزاوية X. قراءة/كتابة java.awt.geom.Point2D.Float.

**الإرجاع:**  
java.awt.geom.Point2D.Float

### setRotationCenter(Point2D.Float value) {#setRotationCenter-java.awt.geom.Point2D.Float-}
```
public final void setRotationCenter(Point2D.Float value)
```

يصف مركز الدوران المستخدم لتدوير مسار الحركة بزاوية X. قراءة/كتابة java.awt.geom.Point2D.Float.

**المعلمات:**  
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getOrigin() {#getOrigin--}
```
public final int getOrigin()
```

يحدد ما هو أصل مسار الحركة بالنسبة إلى مثل تخطيط الشريحة أو العنصر الأصل. قراءة/كتابة [MotionOriginType](../../com.aspose.slides/motionorigintype).

**الإرجاع:**  
int

### setOrigin(int value) {#setOrigin-int-}
```
public final void setOrigin(int value)
```

يحدد ما هو أصل مسار الحركة بالنسبة إلى مثل تخطيط الشريحة أو العنصر الأصل. قراءة/كتابة [MotionOriginType](../../com.aspose.slides/motionorigintype).

**المعلمات:**  
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | int |  |

### getPath() {#getPath--}
```
public final IMotionPath getPath()
```

يحدد primitive للمسار متبوعًا بالإحداثيات للحركة. قراءة/كتابة [IMotionPath](../../com.aspose.slides/imotionpath).

**الإرجاع:**  
[IMotionPath](../../com.aspose.slides/imotionpath)

### setPath(IMotionPath value) {#setPath-com.aspose.slides.IMotionPath-}
```
public final void setPath(IMotionPath value)
```

يحدد primitive للمسار متبوعًا بالإحداثيات للحركة. قراءة/كتابة [IMotionPath](../../com.aspose.slides/imotionpath).

**المعلمات:**  
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | [IMotionPath](../../com.aspose.slides/imotionpath) |  |

### getPathEditMode() {#getPathEditMode--}
```
public final int getPathEditMode()
```

يحدد كيفية تحرك مسار الحركة عند تحريك الشكل. قراءة/كتابة [MotionPathEditMode](../../com.aspose.slides/motionpatheditmode).

**الإرجاع:**  
int

### setPathEditMode(int value) {#setPathEditMode-int-}
```
public final void setPathEditMode(int value)
```

يحدد كيفية تحرك مسار الحركة عند تحريك الشكل. قراءة/كتابة [MotionPathEditMode](../../com.aspose.slides/motionpatheditmode).

**المعلمات:**  
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | int |  |

### getAngle() {#getAngle--}
```
public final float getAngle()
```

يصف الزاوية النسبية لمسار الحركة. قراءة/كتابة float.

**الإرجاع:**  
float

### setAngle(float value) {#setAngle-float-}
```
public final void setAngle(float value)
```

يصف الزاوية النسبية لمسار الحركة. قراءة/كتابة float.

**المعلمات:**  
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |