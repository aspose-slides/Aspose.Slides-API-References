---
title: IPlaceholder
second_title: Aspose.Slides لـ Java مرجع API
description: يمثل عنصر نائب على شريحة.
type: docs
url: /ar/com.aspose.slides/iplaceholder/
---```
public interface IPlaceholder
```

يمثل عنصر نائب على شريحة.
## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getOrientation()](#getOrientation--) | يحصل على اتجاه عنصر نائب. |
| [getSize()](#getSize--) | يحصل على حجم عنصر نائب. |
| [getType()](#getType--) | يحصل على نوع عنصر نائب. |
| [getIndex()](#getIndex--) | يحصل على فهرس عنصر نائب. |
### getOrientation() {#getOrientation--}
```
public abstract byte getOrientation()
```

يحصل على اتجاه عنصر نائب. قراءة فقط [Orientation](../../com.aspose.slides/orientation).

**الإرجاع:**  
byte
### getSize() {#getSize--}
```
public abstract byte getSize()
```

يحصل على حجم عنصر نائب. قراءة فقط [PlaceholderSize](../../com.aspose.slides/placeholdersize).

**الإرجاع:**  
byte
### getType() {#getType--}
```
public abstract byte getType()
```

يحصل على نوع عنصر نائب. قراءة فقط [PlaceholderType](../../com.aspose.slides/placeholdertype).

**الإرجاع:**  
byte
### getIndex() {#getIndex--}
```
public abstract long getIndex()
```

يحصل على فهرس عنصر نائب. قراءة فقط long.

**الإرجاع:**  
long