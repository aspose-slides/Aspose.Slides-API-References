---
title: TiffCompressionTypes
second_title: مرجع API لـ Aspose.Slides للغة Java
description: يوفر خيارات تتحكم في كيفية ضغط عرض تقديمي بتنسيق TIFF.
type: docs
url: /ar/com.aspose.slides/tiffcompressiontypes/
---
**الوراثة:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class TiffCompressionTypes extends System.Enum
```

يوفر خيارات تتحكم في كيفية ضغط عرض تقديمي بتنسيق TIFF.
## الحقول

| الحقل | الوصف |
| --- | --- |
| [Default](#Default) | يحدد مخطط الضغط الافتراضي (LZW). |
| [None](#None) | يحدد عدم وجود ضغط. |
| [CCITT3](#CCITT3) | يحدد مخطط ضغط CCITT3. |
| [CCITT4](#CCITT4) | يحدد مخطط ضغط CCITT4. |
| [LZW](#LZW) | يحدد مخطط ضغط LZW (الافتراضي). |
| [RLE](#RLE) | يحدد مخطط ضغط RLE. |
### الافتراضي {#Default}
```
public static final int Default
```

يحدد مخطط الضغط الافتراضي (LZW).

### بدون {#None}
```
public static final int None
```

يحدد عدم وجود ضغط.

### CCITT3 {#CCITT3}
```
public static final int CCITT3
```

يحدد مخطط ضغط CCITT3.

### CCITT4 {#CCITT4}
```
public static final int CCITT4
```

يحدد مخطط ضغط CCITT4.

### LZW {#LZW}
```
public static final int LZW
```

يحدد مخطط ضغط LZW (الافتراضي).

### RLE {#RLE}
```
public static final int RLE
```

يحدد مخطط ضغط RLE.