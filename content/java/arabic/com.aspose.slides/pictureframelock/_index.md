---
title: PictureFrameLock
second_title: مرجع API ل Aspose.Slides للـ Java
description: يحدد العمليات التي تم تعطيلها على الكائن الأب PictureFrame.
type: docs
url: /ar/com.aspose.slides/pictureframelock/
---
**الوراثة:**
java.lang.Object, [com.aspose.slides.BaseShapeLock](../../com.aspose.slides/baseshapelock)

**جميع الواجهات المُنفذة:**
[com.aspose.slides.IPictureFrameLock](../../com.aspose.slides/ipictureframelock)
```
public class PictureFrameLock extends BaseShapeLock implements IPictureFrameLock
```

يحدد العمليات التي تم تعطيلها على العنصر الأب PictureFrame.
## الأساليب

| الطريقة | الوصف |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | يحدد ما إذا كان إضافة هذا الشكل إلى مجموعة محظورة. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | يحدد ما إذا كان إضافة هذا الشكل إلى مجموعة محظورة. |
| [getSelectLocked()](#getSelectLocked--) | يحدد ما إذا كان اختيار هذا الشكل محظوراً. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | يحدد ما إذا كان اختيار هذا الشكل محظوراً. |
| [getRotationLocked()](#getRotationLocked--) | يحدد ما إذا كان تغيير زاوية الدوران لهذا الشكل محظوراً. |
| [setRotationLocked(boolean value)](#setRotationLocked-boolean-) | يحدد ما إذا كان تغيير زاوية الدوران لهذا الشكل محظوراً. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | يحدد ما إذا كان يجب الحفاظ على نسبة العرض إلى الارتفاع عند تغيير حجم الشكل. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | يحدد ما إذا كان يجب الحفاظ على نسبة العرض إلى الارتفاع عند تغيير حجم الشكل. |
| [getPositionLocked()](#getPositionLocked--) | يحدد ما إذا كان تحريك هذا الشكل محظوراً. |
| [setPositionLocked(boolean value)](#setPositionLocked-boolean-) | يحدد ما إذا كان تحريك هذا الشكل محظوراً. |
| [getSizeLocked()](#getSizeLocked--) | يحدد ما إذا كان تغيير حجم هذا الشكل محظوراً. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | يحدد ما إذا كان تغيير حجم هذا الشكل محظوراً. |
| [getEditPointsLocked()](#getEditPointsLocked--) | يحدد ما إذا كان تغيير شكل المحيط لهذا الشكل مباشرةً محظوراً. |
| [setEditPointsLocked(boolean value)](#setEditPointsLocked-boolean-) | يحدد ما إذا كان تغيير شكل المحيط لهذا الشكل مباشرةً محظوراً. |
| [getAdjustHandlesLocked()](#getAdjustHandlesLocked--) | يحدد ما إذا كان تعديل القيم محظوراً. |
| [setAdjustHandlesLocked(boolean value)](#setAdjustHandlesLocked-boolean-) | يحدد ما إذا كان تعديل القيم محظوراً. |
| [getArrowheadsLocked()](#getArrowheadsLocked--) | يحدد ما إذا كان تغيير رؤوس السهام محظوراً. |
| [setArrowheadsLocked(boolean value)](#setArrowheadsLocked-boolean-) | يحدد ما إذا كان تغيير رؤوس السهام محظوراً. |
| [getShapeTypeLocked()](#getShapeTypeLocked--) | يحدد ما إذا كان تغيير نوع الشكل محظوراً. |
| [setShapeTypeLocked(boolean value)](#setShapeTypeLocked-boolean-) | يحدد ما إذا كان تغيير نوع الشكل محظوراً. |
| [getCropLocked()](#getCropLocked--) | يحدد ما إذا كان قص الصورة محظوراً. |
| [setCropLocked(boolean value)](#setCropLocked-boolean-) | يحدد ما إذا كان قص الصورة محظوراً. |
### getGroupingLocked() {#getGroupingLocked--}
```
public boolean getGroupingLocked()
```

يحدد ما إذا كان إضافة هذا الشكل إلى مجموعة محظورة. قابل للقراءة/الكتابة boolean.

**إرجاع:**
boolean
### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public void setGroupingLocked(boolean value)
```

يحدد ما إذا كان إضافة هذا الشكل إلى مجموعة محظورة. قابل للقراءة/الكتابة boolean.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getSelectLocked() {#getSelectLocked--}
```
public boolean getSelectLocked()
```

يحدد ما إذا كان اختيار هذا الشكل محظوراً. قابل للقراءة/الكتابة boolean.

**إرجاع:**
boolean
### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public void setSelectLocked(boolean value)
```

يحدد ما إذا كان اختيار هذا الشكل محظوراً. قابل للقراءة/الكتابة boolean.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getRotationLocked() {#getRotationLocked--}
```
public boolean getRotationLocked()
```

يحدد ما إذا كان تغيير زاوية الدوران لهذا الشكل محظوراً. قابل للقراءة/الكتابة boolean.

**إرجاع:**
boolean
### setRotationLocked(boolean value) {#setRotationLocked-boolean-}
```
public void setRotationLocked(boolean value)
```

يحدد ما إذا كان تغيير زاوية الدوران لهذا الشكل محظوراً. قابل للقراءة/الكتابة boolean.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public boolean getAspectRatioLocked()
```

يحدد ما إذا كان يجب الحفاظ على نسبة العرض إلى الارتفاع عند تغيير حجم الشكل. قابل للقراءة/الكتابة boolean.

**إرجاع:**
boolean
### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public void setAspectRatioLocked(boolean value)
```

يحدد ما إذا كان يجب الحفاظ على نسبة العرض إلى الارتفاع عند تغيير حجم الشكل. قابل للقراءة/الكتابة boolean.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getPositionLocked() {#getPositionLocked--}
```
public boolean getPositionLocked()
```

يحدد ما إذا كان تحريك هذا الشكل محظوراً. قابل للقراءة/الكتابة boolean.

**إرجاع:**
boolean
### setPositionLocked(boolean value) {#setPositionLocked-boolean-}
```
public void setPositionLocked(boolean value)
```

يحدد ما إذا كان تحريك هذا الشكل محظوراً. قابل للقراءة/الكتابة boolean.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getSizeLocked() {#getSizeLocked--}
```
public boolean getSizeLocked()
```

يحدد ما إذا كان تغيير حجم هذا الشكل محظوراً. قابل للقراءة/الكتابة boolean.

**إرجاع:**
boolean
### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public void setSizeLocked(boolean value)
```

يحدد ما إذا كان تغيير حجم هذا الشكل محظوراً. قابل للقراءة/الكتابة boolean.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getEditPointsLocked() {#getEditPointsLocked--}
```
public boolean getEditPointsLocked()
```

يحدد ما إذا كان تغيير شكل المحيط لهذا الشكل مباشرةً محظوراً. قابل للقراءة/الكتابة boolean.

**إرجاع:**
boolean
### setEditPointsLocked(boolean value) {#setEditPointsLocked-boolean-}
```
public void setEditPointsLocked(boolean value)
```

يحدد ما إذا كان تغيير شكل المحيط لهذا الشكل مباشرةً محظوراً. قابل للقراءة/الكتابة boolean.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getAdjustHandlesLocked() {#getAdjustHandlesLocked--}
```
public boolean getAdjustHandlesLocked()
```

يحدد ما إذا كان تعديل القيم محظوراً. قابل للقراءة/الكتابة boolean.

**إرجاع:**
boolean
### setAdjustHandlesLocked(boolean value) {#setAdjustHandlesLocked-boolean-}
```
public void setAdjustHandlesLocked(boolean value)
```

يحدد ما إذا كان تعديل القيم محظوراً. قابل للقراءة/الكتابة boolean.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getArrowheadsLocked() {#getArrowheadsLocked--}
```
public boolean getArrowheadsLocked()
```

يحدد ما إذا كان تغيير رؤوس السهام محظوراً. قابل للقراءة/الكتابة boolean.

**إرجاع:**
boolean
### setArrowheadsLocked(boolean value) {#setArrowheadsLocked-boolean-}
```
public void setArrowheadsLocked(boolean value)
```

يحدد ما إذا كان تغيير رؤوس السهام محظوراً. قابل للقراءة/الكتابة boolean.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getShapeTypeLocked() {#getShapeTypeLocked--}
```
public boolean getShapeTypeLocked()
```

يحدد ما إذا كان تغيير نوع الشكل محظوراً. قابل للقراءة/الكتابة boolean.

**إرجاع:**
boolean
### setShapeTypeLocked(boolean value) {#setShapeTypeLocked-boolean-}
```
public void setShapeTypeLocked(boolean value)
```

يحدد ما إذا كان تغيير نوع الشكل محظوراً. قابل للقراءة/الكتابة boolean.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getCropLocked() {#getCropLocked--}
```
public boolean getCropLocked()
```

يحدد ما إذا كان قص الصورة محظوراً. قابل للقراءة/الكتابة boolean.

**إرجاع:**
boolean
### setCropLocked(boolean value) {#setCropLocked-boolean-}
```
public void setCropLocked(boolean value)
```

يحدد ما إذا كان قص الصورة محظوراً. قابل للقراءة/الكتابة boolean.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |