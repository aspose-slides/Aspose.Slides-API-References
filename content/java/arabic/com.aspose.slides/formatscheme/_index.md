---
title: FormatScheme
second_title: مرجع Aspose.Slides للـ Java API
description: يخزن التنسيقات المحددة بالثيم للأشكال.
type: docs
url: /ar/com.aspose.slides/formatscheme/
---
**الوراثة:**
java.lang.Object

**جميع الواجهات المنفذة:**
[com.aspose.slides.IFormatScheme](../../com.aspose.slides/iformatscheme), com.aspose.slides.IDOMObject
```
public class FormatScheme implements IFormatScheme, IDOMObject
```

يخزن تنسيقات محددة بالثيم للأشكال.
## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getFillStyles()](#getFillStyles--) | تُرجع مجموعة من أنماط التعبئة المحددة بالثيم. |
| [getLineStyles()](#getLineStyles--) | تُرجع مجموعة من أنماط الخط المحددة بالثيم. |
| [getEffectStyles()](#getEffectStyles--) | تُرجع مجموعة من أنماط التأثير المحددة بالثيم. |
| [getBackgroundFillStyles()](#getBackgroundFillStyles--) | تُرجع مجموعة من أنماط تعبئة الخلفية المحددة بالثيم. |
| [getSlide()](#getSlide--) | تُرجع الشريحة الأم. |
| [getPresentation()](#getPresentation--) | تُرجع العرض التقديمي الأم. |
| [getParent_Immediate()](#getParent-Immediate--) |  |

### getFillStyles() {#getFillStyles--}
```
public final IFillFormatCollection getFillStyles()
```

تُرجع مجموعة من أنماط التعبئة المحددة بالثيم. للقراءة فقط [IFillFormatCollection](../../com.aspose.slides/ifillformatcollection).

**الإرجاع:**
[IFillFormatCollection](../../com.aspose.slides/ifillformatcollection)

### getLineStyles() {#getLineStyles--}
```
public final ILineFormatCollection getLineStyles()
```

تُرجع مجموعة من أنماط الخط المحددة بالثيم. للقراءة فقط [ILineFormatCollection](../../com.aspose.slides/ilineformatcollection).

**الإرجاع:**
[ILineFormatCollection](../../com.aspose.slides/ilineformatcollection)

### getEffectStyles() {#getEffectStyles--}
```
public final IEffectStyleCollection getEffectStyles()
```

تُرجع مجموعة من أنماط التأثير المحددة بالثيم. للقراءة فقط [IEffectStyleCollection](../../com.aspose.slides/ieffectstylecollection).

**الإرجاع:**
[IEffectStyleCollection](../../com.aspose.slides/ieffectstylecollection)

### getBackgroundFillStyles() {#getBackgroundFillStyles--}
```
public final IFillFormatCollection getBackgroundFillStyles()
```

تُرجع مجموعة من أنماط تعبئة الخلفية المحددة بالثيم. للقراءة فقط [IFillFormatCollection](../../com.aspose.slides/ifillformatcollection).

**الإرجاع:**
[IFillFormatCollection](../../com.aspose.slides/ifillformatcollection)

### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

تُرجع الشريحة الأم. للقراءة فقط [IBaseSlide](../../com.aspose.slides/ibaseslide).

**الإرجاع:**
[IBaseSlide](../../com.aspose.slides/ibaseslide)

### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

تُرجع العرض التقديمي الأم. للقراءة فقط [IPresentation](../../com.aspose.slides/ipresentation).

**الإرجاع:**
[IPresentation](../../com.aspose.slides/ipresentation)

### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

تُرجع كائن Parent_Immediate. للقراءة فقط IDOMObject.

**الإرجاع:**
com.aspose.slides.IDOMObject