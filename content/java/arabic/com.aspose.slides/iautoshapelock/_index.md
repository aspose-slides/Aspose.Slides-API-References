---
title: IAutoShapeLock
second_title: مرجع API Aspose.Slides للـ Java
description: يحدد العمليات التي تم تعطيلها على العنصر الأصلي AutoshapeEx.
type: docs
url: /ar/com.aspose.slides/iautoshapelock/
---
**جميع الواجهات المُطبقة:**
[com.aspose.slides.IBaseShapeLock](../../com.aspose.slides/ibaseshapelock)
```
public interface IAutoShapeLock extends IBaseShapeLock
```

يحدِّد أي العمليات معطَّلة على العنصر الأصلي AutoshapeEx.
## الطرق

| طريقة | الوصف |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | يحدِّد ما إذا كان إضافة هذا الشكل إلى مجموعة محظورًا. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | يحدِّد ما إذا كان إضافة هذا الشكل إلى مجموعة محظورًا. |
| [getSelectLocked()](#getSelectLocked--) | يحدِّد ما إذا كان تحديد هذا الشكل محظورًا. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | يحدِّد ما إذا كان تحديد هذا الشكل محظورًا. |
| [getRotateLocked()](#getRotateLocked--) | يحدِّد ما إذا كان تغيير زاوية دوران هذا الشكل محظورًا. |
| [setRotateLocked(boolean value)](#setRotateLocked-boolean-) | يحدِّد ما إذا كان تغيير زاوية دوران هذا الشكل محظورًا. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | يحدِّد ما إذا كان يجب على الشكل الحفاظ على نسبة الأبعاد عند تغيير الحجم. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | يحدِّد ما إذا كان يجب على الشكل الحفاظ على نسبة الأبعاد عند تغيير الحجم. |
| [getPositionLocked()](#getPositionLocked--) | يحدِّد ما إذا كان نقل هذا الشكل محظورًا. |
| [setPositionLocked(boolean value)](#setPositionLocked-boolean-) | يحدِّد ما إذا كان نقل هذا الشكل محظورًا. |
| [getSizeLocked()](#getSizeLocked--) | يحدِّد ما إذا كان تغيير حجم هذا الشكل محظورًا. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | يحدِّد ما إذا كان تغيير حجم هذا الشكل محظورًا. |
| [getEditPointsLocked()](#getEditPointsLocked--) | يحدِّد ما إذا كان تعديل مباشر لمحيط هذا الشكل محظورًا. |
| [setEditPointsLocked(boolean value)](#setEditPointsLocked-boolean-) | يحدِّد ما إذا كان تعديل مباشر لمحيط هذا الشكل محظورًا. |
| [getAdjustHandlesLocked()](#getAdjustHandlesLocked--) | يحدِّد ما إذا كان تعديل قيم الضبط محظورًا. |
| [setAdjustHandlesLocked(boolean value)](#setAdjustHandlesLocked-boolean-) | يحدِّد ما إذا كان تعديل قيم الضبط محظورًا. |
| [getArrowheadsLocked()](#getArrowheadsLocked--) | يحدِّد ما إذا كان تعديل رؤوس السهام محظورًا. |
| [setArrowheadsLocked(boolean value)](#setArrowheadsLocked-boolean-) | يحدِّد ما إذا كان تعديل رؤوس السهام محظورًا. |
| [getShapeTypeLocked()](#getShapeTypeLocked--) | يحدِّد ما إذا كان تعديل نوع الشكل محظورًا. |
| [setShapeTypeLocked(boolean value)](#setShapeTypeLocked-boolean-) | يحدِّد ما إذا كان تعديل نوع الشكل محظورًا. |
| [getTextLocked()](#getTextLocked--) | يحدِّد ما إذا كان تحرير النص محظورًا. |
| [setTextLocked(boolean value)](#setTextLocked-boolean-) | يحدِّد ما إذا كان تحرير النص محظورًا. |

### getGroupingLocked() {#getGroupingLocked--}
```
public abstract boolean getGroupingLocked()
```

يحدِّد ما إذا كان إضافة هذا الشكل إلى مجموعة محظورًا. قيمة منطقية قابلة للقراءة والكتابة.

**القيمة المرجعة:**
boolean
### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public abstract void setGroupingLocked(boolean value)
```

يحدِّد ما إذا كان إضافة هذا الشكل إلى مجموعة محظورًا. قيمة منطقية قابلة للقراءة والكتابة.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getSelectLocked() {#getSelectLocked--}
```
public abstract boolean getSelectLocked()
```

يحدِّد ما إذا كان تحديد هذا الشكل محظورًا. قيمة منطقية قابلة للقراءة والكتابة.

**القيمة المرجعة:**
boolean
### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public abstract void setSelectLocked(boolean value)
```

يحدِّد ما إذا كان تحديد هذا الشكل محظورًا. قيمة منطقية قابلة للقراءة والكتابة.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getRotateLocked() {#getRotateLocked--}
```
public abstract boolean getRotateLocked()
```

يحدِّد ما إذا كان تغيير زاوية دوران هذا الشكل محظورًا. قيمة منطقية قابلة للقراءة والكتابة.

**القيمة المرجعة:**
boolean
### setRotateLocked(boolean value) {#setRotateLocked-boolean-}
```
public abstract void setRotateLocked(boolean value)
```

يحدِّد ما إذا كان تغيير زاوية دوران هذا الشكل محظورًا. قيمة منطقية قابلة للقراءة والكتابة.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public abstract boolean getAspectRatioLocked()
```

يحدِّد ما إذا كان يجب على الشكل الحفاظ على نسبة الأبعاد عند تغيير الحجم. قيمة منطقية قابلة للقراءة والكتابة.

**القيمة المرجعة:**
boolean
### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public abstract void setAspectRatioLocked(boolean value)
```

يحدِّد ما إذا كان يجب على الشكل الحفاظ على نسبة الأبعاد عند تغيير الحجم. قيمة منطقية قابلة للقراءة والكتابة.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getPositionLocked() {#getPositionLocked--}
```
public abstract boolean getPositionLocked()
```

يحدِّد ما إذا كان نقل هذا الشكل محظورًا. قيمة منطقية قابلة للقراءة والكتابة.

**القيمة المرجعة:**
boolean
### setPositionLocked(boolean value) {#setPositionLocked-boolean-}
```
public abstract void setPositionLocked(boolean value)
```

يحدِّد ما إذا كان نقل هذا الشكل محظورًا. قيمة منطقية قابلة للقراءة والكتابة.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getSizeLocked() {#getSizeLocked--}
```
public abstract boolean getSizeLocked()
```

يحدِّد ما إذا كان تغيير حجم هذا الشكل محظورًا. قيمة منطقية قابلة للقراءة والكتابة.

**القيمة المرجعة:**
boolean
### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public abstract void setSizeLocked(boolean value)
```

يحدِّد ما إذا كان تغيير حجم هذا الشكل محظورًا. قيمة منطقية قابلة للقراءة والكتابة.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getEditPointsLocked() {#getEditPointsLocked--}
```
public abstract boolean getEditPointsLocked()
```

يحدِّد ما إذا كان تعديل مباشر لمحيط هذا الشكل محظورًا. قيمة منطقية قابلة للقراءة والكتابة.

**القيمة المرجعة:**
boolean
### setEditPointsLocked(boolean value) {#setEditPointsLocked-boolean-}
```
public abstract void setEditPointsLocked(boolean value)
```

يحدِّد ما إذا كان تعديل مباشر لمحيط هذا الشكل محظورًا. قيمة منطقية قابلة للقراءة والكتابة.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getAdjustHandlesLocked() {#getAdjustHandlesLocked--}
```
public abstract boolean getAdjustHandlesLocked()
```

يحدِّد ما إذا كان تعديل قيم الضبط محظورًا. قيمة منطقية قابلة للقراءة والكتابة.

**القيمة المرجعة:**
boolean
### setAdjustHandlesLocked(boolean value) {#setAdjustHandlesLocked-boolean-}
```
public abstract void setAdjustHandlesLocked(boolean value)
```

يحدِّد ما إذا كان تعديل قيم الضبط محظورًا. قيمة منطقية قابلة للقراءة والكتابة.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getArrowheadsLocked() {#getArrowheadsLocked--}
```
public abstract boolean getArrowheadsLocked()
```

يحدِّد ما إذا كان تعديل رؤوس السهام محظورًا. قيمة منطقية قابلة للقراءة والكتابة.

**القيمة المرجعة:**
boolean
### setArrowheadsLocked(boolean value) {#setArrowheadsLocked-boolean-}
```
public abstract void setArrowheadsLocked(boolean value)
```

يحدِّد ما إذا كان تعديل رؤوس السهام محظورًا. قيمة منطقية قابلة للقراءة والكتابة.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getShapeTypeLocked() {#getShapeTypeLocked--}
```
public abstract boolean getShapeTypeLocked()
```

يحدِّد ما إذا كان تعديل نوع الشكل محظورًا. قيمة منطقية قابلة للقراءة والكتابة.

**القيمة المرجعة:**
boolean
### setShapeTypeLocked(boolean value) {#setShapeTypeLocked-boolean-}
```
public abstract void setShapeTypeLocked(boolean value)
```

يحدِّد ما إذا كان تعديل نوع الشكل محظورًا. قيمة منطقية قابلة للقراءة والكتابة.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |

### getTextLocked() {#getTextLocked--}
```
public abstract boolean getTextLocked()
```

يحدِّد ما إذا كان تحرير النص محظورًا. قيمة منطقية قابلة للقراءة والكتابة.

**القيمة المرجعة:**
boolean
### setTextLocked(boolean value) {#setTextLocked-boolean-}
```
public abstract void setTextLocked(boolean value)
```

يحدِّد ما إذا كان تحرير النص محظورًا. قيمة منطقية قابلة للقراءة والكتابة.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |