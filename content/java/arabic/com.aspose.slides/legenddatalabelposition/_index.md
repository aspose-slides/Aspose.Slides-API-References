---
title: LegendDataLabelPosition
second_title: مرجع API Aspose.Slides for Java
description: يحدد موضع تسميات البيانات.
type: docs
url: /ar/com.aspose.slides/legenddatalabelposition/
---
**الوراثة:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class LegendDataLabelPosition extends System.Enum
```

يحدد موضع تسميات البيانات.
## الحقول

| الحقل | الوصف |
| --- | --- |
| [NotDefined](#NotDefined) | الموضع غير معرف. |
| [Bottom](#Bottom) | يحدد أن تسميات البيانات يجب عرضها أسفل مؤشر البيانات. |
| [BestFit](#BestFit) | يحدد أن تسميات البيانات يجب عرضها في أفضل موضع. |
| [Center](#Center) | يحدد أن تسميات البيانات يجب عرضها في وسط مؤشر البيانات. |
| [InsideBase](#InsideBase) | يحدد أن تسميات البيانات يجب عرضها داخل قاعدة مؤشر البيانات. |
| [InsideEnd](#InsideEnd) | يحدد أن تسميات البيانات يجب عرضها داخل نهاية مؤشر البيانات. |
| [Left](#Left) | يحدد أن تسميات البيانات يجب عرضها إلى يسار مؤشر البيانات. |
| [OutsideEnd](#OutsideEnd) | يحدد أن تسميات البيانات يجب عرضها خارج نهاية مؤشر البيانات. |
| [Right](#Right) | يحدد أن تسميات البيانات يجب عرضها إلى يمين مؤشر البيانات. |
| [Top](#Top) | يحدد أن تسميات البيانات يجب عرضها فوق مؤشر البيانات. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```


الموضع غير معرف.

### Bottom {#Bottom}
```
public static final int Bottom
```


يحدد أن تسميات البيانات يجب عرضها أسفل مؤشر البيانات.

### BestFit {#BestFit}
```
public static final int BestFit
```


يحدد أن تسميات البيانات يجب عرضها في أفضل موضع.

### Center {#Center}
```
public static final int Center
```


يحدد أن تسميات البيانات يجب عرضها في وسط مؤشر البيانات.

### InsideBase {#InsideBase}
```
public static final int InsideBase
```


يحدد أن تسميات البيانات يجب عرضها داخل قاعدة مؤشر البيانات.

### InsideEnd {#InsideEnd}
```
public static final int InsideEnd
```


يحدد أن تسميات البيانات يجب عرضها داخل نهاية مؤشر البيانات.

### Left {#Left}
```
public static final int Left
```


يحدد أن تسميات البيانات يجب عرضها إلى يسار مؤشر البيانات.

### OutsideEnd {#OutsideEnd}
```
public static final int OutsideEnd
```


يحدد أن تسميات البيانات يجب عرضها خارج نهاية مؤشر البيانات.

### Right {#Right}
```
public static final int Right
```


يحدد أن تسميات البيانات يجب عرضها إلى يمين مؤشر البيانات.

### Top {#Top}
```
public static final int Top
```


يحدد أن تسميات البيانات يجب عرضها فوق مؤشر البيانات.