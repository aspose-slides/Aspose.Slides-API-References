---
title: AdjustValue
second_title: مرجع واجهة برمجة تطبيقات Aspose.Slides للـ Java
description: يمثل قيمة تعديل شكل هندسي.
type: docs
url: /ar/com.aspose.slides/adjustvalue/
---
**الوراثة:**
java.lang.Object

**جميع الواجهات المنفذة:**
[com.aspose.slides.IAdjustValue](../../com.aspose.slides/iadjustvalue)
```
public class AdjustValue implements IAdjustValue
```

يمثل قيمة تعديل شكل هندسي. هذه القيم تؤثر على شكل الشكل.
## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getRawValue()](#getRawValue--) | إرجاع أو تعيين قيمة تعديل "كما هي". قراءة/كتابة long. |
| [setRawValue(long value)](#setRawValue-long-) | إرجاع أو تعيين قيمة تعديل "كما هي". قراءة/كتابة long. |
| [getAngleValue()](#getAngleValue--) | إرجاع أو تعيين قيمة، مع تفسيرها كزاوية بالدرجات. قراءة/كتابة float. |
| [setAngleValue(float value)](#setAngleValue-float-) | إرجاع أو تعيين قيمة، مع تفسيرها كزاوية بالدرجات. قراءة/كتابة float. |
| [getName()](#getName--) | إرجاع اسم هذه القيمة المعدلة. |
| [getType()](#getType--) | إرجاع نوع تعديل الشكل. |

### getRawValue() {#getRawValue--}
```
public final long getRawValue()
```

إرجاع أو تعيين قيمة تعديل "كما هي". قراءة/كتابة long.

**الإرجاع:**
long

### setRawValue(long value) {#setRawValue-long-}
```
public final void setRawValue(long value)
```

إرجاع أو تعيين قيمة تعديل "كما هي". قراءة/كتابة long.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | long |  |

### getAngleValue() {#getAngleValue--}
```
public final float getAngleValue()
```

إرجاع أو تعيين قيمة، مع تفسيرها كزاوية بالدرجات. قراءة/كتابة float.

**الإرجاع:**
float

### setAngleValue(float value) {#setAngleValue-float-}
```
public final void setAngleValue(float value)
```

إرجاع أو تعيين قيمة، مع تفسيرها كزاوية بالدرجات. قراءة/كتابة float.

**المعاملات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getName() {#getName--}
```
public final String getName()
```

إرجاع اسم هذه القيمة المعدلة. قراءة فقط String.

**الإرجاع:**
java.lang.String

### getType() {#getType--}
```
public final int getType()
```

إرجاع نوع تعديل الشكل. قراءة فقط [ShapeAdjustmentType](../../com.aspose.slides/shapeadjustmenttype).

**الإرجاع:**
int