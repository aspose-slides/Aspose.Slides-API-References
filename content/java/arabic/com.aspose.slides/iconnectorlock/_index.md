---
title: IConnectorLock
second_title: مرجع API Aspose.Slides للـ Java
description: يحدد أي العمليات تم تعطيلها على الموصل الأب.
type: docs
url: /ar/com.aspose.slides/iconnectorlock/
---
**جميع الواجهات المنفذة:**
[com.aspose.slides.IBaseShapeLock](../../com.aspose.slides/ibaseshapelock)
```
public interface IConnectorLock extends IBaseShapeLock
```

يحدد أي العمليات تم تعطيلها على الموصل الأب.
## الأساليب

| الطريقة | الوصف |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | يحدد ما إذا كان إضافة هذا الشكل إلى مجموعة ممنوعًا. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | يحدد ما إذا كان إضافة هذا الشكل إلى مجموعة ممنوعًا. |
| [getSelectLocked()](#getSelectLocked--) | يحدد ما إذا كان تحديد هذا الشكل ممنوعًا. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | يحدد ما إذا كان تحديد هذا الشكل ممنوعًا. |
| [getRotateLocked()](#getRotateLocked--) | يحدد ما إذا كان تغيير زاوية الدوران لهذا الشكل ممنوعًا. |
| [setRotateLocked(boolean value)](#setRotateLocked-boolean-) | يحدد ما إذا كان تغيير زاوية الدوران لهذا الشكل ممنوعًا. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | يحدد ما إذا كان يجب على الشكل الحفاظ على نسبة الأبعاد أثناء تغيير الحجم. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | يحدد ما إذا كان يجب على الشكل الحفاظ على نسبة الأبعاد أثناء تغيير الحجم. |
| [getPositionMove()](#getPositionMove--) | يحدد ما إذا كان تحريك هذا الشكل ممنوعًا. |
| [setPositionMove(boolean value)](#setPositionMove-boolean-) | يحدد ما إذا كان تحريك هذا الشكل ممنوعًا. |
| [getSizeLocked()](#getSizeLocked--) | يحدد ما إذا كان تغيير حجم هذا الشكل ممنوعًا. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | يحدد ما إذا كان تغيير حجم هذا الشكل ممنوعًا. |
| [getEditPointsLocked()](#getEditPointsLocked--) | يحدد ما إذا كان التغيير المباشر لمحيط هذا الشكل ممنوعًا. |
| [setEditPointsLocked(boolean value)](#setEditPointsLocked-boolean-) | يحدد ما إذا كان التغيير المباشر لمحيط هذا الشكل ممنوعًا. |
| [getAdjustHandlesLocked()](#getAdjustHandlesLocked--) | يحدد ما إذا كان تغيير قيم الضبط ممنوعًا. |
| [setAdjustHandlesLocked(boolean value)](#setAdjustHandlesLocked-boolean-) | يحدد ما إذا كان تغيير قيم الضبط ممنوعًا. |
| [getArrowheadsLocked()](#getArrowheadsLocked--) | يحدد ما وإذا كان تغيير رؤوس السهام ممنوعًا. |
| [setArrowheadsLocked(boolean value)](#setArrowheadsLocked-boolean-) | يحدد ما وإذا كان تغيير رؤوس السهام ممنوعًا. |
| [getShapeTypeLocked()](#getShapeTypeLocked--) | يحدد ما إذا كان تغيير نوع الشكل ممنوعًا. |
| [setShapeTypeLocked(boolean value)](#setShapeTypeLocked-boolean-) | يحدد ما إذا كان تغيير نوع الشكل ممنوعًا. |
### getGroupingLocked() {#getGroupingLocked--}
```
public abstract boolean getGroupingLocked()
```

يحدد ما إذا كان إضافة هذا الشكل إلى مجموعة ممنوعًا. منطقية قراءة/كتابة.

**القيمة المرجعة:**
boolean
### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public abstract void setGroupingLocked(boolean value)
```

يحدد ما إذا كان إضافة هذا الشكل إلى مجموعة ممنوعًا. منطقية قراءة/كتابة.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |
### getSelectLocked() {#getSelectLocked--}
```
public abstract boolean getSelectLocked()
```

يحدد ما إذا كان تحديد هذا الشكل ممنوعًا. منطقية قراءة/كتابة.

**القيمة المرجعة:**
boolean
### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public abstract void setSelectLocked(boolean value)
```

يحدد ما إذا كان تحديد هذا الشكل ممنوعًا. منطقية قراءة/كتابة.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |
### getRotateLocked() {#getRotateLocked--}
```
public abstract boolean getRotateLocked()
```

يحدد ما إذا كان تغيير زاوية الدوران لهذا الشكل ممنوعًا. منطقية قراءة/كتابة.

**القيمة المرجعة:**
boolean
### setRotateLocked(boolean value) {#setRotateLocked-boolean-}
```
public abstract void setRotateLocked(boolean value)
```

يحدد ما إذا كان تغيير زاوية الدوران لهذا الشكل ممنوعًا. منطقية قراءة/كتابة.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |
### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public abstract boolean getAspectRatioLocked()
```

يحدد ما إذا كان يجب على الشكل الحفاظ على نسبة الأبعاد أثناء تغيير الحجم. منطقية قراءة/كتابة.

**القيمة المرجعة:**
boolean
### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public abstract void setAspectRatioLocked(boolean value)
```

يحدد ما إذا كان يجب على الشكل الحفاظ على نسبة الأبعاد أثناء تغيير الحجم. منطقية قراءة/كتابة.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |
### getPositionMove() {#getPositionMove--}
```
public abstract boolean getPositionMove()
```

يحدد ما إذا كان تحريك هذا الشكل ممنوحًا. منطقية قراءة/كتابة.

**القيمة المرجعة:**
boolean
### setPositionMove(boolean value) {#setPositionMove-boolean-}
```
public abstract void setPositionMove(boolean value)
```

يحدد ما إذا كان تحريك هذا الشكل ممنوحًا. منطقية قراءة/كتابة.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |
### getSizeLocked() {#getSizeLocked--}
```
public abstract boolean getSizeLocked()
```

يحدد ما إذا كان تغيير حجم هذا الشكل ممنوحًا. منطقية قراءة/كتابة.

**القيمة المرجعة:**
boolean
### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public abstract void setSizeLocked(boolean value)
```

يحدد ما إذا كان تغيير حجم هذا الشكل ممنوحًا. منطقية قراءة/كتابة.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |
### getEditPointsLocked() {#getEditPointsLocked--}
```
public abstract boolean getEditPointsLocked()
```

يحدد ما إذا كان التغيير المباشر لمحيط هذا الشكل ممنوحًا. منطقية قراءة/كتابة.

**القيمة المرجعة:**
boolean
### setEditPointsLocked(boolean value) {#setEditPointsLocked-boolean-}
```
public abstract void setEditPointsLocked(boolean value)
```

يحدد ما إذا كان التغيير المباشر لمحيط هذا الشكل ممنوحًا. منطقية قراءة/كتابة.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |
### getAdjustHandlesLocked() {#getAdjustHandlesLocked--}
```
public abstract boolean getAdjustHandlesLocked()
```

يحدد ما إذا كان تغيير قيم الضبط ممنوحًا. منطقية قراءة/كتابة.

**القيمة المرجعة:**
boolean
### setAdjustHandlesLocked(boolean value) {#setAdjustHandlesLocked-boolean-}
```
public abstract void setAdjustHandlesLocked(boolean value)
```

يحدد ما إذا كان تغيير قيم الضبط ممنوحًا. منطقية قراءة/كتابة.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |
### getArrowheadsLocked() {#getArrowheadsLocked--}
```
public abstract boolean getArrowheadsLocked()
```

يحدد ما وإذا كان تغيير رؤوس السهام ممنوحًا. منطقية قراءة/كتابة.

**القيمة المرجعة:**
boolean
### setArrowheadsLocked(boolean value) {#setArrowheadsLocked-boolean-}
```
public abstract void setArrowheadsLocked(boolean value)
```

يحدد ما وإذا كان تغيير رؤوس السهام ممنوحًا. منطقية قراءة/كتابة.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |
### getShapeTypeLocked() {#getShapeTypeLocked--}
```
public abstract boolean getShapeTypeLocked()
```

يحدد ما إذا كان تغيير نوع الشكل ممنوحًا. منطقية قراءة/كتابة.

**القيمة المرجعة:**
boolean
### setShapeTypeLocked(boolean value) {#setShapeTypeLocked-boolean-}
```
public abstract void setShapeTypeLocked(boolean value)
```

يحدد ما إذا كان تغيير نوع الشكل ممنوحًا. منطقية قراءة/كتابة.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | boolean |  |