---
title: SmartArtLayoutType
second_title: مرجع API لـ Aspose.Slides للـ Java
description: يمثل نوع التخطيط لرسم SmartArt.
type: docs
url: /ar/com.aspose.slides/smartartlayouttype/
---
**الوراثة:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class SmartArtLayoutType extends System.Enum
```

يمثل نوع تخطيط مخطط SmartArt.

## الحقول

| الحقل | الوصف |
| --- | --- |
| [AccentProcess](#AccentProcess) | يُستخدم لإظهار تقدم، أو خط زمني، أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. |
| [AccentedPicture](#AccentedPicture) | يُستخدم لإظهار فكرة مركزية، فوتوغرافية مع أفكار ذات صلة على الجانب. |
| [AlternatingFlow](#AlternatingFlow) | يُستخدم لإظهار مجموعات من المعلومات أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. |
| [AlternatingHexagons](#AlternatingHexagons) | يُستخدم لتمثيل سلسلة من الأفكار المترابطة. |
| [AlternatingPictureBlocks](#AlternatingPictureBlocks) | يُستخدم لإظهار سلسلة من الصور من الأعلى إلى الأسفل. |
| [AlternatingPictureCircles](#AlternatingPictureCircles) | يُستخدم لإظهار مجموعة من الصور مع النص. |
| [ArrowRibbon](#ArrowRibbon) | يُستخدم لإظهار مفاهيم مرتبطة أو متقابلة مع بعض الصلة، مثل القوى المتعارضة. |
| [AscendingPictureAccentProcess](#AscendingPictureAccentProcess) | يُستخدم لإظهار سلسلة تصاعدية من الصور مع نص وصفي. |
| [Balance](#Balance) | يُستخدم للمقارنة أو إظهار العلاقة بين فكرتين. |
| [BasicBendingProcess](#BasicBendingProcess) | يُستخدم لإظهار تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. |
| [BasicBlockList](#BasicBlockList) | يُستخدم لإظهار كتل معلومات غير متسلسلة أو مجمعة. |
| [BasicChevronProcess](#BasicChevronProcess) | يُستخدم لإظهار تقدم؛ أو خط زمني؛ أو خطوات متسلسلة في مهمة أو عملية أو سير عمل؛ أو لتأكيد الحركة أو الاتجاه. |
| [BasicCycle](#BasicCycle) | يُستخدم لتمثيل تسلسل مستمر للمراحل أو المهام أو الأحداث في تدفق دائري. |
| [BasicMatrix](#BasicMatrix) | يُستخدم لإظهار علاقة المكونات بالكل في أربعة أقسام. |
| [BasicPie](#BasicPie) | يُستخدم لإظهار كيف تشكل الأجزاء الفردية كلًا. |
| [BasicProcess](#BasicProcess) | يُستخدم لإظهار تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. |
| [BasicPyramid](#BasicPyramid) | يُستخدم لإظهار علاقات نسبية، مترابطة أو هرمية حيث يكون أكبر مكون في الأسفل ويتضيق صعودًا. |
| [BasicRadial](#BasicRadial) | يُستخدم لإظهار العلاقة بفكرة مركزية في دورة. |
| [BasicTarget](#BasicTarget) | يُستخدم لإظهار الاحتواء، الفروقات الطبقية، أو العلاقات الهرمية. |
| [BasicTimeline](#BasicTimeline) | يُستخدم لإظهار خطوات متسلسلة في مهمة أو عملية أو سير عمل، أو لعرض معلومات خط زمني. |
| [BasicVenn](#BasicVenn) | يُستخدم لإظهار علاقات متداخلة أو مترابطة. |
| [BendingPictureAccentList](#BendingPictureAccentList) | يُستخدم لإظهار كتل معلومات غير متسلسلة أو مجمعة. |
| [BendingPictureBlocks](#BendingPictureBlocks) | يُستخدم لإظهار سلسلة من الصور. |
| [BendingPictureCaption](#BendingPictureCaption) | يُستخدم لإظهار سلسلة متسلسلة من الصور. |
| [BendingPictureCaptionList](#BendingPictureCaptionList) | يُستخدم لإظهار سلسلة من الصور. |
| [BendingPictureSemiTransparentText](#BendingPictureSemiTransparentText) | يُستخدم لإظهار سلسلة من الصور. |
| [BlockCycle](#BlockCycle) | يُستخدم لتمثيل تسلسل مستمر للمراحل أو المهام أو الأحداث في تدفق دائري. |
| [BubblePictureList](#BubblePictureList) | يُستخدم لإظهار سلسلة من الصور. |
| [CaptionedPictures](#CaptionedPictures) | يُستخدم لإظهار صور ذات مستويات متعددة من النص. |
| [ChevronList](#ChevronList) | يُستخدم لإظهار تقدم عبر عدة عمليات تشكل سير عمل شامل. |
| [CircleAccentTimeline](#CircleAccentTimeline) | يُستخدم لإظهار سلسلة من الأحداث أو معلومات خط زمني. |
| [CircleArrowProcess](#CircleArrowProcess) | يُستخدم لإظهار عناصر متسلسلة مع نص داعم لكل عنصر. |
| [CirclePictureHierarchy](#CirclePictureHierarchy) | يُستخدم لإظهار معلومات هرمية أو علاقات تقارير في منظمة. |
| [CircleRelationship](#CircleRelationship) | يُستخدم لإظهار العلاقة إلى أو من فكرة مركزية. |
| [CircularBendingProcess](#CircularBendingProcess) | يُستخدم لإظهار تسلسل طويل أو غير خطي أو خطوات في مهمة أو عملية أو سير عمل. |
| [CircularPictureCallout](#CircularPictureCallout) | يُستخدم لإظهار فكرة مركزية وأفكار فرعية أو عناصر ذات صلة. |
| [ClosedChevronProcess](#ClosedChevronProcess) | يُستخدم لإظهار تقدم، أو خط زمني، أو خطوات متسلسلة في مهمة أو عملية أو سير عمل، أو لتأكيد الحركة أو الاتجاه. |
| [ContinuousArrowProcess](#ContinuousArrowProcess) | يُستخدم لإظهار خط زمني أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. |
| [ContinuousBlockProcess](#ContinuousBlockProcess) | يُستخدم لإظهار تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. |
| [ContinuousCycle](#ContinuousCycle) | يُستخدم لتمثيل تسلسل مستمر للمراحل أو المهام أو الأحداث في تدفق دائري. |
| [ContinuousPictureList](#ContinuousPictureList) | يُستخدم لإظهار مجموعات من المعلومات المترابطة. |
| [ConvergingArrows](#ConvergingArrows) | يُستخدم لإظهار أفكار أو مفاهيم تتقارب إلى نقطة مركزية. |
| [ConvergingRadial](#ConvergingRadial) | يُستخدم لإظهار علاقات المفاهيم أو المكونات بفكرة مركزية في دورة. |
| [CounterbalanceArrows](#CounterbalanceArrows) | يُستخدم لإظهار فكرتين أو مفهومين متعارضين. |
| [CycleMatrix](#CycleMatrix) | يُستخدم لإظهار العلاقة بفكرة مركزية في تقدم دوري. |
| [DescendingBlockList](#DescendingBlockList) | يُستخدم لإظهار مجموعات من الأفكار المرتبطة أو قوائم من المعلومات. |
| [DescendingProcess](#DescendingProcess) | يُستخدم لإظهار سلسلة هابطة من الأحداث. |
| [DetailedProcess](#DetailedProcess) | يُستخدم مع كميات كبيرة من النص من المستوى 2 لإظهار تقدم عبر المراحل. |
| [DivergingArrows](#DivergingArrows) | يُستخدم لإظهار أفكار أو مفاهيم تتقدم خارجيًا من مصدر مركزي. |
| [DivergingRadial](#DivergingRadial) | يُستخدم لإظهار علاقة بفكرة مركزية في دورة. |
| [Equation](#Equation) | يُستخدم لإظهار خطوات أو مهام متسلسلة تصوّر خطة أو نتيجة. |
| [FramedTextPicture](#FramedTextPicture) | يُستخدم لإظهار صور مع نص المستوى 1 المقابل معروض في إطار. |
| [Funnel](#Funnel) | يُستخدم لإظهار تصفية المعلومات أو كيفية دمج الأجزاء لتشكل كلًا. |
| [Gear](#Gear) | يُستخدم لإظهار أفكار متشابكة. |
| [GridMatrix](#GridMatrix) | يُستخدم لإظهار وضعية المفاهيم على محورين. |
| [GroupedList](#GroupedList) | يُستخدم لإظهار مجموعات ومجموعات فرعية من المعلومات، أو خطوات وخطوات فرعية في مهمة أو عملية أو سير عمل. |
| [HalfCircleOrganizationChart](#HalfCircleOrganizationChart) | يُستخدم لإظهار معلومات هرمية أو علاقات تقارير في منظمة. |
| [HexagonCluster](#HexagonCluster) | يُستخدم لإظهار صور مع نص وصفي مرتبط. |
| [Hierarchy](#Hierarchy) | يُستخدم لإظهار علاقات هرمية تتطور من الأعلى إلى الأسفل. |
| [HierarchyList](#HierarchyList) | يُستخدم لإظهار علاقات هرمية تتطور عبر المجموعات. |
| [HorizontalBulletList](#HorizontalBulletList) | يُستخدم لإظهار قوائم معلومات غير متسلسلة أو مجمعة. |
| [HorizontalHierarchy](#HorizontalHierarchy) | يُستخدم لإظهار علاقات هرمية تتطور أفقيًا. |
| [HorizontalLabeledHierarchy](#HorizontalLabeledHierarchy) | يُستخدم لإظهار علاقات هرمية تتطور أفقيًا ومجموعة هرميًا. |
| [HorizontalMultiLevelHierarchy](#HorizontalMultiLevelHierarchy) | يُستخدم لإظهار كميات كبيرة من المعلومات الهرمية تتطور أفقيًا. |
| [HorizontalOrganizationChart](#HorizontalOrganizationChart) | يُستخدم لإظهار معلومات هرمية أفقيًا أو علاقات تقارير في منظمة. |
| [HorizontalPictureList](#HorizontalPictureList) | يُستخدم لإظهار معلومات غير متسلسلة أو مجمعة مع التركيز على الصور المرتبطة. |
| [IncreasingArrowsProcess](#IncreasingArrowsProcess) | يُستخدم لإظهار خطوات متسلسلة ومتداخلة في عملية. |
| [IncreasingCircleProcess](#IncreasingCircleProcess) | يُستخدم لإظهار سلسلة من الخطوات، حيث يزداد داخل الدائرة مع كل خطوة. |
| [InvertedPyramid](#InvertedPyramid) | يُستخدم لإظهار علاقات نسبية، مترابطة أو هرمية حيث يكون أكبر مكون في الأعلى ويتضيق نزولًا. |
| [LabeledHierarchy](#LabeledHierarchy) | يُستخدم لإظهار علاقات هرمية تتطور من الأعلى إلى الأسفل ومجمعة هرميًا. |
| [LinearVenn](#LinearVenn) | يُستخدم لإظهار علاقات متداخلة في تسلسل. |
| [LinedList](#LinedList) | يُستخدم لإظهار كميات كبيرة من النص مقسمة إلى فئات وفئات فرعية. |
| [MultidirectionalCycle](#MultidirectionalCycle) | يُستخدم لتمثيل تسلسل مستمر للمراحل أو المهام أو الأحداث التي يمكن أن تحدث في أي اتجاه. |
| [NameandTitleOrganizationChart](#NameandTitleOrganizationChart) | يُستخدم لإظهار معلومات هرمية أو علاقات تقارير في منظمة. |
| [NestedTarget](#NestedTarget) | يُستخدم لإظهار علاقات الاحتواء. |
| [NondirectionalCycle](#NondirectionalCycle) | يُستخدم لتمثيل تسلسل مستمر للمراحل أو المهام أو الأحداث في تدفق دائري. |
| [OpposingArrows](#OpposingArrows) | يُستخدم لإظهار فكرتين متعارضتين، أو أفكار تتباعد عن نقطة مركزية. |
| [OpposingIdeas](#OpposingIdeas) | يُستخدم لإظهار فكرتين متعارضتين أو متضادتين. |
| [OrganizationChart](#OrganizationChart) | يُستخدم لإظهار معلومات هرمية أو علاقات تقارير في منظمة. |
| [PhasedProcess](#PhasedProcess) | يُستخدم لإظهار ثلاث مراحل من عملية. |
| [PictureAccentBlocks](#PictureAccentBlocks) | يُستخدم لإظهار مجموعة من الصور في كتل تبدأ من الزاوية. |
| [PictureAccentList](#PictureAccentList) | يُستخدم لإظهار معلومات مجمعة أو ذات صلة. |
| [PictureAccentProcess](#PictureAccentProcess) | يُستخدم لإظهار تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. |
| [PictureCaptionList](#PictureCaptionList) | يُستخدم لإظهار كتل معلومات غير متسلسلة أو مجمعة. |
| [PictureGrid](#PictureGrid) | يُستخدم لإظهار صور مرتبة على شبكة مربعة. |
| [PictureLineup](#PictureLineup) | يُستخدم لإظهار سلسلة من الصور جنبًا إلى جنب. |
| [PictureStrips](#PictureStrips) | يُستخدم لإظهار سلسلة من الصور من الأعلى إلى الأسفل مع نص المستوى 1 بجانب كل واحدة. |
| [PieProcess](#PieProcess) | يُستخدم لإظهار خطوات في عملية مع زيادة حجم كل شريحة دائرية حتى سبعة أشكال. |
| [PlusandMinus](#PlusandMinus) | يُستخدم لإظهار إيجابيات وسلبيات فكرتين. |
| [ProcessArrows](#ProcessArrows) | يُستخدم لإظهار معلومات توضح عملية أو سير عمل. |
| [ProcessList](#ProcessList) | يُستخدم لإظهار مجموعات متعددة من المعلومات أو خطوات وخطوات فرعية في مهمة أو عملية أو سير عمل. |
| [PyramidList](#PyramidList) | يُستخدم لإظهار علاقات نسبية، مترابطة أو هرمية. |
| [RadialCluster](#RadialCluster) | يُستخدم لإظهار بيانات تتعلق بفكرة أو موضوع مركزي. |
| [RadialCycle](#RadialCycle) | يُستخدم لإظهار العلاقة بفكرة مركزية. |
| [RadialList](#RadialList) | يُستخدم لإظهار علاقة بفكرة مركزية في دورة. |
| [RadialVenn](#RadialVenn) | يُستخدم لإظهار كل من العلاقات المتداخلة والعلاقة بفكرة مركزية في دورة. |
| [RandomToResultProcess](#RandomToResultProcess) | يُستخدم لإظهار، عبر سلسلة من الخطوات، كيف يمكن لعدة أفكار فوضوية أن تؤدي إلى هدف أو فكرة موحدة. |
| [RepeatingBendingProcess](#RepeatingBendingProcess) | يُستخدم لإظهار تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. |
| [ReverseList](#ReverseList) | يُستخدم للتنقل بين عنصرين. |
| [SegmentedCycle](#SegmentedCycle) | يُستخدم لإظهار تقدم أو تسلسل من المراحل أو المهام أو الأحداث في تدفق دائري. |
| [SegmentedProcess](#SegmentedProcess) | يُستخدم لإظهار تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. |
| [SegmentedPyramid](#SegmentedPyramid) | يُستخدم لإظهار علاقات احتواء أو نسبية أو مترابطة. |
| [SnapshotPictureList](#SnapshotPictureList) | يُستخدم لإظهار صور مع نص توضيحي. |
| [SpiralPicture](#SpiralPicture) | يُستخدم لإظهار سلسلة من خمس صور كحد أقصى مع تعليقات المستوى 1 المقابلة التي تتجول إلى المركز. |
| [SquareAccentList](#SquareAccentList) | يُستخدم لإظهار قوائم من المعلومات مقسمة إلى فئات. |
| [StackedList](#StackedList) | يُستخدم لإظهار مجموعات من المعلومات أو خطوات في مهمة أو عملية أو سير عمل. |
| [StackedVenn](#StackedVenn) | يُستخدم لإظهار علاقات متداخلة. |
| [StaggeredProcess](#StaggeredProcess) | يُستخدم لإظهار تقدم نزولي عبر المراحل. |
| [StepDownProcess](#StepDownProcess) | يُستخدم لإظهار عملية هابطة مع خطوات متعددة وخطوات فرعية. |
| [StepUpProcess](#StepUpProcess) | يُستخدم لإظهار سلسلة تصاعدية من الخطوات أو قوائم المعلومات. |
| [SubStepProcess](#SubStepProcess) | يُستخدم لإظهار عملية متعددة الخطوات مع خطوات فرعية بين كل حالة من نص المستوى 1. |
| [TableHierarchy](#TableHierarchy) | يُستخدم لإظهار مجموعات من المعلومات مبنية من الأعلى إلى الأسفل، والهياكل الهرمية داخل كل مجموعة. |
| [TableList](#TableList) | يُستخدم لإظهار معلومات مجمعة أو ذات صلة ذات قيمة متساوية. |
| [TargetList](#TargetList) | يُستخدم لإظهار معلومات مترابطة أو متداخلة. |
| [TextCycle](#TextCycle) | يُستخدم لتمثيل تسلسل مستمر للمراحل أو المهام أو الأحداث في تدفق دائري. |
| [TitlePictureLineup](#TitlePictureLineup) | يُستخدم لإظهار سلسلة من الصور التي لكل منها عنوانه ووصفه الخاص. |
| [TitledMatrix](#TitledMatrix) | يُستخدم لإظهار علاقات الأرباع الأربعة بالكل. |
| [TitledPictureAccentList](#TitledPictureAccentList) | يُستخدم لإظهار قوائم من المعلومات مع صورة مميزة لكل نص من المستوى 2. |
| [TitledPictureBlocks](#TitledPictureBlocks) | يُستخدم لإظهار سلسلة من الصور. |
| [TrapezoidList](#TrapezoidList) | يُستخدم لإظهار معلومات مجمعة أو ذات صلة ذات قيمة متساوية. |
| [UpwardArrow](#UpwardArrow) | يُستخدم لإظهار تقدم أو خطوات تتجه صعودًا في مهمة أو عملية أو سير عمل. |
| [VerticalAccentList](#VerticalAccentList) | يُستخدم لإظهار قوائم من المعلومات. |
| [VerticalArrowList](#VerticalArrowList) | يُستخدم لإظهار تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل تتحرك نحو هدف مشترك. |
| [VerticalBendingProcess](#VerticalBendingProcess) | يُستخدم لإظهار تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. |
| [VerticalBlockList](#VerticalBlockList) | يُستخدم لإظهار مجموعات من المعلومات أو خطوات في مهمة أو عملية أو سير عمل. |
| [VerticalBoxList](#VerticalBoxList) | يُستخدم لإظهار عدة مجموعات من المعلومات، خاصة المجموعات التي تحتوي على كميات كبيرة من نص المستوى 2. |
| [VerticalBulletList](#VerticalBulletList) | يُستخدم لإظهار كتل معلومات غير متسلسلة أو مجمعة. |
| [VerticalChevronList](#VerticalChevronList) | يُستخدم لإظهار تقدم، أو خطوات متسلسلة في مهمة أو عملية أو سير عمل، أو لتأكيد الحركة أو الاتجاه. |
| [VerticalCircleList](#VerticalCircleList) | يُستخدم لإظهار بيانات متسلسلة أو مجمعة. |
| [VerticalCurvedList](#VerticalCurvedList) | يُستخدم لإظهار قائمة من المعلومات على شكل منحنى. |
| [VerticalEquation](#VerticalEquation) | يُستخدم لإظهار خطوات أو مهام متسلسلة تصوّر خطة أو نتيجة. |
| [VerticalPictureAccentList](#VerticalPictureAccentList) | يُستخدم لإظهار كتل معلومات غير متسلسلة أو مجمعة. |
| [VerticalPictureList](#VerticalPictureList) | يُستخدم لإظهار كتل معلومات غير متسلسلة أو مجمعة. |
| [VerticalProcess](#VerticalProcess) | يُستخدم لإظهار تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل من الأعلى إلى الأسفل. |
| [Custom](#Custom) | يمثل مخطط SmartArt بقالب تخطيط مخصص |
| [PictureOrganizationChart](#PictureOrganizationChart) | يُستخدم لإظهار معلومات هرمية أو علاقات تقارير في منظمة، مع صور مقابلة. |

### AccentProcess {#AccentProcess}
```
public static final int AccentProcess
```

استخدم لعرض تقدم، أو خط زمني، أو خطوات متسلسلة في مهمة، عملية، أو سير عمل. يعمل بشكل جيد لتوضيح كل من النص Level 1 والنص Level 2.

### AccentedPicture {#AccentedPicture}
```
public static final int AccentedPicture
```

استخدم لعرض فكرة تصويرية مركزية مع أفكار ذات صلة على الجانب. يظهر نص المستوى 1 العلوي فوق الصورة المركزية. يظهر النص المقابل للأشكال الأخرى من المستوى 1 بجانب الصور الدائرية الصغيرة. هذا التخطيط يعمل جيدًا أيضًا بدون نص.

### AlternatingFlow {#AlternatingFlow}
```
public static final int AlternatingFlow
```

استخدم لعرض مجموعات من المعلومات أو خطوات متسلسلة في مهمة، عملية، أو سير عمل. يبرز التفاعل أو العلاقات بين مجموعات المعلومات.

### AlternatingHexagons {#AlternatingHexagons}
```
public static final int AlternatingHexagons
```

استخدم لتمثيل سلسلة من الأفكار المترابطة. يظهر نص المستوى 1 داخل السداسيات. يظهر نص المستوى 2 خارج الأشكال.

### AlternatingPictureBlocks {#AlternatingPictureBlocks}
```
public static final int AlternatingPictureBlocks
```

استخدم لعرض سلسلة من الصور من الأعلى إلى الأسفل. يظهر النص بالتناوب على اليمين أو اليسار من الصورة.

### AlternatingPictureCircles {#AlternatingPictureCircles}
```
public static final int AlternatingPictureCircles
```

استخدم لعرض مجموعة من الصور مع النص. يظهر النص المقابل في الدوائر المركزية مع تناوب الصور من اليسار إلى اليمين.

### ArrowRibbon {#ArrowRibbon}
```
public static final int ArrowRibbon
```

استخدم لعرض مفاهيم ذات صلة أو متقابلة مع بعض الاتصال، مثل القوى المتعارضة. تُستخدم السطران الأولان من نص المستوى 1 للنص داخل الأسهم. لا يظهر النص غير المستخدم، لكنه يظل متاحًا إذا قمت بتغيير التخطيطات.

### AscendingPictureAccentProcess {#AscendingPictureAccentProcess}
```
public static final int AscendingPictureAccentProcess
```

استخدم لعرض سلسلة صاعدة من الصور مع نص وصفي. يعمل بشكل أفضل مع كمية قليلة من النص.

### Balance {#Balance}
```
public static final int Balance
```

استخدم للمقارنة أو إظهار العلاقة بين فكرتين. كل من السطرين الأولين من نص المستوى 1 يتطابق مع نص في أعلى أحد جانبي نقطة المركز. يبرز نص المستوى 2، وهو مقيد بأربع أشكال على كل جانب من نقطة المركز. تميل الموازنة نحو الجانب الذي يحتوي على أكبر عدد من الأشكال التي تحمل نص المستوى 2. لا يظهر النص غير المستخدم، لكنه يظل متاحًا إذا قمت بتغيير التخطيطات.

### BasicBendingProcess {#BasicBendingProcess}
```
public static final int BasicBendingProcess
```

استخدم لعرض تقدم أو خطوات متسلسلة في مهمة، عملية، أو سير عمل. يحدّث أقصى استفادة من مساحة العرض الأفقية والعمودية للأشكال.

### BasicBlockList {#BasicBlockList}
```
public static final int BasicBlockList
```

استخدم لعرض كتل معلومات غير متسلسلة أو مجموعات. يحدّث أقصى استفادة من مساحة العرض الأفقية والعمودية للأشكال.

### BasicChevronProcess {#BasicChevronProcess}
```
public static final int BasicChevronProcess
```

استخدم لعرض تقدم؛ خط زمني؛ خطوات متسلسلة في مهمة، عملية، أو سير عمل؛ أو لتأكيد الحركة أو الاتجاه. يظهر نص المستوى 1 داخل شكل السهم بينما يظهر نص المستوى 2 أسفل أشكال السهام.

### BasicCycle {#BasicCycle}
```
public static final int BasicCycle
```

استخدم لتمثيل تسلسل مستمر لمراحل أو مهام أو أحداث في تدفق دائري. يبرز المراحل أو الخطوات بدلاً من الأسهم أو التدفق الرابط. يعمل بشكل أفضل مع نص المستوى 1 فقط.

### BasicMatrix {#BasicMatrix}
```
public static final int BasicMatrix
```

استخدم لعرض علاقة المكوّنات بالكل في أرباع. تظهر الأسطر الأربعة الأولى من نص المستوى 1 في الأرباع. لا يظهر النص غير المستخدم، لكنه يظل متاحًا إذا قمت بتغيير التخطيطات.

### BasicPie {#BasicPie}
```
public static final int BasicPie
```

استخدم لإظهار كيف تشكل الأجزاء الفردية كلًا. تتطابق الأسطر السبعة الأولى من نص المستوى 1 مع الشرائح أو أشكال الفطيرة الموزعة بالتساوي. يظهر شكل نص المستوى 1 العلوي خارج بقية الفطيرة للتأكيد. لا يظهر النص غير المستخدم، لكنه يظل متاحًا إذا قمت بتغيير التخطيطات.

### BasicProcess {#BasicProcess}
```
public static final int BasicProcess
```

استخدم لعرض تقدم أو خطوات متسلسلة في مهمة، عملية، أو سير عمل.

### BasicPyramid {#BasicPyramid}
```
public static final int BasicPyramid
```

استخدم لعرض علاقات نسبية، مترابطة، أو هرمية مع أكبر مكوّن في القاع ويتضاؤل نحو الأعلى. يظهر نص المستوى 1 في أقسام الهرم ويظهر نص المستوى 2 في الأشكال بجانب كل قسم.

### BasicRadial {#BasicRadial}
```
public static final int BasicRadial
```

استخدم لعرض العلاقة بفكرة مركزية في دورة. السطر الأول من نص المستوى 1 يتطابق مع الشكل المركزي، ونص المستوى 2 يتطابق مع الأشكال الدائرية المحيطة. لا يظهر النص غير المستخدم، لكنه يظل متاحًا إذا قمت بتغيير التخطيطات.

### BasicTarget {#BasicTarget}
```
public static final int BasicTarget
```

استخدم لعرض الاحتواء، التدرجات، أو العلاقات الهرمية. تت关联 الأسطر الخمسة الأولى من نص المستوى 1 بدائرة. لا يظهر النص غير المستخدم، لكنه يظل متاحًا إذا قمت بتغيير التخطيطات.

### BasicTimeline {#BasicTimeline}
```
public static final int BasicTimeline
```

استخدم لعرض خطوات متسلسلة في مهمة، عملية، أو سير عمل، أو لإظهار معلومات خط زمني. يعمل جيدًا مع كل من نص المستوى 1 ونص المستوى 2.

### BasicVenn {#BasicVenn}
```
public static final int BasicVenn
```

استخدم لإظهار علاقات متداخلة أو مترابطة. تتطابق الأسطر السبعة الأولى من نص المستوى 1 مع دائرة. إذا كان عدد أسطر نص المستوى 1 أربعة أو أقل، يكون النص داخل الدوائر. إذا كان أكثر من أربعة أسطر، يكون النص خارج الدوائر. لا يظهر النص غير المستخدم، لكنه يظل متاحًا إذا قمت بتغيير التخطيطات.

### BendingPictureAccentList {#BendingPictureAccentList}
```
public static final int BendingPictureAccentList
```

استخدم لعرض كتل معلومات غير متسلسلة أو مجموعات. تم تصميم الأشكال الدائرية الصغيرة لاحتواء الصور. يعمل جيدًا لتوضيح كل من نص المستوى 1 ونص المستوى 2. يحدّث أقصى استفادة من مساحة العرض الأفقية والعمودية للأشكال.

### BendingPictureBlocks {#BendingPictureBlocks}
```
public static final int BendingPictureBlocks
```

استخدم لعرض سلسلة من الصور. يمكن للصندوق الذي يغطي الزاوية السفلية أن يحتوي على كمية صغيرة من النص.

### BendingPictureCaption {#BendingPictureCaption}
```
public static final int BendingPictureCaption
```

استخدم لعرض سلسلة متسلسلة من الصور. يمكن للصندوق الذي يغطي الزاوية السفلية أن يحتوي على كمية صغيرة من النص.

### BendingPictureCaptionList {#BendingPictureCaptionList}
```
public static final int BendingPictureCaptionList
```

استخدم لعرض سلسلة من الصور. يظهر العنوان والوصف في شكل توضيحي تحت كل صورة.

### BendingPictureSemiTransparentText {#BendingPictureSemiTransparentText}
```
public static final int BendingPictureSemiTransparentText
```

استخدم لعرض سلسلة من الصور. يغطي صندوق شبه شفاف الجزء السفلي من الصورة ويحتوي على جميع مستويات النص.

### BlockCycle {#BlockCycle}
```
public static final int BlockCycle
```

استخدم لتمثيل تسلسل مستمر لمراحل أو مهام أو أحداث في تدفق دائري. يبرز المراحل أو الخطوات بدلاً من الأسهم أو التدفق الرابط.

### BubblePictureList {#BubblePictureList}
```
public static final int BubblePictureList
```

استخدم لعرض سلسلة من الصور. يمكن أن يحتوي على ما يصل إلى ثمانية صور من المستوى 1. لا يظهر النص أو الصور غير المستخدمة، لكنها تظل متاحة إذا قمت بتغيير التخطيطات. يعمل بشكل أفضل مع كميات صغيرة من النص.

### CaptionedPictures {#CaptionedPictures}
```
public static final int CaptionedPictures
```

استخدم لعرض صور مع مستويات متعددة من النص. يعمل بشكل أفضل مع كمية قليلة من نص المستوى 1 وكمية متوسطة من نص المستوى 2.

### ChevronList {#ChevronList}
```
public static final int ChevronList
```

استخدم لعرض تقدم عبر عدة عمليات تشكل سير عمل شامل. يعمل أيضًا لتوضيح عمليات متقابلة. يتطابق نص المستوى 1 مع أول شكل سهم على اليسار، بينما يتطابق نص المستوى 2 مع الخطوات الفرعية الأفقية لكل شكل يحتوي على نص المستوى 1.

### CircleAccentTimeline {#CircleAccentTimeline}
```
public static final int CircleAccentTimeline
```

استخدم لعرض سلسلة من الأحداث أو معلومات خط زمني. يظهر نص المستوى 1 بجوار الأشكال الدائرية الكبيرة. يظهر نص المستوى 2 بجوار الأشكال الدائرية الصغيرة.

### CircleArrowProcess {#CircleArrowProcess}
```
public static final int CircleArrowProcess
```

استخدم لعرض عناصر متسلسلة مع نص داعم لكل عنصر. يعمل هذا المخطط بشكل أفضل مع كمية صغيرة من نص المستوى 1.

### CirclePictureHierarchy {#CirclePictureHierarchy}
```
public static final int CirclePictureHierarchy
```

استخدم لعرض معلومات هرمية أو علاقات تقارير في منظمة. تظهر الصور في دوائر ويظهر النص المقابل بجوار الصور.

### CircleRelationship {#CircleRelationship}
```
public static final int CircleRelationship
```

استخدم لعرض العلاقة إلى أو من فكرة مركزية. يضاف نص المستوى 2 غير المتسلسل وهو مقيد بخمس عناصر. لا يمكن أن يكون هناك سوى عنصر واحد من المستوى 1.

### CircularBendingProcess {#CircularBendingProcess}
```
public static final int CircularBendingProcess
```

استخدم لعرض سلسلة طويلة أو غير خطية من الخطوات في مهمة، عملية، أو سير عمل. يعمل بشكل أفضل مع نص المستوى 1 فقط. يحدّث أقصى استفادة من مساحة العرض الأفقية والعمودية للأشكال.

### CircularPictureCallout {#CircularPictureCallout}
```
public static final int CircularPictureCallout
```

استخدم لعرض فكرة مركزية وأفكار فرعية أو عناصر ذات صلة. يغطي النص للصورة الأولى الجزء السفلي من الصورة. يظهر النص المقابل للأشكال الأخرى من المستوى 1 بجانب الصور الدائرية الصغيرة. يعمل هذا المخطط أيضًا جيدًا بدون نص.

### ClosedChevronProcess {#ClosedChevronProcess}
```
public static final int ClosedChevronProcess
```

استخدم لعرض تقدم، أو خط زمني، أو خطوات متسلسلة في مهمة، عملية، أو سير عمل، أو لتأكيد الحركة أو الاتجاه. يمكن استخدامه لتأكيد المعلومات في الشكل الابتدائي. يعمل بشكل أفضل مع نص المستوى 1 فقط.

### ContinuousArrowProcess {#ContinuousArrowProcess}
```
public static final int ContinuousArrowProcess
```

استخدم لعرض خط زمني أو خطوات متسلسلة في مهمة، عملية، أو سير عمل. يعمل بشكل أفضل مع نص المستوى 1 لأن كل سطر من نص المستوى 1 يظهر داخل شكل السهم. يظهر نص المستوى 2 خارج شكل السهم.

### ContinuousBlockProcess {#ContinuousBlockProcess}
```
public static final int ContinuousBlockProcess
```

استخدم لعرض تقدم أو خطوات متسلسلة في مهمة، عملية، أو سير عمل. يعمل بشكل أفضل مع نص المستوى 1 ومستوى 2 قليلين.

### ContinuousCycle {#ContinuousCycle}
```
public static final int ContinuousCycle
```

استخدم لتمثيل تسلسل مستمر لمراحل أو مهام أو أحداث في تدفق دائري. يبرز الاتصال بين جميع المكوّنات. يعمل بشكل أفضل مع نص المستوى 1 فقط.

### ContinuousPictureList {#ContinuousPictureList}
```
public static final int ContinuousPictureList
```

استخدم لعرض مجموعات من المعلومات المترابطة. تم تصميم الأشكال الدائرية لاحتواء الصور.

### ConvergingArrows {#ConvergingArrows}
```
public static final int ConvergingArrows
```

استخدم لعرض أفكار أو مفاهيم تتقارب إلى نقطة مركزية. يعمل بشكل أفضل مع نص المستوى 1 فقط.

### ConvergingRadial {#ConvergingRadial}
```
public static final int ConvergingRadial
```

استخدم لعرض علاقات المفاهيم أو المكوّنات بفكرة مركزية في دورة. السطر الأول من نص المستوى 1 يتطابق مع الشكل الدائري المركزي، وأسطر نص المستوى 2 تتطابق مع الأشكال المستطيلة المحيطة. لا يظهر النص غير المستخدم، لكنه يظل متاحًا إذا قمت بتغيير التخطيطات.

### CounterbalanceArrows {#CounterbalanceArrows}
```
public static final int CounterbalanceArrows
```

استخدم لعرض فكرتين متعارضتين أو مفهومين. كل من السطرين الأولين من نص المستوى 1 يتطابق مع سهم ويعمل جيدًا مع نص المستوى 2. لا يظهر النص غير المستخدم، لكنه يظل متاحًا إذا قمت بتغيير التخطيطات.

### CycleMatrix {#CycleMatrix}
```
public static final int CycleMatrix
```

استخدم لعرض العلاقة بفكرة مركزية في تقدم دوري. كل من الأسطر الأربعة الأولى من نص المستوى 1 يتطابق مع شريحة أو شكل فطيرة، ويظهر نص المستوى 2 في شكل مستطيل بجانب الشريحة أو الفطيرة. لا يظهر النص غير المستخدم، لكنه يظل متاحًا إذا قمت بتغيير التخطيطات.

### DescendingBlockList {#DescendingBlockList}
```
public static final int DescendingBlockList
```

استخدم لعرض مجموعات من الأفكار المرتبطة أو قوائم المعلومات. تنخفض أشكال النص في الارتفاع بشكل متسلسل، ويظهر نص المستوى 1 عموديًا.

### DescendingProcess {#DescendingProcess}
```
public static final int DescendingProcess
```

استخدم لعرض سلسلة متناقصة من الأحداث. نص المستوى 1 الأول في أعلى السهم، والنص الأخير من المستوى 1 يظهر في أسفل السهم. تظهر فقط أول سبعة عناصر من المستوى 1. يعمل بشكل أفضل مع كميات نص صغيرة إلى متوسطة.

### DetailedProcess {#DetailedProcess}
```
public static final int DetailedProcess
```

استخدم مع كميات كبيرة من نص المستوى 2 لإظهار تقدم عبر المراحل.

### DivergingArrows {#DivergingArrows}
```
public static final int DivergingArrows
```

استخدم لعرض أفكار أو مفاهيم تتقدم إلى الخارج من مصدر مركزي. يعمل بشكل أفضل مع نص المستوى 1 فقط.

### DivergingRadial {#DivergingRadial}
```
public static final int DivergingRadial
```
Use to show relationships to a central idea in a cycle. The first Level 1 line of text corresponds to the central circular shape. Emphasizes the surrounding circles rather than the central idea. Unused text does not appear, but remains available if you switch layouts.

### Equation {#Equation}
```
public static final int Equation
```


Use to show sequential steps or tasks that depict a plan or result. The last Level 1 line of text appears after the equals sign (=).Works best with Level 1 text only.

### FramedTextPicture {#FramedTextPicture}
```
public static final int FramedTextPicture
```


Use to show pictures with corresponding Level 1 text displayed in a frame.

### Funnel {#Funnel}
```
public static final int Funnel
```


Use to show the filtering of information or how parts merge into a whole. Emphasizes the final outcome. Can contain up to four lines of Level 1 text; the last of these four Level 1 text lines appears below the funnel and the other lines correspond to a circular shape. Unused text does not appear, but remains available if you switch layouts.

### Gear {#Gear}
```
public static final int Gear
```


Use to show interlocking ideas. Each of the first three lines of Level 1 text corresponds to a gear shape, and their corresponding Level 2 text appears in rectangles next to the gear shape. Unused text does not appear, but remains available if you switch layouts.

### GridMatrix {#GridMatrix}
```
public static final int GridMatrix
```


Use to show the placement of concepts along two axes. Emphasizes the individual components rather than the whole. The first four lines of Level 1 text appear in the quadrants. Unused text does not appear, but remains available if you switch layouts.

### GroupedList {#GroupedList}
```
public static final int GroupedList
```


Use to show groups and sub-groups of information, or steps and sub-steps in a task, process, or workflow. Level 1 text corresponds to the top level horizontal shapes, and Level 2 text corresponds to vertical sub-steps under each related top level shape. Works well for emphasizing sub-groups or sub-steps, hierarchical information, or multiple lists of information.

### HalfCircleOrganizationChart {#HalfCircleOrganizationChart}
```
public static final int HalfCircleOrganizationChart
```


Use to show hierarchical information or reporting relationships in an organization. The assistant shapes and Org Chart hanging layouts are available with this layout.

### HexagonCluster {#HexagonCluster}
```
public static final int HexagonCluster
```


Use to show pictures with associated descriptive text. Small hexagons indicate the picture and text pair. Works best with small amounts of text.

### Hierarchy {#Hierarchy}
```
public static final int Hierarchy
```


Use to show hierarchical relationships progressing from top to bottom.

### HierarchyList {#HierarchyList}
```
public static final int HierarchyList
```


Use to show hierarchical relationships progressing across groups. Can also be used to group or list information.

### HorizontalBulletList {#HorizontalBulletList}
```
public static final int HorizontalBulletList
```


Use to show non-sequential or grouped lists of information. Works well with large amounts of text. All text has the same level of emphasis, and direction is not implied.

### HorizontalHierarchy {#HorizontalHierarchy}
```
public static final int HorizontalHierarchy
```


Use to show hierarchical relationships progressing horizontally. Works well for decision trees.

### HorizontalLabeledHierarchy {#HorizontalLabeledHierarchy}
```
public static final int HorizontalLabeledHierarchy
```


Use to show hierarchical relationships progressing horizontally and grouped hierarchically. Emphasizes heading or level 1 text. The first line of Level 1 text appears in the shape at the beginning of the hierarchy, and the second and all subsequent lines of Level 1 text appear at the top of the tall rectangles.

### HorizontalMultiLevelHierarchy {#HorizontalMultiLevelHierarchy}
```
public static final int HorizontalMultiLevelHierarchy
```


Use to show large amounts of hierarchical information progressing horizontally. The top of the hierarchy is displayed vertically. This layout supports many levels in the hierarchy.

### HorizontalOrganizationChart {#HorizontalOrganizationChart}
```
public static final int HorizontalOrganizationChart
```


Use to show hierarchical information horizontally or reporting relationships in an organization. The assistant shape and the Org Chart hanging layouts are available with this layout.

### HorizontalPictureList {#HorizontalPictureList}
```
public static final int HorizontalPictureList
```


Use to show non-sequential or grouped information with an emphasis on related pictures. The top shapes are designed to contain pictures.

### IncreasingArrowsProcess {#IncreasingArrowsProcess}
```
public static final int IncreasingArrowsProcess
```


Use to show sequential and overlapping steps in a process. Limited to five Level 1 items. Level 2 can contain large amounts of text.

### IncreasingCircleProcess {#IncreasingCircleProcess}
```
public static final int IncreasingArrowsProcess
```


Use to show a series of steps, with the interior of the circle increasing with each step. Limited to seven Level 1 steps but unlimited Level 2 items. Works well with large amounts of Level 2 text.

### InvertedPyramid {#InvertedPyramid}
```
public static final int InvertedPyramid
```


Use to show proportional, interconnected, or hierarchical relationships with the largest component on the top and narrowing down. Level 1 text appears in the pyramid segments and Level 2 text appears in shapes alongside each segment.

### LabeledHierarchy {#LabeledHierarchy}
```
public static final int LabeledHierarchy
```


Use to show hierarchical relationships progressing from top to bottom and grouped hierarchically. Emphasizes heading or level 1 text. The first line of Level 1 text appears in the shape at the beginning of the hierarchy, and all subsequent lines of Level 1 text appear to the left of the long rectangles.

### LinearVenn {#LinearVenn}
```
public static final int LinearVenn
```


Use to show overlapping relationships in a sequence. Works best with Level 1 text only.

### LinedList {#LinedList}
```
public static final int LinedList
```


Use to show large amounts of text divided into categories and subcategories. Works well with multiple levels of text. Text at the same level is separated by lines.

### MultidirectionalCycle {#MultidirectionalCycle}
```
public static final int MultidirectionalCycle
```


Use to represent a continuing sequence of stages, tasks, or events that can occur in any direction.

### NameandTitleOrganizationChart {#NameandTitleOrganizationChart}
```
public static final int NameandTitleOrganizationChart
```


Use to show hierarchical information or reporting relationships in an organization. To enter text in the title box, type directly in the smaller rectangular shape. The assistant shape and Org Chart hanging layouts are available with this layout.

### NestedTarget {#NestedTarget}
```
public static final int NestedTarget
```


Use to show containment relationships. Each of the first three lines of Level 1 text correspond to the upper left text in the shapes, and Level 2 text corresponds to the smaller shapes. Works best with minimal Level 2 lines of text. Unused text does not appear, but remains available if you switch layouts.

### NondirectionalCycle {#NondirectionalCycle}
```
public static final int NondirectionalCycle
```


Use to represent a continuing sequence of stages, tasks, or events in a circular flow. Each shape has the same level of importance. Works well when direction does not need to be indicated.

### OpposingArrows {#OpposingArrows}
```
public static final int OpposingArrows
```


Use to show two opposing ideas, or ideas that diverge from a central point. Each of the first two lines of Level 1 text corresponds to an arrow. Unused text does not appear, but remains available if you switch layouts.

### OpposingIdeas {#OpposingIdeas}
```
public static final int OpposingIdeas
```


Use to show two opposing or contrasting ideas. Can have one or two Level 1 items. Each Level 1 text can contain multiple sub-levels. Works well with large amounts of text.

### OrganizationChart {#OrganizationChart}
```
public static final int OrganizationChart
```


Use to show hierarchical information or reporting relationships in an organization. The assistant shape and the Org Chart hanging layouts are available with this layout.

### PhasedProcess {#PhasedProcess}
```
public static final int PhasedProcess
```


Use to show three phases of a process. Limited to three Level 1 items. The first two Level 1 items can each contain four Level 2 items, and the third Level 1 item can contain an unlimited number of Level 2 items. Works best with small amounts of text.

### PictureAccentBlocks {#PictureAccentBlocks}
```
public static final int PictureAccentBlocks
```


Use to show a group of pictures in blocks starting from the corner. The corresponding text displays vertically. Works well as an accent on title or sub-title slides or for section breaks of a document.

### PictureAccentList {#PictureAccentList}
```
public static final int PictureAccentList
```


Use to show grouped or related information. The small shapes on the upper corners are designed to contain pictures. Emphasizes Level 2 text over Level 1 text, and is a good choice for large amounts of Level 2 text.

### PictureAccentProcess {#PictureAccentProcess}
```
public static final int PictureAccentProcess
```


Use to show sequential steps in a task, process, or workflow. The rectangular shapes in the background are designed to contain pictures.

### PictureCaptionList {#PictureCaptionList}
```
public static final int PictureCaptionList
```


Use to show non-sequential or grouped blocks of information. The top shapes are designed to contain pictures and pictures are emphasized over text. Works well for pictures with short text captions.

### PictureGrid {#PictureGrid}
```
public static final int PictureGrid
```


Use to show pictures laid out on a square grid. Best with a small amount of Level 1 text, which appears above the picture.

### PictureLineup {#PictureLineup}
```
public static final int PictureLineup
```


Use to show a series of pictures side by side. Level 1 text covers the top of the picture. Level 2 text appears below the picture.

### PictureStrips {#PictureStrips}
```
public static final int PictureStrips
```


Use to show a series of pictures from top to bottom with Level 1 text beside each.

### PieProcess {#PieProcess}
```
public static final int PieProcess
```


Use to show steps in a process with each pie slice increasing in size up to seven shapes. Level 1 text displays vertically.

### PlusandMinus {#PlusandMinus}
```
public static final int PlusandMinus
```


Use to show the pros and cons of two ideas. Each Level 1 text can contain multiple sub-levels. Works well with large amounts of text. Limited to two Level 1 items.

### ProcessArrows {#ProcessArrows}
```
public static final int ProcessArrows
```


Use to show information illustrating a process or workflow. Level 1 text appears in the circular shapes and Level 2 text appears in the arrow shapes. Works best for minimal text and to emphasize movement or direction.

### ProcessList {#ProcessList}
```
public static final int ProcessList
```


Use to show multiple groups of information or steps and sub-steps in a task, process, or workflow. Level 1 text corresponds to the top horizontal shapes, and Level 2 text corresponds to vertical sub-steps under each related top level shape.

### PyramidList {#PyramidList}
```
public static final int PyramidList
```


Use to show proportional, interconnected, or hierarchical relationships. Text appears in the rectangular shapes on top of the pyramid background.

### RadialCluster {#RadialCluster}
```
public static final int RadialCluster
```


Use to show data that relates to a central idea or theme. The top Level 1 text appears in the center. Level 2 text appears in surrounding shapes. Can contain up to seven Level 2 shapes. Unused text does not appear, but remains available if you switch layouts. Works best with small amounts of text.

### RadialCycle {#RadialCycle}
```
public static final int RadialCycle
```


Use to show the relationship to a central idea. Emphasizes both information in the center circle and how information in the outer ring of circles contributes to the central idea. The first Level 1 line of text corresponds to the central circle, and its Level 2 text corresponds to the outer ring of circles. Unused text does not appear, but remains available if you switch layouts.

### RadialList {#RadialList}
```
public static final int RadialList
```


Use to show relationships to a central idea in a cycle. The center shape can contain a picture. Level 1 text appears in the smaller circles and any related Level 2 text appears to the side of the smaller circles.

### RadialVenn {#RadialVenn}
```
public static final int RadialVenn
```


Use to show both overlapping relationships and the relationship to a central idea in a cycle. The first line of Level 1 text corresponds to the central shape and the lines of Level 2 text correspond to the surrounding circular shapes. Unused text does not appear, but remains available if you switch layouts.

### RandomToResultProcess {#RandomToResultProcess}
```
public static final int RandomToResultProcess
```
Use to show, through a series of steps, how several chaotic ideas can result in a unified goal or idea. Supports multiple items of Level 1 text, but the first and last Level 1 corresponding shapes are fixed. Works best with small amounts of Level 1 text and medium amounts of Level 2 text.

### RepeatingBendingProcess {#RepeatingBendingProcess}
```
public static final int RepeatingBendingProcess
```

يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. يعظم مساحة العرض الأفقية والعمودية للأشكال.

### ReverseList {#ReverseList}
```
public static final int ReverseList
```

يُستخدم للتغيير بين عنصرين. تُعرض فقط أول عنصرين من النص، ويمكن لكل عنصر أن يحتوي على كمية كبيرة من النص. يعمل بشكل جيد لإظهار تغيير بين عنصرين أو تحول في الترتيب.

### SegmentedCycle {#SegmentedCycle}
```
public static final int SegmentedCycle
```

يُستخدم لعرض تقدم أو تسلسل من المراحل أو المهام أو الأحداث في تدفق دائري. يبرز الأجزاء المترابطة. كل من السطور السبع الأولى من نص المستوى 1 يتطابق مع شريحة أو شكل فطيرة. النص غير المستخدم لا يظهر، لكنه يبقى متاحًا إذا قمت بتبديل القوالب.

### SegmentedProcess {#SegmentedProcess}
```
public static final int SegmentedProcess
```

يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. يبرز نص المستوى 2، حيث تظهر كل سطر في شكل منفصل.

### SegmentedPyramid {#SegmentedPyramid}
```
public static final int SegmentedPyramid
```

يُستخدم لإظهار علاقات احتواء أو نسبية أو مترابطة. تظهر السطور التسعة الأولى من نص المستوى 1 في الأشكال المثلثية. النص غير المستخدم لا يظهر، لكنه يبقى متاحًا إذا قمت بتبديل القوالب. يعمل بأفضل شكل مع نص المستوى 1 فقط.

### SnapshotPictureList {#SnapshotPictureList}
```
public static final int SnapshotPictureList
```

يُستخدم لعرض صور مع نص توضيحي. يمكن لنص المستوى 2 عرض قوائم من المعلومات. يعمل جيدًا مع كمية كبيرة من النص.

### SpiralPicture {#SpiralPicture}
```
public static final int SpiralPicture
```

يُستخدم لعرض سلسلة من حتى خمس صور مع تعليقات المستوى 1 المقابلة التي تت spirال إلى المركز.

### SquareAccentList {#SquareAccentList}
```
public static final int SquareAccentList
```

يُستخدم لعرض قوائم معلومات مقسمة إلى فئات. يظهر نص المستوى 2 بجانب شكل مربع صغير. يعمل جيدًا مع كميات كبيرة من نص المستوى 2.

### StackedList {#StackedList}
```
public static final int StackedList
```

يُستخدم لعرض مجموعات من المعلومات أو خطوات في مهمة أو عملية أو سير عمل. تحتوي الأشكال الدائرية على نص المستوى 1، وتحتوي المستطيلات المقابلة على نص المستوى 2. يعمل جيدًا لتفاصيل كثيرة ونص المستوى 1 القليل.

### StackedVenn {#StackedVenn}
```
public static final int StackedVenn
```

يُستخدم لعرض علاقات متداخلة. خيار جيد لتأكيد النمو أو التدرج. يعمل بأفضل شكل مع نص المستوى 1 فقط. السطور السبع الأولى من نص المستوى 1 تتطابق مع شكل دائري. النص غير المستخدم لا يظهر، لكنه يبقى متاحًا إذا قمت بتبديل القوالب.

### StaggeredProcess {#StaggeredProcess}
```
public static final int StaggeredProcess
```

يُستخدم لعرض تقدم نزولي عبر المراحل. كل من السطور الخمس الأولى من نص المستوى 1 يتطابق مع مستطيل. النص غير المستخدم لا يظهر، لكنه يبقى متاحًا إذا قمت بتبديل القوالب.

### StepDownProcess {#StepDownProcess}
```
public static final int StepDownProcess
```

يُستخدم لإظهار عملية نزول مع خطوات فرعية متعددة. يعمل بأفضل شكل مع كميات صغيرة من النص.

### StepUpProcess {#StepUpProcess}
```
public static final int StepUpProcess
```

يُستخدم لإظهار سلسلة صعودية من الخطوات أو قوائم المعلومات.

### SubStepProcess {#SubStepProcess}
```
public static final int SubStepProcess
```

يُستخدم لعرض عملية متعددة الخطوات مع خطوات فرعية بين كل مثال من نص المستوى 1. يعمل بأفضل شكل مع كميات صغيرة من النص ومحدود إلى سبع خطوات مستوى 1. يمكن لكل خطوة مستوى 1 أن تحتوي على عدد غير محدود من الخطوات الفرعية.

### TableHierarchy {#TableHierarchy}
```
public static final int TableHierarchy
```

يُستخدم لعرض مجموعات من المعلومات مبنية من الأعلى إلى الأسفل، والهرميات داخل كل مجموعة. لا يحتوي هذا القالب على خطوط ربط.

### TableList {#TableList}
```
public static final int TableList
```

يُستخدم لعرض معلومات مجمعة أو ذات صلة ذات قيمة متساوية. السطر الأول من نص المستوى 1 يتطابق مع الشكل العلوي ويُستخدم نص المستوى 2 للقوائم اللاحقة.

### TargetList {#TargetList}
```
public static final int TargetList
```

يُستخدم لعرض معلومات مترابطة أو متداخلة. تظهر السطور السبعة الأولى من نص المستوى 1 في الشكل المستطيل. النص غير المستخدم لا يظهر، لكنه يبقى متاحًا إذا قمت بتبديل القوالب. يعمل جيدًا مع نصي المستوى 1 و2 معًا.

### TextCycle {#TextCycle}
```
public static final int TextCycle
```

يُستخدم لتمثيل تسلسل مستمر من المراحل أو المهام أو الأحداث في تدفق دائري. يبرز الأسهم أو التدفق بدلاً من المراحل أو الخطوات. يعمل بأفضل شكل مع نص المستوى 1 فقط.

### TitlePictureLineup {#TitlePictureLineup}
```
public static final int TitlePictureLineup
```

يُستخدم لعرض سلسلة من الصور لكل منها عنوانه ووصفه الخاص. يظهر نص المستوى 1 في الصندوق أعلى الصورة. يظهر نص المستوى 2 أسفل الصورة.

### TitledMatrix {#TitledMatrix}
```
public static final int TitledMatrix
```

يُستخدم لعرض علاقات الأرباع الأربعة بالكل. السطر الأول من نص المستوى 1 يتطابق مع الشكل المركزي، وتظهر السطور الأربعة الأولى من نص المستوى 2 في الأرباع. النص غير المستخدم لا يظهر، لكنه يبقى متاحًا إذا قمت بتبديل القوالب.

### TitledPictureAccentList {#TitledPictureAccentList}
```
public static final int TitledPictureAccentList
```

يُستخدم لعرض قوائم معلومات مع صورة مميزة لكل نص من نص المستوى 2. يظهر نص المستوى 1 في صندوق منفصل أعلى القائمة.

### TitledPictureBlocks {#TitledPictureBlocks}
```
public static final int TitledPictureBlocks
```

يُستخدم لعرض سلسلة من الصور. يظهر نص المستوى 1 فوق كل صورة. يظهر نص المستوى 2 على الجانب ويتداخل قليلاً مع كل صورة.

### TrapezoidList {#TrapezoidList}
```
public static final int TrapezoidList
```

يُستخدم لعرض معلومات مجمعة أو ذات صلة ذات قيمة متساوية. يعمل جيدًا مع كميات كبيرة من النص.

### UpwardArrow {#UpwardArrow}
```
public static final int UpwardArrow
```

يُستخدم لعرض تقدم أو خطوات تميل صعودًا في مهمة أو عملية أو سير عمل. كل من السطور الخمس الأولى من نص المستوى 1 يتطابق مع نقطة على السهم. يعمل بأفضل شكل مع نص قليل. النص غير المستخدم لا يظهر، لكنه يبقى متاحًا إذا قمت بتبديل القوالب.

### VerticalAccentList {#VerticalAccentList}
```
public static final int VerticalAccentList
```

يُستخدم لعرض قوائم معلومات. يظهر نص المستوى 2 في أشكال مستطيلة فوق الشيفرونات العمودية. يبرز نص المستوى 2 على نص المستوى 1، وهو خيار جيد لكميات متوسطة من نص المستوى 2.

### VerticalArrowList {#VerticalArrowList}
```
public static final int VerticalArrowList
```

يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل تتجه نحو هدف مشترك. يعمل جيدًا للقوائم النقطية للمعلومات.

### VerticalBendingProcess {#VerticalBendingProcess}
```
public static final int VerticalBendingProcess
```

يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل. يعظم مساحة العرض الأفقية والعمودية للأشكال. يضع تركيزًا أكبر على العلاقات المتبادلة بين الأشكال بدلاً من الاتجاه أو الحركة.

### VerticalBlockList {#VerticalBlockList}
```
public static final int VerticalBlockList
```

يُستخدم لعرض مجموعات معلومات أو خطوات في مهمة أو عملية أو سير عمل. يعمل جيدًا مع كميات كبيرة من نص المستوى 2. خيار جيد للنص مع نقطة رئيسية وعدد متعدد من النقاط الفرعية.

### VerticalBoxList {#VerticalBoxList}
```
public static final int VerticalBoxList
```

يُستخدم لعرض عدة مجموعات من المعلومات، خاصة المجموعات التي تحتوي على كميات كبيرة من نص المستوى 2. خيار جيد للقوائم النقطية للمعلومات.

### VerticalBulletList {#VerticalBulletList}
```
public static final int VerticalBulletList
```

يُستخدم لعرض كتل معلومات غير متسلسلة أو مجموعات. يعمل جيدًا للقوائم ذات العناوين الطويلة أو المعلومات على المستوى الأعلى.

### VerticalChevronList {#VerticalChevronList}
```
public static final int VerticalChevronList
```

يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل، أو لتأكيد الحركة أو الاتجاه. يبرز نص المستوى 2 على نص المستوى 1، وهو خيار جيد لكميات كبيرة من نص المستوى 2.

### VerticalCircleList {#VerticalCircleList}
```
public static final int VerticalCircleList
```

يُستخدم لعرض بيانات متسلسلة أو مجمعة. يعمل بأفضل شكل مع نص المستوى 1، الذي يُعرض بجانب شكل دائري كبير. تُفصل المستويات الأدنى من النص بأشكال دائرية أصغر.

### VerticalCurvedList {#VerticalCurvedList}
```
public static final int VerticalCurvedList
```

يُستخدم لعرض قائمة منحنية من المعلومات. لإضافة صور إلى أشكال الدوائر المميزة، استخدم تعبئة الصورة.

### VerticalEquation {#VerticalEquation}
```
public static final int VerticalEquation
```

يُستخدم لعرض خطوات أو مهام متسلسلة تصف خطة أو نتيجة. يظهر السطر الأخير من نص المستوى 1 بعد السهم. يعمل بأفضل شكل مع نص المستوى 1 فقط.

### VerticalPictureAccentList {#VerticalPictureAccentList}
```
public static final int VerticalPictureAccentList
```

يُستخدم لعرض كتل معلومات غير متسلسلة أو مجموعات. الدوائر الصغيرة مصممة لاستيعاب الصور.

### VerticalPictureList {#VerticalPictureList}
```
public static final int VerticalPictureList
```

يُستخدم لعرض كتل معلومات غير متسلسلة أو مجموعات. الأشكال الصغيرة على اليسار مصممة لاستيعاب الصور.

### VerticalProcess {#VerticalProcess}
```
public static final int VerticalProcess
```

يُستخدم لعرض تقدم أو خطوات متسلسلة في مهمة أو عملية أو سير عمل من الأعلى إلى الأسفل. يعمل بأفضل شكل مع نص المستوى 1، حيث أن المسافة العمودية محدودة.

### Custom {#Custom}
```
public static final int Custom
```

يمثل مخطط SmartArt بقالب تخطيط مخصص

### PictureOrganizationChart {#PictureOrganizationChart}
```
public static final int PictureOrganizationChart
```

يُستخدم لعرض معلومات هرمية أو علاقات تقارير داخل منظمة، مع صور مقابلة. الشكل المساعد وتخطيطات مخطط المؤسسة المتدلية متاحة مع هذا القالب.