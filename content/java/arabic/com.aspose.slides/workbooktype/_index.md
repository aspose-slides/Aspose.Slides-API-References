---
title: WorkbookType
second_title: مرجع واجهة برمجة تطبيقات Aspose.Slides للـ Java
description: يحدد نوع ملف دفتر عمل Open XML.
type: docs
url: /ar/com.aspose.slides/workbooktype/
---
**الوراثة:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class WorkbookType extends System.Enum
```

يحدد نوع ملف دفتر عمل Open XML.
## الحقول

| الحقل | الوصف |
| --- | --- |
| [NotDefined](#NotDefined) | لم يتم تعريف نوع دفتر العمل. |
| [Workbook](#Workbook) | دفتر عمل Excel (*.xlsx). |
| [WorkbookMacro](#WorkbookMacro) | دفتر عمل Excel يدعم ماكرو (*.xlsm). |
| [Template](#Template) | قالب Excel (*.xltx). |
| [TemplateMacro](#TemplateMacro) | قالب Excel يدعم ماكرو (*.xltm). |
| [WorkbookBinaryMacro](#WorkbookBinaryMacro) | دفتر عمل Excel ثنائي يدعم ماكرو (*.xlsb). |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```

لم يتم تعريف نوع دفتر العمل.

### Workbook {#Workbook}
```
public static final int Workbook
```

دفتر عمل Excel (*.xlsx).

### WorkbookMacro {#WorkbookMacro}
```
public static final int WorkbookMacro
```

دفتر عمل Excel يدعم ماكرو (*.xlsm).

### Template {#Template}
```
public static final int Template
```

قالب Excel (*.xltx).

### TemplateMacro {#TemplateMacro}
```
public static final int TemplateMacro
```

قالب Excel يدعم ماكرو (*.xltm).

### WorkbookBinaryMacro {#WorkbookBinaryMacro}
```
public static final int WorkbookBinaryMacro
```

دفتر عمل Excel ثنائي يدعم ماكرو (*.xlsb).