---
title: ChartWall
second_title: Aspose.Slides لمرجع API لجافا
description: يمثل الجدران في المخططات الثلاثية الأبعاد.
type: docs
url: /ar/com.aspose.slides/chartwall/
---
**الوراثة:**
java.lang.Object, com.aspose.slides.DomObject

**جميع الواجهات المنفذة:**
[com.aspose.slides.IChartWall](../../com.aspose.slides/ichartwall)
```
public class ChartWall extends DomObject<Chart> implements IChartWall
```

يمثل الجدران في المخططات ثلاثية الأبعاد.
## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getThickness()](#getThickness--) | يعيد أو يضبط سمك الجدران كنسبة مئوية من أكبر بعد لحجم المخطط. |
| [setThickness(int value)](#setThickness-int-) | يعيد أو يضبط سمك الجدران كنسبة مئوية من أكبر بعد لحجم المخطط. |
| [getFormat()](#getFormat--) | يعيد تعبئة الجدار، الخط، التأثير، الأنماط ثلاثية الأبعاد. |
| [getPictureType()](#getPictureType--) | يعيد أو يضبط نوع الصورة. |
| [setPictureType(int value)](#setPictureType-int-) | يعيد أو يضبط نوع الصورة. |
### getThickness() {#getThickness--}
```
public final int getThickness()
```

يعيد أو يضبط سمك الجدران كنسبة مئوية من أكبر بعد لحجم المخطط. قابل للقراءة والكتابة int.

**القيمة المرجعة:**
int
### setThickness(int value) {#setThickness-int-}
```
public final void setThickness(int value)
```

يعيد أو يضبط سمك الجدران كنسبة مئوية من أكبر بعد لحجم المخطط. قابل للقراءة والكتابة int.

**المعلمات:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| value | int |  |
### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

يعيد تعبئة الجدار، الخط، التأثير، الأنماط ثلاثية الأبعاد. للقراءة فقط [IFormat](../../com.aspose.slides/iformat).

**القيمة المرجعة:**
[IFormat](../../com.aspose.slides/iformat)
### getPictureType() {#getPictureType--}
```
public final int getPictureType()
```

يعيد أو يضبط نوع الصورة. قابل للقراءة والكتابة [PictureType](../../com.aspose.slides/picturetype).

**القيمة المرجعة:**
int
### setPictureType(int value) {#setPictureType-int-}
```
public final void setPictureType(int value)
```

يعيد أو يضبط نوع الصورة. قابل للقراءة والكتابة [PictureType](../../com.aspose.slides/picturetype).

**المعلمات:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| value | int |  |