---
title: AlphaReplace
second_title: Aspose.Slides لواجهة برمجة التطبيقات في Java
description: يمثل تأثير استبدال ألفا.
type: docs
url: /ar/com.aspose.slides/alphareplace/
---
**الوراثة:**
[com.aspose.slides.PVIObject](../../com.aspose.slides/pviobject), [com.aspose.slides.ImageTransformOperation](../../com.aspose.slides/imagetransformoperation)

**جميع الواجهات المنفذة:**
[com.aspose.slides.IAlphaReplace](../../com.aspose.slides/ialphareplace), com.aspose.slides.IVisualEffect
```
public final class AlphaReplace extends ImageTransformOperation implements IAlphaReplace, IVisualEffect
```

يمثل تأثير استبدال ألفا. يتم استبدال قيم ألفا (الشفافية) بقيمة ألفا ثابتة.

## الطرق

| الطريقة | الوصف |
| --- | --- |
| [getEffective()](#getEffective--) | يحصل على بيانات تأثير استبدال ألفا الفعّالة مع تطبيق الوراثة. |
| [equals(Object obj)](#equals-java.lang.Object-) | يحدّد ما إذا كان الـ[AlphaReplace](../../com.aspose.slides/alphareplace) المحدد مساويًا للـ[AlphaReplace](../../com.aspose.slides/alphareplace) الحالي. |
| [hashCode()](#hashCode--) | يعمل كدالة تجزئة لنوع معين. |

### getEffective() {#getEffective--}
```
public final IAlphaReplaceEffectiveData getEffective()
```

يحصل على بيانات تأثير استبدال ألفا الفعّالة مع تطبيق الوراثة.

**الإرجاع:**
[IAlphaReplaceEffectiveData](../../com.aspose.slides/ialphareplaceeffectivedata) - [IAlphaReplaceEffectiveData](../../com.aspose.slides/ialphareplaceeffectivedata).

### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```

يحدّد ما إذا كان الـ[AlphaReplace](../../com.aspose.slides/alphareplace) المحدد مساويًا للـ[AlphaReplace](../../com.aspose.slides/alphareplace) الحالي.

**المعلمات:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| obj | java.lang.Object | ال[AlphaReplace](../../com.aspose.slides/alphareplace) للمقارنة. |

**الإرجاع:**
boolean - صحيح إذا كانت الكائنات متساوية؛ وإلا فخطأ.

### hashCode() {#hashCode--}
```
public int hashCode()
```

يعمل كدالة تجزئة لنوع معين.

**الإرجاع:**
int - رمز تجزئة للكائن الحالي.