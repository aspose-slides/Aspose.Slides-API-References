---
title: BulletFormat
second_title: مرجع API Aspose.Slides للغة Java
description: يمثل خصائص تنسيق نقاط الفقرة.
type: docs
url: /ar/com.aspose.slides/bulletformat/
---
**الوراثة:**
java.lang.Object, [com.aspose.slides.PVIObject](../../com.aspose.slides/pviobject)

**جميع الواجهات المنفذة:**
[com.aspose.slides.IBulletFormat](../../com.aspose.slides/ibulletformat)
```
public final class BulletFormat extends PVIObject implements IBulletFormat
```

يمثل خصائص تنسيق نقاط الفقرة.
## الطرق

| طريقة | الوصف |
| --- | --- |
| [getType()](#getType--) | إرجاع أو تعيين نوع النقطة لفقرة دون وراثة. |
| [setType(byte value)](#setType-byte-) | إرجاع أو تعيين نوع النقطة لفقرة دون وراثة. |
| [getChar()](#getChar--) | إرجاع أو تعيين حرف النقطة لفقرة دون وراثة. |
| [setChar(char value)](#setChar-char-) | إرجاع أو تعيين حرف النقطة لفقرة دون وراثة. |
| [getFont()](#getFont--) | إرجاع أو تعيين خط النقطة لفقرة دون وراثة. |
| [setFont(IFontData value)](#setFont-com.aspose.slides.IFontData-) | إرجاع أو تعيين خط النقطة لفقرة دون وراثة. |
| [getHeight()](#getHeight--) | إرجاع أو تعيين ارتفاع النقطة لفقرة دون وراثة. |
| [setHeight(float value)](#setHeight-float-) | إرجاع أو تعيين ارتفاع النقطة لفقرة دون وراثة. |
| [getColor()](#getColor--) | إرجاع تنسيق اللون لنقطة فقرة دون وراثة. |
| [getNumberedBulletStartWith()](#getNumberedBulletStartWith--) | إرجاع أو تعيين الرقم الأول المستخدم لمجموعة النقاط المرقمة دون وراثة. |
| [setNumberedBulletStartWith(short value)](#setNumberedBulletStartWith-short-) | إرجاع أو تعيين الرقم الأول المستخدم لمجموعة النقاط المرقمة دون وراثة. |
| [getNumberedBulletStyle()](#getNumberedBulletStyle--) | إرجاع أو تعيين نمط نقطة مرقمة دون وراثة. |
| [setNumberedBulletStyle(byte value)](#setNumberedBulletStyle-byte-) | إرجاع أو تعيين نمط نقطة مرقمة دون وراثة. |
| [isBulletHardColor()](#isBulletHardColor--) | تحديد ما إذا كانت النقطة تمتلك لونًا خاصًا أو تورثه من الجزء الأول في الفقرة. |
| [setBulletHardColor(byte value)](#setBulletHardColor-byte-) | تحديد ما إذا كانت النقطة تمتلك لونًا خاصًا أو تورثه من الجزء الأول في الفقرة. |
| [isBulletHardFont()](#isBulletHardFont--) | تحديد ما إذا كانت النقطة تمتلك خطًا خاصًا أو تورثه من الجزء الأول في الفقرة. |
| [setBulletHardFont(byte value)](#setBulletHardFont-byte-) | تحديد ما إذا كانت النقطة تمتلك خطًا خاصًا أو تورثه من الجزء الأول في الفقرة. |
| [getPicture()](#getPicture--) | إرجاع الصورة المستخدمة كنقطة في فقرة دون وراثة. |
| [applyDefaultParagraphIndentsShifts()](#applyDefaultParagraphIndentsShifts--) | تعيين إزاحات غير صفرية افتراضية للمسافة البادئة الفعلية للفقرة وMarginLeft عندما تكون النقاط مفعلة (كما يفعل PowerPoint عند تمكين نقاط/ترقيم الفقرات). |
| [getEffective()](#getEffective--) | الحصول على بيانات تنسيق النقطة الفعّالة مع تطبيق الوراثة. |
| [getVersion()](#getVersion--) |  |

### getType() {#getType--}
```
public final byte getType()
```

إرجاع أو تعيين نوع النقطة لفقرة دون وراثة. قراءة/كتابة [BulletType](../../com.aspose.slides/bullettype).

**الإرجاع:**
byte
### setType(byte value) {#setType-byte-}
```
public final void setType(byte value)
```

إرجاع أو تعيين نوع النقطة لفقرة دون وراثة. قراءة/كتابة [BulletType](../../com.aspose.slides/bullettype).

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | byte |  |

### getChar() {#getChar--}
```
public final char getChar()
```

إرجاع أو تعيين حرف النقطة لفقرة دون وراثة. قراءة/كتابة char .

**الإرجاع:**
char
### setChar(char value) {#setChar-char-}
```
public final void setChar(char value)
```

إرجاع أو تعيين حرف النقطة لفقرة دون وراثة. قراءة/كتابة char .

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | char |  |

### getFont() {#getFont--}
```
public final IFontData getFont()
```

إرجاع أو تعيين خط النقطة لفقرة دون وراثة. قراءة/كتابة [IFontData](../../com.aspose.slides/ifontdata).

**الإرجاع:**
[IFontData](../../com.aspose.slides/ifontdata)
### setFont(IFontData value) {#setFont-com.aspose.slides.IFontData-}
```
public final void setFont(IFontData value)
```

إرجاع أو تعيين خط النقطة لفقرة دون وراثة. قراءة/كتابة [IFontData](../../com.aspose.slides/ifontdata).

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | [IFontData](../../com.aspose.slides/ifontdata) |  |

### getHeight() {#getHeight--}
```
public final float getHeight()
```

إرجاع أو تعيين ارتفاع النقطة لفقرة دون وراثة. القيمة Float.NaN تحدد أن النقطة ترث الارتفاع من الجزء الأول في الفقرة. قراءة/كتابة float .

--------------------

القيمة السالبة للارتفاع تعني أن الارتفاع يُعطى بالنقاط والقيمة الموجبة تعني أن الارتفاع نسبة مئوية من النص المحيط.

**الإرجاع:**
float
### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```

إرجاع أو تعيين ارتفاع النقطة لفقرة دون وراثة. القيمة Float.NaN تحدد أن النقطة ترث الارتفاع من الجزء الأول في الفقرة. قراءة/كتابة float .

--------------------

القيمة السالبة للارتفاع تعني أن الارتفاع يُعطى بالنقاط والقيمة الموجبة تعني أن الارتفاع نسبة مئوية من النص المحيط.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | float |  |

### getColor() {#getColor--}
```
public final IColorFormat getColor()
```

إرجاع تنسيق اللون لنقطة فقرة دون وراثة. قراءة فقط [IColorFormat](../../com.aspose.slides/icolorformat).

**الإرجاع:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getNumberedBulletStartWith() {#getNumberedBulletStartWith--}
```
public final short getNumberedBulletStartWith()
```

إرجاع أو تعيين الرقم الأول المستخدم لمجموعة النقاط المرقمة دون وراثة. قراءة/كتابة short .

**الإرجاع:**
short
### setNumberedBulletStartWith(short value) {#setNumberedBulletStartWith-short-}
```
public final void setNumberedBulletStartWith(short value)
```

إرجاع أو تعيين الرقم الأول المستخدم لمجموعة النقاط المرقمة دون وراثة. قراءة/كتابة short .

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | short |  |

### getNumberedBulletStyle() {#getNumberedBulletStyle--}
```
public final byte getNumberedBulletStyle()
```

إرجاع أو تعيين نمط نقطة مرقمة دون وراثة. قراءة/كتابة [NumberedBulletStyle](../../com.aspose.slides/numberedbulletstyle).

**الإرجاع:**
byte
### setNumberedBulletStyle(byte value) {#setNumberedBulletStyle-byte-}
```
public final void setNumberedBulletStyle(byte value)
```

إرجاع أو تعيين نمط نقطة مرقمة دون وراثة. قراءة/كتابة [NumberedBulletStyle](../../com.aspose.slides/numberedbulletstyle).

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | byte |  |

### isBulletHardColor() {#isBulletHardColor--}
```
public final byte isBulletHardColor()
```

تحديد ما إذا كانت النقطة تمتلك لونًا خاصًا أو تورثه من الجزء الأول في الفقرة. **NullableBool.True** إذا كان للنقطة لون خاص و**NullableBool.False** إذا كانت تورث اللون من الجزء الأول في الفقرة. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**الإرجاع:**
byte
### setBulletHardColor(byte value) {#setBulletHardColor-byte-}
```
public final void setBulletHardColor(byte value)
```

تحديد ما إذا كانت النقطة تمتلك لونًا خاصًا أو تورثه من الجزء الأول في الفقرة. **NullableBool.True** إذا كان للنقطة لون خاص و**NullableBool.False** إذا كانت تورث اللون من الجزء الأول في الفقرة. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | byte |  |

### isBulletHardFont() {#isBulletHardFont--}
```
public final byte isBulletHardFont()
```

تحديد ما إذا كانت النقطة تمتلك خطًا خاصًا أو تورثه من الجزء الأول في الفقرة. **NullableBool.True** إذا كان للنقطة خط خاص و**NullableBool.False** إذا كانت تورث الخط من الجزء الأول في الفقرة. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**الإرجاع:**
byte
### setBulletHardFont(byte value) {#setBulletHardFont-byte-}
```
public final void setBulletHardFont(byte value)
```

تحديد ما إذا كانت النقطة تمتلك خطًا خاصًا أو تورثه من الجزء الأول في الفقرة. **NullableBool.True** إذا كان للنقطة خط خاص و**NullableBool.False** إذا كانت تورث الخط من الجزء الأول في الفقرة. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | byte |  |

### getPicture() {#getPicture--}
```
public final ISlidesPicture getPicture()
```

إرجاع الصورة المستخدمة كنقطة في فقرة دون وراثة. قراءة فقط [ISlidesPicture](../../com.aspose.slides/islidespicture).

**الإرجاع:**
[ISlidesPicture](../../com.aspose.slides/islidespicture)
### applyDefaultParagraphIndentsShifts() {#applyDefaultParagraphIndentsShifts--}
```
public final void applyDefaultParagraphIndentsShifts()
```

تعيين إزاحات غير صفرية افتراضية للمسافة البادئة الفعلية للفقرة وMarginLeft عندما تكون النقاط مفعلة (كما يفعل PowerPoint عند تمكين نقاط/ترقيم الفقرات). إذا تم تعطيل النقاط، يتم إعادة تعيين المسافة البادئة وMarginLeft (كما يفعل PowerPoint عند تعطيل نقاط/ترقيم الفقرات). تُطبق إزاحات البادئة وفقًا لسياق النقطة الحالي - IBulletFormat.Type، .NumberedBulletStyle وFontHeight للجزء الأول. تُطبق إزاحات البادئة غير الصفرية على المسافة البادئة وMarginLeft الفعّالين للفقرة الحالية (لتصبح القيم الناتجة قيمًا محلية).

### getEffective() {#getEffective--}
```
public final IBulletFormatEffectiveData getEffective()
```

الحصول على بيانات تنسيق النقطة الفعّالة مع تطبيق الوراثة.

--------------------

> ```
> This example demonstrates getting some effective bullet format properties.
>  
>  Presentation pres = new Presentation("MyPresentation.pptx");
>  try {
>      IAutoShape shape = (IAutoShape)pres.getSlides().get_Item(0).getShapes().get_Item(0);
>      IBulletFormatEffectiveData effectiveBulletFormat = shape.getTextFrame().getParagraphs().get_Item(0).getParagraphFormat().getBullet().getEffective();
>      System.out.println("Bullet type: " + effectiveBulletFormat.getType());
>      if (effectiveBulletFormat.getType() == BulletType.Numbered)
>      {
>          System.out.println("Numbered style: " + effectiveBulletFormat.getNumberedBulletStyle());
>          System.out.println("Starting number: " + effectiveBulletFormat.getNumberedBulletStartWith());
>      }
>  } finally {
>      if (pres != null) pres.dispose();
>  }
> ```


**الإرجاع:**
[IBulletFormatEffectiveData](../../com.aspose.slides/ibulletformateffectivedata) - A [IBulletFormatEffectiveData](../../com.aspose.slides/ibulletformateffectivedata).
### getVersion() {#getVersion--}
```
public long getVersion()
```

الإصدار. قراءة فقط long.

**الإرجاع:**
long