---
title: IScaleEffect
second_title: Aspose.Slides للـ Java مرجع API
description: يمثل تأثير مقياس الرسوم المتحركة.
type: docs
url: /ar/com.aspose.slides/iscaleeffect/
---
**جميع الواجهات المنفذة:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IScaleEffect extends IBehavior
```

يمثل تأثير مقياس الرسوم المتحركة.
## الطرق

| طريقة | الوصف |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | يحدد ما إذا كان يجب تكبير المحتوى. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | يحدد ما إذا كان يجب تكبير المحتوى. |
| [getFrom()](#getFrom--) | يحدد إحداثي x/y لبدء الرسوم المتحركة من (بالنسبة المئوية). |
| [setFrom(Point2D.Float value)](#setFrom-java.awt.geom.Point2D.Float-) | يحدد إحداثي x/y لبدء الرسوم المتحركة من (بالنسبة المئوية). |
| [getTo()](#getTo--) | يحدد الموقع المستهدف لتأثير مقياس الرسوم المتحركة (بالنسبة المئوية). |
| [setTo(Point2D.Float value)](#setTo-java.awt.geom.Point2D.Float-) | يحدد الموقع المستهدف لتأثير مقياس الرسوم المتحركة (بالنسبة المئوية). |
| [getBy()](#getBy--) | يصف قيمة الإزاحة النسبية للرسوم المتحركة (بالنسبة المئوية). |
| [setBy(Point2D.Float value)](#setBy-java.awt.geom.Point2D.Float-) | يصف قيمة الإزاحة النسبية للرسوم المتحركة (بالنسبة المئوية). |
### getZoomContent() {#getZoomContent--}
```
public abstract byte getZoomContent()
```


يحدد ما إذا كان يجب تكبير المحتوى. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**الإرجاع:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public abstract void setZoomContent(byte value)
```


يحدد ما إذا كان يجب تكبير المحتوى. قراءة/كتابة [NullableBool](../../com.aspose.slides/nullablebool).

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public abstract Point2D.Float getFrom()
```


يحدد إحداثي x/y لبدء الرسوم المتحركة من (بالنسبة المئوية). قراءة/كتابة java.awt.geom.Point2D.Float.

**الإرجاع:**
java.awt.geom.Point2D.Float
### setFrom(Point2D.Float value) {#setFrom-java.awt.geom.Point2D.Float-}
```
public abstract void setFrom(Point2D.Float value)
```


يحدد إحداثي x/y لبدء الرسوم المتحركة من (بالنسبة المئوية). قراءة/كتابة java.awt.geom.Point2D.Float.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getTo() {#getTo--}
```
public abstract Point2D.Float getTo()
```


يحدد الموقع المستهدف لتأثير مقياس الرسوم المتحركة (بالنسبة المئوية). قراءة/كتابة java.awt.geom.Point2D.Float.

**الإرجاع:**
java.awt.geom.Point2D.Float
### setTo(Point2D.Float value) {#setTo-java.awt.geom.Point2D.Float-}
```
public abstract void setTo(Point2D.Float value)
```


يحدد الموقع المستهدف لتأثير مقياس الرسوم المتحركة (بالنسبة المئوية). قراءة/كتابة java.awt.geom.Point2D.Float.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getBy() {#getBy--}
```
public abstract Point2D.Float getBy()
```


يصف قيمة الإزاحة النسبية للرسوم المتحركة (بالنسبة المئوية). قراءة/كتابة java.awt.geom.Point2D.Float.

**الإرجاع:**
java.awt.geom.Point2D.Float
### setBy(Point2D.Float value) {#setBy-java.awt.geom.Point2D.Float-}
```
public abstract void setBy(Point2D.Float value)
```


يصف قيمة الإزاحة النسبية للرسوم المتحركة (بالنسبة المئوية). قراءة/كتابة java.awt.geom.Point2D.Float.

**المعلمات:**
| المعامل | النوع | الوصف |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |