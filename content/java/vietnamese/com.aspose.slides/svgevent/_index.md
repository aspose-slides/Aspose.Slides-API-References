---
title: SvgEvent
second_title: Tham chiếu API Aspose.Slides cho Java
description: Đại diện cho các tùy chọn cho hình dạng SVG.
type: docs
url: /vi/com.aspose.slides/svgevent/
---
**Kế thừa:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class SvgEvent extends System.Enum
```

Đại diện cho các tùy chọn cho hình dạng SVG.
## Trường

| Trường | Mô tả |
| --- | --- |
| [OnFocusIn](#OnFocusIn) | Xảy ra khi một phần tử nhận được tiêu điểm, ví dụ khi một đoạn văn bản được chọn. |
| [OnFocusOut](#OnFocusOut) | Xảy ra khi một phần tử mất tiêu điểm, ví dụ khi một đoạn văn bản không còn được chọn. |
| [OnActivate](#OnActivate) | Xảy ra khi một phần tử được kích hoạt, ví dụ thông qua cú nhấp chuột hoặc nhấn phím. |
| [OnClick](#OnClick) | Xảy ra khi nút của thiết bị trỏ được nhấp vào một phần tử. |
| [OnMouseDown](#OnMouseDown) | Xảy ra khi nút của thiết bị trỏ được nhấn lên một phần tử. |
| [OnMouseUp](#OnMouseUp) | Xảy ra khi nút của thiết bị trỏ được thả ra khỏi một phần tử. |
| [OnMouseOver](#OnMouseOver) | Xảy ra khi thiết bị trỏ di chuyển vào một phần tử. |
| [OnMouseMove](#OnMouseMove) | Xảy ra khi thiết bị trỏ di chuyển trong khi đang ở trên một phần tử. |
| [OnMouseOut](#OnMouseOut) | Xảy ra khi thiết bị trỏ di chuyển ra khỏi một phần tử. |
| [OnLoad](#OnLoad) | Xảy ra khi tác nhân người dùng đã phân tích hoàn toàn phần tử và các phần tử con của nó và tất cả các tài nguyên được tham chiếu, cần thiết để hiển thị nó. |
| [OnUnload](#OnUnload) | Xảy ra khi triển khai DOM xóa tài liệu khỏi một cửa sổ hoặc khung. |
| [OnAbort](#OnAbort) | Xảy ra khi quá trình tải trang bị dừng trước khi một phần tử được cho phép tải đầy đủ. |
| [OnError](#OnError) | Xảy ra khi một phần tử không tải đúng cách hoặc khi xảy ra lỗi trong quá trình thực thi kịch bản. |
| [OnResize](#OnResize) | Xảy ra khi chế độ xem tài liệu đang được thay đổi kích thước. Chỉ áp dụng cho các phần tử svg bên ngoài nhất. |
| [OnScroll](#OnScroll) | Xảy ra khi chế độ xem tài liệu đang được di chuyển dọc theo trục X hoặc Y hoặc cả hai. Chỉ áp dụng cho các phần tử svg bên ngoài nhất. |
| [OnZoom](#OnZoom) | Xảy ra khi mức độ phóng đại của chế độ xem tài liệu đang được thay đổi. Chỉ áp dụng cho các phần tử svg bên ngoài nhất. |
| [OnBegin](#OnBegin) | Xảy ra khi một phần tử hoạt ảnh bắt đầu. |
| [OnEnd](#OnEnd) | Xảy ra khi một phần tử hoạt ảnh kết thúc. |
| [OnRepeat](#OnRepeat) | Xảy ra khi một phần tử hoạt ảnh lặp lại. |
### OnFocusIn {#OnFocusIn}
```
public static final int OnFocusIn
```

Xảy ra khi một phần tử nhận được tiêu điểm, ví dụ khi một đoạn văn bản được chọn.

### OnFocusOut {#OnFocusOut}
```
public static final int OnFocusOut
```

Xảy ra khi một phần tử mất tiêu điểm, ví dụ khi một đoạn văn bản không còn được chọn.

### OnActivate {#OnActivate}
```
public static final int OnActivate
```

Xảy ra khi một phần tử được kích hoạt, ví dụ thông qua cú nhấp chuột hoặc nhấn phím.

### OnClick {#OnClick}
```
public static final int OnClick
```

Xảy ra khi nút của thiết bị trỏ được nhấp vào một phần tử.

### OnMouseDown {#OnMouseDown}
```
public static final int OnMouseDown
```

Xảy ra khi nút của thiết bị trỏ được nhấn lên một phần tử.

### OnMouseUp {#OnMouseUp}
```
public static final int OnMouseUp
```

Xảy ra khi nút của thiết bị trỏ được thả ra khỏi một phần tử.

### OnMouseOver {#OnMouseOver}
```
public static final int OnMouseOver
```

Xảy ra khi thiết bị trỏ di chuyển vào một phần tử.

### OnMouseMove {#OnMouseMove}
```
public static final int OnMouseMove
```

Xảy ra khi thiết bị trỏ di chuyển trong khi đang ở trên một phần tử.

### OnMouseOut {#OnMouseOut}
```
public static final int OnMouseOut
```

Xảy ra khi thiết bị trỏ di chuyển ra khỏi một phần tử.

### OnLoad {#OnLoad}
```
public static final int OnLoad
```

Xảy ra khi tác nhân người dùng đã phân tích hoàn toàn phần tử và các phần tử con của nó và tất cả các tài nguyên được tham chiếu, cần thiết để hiển thị nó.

### OnUnload {#OnUnload}
```
public static final int OnUnload
```

Xảy ra khi triển khai DOM xóa tài liệu khỏi một cửa sổ hoặc khung. Chỉ áp dụng cho các phần tử svg bên ngoài nhất.

### OnAbort {#OnAbort}
```
public static final int OnAbort
```

Xảy ra khi quá trình tải trang bị dừng trước khi một phần tử được cho phép tải đầy đủ.

### OnError {#OnError}
```
public static final int OnError
```

Xảy ra khi một phần tử không tải đúng cách hoặc khi xảy ra lỗi trong quá trình thực thi kịch bản.

### OnResize {#OnResize}
```
public static final int OnResize
```

Xảy ra khi chế độ xem tài liệu đang được thay đổi kích thước. Chỉ áp dụng cho các phần tử svg bên ngoài nhất.

### OnScroll {#OnScroll}
```
public static final int OnScroll
```

Xảy ra khi chế độ xem tài liệu đang được di chuyển dọc theo trục X hoặc Y hoặc cả hai. Chỉ áp dụng cho các phần tử svg bên ngoài nhất.

### OnZoom {#OnZoom}
```
public static final int OnZoom
```

Xảy ra khi mức độ phóng đại của chế độ xem tài liệu đang được thay đổi. Chỉ áp dụng cho các phần tử svg bên ngoài nhất.

### OnBegin {#OnBegin}
```
public static final int OnBegin
```

Xảy ra khi một phần tử hoạt ảnh bắt đầu.

### OnEnd {#OnEnd}
```
public static final int OnEnd
```

Xảy ra khi một phần tử hoạt ảnh kết thúc.

### OnRepeat {#OnRepeat}
```
public static final int OnRepeat
```

Xảy ra khi một phần tử hoạt ảnh lặp lại.