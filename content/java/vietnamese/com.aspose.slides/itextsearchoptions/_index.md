---
title: ITextSearchOptions
second_title: Aspose.Slides cho Java Tham khảo API
description: Biểu diễn các tùy chọn có thể được sử dụng để tìm kiếm văn bản trong một Presentation Slide hoặc TextFrame.
type: docs
url: /vi/com.aspose.slides/itextsearchoptions/
---```
public interface ITextSearchOptions
```

Biểu diễn các tùy chọn có thể được sử dụng để tìm kiếm văn bản trong một Presentation, Slide hoặc TextFrame.
## Phương thức

| Phương thức | Mô tả |
| --- | --- |
| [getCaseSensitive()](#getCaseSensitive--) | Đặt true để sử dụng tìm kiếm phân biệt chữ hoa chữ thường, false - nếu không. |
| [setCaseSensitive(boolean value)](#setCaseSensitive-boolean-) | Đặt true để sử dụng tìm kiếm phân biệt chữ hoa chữ thường, false - nếu không. |
| [getWholeWordsOnly()](#getWholeWordsOnly--) | Đặt true để chỉ khớp toàn bộ từ, false - nếu không. |
| [setWholeWordsOnly(boolean value)](#setWholeWordsOnly-boolean-) | Đặt true để chỉ khớp toàn bộ từ, false - nếu không. |
| [getIncludeNotes()](#getIncludeNotes--) | Đặt true để bao gồm văn bản có trong ghi chú slide khi thực hiện tìm kiếm, thay thế hoặc đánh dấu. |
| [setIncludeNotes(boolean value)](#setIncludeNotes-boolean-) | Đặt true để bao gồm văn bản có trong ghi chú slide khi thực hiện tìm kiếm, thay thế hoặc đánh dấu. |
### getCaseSensitive() {#getCaseSensitive--}
```
public abstract boolean getCaseSensitive()
```

Đặt true để sử dụng tìm kiếm phân biệt chữ hoa chữ thường, false - nếu không. Đọc/ghi boolean.

**Trả về:**
boolean
### setCaseSensitive(boolean value) {#setCaseSensitive-boolean-}
```
public abstract void setCaseSensitive(boolean value)
```

Đặt true để sử dụng tìm kiếm phân biệt chữ hoa chữ thường, false - nếu không. Đọc/ghi boolean.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |

### getWholeWordsOnly() {#getWholeWordsOnly--}
```
public abstract boolean getWholeWordsOnly()
```

Đặt true để chỉ khớp toàn bộ từ, false - nếu không. Đọc/ghi boolean.

**Trả về:**
boolean
### setWholeWordsOnly(boolean value) {#setWholeWordsOnly-boolean-}
```
public abstract void setWholeWordsOnly(boolean value)
```

Đặt true để chỉ khớp toàn bộ từ, false - nếu không. Đọc/ghi boolean.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |

### getIncludeNotes() {#getIncludeNotes--}
```
public abstract boolean getIncludeNotes()
```

Đặt true để bao gồm văn bản có trong ghi chú slide khi thực hiện tìm kiếm, thay thế hoặc đánh dấu. Giá trị mặc định là false.

**Trả về:**
boolean
### setIncludeNotes(boolean value) {#setIncludeNotes-boolean-}
```
public abstract void setIncludeNotes(boolean value)
```

Đặt true để bao gồm văn bản có trong ghi chú slide khi thực hiện tìm kiếm, thay thế hoặc đánh dấu. Giá trị mặc định là false.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |