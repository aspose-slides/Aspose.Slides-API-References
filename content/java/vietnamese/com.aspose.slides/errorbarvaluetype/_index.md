---
title: ErrorBarValueType
second_title: Tham chiếu API Aspose.Slides cho Java
description: Biểu thị loại giá trị thanh lỗi
type: docs
url: /vi/com.aspose.slides/errorbarvaluetype/
---
**Kế thừa:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class ErrorBarValueType extends System.Enum
```

Biểu thị loại giá trị thanh lỗi
## Trường

| Trường | Mô tả |
| --- | --- |
| [Custom](#Custom) | Xác định rằng độ dài của các thanh lỗi sẽ được xác định bởi các phần tử Plus và Minus. |
| [Fixed](#Fixed) | Xác định rằng độ dài của các thanh lỗi sẽ là giá trị cố định được xác định bởi Error Bar Value. |
| [Percentage](#Percentage) | Xác định rằng độ dài của các thanh lỗi sẽ là phần trăm Error Bar Value của dữ liệu. |
| [StandardDeviation](#StandardDeviation) | Xác định rằng độ dài của các thanh lỗi sẽ là độ lệch chuẩn Error Bar Value của dữ liệu. |
| [StandardError](#StandardError) | Xác định rằng độ dài của các thanh lỗi sẽ là lỗi chuẩn Error Bar Value của dữ liệu. |
### Custom {#Custom}
```
public static final int Custom
```


Xác định rằng độ dài của các thanh lỗi sẽ được xác định bởi các phần tử Plus và Minus.

### Fixed {#Fixed}
```
public static final int Fixed
```


Xác định rằng độ dài của các thanh lỗi sẽ là giá trị cố định được xác định bởi Error Bar Value.

### Percentage {#Percentage}
```
public static final int Percentage
```


Xác định rằng độ dài của các thanh lỗi sẽ là phần trăm Error Bar Value của dữ liệu.

### StandardDeviation {#StandardDeviation}
```
public static final int StandardDeviation
```


Xác định rằng độ dài của các thanh lỗi sẽ là độ lệch chuẩn Error Bar Value của dữ liệu.

### StandardError {#StandardError}
```
public static final int StandardError
```


Xác định rằng độ dài của các thanh lỗi sẽ là lỗi chuẩn Error Bar Value của dữ liệu.