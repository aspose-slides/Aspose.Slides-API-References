---
title: SmartArtLayoutType
second_title: Tham chiếu API Aspose.Slides cho Java
description: Đại diện cho loại bố cục của một sơ đồ SmartArt.
type: docs
url: /vi/com.aspose.slides/smartartlayouttype/
---
**Kế thừa:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class SmartArtLayoutType extends System.Enum
```

Đại diện cho kiểu bố cục của SmartArt diagram.
## Trường

| Trường | Mô tả |
| --- | --- |
| [AccentProcess](#AccentProcess) | Sử dụng để hiển thị một quá trình tiến triển, một dòng thời gian, hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. |
| [AccentedPicture](#AccentedPicture) | Sử dụng để hiển thị một ý tưởng trung tâm, dạng ảnh, với các ý tưởng liên quan ở bên cạnh. |
| [AlternatingFlow](#AlternatingFlow) | Sử dụng để hiển thị các nhóm thông tin hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. |
| [AlternatingHexagons](#AlternatingHexagons) | Sử dụng để biểu diễn một loạt các ý tưởng có liên kết với nhau. |
| [AlternatingPictureBlocks](#AlternatingPictureBlocks) | Sử dụng để hiển thị một loạt các hình ảnh từ trên xuống dưới. |
| [AlternatingPictureCircles](#AlternatingPictureCircles) | Sử dụng để hiển thị một bộ các hình ảnh kèm văn bản. |
| [ArrowRibbon](#ArrowRibbon) | Sử dụng để hiển thị các khái niệm liên quan hoặc đối lập có một số liên kết, chẳng hạn như các lực đối nghịch. |
| [AscendingPictureAccentProcess](#AscendingPictureAccentProcess) | Sử dụng để hiển thị một loạt hình ảnh tăng dần cùng văn bản mô tả. |
| [Balance](#Balance) | Sử dụng để so sánh hoặc hiển thị mối quan hệ giữa hai ý tưởng. |
| [BasicBendingProcess](#BasicBendingProcess) | Sử dụng để hiển thị một quá trình tiến triển hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. |
| [BasicBlockList](#BasicBlockList) | Sử dụng để hiển thị các khối thông tin không tuần tự hoặc được nhóm lại. |
| [BasicChevronProcess](#BasicChevronProcess) | Sử dụng để hiển thị một quá trình; một dòng thời gian; các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc; hoặc để nhấn mạnh chuyển động hoặc hướng đi. |
| [BasicCycle](#BasicCycle) | Sử dụng để biểu diễn một chuỗi liên tục của các giai đoạn, nhiệm vụ hoặc sự kiện trong một luồng vòng tròn. |
| [BasicMatrix](#BasicMatrix) | Sử dụng để hiển thị mối quan hệ của các thành phần với toàn thể trong các khu vực tư giác. |
| [BasicPie](#BasicPie) | Sử dụng để hiển thị cách các phần riêng lẻ tạo thành một toàn thể. |
| [BasicProcess](#BasicProcess) | Sử dụng để hiển thị một quá trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. |
| [BasicPyramid](#BasicPyramid) | Sử dụng để hiển thị các mối quan hệ tỷ lệ, liên kết hoặc phân cấp với thành phần lớn nhất ở dưới cùng và thu hẹp lên trên. |
| [BasicRadial](#BasicRadial) | Sử dụng để hiển thị mối quan hệ với một ý tưởng trung tâm trong một chu kỳ. |
| [BasicTarget](#BasicTarget) | Sử dụng để hiển thị các mối quan hệ chứa, bậc hoặc phân cấp. |
| [BasicTimeline](#BasicTimeline) | Sử dụng để hiển thị các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc, hoặc để hiển thị thông tin dòng thời gian. |
| [BasicVenn](#BasicVenn) | Sử dụng để hiển thị các mối quan hệ chồng chéo hoặc liên kết. |
| [BendingPictureAccentList](#BendingPictureAccentList) | Sử dụng để hiển thị các khối thông tin không tuần tự hoặc được nhóm lại. |
| [BendingPictureBlocks](#BendingPictureBlocks) | Sử dụng để hiển thị một loạt hình ảnh. |
| [BendingPictureCaption](#BendingPictureCaption) | Sử dụng để hiển thị một loạt hình ảnh theo thứ tự. |
| [BendingPictureCaptionList](#BendingPictureCaptionList) | Sử dụng để hiển thị một loạt hình ảnh. |
| [BendingPictureSemiTransparentText](#BendingPictureSemiTransparentText) | Sử dụng để hiển thị một loạt hình ảnh. |
| [BlockCycle](#BlockCycle) | Sử dụng để biểu diễn một chuỗi liên tục của các giai đoạn, nhiệm vụ hoặc sự kiện trong một luồng vòng tròn. |
| [BubblePictureList](#BubblePictureList) | Sử dụng để hiển thị một loạt hình ảnh. |
| [CaptionedPictures](#CaptionedPictures) | Sử dụng để hiển thị các hình ảnh có nhiều cấp độ văn bản. |
| [ChevronList](#ChevronList) | Sử dụng để hiển thị một quá trình tiến triển qua nhiều quy trình tạo nên một luồng công việc tổng thể. |
| [CircleAccentTimeline](#CircleAccentTimeline) | Sử dụng để hiển thị một loạt sự kiện hoặc thông tin dòng thời gian. |
| [CircleArrowProcess](#CircleArrowProcess) | Sử dụng để hiển thị các mục tuần tự với văn bản hỗ trợ cho mỗi mục. |
| [CirclePictureHierarchy](#CirclePictureHierarchy) | Sử dụng để hiển thị thông tin phân cấp hoặc mối quan hệ báo cáo trong một tổ chức. |
| [CircleRelationship](#CircleRelationship) | Sử dụng để hiển thị mối quan hệ đến hoặc từ một ý tưởng trung tâm. |
| [CircularBendingProcess](#CircularBendingProcess) | Sử dụng để hiển thị một chuỗi dài hoặc phi tuyến tính các bước trong một nhiệm vụ, quy trình hoặc luồng công việc. |
| [CircularPictureCallout](#CircularPictureCallout) | Sử dụng để hiển thị một ý tưởng trung tâm và các ý tưởng phụ hoặc các mục liên quan. |
| [ClosedChevronProcess](#ClosedChevronProcess) | Sử dụng để hiển thị một quá trình, một dòng thời gian, hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc, hoặc để nhấn mạnh chuyển động hoặc hướng đi. |
| [ContinuousArrowProcess](#ContinuousArrowProcess) | Sử dụng để hiển thị một dòng thời gian hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. |
| [ContinuousBlockProcess](#ContinuousBlockProcess) | Sử dụng để hiển thị một quá trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. |
| [ContinuousCycle](#ContinuousCycle) | Sử dụng để biểu diễn một chuỗi liên tục của các giai đoạn, nhiệm vụ hoặc sự kiện trong một luồng vòng tròn. |
| [ContinuousPictureList](#ContinuousPictureList) | Sử dụng để hiển thị các nhóm thông tin liên kết với nhau. |
| [ConvergingArrows](#ConvergingArrows) | Sử dụng để hiển thị các ý tưởng hoặc khái niệm hội tụ về một điểm trung tâm. |
| [ConvergingRadial](#ConvergingRadial) | Sử dụng để hiển thị các mối quan hệ của các khái niệm hoặc thành phần với một ý tưởng trung tâm trong một chu kỳ. |
| [CounterbalanceArrows](#CounterbalanceArrows) | Sử dụng để hiển thị hai ý tưởng hoặc khái niệm đối lập. |
| [CycleMatrix](#CycleMatrix) | Sử dụng để hiển thị mối quan hệ với một ý tưởng trung tâm trong một tiến trình vòng tròn. |
| [DescendingBlockList](#DescendingBlockList) | Sử dụng để hiển thị các nhóm ý tưởng liên quan hoặc danh sách thông tin. |
| [DescendingProcess](#DescendingProcess) | Sử dụng để hiển thị một chuỗi sự kiện giảm dần. |
| [DetailedProcess](#DetailedProcess) | Sử dụng với lượng lớn văn bản Cấp 2 để hiển thị một quá trình tiến triển qua các giai đoạn. |
| [DivergingArrows](#DivergingArrows) | Sử dụng để hiển thị các ý tưởng hoặc khái niệm tiến lên từ một nguồn trung tâm. |
| [DivergingRadial](#DivergingRadial) | Sử dụng để hiển thị các mối quan hệ với một ý tưởng trung tâm trong một chu kỳ. |
| [Equation](#Equation) | Sử dụng để hiển thị các bước hoặc nhiệm vụ tuần tự mô tả một kế hoạch hoặc kết quả. |
| [FramedTextPicture](#FramedTextPicture) | Sử dụng để hiển thị các hình ảnh với văn bản Cấp 1 tương ứng được hiển thị trong khung. |
| [Funnel](#Funnel) | Sử dụng để hiển thị việc lọc thông tin hoặc cách các phần hợp nhất thành một toàn thể. |
| [Gear](#Gear) | Sử dụng để hiển thị các ý tưởng liên kết nhau. |
| [GridMatrix](#GridMatrix) | Sử dụng để hiển thị vị trí của các khái niệm dọc theo hai trục. |
| [GroupedList](#GroupedList) | Sử dụng để hiển thị các nhóm và nhóm phụ của thông tin, hoặc các bước và bước phụ trong một nhiệm vụ, quy trình hoặc luồng công việc. |
| [HalfCircleOrganizationChart](#HalfCircleOrganizationChart) | Sử dụng để hiển thị thông tin phân cấp hoặc mối quan hệ báo cáo trong một tổ chức. |
| [HexagonCluster](#HexagonCluster) | Sử dụng để hiển thị các hình ảnh kèm văn bản mô tả đi kèm. |
| [Hierarchy](#Hierarchy) | Sử dụng để hiển thị các mối quan hệ phân cấp tiến từ trên xuống dưới. |
| [HierarchyList](#HierarchyList) | Sử dụng để hiển thị các mối quan hệ phân cấp tiến qua các nhóm. |
| [HorizontalBulletList](#HorizontalBulletList) | Sử dụng để hiển thị các danh sách thông tin không tuần tự hoặc được nhóm lại. |
| [HorizontalHierarchy](#HorizontalHierarchy) | Sử dụng để hiển thị các mối quan hệ phân cấp tiến theo chiều ngang. |
| [HorizontalLabeledHierarchy](#HorizontalLabeledHierarchy) | Sử dụng để hiển thị các mối quan hệ phân cấp tiến theo chiều ngang và được nhóm phân cấp. |
| [HorizontalMultiLevelHierarchy](#HorizontalMultiLevelHierarchy) | Sử dụng để hiển thị lượng lớn thông tin phân cấp tiến theo chiều ngang. |
| [HorizontalOrganizationChart](#HorizontalOrganizationChart) | Sử dụng để hiển thị thông tin phân cấp theo chiều ngang hoặc mối quan hệ báo cáo trong một tổ chức. |
| [HorizontalPictureList](#HorizontalPictureList) | Sử dụng để hiển thị thông tin không tuần tự hoặc được nhóm lại với nhấn mạnh vào các hình ảnh liên quan. |
| [IncreasingArrowsProcess](#IncreasingArrowsProcess) | Sử dụng để hiển thị các bước tuần tự và chồng chéo trong một quy trình. |
| [IncreasingCircleProcess](#IncreasingCircleProcess) | Sử dụng để hiển thị một loạt các bước, với phần trong của vòng tròn tăng lên theo mỗi bước. |
| [InvertedPyramid](#InvertedPyramid) | Sử dụng để hiển thị các mối quan hệ tỷ lệ, liên kết hoặc phân cấp với thành phần lớn nhất ở trên cùng và thu hẹp xuống. |
| [LabeledHierarchy](#LabeledHierarchy) | Sử dụng để hiển thị các mối quan hệ phân cấp tiến từ trên xuống dưới và được nhóm phân cấp. |
| [LinearVenn](#LinearVenn) | Sử dụng để hiển thị các mối quan hệ chồng chéo trong một chuỗi. |
| [LinedList](#LinedList) | Sử dụng để hiển thị lượng lớn văn bản chia thành các danh mục và danh mục phụ. |
| [MultidirectionalCycle](#MultidirectionalCycle) | Sử dụng để biểu diễn một chuỗi liên tục của các giai đoạn, nhiệm vụ hoặc sự kiện có thể xảy ra theo bất kỳ hướng nào. |
| [NameandTitleOrganizationChart](#NameandTitleOrganizationChart) | Sử dụng để hiển thị thông tin phân cấp hoặc mối quan hệ báo cáo trong một tổ chức. |
| [NestedTarget](#NestedTarget) | Sử dụng để hiển thị các mối quan hệ chứa. |
| [NondirectionalCycle](#NondirectionalCycle) | Sử dụng để biểu diễn một chuỗi liên tục của các giai đoạn, nhiệm vụ hoặc sự kiện trong một luồng vòng tròn. |
| [OpposingArrows](#OpposingArrows) | Sử dụng để hiển thị hai ý tưởng đối lập, hoặc các ý tưởng tách ra từ một điểm trung tâm. |
| [OpposingIdeas](#OpposingIdeas) | Sử dụng để hiển thị hai ý tưởng hoặc khái niệm đối lập hoặc trái ngược. |
| [OrganizationChart](#OrganizationChart) | Sử dụng để hiển thị thông tin phân cấp hoặc mối quan hệ báo cáo trong một tổ chức. |
| [PhasedProcess](#PhasedProcess) | Sử dụng để hiển thị ba giai đoạn của một quy trình. |
| [PictureAccentBlocks](#PictureAccentBlocks) | Sử dụng để hiển thị một nhóm các hình ảnh theo khối bắt đầu từ góc. |
| [PictureAccentList](#PictureAccentList) | Sử dụng để hiển thị thông tin được nhóm hoặc liên quan. |
| [PictureAccentProcess](#PictureAccentProcess) | Sử dụng để hiển thị các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. |
| [PictureCaptionList](#PictureCaptionList) | Sử dụng để hiển thị các khối thông tin không tuần tự hoặc được nhóm lại. |
| [PictureGrid](#PictureGrid) | Sử dụng để hiển thị các hình ảnh được bố trí trên lưới vuông. |
| [PictureLineup](#PictureLineup) | Sử dụng để hiển thị một loạt hình ảnh cạnh nhau. |
| [PictureStrips](#PictureStrips) | Sử dụng để hiển thị một loạt hình ảnh từ trên xuống dưới với văn bản Cấp 1 bên cạnh mỗi hình. |
| [PieProcess](#PieProcess) | Sử dụng để hiển thị các bước trong một quy trình với mỗi lát bánh tăng kích thước lên tới bảy hình dạng. |
| [PlusandMinus](#PlusandMinus) | Sử dụng để hiển thị ưu và nhược điểm của hai ý tưởng. |
| [ProcessArrows](#ProcessArrows) | Sử dụng để hiển thị thông tin minh họa một quy trình hoặc luồng công việc. |
| [ProcessList](#ProcessList) | Sử dụng để hiển thị nhiều nhóm thông tin hoặc các bước và bước phụ trong một nhiệm vụ, quy trình hoặc luồng công việc. |
| [PyramidList](#PyramidList) | Sử dụng để hiển thị các mối quan hệ tỷ lệ, liên kết hoặc phân cấp. |
| [RadialCluster](#RadialCluster) | Sử dụng để hiển thị dữ liệu liên quan đến một ý tưởng hoặc chủ đề trung tâm. |
| [RadialCycle](#RadialCycle) | Sử dụng để hiển thị mối quan hệ với một ý tưởng trung tâm. |
| [RadialList](#RadialList) | Sử dụng để hiển thị các mối quan hệ với một ý tưởng trung tâm trong một chu kỳ. |
| [RadialVenn](#RadialVenn) | Sử dụng để hiển thị cả các mối quan hệ chồng chéo và mối quan hệ với một ý tưởng trung tâm trong một chu kỳ. |
| [RandomToResultProcess](#RandomToResultProcess) | Sử dụng để hiển thị, qua một loạt các bước, cách nhiều ý tưởng hỗn loạn có thể dẫn đến một mục tiêu hoặc ý tưởng thống nhất. |
| [RepeatingBendingProcess](#RepeatingBendingProcess) | Sử dụng để hiển thị một quá trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. |
| [ReverseList](#ReverseList) | Sử dụng để chuyển đổi giữa hai mục. |
| [SegmentedCycle](#SegmentedCycle) | Sử dụng để hiển thị một quá trình hoặc một chuỗi các giai đoạn, nhiệm vụ hoặc sự kiện trong một luồng vòng tròn. |
| [SegmentedProcess](#SegmentedProcess) | Sử dụng để hiển thị một quá trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. |
| [SegmentedPyramid](#SegmentedPyramid) | Sử dụng để hiển thị các mối quan hệ chứa, tỷ lệ hoặc liên kết. |
| [SnapshotPictureList](#SnapshotPictureList) | Sử dụng để hiển thị các hình ảnh kèm văn bản giải thích. |
| [SpiralPicture](#SpiralPicture) | Sử dụng để hiển thị một loạt tối đa năm hình ảnh với phụ đề Cấp 1 tương ứng xoắn vào trung tâm. |
| [SquareAccentList](#SquareAccentList) | Sử dụng để hiển thị danh sách thông tin chia thành các danh mục. |
| [StackedList](#StackedList) | Sử dụng để hiển thị các nhóm thông tin hoặc các bước trong một nhiệm vụ, quy trình hoặc luồng công việc. |
| [StackedVenn](#StackedVenn) | Sử dụng để hiển thị các mối quan hệ chồng chéo. |
| [StaggeredProcess](#StaggeredProcess) | Sử dụng để hiển thị một quá trình tiến xuống qua các giai đoạn. |
| [StepDownProcess](#StepDownProcess) | Sử dụng để hiển thị một quy trình giảm dần với nhiều bước và bước phụ. |
| [StepUpProcess](#StepUpProcess) | Sử dụng để hiển thị một loạt các bước hoặc danh sách thông tin tăng dần. |
| [SubStepProcess](#SubStepProcess) | Sử dụng để hiển thị một quy trình đa bước với các bước phụ giữa mỗi đoạn văn bản Cấp 1. |
| [TableHierarchy](#TableHierarchy) | Sử dụng để hiển thị các nhóm thông tin được xây dựng từ trên xuống dưới, và các cấp bậc trong mỗi nhóm. |
| [TableList](#TableList) | Sử dụng để hiển thị thông tin được nhóm hoặc liên quan có giá trị bằng nhau. |
| [TargetList](#TargetList) | Sử dụng để hiển thị thông tin liên quan hoặc chồng chéo. |
| [TextCycle](#TextCycle) | Sử dụng để biểu diễn một chuỗi liên tục của các giai đoạn, nhiệm vụ hoặc sự kiện trong một luồng vòng tròn. |
| [TitlePictureLineup](#TitlePictureLineup) | Sử dụng để hiển thị một loạt hình ảnh, mỗi hình có tiêu đề và mô tả riêng. |
| [TitledMatrix](#TitledMatrix) | Sử dụng để hiển thị các mối quan hệ của bốn khu vực tư giác với một toàn thể. |
| [TitledPictureAccentList](#TitledPictureAccentList) | Sử dụng để hiển thị danh sách thông tin với một hình ảnh nhấn mạnh cho mỗi văn bản Cấp 2. |
| [TitledPictureBlocks](#TitledPictureBlocks) | Sử dụng để hiển thị một loạt hình ảnh. |
| [TrapezoidList](#TrapezoidList) | Sử dụng để hiển thị thông tin được nhóm hoặc liên quan có giá trị bằng nhau. |
| [UpwardArrow](#UpwardArrow) | Sử dụng để hiển thị một quá trình hoặc các bước xu hướng tăng lên trong một nhiệm vụ, quy trình hoặc luồng công việc. |
| [VerticalAccentList](#VerticalAccentList) | Sử dụng để hiển thị danh sách thông tin. |
| [VerticalArrowList](#VerticalArrowList) | Sử dụng để hiển thị một quá trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc di chuyển về phía mục tiêu chung. |
| [VerticalBendingProcess](#VerticalBendingProcess) | Sử dụng để hiển thị một quá trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. |
| [VerticalBlockList](#VerticalBlockList) | Sử dụng để hiển thị các nhóm thông tin hoặc các bước trong một nhiệm vụ, quy trình hoặc luồng công việc. |
| [VerticalBoxList](#VerticalBoxList) | Sử dụng để hiển thị một số nhóm thông tin, đặc biệt là các nhóm có lượng lớn văn bản Cấp 2. |
| [VerticalBulletList](#VerticalBulletList) | Sử dụng để hiển thị các khối thông tin không tuần tự hoặc được nhóm lại. |
| [VerticalChevronList](#VerticalChevronList) | Sử dụng để hiển thị một quá trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc, hoặc để nhấn mạnh chuyển động hoặc hướng đi. |
| [VerticalCircleList](#VerticalCircleList) | Sử dụng để hiển thị dữ liệu tuần tự hoặc được nhóm. |
| [VerticalCurvedList](#VerticalCurvedList) | Sử dụng để hiển thị một danh sách thông tin cong. |
| [VerticalEquation](#VerticalEquation) | Sử dụng để hiển thị các bước hoặc nhiệm vụ tuần tự mô tả một kế hoạch hoặc kết quả. |
| [VerticalPictureAccentList](#VerticalPictureAccentList) | Sử dụng để hiển thị các khối thông tin không tuần tự hoặc được nhóm lại. |
| [VerticalPictureList](#VerticalPictureList) | Sử dụng để hiển thị các khối thông tin không tuần tự hoặc được nhóm lại. |
| [VerticalProcess](#VerticalProcess) | Sử dụng để hiển thị một quá trình hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc từ trên xuống dưới. |
| [Custom](#Custom) | Represents a SmartArt diagram with custom layout template |
| [PictureOrganizationChart](#PictureOrganizationChart) | Use to show hierarchical information or reporting relationships in an organization, with corresponding pictures. |

### AccentProcess {#AccentProcess}
```
public static final int AccentProcess
```

Use to show a progression, a timeline, or sequential steps in a task, process, or workflow. Works well for illustrating both Cấp độ 1 and Cấp độ 2 text.

### AccentedPicture {#AccentedPicture}
```
public static final int AccentedPicture
```

Use to show a central, photographic idea with related ideas on the side. The top Cấp độ 1 text appears over the central picture. Corresponding text for other Cấp độ 1 shapes appear next to the small circular pictures. This layout also works well with no text.

### AlternatingFlow {#AlternatingFlow}
```
public static final int AlternatingFlow
```

Use to show groups of information or sequential steps in a task, process, or workflow. Emphasizes the interaction or relationships among the groups of information.

### AlternatingHexagons {#AlternatingHexagons}
```
public static final int AlternatingHexagons
```

Use to represent a series of interconnected ideas. Cấp độ 1 text appears inside the hexagons. Cấp độ 2 text appears outside the shapes.

### AlternatingPictureBlocks {#AlternatingPictureBlocks}
```
public static final int AlternatingPictureBlocks
```

Use to show a series of pictures from top to bottom. Text appears alternately on the right or left of the picture.

### AlternatingPictureCircles {#AlternatingPictureCircles}
```
public static final int AlternatingPictureCircles
```

Use to show a set of pictures with text. The corresponding text appears in the central circles with the images alternating from left to right.

### ArrowRibbon {#ArrowRibbon}
```
public static final int ArrowRibbon
```

Use to show either related or contrasting concepts with some connection, such as opposing forces. The first two lines of Cấp độ 1 text are used for text in the arrows. Unused text does not appear, but remains available if you switch layouts.

### AscendingPictureAccentProcess {#AscendingPictureAccentProcess}
```
public static final int AscendingPictureAccentProcess
```

Use to show an ascending series of pictures with descriptive text. Works best with a small amount of text.

### Balance {#Balance}
```
public static final int Balance
```

Use to compare or show the relationship between two ideas. Each of the first two lines of Cấp độ 1 text corresponds to text at the top of one side of the center point. Emphasizes Cấp độ 2 text, which is limited to four shapes on each side of the center point. The balance tips towards the side with the most shapes containing Cấp độ 2 text. Unused text does not appear, but remains available if you switch layouts.

### BasicBendingProcess {#BasicBendingProcess}
```
public static final int BasicBendingProcess
```

Use to show a progression or sequential steps in a task, process, or workflow. Maximizes both horizontal and vertical display space for shapes.

### BasicBlockList {#BasicBlockList}
```
public static final int BasicBlockList
```

Use to show non-sequential or grouped blocks of information. Maximizes both horizontal and vertical display space for shapes.

### BasicChevronProcess {#BasicChevronProcess}
```
public static final int BasicChevronProcess
```

Use to show a progression; a timeline; sequential steps in a task, process, or workflow; or to emphasize movement or direction. Cấp độ 1 text appears inside an arrow shape while Cấp độ 2 text appears below the arrow shapes.

### BasicCycle {#BasicCycle}
```
public static final int BasicCycle
```

Use to represent a continuing sequence of stages, tasks, or events in a circular flow. Emphasizes the stages or steps rather than the connecting arrows or flow. Works best with Cấp độ 1 text only.

### BasicMatrix {#BasicMatrix}
```
public static final int BasicMatrix
```

Use to show the relationship of components to a whole in quadrants. The first four lines of Cấp độ 1 text appear in the quadrants. Unused text does not appear, but remains available if you switch layouts.

### BasicPie {#BasicPie}
```
public static final int BasicPie
```

Use to show how individual parts form a whole. The first seven lines of Cấp độ 1 text correspond to the evenly distributed wedge or pie shapes. The top Cấp độ 1 text shape appears outside of the rest of the pie for emphasis. Unused text does not appear, but remains available if you switch layouts.

### BasicProcess {#BasicProcess}
```
public static final int BasicProcess
```

Use to show a progression or sequential steps in a task, process, or workflow.

### BasicPyramid {#BasicPyramid}
```
public static final int BasicPyramid
```

Use to show proportional, interconnected, or hierarchical relationships with the largest component on the bottom and narrowing up. Cấp độ 1 text appears in the pyramid segments and Cấp độ 2 text appears in shapes alongside each segment.

### BasicRadial {#BasicRadial}
```
public static final int BasicRadial
```

Use to show the relationship to a central idea in a cycle. The first line of Cấp độ 1 text corresponds to the central shape, and its Cấp độ 2 text corresponds to the surrounding circular shapes. Unused text does not appear, but remains available if you switch layouts.

### BasicTarget {#BasicTarget}
```
public static final int BasicTarget
```

Use to show containment, gradations, or hierarchical relationships. The first five lines of Cấp độ 1 text are associated with a circle. Unused text does not appear, but remains available if you switch layouts.

### BasicTimeline {#BasicTimeline}
```
public static final int BasicTimeline
```

Use to show sequential steps in a task, process, or workflow, or to show timeline information. Works well with both Cấp độ 1 and Cấp độ 2 text.

### BasicVenn {#BasicVenn}
```
public static final int BasicVenn
```

Use to show overlapping or interconnected relationships. The first seven lines of Cấp độ 1 text correspond with a circle. If there are four or fewer lines of Cấp độ 1 text, the text is inside the circles. If there are more than four lines of Cấp độ 1 text, the text is outside of the circles. Unused text does not appear, but remains available if you switch layouts.

### BendingPictureAccentList {#BendingPictureAccentList}
```
public static final int BendingPictureAccentList
```

Use to show non-sequential or grouped blocks of information. The small circular shapes are designed to contain pictures. Works well for illustrating both Cấp độ 1 and Cấp độ 2 text. Maximizes both horizontal and vertical display space for shapes.

### BendingPictureBlocks {#BendingPictureBlocks}
```
public static final int BendingPictureBlocks
```

Use to show a series of pictures. The box covering the bottom corner can contain small amounts of text.

### BendingPictureCaption {#BendingPictureCaption}
```
public static final int BendingPictureCaption
```

Use to show a sequential series of pictures. The box covering the bottom corner can contain small amounts of text.

### BendingPictureCaptionList {#BendingPictureCaptionList}
```
public static final int BendingPictureCaptionList
```

Use to show a series of pictures. The title and description appear in a callout shape under each picture.

### BendingPictureSemiTransparentText {#BendingPictureSemiTransparentText}
```
public static final int BendingPictureSemiTransparentText
```

Use to show a series of pictures. A semi-transparent box covers the lower portion of the picture and contains all levels of text.

### BlockCycle {#BlockCycle}
```
public static final int BlockCycle
```

Use to represent a continuing sequence of stages, tasks, or events in a circular flow. Emphasizes the stages or steps rather than the connecting arrows or flow.

### BubblePictureList {#BubblePictureList}
```
public static final int BubblePictureList
```

Use to show a series of pictures. Can contain up to eight Cấp độ 1 pictures. Unused text and pictures do not appear, but remain available if you switch layouts. Works best with small amounts of text.

### CaptionedPictures {#CaptionedPictures}
```
public static final int CaptionedPictures
```

Use to show pictures with multiple levels of text. Works best with a small amount of Cấp độ 1 text and a medium amount of Cấp độ 2 text.

### ChevronList {#ChevronList}
```
public static final int ChevronList
```

Use to show a progression through several processes that make up an overall workflow. Also works for illustrating contrasting processes. The Cấp độ 1 text corresponds to the first arrow shape on the left, while the Cấp độ 2 text corresponds to horizontal sub-steps for each shape that contains Cấp độ 1 text.

### CircleAccentTimeline {#CircleAccentTimeline}
```
public static final int CircleAccentTimeline
```

Use to show a series of events or timeline information. Cấp độ 1 text appears next to larger circular shapes. Cấp độ 2 text appears next to smaller circular shapes.

### CircleArrowProcess {#CircleArrowProcess}
```
public static final int CircleArrowProcess
```

Use to show sequential items with supporting text for each item. This diagram works best with small amounts of Cấp độ 1 text.

### CirclePictureHierarchy {#CirclePictureHierarchy}
```
public static final int CirclePictureHierarchy
```

Use to show hierarchical information or reporting relationships in an organization. Pictures appear in circles and corresponding text appears next to the pictures.

### CircleRelationship {#CircleRelationship}
```
public static final int CircleRelationship
```

Use to show the relationship to or from a central idea. Cấp độ 2 text is added non-sequentially and is limited to five items. There can only be one Cấp độ 1 item.

### CircularBendingProcess {#CircularBendingProcess}
```
public static final int CircularBendingProcess
```

Use to show a long or non-linear sequence or steps in a task, process, or workflow. Works best with Cấp độ 1 text only. Maximizes both horizontal and vertical display space for shapes.

### CircularPictureCallout {#CircularPictureCallout}
```
public static final int CircularPictureCallout
```

Use to show a central idea and sub-ideas or related items. The text for the first picture covers the lower portion of the picture. The corresponding text for other Cấp độ 1 shapes appears next to the small circular pictures. This diagram also works well with no text.

### ClosedChevronProcess {#ClosedChevronProcess}
```
public static final int ClosedChevronProcess
```

Use to show a progression, a timeline, or sequential steps in a task, process, or workflow, or to emphasize movement or direction. Can be used to emphasize information in the starting shape. Works best with Cấp độ 1 text only.

### ContinuousArrowProcess {#ContinuousArrowProcess}
```
public static final int ContinuousArrowProcess
```

Use to show a timeline or sequential steps in a task, process, or workflow. Works best with Cấp độ 1 text because each line of Cấp độ 1 text appears inside the arrow shape. Cấp độ 2 text appears outside the arrow shape.

### ContinuousBlockProcess {#ContinuousBlockProcess}
```
public static final int ContinuousBlockProcess
```

Use to show a progression or sequential steps in a task, process, or workflow. Works best with minimal Cấp độ 1 and Cấp độ 2 text.

### ContinuousCycle {#ContinuousCycle}
```
public static final int ContinuousCycle
```

Use to represent a continuing sequence of stages, tasks, or events in a circular flow. Emphasizes the connection between all components. Works best with Cấp độ 1 text only.

### ContinuousPictureList {#ContinuousPictureList}
```
public static final int ContinuousPictureList
```

Use to show groups of interconnected information. The circular shapes are designed to contain pictures.

### ConvergingArrows {#ConvergingArrows}
```
public static final int ConvergingArrows
```

Use to show ideas or concepts that converge to a central point. Works best with Cấp độ 1 text only.

### ConvergingRadial {#ConvergingRadial}
```
public static final int ConvergingRadial
```

Use to show relationships of concepts or components to a central idea in a cycle. The first line of Cấp độ 1 text corresponds to the central circular shape and the lines of Cấp độ 2 text correspond to the surrounding rectangular shapes. Unused text does not appear, but remains available if you switch layouts.

### CounterbalanceArrows {#CounterbalanceArrows}
```
public static final int CounterbalanceArrows
```

Use to show two opposing ideas or concepts. Each of the first two lines of Cấp độ 1 text corresponds to an arrow and works well with Cấp độ 2 text. Unused text does not appear, but remains available if you switch layouts.

### CycleMatrix {#CycleMatrix}
```
public static final int CycleMatrix
```

Use to show the relationship to a central idea in a cyclical progression. Each of the first four lines of Cấp độ 1 text corresponds to a wedge or pie shape, and Cấp độ 2 text appears in a rectangular shape to the side of the wedge or pie shape. Unused text does not appear, but remains available if you switch layouts.

### DescendingBlockList {#DescendingBlockList}
```
public static final int DescendingBlockList
```

Use to show groups of related ideas or lists of information. The text shapes decrease in height sequentially, and the Cấp độ 1 text displays vertically.

### DescendingProcess {#DescendingProcess}
```
public static final int DescendingProcess
```

Use to show a descending series of events. The first Cấp độ 1 text is at the top of arrow, and the last Cấp độ 1 text displays at the bottom of the arrow. Only the first seven Cấp độ 1 items appear. Works best with small to medium amounts of text.

### DetailedProcess {#DetailedProcess}
```
public static final int DetailedProcess
```

Use with large amounts of Cấp độ 2 text to show a progression through stages.

### DivergingArrows {#DivergingArrows}
```
public static final int DivergingArrows
```

Use to show ideas or concepts that progress outward from a central source. Works best with Cấp độ 1 text only.

### DivergingRadial {#DivergingRadial}
```
public static final int DivergingRadial
```

Use to show ideas or concepts that progress outward from a central source. Works best with Cấp độ 1 text only.
Use to show relationships to a central idea in a cycle. The first Level 1 line of text corresponds to the central circular shape. Emphasizes the surrounding circles rather than the central idea. Unused text does not appear, but remains available if you switch layouts.

### Equation {#Equation}
```
public static final int Equation
```


Use to show sequential steps or tasks that depict a plan or result. The last Level 1 line of text appears after the equals sign (=).Works best with Level 1 text only.

### FramedTextPicture {#FramedTextPicture}
```
public static final int FramedTextPicture
```


Use to show pictures with corresponding Level 1 text displayed in a frame.

### Funnel {#Funnel}
```
public static final int Funnel
```


Use to show the filtering of information or how parts merge into a whole. Emphasizes the final outcome. Can contain up to four lines of Level 1 text; the last of these four Level 1 text lines appears below the funnel and the other lines correspond to a circular shape. Unused text does not appear, but remains available if you switch layouts.

### Gear {#Gear}
```
public static final int Gear
```


Use to show interlocking ideas. Each of the first three lines of Level 1 text corresponds to a gear shape, and their corresponding Level 2 text appears in rectangles next to the gear shape. Unused text does not appear, but remains available if you switch layouts.

### GridMatrix {#GridMatrix}
```
public static final int GridMatrix
```


Use to show the placement of concepts along two axes. Emphasizes the individual components rather than the whole. The first four lines of Level 1 text appear in the quadrants. Unused text does not appear, but remains available if you switch layouts.

### GroupedList {#GroupedList}
```
public static final int GroupedList
```


Use to show groups and sub-groups of information, or steps and sub-steps in a task, process, or workflow. Level 1 text corresponds to the top level horizontal shapes, and Level 2 text corresponds to vertical sub-steps under each related top level shape. Works well for emphasizing sub-groups or sub-steps, hierarchical information, or multiple lists of information.

### HalfCircleOrganizationChart {#HalfCircleOrganizationChart}
```
public static final int HalfCircleOrganizationChart
```


Use to show hierarchical information or reporting relationships in an organization. The assistant shapes and Org Chart hanging layouts are available with this layout.

### HexagonCluster {#HexagonCluster}
```
public static final int HexagonCluster
```


Use to show pictures with associated descriptive text. Small hexagons indicate the picture and text pair. Works best with small amounts of text.

### Hierarchy {#Hierarchy}
```
public static final int Hierarchy
```


Use to show hierarchical relationships progressing from top to bottom.

### HierarchyList {#HierarchyList}
```
public static final int HierarchyList
```


Use to show hierarchical relationships progressing across groups. Can also be used to group or list information.

### HorizontalBulletList {#HorizontalBulletList}
```
public static final int HorizontalBulletList
```


Use to show non-sequential or grouped lists of information. Works well with large amounts of text. All text has the same level of emphasis, and direction is not implied.

### HorizontalHierarchy {#HorizontalHierarchy}
```
public static final int HorizontalHierarchy
```


Use to show hierarchical relationships progressing horizontally. Works well for decision trees.

### HorizontalLabeledHierarchy {#HorizontalLabeledHierarchy}
```
public static final int HorizontalLabeledHierarchy
```


Use to show hierarchical relationships progressing horizontally and grouped hierarchically. Emphasizes heading or level 1 text. The first line of Level 1 text appears in the shape at the beginning of the hierarchy, and the second and all subsequent lines of Level 1 text appear at the top of the tall rectangles.

### HorizontalMultiLevelHierarchy {#HorizontalMultiLevelHierarchy}
```
public static final int HorizontalMultiLevelHierarchy
```


Use to show large amounts of hierarchical information progressing horizontally. The top of the hierarchy is displayed vertically. This layout supports many levels in the hierarchy.

### HorizontalOrganizationChart {#HorizontalOrganizationChart}
```
public static final int HorizontalOrganizationChart
```


Use to show hierarchical information horizontally or reporting relationships in an organization. The assistant shape and the Org Chart hanging layouts are available with this layout.

### HorizontalPictureList {#HorizontalPictureList}
```
public static final int HorizontalPictureList
```


Use to show non-sequential or grouped information with an emphasis on related pictures. The top shapes are designed to contain pictures.

### IncreasingArrowsProcess {#IncreasingArrowsProcess}
```
public static final int IncreasingArrowsProcess
```


Use to show sequential and overlapping steps in a process. Limited to five Level 1 items. Level 2 can contain large amounts of text.

### IncreasingCircleProcess {#IncreasingCircleProcess}
```
public static final int IncreasingCircleProcess
```


Use to show a series of steps, with the interior of the circle increasing with each step. Limited to seven Level 1 steps but unlimited Level 2 items. Works well with large amounts of Level 2 text.

### InvertedPyramid {#InvertedPyramid}
```
public static final int InvertedPyramid
```


Use to show proportional, interconnected, or hierarchical relationships with the largest component on the top and narrowing down. Level 1 text appears in the pyramid segments and Level 2 text appears in shapes alongside each segment.

### LabeledHierarchy {#LabeledHierarchy}
```
public static final int LabeledHierarchy
```


Use to show hierarchical relationships progressing from top to bottom and grouped hierarchically. Emphasizes heading or level 1 text. The first line of Level 1 text appears in the shape at the beginning of the hierarchy, and all subsequent lines of Level 1 text appear to the left of the long rectangles.

### LinearVenn {#LinearVenn}
```
public static final int LinearVenn
```


Use to show overlapping relationships in a sequence. Works best with Level 1 text only.

### LinedList {#LinedList}
```
public static final int LinedList
```


Use to show large amounts of text divided into categories and subcategories. Works well with multiple levels of text. Text at the same level is separated by lines.

### MultidirectionalCycle {#MultidirectionalCycle}
```
public static final int MultidirectionalCycle
```


Use to represent a continuing sequence of stages, tasks, or events that can occur in any direction.

### NameandTitleOrganizationChart {#NameandTitleOrganizationChart}
```
public static final int NameandTitleOrganizationChart
```


Use to show hierarchical information or reporting relationships in an organization. To enter text in the title box, type directly in the smaller rectangular shape. The assistant shape and Org Chart hanging layouts are available with this layout.

### NestedTarget {#NestedTarget}
```
public static final int NestedTarget
```


Use to show containment relationships. Each of the first three lines of Level 1 text correspond to the upper left text in the shapes, and Level 2 text corresponds to the smaller shapes. Works best with minimal Level 2 lines of text. Unused text does not appear, but remains available if you switch layouts.

### NondirectionalCycle {#NondirectionalCycle}
```
public static final int NondirectionalCycle
```


Use to represent a continuing sequence of stages, tasks, or events in a circular flow. Each shape has the same level of importance. Works well when direction does not need to be indicated.

### OpposingArrows {#OpposingArrows}
```
public static final int OpposingArrows
```


Use to show two opposing ideas, or ideas that diverge from a central point. Each of the first two lines of Level 1 text corresponds to an arrow. Unused text does not appear, but remains available if you switch layouts.

### OpposingIdeas {#OpposingIdeas}
```
public static final int OpposingIdeas
```


Use to show two opposing or contrasting ideas. Can have one or two Level 1 items. Each Level 1 text can contain multiple sub-levels. Works well with large amounts of text.

### OrganizationChart {#OrganizationChart}
```
public static final int OrganizationChart
```


Use to show hierarchical information or reporting relationships in an organization. The assistant shape and the Org Chart hanging layouts are available with this layout.

### PhasedProcess {#PhasedProcess}
```
public static final int PhasedProcess
```


Use to show three phases of a process. Limited to three Level 1 items. The first two Level 1 items can each contain four Level 2 items, and the third Level 1 item can contain an unlimited number of Level 2 items. Works best with small amounts of text.

### PictureAccentBlocks {#PictureAccentBlocks}
```
public static final int PictureAccentBlocks
```


Use to show a group of pictures in blocks starting from the corner. The corresponding text displays vertically. Works well as an accent on title or sub-title slides or for section breaks of a document.

### PictureAccentList {#PictureAccentList}
```
public static final int PictureAccentList
```


Use to show grouped or related information. The small shapes on the upper corners are designed to contain pictures. Emphasizes Level 2 text over Level 1 text, and is a good choice for large amounts of Level 2 text.

### PictureAccentProcess {#PictureAccentProcess}
```
public static final int PictureAccentProcess
```


Use to show sequential steps in a task, process, or workflow. The rectangular shapes in the background are designed to contain pictures.

### PictureCaptionList {#PictureCaptionList}
```
public static final int PictureCaptionList
```


Use to show non-sequential or grouped blocks of information. The top shapes are designed to contain pictures and pictures are emphasized over text. Works well for pictures with short text captions.

### PictureGrid {#PictureGrid}
```
public static final int PictureGrid
```


Use to show pictures laid out on a square grid. Best with a small amount of Level 1 text, which appears above the picture.

### PictureLineup {#PictureLineup}
```
public static final int PictureLineup
```


Use to show a series of pictures side by side. Level 1 text covers the top of the picture. Level 2 text appears below the picture.

### PictureStrips {#PictureStrips}
```
public static final int PictureStrips
```


Use to show a series of pictures from top to bottom with Level 1 text beside each.

### PieProcess {#PieProcess}
```
public static final int PieProcess
```


Use to show steps in a process with each pie slice increasing in size up to seven shapes. Level 1 text displays vertically.

### PlusandMinus {#PlusandMinus}
```
public static final int PlusandMinus
```


Use to show the pros and cons of two ideas. Each Level 1 text can contain multiple sub-levels. Works well with large amounts of text. Limited to two Level 1 items.

### ProcessArrows {#ProcessArrows}
```
public static final int ProcessArrows
```


Use to show information illustrating a process or workflow. Level 1 text appears in the circular shapes and Level 2 text appears in the arrow shapes. Works best for minimal text and to emphasize movement or direction.

### ProcessList {#ProcessList}
```
public static final int ProcessList
```


Use to show multiple groups of information or steps and sub-steps in a task, process, or workflow. Level 1 text corresponds to the top horizontal shapes, and Level 2 text corresponds to vertical sub-steps under each related top level shape.

### PyramidList {#PyramidList}
```
public static final int PyramidList
```


Use to show proportional, interconnected, or hierarchical relationships. Text appears in the rectangular shapes on top of the pyramid background.

### RadialCluster {#RadialCluster}
```
public static final int RadialCluster
```


Use to show data that relates to a central idea or theme. The top Level 1 text appears in the center. Level 2 text appears in surrounding shapes. Can contain up to seven Level 2 shapes. Unused text does not appear, but remains available if you switch layouts. Works best with small amounts of text.

### RadialCycle {#RadialCycle}
```
public static final int RadialCycle
```


Use to show the relationship to a central idea. Emphasizes both information in the center circle and how information in the outer ring of circles contributes to the central idea. The first Level 1 line of text corresponds to the central circle, and its Level 2 text corresponds to the outer ring of circles. Unused text does not appear, but remains available if you switch layouts.

### RadialList {#RadialList}
```
public static final int RadialList
```


Use to show relationships to a central idea in a cycle. The center shape can contain a picture. Level 1 text appears in the smaller circles and any related Level 2 text appears to the side of the smaller circles.

### RadialVenn {#RadialVenn}
```
public static final int RadialVenn
```


Use to show both overlapping relationships and the relationship to a central idea in a cycle. The first line of Level 1 text corresponds to the central shape and the lines of Level 2 text correspond to the surrounding circular shapes. Unused text does not appear, but remains available if you switch layouts.

### RandomToResultProcess {#RandomToResultProcess}
```
public static final int RandomToResultProcess
```
Được dùng để hiển thị, qua một loạt các bước, cách mà một vài ý tưởng hỗn loạn có thể dẫn đến một mục tiêu hoặc ý tưởng thống nhất. Hỗ trợ nhiều mục văn bản cấp 1, nhưng các hình dạng tương ứng của cấp 1 đầu tiên và cuối cùng là cố định. Hoạt động tốt nhất với lượng văn bản cấp 1 ít và lượng văn bản cấp 2 vừa phải.

### RepeatingBendingProcess {#RepeatingBendingProcess}
```
public static final int RepeatingBendingProcess
```

Được dùng để hiển thị một quá trình tiến triển hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. Tối đa hoá không gian hiển thị theo chiều ngang và chiều dọc cho các hình dạng.

### ReverseList {#ReverseList}
```
public static final int ReverseList
```

Được dùng để chuyển đổi giữa hai mục. Chỉ hiển thị hai mục văn bản đầu tiên, và mỗi mục có thể chứa một lượng lớn văn bản. Thích hợp để hiển thị sự thay đổi giữa hai mục hoặc sự dịch chuyển thứ tự.

### SegmentedCycle {#SegmentedCycle}
```
public static final int SegmentedCycle
```

Được dùng để hiển thị một quá trình tiến triển hoặc một chuỗi các giai đoạn, nhiệm vụ, hoặc sự kiện trong một luồng vòng tròn. Nhấn mạnh các phần liên kết với nhau. Mỗi bảy dòng đầu tiên của văn bản cấp 1 tương ứng với một hình dạng hình bánh hoặc hình bánh tròn. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn khả dụng nếu bạn chuyển bố cục.

### SegmentedProcess {#SegmentedProcess}
```
public static final int SegmentedProcess
```

Được dùng để hiển thị một quá trình tiến triển hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. Nhấn mạnh văn bản cấp 2, vì mỗi dòng xuất hiện trong một hình dạng riêng biệt.

### SegmentedPyramid {#SegmentedPyramid}
```
public static final int SegmentedPyramid
```

Được dùng để hiển thị các mối quan hệ bao hàm, tỉ lệ hoặc liên kết với nhau. Chín dòng đầu tiên của văn bản cấp 1 xuất hiện trong các hình tam giác. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn khả dụng nếu bạn chuyển bố cục. Hoạt động tốt nhất khi chỉ có văn bản cấp 1.

### SnapshotPictureList {#SnapshotPictureList}
```
public static final int SnapshotPictureList
```

Được dùng để hiển thị hình ảnh kèm theo văn bản giải thích. Văn bản cấp 2 có thể hiển thị danh sách thông tin. Thích hợp với một lượng lớn văn bản.

### SpiralPicture {#SpiralPicture}
```
public static final int SpiralPicture
```

Được dùng để hiển thị một chuỗi tối đa năm hình ảnh kèm chú thích cấp 1 xoáy vào trung tâm.

### SquareAccentList {#SquareAccentList}
```
public static final int SquareAccentList
```

Được dùng để hiển thị danh sách thông tin được chia thành các danh mục. Văn bản cấp 2 xuất hiện bên cạnh một hình vuông nhỏ. Thích hợp với lượng lớn văn bản cấp 2.

### StackedList {#StackedList}
```
public static final int StackedList
```

Được dùng để hiển thị các nhóm thông tin hoặc các bước trong một nhiệm vụ, quy trình hoặc luồng công việc. Các hình dạng vòng tròn chứa văn bản cấp 1, và các hình chữ nhật tương ứng chứa văn bản cấp 2. Thích hợp cho nhiều chi tiết và ít văn bản cấp 1.

### StackedVenn {#StackedVenn}
```
public static final int StackedVenn
```

Được dùng để hiển thị các mối quan hệ chồng chéo. Lựa chọn tốt để nhấn mạnh sự phát triển hoặc độ dần. Hoạt động tốt nhất khi chỉ có văn bản cấp 1. Bảy dòng đầu tiên của văn bản cấp 1 tương ứng với một hình dạng vòng tròn. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn khả dụng nếu bạn chuyển bố cục.

### StaggeredProcess {#StaggeredProcess}
```
public static final int StaggeredProcess
```

Được dùng để hiển thị một quá trình tiến xuống qua các giai đoạn. Mỗi năm dòng đầu tiên của văn bản cấp 1 tương ứng với một hình chữ nhật. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn khả dụng nếu bạn chuyển bố cục.

### StepDownProcess {#StepDownProcess}
```
public static final int StepDownProcess
```

Được dùng để hiển thị một quá trình hạ xuống với nhiều bước và bước con. Hoạt động tốt nhất với một lượng nhỏ văn bản.

### StepUpProcess {#StepUpProcess}
```
public static final int StepUpProcess
```

Được dùng để hiển thị một chuỗi các bước tăng lên hoặc danh sách thông tin.

### SubStepProcess {#SubStepProcess}
```
public static final int SubStepProcess
```

Được dùng để hiển thị một quá trình đa bước với các bước con giữa mỗi đoạn văn bản cấp 1. Hoạt động tốt nhất với một lượng nhỏ văn bản và giới hạn bảy bước cấp 1. Mỗi bước cấp 1 có thể có số bước con không giới hạn.

### TableHierarchy {#TableHierarchy}
```
public static final int TableHierarchy
```

Được dùng để hiển thị các nhóm thông tin được xây dựng từ trên xuống dưới, và các cấp bậc trong mỗi nhóm. Bố cục này không chứa các đường nối.

### TableList {#TableList}
```
public static final int TableList
```

Được dùng để hiển thị thông tin được nhóm hoặc liên quan có giá trị bằng nhau. Dòng văn bản cấp 1 đầu tiên tương ứng với hình dạng trên cùng và văn bản cấp 2 của nó được dùng cho các danh sách tiếp theo.

### TargetList {#TargetList}
```
public static final int TargetList
```

Được dùng để hiển thị thông tin có quan hệ chồng chéo hoặc giao nhau. Bảy dòng đầu tiên của văn bản cấp 1 xuất hiện trong hình chữ nhật. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn khả dụng nếu bạn chuyển bố cục. Thích hợp cho cả văn bản cấp 1 và cấp 2.

### TextCycle {#TextCycle}
```
public static final int TextCycle
```

Được dùng để thể hiện một chuỗi liên tục các giai đoạn, nhiệm vụ hoặc sự kiện trong một luồng vòng tròn. Nhấn mạnh các mũi tên hoặc luồng chứ không phải các giai đoạn hay bước. Hoạt động tốt nhất khi chỉ có văn bản cấp 1.

### TitlePictureLineup {#TitlePictureLineup}
```
public static final int TitlePictureLineup
```

Được dùng để hiển thị một loạt hình ảnh, mỗi hình có tiêu đề và mô tả riêng. Văn bản cấp 1 xuất hiện trong hộp phía trên hình ảnh. Văn bản cấp 2 xuất hiện dưới hình ảnh.

### TitledMatrix {#TitledMatrix}
```
public static final int TitledMatrix
```

Được dùng để hiển thị mối quan hệ của bốn phần tư với tổng thể. Dòng văn bản cấp 1 đầu tiên tương ứng với hình dạng trung tâm, và bốn dòng đầu tiên của văn bản cấp 2 xuất hiện trong các phần tư. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn khả dụng nếu bạn chuyển bố cục.

### TitledPictureAccentList {#TitledPictureAccentList}
```
public static final int TitledPictureAccentList
```

Được dùng để hiển thị danh sách thông tin với một hình ảnh nhấn mạnh cho mỗi văn bản cấp 2. Văn bản cấp 1 hiển thị trong một hộp riêng ở đầu danh sách.

### TitledPictureBlocks {#TitledPictureBlocks}
```
public static final int TitledPictureBlocks
```

Được dùng để hiển thị một loạt hình ảnh. Văn bản cấp 1 xuất hiện phía trên mỗi hình ảnh. Văn bản cấp 2 xuất hiện ở bên cạnh và hơi chồng lên mỗi hình ảnh.

### TrapezoidList {#TrapezoidList}
```
public static final int TrapezoidList
```

Được dùng để hiển thị các thông tin được nhóm hoặc liên quan có giá trị bằng nhau. Thích hợp với lượng lớn văn bản.

### UpwardArrow {#UpwardArrow}
```
public static final int UpwardArrow
```

Được dùng để hiển thị một quá trình tiến triển hoặc các bước có xu hướng đi lên trong một nhiệm vụ, quy trình hoặc luồng công việc. Năm dòng đầu tiên của văn bản cấp 1 tương ứng với các điểm trên mũi tên. Hoạt động tốt nhất với văn bản tối thiểu. Văn bản không sử dụng sẽ không hiển thị, nhưng vẫn khả dụng nếu bạn chuyển bố cục.

### VerticalAccentList {#VerticalAccentList}
```
public static final int VerticalAccentList
```

Được dùng để hiển thị danh sách thông tin. Văn bản cấp 2 xuất hiện trong các hình chữ nhật trên các mũi tên chevron dọc. Nhấn mạnh văn bản cấp 2 hơn văn bản cấp 1, và là lựa chọn tốt cho lượng văn bản cấp 2 trung bình.

### VerticalArrowList {#VerticalArrowList}
```
public static final int VerticalArrowList
```

Được dùng để hiển thị một quá trình tiến triển hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc hướng tới một mục tiêu chung. Thích hợp cho các danh sách gạch đầu dòng của thông tin.

### VerticalBendingProcess {#VerticalBendingProcess}
```
public static final int VerticalBendingProcess
```

Được dùng để hiển thị một quá trình tiến triển hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc. Tối đa hoá không gian hiển thị theo chiều ngang và chiều dọc cho các hình dạng. Đặt nặng hơn vào các mối quan hệ giữa các hình dạng hơn là hướng hay chuyển động.

### VerticalBlockList {#VerticalBlockList}
```
public static final int VerticalBlockList
```

Được dùng để hiển thị các nhóm thông tin hoặc các bước trong một nhiệm vụ, quy trình hoặc luồng công việc. Thích hợp với lượng lớn văn bản cấp 2. Lựa chọn tốt cho văn bản có một ý chính và nhiều điểm phụ.

### VerticalBoxList {#VerticalBoxList}
```
public static final int VerticalBoxList
```

Được dùng để hiển thị nhiều nhóm thông tin, đặc biệt là các nhóm có lượng lớn văn bản cấp 2. Lựa chọn tốt cho các danh sách gạch đầu dòng của thông tin.

### VerticalBulletList {#VerticalBulletList}
```
public static final int VerticalBulletList
```

Được dùng để hiển thị các khối thông tin không tuần tự hoặc được nhóm lại. Thích hợp cho các danh sách có tiêu đề dài hoặc thông tin cấp cao nhất.

### VerticalChevronList {#VerticalChevronList}
```
public static final int VerticalChevronList
```

Được dùng để hiển thị một quá trình tiến triển hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc, hoặc để nhấn mạnh chuyển động hoặc hướng. Nhấn mạnh văn bản cấp 2 hơn văn bản cấp 1, và là lựa chọn tốt cho lượng lớn văn bản cấp 2.

### VerticalCircleList {#VerticalCircleList}
```
public static final int VerticalCircleList
```

Được dùng để hiển thị dữ liệu tuần tự hoặc được nhóm. Hoạt động tốt nhất cho văn bản cấp 1, hiển thị cạnh một hình dạng vòng tròn lớn. Các cấp dưới của văn bản được tách ra bằng các hình dạng vòng tròn nhỏ hơn.

### VerticalCurvedList {#VerticalCurvedList}
```
public static final int VerticalCurvedList
```

Được dùng để hiển thị một danh sách thông tin cong. Để thêm hình ảnh vào các hình tròn nhấn mạnh, áp dụng tô hình ảnh.

### VerticalEquation {#VerticalEquation}
```
public static final int VerticalEquation
```

Được dùng để hiển thị các bước hoặc nhiệm vụ tuần tự mô tả một kế hoạch hoặc kết quả. Dòng văn bản cấp 1 cuối cùng xuất hiện sau mũi tên. Hoạt động tốt nhất khi chỉ có văn bản cấp 1.

### VerticalPictureAccentList {#VerticalPictureAccentList}
```
public static final int VerticalPictureAccentList
```

Được dùng để hiển thị các khối thông tin không tuần tự hoặc được nhóm lại. Các vòng tròn nhỏ được thiết kế để chứa hình ảnh.

### VerticalPictureList {#VerticalPictureList}
```
public static final int VerticalPictureList
```

Được dùng để hiển thị các khối thông tin không tuần tự hoặc được nhóm lại. Các hình dạng nhỏ phía bên trái được thiết kế để chứa hình ảnh.

### VerticalProcess {#VerticalProcess}
```
public static final int VerticalProcess
```

Được dùng để hiển thị một quá trình tiến triển hoặc các bước tuần tự trong một nhiệm vụ, quy trình hoặc luồng công việc từ trên xuống dưới. Hoạt động tốt nhất với văn bản cấp 1, vì không gian dọc bị hạn chế.

### Custom {#Custom}
```
public static final int Custom
```

Biểu diễn một biểu đồ SmartArt với mẫu bố cục tùy chỉnh

### PictureOrganizationChart {#PictureOrganizationChart}
```
public static final int PictureOrganizationChart
```

Được dùng để hiển thị thông tin phân cấp hoặc mối quan hệ báo cáo trong một tổ chức, kèm theo các hình ảnh tương ứng. Các hình dạng trợ lý và bố cục treo Org Chart có sẵn với bố cục này.