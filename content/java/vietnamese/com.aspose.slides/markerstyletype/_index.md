---
title: MarkerStyleType
second_title: Tham chiếu API Aspose.Slides cho Java
description: Xác định dạng của điểm đánh dấu trên điểm dữ liệu của biểu đồ.
type: docs
url: /vi/com.aspose.slides/markerstyletype/
---
**Kế thừa:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class MarkerStyleType extends System.Enum
```

Xác định dạng của điểm đánh dấu trên điểm dữ liệu của biểu đồ.
## Trường

| Trường | Mô tả |
| --- | --- |
| [NotDefined](#NotDefined) |  |
| [Circle](#Circle) | Chỉ định một vòng tròn sẽ được vẽ ở mỗi điểm dữ liệu. |
| [Dash](#Dash) | Chỉ định một dấu gạch ngang sẽ được vẽ ở mỗi điểm dữ liệu. |
| [Diamond](#Diamond) | Chỉ định một hình thoi sẽ được vẽ ở mỗi điểm dữ liệu. |
| [Dot](#Dot) | Chỉ định một chấm sẽ được vẽ ở mỗi điểm dữ liệu. |
| [None](#None) | Chỉ định không có gì sẽ được vẽ ở mỗi điểm dữ liệu. |
| [Picture](#Picture) | Chỉ định một hình ảnh sẽ được vẽ ở mỗi điểm dữ liệu. |
| [Plus](#Plus) | Chỉ định một dấu cộng sẽ được vẽ ở mỗi điểm dữ liệu. |
| [Square](#Square) | Chỉ định một hình vuông sẽ được vẽ ở mỗi điểm dữ liệu. |
| [Star](#Star) | Chỉ định một ngôi sao sẽ được vẽ ở mỗi điểm dữ liệu. |
| [Triangle](#Triangle) | Chỉ định một tam giác sẽ được vẽ ở mỗi điểm dữ liệu. |
| [X](#X) | Chỉ định một X sẽ được vẽ ở mỗi điểm dữ liệu. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```




### Circle {#Circle}
```
public static final int Circle
```


Chỉ định một vòng tròn sẽ được vẽ ở mỗi điểm dữ liệu.

### Dash {#Dash}
```
public static final int Dash
```


Chỉ định một dấu gạch ngang sẽ được vẽ ở mỗi điểm dữ liệu.

### Diamond {#Diamond}
```
public static final int Diamond
```


Chỉ định một hình thoi sẽ được vẽ ở mỗi điểm dữ liệu.

### Dot {#Dot}
```
public static final int Dot
```


Chỉ định một chấm sẽ được vẽ ở mỗi điểm dữ liệu.

### None {#None}
```
public static final int None
```


Chỉ định không có gì sẽ được vẽ ở mỗi điểm dữ liệu.

### Picture {#Picture}
```
public static final int Picture
```


Chỉ định một hình ảnh sẽ được vẽ ở mỗi điểm dữ liệu.

### Plus {#Plus}
```
public static final int Plus
```


Chỉ định một dấu cộng sẽ được vẽ ở mỗi điểm dữ liệu.

### Square {#Square}
```
public static final int Square
```


Chỉ định một hình vuông sẽ được vẽ ở mỗi điểm dữ liệu.

### Star {#Star}
```
public static final int Star
```


Chỉ định một ngôi sao sẽ được vẽ ở mỗi điểm dữ liệu.

### Triangle {#Triangle}
```
public static final int Triangle
```


Chỉ định một tam giác sẽ được vẽ ở mỗi điểm dữ liệu.

### X {#X}
```
public static final int X
```


Chỉ định một X sẽ được vẽ ở mỗi điểm dữ liệu.