---
title: ILayoutable
second_title: Tham chiếu API Aspose.Slides cho Java
description: Xác định vị trí chính xác của một phần tử biểu đồ.
type: docs
url: /vi/com.aspose.slides/ilayoutable/
---
**Tất cả các giao diện được thực hiện:**
[com.aspose.slides.IChartComponent](../../com.aspose.slides/ichartcomponent)
```
public interface ILayoutable extends IChartComponent
```

Xác định vị trí chính xác của một phần tử biểu đồ.
## Phương thức

| Method | Mô tả |
| --- | --- |
| [getX()](#getX--) | Xác định vị trí x (trái) của phần tử biểu đồ dưới dạng một phần của chiều rộng của biểu đồ. |
| [setX(float value)](#setX-float-) | Xác định vị trí x (trái) của phần tử biểu đồ dưới dạng một phần của chiều rộng của biểu đồ. |
| [getY()](#getY--) | Xác định vị trí trên của phần tử biểu đồ dưới dạng một phần của chiều cao của biểu đồ. |
| [setY(float value)](#setY-float-) | Xác định vị trí trên của phần tử biểu đồ dưới dạng một phần của chiều cao của biểu đồ. |
| [getWidth()](#getWidth--) | Xác định chiều rộng của phần tử biểu đồ dưới dạng một phần của chiều rộng của biểu đồ. |
| [setWidth(float value)](#setWidth-float-) | Xác định chiều rộng của phần tử biểu đồ dưới dạng một phần của chiều rộng của biểu đồ. |
| [getHeight()](#getHeight--) | Xác định chiều cao của phần tử biểu đồ dưới dạng một phần của chiều cao của biểu đồ. |
| [setHeight(float value)](#setHeight-float-) | Xác định chiều cao của phần tử biểu đồ dưới dạng một phần của chiều cao của biểu đồ. |
| [getRight()](#getRight--) | Lấy giá trị bên phải của phần tử biểu đồ dưới dạng một phần của chiều rộng của biểu đồ. |
| [getBottom()](#getBottom--) | Lấy giá trị phía trên của phần tử biểu đồ dưới dạng một phần của chiều cao của biểu đồ. |
### getX() {#getX--}
```
public abstract float getX()
```

Xác định vị trí x (trái) của phần tử biểu đồ dưới dạng một phần của chiều rộng của biểu đồ. Đọc/ghi float.

**Trả về:**
float
### setX(float value) {#setX-float-}
```
public abstract void setX(float value)
```

Xác định vị trí x (trái) của phần tử biểu đồ dưới dạng một phần của chiều rộng của biểu đồ. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getY() {#getY--}
```
public abstract float getY()
```

Xác định vị trí trên của phần tử biểu đồ dưới dạng một phần của chiều cao của biểu đồ. Đọc/ghi float.

**Trả về:**
float
### setY(float value) {#setY-float-}
```
public abstract void setY(float value)
```

Xác định vị trí trên của phần tử biểu đồ dưới dạng một phần của chiều cao của biểu đồ. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getWidth() {#getWidth--}
```
public abstract float getWidth()
```

Xác định chiều rộng của phần tử biểu đồ dưới dạng một phần của chiều rộng của biểu đồ. Đọc/ghi float.

**Trả về:**
float
### setWidth(float value) {#setWidth-float-}
```
public abstract void setWidth(float value)
```

Xác định chiều rộng của phần tử biểu đồ dưới dạng một phần của chiều rộng của biểu đồ. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getHeight() {#getHeight--}
```
public abstract float getHeight()
```

Xác định chiều cao của phần tử biểu đồ dưới dạng một phần của chiều cao của biểu đồ. Đọc/ghi float.

**Trả về:**
float
### setHeight(float value) {#setHeight-float-}
```
public abstract void setHeight(float value)
```

Xác định chiều cao của phần tử biểu đồ dưới dạng một phần của chiều cao của biểu đồ. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getRight() {#getRight--}
```
public abstract float getRight()
```

Lấy giá trị bên phải của phần tử biểu đồ dưới dạng một phần của chiều rộng của biểu đồ. Chỉ đọc float.

**Trả về:**
float
### getBottom() {#getBottom--}
```
public abstract float getBottom()
```

Lấy giá trị phía trên của phần tử biểu đồ dưới dạng một phần của chiều cao của biểu đồ. Chỉ đọc float.

**Trả về:**
float