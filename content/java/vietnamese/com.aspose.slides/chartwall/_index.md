---
title: ChartWall
second_title: Tham chiếu API Aspose.Slides cho Java
description: Biểu diễn các bức tường trên biểu đồ 3D.
type: docs
url: /vi/com.aspose.slides/chartwall/
---
**Kế thừa:**
java.lang.Object, com.aspose.slides.DomObject

**Tất cả các giao diện được thực thi:**
[com.aspose.slides.IChartWall](../../com.aspose.slides/ichartwall)
```
public class ChartWall extends DomObject<Chart> implements IChartWall
```

Biểu diễn các bức tường trên biểu đồ 3D.
## Phương thức

| Phương thức | Mô tả |
| --- | --- |
| [getThickness()](#getThickness--) | Trả về hoặc đặt độ dày tường dưới dạng phần trăm của kích thước lớn nhất của thể tích đồ thị. |
| [setThickness(int value)](#setThickness-int-) | Trả về hoặc đặt độ dày tường dưới dạng phần trăm của kích thước lớn nhất của thể tích đồ thị. |
| [getFormat()](#getFormat--) | Trả về màu nền tường, đường viền, hiệu ứng, kiểu 3D. |
| [getPictureType()](#getPictureType--) | Trả về hoặc đặt loại hình ảnh. |
| [setPictureType(int value)](#setPictureType-int-) | Trả về hoặc đặt loại hình ảnh. |
### getThickness() {#getThickness--}
```
public final int getThickness()
```

Trả về hoặc đặt độ dày tường dưới dạng phần trăm của kích thước lớn nhất của thể tích đồ thị. Đọc/ghi int.

**Trả về:**
int
### setThickness(int value) {#setThickness-int-}
```
public final void setThickness(int value)
```

Trả về hoặc đặt độ dày tường dưới dạng phần trăm của kích thước lớn nhất của thể tích đồ thị. Đọc/ghi int.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | int |  |
### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

Trả về màu nền tường, đường viền, hiệu ứng, kiểu 3D. Chỉ đọc [IFormat](../../com.aspose.slides/iformat).

**Trả về:**
[IFormat](../../com.aspose.slides/iformat)
### getPictureType() {#getPictureType--}
```
public final int getPictureType()
```

Trả về hoặc đặt loại hình ảnh. Đọc/ghi [PictureType](../../com.aspose.slides/picturetype).

**Trả về:**
int
### setPictureType(int value) {#setPictureType-int-}
```
public final void setPictureType(int value)
```

Trả về hoặc đặt loại hình ảnh. Đọc/ghi [PictureType](../../com.aspose.slides/picturetype).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | int |  |