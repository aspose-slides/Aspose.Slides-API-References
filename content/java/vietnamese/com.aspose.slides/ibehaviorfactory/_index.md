---
title: IBehaviorFactory
second_title: Aspose.Slides for Java API Reference
description: Cho phép tạo các hiệu ứng hoạt ảnh
type: docs
url: /vi/com.aspose.slides/ibehaviorfactory/
---```
public interface IBehaviorFactory
```

Cho phép tạo các hiệu ứng hoạt ảnh

--------------------

Để tương thích với COM.

## Phương thức

| Phương thức | Mô tả |
| --- | --- |
| [createColorEffect()](#createColorEffect--) | Tạo hiệu ứng màu. |
| [createCommandEffect()](#createCommandEffect--) | Tạo hiệu ứng lệnh. |
| [createFilterEffect()](#createFilterEffect--) | Tạo hiệu ứng bộ lọc. |
| [createMotionEffect()](#createMotionEffect--) | Tạo hiệu ứng chuyển động. |
| [createPropertyEffect()](#createPropertyEffect--) | Tạo hiệu ứng thuộc tính. |
| [createRotationEffect()](#createRotationEffect--) | Tạo hiệu ứng xoay. |
| [createScaleEffect()](#createScaleEffect--) | Tạo hiệu ứng tỷ lệ. |
| [createSetEffect()](#createSetEffect--) | Tạo hiệu ứng đặt. |
### createColorEffect() {#createColorEffect--}
```
public abstract IColorEffect createColorEffect()
```

Tạo hiệu ứng màu.

**Trả về:**
[IColorEffect](../../com.aspose.slides/icoloreffect) - Hiệu ứng màu.
### createCommandEffect() {#createCommandEffect--}
```
public abstract ICommandEffect createCommandEffect()
```

Tạo hiệu ứng lệnh.

**Trả về:**
[ICommandEffect](../../com.aspose.slides/icommandeffect) - Hiệu ứng lệnh.
### createFilterEffect() {#createFilterEffect--}
```
public abstract IFilterEffect createFilterEffect()
```

Tạo hiệu ứng bộ lọc.

**Trả về:**
[IFilterEffect](../../com.aspose.slides/ifiltereffect) - Hiệu ứng bộ lọc.
### createMotionEffect() {#createMotionEffect--}
```
public abstract IMotionEffect createMotionEffect()
```

Tạo hiệu ứng chuyển động.

**Trả về:**
[IMotionEffect](../../com.aspose.slides/imotioneffect) - Hiệu ứng chuyển động.
### createPropertyEffect() {#createPropertyEffect--}
```
public abstract IPropertyEffect createPropertyEffect()
```

Tạo hiệu ứng thuộc tính.

**Trả về:**
[IPropertyEffect](../../com.aspose.slides/ipropertyeffect) - Hiệu ứng thuộc tính.
### createRotationEffect() {#createRotationEffect--}
```
public abstract IRotationEffect createRotationEffect()
```

Tạo hiệu ứng xoay.

**Trả về:**
[IRotationEffect](../../com.aspose.slides/irotationeffect) - Hiệu ứng xoay.
### createScaleEffect() {#createScaleEffect--}
```
public abstract IScaleEffect createScaleEffect()
```

Tạo hiệu ứng tỷ lệ.

**Trả về:**
[IScaleEffect](../../com.aspose.slides/iscaleeffect) - Hiệu ứng tỷ lệ.
### createSetEffect() {#createSetEffect--}
```
public abstract ISetEffect createSetEffect()
```

Tạo hiệu ứng đặt.

**Trả về:**
[ISetEffect](../../com.aspose.slides/iseteffect) - Hiệu ứng đặt.