---
title: CrossesType
second_title: Tham chiếu API Java của Aspose.Slides
description: Xác định nơi trục sẽ giao cắt.
type: docs
url: /vi/com.aspose.slides/crossestype/
---
**Kế thừa:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class CrossesType extends System.Enum
```

Xác định nơi trục sẽ giao cắt.
## Trường

| Trường | Mô tả |
| --- | --- |
| [AxisCrossesAtZero](#AxisCrossesAtZero) | Trục danh mục giao cắt tại điểm số 0 của trục giá trị (nếu có thể), hoặc giá trị tối thiểu (nếu giá trị tối thiểu lớn hơn số 0) hoặc giá trị tối đa (nếu giá trị tối đa nhỏ hơn số 0). |
| [Maximum](#Maximum) | Trục giao cắt tại giá trị tối đa. |
| [Custom](#Custom) | Giá trị tùy chỉnh từ thuộc tính CrossAt |
### AxisCrossesAtZero {#AxisCrossesAtZero}
```
public static final int AxisCrossesAtZero
```

Trục danh mục giao cắt tại điểm số 0 của trục giá trị (nếu có thể), hoặc giá trị tối thiểu (nếu giá trị tối thiểu lớn hơn số 0) hoặc giá trị tối đa (nếu giá trị tối đa nhỏ hơn số 0).

### Maximum {#Maximum}
```
public static final int Maximum
```

Trục giao cắt tại giá trị tối đa.

### Custom {#Custom}
```
public static final int Custom
```

Giá trị tùy chỉnh từ thuộc tính CrossAt