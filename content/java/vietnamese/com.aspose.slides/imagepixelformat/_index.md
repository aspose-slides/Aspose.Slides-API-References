---
title: ImagePixelFormat
second_title: Tham chiếu API Aspose.Slides cho Java
description: Xác định định dạng pixel cho các hình ảnh được tạo.
type: docs
url: /vi/com.aspose.slides/imagepixelformat/
---
**Kế thừa:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class ImagePixelFormat extends System.Enum
```

Xác định định dạng pixel cho các hình ảnh được tạo.
## Trường

| Trường | Mô tả |
| --- | --- |
| [Format1bppIndexed](#Format1bppIndexed) | 1 bit cho mỗi pixel, dạng chỉ mục. |
| [Format4bppIndexed](#Format4bppIndexed) | 4 bit cho mỗi pixel, dạng chỉ mục. |
| [Format8bppIndexed](#Format8bppIndexed) | 8 bit cho mỗi pixel, dạng chỉ mục. |
| [Format24bppRgb](#Format24bppRgb) | 24 bit cho mỗi pixel, RGB. |
| [Format32bppArgb](#Format32bppArgb) | 32 bit cho mỗi pixel, ARGB. |
### Format1bppIndexed {#Format1bppIndexed}
```
public static final int Format1bppIndexed
```


1 bit cho mỗi pixel, dạng chỉ mục.

### Format4bppIndexed {#Format4bppIndexed}
```
public static final int Format4bppIndexed
```


4 bit cho mỗi pixel, dạng chỉ mục.

### Format8bppIndexed {#Format8bppIndexed}
```
public static final int Format8bppIndexed
```


8 bit cho mỗi pixel, dạng chỉ mục.

### Format24bppRgb {#Format24bppRgb}
```
public static final int Format24bppRgb
```


24 bit cho mỗi pixel, RGB.

### Format32bppArgb {#Format32bppArgb}
```
public static final int Format32bppArgb
```


32 bit cho mỗi pixel, ARGB.