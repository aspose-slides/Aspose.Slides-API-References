---
title: CommentsPositions
second_title: Tham chiếu API Aspose.Slides cho Java
description: Đại diện cho quy tắc hiển thị bình luận trong tài liệu xuất khẩu
type: docs
url: /vi/com.aspose.slides/commentspositions/
---
**Kế thừa:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class CommentsPositions extends System.Enum
```

Đại diện cho quy tắc hiển thị bình luận trong tài liệu xuất khẩu
## Trường

| Trường | Mô tả |
| --- | --- |
| [None](#None) | Chỉ định rằng bình luận sẽ không được hiển thị hoàn toàn. |
| [Bottom](#Bottom) | Chỉ định rằng bình luận sẽ được hiển thị ở cuối trang. |
| [Right](#Right) | Chỉ định rằng bình luận sẽ được hiển thị ở bên phải trang. |
### None {#None}
```
public static final int None
```

Chỉ định rằng bình luận sẽ không được hiển thị hoàn toàn.

### Bottom {#Bottom}
```
public static final int Bottom
```

Chỉ định rằng bình luận sẽ được hiển thị ở cuối trang.

### Right {#Right}
```
public static final int Right
```

Chỉ định rằng bình luận sẽ được hiển thị ở bên phải trang.