---
title: NormalViewRestoredProperties
second_title: Tham chiếu API Aspose.Slides cho Java
description: Xác định kích thước của vùng slide (width khi là con của restoredTop, height khi là con của restoredLeft) của chế độ xem bình thường, khi vùng có kích thước khôi phục biến đổi (không thu nhỏ cũng không phóng to).
type: docs
url: /vi/com.aspose.slides/normalviewrestoredproperties/
---
**Kế thừa:**
java.lang.Object

**Tất cả các giao diện được triển khai:**
[com.aspose.slides.INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)
```
public class NormalViewRestoredProperties implements INormalViewRestoredProperties
```

Xác định kích thước của vùng slide ((width khi là con của restoredTop, height khi là con của restoredLeft) của chế độ xem bình thường, khi vùng có kích thước khôi phục biến đổi (không thu nhỏ cũng không phóng to)).

## Phương thức

| Phương thức | Mô tả |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | Xác định kích thước của vùng slide (width khi là con của RestoredTop, height khi là con của RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | Xác định kích thước của vùng slide (width khi là con của RestoredTop, height khi là con của RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | Xác định xem kích thước của vùng nội dung bên có nên bù cho kích thước mới khi thay đổi kích thước cửa sổ chứa chế độ xem trong ứng dụng hay không Read/write boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | Xác định xem kích thước của vùng nội dung bên có nên bù cho kích thước mới khi thay đổi kích thước cửa sổ chứa chế độ xem trong ứng dụng hay không Read/write boolean. |

### getDimensionSize() {#getDimensionSize--}
```
public final float getDimensionSize()
```

Xác định kích thước của vùng slide (width khi là con của RestoredTop, height khi là con của RestoredLeft). Read/write float.

**Trả về:**
float

### setDimensionSize(float value) {#setDimensionSize-float-}
```
public final void setDimensionSize(float value)
```

Xác định kích thước của vùng slide (width khi là con của RestoredTop, height khi là con của RestoredLeft). Read/write float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public final boolean getAutoAdjust()
```

Xác định xem kích thước của vùng nội dung bên có nên bù cho kích thước mới khi thay đổi kích thước cửa sổ chứa chế độ xem trong ứng dụng hay không Read/write boolean.

**Trả về:**
boolean

### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public final void setAutoAdjust(boolean value)
```

Xác định xem kích thước của vùng nội dung bên có nên bù cho kích thước mới khi thay đổi kích thước cửa sổ chứa chế độ xem trong ứng dụng hay không Read/write boolean.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | boolean |  |