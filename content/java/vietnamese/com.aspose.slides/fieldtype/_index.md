---
title: FieldType
second_title: Tham khảo API Aspose.Slides cho Java
description: Biểu diễn một loại trường.
type: docs
url: /vi/com.aspose.slides/fieldtype/
---
**Kế thừa:**
java.lang.Object

**Tất cả các giao diện đã thực hiện:**
[com.aspose.slides.IFieldType](../../com.aspose.slides/ifieldtype)
```
public final class FieldType implements IFieldType
```

Biểu diễn một loại trường. Giá trị này xác định văn bản nào sẽ được đặt vào phần trường khi nó được cập nhật.

## Hàm tạo

| Hàm tạo | Mô tả |
| --- | --- |
| [FieldType(String str)](#FieldType-java.lang.String-) | Khởi tạo một thể hiện mới của lớp FieldType. |

## Phương thức

| Phương thức | Mô tả |
| --- | --- |
| [getInternalString()](#getInternalString--) | Trả về tên nội bộ của đối tượng FieldType này. |
| [setInternalString(String value)](#setInternalString-java.lang.String-) | Trả về tên nội bộ của đối tượng FieldType này. |
| [equals(Object obj)](#equals-java.lang.Object-) | Kiểm tra xem trường này có bằng với trường khác hay không. |
| [hashCode()](#hashCode--) | Trả về mã băm cho đối tượng này. |
| [op_Equality(FieldType a, FieldType b)](#op-Equality-com.aspose.slides.FieldType-com.aspose.slides.FieldType-) | Kiểm tra xem hai đối tượng FieldType có bằng nhau hay không. |
| [op_Inequality(FieldType a, FieldType b)](#op-Inequality-com.aspose.slides.FieldType-com.aspose.slides.FieldType-) | Kiểm tra xem hai đối tượng FieldType có không bằng nhau hay không. |
| [getSlideNumber()](#getSlideNumber--) | Số slide hiện tại. |
| [getFooter()](#getFooter--) | Chân trang của slide. |
| [getHeader()](#getHeader--) | Đầu trang của slide. |
| [getDateTime()](#getDateTime--) | Ngày và giờ hiện tại ở định dạng ngày giờ mặc định cho ứng dụng render. |
| [getDateTime1()](#getDateTime1--) | Ngày và giờ hiện tại ở định dạng đã định sẵn thứ nhất (MM/DD/YYYY cho tiếng Anh). |
| [getDateTime2()](#getDateTime2--) | Ngày và giờ hiện tại ở định dạng đã định sẵn thứ hai (Day, Month DD, YYYY cho tiếng Anh). |
| [getDateTime3()](#getDateTime3--) | Ngày và giờ hiện tại ở định dạng đã định sẵn thứ ba (DD Month YYYY cho tiếng Anh). |
| [getDateTime4()](#getDateTime4--) | Ngày và giờ hiện tại ở định dạng đã định sẵn thứ tư (Month DD, YYYY cho tiếng Anh). |
| [getDateTime5()](#getDateTime5--) | Ngày và giờ hiện tại ở định dạng đã định sẵn thứ năm (DD-Mon-YY cho tiếng Anh). |
| [getDateTime6()](#getDateTime6--) | Ngày và giờ hiện tại ở định dạng đã định sẵn thứ sáu (Month YY cho tiếng Anh). |
| [getDateTime7()](#getDateTime7--) | Ngày và giờ hiện tại ở định dạng đã định sẵn thứ bảy (Mon-YY cho tiếng Anh). |
| [getDateTime8()](#getDateTime8--) | Ngày và giờ hiện tại ở định dạng đã định sẵn thứ tám (MM/DD/YYYY hh:mm AM/PM cho tiếng Anh). |
| [getDateTime9()](#getDateTime9--) | Ngày và giờ hiện tại ở định dạng đã định sẵn thứ chín (MM/DD/YYYY hh:mm:ss AM/PM cho tiếng Anh). |
| [getDateTime10()](#getDateTime10--) | Ngày và giờ hiện tại ở định dạng đã định sẵn thứ mười (hh:mm cho tiếng Anh). |
| [getDateTime11()](#getDateTime11--) | Ngày và giờ hiện tại ở định dạng đã định sẵn thứ mười một (hh:mm:ss cho tiếng Anh). |
| [getDateTime12()](#getDateTime12--) | Ngày và giờ hiện tại ở định dạng đã định sẵn thứ mười hai (hh:mm AM/PM cho tiếng Anh). |
| [getDateTime13()](#getDateTime13--) | Ngày và giờ hiện tại ở định dạng đã định sẵn thứ mười ba (hh:mm:ss AM/PM cho tiếng Anh). |

### FieldType(String str) {#FieldType-java.lang.String-}
```
public FieldType(String str)
```

Khởi tạo một thể hiện mới của lớp FieldType.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| str | java.lang.String |  |

### getInternalString() {#getInternalString--}
```
public final String getInternalString()
```

Trả về tên nội bộ của đối tượng FieldType này. Đọc/ghi String.

**Trả về:**
java.lang.String

### setInternalString(String value) {#setInternalString-java.lang.String-}
```
public final void setInternalString(String value)
```

Trả về tên nội bộ của đối tượng FieldType này. Đọc/ghi String.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | java.lang.String |  |

### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```

Kiểm tra xem trường này có bằng với trường khác hay không.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| obj | java.lang.Object | Trường để so sánh. |

**Trả về:**
boolean - Đúng nếu các trường bằng nhau.

### hashCode() {#hashCode--}
```
public int hashCode()
```

Trả về mã băm cho đối tượng này.

**Trả về:**
int - Mã băm int.

### op_Equality(FieldType a, FieldType b) {#op-Equality-com.aspose.slides.FieldType-com.aspose.slides.FieldType-}
```
public static boolean op_Equality(FieldType a, FieldType b)
```

Kiểm tra xem hai đối tượng FieldType có bằng nhau hay không.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| a | [FieldType](../../com.aspose.slides/fieldtype) | FieldType đầu tiên để so sánh. |
| b | [FieldType](../../com.aspose.slides/fieldtype) | FieldType thứ hai để so sánh. |

**Trả về:**
boolean - Đúng nếu các đối tượng FieldType bằng nhau.

### op_Inequality(FieldType a, FieldType b) {#op-Inequality-com.aspose.slides.FieldType-com.aspose.slides.FieldType-}
```
public static boolean op_Inequality(FieldType a, FieldType b)
```

Kiểm tra xem hai đối tượng FieldType không bằng nhau hay không.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| a | [FieldType](../../com.aspose.slides/fieldtype) | FieldType đầu tiên để so sánh. |
| b | [FieldType](../../com.aspose.slides/fieldtype) | FieldType thứ hai để so sánh. |

**Trả về:**
boolean - Đúng nếu các đối tượng FieldType không bằng nhau.

### getSlideNumber() {#getSlideNumber--}
```
public static FieldType getSlideNumber()
```

Số slide hiện tại. Chỉ đọc [FieldType](../../com.aspose.slides/fieldtype).

**Trả về:**
[FieldType](../../com.aspose.slides/fieldtype)

### getFooter() {#getFooter--}
```
public static FieldType getFooter()
```

Chân trang của slide. Chỉ đọc [FieldType](../../com.aspose.slides/fieldtype).

**Trả về:**
[FieldType](../../com.aspose.slides/fieldtype)

### getHeader() {#getHeader--}
```
public static FieldType getHeader()
```

Đầu trang của slide. Chỉ đọc [FieldType](../../com.aspose.slides/fieldtype).

**Trả về:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime() {#getDateTime--}
```
public static FieldType getDateTime()
```

Ngày và giờ hiện tại ở định dạng ngày giờ mặc định cho ứng dụng render. Chỉ đọc [FieldType](../../com.aspose.slides/fieldtype).

**Trả về:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime1() {#getDateTime1--}
```
public static FieldName getDateTime1()
```

Ngày và giờ hiện tại ở định dạng đã định sẵn thứ nhất (MM/DD/YYYY cho tiếng Anh). Chỉ đọc [FieldType](../../com.aspose.slides/fieldtype).

**Trả về:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime2() {#getDateTime2--}
```
public static FieldType getDateTime2()
```

Ngày và giờ hiện tại ở định dạng đã định sẵn thứ hai (Day, Month DD, YYYY cho tiếng Anh). Chỉ đọc [FieldType](../../com.aspose.slides/fieldtype).

**Trả về:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime3() {#getDateTime3--}
```
public static FieldType getDateTime3()
```

Ngày và giờ hiện tại ở định dạng đã định sẵn thứ ba (DD Month YYYY cho tiếng Anh). Chỉ đọc [FieldType](../../com.aspose.slides/fieldtype).

**Trả về:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime4() {#getDateTime4--}
```
public static FieldType getDateTime4()
```

Ngày và giờ hiện tại ở định dạng đã định sẵn thứ tư (Month DD, YYYY cho tiếng Anh). Chỉ đọc [FieldType](../../com.aspose.slides/fieldtype).

**Trả về:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime5() {#getDateTime5--}
```
public static FieldType getDateTime5()
```

Ngày và giờ hiện tại ở định dạng đã định sẵn thứ năm (DD-Mon-YY cho tiếng Anh). Chỉ đọc [FieldType](../../com.aspose.slides/fieldtype).

**Trả về:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime6() {#getDateTime6--}
```
public static FieldType getDateTime6()
```

Ngày và giờ hiện tại ở định dạng đã định sẵn thứ sáu (Month YY cho tiếng Anh). Chỉ đọc [FieldType](../../com.aspose.slides/fieldtype).

**Trả về:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime7() {#getDateTime7--}
```
public static FieldType getDateTime7()
```

Ngày và giờ hiện tại ở định dạng đã định sẵn thứ bảy (Mon-YY cho tiếng Anh). Chỉ đọc [FieldType](../../com.aspose.slides/fieldtype).

**Trả về:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime8() {#getDateTime8--}
```
public static FieldType getDateTime8()
```

Ngày và giờ hiện tại ở định dạng đã định sẵn thứ tám (MM/DD/YYYY hh:mm AM/PM cho tiếng Anh). Chỉ đọc [FieldType](../../com.aspose.slides/fieldtype).

**Trả về:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime9() {#getDateTime9--}
```
public static FieldType getDateTime9()
```

Ngày và giờ hiện tại ở định dạng đã định sẵn thứ chín (MM/DD/YYYY hh:mm:ss AM/PM cho tiếng Anh). Chỉ đọc [FieldType](../../com.aspose.slides/fieldtype).

**Trả về:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime10() {#getDateTime10--}
```
public static FieldType getDateTime10()
```

Ngày và giờ hiện tại ở định dạng đã định sẵn thứ mười (hh:mm cho tiếng Anh). Chỉ đọc [FieldType](../../com.aspose.slides/fieldtype).

**Trả về:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime11() {#getDateTime11--}
```
public static FieldType getDateTime11()
```

Ngày và giờ hiện tại ở định dạng đã định sẵn thứ mười một (hh:mm:ss cho tiếng Anh). Chỉ đọc [FieldType](../../com.aspose.slides/fieldtype).

**Trả về:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime12() {#getDateTime12--}
```
public static FieldType getDateTime12()
```

Ngày và giờ hiện tại ở định dạng đã định sẵn thứ mười hai (hh:mm AM/PM cho tiếng Anh). Chỉ đọc [FieldType](../../com.aspose.slides/fieldtype).

**Trả về:**
[FieldType](../../com.aspose.slides/fieldtype)

### getDateTime13() {#getDateTime13--}
```
public static FieldType getDateTime13()
```

Ngày và giờ hiện tại ở định dạng đã định sẵn thứ mười ba (hh:mm:ss AM/PM cho tiếng Anh). Chỉ đọc [FieldType](../../com.aspose.slides/fieldtype).

**Trả về:**
[FieldType](../../com.aspose.slides/fieldtype)