---
title: LegendDataLabelPosition
second_title: Tham chiếu API Aspose.Slides cho Java
description: Xác định vị trí của các nhãn dữ liệu.
type: docs
url: /vi/com.aspose.slides/legenddatalabelposition/
---
**Kế thừa:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class LegendDataLabelPosition extends System.Enum
```

Xác định vị trí của các nhãn dữ liệu.
## Trường

| Trường | Mô tả |
| --- | --- |
| [NotDefined](#NotDefined) | Vị trí chưa được xác định. |
| [Bottom](#Bottom) | Chỉ định rằng các nhãn dữ liệu sẽ được hiển thị bên dưới dấu dữ liệu. |
| [BestFit](#BestFit) | Chỉ định rằng các nhãn dữ liệu sẽ được hiển thị ở vị trí tốt nhất. |
| [Center](#Center) | Chỉ định rằng các nhãn dữ liệu sẽ được hiển thị căn giữa dấu dữ liệu. |
| [InsideBase](#InsideBase) | Chỉ định rằng các nhãn dữ liệu sẽ được hiển thị bên trong phần gốc của dấu dữ liệu. |
| [InsideEnd](#InsideEnd) | Chỉ định rằng các nhãn dữ liệu sẽ được hiển thị bên trong phần cuối của dấu dữ liệu. |
| [Left](#Left) | Chỉ định rằng các nhãn dữ liệu sẽ được hiển thị phía trái dấu dữ liệu. |
| [OutsideEnd](#OutsideEnd) | Chỉ định rằng các nhãn dữ liệu sẽ được hiển thị bên ngoài phần cuối của dấu dữ liệu. |
| [Right](#Right) | Chỉ định rằng các nhãn dữ liệu sẽ được hiển thị phía phải dấu dữ liệu. |
| [Top](#Top) | Chỉ định rằng các nhãn dữ liệu sẽ được hiển thị phía trên dấu dữ liệu. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```

Vị trí chưa được xác định.

### Bottom {#Bottom}
```
public static final int Bottom
```

Chỉ định rằng các nhãn dữ liệu sẽ được hiển thị bên dưới dấu dữ liệu.

### BestFit {#BestFit}
```
public static final int BestFit
```

Chỉ định rằng các nhãn dữ liệu sẽ được hiển thị ở vị trí tốt nhất.

### Center {#Center}
```
public static final int Center
```

Chỉ định rằng các nhãn dữ liệu sẽ được hiển thị căn giữa dấu dữ liệu.

### InsideBase {#InsideBase}
```
public static final int InsideBase
```

Chỉ định rằng các nhãn dữ liệu sẽ được hiển thị bên trong phần gốc của dấu dữ liệu.

### InsideEnd {#InsideEnd}
```
public static final int InsideEnd
```

Chỉ định rằng các nhãn dữ liệu sẽ được hiển thị bên trong phần cuối của dấu dữ liệu.

### Left {#Left}
```
public static final int Left
```

Chỉ định rằng các nhãn dữ liệu sẽ được hiển thị phía trái dấu dữ liệu.

### OutsideEnd {#OutsideEnd}
```
public static final int OutsideEnd
```

Chỉ định rằng các nhãn dữ liệu sẽ được hiển thị bên ngoài phần cuối của dấu dữ liệu.

### Right {#Right}
```
public static final int Right
```

Chỉ định rằng các nhãn dữ liệu sẽ được hiển thị phía phải dấu dữ liệu.

### Top {#Top}
```
public static final int Top
```

Chỉ định rằng các nhãn dữ liệu sẽ được hiển thị phía trên dấu dữ liệu.