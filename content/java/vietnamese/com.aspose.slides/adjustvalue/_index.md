---
title: AdjustValue
second_title: Tham chiếu API Aspose.Slides cho Java
description: Biểu diễn giá trị điều chỉnh của các hình dạng hình học.
type: docs
url: /vi/com.aspose.slides/adjustvalue/
---
**Inheritance:**
java.lang.Object

**All Implemented Interfaces:**
[com.aspose.slides.IAdjustValue](../../com.aspose.slides/iadjustvalue)
```
public class AdjustValue implements IAdjustValue
```

Biểu diễn giá trị điều chỉnh của một hình dạng hình học. Các giá trị này ảnh hưởng đến hình dạng của shape.
## Phương thức

| Method | Description |
| --- | --- |
| [getRawValue()](#getRawValue--) | Trả về hoặc đặt giá trị điều chỉnh "as is". |
| [setRawValue(long value)](#setRawValue-long-) | Trả về hoặc đặt giá trị điều chỉnh "as is". |
| [getAngleValue()](#getAngleValue--) | Trả về hoặc đặt giá trị, diễn giải nó như góc tính bằng độ. |
| [setAngleValue(float value)](#setAngleValue-float-) | Trả về hoặc đặt giá trị, diễn giải nó như góc tính bằng độ. |
| [getName()](#getName--) | Trả về tên của giá trị điều chỉnh này. |
| [getType()](#getType--) | Trả về kiểu của điều chỉnh hình dạng. |
### getRawValue() {#getRawValue--}
```
public final long getRawValue()
```


Trả về hoặc đặt giá trị điều chỉnh "as is". Đọc/ghi long.

**Giá trị trả về:**
long
### setRawValue(long value) {#setRawValue-long-}
```
public final void setRawValue(long value)
```


Trả về hoặc đặt giá trị điều chỉnh "as is". Đọc/ghi long.

**Tham số:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | long |  |

### getAngleValue() {#getAngleValue--}
```
public final float getAngleValue()
```


Trả về hoặc đặt giá trị, diễn giải nó như góc tính bằng độ. Đọc/ghi float.

**Giá trị trả về:**
float
### setAngleValue(float value) {#setAngleValue-float-}
```
public final void setAngleValue(float value)
```


Trả về hoặc đặt giá trị, diễn giải nó như góc tính bằng độ. Đọc/ghi float.

**Tham số:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getName() {#getName--}
```
public final String getName()
```


Trả về tên của giá trị điều chỉnh này. Chỉ đọc String.

**Giá trị trả về:**
java.lang.String
### getType() {#getType--}
```
public final int getType()
```


Trả về kiểu của điều chỉnh hình dạng. Chỉ đọc [ShapeAdjustmentType](../../com.aspose.slides/shapeadjustmenttype).

**Giá trị trả về:**
int