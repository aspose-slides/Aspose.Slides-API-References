---
title: LegendPositionType
second_title: Tham chiếu API Java của Aspose.Slides
description: Xác định vị trí của chú giải trên biểu đồ.
type: docs
url: /vi/com.aspose.slides/legendpositiontype/
---
**Kế thừa:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class LegendPositionType extends System.Enum
```

Xác định vị trí của chú giải trên biểu đồ.
## Trường

| Trường | Mô tả |
| --- | --- |
| [Bottom](#Bottom) | Chỉ định rằng chú giải sẽ được vẽ ở phía dưới của biểu đồ. |
| [Left](#Left) | Chỉ định rằng chú giải sẽ được vẽ ở phía trái của biểu đồ. |
| [Right](#Right) | Chỉ định rằng chú giải sẽ được vẽ ở phía phải của biểu đồ. |
| [Top](#Top) | Chỉ định rằng chú giải sẽ được vẽ ở phía trên của biểu đồ. |
| [TopRight](#TopRight) | Chỉ định rằng chú giải sẽ được vẽ ở phía trên bên phải của biểu đồ. |
### Dưới {#Bottom}
```
public static final int Bottom
```

Chỉ định rằng chú giải sẽ được vẽ ở phía dưới của biểu đồ.

### Trái {#Left}
```
public static final int Left
```

Chỉ định rằng chú giải sẽ được vẽ ở phía trái của biểu đồ.

### Phải {#Right}
```
public static final int Right
```

Chỉ định rằng chú giải sẽ được vẽ ở phía phải của biểu đồ.

### Trên {#Top}
```
public static final int Top
```

Chỉ định rằng chú giải sẽ được vẽ ở phía trên của biểu đồ.

### TrênBênPhải {#TopRight}
```
public static final int TopRight
```

Chỉ định rằng chú giải sẽ được vẽ ở phía trên bên phải của biểu đồ.