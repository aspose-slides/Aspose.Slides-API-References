---
title: IOuterShadowEffectiveData
second_title: Aspose.Slides cho Tham chiếu API Java
description: Đối tượng bất biến đại diện cho hiệu ứng Outer Shadow.
type: docs
url: /vi/com.aspose.slides/ioutershadoweffectivedata/
---
**Tất cả các giao diện đã triển khai:**
[com.aspose.slides.IEffectEffectiveData](../../com.aspose.slides/ieffecteffectivedata)
```
public interface IOuterShadowEffectiveData extends IEffectEffectiveData
```

Đối tượng bất biến đại diện cho hiệu ứng Outer Shadow.

## Phương thức

| Method | Description |
| --- | --- |
| [getBlurRadius()](#getBlurRadius--) | Bán kính nhòe. |
| [getDirection()](#getDirection--) | Hướng của bóng. |
| [getDistance()](#getDistance--) | Khoảng cách của bóng. |
| [getShadowColor()](#getShadowColor--) | Màu của bóng. |
| [getRectangleAlign()](#getRectangleAlign--) | Căn hình chữ nhật. |
| [getSkewHorizontal()](#getSkewHorizontal--) | Xác định góc nghiêng ngang (độ). |
| [getSkewVertical()](#getSkewVertical--) | Xác định góc nghiêng dọc (độ). |
| [getRotateShadowWithShape()](#getRotateShadowWithShape--) | Xác định liệu bóng có quay cùng hình dạng khi hình dạng bị quay hay không. |
| [getScaleHorizontal()](#getScaleHorizontal--) | Xác định hệ số phóng đại ngang, giá trị âm gây lật. |
| [getScaleVertical()](#getScaleVertical--) | Xác định hệ số phóng đại dọc, giá trị âm gây lật. |

### getBlurRadius() {#getBlurRadius--}
```
public abstract double getBlurRadius()
```

Bán kính nhòe. double chỉ đọc.

**Trả về:**
double

### getDirection() {#getDirection--}
```
public abstract float getDirection()
```

Hướng của bóng. float chỉ đọc.

**Trả về:**
float

### getDistance() {#getDistance--}
```
public abstract double getDistance()
```

Khoảng cách của bóng. double chỉ đọc.

**Trả về:**
double

### getShadowColor() {#getShadowColor--}
```
public abstract Color getShadowColor()
```

Màu của bóng. java.awt.Color chỉ đọc.

**Trả về:**
java.awt.Color

### getRectangleAlign() {#getRectangleAlign--}
```
public abstract byte getRectangleAlign()
```

Căn hình chữ nhật. [RectangleAlignment](../../com.aspose.slides/rectanglealignment) chỉ đọc.

**Trả về:**
byte

### getSkewHorizontal() {#getSkewHorizontal--}
```
public abstract double getSkewHorizontal()
```

Xác định góc nghiêng ngang (độ). double chỉ đọc.

**Trả về:**
double

### getSkewVertical() {#getSkewVertical--}
```
public abstract double getSkewVertical()
```

Xác định góc nghiêng dọc (độ). double chỉ đọc.

**Trả về:**
double

### getRotateShadowWithShape() {#getRotateShadowWithShape--}
```
public abstract boolean getRotateShadowWithShape()
```

Xác định liệu bóng có quay cùng hình dạng khi hình dạng bị quay hay không. boolean chỉ đọc.

**Trả về:**
boolean

### getScaleHorizontal() {#getScaleHorizontal--}
```
public abstract double getScaleHorizontal()
```

Xác định hệ số phóng đại ngang, giá trị âm gây lật. double chỉ đọc.

**Trả về:**
double

### getScaleVertical() {#getScaleVertical--}
```
public abstract double getScaleVertical()
```

Xác định hệ số phóng đại dọc, giá trị âm gây lật. double chỉ đọc.

**Trả về:**
double