---
title: ScaleEffect
second_title: Tham chiếu API Aspose.Slides cho Java
description: Đại diện cho hiệu ứng thu phóng hoạt hình.
type: docs
url: /vi/com.aspose.slides/scaleeffect/
---
**Inheritance:**  
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**All Implemented Interfaces:**  
[com.aspose.slides.IScaleEffect](../../com.aspose.slides/iscaleeffect)  
```
public class ScaleEffect extends Behavior implements IScaleEffect
```

Biểu diễn hiệu ứng thu phóng hoạt hình.

## Constructors

| Constructor | Mô tả |
| --- | --- |
| [ScaleEffect()](#ScaleEffect--) |  |

## Methods

| Phương thức | Mô tả |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Xác định liệu nội dung có nên được phóng to hay không. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Xác định liệu nội dung có nên được phóng to hay không. |
| [getFrom()](#getFrom--) | Xác định tọa độ x/y để bắt đầu hoạt hình (theo phần trăm). |
| [setFrom(Point2D.Float value)](#setFrom-java.awt.geom.Point2D.Float-) | Xác định tọa độ x/y để bắt đầu hoạt hình (theo phần trăm). |
| [getTo()](#getTo--) | Xác định vị trí mục tiêu cho hiệu ứng thu phóng hoạt hình (theo phần trăm). |
| [setTo(Point2D.Float value)](#setTo-java.awt.geom.Point2D.Float-) | Xác định vị trí mục tiêu cho hiệu ứng thu phóng hoạt hình (theo phần trăm). |
| [getBy()](#getBy--) | Mô tả giá trị offset tương đối cho hoạt hình (theo phần trăm). |
| [setBy(Point2D.Float value)](#setBy-java.awt.geom.Point2D.Float-) | Mô tả giá trị offset tương đối cho hoạt hình (theo phần trăm). |

### ScaleEffect() {#ScaleEffect--}
```
public ScaleEffect()
```

### getZoomContent() {#getZoomContent--}
```
public final byte getZoomContent()
```

Xác định liệu nội dung có nên được phóng to hay không. Đọc/ghi [NullableBool](../../com.aspose.slides/nullablebool).

**Giá trị trả về:**  
byte

### setZoomContent(byte value) {#setZoomContent-byte-}
```
public final void setZoomContent(byte value)
```

Xác định liệu nội dung có nên được phóng to hay không. Đọc/ghi [NullableBool](../../com.aspose.slides/nullablebool).

**Tham số:**  
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public final Point2D.Float getFrom()
```

Xác định tọa độ x/y để bắt đầu hoạt hình (theo phần trăm). Đọc/ghi java.awt.geom.Point2D.Float.

**Giá trị trả về:**  
java.awt.geom.Point2D.Float

### setFrom(Point2D.Float value) {#setFrom-java.awt.geom.Point2D.Float-}
```
public final void setFrom(Point2D.Float value)
```

Xác định tọa độ x/y để bắt đầu hoạt hình (theo phần trăm). Đọc/ghi java.awt.geom.Point2D.Float.

**Tham số:**  
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getTo() {#getTo--}
```
public final Point2D.Float getTo()
```

Xác định vị trí mục tiêu cho hiệu ứng thu phóng hoạt hình (theo phần trăm). Đọc/ghi java.awt.geom.Point2D.Float.

**Giá trị trả về:**  
java.awt.geom.Point2D.Float

### setTo(Point2D.Float value) {#setTo-java.awt.geom.Point2D.Float-}
```
public final void setTo(Point2D.Float value)
```

Xác định vị trí mục tiêu cho hiệu ứng thu phóng hoạt hình (theo phần trăm). Đọc/ghi java.awt.geom.Point2D.Float.

**Tham số:**  
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getBy() {#getBy--}
```
public final Point2D.Float getBy()
```

Mô tả giá trị offset tương đối cho hoạt hình (theo phần trăm). Đọc/ghi java.awt.geom.Point2D.Float.

**Giá trị trả về:**  
java.awt.geom.Point2D.Float

### setBy(Point2D.Float value) {#setBy-java.awt.geom.Point2D.Float-}
```
public final void setBy(Point2D.Float value)
```

Mô tả giá trị offset tương đối cho hoạt hình (theo phần trăm). Đọc/ghi java.awt.geom.Point2D.Float.

**Tham số:**  
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |