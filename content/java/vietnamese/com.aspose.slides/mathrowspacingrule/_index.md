---
title: MathRowSpacingRule
second_title: Tham chiếu API Aspose.Slides cho Java
description: Kiểu khoảng cách dọc giữa các cột trong ma trận hoặc mảng
type: docs
url: /vi/com.aspose.slides/mathrowspacingrule/
---
**Kế thừa:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class MathRowSpacingRule extends System.Enum
```

Kiểu khoảng cách dọc giữa các cột trong ma trận hoặc mảng
## Trường

| Trường | Mô tả |
| --- | --- |
| [SingleLineGap](#SingleLineGap) | khoảng cách một dòng |
| [OneAndAHalfLineGap](#OneAndAHalfLineGap) | khoảng cách một dòng rưỡi |
| [TwoLineGap](#TwoLineGap) | khoảng cách hai dòng |
| [Exactly](#Exactly) | chính xác (dựa vào giá trị của thuộc tính RowSpacing, đo bằng điểm) |
| [Multiple](#Multiple) | nhiều (dựa vào giá trị của thuộc tính RowSpacing, đo bằng dòng) |
### SingleLineGap {#SingleLineGap}
```
public static final int SingleLineGap
```


khoảng cách một dòng

### OneAndAHalfLineGap {#OneAndAHalfLineGap}
```
public static final int OneAndAHalfLineGap
```


Khoảng cách một dòng rưỡi

### TwoLineGap {#TwoLineGap}
```
public static final int TwoLineGap
```


Khoảng cách hai dòng

### Exactly {#Exactly}
```
public static final int Exactly
```


Chính xác (dựa vào giá trị của thuộc tính RowSpacing, đo bằng điểm)

### Multiple {#Multiple}
```
public static final int Multiple
```


Nhiều (dựa vào giá trị của thuộc tính RowSpacing, đo bằng dòng)