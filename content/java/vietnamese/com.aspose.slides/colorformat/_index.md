---
title: ColorFormat
second_title: Tham chiếu API Aspose.Slides cho Java
description: Đại diện cho một màu được sử dụng trong bản trình chiếu.
type: docs
url: /vi/com.aspose.slides/colorformat/
---
**Kế thừa:**
java.lang.Object, [com.aspose.slides.PVIObject](../../com.aspose.slides/pviobject)

**Tất cả các giao diện được triển khai:**
[com.aspose.slides.IColorFormat](../../com.aspose.slides/icolorformat)
```
public final class ColorFormat extends PVIObject implements IColorFormat
```

Đại diện cho một màu được sử dụng trong bản trình chiếu.
## Phương thức

| Phương thức | Mô tả |
| --- | --- |
| [getColorType()](#getColorType--) | Trả về hoặc đặt phương thức định nghĩa màu. |
| [setColorType(int value)](#setColorType-int-) | Trả về hoặc đặt phương thức định nghĩa màu. |
| [getColor()](#getColor--) | Trả về màu kết quả (với tất cả các biến đổi màu đã áp dụng). |
| [setColor(Color value)](#setColor-java.awt.Color-) | Trả về màu kết quả (với tất cả các biến đổi màu đã áp dụng). |
| [getPresetColor()](#getPresetColor--) | Trả về hoặc đặt cài đặt màu sẵn. |
| [setPresetColor(int value)](#setPresetColor-int-) | Trả về hoặc đặt cài đặt màu sẵn. |
| [getSystemColor()](#getSystemColor--) | Trả về hoặc đặt màu được xác định bởi bảng màu hệ thống. |
| [setSystemColor(int value)](#setSystemColor-int-) | Trả về hoặc đặt màu được xác định bởi bảng màu hệ thống. |
| [getSchemeColor()](#getSchemeColor--) | Trả về hoặc đặt màu được xác định bởi một bảng màu. |
| [setSchemeColor(int value)](#setSchemeColor-int-) | Trả về hoặc đặt màu được xác định bởi một bảng màu. |
| [getR()](#getR--) | Trả về hoặc đặt thành phần màu đỏ. |
| [setR(byte value)](#setR-byte-) | Trả về hoặc đặt thành phần màu đỏ. |
| [getG()](#getG--) | Trả về hoặc đặt thành phần màu xanh lá. |
| [setG(byte value)](#setG-byte-) | Trả về hoặc đặt thành phần màu xanh lá. |
| [getB()](#getB--) | Trả về hoặc đặt thành phần màu xanh dương. |
| [setB(byte value)](#setB-byte-) | Trả về hoặc đặt thành phần màu xanh dương. |
| [getFloatR()](#getFloatR--) | Trả về hoặc đặt thành phần màu đỏ. |
| [setFloatR(float value)](#setFloatR-float-) | Trả về hoặc đặt thành phần màu đỏ. |
| [getFloatG()](#getFloatG--) | Trả về hoặc đặt thành phần màu xanh lá. |
| [setFloatG(float value)](#setFloatG-float-) | Trả về hoặc đặt thành phần màu xanh lá. |
| [getFloatB()](#getFloatB--) | Trả về hoặc đặt thành phần màu xanh dương. |
| [setFloatB(float value)](#setFloatB-float-) | Trả về hoặc đặt thành phần màu xanh dương. |
| [getHue()](#getHue--) | Trả về hoặc đặt thành phần hue của màu trong biểu diễn HSL. |
| [setHue(float value)](#setHue-float-) | Trả về hoặc đặt thành phần hue của màu trong biểu diễn HSL. |
| [getSaturation()](#getSaturation--) | Trả về hoặc đặt thành phần bão hòa của màu trong biểu diễn HSL. |
| [setSaturation(float value)](#setSaturation-float-) | Trả về hoặc đặt thành phần bão hòa của màu trong biểu diễn HSL. |
| [getLuminance()](#getLuminance--) | Trả về hoặc đặt thành phần độ sáng của màu trong biểu diễn HSL. |
| [setLuminance(float value)](#setLuminance-float-) | Trả về hoặc đặt thành phần độ sáng của màu trong biểu diễn HSL. |
| [getColorTransform()](#getColorTransform--) | Trả về tập hợp các biến đổi màu đã áp dụng cho một màu. |
| [toString(int format)](#toString-int-) | Trả về một String biểu thị định dạng màu hiện tại. |
| [copyFrom(IColorFormat color)](#copyFrom-com.aspose.slides.IColorFormat-) | Sao chép định dạng màu từ "color". |
| [equals(Object obj)](#equals-java.lang.Object-) | Kiểm tra tính bằng nhau với đối tượng chỉ định. |
| [hashCode()](#hashCode--) | Trả về mã băm. |
| [getVersion()](#getVersion--) |  |
| [getParent_ISlideComponent()](#getParent-ISlideComponent--) |  |
| [getParent_IPresentationComponent()](#getParent-IPresentationComponent--) |  |

### getColorType() {#getColorType--}
```
public final int getColorType()
```

Trả về hoặc đặt phương thức định nghĩa màu. Đọc/ghi [ColorType](../../com.aspose.slides/colortype).

**Trả về:**
int

### setColorType(int value) {#setColorType-int-}
```
public final void setColorType(int value)
```

Trả về hoặc đặt phương thức định nghĩa màu. Đọc/ghi [ColorType](../../com.aspose.slides/colortype).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | int |  |

### getColor() {#getColor--}
```
public final Color getColor()
```

Trả về màu kết quả (với tất cả các biến đổi màu đã áp dụng). Đặt màu RGB và xóa tất cả các biến đổi màu. Đọc/ghi java.awt.Color.

**Trả về:**
java.awt.Color

### setColor(Color value) {#setColor-java.awt.Color-}
```
public final void setColor(Color value)
```

Trả về màu kết quả (với tất cả các biến đổi màu đã áp dụng). Đặt màu RGB và xóa tất cả các biến đổi màu. Đọc/ghi java.awt.Color.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | java.awt.Color |  |

### getPresetColor() {#getPresetColor--}
```
public final int getPresetColor()
```

Trả về hoặc đặt cài đặt màu sẵn. Đọc/ghi [PresetColor](../../com.aspose.slides/presetcolor).

**Trả về:**
int

### setPresetColor(int value) {#setPresetColor-int-}
```
public final void setPresetColor(int value)
```

Trả về hoặc đặt cài đặt màu sẵn. Đọc/ghi [PresetColor](../../com.aspose.slides/presetcolor).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | int |  |

### getSystemColor() {#getSystemColor--}
```
public final int getSystemColor()
```

Trả về hoặc đặt màu được xác định bởi bảng màu hệ thống. Đọc/ghi [SystemColor](../../com.aspose.slides/systemcolor).

**Trả về:**
int

### setSystemColor(int value) {#setSystemColor-int-}
```
public final void setSystemColor(int value)
```

Trả về hoặc đặt màu được xác định bởi bảng màu hệ thống. Đọc/ghi [SystemColor](../../com.aspose.slides/systemcolor).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | int |  |

### getSchemeColor() {#getSchemeColor--}
```
public final int getSchemeColor()
```

Trả về hoặc đặt màu được xác định bởi một bảng màu. Đọc/ghi [SchemeColor](../../com.aspose.slides/schemecolor).

**Trả về:**
int

### setSchemeColor(int value) {#setSchemeColor-int-}
```
public final void setSchemeColor(int value)
```

Trả về hoặc đặt màu được xác định bởi một bảng màu. Đọc/ghi [SchemeColor](../../com.aspose.slides/schemecolor).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | int |  |

### getR() {#getR--}
```
public final byte getR()
```

Trả về hoặc đặt thành phần màu đỏ. Tất cả các biến đổi màu bị bỏ qua. Đọc/ghi byte.

**Trả về:**
byte

### setR(byte value) {#setR-byte-}
```
public final void setR(byte value)
```

Trả về hoặc đặt thành phần màu đỏ. Tất cả các biến đổi màu bị bỏ qua. Đọc/ghi byte.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | byte |  |

### getG() {#getG--}
```
public final byte getG()
```

Trả về hoặc đặt thành phần màu xanh lá. Tất cả các biến đổi màu bị bỏ qua.

**Trả về:**
byte

### setG(byte value) {#setG-byte-}
```
public final void setG(byte value)
```

Trả về hoặc đặt thành phần màu xanh lá. Tất cả các biến đổi màu bị bỏ qua.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | byte |  |

### getB() {#getB--}
```
public final byte getB()
```

Trả về hoặc đặt thành phần màu xanh dương. Tất cả các biến đổi màu bị bỏ qua. Đọc/ghi byte.

**Trả về:**
byte

### setB(byte value) {#setB-byte-}
```
public final void setB(byte value)
```

Trả về hoặc đặt thành phần màu xanh dương. Tất cả các biến đổi màu bị bỏ qua. Đọc/ghi byte.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | byte |  |

### getFloatR() {#getFloatR--}
```
public final float getFloatR()
```

Trả về hoặc đặt thành phần màu đỏ. Tất cả các biến đổi màu bị bỏ qua. Đọc/ghi float.

**Trả về:**
float

### setFloatR(float value) {#setFloatR-float-}
```
public final void setFloatR(float value)
```

Trả về hoặc đặt thành phần màu đỏ. Tất cả các biến đổi màu bị bỏ qua. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getFloatG() {#getFloatG--}
```
public final float getFloatG()
```

Trả về hoặc đặt thành phần màu xanh lá. Tất cả các biến đổi màu bị bỏ qua. Đọc/ghi float.

**Trả về:**
float

### setFloatG(float value) {#setFloatG-float-}
```
public final void setFloatG(float value)
```

Trả về hoặc đặt thành phần màu xanh lá. Tất cả các biến đổi màu bị bỏ qua. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getFloatB() {#getFloatB--}
```
public final float getFloatB()
```

Trả về hoặc đặt thành phần màu xanh dương. Tất cả các biến đổi màu bị bỏ qua. Đọc/ghi float.

**Trả về:**
float

### setFloatB(float value) {#setFloatB-float-}
```
public final void setFloatB(float value)
```

Trả về hoặc đặt thành phần màu xanh dương. Tất cả các biến đổi màu bị bỏ qua. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getHue() {#getHue--}
```
public final float getHue()
```

Trả về hoặc đặt thành phần hue của màu trong biểu diễn HSL. Tất cả các biến đổi màu bị bỏ qua. Đọc/ghi float.

**Trả về:**
float

### setHue(float value) {#setHue-float-}
```
public final void setHue(float value)
```

Trả về hoặc đặt thành phần hue của màu trong biểu diễn HSL. Tất cả các biến đổi màu bị bỏ qua. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getSaturation() {#getSaturation--}
```
public final float getSaturation()
```

Trả về hoặc đặt thành phần bão hòa của màu trong biểu diễn HSL. Tất cả các biến đổi màu bị bỏ qua. Đọc/ghi float.

**Trả về:**
float

### setSaturation(float value) {#setSaturation-float-}
```
public final void setSaturation(float value)
```

Trả về hoặc đặt thành phần bão hòa của màu trong biểu diễn HSL. Tất cả các biến đổi màu bị bỏ qua. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getLuminance() {#getLuminance--}
```
public final float getLuminance()
```

Trả về hoặc đặt thành phần độ sáng của màu trong biểu diễn HSL. Tất cả các biến đổi màu bị bỏ qua. Đọc/ghi float.

**Trả về:**
float

### setLuminance(float value) {#setLuminance-float-}
```
public final void setLuminance(float value)
```

Trả về hoặc đặt thành phần độ sáng của màu trong biểu diễn HSL. Tất cả các biến đổi màu bị bỏ qua. Đọc/ghi float.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | float |  |

### getColorTransform() {#getColorTransform--}
```
public final IColorOperationCollection getColorTransform()
```

Trả về tập hợp các biến đổi màu đã áp dụng cho một màu. Chỉ đọc [IColorOperationCollection](../../com.aspose.slides/icoloroperationcollection).

**Trả về:**
[IColorOperationCollection](../../com.aspose.slides/icoloroperationcollection)

### toString(int format) {#toString-int-}
```
public final String toString(int format)
```

Trả về một String biểu thị định dạng màu hiện tại.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| format | int | Loại định dạng chuỗi màu. |

**Trả về:**
java.lang.String - Một chuỗi biểu thị định dạng màu hiện tại.

### copyFrom(IColorFormat color) {#copyFrom-com.aspose.slides.IColorFormat-}
```
public final void copyFrom(IColorFormat color)
```

Sao chép định dạng màu từ "color".

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| color | [IColorFormat](../../com.aspose.slides/icolorformat) |  |

### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```

Kiểm tra tính bằng nhau với đối tượng chỉ định.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| obj | java.lang.Object | Đối tượng. |

**Trả về:**
boolean - True nếu các đối tượng bằng nhau, ngược lại false.

### hashCode() {#hashCode--}
```
public int hashCode()
```

Trả về mã băm.

**Trả về:**
int - Mã băm.

### getVersion() {#getVersion--}
```
public long getVersion()
```

Phiên bản. Chỉ đọc long.

**Trả về:**
long

### getParent_ISlideComponent() {#getParent-ISlideComponent--}
```
public final ISlideComponent getParent_ISlideComponent()
```

**Trả về:**
[ISlideComponent](../../com.aspose.slides/islidecomponent)

### getParent_IPresentationComponent() {#getParent-IPresentationComponent--}
```
public final IPresentationComponent getParent_IPresentationComponent()
```

Trả về parent IPresentationComponent. Chỉ đọc [IPresentationComponent](../../com.aspose.slides/ipresentationcomponent).

**Trả về:**
[IPresentationComponent](../../com.aspose.slides/ipresentationcomponent)