---
title: AxisPositionType
second_title: Tham chiếu API Aspose.Slides cho Java
description: Xác định vị trí của trục.
type: docs
url: /vi/com.aspose.slides/axispositiontype/
---
**Kế thừa:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class AxisPositionType extends System.Enum
```

Xác định vị trí của trục.
## Trường

| Trường | Mô tả |
| --- | --- |
| [Bottom](#Bottom) | Chỉ định rằng trục sẽ được hiển thị ở phía dưới của vùng vẽ. |
| [Left](#Left) | Chỉ định rằng trục sẽ được hiển thị ở phía bên trái của vùng vẽ. |
| [Right](#Right) | Chỉ định rằng trục sẽ được hiển thị ở phía bên phải của vùng vẽ. |
| [Top](#Top) | Chỉ định rằng trục sẽ được hiển thị ở phía trên của vùng vẽ. |
### Dưới {#Bottom}
```
public static final int Bottom
```


Chỉ định rằng trục sẽ được hiển thị ở phía dưới của vùng vẽ.

### Trái {#Left}
```
public static final int Left
```


Chỉ định rằng trục sẽ được hiển thị ở phía bên trái của vùng vẽ.

### Phải {#Right}
```
public static final int Right
```


Chỉ định rằng trục sẽ được hiển thị ở phía bên phải của vùng vẽ.

### Trên {#Top}
```
public static final int Top
```


Chỉ định rằng trục sẽ được hiển thị ở phía trên của vùng vẽ.