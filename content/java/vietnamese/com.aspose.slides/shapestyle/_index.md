---
title: ShapeStyle
second_title: Tham chiếu API Aspose.Slides cho Java
description: Mô tả tham chiếu kiểu dáng của hình.
type: docs
url: /vi/com.aspose.slides/shapestyle/
---
**Kế thừa:**
java.lang.Object, com.aspose.slides.DomObject

**Tất cả các giao diện được triển khai:**
[com.aspose.slides.IShapeStyle](../../com.aspose.slides/ishapestyle)
```
public class ShapeStyle extends DomObject<Shape> implements IShapeStyle
```

Mô tả tham chiếu kiểu dáng của hình.

## Các phương thức

| Phương thức | Mô tả |
| --- | --- |
| [getLineColor()](#getLineColor--) | Trả về màu viền của hình. |
| [getLineStyleIndex()](#getLineStyleIndex--) | Trả về hoặc đặt chỉ số cột của đường trong ma trận kiểu dáng. |
| [setLineStyleIndex(int value)](#setLineStyleIndex-int-) | Trả về hoặc đặt chỉ số cột của đường trong ma trận kiểu dáng. |
| [getFillColor()](#getFillColor--) | Trả về màu tô của hình. |
| [getFillStyleIndex()](#getFillStyleIndex--) | Trả về hoặc đặt chỉ số cột tô của hình trong các ma trận kiểu dáng. |
| [setFillStyleIndex(short value)](#setFillStyleIndex-short-) | Trả về hoặc đặt chỉ số cột tô của hình trong các ma trận kiểu dáng. |
| [getEffectColor()](#getEffectColor--) | Trả về màu hiệu ứng của hình. |
| [getEffectStyleIndex()](#getEffectStyleIndex--) | Trả về hoặc đặt chỉ số cột hiệu ứng của hình trong ma trận kiểu dáng. |
| [setEffectStyleIndex(long value)](#setEffectStyleIndex-long-) | Trả về hoặc đặt chỉ số cột hiệu ứng của hình trong ma trận kiểu dáng. |
| [getFontColor()](#getFontColor--) | Trả về màu phông chữ của hình. |
| [getFontCollectionIndex()](#getFontCollectionIndex--) | Trả về hoặc đặt chỉ số phông chữ của hình trong một bộ sưu tập phông chữ. |
| [setFontCollectionIndex(byte value)](#setFontCollectionIndex-byte-) | Trả về hoặc đặt chỉ số phông chữ của hình trong một bộ sưu tập phông chữ. |
### getLineColor() {#getLineColor--}
```
public final IColorFormat getLineColor()
```

Trả về màu viền của hình. Chỉ đọc [IColorFormat](../../com.aspose.slides/icolorformat).

**Giá trị trả về:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getLineStyleIndex() {#getLineStyleIndex--}
```
public final int getLineStyleIndex()
```

Trả về hoặc đặt chỉ số cột của đường trong ma trận kiểu dáng. Đọc/ghi int.

**Giá trị trả về:**
int
### setLineStyleIndex(int value) {#setLineStyleIndex-int-}
```
public final void setLineStyleIndex(int value)
```

Trả về hoặc đặt chỉ số cột của đường trong ma trận kiểu dáng. Đọc/ghi int.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | int |  |
### getFillColor() {#getFillColor--}
```
public final IColorFormat getFillColor()
```

Trả về màu tô của hình. Chỉ đọc [IColorFormat](../../com.aspose.slides/icolorformat).

**Giá trị trả về:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getFillStyleIndex() {#getFillStyleIndex--}
```
public final short getFillStyleIndex()
```

Trả về hoặc đặt chỉ số cột tô của hình trong các ma trận kiểu dáng. 0 có nghĩa là không tô, giá trị dương - chỉ số trong các kiểu tô của chủ đề, giá trị âm - chỉ số trong các kiểu nền của chủ đề. Đọc/ghi short.

**Giá trị trả về:**
short
### setFillStyleIndex(short value) {#setFillStyleIndex-short-}
```
public final void setFillStyleIndex(short value)
```

Trả về hoặc đặt chỉ số cột tô của hình trong các ma trận kiểu dáng. 0 có nghĩa là không tô, giá trị dương - chỉ số trong các kiểu tô của chủ đề, giá trị âm - chỉ số trong các kiểu nền của chủ đề. Đọc/ghi short.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | short |  |
### getEffectColor() {#getEffectColor--}
```
public final IColorFormat getEffectColor()
```

Trả về màu hiệu ứng của hình. Chỉ đọc [IColorFormat](../../com.aspose.slides/icolorformat).

**Giá trị trả về:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getEffectStyleIndex() {#getEffectStyleIndex--}
```
public final long getEffectStyleIndex()
```

Trả về hoặc đặt chỉ số cột hiệu ứng của hình trong ma trận kiểu dáng. Đọc/ghi long.

**Giá trị trả về:**
long
### setEffectStyleIndex(long value) {#setEffectStyleIndex-long-}
```
public final void setEffectStyleIndex(long value)
```

Trả về hoặc đặt chỉ số cột hiệu ứng của hình trong ma trận kiểu dáng. Đọc/ghi long.

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | long |  |
### getFontColor() {#getFontColor--}
```
public final IColorFormat getFontColor()
```

Trả về màu phông chữ của hình. Chỉ đọc [IColorFormat](../../com.aspose.slides/icolorformat).

**Giá trị trả về:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getFontCollectionIndex() {#getFontCollectionIndex--}
```
public final byte getFontCollectionIndex()
```

Trả về hoặc đặt chỉ số phông chữ của hình trong một bộ sưu tập phông chữ. Đọc/ghi [FontCollectionIndex](../../com.aspose.slides/fontcollectionindex).

**Giá trị trả về:**
byte
### setFontCollectionIndex(byte value) {#setFontCollectionIndex-byte-}
```
public final void setFontCollectionIndex(byte value)
```

Trả về hoặc đặt chỉ số phông chữ của hình trong một bộ sưu tập phông chữ. Đọc/ghi [FontCollectionIndex](../../com.aspose.slides/fontcollectionindex).

**Tham số:**
| Tham số | Kiểu | Mô tả |
| --- | --- | --- |
| value | byte |  |