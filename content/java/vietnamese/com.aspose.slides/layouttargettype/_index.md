---
title: LayoutTargetType
second_title: Tham chiếu API Aspose.Slides cho Java
description: Nếu bố cục của vùng vẽ được xác định thủ công, thuộc tính này xác định nên bố trí vùng vẽ bằng phần trong (không bao gồm trục và nhãn trục) hoặc phần ngoài (bao gồm trục và nhãn trục).
type: docs
url: /vi/com.aspose.slides/layouttargettype/
---
**Kế thừa:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class LayoutTargetType extends System.Enum
```

Nếu bố cục của vùng vẽ được xác định thủ công, thuộc tính này xác định nên bố trí vùng vẽ bằng phần trong (không bao gồm trục và nhãn trục) hoặc phần ngoài (bao gồm trục và nhãn trục).

## Trường

| Field | Description |
| --- | --- |
| [Inner](#Inner) | Xác định kích thước vùng vẽ sẽ quyết định kích thước của vùng vẽ, không bao gồm các dấu tick và nhãn trục. |
| [Outer](#Outer) | Xác định kích thước vùng vẽ sẽ quyết định kích thước của vùng vẽ, các dấu tick và nhãn trục. |
### Nội bộ {#Inner}
```
public static final int Inner
```

Xác định kích thước vùng vẽ sẽ quyết định kích thước của vùng vẽ, không bao gồm các dấu tick và nhãn trục.

### Ngoài {#Outer}
```
public static final int Outer
```

Xác định kích thước vùng vẽ sẽ quyết định kích thước của vùng vẽ, các dấu tick và nhãn trục.