---
title: GradientStopEffectiveData
second_title: Referensi API Aspose.Slides untuk Java
description: Objek tidak dapat diubah yang mewakili sebuah titik gradasi.
type: docs
url: /id/com.aspose.slides/gradientstopeffectivedata/
---
**Pewarisan:**
java.lang.Object

**Semua Antarmuka yang Diimplementasikan:**
com.aspose.slides.IEffectiveData, [com.aspose.slides.IGradientStopEffectiveData](../../com.aspose.slides/igradientstopeffectivedata)
```
public class GradientStopEffectiveData implements IEffectiveData, IGradientStopEffectiveData
```

Objek tidak dapat diubah yang mewakili sebuah titik gradasi.
## Metode

| Metode | Deskripsi |
| --- | --- |
| [getPosition()](#getPosition--) | Mengembalikan posisi (0..1) dari sebuah titik gradasi. |
| [getColor()](#getColor--) | Mengembalikan warna dari sebuah titik gradasi. |
| [equals(Object obj)](#equals-java.lang.Object-) | Menentukan apakah [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) yang ditentukan sama dengan [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) saat ini. |
| [hashCode()](#hashCode--) |  |
### getPosition() {#getPosition--}
```
public final float getPosition()
```

Mengembalikan posisi (0..1) dari sebuah titik gradasi. Hanya-baca float.

**Mengembalikan:**
float
### getColor() {#getColor--}
```
public final Color getColor()
```

Mengembalikan warna dari sebuah titik gradasi. Hanya-baca java.awt.Color.

**Mengembalikan:**
java.awt.Color
### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```

Menentukan apakah [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) yang ditentukan sama dengan [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) saat ini.

**Parameter:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| obj | java.lang.Object | [GradientStopEffectiveData](../../com.aspose.slides/gradientstopeffectivedata) untuk dibandingkan. |

**Mengembalikan:**
boolean - true jika objek sama; jika tidak, false.
### hashCode() {#hashCode--}
```
public int hashCode()
```




**Mengembalikan:**
int