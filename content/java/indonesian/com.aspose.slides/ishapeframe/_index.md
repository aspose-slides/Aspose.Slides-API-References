---
title: IShapeFrame
second_title: Referensi API Aspose.Slides untuk Java
description: Mewakili properti bingkai bentuk.
type: docs
url: /id/com.aspose.slides/ishapeframe/
---
**Semua Antarmuka yang Diimplementasikan:**
com.aspose.slides.IGenericCloneable
```
public interface IShapeFrame extends IGenericCloneable<IShapeFrame>
```

Mewakili properti bingkai bentuk.
## Metode

| Metode | Deskripsi |
| --- | --- |
| [getX()](#getX--) | Mengembalikan koordinat X sudut kiri atas sebuah frame. |
| [getY()](#getY--) | Mengembalikan koordinat Y sudut kiri atas sebuah frame. |
| [getWidth()](#getWidth--) | Mengembalikan lebar sebuah frame. |
| [getHeight()](#getHeight--) | Mengembalikan tinggi sebuah frame. |
| [getRotation()](#getRotation--) | Mengembalikan jumlah derajat frame diputar di sekitar sumbu z. |
| [getCenterX()](#getCenterX--) | Mengembalikan koordinat X pusat sebuah frame. |
| [getCenterY()](#getCenterY--) | Mengembalikan koordinat Y pusat sebuah frame. |
| [getFlipH()](#getFlipH--) | Menentukan apakah sebuah frame dibalik secara horizontal. |
| [getFlipV()](#getFlipV--) | Menentukan apakah sebuah frame dibalik secara vertikal. |
| [getRectangle()](#getRectangle--) | Mengembalikan koordinat sebuah frame. |
### getX() {#getX--}
```
public abstract float getX()
```

Mengembalikan koordinat X sudut kiri atas sebuah frame. Float hanya-baca.

**Mengembalikan:**
float
### getY() {#getY--}
```
public abstract float getY()
```

Mengembalikan koordinat Y sudut kiri atas sebuah frame. Float hanya-baca.

**Mengembalikan:**
float
### getWidth() {#getWidth--}
```
public abstract float getWidth()
```

Mengembalikan lebar sebuah frame. Float hanya-baca.

**Mengembalikan:**
float
### getHeight() {#getHeight--}
```
public abstract float getHeight()
```

Mengembalikan tinggi sebuah frame. Float hanya-baca.

**Mengembalikan:**
float
### getRotation() {#getRotation--}
```
public abstract float getRotation()
```

Mengembalikan jumlah derajat frame diputar di sekitar sumbu z. Nilai positif menunjukkan rotasi searah jarum jam; nilai negatif menunjukkan rotasi berlawanan arah jarum jam. Float hanya-baca.

**Mengembalikan:**
float
### getCenterX() {#getCenterX--}
```
public abstract float getCenterX()
```

Mengembalikan koordinat X pusat sebuah frame. Float hanya-baca.

**Mengembalikan:**
float
### getCenterY() {#getCenterY--}
```
public abstract float getCenterY()
```

Mengembalikan koordinat Y pusat sebuah frame. Float hanya-baca.

**Mengembalikan:**
float
### getFlipH() {#getFlipH--}
```
public abstract byte getFlipH()
```

Menentukan apakah sebuah frame dibalik secara horizontal. [NullableBool](../../com.aspose.slides/nullablebool) hanya-baca.

**Mengembalikan:**
byte
### getFlipV() {#getFlipV--}
```
public abstract byte getFlipV()
```

Menentukan apakah sebuah frame dibalik secara vertikal. [NullableBool](../../com.aspose.slides/nullablebool) hanya-baca.

**Mengembalikan:**
byte
### getRectangle() {#getRectangle--}
```
public abstract Rectangle2D.Float getRectangle()
```

Mengembalikan koordinat sebuah frame. java.awt.geom.Rectangle2D.Float hanya-baca.

**Mengembalikan:**
java.awt.geom.Rectangle2D.Float