---
title: LegendDataLabelPosition
second_title: Referensi API Aspose.Slides untuk Java
description: Menentukan posisi label data.
type: docs
url: /id/com.aspose.slides/legenddatalabelposition/
---
**Pewarisan:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class LegendDataLabelPosition extends System.Enum
```

Menentukan posisi label data.
## Bidang

| Bidang | Deskripsi |
| --- | --- |
| [NotDefined](#NotDefined) | Posisi tidak ditentukan. |
| [Bottom](#Bottom) | Menentukan bahwa label data harus ditampilkan di bawah penanda data. |
| [BestFit](#BestFit) | Menentukan bahwa label data harus ditampilkan pada posisi terbaik. |
| [Center](#Center) | Menentukan bahwa label data harus ditampilkan di tengah penanda data. |
| [InsideBase](#InsideBase) | Menentukan bahwa label data harus ditampilkan di dalam basis penanda data. |
| [InsideEnd](#InsideEnd) | Menentukan bahwa label data harus ditampilkan di dalam ujung penanda data. |
| [Left](#Left) | Menentukan bahwa label data harus ditampilkan di sebelah kiri penanda data. |
| [OutsideEnd](#OutsideEnd) | Menentukan bahwa label data harus ditampilkan di luar ujung penanda data. |
| [Right](#Right) | Menentukan bahwa label data harus ditampilkan di sebelah kanan penanda data. |
| [Top](#Top) | Menentukan bahwa label data harus ditampilkan di atas penanda data. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```

Posisi tidak ditentukan.

### Bottom {#Bottom}
```
public static final int Bottom
```

Menentukan bahwa label data harus ditampilkan di bawah penanda data.

### BestFit {#BestFit}
```
public static final int BestFit
```

Menentukan bahwa label data harus ditampilkan pada posisi terbaik.

### Center {#Center}
```
public static final int Center
```

Menentukan bahwa label data harus ditampilkan di tengah penanda data.

### InsideBase {#InsideBase}
```
public static final int InsideBase
```

Menentukan bahwa label data harus ditampilkan di dalam basis penanda data.

### InsideEnd {#InsideEnd}
```
public static final int InsideEnd
```

Menentukan bahwa label data harus ditampilkan di dalam ujung penanda data.

### Left {#Left}
```
public static final int Left
```

Menentukan bahwa label data harus ditampilkan di sebelah kiri penanda data.

### OutsideEnd {#OutsideEnd}
```
public static final int OutsideEnd
```

Menentukan bahwa label data harus ditampilkan di luar ujung penanda data.

### Right {#Right}
```
public static final int Right
```

Menentukan bahwa label data harus ditampilkan di sebelah kanan penanda data.

### Top {#Top}
```
public static final int Top
```

Menentukan bahwa label data harus ditampilkan di atas penanda data.