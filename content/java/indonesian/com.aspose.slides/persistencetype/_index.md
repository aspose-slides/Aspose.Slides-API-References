---
title: PersistenceType
second_title: Referensi API Aspose.Slides untuk Java
description: Menentukan metode yang digunakan untuk menyimpan properti kontrol ActiveX.
type: docs
url: /id/com.aspose.slides/persistencetype/
---
**Pewarisan:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class PersistenceType extends System.Enum
```

Menentukan metode yang digunakan untuk menyimpan properti kontrol ActiveX.
## Bidang

| Bidang | Deskripsi |
| --- | --- |
| [NotDefined](#NotDefined) | ID persistance tidak ditentukan. |
| [PersistPropertyBag](#PersistPropertyBag) | Menentukan bahwa kontrol ActiveX dipersistensikan menggunakan persistensi berbasis property-bag. |
| [PersistStream](#PersistStream) | Menentukan bahwa kontrol ActiveX dipersistensikan menggunakan persistensi berbasis stream yang tidak mendukung inisialisasi kontrol ActiveX ke keadaan default. |
| [PersistStreamInit](#PersistStreamInit) | Menentukan bahwa kontrol ActiveX dipersistensikan menggunakan persistensi berbasis stream yang mendukung inisialisasi kontrol ActiveX ke keadaan default. |
| [PersistStorage](#PersistStorage) | Menentukan bahwa kontrol ActiveX dipersistensikan menggunakan persistensi berbasis storage. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```

ID persistance tidak ditentukan.

### PersistPropertyBag {#PersistPropertyBag}
```
public static final int PersistPropertyBag
```

Menentukan bahwa kontrol ActiveX dipersistensikan menggunakan persistensi berbasis property-bag. Persistensi berbasis property-bag menyimpan kontrol ActiveX dengan cara kumpulan pasangan nama dan nilai yang menentukan data yang dipersistensikan oleh kontrol ActiveX.

### PersistStream {#PersistStream}
```
public static final int PersistStream
```

Menentukan bahwa kontrol ActiveX dipersistensikan menggunakan persistensi berbasis stream yang tidak mendukung inisialisasi kontrol ActiveX ke keadaan default.

### PersistStreamInit {#PersistStreamInit}
```
public static final int PersistStreamInit
```

Menentukan bahwa kontrol ActiveX dipersistensikan menggunakan persistensi berbasis stream yang mendukung inisialisasi kontrol ActiveX ke keadaan default.

### PersistStorage {#PersistStorage}
```
public static final int PersistStorage
```

Menentukan bahwa kontrol ActiveX dipersistensikan menggunakan persistensi berbasis storage.