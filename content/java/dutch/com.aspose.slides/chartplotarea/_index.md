---
title: ChartPlotArea
second_title: Aspose.Slides voor Java API-referentie
description: Stelt een rechthoek voor waar de grafiek moet worden getekend.
type: docs
url: /nl/com.aspose.slides/chartplotarea/
---
**Erfenis:**
java.lang.Object, com.aspose.slides.DomObject

**Alle geïmplementeerde interfaces:**
[com.aspose.slides.IChartPlotArea](../../com.aspose.slides/ichartplotarea)
```
public class ChartPlotArea extends DomObject<Chart> implements IChartPlotArea
```

Stelt een rechthoek voor waar de grafiek moet worden getekend.
## Methoden

| Methode | Beschrijving |
| --- | --- |
| [getFormat()](#getFormat--) | Retourneert het formaat van een plotgebied. |
| [getX()](#getX--) | Retourneert of stelt de x-coördinaat van de linkerbovenhoek van de begrenzende rechthoek van het plotgebied in als een fractie van de breedte van de grafiek (van 0 tot 1). |
| [setX(float value)](#setX-float-) | Retourneert of stelt de x-coördinaat van de linkerbovenhoek van de begrenzende rechthoek van het plotgebied in als een fractie van de breedte van de grafiek (van 0 tot 1). |
| [getY()](#getY--) | Retourneert of stelt de y-coördinaat van de linkerbovenhoek van de begrenzende rechthoek van het plotgebied in als een fractie van de hoogte van de grafiek (van 0 tot 1). |
| [setY(float value)](#setY-float-) | Retourneert of stelt de y-coördinaat van de linkerbovenhoek van de begrenzende rechthoek van het plotgebied in als een fractie van de hoogte van de grafiek (van 0 tot 1). |
| [getWidth()](#getWidth--) | Retourneert of stelt de breedte van een plotgebied begrenzende rechthoek in als een fractie van de breedte van de grafiek (van 0 tot 1). |
| [setWidth(float value)](#setWidth-float-) | Retourneert of stelt de breedte van een plotgebied begrenzende rechthoek in als een fractie van de breedte van de grafiek (van 0 tot 1). |
| [getHeight()](#getHeight--) | Retourneert of stelt de hoogte van een plotgebied begrenzende rechthoek in als een fractie van de hoogte van de grafiek (van 0 tot 1). |
| [setHeight(float value)](#setHeight-float-) | Retourneert of stelt de hoogte van een plotgebied begrenzende rechthoek in als een fractie van de hoogte van de grafiek (van 0 tot 1). |
| [getRight()](#getRight--) | Rechts. |
| [getBottom()](#getBottom--) | Onderkant. |
| [getChart()](#getChart--) | Grafiek. |
| [isLocationAutocalculated()](#isLocationAutocalculated--) | Definieert hoe de locatie moet worden berekend: true \u2013 automatisch berekend; gedefinieerd door de X, Y, Width, Height-eigenschappen. |
| [getLayoutTargetType()](#getLayoutTargetType--) | Als de lay-out van het plotgebied handmatig is gedefinieerd, geeft deze eigenschap aan of het plotgebied moet worden ingedeeld volgens de binnenkant (exclusief assen en aslabels) of volgens de buitenkant (inclusief assen en aslabels). |
| [setLayoutTargetType(int value)](#setLayoutTargetType-int-) | Als de lay-out van het plotgebied handmatig is gedefinieerd, geeft deze eigenschap aan of het plotgebied moet worden ingedeeld volgens de binnenkant (exclusief assen en aslabels) of volgens de buitenkant (inclusief assen en aslabels). |
| [getActualX()](#getActualX--) | Specificeert de werkelijke x-locatie (links) van het grafiekelement ten opzichte van de linkerbovenhoek van de grafiek. |
| [getActualY()](#getActualY--) | Specificeert de werkelijke bovenkant van het grafiekelement ten opzichte van de linkerbovenhoek van de grafiek. |
| [getActualWidth()](#getActualWidth--) | Specificeert de werkelijke breedte van het grafiekelement. |
| [getActualHeight()](#getActualHeight--) | Specificeert de werkelijke hoogte van het grafiekelement. |
| [getSlide()](#getSlide--) | Retourneert de bovenliggende dia van een FillFormat. |
| [getPresentation()](#getPresentation--) | Retourneert de bovenliggende presentatie van een FillFormat. |
### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

Retourneert het formaat van een plotgebied. Alleen-lezen [IFormat](../../com.aspose.slides/iformat).

**Retourneert:**
[IFormat](../../com.aspose.slides/iformat)
### getX() {#getX--}
```
public final float getX()
```

Retourneert of stelt de x-coördinaat van de linkerbovenhoek van de begrenzende rechthoek van het plotgebied in als een fractie van de breedte van de grafiek (van 0 tot 1). Lezen/schrijven float.

**Retourneert:**
float
### setX(float value) {#setX-float-}
```
public final void setX(float value)
```

Retourneert of stelt de x-coördinaat van de linkerbovenhoek van de begrenzende rechthoek van het plotgebied in als een fractie van de breedte van de grafiek (van 0 tot 1). Lezen/schrijven float.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | float |  |
### getY() {#getY--}
```
public final float getY()
```

Retourneert of stelt de y-coördinaat van de linkerbovenhoek van de begrenzende rechthoek van het plotgebied in als een fractie van de hoogte van de grafiek (van 0 tot 1). Lezen/schrijven float.

**Retourneert:**
float
### setY(float value) {#setY-float-}
```
public final void setY(float value)
```

Retourneert of stelt de y-coördinaat van de linkerbovenhoek van de begrenzende rechthoek van het plotgebied in als een fractie van de hoogte van de grafiek (van 0 tot 1). Lezen/schrijven float.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | float |  |
### getWidth() {#getWidth--}
```
public final float getWidth()
```

Retourneert of stelt de breedte van een plotgebied begrenzende rechthoek in als een fractie van de breedte van de grafiek (van 0 tot 1). Lezen/schrijven float.

**Retourneert:**
float
### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```

Retourneert of stelt de breedte van een plotgebied begrenzende rechthoek in als een fractie van de breedte van de grafiek (van 0 tot 1). Lezen/schrijven float.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | float |  |
### getHeight() {#getHeight--}
```
public final float getHeight()
```

Retourneert of stelt de hoogte van een plotgebied begrenzende rechthoek in als een fractie van de hoogte van de grafiek (van 0 tot 1). Lezen/schrijven float.

**Retourneert:**
float
### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```

Retourneert of stelt de hoogte van een plotgebied begrenzende rechthoek in als een fractie van de hoogte van de grafiek (van 0 tot 1). Lezen/schrijven float.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | float |  |
### getRight() {#getRight--}
```
public final float getRight()
```

Rechts. Alleen-lezen float.

**Retourneert:**
float
### getBottom() {#getBottom--}
```
public final float getBottom()
```

Onderkant. Alleen-lezen float.

**Retourneert:**
float
### getChart() {#getChart--}
```
public final IChart getChart()
```

Grafiek. Alleen-lezen [IChart](../../com.aspose.slides/ichart).

**Retourneert:**
[IChart](../../com.aspose.slides/ichart)
### isLocationAutocalculated() {#isLocationAutocalculated--}
```
public final boolean isLocationAutocalculated()
```

Definieert hoe de locatie moet worden berekend: true \u2013 automatisch berekend; gedefinieerd door de X, Y, Width, Height-eigenschappen. Alleen-lezen boolean.

**Retourneert:**
boolean
### getLayoutTargetType() {#getLayoutTargetType--}
```
public final int getLayoutTargetType()
```

Als de lay-out van het plotgebied handmatig is gedefinieerd, geeft deze eigenschap aan of het plotgebied moet worden ingedeeld volgens de binnenkant (exclusief assen en aslabels) of volgens de buitenkant (inclusief assen en aslabels). Lezen/schrijven [LayoutTargetType](../../com.aspose.slides/layouttargettype)(\#getLayoutTargetType.getLayoutTargetType/\#setLayoutTargetType(int).setLayoutTargetType(int)).

--------------------

> ```
> Presentation presentation = new Presentation();
>   try
>   {
>       ISlide slide = presentation.getSlides().get_Item(0);
>       IChart chart = slide.getShapes().addChart(ChartType.ClusteredColumn, 20, 100, 600, 400);
>       chart.getPlotArea().setX(0.2f);
>       chart.getPlotArea().setY(0.2f);
>       chart.getPlotArea().setWidth(0.7f);
>       chart.getPlotArea().setHeight(0.7f);
>       chart.getPlotArea().setLayoutTargetType(LayoutTargetType.Inner);
>       ...
>   } finally {
>       if (presentation != null) presentation.dispose();
>   }
> ```

**Retourneert:**
int
### setLayoutTargetType(int value) {#setLayoutTargetType-int-}
```
public final void setLayoutTargetType(int value)
```

Als de lay-out van het plotgebied handmatig is gedefinieerd, geeft deze eigenschap aan of het plotgebied moet worden ingedeeld volgens de binnenkant (exclusief assen en aslabels) of volgens de buitenkant (inclusief assen en aslabels). Lezen/schrijven [LayoutTargetType](../../com.aspose.slides/layouttargettype)(\#getLayoutTargetType.getLayoutTargetType/\#setLayoutTargetType(int).setLayoutTargetType(int)).

--------------------

> ```
> Presentation presentation = new Presentation();
>   try
>   {
>       ISlide slide = presentation.getSlides().get_Item(0);
>       IChart chart = slide.getShapes().addChart(ChartType.ClusteredColumn, 20, 100, 600, 400);
>       chart.getPlotArea().setX(0.2f);
>       chart.getPlotArea().setY(0.2f);
>       chart.getPlotArea().setWidth(0.7f);
>       chart.getPlotArea().setHeight(0.7f);
>       chart.getPlotArea().setLayoutTargetType(LayoutTargetType.Inner);
>       ...
>   } finally {
>       if (presentation != null) presentation.dispose();
>   }
> ```

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | int |  |
### getActualX() {#getActualX--}
```
public final float getActualX()
```

Specificeert de werkelijke x-locatie (links) van het grafiekelement ten opzichte van de linkerbovenhoek van de grafiek. Roep de methode IChart.ValidateChartLayout() aan voordat u de werkelijke waarden opvraagt. Lezen float.

**Retourneert:**
float
### getActualY() {#getActualY--}
```
public final float getActualY()
```

Specificeert de werkelijke bovenkant van het grafiekelement ten opzichte van de linkerbovenhoek van de grafiek. Roep de methode IChart.ValidateChartLayout() aan voordat u de werkelijke waarden opvraagt. Lezen float.

**Retourneert:**
float
### getActualWidth() {#getActualWidth--}
```
public final float getActualWidth()
```

Specificeert de werkelijke breedte van het grafiekelement. Roep de methode IChart.ValidateChartLayout() aan voordat u de werkelijke waarden opvraagt. Lezen float.

**Retourneert:**
float
### getActualHeight() {#getActualHeight--}
```
public final float getActualHeight()
```

Specificeert de werkelijke hoogte van het grafiekelement. Roep de methode IChart.ValidateChartLayout() aan voordat u de werkelijke waarden opvraagt. Lezen float.

**Retourneert:**
float
### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

Retourneert de bovenliggende dia van een FillFormat. Alleen-lezen [BaseSlide](../../com.aspose.slides/baseslide).

**Retourneert:**
[IBaseSlide](../../com.aspose.slides/ibaseslide)
### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

Retourneert de bovenliggende presentatie van een FillFormat. Alleen-lezen [IPresentation](../../com.aspose.slides/ipresentation).

**Retourneert:**
[IPresentation](../../com.aspose.slides/ipresentation)