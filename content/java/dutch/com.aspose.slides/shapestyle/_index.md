---
title: ShapeStyle
second_title: Aspose.Slides voor Java API-referentie
description: Vertegenwoordigt de stijlreferentie van vormen.
type: docs
url: /nl/com.aspose.slides/shapestyle/
---
**Erfenis:**
java.lang.Object, com.aspose.slides.DomObject

**Alle geïmplementeerde interfaces:**
[com.aspose.slides.IShapeStyle](../../com.aspose.slides/ishapestyle)
```
public class ShapeStyle extends DomObject<Shape> implements IShapeStyle
```

Representeert de stijlreferentie van de vorm.
## Methoden

| Methode | Beschrijving |
| --- | --- |
| [getLineColor()](#getLineColor--) | Geeft de omtrekkleur van een vorm terug. |
| [getLineStyleIndex()](#getLineStyleIndex--) | Geeft de kolomindex van de lijn in een stijlmatrix terug of stelt deze in. |
| [setLineStyleIndex(int value)](#setLineStyleIndex-int-) | Geeft de kolomindex van de lijn in een stijlmatrix terug of stelt deze in. |
| [getFillColor()](#getFillColor--) | Geeft de vulkleur van een vorm terug. |
| [getFillStyleIndex()](#getFillStyleIndex--) | Geeft de vulkolomindex van een vorm in stijlmatrices terug of stelt deze in. |
| [setFillStyleIndex(short value)](#setFillStyleIndex-short-) | Geeft de vulkolomindex van een vorm in stijlmatrices terug of stelt deze in. |
| [getEffectColor()](#getEffectColor--) | Geeft de effectkleur van een vorm terug. |
| [getEffectStyleIndex()](#getEffectStyleIndex--) | Geeft de effectkolomindex van een vorm in een stijlmatrix terug of stelt deze in. |
| [setEffectStyleIndex(long value)](#setEffectStyleIndex-long-) | Geeft de effectkolomindex van een vorm in een stijlmatrix terug of stelt deze in. |
| [getFontColor()](#getFontColor--) | Geeft de lettertypekleur van een vorm terug. |
| [getFontCollectionIndex()](#getFontCollectionIndex--) | Geeft de lettertype-index van een vorm in een lettertypecollectie terug of stelt deze in. |
| [setFontCollectionIndex(byte value)](#setFontCollectionIndex-byte-) | Geeft de lettertype-index van een vorm in een lettertypecollectie terug of stelt deze in. |
### getLineColor() {#getLineColor--}
```
public final IColorFormat getLineColor()
```

Geeft de omtrekkleur van een vorm terug. Alleen-lezen [IColorFormat](../../com.aspose.slides/icolorformat).

**Retour:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getLineStyleIndex() {#getLineStyleIndex--}
```
public final int getLineStyleIndex()
```

Geeft de kolomindex van de lijn in een stijlmatrix terug of stelt deze in. Lees/Schrijf int.

**Retour:**
int
### setLineStyleIndex(int value) {#setLineStyleIndex-int-}
```
public final void setLineStyleIndex(int value)
```

Geeft de kolomindex van de lijn in een stijlmatrix terug of stelt deze in. Lees/Schrijf int.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | int |  |
### getFillColor() {#getFillColor--}
```
public final IColorFormat getFillColor()
```

Geeft de vulkleur van een vorm terug. Alleen-lezen [IColorFormat](../../com.aspose.slides/icolorformat).

**Retour:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getFillStyleIndex() {#getFillStyleIndex--}
```
public final short getFillStyleIndex()
```

Geeft de vulkolomindex van een vorm in stijlmatrices terug of stelt deze in. 0 betekent geen vulling, een positieve waarde – index in de vulstijlen van het thema, een negatieve waarde – index in de achtergrondstijlen van het thema. Lees/Schrijf short.

**Retour:**
short
### setFillStyleIndex(short value) {#setFillStyleIndex-short-}
```
public final void setFillStyleIndex(short value)
```

Geeft de vulkolomindex van een vorm in stijlmatrices terug of stelt deze in. 0 betekent geen vulling, een positieve waarde – index in de vulstijlen van het thema, een negatieve waarde – index in de achtergrondstijlen van het thema. Lees/Schrijf short.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | short |  |
### getEffectColor() {#getEffectColor--}
```
public final IColorFormat getEffectColor()
```

Geeft de effectkleur van een vorm terug. Alleen-lezen [IColorFormat](../../com.aspose.slides/icolorformat).

**Retour:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getEffectStyleIndex() {#getEffectStyleIndex--}
```
public final long getEffectStyleIndex()
```

Geeft de effectkolomindex van een vorm in een stijlmatrix terug of stelt deze in. Lees/Schrijf long.

**Retour:**
long
### setEffectStyleIndex(long value) {#setEffectStyleIndex-long-}
```
public final void setEffectStyleIndex(long value)
```

Geeft de effectkolomindex van een vorm in een stijlmatrix terug of stelt deze in. Lees/Schrijf long.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | long |  |
### getFontColor() {#getFontColor--}
```
public final IColorFormat getFontColor()
```

Geeft de lettertypekleur van een vorm terug. Alleen-lezen [IColorFormat](../../com.aspose.slides/icolorformat).

**Retour:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getFontCollectionIndex() {#getFontCollectionIndex--}
```
public final byte getFontCollectionIndex()
```

Geeft de lettertype-index van een vorm in een lettertypecollectie terug of stelt deze in. Lees/Schrijf [FontCollectionIndex](../../com.aspose.slides/fontcollectionindex).

**Retour:**
byte
### setFontCollectionIndex(byte value) {#setFontCollectionIndex-byte-}
```
public final void setFontCollectionIndex(byte value)
```

Geeft de lettertype-index van een vorm in een lettertypecollectie terug of stelt deze in. Lees/Schrijf [FontCollectionIndex](../../com.aspose.slides/fontcollectionindex).

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| value | byte |  |