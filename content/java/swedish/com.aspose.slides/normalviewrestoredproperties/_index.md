---
title: NormalViewRestoredProperties
second_title: Aspose.Slides för Java API-referens
description: Anger storleken på bildspelsregionen bredd när den är ett barn till restoredTop höjd när den är ett barn till restoredLeft i normalvyn när regionen har en variabel återställd storlek (varken minimerad eller maximerad).
type: docs
url: /sv/com.aspose.slides/normalviewrestoredproperties/
---
**Arv:**
java.lang.Object

**Alla implementerade gränssnitt:**
[com.aspose.slides.INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)
```
public class NormalViewRestoredProperties implements INormalViewRestoredProperties
```

Anger storleken på bildspelsregionen ((bredd när den är ett barn till restoredTop, höjd när den är ett barn till restoredLeft) i normalvyn, när regionen har en variabel återställd storlek (varken minimerad eller maximerad)).

## Metoder

| Metod | Beskrivning |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | Anger storleken på bildspelsregionen (bredd när den är ett barn till RestoredTop, höjd när den är ett barn till RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | Anger storleken på bildspelsregionen (bredd när den är ett barn till RestoredTop, höjd när den är ett barn till RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | Anger om storleken på sidoinnehållsregionen ska kompensera för den nya storleken vid ändring av fönstrets storlek som innehåller vyn i programmet Läs/skriv boolesk. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | Anger om storleken på sidoinnehållsregionen ska kompensera för den nya storleken vid ändring av fönstrets storlek som innehåller vyn i programmet Läs/skriv boolesk. |

### getDimensionSize() {#getDimensionSize--}
```
public final float getDimensionSize()
```

Anger storleken på bildspelsregionen (bredd när den är ett barn till RestoredTop, höjd när den är ett barn till RestoredLeft). Läs/skriv float.

**Returnerar:**
float

### setDimensionSize(float value) {#setDimensionSize-float-}
```
public final void setDimensionSize(float value)
```

Anger storleken på bildspelsregionen (bredd när den är ett barn till RestoredTop, höjd när den är ett barn till RestoredLeft). Läs/skriv float.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public final boolean getAutoAdjust()
```

Anger om storleken på sidoinnehållsregionen ska kompensera för den nya storleken vid ändring av fönstrets storlek som innehåller vyn i programmet Läs/skriv boolesk.

**Returnerar:**
boolean

### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public final void setAutoAdjust(boolean value)
```

Anger om storleken på sidoinnehållsregionen ska kompensera för den nya storleken vid ändring av fönstrets storlek som innehåller vyn i programmet Läs/skriv boolesk.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | boolean |  |