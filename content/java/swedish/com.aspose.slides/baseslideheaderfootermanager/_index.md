---
title: BaseSlideHeaderFooterManager
second_title: Aspose.Slides för Java API-referens
description: Representerar en hanterare som innehåller beteendet för sidfot, datum-tid och sidnummer-platshållare för alla bildtyper.
type: docs
url: /sv/com.aspose.slides/baseslideheaderfootermanager/
---
**Arv:**
java.lang.Object, [com.aspose.slides.BaseHeaderFooterManager](../../com.aspose.slides/baseheaderfootermanager)
```
public abstract class BaseSlideHeaderFooterManager extends BaseHeaderFooterManager
```

Representerar hanterare som innehåller beteendet för sidfot, datum-tid och sidnummer-platshållare för alla bildtyper.
## Metoder

| Metod | Beskrivning |
| --- | --- |
| [isFooterVisible()](#isFooterVisible--) | Hämtar värde som indikerar att en sidfot-platshållare finns. |
| [isSlideNumberVisible()](#isSlideNumberVisible--) | Hämtar värde som indikerar att en sidnummer-platshållare finns. |
| [isDateTimeVisible()](#isDateTimeVisible--) | Hämtar värde som indikerar att en datum-tid-platshållare finns. |
| [setFooterVisibility(boolean isVisible)](#setFooterVisibility-boolean-) | Ändrar synligheten för bildsidfot-platshållare. |
| [setSlideNumberVisibility(boolean isVisible)](#setSlideNumberVisibility-boolean-) | Ändrar synligheten för bildsidnummer-platshållare. |
| [setDateTimeVisibility(boolean isVisible)](#setDateTimeVisibility-boolean-) | Ändrar synligheten för bilddatum-tid-platshållare. |
| [setFooterText(String text)](#setFooterText-java.lang.String-) | Ställer in text för bildsidfot-platshållare. |
| [setDateTimeText(String text)](#setDateTimeText-java.lang.String-) | Ställer in text för bilddatum-tid-platshållare. |
### isFooterVisible() {#isFooterVisible--}
```
public final boolean isFooterVisible()
```

Hämtar värde som indikerar att en sidfot-platshållare finns. Läser boolesk.

**Returnerar:**
boolean
### isSlideNumberVisible() {#isSlideNumberVisible--}
```
public final boolean isSlideNumberVisible()
```

Hämtar värde som indikerar att en sidnummer-platshållare finns. Läser boolesk.

**Returnerar:**
boolean
### isDateTimeVisible() {#isDateTimeVisible--}
```
public final boolean isDateTimeVisible()
```

Hämtar värde som indikerar att en datum-tid-platshållare finns. Läser boolesk.

**Returnerar:**
boolean
### setFooterVisibility(boolean isVisible) {#setFooterVisibility-boolean-}
```
public final void setFooterVisibility(boolean isVisible)
```

Ändrar synligheten för bildsidfot-platshållare.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| isVisible | boolean | true - gör en sidfot-platshållare synlig, annars - döljer den. |

### setSlideNumberVisibility(boolean isVisible) {#setSlideNumberVisibility-boolean-}
```
public final void setSlideNumberVisibility(boolean isVisible)
```

Ändrar synligheten för bildsidnummer-platshållare.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| isVisible | boolean | true - gör en sidnummer-platshållare synlig, annars - döljer den. |

### setDateTimeVisibility(boolean isVisible) {#setDateTimeVisibility-boolean-}
```
public final void setDateTimeVisibility(boolean isVisible)
```

Ändrar synligheten för bilddatum-tid-platshållare.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| isVisible | boolean | true - gör en datum-tid-platshållare synlig, annars - döljer den. |

### setFooterText(String text) {#setFooterText-java.lang.String-}
```
public final void setFooterText(String text)
```

Ställer in text för bildsidfot-platshållare.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| text | java.lang.String | Text att sätta. |

### setDateTimeText(String text) {#setDateTimeText-java.lang.String-}
```
public final void setDateTimeText(String text)
```

Ställer in text för bilddatum-tid-platshållare.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| text | java.lang.String | Text att sätta. |