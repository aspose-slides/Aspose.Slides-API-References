---
title: Timing
second_title: Aspose.Slides för Java API-referens
description: Representerar animationstiming.
type: docs
url: /sv/com.aspose.slides/timing/
---
**Inheritance:**
java.lang.Object

**All Implemented Interfaces:**
[com.aspose.slides.ITiming](../../com.aspose.slides/itiming), com.aspose.slides.IDOMObject
```
public class Timing implements ITiming, IDOMObject
```

Representerar animationstid.
## Metoder

| Metod | Beskrivning |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | Beskriver procentsatsen av varaktigheten för accelerationsbeteendeeffekten. |
| [setAccelerate(float value)](#setAccelerate-float-) | Beskriver procentsatsen av varaktigheten för accelerationsbeteendeeffekten. |
| [getDecelerate()](#getDecelerate--) | Beskriver procentsatsen av varaktigheten för decelerationsbeteendeeffekten. |
| [setDecelerate(float value)](#setDecelerate-float-) | Beskriver procentsatsen av varaktigheten för decelerationsbeteendeeffekten. |
| [getAutoReverse()](#getAutoReverse--) | Beskriver om animationen automatiskt ska spelas upp i omvänd riktning efter att ha spelats framåt. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | Beskriver om animationen automatiskt ska spelas upp i omvänd riktning efter att ha spelats framåt. |
| [getDuration()](#getDuration--) | Beskriver varaktigheten för animationseffekten. |
| [setDuration(float value)](#setDuration-float-) | Beskriver varaktigheten för animationseffekten. |
| [getRepeatCount()](#getRepeatCount--) | Beskriver hur många gånger effekten ska upprepas. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | Beskriver hur många gånger effekten ska upprepas. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | Detta attribut anger om effekten ska upprepas till slutet av bilden. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | Detta attribut anger om effekten ska upprepas till slutet av bilden. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | Detta attribut anger om effekten ska upprepas till nästa klick. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | Detta attribut anger om effekten ska upprepas till nästa klick. |
| [getRepeatDuration()](#getRepeatDuration--) | Beskriver hur många gånger effekten ska upprepas. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | Beskriver hur många gånger effekten ska upprepas. |
| [getRestart()](#getRestart--) | Anger om en effekt ska startas om efter avslut. |
| [setRestart(int value)](#setRestart-int-) | Anger om en effekt ska startas om efter avslut. |
| [getRewind()](#getRewind--) | Detta attribut anger om effekten ska spolas tillbaka när den är klar. |
| [setRewind(boolean value)](#setRewind-boolean-) | Detta attribut anger om effekten ska spolas tillbaka när den är klar. |
| [getSpeed()](#getSpeed--) | Anger procentsatsen för att påskynda (eller sakta ner) tidsinställningen. |
| [setSpeed(float value)](#setSpeed-float-) | Anger procentsatsen för att påskynda (eller sakta ner) tidsinställningen. |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | Beskriver fördröjningstid efter utlösare. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | Beskriver fördröjningstid efter utlösare. |
| [getTriggerType()](#getTriggerType--) | Beskriver utlösartyp. |
| [setTriggerType(int value)](#setTriggerType-int-) | Beskriver utlösartyp. |
| [getParent_Immediate()](#getParent-Immediate--) |  |

### getAccelerate() {#getAccelerate--}
```
public final float getAccelerate()
```

Beskriver procentsatsen av varaktigheten för accelerationsbeteendeeffekten. Läs/skriv float.

**Returnerar:**
float
### setAccelerate(float value) {#setAccelerate-float-}
```
public final void setAccelerate(float value)
```

Beskriver procentsatsen av varaktigheten för accelerationsbeteendeeffekten. Läs/skriv float.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public final float getDecelerate()
```

Beskriver procentsatsen av varaktigheten för decelerationsbeteendeeffekten. Läs/skriv float.

**Returnerar:**
float
### setDecelerate(float value) {#setDecelerate-float-}
```
public final void setDecelerate(float value)
```

Beskriver procentsatsen av varaktigheten för decelerationsbeteendeeffekten. Läs/skriv float.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public final boolean getAutoReverse()
```

Beskriver om animationen automatiskt ska spelas upp i omvänd riktning efter att ha spelats framåt. Läs/skriv boolean.

**Returnerar:**
boolean
### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public final void setAutoReverse(boolean value)
```

Beskriver om animationen automatiskt ska spelas upp i omvänd riktning efter att ha spelats framåt. Läs/skriv boolean.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public final float getDuration()
```

Beskriver varaktigheten för animationseffekten. Läs/skriv float.

**Returnerar:**
float
### setDuration(float value) {#setDuration-float-}
```
public final void setDuration(float value)
```

Beskriver varaktigheten för animationseffekten. Läs/skriv float.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public final float getRepeatCount()
```

Beskriver hur många gånger effekten ska upprepas. Läs/skriv float.

**Returnerar:**
float
### setRepeatCount(float value) {#setRepeatCount-float-}
```
public final void setRepeatCount(float value)
```

Beskriver hur många gånger effekten ska upprepas. Läs/skriv float.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public final boolean getRepeatUntilEndSlide()
```

Detta attribut anger om effekten ska upprepas till slutet av bilden. Läs/skriv boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Hämta effektsekvensen för den första bilden
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Hämta den första effekten i huvudsekvensen.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ändra effektens Timing/Repeat till "Till slutet av bilden"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Returnerar:**
boolean
### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public final void setRepeatUntilEndSlide(boolean value)
```

Detta attribut anger om effekten ska upprepas till slutet av bilden. Läs/skriv boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Hämta effektsekvensen för den första bilden
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Hämta den första effekten i huvudsekvensen.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ändra effektens Timing/Repeat till "Till slutet av bilden"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public final boolean getRepeatUntilNextClick()
```

Detta attribut anger om effekten ska upprepas till nästa klick. Läs/skriv boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Hämta effektsekvensen för den första bilden
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Hämta den första effekten i huvudsekvensen.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ändra effektens Timing/Repeat till "Till nästa klick"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Returnerar:**
boolean
### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public final void setRepeatUntilNextClick(boolean value)
```

Detta attribut anger om effekten ska upprepas till nästa klick. Läs/skriv boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Hämta effektsekvensen för den första bilden
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Hämta den första effekten i huvudsekvensen.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Ändra effektens Timing/Repeat till "Till nästa klick"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public final float getRepeatDuration()
```

Beskriver hur många gånger effekten ska upprepas. Läs/skriv float.

**Returnerar:**
float
### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public final void setRepeatDuration(float value)
```

Beskriver hur många gånger effekten ska upprepas. Läs/skriv float.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public final int getRestart()
```

Anger om en effekt ska startas om efter avslut. Läs/skriv [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Returnerar:**
int
### setRestart(int value) {#setRestart-int-}
```
public final void setRestart(int value)
```

Anger om en effekt ska startas om efter avslut. Läs/skriv [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public final boolean getRewind()
```

Detta attribut anger om effekten ska spolas tillbaka när den är klar. Läs/skriv boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Hämta effektsekvensen för den första bilden
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Hämta den första effekten i huvudsekvensen.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Aktivera effektens Timing/Rewind.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Returnerar:**
boolean
### setRewind(boolean value) {#setRewind-boolean-}
```
public final void setRewind(boolean value)
```

Detta attribut anger om effekten ska spolas tillbaka när den är klar. Läs/skriv boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Hämta effektsekvensen för den första bilden
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Hämta den första effekten i huvudsekvensen.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Aktivera effektens Timing/Rewind.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | boolean |  |

### getSpeed() {#getSpeed--}
```
public final float getSpeed()
```

Anger procentsatsen för att påskynda (eller sakta ner) tidsinställningen. Läs/skriv float.

**Returnerar:**
float
### setSpeed(float value) {#setSpeed-float-}
```
public final void setSpeed(float value)
```

Anger procentsatsen för att påskynda (eller sakta ner) tidsinställningen. Läs/skriv float.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public final float getTriggerDelayTime()
```

Beskriver fördröjningstid efter utlösare. Läs/skriv float.

**Returnerar:**
float
### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public final void setTriggerDelayTime(float value)
```

Beskriver fördröjningstid efter utlösare. Läs/skriv float.

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public final int getTriggerType()
```

Beskriver utlösartyp. Läs/skriv [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Returnerar:**
int
### setTriggerType(int value) {#setTriggerType-int-}
```
public final void setTriggerType(int value)
```

Beskriver utlösartyp. Läs/skriv [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Parametrar:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | int |  |

### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

Returnerar Parent_Immediate object. Endast läs IDOMObject.

**Returnerar:**
com.aspose.slides.IDOMObject