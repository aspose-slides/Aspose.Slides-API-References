---
title: IScaleEffect
second_title: Référence de l'API Aspose.Slides pour Java
description: Représente l'effet d'échelle d'animation.
type: docs
url: /fr/com.aspose.slides/iscaleeffect/
---
**Toutes les interfaces implémentées :**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IScaleEffect extends IBehavior
```

Représente l'effet d'échelle d'animation.
## Méthodes

| Méthode | Description |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Détermine si un contenu doit être agrandi. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Détermine si un contenu doit être agrandi. |
| [getFrom()](#getFrom--) | Spécifie une coordonnée x/y à partir de laquelle démarrer l'animation (en pourcentages). |
| [setFrom(Point2D.Float value)](#setFrom-java.awt.geom.Point2D.Float-) | Spécifie une coordonnée x/y à partir de laquelle démarrer l'animation (en pourcentages). |
| [getTo()](#getTo--) | Spécifie la position cible pour un effet d'échelle d'animation (en pourcentages). |
| [setTo(Point2D.Float value)](#setTo-java.awt.geom.Point2D.Float-) | Spécifie la position cible pour un effet d'échelle d'animation (en pourcentages). |
| [getBy()](#getBy--) | décrit la valeur de décalage relative pour l'animation (en pourcentages). |
| [setBy(Point2D.Float value)](#setBy-java.awt.geom.Point2D.Float-) | décrit la valeur de décalage relative pour l'animation (en pourcentages). |
### getZoomContent() {#getZoomContent--}
```
public abstract byte getZoomContent()
```

Détermine si un contenu doit être agrandi. Lecture/écriture [NullableBool](../../com.aspose.slides/nullablebool).

**Renvoie :**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public abstract void setZoomContent(byte value)
```

Détermine si un contenu doit être agrandi. Lecture/écriture [NullableBool](../../com.aspose.slides/nullablebool).

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public abstract Point2D.Float getFrom()
```

Spécifie une coordonnée x/y à partir de laquelle démarrer l'animation (en pourcentages). Lecture/écriture java.awt.geom.Point2D.Float.

**Renvoie :**
java.awt.geom.Point2D.Float
### setFrom(Point2D.Float value) {#setFrom-java.awt.geom.Point2D.Float-}
```
public abstract void setFrom(Point2D.Float value)
```

Spécifie une coordonnée x/y à partir de laquelle démarrer l'animation (en pourcentages). Lecture/écriture java.awt.geom.Point2D.Float.

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getTo() {#getTo--}
```
public abstract Point2D.Float getTo()
```

Spécifie la position cible pour un effet d'échelle d'animation (en pourcentages). Lecture/écriture java.awt.geom.Point2D.Float.

**Renvoie :**
java.awt.geom.Point2D.Float
### setTo(Point2D.Float value) {#setTo-java.awt.geom.Point2D.Float-}
```
public abstract void setTo(Point2D.Float value)
```

Spécifie la position cible pour un effet d'échelle d'animation (en pourcentages). Lecture/écriture java.awt.geom.Point2D.Float.

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getBy() {#getBy--}
```
public abstract Point2D.Float getBy()
```

décrit la valeur de décalage relative pour l'animation (en pourcentages). Lecture/écriture java.awt.geom.Point2D.Float.

**Renvoie :**
java.awt.geom.Point2D.Float
### setBy(Point2D.Float value) {#setBy-java.awt.geom.Point2D.Float-}
```
public abstract void setBy(Point2D.Float value)
```

décrit la valeur de décalage relative pour l'animation (en pourcentages). Lecture/écriture java.awt.geom.Point2D.Float.

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |