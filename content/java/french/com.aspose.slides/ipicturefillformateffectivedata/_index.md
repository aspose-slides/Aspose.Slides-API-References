---
title: IPictureFillFormatEffectiveData
second_title: Référence de l'API Aspose.Slides pour Java
description: Objet immuable qui contient les propriétés du remplissage d'image.
type: docs
url: /fr/com.aspose.slides/ipicturefillformateffectivedata/
---
**Toutes les interfaces implémentées:**
[com.aspose.slides.IFillParamSource](../../com.aspose.slides/ifillparamsource)
```
public interface IPictureFillFormatEffectiveData extends IFillParamSource
```

Objet immuable qui contient les propriétés du remplissage d’image.

--------------------

Cette interface est utilisée comme partie de [IFillFormatEffectiveData](../../com.aspose.slides/ifillformateffectivedata).
## Méthodes

| Méthode | Description |
| --- | --- |
| [getDpi()](#getDpi--) | Renvoie le dpi qui est utilisé pour remplir une image. |
| [getPictureFillMode()](#getPictureFillMode--) | Renvoie le mode de remplissage d’image. |
| [getPicture()](#getPicture--) | Renvoie l’image. |
| [getCropLeft()](#getCropLeft--) | Renvoie le pourcentage de la largeur réelle de l’image recadré du côté gauche de l’image. |
| [getCropTop()](#getCropTop--) | Renvoie le pourcentage de la hauteur réelle de l’image recadré du haut de l’image. |
| [getCropRight()](#getCropRight--) | Renvoie le pourcentage de la largeur réelle de l’image recadré du côté droit de l’image. |
| [getCropBottom()](#getCropBottom--) | Renvoie le pourcentage de la hauteur réelle de l’image recadré du bas de l’image. |
### getDpi() {#getDpi--}
```
public abstract int getDpi()
```


Renvoie le dpi qui est utilisé pour remplir une image. Lecture seule int.

**Renvoie :**
int
### getPictureFillMode() {#getPictureFillMode--}
```
public abstract int getPictureFillMode()
```


Renvoie le mode de remplissage d’image. Lecture seule [PictureFillMode](../../com.aspose.slides/picturefillmode).

**Renvoie :**
int
### getPicture() {#getPicture--}
```
public abstract IPictureEffectiveData getPicture()
```


Renvoie l’image. Lecture seule [IPictureEffectiveData](../../com.aspose.slides/ipictureeffectivedata).

**Renvoie :**
[IPictureEffectiveData](../../com.aspose.slides/ipictureeffectivedata)
### getCropLeft() {#getCropLeft--}
```
public abstract float getCropLeft()
```


Renvoie le pourcentage de la largeur réelle de l’image recadré du côté gauche. Lecture seule float.

**Renvoie :**
float
### getCropTop() {#getCropTop--}
```
public abstract float getCropTop()
```


Renvoie le pourcentage de la hauteur réelle de l’image recadré du haut. Lecture seule float.

**Renvoie :**
float
### getCropRight() {#getCropRight--}
```
public abstract float getCropRight()
```


Renvoie le pourcentage de la largeur réelle de l’image recadré du côté droit. Lecture seule float.

**Renvoie :**
float
### getCropBottom() {#getCropBottom--}
```
public abstract float getCropBottom()
```


Renvoie le pourcentage de la hauteur réelle de l’image recadré du bas. Lecture seule float.

**Renvoie :**
float