---
title: MathRowSpacingRule
second_title: Référence de l'API Aspose.Slides pour Java
description: Type d'espacement vertical entre les colonnes d'une matrice ou d'un tableau
type: docs
url: /fr/com.aspose.slides/mathrowspacingrule/
---
**Héritage:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class MathRowSpacingRule extends System.Enum
```

Le type d'espacement vertical entre les colonnes d'une matrice ou d'un tableau
## Champs

| Champ | Description |
| --- | --- |
| [SingleLineGap](#SingleLineGap) | écart d'une ligne |
| [OneAndAHalfLineGap](#OneAndAHalfLineGap) | écart d'une ligne et demie |
| [TwoLineGap](#TwoLineGap) | écart de deux lignes |
| [Exactly](#Exactly) | Exactly (se base sur la valeur de la propriété RowSpacing, mesurée en points) |
| [Multiple](#Multiple) | Multiple (se base sur la valeur de la propriété RowSpacing, mesurée en lignes) |
### SingleLineGap {#SingleLineGap}
```
public static final int SingleLineGap
```

écart d'une ligne

### OneAndAHalfLineGap {#OneAndAHalfLineGap}
```
public static final int OneAndAHalfLineGap
```

écart d'une ligne et demie

### TwoLineGap {#TwoLineGap}
```
public static final int TwoLineGap
```

écart de deux lignes

### Exactly {#Exactly}
```
public static final int Exactly
```

Exactly (se base sur la valeur de la propriété RowSpacing, mesurée en points)

### Multiple {#Multiple}
```
public static final int Multiple
```

Multiple (se base sur la valeur de la propriété RowSpacing, mesurée en lignes)