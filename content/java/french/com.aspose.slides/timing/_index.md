---
title: Timing
second_title: Référence de l'API Aspose.Slides pour Java
description: Représente le chronométrage de l'animation.
type: docs
url: /fr/com.aspose.slides/timing/
---
**Héritage :**  
java.lang.Object

**Toutes les interfaces implémentées :**  
[com.aspose.slides.ITiming](../../com.aspose.slides/itiming), com.aspose.slides.IDOMObject  
```
public class Timing implements ITiming, IDOMObject
```

Représente le chronométrage de l'animation.

## Méthodes

| Méthode | Description |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | Décrit le pourcentage de durée de l'effet d'accélération du comportement. |
| [setAccelerate(float value)](#setAccelerate-float-) | Décrit le pourcentage de durée de l'effet d'accélération du comportement. |
| [getDecelerate()](#getDecelerate--) | Décrit le pourcentage de durée de l'effet de décélération du comportement. |
| [setDecelerate(float value)](#setDecelerate-float-) | Décrit le pourcentage de durée de l'effet de décélération du comportement. |
| [getAutoReverse()](#getAutoReverse--) | Décrit si l'animation doit être lue automatiquement en sens inverse après sa lecture vers l'avant. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | Décrit si l'animation doit être lue automatiquement en sens inverse après sa lecture vers l'avant. |
| [getDuration()](#getDuration--) | Décrit la durée de l'effet d'animation. |
| [setDuration(float value)](#setDuration-float-) | Décrit la durée de l'effet d'animation. |
| [getRepeatCount()](#getRepeatCount--) | Décrit le nombre de fois que l'effet doit se répéter. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | Décrit le nombre de fois que l'effet doit se répéter. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | Cet attribut indique si l'effet se répétera jusqu'à la fin de la diapositive. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | Cet attribut indique si l'effet se répétera jusqu'à la fin de la diapositive. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | Cet attribut indique si l'effet se répétera jusqu'au prochain clic. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | Cet attribut indique si l'effet se répétera jusqu'au prochain clic. |
| [getRepeatDuration()](#getRepeatDuration--) | Décrit le nombre de fois que l'effet doit se répéter. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | Décrit le nombre de fois que l'effet doit se répéter. |
| [getRestart()](#getRestart--) | Spécifie si un effet doit redémarrer après son achèvement. |
| [setRestart(int value)](#setRestart-int-) | Spécifie si un effet doit redémarrer après son achèvement. |
| [getRewind()](#getRewind--) | Cet attribut indique si l'effet reviendra en arrière lorsqu'il aura fini de jouer. |
| [setRewind(boolean value)](#setRewind-boolean-) | Cet attribut indique si l'effet reviendra en arrière lorsqu'il aura fini de jouer. |
| [getSpeed()](#getSpeed--) | Spécifie le pourcentage d'accélération (ou de ralentissement) du chronométrage. |
| [setSpeed(float value)](#setSpeed-float-) | Spécifie le pourcentage d'accélération (ou de ralentissement) du chronométrage. |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | Décrit le délai après le déclencheur. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | Décrit le délai après le déclencheur. |
| [getTriggerType()](#getTriggerType--) | Décrit le type de déclencheur. |
| [setTriggerType(int value)](#setTriggerType-int-) | Décrit le type de déclencheur. |
| [getParent_Immediate()](#getParent-Immediate--) |  |

### getAccelerate() {#getAccelerate--}
```
public final float getAccelerate()
```

Décrit le pourcentage de durée de l'effet d'accélération du comportement. Lecture/écriture float.

**Retour :**  
float

### setAccelerate(float value) {#setAccelerate-float-}
```
public final void setAccelerate(float value)
```

Décrit le pourcentage de durée de l'effet d'accélération du comportement. Lecture/écriture float.

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public final float getDecelerate()
```

Décrit le pourcentage de durée de l'effet de décélération du comportement. Lecture/écriture float.

**Retour :**  
float

### setDecelerate(float value) {#setDecelerate-float-}
```
public final void setDecelerate(float value)
```

Décrit le pourcentage de durée de l'effet de décélération du comportement. Lecture/écriture float.

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public final boolean getAutoReverse()
```

Décrit si l'animation doit être lue automatiquement en sens inverse après sa lecture vers l'avant. Lecture/écriture boolean.

**Retour :**  
boolean

### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public final void setAutoReverse(boolean value)
```

Décrit si l'animation doit être lue automatiquement en sens inverse après sa lecture vers l'avant. Lecture/écriture boolean.

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public final float getDuration()
```

Décrit la durée de l'effet d'animation. Lecture/écriture float.

**Retour :**  
float

### setDuration(float value) {#setDuration-float-}
```
public final void setDuration(float value)
```

Décrit la durée de l'effet d'animation. Lecture/écriture float.

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public final float getRepeatCount()
```

Décrit le nombre de fois que l'effet doit se répéter. Lecture/écriture float.

**Retour :**  
float

### setRepeatCount(float value) {#setRepeatCount-float-}
```
public final void setRepeatCount(float value)
```

Décrit le nombre de fois que l'effet doit se répéter. Lecture/écriture float.

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public final boolean getRepeatUntilEndSlide()
```

Cet attribut indique si l'effet se répétera jusqu'à la fin de la diapositive. Lecture/écriture boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtenir la séquence d'effets pour la première diapositive
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtenir le premier effet de la séquence principale.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Modifier le Timing/Repeat de l'effet en "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Retour :**  
boolean

### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public final void setRepeatUntilEndSlide(boolean value)
```

Cet attribut indique si l'effet se répétera jusqu'à la fin de la diapositive. Lecture/écriture boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtenir la séquence d'effets pour la première diapositive
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtenir le premier effet de la séquence principale.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Modifier le Timing/Repeat de l'effet en "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public final boolean getRepeatUntilNextClick()
```

Cet attribut indique si l'effet se répétera jusqu'au prochain clic. Lecture/écriture boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtenir la séquence d'effets pour la première diapositive
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtenir le premier effet de la séquence principale.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Modifier le Timing/Repeat de l'effet en "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Retour :**  
boolean

### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public final void setRepeatUntilNextClick(boolean value)
```

Cet attribut indique si l'effet se répétera jusqu'au prochain clic. Lecture/écriture boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtenir la séquence d'effets pour la première diapositive
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtenir le premier effet de la séquence principale.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Modifier le Timing/Repeat de l'effet en "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public final float getRepeatDuration()
```

Décrit le nombre de fois que l'effet doit se répéter. Lecture/écriture float.

**Retour :**  
float

### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public final void setRepeatDuration(float value)
```

Décrit le nombre de fois que l'effet doit se répéter. Lecture/écriture float.

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public final int getRestart()
```

Spécifie si un effet doit redémarrer après son achèvement. Lecture/écriture [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Retour :**  
int

### setRestart(int value) {#setRestart-int-}
```
public final void setRestart(int value)
```

Spécifie si un effet doit redémarrer après son achèvement. Lecture/écriture [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public final boolean getRewind()
```

Cet attribut indique si l'effet reviendra en arrière lorsqu'il aura fini de jouer. Lecture/écriture boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtenir la séquence d'effets pour la première diapositive
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtenir le premier effet de la séquence principale.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Activer le Timing/Rewind de l'effet.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Retour :**  
boolean

### setRewind(boolean value) {#setRewind-boolean-}
```
public final void setRewind(boolean value)
```

Cet attribut indique si l'effet reviendra en arrière lorsqu'il aura fini de jouer. Lecture/écriture boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Obtenir la séquence d'effets pour la première diapositive
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Obtenir le premier effet de la séquence principale.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Activer le Timing/Rewind de l'effet.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getSpeed() {#getSpeed--}
```
public final float getSpeed()
```

Spécifie le pourcentage d'accélération (ou de ralentissement) du chronométrage. Lecture/écriture float.

**Retour :**  
float

### setSpeed(float value) {#setSpeed-float-}
```
public final void setSpeed(float value)
```

Spécifie le pourcentage d'accélération (ou de ralentissement) du chronométrage. Lecture/écriture float.

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public final float getTriggerDelayTime()
```

Décrit le délai après le déclencheur. Lecture/écriture float.

**Retour :**  
float

### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public final void setTriggerDelayTime(float value)
```

Décrit le délai après le déclencheur. Lecture/écriture float.

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public final int getTriggerType()
```

Décrit le type de déclencheur. Lecture/écriture [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Retour :**  
int

### setTriggerType(int value) {#setTriggerType-int-}
```
public final void setTriggerType(int value)
```

Décrit le type de déclencheur. Lecture/écriture [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Paramètres :**
| Paramètre | Type | Description |
| --- | --- | --- |
| value | int |  |

### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

Renvoie l'objet Parent_Immediate. Lecture seule IDOMObject.

**Retour :**  
com.aspose.slides.IDOMObject