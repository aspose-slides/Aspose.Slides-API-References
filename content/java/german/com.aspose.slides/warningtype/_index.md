---
title: WarningType
second_title: Aspose.Slides für Java API Referenz
description: Stellt einen Warnungstyp dar.
type: docs
url: /de/com.aspose.slides/warningtype/
---
**Vererbung:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class WarningType extends System.Enum
```

Stellt einen Warnungstyp dar.
## Felder

| Feld | Beschreibung |
| --- | --- |
| [SourceFileCorruption](#SourceFileCorruption) | Ein Problem wurde im Quelldokument festgestellt, das es sehr wahrscheinlich macht, dass das Dokument nicht geöffnet werden kann, wenn es im Originalformat gespeichert wird. |
| [DataLoss](#DataLoss) | Text/Diagramm/Bild oder andere Daten werden nach dem Laden vollständig im Dokumentbaum oder im nach dem Speichern erstellten Dokument fehlen. |
| [MajorFormattingLoss](#MajorFormattingLoss) | Großer Formatierungsverlust. |
| [MinorFormattingLoss](#MinorFormattingLoss) | Geringer Formatierungsverlust. |
| [CompatibilityIssue](#CompatibilityIssue) | Dies ist ein bekanntes Problem, das das Öffnen des Dokuments durch bestimmte Benutzeragenten oder frühere Versionen von Benutzeragenten verhindert. |
| [UnexpectedContent](#UnexpectedContent) | Einige Inhalte im Quell-Dokument konnten nicht erkannt werden (z. B. |

### SourceFileCorruption {#SourceFileCorruption}
```
public static final int SourceFileCorruption
```

Ein Problem wurde im Quelldokument festgestellt, das es sehr wahrscheinlich macht, dass das Dokument nicht geöffnet werden kann, wenn es im Originalformat gespeichert wird.

### DataLoss {#DataLoss}
```
public static final int DataLoss
```

Text/Diagramm/Bild oder andere Daten werden nach dem Laden vollständig im Dokumentbaum oder im nach dem Speichern erstellten Dokument fehlen.

### MajorFormattingLoss {#MajorFormattingLoss}
```
public static final int MajorFormattingLoss
```

Großer Formatierungsverlust.

### MinorFormattingLoss {#MinorFormattingLoss}
```
public static final int MinorFormattingLoss
```

Geringer Formatierungsverlust.

### CompatibilityIssue {#CompatibilityIssue}
```
public static final int CompatibilityIssue
```

Dies ist ein bekanntes Problem, das das Öffnen des Dokuments durch bestimmte Benutzeragenten oder frühere Versionen von Benutzeragenten verhindert.

### UnexpectedContent {#UnexpectedContent}
```
public static final int UnexpectedContent
```

Einige Inhalte im Quell-Dokument konnten nicht erkannt werden (z. B. ist nicht unterstützt), dies kann zu Problemen oder zu Daten-/Formatierungsverlust führen.