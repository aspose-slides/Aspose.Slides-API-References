---
title: NormalViewRestoredProperties
second_title: "Aspose.Slides für Java API-Referenz"
description: "Gibt die Größe des Folienbereichs an: Breite, wenn ein Kind von restoredTop, Höhe, wenn ein Kind von restoredLeft, der normalen Ansicht, wenn der Bereich eine variable wiederhergestellte Größe hat, weder minimiert noch maximiert."
type: docs
url: /de/com.aspose.slides/normalviewrestoredproperties/
---
**Vererbung:**
java.lang.Object

**Alle implementierten Schnittstellen:**
[com.aspose.slides.INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)
```
public class NormalViewRestoredProperties implements INormalViewRestoredProperties
```

Legt die Größe des Folienbereichs fest ((Breite, wenn ein Kind von restoredTop, Höhe, wenn ein Kind von restoredLeft) der normalen Ansicht, wenn der Bereich eine variable wiederhergestellte Größe hat (weder minimiert noch maximiert)).

## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | Legt die Größe des Folienbereichs fest (Breite, wenn ein Kind von RestoredTop, Höhe, wenn ein Kind von RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | Legt die Größe des Folienbereichs fest (Breite, wenn ein Kind von RestoredTop, Höhe, wenn ein Kind von RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | Legt fest, ob die Größe des Seiteninhaltsbereichs die neue Größe beim Ändern der Fenstergröße, das die Ansicht in der Anwendung enthält, kompensieren soll Lese-/Schreib boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | Legt fest, ob die Größe des Seiteninhaltsbereichs die neue Größe beim Ändern der Fenstergröße, das die Ansicht in der Anwendung enthält, kompensieren soll Lese-/Schreib boolean. |

### getDimensionSize() {#getDimensionSize--}
```
public final float getDimensionSize()
```

Legt die Größe des Folienbereichs fest (Breite, wenn ein Kind von RestoredTop, Höhe, wenn ein Kind von RestoredLeft). Lese-/Schreib float.

**Rückgabewert:**
float

### setDimensionSize(float value) {#setDimensionSize-float-}
```
public final void setDimensionSize(float value)
```

Legt die Größe des Folienbereichs fest (Breite, wenn ein Kind von RestoredTop, Höhe, wenn ein Kind von RestoredLeft). Lese-/Schreib float.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public final boolean getAutoAdjust()
```

Legt fest, ob die Größe des Seiteninhaltsbereichs die neue Größe beim Ändern der Fenstergröße, das die Ansicht in der Anwendung enthält, kompensieren soll Lese-/Schreib boolean.

**Rückgabewert:**
boolean

### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public final void setAutoAdjust(boolean value)
```

Legt fest, ob die Größe des Seiteninhaltsbereichs die neue Größe beim Ändern der Fenstergröße, das die Ansicht in der Anwendung enthält, kompensieren soll Lese-/Schreib boolean.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | boolean |  |