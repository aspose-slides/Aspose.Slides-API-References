---
title: IScaleEffect
second_title: Aspose.Slides für die Java-API-Referenz
description: Stellt den Skalierungseffekt der Animation dar.
type: docs
url: /de/com.aspose.slides/iscaleeffect/
---
**Alle implementierten Schnittstellen:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IScaleEffect extends IBehavior
```

Stellt den Skalierungseffekt der Animation dar.
## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Bestimmt, ob ein Inhalt gezoomt werden soll. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Bestimmt, ob ein Inhalt gezoomt werden soll. |
| [getFrom()](#getFrom--) | Gibt eine x/y-Koordinate an, von der die Animation ausgeführt wird (in Prozent). |
| [setFrom(Point2D.Float value)](#setFrom-java.awt.geom.Point2D.Float-) | Gibt eine x/y-Koordinate an, von der die Animation ausgeführt wird (in Prozent). |
| [getTo()](#getTo--) | Gibt den Zielort für einen Skalierungseffekt der Animation an (in Prozent). |
| [setTo(Point2D.Float value)](#setTo-java.awt.geom.Point2D.Float-) | Gibt den Zielort für einen Skalierungseffekt der Animation an (in Prozent). |
| [getBy()](#getBy--) | Beschreibt den relativen Versatzwert für die Animation (in Prozent). |
| [setBy(Point2D.Float value)](#setBy-java.awt.geom.Point2D.Float-) | Beschreibt den relativen Versatzwert für die Animation (in Prozent). |
### getZoomContent() {#getZoomContent--}
```
public abstract byte getZoomContent()
```

Bestimmt, ob ein Inhalt gezoomt werden soll. Lesen/Schreiben [NullableBool](../../com.aspose.slides/nullablebool).

**Rückgabewert:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public abstract void setZoomContent(byte value)
```

Bestimmt, ob ein Inhalt gezoomt werden soll. Lesen/Schreiben [NullableBool](../../com.aspose.slides/nullablebool).

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | byte |  |
### getFrom() {#getFrom--}
```
public abstract Point2D.Float getFrom()
```

Gibt eine x/y-Koordinate an, von der die Animation ausgeführt wird (in Prozent). Lesen/Schreiben java.awt.geom.Point2D.Float.

**Rückgabewert:**
java.awt.geom.Point2D.Float
### setFrom(Point2D.Float value) {#setFrom-java.awt.geom.Point2D.Float-}
```
public abstract void setFrom(Point2D.Float value)
```

Gibt eine x/y-Koordinate an, von der die Animation ausgeführt wird (in Prozent). Lesen/Schreiben java.awt.geom.Point2D.Float.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |
### getTo() {#getTo--}
```
public abstract Point2D.Float getTo()
```

Gibt den Zielort für einen Skalierungseffekt der Animation an (in Prozent). Lesen/Schreiben java.awt.geom.Point2D.Float.

**Rückgabewert:**
java.awt.geom.Point2D.Float
### setTo(Point2D.Float value) {#setTo-java.awt.geom.Point2D.Float-}
```
public abstract void setTo(Point2D.Float value)
```

Gibt den Zielort für einen Skalierungseffekt der Animation an (in Prozent). Lesen/Schreiben java.awt.geom.Point2D.Float.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |
### getBy() {#getBy--}
```
public abstract Point2D.Float getBy()
```

Beschreibt den relativen Versatzwert für die Animation (in Prozent). Lesen/Schreiben java.awt.geom.Point2D.Float.

**Rückgabewert:**
java.awt.geom.Point2D.Float
### setBy(Point2D.Float value) {#setBy-java.awt.geom.Point2D.Float-}
```
public abstract void setBy(Point2D.Float value)
```

Beschreibt den relativen Versatzwert für die Animation (in Prozent). Lesen/Schreiben java.awt.geom.Point2D.Float.

**Parameter:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |