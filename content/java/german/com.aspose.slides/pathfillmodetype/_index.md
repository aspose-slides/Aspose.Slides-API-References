---
title: PathFillModeType
second_title: Aspose.Slides für Java API Referenz
description: Gibt an, wie ein Pfad ausgefüllt werden soll
type: docs
url: /de/com.aspose.slides/pathfillmodetype/
---
**Vererbung:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class PathFillModeType extends System.Enum
```

Gibt an, wie ein Pfad ausgefüllt werden soll
## Felder

| Feld | Beschreibung |
| --- | --- |
| [None](#None) | No Path Fill Dies gibt an, dass der entsprechende Pfad keine Füllung hat. |
| [Normal](#Normal) | Normal Path Fill Dies gibt an, dass der entsprechende Pfad eine normal schattierte Farbe auf seine Füllung angewendet bekommt. |
| [Lighten](#Lighten) | Lighten Path Fill Dies gibt an, dass der entsprechende Pfad eine leicht schattierte Farbe auf seine Füllung angewendet bekommt. |
| [LightenLess](#LightenLess) | Lighten Path Fill Less Dies gibt an, dass der entsprechende Pfad eine etwas hellere schattierte Farbe auf seine Füllung angewendet bekommt. |
| [Darken](#Darken) | Darken Path Fill Dies gibt an, dass der entsprechende Pfad eine dunklere schattierte Farbe auf seine Füllung angewendet bekommt. |
| [DarkenLess](#DarkenLess) | Darken Path Fill Less Dies gibt an, dass der entsprechende Pfad eine etwas dunklere schattierte Farbe auf seine Füllung angewendet bekommt. |
### None {#None}
```
public static final byte None
```

No Path Fill Dies gibt an, dass der entsprechende Pfad keine Füllung hat.

### Normal {#Normal}
```
public static final byte Normal
```

Normal Path Fill Dies gibt an, dass der entsprechende Pfad eine normal schattierte Farbe auf seine Füllung angewendet bekommt.

### Lighten {#Lighten}
```
public static final byte Lighten
```

Lighten Path Fill Dies gibt an, dass der entsprechende Pfad eine leicht schattierte Farbe auf seine Füllung angewendet bekommt.

### LightenLess {#LightenLess}
```
public static final byte LightenLess
```

Lighten Path Fill Less Dies gibt an, dass der entsprechende Pfad eine etwas hellere schattierte Farbe auf seine Füllung angewendet bekommt.

### Darken {#Darken}
```
public static final byte Darken
```

Darken Path Fill Dies gibt an, dass der entsprechende Pfad eine dunklere schattierte Farbe auf seine Füllung angewendet bekommt.

### DarkenLess {#DarkenLess}
```
public static final byte DarkenLess
```

Darken Path Fill Less Dies gibt an, dass der entsprechende Pfad eine etwas dunklere schattierte Farbe auf seine Füllung angewendet bekommt.