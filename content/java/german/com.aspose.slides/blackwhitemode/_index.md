---
title: BlackWhiteMode
second_title: Aspose.Slides für Java API Referenz
description: Bestimmt, wie farbige Formen in Schwarz-weiß umgewandelt werden sollen.
type: docs
url: /de/com.aspose.slides/blackwhitemode/
---
**Vererbung:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class BlackWhiteMode extends System.Enum
```

Bestimmt, wie farbige Formen in Schwarz-weiß umgewandelt werden sollen.

## Felder

| Feld | Beschreibung |
| --- | --- |
| [NotDefined](#NotDefined) | Die Eigenschaft ist nicht gesetzt |
| [Color](#Color) | Das Objekt wird mit normaler Farbgebung dargestellt |
| [Automatic](#Automatic) | Das Objekt wird mit automatischer Farbgebung dargestellt |
| [Gray](#Gray) | Das Objekt wird mit grauer Farbgebung dargestellt |
| [LightGray](#LightGray) | Das Objekt wird mit hellgrauer Farbgebung dargestellt |
| [InverseGray](#InverseGray) | Das Objekt wird mit invertierter grauer Farbgebung dargestellt |
| [GrayWhite](#GrayWhite) | Das Objekt wird mit grauer und weißer Farbgebung dargestellt |
| [BlackGray](#BlackGray) | Das Objekt wird mit schwarzer und grauer Farbgebung dargestellt |
| [BlackWhite](#BlackWhite) | Das Objekt wird mit schwarzer und weißer Farbgebung dargestellt |
| [Black](#Black) | Das Objekt wird nur mit schwarzer Farbgebung dargestellt |
| [White](#White) | Das Objekt wird mit weißer Farbgebung dargestellt |
| [Hidden](#Hidden) | Das Objekt wird nicht dargestellt |
### NotDefined {#NotDefined}
```
public static final byte NotDefined
```

Die Eigenschaft ist nicht gesetzt

### Color {#Color}
```
public static final byte Color
```

Das Objekt wird mit normaler Farbgebung dargestellt

### Automatic {#Automatic}
```
public static final byte Automatic
```

Das Objekt wird mit automatischer Farbgebung dargestellt

### Gray {#Gray}
```
public static final byte Gray
```

Das Objekt wird mit grauer Farbgebung dargestellt

### LightGray {#LightGray}
```
public static final byte LightGray
```

Das Objekt wird mit hellgrauer Farbgebung dargestellt

### InverseGray {#InverseGray}
```
public static final byte InverseGray
```

Das Objekt wird mit invertierter grauer Farbgebung dargestellt

### GrayWhite {#GrayWhite}
```
public static final byte GrayWhite
```

Das Objekt wird mit grauer und weißer Farbgebung dargestellt

### BlackGray {#BlackGray}
```
public static final byte BlackGray
```

Das Objekt wird mit schwarzer und grauer Farbgebung dargestellt

### BlackWhite {#BlackWhite}
```
public static final byte BlackWhite
```

Das Objekt wird mit schwarzer und weißer Farbgebung dargestellt

### Black {#Black}
```
public static final byte Black
```

Das Objekt wird nur mit schwarzer Farbgebung dargestellt

### White {#White}
```
public static final byte White
```

Das Objekt wird mit weißer Farbgebung dargestellt

### Hidden {#Hidden}
```
public static final byte Hidden
```

Das Objekt wird nicht dargestellt