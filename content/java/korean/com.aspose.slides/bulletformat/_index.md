---
title: BulletFormat
second_title: Aspose.Slides Java API 레퍼런스
description: 단락 글머리 기호 서식 속성을 나타냅니다.
type: docs
url: /ko/com.aspose.slides/bulletformat/
---
**Inheritance:**  
상속:, [com.aspose.slides.PVIObject](../../com.aspose.slides/pviobject)

**All Implemented Interfaces:**  
구현된 모든 인터페이스:, [com.aspose.slides.IBulletFormat](../../com.aspose.slides/ibulletformat)  
```
public final class BulletFormat extends PVIObject implements IBulletFormat
```

단락 글머리 기호 서식 속성을 나타냅니다.
## Methods

| Method | Description |
| --- | --- |
| [getType()](#getType--) | 단락에 대한 상속이 없는 글머리 기호 유형을 반환하거나 설정합니다. |
| [setType(byte value)](#setType-byte-) | 단락에 대한 상속이 없는 글머리 기호 유형을 반환하거나 설정합니다. |
| [getChar()](#getChar--) | 단락에 대한 상속이 없는 글머리 기호 문자를 반환하거나 설정합니다. |
| [setChar(char value)](#setChar-char-) | 단락에 대한 상속이 없는 글머리 기호 문자를 반환하거나 설정합니다. |
| [getFont()](#getFont--) | 단락에 대한 상속이 없는 글머리 기호 글꼴을 반환하거나 설정합니다. |
| [setFont(IFontData value)](#setFont-com.aspose.slides.IFontData-) | 단락에 대한 상속이 없는 글머리 기호 글꼴을 반환하거나 설정합니다. |
| [getHeight()](#getHeight--) | 단락에 대한 상속이 없는 글머리 기호 높이를 반환하거나 설정합니다. |
| [setHeight(float value)](#setHeight-float-) | 단락에 대한 상속이 없는 글머리 기호 높이를 반환하거나 설정합니다. |
| [getColor()](#getColor--) | 단락에 대한 상속이 없는 글머리 기호의 색상 형식을 반환합니다. |
| [getNumberedBulletStartWith()](#getNumberedBulletStartWith--) | 단락에 대한 상속이 없는 번호 매기기 글머리 기호 그룹에 사용되는 첫 번째 번호를 반환하거나 설정합니다. |
| [setNumberedBulletStartWith(short value)](#setNumberedBulletStartWith-short-) | 단락에 대한 상속이 없는 번호 매기기 글머리 기호 그룹에 사용되는 첫 번째 번호를 반환하거나 설정합니다. |
| [getNumberedBulletStyle()](#getNumberedBulletStyle--) | 단락에 대한 상속이 없는 번호 매기기 글머리 기호의 스타일을 반환하거나 설정합니다. |
| [setNumberedBulletStyle(byte value)](#setNumberedBulletStyle-byte-) | 단락에 대한 상속이 없는 번호 매기기 글머리 기호의 스타일을 반환하거나 설정합니다. |
| [isBulletHardColor()](#isBulletHardColor--) | 글머리 기호가 자체 색상을 가지고 있는지 또는 단락의 첫 번째 구절에서 상속받는지 여부를 판단합니다. |
| [setBulletHardColor(byte value)](#setBulletHardColor-byte-) | 글머리 기호가 자체 색상을 가지고 있는지 또는 단락의 첫 번째 구절에서 상속받는지 여부를 판단합니다. |
| [isBulletHardFont()](#isBulletHardFont--) | 글머리 기호가 자체 글꼴을 가지고 있는지 또는 단락의 첫 번째 구절에서 상속받는지 여부를 판단합니다. |
| [setBulletHardFont(byte value)](#setBulletHardFont-byte-) | 글머리 기호가 자체 글꼴을 가지고 있는지 또는 단락의 첫 번째 구절에서 상속받는지 여부를 판단합니다. |
| [getPicture()](#getPicture--) | 단락에 대한 상속이 없는 글머리 기호로 사용되는 그림을 반환합니다. |
| [applyDefaultParagraphIndentsShifts()](#applyDefaultParagraphIndentsShifts--) | 글머리 기호가 활성화된 경우(예: PowerPoint에서 단락 글머리 기호/번호 매기기를 활성화하면) 효과적인 단락 Indent와 MarginLeft에 대해 기본값이 아닌 이동을 설정합니다. |
| [getEffective()](#getEffective--) | 상속이 적용된 유효한 글머리 기호 서식 데이터를 가져옵니다. |
| [getVersion()](#getVersion--) |  |
### getType() {#getType--}
```
public final byte getType()
```

단락에 대한 상속이 없는 글머리 기호 유형을 반환하거나 설정합니다. 읽기/쓰기 [BulletType](../../com.aspose.slides/bullettype).

**Returns:**  
byte
### setType(byte value) {#setType-byte-}
```
public final void setType(byte value)
```

단락에 대한 상속이 없는 글머리 기호 유형을 반환하거나 설정합니다. 읽기/쓰기 [BulletType](../../com.aspose.slides/bullettype).

**Parameters:**  
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | byte |  |
### getChar() {#getChar--}
```
public final char getChar()
```

단락에 대한 상속이 없는 글머리 기호 문자를 반환하거나 설정합니다. 읽기/쓰기  char .

**Returns:**  
char
### setChar(char value) {#setChar-char-}
```
public final void setChar(char value)
```

단락에 대한 상속이 없는 글머리 기호 문자를 반환하거나 설정합니다. 읽기/쓰기  char .

**Parameters:**  
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | char |  |
### getFont() {#getFont--}
```
public final IFontData getFont()
```

단락에 대한 상속이 없는 글머리 기호 글꼴을 반환하거나 설정합니다. 읽기/쓰기 [IFontData](../../com.aspose.slides/ifontdata).

**Returns:**  
[IFontData](../../com.aspose.slides/ifontdata)
### setFont(IFontData value) {#setFont-com.aspose.slides.IFontData-}
```
public final void setFont(IFontData value)
```

단락에 대한 상속이 없는 글머리 기호 글꼴을 반환하거나 설정합니다. 읽기/쓰기 [IFontData](../../com.aspose.slides/ifontdata).

**Parameters:**  
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | [IFontData](../../com.aspose.slides/ifontdata) |  |
### getHeight() {#getHeight--}
```
public final float getHeight()
```

단락에 대한 상속이 없는 글머리 기호 높이를 반환하거나 설정합니다. Float.NaN 값은 글머리 기호가 단락의 첫 번째 구절에서 높이를 상속받는다는 것을 의미합니다. 읽기/쓰기  float .

--------------------

음수 높이 값은 높이가 포인트 단위로 지정된다는 의미이며, 양수 값은 주변 텍스트의 백분율로 지정된다는 의미입니다.

**Returns:**  
float
### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```

단락에 대한 상속이 없는 글머리 기호 높이를 반환하거나 설정합니다. Float.NaN 값은 글머리 기호가 단락의 첫 번째 구절에서 높이를 상속받는다는 것을 의미합니다. 읽기/쓰기  float .

--------------------

음수 높이 값은 높이가 포인트 단위로 지정된다는 의미이며, 양수 값은 주변 텍스트의 백분율로 지정된다는 의미입니다.

**Parameters:**  
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | float |  |
### getColor() {#getColor--}
```
public final IColorFormat getColor()
```

단락에 대한 상속이 없는 글머리 기호의 색상 형식을 반환합니다. 읽기 전용 [IColorFormat](../../com.aspose.slides/icolorformat).

**Returns:**  
[IColorFormat](../../com.aspose.slides/icolorformat)
### getNumberedBulletStartWith() {#getNumberedBulletStartWith--}
```
public final short getNumberedBulletStartWith()
```

단락에 대한 상속이 없는 번호 매기기 글머리 기호 그룹에 사용되는 첫 번째 번호를 반환하거나 설정합니다. 읽기/쓰기  short .

**Returns:**  
short
### setNumberedBulletStartWith(short value) {#setNumberedBulletStartWith-short-}
```
public final void setNumberedBulletStartWith(short value)
```

단락에 대한 상속이 없는 번호 매기기 글머리 기호 그룹에 사용되는 첫 번째 번호를 반환하거나 설정합니다. 읽기/쓰기  short .

**Parameters:**  
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | short |  |
### getNumberedBulletStyle() {#getNumberedBulletStyle--}
```
public final byte getNumberedBulletStyle()
```

단락에 대한 상속이 없는 번호 매기기 글머리 기호의 스타일을 반환하거나 설정합니다. 읽기/쓰기 [NumberedBulletStyle](../../com.aspose.slides/numberedbulletstyle).

**Returns:**  
byte
### setNumberedBulletStyle(byte value) {#setNumberedBulletStyle-byte-}
```
public final void setNumberedBulletStyle(byte value)
```

단락에 대한 상속이 없는 번호 매기기 글머리 기호의 스타일을 반환하거나 설정합니다. 읽기/쓰기 [NumberedBulletStyle](../../com.aspose.slides/numberedbulletstyle).

**Parameters:**  
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | byte |  |
### isBulletHardColor() {#isBulletHardColor--}
```
public final byte isBulletHardColor()
```

글머리 기호가 자체 색상을 가지고 있는지 또는 단락의 첫 번째 구절에서 상속받는지 여부를 판단합니다. **NullableBool.True**이면 자체 색상을 가지고, **NullableBool.False**이면 첫 번째 구절에서 색상을 상속받습니다. 읽기/쓰기 [NullableBool](../../com.aspose.slides/nullablebool).

**Returns:**  
byte
### setBulletHardColor(byte value) {#setBulletHardColor-byte-}
```
public final void setBulletHardColor(byte value)
```

글머리 기호가 자체 색상을 가지고 있는지 또는 단락의 첫 번째 구절에서 상속받는지 여부를 판단합니다. **NullableBool.True**이면 자체 색상을 가지고, **NullableBool.False**이면 첫 번째 구절에서 색상을 상속받습니다. 읽기/쓰기 [NullableBool](../../com.aspose.slides/nullablebool).

**Parameters:**  
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | byte |  |
### isBulletHardFont() {#isBulletHardFont--}
```
public final byte isBulletHardFont()
```

글머리 기호가 자체 글꼴을 가지고 있는지 또는 단락의 첫 번째 구절에서 상속받는지 여부를 판단합니다. **NullableBool.True**이면 자체 글꼴을 가지고, **NullableBool.False**이면 첫 번째 구절에서 글꼴을 상속받습니다. 읽기/쓰기 [NullableBool](../../com.aspose.slides/nullablebool).

**Returns:**  
byte
### setBulletHardFont(byte value) {#setBulletHardFont-byte-}
```
public final void setBulletHardFont(byte value)
```

글머리 기호가 자체 글꼴을 가지고 있는지 또는 단락의 첫 번째 구절에서 상속받는지 여부를 판단합니다. **NullableBool.True**이면 자체 글꼴을 가지고, **NullableBool.False**이면 첫 번째 구절에서 글꼴을 상속받습니다. 읽기/쓰기 [NullableBool](../../com.aspose.slides/nullablebool).

**Parameters:**  
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | byte |  |
### getPicture() {#getPicture--}
```
public final ISlidesPicture getPicture()
```

단락에 대한 상속이 없는 글머리 기호로 사용되는 그림을 반환합니다. 읽기 전용 [ISlidesPicture](../../com.aspose.slides/islidespicture).

**Returns:**  
[ISlidesPicture](../../com.aspose.slides/islidespicture)
### applyDefaultParagraphIndentsShifts() {#applyDefaultParagraphIndentsShifts--}
```
public final void applyDefaultParagraphIndentsShifts()
```

글머리 기호가 활성화된 경우(예: PowerPoint에서 단락 글머리 기호/번호 매기기를 활성화하면) 효과적인 단락 Indent와 MarginLeft에 대해 기본값이 아닌 이동을 설정합니다. 글머리 기호가 비활성화된 경우 단락 Indent와 MarginLeft를 초기화합니다(예: PowerPoint에서 단락 글머리 기호/번호 매기기를 비활성화하면). 이동은 현재 글머리 기호 컨텍스트—IBulletFormat.Type, .NumberedBulletStyle 및 첫 번째 구절의 FontHeight—를 기준으로 적용됩니다. 비제로 이동은 현재 단락의 유효한 Indent와 MarginLeft에 적용되어 결과 값을 로컬 값으로 만듭니다.
### getEffective() {#getEffective--}
```
public final IBulletFormatEffectiveData getEffective()
```

상속이 적용된 유효한 글머리 기호 서식 데이터를 가져옵니다.

--------------------

> ```
> This example demonstrates getting some effective bullet format properties.
>  
>  Presentation pres = new Presentation("MyPresentation.pptx");
>  try {
>      IAutoShape shape = (IAutoShape)pres.getSlides().get_Item(0).getShapes().get_Item(0);
>      IBulletFormatEffectiveData effectiveBulletFormat = shape.getTextFrame().getParagraphs().get_Item(0).getParagraphFormat().getBullet().getEffective();
>      System.out.println("Bullet type: " + effectiveBulletFormat.getType());
>      if (effectiveBulletFormat.getType() == BulletType.Numbered)
>      {
>          System.out.println("Numbered style: " + effectiveBulletFormat.getNumberedBulletStyle());
>          System.out.println("Starting number: " + effectiveBulletFormat.getNumberedBulletStartWith());
>      }
>  } finally {
>      if (pres != null) pres.dispose();
>  }
> ```


**Returns:**  
[IBulletFormatEffectiveData](../../com.aspose.slides/ibulletformateffectivedata) - A [IBulletFormatEffectiveData](../../com.aspose.slides/ibulletformateffectivedata).
### getVersion() {#getVersion--}
```
public long getVersion()
```

버전. 읽기 전용 long.

**Returns:**  
long