---
title: PieSplitType
second_title: Aspose.Slides for Java API 레퍼런스
description: 파이-인-파이 또는 바-인-바 차트에서 두 번째 파이 또는 바에 대한 분할 점 유형을 나타냅니다.
type: docs
url: /ko/com.aspose.slides/piesplittype/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class PieSplitType extends System.Enum
```

파이-인-파이 또는 바-인-바 차트에서 두 번째 파이 또는 바에 대한 분할 점 유형을 나타냅니다.
## 필드

| 필드 | 설명 |
| --- | --- |
| [Default](#Default) | 이 차트 유형에 대한 기본 메커니즘을 사용하여 데이터 포인트를 분할하도록 지정합니다. |
| [Custom](#Custom) | 사용자 정의 분할 값에 따라 파이와 두 번째 차트 사이에 데이터 포인트를 분할하도록 지정합니다. |
| [ByPercentage](#ByPercentage) | 분할 위치 비율보다 작은 비율을 가진 포인트를 두 번째 차트에 배치하여 파이와 두 번째 차트 사이에 데이터 포인트를 분할하도록 지정합니다. |
| [ByPos](#ByPos) | 데이터 포인트의 마지막 분할 위치를 두 번째 차트에 배치하여 파이와 두 번째 차트 사이에 데이터 포인트를 분할하도록 지정합니다. |
| [ByValue](#ByValue) | 분할 위치보다 작은 값을 가진 데이터 포인트를 두 번째 차트에 배치하여 파이와 두 번째 차트 사이에 데이터 포인트를 분할하도록 지정합니다. |
### 기본 {#Default}
```
public static final int Default
```

이 차트 유형에 대한 기본 메커니즘을 사용하여 데이터 포인트를 분할하도록 지정합니다.

### 사용자 지정 {#Custom}
```
public static final int Custom
```

사용자 정의 분할 값에 따라 파이와 두 번째 차트 사이에 데이터 포인트를 분할하도록 지정합니다.

### 백분율 기준 {#ByPercentage}
```
public static final int ByPercentage
```

분할 위치 비율보다 작은 비율을 가진 포인트를 두 번째 차트에 배치하여 파이와 두 번째 차트 사이에 데이터 포인트를 분할하도록 지정합니다.

### 위치 기준 {#ByPos}
```
public static final int ByPos
```

데이터 포인트의 마지막 분할 위치를 두 번째 차트에 배치하여 파이와 두 번째 차트 사이에 데이터 포인트를 분할하도록 지정합니다.

### 값 기준 {#ByValue}
```
public static final int ByValue
```

분할 위치보다 작은 값을 가진 데이터 포인트를 두 번째 차트에 배치하여 파이와 두 번째 차트 사이에 데이터 포인트를 분할하도록 지정합니다.