---
title: LayoutTargetType
second_title: Aspose.Slides for Java API 레퍼런스
description: 플롯 영역의 레이아웃이 수동으로 정의된 경우, 이 속성은 축 및 축 레이블을 포함하지 않는 내부 또는 축 및 축 레이블을 포함하는 외부 중 어느 쪽으로 플롯 영역을 레이아웃할지를 지정합니다.
type: docs
url: /ko/com.aspose.slides/layouttargettype/
---
**상속:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class LayoutTargetType extends System.Enum
```

플롯 영역의 레이아웃이 수동으로 정의된 경우, 이 속성은 플롯 영역을 내부(축 및 축 레이블을 포함하지 않음)로 레이아웃할지 외부(축 및 축 레이블을 포함)로 레이아웃할지를 지정합니다.
## 필드

| 필드 | 설명 |
| --- | --- |
| [Inner](#Inner) | 플롯 영역 크기가 플롯 영역의 크기를 결정하도록 지정하며, 눈금표시와 축 레이블은 포함하지 않습니다. |
| [Outer](#Outer) | 플롯 영역 크기가 플롯 영역, 눈금표시 및 축 레이블의 크기를 결정하도록 지정합니다. |
### 내부 {#Inner}
```
public static final int Inner
```


플롯 영역 크기가 플롯 영역의 크기를 결정하도록 지정하며, 눈금표시와 축 레이블은 포함하지 않습니다.

### 외부 {#Outer}
```
public static final int Outer
```


플롯 영역 크기가 플롯 영역, 눈금표시 및 축 레이블의 크기를 결정하도록 지정합니다.