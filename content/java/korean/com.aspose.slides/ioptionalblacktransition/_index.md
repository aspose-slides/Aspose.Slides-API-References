---
title: IOptionalBlackTransition
second_title: Aspose.Slides for Java API 레퍼런스
description: 선택적인 검은색 슬라이드 전환 효과.
type: docs
url: /ko/com.aspose.slides/ioptionalblacktransition/
---
**모든 구현된 인터페이스:**
[com.aspose.slides.ITransitionValueBase](../../com.aspose.slides/itransitionvaluebase)
```
public interface IOptionalBlackTransition extends ITransitionValueBase
```

선택적인 검은색 슬라이드 전환 효과.
## 메서드

| 메서드 | 설명 |
| --- | --- |
| [getFromBlack()](#getFromBlack--) | 이 속성은 전환이 검은 화면에서 시작되는지 여부를 지정합니다(그런 다음 새로운 슬라이드를 검은 화면 위로 전환합니다). |
| [setFromBlack(boolean value)](#setFromBlack-boolean-) | 이 속성은 전환이 검은 화면에서 시작되는지 여부를 지정합니다(그런 다음 새로운 슬라이드를 검은 화면 위로 전환합니다). |
### getFromBlack() {#getFromBlack--}
```
public abstract boolean getFromBlack()
```


이 속성은 전환이 검은 화면에서 시작되는지 여부를 지정합니다(그런 다음 새로운 슬라이드를 검은 화면 위로 전환합니다). 읽기/쓰기 boolean.

**반환:**
boolean
### setFromBlack(boolean value) {#setFromBlack-boolean-}
```
public abstract void setFromBlack(boolean value)
```


이 속성은 전환이 검은 화면에서 시작되는지 여부를 지정합니다(그런 다음 새로운 슬라이드를 검은 화면 위로 전환합니다). 읽기/쓰기 boolean.

**매개변수:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| value | boolean |  |