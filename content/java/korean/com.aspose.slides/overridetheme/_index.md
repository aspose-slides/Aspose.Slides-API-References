---
title: OverrideTheme
second_title: Aspose.Slides Java API 레퍼런스
description: 오버라이드 테마를 나타냅니다.
type: docs
url: /ko/com.aspose.slides/overridetheme/
---
**상속:**  
java.lang.Object, [com.aspose.slides.Theme](../../com.aspose.slides/theme)

**구현된 모든 인터페이스:**  
[com.aspose.slides.IOverrideTheme](../../com.aspose.slides/ioverridetheme)  
```
public final class OverrideTheme extends Theme implements IOverrideTheme
```

오버라이드 테마를 나타냅니다.

## 메서드

| 메서드 | 설명 |
| --- | --- |
| [initColorScheme()](#initColorScheme--) | 새 객체를 사용하여 InheritedTheme의 ColorScheme을 재정의하기 위해 ColorScheme을 초기화합니다. |
| [initColorSchemeFrom(IColorScheme colorScheme)](#initColorSchemeFrom-com.aspose.slides.IColorScheme-) | 새 객체를 사용하여 InheritedTheme의 ColorScheme을 재정의하기 위해 ColorScheme을 초기화합니다. |
| [initColorSchemeFromInherited()](#initColorSchemeFromInherited--) | 새 객체를 사용하여 InheritedTheme의 ColorScheme을 재정의하기 위해 ColorScheme을 초기화합니다. |
| [initFontScheme()](#initFontScheme--) | 새 객체를 사용하여 InheritedTheme의 FontScheme을 재정의하기 위해 FontScheme을 초기화합니다. |
| [initFontSchemeFrom(IFontScheme fontScheme)](#initFontSchemeFrom-com.aspose.slides.IFontScheme-) | 새 객체를 사용하여 InheritedTheme의 FontScheme을 재정의하기 위해 FontScheme을 초기화합니다. |
| [initFontSchemeFromInherited()](#initFontSchemeFromInherited--) | 새 객체를 사용하여 InheritedTheme의 FontScheme을 재정의하기 위해 FontScheme을 초기화합니다. |
| [initFormatScheme()](#initFormatScheme--) | 새 객체를 사용하여 InheritedTheme의 FormatScheme을 재정의하기 위해 FormatScheme을 초기화합니다. |
| [initFormatSchemeFrom(IFormatScheme formatScheme)](#initFormatSchemeFrom-com.aspose.slides.IFormatScheme-) | 새 객체를 사용하여 InheritedTheme의 FormatScheme을 재정의하기 위해 FormatScheme을 초기화합니다. |
| [initFormatSchemeFromInherited()](#initFormatSchemeFromInherited--) | 새 객체를 사용하여 InheritedTheme의 FormatScheme을 재정의하기 위해 FormatScheme을 초기화합니다. |
| [getColorScheme()](#getColorScheme--) | 색상 스키마를 반환합니다. |
| [getFontScheme()](#getFontScheme--) | 글꼴 스키마를 반환합니다. |
| [getFormatScheme()](#getFormatScheme--) | 형태 포맷 스키마를 반환합니다. |
| [isEmpty()](#isEmpty--) | true 값은 ColorScheme, FontScheme, FormatScheme가 null이며 이 테마 객체를 사용한 모든 오버라이드가 비활성화됨을 의미합니다. |
| [clear()](#clear--) | ColorScheme, FontScheme, FormatScheme를 null로 설정하여 이 테마 객체를 사용한 모든 오버라이드를 비활성화합니다. |
| [getVersion()](#getVersion--) |  |

### initColorScheme() {#initColorScheme--}
```
public final void initColorScheme()
```

새 객체를 사용하여 InheritedTheme의 ColorScheme을 재정의하기 위해 ColorScheme을 초기화합니다.

### initColorSchemeFrom(IColorScheme colorScheme) {#initColorSchemeFrom-com.aspose.slides.IColorScheme-}
```
public final void initColorSchemeFrom(IColorScheme colorScheme)
```

새 객체를 사용하여 InheritedTheme의 ColorScheme을 재정의하기 위해 ColorScheme을 초기화합니다.

**매개변수:**
| 매개변수 | 타입 | 설명 |
| --- | --- | --- |
| colorScheme | [IColorScheme](../../com.aspose.slides/icolorscheme) | 초기화에 사용할 데이터. |

### initColorSchemeFromInherited() {#initColorSchemeFromInherited--}
```
public final void initColorSchemeFromInherited()
```

새 객체를 사용하여 InheritedTheme의 ColorScheme을 재정의하기 위해 ColorScheme을 초기화합니다. 그리고 새 객체의 데이터를 InheritedTheme의 ColorScheme 데이터로 초기화합니다.

### initFontScheme() {#initFontScheme--}
```
public final void initFontScheme()
```

새 객체를 사용하여 InheritedTheme의 FontScheme을 재정의하기 위해 FontScheme을 초기화합니다.

### initFontSchemeFrom(IFontScheme fontScheme) {#initFontSchemeFrom-com.aspose.slides.IFontScheme-}
```
public final void initFontSchemeFrom(IFontScheme fontScheme)
```

새 객체를 사용하여 InheritedTheme의 FontScheme을 재정의하기 위해 FontScheme을 초기화합니다.

**매개변수:**
| 매개변수 | 타입 | 설명 |
| --- | --- | --- |
| fontScheme | [IFontScheme](../../com.aspose.slides/ifontscheme) | 초기화에 사용할 데이터. |

### initFontSchemeFromInherited() {#initFontSchemeFromInherited--}
```
public final void initFontSchemeFromInherited()
```

새 객체를 사용하여 InheritedTheme의 FontScheme을 재정의하기 위해 FontScheme을 초기화합니다. 그리고 새 객체의 데이터를 InheritedTheme의 FontScheme 데이터로 초기화합니다.

### initFormatScheme() {#initFormatScheme--}
```
public final void initFormatScheme()
```

새 객체를 사용하여 InheritedTheme의 FormatScheme을 재정의하기 위해 FormatScheme을 초기화합니다.

### initFormatSchemeFrom(IFormatScheme formatScheme) {#initFormatSchemeFrom-com.aspose.slides.IFormatScheme-}
```
public final void initFormatSchemeFrom(IFormatScheme formatScheme)
```

새 객체를 사용하여 InheritedTheme의 FormatScheme을 재정의하기 위해 FormatScheme을 초기화합니다.

**매개변수:**
| 매개변수 | 타입 | 설명 |
| --- | --- | --- |
| formatScheme | [IFormatScheme](../../com.aspose.slides/iformatscheme) | 초기화에 사용할 데이터. |

### initFormatSchemeFromInherited() {#initFormatSchemeFromInherited--}
```
public final void initFormatSchemeFromInherited()
```

새 객체를 사용하여 InheritedTheme의 FormatScheme을 재정의하기 위해 FormatScheme을 초기화합니다. 그리고 새 객체의 데이터를 InheritedTheme의 FormatScheme 데이터로 초기화합니다.

### getColorScheme() {#getColorScheme--}
```
public IColorScheme getColorScheme()
```

색상 스키마를 반환합니다. 읽기 전용 [IColorScheme](../../com.aspose.slides/icolorscheme).

**반환값:**
[IColorScheme](../../com.aspose.slides/icolorscheme)

### getFontScheme() {#getFontScheme--}
```
public IFontScheme getFontScheme()
```

글꼴 스키마를 반환합니다. 읽기 전용 [IFontScheme](../../com.aspose.slides/ifontscheme).

**반환값:**
[IFontScheme](../../com.aspose.slides/ifontscheme)

### getFormatScheme() {#getFormatScheme--}
```
public IFormatScheme getFormatScheme()
```

형태 포맷 스키마를 반환합니다. 읽기 전용 [IFormatScheme](../../com.aspose.slides/iformatscheme).

**반환값:**
[IFormatScheme](../../com.aspose.slides/iformatscheme)

### isEmpty() {#isEmpty--}
```
public final boolean isEmpty()
```

true 값은 ColorScheme, FontScheme, FormatScheme가 null이며 이 테마 객체를 사용한 모든 오버라이드가 비활성화됨을 의미합니다. 읽기 전용 부울.

**반환값:**
boolean

### clear() {#clear--}
```
public final void clear()
```

ColorScheme, FontScheme, FormatScheme를 null로 설정하여 이 테마 객체를 사용한 모든 오버라이드를 비활성화합니다.

### getVersion() {#getVersion--}
```
public long getVersion()
```

버전. 읽기 전용 long.

**반환값:**
long