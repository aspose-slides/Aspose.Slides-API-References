---
title: CrossesType
second_title: Aspose.Slides for Java API 레퍼런스
description: 축이 교차하는 위치를 결정합니다.
type: docs
url: /ko/com.aspose.slides/crossestype/
---
**상속:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class CrossesType extends System.Enum
```

축이 교차하는 위치를 결정합니다.
## 필드

| 필드 | 설명 |
| --- | --- |
| [AxisCrossesAtZero](#AxisCrossesAtZero) | 카테고리 축은 값 축의 0점(가능한 경우)이나 최소값(최소값이 0보다 큰 경우) 또는 최대값(최대값이 0보다 작은 경우)에서 교차합니다. |
| [Maximum](#Maximum) | 축은 최대값에서 교차합니다. |
| [Custom](#Custom) | CrossAt 속성에서 사용자 정의 값 |
### AxisCrossesAtZero {#AxisCrossesAtZero}
```
public static final int AxisCrossesAtZero
```

카테고리 축은 값 축의 0점(가능한 경우)이나 최소값(최소값이 0보다 큰 경우) 또는 최대값(최대값이 0보다 작은 경우)에서 교차합니다.

### Maximum {#Maximum}
```
public static final int Maximum
```

축은 최대값에서 교차합니다.

### Custom {#Custom}
```
public static final int Custom
```

CrossAt 속성에서 사용자 정의 값