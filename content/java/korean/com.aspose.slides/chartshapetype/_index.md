---
title: ChartShapeType
second_title: Aspose.Slides for Java API 레퍼런스
description: 차트의 모양을 나타냅니다.
type: docs
url: /ko/com.aspose.slides/chartshapetype/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class ChartShapeType extends System.Enum
```

차트의 모양을 나타냅니다.
## Fields

| 필드 | 설명 |
| --- | --- |
| [NotDefined](#NotDefined) | 차트 모양이 정의되지 않았습니다. |
| [Box](#Box) | 차트가 박스 형태로 그려지도록 지정합니다. |
| [Cone](#Cone) | 차트가 원뿔 모양으로 그려지도록 지정합니다. 원뿔의 바닥은 바닥에 놓이고, 꼭지는 데이터 마커의 위쪽에 놓입니다. |
| [ConeToMax](#ConeToMax) | 차트가 잘린 원뿔 형태로 그려지도록 지정합니다. 원뿔의 꼭지는 최대 데이터 값이 됩니다. |
| [Cylinder](#Cylinder) | 차트가 원통 형태로 그려지도록 지정합니다. |
| [Pyramid](#Pyramid) | 차트가 직사각형 피라미드 형태로 그려지도록 지정합니다. 피라미드의 바닥은 바닥에 놓이고, 꼭지는 데이터 마커의 위쪽에 놓입니다. |
| [PyramidToMaximum](#PyramidToMaximum) | 차트가 잘린 원뿔 형태로 그려지도록 지정합니다. 원뿔의 꼭지는 최대 데이터 값이 됩니다. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```

차트 모양이 정의되지 않았습니다.

### Box {#Box}
```
public static final int Box
```

차트가 박스 형태로 그려지도록 지정합니다.

### Cone {#Cone}
```
public static final int Cone
```

차트가 원뿔 모양으로 그려지도록 지정합니다. 원뿔의 바닥은 바닥에 놓이고, 꼭지는 데이터 마커의 위쪽에 놓입니다.

### ConeToMax {#ConeToMax}
```
public static final int ConeToMax
```

차트가 잘린 원뿔 형태로 그려지도록 지정합니다. 원뿔의 꼭지는 최대 데이터 값이 됩니다.

### Cylinder {#Cylinder}
```
public static final int Cylinder
```

차트가 원통 형태로 그려지도록 지정합니다.

### Pyramid {#Pyramid}
```
public static final int Pyramid
```

차트가 직사각형 피라미드 형태로 그려지도록 지정합니다. 피라미드의 바닥은 바닥에 놓이고, 꼭지는 데이터 마커의 위쪽에 놓입니다.

### PyramidToMaximum {#PyramidToMaximum}
```
public static final int PyramidToMaximum
```

차트가 잘린 원뿔 형태로 그려지도록 지정합니다. 원뿔의 꼭지는 최대 데이터 값이 됩니다.