---
title: NormalViewRestoredProperties
second_title: Aspose.Slides for Java API 參考
description: 指定幻燈片區域的尺寸（當子項為 restoredTop 時為寬度，當子項為 restoredLeft 時為高度），在一般檢視中，當區域為可變的還原大小（既非最小化亦非最大化）時。
type: docs
url: /zh-hant/com.aspose.slides/normalviewrestoredproperties/
---
**繼承：**
java.lang.Object

**所有已實作的介面：**
[com.aspose.slides.INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)
```
public class NormalViewRestoredProperties implements INormalViewRestoredProperties
```

指定幻燈片區域的尺寸（在還原大小（既非最小化也非最大化）且屬於一般檢視時，子項為 restoredTop 時為寬度，子項為 restoredLeft 時為高度）。

## 方法

| 方法 | 說明 |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | 指定幻燈片區域的大小（當子項為 RestoredTop 時為寬度，當子項為 RestoredLeft 時為高度）。 |
| [setDimensionSize(float value)](#setDimensionSize-float-) | 指定幻燈片區域的大小（當子項為 RestoredTop 時為寬度，當子項為 RestoredLeft 時為高度）。 |
| [getAutoAdjust()](#getAutoAdjust--) | 指定在調整包含視圖的視窗大小時，側內容區域的尺寸是否應補償新尺寸。讀寫 boolean。 |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | 指定在調整包含視圖的視窗大小時，側內容區域的尺寸是否應補償新尺寸。讀寫 boolean。 |

### getDimensionSize() {#getDimensionSize--}
```
public final float getDimensionSize()
```

指定幻燈片區域的大小（當子項為 RestoredTop 時為寬度，當子項為 RestoredLeft 時為高度）。讀寫 float。

**返回：**
float

### setDimensionSize(float value) {#setDimensionSize-float-}
```
public final void setDimensionSize(float value)
```

指定幻燈片區域的大小（當子項為 RestoredTop 時為寬度，當子項為 RestoredLeft 時為高度）。讀寫 float。

**參數：**
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public final boolean getAutoAdjust()
```

指定在調整包含視圖的視窗大小時，側內容區域的尺寸是否應補償新尺寸。讀寫 boolean。

**返回：**
boolean

### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public final void setAutoAdjust(boolean value)
```

指定在調整包含視圖的視窗大小時，側內容區域的尺寸是否應補償新尺寸。讀寫 boolean。

**參數：**
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | boolean |  |