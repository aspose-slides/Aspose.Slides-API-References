---
title: ScaleEffect
second_title: Aspose.Slides Java API 參考
description: 表示動畫縮放效果。
type: docs
url: /zh-hant/com.aspose.slides/scaleeffect/
---
**繼承:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**所有已實作的介面:**
[com.aspose.slides.IScaleEffect](../../com.aspose.slides/iscaleeffect)
```
public class ScaleEffect extends Behavior implements IScaleEffect
```

表示動畫縮放效果。
## 建構子

| 建構子 | 描述 |
| --- | --- |
| [ScaleEffect()](#ScaleEffect--) |  |
## 方法

| 方法 | 描述 |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | 判斷內容是否應該縮放。 |
| [setZoomContent(byte value)](#setZoomContent-byte-) | 判斷內容是否應該縮放。 |
| [getFrom()](#getFrom--) | 指定動畫起始的 x/y 座標（以百分比表示）。 |
| [setFrom(Point2D.Float value)](#setFrom-java.awt.geom.Point2D.Float-) | 指定動畫起始的 x/y 座標（以百分比表示）。 |
| [getTo()](#getTo--) | 指定動畫縮放效果的目標位置（以百分比表示）。 |
| [setTo(Point2D.Float value)](#setTo-java.awt.geom.Point2D.Float-) | 指定動畫縮放效果的目標位置（以百分比表示）。 |
| [getBy()](#getBy--) | 描述動畫的相對偏移值（以百分比表示）。 |
| [setBy(Point2D.Float value)](#setBy-java.awt.geom.Point2D.Float-) | 描述動畫的相對偏移值（以百分比表示）。 |
### ScaleEffect() {#ScaleEffect--}
```
public ScaleEffect()
```

### getZoomContent() {#getZoomContent--}
```
public final byte getZoomContent()
```

判斷內容是否應該縮放。讀/寫 [NullableBool](../../com.aspose.slides/nullablebool)。

**傳回:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public final void setZoomContent(byte value)
```

判斷內容是否應該縮放。讀/寫 [NullableBool](../../com.aspose.slides/nullablebool)。

**參數:**
| 參數 | 型別 | 描述 |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public final Point2D.Float getFrom()
```

指定動畫起始的 x/y 座標（以百分比表示）。讀/寫 java.awt.geom.Point2D.Float。

**傳回:**
java.awt.geom.Point2D.Float
### setFrom(Point2D.Float value) {#setFrom-java.awt.geom.Point2D.Float-}
```
public final void setFrom(Point2D.Float value)
```

指定動畫起始的 x/y 座標（以百分比表示）。讀/寫 java.awt.geom.Point2D.Float。

**參數:**
| 參數 | 型別 | 描述 |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getTo() {#getTo--}
```
public final Point2D.Float getTo()
```

指定動畫縮放效果的目標位置（以百分比表示）。讀/寫 java.awt.geom.Point2D.Float。

**傳回:**
java.awt.geom.Point2D.Float
### setTo(Point2D.Float value) {#setTo-java.awt.geom.Point2D.Float-}
```
public final void setTo(Point2D.Float value)
```

指定動畫縮放效果的目標位置（以百分比表示）。讀/寫 java.awt.geom.Point2D.Float。

**參數:**
| 參數 | 型別 | 描述 |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getBy() {#getBy--}
```
public final Point2D.Float getBy()
```

描述動畫的相對偏移值（以百分比表示）。讀/寫 java.awt.geom.Point2D.Float。

**傳回:**
java.awt.geom.Point2D.Float
### setBy(Point2D.Float value) {#setBy-java.awt.geom.Point2D.Float-}
```
public final void setBy(Point2D.Float value)
```

描述動畫的相對偏移值（以百分比表示）。讀/寫 java.awt.geom.Point2D.Float。

**參數:**
| 參數 | 型別 | 描述 |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |