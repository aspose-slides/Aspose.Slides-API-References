---
title: ILayoutable
second_title: Aspose.Slides for Java API 參考
description: 指定圖表元素的精確位置。
type: docs
url: /zh-hant/com.aspose.slides/ilayoutable/
---
**所有已實作的介面：**
[com.aspose.slides.IChartComponent](../../com.aspose.slides/ichartcomponent)
```
public interface ILayoutable extends IChartComponent
```

指定圖表元素的精確位置。
## 方法

| 方法 | 描述 |
| --- | --- |
| [getX()](#getX--) | 指定圖表元素的 x 位置（左側），作為圖表寬度的比例。 |
| [setX(float value)](#setX-float-) | 指定圖表元素的 x 位置（左側），作為圖表寬度的比例。 |
| [getY()](#getY--) | 指定圖表元素的頂部位置，作為圖表高度的比例。 |
| [setY(float value)](#setY-float-) | 指定圖表元素的頂部位置，作為圖表高度的比例。 |
| [getWidth()](#getWidth--) | 指定圖表元素的寬度，作為圖表寬度的比例。 |
| [setWidth(float value)](#setWidth-float-) | 指定圖表元素的寬度，作為圖表寬度的比例。 |
| [getHeight()](#getHeight--) | 指定圖表元素的高度，作為圖表高度的比例。 |
| [setHeight(float value)](#setHeight-float-) | 指定圖表元素的高度，作為圖表高度的比例。 |
| [getRight()](#getRight--) | 取得圖表元素的右側位置，作為圖表寬度的比例。 |
| [getBottom()](#getBottom--) | 取得圖表元素的頂部位置，作為圖表高度的比例。 |
### getX() {#getX--}
```
public abstract float getX()
```


指定圖表元素的 x 位置（左側），作為圖表寬度的比例。可讀寫 float。

**返回:**
float
### setX(float value) {#setX-float-}
```
public abstract void setX(float value)
```


指定圖表元素的 x 位置（左側），作為圖表寬度的比例。可讀寫 float。

**參數:**
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | float |  |

### getY() {#getY--}
```
public abstract float getY()
```


指定圖表元素的頂部位置，作為圖表高度的比例。可讀寫 float。

**返回:**
float
### setY(float value) {#setY-float-}
```
public abstract void setY(float value)
```


指定圖表元素的頂部位置，作為圖表高度的比例。可讀寫 float。

**參數:**
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | float |  |

### getWidth() {#getWidth--}
```
public abstract float getWidth()
```


指定圖表元素的寬度，作為圖表寬度的比例。可讀寫 float。

**返回:**
float
### setWidth(float value) {#setWidth-float-}
```
public abstract void setWidth(float value)
```


指定圖表元素的寬度，作為圖表寬度的比例。可讀寫 float。

**參數:**
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | float |  |

### getHeight() {#getHeight--}
```
public abstract float getHeight()
```


指定圖表元素的高度，作為圖表高度的比例。可讀寫 float。

**返回:**
float
### setHeight(float value) {#setHeight-float-}
```
public abstract void setHeight(float value)
```


指定圖表元素的高度，作為圖表高度的比例。可讀寫 float。

**參數:**
| 參數 | 類型 | 說明 |
| --- | --- | --- |
| value | float |  |

### getRight() {#getRight--}
```
public abstract float getRight()
```


取得圖表元素的右側位置，作為圖表寬度的比例。唯讀 float。

**返回:**
float
### getBottom() {#getBottom--}
```
public abstract float getBottom()
```


取得圖表元素的頂部位置，作為圖表高度的比例。唯讀 float。

**返回:**
float