---
title: HyperlinkActionType
second_title: Aspose.Slides dla Java – referencja API
description: Reprezentuje rodzaj akcji hiperłącza.
type: docs
url: /pl/com.aspose.slides/hyperlinkactiontype/
---
**Dziedziczenie:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class HyperlinkActionType extends System.Enum
```

Reprezentuje typ akcji hiperłącza.
## Pól

| Pole | Opis |
| --- | --- |
| [Unknown](#Unknown) | Nie rozpoznany typ akcji. |
| [NoAction](#NoAction) | Brak akcji. |
| [Hyperlink](#Hyperlink) | Zwykłe hiperłącze. |
| [JumpFirstSlide](#JumpFirstSlide) | Przejście do pierwszego slajdu prezentacji. |
| [JumpPreviousSlide](#JumpPreviousSlide) | Przejście do poprzedniego slajdu. |
| [JumpNextSlide](#JumpNextSlide) | Przejście do następnego slajdu. |
| [JumpLastSlide](#JumpLastSlide) | Przejście do ostatniego slajdu prezentacji. |
| [JumpEndShow](#JumpEndShow) | Przejście do końca pokazu slajdów. |
| [JumpLastViewedSlide](#JumpLastViewedSlide) | Przejście do ostatnio wyświetlanego slajdu. |
| [JumpSpecificSlide](#JumpSpecificSlide) | Przejście do określonego slajdu, wskazanego przez właściwość ([IHyperlink.getTargetSlide](../../com.aspose.slides/ihyperlink\#getTargetSlide)). |
| [StartCustomSlideShow](#StartCustomSlideShow) | Rozpoczęcie niestandardowego pokazu slajdów. |
| [OpenFile](#OpenFile) | Otwarcie odwoływanego pliku. |
| [OpenPresentation](#OpenPresentation) | Otwarcie odwoływanej prezentacji. |
| [StartStopMedia](#StartStopMedia) | Rozpocznij/zatrzymaj odtwarzanie pliku multimedialnego. |
| [StartMacro](#StartMacro) | Rozpoczęcie wykonywania skryptu makra. |
| [StartProgram](#StartProgram) | Uruchomienie programu. |
### Unknown {#Unknown}
```
public static final int Unknown
```

Nie rozpoznany typ akcji.

### NoAction {#NoAction}
```
public static final int NoAction
```

Brak akcji.

### Hyperlink {#Hyperlink}
```
public static final int Hyperlink
```

Zwykłe hiperłącze.

### JumpFirstSlide {#JumpFirstSlide}
```
public static final int JumpFirstSlide
```

Przejście do pierwszego slajdu prezentacji.

### JumpPreviousSlide {#JumpPreviousSlide}
```
public static final int JumpPreviousSlide
```

Przejście do poprzedniego slajdu.

### JumpNextSlide {#JumpNextSlide}
```
public static final int JumpNextSlide
```

Przejście do następnego slajdu.

### JumpLastSlide {#JumpLastSlide}
```
public static final int JumpLastSlide
```

Przejście do ostatniego slajdu prezentacji.

### JumpEndShow {#JumpEndShow}
```
public static final int JumpEndShow
```

Przejście do końca pokazu slajdów.

### JumpLastViewedSlide {#JumpLastViewedSlide}
```
public static final int JumpLastViewedSlide
```

Przejście do ostatnio wyświetlanego slajdu.

### JumpSpecificSlide {#JumpSpecificSlide}
```
public static final int JumpSpecificSlide
```

Przejście do określonego slajdu, wskazanego przez właściwość ([IHyperlink.getTargetSlide](../../com.aspose.slides/ihyperlink\#getTargetSlide)).

### StartCustomSlideShow {#StartCustomSlideShow}
```
public static final int StartCustomSlideShow
```

Rozpoczęcie niestandardowego pokazu slajdów.

### OpenFile {#OpenFile}
```
public static final int OpenFile
```

Otwarcie odwoływanego pliku.

### OpenPresentation {#OpenPresentation}
```
public static final int OpenPresentation
```

Otwarcie odwoływanej prezentacji.

### StartStopMedia {#StartStopMedia}
```
public static final int StartStopMedia
```

Rozpocznij/zatrzymaj odtwarzanie pliku multimedialnego.

### StartMacro {#StartMacro}
```
public static final int StartMacro
```

Rozpoczęcie wykonywania skryptu makra.

### StartProgram {#StartProgram}
```
public static final int StartProgram
```

Uruchomienie programu.