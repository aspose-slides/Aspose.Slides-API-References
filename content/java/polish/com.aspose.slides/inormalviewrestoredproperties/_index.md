---
title: INormalViewRestoredProperties
second_title: Aspose.Slides for Java Referencja API
description: Określa rozmiar regionu slajdu (szerokość, gdy jest elementem podrzędnym RestoredTop, wysokość, gdy jest elementem podrzędnym RestoredLeft) w normalnym widoku, gdy region ma zmienny przywrócony rozmiar (nie jest zminimalizowany ani zmaksymalizowany).
type: docs
url: /pl/com.aspose.slides/inormalviewrestoredproperties/
---```
public interface INormalViewRestoredProperties
```

Określa rozmiar regionu slajdu ((szerokość, gdy jest elementem podrzędnym restoredTop, wysokość, gdy jest elementem podrzędnym restoredLeft) w normalnym widoku, gdy region ma zmienny przywrócony rozmiar (nie jest zminimalizowany ani zmaksymalizowany)).
## Metody

| Metoda | Opis |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | Określa rozmiar regionu slajdu (szerokość, gdy jest elementem podrzędnym RestoredTop, wysokość, gdy jest elementem podrzędnym RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | Określa rozmiar regionu slajdu (szerokość, gdy jest elementem podrzędnym RestoredTop, wysokość, gdy jest elementem podrzędnym RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | Określa, czy rozmiar bocznego regionu zawartości powinien kompensować nowy rozmiar przy zmianie rozmiaru okna zawierającego widok w aplikacji Odczyt/zapis boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | Określa, czy rozmiar bocznego regionu zawartości powinien kompensować nowy rozmiar przy zmianie rozmiaru okna zawierającego widok w aplikacji Odczyt/zapis boolean. |
### getDimensionSize() {#getDimensionSize--}
```
public abstract float getDimensionSize()
```

Określa rozmiar regionu slajdu (szerokość, gdy jest elementem podrzędnym RestoredTop, wysokość, gdy jest elementem podrzędnym RestoredLeft). Odczyt/zapis float.

**Zwraca:**
float
### setDimensionSize(float value) {#setDimensionSize-float-}
```
public abstract void setDimensionSize(float value)
```

Określa rozmiar regionu slajdu (szerokość, gdy jest elementem podrzędnym RestoredTop, wysokość, gdy jest elementem podrzędnym RestoredLeft). Odczyt/zapis float.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public abstract boolean getAutoAdjust()
```

Określa, czy rozmiar bocznego regionu zawartości powinien kompensować nowy rozmiar przy zmianie rozmiaru okna zawierającego widok w aplikacji Odczyt/zapis boolean.

**Zwraca:**
boolean
### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public abstract void setAutoAdjust(boolean value)
```

Określa, czy rozmiar bocznego regionu zawartości powinien kompensować nowy rozmiar przy zmianie rozmiaru okna zawierającego widok w aplikacji Odczyt/zapis boolean.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | boolean |  |