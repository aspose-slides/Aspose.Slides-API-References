---
title: IScaleEffect
second_title: Referencja API Aspose.Slides dla Javy
description: Reprezentuje efekt skalowania animacji.
type: docs
url: /pl/com.aspose.slides/iscaleeffect/
---
**Wszystkie implementowane interfejsy:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IScaleEffect extends IBehavior
```

Reprezentuje efekt skalowania animacji.
## Metody

| Metoda | Opis |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Określa, czy zawartość powinna być przybliżona. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Określa, czy zawartość powinna być przybliżona. |
| [getFrom()](#getFrom--) | Określa współrzędną x/y, od której rozpocząć animację (w procentach). |
| [setFrom(Point2D.Float value)](#setFrom-java.awt.geom.Point2D.Float-) | Określa współrzędną x/y, od której rozpocząć animację (w procentach). |
| [getTo()](#getTo--) | Określa docelową lokalizację efektu skalowania animacji (w procentach). |
| [setTo(Point2D.Float value)](#setTo-java.awt.geom.Point2D.Float-) | Określa docelową lokalizację efektu skalowania animacji (w procentach). |
| [getBy()](#getBy--) | Opisuje względną wartość przesunięcia dla animacji (w procentach). |
| [setBy(Point2D.Float value)](#setBy-java.awt.geom.Point2D.Float-) | Opisuje względną wartość przesunięcia dla animacji (w procentach). |
### getZoomContent() {#getZoomContent--}
```
public abstract byte getZoomContent()
```

Określa, czy zawartość powinna być przybliżona. Odczyt/zapis [NullableBool](../../com.aspose.slides/nullablebool).

**Zwraca:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public abstract void setZoomContent(byte value)
```

Określa, czy zawartość powinna być przybliżona. Odczyt/zapis [NullableBool](../../com.aspose.slides/nullablebool).

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public abstract Point2D.Float getFrom()
```

Określa współrzędną x/y, od której rozpocząć animację (w procentach). Odczyt/zapis java.awt.geom.Point2D.Float.

**Zwraca:**
java.awt.geom.Point2D.Float
### setFrom(Point2D.Float value) {#setFrom-java.awt.geom.Point2D.Float-}
```
public abstract void setFrom(Point2D.Float value)
```

Określa współrzędną x/y, od której rozpocząć animację (w procentach). Odczyt/zapis java.awt.geom.Point2D.Float.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getTo() {#getTo--}
```
public abstract Point2D.Float getTo()
```

Określa docelową lokalizację efektu skalowania animacji (w procentach). Odczyt/zapis java.awt.geom.Point2D.Float.

**Zwraca:**
java.awt.geom.Point2D.Float
### setTo(Point2D.Float value) {#setTo-java.awt.geom.Point2D.Float-}
```
public abstract void setTo(Point2D.Float value)
```

Określa docelową lokalizację efektu skalowania animacji (w procentach). Odczyt/zapis java.awt.geom.Point2D.Float.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getBy() {#getBy--}
```
public abstract Point2D.Float getBy()
```

Opisuje względną wartość przesunięcia dla animacji (w procentach). Odczyt/zapis java.awt.geom.Point2D.Float.

**Zwraca:**
java.awt.geom.Point2D.Float
### setBy(Point2D.Float value) {#setBy-java.awt.geom.Point2D.Float-}
```
public abstract void setBy(Point2D.Float value)
```

Opisuje względną wartość przesunięcia dla animacji (w procentach). Odczyt/zapis java.awt.geom.Point2D.Float.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |