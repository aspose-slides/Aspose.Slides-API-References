---
title: ITiming
second_title: Aspose.Slides for Java API Reference
description: Represents animation timing.
type: docs
url: /pl/com.aspose.slides/itiming/
---```
public interface ITiming
```

Reprezentuje synchronizację animacji.
## Metody

| Metoda | Opis |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | Opisuje procentowy wpływ przyspieszającego zachowania trwania. |
| [setAccelerate(float value)](#setAccelerate-float-) | Opisuje procentowy wpływ przyspieszającego zachowania trwania. |
| [getDecelerate()](#getDecelerate--) | Opisuje procentowy wpływ zwalniającego zachowania trwania. |
| [setDecelerate(float value)](#setDecelerate-float-) | Opisuje procentowy wpływ zwalniającego zachowania trwania. |
| [getAutoReverse()](#getAutoReverse--) | Opisuje, czy animacja ma być automatycznie odtwarzana w odwrotnym kierunku po odtworzeniu w kierunku normalnym. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | Opisuje, czy animacja ma być automatycznie odtwarzana w odwrotnym kierunku po odtworzeniu w kierunku normalnym. |
| [getDuration()](#getDuration--) | Opisuje czas trwania efektu animacji. |
| [setDuration(float value)](#setDuration-float-) | Opisuje czas trwania efektu animacji. |
| [getRepeatCount()](#getRepeatCount--) | Opisuje liczbę powtórzeń efektu. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | Opisuje liczbę powtórzeń efektu. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | Ten atrybut określa, czy efekt będzie powtarzany do końca slajdu. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | Ten atrybut określa, czy efekt będzie powtarzany do końca slajdu. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | Ten atrybut określa, czy efekt będzie powtarzany do następnego kliknięcia. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | Ten atrybut określa, czy efekt będzie powtarzany do następnego kliknięcia. |
| [getRepeatDuration()](#getRepeatDuration--) | Opisuje liczbę powtórzeń efektu. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | Opisuje liczbę powtórzeń efektu. |
| [getRestart()](#getRestart--) | Określa, czy efekt ma zostać uruchomiony ponownie po zakończeniu. |
| [setRestart(int value)](#setRestart-int-) | Określa, czy efekt ma zostać uruchomiony ponownie po zakończeniu. |
| [getSpeed()](#getSpeed--) | Określa procent, o jaki przyspieszyć (lub spowolnić) synchronizację. |
| [setSpeed(float value)](#setSpeed-float-) | Określa procent, o jaki przyspieszyć (lub spowolnić) synchronizację. |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | Opisuje opóźnienie po wyzwoleniu. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | Opisuje opóźnienie po wyzwoleniu. |
| [getTriggerType()](#getTriggerType--) | Opisuje typ wyzwalacza. |
| [setTriggerType(int value)](#setTriggerType-int-) | Opisuje typ wyzwalacza. |
| [getRewind()](#getRewind--) | Ten atrybut określa, czy efekt zostanie przewinięty po zakończeniu odtwarzania. |
| [setRewind(boolean value)](#setRewind-boolean-) | Ten atrybut określa, czy efekt zostanie przewinięty po zakończeniu odtwarzania. |
### getAccelerate() {#getAccelerate--}
```
public abstract float getAccelerate()
```

Opisuje procentowy wpływ przyspieszającego zachowania trwania. Odczyt/zapis float.

**Zwraca:**
float
### setAccelerate(float value) {#setAccelerate-float-}
```
public abstract void setAccelerate(float value)
```

Opisuje procentowy wpływ przyspieszającego zachowania trwania. Odczyt/zapis float.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | float |  |
### getDecelerate() {#getDecelerate--}
```
public abstract float getDecelerate()
```

Opisuje procentowy wpływ zwalniającego zachowania trwania. Odczyt/zapis float.

**Zwraca:**
float
### setDecelerate(float value) {#setDecelerate-float-}
```
public abstract void setDecelerate(float value)
```

Opisuje procentowy wpływ zwalniającego zachowania trwania. Odczyt/zapis float.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | float |  |
### getAutoReverse() {#getAutoReverse--}
```
public abstract boolean getAutoReverse()
```

Opisuje, czy animacja ma być automatycznie odtwarzana w odwrotnym kierunku po odtworzeniu w kierunku normalnym. Odczyt/zapis boolean.

**Zwraca:**
boolean
### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public abstract void setAutoReverse(boolean value)
```

Opisuje, czy animacja ma być automatycznie odtwarzana w odwrotnym kierunku po odtworzeniu w kierunku normalnym. Odczyt/zapis boolean.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | boolean |  |
### getDuration() {#getDuration--}
```
public abstract float getDuration()
```

Opisuje czas trwania efektu animacji. Odczyt/zapis float.

**Zwraca:**
float
### setDuration(float value) {#setDuration-float-}
```
public abstract void setDuration(float value)
```

Opisuje czas trwania efektu animacji. Odczyt/zapis float.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | float |  |
### getRepeatCount() {#getRepeatCount--}
```
public abstract float getRepeatCount()
```

Opisuje liczbę powtórzeń efektu. Odczyt/zapis float.

**Zwraca:**
float
### setRepeatCount(float value) {#setRepeatCount-float-}
```
public abstract void setRepeatCount(float value)
```

Opisuje liczbę powtórzeń efektu. Odczyt/zapis float.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | float |  |
### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public abstract boolean getRepeatUntilEndSlide()
```

Ten atrybut określa, czy efekt będzie powtarzany do końca slajdu. Odczyt/zapis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Pobierz sekwencję efektów dla pierwszego slajdu
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Pobierz pierwszy efekt głównej sekwencji.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Zmień synchronizację/powtórzenie efektu na "Do końca slajdu"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Zwraca:**
boolean
### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public abstract void setRepeatUntilEndSlide(boolean value)
```

Ten atrybut określa, czy efekt będzie powtarzany do końca slajdu. Odczyt/zapis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Pobierz sekwencję efektów dla pierwszego slajdu
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Pobierz pierwszy efekt głównej sekwencji.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Zmień synchronizację/powtórzenie efektu na "Do końca slajdu"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | boolean |  |
### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public abstract boolean getRepeatUntilNextClick()
```

Ten atrybut określa, czy efekt będzie powtarzany do następnego kliknięcia. Odczyt/zapis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Pobierz sekwencję efektów dla pierwszego slajdu
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Pobierz pierwszy efekt głównej sekwencji.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Zmień synchronizację/powtórzenie efektu na "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Zwraca:**
boolean
### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public abstract void setRepeatUntilNextClick(boolean value)
```

Ten atrybut określa, czy efekt będzie powtarzany do następnego kliknięcia. Odczyt/zapis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Pobierz sekwencję efektów dla pierwszego slajdu
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Pobierz pierwszy efekt głównej sekwencji.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Zmień synchronizację/powtórzenie efektu na "Do następnego kliknięcia"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | boolean |  |
### getRepeatDuration() {#getRepeatDuration--}
```
public abstract float getRepeatDuration()
```

Opisuje liczbę powtórzeń efektu. Odczyt/zapis float.

**Zwraca:**
float
### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public abstract void setRepeatDuration(float value)
```

Opisuje liczbę powtórzeń efektu. Odczyt/zapis float.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | float |  |
### getRestart() {#getRestart--}
```
public abstract int getRestart()
```

Określa, czy efekt ma zostać uruchomiony ponownie po zakończeniu. Odczyt/zapis [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Zwraca:**
int
### setRestart(int value) {#setRestart-int-}
```
public abstract void setRestart(int value)
```

Określa, czy efekt ma zostać uruchomiony ponownie po zakończeniu. Odczyt/zapis [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | int |  |
### getSpeed() {#getSpeed--}
```
public abstract float getSpeed()
```

Określa procent, o jaki przyspieszyć (lub spowolnić) synchronizację. Odczyt/zapis float.

**Zwraca:**
float
### setSpeed(float value) {#setSpeed-float-}
```
public abstract void setSpeed(float value)
```

Określa procent, o jaki przyspieszyć (lub spowolnić) synchronizację. Odczyt/zapis float.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | float |  |
### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public abstract float getTriggerDelayTime()
```

Opisuje opóźnienie po wyzwoleniu. Odczyt/zapis float.

**Zwraca:**
float
### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public abstract void setTriggerDelayTime(float value)
```

Opisuje opóźnienie po wyzwoleniu. Odczyt/zapis float.

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | float |  |
### getTriggerType() {#getTriggerType--}
```
public abstract int getTriggerType()
```

Opisuje typ wyzwalacza. Odczyt/zapis [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Zwraca:**
int
### setTriggerType(int value) {#setTriggerType-int-}
```
public abstract void setTriggerType(int value)
```

Opisuje typ wyzwalacza. Odczyt/zapis [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | int |  |
### getRewind() {#getRewind--}
```
public abstract boolean getRewind()
```

Ten atrybut określa, czy efekt zostanie przewinięty po zakończeniu odtwarzania. Odczyt/zapis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Pobierz sekwencję efektów dla pierwszego slajdu
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Pobierz pierwszy efekt głównej sekwencji.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Włącz Timing/Rewind efektu.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Zwraca:**
boolean
### setRewind(boolean value) {#setRewind-boolean-}
```
public abstract void setRewind(boolean value)
```

Ten atrybut określa, czy efekt zostanie przewinięty po zakończeniu odtwarzania. Odczyt/zapis boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Pobierz sekwencję efektów dla pierwszego slajdu
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Pobierz pierwszy efekt głównej sekwencji.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Włącz Timing/Rewind efektu.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parametry:**
| Parametr | Typ | Opis |
| --- | --- | --- |
| value | boolean |  |