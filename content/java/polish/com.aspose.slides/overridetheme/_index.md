---
title: OverrideTheme
second_title: Referencja API Aspose.Slides dla Javy
description: Reprezentuje nadpisujący motyw.
type: docs
url: /pl/com.aspose.slides/overridetheme/
---
**Inheritance:**  
java.lang.Object, [com.aspose.slides.Theme](../../com.aspose.slides/theme)

**All Implemented Interfaces:**  
[com.aspose.slides.IOverrideTheme](../../com.aspose.slides/ioverridetheme)  
```
public final class OverrideTheme extends Theme implements IOverrideTheme
```

Reprezentuje nadpisujący motyw.

## Metody

| Metoda | Opis |
| --- | --- |
| [initColorScheme()](#initColorScheme--) | Zainicjuj ColorScheme nowym obiektem, aby nadpisać ColorScheme z InheritedTheme. |
| [initColorSchemeFrom(IColorScheme colorScheme)](#initColorSchemeFrom-com.aspose.slides.IColorScheme-) | Zainicjuj ColorScheme nowym obiektem, aby nadpisać ColorScheme z InheritedTheme. |
| [initColorSchemeFromInherited()](#initColorSchemeFromInherited--) | Zainicjuj ColorScheme nowym obiektem, aby nadpisać ColorScheme z InheritedTheme. |
| [initFontScheme()](#initFontScheme--) | Zainicjuj FontScheme nowym obiektem, aby nadpisać FontScheme z InheritedTheme. |
| [initFontSchemeFrom(IFontScheme fontScheme)](#initFontSchemeFrom-com.aspose.slides.IFontScheme-) | Zainicjuj FontScheme nowym obiektem, aby nadpisać FontScheme z InheritedTheme. |
| [initFontSchemeFromInherited()](#initFontSchemeFromInherited--) | Zainicjuj FontScheme nowym obiektem, aby nadpisać FontScheme z InheritedTheme. |
| [initFormatScheme()](#initFormatScheme--) | Zainicjuj FormatScheme nowym obiektem, aby nadpisać FormatScheme z InheritedTheme. |
| [initFormatSchemeFrom(IFormatScheme formatScheme)](#initFormatSchemeFrom-com.aspose.slides.IFormatScheme-) | Zainicjuj FormatScheme nowym obiektem, aby nadpisać FormatScheme z InheritedTheme. |
| [initFormatSchemeFromInherited()](#initFormatSchemeFromInherited--) | Zainicjuj FormatScheme nowym obiektem, aby nadpisać FormatScheme z InheritedTheme. |
| [getColorScheme()](#getColorScheme--) | Zwraca schemę kolorów. |
| [getFontScheme()](#getFontScheme--) | Zwraca schemę czcionek. |
| [getFormatScheme()](#getFormatScheme--) | Zwraca schemę formatu kształtu. |
| [isEmpty()](#isEmpty--) | Wartość true oznacza, że ColorScheme, FontScheme, FormatScheme jest nullem i wszelkie nadpisywanie przy użyciu tego obiektu motywu jest wyłączone. |
| [clear()](#clear--) | Ustaw ColorScheme, FontScheme, FormatScheme na null, aby wyłączyć wszelkie nadpisywanie przy użyciu tego obiektu motywu. |
| [getVersion()](#getVersion--) |  |

### initColorScheme() {#initColorScheme--}
```
public final void initColorScheme()
```

Zainicjuj ColorScheme nowym obiektem, aby nadpisać ColorScheme z InheritedTheme.

### initColorSchemeFrom(IColorScheme colorScheme) {#initColorSchemeFrom-com.aspose.slides.IColorScheme-}
```
public final void initColorSchemeFrom(IColorScheme colorScheme)
```

Zainicjuj ColorScheme nowym obiektem, aby nadpisać ColorScheme z InheritedTheme.

**Parameters:**  
| Parameter | Type | Description |
| --- | --- | --- |
| colorScheme | [IColorScheme](../../com.aspose.slides/icolorscheme) | Dane do zainicjowania. |

### initColorSchemeFromInherited() {#initColorSchemeFromInherited--}
```
public final void initColorSchemeFromInherited()
```

Zainicjuj ColorScheme nowym obiektem, aby nadpisać ColorScheme z InheritedTheme. I zainicjuj dane tego nowego obiektu danymi ColorScheme z InheritedTheme.

### initFontScheme() {#initFontScheme--}
```
public final void initFontScheme()
```

Zainicjuj FontScheme nowym obiektem, aby nadpisać FontScheme z InheritedTheme.

### initFontSchemeFrom(IFontScheme fontScheme) {#initFontSchemeFrom-com.aspose.slides.IFontScheme-}
```
public final void initFontSchemeFrom(IFontScheme fontScheme)
```

Zainicjuj FontScheme nowym obiektem, aby nadpisać FontScheme z InheritedTheme.

**Parameters:**  
| Parameter | Type | Description |
| --- | --- | --- |
| fontScheme | [IFontScheme](../../com.aspose.slides/ifontscheme) | Dane do zainicjowania. |

### initFontSchemeFromInherited() {#initFontSchemeFromInherited--}
```
public final void initFontSchemeFromInherited()
```

Zainicjuj FontScheme nowym obiektem, aby nadpisać FontScheme z InheritedTheme. I zainicjuj dane tego nowego obiektu danymi FontScheme z InheritedTheme.

### initFormatScheme() {#initFormatScheme--}
```
public final void initFormatScheme()
```

Zainicjuj FormatScheme nowym obiektem, aby nadpisać FormatScheme z InheritedTheme.

### initFormatSchemeFrom(IFormatScheme formatScheme) {#initFormatSchemeFrom-com.aspose.slides.IFormatScheme-}
```
public final void initFormatSchemeFrom(IFormatScheme formatScheme)
```

Zainicjuj FormatScheme nowym obiektem, aby nadpisać FormatScheme z InheritedTheme.

**Parameters:**  
| Parameter | Type | Description |
| --- | --- | --- |
| formatScheme | [IFormatScheme](../../com.aspose.slides/iformatscheme) | Dane do zainicjowania. |

### initFormatSchemeFromInherited() {#initFormatSchemeFromInherited--}
```
public final void initFormatSchemeFromInherited()
```

Zainicjuj FormatScheme nowym obiektem, aby nadpisać FormatScheme z InheritedTheme. I zainicjuj dane tego nowego obiektu danymi FormatScheme z InheritedTheme.

### getColorScheme() {#getColorScheme--}
```
public IColorScheme getColorScheme()
```

Zwraca schemę kolorów. Tylko do odczytu [IColorScheme](../../com.aspose.slides/icolorscheme).

**Returns:**  
[IColorScheme](../../com.aspose.slides/icolorscheme)

### getFontScheme() {#getFontScheme--}
```
public IFontScheme getFontScheme()
```

Zwraca schemę czcionek. Tylko do odczytu [IFontScheme](../../com.aspose.slides/ifontscheme).

**Returns:**  
[IFontScheme](../../com.aspose.slides/ifontscheme)

### getFormatScheme() {#getFormatScheme--}
```
public IFormatScheme getFormatScheme()
```

Zwraca schemę formatu kształtu. Tylko do odczytu [IFormatScheme](../../com.aspose.slides/iformatscheme).

**Returns:**  
[IFormatScheme](../../com.aspose.slides/iformatscheme)

### isEmpty() {#isEmpty--}
```
public final boolean isEmpty()
```

Wartość true oznacza, że ColorScheme, FontScheme, FormatScheme jest nullem i wszelkie nadpisywanie przy użyciu tego obiektu motywu jest wyłączone. Tylko do odczytu boolean.

**Returns:**  
boolean

### clear() {#clear--}
```
public final void clear()
```

Ustaw ColorScheme, FontScheme, FormatScheme na null, aby wyłączyć wszelkie nadpisywanie przy użyciu tego obiektu motywu.

### getVersion() {#getVersion--}
```
public long getVersion()
```

Wersja. Tylko do odczytu long.

**Returns:**  
long