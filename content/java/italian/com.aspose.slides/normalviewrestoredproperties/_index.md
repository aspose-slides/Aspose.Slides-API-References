---
title: NormalViewRestoredProperties
second_title: Riferimento API di Aspose.Slides per Java
description: Specifica la dimensione dell'area della diapositiva, larghezza quando è figlio di restoredTop, altezza quando è figlio di restoredLeft, della vista normale, quando l'area ha una dimensione restaurata variabile (né ridotta né massimizzata).
type: docs
url: /it/com.aspose.slides/normalviewrestoredproperties/
---
**Eredità:**
java.lang.Object

**Tutte le interfacce implementate:**
[com.aspose.slides.INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)
```
public class NormalViewRestoredProperties implements INormalViewRestoredProperties
```

Specifica le dimensioni dell'area della diapositiva ((larghezza quando è figlio di restoredTop, altezza quando è figlio di restoredLeft) della vista normale, quando l'area è di dimensione variabile restaurata (né ridotta né massimizzata)).
## Metodi

| Metodo | Descrizione |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | Specifica la dimensione dell'area della diapositiva (larghezza quando è figlio di RestoredTop, altezza quando è figlio di RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | Specifica la dimensione dell'area della diapositiva (larghezza quando è figlio di RestoredTop, altezza quando è figlio di RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | Specifica se la dimensione dell'area del contenuto laterale deve compensare la nuova dimensione quando si ridimensiona la finestra che contiene la vista all'interno dell'applicazione Lettura/Scrittura boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | Specifica se la dimensione dell'area del contenuto laterale deve compensare la nuova dimensione quando si ridimensiona la finestra che contiene la vista all'interno dell'applicazione Lettura/Scrittura boolean. |
### getDimensionSize() {#getDimensionSize--}
```
public final float getDimensionSize()
```

Specifica la dimensione dell'area della diapositiva (larghezza quando è figlio di RestoredTop, altezza quando è figlio di RestoredLeft). Lettura/Scrittura float.

**Restituisce:**
float
### setDimensionSize(float value) {#setDimensionSize-float-}
```
public final void setDimensionSize(float value)
```

Specifica la dimensione dell'area della diapositiva (larghezza quando è figlio di RestoredTop, altezza quando è figlio di RestoredLeft). Lettura/Scrittura float.

**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public final boolean getAutoAdjust()
```

Specifica se la dimensione dell'area del contenuto laterale deve compensare la nuova dimensione quando si ridimensiona la finestra che contiene la vista all'interno dell'applicazione Lettura/Scrittura boolean.

**Restituisce:**
boolean
### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public final void setAutoAdjust(boolean value)
```

Specifica se la dimensione dell'area del contenuto laterale deve compensare la nuova dimensione quando si ridimensiona la finestra che contiene la vista all'interno dell'applicazione Lettura/Scrittura boolean.

**Parametri:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| value | boolean |  |