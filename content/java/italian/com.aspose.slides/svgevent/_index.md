---
title: SvgEvent
second_title: Riferimento API di Aspose.Slides per Java
description: Rappresenta le opzioni per la forma SVG.
type: docs
url: /it/com.aspose.slides/svgevent/
---
**Eredità:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class SvgEvent extends System.Enum
```

Rappresenta le opzioni per la forma SVG.
## Campi

| Campo | Descrizione |
| --- | --- |
| [OnFocusIn](#OnFocusIn) | Si verifica quando un elemento riceve il focus, ad esempio quando un testo viene selezionato. |
| [OnFocusOut](#OnFocusOut) | Si verifica quando un elemento perde il focus, ad esempio quando un testo non è più selezionato. |
| [OnActivate](#OnActivate) | Si verifica quando un elemento viene attivato, ad esempio tramite un clic del mouse o una pressione di tasto. |
| [OnClick](#OnClick) | Si verifica quando il pulsante del dispositivo di puntamento viene cliccato su un elemento. |
| [OnMouseDown](#OnMouseDown) | Si verifica quando il pulsante del dispositivo di puntamento viene premuto su un elemento. |
| [OnMouseUp](#OnMouseUp) | Si verifica quando il pulsante del dispositivo di puntamento viene rilasciato su un elemento. |
| [OnMouseOver](#OnMouseOver) | Si verifica quando il dispositivo di puntamento viene spostato su un elemento. |
| [OnMouseMove](#OnMouseMove) | Si verifica quando il dispositivo di puntamento viene mosso mentre è sopra un elemento. |
| [OnMouseOut](#OnMouseOut) | Si verifica quando il dispositivo di puntamento viene spostato fuori da un elemento. |
| [OnLoad](#OnLoad) | Si verifica quando l'agente utente ha completamente analizzato l'elemento e i suoi discendenti e tutte le risorse referenziate, necessarie per renderizzarlo. |
| [OnUnload](#OnUnload) | Si verifica quando l'implementazione DOM rimuove un documento da una finestra o da un frame. |
| [OnAbort](#OnAbort) | Si verifica quando il caricamento della pagina viene interrotto prima che un elemento abbia potuto caricarsi completamente. |
| [OnError](#OnError) | Si verifica quando un elemento non si carica correttamente o quando si verifica un errore durante l'esecuzione di uno script. |
| [OnResize](#OnResize) | Si verifica quando una vista del documento viene ridimensionata. |
| [OnScroll](#OnScroll) | Si verifica quando una vista del documento viene spostata lungo l'asse X, Y o entrambi. |
| [OnZoom](#OnZoom) | Si verifica quando il livello di zoom di una vista del documento viene modificato. |
| [OnBegin](#OnBegin) | Si verifica quando un elemento di animazione inizia. |
| [OnEnd](#OnEnd) | Si verifica quando un elemento di animazione termina. |
| [OnRepeat](#OnRepeat) | Si verifica quando un elemento di animazione si ripete. |
### OnFocusIn {#OnFocusIn}
```
public static final int OnFocusIn
```

Si verifica quando un elemento riceve il focus, ad esempio quando un testo viene selezionato.

### OnFocusOut {#OnFocusOut}
```
public static final int OnFocusOut
```

Si verifica quando un elemento perde il focus, ad esempio quando un testo non è più selezionato.

### OnActivate {#OnActivate}
```
public static final int OnActivate
```

Si verifica quando un elemento viene attivato, ad esempio tramite un clic del mouse o una pressione di tasto.

### OnClick {#OnClick}
```
public static final int OnClick
```

Si verifica quando il pulsante del dispositivo di puntamento viene cliccato su un elemento.

### OnMouseDown {#OnMouseDown}
```
public static final int OnMouseDown
```

Si verifica quando il pulsante del dispositivo di puntamento viene premuto su un elemento.

### OnMouseUp {#OnMouseUp}
```
public static final int OnMouseUp
```

Si verifica quando il pulsante del dispositivo di puntamento viene rilasciato su un elemento.

### OnMouseOver {#OnMouseOver}
```
public static final int OnMouseOver
```

Si verifica quando il dispositivo di puntamento viene spostato su un elemento.

### OnMouseMove {#OnMouseMove}
```
public static final int OnMouseMove
```

Si verifica quando il dispositivo di puntamento viene mosso mentre è sopra un elemento.

### OnMouseOut {#OnMouseOut}
```
public static final int OnMouseOut
```

Si verifica quando il dispositivo di puntamento viene spostato fuori da un elemento.

### OnLoad {#OnLoad}
```
public static final int OnLoad
```

Si verifica quando l'agente utente ha completamente analizzato l'elemento e i suoi discendenti e tutte le risorse referenziate, necessarie per renderizzarlo.

### OnUnload {#OnUnload}
```
public static final int OnUnload
```

Si verifica quando l'implementazione DOM rimuove un documento da una finestra o da un frame. Applicabile solo agli elementi svg più esterni.

### OnAbort {#OnAbort}
```
public static final int OnAbort
```

Si verifica quando il caricamento della pagina viene interrotto prima che un elemento abbia potuto caricarsi completamente.

### OnError {#OnError}
```
public static final int OnError
```

Si verifica quando un elemento non si carica correttamente o quando si verifica un errore durante l'esecuzione di uno script.

### OnResize {#OnResize}
```
public static final int OnResize
```

Si verifica quando una vista del documento viene ridimensionata. Applicabile solo agli elementi svg più esterni.

### OnScroll {#OnScroll}
```
public static final int OnScroll
```

Si verifica quando una vista del documento viene spostata lungo l'asse X, Y o entrambi. Applicabile solo agli elementi svg più esterni.

### OnZoom {#OnZoom}
```
public static final int OnZoom
```

Si verifica quando il livello di zoom di una vista del documento viene modificato. Applicabile solo agli elementi svg più esterni.

### OnBegin {#OnBegin}
```
public static final int OnBegin
```

Si verifica quando un elemento di animazione inizia.

### OnEnd {#OnEnd}
```
public static final int OnEnd
```

Si verifica quando un elemento di animazione termina.

### OnRepeat {#OnRepeat}
```
public static final int OnRepeat
```

Si verifica quando un elemento di animazione si ripete.