---
title: INormalViewRestoredProperties
second_title: Aspose.Slides for Java API Reference
description: Určuje velikost oblasti snímku (šířka, když je podřízený restoredTop, výška, když je podřízený restoredLeft) normálního pohledu, když je oblast proměnné obnovitelné velikosti (ani minimalizovaná, ani maximalizovaná).
type: docs
url: /cs/com.aspose.slides/inormalviewrestoredproperties/
---```
public interface INormalViewRestoredProperties
```

Určuje velikost oblasti snímku ((šířka když je podřízený restoredTop, výška když je podřízený restoredLeft) normálního pohledu, když je oblast proměnné obnovitelné velikosti (ani minimalizovaná, ani maximalizovaná)).
## Metody

| Metoda | Popis |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | Určuje velikost oblasti snímku (šířka když je podřízený RestoredTop, výška když je podřízený RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | Určuje velikost oblasti snímku (šířka když je podřízený RestoredTop, výška když je podřízený RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | Určuje, zda má velikost oblasti postranního obsahu kompenzovat novou velikost při změně velikosti okna obsahujícího pohled v aplikaci. Číst/zapisovat boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | Určuje, zda má velikost oblasti postranního obsahu kompenzovat novou velikost při změně velikosti okna obsahujícího pohled v aplikaci. Číst/zapisovat boolean. |
### getDimensionSize() {#getDimensionSize--}
```
public abstract float getDimensionSize()
```

Určuje velikost oblasti snímku (šířka když je podřízený RestoredTop, výška když je podřízený RestoredLeft). Číst/zapisovat float.

**Vrací:**
float
### setDimensionSize(float value) {#setDimensionSize-float-}
```
public abstract void setDimensionSize(float value)
```

Určuje velikost oblasti snímku (šířka když je podřízený RestoredTop, výška když je podřízený RestoredLeft). Číst/zapisovat float.

**Parametry:**
| Parameter | Typ | Popis |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public abstract boolean getAutoAdjust()
```

Určuje, zda má velikost oblasti postranního obsahu kompenzovat novou velikost při změně velikosti okna obsahujícího pohled v aplikaci. Číst/zapisovat boolean.

**Vrací:**
boolean
### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public abstract void setAutoAdjust(boolean value)
```

Určuje, zda má velikost oblasti postranního obsahu kompenzovat novou velikost při změně velikosti okna obsahujícího pohled v aplikaci. Číst/zapisovat boolean.

**Parametry:**
| Parameter | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |