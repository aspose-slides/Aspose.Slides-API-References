---
title: IScaleEffect
second_title: Aspose.Slides pro Java – referenční dokumentace API
description: Reprezentuje efekt měřítka animace.
type: docs
url: /cs/com.aspose.slides/iscaleeffect/
---
**Všechny implementované rozhraní:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IScaleEffect extends IBehavior
```

Reprezentuje efekt měřítka animace.
## Metody

| Metoda | Popis |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Určuje, zda by měl být obsah zvětšen. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Určuje, zda by měl být obsah zvětšen. |
| [getFrom()](#getFrom--) | Určuje souřadnici x/y, odkud animace začíná (v procentech). |
| [setFrom(Point2D.Float value)](#setFrom-java.awt.geom.Point2D.Float-) | Určuje souřadnici x/y, odkud animace začíná (v procentech). |
| [getTo()](#getTo--) | Určuje cílové umístění pro efekt měřítka animace (v procentech). |
| [setTo(Point2D.Float value)](#setTo-java.awt.geom.Point2D.Float-) | Určuje cílové umístění pro efekt měřítka animace (v procentech). |
| [getBy()](#getBy--) | Popisuje relativní hodnotu posunu pro animaci (v procentech). |
| [setBy(Point2D.Float value)](#setBy-java.awt.geom.Point2D.Float-) | Popisuje relativní hodnotu posunu pro animaci (v procentech). |
### getZoomContent() {#getZoomContent--}
```
public abstract byte getZoomContent()
```

Určuje, zda by měl být obsah zvětšen. Čtení/Zápis [NullableBool](../../com.aspose.slides/nullablebool).

**Vrací:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public abstract void setZoomContent(byte value)
```

Určuje, zda by měl být obsah zvětšen. Čtení/Zápis [NullableBool](../../com.aspose.slides/nullablebool).

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public abstract Point2D.Float getFrom()
```

Určuje souřadnici x/y, odkud animace začíná (v procentech). Čtení/Zápis java.awt.geom.Point2D.Float.

**Vrací:**
java.awt.geom.Point2D.Float
### setFrom(Point2D.Float value) {#setFrom-java.awt.geom.Point2D.Float-}
```
public abstract void setFrom(Point2D.Float value)
```

Určuje souřadnici x/y, odkud animace začíná (v procentech). Čtení/Zápis java.awt.geom.Point2D.Float.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getTo() {#getTo--}
```
public abstract Point2D.Float getTo()
```

Určuje cílové umístění pro efekt měřítka animace (v procentech). Čtení/Zápis java.awt.geom.Point2D.Float.

**Vrací:**
java.awt.geom.Point2D.Float
### setTo(Point2D.Float value) {#setTo-java.awt.geom.Point2D.Float-}
```
public abstract void setTo(Point2D.Float value)
```

Určuje cílové umístění pro efekt měřítka animace (v procentech). Čtení/Zápis java.awt.geom.Point2D.Float.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getBy() {#getBy--}
```
public abstract Point2D.Float getBy()
```

Popisuje relativní hodnotu posunu pro animaci (v procentech). Čtení/Zápis java.awt.geom.Point2D.Float.

**Vrací:**
java.awt.geom.Point2D.Float
### setBy(Point2D.Float value) {#setBy-java.awt.geom.Point2D.Float-}
```
public abstract void setBy(Point2D.Float value)
```

Popisuje relativní hodnotu posunu pro animaci (v procentech). Čtení/Zápis java.awt.geom.Point2D.Float.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |