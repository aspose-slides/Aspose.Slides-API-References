---
title: InterruptionTokenSource
second_title: Aspose.Slides pro Java – reference API
description: Reprezentuje zdroj .
type: docs
url: /cs/com.aspose.slides/interruptiontokensource/
---
**Dědičnost:**
java.lang.Object
```
public class InterruptionTokenSource
```

Reprezentuje zdroj [InterruptionToken](../../com.aspose.slides/interruptiontoken).

## Konstruktory

| Konstruktor | Popis |
| --- | --- |
| [InterruptionTokenSource()](#InterruptionTokenSource--) | Vytvoří nový [InterruptionTokenSource](../../com.aspose.slides/interruptiontokensource). |

## Metody

| Metoda | Popis |
| --- | --- |
| [getToken()](#getToken--) | Vrátí nový token navázaný na tento [InterruptionTokenSource](../../com.aspose.slides/interruptiontokensource). |
| [isInterruptionRequested()](#isInterruptionRequested--) | Vrátí true, pokud bylo požádáno o přerušení, jinak false. |
| [interrupt()](#interrupt--) | Inicializuje požadavek na přerušení. |

### InterruptionTokenSource() {#InterruptionTokenSource--}
```
public InterruptionTokenSource()
```

Vytvoří nový [InterruptionTokenSource](../../com.aspose.slides/interruptiontokensource).

### getToken() {#getToken--}
```
public final InterruptionToken getToken()
```

Vrátí nový token navázaný na tento [InterruptionTokenSource](../../com.aspose.slides/interruptiontokensource).

**Vrací:**
[InterruptionToken](../../com.aspose.slides/interruptiontoken)

### isInterruptionRequested() {#isInterruptionRequested--}
```
public final boolean isInterruptionRequested()
```

Vrátí true, pokud bylo požádáno o přerušení, jinak false.

**Vrací:**
boolean

### interrupt() {#interrupt--}
```
public final void interrupt()
```

Inicializuje požadavek na přerušení.