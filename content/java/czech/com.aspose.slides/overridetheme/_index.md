---
title: OverrideTheme
second_title: Aspose.Slides pro Java API Reference
description: Reprezentuje přepisovací motiv.
type: docs
url: /cs/com.aspose.slides/overridetheme/
---
**Inheritance:**  
java.lang.Object, [com.aspose.slides.Theme](../../com.aspose.slides/theme)

**All Implemented Interfaces:**  
[com.aspose.slides.IOverrideTheme](../../com.aspose.slides/ioverridetheme)  
```
public final class OverrideTheme extends Theme implements IOverrideTheme
```

Represents a overriding theme.

## Metody

| Method | Description |
| --- | --- |
| [initColorScheme()](#initColorScheme--) | Inicializuje ColorScheme novým objektem pro přepisování ColorScheme z InheritedTheme. |
| [initColorSchemeFrom(IColorScheme colorScheme)](#initColorSchemeFrom-com.aspose.slides.IColorScheme-) | Inicializuje ColorScheme novým objektem pro přepisování ColorScheme z InheritedTheme. |
| [initColorSchemeFromInherited()](#initColorSchemeFromInherited--) | Inicializuje ColorScheme novým objektem pro přepisování ColorScheme z InheritedTheme. |
| [initFontScheme()](#initFontScheme--) | Inicializuje FontScheme novým objektem pro přepisování FontScheme z InheritedTheme. |
| [initFontSchemeFrom(IFontScheme fontScheme)](#initFontSchemeFrom-com.aspose.slides.IFontScheme-) | Inicializuje FontScheme novým objektem pro přepisování FontScheme z InheritedTheme. |
| [initFontSchemeFromInherited()](#initFontSchemeFromInherited--) | Inicializuje FontScheme novým objektem pro přepisování FontScheme z InheritedTheme. |
| [initFormatScheme()](#initFormatScheme--) | Inicializuje FormatScheme novým objektem pro přepisování FormatScheme z InheritedTheme. |
| [initFormatSchemeFrom(IFormatScheme formatScheme)](#initFormatSchemeFrom-com.aspose.slides.IFormatScheme-) | Inicializuje FormatScheme novým objektem pro přepisování FormatScheme z InheritedTheme. |
| [initFormatSchemeFromInherited()](#initFormatSchemeFromInherited--) | Inicializuje FormatScheme novým objektem pro přepisování FormatScheme z InheritedTheme. |
| [getColorScheme()](#getColorScheme--) | Vrací schéma barev. |
| [getFontScheme()](#getFontScheme--) | Vrací schéma fontů. |
| [getFormatScheme()](#getFormatScheme--) | Vrací schéma formátu tvaru. |
| [isEmpty()](#isEmpty--) | Hodnota true znamená, že ColorScheme, FontScheme, FormatScheme jsou null a jakékoli přepisování tímto objektem tématu je zakázáno. |
| [clear()](#clear--) | Nastaví ColorScheme, FontScheme, FormatScheme na null, aby zakázalo jakékoli přepisování tímto objektem tématu. |
| [getVersion()](#getVersion--) |  |

### initColorScheme() {#initColorScheme--}
```
public final void initColorScheme()
```

Inicializuje ColorScheme novým objektem pro přepisování ColorScheme z InheritedTheme.

### initColorSchemeFrom(IColorScheme colorScheme) {#initColorSchemeFrom-com.aspose.slides.IColorScheme-}
```
public final void initColorSchemeFrom(IColorScheme colorScheme)
```

Inicializuje ColorScheme novým objektem pro přepisování ColorScheme z InheritedTheme.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| colorScheme | [IColorScheme](../../com.aspose.slides/icolorscheme) | Data pro inicializaci. |

### initColorSchemeFromInherited() {#initColorSchemeFromInherited--}
```
public final void initColorSchemeFromInherited()
```

Inicializuje ColorScheme novým objektem pro přepisování ColorScheme z InheritedTheme. A inicializuje data tohoto nového objektu daty ze ColorScheme z InheritedTheme.

### initFontScheme() {#initFontScheme--}
```
public final void initFontScheme()
```

Inicializuje FontScheme novým objektem pro přepisování FontScheme z InheritedTheme.

### initFontSchemeFrom(IFontScheme fontScheme) {#initFontSchemeFrom-com.aspose.slides.IFontScheme-}
```
public final void initFontSchemeFrom(IFontScheme fontScheme)
```

Inicializuje FontScheme novým objektem pro přepisování FontScheme z InheritedTheme.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| fontScheme | [IFontScheme](../../com.aspose.slides/ifontscheme) | Data pro inicializaci. |

### initFontSchemeFromInherited() {#initFontSchemeFromInherited--}
```
public final void initFontSchemeFromInherited()
```

Inicializuje FontScheme novým objektem pro přepisování FontScheme z InheritedTheme. A inicializuje data tohoto nového objektu daty ze FontScheme z InheritedTheme.

### initFormatScheme() {#initFormatScheme--}
```
public final void initFormatScheme()
```

Inicializuje FormatScheme novým objektem pro přepisování FormatScheme z InheritedTheme.

### initFormatSchemeFrom(IFormatScheme formatScheme) {#initFormatSchemeFrom-com.aspose.slides.IFormatScheme-}
```
public final void initFormatSchemeFrom(IFormatScheme formatScheme)
```

Inicializuje FormatScheme novým objektem pro přepisování FormatScheme z InheritedTheme.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| formatScheme | [IFormatScheme](../../com.aspose.slides/iformatscheme) | Data pro inicializaci. |

### initFormatSchemeFromInherited() {#initFormatSchemeFromInherited--}
```
public final void initFormatSchemeFromInherited()
```

Inicializuje FormatScheme novým objektem pro přepisování FormatScheme z InheritedTheme. A inicializuje data tohoto nového objektu daty ze FormatScheme z InheritedTheme.

### getColorScheme() {#getColorScheme--}
```
public IColorScheme getColorScheme()
```

Vrací schéma barev. Pouze pro čtení [IColorScheme](../../com.aspose.slides/icolorscheme).

**Vrací:**
[IColorScheme](../../com.aspose.slides/icolorscheme)

### getFontScheme() {#getFontScheme--}
```
public IFontScheme getFontScheme()
```

Vrací schéma fontů. Pouze pro čtení [IFontScheme](../../com.aspose.slides/ifontscheme).

**Vrací:**
[IFontScheme](../../com.aspose.slides/ifontscheme)

### getFormatScheme() {#getFormatScheme--}
```
public IFormatScheme getFormatScheme()
```

Vrací schéma formátu tvaru. Pouze pro čtení [IFormatScheme](../../com.aspose.slides/iformatscheme).

**Vrací:**
[IFormatScheme](../../com.aspose.slides/iformatscheme)

### isEmpty() {#isEmpty--}
```
public final boolean isEmpty()
```

Hodnota true znamená, že ColorScheme, FontScheme, FormatScheme jsou null a jakékoli přepisování tímto objektem tématu je zakázáno. Pouze pro čtení boolean.

**Vrací:**
boolean

### clear() {#clear--}
```
public final void clear()
```

Nastaví ColorScheme, FontScheme, FormatScheme na null, aby zakázalo jakékoli přepisování tímto objektem tématu.

### getVersion() {#getVersion--}
```
public long getVersion()
```

Verze. Pouze pro čtení long.

**Vrací:**
long