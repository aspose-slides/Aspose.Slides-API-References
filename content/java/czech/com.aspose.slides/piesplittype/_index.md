---
title: PieSplitType
second_title: Aspose.Slides pro Java API Reference
description: Reprezentuje typ rozdělovacích bodů ve druhém výsečovém nebo sloupcovém grafu v grafu typu výseč-ve-výseč nebo sloupec-ve-výseč.
type: docs
url: /cs/com.aspose.slides/piesplittype/
---
**Dědičnost:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class PieSplitType extends System.Enum
```

Reprezentuje typ rozdělovacích bodů ve druhém výsečovém nebo sloupcovém grafu v grafu typu výseč-ve-výseč nebo sloupec-ve-výseč.
## Pole

| Pole | Popis |
| --- | --- |
| [Default](#Default) | Určuje, že datové body budou rozděleny pomocí výchozího mechanismu pro tento typ grafu. |
| [Custom](#Custom) | Určuje, že datové body budou rozděleny mezi výseč a druhý graf podle hodnot Custom Split. |
| [ByPercentage](#ByPercentage) | Určuje, že datové body budou rozděleny mezi výseč a druhý graf tak, že body s procentuálním podílem menším než Split Position procent budou umístěny do druhého grafu. |
| [ByPos](#ByPos) | Určuje, že datové body budou rozděleny mezi výseč a druhý graf tak, že poslední Split Position datových bodů bude umístěn do druhého grafu. |
| [ByValue](#ByValue) | Určuje, že datové body budou rozděleny mezi výseč a druhý graf tak, že datové body s hodnotou menší než Split Position budou umístěny do druhého grafu. |
### Default {#Default}
```
public static final int Default
```

Určuje, že datové body budou rozděleny pomocí výchozího mechanismu pro tento typ grafu.

### Custom {#Custom}
```
public static final int Custom
```

Určuje, že datové body budou rozděleny mezi výseč a druhý graf podle hodnot Custom Split.

### ByPercentage {#ByPercentage}
```
public static final int ByPercentage
```

Určuje, že datové body budou rozděleny mezi výseč a druhý graf tak, že body s procentuálním podílem menším než Split Position procent budou umístěny do druhého grafu.

### ByPos {#ByPos}
```
public static final int ByPos
```

Určuje, že datové body budou rozděleny mezi výseč a druhý graf tak, že poslední Split Position datových bodů bude umístěn do druhého grafu.

### ByValue {#ByValue}
```
public static final int ByValue
```

Určuje, že datové body budou rozděleny mezi výseč a druhý graf tak, že datové body s hodnotou menší než Split Position budou umístěny do druhého grafu.