---
title: Timing
second_title: Aspose.Slides pro Java API Reference
description: Reprezentuje časování animace.
type: docs
url: /cs/com.aspose.slides/timing/
---
**Dědičnost:**
java.lang.Object

**Všechny implementované rozhraní:**
[com.aspose.slides.ITiming](../../com.aspose.slides/itiming), com.aspose.slides.IDOMObject
```
public class Timing implements ITiming, IDOMObject
```

Reprezentuje časování animace.
## Metody

| Metoda | Popis |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | Popisuje procento trvání efektu zrychlení chování. |
| [setAccelerate(float value)](#setAccelerate-float-) | Popisuje procento trvání efektu zrychlení chování. |
| [getDecelerate()](#getDecelerate--) | Popisuje procento trvání efektu zpomalení chování. |
| [setDecelerate(float value)](#setDecelerate-float-) | Popisuje procento trvání efektu zpomalení chování. |
| [getAutoReverse()](#getAutoReverse--) | Popisuje, zda se animace má automaticky přehrát v opačném směru po přehrání v dopředném směru. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | Popisuje, zda se animace má automaticky přehrát v opačném směru po přehrání v dopředném směru. |
| [getDuration()](#getDuration--) | Popisuje trvání efektu animace. |
| [setDuration(float value)](#setDuration-float-) | Popisuje trvání efektu animace. |
| [getRepeatCount()](#getRepeatCount--) | Popisuje počet opakování efektu. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | Popisuje počet opakování efektu. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | Tento atribut určuje, zda se efekt bude opakovat až do konce snímku. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | Tento atribut určuje, zda se efekt bude opakovat až do konce snímku. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | Tento atribut určuje, zda se efekt bude opakovat až do dalšího kliknutí. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | Tento atribut určuje, zda se efekt bude opakovat až do dalšího kliknutí. |
| [getRepeatDuration()](#getRepeatDuration--) | Popisuje počet opakování efektu. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | Popisuje počet opakování efektu. |
| [getRestart()](#getRestart--) | Určuje, zda se efekt po dokončení má restartovat. |
| [setRestart(int value)](#setRestart-int-) | Určuje, zda se efekt po dokončení má restartovat. |
| [getRewind()](#getRewind--) | Tento atribut určuje, zda se efekt po přehrání přetočí zpět. |
| [setRewind(boolean value)](#setRewind-boolean-) | Tento atribut určuje, zda se efekt po přehrání přetočí zpět. |
| [getSpeed()](#getSpeed--) | Určuje procento, o které se má časování zrychlit (nebo zpomalit). |
| [setSpeed(float value)](#setSpeed-float-) | Určuje procento, o které se má časování zrychlit (nebo zpomalit). |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | Popisuje dobu zpoždění po spuštění. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | Popisuje dobu zpoždění po spuštění. |
| [getTriggerType()](#getTriggerType--) | Popisuje typ spouštění. |
| [setTriggerType(int value)](#setTriggerType-int-) | Popisuje typ spouštění. |
| [getParent_Immediate()](#getParent-Immediate--) |  |
### getAccelerate() {#getAccelerate--}
```
public final float getAccelerate()
```

Popisuje procento trvání efektu zrychlení chování. Číst/Zapisovat float.

**Vrací:**
float
### setAccelerate(float value) {#setAccelerate-float-}
```
public final void setAccelerate(float value)
```

Popisuje procento trvání efektu zrychlení chování. Číst/Zapisovat float.

**Parametry:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getDecelerate() {#getDecelerate--}
```
public final float getDecelerate()
```

Popisuje procento trvání efektu zpomalení chování. Číst/Zapisovat float.

**Vrací:**
float
### setDecelerate(float value) {#setDecelerate-float-}
```
public final void setDecelerate(float value)
```

Popisuje procento trvání efektu zpomalení chování. Číst/Zapisovat float.

**Parametry:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getAutoReverse() {#getAutoReverse--}
```
public final boolean getAutoReverse()
```

Popisuje, zda se animace má automaticky přehrát v opačném směru po přehrání v dopředném směru. Číst/Zapisovat boolean.

**Vrací:**
boolean
### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public final void setAutoReverse(boolean value)
```

Popisuje, zda se animace má automaticky přehrát v opačném směru po přehrání v dopředném směru. Číst/Zapisovat boolean.

**Parametry:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |
### getDuration() {#getDuration--}
```
public final float getDuration()
```

Popisuje trvání efektu animace. Číst/Zapisovat float.

**Vrací:**
float
### setDuration(float value) {#setDuration-float-}
```
public final void setDuration(float value)
```

Popisuje trvání efektu animace. Číst/Zapisovat float.

**Parametry:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getRepeatCount() {#getRepeatCount--}
```
public final float getRepeatCount()
```

Popisuje počet opakování efektu. Číst/Zapisovat float.

**Vrací:**
float
### setRepeatCount(float value) {#setRepeatCount-float-}
```
public final void setRepeatCount(float value)
```

Popisuje počet opakování efektu. Číst/Zapisovat float.

**Parametry:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public final boolean getRepeatUntilEndSlide()
```

Tento atribut určuje, zda se efekt bude opakovat až do konce snímku. Číst/Zapisovat boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Získá sekvenci efektů pro první snímek
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Získá první efekt hlavní sekvence.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Změní časování/opakování efektu na "Do konce snímku"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Vrací:**
boolean
### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public final void setRepeatUntilEndSlide(boolean value)
```

Tento atribut určuje, zda se efekt bude opakovat až do konce snímku. Číst/Zapisovat boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Získá sekvenci efektů pro první snímek
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Získá první efekt hlavní sekvence.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Změní časování/opakování efektu na "Do konce snímku"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parametry:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |
### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public final boolean getRepeatUntilNextClick()
```

Tento atribut určuje, zda se efekt bude opakovat až do dalšího kliknutí. Číst/Zapisovat boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Získá sekvenci efektů pro první snímek
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Získá první efekt hlavní sekvence.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Změní časování/opakování efektu na "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Vrací:**
boolean
### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public final void setRepeatUntilNextClick(boolean value)
```

Tento atribut určuje, zda se efekt bude opakovat až do dalšího kliknutí. Číst/Zapisovat boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Získá sekvenci efektů pro první snímek
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Získá první efekt hlavní sekvence.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Změní časování/opakování efektu na "Do dalšího kliknutí"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parametry:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |
### getRepeatDuration() {#getRepeatDuration--}
```
public final float getRepeatDuration()
```

Popisuje počet opakování efektu. Číst/Zapisovat float.

**Vrací:**
float
### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public final void setRepeatDuration(float value)
```

Popisuje počet opakování efektu. Číst/Zapisovat float.

**Parametry:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getRestart() {#getRestart--}
```
public final int getRestart()
```

Určuje, zda se efekt po dokončení má restartovat. Číst/Zapisovat [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Vrací:**
int
### setRestart(int value) {#setRestart-int-}
```
public final void setRestart(int value)
```

Určuje, zda se efekt po dokončení má restartovat. Číst/Zapisovat [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Parametry:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | int |  |
### getRewind() {#getRewind--}
```
public final boolean getRewind()
```

Tento atribut určuje, zda se efekt po přehrání přetočí zpět. Číst/Zapisovat boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Získá sekvenci efektů pro první snímek
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Získá první efekt hlavní sekvence.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Zapne časování/Rewind efektu.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Vrací:**
boolean
### setRewind(boolean value) {#setRewind-boolean-}
```
public final void setRewind(boolean value)
```

Tento atribut určuje, zda se efekt po přehrání přetočí zpět. Číst/Zapisovat boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // Získá sekvenci efektů pro první snímek
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // Získá první efekt hlavní sekvence.
>      IEffect effect = effectsSequence.get_Item(0);
>      // Zapne efekt Timing/Rewind.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parametry:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |
### getSpeed() {#getSpeed--}
```
public final float getSpeed()
```

Určuje procento, o které se má časování zrychlit (nebo zpomalit). Číst/Zapisovat float.

**Vrací:**
float
### setSpeed(float value) {#setSpeed-float-}
```
public final void setSpeed(float value)
```

Určuje procento, o které se má časování zrychlit (nebo zpomalit). Číst/Zapisovat float.

**Parametry:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public final float getTriggerDelayTime()
```

Popisuje dobu zpoždění po spuštění. Číst/Zapisovat float.

**Vrací:**
float
### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public final void setTriggerDelayTime(float value)
```

Popisuje dobu zpoždění po spuštění. Číst/Zapisovat float.

**Parametry:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |
### getTriggerType() {#getTriggerType--}
```
public final int getTriggerType()
```

Popisuje typ spouštění. Číst/Zapisovat [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Vrací:**
int
### setTriggerType(int value) {#setTriggerType-int-}
```
public final void setTriggerType(int value)
```

Popisuje typ spouštění. Číst/Zapisovat [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Parametry:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | int |  |
### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

Vrací objekt Parent_Immediate. Pouze pro čtení IDOMObject.

**Vrací:**
com.aspose.slides.IDOMObject