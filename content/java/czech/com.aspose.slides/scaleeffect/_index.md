---
title: ScaleEffect
second_title: Aspose.Slides pro Java – reference API
description: Reprezentuje efekt měřítka animace.
type: docs
url: /cs/com.aspose.slides/scaleeffect/
---
**Dědění:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**Všechny implementované rozhraní:**
[com.aspose.slides.IScaleEffect](../../com.aspose.slides/iscaleeffect)
```
public class ScaleEffect extends Behavior implements IScaleEffect
```

Reprezentuje efekt měřítka animace.
## Konstruktory

| Konstruktor | Popis |
| --- | --- |
| [ScaleEffect()](#ScaleEffect--) |  |
## Metody

| Metoda | Popis |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | Určuje, zda má být obsah přiblížen. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | Určuje, zda má být obsah přiblížen. |
| [getFrom()](#getFrom--) | Určuje souřadnici x/y, odkud má animace začít (v procentech). |
| [setFrom(Point2D.Float value)](#setFrom-java.awt.geom.Point2D.Float-) | Určuje souřadnici x/y, odkud má animace začít (v procentech). |
| [getTo()](#getTo--) | Určuje cílovou polohu pro efekt měřítka animace (v procentech). |
| [setTo(Point2D.Float value)](#setTo-java.awt.geom.Point2D.Float-) | Určuje cílovou polohu pro efekt měřítka animace (v procentech). |
| [getBy()](#getBy--) | Popisuje relativní hodnotu posunu pro animaci (v procentech). |
| [setBy(Point2D.Float value)](#setBy-java.awt.geom.Point2D.Float-) | Popisuje relativní hodnotu posunu pro animaci (v procentech). |
### ScaleEffect() {#ScaleEffect--}
```
public ScaleEffect()
```

### getZoomContent() {#getZoomContent--}
```
public final byte getZoomContent()
```

Určuje, zda má být obsah přiblížen. Číst/Zapisovat [NullableBool](../../com.aspose.slides/nullablebool).

**Vrací:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public final void setZoomContent(byte value)
```

Určuje, či má být obsah přiblížen. Číst/Zapisovat [NullableBool](../../com.aspose.slides/nullablebool).

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | byte |  |
### getFrom() {#getFrom--}
```
public final Point2D.Float getFrom()
```

Určuje souřadnici x/y, odkud má animace začít (v procentech). Číst/Zapisovat java.awt.geom.Point2D.Float.

**Vrací:**
java.awt.geom.Point2D.Float
### setFrom(Point2D.Float value) {#setFrom-java.awt.geom.Point2D.Float-}
```
public final void setFrom(Point2D.Float value)
```

Určuje souřadnici x/y, odkud má animace začít (v procentech). Číst/Zapisovat java.awt.geom.Point2D.Float.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |
### getTo() {#getTo--}
```
public final Point2D.Float getTo()
```

Určuje cílovou polohu pro efekt měřítka animace (v procentech). Číst/Zapisovat java.awt.geom.Point2D.Float.

**Vrací:**
java.awt.geom.Point2D.Float
### setTo(Point2D.Float value) {#setTo-java.awt.geom.Point2D.Float-}
```
public final void setTo(Point2D.Float value)
```

Určuje cílovou polohu pro efekt měřítka animace (v procentech). Číst/Zapisovat java.awt.geom.Point2D.Float.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |
### getBy() {#getBy--}
```
public final Point2D.Float getBy()
```

Popisuje relativní hodnotu posunu pro animaci (v procentech). Číst/Zapisovat java.awt.geom.Point2D.Float.

**Vrací:**
java.awt.geom.Point2D.Float
### setBy(Point2D.Float value) {#setBy-java.awt.geom.Point2D.Float-}
```
public final void setBy(Point2D.Float value)
```

Popisuje relativní hodnotu posunu pro animaci (v procentech). Číst/Zapisovat java.awt.geom.Point2D.Float.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |