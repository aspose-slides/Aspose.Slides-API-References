---
title: BaseSlideHeaderFooterManager
second_title: Aspose.Slides pro Java API Reference
description: Reprezentuje správce, který obsahuje chování zápatí, datum-čas a zástupných prvků číslování stránek pro všechny typy snímků.
type: docs
url: /cs/com.aspose.slides/baseslideheaderfootermanager/
---
**Dědičnost:**
java.lang.Object, [com.aspose.slides.BaseHeaderFooterManager](../../com.aspose.slides/baseheaderfootermanager)
```
public abstract class BaseSlideHeaderFooterManager extends BaseHeaderFooterManager
```

Reprezentuje správce, který obsahuje chování zápatí, datum-čas a zástupných prvků číslování stránek pro všechny typy snímků.
## Metody

| Metoda | Popis |
| --- | --- |
| [isFooterVisible()](#isFooterVisible--) | Získá hodnotu, která označuje, že je přítomen zástupný prvek zápatí. |
| [isSlideNumberVisible()](#isSlideNumberVisible--) | Získá hodnotu, která označuje, že je přítomen zástupný prvek číslování stránek. |
| [isDateTimeVisible()](#isDateTimeVisible--) | Získá hodnotu, která označuje, že je přítomen zástupný prvek datum-čas. |
| [setFooterVisibility(boolean isVisible)](#setFooterVisibility-boolean-) | Mění viditelnost zástupného prvku zápatí snímku. |
| [setSlideNumberVisibility(boolean isVisible)](#setSlideNumberVisibility-boolean-) | Mění viditelnost zástupného prvku číslování stránek snímku. |
| [setDateTimeVisibility(boolean isVisible)](#setDateTimeVisibility-boolean-) | Mění viditelnost zástupného prvku datum-čas snímku. |
| [setFooterText(String text)](#setFooterText-java.lang.String-) | Nastaví text do zástupného prvku zápatí snímku. |
| [setDateTimeText(String text)](#setDateTimeText-java.lang.String-) | Nastaví text do zástupného prvku datum-čas snímku. |
### isFooterVisible() {#isFooterVisible--}
```
public final boolean isFooterVisible()
```

Získá hodnotu, která označuje, že je přítomen zástupný prvek zápatí. Čte boolean.

**Vrací:**
boolean
### isSlideNumberVisible() {#isSlideNumberVisible--}
```
public final boolean isSlideNumberVisible()
```

Získá hodnotu, která označuje, že je přítomen zástupný prvek číslování stránek. Čte boolean.

**Vrací:**
boolean
### isDateTimeVisible() {#isDateTimeVisible--}
```
public final boolean isDateTimeVisible()
```

Získá hodnotu, která označuje, že je přítomen zástupný prvek datum-čas. Čte boolean.

**Vrací:**
boolean
### setFooterVisibility(boolean isVisible) {#setFooterVisibility-boolean-}
```
public final void setFooterVisibility(boolean isVisible)
```

Mění viditelnost zástupného prvku zápatí snímku.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| isVisible | boolean | true - zobrazí zástupný prvek zápatí, jinak jej skryje. |

### setSlideNumberVisibility(boolean isVisible) {#setSlideNumberVisibility-boolean-}
```
public final void setSlideNumberVisibility(boolean isVisible)
```

Mění viditelnost zástupného prvku číslování stránek snímku.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| isVisible | boolean | true - zobrazí zástupný prvek číslování stránek, jinak jej skryje. |

### setDateTimeVisibility(boolean isVisible) {#setDateTimeVisibility-boolean-}
```
public final void setDateTimeVisibility(boolean isVisible)
```

Mění viditelnost zástupného prvku datum-čas snímku.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| isVisible | boolean | true - zobrazí zástupný prvek datum-čas, jinak jej skryje. |

### setFooterText(String text) {#setFooterText-java.lang.String-}
```
public final void setFooterText(String text)
```

Nastaví text do zástupného prvku zápatí snímku.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| text | java.lang.String | Text k nastavení. |

### setDateTimeText(String text) {#setDateTimeText-java.lang.String-}
```
public final void setDateTimeText(String text)
```

Nastaví text do zástupného prvku datum-čas snímku.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| text | java.lang.String | Text k nastavení. |