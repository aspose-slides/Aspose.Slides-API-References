---
title: IOptionalBlackTransition
second_title: Aspose.Slides pro Java – Referenční příručka API
description: Volitelný černý efekt přechodu snímku.
type: docs
url: /cs/com.aspose.slides/ioptionalblacktransition/
---
**Všechna implementovaná rozhraní:**
[com.aspose.slides.ITransitionValueBase](../../com.aspose.slides/itransitionvaluebase)
```
public interface IOptionalBlackTransition extends ITransitionValueBase
```

Volitelný černý efekt přechodu snímku.
## Metody

| Metoda | Popis |
| --- | --- |
| [getFromBlack()](#getFromBlack--) | Tento atribut určuje, zda přechod začne z černé obrazovky (a poté přechod nového snímku přes černou). |
| [setFromBlack(boolean value)](#setFromBlack-boolean-) | Tento atribut určuje, zda přechod začne z černé obrazovky (a poté přechod nového snímku přes černou). |
### getFromBlack() {#getFromBlack--}
```
public abstract boolean getFromBlack()
```


Tento atribut určuje, zda přechod začne z černé obrazovky (a poté přechod nového snímku přes černou). Čtení/zápis boolean.

**Vrací:**
boolean
### setFromBlack(boolean value) {#setFromBlack-boolean-}
```
public abstract void setFromBlack(boolean value)
```


Tento atribut určuje, zda přechod začne z černé obrazovky (a poté přechod nového snímku přes černou). Čtení/zápis boolean.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |