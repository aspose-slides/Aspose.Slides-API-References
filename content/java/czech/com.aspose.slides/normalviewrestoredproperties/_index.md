---
title: NormalViewRestoredProperties
second_title: Aspose.Slides pro Java API Reference
description: Určuje velikost oblasti snímku – šířka, když je podřízený restoredTop, výška, když je podřízený restoredLeft, v normálním zobrazení, když je oblast proměnné obnovitelné velikosti (ani minimalizovaná, ani maximalizovaná).
type: docs
url: /cs/com.aspose.slides/normalviewrestoredproperties/
---
**Inheritance:**
java.lang.Object

**All Implemented Interfaces:**
[com.aspose.slides.INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)
```
public class NormalViewRestoredProperties implements INormalViewRestoredProperties
```

Určuje velikost oblasti snímku ((šířka, když je podřízený restoredTop, výška, když je podřízený restoredLeft) normálního zobrazení, když je oblast proměnné obnovitelné velikosti (ani minimalizovaná, ani maximalizovaná)).

## Metody

| Metoda | Popis |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | Určuje velikost oblasti snímku (šířka, když je podřízený RestoredTop, výška, když je podřízený RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | Určuje velikost oblasti snímku (šířka, když je podřízený RestoredTop, výška, když je podřízený RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | Určuje, zda má velikost oblasti postranního obsahu kompenzovat novou velikost při změně velikosti okna obsahujícího zobrazení v aplikaci. Číst/zapisovat boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | Určuje, zda má velikost oblasti postranního obsahu kompenzovat novou velikost při změně velikosti okna obsahujícího zobrazení v aplikaci. Číst/zapisovat boolean. |

### getDimensionSize() {#getDimensionSize--}
```
public final float getDimensionSize()
```

Určuje velikost oblasti snímku (šířka, když je podřízený RestoredTop, výška, když je podřízený RestoredLeft). Číst/zapisovat float.

**Vrací:**
float

### setDimensionSize(float value) {#setDimensionSize-float-}
```
public final void setDimensionSize(float value)
```

Určuje velikost oblasti snímku (šířka, když je podřízený RestoredTop, výška, když je podřízený RestoredLeft). Číst/zapisovat float.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public final boolean getAutoAdjust()
```

Určuje, zda má velikost oblasti postranního obsahu kompenzovat novou velikost při změně velikosti okna obsahujícího zobrazení v aplikaci. Číst/zapisovat boolean.

**Vrací:**
boolean

### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public final void setAutoAdjust(boolean value)
```

Určuje, zda má velikost oblasti postranního obsahu kompenzovat novou velikost při změně velikosti okna obsahujícího zobrazení v aplikaci. Číst/zapisovat boolean.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |