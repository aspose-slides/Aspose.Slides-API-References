---
title: OptionalBlackTransition
second_title: Aspose.Slides pro Java API Reference
description: Volitelný černý efekt přechodu snímku.
type: docs
url: /cs/com.aspose.slides/optionalblacktransition/
---
**Inheritance:**
java.lang.Object, [com.aspose.slides.TransitionValueBase](../../com.aspose.slides/transitionvaluebase)

**All Implemented Interfaces:**
[com.aspose.slides.IOptionalBlackTransition](../../com.aspose.slides/ioptionalblacktransition)
```
public class OptionalBlackTransition extends TransitionValueBase implements IOptionalBlackTransition
```

Volitelný černý efekt přechodu snímku.
## Metody

| Metoda | Popis |
| --- | --- |
| [getFromBlack()](#getFromBlack--) | Tento atribut určuje, zda přechod začne z černé obrazovky (a poté přejde na nový snímek přes černé pozadí). |
| [setFromBlack(boolean value)](#setFromBlack-boolean-) | Tento atribut určuje, zda přechod začne z černé obrazovky (a poté přejde na nový snímek přes černé pozadí). |
### getFromBlack() {#getFromBlack--}
```
public final boolean getFromBlack()
```

Tento atribut určuje, zda přechod začne z černé obrazovky (a poté přejde na nový snímek přes černé pozadí). Čtení a zápis boolean.

**Vrací:**
boolean
### setFromBlack(boolean value) {#setFromBlack-boolean-}
```
public final void setFromBlack(boolean value)
```

Tento atribut určuje, zda přechod začne z černé obrazovky (a poté přejde na nový snímek přes černé pozadí). Čtení a zápis boolean.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |