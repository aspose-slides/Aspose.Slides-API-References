---
title: IPictureFrameLock
second_title: Aspose.Slides pro Java – API Reference
description: Určuje, které operace jsou na nadřazeném objektu PictureFrameEx zakázány.
type: docs
url: /cs/com.aspose.slides/ipictureframelock/
---
**Všechna implementovaná rozhraní:**
[com.aspose.slides.IBaseShapeLock](../../com.aspose.slides/ibaseshapelock)
```
public interface IPictureFrameLock extends IBaseShapeLock
```

Určuje, které operace jsou na nadřazeném objektu PictureFrameEx zakázány.
## Metody

| Metoda | Popis |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | Určuje, zda je zakázáno přidávat tento tvar do skupiny. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | Určuje, zda je zakázáno přidávat tento tvar do skupiny. |
| [getSelectLocked()](#getSelectLocked--) | Určuje, zda je zakázáno vybírat tento tvar. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | Určuje, zda je zakázáno vybírat tento tvar. |
| [getRotationLocked()](#getRotationLocked--) | Určuje, zda je zakázáno měnit úhel otáčení tohoto tvaru. |
| [setRotationLocked(boolean value)](#setRotationLocked-boolean-) | Určuje, zda je zakázáno měnit úhel otáčení tohoto tvaru. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | Určuje, zda musí tvar zachovávat poměr stran při změně velikosti. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | Určuje, zda musí tvar zachovávat poměr stran při změně velikosti. |
| [getPositionLocked()](#getPositionLocked--) | Určuje, zda je zakázáno přesouvat tento tvar. |
| [setPositionLocked(boolean value)](#setPositionLocked-boolean-) | Určuje, zda je zakázáno přesouvat tento tvar. |
| [getSizeLocked()](#getSizeLocked--) | Určuje, zda je zakázáno měnit velikost tohoto tvaru. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | Určuje, zda je zakázáno měnit velikost tohoto tvaru. |
| [getEditPointsLocked()](#getEditPointsLocked--) | Určuje, zda je zakázáno přímo měnit obrys tohoto tvaru. |
| [setEditPointsLocked(boolean value)](#setEditPointsLocked-boolean-) | Určuje, zda je zakázáno přímo měnit obrys tohoto tvaru. |
| [getAdjustHandlesLocked()](#getAdjustHandlesLocked--) | Určuje, zda je zakázáno měnit hodnoty úprav. |
| [setAdjustHandlesLocked(boolean value)](#setAdjustHandlesLocked-boolean-) | Určuje, zda je zakázáno měnit hodnoty úprav. |
| [getArrowheadsLocked()](#getArrowheadsLocked--) | Určuje, zda je zakázáno měnit šipky. |
| [setArrowheadsLocked(boolean value)](#setArrowheadsLocked-boolean-) | Určuje, zda je zakázáno měnit šipky. |
| [getShapeTypeLocked()](#getShapeTypeLocked--) | Určuje, zda je zakázáno měnit typ tvaru. |
| [setShapeTypeLocked(boolean value)](#setShapeTypeLocked-boolean-) | Určuje, zda je zakázáno měnit typ tvaru. |
| [getCropLocked()](#getCropLocked--) | Určuje, zda je zakázáno ořezávat obrázek. |
| [setCropLocked(boolean value)](#setCropLocked-boolean-) | Určuje, zda je zakázáno ořezávat obrázek. |
### getGroupingLocked() {#getGroupingLocked--}
```
public abstract boolean getGroupingLocked()
```

Určuje, zda je zakázáno přidávat tento tvar do skupiny. Čtení/zápis boolean.

**Vrací:**
boolean
### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public abstract void setGroupingLocked(boolean value)
```

Určuje, zda je zakázáno přidávat tento tvar do skupiny. Čtení/zápis boolean.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |

### getSelectLocked() {#getSelectLocked--}
```
public abstract boolean getSelectLocked()
```

Určuje, zda je zakázáno vybírat tento tvar. Čtení/zápis boolean.

**Vrací:**
boolean
### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public abstract void setSelectLocked(boolean value)
```

Určuje, zda je zakázáno vybírat tento tvar. Čtení/zápis boolean.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |

### getRotationLocked() {#getRotationLocked--}
```
public abstract boolean getRotationLocked()
```

Určuje, zda je zakázáno měnit úhel otáčení tohoto tvaru. Čtení/zápis boolean.

**Vrací:**
boolean
### setRotationLocked(boolean value) {#setRotationLocked-boolean-}
```
public abstract void setRotationLocked(boolean value)
```

Určuje, zda je zakázáno měnit úhel otáčení tohoto tvaru. Čtení/zápis boolean.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |

### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public abstract boolean getAspectRatioLocked()
```

Určuje, zda musí tvar zachovávat poměr stran při změně velikosti. Čtení/zápis boolean.

**Vrací:**
boolean
### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public abstract void setAspectRatioLocked(boolean value)
```

Určuje, zda musí tvar zachovávat poměr stran při změně velikosti. Čtení/zápis boolean.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |

### getPositionLocked() {#getPositionLocked--}
```
public abstract boolean getPositionLocked()
```

Určuje, zda je zakázáno přesouvat tento tvar. Čtení/zápis boolean.

**Vrací:**
boolean
### setPositionLocked(boolean value) {#setPositionLocked-boolean-}
```
public abstract void setPositionLocked(boolean value)
```

Určuje, zda je zakázáno přesouvat tento tvar. Čtení/zápis boolean.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |

### getSizeLocked() {#getSizeLocked--}
```
public abstract boolean getSizeLocked()
```

Určuje, zda je zakázáno měnit velikost tohoto tvaru. Čtení/zápis boolean.

**Vrací:**
boolean
### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public abstract void setSizeLocked(boolean value)
```

Určuje, zda je zakázáno měnit velikost tohoto tvaru. Čtení/zápis boolean.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |

### getEditPointsLocked() {#getEditPointsLocked--}
```
public abstract boolean getEditPointsLocked()
```

Určuje, zda je zakázáno přímo měnit obrys tohoto tvaru. Čtení/zápis boolean.

**Vrací:**
boolean
### setEditPointsLocked(boolean value) {#setEditPointsLocked-boolean-}
```
public abstract void setEditPointsLocked(boolean value)
```

Určuje, zda je zakázáno přímo měnit obrys tohoto tvaru. Čtení/zápis boolean.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |

### getAdjustHandlesLocked() {#getAdjustHandlesLocked--}
```
public abstract boolean getAdjustHandlesLocked()
```

Určuje, zda je zakázáno měnit hodnoty úprav. Čtení/zápis boolean.

**Vrací:**
boolean
### setAdjustHandlesLocked(boolean value) {#setAdjustHandlesLocked-boolean-}
```
public abstract void setAdjustHandlesLocked(boolean value)
```

Určuje, zda je zakázáno měnit hodnoty úprav. Čtení/zápis boolean.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |

### getArrowheadsLocked() {#getArrowheadsLocked--}
```
public abstract boolean getArrowheadsLocked()
```

Určuje, zda je zakázáno měnit šipky. Čtení/zápis boolean.

**Vrací:**
boolean
### setArrowheadsLocked(boolean value) {#setArrowheadsLocked-boolean-}
```
public abstract void setArrowheadsLocked(boolean value)
```

Určuje, zda je zakázáno měnit šipky. Čtení/zápis boolean.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |

### getShapeTypeLocked() {#getShapeTypeLocked--}
```
public abstract boolean getShapeTypeLocked()
```

Určuje, zda je zakázáno měnit typ tvaru. Čtení/zápis boolean.

**Vrací:**
boolean
### setShapeTypeLocked(boolean value) {#setShapeTypeLocked-boolean-}
```
public abstract void setShapeTypeLocked(boolean value)
```

Určuje, zda je zakázáno měnit typ tvaru. Čtení/zápis boolean.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |

### getCropLocked() {#getCropLocked--}
```
public abstract boolean getCropLocked()
```

Určuje, zda je zakázáno ořezávat obrázek. Čtení/zápis boolean.

**Vrací:**
boolean
### setCropLocked(boolean value) {#setCropLocked-boolean-}
```
public abstract void setCropLocked(boolean value)
```

Určuje, zda je zakázáno ořezávat obrázek. Čtení/zápis boolean.

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | boolean |  |