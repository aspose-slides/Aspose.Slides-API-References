---
title: IDataSourceTypeForErrorBarsCustomValues
second_title: Aspose.Slides for Java API Reference
description: Specifikuje typy hodnot v seznamu vlastností ChartDataPoint.ErrorBarsCustomValues
type: docs
url: /cs/com.aspose.slides/idatasourcetypeforerrorbarscustomvalues/
---```
public interface IDataSourceTypeForErrorBarsCustomValues
```

Specifikuje typy hodnot v seznamu vlastností ChartDataPoint.ErrorBarsCustomValues
## Metody

| Metoda | Popis |
| --- | --- |
| [getDataSourceTypeForXMinusValues()](#getDataSourceTypeForXMinusValues--) | Určuje, zda je ve vlastnosti XMinus objektu datových bodů pro vlastní hodnoty chybových úseček platná vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble. |
| [setDataSourceTypeForXMinusValues(int value)](#setDataSourceTypeForXMinusValues-int-) | Určuje, zda je ve vlastnosti XMinus objektu datových bodů pro vlastní hodnoty chybových úseček platná vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble. |
| [getDataSourceTypeForXPlusValues()](#getDataSourceTypeForXPlusValues--) | Určuje, zda je ve vlastnosti XPlus objektu datových bodů pro vlastní hodnoty chybových úseček platná vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble. |
| [setDataSourceTypeForXPlusValues(int value)](#setDataSourceTypeForXPlusValues-int-) | Určuje, zda je ve vlastnosti XPlus objektu datových bodů pro vlastní hodnoty chybových úseček platná vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble. |
| [getDataSourceTypeForYMinusValues()](#getDataSourceTypeForYMinusValues--) | Určuje, zda je ve vlastnosti YMinus objektu datových bodů pro vlastní hodnoty chybových úseček platná vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble. |
| [setDataSourceTypeForYMinusValues(int value)](#setDataSourceTypeForYMinusValues-int-) | Určuje, zda je ve vlastnosti YMinus objektu datových bodů pro vlastní hodnoty chybových úseček platná vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble. |
| [getDataSourceTypeForYPlusValues()](#getDataSourceTypeForYPlusValues--) | Určuje, zda je ve vlastnosti YPlus objektu datových bodů pro vlastní hodnoty chybových úseček platná vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble. |
| [setDataSourceTypeForYPlusValues(int value)](#setDataSourceTypeForYPlusValues-int-) | Určuje, zda je ve vlastnosti YPlus objektu datových bodů pro vlastní hodnoty chybových úseček platná vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble. |

### getDataSourceTypeForXMinusValues() {#getDataSourceTypeForXMinusValues--}
```
public abstract int getDataSourceTypeForXMinusValues()
```

Určuje, zda je ve vlastnosti XMinus objektu datových bodů pro vlastní hodnoty chybových úseček platná vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble. Jinak řečeno, určuje typ hodnoty vlastnosti ChartDataPoint.ErrorBarsCustomValues.XMinus.Data. Čtení/Zápis [DataSourceType](../../com.aspose.slides/datasourcetype).

**Návratová hodnota:**
int

### setDataSourceTypeForXMinusValues(int value) {#setDataSourceTypeForXMinusValues-int-}
```
public abstract void setDataSourceTypeForXMinusValues(int value)
```

Určuje, zda je ve vlastnosti XMinus objektu datových bodů pro vlastní hodnoty chybových úseček platná vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble. Jinak řečeno, určuje typ hodnoty vlastnosti ChartDataPoint.ErrorBarsCustomValues.XMinus.Data. Čtení/Zápis [DataSourceType](../../com.aspose.slides/datasourcetype).

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | int |  |

### getDataSourceTypeForXPlusValues() {#getDataSourceTypeForXPlusValues--}
```
public abstract int getDataSourceTypeForXPlusValues()
```

Určuje, zda je ve vlastnosti XPlus objektu datových bodů pro vlastní hodnoty chybových úseček platná vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble. Jinak řečeno, určuje typ hodnoty vlastnosti ChartDataPoint.ErrorBarsCustomValues.XPlus.Data. Čtení/Zápis [DataSourceType](../../com.aspose.slides/datasourcetype).

**Návratová hodnota:**
int

### setDataSourceTypeForXPlusValues(int value) {#setDataSourceTypeForXPlusValues-int-}
```
public abstract void setDataSourceTypeForXPlusValues(int value)
```

Určuje, zda je ve vlastnosti XPlus objektu datových bodů pro vlastní hodnoty chybových úseček platná vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble. Jinak řečeno, určuje typ hodnoty vlastnosti ChartDataPoint.ErrorBarsCustomValues.XPlus.Data. Čtení/Zápis [DataSourceType](../../com.aspose.slides/datasourcetype).

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | int |  |

### getDataSourceTypeForYMinusValues() {#getDataSourceTypeForYMinusValues--}
```
public abstract int getDataSourceTypeForYMinusValues()
```

Určuje, zda je ve vlastnosti YMinus objektu datových bodů pro vlastní hodnoty chybových úseček platná vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble. Jinak řečeno, určuje typ hodnoty vlastnosti ChartDataPointEx.ErrorBarsCustomValues.YMinus.Data. Čtení/Zápis [DataSourceType](../../com.aspose.slides/datasourcetype).

**Návratová hodnota:**
int

### setDataSourceTypeForYMinusValues(int value) {#setDataSourceTypeForYMinusValues-int-}
```
public abstract void setDataSourceTypeForYMinusValues(int value)
```

Určuje, zda je ve vlastnosti YMinus objektu datových bodů pro vlastní hodnoty chybových úseček platná vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble. Jinak řečeno, určuje typ hodnoty vlastnosti ChartDataPointEx.ErrorBarsCustomValues.YMinus.Data. Čtení/Zápis [DataSourceType](../../com.aspose.slides/datasourcetype).

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | int |  |

### getDataSourceTypeForYPlusValues() {#getDataSourceTypeForYPlusValues--}
```
public abstract int getDataSourceTypeForYPlusValues()
```

Určuje, zda je ve vlastnosti YPlus objektu datových bodů pro vlastní hodnoty chybových úseček platná vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble. Jinak řečeno, určuje typ hodnoty vlastnosti ChartDataPointEx.ErrorBarsCustomValues.YPlus.Data. Čtení/Zápis [DataSourceType](../../com.aspose.slides/datasourcetype).

**Návratová hodnota:**
int

### setDataSourceTypeForYPlusValues(int value) {#setDataSourceTypeForYPlusValues-int-}
```
public abstract void setDataSourceTypeForYPlusValues(int value)
```

Určuje, zda je ve vlastnosti YPlus objektu datových bodů pro vlastní hodnoty chybových úseček platná vlastnost AsCell nebo AsLiteralString nebo AsLiteralDouble. Jinak řečeno, určuje typ hodnoty vlastnosti ChartDataPointEx.ErrorBarsCustomValues.YPlus.Data. Čtení/Zápis [DataSourceType](../../com.aspose.slides/datasourcetype).

**Parametry:**
| Parametr | Typ | Popis |
| --- | --- | --- |
| value | int |  |