---
title: NotesPositions
second_title: مرجع API Aspose.Slides برای Java
description: قوانینی را که برای رندر کردن یادداشت‌ها در سند صادر شده استفاده می‌شود، نشان می‌دهد
type: docs
url: /fa/com.aspose.slides/notespositions/
---
**ارث‌برداری:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class NotesPositions extends System.Enum
```

قانونی را که برای رندر کردن یادداشت‌ها در سند صادر شده استفاده می‌شود، نشان می‌دهد
## فیلدها

| فیلد | توضیح |
| --- | --- |
| [None](#None) | مشخص می‌کند که یادداشت‌ها به‌طور کامل نمایش داده نشوند. |
| [BottomFull](#BottomFull) | مشخص می‌کند که یادداشت‌ها به‌صورت کامل و بر اساس نیاز با استفاده از صفحات اضافی نمایش داده شوند. |
| [BottomTruncated](#BottomTruncated) | مشخص می‌کند که یادداشت‌ها تنها در یک صفحه نمایش داده شوند. |
### None {#None}
```
public static final int None
```

مشخص می‌کند که یادداشت‌ها به‌طور کامل نمایش داده نشوند.

### BottomFull {#BottomFull}
```
public static final int BottomFull
```

مشخص می‌کند که یادداشت‌ها به‌صورت کامل و بر اساس نیاز با استفاده از صفحات اضافی نمایش داده شوند.

### BottomTruncated {#BottomTruncated}
```
public static final int BottomTruncated
```

مشخص می‌کند که یادداشت‌ها تنها در یک صفحه نمایش داده شوند.