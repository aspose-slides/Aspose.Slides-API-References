---
title: PresentationHeaderFooterManager
second_title: مرجع API Aspose.Slides برای جاوا
description: مدیری را نمایندگی می‌کند که رفتار تمام نگهدارنده‌های پاورقی، تاریخ-زمان و شماره صفحه ارائه را در خود دارد.
type: docs
url: /fa/com.aspose.slides/presentationheaderfootermanager/
---
**ارث‌بری:**
java.lang.Object, [com.aspose.slides.BaseHeaderFooterManager](../../com.aspose.slides/baseheaderfootermanager)

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IPresentationHeaderFooterManager](../../com.aspose.slides/ipresentationheaderfootermanager)
```
public class PresentationHeaderFooterManager extends BaseHeaderFooterManager implements IPresentationHeaderFooterManager
```

مدیری را نمایندگی می‌کند که رفتار تمام نگهدارنده‌های پاورقی، تاریخ-زمان و شماره صفحه ارائه را در خود دارد.
## متدها

| متد | توضیح |
| --- | --- |
| [setAllHeadersVisibility(boolean isVisible)](#setAllHeadersVisibility-boolean-) | تمام قابلیت مشاهدهٔ نگهدارنده‌های سرصفحه را تغییر می‌دهد، از جملهٔ استاد یادداشت‌ها، اسلایدهای یادداشت و استاد جزوه. |
| [setAllFootersVisibility(boolean isVisible)](#setAllFootersVisibility-boolean-) | تمام قابلیت مشاهدهٔ نگهدارنده‌های پاورقی را تغییر می‌دهد، از جملهٔ اسلایدهای استاد، اسلایدهای طرح‌بندی، اسلایدها، استاد یادداشت‌ها، اسلایدهای یادداشت و استاد جزوه. |
| [setAllSlideNumbersVisibility(boolean isVisible)](#setAllSlideNumbersVisibility-boolean-) | تمام قابلیت مشاهدهٔ نگهدارنده‌های شماره صفحه را تغییر می‌دهد، از جملهٔ اسلایدهای استاد، اسلایدهای طرح‌بندی، اسلایدها، استاد یادداشت‌ها، اسلایدهای یادداشت و استاد جزوه. |
| [setAllDateTimesVisibility(boolean isVisible)](#setAllDateTimesVisibility-boolean-) | تمام قابلیت مشاهدهٔ نگهدارنده‌های تاریخ-زمان را تغییر می‌دهد، از جملهٔ اسلایدهای استاد، اسلایدهای طرح‌بندی، اسلایدها، استاد یادداشت‌ها، اسلایدهای یادداشت و استاد جزوه. |
| [setAllHeadersText(String text)](#setAllHeadersText-java.lang.String-) | متن را برای تمام نگهدارنده‌های سرصفحه تنظیم می‌کند، از جملهٔ استاد یادداشت‌ها، اسلایدهای یادداشت و استاد جزوه. |
| [setAllFootersText(String text)](#setAllFootersText-java.lang.String-) | متن را برای تمام نگهدارنده‌های پاورقی تنظیم می‌کند، از جملهٔ اسلایدهای استاد، اسلایدهای طرح‌بندی، اسلایدها، استاد یادداشت‌ها، اسلایدهای یادداشت و استاد جزوه. |
| [setAllDateTimesText(String text)](#setAllDateTimesText-java.lang.String-) | متن را برای تمام نگهدارنده‌های تاریخ-زمان تنظیم می‌کند، از جملهٔ اسلایدهای استاد، اسلایدهای طرح‌بندی، اسلایدها، استاد یادداشت‌ها، اسلایدهای یادداشت و استاد جزوه. |
| [setVisibilityOnAllTitleSlides(boolean isVisible)](#setVisibilityOnAllTitleSlides-boolean-) | قابلیت مشاهدهٔ نگهدارنده‌های پاورقی، تاریخ-زمان و شماره صفحه را برای تمام اسلایدهای عنوان و اسلاید اولین طرح‌بندی تغییر می‌دهد. |

### setAllHeadersVisibility(boolean isVisible) {#setAllHeadersVisibility-boolean-}
```
public final void setAllHeadersVisibility(boolean isVisible)
```

تمام قابلیت مشاهدهٔ نگهدارنده‌های سرصفحه را تغییر می‌دهد، از جملهٔ استاد یادداشت‌ها، اسلایدهای یادداشت و استاد جزوه.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - نگهدارنده‌های سرصفحه را قابل مشاهده می‌کند، در غیر این صورت آن‌ها را مخفی می‌سازد. |

### setAllFootersVisibility(boolean isVisible) {#setAllFootersVisibility-boolean-}
```
public final void setAllFootersVisibility(boolean isVisible)
```

تمام قابلیت مشاهدهٔ نگهدارنده‌های پاورقی را تغییر می‌دهد، از جملهٔ اسلایدهای استاد، اسلایدهای طرح‌بندی، اسلایدها، استاد یادداشت‌ها، اسلایدهای یادداشت و استاد جزوه.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - نگهدارنده‌های پاورقی را قابل مشاهده می‌کند، در غیر این صورت آن‌ها را مخفی می‌سازد. |

### setAllSlideNumbersVisibility(boolean isVisible) {#setAllSlideNumbersVisibility-boolean-}
```
public final void setAllSlideNumbersVisibility(boolean isVisible)
```

تمام قابلیت مشاهدهٔ نگهدارنده‌های شماره صفحه را تغییر می‌دهد، از جملهٔ اسلایدهای استاد، اسلایدهای طرح‌بندی، اسلایدها، استاد یادداشت‌ها، اسلایدهای یادداشت و استاد جزوه.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - نگهدارنده‌های شماره صفحه را قابل مشاهده می‌کند، در غیر این صورت آن‌ها را مخفی می‌سازد. |

### setAllDateTimesVisibility(boolean isVisible) {#setAllDateTimesVisibility-boolean-}
```
public final void setAllDateTimesVisibility(boolean isVisible)
```

تمام قابلیت مشاهدهٔ نگهدارنده‌های تاریخ-زمان را تغییر می‌دهد، از جملهٔ اسلایدهای استاد، اسلایدهای طرح‌بندی، اسلایدها، استاد یادداشت‌ها، اسلایدهای یادداشت و استاد جزوه.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - نگهدارنده‌های تاریخ-زمان را قابل مشاهده می‌کند، در غیر این صورت آن‌ها را مخفی می‌سازد. |

### setAllHeadersText(String text) {#setAllHeadersText-java.lang.String-}
```
public final void setAllHeadersText(String text)
```

متن را برای تمام نگهدارنده‌های سرصفحه تنظیم می‌کند، از جملهٔ استاد یادداشت‌ها، اسلایدهای یادداشت و استاد جزوه.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| text | java.lang.String | متنی که باید تنظیم شود. |

### setAllFootersText(String text) {#setAllFootersText-java.lang.String-}
```
public final void setAllFootersText(String text)
```

متن را برای تمام نگهدارنده‌های پاورقی تنظیم می‌کند، از جملهٔ اسلایدهای استاد، اسلایدهای طرح‌بندی، اسلایدها، استاد یادداشت‌ها، اسلایدهای یادداشت و استاد جزوه.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| text | java.lang.String | متنی که باید تنظیم شود. |

### setAllDateTimesText(String text) {#setAllDateTimesText-java.lang.String-}
```
public final void setAllDateTimesText(String text)
```

متن را برای تمام نگهدارنده‌های تاریخ-زمان تنظیم می‌کند، از جملهٔ اسلایدهای استاد، اسلایدهای طرح‌بندی، اسلایدها، استاد یادداشت‌ها، اسلایدهای یادداشت و استاد جزوه.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| text | java.lang.String | متنی که باید تنظیم شود. |

### setVisibilityOnAllTitleSlides(boolean isVisible) {#setVisibilityOnAllTitleSlides-boolean-}
```
public final void setVisibilityOnAllTitleSlides(boolean isVisible)
```

قابلیت مشاهدهٔ نگهدارنده‌های پاورقی، تاریخ-زمان و شماره صفحه را برای تمام اسلایدهای عنوان و اسلاید اولین طرح‌بندی تغییر می‌دهد. اسلایدهای عنوان \u2013 اسلایدهایی که بر پایهٔ اولین اسلاید طرح‌بندی هستند (بدون در نظر گرفتن نوع این اولین طرح‌بندی).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - نگهدارنده‌ها را قابل مشاهده می‌کند، در غیر این صورت آن‌ها را مخفی می‌سازد. |