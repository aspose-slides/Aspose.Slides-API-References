---
title: NormalViewRestoredProperties
second_title: Aspose.Slides برای Java مرجع API
description: اندازه‌گیری ناحیه اسلاید را مشخص می‌کند؛ عرض هنگام اینکه فرزند restoredTop باشد، ارتفاع هنگام اینکه فرزند restoredLeft باشد، در نمای عادی هنگامی که ناحیه دارای اندازه متغیر restored باشد (نه حداقل‌شده نه حداکثر‌شده).
type: docs
url: /fa/com.aspose.slides/normalviewrestoredproperties/
---
**ارث‌بری:**
java.lang.Object

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)
```
public class NormalViewRestoredProperties implements INormalViewRestoredProperties
```

اندازه‌گیری ناحیهٔ اسلاید را مشخص می‌کند ((عرض وقتی فرزند restoredTop، ارتفاع وقتی فرزند restoredLeft) در نمای معمولی، هنگامی که ناحیه به اندازهٔ متغیر restored (نه حداقل‌شده نه حداکثر‌شده) است).
## متدها

| متد | توضیح |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | اندازهٔ ناحیهٔ اسلاید را مشخص می‌کند (عرض وقتی فرزند RestoredTop، ارتفاع وقتی فرزند RestoredLeft). |
| [setDimensionSize(float value)](#setDimensionSize-float-) | اندازهٔ ناحیهٔ اسلاید را مشخص می‌کند (عرض وقتی فرزند RestoredTop، ارتفاع وقتی فرزند RestoredLeft). |
| [getAutoAdjust()](#getAutoAdjust--) | مشخص می‌کند آیا اندازهٔ ناحیهٔ محتوای جانبی باید برای اندازهٔ جدید جبران کند هنگام تغییر اندازهٔ پنجرهٔ حاوی نما در برنامه. خواندنی/نوشتنی boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | مشخص می‌کند آیا اندازهٔ ناحیهٔ محتوای جانبی باید برای اندازهٔ جدید جبران کند هنگام تغییر اندازهٔ پنجرهٔ حاوی نما در برنامه. خواندنی/نوشتنی boolean. |
### getDimensionSize() {#getDimensionSize--}
```
public final float getDimensionSize()
```

اندازهٔ ناحیهٔ اسلاید را مشخص می‌کند (عرض وقتی فرزند RestoredTop، ارتفاع وقتی فرزند RestoredLeft). خواندنی/نوشتنی float.

**بازگشت:**
float
### setDimensionSize(float value) {#setDimensionSize-float-}
```
public final void setDimensionSize(float value)
```

اندازهٔ ناحیهٔ اسلاید را مشخص می‌کند (عرض وقتی فرزند RestoredTop، ارتفاع وقتی فرزند RestoredLeft). خواندنی/نوشتنی float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public final boolean getAutoAdjust()
```

مشخص می‌کند آیا اندازهٔ ناحیهٔ محتوای جانبی باید برای اندازهٔ جدید جبران کند هنگام تغییر اندازهٔ پنجرهٔ حاوی نما در برنامه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public final void setAutoAdjust(boolean value)
```

مشخص می‌کند آیا اندازهٔ ناحیهٔ محتوای جانبی باید برای اندازهٔ جدید جبران کند هنگام تغییر اندازهٔ پنجرهٔ حاوی نما در برنامه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |