---
title: ConnectorLock
second_title: مرجع API Aspose.Slides برای جاوا
description: تشخیص می‌دهد کدام عملیات‌ها در Connector والد غیرفعال هستند.
type: docs
url: /fa/com.aspose.slides/connectorlock/
---
**Inheritance:**
java.lang.Object, [com.aspose.slides.BaseShapeLock](../../com.aspose.slides/baseshapelock)

**All Implemented Interfaces:**
[com.aspose.slides.IConnectorLock](../../com.aspose.slides/iconnectorlock)
```
public class ConnectorLock extends BaseShapeLock implements IConnectorLock
```

تشخیص می‌دهد که کدام عملیات‌ها در Connector والد غیرفعال هستند.
## متدها

| Method | Description |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | تشخیص می‌دهد که افزودن این شکل به یک گروه ممنوع است. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | تشخیص می‌دهد که افزودن این شکل به یک گروه ممنوع است. |
| [getSelectLocked()](#getSelectLocked--) | تشخیص می‌دهد که انتخاب این شکل ممنوع است. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | تشخیص می‌دهد که انتخاب این شکل ممنوع است. |
| [getRotateLocked()](#getRotateLocked--) | تشخیص می‌دهد که تغییر زاویه چرخش این شکل ممنوع است. |
| [setRotateLocked(boolean value)](#setRotateLocked-boolean-) | تشخیص می‌دهد که تغییر زاویه چرخش این شکل ممنوع است. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | تشخیص می‌دهد که شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | تشخیص می‌دهد که شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. |
| [getPositionMove()](#getPositionMove--) | تشخیص می‌دهد که جابجایی این شکل ممنوع است. |
| [setPositionMove(boolean value)](#setPositionMove-boolean-) | تشخیص می‌دهد که جابجایی این شکل ممنوع است. |
| [getSizeLocked()](#getSizeLocked--) | تشخیص می‌دهد که تغییر اندازه این شکل ممنوع است. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | تشخیص می‌دهد که تغییر اندازه این شکل ممنوع است. |
| [getEditPointsLocked()](#getEditPointsLocked--) | تشخیص می‌دهد که تغییر مستقیم کانتور این شکل ممنوع است. |
| [setEditPointsLocked(boolean value)](#setEditPointsLocked-boolean-) | تشخیص می‌دهد که تغییر مستقیم کانتور این شکل ممنوع است. |
| [getAdjustHandlesLocked()](#getAdjustHandlesLocked--) | تشخیص می‌دهد که تغییر مقادیر تنظیم ممنوع است. |
| [setAdjustHandlesLocked(boolean value)](#setAdjustHandlesLocked-boolean-) | تشخیص می‌دهد که تغییر مقادیر تنظیم ممنوع است. |
| [getArrowheadsLocked()](#getArrowheadsLocked--) | تشخیص می‌دهد که تغییر سرهای فلش ممنوع است. |
| [setArrowheadsLocked(boolean value)](#setArrowheadsLocked-boolean-) | تشخیص می‌دهد که تغییر سرهای فلش ممنوع است. |
| [getShapeTypeLocked()](#getShapeTypeLocked--) | تشخیص می‌دهد که تغییر نوع شکل ممنوع است. |
| [setShapeTypeLocked(boolean value)](#setShapeTypeLocked-boolean-) | تشخیص می‌دهد که تغییر نوع شکل ممنوع است. |
### getGroupingLocked() {#getGroupingLocked--}
```
public boolean getGroupingLocked()
```

تشخیص می‌دهد که افزودن این شکل به یک گروه ممنوع است. بولی قابل خواندن/نوشتن.

**بازگشت:**
boolean
### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public void setGroupingLocked(boolean value)
```

تشخیص می‌دهد که افزودن این شکل به یک گروه ممنوع است. بولی قابل خواندن/نوشتن.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getSelectLocked() {#getSelectLocked--}
```
public boolean getSelectLocked()
```

تشخیص می‌دهد که انتخاب این شکل ممنوع است. بولی قابل خواندن/نوشتن.

**بازگشت:**
boolean
### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public void setSelectLocked(boolean value)
```

تشخیص می‌دهد که انتخاب این شکل ممنوع است. بولی قابل خواندن/نوشتن.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getRotateLocked() {#getRotateLocked--}
```
public boolean getRotateLocked()
```

تشخیص می‌دهد که تغییر زاویه چرخش این شکل ممنوع است. بولی قابل خواندن/نوشتن.

**بازگشت:**
boolean
### setRotateLocked(boolean value) {#setRotateLocked-boolean-}
```
public void setRotateLocked(boolean value)
```

تشخیص می‌دهد که تغییر زاویه چرخش این شکل ممنوع است. بولی قابل خواندن/نوشتن.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public boolean getAspectRatioLocked()
```

تشخیص می‌دهد که شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. بولی قابل خواندن/نوشتن.

**بازگشت:**
boolean
### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public void setAspectRatioLocked(boolean value)
```

تشخیص می‌دهد که شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. بولی قابل خواندن/نوشتن.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getPositionMove() {#getPositionMove--}
```
public boolean getPositionMove()
```

تشخیص می‌دهد که جابجایی این شکل ممنوع است. بولی قابل خواندن/نوشتن.

**بازگشت:**
boolean
### setPositionMove(boolean value) {#setPositionMove-boolean-}
```
public void setPositionMove(boolean value)
```

تشخیص می‌دهد که جابجایی این شکل ممنوع است. بولی قابل خواندن/نوشتن.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getSizeLocked() {#getSizeLocked--}
```
public boolean getSizeLocked()
```

تشخیص می‌دهد که تغییر اندازه این شکل ممنوع است. بولی قابل خواندن/نوشتن.

**بازگشت:**
boolean
### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public void setSizeLocked(boolean value)
```

تشخیص می‌دهد که تغییر اندازه این شکل ممنوع است. بولی قابل خواندن/نوشتن.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getEditPointsLocked() {#getEditPointsLocked--}
```
public boolean getEditPointsLocked()
```

تشخیص می‌دهد که تغییر مستقیم کانتور این شکل ممنوع است. بولی قابل خواندن/نوشتن.

**بازگشت:**
boolean
### setEditPointsLocked(boolean value) {#setEditPointsLocked-boolean-}
```
public void setEditPointsLocked(boolean value)
```

تشخیص می‌دهد که تغییر مستقیم کانتور این شکل ممنوع است. بولی قابل خواندن/نوشتن.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getAdjustHandlesLocked() {#getAdjustHandlesLocked--}
```
public boolean getAdjustHandlesLocked()
```

تشخیص می‌دهد که تغییر مقادیر تنظیم ممنوع است. بولی قابل خواندن/نوشتن.

**بازگشت:**
boolean
### setAdjustHandlesLocked(boolean value) {#setAdjustHandlesLocked-boolean-}
```
public void setAdjustHandlesLocked(boolean value)
```

تشخیص می‌دهد که تغییر مقادیر تنظیم ممنوع است. بولی قابل خواندن/نوشتن.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getArrowheadsLocked() {#getArrowheadsLocked--}
```
public boolean getArrowheadsLocked()
```

تشخیص می‌دهد که تغییر سرهای فلش ممنوع است. بولی قابل خواندن/نوشتن.

**بازگشت:**
boolean
### setArrowheadsLocked(boolean value) {#setArrowheadsLocked-boolean-}
```
public void setArrowheadsLocked(boolean value)
```

تشخیص می‌دهد که تغییر سرهای فلش ممنوع است. بولی قابل خواندن/نوشتن.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getShapeTypeLocked() {#getShapeTypeLocked--}
```
public boolean getShapeTypeLocked()
```

تشخیص می‌دهد که تغییر نوع شکل ممنوع است. بولی قابل خواندن/نوشتن.

**بازگشت:**
boolean
### setShapeTypeLocked(boolean value) {#setShapeTypeLocked-boolean-}
```
public void setShapeTypeLocked(boolean value)
```

تشخیص می‌دهد که تغییر نوع شکل ممنوع است. بولی قابل خواندن/نوشتن.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |