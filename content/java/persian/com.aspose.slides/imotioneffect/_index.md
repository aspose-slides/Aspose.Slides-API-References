---
title: IMotionEffect
second_title: مرجع API Aspose.Slides برای جاوا
description: رفتار اثر حرکتی را نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/imotioneffect/
---
**All Implemented Interfaces:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IMotionEffect extends IBehavior
```

نشان‌دهندهٔ رفتار اثر حرکتی است.
## متدها

| متد | توضیح |
| --- | --- |
| [getFrom()](#getFrom--) | مختصات x/y را برای شروع انیمیشن تعیین می‌کند (به درصد). |
| [setFrom(Point2D.Float value)](#setFrom-java.awt.geom.Point2D.Float-) | مختصات x/y را برای شروع انیمیشن تعیین می‌کند (به درصد). |
| [getTo()](#getTo--) | موقعیت هدف برای اثر حرکتی انیمیشن را تعیین می‌کند (به درصد). |
| [setTo(Point2D.Float value)](#setTo-java.awt.geom.Point2D.Float-) | موقعیت هدف برای اثر حرکتی انیمیشن را تعیین می‌کند (به درصد). |
| [getBy()](#getBy--) | مقدار انحراف نسبی برای انیمیشن را توصیف می‌کند (به درصد). |
| [setBy(Point2D.Float value)](#setBy-java.awt.geom.Point2D.Float-) | مقدار انحراف نسبی برای انیمیشن را توصیف می‌کند (به درصد). |
| [getRotationCenter()](#getRotationCenter--) | مرکز چرخش مورد استفاده برای چرخاندن مسیر حرکتی به زاویه X را توصیف می‌کند. |
| [setRotationCenter(Point2D.Float value)](#setRotationCenter-java.awt.geom.Point2D.Float-) | مرکز چرخش مورد استفاده برای چرخاندن مسیر حرکتی به زاویه X را توصیف می‌کند. |
| [getOrigin()](#getOrigin--) | مشخص می‌کند مبدا مسیر حرکتی نسبت به چه چیزی است، مانند چیدمان اسلاید یا والد. |
| [setOrigin(int value)](#setOrigin-int-) | مشخص می‌کند مبدا مسیر حرکتی نسبت به چه چیزی است، مانند چیدمان اسلاید یا والد. |
| [getPath()](#getPath--) | ابتدایی مسیر را به همراه مختصات برای حرکت انیمیشن تعیین می‌کند. |
| [setPath(IMotionPath value)](#setPath-com.aspose.slides.IMotionPath-) | ابتدایی مسیر را به همراه مختصات برای حرکت انیمیشن تعیین می‌کند. |
| [getPathEditMode()](#getPathEditMode--) | مشخص می‌کند مسیر حرکتی هنگام جابجایی شکل چگونه حرکت می‌کند. |
| [setPathEditMode(int value)](#setPathEditMode-int-) | مشخص می‌کند مسیر حرکتی هنگام جابجایی شکل چگونه حرکت می‌کند. |
| [getAngle()](#getAngle--) | زاویه نسبی مسیر حرکتی را توصیف می‌کند. |
| [setAngle(float value)](#setAngle-float-) | زاویه نسبی مسیر حرکتی را توصیف می‌کند. |
### getFrom() {#getFrom--}
```
public abstract Point2D.Float getFrom()
```


مختصات x/y را برای شروع انیمیشن تعیین می‌کند (به درصد). خواندنی/نوشتنی `java.awt.geom.Point2D.Float`.

**باز می‌گردد:**
`java.awt.geom.Point2D.Float`
### setFrom(Point2D.Float value) {#setFrom-java.awt.geom.Point2D.Float-}
```
public abstract void setFrom(Point2D.Float value)
```


مختصات x/y را برای شروع انیمیشن تعیین می‌کند (به درصد). خواندنی/نوشتنی `java.awt.geom.Point2D.Float`.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | `java.awt.geom.Point2D.Float` |  |
### getTo() {#getTo--}
```
public abstract Point2D.Float getTo()
```


موقعیت هدف برای اثر حرکتی انیمیشن را تعیین می‌کند (به درصد). خواندنی/نوشتنی `java.awt.geom.Point2D.Float`.

**باز می‌گردد:**
`java.awt.geom.Point2D.Float`
### setTo(Point2D.Float value) {#setTo-java.awt.geom.Point2D.Float-}
```
public abstract void setTo(Point2D.Float value)
```


موقعیت هدف برای اثر حرکتی انیمیشن را تعیین می‌کند (به درصد). خواندنی/نوشتنی `java.awt.geom.Point2D.Float`.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | `java.awt.geom.Point2D.Float` |  |
### getBy() {#getBy--}
```
public abstract Point2D.Float getBy()
```


مقدار انحراف نسبی برای انیمیشن را توصیف می‌کند (به درصد). خواندنی/نوشتنی `java.awt.geom.Point2D.Float`.

**باز می‌گردد:**
`java.awt.geom.Point2D.Float`
### setBy(Point2D.Float value) {#setBy-java.awt.geom.Point2D.Float-}
```
public abstract void setBy(Point2D.Float value)
```


مقدار انحراف نسبی برای انیمیشن را توصیف می‌کند (به درصد). خواندنی/نوشتنی `java.awt.geom.Point2D.Float`.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | `java.awt.geom.Point2D.Float` |  |
### getRotationCenter() {#getRotationCenter--}
```
public abstract Point2D.Float getRotationCenter()
```


مرکز چرخش مورد استفاده برای چرخاندن مسیر حرکتی به زاویه X را توصیف می‌کند. خواندنی/نوشتنی `java.awt.geom.Point2D.Float`.

**باز می‌گردد:**
`java.awt.geom.Point2D.Float`
### setRotationCenter(Point2D.Float value) {#setRotationCenter-java.awt.geom.Point2D.Float-}
```
public abstract void setRotationCenter(Point2D.Float value)
```


مرکز چرخش مورد استفاده برای چرخاندن مسیر حرکتی به زاویه X را توصیف می‌کند. خواندنی/نوشتنی `java.awt.geom.Point2D.Float`.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | `java.awt.geom.Point2D.Float` |  |
### getOrigin() {#getOrigin--}
```
public abstract int getOrigin()
```


مشخص می‌کند مبدا مسیر حرکتی نسبت به چه چیزی است، مانند چیدمان اسلاید یا والد. خواندنی/نوشتنی [MotionOriginType](../../com.aspose.slides/motionorigintype).

**باز می‌گردد:**
int
### setOrigin(int value) {#setOrigin-int-}
```
public abstract void setOrigin(int value)
```


مشخص می‌کند مبدا مسیر حرکتی نسبت به چه چیزی است، مانند چیدمان اسلاید یا والد. خواندنی/نوشتنی [MotionOriginType](../../com.aspose.slides/motionorigintype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |
### getPath() {#getPath--}
```
public abstract IMotionPath getPath()
```


ابتدایی مسیر را به همراه مختصات برای حرکت انیمیشن تعیین می‌کند. خواندنی/نوشتنی [IMotionPath](../../com.aspose.slides/imotionpath).

**باز می‌گردد:**
[IMotionPath](../../com.aspose.slides/imotionpath)
### setPath(IMotionPath value) {#setPath-com.aspose.slides.IMotionPath-}
```
public abstract void setPath(IMotionPath value)
```


ابتدایی مسیر را به همراه مختصات برای حرکت انیمیشن تعیین می‌کند. خواندنی/نوشتنی [IMotionPath](../../com.aspose.slides/imotionpath).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | [IMotionPath](../../com.aspose.slides/imotionpath) |  |
### getPathEditMode() {#getPathEditMode--}
```
public abstract int getPathEditMode()
```


مشخص می‌کند مسیر حرکتی هنگام جابجایی شکل چگونه حرکت می‌کند. خواندنی/نوشتنی [MotionPathEditMode](../../com.aspose.slides/motionpatheditmode).

**باز می‌گردد:**
int
### setPathEditMode(int value) {#setPathEditMode-int-}
```
public abstract void setPathEditMode(int value)
```


مشخص می‌کند مسیر حرکتی هنگام جابجایی شکل چگونه حرکت می‌کند. خواندنی/نوشتنی [MotionPathEditMode](../../com.aspose.slides/motionpatheditmode).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |
### getAngle() {#getAngle--}
```
public abstract float getAngle()
```


زاویه نسبی مسیر حرکتی را توصیف می‌کند. خواندنی/نوشتنی float.

**باز می‌گردد:**
float
### setAngle(float value) {#setAngle-float-}
```
public abstract void setAngle(float value)
```


زاویه نسبی مسیر حرکتی را توصیف می‌کند. خواندنی/نوشتنی float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |