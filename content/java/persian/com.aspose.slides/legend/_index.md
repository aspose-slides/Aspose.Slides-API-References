---
title: Legend
second_title: Aspose.Slides برای جاوا مرجع API
description: خواص لگند نمودارها را نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/legend/
---
**وراثت:**  
java.lang.Object, com.aspose.slides.DomObject

**همه رابط‌های پیاده‌سازی‌شده:**  
[com.aspose.slides.ILegend](../../com.aspose.slides/ilegend)  
```
public class Legend extends DomObject<Chart> implements ILegend
```

خواص لگند نمودار را نشان می‌دهد.

## متدها

| متد | توضیح |
| --- | --- |
| [getX()](#getX--) | مقدار x لگند را که به صورت یک کسر از عرض نمودار است برمی‌گرداند یا تنظیم می‌کند. |
| [setX(float value)](#setX-float-) | مقدار x لگند را که به صورت یک کسر از عرض نمودار است برمی‌گرداند یا تنظیم می‌کند. |
| [getY()](#getY--) | مقدار y لگند را که به صورت یک کسر از ارتفاع نمودار است برمی‌گرداند یا تنظیم می‌کند. |
| [setY(float value)](#setY-float-) | مقدار y لگند را که به صورت یک کسر از ارتفاع نمودار است برمی‌گرداند یا تنظیم می‌کند. |
| [getWidth()](#getWidth--) | عرض لگند را که به صورت یک کسر از عرض نمودار است برمی‌گرداند یا تنظیم می‌کند. |
| [setWidth(float value)](#setWidth-float-) | عرض لگند را که به صورت یک کسر از عرض نمودار است برمی‌گرداند یا تنظیم می‌کند. |
| [getHeight()](#getHeight--) | ارتفاع لگند را که به صورت یک کسر از ارتفاع نمودار است برمی‌گرداند یا تنظیم می‌کند. |
| [setHeight(float value)](#setHeight-float-) | ارتفاع لگند را که به صورت یک کسر از ارتفاع نمودار است برمی‌گرداند یا تنظیم می‌کند. |
| [getRight()](#getRight--) | راست. |
| [getBottom()](#getBottom--) | پایین. |
| [getOverlay()](#getOverlay--) | تعیین می‌کند که آیا عناصر دیگر نمودار می‌توانند بر روی لگند همپوشانی داشته باشند یا نه. |
| [setOverlay(boolean value)](#setOverlay-boolean-) | تعیین می‌کند که آیا عناصر دیگر نمودار می‌توانند بر روی لگند همپوشانی داشته باشند یا نه. |
| [getTextFormat()](#getTextFormat--) | قالب متن. |
| [getPosition()](#getPosition--) | موقعیت لگند را روی نمودار مشخص می‌کند. |
| [setPosition(int value)](#setPosition-int-) | موقعیت لگند را روی نمودار مشخص می‌کند. |
| [getFormat()](#getFormat--) | قالب یک لگند را برمی‌گرداند. |
| [getChart()](#getChart--) | نمودار را برمی‌گرداند. |
| [getEntries()](#getEntries--) | ورودی‌های لگند را دریافت می‌کند. |
| [getActualX()](#getActualX--) | مکان واقعی x (چپ) عنصر نمودار نسبت به گوشهٔ بالا-چپ نمودار را مشخص می‌کند. |
| [getActualY()](#getActualY--) | موقعیت واقعی بالای عنصر نمودار نسبت به گوشهٔ بالا-چپ نمودار را مشخص می‌کند. |
| [getActualWidth()](#getActualWidth--) | عرض واقعی عنصر نمودار را مشخص می‌کند. |
| [getActualHeight()](#getActualHeight--) | ارتفاع واقعی عنصر نمودار را مشخص می‌کند. |
| [getSlide()](#getSlide--) | اسلاید والد FillFormat را برمی‌گرداند. |
| [getPresentation()](#getPresentation--) | ارائه والد FillFormat را برمی‌گرداند. |

### getX() {#getX--}
```
public final float getX()
```

مقدار x لگند را که به صورت یک کسر از عرض نمودار است برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن float.

**بازگشت:**  
float

### setX(float value) {#setX-float-}
```
public final void setX(float value)
```

مقدار x لگند را که به صورت یک کسر از عرض نمودار است برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن float.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getY() {#getY--}
```
public final float getY()
```

مقدار y لگند را که به صورت یک کسر از ارتفاع نمودار است برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن float.

**بازگشت:**  
float

### setY(float value) {#setY-float-}
```
public final void setY(float value)
```

مقدار y لگند را که به صورت یک کسر از ارتفاع نمودار است برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن float.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getWidth() {#getWidth--}
```
public final float getWidth()
```

عرض لگند را که به صورت یک کسر از عرض نمودار است برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن float.

**بازگشت:**  
float

### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```

عرض لگند را که به صورت یک کسر از عرض نمودار است برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن float.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getHeight() {#getHeight--}
```
public final float getHeight()
```

ارتفاع لگند را که به صورت یک کسر از ارتفاع نمودار است برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن float.

**بازگشت:**  
float

### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```

ارتفاع لگند را که به صورت یک کسر از ارتفاع نمودار است برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن float.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getRight() {#getRight--}
```
public final float getRight()
```

راست. فقط‌خواندنی float.

**بازگشت:**  
float

### getBottom() {#getBottom--}
```
public final float getBottom()
```

پایین. فقط‌خواندنی float.

**بازگشت:**  
float

### getOverlay() {#getOverlay--}
```
public final boolean getOverlay()
```

تعیین می‌کند که آیا عناصر دیگر نمودار می‌توانند بر روی لگند همپوشانی داشته باشند یا نه. قابل خواندن/نوشتن boolean.

**بازگشت:**  
boolean

### setOverlay(boolean value) {#setOverlay-boolean-}
```
public final void setOverlay(boolean value)
```

تعیین می‌کند که آیا عناصر دیگر نمودار می‌توانند بر روی لگند همپوشانی داشته باشند یا نه. قابل خواندن/نوشتن boolean.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getTextFormat() {#getTextFormat--}
```
public final IChartTextFormat getTextFormat()
```

قالب متن. فقط‌خواندنی [IChartTextFormat](../../com.aspose.slides/icharttextformat).

**بازگشت:**  
[IChartTextFormat](../../com.aspose.slides/icharttextformat)

### getPosition() {#getPosition--}
```
public final int getPosition()
```

موقعیت لگند را روی نمودار مشخص می‌کند. مقادیر غیر-NaN از ویژگی‌های X، Y، Width، Height تأثیر این ویژگی را نادیده می‌گیرند. قابل خواندن/نوشتن [LegendPositionType](../../com.aspose.slides/legendpositiontype).

**بازگشت:**  
int

### setPosition(int value) {#setPosition-int-}
```
public final void setPosition(int value)
```

موقعیت لگند را روی نمودار مشخص می‌کند. مقادیر غیر-NaN از ویژگی‌های X، Y، Width، Height تأثیر این ویژگی را نادیده می‌گیرند. قابل خواندن/نوشتن [LegendPositionType](../../com.aspose.slides/legendpositiontype).

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

قالب یک لگند را برمی‌گرداند. فقط‌خواندنی [IFormat](../../com.aspose.slides/iformat).

**بازگشت:**  
[IFormat](../../com.aspose.slides/iformat)

### getChart() {#getChart--}
```
public final IChart getChart()
```

نمودار را برمی‌گرداند. فقط‌خواندنی [IChart](../../com.aspose.slides/ichart).

**بازگشت:**  
[IChart](../../com.aspose.slides/ichart)

### getEntries() {#getEntries--}
```
public final ILegendEntryCollection getEntries()
```

ورودی‌های لگند را دریافت می‌کند. فقط‌خواندنی [ILegendEntryCollection](../../com.aspose.slides/ilegendentrycollection).

**بازگشت:**  
[ILegendEntryCollection](../../com.aspose.slides/ilegendentrycollection)

### getActualX() {#getActualX--}
```
public final float getActualX()
```

موقعیت واقعی x (چپ) عنصر نمودار نسبت به گوشهٔ بالا-چپ نمودار را مشخص می‌کند. قبل از دریافت مقادیر واقعی، متد IChart.validateChartLayout() را صدا بزنید. خواندنی float.

**بازگشت:**  
float

### getActualY() {#getActualY--}
```
public final float getActualY()
```

موقعیت واقعی بالای عنصر نمودار نسبت به گوشهٔ بالا-چپ نمودار را مشخص می‌کند. قبل از دریافت مقادیر واقعی، متد IChart.validateChartLayout() را صدا بزنید. خواندنی float.

**بازگشت:**  
float

### getActualWidth() {#getActualWidth--}
```
public final float getActualWidth()
```

عرض واقعی عنصر نمودار را مشخص می‌کند. قبل از دریافت مقادیر واقعی، متد IChart.validateChartLayout() را صدا بزنید. خواندنی float.

**بازگشت:**  
float

### getActualHeight() {#getActualHeight--}
```
public final float getActualHeight()
```

ارتفاع واقعی عنصر نمودار را مشخص می‌کند. قبل از دریافت مقادیر واقعی، متد IChart.validateChartLayout() را صدا بزنید. خواندنی float.

**بازگشت:**  
float

### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

اسلاید والد FillFormat را برمی‌گرداند. فقط‌خواندنی [BaseSlide](../../com.aspose.slides/baseslide).

**بازگشت:**  
[IBaseSlide](../../com.aspose.slides/ibaseslide)

### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

ارائه والد FillFormat را برمی‌گرداند. فقط‌خواندنی [IPresentation](../../com.aspose.slides/ipresentation).

**بازگشت:**  
[IPresentation](../../com.aspose.slides/ipresentation)