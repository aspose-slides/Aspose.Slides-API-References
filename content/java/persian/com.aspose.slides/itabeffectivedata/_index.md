---
title: ITabEffectiveData
second_title: مرجع API Aspose.Slides برای جاوا
description: شیء غیرقابل تغییر که ویژگی‌های توقف تب‌بندی متن‌های مؤثر را شامل می‌شود.
type: docs
url: /fa/com.aspose.slides/itabeffectivedata/
---
**تمام رابط‌های پیاده‌سازی شده:**
java.lang.Comparable
```
public interface ITabEffectiveData extends Comparable
```

شیء غیرقابل تغییر که ویژگی‌های توقف تب‌بندی متن مؤثر را شامل می‌شود.

--------------------

این رابط به عنوان بخشی از [IParagraphFormatEffectiveData](../../com.aspose.slides/iparagraphformateffectivedata) مورد استفاده قرار می‌گیرد.
## متدها

| متد | توضیح |
| --- | --- |
| [getPosition()](#getPosition--) | موقعیت یک تب را باز می‌گرداند. |
| [getAlignment()](#getAlignment--) | سبک ترازبندی یک تب را باز می‌گرداند. |
### getPosition() {#getPosition--}
```
public abstract double getPosition()
```

موقعیت یک تب را باز می‌گرداند. اختصاص این ویژگی می‌تواند شاخص تب را در مجموعه تغییر داده و Enumerator را نامعتبر سازد. فقط-خواندنی double.

**باز می‌گردد:**
double
### getAlignment() {#getAlignment--}
```
public abstract int getAlignment()
```

سبک ترازبندی یک تب را باز می‌گرداند. فقط-خواندنی [TabAlignment](../../com.aspose.slides/tabalignment).

**باز می‌گردد:**
int