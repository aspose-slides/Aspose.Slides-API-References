---
title: IMasterThemeManager
second_title: مرجع API Aspose.Slides برای Java
description: دسترسی به تم اصلی ارائه را فراهم می‌کند.
type: docs
url: /fa/com.aspose.slides/imasterthememanager/
---
**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IThemeManager](../../com.aspose.slides/ithememanager)
```
public interface IMasterThemeManager extends IThemeManager
```

دسترسی به تم اصلی ارائه را فراهم می‌کند.
## متدها

| Method | Description |
| --- | --- |
| [isOverrideThemeEnabled()](#isOverrideThemeEnabled--) | تعیین می‌کند که آیا OverrideTheme تم مؤثر وارث‌شده (Presentation.MasterTheme) را بازنویسی می‌کند یا نه. |
| [setOverrideThemeEnabled(boolean value)](#setOverrideThemeEnabled-boolean-) | تعیین می‌کند که آیا OverrideTheme تم مؤثر وارث‌شده (Presentation.MasterTheme) را بازنویسی می‌کند یا نه. |
| [getOverrideTheme()](#getOverrideTheme--) | شیء تم بازنویسی‌شده را بر می‌گرداند. |
| [setOverrideTheme(IMasterTheme value)](#setOverrideTheme-com.aspose.slides.IMasterTheme-) | شیء تم بازنویسی‌شده را بر می‌گرداند. |
### isOverrideThemeEnabled() {#isOverrideThemeEnabled--}
```
public abstract boolean isOverrideThemeEnabled()
```

تعیین می‌کند که آیا OverrideTheme تم مؤثر وارث‌شده (Presentation.MasterTheme) را بازنویسی می‌کند یا نه. خواندنی/نوشتنی boolean.

**باز می‌گرداند:**
boolean
### setOverrideThemeEnabled(boolean value) {#setOverrideThemeEnabled-boolean-}
```
public abstract void setOverrideThemeEnabled(boolean value)
```

تعیین می‌کند که آیا OverrideTheme تم مؤثر وارث‌شده (Presentation.MasterTheme) را بازنویسی می‌کند یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getOverrideTheme() {#getOverrideTheme--}
```
public abstract IMasterTheme getOverrideTheme()
```

شیء تم بازنویسی‌شده را بر می‌گرداند. خواندنی/نوشتنی [IMasterTheme](../../com.aspose.slides/imastertheme).

**باز می‌گرداند:**
[IMasterTheme](../../com.aspose.slides/imastertheme)
### setOverrideTheme(IMasterTheme value) {#setOverrideTheme-com.aspose.slides.IMasterTheme-}
```
public abstract void setOverrideTheme(IMasterTheme value)
```

شیء تم بازنویسی‌شده را بر می‌گرداند. خواندنی/نوشتنی [IMasterTheme](../../com.aspose.slides/imastertheme).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | [IMasterTheme](../../com.aspose.slides/imastertheme) |  |