---
title: IFormat
second_title: Aspose.Slides برای Java API Reference
description: خواص قالب نمودار را نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/iformat/
---```
public interface IFormat
```

خواص قالب نمودار را نشان می‌دهد.
## متدها

| متد | توضیح |
| --- | --- |
| [getFill()](#getFill--) | خواص سبک پر کردن یک نمودار را بر می‌گرداند. |
| [getLine()](#getLine--) | خواص سبک خط یک نمودار را بر می‌گرداند. |
| [getEffect()](#getEffect--) | اثراتی که برای یک نمودار استفاده می‌شوند را بر می‌گرداند. |
| [getEffect3D()](#getEffect3D--) | قالب سه‌بعدی یک نمودار را بر می‌گرداند. |
### getFill() {#getFill--}
```
public abstract IFillFormat getFill()
```

خواص سبک پر کردن یک نمودار را بر می‌گرداند. فقط-خواندنی [IFillFormat](../../com.aspose.slides/ifillformat).

**بر می‌گرداند:**
[IFillFormat](../../com.aspose.slides/ifillformat)
### getLine() {#getLine--}
```
public abstract ILineFormat getLine()
```

خواص سبک خط یک نمودار را بر می‌گرداند. فقط-خواندنی [ILineFormat](../../com.aspose.slides/ilineformat).

**بر می‌گرداند:**
[ILineFormat](../../com.aspose.slides/ilineformat)
### getEffect() {#getEffect--}
```
public abstract IEffectFormat getEffect()
```

اثرات مورد استفاده در یک نمودار را بر می‌گرداند. فقط-خواندنی [IEffectFormat](../../com.aspose.slides/ieffectformat).

**بر می‌گرداند:**
[IEffectFormat](../../com.aspose.slides/ieffectformat)
### getEffect3D() {#getEffect3D--}
```
public abstract IThreeDFormat getEffect3D()
```

قالب سه‌بعدی یک نمودار را بر می‌گرداند. فقط-خواندنی [IThreeDFormat](../../com.aspose.slides/ithreedformat).

**بر می‌گرداند:**
[IThreeDFormat](../../com.aspose.slides/ithreedformat)