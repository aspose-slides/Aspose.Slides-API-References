---
title: PieSplitType
second_title: مستندات API Aspose.Slides برای جاوا
description: نوعی از نقاط تقسیم را در پای دوم یا نوار دوم در نمودار پای-در-پای یا نوار-در-پای توصیف می‌کند.
type: docs
url: /fa/com.aspose.slides/piesplittype/
---
**ارث‌بری:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class PieSplitType extends System.Enum
```

نوعی از نقاط تقسیم را در پای دوم یا نوار دوم در نمودار پای-در-پای یا نوار-در-پای توصیف می‌کند.
## فیلدها

| فیلد | توضیح |
| --- | --- |
| [Default](#Default) | نقطه‌های داده باید با استفاده از مکانیزم پیش‌فرض برای این نوع نمودار تقسیم شوند. |
| [Custom](#Custom) | نقطه‌های داده باید بر اساس مقادیر تقسیم‌بندی سفارشی بین پای اصلی و نمودار دوم تقسیم شوند. |
| [ByPercentage](#ByPercentage) | نقطه‌های داده باید با قرار دادن نقطه‌هایی که درصدشان کمتر از مقدار Split Position است، بین پای اصلی و نمودار دوم تقسیم شوند. |
| [ByPos](#ByPos) | نقطه‌های داده باید با قرار دادن آخرین موقعیت تقسیم (Split Position) نقاط داده در نمودار دوم، بین پای اصلی و نمودار دوم تقسیم شوند. |
| [ByValue](#ByValue) | نقطه‌های داده باید با قرار دادن نقاط داده‌ای که مقدارشان کمتر از Split Position است، بین پای اصلی و نمودار دوم تقسیم شوند. |

### Default {#Default}
```
public static final int Default
```

نقطه‌های داده باید با استفاده از مکانیزم پیش‌فرض برای این نوع نمودار تقسیم شوند.

### Custom {#Custom}
```
public static final int Custom
```

نقطه‌های داده باید بر اساس مقادیر تقسیم‌بندی سفارشی بین پای اصلی و نمودار دوم تقسیم شوند.

### ByPercentage {#ByPercentage}
```
public static final int ByPercentage
```

نقطه‌های داده باید با قرار دادن نقطه‌هایی که درصدشان کمتر از مقدار Split Position است، بین پای اصلی و نمودار دوم تقسیم شوند.

### ByPos {#ByPos}
```
public static final int ByPos
```

نقطه‌های داده باید با قرار دادن آخرین موقعیت تقسیم (Split Position) نقاط داده در نمودار دوم، بین پای اصلی و نمودار دوم تقسیم شوند.

### ByValue {#ByValue}
```
public static final int ByValue
```

نقطه‌های داده باید با قرار دادن نقاط داده‌ای که مقدارشان کمتر از Split Position است، بین پای اصلی و نمودار دوم تقسیم شوند.