---
title: IMotionCmdPath
second_title: Aspose.Slides for Java API Reference
description: Represent one command of a path.
type: docs
url: /fa/com.aspose.slides/imotioncmdpath/
---```
public interface IMotionCmdPath
```

یک فرمان از مسیر را نمایش می‌دهد.
## متدها

| متد | توضیح |
| --- | --- |
| [getPoints()](#getPoints--) | نقاط فرمان را مشخص می‌کند. |
| [setPoints(Point2D.Float[] value)](#setPoints-java.awt.geom.Point2D.Float---) | نقاط فرمان را مشخص می‌کند. |
| [getCommandType()](#getCommandType--) | نوع فرمان را مشخص می‌کند. |
| [setCommandType(int value)](#setCommandType-int-) | نوع فرمان را مشخص می‌کند. |
| [isRelative()](#isRelative--) | مختصات فرمان را به‌صورت نسبی یا غیر نسبی تعیین می‌کند. |
| [setRelative(boolean value)](#setRelative-boolean-) | مختصات فرمان را به‌صورت نسبی یا غیر نسبی تعیین می‌کند. |
| [getPointsType()](#getPointsType--) | نوع نقاط فرمان را مشخص می‌کند خواندنی/نوشتنی [MotionPathPointsType](../../com.aspose.slides/motionpathpointstype). |
| [setPointsType(int value)](#setPointsType-int-) | نوع نقاط فرمان را مشخص می‌کند خواندنی/نوشتنی [MotionPathPointsType](../../com.aspose.slides/motionpathpointstype). |
### getPoints() {#getPoints--}
```
public abstract Point2D.Float[] getPoints()
```


نقاط فرمان را مشخص می‌کند. خواندنی/نوشتنی java.awt.geom.Point2D.Float[].

**بازگشت:**
java.awt.geom.Point2D.Float[]
### setPoints(Point2D.Float[] value) {#setPoints-java.awt.geom.Point2D.Float---}
```
public abstract void setPoints(Point2D.Float[] value)
```


نقاط فرمان را مشخص می‌کند. خواندنی/نوشتنی java.awt.geom.Point2D.Float[].

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float[] |  |

### getCommandType() {#getCommandType--}
```
public abstract int getCommandType()
```


نوع فرمان را مشخص می‌کند. خواندنی/نوشتنی [MotionCommandPathType](../../com.aspose.slides/motioncommandpathtype).

**بازگشت:**
int
### setCommandType(int value) {#setCommandType-int-}
```
public abstract void setCommandType(int value)
```


نوع فرمان را مشخص می‌کند. خواندنی/نوشتنی [MotionCommandPathType](../../com.aspose.slides/motioncommandpathtype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### isRelative() {#isRelative--}
```
public abstract boolean isRelative()
```


مختصات فرمان را به‌صورت نسبی یا غیر نسبی تعیین می‌کند. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setRelative(boolean value) {#setRelative-boolean-}
```
public abstract void setRelative(boolean value)
```


مختصات فرمان را به‌صورت نسبی یا غیر نسبی تعیین می‌کند. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getPointsType() {#getPointsType--}
```
public abstract int getPointsType()
```


نوع نقاط فرمان را مشخص می‌کند خواندنی/نوشتنی [MotionPathPointsType](../../com.aspose.slides/motionpathpointstype).

**بازگشت:**
int
### setPointsType(int value) {#setPointsType-int-}
```
public abstract void setPointsType(int value)
```


نوع نقاط فرمان را مشخص می‌کند خواندنی/نوشتنی [MotionPathPointsType](../../com.aspose.slides/motionpathpointstype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |