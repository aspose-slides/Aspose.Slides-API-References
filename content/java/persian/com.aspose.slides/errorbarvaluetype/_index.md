---
title: ErrorBarValueType
second_title: مرجع API Aspose.Slides برای Java
description: نوع مقدار نوار خطا را نشان می‌دهد
type: docs
url: /fa/com.aspose.slides/errorbarvaluetype/
---
**وراثت:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class ErrorBarValueType extends System.Enum
```

نوع مقدار نوار خطا را نشان می‌دهد
## فیلدها

| فیلد | توضیح |
| --- | --- |
| [Custom](#Custom) | مشخص می‌کند که طول نوارهای خطا باید توسط عناصر Plus و Minus تعیین شود. |
| [Fixed](#Fixed) | مشخص می‌کند که طول نوارهای خطا باید مقدار ثابت تعیین‌شده توسط Error Bar Value باشد. |
| [Percentage](#Percentage) | مشخص می‌کند که طول نوارهای خطا باید درصد Error Bar Value از داده باشد. |
| [StandardDeviation](#StandardDeviation) | مشخص می‌کند که طول نوارهای خطا باید انحرافات معیار Error Bar Value از داده باشد. |
| [StandardError](#StandardError) | مشخص می‌کند که طول نوارهای خطا باید خطاهای استاندارد Error Bar Value از داده باشد. |
### Custom {#Custom}
```
public static final int Custom
```

مشخص می‌کند که طول نوارهای خطا باید توسط عناصر Plus و Minus تعیین شود.

### Fixed {#Fixed}
```
public static final int Fixed
```

مشخص می‌کند که طول نوارهای خطا باید مقدار ثابت تعیین‌شده توسط Error Bar Value باشد.

### Percentage {#Percentage}
```
public static final int Percentage
```

مشخص می‌کند که طول نوارهای خطا باید درصد Error Bar Value از داده باشد.

### StandardDeviation {#StandardDeviation}
```
public static final int StandardDeviation
```

مشخص می‌کند که طول نوارهای خطا باید انحرافات معیار Error Bar Value از داده باشد.

### StandardError {#StandardError}
```
public static final int StandardError
```

مشخص می‌کند که طول نوارهای خطا باید خطاهای استاندارد Error Bar Value از داده باشد.