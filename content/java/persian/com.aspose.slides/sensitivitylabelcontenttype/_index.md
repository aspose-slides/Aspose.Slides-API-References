---
title: SensitivityLabelContentType
second_title: Aspose.Slides برای Java مرجع API
description: انواع علامت‌گذاری محتوا که باید بر روی یک سند اعمال شوند را نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/sensitivitylabelcontenttype/
---
**ارث‌بری:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class SensitivityLabelContentType extends System.Enum
```

نمادهای انواع علامت‌گذاری محتوا که باید بر روی یک سند اعمال شوند.
## فیلدها

| فیلد | توضیح |
| --- | --- |
| [None](#None) | دلالت می‌کند که برچسب به‌صورت پیش‌فرض یا خودکار اعمال می‌شود. |
| [Header](#Header) | دلالت می‌کند که برچسب به‌صورت دستی اعمال شده است. |
| [Footer](#Footer) | دلالت می‌کند که برچسب به‌صورت دستی اعمال شده است. |
| [Watermark](#Watermark) | دلالت می‌کند که برچسب به‌صورت دستی اعمال شده است. |
| [Encryption](#Encryption) | دلالت می‌کند که برچسب به‌صورت دستی اعمال شده است. |
### None {#None}
```
public static final int None
```

دلالت می‌کند که برچسب به‌صورت پیش‌فرض یا خودکار اعمال می‌شود.

### Header {#Header}
```
public static final int Header
```

دلالت می‌کند که برچسب به‌صورت دستی اعمال شده است. برای هر برچسب حساسیتی که مستقیماً توسط کاربر اعمال شده استفاده شود.

### Footer {#Footer}
```
public static final int Footer
```

دلالت می‌کند که برچسب به‌صورت دستی اعمال شده است. برای هر برچسب حساسیتی که مستقیماً توسط کاربر اعمال شده استفاده شود.

### Watermark {#Watermark}
```
public static final int Watermark
```

دلالت می‌کند که برچسب به‌صورت دستی اعمال شده است. برای هر برچسب حساسیتی که مستقیماً توسط کاربر اعمال شده استفاده شود.

### Encryption {#Encryption}
```
public static final int Encryption
```

دلالت می‌کند که برچسب به‌صورت دستی اعمال شده است. برای هر برچسب حساسیتی که مستقیماً توسط کاربر اعمال شده استفاده شود.