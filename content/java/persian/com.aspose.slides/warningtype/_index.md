---
title: WarningType
second_title: مرجع API Aspose.Slides برای جاوا
description: یک نوع هشدار را نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/warningtype/
---
**وراثت:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class WarningType extends System.Enum
```

نوعی هشدار را نشان می‌دهد.
## فیلدها

| فیلد | توضیح |
| --- | --- |
| [SourceFileCorruption](#SourceFileCorruption) | در سند منبع مشکلی شناسایی شده است که به شدّت احتمال می‌دهد سند در صورت ذخیره شدن در قالب اصلی خود قادر به باز شدن نباشد. |
| [DataLoss](#DataLoss) | متن/نمودار/تصویر یا سایر داده‌ها به‌طور کامل از ساختار سند پس از بارگذاری یا سند ایجاد شده پس از ذخیره‌سازی حذف خواهند شد. |
| [MajorFormattingLoss](#MajorFormattingLoss) | از دست رفتن بزرگ قالب‌بندی. |
| [MinorFormattingLoss](#MinorFormattingLoss) | از دست رفتن جزئی قالب‌بندی. |
| [CompatibilityIssue](#CompatibilityIssue) | این یک مشکل شناخته‌شده است که باز شدن سند توسط برخی عوامل کاربری یا نسخه‌های قبلی آن‌ها را منع می‌کند. |
| [UnexpectedContent](#UnexpectedContent) | برخی محتوای موجود در سند منبع قابل شناسایی نبود (مثلاً |
### SourceFileCorruption {#SourceFileCorruption}
```
public static final int SourceFileCorruption
```

در سند منبع مشکلی شناسایی شده است که به شدّت احتمال می‌دهد سند در صورت ذخیره شدن در قالب اصلی خود قادر به باز شدن نباشد.

### DataLoss {#DataLoss}
```
public static final int DataLoss
```

متن/نمودار/تصویر یا سایر داده‌ها به‌طور کامل از ساختار سند پس از بارگذاری یا سند ایجاد شده پس از ذخیره‌سازی حذف خواهند شد.

### MajorFormattingLoss {#MajorFormattingLoss}
```
public static final int MajorFormattingLoss
```

از دست رفتن بزرگ قالب‌بندی.

### MinorFormattingLoss {#MinorFormattingLoss}
```
public static final int MinorFormattingLoss
```

از دست رفتن جزئی قالب‌بندی.

### CompatibilityIssue {#CompatibilityIssue}
```
public static final int CompatibilityIssue
```

این یک مشکل شناخته‌شده است که باز شدن سند توسط برخی عوامل کاربری یا نسخه‌های قبلی آن‌ها را منع می‌کند.

### UnexpectedContent {#UnexpectedContent}
```
public static final int UnexpectedContent
```

برخی محتویات در سند منبع قابل شناسایی نبودند (یعنی پشتیبانی نمی‌شوند)، این ممکن است باعث بروز مشکلات یا از دست رفتن داده/قالب‌بندی شود یا نشود.