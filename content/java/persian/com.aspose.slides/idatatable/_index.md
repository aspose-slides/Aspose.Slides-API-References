---
title: IDataTable
second_title: مرجع API Aspose.Slides برای جاوا
description: ویژگی‌های جدول داده را نمایش می‌دهد.
type: docs
url: /fa/com.aspose.slides/idatatable/
---
**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IFormattedTextContainer](../../com.aspose.slides/iformattedtextcontainer)
```
public interface IDataTable extends IFormattedTextContainer
```

نمایش ویژگی‌های جدول داده.

## متدها

| متد | توضیح |
| --- | --- |
| [getFormat()](#getFormat--) | خط، پرکننده و سبک‌های اثر یک شیء را برمی‌گرداند. |
| [hasBorderHorizontal()](#hasBorderHorizontal--) | اگر جدول داده نمودار دارای حاشیه‌های افقی سلول باشد، true. |
| [setBorderHorizontal(boolean value)](#setBorderHorizontal-boolean-) | اگر جدول داده نمودار دارای حاشیه‌های افقی سلول باشد، true. |
| [hasBorderOutline()](#hasBorderOutline--) | اگر جدول داده نمودار دارای حاشیه‌های کلی سلول باشد، true. |
| [setBorderOutline(boolean value)](#setBorderOutline-boolean-) | اگر جدول داده نمودار دارای حاشیه‌های کلی سلول باشد، true. |
| [hasBorderVertical()](#hasBorderVertical--) | اگر جدول داده نمودار دارای حاشیه‌های عمودی سلول باشد، true. |
| [setBorderVertical(boolean value)](#setBorderVertical-boolean-) | اگر جدول داده نمودار دارای حاشیه‌های عمودی سلول باشد، true. |
| [getShowLegendKey()](#getShowLegendKey--) | اگر کلید افسانه برچسب داده قابل مشاهده باشد، true. |
| [setShowLegendKey(boolean value)](#setShowLegendKey-boolean-) | اگر کلید افسانه برچسب داده قابل مشاهده باشد، true. |

### getFormat() {#getFormat--}
```
public abstract IFormat getFormat()
```

خط، پرکننده و سبک‌های اثر یک شیء را برمی‌گرداند. فقط‌خواندنی [IFormat](../../com.aspose.slides/iformat).

**مقدار بازگشتی:**
[IFormat](../../com.aspose.slides/iformat)

### hasBorderHorizontal() {#hasBorderHorizontal--}
```
public abstract boolean hasBorderHorizontal()
```

اگر جدول داده نمودار دارای حاشیه‌های افقی سلول باشد، true. قابل‌خواندن و نوشتن boolean.

**مقدار بازگشتی:**
boolean

### setBorderHorizontal(boolean value) {#setBorderHorizontal-boolean-}
```
public abstract void setBorderHorizontal(boolean value)
```

اگر جدول داده نمودار دارای حاشیه‌های افقی سلول باشد، true. قابل‌خواندن و نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### hasBorderOutline() {#hasBorderOutline--}
```
public abstract boolean hasBorderOutline()
```

اگر جدول داده نمودار دارای حاشیه‌های کلی سلول باشد، true. قابل‌خواندن و نوشتن boolean.

**مقدار بازگشتی:**
boolean

### setBorderOutline(boolean value) {#setBorderOutline-boolean-}
```
public abstract void setBorderOutline(boolean value)
```

اگر جدول داده نمودار دارای حاشیه‌های کلی سلول باشد، true. قابل‌خواندن و نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### hasBorderVertical() {#hasBorderVertical--}
```
public abstract boolean hasBorderVertical()
```

اگر جدول داده نمودار دارای حاشیه‌های عمودی سلول باشد، true. قابل‌خواندن و نوشتن boolean.

**مقدار بازگشتی:**
boolean

### setBorderVertical(boolean value) {#setBorderVertical-boolean-}
```
public abstract void setBorderVertical(boolean value)
```

اگر جدول داده نمودار دارای حاشیه‌های عمودی سلول باشد، true. قابل‌خواندن و نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getShowLegendKey() {#getShowLegendKey--}
```
public abstract boolean getShowLegendKey()
```

اگر کلید افسانه برچسب داده قابل مشاهده باشد، true. قابل‌خواندن و نوشتن boolean.

**مقدار بازگشتی:**
boolean

### setShowLegendKey(boolean value) {#setShowLegendKey-boolean-}
```
public abstract void setShowLegendKey(boolean value)
```

اگر کلید افسانه برچسب داده قابل مشاهده باشد، true. قابل‌خواندن و نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |