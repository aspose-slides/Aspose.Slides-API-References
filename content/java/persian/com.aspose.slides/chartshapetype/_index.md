---
title: ChartShapeType
second_title: مرجع API Aspose.Slides برای جاوا
description: یک شکل از نمودار را نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/chartshapetype/
---
**ارث‌بری:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class ChartShapeType extends System.Enum
```

یک شکل از نمودار را نشان می‌دهد.
## فیلدها

| فیلد | توضیح |
| --- | --- |
| [NotDefined](#NotDefined) | شکل نمودار تعریف نشده است. |
| [Box](#Box) | مشخص می‌کند که نمودار باید با شکل جعبه‌ای رسم شود. |
| [Cone](#Cone) | مشخص می‌کند که نمودار باید به شکل مخروط رسم شود، به طوری که پایه مخروط بر روی کف باشد و نقطه مخروط در بالای نشانگر داده قرار گیرد. |
| [ConeToMax](#ConeToMax) | مشخص می‌کند که نمودار باید با مخروط‌های بریده شده رسم شود به‌طوری که نقطه مخروط برابر با حداکثر مقدار داده باشد. |
| [Cylinder](#Cylinder) | مشخص می‌کند که نمودار باید به شکل سیلندری رسم شود. |
| [Pyramid](#Pyramid) | مشخص می‌کند که نمودار باید به شکل هرم مستطیلی رسم شود، به طوری که پایه هرم بر روی کف باشد و نقطه هرم در بالای نشانگر داده قرار گیرد. |
| [PyramidToMaximum](#PyramidToMaximum) | مشخص می‌کند که نمودار باید با مخروط‌های بریده شده رسم شود به‌طوری که نقطه مخروط برابر با حداکثر مقدار داده باشد. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```

شکل نمودار تعریف نشده است.

### Box {#Box}
```
public static final int Box
```

مشخص می‌کند که نمودار باید با شکل جعبه‌ای رسم شود.

### Cone {#Cone}
```
public static final int Cone
```

مشخص می‌کند که نمودار باید به شکل مخروط رسم شود، به طوری که پایه مخروط بر روی کف باشد و نقطه مخروط در بالای نشانگر داده قرار گیرد.

### ConeToMax {#ConeToMax}
```
public static final int ConeToMax
```

مشخص می‌کند که نمودار باید با مخروط‌های بریده شده رسم شود به‌طوری که نقطه مخروط برابر با حداکثر مقدار داده باشد.

### Cylinder {#Cylinder}
```
public static final int Cylinder
```

مشخص می‌کند که نمودار باید به شکل سیلندری رسم شود.

### Pyramid {#Pyramid}
```
public static final int Pyramid
```

مشخص می‌کند که نمودار باید به شکل هرم مستطیلی رسم شود، به طوری که پایه هرم بر روی کف باشد و نقطه هرم در بالای نشانگر داده قرار گیرد.

### PyramidToMaximum {#PyramidToMaximum}
```
public static final int PyramidToMaximum
```

مشخص می‌کند که نمودار باید با مخروط‌های بریده شده رسم شود به‌طوری که نقطه مخروط برابر با حداکثر مقدار داده باشد.