---
title: CustomXmlPart
second_title: Aspose.Slides برای مرجع API جاوا
description: نمایش می‌دهد بخش XML سفارشی.
type: docs
url: /fa/com.aspose.slides/customxmlpart/
---
**وراثت:**
java.lang.Object

**تمام واسط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.ICustomXmlPart](../../com.aspose.slides/icustomxmlpart)
```
public class CustomXmlPart implements ICustomXmlPart
```

نمایش می‌دهد بخش xml سفارشی.
## متدها

| متد | شرح |
| --- | --- |
| [getXmlData()](#getXmlData--) | داده‌های xml را برمی‌گرداند یا تنظیم می‌کند. |
| [setXmlData(byte[] value)](#setXmlData-byte---) | داده‌های xml را برمی‌گرداند یا تنظیم می‌کند. |
| [getXmlAsString()](#getXmlAsString--) | داده‌های xml را به صورت رشته UTF-8 برمی‌گرداند یا تنظیم می‌کند. |
| [setXmlAsString(String value)](#setXmlAsString-java.lang.String-) | داده‌های xml را به صورت رشته UTF-8 برمی‌گرداند یا تنظیم می‌کند. |
| [getItemId()](#getItemId--) | یک شناسهٔ یکتا جهانی (GUID) که یک بخش xml سفارشی را در یک سند Office Open XML به‌صورت یکتا شناسایی می‌کند، مشخص می‌کند. |
| [setItemId(UUID value)](#setItemId-java.util.UUID-) | یک شناسهٔ یکتا جهانی (GUID) که یک بخش xml سفارشی را در یک سند Office Open XML به‌صورت یکتا شناسایی می‌کند، مشخص می‌کند. |
| [getNamespaceSchemas()](#getNamespaceSchemas--) | مجموعهٔ طرح‌های XML که با بخش xml سفارشی مرتبط‌اند را برمی‌گرداند. |
| [remove()](#remove--) | بخش xml سفارشی را از ارائه حذف می‌کند. |
### getXmlData() {#getXmlData--}
```
public final byte[] getXmlData()
```

داده‌های xml را برمی‌گرداند یا تنظیم می‌کند. خواندن/نوشتن byte[].

**بازگشت:**
byte[]
### setXmlData(byte[] value) {#setXmlData-byte---}
```
public final void setXmlData(byte[] value)
```

داده‌های xml را برمی‌گرداند یا تنظیم می‌کند. خواندن/نوشتن byte[].

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | byte[] |  |

### getXmlAsString() {#getXmlAsString--}
```
public final String getXmlAsString()
```

داده‌های xml را به صورت رشته UTF-8 برمی‌گرداند یا تنظیم می‌کند. خواندن/نوشتن String.

**بازگشت:**
java.lang.String
### setXmlAsString(String value) {#setXmlAsString-java.lang.String-}
```
public final void setXmlAsString(String value)
```

داده‌های xml را به صورت رشته UTF-8 برمی‌گرداند یا تنظیم می‌کند. خواندن/نوشتن String.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | java.lang.String |  |

### getItemId() {#getItemId--}
```
public final UUID getItemId()
```

یک شناسهٔ یکتا جهانی (GUID) که یک بخش xml سفارشی را در یک سند Office Open XML به‌صورت یکتا شناسایی می‌کند، مشخص می‌کند. فقط-خواندنی java.util.UUID.

**بازگشت:**
java.util.UUID
### setItemId(UUID value) {#setItemId-java.util.UUID-}
```
public final void setItemId(UUID value)
```

یک شناسهٔ یکتا جهانی (GUID) که یک بخش xml سفارشی را در یک سند Office Open XML به‌صورت یکتا شناسایی می‌کند، مشخص می‌کند. فقط-خواندنی java.util.UUID.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | java.util.UUID |  |

### getNamespaceSchemas() {#getNamespaceSchemas--}
```
public final String[] getNamespaceSchemas()
```

مجموعهٔ طرح‌های XML که با بخش xml سفارشی مرتبط‌اند را برمی‌گرداند. فقط-خواندنی String[].

**بازگشت:**
java.lang.String[]
### remove() {#remove--}
```
public final void remove()
```

بخش xml سفارشی را از ارائه حذف می‌کند.