---
title: TextSearchOptions
second_title: مرجع API Aspose.Slides برای جاوا
description: گزینه‌هایی را نشان می‌دهد که می‌توان برای جستجوی متن در یک Presentation Slide یا TextFrame استفاده کرد.
type: docs
url: /fa/com.aspose.slides/textsearchoptions/
---
**Inheritance:**
java.lang.Object

**All Implemented Interfaces:**
[com.aspose.slides.ITextSearchOptions](../../com.aspose.slides/itextsearchoptions)
```
public class TextSearchOptions implements ITextSearchOptions
```

گزینه‌هایی که می‌توان برای جستجوی متن در یک Presentation، Slide یا TextFrame استفاده کرد.

## Constructors

| سازنده | توضیح |
| --- | --- |
| [TextSearchOptions()](#TextSearchOptions--) | Creates new default text search options. |
## Methods

| متد | توضیح |
| --- | --- |
| [getCaseSensitive()](#getCaseSensitive--) | Set true to use case-sensitive search, false - otherwise. |
| [setCaseSensitive(boolean value)](#setCaseSensitive-boolean-) | Set true to use case-sensitive search, false - otherwise. |
| [getWholeWordsOnly()](#getWholeWordsOnly--) | Set true to match only whole words, false - otherwise. |
| [setWholeWordsOnly(boolean value)](#setWholeWordsOnly-boolean-) | Set true to match only whole words, false - otherwise. |
| [getIncludeNotes()](#getIncludeNotes--) | Set true to include text contained in slide notes ([NotesSlide](../../com.aspose.slides/notesslide)) when performing text search, replacement, or highlighting operations. |
| [setIncludeNotes(boolean value)](#setIncludeNotes-boolean-) | Set true to include text contained in slide notes ([NotesSlide](../../com.aspose.slides/notesslide)) when performing text search, replacement, or highlighting operations. |
### TextSearchOptions() {#TextSearchOptions--}
```
public TextSearchOptions()
```

یک گزینه جستجوی متن پیش‌فرض جدید می‌سازد.

### getCaseSensitive() {#getCaseSensitive--}
```
public final boolean getCaseSensitive()
```

در صورت true شدن، جستجوی حساس به حروف بزرگ/کوچک استفاده می‌شود، در غیر این صورت false. خواندنی/نوشتنی  boolean .

**بازگشت:**
boolean
### setCaseSensitive(boolean value) {#setCaseSensitive-boolean-}
```
public final void setCaseSensitive(boolean value)
```

در صورت true شدن، جستجوی حساس به حروف بزرگ/کوچک استفاده می‌شود، در غیر این صورت false. خواندنی/نوشتنی  boolean .

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getWholeWordsOnly() {#getWholeWordsOnly--}
```
public final boolean getWholeWordsOnly()
```

در صورت true شدن، فقط کلمات کامل مطابقت داده می‌شوند، در غیر این صورت false. خواندنی/نوشتنی  boolean .

**بازگشت:**
boolean
### setWholeWordsOnly(boolean value) {#setWholeWordsOnly-boolean-}
```
public final void setWholeWordsOnly(boolean value)
```

در صورت true شدن، فقط کلمات کامل مطابقت داده می‌شوند، در غیر این صورت false. خواندنی/نوشتنی  boolean .

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getIncludeNotes() {#getIncludeNotes--}
```
public final boolean getIncludeNotes()
```

در صورت true شدن، متن موجود در یادداشت‌های اسلاید ([NotesSlide](../../com.aspose.slides/notesslide)) هنگام انجام عملیات جستجوی متن، جایگزینی یا برجسته‌سازی گنجانده می‌شود. مقدار پیش‌فرض false است.

**بازگشت:**
boolean
### setIncludeNotes(boolean value) {#setIncludeNotes-boolean-}
```
public final void setIncludeNotes(boolean value)
```

در صورت true شدن، متن موجود در یادداشت‌های اسلاید ([NotesSlide](../../com.aspose.slides/notesslide)) هنگام انجام عملیات جستجوی متن، جایگزینی یا برجسته‌سازی گنجانده می‌شود. مقدار پیش‌فرض false است.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |