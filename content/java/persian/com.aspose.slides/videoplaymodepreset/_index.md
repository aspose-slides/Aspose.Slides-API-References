---
title: VideoPlayModePreset
second_title: مرجع API Aspose.Slides برای Java
description: ثابت‌هایی که نحوه پخش ویدئو را تعریف می‌کنند.
type: docs
url: /fa/com.aspose.slides/videoplaymodepreset/
---
**وراثت:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class VideoPlayModePreset extends System.Enum
```

ثابت‌هایی که نحوه پخش ویدئو را تعریف می‌کنند.
## فیلدها

| فیلد | توضیح |
| --- | --- |
| [Mixed](#Mixed) |  |
| [Auto](#Auto) | به‌صورت خودکار پخش می‌شود. |
| [OnClick](#OnClick) | فقط زمانی که قاب ویدئو کلیک شود پخش می‌شود. |
| [AllSlides](#AllSlides) | در تمام اسلایدها پخش می‌شود. |
| [InClickSequence](#InClickSequence) | با یک کلیک به‌صورت خودکار پخش می‌شود. |
### ترکیبی {#Mixed}
```
public static final int Mixed
```




### خودکار {#Auto}
```
public static final int Auto
```


به‌صورت خودکار پخش می‌شود.

### در_هنگام_کلیک {#OnClick}
```
public static final int OnClick
```


فقط زمانی که قاب ویدئو کلیک شود پخش می‌شود.

### تمام_اسلایدها {#AllSlides}
```
public static final int AllSlides
```


در تمام اسلایدها پخش می‌شود.

### در_توالی_کلیک {#InClickSequence}
```
public static final int InClickSequence
```


با یک کلیک به‌صورت خودکار پخش می‌شود.