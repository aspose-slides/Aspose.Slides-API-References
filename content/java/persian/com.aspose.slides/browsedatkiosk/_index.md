---
title: BrowsedAtKiosk
second_title: Aspose.Slides برای مرجع API جاوا
description: در کیوسک به صورت تمام صفحه مرور شد
type: docs
url: /fa/com.aspose.slides/browsedatkiosk/
---
**ارث‌بری:**
java.lang.Object, [com.aspose.slides.SlideShowType](../../com.aspose.slides/slideshowtype)
```
public class BrowsedAtKiosk extends SlideShowType
```

در کیوسک مرور شده (تمام صفحه)

--------------------

> ```
> Presentation pres = new Presentation();
>  try {
>      pres.getSlideShowSettings().setSlideShowType(new BrowsedAtKiosk());
>      pres.save("pres.pptx", SaveFormat.Pptx);
>  } finally {
>      if (pres != null) pres.dispose();
>  }
> ```
## سازنده‌ها

| سازنده | توضیحات |
| --- | --- |
| [BrowsedAtKiosk()](#BrowsedAtKiosk--) | یک نمونه جدید از کلاس BrowsedAtKiosk را مقداردهی اولیه می‌کند. |
### BrowsedAtKiosk() {#BrowsedAtKiosk--}
```
public BrowsedAtKiosk()
```

یک نمونه جدید از کلاس BrowsedAtKiosk را مقداردهی اولیه می‌کند.

--------------------

> ```
> Presentation pres = new Presentation();
>  try {
>      pres.getSlideShowSettings().setSlideShowType(new BrowsedAtKiosk());
>      pres.save("pres.pptx", SaveFormat.Pptx);
>  } finally {
>      if (pres != null) pres.dispose();
>  }
> ```