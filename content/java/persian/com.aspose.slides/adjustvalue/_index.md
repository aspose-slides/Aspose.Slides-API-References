---
title: AdjustValue
second_title: Aspose.Slides برای مرجع API جاوا
description: نمایانگر مقدار تنظیم یک شکل هندسی است.
type: docs
url: /fa/com.aspose.slides/adjustvalue/
---
**وراثت:**  
java.lang.Object

**تمام اینترفیس‌های پیاده‌سازی‌شده:**  
[com.aspose.slides.IAdjustValue](../../com.aspose.slides/iadjustvalue)  
```
public class AdjustValue implements IAdjustValue
```

نمایانگر مقدار تنظیم یک شکل هندسی است. این مقادیر بر فرم شکل تأثیر می‌گذارند.

## متدها

| متد | توضیح |
| --- | --- |
| [getRawValue()](#getRawValue--) | مقدار تنظیم را همان‌گونه برمی‌گرداند یا تنظیم می‌کند. |
| [setRawValue(long value)](#setRawValue-long-) | مقدار تنظیم را همان‌گونه برمی‌گرداند یا تنظیم می‌کند. |
| [getAngleValue()](#getAngleValue--) | مقدار را برمی‌گرداند یا تنظیم می‌کند، به عنوان زاویه‌ای به درجه تفسیر می‌شود. |
| [setAngleValue(float value)](#setAngleValue-float-) | مقدار را برمی‌گرداند یا تنظیم می‌کند، به عنوان زاویه‌ای به درجه تفسیر می‌شود. |
| [getName()](#getName--) | نام این مقدار تنظیم را برمی‌گرداند. |
| [getType()](#getType--) | نوع تنظیم شکل را برمی‌گرداند. |

### getRawValue() {#getRawValue--}
```
public final long getRawValue()
```

مقدار تنظیم را همان‌گونه برمی‌گرداند یا تنظیم می‌کند. خواندنی/قابل نوشتن long.

**بازگشت:**  
long

### setRawValue(long value) {#setRawValue-long-}
```
public final void setRawValue(long value)
```

مقدار تنظیم را همان‌گونه برمی‌گرداند یا تنظیم می‌کند. خواندنی/قابل نوشتن long.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | long |  |

### getAngleValue() {#getAngleValue--}
```
public final float getAngleValue()
```

مقدار را برمی‌گرداند یا تنظیم می‌کند، به عنوان زاویه‌ای به درجه تفسیر می‌شود. خواندنی/قابل نوشتن float.

**بازگشت:**  
float

### setAngleValue(float value) {#setAngleValue-float-}
```
public final void setAngleValue(float value)
```

مقدار را برمی‌گرداند یا تنظیم می‌کند، به عنوان زاویه‌ای به درجه تفسیر می‌شود. خواندنی/قابل نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getName() {#getName--}
```
public final String getName()
```

نام این مقدار تنظیم را برمی‌گرداند. فقط خواندنی String.

**بازگشت:**  
java.lang.String

### getType() {#getType--}
```
public final int getType()
```

نوع تنظیم شکل را برمی‌گرداند. فقط خواندنی [ShapeAdjustmentType](../../com.aspose.slides/shapeadjustmenttype).

**بازگشت:**  
int