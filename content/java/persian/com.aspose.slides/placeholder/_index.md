---
title: Placeholder
second_title: Aspose.Slides برای مرجع API جاوا
description: یک جای‌دار را در اسلاید نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/placeholder/
---
**ارث-بری:**
java.lang.Object

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IPlaceholder](../../com.aspose.slides/iplaceholder)
```
public class Placeholder implements IPlaceholder
```

یک نگهدارنده در اسلاید را نشان می‌دهد.
## متدها

| متد | توضیح |
| --- | --- |
| [getOrientation()](#getOrientation--) | orientation یک نگهدارنده را می‌گیرد یا تنظیم می‌کند. |
| [getSize()](#getSize--) | size یک نگهدارنده را می‌گیرد یا تنظیم می‌کند. |
| [getType()](#getType--) | type یک نگهدارنده را می‌گیرد یا تنظیم می‌کند. |
| [getIndex()](#getIndex--) | index یک نگهدارنده را می‌گیرد یا تنظیم می‌کند. |
### getOrientation() {#getOrientation--}
```
public byte getOrientation()
```


orientation یک نگهدارنده را می‌گیرد یا تنظیم می‌کند. فقط خواندنی [Orientation](../../com.aspose.slides/orientation).

**بازگشت:**
byte
### getSize() {#getSize--}
```
public byte getSize()
```


size یک نگهدارنده را می‌گیرد یا تنظیم می‌کند. فقط خواندنی [PlaceholderSize](../../com.aspose.slides/placeholdersize).

**بازگشت:**
byte
### getType() {#getType--}
```
public byte getType()
```


type یک نگهدارنده را می‌گیرد یا تنظیم می‌کند. فقط خواندنی [PlaceholderType](../../com.aspose.slides/placeholdertype).

**بازگشت:**
byte
### getIndex() {#getIndex--}
```
public long getIndex()
```


index یک نگهدارنده را می‌گیرد یا تنظیم می‌کند. فقط خواندنی long.

**بازگشت:**
long