---
title: GraphicalObjectLock
second_title: Aspose.Slides برای مرجع API جاوا
description: تشخیص می‌دهد کدام عملیات‌ها در شیء گرافیکی والد غیرفعال هستند.
type: docs
url: /fa/com.aspose.slides/graphicalobjectlock/
---
**وارثی:**
java.lang.Object, [com.aspose.slides.BaseShapeLock](../../com.aspose.slides/baseshapelock)

**تمام اینترفیس‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IGraphicalObjectLock](../../com.aspose.slides/igraphicalobjectlock)
```
public class GraphicalObjectLock extends BaseShapeLock implements IGraphicalObjectLock
```

تشخیص می‌دهد که کدام عملیات‌ها بر روی شیء گرافیکی والد غیرفعال هستند.
## متدها

| متد | توضیح |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | تعیین می‌کند که افزودن این شکل به گروه ممنوع است یا خیر. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | تعیین می‌کند که افزودن این شکل به گروه ممنوع است یا خیر. |
| [getDrilldownLocked()](#getDrilldownLocked--) | تعیین می‌کند که انتخاب زیرشکل‌های این شیء ممنوع است یا خیر. |
| [setDrilldownLocked(boolean value)](#setDrilldownLocked-boolean-) | تعیین می‌کند که انتخاب زیرشکل‌های این شیء ممنوع است یا خیر. |
| [getSelectLocked()](#getSelectLocked--) | تعیین می‌کند که انتخاب این شکل ممنوع است یا خیر. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | تعیین می‌کند که انتخاب این شکل ممنوع است یا خیر. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | تعیین می‌کند که شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | تعیین می‌کند که شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. |
| [getPositionLocked()](#getPositionLocked--) | تعیین می‌کند که جابه‌جایی این شکل ممنوع است یا خیر. |
| [setPositionLocked(boolean value)](#setPositionLocked-boolean-) | تعیین می‌کند که جابه‌جایی این شکل ممنوع است یا خیر. |
| [getSizeLocked()](#getSizeLocked--) | تعیین می‌کند که تغییر اندازه این شکل ممنوع است یا خیر. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | تعیین می‌کند که تغییر اندازه این شکل ممنوع است یا خیر. |
### getGroupingLocked() {#getGroupingLocked--}
```
public boolean getGroupingLocked()
```

تعیین می‌کند که افزودن این شکل به گروه ممنوع است یا خیر. قابل خواندن/قابل نوشتن boolean.

**برگشت:**
boolean
### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public void setGroupingLocked(boolean value)
```

تعیین می‌کند که افزودن این شکل به گروه ممنوع است یا خیر. قابل خواندن/قابل نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getDrilldownLocked() {#getDrilldownLocked--}
```
public boolean getDrilldownLocked()
```

تعیین می‌کند که انتخاب زیرشکل‌های این شیء ممنوع است یا خیر. قابل خواندن/قابل نوشتن boolean.

**برگشت:**
boolean
### setDrilldownLocked(boolean value) {#setDrilldownLocked-boolean-}
```
public void setDrilldownLocked(boolean value)
```

تعیین می‌کند که انتخاب زیرشکل‌های این شیء ممنوع است یا خیر. قابل خواندن/قابل نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getSelectLocked() {#getSelectLocked--}
```
public boolean getSelectLocked()
```

تعیین می‌کند که انتخاب این شکل ممنوع است یا خیر. قابل خواندن/قابل نوشتن boolean.

**برگشت:**
boolean
### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public void setSelectLocked(boolean value)
```

تعیین می‌کند که انتخاب این شکل ممنوع است یا خیر. قابل خواندن/قابل نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public boolean getAspectRatioLocked()
```

تعیین می‌کند که شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. قابل خواندن/قابل نوشتن boolean.

**برگشت:**
boolean
### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public void setAspectRatioLocked(boolean value)
```

تعیین می‌کند که شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. قابل خواندن/قابل نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getPositionLocked() {#getPositionLocked--}
```
public boolean getPositionLocked()
```

تعیین می‌کند که جابه‌جایی این شکل ممنوع است یا خیر. قابل خواندن/قابل نوشتن boolean.

**برگشت:**
boolean
### setPositionLocked(boolean value) {#setPositionLocked-boolean-}
```
public void setPositionLocked(boolean value)
```

تعیین می‌کند که جابه‌جایی این شکل ممنوع است یا خیر. قابل خواندن/قابل نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getSizeLocked() {#getSizeLocked--}
```
public boolean getSizeLocked()
```

تعیین می‌کند که تغییر اندازه این شکل ممنوع است یا خیر. قابل خواندن/قابل نوشتن boolean.

**برگشت:**
boolean
### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public void setSizeLocked(boolean value)
```

تعیین می‌کند که تغییر اندازه این شکل ممنوع است یا خیر. قابل خواندن/قابل نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |