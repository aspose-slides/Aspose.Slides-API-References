---
title: IInnerShadowEffectiveData
second_title: مرجع API Aspose.Slides برای Java
description: شیء غیرقابل تغییر که یک اثر سایه داخلی را نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/iinnershadoweffectivedata/
---
**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IEffectEffectiveData](../../com.aspose.slides/ieffecteffectivedata)
```
public interface IInnerShadowEffectiveData extends IEffectEffectiveData
```

شیء غیرقابل تغییر که یک اثر سایه داخلی را نشان می‌دهد.
## متدها

| Method | Description |
| --- | --- |
| [getBlurRadius()](#getBlurRadius--) | Blur radius. |
| [getDirection()](#getDirection--) | Direction of shadow. |
| [getDistance()](#getDistance--) | Distance of shadow. |
| [getShadowColor()](#getShadowColor--) | Color of shadow. |
### getBlurRadius() {#getBlurRadius--}
```
public abstract double getBlurRadius()
```

Blur radius. double فقط‌خواندنی.

**باز می‌گرداند:**
double
### getDirection() {#getDirection--}
```
public abstract float getDirection()
```

Direction of shadow. float فقط‌خواندنی.

**باز می‌گرداند:**
float
### getDistance() {#getDistance--}
```
public abstract double getDistance()
```

Distance of shadow. double فقط‌خواندنی.

**باز می‌گرداند:**
double
### getShadowColor() {#getShadowColor--}
```
public abstract Color getShadowColor()
```

Color of shadow. java.awt.Color فقط‌خواندنی.

**باز می‌گرداند:**
java.awt.Color