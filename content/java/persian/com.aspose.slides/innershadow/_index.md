---
title: InnerShadow
second_title: مرجع API Aspose.Slides برای Java
description: نمایانگر یک اثر سایه داخلی است.
type: docs
url: /fa/com.aspose.slides/innershadow/
---
**ارث‌بری:**
java.lang.Object

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IInnerShadow](../../com.aspose.slides/iinnershadow), com.aspose.slides.IVisualEffect, com.aspose.slides.IDOMObject, com.aspose.slides.IPVIObject, java.lang.Cloneable
```
public final class InnerShadow implements IInnerShadow, IVisualEffect, IDOMObject, IPVIObject, Cloneable
```

نمایانگر یک اثر سایه داخلی است.
## متدها

| متد | توضیح |
| --- | --- |
| [getBlurRadius()](#getBlurRadius--) | شعاع تاری. |
| [setBlurRadius(double value)](#setBlurRadius-double-) | شعاع تاری. |
| [getDirection()](#getDirection--) | جهت سایه. |
| [setDirection(float value)](#setDirection-float-) | جهت سایه. |
| [getDistance()](#getDistance--) | فاصله سایه. |
| [setDistance(double value)](#setDistance-double-) | فاصله سایه. |
| [getShadowColor()](#getShadowColor--) | رنگ سایه. |
| [getEffective()](#getEffective--) | داده‌های مؤثر اثر سایه داخلی را با اعمال وراثت دریافت می‌کند. |
| [getParent_Immediate()](#getParent-Immediate--) |  |
| [getVersion()](#getVersion--) |  |
| [getParent_IPresentationComponent()](#getParent-IPresentationComponent--) |  |
| [equals(Object obj)](#equals-java.lang.Object-) | تعیین می‌کند آیا [InnerShadow](../../com.aspose.slides/innershadow) مشخص شده برابر با [InnerShadow](../../com.aspose.slides/innershadow) فعلی است. |
| [hashCode()](#hashCode--) | به‌عنوان یک تابع هش برای یک نوع خاص عمل می‌کند. |
### getBlurRadius() {#getBlurRadius--}
```
public final double getBlurRadius()
```

شعاع تاری. قابل‌خواندن/قابل‌نوشتن double.

**باز می‌گردد:**
double
### setBlurRadius(double value) {#setBlurRadius-double-}
```
public final void setBlurRadius(double value)
```

شعاع تاری. قابل‌خواندن/قابل‌نوشتن double.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | double |  |
### getDirection() {#getDirection--}
```
public final float getDirection()
```

جهت سایه. قابل‌خواندن/قابل‌نوشتن float.

**باز می‌گردد:**
float
### setDirection(float value) {#setDirection-float-}
```
public final void setDirection(float value)
```

جهت سایه. قابل‌خواندن/قابل‌نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |
### getDistance() {#getDistance--}
```
public final double getDistance()
```

فاصله سایه. قابل‌خواندن/قابل‌نوشتن double.

**باز می‌گردد:**
double
### setDistance(double value) {#setDistance-double-}
```
public final void setDistance(double value)
```

فاصله سایه. قابل‌خواندن/قابل‌نوشتن double.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | double |  |
### getShadowColor() {#getShadowColor--}
```
public final IColorFormat getShadowColor()
```

رنگ سایه. فقط‌خواندنی [IColorFormat](../../com.aspose.slides/icolorformat).

**باز می‌گردد:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getEffective() {#getEffective--}
```
public final IInnerShadowEffectiveData getEffective()
```

داده‌های مؤثر اثر سایه داخلی را با اعمال وراثت دریافت می‌کند.

**باز می‌گردد:**
[IInnerShadowEffectiveData](../../com.aspose.slides/iinnershadoweffectivedata) - یک [IInnerShadowEffectiveData](../../com.aspose.slides/iinnershadoweffectivedata).
### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

شی Parent_Immediate را باز می‌گرداند. فقط‌خواندنی IDOMObject.

**باز می‌گردد:**
com.aspose.slides.IDOMObject
### getVersion() {#getVersion--}
```
public final long getVersion()
```

نسخه. فقط‌خواندنی long.

**باز می‌گردد:**
long
### getParent_IPresentationComponent() {#getParent-IPresentationComponent--}
```
public final IPresentationComponent getParent_IPresentationComponent()
```

شی IPresentationComponent مادر را باز می‌گرداند. فقط‌خواندنی [IPresentationComponent](../../com.aspose.slides/ipresentationcomponent).

**باز می‌گردد:**
[IPresentationComponent](../../com.aspose.slides/ipresentationcomponent)
### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```

تعیین می‌کند آیا [InnerShadow](../../com.aspose.slides/innershadow) مشخص شده برابر با [InnerShadow](../../com.aspose.slides/innershadow) فعلی است.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| obj | java.lang.Object | [InnerShadow](../../com.aspose.slides/innershadow) برای مقایسه. |

**باز می‌گردد:**
boolean - صحیح اگر اشیاء برابر باشند؛ در غیر این صورت، غلط.
### hashCode() {#hashCode--}
```
public int hashCode()
```

به‌عنوان یک تابع هش برای یک نوع خاص عمل می‌کند.

**باز می‌گردد:**
int - یک کد هش برای شیء فعلی.