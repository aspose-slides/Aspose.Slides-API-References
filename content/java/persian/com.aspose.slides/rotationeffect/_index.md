---
title: RotationEffect
second_title: Aspose.Slides برای Java مرجع API
description: رفتار چرخش اثر را نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/rotationeffect/
---
**ارث‌بری:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**تمام واسط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IRotationEffect](../../com.aspose.slides/irotationeffect)
```
public class RotationEffect extends Behavior implements IRotationEffect
```

رفتار چرخش اثر را نشان می‌دهد.
## سازنده‌ها

| سازنده | توضیح |
| --- | --- |
| [RotationEffect()](#RotationEffect--) |  |
## متدها

| متد | توضیح |
| --- | --- |
| [getFrom()](#getFrom--) | مقدار شروع انیمیشن را توصیف می‌کند. |
| [setFrom(float value)](#setFrom-float-) | مقدار شروع انیمیشن را توصیف می‌کند. |
| [getTo()](#getTo--) | مقدار پایان انیمیشن را توصیف می‌کند. |
| [setTo(float value)](#setTo-float-) | مقدار پایان انیمیشن را توصیف می‌کند. |
| [getBy()](#getBy--) | مقدار جابجایی نسبی برای انیمیشن را توصیف می‌کند. |
| [setBy(float value)](#setBy-float-) | مقدار جابجایی نسبی برای انیمیشن را توصیف می‌کند. |
### RotationEffect() {#RotationEffect--}
```
public RotationEffect()
```

### getFrom() {#getFrom--}
```
public final float getFrom()
```

مقدار شروع انیمیشن را توصیف می‌کند. float قابل خواندن/نوشتن.

**بازگشت:**
float
### setFrom(float value) {#setFrom-float-}
```
public final void setFrom(float value)
```

مقدار شروع انیمیشن را توصیف می‌کند. float قابل خواندن/نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getTo() {#getTo--}
```
public final float getTo()
```

مقدار پایان انیمیشن را توصیف می‌کند. float قابل خواندن/نوشتن.

**بازگشت:**
float
### setTo(float value) {#setTo-float-}
```
public final void setTo(float value)
```

مقدار پایان انیمیشن را توصیف می‌کند. float قابل خواندن/نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getBy() {#getBy--}
```
public final float getBy()
```

مقدار جابجایی نسبی برای انیمیشن را توصیف می‌کند. float قابل خواندن/نوشتن.

**بازگشت:**
float
### setBy(float value) {#setBy-float-}
```
public final void setBy(float value)
```

مقدار جابجایی نسبی برای انیمیشن را توصیف می‌کند. float قابل خواندن/نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |