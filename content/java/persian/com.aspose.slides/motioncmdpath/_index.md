---
title: MotionCmdPath
second_title: Aspose.Slides برای مرجع API جاوا
description: یک فرمان از مسیر را نمایش می‌دهد.
type: docs
url: /fa/com.aspose.slides/motioncmdpath/
---
**وراثت:**
java.lang.Object

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IMotionCmdPath](../../com.aspose.slides/imotioncmdpath)
```
public class MotionCmdPath implements IMotionCmdPath
```

یک فرمان از مسیر را نمایش می‌دهد.
## متدها

| متد | توضیح |
| --- | --- |
| [getPoints()](#getPoints--) | نقاط فرمان را مشخص می‌کند. |
| [setPoints(Point2D.Float[] value)](#setPoints-java.awt.geom.Point2D.Float---) | نقاط فرمان را مشخص می‌کند. |
| [getCommandType()](#getCommandType--) | نوع فرمان را مشخص می‌کند. |
| [setCommandType(int value)](#setCommandType-int-) | نوع فرمان را مشخص می‌کند. |
| [isRelative()](#isRelative--) | مختصات فرمان را نسبی یا غیرنسبی تعیین می‌کند. |
| [setRelative(boolean value)](#setRelative-boolean-) | مختصات فرمان را نسبی یا غیرنسبی تعیین می‌کند. |
| [getPointsType()](#getPointsType--) | نوع نقاط فرمان را مشخص می‌کند خواندن/نوشتن [MotionPathPointsType](../../com.aspose.slides/motionpathpointstype). |
| [setPointsType(int value)](#setPointsType-int-) | نوع نقاط فرمان را مشخص می‌کند خواندن/نوشتن [MotionPathPointsType](../../com.aspose.slides/motionpathpointstype). |
### getPoints() {#getPoints--}
```
public final Point2D.Float[] getPoints()
```

نقاط فرمان را مشخص می‌کند. خواندن/نوشتن java.awt.geom.Point2D.Float[].

**باز می‌گرداند:**
java.awt.geom.Point2D.Float[]
### setPoints(Point2D.Float[] value) {#setPoints-java.awt.geom.Point2D.Float---}
```
public final void setPoints(Point2D.Float[] value)
```

نقاط فرمان را مشخص می‌کند. خواندن/نوشتن java.awt.geom.Point2D.Float[].

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float[] |  |

### getCommandType() {#getCommandType--}
```
public final int getCommandType()
```

نوع فرمان را مشخص می‌کند. خواندن/نوشتن [MotionCommandPathType](../../com.aspose.slides/motioncommandpathtype).

**باز می‌گرداند:**
int
### setCommandType(int value) {#setCommandType-int-}
```
public final void setCommandType(int value)
```

نوع فرمان را مشخص می‌کند. خواندن/نوشتن [MotionCommandPathType](../../com.aspose.slides/motioncommandpathtype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### isRelative() {#isRelative--}
```
public final boolean isRelative()
```

مختصات فرمان را نسبی یا غیرنسبی تعیین می‌کند. خواندن/نوشتن boolean.

**باز می‌گرداند:**
boolean
### setRelative(boolean value) {#setRelative-boolean-}
```
public final void setRelative(boolean value)
```

مختصات فرمان را نسبی یا غیرنسبی تعیین می‌کند. خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getPointsType() {#getPointsType--}
```
public final int getPointsType()
```

نوع نقاط فرمان را مشخص می‌کند خواندن/نوشتن [MotionPathPointsType](../../com.aspose.slides/motionpathpointstype).

**باز می‌گرداند:**
int
### setPointsType(int value) {#setPointsType-int-}
```
public final void setPointsType(int value)
```

نوع نقاط فرمان را مشخص می‌کند خواندن/نوشتن [MotionPathPointsType](../../com.aspose.slides/motionpathpointstype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |