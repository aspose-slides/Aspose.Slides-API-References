---
title: BaseSlideHeaderFooterManager
second_title: مرجع API Aspose.Slides برای جاوا
description: مدیر را نشان می‌دهد که رفتار جای‌گیرهای پابرگ، تاریخ-زمان، شماره صفحه را برای تمام انواع اسلاید نگه می‌دارد.
type: docs
url: /fa/com.aspose.slides/baseslideheaderfootermanager/
---
**ارث‌برداری:**  
java.lang.Object, [com.aspose.slides.BaseHeaderFooterManager](../../com.aspose.slides/baseheaderfootermanager)  
```
public abstract class BaseSlideHeaderFooterManager extends BaseHeaderFooterManager
```

مدیری را نشان می‌دهد که رفتار جای‌گیرهای پابرگ، تاریخ-زمان، شماره صفحه را برای تمام انواع اسلاید نگه می‌دارد.

## متدها

| متد | توضیحات |
| --- | --- |
| [isFooterVisible()](#isFooterVisible--) | مقدار نشان‌دهنده وجود یک جای‌گیر پابرگ را برمی‌گرداند. |
| [isSlideNumberVisible()](#isSlideNumberVisible--) | مقدار نشان‌دهنده وجود یک جای‌گیر شماره صفحه را برمی‌گرداند. |
| [isDateTimeVisible()](#isDateTimeVisible--) | مقدار نشان‌دهنده وجود یک جای‌گیر تاریخ-زمان را برمی‌گرداند. |
| [setFooterVisibility(boolean isVisible)](#setFooterVisibility-boolean-) | قابلیت مشاهده‌ی جای‌گیر پابرگ اسلاید را تغییر می‌دهد. |
| [setSlideNumberVisibility(boolean isVisible)](#setSlideNumberVisibility-boolean-) | قابلیت مشاهده‌ی جای‌گیر شماره صفحه اسلاید را تغییر می‌دهد. |
| [setDateTimeVisibility(boolean isVisible)](#setDateTimeVisibility-boolean-) | قابلیت مشاهده‌ی جای‌گیر تاریخ-زمان اسلاید را تغییر می‌دهد. |
| [setFooterText(String text)](#setFooterText-java.lang.String-) | متن را برای جای‌گیر پابرگ اسلاید تنظیم می‌کند. |
| [setDateTimeText(String text)](#setDateTimeText-java.lang.String-) | متن را برای جای‌گیر تاریخ-زمان اسلاید تنظیم می‌کند. |

### isFooterVisible() {#isFooterVisible--}
```
public final boolean isFooterVisible()
```

مقداری را برمی‌گرداند که نشان می‌دهد یک جای‌گیر پابرگ موجود است. خواندن بولی.

**بازگشت:**  
boolean

### isSlideNumberVisible() {#isSlideNumberVisible--}
```
public final boolean isSlideNumberVisible()
```

مقداری را برمی‌گرداند که نشان می‌دهد یک جای‌گیر شماره صفحه موجود است. خواندن بولی.

**بازگشت:**  
boolean

### isDateTimeVisible() {#isDateTimeVisible--}
```
public final boolean isDateTimeVisible()
```

مقداری را برمی‌گرداند که نشان می‌دهد یک جای‌گیر تاریخ-زمان موجود است. خواندن بولی.

**بازگشت:**  
boolean

### setFooterVisibility(boolean isVisible) {#setFooterVisibility-boolean-}
```
public final void setFooterVisibility(boolean isVisible)
```

قابلیت مشاهده‌ی جای‌گیر پابرگ اسلاید را تغییر می‌دهد.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - باعث می‌شود جای‌گیر پابرگ قابل مشاهده باشد، در غیر این صورت پنهان می‌شود. |

### setSlideNumberVisibility(boolean isVisible) {#setSlideNumberVisibility-boolean-}
```
public final void setSlideNumberVisibility(boolean isVisible)
```

قابلیت مشاهده‌ی جای‌گیر شماره صفحه اسلاید را تغییر می‌دهد.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - باعث می‌شود جای‌گیر شماره صفحه قابل مشاهده باشد، در غیر این صورت پنهان می‌شود. |

### setDateTimeVisibility(boolean isVisible) {#setDateTimeVisibility-boolean-}
```
public final void setDateTimeVisibility(boolean isVisible)
```

قابلیت مشاهده‌ی جای‌گیر تاریخ-زمان اسلاید را تغییر می‌دهد.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - باعث می‌شود جای‌گیر تاریخ-زمان قابل مشاهده باشد، در غیر این صورت پنهان می‌شود. |

### setFooterText(String text) {#setFooterText-java.lang.String-}
```
public final void setFooterText(String text)
```

متن را برای جای‌گیر پابرگ اسلاید تنظیم می‌کند.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| text | java.lang.String | متنی که باید تنظیم شود. |

### setDateTimeText(String text) {#setDateTimeText-java.lang.String-}
```
public final void setDateTimeText(String text)
```

متن را برای جای‌گیر تاریخ-زمان اسلاید تنظیم می‌کند.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| text | java.lang.String | متنی که باید تنظیم شود. |