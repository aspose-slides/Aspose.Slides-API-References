---
title: SvgEvent
second_title: مرجع API Aspose.Slides برای جاوا
description: نمایانگر گزینه‌های شکل SVG است.
type: docs
url: /fa/com.aspose.slides/svgevent/
---
**ارث‌بری:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class SvgEvent extends System.Enum
```

نمایانگر گزینه‌های شکل SVG است.
## فیلدها

| فیلد | توضیح |
| --- | --- |
| [OnFocusIn](#OnFocusIn) | در زمان دریافت فوکوس توسط یک عنصر، مانند زمانی که متن انتخاب می‌شود، رخ می‌دهد. |
| [OnFocusOut](#OnFocusOut) | در زمان از دست دادن فوکوس توسط یک عنصر، مانند زمانی که متن انتخاب‌ شده لغو می‌شود، رخ می‌دهد. |
| [OnActivate](#OnActivate) | در زمان فعال شدن یک عنصر، به عنوان مثال از طریق کلیک موس یا فشار کلید، رخ می‌دهد. |
| [OnClick](#OnClick) | در زمان کلیک دکمه دستگاه اشاره‌گر بر روی یک عنصر، رخ می‌دهد. |
| [OnMouseDown](#OnMouseDown) | در زمان فشار دادن دکمه دستگاه اشاره‌گر بر روی یک عنصر، رخ می‌دهد. |
| [OnMouseUp](#OnMouseUp) | در زمان رها شدن دکمه دستگاه اشاره‌گر بر روی یک عنصر، رخ می‌دهد. |
| [OnMouseOver](#OnMouseOver) | در زمان حرکت دستگاه اشاره‌گر به سمت یک عنصر، رخ می‌دهد. |
| [OnMouseMove](#OnMouseMove) | در زمان حرکت دستگاه اشاره‌گر در حالی که بر روی یک عنصر قرار دارد، رخ می‌دهد. |
| [OnMouseOut](#OnMouseOut) | در زمان دور شدن دستگاه اشاره‌گر از یک عنصر، رخ می‌دهد. |
| [OnLoad](#OnLoad) | در زمان اینکه کارگزار کاربر عنصر و فرزندان آن و تمام منابع مورداستناد را به طور کامل تجزیه کرده باشد، که برای رندر آن لازم است، رخ می‌دهد. |
| [OnUnload](#OnUnload) | در زمان حذف یک سند توسط پیاده‌سازی DOM از یک پنجره یا فریم، رخ می‌دهد. |
| [OnAbort](#OnAbort) | در زمان توقف بارگذاری صفحه قبل از اینکه عنصر به طور کامل بارگذاری شود، رخ می‌دهد. |
| [OnError](#OnError) | در زمان عدم بارگذاری صحیح یک عنصر یا بروز خطا در حین اجرای اسکریپت، رخ می‌دهد. |
| [OnResize](#OnResize) | در زمان تغییر اندازه نمای سند، رخ می‌دهد. |
| [OnScroll](#OnScroll) | در زمان جابه‌جایی نمای سند در محور X یا Y یا هر دو، رخ می‌دهد. |
| [OnZoom](#OnZoom) | در زمان تغییر سطح زوم نمای سند، رخ می‌دهد. |
| [OnBegin](#OnBegin) | در زمان شروع یک عنصر انیمیشن، رخ می‌دهد. |
| [OnEnd](#OnEnd) | در زمان پایان یک عنصر انیمیشن، رخ می‌دهد. |
| [OnRepeat](#OnRepeat) | در زمان تکرار یک عنصر انیمیشن، رخ می‌دهد. |
### OnFocusIn {#OnFocusIn}
```
public static final int OnFocusIn
```

در زمان دریافت فوکوس توسط یک عنصر، مانند زمانی که متن انتخاب می‌شود، رخ می‌دهد.

### OnFocusOut {#OnFocusOut}
```
public static final int OnFocusOut
```

در زمان از دست دادن فوکوس توسط یک عنصر، مانند زمانی که متن انتخاب‌ شده لغو می‌شود، رخ می‌دهد.

### OnActivate {#OnActivate}
```
public static final int OnActivate
```

در زمان فعال شدن یک عنصر، به عنوان مثال از طریق کلیک موس یا فشار کلید، رخ می‌دهد.

### OnClick {#OnClick}
```
public static final int OnClick
```

در زمان کلیک دکمه دستگاه اشاره‌گر بر روی یک عنصر، رخ می‌دهد.

### OnMouseDown {#OnMouseDown}
```
public static final int OnMouseDown
```

در زمان فشار دادن دکمه دستگاه اشاره‌گر بر روی یک عنصر، رخ می‌دهد.

### OnMouseUp {#OnMouseUp}
```
public static final int OnMouseUp
```

در زمان رها شدن دکمه دستگاه اشاره‌گر بر روی یک عنصر، رخ می‌دهد.

### OnMouseOver {#OnMouseOver}
```
public static final int OnMouseOver
```

در زمان حرکت دستگاه اشاره‌گر به سمت یک عنصر، رخ می‌دهد.

### OnMouseMove {#OnMouseMove}
```
public static final int OnMouseMove
```

در زمان حرکت دستگاه اشاره‌گر در حالی که بر روی یک عنصر قرار دارد، رخ می‌دهد.

### OnMouseOut {#OnMouseOut}
```
public static final int OnMouseOut
```

در زمان دور شدن دستگاه اشاره‌گر از یک عنصر، رخ می‌دهد.

### OnLoad {#OnLoad}
```
public static final int OnLoad
```

در زمان اینکه کارگزار کاربر عنصر و فرزندان آن و تمام منابع مورداستناد را به طور کامل تجزیه کرده باشد، که برای رندر آن لازم است، رخ می‌دهد.

### OnUnload {#OnUnload}
```
public static final int OnUnload
```

در زمان حذف یک سند توسط پیاده‌سازی DOM از یک پنجره یا فریم. فقط برای عناصر SVG بیرونی اعمال می‌شود.

### OnAbort {#OnAbort}
```
public static final int OnAbort
```

در زمان توقف بارگذاری صفحه قبل از اینکه عنصر به طور کامل بارگذاری شود، رخ می‌دهد.

### OnError {#OnError}
```
public static final int OnError
```

در زمان عدم بارگذاری صحیح یک عنصر یا بروز خطا در حین اجرای اسکریپت، رخ می‌دهد.

### OnResize {#OnResize}
```
public static final int OnResize
```

در زمان تغییر اندازه نمای سند. فقط برای عناصر SVG بیرونی اعمال می‌شود.

### OnScroll {#OnScroll}
```
public static final int OnScroll
```

در زمان جابه‌جایی نمای سند در محور X یا Y یا هر دو. فقط برای عناصر SVG بیرونی اعمال می‌شود.

### OnZoom {#OnZoom}
```
public static final int OnZoom
```

در زمان تغییر سطح زوم نمای سند. فقط برای عناصر SVG بیرونی اعمال می‌شود.

### OnBegin {#OnBegin}
```
public static final int OnBegin
```

در زمان شروع یک عنصر انیمیشن.

### OnEnd {#OnEnd}
```
public static final int OnEnd
```

در زمان پایان یک عنصر انیمیشن.

### OnRepeat {#OnRepeat}
```
public static final int OnRepeat
```

در زمان تکرار یک عنصر انیمیشن.