---
title: IFillFormatCollectionEffectiveData
second_title: Aspose.Slides برای مرجع API جاوا
description: شیء ثابت که یک مجموعه فقط‌خواندنی از قالب‌های پرکننده مؤثر را نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/ifillformatcollectioneffectivedata/
---
**همه رابط‌های پیاده‌سازی شده:**
com.aspose.slides.IGenericCollection
```
public interface IFillFormatCollectionEffectiveData extends IGenericCollection<IFillFormatEffectiveData>
```

شیء ثابت که یک مجموعه فقط خواندنی از قالب‌های پرکننده مؤثر را نشان می‌دهد.

--------------------

این رابط به عنوان بخشی از [IFormatSchemeEffectiveData](../../com.aspose.slides/iformatschemeeffectivedata) استفاده می‌شود.
## متدها

| متد | توضیح |
| --- | --- |
| [get_Item(int index)](#get-Item-int-) | عنصری را که در اندیس مشخص شده قرار دارد برمی‌گرداند. |
### get_Item(int index) {#get-Item-int-}
```
public abstract IFillFormatEffectiveData get_Item(int index)
```

عنصری را که در اندیس مشخص شده قرار دارد برمی‌گرداند. فقط‌خواندنی [IFillFormatEffectiveData](../../com.aspose.slides/ifillformateffectivedata).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| index | int |  |

**بازگشت:**
[IFillFormatEffectiveData](../../com.aspose.slides/ifillformateffectivedata)