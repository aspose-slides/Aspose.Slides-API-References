---
title: IMasterNotesSlideHeaderFooterManager
second_title: مرجع API Aspose.Slides برای جاوا
description: نماینده‌ای است که رفتار فوتر، تاریخ-زمان، شماره صفحه اسلاید یادداشت اصلی و تمام جای‌گیرهای فرزند را در بر می‌گیرد.
type: docs
url: /fa/com.aspose.slides/imasternotesslideheaderfootermanager/
---
**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IBaseHandoutNotesSlideHeaderFooterManag](../../com.aspose.slides/ibasehandoutnotesslideheaderfootermanag)
```
public interface IMasterNotesSlideHeaderFooterManager extends IBaseHandoutNotesSlideHeaderFooterManag
```

نماینده مدیری است که رفتار فوتر، تاریخ-زمان، شمارهٔ صفحهٔ اسلاید یادداشت اصلی و تمام جای‌گیرهای فرزند را در بر می‌گیرد. جای‌گیرهای فرزند به این معنی است که جای‌گیرها در اسلایدهای یادداشت وابسته نگهداری می‌شوند. اسلایدهای یادداشت وابسته از اسلاید یادداشت اصلی استفاده می‌کنند و به آن وابسته هستند.

## متدها

| متد | توضیح |
| --- | --- |
| [setHeaderAndChildHeadersVisibility(boolean isVisible)](#setHeaderAndChildHeadersVisibility-boolean-) | تغییر وضعیت نمایش جای‌گیر سرصفحه اسلاید یادداشت اصلی و تمام جای‌گیرهای سرصفحهٔ فرزند. |
| [setHeaderAndChildHeadersText(String text)](#setHeaderAndChildHeadersText-java.lang.String-) | تنظیم متن برای جای‌گیر سرصفحه اسلاید یادداشت اصلی و تمام جای‌گیرهای سرصفحهٔ فرزند. |
| [setFooterAndChildFootersVisibility(boolean isVisible)](#setFooterAndChildFootersVisibility-boolean-) | تغییر وضعیت نمایش جای‌گیر فوتر اسلاید یادداشت اصلی و تمام جای‌گیرهای فوتر فرزند. |
| [setSlideNumberAndChildSlideNumbersVisibility(boolean isVisible)](#setSlideNumberAndChildSlideNumbersVisibility-boolean-) | تغییر وضعیت نمایش جای‌گیر شمارهٔ صفحه اسلاید یادداشت اصلی و تمام جای‌گیرهای شمارهٔ صفحهٔ فرزند. |
| [setDateTimeAndChildDateTimesVisibility(boolean isVisible)](#setDateTimeAndChildDateTimesVisibility-boolean-) | تغییر وضعیت نمایش جای‌گیر تاریخ-زمان اسلاید یادداشت اصلی و تمام جای‌گیرهای تاریخ-زمان فرزند. |
| [setFooterAndChildFootersText(String text)](#setFooterAndChildFootersText-java.lang.String-) | تنظیم متن برای جای‌گیر فوتر اسلاید یادداشت اصلی و تمام جای‌گیرهای فوتر فرزند. |
| [setDateTimeAndChildDateTimesText(String text)](#setDateTimeAndChildDateTimesText-java.lang.String-) | تنظیم متن برای جای‌گیر تاریخ-زمان اسلاید یادداشت اصلی و تمام جای‌گیرهای تاریخ-زمان فرزند. |

### setHeaderAndChildHeadersVisibility(boolean isVisible) {#setHeaderAndChildHeadersVisibility-boolean-}
```
public abstract void setHeaderAndChildHeadersVisibility(boolean isVisible)
```

تغییر وضعیت نمایش جای‌گیر سرصفحه اسلاید یادداشت اصلی و تمام جای‌گیرهای سرصفحهٔ فرزند. جای‌گیرهای فرزند به این معنی است که جای‌گیرها در اسلایدهای یادداشت وابسته نگهداری می‌شوند. اسلایدهای یادداشت وابسته از اسلاید یادداشت اصلی استفاده می‌کنند و به آن وابسته هستند.

**Parameters:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - جای‌گیرهای سرصفحه را قابل مشاهده می‌کند، در غیر این صورت آن‌ها را مخفی می‌سازد. |

### setHeaderAndChildHeadersText(String text) {#setHeaderAndChildHeadersText-java.lang.String-}
```
public abstract void setHeaderAndChildHeadersText(String text)
```

متن را برای جای‌گیر سرصفحه اسلاید یادداشت اصلی و تمام جای‌گیرهای سرصفحهٔ فرزند تنظیم می‌کند. جای‌گیرهای فرزند به این معنی است که جای‌گیرها در اسلایدهای یادداشت وابسته نگهداری می‌شوند. اسلایدهای یادداشت وابسته از اسلاید یادداشت اصلی استفاده می‌کنند و به آن وابسته هستند.

**Parameters:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| text | java.lang.String | متنی که تنظیم می‌شود. |

### setFooterAndChildFootersVisibility(boolean isVisible) {#setFooterAndChildFootersVisibility-boolean-}
```
public abstract void setFooterAndChildFootersVisibility(boolean isVisible)
```

تغییر وضعیت نمایش جای‌گیر فوتر اسلاید یادداشت اصلی و تمام جای‌گیرهای فوتر فرزند. جای‌گیرهای فرزند به این معنی است که جای‌گیرها در اسلایدهای یادداشت وابسته نگهداری می‌شوند. اسلایدهای یادداشت وابسته از اسلاید یادداشت اصلی استفاده می‌کنند و به آن وابسته هستند.

**Parameters:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - جای‌گیرهای فوتر را قابل مشاهده می‌کند، در غیر این صورت آن‌ها را مخفی می‌سازد. |

### setSlideNumberAndChildSlideNumbersVisibility(boolean isVisible) {#setSlideNumberAndChildSlideNumbersVisibility-boolean-}
```
public abstract void setSlideNumberAndChildSlideNumbersVisibility(boolean isVisible)
```

تغییر وضعیت نمایش جای‌گیر شمارهٔ صفحه اسلاید یادداشت اصلی و تمام جای‌گیرهای شمارهٔ صفحهٔ فرزند. جای‌گیرهای فرزند به این معنی است که جای‌گیرها در اسلایدهای یادداشت وابسته نگهداری می‌شوند. اسلایدهای یادداشت وابسته از اسلاید یادداشت اصلی استفاده می‌کنند و به آن وابسته هستند.

**Parameters:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - جای‌گیرهای شمارهٔ صفحه را قابل مشاهده می‌کند، در غیر این صورت آن‌ها را مخفی می‌سازد. |

### setDateTimeAndChildDateTimesVisibility(boolean isVisible) {#setDateTimeAndChildDateTimesVisibility-boolean-}
```
public abstract void setDateTimeAndChildDateTimesVisibility(boolean isVisible)
```

تغییر وضعیت نمایش جای‌گیر تاریخ-زمان اسلاید یادداشت اصلی و تمام جای‌گیرهای تاریخ-زمان فرزند. جای‌گیرهای فرزند به این معنی است که جای‌گیرها در اسلایدهای یادداشت وابسته نگهداری می‌شوند. اسلایدهای یادداشت وابسته از اسلاید یادداشت اصلی استفاده می‌کنند و به آن وابسته هستند.

**Parameters:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - جای‌گیرهای تاریخ-زمان را قابل مشاهده می‌کند، در غیر این صورت آن‌ها را مخفی می‌سازد. |

### setFooterAndChildFootersText(String text) {#setFooterAndChildFootersText-java.lang.String-}
```
public abstract void setFooterAndChildFootersText(String text)
```

متن را برای جای‌گیر فوتر اسلاید یادداشت اصلی و تمام جای‌گیرهای فوتر فرزند تنظیم می‌کند. جای‌گیرهای فرزند به این معنی است که جای‌گیرها در اسلایدهای یادداشت وابسته نگهداری می‌شوند. اسلایدهای یادداشت وابسته از اسلاید یادداشت اصلی استفاده می‌کنند و به آن وابسته هستند.

**Parameters:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| text | java.lang.String | متنی که تنظیم می‌شود. |

### setDateTimeAndChildDateTimesText(String text) {#setDateTimeAndChildDateTimesText-java.lang.String-}
```
public abstract void setDateTimeAndChildDateTimesText(String text)
```

متن را برای جای‌گیر تاریخ-زمان اسلاید یادداشت اصلی و تمام جای‌گیرهای تاریخ-زمان فرزند تنظیم می‌کند. جای‌گیرهای فرزند به این معنی است که جای‌گیرها در اسلایدهای یادداشت وابسته نگهداری می‌شوند. اسلایدهای یادداشت وابسته از اسلاید یادداشت اصلی استفاده می‌کنند و به آن وابسته هستند.

**Parameters:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| text | java.lang.String | متنی که تنظیم می‌شود. |