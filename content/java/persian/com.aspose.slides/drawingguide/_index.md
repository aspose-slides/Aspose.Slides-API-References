---
title: DrawingGuide
second_title: Aspose.Slides برای جاوا مرجع API
description: نمایانگر یک راهنمای قابل تنظیم برای رسم.
type: docs
url: /fa/com.aspose.slides/drawingguide/
---
**Inheritance:**
java.lang.Object

**All Implemented Interfaces:**
[com.aspose.slides.IDrawingGuide](../../com.aspose.slides/idrawingguide)
```
public final class DrawingGuide implements IDrawingGuide
```

نمایانگر یک راهنمای قابل تنظیم برای رسم است.
## متدها

| متد | توضیح |
| --- | --- |
| [getOrientation()](#getOrientation--) | جهت راهنمای نقاشی را برمی‌گرداند یا تنظیم می‌کند. |
| [setOrientation(byte value)](#setOrientation-byte-) | جهت راهنمای نقاشی را برمی‌گرداند یا تنظیم می‌کند. |
| [getPosition()](#getPosition--) | موقعیت راهنمای نقاشی را بر حسب نقاط از گوشه بالای چپ اسلاید برمی‌گرداند یا تنظیم می‌کند. |
| [setPosition(float value)](#setPosition-float-) | موقعیت راهنمای نقاشی را بر حسب نقاط از گوشه بالای چپ اسلاید برمی‌گرداند یا تنظیم می‌کند. |
| [getColor()](#getColor--) | رنگ راهنمای نقاشی را برمی‌گرداند یا تنظیم می‌کند. |
| [setColor(Color value)](#setColor-java.awt.Color-) | رنگ راهنمای نقاشی را برمی‌گرداند یا تنظیم می‌کند. |
### getOrientation() {#getOrientation--}
```
public final byte getOrientation()
```

جهت راهنمای نقاشی را برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن [Orientation](../../com.aspose.slides/orientation).

**بازگشت:**
byte
### setOrientation(byte value) {#setOrientation-byte-}
```
public final void setOrientation(byte value)
```

جهت راهنمای نقاشی را برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن [Orientation](../../com.aspose.slides/orientation).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | byte |  |
### getPosition() {#getPosition--}
```
public final float getPosition()
```

موقعیت راهنمای نقاشی را بر حسب نقاط از گوشه بالای چپ اسلاید برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن float.

--------------------

محدوده مقدار معمولی از صفر تا ارتفاع اسلاید برای راهنمای افقی و از صفر تا عرض اسلاید برای راهنمای عمودی است.

**بازگشت:**
float
### setPosition(float value) {#setPosition-float-}
```
public final void setPosition(float value)
```

موقعیت راهنمای نقاشی را بر حسب نقاط از گوشه بالای چپ اسلاید برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن float.

--------------------

محدوده مقدار معمولی از صفر تا ارتفاع اسلاید برای راهنمای افقی و از صفر تا عرض اسلاید برای راهنمای عمودی است.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |
### getColor() {#getColor--}
```
public final Color getColor()
```

رنگ راهنمای نقاشی را برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن java.awt.Color.

**بازگشت:**
java.awt.Color
### setColor(Color value) {#setColor-java.awt.Color-}
```
public final void setColor(Color value)
```

رنگ راهنمای نقاشی را برمی‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن java.awt.Color.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | java.awt.Color |  |