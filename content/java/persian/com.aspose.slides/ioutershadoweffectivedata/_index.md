---
title: IOuterShadowEffectiveData
second_title: Aspose.Slides برای مرجع API جاوا
description: شیء غیرقابل تغییر که نمایانگر اثر سایه خارجی است.
type: docs
url: /fa/com.aspose.slides/ioutershadoweffectivedata/
---
**تمام رابط‌های پیاده‌سازی شده:**
[com.aspose.slides.IEffectEffectiveData](../../com.aspose.slides/ieffecteffectivedata)
```
public interface IOuterShadowEffectiveData extends IEffectEffectiveData
```

شیء غیرقابل تغییر که نمایانگر افکت سایه خارجی است.
## متدها

| Method | Description |
| --- | --- |
| [getBlurRadius()](#getBlurRadius--) | شعاع تاری. |
| [getDirection()](#getDirection--) | جهت سایه. |
| [getDistance()](#getDistance--) | فاصله سایه. |
| [getShadowColor()](#getShadowColor--) | رنگ سایه. |
| [getRectangleAlign()](#getRectangleAlign--) | تراز مستطیل. |
| [getSkewHorizontal()](#getSkewHorizontal--) | زاویه اسکِی افقی (به درجه) را مشخص می‌کند. |
| [getSkewVertical()](#getSkewVertical--) | زاویه اسکِی عمودی (به درجه) را مشخص می‌کند. |
| [getRotateShadowWithShape()](#getRotateShadowWithShape--) | مشخص می‌کند آیا سایه باید همراه شکل بچرخد اگر شکل چرخانده شود. |
| [getScaleHorizontal()](#getScaleHorizontal--) | فاکتور مقیاس افقی را مشخص می‌کند، مقیاس منفی باعث وارون می‌شود. |
| [getScaleVertical()](#getScaleVertical--) | فاکتور مقیاس عمودی را مشخص می‌کند، مقیاس منفی باعث وارون می‌شود. |

### getBlurRadius() {#getBlurRadius--}
```
public abstract double getBlurRadius()
```

شعاع تاری. فقط-خواندنی double.

**باز می‌گرداند:**
double

### getDirection() {#getDirection--}
```
public abstract float getDirection()
```

جهت سایه. فقط-خواندنی float.

**باز می‌گرداند:**
float

### getDistance() {#getDistance--}
```
public abstract double getDistance()
```

فاصله سایه. فقط-خواندنی double.

**باز می‌گرداند:**
double

### getShadowColor() {#getShadowColor--}
```
public abstract Color getShadowColor()
```

رنگ سایه. فقط-خواندنی java.awt.Color.

**باز می‌گرداند:**
java.awt.Color

### getRectangleAlign() {#getRectangleAlign--}
```
public abstract byte getRectangleAlign()
```

تراز مستطیل. فقط-خواندنی [RectangleAlignment](../../com.aspose.slides/rectanglealignment).

**باز می‌گرداند:**
byte

### getSkewHorizontal() {#getSkewHorizontal--}
```
public abstract double getSkewHorizontal()
```

زاویه اسکِی افقی (به درجه) را مشخص می‌کند. فقط-خواندنی double.

**باز می‌گرداند:**
double

### getSkewVertical() {#getSkewVertical--}
```
public abstract double getSkewVertical()
```

زاویه اسکِی عمودی (به درجه) را مشخص می‌کند. فقط-خواندنی double.

**باز می‌گرداند:**
double

### getRotateShadowWithShape() {#getRotateShadowWithShape--}
```
public abstract boolean getRotateShadowWithShape()
```

مشخص می‌کند آیا سایه باید همراه شکل بچرخد اگر شکل چرخانده شود. فقط-خواندنی boolean.

**باز می‌گرداند:**
boolean

### getScaleHorizontal() {#getScaleHorizontal--}
```
public abstract double getScaleHorizontal()
```

فاکتور مقیاس افقی را مشخص می‌کند، مقیاس منفی باعث وارون می‌شود. فقط-خواندنی double.

**باز می‌گرداند:**
double

### getScaleVertical() {#getScaleVertical--}
```
public abstract double getScaleVertical()
```

فاکتور مقیاس عمودی را مشخص می‌کند، مقیاس منفی باعث وارون می‌شود. فقط-خواندنی double.

**باز می‌گرداند:**
double