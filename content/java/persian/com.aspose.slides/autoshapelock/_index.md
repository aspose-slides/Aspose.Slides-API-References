---
title: AutoShapeLock
second_title: مرجع API Aspose.Slides برای جاوا
description: تعیین می‌کند که کدام عملیات‌ها بر روی AutoshapeEx والد غیرفعال هستند.
type: docs
url: /fa/com.aspose.slides/autoshapelock/
---
**ارث‌بری:**
java.lang.Object, [com.aspose.slides.BaseShapeLock](../../com.aspose.slides/baseshapelock)

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IAutoShapeLock](../../com.aspose.slides/iautoshapelock)
```
public class AutoShapeLock extends BaseShapeLock implements IAutoShapeLock
```

تعیین می‌کند که کدام عملیات‌ها بر روی AutoshapeEx والد غیرفعال هستند.
## متدها

| متد | توضیحات |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | تعیین می‌کند که افزودن این شکل به یک گروه ممنوع است یا خیر. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | تعیین می‌کند که افزودن این شکل به یک گروه ممنوع است یا خیر. |
| [getSelectLocked()](#getSelectLocked--) | تعیین می‌کند که انتخاب این شکل ممنوع است یا خیر. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | تعیین می‌کند که انتخاب این شکل ممنوع است یا خیر. |
| [getRotateLocked()](#getRotateLocked--) | تعیین می‌کند که تغییر زاویه چرخش این شکل ممنوع است یا خیر. |
| [setRotateLocked(boolean value)](#setRotateLocked-boolean-) | تعیین می‌کند که تغییر زاویه چرخش این شکل ممنوع است یا خیر. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | تعیین می‌کند که آیا یک شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند یا خیر. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | تعیین می‌کند که آیا یک شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند یا خیر. |
| [getPositionLocked()](#getPositionLocked--) | تعیین می‌کند که حرکت این شکل ممنوع است یا خیر. |
| [setPositionLocked(boolean value)](#setPositionLocked-boolean-) | تعیین می‌کند که حرکت این شکل ممنوع است یا خیر. |
| [getSizeLocked()](#getSizeLocked--) | تعیین می‌کند که تغییر اندازه این شکل ممنوع است یا خیر. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | تعیین می‌کند که تغییر اندازه این شکل ممنوع است یا خیر. |
| [getEditPointsLocked()](#getEditPointsLocked--) | تعیین می‌کند که تغییر مستقیم کانتور این شکل ممنوع است یا خیر. |
| [setEditPointsLocked(boolean value)](#setEditPointsLocked-boolean-) | تعیین می‌کند که تغییر مستقیم کانتور این شکل ممنوع است یا خیر. |
| [getAdjustHandlesLocked()](#getAdjustHandlesLocked--) | تعیین می‌کند که تغییر مقادیر تنظیمات ممنوع است یا خیر. |
| [setAdjustHandlesLocked(boolean value)](#setAdjustHandlesLocked-boolean-) | تعیین می‌کند که تغییر مقادیر تنظیمات ممنوع است یا خیر. |
| [getArrowheadsLocked()](#getArrowheadsLocked--) | تعیین می‌کند که تغییر سرهای فلش ممنوع است یا خیر. |
| [setArrowheadsLocked(boolean value)](#setArrowheadsLocked-boolean-) | تعیین می‌کند که تغییر سرهای فلش ممنوع است یا خیر. |
| [getShapeTypeLocked()](#getShapeTypeLocked--) | تعیین می‌کند که تغییر نوع شکل ممنوع است یا خیر. |
| [setShapeTypeLocked(boolean value)](#setShapeTypeLocked-boolean-) | تعیین می‌کند که تغییر نوع شکل ممنوع است یا خیر. |
| [getTextLocked()](#getTextLocked--) | تعیین می‌کند که ویرایش متن ممنوع است یا خیر. |
| [setTextLocked(boolean value)](#setTextLocked-boolean-) | تعیین می‌کند که ویرایش متن ممنوع است یا خیر. |
### getGroupingLocked() {#getGroupingLocked--}
```
public boolean getGroupingLocked()
```

تعیین می‌کند که افزودن این شکل به یک گروه ممنوع است یا خیر. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public void setGroupingLocked(boolean value)
```

تعیین می‌کند که افزودن این شکل به یک گروه ممنوع است یا خیر. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | boolean |  |
### getSelectLocked() {#getSelectLocked--}
```
public boolean getSelectLocked()
```

تعیین می‌کند که انتخاب این شکل ممنوع است یا خیر. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public void setSelectLocked(boolean value)
```

تعیین می‌کند که انتخاب این شکل ممنوع است یا خیر. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | boolean |  |
### getRotateLocked() {#getRotateLocked--}
```
public boolean getRotateLocked()
```

تعیین می‌کند که تغییر زاویه چرخش این شکل ممنوع است یا خیر. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setRotateLocked(boolean value) {#setRotateLocked-boolean-}
```
public void setRotateLocked(boolean value)
```

تعیین می‌کند که تغییر زاویه چرخش این شکل ممنوع است یا خیر. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | boolean |  |
### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public boolean getAspectRatioLocked()
```

تعیین می‌کند که آیا یک شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند یا خیر. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public void setAspectRatioLocked(boolean value)
```

تعیین می‌کند که آیا یک شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند یا خیر. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | boolean |  |
### getPositionLocked() {#getPositionLocked--}
```
public boolean getPositionLocked()
```

تعیین می‌کند که حرکت این شکل ممنوع است یا خیر. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setPositionLocked(boolean value) {#setPositionLocked-boolean-}
```
public void setPositionLocked(boolean value)
```

تعیین می‌کند که حرکت این شکل ممنوع است یا خیر. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | boolean |  |
### getSizeLocked() {#getSizeLocked--}
```
public boolean getSizeLocked()
```

تعیین می‌کند که تغییر اندازه این شکل ممنوع است یا خیر. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public void setSizeLocked(boolean value)
```

تعیین می‌کند که تغییر اندازه این شکل ممنوع است یا خیر. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | boolean |  |
### getEditPointsLocked() {#getEditPointsLocked--}
```
public boolean getEditPointsLocked()
```

تعیین می‌کند که تغییر مستقیم کانتور این شکل ممنوع است یا خیر. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setEditPointsLocked(boolean value) {#setEditPointsLocked-boolean-}
```
public void setEditPointsLocked(boolean value)
```

تعیین می‌کند که تغییر مستقیم کانتور این شکل ممنوع است یا خیر. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | boolean |  |
### getAdjustHandlesLocked() {#getAdjustHandlesLocked--}
```
public boolean getAdjustHandlesLocked()
```

تعیین می‌کند که تغییر مقادیر تنظیمات ممنوع است یا خیر. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setAdjustHandlesLocked(boolean value) {#setAdjustHandlesLocked-boolean-}
```
public void setAdjustHandlesLocked(boolean value)
```

تعیین می‌کند که تغییر مقادیر تنظیمات ممنوع است یا خیر. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | boolean |  |
### getArrowheadsLocked() {#getArrowheadsLocked--}
```
public boolean getArrowheadsLocked()
```

تعیین می‌کند که تغییر سرهای فلش ممنوع است یا خیر. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setArrowheadsLocked(boolean value) {#setArrowheadsLocked-boolean-}
```
public void setArrowheadsLocked(boolean value)
```

تعیین می‌کند که تغییر سرهای فلش ممنوع است یا خیر. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | boolean |  |
### getShapeTypeLocked() {#getShapeTypeLocked--}
```
public boolean getShapeTypeLocked()
```

تعیین می‌کند که تغییر نوع شکل ممنوع است یا خیر. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setShapeTypeLocked(boolean value) {#setShapeTypeLocked-boolean-}
```
public void setShapeTypeLocked(boolean value)
```

تعیین می‌کند که تغییر نوع شکل ممنوع است یا خیر. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | boolean |  |
### getTextLocked() {#getTextLocked--}
```
public boolean getTextLocked()
```

تعیین می‌کند که ویرایش متن ممنوع است یا خیر. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setTextLocked(boolean value) {#setTextLocked-boolean-}
```
public void setTextLocked(boolean value)
```

تعیین می‌کند که ویرایش متن ممنوع است یا خیر. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | boolean |  |