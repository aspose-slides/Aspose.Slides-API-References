---
title: IScaleEffect
second_title: Aspose.Slides برای مرجع API جاوا
description: نمایش اثر مقیاس انیمیشن.
type: docs
url: /fa/com.aspose.slides/iscaleeffect/
---
**تمام واسط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IBehavior](../../com.aspose.slides/ibehavior)
```
public interface IScaleEffect extends IBehavior
```

نمایش اثر مقیاس انیمیشن.
## متدها

| متد | توضیح |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | تعیین می‌کند که آیا محتوا باید زوم شود یا نه. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | تعیین می‌کند که آیا محتوا باید زوم شود یا نه. |
| [getFrom()](#getFrom--) | یک مختصات x/y را برای شروع انیمیشن مشخص می‌کند (به درصد). |
| [setFrom(Point2D.Float value)](#setFrom-java.awt.geom.Point2D.Float-) | یک مختصات x/y را برای شروع انیمیشن مشخص می‌کند (به درصد). |
| [getTo()](#getTo--) | موقعیت هدف را برای اثر مقیاس انیمیشن مشخص می‌کند (به درصد). |
| [setTo(Point2D.Float value)](#setTo-java.awt.geom.Point2D.Float-) | موقعیت هدف را برای اثر مقیاس انیمیشن مشخص می‌کند (به درصد). |
| [getBy()](#getBy--) | مقدار جابجایی نسبی انیمیشن را توصیف می‌کند (به درصد). |
| [setBy(Point2D.Float value)](#setBy-java.awt.geom.Point2D.Float-) | مقدار جابجایی نسبی انیمیشن را توصیف می‌کند (به درصد). |

### getZoomContent() {#getZoomContent--}
```
public abstract byte getZoomContent()
```

تعیین می‌کند که آیا محتوا باید زوم شود یا نه. خواندنی/نوشتنی [NullableBool](../../com.aspose.slides/nullablebool).

**بازگشت:**
byte

### setZoomContent(byte value) {#setZoomContent-byte-}
```
public abstract void setZoomContent(byte value)
```

تعیین می‌کند که آیا محتوا باید زوم شود یا نه. خواندنی/نوشتنی [NullableBool](../../com.aspose.slides/nullablebool).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public abstract Point2D.Float getFrom()
```

یک مختصات x/y را برای شروع انیمیشن مشخص می‌کند (به درصد). خواندنی/نوشتنی java.awt.geom.Point2D.Float.

**بازگشت:**
java.awt.geom.Point2D.Float

### setFrom(Point2D.Float value) {#setFrom-java.awt.geom.Point2D.Float-}
```
public abstract void setFrom(Point2D.Float value)
```

یک مختصات x/y را برای شروع انیمیشن مشخص می‌کند (به درصد). خواندنی/نوشتنی java.awt.geom.Point2D.Float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getTo() {#getTo--}
```
public abstract Point2D.Float getTo()
```

موقعیت هدف را برای اثر مقیاس انیمیشن مشخص می‌کند (به درصد). خواندنی/نوشتنی java.awt.geom.Point2D.Float.

**بازگشت:**
java.awt.geom.Point2D.Float

### setTo(Point2D.Float value) {#setTo-java.awt.geom.Point2D.Float-}
```
public abstract void setTo(Point2D.Float value)
```

موقعیت هدف را برای اثر مقیاس انیمیشن مشخص می‌کند (به درصد). خواندنی/نوشتنی java.awt.geom.Point2D.Float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getBy() {#getBy--}
```
public abstract Point2D.Float getBy()
```

مقدار جابجایی نسبی انیمیشن را توصیف می‌کند (به درصد). خواندنی/نوشتنی java.awt.geom.Point2D.Float.

**بازگشت:**
java.awt.geom.Point2D.Float

### setBy(Point2D.Float value) {#setBy-java.awt.geom.Point2D.Float-}
```
public abstract void setBy(Point2D.Float value)
```

مقدار جابجایی نسبی انیمیشن را توصیف می‌کند (به درصد). خواندنی/نوشتنی java.awt.geom.Point2D.Float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |