---
title: IPresentationHeaderFooterManager
second_title: Aspose.Slides برای Java مرجع API
description: نماینده مدیری است که رفتار تمام مکان‌نگه‌دارهای پاورقی، تاریخ-زمان و شماره صفحهٔ ارائه را بر عهده دارد.
type: docs
url: /fa/com.aspose.slides/ipresentationheaderfootermanager/
---
**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IBaseHeaderFooterManager](../../com.aspose.slides/ibaseheaderfootermanager)
```
public interface IPresentationHeaderFooterManager extends IBaseHeaderFooterManager
```

نماینده‌ی مدیری است که رفتار تمام مکان‌نگه‌دارهای پاورقی، تاریخ-زمان و شماره صفحه‌ی ارائه را بر عهده دارد.
## متدها

| متد | توضیح |
| --- | --- |
| [setAllHeadersVisibility(boolean isVisible)](#setAllHeadersVisibility-boolean-) | وضعیت قابل مشاهده بودن تمام مکان‌نگه‌دارهای سرصفحه را تغییر می‌دهد، شامل master یادداشت‌ها، اسلایدهای یادداشت و master نسخه چاپی. |
| [setAllFootersVisibility(boolean isVisible)](#setAllFootersVisibility-boolean-) | وضعیت قابل مشاهده بودن تمام مکان‌نگه‌دارهای پاورقی را تغییر می‌دهد، شامل اسلایدهای master، اسلایدهای طرح‌بندی و اسلایدها. |
| [setAllSlideNumbersVisibility(boolean isVisible)](#setAllSlideNumbersVisibility-boolean-) | وضعیت قابل مشاهده بودن تمام مکان‌نگه‌دارهای شماره صفحه را تغییر می‌دهد، شامل اسلایدهای master، اسلایدهای طرح‌بندی و اسلایدها. |
| [setAllDateTimesVisibility(boolean isVisible)](#setAllDateTimesVisibility-boolean-) | وضعیت قابل مشاهده بودن تمام مکان‌نگه‌دارهای تاریخ-زمان را تغییر می‌دهد، شامل اسلایدهای master، اسلایدهای طرح‌بندی و اسلایدها. |
| [setAllHeadersText(String text)](#setAllHeadersText-java.lang.String-) | متن را برای تمام مکان‌نگه‌دارهای سرصفحه تنظیم می‌کند، شامل master یادداشت‌ها، اسلایدهای یادداشت و master نسخه چاپی. |
| [setAllFootersText(String text)](#setAllFootersText-java.lang.String-) | متن را برای تمام مکان‌نگه‌دارهای پاورقی تنظیم می‌کند، شامل اسلایدهای master، اسلایدهای طرح‌بندی و اسلایدها. |
| [setAllDateTimesText(String text)](#setAllDateTimesText-java.lang.String-) | متن را برای تمام مکان‌نگه‌دارهای تاریخ-زمان تنظیم می‌کند، شامل اسلایدهای master، اسلایدهای طرح‌بندی و اسلایدها. |
| [setVisibilityOnAllTitleSlides(boolean isVisible)](#setVisibilityOnAllTitleSlides-boolean-) | وضعیت قابل مشاهده بودن مکان‌نگه‌دارهای پاورقی، تاریخ-زمان و شماره صفحه را برای تمام اسلایدهای عنوان و اولین اسلاید طرح‌بندی تغییر می‌دهد. |

### setAllHeadersVisibility(boolean isVisible) {#setAllHeadersVisibility-boolean-}
```
public abstract void setAllHeadersVisibility(boolean isVisible)
```

وضعیت قابل مشاهده بودن تمام مکان‌نگه‌دارهای سرصفحه را تغییر می‌دهد، شامل master یادداشت‌ها، اسلایدهای یادداشت و master نسخه چاپی.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - مکان‌نگه‌دارهای سرصفحه را قابل مشاهده می‌کند، در غیر این صورت آن‌ها را مخفی می‌کند. |

### setAllFootersVisibility(boolean isVisible) {#setAllFootersVisibility-boolean-}
```
public abstract void setAllFootersVisibility(boolean isVisible)
```

وضعیت قابل مشاهده بودن تمام مکان‌نگه‌دارهای پاورقی را تغییر می‌دهد، شامل اسلایدهای master، اسلایدهای طرح‌بندی و اسلایدها.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - مکان‌نگه‌دارهای پاورقی را قابل مشاهده می‌کند، در غیر این صورت آن‌ها را مخفی می‌کند. |

### setAllSlideNumbersVisibility(boolean isVisible) {#setAllSlideNumbersVisibility-boolean-}
```
public abstract void setAllSlideNumbersVisibility(boolean isVisible)
```

وضعیت قابل مشاهده بودن تمام مکان‌نگه‌دارهای شماره صفحه را تغییر می‌دهد، شامل اسلایدهای master، اسلایدهای طرح‌بندی و اسلایدها.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - مکان‌نگه‌دارهای شماره صفحه را قابل مشاهده می‌کند، در غیر این صورت آن‌ها را مخفی می‌کند. |

### setAllDateTimesVisibility(boolean isVisible) {#setAllDateTimesVisibility-boolean-}
```
public abstract void setAllDateTimesVisibility(boolean isVisible)
```

وضعیت قابل مشاهده بودن تمام مکان‌نگه‌دارهای تاریخ-زمان را تغییر می‌دهد، شامل اسلایدهای master، اسلایدهای طرح‌بندی و اسلایدها.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - مکان‌نگه‌دارهای تاریخ-زمان را قابل مشاهده می‌کند، در غیر این صورت آن‌ها را مخفی می‌کند. |

### setAllHeadersText(String text) {#setAllHeadersText-java.lang.String-}
```
public abstract void setAllHeadersText(String text)
```

متن را برای تمام مکان‌نگه‌دارهای سرصفحه تنظیم می‌کند، شامل master یادداشت‌ها، اسلایدهای یادداشت و master نسخه چاپی.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| text | java.lang.String | متنی که باید تنظیم شود. |

### setAllFootersText(String text) {#setAllFootersText-java.lang.String-}
```
public abstract void setAllFootersText(String text)
```

متن را برای تمام مکان‌نگه‌دارهای پاورقی تنظیم می‌کند، شامل اسلایدهای master، اسلایدهای طرح‌بندی و اسلایدها.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| text | java.lang.String | متنی که باید تنظیم شود. |

### setAllDateTimesText(String text) {#setAllDateTimesText-java.lang.String-}
```
public abstract void setAllDateTimesText(String text)
```

متن را برای تمام مکان‌نگه‌دارهای تاریخ-زمان تنظیم می‌کند، شامل اسلایدهای master، اسلایدهای طرح‌بندی و اسلایدها.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| text | java.lang.String | متنی که باید تنظیم شود. |

### setVisibilityOnAllTitleSlides(boolean isVisible) {#setVisibilityOnAllTitleSlides-boolean-}
```
public abstract void setVisibilityOnAllTitleSlides(boolean isVisible)
```

وضعیت قابل مشاهده بودن مکان‌نگه‌دارهای پاورقی، تاریخ-زمان و شماره صفحه را برای تمام اسلایدهای عنوان و اولین اسلاید طرح‌بندی تغییر می‌دهد. اسلایدهای عنوان – اسلایدهایی که بر پایه اولین اسلاید طرح‌بندی ساخته شده‌اند (بدون در نظر گرفتن نوع این اولین طرح‌بندی).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| isVisible | boolean | true - مکان‌نگه‌دارها را قابل مشاهده می‌کند، در غیر این صورت آن‌ها را مخفی می‌کند. |