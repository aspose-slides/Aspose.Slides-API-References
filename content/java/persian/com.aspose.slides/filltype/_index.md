---
title: FillType
second_title: Aspose.Slides برای مرجع API جاوا
description: نوع پرکردن داخلی اشیاء بصری مختلف را مشخص می‌کند.
type: docs
url: /fa/com.aspose.slides/filltype/
---
**ارث‌بری:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class FillType extends System.Enum
```

نوع پرکردن داخلی اشیاء بصری مختلف را تعیین می‌کند.
## فیلدها

| فیلد | توضیح |
| --- | --- |
| [NotDefined](#NotDefined) | نوع پرکردن تعریف نشده است. |
| [NoFill](#NoFill) | هیچ پرکردنی اعمال نشده است. |
| [Solid](#Solid) | با یک رنگ ثابت پر می‌شود. |
| [Gradient](#Gradient) | پرکردن به صورت گرادیان است. |
| [Pattern](#Pattern) | الگو تکراری برای پر کردن شیء استفاده می‌شود. |
| [Picture](#Picture) | یک تصویر تک برای پر کردن شیء استفاده می‌شود. |
| [Group](#Group) | شیء بصری خصوصیات پرکردن را از گروه ارث می‌برد. |
### NotDefined {#NotDefined}
```
public static final byte NotDefined
```

نوع پرکردن تعریف نشده است.

### NoFill {#NoFill}
```
public static final byte NoFill
```

هیچ پرکردنی اعمال نشده است.

### Solid {#Solid}
```
public static final byte Solid
```

با یک رنگ ثابت پر می‌شود.

### Gradient {#Gradient}
```
public static final byte Gradient
```

پرکردن به صورت گرادیان است.

### Pattern {#Pattern}
```
public static final byte Pattern
```

الگو تکراری برای پر کردن شیء استفاده می‌شود.

### Picture {#Picture}
```
public static final byte Picture
```

یک تصویر تک برای پر کردن شیء استفاده می‌شود.

### Group {#Group}
```
public static final byte Group
```

شیء بصری خصوصیات پرکردن را از گروه ارث می‌برد.