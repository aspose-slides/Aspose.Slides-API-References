---
title: TextHighlightingOptions
second_title: مرجع API Aspose.Slides برای جاوا
description: نمایانگر گزینه‌هایی است که می‌توان برای برجسته‌سازی متن در چارچوب متن استفاده کرد.
type: docs
url: /fa/com.aspose.slides/texthighlightingoptions/
---
**ارث‌بری:**
java.lang.Object

**همه رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.ITextHighlightingOptions](../../com.aspose.slides/itexthighlightingoptions)
```
public class TextHighlightingOptions implements ITextHighlightingOptions
```

نمایانگر گزینه‌هایی است که می‌توان برای برجسته‌سازی متن در چارچوب متن استفاده کرد.
## سازنده‌ها

| سازنده | توضیح |
| --- | --- |
| [TextHighlightingOptions()](#TextHighlightingOptions--) | گزینه‌های پیش‌فرض برجسته‌سازی متن را می‌سازد. |
## متدها

| متد | توضیح |
| --- | --- |
| [getCaseSensitive()](#getCaseSensitive--) | مقدار true را تنظیم کنید تا جستجوی حساس به حروف انجام شود، در غیر این صورت false. |
| [setCaseSensitive(boolean value)](#setCaseSensitive-boolean-) | مقدار true را تنظیم کنید تا جستجوی حساس به حروف انجام شود، در غیر این صورت false. |
| [getWholeWordsOnly()](#getWholeWordsOnly--) | مقدار true را تنظیم کنید تا فقط کلمات کامل مطابقت داده شوند، در غیر این صورت false. |
| [setWholeWordsOnly(boolean value)](#setWholeWordsOnly-boolean-) | مقدار true را تنظیم کنید تا فقط کلمات کامل مطابقت داده شوند، در غیر این صورت false. |
### TextHighlightingOptions() {#TextHighlightingOptions--}
```
public TextHighlightingOptions()
```

گزینه‌های پیش‌فرض برجسته‌سازی متن را می‌سازد.

### getCaseSensitive() {#getCaseSensitive--}
```
public final boolean getCaseSensitive()
```

مقدار true را تنظیم کنید تا جستجوی حساس به حروف انجام شود، در غیر این صورت false. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setCaseSensitive(boolean value) {#setCaseSensitive-boolean-}
```
public final void setCaseSensitive(boolean value)
```

مقدار true را تنظیم کنید تا جستجوی حساس به حروف انجام شود، در غیر این صورت false. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getWholeWordsOnly() {#getWholeWordsOnly--}
```
public final boolean getWholeWordsOnly()
```

مقدار true را تنظیم کنید تا فقط کلمات کامل مطابقت داده شوند، در غیر این صورت false. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setWholeWordsOnly(boolean value) {#setWholeWordsOnly-boolean-}
```
public final void setWholeWordsOnly(boolean value)
```

مقدار true را تنظیم کنید تا فقط کلمات کامل مطابقت داده شوند، در غیر این صورت false. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |