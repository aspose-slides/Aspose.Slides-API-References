---
title: OverrideTheme
second_title: Aspose.Slides برای مرجع API جاوا
description: نمایانگر یک تم حاکم بر دیگران است.
type: docs
url: /fa/com.aspose.slides/overridetheme/
---
**Inheritance:**  
java.lang.Object, [com.aspose.slides.Theme](../../com.aspose.slides/theme)

**All Implemented Interfaces:**  
[com.aspose.slides.IOverrideTheme](../../com.aspose.slides/ioverridetheme)  
```
public final class OverrideTheme extends Theme implements IOverrideTheme
```

نمایانگر یک تم حاکم بر دیگران است.
## متدها

| متد | توضیح |
| --- | --- |
| [initColorScheme()](#initColorScheme--) | مقداردهی اولیه ColorScheme با شیء جدید برای حاکم‌سازی ColorScheme تم ارث‌پذیرفته. |
| [initColorSchemeFrom(IColorScheme colorScheme)](#initColorSchemeFrom-com.aspose.slides.IColorScheme-) | مقداردهی اولیه ColorScheme با شیء جدید برای حاکم‌سازی ColorScheme تم ارث‌پذیرفته. |
| [initColorSchemeFromInherited()](#initColorSchemeFromInherited--) | مقداردهی اولیه ColorScheme با شیء جدید برای حاکم‌سازی ColorScheme تم ارث‌پذیرفته. |
| [initFontScheme()](#initFontScheme--) | مقداردهی اولیه FontScheme با شیء جدید برای حاکم‌سازی FontScheme تم ارث‌پذیرفته. |
| [initFontSchemeFrom(IFontScheme fontScheme)](#initFontSchemeFrom-com.aspose.slides.IFontScheme-) | مقداردهی اولیه FontScheme با شیء جدید برای حاکم‌سازی FontScheme تم ارث‌پذیرفته. |
| [initFontSchemeFromInherited()](#initFontSchemeFromInherited--) | مقداردهی اولیه FontScheme با شیء جدید برای حاکم‌سازی FontScheme تم ارث‌پذیرفته. |
| [initFormatScheme()](#initFormatScheme--) | مقداردهی اولیه FormatScheme با شیء جدید برای حاکم‌سازی FormatScheme تم ارث‌پذیرفته. |
| [initFormatSchemeFrom(IFormatScheme formatScheme)](#initFormatSchemeFrom-com.aspose.slides.IFormatScheme-) | مقداردهی اولیه FormatScheme با شیء جدید برای حاکم‌سازی FormatScheme تم ارث‌پذیرفته. |
| [initFormatSchemeFromInherited()](#initFormatSchemeFromInherited--) | مقداردهی اولیه FormatScheme با شیء جدید برای حاکم‌سازی FormatScheme تم ارث‌پذیرفته. |
| [getColorScheme()](#getColorScheme--) | طرح رنگ را برمی‌گرداند. |
| [getFontScheme()](#getFontScheme--) | طرح قلم را برمی‌گرداند. |
| [getFormatScheme()](#getFormatScheme--) | طرح قالب شکل را برمی‌گرداند. |
| [isEmpty()](#isEmpty--) | مقدار true به این معناست که ColorScheme، FontScheme و FormatScheme تهی (null) هستند و هر گونه حاکم‌سازی با این شیء تم غیرفعال شده است. |
| [clear()](#clear--) | برای غیرفعال‌کردن هر حاکم‌سازی با این شیء تم، ColorScheme، FontScheme و FormatScheme را به null تنظیم می‌کند. |
| [getVersion()](#getVersion--) |  |

### initColorScheme() {#initColorScheme--}
```
public final void initColorScheme()
```

ColorScheme را با شیء جدید برای حاکم‌سازی ColorScheme تم ارث‌پذیرفته مقداردهی می‌کند.

### initColorSchemeFrom(IColorScheme colorScheme) {#initColorSchemeFrom-com.aspose.slides.IColorScheme-}
```
public final void initColorSchemeFrom(IColorScheme colorScheme)
```

ColorScheme را با شیء جدید برای حاکم‌سازی ColorScheme تم ارث‌پذیرفته مقداردهی می‌کند.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| colorScheme | [IColorScheme](../../com.aspose.slides/icolorscheme) | داده‌های مورد نیاز برای مقداردهی اولیه. |

### initColorSchemeFromInherited() {#initColorSchemeFromInherited--}
```
public final void initColorSchemeFromInherited()
```

ColorScheme را با شیء جدید برای حاکم‌سازی ColorScheme تم ارث‌پذیرفته مقداردهی می‌کند و داده‌های این شیء جدید را با داده‌های ColorScheme تم ارث‌پذیرفته مقداردهی می‌کند.

### initFontScheme() {#initFontScheme--}
```
public final void initFontScheme()
```

FontScheme را با شیء جدید برای حاکم‌سازی FontScheme تم ارث‌پذیرفته مقداردهی می‌کند.

### initFontSchemeFrom(IFontScheme fontScheme) {#initFontSchemeFrom-com.aspose.slides.IFontScheme-}
```
public final void initFontSchemeFrom(IFontScheme fontScheme)
```

FontScheme را با شیء جدید برای حاکم‌سازی FontScheme تم ارث‌پذیرفته مقداردهی می‌کند.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| fontScheme | [IFontScheme](../../com.aspose.slides/ifontscheme) | داده‌های مورد نیاز برای مقداردهی اولیه. |

### initFontSchemeFromInherited() {#initFontSchemeFromInherited--}
```
public final void initFontSchemeFromInherited()
```

FontScheme را با شیء جدید برای حاکم‌سازی FontScheme تم ارث‌پذیرفته مقداردهی می‌کند و داده‌های این شیء جدید را با داده‌های FontScheme تم ارث‌پذیرفته مقداردهی می‌کند.

### initFormatScheme() {#initFormatScheme--}
```
public final void initFormatScheme()
```

FormatScheme را با شیء جدید برای حاکم‌سازی FormatScheme تم ارث‌پذیرفته مقداردهی می‌کند.

### initFormatSchemeFrom(IFormatScheme formatScheme) {#initFormatSchemeFrom-com.aspose.slides.IFormatScheme-}
```
public final void initFormatSchemeFrom(IFormatScheme formatScheme)
```

FormatScheme را با شیء جدید برای حاکم‌سازی FormatScheme تم ارث‌پذیرفته مقداردهی می‌کند.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| formatScheme | [IFormatScheme](../../com.aspose.slides/iformatscheme) | داده‌های مورد نیاز برای مقداردهی اولیه. |

### initFormatSchemeFromInherited() {#initFormatSchemeFromInherited--}
```
public final void initFormatSchemeFromInherited()
```

FormatScheme را با شیء جدید برای حاکم‌سازی FormatScheme تم ارث‌پذیرفته مقداردهی می‌کند و داده‌های این شیء جدید را با داده‌های FormatScheme تم ارث‌پذیرفته مقداردهی می‌کند.

### getColorScheme() {#getColorScheme--}
```
public IColorScheme getColorScheme()
```

طرح رنگ را برمی‌گرداند. فقط-خواندنی [IColorScheme](../../com.aspose.slides/icolorscheme).

**بازگردادها:**
[IColorScheme](../../com.aspose.slides/icolorscheme)

### getFontScheme() {#getFontScheme--}
```
public IFontScheme getFontScheme()
```

طرح قلم را برمی‌گرداند. فقط-خواندنی [IFontScheme](../../com.aspose.slides/ifontscheme).

**بازگردادها:**
[IFontScheme](../../com.aspose.slides/ifontscheme)

### getFormatScheme() {#getFormatScheme--}
```
public IFormatScheme getFormatScheme()
```

طرح قالب شکل را برمی‌گرداند. فقط-خواندنی [IFormatScheme](../../com.aspose.slides/iformatscheme).

**بازگردادها:**
[IFormatScheme](../../com.aspose.slides/iformatscheme)

### isEmpty() {#isEmpty--}
```
public final boolean isEmpty()
```

مقدار true به این معناست که ColorScheme، FontScheme و FormatScheme تهی (null) هستند و هر گونه حاکم‌سازی با این شیء تم غیرفعال شده است. فقط-خواندنی بولی.

**بازگردادها:**
boolean

### clear() {#clear--}
```
public final void clear()
```

ColorScheme، FontScheme و FormatScheme را به null تنظیم می‌کند تا هر حاکم‌سازی با این شیء تم غیرفعال شود.

### getVersion() {#getVersion--}
```
public long getVersion()
```

نسخه. فقط-خواندنی long.

**بازگردادها:**
long