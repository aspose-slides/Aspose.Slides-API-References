---
title: ScaleEffect
second_title: مرجع API Aspose.Slides برای جاوا
description: نمایانگر اثر مقیاس‌گذاری انیمیشن.
type: docs
url: /fa/com.aspose.slides/scaleeffect/
---
**ارث‌بری:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**تمام رابط‌های پیاده‌سازی شده:**
[com.aspose.slides.IScaleEffect](../../com.aspose.slides/iscaleeffect)
```
public class ScaleEffect extends Behavior implements IScaleEffect
```

نمایانگر اثر مقیاس‌گذاری انیمیشن.
## سازنده‌ها

| سازنده | توضیح |
| --- | --- |
| [ScaleEffect()](#ScaleEffect--) |  |
## متدها

| متد | توضیح |
| --- | --- |
| [getZoomContent()](#getZoomContent--) | تعیین می‌کند که آیا محتوا باید بزرگنمایی شود یا خیر. |
| [setZoomContent(byte value)](#setZoomContent-byte-) | تعیین می‌کند که آیا محتوا باید بزرگنمایی شود یا خیر. |
| [getFrom()](#getFrom--) | مختصات x/y را برای شروع انیمیشن تعیین می‌کند (به درصد). |
| [setFrom(Point2D.Float value)](#setFrom-java.awt.geom.Point2D.Float-) | مختصات x/y را برای شروع انیمیشن تعیین می‌کند (به درصد). |
| [getTo()](#getTo--) | موقعیت هدف برای اثر مقیاس‌گذاری انیمیشن را تعیین می‌کند (به درصد). |
| [setTo(Point2D.Float value)](#setTo-java.awt.geom.Point2D.Float-) | موقعیت هدف برای اثر مقیاس‌گذاری انیمیشن را تعیین می‌کند (به درصد). |
| [getBy()](#getBy--) | مقدار جابجایی نسبی انیمیشن را توصیف می‌کند (به درصد). |
| [setBy(Point2D.Float value)](#setBy-java.awt.geom.Point2D.Float-) | مقدار جابجایی نسبی انیمیشن را توصیف می‌کند (به درصد). |
### ScaleEffect() {#ScaleEffect--}
```
public ScaleEffect()
```


### getZoomContent() {#getZoomContent--}
```
public final byte getZoomContent()
```


تعیین می‌کند که آیا محتوا باید بزرگنمایی شود یا خیر. خواندن/نوشتن [NullableBool](../../com.aspose.slides/nullablebool).

**بازگشت:**
byte
### setZoomContent(byte value) {#setZoomContent-byte-}
```
public final void setZoomContent(byte value)
```


تعیین می‌کند که آیا محتوا باید بزرگنمایی شود یا خیر. خواندن/نوشتن [NullableBool](../../com.aspose.slides/nullablebool).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | byte |  |

### getFrom() {#getFrom--}
```
public final Point2D.Float getFrom()
```


مختصات x/y را برای شروع انیمیشن تعیین می‌کند (به درصد). خواندن/نوشتن java.awt.geom.Point2D.Float.

**بازگشت:**
java.awt.geom.Point2D.Float
### setFrom(Point2D.Float value) {#setFrom-java.awt.geom.Point2D.Float-}
```
public final void setFrom(Point2D.Float value)
```


مختصات x/y را برای شروع انیمیشن تعیین می‌کند (به درصد). خواندن/نوشتن java.awt.geom.Point2D.Float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getTo() {#getTo--}
```
public final Point2D.Float getTo()
```


موقعیت هدف برای اثر مقیاس‌گذاری انیمیشن را تعیین می‌کند (به درصد). خواندن/نوشتن java.awt.geom.Point2D.Float.

**بازگشت:**
java.awt.geom.Point2D.Float
### setTo(Point2D.Float value) {#setTo-java.awt.geom.Point2D.Float-}
```
public final void setTo(Point2D.Float value)
```


موقعیت هدف برای اثر مقیاس‌گذاری انیمیشن را تعیین می‌کند (به درصد). خواندن/نوشتن java.awt.geom.Point2D.Float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getBy() {#getBy--}
```
public final Point2D.Float getBy()
```


مقدار جابجایی نسبی انیمیشن را توصیف می‌کند (به درصد). خواندن/نوشتن java.awt.geom.Point2D.Float.

**بازگشت:**
java.awt.geom.Point2D.Float
### setBy(Point2D.Float value) {#setBy-java.awt.geom.Point2D.Float-}
```
public final void setBy(Point2D.Float value)
```


مقدار جابجایی نسبی انیمیشن را توصیف می‌کند (به درصد). خواندن/نوشتن java.awt.geom.Point2D.Float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |