---
title: TransitionValueBase
second_title: Aspose.Slides برای مرجع API جاوا
description: کلاس پایه برای اثرات انتقال اسلاید.
type: docs
url: /fa/com.aspose.slides/transitionvaluebase/
---
**ارث‌بری:**
java.lang.Object

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.ITransitionValueBase](../../com.aspose.slides/itransitionvaluebase)
```
public class TransitionValueBase implements ITransitionValueBase
```

کلاس پایه برای اثرات انتقال اسلاید.
## متدها

| متد | توضیح |
| --- | --- |
| [equals(Object obj)](#equals-java.lang.Object-) | تعیین می‌کند که آیا دو نمونه TransitionValueBase برابر هستند یا خیر. |
| [hashCode()](#hashCode--) | به‌عنوان یک تابع هش برای نوع خاصی عمل می‌کند که برای الگوریتم‌های هش و ساختارهای داده‌ای مانند جدول هش مناسب است. |
### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```


تعیین می‌کند که آیا دو نمونه TransitionValueBase برابر هستند یا خیر.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| obj | java.lang.Object | TransitionValueBase‌ای که با TransitionValueBase فعلی مقایسه می‌شود. |

**مقدار بازگشتی:**
boolean - **true** اگر TransitionValueBase مشخص‌شده با TransitionValueBase فعلی برابر باشد؛ در غیر این صورت، **false**.
### hashCode() {#hashCode--}
```
public int hashCode()
```


به‌عنوان یک تابع هش برای نوع خاصی عمل می‌کند که برای الگوریتم‌های هش و ساختارهای داده‌ای مانند جدول هش مناسب است.

**مقدار بازگشتی:**
int - 23454

--------------------

بازنویسی شده برای رضایت کامپایلر. همیشه مقدار ثابت باز می‌گرداند زیرا شیء قابل تغییر است.