---
title: IFontScheme
second_title: Aspose.Slides for Java API Reference
description: فونت‌های تعریف‌شده در قالب را ذخیره می‌کند.
type: docs
url: /fa/com.aspose.slides/ifontscheme/
---```
public interface IFontScheme
```

فونت‌های تعریف‌شده در قالب را ذخیره می‌کند.
## متدها

| متد | توضیح |
| --- | --- |
| [getMinor()](#getMinor--) | مجموعهٔ فونت‌ها را برای بخش «بدنه» اسلاید برمی‌گرداند. |
| [getMajor()](#getMajor--) | مجموعهٔ فونت‌ها را برای بخش «سرصفحه» اسلاید برمی‌گرداند. |
| [getName()](#getName--) | نام طرح‌بندی فونت را برمی‌گرداند. |
| [setName(String value)](#setName-java.lang.String-) | نام طرح‌بندی فونت را برمی‌گرداند. |
### getMinor() {#getMinor--}
```
public abstract IFonts getMinor()
```


مجموعهٔ فونت‌ها را برای بخش «بدنه» اسلاید برمی‌گرداند. فقط-خواندنی [IFonts](../../com.aspose.slides/ifonts).

**مقدار بازگشتی:**
[IFonts](../../com.aspose.slides/ifonts)
### getMajor() {#getMajor--}
```
public abstract IFonts getMajor()
```


مجموعهٔ فونت‌ها را برای بخش «سرصفحه» اسلاید برمی‌گرداند. فقط-خواندنی [IFonts](../../com.aspose.slides/ifonts).

**مقدار بازگشتی:**
[IFonts](../../com.aspose.slides/ifonts)
### getName() {#getName--}
```
public abstract String getName()
```


نام طرح‌بندی فونت را برمی‌گرداند. خواندنی/نوشتنی String.

**مقدار بازگشتی:**
java.lang.String
### setName(String value) {#setName-java.lang.String-}
```
public abstract void setName(String value)
```


نام طرح‌بندی فونت را برمی‌گرداند. خواندنی/نوشتنی String.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | java.lang.String |  |