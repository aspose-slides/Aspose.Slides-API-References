---
title: IBulletFormat
second_title: Aspose.Slides for Java API Reference
description: نمایندهٔ ویژگی‌های قالب‌بندی گلوله‌های پاراگراف.
type: docs
url: /fa/com.aspose.slides/ibulletformat/
---```
public interface IBulletFormat
```

نمایندهٔ ویژگی‌های قالب‌بندی گلوله‌های پاراگراف.
## Methods

| Method | Description |
| --- | --- |
| [getType()](#getType--) | دریافت یا تنظیم نوع گلولهٔ یک پاراگراف بدون ارث‌بری. |
| [setType(byte value)](#setType-byte-) | دریافت یا تنظیم نوع گلولهٔ یک پاراگراف بدون ارث‌بری. |
| [getChar()](#getChar--) | دریافت یا تنظیم کاراکتر گلولهٔ یک پاراگراف بدون ارث‌بری. |
| [setChar(char value)](#setChar-char-) | دریافت یا تنظیم کاراکتر گلولهٔ یک پاراگراف بدون ارث‌بری. |
| [getFont()](#getFont--) | دریافت یا تنظیم قلم گلولهٔ یک پاراگراف بدون ارث‌بری. |
| [setFont(IFontData value)](#setFont-com.aspose.slides.IFontData-) | دریافت یا تنظیم قلم گلولهٔ یک پاراگراف بدون ارث‌بری. |
| [getHeight()](#getHeight--) | دریافت یا تنظیم ارتفاع گلولهٔ یک پاراگراف بدون ارث‌بری. |
| [setHeight(float value)](#setHeight-float-) | دریافت یا تنظیم ارتفاع گلولهٔ یک پاراگراف بدون ارث‌بری. |
| [getColor()](#getColor--) | دریافت فرمت رنگ یک گلولهٔ پاراگراف بدون ارث‌بری. |
| [getPicture()](#getPicture--) | دریافت تصویر استفاده‌شده به عنوان گلوله در یک پاراگراف بدون ارث‌بری. |
| [getNumberedBulletStartWith()](#getNumberedBulletStartWith--) | دریافت یا تنظیم اولین شمارهٔ مورد استفاده برای گروهی از گلوله‌های شماره‌دار بدون ارث‌بری. |
| [setNumberedBulletStartWith(short value)](#setNumberedBulletStartWith-short-) | دریافت یا تنظیم اولین شمارهٔ مورد استفاده برای گروهی از گلوله‌های شماره‌دار بدون ارث‌بری. |
| [getNumberedBulletStyle()](#getNumberedBulletStyle--) | دریافت یا تنظیم سبک یک گلوله شماره‌دار بدون ارث‌بری. |
| [setNumberedBulletStyle(byte value)](#setNumberedBulletStyle-byte-) | دریافت یا تنظیم سبک یک گلوله شماره‌دار بدون ارث‌بری. |
| [isBulletHardColor()](#isBulletHardColor--) | تشخیص اینکه آیا گلوله رنگ خود را دارد یا از اولین بخش در پاراگراف به ارث می‌برد. |
| [setBulletHardColor(byte value)](#setBulletHardColor-byte-) | تشخیص اینکه آیا گلوله رنگ خود را دارد یا از اولین بخش در پاراگراف به ارث می‌برد. |
| [isBulletHardFont()](#isBulletHardFont--) | تشخیص اینکه آیا گلوله قلم خود را دارد یا از اولین بخش در پاراگراف به ارث می‌برد. |
| [setBulletHardFont(byte value)](#setBulletHardFont-byte-) | تشخیص اینکه آیا گلوله قلم خود را دارد یا از اولین بخش در پاراگراف به ارث می‌برد. |
| [applyDefaultParagraphIndentsShifts()](#applyDefaultParagraphIndentsShifts--) | تنظیم شیفت‌های پیش‌فرض غیر-صفر برای تورفتگی و MarginLeft مؤثر پاراگراف هنگام فعال بودن گلوله‌ها (مانند کاری که PowerPoint انجام می‌دهد وقتی پاراگراف-گلوله/شماره‌گذاری را فعال می‌کند). |
| [getEffective()](#getEffective--) | دریافت داده‌های قالب‌بندی مؤثر گلوله با اعمال ارث‌بری. |
### getType() {#getType--}
```
public abstract byte getType()
```

دریافت یا تنظیم نوع گلولهٔ یک پاراگراف بدون ارث‌بری. خواندن/نوشتن [BulletType](../../com.aspose.slides/bullettype).

**باز می‌گردد:**
byte
### setType(byte value) {#setType-byte-}
```
public abstract void setType(byte value)
```

دریافت یا تنظیم نوع گلولهٔ یک پاراگراف بدون ارث‌بری. خواندن/نوشتن [BulletType](../../com.aspose.slides/bullettype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | byte |  |

### getChar() {#getChar--}
```
public abstract char getChar()
```

دریافت یا تنظیم کاراکتر گلولهٔ یک پاراگراف بدون ارث‌بری. خواندن/نوشتن char.

**باز می‌گردد:**
char
### setChar(char value) {#setChar-char-}
```
public abstract void setChar(char value)
```

دریافت یا تنظیم کاراکتر گلولهٔ یک پاراگراف بدون ارث‌بری. خواندن/نوشتن char.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | char |  |

### getFont() {#getFont--}
```
public abstract IFontData getFont()
```

دریافت یا تنظیم قلم گلولهٔ یک پاراگراف بدون ارث‌بری. خواندن/نوشتن [IFontData](../../com.aspose.slides/ifontdata).

**باز می‌گردد:**
[IFontData](../../com.aspose.slides/ifontdata)
### setFont(IFontData value) {#setFont-com.aspose.slides.IFontData-}
```
public abstract void setFont(IFontData value)
```

دریافت یا تنظیم قلم گلولهٔ یک پاراگراف بدون ارث‌بری. خواندن/نوشتن [IFontData](../../com.aspose.slides/ifontdata).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | [IFontData](../../com.aspose.slides/ifontdata) |  |

### getHeight() {#getHeight--}
```
public abstract float getHeight()
```

دریافت یا تنظیم ارتفاع گلولهٔ یک پاراگراف بدون ارث‌بری. مقدار Float.NaN نشان می‌دهد که گلوله ارتفاع را از اولین بخش در پاراگراف به ارث می‌برد. خواندن/نوشتن float.

**باز می‌گردد:**
float
### setHeight(float value) {#setHeight-float-}
```
public abstract void setHeight(float value)
```

دریافت یا تنظیم ارتفاع گلولهٔ یک پاراگراف بدون ارث‌بری. مقدار Float.NaN نشان می‌دهد که گلوله ارتفاع را از اولین بخش در پاراگراف به ارث می‌برد. خواندن/نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getColor() {#getColor--}
```
public abstract IColorFormat getColor()
```

دریافت فرمت رنگ یک گلولهٔ پاراگراف بدون ارث‌بری. فقط-خواندنی [IColorFormat](../../com.aspose.slides/icolorformat).

**باز می‌گردد:**
[IColorFormat](../../com.aspose.slides/icolorformat)
### getPicture() {#getPicture--}
```
public abstract ISlidesPicture getPicture()
```

دریافت تصویر استفاده‌شده به عنوان گلوله در یک پاراگراف بدون ارث‌بری. فقط-خواندنی [ISlidesPicture](../../com.aspose.slides/islidespicture).

**باز می‌گردد:**
[ISlidesPicture](../../com.aspose.slides/islidespicture)
### getNumberedBulletStartWith() {#getNumberedBulletStartWith--}
```
public abstract short getNumberedBulletStartWith()
```

دریافت یا تنظیم اولین شمارهٔ مورد استفاده برای گروهی از گلوله‌های شماره‌دار بدون ارث‌بری. خواندن/نوشتن short.

**باز می‌گردد:**
short
### setNumberedBulletStartWith(short value) {#setNumberedBulletStartWith-short-}
```
public abstract void setNumberedBulletStartWith(short value)
```

دریافت یا تنظیم اولین شمارهٔ مورد استفاده برای گروهی از گلوله‌های شماره‌دار بدون ارث‌بری. خواندن/نوشتن short.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | short |  |

### getNumberedBulletStyle() {#getNumberedBulletStyle--}
```
public abstract byte getNumberedBulletStyle()
```

دریافت یا تنظیم سبک یک گلوله شماره‌دار بدون ارث‌بری. خواندن/نوشتن [NumberedBulletStyle](../../com.aspose.slides/numberedbulletstyle)(\#getNumberedBulletStyle.getNumberedBulletStyle/\#setNumberedBulletStyle(byte).setNumberedBulletStyle(byte)).

**باز می‌گردد:**
byte
### setNumberedBulletStyle(byte value) {#setNumberedBulletStyle-byte-}
```
public abstract void setNumberedBulletStyle(byte value)
```

دریافت یا تنظیم سبک یک گلوله شماره‌دار بدون ارث‌بری. خواندن/نوشتن [NumberedBulletStyle](../../com.aspose.slides/numberedbulletstyle)(\#getNumberedBulletStyle.getNumberedBulletStyle/\#setNumberedBulletStyle(byte).setNumberedBulletStyle(byte)).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | byte |  |

### isBulletHardColor() {#isBulletHardColor--}
```
public abstract byte isBulletHardColor()
```

تشخیص اینکه آیا گلوله رنگ خود را دارد یا از اولین بخش در پاراگراف به ارث می‌برد. **NullableBool\#True** اگر گلوله رنگ خود را داشته باشد و **NullableBool\#False** اگر رنگ را از اولین بخش در پاراگراف به ارث ببرد. خواندن/نوشتن [NullableBool](../../com.aspose.slides/nullablebool).

**باز می‌گردد:**
byte
### setBulletHardColor(byte value) {#setBulletHardColor-byte-}
```
public abstract void setBulletHardColor(byte value)
```

تشخیص اینکه آیا گلوله رنگ خود را دارد یا از اولین بخش در پاراگراف به ارث می‌برد. **NullableBool\#True** اگر گلوله رنگ خود را داشته باشد و **NullableBool\#False** اگر رنگ را از اولین بخش در پاراگراف به ارث ببرد. خواندن/نوشتن [NullableBool](../../com.aspose.slides/nullablebool).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | byte |  |

### isBulletHardFont() {#isBulletHardFont--}
```
public abstract byte isBulletHardFont()
```

تشخیص اینکه آیا گلوله قلم خود را دارد یا از اولین بخش در پاراگراف به ارث می‌برد. **NullableBool\#True** اگر گلوله قلم خود را داشته باشد و **NullableBool\#False** اگر قلم را از اولین بخش در پاراگراف به ارث ببرد. خواندن/نوشتن [NullableBool](../../com.aspose.slides/nullablebool).

**باز می‌گردد:**
byte
### setBulletHardFont(byte value) {#setBulletHardFont-byte-}
```
public abstract void setBulletHardFont(byte value)
```

تشخیص اینکه آیا گلوله قلم خود را دارد یا از اولین بخش در پاراگراف به ارث می‌برد. **NullableBool\#True** اگر گلوله قلم خود را داشته باشد و **NullableBool\#False** اگر قلم را از اولین بخش در پاراگراف به ارث ببرد. خواندن/نوشتن [NullableBool](../../com.aspose.slides/nullablebool).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | byte |  |

### applyDefaultParagraphIndentsShifts() {#applyDefaultParagraphIndentsShifts--}
```
public abstract void applyDefaultParagraphIndentsShifts()
```

تنظیم شیفت‌های پیش‌فرض غیر-صفر برای تورفتگی و MarginLeft مؤثر پاراگراف هنگام فعال بودن گلوله‌ها (مانند کاری که PowerPoint انجام می‌دهد وقتی پاراگراف-گلوله/شماره‌گذاری را فعال می‌کند). اگر گلوله‌ها غیرفعال شوند، تنها تورفتگی و MarginLeft پاراگراف بازنشانی می‌شود (مانند کاری که PowerPoint انجام می‌دهد وقتی این قابلیت را غیرفعال می‌کند). شیفت‌های تورفتگی بر پایهٔ زمینهٔ فعلی گلوله - IBulletFormat.Type، .NumberedBulletStyle و FontHeight اولین بخش اعمال می‌شوند. شیفت‌های غیر-صفر بر روی Indent و MarginLeft مؤثر پاراگراف فعلی اعمال می‌شوند (مقدارهای نتیجه به مقادیر محلی تبدیل می‌شوند).

### getEffective() {#getEffective--}
```
public abstract IBulletFormatEffectiveData getEffective()
```

دریافت داده‌های قالب‌بندی مؤثر گلوله با اعمال ارث‌بری.

--------------------

> ```
> This example demonstrates getting some effective bullet format properties.
>  
>  Presentation pres = new Presentation("MyPresentation.pptx");
>  try {
>      IAutoShape shape = (IAutoShape)pres.getSlides().get_Item(0).getShapes().get_Item(0);
>      IBulletFormatEffectiveData effectiveBulletFormat = shape.getTextFrame().getParagraphs().get_Item(0).getParagraphFormat().getBullet().getEffective();
>      System.out.println("Bullet type: " + effectiveBulletFormat.getType());
>      if (effectiveBulletFormat.getType() == BulletType.Numbered)
>      {
>          System.out.println("Numbered style: " + effectiveBulletFormat.getNumberedBulletStyle());
>          System.out.println("Starting number: " + effectiveBulletFormat.getNumberedBulletStartWith());
>      }
>  } finally {
>      if (pres != null) pres.dispose();
>  }
> ```


**باز می‌گردد:**
[IBulletFormatEffectiveData](../../com.aspose.slides/ibulletformateffectivedata) - A [IBulletFormatEffectiveData](../../com.aspose.slides/ibulletformateffectivedata).