---
title: SlideShowTransition
second_title: مرجع API Aspose.Slides برای Java
description: نماینده انتقال نمایش اسلاید.
type: docs
url: /fa/com.aspose.slides/slideshowtransition/
---
**ارث‌بری:**
java.lang.Object, com.aspose.slides.DomObject

**تمام رابط‌های پیاده‌سازی شده:**
[com.aspose.slides.ISlideShowTransition](../../com.aspose.slides/islideshowtransition)
```
public class SlideShowTransition extends DomObject<BaseSlide> implements ISlideShowTransition
```

نماینده انتقال نمایش اسلاید.
## متدها

| متد | توضیحات |
| --- | --- |
| [getSound()](#getSound--) | داده‌های صوتی نهفته را بر می‌گرداند یا تنظیم می‌کند. |
| [setSound(IAudio value)](#setSound-com.aspose.slides.IAudio-) | داده‌های صوتی نهفته را بر می‌گرداند یا تنظیم می‌کند. |
| [getSoundMode()](#getSoundMode--) | حالت صوت را برای انتقال اسلاید تنظیم یا بر می‌گرداند. |
| [setSoundMode(int value)](#setSoundMode-int-) | حالت صوت را برای انتقال اسلاید تنظیم یا بر می‌گرداند. |
| [getSoundLoop()](#getSoundLoop--) | این ویژگی مشخص می‌کند که آیا صدا تا وقوع رویداد صوتی بعدی در نمایش اسلاید حلقه می‌شود یا خیر. |
| [setSoundLoop(boolean value)](#setSoundLoop-boolean-) | این ویژگی مشخص می‌کند که آیا صدا تا وقوع رویداد صوتی بعدی در نمایش اسلاید حلقه می‌شود یا خیر. |
| [getAdvanceOnClick()](#getAdvanceOnClick--) | مشخص می‌کند آیا کلیک ماوس اسلاید را پیشروی می‌دهد یا خیر. |
| [setAdvanceOnClick(boolean value)](#setAdvanceOnClick-boolean-) | مشخص می‌کند آیا کلیک ماوس اسلاید را پیشروی می‌دهد یا خیر. |
| [getAdvanceAfter()](#getAdvanceAfter--) | این ویژگی مشخص می‌کند که آیا نمایش اسلاید پس از زمان معینی به اسلاید بعدی می‌رود یا خیر. |
| [setAdvanceAfter(boolean value)](#setAdvanceAfter-boolean-) | این ویژگی مشخص می‌کند که آیا نمایش اسلاید پس از زمان معینی به اسلاید بعدی می‌رود یا خیر. |
| [getAdvanceAfterTime()](#getAdvanceAfterTime--) | زمانی را بر میلی‌ثانیه مشخص می‌کند که پس از آن انتقال باید آغاز شود. |
| [setAdvanceAfterTime(long value)](#setAdvanceAfterTime-long-) | زمانی را بر میلی‌ثانیه مشخص می‌کند که پس از آن انتقال باید آغاز شود. |
| [getSpeed()](#getSpeed--) | سرعت انتقالی که هنگام انتقال از اسلاید جاری به اسلاید بعدی استفاده می‌شود را مشخص می‌کند. |
| [setSpeed(int value)](#setSpeed-int-) | سرعت انتقالی که هنگام انتقال از اسلاید جاری به اسلاید بعدی استفاده می‌شود را مشخص می‌کند. |
| [getValue()](#getValue--) | مقدار انتقال اسلاید نمایش. |
| [getType()](#getType--) | نوع انتقال. |
| [setType(int value)](#setType-int-) | نوع انتقال. |
| [getSoundIsBuiltIn()](#getSoundIsBuiltIn--) | مشخص می‌کند آیا این صدا یک صدا داخلی است یا خیر. |
| [setSoundIsBuiltIn(boolean value)](#setSoundIsBuiltIn-boolean-) | مشخص می‌کند آیا این صدا یک صدا داخلی است یا خیر. |
| [getSoundName()](#getSoundName--) | نام قابل خواندن توسط انسان برای صدای انتقال را مشخص می‌کند. |
| [setSoundName(String value)](#setSoundName-java.lang.String-) | نام قابل خواندن توسط انسان برای صدای انتقال را مشخص می‌کند. |
| [getDuration()](#getDuration--) | مدت اثر انتقال اسلاید را بر میلی‌ثانیه بر می‌گرداند یا تنظیم می‌کند. |
| [setDuration(int value)](#setDuration-int-) | مدت اثر انتقال اسلاید را بر میلی‌ثانیه بر می‌گرداند یا تنظیم می‌کند. |
| [equals(Object obj)](#equals-java.lang.Object-) | تعیین می‌کند آیا دو نمونه SlideShowTransition برابر هستند یا خیر. |
| [hashCode()](#hashCode--) | به‌عنوان تابع هش برای یک نوع خاص عمل می‌کند که برای الگوریتم‌های هش و ساختارهای داده‌ای مانند جدول هش مناسب است. |

### getSound() {#getSound--}
```
public final IAudio getSound()
```

داده‌های صوتی نهفته را بر می‌گرداند یا تنظیم می‌کند. خواندن/نوشتن [IAudio](../../com.aspose.slides/iaudio).

**بازگشت:**
[IAudio](../../com.aspose.slides/iaudio)

### setSound(IAudio value) {#setSound-com.aspose.slides.IAudio-}
```
public final void setSound(IAudio value)
```

داده‌های صوتی نهفته را بر می‌گرداند یا تنظیم می‌کند. خواندن/نوشتن [IAudio](../../com.aspose.slides/iaudio).

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | [IAudio](../../com.aspose.slides/iaudio) |  |

### getSoundMode() {#getSoundMode--}
```
public final int getSoundMode()
```

حالت صوت را برای انتقال اسلاید تنظیم یا بر می‌گرداند. خواندن/نوشتن [TransitionSoundMode](../../com.aspose.slides/transitionsoundmode).

**بازگشت:**
int

### setSoundMode(int value) {#setSoundMode-int-}
```
public final void setSoundMode(int value)
```

حالت صوت را برای انتقال اسلاید تنظیم یا بر می‌گرداند. خواندن/نوشتن [TransitionSoundMode](../../com.aspose.slides/transitionsoundmode).

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | int |  |

### getSoundLoop() {#getSoundLoop--}
```
public final boolean getSoundLoop()
```

این ویژگی مشخص می‌کند که آیا صدا تا وقوع رویداد صوتی بعدی در نمایش اسلاید حلقه می‌شود یا خیر. خواندن/نوشتن boolean.

**بازگشت:**
boolean

### setSoundLoop(boolean value) {#setSoundLoop-boolean-}
```
public final void setSoundLoop(boolean value)
```

این ویژگی مشخص می‌کند که آیا صدا تا وقوع رویداد صوتی بعدی در نمایش اسلاید حلقه می‌شود یا خیر. خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | boolean |  |

### getAdvanceOnClick() {#getAdvanceOnClick--}
```
public final boolean getAdvanceOnClick()
```

مشخص می‌کند آیا کلیک ماوس اسلاید را پیشروی می‌دهد یا خیر. اگر این ویژگی مشخص نشده باشد مقدار true فرض می‌شود. خواندن/نوشتن boolean.

**بازگشت:**
boolean

### setAdvanceOnClick(boolean value) {#setAdvanceOnClick-boolean-}
```
public final void setAdvanceOnClick(boolean value)
```

مشخص می‌کند آیا کلیک ماوس اسلاید را پیشروی می‌دهد یا خیر. اگر این ویژگی مشخص نشده باشد مقدار true فرض می‌شود. خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | boolean |  |

### getAdvanceAfter() {#getAdvanceAfter--}
```
public final boolean getAdvanceAfter()
```

این ویژگی مشخص می‌کند که آیا نمایش اسلاید پس از زمان معینی به اسلاید بعدی می‌رود یا خیر. خواندن/نوشتن boolean.

--------------------

> ```
> Presentation pres = new Presentation("demo.pptx");
>  try {
>      // دریافت اولین انتقال اسلاید
>      ISlideShowTransition slideTransition = pres.getSlides().get_Item(0).getSlideShowTransition();
> 
>      // بررسی اینکه آیا پرچم پیشروی اسلاید پس از زمان فعال است یا خیر
>      if (slideTransition.getAdvanceAfter())
>      {
>          // دریافت مقدار زمان پیشروی اسلاید پس از زمان
>          long advanceAfterTime = slideTransition.getAdvanceAfterTime();
>      }
>  } finally {
>      if (pres != null) pres.dispose();
>  }
> ```

**بازگشت:**
boolean

### setAdvanceAfter(boolean value) {#setAdvanceAfter-boolean-}
```
public final void setAdvanceAfter(boolean value)
```

این ویژگی مشخص می‌کند که آیا نمایش اسلاید پس از زمان معینی به اسلاید بعدی می‌رود یا خیر. خواندن/نوشتن boolean.

--------------------

> ```
> Presentation pres = new Presentation("demo.pptx");
>  try {
>      // دریافت اولین انتقال اسلاید
>      ISlideShowTransition slideTransition = pres.getSlides().get_Item(0).getSlideShowTransition();
> 
>      // بررسی اینکه آیا پرچم پیشروی اسلاید پس از زمان فعال است یا خیر
>      if (slideTransition.getAdvanceAfter())
>      {
>          // دریافت مقدار زمان پیشروی اسلاید پس از زمان
>          long advanceAfterTime = slideTransition.getAdvanceAfterTime();
>      }
>  } finally {
>      if (pres != null) pres.dispose();
>  }
> ```


**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | boolean |  |

### getAdvanceAfterTime() {#getAdvanceAfterTime--}
```
public final long getAdvanceAfterTime()
```

زمانی را بر میلی‌ثانیه مشخص می‌کند که پس از آن انتقال باید آغاز شود. این تنظیم می‌تواند همراه با ویژگی advClick استفاده شود. اگر این ویژگی مشخص نشده باشد فرض می‌شود خودکار پیشروی رخ نمی‌دهد. خواندن/نوشتن long.

**بازگشت:**
long

### setAdvanceAfterTime(long value) {#setAdvanceAfterTime-long-}
```
public final void setAdvanceAfterTime(long value)
```

زمانی را بر میلی‌ثانیه مشخص می‌کند که پس از آن انتقال باید آغاز شود. این تنظیم می‌تواند همراه با ویژگی advClick استفاده شود. اگر این ویژگی مشخص نشده باشد فرض می‌شود خودکار پیشروی رخ نمی‌دهد. خواندن/نوشتن long.

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | long |  |

### getSpeed() {#getSpeed--}
```
public final int getSpeed()
```

سرعت انتقالی که هنگام انتقال از اسلاید جاری به اسلاید بعدی استفاده می‌شود را مشخص می‌کند. خواندن/نوشتن [TransitionSpeed](../../com.aspose.slides/transitionspeed).

**بازگشت:**
int

### setSpeed(int value) {#setSpeed-int-}
```
public final void setSpeed(int value)
```

سرعت انتقالی که هنگام انتقال از اسلاید جاری به اسلاید بعدی استفاده می‌شود را مشخص می‌کند. خواندن/نوشتن [TransitionSpeed](../../com.aspose.slides/transitionspeed).

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | int |  |

### getValue() {#getValue--}
```
public final ITransitionValueBase getValue()
```

مقدار انتقال اسلاید نمایش. فقط‌خواندنی [ITransitionValueBase](../../com.aspose.slides/itransitionvaluebase).

**بازگشت:**
[ITransitionValueBase](../../com.aspose.slides/itransitionvaluebase)

### getType() {#getType--}
```
public final int getType()
```

نوع انتقال. خواندن/نوشتن [TransitionType](../../com.aspose.slides/transitiontype).

**بازگشت:**
int

### setType(int value) {#setType-int-}
```
public final void setType(int value)
```

نوع انتقال. خواندن/نوشتن [TransitionType](../../com.aspose.slides/transitiontype).

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | int |  |

### getSoundIsBuiltIn() {#getSoundIsBuiltIn--}
```
public final boolean getSoundIsBuiltIn()
```

مشخص می‌کند آیا این صدا یک صدا داخلی است یا خیر. اگر این ویژگی برابر true باشد، برنامه‌ساز باید نام ویژگی name را که برای این صدا در فهرست صداهای داخلی وجود دارد چک کند و در صورت نیاز نام یا رابط کاربری سفارشی ارائه دهد. خواندن/نوشتن boolean.

**بازگشت:**
boolean

### setSoundIsBuiltIn(boolean value) {#setSoundIsBuiltIn-boolean-}
```
public final void setSoundIsBuiltIn(boolean value)
```

مشخص می‌کند آیا این صدا یک صدا داخلی است یا خیر. اگر این ویژگی برابر true باشد، برنامه‌ساز باید نام ویژگی name را که برای این صدا در فهرست صداهای داخلی وجود دارد چک کند و در صورت نیاز نام یا رابط کاربری سفارشی ارائه دهد. خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | boolean |  |

### getSoundName() {#getSoundName--}
```
public final String getSoundName()
```

نام قابل خواندن توسط انسان برای صدای انتقال را مشخص می‌کند. ویژگی Sound (\#getSound.getSound/\#setSound(IAudio).setSound(IAudio)) باید برای دریافت یا تنظیم نام صدا اختصاص داده شود. خواندن/نوشتن String.

**بازگشت:**
java.lang.String

### setSoundName(String value) {#setSoundName-java.lang.String-}
```
public final void setSoundName(String value)
```

نام قابل خواندن توسط انسان برای صدای انتقال را مشخص می‌کند. ویژگی Sound (\#getSound.getSound/\#setSound(IAudio).setSound(IAudio)) باید برای دریافت یا تنظیم نام صدا اختصاص داده شود. خواندن/نوشتن String.

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | java.lang.String |  |

### getDuration() {#getDuration--}
```
public final int getDuration()
```

مدت اثر انتقال اسلاید را بر میلی‌ثانیه بر می‌گرداند یا تنظیم می‌کند. خواندن/نوشتن int.

--------------------

مطابق با ویژگی p14:dur عنصر p:transition در طرح‌نامه PresentationML. اگر تنظیم نشود، مدت زمان به‌صورت خودکار بر پایه ویژگی \#getSpeed.getSpeed/\#setSpeed(int).setSpeed(int) و نوع انتقال تعیین می‌شود.

**بازگشت:**
int

### setDuration(int value) {#setDuration-int-}
```
public final void setDuration(int value)
```

مدت اثر انتقال اسلاید را بر میلی‌ثانیه بر می‌گرداند یا تنظیم می‌کند. خواندن/نوشتن int.

--------------------

مطابق با ویژگی p14:dur عنصر p:transition در طرح‌نامه PresentationML. اگر تنظیم نشود، مدت زمان به‌صورت خودکار بر پایه ویژگی \#getSpeed.getSpeed/\#setSpeed(int).setSpeed(int) و نوع انتقال تعیین می‌شود.

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| value | int |  |

### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```

تعیین می‌کند آیا دو نمونه SlideShowTransition برابر هستند یا خیر. خواندن/نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیحات |
| --- | --- | --- |
| obj | java.lang.Object | شیء SlideShowTransition برای مقایسه با SlideShowTransition فعلی. |

**بازگشت:**
boolean -  **true**  اگر SlideShowTransition مشخص‌شده برابر با SlideShowTransition فعلی باشد؛ در غیر این صورت  **false** .

### hashCode() {#hashCode--}
```
public int hashCode()
```

به‌عنوان تابع هش برای یک نوع خاص عمل می‌کند که برای الگوریتم‌های هش و ساختارهای داده‌ای مانند جدول هش مناسب است.

**بازگشت:**
int - 23454

--------------------

بازنویسی شده برای خوشحال کردن کامپایلر. همیشه مقدار ثابت بر می‌گرداند چون شیء قابل تغییر است.