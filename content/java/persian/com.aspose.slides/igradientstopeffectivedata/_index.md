---
title: IGradientStopEffectiveData
second_title: Aspose.Slides for Java API Reference
description: immutable object which represents a gradient stop.
type: docs
url: /fa/com.aspose.slides/igradientstopeffectivedata/
---```
public interface IGradientStopEffectiveData
```

شیء غیربدیل که یک توقف‌گرادیانت را نشان می‌دهد.
## متدها

| متد | توضیح |
| --- | --- |
| [getPosition()](#getPosition--) | موقعیت (0..1) یک توقف‌گرادیانت را بازمی‌گرداند. |
| [getColor()](#getColor--) | رنگ یک توقف‌گرادیانت را بازمی‌گرداند. |
### getPosition() {#getPosition--}
```
public abstract float getPosition()
```


موقعیت (0..1) یک توقف‌گرادیانت را بازمی‌گرداند. فقط-خواندنی float.

**بازمی‌گرداند:**
float
### getColor() {#getColor--}
```
public abstract Color getColor()
```


رنگ یک توقف‌گرادیانت را بازمی‌گرداند. فقط-خواندنی java.awt.Color.

**بازمی‌گرداند:**
java.awt.Color