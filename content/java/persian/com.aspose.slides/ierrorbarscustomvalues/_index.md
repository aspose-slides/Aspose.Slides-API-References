---
title: IErrorBarsCustomValues
second_title: Aspose.Slides for Java API Reference
description: Specifies the errors bar values.
type: docs
url: /fa/com.aspose.slides/ierrorbarscustomvalues/
---```
public interface IErrorBarsCustomValues
```

مقادیر نوارهای خطا را مشخص می‌کند. فقط زمانی استفاده می‌شود که نوع مقدار نوارهای خطا Custom باشد.
## متدها

| متد | توضیح |
| --- | --- |
| [getXMinus()](#getXMinus--) | مقدار نوار خطا را در جهت منفی مشخص می‌کند. |
| [getYMinus()](#getYMinus--) | مقدار نوار خطا را در جهت منفی مشخص می‌کند. |
| [getXPlus()](#getXPlus--) | مقدار نوار خطا را در جهت مثبت مشخص می‌کند. |
| [getYPlus()](#getYPlus--) | مقدار نوار خطا را در جهت مثبت مشخص می‌کند. |
### getXMinus() {#getXMinus--}
```
public abstract IDoubleChartValue getXMinus()
```

مقدار نوار خطا را در جهت منفی مشخص می‌کند. در صورتی که نوع مقدار نوارهای خطا Custom باشد و ErrorBarsXFormat مجاز باشد، در دسترس است. در هر مورد دیگر این ویژگی مقدار null را برمی‌گرداند. فقط-خواندنی [IDoubleChartValue](../../com.aspose.slides/idoublechartvalue).

**باز می‌گرداند:**
[IDoubleChartValue](../../com.aspose.slides/idoublechartvalue)
### getYMinus() {#getYMinus--}
```
public abstract IDoubleChartValue getYMinus()
```

مقدار نوار خطا را در جهت منفی مشخص می‌کند. در صورتی که نوع مقدار نوارهای خطا Custom باشد و ErrorBarsYFormat مجاز باشد، در دسترس است. در هر مورد دیگر این ویژگی مقدار null را برمی‌گرداند. فقط-خواندنی [IDoubleChartValue](../../com.aspose.slides/idoublechartvalue).

**باز می‌گرداند:**
[IDoubleChartValue](../../com.aspose.slides/idoublechartvalue)
### getXPlus() {#getXPlus--}
```
public abstract IDoubleChartValue getXPlus()
```

مقدار نوار خطا را در جهت مثبت مشخص می‌کند. در صورتی که نوع مقدار نوارهای خطا Custom باشد و ErrorBarsXFormat مجاز باشد، در دسترس است. در هر مورد دیگر این ویژگی مقدار null را برمی‌گرداند. فقط-خواندنی [IDoubleChartValue](../../com.aspose.slides/idoublechartvalue).

**باز می‌گرداند:**
[IDoubleChartValue](../../com.aspose.slides/idoublechartvalue)
### getYPlus() {#getYPlus--}
```
public abstract IDoubleChartValue getYPlus()
```

مقدار نوار خطا را در جهت مثبت مشخص می‌کند. در صورتی که نوع مقدار نوارهای خطا Custom باشد و ErrorBarsYFormat مجاز باشد، در دسترس است. در هر مورد دیگر این ویژگی مقدار null را برمی‌گرداند. فقط-خواندنی [IDoubleChartValue](../../com.aspose.slides/idoublechartvalue).

**باز می‌گرداند:**
[IDoubleChartValue](../../com.aspose.slides/idoublechartvalue)