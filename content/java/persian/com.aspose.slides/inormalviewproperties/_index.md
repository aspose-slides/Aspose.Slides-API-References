---
title: INormalViewProperties
second_title: Aspose.Slides for Java API Reference
description: Represents normal view properties.
type: docs
url: /fa/com.aspose.slides/inormalviewproperties/
---```
public interface INormalViewProperties
```

نمایش عادی را نشان می‌دهد. نمای عادی شامل سه ناحیه محتوا است: خود اسلاید، یک ناحیه محتوا کناری، و یک ناحیه محتوا پایین.
## متدها

| متد | توضیح |
| --- | --- |
| [getShowOutlineIcons()](#getShowOutlineIcons--) | مشخص می‌کند که آیا برنامه باید در صورت نمایش محتوای طرح کلی در هر یک از ناحیه‌های محتوا در حالت نمای عادی، آیکون‌ها را نشان دهد یا خیر. |
| [setShowOutlineIcons(boolean value)](#setShowOutlineIcons-boolean-) | مشخص می‌کند که آیا برنامه باید در صورت نمایش محتوای طرح کلی در هر یک از ناحیه‌های محتوا در حالت نمای عادی، آیکون‌ها را نشان دهد یا خیر. |
| [getSnapVerticalSplitter()](#getSnapVerticalSplitter--) | مشخص می‌کند که آیا تقسیم‌کننده عمودی باید هنگامی که ناحیه کناری به اندازه کافی کوچک باشد، به حالت به‌حداقل رسیده بچسبد. |
| [setSnapVerticalSplitter(boolean value)](#setSnapVerticalSplitter-boolean-) | مشخص می‌کند که آیا تقسیم‌کننده عمودی باید هنگامی که ناحیه کناری به اندازه کافی کوچک باشد، به حالت به‌حداقل رسیده بچسبد. |
| [getVerticalBarState()](#getVerticalBarState--) | وضعیتی که نوار تقسیم‌کننده عمودی باید در آن نمایش داده شود را مشخص می‌کند. |
| [setVerticalBarState(int value)](#setVerticalBarState-int-) | وضعیتی که نوار تقسیم‌کننده عمودی باید در آن نمایش داده شود را مشخص می‌کند. |
| [getHorizontalBarState()](#getHorizontalBarState--) | وضعیتی که نوار تقسیم‌کننده افقی باید در آن نمایش داده شود را مشخص می‌کند. |
| [setHorizontalBarState(int value)](#setHorizontalBarState-int-) | وضعیتی که نوار تقسیم‌کننده افقی باید در آن نمایش داده شود را مشخص می‌کند. |
| [getPreferSingleView()](#getPreferSingleView--) | مشخص می‌کند که آیا کاربر ترجیح می‌دهد یک ناحیه محتوا تک-پنجره‌ای تمام‌صفحه را به جای نمای عادی استاندارد با سه ناحیه محتوا ببیند یا خیر. |
| [setPreferSingleView(boolean value)](#setPreferSingleView-boolean-) | مشخص می‌کند که آیا کاربر ترجیح می‌دهد یک ناحیه محتوا تک-پنجره‌ای تمام‌صفحه را به جای نمای عادی استاندارد با سه ناحیه محتوا ببیند یا خیر. |
| [getRestoredLeft()](#getRestoredLeft--) | این عنصر اندازه‌گیری ناحیه محتوا کناری نمای عادی را زمانی که ناحیه دارای اندازه بازگردانی متغیر (نه به‌حداقل رسیده و نه بیشینه) باشد، مشخص می‌کند. |
| [getRestoredTop()](#getRestoredTop--) | این عنصر اندازه‌گیری ناحیه بالای اسلاید نمای عادی را زمانی که ناحیه دارای اندازه بازگردانی متغیر (نه به‌حداقل رسیده و نه بیشینه) باشد، مشخص می‌کند. |
### getShowOutlineIcons() {#getShowOutlineIcons--}
```
public abstract boolean getShowOutlineIcons()
```

مشخص می‌کند که آیا برنامه باید در صورت نمایش محتوای طرح کلی در هر یک از ناحیه‌های محتوا در حالت نمای عادی، آیکون‌ها را نشان دهد یا خیر. قابل خواندن/نوشتن بولی.

**بازگشت:**  
boolean
### setShowOutlineIcons(boolean value) {#setShowOutlineIcons-boolean-}
```
public abstract void setShowOutlineIcons(boolean value)
```

مشخص می‌کند که آیا برنامه باید در صورت نمایش محتوای طرح کلی در هر یک از ناحیه‌های محتوا در حالت نمای عادی، آیکون‌ها را نشان دهد یا خیر. قابل خواندن/نوشتن بولی.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |
### getSnapVerticalSplitter() {#getSnapVerticalSplitter--}
```
public abstract boolean getSnapVerticalSplitter()
```

مشخص می‌کند که آیا تقسیم‌کننده عمودی باید هنگامی که ناحیه کناری به اندازه کافی کوچک باشد، به حالت به‌حداقل رسیده بچسبد. قابل خواندن/نوشتن بولی.

**بازگشت:**  
boolean
### setSnapVerticalSplitter(boolean value) {#setSnapVerticalSplitter-boolean-}
```
public abstract void setSnapVerticalSplitter(boolean value)
```

مشخص می‌کند که آیا تقسیم‌کننده عمودی باید هنگامی که ناحیه کناری به اندازه کافی کوچک باشد، به حالت به‌حداقل رسیده بچسبد. قابل خواندن/نوشتن بولی.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |
### getVerticalBarState() {#getVerticalBarState--}
```
public abstract int getVerticalBarState()
```

وضعیتی که نوار تقسیم‌کننده عمودی باید در آن نمایش داده شود را مشخص می‌کند. یک نوار تقسیم‌کننده عمودی اسلاید را از ناحیه محتوا کناری جدا می‌کند.

**بازگشت:**  
int
### setVerticalBarState(int value) {#setVerticalBarState-int-}
```
public abstract void setVerticalBarState(int value)
```

وضعیتی که نوار تقسیم‌کننده عمودی باید در آن نمایش داده شود را مشخص می‌کند. یک نوار تقسیم‌کننده عمودی اسلاید را از ناحیه محتوا کناری جدا می‌کند.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |
### getHorizontalBarState() {#getHorizontalBarState--}
```
public abstract int getHorizontalBarState()
```

وضعیتی که نوار تقسیم‌کننده افقی باید در آن نمایش داده شود را مشخص می‌کند. یک نوار تقسیم‌کننده افقی اسلاید را از ناحیه محتوا زیر اسلاید جدا می‌کند.

**بازگشت:**  
int
### setHorizontalBarState(int value) {#setHorizontalBarState-int-}
```
public abstract void setHorizontalBarState(int value)
```

وضعیتی که نوار تقسیم‌کننده افقی باید در آن نمایش داده شود را مشخص می‌کند. یک نوار تقسیم‌کننده افقی اسلاید را از ناحیه محتوا زیر اسلاید جدا می‌کند.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |
### getPreferSingleView() {#getPreferSingleView--}
```
public abstract boolean getPreferSingleView()
```

مشخص می‌کند که آیا کاربر ترجیح می‌دهد یک ناحیه محتوا تک-پنجره‌ای تمام‌صفحه را به جای نمای عادی استاندارد با سه ناحیه محتوا ببیند یا خیر. اگر فعال باشد، برنامه ممکن است یکی از ناحیه‌های محتوا را در کل پنجره نمایش دهد. قابل خواندن/نوشتن بولی.

**بازگشت:**  
boolean
### setPreferSingleView(boolean value) {#setPreferSingleView-boolean-}
```
public abstract void setPreferSingleView(boolean value)
```

مشخص می‌کند که آیا کاربر ترجیح می‌دهد یک ناحیه محتوا تک-پنجره‌ای تمام‌صفحه را به جای نمای عادی استاندارد با سه ناحیه محتوا ببیند یا خیر. اگر فعال باشد، برنامه ممکن است یکی از ناحیه‌های محتوا را در کل پنجره نمایش دهد. قابل خواندن/نوشتن بولی.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |
### getRestoredLeft() {#getRestoredLeft--}
```
public abstract INormalViewRestoredProperties getRestoredLeft()
```

این عنصر اندازه‌گیری ناحیه محتوا کناری نمای عادی را زمانی که ناحیه دارای اندازه بازگردانی متغیر (نه به‌حداقل رسیده و نه بیشینه) باشد، مشخص می‌کند. فقط خواندنی [INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties).

**بازگشت:**  
[INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)
### getRestoredTop() {#getRestoredTop--}
```
public abstract INormalViewRestoredProperties getRestoredTop()
```

این عنصر اندازه‌گیری ناحیه بالای اسلاید نمای عادی را زمانی که ناحیه دارای اندازه بازگردانی متغیر (نه به‌حداقل رسیده و نه بیشینه) باشد، مشخص می‌کند. فقط خواندنی [INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties).

**بازگشت:**  
[INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)