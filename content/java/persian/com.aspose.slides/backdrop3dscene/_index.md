---
title: Backdrop3DScene
second_title: مرجع API Aspose.Slides برای جاوا
description: یک صفحه تعریف می‌کند که اثراتی مانند درخشش و سایه در رابطه با شکلی که به آن اعمال می‌شوند، اعمال می‌گردند.
type: docs
url: /fa/com.aspose.slides/backdrop3dscene/
---
**ارث‌برداری:**
java.lang.Object, [com.aspose.slides.PVIObject](../../com.aspose.slides/pviobject)

**تمام واسط‌های پیاده‌سازی شده:**
[com.aspose.slides.IBackdrop3DScene](../../com.aspose.slides/ibackdrop3dscene)
```
public final class Backdrop3DScene extends PVIObject implements IBackdrop3DScene
```

یک صفحه تعریف می‌کند که اثراتی مانند درخشش و سایه در ارتباط با شکلی که به آن اعمال می‌شوند، اعمال می‌گردند.
## متدها

| متد | توضیح |
| --- | --- |
| [getVersion()](#getVersion--) |  |
| [getNormalVector()](#getNormalVector--) | Returns or sets a normal vector. |
| [setNormalVector(float[] value)](#setNormalVector-float---) | Returns or sets a normal vector. |
| [getAnchorPoint()](#getAnchorPoint--) | Returns or sets a point in 3D space. |
| [setAnchorPoint(float[] value)](#setAnchorPoint-float---) | Returns or sets a point in 3D space. |
| [getUpVector()](#getUpVector--) | Returns or sets a vector representing up. |
| [setUpVector(float[] value)](#setUpVector-float---) | Returns or sets a vector representing up. |
### getVersion() {#getVersion--}
```
public long getVersion()
```

نسخه. فقط‌خواندنی long.

**بازمی‌گردد:**
long
### getNormalVector() {#getNormalVector--}
```
public final float[] getNormalVector()
```

باز می‌گردد یا تنظیم می‌کند یک بردار نرمال. به‌طور دقیق‌تر، این ویژگی یک بردار نرمال نسبت به سطح صفحه پس‌زمینه را تعریف می‌کند. بردار توسط آرایه‌ای از ۳ مقدار float که مختصات X، Y و Z را تعیین می‌کنند، نمایان می‌شود. خواندنی/نوشتنی float[].

**بازمی‌گردد:**
float[]
### setNormalVector(float[] value) {#setNormalVector-float---}
```
public final void setNormalVector(float[] value)
```

باز می‌گردد یا تنظیم می‌کند یک بردار نرمال. به‌طور دقیق‌تر، این ویژگی یک بردار نرمال نسبت به سطح صفحه پس‌زمینه را تعریف می‌کند. بردار توسط آرایه‌ای از ۳ مقدار float که مختصات X، Y و Z را تعیین می‌کنند، نمایان می‌شود. خواندنی/نوشتنی float[].

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float[] |  |
### getAnchorPoint() {#getAnchorPoint--}
```
public final float[] getAnchorPoint()
```

باز می‌گردد یا تنظیم می‌کند یک نقطه در فضای ۳بعدی. این نقطه نقطه‌ای در فضا است که صفحه پس‌زمینه را ثابت می‌کند. نقطه ۳‌بعدی توسط آرایه‌ای از ۳ مقدار float که مختصات X، Y و Z را تعیین می‌کنند، نمایان می‌شود. خواندنی/نوشتنی float[].

**بازمی‌گردد:**
float[]
### setAnchorPoint(float[] value) {#setAnchorPoint-float---}
```
public final void setAnchorPoint(float[] value)
```

باز می‌گردد یا تنظیم می‌کند یک نقطه در فضای ۳بعدی. این نقطه نقطه‌ای در فضا است که صفحه پس‌زمینه را ثابت می‌کند. نقطه ۳‌بعدی توسط آرایه‌ای از ۳ مقدار float که مختصات X، Y و Z را تعیین می‌کنند، نمایان می‌شود. خواندنی/نوشتنی float[].

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float[] |  |
### getUpVector() {#getUpVector--}
```
public final float[] getUpVector()
```

باز می‌گردد یا تنظیم می‌کند یک بردار نشان‌دهنده جهت بالا. به‌طور دقیق‌تر، این ویژگی یک بردار نشان‌دهنده جهت بالا نسبت به سطح صفحه پس‌زمینه را تعریف می‌کند. بردار توسط آرایه‌ای از ۳ مقدار float که مختصات X، Y و Z را تعیین می‌کنند، نمایان می‌شود. خواندنی/نوشتنی float[].

**بازمی‌گردد:**
float[]
### setUpVector(float[] value) {#setUpVector-float---}
```
public final void setUpVector(float[] value)
```

باز می‌گردد یا تنظیم می‌کند یک بردار نشان‌دهنده جهت بالا. به‌طور دقیق‌تر، این ویژگی یک بردار نشان‌دهنده جهت بالا نسبت به سطح صفحه پس‌زمینه را تعریف می‌کند. بردار توسط آرایه‌ای از ۳ مقدار float که مختصات X، Y و Z را تعیین می‌کنند، نمایان می‌شود. خواندنی/نوشتنی float[].

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float[] |  |