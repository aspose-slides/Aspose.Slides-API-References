---
title: IPictureFillFormatEffectiveData
second_title: مرجع API Aspose.Slides برای Java
description: شیء غیرقابل تغییر که خصوصیات پر کردن تصویر را شامل می‌شود.
type: docs
url: /fa/com.aspose.slides/ipicturefillformateffectivedata/
---
**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IFillParamSource](../../com.aspose.slides/ifillparamsource)
```
public interface IPictureFillFormatEffectiveData extends IFillParamSource
```

شیء غیرقابل تغییر که خصوصیات پر کردن تصویر را در بر دارد.

--------------------

این رابط به عنوان بخشی از [IFillFormatEffectiveData](../../com.aspose.slides/ifillformateffectivedata) استفاده می‌شود.
## متدها

| Method | Description |
| --- | --- |
| [getDpi()](#getDpi--) | dpi‌ای که برای پر کردن تصویر استفاده می‌شود را برمی‌گرداند. |
| [getPictureFillMode()](#getPictureFillMode--) | حالت پر کردن تصویر را برمی‌گرداند. |
| [getPicture()](#getPicture--) | تصویر را برمی‌گرداند. |
| [getCropLeft()](#getCropLeft--) | درصدی از عرض واقعی تصویر که از سمت چپ تصویر بریده شده است را برمی‌گرداند. |
| [getCropTop()](#getCropTop--) | درصدی از ارتفاع واقعی تصویر که از بالای تصویر بریده شده است را برمی‌گرداند. |
| [getCropRight()](#getCropRight--) | درصدی از عرض واقعی تصویر که از سمت راست تصویر بریده شده است را برمی‌گرداند. |
| [getCropBottom()](#getCropBottom--) | درصدی از ارتفاع واقعی تصویر که از پایین تصویر بریده شده است را برمی‌گرداند. |
### getDpi() {#getDpi--}
```
public abstract int getDpi()
```

dpi‌ای که برای پر کردن تصویر استفاده می‌شود را برمی‌گرداند. فقط-خواندنی int.

**باز می‌گرداند:**
int
### getPictureFillMode() {#getPictureFillMode--}
```
public abstract int getPictureFillMode()
```

حالت پر کردن تصویر را برمی‌گرداند. فقط-خواندنی [PictureFillMode](../../com.aspose.slides/picturefillmode).

**باز می‌گرداند:**
int
### getPicture() {#getPicture--}
```
public abstract IPictureEffectiveData getPicture()
```

تصویر را برمی‌گرداند. فقط-خواندنی [IPictureEffectiveData](../../com.aspose.slides/ipictureeffectivedata).

**باز می‌گرداند:**
[IPictureEffectiveData](../../com.aspose.slides/ipictureeffectivedata)
### getCropLeft() {#getCropLeft--}
```
public abstract float getCropLeft()
```

درصدی از عرض واقعی تصویر که از سمت چپ تصویر بریده شده است را برمی‌گرداند. فقط-خواندنی float.

**باز می‌گرداند:**
float
### getCropTop() {#getCropTop--}
```
public abstract float getCropTop()
```

درصدی از ارتفاع واقعی تصویر که از بالای تصویر بریده شده است را برمی‌گرداند. فقط-خواندنی float.

**باز می‌گرداند:**
float
### getCropRight() {#getCropRight--}
```
public abstract float getCropRight()
```

درصدی از عرض واقعی تصویر که از سمت راست تصویر بریده شده است را برمی‌گرداند. فقط-خواندنی float.

**باز می‌گرداند:**
float
### getCropBottom() {#getCropBottom--}
```
public abstract float getCropBottom()
```

درصدی از ارتفاع واقعی تصویر که از پایین تصویر بریده شده است را برمی‌گرداند. فقط-خواندنی float.

**باز می‌گرداند:**
float