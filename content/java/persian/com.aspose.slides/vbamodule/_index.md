---
title: VbaModule
second_title: مرجع API Aspose.Slides برای جاوا
description: ماژولی را نشان می‌دهد که در پروژه VBA وجود دارد.
type: docs
url: /fa/com.aspose.slides/vbamodule/
---
**وراثت:**
java.lang.Object

**تمام اینترفیس‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IVbaModule](../../com.aspose.slides/ivbamodule)
```
public final class VbaModule implements IVbaModule
```

ماژولی را نشان می‌دهد که در پروژه VBA وجود دارد.
## متدها

| متد | توضیح |
| --- | --- |
| [getName()](#getName--) | نام ماژول را دریافت می‌کند. |
| [getSourceCode()](#getSourceCode--) | کد منبع ماژول را بر می‌گرداند یا تنظیم می‌کند. |
| [setSourceCode(String value)](#setSourceCode-java.lang.String-) | کد منبع ماژول را بر می‌گرداند یا تنظیم می‌کند. |
### getName() {#getName--}
```
public final String getName()
```

نام ماژول را دریافت می‌کند. فقط‌خواندنی String.

**بازگشت:**
java.lang.String
### getSourceCode() {#getSourceCode--}
```
public final String getSourceCode()
```

کد منبع ماژول را بر می‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن String.

**بازگشت:**
java.lang.String
### setSourceCode(String value) {#setSourceCode-java.lang.String-}
```
public final void setSourceCode(String value)
```

کد منبع ماژول را بر می‌گرداند یا تنظیم می‌کند. قابل خواندن/نوشتن String.

**پارامترها:**
| پارامتر | نوع | شرح |
| --- | --- | --- |
| value | java.lang.String |  |