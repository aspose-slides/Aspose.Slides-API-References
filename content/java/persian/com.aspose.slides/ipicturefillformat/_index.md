---
title: IPictureFillFormat
second_title: Aspose.Slides برای Java API مرجع
description: یک سبک پر کردن تصویر را نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/ipicturefillformat/
---
**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IFillParamSource](../../com.aspose.slides/ifillparamsource)
```
public interface IPictureFillFormat extends IFillParamSource
```

نمایانگر یک سبک پر کردن تصویر است.
## متدها

| متد | توضیح |
| --- | --- |
| [getDpi()](#getDpi--) | بازگرداندن یا تنظیم dpi که برای پر کردن یک تصویر استفاده می‌شود. |
| [setDpi(int value)](#setDpi-int-) | بازگرداندن یا تنظیم dpi که برای پر کردن یک تصویر استفاده می‌شود. |
| [getPictureFillMode()](#getPictureFillMode--) | بازگرداندن یا تنظیم حالت پر کردن تصویر. |
| [setPictureFillMode(int value)](#setPictureFillMode-int-) | بازگرداندن یا تنظیم حالت پر کردن تصویر. |
| [getPicture()](#getPicture--) | بازگرداندن تصویر. |
| [getCropLeft()](#getCropLeft--) | بازگرداندن یا تنظیم درصد عرض واقعی تصویر که از سمت چپ تصویر برش خورده است. |
| [setCropLeft(float value)](#setCropLeft-float-) | بازگرداندن یا تنظیم درصد عرض واقعی تصویر که از سمت چپ تصویر برش خورده است. |
| [getCropTop()](#getCropTop--) | بازگرداندن یا تنظیم درصد ارتفاع واقعی تصویر که از سمت بالا تصویر برش خورده است. |
| [setCropTop(float value)](#setCropTop-float-) | بازگرداندن یا تنظیم درصد ارتفاع واقعی تصویر که از سمت بالا تصویر برش خورده است. |
| [getCropRight()](#getCropRight--) | بازگرداندن یا تنظیم درصد عرض واقعی تصویر که از سمت راست تصویر برش خورده است. |
| [setCropRight(float value)](#setCropRight-float-) | بازگرداندن یا تنظیم درصد عرض واقعی تصویر که از سمت راست تصویر برش خورده است. |
| [getCropBottom()](#getCropBottom--) | بازگرداندن یا تنظیم درصد ارتفاع واقعی تصویر که از سمت پایین تصویر برش خورده است. |
| [setCropBottom(float value)](#setCropBottom-float-) | بازگرداندن یا تنظیم درصد ارتفاع واقعی تصویر که از سمت پایین تصویر برش خورده است. |
| [getStretchOffsetLeft()](#getStretchOffsetLeft--) | بازگرداندن یا تنظیم لبهٔ چپ مستطیل پرکننده که توسط افست درصدی از لبهٔ چپ جعبهٔ محدود‌کنندهٔ شکل تعریف می‌شود. یک درصد مثبت داخلی‌تر و یک درصد منفی بیرونی‌تر را مشخص می‌کند. |
| [setStretchOffsetLeft(float value)](#setStretchOffsetLeft-float-) | بازگرداندن یا تنظیم لبهٔ چپ مستطیل پرکننده که توسط افست درصدی از لبهٔ چپ جعبهٔ محدود‌کنندهٔ شکل تعریف می‌شود. یک درصد مثبت داخلی‌تر و یک درصد منفی بیرونی‌تر را مشخص می‌کند. |
| [getStretchOffsetTop()](#getStretchOffsetTop--) | بازگرداندن یا تنظیم لبهٔ بالای مستطیل پرکننده که توسط افست درصدی از لبهٔ بالای جعبهٔ محدود‌کنندهٔ شکل تعریف می‌شود. یک درصد مثبت داخلی‌تر و یک درصد منفی بیرونی‌تر را مشخص می‌کند. |
| [setStretchOffsetTop(float value)](#setStretchOffsetTop-float-) | بازگرداندن یا تنظیم لبهٔ بالای مستطیل پرکننده که توسط افست درصدی از لبهٔ بالای جعبهٔ محدود‌کنندهٔ شکل تعریف می‌شود. یک درصد مثبت داخلی‌تر و یک درصد منفی بیرونی‌تر را مشخص می‌کند. |
| [getStretchOffsetRight()](#getStretchOffsetRight--) | بازگرداندن یا تنظیم لبهٔ راست مستطیل پرکننده که توسط افست درصدی از لبهٔ راست جعبهٔ محدود‌کنندهٔ شکل تعریف می‌شود. یک درصد مثبت داخلی‌تر و یک درصد منفی بیرونی‌تر را مشخص می‌کند. |
| [setStretchOffsetRight(float value)](#setStretchOffsetRight-float-) | بازگرداندن یا تنظیم لبهٔ راست مستطیل پرکننده که توسط افست درصدی از لبهٔ راست جعبهٔ محدود‌کنندهٔ شکل تعریف می‌شود. یک درصد مثبت داخلی‌تر و یک درصد منفی بیرونی‌تر را مشخص می‌کند. |
| [getStretchOffsetBottom()](#getStretchOffsetBottom--) | بازگرداندن یا تنظیم لبهٔ پایین مستطیل پرکننده که توسط افست درصدی از لبهٔ پایین جعبهٔ محدود‌کنندهٔ شکل تعریف می‌شود. یک درصد مثبت داخلی‌تر و یک درصد منفی بیرونی‌تر را مشخص می‌کند. |
| [setStretchOffsetBottom(float value)](#setStretchOffsetBottom-float-) | بازگرداندن یا تنظیم لبهٔ پایین مستطیل پرکننده که توسط افست درصدی از لبهٔ پایین جعبهٔ محدود‌کنندهٔ شکل تعریف می‌شود. یک درصد مثبت داخلی‌تر و یک درصد منفی بیرونی‌تر را مشخص می‌کند. |
| [deletePictureCroppedAreas()](#deletePictureCroppedAreas--) | حذف نواحی برش‌خورده تصویر پرکننده. |
| [compressImage(boolean deleteCroppedAreasOfImage, int resolution)](#compressImage-boolean-int-) | فشرده‌سازی تصویر با کاهش اندازهٔ آن بر اساس اندازهٔ شکل و وضوح مشخص‌شده. |
| [compressImage(boolean deleteCroppedAreasOfImage, float resolution)](#compressImage-boolean-float-) | فشرده‌سازی تصویر با کاهش اندازهٔ آن بر اساس اندازهٔ شکل و وضوح مشخص‌شده. |
| [getTileOffsetX()](#getTileOffsetX--) | بازگرداندن یا تنظیم افست افقی بافت از مبدأ شکل به نقاط. |
| [setTileOffsetX(float value)](#setTileOffsetX-float-) | بازگرداندن یا تنظیم افست افقی بافت از مبدأ شکل به نقاط. |
| [getTileOffsetY()](#getTileOffsetY--) | بازگرداندن یا تنظیم افست عمودی بافت از مبدأ شکل به نقاط. |
| [setTileOffsetY(float value)](#setTileOffsetY-float-) | بازگرداندن یا تنظیم افست عمودی بافت از مبدأ شکل به نقاط. |
| [getTileScaleX()](#getTileScaleX--) | بازگرداندن یا تنظیم مقیاس افقی برای پر کردن بافت به درصد. |
| [setTileScaleX(float value)](#setTileScaleX-float-) | بازگرداندن یا تنظیم مقیاس افقی برای پر کردن بافت به درصد. |
| [getTileScaleY()](#getTileScaleY--) | بازگرداندن یا تنظیم مقیاس عمودی برای پر کردن بافت به درصد. |
| [setTileScaleY(float value)](#setTileScaleY-float-) | بازگرداندن یا تنظیم مقیاس عمودی برای پر کردن بافت به درصد. |
| [getTileAlignment()](#getTileAlignment--) | بازگرداندن یا تنظیم نحوهٔ تراز بافت داخل شکل. |
| [setTileAlignment(byte value)](#setTileAlignment-byte-) | بازگرداندن یا تنظیم نحوهٔ تراز بافت داخل شکل. |
| [getTileFlip()](#getTileFlip--) | چرخاندن کاشی بافت حول محور افقی، عمودی یا هر دو. |
| [setTileFlip(int value)](#setTileFlip-int-) | چرخاندن کاشی بافت حول محور افقی، عمودی یا هر دو. |

### getDpi() {#getDpi--}
```
public abstract int getDpi()
```

بازگرداندن یا تنظیم dpi که برای پر کردن یک تصویر استفاده می‌شود. خواندنی/نوشتنی int.

**بازگشت:**
int

### setDpi(int value) {#setDpi-int-}
```
public abstract void setDpi(int value)
```

بازگرداندن یا تنظیم dpi که برای پر کردن یک تصویر استفاده می‌شود. خواندنی/نوشتنی int.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getPictureFillMode() {#getPictureFillMode--}
```
public abstract int getPictureFillMode()
```

بازگرداندن یا تنظیم حالت پر کردن تصویر. خواندنی/نوشتنی [PictureFillMode](../../com.aspose.slides/picturefillmode).

**بازگشت:**
int

### setPictureFillMode(int value) {#setPictureFillMode-int-}
```
public abstract void setPictureFillMode(int value)
```

بازگرداندن یا تنظیم حالت پر کردن تصویر. خواندنی/نوشتنی [PictureFillMode](../../com.aspose.slides/picturefillmode).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getPicture() {#getPicture--}
```
public abstract ISlidesPicture getPicture()
```

بازگرداندن تصویر. فقط خواندنی [ISlidesPicture](../../com.aspose.slides/islidespicture).

**بازگشت:**
[ISlidesPicture](../../com.aspose.slides/islidespicture)

### getCropLeft() {#getCropLeft--}
```
public abstract float getCropLeft()
```

بازگرداندن یا تنظیم درصد عرض واقعی تصویر که از سمت چپ تصویر برش خورده است. خواندنی/نوشتنی float.

**بازگشت:**
float

### setCropLeft(float value) {#setCropLeft-float-}
```
public abstract void setCropLeft(float value)
```

بازگرداندن یا تنظیم درصد عرض واقعی تصویر که از سمت چپ تصویر برش خورده است. خواندنی/نوشتنی float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getCropTop() {#getCropTop--}
```
public abstract float getCropTop()
```

بازگرداندن یا تنظیم درصد ارتفاع واقعی تصویر که از سمت بالا تصویر برش خورده است. خواندنی/نوشتنی float.

**بازگشت:**
float

### setCropTop(float value) {#setCropTop-float-}
```
public abstract void setCropTop(float value)
```

بازگرداندن یا تنظیم درصد ارتفاع واقعی تصویر که از سمت بالا تصویر برش خورده است. خواندنی/نوشتنی float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getCropRight() {#getCropRight--}
```
public abstract float getCropRight()
```

بازگرداندن یا تنظیم درصد عرض واقعی تصویر که از سمت راست تصویر برش خورده است. خواندنی/نوشتنی float.

**بازگشت:**
float

### setCropRight(float value) {#setCropRight-float-}
```
public abstract void setCropRight(float value)
```

بازگرداندن یا تنظیم درصد عرض واقعی تصویر که از سمت راست تصویر برش خورده است. خواندنی/نوشتنی float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getCropBottom() {#getCropBottom--}
```
public abstract float getCropBottom()
```

بازگرداندن یا تنظیم درصد ارتفاع واقعی تصویر که از سمت پایین تصویر برش خورده است. خواندنی/نوشتنی float.

**بازگشت:**
float

### setCropBottom(float value) {#setCropBottom-float-}
```
public abstract void setCropBottom(float value)
```

بازگرداندن یا تنظیم درصد ارتفاع واقعی تصویر که از سمت پایین تصویر برش خورده است. خواندنی/نوشتنی float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getStretchOffsetLeft() {#getStretchOffsetLeft--}
```
public abstract float getStretchOffsetLeft()
```

بازگرداندن یا تنظیم لبهٔ چپ مستطیل پرکننده که توسط افست درصدی از لبهٔ چپ جعبهٔ محدود‌کنندهٔ شکل تعریف می‌شود. یک درصد مثبت داخلی‌تر و یک درصد منفی بیرونی‌تر را مشخص می‌کند. خواندنی/نوشتنی float.

**بازگشت:**
float

### setStretchOffsetLeft(float value) {#setStretchOffsetLeft-float-}
```
public abstract void setStretchOffsetLeft(float value)
```

بازگرداندن یا تنظیم لبهٔ چپ مستطیل پرکننده که توسط افست درصدی از لبهٔ چپ جعبهٔ محدود‌کنندهٔ شکل تعریف می‌شود. یک درصد مثبت داخلی‌تر و یک درصد منفی بیرونی‌تر را مشخص می‌کند. خواندنی/نوشتنی float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getStretchOffsetTop() {#getStretchOffsetTop--}
```
public abstract float getStretchOffsetTop()
```

بازگرداندن یا تنظیم لبهٔ بالای مستطیل پرکننده که توسط افست درصدی از لبهٔ بالای جعبهٔ محدود‌کنندهٔ شکل تعریف می‌شود. یک درصد مثبت داخلی‌تر و یک درصد منفی بیرونی‌تر را مشخص می‌کند. خواندنی/نوشتنی float.

**بازگشت:**
float

### setStretchOffsetTop(float value) {#setStretchOffsetTop-float-}
```
public abstract void setStretchOffsetTop(float value)
```

بازگرداندن یا تنظیم لبهٔ بالای مستطیل پرکننده که توسط افست درصدی از لبهٔ بالای جعبهٔ محدود‌کنندهٔ شکل تعریف می‌شود. یک درصد مثبت داخلی‌تر و یک درصد منفی بیرونی‌تر را مشخص می‌کند. خواندنی/نوشتنی float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getStretchOffsetRight() {#getStretchOffsetRight--}
```
public abstract float getStretchOffsetRight()
```

بازگرداندن یا تنظیم لبهٔ راست مستطیل پرکننده که توسط افست درصدی از لبهٔ راست جعبهٔ محدود‌کنندهٔ شکل تعریف می‌شود. یک درصد مثبت داخلی‌تر و یک درصد منفی بیرونی‌تر را مشخص می‌کند. خواندنی/نوشتنی float.

**بازگشت:**
float

### setStretchOffsetRight(float value) {#setStretchOffsetRight-float-}
```
public abstract void setStretchOffsetRight(float value)
```

بازگرداندن یا تنظیم لبهٔ راست مستطیل پرکننده که توسط افست درصدی از لبهٔ راست جعبهٔ محدود‌کنندهٔ شکل تعریف می‌شود. یک درصد مثبت داخلی‌تر و یک درصد منفی بیرونی‌تر را مشخص می‌کند. خواندنی/نوشتنی float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getStretchOffsetBottom() {#getStretchOffsetBottom--}
```
public abstract float getStretchOffsetBottom()
```

بازگرداندن یا تنظیم لبهٔ پایین مستطیل پرکننده که توسط افست درصدی از لبهٔ پایین جعبهٔ محدود‌کنندهٔ شکل تعریف می‌شود. یک درصد مثبت داخلی‌تر و یک درصد منفی بیرونی‌تر را مشخص می‌کند. خواندنی/نوشتنی float.

**بازگشت:**
float

### setStretchOffsetBottom(float value) {#setStretchOffsetBottom-float-}
```
public abstract void setStretchOffsetBottom(float value)
```

بازگرداندن یا تنظیم لبهٔ پایین مستطیل پرکننده که توسط افست درصدی از لبهٔ پایین جعبهٔ محدود‌کنندهٔ شکل تعریف می‌شود. یک درصد مثبت داخلی‌تر و یک درصد منفی بیرونی‌تر را مشخص می‌کند. خواندنی/نوشتنی float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### deletePictureCroppedAreas() {#deletePictureCroppedAreas--}
```
public abstract IPPImage deletePictureCroppedAreas()
```

حذف نواحی برش‌خورده تصویر پرکننده.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      ISlide slide = presentation.getSlides().get_Item(0);
>      // فریم تصویر را دریافت می‌کند
>      IPictureFrame picFrame = (IPictureFrame)slide.getShapes().get_Item(0);
>      // نواحی برش‌خورده تصویر فریم تصویر را حذف می‌کند
>      IPPImage croppedImage = picFrame.getPictureFormat().deletePictureCroppedAreas();
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**بازگشت:**  
[IPPImage](../../com.aspose.slides/ippimage) - تصویر برش‌خورده یا تصویر اصلی اگر برش ضروری نیست.

--------------------

این متد فایل‌های متافایل WMF/EMF را به تصویر PNG شطرنجی تبدیل می‌کند در حالی که برش می‌دهد.

### compressImage(boolean deleteCroppedAreasOfImage, int resolution) {#compressImage-boolean-int-}
```
public abstract boolean compressImage(boolean deleteCroppedAreasOfImage, int resolution)
```

فشرده‌سازی تصویر با کاهش اندازهٔ آن بر اساس اندازهٔ شکل و رزولوشن مشخص‌شده. به‌طور اختیاری، نواحی برش‌خورده را نیز حذف می‌کند.

--------------------

> ```
> The following example demonstrates how to use the ```
> CompressImage
> ``` متد برای کاهش اندازه یک تصویر در ارائه با تنظیم رزولوشن هدف و حذف نواحی برش‌خورده:  
>   
>  Presentation presentation = new Presentation("demo.pptx");
>  try {
>      ISlide slide = presentation.getSlides().get_Item(0);
>      IPictureFrame picFrame = (IPictureFrame)slide.getShapes().get_Item(0);
>      // Compress the image with a target resolution of 150 DPI (Web resolution) and remove cropped areas
>      boolean result = picFrame.getPictureFormat().compressImage(true, PicturesCompression.Dpi150);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```
**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| deleteCroppedAreasOfImage | boolean | اگر مقدار true باشد، متد نواحی برش‌خوردهٔ تصویر را حذف می‌کند و ممکن است اندازهٔ آن را بیشتر کاهش دهد. |
| resolution | int | رزولوشن هدف برای فشرده‌سازی، که به عنوان مقدار enum [PicturesCompression](../../com.aspose.slides/picturescompression) مشخص می‌شود. |

--------------------

این متد اندازه و رزولوشن تصویر را مشابه ویژگی «Picture Format -> Compress Pictures» در PowerPoint تغییر می‌دهد. |

**بازگشت:**
boolean - یک مقدار بولی که نشان می‌دهد آیا تصویر با موفقیت فشرده شده است یا نه. اگر تصویر تغییر اندازه یا برش خورده باشد true برگردانده می‌شود، در غیر این صورت false.
### compressImage(boolean deleteCroppedAreasOfImage, float resolution) {#compressImage-boolean-float-}
```
public abstract boolean compressImage(boolean deleteCroppedAreasOfImage, float resolution)
```


Compresses the image by reducing its size based on the shape size and specified resolution. Optionally, it also deletes cropped areas.

--------------------

> ```
> مثال زیر نشان می‌دهد چگونه از متد ```
> CompressImage
> ``` برای کاهش اندازه یک تصویر در ارائه با تنظیم رزولوشن هدف و حذف نواحی برش‌خورده استفاده شود:  
>   
>  Presentation presentation = new Presentation("demo.pptx");
>  try {
>      ISlide slide = presentation.getSlides().get_Item(0);
>      // Gets the PictureFrame
>      IPictureFrame picFrame = (IPictureFrame)slide.getShapes().get_Item(0);
>      // Compress the image with a target resolution of 150 DPI (Web resolution) and remove cropped areas
>      boolean result = picFrame.getPictureFormat().compressImage(true, 150f); // Web resolution
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| deleteCroppedAreasOfImage | boolean | اگر مقدار true باشد، متد نواحی برش‌خوردهٔ تصویر را حذف می‌کند و ممکن است اندازهٔ آن را بیشتر کاهش دهد. |
| resolution | float | رزولوشن هدف به DPI. این مقدار باید مثبت باشد و تعیین می‌کند تصویر چگونه تغییر اندازه می‌یابد.

--------------------

این متد اندازه و رزولوشن تصویر را مشابه ویژگی «Picture Format -> Compress Pictures» در PowerPoint تغییر می‌دهد. |

**بازگشت:**
boolean - یک مقدار بولی که نشان می‌دهد آیا تصویر با موفقیت فشرده شده است یا نه. اگر تصویر تغییر اندازه یا برش خورده باشد true برگردانده می‌شود، در غیر این صورت false.
### getTileOffsetX() {#getTileOffsetX--}
```
public abstract float getTileOffsetX()
```


Returns or sets the horizontal offset of the texture from the shape's origin in points. A positive value moves the texture to the right, while a negative value moves it to the left. Read/write  float .

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      ISlide slide = presentation.getSlides().get_Item(0);
>      // Gets the picture fill format of the shape
>      IPictureFillFormat pictureFillFormat = slide.getShapes().get_Item(0).getFillFormat().getPictureFillFormat();
>      // Sets the picture fill mode to Tile
>      pictureFillFormat.setPictureFillMode(PictureFillMode.Tile);
>      // Sets the horizontal offset of the texture to 20 points
>      pictureFillFormat.setTileOffsetX(20f);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```
**بازگشت:**
float
### setTileOffsetX(float value) {#setTileOffsetX-float-}
```
public abstract void setTileOffsetX(float value)
```


Returns or sets the horizontal offset of the texture from the shape's origin in points. A positive value moves the texture to the right, while a negative value moves it to the left. Read/write  float .

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      ISlide slide = presentation.getSlides().get_Item(0);
>      // Gets the picture fill format of the shape
>      IPictureFillFormat pictureFillFormat = slide.getShapes().get_Item(0).getFillFormat().getPictureFillFormat();
>      // Sets the picture fill mode to Tile
>      pictureFillFormat.setPictureFillMode(PictureFillMode.Tile);
>      // Sets the horizontal offset of the texture to 20 points
>      pictureFillFormat.setTileOffsetX(20f);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getTileOffsetY() {#getTileOffsetY--}
```
public abstract float getTileOffsetY()
```


Returns or sets the vertical offset of the texture from the shape's origin in points. A positive value moves the texture down, while a negative value moves it up. Read/write  float .

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      ISlide slide = presentation.getSlides().get_Item(0);
>      // Gets the picture fill format of the shape
>      IPictureFillFormat pictureFillFormat = slide.getShapes().get_Item(0).getFillFormat().getPictureFillFormat();
>      // Sets the picture fill mode to Tile
>      pictureFillFormat.setPictureFillMode(PictureFillMode.Tile);
>      // Sets the vertical offset of the texture to -50 points
>      pictureFillFormat.setTileOffsetY(-50);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**بازگشت:**
float
### setTileOffsetY(float value) {#setTileOffsetY-float-}
```
public abstract void setTileOffsetY(float value)
```

Returns or sets the vertical offset of the texture from the shape's origin in points. A positive value moves the texture down, while a negative value moves it up. Read/write  float .

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      ISlide slide = presentation.getSlides().get_Item(0);
>      // Gets the picture fill format of the shape
>      IPictureFillFormat pictureFillFormat = slide.getShapes().get_Item(0).getFillFormat().getPictureFillFormat();
>      // Sets the picture fill mode to Tile
>      pictureFillFormat.setPictureFillMode(PictureFillMode.Tile);
>      // Sets the vertical offset of the texture to -50 points
>      pictureFillFormat.setTileOffsetY(-50);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getTileScaleX() {#getTileScaleX--}
```
public abstract float getTileScaleX()
```

Returns or sets the horizontal scale for the texture fill as a percentage. Read/write  float .

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      ISlide slide = presentation.getSlides().get_Item(0);
>      // Gets the picture fill format of the shape
>      IPictureFillFormat pictureFillFormat = slide.getShapes().get_Item(0).getFillFormat().getPictureFillFormat();
>      // Sets the picture fill mode to Tile
>      pictureFillFormat.setPictureFillMode(PictureFillMode.Tile);
>      // Sets the horizontal scale for the texture to 120 percents
>      pictureFillFormat.setTileScaleX(120);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Returns:**
float
### setTileScaleX(float value) {#setTileScaleX-float-}
```
public abstract void setTileScaleX(float value)
```


Returns or sets the horizontal scale for the texture fill as a percentage. Read/write  float .

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      ISlide slide = presentation.getSlides().get_Item(0);
>      // Gets the picture fill format of the shape
>      IPictureFillFormat pictureFillFormat = slide.getShapes().get_Item(0).getFillFormat().getPictureFillFormat();
>      // Sets the picture fill mode to Tile
>      pictureFillFormat.setPictureFillMode(PictureFillMode.Tile);
>      // Sets the horizontal scale for the texture to 120 percents
>      pictureFillFormat.setTileScaleX(120);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```
**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getTileScaleY() {#getTileScaleY--}
```
public abstract float getTileScaleY()
```


Returns or sets the vertical scale for the texture fill as a percentage. Read/write  float .

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      ISlide slide = presentation.getSlides().get_Item(0);
>      // Gets the picture fill format of the shape
>      IPictureFillFormat pictureFillFormat = slide.getShapes().get_Item(0).getFillFormat().getPictureFillFormat();
>      // Sets the picture fill mode to Tile
>      pictureFillFormat.setPictureFillMode(PictureFillMode.Tile);
>      // Sets the vertical scale for the texture to 120 percents
>      pictureFillFormat.setTileScaleY(120);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**بازگشت:**
float
### setTileScaleY(float value) {#setTileScaleY-float-}
```
public abstract void setTileScaleY(float value)
```


Returns or sets the vertical scale for the texture fill as a percentage. Read/write  float .

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      ISlide slide = presentation.getSlides().get_Item(0);
>      // Gets the picture fill format of the shape
>      IPictureFillFormat pictureFillFormat = slide.getShapes().get_Item(0).getFillFormat().getPictureFillFormat();
>      // Sets the picture fill mode to Tile
>      pictureFillFormat.setPictureFillMode(PictureFillMode.Tile);
>      // Sets the vertical scale for the texture to 120 percents
>      pictureFillFormat.setTileScaleY(120);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getTileAlignment() {#getTileAlignment--}
```
public abstract byte getTileAlignment()
```


Returns or sets how the texture is aligned within the shape. This setting controls the starting point of the texture pattern and how it repeats across the shape. Read/write [RectangleAlignment](../../com.aspose.slides/rectanglealignment).

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      ISlide slide = presentation.getSlides().get_Item(0);
>      // Gets the picture fill format of the shape
>      IPictureFillFormat pictureFillFormat = slide.getShapes().get_Item(0).getFillFormat().getPictureFillFormat();
>      // Sets the picture fill mode to Tile
>      pictureFillFormat.setPictureFillMode(PictureFillMode.Tile);
>      // Sets the alignment for the tiling to the right bottom
>      pictureFillFormat.setTileAlignment(RectangleAlignment.BottomRight);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```
--------------------

Default is [RectangleAlignment.TopLeft](../../com.aspose.slides/rectanglealignment\#TopLeft).

**Returns:**
byte
### setTileAlignment(byte value) {#setTileAlignment-byte-}
```
public abstract void setTileAlignment(byte value)
```


Returns or sets how the texture is aligned within the shape. This setting controls the starting point of the texture pattern and how it repeats across the shape. Read/write [RectangleAlignment](../../com.aspose.slides/rectanglealignment).

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      ISlide slide = presentation.getSlides().get_Item(0);
>      // Gets the picture fill format of the shape
>      IPictureFillFormat pictureFillFormat = slide.getShapes().get_Item(0).getFillFormat().getPictureFillFormat();
>      // Sets the picture fill mode to Tile
>      pictureFillFormat.setPictureFillMode(PictureFillMode.Tile);
>      // Sets the alignment for the tiling to the right bottom
>      pictureFillFormat.setTileAlignment(RectangleAlignment.BottomRight);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

--------------------

Default is [RectangleAlignment.TopLeft](../../com.aspose.slides/rectanglealignment\#TopLeft).

**Parameters:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | byte |  |

### getTileFlip() {#getTileFlip--}
```
public abstract int getTileFlip()
```


Flips the texture tile around its horizontal, vertical or both axis. Read/write [TileFlip](../../com.aspose.slides/tileflip).

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      ISlide slide = presentation.getSlides().get_Item(0);
>      // Gets the picture fill format of the shape
>      IPictureFillFormat pictureFillFormat = slide.getShapes().get_Item(0).getFillFormat().getPictureFillFormat();
>      // Sets the picture fill mode to Tile
>      pictureFillFormat.setPictureFillMode(PictureFillMode.Tile);
>      // Flips the texture tile around its vertical axis.
>      pictureFillFormat.setTileFlip(TileFlip.FlipY);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```
--------------------

Default is [TileFlip.NoFlip](../../com.aspose.slides/tileflip\#NoFlip).

**Returns:**
int
### setTileFlip(int value) {#setTileFlip-int-}
```
public abstract void setTileFlip(int value)
```


Flips the texture tile around its horizontal, vertical or both axis. Read/write [TileFlip](../../com.aspose.slides/tileflip).

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      ISlide slide = presentation.getSlides().get_Item(0);
>      // Gets the picture fill format of the shape
>      IPictureFillFormat pictureFillFormat = slide.getShapes().get_Item(0).getFillFormat().getPictureFillFormat();
>      // Sets the picture fill mode to Tile
>      pictureFillFormat.setPictureFillMode(PictureFillMode.Tile);
>      // Flips the texture tile around its vertical axis.
>      pictureFillFormat.setTileFlip(TileFlip.FlipY);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

--------------------

پیش‌فرض [TileFlip.NoFlip](../../com.aspose.slides/tileflip\#NoFlip) است.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |