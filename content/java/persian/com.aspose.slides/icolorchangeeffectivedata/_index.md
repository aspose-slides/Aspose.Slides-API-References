---
title: IColorChangeEffectiveData
second_title: Aspose.Slides برای Java مرجع API
description: شیء غیرقابل تغییر که نمایانگر یک اثر تغییر رنگ است.
type: docs
url: /fa/com.aspose.slides/icolorchangeeffectivedata/
---
**تمام رابط‌های پیاده‌سازی شده:**
[com.aspose.slides.IEffectEffectiveData](../../com.aspose.slides/ieffecteffectivedata)
```
public interface IColorChangeEffectiveData extends IEffectEffectiveData
```

شیء غیرقابل تغییر که نمایانگر یک اثر تغییر رنگ است. نمونه‌های FromColor با نمونه‌های ToColor جایگزین می‌شوند.
## متدها

| متد | توضیح |
| --- | --- |
| [getFromColor()](#getFromColor--) | رنگی که جایگزین خواهد شد. |
| [getToColor()](#getToColor--) | رنگی که جایگزین می‌کند. |
| [getUseAlpha()](#getUseAlpha--) | مقداری بولی برمی‌گرداند که تعیین می‌کند آیا مؤلفه آلفا استفاده شود یا نه. |
### getFromColor() {#getFromColor--}
```
public abstract Color getFromColor()
```

رنگی که جایگزین خواهد شد. فقط خواندنی java.awt.Color.

**باز می‌گرداند:**
java.awt.Color
### getToColor() {#getToColor--}
```
public abstract Color getToColor()
```

رنگی که جایگزین می‌کند. فقط خواندنی java.awt.Color.

**باز می‌گرداند:**
java.awt.Color
### getUseAlpha() {#getUseAlpha--}
```
public abstract boolean getUseAlpha()
```

مقداری بولی برمی‌گرداند که تعیین می‌کند آیا مؤلفه آلفا استفاده شود یا نه. فقط خواندنی boolean.

**باز می‌گرداند:**
boolean