---
title: ChartWall
second_title: مرجع API Aspose.Slides برای جاوا
description: نمایانگر دیوارها در نمودارهای سه‌بعدی است.
type: docs
url: /fa/com.aspose.slides/chartwall/
---
**وراثت:**  
java.lang.Object, com.aspose.slides.DomObject

**تمام رابط‌های پیاده‌سازی شده:**  
[com.aspose.slides.IChartWall](../../com.aspose.slides/ichartwall)  
```
public class ChartWall extends DomObject<Chart> implements IChartWall
```

نمایانگر دیوارها در نمودارهای سه‌بعدی است.

## متدها

| متد | توضیح |
| --- | --- |
| [getThickness()](#getThickness--) | مقدار ضخامت دیوارها را به درصد بزرگ‌ترین بعد از حجم نمودار بر می‌گرداند یا تنظیم می‌کند. |
| [setThickness(int value)](#setThickness-int-) | مقدار ضخامت دیوارها را به درصد بزرگ‌ترین بعد از حجم نمودار بر می‌گرداند یا تنظیم می‌کند. |
| [getFormat()](#getFormat--) | پر کردن، خط، افکت و سبک‌های سه‌بعدی دیوار را بر می‌گرداند. |
| [getPictureType()](#getPictureType--) | نوع تصویر را بر می‌گرداند یا تنظیم می‌کند. |
| [setPictureType(int value)](#setPictureType-int-) | نوع تصویر را بر می‌گرداند یا تنظیم می‌کند. |

### getThickness() {#getThickness--}
```
public final int getThickness()
```

مقدار ضخامت دیوارها را به درصد بزرگ‌ترین بعد از حجم نمودار بر می‌گرداند یا تنظیم می‌کند. خواندنی/قابل نوشتن int.

**بازگشت:**  
int

### setThickness(int value) {#setThickness-int-}
```
public final void setThickness(int value)
```

مقدار ضخامت دیوارها را به درصد بزرگ‌ترین بعد از حجم نمودار بر می‌گرداند یا تنظیم می‌کند. خواندنی/قابل نوشتن int.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getFormat() {#getFormat--}
```
public final IFormat getFormat()
```

پر کردن، خط، افکت و سبک‌های سه‌بعدی دیوار را بر می‌گرداند. فقط-خواندنی [IFormat](../../com.aspose.slides/iformat).

**بازگشت:**  
[IFormat](../../com.aspose.slides/iformat)

### getPictureType() {#getPictureType--}
```
public final int getPictureType()
```

نوع تصویر را بر می‌گرداند یا تنظیم می‌کند. خواندنی/قابل نوشتن [PictureType](../../com.aspose.slides/picturetype).

**بازگشت:**  
int

### setPictureType(int value) {#setPictureType-int-}
```
public final void setPictureType(int value)
```

نوع تصویر را بر می‌گرداند یا تنظیم می‌کند. خواندنی/قابل نوشتن [PictureType](../../com.aspose.slides/picturetype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |