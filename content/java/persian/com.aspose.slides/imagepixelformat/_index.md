---
title: ImagePixelFormat
second_title: Aspose.Slides برای مرجع API جاوا
description: قالب پیکسل برای تصاویر تولید شده را مشخص می‌کند.
type: docs
url: /fa/com.aspose.slides/imagepixelformat/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class ImagePixelFormat extends System.Enum
```

قالب پیکسل برای تصاویر تولید شده را مشخص می‌کند.
## Fields

| Field | Description |
| --- | --- |
| [Format1bppIndexed](#Format1bppIndexed) | 1 بیت در هر پیکسل، فهرست‌دار. |
| [Format4bppIndexed](#Format4bppIndexed) | 4 بیت در هر پیکسل، فهرست‌دار. |
| [Format8bppIndexed](#Format8bppIndexed) | 8 بیت در هر پیکسل، فهرست‌دار. |
| [Format24bppRgb](#Format24bppRgb) | 24 بیت در هر پیکسل، RGB. |
| [Format32bppArgb](#Format32bppArgb) | 32 بیت در هر پیکسل، ARGB. |
### Format1bppIndexed {#Format1bppIndexed}
```
public static final int Format1bppIndexed
```

1 بیت در هر پیکسل، فهرست‌دار.

### Format4bppIndexed {#Format4bppIndexed}
```
public static final int Format4bppIndexed
```

4 بیت در هر پیکسل، فهرست‌دار.

### Format8bppIndexed {#Format8bppIndexed}
```
public static final int Format8bppIndexed
```

8 بیت در هر پیکسل، فهرست‌دار.

### Format24bppRgb {#Format24bppRgb}
```
public static final int Format24bppRgb
```

24 بیت در هر پیکسل، RGB.

### Format32bppArgb {#Format32bppArgb}
```
public static final int Format32bppArgb
```

32 بیت در هر پیکسل، ARGB.