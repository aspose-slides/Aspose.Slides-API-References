---
title: ITextSearchOptions
second_title: Aspose.Slides for Java API Reference
description: نمایانگر گزینه‌هایی است که می‌توان برای جستجوی متن در یک ارائه، اسلاید یا فریم متن استفاده کرد.
type: docs
url: /fa/com.aspose.slides/itextsearchoptions/
---```
public interface ITextSearchOptions
```

نمایانگر گزینه‌هایی است که می‌توان برای جستجو متن در یک ارائه، اسلاید یا فریم متن استفاده کرد.
## متدها

| متد | توضیحات |
| --- | --- |
| [getCaseSensitive()](#getCaseSensitive--) | true تنظیم کنید تا جستجوی حساس به حروف بزرگ انجام شود، در غیر این صورت false. |
| [setCaseSensitive(boolean value)](#setCaseSensitive-boolean-) | true تنظیم کنید تا جستجوی حساس به حروف بزرگ انجام شود، در غیر این صورت false. |
| [getWholeWordsOnly()](#getWholeWordsOnly--) | true تنظیم کنید تا فقط کلمات کامل مطابقت داده شوند، در غیر این صورت false. |
| [setWholeWordsOnly(boolean value)](#setWholeWordsOnly-boolean-) | true تنظیم کنید تا فقط کلمات کامل مطابقت داده شوند، در غیر این صورت false. |
| [getIncludeNotes()](#getIncludeNotes--) | true تنظیم کنید تا متن موجود در یادداشت‌های اسلاید هنگام انجام جستجو، جایگزینی یا هایلایت متن در نظر گرفته شود. |
| [setIncludeNotes(boolean value)](#setIncludeNotes-boolean-) | true تنظیم کنید تا متن موجود در یادداشت‌های اسلاید هنگام انجام جستجو، جایگزینی یا هایلایت متن در نظر گرفته شود. |
### getCaseSensitive() {#getCaseSensitive--}
```
public abstract boolean getCaseSensitive()
```

true تنظیم کنید تا جستجوی حساس به حروف بزرگ انجام شود، در غیر این صورت false. خواندنی/نوشتنی boolean.

**بازگشت:**  
boolean
### setCaseSensitive(boolean value) {#setCaseSensitive-boolean-}
```
public abstract void setCaseSensitive(boolean value)
```

true تنظیم کنید تا جستجوی حساس به حروف بزرگ انجام شود، در غیر این صورت false. خواندنی/نوشتنی boolean.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getWholeWordsOnly() {#getWholeWordsOnly--}
```
public abstract boolean getWholeWordsOnly()
```

true تنظیم کنید تا فقط کلمات کامل مطابقت داده شوند، در غیر این صورت false. خواندنی/نوشتنی boolean.

**بازگشت:**  
boolean
### setWholeWordsOnly(boolean value) {#setWholeWordsOnly-boolean-}
```
public abstract void setWholeWordsOnly(boolean value)
```

true تنظیم کنید تا فقط کلمات کامل مطابقت داده شوند، در غیر این صورت false. خواندنی/نوشتنی boolean.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getIncludeNotes() {#getIncludeNotes--}
```
public abstract boolean getIncludeNotes()
```

true تنظیم کنید تا متن موجود در یادداشت‌های اسلاید هنگام انجام جستجو، جایگزینی یا هایلایت متن در نظر گرفته شود. مقدار پیش‌فرض false است.

**بازگشت:**  
boolean
### setIncludeNotes(boolean value) {#setIncludeNotes-boolean-}
```
public abstract void setIncludeNotes(boolean value)
```

true تنظیم کنید تا متن موجود در یادداشت‌های اسلاید هنگام انجام جستجو، جایگزینی یا هایلایت متن در نظر گرفته شود. مقدار پیش‌فرض false است.

**پارامترها:**  
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |