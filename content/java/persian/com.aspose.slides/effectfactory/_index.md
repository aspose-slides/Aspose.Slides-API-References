---
title: EffectFactory
second_title: Aspose.Slides برای Java مرجع API
description: به ایجاد افکت‌ها اجازه می‌دهد
type: docs
url: /fa/com.aspose.slides/effectfactory/
---
**ارث‌بری:**
java.lang.Object

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IEffectFactory](../../com.aspose.slides/ieffectfactory)
```
public class EffectFactory implements IEffectFactory
```

اجازه می‌دهد تا افکت‌ها ایجاد شوند

--------------------

برای سازگاری با COM.
## سازنده‌ها

| سازنده | توضیح |
| --- | --- |
| [EffectFactory()](#EffectFactory--) |  |
## متدها

| متد | توضیح |
| --- | --- |
| [createGlow()](#createGlow--) | یک افکت تابش ایجاد می‌کند. |
| [createInnerShadow()](#createInnerShadow--) | یک افکت سایه داخلی ایجاد می‌کند. |
| [createOuterShadow()](#createOuterShadow--) | یک افکت سایه بیرونی ایجاد می‌کند. |
| [createPresetShadow()](#createPresetShadow--) | یک افکت سایه پیش‌تنظیم‌شده ایجاد می‌کند. |
| [createReflection()](#createReflection--) | یک افکت انعکاس ایجاد می‌کند. |
| [createSoftEdge()](#createSoftEdge--) | یک افکت لبه نرم ایجاد می‌کند. |
| [getImageTransformOperationFactory()](#getImageTransformOperationFactory--) | ImageTransformOperationFactory را بازمی‌گرداند. |

### EffectFactory() {#EffectFactory--}
```
public EffectFactory()
```


### createGlow() {#createGlow--}
```
public final IGlow createGlow()
```

یک افکت تابش ایجاد می‌کند.

**بازمی‌گرداند:**
[IGlow](../../com.aspose.slides/iglow) - افکت تابش.
### createInnerShadow() {#createInnerShadow--}
```
public final IInnerShadow createInnerShadow()
```

یک افکت سایه داخلی ایجاد می‌کند.

**بازمی‌گرداند:**
[IInnerShadow](../../com.aspose.slides/iinnershadow) - افکت سایه داخلی.
### createOuterShadow() {#createOuterShadow--}
```
public final IOuterShadow createOuterShadow()
```

یک افکت سایه بیرونی ایجاد می‌کند.

**بازمی‌گرداند:**
[IOuterShadow](../../com.aspose.slides/ioutershadow) - افکت سایه بیرونی.
### createPresetShadow() {#createPresetShadow--}
```
public final IPresetShadow createPresetShadow()
```

یک افکت سایه پیش‌تنظیم‌شده ایجاد می‌کند.

**بازمی‌گرداند:**
[IPresetShadow](../../com.aspose.slides/ipresetshadow) - افکت سایه پیش‌تنظیم‌شده.
### createReflection() {#createReflection--}
```
public final IReflection createReflection()
```

یک افکت انعکاس ایجاد می‌کند.

**بازمی‌گرداند:**
[IReflection](../../com.aspose.slides/ireflection) - افکت انعکاس.
### createSoftEdge() {#createSoftEdge--}
```
public final ISoftEdge createSoftEdge()
```

یک افکت لبه نرم ایجاد می‌کند.

**بازمی‌گرداند:**
[ISoftEdge](../../com.aspose.slides/isoftedge) - افکت لبه نرم.
### getImageTransformOperationFactory() {#getImageTransformOperationFactory--}
```
public final IImageTransformOperationFactory getImageTransformOperationFactory()
```

ImageTransformOperationFactory را بازمی‌گرداند. فقط‌خواندنی [IImageTransformOperationFactory](../../com.aspose.slides/iimagetransformoperationfactory).

**بازمی‌گرداند:**
[IImageTransformOperationFactory](../../com.aspose.slides/iimagetransformoperationfactory)