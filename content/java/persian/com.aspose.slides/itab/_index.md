---
title: ITab
second_title: مرجع API Aspose.Slides برای جاوا
description: نمایانگر یک تب‌گذاری برای متن.
type: docs
url: /fa/com.aspose.slides/itab/
---
**تمام اینترفیس‌های پیاده‌سازی‌شده:**
java.lang.Comparable
```
public interface ITab extends Comparable
```

نمایانگر یک تب‌گذاری برای متن.
## متدها

| متد | شرح |
| --- | --- |
| [getPosition()](#getPosition--) | مقدار موقعیت یک تب را برمی‌گرداند یا تنظیم می‌کند. |
| [setPosition(double value)](#setPosition-double-) | مقدار موقعیت یک تب را برمی‌گرداند یا تنظیم می‌کند. |
| [getAlignment()](#getAlignment--) | سبک تراز یک تب را برمی‌گرداند یا تنظیم می‌کند. |
| [setAlignment(int value)](#setAlignment-int-) | سبک تراز یک تب را برمی‌گرداند یا تنظیم می‌کند. |
### getPosition() {#getPosition--}
```
public abstract double getPosition()
```

مقدار موقعیت یک تب را برمی‌گرداند یا تنظیم می‌کند. انتساب این ویژگی می‌تواند ایندکس تب را در مجموعه تغییر دهد و Enumerator را نامعتبر کند. خواندنی/قابل نوشتن double.

**باز می‌گرداند:**
double
### setPosition(double value) {#setPosition-double-}
```
public abstract void setPosition(double value)
```

مقدار موقعیت یک تب را برمی‌گرداند یا تنظیم می‌کند. انتساب این ویژگی می‌تواند ایندکس تب را در مجموعه تغییر دهد و Enumerator را نامعتبر کند. خواندنی/قابل نوشتن double.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | double |  |
### getAlignment() {#getAlignment--}
```
public abstract int getAlignment()
```

سبک تراز یک تب را برمی‌گرداند یا تنظیم می‌کند. خواندنی/قابل نوشتن [TabAlignment](../../com.aspose.slides/tabalignment).

**باز می‌گرداند:**
int
### setAlignment(int value) {#setAlignment-int-}
```
public abstract void setAlignment(int value)
```

سبک تراز یک تب را برمی‌گرداند یا تنظیم می‌کند. خواندنی/قابل نوشتن [TabAlignment](../../com.aspose.slides/tabalignment).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |