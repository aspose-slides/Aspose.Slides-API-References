---
title: CrossesType
second_title: Aspose.Slides برای Java مرجع API
description: مشخص می‌کند که محور در کجا تقاطع می‌کند.
type: docs
url: /fa/com.aspose.slides/crossestype/
---
**وارثی:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class CrossesType extends System.Enum
```

مشخص می‌کند محور در کجا تقاطع می‌کند.
## فیلدها

| فیلد | توضیحات |
| --- | --- |
| [AxisCrossesAtZero](#AxisCrossesAtZero) | محور دسته‌بندی در نقطه صفر محور مقدار (در صورت امکان)، یا مقدار حداقل (اگر حداقل بزرگتر از صفر باشد) یا مقدار حداکثر (اگر حداکثر کمتر از صفر باشد) تقاطع می‌کند. |
| [Maximum](#Maximum) | محور در مقدار حداکثر تقاطع می‌کند. |
| [Custom](#Custom) | مقدار سفارشی از ویژگی CrossAt |
### AxisCrossesAtZero {#AxisCrossesAtZero}
```
public static final int AxisCrossesAtZero
```


محور دسته‌بندی در نقطه صفر محور مقدار (در صورت امکان)، یا مقدار حداقل (اگر حداقل بزرگتر از صفر باشد) یا مقدار حداکثر (اگر حداکثر کمتر از صفر باشد) تقاطع می‌کند.

### Maximum {#Maximum}
```
public static final int Maximum
```


محور در مقدار حداکثر تقاطع می‌کند.

### Custom {#Custom}
```
public static final int Custom
```


مقدار سفارشی از ویژگی CrossAt