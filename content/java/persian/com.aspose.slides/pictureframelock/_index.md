---
title: PictureFrameLock
second_title: مرجع API Aspose.Slides برای Java
description: تعیین می‌کند که کدام عملیات‌ها بر روی PictureFrame والد غیرفعال هستند.
type: docs
url: /fa/com.aspose.slides/pictureframelock/
---
**Inheritance:**  
ارث‌بری:  
java.lang.Object, [com.aspose.slides.BaseShapeLock](../../com.aspose.slides/baseshapelock)

**All Implemented Interfaces:**  
تمام رابط‌های پیاده‌سازی‌شده:  
[com.aspose.slides.IPictureFrameLock](../../com.aspose.slides/ipictureframelock)  
```
public class PictureFrameLock extends BaseShapeLock implements IPictureFrameLock
```

تعیین می‌کند که چه عملیات‌هایی بر روی PictureFrame والد غیرفعال هستند.
## Methods

| Method | Description |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | تعیین می‌کند که افزودن این شکل به یک گروه ممنوع است. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | تعیین می‌کند که افزودن این شکل به یک گروه ممنوع است. |
| [getSelectLocked()](#getSelectLocked--) | تعیین می‌کند که انتخاب این شکل ممنوع است. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | تعیین می‌کند که انتخاب این شکل ممنوع است. |
| [getRotationLocked()](#getRotationLocked--) | تعیین می‌کند که تغییر زاویه چرخش این شکل ممنوع است. |
| [setRotationLocked(boolean value)](#setRotationLocked-boolean-) | تعیین می‌کند که تغییر زاویه چرخش این شکل ممنوع است. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | تعیین می‌کند که یک شکل باید نسبت عرض-ارتفاع را هنگام تغییر اندازه حفظ کند. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | تعیین می‌کند که یک شکل باید نسبت عرض-ارتفاع را هنگام تغییر اندازه حفظ کند. |
| [getPositionLocked()](#getPositionLocked--) | تعیین می‌کند که جابه‌جایی این شکل ممنوع است. |
| [setPositionLocked(boolean value)](#setPositionLocked-boolean-) | تعیین می‌کند که جابه‌جایی این شکل ممنوع است. |
| [getSizeLocked()](#getSizeLocked--) | تعیین می‌کند که تغییر اندازه این شکل ممنوع است. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | تعیین می‌کند که تغییر اندازه این شکل ممنوع است. |
| [getEditPointsLocked()](#getEditPointsLocked--) | تعیین می‌کند که تغییر مستقیم کانتور این شکل ممنوع است. |
| [setEditPointsLocked(boolean value)](#setEditPointsLocked-boolean-) | تعیین می‌کند که تغییر مستقیم کانتور این شکل ممنوع است. |
| [getAdjustHandlesLocked()](#getAdjustHandlesLocked--) | تعیین می‌کند که تغییر مقادیر تنظیم ممنوع است. |
| [setAdjustHandlesLocked(boolean value)](#setAdjustHandlesLocked-boolean-) | تعیین می‌کند که تغییر مقادیر تنظیم ممنوع است. |
| [getArrowheadsLocked()](#getArrowheadsLocked--) | تعیین می‌کند که تغییر سرهای پیکان ممنوع است. |
| [setArrowheadsLocked(boolean value)](#setArrowheadsLocked-boolean-) | تعیین می‌کند که تغییر سرهای پیکان ممنوع است. |
| [getShapeTypeLocked()](#getShapeTypeLocked--) | تعیین می‌کند که تغییر نوع شکل ممنوع است. |
| [setShapeTypeLocked(boolean value)](#setShapeTypeLocked-boolean-) | تعیین می‌کند که تغییر نوع شکل ممنوع است. |
| [getCropLocked()](#getCropLocked--) | تعیین می‌کند که برش تصویر ممنوع است. |
| [setCropLocked(boolean value)](#setCropLocked-boolean-) | تعیین می‌کند که برش تصویر ممنوع است. |
### getGroupingLocked() {#getGroupingLocked--}
```
public boolean getGroupingLocked()
```

تعیین می‌کند که افزودن این شکل به یک گروه ممنوع است. خواندنی/نوشتنی boolean.

**Returns:**  
بازگرداندن:  
boolean
### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public void setGroupingLocked(boolean value)
```

تعیین می‌کند که افزودن این شکل به یک گروه ممنوع است. خواندنی/نوشتنی boolean.

**Parameters:**  
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |
### getSelectLocked() {#getSelectLocked--}
```
public boolean getSelectLocked()
```

تعیین می‌کند که انتخاب این شکل ممنوع است. خواندنی/نوشتنی boolean.

**Returns:**  
بازگرداندن:  
boolean
### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public void setSelectLocked(boolean value)
```

تعیین می‌کند که انتخاب این شکل ممنوع است. خواندنی/نوشتنی boolean.

**Parameters:**  
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |
### getRotationLocked() {#getRotationLocked--}
```
public boolean getRotationLocked()
```

تعیین می‌کند که تغییر زاویه چرخش این شکل ممنوع است. خواندنی/نوشتنی boolean.

**Returns:**  
بازگرداندن:  
boolean
### setRotationLocked(boolean value) {#setRotationLocked-boolean-}
```
public void setRotationLocked(boolean value)
```

تعیین می‌کند که تغییر زاویه چرخش این شکل ممنوع است. خواندنی/نوشتنی boolean.

**Parameters:**  
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |
### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public boolean getAspectRatioLocked()
```

تعیین می‌کند که یک شکل باید نسبت عرض-ارتفاع را هنگام تغییر اندازه حفظ کند. خواندنی/نوشتنی boolean.

**Returns:**  
بازگرداندن:  
boolean
### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public void setAspectRatioLocked(boolean value)
```

تعیین می‌کند که یک شکل باید نسبت عرض-ارتفاع را هنگام تغییر اندازه حفظ کند. خواندنی/نوشتنی boolean.

**Parameters:**  
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |
### getPositionLocked() {#getPositionLocked--}
```
public boolean getPositionLocked()
```

تعیین می‌کند که جابه‌جایی این شکل ممنوع است. خواندنی/نوشتنی boolean.

**Returns:**  
بازگرداندن:  
boolean
### setPositionLocked(boolean value) {#setPositionLocked-boolean-}
```
public void setPositionLocked(boolean value)
```

تعیین می‌کند که جابه‌جایی این شکل ممنوع است. خواندنی/نوشتنی boolean.

**Parameters:**  
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |
### getSizeLocked() {#getSizeLocked--}
```
public boolean getSizeLocked()
```

تعیین می‌کند که تغییر اندازه این شکل ممنوع است. خواندنی/نوشتنی boolean.

**Returns:**  
بازگرداندن:  
boolean
### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public void setSizeLocked(boolean value)
```

تعیین می‌کند که تغییر اندازه این شکل ممنوع است. خواندنی/نوشتنی boolean.

**Parameters:**  
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |
### getEditPointsLocked() {#getEditPointsLocked--}
```
public boolean getEditPointsLocked()
```

تعیین می‌کند که تغییر مستقیم کانتور این شکل ممنوع است. خواندنی/نوشتنی boolean.

**Returns:**  
بازگرداندن:  
boolean
### setEditPointsLocked(boolean value) {#setEditPointsLocked-boolean-}
```
public void setEditPointsLocked(boolean value)
```

تعیین می‌کند که تغییر مستقیم کانتور این شکل ممنوع است. خواندنی/نوشتنی boolean.

**Parameters:**  
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |
### getAdjustHandlesLocked() {#getAdjustHandlesLocked--}
```
public boolean getAdjustHandlesLocked()
```

تعیین می‌کند که تغییر مقادیر تنظیم ممنوع است. خواندنی/نوشتنی boolean.

**Returns:**  
بازگرداندن:  
boolean
### setAdjustHandlesLocked(boolean value) {#setAdjustHandlesLocked-boolean-}
```
public void setAdjustHandlesLocked(boolean value)
```

تعیین می‌کند که تغییر مقادیر تنظیم ممنوع است. خواندنی/نوشتنی boolean.

**Parameters:**  
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |
### getArrowheadsLocked() {#getArrowheadsLocked--}
```
public boolean getArrowheadsLocked()
```

تعیین می‌کند که تغییر سرهای پیکان ممنوع است. خواندنی/نوشتنی boolean.

**Returns:**  
بازگرداندن:  
boolean
### setArrowheadsLocked(boolean value) {#setArrowheadsLocked-boolean-}
```
public void setArrowheadsLocked(boolean value)
```

تعیین می‌کند که تغییر سرهای پیکان ممنوع است. خواندنی/نوشتنی boolean.

**Parameters:**  
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |
### getShapeTypeLocked() {#getShapeTypeLocked--}
```
public boolean getShapeTypeLocked()
```

تعیین می‌کند که تغییر نوع شکل ممنوع است. خواندنی/نوشتنی boolean.

**Returns:**  
بازگرداندن:  
boolean
### setShapeTypeLocked(boolean value) {#setShapeTypeLocked-boolean-}
```
public void setShapeTypeLocked(boolean value)
```

تعیین می‌کند که تغییر نوع شکل ممنوع است. خواندنی/نوشتنی boolean.

**Parameters:**  
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |
### getCropLocked() {#getCropLocked--}
```
public boolean getCropLocked()
```

تعیین می‌کند که برش تصویر ممنوع است. خواندنی/نوشتنی boolean.

**Returns:**  
بازگرداندن:  
boolean
### setCropLocked(boolean value) {#setCropLocked-boolean-}
```
public void setCropLocked(boolean value)
```

تعیین می‌کند که برش تصویر ممنوع است. خواندنی/نوشتنی boolean.

**Parameters:**  
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |