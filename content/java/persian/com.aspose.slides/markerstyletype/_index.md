---
title: MarkerStyleType
second_title: مرجع API Aspose.Slides برای جاوا
description: شکل نشانگر را در نقطه دادهٔ نمودارها تعیین می‌کند.
type: docs
url: /fa/com.aspose.slides/markerstyletype/
---
**ارث‌بری:**  
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class MarkerStyleType extends System.Enum
```

فرم نشانگر را در نقطه دادهٔ نمودار تعیین می‌کند.
## فیلدها

| فیلد | توضیح |
| --- | --- |
| [NotDefined](#NotDefined) |  |
| [Circle](#Circle) | مشخص می‌کند که یک دایره در هر نقطه داده رسم شود. |
| [Dash](#Dash) | مشخص می‌کند که یک خط قطع‌دار در هر نقطه داده رسم شود. |
| [Diamond](#Diamond) | مشخص می‌کند که یک الماس در هر نقطه داده رسم شود. |
| [Dot](#Dot) | مشخص می‌کند که یک نقطه در هر نقطه داده رسم شود. |
| [None](#None) | مشخص می‌کند که هیچ چیزی در هر نقطه داده رسم نشود. |
| [Picture](#Picture) | مشخص می‌کند که یک تصویر در هر نقطه داده رسم شود. |
| [Plus](#Plus) | مشخص می‌کند که یک علامت مثبت در هر نقطه داده رسم شود. |
| [Square](#Square) | مشخص می‌کند که یک مربع در هر نقطه داده رسم شود. |
| [Star](#Star) | مشخص می‌کند که یک ستاره در هر نقطه داده رسم شود. |
| [Triangle](#Triangle) | مشخص می‌کند که یک مثلث در هر نقطه داده رسم شود. |
| [X](#X) | مشخص می‌کند که یک X در هر نقطه داده رسم شود. |
### NotDefined {#NotDefined}
```
public static final int NotDefined
```




### Circle {#Circle}
```
public static final int Circle
```

مشخص می‌کند که یک دایره در هر نقطه داده رسم شود.

### Dash {#Dash}
```
public static final int Dash
```

مشخص می‌کند که یک خط قطع‌دار در هر نقطه داده رسم شود.

### Diamond {#Diamond}
```
public static final int Diamond
```

مشخص می‌کند که یک الماس در هر نقطه داده رسم شود.

### Dot {#Dot}
```
public static final int Dot
```

مشخص می‌کند که یک نقطه در هر نقطه داده رسم شود.

### None {#None}
```
public static final int None
```

مشخص می‌کند که هیچ چیزی در هر نقطه داده رسم نشود.

### Picture {#Picture}
```
public static final int Picture
```

مشخص می‌کند که یک تصویر در هر نقطه داده رسم شود.

### Plus {#Plus}
```
public static final int Plus
```

مشخص می‌کند که یک علامت مثبت در هر نقطه داده رسم شود.

### Square {#Square}
```
public static final int Square
```

مشخص می‌کند که یک مربع در هر نقطه داده رسم شود.

### Star {#Star}
```
public static final int Star
```

مشخص می‌کند که یک ستاره در هر نقطه داده رسم شود.

### Triangle {#Triangle}
```
public static final int Triangle
```

مشخص می‌کند که یک مثلث در هر نقطه داده رسم شود.

### X {#X}
```
public static final int X
```

مشخص می‌کند که یک X در هر نقطه داده رسم شود.