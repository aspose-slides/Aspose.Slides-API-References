---
title: DataSourceType
second_title: مرجع API Aspose.Slides برای جاوا
description: انواع منبع داده.
type: docs
url: /fa/com.aspose.slides/datasourcetype/
---
**ارث‌بری:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class DataSourceType extends System.Enum
```

انواع منبع داده‌ها.
## فیلدها

| فیلد | توضیح |
| --- | --- |
| [Worksheet](#Worksheet) | از اشیاء CellXls برای ذخیره XValue، YValue، Value یا BubbleSize نقطه داده در worksheet استفاده کنید. |
| [StringLiterals](#StringLiterals) | از متغیر رشته‌ای سریع‌الوصول برای ذخیره XValue، YValue، Value یا BubbleSize نقطه داده استفاده کنید. |
| [DoubleLiterals](#DoubleLiterals) | از متغیر double سریع‌الوصول برای ذخیره XValue، YValue، Value یا BubbleSize نقطه داده استفاده کنید. |
### Worksheet {#Worksheet}
```
public static final int Worksheet
```

از اشیاء CellXls برای ذخیره XValue، YValue، Value یا BubbleSize نقطه داده در worksheet استفاده کنید.

### StringLiterals {#StringLiterals}
```
public static final int StringLiterals
```

از متغیر رشته‌ای سریع‌الوصول برای ذخیره XValue، YValue، Value یا BubbleSize نقطه داده استفاده کنید. Worksheet برای ذخیره مقادیر استفاده نمی‌شود.

### DoubleLiterals {#DoubleLiterals}
```
public static final int DoubleLiterals
```

از متغیر double سریع‌الوصول برای ذخیره XValue، YValue، Value یا BubbleSize نقطه داده استفاده کنید. Worksheet برای ذخیره مقادیر استفاده نمی‌شود.