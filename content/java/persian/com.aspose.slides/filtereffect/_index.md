---
title: FilterEffect
second_title: مرجع API Aspose.Slides برای جاوا
description: نمایش اثر فیلتر رفتار.
type: docs
url: /fa/com.aspose.slides/filtereffect/
---
**ارث‌بری:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IFilterEffect](../../com.aspose.slides/ifiltereffect)
```
public class FilterEffect extends Behavior implements IFilterEffect
```

نمایش اثر فیلتر رفتار.
## سازنده‌ها

| سازنده | توضیح |
| --- | --- |
| [FilterEffect()](#FilterEffect--) | سازنده پیش‌فرض. |
## متدها

| متد | توضیح |
| --- | --- |
| [getReveal()](#getReveal--) | نمایش می‌دهد که اثر با رفتار باید (in/out) خواندن/نوشتن [FilterEffectRevealType](../../com.aspose.slides/filtereffectrevealtype) را آشکار کند. |
| [setReveal(int value)](#setReveal-int-) | نمایش می‌دهد که اثر با رفتار باید (in/out) خواندن/نوشتن [FilterEffectRevealType](../../com.aspose.slides/filtereffectrevealtype) را آشکار کند. |
| [getType()](#getType--) | نوع اثر فیلتر را نشان می‌دهد. |
| [setType(int value)](#setType-int-) | نوع اثر فیلتر را نشان می‌دهد. |
| [getSubtype()](#getSubtype--) | زیرنوع اثر فیلتر را نشان می‌دهد. |
| [setSubtype(int value)](#setSubtype-int-) | زیرنوع اثر فیلتر را نشان می‌دهد. |
### FilterEffect() {#FilterEffect--}
```
public FilterEffect()
```

سازنده پیش‌فرض.

### getReveal() {#getReveal--}
```
public final int getReveal()
```

نمایش می‌دهد که اثر با رفتار باید (in/out) خواندن/نوشتن [FilterEffectRevealType](../../com.aspose.slides/filtereffectrevealtype) را آشکار کند.

**بازگشت:**
int
### setReveal(int value) {#setReveal-int-}
```
public final void setReveal(int value)
```

نمایش می‌دهد که اثر با رفتار باید (in/out) خواندن/نوشتن [FilterEffectRevealType](../../com.aspose.slides/filtereffectrevealtype) را آشکار کند.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getType() {#getType--}
```
public final int getType()
```

نوع اثر فیلتر را نشان می‌دهد. خواندن/نوشتن [FilterEffectType](../../com.aspose.slides/filtereffecttype).

**بازگشت:**
int
### setType(int value) {#setType-int-}
```
public final void setType(int value)
```

نوع اثر فیلتر را نشان می‌دهد. خواندن/نوشتن [FilterEffectType](../../com.aspose.slides/filtereffecttype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getSubtype() {#getSubtype--}
```
public final int getSubtype()
```

زیرنوع اثر فیلتر را نشان می‌دهد. خواندن/نوشتن [FilterEffectSubtype](../../com.aspose.slides/filtereffectsubtype).

**بازگشت:**
int
### setSubtype(int value) {#setSubtype-int-}
```
public final void setSubtype(int value)
```

زیرنوع اثر فیلتر را نشان می‌دهد. خواندن/نوشتن [FilterEffectSubtype](../../com.aspose.slides/filtereffectsubtype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |