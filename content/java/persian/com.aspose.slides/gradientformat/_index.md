---
title: GradientFormat
second_title: Aspose.Slides برای مرجع API جاوا
description: نمایش یک قالب گرادیان.
type: docs
url: /fa/com.aspose.slides/gradientformat/
---
**ارث‌بری:**  
java.lang.Object, [com.aspose.slides.PVIObject](../../com.aspose.slides/pviobject)

**تمام رابط‌های پیاده‌سازی‌شده:**  
[com.aspose.slides.IGradientFormat](../../com.aspose.slides/igradientformat)  
```
public final class GradientFormat extends PVIObject implements IGradientFormat
```

نمایش یک قالب گرادیان.

## متدها

| متد | توضیح |
| --- | --- |
| [getVersion()](#getVersion--) |  |
| [getTileFlip()](#getTileFlip--) | حالت چرخش برای یک گرادیان را برمی‌گرداند یا تنظیم می‌کند. |
| [setTileFlip(int value)](#setTileFlip-int-) | حالت چرخش برای یک گرادیان را برمی‌گرداند یا تنظیم می‌کند. |
| [getGradientDirection()](#getGradientDirection--) | سبک یک گرادیان را برمی‌گرداند یا تنظیم می‌کند. |
| [setGradientDirection(int value)](#setGradientDirection-int-) | سبک یک گرادیان را برمی‌گرداند یا تنظیم می‌کند. |
| [getLinearGradientAngle()](#getLinearGradientAngle--) | زاویه یک گرادیان را برمی‌گرداند یا تنظیم می‌کند. |
| [setLinearGradientAngle(float value)](#setLinearGradientAngle-float-) | زاویه یک گرادیان را برمی‌گرداند یا تنظیم می‌کند. |
| [getLinearGradientScaled()](#getLinearGradientScaled--) | تعیین می‌کند که آیا یک گرادیان مقیاس‌گذاری شده است یا خیر. |
| [setLinearGradientScaled(byte value)](#setLinearGradientScaled-byte-) | تعیین می‌کند که آیا یک گرادیان مقیاس‌گذاری شده است یا خیر. |
| [getGradientShape()](#getGradientShape--) | شکل یک گرادیان را برمی‌گرداند یا تنظیم می‌کند. |
| [setGradientShape(byte value)](#setGradientShape-byte-) | شکل یک گرادیان را برمی‌گرداند یا تنظیم می‌کند. |
| [getGradientStops()](#getGradientStops--) | مجموعه نقاط توقف گرادیان را برمی‌گرداند. |

### getVersion() {#getVersion--}
```
public long getVersion()
```

نسخه. فقط‌قابل‌خواندن long.

**باز می‌گرداند:**  
long

### getTileFlip() {#getTileFlip--}
```
public final int getTileFlip()
```

حالت چرخش برای یک گرادیان را برمی‌گرداند یا تنظیم می‌کند. قابل‌خواندن/قابل‌نوشتن [TileFlip](../../com.aspose.slides/tileflip).

**باز می‌گرداند:**  
int

### setTileFlip(int value) {#setTileFlip-int-}
```
public final void setTileFlip(int value)
```

حالت چرخش برای یک گرادیان را برمی‌گرداند یا تنظیم می‌کند. قابل‌خواندن/قابل‌نوشتن [TileFlip](../../com.aspose.slides/tileflip).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getGradientDirection() {#getGradientDirection--}
```
public final int getGradientDirection()
```

سبک یک گرادیان را برمی‌گرداند یا تنظیم می‌کند. قابل‌خواندن/قابل‌نوشتن [GradientDirection](../../com.aspose.slides/gradientdirection).

**باز می‌گرداند:**  
int

### setGradientDirection(int value) {#setGradientDirection-int-}
```
public final void setGradientDirection(int value)
```

سبک یک گرادیان را برمی‌گرداند یا تنظیم می‌کند. قابل‌خواندن/قابل‌نوشتن [GradientDirection](../../com.aspose.slides/gradientdirection).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getLinearGradientAngle() {#getLinearGradientAngle--}
```
public final float getLinearGradientAngle()
```

زاویه یک گرادیان را برمی‌گرداند یا تنظیم می‌کند. قابل‌خواندن/قابل‌نوشتن float.

**باز می‌گرداند:**  
float

### setLinearGradientAngle(float value) {#setLinearGradientAngle-float-}
```
public final void setLinearGradientAngle(float value)
```

زاویه یک گرادیان را برمی‌گرداند یا تنظیم می‌کند. قابل‌خواندن/قابل‌نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getLinearGradientScaled() {#getLinearGradientScaled--}
```
public final byte getLinearGradientScaled()
```

تعیین می‌کند که آیا یک گرادیان مقیاس‌گذاری شده است یا خیر. قابل‌خواندن/قابل‌نوشتن [NullableBool](../../com.aspose.slides/nullablebool).

**باز می‌گرداند:**  
byte

### setLinearGradientScaled(byte value) {#setLinearGradientScaled-byte-}
```
public final void setLinearGradientScaled(byte value)
```

تعیین می‌کند که آیا یک گرادیان مقیاس‌گذاری شده است یا خیر. قابل‌خواندن/قابل‌نوشتن [NullableBool](../../com.aspose.slides/nullablebool).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | byte |  |

### getGradientShape() {#getGradientShape--}
```
public final byte getGradientShape()
```

شکل یک گرادیان را برمی‌گرداند یا تنظیم می‌کند. قابل‌خواندن/قابل‌نوشتن [GradientShape](../../com.aspose.slides/gradientshape).

**باز می‌گرداند:**  
byte

### setGradientShape(byte value) {#setGradientShape-byte-}
```
public final void setGradientShape(byte value)
```

شکل یک گرادیان را برمی‌گرداند یا تنظیم می‌کند. قابل‌خواندن/قابل‌نوشتن [GradientShape](../../com.aspose.slides/gradientshape).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | byte |  |

### getGradientStops() {#getGradientStops--}
```
public final IGradientStopCollection getGradientStops()
```

مجموعه نقاط توقف گرادیان را برمی‌گرداند. فقط‌قابل‌خواندن [IGradientStopCollection](../../com.aspose.slides/igradientstopcollection).

**باز می‌گرداند:**  
[IGradientStopCollection](../../com.aspose.slides/igradientstopcollection)