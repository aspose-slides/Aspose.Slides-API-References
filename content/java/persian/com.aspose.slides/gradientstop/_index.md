---
title: GradientStop
second_title: مرجع API Aspose.Slides برای جاوا
description: یک قالب گرادیان را نمایش می‌دهد.
type: docs
url: /fa/com.aspose.slides/gradientstop/
---
**ارث‌بری:**
java.lang.Object, [com.aspose.slides.PVIObject](../../com.aspose.slides/pviobject)

**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IGradientStop](../../com.aspose.slides/igradientstop)
```
public final class GradientStop extends PVIObject implements IGradientStop
```

یک قالب گرادیان را نمایش می‌دهد.
## متدها

| Method | Description |
| --- | --- |
| [getVersion()](#getVersion--) |  |
| [getPosition()](#getPosition--) | موقعیت (0..1) یک نقطه توقف گرادیان را برمی‌گرداند یا تنظیم می‌کند. |
| [setPosition(float value)](#setPosition-float-) | موقعیت (0..1) یک نقطه توقف گرادیان را برمی‌گرداند یا تنظیم می‌کند. |
| [getColor()](#getColor--) | رنگ یک نقطه توقف گرادیان را برمی‌گرداند. |
### getVersion() {#getVersion--}
```
public long getVersion()
```


نسخه. فقط-خواندنی long.

**برمی‌گرداند:**
long
### getPosition() {#getPosition--}
```
public final float getPosition()
```


موقعیت (0..1) یک نقطه توقف گرادیان را برمی‌گرداند یا تنظیم می‌کند. قابل‌خواندن/قابل‌نوشتن  float .

**برمی‌گرداند:**
float
### setPosition(float value) {#setPosition-float-}
```
public final void setPosition(float value)
```


موقعیت (0..1) یک نقطه توقف گرادیان را برمی‌گرداند یا تنظیم می‌کند. قابل‌خواندن/قابل‌نوشتن  float .

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | float |  |

### getColor() {#getColor--}
```
public final IColorFormat getColor()
```


رنگ یک نقطه توقف گرادیان را برمی‌گرداند. فقط-خواندنی [IColorFormat](../../com.aspose.slides/icolorformat).

**برمی‌گرداند:**
[IColorFormat](../../com.aspose.slides/icolorformat)