---
title: IShapeFrame
second_title: Aspose.Slides برای مرجع API جاوا
description: خواص فریم‌های شکل را نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/ishapeframe/
---
**تمام رابط‌های پیاده‌سازی‌شده:**
com.aspose.slides.IGenericCloneable
```
public interface IShapeFrame extends IGenericCloneable<IShapeFrame>
```

خواص فریم شکل را نشان می‌دهد.
## متدها

| متد | توضیح |
| --- | --- |
| [getX()](#getX--) | مختصات X گوشهٔ بالا-چپ یک فریم را باز می‌گرداند. |
| [getY()](#getY--) | مختصات Y گوشهٔ بالا-چپ یک فریم را باز می‌گرداند. |
| [getWidth()](#getWidth--) | عرض یک فریم را باز می‌گرداند. |
| [getHeight()](#getHeight--) | ارتفاع یک فریم را باز می‌گرداند. |
| [getRotation()](#getRotation--) | تعداد درجه‌های چرخش فریم حول محور z را باز می‌گرداند. |
| [getCenterX()](#getCenterX--) | مختصات X مرکز فریم را باز می‌گرداند. |
| [getCenterY()](#getCenterY--) | مختصات Y مرکز فریم را باز می‌گرداند. |
| [getFlipH()](#getFlipH--) | تعیین می‌کند که آیا یک فریم به صورت افقی وارونه است یا خیر. |
| [getFlipV()](#getFlipV--) | تعیین می‌کند که آیا یک فریم به صورت عمودی وارونه است یا خیر. |
| [getRectangle()](#getRectangle--) | مختصات یک فریم را باز می‌گرداند. |
### getX() {#getX--}
```
public abstract float getX()
```


مختصات X گوشهٔ بالا-چپ یک فریم را باز می‌گرداند. فقط-خواندنی float.

**باز می‌گرداند:**
float
### getY() {#getY--}
```
public abstract float getY()
```


مختصات Y گوشهٔ بالا-چپ یک فریم را باز می‌گرداند. فقط-خواندنی float.

**باز می‌گرداند:**
float
### getWidth() {#getWidth--}
```
public abstract float getWidth()
```


عرض یک فریم را باز می‌گرداند. فقط-خواندنی float.

**باز می‌گرداند:**
float
### getHeight() {#getHeight--}
```
public abstract float getHeight()
```


ارتفاع یک فریم را باز می‌گرداند. فقط-خواندنی float.

**باز می‌گرداند:**
float
### getRotation() {#getRotation--}
```
public abstract float getRotation()
```


تعداد درجه‌های چرخش فریم حول محور z را باز می‌گرداند. مقدار مثبت نشان‌دهنده چرخش ساعت‌گرد؛ مقدار منفی نشان‌دهنده چرخش پادساعت‌گرد است. فقط-خواندنی float.

**باز می‌گرداند:**
float
### getCenterX() {#getCenterX--}
```
public abstract float getCenterX()
```


مختصات X مرکز فریم را باز می‌گرداند. فقط-خواندنی float.

**باز می‌گرداند:**
float
### getCenterY() {#getCenterY--}
```
public abstract float getCenterY()
```


مختصات Y مرکز فریم را باز می‌گرداند. فقط-خواندنی float.

**باز می‌گرداند:**
float
### getFlipH() {#getFlipH--}
```
public abstract byte getFlipH()
```


تعیین می‌کند که آیا یک فریم به صورت افقی وارونه است یا خیر. فقط-خواندنی [NullableBool](../../com.aspose.slides/nullablebool).

**باز می‌گرداند:**
byte
### getFlipV() {#getFlipV--}
```
public abstract byte getFlipV()
```


تعیین می‌کند که آیا یک فریم به صورت عمودی وارونه است یا خیر. فقط-خواندنی [NullableBool](../../com.aspose.slides/nullablebool).

**باز می‌گرداند:**
byte
### getRectangle() {#getRectangle--}
```
public abstract Rectangle2D.Float getRectangle()
```


مختصات یک فریم را باز می‌گرداند. فقط-خواندنی java.awt.geom.Rectangle2D.Float.

**باز می‌گرداند:**
java.awt.geom.Rectangle2D.Float