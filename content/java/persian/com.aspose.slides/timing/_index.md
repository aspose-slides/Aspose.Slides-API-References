---
title: Timing
second_title: مرجع API Aspose.Slides برای جاوا
description: نمایانگر زمان‌بندی انیمیشن.
type: docs
url: /fa/com.aspose.slides/timing/
---
**وراثت:**
java.lang.Object

**تمام رابط‌های پیاده‌سازی شده:**
[com.aspose.slides.ITiming](../../com.aspose.slides/itiming), com.aspose.slides.IDOMObject
```
public class Timing implements ITiming, IDOMObject
```

نمایانگر زمان‌بندی انیمیشن.
## متدها

| متد | توضیح |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | درصد مدت زمان رفتار شتاب‌دار اثر را توصیف می‌کند. |
| [setAccelerate(float value)](#setAccelerate-float-) | درصد مدت زمان رفتار شتاب‌دار اثر را توصیف می‌کند. |
| [getDecelerate()](#getDecelerate--) | درصد مدت زمان رفتار کاهش سرعت اثر را توصیف می‌کند. |
| [setDecelerate(float value)](#setDecelerate-float-) | درصد مدت زمان رفتار کاهش سرعت اثر را توصیف می‌کند. |
| [getAutoReverse()](#getAutoReverse--) | توضیح می‌دهد که آیا پس از پخش انیمیشن در جهت جلو، به‌صورت خودکار به‌صورت معکوس پخش شود. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | توضیح می‌دهد که آیا پس از پخش انیمیشن در جهت جلو، به‌صورت خودکار به‌صورت معکوس پخش شود. |
| [getDuration()](#getDuration--) | مدت زمان اثر انیمیشن را توصیف می‌کند. |
| [setDuration(float value)](#setDuration-float-) | مدت زمان اثر انیمیشن را توصیف می‌کند. |
| [getRepeatCount()](#getRepeatCount--) | تعداد دفعات تکرار اثر را توصیف می‌کند. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | تعداد دفعات تکرار اثر را توصیف می‌کند. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | این ویژگی مشخص می‌کند که آیا اثر تا انتهای اسلاید تکرار شود. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | این ویژگی مشخص می‌کند که آیا اثر تا انتهای اسلاید تکرار شود. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | این ویژگی مشخص می‌کند که آیا اثر تا کلیک بعدی تکرار شود. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | این ویژگی مشخص می‌کند که آیا اثر تا کلیک بعدی تکرار شود. |
| [getRepeatDuration()](#getRepeatDuration--) | تعداد دفعات تکرار اثر را توصیف می‌کند. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | تعداد دفعات تکرار اثر را توصیف می‌کند. |
| [getRestart()](#getRestart--) | مشخص می‌کند آیا اثر پس از کامل شدن مجدداً آغاز شود. |
| [setRestart(int value)](#setRestart-int-) | مشخص می‌کند آیا اثر پس از کامل شدن مجدداً آغاز شود. |
| [getRewind()](#getRewind--) | این ویژگی مشخص می‌کند که آیا اثر پس از پایان پخش به‌حالت اولیه بازگردد. |
| [setRewind(boolean value)](#setRewind-boolean-) | این ویژگی مشخص می‌کند که آیا اثر پس از پایان پخش به‌حالت اولیه بازگردد. |
| [getSpeed()](#getSpeed--) | درصدی که زمان‌بندی با آن تسریع (یا کاهش) می‌یابد را مشخص می‌کند. |
| [setSpeed(float value)](#setSpeed-float-) | درصدی که زمان‌بندی با آن تسریع (یا کاهش) می‌یابد را مشخص می‌کند. |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | زمان تاخیر پس از تحریک را توصیف می‌کند. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | زمان تاخیر پس از تحریک را توصیف می‌کند. |
| [getTriggerType()](#getTriggerType--) | نوع محرک را توصیف می‌کند. |
| [setTriggerType(int value)](#setTriggerType-int-) | نوع محرک را توصیف می‌کند. |
| [getParent_Immediate()](#getParent-Immediate--) |  |
### getAccelerate() {#getAccelerate--}
```
public final float getAccelerate()
```

درصد مدت زمان رفتار شتاب‌دار اثر را توصیف می‌کند. قابل خواندن/قابل نوشتن float.

**بازگشت:**
float
### setAccelerate(float value) {#setAccelerate-float-}
```
public final void setAccelerate(float value)
```

درصد مدت زمان رفتار شتاب‌دار اثر را توصیف می‌کند. قابل خواندن/قابل نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public final float getDecelerate()
```

درصد مدت زمان رفتار کاهش سرعت اثر را توصیف می‌کند. قابل خواندن/قابل نوشتن float.

**بازگشت:**
float
### setDecelerate(float value) {#setDecelerate-float-}
```
public final void setDecelerate(float value)
```

درصد مدت زمان رفتار کاهش سرعت اثر را توصیف می‌کند. قابل خواندن/قابل نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public final boolean getAutoReverse()
```

توضیح می‌دهد که آیا پس از پخش انیمیشن در جهت جلو، به‌صورت خودکار به‌صورت معکوس پخش شود. قابل خواندن/قابل نوشتن boolean.

**بازگشت:**
boolean
### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public final void setAutoReverse(boolean value)
```

توضیح می‌دهد که آیا پس از پخش انیمیشن در جهت جلو، به‌صورت خودکار به‌صورت معکوس پخش شود. قابل خواندن/قابل نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public final float getDuration()
```

مدت زمان اثر انیمیشن را توصیف می‌کند. قابل خواندن/قابل نوشتن float.

**بازگشت:**
float
### setDuration(float value) {#setDuration-float-}
```
public final void setDuration(float value)
```

مدت زمان اثر انیمیشن را توصیف می‌کند. قابل خواندن/قابل نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public final float getRepeatCount()
```

تعداد دفعات تکرار اثر را توصیف می‌کند. قابل خواندن/قابل نوشتن float.

**بازگشت:**
float
### setRepeatCount(float value) {#setRepeatCount-float-}
```
public final void setRepeatCount(float value)
```

تعداد دفعات تکرار اثر را توصیف می‌کند. قابل خواندن/قابل نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public final boolean getRepeatUntilEndSlide()
```

این ویژگی مشخص می‌کند که آیا اثر تا انتهای اسلاید تکرار شود. قابل خواندن/قابل نوشتن boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت دنباله اثرات برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین اثر از دنباله اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // تغییر زمان‌بندی/تکرار اثر به "تا انتهای اسلاید"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**بازگشت:**
boolean
### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public final void setRepeatUntilEndSlide(boolean value)
```

این ویژگی مشخص می‌کند که آیا اثر تا انتهای اسلاید تکرار شود. قابل خواندن/قابل نوشتن boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت دنباله اثرات برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین اثر از دنباله اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // تغییر زمان‌بندی/تکرار اثر به "تا انتهای اسلاید"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public final boolean getRepeatUntilNextClick()
```

این ویژگی مشخص می‌کند که آیا اثر تا کلیک بعدی تکرار شود. قابل خواندن/قابل نوشتن boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت دنباله اثرات برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین اثر از دنباله اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // تغییر زمان‌بندی/تکرار اثر به "تا کلیک بعدی"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**بازگشت:**
boolean
### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public final void setRepeatUntilNextClick(boolean value)
```

این ویژگی مشخص می‌کند که آیا اثر تا کلیک بعدی تکرار شود. قابل خواندن/قابل نوشتن boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت دنباله اثرات برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین اثر از دنباله اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // تغییر زمان‌بندی/تکرار اثر به "تا کلیک بعدی"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public final float getRepeatDuration()
```

تعداد دفعات تکرار اثر را توصیف می‌کند. قابل خواندن/قابل نوشتن float.

**بازگشت:**
float
### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public final void setRepeatDuration(float value)
```

تعداد دفعات تکرار اثر را توصیف می‌کند. قابل خواندن/قابل نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public final int getRestart()
```

مشخص می‌کند آیا اثر پس از کامل شدن مجدداً آغاز شود. قابل خواندن/قابل نوشتن [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**بازگشت:**
int
### setRestart(int value) {#setRestart-int-}
```
public final void setRestart(int value)
```

مشخص می‌کند آیا اثر پس از کامل شدن مجدداً آغاز شود. قابل خواندن/قابل نوشتن [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public final boolean getRewind()
```

این ویژگی مشخص می‌کند که آیا اثر پس از پایان پخش به‌حالت اولیه بازگردد. قابل خواندن/قابل نوشتن boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت دنباله اثرات برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین اثر از دنباله اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // فعال‌سازی زمان‌بندی/بازپخش اثر.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**بازگشت:**
boolean
### setRewind(boolean value) {#setRewind-boolean-}
```
public final void setRewind(boolean value)
```

این ویژگی مشخص می‌کند که آیا اثر پس از پایان پخش به‌حالت اولیه بازگردد. قابل خواندن/قابل نوشتن boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت دنباله اثرات برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین اثر از دنباله اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // فعال‌سازی زمان‌بندی/بازپخش اثر.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getSpeed() {#getSpeed--}
```
public final float getSpeed()
```

درصدی که زمان‌بندی با آن تسریع (یا کاهش) می‌یابد را مشخص می‌کند. قابل خواندن/قابل نوشتن float.

**بازگشت:**
float
### setSpeed(float value) {#setSpeed-float-}
```
public final void setSpeed(float value)
```

درصدی که زمان‌بندی با آن تسریع (یا کاهش) می‌یابد را مشخص می‌کند. قابل خواندن/قابل نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public final float getTriggerDelayTime()
```

زمان تاخیر پس از تحریک را توصیف می‌کند. قابل خواندن/قابل نوشتن float.

**بازگشت:**
float
### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public final void setTriggerDelayTime(float value)
```

زمان تاخیر پس از تحریک را توصیف می‌کند. قابل خواندن/قابل نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public final int getTriggerIntype()
```

نوع محرک را توصیف می‌کند. قابل خواندن/قابل نوشتن [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**بازگشت:**
int
### setTriggerType(int value) {#setTriggerType-int-}
```
public final void setTriggerType(int value)
```

نوع محرک را توصیف می‌کند. قابل خواندن/قابل نوشتن [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

شیء Parent_Immediate را باز می‌گرداند. فقط خواندنی IDOMObject.

**بازگشت:**
com.aspose.slides.IDOMObject