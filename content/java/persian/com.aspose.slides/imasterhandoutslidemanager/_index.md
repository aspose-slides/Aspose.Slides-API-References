---
title: IMasterHandoutSlideManager
second_title: Aspose.Slides برای Java API Reference
description: مدیر اسلاید دست‌نوشته اصلی.
type: docs
url: /fa/com.aspose.slides/imasterhandoutslidemanager/
---```
public interface IMasterHandoutSlideManager
```

مدیر اسلاید دست‌نوشته اصلی.
## متدها

| متد | توضیح |
| --- | --- |
| [getMasterHandoutSlide()](#getMasterHandoutSlide--) | اگر یک master برای تمام اسلایدهای یادداشت این ارائه وجود داشته باشد، آن را برمی‌گرداند، در غیر این صورت مقدار null را برمی‌گرداند. |
| [setDefaultMasterHandoutSlide()](#setDefaultMasterHandoutSlide--) | master پیش‌فرض اسلاید توزیع را به اسلاید توزیع مرتبط تنظیم می‌کند. |
| [removeMasterHandoutSlide()](#removeMasterHandoutSlide--) | master اسلاید توزیع را حذف می‌کند. |
### getMasterHandoutSlide() {#getMasterHandoutSlide--}
```
public abstract IMasterHandoutSlide getMasterHandoutSlide()
```


اگر یک master برای تمام اسلایدهای یادداشت این ارائه وجود داشته باشد، آن را برمی‌گرداند، در غیر این صورت مقدار null را برمی‌گرداند. فقط-خواندنی [IMasterHandoutSlide](../../com.aspose.slides/imasterhandoutslide).

**بازگشت:**
[IMasterHandoutSlide](../../com.aspose.slides/imasterhandoutslide)
### setDefaultMasterHandoutSlide() {#setDefaultMasterHandoutSlide--}
```
public abstract IMasterHandoutSlide setDefaultMasterHandoutSlide()
```


master پیش‌فرض اسلاید توزیع را به اسلاید توزیع مرتبط تنظیم می‌کند.

**بازگشت:**
[IMasterHandoutSlide](../../com.aspose.slides/imasterhandoutslide) - master اسلاید توزیع [IMasterHandoutSlide](../../com.aspose.slides/imasterhandoutslide)
### removeMasterHandoutSlide() {#removeMasterHandoutSlide--}
```
public abstract void removeMasterHandoutSlide()
```


master اسلاید توزیع را حذف می‌کند.