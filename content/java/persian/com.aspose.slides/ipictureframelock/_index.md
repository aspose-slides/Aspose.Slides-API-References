---
title: IPictureFrameLock
second_title: Aspose.Slides برای مرجع API جاوا
description: مشخص می‌کند کدام عملیات‌ها در PictureFrameEx والد غیرفعال هستند.
type: docs
url: /fa/com.aspose.slides/ipictureframelock/
---
**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IBaseShapeLock](../../com.aspose.slides/ibaseshapelock)
```
public interface IPictureFrameLock extends IBaseShapeLock
```

عملیات‌هایی که در شیء والد PictureFrameEx غیرفعال هستند را تعیین می‌کند.
## متدها

| متد | توضیح |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | مشخص می‌کند آیا افزودن این شکل به یک گروه ممنوع است یا نه. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | مشخص می‌کند آیا افزودن این شکل به یک گروه ممنوع است یا نه. |
| [getSelectLocked()](#getSelectLocked--) | مشخص می‌کند آیا انتخاب این شکل ممنوع است یا نه. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | مشخص می‌کند آیا انتخاب این شکل ممنوع است یا نه. |
| [getRotationLocked()](#getRotationLocked--) | مشخص می‌کند آیا تغییر زاویه چرخش این شکل ممنوع است یا نه. |
| [setRotationLocked(boolean value)](#setRotationLocked-boolean-) | مشخص می‌کند آیا تغییر زاویه چرخش این شکل ممنوع است یا نه. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | مشخص می‌کند آیا شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند یا نه. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | مشخص می‌کند آیا شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند یا نه. |
| [getPositionLocked()](#getPositionLocked--) | مشخص می‌کند آیا حرکت این شکل ممنوع است یا نه. |
| [setPositionLocked(boolean value)](#setPositionLocked-boolean-) | مشخص می‌کند آیا حرکت این شکل ممنوع است یا نه. |
| [getSizeLocked()](#getSizeLocked--) | مشخص می‌کند آیا تغییر اندازه این شکل ممنوع است یا نه. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | مشخص می‌کند آیا تغییر اندازه این شکل ممنوع است یا نه. |
| [getEditPointsLocked()](#getEditPointsLocked--) | مشخص می‌کند آیا تغییر مستقیم کانتور این شکل ممنوع است یا نه. |
| [setEditPointsLocked(boolean value)](#setEditPointsLocked-boolean-) | مشخص می‌کند آیا تغییر مستقیم کانتور این شکل ممنوع است یا نه. |
| [getAdjustHandlesLocked()](#getAdjustHandlesLocked--) | مشخص می‌کند آیا تغییر مقادیر تنظیم ممنوع است یا نه. |
| [setAdjustHandlesLocked(boolean value)](#setAdjustHandlesLocked-boolean-) | مشخص می‌کند آیا تغییر مقادیر تنظیم ممنوع است یا نه. |
| [getArrowheadsLocked()](#getArrowheadsLocked--) | مشخص می‌کند آیا تغییر سرهای پیکان ممنوع است یا نه. |
| [setArrowheadsLocked(boolean value)](#setArrowheadsLocked-boolean-) | مشخص می‌کند آیا تغییر سرهای پیکان ممنوع است یا نه. |
| [getShapeTypeLocked()](#getShapeTypeLocked--) | مشخص می‌کند آیا تغییر نوع شکل ممنوع است یا نه. |
| [setShapeTypeLocked(boolean value)](#setShapeTypeLocked-boolean-) | مشخص می‌کند آیا تغییر نوع شکل ممنوع است یا نه. |
| [getCropLocked()](#getCropLocked--) | مشخص می‌کند آیا برش تصویر ممنوع است یا نه. |
| [setCropLocked(boolean value)](#setCropLocked-boolean-) | مشخص می‌کند آیا برش تصویر ممنوع است یا نه. |
### getGroupingLocked() {#getGroupingLocked--}
```
public abstract boolean getGroupingLocked()
```

مشخص می‌کند آیا افزودن این شکل به یک گروه ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public abstract void setGroupingLocked(boolean value)
```

مشخص می‌کند آیا افزودن این شکل به یک گروه ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getSelectLocked() {#getSelectLocked--}
```
public abstract boolean getSelectLocked()
```

مشخص می‌کند آیا انتخاب این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public abstract void setSelectLocked(boolean value)
```

مشخص می‌کند آیا انتخاب این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getRotationLocked() {#getRotationLocked--}
```
public abstract boolean getRotationLocked()
```

مشخص می‌کند آیا تغییر زاویه چرخش این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setRotationLocked(boolean value) {#setRotationLocked-boolean-}
```
public abstract void setRotationLocked(boolean value)
```

مشخص می‌کند آیا تغییر زاویه چرخش این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public abstract boolean getAspectRatioLocked()
```

مشخص می‌کند آیا شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public abstract void setAspectRatioLocked(boolean value)
```

مشخص می‌کند آیا شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getPositionLocked() {#getPositionLocked--}
```
public abstract boolean getPositionLocked()
```

مشخص می‌کند آیا حرکت این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setPositionLocked(boolean value) {#setPositionLocked-boolean-}
```
public abstract void setPositionLocked(boolean value)
```

مشخص می‌کند آیا حرکت این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getSizeLocked() {#getSizeLocked--}
```
public abstract boolean getSizeLocked()
```

مشخص می‌کند آیا تغییر اندازه این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public abstract void setSizeLocked(boolean value)
```

مشخص می‌کند آیا تغییر اندازه این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getEditPointsLocked() {#getEditPointsLocked--}
```
public abstract boolean getEditPointsLocked()
```

مشخص می‌کند آیا تغییر مستقیم کانتور این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setEditPointsLocked(boolean value) {#setEditPointsLocked-boolean-}
```
public abstract void setEditPointsLocked(boolean value)
```

مشخص می‌کند آیا تغییر مستقیم کانتور این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getAdjustHandlesLocked() {#getAdjustHandlesLocked--}
```
public abstract boolean getAdjustHandlesLocked()
```

مشخص می‌کند آیا تغییر مقادیر تنظیم ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setAdjustHandlesLocked(boolean value) {#setAdjustHandlesLocked-boolean-}
```
public abstract void setAdjustHandlesLocked(boolean value)
```

مشخص می‌کند آیا تغییر مقادیر تنظیم ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getArrowheadsLocked() {#getArrowheadsLocked--}
```
public abstract boolean getArrowheadsLocked()
```

مشخص می‌کند آیا تغییر سرهای پیکان ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setArrowheadsLocked(boolean value) {#setArrowheadsLocked-boolean-}
```
public abstract void setArrowheadsLocked(boolean value)
```

مشخص می‌کند آیا تغییر سرهای پیکان ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getShapeTypeLocked() {#getShapeTypeLocked--}
```
public abstract boolean getShapeTypeLocked()
```

مشخص می‌کند آیا تغییر نوع شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setShapeTypeLocked(boolean value) {#setShapeTypeLocked-boolean-}
```
public abstract void setShapeTypeLocked(boolean value)
```

مشخص می‌کند آیا تغییر نوع شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getCropLocked() {#getCropLocked--}
```
public abstract boolean getCropLocked()
```

مشخص می‌کند آیا برش تصویر ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean
### setCropLocked(boolean value) {#setCropLocked-boolean-}
```
public abstract void setCropLocked(boolean value)
```

مشخص می‌کند آیا برش تصویر ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |