---
title: NotesSlide
second_title: Aspose.Slides برای مرجع API جاوا
description: یک اسلاید یادداشت را در یک ارائه نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/notesslide/
---
**Inheritance:**  
ارث-بری: java.lang.Object, [com.aspose.slides.BaseSlide](../../com.aspose.slides/baseslide)

**All Implemented Interfaces:**  
تمام رابط‌های پیاده‌سازی‌شده:  
[com.aspose.slides.INotesSlide](../../com.aspose.slides/inotesslide)  
```
public class NotesSlide extends BaseSlide implements INotesSlide
```

نمایش اسلاید یادداشت‌ها در یک ارائه.
## Methods

| Method | Description |
| --- | --- |
| [getHeaderFooterManager()](#getHeaderFooterManager--) | مدیر HeaderFooter اسلاید یادداشت‌ها را باز می‌گرداند. |
| [getNotesTextFrame()](#getNotesTextFrame--) | اگر موجود باشد، یک TextFrame حاوی متن یادداشت‌ها را باز می‌گرداند. |
| [getThemeManager()](#getThemeManager--) | مدیر theme manager حاکم را باز می‌گرداند. |
| [getParentSlide()](#getParentSlide--) | اسلاید والد را باز می‌گرداند. |
| [getShowMasterShapes()](#getShowMasterShapes--) | مشخص می‌کند آیا اشکال روی اسلاید اصلی باید روی اسلایدها نمایش داده شوند یا نه. |
| [setShowMasterShapes(boolean value)](#setShowMasterShapes-boolean-) | مشخص می‌کند آیا اشکال روی اسلاید اصلی باید روی اسلایدها نمایش داده شوند یا نه. |

### getHeaderFooterManager() {#getHeaderFooterManager--}
```
public final INotesSlideHeaderFooterManager getHeaderFooterManager()
```

مدیر HeaderFooter اسلاید یادداشت‌ها را باز می‌گرداند. فقط خواندنی [INotesSlideHeaderFooterManager](../../com.aspose.slides/inotesslideheaderfootermanager).

**Returns:**  
[INotesSlideHeaderFooterManager](../../com.aspose.slides/inotesslideheaderfootermanager)

### getNotesTextFrame() {#getNotesTextFrame--}
```
public final ITextFrame getNotesTextFrame()
```

اگر موجود باشد، یک TextFrame حاوی متن یادداشت‌ها را باز می‌گرداند. فقط خواندنی [ITextFrame](../../com.aspose.slides/itextframe).

**Returns:**  
[ITextFrame](../../com.aspose.slides/itextframe)

### getThemeManager() {#getThemeManager--}
```
public final IOverrideThemeManager getThemeManager()
```

مدیر theme manager حاکم را باز می‌گرداند. فقط خواندنی [IOverrideThemeManager](../../com.aspose.slides/ioverridethememanager).

**Returns:**  
[IOverrideThemeManager](../../com.aspose.slides/ioverridethememanager)

### getParentSlide() {#getParentSlide--}
```
public final ISlide getParentSlide()
```

اسلاید والد را باز می‌گرداند. فقط خواندنی [ISlide](../../com.aspose.slides/islide).

**Returns:**  
[ISlide](../../com.aspose.slides/islide)

### getShowMasterShapes() {#getShowMasterShapes--}
```
public boolean getShowMasterShapes()
```

مشخص می‌کند آیا اشکال روی اسلاید اصلی باید روی اسلایدها نمایش داده شوند یا نه. قابل خواندن/قابل نوشتن boolean.

**Returns:**  
boolean

### setShowMasterShapes(boolean value) {#setShowMasterShapes-boolean-}
```
public void setShowMasterShapes(boolean value)
```

مشخص می‌کند آیا اشکال روی اسلاید اصلی باید روی اسلایدها نمایش داده شوند یا نه. قابل خواندن/قابل نوشتن boolean.

**Parameters:**  
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |