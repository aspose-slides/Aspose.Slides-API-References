---
title: ITextHighlightingOptions
second_title: Aspose.Slides برای Java مرجع API
description: گزینه‌هایی را که می‌توان برای برجسته‌سازی متن در فریم متن استفاده کرد، نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/itexthighlightingoptions/
---```
public interface ITextHighlightingOptions
```

گزینه‌هایی را که می‌توان برای برجسته‌سازی متن در فریم متن استفاده کرد، نشان می‌دهد.
## متدها

| متد | توضیح |
| --- | --- |
| [getCaseSensitive()](#getCaseSensitive--) | اگر true تنظیم شود، جستجوی حساس به حروف بزرگ انجام می‌شود؛ در غیر این صورت false. |
| [setCaseSensitive(boolean value)](#setCaseSensitive-boolean-) | اگر true تنظیم شود، جستجوی حساس به حروف بزرگ انجام می‌شود؛ در غیر این صورت false. |
| [getWholeWordsOnly()](#getWholeWordsOnly--) | اگر true تنظیم شود، تنها کلمات کامل مطابقت داده می‌شوند؛ در غیر این صورت false. |
| [setWholeWordsOnly(boolean value)](#setWholeWordsOnly-boolean-) | اگر true تنظیم شود، تنها کلمات کامل مطابقت داده می‌شوند؛ در غیر این صورت false. |
### getCaseSensitive() {#getCaseSensitive--}
```
public abstract boolean getCaseSensitive()
```

اگر true تنظیم شود، جستجوی حساس به حروف بزرگ انجام می‌شود؛ در غیر این صورت false. خواند و نوشتن boolean.

**بازگشت:**
boolean
### setCaseSensitive(boolean value) {#setCaseSensitive-boolean-}
```
public abstract void setCaseSensitive(boolean value)
```

اگر true تنظیم شود، جستجوی حساس به حروف بزرگ انجام می‌شود؛ در غیر این صورت false. خواند و نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getWholeWordsOnly() {#getWholeWordsOnly--}
```
public abstract boolean getWholeWordsOnly()
```

اگر true تنظیم شود، تنها کلمات کامل مطابقت داده می‌شوند؛ در غیر این صورت false. خواند و نوشتن boolean.

**بازگشت:**
boolean
### setWholeWordsOnly(boolean value) {#setWholeWordsOnly-boolean-}
```
public abstract void setWholeWordsOnly(boolean value)
```

اگر true تنظیم شود، تنها کلمات کامل مطابقت داده می‌شوند؛ در غیر این صورت false. خواند و نوشتن boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |