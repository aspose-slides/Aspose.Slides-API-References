---
title: INormalViewRestoredProperties
second_title: Aspose.Slides برای Java مرجع API
description: اندازه‌گیری ناحیه اسلاید (عرض زمانی که یک فرزند RestoredTop باشد، ارتفاع زمانی که یک فرزند RestoredLeft باشد) در نمای نرمال، هنگامی که ناحیه دارای اندازه متغیر بازگردانده شده باشد (نه حداقل شده و نه بیشینه شده).
type: docs
url: /fa/com.aspose.slides/inormalviewrestoredproperties/
---```
public interface INormalViewRestoredProperties
```

اندازه‌گیری ناحیه اسلاید (عرض زمانی که یک فرزند RestoredTop باشد، ارتفاع زمانی که یک فرزند RestoredLeft باشد) در نمای نرمال، هنگامی که ناحیه دارای اندازه متغیر بازگردانده شده باشد (نه حداقل شده و نه بیشینه شده).

## Methods

| متد | توضیح |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | اندازه ناحیه اسلاید (عرض زمانی که یک فرزند RestoredTop باشد، ارتفاع زمانی که یک فرزند RestoredLeft باشد) را مشخص می‌کند. |
| [setDimensionSize(float value)](#setDimensionSize-float-) | اندازه ناحیه اسلاید (عرض زمانی که یک فرزند RestoredTop باشد، ارتفاع زمانی که یک فرزند RestoredLeft باشد) را مشخص می‌کند. |
| [getAutoAdjust()](#getAutoAdjust--) | مشخص می‌کند آیا اندازهٔ ناحیهٔ محتوای کناری باید برای اندازهٔ جدید هنگام تغییر اندازهٔ پنجرهٔ حاوی نما در برنامه جبران شود. قابل خواندن/قابل نوشتن boolean. |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | مشخص می‌کند آیا اندازهٔ ناحیهٔ محتوای کناری باید برای اندازهٔ جدید هنگام تغییر اندازهٔ پنجرهٔ حاوی نما در برنامه جبران شود. قابل خواندن/قابل نوشتن boolean. |
### getDimensionSize() {#getDimensionSize--}
```
public abstract float getDimensionSize()
```

اندازه ناحیه اسلاید (عرض زمانی که یک فرزند RestoredTop باشد، ارتفاع زمانی که یک فرزند RestoredLeft باشد) را مشخص می‌کند. قابل خواندن/قابل نوشتن float.

**Returns:**
float
### setDimensionSize(float value) {#setDimensionSize-float-}
```
public abstract void setDimensionSize(float value)
```

اندازه ناحیه اسلاید (عرض زمانی که یک فرزند RestoredTop باشد، ارتفاع زمانی که یک فرزند RestoredLeft باشد) را مشخص می‌کند. قابل خواندن/قابل نوشتن float.

**Parameters:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public abstract boolean getAutoAdjust()
```

مشخص می‌کند آیا اندازهٔ ناحیهٔ محتوای کناری باید برای اندازهٔ جدید هنگام تغییر اندازهٔ پنجرهٔ حاوی نما در برنامه جبران شود. قابل خواندن/قابل نوشتن boolean.

**Returns:**
boolean
### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public abstract void setAutoAdjust(boolean value)
```

مشخص می‌کند آیا اندازهٔ ناحیهٔ محتوای کناری باید برای اندازهٔ جدید هنگام تغییر اندازهٔ پنجرهٔ حاوی نما در برنامه جبران شود. قابل خواندن/قابل نوشتن boolean.

**Parameters:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |