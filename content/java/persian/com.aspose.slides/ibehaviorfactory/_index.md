---
title: IBehaviorFactory
second_title: Aspose.Slides for Java API Reference
description: Allows to create animation effects
type: docs
url: /fa/com.aspose.slides/ibehaviorfactory/
---```
public interface IBehaviorFactory
```

اجازه می‌دهد تا افکت‌های انیمیشن ایجاد کند

--------------------

برای سازگاری با COM.
## متدها

| متد | توضیح |
| --- | --- |
| [createColorEffect()](#createColorEffect--) | یک افکت رنگی ایجاد می‌کند. |
| [createCommandEffect()](#createCommandEffect--) | یک افکت دستور ایجاد می‌کند. |
| [createFilterEffect()](#createFilterEffect--) | یک افکت فیلتر ایجاد می‌کند. |
| [createMotionEffect()](#createMotionEffect--) | یک افکت حرکت ایجاد می‌کند. |
| [createPropertyEffect()](#createPropertyEffect--) | یک افکت ویژگی ایجاد می‌کند. |
| [createRotationEffect()](#createRotationEffect--) | یک افکت چرخش ایجاد می‌کند. |
| [createScaleEffect()](#createScaleEffect--) | یک افکت مقیاس ایجاد می‌کند. |
| [createSetEffect()](#createSetEffect--) | یک افکت تنظیم ایجاد می‌کند. |
### createColorEffect() {#createColorEffect--}
```
public abstract IColorEffect createColorEffect()
```


یک افکت رنگی ایجاد می‌کند.

**بازگشت:**
[IColorEffect](../../com.aspose.slides/icoloreffect) - افکت رنگی.
### createCommandEffect() {#createCommandEffect--}
```
public abstract ICommandEffect createCommandEffect()
```


یک افکت دستور ایجاد می‌کند.

**بازگشت:**
[ICommandEffect](../../com.aspose.slides/icommandeffect) - افکت دستور.
### createFilterEffect() {#createFilterEffect--}
```
public abstract IFilterEffect createFilterEffect()
```


یک افکت فیلتر ایجاد می‌کند.

**بازگشت:**
[IFilterEffect](../../com.aspose.slides/ifiltereffect) - افکت فیلتر.
### createMotionEffect() {#createMotionEffect--}
```
public abstract IMotionEffect createMotionEffect()
```


یک افکت حرکت ایجاد می‌کند.

**بازگشت:**
[IMotionEffect](../../com.aspose.slides/imotioneffect) - افکت حرکت.
### createPropertyEffect() {#createPropertyEffect--}
```
public abstract IPropertyEffect createPropertyEffect()
```


یک افکت ویژگی ایجاد می‌کند.

**بازگشت:**
[IPropertyEffect](../../com.aspose.slides/ipropertyeffect) - افکت ویژگی.
### createRotationEffect() {#createRotationEffect--}
```
public abstract IRotationEffect createRotationEffect()
```


یک افکت چرخش ایجاد می‌کند.

**بازگشت:**
[IRotationEffect](../../com.aspose.slides/irotationeffect) - افکت چرخش.
### createScaleEffect() {#createScaleEffect--}
```
public abstract IScaleEffect createScaleEffect()
```


یک افکت مقیاس ایجاد می‌کند.

**بازگشت:**
[IScaleEffect](../../com.aspose.slides/iscaleeffect) - افکت مقیاس.
### createSetEffect() {#createSetEffect--}
```
public abstract ISetEffect createSetEffect()
```


یک افکت تنظیم ایجاد می‌کند.

**بازگشت:**
[ISetEffect](../../com.aspose.slides/iseteffect) - افکت تنظیم.