---
title: IAutoShapeLock
second_title: Aspose.Slides برای مرجع API جاوا
description: تعیین می‌کند کدام عملیات‌ها بر روی AutoshapeEx والد غیرفعال هستند.
type: docs
url: /fa/com.aspose.slides/iautoshapelock/
---
**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IBaseShapeLock](../../com.aspose.slides/ibaseshapelock)
```
public interface IAutoShapeLock extends IBaseShapeLock
```

مشخص می‌کند کدام عملیات‌ها بر روی AutoshapeEx والد غیرفعال هستند.
## متدها

| متد | توضیح |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | تعیین می‌کند آیا افزودن این شکل به یک گروه ممنوع است. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | تعیین می‌کند آیا افزودن این شکل به یک گروه ممنوع است. |
| [getSelectLocked()](#getSelectLocked--) | تعیین می‌کند آیا انتخاب این شکل ممنوع است. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | تعیین می‌کند آیا انتخاب این شکل ممنوع است. |
| [getRotateLocked()](#getRotateLocked--) | تعیین می‌کند آیا تغییر زاویه چرخش این شکل ممنوع است. |
| [setRotateLocked(boolean value)](#setRotateLocked-boolean-) | تعیین می‌کند آیا تغییر زاویه چرخش این شکل ممنوع است. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | تعیین می‌کند آیا این شکل برای حفظ نسبت ابعاد هنگام تغییر اندازه لازم است. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | تعیین می‌کند آیا این شکل برای حفظ نسبت ابعاد هنگام تغییر اندازه لازم است. |
| [getPositionLocked()](#getPositionLocked--) | تعیین می‌کند آیا جابجایی این شکل ممنوع است. |
| [setPositionLocked(boolean value)](#setPositionLocked-boolean-) | تعیین می‌کند آیا جابجایی این شکل ممنوع است. |
| [getSizeLocked()](#getSizeLocked--) | تعیین می‌کند آیا تغییر اندازه این شکل ممنوع است. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | تعیین می‌کند آیا تغییر اندازه این شکل ممنوع است. |
| [getEditPointsLocked()](#getEditPointsLocked--) | تعیین می‌کند آیا تغییر مستقیم شکل خطوط این شکل ممنوع است. |
| [setEditPointsLocked(boolean value)](#setEditPointsLocked-boolean-) | تعیین می‌کند آیا تغییر مستقیم شکل خطوط این شکل ممنوع است. |
| [getAdjustHandlesLocked()](#getAdjustHandlesLocked--) | تعیین می‌کند آیا تغییر مقادیر تنظیمات ممنوع است. |
| [setAdjustHandlesLocked(boolean value)](#setAdjustHandlesLocked-boolean-) | تعیین می‌کند آیا تغییر مقادیر تنظیمات ممنوع است. |
| [getArrowheadsLocked()](#getArrowheadsLocked--) | تعیین می‌کند آیا تغییر سرهای پیکان‌ها ممنوع است. |
| [setArrowheadsLocked(boolean value)](#setArrowheadsLocked-boolean-) | تعیین می‌کند آیا تغییر سرهای پیکان‌ها ممنوع است. |
| [getShapeTypeLocked()](#getShapeTypeLocked--) | تعیین می‌کند آیا تغییر نوع شکل ممنوع است. |
| [setShapeTypeLocked(boolean value)](#setShapeTypeLocked-boolean-) | تعیین می‌کند آیا تغییر نوع شکل ممنوع است. |
| [getTextLocked()](#getTextLocked--) | تعیین می‌کند آیا ویرایش متن ممنوع است. |
| [setTextLocked(boolean value)](#setTextLocked-boolean-) | تعیین می‌کند آیا ویرایش متن ممنوع است. |
### getGroupingLocked() {#getGroupingLocked--}
```
public abstract boolean getGroupingLocked()
```

تعیین می‌کند آیا افزودن این شکل به یک گروه ممنوع است. بولی خواندن/قابل نوشتن.

**بازگرداندن:**
boolean
### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public abstract void setGroupingLocked(boolean value)
```

تعیین می‌کند آیا افزودن این شکل به یک گروه ممنوع است. بولی خواندن/قابل نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |
### getSelectLocked() {#getSelectLocked--}
```
public abstract boolean getSelectLocked()
```

تعیین می‌کند آیا انتخاب این شکل ممنوع است. بولی خواندن/قابل نوشتن.

**بازگرداندن:**
boolean
### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public abstract void setSelectLocked(boolean value)
```

تعیین می‌کند آیا انتخاب این شکل ممنوع است. بولی خواندن/قابل نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |
### getRotateLocked() {#getRotateLocked--}
```
public abstract boolean getRotateLocked()
```

تعیین می‌کند آیا تغییر زاویه چرخش این شکل ممنوع است. بولی خواندن/قابل نوشتن.

**بازگرداندن:**
boolean
### setRotateLocked(boolean value) {#setRotateLocked-boolean-}
```
public abstract void setRotateLocked(boolean value)
```

تعیین می‌کند آیا تغییر زاویه چرخش این شکل ممنوع است. بولی خواندن/قابل نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |
### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public abstract boolean getAspectRatioLocked()
```

تعیین می‌کند آیا این شکل برای حفظ نسبت ابعاد هنگام تغییر اندازه لازم است. بولی خواندن/قابل نوشتن.

**بازگرداندن:**
boolean
### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public abstract void setAspectRatioLocked(boolean value)
```

تعیین می‌کند آیا این شکل برای حفظ نسبت ابعاد هنگام تغییر اندازه لازم است. بولی خواندن/قابل نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |
### getPositionLocked() {#getPositionLocked--}
```
public abstract boolean getPositionLocked()
```

تعیین می‌کند آیا جابجایی این شکل ممنوع است. بولی خواندن/قابل نوشتن.

**بازگرداندن:**
boolean
### setPositionLocked(boolean value) {#setPositionLocked-boolean-}
```
public abstract void setPositionLocked(boolean value)
```

تعیین می‌کند آیا جابجایی این شکل ممنوع است. بولی خواندن/قابل نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |
### getSizeLocked() {#getSizeLocked--}
```
public abstract boolean getSizeLocked()
```

تعیین می‌کند آیا تغییر اندازه این شکل ممنوع است. بولی خواندن/قابل نوشتن.

**بازگرداندن:**
boolean
### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public abstract void setSizeLocked(boolean value)
```

تعیین می‌کند آیا تغییر اندازه این شکل ممنوع است. بولی خواندن/قابل نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |
### getEditPointsLocked() {#getEditPointsLocked--}
```
public abstract boolean getEditPointsLocked()
```

تعیین می‌کند آیا تغییر مستقیم شکل خطوط این شکل ممنوع است. بولی خواندن/قابل نوشتن.

**بازگرداندن:**
boolean
### setEditPointsLocked(boolean value) {#setEditPointsLocked-boolean-}
```
public abstract void setEditPointsLocked(boolean value)
```

تعیین می‌کند آیا تغییر مستقیم شکل خطوط این شکل ممنوع است. بولی خواندن/قابل نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |
### getAdjustHandlesLocked() {#getAdjustHandlesLocked--}
```
public abstract boolean getAdjustHandlesLocked()
```

تعیین می‌کند آیا تغییر مقادیر تنظیمات ممنوع است. بولی خواندن/قابل نوشتن.

**بازگرداندن:**
boolean
### setAdjustHandlesLocked(boolean value) {#setAdjustHandlesLocked-boolean-}
```
public abstract void setAdjustHandlesLocked(boolean value)
```

تعیین می‌کند آیا تغییر مقادیر تنظیمات ممنوع است. بولی خواندن/قابل نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |
### getArrowheadsLocked() {#getArrowheadsLocked--}
```
public abstract boolean getArrowheadsLocked()
```

تعیین می‌کند آیا تغییر سرهای پیکان‌ها ممنوع است. بولی خواندن/قابل نوشتن.

**بازگرداندن:**
boolean
### setArrowheadsLocked(boolean value) {#setArrowheadsLocked-boolean-}
```
public abstract void setArrowheadsLocked(boolean value)
```

تعیین می‌کند آیا تغییر سرهای پیکان‌ها ممنوع است. بولی خواندن/قابل نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |
### getShapeTypeLocked() {#getShapeTypeLocked--}
```
public abstract boolean getShapeTypeLocked()
```

تعیین می‌کند آیا تغییر نوع شکل ممنوع است. بولی خواندن/قابل نوشتن.

**بازگرداندن:**
boolean
### setShapeTypeLocked(boolean value) {#setShapeTypeLocked-boolean-}
```
public abstract void setShapeTypeLocked(boolean value)
```

تعیین می‌کند آیا تغییر نوع شکل ممنوع است. بولی خواندن/قابل نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |
### getTextLocked() {#getTextLocked--}
```
public abstract boolean getTextLocked()
```

تعیین می‌کند آیا ویرایش متن ممنوع است. بولی خواندن/قابل نوشتن.

**بازگرداندن:**
boolean
### setTextLocked(boolean value) {#setTextLocked-boolean-}
```
public abstract void setTextLocked(boolean value)
```

تعیین می‌کند آیا ویرایش متن ممنوع است. بولی خواندن/قابل نوشتن.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |