---
title: FormatScheme
second_title: مرجع API Aspose.Slides برای جاوا
description: قالب‌های تعریف‌شده توسط تم را برای اشکال ذخیره می‌کند.
type: docs
url: /fa/com.aspose.slides/formatscheme/
---
**ارث‌بری:**
java.lang.Object

**تمام واسط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IFormatScheme](../../com.aspose.slides/iformatscheme), com.aspose.slides.IDOMObject
```
public class FormatScheme implements IFormatScheme, IDOMObject
```

قالب‌های تعریف‌شده توسط تم را برای اشکال ذخیره می‌کند.
## متدها

| متد | توضیح |
| --- | --- |
| [getFillStyles()](#getFillStyles--) | یک مجموعه از سبک‌های پر کردن تعریف‌شده توسط تم را برمی‌گرداند. |
| [getLineStyles()](#getLineStyles--) | یک مجموعه از سبک‌های خط تعریف‌شده توسط تم را برمی‌گرداند. |
| [getEffectStyles()](#getEffectStyles--) | یک مجموعه از سبک‌های اثر تعریف‌شده توسط تم را برمی‌گرداند. |
| [getBackgroundFillStyles()](#getBackgroundFillStyles--) | یک مجموعه از سبک‌های پر کردن پس‌زمینه تعریف‌شده توسط تم را برمی‌گرداند. |
| [getSlide()](#getSlide--) | اسلاید Slide والد را برمی‌گرداند. |
| [getPresentation()](#getPresentation--) | ارائه Presentation والد را برمی‌گرداند. |
| [getParent_Immediate()](#getParent-Immediate--) |  |
### getFillStyles() {#getFillStyles--}
```
public final IFillFormatCollection getFillStyles()
```

یک مجموعه از سبک‌های پر کردن تعریف‌شده توسط تم را برمی‌گرداند. فقط‌خواندنی [IFillFormatCollection](../../com.aspose.slides/ifillformatcollection).

**بازمی‌گرداند:**
[IFillFormatCollection](../../com.aspose.slides/ifillformatcollection)
### getLineStyles() {#getLineStyles--}
```
public final ILineFormatCollection getLineStyles()
```

یک مجموعه از سبک‌های خط تعریف‌شده توسط تم را برمی‌گرداند. فقط‌خواندنی [ILineFormatCollection](../../com.aspose.slides/ilineformatcollection).

**بازمی‌گرداند:**
[ILineFormatCollection](../../com.aspose.slides/ilineformatcollection)
### getEffectStyles() {#getEffectStyles--}
```
public final IEffectStyleCollection getEffectStyles()
```

یک مجموعه از سبک‌های اثر تعریف‌شده توسط تم را برمی‌گرداند. فقط‌خواندنی [IEffectStyleCollection](../../com.aspose.slides/ieffectstylecollection).

**بازمی‌گرداند:**
[IEffectStyleCollection](../../com.aspose.slides/ieffectstylecollection)
### getBackgroundFillStyles() {#getBackgroundFillStyles--}
```
public final IFillFormatCollection getBackgroundFillStyles()
```

یک مجموعه از سبک‌های پر کردن پس‌زمینه تعریف‌شده توسط تم را برمی‌گرداند. فقط‌خواندنی [IFillFormatCollection](../../com.aspose.slides/ifillformatcollection).

**بازمی‌گرداند:**
[IFillFormatCollection](../../com.aspose.slides/ifillformatcollection)
### getSlide() {#getSlide--}
```
public final IBaseSlide getSlide()
```

اسلاید Slide والد را برمی‌گرداند. فقط‌خواندنی [IBaseSlide](../../com.aspose.slides/ibaseslide).

**بازمی‌گرداند:**
[IBaseSlide](../../com.aspose.slides/ibaseslide)
### getPresentation() {#getPresentation--}
```
public final IPresentation getPresentation()
```

ارائه Presentation والد را برمی‌گرداند. فقط‌خواندنی [IPresentation](../../com.aspose.slides/ipresentation).

**بازمی‌گرداند:**
[IPresentation](../../com.aspose.slides/ipresentation)
### getParent_Immediate() {#getParent-Immediate--}
```
public final IDOMObject getParent_Immediate()
```

شی Parent_Immediate را برمی‌گرداند. فقط‌خواندنی IDOMObject.

**بازمی‌گرداند:**
com.aspose.slides.IDOMObject