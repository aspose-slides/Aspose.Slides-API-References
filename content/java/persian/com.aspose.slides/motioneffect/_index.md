---
title: MotionEffect
second_title: مرجع API Aspose.Slides برای جاوا
description: نمایانگر رفتار افکت حرکت.
type: docs
url: /fa/com.aspose.slides/motioneffect/
---
**ارث بری:**
java.lang.Object, [com.aspose.slides.Behavior](../../com.aspose.slides/behavior)

**تمام رابط‌های پیاده‌سازی شده:**
[com.aspose.slides.IMotionEffect](../../com.aspose.slides/imotioneffect)
```
public class MotionEffect extends Behavior implements IMotionEffect
```

نمایانگر رفتار افکت حرکت است.
## سازنده‌ها

| سازنده | توضیح |
| --- | --- |
| [MotionEffect()](#MotionEffect--) |  |
## متدها

| متد | توضیح |
| --- | --- |
| [getFrom()](#getFrom--) | مختصات x/y را برای شروع انیمیشن مشخص می‌کند (بر حسب درصد). |
| [setFrom(Point2D.Float value)](#setFrom-java.awt.geom.Point2D.Float-) | مختصات x/y را برای شروع انیمیشن مشخص می‌کند (بر حسب درصد). |
| [getTo()](#getTo--) | موقعیت هدف برای اثر حرکتی انیمیشن را مشخص می‌کند (بر حسب درصد). |
| [setTo(Point2D.Float value)](#setTo-java.awt.geom.Point2D.Float-) | موقعیت هدف برای اثر حرکتی انیمیشن را مشخص می‌کند (بر حسب درصد). |
| [getBy()](#getBy--) | مقدار جابجایی نسبی برای انیمیشن را توصیف می‌کند (بر حسب درصد). |
| [setBy(Point2D.Float value)](#setBy-java.awt.geom.Point2D.Float-) | مقدار جابجایی نسبی برای انیمیشن را توصیف می‌کند (بر حسب درصد). |
| [getRotationCenter()](#getRotationCenter--) | مرکز چرخش استفاده‌شده برای چرخاندن مسیر حرکت به اندازهٔ زاویهٔ X را توصیف می‌کند. |
| [setRotationCenter(Point2D.Float value)](#setRotationCenter-java.awt.geom.Point2D.Float-) | مرکز چرخش استفاده‌شده برای چرخاندن مسیر حرکت به اندازهٔ زاویهٔ X را توصیف می‌کند. |
| [getOrigin()](#getOrigin--) | منشاء مسیر حرکت نسبت به مواردی مانند طرح اسلاید یا والد را مشخص می‌کند. |
| [setOrigin(int value)](#setOrigin-int-) | منشاء مسیر حرکت نسبت به مواردی مانند طرح اسلاید یا والد را مشخص می‌کند. |
| [getPath()](#getPath--) | primitive مسیر را به همراه مختصات برای حرکت انیمیشن مشخص می‌کند. |
| [setPath(IMotionPath value)](#setPath-com.aspose.slides.IMotionPath-) | primitive مسیر را به همراه مختصات برای حرکت انیمیشن مشخص می‌کند. |
| [getPathEditMode()](#getPathEditMode--) | نحوهٔ حرکت مسیر حرکت هنگام جابه‌جایی شکل را مشخص می‌کند. |
| [setPathEditMode(int value)](#setPathEditMode-int-) | نحوهٔ حرکت مسیر حرکت هنگام جابه‌جایی شکل را مشخص می‌کند. |
| [getAngle()](#getAngle--) | زاویهٔ نسبی مسیر حرکت را توصیف می‌کند. |
| [setAngle(float value)](#setAngle-float-) | زاویهٔ نسبی مسیر حرکت را توصیف می‌کند. |
### MotionEffect() {#MotionEffect--}
```
public MotionEffect()
```

### getFrom() {#getFrom--}
```
public final Point2D.Float getFrom()
```

مختصات x/y را برای شروع انیمیشن مشخص می‌کند (بر حسب درصد). خواندنی/قابل نوشتن java.awt.geom.Point2D.Float.

**بازگشت:**
java.awt.geom.Point2D.Float
### setFrom(Point2D.Float value) {#setFrom-java.awt.geom.Point2D.Float-}
```
public final void setFrom(Point2D.Float value)
```

مختصات x/y را برای شروع انیمیشن مشخص می‌کند (بر حسب درصد). خواندنی/قابل نوشتن java.awt.geom.Point2D.Float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getTo() {#getTo--}
```
public final Point2D.Float getTo()
```

موقعیت هدف برای اثر حرکتی انیمیشن را مشخص می‌کند (بر حسب درصد). خواندنی/قابل نوشتن java.awt.geom.Point2D.Float.

**بازگشت:**
java.awt.geom.Point2D.Float
### setTo(Point2D.Float value) {#setTo-java.awt.geom.Point2D.Float-}
```
public final void setTo(Point2D.Float value)
```

موقعیت هدف برای اثر حرکتی انیمیشن را مشخص می‌کند (بر حسب درصد). خواندنی/قابل نوشتن java.awt.geom.Point2D.Float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getBy() {#getBy--}
```
public final Point2D.Float getBy()
```

مقدار جابجایی نسبی برای انیمیشن را توصیف می‌کند (بر حسب درصد). خواندنی/قابل نوشتن java.awt.geom.Point2D.Float.

**بازگشت:**
java.awt.geom.Point2D.Float
### setBy(Point2D.Float value) {#setBy-java.awt.geom.Point2D.Float-}
```
public final void setBy(Point2D.Float value)
```

مقدار جابجایی نسبی برای انیمیشن را توصیف می‌کند (بر حسب درصد). خواندنی/قابل نوشتن java.awt.geom.Point2D.Float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getRotationCenter() {#getRotationCenter--}
```
public final Point2D.Float getRotationCenter()
```

مرکز چرخش استفاده‌شده برای چرخاندن مسیر حرکت به اندازهٔ زاویهٔ X را توصیف می‌کند. خواندنی/قابل نوشتن java.awt.geom.Point2D.Float.

**بازگشت:**
java.awt.geom.Point2D.Float
### setRotationCenter(Point2D.Float value) {#setRotationCenter-java.awt.geom.Point2D.Float-}
```
public final void setRotationCenter(Point2D.Float value)
```

مرکز چرخش استفاده‌شده برای چرخاندن مسیر حرکت به اندازهٔ زاویهٔ X را توصیف می‌کند. خواندنی/قابل نوشتن java.awt.geom.Point2D.Float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | java.awt.geom.Point2D.Float |  |

### getOrigin() {#getOrigin--}
```
public final int getOrigin()
```

منشاء مسیر حرکت نسبت به مواردی مانند طرح اسلاید یا والد را مشخص می‌کند. خواندنی/قابل نوشتن [MotionOriginType](../../com.aspose.slides/motionorigintype).

**بازگشت:**
int
### setOrigin(int value) {#setOrigin-int-}
```
public final void setOrigin(int value)
```

منشاء مسیر حرکت نسبت به مواردی مانند طرح اسلاید یا والد را مشخص می‌کند. خواندنی/قابل نوشتن [MotionOriginType](../../com.aspose.slides/motionorigintype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getPath() {#getPath--}
```
public final IMotionPath getPath()
```

primitive مسیر را به همراه مختصات برای حرکت انیمیشن مشخص می‌کند. خواندنی/قابل نوشتن [IMotionPath](../../com.aspose.slides/imotionpath).

**بازگشت:**
[IMotionPath](../../com.aspose.slides/imotionpath)
### setPath(IMotionPath value) {#setPath-com.aspose.slides.IMotionPath-}
```
public final void setPath(IMotionPath value)
```

primitive مسیر را به همراه مختصات برای حرکت انیمیشن مشخص می‌کند. خواندنی/قابل نوشتن [IMotionPath](../../com.aspose.slides/imotionpath).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | [IMotionPath](../../com.aspose.slides/imotionpath) |  |

### getPathEditMode() {#getPathEditMode--}
```
public final int getPathEditMode()
```

نحوهٔ حرکت مسیر حرکت هنگام جابه‌جایی شکل را مشخص می‌کند. خواندنی/قابل نوشتن [MotionPathEditMode](../../com.aspose.slides/motionpatheditmode).

**بازگشت:**
int
### setPathEditMode(int value) {#setPathEditMode-int-}
```
public final void setPathEditMode(int value)
```

نحوهٔ حرکت مسیر حرکت هنگام جابه‌جایی شکل را مشخص می‌کند. خواندنی/قابل نوشتن [MotionPathEditMode](../../com.aspose.slides/motionpatheditmode).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getAngle() {#getAngle--}
```
public final float getAngle()
```

زاویهٔ نسبی مسیر حرکت را توصیف می‌کند. خواندنی/قابل نوشتن float.

**بازگشت:**
float
### setAngle(float value) {#setAngle-float-}
```
public final void setAngle(float value)
```

زاویهٔ نسبی مسیر حرکت را توصیف می‌کند. خواندنی/قابل نوشتن float.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |