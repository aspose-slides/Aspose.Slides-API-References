---
title: ITiming
second_title: Aspose.Slides برای Java مرجع API
description: نشان‌دهنده زمان‌بندی انیمیشن.
type: docs
url: /fa/com.aspose.slides/itiming/
---```
public interface ITiming
```

نشان‌دهنده زمان‌بندی انیمیشن.
## متدها

| Method | Description |
| --- | --- |
| [getAccelerate()](#getAccelerate--) | درصد رفتار شتابدار مدت زمان اثر را توصیف می‌کند. |
| [setAccelerate(float value)](#setAccelerate-float-) | درصد رفتار شتابدار مدت زمان اثر را توصیف می‌کند. |
| [getDecelerate()](#getDecelerate--) | درصد رفتار کاهش سرعت مدت زمان اثر را توصیف می‌کند. |
| [setDecelerate(float value)](#setDecelerate-float-) | درصد رفتار کاهش سرعت مدت زمان اثر را توصیف می‌کند. |
| [getAutoReverse()](#getAutoReverse--) | مشخص می‌کند که آیا انیمیشن پس از پخش در جهت پیشرفته به صورت خودکار به حالت معکوس پخش شود یا خیر. |
| [setAutoReverse(boolean value)](#setAutoReverse-boolean-) | مشخص می‌کند که آیا انیمیشن پس از پخش در جهت پیشرفته به صورت خودکار به حالت معکوس پخش شود یا خیر. |
| [getDuration()](#getDuration--) | مدت زمان اثر انیمیشن را توصیف می‌کند. |
| [setDuration(float value)](#setDuration-float-) | مدت زمان اثر انیمیشن را توصیف می‌کند. |
| [getRepeatCount()](#getRepeatCount--) | تعداد دفعاتی که اثر باید تکرار شود را توصیف می‌کند. |
| [setRepeatCount(float value)](#setRepeatCount-float-) | تعداد دفعاتی که اثر باید تکرار شود را توصیف می‌کند. |
| [getRepeatUntilEndSlide()](#getRepeatUntilEndSlide--) | این ویژگی مشخص می‌کند که آیا اثر تا پایان اسلاید تکرار شود یا خیر. |
| [setRepeatUntilEndSlide(boolean value)](#setRepeatUntilEndSlide-boolean-) | این ویژگی مشخص می‌کند که آیا اثر تا پایان اسلاید تکرار شود یا خیر. |
| [getRepeatUntilNextClick()](#getRepeatUntilNextClick--) | این ویژگی مشخص می‌کند که آیا اثر تا کلیک بعدی تکرار شود یا خیر. |
| [setRepeatUntilNextClick(boolean value)](#setRepeatUntilNextClick-boolean-) | این ویژگی مشخص می‌کند که آیا اثر تا کلیک بعدی تکرار شود یا خیر. |
| [getRepeatDuration()](#getRepeatDuration--) | تعداد دفعاتی که اثر باید تکرار شود را توصیف می‌کند. |
| [setRepeatDuration(float value)](#setRepeatDuration-float-) | تعداد دفعاتی که اثر باید تکرار شود را توصیف می‌کند. |
| [getRestart()](#getRestart--) | مشخص می‌کند که آیا اثر پس از تکمیل دوباره آغاز شود یا نه. |
| [setRestart(int value)](#setRestart-int-) | مشخص می‌کند که آیا اثر پس از تکمیل دوباره آغاز شود یا نه. |
| [getSpeed()](#getSpeed--) | درصدی که زمان‌بندی باید سریع‌تر (یا آهسته‌تر) شود را مشخص می‌کند. |
| [setSpeed(float value)](#setSpeed-float-) | درصدی که زمان‌بندی باید سریع‌تر (یا آهسته‌تر) شود را مشخص می‌کند. |
| [getTriggerDelayTime()](#getTriggerDelayTime--) | زمان تاخیر پس از فعال‌سازی را توصیف می‌کند. |
| [setTriggerDelayTime(float value)](#setTriggerDelayTime-float-) | زمان تاخیر پس از فعال‌سازی را توصیف می‌کند. |
| [getTriggerType()](#getTriggerType--) | نوع فعال‌ساز را توصیف می‌کند. |
| [setTriggerType(int value)](#setTriggerType-int-) | نوع فعال‌ساز را توصیف می‌کند. |
| [getRewind()](#getRewind--) | این ویژگی مشخص می‌کند که آیا اثر پس از پایان پخش به عقب برگردد یا خیر. |
| [setRewind(boolean value)](#setRewind-boolean-) | این ویژگی مشخص می‌کند که آیا اثر پس از پایان پخش به عقب برگردد یا خیر. |

### getAccelerate() {#getAccelerate--}
```
public abstract float getAccelerate()
```

درصد رفتار شتابدار مدت زمان اثر را توصیف می‌کند. قابل خواندن/نوشتن float.

**Returns:**
float
### setAccelerate(float value) {#setAccelerate-float-}
```
public abstract void setAccelerate(float value)
```

درصد رفتار شتابدار مدت زمان اثر را توصیف می‌کند. قابل خواندن/نوشتن float.

**Parameters:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getDecelerate() {#getDecelerate--}
```
public abstract float getDecelerate()
```

درصد رفتار کاهش سرعت مدت زمان اثر را توصیف می‌کند. قابل خواندن/نوشتن float.

**Returns:**
float
### setDecelerate(float value) {#setDecelerate-float-}
```
public abstract void setDecelerate(float value)
```

درصد رفتار کاهش سرعت مدت زمان اثر را توصیف می‌کند. قابل خواندن/نوشتن float.

**Parameters:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getAutoReverse() {#getAutoReverse--}
```
public abstract boolean getAutoReverse()
```

مشخص می‌کند که آیا انیمیشن پس از پخش در جهت پیشرفته به صورت خودکار به حالت معکوس پخش شود یا خیر. قابل خواندن/نوشتن boolean.

**Returns:**
boolean
### setAutoReverse(boolean value) {#setAutoReverse-boolean-}
```
public abstract void setAutoReverse(boolean value)
```

مشخص می‌کند که آیا انیمیشن پس از پخش در جهت پیشرفته به صورت خودکار به حالت معکوس پخش شود یا خیر. قابل خواندن/نوشتن boolean.

**Parameters:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getDuration() {#getDuration--}
```
public abstract float getDuration()
```

مدت زمان اثر انیمیشن را توصیف می‌کند. قابل خواندن/نوشتن float.

**Returns:**
float
### setDuration(float value) {#setDuration-float-}
```
public abstract void setDuration(float value)
```

مدت زمان اثر انیمیشن را توصیف می‌کند. قابل خواندن/نوشتن float.

**Parameters:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getRepeatCount() {#getRepeatCount--}
```
public abstract float getRepeatCount()
```

تعداد دفعاتی که اثر باید تکرار شود را توصیف می‌کند. قابل خواندن/نوشتن float.

**Returns:**
float
### setRepeatCount(float value) {#setRepeatCount-float-}
```
public abstract void setRepeatCount(float value)
```

تعداد دفعاتی که اثر باید تکرار شود را توصیف می‌کند. قابل خواندن/نوشتن float.

**Parameters:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getRepeatUntilEndSlide() {#getRepeatUntilEndSlide--}
```
public abstract boolean getRepeatUntilEndSlide()
```

این ویژگی مشخص می‌کند که آیا اثر تا پایان اسلاید تکرار شود یا خیر. قابل خواندن/نوشتن boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت توالی اثرات برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین اثر از توالی اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // تغییر زمان‌بندی/تکرار اثر به "تا انتهای اسلاید"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Returns:**
boolean
### setRepeatUntilEndSlide(boolean value) {#setRepeatUntilEndSlide-boolean-}
```
public abstract void setRepeatUntilEndSlide(boolean value)
```

این ویژگی مشخص می‌کند که آیا اثر تا پایان اسلاید تکرار شود یا خیر. قابل خواندن/نوشتن boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت توالی اثرات برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین اثر از توالی اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // تغییر زمان‌بندی/تکرار اثر به "Until End of Slide"
>      effect.getTiming().setRepeatUntilEndSlide(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Parameters:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getRepeatUntilNextClick() {#getRepeatUntilNextClick--}
```
public abstract boolean getRepeatUntilNextClick()
```

این ویژگی مشخص می‌کند که آیا اثر تا کلیک بعدی تکرار شود یا خیر. قابل خواندن/نوشتن boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت توالی اثرات برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین اثر از توالی اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // تغییر زمان‌بندی/تکرار اثر به "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Returns:**
boolean
### setRepeatUntilNextClick(boolean value) {#setRepeatUntilNextClick-boolean-}
```
public abstract void setRepeatUntilNextClick(boolean value)
```

این ویژگی مشخص می‌کند که آیا اثر تا کلیک بعدی تکرار شود یا خیر. قابل خواندن/نوشتن boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت توالی اثرات برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین اثر از توالی اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // تغییر زمان‌بندی/تکرار اثر به "Until Next Click"
>      effect.getTiming().setRepeatUntilNextClick(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parameters:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getRepeatDuration() {#getRepeatDuration--}
```
public abstract float getRepeatDuration()
```

تعداد دفعاتی که اثر باید تکرار شود را توصیف می‌کند. قابل خواندن/نوشتن float.

**Returns:**
float
### setRepeatDuration(float value) {#setRepeatDuration-float-}
```
public abstract void setRepeatDuration(float value)
```

تعداد دفعاتی که اثر باید تکرار شود را توصیف می‌کند. قابل خواندن/نوشتن float.

**Parameters:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getRestart() {#getRestart--}
```
public abstract int getRestart()
```

مشخص می‌کند که آیا اثر پس از تکمیل دوباره آغاز شود یا نه. قابل خواندن/نوشتن [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Returns:**
int
### setRestart(int value) {#setRestart-int-}
```
public abstract void setRestart(int value)
```

مشخص می‌کند که آیا اثر پس از تکمیل دوباره آغاز شود یا نه. قابل خواندن/نوشتن [EffectRestartType](../../com.aspose.slides/effectrestarttype).

**Parameters:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getSpeed() {#getSpeed--}
```
public abstract float getSpeed()
```

درصدی که زمان‌بندی باید سریع‌تر (یا آهسته‌تر) شود را مشخص می‌کند. قابل خواندن/نوشتن float.

**Returns:**
float
### setSpeed(float value) {#setSpeed-float-}
```
public abstract void setSpeed(float value)
```

درصدی که زمان‌بندی باید سریع‌تر (یا آهسته‌تر) شود را مشخص می‌کند. قابل خواندن/نوشتن float.

**Parameters:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getTriggerDelayTime() {#getTriggerDelayTime--}
```
public abstract float getTriggerDelayTime()
```

زمان تاخیر پس از فعال‌سازی را توصیف می‌کند. قابل خواندن/نوشتن float.

**Returns:**
float
### setTriggerDelayTime(float value) {#setTriggerDelayTime-float-}
```
public abstract void setTriggerDelayTime(float value)
```

زمان تاخیر پس از فعال‌سازی را توصیف می‌کند. قابل خواندن/نوشتن float.

**Parameters:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | float |  |

### getTriggerType() {#getTriggerType--}
```
public abstract int getTriggerType()
```

نوع فعال‌ساز را توصیف می‌کند. قابل خواندن/نوشتن [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Returns:**
int
### setTriggerType(int value) {#setTriggerType-int-}
```
public abstract void setTriggerType(int value)
```

نوع فعال‌ساز را توصیف می‌کند. قابل خواندن/نوشتن [EffectTriggerType](../../com.aspose.slides/effecttriggertype).

**Parameters:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getRewind() {#getRewind--}
```
public abstract boolean getRewind()
```

این ویژگی مشخص می‌کند که آیا اثر پس از پایان پخش به عقب برگردد یا خیر. قابل خواندن/نوشتن boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت توالی اثرات برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین اثر از توالی اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // فعال کردن زمان‌بندی/بازپخش اثر.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```


**Returns:**
boolean
### setRewind(boolean value) {#setRewind-boolean-}
```
public abstract void setRewind(boolean value)
```

این ویژگی مشخص می‌کند که آیا اثر پس از پایان پخش به عقب برگردد یا خیر. قابل خواندن/نوشتن boolean.

--------------------

> ```
> Presentation presentation = new Presentation("demo.pptx");
>  try {
>      // دریافت توالی اثرات برای اولین اسلاید
>      ISequence effectsSequence = presentation.getSlides().get_Item(0).getTimeline().getMainSequence();
>      // دریافت اولین اثر از توالی اصلی.
>      IEffect effect = effectsSequence.get_Item(0);
>      // فعال کردن زمان‌بندی/بازپخش اثر.
>      effect.getTiming().setRewind(true);
>  } finally {
>      if (presentation != null) presentation.dispose();
>  }
> ```

**Parameters:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |