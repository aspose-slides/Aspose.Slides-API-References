---
title: AudioPlayModePreset
second_title: مرجع API Aspose.Slides برای جاوا
description: ثابت‌هایی که نحوه پخش صدا را تعریف می‌کنند.
type: docs
url: /fa/com.aspose.slides/audioplaymodepreset/
---
**ارث‌بری:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class AudioPlayModePreset extends System.Enum
```

ثابت‌هایی که نحوه پخش صدا را تعریف می‌کنند.
## فیلدها

| Field | Description |
| --- | --- |
| [Mixed](#Mixed) |  |
| [Auto](#Auto) | به‌صورت خودکار پخش می‌شود. |
| [OnClick](#OnClick) | فقط زمانی که نماد کلیک شود پخش می‌شود. |
| [AllSlides](#AllSlides) | در تمام اسلایدها پخش می‌شود. |
| [InClickSequence](#InClickSequence) | به‌صورت خودکار با یک کلیک پخش می‌شود. |
### Mixed {#Mixed}
```
public static final int Mixed
```




### Auto {#Auto}
```
public static final int Auto
```

به‌صورت خودکار پخش می‌شود.

### OnClick {#OnClick}
```
public static final int OnClick
```

فقط زمانی که نماد کلیک شود پخش می‌شود.

### AllSlides {#AllSlides}
```
public static final int AllSlides
```

در تمام اسلایدها پخش می‌شود. لطفاً به جای آن از ویژگی ([IAudioFrame.getPlayAcrossSlides](../../com.aspose.slides/iaudioframe\#getPlayAcrossSlides)/[IAudioFrame.setPlayAcrossSlides(boolean)](../../com.aspose.slides/iaudioframe\#setPlayAcrossSlides-boolean-)) استفاده کنید.

--------------------

این عنصر از نسخه PowerPoint 2013 به‌بعد منسوخ شده است.

### InClickSequence {#InClickSequence}
```
public static final int InClickSequence
```

به‌صورت خودکار با یک کلیک پخش می‌شود.