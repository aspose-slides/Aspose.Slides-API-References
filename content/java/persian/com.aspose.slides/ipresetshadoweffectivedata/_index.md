---
title: IPresetShadowEffectiveData
second_title: مرجع API Aspose.Slides برای Java
description: شیء غیرقابل تغییر که یک اثر Preset Shadow را نشان می‌دهد.
type: docs
url: /fa/com.aspose.slides/ipresetshadoweffectivedata/
---
**همهٔ رابط‌های پیاده‌سازی شده:**
[com.aspose.slides.IEffectEffectiveData](../../com.aspose.slides/ieffecteffectivedata)
```
public interface IPresetShadowEffectiveData extends IEffectEffectiveData
```

شیء غیرقابل تغییر که یک اثر Preset Shadow را نشان می‌دهد.
## متدها

| متد | توضیح |
| --- | --- |
| [getDirection()](#getDirection--) | جهت سایه. |
| [getDistance()](#getDistance--) | فاصلهٔ سایه. |
| [getShadowColor()](#getShadowColor--) | رنگ سایه. |
| [getPreset()](#getPreset--) | Preset. |

### getDirection() {#getDirection--}
```
public abstract float getDirection()
```

جهت سایه. فقط‌خواندنی float.

**بازگرداندن:**
float

### getDistance() {#getDistance--}
```
public abstract double getDistance()
```

فاصلهٔ سایه. فقط‌خواندنی double.

**بازگرداندن:**
double

### getShadowColor() {#getShadowColor--}
```
public abstract Color getShadowColor()
```

رنگ سایه. فقط‌خواندنی java.awt.Color.

**بازگرداندن:**
java.awt.Color

### getPreset() {#getPreset--}
```
public abstract int getPreset()
```

Preset. فقط‌خواندنی [PresetShadowType](../../com.aspose.slides/presetshadowtype).

**بازگرداندن:**
int