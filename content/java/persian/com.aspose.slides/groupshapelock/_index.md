---
title: GroupShapeLock
second_title: Aspose.Slides برای Java API مرجع
description: مشخص می‌کند که کدام عملیات‌ها در GroupShape والد غیرفعال هستند.
type: docs
url: /fa/com.aspose.slides/groupshapelock/
---
**ارث‌بری:**
java.lang.Object, [com.aspose.slides.BaseShapeLock](../../com.aspose.slides/baseshapelock)

**تمام رابط‌های پیاده‌سازی شده:**
[com.aspose.slides.IGroupShapeLock](../../com.aspose.slides/igroupshapelock)
```
public class GroupShapeLock extends BaseShapeLock implements IGroupShapeLock
```

مشخص می‌کند که کدام عملیات‌ها در GroupShape والد غیرفعال هستند.
## متدها

| متد | توضیح |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | مشخص می‌کند که افزودن این شکل به یک گروه ممنوع است یا نه. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | مشخص می‌کند که افزودن این شکل به یک گروه ممنوع است یا نه. |
| [getUngroupingLocked()](#getUngroupingLocked--) | مشخص می‌کند که تقسیم این گروه‌شکل ممنوع است یا نه. |
| [setUngroupingLocked(boolean value)](#setUngroupingLocked-boolean-) | مشخص می‌کند که تقسیم این گروه‌شکل ممنوع است یا نه. |
| [getSelectLocked()](#getSelectLocked--) | مشخص می‌کند که انتخاب این شکل ممنوع است یا نه. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | مشخص می‌کند که انتخاب این شکل ممنوع است یا نه. |
| [getRotationLocked()](#getRotationLocked--) | مشخص می‌کند که تغییر زاویه چرخش این شکل ممنوع است یا نه. |
| [setRotationLocked(boolean value)](#setRotationLocked-boolean-) | مشخص می‌کند که تغییر زاویه چرخش این شکل ممنوع است یا نه. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | مشخص می‌کند که شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | مشخص می‌کند که شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. |
| [getPositionLocked()](#getPositionLocked--) | مشخص می‌کند که حرکت این شکل ممنوع است یا نه. |
| [setPositionLocked(boolean value)](#setPositionLocked-boolean-) | مشخص می‌کند که حرکت این شکل ممنوع است یا نه. |
| [getSizeLocked()](#getSizeLocked--) | مشخص می‌کند که تغییر اندازه این شکل ممنوع است یا نه. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | مشخص می‌کند که تغییر اندازه این شکل ممنوع است یا نه. |

### getGroupingLocked() {#getGroupingLocked--}
```
public boolean getGroupingLocked()
```

مشخص می‌کند که افزودن این شکل به یک گروه ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean

### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public void setGroupingLocked(boolean value)
```

مشخص می‌کند که افزودن این شکل به یک گروه ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getUngroupingLocked() {#getUngroupingLocked--}
```
public boolean getUngroupingLocked()
```

مشخص می‌کند که تقسیم این گروه‌شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean

### setUngroupingLocked(boolean value) {#setUngroupingLocked-boolean-}
```
public void setUngroupingLocked(boolean value)
```

مشخص می‌کند که تقسیم این گروه‌شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getSelectLocked() {#getSelectLocked--}
```
public boolean getSelectLocked()
```

مشخص می‌کند که انتخاب این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean

### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public void setSelectLocked(boolean value)
```

مشخص می‌کند که انتخاب این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getRotationLocked() {#getRotationLocked--}
```
public boolean getRotationLocked()
```

مشخص می‌کند که تغییر زاویه چرخش این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean

### setRotationLocked(boolean value) {#setRotationLocked-boolean-}
```
public void setRotationLocked(boolean value)
```

مشخص می‌کند که تغییر زاویه چرخش این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public boolean getAspectRatioLocked()
```

مشخص می‌کند که شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean

### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public void setAspectRatioLocked(boolean value)
```

مشخص می‌کند که شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getPositionLocked() {#getPositionLocked--}
```
public boolean getPositionLocked()
```

مشخص می‌کند که حرکت این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean

### setPositionLocked(boolean value) {#setPositionLocked-boolean-}
```
public void setPositionLocked(boolean value)
```

مشخص می‌کند که حرکت این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |

### getSizeLocked() {#getSizeLocked--}
```
public boolean getSizeLocked()
```

مشخص می‌کند که تغییر اندازه این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**بازگشت:**
boolean

### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public void setSizeLocked(boolean value)
```

مشخص می‌کند که تغییر اندازه این شکل ممنوع است یا نه. خواندنی/نوشتنی boolean.

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | boolean |  |