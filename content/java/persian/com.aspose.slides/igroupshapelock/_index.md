---
title: IGroupShapeLock
second_title: مرجع API Aspose.Slides برای جاوا
description: تعیین می‌کند کدام عملیات‌ها در GroupShape والد غیرفعال هستند.
type: docs
url: /fa/com.aspose.slides/igroupshapelock/
---
**تمام رابط‌های پیاده‌سازی‌شده:**
[com.aspose.slides.IBaseShapeLock](../../com.aspose.slides/ibaseshapelock)
```
public interface IGroupShapeLock extends IBaseShapeLock
```

تعیین می‌کند که کدام عملیات‌ها در GroupShape والد غیرفعال هستند.
## متدها

| Method | Description |
| --- | --- |
| [getGroupingLocked()](#getGroupingLocked--) | تعیین می‌کند که افزودن این شکل به یک گروه ممنوع است. |
| [setGroupingLocked(boolean value)](#setGroupingLocked-boolean-) | تعیین می‌کند که افزودن این شکل به یک گروه ممنوع است. |
| [getUngroupingLocked()](#getUngroupingLocked--) | تعیین می‌کند که تقسیم این گروه‌شکل ممنوع است. |
| [setUngroupingLocked(boolean value)](#setUngroupingLocked-boolean-) | تعیین می‌کند که تقسیم این گروه‌شکل ممنوع است. |
| [getSelectLocked()](#getSelectLocked--) | تعیین می‌کند که انتخاب این شکل ممنوع است. |
| [setSelectLocked(boolean value)](#setSelectLocked-boolean-) | تعیین می‌کند که انتخاب این شکل ممنوع است. |
| [getRotationLocked()](#getRotationLocked--) | تعیین می‌کند که تغییر زاویه چرخش این شکل ممنوع است. |
| [setRotationLocked(boolean value)](#setRotationLocked-boolean-) | تعیین می‌کند که تغییر زاویه چرخش این شکل ممنوع است. |
| [getAspectRatioLocked()](#getAspectRatioLocked--) | تعیین می‌کند که شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. |
| [setAspectRatioLocked(boolean value)](#setAspectRatioLocked-boolean-) | تعیین می‌کند که شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. |
| [getPositionLocked()](#getPositionLocked--) | تعیین می‌کند که جابجایی این شکل ممنوع است. |
| [setPositionLocked(boolean value)](#setPositionLocked-boolean-) | تعیین می‌کند که جابجایی این شکل ممنوع است. |
| [getSizeLocked()](#getSizeLocked--) | تعیین می‌کند که تغییر اندازه این شکل ممنوع است. |
| [setSizeLocked(boolean value)](#setSizeLocked-boolean-) | تعیین می‌کند که تغییر اندازه این شکل ممنوع است. |
### getGroupingLocked() {#getGroupingLocked--}
```
public abstract boolean getGroupingLocked()
```

تعیین می‌کند که افزودن این شکل به یک گروه ممنوع است. بولی خواندنی/نوشتنی.

**بازمی‌گرداند:**
boolean
### setGroupingLocked(boolean value) {#setGroupingLocked-boolean-}
```
public abstract void setGroupingLocked(boolean value)
```

تعیین می‌کند که افزودن این شکل به یک گروه ممنوع است. بولی خواندنی/نوشتنی.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getUngroupingLocked() {#getUngroupingLocked--}
```
public abstract boolean getUngroupingLocked()
```

تعیین می‌کند که تقسیم این گروه‌شکل ممنوع است. بولی خواندنی/نوشتنی.

**بازمی‌گرداند:**
boolean
### setUngroupingLocked(boolean value) {#setUngroupingLocked-boolean-}
```
public abstract void setUngroupingLocked(boolean value)
```

تعیین می‌کند که تقسیم این گروه‌شکل ممنوع است. بولی خواندنی/نوشتنی.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getSelectLocked() {#getSelectLocked--}
```
public abstract boolean getSelectLocked()
```

تعیین می‌کند که انتخاب این شکل ممنوع است. بولی خواندنی/نوشتنی.

**بازمی‌گرداند:**
boolean
### setSelectLocked(boolean value) {#setSelectLocked-boolean-}
```
public abstract void setSelectLocked(boolean value)
```

تعیین می‌کند که انتخاب این شکل ممنوع است. بولی خواندنی/نوشتنی.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getRotationLocked() {#getRotationLocked--}
```
public abstract boolean getRotationLocked()
```

تعیین می‌کند که تغییر زاویه چرخش این شکل ممنوع است. بولی خواندنی/نوشتنی.

**بازمی‌گرداند:**
boolean
### setRotationLocked(boolean value) {#setRotationLocked-boolean-}
```
public abstract void setRotationLocked(boolean value)
```

تعیین می‌کند که تغییر زاویه چرخش این شکل ممنوع است. بولی خواندنی/نوشتنی.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getAspectRatioLocked() {#getAspectRatioLocked--}
```
public abstract boolean getAspectRatioLocked()
```

تعیین می‌کند که شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. بولی خواندنی/نوشتنی.

**بازمی‌گرداند:**
boolean
### setAspectRatioLocked(boolean value) {#setAspectRatioLocked-boolean-}
```
public abstract void setAspectRatioLocked(boolean value)
```

تعیین می‌کند که شکل باید نسبت ابعاد را هنگام تغییر اندازه حفظ کند. بولی خواندنی/نوشتنی.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getPositionLocked() {#getPositionLocked--}
```
public abstract boolean getPositionLocked()
```

تعیین می‌کند که جابجایی این شکل ممنوع است. بولی خواندنی/نوشتنی.

**بازمی‌گرداند:**
boolean
### setPositionLocked(boolean value) {#setPositionLocked-boolean-}
```
public abstract void setPositionLocked(boolean value)
```

تعیین می‌کند که جابجایی این شکل ممنوع است. بولی خواندنی/نوشتنی.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |

### getSizeLocked() {#getSizeLocked--}
```
public abstract boolean getSizeLocked()
```

تعیین می‌کن

د که تغییر اندازه این شکل ممنوع است. بولی خواندنی/نوشتنی.

**بازمی‌گرداند:**
boolean
### setSizeLocked(boolean value) {#setSizeLocked-boolean-}
```
public abstract void setSizeLocked(boolean value)
```

تعیین می‌کند که تغییر اندازه این شکل ممنوع است. بولی خواندنی/نوشتنی.

**پارامترها:**
| Parameter | Type | Description |
| --- | --- | --- |
| value | boolean |  |