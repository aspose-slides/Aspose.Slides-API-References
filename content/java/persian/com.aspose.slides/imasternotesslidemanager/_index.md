---
title: IMasterNotesSlideManager
second_title: Aspose.Slides for Java API Reference
description: Master notes slide manager.
type: docs
url: /fa/com.aspose.slides/imasternotesslidemanager/
---```
public interface IMasterNotesSlideManager
```

مدیر اسلاید یادداشت‌های اصلی.
## متدها

| Method | توضیح |
| --- | --- |
| [getMasterNotesSlide()](#getMasterNotesSlide--) | اگر برای همه اسلایدهای یادداشت این ارائه یک master وجود داشته باشد، آن را بر می‌گرداند؛ در غیر اینصورت null بر می‌گرداند. |
| [setDefaultMasterNotesSlide()](#setDefaultMasterNotesSlide--) | master پیش‌فرض اسلاید یادداشت را برای اسلاید یادداشت مرتبط تنظیم می‌کند. |
| [removeMasterNotesSlide()](#removeMasterNotesSlide--) | master اسلاید یادداشت را حذف می‌کند. |
### getMasterNotesSlide() {#getMasterNotesSlide--}
```
public abstract IMasterNotesSlide getMasterNotesSlide()
```


اگر برای همه اسلایدهای یادداشت این ارائه یک master وجود داشته باشد، آن را بر می‌گرداند؛ در غیر اینصورت null بر می‌گرداند. فقط خواندنی [IMasterNotesSlide](../../com.aspose.slides/imasternotesslide).

**بازگشت:**
[IMasterNotesSlide](../../com.aspose.slides/imasternotesslide)
### setDefaultMasterNotesSlide() {#setDefaultMasterNotesSlide--}
```
public abstract IMasterNotesSlide setDefaultMasterNotesSlide()
```


master پیش‌فرض اسلاید یادداشت را برای اسلاید یادداشت مرتبط تنظیم می‌کند.

**بازگشت:**
[IMasterNotesSlide](../../com.aspose.slides/imasternotesslide) - master پیش‌فرض اسلاید یادداشت [IMasterNotesSlide](../../com.aspose.slides/imasternotesslide)
### removeMasterNotesSlide() {#removeMasterNotesSlide--}
```
public abstract void removeMasterNotesSlide()
```


master اسلاید یادداشت را حذف می‌کند.