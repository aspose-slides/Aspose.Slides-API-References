---
title: InterruptionTokenSource
second_title: مرجع API Aspose.Slides برای جاوا
description: منبع . را نشان می‌دهد
type: docs
url: /fa/com.aspose.slides/interruptiontokensource/
---
**ارث‌بری:**
java.lang.Object
```
public class InterruptionTokenSource
```

منبع [InterruptionToken](../../com.aspose.slides/interruptiontoken) را نشان می‌دهد.

## سازنده‌ها

| سازنده | توضیح |
| --- | --- |
| [InterruptionTokenSource()](#InterruptionTokenSource--) | یک [InterruptionTokenSource](../../com.aspose.slides/interruptiontokensource) جدید ایجاد می‌کند. |

## متدها

| متد | توضیح |
| --- | --- |
| [getToken()](#getToken--) | توکن جدیدی را که به این [InterruptionTokenSource](../../com.aspose.slides/interruptiontokensource) متصل است برمی‌گرداند. |
| [isInterruptionRequested()](#isInterruptionRequested--) | اگر درخواست وقفه شده باشد true و در غیر این صورت false باز می‌گرداند. |
| [interrupt()](#interrupt--) | درخواست برای وقفه را مقداردهی اولیه می‌کند. |

### InterruptionTokenSource() {#InterruptionTokenSource--}
```
public InterruptionTokenSource()
```

یک [InterruptionTokenSource](../../com.aspose.slides/interruptiontokensource) جدید ایجاد می‌کند.

### getToken() {#getToken--}
```
public final InterruptionToken getToken()
```

توکن جدیدی را که به این [InterruptionTokenSource](../../com.aspose.slides/interruptiontokensource) متصل است برمی‌گرداند.

**باز می‌گرداند:**
[InterruptionToken](../../com.aspose.slides/interruptiontoken)

### isInterruptionRequested() {#isInterruptionRequested--}
```
public final boolean isInterruptionRequested()
```

اگر درخواست وقفه شده باشد true و در غیر این صورت false باز می‌گرداند.

**باز می‌گرداند:**
boolean

### interrupt() {#interrupt--}
```
public final void interrupt()
```

درخواست برای وقفه را مقداردهی اولیه می‌کند.