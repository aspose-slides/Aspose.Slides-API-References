---
title: IDataSourceTypeForErrorBarsCustomValues
second_title: Aspose.Slides for Java API Reference
description: Specifies types of values in ChartDataPoint.ErrorBarsCustomValues properties list
type: docs
url: /fa/com.aspose.slides/idatasourcetypeforerrorbarscustomvalues/
---```
public interface IDataSourceTypeForErrorBarsCustomValues
```

انواع مقادیر را در فهرست ویژگی‌های ChartDataPoint.ErrorBarsCustomValues مشخص می‌کند
## متدها

| متد | توضیح |
| --- | --- |
| [getDataSourceTypeForXMinusValues()](#getDataSourceTypeForXMinusValues--) | مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء XMinus property برای مقادیر سفارشی نوارهای خطا واقعاً وجود دارد. |
| [setDataSourceTypeForXMinusValues(int value)](#setDataSourceTypeForXMinusValues-int-) | مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء XMinus property برای مقادیر سفارشی نوارهای خطا واقعاً وجود دارد. |
| [getDataSourceTypeForXPlusValues()](#getDataSourceTypeForXPlusValues--) | مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء XPlus property برای مقادیر سفارشی نوارهای خطا واقعاً وجود دارد. |
| [setDataSourceTypeForXPlusValues(int value)](#setDataSourceTypeForXPlusValues-int-) | مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء XPlus property برای مقادیر سفارشی نوارهای خطا واقعاً وجود دارد. |
| [getDataSourceTypeForYMinusValues()](#getDataSourceTypeForYMinusValues--) | مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء YMinus property برای مقادیر سفارشی نوارهای خطا واقعاً وجود دارد. |
| [setDataSourceTypeForYMinusValues(int value)](#setDataSourceTypeForYMinusValues-int-) | مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء YMinus property برای مقادیر سفارشی نوارهای خطا واقعاً وجود دارد. |
| [getDataSourceTypeForYPlusValues()](#getDataSourceTypeForYPlusValues--) | مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء YPlus property برای مقادیر سفارشی نوارهای خطا واقعاً وجود دارد. |
| [setDataSourceTypeForYPlusValues(int value)](#setDataSourceTypeForYPlusValues-int-) | مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء YPlus property برای مقادیر سفارشی نوارهای خطا واقعاً وجود دارد. |

### getDataSourceTypeForXMinusValues() {#getDataSourceTypeForXMinusValues--}
```
public abstract int getDataSourceTypeForXMinusValues()
```

مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء XMinus property برای مقادیر سفارشی نوارهای خطا واقعاً وجود دارد. به عبارت دیگر نوع مقدار ChartDataPoint.ErrorBarsCustomValues.XMinus.Data را مشخص می‌کند. خواند/نوشت [DataSourceType](../../com.aspose.slides/datasourcetype).

**بازگشت:**
int

### setDataSourceTypeForXMinusValues(int value) {#setDataSourceTypeForXMinusValues-int-}
```
public abstract void setDataSourceTypeForXMinusValues(int value)
```

مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء XMinus property برای مقادیر سفارشی نوارهای خطا واقعاً وجود دارد. به عبارت دیگر نوع مقدار ChartDataPoint.ErrorBarsCustomValues.XMinus.Data را مشخص می‌کند. خواند/نوشت [DataSourceType](../../com.aspose.slides/datasourcetype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getDataSourceTypeForXPlusValues() {#getDataSourceTypeForXPlusValues--}
```
public abstract int getDataSourceTypeForXPlusValues()
```

مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء XPlus property برای مقادیر سفارشی نوارهای خطا واقعاً وجود دارد. به عبارت دیگر نوع مقدار ChartDataPoint.ErrorBarsCustomValues.XPlus.Data را مشخص می‌کند. خواند/نوشت [DataSourceType](../../com.aspose.slides/datasourcetype).

**بازگشت:**
int

### setDataSourceTypeForXPlusValues(int value) {#setDataSourceTypeForXPlusValues-int-}
```
public abstract void setDataSourceTypeForXPlusValues(int value)
```

مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء XPlus property برای مقادیر سفارشی نوارهای خطا واقعاً وجود دارد. به عبارت دیگر نوع مقدار ChartDataPoint.ErrorBarsCustomValues.XPlus.Data را مشخص می‌کند. خواند/نوشت [DataSourceType](../../com.aspose.slides/datasourcetype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getDataSourceTypeForYMinusValues() {#getDataSourceTypeForYMinusValues--}
```
public abstract int getDataSourceTypeForYMinusValues()
```

مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء YMinus property برای مقادیر سفارشی نوارهای خطا واقعاً وجود دارد. به عبارت دیگر نوع مقدار ChartDataPointEx.ErrorBarsCustomValues.YMinus.Data را مشخص می‌کند. خواند/نوشت [DataSourceType](../../com.aspose.slides/datasourcetype).

**بازگشت:**
int

### setDataSourceTypeForYMinusValues(int value) {#setDataSourceTypeForYMinusValues-int-}
```
public abstract void setDataSourceTypeForYMinusValues(int value)
```

مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء YMinus property برای مقادیر سفارشی نوارهای خطا واقعاً وجود دارد. به عبارت دیگر نوع مقدار ChartDataPointEx.ErrorBarsCustomValues.YMinus.Data را مشخص می‌کند. خواند/نوشت [DataSourceType](../../com.aspose.slides/datasourcetype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |

### getDataSourceTypeForYPlusValues() {#getDataSourceTypeForYPlusValues--}
```
public abstract int getDataSourceTypeForYPlusValues()
```

مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء YPlus property برای مقادیر سفارشی نوارهای خطا واقعاً وجود دارد. به عبارت دیگر نوع مقدار ChartDataPointEx.ErrorBarsCustomValues.YPlus.Data را مشخص می‌کند. خواند/نوشت [DataSourceType](../../com.aspose.slides/datasourcetype).

**بازگشت:**
int

### setDataSourceTypeForYPlusValues(int value) {#setDataSourceTypeForYPlusValues-int-}
```
public abstract void setDataSourceTypeForYPlusValues(int value)
```

مشخص می‌کند که آیا ویژگی AsCell یا AsLiteralString یا AsLiteralDouble در شیء YPlus property برای مقادیر سفارشی نوارهای خطا واقعاً وجود دارد. به عبارت دیگر نوع مقدار ChartDataPointEx.ErrorBarsCustomValues.YPlus.Data را مشخص می‌کند. خواند/نوشت [DataSourceType](../../com.aspose.slides/datasourcetype).

**پارامترها:**
| پارامتر | نوع | توضیح |
| --- | --- | --- |
| value | int |  |