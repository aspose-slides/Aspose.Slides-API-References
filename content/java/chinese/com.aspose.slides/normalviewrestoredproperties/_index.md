---
title: NormalViewRestoredProperties
second_title: Aspose.Slides Java API 参考
description: 指定普通视图中幻灯片区域的大小（当区域为可变的恢复大小（既未最小化也未最大化）时，宽度为 restoredTop 的子项，或高度为 restoredLeft 的子项）。
type: docs
url: /zh/com.aspose.slides/normalviewrestoredproperties/
---
**继承:**  
java.lang.Object

**所有实现的接口:**  
[com.aspose.slides.INormalViewRestoredProperties](../../com.aspose.slides/inormalviewrestoredproperties)  
```
public class NormalViewRestoredProperties implements INormalViewRestoredProperties
```

指定幻灯片区域的大小（在普通视图中，当区域为可变的恢复大小（既未最小化也未最大化）时，宽度为 restoredTop 的子项时，或者高度为 restoredLeft 的子项时）。

## 方法

| 方法 | 描述 |
| --- | --- |
| [getDimensionSize()](#getDimensionSize--) | 指定幻灯片区域的大小（当为 RestoredTop 的子项时宽度，当为 RestoredLeft 的子项时高度）。 |
| [setDimensionSize(float value)](#setDimensionSize-float-) | 指定幻灯片区域的大小（当为 RestoredTop 的子项时宽度，当为 RestoredLeft 的子项时高度）。 |
| [getAutoAdjust()](#getAutoAdjust--) | 指定在调整包含视图的窗口大小时，侧内容区域的大小是否应补偿新尺寸。Read/write boolean |
| [setAutoAdjust(boolean value)](#setAutoAdjust-boolean-) | 指定在调整包含视图的窗口大小时，侧内容区域的大小是否应补偿新尺寸。Read/write boolean |

### getDimensionSize() {#getDimensionSize--}
```
public final float getDimensionSize()
```

指定幻灯片区域的大小（当为 RestoredTop 的子项时宽度，当为 RestoredLeft 的子项时高度）。Read/write float.

**返回:**  
float

### setDimensionSize(float value) {#setDimensionSize-float-}
```
public final void setDimensionSize(float value)
```

指定幻灯片区域的大小（当为 RestoredTop 的子项时宽度，当为 RestoredLeft 的子项时高度）。Read/write float.

**参数:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| value | float |  |

### getAutoAdjust() {#getAutoAdjust--}
```
public final boolean getAutoAdjust()
```

指定在调整包含视图的窗口大小时，侧内容区域的大小是否应补偿新尺寸。Read/write boolean

**返回:**  
boolean

### setAutoAdjust(boolean value) {#setAutoAdjust-boolean-}
```
public final void setAutoAdjust(boolean value)
```

指定在调整包含视图的窗口大小时，侧内容区域的大小是否应补偿新尺寸。Read/write boolean

**参数:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| value | boolean |  |