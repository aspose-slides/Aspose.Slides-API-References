---
title: DataSourceType
second_title: Aspose.Slides Java API 参考
description: 数据源类型。
type: docs
url: /zh/com.aspose.slides/datasourcetype/
---
**继承：**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class DataSourceType extends System.Enum
```

数据源类型。
## 字段

| 字段 | 描述 |
| --- | --- |
| [Worksheet](#Worksheet) | 使用 CellXls 对象在工作表中存储数据点的 XValue、YValue、Value 或 BubbleSize。 |
| [StringLiterals](#StringLiterals) | 使用快速访问的字符串变量存储数据点的 XValue、YValue、Value 或 BubbleSize。 |
| [DoubleLiterals](#DoubleLiterals) | 使用快速访问的 double 变量存储数据点的 XValue、YValue、Value 或 BubbleSize。 |
### 工作表 {#Worksheet}
```
public static final int Worksheet
```

使用 CellXls 对象在工作表中存储数据点的 XValue、YValue、Value 或 BubbleSize。

### 字符串文字 {#StringLiterals}
```
public static final int StringLiterals
```

使用快速访问的字符串变量存储数据点的 XValue、YValue、Value 或 BubbleSize。工作表不用于存储值。

### 双精度文字 {#DoubleLiterals}
```
public static final int DoubleLiterals
```

使用快速访问的 double 变量存储数据点的 XValue、YValue、Value 或 BubbleSize。工作表不用于存储值。