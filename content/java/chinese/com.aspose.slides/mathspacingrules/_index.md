---
title: MathSpacingRules
second_title: Aspose.Slides Java API 参考
description: 矩阵列之间的水平间隙类型
type: docs
url: /zh/com.aspose.slides/mathspacingrules/
---
**继承:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class MathSpacingRules extends System.Enum
```

矩阵列之间的间隙（水平间距）类型
## 字段

| 字段 | 描述 |
| --- | --- |
| [SingleSpacingGap](#SingleSpacingGap) | 单倍（列为 1 em 或 行为 1 行） |
| [OneAndHalfSpacingGap](#OneAndHalfSpacingGap) | 一倍半（列为 1.5 em 或 行为 1.5 行） |
| [DoubleSpacingGap](#DoubleSpacingGap) | 双倍间距（列为 2 em 或 行为 2 行） |
| [Exactly](#Exactly) | 精确值：列使用 ColumnGap 的值，单位为 twips；行使用 RowGap 的值，单位为 twips |
| [Multiple](#Multiple) | 多个：列使用 ColumnGap 的值，以 0.5 em 为增量；行使用 RowGap 的值，以行数为单位 |

### SingleSpacingGap {#SingleSpacingGap}
```
public static final int SingleSpacingGap
```

单倍（列为 1 em 或 行为 1 行）

### OneAndHalfSpacingGap {#OneAndHalfSpacingGap}
```
public static final int OneAndHalfSpacingGap
```

一倍半（列为 1.5 em 或 行为 1.5 行）

### DoubleSpacingGap {#DoubleSpacingGap}
```
public static final int DoubleSpacingGap
```

双倍间距（列为 2 em 或 行为 2 行）

### Exactly {#Exactly}
```
public static final int Exactly
```

精确值：列使用 ColumnGap 的值，单位为 twips；行使用 RowGap 的值，单位为 twips

### Multiple {#Multiple}
```
public static final int Multiple
```

多个：列使用 ColumnGap 的值，以 0.5 em 为增量；行使用 RowGap 的值，以行数为单位