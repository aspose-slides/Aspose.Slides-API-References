---
title: Timing
second_title: Aspose.Sildes για PHP μέσω Java API
description: 
type: docs

url: /el/aspose.slides/timing/
---
## Timing κλάση

Αντιπροσωπεύει τον χρονισμό της κίνησης.

### getAccelerate {#getAccelerate}

| Όνομα | Περιγραφή |
| --- | --- |
| getAccelerate () | Περιγράφει το ποσοστό της διάρκειας για την επίδραση επιτάχυνσης της συμπεριφοράς. Ανάγνωση/εγγραφή float. |

**Επιστρέφει:**
float


---

### getAutoReverse {#getAutoReverse}

| Όνομα | Περιγραφή |
| --- | --- |
| getAutoReverse () | Περιγράφει αν η κίνηση θα παιχτεί αυτόματα ανάποδα μετά την προώθηση. Ανάγνωση/εγγραφή boolean. |

**Επιστρέφει:**
boolean


---

### getDecelerate {#getDecelerate}

| Όνομα | Περιγραφή |
| --- | --- |
| getDecelerate () | Περιγράφει το ποσοστό της διάρκειας για την επίδραση επιβράδυνσης της συμπεριφοράς. Ανάγνωση/εγγραφή float. |

**Επιστρέφει:**
float


---

### getDuration {#getDuration}

| Όνομα | Περιγραφή |
| --- | --- |
| getDuration () | Περιγράφει τη διάρκεια της επίδρασης της κίνησης. Ανάγνωση/εγγραφή float. |

**Επιστρέφει:**
float


---

### getRepeatCount {#getRepeatCount}

| Όνομα | Περιγραφή |
| --- | --- |
| getRepeatCount () | Περιγράφει τον αριθμό των επαναλήψεων του εφέ. Ανάγνωση/εγγραφή float. |

**Επιστρέφει:**
float


---

### getRepeatDuration {#getRepeatDuration}

| Όνομα | Περιγραφή |
| --- | --- |
| getRepeatDuration () | Περιγράφει τη διάρκεια της επανάληψης του εφέ. Ανάγνωση/εγγραφή float. |

**Επιστρέφει:**
float


---

### getRepeatUntilEndSlide {#getRepeatUntilEndSlide}

| Όνομα | Περιγραφή |
| --- | --- |
| getRepeatUntilEndSlide () | Αυτό το χαρακτηριστικό καθορίζει αν το εφέ θα επαναληφθεί μέχρι το τέλος της διαφάνειας. Ανάγνωση/εγγραφή boolean. |

**Επιστρέφει:**
boolean


---

### getRepeatUntilNextClick {#getRepeatUntilNextClick}

| Όνομα | Περιγραφή |
| --- | --- |
| getRepeatUntilNextClick () | Αυτό το χαρακτηριστικό καθορίζει αν το εφέ θα επαναληφθεί μέχρι το επόμενο κλικ. Ανάγνωση/εγγραφή boolean. |

**Επιστρέφει:**
boolean


---

### getRestart {#getRestart}

| Όνομα | Περιγραφή |
| --- | --- |
| getRestart () | Καθορίζει αν ένα εφέ θα επανεκκινηθεί μετά την ολοκλήρωση. Ανάγνωση/εγγραφή EffectRestartType. |

**Επιστρέφει:**
int


---

### getRewind {#getRewind}

| Όνομα | Περιγραφή |
| --- | --- |
| getRewind () | Αυτό το χαρακτηριστικό καθορίζει αν το εφέ θα επανέλθει στην αρχή όταν τελειώσει η εκτέλεση. Ανάγνωση/εγγραφή boolean. |

**Επιστρέφει:**
boolean


---

### getSpeed {#getSpeed}

| Όνομα | Περιγραφή |
| --- | --- |
| getSpeed () | Καθορίζει το ποσοστό με το οποίο θα επιταχυνθεί (ή θα μειωθεί) ο χρονισμός. Ανάγνωση/εγγραφή float. |

**Επιστρέφει:**
float


---

### getTriggerDelayTime {#getTriggerDelayTime}

| Όνομα | Περιγραφή |
| --- | --- |
| getTriggerDelayTime () | Περιγράφει τον χρόνο καθυστέρησης μετά το trigger. Ανάγνωση/εγγραφή float. |

**Επιστρέφει:**
float


---

### getTriggerType {#getTriggerType}

| Όνομα | Περιγραφή |
| --- | --- |
| getTriggerType () | Περιγράφει τον τύπο του trigger. Ανάγνωση/εγγραφή EffectTriggerType. |

**Επιστρέφει:**
int


---

### setAccelerate {#setAccelerate}

| Όνομα | Περιγραφή |
| --- | --- |
| setAccelerate (float) | Ορίζει το ποσοστό της διάρκειας για την επίδραση επιτάχυνσης της συμπεριφοράς. Ανάγνωση/εγγραφή float. |

**Επιστρέφει:**
void


---

### setAutoReverse {#setAutoReverse}

| Όνομα | Περιγραφή |
| --- | --- |
| setAutoReverse (boolean) | Ορίζει αν η κίνηση θα παιχτεί αυτόματα ανάποδα μετά την προώθηση. Ανάγνωση/εγγραφή boolean. |

**Επιστρέφει:**
void


---

### setDecelerate {#setDecelerate}

| Όνομα | Περιγραφή |
| --- | --- |
| setDecelerate (float) | Ορίζει το ποσοστό της διάρκειας για την επίδραση επιβράδυνσης της συμπεριφοράς. Ανάγνωση/εγγραφή float. |

**Επιστρέφει:**
void


---

### setDuration {#setDuration}

| Όνομα | Περιγραφή |
| --- | --- |
| setDuration (float) | Ορίζει τη διάρκεια της επίδρασης της κίνησης. Ανάγνωση/εγγραφή float. |

**Επιστρέφει:**
void


---

### setRepeatCount {#setRepeatCount}

| Όνομα | Περιγραφή |
| --- | --- |
| setRepeatCount (float) | Ορίζει τον αριθμό των επαναλήψεων του εφέ. Ανάγνωση/εγγραφή float. |

**Επιστρέφει:**
void


---

### setRepeatDuration {#setRepeatDuration}

| Όνομα | Περιγραφή |
| --- | --- |
| setRepeatDuration (float) | Ορίζει τη διάρκεια της επανάληψης του εφέ. Ανάγνωση/εγγραφή float. |

**Επιστρέφει:**
void


---

### setRepeatUntilEndSlide {#setRepeatUntilEndSlide}

| Όνομα | Περιγραφή |
| --- | --- |
| setRepeatUntilEndSlide (boolean) | Αυτό το χαρακτηριστικό καθορίζει αν το εφέ θα επαναληφθεί μέχρι το τέλος της διαφάνειας. Ανάγνωση/εγγραφή boolean. |

**Επιστρέφει:**
void


---

### setRepeatUntilNextClick {#setRepeatUntilNextClick}

| Όνομα | Περιγραφή |
| --- | --- |
| setRepeatUntilNextClick (boolean) | Αυτό το χαρακτηριστικό καθορίζει αν το εφέ θα επαναληφθεί μέχρι το επόμενο κλικ. Ανάγνωση/εγγραφή boolean. |

**Επιστρέφει:**
void


---

### setRestart {#setRestart}

| Όνομα | Περιγραφή |
| --- | --- |
| setRestart (int) | Καθορίζει αν ένα εφέ θα επανεκκινηθεί μετά την ολοκλήρωση. Ανάγνωση/εγγραφή EffectRestartType. |

**Επιστρέφει:**
void


---

### setRewind {#setRewind}

| Όνομα | Περιγραφή |
| --- | --- |
| setRewind (boolean) | Αυτό το χαρακτηριστικό καθορίζει αν το εφέ θα επανέλθει στην αρχή όταν τελειώσει η εκτέλεση. Ανάγνωση/εγγραφή boolean. |

**Επιστρέφει:**
void


---

### setSpeed {#setSpeed}

| Όνομα | Περιγραφή |
| --- | --- |
| setSpeed (float) | Καθορίζει το ποσοστό με το οποίο θα επιταχυνθεί (ή θα μειωθεί) ο χρονισμός. Ανάγνωση/εγγραφή float. |

**Επιστρέφει:**
void


---

### setTriggerDelayTime {#setTriggerDelayTime}

| Όνομα | Περιγραφή |
| --- | --- |
| setTriggerDelayTime (float) | Ορίζει τον χρόνο καθυστέρησης μετά το trigger. Ανάγνωση/εγγραφή float. |

**Επιστρέφει:**
void


---

### setTriggerType {#setTriggerType}

| Όνομα | Περιγραφή |
| --- | --- |
| setTriggerType (int) | Ορίζει τον τύπο του trigger. Ανάγνωση/εγγραφή EffectTriggerType. |

**Επιστρέφει:**
void


---