---
title: BasePortionFormat
second_title: Aspose.Sildes για PHP μέσω Αναφοράς API Java
description: 
type: docs

url: /el/aspose.slides/baseportionformat/
---
## BasePortionFormat κλάση

Κοινές ιδιότητες μορφοποίησης τμημάτων κειμένου.

### getAlternativeLanguageId {#getAlternativeLanguageId}

| Όνομα | Περιγραφή |
| --- | --- |
| getAlternativeLanguageId () | Επιστρέφει ή ορίζει το Id μιας εναλλακτικής γλώσσας. Ανάγνωση/Εγγραφή String. |

**Επιστρέφει:**
String

---

### getComplexScriptFont {#getComplexScriptFont}

| Όνομα | Περιγραφή |
| --- | --- |
| getComplexScriptFont () | Επιστρέφει ή ορίζει τις πληροφορίες γραμματοσειράς σύνθετου σεναρίου. Null σημαίνει ότι η γραμματοσειρά δεν είναι καθορισμένη και πρέπει να κληρονομηθεί από το Master. Ανάγνωση/Εγγραφή IFontData. |

**Επιστρέφει:**
[FontData](../fontdata)

---

### getEastAsianFont {#getEastAsianFont}

| Όνομα | Περιγραφή |
| --- | --- |
| getEastAsianFont () | Επιστρέφει ή ορίζει τις πληροφορίες γραμματοσειράς Ανατολικής Ασίας. Null σημαίνει ότι η γραμματοσειρά δεν είναι καθορισμένη και πρέπει να κληρονομηθεί από το Master. Ανάγνωση/Εγγραφή IFontData. |

**Επιστρέφει:**
[FontData](../fontdata)

---

### getEffectFormat {#getEffectFormat}

| Όνομα | Περιγραφή |
| --- | --- |
| getEffectFormat () | Επιστρέφει τις ιδιότητες EffectFormat του κειμένου. Δεν εφαρμόζεται κληρονόμηση. Μόνο για ανάγνωση IEffectFormat. |

**Επιστρέφει:**
[EffectFormat](../effectformat)

---

### getEscapement {#getEscapement}

| Όνομα | Περιγραφή |
| --- | --- |
| getEscapement () | Επιστρέφει ή ορίζει το κείμενο ανώτερου ή κατώτερου δείκτη. Τιμή από -100% (κάτω δείκτη) έως 100% (άνω δείκτη). Float.NaN σημαίνει ότι η τιμή δεν είναι καθορισμένη και πρέπει να κληρονομηθεί από το Master. Ανάγνωση/Εγγραφή float. |

**Επιστρέφει:**
float

---

### getFillFormat {#getFillFormat}

| Όνομα | Περιγραφή |
| --- | --- |
| getFillFormat () | Επιστρέφει τις ιδιότητες FillFormat του κειμένου. Δεν εφαρμόζεται κληρονόμηση. Μόνο για ανάγνωση IFillFormat. |

**Επιστρέφει:**
[FillFormat](../fillformat)

---

### getFontBold {#getFontBold}

| Όνομα | Περιγραφή |
| --- | --- |
| getFontBold () | Καθορίζει εάν η γραμματοσειρά είναι έντονη. Δεν εφαρμόζεται κληρονόμηση. Ανάγνωση/Εγγραφή NullableBool. |

**Επιστρέφει:**
byte

---

### getFontHeight {#getFontHeight}

| Όνομα | Περιγραφή |
| --- | --- |
| getFontHeight () | Επιστρέφει ή ορίζει το ύψος γραμματοσειράς ενός τμήματος. Float.NaN σημαίνει ότι το ύψος δεν είναι καθορισμένο και πρέπει να κληρονομηθεί από το Master. Ανάγνωση/Εγγραφή float. |

**Επιστρέφει:**
float

---

### getFontItalic {#getFontItalic}

| Όνομα | Περιγραφή |
| --- | --- |
| getFontItalic () | Καθορίζει εάν η γραμματοσειρά είναι πλάγια. Δεν εφαρμόζεται κληρονόμηση. Ανάγνωση/Εγγραφή NullableBool. |

**Επιστρέφει:**
byte

---

### getFontUnderline {#getFontUnderline}

| Όνομα | Περιγραφή |
| --- | --- |
| getFontUnderline () | Επιστρέφει ή ορίζει τον τύπο υπογράμμισης του κειμένου. Δεν εφαρμόζεται κληρονόμηση. Ανάγνωση/Εγγραφή TextUnderlineType. |

**Επιστρέφει:**
byte

---

### getHighlightColor {#getHighlightColor}

| Όνομα | Περιγραφή |
| --- | --- |
| getHighlightColor () | Επιστρέφει το χρώμα που χρησιμοποιείται για να υπογραμμίσει κείμενο. Δεν εφαρμόζεται κληρονόμηση. Μόνο για ανάγνωση IColorFormat. |

**Επιστρέφει:**
[ColorFormat](../colorformat)

---

### getKerningMinimalSize {#getKerningMinimalSize}

| Όνομα | Περιγραφή |
| --- | --- |
| getKerningMinimalSize () | Επιστρέφει ή ορίζει το ελάχιστο μέγεθος γραμματοσειράς για το οποίο η διαστημάτωση (kerning) πρέπει να ενεργοποιηθεί. Float.NaN σημαίνει ότι η τιμή δεν είναι καθορισμένη και πρέπει να κληρονομηθεί από το Master. Ανάγνωση/Εγγραφή float. |

**Επιστρέφει:**
float

---

### getKumimoji {#getKumimoji}

| Όνομα | Περιγραφή |
| --- | --- |
| getKumimoji () | Καθορίζει εάν οι αριθμοί θα αγνοούν τη διάταξη κάθετου κειμένου ειδικά για ανατολικές γλώσσες. Δεν εφαρμόζεται κληρονόμηση. Ανάγνωση/Εγγραφή NullableBool. |

**Επιστρέφει:**
byte

---

### getLanguageId {#getLanguageId}

| Όνομα | Περιγραφή |
| --- | --- |
| getLanguageId () | Επιστρέφει ή ορίζει το Id μιας γλώσσας ελέγχου. Χρησιμοποιείται για ορθογραφικό και γραμματικό έλεγχο. Ανάγνωση/Εγγραφή String. |

**Επιστρέφει:**
String

---

### getLatinFont {#getLatinFont}

| Όνομα | Περιγραφή |
| --- | --- |
| getLatinFont () | Επιστρέφει ή ορίζει τις πληροφορίες γραμματοσειράς Λατινικά. Null σημαίνει ότι η γραμματοσειρά δεν είναι καθορισμένη και πρέπει να κληρονομηθεί από το Master. Ανάγνωση/Εγγραφή IFontData. |

**Επιστρέφει:**
[FontData](../fontdata)

---

### getLineFormat {#getLineFormat}

| Όνομα | Περιγραφή |
| --- | --- |
| getLineFormat () | Επιστρέφει τις ιδιότητες LineFormat για το περίγραμμα κειμένου. Δεν εφαρμόζεται κληρονόμηση. Μόνο για ανάγνωση ILineFormat. |

**Επιστρέφει:**
[LineFormat](../lineformat)

---

### getNormaliseHeight {#getNormaliseHeight}

| Όνομα | Περιγραφή |
| --- | --- |
| getNormaliseHeight () | Καθορίζει εάν το ύψος του κειμένου πρέπει να κανονικοποιηθεί. Δεν εφαρμόζεται κληρονόμηση. Ανάγνωση/Εγγραφή NullableBool. |

**Επιστρέφει:**
byte

---

### getProofDisabled {#getProofDisabled}

| Όνομα | Περιγραφή |
| --- | --- |
| getProofDisabled () | Καθορίζει εάν το κείμενο δεν πρέπει να ελεγχθεί. Δεν εφαρμόζεται κληρονόμηση. Ανάγνωση/Εγγραφή NullableBool. |

**Επιστρέφει:**
byte

---

### getSpacing {#getSpacing}

| Όνομα | Περιγραφή |
| --- | --- |
| getSpacing () | Επιστρέφει ή ορίζει την αύξηση του διαστήματος μεταξύ χαρακτήρων. Float.NaN σημαίνει ότι η τιμή δεν είναι καθορισμένη και πρέπει να κληρονομηθεί από το Master. Ανάγνωση/Εγγραφή float. |

**Επιστρέφει:**
float

---

### getSpellCheck {#getSpellCheck}

| Όνομα | Περιγραφή |
| --- | --- |
| getSpellCheck () | Λαμβάνει ή ορίζει μια τιμή που υποδεικνύει εάν ο ορθογραφικός έλεγχος είναι ενεργοποιημένος για το τμήμα κειμένου. Όταν αυτή η ιδιότητα οριστεί ως false, οι ορθογραφικοί έλεγχοι για τα στοιχεία κειμένου καταστέλλονται. Όταν οριστεί ως true, ο ορθογραφικός έλεγχος επιτρέπεται. Η προεπιλεγμένη τιμή είναι false. |

**Επιστρέφει:**
boolean

---

### getStrikethroughType {#getStrikethroughType}

| Όνομα | Περιγραφή |
| --- | --- |
| getStrikethroughType () | Επιστρέφει ή ορίζει τον τύπο διαγράμμισης κειμένου. Δεν εφαρμόζεται κληρονόμηση. Ανάγνωση/Εγγραφή TextStrikethroughType. |

**Επιστρέφει:**
byte

---

### getSymbolFont {#getSymbolFont}

| Όνομα | Περιγραφή |
| --- | --- |
| getSymbolFont () | Επιστρέφει ή ορίζει τις πληροφορίες συμβολικής γραμματοσειράς. Null σημαίνει ότι η γραμματοσειρά δεν είναι καθορισμένη και πρέπει να κληρονομηθεί από το Master. Ανάγνωση/Εγγραφή IFontData. |

**Επιστρέφει:**
[FontData](../fontdata)

---

### getTextCapType {#getTextCapType}

| Όνομα | Περιγραφή |
| --- | --- |
| getTextCapType () | Επιστρέφει ή ορίζει τον τύπο κεφαλαίων του κειμένου. Δεν εφαρμόζεται κληρονόμηση. Ανάγνωση/Εγγραφή TextCapType. |

**Επιστρέφει:**
byte

---

### getUnderlineFillFormat {#getUnderlineFillFormat}

| Όνομα | Περιγραφή |
| --- | --- |
| getUnderlineFillFormat () | Επιστρέφει τις ιδιότητες FillFormat της γραμμής υπογράμμισης. Δεν εφαρμόζεται κληρονόμηση. Μόνο για ανάγνωση IFillFormat. |

**Επιστρέφει:**
[FillFormat](../fillformat)

---

### getUnderlineLineFormat {#getUnderlineLineFormat}

| Όνομα | Περιγραφή |
| --- | --- |
| getUnderlineLineFormat () | Επιστρέφει τις ιδιότητες LineFormat που χρησιμοποιούνται για το περίγραμμα της γραμμής υπογράμμισης. Δεν εφαρμόζεται κληρονόμηση. Μόνο για ανάγνωση ILineFormat. |

**Επιστρέφει:**
[LineFormat](../lineformat)

---

### getVersion {#getVersion}

| Όνομα | Περιγραφή |
| --- | --- |
| getVersion () |  |

**Επιστρέφει:**
long

---

### isHardUnderlineFill {#isHardUnderlineFill}

| Όνομα | Περιγραφή |
| --- | --- |
| isHardUnderlineFill () | Καθορίζει εάν το στυλ υπογράμμισης έχει δικές του ιδιότητες FillFormat ή κληρονομεί από τις ιδιότητες FillFormat του κειμένου. Ανάγνωση/Εγγραφή NullableBool. |

**Επιστρέφει:**
byte

---

### isHardUnderlineLine {#isHardUnderlineLine}

| Όνομα | Περιγραφή |
| --- | --- |
| isHardUnderlineLine () | Καθορίζει εάν το στυλ υπογράμμισης έχει δικές του ιδιότητες LineFormat ή κληρονομεί από τις ιδιότητες LineFormat του κειμένου. Ανάγνωση/Εγγραφή NullableBool. |

**Επιστρέφει:**
byte

---

### setAlternativeLanguageId {#setAlternativeLanguageId}

| Όνομα | Περιγραφή |
| --- | --- |
| setAlternativeLanguageId (String) | Επιστρέφει ή ορίζει το Id μιας εναλλακτικής γλώσσας. Ανάγνωση/Εγγραφή String. |

**Επιστρέφει:**
void

---

### setComplexScriptFont {#setComplexScriptFont}

| Όνομα | Περιγραφή |
| --- | --- |
| setComplexScriptFont ([FontData](../fontdata)) | Επιστρέφει ή ορίζει τις πληροφορίες γραμματοσειράς σύνθετου σεναρίου. Null σημαίνει ότι η γραμματοσειρά δεν είναι καθορισμένη και πρέπει να κληρονομηθεί από το Master. Ανάγνωση/Εγγραφή IFontData. |

**Επιστρέφει:**
void

---

### setEastAsianFont {#setEastAsianFont}

| Όνομα | Περιγραφή |
| --- | --- |
| setEastAsianFont ([FontData](../fontdata)) | Επιστρέφει ή ορίζει τις πληροφορίες γραμματοσειράς Ανατολικής Ασίας. Null σημαίνει ότι η γραμματοσειρά δεν είναι καθορισμένη και πρέπει να κληρονομηθεί από το Master. Ανάγνωση/Εγγραφή IFontData. |

**Επιστρέφει:**
void

---

### setEscapement {#setEscapement}

| Όνομα | Περιγραφή |
| --- | --- |
| setEscapement (float) | Επιστρέφει ή ορίζει το κείμενο ανώτερου ή κατώτερου δείκτη. Τιμή από -100% (κάτω δείκτη) έως 100% (άνω δείκτη). Float.NaN σημαίνει ότι η τιμή δεν είναι καθορισμένη και πρέπει να κληρονομηθεί από το Master. Ανάγνωση/Εγγραφή float. |

**Επιστρέφει:**
void

---

### setFontBold {#setFontBold}

| Όνομα | Περιγραφή |
| --- | --- |
| setFontBold (byte) | Καθορίζει εάν η γραμματοσειρά είναι έντονη. Δεν εφαρμόζεται κληρονόμηση. Ανάγνωση/Εγγραφή NullableBool. |

**Επιστρέφει:**
void

---

### setFontHeight {#setFontHeight}

| Όνομα | Περιγραφή |
| --- | --- |
| setFontHeight (float) | Επιστρέφει ή ορίζει το ύψος γραμματοσειράς ενός τμήματος. Float.NaN σημαίνει ότι το ύψος δεν είναι καθορισμένο και πρέπει να κληρονομηθεί από το Master. Ανάγνωση/Εγγραφή float. |

**Επιστρέφει:**
void

---

### setFontItalic {#setFontItalic}

| Όνομα | Περιγραφή |
| --- | --- |
| setFontItalic (byte) | Καθορίζει εάν η γραμματοσειρά είναι πλάγια. Δεν εφαρμόζεται κληρονόμηση. Ανάγνωση/Εγγραφή NullableBool. |

**Επιστρέφει:**
void

---

### setFontUnderline {#setFontUnderline}

| Όνομα | Περιγραφή |
| --- | --- |
| setFontUnderline (byte) | Επιστρέφει ή ορίζει τον τύπο υπογράμμισης του κειμένου. Δεν εφαρμόζεται κληρονόμηση. Ανάγνωση/Εγγραφή TextUnderlineType. |

**Επιστρέφει:**
void

---

### setHardUnderlineFill {#setHardUnderlineFill}

| Όνομα | Περιγραφή |
| --- | --- |
| setHardUnderlineFill (byte) | Καθορίζει εάν το στυλ υπογράμμισης έχει δικές του ιδιότητες FillFormat ή κληρονομεί από τις ιδιότητες FillFormat του κειμένου. Ανάγνωση/Εγγραφή NullableBool. |

**Επιστρέφει:**
void

---

### setHardUnderlineLine {#setHardUnderlineLine}

| Όνομα | Περιγραφή |
| --- | --- |
| setHardUnderlineLine (byte) | Καθορίζει εάν το στυλ υπογράμμισης έχει δικές του ιδιότητες LineFormat ή κληρονομεί από τις ιδιότητες LineFormat του κειμένου. Ανάγνωση/Εγγραφή NullableBool. |

**Επιστρέφει:**
void

---

### setKerningMinimalSize {#setKerningMinimalSize}

| Όνομα | Περιγραφή |
| --- | --- |
| setKerningMinimalSize (float) | Επιστρέφει ή ορίζει το ελάχιστο μέγεθος γραμματοσειράς για το οποίο η διαστημάτωση (kerning) πρέπει να ενεργοποιηθεί. Float.NaN σημαίνει ότι η τιμή δεν είναι καθορισμένη και πρέπει να κληρονομηθεί από το Master. Ανάγνωση/Εγγραφή float. |

**Επιστρέφει:**
void

---

### setKumimoji {#setKumimoji}

| Όνομα | Περιγραφή |
| --- | --- |
| setKumimoji (byte) | Καθορίζει εάν οι αριθμοί θα αγνοούν τη διάταξη κάθετου κειμένου ειδικά για ανατολικές γλώσσες. Δεν εφαρμόζεται κληρονόμηση. Ανάγνωση/Εγγραφή NullableBool. |

**Επιστρέφει:**
void

---

### setLanguageId {#setLanguageId}

| Όνομα | Περιγραφή |
| --- | --- |
| setLanguageId (String) | Επιστρέφει ή ορίζει το Id μιας γλώσσας ελέγχου. Χρησιμοποιείται για ορθογραφικό και γραμματικό έλεγχο. Ανάγνωση/Εγγραφή String. |

**Επιστρέφει:**
void

---

### setLatinFont {#setLatinFont}

| Όνομα | Περιγραφή |
| --- | --- |
| setLatinFont ([FontData](../fontdata)) | Επιστρέφει ή ορίζει τις πληροφορίες γραμματοσειράς Λατινικά. Null σημαίνει ότι η γραμματοσειρά δεν είναι καθορισμένη και πρέπει να κληρονομηθεί από το Master. Ανάγνωση/Εγγραφή IFontData. |

**Επιστρέφει:**
void

---

### setNormaliseHeight {#setNormaliseHeight}

| Όνομα | Περιγραφή |
| --- | --- |
| setNormaliseHeight (byte) | Καθορίζει εάν το ύψος του κειμένου πρέπει να κανονικοποιηθεί. Δεν εφαρμόζεται κληρονόμηση. Ανάγνωση/Εγγραφή NullableBool. |

**Επιστρέφει:**
void

---

### setProofDisabled {#setProofDisabled}

| Όνομα | Περιγραφή |
| --- | --- |
| setProofDisabled (byte) | Καθορίζει εάν το κείμενο δεν πρέπει να ελεγχθεί. Δεν εφαρμόζεται κληρονόμηση. Ανάγνωση/Εγγραφή NullableBool. |

**Επιστρέφει:**
void

---

### setSpacing {#setSpacing}

| Όνομα | Περιγραφή |
| --- | --- |
| setSpacing (float) | Επιστρέφει ή ορίζει την αύξηση του διαστήματος μεταξύ χαρακτήρων. Float.NaN σημαίνει ότι η τιμή δεν είναι καθορισμένη και πρέπει να κληρονομηθεί από το Master. Ανάγνωση/Εγγραφή float. |

**Επιστρέφει:**
void

---

### setSpellCheck {#setSpellCheck}

| Όνομα | Περιγραφή |
| --- | --- |
| setSpellCheck (boolean) | Λαμβάνει ή ορίζει μια τιμή που υποδεικνύει εάν ο ορθογραφικός έλεγχος είναι ενεργοποιημένος για το τμήμα κειμένου. Όταν αυτή η ιδιότητα οριστεί ως false, οι ορθογραφικοί έλεγχοι για τα στοιχεία κειμένου καταστέλλονται. Όταν οριστεί ως true, ο ορθογραφικός έλεγχος επιτρέπεται. Η προεπιλεγμένη τιμή είναι false. |

**Επιστρέφει:**
void

---

### setStrikethroughType {#setStrikethroughType}

| Όνομα | Περιγραφή |
| --- | --- |
| setStrikethroughType (byte) | Επιστρέφει ή ορίζει τον τύπο διαγράμμισης κειμένου. Δεν εφαρμόζεται κληρονόμηση. Ανάγνωση/Εγγραφή TextStrikethroughType. |

**Επιστρέφει:**
void

---

### setSymbolFont {#setSymbolFont}

| Όνομα | Περιγραφή |
| --- | --- |
| setSymbolFont ([FontData](../fontdata)) | Επιστρέφει ή ορίζει τις πληροφορίες συμβολικής γραμματοσειράς. Null σημαίνει ότι η γραμματοσειρά δεν είναι καθορισμένη και πρέπει να κληρονομηθεί από το Master. Ανάγνωση/Εγγραφή IFontData. |

**Επιστρέφει:**
void

---

### setTextCapType {#setTextCapType}

| Όνομα | Περιγραφή |
| --- | --- |
| setTextCapType (byte) | Επιστρέφει ή ορίζει τον τύπο κεφαλαίων του κειμένου. Δεν εφαρμόζεται κληρονόμηση. Ανάγνωση/Εγγραφή TextCapType. |

**Επιστρέφει:**
void