---
title: ChartTitle
second_title: Aspose.Sildes για PHP μέσω αναφοράς API Java
description: 
type: docs
url: /el/aspose.slides/charttitle/
---
## ChartTitle κλάση

Αναπαριστά τις ιδιότητες του τίτλου του γραφήματος.

### addTextFrameForOverriding {#addTextFrameForOverriding}

| Όνομα | Περιγραφή |
| --- | --- |
| addTextFrameForOverriding (String) | Αρχικοποιεί το TextFrameForOverriding με το κείμενο στην παράμετρο "text". Εάν το TextFrameForOverriding είναι ήδη αρχικοποιημένο, τότε απλώς αλλάζει το κείμενό του. |

**Παράμετροι:**

| Όνομα | Τύπος | Περιγραφή |
| --- | --- | --- |
| text | String | Κείμενο για ένα νέο TextFrameForOverriding. |

**Επιστρέφει:**
[TextFrame](../textframe)

---

### getActualHeight {#getActualHeight}

| Όνομα | Περιγραφή |
| --- | --- |
| getActualHeight () | Καθορίζει το πραγματικό ύψος του στοιχείου γραφήματος. Καλέστε τη μέθοδο IChart.validateChartLayout() πριν για να λάβετε τις πραγματικές τιμές. Ανάγνωση float. |

**Επιστρέφει:**
float

---

### getActualWidth {#getActualWidth}

| Όνομα | Περιγραφή |
| --- | --- |
| getActualWidth () | Καθορίζει το πραγματικό πλάτος του στοιχείου γραφήματος. Καλέστε τη μέθοδο IChart.validateChartLayout() πριν για να λάβετε τις πραγματικές τιμές. Ανάγνωση float. |

**Επιστρέφει:**
float

---

### getActualX {#getActualX}

| Όνομα | Περιγραφή |
| --- | --- |
| getActualX () | Καθορίζει την πραγματική θέση x (αριστερά) του στοιχείου γραφήματος σε σχέση με την αριστερή άνω γωνία του γραφήματος. Καλέστε τη μέθοδο IChart.validateChartLayout() πριν για να λάβετε τις πραγματικές τιμές. Ανάγνωση float. |

**Επιστρέφει:**
float

---

### getActualY {#getActualY}

| Όνομα | Περιγραφή |
| --- | --- |
| getActualY () | Καθορίζει το πραγματικό πάνω μέρος του στοιχείου γραφήματος σε σχέση με την αριστερή άνω γωνία του γραφήματος. Καλέστε τη μέθοδο IChart.validateChartLayout() πριν για να λάβετε τις πραγματικές τιμές. Ανάγνωση float. |

**Επιστρέφει:**
float

---

### getBottom {#getBottom}

| Όνομα | Περιγραφή |
| --- | --- |
| getBottom () | Κάτω. Ανάγνωση μόνο float. |

**Επιστρέφει:**
float

---

### getChart {#getChart}

| Όνομα | Περιγραφή |
| --- | --- |
| getChart () | Επιστρέφει το γονικό γράφημα. Ανάγνωση μόνο IChart. |

**Επιστρέφει:**
[Chart](../chart)

---

### getFormat {#getFormat}

| Όνομα | Περιγραφή |
| --- | --- |
| getFormat () | Επιστρέφει τα στιλ γεμίσματος, γραμμής και εφέ ενός τίτλου. Ανάγνωση μόνο IFormat. |

**Επιστρέφει:**
[Format](../format)

---

### getHeight {#getHeight}

| Όνομα | Περιγραφή |
| --- | --- |
| getHeight () | Επιστρέφει ή ορίζει το ύψος ενός τίτλου ως κλάσμα του ύψους του γραφήματος. Ανάγνωση/εγγραφή float. |

**Επιστρέφει:**
float

---

### getOverlay {#getOverlay}

| Όνομα | Περιγραφή |
| --- | --- |
| getOverlay () | Καθορίζει αν άλλα στοιχεία γραφήματος επιτρέπεται να επικαλύπτουν τον τίτλο. Ανάγνωση/εγγραφή boolean. |

**Επιστρέφει:**
boolean

---

### getPresentation {#getPresentation}

| Όνομα | Περιγραφή |
| --- | --- |
| getPresentation () | Επιστρέφει την γονική παρουσίαση ενός FillFormat. Ανάγνωση μόνο IPresentation. |

**Επιστρέφει:**
[Presentation](../presentation)

---

### getRight {#getRight}

| Όνομα | Περιγραφή |
| --- | --- |
| getRight () | Δεξιά. Ανάγνωση μόνο float. |

**Επιστρέφει:**
float

---

### getSlide {#getSlide}

| Όνομα | Περιγραφή |
| --- | --- |
| getSlide () | Επιστρέφει τη γονική διαφάνεια ενός FillFormat. Ανάγνωση μόνο BaseSlide. |

**Επιστρέφει:**
[MasterHandoutSlide](../masterhandoutslide), [BaseSlide](../baseslide), [LayoutSlide](../layoutslide), [Slide](../slide), [MasterSlide](../masterslide), [NotesSlide](../notesslide), [MasterNotesSlide](../masternotesslide)

---

### getTextFormat {#getTextFormat}

| Όνομα | Περιγραφή |
| --- | --- |
| getTextFormat () | Επιστρέφει τη μορφή κειμένου. Ανάγνωση μόνο IChartTextFormat. |

**Επιστρέφει:**
[ChartTextFormat](../charttextformat)

---

### getTextFrameForOverriding {#getTextFrameForOverriding}

| Όνομα | Περιγραφή |
| --- | --- |
| getTextFrameForOverriding () | Μπορεί να περιέχει πλούσιο μορφοποιημένο κείμενο. Εάν αυτή η ιδιότητα δεν είναι null, τότε αυτή η μορφοποιημένη τιμή κειμένου αντικαθιστά το αυτόματα δημιουργημένο κείμενο. Το αυτόματα δημιουργημένο κείμενο είναι μια έμμεση ιδιότητα της ετικέτας δεδομένων, της ετικέτας μονάδας προβολής του άξονα τιμών, του τίτλου άξονα, του τίτλου γραφήματος, της ετικέτας της γραμμής τάσης. Το αυτόματα δημιουργημένο κείμενο μορφοποιείται με την ιδιότητα IFormattedTextContainer.TextFormat. Ανάγνωση μόνο ITextFrame. |

**Επιστρέφει:**
[TextFrame](../textframe)

---

### getWidth {#getWidth}

| Όνομα | Περιγραφή |
| --- | --- |
| getWidth () | Επιστρέφει ή ορίζει το πλάτος ενός τίτλου ως κλάσμα του πλάτους του γραφήματος. Ανάγνωση/εγγραφή float. |

**Επιστρέφει:**
float

---

### getX {#getX}

| Όνομα | Περιγραφή |
| --- | --- |
| getX () | Επιστρέφει ή ορίζει τη συντεταγμένη x ενός τίτλου ως κλάσμα του πλάτους του γραφήματος. Ανάγνωση/εγγραφή float. |

**Επιστρέφει:**
float

---

### getY {#getY}

| Όνομα | Περιγραφή |
| --- | --- |
| getY () | Επιστρέφει ή ορίζει τη συντεταγμένη y ενός τίτλου ως κλάσμα του ύψους του γραφήματος. Ανάγνωση/εγγραφή float. |

**Επιστρέφει:**
float

---

### setHeight {#setHeight}

| Όνομα | Περιγραφή |
| --- | --- |
| setHeight (float) | Επιστρέφει ή ορίζει το ύψος ενός τίτλου ως κλάσμα του ύψους του γραφήματος. Ανάγνωση/εγγραφή float. |

**Επιστρέφει:**
void

---

### setOverlay {#setOverlay}

| Όνομα | Περιγραφή |
| --- | --- |
| setOverlay (boolean) | Καθορίζει αν άλλα στοιχεία γραφήματος επιτρέπεται να επικαλύψουν τον τίτλο. Ανάγνωση/εγγραφή boolean. |

**Επιστρέφει:**
void

---

### setWidth {#setWidth}

| Όνομα | Περιγραφή |
| --- | --- |
| setWidth (float) | Επιστρέφει ή ορίζει το πλάτος ενός τίτλου ως κλάσμα του πλάτους του γραφήματος. Ανάγνωση/εγγραφή float. |

**Επιστρέφει:**
void

---

### setX {#setX}

| Όνομα | Περιγραφή |
| --- | --- |
| setX (float) | Επιστρέφει ή ορίζει τη συντεταγμένη x ενός τίτλου ως κλάσμα του πλάτους του γραφήματος. Ανάγνωση/εγγραφή float. |

**Επιστρέφει:**
void

---

### setY {#setY}

| Όνομα | Περιγραφή |
| --- | --- |
| setY (float) | Επιστρέφει ή ορίζει τη συντεταγμένη y ενός τίτλου ως κλάσμα του ύψους του γραφήματος. Ανάγνωση/εγγραφή float. |

**Επιστρέφει:**
void

---