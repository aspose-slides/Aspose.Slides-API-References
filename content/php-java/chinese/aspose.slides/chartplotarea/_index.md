---
title: ChartPlotArea
second_title: Aspose.Sildes for PHP via Java API 参考
description: 
type: docs
url: /zh/aspose.slides/chartplotarea/
---
## ChartPlotArea 类

 表示绘制图表的矩形区域。
 
### getActualHeight {#getActualHeight}

| 名称 | 描述 |
| --- | --- |
| getActualHeight () | 指定图表元素的实际高度。调用方法 IChart.ValidateChartLayout() 以在此之前获取实际值。读取 float。 |

 **返回:**
float


---


### getActualWidth {#getActualWidth}

| 名称 | 描述 |
| --- | --- |
| getActualWidth () | 指定图表元素的实际宽度。调用方法 IChart.ValidateChartLayout() 以在此之前获取实际值。读取 float。 |

 **返回:**
float


---


### getActualX {#getActualX}

| 名称 | 描述 |
| --- | --- |
| getActualX () | 指定图表元素相对于图表左上角的实际 X 位置（左）。调用方法 IChart.ValidateChartLayout() 以在此之前获取实际值。读取 float。 |

 **返回:**
float


---


### getActualY {#getActualY}

| 名称 | 描述 |
| --- | --- |
| getActualY () | 指定图表元素相对于图表左上角的实际顶部位置。调用方法 IChart.ValidateChartLayout() 以在此之前获取实际值。读取 float。 |

 **返回:**
float


---


### getBottom {#getBottom}

| 名称 | 描述 |
| --- | --- |
| getBottom () | 底部。只读 float。 |

 **返回:**
float


---


### getChart {#getChart}

| 名称 | 描述 |
| --- | --- |
| getChart () | 图表。只读 IChart。 |

 **返回:**
[Chart](../chart)


---


### getFormat {#getFormat}

| 名称 | 描述 |
| --- | --- |
| getFormat () | 返回绘图区的格式。只读 IFormat。 |

 **返回:**
[Format](../format)


---


### getHeight {#getHeight}

| 名称 | 描述 |
| --- | --- |
| getHeight () | 返回或设置绘图区边界框的高度，以图表高度的比例表示（范围 0 到 1）。读取/写入 float。 |

 **返回:**
float


---


### getLayoutTargetType {#getLayoutTargetType}

| 名称 | 描述 |
| --- | --- |
| getLayoutTargetType () | 如果手动定义绘图区布局，此属性指定应按内部（不包括坐标轴和坐标轴标签）还是外部（包括坐标轴和坐标轴标签）进行布局。读取/写入 LayoutTargetType( #getLayoutTargetType/ #setLayoutTargetType(int)). |

 **返回:**
int


---


### getPresentation {#getPresentation}

| 名称 | 描述 |
| --- | --- |
| getPresentation () | 返回 FillFormat 的父级演示文稿。只读 IPresentation。 |

 **返回:**
[Presentation](../presentation)


---


### getRight {#getRight}

| 名称 | 描述 |
| --- | --- |
| getRight () | 右侧。只读 float。 |

 **返回:**
float


---


### getSlide {#getSlide}

| 名称 | 描述 |
| --- | --- |
| getSlide () | 返回 FillFormat 的父级幻灯片。只读 BaseSlide。 |

 **返回:**
[MasterHandoutSlide](../masterhandoutslide), [BaseSlide](../baseslide), [LayoutSlide](../layoutslide), [Slide](../slide), [MasterSlide](../masterslide), [NotesSlide](../notesslide), [MasterNotesSlide](../masternotesslide)


---


### getWidth {#getWidth}

| 名称 | 描述 |
| --- | --- |
| getWidth () | 返回或设置绘图区边界框的宽度，以图表宽度的比例表示（范围 0 到 1）。读取/写入 float。 |

 **返回:**
float


---


### getX {#getX}

| 名称 | 描述 |
| --- | --- |
| getX () | 返回或设置绘图区边界框左上角的 X 坐标，以图表宽度的比例表示（范围 0 到 1）。读取/写入 float。 |

 **返回:**
float


---


### getY {#getY}

| 名称 | 描述 |
| --- | --- |
| getY () | 返回或设置绘图区边界框左上角的 Y 坐标，以图表高度的比例表示（范围 0 到 1）。读取/写入 float。 |

 **返回:**
float


---


### isLocationAutocalculated {#isLocationAutocalculated}

| 名称 | 描述 |
| --- | --- |
| isLocationAutocalculated () | 定义如何计算位置：true � 自动计算；由 X、Y、Width、Height 属性定义。只读 boolean。 |

 **返回:**
boolean


---


### setHeight {#setHeight}

| 名称 | 描述 |
| --- | --- |
| setHeight (float) | 返回或设置绘图区边界框的高度，以图表高度的比例表示（范围 0 到 1）。读取/写入 float。 |

 **返回:**
void


---


### setLayoutTargetType {#setLayoutTargetType}

| 名称 | 描述 |
| --- | --- |
| setLayoutTargetType (int) | 如果手动定义绘图区布局，此属性指定应按内部（不包括坐标轴和坐标轴标签）还是外部（包括坐标轴和坐标轴标签）进行布局。读取/写入 LayoutTargetType( #getLayoutTargetType/ #setLayoutTargetType(int)). |

 **返回:**
void


---


### setWidth {#setWidth}

| 名称 | 描述 |
| --- | --- |
| setWidth (float) | 返回或设置绘图区边界框的宽度，以图表宽度的比例表示（范围 0 到 1）。读取/写入 float。 |

 **返回:**
void


---


### setX {#setX}

| 名称 | 描述 |
| --- | --- |
| setX (float) | 返回或设置绘图区边界框左上角的 X 坐标，以图表宽度的比例表示（范围 0 到 1）。读取/写入 float。 |

 **返回:**
void


---


### setY {#setY}

| 名称 | 描述 |
| --- | --- |
| setY (float) | 返回或设置绘图区边界框左上角的 Y 坐标，以图表高度的比例表示（范围 0 到 1）。读取/写入 float。 |

 **返回:**
void


---